---
phase: "164.5.1.1"
slug: "fanoutcohort-the-ledger-refresh-fan-out-admits-the-private-s"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-17"
---

# Phase 164.5.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `164.5.1.1-RESEARCH.md` § Validation Architecture, with the orchestrator's own
> re-measurements substituted where they disagreed with the researcher (noted inline).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | This repo's own `RED-UNDER` / `RED-UNDER-M` mutation-gate idiom over `supabase/tests/*.sql`, executed by `scripts/mutation-runner/run.mjs` against throwaway `pg-lane` PostgreSQL clusters (`scripts/pg-lane/run.sh`). Secondary: vitest for the floor ratchet. |
| **Config file** | `scripts/mutation-runner/run.mjs` — constants `FILES_FLOOR`, `ARMS_FLOOR`, `WAIVED_CEILING`, each read BY SYMBOL. Plus each gate `.sql` file's own `RED-UNDER-SETUP` apply list. |
| **Quick run command** | `node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_fanout.sql --arm "<arm-id>"` (narrowed diagnostic — ⛔ exits 2 BY DESIGN, so it can never be mistaken for a pass) |
| **Full suite command** | `node scripts/mutation-runner/run.mjs` |
| **Estimated runtime** | ~9-10 min for the full corpus; `sql-mutation`'s `timeout-minutes` is a declared CEILING of 20 and must not be raised |

⛔ **Floors, re-measured by the orchestrator 2026-09-17 — the researcher was right and `CLAUDE.md` is stale.**
`scripts/mutation-runner/run.mjs:1866` → `ARMS_FLOOR = 392`; `:877` → `FILES_FLOOR = 46`;
`:1980` → `WAIVED_CEILING = 0`. `CLAUDE.md`'s dated "2026-09-07" block still says `384/46`.
Read these BY SYMBOL at execution time; do not copy the numbers out of this table either.

---

## Sampling Rate

- **After every task commit:** narrowed `--file/--arm` diagnostic run for the arm just written.
- **After every plan wave:** full `node scripts/mutation-runner/run.mjs` corpus run.
- **Before any apply is requested:** full suite green **and** `mutation-runner-floors.test.ts` green
  **and** the three reviewers dispositioned.
- **Max feedback latency:** ~60 s for a narrowed arm; ~10 min for the corpus.

---

## Per-Task Verification Map

Task IDs are assigned by the planner; this table is seeded with the obligations and is completed
once PLAN.md exists.

| Obligation | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|
| A `private` strategy IS SELECTED by the fan-out | new `RED-UNDER` arm | `node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_fanout.sql --arm "<id>"` | ❌ Wave 0 | ⬜ pending |
| Cohort agreement: no status is stale in the view that the fan-out refuses | new `RED-UNDER` arm | same runner, new arm id | ❌ Wave 0 | ⬜ pending |
| Both new arms genuinely BITE | neuter → observe RED → restore → observe GREEN, per arm | three narrowed runs per arm | N/A (procedure) | ⬜ pending |
| `supabase/schema/functions/*.sql` snapshot regenerated after the body change | drift gate | `npm run schema:functions` then `git diff --exit-code supabase/schema/functions/` | ✅ exists | ⬜ pending |
| Foreign-candidate preconditions in BOTH gate files widened with the literal | reviewed edit | `grep -c "status IN" supabase/tests/test_ledger_refresh_fanout.sql supabase/tests/test_ledger_refresh_composite_arm.sql` | ✅ exists | ⬜ pending |
| `ARMS_FLOOR` / `FILES_FLOOR` raised to the newly measured values | vitest ratchet | `npx vitest run src/__tests__/mutation-runner-floors.test.ts` | ✅ exists | ⬜ pending |
| No existing arm regressed | full corpus | `node scripts/mutation-runner/run.mjs` | ✅ exists | ⬜ pending |
| PROD: the natural `25 * * * *` tick enqueues a non-empty candidate set | production reading | manual SQL read of `cron.job_run_details` + `compute_jobs` + `ledger_refresh_staleness` | N/A | ⬜ pending |
| `T-164.5.1-09-07` all-candidates-failed branch deliberately exercised | plan 08's recorded mechanism | see Manual-Only below | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] New `private`-admits arm in `supabase/tests/test_ledger_refresh_fanout.sql`.
- [ ] New cohort-agreement arm.
- ✅ **RESOLVED by the orchestrator before planning** — the researcher listed "confirm
  `strategies_status_check`'s literal list" as an open Wave 0 gap and as assumption A2. Measured
  at `supabase/schema/baseline.sql:10071`: `status` is **`text` with a CHECK constraint**, not an
  ENUM (`CREATE TYPE.*status` → **0** hits), admitting
  `{draft, pending_review, published, archived, private}`. ⭐ Consequence for the cohort-agreement
  arm: it must derive its status list from `pg_get_constraintdef('strategies_status_check')`, NOT
  from `pg_enum` and NOT from a hand-written list — a hand-written list would rot exactly the way
  the one this phase is repairing did.

---

## Manual-Only Verifications

| Behavior | Why Manual | Test Instructions |
|---|---|---|
| The natural PROD tick finds candidates | pg_cron runs under a registered identity and privileges; a hand invocation proves only that the FUNCTION finds candidates, which is not the thing that failed | After the PROD apply, wait for the next `25 * * * *` tick, then read `cron.job_run_details` for jobid 40, `compute_jobs` for `kind='derive_broker_dailies'`, and `ledger_refresh_staleness` against the BEFORE census (6 strategies, min 23 / avg 52.50 / max 141). ⛔ A green job is NOT success — `days_since_last_return` must go DOWN. |
| `T-164.5.1-09-07` — the all-candidates-failed branch | plan 08 already decided the mechanism and explicitly REJECTED fabricating a PROD failure state | Use plan 08's recorded mechanism (a mandated manual invocation whose interactive NOTICE/WARNING panel `cron.job_run_details.return_message` cannot surface, plus a code-read confirming the per-candidate handler neither leaks the advisory lock nor aborts the tick). ⛔ Do not invent a new mechanism. Sequencing: natural tick FIRST, manual invocation immediately after. |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 600 s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
