---
phase: 164.5.1.1-fanoutcohort-the-ledger-refresh-fan-out-admits-the-private-s
plan: 04
subsystem: db
tags: [pg_cron, fan-out, prod-apply, ledger-refresh, closure]
requires:
  - phase: 164.5.1.1-01..03
    provides: "the forward migration, gate arms P and Q, and the ARMS_FLOOR pins"
provides:
  - "the PROD apply of 20260917120000, its run ids and its post-apply body-hash comparison"
  - "the NATURAL tick measurement that closes FANOUT-COHORT-PRIVATE-01"
  - "the disposition of T-164.5.1-09-07 (substituted by arm R, not exercised)"
  - "the answer to plan 03's handed-over api_key-sharing question"
written_after_the_fact: true
---

# Phase 164.5.1.1 — Plan 04 Summary

⛔ **WRITTEN AFTER THE FACT, 2026-09-20, and labelled as such.** Plan 04's work happened on
2026-09-17 and was recorded in `164.5.1.1-PROD-SESSION.md` as it went; what never got written was
this summary, which plan 04's own `<output>` block required. `164.5.1.1-VERIFICATION.md` caught
that — along with three other loose ends — by executing plan 04's own verify commands rather than
reading the session file's prose. Nothing below is a new production read.

## What plan 04 actually did

**Task 1 — three reviewers, then the founder's go.** `rls-policy-auditor`: 0/0/0/0, no leak scopes.
`migration-reviewer`: 0/0/1 MEDIUM (confidence 8), recorded and carried, nothing blocking.
`silent-failure-hunter`: **2 must-fix + 1 HIGH + 1 MEDIUM on the first pass — "DO NOT request the
apply yet"**. All four were fixed and re-reviewed before anything was applied. ⭐ That reviewer
earning a refusal, and the refusal being honored rather than argued with, is the part of task 1
worth remembering. Founder approval `proceed`, 2026-09-17.

**Task 2 — the apply.** `Supabase Migrate` run **`35247369125`**, `completed/success`, with
`apply-test` (shared TEST) → `apply-test-verdict` → `apply` (PROD) all green; `Migration Drift
Check` on the PR, run **`35243584543`**, `completed/success`. ⚠️ Every id there is a run id or a
live PROD read — none of it is inferred from a green board, which is the standing rule in this
repo and the reason the ids are written down at all.

The post-apply verification compared a **sha256 of PROD's then-current function body**, so it
cannot be produced by anyone who has not actually read production.

**Task 3 — the NATURAL tick.** ⛔ A hand invocation was not used and would not have counted: it
proves the function finds candidates, only the scheduled tick proves `pg_cron` does. `cron.job` 40
fired on its own schedule — tick **`11231`**, 2026-09-17 17:25:00.229704Z, `succeeded`.

| what | before | after tick 11231 |
|---|---|---|
| `derive_broker_dailies` rows enqueued | 0 at every tick | **2**, on the tick's own millisecond |
| stale rows | 6 | **4** |
| min / avg / max days | 23 / 52.50 / 141 | 23 / **56.75** / 141 |

⭐ **The average RISING is the tell.** The two freshest rows left the set and the older ones remain;
a falling average would have been the suspicious reading. ⛔ `return_message: "1 row"` is not cited
as evidence anywhere — the function returns INTEGER and `SELECT f()` always returns one row, so
that field carries the row count and never the value. Three ticks before the apply read
`succeeded / "1 row"` while enqueuing zero. The verdict lives in `compute_jobs`.

## The four things plan 04 left open, and how each is disposed

**1. Plan 03's handed-over question — ANSWERED, and the answer is NO.** Does a `private` strategy
share an `api_key_id` with an in-set sibling? **Falsified, 5 of 5**, measured on PROD in Phase
164.5.1.2's own session (§ V), whose wording names this phase explicitly. Five live key-bearing
strategies have their trades never synced and no sibling rescues them. ⭐ Answering by citation is
the right close, not a dodge: the question needed a production read, one happened, and re-opening
PROD to re-ask a settled question would add risk and no information. ⚠️ The sixth `private`
strategy carries no key and ⛔ must not be counted against the constant's harm.

**2. `T-164.5.1-09-07` — SUPERSEDED, not exercised.** The all-candidates-failed branch was never
reached: the tick's two jobs both succeeded. What closed the risk is **mutation-gate arm R**
(`supabase/tests/test_ledger_refresh_fanout.sql`), merged as PR #810 during Phase **164.5.1's**
closure. ⭐ A permanent gate re-proves the branch every run where a live exercise proves it once,
so it is arguably stronger — but it is a **substitution, in a different phase, by a different
mechanism**, recorded at `medium` in `164.5.1-SECURITY.md`. The founder accepted it for this truth;
that acceptance lives as an `overrides:` entry in `164.5.1.1-VERIFICATION.md`, not as an edited-away
gap. ⛔ It must never be cited as compliance with plan 04's literal mechanism.

**3. `FANOUT-COHORT-PRIVATE-01` — CLOSED** in `TODOS.md` against the tick `11231` reading, with its
`T-164.5.1-09-07` clause closed citing arm R.

**4. ⚠️ The § B marker — DISCLOSED, still open.** Plan 04 task 3 required the marker query's output
as the first statement of its session; § B carries none, and no note can retroactively produce a
read that was not taken. **Measured: § B performed no writes** — it observed a tick that fired on
its own schedule, then read `cron.job_run_details`, `compute_jobs` and the staleness census — and
the marker rule is scoped to statements that write, with § A (the apply, which did write) carrying
its marker. So the miss is against the literal acceptance criterion with **zero safety
consequence**. ⛔ Deliberately NOT written as an override: the founder accepted arm R and has not
been asked about this one. Surfaced for a decision rather than graded away.

## Issues encountered

The honest one is the delay itself: plan 04's work was done and recorded on 2026-09-17, and the
paperwork that makes it checkable was not. `164.5.1.1-PROD-SESSION.md` § B said so in its own words
— *"the requirement is now reachable and still outstanding. Do not record it as met."* — and that
instruction was honored by the verifier rather than overridden. ⭐ A phase whose engineering is
sound can still fail verification on its record, and that is the gate working, not the gate being
pedantic.

## Threat flags

None beyond what plans 01-03 and `164.5.1-SECURITY.md` already register. This plan shipped no code:
it applied an already-reviewed migration and recorded measurements.
