---
phase: 164.5.1.1
plan: 03
subsystem: testing
tags: [postgres, plpgsql, sql-gates, pg-cron, fastapi, deferrals, backlog, measurement]

# Dependency graph
requires:
  - phase: 164.5.1.1-01
    provides: "The widened single-key conjunct, the regenerated canonical snapshot, and arm P — the one predicate this phase had a PROD measurement for"
  - phase: 164.5.1.1-02
    provides: "Arm Q and ARMS_FLOOR 394; and the byte-unchanged composite gate this plan is the first to touch"
  - phase: 110
    provides: "20260716130000_strategies_status_private.sql — the owner-only terminal status that three production predicates and one Python constant never learned about"
provides:
  - "An enumeration, from the canonical bodies rather than from migrations, of every production site of the two-value lifecycle literal, each with its real production invocation status"
  - "A measurement of ALLOWED_STRATEGY_STATUSES — one declaration, one reader — and of what a filtered-out strategy actually loses, including the cursor that advances anyway"
  - "A 46-line dated annotation in test_ledger_refresh_composite_arm.sql that stops the next reader widening a guard which must not move, with the literal byte-unchanged"
  - "Three TODOS bookings, each with an owner, a trigger and a destination phase that exists"
  - "Phase 164.5.1.2 FANOUTSIBLINGS in the ROADMAP — a goal that states the question and refuses to promise a widening"
  - "A measured CORRECTION of this phase's own RESEARCH.md: derive_broker_dailies never reads `trades`, so the constant's gap does not starve the ledger refresh"
affects: [164.5.1.1-04, 164.5.1.2, ledger-refresh, sql-gates, cron-sync]

actuals:
  tokens: 6273
  tasks: 2
  commits: 3
  plan_head_before: 99e9e237b0bdb009ff842ad4630a50aa82ba6670

tech-stack:
  added: []
  patterns:
    - "Enumerate a predicate from supabase/schema/functions/ (the replayed current bodies), never migration-by-migration — a later redefinition fools the second question into answering the first"
    - "A cron manifest answers 'is it scheduled', NEVER 'does it run' — a worker-invoked RPC is live with zero pg_cron rows, and reading the manifest alone inverts the verdict"
    - "Bound a source measurement to the next TOP-LEVEL statement, and name the bound in the claim; an arbitrary line window is how this phase's own RESEARCH.md got the composite conjunct wrong"
    - "A comment beside a literal is the only thing standing between a correct narrow precondition and a well-meaning widening — write the reason, not the value"

key-files:
  created: []
  modified:
    - supabase/tests/test_ledger_refresh_composite_arm.sql
    - TODOS.md
    - .planning/ROADMAP.md

key-decisions:
  - "The composite gate's precondition literal is ANNOTATED, never widened. It mirrors the conjunct of the function it guards, that conjunct is NOT widened by this phase, and widening the guard alone would abort the file on a committed row the guarded function would itself refuse — a false abort on a SHARED test project, surfacing in a different file."
  - "Task 1's first <automated> verify was INVERTED and was rewritten before it was believed: `set -e` plus `grep -c` (which exits 1 on zero matches) aborted the script before the assertion it exists to make, so it could only ever pass in the state it exists to refuse. Corrected with `|| true` and calibrated in BOTH directions against a widened mutant."
  - "`gsd-tools phase insert` was TRIED FIRST per the standing rule and produced a MEASURED clobber — wrong phase number (164.5.1.1.1, not 164.5.1.2), three unrelated roadmap regions reformatted, .planning/state.json rewritten, a stray phase directory created. Reverted from a cp byte backup and the entry hand-written scoped: one hunk, 30 added, 0 removed."
  - "The destination phase is ONE phase (164.5.1.2 FANOUTSIBLINGS) for all three findings, not three: the three sites are coupled — widening the poll-positions lifecycle conjunct is inert unless the trade-sync constant moves with it — and splitting them would hide that coupling behind three separate decisions."
  - "The plan's premise that the fan-out's job 'consumes already-synced trades' is FALSE and was recorded as a correction rather than propagated. The conclusion it was used to support (book the constant, do not change it) survives on different grounds."
  - "Task 2(d) was a measured NO-OP: this phase's ROADMAP entry already carried `**Plans:** 4 plans` and one line per plan. Nothing was rewritten to look like work."

patterns-established:
  - "Half-closing a backlog entry: record the shipped artefacts with a date, and state in the SAME breath that the entry's BLOCKS clause is not lifted and which plan lifts it"
  - "A booking that carries a correction to the research that produced it, so the next reader cannot re-derive the wrong framing from the document that is still on disk"

requirements-completed: [FANOUT-COHORT-PRIVATE-01]

coverage:
  - id: D1
    description: "Every production site of the two-value lifecycle literal is enumerated from the canonical bodies, with which one this phase widened and whether each other one is actually invoked in production"
    requirement: "FANOUT-COHORT-PRIVATE-01"
    verification:
      - kind: other
        ref: "grep -rn over supabase/schema/functions/ — three bodies carry it; cross-read against scripts/prod-prober/cron-manifest.json (captured 2026-09-17T08:05:56Z) and against every caller in analytics-service/ and src/"
        status: pass
    human_judgment: false
  - id: D2
    description: "The composite gate's foreign-candidate precondition is annotated and its status literal is byte-unchanged; the edit is purely additive"
    requirement: "FANOUT-COHORT-PRIVATE-01"
    verification:
      - kind: other
        ref: "corrected task-1 verify: narrow_literal=1 widened_literal=0 lines_removed=0, calibrated against a widened mutant which exits 1"
        status: pass
      - kind: other
        ref: "node scripts/lint-sql-gates.mjs --self-test (7 rules, red+green each) then node scripts/lint-sql-gates.mjs (73 files, 0 findings)"
        status: pass
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs — exit 0, arms 394/394/0, biting 394, test_ledger_refresh_composite_arm.sql sections 20 / judged 20 / annotated 20 / biting 20"
        status: pass
    human_judgment: false
  - id: D3
    description: "ALLOWED_STRATEGY_STATUSES is measured — every reader named, and what each does with a strategy that fails the filter — and is UNCHANGED, as is every file under analytics-service/"
    requirement: "FANOUT-COHORT-PRIVATE-01"
    verification:
      - kind: other
        ref: "task-1 verify 2: sites=5 (one declaration at cron.py:148, one reader at :658, three binary cache hits), analytics_service_untouched=yes"
        status: pass
    human_judgment: false
  - id: D4
    description: "Three findings are booked with an owner, a trigger and a destination phase that exists in the ROADMAP, and no deferral in the diff has a blank destination"
    verification:
      - kind: other
        ref: "task-2 verify 1 (against the plan base): roadmap_lines_removed=0 todos_added=153 todos_removed=0"
        status: pass
      - kind: other
        ref: "task-2 verify 2: blank_destinations=(none), added_destinations_naming_a_phase=4"
        status: pass
      - kind: unit
        ref: "src/__tests__/check-planning-hygiene.test.ts (42 tests)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The correction that run_derive_broker_dailies_job never reads `trades`, so the trade-sync constant's gap does not starve the ledger refresh"
    verification:
      - kind: other
        ref: "enumeration of .table(...) and .rpc(...) over the handler's WHOLE body, lines 2624-5956 bounded to the next top-level statement: strategy_analytics, csv_daily_returns, allocator_equity_derived, enqueue_compute_job; zero occurrences of `trades`"
        status: pass
    human_judgment: true
    rationale: "A static enumeration proves this handler issues no read of `trades` through PostgREST or its one RPC. It cannot prove the ledger refresh is unaffected END TO END — the follow-on compute_analytics_from_csv was not traced here, and only a production observation settles what a refreshed factsheet actually contains. The load-bearing use of this measurement is narrow (do not repeat RESEARCH.md's framing), and the wider claim is left to the destination phase."
  - id: D6
    description: "The 141-day composite is NOT reported as fixed, improved or addressed by this phase, in the gate comment, in TODOS, in the ROADMAP or in this SUMMARY (CTX-10)"
    verification: []
    human_judgment: true
    rationale: "A negative claim about wording across four artifacts. It is asserted in each of them by name, but nothing here mechanically proves the absence of a contrary statement elsewhere — plan 04 task 3 owns the automated polarity-aware check, per the ROADMAP's cross-cutting CTX-10 entry."

# Metrics
duration: 28 min
completed: 2026-09-17
status: complete
---

# Phase 164.5.1.1 Plan 03: Measure the siblings, annotate the guard, book the findings Summary

**Three production bodies carry the lifecycle literal and only one of them is both scheduled and widened; the composite gate gains 46 lines saying why its narrow precondition must not move; and the three findings left unchanged are booked with an owner, a trigger and a new Phase 164.5.1.2 to decide them in.**

## Performance

- **Duration:** 28 min
- **Started:** 2026-09-17T13:31:07Z
- **Completed:** 2026-09-17T13:59:51Z
- **Tasks:** 2
- **Files modified:** 3 (0 created, 3 modified)

## Accomplishments

- **The literal is enumerated from the right place, and the answer is not the one the cron manifest alone gives.** Three canonical bodies under `supabase/schema/functions/` carry it. One is widened and scheduled. One is **not scheduled and not called by anything**. One has **no pg_cron row and runs anyway**, daily, from the Railway worker — a verdict that inverts if you read only the manifest.
- **The composite gate is annotated, not widened, and the instruction to widen it is refused in the file itself.** The precondition mirrors the conjunct of the function it guards; that conjunct stays at two values; widening the guard alone is a false abort on a shared project, not a tighter gate. `RESEARCH.md`, `PATTERNS.md` and `VALIDATION.md` all instruct otherwise on a premise measured false, and the annotation says so by name so the instruction cannot be re-derived from three documents still sitting on disk.
- **The constant is measured, not changed — and a second consequence nobody had named was found.** `ALLOWED_STRATEGY_STATUSES` has one declaration and one reader. Beyond the obvious loss (no `sync_trades`, no `trades` rows, no recompute re-entry), **`last_sync_at` advances anyway** on a tick that stored nothing, because `any_trades_to_store` is false when every strategy on the key was filtered out.
- **The sibling poll-positions fan-out is excluded TWICE, and the second exclusion hides behind the first.** Widening its lifecycle conjunct alone would change nothing observable, because the same `WHERE` also demands a `sync_trades` job done in the last 30 days — which the very constant above denies. That coupling is the measured reason the destination phase must decide rather than widen, and it is invisible to anyone reading the literal.
- **Three bookings, one destination, none of them a note.** Each entry carries a measured statement, what a user observes today, an owner, a trigger stated so a human recognises it under pressure, and Phase 164.5.1.2 by name. `FANOUT-COHORT-PRIVATE-01` records its ledger half as shipped and says explicitly that its `BLOCKS` clause is **not** lifted.

## Task Commits

1. **Task 1: measure the siblings, measure the constant, annotate the guard** — `8782ff64` (docs)
2. **Task 2: book the findings with owner, trigger and destination phase** — `f37d40f4` (docs)
3. **Correction to task 2's own measurement** — `ed77800a` (fix) — the derive-handler claim re-taken over the whole body rather than an arbitrary line window.

**Plan metadata:** the `docs(164.5.1.1-03)` commit carrying this SUMMARY. `commits: 3` in the frontmatter is `git rev-list --count 99e9e237..HEAD` measured **at SUMMARY-write time**; the metadata commit is the fourth and is not counted by that instrument.

## Files Created/Modified

- `supabase/tests/test_ledger_refresh_composite_arm.sql` — **+46, -0.** A dated comment block immediately above the FOREIGN-CANDIDATE PRECONDITION's query. The literal is byte-unchanged. First change to this file since `ce7ac08a` (Phase 164.8.6).
- `TODOS.md` — **+153, -0** against the plan base. Three new entries, plus the half-close annotation on `FANOUT-COHORT-PRIVATE-01`. Append-and-annotate: no existing line rewritten.
- `.planning/ROADMAP.md` — **+30, -0**, one hunk. Phase 164.5.1.2 FANOUTSIBLINGS inserted between 164.5.1.1 and 164.5.2.

⚠️ **On the ROADMAP write, because the orchestrator owns that file:** this plan's `files_modified` names it, and what was written is **plan content** — a new phase entry the three bookings point at. **No progress bookkeeping was touched:** no checkbox was ticked, no progress block recomputed, no plan-count or status row changed, and `STATE.md` was not opened. Plan 02's checkbox in this phase's entry is still `[ ]` and was deliberately left for the orchestrator.

## The things a later reader cannot re-derive

### (a) Every production site of the two-value lifecycle literal

Measured from `supabase/schema/functions/` — the replayed current bodies. ⛔ A migration-by-migration search answers a different question and is fooled by a later redefinition.

| Canonical body | Literal | Widened by this phase? | Invoked in production? |
|---|---|---|---|
| `enqueue_ledger_refresh_for_strategies.sql:319` | **three-value** (post-widening) | **YES** — plan 01, migration `20260917120000` | **YES** — pg_cron jobid 40, `ledger_refresh_fanout`, `25 * * * *`, active |
| `enqueue_ledger_composite_refresh.sql:266` | two-value | NO | **NO — nothing calls it at all** |
| `enqueue_poll_positions_for_all_strategies.sql:44` | two-value | NO | **YES — but NOT via pg_cron** |

Sources, so each row is checkable rather than remembered:

- **Scheduling** is read from `scripts/prod-prober/cron-manifest.json`, captured `2026-09-17T08:05:56Z` against the PROD database marker. Occurrences of each function name in that file: `enqueue_ledger_refresh_for_strategies` **1**, `enqueue_ledger_composite_refresh` **0**, `enqueue_poll_positions_for_all_strategies` **0**.
- ⛔ **A zero there does NOT mean dormant, and this is the finding most easily got backwards.** `enqueue_poll_positions_for_all_strategies` is called once per UTC day by the Railway worker's `daily_enqueue_tick` (`analytics-service/main_worker.py:1110-1121`), under `pg_try_advisory_lock('daily_position_polling')`. Its conjunct is **live**.
- `enqueue_ledger_composite_refresh` has **no caller anywhere** — not pg_cron, not the worker, not a route (only two test/allowlist mentions). `20260825140000_ledger_refresh_composite_arm.sql:616` applied it dormant in its own `RAISE NOTICE`: *"no schedule registered"*.

**What a user observes today, per unwidened site:**

- **Composite fan-out:** nothing this conjunct does is observable, because the function never runs. Widening it alone would change nothing. The open question there is the absent schedule, not the literal.
- **Poll-positions fan-out:** every production strategy carries the owner-only terminal status, so the daily loop's candidate set should be empty and no `poll_positions` job should be created for any production strategy. ⚠️ **Stated as an INFERENCE from two measurements** (the predicate, and that the loop runs) — counting `poll_positions` rows needs PROD and is not a grep.

**One near-miss, recorded so it is not mistaken for a fourth site:** PROD cron `reconcile_dropped_enqueue_sweep` (jobid 36) filters `s.status <> 'archived'` — a **broader, different** predicate. It is not an instance of this literal and needs no decision here.

### (b) Every reader of `ALLOWED_STRATEGY_STATUSES`, and what a filtered-out strategy loses

`analytics-service/routers/cron.py:148` = `{"draft", "pending_review", "published"}`. **UNCHANGED by this plan** (`git diff --quiet -- analytics-service/` → `analytics_service_untouched=yes`).

| Site | What it is | What it does with a strategy that fails the filter |
|---|---|---|
| `cron.py:148` | the declaration, with a three-line comment naming the harm it prevents (syncing into an archived/deleted strategy overwrites its submission snapshot and flips its approval-gate verdict between Submit and Approve) | n/a — it is a real guard, not an oversight; it simply never learned about a status that shipped in July 2026 |
| `cron.py:658` | **the only reader**, inside the `POST /cron-sync` handler, filtering the embedded `strategies` rows down to `strategy_ids` | the strategy is **dropped from `strategy_ids`** → no `sync_trades` RPC (`:357-358`), no rows stored, and no `derive_broker_dailies` re-entry (`recompute_strategy_ids` at `:471` is built only from strategies that stored trades) |

The other three grep hits are `__pycache__` byte-code and a mypy cache — `sites=5` in the verify counts those; the source truth is **one declaration, one reader**.

⚠️ **A consequence not in the plan and not in RESEARCH.md.** When **every** strategy on a key is filtered out, `any_trades_to_store = bool(trades) and bool(strategy_ids)` is **false** (`:420`), so `should_advance_cursor` is **true** and `last_sync_at` is bumped on a tick that stored nothing. A new instance of the `last_sync_at` LIES class, on a path where "nothing to store" and "nothing eligible to store into" are indistinguishable downstream.

⛔ **And the finding RESEARCH.md surfaced is HALF WRONG — measured, not argued.** It frames the gap as starving the ledger refresh: *"a newly-admitted strategy may be refreshed against trade data that cron never synced"*. `run_derive_broker_dailies_job` (`analytics-service/services/job_worker.py:2624-5956`, bounded to the next top-level statement) runs its **own** venue crawl — realized-PnL ledger, funding, equity — and over its whole body touches three tables through PostgREST (`strategy_analytics`, `csv_daily_returns`, `allocator_equity_derived`) and issues one RPC (`enqueue_compute_job`). **`trades` appears zero times.** It is a chain tail *positionally* (`JOB_CHAIN_FOLLOW_ON`), not a consumer of the chain head's output. The gap costs the `trades` table and the daily recompute re-entry; it does **not** block what plan 01 shipped. The booking survives on the narrower grounds, which are still data-integrity grounds.

### (c) The destination phase

**Phase 164.5.1.2 FANOUTSIBLINGS**, inserted between 164.5.1.1 and 164.5.2. `gsd-tools phase next-decimal 164.5.1` independently returned `"next": "164.5.1.2"`.

Its goal **states the question and refuses to promise a widening** — two of the three sites may be correct as they stand, and the composite one interacts with a founder-locked exclusion that widening would reverse as a side effect. Its five success criteria are all decisions-with-evidence; criterion 2 requires that a poll-positions widening address the `sync_trades`-in-30-days conjunct **in the same decision or be refused as inert**, and criterion 3 requires that any composite widening reverse D-01/CTX-10 deliberately rather than by side effect.

### (d) The one question handed to plan 04's PROD session

**Does an owner-only strategy share an `api_key` with a sibling strategy whose status IS in `ALLOWED_STRATEGY_STATUSES`?** If it does, `/cron-sync` still syncs the key and trades still arrive via the sibling row, and the constant's gap is materially narrower than it looks. It cannot be settled from code — `strategies.api_key_id` is the many-to-one link and only production rows answer it. Plan 04 is already taking a production read; this is one extra query in that session.

### (e) The composite gate's annotation, in one sentence

46 comment lines directly above the precondition's query, recording: that the literal mirrors the guarded function's own conjunct; that this phase widened the single-key conjunct (`20260917120000`) and deliberately not this one; that widening the guard alone is a **false abort**, not a tighter gate, and lands on a shared project in an unrelated file (the D-05 hazard this file already refuses to create elsewhere); that RESEARCH/PATTERNS/VALIDATION instruct the opposite on a false premise; that the 141-day composite is **not** fixed here; and that Phase 164.5.1.2 is where the decision lives. ⛔ Written in **words** — the three-value form is deliberately not pasted into this file, because a literal added as prose is how a comment-counting grep elsewhere in this repo gets a wrong answer.

### (f) The corpus, re-run at the final bytes

```
coverage: files 46/73
lane-blocked: 0 file(s) …
lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires
  pending: 0 idiom file(s) without RED-UNDER —
arms: 394/394/0   (executed/annotated/waived)
biting: 394
lane-invocations: 394
  file test_ledger_refresh_composite_arm.sql: sections 20 / judged 20 / annotated 20 / waived 0 / biting 20
  file test_ledger_refresh_fanout.sql: sections 22 / judged 22 / annotated 22 / waived 0 / biting 22
per-arm lane time: mean 2.0s over 394 arm run(s)
✅ No defects. Every annotated arm bit its own arm first.
```

**`ARMS_FLOOR` stays 394 and none of the three coupled pins moved** — this plan adds no arm, so nothing in the arm count changed, exactly as plan 02's hand-off predicted. Run on a **clean tree with no concurrent commits**, per plan 02's dirty-checkout warning.

## Decisions Made

1. **Annotate, never widen.** The full reasoning is in (e) and in the file itself.
2. **One destination phase, not three.** The three findings are coupled — the poll-positions widening is inert without the constant — and three separate phases would hide the coupling behind three separate decisions.
3. **Hand-edit the ROADMAP after the tool clobbered.** Standing rule is try `gsd-tools` first; it was tried, it clobbered, the clobber was measured, and the intervention is scoped to exactly what it caused. See deviation 2.
4. **Record the RESEARCH.md correction rather than quietly rely on it.** The booking would have read as supported by a false mechanism; now it reads as supported by the true, narrower one.
5. **Task 2(d) was a measured no-op.** This phase's ROADMAP entry already carried `**Plans:** 4 plans` and one line per plan with its objective. Nothing was rewritten to manufacture a diff.
6. **`requirements.mark-complete` was NOT run.** `FANOUT-COHORT-PRIVATE-01` is a `TODOS.md` id, not a `REQUIREMENTS.md` id (this phase's ROADMAP entry says so explicitly), and its own text says the entry is only half-closed. `requirements-completed` copies the plan's frontmatter verbatim as the template requires; the ledger entry itself is annotated, not closed.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Task 1's first `<automated>` verify was INVERTED — it could only pass in the state it exists to refuse**

- **Found during:** Task 1
- **Issue:** The command is `set -e` followed by `x=$(grep -cE "<the widened literal>" "$f")` and then `[ "$x" -eq 0 ]`. `grep -c` **exits 1 when it matches nothing**, so on a correctly-unwidened file `set -e` kills the script at the assignment — before the first `echo` and before every assertion. Measured: the command produced **no output at all** and exited 1 while `narrow_literal=1`, `widened_literal=0`, `lines_removed=0` were all individually true. It would have exited 0 only if the widened literal were PRESENT, i.e. only in the failure state.
- **Fix:** `|| true` on both counting greps. This is a correction to the measurement, not to the artifact.
- **Files modified:** none.
- **Verification:** **calibrated in both directions.** Real file → `narrow_literal=1 widened_literal=0`, `lines_removed=0`, exit 0. A scratch mutant with the precondition widened to the three-value form → `narrow_literal=0 widened_literal=1`, exit 1. Both literal assertions bite.
- **Committed in:** n/a (no artifact change)

**2. [Rule 1 - Bug] `gsd-tools phase insert` clobbered the ROADMAP and `state.json`, and computed the wrong phase number**

- **Found during:** Task 2
- **Issue:** Run first per the standing "try the tool, intervene only on a measured clobber" rule. `phase insert 164.5.1.1 "probe"` returned `"phase_number": "164.5.1.1.1"` — a sub-sub-decimal under the phase, not the sibling `164.5.1.2` that the tool's own `phase next-decimal 164.5.1` reports as next. It also (a) inserted blank lines into **three unrelated roadmap regions** (after `**KEPT —**`, `**DROPPED —**` and `**What survives**`) and deleted one at `:1946`, (b) rewrote `.planning/state.json`'s `next` block and timestamp, and (c) created `.planning/phases/164.5.1.1.1-probe/`.
- **Fix:** `cp` byte backup restored over `.planning/ROADMAP.md`; `.planning/state.json` rewritten from `git show HEAD:` (⛔ not `git checkout --`, per this repo's destructive-edit rule); the stray directory removed. `git status --short` and `git diff --stat` both empty afterwards. The phase entry was then hand-written as a single scoped insertion.
- **Files modified:** `.planning/ROADMAP.md` (the intended insert only).
- **Verification:** `git diff -- .planning/ROADMAP.md` is **one hunk**, `30	0` — 30 added, 0 removed. No region outside the edit window is touched and no progress block is recomputed.
- **Committed in:** `f37d40f4`

**3. [Rule 1 - Bug] The plan's own premise about the fan-out's job is false**

- **Found during:** Task 1(b)
- **Issue:** The plan (following `RESEARCH.md`) instructs recording that *"the job this phase's fan-out enqueues consumes already-synced trades rather than pulling fresh ones — so a strategy this phase newly admits may be refreshed against trade data that nothing has been syncing."* `run_derive_broker_dailies_job` does its own venue crawl and never reads `trades`.
- **Fix:** The measurement was recorded as measured, the false premise named as false in both the TODOS entry and the new phase's goal, with a ⛔ against re-deriving the original framing from a document still on disk. The booking's conclusion is unchanged; its stated mechanism is.
- **Files modified:** `TODOS.md`, `.planning/ROADMAP.md`
- **Verification:** `.table(...)` / `.rpc(...)` enumeration over the handler's whole body — see (b).
- **Committed in:** `f37d40f4`, corrected in `ed77800a`

**4. [Rule 1 - Bug] My own correction was measured over an arbitrary line window**

- **Found during:** post-task-2 verification
- **Issue:** Deviation 3's claim was first taken over `job_worker.py:2624-3400` — a window, not the handler, which runs to `:5956`. It read "touches exactly ONE table" when the true count is three plus one RPC. ⛔ **Reading a partial line range is exactly the error that produced this phase's `RESEARCH.md` claim that the composite function has no status conjunct — the error plan 03 exists to reverse — and it was nearly repeated in the entry booking that reversal.**
- **Fix:** Re-measured bounded to the next top-level statement; both files now state the bound they measured, and both name the near-repeat so the lesson travels with the claim.
- **Files modified:** `TODOS.md`, `.planning/ROADMAP.md`
- **Verification:** `git diff --numstat 99e9e237..HEAD -- TODOS.md` → `153 0`; `roadmap_lines_removed=0`; `check-planning-hygiene` 42 passed.
- **Committed in:** `ed77800a`

---

**Total deviations:** 4 auto-fixed (4 × Rule 1 bug; 2 were corrections to a MEASUREMENT, 2 to text this plan itself wrote).
**Impact on plan:** No scope creep and no artifact outside `files_modified`. `ALLOWED_STRATEGY_STATUSES`, the sibling fan-out and the composite gate's literal are all untouched, as CTX-03, CTX-04 and this plan's prohibitions require. Deviations 1 and 4 are the load-bearing ones: a verify that could not fail, and a measurement taken over the wrong bound — both of the exact classes this phase exists to repair, both caught before they were believed.

## Issues Encountered

- **The ROADMAP was written by this plan, and the orchestrator owns that file.** Resolved by scope rather than by exception: plan content (a new phase entry) was written; progress bookkeeping (checkboxes, plan counts, progress block, `STATE.md`) was not touched at all. Flagged here and in the hand-back so the orchestrator's own sync finds the file in the state it expects.
- **`grep` in the interactive shell is `ugrep` and REFUSED the verify's own regex** (`exceeds complexity limits`, exit 2) while `/usr/bin/grep` under `bash -c` ran it and returned `3`. The plan's commands specify `bash -c`, so the authoritative reading was never in doubt — but a hand-run of the same command from the interactive shell would have produced a confusing error, not a verdict. Same family as this repo's dated `grep -a` rule.
- **The task-2 verifies measure the WORKING-TREE diff**, so they read `todos_added=7` once task 2 was committed and only the deviation-4 fix was outstanding. Re-run against the plan base (`git diff 99e9e237 -- …`) they read `153 / 0`, which is the quantity the acceptance criterion is about. Recorded because the next reader will hit the same thing.
- **No database was touched by any command run in this plan.** No `supabase db push`, no `db reset`, no `--project-ref`, no `--db-url`. The only database is the throwaway local PostgreSQL cluster the mutation runner builds and destroys per lane; everything else (`grep`, `node scripts/…`, `npx vitest`, `git`) is hermetic.

## Known Stubs

None. This plan renders nothing, defers no data source, skips no test, and left no `<verify>` unrun.

## Threat Flags

None. No new network endpoint, auth path, file access pattern or trust-boundary schema change. The plan's register is mitigated as written: **T-164.5.1.1-14** (a finding with no destination) by three bookings each naming Phase 164.5.1.2 on the field's own line, with the verify reporting `blank_destinations: (none)`; **T-164.5.1.1-15** (a future editor "completing" the widening) by the 46-line annotation beside the literal plus a purely-additive, literal-unchanged verify; **T-164.5.1.1-16** (this phase read as having fixed the 141-day composite) by an explicit refusal in the gate comment, in the TODOS entry, in the new phase's goal and in this SUMMARY — ⚠️ with the residual recorded honestly in coverage `D6`: the automated polarity-aware check is plan 04's; **T-164.5.1.1-17** (a handler clobbering the ROADMAP) by a `cp` byte backup taken before the tooling call, which is what made deviation 2 recoverable; **T-164.5.1.1-18** (identifiers in a public ledger) by inspection — no strategy id, account number, broker server name or absolute home path appears in the gate comment, the TODOS entries, the ROADMAP entry or this SUMMARY, and the composite is referred to only by its age; **T-164.5.1.1-SC** by adding no package-manager install of any kind.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Ready for plan 04**, which owns the one-way door. Two things it inherits from here:
  1. ⭐ **One extra query for its PROD session:** does an owner-only strategy share an `api_key` with a sibling whose status IS in `ALLOWED_STRATEGY_STATUSES`? See (d). It is the only open question in this plan's findings that code cannot answer, and plan 04 is already reading production.
  2. ⛔ **CTX-10 is asserted in four places by this plan but proven in none** — plan 04 task 3's automated polarity-aware check is what turns that into a measurement. Coverage `D6` records the residual rather than hiding it.
- **`supabase/tests/test_ledger_refresh_composite_arm.sql` is now MODIFIED by this phase** (+46 comment lines, literal byte-unchanged). Plan 01's task-2 and plan 02's task-1 verifies assert `git diff --quiet` on that file against HEAD; that assertion was satisfied when they ran and is intentionally no longer true, which is precisely why this plan was placed in wave 3. **Do not re-run those two verifies expecting `composite_gate_untouched=yes`.**
- **Phase 164.5.1.2 FANOUTSIBLINGS exists in the ROADMAP with 0 plans** and is the declared home for all three findings. It is *not* a prerequisite for plan 04.
- ⚠️ **EXPECTED-RED on the PR, inherited from plans 01 and 02 and unchanged:** gates carrying applied-ness probes go red on a PR that ADDS a migration, by construction (`[164.8-PUSH-RACE-VAC08]`). Confirm any such red against the ledger-frontier exemption rather than assuming it, and never clear it by widening a baseline.

## Self-Check: PASSED

Files claimed modified, checked on disk:

- `FOUND: supabase/tests/test_ledger_refresh_composite_arm.sql`
- `FOUND: TODOS.md`
- `FOUND: .planning/ROADMAP.md`

Commits claimed, checked in `git log --oneline --all`:

- `FOUND: 8782ff64`
- `FOUND: f37d40f4`
- `FOUND: ed77800a`

Plan-level `<verification>`, re-run at the final bytes:

- `node scripts/lint-sql-gates.mjs --self-test` → *"7 rules, red+green each"*; `node scripts/lint-sql-gates.mjs` → *"scanned 73 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s)"*
- `node scripts/mutation-runner/run.mjs` → **exit 0**, `arms: 394/394/0`, `biting: 394`, `lane-invocations: 394`, `✅ No defects`, tallies agree, zero waivers
- `npx vitest run src/__tests__/mutation-runner-floors.test.ts` → **81 passed**, unchanged
- `npx vitest run src/__tests__/check-planning-hygiene.test.ts` → **42 passed**
- `node scripts/verify-plan-anchors.mjs …/164.5.1.1-03-PLAN.md` → exit 0, *"claims: 12 checked, no stale claims"*
- `node scripts/lint-app-guc.mjs --self-test` → OK; `node scripts/lint-app-guc.mjs` → *"findings 0"* (5 annotated, unchanged)

Task-level `<acceptance_criteria>`, re-run at the final bytes:

- Task 1 verify 1 (corrected) → `narrow_literal=1 widened_literal=0`, `lines_removed=0`, exit 0
- Task 1 verify 2 → `sites=5`, `analytics_service_untouched=yes`, exit 0
- Task 2 verify 1 (against the plan base) → `phase_entries=81 roadmap_lines_removed=0 todos_added=153 todos_removed=0`, exit 0
- Task 2 verify 2 → `blank_destinations: (none)`, `added_destinations_naming_a_phase=4`, exit 0

---
*Phase: 164.5.1.1-fanoutcohort*
*Completed: 2026-09-17*
