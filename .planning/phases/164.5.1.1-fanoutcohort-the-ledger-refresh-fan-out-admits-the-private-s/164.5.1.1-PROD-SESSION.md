# Phase 164.5.1.1 FANOUTCOHORT — PROD session record

⛔ This file records what was MEASURED against production, and what a human
approved. A claim without a run id, a SHA or a pasted output does not belong
in it.

---

## § R — The three reviewers, and the founder's go (plan 04 task 1)

**Gate:** `checkpoint:decision`, `gate="blocking-human"`.
**Reviewed:** the SHIPPED diff, `git diff main...HEAD -- supabase scripts src` —
6 files, 1615 insertions, 143 deletions. Not the plans.
**Founder approval: 2026-09-17, `proceed`.** Explicitly re-confirmed after the
orchestrator declined to read a bare "Ok" as a one-way-door confirmation and
restated that `proceed` unlocks the ship and the apply REQUEST only.

### Verdicts

| reviewer | verdict |
|---|---|
| `rls-policy-auditor` | **0 CRITICAL / 0 HIGH / 0 MEDIUM / 0 LOW. Leak scopes: none.** |
| `migration-reviewer` | **0 / 0 / 1 MEDIUM (confidence 8) / 0.** Recorded and carried; nothing blocks the apply. |
| `silent-failure-hunter` | **2 must-fix + 1 HIGH + 1 MEDIUM** on the first pass — "DO NOT request the apply yet". All four fixed; re-review at `21104d57`: **"the apply may-be-requested gate CLEARS from my lens. No blockers, no new defects."** |

### What the blockers were, because this is the part worth keeping

**1. The migration shipped a function body whose comment asserted a guard the
same migration had DELETED.** The predecessor `20260911130000` carries check 7b
(`v_diag_needle`, declared `:1452`, checked `:1607`) over the
`GET DIAGNOSTICS v_found = ROW_COUNT` read. The re-base dropped it — zero
occurrences — while the body it ships still read *"Check 7b holds needle (5)
over the row-count read itself and REFUSES THE APPLY when the line is gone"*.
`CREATE OR REPLACE` puts that sentence live on PROD.

Consequence had it shipped: delete that read afterwards and `v_found` stays
NULL on a read that did not raise, the NULL-safe branch files an UNMEASURED
activation read as `flag_row_invisible_or_absent`, the instrument still writes
its row so arm M1 does not redden, the corpus exits 0 — and the prose beside
the code tells the next reader the apply would have refused. A guard removed
under a sentence claiming it is present is worse than one that was never
there: the reader stops looking.

Found while fixing it: the same re-base had also dropped the predecessor's
retired-GUC absence check. An `app.*` database setting cannot be set on this
platform (42501), so a body reading it is permanently dormant for a cause no
`cron_runs` row names. Both restored, as checks 7b and 7c. The needle is now
named by its SHAPE — "(5)" had been silently re-bound to `'draft'` in the new
file, and a number that travels between re-bases is a claim that rots.

**2. Three vitest gates were RED, and the orchestrator had not measured them.**
The `ARMS_FLOOR` 392 → 394 move landed in two files and was missed in two
others: `gate-family-meta.test.ts:883`, and `armsSeen` / `stepsSeen` /
`needles` in `mutation-annotation-parser.test.ts`. The orchestrator had
reported "the corpus is green" — the MUTATION CORPUS was; the vitest layer was
not, and was never looked at. That is this repo's documented trap: count the
checks, do not read "no red".

### Carried, not fixed

`FANOUT-COHORT-SYNC-CONSTANT-01` (migration-reviewer's MEDIUM) — the widened
cohort disagrees with `ALLOWED_STRATEGY_STATUSES` (`analytics-service/routers/cron.py:148`),
so strategies this fan-out now admits are ones the ccxt trade-sync cron
refuses. Widening the Python constant in the same ship would arm a second,
unmeasured production behaviour. Impact narrowed by plan 03's measurement:
`run_derive_broker_dailies_job` touches the `trades` table **zero** times
across its whole 3413-line body, so the gap costs the `trades` table and the
daily recompute re-entry — it does not starve what plan 01 ships.

Three Info items dropped rather than fixed, under the 2026-09-15 scoping
(fix-or-drop unless it pins data-integrity or user-facing behaviour): arm Q's
`[^']` class cannot resynchronise across a doubled quote (loud RED, never a
false green); "8" labels both a needle and a check (the parenthesised-vs-bare
convention already distinguishes them, and every message names its needle by
variable).

### Gate state at `04a55f7b`, both layers

```
mutation corpus   arms: 394/394/0   biting: 394   lane-invocations: 394
                  coverage: files 46/73   lane-blocked: 0   pg_cron AVAILABLE
                  ✅ No defects. Every annotated arm bit its own arm first.   exit 0
vitest            210 passed across gate-family-meta, mutation-annotation-parser,
                  mutation-runner-floors
lint-sql-gates    7 rules self-test; 73 files; 0 findings, 0 measure-fails
lint-app-guc      self-test OK; 0 findings
schema:functions  snapshot current, 120 functions; DRIFT-05 gate (a) agrees 120/120
plan-anchors      no stale claims
```

⚠️ The corpus was run by the ORCHESTRATOR, four times across the phase, not
taken from any executor's claim. One run was killed by memory pressure
mid-flight; the working tree was checked for a surviving mutant before the
retry (none — the runner mutates copies in slot directories) and three orphaned
runner directories were pruned.

---

## § A — The apply, 2026-09-17 (plan 04 task 2)

⛔ **Every id below is a RUN ID or a live PROD read. Nothing here is inferred from a green board.**

### The chain, in order

| step | run / job | conclusion |
|---|---|---|
| squash merge onto `main` | `b24853fa06d8` (PR #807) | merged 16:33:55Z by the founder |
| `Supabase Migrate` | run **`35247369125`** | `completed/success` |
| ├ `apply-test` (shared TEST) | | `success` |
| ├ `apply-test-verdict` | | `success` — a SKIPPED test apply would have been a named red check, not grey |
| ├ `plan` (`environment: Production`) | | `success`, after the founder's approval |
| ├ `apply` (PROD) | | `success` — step 4 `Push migrations to production` |
| └ `prod-credential-divergence-verdict` | | `success` |
| `Migration Drift Check` (on the PR) | run **`35243584543`** | `completed/success` |

**`apply-test`'s marker step is the TEST-not-PROD proof.** Step 8 `Which database am I on` passed, and
that step is written to ABORT on a NULL or non-TEST answer — its success is the measurement, not its
silence. The `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` trap did not fire: this migration's
`DO $verify$` reads CATALOGUE METADATA ONLY, and TEST holds PROD's catalogue.

**VAC-04 did not go blind, and this is the part worth reading.** It DETECTED the drift and said so:

```
⚠️ VAC-04: enqueue_ledger_refresh_for_strategies/0 DRIFTS
   1 body comparison(s) — 0 match, 1 acknowledged drift, 0 mismatch
   → no unacknowledged repo-vs-PROD body drift.
```

It passed only because the migration header carries `prod-body-ack: 9d08957172d399d4…`. That ack is a
sha256 OF PROD'S THEN-CURRENT BODY, so it cannot be written by anyone who has not actually read PROD —
it is a pin, not a mute button.

### What PROD itself says, read after the apply (not inferred from the workflow)

Marker first, every time: `⛔ PRODUCTION — real customer data.`

| read | value |
|---|---|
| `'private'` present in the live body | **true** |
| exact conjunct `AND s.status IN ('published', 'pending_review', 'private')` | **true** |
| live body md5 / length | `039c394efae4b6ada150a242c3fdbb31` / 27631 |
| `cron.job` 40 | schedule `25 * * * *`, `active = true` |

### The census is UNCHANGED, and that is the correct outcome

`ledger_refresh_staleness` at 17:05Z: **6 stale, min 23 / avg 52.50 / max 141 days, 1 composite** —
byte-identical to the P4 BEFORE census. The migration changes ELIGIBILITY, not state; nothing moves
until a tick runs. ⛔ Do not read this as "the fix did nothing".

**The diagnosis is confirmed at the row level:** grouping the six stale strategies by lifecycle status
returns exactly one group — `private`, n=6. Every stale strategy carried the status the old conjunct
refused, which is why the fan-out selected zero of six.

### Still open at the time of writing

Task 3 — the NATURAL `25 * * * *` tick (next: 17:25Z). ⛔ A hand invocation would prove only that the
function finds candidates; only the tick proves pg_cron does. Also pending: the separate post-apply
regeneration of `supabase/schema/baseline.sql`, which is what clears `sql-gate-lint` on `main`
(precedent: PR #778 merged red, PR #782 cleared it).

## § B — The NATURAL tick, 2026-09-17 17:25Z (plan 04 task 3)

⛔ **A hand invocation was NOT used and would not have counted.** It proves the function finds
candidates; only the scheduled tick proves pg_cron does. `cron.job` 40 fired on its own schedule.

### The tick

`runid 11231`, jobid 40, `2026-09-17 17:25:00.229704Z` → `17:25:00.427556Z`, status `succeeded`.

⚠️ **`return_message: "1 row"` — IDENTICAL to every pre-fix tick, and it proves nothing.** The
function returns INTEGER and `SELECT f()` always returns one row, so this field carries the ROW
COUNT, never the value. The three ticks before the apply (11207 / 11215 / 11223, 14:25 / 15:25 /
16:25Z) all read `succeeded` + `1 row` while enqueuing ZERO. The verdict lives in `compute_jobs`.

### What was actually enqueued — the measurement that decides

| strategy | kind | status | attempts | created |
|---|---|---|---|---|
| `8d382aaf…` | `derive_broker_dailies` | `done` | 1 | 17:25:00.229743Z |
| `4eab92b0…` | `derive_broker_dailies` | `done` | 1 | 17:25:00.229743Z |

Created on the tick's own millisecond, both completed, no `last_error`. **Before the apply this
number was 0 at every tick for the same six strategies.**

### The census MOVED, and the direction of the average is the tell

| | BEFORE (P4) | after tick 11231 |
|---|---|---|
| stale rows | 6 | **4** |
| min / avg / max days | 23 / 52.50 / 141 | 23 / **56.75** / 141 |
| stale composites | 1 | 1 |

The average RISES because the two freshest rows left the set and the older ones remain. A falling
average here would have been the suspicious reading, not this one.

### All four ROADMAP projections — checked, not assumed

1. **Venue cap `venue_rank <= 2`** — EXACTLY 2 enqueued on one tick. ✅
2. **4 of 5 mt5 strategies become candidates** — 2 refreshed this tick, 2 (`298762ad…` 35 d,
   `401d5f31…` 28 d) remain eligible for following ticks, 1 excluded. ✅
3. **`87bb2086…` stays excluded, and FOR THE PROJECTED REASON** — read at the row level:
   `disconnected_at IS NOT NULL` = **true**, while the other two mt5 candidates read **false**.
   Checking that it is merely absent would have confirmed the exclusion without confirming its
   CAUSE; the cause is what the projection claimed. ✅
4. **Deribit composite `081f2912…`, 141 days** — STILL STALE, still excluded, by D-01 and BY DESIGN.
   ⛔ It is NOT fixed by this phase and must never be reported as such. ✅

### `T-164.5.1-09-07` — unblocked, NOT yet exercised

The all-candidates-failed branch was blocked only because the candidate set was empty for an
unrelated reason. It is no longer empty. This tick did not exercise that branch — both jobs
SUCCEEDED — so the requirement is now reachable and still outstanding. Do not record it as met.

---

## § C — closing the four items plan 04 task 3 left open (2026-09-20)

⛔ **Written AFTER the fact and labelled as such.** Nothing below is a new production read. Three
items close by citation to measurements that already exist; the fourth is a disclosure, not a
closure. `164.5.1.1-VERIFICATION.md` found all four independently rather than taking § B's own
honest self-report at face value, and § B's instruction — *"Do not record it as met"* — is honored
here, not overridden.

### C-1 — plan 03's handed-over question: ANSWERED, and the answer is NO

**The question.** Does an owner-only (`private`) strategy share an `api_key_id` with a sibling
strategy whose status IS in `ALLOWED_STRATEGY_STATUSES`? If one did, the sibling would sync the key
anyway and the cohort defect would be narrower than it looked.

**The answer, measured on PROD in Phase 164.5.1.2's own session (§ V), not here:** the hypothesis
is **FALSIFIED, 5 of 5.** Its wording names this phase explicitly — *"the one open question the
ROADMAP handed to Phase 164.5.1.1 and which was never answered there"*. Five real, live,
key-bearing production strategies have their trades never synced, and **no sibling row rescues
them**.

⭐ **Why answering by citation is the right close and not a dodge.** The question needed a
production read; a production read happened; it was the sibling phase's, taken while its own
session was open, and it answers this question at full strength. Re-opening PROD to re-ask a
settled question would add risk and no information. ⚠️ The sixth `private` strategy carries no key
at all and ⛔ must not be counted against the constant's harm — including it would overstate what a
widening delivers.

### C-2 — `T-164.5.1-09-07`: SUPERSEDED by arm R, not exercised

Plan 04 wanted the fan-out's all-candidates-failed branch exercised, or read. **Neither happened,
and this record does not pretend otherwise.** The natural tick (§ B) did not enter that branch —
both jobs SUCCEEDED — so the branch stayed unreached.

What closed the underlying risk instead: **mutation-gate arm R** in
`supabase/tests/test_ledger_refresh_fanout.sql`, merged as PR #810 during Phase 164.5.1's own
closure. ⭐ **A permanent gate is stronger than a one-off exercise** — it re-proves the branch on
every run, where a single live exercise proves it once and then decays. But it is a **substitution,
not compliance**, it shipped in a different phase, and `164.5.1-SECURITY.md` already recorded it as
a disclosed substitution at `medium` — below the blocking threshold.

⛔ **The founder accepted arm R for this truth**, and that acceptance is recorded as an `overrides:`
entry in `164.5.1.1-VERIFICATION.md` rather than by editing the gap away.

### C-3 — ⚠️ the § B marker: a DISCLOSURE, and it stays open

Plan 04 task 3 required the database marker query's output as the first statement of its session.
**§ B does not carry one, and no note can retroactively produce a read that was not taken.**

**What is true, measured rather than asserted: § B performed NO WRITES.** It observed a `pg_cron`
tick that fired on its own schedule and then read `cron.job_run_details`, `compute_jobs` and the
staleness census. The marker rule's own scope in `CLAUDE.md` is *"before any statement that
writes"*, and § A — the session that **did** write, the apply — carries its marker.

So the miss is against **plan 04's literal acceptance criterion**, and its safety consequence is
**zero**. ⛔ That is an argument about severity, not a closure, and it is deliberately NOT written
as an `overrides:` entry: the founder accepted arm R for C-2 and has not been asked about this one.
It is surfaced for a decision rather than quietly graded.

### C-4 — `FANOUT-COHORT-PRIVATE-01`: closed in `TODOS.md`, against the real mechanism

The ledger arm was half-closed by plans 01-03; the remaining half required the NATURAL tick to
select a non-empty candidate set. **Tick `11231` did exactly that** (§ B): two
`derive_broker_dailies` rows created on the tick's own millisecond, stale census 6 → 4. The entry
is closed on that reading, and its `T-164.5.1-09-07` clause is closed citing arm R — ⛔ never by
claiming plan 04's original literal mechanism ran.
