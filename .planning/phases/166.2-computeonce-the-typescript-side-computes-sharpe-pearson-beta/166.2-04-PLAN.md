---
phase: 166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta
plan: 04
type: execute
wave: 2
depends_on: ["166.2-01"]
files_modified:
  - src/lib/factsheet/compute.ts
  - src/lib/factsheet/bootstrap.ts
  - src/lib/factsheet/og-metrics.ts
  - src/lib/factsheet/return-stats.sites.test.ts
  - src/lib/factsheet/og-metrics.test.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The factsheet headline family computes dispersion and Sharpe through return-stats: compute.ts carries no private Sharpe and keeps a private mean or pstdev only where replacing it would move an existing test's value, recorded with the reason; bootstrap.ts and og-metrics.ts carry no Sharpe of their own (D-17)"
    - "On a compounding-NAV constant yield, compute's sharpe, skew, kurt and ann_vol, and bootstrapCI's Sharpe fields, are EXACTLY what an all-zero series gives today (0, 0, 0, 0 and the all-zero CI) (D-07, D-17)"
    - "compute on a series holding a NaN still reports a Sharpe of 0, as today: sharpe() returns null on a non-finite input and compute answers null as 0, pinned by the test 'T13 non-finite input keeps today's sharpe' (D-17 I3)"
    - "buildComparatorBlock's vol-matched curve for a constant-yield benchmark equals its unscaled benchmark curve (scale 1) with comparator-block.ts UNEDITED, because compute's ann_vol is 0 at the source (D-07, D-17)"
    - "computeOgHeadline computes its Sharpe with return-stats' sharpe (population, the asset-class periods), answered as NaN (its \"hide\" value) on null, so on a compounding-NAV constant yield the OG card's computed Sharpe is NaN exactly as for an all-zero series; this is the computed arm only, the one Phase 169 D-25 assigns to 166.1 plan 06, and the persisted-value read is Phase 169 plan 04's (D-17, D-19)"
    - "A cent-rounded 1% APY NAV still gives a finite Sharpe at the sites with that control, so widening the floor goes red (D-07)"
    - "Every T13, T14 (computed arm), T15 and T18 test was written first and observed RED on the unedited site, every edited site was neutered, observed RED and restored from a byte backup proven with cmp, and the existing factsheet snapshot and golden tests pass without an update"
    - "comparator-block.ts, types.ts and fetch-and-build-payload.ts are not edited by this plan (Phase 169 owns them, CONTEXT D-17, D-19)"
    - "og-metrics.test.ts changes in exactly two assertions and nothing else (CONTEXT D-19, amended 2026-09-26): the Sharpe side assertion of \"CAGR hidden (NaN) for a dense sub-year series (300 trading days < 0.95y)\" and of \"single / duplicate / unsorted dates never produce Infinity\" each become `expect(Number.isNaN(...sharpe)).toBe(true)` with a one-line D-07 why; both tests keep their input, and no other line of the file moves"
  artifacts:
    - path: src/lib/factsheet/return-stats.sites.test.ts
      provides: "red tests for T13, T14's computed arm, T15 and T18"
  key_links:
    - from: src/lib/factsheet/compute.ts
      to: src/lib/return-stats.ts
      via: "dispersion(rets, 0) supplies m and a residue-snapped s; sharpe() supplies the Sharpe"
      pattern: "@/lib/return-stats"
    - from: src/lib/factsheet/og-metrics.ts
      to: src/lib/return-stats.ts
      via: "computeOgHeadline's computed Sharpe is sharpe(values, population, periodsPerYear) ?? NaN"
      pattern: "@/lib/return-stats"
    - from: src/lib/factsheet/comparator-block.ts
      to: src/lib/factsheet/compute.ts
      via: "vmScale's existing ann_vol > 0 guard reads the snapped ann_vol (no edit to comparator-block.ts)"
      pattern: "ann_vol"
---

<objective>
Third slice of D-17 (compute once), first half (D-20 W2 split): the factsheet HEADLINE family (CONTEXT
D-17 rows T13, T15, T14's computed arm per D-19, and T18 proven at its source) stops carrying its own
mean, standard deviation and Sharpe and calls plan 166.2-01's `src/lib/return-stats.ts`.

Purpose: the factsheet builds its headline numbers in TS for the strategy, allocator-portfolio and
scenario payloads and the levered browser re-derive. On a constant-yield strategy each one shows a
Sharpe of about 1e13, a fabricated skew and kurtosis, or a vol-matched comparator curve scaled by 1e13.
Row T14 (the OG card's Sharpe) is split (CONTEXT D-19): Phase 169 plan 04 makes the card read the
PERSISTED Sharpe when the row is rankable; the Sharpe `computeOgHeadline` still COMPUTES is this
plan's. Row T18 (the comparator vol-match) is fixed at its source, `compute`'s `ann_vol`, so
`comparator-block.ts` stays untouched. Plan 166.2-05 takes the benchmark, rolling and correlation rows.
Output: three edited files and one test file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-01-SUMMARY.md` (the final `return-stats.ts` API at
its top, and the fixture's exports). This plan does NOT edit `src/lib/return-stats.ts` or plan 166.2-01's test
files. Plans 166.2-02, 166.2-03 and 166.2-05 run in the same wave and share no file with this plan. An API gap is a
STOP-and-report.

**Environment, drill rule, the D-07 invariant in TS and the never-edit-a-golden rule** are exactly plan
166.2-01's: MAIN checkout's `node_modules` linked only for the run and removed before every commit; exit 127 is
a FAIL; `tsc` in the background with a 600 s watchdog; byte backups under a `166.2-04` scratchpad name,
neuter, RED, `cp`, `cmp`; each test computes the site's output on the constant yield AND on an all-zero
series and asserts the field identical (`Object.is` for NaN). Never `git checkout --`, `git restore`,
`git stash`. Never quote a retired expression verbatim in an updated code comment: this plan's acceptance greps count comment lines. Never update a vitest snapshot to make a test pass: a snapshot diff is a finding to report.
The snapshot rounds every finite number to 12 significant digits, and `return-stats`' Sharpe arithmetic
is `compute`'s own order, so it must not move.

**Ownership and couplings (CONTEXT D-17, D-19).** Phase 169 owns `comparator-block.ts`, `types.ts`,
`composite-read-path.ts`, `fetch-and-build-payload.ts`, `og-metrics.test.ts` and the OG route; this plan
edits none of them, except the two named Sharpe side assertions in `og-metrics.test.ts` (D-19 amendment,
2026-09-26, orchestrator decision: the owner of the D-07 fix owns the two assertions that pin the defect;
Phase 169 is still in planning and may not start execution, so waiting for it would block this phase). Two files are shared with Phase 169 in DIFFERENT hunks: `og-metrics.ts` (169 plan 04
adds an optional persisted-scalars argument and a branch that uses the persisted values when rankable;
this plan replaces only the lines that compute the variance, the sd and the Sharpe inside the
`values.length >= 30` block, keeps the 30-observation gate, and adds one import line) and `compute.ts`
(169 plan 06 edits the return windows; this plan edits only the lines that compute `m`, `s`, `ann_vol`,
`sharpe`, `skew`, `kurt` and the private helpers they used). Whichever phase merges second rebases and
re-runs both phases' tests; plan 166.2-06 Task 2 does that for this phase. Phase 169 plan 14 (ZOOMKPIS) calls
`compute()` on a slice and inherits this plan's edit with no file overlap (D-19 correction).

**Which ddof.** Population (`ddof: 0`) in `compute.ts` and `og-metrics.ts`; read `bootstrap.ts`
`headlineStats` and keep whatever it divides by (measured 2026-09-26: `varSum / n`, population).

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/factsheet/compute.ts compute -->
<!-- verify-symbol: src/lib/factsheet/compute.ts pstdev -->
<!-- verify-symbol: src/lib/factsheet/bootstrap.ts headlineStats -->
<!-- verify-symbol: src/lib/factsheet/bootstrap.ts bootstrapCI -->
<!-- verify-symbol: src/lib/factsheet/comparator-block.ts buildComparatorBlock -->
<!-- verify-symbol: src/lib/factsheet/og-metrics.ts computeOgHeadline -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): compute's Sharpe, skew, kurtosis and ann_vol on return-stats (T13), proven end-to-end through the comparator vol-match (T18) with comparator-block.ts unedited, red first</name>
  <files>src/lib/factsheet/compute.ts, src/lib/factsheet/return-stats.sites.test.ts</files>
  <read_first>
    - src/lib/factsheet/compute.ts - `compute` (`m`, `s`, `annVol`, `sharpe`, `skew`, `kurt`; the Sortino `ddDev` is NOT in the class), the private `mean` and `pstdev` and every other use of them in the file
    - src/lib/factsheet/comparator-block.ts - `buildComparatorBlock` and how `vmScale` reads `benchSummary.ann_vol` (READ ONLY)
    - src/lib/factsheet/compute.test.ts, compute.metrics.test.ts, compute.dd.test.ts, and src/__tests__/phase-52-frozen-spine-guards.test.ts (compute.ts is carved out of the frozen islands for reviewed math edits; confirm it stays carved out)
    - src/lib/factsheet/build-payload.test.ts - its snapshot rounding and whether a payload fixture builder exists
  </read_first>
  <behavior>
    - T13: `compute(navConstantYield(y), dates, ...)` gives `sharpe`, `skew`, `kurt` and `ann_vol` identical to the all-zero series' (0, 0, 0, 0) for every yield id; the cent-rounded 1% APY series gives a finite non-zero Sharpe; on a noisy series the Sharpe equals the compute-order expression built in the test from `mean` and `stdDev`, and the existing compute tests pass unedited
    - T13 non-finite input keeps today's sharpe: `compute` on a noisy series with one element replaced by NaN reports `sharpe` 0, computed in the test against today's rule (`s > 0` is false on a NaN `s`), so a change to how `compute` maps `sharpe()`'s null goes red (D-17 I3)
    - T18 (end-to-end, no edit to comparator-block.ts): `buildComparatorBlock` with a constant-yield benchmark returns a vol-matched curve equal to the block's unscaled benchmark curve, which is what an all-zero benchmark gets today; a noisy benchmark keeps a scale different from 1
    - If `build-payload.test.ts` exposes a payload fixture builder, the payload's headline Sharpe for a constant-yield series equals the all-zero series'; if it has none, record that and rely on `compute`
  </behavior>
  <action>
    Per D-17 and D-07. Step 0: `node scripts/verify-plan-anchors.mjs .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-04-PLAN.md`, halt on non-zero. Write the T13 (both cases) and T18 tests first in `src/lib/factsheet/return-stats.sites.test.ts` and observe RED; record the RED ids (the non-finite case may be GREEN on the old code, since it pins today's value: record it as a pin). Then in `compute`: take `m` and `s` from `dispersion(rets, 0)` (so `s` is 0 on residue and `annVol` follows), take the Sharpe from `sharpe(rets, { periodsPerYear, ddof: 0, rf })` answered as 0 on null (today's degenerate value), and keep skew and kurtosis on their existing `s > 0` gate, which is now correct over the snapped `s`. Replace every other use of the private `mean` / `pstdev` in `compute.ts` with `mean` / `stdDev(xs, false)` from `@/lib/portfolio-math-utils` (the same left-to-right sums, so bitwise equal) and delete the private helpers; if replacing a use moves an existing test's value, keep that one private use and record which and why in the SUMMARY (the Sharpe / skew / kurtosis / `ann_vol` lines are not optional). Do not edit `comparator-block.ts`. Drill T13 (restore the old `s` and Sharpe lines) and T18 through T13's neuter (with `compute` neutered, the T18 test must also go RED; record it). `cp` restore, `cmp`.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet/return-stats.sites.test.ts src/lib/factsheet/compute src/lib/factsheet/comparator-block src/lib/factsheet/build-payload src/__tests__/phase-52-frozen-spine-guards && test -z "$(git diff --name-only "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/comparator-block.ts src/lib/factsheet/__snapshots__)"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", any "obsolete" / "mismatched" snapshot line, or a diff in comparator-block.ts or the snapshot directory</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' src/lib/factsheet/compute.ts` prints at least 1
    - `grep -c 'const sharpe = s > 0 ?' src/lib/factsheet/compute.ts` prints 0, and `grep -c 'function pstdev' src/lib/factsheet/compute.ts` prints 0 (or the SUMMARY records the kept use and why, and plan 166.2-06's gate names it as a recorded exception)
    - the SUMMARY records the T13 and T18 RED ids (and the non-finite pin), the drill rows, and whether the end-to-end payload check ran
  </acceptance_criteria>
  <done>The factsheet headline Sharpe, skew, kurtosis and vol come from return-stats and equal the all-zero output on a constant yield, and the comparator vol-match is right without touching its file, each proven by a test observed red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: bootstrapCI's Sharpe (T15) and computeOgHeadline's computed Sharpe (T14's computed arm) on return-stats, then the plan's vitest, tsc and eslint gates</name>
  <files>src/lib/factsheet/bootstrap.ts, src/lib/factsheet/og-metrics.ts, src/lib/factsheet/return-stats.sites.test.ts, src/lib/factsheet/og-metrics.test.ts</files>
  <read_first>
    - src/lib/factsheet/bootstrap.ts - `headlineStats` and `bootstrapCI` (its Sortino is NOT in the class)
    - src/lib/factsheet/og-metrics.ts - `computeOgHeadline` (the `values.length >= 30` block: `m`, `v`, `s`, the Sharpe line; the drawdown and CAGR code is NOT in the class) and src/lib/factsheet/og-metrics.test.ts (Phase 169 plan 04 owns that file; this plan changes ONLY its two named Sharpe side assertions, D-19 amendment 2026-09-26). Before editing, run `git fetch origin main` and `git log --oneline HEAD..origin/main -- src/lib/factsheet/og-metrics.ts`: if Phase 169 plan 04 has merged, the function carries a persisted-scalars branch; edit only the computed arm and leave that branch as it is
  </read_first>
  <behavior>
    - T15: `bootstrapCI(navConstantYield(y))` Sharpe fields equal `bootstrapCI(zeros)` Sharpe fields (same seed, same length)
    - T14 (computed arm): `computeOgHeadline(rows from navConstantYield(y), "crypto").sharpe` is NaN for every yield id, `Object.is`-identical to the all-zero rows' (today about 1e13); the cent-rounded 1% APY rows give a finite Sharpe; `cagr` and `maxDd` on a noisy input are bitwise unchanged; the existing og-metrics tests (including the crypto / traditional ratio of sqrt(365/252)) pass with only the two named Sharpe side assertions changed to `Number.isNaN` (their inputs are residue-dispersion series: 300 × 0.001 and 40 × 0.002, whose old Sharpe was about 2.9e16). Called WITHOUT any persisted-scalars argument, so the test pins the computed arm whether or not Phase 169 plan 04 has merged
  </behavior>
  <action>
    Per D-17, D-19 and D-07. Write the T15 and T14 tests first and observe RED; record the ids. In `headlineStats`, the Sharpe becomes `sharpe(rets, { periodsPerYear, ddof })` answered as 0 on null, with the ddof it uses today. T14's computed arm, per D-19: in `computeOgHeadline`, replace the `m` / `v` / `s` lines and the Sharpe expression inside the `values.length >= 30` block with `sharpe(values, { periodsPerYear, ddof: 0 }) ?? NaN` (NaN is the card's existing hide value), keep the 30-observation gate, the drawdown and the CAGR code as they are, and add the one import line; if Phase 169 plan 04 has merged, its persisted branch is untouched. In `og-metrics.test.ts` change ONLY the two Sharpe side assertions measured red by the fix (D-19 amendment 2026-09-26): in "CAGR hidden (NaN) for a dense sub-year series (300 trading days < 0.95y)" the line `expect(Number.isFinite(sharpe)).toBe(true);` becomes `expect(Number.isNaN(sharpe)).toBe(true);`, and in "single / duplicate / unsorted dates never produce Infinity" the line `expect(Number.isFinite(r1.sharpe)).toBe(true);` becomes `expect(Number.isNaN(r1.sharpe)).toBe(true);`; put above each a one-line comment `// A constant series has no dispersion, so no Sharpe (D-07).` Keep both inputs and every other line (the CAGR and Infinity assertions are what those tests are about). Do not keep `s > 0`, do not map null to a finite number, do not widen a floor. Drill T15 and T14 (restore the old `s > 0 ?` Sharpe line in each), RED, `cp` restore, `cmp`. Then run the gates below, and record per site T13, T14 (computed arm), T15 and T18 that the fixed output is one the site already emits for an exact constant, so no renderer changes; if any would emit a new shape, STOP and report.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/factsheet src/lib/return-stats src/app/api/og "src/app/factsheet" && test -z "$(git diff --name-only "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/comparator-block.ts src/lib/factsheet/__snapshots__)" && QZ_OGD="$(git diff -U0 "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/og-metrics.test.ts | grep -E '^[-+]' | grep -vE '^(---|\+\+\+) ')" && test "$(printf '%s\n' "$QZ_OGD" | grep -c '^-')" = 2 && test "$(printf '%s\n' "$QZ_OGD" | grep '^-' | grep -c 'Number.isFinite(.*sharpe)')" = 2 && test "$(printf '%s\n' "$QZ_OGD" | grep '^+' | grep -c 'Number.isNaN(.*sharpe)')" = 2 && test -z "$(printf '%s\n' "$QZ_OGD" | grep '^+' | grep -v 'Number.isNaN(.*sharpe)' | grep -v '^+[[:space:]]*// A constant series has no dispersion, so no Sharpe (D-07)\.$')"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", any "obsolete" / "mismatched" snapshot line, a diff in comparator-block.ts or the snapshot directory, or an og-metrics.test.ts diff other than exactly the two named Sharpe side assertions (2 removed `Number.isFinite(...sharpe)` lines, 2 added `Number.isNaN(...sharpe)` lines, plus only the one-line D-07 comments)</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/factsheet/compute.ts src/lib/factsheet/bootstrap.ts src/lib/factsheet/og-metrics.ts src/lib/factsheet/return-stats.sites.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' src/lib/factsheet/bootstrap.ts src/lib/factsheet/og-metrics.ts` prints at least 1 for each file
    - `grep -c 'const sharpe = s > 0 ?' src/lib/factsheet/bootstrap.ts` prints 0 and `grep -c 's > 0 ? (m \* periodsPerYear)' src/lib/factsheet/og-metrics.ts` prints 0
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/factsheet/__snapshots__/ src/lib/factsheet/comparator-block.ts src/lib/factsheet/types.ts src/lib/factsheet/fetch-and-build-payload.ts src/lib/return-stats.ts` is empty for this plan's commits
    - the SUMMARY records T14 (computed arm) and T15 RED ids, the T13/T14/T15/T18 drill table, whether Phase 169 plan 04 had merged into `og-metrics.ts` at edit time, and the per-site "already emitted" line
    - `git diff --stat -- analytics-service/ supabase/` is empty for this plan's commits, and `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>The factsheet headline, bootstrap and OG-card computed Sharpe come from return-stats and equal the all-zero output on a constant yield, each proven by a test observed red; snapshots unchanged; vitest, tsc and eslint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dailies -> TS factsheet math -> public factsheet page, OG social preview, allocator overview, scenario preview | a fabricated Sharpe, skew or vol-matched curve reaches a published factsheet or a public social card |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.2-25 | Tampering (integrity of a displayed number) | T13, T14's computed arm, T15, T18 (the OG card is a public social preview) | medium | mitigate | one shared module; red test per site; D-07 equality with the all-zero input; drill table |
| T-166.2-26 | Tampering (a regression hidden by a snapshot refresh) | build-payload snapshot | medium | mitigate | snapshot directory must be unchanged; a snapshot diff is reported, never accepted |
| T-166.2-27 | Denial of service (over-wide floor hides real data) | the shared floor at these sites | medium | mitigate | cent-rounded 1% APY controls go red if the floor widens |
| T-166.2-31 | Tampering (cross-phase clobber) | compute.ts, og-metrics.ts shared with 169 | medium | mitigate | edits confined to named hunks; 169-owned files diff-gated to empty; plan 166.2-06 Task 2 merges origin/main and re-runs the site tests |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-04-PLAN.md` exits 0 before Task 1 starts.
- Both Task 2 verify commands exit 0.
</verification>

<success_criteria>
- D-17, third slice, first half: the factsheet headline, bootstrap and the OG card's computed Sharpe come from return-stats (T14's persisted read left to Phase 169 plan 04, D-19; T18 fixed at its source), each with a test observed red, snapshots unchanged.
</success_criteria>

<output>
Create `.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-04-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
