---
phase: 166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta
verified: 2026-09-26T17:10:00Z
verified_at_sha: 4a94bdde82467e25fcaba02e620e115133ccd52a
status: gaps_found
score: 8/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
drift_subjects:
  - src/lib/return-stats.ts
  - src/lib/return-stats.single-source.test.ts
  - src/lib/return-stats.test.ts
  - src/lib/return-stats.sites.test.ts
  - src/lib/return-stats.sites-risk.test.ts
  - src/lib/return-stats.pearson-contract.test.ts
  - src/__tests__/fixtures/dispersion-nav.ts
  - src/app/(dashboard)/compare/lib/holding-compare-adapter.ts
  - src/lib/sample-basis-ratios.ts
  - src/lib/scenario.ts
  - src/lib/diversification.ts
  - src/components/strategy/CompareCorrelationMatrix.tsx
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
  - src/lib/portfolio-stats.ts
  - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts
  - src/app/(dashboard)/allocations/lib/scenario-stress.ts
  - src/app/(dashboard)/allocations/widgets/attribution/AlphaBetaDecomposition.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.tsx
  - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts
  - src/components/portfolio/CorrelationHeatmap.tsx
  - src/lib/factsheet/compute.ts
  - src/lib/factsheet/calmar-by-year.ts
  - src/lib/factsheet/bootstrap.ts
  - src/lib/factsheet/og-metrics.ts
  - src/lib/factsheet/joint.ts
  - src/lib/factsheet/rolling.ts
  - src/lib/factsheet/build-payload.ts
  - src/lib/factsheet/allocator.ts
  - src/lib/factsheet/peer-cohort.ts
  - src/lib/factsheet/types.ts
  - src/lib/factsheet/fetch-and-build-payload.ts
  - src/lib/factsheet/return-stats.sites.test.ts
  - src/lib/factsheet/return-stats.sites-joint.test.ts
  - src/app/factsheet/[id]/v2/MetricsColumn.tsx
  - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.tsx
  - src/app/factsheet/[id]/v2/basis-context.tsx
  - src/app/api/og/factsheet/[id]/route.page-agreement.test.tsx
gaps:
  - truth: "One release commit ships it: ONE unified CHANGELOG entry covers EVERY one of 166.2's own commits, and VERSION / package.json carry a version derived from origin/main's VERSION re-read at release time (ROADMAP goal sentence; plan 166.2-07 must_haves)"
    status: failed
    reason: "The last release commit is be37c440e (the round-1 fold). Sixteen non-planning code commits landed after it (review rounds 2 and 3: 1afe06ea6, 0dd022822, 9769f8dfa, 89521ac23, fd97c010a, bb61e178a, 2a50833ae, 0c6f0ba98, dbcef5337, 7edee5002, 18671b090, a85060a43, b9d7c7590, 5cf32da75, 252211514, a3592327a), and `git log be37c440e..HEAD -- CHANGELOG.md VERSION package.json` is EMPTY. The 0.97.0.0 entry names none of them: nothing about Profit Factor, Calmar by Year, skew/kurtosis/Avg Win/Avg Loss moving to '—' (D-27), rollingSortino's gap, the Rolling Metrics 'Now' column, the bootstrap resample-count caption, the KPI strip's pair coverage, the v7 payload cache key or beta = 0 for a flat strategy leg. Separately, origin/main now reads 0.100.0.0 (Phase 166.1 merged at d9c173346) while the branch still carries 0.97.0.0, so the plan's own derivation rule (second component + 1, last two zeroed) is red at HEAD and needs a NEW release commit, as plan 07 anticipated."
    artifacts:
      - path: CHANGELOG.md
        issue: "The [0.97.0.0] entry stops at the round-1 fixes; 16 own code commits map to no bullet (CLAUDE.md CHANGELOG discipline, step 5)."
      - path: VERSION
        issue: "0.97.0.0 is below origin/main's 0.100.0.0; derivation gives 0.101.0.0 at this reading (re-read at ship time)."
      - path: package.json
        issue: "Byte-equal to VERSION (OK), but carries the same stale version."
    missing:
      - "A new chore(release) commit (never an amend) that merges origin/main, re-derives the version from origin/main's VERSION, and replaces the 0.97.0.0 entry with one unified entry whose bullets cover every own commit, including the 16 listed above and the D-26 / D-27 decisions."
      - "After that merge, re-run the compute-once gate (src/lib/return-stats.single-source.test.ts), tsc and the T1-T20 site tests on the merged tree (D-20 W1). Measured here without a checkout: the code merges clean (only planning files, CHANGELOG.md, VERSION and package.json conflict), and main's added src lines carry no Math.sqrt or cov-division line."
behavior_unverified_items: []
human_verification:
  - test: "After deploy, open the factsheet of a strategy whose daily series has no real dispersion (a constant yield) in the logged-in browser, and its OG card image."
    expected: "The KPI Sharpe and the Main Metrics Sharpe read '—', matching the OG card's '—'. Beta, Correlation, R², Information Ratio, Treynor, Up/Down Capture, Skew, Kurtosis, Profit Factor, Avg Loss, the headline Calmar and the peer Sharpe bar also read '—'. Avg Win reads a positive number, because every day of a positive constant yield wins. Nothing reads 0.00."
    why_human: "route.page-agreement.test.tsx and MetricsColumn.no-loss-residues.test.tsx render the components in jsdom, but not the deployed page with its real cache, fonts and layout."
  - test: "After deploy, open the allocations Risk tab with a flat leg in the book."
    expected: "Every cell of the flat leg in the CorrelationMatrix widget reads '—' on the neutral background, with the hover title 'Insufficient data'. The noisy pair still shows a real coloured value."
    why_human: "The colour, title tooltip and layout can only be seen in the browser. CorrelationMatrix.residue.test.tsx pins the DOM only."
  - test: "After deploy, open a scenario in the composer with one flat member."
    expected: "The correlation heatmap caption reads 'Avg |ρ| … · k of n pairs measured', and the undefined pair's cell reads '—'. The KPI strip's Avg |ρ| states its pair coverage."
    why_human: "The caption and strip copy are on screen only after deploy. CorrelationHeatmap.test.tsx and KpiStrip.pair-coverage.test.tsx pin the DOM only."
  - test: "After deploy, open the Attribution widget (AlphaBetaDecomposition) for a scenario whose benchmark window is flat, then one whose strategy leg is flat."
    expected: "With a flat benchmark, alpha and beta read '—' and the note names the benchmark as flat. With a flat strategy leg, beta reads 0 (review round 2, HI-01) and is not '—'."
    why_human: "The rendered copy and the choice of note are visual. attribution.test.tsx pins the DOM only."
  - test: "After deploy, open the factsheet Bootstrap CI panel for a constant-yield strategy, and for one where only some resamples carry a Sharpe."
    expected: "The first reads '—' with 'no resample has a Sharpe'. The second states how many resamples the interval rests on, and shows '—' below 40 (MIN_SHARPE_RESAMPLES)."
    why_human: "Caption wording and layout. BootstrapCIPanel.resample-count.test.tsx pins the DOM only."
  - test: "After deploy, open the factsheet Calmar by Year panel for a strategy with a year that has no drawdown, and look at the Rolling Metrics 'Now' column and the rolling Sharpe/Beta charts for a series with a flat window."
    expected: "The no-drawdown year's Calmar reads '—' and the other years read numbers. The rolling charts break at the flat window (a gap, never a drop to 0). 'Now' reflects the current window."
    why_human: "Chart gaps and the panel layout can only be seen in the browser."
---

# Phase 166.2: COMPUTEONCE Verification Report

**Phase Goal:** The TypeScript side computes Sharpe, Pearson and beta ONCE and every page reads it. One floored module, `src/lib/return-stats.ts`, pinned to the Python floor constant, serves every Tier-2 site T1-T20. Dead copies are deleted, a source-scan gate fails CI when a private copy of the formula returns, and one release commit ships it. Founder direction D-17; founder decision D7 "Show — everywhere" (D-24, D-26, D-27).
**Verified:** 2026-09-26T17:10:00Z at `4a94bdde82467e25fcaba02e620e115133ccd52a` (feat/166.2)
**Status:** gaps_found (one gap: the release entry is stale). Six post-deploy browser items are also open, and they are human_needed.
**Re-verification:** No. This is the initial verification.

**The contract used.** The plans' frontmatter truths still read "null answered as today's 0" at several sites. That is D-07's original wording. Founder decision D7 (the D-07 amendment and D-24) reversed it for display, and D-26 and D-27 extended it. Each such truth was verified against the AMENDED contract: a constant yield still equals the all-zero series at every field, and both now read NaN / null / "—". Where a plan truth says 0 and the code says NaN, that is superseded by a founder decision, not FAILED.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ONE TS module `src/lib/return-stats.ts` holds the floor (`DISPERSION_RESIDUE_REL`, `residueFloor`, `dispersionIsResidue`, `dispersionIsReal`) and the only `dispersion`, `sharpe`, `pearson` and `beta`, all built on `portfolio-math-utils`' `mean` and `stdDev` (SC-3; D-15, D-17) | ✓ VERIFIED | Read the module. Its exports are exactly those eight symbols. `dispersion` snaps a residue sd to 0. `sharpe` returns null below 2 points, on a non-finite input, or on sd 0. `pearson` uses the G11.E.5 null contract. `beta` returns null on a flat or non-finite `x`, returns 0 on a flat `y` (round 2 HI-01), and throws a RangeError on unequal lengths. `return-stats.test.ts` passes. |
| 2 | The floor is pinned to the Python constant by exactly one numeric-literal definition across `dispersion.py` and `metrics.py` (D-21 Info 4), and the pin holds whichever of 166.1 and 166.2 merges second | ✓ VERIFIED | The pin test passes at HEAD, where the branch carries the pre-166.1 Python: `metrics.py` has 1 literal and `dispersion.py` is absent. Measured on `origin/main` (166.1 merged) with `git show`: `dispersion.py` 1, `metrics.py` 0. So exactly one either way. |
| 3 | Every T1-T20 site calls `return-stats` or is deleted as dead. `correlation-math.ts`, `rollingCorrelation` and `computeRollingMetric` are gone. T18 is fixed at its source (SC-3; D-17) | ✓ VERIFIED | `grep -rl '@/lib/return-stats' src` over live code finds 17 files. They are the 16 Tier-2 site files, plus `AlphaBetaDecomposition.tsx` (D-24). `src/lib/correlation-math.ts` does not exist. A grep of `src` for `correlation-math`, `rollingCorrelation` and `computeRollingMetric` finds 0 hits, comments included. No `sharpe(…)`, `pearson(…)` or `beta(…)` result is answered with `?? 0` in any live importer (grep). `compute` carries `sharpeRatio(...) ?? NaN`, as does `computeOgHeadline`. |
| 4 | On a compounding-NAV constant yield, each site emits exactly what an all-zero series emits at that field, proven by a red test (SC-3; D-07 invariant as amended by D7) | ✓ VERIFIED | The site-test files all pass at HEAD: `return-stats.sites.test.ts` (T1-T5), `return-stats.sites-risk.test.ts` (T9-T12), `factsheet/return-stats.sites.test.ts` (T13-T15, T18, HI3-01, HI3-02), `factsheet/return-stats.sites-joint.test.ts` (T16, T17, T19, T20), `CompareCorrelationMatrix.residue.test.tsx` (T6) and `CorrelationMatrix.residue.test.tsx` (T7). Each compares the constant-yield output to the all-zero output (`Object.is`), and a cent-rounded NAV control keeps a finite value. The "observed RED first" and neuter drills are the executors' claims and were not re-run here. The tests do fail under the old arithmetic by construction, because a residue sd of about 1e-16 gives a finite trillion-scale Sharpe. |
| 5 | The source-scan gate pins the rule in three layers: the 16-file import rule, the retired-expression table, and the whole-tree shape matcher with a count-pinned Sortino/Treynor allowlist. The merge-base pin runs in every vitest run (SC-3; D-17, D-20 B1, D-22, D-24) | ✓ VERIFIED | `return-stats.single-source.test.ts` passes. Its merge-base block runs UNCONDITIONALLY. It archives `ea4167a3f` in-test, and `QZ_166_2_06_SCAN_ROOT` is only an override. It printed `shape-scan-base: hits=26 unallowlisted=20 allowlisted=6` in this verifier's run. |
| 6 | D7 / D-24: a statistic that does not exist stays empty end to end and renders "—" on every page. The factsheet and the OG card agree (founder decision D7) | ✓ VERIFIED (render-test level) | `compute` Sharpe is `?? NaN`. `MIN_SHARPE_RESAMPLES = 40` gates the bootstrap CI. `CorrelationMatrix` renders "—" titled "Insufficient data". `computeAlphaBeta` returns `{ alpha: null, beta: null }` on n < 2 or on a null beta. `route.page-agreement.test.tsx` "a compounding constant yield reads '—' on the OG card AND on the page, never 0.00" passes, with a dispersing control. CorrelationHeatmap's "D7: states the measured pair count" passes. The browser read after deploy is human_needed (items 1-6 below). |
| 7 | D-26: the empty scenario body carries NaN for statistics that do not exist; only counts and spans stay 0 | ✓ VERIFIED | `emptyComputeSummary` in `scenario-factsheet-payload.ts` sets `sharpe`, `sortino`, `calmar`, `skew`, `kurt`, `win_rate`, `avg_win`, `avg_loss` and every return window to NaN. `n` and `longest_dd` are 0. `scenario-factsheet-payload.empty-render.test.tsx` passes. |
| 8 | D-27: skew and kurtosis are NaN with no dispersion. Profit Factor (no losing day), per-year Calmar (no drawdown), Avg Loss and Avg Win are NaN where they do not exist | ✓ VERIFIED | `compute.ts`: `skew` / `kurt` use `s > 0 ? … : NaN` over the floored `s`. `avgWin` / `avgLoss` use `count > 0 ? … : NaN`. `profitFactor` is `lossSum !== 0 ? … : NaN`. In `calmar-by-year.ts`, `calmar` is `maxDd !== 0 ? … : NaN`. `MetricsColumn.no-loss-residues.test.tsx` renders fresh and after a JSON round trip, and passes. |
| 9 | ONE release commit ships it: one unified CHANGELOG entry covering EVERY own commit, VERSION = package.json derived from origin/main at release time, no Python or Supabase path, and no CI-skip token (ROADMAP goal; plan 07) | ✗ FAILED | Branch scope is OK: `git diff --name-only origin/main...HEAD` lists 0 paths under `analytics-service/` or `supabase/`. CI-skip sweep is OK: 0 hits in `git log origin/main..HEAD --format=%B`. VERSION and package.json are byte-equal (`0.97.0.0`). **But the CHANGELOG entry misses 16 own code commits from review rounds 2 and 3, and VERSION is below origin/main's `0.100.0.0`.** See Gaps. |

**Score:** 8/9 truths verified (0 present-but-behavior-unverified)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/return-stats.ts` | floor + dispersion, sharpe, pearson, beta | ✓ VERIFIED | Has 17 live importers. It is substantive. |
| `src/lib/return-stats.test.ts` | floor tests, cross-language pin | ✓ VERIFIED | passes |
| `src/__tests__/fixtures/dispersion-nav.ts` | `navConstantYield`, `CONSTANT_YIELDS` | ✓ VERIFIED | used by every site test |
| `src/lib/return-stats.sites.test.ts` | T1-T5 | ✓ VERIFIED | passes |
| `src/lib/return-stats.pearson-contract.test.ts` | G11.E.5 contract, moved | ✓ VERIFIED | passes |
| `src/lib/return-stats.sites-risk.test.ts` | T9-T12 | ✓ VERIFIED | passes |
| `src/lib/factsheet/return-stats.sites.test.ts` | T13-T15, T18, HI3-01/02 | ✓ VERIFIED | passes |
| `src/lib/factsheet/return-stats.sites-joint.test.ts` | T16, T17, T19, T20 | ✓ VERIFIED | passes |
| `src/components/strategy/CompareCorrelationMatrix.residue.test.tsx` | T6 render | ✓ VERIFIED | passes |
| `src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx` | T7 render | ✓ VERIFIED | passes |
| `src/lib/return-stats.single-source.test.ts` | the D-17 gate | ✓ VERIFIED | passes; the 26-hit line prints |
| `src/lib/correlation-math.ts` | DELETED | ✓ VERIFIED | absent |
| `CHANGELOG.md` / `VERSION` / `package.json` | one unified entry for the phase | ✗ STALE | See the gap. |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| 16 Tier-2 site files + `AlphaBetaDecomposition.tsx` | `src/lib/return-stats.ts` | `@/lib/return-stats` import, used in live code | ✓ WIRED (grep, and the gate's import rule) |
| `src/lib/return-stats.ts` | `src/lib/portfolio-math-utils.ts` | `mean`, `stdDev` | ✓ WIRED |
| `comparator-block.ts` (unedited) | `compute.ts` | `ann_vol > 0` guard reads the snapped `ann_vol` (T18) | ✓ WIRED (T18 site test) |
| `return-stats.test.ts` | `analytics-service/services/{dispersion,metrics}.py` | text read of the literal | ✓ WIRED on the branch and on origin/main |

### Data-Flow Trace (Level 4)

| Artifact | Data | Source | Real data | Status |
|----------|------|--------|-----------|--------|
| Factsheet Sharpe / OG Sharpe | `ComputeResult.sharpe`, `computeOgHeadline().sharpe` | `return-stats.sharpe` over the payload's daily returns | yes; NaN only where the ratio does not exist | ✓ FLOWING (page-agreement test builds both from one series) |
| Risk-tab matrix cell | `pearson(av, bv)` | per-key daily returns | yes; null renders "—" | ✓ FLOWING |
| Scenario matrix / Avg \|ρ\| | `computeScenario` correlation | constituents' returns | yes; undefined pairs are left out of the sum and the count | ✓ FLOWING |

### Behavioral Spot-Checks (gates run by this verifier at HEAD)

`node_modules` was symlinked from the main checkout for the runs. The worktree had none, so no link was nested. The link was removed afterwards.

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Typecheck | `npx tsc --noEmit` | `tsc exit 0` (no output) | ✓ PASS |
| Brief's vitest scope | `npx vitest run src/lib/factsheet src/app/factsheet 'src/app/(dashboard)/allocations' src/app/api/og src/__tests__/critical-regressions.test.ts --test-timeout=30000` | `Test Files  208 passed (208)` / `Tests  3234 passed (3234)` / `vitest exit 0` (summary reporter: `critical-regressions.test.ts` was in the path list but was not confirmed by name; VERSION = package.json was checked by hand) | ✓ PASS |
| Phase's own census, residue and gate files | `npx vitest run src/lib/return-stats src/lib/portfolio-stats.test.ts src/lib/diversification.test.ts src/lib/scenario src/components/strategy/CompareCorrelationMatrix src/components/portfolio/CorrelationHeatmap.test.tsx 'src/app/(dashboard)/compare' src/__tests__/contracts/env-manifest.test.ts src/__tests__/phase-148-owner-lane-cache-isolation.test.ts src/app/factsheet-share --test-timeout=30000` | `shape-scan-base: hits=26 unallowlisted=20 allowlisted=6` / `Test Files  31 passed (31)` / `Tests  702 passed (702)` / `vitest exit 0` | ✓ PASS |
| ESLint on the phase's changed files | `npx eslint` over the 64 existing `.ts`/`.tsx` files in `git diff --name-only origin/main...HEAD` (the deleted `correlation-math.*` dropped) | no output, `eslint exit 0` | ✓ PASS |
| Branch scope | `git diff --name-only origin/main...HEAD \| grep -cE '^(analytics-service\|supabase)/'` | `0` | ✓ PASS |
| CI-skip sweep | `git log origin/main..HEAD --format=%B \| grep -ciE '<the six tokens>'` | `0` | ✓ PASS |
| Release entry currency | `git log --oneline be37c440e..HEAD -- CHANGELOG.md VERSION package.json` | empty, with 16 code commits after `be37c440e` | ✗ FAIL (the gap) |
| Mergeability with current main | `git merge-tree --write-tree --name-only HEAD origin/main` | conflicts only in `.planning/` files, `CHANGELOG.md`, `VERSION` and `package.json`; `basis-context.tsx` auto-merges | ℹ️ ship-time |

### Probe Execution

Step 7c: SKIPPED. The phase declares no `probe-*.sh`, and it is not a migration phase.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| SC-3 (as extended by 166.1 D-15 / D-17) | 166.2-01 … 07 | Each TS ratio is computed once, from the floor. Constant yield equals all-zero, proven by a red test, and a source-scan gate pins the rule. | ✓ SATISFIED (code) / ✗ release entry stale | Truths 1-8; truth 9 is the gap |

No orphaned requirement: the ROADMAP maps SC-3 only, and every plan claims it.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| none | — | TBD / FIXME / XXX in the phase's added lines | — | 0 hits over the diff of the 70 changed non-planning files |
| `src/lib/portfolio-stats.ts` | `computeRiskDecomposition` | `percentage: portfolioVarianceIsReal ? … : 0` | ℹ️ Info (pre-existing) | See known limits IN4-02 / SFH-R4 MEDIUM-1 |

### Known Limits (review round 4, no CRITICAL or HIGH; recorded, not fixed, under the founder rule for MEDIUM-or-lower)

Round 4 (`166.2-REVIEW.md` top section and `166.2-REVIEW-SFH-R4.md`, both at `cd2349d42`) found 0 CRITICAL and 0 HIGH. The round-3 HIGHs HI3-01 (Profit Factor) and HI3-02 (Calmar by Year) are confirmed closed. This verifier re-read both arms in code (truth 8).

| id | severity | D9 class | what |
|----|----------|----------|------|
| **WR4-01** | MEDIUM | pre-existing, separable | `compute.ts` `drawdowns` starts `peak` at `-Infinity`, so the first equity point (`1 + r0`) becomes the peak. A loss on the first day of a window is therefore never counted as drawdown. A year that opens with a loss and then only gains reads "Max DD 0.00% · Calmar —". The headline `max_dd` shares the defect. Fix belongs to a separate phase: seed the peak at 1.0 after deciding parity with the analytics service's drawdown series. |
| **SFH-R4 MEDIUM-1 / IN4-02** | MEDIUM (SFH) / LOW (code review) | pre-existing, separable | `computeRiskDecomposition` reads 0% per strategy when the book has no variance. `RiskDecomposition.tsx` `buildCovMatrix` also returns an all-zero matrix when fewer than 2 common dates exist, which shows "no overlap" as "zero risk" with no empty state. This is on the authenticated allocations dashboard only. |
| **SFH-R4 MEDIUM-2** | MEDIUM | pre-existing, separable | The KPI strip's Avg \|ρ\| delta against the live baseline can compare two different pair sets. It is NOT named in CONTEXT D-26's recorded limits (SFH-R4 asked for that), so it is recorded here. |
| **IN4-01** | LOW | none | The payload cache key stays `factsheet-v2-payload-v7` although `compute`'s outputs changed in round 3. v7 never shipped. A preview entry filled before round 3 can serve pre-pass values for up to 3600 s. |
| **IN4-03 / SFH-R4 LOW-2** | LOW | pre-existing | `computeWinRate` returns `{ winRate: 0, profitFactor: 0 }` on empty or all-zero input. It has no production caller (dead export). |
| **SFH-R4 LOW-1** | LOW | latent | The Extended Metrics common-sense ratio (`tailRatio * m.profit_factor`) relies on an invariant (a negative P5 implies a losing day, so PF is finite), not on a guard. After a JSON round trip, `x * null` would be 0. It is safe today. |
| **SFH-R4 LOW-3** | LOW | this pass's test | Two sibling assertions in `MetricsColumn.no-loss-residues.test.tsx` (Avg Win / Avg Loss, Common-sense Ratio) pass under the old code. Each shares its `it` block with a biting assertion. |
| SFH-R3 LOW-1 | LOW | carried | `zipDrop` joins the composer's rolling line across an interior gap. It never draws 0. Documented in its comment. |
| SFH-R3 LOW-2 | LOW | carried | Dropping Sortino resamples biases the CI low. |
| SFH-R3 LOW-3 | LOW | carried | `degenerateNote` at `nValid === 1`. |
| D-26 limits (SFH-R3 LOW-5 / LOW-6) | LOW | carried | The empty scenario body reports 0 observations for a 1-9-day blend. The scenario-share page's Avg \|ρ\| has no pair-coverage qualifier. `emptyQuantiles` stays 0. |

### Deviations Recorded (not gaps)

- Phase 169's `src/lib/factsheet/og-metrics.test.ts` was edited beyond the two lines D-19 admitted (IN-03, round 1). This is recorded in CONTEXT D-24 and the ROADMAP.
- `src/lib/factsheet/types.ts` and `src/lib/factsheet/fetch-and-build-payload.ts` were edited: NaN-able field comments and the v7 cache-key bump in round 2 (`89521ac23`). D-17's split list says 166.x "edits none of" these two Phase-169-owned files. The v7 key is recorded in CONTEXT D-27. The edit to 169-owned files is not called out as a deviation in the ROADMAP. Phase 169's second merger should rebase.
- Rounds 2 and 3 have no `REVIEW-FIX` report. `166.2-REVIEW-FIX.md` is iteration 1 only. The fix commits and the round-3 and round-4 confirmation reviews are the record of closure.

### Human Verification Required

These six items are owned by the orchestrator in the logged-in browser after deploy. None is marked passed here.

1. **Factsheet "—" for a constant-yield series.** Open the factsheet of a strategy with no real dispersion, and its OG card. KPI and Main Metrics Sharpe, Beta, Correlation, R², IR, Treynor, captures, Skew, Kurtosis, Profit Factor, Avg Loss, the headline Calmar and the peer Sharpe bar should all read "—", matching the OG card. Avg Win should read a positive number, because every day wins. Nothing should read 0.00.
2. **Risk-tab matrix.** With a flat leg, the leg's cells should read "—" on neutral, with the title "Insufficient data". The noisy pair should keep its value.
3. **Scenario heatmap caption.** With one flat member, the caption should read "· k of n pairs measured" and the undefined cell "—". The KPI strip's Avg \|ρ\| should state its coverage.
4. **AlphaBetaDecomposition.** A flat benchmark should give "—" and name the benchmark. A flat strategy leg should give beta 0.
5. **Bootstrap CI caption.** A constant yield should read "no resample has a Sharpe". Partial survival should state the resample count, with "—" below 40.
6. **Calmar by Year and the rolling panels.** A no-drawdown year should read "—". Rolling charts should gap at a flat window. "Now" should show the current window.

### Gaps Summary

The code goal is achieved. There is one TS home for dispersion, Sharpe, Pearson and beta, pinned to the Python floor, and that pin holds on both sides of the 166.1 merge. All T1-T20 sites read the module or were deleted. The constant-yield equals all-zero invariant is pinned per site. Founder decision D7 is applied end to end at render-test level, and the three-layer gate runs in every vitest run with the 26-hit merge-base pin. tsc, both vitest scopes (3,936 tests) and eslint are green at HEAD.

The one gap is the ROADMAP's "one release commit ships it". The 0.97.0.0 entry was folded after round 1 and never again. Sixteen own code commits from rounds 2 and 3 map to no CHANGELOG bullet, and the version is now below origin/main's 0.100.0.0. Plan 07 already requires a NEW release commit when the version derivation goes red at ship time. That commit must also merge origin/main, re-run the gate, tsc and the site tests on the merged tree, and write one unified entry covering every own commit. Until then, the release truth is false at HEAD.

---

_Verified: 2026-09-26T17:10:00Z_
_Verifier: Claude (gsd-verifier)_
