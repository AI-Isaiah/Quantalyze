---
phase: 166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta
plan: 06
type: execute
wave: 3
depends_on: ["166.2-01", "166.2-02", "166.2-03", "166.2-04", "166.2-05"]
files_modified:
  - src/lib/return-stats.single-source.test.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A source-scan gate fails CI if any of the 16 Tier-2 site files stops importing @/lib/return-stats (D-17)"
    - "The same gate fails CI if ANY retired expression the plans 166.2-01, 166.2-02, 166.2-03, 166.2-04 and 166.2-05 acceptance criteria grep for comes back in its file (a {file, token, plan} table, every pair measured present before the phase), if correlation-math.ts reappears on disk or is imported, or if rollingCorrelation or computeRollingMetric reappears in live code (D-17, D-20 B1)"
    - "The same gate fails CI if live code under src/ (tests, __tests__ and fixtures excluded) outside src/lib/return-stats.ts matches one of the nine SHIPPED forms, except the entries of an explicit, commented, COUNT-PINNED allowlist (Sortino and Treynor); a stale allowlist entry also fails. The forms it matches: SHARPE S0 (x * periodsPerYear, ppy, sqrtN, ANNUAL or N) / d; S1 (...) / (d * Math.sqrt( or sqrtN; S2 (m / s) * Math.sqrt(; S3 m / s * Math.sqrt(; S4 Math.sqrt(N) * m / s; PEARSON P0 cov / Math.sqrt(; P1 Math.sqrt(ID * ID); P2 cov / (a * b); BETA cov / var. KNOWN UNMATCHED (measured by the round-5 checker and re-measured by the planner, D-22): mean(r) / stdDev(r) * Math.sqrt(N); mean(r) * Math.sqrt(N) / stdDev(r); (mu * Math.sqrt(N)) / sd; Math.sqrt(N) * (m / s); (m * 252) / s; cov / (Math.sqrt(va) * Math.sqrt(vb)); covariance(a,b) / variance(b). A copy in an unmatched form is caught only by the other two layers of D-17's guard, the import rule and the retired-expression table; the regexes are not widened in D-22 because that would move the measured 26-hit pin (D-17, D-20 B1, D-22)"
    - "Liveness is proven in the file through the committed matcher: every shipped form has a positive fixture (S0, S1 via a Sharpe in a NEW file, S2, S3, S4, P2, P1 via the two-step denom Pearson, BETA), the one-step Pearson cov / Math.sqrt(varA * varB) yields exactly two hits, P0 and P1, and a Sharpe split across lines trips it; an annualised volatility, a tracking error, a numeric-operand root and a comment-only Sharpe do not; a stale allowlist entry is reported (D-20 B1, D-21 B1, D-22)"
    - "The COMMITTED test's own matcher, run over the merge-base src/ tree, reports exactly 26 hits (20 not allowlisted, 6 allowlisted); a scratchpad copy of the regexes does not count as this measurement (D-22)"
    - "The matcher's pre-phase count is pinned from a measurement with the shipped regexes: 26 hits at the merge-base, 20 in-class plus 5 Sortino plus 1 Treynor, re-measured by the executor before writing (D-21 B1)"
    - "The gate was observed RED on four neuters (a dropped import, a restored private pearson, a Sharpe in a new untracked file, a restored retired Sharpe line) and GREEN after each byte-backup restore proven with cmp"
    - "origin/main is merged into the branch after the code plans, the T1-T20 site tests, the gate and tsc are re-run GREEN on the merged tree, and the merged base sha is recorded; a conflict inside a 166.2 hunk keeps the shared-module call, and a new in-class shape brought in by main is a STOP (D-20 W1)"
  artifacts:
    - path: src/lib/return-stats.single-source.test.ts
      provides: "the D-17 compute-once source-scan gate: import rule, retired-expression table, whole-tree shape matcher with a count-pinned allowlist, liveness fixtures"
  key_links:
    - from: src/lib/return-stats.single-source.test.ts
      to: src/lib/return-stats.ts
      via: "asserts every Tier-2 site file imports it, and that no ratio shape lives outside it"
      pattern: "@/lib/return-stats"
---

<objective>
Pin the founder's compute-once rule (CONTEXT D-17) with a source-scan gate strong enough to catch a
twenty-first copy in any of its nine matched forms anywhere under `src/` (D-22 names the forms it does not match), then merge `origin/main` into the branch and re-prove every site
and the gate on the merged tree (D-20 W1), so plan 166.2-07's sweep and release run on the tree that will ship.

Purpose: a rule that lives only in a plan is not a gate; the Phase 108 "second Sharpe" precedent
(`src/lib/scenario-backbone-gates.test.ts`) is how this repo keeps a deleted copy deleted. The round-3
check found the first design too narrow (an import rule plus five expressions): a retired copy returning
under a different spelling, or a new Sharpe in a new file, passed it. Phase 169 PR 1 and 167.1.2 plans 06
and 11 edit files this phase edits, and may land on `main` first.
Output: one gate test commit, one merge commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-CONTEXT.md
Also read `166.2-01-SUMMARY.md`, `166.2-02-SUMMARY.md`, `166.2-03-SUMMARY.md`, `166.2-04-SUMMARY.md` and
`166.2-05-SUMMARY.md` in this phase directory (any private helper a plan KEPT with a recorded reason).

**Environment.** TS tools through the MAIN checkout's `node_modules`, linked only for the run and removed
before every commit when it is a symlink. No database, no credential. Quote every `(dashboard)` path.
`tsc` in the background with a 600 s watchdog. Run `git fetch origin main` before ANY `git merge-base`
against `origin/main`. Byte backups under a `166.2-06` scratchpad name. Never `git checkout --`,
`git restore`, `git stash`, and never `git rebase` this branch (it is merged, not rebased, as at
`7bfce8490`).

**The 16 Tier-2 site files (the import rule).** `src/app/(dashboard)/compare/lib/holding-compare-adapter.ts`,
`src/lib/sample-basis-ratios.ts`, `src/lib/scenario.ts`, `src/lib/diversification.ts`,
`src/components/strategy/CompareCorrelationMatrix.tsx`,
`src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx`, `src/lib/portfolio-stats.ts`,
`src/app/(dashboard)/allocations/lib/scenario-benchmark.ts`,
`src/app/(dashboard)/allocations/lib/scenario-stress.ts`, `src/lib/factsheet/compute.ts`,
`src/lib/factsheet/bootstrap.ts`, `src/lib/factsheet/joint.ts`, `src/lib/factsheet/rolling.ts`,
`src/lib/factsheet/build-payload.ts`, `src/lib/factsheet/allocator.ts`, `src/lib/factsheet/og-metrics.ts`
(T14's computed arm, D-19).

**The retired-expression table (D-20 B1; 28 file/token pairs, each measured present at `d1bd9f50f`).**
Every literal an acceptance criterion in plans 166.2-01 to 166.2-05 greps to zero, one row each, with the plan that
retired it:
- 04: `holding-compare-adapter.ts` `std > 0 ?`; `sample-basis-ratios.ts` `volatility > 0 ?`; `scenario.ts`
  `volatility > 0 ?` and `statA.std > 0 && statB.std > 0`.
- 05a: `widgets/risk/CorrelationMatrix.tsx` `function pearson`.
- 05b: `portfolio-stats.ts` `computeRollingMetric`, `varB > 0 ? cov / varB`, `totalVariance > 0 ?`;
  `scenario-benchmark.ts` `1e-12 * (Math.abs`, `sampCov / (stdP * stdB)`,
  `(excessMean * periodsPerYear) / te`; `scenario-stress.ts` `1e-12 * (Math.abs`.
- 06a: `og-metrics.ts` `s > 0 ? (m * periodsPerYear)`; `compute.ts` `const sharpe = s > 0 ?` and
  `function pstdev`; `bootstrap.ts` `const sharpe = s > 0 ?`.
- 06b: `joint.ts` `function pstdev`, `function mean`, `varB > 0 ? cov / varB`, `s > 0 && sb > 0`,
  `trackingError > 0 ?`; `rolling.ts` `function pstdev`, `function mean`, `varB !== 0 ? cov / varB`,
  `s > 0 ? (m * periodsPerYear) / (s * sqrtN)`; `build-payload.ts` `denom > 0 ? cov / denom : NaN`,
  `Math.sqrt(va * vb)`; `allocator.ts` `s > 0 && mmS > 0 ?`.
Plus the whole-tree retirements: no import of the `correlation-math` module, no `rollingCorrelation` and no
`computeRollingMetric` token in live code, and `src/lib/correlation-math.ts` absent from disk. A pair a
plan's SUMMARY records as a KEPT exception (only `compute.ts` `function pstdev` is permitted one, by plan
166.2-04) is left out of the table and named in a comment with the SUMMARY's reason.

**The whole-tree shape matcher (D-20 B1, widened by D-21 B1).** Applied to the comment-stripped WHOLE
text of each file (so an expression split across lines still matches), `\s` spanning newlines. `ID`
below is an identifier-START operand, `[A-Za-z_$][\w.$]*`, so a numeric operand never matches:
- SHARPE (an annualised ratio), five forms:
  (S0) `\*\s*(?:periodsPerYear|ppy|sqrtN|ANNUAL|N)\s*\)\s*\/\s*[\w.]+`;
  (S1) `\)\s*\/\s*\(\s*[\w.]+\s*\*\s*(?:Math\.sqrt\(|sqrtN\b)`;
  (S2) `(?<!Math\.sqrt)\(\s*[\w.]+\s*\/\s*[\w.]+\s*\)\s*\*\s*Math\.sqrt\(`;
  (S3, D-21, unparenthesised `m / s * Math.sqrt(N)`) `(?<![\w.$)])ID\s*\/\s*ID\s*\*\s*Math\.sqrt\(`;
  (S4, D-21, unparenthesised `Math.sqrt(N) * m / s`) `Math\.sqrt\([^()]*\)\s*\*\s*ID\s*\/\s*ID(?![\w.$(])`.
- PEARSON, three forms: (P0) `\w*cov\w*\s*\/\s*Math\.sqrt\(` (case-insensitive);
  (P1, widened by D-21) `Math\.sqrt\(\s*ID\s*\*\s*ID\s*\)`, a product of two identifiers under a root,
  which also catches the two-step form `const denom = Math.sqrt(va * vb); ... cov / denom` (the
  `build-payload.ts` `pearsonCorr` shape, and the reference form `return-stats` `pearson` adopts);
  (P2) `\w*cov\w*\s*\/\s*\(\s*[\w.]+\s*\*\s*[\w.]+\s*\)` (case-insensitive).
- BETA: `\w*cov\w*\s*\/\s*var\w*` (case-insensitive).
**Measured at `df4d9921d` with exactly these regexes (a scratchpad script; D-21 B1): 26 HITS.** The
count is of MATCHES, not of T-labels: T5, T10, T12 and T18 are predicate-only sites with no ratio shape,
and one site can yield several hits. Breakdown by file and form:
- 20 in-class hits. SHARPE 10: `scenario-benchmark.ts` S0 (the information ratio, `/ te`), `joint.ts`
  S0 (`/ trackingError`), `portfolio-stats.ts` S0 (`* sqrtN) / s`), `sample-basis-ratios.ts` S0
  (`/ volatility`), `scenario.ts` S0 (`/ volatility`), `bootstrap.ts` S1, `compute.ts` S1,
  `og-metrics.ts` S1, `rolling.ts` S1 (`sqrtN`), `holding-compare-adapter.ts` S2. PEARSON 7:
  `widgets/risk/CorrelationMatrix.tsx` P1, `correlation-math.ts` P1, `build-payload.ts` P1 (the two-step
  `denom`, matched only since D-21), `scenario-benchmark.ts` P2, `allocator.ts` P2, `joint.ts` P2,
  `scenario.ts` P2. BETA 3: `joint.ts`, `rolling.ts`, `portfolio-stats.ts`.
- 6 out-of-class hits, all S0: Sortino in `compute.ts` (`ddDev`), `bootstrap.ts` (`downDev`),
  `rolling.ts` `rollingSortino` (`dd`), `sample-basis-ratios.ts` and `scenario.ts` (`downsideVol`);
  Treynor in `joint.ts` (`/ beta`).
- The two new SHARPE forms S3 and S4 match nothing at `df4d9921d`, and 0 false positives: peer-cohort's
  Box-Muller `Math.sqrt(9 * d)` (numeric operand), style-drift, every annualised vol and every tracking
  error do not match.
The executor re-measures this at the merge-base before writing (Task 1) and STOPs on any difference.
After plans 166.2-01 to 166.2-05 the 20 in-class hits are gone, and the expected count-pinned allowlist is the six
out-of-class hits: Sortino in `compute.ts`, `bootstrap.ts`, `rolling.ts` `rollingSortino`,
`sample-basis-ratios.ts` and `scenario.ts`, one each; Treynor in `joint.ts`, one. By design the widened
P1 flags ANY future product of two identifiers under a square root; a legitimate non-Pearson one gets a
count-pinned allowlist entry with its reason (the allowlist is the escape, never a narrower regex).
**Known unmatched (D-22; measured by the round-5 checker, re-measured by the planner with these exact
regexes, 0 hits each):** `mean(r) / stdDev(r) * Math.sqrt(N)`, `mean(r) * Math.sqrt(N) / stdDev(r)`,
`(mu * Math.sqrt(N)) / sd`, `Math.sqrt(N) * (m / s)`, `(m * 252) / s`,
`cov / (Math.sqrt(va) * Math.sqrt(vb))`, `covariance(a,b) / variance(b)`. The matcher is therefore not
a whole-language guard: a copy in one of these forms is caught only by D-17's other two layers, the
import rule (a Tier-2 file that stops importing `@/lib/return-stats`) and the retired-expression table.
The regexes are NOT widened in this round, because any widening moves the measured 26-hit pin and would
need a fresh measurement.
**Constraint on later phases (D-21 Info 2):** this gate constrains every later `src/` edit, including
Phase 169's and 167.x's plans that touch the Tier-2 files: a new ratio copy there fails CI until it
calls `@/lib/return-stats` or earns a reasoned allowlist entry. The template's `stripComments` drops any line whose
trimmed start is `*`, which would also drop a continuation line starting with a multiplication and hide a
split Sharpe, so this matcher strips block comments by span and line comments by a `//` not preceded by
`:` instead.

**Symbols this plan reads (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/scenario-backbone-gates.test.ts stripComments -->
<!-- verify-symbol: src/lib/scenario-backbone-gates.test.ts walkSource -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the D-17 compute-once gate - import rule, retired-expression table, whole-tree shape matcher with a count-pinned allowlist - liveness proven and neuter-drilled four ways</name>
  <files>src/lib/return-stats.single-source.test.ts</files>
  <read_first>
    - src/lib/scenario-backbone-gates.test.ts - the template: `// @vitest-environment node`, `walkSource`, `stripComments`, tokens built by concatenation so the gate cannot match itself, the self-exclusion path, the liveness fixture
    - .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-CONTEXT.md - D-17 (the classification table and the 169 split) and D-20 (B1)
    - `166.2-01-SUMMARY.md`, `166.2-02-SUMMARY.md`, `166.2-03-SUMMARY.md`, `166.2-04-SUMMARY.md`, `166.2-05-SUMMARY.md` - any private helper a plan KEPT with a recorded reason (the gate must not forbid a recorded exception; name it in a comment instead)
  </read_first>
  <behavior>
    - Import rule: each of the 16 Tier-2 site files exists and has a live (comment-stripped) import from `@/lib/return-stats`
    - Retired table: for each `{ file, token, plan }` row, the file exists (a renamed file must not make the rule vacuous) and the live code holds 0 occurrences of the token; the failure message names the file, the token and the plan
    - Whole-tree retirements: no live `correlation-math` import, no `rollingCorrelation` or `computeRollingMetric` token anywhere under `src/` (this file excluded), and `src/lib/correlation-math.ts` absent from disk
    - Whole-tree shapes: over every `.ts` / `.tsx` under `src/` except `*.test.ts(x)`, `__tests__/`, `fixtures/`, `__snapshots__/` and `src/lib/return-stats.ts`, every SHARPE / PEARSON / BETA match is covered by an allowlist entry; any other match fails with file, line, shape and the matched text
    - Allowlist: each entry is `{ file, shape, denominator, reason, count }` with a one-line comment saying why it is not a dispersion ratio (Sortino: downside deviation, RESEARCH A5; Treynor: divides by beta); an entry whose live match count differs from `count` fails as stale; the header comment says annualised volatility and tracking error are a dispersion times `Math.sqrt(N)`, not a ratio, so the shapes do not match them by design
    - Liveness (through the SAME matcher functions): a fixture for a NEW file `src/lib/new-feature.ts` holding `const sharpe = sd > 0 ? (mu * periodsPerYear) / (sd * Math.sqrt(periodsPerYear)) : 0;` yields one unallowlisted SHARPE hit (S1); the one-step Pearson `const corr = cov / Math.sqrt(varA * varB);` yields exactly two hits, P0 and P1, and the test asserts that pair and nothing else (D-22); a beta (`cov / varX`) and a Sharpe split across two lines each yield a hit; per D-22 the three forms that had no positive fixture get one, each yielding exactly one hit of that form (measured by the planner with the context block's regexes): `const ratio = (mu * periodsPerYear) / sd;` (S0), `const sharpe = (mu / sd) * Math.sqrt(periodsPerYear);` (S2) and `const corr = cov / (sa * sb);` (P2); per D-21 each new form has its own positive fixture, each yielding exactly one hit: the two-step Pearson `const denom = Math.sqrt(va * vb);` then `return denom > 0 ? cov / denom : NaN;` (P1), `const sharpe = mu / sd * Math.sqrt(periodsPerYear);` (S3) and `const sharpe = Math.sqrt(periodsPerYear) * mu / sd;` (S4); `const annVol = sd * Math.sqrt(periodsPerYear);`, `stdDev(diff, true) * Math.sqrt(periodsPerYear)`, `const c = 1 / Math.sqrt(9 * d);` (a numeric operand, peer-cohort's Box-Muller), `const x = Math.sqrt(periodsPerYear) * sd / 100;` and a Sharpe inside a comment yield none; a fixture allowlist entry with count 1 over a text holding no match is reported stale; a fixture with a live `correlation-math` import and a private `function pstdev` trips the retired matchers and a comment-only mention does not
    - Merge-base measurement through the committed matcher (D-22): the whole-tree scan is one function taking a root directory; the live gate calls it on `src/`, and a `describe.runIf(process.env.QZ_166_1_07_SCAN_ROOT)` block named `merge-base tree` calls the SAME function on that root, asserts exactly 26 hits of which 20 are not allowlisted and 6 are allowlisted, prints every hit as file, form and matched text, and only after the assertions pass prints the line `shape-scan-base: hits=26 unallowlisted=20 allowlisted=6`
    - The header comment states what the whole-tree matcher matches and what it does not (D-22): it lists the nine shipped forms (S0-S4, P0-P2, BETA), then a KNOWN-UNMATCHED list of exactly these seven forms: `mean(r) / stdDev(r) * Math.sqrt(N)`, `mean(r) * Math.sqrt(N) / stdDev(r)`, `(mu * Math.sqrt(N)) / sd`, `Math.sqrt(N) * (m / s)`, `(m * 252) / s`, `cov / (Math.sqrt(va) * Math.sqrt(vb))`, `covariance(a,b) / variance(b)`; it says the matcher is one of three layers of D-17's guard, the other two being the import rule and the retired-expression table, and that the regexes were not widened in D-22 because widening would move the measured 26-hit pin (build those literals by concatenation or keep them in comment text the matcher strips, so the gate cannot match itself)
    - The header comment names D-17 and the founder's sentence, D-20 B1, that T14's persisted-value read is Phase 169 plan 04's and its computed arm is in the import list (D-19), and that T18 (`comparator-block.ts`) is fixed at its source and why it is not in the import list
  </behavior>
  <action>
    Per D-17 and D-20 B1. Run `git fetch origin main` and `node scripts/verify-plan-anchors.mjs .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-06-PLAN.md` first (halt on non-zero). Before writing, re-measure: for every row of the retired table, count the token in the file at `"$(git merge-base HEAD origin/main)"` (via `git show <base>:<file>`) and at HEAD; record both columns in the SUMMARY. Every row must read at least 1 at the base (a 0 there is a dead token: STOP and fix the row, never ship it) and 0 at HEAD (a non-zero at HEAD is an unfinished site: STOP and report). Then run the context block's shape regexes (all nine: S0-S4, P0-P2, BETA) over the merge-base tree (`git archive` of `src/` into a `mktemp -d` directory, a scratchpad script is fine) and require exactly the context block's 26 hits with its 20 + 6 breakdown by file and form (a different count or file is a STOP: report it, never re-pin silently); then over the HEAD tree and record every hit; each HEAD hit must be one of the six expected allowlist lines, else STOP and report (a new in-class shape has an owner decision to make, not an allowlist entry). Write `src/lib/return-stats.single-source.test.ts` on the `scenario-backbone-gates.test.ts` template (write the helpers locally; do not edit that file), with the retired table and the allowlist as data and every forbidden token built by concatenation. Run it GREEN. Then (D-22) run the COMMITTED file's own matcher over the merge-base tree, the second verify command below: `git archive` of the merge-base `src/` into a `mktemp -d` directory, then the test with `QZ_166_1_07_SCAN_ROOT` pointing at that `src/`; it must print `shape-scan-base: hits=26 unallowlisted=20 allowlisted=6`, and its per-hit list must agree by file and form with the context block's breakdown (a difference is a STOP, as for the scratchpad measurement; the scratchpad run does not replace this one). Record the printed line and the per-hit list in the SUMMARY. Drills, each with a byte backup, the gate observed RED with the failing test name recorded, `cp` restore, `cmp`, GREEN again: (1) delete the `@/lib/return-stats` import line of `src/lib/factsheet/rolling.ts` (the import rule; tsc errors are irrelevant, the gate reads text); (2) append a live `function pearson(a: number[], b: number[]) { return 0; }` line to `src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx` (the retired table); (3) create an untracked `src/lib/zz-166-1-07-drill.ts` holding the liveness fixture's Sharpe line (the whole-tree matcher; the drill ends with `rm` of that file and `git status --porcelain` showing it gone, not a `cmp`); (4) re-insert the old `s > 0 ? (m * periodsPerYear) / (s * Math.sqrt(periodsPerYear)) : NaN` Sharpe line into `computeOgHeadline` in `src/lib/factsheet/og-metrics.ts` (both the retired table and the whole-tree matcher must fail; record both test names). Remove the node_modules link and commit the test alone as `test(166.2-06): pin the compute-once rule for dispersion ratios (D-17)`.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/return-stats.single-source src/lib/scenario-backbone-gates && test ! -e src/lib/zz-166-1-07-drill.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", or the drill file still on disk</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && git fetch -q origin main && QZ_BASE="$(git merge-base HEAD origin/main)" && QZ_D="$(mktemp -d)" && git archive -o "$QZ_D/base.tar" "$QZ_BASE" src && tar -x -C "$QZ_D" -f "$QZ_D/base.tar" && { QZ_166_1_07_SCAN_ROOT="$QZ_D/src" ./node_modules/.bin/vitest run src/lib/return-stats.single-source -t 'merge-base tree' > "$QZ_D/scan.out" 2>&1; QZ_RC=$?; cat "$QZ_D/scan.out"; test "$QZ_RC" = 0; } && grep -q 'shape-scan-base: hits=26 unallowlisted=20 allowlisted=6' "$QZ_D/scan.out"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or the `shape-scan-base:` line absent or carrying any other numbers (a skipped block prints no line, so it fails too)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'D-17' src/lib/return-stats.single-source.test.ts` prints at least 1 and `grep -c 'D-20' src/lib/return-stats.single-source.test.ts` prints at least 1
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- src/lib/scenario-backbone-gates.test.ts` is empty
    - the committed test holds the D-22 positive fixtures `(mu * periodsPerYear) / sd` (S0), `(mu / sd) * Math.sqrt(periodsPerYear)` (S2) and `cov / (sa * sb)` (P2), each asserted to exactly one hit of its form, and the one-step Pearson fixture asserted to exactly the two hits P0 and P1
    - the header comment carries the seven-form known-unmatched list and names the import rule and the retired-expression table as the other two layers of D-17's guard: `grep -c 'D-22' src/lib/return-stats.single-source.test.ts` prints at least 1
    - the SUMMARY quotes the `shape-scan-base: hits=26 unallowlisted=20 allowlisted=6` line from the committed matcher's merge-base run, beside its per-hit list (D-22)
    - the SUMMARY records the retired table with its base and HEAD counts (every base count at least 1, every HEAD count 0), the whole-tree shape hits at the merge-base (26: the 20 + 6 breakdown by file and form, per D-21) and at HEAD (only allowlisted lines), and the four drill rows (the neuter, the RED test name or names, the restore proof)
  </acceptance_criteria>
  <done>A second copy of a dispersion ratio in any of the nine matched forms anywhere under src/, or the return of any retired expression, fails CI, and the committed matcher reports 26 hits on the merge-base tree; the matcher's liveness is proven in the file and the gate was observed red on four neuters.</done>
</task>

<task type="auto">
  <name>Task 2: merge origin/main and re-prove T1-T20 and the gate on the merged tree (D-20 W1)</name>
  <files>none of this plan's own; the merge commit brings origin/main's files, and a conflict is resolved only inside a 166.2 hunk</files>
  <read_first>
    - .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-CONTEXT.md - D-17 "Split with Phase 169" and "Other couplings", D-19, D-20 W1
    - `git log --oneline HEAD..origin/main -- src/lib/factsheet/compute.ts src/lib/factsheet/build-payload.ts src/lib/factsheet/og-metrics.ts src/lib/factsheet/__snapshots__ src/lib/scenario.ts "src/app/(dashboard)/compare/lib/holding-compare-adapter.ts"` - which of Phase 169 PR 1 (compute.ts, build-payload.ts and its snapshot, og-metrics.ts `computeOgHeadline`) and 167.1.2 plans 06 and 11 (scenario.ts, holding-compare-adapter.ts) have landed
  </read_first>
  <action>
    Per D-20 W1. `git fetch origin main`; record `git rev-parse HEAD` (PRE_MERGE) and `git rev-parse origin/main` (MERGED_BASE). If `git merge-base --is-ancestor origin/main HEAD` exits 0, record "already up to date at MERGED_BASE" and skip to the re-proof. Otherwise `git merge --no-ff origin/main` with the message `Merge origin/main into this phase's branch before the release sweep` (no CI-skip trailer token anywhere). On a conflict: keep origin/main's side for every hunk this phase does not own, and inside a 166.2 hunk keep the call to the shared `@/lib/return-stats` function applied to whatever main's code now looks like (for example, if Phase 169 plan 04's persisted branch restructured `computeOgHeadline`, its computed arm still ends in `sharpe(values, { periodsPerYear, ddof: 0 }) ?? NaN`); a snapshot conflict is NOT resolved by regenerating the snapshot: take main's snapshot and let the re-proof decide. Record every conflicted path and how it was resolved. Re-proof on the merged tree: the two verify commands below, then re-run the whole-tree shape measurement from Task 1; a new hit brought in by main that is in-class (a Sharpe, Pearson or beta over a dispersion) is a STOP for the orchestrator (a new copy has an owner decision), an out-of-class one (for example a new Sortino) gets a count-pinned allowlist entry with its reason, committed as its own `test(166.2-06)` commit. Remove the node_modules link before every commit.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && git fetch -q origin main && git merge-base --is-ancestor origin/main HEAD && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/return-stats src/lib/factsheet src/lib/scenario src/lib/sample-basis-ratios src/lib/diversification src/lib/portfolio-stats src/lib/scenario-backbone-gates src/components/strategy "src/app/(dashboard)/compare" "src/app/(dashboard)/allocations" src/app/api/og "src/app/factsheet"</automated>
    <fails_when>non-zero exit (127 = FAIL; exit 1 from merge-base means origin/main is not merged), vitest " failed" in its summary, "No test files found", or any "obsolete" / "mismatched" snapshot line</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit</automated>
    <fails_when>non-zero exit (127 = FAIL) or any tsc error line</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY records PRE_MERGE, MERGED_BASE, the merge commit sha (or "already up to date"), which of Phase 169 PR 1 and 167.1.2 plans 06 / 11 were in MERGED_BASE (from the `git log` in read_first), every conflicted path with its resolution, and the summary lines of both verify commands
    - `git diff --stat MERGED_BASE HEAD -- src/lib/factsheet/__snapshots__/` is empty (the phase moves no snapshot, on main's snapshot too)
    - `git log -1 --format=%B <merge sha>` matches none of the six CI-skip token forms listed in CLAUDE.md under a case-insensitive grep
    - `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>The branch contains origin/main, and every T1-T20 site test, the compute-once gate and tsc are green on the merged tree at a recorded base sha.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future edits -> the TS ratio sites | a second copy of a formula reintroduces the drift D-17 removed |
| origin/main -> this branch | a concurrent phase's edit to a shared file can silently undo a 166.2 site or move a snapshot |
| merge commit message -> CI | a skip token suppresses CI even when quoted |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.2-32 | Tampering (integrity of a displayed number, regression) | every TS file under src/ | medium | mitigate | import rule + 28-pair retired table + whole-tree shape matcher with a count-pinned allowlist; liveness fixtures; four neuter drills |
| T-166.2-33 | Tampering (cross-phase clobber at merge) | compute.ts, build-payload.ts, og-metrics.ts, scenario.ts, holding-compare-adapter.ts | medium | mitigate | merge origin/main before the sweep; resolve only inside 166.2 hunks; re-run T1-T20, the gate and tsc on the merged tree; snapshot directory unchanged |
| T-166.2-12b | Tampering (CI suppression) | merge commit message | high | mitigate | acceptance greps the merge message for all six token forms |
</threat_model>

<verification>
- The Task 1 verify and both Task 2 verify commands exit 0.
</verification>

<success_criteria>
- The compute-once rule is a CI gate that catches a returned retired expression and a new ratio copy in any of the nine matched forms anywhere under src/ (the seven known-unmatched forms are named in the test header, D-22), proven live and neuter-drilled; the branch holds origin/main and is green on the merged tree at a recorded base sha.
</success_criteria>

<output>
Create `.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-06-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
