---
phase: 166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta
plan: 02
type: execute
wave: 2
depends_on: ["166.2-01"]
files_modified:
  - src/lib/correlation-math.ts
  - src/lib/correlation-math.test.ts
  - src/lib/return-stats.pearson-contract.test.ts
  - src/components/strategy/CompareCorrelationMatrix.tsx
  - src/components/strategy/CompareCorrelationMatrix.residue.test.tsx
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx
  - src/lib/diversification.ts
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "There is ONE TS Pearson correlation: correlation-math.ts is deleted, /compare's correlation matrix and the allocator CorrelationMatrix widget call return-stats' pearson, and the widget's local pearson is gone (D-17)"
    - "The dead rollingCorrelation (0 production callers, measured 2026-09-25, re-measured before deletion) is deleted with its describe block (D-17)"
    - "On a compounding-NAV constant yield, /compare's pair cell is null (the flat-window gap, as for an all-zero leg) and the CorrelationMatrix widget renders the same cell as for an all-zero leg (D-07, D-17)"
    - "G11.E.5's null contract for an undefined correlation is kept: its test cases moved with pearson, unchanged in what they assert (D-17)"
    - "diversification.ts's module doc no longer names correlation-math.ts::pearson; the sentence that replaces it is true of the code and the test at edit time (D-20 I2)"
    - "Every site test was written first and observed RED on the unedited site, and every site was neutered, observed RED and restored from a byte backup proven with cmp"
  artifacts:
    - path: src/lib/return-stats.pearson-contract.test.ts
      provides: "the G11.E.5 pearson contract cases, moved from correlation-math.test.ts and pointed at return-stats"
    - path: src/components/strategy/CompareCorrelationMatrix.residue.test.tsx
      provides: "the T6 render test"
    - path: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx
      provides: "the T7 render test"
  key_links:
    - from: src/components/strategy/CompareCorrelationMatrix.tsx
      to: src/lib/return-stats.ts
      via: "import { pearson } from @/lib/return-stats"
      pattern: "@/lib/return-stats"
    - from: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
      to: src/lib/return-stats.ts
      via: "the widget's cells call the shared pearson"
      pattern: "@/lib/return-stats"
---

<objective>
Second slice of D-17 (compute once), first half (D-20 W2 split): the two correlation-matrix sites (CONTEXT
D-17 rows T6, T6b, T7) stop carrying their own Pearson and call plan 166.2-01's `src/lib/return-stats.ts`
`pearson`; `correlation-math.ts` is deleted with its dead `rollingCorrelation`.

Purpose: `correlation-math.ts` `pearson` and the CorrelationMatrix widget's private `pearson` are two
copies of one formula, each dividing by a dispersion that is about 1.3e-16 on a constant yield. After
this plan the TS side has one Pearson.
Output: one module deleted (its contract tests moved), two sites on the shared module, two new render
tests, one doc sentence corrected.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-CONTEXT.md
@.planning/phases/166.1-qstatsrecompute/166.1-RESEARCH.md
Also read `.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-01-SUMMARY.md` (the final `return-stats.ts` API at
its top, and the fixture's exports). This plan does NOT edit `src/lib/return-stats.ts`,
`src/lib/return-stats.test.ts` or `src/lib/return-stats.sites.test.ts` (plan 166.2-01's files). Plans 166.2-03, 166.2-04
and 166.2-05 run in the same wave and share no file with this plan. An API gap in `return-stats.ts` is a
STOP-and-report, not a local helper.

**Environment, drill rule, the D-07 invariant in TS and the never-edit-a-golden rule** are exactly plan
166.2-01's: MAIN checkout's `node_modules` linked only for the run and removed before every commit; exit 127 is
a FAIL; quote every `(dashboard)` path; `tsc` in the background with a 600 s watchdog; byte backups under
a `166.2-02` scratchpad name, neuter, RED, `cp`, `cmp`; each test computes the site's output on the
constant yield AND on an all-zero series and asserts the ratio field identical. Never `git checkout --`,
`git restore`, `git stash`. Never quote a retired expression verbatim in an updated code comment: this plan's acceptance greps count comment lines. A deletion is a `git rm` of a tracked file, recorded in the SUMMARY.

**Ownership (CONTEXT D-17).** Phase 169 plan 08 feeds the Risk tab's widgets the per-key book and drops the
precomputed legacy correlation matrix; it asserts it edits nothing under `widgets/risk/`. This plan edits
neither `RiskTabPanel.tsx` nor `book-risk-input.ts`, and keeps the widget's `precomputed` arm untouched.

**Dead code, measured 2026-09-25.** `rollingCorrelation` has no importer outside
`src/lib/correlation-math.test.ts` (its only consumer, `CorrelationWithBenchmark`, reads the persisted
`btc_rolling_correlation_90d` since audit G11.A P64). Re-measure before deleting; if it has gained a
production importer, STOP and report.

**Symbols this plan edits (cite by symbol; line numbers drift).**
<!-- verify-symbol: src/lib/correlation-math.ts pearson -->
<!-- verify-symbol: src/lib/correlation-math.ts rollingCorrelation -->
<!-- verify-symbol: src/components/strategy/CompareCorrelationMatrix.tsx pearson -->
<!-- verify-symbol: src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx pearson -->
<!-- verify-symbol: src/lib/diversification.ts correlation-math.ts::pearson -->
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): one Pearson - correlation-math.ts deleted, /compare's matrix on return-stats' pearson end-to-end (T6), the dead rollingCorrelation gone, the diversification doc corrected, red first</name>
  <files>src/lib/correlation-math.ts, src/lib/correlation-math.test.ts, src/lib/return-stats.pearson-contract.test.ts, src/components/strategy/CompareCorrelationMatrix.tsx, src/components/strategy/CompareCorrelationMatrix.residue.test.tsx, src/lib/diversification.ts</files>
  <read_first>
    - src/lib/correlation-math.ts - `pearson` (the G11.E.5 doc comment) and `rollingCorrelation`
    - src/lib/correlation-math.test.ts - the `pearson` and `rollingCorrelation` describe blocks
    - src/components/strategy/CompareCorrelationMatrix.tsx - how it calls `pearson` and renders a null cell (the em-dash); src/app/(dashboard)/compare/page.test.tsx for how a `CompareItem` is built
    - src/lib/return-stats.ts and src/__tests__/fixtures/dispersion-nav.ts - plan 166.2-01's API
    - src/components/charts/CorrelationWithBenchmark.tsx - the G11.A P64 doc block (why `rollingCorrelation` is dead; read only)
    - src/lib/diversification.ts - the module doc paragraph naming `correlation-math.ts::pearson`; src/lib/diversification.test.ts (measured 2026-09-26: it does not import any pearson)
  </read_first>
  <behavior>
    - T6: `CompareCorrelationMatrix` rendered with one constant-yield pick and one noisy pick shows the same cell for that pair as with an all-zero pick (the flat-window null); two noisy picks keep a finite cell
    - The G11.E.5 contract cases from `correlation-math.test.ts`'s `pearson` block pass UNCHANGED in what they assert when pointed at `@/lib/return-stats`
    - No live code imports `correlation-math` after the change
  </behavior>
  <action>
    Per D-17 and D-07. Step 0: `node scripts/verify-plan-anchors.mjs .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-02-PLAN.md`, halt on non-zero. Re-measure the dead-code premise (`grep -rn "rollingCorrelation" src` excluding `correlation-math*`); STOP if a production importer exists. Write the T6 render test first in `src/components/strategy/CompareCorrelationMatrix.residue.test.tsx` (jsdom, the repo default) and observe RED against today's `correlation-math` `pearson` behind `CompareCorrelationMatrix`. Then: `git mv src/lib/correlation-math.test.ts src/lib/return-stats.pearson-contract.test.ts`, change its import to `@/lib/return-stats`, and delete its `rollingCorrelation` describe block (the function is dead); leave every `pearson` case's assertion as it is. `git rm src/lib/correlation-math.ts`. Point `CompareCorrelationMatrix.tsx` at `import { pearson } from "@/lib/return-stats"`. Per D-20 I2, in `src/lib/diversification.ts` replace ONLY the module-doc sentence that names `correlation-math.ts::pearson` with a sentence that is true at edit time (re-measure `diversification.test.ts` first: if it calls no pearson, say the consistency test rebuilds the correlation from this lib's own covariance and sigma, and that the one TS Pearson is `return-stats.ts` `pearson`); change nothing else in that file. Drill T6: byte backup of `CompareCorrelationMatrix.tsx`, replace the import with a local copy of the OLD `denom > 0` pearson, RED, `cp` restore, `cmp`.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/return-stats src/lib/diversification src/components/strategy "src/app/(dashboard)/compare" && test ! -e src/lib/correlation-math.ts && test -z "$(grep -rln --include='*.ts' --include='*.tsx' 'correlation-math' src)"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, "No test files found", `correlation-math.ts` still on disk, or any file under `src/` still naming `correlation-math` (import or comment)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git diff --name-status "$(git merge-base HEAD origin/main)" -- src/lib/correlation-math.ts` shows `D`, and the moved test file exists with no `rollingCorrelation` token
    - `grep -c '@/lib/return-stats' src/components/strategy/CompareCorrelationMatrix.tsx` prints 1
    - `git diff "$(git merge-base HEAD origin/main)" -- src/lib/diversification.ts` touches only the module doc comment (every changed line starts with ` *` after the diff marker)
    - the SUMMARY records the T6 RED ids, the dead-code re-measurement, the diversification doc re-measurement, and the T6 drill row
  </acceptance_criteria>
  <done>The TS side has one Pearson correlation; /compare's matrix reads it and shows no residue correlation, proven by a test observed red; no doc under src/ names the deleted module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: T7 - the CorrelationMatrix widget on the shared pearson, then the plan's vitest, tsc and eslint gates</name>
  <files>src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx, src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx - the local `pearson`, the `precomputed` arm, and how cells render; CorrelationMatrix.boundary.test.tsx and CorrelationMatrix.all-columns.test.tsx for the render style (both must pass unedited)
  </read_first>
  <behavior>
    - T7: rendering the widget with one constant-yield leg produces the same cell text and colour for that leg's pairs as rendering it with an all-zero leg; a noisy pair is unchanged
  </behavior>
  <action>
    Per D-17 and D-07. Write the T7 test first in `CorrelationMatrix.residue.test.tsx` (through render, in the style of the boundary test), observe RED, record the ids. Delete the widget's local `pearson` and call `pearson(av, bv) ?? 0` from `@/lib/return-stats` (0 is what the widget emits today for an exactly constant leg); keep the `precomputed` arm untouched (169 plan 08 decides what feeds it). Drill T7 (byte backup, restore the old local `pearson`, RED, `cp` restore, `cmp`). Then run the gates below. Record for T6 and T7 that the fixed output is one the site already emits for an exact constant, so no renderer changes.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/lib/return-stats src/lib/diversification src/components/strategy "src/app/(dashboard)/compare" "src/app/(dashboard)/allocations/widgets"</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, or "No test files found"</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint src/lib/return-stats.pearson-contract.test.ts src/lib/diversification.ts src/components/strategy/CompareCorrelationMatrix.tsx src/components/strategy/CompareCorrelationMatrix.residue.test.tsx "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx" "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx"</automated>
    <fails_when>non-zero exit (127 = FAIL), any tsc error line, or any eslint error</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '@/lib/return-stats' "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx"` prints at least 1, and `grep -c 'function pearson' "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx"` prints 0
    - `git diff --stat "$(git merge-base HEAD origin/main)" -- "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.boundary.test.tsx" "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.all-columns.test.tsx"` is empty
    - the SUMMARY carries the T6-T7 drill table and the per-site "already emitted" line
    - `git diff --stat -- analytics-service/ supabase/ src/lib/return-stats.ts` is empty for this plan's commits, and `test -L node_modules` is false at every commit
  </acceptance_criteria>
  <done>Both correlation matrices compute through the one return-stats pearson and emit, on a constant yield, what they emit on an all-zero leg; vitest, tsc and eslint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| daily returns -> TS-computed correlation -> /compare and the allocator Risk tab | a fabricated correlation renders to an allocator |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.2-23 | Tampering (integrity of a displayed number) | T6-T7 | medium | mitigate | one shared pearson; red test per site; D-07 equality with the all-zero input; drill table |
| T-166.2-30 | Repudiation (a contract lost in a deletion) | correlation-math.ts removal | low | mitigate | the G11.E.5 pearson cases move with the function via `git mv`, assertions unchanged; the dead-code premise is re-measured before the deletion |
</threat_model>

<verification>
- `node scripts/verify-plan-anchors.mjs .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-02-PLAN.md` exits 0 before Task 1 starts.
- Both Task 2 verify commands exit 0.
</verification>

<success_criteria>
- D-17, second slice, first half: one Pearson serves T6-T7; the dead rolling correlation is gone; each site has a test observed red.
</success_criteria>

<output>
Create `.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-02-SUMMARY.md` when done. Remove the node_modules
link, stage the SUMMARY, then run
`QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && test -x "$QZ_MAIN/node_modules/.bin/tsx" && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK before committing.
</output>
