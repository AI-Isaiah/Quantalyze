---
phase: 166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta
plan: 07
type: execute
wave: 4
depends_on: ["166.2-06"]
files_modified:
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: true
requirements: [SC-3]
user_setup: []

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every gate the phase touched is green at one recorded SHA that contains origin/main: the full vitest suite, tsc, eslint over the union of the TS files plans 166.2-01 to 166.2-06 touched, the plan anchor verifier in CI's --pending form, and planning hygiene (D-18 W2, D-20 B2, D-20 W1). The Python gates (pytest, mypy, ruff, the qstats-gate census) are Phase 166.1's release sweep: this phase touches no Python file (split 2026-09-26, 166.1-CONTEXT D-23)"
    - "The phase ships from its OWN branch, feat/166.2, cut from feat/166.1 at the 2026-09-26 split; the branch's diff against origin/main (three-dot) names no path under the Python service directory and none under the Supabase directory, checked by a command that prints the offending paths and exits non-zero (166.1-CONTEXT D-23, revision 2026-09-26 separate branches)"
    - "VERSION and package.json carry the SAME 4-digit string, bumped on MEANING (user-visible values change) as a minor-level bump derived from origin/main's VERSION re-read at release time (second component + 1, last two zeroed), so whichever of 166.1 and 166.2 merges second never reuses the first's version; a command asserts the derivation, and the ship step re-runs it immediately before the PR opens; npm version is never run (D-18, D-23 revision)"
    - "ONE unified CHANGELOG entry covers EVERY one of 166.2's OWN commits (the commits on this branch reachable from neither origin/main nor feat/166.1), each mapped to at least one bullet, in the repo's own section vocabulary; the planning-only commits inherited from feat/166.1 at the cut are listed separately, proven planning-only, and claimed by no bullet; it records the D-17 / D-19 split with Phase 169 and that the Python floor and PROD recompute ship as Phases 166.1 and 166.3 (split 2026-09-26, D-23 revision)"
    - "No commit message on the branch contains a CI-skip trailer token, not even in a sentence denying it: a command sweeps EVERY message in origin/main..HEAD, merge messages included (D-23 revision)"
  artifacts:
    - path: VERSION
      provides: "next minor 4-digit version"
    - path: package.json
      provides: "version byte-equal to VERSION"
    - path: CHANGELOG.md
      provides: "one unified entry for Phase 166.2 at the top"
  key_links:
    - from: VERSION
      to: package.json
      via: "src/__tests__/critical-regressions.test.ts CRITICAL-02 asserts byte equality"
      pattern: "\"version\""
---

<objective>
Run the whole-phase gate sweep at one SHA that already contains `origin/main` (plan 166.2-06 merged it), and
write the single `chore(release)` commit: VERSION, package.json and one unified CHANGELOG entry that maps
every branch commit.

Purpose: the repo rule (CLAUDE.md "CHANGELOG discipline") is that every ship writes an entry in the same
commit as the VERSION bump, built from an enumerated commit checklist. This plan waits on the code
plans (through plan 166.2-06). The Python half (Phase 166.1) and the PROD recompute (Phase 166.3) ship
separately since the 2026-09-26 split, so this sweep and entry cover the TS half only. Per D-20 B2 the
anchor check is CI's `--pending` form: executed plans cite symbols this phase deletes by design.
The phase runs on its own branch, `feat/166.2`, cut from `feat/166.1` at the 2026-09-26 split and
created by the orchestrator (166.1-CONTEXT D-23, revision "separate branches"); `feat/166.1` carries
Phase 166.1 alone. This release therefore claims only 166.2's own commits and must carry no Python or
database path.
Output: one release commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-CONTEXT.md
@VERSION
Also read `166.2-01-SUMMARY.md`, `166.2-02-SUMMARY.md`, `166.2-03-SUMMARY.md`, `166.2-04-SUMMARY.md`,
`166.2-05-SUMMARY.md` and `166.2-06-SUMMARY.md` (its MERGED_BASE), in this phase directory.

**Environment.** TS tools through the main checkout's `node_modules`, linked only for the run and
removed before every commit when it is a symlink (exit 127 = FAIL). No Python run, no database, no
credential.
Run `git fetch origin main` before ANY `git merge-base` against `origin/main`: a stale local ref makes
the merge-base, the version comparison and the commit checklist wrong without an error.

**Main moved since plan 166.2-06?** If, after `git fetch origin main`, `git merge-base --is-ancestor origin/main HEAD`
exits non-zero, `main` has moved since plan 166.2-06's merge: re-run plan 166.2-06 Task 2 (merge and re-prove, with
its SUMMARY row appended) before this plan's sweep. The sweep's SHA must contain `origin/main`.

**Branch (166.1-CONTEXT D-23, revision 2026-09-26 "separate branches").** This plan runs on `feat/166.2`
only; the orchestrator cut it from `feat/166.1` at the split commit. Two consequences, both enforced by a
command below. (a) The branch must carry no Python-service or database path: the three-dot diff against
`origin/main` is checked for both directory prefixes, and a hit is a STOP (it means 166.1 work reached
this branch, or `origin/main` was merged incompletely). (b) The branch inherits `feat/166.1`'s
planning-only commits from before the cut, and after 166.1 squash-merges they still sit in
`origin/main..HEAD`. **166.2's OWN commits** are therefore
`git log HEAD --no-merges --oneline --not origin/main feat/166.1` (ancestry exclusion, which stays correct
after either branch merges `origin/main`; use `origin/feat/166.1` if the local ref is gone, and if
neither resolves, STOP and ask the orchestrator to restore the ref). The **inherited** commits are the
rest of `git log origin/main..HEAD --no-merges`; each must be planning-only
(`git show --name-only --format= <sha> | grep -v '^\.planning/'` prints nothing, the same exemption
`scripts/check-version-bump.mjs` `judge` grants a planning-only change) and is listed separately in the
SUMMARY, mapped to no bullet. An inherited commit that touches a path outside `.planning/` is a STOP.

**Version (CONTEXT D-18, Info, as amended by D-19 and the D-23 revision).** Never take a version number
from this plan, and never from this branch's own VERSION file. At release time run
`git fetch origin main`, re-read `origin/main`'s `VERSION` (`git show origin/main:VERSION`), increment the
second component and zero the last two. Phases 166.1 and 166.2 release on separate branches, and
whichever merges second must not reuse the first's version: the derivation is asserted by a Task 2
verify command, and the ship step re-runs that command immediately before the PR opens, with nothing
in between. If it goes red (the sibling merged after this release commit), re-run Task 2 as a NEW
`chore(release)` commit that re-derives the version and replaces this entry's heading; never amend,
never rebase. Record the `origin/main` reading, this branch's reading (lineage only) and the result in
the SUMMARY. (Lineage only, not an input: at the 2026-09-26 revision `origin/main` read `0.93.0.2`
and this branch `0.92.0.0`; main moves again before this plan runs, and Phase 166.1 may ship first.)

**The file union (named literally; zsh does not word-split an unquoted variable).**
- TS, plans 166.2-01 to 166.2-06 (files that exist after the phase; `correlation-math.ts` and its test were deleted or
  moved by plan 166.2-02): the 16 Tier-2 site files listed in plan 166.2-06's context, plus `src/lib/return-stats.ts`,
  `src/lib/return-stats.test.ts`, `src/lib/return-stats.sites.test.ts`, `src/lib/return-stats.sites-risk.test.ts`,
  `src/lib/return-stats.pearson-contract.test.ts`, `src/lib/return-stats.single-source.test.ts`,
  `src/__tests__/fixtures/dispersion-nav.ts`, `src/components/strategy/CompareCorrelationMatrix.residue.test.tsx`,
  `src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx`,
  `src/lib/portfolio-stats.test.ts`, `src/lib/factsheet/return-stats.sites.test.ts`,
  `src/lib/factsheet/return-stats.sites-joint.test.ts`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): whole-phase gate sweep at one SHA containing origin/main, recorded verbatim</name>
  <precondition>The current branch is feat/166.2 (`git rev-parse --abbrev-ref HEAD` prints `feat/166.2`), created by the orchestrator from feat/166.1 at the 2026-09-26 split; on any other branch, STOP.</precondition>
  <files>none - read-only sweep; results go to the SUMMARY</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline", "CI gate integrity", "Completion is HISTORICAL"
    - .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-VALIDATION.md - the per-task map
    - .planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-06-SUMMARY.md - MERGED_BASE
  </read_first>
  <action>
    Per D-18 W2, D-20 B2 and D-20 W1. Run `git fetch origin main` first and apply the context's "Main moved since plan 166.2-06?" rule. Record `git rev-parse HEAD` as SWEEP_SHA and `git merge-base HEAD origin/main` as SWEEP_BASE, then run every verify command below (the third is the context's branch rule (a): it prints any Python-service or database path the branch carries, and a printed path is a STOP to report, never a file to revert here) and paste the final summary line of each into the SUMMARY, plus: the anchor verifier's `scanned:` and `OK:` lines, and a grep over this phase directory for the two verification fingerprint keys CLAUDE.md bans (expect zero). The anchor check is `--pending` (CI's `plan-anchor-verify` form) and NOT a glob over this phase's plans: executed plans 166.2-01 to 166.2-05 cite symbols this phase deletes on purpose (`correlation-math.ts` `pearson`, `compute.ts` `pstdev`), so re-checking them would exit 1 forever; a pending plan of ANOTHER phase that cites a symbol this phase deleted is a real cross-phase finding: STOP and report it with the plan path and the symbol. Any red result stops the plan; do not release on red. Long runs go in the background with a 600 s watchdog.
  </action>
  <verify>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run && ./node_modules/.bin/tsc --noEmit && ./node_modules/.bin/eslint "src/app/(dashboard)/compare/lib/holding-compare-adapter.ts" src/lib/sample-basis-ratios.ts src/lib/scenario.ts src/lib/diversification.ts src/components/strategy/CompareCorrelationMatrix.tsx "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx" src/lib/portfolio-stats.ts "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts" "src/app/(dashboard)/allocations/lib/scenario-stress.ts" src/lib/factsheet/compute.ts src/lib/factsheet/bootstrap.ts src/lib/factsheet/joint.ts src/lib/factsheet/rolling.ts src/lib/factsheet/build-payload.ts src/lib/factsheet/allocator.ts src/lib/factsheet/og-metrics.ts src/lib/return-stats.ts src/lib/return-stats.test.ts src/lib/return-stats.sites.test.ts src/lib/return-stats.sites-risk.test.ts src/lib/return-stats.pearson-contract.test.ts src/lib/return-stats.single-source.test.ts src/__tests__/fixtures/dispersion-nav.ts src/components/strategy/CompareCorrelationMatrix.residue.test.tsx "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx" src/lib/portfolio-stats.test.ts src/lib/factsheet/return-stats.sites.test.ts src/lib/factsheet/return-stats.sites-joint.test.ts</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" in its summary, any tsc error line, or any eslint error over the plan 166.2-01 to 166.2-06 union</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && git fetch -q origin main && git merge-base --is-ancestor origin/main HEAD && node scripts/verify-plan-anchors.mjs --pending && "$QZ_MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit (127 = FAIL; exit 1 from merge-base means main moved and plan 166.2-06 Task 2 must be re-run), or the anchor verifier prints "FAIL:" or does not end with an "OK:" line, or a hygiene violation line</fails_when>
    <automated>test "$(git rev-parse --abbrev-ref HEAD)" = feat/166.2 && git fetch -q origin main && QZ_NAMES="$(git diff --name-only origin/main...HEAD)" && QZ_BAD="$(printf '%s\n' "$QZ_NAMES" | grep -E '^(analytics-service|supabase)/' || true)" && { test -z "$QZ_BAD" || { printf '%s\n' "$QZ_BAD" | sed 's|^|forbidden path on feat/166.2: |'; false; }; } && echo "branch-scope: feat/166.2 carries no analytics-service/ or supabase/ path"</automated>
    <fails_when>non-zero exit: the branch is not feat/166.2, the fetch failed, or `git diff --name-only origin/main...HEAD` names any path under `analytics-service/` or `supabase/` (each is printed on a `forbidden path on feat/166.2:` line)</fails_when>
  </verify>
  <acceptance_criteria>
    - the SUMMARY quotes the `branch-scope:` line printed by the third verify command
    - the SUMMARY records SWEEP_SHA, SWEEP_BASE (equal to or a descendant of plan 166.2-06's MERGED_BASE) and the summary line of each check
    - the SUMMARY quotes the eslint exit status over the named TS union, and the anchor verifier's `scanned:` and `OK:` lines
  </acceptance_criteria>
  <done>Every phase gate is green at one recorded SHA that contains origin/main.</done>
</task>

<task type="auto">
  <name>Task 2: the single chore(release) commit - VERSION and package.json byte-equal, one unified CHANGELOG entry mapped from an enumerated commit checklist</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md - "CHANGELOG discipline (founder instruction, 2026-09-09)" and "CI gate integrity" trap 1
    - CHANGELOG.md - the top entry (copy its heading separators byte-for-byte: a hyphen between version and date, an em-dash before the title)
    - VERSION, package.json
  </read_first>
  <action>
    Follow CLAUDE.md's mechanism exactly. (0) `git fetch origin main`, then re-read `origin/main`'s VERSION (context rule "Version": the new version is derived from `origin/main`'s alone). (1) Enumerate and COUNT 166.2's OWN commits with the context's branch rule (b), `git log HEAD --no-merges --oneline --not origin/main feat/166.1`; keep the list as a checklist in the SUMMARY. Separately list (i) the inherited commits from `feat/166.1` before the cut, each with its planning-only proof, mapped to no bullet, because Phase 166.1's own release entry covers that phase and a planning-only commit needs no entry, and (ii) any merge commit of `origin/main`, including plan 166.2-06's, which brings other phases' work that their own entries already cover, with its MERGED_BASE, mapped to one Notes bullet. The entry covers 166.2's own commits only, per the D-23 revision. (2) `git diff "$(git merge-base HEAD origin/main)"...HEAD`: read what changed. (3) Group by theme before writing, in the repo's section vocabulary: Fixed (the TS sites T1-T20 per D-15/D-17), Changed (what users will see differ: the TS-computed Sharpe / correlation / beta / information ratio / ratio cells on /compare, the scenario composer and benchmark panel, the Risk tab and the factsheet, and the OG card's computed Sharpe, for a constant-yield series), Removed (`correlation-math.ts`, the dead `rollingCorrelation` and `computeRollingMetric`, and every private copy of the Sharpe, Pearson, beta, mean and pstdev formulas the sites carried), Root cause (a standard deviation derived from a compounding NAV is about 1e-16, never exactly 0; and twenty private copies of the same ratio formula, which is why D-17 computes each once), Tests (the TS site files and fixture, the cross-language pin to the Python floor constant, the compute-once source-scan gate with its whole-tree matcher and count-pinned allowlist, the drill tables), Notes (`src/lib/return-stats.ts` as the one TS home, pinned to the Python constant; the D-17 / D-19 split with Phase 169 (T14's persisted read is 169 plan 04's, its computed arm is this phase's; 169 plan 14's zoom KPIs inherit the floored `compute`), naming the items CONTEXT D-17 records for the orchestrator; the 2026-09-26 split: the Python floor sites ship as Phase 166.1 and the PROD recompute as Phase 166.3, and this entry claims neither; no migration; the merge of `origin/main` at MERGED_BASE). (4) Set the new version in VERSION and in package.json's `"version"` by direct edit, never `npm version`. Write ONE entry at the top, `## [<version>] - <execution date> — COMPUTEONCE: each TypeScript Sharpe, correlation and beta is computed once, in one floored module, and every page reads it`. (5) Cross-check that every OWN commit from step 1 maps to at least one bullet; list the mapping in the SUMMARY; an unmapped own commit is a defect. (6) No credential-shaped literal, home path, username, strategy name or id in the entry, and no CI-skip trailer token anywhere in the commit message (the fourth verify command sweeps every message on the branch, not just this one). (7) Run CRITICAL-02 and the CI version gate (`node scripts/check-version-bump.mjs --self-test`, then `node scripts/check-version-bump.mjs`; confirm `origin/main` resolves first). Remove the node_modules link, then commit exactly the three files as `chore(release): v<version> — COMPUTEONCE` with the session's attribution lines.
  </action>
  <verify>
    <automated>test "$(tr -d '\n' < VERSION)" = "$(node -p 'require("./package.json").version')" && tr -d '\n' < VERSION | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+\.[0-9]+$' && head -40 CHANGELOG.md | grep -Eq "^## \[$(tr -d '\n' < VERSION | sed 's/\./\\./g')\] - "</automated>
    <fails_when>non-zero exit: VERSION and package.json differ, VERSION is not 4-digit, or no heading for that version at the top of CHANGELOG.md</fails_when>
    <automated>QZ_WT="$(git worktree list --porcelain)" && QZ_MAIN="$(printf '%s\n' "$QZ_WT" | awk '/^worktree /{print $2; exit}')" && { test -e node_modules || ln -s "$QZ_MAIN/node_modules" node_modules; } && ./node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts && test -f scripts/check-version-bump.mjs && git fetch -q origin main && git rev-parse --verify --quiet origin/main > /dev/null && node scripts/check-version-bump.mjs --self-test && node scripts/check-version-bump.mjs</automated>
    <fails_when>non-zero exit (127 = FAIL), vitest " failed" / "No test files found", the self-test does not print "SELF-TEST PASSED", or the live run prints any "::error::" defect instead of "No defects"</fails_when>
    <automated>git fetch -q origin main && QZ_BASEV="$(git show origin/main:VERSION)" && QZ_BASEV="$(printf '%s' "$QZ_BASEV" | tr -d '\n')" && QZ_WANT="$(printf '%s\n' "$QZ_BASEV" | awk -F. 'NF==4{print $1"."($2+1)".0.0"}')" && test -n "$QZ_WANT" && test "$(tr -d '\n' < VERSION)" = "$QZ_WANT" && echo "version-derivation: origin/main=$QZ_BASEV release=$QZ_WANT"</automated>
    <fails_when>non-zero exit: origin/main's VERSION is unreadable or not 4-part, or this branch's VERSION is not origin/main's second component + 1 with the last two zeroed (a sibling release merged since the bump; re-run Task 2 as a new release commit). The ship step re-runs this exact command immediately before the PR opens.</fails_when>
    <automated>git fetch -q origin main && QZ_MSGS="$(git log origin/main..HEAD --format=%B)" && test -n "$QZ_MSGS" && n=$(printf '%s\n' "$QZ_MSGS" | grep -aEic '\[(ci skip|skip ci|no ci|skip actions|actions skip)\]|skip-checks:true' || true); [ "$n" = 0 ]</automated>
    <fails_when>non-zero exit: the range is empty (a stale or missing origin/main), or any commit message on the branch, merge messages and inherited commits included, carries a CI-skip trailer token in any of the six forms CLAUDE.md lists</fails_when>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= <release sha>` lists exactly CHANGELOG.md, VERSION, package.json
    - the fourth verify command exits 0: no message in `git log origin/main..HEAD --format=%B` carries a CI-skip token
    - the third verify command exits 0 and the SUMMARY quotes its `version-derivation:` line
    - the SUMMARY carries the own-commit checklist with its count, the inherited planning-only commits and the merge commits listed separately, the commit-to-bullet mapping (every own commit mapped), and both VERSION readings with the chosen result
    - `grep -c 'COMPUTEONCE' CHANGELOG.md` prints at least 1
    - `test -L node_modules` is false at commit time
  </acceptance_criteria>
  <done>One release commit carries a byte-equal 4-digit bump derived from origin/main's VERSION and a unified entry that maps every one of 166.2's own commits; no message on the branch carries a CI-skip token.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| commit message / CHANGELOG -> public repo and CI | a skip token suppresses CI even when quoted; CHANGELOG is scanned by gitleaks on push to main |
| sweep SHA -> the shipped tree | a sweep on a tree without origin/main certifies a tree that will not ship |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166.2-12 | Tampering (CI suppression) | every commit message on the branch | high | mitigate | Task 2's fourth verify command greps every message in `origin/main..HEAD` (merges and inherited commits included) for all six token forms |
| T-166.2-13 | Information disclosure | CHANGELOG entry | medium | mitigate | no id, name, home path or credential-shaped literal; hygiene check in Task 1 |
| T-166.2-14 | Repudiation (unshipped or over-claimed fact) | CHANGELOG | medium | mitigate | enumerated own-commit checklist with a commit-to-bullet mapping in the SUMMARY; inherited feat/166.1 commits proven planning-only and claimed by no bullet |
| T-166.2-35 | Tampering (a release carrying another phase's code) | the feat/166.2 branch | high | mitigate | Task 1's third verify command fails on any `analytics-service/` or `supabase/` path in `git diff --name-only origin/main...HEAD` and on any branch other than feat/166.2 |
| T-166.2-36 | Repudiation (a duplicated version) | VERSION | medium | mitigate | Task 2's third verify command derives the version from `origin/main`'s at release time and is re-run immediately before the PR opens |
| T-166.2-34 | Repudiation (a green sweep on the wrong tree) | the sweep | medium | mitigate | the sweep asserts `git merge-base --is-ancestor origin/main HEAD` after a fetch, and re-runs plan 166.2-06 Task 2 if main moved |
</threat_model>

<verification>
- All three Task 1 verify commands and all four Task 2 verify commands exit 0.
</verification>

<success_criteria>
- Every phase gate is green at one SHA containing origin/main on feat/166.2, which carries no Python-service or database path; one release commit; VERSION equals package.json and is origin/main's next minor; the entry maps every one of 166.2's own commits and records the D-17 / D-19 split with Phase 169; no message on the branch carries a CI-skip token.
</success_criteria>

<output>
Create `.planning/phases/166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta/166.2-07-SUMMARY.md` when done.
</output>
