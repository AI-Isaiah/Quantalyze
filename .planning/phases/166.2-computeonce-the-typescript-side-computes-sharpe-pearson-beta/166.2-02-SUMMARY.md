---
phase: 166.2-computeonce-the-typescript-side-computes-sharpe-pearson-beta
plan: 02
status: complete
subsystem: analytics-math (TypeScript)
tags: [compute-once, pearson, correlation-matrix, dispersion-floor, D-17, D-07]
requires:
  - phase: 166.2-01
    provides: "src/lib/return-stats.ts pearson; src/__tests__/fixtures/dispersion-nav.ts"
provides:
  - "one TS Pearson: /compare's CompareCorrelationMatrix (T6) and the Risk-tab CorrelationMatrix widget (T7) call return-stats pearson"
  - "src/lib/correlation-math.ts deleted with its dead rollingCorrelation (T6b)"
  - "src/lib/return-stats.pearson-contract.test.ts: the G11.E.5 pearson contract, moved"
affects: [166.2-06, 166.2-07, 169]
tech-stack:
  added: []
  patterns:
    - "render-level D-07 test: a component rendered with a constant-yield input must produce the identical cell (text, colour, title or label) it produces for an all-zero input"
key-files:
  created:
    - src/components/strategy/CompareCorrelationMatrix.residue.test.tsx
    - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx
    - src/lib/return-stats.pearson-contract.test.ts
  modified:
    - src/components/strategy/CompareCorrelationMatrix.tsx
    - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
    - src/lib/diversification.ts
  deleted:
    - src/lib/correlation-math.ts
    - src/lib/correlation-math.test.ts
key-decisions:
  - "The widget maps a null pearson to 0 (pearson(av, bv) ?? 0): 0 is the cell an exact-constant leg already renders, so no renderer changes"
  - "Task 1's verify grep clause stays red on a plan-01 doc comment in return-stats.ts; not edited here (the plan forbids it) and routed to the orchestrator"
requirements-completed: [SC-3]
duration: 10 min
completed: 2026-09-26
plan_head_before: df75f6a77507dbad9c6212f0d104985215e76478
actuals:
  tokens: 5308
  tasks: 2
  commits: 3
patterns-established:
  - "compute once: no TS component keeps a private Pearson; both correlation matrices import @/lib/return-stats"
coverage:
  - id: D1
    description: "T6: /compare's correlation matrix renders a constant-yield pick's pair cell exactly as an all-zero pick's (the em-dash flat-window null); two noisy picks and a cent-rounded NAV keep a finite cell"
    requirement: SC-3
    verification:
      - kind: unit
        ref: "src/components/strategy/CompareCorrelationMatrix.residue.test.tsx"
        status: pass
    human_judgment: false
  - id: D2
    description: "T7: the Risk-tab CorrelationMatrix widget renders every cell of a constant-yield leg exactly as for an all-zero leg; the noisy pair is unchanged"
    requirement: SC-3
    verification:
      - kind: unit
        ref: "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx"
        status: pass
      - kind: unit
        ref: "CorrelationMatrix.boundary.test.tsx and CorrelationMatrix.all-columns.test.tsx, unedited"
        status: pass
    human_judgment: false
  - id: D3
    description: "correlation-math.ts deleted; its G11.E.5 pearson contract cases pass unchanged against return-stats"
    requirement: SC-3
    verification:
      - kind: unit
        ref: "src/lib/return-stats.pearson-contract.test.ts"
        status: pass
      - kind: other
        ref: "vitest run src/lib/return-stats src/lib/diversification src/components/strategy src/app/(dashboard)/compare src/app/(dashboard)/allocations/widgets (89 files, 1263 tests)"
        status: pass
      - kind: other
        ref: "tsc --noEmit; eslint over the 6 touched files"
        status: pass
    human_judgment: false
---

# Phase 166.2 Plan 02: one TS Pearson for both correlation matrices Summary

**`correlation-math.ts` is gone and the CorrelationMatrix widget's private copy is deleted. /compare's correlation matrix and the Risk tab's correlation widget now both call `return-stats` `pearson`. A compounding constant-yield strategy now shows "no correlation", exactly like a flat one. Before this plan it showed a rounding-noise figure between -0.04 and 0.08.**

## Performance

- **Duration:** about 10 min measured (first captured timestamp to last)
- **Started:** 2026-09-26T10:22:17Z (first timestamp captured in this session, at Step 0)
- **Completed:** 2026-09-26T10:31:56Z
- **Tasks:** 2
- **Files:** 8 (3 created, 3 edited, 2 deleted)

## Accomplishments

- T6: `CompareCorrelationMatrix` imports `pearson` from `@/lib/return-stats`. Before the change, a constant-yield pick against a noisy pick showed a residue cell. That cell now reads "—" with the "Insufficient data" title, which is the all-zero pick's cell.
- T6b: `src/lib/correlation-math.ts` was deleted with `git rm`. Its G11.E.5 `pearson` cases were moved with `git mv` to `src/lib/return-stats.pearson-contract.test.ts`. The import was repointed and every assertion is byte-identical. The `rollingCorrelation` describe block was deleted with the function. Git records the move as D + A, not R, because the file lost half its lines.
- T7: the widget's private `pearson` and its now-unused `mean` import were deleted. The computed arm calls `pearson(av, bv) ?? 0`, and the `precomputed` arm is untouched.
- `diversification.ts`: the one module-doc sentence that named the deleted module was replaced by a sentence that was true when written. Nothing else in the file changed.

## Task Commits

1. **Task 1 (tracer): one Pearson, correlation-math.ts deleted, T6** - `ac1946d31` (feat)
2. **Task 2 RED: T7 failing render test** - `e4ee8e31c` (test)
3. **Task 2 GREEN: widget on the shared pearson** - `90476b60b` (feat)

Tracer gate: after `ac1946d31` the Task 1 verify was re-run. Vitest was green on 53 files and 950 tests, and `correlation-math.ts` was absent from disk. The grep clause was red, and the only cause is a comment in a plan-01 file (Deviation 1). The slice itself was proven, so the plan expanded to Task 2. This was an autonomous decision and is recorded here.

## Re-measurements before editing

- **Dead code.** Ran `grep -rn "rollingCorrelation" src` with `correlation-math*` excluded. The one hit was `src/components/charts/CorrelationWithBenchmark.tsx:37`, a doc comment. There was no import and no call, so no production importer existed and the deletion went ahead.
- **Importers of the module.** `grep -rn "correlation-math" src` found three hits: `CompareCorrelationMatrix.tsx` (the one live import), `correlation-math.test.ts` (moved), and doc comments in `diversification.ts` and `return-stats.ts`.
- **diversification.test.ts.** It imports from `@/lib/scenario` and `@/lib/diversification` only, and `grep -c pearson` prints 0. Its consistency pin rebuilds ρ as `cov[i][j] / (sa * sb)` from this lib's covariance and σ. The replacement doc sentence says exactly that.

## RED before the fix (observed on the unedited sites, captured with vitest's JSON reporter)

| Site | RED ids | Value the unedited site rendered |
|---|---|---|
| T6 `CompareCorrelationMatrix` pair cell | all 11 (daily_1e-5, daily_1e-4, daily_1e-3, apy_0.01pct, apy_0.1pct, apy_1pct, apy_3pct, apy_5pct, apy_10pct, apy_50pct, apy_100pct) | "0.08", "0.04", "-0.04", "0.05", "0.03", "-0.01", "0.01", "-0.03", "0.04", "-0.01", "0.04"; the all-zero pick gives "—" / "Insufficient data" |
| T7 widget cells (3 legs, 9 cells) | all 11, same ids | vitest truncated the array diff, so the cells were re-measured with a scratch replica of the old private pearson and `correlationColor` over the same inputs (flat leg against NoisyA / NoisyB): daily_1e-5 0.08 / 0.01, daily_1e-4 0.04 / 0.02, daily_1e-3 -0.04 / 0.02, apy_0.01pct 0.05 / 0.03, apy_0.1pct 0.03 / -0.01, apy_1pct -0.01 / 0.04, apy_3pct 0.01 / 0.03, apy_5pct -0.03 / -0.04, apy_10pct 0.04 / 0.01, apy_50pct -0.01 / 0.00, apy_100pct 0.04 / -0.00, each tinted off-white (for example rgb(238,244,243) for 0.08); the all-zero leg gives "0.00" on white. apy_50pct against NoisyB rounds to 0.00 on white, and the cell against NoisyA still differs |

Both controls were green before and after the change: two noisy legs, and a cent-rounded 1% APY NAV (real dispersion). The reference cases were also green before and after: the all-zero cell is the em-dash at T6 and 0.00 on white at T7.

## Drill table (byte backup in the session scratchpad under a `166.2-02-` name, neuter, observe, `cp` restore, `cmp`)

| Drill | Neuter | Observed | Restore |
|---|---|---|---|
| T6 | `CompareCorrelationMatrix.tsx`: the return-stats import replaced by a local copy of the old `denom > 0` sums-form pearson | T6 11 of 14 red (the 11 yield ids); the reference and both controls green | `cp` + `cmp` OK |
| T7 | `CorrelationMatrix.tsx`: the return-stats import replaced by the old private pearson (and its `mean` import) | T7 11 red; the widget dir otherwise green (11 failed, 25 passed of 36) | `cp` + `cmp` OK |

## Each fixed output is one the site already emits for an exact constant (D-07)

- T6: the pair cell is `null`, rendered as "—" with the "Insufficient data" title. That is the cell an all-zero (exactly flat) pick has always rendered. It uses the G11.E.5 flat-window null that `CompareCorrelationMatrix` already renders.
- T7: the cell is 0, rendered as "0.00" on white. That is the cell an exact-constant leg has always rendered, because the old private pearson returned 0 when its denominator was 0.

No renderer, label or colour mapping changed.

## Files Created/Modified

- `src/components/strategy/CompareCorrelationMatrix.tsx`: imports `pearson` from `@/lib/return-stats`, one line changed.
- `src/components/strategy/CompareCorrelationMatrix.residue.test.tsx`: the T6 render test.
- `src/lib/return-stats.pearson-contract.test.ts`: the G11.E.5 pearson contract. It was moved from `correlation-math.test.ts`, and its `rollingCorrelation` block was dropped.
- `src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx`: the private pearson and the `mean` import were deleted, and the computed arm now uses the shared pearson with `?? 0`.
- `src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.residue.test.tsx`: the T7 render test.
- `src/lib/diversification.ts`: one module-doc sentence changed.
- `src/lib/correlation-math.ts`, `src/lib/correlation-math.test.ts`: deleted.

## Decisions Made

- The T7 tests compare the WHOLE cell: text, background colour, text colour and aria-label. A residue correlation near 0 can round to "0.00" or "-0.00" in text and still tint the cell. Comparing colour as well catches that case.
- The T6 test compares text, title and background, so a residue cell cannot pass by showing a number that happens to sit near 0.

## Deviations from Plan

### 1. [Plan defect, not auto-fixable here] Task 1's verify grep clause is red on a plan-01 comment

- **Found during:** Task 1, during orientation, and confirmed by running the verify.
- **Issue:** the verify ends with `test -z "$(grep -rln ... 'correlation-math' src)"`. After this plan, exactly one file under `src/` still names the module: `src/lib/return-stats.ts:38`, in the module doc comment of `return-stats.ts`, which plan 166.2-01 wrote. The line reads ` * of \`build-payload.ts\` \`pearsonCorr\` and \`correlation-math.ts\` \`pearson\`, and`. This plan must not edit `return-stats.ts`: the context says so, and Task 2's acceptance requires `git diff --stat -- ... src/lib/return-stats.ts` to be empty. No wave-2 sibling owns the file either. Plan 06's permanent gate explicitly lets a comment-only mention through.
- **Clause-by-clause result:** vitest `Test Files  53 passed (53)` / `Tests  950 passed (950)`. `test ! -e src/lib/correlation-math.ts` exits 0. The grep lists exactly one path, `src/lib/return-stats.ts`, and the match is a ` *` comment line. The verify as written exits 1.
- **Action:** `return-stats.ts` was not edited and the grep was not narrowed. **Routed to the orchestrator:** a one-line doc edit to `return-stats.ts`, made by whoever may touch it (for example the orchestrator, or plan 06 or 07), clears the clause.

### 2. [Plan defect] Acceptance criterion A3's base includes plan 01's code change

- **Issue:** the criterion measures `git diff "$(git merge-base HEAD origin/main)" -- src/lib/diversification.ts` and expects only ` *` lines. That base is older than plan 166.2-01, which changed a code line and an import in the file (the T5 guard). The literal diff therefore shows `+import { dispersionIsReal } ...` and the guard line. Neither comes from this plan.
- **This plan's own diff** (`git diff <plan_head_before> -- src/lib/diversification.ts`) touches only ` *` doc lines: two removed and three added. The intent of the criterion holds.

### 3. [Environment] Three test timeouts under load on the first Task 2 vitest run

- The first run of Task 2 verify 1 hit 5 s timeouts in `StrategyTable.test.tsx`, `DrawdownChart.scenario.test.tsx` and `EquityChart.test.tsx`. Load average was 67, with three sibling executors running. None of the three imports a module this plan touched. All three passed alone (`Tests  97 passed (97)`). A full re-run of the verify was then green: `Test Files  89 passed (89)` / `Tests  1263 passed (1263)`, exit 0.

**Total deviations:** 2 plan defects recorded, not fixed by this plan, and 1 environment flake that was re-measured green. **Impact:** no code outside this plan's files changed. Deviation 1 leaves the Task 1 verify red on a comment until the orchestrator or a later plan edits `return-stats.ts`.

## Issues Encountered

- A pending plan still anchors to the deleted symbols. The `--pending` anchor verifier, run before this SUMMARY existed, reported 3 stale claims, all in `166.2-02-PLAN.md` itself (its own `verify-symbol` lines for `correlation-math.ts` `pearson` and `rollingCorrelation`, and the diversification doc token). This plan removes those symbols on purpose. No other pending plan anchored to them. See the gate outputs for the re-run after this SUMMARY exists.

## Gate outputs (verbatim tails)

- Anchor verifier before Task 1: `OK: 1 plan file(s), no stale claims.` (9 claims, exit 0)
- Anchor verifier in CI's form after this SUMMARY exists (`--pending`): `OK: 13 plan file(s), no stale claims.` (108 claims), so no pending plan anchors a symbol this plan removed
- Task 1 verify (as written): `Test Files  53 passed (53)` / `Tests  950 passed (950)`, then exit 1 on the grep clause (Deviation 1)
- Task 2 verify 1: `Test Files  89 passed (89)` / `Tests  1263 passed (1263)`, exit 0
- Task 2 verify 2: `TSC_EXIT=0`, `ESLINT_EXIT=0`, exit 0
- Acceptance: `D	src/lib/correlation-math.ts`; the moved test has 0 `rollingCorrelation` tokens. `grep -c '@/lib/return-stats'` prints 1 for CompareCorrelationMatrix and 1 for the widget. `grep -c 'function pearson'` on the widget prints 0. The boundary and all-columns tests show an empty diff. `git diff --stat <plan_head_before> HEAD -- analytics-service/ supabase/ src/lib/return-stats.ts` is empty. `node_modules` appears in no commit tree.

## User Setup Required

None.

## Next Phase Readiness

T6 and T7 are on the shared module. Plan 166.2-06's gate can require that `correlation-math.ts` stays absent and that there is no live `correlation-math` import. Deviation 1's comment is not a live import. REQUIREMENTS.md, STATE.md and ROADMAP.md were not touched, because the orchestrator owns them.

## Self-Check: PASSED

- FOUND: the three created test files; `src/lib/correlation-math.ts` absent
- FOUND commits: `ac1946d31`, `e4ee8e31c`, `90476b60b`
