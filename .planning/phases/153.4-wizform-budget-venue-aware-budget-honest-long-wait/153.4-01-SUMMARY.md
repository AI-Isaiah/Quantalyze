---
phase: 153.4-wizform-budget-venue-aware-budget-honest-long-wait
plan: 01
subsystem: infra
tags: [seam, circuit-breaker, timeouts, mt5, pin-tests, retry-audit]

# Dependency graph
requires:
  - phase: 153.1-wizform-codes-honest-codes-venue-capability-foundation
    provides: "the DERIVED A-25 assertion + its SELF-TEST (green at HEAD), and VENUE_CAPABILITIES.serialized"
  - phase: 153.3-wizform-gw-mt5-gateway-honesty
    provides: "the server chain this budget must contain — a 20 s bounded terminal-lease wait, a 75 s end-to-end probe deadline, and a 10 s release outside it"
provides:
  - "`SeamBudgetKey` gains `validate-key-serialized` — a fourth budget magnitude at 120 000 ms on `mt5-gateway` with `retries: SEAM_RETRIES` (0)"
  - "`BREAKER_LOCK_TOMBSTONE_S` 60 → 90 in the SAME commit (D-26), so A-25 holds exactly: (30 + 90) × 1000 = 120 000 ≥ 120 000"
  - "a hand-typed pin asserting the client budget exceeds the 105 s server worst case (WIZFORM-05 / D-04) — the first assertion of that invariant anywhere"
  - "a written NO-retry audit verdict for the new row, and the ten-wrapper exhaustiveness holding at compile time and at run time"
affects: [153.4-02, 153.4-03, 155-mt5-verify]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "A budget row and the breaker constant it outgrows are ONE commit, never two"
    - "A SELF-TEST's ceiling mirrors the assertion under test, not the module the assertion deliberately does not read"

key-files:
  created: []
  modified:
    - src/lib/resilient-fetch.ts
    - src/lib/seam-constants.pin.test.ts
    - src/lib/seam-retry-registry.ts
    - src/lib/seam-retry-registry.test.ts
    - src/lib/resilient-fetch.test.ts
    - src/test/helpers/upstash-breaker.ts
    - tests/redis/seam-breaker.redis.test.ts
    - docs/runbooks/seam-breaker.md

key-decisions:
  - "The key is named after the CAPABILITY (`validate-key-serialized`), never the venue — `validate-key-mt5` would be the instance-not-class defect this repo has already paid for and would need renaming the day a second serialized venue arrives"
  - "The DERIVED A-25 assertion CANNOT redden on a module-constant revert, by design — its ceiling is hand-typed. The plan's stated falsification was therefore un-runnable as written; the real D-26 violation (budget landed, tombstone raise did not, on both the module and the pin) was run instead and reds BOTH A-25 assertions"
  - "The SELF-TEST's ceiling moved from the live module constants to the hand-typed `(90 + 30) * 1_000` — the same expression the assertion under test applies. Reading the live constants left a real blind spot: raise the assertion's own literals past 150 000 and the SELF-TEST stays green while the assertion it validates has gone decorative"
  - "The pin file's `RETRY_AUDIT_NO_ANALYTICS` count 5 → 6 was moved out of Task 2's commit into Task 3's, so the count and the population it counts land together — a count moving without its map is precisely the 141.2 / D-03 failure that file records"
  - "The tombstone raise falsified THREE hand-typed twins the plan did not enumerate (the breaker double, the max-legal-lock-span pins, and the redis lane's own constant) plus the operator runbook; all four moved in Task 2's commit"

patterns-established:
  - "Pattern: a hand-typed twin of a DERIVED module value (MAX_BREAKER_LOCK_SPAN_MS) is a pin site that no plan's file list will predict — re-run the behavioural suites, do not trust the enumerated site list"
  - "Pattern: a NO-retry verdict states the audit, not a cross-reference — the amplification argument (open breaker, wall-clock, serialization) is written out per entry"

requirements-completed: [WIZFORM-05]

# Metrics
duration: 21min
completed: 2026-08-10
---

# Phase 153.4 Plan 01: The 120 s serialized budget and the tombstone that spans it Summary

**`validate-key-serialized` lands at 120 000 ms with `BREAKER_LOCK_TOMBSTONE_S` raised 60 → 90 in the same commit, and fifteen hand-typed pin sites move with it — including four the plan never enumerated, found by running the suite rather than by reading the table.**

## Performance

- **Duration:** ~21 min
- **Started:** 2026-08-10T20:05:07Z
- **Completed:** 2026-08-10T20:26:29Z
- **Tasks:** 3 of 3
- **Files modified:** 8

## Task Commits

1. **Task 1: the 120 000 ms row and the tombstone raise — ONE commit (D-26)** — `20ee68f4` (feat)
2. **Task 2: move all pin sites in `seam-constants.pin.test.ts`, including the three that stay green while their premise breaks** — `1f707901` (test)
3. **Task 3: the NO-retry audit verdict, the exhaustiveness fence, and the counts that follow it** — `d813e094` (feat)

## Accomplishments

- **A fourth budget magnitude, paid for in the same commit.** `SEAM_BUDGETS["validate-key-serialized"]` is 120 000 ms on `["mt5-gateway"]` with `retries: SEAM_RETRIES` (0), and `BREAKER_LOCK_TOMBSTONE_S` is 90. A-25 now holds with EQUALITY — `(30 + 90) × 1000 = 120 000 ≥ 120 000` — which the constant's rewritten docblock states explicitly, along with the consequence: there is zero headroom left, so the next budget raise of any size must move the tombstone again.
- **The client-vs-server invariant is asserted for the first time.** Nothing anywhere pinned WIZFORM-05's actual claim. A new `it` hand-types the 105 000 ms server worst case (20 s bounded lease acquisition + 75 s end-to-end probe deadline + 10 s release outside that deadline — all three re-derived from `analytics-service/services/mt5_concurrency.py` and `analytics-service/routers/exchange.py` at HEAD) and asserts the client budget exceeds it. No Python constant is imported or parsed; the Python side pins the same relationship independently from its own end, so the two are independent oracles rather than one restated twice.
- **The ten-wrapper exhaustiveness fence fired in anger and was closed properly.** Task 1 deliberately produced `src/lib/seam-retry-registry.test.ts(136,7): error TS2322: Type 'true' is not assignable to type 'never'` — the `_MissingAnalyticsVerdict` binding refusing a 14th `SeamBudgetKey` that carries no retry audit verdict. Task 3 cleared it by writing the verdict, not by widening the fence.
- **Both A-25 assertions kept, doing two different jobs, and proven to do them.** The literal-vs-literal one catches the breaker constants moving; the derived one catches a budget row outgrowing them. Under the real D-26 violation both red, with different messages naming different causes.
- **Full suite green:** 770 files / 11 508 passed / 287 skipped / 0 failed; `npx tsc --noEmit` clean; `npm run lint` 0 errors.

## Falsifiability — RUN and OBSERVED

### F1 — the D-26 violation: the budget landed, the tombstone raise did not

Mutation: `BREAKER_LOCK_TOMBSTONE_S` reverted to `60` in `resilient-fetch.ts` **and** the derived assertion's hand-typed ceiling reverted to `(60 + 30) * 1_000` — i.e. exactly the state D-26 forbids. Four assertions red, and the two A-25 ones red for different reasons:

```
FAIL > A-25: the tombstone outlives the longest seam budget
AssertionError: The lock tombstone no longer spans the longest seam budget, so a
120s-budget request that failed after a full cooldown can no longer see the lock it
overlapped — and re-arms the circuit on evidence it gathered before the previous trip.:
expected 60000 to be greater than or equal to 90000

FAIL > A-25 (DERIVED): the LONGEST budget in the live table fits inside tombstone + cooldown
AssertionError: A-25 IS BROKEN: the longest seam budget no longer fits inside (lock
tombstone + breaker cooldown) … expected 120000 to be less than or equal to 90000

FAIL > BREAKER_LOCK_TOMBSTONE_S is the literal 90        (expected 60 to be 90)
FAIL > FAKE_LOCK_TOMBSTONE_MS equals the millisecond form of BREAKER_LOCK_TOMBSTONE_S
```

⚠️ **The plan's stated falsification for this row was un-runnable as written**, and this is worth recording rather than quietly substituting. It asked for a revert of `BREAKER_LOCK_TOMBSTONE_S` alone and predicted the DERIVED assertion would red. It cannot: the derived assertion's ceiling is a hand-typed `(90 + 30) * 1_000` **by rule** — reading the module's own constants there is the ONE RULE violation that made the incumbent pin blind in the first place. A module-only revert moves nothing on either side of that comparison. The mutation actually specified by VALIDATION ledger row SC-4 — *a budget above the ceiling the pins declare* — is the one run above, and it is the one that maps onto must-have truth 3.

### F2 — the inversion re-created

Mutation: the new row's `timeoutMs` set to `100_000`, below the 105 s server worst case.

```
FAIL > WIZFORM-05 / D-04: the serialized client budget exceeds the 105 s server worst case
AssertionError: The serialized client budget no longer exceeds the 105 s server worst case
(20 s bounded lease wait + 75 s end-to-end probe deadline + 10 s release outside the
deadline). The client would abandon the request before the server's honest verdict can
land … expected 100000 to be greater than 105000
```

(Also red, correctly: the `timeoutMs` value pin, the fourth-magnitude representative, and the derived A-25 vacuity floor.)

### F3 — the retry verdict flipped from NO to YES

Mutation: the key moved out of `RETRY_AUDIT_NO_ANALYTICS` into `RETRY_SAFE_ANALYTICS` with `retries: 1`. Four guards red **across both files**, including the row↔registry agreement pin the plan named:

```
FAIL  seam-constants.pin.test.ts > the four registry maps have their hand-typed sizes
  RETRY_SAFE_ANALYTICS no longer holds exactly 4 audited retry verdicts … expected 5 to be 4
FAIL  seam-constants.pin.test.ts > every allowlisted analytics wrapper (validate-key-serialized)
      has its SEAM_BUDGETS row at retries 1
  "validate-key-serialized" is retry-safe in the registry but its budget row is 0 …
FAIL  seam-retry-registry.test.ts > RETRY_SAFE_ANALYTICS keys equal the hand-typed safe set
FAIL  seam-retry-registry.test.ts > RETRY_SAFE_ANALYTICS contributes exactly 4 non-empty
      evidence strings (per-map anti-vacuity fence)   … expected 5 to be 4
FAIL  seam-retry-registry.test.ts > RETRY_AUDIT_NO_ANALYTICS contributes exactly 6 …
```

### Behaviour assertion (run)

```
RETRY_SAFE_ANALYTICS['validate-key-serialized'] = undefined
typeof NO verdict = string | length = 1309
```

All three mutations were restored from copies taken before the mutation; `git diff` against the committed tree confirmed byte-identical restoration before each commit.

## Deviations from Plan

### Auto-fixed issues

**1. [Rule 3 — Blocking] Three hand-typed tombstone twins the plan did not enumerate**

- **Found during:** Task 2, on first run of the pin test and then of the behavioural seam suites.
- **Issue:** the tombstone raise falsified three sites outside the plan's `files_modified`:
  - `src/test/helpers/upstash-breaker.ts` — `FAKE_LOCK_TOMBSTONE_MS = 60_000`, the breaker double's hand-typed twin. Its own drift pin reds and its message explicitly forbids closing the gap by reading production's value.
  - `src/lib/resilient-fetch.test.ts` — the G2 / G2b cases hand-type `90 000` as the widest legal lock span. That ceiling is **derived inside the module** from cooldown + tombstone, so it silently became 120 000 and two "must decode to null" cases started decoding. This is the sharpest of the three: the hand-typed twin is the only thing that notices a derived value move.
  - `tests/redis/seam-breaker.redis.test.ts` — `LOCK_TOMBSTONE_S = 60` and the key-TTL bands `[89, 90]`. This lane runs under `vitest.redis.config.ts` (`npm run test:redis`) against a live/local Upstash and is **not** in the default include, so it would not have reddened in CI — it would simply have been wrong the next time an operator ran it.
- **Fix:** all three moved BY HAND (60 → 90, 90 000 → 120 000, TTL bands → `[119, 120]`), each with a comment naming D-26 and why the value is hand-typed rather than imported.
- **Commit:** `1f707901`

**2. [Rule 2 — Missing critical] The operator runbook restated both halves and would have shipped stale**

- **Found during:** Task 2, grepping for tombstone restatements repo-wide.
- **Issue:** `docs/runbooks/seam-breaker.md` states the span ceiling as `= 90 000 ms` in prose and carries `| BREAKER_LOCK_TOMBSTONE_S | 60 |` in the constants table. An operator diagnosing a live breaker incident reads this table; a wrong TTL here is a misdiagnosis on the exact surface the runbook exists to prevent one on.
- **Fix:** both moved, with the tight-equality consequence written into the table cell.
- **Commit:** `1f707901`

**3. [Rule 1 — Correctness] The SELF-TEST's ceiling read the module under test**

- **Found during:** Task 2, reading 153.1-01's landed SELF-TEST.
- **Issue:** the plan predicted a synthetic of `120_000` against a `(60 + 30) * 1_000` literal. What actually landed was a synthetic of `150_000` against `(BREAKER_COOLDOWN_S + BREAKER_LOCK_TOMBSTONE_S) * 1_000` — so the synthetic needed no change (150 000 clears the new 120 000 ceiling, exactly as 153.1-01 intended), but the ceiling was reading the live module. That leaves a real blind spot: raise the *assertion's own* hand-typed seconds past 150 000 without touching the module and the SELF-TEST stays green while the assertion it validates has gone decorative.
- **Fix:** the SELF-TEST ceiling is now the hand-typed `(90 + 30) * 1_000` — the same expression the assertion under test applies, which is what a SELF-TEST is for. The synthetic `150_000` is unchanged and its docblock now records the boundary as survived rather than upcoming.
- **Commit:** `1f707901`

**4. [Rule 3 — Blocking] The `RETRY_AUDIT_NO_ANALYTICS` count moved to Task 3's commit**

- **Found during:** Task 2, first run of the pin test.
- **Issue:** the plan places the `.toBe(5) → .toBe(6)` count in Task 2 and the registry entry it counts in Task 3, which would have left Task 2's commit red on that assertion.
- **Fix:** the count landed in Task 3 with the population it counts. This is the file's own doctrine — a count that moves apart from its map is the 141.2 / D-03 failure `seam-retry-registry.test.ts` records at length.
- **Commit:** `d813e094`

### Acceptance criteria not met as literally written

**`grep -n 'validate-key-serialized' src/lib/resilient-fetch.ts` returns 3 lines, not the 2 the criterion demands.** The third is the `BREAKER_LOCK_TOMBSTONE_S` docblock sentence, which the same task's own action text mandates ("naming `validate-key-serialized` at 120 000 ms as the new longest budget"). The criterion and the action contradict each other; the action wins, because the security property the count was proxying for is *never interpolate a wire value into a breaker key* (T-140-01), and that holds — all three occurrences are literal text, none is a template expression, and `breakerKeysFor` still derives every key from the frozen closed set.

## Threat Flags

None. The plan's register is unchanged by what shipped: `T-153.4-01` (module-constant key) holds — no occurrence of the literal is interpolated; `T-153.4-02` (retry amplification) is mitigated at four points and the mutation in F3 proves all four can fail; `T-153.4-03` is recorded as **accepted, not mitigated**, in the row's own `notes`, which deliberately do not contain the word "mitigated"; `T-153.4-04` is what F1 exercises. No new network endpoint, auth path, file access or schema surface was introduced — this plan changes two numbers and the prose that restates them.

## Known Stubs

None.

## Verification

| Gate | Result |
|------|--------|
| `npx vitest run` (full suite) | 770 files passed / 19 skipped · **11 508 passed / 287 skipped / 0 failed** |
| `npx tsc --noEmit` | clean |
| `npm run lint` | 0 errors (1 pre-existing warning in `EquityChart.tsx`, out of scope) |
| `npx vitest run src/lib/seam-budgets.invariant.test.ts` | 83 passed — SC-4a/b/d/e/f still green; the new row adds no route leg (that is 153.4-02's) |

⚠️ CI is Node 22, local is Node 25. Nothing in this diff is runtime-version sensitive (four numeric literals and prose), but a CI-only failure here would not be a flake — reproduce with `PATH=/opt/homebrew/opt/node@22/bin:$PATH`.

## What the next plan needs to know

- `budgetKeyFor(exchange)` does **not** exist yet — 153.4-02 owns it, and until it lands the new row is minted and pinned but **unspent**: every `/api/validate-key` call still takes `"validate-key"` at 30 000 ms. The row is inert, not live.
- `budgetKeyFor`'s `default:` must **return `"validate-key"` and never throw**, deliberately diverging from `process-key-client.ts`'s `never`-assignment analog, because this one takes a caller-supplied string. Write the divergence down or a reviewer will "fix" it back. ⛔ Never interpolate the argument into a breaker key.
- SC-4b models the two arms as mutually exclusive **`branch` legs** so the route sum takes a MAX, not a SUM (D-04 / RESEARCH Pattern 2). Adding the serialized leg without a `branch` label would charge 150 000 ms against a route ceiling that only ever spends one of the two.
- The A-25 equality is **tight**: any future budget above 120 000 ms must move `BREAKER_LOCK_TOMBSTONE_S`, the four hand-typed twins listed under deviation 1, and the runbook, all in one commit.
- Phase 155 (MT5-VERIFY) owns tightening 120 000 from the p50/p95 that 153.3's `stage` / `duration_ms` instrumentation produces. Until then the number is PROVISIONAL by decision, and the row says so.

## Self-Check: PASSED

Files asserted present:

- `src/lib/resilient-fetch.ts` — FOUND (`validate-key-serialized` at the union member, the row key, and the tombstone docblock; `BREAKER_LOCK_TOMBSTONE_S = 90`)
- `src/lib/seam-constants.pin.test.ts` — FOUND (`105_000` present; `three magnitudes`, `9-wrapper`, `60_000 - 30_000` all grep to 0)
- `src/lib/seam-retry-registry.ts` — FOUND (`validate-key-serialized` verdict present; `Exactly FIVE entries` greps to 0)
- `src/lib/seam-retry-registry.test.ts` — FOUND (`ALL nine wrappers` greps to 0)
- `src/test/helpers/upstash-breaker.ts`, `src/lib/resilient-fetch.test.ts`, `tests/redis/seam-breaker.redis.test.ts`, `docs/runbooks/seam-breaker.md` — FOUND

Commits asserted present in `git log`: `20ee68f4`, `1f707901`, `d813e094` — all three FOUND.
