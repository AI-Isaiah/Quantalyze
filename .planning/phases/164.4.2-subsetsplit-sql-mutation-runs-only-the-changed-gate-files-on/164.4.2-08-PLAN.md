---
phase: 164.4.2-subsetsplit
plan: "08"
type: execute
wave: 6
depends_on: ["164.4.2-06", "164.4.2-07"]
files_modified:
  - .github/workflows/ci.yml
  - src/__tests__/local-stack-lane-wiring.test.ts
  - docs/runbooks/shared-test-db-mutex.md
  - CLAUDE.md
autonomous: false
requirements: [AREA-F, AREA-B]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-F, no CI step or comment still claims the lane REFUSES a dump older than the migrations; the currency steps are named for what they now do — bound and name the replay set."
    - "Every job that boots the lane prints its replay set in a named step before the boot."
    - "The mutex runbook and CLAUDE.md describe the holders and the lane that exist, with every superseded statement kept as dated lineage rather than silently rewritten."
    - "Plan 05's and plan 06's CI-dependent criteria are each read off a run bound to the branch head SHA, with the runs COUNTED, and recorded with run id and SHA."
  artifacts:
    - .github/workflows/ci.yml
    - docs/runbooks/shared-test-db-mutex.md
    - CLAUDE.md
  key_links:
    - "The currency step's `run:` line is the lane's `--check-currency` seam, which dispatches the SAME function load_baseline() calls (plan 06)."
    - "A green is COUNTED: `gh api .../actions/runs?head_sha=<sha>` returns a non-zero count, and each job is read by name — not a board that merely shows no red."
---

<objective>
Carry D-F (DECISION F) out to the CI surface, finish what plan 05 left open (the old plan 06's
Task 3: the mutex runbook and a counted, SHA-bound green), and correct the CLAUDE.md sentences this
phase made stale.

Purpose: after plan 06 the lane replays, but two ci.yml steps are still named for a refusal and
several job comments argue from a refusal that no longer exists. A step name that describes a
mechanism that moved is how a future reader "fixes" the wrong thing. Plan 05's own SUMMARY lists
every CI-dependent criterion as PENDING; worktree agents cannot push, so those readings are an
explicit orchestrator step here, not an assumption.

⭐ Plan 05 already completed the old plan 06's Tasks 1 and 2 (the serialization pin, the
docs-path-filter roster, the anti-skip gate pin, `lint-sql-gates.test.ts` and the MW02 fixture).
Nothing here redoes them.

Output: a ci.yml that names the replay, a runbook and CLAUDE.md that match the jobs, and a
recorded, counted, SHA-bound CI reading.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-05-SUMMARY.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-06-SUMMARY.md
@docs/runbooks/shared-test-db-mutex.md
@CLAUDE.md

<!-- planner-discipline-allow: refuse a schema dump older than the migrations -->
**Read by NAME in `.github/workflows/ci.yml` (never by line):** the `frontend-local-stack` job's
checkout step and its currency step (today named "Baseline currency - refuse a schema dump older
than the migrations"); the `frontend-live-db-lane` job's checkout and the comment explaining why its
currency step lives in `frontend-local-stack` only; the `sql-tests` job's header comment, checkout
step, its currency step (same name today) and its "Boot the local-stack lane (Supabase CLI stack +
committed baseline)" step.

**The stale CLAUDE.md sentences plan 05 listed (quote them, verify each at HEAD before editing):**
- "CI's `sql-tests` uses its own `TEST_SUPABASE_DB_URL`" — `sql-tests` uses no secret now.
- "**VAC-08** (repo-vs-TEST ledger + body drift) runs in `sql-tests`" — it runs in `test-db-drift`.
- the `[164.8-PUSH-RACE-VAC08]` bullet: "`ci.yml`'s `sql-tests` and `apply-test` contend for the
  SAME advisory key" — the contender is now `test-db-drift`; and its `ci.yml` key-occurrence census.
- the VAC-08 SHA-bound reading citing a `sql-tests` job id — historical, true as lineage; a future
  reading comes from `test-db-drift`.
Plus what D-F makes worth saying there: the lane replays migrations newer than the dump, so part
(b) of `[164.8-PUSH-RACE-VAC08]` — gates red on a migration PR until merge — is narrowed for
`sql-tests`, whose lane now carries the PR's own migration; and where the carried set is recorded.

⛔ Constraints:
- ⛔ CLAUDE.md's house style for a stale claim is a dated `⛔ CORRECTED <date>` note beside the
  original, which stays as lineage. Follow it; never silently rewrite a dated reading. Measure every
  number you write there at execution time (e.g. `grep -c 61616158 .github/workflows/ci.yml`) and
  date it; never copy a number from this plan or from plan 05's SUMMARY.
- ⛔ Cite by SYMBOL or step NAME, never by line number.
- ⛔ Never write a CI skip token in any commit message or PR body, not even inside a sentence
  denying it — say "the skip trailer".
- ⛔ Never `git checkout --`, `git restore` or `git stash`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): every lane-booting job names its replay set in a step whose name says so</name>
  <files>.github/workflows/ci.yml, src/__tests__/local-stack-lane-wiring.test.ts</files>
  <read_first>
    - .github/workflows/ci.yml (the three jobs named in `<context>`, by name)
    - src/__tests__/local-stack-lane-wiring.test.ts (the `it(` titled "both lane-booting CI jobs fetch full history, and frontend-local-stack runs the currency seam before the boot", and the currency arm plan 06 re-argued)
    - scripts/local-stack/run.sh (the `--check-currency` seam and `load_baseline`, as plan 06 left them)
  </read_first>
  <action>
Per D-F, in `ci.yml`: rename BOTH currency steps (in `frontend-local-stack` and `sql-tests`) to
`Baseline currency - name the migrations the lane replays on top of the dump`, keeping each step's
`run: bash scripts/local-stack/run.sh --check-currency` line byte-identical. Rewrite each step's
comment, the `sql-tests` boot step's comment and header comment, and the `frontend-live-db-lane`
comment so none still says the lane rejects an older dump: say it determines the replay set from the
committed marker, prints it, replays it after the baseline, and fails loud when the set cannot be
determined or a replayed migration errors.

Then decide `fetch-depth: 0` by MEASUREMENT, per job (`frontend-local-stack`,
`frontend-live-db-lane`, `sql-tests`). Its only stated reason was the epoch guard, which the lane no
longer runs. For each job, list what its steps run and whether any of it reads git history (grep the
job block for `git `; for a vitest step, grep the files it runs for a git subprocess). Where nothing
does, drop `fetch-depth: 0` and its comment. Where something does, keep it and rewrite the comment to
name THAT consumer. Record the per-job finding in the SUMMARY.

In `local-stack-lane-wiring.test.ts`, re-subject the history arm to what was measured: it asserts
the depth each job actually needs and names the consumer, or — for a job that no longer needs it —
the arm drops that job and the SUMMARY says why. Extend the seam-before-boot half so it covers
`sql-tests` as well as `frontend-local-stack`, matched by the step's `run:` line (not its name).
⛔ Observe the extended arm RED on a byte-backed ci.yml copy with `sql-tests`' seam step moved after
its boot step, record the message verbatim, restore, `cmp`-verify.
  </action>
  <verify>
    <automated>bash -c 'set -eu; node -e "const y=require(\"fs\").readFileSync(\".github/workflows/ci.yml\",\"utf8\");const n=(y.match(/- name: Baseline currency - name the migrations the lane replays on top of the dump/g)||[]).length;if(n!==2)throw new Error(\"MEASURE_FAIL: expected 2 renamed currency steps, found \"+n);console.log(\"currency steps renamed: 2\")"; bash scripts/local-stack/run.sh --check-currency | grep -E "^baseline-replay: [0-9]+ migration\(s\) newer than the dump"; npx vitest run src/__tests__/local-stack-lane-wiring.test.ts'</automated>
    <fails_when>the node check throws `MEASURE_FAIL: expected 2 renamed currency steps`, the seam prints no `baseline-replay:` line (grep exits 1), or vitest reports a failed arm in local-stack-lane-wiring.test.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - exactly 2 steps in ci.yml carry the new name, each with `run: bash scripts/local-stack/run.sh --check-currency`
    - `grep -c "refuse a schema dump older" .github/workflows/ci.yml` prints 0 and `grep -ci "refuses a baseline older" .github/workflows/ci.yml` prints 0
    - `actionlint .github/workflows/ci.yml` exits 0
    - `npx vitest run src/__tests__/local-stack-lane-wiring.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts src/__tests__/contracts/ci-docs-path-filter.contract.test.ts` passes
    - the SUMMARY records the per-job fetch-depth finding and the extended arm's RED message verbatim
  </acceptance_criteria>
  <done>Both currency steps are named for bounding and naming the replay set, no comment argues from the retired refusal, clone depth follows a measured consumer, and the seam-before-boot pin covers `sql-tests`.</done>
</task>

<task type="auto">
  <name>Task 2: the mutex runbook and CLAUDE.md describe what exists, with lineage kept</name>
  <files>docs/runbooks/shared-test-db-mutex.md, CLAUDE.md</files>
  <read_first>
    - docs/runbooks/shared-test-db-mutex.md (every `sql-tests` mention)
    - CLAUDE.md (the sections "Which database am I on?", "Currency 2026-09-09 — what shared TEST IS now", and "SQL gate integrity jobs")
    - .github/workflows/ci.yml (`test-db-drift`, `python`, `e2e-seeded` acquire steps, by name — to re-measure the key census)
  </read_first>
  <action>
Runbook: classify every `sql-tests` mention in `docs/runbooks/shared-test-db-mutex.md` as one of
three things and act accordingly — a LIVE statement about who holds the key or which job to look at
during an incident: correct it to name `python`, `e2e-seeded` and `test-db-drift`; a DATED
historical reading (the Phase 158 three-holder framing, the lock-time arithmetic, the starvation
narrative): keep it and mark it as lineage with the date and what superseded it; a statement about
the corpus rather than the lock: leave it. Add one short section at the top of the holders
discussion recording what this phase changed: one holder removed, the corpus moved to a database
private to its runner, VAC-08 moved into `test-db-drift` which now holds the key, cross-run
contention reduced but NOT eliminated, and a pointer to `164.4.2-MEASUREMENT.md` for the AFTER
numbers.

CLAUDE.md: for each stale sentence `<context>` lists, verify it at HEAD, then add a dated
`⛔ CORRECTED` note beside it stating the current fact, measuring any number fresh (the key census in
`ci.yml` AND in `supabase-migrate.yml`, by `grep -c 61616158`). Add one dated note under the TEST
currency section recording D-F: the local-stack lane replays the migrations newer than
`supabase/schema/baseline.sql`, the carried set is `supabase/schema/baseline-carried-migrations.txt`
bound to the dump's sha256, and part (b) of `[164.8-PUSH-RACE-VAC08]` is narrowed for `sql-tests`
because its lane replays a PR's own migration before merge. ⛔ Do not touch the floors section's
rule against restated constants; this task writes no floor value.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const r=fs.readFileSync('docs/runbooks/shared-test-db-mutex.md','utf8');if(!r.includes('test-db-drift'))throw new Error('MEASURE_FAIL: the runbook never names the new holder');const c=fs.readFileSync('CLAUDE.md','utf8');for(const k of ['test-db-drift','baseline-carried-migrations.txt'])if(!c.includes(k))throw new Error('MEASURE_FAIL: CLAUDE.md never mentions '+k);console.log('runbook and CLAUDE.md name the current holders and the carried-migrations marker')"</automated>
    <fails_when>node throws `MEASURE_FAIL: the runbook never names the new holder` or `MEASURE_FAIL: CLAUDE.md never mentions …`</fails_when>
  </verify>
  <acceptance_criteria>
    - the node check prints its OK line
    - each of the four stale CLAUDE.md sentences still exists verbatim (lineage) AND has a `CORRECTED` note within the same bullet or paragraph (the diff of `CLAUDE.md` from the plan's base SHA, recorded at plan start, to HEAD shows only additions around them, no deletions of the original sentences)
    - the SUMMARY lists every runbook `sql-tests` mention with its classification (live / dated / corpus)
    - `npm run check:planning-hygiene` prints its OK line after `git add`
  </acceptance_criteria>
  <done>The runbook names the holders that exist and keeps superseded readings as dated lineage; CLAUDE.md carries dated corrections for every sentence this phase made stale and a dated D-F note.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 (orchestrator): push, then read every PENDING criterion off a COUNTED, SHA-bound run</name>
  <files>(none — the reading is recorded in 164.4.2-08-SUMMARY.md)</files>
  <what-built>Plans 06-08 on the phase branch: D-F's replaying lane, the renamed currency steps, the runbook and CLAUDE.md corrections — plus plan 05's `sql-tests` / `test-db-drift` split, whose CI criteria are still PENDING.</what-built>
  <how-to-verify>Orchestrator: push the phase branch; `gh api "repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<sha>" -q '.workflow_runs | length'` must be non-zero; open the `CI` run at that SHA and read each job and step named in the action below; paste every line verbatim into this plan's SUMMARY.</how-to-verify>
  <resume-signal>Reply "recorded: run <id> @ <sha>, <n> runs, frontend success" — or name the red job and step, which routes back to the plan that owns it.</resume-signal>
  <read_first>
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-05-SUMMARY.md (the table "CI-run-dependent done criteria — PENDING the orchestrator's CI run")
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-06-SUMMARY.md
  </read_first>
  <action>
The executor STOPS here and returns the checkpoint; worktree agents cannot push. The ORCHESTRATOR
pushes the phase branch and reads one run. First COUNT: `gh api "repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<sha>" -q '.workflow_runs | length'`
must be non-zero — an empty count satisfies a numeric comparison and proves nothing. Then read, from
the `CI` run at that head SHA, by job and step NAME:
  * every row of plan 05's PENDING table, EXCEPT that the currency row is now read from the renamed
    step and proves itself with the `baseline-currency: … defects=0` line plus the
    `baseline-replay:` line (expected `(none)` unless a migration newer than the dump is on the
    branch — whatever it prints, record it verbatim);
  * plan 06's lane lines in `sql-tests`, `frontend-local-stack` and `frontend-live-db-lane`: the
    `replay complete: <K> migration(s) applied on top of the dump` log line in each boot step;
  * the corpus step's final line in `sql-tests` — which answers, on a real runner, whether the two
    ledger-oracle corpus files pass on the lane now that it has a ledger;
  * `frontend` concluded success, and `sql-tests`, `test-db-drift`, `frontend-local-stack`,
    `frontend-live-db-lane` and `sql-mutation` each concluded.
Record the run id, head SHA, the run count and every line above in the SUMMARY. A RED anywhere is
routed back to the plan that owns it, not re-described. ⛔ Never write a CI skip token in any commit
message pushed here.
  </action>
  <verify>
    <human-check>The recorded run is bound to the branch head SHA, the `workflow_runs` count for that SHA is non-zero, every row above has its verbatim line, and `frontend` is green. A board showing only two Vercel checks reads as clean while CI never ran.</human-check>
  </verify>
  <acceptance_criteria>
    - the SUMMARY carries: head SHA, `CI` run id, the non-zero `workflow_runs` count, and one verbatim log line per criterion listed in the action
    - `frontend` concluded `success` on that run
  </acceptance_criteria>
  <done>Every criterion plan 05 left PENDING and every lane line plan 06 added has been read off a counted, SHA-bound run and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a step name / comment -> the mechanism it describes | a name for a retired refusal invites the wrong "fix" |
| a green board -> a green CI | a suppressed or absent run shows no red |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-17 | Repudiation | re-argued wiring pins | high | mitigate | The extended seam-before-boot arm is neutered and observed RED before it is trusted; RED message recorded verbatim. |
| T-164.4.2-18 | Spoofing | a green board that is not a green CI | high | mitigate | The run is COUNTED via the runs API and bound to the head SHA; each job read by name. |
| T-164.4.2-19 | Tampering | CI suppressed by a skip token in a commit message | high | mitigate | No commit message in this phase writes one, in any form, including inside a denial. |
| T-164.4.2-36 | Tampering | a shallow clone silently changing a job's behaviour | medium | mitigate | `fetch-depth` is removed only where a per-job measurement found no git-history consumer; where one exists the comment names it and the pin asserts it. |
</threat_model>

<verification>
- Task 1's automated check and the four vitest files pass; `actionlint` is clean.
- Task 2's check passes; CLAUDE.md shows only additions around the stale sentences.
- The Task 3 reading is recorded with run id, head SHA and a non-zero run count.
- `npm run check:planning-hygiene` prints its OK line.
</verification>

<success_criteria>
Nothing in CI, the runbook or CLAUDE.md still describes the retired refusal or the retired holder
set as current, and the tracer's green — plus D-F's lane lines — is a counted fact rather than an
absence of red.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Consumer |
|---|---|---|
| renamed currency steps + corrected comments in `ci.yml` | workflow | every lane-booting job |
| re-subjected history / seam-before-boot arm | pin | vitest |
| mutex runbook holders section | runbook | incident responder |
| dated CLAUDE.md corrections + D-F note | project instructions | every session |
| the counted SHA-bound CI reading | evidence in the SUMMARY | phase verifier |

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-08-SUMMARY.md` when done.
</output>
