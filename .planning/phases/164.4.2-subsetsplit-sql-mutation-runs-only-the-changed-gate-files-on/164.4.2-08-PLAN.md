---
phase: 164.4.2-subsetsplit
plan: "08"
type: execute
wave: 7
depends_on: ["164.4.2-07"]
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements: [AREA-D, REDUNDER-SUBSET-SPLIT]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On a pull request, sql-mutation mutates only the gate files that pull request changed."
    - "On a push to main, sql-mutation mutates the FULL corpus and enforces every floor — a merge is the moment the full picture is owed."
    - "A run whose scope line is absent FAILS the job; an absent line is never read as a full run."
    - "A narrowed run on a push is a job failure, not a quiet optimisation."
    - "The subset never reaches a shell as interpolated text."
  artifacts:
    - .github/workflows/ci.yml
  key_links:
    - "changed-paths already carries fetch-depth: 0 and is already a needs: of sql-mutation — the derivation lives there so no second job needs a deep clone and no second diff mechanism exists."
    - "The assert step's arms are driven RED and GREEN against synthetic runner logs, the same way an existing contract test already spawns bash on a step body extracted from ci.yml."
---

<objective>
DECISION D's wiring: select the subset on a pull request, the full corpus on a push, and make
the vacuity fence a property of the JOB rather than of the runner alone.

Purpose: the runner gained the mode in plan 07 and nothing calls it yet. This plan calls it —
and adds the out-of-process half of the fence, because `sql-mutation`'s assert step's whole job
is reading numbers out of a text file it did not produce, and a subset run changes which of
those numbers are comparable.

⭐ It also answers the ROADMAP's open question about the full-corpus run's attribution:
CONTEXT DECISION D puts the full corpus on the PUSH to main rather than on a schedule, so its
red lands on the merge commit, on the board branch protection and Railway already read, with an
author and a diff. A scheduled run's red lands on nobody.

Output: a PR that touches three gate files mutates three gate files; a merge mutates all of them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-PATTERNS.md
@src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts

**Read by name in `.github/workflows/ci.yml` before editing:** the `changed-paths` job (its
`outputs:` block, its `fetch-depth: 0` checkout and its two-step self-test-then-run shape); the
`sql-mutation` job's "Mutate every annotated RED-UNDER arm and require it to bite" step (its
MODE IDENTITY comment and its file-not-pipe stdout capture) and its "Assert the run PRINTED its
coverage and cleared both floors" step (every arm in it).

**Which assertions change in a subset run, and which must not:**
| assertion | subset run |
|---|---|
| `coverage: files N/M` present, N >= FILES_FLOOR | UNCHANGED — N comes from the full scan |
| `unreachable:` / `lane-blocked:` / `lane-probe:` present, claim equals names | UNCHANGED — all derived from the full scan |
| `arms: E/A/W` present, E > 0, W <= ceiling | UNCHANGED |
| `biting: N` present, N <= E, `lane-invocations` equals E | UNCHANGED |
| per-file rows present, rows sum to `biting` | UNCHANGED |
| per-file row COUNT equals the coverage numerator | CHANGED — it equals the narrowed count instead |
| `biting >= ARMS_FLOOR` | NOT COMPARED — the run says so, and says where it IS compared |

⛔ Constraints:
- ⛔ This plan owns `ci.yml` in wave 7; nothing else edits it concurrently.
- ⛔ Nothing raises `sql-mutation`'s `timeout-minutes`. The subset rule IS the answer to a
  crossing; a third value is not available.
- ⛔ The full-corpus invocation stays BYTE-IDENTICAL to the command in the runner's own header
  and to what a developer runs locally (MODE IDENTITY). The subset arm is a second, named
  invocation — argue it in the comment rather than blurring the rule.
- ⛔ The changed-file list must never be interpolated into a shell command. It crosses into the
  step as an `env:` value and is written to a file in `RUNNER_TEMP` — not into the workspace,
  which the runner asserts is unchanged across its own run.
- ⚠️ `grep -c` exits 1 on a zero count; use `set +e; …; rc=$?; set -e; [ "$rc" -le 1 ]`.
  ⛔ No `PIPESTATUS`.
- ⛔ Never write a CI skip token in a commit message, in any form.
</context>

<tasks>

<task type="auto">
  <name>Task 1: changed-paths publishes the subset, in the job that already has the deep clone</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Extend the `changed-paths` job with the two outputs plan 07's derivation emits (the mode flag
and the name list), produced by a step that runs `node scripts/sql-gate-subset.mjs` — preceded
by its `--self-test`, matching this job's existing red-path-first shape for the docs classifier
and the pairing `sql-function-snapshot.yml` uses.

⭐ Say WHY it lives here, in the job's comment: this job is already `needs:` of `sql-mutation`,
already checks out with `fetch-depth: 0` because a three-dot diff needs the base branch, and
already owns "what changed against the merge base". A second job computing the same thing would
be a second thing to keep identical in ref resolution, rename handling and fetch depth — the
divergence class this repo has a dated record of.

⛔ Do not add an `if:` to this job and do not change its existing `docs_only` output. A
conditional here would not skip one gate; it would skip every dependent at once.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');const i=y.indexOf('\n  changed-paths:');const j=y.indexOf('\n  frontend-typecheck:',i);const b=y.slice(i,j);if(!b.includes('sql-gate-subset.mjs --self-test'))throw new Error('MEASURE_FAIL: the derivation self-test does not run before the derivation');if((b.match(/sql-gate-subset\.mjs/g)||[]).length<2)throw new Error('MEASURE_FAIL: the derivation is not invoked for real');if(!/outputs:[\s\S]{0,400}sql_gate/.test(b))throw new Error('MEASURE_FAIL: the job publishes no subset output');console.log('changed-paths self-tests then publishes the subset')"</automated>
  </verify>
  <done>`changed-paths` proves the derivation can fail, then runs it, and publishes both outputs; `docs_only` and the job's unconditional shape are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: sql-mutation narrows on a pull request and never on a push</name>
  <files>.github/workflows/ci.yml</files>
  <action>
In the "Mutate every annotated RED-UNDER arm and require it to bite" step, select the mode from
`changed-paths`'s outputs:

  * mode is subset AND the event is `pull_request` -> write the name list from its `env:`
    variable into `"$RUNNER_TEMP"/sql-gate-subset.txt` (one path per line, via `printf`, never
    `eval` and never inline interpolation), then invoke the runner with the subset flag pointing
    at that file;
  * anything else -> the existing full-corpus command, BYTE-IDENTICAL, including its stdout
    capture to a file rather than a pipe (`$?` after a pipe is the last command's status, and
    that hazard has bitten this repo's own plan execution before).

⛔ Belt AND braces on the event, because this is the arm that would hide a regression at the
only moment the full picture is owed: the derivation already hard-codes a full verdict for any
event that is not a pull request, and this step must ALSO refuse to take the subset branch on a
push. Two independent controls, because one of them is a script this job does not own.

Extend the step's MODE IDENTITY comment rather than deleting it: state that the full-corpus arm
is still the byte-identical developer command, that the subset arm is a distinct named mode
that prints its own scope, and that a subset arm reachable on a push would be the defect this
whole phase family exists to refuse.

⛔ Do not touch `timeout-minutes`, the `needs:`/`if:` lines, the pg_cron provisioning, the
dead-holder drill, the meta-command preflight or the runner self-test step.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');const i=y.indexOf('\n  sql-mutation:');const j=y.indexOf('\n  plan-anchor-verify:',i);const b=y.slice(i,j);if(!/subset/i.test(b))throw new Error('MEASURE_FAIL: sql-mutation has no subset arm');if(!/RUNNER_TEMP/.test(b))throw new Error('MEASURE_FAIL: the subset list is not written to RUNNER_TEMP');if(!/pull_request/.test(b))throw new Error('MEASURE_FAIL: the step does not gate the subset arm on the event');const t=b.match(/timeout-minutes:\s*(\d+)/);if(!t||Number(t[1])>20)throw new Error('MEASURE_FAIL: sql-mutation timeout-minutes is '+(t&&t[1])+' — 20 is a declared ceiling');console.log('sql-mutation narrows only on a pull request; timeout ceiling intact')"</automated>
  </verify>
  <done>A pull request with changed gate files runs the narrowed mode; every other event runs the byte-identical full command; the list reaches the runner through a file, never through a shell string; `timeout-minutes` is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: the assert step judges a narrowed run honestly — proven on synthetic logs</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Extend the "Assert the run PRINTED its coverage and cleared both floors" step with the subset
arm, in the step's own established discipline — an absent line is a `MEASURE_FAIL` with its own
message and never a zero, an unreadable count is checked BEFORE any comparison, and the `|| true`
after each `grep -a -m1` stays because an empty-but-present value must reach the numeric guard
rather than aborting the step under `set -euo pipefail`.

New arms:
  1. the `scope:` line is ABSENT -> MEASURE_FAIL. ⛔ A run that did not say what it covered is
     never assumed to have covered everything. This is the single most load-bearing arm in the
     step.
  2. the line says SUBSET while the event is a push -> MEASURE_FAIL naming both facts. A merge
     is the moment the full picture is owed.
  3. the line says SUBSET -> the count it CLAIMS must equal the number of `*.sql` names it
     PRINTS (the same claim-versus-names discipline the `unreachable:` and `lane-blocked:` arms
     already use), and the per-file row count must equal that number rather than the coverage
     numerator.
  4. the line says SUBSET -> ARMS_FLOOR is NOT compared, and the step SAYS so in its closing
     summary line together with where it is compared. ⛔ It must not print a sentence claiming
     both floors held.
  5. the line says FULL (including the subset-fallback form) -> every existing arm applies
     unchanged, ARMS_FLOOR included.

⛔ PROVE THE ARMS, without waiting for a lucky pull request. The step reads a LOG FILE, so it
can be driven against synthetic ones: extract the step body from `ci.yml` and spawn `bash` on
it — the idiom an existing contract test in this repo already uses — against four crafted logs:
a FULL log, a SUBSET log, a log with no `scope:` line, and a SUBSET log with the event set to a
push. Observe the verdict for each, record every RED message VERBATIM in the SUMMARY, and keep
the harness's own output as evidence. ⛔ A fence nobody watched fail is a fence nobody has.

Then take the real run: push the branch, and record the run id, the head SHA and the `scope:`
line `sql-mutation` actually printed. ⚠️ Expect the FULL form with the subset-fallback reason —
this branch changes no gate file, and that is the SAFE arm doing its job. Say that plainly
rather than implying the subset arm was observed in CI; the synthetic-log evidence above is
what covers it.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');const i=y.indexOf('Assert the run PRINTED its coverage');const b=y.slice(i,y.indexOf('\n  plan-anchor-verify:',i));for(const [pat,msg] of [[/scope:/, 'the assert step never reads the scope line'],[/MEASURE_FAIL[\s\S]{0,400}scope/, 'an absent scope line is not a MEASURE_FAIL'],[/github\.event_name|GITHUB_EVENT_NAME/, 'the step cannot tell a push from a pull request']])if(!pat.test(b))throw new Error('MEASURE_FAIL: '+msg);console.log('the assert step judges the scope line and knows the event')"</automated>
  </verify>
  <done>All five arms exist; four were driven against synthetic logs and their verdicts recorded; a real CI run's `scope:` line is recorded with its run id and head SHA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `changed-paths` output -> a shell step in `sql-mutation` | fork-PR-influenced text crosses a job boundary |
| a runner log -> a pass/fail verdict | the step judges a file it did not produce |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-24 | Tampering | the name list reaching a shell | high | mitigate | Strict anchored pattern at the derivation (plan 07), `env:` handoff rather than inline interpolation, `printf` into a `RUNNER_TEMP` file, and the runner re-validating every line it reads. |
| T-164.4.2-25 | Repudiation | a narrowed run on a merge | critical | mitigate | Two independent controls — the derivation's non-PR hard-coding and the step's own event guard — plus an assert arm that fails the job if a SUBSET line appears on a push. |
| T-164.4.2-26 | Spoofing | an absent scope line read as full coverage | critical | mitigate | Absent line is a MEASURE_FAIL with its own message, driven RED against a synthetic log before it is trusted. |
| T-164.4.2-27 | Tampering | the subset list written into the workspace | medium | mitigate | Written to `RUNNER_TEMP`; the runner's own dirty-checkout defect would otherwise fire on a file beside the corpus. |
</threat_model>

<verification>
- All three task checks pass.
- The four synthetic-log runs are recorded with their verdicts and RED messages.
- A real CI run is recorded: run id, head SHA, the `scope:` line, and `sql-mutation` green.
- `sql-mutation`'s `timeout-minutes` is unchanged and no floor, ceiling or waiver moved.
</verification>

<success_criteria>
`sql-mutation` is no longer a job that can only get slower, and the narrowing cannot be
mistaken for coverage: every run says what it covered, a merge always covers everything, and a
run that says nothing fails.
</success_criteria>

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-08-SUMMARY.md` when done.
</output>
