---
phase: 164.4.2-subsetsplit
plan: "03"
type: execute
wave: 2
depends_on: ["164.4.2-02"]
files_modified:
  - supabase/schema/baseline.sql
  - supabase/schema/BASELINE.md
  - scripts/local-stack/run.sh
  - src/__tests__/local-stack-lane-wiring.test.ts
  - .github/workflows/ci.yml
autonomous: false
requirements: [AREA-A]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The ephemeral (local-stack) path REFUSES to boot from a baseline that is older than the migrations directory."
    - "The currency guard is meaningful in CI, not only on a developer's full clone — the jobs that boot the lane fetch real history."
    - "A boot that loaded the baseline without measuring currency fails, rather than passing quietly."
  artifacts:
    - scripts/local-stack/run.sh
    - src/__tests__/local-stack-lane-wiring.test.ts
  key_links:
    - "load_baseline() calls the shared gate BEFORE psql loads the dump — the same position refuse_stale_baseline occupies in the restore script's call order."
    - "frontend-local-stack and frontend-live-db-lane both check out with fetch-depth: 0, because a shallow clone makes git log -1 --format=%ct read the clone date for every path."
---

<objective>
DECISION A's first deliverable, half two: the EPHEMERAL path now refuses a stale baseline, in
CI as well as locally — and the stale baseline that exists today is cleared first, by the only
party who can clear it.

Purpose: `164.4.2-CONTEXT.md` Area A: "A job moved onto a stale baseline is a job whose green
got cheaper, which is the opposite of this phase's purpose." Plans 04 and 05 move a job onto
this path; this plan is the precondition. ⚠️ MEASURED at `594e5471` and re-measured by plan 02
at HEAD: `baseline.sql` last changed 2026-09-18, `supabase/migrations/` 2026-09-20, so wiring
the guard without refreshing the dump would turn `frontend-local-stack` and
`frontend-live-db-lane` red on arrival. That ordering is why the checkpoint comes first.

⭐ It is not only a hygiene concern. `supabase/tests/test_strategy_sync_cursors_rls.sql` is in
the corpus plan 05 moves onto this lane, and the table it asserts on is created by
`20260919120000_strategy_sync_cursors.sql` — one of the two migrations that landed AFTER the
dump. On today's baseline that file cannot pass on the ephemeral lane for a reason that has
nothing to do with what it asserts.

Output: a refreshed baseline, a lane that refuses a stale one, and CI that can tell.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-RESEARCH.md
@scripts/local-stack/run.sh
@supabase/schema/BASELINE.md
@src/__tests__/local-stack-lane-wiring.test.ts

**The insertion point (PATTERNS §1):** `load_baseline()` in `scripts/local-stack/run.sh` —
after the "no schema baseline at ..." existence refusal and BEFORE the `psql ... -f
"$BASELINE_FILE"` load, which is the position `refuse_stale_baseline` occupies in
`restore-test-from-baseline.sh`'s own call order (filesystem-only refusals fire before the
first connection).

**Why `fetch-depth: 0` is mandatory (RESEARCH R1, quoting this repo's own comment in
`.github/workflows/test-restore-from-baseline.yml`):** a shallow clone makes
`git log -1 --format=%ct` "read the clone date". Two paths that both report the clone's
timestamp compare equal, and the guard passes vacuously. MEASURED: `frontend-local-stack`'s
checkout carries only `persist-credentials: false` — no `fetch-depth` key — and
`frontend-live-db-lane` boots the same lane.

⛔ Constraints, all binding:
- ⛔ Run NO database command against any remote. This checkout's Supabase CLI is linked to
  PRODUCTION. ⛔ Do not run `scripts/local-stack/run.sh up` locally either: `supabase start`
  is not on this phase's permitted local-DB list (`scripts/pg-lane/run.sh` is the only one).
  The lane's real boot is exercised in CI, on the branch's own PR run.
- ⛔ Nothing raises a timeout, lowers a floor, adds a waiver, or clears a red by widening an
  allowlist.
- ⛔ Never `git checkout -- <path>`, `git restore` or `git stash` in a neuter/restore cycle —
  use a `cp` byte backup verified with `cmp`, re-taken after every edit.
- ⛔ `.github/workflows/ci.yml` is a heavily-pinned shared file. This plan owns it in wave 2;
  no other plan may edit it concurrently.
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Founder regenerates the committed baseline dump from PRODUCTION</name>
  <action>Regenerate `supabase/schema/baseline.sql` from the PRODUCTION catalogue, and update `supabase/schema/BASELINE.md` alongside it</action>
  <instructions>
    I have built the currency gate (plan 02) and run it at HEAD. It REFUSES, correctly:
    `supabase/schema/baseline.sql` last changed 2026-09-18 and `supabase/migrations/` last
    changed 2026-09-20, so two migrations landed after the dump —
    `20260919120000_strategy_sync_cursors.sql` and
    `20260920120000_api_keys_venue_account_id_grant.sql`. Both were applied to PROD by the
    normal merge path; only the committed dump is behind.

    ⛔ This is the one step I cannot take. Regenerating the dump is a read-only
    `supabase db dump --linked` against PRODUCTION, and this phase runs no database command
    against any remote — BASELINE.md itself records that committing a regenerated dump is "a
    deliberate, reviewed act, never a casual `git add`".

    Follow the procedure already written in `supabase/schema/BASELINE.md`:
      1. take the schema-only dump from the production catalogue;
      2. run the secret scan recorded in `scripts/local-stack/REPLAY-SPIKE.md` FIRST and treat
         any hit as do-not-commit (this repository is PUBLIC);
      3. update BASELINE.md's provenance table — date, CLI version, sha256, and the measured
         shape (tables / policies / function statements / data statements, which must stay 0);
      4. add a dated "Regenerated" section saying WHICH migrations the new dump now carries.

    Then tell me it is committed and I will re-run both gates.
  </instructions>
  <verification>
    I will run, and both must be green, for two DIFFERENT questions:
      * `node scripts/check-baseline-staleness.mjs` — INTEGRITY: the sha256 in BASELINE.md
        matches the file on disk;
      * `node scripts/check-baseline-currency.mjs` — CURRENCY: the dump is at least as fresh
        as `supabase/migrations/`.
    I will also re-run the baseline-adjacent gates (`npx vitest run
    src/__tests__/baseline-content-drift-allowlist.test.ts src/__tests__/baseline-wiring-claim.test.ts`,
    `node scripts/baseline-content-drift-check.mjs`) and report any fallout.
    ⛔ If one of those reddens I will report it as a finding — I will not clear it by widening
    an allowlist.
  </verification>
  <resume-signal>Type "done" when the regenerated baseline and its BASELINE.md update are committed</resume-signal>
</task>

<task type="auto">
  <name>Task 2: load_baseline() refuses a stale dump — proven by driving it RED</name>
  <files>scripts/local-stack/run.sh, src/__tests__/local-stack-lane-wiring.test.ts</files>
  <action>
In `scripts/local-stack/run.sh`, call the shared currency gate from `load_baseline()` at the
position named in `<context>`: after the existence refusal, before `psql` is invoked. Invoke it
as `node "${REPO_ROOT}/scripts/check-baseline-currency.mjs"` with `BASELINE_FILE` and
`MIGRATIONS_DIR` passed explicitly so the lane and the gate cannot disagree about which files
were compared, and expose the same `FRESHNESS_TS_CMD` seam by pass-through so the arm below can
drive it.

On refusal the lane must ABORT before the load, with the gate's own message plus one line of
lane context naming what it was about to do. ⛔ Not a warning: a lane that logs "stale" and
loads anyway is precisely the gate-that-measures-nothing class this phase family exists to
remove.

Follow the file's own seam idiom (`--print-baseline-path`, `--print-workdir`): add
`--check-currency` to the dispatcher, running the same call `load_baseline()` makes and exiting
with its status, so a test can ASK the lane rather than pattern-match an assignment line
(R2-W06: any ordinary re-spelling reads as "unwired" to a substring match). Document it in
`usage()` beside the other print seams.

Extend `src/__tests__/local-stack-lane-wiring.test.ts` with one arm pinning that the lane's
boot path measures currency, following THAT file's existing idiom for how it interrogates the
lane. Keep the arm's claim narrow and true.

⛔ PROVE IT BITES, with the lane's own seam rather than by editing history: run
`FRESHNESS_TS_CMD=<stub> bash scripts/local-stack/run.sh --check-currency` with a stub that
reports the baseline OLDER than the migrations, observe the REFUSAL, record the message
VERBATIM for the SUMMARY; then run it with a stub reporting the baseline NEWER and observe the
pass. Two stubs, two directions — a check only proven in one direction is half a check.
Byte-backup before any edit to the test file, `cmp`-verify any restore, re-take after each
edit.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/local-stack-lane-wiring.test.ts</automated>
  </verify>
  <done>`bash scripts/local-stack/run.sh --check-currency` reaches a verdict and exits with the gate's status; the stale-stub run was observed refusing and its message is in the SUMMARY; the lane-wiring test passes with its new arm.</done>
</task>

<task type="auto">
  <name>Task 3: the two jobs that boot the lane fetch real history, and CI notices if the guard goes missing</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Add `fetch-depth: 0` to the `actions/checkout` step of BOTH `frontend-local-stack` and
`frontend-live-db-lane`, keeping `persist-credentials: false` as-is. Put the reason in a
comment at each site, in this file's voice and citing the mechanism rather than an authority:
a shallow clone makes `git log -1 --format=%ct` report the clone's own commit time for every
path, so the currency guard would compare two identical timestamps and pass having measured
nothing. ⛔ Do not cite line numbers — `[164.7-CITATION-DRIFT-01]` is this repo's dated record
of what that costs; cite by job name and step.

Add ONE step to `frontend-local-stack`, before the boot, that runs the lane's own
`--check-currency` seam and fails the job on refusal. Two reasons it is a separate step rather
than left implicit in `up`: the verdict gets its own named line in the job log, and a future
edit that drops the call out of `load_baseline()` fails HERE instead of booting quietly on a
stale schema.

⚠️ `frontend-live-db-lane` needs the `fetch-depth` change too (it boots the same lane and the
guard now runs inside `up`), but it does NOT need the extra step — one owner for the explicit
assertion is enough, and a second copy is a second thing to keep identical.

⛔ Change nothing else in either job: not the timeouts, not the `if:`, not the aggregator rows.
Those belong to plan 05.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8');const jobs=['frontend-local-stack','frontend-live-db-lane'];for(const j of jobs){const i=y.indexOf('\n  '+j+':');if(i<0)throw new Error('job not found: '+j);const next=y.indexOf('\n  frontend-policy:',i);const body=y.slice(i,next>0?next:y.indexOf('\n  sql-gate-lint:',i));if(!/fetch-depth:\s*0/.test(body))throw new Error('MEASURE_FAIL: '+j+' still checks out without fetch-depth: 0');}console.log('both lane-booting jobs fetch full history')"</automated>
  </verify>
  <done>Both lane-booting jobs check out with full history; `frontend-local-stack` carries a named currency step whose absence would be visible; no other job, timeout or `if:` was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed schema dump -> every ephemeral database | one file defines what every lane believes the catalogue is |
| PRODUCTION catalogue -> a PUBLIC repository | the dump is committed to a world-readable repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-06 | Spoofing | a green lane on a stale catalogue | high | mitigate | `load_baseline()` refuses before `psql` runs; the refusal is observed in both directions with an injected `FRESHNESS_TS_CMD` stub. |
| T-164.4.2-07 | Information Disclosure | the regenerated `baseline.sql` | high | mitigate | The human-action checkpoint requires BASELINE.md's own documented secret scan BEFORE commit, and any hit is do-not-commit. The repository is public. |
| T-164.4.2-08 | Tampering | the guard silently removed from the boot path | medium | mitigate | A named CI step invokes the lane's `--check-currency` seam independently of `up`, and a vitest arm pins that the boot path measures currency. |
| T-164.4.2-09 | Spoofing | the guard passing on a shallow clone | high | mitigate | `fetch-depth: 0` on both lane-booting jobs, with the mechanism recorded at each site. |
</threat_model>

<verification>
- `node scripts/check-baseline-currency.mjs` exits 0 at the plan's end (it exited 1 at the plan's start — record both).
- `node scripts/check-baseline-staleness.mjs` still exits 0 (integrity, a different question).
- `npx vitest run src/__tests__/local-stack-lane-wiring.test.ts` passes.
- The stale-stub refusal message is recorded verbatim in the SUMMARY.
- A real PR run of this branch shows `frontend-local-stack` and `frontend-live-db-lane` green with the new step present — record the run id and bind it to the head SHA.
</verification>

<success_criteria>
The ephemeral path cannot boot from a schema older than the migrations that describe it, that
refusal has been observed rather than assumed, and it is honest in CI because the jobs that run
it fetch real history.
</success_criteria>

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-03-SUMMARY.md` when done.
</output>
