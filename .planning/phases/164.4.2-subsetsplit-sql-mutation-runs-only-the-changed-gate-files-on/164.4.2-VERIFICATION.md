---
phase: 164.4.2-subsetsplit
verified: 2026-09-24T00:00:00Z
status: human_needed
score: 15/17 must-haves verified
verified_at_sha: b91bfeb572bf952296a1491205cd20c30122c8bc
drift_subjects:
  - scripts/mutation-runner/run.mjs
  - scripts/sql-gate-subset.mjs
  - scripts/classify-changed-paths.mjs
  - scripts/check-baseline-currency.mjs
  - scripts/restore-test-from-baseline.sh
  - scripts/local-stack/run.sh
  - scripts/local-stack/nonpublic-objects.mjs
  - scripts/local-stack/nonpublic-catalogue.sql
  - scripts/local-stack/acl-fidelity.mjs
  - scripts/local-stack/capability-probe.mjs
  - scripts/local-stack/sql-corpus-report.mjs
  - supabase/schema/baseline-carried-migrations.txt
  - supabase/schema/baseline.sql
  - .github/workflows/ci.yml
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/local-stack-lane-wiring.test.ts
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Fill 164.4.2-MEASUREMENT.md's `## AFTER` tables from the first concluded merge-push run of ci.yml on main whose head has this phase's merge commit as an ancestor, then apply clauses (a)-(d)."
    expected: "(a) run-total below 16m18s, (b) sql-tests job-total below 8m31s, (c) python + e2e-seeded + test-db-drift combined acquire-wait below 12m05s, on a non-degenerate run. A clause that comes out wrong is recorded as 'the change did not work', not re-described. Provisional on one run, final at 5."
    why_human: "POST-MERGE. Only push runs on main at the new head are admissible, and none can exist before the merge. MEASUREMENT.md itself forbids calling this phase passed until the section is filled. Owner: orchestrator/founder, after the merge."
  - test: "On the first pull request after the merge that changes only supabase/tests/test_*.sql gate files (no migration, no runner/lane/ci.yml change), read sql-mutation's `scope:` line and the assert step's verdict, bound to that PR's head SHA with the runs counted."
    expected: "`scope: SUBSET k/N annotated files: <the changed names>`, the `ARMS_FLOOR: NOT compared` and static-upper-bound lines, the assert step's `SUBSET run on a pull_request … This is NOT full-corpus coverage.` line, and a green job."
    why_human: "POST-MERGE. The SUBSET branch cannot run in CI on this branch, because the branch changes the runner and ci.yml and the derivation correctly forces FULL. It was verified locally (real pg-lane run plus the real assert step body, below), but not yet observed in CI. Owner: whoever lands the next gate-only PR."
  - test: "Decide whether the delivered PR subset reach is acceptable against the ROADMAP goal 'sql-mutation stops being a job that can only get slower'."
    expected: "Either accept (record that the full-corpus path is still on a growing trajectory under the 20-minute ceiling and that the ceiling rule answers a crossing), or book a routed follow-on that names what happens when the FULL run nears 20 minutes."
    why_human: "Judgment call between two founder-owned texts. Under locked DECISION D, a PR that changes no gate file runs the FULL corpus, and so does every PR touching supabase/migrations/ or the mutation machinery, plus every push to main. Only gate-only PRs narrow. The ROADMAP instead asked for an explicit `0 files in scope` verdict on such PRs, with the floors enforced by a separate full run. The vacuity property holds either way. But on the paths that make up most of the job's volume, wall-clock headroom against the ceiling has not changed. No TODOS follow-on covers the FULL run reaching the ceiling."
---

# Phase 164.4.2: SUBSETSPLIT Verification Report

**Phase Goal:** `sql-mutation` stops being a job that can only get slower. On a PR it mutates only the gate files that PR changed, and a full-corpus run keeps enforcing the floors. Folded-in scope: DB-touching jobs stop sharing shared TEST (`sql-tests` onto a private local-stack lane as the tracer, VAC-08 rehomed to `test-db-drift`), plus DECISION F (the lane replays migrations newer than the dump) and DECISION G (the lane replays the non-`public` PROD objects).
**Verified:** 2026-09-24, at `b91bfeb5` on `feat/164.4.2-subsetsplit`
**Status:** human_needed
**Re-verification:** No. This is the initial verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | On a pull request, `sql-mutation` mutates only the gate files that PR changed (DECISION D) | ✓ VERIFIED | `judge()` in `scripts/sql-gate-subset.mjs` returns SUBSET only for `pull_request` with conforming, present gate files and no machinery change. The mutate step in `ci.yml` takes `--subset-from` only when `SQL_GATE_MODE=subset` AND `GITHUB_EVENT_NAME=pull_request`, and the names cross via `env:` + `read -r -a` + `printf`, never interpolated. **Run independently on the pg-lane:** `run.mjs --subset-from <1 file>` exited 0 and printed `scope: SUBSET 1/49 annotated files: test_api_keys_venue_identity_uniq.sql`, `arms: 7/7/0`, `biting: 7`, `lane-invocations: 7`. It has not yet been observed in CI (post-merge human item 2). |
| 2 | On a push to main, the FULL corpus runs and every floor is enforced | ✓ VERIFIED | The derivation forces FULL for any non-PR event, and the mutate step refuses SUBSET on a non-PR event on its own. The FULL path was observed in CI run `35926142486` at `0ca0dc6c`: `scope: FULL 49/49`, both floors held, 426 biting (orchestrator-recorded; the `sql-mutation` job is `success` there, checked). I extracted the real assert-step body from `ci.yml` and ran it on my real SUBSET log with `GITHUB_EVENT_NAME=push`: exit 1, `MEASURE_FAIL: the run narrowed to a SUBSET … on a 'push' event`. |
| 3 | A PR touching no gate file never reports a green indistinguishable from a full pass (ROADMAP shape 1) | ✓ VERIFIED (deviation noted) | `judge()` returns FULL with the reason `0 of N changed file(s) are gate files`. The derivation self-test row 3 passed, including "the EMPTY change set is FULL — never 'nothing to do'". The vacuity property holds because the run IS a full pass. The ROADMAP's literal `0 files in scope — floors enforced by the scheduled run` verdict was replaced by FULL under locked DECISION D. The consequence of that choice is human item 3. |
| 4 | FILES_FLOOR / ARMS_FLOOR / WAIVED_CEILING stay enforced on every merge path and are never lowered or compared against a narrowed tally (ROADMAP shape 2) | ✓ VERIFIED | Read by symbol: `FILES_FLOOR = 49`, `ARMS_FLOOR = 426`, `WAIVED_CEILING = 0`. In `runCorpus`, a SUBSET run still checks FILES_FLOOR (full-scan numerator) and WAIVED_CEILING. It does NOT compare the narrowed biting count to ARMS_FLOOR and says so. It also checks a full-corpus static upper bound (my run: `426 annotated-unwaived >= floor 426`). The push path compares `bitingArms < armsFloor`. |
| 5 | The split is printed on every run, never silent, and an absent `scope:` line fails the job | ✓ VERIFIED | Exactly one `scope:` line is logged before any lane runs. The real assert step, run on my SUBSET log with the scope line removed, exited 1 with `MEASURE_FAIL: the run printed NO 'scope:' line`. The same body on the intact log with `pull_request` exited 0 with `… This is NOT full-corpus coverage.` |
| 6 | `sql-mutation` `timeout-minutes` is not raised | ✓ VERIFIED | `timeout-minutes: 20` on the `sql-mutation:` job. The only `+timeout-minutes: 20` in the branch diff belongs to `sql-tests`, which went down from 90. |
| 7 | The full-corpus red is attributable (ROADMAP "also in scope") | ✓ VERIFIED | DECISION D puts the full run on the push to main, so a red lands on the merge commit, which the `frontend` aggregator and Railway read. No scheduled job exists to go unseen. |
| 8 | The floors ratchet has arms for the subset path (ROADMAP "also in scope") | ✓ VERIFIED | `mutation-runner-floors.test.ts` has a `164.4.2-07 (D-D)` describe block plus the SUBSET GREEN and RED extract-and-run arms. The file passed in my named vitest run. |
| 9 | `sql-tests` runs the corpus against a database private to its runner and waits on no key (DECISION B) | ✓ VERIFIED | The `sql-tests:` job body, with comments stripped, has 0 matches for `61616158`, `secrets.` or `vars.`. It boots `scripts/local-stack/run.sh` and runs `Run SQL self-tests against the local-stack lane`. `sql-tests` was `success` in runs `35922576855` and `35926142486`. |
| 10 | VAC-08 still runs on every merge against shared TEST, with the mutex and the schema-apply wait | ✓ VERIFIED | The `test-db-drift:` job has `Wait for the TEST schema apply…`, `Acquire shared-test-db mutex` (key 61616158), `VAC-08 - repo-vs-TEST ledger and function body drift`, and `needs: [python, changed-paths]`. The `frontend` aggregator lists it in `needs:` and in its result loop, and tolerates a skip only on a fork PR or `workflow_dispatch`. Run `35922576855`: `test-db-drift` success, VAC-08 `0 NEW drift` (orchestrator-recorded). |
| 11 | Whether the ephemeral lane can host the corpus is MEASURED, not assumed (Area A/B precondition) | ✓ VERIFIED | `capability-probe.mjs` is wired as the `sql-tests` step `Capability probe - does the lane supply what the SQL self-test corpus demands`. **Independent census at HEAD** (`sql-corpus-report.mjs` on a lane I booted): `sql-corpus: files=76 pass=75 fail=0 whole-file-skip=0 lane-only=1`. |
| 12 | Baseline currency has ONE implementation reachable from every caller, and an undeterminable set fails loud (DECISION A) | ✓ VERIFIED | `refuse_stale_baseline()` in `restore-test-from-baseline.sh` calls `node "$CURRENCY_CHECK"`. `load_baseline()` guards on `check_baseline_currency` (`--replay-set`) before psql reads the dump, and all three lane-booting jobs run `bash scripts/local-stack/run.sh --check-currency`. `check-baseline-currency.mjs --self-test`: `PASSED: 49/49`. At HEAD, the default mode reports `defects=0` and `--replay-set` reports `carried=273 replay=0 marker-sha=match defects=0`. `restore-test-from-baseline.test.ts` passed. |
| 13 | DECISION F: the lane replays exactly the migrations newer than the dump, names them, and writes a ledger row per carried and per replayed migration | ✓ VERIFIED | **Behavioural, run independently.** I booted the real lane with `LANE_MIGRATIONS_DIR` pointing at a scratch copy of the 273 migrations plus one probe migration. Output: `baseline-replay: 1 migration(s) newer than the dump: 29990101000000_verifier_replay_probe.sql`, `ledger: 273 carried …`, `replayed … (ledger row written)`, `replay complete: 1`. Catalogue read: probe row = 42, ledger = 274, carried-provenance rows = 273, replayed rows = 1. The failure arms (sha mismatch, absent marker, dump ahead of checkout, refdata filter) are driven by `local-stack-lane-wiring.test.ts`, which passed. |
| 14 | DECISION G: the lane carries the `auth.users` trigger and the folded `cron.job` set, a fidelity gate fails the boot on drift, and the 8 previously failing files pass UNEDITED | ✓ VERIFIED | Same boot: `nonpublic-extract: … auth-triggers=1 (on_auth_user_created<-20260405061912_rls_policies.sql) … cron-jobs=15`, `15 cron job(s) registered as the loading role postgres`, `nonpublic-fidelity: auth-users-triggers=1/1 cron-jobs=15/15 drift=0 verdict OK`. The catalogue read shows 15 `cron.job` rows and 1 non-internal trigger on `auth.users`. All 8 named files printed `PASS`. `git diff --stat <merge-base> HEAD -- supabase/tests` is empty, so they are unedited. |
| 15 | [REDUNDER-SUBSET-SPLIT] is closed by its owner with the mechanism named, and the release carries one CHANGELOG entry with VERSION == package.json | ✓ VERIFIED | The TODOS entry is `[x]`, `CLOSED 2026-09-24 by its owner`, names the mechanism, and says timeout was NOT raised. Seven `[164.4.2-*]` follow-ons are booked. `VERSION` is `0.87.0.0` and `package.json` is `"0.87.0.0"`. CHANGELOG `## [0.87.0.0] - 2026-09-24`. `critical-regressions.test.ts` passed. |
| 16 | Area E: the run's critical path falls, and the win does not merely relocate or redistribute (clauses a-d) | ? UNCERTAIN (post-merge) | The protocol is fully specified in MEASUREMENT.md `## AFTER`, and every value cell is empty on purpose. Only merge-push runs are admissible, so this cannot be measured before merge. Human item 1. |
| 17 | Goal-level: `sql-mutation` stops being a job that can only get slower, split BEFORE the ceiling is reached | ? UNCERTAIN (decision) | Only gate-only PRs narrow. Every push, every PR changing `supabase/migrations/`, the runner, pg-lane, `ci.yml` or the subset scripts, and every PR changing no gate file run the FULL corpus, which still grows under the same 20-minute ceiling. Nothing booked covers that path nearing 20. Human item 3. |

**Score:** 15/17 truths verified (0 present-but-behavior-unverified; 2 uncertain, routed to human)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/mutation-runner/run.mjs` | `--subset-from` mode, `scope:` line, floor semantics | ✓ VERIFIED | Real SUBSET run exit 0. `subsetFlagConflict` / `readSubsetList` refusals are covered by the runner's subset self-test rows. |
| `scripts/sql-gate-subset.mjs` | PR gate-file derivation, strict `GATE_FILE_RE`, FULL fallbacks | ✓ VERIFIED | `--self-test` 9/9 rows, 22/22 assertions. Imported by `run.mjs` (`GATE_FILE_RE`) and run by `changed-paths` (`id: sql_gate_subset`). |
| `scripts/classify-changed-paths.mjs` | one merge-base diff (`changedFilesAgainstBase`), throws on unreadable base | ✓ VERIFIED | `--self-test` 13/13. Imported by `sql-gate-subset.mjs`. |
| `scripts/check-baseline-currency.mjs` | single currency implementation plus `--replay-set` | ✓ VERIFIED | Self-test 49/49. Called by run.sh and the restore script. |
| `supabase/schema/baseline-carried-migrations.txt` | marker bound to the dump's sha256 | ✓ VERIFIED | `marker-sha=match`, 273 carried, equal to the 273 files in `supabase/migrations/`. |
| `scripts/local-stack/run.sh` | load order reset → dump → ACL fidelity → refdata → nonpublic → fidelity → replay | ✓ VERIFIED | Order read in `load_baseline()` and observed in the boot log. |
| `scripts/local-stack/nonpublic-objects.mjs` + `nonpublic-catalogue.sql` | D-G extraction and `--check` | ✓ VERIFIED | Behaviour observed on the lane. Unit arms in `local-stack-lane-wiring.test.ts` passed. |
| `scripts/local-stack/capability-probe.mjs` | demand vs supply probe | ✓ VERIFIED | Wired in `sql-tests`. Also supplies the loopback-DSN refusal used by `load_baseline()`. |
| `scripts/local-stack/sql-corpus-report.mjs` | diagnostic census | ✓ VERIFIED | Ran: 75 pass + 1 LANE-ONLY of 76. |
| `.github/workflows/ci.yml` | changed-paths outputs, sql-mutation branches and assert arms, sql-tests on lane, test-db-drift, aggregator | ✓ VERIFIED | See truths 1, 2, 5, 9, 10. |
| `docs/runbooks/shared-test-db-mutex.md`, `CLAUDE.md` | dated corrections, lineage kept | ✓ VERIFIED | CLAUDE.md carries the `CORRECTED 2026-09-23 (Phase 164.4.2)` notes and the D-F note. |
| `164.4.2-MEASUREMENT.md` | BEFORE over ≥5 runs, refutation fixed in advance, AFTER protocol | ✓ VERIFIED (AFTER values pending) | 6 BEFORE runs with ancestry proof. AFTER protocol present, values empty by design. |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `changed-paths` | `sql-mutation` | `outputs.sql_gate_mode/sql_gate_files` → mutate-step `env:` | WIRED |
| `sql-gate-subset.mjs` | `classify-changed-paths.mjs` | `import { changedFilesAgainstBase }` | WIRED |
| `run.mjs` | `sql-gate-subset.mjs` | `import { GATE_FILE_RE }` | WIRED |
| `load_baseline()` | `check-baseline-currency.mjs --replay-set` | `check_baseline_currency`, before psql `-f` | WIRED |
| CI currency steps (3 jobs) | the same function | `bash scripts/local-stack/run.sh --check-currency` | WIRED |
| `load_reference_data` | the gate's filtered allowlist | `--allowlist "$REFDATA_ALLOWLIST_FILTERED" --migrations "$MIGRATIONS_DIR"` | WIRED |
| `refuse_stale_baseline()` | `check-baseline-currency.mjs` default mode | `node "$CURRENCY_CHECK"` | WIRED |
| `frontend` aggregator | `sql-tests`, `test-db-drift`, `sql-mutation` | `needs:` plus the result loop | WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Subset derivation verdicts | `node scripts/sql-gate-subset.mjs --self-test` | `SELF-TEST PASSED: 9/9 rows, 22/22 assertions` | ✓ PASS |
| Shared diff | `node scripts/classify-changed-paths.mjs --self-test` | `PASSED: 13/13` | ✓ PASS |
| Currency gate | `node scripts/check-baseline-currency.mjs --self-test` | `PASSED: 49/49 … every defect kind fired` | ✓ PASS |
| Currency at HEAD | `--replay-set` with explicit paths | `carried=273 replay=0 marker-sha=match defects=0` | ✓ PASS |
| Real SUBSET run (pg-lane) | `run.mjs --subset-from <1 gate>` | exit 0, `scope: SUBSET 1/49 …`, 7/7 biting | ✓ PASS |
| Real CI assert step on that log | extracted `run:` body, `pull_request` / `push` / scope removed | exit 0 / exit 1 MEASURE_FAIL / exit 1 MEASURE_FAIL | ✓ PASS |
| D-F + D-G lane boot, K=1 | `LANE_MIGRATIONS_DIR=<scratch copy + probe> run.sh up` | exit 0, replay 1, fidelity OK, ledger 274 = 273 + 1 | ✓ PASS |
| SQL corpus on the lane | `node scripts/local-stack/sql-corpus-report.mjs` | `files=76 pass=75 fail=0 whole-file-skip=0 lane-only=1` | ✓ PASS |
| Teardown | `run.sh down`, `--assert-teardown` | `surviving quantalyze containers -> 0` | ✓ PASS |
| Named vitest files | floors, lane-wiring, ci-anti-skip-gate, critical-regressions | 4 files, 354/354 | ✓ PASS |
| Named vitest files | restore-test-from-baseline, ghcr-login contract, docs-path-filter contract | 3 files, 82/82 | ✓ PASS |

All DB runs were local only, serialised under the host lock (`gsd-164.4.2-dbrun.lock`, created and then removed), one step per command. No remote database was touched.

### CI evidence (SHA-bound, counted)

| Run | Head | Relevant jobs | Overall |
|---|---|---|---|
| `35922576855` | `ee965381` | sql-tests, test-db-drift, sql-mutation, frontend-local-stack, frontend-live-db-lane, frontend: all success | failure, from `version-gate` only (VERSION not yet bumped at that point in the branch) |
| `35926142486` | `0ca0dc6c` | same set, all success; `scope: FULL 49/49` | failure, from `version-gate` only |
| release `beacb716`, review-fix `c29cf651` | ancestors of HEAD | CI, Contracts, Migration Drift Check, SQL Function Snapshot: all success | success |
| **`b91bfeb5` (HEAD)** | | 4 runs counted. Contracts, Migration Drift Check and SQL Function Snapshot succeeded. **CI run `35936373830` was `queued` at verification time.** | **PENDING** |

⚠️ HEAD carries round-2 review fixes to `check-baseline-currency.mjs`, `classify-changed-paths.mjs`, `capability-probe.mjs` and `nonpublic-objects.mjs` made after the last green CI (`c29cf651`). Local evidence covers them: the self-tests, the named vitest files and the real lane boot above all ran at HEAD. The CI reading for HEAD is still pending.

### Probe Execution

Step 7c does not apply as written: the phase declares no `scripts/*/tests/probe-*.sh`. The phase's own probes (`capability-probe.mjs`, the currency gate, nonpublic fidelity, the corpus census) were run directly and are recorded above.

### Requirements Coverage

`.planning/REQUIREMENTS.md` maps no requirement ID to Phase 164.4.2, and the ROADMAP says `Requirements: TBD — no v1.20 requirement IDs`. The PLAN `requirements:` IDs are CONTEXT area labels plus the TODOS obligation. Each is accounted for below. There are no ORPHANED REQUIREMENTS.md rows.

| ID | Source plans | Status | Evidence |
|---|---|---|---|
| AREA-A (currency guard first) | 02, 03, 04, 06 | ✓ SATISFIED | Truth 12 |
| AREA-B (sql-tests tracer) | 04, 05, 08 | ✓ SATISFIED | Truths 9, 10, 11 |
| AREA-C (fallback only on measured refutation) | 04 | ✓ SATISFIED (not taken) | The probe did not refute A, and the corpus runs 75+1/76 on the lane. The fallback was correctly not taken. |
| AREA-D (subset rule + vacuity fence) | 07, 09 | ✓ SATISFIED | Truths 1-5 |
| AREA-E (falsification) | 01, 10 | ? NEEDS HUMAN (post-merge) | BEFORE done, AFTER pending (truth 16) |
| AREA-F (replay newer migrations) | 06, 08, 10 | ✓ SATISFIED | Truth 13 |
| AREA-G (non-public objects) | 11 | ✓ SATISFIED | Truth 14 |
| REDUNDER-SUBSET-SPLIT (TODOS) | 07, 09, 10 | ✓ SATISFIED (mechanism) | Truth 15. The goal-level reach is truth 17. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (all non-planning added lines vs merge base) | — | TBD / FIXME / XXX | none found | — |
| (same) | — | TODO / HACK / PLACEHOLDER | none found | — |

Both code-review rounds reported `status: all_fixed` (16/16 and 12/12, 0 skipped).

Known limits already booked by the phase, all non-blocking and routed in TODOS: `[164.4.2-REMAINING-KEY-HOLDERS]`, `[164.4.2-RESTORE-CURRENCY-BY-EPOCH]`, `[164.4.2-LANE-REPLAY-REFUSES-ON-EMPTY-DB]`, `[164.4.2-WIZARD-ARMS-SEED-AS-AUTHENTICATED]`, `[164.4.2-PROD-SUPAUTILS-FUNCTION-DENIAL-CRASH]`, `[164.4.2-DERIVE-KEY-DAILIES-TEST-VS-ROADMAP]` (my boot confirms the lane registers `derive-allocator-key-dailies`), `[164.4.2-LANE-ONLY-REASON-STALE]`.

### Human Verification Required

#### 1. Area E AFTER measurement (post-merge)

**Test:** Fill `164.4.2-MEASUREMENT.md` `## AFTER` from the first concluded admissible merge-push run, using its own recipe (including the `test-db-drift` `select` addition), and apply clauses (a)-(d).
**Expected:** On a non-degenerate run: critical path < 16m18s, `sql-tests` job-total < 8m31s, and a three-holder combined wait < 12m05s. A clause that fails is recorded as a failure.
**Why human:** Only push runs on `main` after the merge are admissible, and MEASUREMENT.md bars `passed` until this is filled. Owner: orchestrator/founder, post-merge.

#### 2. SUBSET observed in CI (post-merge)

**Test:** On the first gate-only PR after merge, read `sql-mutation`'s `scope:` line and assert verdict at that PR's head SHA, with the runs counted.
**Expected:** `scope: SUBSET k/N annotated files: <names>`, then the assert step's SUBSET pass line, then green.
**Why human:** This branch changes the runner and `ci.yml`, so its own CI correctly runs FULL. It was verified locally with a real pg-lane run and the real assert-step body. Owner: whoever lands the next gate-only PR.

#### 3. Goal-level reach of the split (decision)

**Test:** Decide whether "only gate-only PRs narrow" meets "`sql-mutation` stops being a job that can only get slower".
**Expected:** Either an explicit acceptance, or a routed follow-on for the FULL run (every push, every migration/machinery PR and every non-gate PR) approaching the 20-minute ceiling.
**Why human:** Locked DECISION D (empty change set → FULL) and the ROADMAP's shape (1) (`0 files in scope` verdict with a separate full run) differ. The code faithfully implements D. That leaves the job's dominant paths with unchanged headroom, and no booked item owns that path.

### Gaps Summary

No blocking gaps. The mechanism the phase promised exists, is wired, and was exercised independently at HEAD:
- the PR subset mode and its derivation;
- the vacuity fence in both the runner and the CI assert step;
- floors that do not move;
- `sql-tests` on a private lane with no key;
- VAC-08 rehomed with its mutex;
- the D-F replay;
- the D-G non-public objects, with the whole SQL corpus passing on the lane.

Three items remain. Two are post-merge measurements the phase cannot produce on its own branch (the Area E AFTER and the first CI-observed SUBSET run). The third is a founder decision on whether the delivered subset reach satisfies the goal's "stops getting slower" intent, given that most `sql-mutation` runs will still be FULL. The CI run for HEAD `b91bfeb5` was queued at verification time and is pending.

---

_Verified: 2026-09-24_
_Verifier: Claude (gsd-verifier)_
