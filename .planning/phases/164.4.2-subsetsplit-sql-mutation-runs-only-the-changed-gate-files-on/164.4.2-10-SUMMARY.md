---
phase: 164.4.2-subsetsplit
plan: "10"
subsystem: infra
status: complete
tags: [release, changelog, measurement, falsification, backlog, ci-gates]

requires:
  - phase: 164.4.2-subsetsplit (plan 09)
    provides: "sql-mutation narrows on pull requests; the SHA-bound FULL scope reading at 0ca0dc6c"
  - phase: 164.4.2-subsetsplit (plans 01-08, 11)
    provides: "the BEFORE measurement, the lane, the replay, D-G, and every checkpoint fix the CHANGELOG records"
provides:
  - "164.4.2-MEASUREMENT.md `## AFTER`: the protocol with every value cell empty, the corrected clauses (a)-(d) applied by reference, and a WIN line"
  - "[REDUNDER-SUBSET-SPLIT] closed in place, naming its mechanism; seven follow-ons booked with owner, trigger and date"
  - "CLAUDE.md: dated notes for the removed Production reviewer and the lane's Postgres pin (additions only)"
  - "v0.87.0.0: VERSION, package.json and one unified CHANGELOG entry in one release commit"
affects: [phase verifier, /gsd-ship, the post-merge AFTER measurement]

actuals:
  tokens: 10500     # chars/4 over the realized diff (42009 added chars)
  tasks: 3
  commits: 4        # MEASURED: git rev-list --count a12df3fe..HEAD before this SUMMARY's commit
plan_head_before: a12df3fe735b6b29bdc68ad8f7a05f01507455b9

tech-stack:
  added: []
  patterns:
    - "an AFTER protocol written before its runs exist: admissible runs, verbatim commands, an empty table, and the verdict rule by reference"
    - "the CHANGELOG commit-to-bullet cross-check recorded as a table, every non-merge hash present exactly once"

key-files:
  created: []
  modified:
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md
    - TODOS.md
    - CLAUDE.md
    - VERSION
    - package.json
    - CHANGELOG.md

key-decisions:
  - "Version 0.87.0.0, a minor bump. The phase changes which database a blocking CI job writes, splits a gate into two jobs, changes when a gate measures the whole corpus, and changes what the lane builds. That is behaviour, the same class as TESTISOLATION's 0.85.0.0, not a patch like MARKERGATE's 0.80.1.0."
  - "The AFTER protocol pastes the Recipe's three commands verbatim, then adds ONE dated alternative to Step 2's select so test-db-drift is captured. Without it, clause (c) could not be measured."
  - "A pull-request run is NOT admissible as AFTER evidence. That includes plan 08's sql-tests 2m31s reading. The protocol says so by name, so it cannot be quoted generously."
  - "Clause (b) of the restore-path entry was MEASURED before being booked. A depth-1 clone gave baseline=migrations=1790202342, defects=0, exit 0."
  - "Seven follow-ons were booked, not four. The plan's four came with two items the orchestrator routed (the wizard arms, the PROD supautils crash) and one plan 11 surfaced (derive-allocator-key-dailies)."

requirements-completed: [AREA-E, AREA-F, REDUNDER-SUBSET-SPLIT]

coverage:
  - id: D1
    description: "MEASUREMENT.md carries an AFTER section with four jobs, the Recipe cited by heading, the corrected clauses (a)-(d) by reference, a WIN line and an empty table"
    requirement: AREA-E
    verification:
      - kind: other
        ref: "plan 10 Task 1 <automated> node check (plus a check that the struck condition is not restated)"
        status: pass
    human_judgment: false
  - id: D2
    description: "The AFTER numbers themselves, from the first concluded merge-push run at the new head, with clauses (a)-(d) applied"
    requirement: AREA-E
    verification: []
    human_judgment: true
    rationale: "Post-merge by construction: no admissible run exists until the phase merges. This is Task 1's <human-check>."
  - id: D3
    description: "[REDUNDER-SUBSET-SPLIT] is closed in place, naming --subset-from and saying timeout-minutes was not raised; seven follow-ons are booked"
    requirement: REDUNDER-SUBSET-SPLIT
    verification:
      - kind: other
        ref: "plan 10 Task 2 <automated> node check"
        status: pass
    human_judgment: false
  - id: D4
    description: "VERSION and package.json are byte-equal at 0.87.0.0 with no trailing newline, and CHANGELOG carries the entry"
    requirement: AREA-F
    verification:
      - kind: other
        ref: "plan 10 Task 3 <automated> node check -> release pair intact: 0.87.0.0"
        status: pass
      - kind: unit
        ref: "npx vitest run src/__tests__/critical-regressions.test.ts (166/166)"
        status: pass
      - kind: other
        ref: "node scripts/check-version-bump.mjs -> No defects"
        status: pass
    human_judgment: false

duration: 12min
completed: 2026-09-24
---

# Phase 164.4.2 Plan 10: the AFTER protocol, the closed backlog item and the v0.87.0.0 release Summary

**Plan 10 wrote the AFTER measurement completely except for its numbers, applying only the corrected refutation clauses. It closed `[REDUNDER-SUBSET-SPLIT]` in place, named by the mechanism that closed it, and booked seven follow-ons with an owner, a trigger and a date. It shipped v0.87.0.0, whose one CHANGELOG entry maps every one of the branch's 89 non-merge commits to a bullet.**

## Performance

- **Duration:** about 12 min
- **Started:** 2026-09-23T22:23:31Z
- **Completed:** 2026-09-23T22:35:18Z (UTC; 2026-09-24 local)
- **Tasks:** 3 of 3
- **Files modified:** 6

## Task Commits

1. **Task 1 (tracer): the AFTER protocol** — `2e86917cd` (docs). Tracer gate in auto mode: the `<verify>` was re-run after a wording fix and passed. ⚡ Verified, then expanded.
2. **Task 2: close `[REDUNDER-SUBSET-SPLIT]` and book the follow-ons** — `15ba01186` (docs)
3. **Deviation (orchestrator instruction): CLAUDE.md additions** — `4e34fcaa7` (docs)
4. **Task 3: release** — `99a0df402` `chore(release): v0.87.0.0 — SUBSETSPLIT`

## Task 1: the AFTER section

The section sits in `164.4.2-MEASUREMENT.md` as `## AFTER — to be filled from merge-push runs at the new head`. It contains:
- **Admissible runs.** Only concluded `push` runs of `ci.yml` on `main` whose head descends from the phase merge. Ancestry is proven with the inverse of the BEFORE proof (`AFTER-CONFIRMED`). A PR or dispatch run is not admissible, and plan 08's PR-run `sql-tests` reading is named as inadmissible. One run fills the table and gives a PROVISIONAL verdict on (a) and (d). Five runs make it final. Degenerate runs are recorded in full and never dropped.
- **Commands.** The three `## Recipe` steps are pasted verbatim, plus ONE dated extension: Step 2's `select` gains `or .name=="test-db-drift"` and nothing else.
- **The four jobs, one table row each.** `sql-tests`' acquire-wait is n/a BY CONSTRUCTION and marked as no evidence. Its boot, which now carries the D-F replay, is recorded beside the replay count K. `sql-tests`' useful-work step has its renamed name, and `test-db-drift`'s useful work is the VAC-08 step.
- **An empty table.** Four job rows, a combined-wait row and a run-level critical-path row, with every value cell blank.
- **The verdict rule.** Clauses (a)-(d), one line each, against the BEFORE bands: critical path below 16m18s; `sql-tests` job-total below 8m31s–9m23s; the three-holder combined wait below 12m05s–14m56s; and (a) alone confirms nothing. The struck original condition is not restated.
- **The WIN line.** A win is (a)+(b)+(c) together, not the ROADMAP's 11–12 min. Half A does not act on merge pushes, so this protocol measures Half B alone.
- **A dated note** that the three "plan 08" sentences now mean plan 10. The sentences themselves are kept.

## Task 2: the four follow-on headlines the plan required, plus three

The plan's four, quoted by headline:
1. `[164.4.2-REMAINING-KEY-HOLDERS]` "`python`, `e2e-seeded` and now `test-db-drift` still hold the shared-TEST advisory key `61616158`; whether they move off it is decided by the AFTER numbers, not assumed"
2. `[164.4.2-LANE-ONLY-REASON-STALE]` "The one `-- LANE-ONLY:` marker in the gate corpus argues from shared TEST, but the job that reads it no longer runs there"
3. `[164.4.2-RESTORE-CURRENCY-BY-EPOCH]` "The shared-TEST restore path still judges baseline CURRENCY by commit dates, which DECISION F forbids for the lane, and on a depth-1 clone that judgement is vacuous"
4. `[164.4.2-LANE-REPLAY-REFUSES-ON-EMPTY-DB]` "D-F's known consequence: a migration newer than the dump that refuses on an empty or unidentified database reddens EVERY lane-booting job until the dump is regenerated after its PRODUCTION apply"

The three added ones: `[164.4.2-WIZARD-ARMS-SEED-AS-AUTHENTICATED]`, `[164.4.2-PROD-SUPAUTILS-FUNCTION-DENIAL-CRASH]` and `[164.4.2-DERIVE-KEY-DAILIES-TEST-VS-ROADMAP]`. All seven are owned by THE FOUNDER, each with a trigger and the date 2026-09-24.

**Clause (b) was measured before it was booked, as the plan required.** A `git clone --depth 1` of this repository went into a `mktemp -d` under the session scratchpad. `git rev-parse --is-shallow-repository` printed `true`, `git rev-list --count HEAD` printed `1`, and the default mode printed:
```
baseline-currency: baseline=1790202342 migrations=1790202342 defects=0
✅ No defects — supabase/schema/baseline.sql is at least as fresh as supabase/migrations
```
It exited 0. The full-history worktree printed `baseline=1790144053 migrations=1790138229 defects=0`. The clause reproduced, so it was kept. Clause (a) was read from the source (`judge`, `readEpoch`, the default `FRESHNESS_TS_CMD` at `const freshnessCmd`).

**Stated in the closure, read by symbol against `main`:** `sql-mutation`'s `timeout-minutes` is 20 on both. `FILES_FLOOR = 49`, `ARMS_FLOOR = 426` and `WAIVED_CEILING = 0` are identical on both.

## Task 3: the release

### Version decision

`0.86.0.1` became **`0.87.0.0`**. `VERSION` holds no trailing newline and is byte-equal to `package.json`'s version. `npm version` was not run, and the lockfile was not touched: its root version has stayed at 0.70.0.0 through every release, which is the repo's convention. `node scripts/check-version-bump.mjs` printed `base=0.86.0.1 head=0.87.0.0 package=0.87.0.0 changed=60 ✅ No defects`.

### CHANGELOG mechanism, step by step

1. `git log origin/main..HEAD --oneline` gave **109 commits: 88 non-merge and 21 merges**, before the release commit. With the release there are **89 non-merge**. The 21 merges are worktree merges and `origin/main` merges. They add no change of their own; the `origin/main` content is main's own released history.
2. `git diff origin/main...HEAD` covers 33 non-planning files, +7612/−723. Read by file, and for `ci.yml` by structural change: new and removed jobs, steps, `needs:`, `if:`, `timeout-minutes:` and `permissions:` lines. Found in the diff and absent from every SUMMARY: `sql-tests`' `timeout-minutes` went from 90 to 20 in `74e041ec9`. It is in the entry.
3. Grouped by theme before writing: Added, Changed, Fixed, Security, Root cause, Tests, Notes.
4. There is one entry, `## [0.87.0.0] - 2026-09-24`. No earlier per-commit entry for this branch existed.
5. Cross-check: the table below.
6. Nothing was asked of the founder.

A gitleaks scan (`gitleaks dir`, repo config) over the extracted entry printed `no leaks found`.

### Commit → bullet mapping (89 non-merge commits, none unmapped)

Bullet keys: **Added** A-currency, A-DF, A-DG, A-ACL, A-probe, A-subset, A-drift-job, A-ghcr, A-pin, A-report · **Changed** C-lane, C-narrow, C-assert, C-steps, C-dumps, C-ledger, C-pins, C-docs · **Fixed** F-stale, F-ledger-rows, F-35of76 · **Security** S-secret, S-login · **Root cause** RC · **Tests** T · **Notes** N-AFTER, N-limits, N-REDUNDER, N-planning.

| Commits | Bullet(s) |
|---|---|
| `d3964a4b8` `cff6fb8cb` | A-currency |
| `2f5256f40` `76cff2f27` | C-dumps |
| `a9bcd876d` `1e5680337` | F-stale, C-steps (the history pin it introduced, later re-subjected) |
| `a6330b1b9` `e4b5e77a0` `f2b670a1f` | A-probe, T |
| `74e041ec9` `099b71fb2` `a8efac648` | C-lane, A-drift-job, S-secret |
| `77e205870` `3daf98007` `e0ce6b1f9` `17ad661b2` | C-pins |
| `2abdc92c2` `fdbfa5e7e` `f892ba7c8` `f2bb3e548` | A-DF, F-ledger-rows, T |
| `6d066a94b` `f3a2a0e09` `65d86e3ad` | A-subset, T |
| `cf05ec10e` | C-steps |
| `4cc58dca7` | C-docs |
| `9dcaff12b` | A-ACL, F-35of76 |
| `6273c940a` | A-report |
| `326a51109` `6e0e12d78` `ba287553f` `09f99384b` | A-DG, F-35of76, T |
| `1186e7c4a` `2b87492e3` | A-ghcr, S-login, T |
| `870b59632` `31d9c1fdf` | C-ledger, T |
| `5ecdea2c1` | A-pin, RC |
| `40286fdb1` | C-narrow |
| `bc3683758` | C-assert, T |
| `2e86917cd` | N-AFTER |
| `15ba01186` | N-REDUNDER, N-limits |
| `4e34fcaa7` | C-docs |
| `99a0df402` | the release commit itself |
| `09efc4b3d` `dc700bb23` `9c7ad67c3` `95728c897` `03dd4a395` `10261b093` `605a76ea9` `2744a0220` `4668ad11f` `dbe4e0a7a` `22a956007` `896d7c889` `bf8b8a98e` `d9f46ec21` `df838a13a` `4e0f07c0c` `87aaca364` `891327dae` `38c67389a` `3a31c72cc` `6e9e1fba0` `dbb01b7ce` `252ab448f` `2894dd862` `861b9df40` `5e1a78cd4` `e5ce0d419` `15c147543` `b949a9988` `0c9462ec2` `d570a5c04` `25135a99f` `26271e395` `a866fcd8b` `5a7e6c14e` `ae7b9f06a` `9a0ef71cc` `47ca31639` `dc224a936` `bd7fcb859` `7c33c51c4` `00a5c643d` `d57dd11db` `ee9653818` `2d75620cb` `afaea4849` `9bab364e4` | N-planning (CONTEXT A-G, the corrected Area E condition `9c7ad67c3`, plans and replans with their revisions, the BEFORE capture, the SUMMARYs and SHA-bound readings, state notes) |

## Verification run

- Task 1 node check: `AFTER protocol laid out for four jobs against the corrected refutation`. A second check confirmed the struck phrase does not appear in the AFTER section.
- Task 2 node check: `[REDUNDER-SUBSET-SPLIT] closed in place`. The closing text contains `--subset-from` and `timeout-minutes`.
- Task 3 node check: `release pair intact: 0.87.0.0`.
- `npx vitest run src/__tests__/critical-regressions.test.ts`: `Tests 166 passed (166)`.
- The 14 test files that read `CLAUDE.md` (excluding `gdpr-export-coverage-hook`, which needs the worktree's missing `node_modules/.bin/tsx`): `Test Files 14 passed (14)`, `Tests 785 passed (785)`.
- `node scripts/verify-plan-anchors.mjs --pending`: `OK: 4 plan file(s), no stale claims.`
- `npm run check:planning-hygiene` after each `git add`: OK, four times. The skip-token sweep over all four commit messages returned 0 each time.

## Deviations from Plan

**1. [Orchestrator instruction] `CLAUDE.md` edited, which is outside `files_modified`.** It got two dated notes and nothing else. `git diff --numstat` read `19 0`, additions only. The first is a `⛔ CORRECTED 2026-09-23 (Phase 164.4.2)` note after the `apply-test` bullet: the founder removed the `Production` environment's required reviewer, so PROD migrations auto-apply once `apply-test` succeeds. The original sentence is kept as lineage. The second is a note that the lane pins `17.6.1.113`, and why. Committed separately (`4e34fcaa7`). The CHANGELOG's C-docs bullet carries it.

**2. [Rule 2] Seven follow-ons booked, not four.** Beyond the plan's four: the wizard arms and the PROD supautils crash (routed by the orchestrator), and the `derive-allocator-key-dailies` test-vs-ROADMAP contradiction (surfaced by plan 11 and otherwise unowned). An item surfaced and then left unbooked disappears. The ROADMAP sentence the last entry cites was checked at HEAD.

**3. [Plan-internal] The Recipe is pasted verbatim, and then extended once.** Step 2's `select` predates `test-db-drift`, so a verbatim-only paste could not measure clause (c). The extension is dated, names its reason and changes one alternative.

**4. [Wording] A first draft of the AFTER verdict rule quoted the struck condition in order to reject it.** The plan says the struck condition may appear only as the lineage MEASUREMENT.md already carries. The draft was reworded before the commit, and the tracer verify was re-run with an extra check that the phrase is absent from the AFTER section.

**Total:** 4 (1 instructed, 1 Rule 2, 2 text). Nothing lowers a floor, raises a ceiling, relaxes a timeout, adds a waiver or widens an allowlist.

## Known Stubs

- **The AFTER table's value cells are empty on purpose.** Filling them is post-merge by construction and is Task 1's `<human-check>`. The phase verification may not pass until it is filled. This was not appended to `.planning/WINDOWS.md` from this worktree: that is a shared ledger, and the orchestrator owns post-wave writes (the same practice as plans 03 and 04).

## Threat Flags

None. No code surface was added. T-164.4.2-28 is mitigated by the mapping table, T-164.4.2-29 by the `<human-check>` and the obligation block in the AFTER section, T-164.4.2-30 by the zero skip-token sweeps, and T-164.4.2-37 by the verdict rule applying only the corrected clauses.

## Next Phase Readiness

- The branch carries the release commit, so `version-gate` on the PR should go green. That is expected, not yet observed. The orchestrator's next push is the reading.
- After the merge, the founder or the verifier fills the AFTER table from the first concluded merge-push run, then applies clauses (a)-(d).
- STATE.md and ROADMAP.md were not touched. The orchestrator owns them.

## Self-Check: PASSED

- FOUND: `164.4.2-MEASUREMENT.md`, `TODOS.md`, `CLAUDE.md`, `VERSION`, `package.json`, `CHANGELOG.md`
- FOUND commits: `2e86917cd`, `15ba01186`, `4e34fcaa7`, `99a0df402`. `git rev-list --count a12df3fe..HEAD` = 4 before this SUMMARY's commit.
