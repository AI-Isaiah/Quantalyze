---
phase: 164.4.2-subsetsplit
plan: "11"
type: execute
wave: 6
depends_on: ["164.4.2-06", "164.4.2-07"]
files_modified:
  - scripts/local-stack/nonpublic-objects.mjs
  - scripts/local-stack/nonpublic-catalogue.sql
  - scripts/local-stack/sql-corpus-report.mjs
  - scripts/local-stack/run.sh
  - src/__tests__/local-stack-lane-wiring.test.ts
autonomous: true
requirements: [AREA-G]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-G, after the dump and its reference data load and BEFORE the D-F replay, the lane carries the `auth.users` trigger `on_auth_user_created` (AFTER INSERT, FOR EACH ROW, executing `public.handle_new_user()`). The statement is extracted from the migration that declares it, never hand-written."
    - "Per D-G, the lane's `cron.job` holds exactly the jobs the carried migrations fold to: every `cron.schedule` in filename order, superseded by a later schedule of the same name and removed by `cron.unschedule`. Each job carries the command bytes of its last declaring call and is registered as the loading role, never the superuser."
    - "Per D-G, a difference between the lane's `auth.users` triggers or `cron.job` rows and the declared set FAILS THE BOOT (exit 1, stack torn down). A shape the extractor cannot prove safe, or a catalogue it cannot trust, is MEASURE_FAIL (exit 2). Neither is ever a warning, and both are observed RED under a neuter."
    - "A migration the D-F replay applies registers its own cron job or trigger natively, exactly once. The extraction never runs a replayed file's statements, so nothing is registered twice."
    - "Per D-G, the 8 files that failed in plan 08's checkpoint pass on the lane UNEDITED: test_csv_finalize_atomic_fold, test_handle_new_user_role_allowlist, test_profiles_privileged_columns_locked, test_derive_allocator_keys_fanout, test_reconcile_dropped_enqueue_sweep, test_retention_crons_safe, test_retention_orphaned_running, test_strategy_analytics_stuck_computing_reaper."
    - "The whole SQL corpus, run on the lane the way CI's `sql-tests` step runs it but continuing past failures, reports every file and reads 75 pass + 1 LANE-ONLY of 76, or fails loud naming each failing file."
  artifacts:
    - scripts/local-stack/nonpublic-objects.mjs
    - scripts/local-stack/nonpublic-catalogue.sql
    - scripts/local-stack/sql-corpus-report.mjs
    - scripts/local-stack/run.sh
    - src/__tests__/local-stack-lane-wiring.test.ts
  key_links:
    - "`load_baseline()` order: `reset_public_default_acls` then the dump then `check_acl_fidelity` then `load_reference_data` then `load_nonpublic_objects` then `check_nonpublic_fidelity` then `replay_migrations`."
    - "The extraction folds ONLY the files in the currency gate's `CARRIED_SET_FILE`, read from the same `MIGRATIONS_DIR` the replay applies from. This is the handover `load_reference_data` already uses for its filtered allowlist."
    - "`--check` RE-DERIVES the expected set from the migrations. It never reads the SQL `--emit` wrote, so a partly-applied or altered emit file cannot vouch for itself."
    - "The catalogue is read through the stack SUPERUSER DSN, and its meta row must say so. pg_cron's `cron.job` row-level security shows a non-superuser only its own jobs, so a read as `postgres` could not see a job registered under another role."
---

<objective>
Implement DECISION G (D-G): after `baseline.sql` loads, and before the D-F replay of migrations
newer than the dump, the local-stack lane applies the PROD objects that live OUTSIDE `public`
which a schema-only dump of `public` cannot carry. That means the `auth.users` trigger and every
`pg_cron` job registration, both EXTRACTED from `supabase/migrations`. A drift gate then proves the
lane holds exactly the declared set.

Purpose: plan 08's checkpoint fixed the lane's ACLs and measured the remaining gap: 8 of 76 SQL
files fail on the lane. The files are correct and the lane is incomplete. Plan 04's probe measured
that the lane HOSTS `pg_cron` and the auth functions. It did not measure whether the lane carries
the OBJECTS PROD registers there. This plan closes that gap without widening the dump (D-G
rejected alternative 1) and without moving `sql-tests` back to shared TEST (D-G rejected
alternative 2).

Output: an extractor and drift gate in one module (`nonpublic-objects.mjs`) with its catalogue SQL;
the lane wiring in `run.sh`; pins and behaviour arms in the lane-wiring test; and a committed
diagnostic that runs the SQL corpus on the lane and reports every file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-08-SUMMARY.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-06-SUMMARY.md
@scripts/local-stack/run.sh
@scripts/local-stack/acl-fidelity.mjs
@src/__tests__/local-stack-lane-wiring.test.ts

<!-- verify-symbol: scripts/local-stack/run.sh load_baseline -->
<!-- verify-symbol: scripts/local-stack/run.sh reset_public_default_acls -->
<!-- verify-symbol: scripts/local-stack/run.sh check_acl_fidelity -->
<!-- verify-symbol: scripts/local-stack/run.sh load_reference_data -->
<!-- verify-symbol: scripts/local-stack/run.sh replay_migrations -->
<!-- verify-symbol: scripts/local-stack/run.sh CARRIED_SET_FILE -->
<!-- verify-symbol: scripts/local-stack/run.sh REPLAY_HANDOFF_DIR -->
<!-- verify-symbol: scripts/lint-sql-gates.mjs maskSql -->
<!-- verify-symbol: scripts/lint-sql-gates.mjs statements -->
<!-- verify-symbol: scripts/extract-reference-inserts.mjs dollarBodyRanges -->
<!-- verify-symbol: scripts/sql-body-normalize.mjs scanSql -->
<!-- verify-symbol: scripts/prod-prober/arms/cron-drift.mjs normalizeCommand -->
<!-- verify-symbol: supabase/migrations/20260405061912_rls_policies.sql on_auth_user_created -->

## Ordering (recorded here, and the reason plan 09 now depends on this plan)

- **Wave 6, alongside plan 08.** This plan depends on 06 (the replay seam and `CARRIED_SET_FILE`)
  and 07. Plan 08's Tasks 1 and 2 are done. Its Task 3 is an orchestrator CI read that edits no
  file. The shared `src/__tests__/local-stack-lane-wiring.test.ts` therefore has one live writer
  in this wave: this plan.
- ⛔ **Plan 08 Task 3's SHA-bound CI re-read happens AFTER plan 11 lands.** Read before this plan
  lands, `sql-tests` goes RED on the 8 files below. That RED is already measured, so re-reading it
  teaches nothing.
- Plan 09's `depends_on` now names `164.4.2-11`. Plans 09 and 10 stay behind this plan, per the
  D-G sequencing paragraph.
- ⚠️ **What only CI can show, and where it is read:** the superuser DSN derivation under CLI
  2.98.2, which plan 08 flagged as unobserved, is now used by two more steps here. Also, this plan
  boots and runs only the SQL corpus locally. `frontend-local-stack` and `frontend-live-db-lane`
  boot the same `load_baseline()`, and the trigger now fires on every GoTrue signup there. Their
  seed helpers upsert the profile row (`createTestUser` in `src/lib/test-helpers/live-db.ts` upserts
  `onConflict: "id"`, and its comment names the signup trigger), so no conflict is expected. That
  is a reading of code, NOT an observation, and plan 08 Task 3's counted, SHA-bound read is where
  both jobs are observed.

## The census, MEASURED at planning time (head `a866fcd8`). Re-measure; do not trust

**Auth trigger.** Every `supabase/migrations/*.sql` was scanned in code positions (`maskSql`) for
`CREATE [OR REPLACE] [CONSTRAINT] TRIGGER`, `DROP TRIGGER` and `ALTER TABLE … ENABLE|DISABLE
TRIGGER` naming an `auth.*` table.
- Exactly ONE hit: `CREATE TRIGGER on_auth_user_created AFTER INSERT ON auth.users FOR EACH ROW
  EXECUTE FUNCTION handle_new_user();`, a TOP-LEVEL statement in
  `20260405061912_rls_policies.sql`.
- No later migration drops, replaces or disables it. The function `public.handle_new_user()` is
  already in the dump, in its latest form.
- The only other non-public DDL in the corpus is 5 `CREATE POLICY … ON storage.*` statements
  across 2 files. These are OUTSIDE D-G's two named classes. None of the 8 failing files depends
  on them. They are NOT planned here and are listed under "Noted, not planned" below.

**pg_cron.** 24 migration files contain a `cron.schedule`/`cron.unschedule` token. That is the "24"
CONTEXT AREA G cites, and ⛔ it over-counts callers:
- 4 of the 24 mention it only in comments: `20260419061003_feedback_delta_enqueue`,
  `20260516160200_retention_crons_schema_drift_probe`, `20260918120000_prod_prober_cadence`
  (which says it deliberately does NOT call `cron.schedule`) and
  `20260919120000_strategy_sync_cursors`.
- **20 files hold 61 code call sites: 31 `cron.schedule` and 30 `cron.unschedule`.** There are 0
  `cron.alter_job`, 0 `cron.schedule_in_database`, and 0 INSERT/UPDATE/DELETE on `cron.*`.
- Every call sits inside a `DO` block. Every argument is a literal: `'…'` for the name and
  schedule, and a dollar-quoted body for the command.
- `unschedule('retention_audit_log')` names a job no migration ever schedules, so in the fold it
  is a no-op.

**No dynamic SQL.** Two independent lexers agree call-for-call:
- `scanSql`'s comment-stripped text (strings kept) and `maskSql`'s code positions (strings
  blanked) both find 61 cron call sites and 1 auth trigger.
- So no call hides inside a string literal that an `EXECUTE` could run. The gate keeps that
  agreement as a live MEASURE_FAIL check rather than trusting this count.

**The final folded set: 15 jobs.**
- `api_key_rotation_reminder`
- `audit_log_cold_purge`
- `audit_log_hot_to_cold`
- `compute_bridge_outcome_deltas`
- `derive-allocator-key-dailies`
- `match_engine_cron`
- `poll-allocator-positions`
- `reap_strategy_analytics_stuck_computing`
- `reconcile_dropped_enqueue_sweep`
- `refresh-allocator-equity`
- `resend_message_correlation_retention_90d`
- `retention_compute_jobs_done`
- `retention_compute_jobs_failed`
- `retention_compute_jobs_orphaned_running`
- `retention_notification_dispatches`

⚠️ **The declared set is deliberately NOT PROD's set.** D-G locks "the set the migrations declare".
Measured against the committed PROD oracle `scripts/prod-prober/cron-manifest.json` (captured
2026-09-18), using its own `normalizeCommand`:
- **13 jobs match** in both schedule and normalized command.
- **`match_engine_cron`**: the command differs. The migration's GUC-built command could never run
  on Supabase, and PROD's row was repointed by hand. `cron-drift.mjs`'s header records this.
- **`derive-allocator-key-dailies`**: absent on PROD. It was deliberately unscheduled by hand at
  the v1.11 recovery. The ROADMAP forbids re-registering it on PROD, and `TODOS.md` explains why.
  The lane is ephemeral and runs no worker, so registering it there creates none of that risk. The
  unedited `test_derive_allocator_keys_fanout.sql` requires it.
- **2 PROD-only jobs**, `ledger_refresh_fanout` and `prod_prober_cadence_check`, were registered
  out of band. No migration schedules them.

PROD's cron configuration stays the concern of the prod-prober's CRON-DRIFT-01 arm and its
manifest. This plan neither reads that manifest at boot nor changes it.

⚠️ **One authored defect the extractor must handle, measured.**
`20260515113637_resend_message_correlation.sql` nests a `$$`-quoted command inside a `DO $$` body.
PostgreSQL rejects the file as authored: `syntax error at or near "DELETE"`,
`scripts/local-stack/REPLAY-SPIKE.md` finding 4. The job nevertheless exists on PROD with exactly
the inner command, and the manifest row matches under `normalizeCommand`. The extractor parses
each call's arguments LOCALLY from the call's own opening parenthesis, so it recovers that
command. The same local parse gives the other 30 schedule sites byte-exact commands.

**The loading roles, and why.** Evidence is repo-only. The tracer boot is the first observation,
and its SUMMARY records it.
- **cron registration runs as the loading role `postgres`** (the lane's `db_url`):
  - `cron.schedule` stamps `cron.job.username = current_user`.
  - All 16 rows of the PROD manifest carry `username: "postgres"`.
  - The corpus reads `cron.job` as `postgres`, which plan 08 measured is NOT a superuser.
    pg_cron's row-level security on `cron.job` shows a non-superuser only its own jobs, so jobs
    registered as `supabase_admin` would be invisible to the very files they exist for.
  - Registering as the superuser would also run every job with superuser rights, a catalogue
    WIDER than PROD's. That is the class of plan 08's ACL defect.
  - ⚠️ If the boot shows `postgres` lacks EXECUTE on `cron.schedule`, the boot FATALs loudly; it
    does not silently switch roles. The documented alternative is to register through the
    superuser with `cron.schedule_in_database(…, username => 'postgres')`. The row it yields is
    identical, and the gate checks `username`. Take it ONLY on that measured refusal, and record
    the refusal in the SUMMARY.
- **The `auth.users` trigger is created through the stack SUPERUSER DSN**, derived exactly as
  `reset_public_default_acls()` derives it (the lane's loopback DSN with the role swapped; a DSN
  not starting with the `postgres` role is refused):
  - `auth.users` is owned by the auth service role, and the `postgres` role's TRIGGER privilege
    on it on this image is unmeasured.
  - A trigger has no owner, so the object is identical whoever creates it.
  - `handle_new_user()` is SECURITY DEFINER and owned per the dump, so runtime behaviour does not
    change.

**Why the trigger loads AFTER reference data, not in PROD's chronological order.**
- The reference-data allowlist replays the teaser sentinel `auth.users` row, then its sentinel
  `public.profiles` row (`ON CONFLICT (id) DO NOTHING`).
- With the trigger already in place, `handle_new_user()` would create the profile first, with
  different values, and the allowlisted row would silently no-op.
- Loading after reference data reproduces the TEST restore's state, where `auth.users` survives,
  the sentinel insert no-ops and the profile gets the migration's explicit values. That is the
  state the corpus was historically green against, and it is plan 08's current 67-file baseline.

**How double registration is avoided (D-F × D-G).**
1. The extraction folds ONLY the gate's carried set. A replayed file's cron and trigger statements
   run once, natively, inside `replay_migrations`, exactly as `db push` runs them on PROD. This
   mirrors how the filtered reference-data allowlist keeps a replayed migration's INSERTs out of
   `load_reference_data`.
2. A replayed reschedule supersedes by name rather than appending. pg_cron's `cron.schedule`
   upserts by name, and `20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start`
   asserts exactly that. All 20 calling files also use the house unschedule-if-exists pattern.
3. A replayed plain `CREATE TRIGGER` of an existing name FATALs the replay, as it would on PROD,
   which already carries the trigger. That is fidelity, not a lane defect.
4. The drift gate runs BEFORE the replay and compares against the carried fold. The reasoning is
   `check_acl_fidelity`'s, recorded as plan 08 deviation 1: a replayed migration may legitimately
   change these objects, so the dump plus the carried fold is the fixed point the gate can be exact
   about. Today's replay set is empty, so the carried fold IS the full declared set.

## Noted, not planned (not in D-G's scope)

- The 5 `storage.*` policies above.
- The tension between `test_derive_allocator_keys_fanout.sql` (6) asserting a job PROD
  deliberately lacks, and the ROADMAP's "do NOT re-register". It is surfaced for the orchestrator,
  not resolved: the files are not edited (D-G).

⛔ Constraints:
- ⛔ The 8 files and every other `supabase/tests/test_*.sql` are NOT edited, marked LANE-ONLY or
  excused (D-G). `supabase/migrations/**` is not edited either.
- ⛔ Run NO database command against any remote. The CLI in this checkout is linked to
  PRODUCTION. Every DSN here is the lane's loopback DSN from `scripts/local-stack/.stack-env`.
- ⛔ Never `git checkout --`, `git restore` or `git stash`. A neuter is undone from a byte backup
  in the scratchpad, taken immediately before the edit and checked with `cmp` after the restore.
  Name the backup per agent.
- ⛔ No floor lowered, no waiver, no allowlist widened, no timeout relaxed.
- ⛔ The repo is PUBLIC. Output from these scripts names files, job names, trigger names, counts
  and sha256 prefixes. It never prints command text, a DSN or a password.
- Cite by SYMBOL. Never write a line number into a code comment.

**Booting the lane from a worktree (applies to every verify below).**
- The lane binds fixed host ports, so every boot takes the host lock: the directory
  `gsd-164.4.2-dbrun.lock` inside the path `git rev-parse --git-common-dir` prints.
- A worktree executor cannot run compound commands that name a `.git` path or wrap
  `bash`/`psql`. The boot steps are therefore SEPARATE simple commands, run ONE AT A TIME, in
  order:
  1. `git rev-parse --git-common-dir`, and note the absolute path it prints.
  2. `mkdir <that path>/gsd-164.4.2-dbrun.lock`. If it fails because the directory exists,
     another agent holds the lane: wait until it is gone (Monitor with an until-loop), then retry.
     Never remove a lock you did not create.
  3. The boot and the measurements the task names.
  4. `scripts/local-stack/run.sh down`
  5. `rmdir <that path>/gsd-164.4.2-dbrun.lock`
- Steps 4 and 5 run on EVERY path, the failing path included.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the auth.users trigger, extracted from its migration, loaded on the lane, gated, and the 3 auth files pass</name>
  <files>scripts/local-stack/nonpublic-objects.mjs, scripts/local-stack/nonpublic-catalogue.sql, scripts/local-stack/run.sh, scripts/local-stack/sql-corpus-report.mjs, src/__tests__/local-stack-lane-wiring.test.ts</files>
  <read_first>
    - scripts/local-stack/run.sh (`load_baseline`, `reset_public_default_acls`, `check_acl_fidelity`, `load_reference_data`, `replay_migrations`, `resolve_psql`, `check_baseline_currency`, and the header block `usage()` prints)
    - scripts/local-stack/acl-fidelity.mjs (argument handling, the one-line `acl-fidelity: … verdict OK|DRIFT|MEASURE_FAIL` summary, exit 0/1/2)
    - scripts/extract-reference-inserts.mjs (`maskSql` + `statements` imports, `dollarBodyRanges`, `insideBody`, and the header's refuse-by-default and public-repo output rules)
    - src/__tests__/local-stack-lane-wiring.test.ts (the `it(` titled "load_baseline() resets public's default ACLs before the dump loads, …" and the acl-fidelity behaviour arm below it, which is the shape to copy)
    - supabase/migrations/20260405061912_rls_policies.sql (the `on_auth_user_created` statement)
    - .github/workflows/ci.yml, step "Run SQL self-tests against the local-stack lane" in job `sql-tests`: `lane_only_marker()`, the per-file `psql … -f "$f"` call, NET 1 and NET 2
  </read_first>
  <action>
Build the thinnest D-G slice through every layer, carrying the auth-trigger class only. Task 2
adds cron to the same module.

(1) Create `scripts/local-stack/nonpublic-objects.mjs`, an ESM module with a header in this repo's
voice that says why it exists: D-G, plan 08's 8-file measurement, and the two rejected
alternatives. It has two modes.

`--emit --migrations <dir> --carried <file> --out-dir <dir>`:
- Read the carried basenames (one per line, the gate's `CARRIED_SET_FILE` format). Take the SAME
  strict-basename check `replay_migrations` applies.
- Fold, in filename order over the carried files only, every auth-schema trigger DDL.
- Find statements with `maskSql` + `statements` from `scripts/lint-sql-gates.mjs`. Exclude
  dollar-body spans with `dollarBodyRanges`/`insideBody` from `scripts/extract-reference-inserts.mjs`.
  Import all of these; do not write a second lexer.
- A statement is accepted ONLY in this shape: `CREATE [OR REPLACE] TRIGGER <name> BEFORE|AFTER
  <event>[ OR <event>…] ON auth.users FOR EACH ROW|STATEMENT EXECUTE FUNCTION|PROCEDURE
  <function>()`. `DROP TRIGGER [IF EXISTS] <name> ON auth.users` removes a name from the fold.
- Every other trigger shape is MEASURE_FAIL (exit 2) and names the file and statement index,
  never the text. That covers a WHEN clause, `UPDATE OF`, `REFERENCING`, `ENABLE`/`DISABLE
  TRIGGER`, a trigger on any auth table other than `users`, or trigger DDL on an auth table inside
  a dollar body.
- Also MEASURE_FAIL: a carried basename with no file in `--migrations`, and a malformed carried
  line.
- Write `<out-dir>/nonpublic-auth-triggers.sql`. It begins with `SET search_path TO public;`,
  then the surviving statements' ORIGINAL bytes, sliced by offset. Nothing is re-rendered.
- Print one census line starting `nonpublic-extract:` with the carried-file count, the
  auth-trigger count and each trigger's `name<-file`. Exit 0.

`--check --migrations <dir> --carried <file> --catalogue <rows>`:
- RE-DERIVE the fold from the migrations. Never read `--emit`'s output.
- Parse the catalogue rows `nonpublic-catalogue.sql` prints and compare. For each trigger, compare
  name, `tgtype` and `tgenabled = 'O'`. Decode the `tgtype` bits completely: ROW 1, BEFORE 2,
  INSERT 4, DELETE 8, UPDATE 16, TRUNCATE 32, INSTEAD 64. The expected function is
  schema-qualified, with an unqualified name read as `public.<name>()`.
- MISSING and EXTRA are both drift.
- Print a line starting `nonpublic-fidelity:` with `auth-users-triggers=<lane>/<declared>`,
  `drift=<n>` and `verdict OK|DRIFT|MEASURE_FAIL`, preceded by one `MISSING`/`EXTRA`/`DIFFERS`
  line per finding.
- Exit 0 on OK and 1 on DRIFT. Exit 2 on MEASURE_FAIL: an empty or unparseable catalogue, a
  missing meta row, or a meta row saying the reader is not a superuser. These are the acl-fidelity
  exit conventions, per the brief.

(2) Create `scripts/local-stack/nonpublic-catalogue.sql`:
- `SET search_path TO pg_catalog`, so `regprocedure` prints schema-qualified.
- A `meta|superuser|<rolsuper of current_user>` row and a `meta|database|<current_database()>` row.
- One `trigger|users|<tgname>|<tgtype>|<tgenabled>|<tgfoid::regprocedure>` row per NON-internal
  (`NOT tgisinternal`) trigger on `auth.users`.
- No trigger or job name appears in it: the catalogue reads everything and the module judges.

(3) `scripts/local-stack/run.sh`:
- Add `load_nonpublic_objects()`. It runs the module's `--emit` with `--migrations
  "$MIGRATIONS_DIR"`, `--carried "$CARRIED_SET_FILE"` and `--out-dir "$REPLAY_HANDOFF_DIR"`. It
  applies `nonpublic-auth-triggers.sql` through the superuser DSN, derived exactly as
  `reset_public_default_acls` derives it, with the same refusal when the DSN does not start with
  the `postgres` role. Use `-X -v ON_ERROR_STOP=1 -q` with `</dev/null`.
- Add `check_nonpublic_fidelity()`. It reads the catalogue through the same superuser DSN into a
  file under `REPLAY_HANDOFF_DIR`, then runs the module's `--check`.
- Every failure path in both functions echoes a `FATAL:` line naming the cause and exits
  non-zero. The existing EXIT trap tears the stack down.
- Call both in `load_baseline()` after `load_reference_data` and before `replay_migrations`, and
  write the reason for that placement in a comment block beside them: reference data first (the
  teaser sentinel, per `<context>`), the gate before the replay (the `check_acl_fidelity`
  precedent). Do not refactor `reset_public_default_acls`: its pins count its own exits.
- Add a D-G paragraph to the header block beside the D-F one.

(4) Create `scripts/local-stack/sql-corpus-report.mjs`. It is a DIAGNOSTIC and its header says
so: it is not a gate, it is not wired into CI, and CI's `sql-tests` step stays the authority.
- It reproduces four things from that step: the per-file `psql -X -v ON_ERROR_STOP=1 -f`, the
  `^-- LANE-ONLY:` exclusion, NET 1 (a file's own `RAISE NOTICE 'SKIP: …` marker appearing in
  its output) and NET 2 (`NOTICE:  SKIP:` in the output). It says it does NOT reproduce the
  sentinel or roster checks.
- It reads `DB_URL` from `scripts/local-stack/.stack-env` with the same loopback refusal as
  `load_baseline`, and resolves psql as `resolve_psql` does.
- It CONTINUES past a failure. It prints one `PASS|FAIL|SKIP|LANE-ONLY <file>` line per file,
  plus the first `ERROR:` line for a FAIL, then `scope: <n> of <corpus> files in supabase/tests`
  and the final line `sql-corpus: files=<n> pass=<p> fail=<f> whole-file-skip=<s> lane-only=<l>`.
- `--file <path>` (repeatable) narrows the run. The scope line then shows the narrowing, so a
  narrowed run can never read as the full corpus.
- Exit 0 only when fail and skip are both 0. Exit 1 otherwise. Exit 2 on no DB_URL, a non-loopback
  DSN, an empty corpus, no psql, or a `--file` that does not exist.

(5) `src/__tests__/local-stack-lane-wiring.test.ts`: add one behaviour arm modelled on the
acl-fidelity arm. It uses a temp dir, a synthetic migrations dir, a carried file and a synthetic
catalogue, with no Docker, and drives the module through `spawnSync`. It asserts:
- OK on an exact match;
- DRIFT (exit 1) naming MISSING when the catalogue lacks the trigger;
- DRIFT naming EXTRA on an undeclared trigger;
- a DROP TRIGGER in a later carried file removes it from the fold;
- a trigger in a file NOT in the carried set is not expected;
- MEASURE_FAIL (exit 2) on a WHEN clause, on a `meta|superuser|f` catalogue and on an empty
  catalogue.
  </action>
  <verify>
    <automated>Steps run ONE AT A TIME; each is a single simple command.
(a) `npx vitest run src/__tests__/local-stack-lane-wiring.test.ts` passes, including the new trigger arm.
(b) `scripts/local-stack/run.sh --check-currency`, then `node scripts/local-stack/nonpublic-objects.mjs --emit --migrations supabase/migrations --carried scripts/local-stack/.stack/replay/carried-set.txt --out-dir <a fresh scratchpad dir>`: exit 0, and the `nonpublic-extract:` line reads 1 auth trigger, `on_auth_user_created<-20260405061912_rls_policies.sql`.
(c) Take the host lock as in `<context>`. `scripts/local-stack/run.sh up`: exit 0, and the log carries a `nonpublic-fidelity:` line with `auth-users-triggers=1/1` and `verdict OK`, then `up complete`.
(d) `node scripts/local-stack/sql-corpus-report.mjs --file supabase/tests/test_handle_new_user_role_allowlist.sql --file supabase/tests/test_profiles_privileged_columns_locked.sql --file supabase/tests/test_csv_finalize_atomic_fold.sql` prints `sql-corpus: files=3 pass=3 fail=0 whole-file-skip=0 lane-only=0` and exits 0.
(e) `scripts/local-stack/run.sh down`, `scripts/local-stack/run.sh --assert-teardown`, then release the lock.</automated>
    <fails_when>The fold is empty, or it counts a statement from a non-carried file. Also fails when the trigger is created somewhere the gate cannot see (it must read through the superuser DSN), when the gate prints OK over a catalogue that lacks the trigger (the MISSING arm in (a) goes red), or when any of the 3 auth files still fails in (d), for example with the `strategies_user_id_fkey` or `handle_new_user did not create a profile` errors plan 08 recorded.</fails_when>
  </verify>
  <done>The lane boots with `on_auth_user_created` on `auth.users` extracted from its migration and proven by the gate; the 3 auth-trigger files pass unedited; the corpus diagnostic exists and reports per file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: every pg_cron registration, folded from the carried migrations, registered as the loading role and gated byte-exact; a replayed reschedule does not double-register</name>
  <files>scripts/local-stack/nonpublic-objects.mjs, scripts/local-stack/nonpublic-catalogue.sql, scripts/local-stack/run.sh, src/__tests__/local-stack-lane-wiring.test.ts</files>
  <read_first>
    - scripts/local-stack/nonpublic-objects.mjs, nonpublic-catalogue.sql and run.sh as Task 1 left them
    - scripts/sql-body-normalize.mjs (`scanSql`'s `stripped` and `masked` contract, in the doc block above it)
    - scripts/prod-prober/arms/cron-drift.mjs (`normalizeCommand`, and the header paragraph listing the fifteen jobnames migrations schedule)
    - supabase/migrations/20260515113637_resend_message_correlation.sql (the nested `$$` command) and supabase/migrations/20260803120000_strategy_analytics_stamp_trigger_null_stamp_clock_start.sql (the upsert-by-name assertion)
    - The 5 cron files' `cron.job` reads, to see which fields they compare: supabase/tests/test_retention_crons_safe.sql reads `command`, and supabase/tests/test_reconcile_dropped_enqueue_sweep.sql reads `schedule`
  </read_first>
  <behavior>
    - A later `cron.schedule` of the same name replaces the earlier one's schedule and command; `cron.unschedule` removes it; an unschedule of a never-scheduled name is a no-op the census names.
    - The command is the literal's exact bytes: a `$cron$…$cron$` body, a `'…'` literal with `''` unescaped, and the nested same-tag `$$` inside `DO $$` (the resend shape) all yield the inner text byte-exact, and a comment between arguments is skipped.
    - A non-literal argument (an identifier, `||`, `format(`, a variable), an unclosed argument list, a `cron.alter_job` or `cron.schedule_in_database` call site, and a cron call site inside a single-quoted string literal are each MEASURE_FAIL (exit 2), naming the file.
    - The gate says DRIFT (exit 1) for a missing job, an extra job, a changed schedule, a command differing by one byte, `active` false, and a `username` other than the loading role.
    - A job declared only in a file NOT in the carried set is not expected.
  </behavior>
  <action>
Extend the Task 1 module and wiring to the cron class, per D-G's "every `cron.schedule`
registration, taken from the migrations in the order they declare them, and superseded or
unscheduled exactly as later migrations do". Write the behaviour arms first and watch them fail.

(1) `nonpublic-objects.mjs --emit`, cron fold. Over the carried files in filename order:
- Locate call sites with `maskSql`'s code positions, matching `cron.schedule(`,
  `cron.unschedule(`, `cron.alter_job(` and `cron.schedule_in_database(` with any whitespace
  around the dot. `maskSql` scans dollar bodies as code, which is why every call inside a `DO`
  block is found.
- Parse each call's arguments from the ORIGINAL text, starting at its own `(`. Accept only a
  `'…'` literal or a `$tag$…$tag$` literal, with whitespace and comments between them.
- `schedule` needs exactly 3 literal args and `unschedule` exactly 1. Anything else is
  MEASURE_FAIL.
- **Dynamic-SQL guard:** count call-site matches in `scanSql`'s comment-stripped text, which
  keeps strings, and compare that with the `maskSql` code-position count, which blanks strings.
  Do this per file. If they disagree, some call sits inside a string literal that an `EXECUTE`
  could run, so exit 2 with MEASURE_FAIL. The measured corpus agrees at 61 = 61.
- A call site that falls inside another call's argument span (a command that itself schedules) is
  MEASURE_FAIL.
- Write `<out-dir>/nonpublic-cron.sql` as one `SELECT cron.schedule(<name>, <schedule>,
  <command>);` per surviving job. It uses the literals' ORIGINAL bytes and is ordered by each
  job's last declaring call (file, then offset).
- Extend the `nonpublic-extract:` line with the call-site counts (schedule, unschedule), the final
  job count, and the job names only. Never print a command.

(2) `nonpublic-catalogue.sql`: add one `cron|…` row per `cron.job` row with jobname, schedule,
`active`, `username`, `database` and the command. Hex-encode every free-text field (jobname,
schedule, command) with `encode(convert_to(x, 'UTF8'), 'hex')`, because commands carry newlines
and `|`. Keep `meta|pg_cron|<installed 0/1>`. If jobs are expected while pg_cron is absent, the
check is MEASURE_FAIL.

(3) `nonpublic-objects.mjs --check`, the cron comparison:
- Compare jobname, schedule and command as EXACT bytes, not normalized, because
  `test_retention_crons_safe.sql` reads the command verbatim.
- Also compare `active = true`, `username = <the value of --cron-owner>` and `database = <the meta
  database row>`.
- Print drift findings with job names and the sha256 prefixes of the two commands, never the text.
- Extend the `nonpublic-fidelity:` line with `cron-jobs=<lane>/<declared>`.

(4) `run.sh`:
- `load_nonpublic_objects` applies `nonpublic-cron.sql` through the LOADING DSN `"$db_url"`, not
  the superuser DSN, in a single transaction (`-1`). The reasons are in `<context>`: `username`
  parity with PROD, the row-level security on `cron.job`, and no superuser-run jobs.
- `check_nonpublic_fidelity` passes `--cron-owner` with the loading role read from `db_url`. That
  role is the one `reset_public_default_acls` already requires the DSN to name.
- Do not add a runtime role fallback. If `postgres` is refused, the boot FATALs, and you take the
  `<context>` alternative ONLY on that measured refusal, recorded in the SUMMARY.

(5) The lane-wiring test: add the `<behavior>` cases as arms of the Task 1 behaviour test, or a
sibling `it(`, driven through `spawnSync` against synthetic migrations and catalogues.
  </action>
  <verify>
    <automated>Steps run ONE AT A TIME; each is a single simple command.
(a) `npx vitest run src/__tests__/local-stack-lane-wiring.test.ts` passes. BEFORE writing the implementation, the new cron arms were run once and FAILED; record that red.
(b) `scripts/local-stack/run.sh --check-currency`, then `node scripts/local-stack/nonpublic-objects.mjs --emit --migrations supabase/migrations --carried scripts/local-stack/.stack/replay/carried-set.txt --out-dir <fresh scratchpad dir>`: exit 0, and the census reads 31 schedule and 30 unschedule call sites and 15 final jobs, named exactly as in `<context>`. If a migration merged since planning moves these numbers, re-measure and record the new census; never force the old one.
(c) Double-registration proof, using the D-F synthetic seam:
- Copy `supabase/migrations/*.sql` into a scratchpad dir M.
- Add ONE replayed file `29990101000005_lane_cron_resched.sql`. It follows the house pattern for `reap_strategy_analytics_stuck_computing` (if a job of that name exists, `cron.unschedule` it, then `cron.schedule` it with schedule `*/15 * * * *` and command `SELECT 1;`), inside `DO $$ … $$` guarded by the pg_cron `pg_extension` check.
- Write a scratchpad assertion file A holding a `DO` block. It RAISEs unless all three hold: exactly one `cron.job` row has that name; its command is `SELECT 1;`; and `cron.job` holds 15 rows.
- Take the host lock. Run `LANE_MIGRATIONS_DIR=<M> scripts/local-stack/run.sh up`. Expect exit 0, the log line `nonpublic-fidelity: … cron-jobs=15/15 … verdict OK` BEFORE `replayed 29990101000005_lane_cron_resched.sql`, and `replay complete: 1 migration(s)`.
- Run `node scripts/local-stack/sql-corpus-report.mjs --file <A>` and expect `pass=1`.
- Run `scripts/local-stack/run.sh down`. Keep the lock.
(d) `scripts/local-stack/run.sh up` with no overrides, then `node scripts/local-stack/sql-corpus-report.mjs --file` once for each of: supabase/tests/test_derive_allocator_keys_fanout.sql, supabase/tests/test_reconcile_dropped_enqueue_sweep.sql, supabase/tests/test_retention_crons_safe.sql, supabase/tests/test_retention_orphaned_running.sql, supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql. The line must read `files=5 pass=5 fail=0 whole-file-skip=0`.
(e) `scripts/local-stack/run.sh down`, `scripts/local-stack/run.sh --assert-teardown`, then release the lock.</automated>
    <fails_when>Fails when a command is extracted with any byte changed, in which case the retention file's verbatim command read fails. Fails when jobs are registered under a role the corpus's `postgres` connection cannot see, which the cron files report as NOT registered. Fails on a double registration, which the assertion in (c) catches as two rows, or as the gate running after the replay and finding 15 declared but a different set present. Fails when any of the 5 cron files still fails, or when a dynamic or non-literal call site is accepted instead of refused (an arm in (a)).</fails_when>
  </verify>
  <done>The lane's cron.job equals the 15-job fold byte-exact under the loading role; a replayed reschedule supersedes by name without a second row; the 5 cron files pass unedited.</done>
</task>

<task type="auto">
  <name>Task 3: pin the wiring, observe the gate and the pins RED under neuter, and run the WHOLE corpus on the lane</name>
  <files>src/__tests__/local-stack-lane-wiring.test.ts</files>
  <read_first>
    - src/__tests__/local-stack-lane-wiring.test.ts (`bashFunctionBody`, `liveLines`, and the ACL order pin, which this pin extends)
    - scripts/local-stack/run.sh as Tasks 1 and 2 left it
  </read_first>
  <action>
(1) Add an `it(` structural pin, in the style of the ACL order pin, that pins D-G's wiring:
- Inside `load_baseline()`, the order is check_acl_fidelity, then load_reference_data, then
  load_nonpublic_objects, then check_nonpublic_fidelity, then replay_migrations. Each is present.
  The pin messages name the consequence: gate after the replay means it compares against a moved
  fixed point; objects before reference data means the teaser sentinel profile changes; no call
  means 8 corpus files fail.
- `load_nonpublic_objects` passes `--carried "$CARRIED_SET_FILE"` and `--migrations
  "$MIGRATIONS_DIR"`. It applies the trigger file through the superuser DSN and the cron file
  through `"$db_url"`. It has at least as many `exit` lines with a non-zero status as it has
  failure paths.
- `check_nonpublic_fidelity` reads the catalogue through the superuser DSN, passes
  `--cron-owner`, and exits non-zero on failure.
- `nonpublic-catalogue.sql`, with its live lines only, reads `cron.job` and filters `NOT
  tgisinternal`. It names no jobname or trigger name literally.

(2) Neuter REDs, each with a byte backup taken immediately before, restored with `cp`, and
checked with `cmp` (never git):
- (i) Delete the `load_nonpublic_objects` call from `load_baseline()`. The new pin FAILS with its
  "no call" message. Restore.
- (ii) Move `check_nonpublic_fidelity` after `replay_migrations`. The pin FAILS with its ordering
  message. Restore.
- (iii) The BOOT-level RED. Take the host lock and delete the `load_nonpublic_objects` call again.
  `scripts/local-stack/run.sh up` must exit non-zero, and its log must carry
  `nonpublic-fidelity: … verdict DRIFT` naming MISSING `on_auth_user_created` and the missing
  jobs, then a `FATAL:` line. Then `scripts/local-stack/run.sh --assert-teardown` finds 0
  containers. Restore, check with `cmp`, and keep the lock for (3).

(3) The whole corpus, on the restored lane:
- Run `scripts/local-stack/run.sh up`, then `node scripts/local-stack/sql-corpus-report.mjs` with
  no `--file`.
- The bar is `sql-corpus: files=76 pass=75 fail=0 whole-file-skip=0 lane-only=1`, with the one
  LANE-ONLY file being `supabase/tests/test_prod_prober_cadence.sql`.
- If the count of `supabase/tests/test_*.sql` has changed since planning, the bar is (corpus - 1)
  pass with the same 1 LANE-ONLY.
- Anything else FAILS the task loudly: name each failing file and its first error line in the
  SUMMARY. Never edit, mark or excuse a file to meet the bar (D-G).
- Then run `down` and release the lock.

(4) Gates: ESLint on the three new `.mjs` files and the test, `shellcheck scripts/local-stack/run.sh`,
and the neighbour files plan 08 ran:
- baseline-wiring-claim
- local-stack-teardown-assertion
- local-stack-lane-wiring
- the ci-anti-skip-gate contract
- critical-regressions
- test-restore-workflow-wiring

The SUMMARY records every RED/GREEN in a table, as plan 08's checkpoint section does.
  </action>
  <verify>
    <automated>Steps run ONE AT A TIME; each is a single simple command.
(a) With neuter (i) in place, `npx vitest run src/__tests__/local-stack-lane-wiring.test.ts` exits non-zero and names the new pin. After the restore, `cmp` against the backup is identical and the same command passes. Repeat for neuter (ii).
(b) Neuter (iii) under the host lock: `scripts/local-stack/run.sh up` exits non-zero with `verdict DRIFT` and a `FATAL:` line; `scripts/local-stack/run.sh --assert-teardown` exits 0; restore and `cmp` identical.
(c) `scripts/local-stack/run.sh up` exits 0, then `node scripts/local-stack/sql-corpus-report.mjs` prints `sql-corpus: files=76 pass=75 fail=0 whole-file-skip=0 lane-only=1` and exits 0.
(d) `scripts/local-stack/run.sh down`, then release the lock.
(e) `npx vitest run src/__tests__/local-stack-lane-wiring.test.ts src/__tests__/baseline-wiring-claim.test.ts src/__tests__/local-stack-teardown-assertion.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts src/__tests__/test-restore-workflow-wiring.test.ts` passes.
(f) `git diff --stat a866fcd8 -- supabase/tests supabase/migrations` prints nothing.</automated>
    <fails_when>Fails when the pin cannot go red under either neuter, meaning it pins nothing. Fails when the boot with the load call removed still reaches `up complete`, meaning the gate is a warning, or when it leaves containers running. Fails when the whole-corpus run shows any FAIL or whole-file SKIP, or a LANE-ONLY count other than 1. Fails when any file under supabase/tests or supabase/migrations changed on this branch since a866fcd8.</fails_when>
  </verify>
  <done>D-G's wiring is pinned and each pin and the gate have been seen red; the whole corpus reads 75 pass + 1 LANE-ONLY of 76 on the lane with no test or migration edited.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration text → extractor | Repo-reviewed SQL is parsed. A misparse would register the wrong object and every cron or auth gate would then measure it. |
| run.sh → lane database | A loopback-only DSN, and a superuser DSN derived from it. Nothing leaves 127.0.0.1/localhost. |
| lane catalogue → drift gate | The gate trusts rows the lane prints. Rows it cannot see are silently absent. |
| script output → public CI log | The repo and its Actions logs are world-readable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-11-01 | Tampering | `nonpublic-objects.mjs --emit` | high | mitigate | Literal-only argument parse from the ORIGINAL bytes. MEASURE_FAIL on a non-literal, unclosed, nested, `alter_job` or `schedule_in_database` call. The two-lexer per-file count agreement catches a call inside a string literal. `--check` re-derives the expected set from source and compares the command bytes exactly. |
| T-164.4.2-11-02 | Elevation of Privilege | cron registration in `load_nonpublic_objects` | high | mitigate | Jobs are registered through the loading DSN, never the superuser. The gate checks `username = --cron-owner`, so a job running with superuser rights (a wider catalogue than PROD's, the ACL-defect class) is DRIFT. |
| T-164.4.2-11-03 | Repudiation (vacuous pass) | `nonpublic-catalogue.sql` read | high | mitigate | The catalogue is read through the superuser DSN. The `meta|superuser|t` row is REQUIRED, and its absence or `f` is MEASURE_FAIL, because `cron.job` RLS hides other roles' rows from a non-superuser. An empty catalogue is MEASURE_FAIL, never OK. |
| T-164.4.2-11-04 | Spoofing (wrong target) | superuser DSN derivation | critical | mitigate | Derived only from the lane's own loopback `db_url`, exactly as `reset_public_default_acls` does, with the same refusal. `load_baseline`'s loopback check runs before it. No remote DSN is ever accepted. |
| T-164.4.2-11-05 | Information Disclosure | extractor, gate and corpus-report output | low | mitigate | Output is file names, job and trigger names, counts and sha256 prefixes only. No command text, DSN or password. The corpus report runs locally and is not wired into CI. |
| T-164.4.2-11-06 | Denial of Service (flake) | active cron jobs firing mid-run on the lane | low | accept | This is parity with PROD and with shared TEST, where the corpus historically ran against the same active jobs. A timing-induced failure surfaces as a named per-file FAIL in the report and CI, never as a pass. |
| T-164.4.2-11-SC | Tampering | package installs | low | accept | None. This plan adds no dependency. |
</threat_model>

<verification>
- The lane boots with `nonpublic-fidelity: auth-users-triggers=1/1 cron-jobs=15/15 … drift=0 verdict OK`, logged before the replay.
- The gate and the pins were each observed RED under a neuter, and restored byte-identical.
- The whole corpus reads 75 pass + 1 LANE-ONLY of 76 on the lane, and nothing under `supabase/tests` or `supabase/migrations` changed.
- The SUMMARY records the measured role outcome (whether `postgres` could call `cron.schedule`), the census as re-measured at execution, and the list of what only CI can show (see `<context>` Ordering).
</verification>

<success_criteria>
- D-G is realised. The lane carries the `auth.users` trigger and the folded `cron.job` set, both extracted from `supabase/migrations`, and drift fails the boot.
- The 8 files from plan 08's checkpoint pass unedited.
- Plan 08 Task 3's SHA-bound CI re-read, which happens after this plan, can expect `sql-tests` green on these grounds.
</success_criteria>

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-11-SUMMARY.md` when done.
</output>
