---
phase: 164.4.2-subsetsplit
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md
autonomous: true
requirements: [AREA-E]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The pre-change acquire-wait and useful-work split for sql-tests, python and e2e-seeded is recorded from real merge-push runs, with run ids, BEFORE any change in this phase lands."
    - "The refutation condition is written down before the change is built, so the result cannot be graded on its own narrative."
    - "A reader can re-run the recipe verbatim and get comparable numbers at a later head."
  artifacts:
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md
  key_links:
    - "Every recorded head SHA is an ancestor of this phase's branch point — that is what makes the capture a BEFORE."
    - "sql-tests's own total job wall-clock is captured beside its acquire-wait, so a relocated cost (local-stack boot) is visible in plan 08's AFTER even though it does not violate the stated refutation condition."
---

<objective>
Capture the falsifying BEFORE measurement for DECISION E (`164.4.2-CONTEXT.md` Area E), from
real `ci.yml` merge-push runs, and write the refutation condition down in advance.

Purpose: DECISION E is explicit that the measurement is decided BEFORE the change is built
"so the change cannot be graded on its own narrative". This plan is the only one in the phase
that must run before anything else changes, and it is doc-only — it touches no code, no
workflow and no database, so it runs in wave 1 beside plan 02 with zero file overlap.

Output: `164.4.2-MEASUREMENT.md` — recipe, BEFORE table, prediction, refutation condition.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-RESEARCH.md

⛔ REPO IS PUBLIC AND `.planning/` IS TRACKED. Run ids, head SHAs and job names are public
facts and are fine. NEVER a credential, connection string, project ref, key, absolute home
path, local username or broker name. Record VERDICTS and COUNTS, never specimens.
⛔ No database command of any kind in this plan. `gh` reads only.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Capture the BEFORE numbers end-to-end, from gh, for the three mutex holders</name>
  <files>.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md</files>
  <action>
Create `164.4.2-MEASUREMENT.md`. This is the thin end-to-end slice of Area E: one recipe, run
for real, producing one table a later reader can reproduce.

Enumerate the merge-push runs first:
`gh run list --workflow=ci.yml --branch main --event push --json databaseId,headSha,createdAt,conclusion --limit 12`
Take the most recent runs that CONCLUDED (skip cancelled and in-progress). Use at least 5.

For each run id, pull per-step timings:
`gh api "repos/AI-Isaiah/Quantalyze/actions/runs/<run_id>/jobs?per_page=100" -q '.jobs[] | select(.name=="sql-tests" or .name=="python" or .name=="e2e-seeded") | {name, started_at, completed_at, steps: [.steps[] | {name, started_at, completed_at}]}'`

Per run, per job, record FOUR quantities, each derived from the step timestamps (RESEARCH R5
confirmed this API surface live and named the two steps for `sql-tests`):
  * acquire-wait — the duration of the step whose name begins `Acquire shared-test-db mutex`
  * useful work — the duration of the job's own work step (for `sql-tests`, the step whose
    name begins `Run SQL self-tests`)
  * the JOB's own total wall-clock (`started_at` -> `completed_at` on the job)
  * the RUN's wall-clock (`started_at` -> `completed_at` on the workflow run)

⭐ The job total is captured deliberately and is NOT decoration: RESEARCH Open Question 2
records that the local-stack boot has a fixed cost that was never measured, so a cost that
merely RELOCATES from mutex-wait into boot-time must be visible in plan 08's AFTER table even
though it does not violate the stated refutation condition.

If a job is absent from a run (a skip), record it as ABSENT with the reason from its `if:`
— ⛔ never as a zero. Unknown is not zero.

Write the exact commands used into the document, verbatim, under a `## Recipe` heading, so
plan 08 re-runs the same thing rather than a remembered approximation. No new script: PATTERNS
§5 measured that this repo has no committed CI-timing tool and states that inventing one where
no analog exists is new surface without a shape to justify it.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync(process.argv[1],'utf8');const runs=new Set([...t.matchAll(/\b\d{10,11}\b/g)].map(m=>m[0]));if(runs.size<5)throw new Error('fewer than 5 run ids recorded: '+runs.size);for(const k of ['acquire-wait','sql-tests','python','e2e-seeded','## Recipe'])if(!t.includes(k))throw new Error('MEASURE_FAIL: the document does not carry '+k);console.log('MEASUREMENT.md BEFORE: '+runs.size+' run id(s), three jobs, recipe present')" .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md</automated>
  </verify>
  <done>The document carries a BEFORE table with >=5 run ids, their head SHAs, and per-job acquire-wait / useful-work / job-total / run-total, plus the verbatim recipe. A job that did not run is named ABSENT with its reason, never 0.</done>
</task>

<task type="auto">
  <name>Task 2: Write the prediction and the refutation condition down BEFORE the change exists</name>
  <files>.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md</files>
  <action>
Add two sections to the same document.

`## Prediction` — quote the ROADMAP's own number for this phase (the typical run should land
near 11-12 min, bounded by `sql-mutation` at 10.8) and state which quantity it is a prediction
ABOUT.

`## Refutation condition — decided in advance (DECISION E)` — copy DECISION E's four clauses
as binding text, in particular:
  * if `sql-tests`'s acquire-wait does not fall to approximately zero, the change DID NOT WORK
    — say so and re-measure; ⛔ do not re-describe the result as a partial win;
  * a faster wall clock ALONE does not confirm it — CI wall-clock varies with runner
    availability and queue depth, so the binding number is `sql-tests`'s own acquire-wait;
  * `python` and `e2e-seeded` stay on the shared key after the tracer, so their acquire-wait
    is expected to change for reasons this phase did not control — record it, do not claim it.

Add one more clause this plan is adding on measured grounds, marked as such: after plan 05 the
VAC-08 drift check lives in its own job that still holds the shared key, so the AFTER table
must include that job by name. ⛔ A win that merely moved the wait into a new job is not a win,
and reporting it as one would be the measurement equivalent of a vacuous gate.

Prove every head SHA recorded in task 1 predates this phase's work:
`git merge-base --is-ancestor <head_sha> HEAD && echo BEFORE-CONFIRMED` for each, and record
the verdict line. That is what makes this capture a BEFORE rather than a claim about one.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync(process.argv[1],'utf8');for(const k of ['## Prediction','## Refutation condition','acquire-wait does not fall','BEFORE-CONFIRMED'])if(!t.includes(k))throw new Error('MEASURE_FAIL: missing '+k);console.log('MEASUREMENT.md: prediction + refutation condition recorded before the change')" .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md</automated>
  </verify>
  <done>The refutation condition is in the repo, dated, before any code in this phase changes; every recorded head SHA is proven an ancestor of HEAD.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions API -> tracked planning doc | public run metadata is copied into a world-readable file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-01 | Information Disclosure | `164.4.2-MEASUREMENT.md` | medium | mitigate | Record run ids, SHAs, job names and durations ONLY. No DSN, no project ref, no secret name value, no absolute home path, no local username. `npm run check:planning-hygiene` is a required gate of this plan. |
| T-164.4.2-02 | Repudiation | the BEFORE table | high | mitigate | Every row is bound to a run id AND a head SHA, and each SHA is proven an ancestor of HEAD — a later reader can re-pull the same run rather than trust the table. |
</threat_model>

<verification>
- `npm run check:planning-hygiene` passes (⚠️ `git add` the new file first; the count line and the OK line are DIFFERENT lines — never `tail -1`).
- Both task `<automated>` checks pass.
- No command in this plan touched a database.
</verification>

<success_criteria>
The phase can be falsified later: BEFORE numbers exist for the three mutex holders across >=5
concluded merge-push runs, the recipe to reproduce them is verbatim in the repo, and the
condition under which this phase must declare failure was written before the work began.
</success_criteria>

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-01-SUMMARY.md` when done.
</output>
