---
phase: 164.4.2-subsetsplit
plan: "10"
type: execute
wave: 8
depends_on: ["164.4.2-09"]
files_modified:
  - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md
  - TODOS.md
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: true
requirements: [AREA-E, AREA-F, REDUNDER-SUBSET-SPLIT]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-E, the AFTER measurement is fully specified before the merge — recipe, four jobs, quantities and the CORRECTED refutation clauses (a)-(d) — so the only thing missing after the merge is the numbers."
    - "[REDUNDER-SUBSET-SPLIT] is closed by the phase that owns it, with the mechanism that closes it named, and timeout-minutes stated as NOT raised."
    - "What this phase did NOT do is booked rather than implied, including what D-F leaves open."
    - "The release carries one CHANGELOG entry in the same commit as the VERSION bump, and every commit on the branch maps to at least one bullet."
  artifacts:
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md
    - CHANGELOG.md
  key_links:
    - "VERSION and package.json are BYTE-EQUAL four-digit strings and VERSION carries no trailing newline — src/__tests__/critical-regressions.test.ts asserts it."
    - "The AFTER section cites MEASUREMENT.md's own `## Recipe` and `## Refutation condition — decided in advance (DECISION E)` sections by heading; it does not restate a remembered variant."
---

<objective>
Make the phase's claim falsifiable after the merge, close its backlog obligation, and make it
shippable.

Purpose: D-E's AFTER measurement can only be taken on merge-push runs at the new head, which do not
exist until this lands — so this plan writes everything about the AFTER except its numbers. ⛔ The
superseded plan 09 restated the ORIGINAL refutation condition ("acquire-wait falls to approximately
zero"), which CONTEXT Area E struck on 2026-09-21 as unfalsifiable by construction. This plan uses
the corrected clauses (a)-(d) only. And every ship writes a CHANGELOG entry in the same commit as the
VERSION bump (CLAUDE.md "CHANGELOG discipline").

Output: an AFTER protocol with one hole in it, a closed backlog item with its follow-ons booked, a
version and an entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-06-SUMMARY.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-08-SUMMARY.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-09-SUMMARY.md

**The CHANGELOG mechanism, a CHECKLIST and not background reading:** (1) `git log <base>..HEAD
--oneline` — enumerate EVERY commit and COUNT them; (2) `git diff <base>...HEAD` — read the full diff;
(3) group by theme BEFORE writing a line; (4) ONE unified entry, newest first,
`## [X.Y.Z.B] - YYYY-MM-DD`, replacing any per-commit entries already on this branch; (5) ⛔
CROSS-CHECK every commit maps to at least one bullet; (6) this repo's section vocabulary
(`### Fixed`, `### Added`, `### Changed`, `### Removed`, `### Tests`, `### Notes`, `### Security`,
`### Root cause`, `### Why`).

⛔ Constraints:
- ⛔ `VERSION` and `package.json` BYTE-EQUAL four-digit strings; `VERSION` has NO trailing newline.
  ⛔ Never run `npm version`.
- ⛔ Never write a CI skip token in a commit message, not even inside a sentence denying it.
- ⛔ Do NOT modify `.planning/milestone.lock`, `.planning/state.json`, `.planning/phases/164.5.3-*`,
  or any other phase's VERIFICATION.
- ⛔ `.planning/` is TRACKED and this repository is PUBLIC: run ids and SHAs only — no DSN, project
  ref, host, absolute path or username.
- ⛔ Never quote a credential-shaped fixture literal in CHANGELOG prose.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the AFTER protocol, complete except for the numbers, with the CORRECTED refutation</name>
  <files>.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md</files>
  <read_first>
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md (the whole file: `## Recipe`, `## BEFORE table`, `## Prediction`, `## Refutation condition — decided in advance (DECISION E)`)
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md (AREA E)
  </read_first>
  <action>
Per D-E, add `## AFTER — to be filled from merge-push runs at the new head` to MEASUREMENT.md,
carrying everything except the numbers:
  * the commands from `## Recipe`, cited by heading and pasted verbatim — never a remembered variant;
  * the FOUR jobs: `sql-tests` (holds no key now; its acquire-wait is zero BY CONSTRUCTION and is
    therefore NOT evidence of anything), `python`, `e2e-seeded`, `test-db-drift` (the holder plan 05
    created);
  * per job: acquire-wait, useful work, the job's own total wall-clock; plus the run's critical path;
  * the verdict rule = the four clauses of `## Refutation condition — decided in advance (DECISION E)`,
    applied by reference and summarised in one line each: (a) critical path outside the BEFORE
    spread, (b) `sql-tests`' job-TOTAL falls, not merely its wait — its boot now also carries D-F's
    replay time, which counts against it, (c) the three remaining holders' combined acquire-wait does
    not absorb what `sql-tests` gave up, (d) a faster wall clock alone confirms nothing. ⛔ Do not
    restate the struck "falls to approximately zero" condition as a criterion; it may appear only as
    the lineage MEASUREMENT.md already carries;
  * one line stating what a WIN looks like for the phase given that three holders remain, so the
    AFTER cannot be graded generously;
  * an empty table with its rows and columns already laid out, so filling it is arithmetic.
Where MEASUREMENT.md says the AFTER table belongs to "plan 08", add a dated note that the replan of
2026-09-23 moved it to plan 10; do not rewrite the original sentence.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync(process.argv[1],'utf8');const i=t.indexOf('## AFTER');if(i<0)throw new Error('MEASURE_FAIL: no AFTER section');const a=t.slice(i);for(const k of ['sql-tests','python','e2e-seeded','test-db-drift','acquire-wait','job-total','critical path','## Recipe','Refutation condition'])if(!a.includes(k))throw new Error('MEASURE_FAIL: the AFTER section omits '+k);console.log('AFTER protocol laid out for four jobs against the corrected refutation')" .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md</automated>
    <fails_when>node throws `MEASURE_FAIL: no AFTER section` or `MEASURE_FAIL: the AFTER section omits <term>` naming a missing job, quantity or section reference</fails_when>
    <human-check>AFTER THE MERGE, before this phase's verification may be called passed: run the recorded recipe against the first concluded merge-push run of `ci.yml` at the new head, fill the AFTER table, and apply clauses (a)-(d) as written. If a clause comes out wrong, record that the change did not work and re-measure — do not re-describe the result.</human-check>
  </verify>
  <acceptance_criteria>
    - the node check prints its OK line
    - the AFTER table has a row for each of the four jobs and a run-level critical-path row, all value cells empty
    - `npm run check:planning-hygiene` prints its OK line after `git add`
  </acceptance_criteria>
  <done>The AFTER section is complete except for measured values, applies the corrected clauses (a)-(d), and the post-merge obligation is an explicit human check.</done>
</task>

<task type="auto">
  <name>Task 2: close [REDUNDER-SUBSET-SPLIT] by its mechanism, and book what this phase did not do</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md (the `[REDUNDER-SUBSET-SPLIT]` entry and the format of neighbouring entries: owner, trigger, date)
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md (`## Noted for later — NOT this phase`)
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-05-SUMMARY.md (Findings 2) and 164.4.2-06-SUMMARY.md
  </read_first>
  <action>
Close `[REDUNDER-SUBSET-SPLIT]` in place by naming the MECHANISM that closes it — the runner's
`--subset-from` mode, the pull-request-only event rule, the `scope:` line and the assert-step arms
that make a narrowed run unmistakable — and state plainly that `timeout-minutes` was NOT raised.

Book what remains, each with an owner, a trigger and a date, as a decision rather than an omission:
  * `python` and `e2e-seeded` still hold advisory key `61616158`, and `test-db-drift` now does too;
    their move is decided by the AFTER numbers (CONTEXT "Noted for later"), not assumed.
  * the LANE-ONLY marker in the gate corpus whose stated reason argues from shared TEST while the
    job that reads it no longer runs there (plan 05 Findings 2; `supabase/tests/` is pin-counted).
  * D-F realised the carried-migrations marker for the LANE only. `refuse_stale_baseline()` in the
    TEST restore path still judges currency by commit epochs — which misclassifies a migration
    committed after a higher-versioned one (measured: 5 such commits in this repo's history). A
    repo-wide sweep of `baseline.sql` consumers is out of this phase's scope (CONTEXT).
  * D-F's known consequence: a migration newer than the dump that refuses on an empty or
    unidentified database (a data-reading `DO` block, a database-identity check) reddens every
    lane-booting job until the dump is regenerated after its PRODUCTION apply — the lane analogue of
    `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`. Cross-reference that entry rather than duplicating it.
⚠️ Deferral discipline: only a data-integrity or user-facing item earns a PHASE via the roadmap;
everything else is fix-or-drop in `TODOS.md`. ⛔ Do not invent an id that nothing references.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('TODOS.md','utf8');const i=t.indexOf('[REDUNDER-SUBSET-SPLIT]');if(i<0)throw new Error('the entry vanished — it must be CLOSED, not deleted');const line=t.slice(t.lastIndexOf('\n',i)+1,t.indexOf('\n',i));if(!/^\s*-\s*\[x\]/i.test(line))throw new Error('MEASURE_FAIL: [REDUNDER-SUBSET-SPLIT] is still open: '+line.slice(0,80));console.log('[REDUNDER-SUBSET-SPLIT] closed in place')"</automated>
    <fails_when>node throws `the entry vanished` or `MEASURE_FAIL: [REDUNDER-SUBSET-SPLIT] is still open`</fails_when>
  </verify>
  <acceptance_criteria>
    - the node check prints its OK line
    - the closing text contains `--subset-from` and `timeout-minutes`
    - each of the four follow-ons is present with an owner, a trigger and a date (the SUMMARY quotes the four entry headlines)
  </acceptance_criteria>
  <done>The owning entry is closed in place with its mechanism named; the four follow-ons are booked with owner, trigger and date.</done>
</task>

<task type="auto">
  <name>Task 3: VERSION, package.json and one CHANGELOG entry — one commit, cross-checked</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - VERSION and package.json (read the current version; do not assume it)
    - CHANGELOG.md (the newest entries, and how comparable behaviour-changing CI entries were versioned)
    - src/__tests__/critical-regressions.test.ts (the VERSION / package.json byte-equality arm)
  </read_first>
  <action>
Decide the bump on MEANING and say the reasoning in the SUMMARY: this phase changes which database a
CI job writes, splits a blocking gate into two jobs, changes when a gate measures the whole corpus,
and (D-F) changes what the lane builds — it replays migrations newer than the dump. Behaviour, not a
patch. Check the choice against how comparable entries were versioned.

Run the six-step mechanism in `<context>` in order. Themes this branch will have, as a starting point
and never a substitute for step 1: the baseline currency gate and the refreshed dumps; D-F's
carried-migrations marker and lane replay with ledger rows; `sql-tests` moved to a private database
and `test-db-drift` created; the runner's subset mode and its vacuity fence; the pins, runbook and
CLAUDE.md re-argued; the measurement document. Carry the known limits in this repo's voice: the
shared key still has three holders; the subset only narrows on a pull request that touches a gate
file; the restore path still judges currency by commit epochs; the AFTER measurement is outstanding
until the first merge-push run.

Commit VERSION, package.json and CHANGELOG.md together as `chore(release): vX.Y.Z.B — <name>`.
Verify byte equality and the absent trailing newline before committing.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const v=fs.readFileSync('VERSION','utf8');if(/\n$/.test(v))throw new Error('VERSION has a trailing newline');if(!/^\d+\.\d+\.\d+\.\d+$/.test(v))throw new Error('VERSION is not a four-digit string: '+JSON.stringify(v));const p=require('./package.json').version;if(p!==v)throw new Error('VERSION ('+v+') and package.json ('+p+') differ');const c=fs.readFileSync('CHANGELOG.md','utf8');if(!c.includes('['+v+']'))throw new Error('CHANGELOG.md has no entry for '+v);console.log('release pair intact: '+v)"</automated>
    <fails_when>node throws `VERSION has a trailing newline`, `VERSION is not a four-digit string`, `VERSION (…) and package.json (…) differ`, or `CHANGELOG.md has no entry for …`</fails_when>
  </verify>
  <acceptance_criteria>
    - the node check prints `release pair intact: <version>`
    - `npx vitest run src/__tests__/critical-regressions.test.ts` passes
    - the SUMMARY records the commit count from step 1 and a commit-to-bullet mapping with no unmapped commit
    - the release commit message contains no CI skip token in any form
  </acceptance_criteria>
  <done>VERSION and package.json are byte-equal four-digit strings, CHANGELOG.md carries one unified entry for that version, and every branch commit maps to a bullet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| branch history -> the release record | the CHANGELOG is the only durable statement of what shipped |
| a pending measurement -> a phase verdict | a phase could be called done before its own falsification ran |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-28 | Repudiation | an unrepresented commit | high | mitigate | The six-step mechanism with an explicit commit-to-bullet cross-check recorded in the SUMMARY. |
| T-164.4.2-29 | Repudiation | a phase closed before its falsification | high | mitigate | The AFTER obligation is a `<human-check>` with clauses (a)-(d) restated at the point of use. |
| T-164.4.2-30 | Tampering | CI suppressed by a skip token in the release commit | high | mitigate | No commit message writes one, in any form. |
| T-164.4.2-37 | Repudiation | grading the AFTER on the struck, unfalsifiable condition | high | mitigate | The AFTER section applies only the corrected clauses (a)-(d) and names `sql-tests`' zero acquire-wait as by-construction, not evidence. |
</threat_model>

<verification>
- All three task checks pass; `npx vitest run src/__tests__/critical-regressions.test.ts` passes.
- `npm run check:planning-hygiene` prints its OK line.
- The commit-to-bullet mapping is in the SUMMARY; the AFTER section names four jobs and the corrected clauses.
</verification>

<success_criteria>
The phase can ship, its backlog obligation is closed by the mechanism that closed it, and its claim
about CI time has a written protocol that the first merge-push run will either confirm or refute.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Consumer |
|---|---|---|
| MEASUREMENT.md `## AFTER` section | protocol | post-merge measurement, phase verifier |
| closed `[REDUNDER-SUBSET-SPLIT]` + four booked follow-ons | backlog | TODOS.md readers |
| VERSION / package.json / CHANGELOG.md entry | release | ship step, `-pr` branch filter |

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-10-SUMMARY.md` when done.
</output>
