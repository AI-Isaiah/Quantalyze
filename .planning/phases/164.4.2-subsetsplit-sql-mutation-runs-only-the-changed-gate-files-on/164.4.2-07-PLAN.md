---
phase: 164.4.2-subsetsplit
plan: "07"
type: execute
wave: 6
depends_on: ["164.4.2-06"]
files_modified:
  - scripts/classify-changed-paths.mjs
  - scripts/sql-gate-subset.mjs
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
autonomous: true
requirements: [AREA-D, REDUNDER-SUBSET-SPLIT]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "There is exactly one implementation of 'which files changed against the PR's merge base' in this repo, and the subset derivation is a second CALLER of it, not a second copy."
    - "An UNREADABLE diff base is a failure, never an empty change set."
    - "An EMPTY narrowed scope is never a pass: the runner refuses it and the derivation asks for the full corpus instead, with its reason printed."
    - "A narrowed run prints the scope it narrowed TO and the corpus size it narrowed FROM, every run, and is never recorded as full coverage."
    - "FILES_FLOOR / ARMS_FLOOR / WAIVED_CEILING keep their full-corpus meaning; a subset run never compares a narrowed tally against a lower-bound floor, and never moves one."
  artifacts:
    - scripts/sql-gate-subset.mjs
    - scripts/mutation-runner/run.mjs
  key_links:
    - "The diagnostic --file/--arm mode still cannot exit 0; the new subset mode is a DIFFERENT mode that can, and the two are distinguished by the printed scope: line as well as by the exit code."
    - "src/__tests__/mutation-runner-floors.test.ts gains the arm that catches the stale-low direction for the subset path — the direction the runner is blind to by construction."
---

<objective>
DECISION D's machinery: a genuine subset MODE for the mutation runner, a derivation that says
which gate files a PR changed, and the vacuity fence that makes a narrowed run readable as
exactly what it is.

Purpose: `sql-mutation` can only get slower, `timeout-minutes` is a DECLARED CEILING of 20 that
has spent its one permitted raise, and the subset rule IS the answer to a crossing. ⛔ The
tempting shortcut is dead on arrival and was verified: `run.mjs`'s existing narrowed mode
(`--file`/`--arm`) computes `exitCode: defects.length > 0 ? 1 : narrowed ? 2 : 0` and says of
itself "This mode never exits 0, so it can never be mistaken for a passing gate." A subset gate
must be able to PASS while preserving that property for the diagnostic mode — that tension is
the whole of Area D.

⛔ This plan writes NO workflow YAML. Plan 08 wires it. The runner change is inert until then:
CI keeps invoking the full corpus exactly as it does today.

Output: one derivation, one new runner mode, one `scope:` line, and floors that did not move.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-PATTERNS.md
@scripts/classify-changed-paths.mjs
@scripts/live-db-lane-corpus.mjs

**Read by symbol in `scripts/mutation-runner/run.mjs` before editing:** `runCorpus` (its named
parameters `filesFloor` / `armsFloor` / `waivedCeiling`, the `narrowed` flag, the target
filter, the `coverage:` / `arms:` / `biting:` / `lane-invocations:` prints, the floor block and
the returned `exitCode`); `parseArgs`-equivalent logic inside `main`; `scopeDirForFile`;
`logCorpusClassification`. ⛔ Read `FILES_FLOOR`, `ARMS_FLOOR` and `WAIVED_CEILING` BY SYMBOL
at execution time — this repo has a dated record of restated copies of those three going stale
four times, and no number for them appears in this plan on purpose.

**Structural fact that makes this tractable:** `runCorpus` scans the whole `scopeDir` into
`corpus` FIRST and only then filters `targets`. So a narrowed run already knows the full
corpus size — which is exactly the number CONTEXT requires it to print beside the narrowed one.

**Idioms to copy:** `scripts/classify-changed-paths.mjs` — three-dot merge-base diff with
`--no-renames` (MANDATORY, not stylistic: with rename detection a moved gate file prints only
its destination), argv-array `execFileSync` (the base ref is attacker-chosen on a fork PR), an
exception that is a `MEASURE_FAIL` and never "0 files", a pure `judge()` driven by a `CASES`
table whose terminal count is derived rather than typed. `scripts/live-db-lane-corpus.mjs` —
derive the corpus, never hand-list it, and catch the stale-low direction one layer up in a
vitest contract test.

⛔ Constraints:
- ⛔ Nothing here raises a timeout, lowers a floor, raises `WAIVED_CEILING`, or adds a waiver.
- ⛔ `PIPESTATUS` has zero prior usages in this repo — do not introduce it. Where a shell must
  count with `grep -c`, use `set +e; VAR=$(grep -ac …); rc=$?; set -e; [ "$rc" -le 1 ] || fail`.
- ⛔ Do NOT edit any file under `supabase/tests/`. Several independent censuses read that
  directory and a single edit can move more than one pin.
- ⛔ Never `git checkout --`, `git restore` or `git stash` in a neuter/restore cycle; `cp` byte
  backups verified with `cmp`, re-taken after every edit.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: one merge-base diff, two callers — export it and add the gate-file derivation</name>
  <files>scripts/classify-changed-paths.mjs, scripts/sql-gate-subset.mjs</files>
  <behavior>
    - event is not a pull request -> the derivation asks for the FULL corpus, naming the event, exactly as the docs filter already refuses to filter a push
    - the merge-base diff throws (unresolvable base ref) -> MEASURE_FAIL, non-zero exit; ⛔ never an empty set
    - the diff succeeds and no changed path is a gate file -> FULL corpus, with the reason and both counts printed
    - the diff succeeds and k paths are gate files -> SUBSET with those k names
    - a changed path that looks like a gate file but does not match the strict name pattern -> FULL corpus, naming it; a path the derivation cannot classify is never silently dropped
    - the emitted name list is order-stable and duplicate-free
  </behavior>
  <action>
In `scripts/classify-changed-paths.mjs`, lift the merge-base diff out of `main()` into an
exported function (name it for what it returns, e.g. `changedFilesAgainstBase()`), keeping its
behaviour byte-identical: the same three-dot form, the same `--no-renames`, the same argv-array
call, the same base-ref resolution from `GITHUB_BASE_REF`, and the same
`MEASURE_FAIL`-and-throw on failure. `main()` then calls it. ⛔ Nothing about the docs-only
verdict changes — this is an extraction, and its own self-test must still pass unchanged
except for one added arm proving the extracted function throws rather than returning `[]` when
the base ref cannot be read.

Write `scripts/sql-gate-subset.mjs` as the second CALLER, in the library+`--self-test` shape:
`export const GATE_FILE_RE` (a strict allow-list anchored on `supabase/tests/test_<name>.sql`
with a conservative name character class), a pure `export function judge({event, changedFiles})`
returning `{mode: "full"|"subset", files, reason}`, `EXPECTED_ASSERTIONS`, `invokedDirectly()`,
and a `CASES` table covering every arm in `<behavior>`.

⭐ The strict pattern is a SECURITY control, not tidiness (RESEARCH, V5): on a fork PR the
author chooses the filenames, and plan 08 hands this list to a shell step. A name that cannot
contain a shell metacharacter by construction is the mitigation; anything that fails the
pattern forces the FULL corpus rather than being dropped.

`main()` prints one always-on human line and, when `GITHUB_OUTPUT` is present, appends the two
outputs plan 08 consumes (a mode flag and the space-separated name list) — the `emit()` shape
`classify-changed-paths.mjs` already uses, so a bare local run still works.

⛔ Observe RED first, per arm family: neuter the throw-on-unreadable-base path so it returns an
empty array, run `--self-test`, watch the "unreadable is not empty" arm fail, record the
message verbatim, restore from the byte backup and `cmp`-verify.
  </action>
  <verify>
    <automated>node scripts/classify-changed-paths.mjs --self-test && node scripts/sql-gate-subset.mjs --self-test</automated>
  </verify>
  <done>Both self-tests pass at their pinned counts; the diff mechanism exists once and is imported by the new derivation; the unreadable-base arm was observed RED and recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: run.mjs gains a subset mode that CAN pass — and says so on every run</name>
  <files>scripts/mutation-runner/run.mjs</files>
  <behavior>
    - a clean subset run over k annotated files exits 0 (it is a gate, not a diagnostic)
    - a subset run with a defect exits 1, unchanged
    - `--file` / `--arm` still exit 2 when clean — the diagnostic mode's property is preserved exactly
    - the subset list file is missing or unreadable -> non-zero exit, MEASURE_FAIL; never an empty subset
    - the subset list is empty -> non-zero exit; an empty narrowed scope is not a pass
    - a listed path failing the strict gate-file pattern -> non-zero exit, naming the path
    - listed files that exist but carry no annotations -> the run does NOT narrow: it runs the FULL corpus with every floor enforced, and says which listed files were unannotated
    - every run prints exactly one `scope:` line; the count it claims equals the number of file names it prints beside it
    - in subset mode FILES_FLOOR and WAIVED_CEILING are still enforced (both are computed over the full scan / bounded from above); ARMS_FLOOR is NOT compared, and the line says where it IS enforced
    - `--subset-from` refuses to combine with `--file`, `--arm` or `--parse-only`
  </behavior>
  <action>
Add `--subset-from <path>` to `main()` and a `subsetFiles` named parameter to `runCorpus`.
`scopeDir` is NEVER changed by this flag — the full corpus must still be scanned, because the
corpus size it narrowed FROM is half of what CONTEXT requires the run to print.

Narrow `targets` by intersecting the listed basenames with `corpus.annotatedFiles`, in the same
place the `onlyFile` filter already does its work. ⛔ `narrowed` stays FALSE for this mode: it
is a distinct state, not a re-use of the diagnostic flag, and the diagnostic mode's `never
exits 0` property must be provably untouched.

Floors, exactly as CONTEXT binds them:
  * `corpus.filesAnnotated` comes from the full scan, so FILES_FLOOR keeps its meaning and stays
    enforced;
  * `armsWaived` over a subset is bounded above by the full-corpus count, so WAIVED_CEILING
    stays enforced (a subset that exceeds it is a real violation);
  * `bitingArms` over a subset is NOT comparable to a lower-bound floor, so ARMS_FLOOR is not
    compared — and the run PRINTS that it was not, and where it is. ⛔ Never renormalise a floor
    to a narrowed denominator and never pass a lowered floor in from the caller.

Print exactly one `scope:` line in every mode — FULL, SUBSET, FULL-after-a-subset-fallback, and
DIAGNOSTIC — each naming the numbers behind it, with the SUBSET form listing the file names so
the claimed count and the printed names can be cross-checked by the caller. ⛔ A narrowed run
must never describe itself as full coverage, in this line or in the closing verdict text.

Extend the runner's `--self-test` with arms for the `<behavior>` list, including the one that
matters most: a clean subset run exits 0 AND a clean `--file` run still exits 2, asserted in the
same arm so a future edit cannot satisfy one by breaking the other.

⛔ Observe RED: neuter the empty-subset refusal so an empty list runs clean, watch the arm fail,
record the message verbatim, restore, `cmp`-verify. Then re-run the FULL corpus once
(`node scripts/mutation-runner/run.mjs`) and confirm its printed numbers and exit status are
unchanged from before this task — the full path must be byte-for-byte the same gate it was.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --self-test</automated>
  </verify>
  <done>The runner has a subset mode that can exit 0, a diagnostic mode that still cannot, a `scope:` line on every run, and floors whose meaning did not move; a full-corpus run produces the same verdict it did before.</done>
</task>

<task type="auto">
  <name>Task 3: the floors ratchet grows the arm that watches the subset path</name>
  <files>src/__tests__/mutation-runner-floors.test.ts</files>
  <action>
The ROADMAP asks this question directly, so answer it with a test rather than a sentence: the
vitest ratchet exists to catch the STALE-LOW direction the runner is blind to by construction,
and the subset path adds a second way to be blind.

Add arms that drive `runCorpus` directly (the file already imports it and the floor constants):
  * a subset run over a fixture scope exits 0 when clean, while the same scope under `--file`
    exits 2 — the two modes are separated by assertion, not by comment;
  * an empty subset cannot produce a zero exit;
  * a subset run raises no ARMS_FLOOR-shaped floor defect even though its biting count is far
    below the constant — and a FULL run over the same fixture with an inflated floor still
    does, so the check is shown live rather than assumed absent;
  * FILES_FLOOR and WAIVED_CEILING still fire in subset mode when violated.

⛔ Every arm must be observed RED before it is trusted: neuter the behaviour it claims to pin,
watch that arm (and only that arm) fail, record the message verbatim, restore from a
`cmp`-verified byte backup. An arm that cannot fail is worse than no arm.
⛔ Do not touch the existing floor constants or any existing arm's expectations.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/mutation-runner-floors.test.ts</automated>
  </verify>
  <done>The ratchet covers the subset path in both directions; every new arm was observed RED; no existing arm or constant changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork-PR author -> `GITHUB_BASE_REF` and changed filenames | both are attacker-chosen inputs to the derivation |
| a narrowed run -> the record of what was checked | a subset that reads as a full pass would hide an unchecked gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-20 | Tampering | attacker-chosen base ref or filename reaching a shell | high | mitigate | argv-array `execFileSync` for the diff (inherited unchanged from the extraction); a strict anchored name pattern on every emitted path, with a non-conforming path forcing the FULL corpus rather than being dropped. |
| T-164.4.2-21 | Repudiation | a narrowed run recorded as full coverage | critical | mitigate | Exactly one `scope:` line per run naming both numbers and the narrowed file names; the closing verdict text distinguishes narrowed from full; plan 08 makes the line's absence a CI failure. |
| T-164.4.2-22 | Spoofing | an empty or unreadable change set passing as clean | critical | mitigate | Unreadable diff base throws; empty subset list exits non-zero; zero gate files resolves to the FULL corpus with its reason printed. Three distinct arms, each observed RED. |
| T-164.4.2-23 | Tampering | a floor silently renormalised to the narrowed denominator | critical | mitigate | FILES_FLOOR and WAIVED_CEILING keep full-corpus semantics and stay enforced; ARMS_FLOOR is not compared in subset mode and the run says so; the vitest ratchet pins both directions. |
</threat_model>

<verification>
- `node scripts/classify-changed-paths.mjs --self-test`, `node scripts/sql-gate-subset.mjs --self-test`, `node scripts/mutation-runner/run.mjs --self-test` and `npx vitest run src/__tests__/mutation-runner-floors.test.ts` all pass.
- A FULL `node scripts/mutation-runner/run.mjs` run produces the same coverage / arms / biting / lane-invocations numbers and the same exit status as before this plan — recorded before and after in the SUMMARY.
- `node scripts/lint-sql-gates.mjs --self-test` and `node scripts/lint-app-guc.mjs --self-test` still pass (nothing under `supabase/tests/` was touched).
- Every RED observation is recorded verbatim.
</verification>

<success_criteria>
`sql-mutation` has a mode that can honestly pass on a narrowed corpus, a derivation that cannot
confuse "nothing changed" with "I could not tell", and floors that mean exactly what they meant
before.
</success_criteria>

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-07-SUMMARY.md` when done.
</output>
