---
phase: 164.4.2-subsetsplit
plan: "07"
type: execute
wave: 5
depends_on: ["164.4.2-05"]
files_modified:
  - scripts/mutation-runner/run.mjs
  - scripts/classify-changed-paths.mjs
  - scripts/sql-gate-subset.mjs
  - src/__tests__/mutation-runner-floors.test.ts
autonomous: true
requirements: [AREA-D, REDUNDER-SUBSET-SPLIT]
coupling_justified: ["164.4.2-06: file-disjoint; the only shared resource is host CPU (plan 06's Docker lane vs this plan's pg-lane clusters), and both plans serialize their DB runs through the host lock described in <context>, so neither observes the other's load as a verdict"]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-D (DECISION D): the mutation runner has a subset mode that CAN exit 0 on a clean narrowed run, while the diagnostic --file/--arm mode still cannot exit 0."
    - "Every runner run prints exactly one `scope:` line naming what it covered and the full-corpus size it narrowed FROM; a narrowed run never describes itself as full coverage."
    - "There is exactly one implementation of 'which files changed against the PR's merge base'; the gate-file derivation is a second CALLER of it."
    - "An UNREADABLE diff base is a failure, never an empty change set; an EMPTY narrowed scope is never a pass."
    - "Every subset that is empty or cannot be matched resolves to FULL with a named reason or fails loud by name: a changed gate path absent from the checkout (deleted or moved away) forces FULL in the derivation; a listed path absent on disk is a runner MEASURE_FAIL; a list with no annotated file, or with any listed file unannotated, runs FULL with the fallback reason naming them — never a clean SUBSET."
    - "FILES_FLOOR / ARMS_FLOOR / WAIVED_CEILING keep their full-corpus meaning; a subset run never compares a narrowed tally against a lower-bound floor, and never moves one."
  artifacts:
    - scripts/sql-gate-subset.mjs
    - scripts/mutation-runner/run.mjs
  key_links:
    - "runCorpus scans the FULL scopeDir before filtering targets, so the corpus size a subset narrowed FROM is always known; --subset-from never changes scopeDir."
    - "src/__tests__/mutation-runner-floors.test.ts carries the arm for the stale-low direction on the subset path — the direction the runner is blind to by construction."
---

<objective>
D-D's machinery (DECISION D): a genuine subset MODE for the mutation runner, a derivation that says
which gate files a PR changed, and the vacuity fence that makes a narrowed run readable as exactly
what it is.

Purpose: `sql-mutation` can only get slower, its `timeout-minutes` is a DECLARED CEILING of 20
that has spent its one permitted raise, and the subset rule IS the answer to a crossing. The
runner's existing narrowed mode computes `exitCode: defects.length > 0 ? 1 : narrowed ? 2 : 0` and
says of itself that it never exits 0 — a subset GATE must be able to pass while that property is
preserved for the diagnostic mode. That tension is the whole of Area D.

⭐ Sequencing: this plan is file-disjoint from plan 06 (DECISION F) and relies on nothing about the
local-stack lane — the mutation runner uses its own throwaway `scripts/pg-lane/run.sh` cluster — so
it runs in the SAME wave as plan 06 rather than behind it. ⛔ It writes NO workflow YAML; plan 09
wires it, so CI keeps invoking the full corpus exactly as today until then.

Output: one runner subset mode, one `scope:` line, one derivation, and floors that did not move.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-PATTERNS.md
@scripts/classify-changed-paths.mjs
@scripts/live-db-lane-corpus.mjs

**Read by symbol in `scripts/mutation-runner/run.mjs` before editing:** `runCorpus` (its named
parameters `filesFloor` / `armsFloor` / `waivedCeiling`, the `narrowed` flag, the target filter,
the `coverage:` / `arms:` / `biting:` / `lane-invocations:` prints, the floor block and the returned
`exitCode`); the argument handling inside `main`; `scopeDirForFile`; `logCorpusClassification`.
⛔ Read `FILES_FLOOR`, `ARMS_FLOOR` and `WAIVED_CEILING` BY SYMBOL at execution time — this repo has
a dated record of restated copies of those three going stale four times, and no number for them
appears in this plan on purpose.

**Idioms to copy:** `scripts/classify-changed-paths.mjs` — three-dot merge-base diff with
`--no-renames` (MANDATORY: with rename detection a moved gate file prints only its destination),
argv-array `execFileSync` (the base ref is attacker-chosen on a fork PR), an exception that is a
`MEASURE_FAIL` and never "0 files", a pure `judge()` driven by a `CASES` table whose terminal count
is derived rather than typed. `scripts/live-db-lane-corpus.mjs` — derive the corpus, never
hand-list it, and catch the stale-low direction one layer up in a vitest contract test.

**`scope:` line forms (this plan defines them; plan 09's assert step parses them):**
`scope: FULL <N>/<N> annotated files`, `scope: FULL <N>/<N> annotated files (subset fallback: <reason>)`,
`scope: SUBSET <k>/<N> annotated files: <basename> <basename> …`, `scope: DIAGNOSTIC <file-or-arm>`.

⛔ Constraints:
- ⛔ Nothing here raises a timeout, lowers a floor, raises `WAIVED_CEILING`, or adds a waiver.
- ⛔ `PIPESTATUS` has zero prior usages in this repo — do not introduce it. Where a shell must count
  with `grep -c`, use `set +e; VAR=$(grep -ac …); rc=$?; set -e; [ "$rc" -le 1 ] || fail`.
- ⛔ Do NOT edit any file under `supabase/tests/`. Several independent censuses read that directory
  and a single edit can move more than one pin.
- ⛔ Never `git checkout --`, `git restore` or `git stash` in a neuter/restore cycle; `cp` byte
  backups verified with `cmp`, re-taken after every edit.
- ⚠️ Do not run the full corpus while another agent is running one on the same host; the pg-lane
  clusters are CPU-heavy and a timeout-shaped failure under load is not a verdict.
- ⛔ HOST COUPLING WITH PLAN 06 (same wave, declared in `coupling_justified`): plan 06 boots the Docker
  Supabase lane on this host while this plan runs pg-lane clusters. Hold a host-wide lock around
  EVERY runner invocation that starts a cluster (the Task 1 tracer, the FULL before/after runs, the
  `--file` run and any self-test arm that boots a lane): `mkdir` of `gsd-164.4.2-dbrun.lock` inside
  the directory `git rev-parse --git-common-dir` prints (shared by every worktree of this checkout),
  retrying every 15 s until it succeeds, removed by an EXIT trap. Plan 06 takes the same lock. Record
  in the SUMMARY how long each run waited; a timeout-shaped failure is re-run under the lock, never
  recorded as a verdict.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): one real gate file mutated through --subset-from, exiting 0, with its scope: line</name>
  <files>scripts/mutation-runner/run.mjs</files>
  <read_first>
    - scripts/mutation-runner/run.mjs (runCorpus, main's argument handling, the floor block, the closing verdict text, the existing --self-test)
    - src/__tests__/mutation-runner-floors.test.ts (how it imports runCorpus and the floor constants)
  </read_first>
  <action>
Per D-D, add `--subset-from <path>` to `main()` and a `subsetFiles` named parameter to `runCorpus`.
`scopeDir` is NEVER changed by this flag — the full corpus is still scanned, because the corpus size
it narrowed FROM is half of what CONTEXT requires the run to print.

Narrow `targets` by intersecting the listed basenames with `corpus.annotatedFiles`, in the same
place the `onlyFile` filter already does its work. ⛔ `narrowed` stays FALSE for this mode: it is a
distinct state, not a re-use of the diagnostic flag, and the diagnostic mode's never-exits-0
property must be provably untouched.

Floors, exactly as CONTEXT binds them: FILES_FLOOR stays enforced (its numerator comes from the full
scan); WAIVED_CEILING stays enforced (a subset's waived count is bounded above by the full count);
ARMS_FLOOR is NOT compared on a subset, and the run PRINTS that it was not and that the full-corpus
push run is where it is. ⛔ Never renormalise a floor to a narrowed denominator and never accept a
lowered floor from the caller.

Print exactly one `scope:` line in EVERY mode, in the forms `<context>` fixes. ⛔ The closing verdict
text of a SUBSET run must not say or imply the whole corpus was covered.

This task wires the happy path only: a readable list of annotated gate files narrows and can exit 0.
Task 3 adds the refusals.
  </action>
  <verify>
    <automated>bash -c 'set -eu; T=$(mktemp -d); F=$(grep -l "RED-UNDER-M:" supabase/tests/test_*.sql | head -1); printf "%s\n" "$F" > "$T/subset.txt"; set +e; node scripts/mutation-runner/run.mjs --subset-from "$T/subset.txt" > "$T/out.log" 2>&1; rc=$?; set -e; test "$rc" = 0 || { tail -40 "$T/out.log"; exit 1; }; test "$(grep -c "^scope: " "$T/out.log")" = 1; grep -q "^scope: SUBSET 1/[0-9]* annotated files: $(basename "$F")$" "$T/out.log"; echo "subset tracer: 1 file, exit 0, one scope line"'</automated>
    <fails_when>the subset run exits non-zero (last 40 log lines printed), prints zero or more than one `scope:` line, or its `scope: SUBSET 1/N` line does not name the listed file</fails_when>
  </verify>
  <acceptance_criteria>
    - the tracer command prints `subset tracer: 1 file, exit 0, one scope line`
    - a FULL `node scripts/mutation-runner/run.mjs` run prints `scope: FULL` and its coverage / arms / biting / lane-invocations numbers and exit status equal the pre-change run's; both readings are recorded verbatim in the SUMMARY
    - a `--file <that file>` run on a clean file still exits 2 and prints `scope: DIAGNOSTIC`
  </acceptance_criteria>
  <done>A narrowed run over one real annotated gate file exits 0 and says exactly what it covered; the full run is the gate it was; the diagnostic mode still cannot pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: one merge-base diff, two callers — export it and add the gate-file derivation</name>
  <files>scripts/classify-changed-paths.mjs, scripts/sql-gate-subset.mjs</files>
  <read_first>
    - scripts/classify-changed-paths.mjs (main, its diff invocation, emit, judge, CASES, --self-test)
    - scripts/live-db-lane-corpus.mjs (library + --self-test shape, invokedDirectly)
  </read_first>
  <behavior>
    - event is not a pull request -> the derivation asks for the FULL corpus, naming the event
    - the merge-base diff throws (unresolvable base ref) -> MEASURE_FAIL, non-zero exit; never an empty set
    - the diff succeeds and no changed path is a gate file -> FULL corpus, with the reason and both counts printed
    - the diff succeeds and k paths are gate files -> SUBSET with those k names
    - a changed path under supabase/tests/ that looks like a gate file but fails the strict name pattern -> FULL corpus, naming it; never silently dropped
    - a changed gate-file path that is ABSENT from the checkout (deleted by the PR, or the source side of a move, which `--no-renames` lists as a delete) -> FULL corpus, the reason naming it as deleted-or-moved; never a SUBSET that omits it and never a SUBSET listing a nonexistent file
    - the emitted name list is order-stable and duplicate-free
  </behavior>
  <action>
In `scripts/classify-changed-paths.mjs`, lift the merge-base diff out of `main()` into an exported
function named for what it returns (e.g. `changedFilesAgainstBase()`), keeping its behaviour
byte-identical: the same three-dot form, the same `--no-renames`, the same argv-array call, the same
base-ref resolution from `GITHUB_BASE_REF`, and the same `MEASURE_FAIL`-and-throw on failure.
`main()` then calls it. ⛔ Nothing about the docs-only verdict changes; its self-test must still
pass unchanged except for one added arm proving the extracted function throws rather than returning
`[]` when the base ref cannot be read.

Write `scripts/sql-gate-subset.mjs` as the second CALLER, in the library + `--self-test` shape:
`export const GATE_FILE_RE` (a strict allow-list anchored on `supabase/tests/test_<name>.sql` with a
conservative name character class), a pure `export function judge({event, changedFiles, presentFiles})`
returning `{mode: "full"|"subset", files, reason}` — `presentFiles` is the set of changed gate paths
that exist in the checkout, computed by `main()` with `existsSync` so `judge` stays pure and the
deleted-or-moved row is table-driven — `EXPECTED_ASSERTIONS`, `invokedDirectly()`, and a `CASES`
table covering every `<behavior>` arm.

⭐ The strict pattern is a SECURITY control (RESEARCH V5): on a fork PR the author chooses filenames,
and plan 09 hands this list to a shell step. A name that cannot contain a shell metacharacter by
construction is the mitigation; anything failing the pattern forces the FULL corpus.

`main()` prints one always-on human line and, when `GITHUB_OUTPUT` is present, appends the two
outputs plan 09 consumes (a mode flag and the space-separated name list) — the `emit()` shape
`classify-changed-paths.mjs` already uses, so a bare local run still works.

⛔ Observe RED first: neuter the throw-on-unreadable-base path so it returns an empty array, run
`--self-test`, watch the "unreadable is not empty" arm fail, record the message verbatim, restore
from the byte backup and `cmp`-verify. Repeat for the deleted-or-moved row: make `judge` ignore
`presentFiles`, watch that arm fail, record, restore, `cmp`-verify.
  </action>
  <verify>
    <automated>node scripts/classify-changed-paths.mjs --self-test && node scripts/sql-gate-subset.mjs --self-test</automated>
    <fails_when>either self-test prints a FAIL line or exits non-zero — including an assertion-count mismatch against its EXPECTED_ASSERTIONS</fails_when>
  </verify>
  <acceptance_criteria>
    - both self-tests exit 0
    - `grep -c "changedFilesAgainstBase\|export function changed" scripts/classify-changed-paths.mjs` is at least 1 and `scripts/sql-gate-subset.mjs` imports the exported diff function rather than spawning its own diff (`grep -c "\"diff\"" scripts/sql-gate-subset.mjs` prints 0)
    - the SUMMARY records the unreadable-base and the deleted-or-moved RED messages verbatim
  </acceptance_criteria>
  <done>The diff mechanism exists once and is imported by the derivation; both self-tests pass; the unreadable-base arm was observed RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the runner's subset refusals, and the floors ratchet's subset arms</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-runner-floors.test.ts</files>
  <read_first>
    - scripts/mutation-runner/run.mjs (as left by Task 1)
    - src/__tests__/mutation-runner-floors.test.ts (every existing arm and the floor constants it imports)
  </read_first>
  <behavior>
    - a clean subset run exits 0; a subset run with a defect exits 1; `--file`/`--arm` still exit 2 when clean
    - the subset list file is missing or unreadable -> non-zero exit, MEASURE_FAIL; never an empty subset
    - the subset list is empty -> non-zero exit
    - a listed path failing the strict gate-file pattern -> non-zero exit, naming the path
    - a listed path that does not exist on disk -> non-zero exit, MEASURE_FAIL naming it (the derivation already forces FULL for a deleted or moved gate file, so a nonexistent path reaching the runner is a broken contract, never something to skip)
    - no listed file is annotated (the overlap with `corpus.annotatedFiles` is EMPTY) -> the run does NOT narrow: it runs the FULL corpus with every floor enforced, and the `scope:` line's fallback reason names the unannotated files
    - SOME listed files are annotated and at least one existing listed file is not -> also FULL with the fallback reason naming the unannotated ones; a SUBSET is emitted only when EVERY listed file is annotated, so no listed file is ever silently left unmutated
    - `--subset-from` refuses to combine with `--file`, `--arm` or `--parse-only`
    - floors ratchet: a subset run raises no ARMS_FLOOR-shaped defect though its biting count is far below the constant, while a FULL run over the same fixture with an inflated floor still does; FILES_FLOOR and WAIVED_CEILING still fire in subset mode when violated
  </behavior>
  <action>
Add the runner refusals `<behavior>` lists, each a named `MEASURE_FAIL` line and a non-zero exit, and
the two FULL-fallback rows (empty overlap; mixed annotated/unannotated), each printing
`scope: FULL <N>/<N> annotated files (subset fallback: <reason>)` with the unannotated names in the
reason, and extend the runner's `--self-test` with an arm per row — including one arm asserting BOTH that a
clean subset run exits 0 AND that a clean `--file` run still exits 2, so a future edit cannot
satisfy one by breaking the other.

In `mutation-runner-floors.test.ts`, add arms that drive `runCorpus` directly over a fixture scope
for the floors rows in `<behavior>`. ⛔ Do not touch the existing floor constants or any existing
arm's expectations.

⛔ Every new arm must be observed RED before it is trusted: neuter the behaviour it pins (for the
runner, make the empty-subset refusal run clean, and separately make the mixed annotated/unannotated
row narrow to the annotated files instead of falling back), watch that arm and only that arm fail, record the
message verbatim, restore from a `cmp`-verified byte backup. An arm that cannot fail is worse than
no arm.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts</automated>
    <fails_when>the runner self-test exits non-zero or prints a FAIL line, or vitest reports a failed arm in mutation-runner-floors.test.ts</fails_when>
  </verify>
  <acceptance_criteria>
    - both commands exit 0
    - the three floor constants are unchanged: `grep -E '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs` prints the same three lines as the same grep over `git show <plan-base-sha>:scripts/mutation-runner/run.mjs` (the plan's base SHA recorded at start; both outputs quoted in the SUMMARY)
    - `node scripts/lint-sql-gates.mjs --self-test` and `node scripts/lint-app-guc.mjs --self-test` still pass
    - every new arm's RED message is recorded verbatim in the SUMMARY
  </acceptance_criteria>
  <done>The runner refuses every vacuous subset shape by name, the ratchet covers the subset path in both directions, and no floor, ceiling or existing arm moved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fork-PR author -> `GITHUB_BASE_REF` and changed filenames | both are attacker-chosen inputs to the derivation |
| a narrowed run -> the record of what was checked | a subset that reads as a full pass would hide an unchecked gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-20 | Tampering | attacker-chosen base ref or filename reaching a shell | high | mitigate | argv-array `execFileSync` for the diff (inherited unchanged by the extraction); strict anchored name pattern on every emitted path, a non-conforming path forcing the FULL corpus. |
| T-164.4.2-21 | Repudiation | a narrowed run recorded as full coverage | critical | mitigate | Exactly one `scope:` line per run naming both numbers and the narrowed names; closing verdict text distinguishes narrowed from full; plan 09 makes the line's absence a CI failure. |
| T-164.4.2-22 | Spoofing | an empty or unreadable change set passing as clean | critical | mitigate | Unreadable diff base throws; empty subset list exits non-zero; zero gate files resolves to FULL with its reason printed; a deleted or moved gate path forces FULL in the derivation and a nonexistent listed path is a runner MEASURE_FAIL; an empty or partial annotated overlap falls back to FULL naming the unannotated files. Each arm observed RED. |
| T-164.4.2-23 | Tampering | a floor silently renormalised to the narrowed denominator | critical | mitigate | FILES_FLOOR and WAIVED_CEILING keep full-corpus semantics and stay enforced; ARMS_FLOOR is not compared on a subset and the run says so; the vitest ratchet pins both directions. |
</threat_model>

<verification>
- The Task 1 tracer, both derivation self-tests, the runner self-test and the floors vitest file pass.
- A FULL runner run's numbers and exit status are unchanged from before this plan, recorded before and after in the SUMMARY.
- `node scripts/lint-sql-gates.mjs --self-test` and `node scripts/lint-app-guc.mjs --self-test` still pass.
- `npm run check:planning-hygiene` prints its OK line.
</verification>

<success_criteria>
`sql-mutation` has a mode that can honestly pass on a narrowed corpus, a derivation that cannot
confuse "nothing changed" with "I could not tell", and floors that mean exactly what they meant.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Consumer |
|---|---|---|
| `run.mjs --subset-from` + the `scope:` line | runner mode | plan 09's `sql-mutation` step and its assert step |
| `changedFilesAgainstBase()` exported from `classify-changed-paths.mjs` | shared function | `sql-gate-subset.mjs`, `classify-changed-paths.mjs` main |
| `scripts/sql-gate-subset.mjs` | new derivation | plan 09's `changed-paths` job |
| subset arms in `mutation-runner-floors.test.ts` | ratchet | vitest |

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-07-SUMMARY.md` when done.
</output>
