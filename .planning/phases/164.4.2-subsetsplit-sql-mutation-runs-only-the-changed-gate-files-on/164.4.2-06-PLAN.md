---
phase: 164.4.2-subsetsplit
plan: "06"
type: execute
wave: 5
depends_on: ["164.4.2-05"]
files_modified:
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/contracts/ci-docs-path-filter.contract.test.ts
  - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts
  - docs/runbooks/shared-test-db-mutex.md
autonomous: true
requirements: [AREA-B]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every pin that asserted the OLD sql-tests shape now asserts the new one, and each still fails when its subject is removed."
    - "The serialization pin still guards the jobs that really do share the key, and gains the new holder rather than shrinking to two."
    - "The mutex runbook describes the holders that exist, with the earlier three-holder statements kept as dated lineage rather than silently rewritten."
  artifacts:
    - src/__tests__/critical-regressions.test.ts
    - docs/runbooks/shared-test-db-mutex.md
  key_links:
    - "A pin re-argued is re-observed RED: neuter, watch it fail for its own reason, restore from a cmp-verified byte backup."
    - "The green that closes this plan is bound to a head SHA and a run id, with the checks COUNTED — not read off a board that merely shows no red."
---

<objective>
Close the pins plan 05 deliberately left RED, by re-arguing each one for the shape that now
exists — never by deleting an assertion or relaxing it to fit.

Purpose: `sql-tests`'s old shape is pinned in at least three contract tests and a runbook.
Those pins exist because someone previously watched a blocking gate decay into a
present-but-ungating one. ⛔ The cheapest way through this plan — delete the row, narrow the
matcher, drop the arm — is exactly the failure they were written to prevent. Each pin gets a
new subject and a re-observed RED.

Output: a green suite and a green CI board that has been COUNTED, not glanced at.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-CONTEXT.md
@src/__tests__/critical-regressions.test.ts
@src/__tests__/contracts/ci-docs-path-filter.contract.test.ts
@docs/runbooks/shared-test-db-mutex.md

**The pins, located by symbol:**
- `critical-regressions.test.ts` — the `DB_JOBS` constant and the arm asserting the three
  DB-touching jobs are serialized against the shared key (Phase 142.1 D-05 intent); and the arm
  requiring `sql-tests` to appear in BOTH the aggregator's `needs:` and its result loop.
- `contracts/ci-docs-path-filter.contract.test.ts` — the filtered-job list containing
  `sql-tests`, and the FORK pull-request arm asserting `sql-tests` is among the rows that skip
  on a fork.
- `contracts/ci-anti-skip-gate.contract.test.ts` — the F10 pin over the `sql-tests` step's
  static accounting and the LANE-ONLY mechanism. The step body was kept verbatim by plan 05, so
  this one is expected to stay green; it is in scope to be RE-VERIFIED and re-anchored if it
  locates its subject by anything the move changed.

⛔ Constraints:
- ⛔ Never `git checkout -- <path>`, `git restore` or `git stash`. Byte-backup with `cp`,
  restore with `cp`, verify with `cmp`, and RE-TAKE the backup after every edit.
- ⛔ A test that can no longer fail is worse than no test. Every arm touched here is neutered,
  observed RED for its own reason, and restored — with the RED message recorded verbatim.
- ⛔ This plan does not touch `.github/workflows/ci.yml`. If a pin cannot be satisfied without
  a workflow edit, STOP and report it rather than reaching into plan 05's file.
- ⛔ `CLAUDE.md` carries sentences this phase makes stale (the key-occurrence census and the
  three-holder framing). Do NOT edit it here — list the exact sentences in the SUMMARY so the
  ship step can put them in front of the founder.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the serialization pin guards the jobs that actually share the key</name>
  <files>src/__tests__/critical-regressions.test.ts</files>
  <behavior>
    - the serialization arm still FAILS if a job that holds key 61616158 is missing its acquire step
    - the arm's job set names `python`, `e2e-seeded` and `test-db-drift`
    - the arm FAILS if `sql-tests` re-acquires the key — the phase's own invariant, asserted rather than assumed
    - the aggregator arms still FAIL if `sql-tests` is dropped from `needs:` or from the result loop
    - an equivalent pair of aggregator arms exists for `test-db-drift`
  </behavior>
  <action>
Re-point the `DB_JOBS` serialization arm at the jobs that hold the key after plan 05, and add
the inverse assertion in the same arm: `sql-tests` must NOT carry an acquire step. The D-05
intent — the shared database is written by one job at a time — is unchanged; what changed is
which jobs can write it, and the arm should say so in its own message so a future reader sees
a decision rather than a shrunken list.

Add the aggregator `needs:`-and-result-loop pair for `test-db-drift`, mirroring the existing
`sql-tests` pair and carrying the same argument: `needs:` alone makes the aggregator WAIT for a
job; only the result row makes it JUDGE one.

⛔ Observe each arm RED before trusting it: for the serialization arm, temporarily remove an
acquire step from the workflow COPY the test reads (or drive the test's own parser with a
fixture, if that is how the file already isolates itself — follow the file's existing idiom),
watch it fail naming that job, restore, `cmp`-verify. Record every RED message verbatim.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts</automated>
  </verify>
  <done>The serialization arm names the three current holders and fails if `sql-tests` re-acquires; both aggregator pairs pass; every touched arm was observed RED for its own reason.</done>
</task>

<task type="auto">
  <name>Task 2: the docs-path-filter and anti-skip contracts describe the jobs that exist</name>
  <files>src/__tests__/contracts/ci-docs-path-filter.contract.test.ts, src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</files>
  <action>
`ci-docs-path-filter.contract.test.ts`: `sql-tests` stays in the filtered-job list — it is still
gated on `docs_only` — but it must leave the FORK pull-request skip row, because it no longer
carries the fork-author `if:` that put it there. Move `test-db-drift` into that row instead:
that job holds the secret and the fork gate now, so the aggregator tolerance the row exercises
belongs to it. Keep the row's own argument intact; only its subject changes.

`ci-anti-skip-gate.contract.test.ts`: RE-RUN it first and read the result before editing
anything. Plan 05 kept the `sql-tests` step body verbatim, so the F10 pin should still hold. If
it fails, it is because it anchors on something the move changed (the step's position, the job's
`env:` block, the DSN variable name) — re-anchor on the step's NAME and its static accounting,
never by loosening what it asserts about the accounting itself.

⛔ Do not convert a failing assertion into a skipped one, a `.todo`, or a widened matcher.
Observe RED, fix the subject, restore from a `cmp`-verified byte backup if an experiment needs
undoing.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/ci-docs-path-filter.contract.test.ts src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts</automated>
  </verify>
  <done>Both contract files pass; the fork-skip row is asserted about the job that can actually be skipped by a fork; no assertion was deleted, skipped or widened.</done>
</task>

<task type="auto">
  <name>Task 3: runbook currency, and a COUNTED green board</name>
  <files>docs/runbooks/shared-test-db-mutex.md</files>
  <action>
Go through every `sql-tests` mention in `docs/runbooks/shared-test-db-mutex.md` and classify it
as one of three things, then act accordingly:
  * a LIVE statement about who holds the key or which job to look at during an incident —
    correct it to name `python`, `e2e-seeded` and `test-db-drift`;
  * a DATED historical reading (the Phase 158 three-holder framing, the ~20 min-of-lock-time
    arithmetic, the starvation incident narrative) — keep it, and mark it as lineage with the
    date and what superseded it. ⛔ Rewriting history to match the present is how a runbook
    stops being evidence;
  * a statement about the corpus itself rather than the lock — leave it alone.

Add one short section at the top of the holders discussion recording what this phase changed:
one holder removed, the corpus moved to a private database, cross-run contention reduced but
NOT eliminated, and the pointer to the measurement that will say by how much.

Then take the green, and COUNT it rather than reading a board:
  * `gh api "repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<sha>" -q '.workflow_runs | length'`
    must be non-zero — an empty count satisfies a numeric comparison and proves nothing;
  * list the checks for that SHA and confirm `sql-tests`, `test-db-drift`, `frontend-local-stack`,
    `frontend-live-db-lane`, `sql-mutation` and `frontend` all concluded, and that `frontend`
    is green.
Record the run id, the head SHA and the check count in the SUMMARY.

⛔ Never write a CI skip token in any commit message, not even inside a sentence denying it —
the tokens fire from anywhere in the message and survive a squash into `main`.
  </action>
  <verify>
    <automated>node -e "const t=require('fs').readFileSync('docs/runbooks/shared-test-db-mutex.md','utf8');if(!t.includes('test-db-drift'))throw new Error('MEASURE_FAIL: the runbook never names the new holder');console.log('runbook names the current holders')"</automated>
    <human-check>Confirm the recorded CI run is bound to this branch's head SHA and that the check COUNT is non-zero — a board showing only two Vercel checks reads as clean while CI never ran.</human-check>
  </verify>
  <done>The runbook names the holders that exist, keeps the superseded readings as dated lineage, and a counted, SHA-bound green run is recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| a pinned assertion -> the mechanism it claims to guard | a pin whose subject moved can pass while guarding nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2-17 | Repudiation | the re-argued pins | high | mitigate | Every touched arm is neutered and observed RED for its own reason before it is trusted; the RED messages are recorded verbatim. |
| T-164.4.2-18 | Spoofing | a green board that is not a green CI | high | mitigate | The green is COUNTED and bound to the head SHA via the runs API, not read from the checks panel. |
| T-164.4.2-19 | Tampering | CI suppressed by a skip token in a commit message | high | mitigate | No commit message in this phase writes one, in any form, including inside a denial. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/critical-regressions.test.ts src/__tests__/contracts/ci-docs-path-filter.contract.test.ts src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` passes.
- `npm test` passes, or every remaining failure is named and attributed.
- A SHA-bound, counted green CI run is recorded with its run id.
- The stale `CLAUDE.md` sentences are listed in the SUMMARY, unedited.
</verification>

<success_criteria>
Nothing that pinned the old job shape now passes by describing a mechanism that moved, and the
tracer's green is a counted fact rather than an absence of red.
</success_criteria>

<output>
Create `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-06-SUMMARY.md` when done.
</output>
