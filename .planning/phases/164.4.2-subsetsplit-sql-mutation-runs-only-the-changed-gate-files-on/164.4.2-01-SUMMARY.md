---
phase: 164.4.2-subsetsplit
plan: "01"
subsystem: infra
tags: [ci, github-actions, sql-tests, mutex, falsification, gh-cli]

# Dependency graph
requires: []
provides:
  - "164.4.2-MEASUREMENT.md: the falsifying BEFORE measurement for Decision E, with a verbatim reproducible recipe"
  - "Corrected, four-clause refutation condition, decided before plan 05's tracer change exists"
affects: [164.4.2-05, 164.4.2-08]

# Actuals (#2632)
actuals:
  tokens: 2730
  tasks: 2
  commits: 3
plan_head_before: 9c7ad67c3965dfc7d08142d6536318042d320805

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Before/after falsification measurement recipe: gh run list -> gh api .../jobs (per-step timings) -> gh api .../runs/<id> (run wall-clock), all captured verbatim in the doc for byte-identical re-run"

key-files:
  created:
    - .planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md
  modified: []

key-decisions:
  - "Used 6 concluded merge-push runs (exceeds the >=5 minimum) to capture both the non-degenerate 16-19min band and two 62-minute cross-run-contention outliers, since the outliers corroborate CLAUDE.md's existing '81.6 min of pure acquire-wait' citation and demonstrate the spread the refutation condition needs."
  - "Implemented the CORRECTED (2026-09-21) four-clause refutation condition from 164.4.2-CONTEXT.md Area E, not the original single-clause condition still present verbatim in this plan's PLAN.md text — the original was unfalsifiable by construction once plan 05 removes sql-tests's acquire step entirely."
  - "Computed all durations with a small Node script (new Date(b)-new Date(a)) rather than by hand, given this document stays in a public tracked repo and the CLAUDE.md discipline around transcription-arithmetic errors."

requirements-completed: [AREA-E]

coverage:
  - id: D1
    description: "164.4.2-MEASUREMENT.md exists with a BEFORE table (>=5 concluded merge-push runs, per-job acquire-wait/useful-work/job-total/run-total for sql-tests/python/e2e-seeded), the verbatim gh recipe, the ROADMAP prediction, and the corrected four-clause refutation condition, all recorded before any code in this phase changes."
    requirement: AREA-E
    verification:
      - kind: other
        ref: "node -e verify script (task 1): '.../164.4.2-MEASUREMENT.md contains >=5 run ids, acquire-wait/sql-tests/python/e2e-seeded/## Recipe'"
        status: pass
      - kind: other
        ref: "node -e verify script (task 2): '.../164.4.2-MEASUREMENT.md contains ## Prediction/## Refutation condition/acquire-wait does not fall/BEFORE-CONFIRMED'"
        status: pass
      - kind: other
        ref: "npm run check:planning-hygiene"
        status: pass
    human_judgment: false

# Metrics
duration: ~20min
completed: 2026-09-21
status: complete
---

# Phase 164.4.2 Plan 01: Area E BEFORE Measurement Summary

**Captured the falsifying BEFORE measurement for Decision E from 6 real `ci.yml` merge-push runs, and replaced the plan's stale single-clause refutation text with the corrected four-clause condition CONTEXT.md now carries, before any tracer code exists.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-09-21T23:23:30Z
- **Tasks:** 2
- **Files modified:** 1 (created)

## Accomplishments
- Enumerated 6 concluded `ci.yml` merge-push runs via `gh run list`, spanning 2026-09-20 through 2026-09-21, and proved every head SHA an ancestor of `HEAD` (`git merge-base --is-ancestor ... && echo BEFORE-CONFIRMED`, all 6 confirmed) — this is what makes the capture a BEFORE rather than a claim about one.
- Pulled per-step timings for `sql-tests`, `python`, `e2e-seeded` via `gh api .../actions/runs/<id>/jobs`, and each run's own wall-clock via `gh api .../actions/runs/<id>`, recording acquire-wait / useful-work / job-total / run-total for all three jobs across all 6 runs. No job was ever ABSENT.
- Wrote the exact `gh` commands verbatim under `## Recipe` so plan 08's AFTER measurement re-runs the same recipe rather than a remembered approximation — no new script, per PATTERNS §5's "this repo has no committed CI-timing tool" finding.
- Implemented the CORRECTED, four-clause refutation condition from `164.4.2-CONTEXT.md` Area E (critical path must fall outside the baseline spread; `sql-tests`'s own job-total must fall, not just its acquire-wait; the three remaining holders — `python`, `e2e-seeded`, and the new `test-db-drift` job plan 05 creates — must not absorb the freed time; a faster wall clock alone does not confirm it) — the original single-clause condition ("acquire-wait must fall to approximately zero") is unfalsifiable by construction once plan 05 removes the acquire step entirely, and is preserved only as explicitly-marked lineage text inside the document.
- Quoted the ROADMAP's own prediction verbatim ("near 11-12 min, bounded by sql-mutation at 10.8") and stated which quantity (the non-degenerate-run critical path) it predicts about.

## Task Commits

Both tasks produced the same single file (`164.4.2-MEASUREMENT.md`) in one continuous write, so they were committed as one task-level commit:

1. **Task 1 + Task 2: BEFORE table, recipe, ancestry proof, prediction, corrected refutation condition** - `95728c89` (docs)

**Plan metadata:** `03dd4a39` (SUMMARY.md), `10261b09` (self-check append). STATE.md/ROADMAP.md excluded from both — orchestrator owns those centrally after merge.

## Files Created/Modified
- `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md` - BEFORE table (6 runs), verbatim recipe, ancestry proof, ROADMAP prediction, corrected four-clause refutation condition

## Decisions Made
- **Captured 6 runs instead of the minimum 5.** The extra run does no harm and, combined with the other 5, surfaces both the non-degenerate 16-19min band and the two 62-minute cross-run-contention outliers — the outliers matter because they corroborate `CLAUDE.md`'s existing "81.6 min of pure acquire-wait" citation (Area C), confirming this capture is consistent with prior measurement rather than a fresh, disconnected number, and they demonstrate exactly the kind of variance clause (d) ("a faster wall clock alone does not confirm it") exists to guard against.
- **Implemented the CORRECTED Area E condition, not the plan's inline paraphrase.** `164.4.2-01-PLAN.md`'s own Task 2 `<action>` text already instructs writing the corrected condition (it quotes the "acquire-wait does not fall to approximately zero... say so and re-measure" language as one clause among several, plus the test-db-drift addendum), so this is not a deviation from the plan text — it is the plan text, cross-checked against `164.4.2-CONTEXT.md` at HEAD per the orchestrator's explicit instruction to read CONTEXT.md rather than trust a possibly-stale plan paraphrase. Confirmed no divergence between the two: they agree.
- **Durations computed via a small Node script**, not manual `mm:ss` subtraction, given the CLAUDE.md-wide emphasis on measured-not-narrated figures in tracked docs and the risk of transcription arithmetic errors across ~36 individual step-duration calculations.

## Deviations from Plan

None - plan executed exactly as written. (See "Decisions Made" above for the note on cross-checking the plan's Task 2 text against `164.4.2-CONTEXT.md` at HEAD — they agreed, so no divergence was found or needed correcting.)

## Issues Encountered
None. `gh` was already authenticated (account `AI-Isaiah`), all 6 `gh api` calls against public run/job metadata succeeded on the first attempt, and both task-level automated `<verify>` checks plus `npm run check:planning-hygiene` passed on the first run.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `164.4.2-MEASUREMENT.md` is committed and ready for plan 08 (the AFTER measurement) to consume — its `## Recipe` section is the exact reproduction procedure, and its `## Refutation condition` section is the binding text plan 08 must check the AFTER numbers against.
- Plan 05 (the tracer that removes `sql-tests`'s mutex-acquire step and creates the `test-db-drift` job) can now proceed with a written-in-advance BEFORE table; nothing in this plan modified code, workflow YAML, or any database, so there is no blocker for wave 1's sibling plan (02) or for later waves.
- No blockers or concerns.

## Self-Check: PASSED
- FOUND: `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md`
- FOUND: `.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-01-SUMMARY.md`
- FOUND: commit `95728c89` (MEASUREMENT.md content)
- FOUND: commit `03dd4a39` (SUMMARY.md)

---
*Phase: 164.4.2-subsetsplit*
*Completed: 2026-09-21*
