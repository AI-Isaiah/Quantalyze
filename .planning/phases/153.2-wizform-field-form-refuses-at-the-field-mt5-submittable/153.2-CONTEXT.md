# Phase 153.2: WIZFORM-FIELD — The form refuses at the field; MT5 declarable *and* submittable - Context

**Gathered:** 2026-08-08
**Status:** Ready for planning
**Source:** Inherited from the Phase 153 split (founder-approved 2026-08-08)

<domain>
## Phase Boundary

**Delivers.** A field the user can get wrong is refused **inline, at the field, before
submit** — and an MT5 strategy can both **declare** its venue and **actually complete a
submit**.

Concretely:

- The 10–5000 character description bound refuses inline next to the field, with the
  offending input highlighted, before submit is reachable — reading
  `MIN_DESCRIPTION_CHARS` (minted by 153.1), never a re-typed literal (D-11, D-23).
- The submit button is **not** `disabled` for a validation reason; the predicate refuses
  and focuses the first invalid control. `MetadataStep.tsx:491`'s `disabled` and the
  `.trim()`-only `handleSubmit` guard at `:222-233` move **together** (FLAG-3).
- Phase 150's allocation-amount form (`AllocateDialog`) is held to the same bar (D-12).
- The submit-path live MT5 re-validation is **removed** — target shape: zero live MT5 calls
  on submit (D-06), with **no** retry loop opened (D-07).
- The ccxt scope probe stops being demanded for a venue that has none, and the catch-all
  that maps every probe failure to `KEY_NETWORK_TIMEOUT` stops — both halves in one ship
  (D-14).
- MT5 becomes declarable in the supported-exchanges metadata step **and** is preselected
  from the key already connected (D-15), via the narrow Option-B widening plus a POSITIVE
  pin assertion (D-20), with the `closed-sets.mt5-flag` pin re-cut in the same commit
  (D-16).

**Requirements:** WIZFORM-01, WIZFORM-04, MT5-14.

**Depends on:** Phase 153.1 — copy members, `VENUE_CAPABILITIES`, `MIN_DESCRIPTION_CHARS`.

**Owns:** `MetadataStep.tsx`(+test), `AllocateDialog.tsx`(+test), `SubmitStep.tsx`
(**routing**), `finalize-wizard/route.ts` (**probe gate + catch-all**),
`closed-sets.mt5-flag.test.ts`, the wizard chip set.

**Does NOT deliver — and who owns each excluded item:**

- Minting `VENUE_CAPABILITIES`, `MIN_DESCRIPTION_CHARS`, the nine `validatePayload` codes,
  the D-34 reorder, the derived roster and the `SubmitStep` roster itself — **Phase 153.1**
  (WIZFORM-CODES). This sub-phase *reads* those; it owns `SubmitStep.tsx` **routing only**
  and `finalize-wizard/route.ts` **outside** `validatePayload`.
- Every Python change — the end-to-end probe deadline, the `initialize()` timeout
  inversion, the terminal lease, `terminal_info()` fail-closed, instrumentation —
  **Phase 153.3** (WIZFORM-GW). D-14(a)'s "read-only is already proven by
  `_validate_mt5_key`" is a *fact this sub-phase relies on*, not code it edits.
- The `validate-key` budget, `budgetKeyFor`, `BREAKER_LOCK_TOMBSTONE_S`, the seam pin
  sites, the long-wait card, `Stop waiting`, `ConnectKeyStep.tsx` /
  `MultiKeyConnectStep.tsx` — **Phase 153.4** (WIZFORM-BUDGET).
- Wizard draft resume / stale screens — **Phase 154**. Live MT5 numeric verification —
  **Phase 155**.

</domain>

<decisions>
## Implementation Decisions

### Submit-path re-validation (WIZFORM-04) — founder-locked

- **D-06: The mandated question "is the per-submit re-validation needed at all?" is
  ANSWERED: no.** The founder's staleness tolerance settles it — a recent successful
  validation plus a live synced series is sufficient evidence. **Target shape: zero live
  MT5 calls on submit.** Drop the live re-check, or cache it behind a minutes-scale TTL.
- **D-07: Do NOT open with a retry loop.** The requirement is explicit that a naive retry
  multiplies the budget this route is capped on and feeds `breaker:railway`; retrying
  into an open breaker is how one slow venue takes down every other user's submits.
  Retry is only discussable AFTER D-06 removes the call.

### Inline field validation (WIZFORM-01)

- **D-11: The client already knows the rule — refuse at the field.** The 10–5000
  character description bound is enforced server-side at `finalize-wizard`; the same
  bound must refuse inline, next to the field, with the offending input highlighted,
  before submit is reachable.
- **D-12: The allocation-amount form added by Phase 150 (OWN-03) is IN SCOPE.** A
  freshly-shipped wizard step must not re-introduce the terminal-envelope class this
  phase deletes.
- **D-13: The acceptance bar is behavioural, not cosmetic.** The observed failure was
  not "an ugly error" — a misleading error sent the founder to **corrupt unrelated
  fields** (adding sFOX to an MT5 account). A fix that makes the error prettier but
  still ambiguous about WHICH field is wrong does not satisfy WIZFORM-01.

### MT5 declarable (MT5-14)

- **D-14: Both halves ship together, or neither.** (a) the ccxt scope probe must handle
  MT5 — or `finalize-wizard` must stop demanding a ccxt probe for a venue that has none
  (read-only is already proven by `_validate_mt5_key`); (b) the catch-all that maps
  **every** probe failure to `KEY_NETWORK_TIMEOUT` must stop, so a PERMANENT
  venue-unsupported condition stops being reported as a temporary blip that says "try
  again". The founder clicked Retry **five times** against a failure that can never
  succeed.
- **D-15: Do not ship the widening without the preselect.** MT5 must be preselected from
  the key already connected, not asked again.
- **D-16: The `closed-sets.mt5-flag` no-widening pin WILL go red — that is the guard
  working.** Re-cut it deliberately **in the same commit**, with its reasoning updated.
  `closed-sets.ts:119-122` currently states mt5 stays OUT of `UI_EXCHANGE_CODES` /
  `EXCHANGES` / `FUNDING_EXCHANGES` / `CRYPTO_EXCHANGES` regardless of the flag; that
  exclusion was a deliberate decision that MT5-14 now outgrows. ⛔ This is NOT the MT5-11
  drift class — do not route around it.
- **D-20: MT5-14 ships as Option B — a narrow wizard-declarable set plus a POSITIVE pin
  assertion.** The public marketing count (`(marketing)/page.tsx:115` renders
  `{EXCHANGES.length} exchanges supported`) does not move, and MT5 is not asserted to be
  an "exchange" in public copy. ⛔ `CRYPTO_EXCHANGES` must stay mt5-free — membership
  there drives √365 vs √252 annualization and would silently corrupt risk metrics.

### The probe is a security control

- **D-22: sFOX keeps the submit-time scope probe byte-unchanged.** Only `mt5` opts out,
  via the capability. The probe is a security control: it must fail *toward* probing when
  the venue is unknown/null.

**Correction to an earlier decision, verified at source by research — the substantive
claim survives, the number does not:**
- D-16 says the `closed-sets.mt5-flag` pin covers four sets; it asserts **two**
  (`UI_EXCHANGE_CODES`, `EXCHANGES`).

### Inherited but NOT owned here

- **D-23 (owner: Phase 153.1)** — `MIN_DESCRIPTION_CHARS` is minted there; the field guard
  and the submit guard must *import* it. Re-typing `10` re-opens the drift D-23 closes.
- **D-09b / D-34 (owner: Phase 153.1)** — the nine `validatePayload` arms gain codes and
  six pre-existing arms are reordered. This sub-phase's catch-all replacement must emit a
  code in the shape the derived-roster invariant scans for (`code:` FIRST, uppercase).
- **D-17 (owner: Phase 153.1)** — "switch to a different exchange" must never render for
  MT5; the gating lives in the copy layer, but this sub-phase's error routing must not
  re-introduce a venue-shaped remedy at the field level.
- **D-22's capability record (owner: Phase 153.1)** — `scopeProbeSupported` is defined in
  `closed-sets.ts` by 153.1; the probe gate here *reads* it and must fail toward probing.
- **D-21 (owner: Phases 153.1 / 153.4)** — `Validating...` (ASCII) is the one spelling; do
  not introduce the typographic ellipsis in any new control.
- **D-01 / D-26 (owner: Phase 153.4)** — the MT5 validate budget stays at today's value
  until 153.4 raises it. Do not size any submit-path behaviour against `120_000`.
- **D-24 / D-29 / D-31 (owner: Phase 153.3)** — the server-side reasons an MT5 verdict is
  slow or unclassifiable. D-06 removes the submit-path call precisely so this sub-phase
  does not have to wait on them.

### Claude's Discretion

- Exact inline-validation presentation, within `DESIGN.md`.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-CONTEXT.md`
  — the parent's full D-01..D-34 (this file carries only this sub-phase's slice)
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-RESEARCH.md`
  — **Finding 2** (⭐ WIZFORM-04 and MT5-14(a) are one gate at one line) and
  **Finding 2b** (the composite loop needs the member's venue, which is NOT selected
  today); **Finding 5** (MT5-14: the preselect already works; only the display entry is
  missing) and **Finding 5b** (⚠️ the widening's real blast radius, and the two candidate
  shapes); **Finding 6** (`buildEnvelope` is a pass-through, so `fix[]` gating must live in
  `formatKeyError`); **Finding 8** (there is no `MIN_DESCRIPTION_CHARS` constant; UI-SPEC
  FLAG-5 requires minting one); **Table D** (what the `closed-sets.mt5-flag` pin ACTUALLY
  asserts); **Pattern 3** (per-venue capability, DEFAULT preserves today's behaviour);
  **Pattern 4** (inline field validation: the aria-derived red border);
  **Pitfall 2** (⛔ deleting `MetadataStep.tsx:491`'s `disabled` without widening `:230`
  re-ships the exact defect), **Pitfall 4** (skipping the scope probe on an UNRESOLVED
  venue disables a security control), **Pitfall 7** (a copy string this phase changes that
  `e2e/` still asserts), **Pitfall 8** (widening `CRYPTO_EXCHANGES` while widening the UI
  sets); §Code Examples — *The structural Retry suppression (do NOT add a prop)* and
  *The flag-gated venue-set widening (the sFOX precedent, if Option A is chosen)*
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-PATTERNS.md`
  — **⭐1 Inline field validation — the aria-derived red border** (the `Field` primitive and
  the RTL analogs); **⭐3 Per-venue CAPABILITY (class), never `venue === "mt5"` (instance)**;
  the `src/app/api/strategies/finalize-wizard/route.ts` (route handler) section;
  the `SubmitStep.tsx` (roster + field-level code routing) section; §Test-file analogs
  (RTL); **Shared Pattern A** (the red border derives from ARIA, never from JS),
  **B** (structural Retry suppression is an ABSENCE, never a prop),
  **F** (flag-gated widening keeps `as const satisfies` on EACH literal),
  **G** (announcements re-state visible copy; never author new copy in `LiveRegion`)
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-VALIDATION.md`
  — Wave 0 items owned here: *"Fail-toward-probing on an unresolved venue in
  `finalize-wizard/route.test.ts` — WIZFORM-04"*, *"Flag-ON **positive** assertion in
  `closed-sets.mt5-flag.test.ts` — MT5-14"*, *"MT5 preselect + `<span>`-not-`<button>`
  pinned chip in `MetadataStep.test.tsx` — MT5-14"*, and the `supportedExchanges`
  always-contains-the-detected-venue row. Test-map rows: all four WIZFORM-01 rows
  (2-char refusal with no POST, first-invalid-control focus opening a collapsed
  `<details>`, submit **not** `disabled` for a validation reason, `AllocateDialog` clearing
  red live off `aria-invalid`), all three WIZFORM-04 rows (zero `keys-permissions` calls on
  an MT5 submit; unresolved venue still probes; a ccxt submit still probes byte-identically),
  and all five MT5-14 rows. Falsifiability rows **SC-1** (×2), **SC-3** (×2 — including
  *"change the mt5 skip to an instance check `venue === "mt5"`"*), **SC-6** (×2 — including
  *"ship the chip-set widening but delete the preselect"*).
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-UI-SPEC.md`
  — **this sub-phase carries UI surfaces.** §**Surface 2 — Inline field validation**
  (the rule, anatomy through `Field`, reveal timing, submit-with-errors, *the submit button
  is NEVER disabled for a validation reason*, server-side rejections that are field-level,
  *the description field — the rule that cost 3 submits*); §**Surface 4 — MT5 in the
  supported-exchanges step** (*a detected venue is a FACT, not a CHOICE*); §**Surface 3**
  Gates **A**–**D** as they apply to field-level routing; §Focus-ring contract;
  §Copywriting Contract; §What this contract FORBIDS (checker-greppable); and the folded-in
  checker findings **⛔ FLAG-3**, **FLAG-1** (`AllocateDialog` is the BEHAVIOURAL precedent,
  not the styling one) and **FLAG-5** (character bounds must read from the constant).
- `153-EVIDENCE-mt5-platform.md` / `153-EVIDENCE-mt5-latency.md` — **not required here**
  (Phases 153.3 and 153.4 own the evidence-driven work). Read §C11–C12 of the platform
  evidence only if you need to restate *why* MT5 has no ccxt scope surface.
- `DESIGN.md` — read before any visual decision.

</canonical_refs>

<specifics>
## Specific Ideas

**Binding traps (verbatim from `.planning/ROADMAP.md` §Phase 153.2):**

- ⛔ **FLAG-3 is ONE indivisible task.** Deleting `MetadataStep.tsx:491`'s `disabled` without widening the `.trim()`-only `handleSubmit` guard at `:222-233` lets a 2-character description POST — re-shipping the very defect this phase deletes.
- ⛔ **MT5-14 and WIZFORM-04 ship together.** Widening the chip set without the probe skip leaves MT5 a HARD BLOCKER — declarable but still unsubmittable.
- ⚠️ The `closed-sets.mt5-flag` pin re-cut and the widening are the SAME commit, and the re-cut ADDS the positive flag-ON assertion the pin lacks today.

**Also binding, from the parent phase's ROADMAP trap list:**

- ⛔ **MT5-14: the `closed-sets.mt5-flag` no-widening pin WILL go red — that is the guard working, not a regression to route around.** The pin must be re-cut deliberately, with its reasoning updated, in the same commit. This is NOT the MT5-11 drift class; the exclusion was a deliberate decision that is now outgrown.
- ⚠️ The allocation-amount form Phase 150 (OWN-03) adds is IN SCOPE for the inline-validation criterion — a freshly-shipped wizard step must not re-introduce the terminal-envelope class this phase deletes.

**File:line anchors this sub-phase must not lose** (all copied from the parent CONTEXT /
ROADMAP; D-10's instruction to re-derive line numbers from source applies here too):

- `MetadataStep.tsx:491` — the `disabled` that must go, and `:222-233` — the `.trim()`-only
  `handleSubmit` guard that must widen in the same task (RESEARCH Pitfall 2 cites `:230`).
- `finalize-wizard/route.ts:220` — the `permissions?force_refresh=true` probe;
  `:617` and `:628` — the `KEY_NETWORK_TIMEOUT` mapping (the catch-all D-14b deletes).
  ⚠️ `:617` is also one of D-34's six reorder sites owned by 153.1 — coordinate.
- `src/lib/closed-sets.ts:105-125` — `MT5_UI_ENABLED` and the no-widening reasoning;
  `:119-122` — the four-set exclusion statement D-16 re-cuts.
- `src/lib/closed-sets.mt5-flag.test.ts` — the pin that must be re-cut deliberately.
- `(marketing)/page.tsx:115` — `{EXCHANGES.length} exchanges supported`, the public count
  D-20 forbids moving.
- `MetadataStep.tsx:18` — already imports `isCryptoExchange` from `closed-sets.ts`, which is
  why that module is the isomorphic home for `VENUE_CAPABILITIES`.
- Founder on the submit retry (WIZFORM-04, verbatim): *"clicking twice is not
  acceptable, especially with this mistake message. A user would just not know what to
  do."*
- The observed WIZFORM-01 failure, verbatim from D-13: a misleading error sent the founder
  to **corrupt unrelated fields** (adding sFOX to an MT5 account).

</specifics>

<deferred>
## Deferred Ideas

- The nine `validatePayload` codes, the derived roster, the D-34 reorder,
  `VENUE_CAPABILITIES`, `MIN_DESCRIPTION_CHARS`, the copy table and the `SubmitStep`
  roster — **Phase 153.1** (D-08, D-09, D-10, D-17, D-21, D-22, D-23, D-34).
- Anything under `analytics-service/` — **Phase 153.3** (D-02, D-03, D-24, D-25, D-27,
  D-29–D-33). In particular: do not "fix" the MT5 validate latency here; D-06 removes the
  submit-path call instead.
- The venue-aware budget, `budgetKeyFor`, the pin surface, `BREAKER_LOCK_TOMBSTONE_S`, the
  long-wait card, `Stop waiting`, `ConnectKeyStep.tsx` / `MultiKeyConnectStep.tsx` —
  **Phase 153.4** (D-01, D-04, D-05, D-19, D-26).
- A **retry loop** on the submit path — D-07 forbids opening with one; it is only
  discussable after D-06 has removed the call, and any such work is a new item for
  `TODOS.md`, not this sub-phase.
- Whether **sFOX** should also opt out of the scope probe — an OPEN QUESTION (RESEARCH
  §Open Questions Q2), not a finding. D-22 pins sFOX byte-unchanged; log the question in
  `TODOS.md`.
- Widening `CRYPTO_EXCHANGES` or `FUNDING_EXCHANGES` to include mt5, or moving the public
  marketing exchange count — forbidden by D-20, not deferred.
- Wizard draft-aware entry chooser, stale-screen root cause, token-less credential dedup —
  **Phase 154** (WIZCONT/STALE).
- Live MT5 numeric verification against an external oracle on a trading day — **Phase 155**
  (MT5-VERIFY), human- and calendar-gated.

</deferred>

---

*Phase: 153.2-wizform-field-form-refuses-at-the-field-mt5-submittable*
*Context inherited: 2026-08-08 from the Phase 153 split*
