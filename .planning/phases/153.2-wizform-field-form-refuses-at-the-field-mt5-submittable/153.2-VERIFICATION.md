---
phase: 153.2-wizform-field-form-refuses-at-the-field-mt5-submittable
verified: 2026-08-09T22:27:15Z
status: human_needed
score: 25/25 must-haves verified
overrides_applied: 0
warnings:
  - id: W-153.2-1
    concern: "Requirement traceability rows disagree with their own checkboxes for MT5-14 and WIZFORM-04 — the SAME inconsistency class D-153.2-A logged and closed for WIZFORM-01, recurring unlogged for the other two of this phase's three requirement IDs"
    evidence: ".planning/REQUIREMENTS.md:460 `[x] MT5-14` vs row :1083 'Pending'; :693 `[x] WIZFORM-04` vs row :1090 'Pending'. Commit 91bf2561 ('REQUIREMENTS: MT5-14 and WIZFORM-04 marked complete') flipped both checkboxes and touched neither row."
    impact: "Documentation only — implementation for both requirements is present and tested in the tree. Blocks nothing, but /gsd:audit-milestone reads the traceability table and would report both as Pending."
    human_decision: "Update rows :1083 and :1090 to Complete, or log as a deferred item with a named owner (the direction D-153.2-A's own owner clause specified: tick both, not a blend)."
  - id: W-153.2-2
    concern: "Phase bookkeeping is stale after 153.2-05"
    evidence: ".planning/ROADMAP.md:346 still `- [ ] 153.2-05-PLAN.md`; .planning/STATE.md:6 `stopped_at: Completed 153.2-04-PLAN.md`; STATE.md:559-567 record nine 153.2-05 decisions under `[Phase ?]` instead of `[Phase 153.2]`. Commit 4b76d4b8 (the 05 completion commit) touched REQUIREMENTS/STATE/SUMMARY/deferred-items but not ROADMAP."
    impact: "No code impact. /gsd-next and the milestone audit read these."
    human_decision: "Fix in the phase-close commit."
human_verification:
  - test: "Server-refusal path in a real browser: force finalize-wizard to answer `{ code: 'METADATA_AUM_INVALID' }` (or submit a draft with an out-of-range AUM against a stale client), and watch where the caret lands."
    expected: "The wizard returns to the metadata step with every typed value intact, the 'More details (optional)' disclosure OPENS, and the caret lands on an AUM control that is actually ON SCREEN — not scrolled past or behind a shut disclosure."
    why_human: "jsdom does not implement `<details>` content hiding, so `document.activeElement` can be the AUM input while a real browser would have no-oped the focus call. The suite asserts `details.open` precisely because the focus line cannot carry this claim — see MetadataStep.test.tsx:944-950 and :1721-1724. Only a browser closes it."
  - test: "Client-refused submit in a real browser: fill a valid description, type `-5` into AUM (inside the collapsed disclosure), click 'Review and submit'."
    expected: "No navigation. The disclosure opens, the caret is in the visible AUM field, the field reads red with its message, and the line above the buttons reads '1 field needs attention. The first is AUM (USD).'"
    why_human: "Same jsdom `<details>` limitation, on the other of the two paths through `revealAndFocus`. This is the path a user reaches without any server round-trip, so it is the more common one."
  - test: "Run the seeded e2e specs: `npx playwright test e2e/api-key-flow.spec.ts e2e/csv-upload-flow.spec.ts` against a TEST-backed dev server, serialized (never concurrently with the python suite)."
    expected: "Both green. The wizard step machine — including the new `persistPointer('metadata')` transition on a field-level refusal — survives a real reload."
    why_human: "Declared NOT run by both 153.2-02 and 153.2-05, with the reason written out rather than dropped silently: the specs need TEST-project credentials plus a dev server, and the shared TEST database is a known concurrency hazard for exactly these suites. The 28-file wizard vitest tree (488 passing, re-run at verification) plus `wizard/localStorage.test.ts` cover the step machine's logic, but not a real reload against a real DB."
  - test: "MT5 flag-ON pass: set `NEXT_PUBLIC_MT5_ENABLED=true` and `MT5_ENABLED=true`, connect an MT5 key, and take that strategy through the metadata step to a completed submit."
    expected: "'MT5' renders in Supported exchanges as a non-interactive chip (not a greyed button) with a 'DETECTED FROM YOUR KEY' eyebrow and the sentence 'MT5 is included because it is the venue of the key you connected.' The submit COMPLETES — no `KEY_NETWORK_TIMEOUT`, no 'switch to a different exchange'."
    why_human: "`MT5_UI_ENABLED` is OFF by default, so this entire surface is dark in CI and in production today. The unit suite proves it with a stubbed flag and a mocked seam; nobody has watched an MT5 strategy actually finish a submit since the probe skip landed. This is the requirement's own headline (MT5-14 was filed a HARD BLOCKER)."
  - test: "Measure the focus-ring contrast on the description textarea, the category/codename selects, the AUM and max-capacity inputs, and AllocateDialog's amount input — tab to each and sample the ring against `bg-surface`."
    expected: "≥3:1 (WCAG 1.4.11 non-text contrast). The ring is inset and full-opacity, never clipped by an ancestor's overflow."
    why_human: "The suite asserts the class STRING (`focus-visible:ring-2 ring-inset ring-accent`, and forbids `ring-accent/20`). It cannot measure a rendered contrast ratio or see a clip. This is the specific defect the phase fixed — the old 20%-alpha ring measured ~1.3:1."
deferred:
  - truth: "Three finalize-wizard rejections still answer code-less (500 draft-load, 500 finalize-RPC, 502 upstream-shape)"
    addressed_in: "WIZFORM-02 close-out / milestone-closing fix round (deferred-items.md D-153.2-D)"
    evidence: "WIZFORM-02 is not one of this phase's requirement IDs (WIZFORM-01, WIZFORM-04, MT5-14). The ledger moved 5 -> 3 (down, never up), and `wizardErrors.invariant.test.ts` reds in BOTH directions so the debt cannot silently grow. Verified GREEN at 3."
  - truth: "ConnectKeyStep.tsx / MultiKeyConnectStep.tsx pass no context.venue or context.surface, so an MT5 user still reads 'switch to a different exchange' on the CONNECT step"
    addressed_in: "Phase 153.4"
    evidence: "153.2-UI-SPEC.md line 21-22: 'Not yours: ... (ConnectKeyStep.tsx, MultiKeyConnectStep.tsx) belong to Phase 153.4.' ROADMAP records the same split. WIZFORM-03 is half-closed by design."
  - truth: "`seam-venue-vocabulary.invariant.test.ts` is RED on `analytics-service/services/ingestion/mt5.py:242` -> `:363`"
    addressed_in: "Phase 153.3"
    evidence: "deferred-items.md D-153.2-C. Everything under `analytics-service/` is excluded from 153.2 by the CONTEXT's explicit exclusion list; 153.2 owns no Python. Confirmed still RED at verification, and confirmed to be pure line drift from 153.3's MT5 gateway work."
---

# Phase 153.2: WIZFORM-FIELD Verification Report

**Phase Goal:** A field the user can get wrong is refused inline, at the field, before submit — and an MT5 strategy can both declare its venue and actually complete a submit
**Verified:** 2026-08-09T22:27:15Z
**Status:** human_needed
**Re-verification:** No — initial verification
**Branch:** `feat/v1.17-153-wizform` @ `4b76d4b8` (working tree clean)

ROADMAP's `success_criteria` array for this phase is empty, so must-haves were taken from the five PLAN frontmatter blocks and merged. No PLAN must-have narrows a roadmap SC (there are none to narrow).

## Goal Achievement

### Observable Truths

#### Plan 01 — the description field refuses

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A 2-character description is refused at the description field before submit is reachable | VERIFIED | `MetadataStep.tsx:66-75` `validateDescription` returns `METADATA_DESCRIPTION_TOO_SHORT`; it feeds `clientFieldErrors.description` (`:678`), which feeds `invalidFields` (`:713`), which is the `handleSubmit` gate at `:774`. `onComplete` is unreachable while it is non-empty. |
| 2 | The submit button is never disabled for a validation reason | VERIFIED | `MetadataStep.tsx:1184` — `<Button type="submit">` carries no `disabled` and no `aria-disabled`. Grep for `disabled` in the file returns only `assetClassSelect`'s locked picker (`:831`). |
| 3 | A description that passes the client is one the server accepts: one constant, two guards | VERIFIED | Client reads `MAGNITUDE_CAPS.MIN_DESCRIPTION_CHARS` / `MAX_DESCRIPTION_CHARS` (`MetadataStep.tsx:68,71`); server reads the SAME members (`finalize-wizard/route.ts:493,505`). No re-typed literal on either side. |
| 4 | The message names the rule and the user's own count, and disappears the instant the value becomes valid | VERIFIED | `messageFor` (`:719-731`) passes `{ charCount: description.length }` for the description only and renders `formatKeyError(...).title`. It is derived DURING RENDER, never latched into state — that is what gives live-clear. 153.2-01 measured the mutation: latching on blur reds `the message and the red border clear LIVE, with no second blur`. |
| 5 | The description control's focus ring is visible to a sighted keyboard user | VERIFIED (code) | `MetadataStep.tsx:949` — `focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-accent`, full opacity, replacing the ~1.3:1 20%-alpha ring. Rendered contrast ratio routed to human item 5. |

#### Plan 02 — the rest of the form

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 6 | Every server rule this form can evaluate refuses at its own field | VERIFIED | `clientFieldErrors` (`:669-686`) covers description, category, aum, maxCapacity. Codename and capitalOwnership are deliberately `null` with the reasoning stated at `:670-675` — a `<select>` over a closed list and a defaulted uncleanable radio group cannot produce a value the server arm rejects. They keep their SLOT so a server refusal still has a field (truth 22). |
| 7 | Submitting with errors puts the cursor in the first control that is wrong | VERIFIED | `handleSubmit` `:774-779` calls `revealAndFocus(invalidFields[0])`; `invalidFields` is filtered through the explicit `FIELD_ORDER` (`:219-231`), not `Object.keys`. A DOM-order row pins it. |
| 8 | A first invalid control hidden inside the collapsed More-details disclosure is revealed before it is focused | VERIFIED | `revealAndFocus` `:536-544` sets `detailsRef.current.open = true` BEFORE `control.focus()`, and tests containment (`detailsRef.current?.contains(control)`) rather than hard-coding which fields live inside. Asserted as `details.open` at MetadataStep.test.tsx:963 and :1732 — the correct oracle. Browser confirmation is human item 1/2. |
| 9 | The number of problems and the name of the first one is stated visibly, not only to a screen reader | VERIFIED | `submitSummary` (`:755-760`) is ONE expression feeding a visible `<p data-testid="metadata-submit-summary">` (`:1154-1161`) and `<LiveRegion assertive message={submitSummary} />` (`:1162`). Field names are read from `FIELD_LABELS`, the same strings the `Field`s are given. |
| 10 | An AUM the client accepts is an AUM the server accepts — the same predicate function | VERIFIED | `MetadataStep.tsx:11` imports `isValidDollar` from `@/lib/dollar-validation`; `finalize-wizard/route.ts:13` imports the identical function and applies it at `:544,556`. `validateDollarField` (`:118-125`) mirrors BOTH halves of the server's condition (`!isOmitted && !isValidDollar`), including the string→number transform SubmitStep performs at the wire. |

#### Plan 03 — AllocateDialog (D-12)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 11 | An amount the user has already corrected does not still read red | VERIFIED | `AllocateDialog.tsx` onChange re-runs `parseAmount` when `fieldError !== null` and takes its VERDICT (not a blanket clear), so a value that becomes invalid for a DIFFERENT reason swaps message rather than going silent. 153.2-03 measured the mutation: neutering the guard reds the live-clear row with the stale message in the diff. |
| 12 | The money field's red border cannot exist without aria-invalid — it is derived, not toggled | VERIFIED | `AllocateDialog.tsx:388` — `aria-[invalid=true]:border-negative` with no JS ternary and no hand-written `aria-invalid` on the input. `Field` is the sole writer (comment at `:373-376` records that a hand-written attribute would win the prop spread and re-open the hazard). |
| 13 | The CTA is still never disabled for a validation reason | VERIFIED | The three `disabled={busy}` occurrences are in-flight double-submit guards. `handleSave` refuses inline and focuses the input (`:246-256`). |
| 14 | The dialog's full-opacity inset focus ring survives the border change | VERIFIED | `:388` retains `focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-accent` alongside the new aria-derived border. |

#### Plan 04 — MT5 declarable AND submittable

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 15 | An MT5 strategy can declare MT5 as its supported exchange in the metadata step | VERIFIED | `closed-sets.ts:472-495` — `WIZARD_ONLY_EXCHANGE_CODES = ["mt5"]`, composed into `WIZARD_EXCHANGE_CODES` under `MT5_UI_ENABLED`, displayed via `WIZARD_EXCHANGES`. `MetadataStep.tsx:1090` sources the chip group from it. |
| 16 | MT5 is preselected from the key already connected — the venue is never asked twice | VERIFIED | `detectedVenue` (`:405-407`) canonicalizes through `canonicalizeWizardExchange` (the public canonicalizer does not know a wizard-only venue and would hand back lowercase `"mt5"`, matching nothing — the ISSUE-004 shape, one venue later). Seeded into state at `:408-410` and passed as `pinned` at `:1092`. |
| 17 | The declared venue set can never exclude the venue of the key the strategy is built on | VERIFIED | Two independent closures: the pinned chip renders as a `<span>` with no handler (`:1254-1260`), so removal is impossible through the UI; and `withDetectedVenue` is applied AGAIN at the wire (`:793`), closing the resumed-draft path where `initial` predates the pin. |
| 18 | An MT5 submit makes zero live calls to the venue | VERIFIED (mutation-tested) | `route.ts:736` `if (!venueSupportsScopeProbe(venue)) return { ok: true }`. I neutered this gate to `if (false && ...)` and re-ran `route.test.ts`: **3 tests reddened by name**, including both class sweeps ('composite member on venue "mt5" did not match its capability row'). Restored; tree clean. |
| 19 | A venue whose identity could not be resolved is STILL scope-probed | VERIFIED (mutation-tested) | `venueSupportsScopeProbe` returns `?? true` (`closed-sets.ts:196-200`) and reads via `Object.hasOwn` so prototype keys cannot resolve. Both call sites pass a possibly-null venue (`route.ts:1200` single-key, `:1442` per composite member) — verified BOTH are gated, not just the one someone happened to test. |
| 20 | A permanent probe condition is never reported as a temporary blip that invites a retry | VERIFIED | Catch-all split at `route.ts:786-840`: permanent status → `KEY_SCOPE_CHECK_UNAVAILABLE` (502), `ProbeMisconfiguredError` → `SEAM_MISCONFIGURED` (500), parse miss → `KEY_SCOPE_CHECK_UNAVAILABLE` (`:864-875`) distinguished from a SERVICE-reported `probe_error` by REFERENCE IDENTITY against the module-scope `PROBE_PARSE_MISS` sentinel, which an upstream payload cannot forge. `KEY_NETWORK_TIMEOUT` survives only as the honest transport tail. |
| 21 | The public marketing exchange count does not move, and CRYPTO_EXCHANGES stays mt5-free | VERIFIED | `closed-sets.mt5-flag.test.ts` — five negative rows (UI_EXCHANGE_CODES / EXCHANGES / FUNDING_EXCHANGES / CRYPTO_EXCHANGES, flag ON and OFF) PLUS the new POSITIVE row `:142` asserting `WIZARD_EXCHANGE_CODES` DOES gain mt5 when the flag is on, and `:162` asserting flag-OFF is an EQUALITY to `EXCHANGES`, not two absences. `CRYPTO_EXCHANGES` at `:553-559` is mt5-free (√365 vs √252 money-math boundary). All green. |

#### Plan 05 — a field-level server rejection routes back to the field

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 22 | A field-level refusal from the server sends the user back to that field, not to a full-page envelope | VERIFIED | `SubmitStep.tsx:498-503` routes on `FIELD_BY_CODE.get(surfaced)`, explicitly NOT on the HTTP status. `WizardClient.tsx:614-622` `handleMetadataFieldError` sets the refusal, `setStep("metadata")` and `persistPointer("metadata", …)` so a reload does not drop the user back on submit. `MetadataStep.tsx:563-569` reveals + focuses on the prop, through the SAME `revealAndFocus` a client refusal uses. `metadataDraft` is not reset — a `[Gate D]` row asserts every typed value survives. |
| 23 | Every field-level code the route can emit has exactly one field — a code with no field is a test failure, never a silent envelope | VERIFIED | `SubmitStep.tsx:95-121` — an 8-entry `ReadonlyMap` keyed by `MetadataFieldId`, which is IMPORTED from `MetadataStep` rather than re-spelled. `SubmitStep.test.tsx:1189-1263` derives `FIELD_LEVEL_CODES` from `WIZARD_ERROR_COPY` (not a hand list), carries an anti-vacuity floor of ≥7, reds BY NAME on an unmapped code, and asserts the reverse direction (no stray key, no stray field id). |
| 24 | A non-field failure still renders the terminal envelope exactly as it does today | VERIFIED | `SubmitStep.test.tsx:1350-1362` — a `KEY_NETWORK_TIMEOUT` 502 renders the envelope title and `onFieldLevelError` is NOT called. `:1364-1381` — with NO handler wired, a field-level code still falls through to the envelope rather than being swallowed. |
| 25 | The message the user reads at the field is copy-table copy, never a raw server string | VERIFIED | `MetadataStep` renders `formatKeyError(code).title` only; the response's `error` string never crosses into the step (only the CODE does — `serverFieldError: { field, code }`). `MetadataStep.test.tsx:1840-1858` asserts on the rendered DOM (not a grep) that the envelope's `cause` paragraph, its `fix[]` bullets and the correlation id are all ABSENT for a server refusal. |

**Score:** 25/25 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx` | client mirrors, widened predicate, no validation-disabled CTA, MT5 chips + preselect, serverFieldError | VERIFIED | 1291 lines. Final state judged, not any single plan's description — all four writers' contributions are present and coherent: `validateDescription` (01), `validateDollarField` + `revealAndFocus` + `submitSummary` (02), `withDetectedVenue` + `InlineChipGroup pinned` (04), `serverRefusal` + `MetadataFieldId` widened 4→6 (05). Contains `MIN_DESCRIPTION_CHARS`, `isValidDollar`, `WIZARD_EXCHANGES`, `serverFieldError`. |
| `.../MetadataStep.test.tsx` | 2-char refusal, never-disabled, live-clear, details-open, MT5 pin, server-refusal rows | VERIFIED | 1876 lines. Flag-ON/OFF MT5 harness via dynamic re-import; a `[153.2 / D-15] no venue is pinned when no key was connected` negative control; a `CONTROL — no serverFieldError` negative control. |
| `src/app/(dashboard)/allocations/components/AllocateDialog.tsx` | aria-derived invalid border + live re-evaluation | VERIFIED | 441 lines. Contains `aria-[invalid=true]:border-negative`; the JS ternary holdout is gone. |
| `.../AllocateDialog.test.tsx` | D-12 live-clear row + aria-derivation row | VERIFIED | 802 lines, 2 mutation-measured rows recorded in the 03 SUMMARY. |
| `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx` | `FIELD_BY_CODE` + venue/surface/charCount envelope context | VERIFIED | 778 lines. Contains `FIELD_BY_CODE`; its single `buildEnvelope` call site (`:588`) now passes `venue`, `surface: "submit"` and `charCount` — the UI-SPEC's mandated `SubmitStep.tsx:414` obligation. |
| `.../SubmitStep.test.tsx` | totality assertion with a vacuity floor; envelope-preservation rows | VERIFIED | 1510 lines. Oracle independence explicitly maintained (expected sentences are literals, never `WIZARD_ERROR_COPY[code].title`). |
| `src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx` | return-to-metadata handoff carrying the field id | VERIFIED | 1331 lines. `metadataServerFieldError` state, `handleMetadataFieldError` callback, retired on any navigation away from metadata (`:600` region). |
| `src/lib/closed-sets.ts` | `WIZARD_EXCHANGE_CODES` / `WIZARD_EXCHANGES` / `venueSupportsScopeProbe` | VERIFIED | 837 lines. `VENUE_CAPABILITIES` with `mt5: { scopeProbeSupported: false, … }`; predicate reads via `Object.hasOwn`, defaults `true`. |
| `src/lib/closed-sets.mt5-flag.test.ts` | pin re-cut — both negatives kept, POSITIVE flag-ON assertion added | VERIFIED | 195 lines. Both directions present; no negative was traded away for the positive. |
| `src/app/api/strategies/finalize-wizard/route.ts` | capability-gated probe at every call site + split catch-all | VERIFIED | 2268 lines. Contains `venueSupportsScopeProbe`; both call sites gated (mutation-tested). |
| `.../finalize-wizard/route.test.ts` | capability sweeps, D-14b split rows | VERIFIED | 3950 lines, 123 passing. See the Plan-04 audit below. |

No artifact is a stub. No artifact is ORPHANED — every one is imported and exercised.

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `MetadataStep.tsx` | `closed-sets.ts MAGNITUDE_CAPS` | imported bounds, never a re-typed literal | WIRED | `:21` import, `:68`/`:71` use. Server reads the same members. |
| `MetadataStep.tsx` | `wizardErrors.ts` | `formatKeyError(code, { charCount }).title` through `Field`'s `error` prop | WIRED | `:12` import, `:727-730`. |
| `MetadataStep.tsx` | `dollar-validation.ts` | `isValidDollar` — the SAME function the route's aum/max_capacity arms call | WIRED | `:11` import; route `:13`. |
| submit-with-errors summary | `LiveRegion.tsx` | one `const` feeding both the visible `<p>` and the announcement | WIRED | `:1154-1162`. |
| `AllocateDialog.tsx` | `Field.tsx` | `Field`'s `error` prop is the ONLY writer of `aria-invalid`; the border derives from it | WIRED | `:336` `Field`, `:388` `aria-[invalid=true]:border-negative`, no attribute on the input. |
| `finalize-wizard/route.ts` | `closed-sets.ts venueSupportsScopeProbe` | the gate reads the CAPABILITY, never a venue name | WIRED | `:11` import, `:736` gate. The literal `"mt5"` appears nowhere in the route's branch logic. |
| `MetadataStep.tsx` | `closed-sets.ts WIZARD_EXCHANGES` | the chip group's item source | WIRED | `:22` import, `:1090`. |
| pinned chip | submitted `supportedExchanges` payload | detected venue unioned in at submit | WIRED | `:793` `withDetectedVenue(supportedExchanges, detectedVenue)`. |
| `SubmitStep.tsx` | `MetadataStep.tsx` | `onFieldLevelError(fieldId, code)` → `WizardClient` → `serverFieldError` | WIRED | `:498-503` → `WizardClient:614-622` → `:1008` prop → `MetadataStep:563-569`. Full three-hop chain traced. |
| `FIELD_BY_CODE` | `wizardErrors.ts` | every `METADATA_*` code is a key; a totality test asserts none missing | WIRED | Derivation is from `Object.keys(WIZARD_ERROR_COPY)`, with an anti-vacuity floor. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `MetadataStep` | `fieldErrors` | `clientFieldErrors` merged with `serverRefusal` (`:704-711`) — the client mirror wins on `??` because it judges the value on screen NOW | Yes | FLOWING — one derivation feeds the message, the aria border, the submit gate, the focus target and the summary. No parallel path. |
| `MetadataStep` | `supportedExchanges` | `withDetectedVenue(initial ?? [], detectedVenue)` at mount, re-applied at the wire | Yes | FLOWING — not an empty literal; the mount seed is canonicalized against the WIZARD set. |
| `MetadataStep` | `serverRefusal` | prop → `useEffect` → state; dropped by `noteFieldEdited` on the first edit of the named field | Yes | FLOWING — clearable by user action, which is the point (an unclearable message is the dead end being deleted). |
| `AllocateDialog` | `fieldError` | `parseAmount(amount)` at save, re-run per keystroke once a message shows | Yes | FLOWING |
| `SubmitStep` | `errorCode` | `seamErrorCode(data)` → `recogniseSeamErrorCode` → `KNOWN_FINALIZE_CODES` membership → `UNKNOWN` floor | Yes | FLOWING — read through the leaf that handles both wire shapes, from the same body as the correlation id. |
| `route.ts` probe | `livePerms` | `fetchLivePermissions(apiKeyId)` real seam call, gated on capability | Yes | FLOWING — the parse-miss sentinel carries NO scope fields by design, so a miss can never present a verdict. |

No HOLLOW artifacts. No prop hardcoded empty at a call site.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| The wizard tree still compiles and passes | `npx vitest run --no-file-parallelism "src/app/(dashboard)/strategies/new/wizard"` | 28 files, **488 passed** | PASS |
| The three owned component suites | `npx vitest run … MetadataStep.test.tsx AllocateDialog.test.tsx SubmitStep.test.tsx` | **160 passed** | PASS |
| The finalize route | `npx vitest run … finalize-wizard/route.test.ts` | **123 passed** | PASS |
| The three invariant/pin guards | `npx vitest run … seam-citations.invariant seam? wizardErrors.invariant closed-sets.mt5-flag` | **92 passed** across 3 files | PASS |
| Types | `npx tsc --noEmit` | clean, no output | PASS |
| ⭐ MUTATION: neuter the capability gate (`if (false && !venueSupportsScopeProbe(venue))`) | `npx vitest run … route.test.ts` | **3 failed / 120 passed** — both class sweeps + the MT5 zero-seam row red BY NAME | PASS (guard is load-bearing) |
| Playwright e2e | not run | — | SKIP → human item 3 |

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist in this repo and no PLAN or SUMMARY declares one. Step 7c: SKIPPED (no probes declared or discoverable).

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|---|---|---|---|---|
| **WIZFORM-01** | 153.2-01, -02, -03, -05 | A field the user can get wrong is validated on the form, inline, next to that field, with the offending input highlighted — never as a terminal page-level error after submit | SATISFIED | Truths 1-14 and 22-25. The last blocking condition 153.2-01/-02 named in STATE — server-side field-level routing — actually landed (`FIELD_BY_CODE` + the three-hop handoff + a totality assertion). Both statements in REQUIREMENTS.md agree: checkbox `:656` `[x]`, row `:1087` Complete. **D-153.2-A verified genuinely closed, and the tick is honest.** |
| **WIZFORM-04** | 153.2-04 | A transient infrastructure failure never becomes a user decision | SATISFIED | Truths 18-20. ⚠️ The literal clause *"bounded automatic retry with backoff"* is deliberately NOT implemented — the requirement's own ⛔ overrides it (*"The fix is NOT 'add a retry loop' — that papers over the real question… Ask first whether the call is needed at all"*), and locked decision D-07 records the same. 153.2-04 took the requirement's prescribed route: the call is REMOVED for the venue where it can never answer, and both halves of the founder-observed incident are closed (the MT5 timeout no longer occurs; the *"switch to a different exchange"* copy is suppressed for MT5 via `context.venue`). Seam budgets and the `breaker:railway` circuit are untouched, as the requirement's ⚠️ demands. **See W-153.2-1 — traceability row `:1090` still reads Pending.** |
| **MT5-14** | 153.2-04 | An MT5 strategy can declare MT5 as its supported exchange in the wizard metadata step, preselected from the connected key | SATISFIED | Truths 15-17, 21. Both halves shipped in ONE commit as D-14 requires (`c9c51a0b` widening + pin re-cut; `18fa51fa` pin/preselect; `d6618b77` probe skip) — the widening never existed without the probe skip, so MT5 was never declarable-but-unsubmittable at any commit. **See W-153.2-1 — traceability row `:1083` still reads Pending.** |

**Orphaned requirements check:** REQUIREMENTS.md rows `:1088` (WIZFORM-02) and `:1089` (WIZFORM-03) both name Phase 153.2 as a partial owner without appearing in any plan's `requirements` field. Neither is orphaned in substance — both were advanced by 153.2-05 as recorded, orchestrator-directed scope widenings, and both are logged Partial with named remaining owners. Their remaining halves are listed under `deferred` above. No requirement is unaccounted for.

### Plan-04 Audit — the interrupted plan's test additions

Given a harder look, per the known interruption (tasks 1-2 committed by the first agent; task 3's ~570 uncommitted lines reviewed-then-finished by a second agent that found three defects in the inherited work):

| Item | Finding |
|---|---|
| The guard that could not fail | **FIXED and verified in the tree.** `route.test.ts:1043-1059` no longer compares two hand-typed constants. The comment states the defect explicitly (*"`PROBE_FAILURE_ENVELOPE.code !== PARSE_MISS_ENVELOPE.code` would compare two constants declared thirty lines apart in THIS file — true the moment they were typed, and unable to fail for any change to the route"*), and the assertion now reads `body.code` off the ROUTE'S OWN RESPONSE, with the named claim ordered BEFORE the broad `toEqual` so a collapse reports the explanation, not an object diff. |
| Anti-vacuity | Present where it matters. The class sweep at `:3706-3719` asserts `nonProbing.length > 0` with the message *"No venue opts out of the probe, so this sweep proves nothing"*, and `< SUPPORTED_EXCHANGES.length` on the other side. A sweep over an empty opt-out set cannot pass silently. |
| Oracle independence | The sweep's oracle is `venueSupportsScopeProbe` over `SUPPORTED_EXCHANGES` — a derivation the route does not share. It is not the route's behaviour compared against itself. An instance skip (`venue === "mt5"`) plus a second opt-out row would red here and nowhere else. |
| Do the rows actually fail? | **Measured, not assumed.** Neutering the gate reddened 3 rows by name (see Behavioral Spot-Checks). The `expectNoSeamCall` helper asserts on the spy, not on the mocked response — and the MT5 row installs a read-only mock DELIBERATELY so that a still-probing route would pass on the response and fail only on the spy. |
| Both call sites | Confirmed independently of the tests: `route.ts:1200` (single-key) and `:1442` (per composite member). Gating only one would have been the instance fix wearing a different hat; the composite class sweep at `:3742-3760` covers it. |

No defect remains in plan 04's test additions.

### Deferred-Items Ledger — verified against the live tree

| Entry | Ledger says | Live tree says | Verdict |
|---|---|---|---|
| **D-153.2-A** (WIZFORM-01 tick inconsistency) | CLOSED by 153.2-05 | `:656` `[x]` and `:1087` Complete — the two agree. The tick is honest: all four claiming plans' work is present, and the condition 153.2-01/-02 named as blocking (server-side field-level routing) actually landed. | **ACCURATE.** |
| **D-153.2-B** (9 bare citations, seam-citations RED) | CLOSED by 153.2-05, commit `afb74f0a` | `npx vitest run src/lib/seam-citations.invariant.test.ts` → **GREEN**. The entry already carries its own ✅ CLOSED header. | **ACCURATE — not stale.** The note that three of nine coordinates had ALREADY drifted before conversion is a real, load-bearing finding: a mechanical "name the symbol at that line" pass would have encoded the drift. |
| **D-153.2-C** (`mt5.py:242 → 363`) | Phase 153.3's; excluded from 153.2 | Confirmed still RED, and confirmed to be pure line drift under `analytics-service/` — a tree 153.2 owns no file in. | **ACCURATE. Not reported as a 153.2 gap.** |
| **D-153.2-D** (`KNOWN_CODELESS_FINALIZE_REJECTIONS` = 3, not 0) | Three arms need NEW copy members; deferred to the WIZFORM-02 close-out | See judgment below. | **REASONING HOLDS. Not a 153.2 gap.** |

**D-153.2-D judgment — does the executor's reasoning hold, or is it a gap?**

It holds, on four independent grounds:

1. **The number moved DOWN, not up.** `wizardErrors.invariant.test.ts:516` reads `= 3`, and the docblock at `:492-514` records the 5 → 3 transition with the two fixed arms DELETED from the ledger rather than annotated. The founder's standing rule against raising a fence to make a suite green was not violated — it was applied in the correct direction, and the docblock says so in terms (*"It is NOT deleted from this ledger to make a number smaller"*).
2. **The guard reds in BOTH directions**, so the debt cannot silently grow while it waits. Verified: the invariant passes at 3 and would red if a fourth code-less rejection appeared.
3. **The stated blocker is real, not a convenience.** I checked the two candidate members against their own copy: `SEAM_MISCONFIGURED` promises retrying will not help — false of a transient Supabase read; `SERVICE_UNREACHABLE` claims we never got an answer — false of the 502, where a 2xx arrived in an unreadable shape. Neither fits without asserting something untrue to the user.
4. **The precedent named is not hypothetical — it is in this phase's own diff.** `KEY_NETWORK_TIMEOUT` became a catch-all by exactly the "reach for the nearest member" move, and D-14b deleted it four commits earlier in this same sub-phase. Repeating the move to close a ledger row would have re-created the defect the phase was mid-way through deleting.

Additionally, **WIZFORM-02 is not one of this phase's three requirement IDs.** Minting three user-facing copy members in Phase 153.1's file, as an unplanned tail-end edit, would be scope creep into another phase's UI-SPEC surface. The entry carries a named owner and a live guard. Correctly deferred.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `src/lib/wizardErrors.ts` | 1423 | The word `TODO` inside a comment | ℹ️ Info | Historical prose (*"the `phase-17 hoist` TODO comments"*) describing Phase 17 markers that were REMOVED. Pre-existing (`git log -L` attributes the block to 153.1-04, not 153.2), and not a live debt marker. |
| `src/lib/wizardErrors.ts` | 453, 1457, 2378 | `SIZE_MB_PLACEHOLDER` | ℹ️ Info | A template-substitution identifier, not a stub. |

**Debt-marker gate: PASSED.** Zero `TBD` / `FIXME` / `XXX` / `HACK` markers across all 13 files this phase modified. Zero placeholder returns, zero empty handlers, zero console-log-only implementations. The `return null` occurrences in `validateDescription` / `validateDollarField` / `focusTargetFor` are the functions' meaningful "no error" / "not rendered" answers, consumed by callers — not empty implementations.

### Human Verification Required

Five items — see the `human_verification` frontmatter for the full text. In brief:

1. **Server-refusal path in a browser** — does the caret land on a VISIBLE AUM control? jsdom cannot answer this; the suite asserts `details.open` precisely because the `activeElement` line cannot carry the claim.
2. **Client-refused submit in a browser** — the same, on the other path through `revealAndFocus`, and the more common one.
3. **The two seeded Playwright specs** — declared not-run by 153.2-02 and 153.2-05, with the reason written out (TEST credentials + the shared-DB concurrency hazard) rather than dropped silently. The 28-file wizard vitest tree plus `localStorage.test.ts` cover the step machine's logic but not a real reload against a real DB.
4. **MT5 flag-ON end-to-end** — `MT5_UI_ENABLED` is OFF by default, so this entire surface is dark in CI and production. Nobody has watched an MT5 strategy finish a submit since the probe skip landed, and MT5-14 is filed a HARD BLOCKER.
5. **Focus-ring contrast measurement** — the suite asserts the class string and forbids `ring-accent/20`; it cannot measure a rendered ratio or see a clip.

Items 1, 2 and 4 were self-identified by the executors and folded into their own SUMMARYs rather than claimed as covered. That judgment was correct and is carried forward here rather than downgraded to passed.

### Gaps Summary

**No gaps.** All 25 must-have truths are verified against the codebase, including two that were mutation-tested rather than merely read. Every artifact exists, is substantive, is wired, and carries real data. Every key link is traced end to end — including the three-hop `SubmitStep → WizardClient → MetadataStep` handoff, which is the phase's newest and most breakable wiring.

The phase goal has two halves and both are met:

- **"refused inline, at the field, before submit"** — the submit button carries no validation `disabled` anywhere on the two owned surfaces; the refusal lives in a predicate that names the field, shows the copy-table rule inline, opens the collapsed disclosure and moves the caret. A refusal the SERVER raises now takes the same path, through the same merged derivation, so the user's experience does not depend on which side caught the problem.
- **"an MT5 strategy can both declare its venue and actually complete a submit"** — the chip set widened and the probe skip shipped in the same plan, so the declarable-but-unsubmittable half-state the ROADMAP warned about never existed at any commit. The skip is a capability question, gated at both call sites, failing TOWARD probing on an unresolved venue — and I proved the guard is load-bearing by breaking it.

Three items are deferred to later owners (WIZFORM-02's three code-less arms, WIZFORM-03's connect-step half, and 153.3's `mt5.py` line drift). All three are logged with named owners and live guards, and none is 153.2's to close.

Two documentation warnings (W-153.2-1, W-153.2-2) are surfaced for a human decision. Neither is user-facing or data-integrity, so neither blocks the phase under the standing stopping rule — but W-153.2-1 is the same inconsistency class the phase logged and closed as D-153.2-A, recurring unlogged for the other two requirement IDs, and the milestone audit reads that table.

Status is `human_needed` rather than `passed` solely because five behaviours cannot be closed without a browser. Nothing in the automated surface is failing.

---

_Verified: 2026-08-09T22:27:15Z_
_Verifier: Claude (gsd-verifier)_
