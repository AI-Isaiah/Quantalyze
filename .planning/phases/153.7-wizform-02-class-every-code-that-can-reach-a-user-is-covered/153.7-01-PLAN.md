---
phase: 153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seam-venue-vocabulary.invariant.test.ts
autonomous: true
requirements: [WIZFORM-02]

must_haves:
  truths:
    - "The derived population contains codes from BOTH shapes: an assignment-shape code (MISSING_SCOPE) AND a call-shape code (MT5_GATEWAY_UNCONFIGURED) — asserted, not inspected"
    - "A synthetic undisposed service_error(...) code under a routers/ directory reds the disposition gate BY NAME, via a permanent tmpdir falsifier running the REAL deriveEmitterSites"
    - "At plan end the disposition assertion is RED naming exactly the 20 undisposed codes — this red is the DESIGN PROPERTY proving the population widened; a green Plan 01 is a FAILED Plan 01"
    - "The matcher's discovered call sites agree exactly with the independent Python AST census in 153.7-RESEARCH.md Q2 (32 sites / 21 literal codes), with the reconciliation recorded"
  artifacts:
    - path: "src/lib/seam-venue-vocabulary.invariant.test.ts"
      provides: "Parameterised deriveEmitterSites(root), widened root, call-shape matcher with per-callee argument-slot table, pinned reasoned exclusions, EXPECTED_EMITTED_CODES re-cut to 37, DERIVED_FLOOR 22, reach pin, both-shapes assertion, tmpdir falsifier"
      contains: "CALLEE_ARG_SLOT"
  key_links:
    - from: "deriveEmitterSites (widened)"
      to: "the EXISTING disposition assertion (it: EVERY derived code has an explicit disposition)"
      via: "the `derived` set — the assertion body is UNTOUCHED; only its population widens"
      pattern: "VENUE_WIRE_CODES_WITHOUT_VERDICT\\.has"
    - from: "tmpdir falsifier"
      to: "deriveEmitterSites(root parameter)"
      via: "the REAL function called on a synthetic root — never a second copy of the scanner"
      pattern: "mkdtempSync"
---

<objective>
Widen the reach of the coverage law's Python scanner so its population is "every code that can
reach a user-facing surface" — the LOCKED boundary decision — instead of "codes in
`analytics-service/services/**` written as `error_code =` assignments". Mechanism only: this plan
is test-only, changes zero user-visible behaviour, and **ends with the disposition assertion RED
by design**, naming the 20 newly discovered undisposed codes.

Purpose: the Phase 153 span failed WIZFORM-02 because `routers/exchange.py`'s codes were invisible
to the scanner (wrong root AND wrong shape). The both-halves assertion already exists and is
already correct (`seam-venue-vocabulary.invariant.test.ts`, the "EVERY derived code has an
explicit disposition" it-block). The entire defect is the `derived` set it runs over.

Output: one modified file, `src/lib/seam-venue-vocabulary.invariant.test.ts`, with the widened
discovery, re-cut pins, two new reach assertions, and a permanent tmpdir falsifier. Final state:
exactly ONE failing test in that file, naming exactly 20 codes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

⚠️ Environment: GSD worktree agents get NO `node_modules` (measured — `npx vitest` exits 1 with
MODULE_NOT_FOUND). Every verify command in this plan requires a checkout with `node_modules`; if
executing in a fresh worktree, run `npm ci` first or execute from the main checkout.

⚠️ CI is Node 22, local may be Node 25. Before declaring the plan done, re-run the quick
verification under `PATH=/opt/homebrew/opt/node@22/bin:$PATH` if available. In any new test code
prefer `vi.spyOn` + `restoreAllMocks` over `vi.stubGlobal` (leaked stubs pass locally, fail CI).
</execution_context>

<context>
@.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-CONTEXT.md
@.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-RESEARCH.md
@.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-PATTERNS.md
@src/lib/seam-venue-vocabulary.invariant.test.ts
@src/lib/source-scan.ts
@analytics-service/tests/test_raw_5xx_census.py

Key sections: PATTERNS §1.1 (every convention in the file being modified, quoted verbatim —
`SERVICES_ROOT`, `pyFiles`, `ASSIGNMENT_RE`, `rhsWindow`, `deriveEmitterSites`, the assertion
shapes, the pin re-cut forms, the SELF-TEST idiom), PATTERNS §1.1f (the real Python call shapes),
PATTERNS §2.1–2.2 (the `test_raw_5xx_census.py` discipline and the Convention A verdict),
RESEARCH Q2 (the AST census oracle), Q3 (matcher hazards, measured), Q4 (pin inventory), Q5 (the
existing assertion + the two genuinely-new ones), Q6 (exclusions), Q8 (falsifier layers).
</context>

<constraints>
## Non-negotiable, from CONTEXT.md (locked) and measured research

1. ⛔ **DO NOT write a new both-halves assertion.** It exists at the "EVERY derived code has an
   explicit disposition — a verdict row or a reasoned exemption" it-block and is already correct.
   A duplicate passes for the same reason the original does and reads as progress in the diff.
   The work is: `derived` gets bigger. Nothing else.
2. **The root widening discovers ZERO new codes** (measured: the two sites outside `services/`
   — in `routers/cron.py` and `routers/process_key.py` — are literal-less forwards). All 20 new
   codes come from the SHAPE widening. Both changes are still required (the shape scan must be
   rooted where `routers/` and `main.py` live), but no step may claim the root change moves the
   count. If Task 1 alone changes the derived code set, the exclusions are wrong — stop.
3. **Pins are re-cut, never deleted.** Use the three re-cut forms in PATTERNS §3: Form 1 (dated
   arrow paragraph) for integers, Form 2 (oracle-change block, the `dynamicishByFile` precedent)
   for `EXPECTED_EMITTED_CODES` because the PREDICATE changes, Form 3 (arithmetic reconciliation)
   where a non-moving pin is the proof.
4. **Convention A only.** The matcher extends `stripCommentsPreserveLines(src, "py")` + a
   depth-aware window inside this same test file. Do NOT shell out to `python3` (no precedent in
   any TS test — that would be a third convention) and do NOT read a committed manifest produced
   by a Python guard (a second source of truth that greens when stale).
5. **No `file.py:NNN` line citations** in any new comment or pin. The `dynamicishByFile` re-cut
   docblock records why (line pins rot; a site moved 242 → 363 and nothing real had changed).
   Anchor on symbols and file→COUNT.
6. **RE-MEASURE every integer before pinning — never increment.** RESEARCH's numbers are valid
   only against HEAD `6ae4352d`. The expected values below are guidance; the pinned values must
   come from the executor's own scan output cross-checked against the AST census.
7. ⭐ **This plan ends RED.** After Task 3, `vitest run src/lib/seam-venue-vocabulary.invariant.test.ts`
   must fail with EXACTLY ONE failing test (the disposition assertion) naming exactly the 20
   codes listed in Task 3. Do NOT "fix" this red by adding disposition rows — that is the entire
   job of Plan 02, and doing it here would collapse the phase's red→green proof. Commit with the
   red; record the failure output verbatim for the SUMMARY.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Parameterise the derivation, widen the root, pin the exclusions — derived set must NOT move</name>
  <files>src/lib/seam-venue-vocabulary.invariant.test.ts</files>
  <action>
    Change `deriveEmitterSites()` to take its root as a parameter, `deriveEmitterSites(root: string)`
    (the two-line refactor RESEARCH Q8 names as the tmpdir-falsifier prerequisite — today it closes
    over the module-level `SERVICES_ROOT`). Replace `SERVICES_ROOT` (which points at
    `analytics-service/services`) with a widened root constant pointing at `analytics-service`
    (name it `ANALYTICS_ROOT` or similar); the production call site passes it explicitly.

    Add an exclusion mechanism to the walk (`pyFiles` currently has none — PATTERNS §1.1a notes
    this is a genuine gap, not an analog to copy). Compose the two existing patterns per PATTERNS
    §6: a hand-typed exclusion list where each entry carries a reason string subject to the same
    substantive-reason discipline as `VENUE_WIRE_CODES_WITHOUT_VERDICT` (length > 80, no "todo"),
    plus a hand-typed count pin on what each exclusion removes (the
    `EXPECTED_NON_LITERAL_STATUS_SITES` precedent from `test_raw_5xx_census.py`). Two exclusions,
    both measured in RESEARCH Q6:
    (a) any path containing a `tests` segment — copy `test_raw_5xx_census.py`'s exact predicate
    ("skipping any path containing a `tests` segment", implemented on path PARTS, not substring).
    Reason must name the three fictional codes it removes: SOME_FUTURE_CODE and
    SOME_FUTURE_UNLISTABLE_CODE (future-code fixtures in `test_long_fetch.py`) and
    CSV_PARSE_FAILED (in `test_process_key_200_discriminator.py`) — file names, no line numbers.
    (b) `services/error_contract.py` — the definition module: three `def service_error*` sites,
    one in-string mention, two internal `service_error_body(code, …)` pass-throughs; it mints no
    literal codes of its own (verified in RESEARCH, Assumption A4 — so the exclusion carries a
    count pin, not just a reason). Put the doctrine sentence above the list: "A guard that implies
    completeness it does not have is worse than none."

    Add a SELF-TEST in the file's established both-polarities idiom (PATTERNS §1.1e): a fixture
    path with a `tests` segment yields nothing; the same content outside it yields the code. The
    negative half is the load-bearing one.

    Re-cut `dynamicishByFile` from 8 keys to 10: the widened root adds two literal-less
    ASSIGNMENT sites, one each in `analytics-service/routers/cron.py` and
    `analytics-service/routers/process_key.py` (both forward a code computed in `services/`).
    Use re-cut Form 1 (dated arrow paragraph: "8 → 10 (2026-08-XX, Phase 153.7-01 / WIZFORM-02)…")
    stating both are literal-less forwards, so the root widening moves the blind-spot pin and
    NOTHING else.

    Explicitly assert in this task's verification that the derived code set is UNCHANGED at the
    same 17 codes — the root widening is necessary-but-empty (CONTEXT correction 2). If it moved,
    the exclusion predicate is wrong.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/lib/seam-venue-vocabulary.invariant.test.ts</automated>
    Expected: ALL tests green at end of Task 1. The roster assertion still matches 17 codes
    member-for-member; `dynamicishByFile` pin matches its 10 measured keys; new exclusion
    SELF-TEST green both polarities.
  </verify>
  <done>
    deriveEmitterSites takes root as a parameter; root widened to analytics-service with two
    pinned, reasoned, self-tested exclusions; derived codes unchanged at 17; file fully green.
  </done>
</task>

<task type="auto">
  <name>Task 2: The call-shape matcher — HIGHEST-RISK TASK; AST cross-check is the acceptance evidence</name>
  <files>src/lib/seam-venue-vocabulary.invariant.test.ts</files>
  <action>
    ⚠️ This is the phase's highest-risk task, for four measured reasons (RESEARCH "single
    highest-risk task"): a too-narrow matcher satisfies every absence assertion in the file (it
    fails OPEN — the disposition, fossil and exclusivity checks are all absence assertions); the
    obvious regex is 22% false-positive (41 vs 32 sites); the argument slot varies by callee; and
    a matcher bug freezes into `EXPECTED_EMITTED_CODES` in the same commit, where it thereafter
    reads as agreement.

    Extend the discovery in the SAME file (Convention A — PATTERNS §2.2 verdict) with call-shape
    matching over `stripCommentsPreserveLines(src, "py")` output. Implement:

    1. A per-callee argument-slot table (a named const beside the scanner, e.g.
       `CALLEE_ARG_SLOT`): `service_error` → positional slot 1, `service_error_response` →
       positional slot 1, `service_error_body` → positional slot 0 (⚠️ it takes the code FIRST —
       there is no status argument; a constant "second positional" rule silently mis-reads it),
       `VenueTransientHTTPException` → keyword `code=` (its constructor is keyword-only). The
       fourth shape is RESEARCH Q2's recommendation: it adds zero new codes today
       (NETWORK_UNAVAILABLE and RATE_LIMITED, both already discovered) and closes the live hole
       where a new 424 venue code minted only there would be invisible.
    2. A balanced-paren argument extractor generalising the existing `rhsWindow` depth tracker
       (~20 lines — PATTERNS §1.1a names it as the generalisation target). Multi-line calls are
       handled by construction; ⛔ do NOT add a `[\s\S]{0,N}` character cap (no
       `EMITTER_BODY_MAX_CHARS` analog — that cap exists for the Next-route matcher which has no
       bracket structure; a cap here recreates the "one long detail string goes invisible"
       failure the wizardErrors invariant documents).
    3. The three false-positive guards from RESEARCH Q3, each stated as a SELF-TEST assertion in
       both polarities, using the file's established idiom (one scanner, exercised by both the
       disk walk and the self-tests — never a second copy of the matcher inside a self-test):
       (a) `def` sites: a match preceded by `def` + whitespace is not a call — fixture
       `def service_error(` yields nothing, `raise service_error(` yields the code.
       (b) In-string mentions: `stripCommentsPreserveLines` deliberately PRESERVES ordinary
       string contents (source-scan semantics rule 2), so a sentence containing `service_error()`
       inside a string literal survives stripping. Task 1's exclusions (error_contract.py +
       tests/) already remove all six measured in-tree cases; regardless, add a SELF-TEST fixture
       with the callee name inside a string asserting it yields nothing — if the implementation
       relies on the exclusions rather than an expression-position check, the SELF-TEST comment
       must SAY so, so a future root change re-opens the question loudly.
       (c) Per-callee slot: one fixture per CALLEE_ARG_SLOT member, including a
       `service_error_body("CODE_FIRST", …)` fixture proving the code is read from slot 0.
    4. Literal-less code arguments (e.g. `service_error_body(result["error_code"], …)`,
       `VenueTransientHTTPException(code=validation["error_code"], …)`) are COUNTED into the same
       literal-less blind-spot site list the assignment scan keeps (codes: []) — never dropped.
       Multiplicity preserved throughout (no Set collapse before the blind-spot count — the
       `Counter`-not-`frozenset` discipline from the Python census).

    ⭐ MANDATORY MITIGATION (CONTEXT locked): after implementing, dump the matcher's discovered
    call sites (file → codes, with multiplicity) and reconcile line-by-line against the
    independent Python AST census in RESEARCH Q2 — 32 sites, 21 distinct literal codes, the
    per-file/per-code table, plus the PATTERNS §2.2 `routers/exchange.py` slice (six mt5-gateway
    emitters: UNCONFIGURED ×4, UNREACHABLE ×2). The two derivations must agree exactly, after
    explicitly reconciling the deliberate deltas: error_contract.py's two internal forwards are
    excluded by Task 1, and the AST census's `lineno` is the raise head while a grep for the
    literal lands two lines later (anchor on symbols, not lines). The reconciliation report is
    this task's acceptance evidence and goes verbatim into the SUMMARY. Any unexplained
    disagreement is a matcher bug until proven a census bug.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/lib/seam-venue-vocabulary.invariant.test.ts -t "SELF-TEST"</automated>
    All SELF-TESTs green (both polarities, one per matcher hazard). Then run the full file:
    EXPECTED intermediate state — the roster assertion and the disposition assertion are now RED
    (the derived set grew past the 17-member roster; 20 codes are undisposed). That red is
    Task 3's input, not a defect. The AST reconciliation report shows exact agreement.
  </verify>
  <done>
    Call-shape matcher implemented per CALLEE_ARG_SLOT; self-tests cover def-site, in-string,
    per-callee slot, literal-less counting, multi-line; AST cross-check agreement recorded.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-cut the pins, add the two reach assertions, add the tmpdir falsifier — end RED naming exactly 20</name>
  <files>src/lib/seam-venue-vocabulary.invariant.test.ts</files>
  <action>
    Re-cut the pin family against the executor's OWN measured scan output (guidance values from
    RESEARCH Q4, re-measure before pinning):

    1. `EXPECTED_EMITTED_CODES` 17 → 37 members, hand-typed (⛔ NEVER `[...derived]` — oracle
       independence; a roster pinned from its own derivation cannot fail). The 20 new members are
       the Task-2 discovery cross-checked against the AST census. Use re-cut Form 2 (the
       `dynamicishByFile` oracle-change block style) because the PREDICATE changed, and restate
       the ENTIRE new predicate in the docblock: root, the two pinned exclusions, both shapes
       (assignment + the four-callee call family with per-callee slots), replacing the stale
       "Measured at 140.5-02 under the predicate above: 17 distinct codes" sentence with the new
       measurement, dated 153.7-01.
    2. `DERIVED_FLOOR` 10 → 22, hand-typed, with the arithmetic stated (~60% of 37) — ⛔ never
       derived from `derived.size` (an anti-pattern the file itself documents). Update the
       assertion's failure-message predicate prose to match the new predicate (the existing
       message restates the whole predicate in prose — carry that convention forward).
    3. `dynamicishByFile`: add any literal-less CALL sites the new shape scan sees (expected:
       `routers/exchange.py` gains counts from the two dynamic `VenueTransientHTTPException`
       forwards — measure, do not assume the count).

    Add the two genuinely-NEW assertions from RESEARCH Q5 (these are reach checks, NOT a second
    both-halves check):
    4. A REACH PIN: hand-typed roots + callee names the discovery uses, asserted so that removing
       `routers/` from the walk or dropping a callee from CALLEE_ARG_SLOT reds BY NAME instead of
       silently shrinking the population. Structural model: `SEAM_CITATION_SURFACE`'s roster pin
       (hand-typed population, length-pinned, existence-checked, widening rule in the failure
       message).
    5. A BOTH-SHAPES-PRESENT assertion: `derived.has("MISSING_SCOPE")` (assignment shape) AND
       `derived.has("MT5_GATEWAY_UNCONFIGURED")` (call shape), with a failure message naming
       which shape each code proves. This is PRD success criterion 1 ("proven by both shapes
       appearing in the discovered set, not by inspection") and makes "the shape matcher silently
       stopped matching" impossible to ship green.

    Add the Layer-3 tmpdir falsifier (RESEARCH Q8; idiom established in
    `src/__tests__/gitleaks-allowlist.test.ts` and four siblings — no new technique):
    6. `mkdtempSync` a synthetic root → write `<tmp>/routers/zzz.py` containing an undisposed
       `service_error(500, "ZZZ_FALSIFIER_ROUTER_CODE", …)` call (multi-line, one argument per
       line, matching the real emitter shape in PATTERNS §1.1f) → run the REAL
       `deriveEmitterSites(tmpRoot)` and the REAL disposition filter (both halves' Maps) →
       assert the undisposed list is exactly `["ZZZ_FALSIFIER_ROUTER_CODE"]` → `rmSync` in a
       finally. ⛔ Never a second copy of the scanner; ⛔ never a committed throwaway `.py` under
       `analytics-service/` (it would be discovered by this scan, by `test_raw_5xx_census.py`,
       and by mypy/ruff, and deleting it later re-opens the hole).

    Optionally, as a SUMMARY receipt only (not a mechanism): the one-off manual mutation — add a
    real undisposed code literal in `routers/`, run, record the red, revert, confirm
    `git status --short` is empty.

    ⭐ END STATE — RED BY DESIGN. Do NOT add disposition rows to green the file. Record the
    failure output verbatim for the SUMMARY: it is the phase's proof-of-life.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/lib/seam-venue-vocabulary.invariant.test.ts 2>&1 | tee /tmp/153.7-01-red.txt; test $? -ne 0</automated>
    Expected: exit ≠ 0 with EXACTLY ONE failing test — "EVERY derived code has an explicit
    disposition — a verdict row or a reasoned exemption" — naming exactly these 20 codes and no
    others: ADAPTER_INIT_FAILED, ADMIN_CHECK_UNAVAILABLE, ANALYTICS_ROW_NOT_CREATED,
    EGRESS_PROXY_MISCONFIGURED, EVAL_FAILED, EVAL_WINDOW_TOO_LARGE, EXCHANGE_PROBE_FAILED,
    INTERNAL, INTERNAL_TOKEN_UNCONFIGURED, KEK_UNAVAILABLE, KEY_MISSING_EXCHANGE,
    KEY_UNDECRYPTABLE, MT5_GATEWAY_UNCONFIGURED, MT5_GATEWAY_UNREACHABLE,
    PORTFOLIO_ANALYTICS_FAILED, ROLE_CHECK_UNAVAILABLE, SCORING_FAILED, SERVICE_KEY_UNCONFIGURED,
    SIMULATION_FAILED, UNAUTHENTICATED. Every OTHER test in the file green: roster (37 members,
    member-for-member), floor (≥22), both-shapes, reach pin, falsifier, all SELF-TESTs,
    dynamicishByFile, fossil, exclusivity, substantive-reason.
    Also: `node_modules/.bin/vitest run src/lib/wizardErrors.invariant.test.ts` stays fully green
    (untouched), and `cd analytics-service && python3 -m pytest -q tests/test_raw_5xx_census.py`
    stays 19 passed (run FROM analytics-service/ with python3, never repo root — VCR cassettes
    miss otherwise).
  </verify>
  <done>
    Pins re-cut with reasoning carried forward; reach pin + both-shapes assertions in place;
    tmpdir falsifier green; the disposition assertion RED naming exactly the 20; red output
    captured for SUMMARY; committed in this state.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics-service wire → TS classifier | the `code` this scanner inventories arrives over the network at runtime; the scanner itself reads only local source at test time |
| test process → os.tmpdir() | the falsifier writes a synthetic tree to a mkdtempSync path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.7-01 | Tampering | matcher fails OPEN (too narrow → every absence assertion passes) | mitigate | AST cross-check (Task 2, mandatory), hand-typed DERIVED_FLOOR 22, both-shapes assertion, reach pin |
| T-153.7-02 | Repudiation | silent exclusion of user-reachable emitters | mitigate | exclusions are hand-typed with >80-char reasons + count pins + both-polarity SELF-TESTs |
| T-153.7-03 | Tampering | falsifier tests a copy of the scanner, not the wired one | mitigate | falsifier calls the REAL deriveEmitterSites(root) — one scanner, exercised both ways |
| T-153.7-04 | Information disclosure | tmpdir fixture leakage | accept | fixture contains only a synthetic code literal; rmSync in finally; no secrets involved |
| T-153.7-SC | Tampering | npm/pip/cargo installs | accept | this phase installs ZERO packages (RESEARCH: "Not applicable", recorded deliberately) |
</threat_model>

<verification>
Whole-plan check: `node_modules/.bin/vitest run src/lib/seam-venue-vocabulary.invariant.test.ts
src/lib/wizardErrors.invariant.test.ts` → seam-venue file has exactly one designed failure (the
disposition assertion, 20 names); wizardErrors invariant fully green; pytest census (from
`analytics-service/`, `python3 -m pytest -q tests/test_raw_5xx_census.py`) 19 passed. Re-run the
quick command under Node 22 PATH before hand-off.
</verification>

<success_criteria>
- deriveEmitterSites is root-parameterised; root = analytics-service/** minus two pinned, reasoned,
  self-tested exclusions; derived set unchanged by the root move alone (17 → 17 after Task 1)
- Call-shape matcher covers service_error / service_error_response / service_error_body /
  VenueTransientHTTPException with per-callee slots; SELF-TESTs both polarities for every measured
  hazard class; literal-less args counted, not dropped; no character cap
- Matcher output reconciled exactly against the RESEARCH Q2 AST census — report in SUMMARY
- EXPECTED_EMITTED_CODES = 37 hand-typed, DERIVED_FLOOR = 22 hand-typed, dynamicishByFile
  re-measured; reach pin + both-shapes assertions added; tmpdir falsifier green
- Final state: EXACTLY ONE red test naming exactly the 20 undisposed codes — recorded verbatim.
  ⭐ A Plan 01 that ends green has NOT widened the population and must not be reported complete.
</success_criteria>

<output>
Create `.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-01-SUMMARY.md`
when done. Must include: the verbatim red output (the 20 names), the AST reconciliation report,
and the optional manual-mutation receipt if performed.
</output>
