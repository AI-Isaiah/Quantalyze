---
phase: 159-rank-public-ranking-integrity
plan: 03
type: execute
wave: 3
depends_on: ["159-02"]
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.test.ts
  - src/app/(dashboard)/compare/page.tsx
  - src/app/discovery/[slug]/[strategyId]/page.tsx
  - src/components/strategy/StrategyTable.tsx
autonomous: true
requirements: [RANK-02]

estimate:
  tokens: 85000
  raw_tokens: 85000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Anonymous readers receive only the columns the public surface needs: daily_returns, metrics_json, and data_quality_flags are absent from every anon-reachable strategy_analytics response (RANK-02)"
    - "The WHOLE splat class is closed, not the two named sites: all four sites at HEAD (queries.ts getStrategiesByCategory, getMyStrategies, getStrategyDetail; compare/page.tsx) are classified and dispositioned (D-02)"
    - "The owner-only site getMyStrategies keeps its splat with a one-line exemption comment citing the .eq user_id scoping (D-02)"
    - "Anon list rows still carry sparkline_returns, sparkline_drawdown, and computation_status — the enumerated consumer reads that must survive the projection (edge: over-narrow projection blanks table sparklines and breaks the Phase-147 series-state logic)"
    - "The anon strategy-detail projection retains computation_status; the data_quality_flags tension is resolved by caller-scoped projections — the anon public variant excludes it, the authed discovery variant retains it (RESEARCH Open Question 2, resolved explicitly)"
    - statement: "The rendered output of the browse, strategy-detail, discovery, and compare pages is unchanged by the projections (this phase has zero visual surface; the only decided visible changes are the census-surfaced rank disappearances, which belong to plan 02's gate, not to these projections)"
      verification: backstop
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "Explicit projection constants for the converted sites + the owner exemption comment"
      contains: "sparkline_returns"
    - path: "src/app/(dashboard)/compare/page.tsx"
      provides: "Explicit analytics projection replacing the wildcard embed"
      contains: "sharpe"
  key_links:
    - from: "src/lib/queries.ts getStrategiesByCategory"
      to: "src/components/strategy/StrategyTable.tsx"
      via: "list projection feeds StrategyTable rows — sparklines, computation_status, and the 3M filter field must arrive"
      pattern: "sparkline_returns"
    - from: "src/app/(dashboard)/compare/page.tsx"
      to: "src/lib/utils.ts EMPTY_ANALYTICS"
      via: "projected partial analytics composed over EMPTY_ANALYTICS defaults so the StrategyAnalytics-typed downstream reads stay total"
      pattern: "EMPTY_ANALYTICS"
  prohibitions:
    - statement: "No anon-reachable select projects daily_returns, metrics_json (as a whole blob), or data_quality_flags"
      status: unverified
      flagged: true
      verification: "region-scoped grep of the getStrategiesByCategory and getStrategyDetail public-variant projection constants and the compare/page.tsx select string: zero occurrences of the three column names as projected columns"
    - statement: "PERCENTILE_ANALYTICS_COLUMNS remains byte-identical (this plan touches queries.ts but not the percentile constants)"
      status: unverified
      flagged: true
      verification: "grep of src/lib/queries.ts still matches the exact seven-member value string"
    - statement: "No task alters visual output — the 3M advanced filter keeps working via the aliased sub-key (or its degradation is explicitly refused and escalated, never silently shipped)"
      status: unverified
      flagged: true
      verification: "SUMMARY records the alias verification outcome against TEST; StrategyTable's 3M filter read site resolves the projected field"
---

<objective>
Close RANK-02 as a class (D-02): every `strategy_analytics (*)` splat at HEAD is classified
anon-reachable vs owner-only; anon-reachable sites become explicit projections excluding
`daily_returns`/`metrics_json`/`data_quality_flags`; the owner-only site keeps its splat
with an exemption comment. RLS is row-level and cannot hide columns — projection is the
only lever.

Purpose: ROADMAP 159 SC-3; the `readPublicVerificationSignals`/SECDEF column-explicitness
precedent extended to the analytics embeds.
Output: converted projections at three sites, one documented exemption, select-string pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md
@.planning/phases/159-rank-public-ranking-integrity/159-PATTERNS.md
@src/lib/queries.ts
@src/lib/database.types.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–07)

See 159-01-PLAN.md for the full phase-wide list. This plan contributes:
- `src/lib/queries.ts` — NEW exported projection constants. `STRATEGY_DETAIL_PUBLIC_ANALYTICS_COLUMNS` and `STRATEGY_DETAIL_DISCOVERY_ANALYTICS_COLUMNS` are pinned BY NAME (Task 2's verify gate greps the public constant's declaration region — renaming requires updating that gate in lockstep); the Task-1 list constant (e.g. `CATEGORY_RANKING_ANALYTICS_COLUMNS`) may follow the `STRATEGY_V2_*` house style. Plus the owner-splat exemption comment
- `src/app/(dashboard)/compare/page.tsx` — explicit compare-page analytics projection
- `src/components/strategy/StrategyTable.tsx` — 3M filter read site moved to the aliased projected field (only if the alias arm is taken)
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Convert getStrategiesByCategory (the anon browse list) to an explicit projection</name>
  <precondition>node_modules is installed in the working checkout (GSD worktrees ship none — npm ci first or run from an installed checkout)</precondition>
  <files>src/lib/queries.ts, src/lib/queries.test.ts, src/components/strategy/StrategyTable.tsx</files>
  <read_first>
    - src/lib/queries.ts lines 195-300 (getStrategiesByCategory :210 splat; shapeRankingRows :251 and its computation_status dependency; the :332 comment block documenting the splat's JSONB payload) and :1118-1139 (getStrategyDetailV2's path-extraction projection — the house analog to copy, including its must-stay guard-comment style and the :1130-1132 JSONB-sub-tree claim)
    - src/components/strategy/StrategyTable.tsx (enumerate EVERY strategy_analytics field it reads — known: sparkline_returns ~:1111, sparkline_drawdown ~:1118, the 3M advanced filter reading metrics_json three_month ~:567-568; there may be more — enumerate before cutting, Pitfall 5)
    - src/app/browse/[slug]/page.tsx and the authed discovery/[slug]/page.tsx callers (confirm the anon reachability classification still holds at HEAD)
    - src/lib/database.types.ts strategy_analytics Row (the full column universe to project FROM)
  </read_first>
  <behavior>
    - Test 1: the select string getStrategiesByCategory issues contains an explicit strategy_analytics column list; daily_returns, metrics_json, and data_quality_flags are not projected.
    - Test 2: the projected list includes sparkline_returns, sparkline_drawdown, computation_status, and every additional field the StrategyTable enumeration found.
    - Test 3: the 3M filter field arrives on the row under its aliased name (if the alias arm is taken).
  </behavior>
  <action>
    Enumerate first, cut second (Pitfall 5): list every strategy_analytics field consumed
    by shapeRankingRows and StrategyTable, then define an exported constant (house style of
    STRATEGY_V2_STRATEGY_COLUMNS, e.g. CATEGORY_RANKING_ANALYTICS_COLUMNS) carrying exactly
    those columns MINUS daily_returns, metrics_json, data_quality_flags, and replace the
    wildcard embed in getStrategiesByCategory's select at ~:210 with it. Carry
    getStrategyDetailV2-style guard comments naming the must-stay columns
    (sparkline_returns, sparkline_drawdown, computation_status) and why.

    The 3M advanced filter (StrategyTable reading metrics_json three_month): preserve it
    WITHOUT projecting the whole metrics_json blob by alias-projecting the single JSON key
    — the PostgREST arrow-operator select form aliasName colon metrics_json arrow
    three_month (assumption A4). VERIFY the alias against the TEST database before landing:
    a throwaway read using env from .env.test.local (NEVER .env.local — that is PROD)
    issuing the aliased select and printing the row shape; record the observed output in
    the SUMMARY. If the alias works: update StrategyTable's read site to the aliased field
    and CORRECT the queries.ts :1130-1132 comment that claims PostgREST cannot project a
    JSONB sub-tree (surface the conflict, pick the measured truth — house Rule 7). If the
    alias is REJECTED by PostgREST (measured, not assumed): do NOT silently degrade the
    filter — that would change visual behavior, contradicting the phase's zero-visual-
    surface constraint; halt this arm, record the measurement, and surface the
    accept-degradation-vs-keep-blob decision in the SUMMARY as an explicit escalation.

    Extend queries.test.ts with select-string capture pins (the mock-builder technique from
    the existing harness): assert the exact projection string; negative-assert the three
    excluded columns. Neuter drill: add one excluded column to the constant → RED → restore.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.test.ts --no-file-parallelism && ! awk '/export async function getStrategiesByCategory/{f=1;next} f&&/^export /{f=0} f' src/lib/queries.ts | grep -q 'strategy_analytics (\*)'</automated>
  </verify>
  <acceptance_criteria>
    - The getStrategiesByCategory region (its function body) contains no wildcard analytics embed; the exported projection constant exists with guard comments
    - Select-string pins pass and were observed RED under the neuter drill
    - The consumer enumeration (every field StrategyTable + shapeRankingRows read) is recorded in the SUMMARY with file:line evidence
    - The TEST-DB alias verification outcome is recorded verbatim (row shape or rejection error) — a decision made on measurement, not on the :1130 comment or on memory
    - `/browse` renders identically: sparklines present, ranking badges present, 3M filter functional (dev-server spot check against TEST data, described in SUMMARY)
  </acceptance_criteria>
  <done>The highest-traffic anon surface receives an explicit, consumer-complete projection with the three sensitive columns absent — proven end-to-end against real TEST data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert getStrategyDetail (caller-scoped) and compare/page.tsx</name>
  <files>src/lib/queries.ts, src/lib/queries.test.ts, src/app/(dashboard)/compare/page.tsx</files>
  <read_first>
    - src/lib/queries.ts lines 900-975 (getStrategyDetail :936-937 ternary splat strings and its callers' contract)
    - src/app/strategy/[id]/page.tsx lines 20-120 (the ANON caller — getUser optional, no redirect; enumerate its analytics reads) and src/app/discovery/[slug]/[strategyId]/page.tsx lines 30-130 (the AUTHED caller — reads data_quality_flags ~:85 and depends on computation_status arriving ~:123)
    - src/app/(dashboard)/compare/page.tsx lines 1-99 (the :68 splat; the :84-88 cast through EMPTY_ANALYTICS fallback; enumerate every analytics field the compare UI consumes downstream)
    - src/lib/utils.ts (EMPTY_ANALYTICS shape — the defaults that keep the StrategyAnalytics type total over a partial projection)
  </read_first>
  <behavior>
    - Test 1: getStrategyDetail called for the public variant issues a projection excluding daily_returns, metrics_json, data_quality_flags and including computation_status.
    - Test 2: getStrategyDetail called for the discovery variant additionally includes data_quality_flags (authed surface keeps its read).
    - Test 3: compare/page.tsx's select string projects an explicit analytics list excluding the three columns.
  </behavior>
  <action>
    Resolve RESEARCH Open Question 2 with caller-scoped projections: give getStrategyDetail
    a variant parameter (public | discovery, default public) selecting between two exported
    constants — STRATEGY_DETAIL_PUBLIC_ANALYTICS_COLUMNS (excludes daily_returns,
    metrics_json, data_quality_flags; RETAINS computation_status — mandatory) and
    STRATEGY_DETAIL_DISCOVERY_ANALYTICS_COLUMNS (public list plus data_quality_flags).
    Column contents = the enumeration of each page's actual analytics reads (read both
    pages first; Pitfall 5). Update the two callers: strategy/[id]/page.tsx stays on the
    default public variant; discovery/[slug]/[strategyId]/page.tsx passes discovery. Keep
    the discovery page's :123 computation_status dependency true and say so in a guard
    comment on the constant.

    In compare/page.tsx: replace the :68 wildcard select with an explicit analytics list
    derived from the compare UI's enumerated reads, excluding the three columns
    (requirement names this site explicitly even though it is authed-allocator). Compose
    the projected partial over EMPTY_ANALYTICS at the existing :88 cast site (spread
    defaults first, projected fields second) so downstream StrategyAnalytics-typed reads
    stay total. The replacement comment must describe the former wildcard embed WITHOUT
    reproducing the literal splat string (a class-closure grep in Task 3 counts it).

    Extend queries.test.ts with select-string pins for both getStrategyDetail variants.
    Neuter drills on each new pin.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.test.ts --no-file-parallelism && ! grep -q 'strategy_analytics (\*)' "src/app/(dashboard)/compare/page.tsx" && awk '/const STRATEGY_DETAIL_PUBLIC_ANALYTICS_COLUMNS/{f=1} f{print; if (/;/) exit}' src/lib/queries.ts | grep -q 'computation_status'</automated>
  </verify>
  <acceptance_criteria>
    - compare/page.tsx contains zero occurrences of the wildcard analytics embed (negative grep passes — including comments)
    - Both getStrategyDetail variants pin their exact select strings; the public variant's constant does not contain the three excluded column names; the discovery variant's constant contains data_quality_flags
    - Both page callers compile (tsc) and their enumerated analytics reads all resolve against the projected shape (enumeration recorded in SUMMARY with file:line)
    - The anon /strategy/[id] page and the authed discovery detail page render identically on TEST data (dev-server spot check in SUMMARY)
  </acceptance_criteria>
  <done>The detail path serves anon readers a minimal explicit projection while the authed discovery surface keeps its data_quality_flags read — the tension resolved explicitly, per-caller, never silently.</done>
</task>

<task type="auto">
  <name>Task 3: Owner exemption, prose reconciliation, and the class-closure gate</name>
  <files>src/lib/queries.ts</files>
  <read_first>
    - src/lib/queries.ts lines 295-345 (getMyStrategies :310 splat + the :311 user_id scoping + the :332 payload-documentation comment block) and :1120 (the getStrategyDetailV2 docblock referencing the wildcard form)
    - The comment-only references RESEARCH enumerates: src/app/discovery/[slug]/[strategyId]/page.tsx ~:123, src/app/(dashboard)/my-strategies/page.test.tsx ~:17/:60, src/lib/queries.test.ts ~:662, src/lib/phase-147-series-resolution-guards.test.ts ~:38 (locate by grep — refs drift), src/lib/queries.has-any-own-strategies.test.ts ~:15
  </read_first>
  <action>
    getMyStrategies keeps its wildcard embed (D-02 owner-only disposition: the query is
    scoped by the user_id equality at ~:311, so the caller only ever receives their own
    rows). Add the one-line exemption comment directly at the select site naming the
    scoping fact and citing D-02/RANK-02.

    Reconcile prose: re-read each comment RESEARCH enumerates and correct only sentences
    this phase made false (surgical — house Rule 3): the :332 block's description of what
    the splat feeds (if it described the now-converted sites), and any test-comment prose
    that now misdescribes a converted site. The my-strategies page test pins the OWNER
    splat shape — that pin stays true and untouched.

    Class-closure gate (D-02 "whole class"): run a repo-wide grep for the wildcard
    analytics embed across src/. Expected surviving occurrences: the getMyStrategies
    select site, plus comment/test lines that legitimately document the owner shape or
    history. Classify EVERY hit in the SUMMARY (site -> disposition). If an unclassified
    CODE occurrence exists (a fifth splat the inventory missed), convert or exempt it in
    this task — close the class, not the list.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.test.ts src/lib/queries.percentiles.test.ts --no-file-parallelism && awk '/export async function getMyStrategies/{f=1;next} f&&/^export /{f=0} f' src/lib/queries.ts | grep -q '\.select(.*strategy_analytics (\*)' && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The getMyStrategies region still contains the splat WITH the adjacent exemption comment citing the user_id scoping (region-scoped positive grep on the `.select(` line — the region's :332 docblock also carries the splat literal, so the gate pins the CODE occurrence, not prose)
    - The SUMMARY carries the complete repo-wide hit list with a disposition for every occurrence — zero unclassified code hits
    - tsc --noEmit clean; both queries test files green
    - Prose corrections listed in the SUMMARY with before/after sentences (only falsified sentences touched)
  </acceptance_criteria>
  <done>The splat class is closed with an auditable inventory: three conversions, one documented owner exemption, and no unclassified occurrence anywhere in src/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon browser → RSC pages → PostgREST | anon-reachable reads of strategy_analytics; RLS is row-level only and cannot hide columns |
| authed allocator → compare page | cross-tenant reads of published strategies |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-08 | Information Disclosure | daily_returns/metrics_json/data_quality_flags reaching anon responses | high | mitigate | explicit projections at all anon-reachable sites (Tasks 1-2) + negative select-string pins + class-closure inventory (Task 3) |
| T-159-09 | Information Disclosure | a future fifth splat re-opening the class | medium | mitigate | select-string pins are neuterable regression tests; Task 3's classified inventory is the audit baseline |
| T-159-10 | Denial of Service | over-narrow projection blanking sparklines/series-state (user-facing breakage) | medium | mitigate | enumerate-before-cut discipline + must-stay guard comments + dev-server render checks on TEST data |
</threat_model>

<verification>
- Task gates above (select-string pins, region-scoped positive/negative greps, tsc).
- The A4 alias outcome is a MEASURED decision recorded in the SUMMARY — never assumed
  either way.
- Phase-level: `npm run lint`; affected-suite sweep including my-strategies/page.test.tsx.
</verification>

<success_criteria>
- ROADMAP 159 SC-3 holds for the WHOLE class: anon responses lack the three columns at
  every reachable site, consumers keep every field they read, the owner exemption is
  documented at the site, and the closure is auditable from the SUMMARY inventory.
</success_criteria>

<output>
Create `.planning/phases/159-rank-public-ranking-integrity/159-03-SUMMARY.md` when done,
recording: the per-site consumer enumerations, the A4 alias measurement, the class-closure
hit inventory with dispositions, and the render spot-check results.
</output>
