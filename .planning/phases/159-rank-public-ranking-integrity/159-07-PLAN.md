---
phase: 159-rank-public-ranking-integrity
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizard/localStorage.ts
  - src/lib/wizard/localStorage.test.ts
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-durable-mint.test.tsx
  - src/lib/visibility.ts
  - src/lib/visibility.test.ts
autonomous: true
requirements: [RANK-08, RANK-09]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The re-mint fingerprint accounts for classification: a classification change on a burned session produces a different fingerprint, so the classification-conflict 409's own remedy can mint a fresh session (RANK-08, D-05 default arm — include, not document-the-exclusion)"
    - "An identical resubmit (same name, series, AND classification) produces the SAME fingerprint — the burn fence still blocks true duplicates (edge: widening must not free real duplicates)"
    - "Both WizardClient fingerprint call sites pass the classification inputs AND both React dep arrays carry the classification state — a fingerprint reading stale state via a missing dep is the silent failure mode (edge: Pitfall 7)"
    - "The signature docblock's falsified claim (date/daily_return as the only fields reaching csv-finalize) is corrected in the same edit (D-05 evidence: metadata classification also reaches csv-finalize and is what the 409 refuses on)"
    - "withPublishedOrOwner validates the uid as a strict UUID BEFORE interpolating into the PostgREST .or() filter; a non-conforming uid fails CLOSED to the published-only predicate and fails LOUD — never a permissive fallback (RANK-09, D-06)"
    - "An injection-shaped uid never reaches .or(): the fail-closed arm calls the published-only equality and .or is never invoked (edge: PostgREST filter-string injection)"
    - statement: "A pre-change persisted burn fingerprint never equals a post-change fingerprint, so stuck sessions re-mint (the safe direction); the server-side series-equality refusal remains the operative fence"
      verification: backstop
  artifacts:
    - path: "src/lib/wizard/localStorage.ts"
      provides: "csvSubmissionSignature widened with NUL-separated classification fields; corrected docblock"
      contains: "csvSubmissionSignature"
    - path: "src/lib/visibility.ts"
      provides: "uid shape validation with fail-closed published-only arm"
      contains: "isUuid"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      to: "src/lib/wizard/localStorage.ts csvSubmissionFingerprint"
      via: "both call sites pass categoryId + assetClass; both effect dep arrays include them"
      pattern: "assetClass"
    - from: "src/lib/visibility.ts"
      to: "src/lib/utils.ts isUuid"
      via: "the house UUID validator gates the interpolation — never a new regex"
      pattern: "isUuid"
  prohibitions:
    - statement: "The fingerprint is NOT upgraded to a cryptographic hash — it is non-cryptographic BY DESIGN (localStorage.ts docblock); the widening adds fields to the signature, nothing else"
      status: unverified
      flagged: true
      verification: "the diff adds no crypto/hash import to localStorage.ts; csvSubmissionFingerprint's FNV-1a mechanism is unchanged"
    - statement: "visibility.ts stays pure and client-safe — no server-only import (next/headers, @/lib/supabase/server, Sentry server SDK) is added; console.error is the logging floor unless captureToSentry is verified client-pure"
      status: unverified
      flagged: true
      verification: "grep of src/lib/visibility.ts imports: only pure modules; the module-header purity comment stays true"
    - statement: "The malformed-uid arm never interpolates the input anywhere — not into .or(), not into a log message template that could echo attacker content into structured logs unsanitized beyond plain console.error"
      status: unverified
      flagged: true
      verification: "source read of the fail-closed arm: .or unreachable; the console.error call is present and the raw uid appears only as a console.error argument, never in a filter string"
---

<objective>
Close the two client-lib integrity gaps: RANK-08 — the CSV re-mint fingerprint includes
classification (D-05 default arm) so the classification-conflict 409's remedy actually
works; RANK-09 — `withPublishedOrOwner` validates the uid's shape with the house `isUuid`
before PostgREST `.or()` interpolation, failing closed to published-only (D-06).

Purpose: ROADMAP 159 SC-5 second and third arms.
Output: widened signature + wired WizardClient deps + fail-closed visibility predicate,
all pinned by tests observed RED.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md
@.planning/phases/159-rank-public-ranking-integrity/159-PATTERNS.md
@src/lib/wizard/localStorage.ts
@src/lib/visibility.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–07)

See 159-01-PLAN.md for the full phase-wide list. This plan contributes:
- `src/lib/wizard/localStorage.ts` — `csvSubmissionSignature` widened (categoryId + assetClass params, NUL-separated), `csvSubmissionFingerprint` passing them through; corrected docblock
- `src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx` — both call sites + both dep arrays carry classification (NOTE: RESEARCH/PATTERNS cite this file at a stale `(dashboard)/submit/` path; the real path is `(dashboard)/strategies/new/wizard/`)
- `src/lib/visibility.ts` — uid shape validation, fail-closed published-only arm
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Widen the fingerprint signature with classification (RED→GREEN)</name>
  <precondition>node_modules is installed in the working checkout (GSD worktrees ship none — npm ci first or run from an installed checkout)</precondition>
  <files>src/lib/wizard/localStorage.ts, src/lib/wizard/localStorage.test.ts</files>
  <read_first>
    - src/lib/wizard/localStorage.ts lines 640-720 (csvSubmissionSignature :664-672 with the NUL-separator convention and the falsified :661-662 docblock claim; csvSubmissionFingerprint :702-717 and its :676-700 design docblock including the non-cryptographic-by-design statement)
    - src/lib/wizard/localStorage.test.ts (existing fingerprint/signature test structure)
    - .planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md §RANK-08 (the D-05 evidence: no cross-classification-dedupe intent exists; the falsified docblock premise; the safety-of-widening analysis)
  </read_first>
  <behavior>
    - Test 1 (RED first): two signatures identical except categoryId differ → different fingerprints (pre-widening they are equal — the defect).
    - Test 2: identical name + series + categoryId + assetClass → identical fingerprint (true duplicates still fence).
    - Test 3: assetClass change alone → different fingerprint.
    - Test 4: field-boundary non-ambiguity — a value ending where the next begins cannot collide across the NUL separators (extend the existing boundary pins to the new fields).
    - Test 5: null/undefined classification inputs serialize deterministically (a stable sentinel, not the string undefined by accident — pick and pin the exact serialization).
  </behavior>
  <action>
    D-05 evidence gate first: grep localStorage.ts, WizardClient.tsx, and csv-finalize's
    fingerprint-related docblocks for any stated intent of cross-classification dedupe. If
    (and only if) such source evidence exists, take D-05's fallback arm — document the
    exclusion AT the fingerprint site including the 409-remedy consequence — and record
    the evidence in the SUMMARY. Expected per RESEARCH: none exists, so take the default
    arm below.

    Widen csvSubmissionSignature with two additional parameters, categoryId and
    assetClass (string-or-null each), serialized as two additional NUL-separated fields
    after the existing name and rows fields, with an explicit deterministic null sentinel.
    csvSubmissionFingerprint gains the same parameters and passes them through — its
    FNV-1a mechanism and length prefix are untouched (non-cryptographic BY DESIGN; do not
    upgrade). Correct the falsified docblock sentence: date/daily_return are NOT the only
    fields reaching csv-finalize — metadata classification also reaches it and is
    precisely what the 146.2 classification-conflict 409 refuses on; classification
    therefore joins the fingerprint (RANK-08/D-05). Grep repo-wide for every caller of
    both functions and update signatures at each call site (compile-breaking widening is
    intentional — no optional-parameter soft edge that lets a caller silently omit
    classification).

    Author the behavior-block tests; observe Test 1 RED against the pre-edit code (equal
    fingerprints), then GREEN. Neuter drill: scratch-drop the categoryId field from the
    signature → Tests 1 RED → restore.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizard/localStorage.test.ts --no-file-parallelism && awk '/export function csvSubmissionSignature/{f=1} f{print} f&&/^}/{exit}' src/lib/wizard/localStorage.ts | grep -q 'assetClass'</automated>
  </verify>
  <acceptance_criteria>
    - The D-05 evidence-gate outcome is recorded in the SUMMARY (expected: no dedupe intent found → default arm taken)
    - Test 1's pre-widening RED was observed first-hand; all five behavior tests pass
    - csvSubmissionSignature's body serializes categoryId and assetClass as NUL-separated fields with a pinned null sentinel (region grep + source read)
    - The corrected docblock no longer claims date/daily_return are the only transmitted fields
    - Every caller compiles: npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Classification is part of the session fingerprint with true-duplicate fencing intact, the false docblock corrected, and the widening enforced at compile time on every caller.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire classification through both WizardClient call sites and dep arrays</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx, src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-durable-mint.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx (the re-mint effect ~:587 with dep array ~:624; handleCsvSubmitFailed ~:635 with dep array ~:651; the classification state: categoryId ~:272, assetClass ~:294 — re-grep, refs drift)
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-durable-mint.test.tsx and WizardClient.csv-burn-persistence.test.tsx (locate which harness drives the re-mint fence; extend the one that does — if it is the burn-persistence file, move this task's test there and record it)
  </read_first>
  <behavior>
    - Test 1: with a burned session, changing categoryId (name/series unchanged) → the fence reads a material change and re-mints (fresh wizardSessionId). Pre-wiring this is RED (fingerprint ignores classification or reads stale state).
    - Test 2: resubmitting with ALL inputs identical → no re-mint (the fence still holds).
    - Test 3: changing assetClass alone → re-mint.
  </behavior>
  <action>
    Pass categoryId and assetClass to BOTH csvSubmissionFingerprint call sites (~:587 and
    ~:635) and add BOTH state values to BOTH dependency arrays (~:624 and ~:651) — a
    widened fingerprint with a stale dep array compares old classification values and the
    409 remedy still dead-ends (Pitfall 7; this is the failure mode the dep-array edit
    exists to kill, name it in a one-line comment at each array). Task 1's
    compile-breaking widening means this file does not compile until this task lands —
    execute Tasks 1 and 2 in the same session, commit after both are green.

    Extend the located re-mint harness per the behavior block; drive the classification
    change through the component's real state flow (the harness's existing interaction
    style), not by calling the fingerprint function directly — the test must fail if the
    dep arrays are missing the new state even when the fingerprint function is correct.
    Neuter drill: scratch-remove categoryId from the ~:624 dep array → Test 1 RED →
    restore.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-durable-mint.test.tsx" "src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-burn-persistence.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both call sites pass both classification values; both dep arrays contain both state variables (source read of the two effect blocks)
    - The dep-array neuter drill's RED was observed first-hand — proving the test exercises the wiring, not the helper
    - All three behavior tests pass; the identical-resubmit control stays green
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The 409's remedy works end-to-end in the component: a classification change re-mints, a true duplicate still fences, and the stale-dep failure mode is pinned by a test that fails without the dep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: uid shape validation in withPublishedOrOwner, fail closed and loud</name>
  <files>src/lib/visibility.ts, src/lib/visibility.test.ts</files>
  <read_first>
    - src/lib/visibility.ts (FULL — the module purity header ~:17-19, withPublishedOnly ~:74, withPublishedOrOwner ~:115-125 with the structural-cast style, the CONTRIB-04 lint note ~:110)
    - src/lib/utils.ts ~:77-83 (isUuid/UUID_RE — the house validator; confirm utils.ts imports nothing server-only before importing it here)
    - src/lib/visibility.test.ts ~:1-60 (the fake-builder harness: .or spy returning the builder, exact-payload + builder-identity assertions)
  </read_first>
  <behavior>
    - Test 1 (RED first): a malformed uid — including the injection-shaped string x) or (user_id.neq.z — causes .eq to be called with status/published and .or to be called NEVER; the same builder is returned (chain survives). Pre-fix, .or receives the interpolated string.
    - Test 2: a valid UUID produces the exact existing .or payload (status.eq.published,user_id.eq.UUID) — behavior unchanged on the happy path.
    - Test 3: the malformed arm emits a console.error carrying a stable route-taggable prefix (loud, greppable).
    - Test 4: empty string and undefined-coerced inputs take the fail-closed arm.
  </behavior>
  <action>
    Import isUuid from the utils module (verify utils.ts client-purity first — it must not
    pull server-only modules into this deliberately isomorphic file; if utils.ts is
    impure, import nothing and apply UUID_RE via a re-export from a pure module instead —
    never author a new regex, house rule). In withPublishedOrOwner: validate authUserId
    with isUuid BEFORE the .or call; on failure, console.error with a
    [visibility.withPublishedOrOwner] prefix naming the rejection (the raw value may
    appear only as a console.error argument), then return the published-only shape — call
    withPublishedOnly on the same query (fail CLOSED per D-06: a malformed uid is treated
    as anon, never a permissive fallback). Keep the module purity header true; extend the
    docblock with the D-06 rationale (PostgREST filter-string injection surface).

    Extend visibility.test.ts per the behavior block using the existing fake-builder
    harness (builder exposes both or and eq spies returning itself). Observe Test 1 RED
    pre-fix. Neuter drill: scratch-bypass the isUuid check → Tests 1 and 4 RED → restore.
  </action>
  <verify>
    <automated>npx vitest run src/lib/visibility.test.ts --no-file-parallelism && grep -q 'isUuid' src/lib/visibility.ts && ! grep -nE "import .*(next/headers|supabase/server)" src/lib/visibility.ts | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - Test 1's pre-fix RED (injection string reaching .or) was observed first-hand
    - The fail-closed arm routes through withPublishedOnly (source read: no second published-only literal predicate authored)
    - The purity greps pass: no server-only import added; isUuid comes from the house module, no new regex in the diff
    - All four behavior tests pass; neuter-drill REDs recorded in the SUMMARY
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>A malformed uid can never reach the PostgREST filter string: the predicate fails closed to published-only, loudly, with the house validator — and the happy path is byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session/auth-derived uid → PostgREST filter string | string interpolation into .or() is a filter-injection surface |
| browser localStorage → wizard session state | client-controlled persisted state drives the re-mint fence |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-21 | Tampering | PostgREST .or() filter injection via a non-UUID uid | high | mitigate | strict isUuid gate before interpolation; fail-closed to published-only; injection-shaped fixture pinned in tests (ASVS V5) |
| T-159-22 | Information Disclosure | a permissive fallback widening visibility on malformed input | high | mitigate | D-06 fail-CLOSED arm = withPublishedOnly, treated as anon; test asserts .or never called |
| T-159-23 | Denial of Service | classification-conflict 409 with an unwinnable remedy (stuck sessions) | medium | mitigate | classification joins the fingerprint at signature + both call sites + both dep arrays; component-level re-mint test |
| T-159-24 | Spoofing | treating the fingerprint as a security control after widening | low | accept | explicitly non-cryptographic by design (docblock preserved); the server-side series-equality refusal remains the operative fence |
</threat_model>

<verification>
- Task gates above (vitest per file, purity greps, tsc).
- Phase-level: `npm run lint`; note Tasks 1+2 are compile-coupled (widening breaks
  WizardClient until wired) — one commit after both are green, per-task verify still run.
</verification>

<success_criteria>
- ROADMAP 159 SC-5 second and third arms hold: the 409's remedy mints a fresh session on
  classification change while true duplicates still fence, and no non-UUID uid is ever
  interpolated into a PostgREST filter — both proven RED→GREEN with wiring-level drills.
</success_criteria>

<output>
Create `.planning/phases/159-rank-public-ranking-integrity/159-07-SUMMARY.md` when done,
recording: the D-05 evidence-gate outcome, all observed REDs, the caller-update inventory
from the compile-breaking widening, and the utils.ts purity verification.
</output>
