---
phase: 159-rank-public-ranking-integrity
plan: 04
type: execute
wave: 3
depends_on: ["159-02"]
files_modified:
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.test.ts
  - src/app/(dashboard)/allocations/lib/scenario-compare.test.ts
autonomous: true
requirements: [RANK-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A blend leg with unknown asset_class (field absent, null, or undefined on the leg object) is treated as crypto for RISK annualization — a sole crypto leg whose projection dropped the field no longer inflates Sharpe via the sqrt-252 clock (RANK-06)"
    - "An EMPTY legs array still annualizes on 252 — only all-unknown non-empty blends move to 365, and the docblock states both facts explicitly (edge: the current docblock's empty-or-all-unknown sentence becomes half false and must be corrected)"
    - "All four production call sites inherit the fix through the ONE helper blendPeriodsPerYear — no call site grows a local 365/252 ternary (MD-01 discipline)"
    - "The fix scope is the RISK basis only — RETURN/CAGR ride the calendar clock and do not move (#597 law)"
    - "The wiring is tested at a production call site, not just the helper: a scenario-compare path with an unknown-asset_class leg produces sqrt-365-basis RISK output (house law: test the wiring)"
    - statement: "Money-math pins ride ECONOMIC invariants — e.g. annualized vol on the 365 clock equals the 252-clock vol times sqrt(365/252) on the same series — never the implementation's own formula re-evaluated"
      verification: backstop
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "blendPeriodsPerYear with unknown-leg-as-crypto RISK semantics + corrected docblock"
      contains: "blendPeriodsPerYear"
  key_links:
    - from: "src/app/(dashboard)/allocations/lib/scenario-compare.ts"
      to: "src/lib/closed-sets.ts blendPeriodsPerYear"
      via: "the :349-region call — the wiring-pin test drives this production path with an unknown-asset_class leg"
      pattern: "blendPeriodsPerYear"
  prohibitions:
    - statement: "No venue literal (deribit/binance/okx/bybit/sfox or any other venue name) is introduced anywhere in this plan's diff — venue membership lives only in closed_sets.py CRYPTO_VENUES; the Python composite path is already closed and must not be re-fixed"
      status: unverified
      flagged: true
      verification: "git diff of this plan's commits: zero added lines under analytics-service/, zero added venue-name string literals in src/"
    - statement: "RETURN/CAGR annualization is untouched — blendPeriodsPerYear's output continues to feed only RISK annualization at each of the four call sites"
      status: unverified
      flagged: true
      verification: "per-call-site source read recorded in SUMMARY confirming the helper's return value flows only into volatility/Sharpe-style RISK math (queries.ts:2232-region comment stays true)"
---

<objective>
Close RANK-06 at the ONE decision point: `blendPeriodsPerYear` in `src/lib/closed-sets.ts`
treats an unknown-`asset_class` leg as crypto for RISK, so a projection that drops the
field can no longer silently understate a crypto blend's volatility (and inflate its
Sharpe) via the √252 clock. All four production call sites inherit the fix through the
helper; the Python side is already closed via `CRYPTO_VENUES` and is not touched.

Purpose: ROADMAP 159 SC-4 second arm; the "unknown" case is a leg OBJECT missing the field
(strategies.asset_class is NOT NULL in the DB — the gap is caller projections).
Output: helper fix, updated economics pins, a production-call-site wiring pin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md
@.planning/phases/159-rank-public-ranking-integrity/159-PATTERNS.md
@src/lib/closed-sets.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–07)

See 159-01-PLAN.md for the full phase-wide list. This plan contributes:
- `src/lib/closed-sets.ts` — modified `blendPeriodsPerYear` (unknown-leg-as-crypto RISK semantics, corrected docblock)
- New wiring-pin tests in `src/app/(dashboard)/allocations/lib/scenario-compare.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Unknown-leg-as-crypto at the helper, with economic-invariant pins</name>
  <precondition>node_modules is installed in the working checkout (GSD worktrees ship none — npm ci first or run from an installed checkout)</precondition>
  <files>src/lib/closed-sets.ts, src/lib/closed-sets.test.ts</files>
  <read_first>
    - src/lib/closed-sets.ts (blendPeriodsPerYear ~:605-609 and its FULL docblock — the sentence claiming empty-or-all-unknown keeps 252 must be corrected, not deleted wholesale)
    - src/lib/closed-sets.test.ts ~:135-150 (the existing #597 blend-rule pin whose economics this requirement deliberately changes — update it, do not leave a contradicting pin)
    - .planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md §RANK-06 (where unknown actually comes from; the two deliberate behavior changes to record; the Python side is CLOSED)
  </read_first>
  <behavior>
    - Test 1 (RED first): legs = one object WITHOUT an asset_class field → 365 (currently 252 — the defect).
    - Test 2: legs = one object with asset_class null → 365; asset_class undefined → 365.
    - Test 3: legs = [] → 252 (empty blend keeps the pre-#597 default; stated in the docblock).
    - Test 4: all legs asset_class traditional → 252 (known-traditional unchanged).
    - Test 5 (economic invariant): for the same returns series, vol annualized on the 365 clock equals vol on the 252 clock times sqrt(365/252) — the pin asserts the RATIO, not the helper's internals.
  </behavior>
  <action>
    Change blendPeriodsPerYear's predicate so a leg counts toward the crypto clock when
    its asset_class is the string crypto OR is nullish (null/undefined/absent), while an
    empty legs array still returns 252 (guard on legs.length before the some()). Update
    the docblock: (a) correct the now-false empty-or-all-unknown sentence — empty stays
    252, all-unknown is now 365; (b) state the RANK-06 rationale (an unknown leg is a
    projection gap, and understating crypto vol on 252 inflates Sharpe — fail toward the
    conservative clock); (c) restate the #597 scope law: this basis feeds RISK only,
    RETURN/CAGR ride the calendar clock. No venue literal anywhere; the Python composite
    path is already closed via closed_sets.py CRYPTO_VENUES — do not touch
    analytics-service/.

    Update the existing #597 pin at closed-sets.test.ts ~:135 to the new economics (its
    change is the requirement, not collateral) and add the behavior-block tests. Observe
    Test 1 RED before the helper edit; neuter drill after (revert the predicate to
    crypto-only → Tests 1-2 RED → restore).
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.test.ts --no-file-parallelism && ! git diff HEAD~1 --stat -- analytics-service/ | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - All five behavior-block tests pass; Test 1's pre-fix RED and the neuter-drill REDs were observed and are recorded in the SUMMARY
    - The docblock contains the corrected empty-vs-all-unknown sentences and the RISK-only scope statement (source read)
    - Zero analytics-service/ files in the diff; zero added venue-name literals in src/ (grep of the diff)
    - The sqrt(365/252) ratio invariant is asserted numerically against a fixture series, not against the helper's own returned constant
  </acceptance_criteria>
  <done>The one decision point treats unknown as crypto for RISK with empty-blend behavior preserved, the contradicting old pin updated, and the economics pinned by an invariant that fails when the clock is wrong.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wiring pin at a production call site + four-site audit</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-compare.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-compare.ts (the blendPeriodsPerYear call in the ~:343-352 region, its leg-source shape, and which outputs the basis feeds)
    - src/app/(dashboard)/allocations/lib/scenario-compare.test.ts (existing harness — how it builds legs/series and asserts outputs)
    - The other three call sites for the audit: src/lib/queries.ts ~:2985 (computeScenario live-baseline; the :2978-2981 comment on per-key legs), src/app/scenario-share/[token]/share-resolve.ts ~:363, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx ~:3222
  </read_first>
  <behavior>
    - Test 1: drive the scenario-compare production path with legs where the sole crypto-in-fact leg OMITS asset_class → the RISK output reflects the 365 basis (fails if the call site stops using the helper or the helper regresses — the wiring pin).
    - Test 2 (control): same path with an explicit traditional leg set → 252-basis output unchanged.
  </behavior>
  <action>
    Add the wiring pin to scenario-compare.test.ts: construct the minimal input the
    existing harness uses, with one leg object lacking asset_class, and assert the
    annualized RISK output matches the 365-clock expectation (compute the expected value
    from the fixture series economics — mean/std and the sqrt(365) factor — not by calling
    blendPeriodsPerYear inside the test, which would be self-referential). Neuter drill:
    stub the helper (or hardcode 252 at the call site in a scratch edit) → Test 1 RED →
    restore.

    Audit the other three call sites (read-only): for each, record in the SUMMARY (a) the
    leg-source projection and whether asset_class can be absent on it today, and (b) the
    outputs the basis feeds — confirming RISK-only (the queries.ts:2232-region comment).
    If any call site's basis output feeds a RETURN/CAGR computation, STOP and surface —
    that is a #597 violation this plan must not paper over. If any call site carries a
    local 365/252 ternary instead of the helper, fix it to import the helper in this task
    (close the class).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-compare.test.ts" src/lib/closed-sets.test.ts --no-file-parallelism && test "$(grep -rl 'blendPeriodsPerYear' src/lib/queries.ts "src/app/scenario-share/[token]/share-resolve.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" "src/app/(dashboard)/allocations/lib/scenario-compare.ts" | wc -l | tr -d ' ')" = 4</automated>
  </verify>
  <acceptance_criteria>
    - The wiring pin passes and was observed RED under the neuter drill (recorded in SUMMARY)
    - The expected 365-basis value in Test 1 is derived from the fixture series' economics in the test body, not from a call to blendPeriodsPerYear
    - All four production call sites still reference the helper (grep gate) and the per-site audit table (leg projection + RISK-only confirmation) is in the SUMMARY
    - Zero local 365/252 ternaries at any call site (audit read)
  </acceptance_criteria>
  <done>The fix is proven at a real call site with a non-self-referential oracle, and the four-site audit shows the class rides the one helper with RISK-only scope intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller projections → blend legs | a leg object missing asset_class silently selects the annualization clock |
| published Sharpe/vol → allocator decisions | understated vol inflates a headline money metric |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-11 | Integrity | crypto blend annualized on 252 via projection gap (inflated Sharpe) | high | mitigate | unknown-as-crypto at the one helper; all four call sites inherit; wiring pin at a production path |
| T-159-12 | Integrity | fix drifting into RETURN/CAGR (calendar clock) | medium | mitigate | RISK-only scope stated in docblock + per-site audit confirming basis feeds RISK only; STOP-and-surface rule on violation |
| T-159-13 | Tampering | a second venue literal re-opening the MD-01 drift class | medium | mitigate | zero-venue-literal prohibition gated by diff grep; Python side declared closed and untouched |
</threat_model>

<verification>
- Task gates above (vitest, four-site grep, diff greps).
- Phase-level: `npm run lint` + tsc clean; scenario suites in the allocations lib directory
  green (drawdown/scenario-state siblings unaffected — spot-run if the harness shares
  fixtures).
</verification>

<success_criteria>
- ROADMAP 159 SC-4 second arm holds: an unknown-asset_class leg annualizes RISK as crypto,
  empty blends keep 252, RETURN clocks unmoved, all through the single MD-01 helper with a
  wiring-proven production path.
</success_criteria>

<output>
Create `.planning/phases/159-rank-public-ranking-integrity/159-04-SUMMARY.md` when done,
recording: observed REDs, the four-site audit table, and the docblock decision sentences.
</output>
