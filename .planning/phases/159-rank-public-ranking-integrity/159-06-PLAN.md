---
phase: 159-rank-public-ranking-integrity
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/csv-finalize/route.ts
  - src/__tests__/csv-finalize-cross-submission-merge.test.ts
autonomous: true
requirements: [RANK-07]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Two concurrent same-session resubmits cannot both take the FILL arm: the FILL UPDATE is compare-and-set on category_id IS NULL, so SQL — not route interleaving — decides the winner (RANK-07)"
    - "A raced-out second writer NEVER receives an applied receipt: a 0-row CAS UPDATE maps to a distinct raced result kind routed into the existing not-applied refusal — PostgREST reports no error on 0-row updates, so row-count observation is mandatory (edge: the BL-01 false-receipt class)"
    - "The loser's remedy is honest and reachable: a resubmit after losing the race reads a now-non-NULL category_id and takes the existing refuse/echo arm, resolving divergent classifications loudly (edge: divergent-classification racers)"
    - "The CAS is safe on BOTH arms reaching the UPDATE — a fresh row's category_id is also NULL (the fold's INSERT never writes it), so fresh-path updates still match"
    - "The wiring is pinned neuterably: removing .is from the chain reddens the race test; removing the row-count mapping reddens the false-receipt test (house law: every pin observed RED once)"
    - statement: "The genuine update-error arm still maps to the existing CSV_PERSIST_FAIL 503 envelope — error handling and raced handling are distinct outcomes (edge: conflating them would misreport infrastructure failures as races)"
      verification: backstop
  artifacts:
    - path: "src/app/api/strategies/csv-finalize/route.ts"
      provides: "CAS UPDATE with observed row count + raced result kind in applyCsvMetadataUpdate"
      contains: "raced"
  key_links:
    - from: "src/app/api/strategies/csv-finalize/route.ts applyCsvMetadataUpdate"
      to: "the FILL-arm refusal at metaResult.kind !== \"applied\""
      via: "the raced kind flows into the existing refusal so the loser gets an honest non-2xx, never a fabricated success"
      pattern: "raced"
  prohibitions:
    - statement: "No advisory lock, SELECT-FOR-UPDATE RPC, or new migration is introduced — the CAS rides the supabase-js .is filter on the existing update chain (house precedent: deletion-request routes)"
      status: unverified
      flagged: true
      verification: "git diff for this plan shows zero paths under supabase/migrations/ and no pg_advisory or FOR UPDATE strings added"
    - statement: "An applied receipt is never returned when the UPDATE matched zero rows"
      status: unverified
      flagged: true
      verification: "the race test asserts exactly one of the two interleaved POSTs reports the applied outcome; the neuter drill (drop the row-count mapping) was observed RED"
---

<objective>
Close RANK-07: the csv-finalize FILL arm's UPDATE becomes compare-and-set on
`category_id IS NULL` with observed row count, so two concurrent same-session resubmits
cannot both fill a session's classification — and the raced-out loser is refused honestly
instead of receiving a false "applied" receipt (the BL-01 class this route already paid
for once).

Purpose: ROADMAP 159 SC-5 first arm; the race window is one UPDATE — SQL is the arbiter.
Output: CAS chain + `raced` result kind in `applyCsvMetadataUpdate`, a two-writer race
test on the real POST handler, neuterable wiring pins.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md
@.planning/phases/159-rank-public-ranking-integrity/159-PATTERNS.md
@src/app/api/admin/deletion-requests/[id]/approve/route.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–07)

See 159-01-PLAN.md for the full phase-wide list. This plan contributes:
- `src/app/api/strategies/csv-finalize/route.ts` — CAS `.is("category_id", null)` + `.select("id")` + `raced` result kind in `applyCsvMetadataUpdate`
- Two-writer race + false-receipt tests in `src/__tests__/csv-finalize-cross-submission-merge.test.ts`
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: CAS with observed row count, proven by a two-writer race test (RED→GREEN)</name>
  <precondition>node_modules is installed in the working checkout (GSD worktrees ship none — npm ci first or run from an installed checkout)</precondition>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-cross-submission-merge.test.ts</files>
  <read_first>
    - src/app/api/strategies/csv-finalize/route.ts: applyCsvMetadataUpdate (~:2034, the UPDATE chain ~:2068-2072 with .eq id + .eq user_id), its invocation ~:2601-2602 under outcome.fresh || outcome.fillClassification, the existing not-applied refusal ~:2610-2629 (CSV_PERSIST_FAIL 503), the FILL discriminator ~:1451 (existingRow.category_id === null) with the anchor comments at ~:423 / ~:1433-1443 / ~:2595, and the refuse/echo arm ~:1673-1699
    - src/app/api/admin/deletion-requests/[id]/approve/route.ts ~:341-358 (the house CAS-with-observed-rowcount pattern: .update().eq().is().select("id"), rowsAffected from data length)
    - src/__tests__/csv-finalize-cross-submission-merge.test.ts (~:97 ordered-read mock recorder, ~:268 real POST import, ~:1417 FILL/REFUSE describe block — the harness to extend)
  </read_first>
  <behavior>
    - Test 1 (RED first): two interleaved POSTs whose mocked resolve reads BOTH return category_id null; the mock honors the .is filter (first update returns one row, second returns empty data) → exactly ONE response reports the applied/FILL outcome; the second is a non-2xx honest refusal. Pre-fix, both report success — observe that RED.
    - Test 2: the raced loser's response body carries the existing refusal envelope (CSV_PERSIST_FAIL class or the distinct raced copy chosen), never the FILL success shape.
    - Test 3 (control): a genuine updateError still produces the CSV_PERSIST_FAIL 503 — error and raced are distinct outcomes.
  </behavior>
  <action>
    In applyCsvMetadataUpdate: append .is("category_id", null) and .select("id") to the
    existing update chain (mirroring the deletion-requests approve pattern), destructure
    data alongside error, and map data length 0 (with no error) to a NEW result kind
    "raced" on the function's result union. Keep the error arm exactly as-is. At the
    invocation site, route "raced" through the existing metaResult.kind !== "applied"
    refusal (~:2610-2629): the loser gets the honest non-2xx; its remedy is documented in
    the refusal copy — resubmit, upon which the resolve read sees a non-NULL category_id
    and the ~:1673 refuse/echo arm adjudicates any divergence. The CAS predicate is safe
    on the fresh arm too (fresh rows carry NULL category_id per the ~:1438 comment) — say
    so in a one-line comment at the chain.

    Extend the cross-submission-merge harness per the behavior block: the mocked builder
    must record and HONOR the .is("category_id", null) filter (first invocation matches,
    second returns empty data) and both POSTs go through the real handler. Observe Test 1
    RED against the pre-edit route (both writers succeed), then GREEN.

    Update the anchor comments (~:423, ~:1433-1443, ~:2595) ONLY where this change makes a
    sentence false — the discriminator prose gains the CAS fact; nothing else moves
    (surgical, house Rule 3).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts --no-file-parallelism && awk '/^async function applyCsvMetadataUpdate/{f=1;next} f&&/^(async function|export |function )/{f=0} f' src/app/api/strategies/csv-finalize/route.ts | grep -q 'category_id.*null' && awk '/^async function applyCsvMetadataUpdate/{f=1;next} f&&/^(async function|export |function )/{f=0} f' src/app/api/strategies/csv-finalize/route.ts | grep -q 'select("id")'</automated>
  </verify>
  <acceptance_criteria>
    - Test 1's pre-fix RED (both writers reporting success) was observed first-hand and is recorded in the SUMMARY
    - The applyCsvMetadataUpdate region contains both .is("category_id", null) and .select("id") on the update chain (region greps pass)
    - Exactly-one-winner, honest-loser, and distinct-error-arm behaviors all pass
    - The raced kind exists on the result union and reaches the existing refusal path (source read of the invocation site)
  </acceptance_criteria>
  <done>SQL decides the FILL winner; the loser is refused honestly with a working remedy; infrastructure errors remain distinct — proven RED→GREEN on the real handler.</done>
</task>

<task type="auto">
  <name>Task 2: Neuterable wiring pins + sibling-suite sweep</name>
  <files>src/__tests__/csv-finalize-cross-submission-merge.test.ts</files>
  <read_first>
    - src/__tests__/csv-finalize-cross-submission-merge.test.ts (as extended in Task 1)
    - src/__tests__/csv-finalize-rpc.test.ts (~:165-220 FILL semantics) and src/__tests__/csv-finalize-c14-regression.test.ts (~:942-1065) — the sibling suites that must stay green
  </read_first>
  <action>
    Run the two neuter drills and record each observed RED: (1) scratch-remove the .is
    filter from the chain → the race test must fail (the wiring pin — if it stays green
    the test is asserting the mock, not the route); (2) scratch-remove the data-length
    mapping (always return applied) → the false-receipt test must fail. Restore from the
    committed state after each drill (never from memory) and confirm green.

    Then sweep the sibling suites: csv-finalize-rpc.test.ts,
    csv-finalize-c14-regression.test.ts, and the full cross-submission-merge file. Any
    sibling failure is adjudicated (does its pinned behavior legitimately change under
    CAS?) — a sibling that pinned "second writer also applies" would be pinning the
    defect; rewrite it to the new honest semantics and say so in the SUMMARY. Run tsc and
    lint on the touched files.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-cross-submission-merge.test.ts src/__tests__/csv-finalize-rpc.test.ts src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both neuter drills' REDs were observed first-hand with failing-test names recorded in the SUMMARY; the tree is restored and green
    - All three csv-finalize suites pass; any sibling assertion rewritten is listed with its before/after semantics
    - tsc --noEmit exits 0
  </acceptance_criteria>
  <done>The CAS wiring is pinned by tests proven capable of failing, and the whole csv-finalize test surface agrees with the new one-winner semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client resubmits → csv-finalize route | concurrent authenticated requests race the FILL discriminator |
| route → strategies table via PostgREST | the UPDATE is the only write in the race window |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-18 | Tampering | TOCTOU on the FILL discriminator (both racers fill; divergent classification silently overwritten) | high | mitigate | SQL CAS .is("category_id", null) — the check and the set are one statement |
| T-159-19 | Repudiation | raced-out writer receiving a false applied receipt (BL-01 class) | high | mitigate | .select("id") + data-length mapping to a distinct raced kind routed into the existing refusal; neuter-drilled pin |
| T-159-20 | Denial of Service | CAS wrongly blocking the fresh arm (legitimate first writes refused) | medium | mitigate | fresh rows carry NULL category_id (fold INSERT never writes it) — control test keeps the fresh path green |
</threat_model>

<verification>
- Task gates above (race suite, region greps, sibling sweep, tsc).
- Phase-level: `npm run lint` on touched files.
</verification>

<success_criteria>
- ROADMAP 159 SC-5 first arm holds: concurrent same-session resubmits resolve to exactly
  one FILL with an honest, remediable refusal for the loser — pinned by tests observed to
  fail when the CAS or the row-count observation is removed.
</success_criteria>

<output>
Create `.planning/phases/159-rank-public-ranking-integrity/159-06-SUMMARY.md` when done,
recording: the observed pre-fix RED, both neuter-drill REDs, and any sibling-suite
adjudications.
</output>
