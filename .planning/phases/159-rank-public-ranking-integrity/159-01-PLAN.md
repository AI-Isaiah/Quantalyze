---
phase: 159-rank-public-ranking-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md
autonomous: false
requirements: [RANK-01]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "159-CENSUS.md exists as a committed artifact BEFORE any RANK-01 filter or migration lands (D-01: the census is the decided C-D1 gate for rank-visibility changes)"
    - "The census records per-category published-with-analytics counts BEFORE and AFTER the isComputedAnalytics gate, checked against BOTH floors: the <5 badge floor (queries.ts) and the RPC min-N 20 (v_min_n) (D-01)"
    - "The census contains a per-strategy percentile before/after snapshot keyed on strategy ids only (D-01)"
    - "Every category that crosses a floor BECAUSE of the gate is recorded as an expected visible change, for surfacing in phase UAT (D-01/C-D1: a disappearing rank is the HONEST outcome, but a decided one)"
    - "The pollution population is enumerated: published strategies whose non-computed analytics rows HOLD KPI values (the 2026-05-27-era fossil class, route.ts ~:1493-1500)"
    - statement: "The census numbers were produced by read-only SQL against the PROD database, not TEST (edge: measuring TEST would gate PROD behavior on the wrong population)"
      verification: backstop
  artifacts:
    - path: ".planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md"
      provides: "C-M1 census — counts vs both floors, percentile snapshot, floor-crossing analysis, expected visible changes"
      contains: "after_gate"
  key_links:
    - from: ".planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md"
      to: "159-02-PLAN.md (RANK-01 gate + RPC migration)"
      via: "wave barrier + 159-02 precondition — the filter-landing plan may not start until this artifact carries real PROD results"
      pattern: "RESULTS"
  prohibitions:
    - statement: "The census artifact contains no emails, no auth uids, no user names, no key material — counts, percentiles, and strategy ids only (repo is PUBLIC and .planning/ is tracked)"
      status: unverified
      flagged: true
      verification: "grep of 159-CENSUS.md for '@' finds no email-shaped token; no column named email/user_id/uid appears in any pasted result table"
    - statement: "The census SQL performs zero mutations — no INSERT/UPDATE/DELETE/ALTER/CREATE appears in any census query block"
      status: unverified
      flagged: true
      verification: "grep -inE '^(insert|update|delete|alter|create|drop|truncate) ' of the SQL blocks in 159-CENSUS.md returns nothing"
---

<objective>
Produce and commit the C-M1 PROD census artifact (`159-CENSUS.md`) — the decided gate
(D-01/C-D1) that must exist BEFORE the RANK-01 percentile filter or the
`get_verified_cohort_rank` migration lands. Per-category published-with-analytics counts
before/after the computed-analytics gate against both floors (<5 badge floor, RPC min-N 20),
a per-strategy percentile before/after snapshot (ids only), and an explicit
floor-crossing / expected-visible-change analysis for phase UAT.

Purpose: ROADMAP 159 SC-2 — a rank that disappears is the HONEST outcome, but it must be a
decided one, surfaced in UAT, never a surprise.
Output: `.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md` with real PROD
numbers, committed.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md
@.planning/phases/159-rank-public-ranking-integrity/159-CONTEXT.md
@supabase/migrations/20260626120000_get_verified_cohort_rank.sql
@src/lib/percentile-core.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–07)

- `.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md` — NEW C-M1 census artifact (this plan)
- `src/lib/closed-sets.ts` — NEW exported constant `PERCENTILE_GATE_COLUMN` + NEW exported function `isRankableAnalyticsRow` (plan 02); modified `blendPeriodsPerYear` (plan 04)
- `supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql` — NEW migration, full re-base of `get_verified_cohort_rank` with the computed-analytics cohort gate (plan 02)
- `supabase/tests/test_get_verified_cohort_rank_gate.sql` — NEW state-adaptive CI SQL gate for the RPC (plan 02)
- `src/lib/queries.ts` — gated percentile callers (plan 02); NEW exported projection constants for the converted splat sites, e.g. `CATEGORY_RANKING_ANALYTICS_COLUMNS`, `STRATEGY_DETAIL_PUBLIC_ANALYTICS_COLUMNS`, `STRATEGY_DETAIL_DISCOVERY_ANALYTICS_COLUMNS` (plan 03)
- `src/app/(dashboard)/compare/page.tsx` — explicit projection replacing the splat (plan 03)
- `analytics-service/services/metrics.py` — quantstats price-guess closed on the strategy-analytics path (plan 05); new sign-invariant + benign-parity tests in `analytics-service/tests/test_metrics.py`
- `src/app/api/strategies/csv-finalize/route.ts` — FILL-arm CAS `.is("category_id", null)` + observed row count + `raced` result kind (plan 06)
- `src/lib/wizard/localStorage.ts` — `csvSubmissionSignature`/`csvSubmissionFingerprint` widened with classification fields (plan 07)
- `src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx` — classification wired into both fingerprint call sites + dep arrays (plan 07)
- `src/lib/visibility.ts` — `withPublishedOrOwner` uid shape validation, fail-closed (plan 07)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Author the 159-CENSUS.md scaffold with the exact read-only SQL</name>
  <files>.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md</files>
  <read_first>
    - .planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md §Code Examples "C-M1 census SQL" (the three queries to copy VERBATIM) and §RANK-01 findings (floors, fossil-row evidence)
    - supabase/migrations/20260626120000_get_verified_cohort_rank.sql lines ~72-88 (the documented ranking convention: count-based rank, abs on max_drawdown, LOWER_IS_BETTER inversion) and ~:152 (v_min_n CONSTANT INT := 20)
    - src/lib/percentile-core.ts (the TS scorer's exact convention: which KPIs are LOWER_IS_BETTER, the count(<=)/n formula — the snapshot SQL must mirror THIS source, not memory)
    - src/lib/queries.ts lines 117-190 (PERCENTILE_ANALYTICS_COLUMNS at :126-127; the two <5 floors at :170/:180)
  </read_first>
  <action>
    Create `.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md` (D-01 artifact).
    Structure:

    1. Header: purpose (C-M1, the C-D1 decision gate), date, and the PII rule stated
       verbatim as a rule of the file: counts, percentiles, and strategy ids ONLY — never
       emails, auth uids, or names (repo is PUBLIC, `.planning/` is tracked).
    2. Section "Query 1 — per-category counts vs the <5 badge floor": copy the first SQL
       query from 159-RESEARCH.md §Code Examples byte-for-byte, followed by a RESULTS
       subsection containing the single marker line `RESULTS: PENDING`.
    3. Section "Query 2 — RPC cohort vs min-N 20": copy the second query verbatim, plus
       `RESULTS: PENDING`.
    4. Section "Query 3 — the pollution population (fossil rows)": copy the third query
       verbatim, plus `RESULTS: PENDING`.
    5. Section "Query 4 — per-strategy percentile before/after snapshot": AUTHOR this SQL
       yourself from source, mirroring percentile-core's convention exactly — for each
       published strategy id and each of the seven PERCENTILE_ANALYTICS_COLUMNS KPIs
       (cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return),
       per-category cohort percentile = 100.0 * count(cohort values <= this value) / n,
       with abs() applied to max_drawdown and the LOWER_IS_BETTER inversion (100 - p)
       applied to exactly the KPIs percentile-core marks LOWER_IS_BETTER (read the set
       from percentile-core source; do not guess). Compute it TWICE per strategy: BEFORE
       cohort = published strategies with a non-null analytics KPI row; AFTER cohort = the
       same restricted to computation_status IN ('complete','complete_with_warnings') —
       the exact two-value list of isComputedAnalytics, never a single 'complete'
       equality. Output columns: strategy id, category slug, kpi, pct_before, pct_after.
       Then `RESULTS: PENDING`.
    6. Section "Floor crossings and expected visible changes (C-D1)": an analysis template
       with three prompts to be filled at the checkpoint: (a) categories whose after_gate
       count crosses below 5 (badge floor) because of the gate; (b) whether the RPC cohort
       crosses below 20; (c) strategies whose percentile visibly moves or disappears —
       each listed by id as an EXPECTED visible change for phase UAT. Include the D-01
       sentence: the filter proceeds regardless — a disappearing rank is the HONEST,
       pre-decided outcome; no test asserts rank direction.

    All SQL is read-only SELECTs. Do not include any project ref, URL, or credential in
    the file.
  </action>
  <verify>
    <automated>test -f .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md && grep -c 'RESULTS: PENDING' .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md | grep -qx 4 && grep -q 'after_gate' .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md && grep -q "complete_with_warnings" .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md && ! grep -inE '^\s*(insert|update|delete|alter|create|drop|truncate) ' .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - The scaffold contains four SQL blocks and exactly four `RESULTS: PENDING` markers
    - Queries 1-3 are byte-identical to 159-RESEARCH.md §Code Examples (diff of the extracted blocks against the research file's blocks is empty)
    - Query 4's LOWER_IS_BETTER KPI set matches the set actually read from src/lib/percentile-core.ts (cite the source line numbers in a comment above the query)
    - The gate predicate everywhere is the two-value IN list ('complete','complete_with_warnings'); zero occurrences of a single-value equality on computation_status
    - The PII rule paragraph and the C-D1 "filter proceeds regardless" sentence are present
  </acceptance_criteria>
  <done>A committed scaffold exists whose SQL is deterministic and source-derived, ready for the orchestrator to execute against PROD — the checkpoint needs no SQL authorship, only execution and transcription.</done>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: ORCHESTRATOR executes the census read-only against PROD and commits results</name>
  <files>.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md</files>
  <read_first>
    - .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md (the scaffold from Task 1 — every SQL block to run is on disk)
  </read_first>
  <action>
    STOP and return `## CHECKPOINT REACHED`. This task is executed by the ORCHESTRATOR
    session, not this executor: MCP tools are stripped from subagents, and the orchestrator
    holds read-only SQL access to PROD via the Supabase MCP (resolves RESEARCH Open
    Question 1). Follow the instructions block; resume only on the resume signal.
  </action>
  <instructions>
    Orchestrator protocol:
    1. Confirm the Supabase MCP target is the PROD project (ref khslejtfbuezsmvmtsdn —
       NEVER the TEST ref qmnijlgmdhviwzwfyzlc) before running anything; record the
       confirmed ref in the census header. If only TEST is reachable, HALT and surface —
       do not substitute TEST numbers.
    2. Execute each of the four SQL blocks from 159-CENSUS.md read-only, in order.
    3. Replace each `RESULTS: PENDING` marker with the actual result table (markdown).
       Counts, percentiles, and strategy ids only — strip anything email- or uid-shaped
       before pasting (repo is PUBLIC).
       <!-- planner-region-allow: RESULTS: PENDING -->
       (Task 1 deliberately authors the four PENDING markers this task replaces — the
       file-wide negative gate below is the intended sequential transform, not a
       conflict.)
    4. Complete the "Floor crossings and expected visible changes (C-D1)" section: name
       each category crossing the <5 floor because of the gate, state whether the RPC
       cohort crosses min-N 20, and list per-strategy visible percentile changes by id.
       These entries are the phase-UAT surfacing input (D-01).
    5. Commit 159-CENSUS.md.
  </instructions>
  <resume-signal>159-CENSUS.md contains zero `RESULTS: PENDING` markers, its floor-crossing analysis is filled, and the file is committed. Only then may plan 159-02 (the filter + migration) begin — this artifact is the decided gate for all rank-visibility changes (D-01).</resume-signal>
  <verification>
    - All four RESULTS sections carry real PROD tables (visual check of the committed file)
    - The confirmed PROD ref appears in the header; no TEST-derived number was substituted
    - The C-D1 analysis names concrete categories/ids or states "none" per prompt
  </verification>
  <verify>
    <automated>! grep -q 'RESULTS: PENDING' .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md && git log --oneline -1 -- .planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - All four RESULTS sections carry real PROD result tables; zero PENDING markers remain
    - The floor-crossing analysis names concrete categories/ids or states "none" per prompt — never left blank
    - The confirmed PROD project ref is recorded in the header
    - `grep -c '@' 159-CENSUS.md` finds no email-shaped token; no uid/email column appears in any pasted table
    - The file is committed (git log shows a commit touching it)
  </acceptance_criteria>
  <done>The C-M1 census exists with real PROD numbers and a completed C-D1 analysis, committed BEFORE any filter or migration lands — the D-01 ordering gate is satisfied and plan 159-02 is unblocked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator MCP → PROD DB | read-only SQL against production data |
| .planning census artifact → public repo | results are world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-01 | Information Disclosure | 159-CENSUS.md in a PUBLIC repo | high | mitigate | PII rule encoded in the scaffold header + checkpoint step 3 strips email/uid-shaped values + acceptance grep; ids/counts/percentiles only |
| T-159-02 | Tampering | census SQL mutating PROD | critical | mitigate | scaffold contains only SELECTs (negative grep in Task 1 gate); orchestrator access is read-only; checkpoint protocol executes only the on-disk blocks |
| T-159-03 | Repudiation | gate decision made without evidence (filter lands before census) | medium | mitigate | wave barrier + plan 159-02 precondition greps this artifact for completed results before starting |
</threat_model>

<verification>
- Task 1 gate: scaffold structure + verbatim-SQL + no-mutation greps.
- Task 2 gate: zero PENDING markers, committed, PII greps clean.
- Phase-level: 159-02 must observe this artifact complete before its first edit (D-01
  ordering: census committed BEFORE the migration exists on the branch).
</verification>

<success_criteria>
- ROADMAP 159 SC-2 holds: the C-M1 census and per-strategy percentile snapshot exist as
  committed phase artifacts BEFORE the filter lands; every floor crossing is a recorded,
  decided, UAT-surfaced expected change; no PII in the artifact.
</success_criteria>

<output>
Create `.planning/phases/159-rank-public-ranking-integrity/159-01-SUMMARY.md` when done,
recording: the confirmed PROD ref, headline counts (categories at/near floors, cohort size
vs 20, fossil-row count), and the C-D1 outcome list.
</output>
