---
phase: 159-rank-public-ranking-integrity
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/tests/test_metrics.py
autonomous: true
requirements: [RANK-05]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An all-non-negative daily-returns series with a >100% day is never re-read as prices on the strategy-analytics path: compute_all_metrics yields a correctly-signed Sharpe and Sortino for an all-winning series (RANK-05; economic invariant, currently sign-flipped)"
    - "The fix mirrors the P114 closed path's mechanism (sharpe_vol_status_from_backbone inline pandas) — no new abstraction, no quantstats fork, the ==0.0.81 pin unchanged (D-04)"
    - "Headline sharpe and sortino are NOT closed via a prepare_returns kwarg — that kwarg does not exist on them in 0.0.81; a kwarg-only sweep leaves the two ranked KPIs exposed (edge: Pitfall 3)"
    - "Every replaced no-kwarg site is pinned by a benign-series parity oracle: on a series that does NOT trigger the price heuristic, the inline math matches quantstats 0.0.81 output within float tolerance (non-self-referential correctness anchor)"
    - "The kwarg matrix was re-verified inside the analytics-service environment before any edit (assumption A1 — the local-behind-pin drift class)"
    - statement: "Golden/parity fixture movement is adjudicated as a NaN-convention finding (skipna vs fillna(0)) and recorded — never regenerated-and-moved-on (edge: Pitfall 4)"
      verification: backstop
  artifacts:
    - path: "analytics-service/services/metrics.py"
      provides: "compute_all_metrics with the price-guess closed at every qs.stats call site (kwarg arm + inline arm)"
      contains: "prepare_returns=False"
    - path: "analytics-service/tests/test_metrics.py"
      provides: "sign-invariant economic tests + benign-series parity pins"
      contains: "sign"
  key_links:
    - from: "analytics-service/services/metrics.py compute_all_metrics"
      to: "analytics-service/services/metrics.py sharpe_vol_status_from_backbone"
      via: "the new inline sharpe/sortino math mirrors the P114 pattern and docblock discipline (D-04)"
      pattern: "sharpe_vol_status_from_backbone"
  prohibitions:
    - statement: "No quantstats version change, no fork, no monkeypatch of quantstats internals — the requirements pin stays ==0.0.81 (D-04)"
      status: unverified
      flagged: true
      verification: "git diff shows zero changes to analytics-service/requirements.txt and requirements-dev.txt; grep of the diff finds no patching of quantstats module attributes"
    - statement: "No golden fixture file is regenerated without a written adjudication of WHICH NaN convention is honest — fixture diffs without a SUMMARY finding are forbidden"
      status: unverified
      flagged: true
      verification: "if the diff touches tests/fixtures/ or any *golden* file, the SUMMARY contains a per-fixture adjudication paragraph"
---

<objective>
Close RANK-05: the quantstats `_prepare_returns` price-detection heuristic (all-non-negative
series with max > 1 silently `pct_change`-d, flipping Sharpe's sign) is dead on the
strategy-analytics path. Functions carrying `prepare_returns=` get `False`; the headline
`sharpe`/`sortino` and the other no-kwarg sites get the P114 inline-pandas mirror (D-04).

Purpose: ROADMAP 159 SC-4 first arm — the KPI writer for `strategy_analytics` feeds
published ranks; a sign-flipped Sharpe is a public money-math lie.
Output: closed call sites in `compute_all_metrics`, sign-invariant + benign-parity tests,
full-suite + mypy gate with fixture adjudication.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md
@.planning/phases/159-rank-public-ranking-integrity/159-PATTERNS.md
@analytics-service/services/metrics.py
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–07)

See 159-01-PLAN.md for the full phase-wide list. This plan contributes:
- `analytics-service/services/metrics.py` — price-guess-free `compute_all_metrics` (kwarg arm + inline P114-mirror arm)
- New tests in `analytics-service/tests/test_metrics.py`: sign-invariant economics + per-site benign-series parity pins
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: A1 in-env kwarg re-verification + headline sharpe/sortino inline mirror (RED→GREEN)</name>
  <precondition>python3 from analytics-service/ imports quantstats at exactly 0.0.81 and pandas successfully (pytest must run ONLY from analytics-service/ — repo-root runs miss VCR cassettes and go live)</precondition>
  <files>analytics-service/services/metrics.py, analytics-service/tests/test_metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py lines 399-730 (compute_all_metrics :399; the qs.stats call cluster — max_drawdown :700, to_drawdown_series :702, volatility :707, sharpe :708, sortino :713 with rf=MAR — and the surrounding stat_returns/returns_for_chart preparation) and lines 1290-1375 (sharpe_vol_status_from_backbone — the P114 pattern AND docblock discipline to mirror: the :1315-1324 defect description, the :1360-1366 inline math)
    - analytics-service/tests/test_metrics.py (existing harness: how series fixtures are built and compute_all_metrics is driven)
    - .planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md §RANK-05 (the verified _prepare_returns heuristic, the kwarg availability matrix, the blast-radius note on fillna(0) vs skipna)
  </read_first>
  <behavior>
    - Test 1 (RED first): an all-non-negative daily-returns series containing one 1.5 (a +150% day) driven through compute_all_metrics yields sharpe > 0 and sortino > 0 — an all-winning series has non-negative risk-adjusted return (economics, not formula). Currently the heuristic re-reads it as prices and flips the sign.
    - Test 2: the same series WITHOUT the >100% day (heuristic not triggered) yields sharpe/sortino unchanged from pre-fix quantstats output within float tolerance (benign parity — the fix must not move untriggered series).
    - Test 3: a mixed-sign series (heuristic can never trigger) is byte-stable across the fix (parity control).
  </behavior>
  <action>
    FIRST, re-verify assumption A1 inside the service environment: from analytics-service/,
    run an inspect.signature sweep over qs.stats functions used by compute_all_metrics
    (volatility, sharpe, sortino, max_drawdown, to_drawdown_series, value_at_risk, cvar,
    omega, gain_to_pain_ratio, tail_ratio, smart_sharpe, smart_sortino, profit_factor) and
    print __version__ plus which accept prepare_returns=. Record the matrix in the SUMMARY.
    If it diverges from RESEARCH's matrix (sharpe/sortino DO carry the kwarg, or others
    lost it), follow the measured matrix and record the divergence — measured beats cited.

    Author the behavior-block tests in tests/test_metrics.py; observe Test 1 RED against
    unmodified compute_all_metrics.

    Then close the two headline sites: replace the qs.stats.sharpe call (~:708) and
    qs.stats.sortino call (~:713) with inline pandas mirroring
    sharpe_vol_status_from_backbone's exact pattern — annualized mean over annualized
    vol via mean times periods_per_year divided by std times sqrt(periods_per_year), with
    _safe_float wrapping; sortino keeps its MAR/rf economics: subtract MAR per period
    before the mean and use downside deviation (std of below-MAR excess, quantstats
    convention) — derive the downside convention from quantstats 0.0.81 source so Test 2's
    benign parity holds, and cite the qs source lines in a comment. Copy the P114 docblock
    discipline: name the _prepare_returns price heuristic as the reason quantstats is not
    called here. Handle NaN exactly as the P114 path does (pandas skipna) and note the
    convention choice at the site.

    GREEN on all three tests; neuter drill: reintroduce a qs.stats.sharpe call in a
    scratch edit → Test 1 RED → restore.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -c "import quantstats; assert quantstats.__version__ == '0.0.81', quantstats.__version__" && python3 -m pytest tests/test_metrics.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - The measured in-env kwarg matrix is in the SUMMARY with __version__ output
    - Test 1's pre-fix RED was observed first-hand (sign-flip reproduced, output recorded), then GREEN after the inline mirror
    - Benign-parity Tests 2-3 pass: untriggered series produce pre-fix-identical sharpe/sortino within tolerance
    - The new inline sites carry the P114-style defect docblock and cite the quantstats source for the sortino downside convention
    - cd analytics-service && python3 -m pytest tests/test_metrics.py -q exits 0
  </acceptance_criteria>
  <done>The two ranked headline KPIs are price-guess-free with the sign flip reproduced, closed, and pinned by economics plus benign parity — the exact mechanism D-04 mandates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the remaining qs.stats call sites — kwarg arm + inline arm</name>
  <files>analytics-service/services/metrics.py, analytics-service/tests/test_metrics.py</files>
  <read_first>
    - analytics-service/services/metrics.py lines 690-980 (the remaining call sites: max_drawdown ~:700, to_drawdown_series ~:702, volatility ~:707, value_at_risk ~:826, cvar ~:836, omega ~:884, gain_to_pain_ratio ~:893, tail_ratio ~:902, smart_sharpe ~:935, smart_sortino ~:944, profit_factor ~:966 — re-grep qs.stats to catch drift; close EVERY site, not the enumerated list)
    - The installed quantstats 0.0.81 source for each replaced function (inspect.getsource) — the inline equivalents must reproduce its documented math minus the _prepare_returns price guess
  </read_first>
  <behavior>
    - Per kwarg-arm site: with prepare_returns=False plus caller-side cleanup, the trigger series (all-non-negative, max>1) is treated as returns; a benign series matches pre-fix output within tolerance.
    - Per inline-arm site: benign-series parity against quantstats 0.0.81 within float tolerance; trigger-series output equals the inline math applied to the raw returns (no pct_change re-read).
  </behavior>
  <action>
    Kwarg arm — sites whose 0.0.81 signature carries prepare_returns= (per Task 1's
    measured matrix; expected: volatility, value_at_risk, cvar, tail_ratio,
    profit_factor): pass prepare_returns=False and add the caller-side cleanup
    _prepare_returns performed minus the price guess — verify per site what the upstream
    pipeline already does (inf-to-NaN handling is largely done upstream per RESEARCH);
    where _prepare_returns' fillna(0) mattered, decide and document the NaN convention at
    the site (skipna preferred, matching the P114 path — record each decision).

    Inline arm — sites WITHOUT the kwarg (expected: max_drawdown, to_drawdown_series,
    omega, gain_to_pain_ratio, smart_sharpe, smart_sortino): replace with inline pandas
    reproducing quantstats 0.0.81's math minus the price guess, same P114 docblock
    discipline. max_drawdown/to_drawdown_series ride the cumulative-product wealth curve;
    omega/gain_to_pain are ratio formulas; smart_sharpe/smart_sortino apply the
    autocorrelation penalty — read each from qs source, cite lines in comments. These feed
    metrics_json (not the seven ranked columns) but the same series triggers the same
    heuristic; the class closes whole (house law).

    For EACH replaced/kwarg'd site add a benign-series parity pin (inline vs live
    quantstats on an untriggered series, tight tolerance) and cover trigger behavior via
    one shared trigger-series test asserting drawdown/omega/etc. are computed from the
    RETURNS (e.g. max_drawdown of an all-positive-returns series is 0 — economics). Keep
    total test runtime sane by parameterizing over sites.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_metrics.py -q && ! awk '/^def compute_all_metrics/{f=1;next} f&&/^def /{f=0} f' services/metrics.py | grep -E 'qs\.stats\.[a-z_]+\(' | grep -v 'prepare_returns=False' | grep -q .</automated>
  </verify>
  <acceptance_criteria>
    - Within compute_all_metrics's function body, every surviving qs.stats call line carries prepare_returns=False — zero no-kwarg qs.stats calls remain on the strategy-analytics path (keep each surviving call and its prepare_returns=False kwarg on ONE source line so the region gate can see them; the gate scopes via the stateful-awk function region, immune to line drift)
    - Every inline site cites its quantstats 0.0.81 source lines and carries the defect docblock
    - Per-site benign parity pins pass with recorded tolerances; the NaN-convention decision per kwarg site is in the SUMMARY
    - cd analytics-service && python3 -m pytest tests/test_metrics.py -q exits 0
  </acceptance_criteria>
  <done>The price-guess class is closed across the WHOLE compute_all_metrics surface — kwarg where it exists, P114 inline where it does not — with non-self-referential parity anchoring every replacement.</done>
</task>

<task type="auto">
  <name>Task 3: Parity/golden adjudication + full analytics-service gate</name>
  <files>analytics-service/tests/test_metrics.py</files>
  <read_first>
    - .planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md §RANK-05 blast radius (the suites that will detect movement and the skipna-vs-fillna(0) divergence condition)
  </read_first>
  <action>
    From analytics-service/ run, in order: python3 -m pytest tests/test_metrics_parity.py
    tests/test_accuracy.py tests/test_mt5_golden_fixtures.py tests/test_teaser_derive_golden.py
    -q, then the FULL python3 -m pytest -q, then mypy --strict on the touched module set
    (the house pre-ship law for analytics-service changes; fix errors via cast(), never
    type-ignore).

    Any moved fixture or parity failure is a FINDING, not a regen: adjudicate WHICH
    convention is honest (interior-NaN days as skipped observations vs as 0-return days),
    record the adjudication per fixture in the SUMMARY, and only then update the fixture
    or the code — the adjudication paragraph is mandatory before any fixture byte moves
    (Pitfall 4). If a golden suite reveals a benign series whose value moved (parity
    breach, not a NaN case), that is a defect in Task 1/2's inline math — fix the math,
    never the fixture.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -q && python3 -m mypy --strict services/metrics.py</automated>
  </verify>
  <acceptance_criteria>
    - Full analytics-service pytest green from analytics-service/ (counts recorded in SUMMARY)
    - mypy --strict clean on metrics.py with zero new type-ignore pragmas (grep of the diff)
    - Every fixture that moved carries a written adjudication; zero silent regens (diff cross-checked against SUMMARY)
    - The four named parity/golden suites' outcomes are individually recorded
  </acceptance_criteria>
  <done>The whole analytics-service suite is green with every consequence of the NaN/parity blast radius adjudicated in writing — the fix ships with its measurement, not despite it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied return series → KPI writer | attacker-shaped or merely unusual series select quantstats' hidden price heuristic |
| strategy_analytics KPIs → published ranks | a sign-flipped Sharpe is served to anonymous readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-14 | Integrity | sign-flipped Sharpe/Sortino on trigger series | high | mitigate | kwarg + inline closure of every qs.stats site in compute_all_metrics; economic sign invariant pinned |
| T-159-15 | Integrity | inline reimplementation drifting from quantstats math on benign series | high | mitigate | per-site benign-series parity oracle against live 0.0.81 within tight tolerance |
| T-159-16 | Integrity | silent golden-fixture regen laundering a NaN-convention change | medium | mitigate | mandatory written adjudication per moved fixture; prohibition gated on diff-vs-SUMMARY cross-check |
| T-159-17 | Tampering | CI env diverging from local pin (A1) invalidating the kwarg matrix | medium | mitigate | in-env signature sweep is Task 1's first step; measured matrix governs |
</threat_model>

<verification>
- Task gates above (pytest from analytics-service/ only, version assert, region-scoped
  no-kwarg grep, mypy --strict).
- Phase-level: mypy --strict must be re-run before /ship (house law — the GSD milestone
  gate runs pytest only).
</verification>

<success_criteria>
- ROADMAP 159 SC-4 first arm holds: no series shape can be re-read as prices anywhere in
  compute_all_metrics; ranked KPIs carry honestly-signed values; the full suite and mypy
  are green with all fixture movement adjudicated.
</success_criteria>

<output>
Create `.planning/phases/159-rank-public-ranking-integrity/159-05-SUMMARY.md` when done,
recording: the measured kwarg matrix, observed pre-fix RED output, per-site NaN decisions,
parity tolerances, and every fixture adjudication.
</output>
