---
status: partial
phase: 159-rank-public-ranking-integrity
source: 159-VERIFICATION.md (human_verification block, 7 items + 1 behavior_unverified)
started: 2026-09-05T20:30:00Z
updated: 2026-09-05T20:30:00Z
---

## Current Test

[testing paused — 3 items outstanding, all blocked on a capability this phase cannot create]

## Tests

### 1. Post-merge UAT: public category page shows no percentile badges
expected: EVERY percentile badge is gone (category dropped 18 -> 1 gate-passing, crossing the <5 floor) — the pre-decided honest outcome (D-01 / 159-CENSUS.md), NOT a regression
result: pass
reported: "PASSED 2026-08-26 on PROD; RE-CONFIRMED 2026-09-05 in a logged-out-equivalent read of `/browse/crypto-sma`."
notes: |
  Re-measured 2026-09-05. Page renders three published strategies and ZERO percentile tokens —
  no `top N%`, no `Nth percentile`, no `#N of M`.

  ⚠️ The world moved since the 2026-08-26 measurement and the item's stated CAUSE is now partly
  stale, while its OUTCOME still holds. On 2026-08-26 the reason given was "all three have zero
  keys". Today `Momentum Sphinx` (Bybit, Long-Only) and `Phoenix Protocol` (OKX, Long-Short)
  both carry real computed metrics — so at least two now pass the gate, not one. Two is still
  under the <5 cohort floor, so badge suppression is still the correct render. The pass is
  therefore re-derived, not inherited.

  ⚠️ The table does show a `RANK` column containing `#1`, `#2` and `—`. That is the table's
  SORT ORDINAL within the three rows displayed, not a cohort percentile: it carries no cohort
  size and `grep -rn "RANK"` finds no such literal in `src/`. The `—` on the third row is
  consistent with rank being gated on computed analytics. Recorded because a future reader
  scanning this page for "rank-like tokens" will see `#1` and needs to know it was examined and
  ruled a different mechanism — not overlooked.

  ⚠️ The item names `/discovery/crypto-sma`, which now 307-redirects. The live route is
  `/browse/crypto-sma`. Anyone re-running the item verbatim tests a redirect.

### 2. Render one composite strategy on the public factsheet route (WINDOWS #6)
expected: The composite (`dqf.composite === true`) factsheet branch renders, not the "still computing" placeholder
result: blocked
blocked_by: prior-phase
reason: |
  RE-MEASURED 2026-09-05 in a real authenticated browser. Still unreachable, and the reason has
  NARROWED since 2026-08-26 — worth recording, because the old reason is now false.

  OLD reason (2026-08-26): "PROD holds exactly THREE published strategies and ALL THREE have
  zero keys, so no composite exists to render."
  NEW measurement (2026-09-05): PROD still holds exactly three published strategies, but they no
  longer all have zero keys. `Momentum Sphinx` is a single-venue Bybit strategy with a live
  track record ending 17d ago; `Phoenix Protocol` is a single-venue OKX strategy, 122d stale.
  Neither is a composite — a composite has no single venue. The third, `Fibonacci Ghost`
  (`8581f739-1a7b-42a4-a209-3acfa327e259`), shows no venue and all em-dash metrics, and its
  factsheet renders the "still computing" placeholder — i.e. it renders the EXACT branch this
  item requires NOT to render.

  ⛔ HONEST LIMIT: I could not prove from the public surface whether `Fibonacci Ghost` is a
  composite or a single-venue strategy that never computed. Both produce the same anonymous
  view. Settling it needs a `dqf.composite` read, which is a DB/authenticated-owner check the
  logged-in account cannot make — `/strategies` redirects to `/allocations`, so this browser is
  an ALLOCATOR, not the manager who owns these rows. So the correct statement is "no composite
  was demonstrated", not "no composite exists".

  The item's own closing rule stands: re-open when a published composite is known to exist.
  Do not close it by looking at a placeholder.

### 3. Two concurrent same-session csv-finalize resubmits against a real DB
expected: Exactly one 2xx applied receipt; the loser receives the honest raced refusal; `category_id` holds the winner's value
result: blocked
blocked_by: prior-phase
reason: |
  SEQUENCED, not stalled. Unchanged from 159-VERIFICATION.md and re-confirmed today: this needs
  REQUEST-LEVEL control (two POSTs carrying the SAME `wizard_session_id`), which a browser
  cannot produce — re-entering the wizard after a completed submit mints a FRESH session, which
  is correct behaviour and is exactly why the conflict arc is unreachable from the UI. It must
  not be driven against shared Supabase TEST.

  Blocked on **Phase 164.5**, whose criterion 6 (VAC-07) is this exact spec on the
  baseline-loaded local stack. Same item also discharges the `behavior_unverified` PostgREST
  zero-row-UPDATE truth currently carried by a mock.

### 4. First post-merge CI run: `test_get_verified_cohort_rank_gate.sql` GREEN with gate assertions ARMED
expected: The skip arm no longer fires once migration 20260821120000 reaches TEST; assertions 4a/4b execute and pass
result: pass
reported: "PASSED 2026-08-26 — verified by direct catalog measurement on BOTH TEST and PROD (2 occurrences each, byte-identical 4948-char bodies), deliberately NOT by a green CI run, because per SKIP-01 a green `sql-tests` run is not evidence a gate is armed."

### 5. Product call: is RANK-02's splat-class scoping accepted?
expected: Explicit decision recorded (accept D-02 scoping, or open a follow-up)
result: pass
reported: "RULED 2026-08-26: RANK-02 ACCEPTED as written (D-02 scoping stands, requirement MET); follow-up booked as [RANK-SPLAT-01] in TODOS.md for the unbounded 45-key anon `metrics_json` alias set."

### 6. End-to-end 409 remedy (159-07 D4): re-classify and resubmit in the real wizard
expected: A fresh `wizard_session_id` is minted and a fresh strategy is created
result: blocked
blocked_by: prior-phase
reason: |
  PARTIALLY DISCHARGED 2026-08-28 — the half the item literally asks for WAS driven against the
  live production route (csv-validate 200, csv-finalize 200, strategy created, fresh session
  minted). The 409 refusal arm was NOT reachable: provoking it needs a direct re-POST of
  csv-finalize carrying the SAME `wizard_session_id`.

  Remaining half merges with test 3 and closes with it on Phase 164.5. Recorded as blocked
  rather than pass so the unreached arm is not laundered into a green.

### 7. 159-04 D5: published Sharpe/vol on class-less legs will visibly move
expected: Confirm the movement is acceptable to ship without a user-facing note
result: pass
reported: "RULED 2026-08-26: SHIP SILENTLY. The movement is a CORRECTION — a crypto leg annualized on the trad sqrt(252) clock read optimistically. Blast radius measured at ruling time: 3 published strategies, 0 live shared scenarios, so no reader holds a screenshot of the superseded figure."

## Summary

total: 7
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 3

## Gaps

- truth: "A user who lands on an uncomputed published factsheet reads a sentence written for them, not for a developer"
  status: failed
  reason: "Observed 2026-09-05 on PROD while re-measuring test 2. The anonymous 'still computing' placeholder at /factsheet/8581f739-1a7b-42a4-a209-3acfa327e259 ends with: 'See the dev-server console for the exact gate the request fell through.' There is no dev-server console for a production visitor, and an anonymous reader has no console to look at. It also volunteers an internal gate detail ('the bundled benchmark window (2023-04-26 onward)') to an unauthenticated reader."
  severity: minor
  test: 2
  root_cause: ""
  artifacts: []
  missing:
    - "Route to Phase 164.2 CURATED-COPY — this is exactly its thesis (every failure sentence a user reads is the true, specific one) and its mechanism (b) class (copy that names a cause the reader cannot act on). NOT a 159 gap to fix here; 159's subject is ranking integrity."
  debug_session: ""
