---
phase: 159-rank-public-ranking-integrity
plan: 02
type: execute
wave: 2
depends_on: ["159-01"]
files_modified:
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.test.ts
  - src/lib/queries.ts
  - src/lib/queries.percentiles.test.ts
  - supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql
  - supabase/tests/test_get_verified_cohort_rank_gate.sql
autonomous: true
requirements: [RANK-01]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A strategy whose analytics row is failed/pending/computing neither contributes to nor receives a published percentile rank in getPercentiles AND getOwnRowPercentiles (RANK-01)"
    - "A complete_with_warnings row IS retained in the rank cohort — the gate rides isComputedAnalytics semantics, never a literal 'complete' equality (edge: complete_with_warnings is a terminal SUCCESS)"
    - "A failed row that HOLDS KPI values (the PROD fossil class the census enumerated) is excluded from percentile scoring (edge: IS NOT NULL predicates alone do NOT exclude it)"
    - "ONE shared filter helper in src/lib/closed-sets.ts serves BOTH TS callers, delegating to isComputedAnalytics — no second status predicate exists (D-03)"
    - "PERCENTILE_ANALYTICS_COLUMNS at queries.ts is byte-identical to its pre-phase value; the gate column rides the separate PERCENTILE_GATE_COLUMN constant composed at the projection sites (D-03)"
    - "The get_verified_cohort_rank RPC moves in lockstep: the computed-analytics gate is added to BOTH cohort predicates (count query AND rank query) in a full-body re-based migration, so the min-N denominator cannot diverge from the rank numerator (D-03)"
    - "159-CENSUS.md was complete and committed before the migration file was created (D-01 ordering)"
    - statement: "The re-based RPC preserves SECURITY DEFINER, the pinned search_path, the REVOKE/GRANT posture, the in-function auth guard, decile quantization, identity strip, and v_min_n = 20 (edge: a partial-body CREATE OR REPLACE silently drops guards)"
      verification: backstop
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "PERCENTILE_GATE_COLUMN constant + isRankableAnalyticsRow helper next to isComputedAnalytics (MD-01 single-source, D-03)"
      contains: "PERCENTILE_GATE_COLUMN"
    - path: "supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql"
      provides: "Full re-based RPC with the computed-analytics cohort gate in both predicates"
      contains: "complete_with_warnings"
    - path: "supabase/tests/test_get_verified_cohort_rank_gate.sql"
      provides: "State-adaptive CI SQL gate pinning the RPC's cohort predicate (Wave-0 gap closure — the RPC has zero CI-executed tests today)"
      contains: "complete_with_warnings"
  key_links:
    - from: "src/lib/queries.ts"
      to: "src/lib/closed-sets.ts"
      via: "both percentile callers import PERCENTILE_GATE_COLUMN + isRankableAnalyticsRow and filter embedded rows through the ONE helper before the rows floor"
      pattern: "isRankableAnalyticsRow"
    - from: "supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql"
      to: "src/lib/closed-sets.ts isComputedAnalytics"
      via: "the SQL IN ('complete','complete_with_warnings') list mirrors isComputedAnalytics exactly — parity-by-construction prose stays true"
      pattern: "parity-by-construction"
  prohibitions:
    - statement: "PERCENTILE_ANALYTICS_COLUMNS is never appended to — the csv-finalize mirror prose at route.ts ~:1031/:1488/:1505 (CLOCK_SAFETY_KPI_COLUMNS 'member for member') must stay true"
      status: unverified
      flagged: true
      verification: "grep of src/lib/queries.ts shows the constant's value string byte-identical to 'cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return'"
    - statement: "No new single-value computation_status equality filter is introduced anywhere in this plan's diff — the gate is always the isComputedAnalytics delegate (TS) or the exact two-value IN list (SQL)"
      status: unverified
      flagged: true
      verification: "git diff of this plan's commits: added lines contain zero occurrences of a computation_status comparison against the single quoted word complete without complete_with_warnings on the same predicate"
    - statement: "No test asserts rank DIRECTION — pins are membership-only (gated rows excluded, complete_with_warnings retained); removing polluted rows moves other percentiles BOTH ways (ROADMAP SC-2)"
      status: unverified
      flagged: true
      verification: "review of new test assertions: none compares a percentile as improved/higher/lower across the gate"
---

<objective>
Close RANK-01: published percentile ranks never fold failed/stale-computation KPIs — on
BOTH engines. TS: a shared gate helper in `closed-sets.ts` (D-03) wired into
`getPercentiles` and `getOwnRowPercentiles` via a separate `PERCENTILE_GATE_COLUMN`
projection composition, `PERCENTILE_ANALYTICS_COLUMNS` byte-unchanged. SQL: the
`get_verified_cohort_rank` RPC re-based in lockstep with the gate in both cohort
predicates, plus the first CI-executed SQL test for this RPC.

Purpose: ROADMAP 159 SC-1 — the exact defect is PROD fossil rows (failed rows holding
sharpe/cagr) polluting public ranks.
Output: gated TS callers, new migration, new state-adaptive SQL gate, membership-only tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md
@.planning/phases/159-rank-public-ranking-integrity/159-PATTERNS.md
@.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md
@src/lib/closed-sets.ts
@supabase/migrations/20260626120000_get_verified_cohort_rank.sql
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–07)

See 159-01-PLAN.md for the full phase-wide list. This plan contributes:
- `src/lib/closed-sets.ts` — NEW exports `PERCENTILE_GATE_COLUMN` (const, value "computation_status") and `isRankableAnalyticsRow(row)` (function)
- `supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql` — NEW migration (full re-base)
- `supabase/tests/test_get_verified_cohort_rank_gate.sql` — NEW state-adaptive SQL gate
</artifacts_this_phase_produces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end percentile gate — helper + getPercentiles, one path proven</name>
  <precondition>159-CENSUS.md contains zero 'RESULTS: PENDING' markers and is committed (D-01: the census gate is satisfied before any filter edit)</precondition>
  <precondition>node_modules is installed in the working checkout (GSD worktrees ship none — run npm ci first or execute from an installed checkout; npx vitest MODULE_NOT_FOUND means this precondition failed)</precondition>
  <files>src/lib/closed-sets.ts, src/lib/queries.ts, src/lib/queries.percentiles.test.ts</files>
  <read_first>
    - src/lib/closed-sets.ts (module-header import rule ~:16-18 "imports ONLY zod"; isComputedAnalytics ~:715-719; STRATEGY_ANALYTICS_COMPUTATION_STATUSES ~:696-702)
    - src/lib/queries.ts lines 100-200 (PERCENTILE_ANALYTICS_COLUMNS :126-127; getPercentiles :141-190 — projection sites ~:150/:156 via the analyticsColumns local at :144, floors at :170 and :180, extractAnalytics skip at :174-178)
    - src/lib/utils.ts ~:171-177 (extractAnalytics is a passthrough cast — a projected computation_status arrives on the extracted object unmodified)
    - src/lib/queries.percentiles.test.ts (the thenable-chain mock harness to extend)
    - .planning/phases/159-rank-public-ranking-integrity/159-RESEARCH.md §RANK-01 (gate-composition sketch; why the constant is byte-frozen)
  </read_first>
  <behavior>
    - Test 1 (RED first): a mocked cohort containing a failed row WITH sharpe/cagr values (the fossil class) — that strategy neither receives a percentile nor moves any other strategy's percentile; currently it does both.
    - Test 2: a complete_with_warnings row is scored exactly like a complete row (membership retained).
    - Test 3: pending and computing rows are excluded; a strategy with no analytics embed stays excluded (existing behavior preserved).
    - Test 4: the rows floor counts RANKABLE rows — 6 strategies of which only 4 are computed returns null (below the <5 floor on the gated population).
    - No test compares percentile magnitudes across the gate (membership only — direction is not uniform).
  </behavior>
  <action>
    In `src/lib/closed-sets.ts`, next to `isComputedAnalytics` (D-03 placement — the MD-01
    single-source module), add two exports: `export const PERCENTILE_GATE_COLUMN =
    "computation_status"` and `export function isRankableAnalyticsRow(row)` accepting an
    object-or-null with an optional `computation_status: string | null` and delegating to
    `isComputedAnalytics(row?.computation_status)` — import-free (module rule: only zod),
    never re-deriving status semantics. Docblock states: this is the RANK-01 rank gate;
    both percentile callers and the get_verified_cohort_rank RPC mirror it.

    In `src/lib/queries.ts` `getPercentiles`: compose the projection at BOTH select sites
    (categorized ~:150 and uncategorized ~:156) as the template string joining
    PERCENTILE_ANALYTICS_COLUMNS and PERCENTILE_GATE_COLUMN inside the
    strategy_analytics embed — PERCENTILE_ANALYTICS_COLUMNS itself stays byte-identical
    (D-03; the csv-finalize mirror prose depends on it). After extractAnalytics (:174-178),
    skip any row failing `isRankableAnalyticsRow` — same skip shape as the existing
    missing-embed skip — so the `rows.length < 5` floor at :180 counts only rankable rows
    (the honest denominator, matching the RPC's gated count). Leave the raw
    `strategies.length < 5` floor at :170 untouched (population floor). Import the two new
    symbols from closed-sets (queries.ts already imports blendPeriodsPerYear from there —
    no cycle).

    Extend `src/lib/queries.percentiles.test.ts` per the behavior block using its existing
    thenable-chain mock harness. Observe Test 1 RED against the pre-edit code, then GREEN.
    Run the neuter drill on the new pins: temporarily make isRankableAnalyticsRow return
    true unconditionally → Tests 1 and 4 must go RED; restore.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.percentiles.test.ts --no-file-parallelism && grep -q 'PERCENTILE_GATE_COLUMN' src/lib/closed-sets.ts && grep -q '"cagr, sharpe, sortino, calmar, max_drawdown, volatility, cumulative_return"' src/lib/queries.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/queries.percentiles.test.ts --no-file-parallelism` exits 0 with the four new behaviors covered
    - RED was OBSERVED first-hand for Test 1 before the fix and for Tests 1+4 under the neuter drill (recorded in SUMMARY)
    - The PERCENTILE_ANALYTICS_COLUMNS value string is byte-identical (exact grep passes)
    - Both getPercentiles select sites compose the gate column via PERCENTILE_GATE_COLUMN (grep the template-literal composition in both branches)
    - isRankableAnalyticsRow delegates to isComputedAnalytics (source read: no independent status comparison in its body)
  </acceptance_criteria>
  <done>The rank gate works end-to-end on the discovery percentile path: fossil rows are out, complete_with_warnings is in, floors count the gated cohort — proven RED→GREEN with anti-vacuity drills.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire getOwnRowPercentiles through the same helper + closed-sets unit pins</name>
  <files>src/lib/queries.ts, src/lib/closed-sets.test.ts, src/lib/queries.percentiles.test.ts</files>
  <read_first>
    - src/lib/queries.ts lines 580-660 (getOwnRowPercentiles :617 — projection at :625, floors at :634/:646; the docblock's "Both < 5 thresholds mirror getPercentiles exactly" contract)
    - src/lib/closed-sets.test.ts (existing describe structure for closed-sets pins)
  </read_first>
  <behavior>
    - isRankableAnalyticsRow unit pins: true for complete and complete_with_warnings; false for pending, computing, failed, null status, undefined status, null row, undefined row.
    - getOwnRowPercentiles: a failed-with-KPIs cohort row is excluded from the own-row comparison population; the gated floor mirrors getPercentiles.
  </behavior>
  <action>
    In `getOwnRowPercentiles` (queries.ts :617-655): compose the same projection at :625
    (PERCENTILE_ANALYTICS_COLUMNS + PERCENTILE_GATE_COLUMN inside the embed) and filter
    rows through the SAME isRankableAnalyticsRow helper before the second floor — the ONE
    shared helper is ROADMAP SC-1's explicit requirement; introducing any local predicate
    is a defect. Keep the function's documented mirror-of-getPercentiles contract true:
    update its docblock sentence about the thresholds to say both floors now count
    rankable rows, same as getPercentiles.

    In `src/lib/closed-sets.test.ts`: add the isRankableAnalyticsRow unit pins across all
    five STRATEGY_ANALYTICS_COMPUTATION_STATUSES members plus null/undefined status and
    null/undefined row. Add a pin that PERCENTILE_GATE_COLUMN === "computation_status".
    Extend queries.percentiles.test.ts with the getOwnRowPercentiles exclusion behavior if
    that function is covered there (locate its existing tests first; if they live
    elsewhere, extend that file instead and record the location in the SUMMARY).

    Neuter drill each new pin (flip the helper's return → observe RED → restore).
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.test.ts src/lib/queries.percentiles.test.ts --no-file-parallelism && test "$(grep -c 'isRankableAnalyticsRow' src/lib/queries.ts)" -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - Both callers reference isRankableAnalyticsRow (grep count >= 2 in queries.ts) and neither contains a local computation_status comparison (source read of both function bodies)
    - closed-sets.test.ts pins all 8 helper input classes listed in the behavior block
    - `npx vitest run src/lib/closed-sets.test.ts src/lib/queries.percentiles.test.ts --no-file-parallelism` exits 0
    - Neuter-drill REDs observed and recorded in SUMMARY
  </acceptance_criteria>
  <done>Both TS percentile callers ride the one shared gate helper with membership-only pins that have been observed to fail.</done>
</task>

<task type="auto">
  <name>Task 3: Re-base get_verified_cohort_rank with the gate in BOTH predicates + first CI SQL gate</name>
  <reversibility rating="one-way">Merging supabase/migrations/** to main auto-applies to PROD; the decided gate for this rank-visibility change is the committed C-M1 census (D-01) — per the phase directive no additional checkpoint is inserted; the census precondition on Task 1 already barred this plan from starting without it.</reversibility>
  <files>supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql, supabase/tests/test_get_verified_cohort_rank_gate.sql</files>
  <read_first>
    - supabase/migrations/20260626120000_get_verified_cohort_rank.sql (FULL read — the body to re-base: cohort predicates ~:174-184 count query and ~:212-227 rank query; v_min_n := 20 at ~:152; auditor lesson ~:56-62; parity prose ~:72-83 and COMMENT ON FUNCTION ~:241-242; SECDEF + search_path + REVOKE/GRANT + auth guard + decile quantization + identity strip + self-verifying DO block)
    - supabase/tests/ (pick one recent test_*.sql as the harness template — e.g. test_compute_jobs_rpc_error_clear_and_fanin.sql — and note the shared-TEST discipline: scope every write to rows the test itself seeds, clean up, never table-wide UPDATEs)
    - .planning/phases/159-rank-public-ranking-integrity/159-PATTERNS.md §migrations (the lockstep-edit spec and Phase-156 state-adaptive gate precedent)
  </read_first>
  <action>
    First re-run the house-rule grep: search ALL of supabase/migrations/ for
    get_verified_cohort_rank and confirm 20260626120000 is still the ONLY definition; if a
    second definition appeared, re-base on the LATEST and record it in the SUMMARY.

    Author `supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql`
    (verify the timestamp sorts after every existing migration filename; bump to the
    current date-time if needed): CREATE OR REPLACE FUNCTION carrying the FULL re-based
    body — never a diff. The one semantic change: add the predicate line requiring
    a.computation_status IN ('complete', 'complete_with_warnings') to BOTH cohort
    predicates — the count query AND the rank query — so the min-N denominator and the
    rank numerator stay the same population (the migration's own :56-62 auditor lesson).
    The two-value list mirrors isComputedAnalytics EXACTLY; a single 'complete' value is
    the Pitfall-1 defect. Preserve verbatim: SECURITY DEFINER, SET search_path = public,
    pg_catalog, the REVOKE/GRANT posture, the in-function auth guard, decile quantization,
    identity strip, v_min_n CONSTANT INT := 20, and the self-verifying DO block (extend
    the DO block to assert the new predicate is present in pg_get_functiondef output —
    both occurrences). Update the header parity prose and COMMENT ON FUNCTION to name the
    TS twin (isRankableAnalyticsRow -> isComputedAnalytics in closed-sets.ts) so
    parity-by-construction stays a true sentence (D-03 lockstep default — no exclusion
    arm is being taken).

    Author `supabase/tests/test_get_verified_cohort_rank_gate.sql` — the RPC's FIRST
    CI-executed test (the HAS_LIVE_DB vitest never runs in CI). STATE-ADAPTIVE per the
    Phase-156 precedent, because TEST receives the migration only after merge: read
    pg_get_functiondef for the function; if it does not contain complete_with_warnings,
    RAISE NOTICE and SKIP every assertion (pre-migration database — by design); once
    armed, assert (a) the gate predicate appears in the definition at least twice (both
    cohort queries), (b) SECURITY DEFINER and the pinned search_path are present, (c) anon
    lacks EXECUTE via has_function_privilege. Add a behavioral arm ONLY if it can be
    seeded within shared-TEST discipline (own-rows-only, cleaned up): seed one
    complete_with_warnings row and one failed-with-KPIs row in a synthetic cohort and
    assert the cohort count includes the former and excludes the latter; if the FK surface
    (auth.users/profiles/verifications) makes safe seeding infeasible, record that
    explicitly in the test header and the SUMMARY — never a silent omission. Note in the
    test header: first armed-and-green observation happens on the first CI run after the
    migration reaches TEST (the Phase-156 lesson — say "would have caught" until then).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql && test "$(grep -c "complete_with_warnings" supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql)" -ge 2 && grep -q 'SECURITY DEFINER' supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql && grep -q 'search_path' supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql && grep -q 'v_min_n' supabase/migrations/20260821120000_get_verified_cohort_rank_computed_gate.sql && test -f supabase/tests/test_get_verified_cohort_rank_gate.sql && grep -q 'pg_get_functiondef' supabase/tests/test_get_verified_cohort_rank_gate.sql</automated>
  </verify>
  <acceptance_criteria>
    - The migration is a FULL body: it contains the auth guard, quantization, identity-strip, REVOKE/GRANT and DO-block regions of the original (diff of structural sections against 20260626120000 shows only the gate predicate, prose, and DO-block additions)
    - The gate predicate appears in BOTH the count query and the rank query (two occurrences inside the function body, verified by region read — not by raw file grep alone, which also counts prose)
    - The DO block asserts the deployed function definition carries the gate (self-verifying at apply time)
    - The SQL test SKIPs cleanly against a pre-migration database and contains the three armed assertions
    - The migration-reviewer invariants pass when the executor's review runs (full-body re-base, no dropped guard)
    - 159-CENSUS.md's completion commit predates this migration file's first commit (git log order)
  </acceptance_criteria>
  <done>The SQL twin is gated in lockstep with the TS helper, parity-by-construction prose is true, the RPC has its first CI-executed gate, and the census provably preceded the migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authed client → percentile read path | public ranks computed from PostgREST reads |
| anon → get_verified_cohort_rank RPC | SECURITY DEFINER bypasses RLS; its predicate is the only cohort control |
| migration merge → PROD | auto-applies on merge to main |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-159-04 | Integrity | percentile cohort polluted by non-computed KPIs | high | mitigate | isRankableAnalyticsRow gate on both TS callers + both RPC predicates (this plan's core) |
| T-159-05 | Information Disclosure | probe-oracle on RPC percentiles after re-base | high | mitigate | decile quantization + min-N 20 + identity strip + anon REVOKE preserved verbatim in the full-body re-base; SQL test asserts anon lacks EXECUTE |
| T-159-06 | Elevation of Privilege | re-based SECDEF function losing search_path pin or auth guard | high | mitigate | full-body rule + DO-block self-verification + migration-reviewer invariants + SQL-test armed assertions |
| T-159-07 | Denial of Service | gate blanking a whole category's badges unexpectedly | medium | accept | pre-decided per D-01/C-D1: census enumerated the floor crossings before this plan ran; disappearances are the honest, UAT-surfaced outcome |
</threat_model>

<verification>
- Task gates above (vitest runs, byte-freeze grep, migration structure greps).
- Schema-push gate: evaluated and deliberately NOT planned as a mid-phase `supabase db push`
  — the migration alters only a function body (zero table/type DDL; database.types.ts is
  unchanged, so no type/config false-positive exists), TEST receives it via the standard
  merge pipeline, and the Phase-156 precedent shows early TEST application of a pending
  migration is an ordering hazard on the shared DB. Live-DB verification = the migration's
  self-verifying DO block at apply time + the state-adaptive SQL gate arming on the first
  post-merge CI run.
- Phase-level: `npm run lint` + tsc clean on the touched TS files.
</verification>

<success_criteria>
- ROADMAP 159 SC-1 holds end-to-end: failed/stale KPIs are outside published percentiles on
  BOTH engines, complete_with_warnings retained, one shared helper, frozen constant
  byte-unchanged, RPC in lockstep with census-first ordering provable from git history.
</success_criteria>

<output>
Create `.planning/phases/159-rank-public-ranking-integrity/159-02-SUMMARY.md` when done,
recording: observed REDs (pre-fix + neuter drills), the migration re-base diff summary, the
SQL-test seeding decision (behavioral arm included or explicitly infeasible), and the
census-precedes-migration git-log evidence.
</output>
