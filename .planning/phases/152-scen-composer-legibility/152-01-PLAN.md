---
phase: 152-scen-composer-legibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/browse/route.ts
  - src/app/api/strategies/browse/route.test.ts
autonomous: true
requirements: [SCEN-02, SCEN-05]

must_haves:
  truths:
    - "GET /api/strategies/browse emits isOwn: true on a row the session owns and isOwn: false on every other row"
    - "created_at and status appear ONLY on rows the session owns — a third-party row's wire shape is unchanged except for isOwn"
    - "Another owner's created_at value appears nowhere in the whole response payload"
  artifacts:
    - path: "src/app/api/strategies/browse/route.ts"
      provides: "isOwn on every row + own-only created_at/status through the named-key fence"
      contains: "isOwn"
    - path: "src/app/api/strategies/browse/route.test.ts"
      provides: "H-0300 fence split into TWO exhaustive arms (third-party / own) + whole-payload sweep"
      contains: "ALLOWED_OWN"
  key_links:
    - from: "src/app/api/strategies/browse/route.ts"
      to: "isOwnRow (route.ts:220)"
      via: "reuse of the already-computed server-side ownership bit"
      pattern: "isOwn: isOwnRow"
    - from: "src/app/api/strategies/browse/route.test.ts"
      to: "route.ts emit literal"
      via: "exhaustive Object.keys toEqual per arm"
      pattern: "ALLOWED_THIRD_PARTY"
---

<objective>
SCEN-02/SCEN-05 wire, server side: put the already-computed ownership bit
(`isOwnRow`, `route.ts:220`) on the wire as `isOwn` (uniform, strict boolean, every
row), and emit the SCEN-05 disambiguation metadata (`created_at`, `status`) on OWN
rows only — through the H-0300 named-key fence, which is extended into two
exhaustive arms, never relaxed.

Purpose: every downstream render (Yours chip, browse dedup line) is a pure
projection of this wire. Landing it first means a missing chip later is a render
bug, not a silent wire drop.
Output: widened `BrowseStrategyRow` + emit literal in `route.ts`; restructured
H-0300 fence + new emission tests in `route.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/152-scen-composer-legibility/152-RESEARCH.md
@.planning/phases/152-scen-composer-legibility/152-PATTERNS.md

⚠️ Path correction (RESEARCH finding 1): the browse route is
`src/app/api/strategies/browse/route.ts`. The `api/allocator/strategies/browse/`
path named in CONTEXT.md and UI-SPEC.md does not exist.

Decisions resolved for this plan:
- D-1 (adopted): `created_at` alone disambiguates (founder's two Alpha Centauri
  rows are 15 days apart). NO `key_count`, NO second query, NO PostgREST embed.
  The SELECT gains exactly two additive columns: `created_at, status`.
- Wire shape (RESEARCH Wire Trace A): `isOwn` is emitted on EVERY row (`false`
  for third-party) — it is a viewer-relative relationship, not the other owner's
  metadata. `created_at`/`status` are own-rows-only and omitted (key absent, not
  `undefined`) otherwise.
</context>

<interfaces>
Current wire fence (route.ts:233-245) — the explicit literal to widen; NEVER a
`...row` spread (H-0300b pins this):

```ts
return {
  id: r.id,
  name: safeLabel,
  codename: r.codename ?? null,
  markets: Array.isArray(r.markets) ? (r.markets as string[]) : [],
  strategy_types: Array.isArray(r.strategy_types) ? (r.strategy_types as string[]) : [],
  is_example: r.is_example === true,
};
```

`isOwnRow` already exists at route.ts:220:
`const isOwnRow = r.user_id !== null && r.user_id === user.id;`

Target wire shape after this plan (BrowseStrategyRow):
- `isOwn: boolean` — required, uniform, strict boolean
- `created_at?: string` — own rows only, key ABSENT otherwise
- `status?: string` — own rows only (raw DB enum on the wire; client product-cases it)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure H-0300 into two exhaustive arms + write isOwn/own-only emission tests (RED)</name>
  <files>src/app/api/strategies/browse/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/browse/route.test.ts (whole file, 923 lines — the STATE
      harness, H-0300a at :731-764, H-0300b at :766-793, T12f two-row own-vs-other
      fixture at :674-717 with its SESSION_ID constant)
    - src/app/api/strategies/browse/route.ts:200-245 (current cast + fence)
  </read_first>
  <behavior>
    - Fence third-party arm: for a row whose user_id ≠ session id,
      `Object.keys(row).sort()` equals exactly
      `["codename","id","isOwn","is_example","markets","name","strategy_types"]`
      (7 keys), PLUS explicit `expect(row).not.toHaveProperty("created_at")`,
      `not.toHaveProperty("status")`, `not.toHaveProperty("key_count")`.
    - Fence own arm: for a row whose user_id === SESSION_ID, the exact key set is
      the 7 base keys plus `created_at` and `status` (9 keys), exhaustive `toEqual`.
    - Emission: own row → `isOwn === true` (strict boolean, `toBe(true)`);
      third-party row → `isOwn === false` (`toBe(false)`, never undefined/null).
    - Own-only fields: own row's `created_at` equals the fixture ISO string and
      `status` equals the fixture enum; third-party row carries neither key.
    - Whole-payload sweep (mirrors H-0300b:789-791): give the third-party fixture
      row a distinctive `created_at` (e.g. "1999-01-01T00:00:00.000Z") and assert
      `expect(JSON.stringify(body)).not.toContain("1999-01-01")`.
  </behavior>
  <action>
    In `route.test.ts`, replace the single `ALLOWED` array of H-0300a (:731-764)
    with two constants — `ALLOWED_THIRD_PARTY` (base 6 keys + `isOwn`) and
    `ALLOWED_OWN` (that + `created_at`, `status`) — and split the test into the two
    arms described in <behavior>, using the T12f two-row fixture pattern (:674-717):
    one row owned by `SESSION_ID`, one owned by another uuid. Both arms MUST remain
    exhaustive `Object.keys(...).sort()).toEqual(...)` comparisons — never
    `expect.objectContaining`, never optional members (RESEARCH Pitfall 7: adding
    keys to one shared list passes the test and destroys the fence in the same
    edit). Add the isOwn-emission test and the own-only-fields test as new `it`
    blocks in the same describe (sibling of T12f). Keep H-0300b (:766-793)
    untouched. Give both fixture rows `created_at` and `status` values so the
    conditional emission is observable (the DB columns are NOT NULL — the STATE
    harness rows must model that).

    Do NOT modify route.ts in this task. Run the file and confirm the NEW arms are
    RED (route does not yet emit isOwn) while pre-existing tests stay green.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/browse/route.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `ALLOWED_THIRD_PARTY` and `ALLOWED_OWN` constants exist; the string
      "expect.objectContaining" does not appear in any H-0300 test.
    - New arms fail with key-set mismatches naming `isOwn` (proves RED is for the
      right reason); every test that existed before this task still passes.
    - `grep -c "not.toHaveProperty(\"created_at\")" src/app/api/strategies/browse/route.test.ts` ≥ 1.
    - The whole-payload sweep asserting the third-party `created_at` value is
      absent from `JSON.stringify(body)` exists.
  </acceptance_criteria>
  <done>Two exhaustive fence arms + emission tests committed, RED against the unmodified route.</done>
</task>

<task type="auto">
  <name>Task 2: Emit isOwn on every row + created_at/status on own rows only (GREEN)</name>
  <files>src/app/api/strategies/browse/route.ts</files>
  <read_first>
    - src/app/api/strategies/browse/route.ts (whole file, 256 lines — SELECT at
      :139-156 with its CONTRIB-03 co-fetch comment, row cast at :200-210,
      isOwnRow at :220, fence at :233-245, BrowseStrategyRow interface at :39-69
      with the is_example TSDoc voice at :59-68)
  </read_first>
  <action>
    Four edits, all additive, per the PATTERNS route section:

    1. SELECT (:153): append `, created_at, status` to the column string, with a
       comment in the CONTRIB-03 voice: co-fetched for SCEN-05; emitted on OWN
       rows only (pseudonymity fence — a third-party row's creation date is a
       correlation vector, RESEARCH Security §pseudonymity break).
    2. Row cast (:200-210): add `created_at: unknown;` and `status: unknown;`.
    3. Emit literal (:233-245): after the `is_example` line add
       `isOwn: isOwnRow,` (isOwnRow is already strictly boolean — reuse it, do
       not recompute) with an H-0300-voice comment (Phase 152 SCEN-02, uniform
       viewer-relative key, `false` discloses nothing the viewer does not know).
       Then two conditional single-key literal spreads:
       `...(isOwnRow && typeof r.created_at === "string" ? { created_at: r.created_at } : {})`
       and the same for `status` — key ABSENT (not `undefined`) off the own
       branch, so JSON round-trips match the fence arms exactly. Comment: Phase
       152 SCEN-05, OWNER-ONLY metadata, never on third-party rows. This is NOT a
       `...row` spread — the fence stays a named-key fence.
    4. `BrowseStrategyRow` interface (:39-69): add `isOwn: boolean;`,
       `created_at?: string;`, `status?: string;`, each with a TSDoc in the
       is_example voice (:59-68): phase/requirement id, what a value means, what
       absence means, why it is not a disclosure widening (isOwn) / why it is
       conditional (created_at, status — the TS optional encodes the own-only
       emission).

    Constraints: keep the user-scoped `createClient()` and `withPublishedOrOwner`
    untouched (service_role has BYPASSRLS; `quantalyze/no-raw-published-predicate`
    is eslint `error`); no new query, no new error path; do not touch the
    `safeLabel` / `displayStrategyName` pseudonymization.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/browse/route.test.ts 2>&1 | tail -5 && npx eslint src/app/api/strategies/browse/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Full `route.test.ts` run green (all pre-existing + all Task-1 arms).
    - `grep -c '\.\.\.row' src/app/api/strategies/browse/route.ts` returns 0 (no
      row spread anywhere).
    - `grep -c "isOwn: isOwnRow" src/app/api/strategies/browse/route.ts` returns 1.
    - eslint clean on route.ts (no-raw-published-predicate untriggered).
    - No change to the SELECT's client (`createClient()` user-scoped) — `git diff`
      shows only the four edits above.
  </acceptance_criteria>
  <done>Route emits the SCEN-02/05 wire; both fence arms green; eslint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/strategies/browse | authed allocator receives rows about OTHER users' pseudonymised strategies |
| route → Supabase | user-scoped client under `strategies_read` RLS + `withPublishedOrOwner` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-01-01 | Information Disclosure | browse emit literal | mitigate | own-only conditional emission of created_at/status; TWO exhaustive fence arms + whole-payload sweep (Task 1) — a third-party row's creation date never reaches the wire |
| T-152-01-02 | Information Disclosure | session id / user_id | mitigate | isOwn computed server-side from `withAllocatorAuth` session; user_id stays read-only-never-emitted; existing `not.toContain(SESSION_ID)` sweep retained |
| T-152-01-03 | Elevation of Privilege | Supabase client | mitigate | no new query; keep user-scoped `createClient()` + `withPublishedOrOwner` (service_role BYPASSRLS rationale at route.ts:126-133) |
| T-152-01-SC | Tampering | package installs | accept | zero installs this phase (RESEARCH Package Legitimacy Audit: n/a) |
</threat_model>

<verification>
- `npx vitest run src/app/api/strategies/browse/route.test.ts` — all green
- `npx eslint src/app/api/strategies/browse/route.ts src/app/api/strategies/browse/route.test.ts`
</verification>

<success_criteria>
- Own row wire: 9 exact keys incl. `isOwn: true`, `created_at`, `status`.
- Third-party row wire: 7 exact keys incl. `isOwn: false`; no owner metadata; sweep proves the other owner's created_at is nowhere in the payload.
- Fence is two exhaustive arms — the "add keys to one ALLOWED list" fix is structurally impossible without a red test.
</success_criteria>

<output>
Create `.planning/phases/152-scen-composer-legibility/152-01-SUMMARY.md` when done.
</output>
