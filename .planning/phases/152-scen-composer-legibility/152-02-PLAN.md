---
phase: 152-scen-composer-legibility
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-state.ts
  - src/app/(dashboard)/allocations/lib/scenario-state.test.ts
autonomous: true
requirements: [SCEN-02]

must_haves:
  truths:
    - "A draft whose addedStrategies[0] carries isOwn: true survives BOTH scenarioDraftSchema and scenarioDraftSaveSchema with the field intact (not stripped)"
    - "A pre-152 v4 blob whose added strategies lack isOwn decodes outcome 'ok' — never the draft-deleting reset"
    - "isOwn: null (what JSON.stringify can produce) decodes 'ok'"
    - "SCENARIO_SCHEMA_VERSION is still 4"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      provides: "isOwn on interface AddedStrategy + nested addedStrategySchema"
      contains: "isOwn: z.boolean().nullish()"
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.test.ts"
      provides: "populated-fixture strip guard + backward decode + null tolerance + version discipline"
      contains: "addedStrategies[0].isOwn"
  key_links:
    - from: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      to: "addedStrategySchema (nested, :845-850)"
      via: "the field MUST be on the nested schema, not the top-level draft schema"
      pattern: "isOwn: z\\.boolean\\(\\)\\.nullish\\(\\)"
    - from: "src/app/(dashboard)/allocations/lib/scenario-state.test.ts"
      to: "scenarioDraftSaveSchema"
      via: "strip guard asserts through the SAVE-boundary schema too (saved/route.ts persists parsed.data.draft)"
      pattern: "scenarioDraftSaveSchema"
---

<objective>
SCEN-02 wire, draft side: declare `isOwn` on the NESTED `addedStrategySchema` so
the ownership bit survives every localStorage codec round-trip and both save-route
POSTs — with the strip-guard test built on a POPULATED `addedStrategies` fixture
(the 151-06 template fixture is `[]`; a copy-paste is provably vacuous — measured
trap, PATTERNS Mapper Note 1).

Purpose: `z.object` strips undeclared keys and `saved/route.ts` persists
`parsed.data.draft` — without this declaration the chip works in dev and silently
vanishes on every refresh. This is the phase's single highest-probability silent
failure.
Output: widened `AddedStrategy` interface + nested zod field; the SCEN-02 codec
test block; SC1 falsifier observed RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-scen-composer-legibility/152-RESEARCH.md
@.planning/phases/152-scen-composer-legibility/152-PATTERNS.md

Binding traps for this plan (from planning context):
- Additive zod field on the NESTED `addedStrategySchema` (scenario-state.ts:845-850),
  `.nullish()`, NO `SCENARIO_SCHEMA_VERSION` bump, NO refine (a refine failure
  routes the codec to the draft-deleting reset — scenario-state.ts:925-932).
- Strip-guard fixture MUST have a non-empty `addedStrategies` array and assert
  `parsed.data.addedStrategies[0].isOwn`.
- Wave-0 gap (152-VALIDATION.md) is CLOSED: planner grep confirmed zero
  `.strict()`/`strictObject` readers of `addedStrategies` under `src/app/api/**`
  and zero references in `analytics-service/**` (matches PATTERNS Mapper Note 3).
  The wire add cannot turn into a 400.
</context>

<interfaces>
The strip point (scenario-state.ts:845-850, current):

```ts
const addedStrategySchema = z.object({
  id: z.string(),
  name: z.string(),
  markets: z.array(z.string()),
  strategy_types: z.array(z.string()),
});
```

Target interface addition (scenario-state.ts:96-104 — keep the branded `id`):
`isOwn?: boolean | null;` on `interface AddedStrategy`.

Mutators (`addStrategyBrowse:484`, `addStrategyBridge:519`) need NO change — they
push the whole strategy object; the field rides along.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SCEN-02 codec test block with a POPULATED fixture (RED)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts:1040-1126 (the
      151-06 `AUM-01 manualAumUsd` describe — the structural template; note
      :1059 `addedStrategies: []` is the vacuity trap, do NOT copy the fixture)
    - src/app/(dashboard)/allocations/lib/scenario-state.ts:835-1000 (nested
      schema, draft schema, save schema + its memberKeyIds superRefine, codec
      version branches — needed to build a save-valid populated fixture)
  </read_first>
  <behavior>
    - Backward decode: a v4 blob whose `addedStrategies[0]` has NO `isOwn` key
      decodes with `outcome === "ok"`; `weightOverrides` and the added strategy
      itself are preserved (never reset).
    - Null tolerance: the same blob with `isOwn: null` decodes `"ok"` — never
      `schema_invalid`, never a reset.
    - Strip guard (the phase's key test): a draft whose `addedStrategies[0]` is a
      full literal `{ id, name, markets, strategy_types, isOwn: true }` safeParses
      through `scenarioDraftSchema` AND `scenarioDraftSaveSchema`, with
      `parsed.data.addedStrategies[0].isOwn` strictly `true` in BOTH results.
      The save-schema fixture must satisfy the memberKeyIds superRefine (read it;
      construct accordingly).
    - Version discipline: `expect(SCENARIO_SCHEMA_VERSION).toBe(4)` re-asserted in
      this block (extends the :1123-1125 discipline).
  </behavior>
  <action>
    Add `describe("SCEN-02 isOwn (Phase 152)")` after the AUM-01 block, mirroring
    its four-test structure but with a local `v4DraftWithAdded()` fixture whose
    `addedStrategies` array is POPULATED (one entry, id cast at the construction
    boundary per the branded-id TSDoc). Comment the fixture with why it differs
    from `v4Draft()` (:1059 vacuity trap — a nested-field test over `[]` asserts
    nothing). Run the file: strip-guard and null-tolerance tests must be RED
    (field not yet declared → stripped / rejected); backward-decode and version
    tests will be green from the start (that is expected — they pin the
    no-regression side).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - New describe exists; its fixture's `addedStrategies` has `length >= 1`
      (grep the fixture literal — no empty array).
    - Strip-guard asserts `parsed.data.addedStrategies[0].isOwn` (exact path)
      through BOTH schemas.
    - Strip-guard test is RED against unmodified scenario-state.ts with a
      strip/parse failure (proves non-vacuity BEFORE implementation).
    - All pre-existing tests in the file still pass.
  </acceptance_criteria>
  <done>SCEN-02 test block committed, strip guard RED for the right reason.</done>
</task>

<task type="auto">
  <name>Task 2: Declare isOwn on interface + nested schema (GREEN) and observe the SC1 falsifier</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.ts:96-104 (interface),
      :845-850 (nested schema), :910-932 (the leverageOverrides + manualAumUsd
      load-bearing comments — the comment discipline to mirror)
  </read_first>
  <action>
    1. `interface AddedStrategy` (:96-104): add `isOwn?: boolean | null;` with a
       TSDoc naming Phase 152 SCEN-02, that the value is the server-computed
       ownership bit from GET /api/strategies/browse, that absence/null (legacy
       drafts, Bridge-added candidates) means unknown → NO chip (never fabricate
       ownership — CONTEXT lock), and that it is additive with no
       SCENARIO_SCHEMA_VERSION bump.
    2. `addedStrategySchema` (:845-850): add `isOwn: z.boolean().nullish(),` with
       a comment mirroring the :920-932 discipline verbatim in voice: ⚠️
       LOAD-BEARING — `z.object` STRIPS unknown keys and saved/route.ts persists
       `parsed.data.draft`, so WITHOUT this declaration the browse-added
       ownership bit is silently dropped on every localStorage round-trip and
       every save POST; `.nullish()` not `.optional()` because `JSON.stringify`
       writes `null` for values it cannot represent and a bare `z.boolean()`
       REJECTS null → schema_invalid → the user's whole scenario deleted;
       DELIBERATELY no refine.
    3. Touch nothing else: no mutator change, no version bump, no new branch in
       the codec.

    Then run the test file (all green), and observe the SC1 falsifier for the
    Falsifiability Ledger: temporarily delete the `isOwn` line from
    `addedStrategySchema` (leave the interface), re-run
    `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" -t "isOwn" --no-file-parallelism`,
    confirm the strip guard goes RED, restore the line, re-run green. Record
    "Observed ✅ RED then GREEN" for SC1 in
    `.planning/phases/152-scen-composer-legibility/152-VALIDATION.md`'s
    Falsifiability Ledger (edit the Observed cell).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism 2>&1 | tail -5 && grep -n "isOwn: z.boolean().nullish()" "src/app/(dashboard)/allocations/lib/scenario-state.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `isOwn: z.boolean().nullish()` present on the NESTED schema (line falls
      inside the `addedStrategySchema` object literal, NOT inside
      `scenarioDraftSchema`'s own key list).
    - `grep -c "SCENARIO_SCHEMA_VERSION = 4" "src/app/(dashboard)/allocations/lib/scenario-state.ts"` unchanged from before the edit (no bump).
    - No `.refine`/`.min`/`.max` added anywhere in the diff.
    - Full scenario-state.test.ts green; falsifier observed RED-then-GREEN and
      recorded in 152-VALIDATION.md SC1 row.
  </acceptance_criteria>
  <done>Field declared on nested schema; strip guard green; SC1 falsifier observed and logged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → codec | user-editable persisted blob re-enters the app |
| client → POST/PUT /api/allocator/scenario/saved | draft blob crosses to DB via scenarioDraftSaveSchema |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-02-01 | Denial of Service (self-inflicted) | codec reset path | mitigate | `.nullish()`, no refine, no version bump — a legacy/null blob can never route to the draft-deleting reset (tests: backward decode + null tolerance) |
| T-152-02-02 | Tampering | hand-edited localStorage `isOwn: true` | accept | self-spoof only: the chip renders on the user's own screen for the user's own draft; it is not a trust signal to any other party and never re-enters the server as an authority claim |
| T-152-02-03 | Denial of Service | draft blob growth | accept | ~12 bytes/entry under existing caps (`addedStrategies.max(200)`, `MAX_DRAFT_BODY_BYTES` 256,000) |
| T-152-02-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism`
- `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.localStorage.test.ts" --no-file-parallelism` (round-trip suite must stay green untouched)
</verification>

<success_criteria>
- Strip guard (populated fixture, both schemas) green; proven RED under the schema-deletion mutation.
- Legacy v4 blobs and `isOwn: null` blobs decode "ok"; version stays 4.
</success_criteria>

<output>
Create `.planning/phases/152-scen-composer-legibility/152-02-SUMMARY.md` when done.
</output>
