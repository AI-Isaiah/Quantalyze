---
phase: 152-scen-composer-legibility
plan: 05
type: execute
wave: 3
depends_on: [152-02, 152-03, 152-04]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [SCEN-02]

must_haves:
  truths:
    - "A strategy added from the Browse drawer in EITHER branch (empty-state mount and main-body mount) carries isOwn into the draft — an own strategy shows the Yours chip on its composer row"
    - "A Bridge-added strategy carries NO isOwn (deliberately absent — never fabricated) and renders no chip"
    - "isOwn false, null, or absent (legacy persisted draft) renders NO chip node at all"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "isOwn mapped at seams A (:4074) and B (:5418); deliberate-absence comment at seam C (:5454); YoursChip on the added row"
      contains: "scenario-yours-"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "two-branch seam tests + four-state chip render tests"
      contains: "scenario-yours-"
  key_links:
    - from: "ScenarioComposer.tsx seams A+B"
      to: "drawer onAdd payload (152-04)"
      via: "isOwn: s.isOwn added to BOTH byte-identical twin literals"
      pattern: "isOwn: s\\.isOwn"
    - from: "added-row name cluster (:6268-6277)"
      to: "YoursChip (152-04 artifact)"
      via: "import + render between TrustTierLabel and CoverageStateChip, gated a.isOwn === true"
      pattern: "YoursChip"
---

<objective>
SCEN-02 complete: map `isOwn` through the composer's two Browse add seams (the
byte-identical twins — the single most likely partial edit in this phase), leave
it deliberately absent at the Bridge seam, and render the "Yours" chip on the
added-strategy row via the shared `YoursChip` from 152-04.

Purpose: an allocator's own uploaded strategy is visually distinguishable from a
third-party one in the composition list — the phase's first Success Criterion.
Output: seam edits + chip render + tests proving both mounts and all four render
states.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-scen-composer-legibility/152-UI-SPEC.md
@.planning/phases/152-scen-composer-legibility/152-PATTERNS.md
@.planning/phases/152-scen-composer-legibility/152-04-SUMMARY.md

Binding traps for this plan:
- Seams A (:4074-4081) and B (:5418-5425) are byte-identical twin JSX literals —
  edit BOTH. Seam C (:5452-5459, BridgeDrawer) gets NO field, only a comment
  (a Bridge candidate carries no ownership signal; absent = honest, CONTEXT lock).
- The test harness's `browseOnAdd` capturing mock records the LAST-RENDERED
  drawer mount only (Mapper Note 2): the "all seams" proof needs TWO test runs
  with different payloads — a blank-slate payload reaches Seam A (empty-state
  branch), a booked payload reaches Seam B (main body). One render cannot cover
  both.
- OwnershipTag COMPONENT reuse forbidden (T-150-08 closed switch) — the chip is
  the shared YoursChip leaf, whose strings are already byte-verbatim.
- Render gate is `a.isOwn === true` — the `{chipState && …}` idiom already at
  :6275-6277; never `!== false` (a legacy draft must render no chip).
</context>

<interfaces>
Seam A/B current literal (both sites, byte-identical):

```tsx
onAdd={(s) =>
  handleAddStrategy({
    id: s.id as AddedStrategy["id"],
    name: s.name,
    markets: s.markets,
    strategy_types: s.strategy_types,
  })
}
```

Target: insert `isOwn: s.isOwn,` (typechecks because 152-04 widened the drawer's
AddedStrategy payload type and 152-02 widened scenario-state's AddedStrategy).

Chip render site (:6268-6277): name span → TrustTierLabel → [NEW YoursChip] →
CoverageStateChip, all `shrink-0`. Import:
`import { YoursChip } from "./YoursChip";` (match 152-04's export form).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Map isOwn at seams A and B; deliberate absence at seam C</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:4060-4095
      (Seam A, empty-state drawer mount), :5405-5430 (Seam B, main-body mount),
      :5445-5465 (Seam C, BridgeDrawer → addStrategyBridge)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx:443-461
      + :585-599 (addStrategy helper + browseOnAdd capturing mock — note it
      captures ONE mount per render), plus an existing blank-slate-branch test
      and a booked-branch test to copy payload setups from
  </read_first>
  <behavior>
    - Run 1 (blank-slate payload → empty-state branch → Seam A): invoking
      `browseOnAdd({ id, name, markets, strategy_types, isOwn: true })` results
      in the added row rendering `scenario-yours-{id}` (after Task 2) — for THIS
      task, assert the draft path: the added strategy renders and, once Task 2
      lands, the chip; write the seam tests to assert via the chip testid and
      run them RED until Task 2 (or assert via a data-probe if the executor
      lands Task 2 first — Task ordering here: implement seams now, tests go
      green when Task 2's chip renders; the RED phase of these tests is the
      pre-seam state).
    - Run 2 (booked payload → composed branch → Seam B): same invocation, same
      assertion — a separate `render` in a separate `it`.
    - Bridge/legacy absence: `browseOnAdd({...})` WITHOUT isOwn → row renders,
      no `scenario-yours-` node (this is also the seam-level never-fabricate
      falsifier).
  </behavior>
  <action>
    In a new `describe("SCEN-02 seams + chip (Phase 152)")`, write the three
    <behavior> tests (they finish green after Task 2). Then edit the source:
    insert `isOwn: s.isOwn,` into BOTH twin literals (Seam A :4074, Seam B
    :5418) — grep afterwards to confirm exactly 2 occurrences. At Seam C
    (:5452-5459) add ONLY a comment: "Phase 152 SCEN-02 — deliberately NO isOwn:
    a Bridge candidate comes from the match engine and carries no ownership
    signal; absent = no chip (never fabricate ownership — CONTEXT lock)."
  </action>
  <verify>
    <automated>grep -c "isOwn: s.isOwn" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isOwn: s.isOwn"` on ScenarioComposer.tsx returns exactly 2
      (both twins, not one).
    - Seam C literal unchanged except the comment — `git diff` on :5445-5465
      shows no `isOwn` key.
    - The two seam tests exist as SEPARATE renders with different payloads
      (blank-slate vs booked) — not two invocations in one render.
  </acceptance_criteria>
  <done>Both twins carry the bit; Bridge absence is deliberate and commented; seam tests staged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Yours chip on the added-strategy row (four render states)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:6245-6280
      (added-row name cluster: name span :6268, TrustTierLabel :6271-6274,
      CoverageStateChip conditional :6275-6277)
    - src/app/(dashboard)/allocations/components/YoursChip.tsx (152-04 artifact —
      import surface)
  </read_first>
  <behavior>
    - `isOwn: true` → `scenario-yours-{id}` renders with text "Yours"; className
      contains "rounded-md" and "bg-badge-other/10".
    - `isOwn: false` → no node. `isOwn: null` → no node. isOwn absent (legacy
      draft shape) → no node. (Three separate assertions — the null and absent
      arms are the falsifiers for a `!== false` gate regression.)
    - DOM order within the name cluster: TrustTierLabel-bearing element (when
      present) precedes the chip; chip precedes CoverageStateChip (when present).
  </behavior>
  <action>
    Write the render-state tests (RED), then: import YoursChip and insert
    `{a.isOwn === true && (<YoursChip data-testid={`scenario-yours-${a.id}`} className="shrink-0" />)}`
    between the TrustTierLabel and the CoverageStateChip conditional, with a
    comment: Phase 152 SCEN-02 — ownership is a persistent fact (rounded-md
    family); gate `=== true`, absence is honest (legacy drafts un-marked until
    the next browse/add — CONTEXT lock). Then confirm Task 1's two seam tests
    are now green (chip is the observable for both mounts).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "SCEN-02" --no-file-parallelism 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All SCEN-02 describe tests green (both seam runs + four chip states +
      order assertion).
    - The gate expression is `a.isOwn === true` —
      `grep -c "a.isOwn === true" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 1 and no `isOwn !== false` appears.
    - Full ScenarioComposer.test.tsx run green (no regression across the 265+
      existing tests).
    - `npx eslint "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` clean.
  </acceptance_criteria>
  <done>SCEN-02 end-to-end: browse → seam → draft → chip, all states honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| drawer payload → persisted draft | client-side seam where ownership could be fabricated or dropped |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-05-01 | Spoofing | Bridge seam | mitigate | isOwn deliberately absent at seam C — never fabricated from a signal-less source; absent renders no chip; commented + tested |
| T-152-05-02 | Spoofing | chip render gate | mitigate | `=== true` gate with explicit false/null/absent no-render tests (a `!== false` regression goes RED) |
| T-152-05-03 | Tampering | hand-edited draft isOwn | accept | self-spoof on the user's own screen only (see T-152-02-02); the chip never re-enters any server decision |
| T-152-05-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism`
- `npx eslint "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"`
</verification>

<success_criteria>
- Own strategy added from EITHER drawer mount shows the Yours chip; Bridge-added and legacy rows never do.
- Chip anatomy is the shared YoursChip — one recipe across composer and browse.
</success_criteria>

<output>
Create `.planning/phases/152-scen-composer-legibility/152-05-SUMMARY.md` when done.
</output>
