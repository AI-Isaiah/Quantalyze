---
phase: 152-scen-composer-legibility
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [SCEN-04]

must_haves:
  truths:
    - "With ≥1 added strategy, exactly ONE header row of eyebrow labels WEIGHT USD MODE LEV NOTIONAL renders above the added-strategies group; with zero added strategies it does not render"
    - "The header is aria-hidden — its labels never enter the accessible tree"
    - "A non-derivable ADDED-row notional reads 'not applicable' with a CAUSE-ACCURATE title + sr-only sentence; the derived branch keeps its original title byte-verbatim"
    - "Per-key rows gain no header and their notional span is untouched"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "header li (testid scenario-added-header) + honest notional branch on the added-row span"
      contains: "scenario-added-header"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "header render-rule tests + notional two-branch tests"
      contains: "NOTIONAL_UNAVAILABLE"
  key_links:
    - from: "header li"
      to: "draft.addedStrategies.length > 0 guard (:6193)"
      via: "mounted inside the SAME guard as the 'Strategies added · N' separator"
      pattern: "Strategies added"
    - from: "added-row notional span (:6324)"
      to: "notionalText(a.id)"
      via: "title switches ONLY when notionalText yields the em-dash; arithmetic untouched"
      pattern: "scenario-constituent-notional"
---

<objective>
SCEN-04: label the numbers. One aria-hidden mono-eyebrow header li above the
added-strategies group (five labels: WEIGHT USD MODE LEV NOTIONAL — UI-SPEC's
correction of CONTEXT's four), and an honest non-derivable notional on the
ADDED-strategy row: em-dash + cause-accurate `title` + `sr-only` sentence, per
151's exact pattern.

Purpose: the founder's verbatim complaint — "What do the numbers actually mean?"
— and an em-dash that currently reads "broken" instead of "not applicable".
Output: header li + notional branch in ScenarioComposer.tsx; render tests; SC3
falsifier observed RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-scen-composer-legibility/152-UI-SPEC.md
@.planning/phases/152-scen-composer-legibility/152-PATTERNS.md

## Decision D-3 — RESOLVED as a deliberate CONTEXT narrowing (record verbatim)

CONTEXT (and UI-SPEC's copy table) pin the notional-cell title as "Set portfolio
AUM to size in dollars". That sentence is CAUSE-INACCURATE for this cell: the
added-row notional's em-dash is driven by `totalBookEquity == null` or a missing
blend share (`notionalText`, ScenarioComposer.tsx:5785-5796) — NOT by
`scenarioAum`. Shipping it would tell a book-less allocator to set a number that
cannot make the cell derivable (the two numbers are documented as deliberately
distinct at CompositionListProps:5691-5698). Per the founder honesty rule
(dishonest-remedy copy is exactly the defect class this phase exists to fix), this
plan pins the CAUSE-ACCURATE sentence below instead. The CONTEXT-pinned AUM
sentence stays untouched on the USD cell (`AUM_UNSET_REMEDY`, :5815) where it IS
accurate. This narrowing was resolved at planning time by the orchestrator's D-3
escalation; do not re-litigate in review.

**Pinned copy (exact, with U+2014 em-dash):**
`Notional needs live book equity — not derivable in this scenario`

## Scope pins

- The `scenario-constituent-notional` testid exists on TWO spans: per-key row
  (:6181) and added row (:6324). ONLY the added-row span (:6324) changes —
  UI-SPEC's scope line covers added-strategy rows; the per-key span is untouched
  (Rule 3 surgical). Tests must scope by row, not by bare getByTestId.
- Do NOT touch `notionalText`'s arithmetic (:5794), `handleWeightChange`,
  `commitDollarInput`, or any 151 control — 151 owns sizing.
- No list-level keyboard nav indexes the constituent ul (RESEARCH Wire Trace C
  census) and a non-row li precedent already exists (:6193) — inserting the
  header li is safe; the three `getAllByRole("listitem")` assertions in the test
  file are scoped to the PCR list, not this ul.
- Pitfall 3 accepted: the header is sized for the DEFAULT Leverage mode; a row
  switched to Target max-DD injects a w-16 sub-control and drifts on that row
  only. State this limitation in a code comment so review does not log it as a
  defect.
</context>

<interfaces>
Mount point (ScenarioComposer.tsx:6193-6198):

```tsx
{draft.addedStrategies.length > 0 && (
  <li className="mt-2 px-1 text-xs uppercase tracking-wider text-text-muted">
    Strategies added · {draft.addedStrategies.length}
  </li>
)}
{draft.addedStrategies.map((a) => {
```

Eyebrow recipe (byte-verbatim, :4312): `font-mono text-fixed-10 uppercase tracking-[0.18em] text-text-muted`
Row outer geometry: li `p-3 border` (:6245) → div `flex w-full items-center justify-between gap-3` (:6249) → cluster `flex items-center gap-2` (:6279).
Column widths: weight `w-20`, USD `w-24`, mode toggle content-width (`renderModeToggle` :5930-5951 classes), leverage `w-16`, notional `w-20`, remove `×` `px-2 py-1 text-xs` (:6330-6337).
Added-row notional span (:6321-6329) — derived title to preserve byte-verbatim:
`"Notional = equity × blend share × leverage — derived, informative only (minimum-investment check); never a weight input"`
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Header label li above the added-strategies group</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:6040-6070
      (the ul: `grid gap-2`), :6193-6198 (mount point), :6245-6337 (row geometry +
      control cluster + widths), :5930-5951 (renderModeToggle classes), :6330-6337
      (remove button classes), :4310-4315 (eyebrow recipe in situ)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx:11845-11890
      (AUM-01 helper cluster: renderUsd/add/setAum — the harness for these tests),
      :585-599 (browse drawer capturing mock)
  </read_first>
  <behavior>
    - Zero added strategies → `queryByTestId("scenario-added-header")` is null
      (even when per-key rows render).
    - ≥1 added strategy → header renders EXACTLY once (`getAllByTestId(...).length === 1`
      with 2+ added rows).
    - Header carries `aria-hidden="true"`; its five labels are, in DOM order,
      exactly `WEIGHT`, `USD`, `MODE`, `LEV`, `NOTIONAL` (no separator glyphs).
    - Header appears AFTER the "Strategies added ·" separator li and BEFORE the
      first added row (assert via `compareDocumentPosition` or sibling order).
    - Full-file run stays green (PCR listitem tests untouched by the non-row li).
  </behavior>
  <action>
    Write the Task-1 behavior tests first in a new
    `describe("SCEN-04 header (Phase 152)")` block (reuse the AUM-01 helper
    cluster; run RED), then implement:

    Insert a new `<li aria-hidden="true" data-testid="scenario-added-header">`
    between the separator li and `{draft.addedStrategies.map(...)}`, INSIDE the
    same `draft.addedStrategies.length > 0` guard. Structure:
    - li classes reproduce the ROW's horizontal inset so labels align over
      columns: `mb-1 border border-transparent px-3` (rows carry `p-3 border`;
      the transparent border matches the 1px inset; vertical padding omitted —
      the grid's gap-2 separates items).
    - Inside: `<div className="flex w-full items-center justify-between gap-3">`
      with an empty left spacer `<span />` (over the name cluster) and a right
      cluster `<div className="flex items-center gap-2 font-mono text-fixed-10 uppercase tracking-[0.18em] text-text-muted">`.
    - Labels in the cluster, mirroring live column widths, right-aligned:
      `<span className="w-20 text-right">WEIGHT</span>`,
      `<span className="w-24 text-right">USD</span>`, MODE (see below),
      `<span className="w-16 text-right">LEV</span>`,
      `<span className="w-20 text-right">NOTIONAL</span>`, then a trailing
      spacer sized like the remove button: an `invisible` span carrying the ×
      button's box classes (`px-2 py-1 text-xs`) with content `×`.
    - MODE column has no fixed width (the toggle is content-sized). Use the
      invisible-sizer idiom so the label column matches the DEFAULT toggle
      byte-for-byte: a `relative` span carrying the toggle's box classes (copy
      from renderModeToggle: padding/border/text classes, minus interactivity),
      containing an `invisible` "Leverage" text node for width plus an
      absolutely-positioned overlay span with the eyebrow classes rendering
      `MODE`. Add the Pitfall-3 comment: header is sized for the default
      Leverage mode; a Target-mode row drifts on that row only — accepted
      limitation (UI-SPEC Component Contract 3), do not "fix" per-row.
    - Comment the li with Phase 152 SCEN-04 + why it is aria-hidden (every
      control already carries its own sr-only/aria-label name; announcing the
      strip would duplicate them).

    Lint constraint: `no-raw-font-px` is `error` — no `text-[10px]`; the eyebrow
    recipe's `text-fixed-10` is the only font size used.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "SCEN-04 header" --no-file-parallelism 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - All five <behavior> assertions implemented as tests and green.
    - `grep -c "scenario-added-header" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 1 (single mount).
    - Label copy is exactly the five strings — `grep -o "WEIGHT\|NOTIONAL" ...` finds each once in the header block; no `·` between labels.
    - The header li sits inside the existing `length > 0` guard (same conditional
      expression — no second guard).
    - `npx eslint "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` clean.
  </acceptance_criteria>
  <done>Header renders once above the added group, aria-hidden, exact copy, tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest non-derivable notional on the added row + observe the SC3 falsifier</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:5779-5800
      (notionalText — DO NOT touch its arithmetic), :5815 (AUM_UNSET_REMEDY, the
      OTHER cell's sentence — stays), :5864-5922 (renderDollarInput's em-dash
      pattern to copy: title + sr-only + the load-bearing comment at :5870-5874),
      :6178-6187 (per-key notional span — UNTOUCHED), :6321-6329 (added-row
      notional span — the target)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx:12143-12174
      (AUM-01 Test 6 — the absence-branch test shape: tagName, title, textContent,
      within(cell).getByText sr-only probe)
  </read_first>
  <behavior>
    - Non-derivable branch (blank-slate payload + one added strategy →
      totalBookEquity is null → notionalText yields "—"; in this state exactly
      ONE `scenario-constituent-notional` testid exists — assert
      `queryAllByTestId(...).length === 1` first, Mapper Note 5): the span's
      `title` is exactly "Notional needs live book equity — not derivable in this
      scenario"; textContent contains "—"; `within(cell).getByText(<same
      sentence>)` finds the sr-only node.
    - Derived branch (booked payload + equity + added strategy in-blend, scoped
      to the ADDED row's span — the LAST testid instance or `within` the li
      containing the added strategy's name): `title` is byte-verbatim the
      original derived sentence ("Notional = equity × blend share × leverage —
      derived, informative only (minimum-investment check); never a weight
      input"); NO sr-only remedy node inside the cell; textContent is a $ figure,
      not "—".
    - Per-key notional span (:6181) diff-untouched.
  </behavior>
  <action>
    Write the two-branch tests in a `describe("SCEN-04 honest notional (Phase 152)")`
    block first (RED), then implement:

    1. Define, adjacent to `AUM_UNSET_REMEDY` (:5815):
       `const NOTIONAL_UNAVAILABLE_NOTE = "Notional needs live book equity — not derivable in this scenario";`
       with a comment recording the D-3 narrowing: deliberately NOT the
       CONTEXT-pinned AUM sentence — this cell's em-dash is caused by
       `totalBookEquity == null` (:5787-5792), not scenarioAum; the AUM sentence
       names a remedy that cannot make this cell derivable and stays on the USD
       cell where it is accurate (founder honesty rule; resolved at planning).
    2. In the ADDED-row span only (:6321-6329): hoist
       `const nText = notionalText(a.id);` in the row body, set
       `title={nText === "—" ? NOTIONAL_UNAVAILABLE_NOTE : "<original derived sentence verbatim>"}`,
       render `{nText}` plus, ONLY when `nText === "—"`,
       `<span className="sr-only">{NOTIONAL_UNAVAILABLE_NOTE}</span>` (the
       :5870-5874 rationale: a title alone is unreachable by keyboard/touch).
       Keep the span's classes and testid unchanged; keep the WEIGHTS-00 comment.
    3. Do not touch :6178-6187 (per-key span), notionalText, or any 151 control.

    Then observe the SC3 falsifier for the Falsifiability Ledger: temporarily
    apply the remedy title to the DERIVED branch too (make the title
    unconditional), run
    `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "honest notional" --no-file-parallelism`,
    confirm the derived-title-verbatim test goes RED, revert, re-run green.
    Record "Observed ✅" in 152-VALIDATION.md's SC3 ledger row.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" -t "SCEN-04" --no-file-parallelism 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Both <behavior> branches green; the derived-branch test pins the original
      title byte-verbatim (it is the falsifier for "patched the wrong branch" —
      Pitfall 4).
    - `git diff` shows zero changes between lines of the per-key notional span
      and zero changes to `notionalText`'s body.
    - The exact string "Notional needs live book equity — not derivable in this
      scenario" appears in ScenarioComposer.tsx exactly once (the const), and
      `AUM_UNSET_REMEDY` still equals "Set portfolio AUM to size in dollars".
    - SC3 falsifier observed RED-then-GREEN and recorded in 152-VALIDATION.md.
  </acceptance_criteria>
  <done>Added-row notional honest in both branches; wrong-branch falsifier proven; ledger updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render → user | user-facing claims about what a number means (honesty surface, no data crossing) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-03-01 | Tampering (integrity of user-facing claims) | notional title copy | mitigate | cause-accurate sentence pinned (D-3); derived-branch byte-verbatim test is the falsifier against dishonest-remedy regressions |
| T-152-03-02 | Information Disclosure | header li | accept | presentational, aria-hidden, renders no data — nothing to disclose |
| T-152-03-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism` (full file — proves the non-row li broke no existing assertion)
- `npx eslint "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"`
</verification>

<success_criteria>
- Header: renders iff ≥1 added strategy, exactly once, aria-hidden, five exact labels, mounted between separator and first row.
- Notional: non-derivable added-row cell reads em-dash + cause-accurate title + sr-only; derived branch untouched byte-verbatim; per-key span untouched.
</success_criteria>

<output>
Create `.planning/phases/152-scen-composer-legibility/152-03-SUMMARY.md` when done.
</output>
