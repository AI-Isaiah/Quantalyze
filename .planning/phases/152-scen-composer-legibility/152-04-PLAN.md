---
phase: 152-scen-composer-legibility
plan: 04
type: execute
wave: 2
depends_on: [152-01]
files_modified:
  - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx
  - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx
  - src/app/(dashboard)/allocations/components/YoursChip.tsx
autonomous: true
requirements: [SCEN-02, SCEN-05]

must_haves:
  truths:
    - "Adding an own strategy from Browse hands the composer an AddedStrategy payload carrying isOwn: true; a row without the wire field hands isOwn undefined (never fabricated)"
    - "Two OWN rows with colliding names (case/whitespace-insensitive) in the FILTERED result both show 'Created {Mon D, YYYY} · {Status}'; a unique own row, and third-party rows with identical labels, show no line"
    - "Narrowing the filter so only one of the colliding rows remains removes the line"
    - "Own browse rows show the muted 'Yours' chip; third-party rows never do"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/YoursChip.tsx"
      provides: "shared ownership chip leaf — byte-verbatim OwnershipTag ANATOMY + team_review ink"
      contains: "bg-badge-other/10 text-text-muted"
    - path: "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"
      provides: "isOwn/created_at/status on StrategyBrowseRow; isOwn through handleAdd; dedup line; chip"
      contains: "browse-dedup-"
    - path: "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx"
      provides: "onAdd payload test + 6 dedup discrimination tests + chip tests"
      contains: "browse-dedup-"
  key_links:
    - from: "StrategyBrowseDrawer.tsx handleAdd (:333)"
      to: "onAdd payload"
      via: "isOwn: s.isOwn pass-through (the 4th construction site — unreachable from ScenarioComposer.test.tsx, Pitfall 2)"
      pattern: "isOwn: s\\.isOwn"
    - from: "dedup line"
      to: "filtered memo (:280)"
      via: "collision set computed over FILTERED rows (D-2), own-only, normalized trim().toLowerCase()"
      pattern: "browse-dedup-"
---

<objective>
SCEN-05 render + the drawer leg of the SCEN-02 wire, plus D-4 chip parity: widen
the drawer's own structural row/payload declarations, pass `isOwn` through
`handleAdd` (the fourth construction site — only testable from this file's suite),
render the own-vs-own duplicate disambiguation line, and show the "Yours" chip on
own browse rows via a new shared `YoursChip` leaf that Plan 152-05 will reuse on
the composer row.

Purpose: the founder's two private "Alpha Centauri" rows become resolvable at a
glance; the ownership bit survives the drawer seam.
Output: YoursChip.tsx (new); widened + rendering StrategyBrowseDrawer; drawer
tests; SC4 falsifier observed RED.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/152-scen-composer-legibility/152-UI-SPEC.md
@.planning/phases/152-scen-composer-legibility/152-PATTERNS.md

Decisions resolved for this plan:
- D-1 (adopted): NO `key_count` on the wire. The dedup line copy is
  `Created {Mon D, YYYY} · {Status}` — the `{N} keys` segment is OMITTED entirely
  (UI-SPEC's own omit-when-absent branch: "If key_count is 0/absent, omit that
  segment rather than render '0 keys' as a claim"). Record as a deliberate
  narrowing of UI-SPEC's copy-table example; created_at alone resolves the
  founder's actual case (rows 15 days apart).
- D-2 (adopted): collision scope = the FILTERED result, own-vs-own only.
  Two O(n) passes over `filtered`: build a normalized-name→count map over rows
  with `isOwn === true`, then tag rows whose count ≥ 2. A filter that narrows a
  collision to one row clears the line (tiebreaker, not a metadata dump).
- D-4 (adopted, orchestrator): the Yours chip DOES render on own browse rows,
  identical anatomy — via the shared YoursChip component, not a second recipe.

Status product-casing: raw wire enum → capitalize first letter, underscores to
spaces: draft→Draft, pending_review→Pending review, published→Published,
archived→Archived, private→Private. Never render the raw enum.
</context>

<interfaces>
Drawer's own declarations (deliberate structural duplicates, documented :38-42):

```ts
// StrategyBrowseRow (:43-56) — add, optional, TSDoc'd like is_example (:49-55):
isOwn?: boolean;      // Phase 152 SCEN-02 — server-computed; absent (old wire) → treat as unknown
created_at?: string;  // Phase 152 SCEN-05 — OWN rows only; absent → no dedup line
status?: string;      // Phase 152 SCEN-05 — OWN rows only; raw enum, client product-cases

// drawer AddedStrategy (:64-69) — the onAdd payload contract — add:
isOwn?: boolean;
```

handleAdd (:333-339) target: append `isOwn: s.isOwn,` (pass-through; undefined
stays undefined — never coerce, never fabricate).

Dedup line anatomy (clone of the sibling codename line :560-564):
`<div className="mt-1 text-xs text-text-muted" data-testid={`browse-dedup-${s.id}`}>`

YoursChip (new file, CoverageStateChip.tsx component-shape precedent):
- module consts: ANATOMY = "inline-flex items-center rounded-md px-2 py-0.5 text-caption font-medium"
  (byte-verbatim OwnershipTag.tsx:35) and INK = "bg-badge-other/10 text-text-muted"
  (byte-verbatim OwnershipTag.tsx:48 team_review ink)
- renders `<span className={cn(ANATOMY, INK, className)} {...testid}>Yours</span>`
- props: `{ className?: string; "data-testid"?: string }` — no variant prop, closed by construction
- header comment: Phase 152 SCEN-02; rounded-md = persistent-fact badge family
  (Phase-150 reasoning); do NOT widen OwnershipTag (T-150-08 closed switch) and
  do NOT route through Badge (DRAFT-fallback spoof); sentence-case "Yours"
  (uppercase belongs to the rounded-sm derived-state family)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen drawer row/payload declarations + carry isOwn through handleAdd</name>
  <files>src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx, src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx:38-69
      (row + payload declarations, is_example TSDoc voice), :333-339 (handleAdd)
    - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx:73-103
      (renderDrawer + flush harness), :239-249 (T9 onAdd payload assertion — the
      template)
    - src/app/api/strategies/browse/route.ts (landed 152-01 — BrowseStrategyRow
      TSDoc, the wire shape being mirrored)
  </read_first>
  <behavior>
    - Own-row fixture (`isOwn: true` via `fetchStrategies` override) → clicking
      add calls `onAdd` with a payload where `payload.isOwn === true`.
    - Legacy-row fixture (no `isOwn` key) → `payload.isOwn` is `undefined`
      (assert `toBeUndefined()` — the drawer never fabricates a boolean).
  </behavior>
  <action>
    Write the two T9-pattern payload tests first (RED), then: add the three
    optional fields to `StrategyBrowseRow` and `isOwn?: boolean` to the drawer's
    `AddedStrategy`, each with a TSDoc in the is_example voice (phase/requirement,
    truthy meaning, absence meaning); append `isOwn: s.isOwn,` to the `handleAdd`
    payload with a comment naming this as construction site 4-of-4 (Pitfall 2)
    and that pass-through of undefined is deliberate (never fabricate ownership).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx" -t "isOwn" --no-file-parallelism 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Both payload tests green; the legacy test asserts `toBeUndefined()`
      explicitly (not just toMatchObject omission).
    - `grep -c "isOwn: s.isOwn" "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"` returns 1.
    - All pre-existing drawer tests green.
  </acceptance_criteria>
  <done>Fourth construction site carries the bit; both branches proven from this file's suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Own-vs-own dedup line over the filtered result + observe the SC4 falsifier</name>
  <files>src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx, src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx:280-305
      (filtered memo — collision detection lives here or in a sibling memo keyed
      on it), :541-605 (row render: name block, example-tag conditional, codename
      secondary line, and the :587-605 PR #620 testid-namespace rationale)
    - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx:322-366
      (T16 — the discriminating conditional-render template)
  </read_first>
  <behavior>
    - Two own rows named "Alpha Centauri" (created_at "2026-08-04T12:00:00.000Z"
      / "2026-07-20T12:00:00.000Z", status "private") → BOTH rows render
      `browse-dedup-{id}` whose text matches `Created {date} · Private`, where
      {date} is computed IN THE TEST as
      `new Date(fixtureIso).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })`
      — environment-stable against the CI-Node22/local-Node25 timezone divergence
      (this pins the WIRING; the format template is pinned separately by
      asserting the text starts with "Created " and contains " · ").
    - A single own row with a unique name → no `browse-dedup-` node.
    - Two THIRD-PARTY rows with identical rendered labels → no line (isOwn gate).
    - Own rows named "alpha centauri " and "Alpha Centauri" → both get the line
      (trim + case-insensitive normalization).
    - With the collision present, typing a search query that leaves only ONE of
      the two rows visible → the remaining row's line disappears (D-2: collision
      computed over `filtered`).
    - A colliding own row MISSING `created_at` (older wire shape) → no line on
      that row (UI-SPEC honesty invariant: rows missing the fields render no line).
    - The testid never begins with `browse-add-` (assert
      `queryAllByTestId(/^browse-add-.*dedup/).length === 0` or equivalent).
  </behavior>
  <action>
    Write the seven discrimination tests first, T16-shape, fixtures injected via
    `fetchStrategies` (RED). Then implement:

    1. Beside the `filtered` memo, derive the collision set with `useMemo` keyed
       on `filtered`: pass 1 builds `Map<normalizedName, count>` over entries
       with `s.isOwn === true` (`normalize = (n: string) => n.trim().toLowerCase()`);
       pass 2 yields `Set<normalizedName>` where count ≥ 2. O(n), no quadratic
       scan. Comment: D-2 — scope is the FILTERED list so the line is a
       tiebreaker, never a metadata dump.
    2. In the row's `min-w-0` text block, directly below the codename/markets
       line (:560-564), render — gated on
       `s.isOwn === true && typeof s.created_at === "string" && collisionSet.has(normalize(s.name))` —
       the div described in <interfaces>, with content:
       `Created {new Date(s.created_at).toLocaleDateString("en-US", { month: "short", day: "numeric", year: "numeric" })}`
       then, only when `s.status` is a non-empty string, ` · {productCase(s.status)}`
       (conditional-separator idiom of the sibling line). `productCase` is a tiny
       local helper (capitalize first letter, underscores→spaces) — comment that
       the raw enum never renders (UI-SPEC copy contract).
    3. Comment the D-1 narrowing at the render site: no key-count segment by
       design (second query per drawer open vs. a tiebreaker created_at already
       resolves; UI-SPEC's own omit-when-absent branch).

    Then observe the SC4 falsifier: temporarily drop the `s.isOwn === true` term
    from the collision-set builder (pass 1 counts ALL rows), run
    `npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx" -t "dedup" --no-file-parallelism`,
    confirm the two-third-party-rows test goes RED, revert, re-run green. Record
    "Observed ✅" in 152-VALIDATION.md's SC4 ledger row.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx" -t "dedup" --no-file-parallelism 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - All seven <behavior> tests green; the date expectation is computed from the
      fixture ISO via the same locale call (no hardcoded "Aug 4, 2026" literal
      that breaks in non-US-timezone CI).
    - `grep -c "browse-dedup-" "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"` ≥ 1 and no new testid starts with `browse-add-`.
    - Collision detection reads `filtered` (or a memo keyed on it) — grep shows
      the collision memo's dependency array contains `filtered`, not `strategies`.
    - SC4 falsifier observed RED-then-GREEN and recorded in 152-VALIDATION.md.
  </acceptance_criteria>
  <done>Founder's Alpha Centauri case resolvable; all absence branches discriminated; ledger updated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: YoursChip leaf + D-4 chip parity on own browse rows</name>
  <files>src/app/(dashboard)/allocations/components/YoursChip.tsx, src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx, src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx</files>
  <read_first>
    - src/components/strategy/OwnershipTag.tsx (whole file, 59 lines — the ANATOMY
      + team_review ink strings to copy byte-verbatim; the :19-24 closed-switch
      rationale you must NOT widen)
    - src/app/(dashboard)/allocations/components/CoverageStateChip.tsx (whole
      file, 69 lines — the leaf-chip component shape + header-comment discipline)
    - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx:541-560
      (row name cluster + example-tag conditional — the chip's render site)
  </read_first>
  <behavior>
    - Own row (`isOwn: true`) → `browse-yours-{id}` renders with exact text
      "Yours"; its className contains "rounded-md" AND "bg-badge-other/10"
      (locked honesty tokens — a regression to accent or the uppercase family
      fails here).
    - Third-party row → `queryByTestId("browse-yours-{id}")` null.
    - Row with `isOwn` absent → no chip (gate is `=== true`, never `!== false`).
  </behavior>
  <action>
    Write the three chip tests first (RED). Create
    `src/app/(dashboard)/allocations/components/YoursChip.tsx` exactly per
    <interfaces> (CoverageStateChip shape: module consts, cn(), header comment
    with the T-150-08 do-not-widen / do-not-Badge rationale). Then in the drawer
    row, after the example-tag conditional (:551-558), render
    `{s.isOwn === true && <YoursChip data-testid={`browse-yours-${s.id}`} className="shrink-0" />}`.
    The testid is outside the `browse-add-` namespace (PR #620 contract).
    D-4 note in the render-site comment: parity with the composer-row chip
    (152-05) via the SAME component — one recipe, two sites; the chip is NOT a
    substitute for the dedup line (both duplicates are own rows).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx" --no-file-parallelism 2>&1 | tail -6 && npx eslint "src/app/(dashboard)/allocations/components/YoursChip.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The three chip tests green; full drawer suite green.
    - YoursChip.tsx contains the byte-verbatim strings
      "inline-flex items-center rounded-md px-2 py-0.5 text-caption font-medium"
      and "bg-badge-other/10 text-text-muted"; renders sentence-case "Yours".
    - `grep -c "OwnershipTag" "src/app/(dashboard)/allocations/components/YoursChip.tsx"` output counts only comment references — no import of OwnershipTag, no import of Badge.
    - eslint clean (no-raw-font-px untriggered — text-caption only).
  </acceptance_criteria>
  <done>Shared chip leaf exists; browse parity live; composer plan can import it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browse wire → drawer render | pseudonymised third-party rows and own rows share one list |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-04-01 | Information Disclosure | dedup line | mitigate | `isOwn === true` gate on collision detection + render; the route (152-01) additionally never emits created_at/status for third-party rows — two independent fences, each tested |
| T-152-04-02 | Spoofing | Yours chip | mitigate | closed YoursChip leaf (no variant prop); never widens OwnershipTag's T-150-08 switch; never routes through Badge's DRAFT fallback; gate `=== true` |
| T-152-04-03 | Tampering (automation contract) | testids | mitigate | `browse-dedup-{id}` / `browse-yours-{id}` outside the `browse-add-` first-match namespace (PR #620); asserted in tests |
| T-152-04-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx" --no-file-parallelism`
- `npx eslint "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx" "src/app/(dashboard)/allocations/components/YoursChip.tsx"`
</verification>

<success_criteria>
- onAdd payload carries isOwn true/undefined honestly (construction site 4-of-4 covered from its only reachable suite).
- Dedup line: renders exactly on filtered own-vs-own collisions, timezone-stable date, product-cased status, no key-count claim.
- YoursChip shared leaf shipped; browse rows show it on own rows only.
</success_criteria>

<output>
Create `.planning/phases/152-scen-composer-legibility/152-04-SUMMARY.md` when done.
</output>
