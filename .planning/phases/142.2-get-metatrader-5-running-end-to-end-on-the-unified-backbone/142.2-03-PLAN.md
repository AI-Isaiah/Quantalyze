---
phase: 142.2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/broker_dailies.py
  - analytics-service/services/nav_twr.py
  - analytics-service/tests/test_broker_dailies.py
autonomous: true
requirements: [MT5-11, MT5-12]

must_haves:
  truths:
    - "Every combiner return path — all FIVE (combine_realized_and_funding :182, combine_native_ledger :233 AND :238, combine_sfox_balance_history :400, combine_mt5_deal_ledger :541) — stamps meta['series_completeness'] with a recognised value"
    - "combine_realized_and_funding stamps fill_derived_unproven ALWAYS (constant, deliberate — a data-driven refinement would newly admit keyed perps on venues with a live silent-truncation bug)"
    - "The D-15 ECONOMIC oracle exists: a realized-empty perp series is materially understated versus the same book with realized PnL present, AND is never certified — not a self-referential verdict-string check"
    - "Deribit behaviour is PRESERVED: ledger_complete on both return paths even when meta carries twr_chain_broken (tightening is census-gated, plan 142.2-04/08)"
    - "The verdict is NOT a member of NAV_TWR_GUARD_KEYS — membership auto-promotes computation_status to complete_with_warnings, which the gate PASSES (the fail-open trap)"
  artifacts:
    - path: "analytics-service/services/broker_dailies.py"
      provides: "SeriesCompleteness Literal + SERIES_COMPLETENESS_VALUES frozenset (the ONE registry) + five stamped return paths"
      contains: "SERIES_COMPLETENESS_VALUES"
    - path: "analytics-service/tests/test_broker_dailies.py"
      provides: "Per-combiner verdict tests + the economic falsification fixture (Wave-0 item)"
      contains: "understated"
  key_links:
    - from: "analytics-service/services/broker_dailies.py"
      to: "meta dict returned by every combiner"
      via: "meta[\"series_completeness\"] = ... before each return"
      pattern: "series_completeness"
---

<objective>
MT5-12 producer side: the code that produced the series states whether it is trustworthy. Define the
value set ONCE in Python and stamp all five combiner return paths. Build the D-15 economic oracle —
the phase's real acceptance test on the Python side.

Purpose: "dailies are canonical" holds for computation and fails for trust (D-15). Only the function
that consumed the raw venue inputs can judge whether they were whole; nothing else in the system is
allowed an opinion (RESEARCH Pattern 1).
Output: value registry, five stamps, per-combiner tests, the economic fixture.

**Value-set contract (LOCKED for the plan set):**
`ledger_complete | sampled_gapped | fill_derived_unproven | user_supplied | composite_stitched`.
This plan stamps the first three (combiners). `user_supplied` (keyless CSV, analytics_runner) and
`composite_stitched` (stitch job) are producers 3 and 2 — plan 142.2-05. Include ALL FIVE in
`SERIES_COMPLETENESS_VALUES` here, because the derive-seam assert (plan 05) validates against it.

**Per-combiner verdict map (LOCKED — from PATTERNS + RESEARCH Pattern 1, justifications verified in code):**
| Combiner | Return path(s) | Verdict |
|---|---|---|
| combine_realized_and_funding (binance/bybit/okx) | :182 | `fill_derived_unproven` — ALWAYS (retention caps, DQ-02 terminus, live OKX-paginator truncation make the input stream unprovably whole) |
| combine_native_ledger (deribit) | **:233 (Zavara allocated-capital branch) AND :238 (NAV branch)** | `ledger_complete` — both paths; stamping "the deribit combiner" once misses the live account |
| combine_sfox_balance_history (sfox) | :400 | `ledger_complete` iff `nav_gap_days == 0`, else `sampled_gapped` (the function already computes nav_gap_days at :393-400) |
| combine_mt5_deal_ledger (mt5) | :541 | `ledger_complete` (full-history fetch, fail-loud Mt5DealClassificationError before any series) |

⚠️ Behaviour scope, accepted by the founder: this is the FIRST completeness check deribit/sfox/mt5
have ever had. sFOX with interior NAV holes → newly refused (sFOX dormant, blast radius zero —
ship it). Deribit with `twr_chain_broken` → behaviour-PRESERVING `ledger_complete` (Pitfall 7;
tightening is gated on the PROD census in plan 142.2-04 and logged as a decision in 142.2-08 — do
not tighten here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-PATTERNS.md
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the one value registry and stamp all five return paths</name>
  <files>analytics-service/services/broker_dailies.py, analytics-service/services/nav_twr.py</files>
  <read_first>
    - analytics-service/services/broker_dailies.py — full file. The five return paths named above;
      the analog is combine_sfox_balance_history's existing meta write at :398-400
      (meta["nav_coverage_gap_days"] / meta["computation_status_hint"]) — the shape all four adopt.
    - analytics-service/services/nav_twr.py:120-200 — NavTWRMeta (TypedDict, total=False) and
      NAV_TWR_GUARD_KEYS (:191). The verdict key goes INTO NavTWRMeta; it stays OUT of
      NAV_TWR_GUARD_KEYS (membership auto-promotes to complete_with_warnings, which the gate passes
      — PATTERNS ⛔ "NAV_TWR_GUARD_KEYS is the trap, not the analog").
  </read_first>
  <action>
    In broker_dailies.py:
    1. Define near the top: `SeriesCompleteness = Literal["ledger_complete", "sampled_gapped",
       "fill_derived_unproven", "user_supplied", "composite_stitched"]` and
       `SERIES_COMPLETENESS_VALUES: frozenset[str] = frozenset({...same five...})` with a docstring
       naming this as THE one registry (no TS copy, no SQL CHECK — MT5-12) and which producer stamps
       which value (combiners here; user_supplied = analytics_runner keyless path; composite_stitched
       = run_stitch_composite_job).
    2. Stamp per the LOCKED map in the objective. For combine_native_ledger, stamp BOTH returns
       (:233 allocated-capital and :238 NAV). For sfox, key off the already-computed nav_gap_days.
       For deribit, add an inline comment stating the twr_chain_broken behaviour-preservation
       decision and pointing at the census (Pitfall 7).
    3. The verdict is an enum string ONLY — never write gap-day counts, row counts, or money into it
       (T-73-02: a verdict channel carries an enum, never a magnitude).
    In nav_twr.py:
    4. Add `series_completeness` to NavTWRMeta's annotations (keep the TypedDict total=False shape
       the file uses; document that the runtime derive-seam assert in job_worker (plan 05) is the
       required-ness enforcement, and mypy --strict pins the key's TYPE wherever NavTWRMeta is the
       declared meta). Do NOT add it to NAV_TWR_GUARD_KEYS.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_broker_dailies.py -x && mypy --strict services/broker_dailies.py services/nav_twr.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'meta\["series_completeness"\]' analytics-service/services/broker_dailies.py` ≥ 4 and every one of the five return paths (:182, :233, :238, :400, :541 at HEAD numbering) is preceded by a stamp on its own meta dict — verified by reading the diff, not by count alone.
    - `grep -n "series_completeness" analytics-service/services/nav_twr.py` shows the NavTWRMeta annotation; `grep -A5 "NAV_TWR_GUARD_KEYS" analytics-service/services/nav_twr.py` shows the tuple does NOT contain series_completeness.
    - SERIES_COMPLETENESS_VALUES contains exactly the five literals — including user_supplied and composite_stitched which no combiner stamps (the seam assert in plan 05 needs them).
    - Existing test_broker_dailies.py suite green unmodified (behaviour-preserving for every current caller: meta gains a key; nothing reads it yet).
    - `cd analytics-service && mypy --strict .` clean (fix via cast(), never # type: ignore).
  </acceptance_criteria>
  <done>One registry, five stamped return paths, verdict absent from the guard-key promotion channel.</done>
</task>

<task type="auto">
  <name>Task 2: Per-combiner verdict tests (including the sfox gap branch and BOTH deribit paths)</name>
  <files>analytics-service/tests/test_broker_dailies.py</files>
  <read_first>
    - analytics-service/tests/test_broker_dailies.py — fixture helpers `_funding_row` (:27) /
      `_realized_record` (:34); the economic-shaped precedent at :132
      `test_combine_funding_lifts_return_regression`.
    - analytics-service/services/broker_dailies.py (post-Task-1)
  </read_first>
  <action>
    Add unit cases, expected values as LITERALS in the test (oracle independence — never import
    SERIES_COMPLETENESS_VALUES for expectations):
    1. combine_realized_and_funding with realized+funding present → meta["series_completeness"] == "fill_derived_unproven" (the constant is deliberate — even a healthy-looking book is unproven).
    2. combine_mt5_deal_ledger fixture → "ledger_complete".
    3. combine_native_ledger NAV path → "ledger_complete"; combine_native_ledger with a
       denominator_config (allocated-capital branch) → "ledger_complete" — TWO separate cases, one
       per return path, so a future edit dropping one stamp reddens.
    4. combine_sfox_balance_history with zero interior gaps → "ledger_complete"; with an interior
       NAV hole (nav_gap_days > 0) → "sampled_gapped" — reuse however the existing sfox tests build
       gapped series.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_broker_dailies.py -x</automated>
  </verify>
  <acceptance_criteria>
    - Six new cases exist covering: ccxt constant, mt5, deribit NAV, deribit allocated-capital, sfox clean, sfox gapped — each asserting a string LITERAL typed into the test.
    - Running from analytics-service/ (NEVER repo root — a root run misses the VCR cassette dir and makes live broker calls): suite green.
  </acceptance_criteria>
  <done>Every verdict branch has a test that pins the literal, including the second deribit return path.</done>
</task>

<task type="auto">
  <name>Task 3: The D-15 economic falsification fixture + SC-1 (Python half) mutation run</name>
  <files>analytics-service/tests/test_broker_dailies.py</files>
  <read_first>
    - .planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md
      — § Oracle Independence (the D-15 fixture MUST assert an ECONOMIC INVARIANT, not the verdict
      string alone; three money bugs survived six review passes behind self-referential oracles) and
      Falsifiability row SC-1.
    - analytics-service/tests/test_broker_dailies.py:132 — test_combine_funding_lifts_return_regression,
      the nearest precedent for asserting money moved.
  </read_first>
  <action>
    Add `test_funding_only_series_is_understated_and_never_certified` (RESEARCH § Code Examples has
    the shape — adapt to the real fixture helpers):
    1. Build the TRUTH: combine_realized_and_funding(realized_records, funding_rows, ...) with a
       book where realized PnL is materially positive (e.g. realized dominating funding).
    2. Build the GAPPED series: same call with realized == [] (the fills gap — the exact failure the
       deleted gate branch existed to refuse).
    3. ECONOMICS assert: cumulative(gapped) < cumulative(truth) − MATERIALITY, with MATERIALITY a
       literal in the test sized so rounding noise cannot satisfy it.
    4. TRUST assert: gapped meta["series_completeness"] is NOT an allow-listed trust verdict —
       assert `meta["series_completeness"] == "fill_derived_unproven"` (literal) AND
       `meta["series_completeness"] not in ("ledger_complete", "user_supplied", "composite_stitched")`
       (hand-typed allow-list literals, NOT imported).
    5. **SC-1 mutation run (Python half — record in SUMMARY):** temporarily change
       combine_realized_and_funding's stamp to "ledger_complete" → this fixture MUST go RED (the
       TRUST assert) → revert → green. Paste the failing assertion into the SUMMARY under
       "Falsifiability evidence: SC-1 (python)". The TS half of SC-1 runs in plan 142.2-06.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_broker_dailies.py -x -k "understated"</automated>
  </verify>
  <acceptance_criteria>
    - The fixture asserts BOTH halves: a numeric cumulative-understatement inequality with a literal materiality bound, AND the never-certified verdict — deleting either assert is detectable in review.
    - No assertion compares the combiner's output to the combiner's own computation of the same quantity (the truth series is a DIFFERENT call with different inputs — that is the independence).
    - SC-1 (python) mutation RUN and Observed: SUMMARY carries the pasted failing assertion; working tree restored.
    - Full file green from analytics-service/; `mypy --strict .` clean.
  </acceptance_criteria>
  <done>The phase's safety property has a Python oracle pinned to economics, observed RED under the SC-1 mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| venue APIs → combiner meta | untrusted/incomplete venue data becomes a trust verdict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.2-06 | Tampering/Repudiation | publishing an understated track record as api_verified (D-15) | mitigate | ccxt path stamps fill_derived_unproven ALWAYS; the economic fixture is the acceptance test (SC-1) |
| T-142.2-07 | Tampering | verdict routed through NAV_TWR_GUARD_KEYS → auto-promoted to complete_with_warnings → gate passes | mitigate | key excluded from the tuple; acceptance grep asserts exclusion |
| T-142.2-08 | Information disclosure | verdict channel carrying magnitudes (account-size leak class T-73-02) | mitigate | enum-only stamp; no counts/money in the value |
| T-142.2-SC | Tampering | package installs | accept | no installs in this plan |
</threat_model>

<verification>
- `cd analytics-service && pytest && mypy --strict .` fully green before the plan closes (pytest ONLY from analytics-service/).
- SC-1 (python) recorded Observed in SUMMARY with pasted evidence.
</verification>

<success_criteria>
- Five stamped return paths, one registry, per-branch tests, and an economic oracle that goes RED when a producer over-certifies.
</success_criteria>

<output>
Create `.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-03-SUMMARY.md` when done.
</output>
