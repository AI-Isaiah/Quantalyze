---
phase: 142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone
verified: 2026-08-04T09:35:00Z
status: human_needed
score: 10/10 automated must-haves verified
overrides_applied: 0
re_verification:
  previous_status: null
  previous_score: null
  note: "Initial verification. No prior 142.2-VERIFICATION.md existed."
human_verification:
  - test: "MT5-05 — a founder completes the MT5 connect flow on production (quantalyze.xyz) with the live funded account, without needing to know an internal error code, a server name, or a flag"
    expected: "Broker-server field renders as plain readable text; submit reaches a rendered strategy after sync (~10-15 min for a fresh key); no step requires the founder to ask what a code means"
    why_human: "Outcome requirement, not the sum of MT5-01..04. The MT5 gateway is Railway-internal and unreachable from a laptop or CI; the shared TEST DB has no worker. Nothing from this phase is deployed. A green suite is explicitly not evidence — v1.15 shipped 6/6 phases green with both open items intact."
    tracked_in: "142.2-HUMAN-UAT.md (status: pending, gate: blocking-human, run_after: /ship + /land-and-deploy)"
deferred:
  - truth: "MT5's NUMBERS agree with the MT5 terminal (equity curve, period return, Sharpe, drawdown, daily bucketing) and the server-UTC offset is measured rather than assumed"
    addressed_in: "Phase 142.3"
    evidence: "ROADMAP.md:526 — 'Requirements: MT5-06..10 (split out of Phase 142.2 on 2026-08-03 at the D-14 valve)'; ROADMAP.md:537 — 'This phase is the gate on any MT5 is done claim.' Explicitly out of 142.2 scope by D-17."
  - truth: "The remaining 9 KEY_INVALID_FORMAT emitting sites in keys/validate-and-encrypt (4) and verify-strategy (5) are split onto honest codes"
    addressed_in: "TODOS.md DEF-142.2-02"
    evidence: "D-06 scoped MT5-04 to the two wizard-connect routes. Verifier re-measured at HEAD: 4 + 5 = 9 deferred sites, matching the booked deferral."
notes_for_humans:
  - "WARNING W-1 (documentation drift, non-blocking): REQUIREMENTS.md still carries MT5-03, MT5-04, MT5-11 and MT5-12 as unticked `- [ ]`, and its coverage table reads 'Pending (MT5-01, MT5-02 complete)'. All four are delivered and independently verified below. MT5-01/02 ARE ticked `- [x]`, which sets a tick-on-delivery precedent this leaves inconsistent."
  - "WARNING W-2 (documentation drift, non-blocking): REQUIREMENTS.md's MT5-04 text still cites the superseded counts — '28 sites ... (14) and (14)' and '11 remaining sites ... (5) and (6)'. The phase measured, and this verifier independently re-measured at HEAD, 24 in scope (12 + 12) and 9 deferred (4 + 5). The correction lives in VALIDATION.md and TODOS.md but never landed in REQUIREMENTS.md, so REQUIREMENTS.md now carries a number its own phase disproved."
---

# Phase 142.2: Get MetaTrader 5 running end to end on the unified backbone — Verification Report

**Phase Goal:** Make MT5 **reachable and honest** — a founder can connect a real MetaTrader 5 account
through the wizard and reach a rendered strategy, with no step requiring a human to know an internal
error code, a server name, or a flag, and with strategy admissibility decided by the **canonical
daily series itself** rather than a hand-maintained venue list.

**Verified:** 2026-08-04
**Status:** `human_needed` — **executed and code-verified, pending the MT5-05 human UAT on production**
**Re-verification:** No — initial verification.

> **Verification method.** SUMMARY.md and VALIDATION.md claims were treated as unverified assertions
> throughout. Every load-bearing property below was checked against source at HEAD, and **six of the
> ten falsifiability-ledger mutations were re-run by this verifier in its own process** — including
> SC-5, the one row the phase itself flagged as taken-on-trust. Every mutation was reverted and the
> tree confirmed clean (`git status --porcelain` empty) before the next check.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ⭐ A keyed **perp** with `fill_derived_unproven` + 135 csv rows + 0 trades is **REFUSED** (D-15 / SC-1) | ✓ VERIFIED | `strategyGate.test.ts:193-217` is the acceptance test; `expect(result.passed).toBe(false)` + `code === "INSUFFICIENT_TRADES"`. **Verifier re-ran the mutation**: widening `SERIES_TRUSTED_FOR_DAILY_BRANCH` with `"fill_derived_unproven"` reddened **exactly this one test**, `expected true to be false` at `:215:27`. Reverted. |
| 2 | ⭐ The gate is a **positive allow-list**; `null` and unrecognised verdicts fall to the trade branch (fail **CLOSED**) | ✓ VERIFIED | `strategyGate.ts:88-92` `SERIES_TRUSTED_FOR_DAILY_BRANCH = new Set(["ledger_complete","user_supplied","composite_stitched"])`; `:115` `.has(input.seriesCompleteness ?? "")` — the `?? ""` coercion can only refuse. **Verifier re-ran SC-2**: replacing membership with a deny-list (`!== "sampled_gapped"`) reddened **4** tests — null, unrecognised, composite-NULL, and the D-15 acceptance test. Reverted. |
| 3 | ⭐ Class fix complete: the predicate lives at **one** site, not three | ✓ VERIFIED | `grep -rn isLedgerBackedExchange` over `src/` = **0 hits** (survives only in CHANGELOG history and planning prose). Zero venue literals in `strategyGate.ts` (`grep -inE 'deribit\|bybit\|binance\|okx\|kraken\|sfox\|mt5\|metatrader\|hyperliquid\|coinbase'` → exit 1). Both admin sites import the shared module: `strategy-review/route.ts:11`, first pass `:251 checkStrategyGate`, TOCTOU re-check `:364 isDailyReturnsSourced`. `long_fetch.py:63 _LEDGER_BACKED_SOURCES` **correctly left alone** — last touched `d80a1bae` (pre-phase); it is adapter-capability routing, not gate routing. |
| 4 | ⭐ All **three** `csv_daily_returns` producers stamp a verdict, each with a verdict story | ✓ VERIFIED | See § Three Producers below. Seam **precedes** the destructive delete; stitch stamp is a **sibling** of `merged_flags`; runner stamps `user_supplied` for keyless CSV **only**. |
| 5 | Composites stay approvable **through the admin approve path** (the one that publishes) | ✓ VERIFIED | `job_worker.py:6845` stamps `composite_stitched` in `headline_payload`; `strategyGate.ts` allow-lists it; `strategy-review/route.test.ts:744` SC-4(a) 200 end-to-end. **Verifier re-ran SC-4**: replacing the re-check's `isDailyReturnsSourced(...)` call with the old hand-copy reddened **2** tests — SC-4(b) composite-NULL (`expected 200 to be 409`) **and** a D-15-through-the-publishing-path case. Reverted. |
| 6 | MT5 is **reachable**: broker-server field is legible plain text while OKX's real passphrase stays masked | ✓ VERIFIED | `ConnectKeyStep.tsx:151-152` `passphraseLabel: "Broker server"`, `passphraseSecret: false`; consumed at `:428` `?? true` (OKX/every other venue defaults to masked) and rendered at `:722` `type={passphraseSecret && !showSecret ? "password" : "text"}`. MT5 card gated behind `MT5_UI_ENABLED`. `MultiKeyConnectStep` offers no MT5 card (`EXCHANGES` at `:87` = binance/okx/bybit/deribit + flagged sfox), so the OKX flip provably cannot reach it. 344 tests green across the four wizard/route suites. |
| 7 | Rejections are **honest**: 24 sites name their true cause; the format copy no longer claims a browser check | ✓ VERIFIED | `grep -oE 'code: "KEY_INVALID_FORMAT"'` at HEAD = **1** per route (was 12+12). Replacements measured: 3 `KEY_INPUT_TOO_LONG` + 5 `KEY_MISSING_REQUIRED_FIELD` + 1 `KEY_UNSUPPORTED_VENUE` + 2 `KEY_VENUE_NOT_ENABLED` + 1 retained `KEY_INVALID_FORMAT` = **12 per route, identical in both**. Four new codes present in the `WizardErrorCode` union (`wizardErrors.ts:116-119`). `grep "Client-side format check"` → **0 hits**; `wizardErrors.ts:586` now reads *"A format check on our side rejected the API secret…"*. |
| 8 | The falsifiability ledger is **honest** — every SC row is Observed-with-evidence or skipped-with-reason | ✓ VERIFIED | 10/10 rows carry pasted assertions. The three flagged provenance caveats are all stated in the file rather than smoothed: SC-1's TS half is a **named substitution**, SC-5 is labelled **observed-by-a-prior-run / taken-on-trust**, SC-6's oracle is correctly attributed to the **every-payload scans** not the stamp test. **This verifier re-ran SC-1(TS), SC-2, SC-3, SC-4, SC-5 and SC-6 first-hand — all six reproduce.** SC-5 is hereby **upgraded from taken-on-trust to independently observed**. |
| 9 | ⭐ MT5-05 is **PENDING**, not passed — no artifact claims it satisfied | ✓ VERIFIED | `142.2-HUMAN-UAT.md` frontmatter `status: pending`, `gate: blocking-human`, all three tests `result: [pending]`. Only `142.2-08-SUMMARY.md` mentions it, as `requirements-pending: [MT5-05]`. **No SUMMARY lists MT5-05 under `requirements-completed`.** ROADMAP.md:495 and REQUIREMENTS.md:270 both carry it as open. |
| 10 | ⭐ **No artifact claims MT5's NUMBERS are verified** | ✓ VERIFIED | Every hit for a number-correctness phrase across all 22 phase artifacts is a **disclaimer or a mitigation**, never a claim: `VALIDATION.md:524` "⛔ 142.2 delivered MT5 REACHABLE and HONEST. It did NOT verify that MT5's NUMBERS are correct."; `142.2-08-PLAN.md:225` books T-142.2-24 as a repudiation risk; `CONTEXT.md:245` records the prohibition. ROADMAP.md:499-501 and :537 mirror it. |

**Score: 10/10 automated truths verified.** The eleventh — MT5-05 — is a human gate and is correctly open.

---

## Three `csv_daily_returns` Producers — Per-Producer Verdict Story

| # | Producer | Verdict | Placement | Status |
|---|----------|---------|-----------|--------|
| 1 | `run_derive_broker_dailies_job` (keyed venues) | Stamped by the combiners: `ledger_complete` ×3 (`broker_dailies.py:339,347,701`), `fill_derived_unproven` (`:264`), `sampled_gapped`/`ledger_complete` by `nav_gap_days` (`:539`). 3 empty-series early returns exempt **in writing** (`:402,:427,:672`, each citing the `job_worker.py:4340` <2-row short-circuit). | Fail-loud assert `job_worker.py:4434`, refusing anything outside `SERIES_COMPLETENESS_VALUES`. Persisted as `_prestamp_payload["series_completeness"]` (`:4840`), a **sibling** of `_prestamp_flags`. | ✓ VERIFIED |
| 2 | `run_stitch_composite_job` (composites) | `composite_stitched` | `job_worker.py:6845`, inside `headline_payload` — **NOT** in `merged_flags`. Test `test_stitch_composite_job.py:4008` asserts the value, `:4020` asserts `"series_completeness" not in headline["data_quality_flags"]`, `:4029` scans every later payload for clobbering. Failure arm deliberately **omits** the column (A1). | ✓ VERIFIED |
| 3 | `analytics_runner` (keyless CSV) | `user_supplied`, **conditionally**: `_stamp_user_supplied = not _is_broker_sourced and not _was_composite` (`analytics_runner.py:~1553`), written at `:1593` as a sibling of `data_quality_flags`. | Column **ABSENT** from every other payload — pinned by 6 `*_omits_series_completeness` scan tests (`test_analytics_runner.py:3270,3304,3340,3366,3390,3418`). | ✓ VERIFIED |

### ⭐ The seam precedes the destructive delete

`job_worker.py`: verdict assert at **:4434** · `_reconcile_span_delete` constructed at **:4563**, executed at **:4580**. The refusal therefore costs nothing — a rejected series cannot destroy the prior one. The code carries an explicit `⛔ PLACEMENT IS LOAD-BEARING` comment at `:4417-4423` naming this exact hazard.

### ⭐ The fail-open the sibling rule prevents

Both non-combiner producers document the same two-part reason for the sibling placement, and both are enforced by tests: `data_quality_flags` is **rebuilt wholesale** on every write (so a verdict inside it would be erased next run), and membership of the guard-key sets **auto-promotes** `computation_status` to `complete_with_warnings` — a status the publish gate **PASSES**. Routing a gating signal through the promotion channel would be a fail-open (D-16). Verified present at all three producers.

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/strategyGate.ts` | Positive allow-list, one exported predicate, zero venue literals | ✓ VERIFIED | 318 lines. `SERIES_TRUSTED_FOR_DAILY_BRANCH` `:88`; `isDailyReturnsSourced` exported `:107`; `seriesCompleteness` is **required and nullable** on the input type `:69` (a caller that forgets it is a compile error, not a silent fail-open). |
| `src/app/api/admin/strategy-review/route.ts` | Calls the shared predicate at both passes; no hand copy; no `api_keys.exchange` lookup | ✓ VERIFIED | Import `:11`; first pass `:251`; re-check `:364`. The venue lookup is deleted with a comment at `:224-237` explaining the WR-01 fail-loud property moved to the `analyticsError` 503 guard. Both selects carry `series_completeness` (`:157`, `:321`). |
| `SyncPreviewStep.tsx` | Single-key arm reads and passes the verdict | ✓ VERIFIED | Select widened `:1179`; passed `:1335` with a comment distinguishing this **safe** `?? null` from the counts above, which **throw** rather than coerce. Composite arm `:920-927` documents its by-design bypass. |
| `supabase/migrations/20260803150000_…series_completeness.sql` | Additive, nullable, no default, no CHECK, self-verifying | ✓ VERIFIED | `ADD COLUMN IF NOT EXISTS … text`; a `DO $$` block raises on absent / non-text / not-nullable / defaulted. Applied to **TEST** only (`qmnijlgmdhviwzwfyzlc`); PROD receives it on merge to `main`. |
| `supabase/tests/test_strategy_analytics_series_completeness.sql` | The one CI-counting DB gate, **no green-skip** | ✓ VERIFIED | 4 assertions incl. a **negative** probe that no CHECK constraint exists. Absence of the column is an `EXCEPTION` (`:73`), not a skip — the green-skip arm was removed in `b10f7849` with the reasoning recorded in an `ANTI-GREEN-SKIP CONTRACT` header. |
| `src/lib/strategyGate.invariant.test.ts` | Pins zero venue literals | ✓ VERIFIED | 15 tests, anti-vacuity floor + 3 scanner SELF-TESTs. Verifier-reproduced (SC-3). |
| `src/lib/wizardErrors.invariant.test.ts` | Per-route roster pinning, literal count 12 | ✓ VERIFIED | Comment-stripped source scan; per-consumer, not a merged set. |
| `analytics-service/tests/test_broker_dailies.py` | The D-15 **economic** oracle | ✓ VERIFIED | `test_funding_only_series_is_understated_and_never_certified` — two combiner calls with different inputs, `cumulative(gapped) < cumulative(truth) − 0.05` with the bound hand-typed, compounding recomputed in the test from first principles, plus an anti-vacuity floor. The verdict assertion is a separate second half, never the whole test. |
| `142.2-HUMAN-UAT.md` | MT5-05 persisted as OPEN | ✓ VERIFIED | `status: pending` / `gate: blocking-human`; three tests all `result: [pending]`; explains why it cannot run locally or in CI. |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `strategyGate.ts` | admin approve (publish path) | `checkStrategyGate` + `isDailyReturnsSourced` import | ✓ WIRED | Both passes; mutation-proven load-bearing (SC-4). |
| `strategyGate.ts` | wizard preview | `checkStrategyGate` at `SyncPreviewStep.tsx:1312` | ✓ WIRED | Verdict threaded from the widened select. |
| `strategy_analytics.series_completeness` | TS gate input | `select(… , series_completeness)` at 3 sites | ✓ WIRED | `route.ts:157`, `route.ts:321`, `SyncPreviewStep.tsx:1179`. |
| `broker_dailies` combiners | derive persist | fail-loud assert `job_worker.py:4434` | ✓ WIRED | Mutation-proven: deleting the MT5 stamp turns the real producer DONE → FAILED. |
| `run_stitch_composite_job` | `strategy_analytics` | `headline_payload["series_completeness"]` | ✓ WIRED | Sibling of `data_quality_flags`, test-pinned in both polarities. |
| `analytics_runner` | `strategy_analytics` | conditional `payload["series_completeness"]` | ✓ WIRED | Keyless-only; absence pinned by 6 every-payload scans. |
| migration | `database.types.ts` | hand-patched column | ✓ WIRED | `database.types.ts:2498/2529/2560`. (Regeneration-script gap booked `DEF-142.2-06`.) |

---

## Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| `strategyGate.ts` | `input.seriesCompleteness` | `strategy_analytics.series_completeness`, written by 3 live producers | Yes — 3 Python write paths verified, each test-pinned | ✓ FLOWING |
| `strategy-review/route.ts` | `analytics?.series_completeness` | real PostgREST select `:157` / `:321` | Yes | ✓ FLOWING |
| `SyncPreviewStep.tsx` | `analytics?.series_completeness` | real select `:1179` inside the poll | Yes | ✓ FLOWING |
| `ConnectKeyStep.tsx` | `passphraseSecret` | per-venue config literal `:152`, consumed `:428` → `:722` | Yes (config, not fetched by design) | ✓ FLOWING |

⚠️ **PROD NULL window, named and self-healing.** Every existing PROD row reads NULL and the gate is
fail-closed on NULL, so the column does not "flow" for pre-existing strategies until a re-derive
stamps it. Plan 04's PROD census sized the affected population at **6 unpublished strategies
(1 keyed, 4 keyless CSV, 1 composite) and 0 published composites**; the three per-producer remedies
and the **prohibition on a backfill `UPDATE`** are booked as `DEF-142.2-09`. Nothing already
published changes, because the gate runs at exactly two moments. This is correct fail-closed
behaviour, not a gap.

---

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Gate + invariant suites | `npx vitest run strategyGate.test.ts strategyGate.invariant.test.ts wizardErrors.invariant.test.ts` | 3 files / **69 passed** | ✓ PASS |
| Admin approve gate | `npx vitest run admin/strategy-review/route.test.ts` | **47 passed** | ✓ PASS |
| Wizard + both routes + copy | `npx vitest run ConnectKeyStep.test.tsx create-with-key/route.test.ts composite/add-key/route.test.ts wizardErrors.test.ts` | 4 files / **344 passed** | ✓ PASS |
| Python producers | `pytest tests/test_broker_dailies.py tests/test_derive_broker_dailies_dualmode.py` (from `analytics-service/`) | **71 passed** | ✓ PASS |
| Stitch + runner producers | `pytest tests/test_stitch_composite_job.py tests/test_analytics_runner.py` | **195 passed** | ✓ PASS |
| Full TS suite | `npm run test` | **737 files / 10,580 passed**, 19 + 287 skipped | ✓ PASS — matches VALIDATION.md **exactly** |
| Full PY suite | `pytest` (from `analytics-service/`) | **4875 passed, 96 skipped** (372s) | ✓ PASS — matches VALIDATION.md **exactly** |
| Real type gate | `mypy --strict --follow-imports=silent services/ routers/ models/` | **Success: no issues found in 89 source files** | ✓ PASS — matches exactly |
| Typecheck | `npm run typecheck` | clean | ✓ PASS |
| Lint | `npm run lint` | **0 errors, 1 warning** (`EquityChart.tsx:1119`, `DEF-142.2-11`) | ✓ PASS — matches exactly |
| Debt-marker gate | `TBD\|FIXME\|XXX` across all 37 phase-modified files | **0 hits** | ✓ PASS |
| E2E | `npm run test:e2e` | not run | ? SKIP — **skipped-with-reason** (see below) |

**Every full-suite number VALIDATION.md claims reproduced exactly under independent execution.**

### E2E skip — verified as honest, not as a dodge

The VALIDATION.md audit finding was re-checked: `checkStrategyGate` is called at exactly two sites
(`SyncPreviewStep.tsx:1312`, `admin/strategy-review/route.ts:251`) and the composite wizard arm at
`SyncPreviewStep.tsx:920-927` provably never routes through either. The decision **not** to widen
`e2e/helpers/seed-test-project.ts` — because blanket-stamping `ledger_complete` on four fixtures that
are not ledger-complete would write a dishonest verdict to satisfy a checkbox — is the correct call
and is recorded as such rather than reported as green.

---

## Probe Execution

| Probe | Command | Result | Status |
|-------|---------|--------|--------|
| — | — | — | N/A — this repo has no `scripts/*/tests/probe-*.sh` convention, and no PLAN or SUMMARY in this phase declares a probe path. Falsifiability mutations (below) are this phase's equivalent and were executed. |

### ⭐ Falsifiability mutations re-run by this verifier

| SC | Mutation | Expected red | Observed | Verdict |
|----|----------|--------------|----------|---------|
| SC-1 (TS) | Add `"fill_derived_unproven"` to the allow-list | D-15 acceptance test | **1 failed / 37 passed** — `strategyGate.test.ts:215:27` | ✓ REPRODUCED |
| SC-2 | Allow-list → deny-list (`!== "sampled_gapped"`) | null-verdict case | **4 failed / 34 passed** — null, unrecognised, composite-NULL, D-15 | ✓ REPRODUCED |
| SC-3 | Re-add `input.seriesCompleteness === "deribit"` | invariant test | **1 failed / 14 passed** — "deribit appears 1 time(s) in the comment-stripped source" | ✓ REPRODUCED |
| SC-4 | Restore the hand-copied inline predicate in the TOCTOU re-check | SC-4(b) composite-NULL | **2 failed / 45 passed** — SC-4(b) `expected 200 to be 409` **and** the D-15-through-publishing case | ✓ REPRODUCED (stronger than recorded) |
| **SC-5** | Delete `combine_mt5_deal_ledger`'s stamp | fail-loud seam through the **real** producer | `test_mt5_derive_branch.py` DONE → FAILED, seam log at `job_worker.py:4452`: *"Series completeness verdict missing or unrecognised (series_completeness=None)"* | ✓ **REPRODUCED — upgrades the ledger's one taken-on-trust row to first-hand** |
| SC-6 | `"series_completeness": None` unconditionally in `_mark_complete` | the every-payload **scans** | **2 failed / 5 passed** — exactly the two scan tests; the stamp test stayed green, confirming the ledger's own note that the stamp test structurally cannot catch it | ✓ REPRODUCED |

All six reverted; `git status --porcelain` empty after each. SC-7..SC-10 were not re-run — their pasted
evidence is internally consistent and their subject matter (masking, error codes) is covered by the
344 green wizard/route tests above.

---

## Requirements Coverage

| Requirement | Source plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MT5-01 | none (pre-delivered) | Server-side MT5 flag on in Vercel prod | ✓ SATISFIED | Founder env flip + redeploy 2026-08-03, verified in production by `curl /security` returning the `mt5-readonly` anchor. Recorded in `VALIDATION.md:545-551` with no implementing task — correct, not a gap. |
| MT5-02 | none (pre-delivered) | Client-side MT5 wizard offer | ✓ SATISFIED | Same flip (`NEXT_PUBLIC_*` is build-time inlined). `MT5_UI_ENABLED` gate at `ConnectKeyStep.tsx:139`. |
| MT5-03 | 02 | Broker-server plain text; OKX byte-identical | ✓ SATISFIED | Truth 6. Per-venue `passphraseSecret` flag; OKX defaults `?? true`. SC-7 ran as **three** mutations incl. the positive half. |
| MT5-04 | 07 | `KEY_INVALID_FORMAT` split across 24 sites; copy honesty | ✓ SATISFIED | Truth 7. 12+12 measured at HEAD by this verifier. 9 deferred sites (4+5) booked `DEF-142.2-02`. |
| MT5-05 | 08 | Founder completes the connect flow needing no internal knowledge | ⏳ **PENDING — blocking human gate** | `142.2-HUMAN-UAT.md` `status: pending`. Correctly **not** approved: nothing is deployed. |
| MT5-11 | 03, 06 | MT5 evaluated on the daily-returns branch | ✓ SATISFIED | `strategyGate.test.ts:218-234` — MT5 `ledger_complete` + 135 rows + 0 trades **PASSES**. Delivered **through** MT5-12 per D-13, as the requirement demands. |
| MT5-12 | 01, 03, 05, 06 | Admissibility from the canonical daily series, not a venue list | ✓ SATISFIED | Truths 2, 3, 4. Column + 3 producers + fail-loud seam + positive allow-list + one predicate. |

**MT5-06..10 are Phase 142.3 (D-17) and are correctly out of scope — not reported as gaps.**

No orphaned requirements: REQUIREMENTS.md maps exactly `MT5-01..05, MT5-11, MT5-12` to Phase 142.2,
and every one appears in a plan's `requirements` field or is recorded as pre-delivered.

---

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` across all 37 phase-modified files | — | **None found.** Debt-marker gate passes. |
| `.planning/REQUIREMENTS.md` | 251, 257, 273, 305 | Delivered requirements still `- [ ]` while MT5-01/02 are `- [x]` | ⚠️ WARNING | Bookkeeping drift only. No code behaviour depends on it. See W-1. |
| `.planning/REQUIREMENTS.md` | 257-259, 268-269 | MT5-04 text cites "28 sites (14)+(14)" and "11 remaining (5)+(6)" — numbers this phase disproved (24 = 12+12; 9 = 4+5) | ⚠️ WARNING | The correction landed in VALIDATION.md and TODOS.md but not here, so the requirements doc carries a number its own phase measured wrong. See W-2. |

Neither warning meets the project's stopping-rule bar (user-facing or data-integrity), so neither
blocks. Both are recorded so the next reader of REQUIREMENTS.md is not misled.

**Scanned and cleared as non-stubs:** the `?? null` / `?? 0` coercions flagged by the empty-value
heuristic are all deliberate and documented, and their direction was checked individually. The gate's
`?? ""` (`strategyGate.ts:115`) and the two `?? null` verdict reads can **only** refuse; the counts
that would fabricate a measurement **throw** instead (`SyncPreviewStep.tsx:1293-1303`,
`route.ts:190-196`). That asymmetry is the correct one and is explicitly reasoned about in-source.

---

## Human Verification Required

### 1. MT5-05 — the founder completes the MT5 connect flow on production

**Test:** After `/ship` + `/land-and-deploy` (deploy order: **migration → Railway worker → Vercel**),
on **quantalyze.xyz**: open the strategy wizard → pick **MetaTrader 5** → confirm the **Broker server**
field renders as plain readable text as you type (not dots) → submit the **live funded** account
(login / investor read-only password / broker server) → wait for sync (~10-15 min for a fresh key) →
reach the preview → pass the gate → see a **rendered strategy**.

**Expected:** A rendered strategy, reached without the founder needing to know an internal error code,
a server name, or a flag. **Any point where the founder must ask what a code means is a failure**,
logged and fixed in-phase.

**Why human:** It is an **outcome** requirement, not the sum of MT5-01..04. The MT5 gateway is
Railway-internal (`mt5-gateway.railway.internal:8001`) and unreachable from a laptop or a CI runner;
the shared TEST database has no worker, so a connect would never sync. No automated assertion can
stand in for a human not needing to ask a question. ⚠️ **A green suite is explicitly not evidence** —
v1.15 shipped 6/6 phases green with both open items intact, which is the precise failure mode MT5-05
exists to catch.

**Tracked in:** `142.2-HUMAN-UAT.md` (`status: pending`, `gate: blocking-human`).

---

## Gaps Summary

**No gaps.** Every automated must-have is verified against source, and the six highest-value
falsifiability mutations were re-run by this verifier rather than accepted from the SUMMARYs. The
phase's own artifacts survived adversarial reading: the ledger states its two provenance caveats
rather than smoothing them, the e2e line is recorded skipped-with-reason rather than green, the
`mypy --strict` over-claim was struck by the phase itself, and MT5-05 is persisted as open rather
than approved untested.

**The honest overall status is "executed and code-verified, pending human UAT" — not "passed."**
The phase goal has two halves. The **reachable-and-honest machinery** is delivered and independently
verified. The **outcome** half (MT5-05: a founder actually completing the flow on production without
internal knowledge) cannot be verified from the repository and has not been attempted, because
nothing from this phase is deployed.

### What this verification does NOT establish

- **That MT5's numbers are correct.** Equity curve, period return, Sharpe, drawdown and daily
  bucketing agreement with the MT5 terminal is **Phase 142.3's** gate. A wrong server-UTC offset
  misassigns near-midnight deals to the adjacent day — **period totals still reconcile while the
  daily series is wrong**, which corrupts every risk metric and is invisible to everything 142.2
  built. Do not archive the milestone or advertise MT5 on the strength of this report.
- **That the migration is on PRODUCTION.** It is applied to **TEST** (`qmnijlgmdhviwzwfyzlc`) only.
  Merging `supabase/migrations/**` to `main` is what applies it to PROD — and the deploy order
  (migration → worker → frontend) is load-bearing.
- **That CI stopped anything.** `main` carries no branch protection (founder decision, deferred until
  paying clients). Every gate above is **advisory at merge**: read them as *"the suite would have
  caught it"*, never *"CI stopped it"*.

### Two non-blocking follow-ups

- **W-1** — tick MT5-03, MT5-04, MT5-11, MT5-12 in `REQUIREMENTS.md` (MT5-01/02 already are), and
  update the coverage table row from "Pending (MT5-01, MT5-02 complete)".
- **W-2** — correct MT5-04's requirement text from "28 sites (14)+(14)" / "11 remaining (5)+(6)" to
  the measured **24 (12+12)** / **9 (4+5)**.

---

_Verified: 2026-08-04T09:35:00Z_
_Verifier: Claude (gsd-verifier) — adversarial goal-backward, six ledger mutations independently re-run_
