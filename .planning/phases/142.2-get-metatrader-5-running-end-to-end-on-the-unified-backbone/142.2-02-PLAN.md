---
phase: 142.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx
autonomous: true
requirements: [MT5-03]

must_haves:
  truths:
    - "The MT5 Broker-server input renders type=\"text\" — a founder can read the server name they typed"
    - "The OKX passphrase input still renders type=\"password\" in BOTH ConnectKeyStep and MultiKeyConnectStep — byte-identical render, because OKX's passphrase is a genuine API credential (D-03)"
    - "SC-7 mutation observed: setting passphraseSecret: false for OKX turns the OKX regression assert RED"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      provides: "passphraseSecret per-venue config key, defaulted true; MT5 entry sets false"
      contains: "passphraseSecret"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx"
      provides: "MT5 type=text assert + OKX type=password contrast assert"
      contains: "Broker server"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx"
      provides: "New OKX passphrase render regression (VALIDATION row 2 — written from scratch, not marked satisfied by absence)"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      to: "the single passphrase <Input> render"
      via: "defaulted read passphraseSecret ?? true feeding the type= ternary"
      pattern: "passphraseSecret"
---

<objective>
MT5-03 (decision D-03, LOCKED): unmask the MT5 broker-server field via a per-venue
`passphraseSecret: boolean` config flag — MT5 `false`, OKX `true` (satisfied by the default, not by
an edit to the OKX entry). The OKX render must stay byte-identical; that is an acceptance criterion
(SC-7), not a hope. A global unmask was REJECTED by the founder.

Purpose: the founder was typing a server name into a dot-masked field and could not verify it —
against helper copy that says "copy the server name exactly as it appears in your MT5 terminal".
Output: config key + one ternary change + three test assertions.

⚠️ The broker-server typeahead is DEFERRED (D-04 — no data source exists anywhere; a partial list is
worse than none). Do NOT build one. Plan 142.2-08 logs it to TODOS.md with the data-source problem named.
⚠️ MT5-03 must NOT touch `MultiKeyConnectStep.tsx` — it carries no MT5 card (grep-verified: only two
error-code strings at :228-229); its passphrase input is hardcoded OKX at :1490-1505. Only its TEST
file gains a regression assert.
⚠️ Storage is unchanged: the broker server still rides the passphrase slot into `encrypt_credentials`
and stays encrypted at rest. This plan changes display only — do not claim otherwise, and do not
"fix" the storage path (out of scope).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add passphraseSecret per-venue config and rewire the single render site</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx</files>
  <read_first>
    - DESIGN.md — MANDATORY before any visual/UI decision (CLAUDE.md rule); dots→legible-text is one.
    - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx — the three-part per-venue
      pattern to extend: (a) optional interface keys at :43,64-70 (`passphraseLabel?`,
      `passphrasePlaceholder?`, `passphraseHelper?`, `secretHelper?`); (b) the defaulted reads at
      :390-402 (every default is the OKX value — THIS is the byte-identity mechanism); (c) the
      single consuming render at :693-706 (`<Input ... type={showSecret ? "text" : "password"}` at
      :697); (d) the MT5 config entry at :129-149 (flag-gated spread on MT5_UI_ENABLED).
    - src/components/ui/Input.tsx — confirms `type` forwards via InputHTMLAttributes; no component change.
  </read_first>
  <action>
    Three edits, following the file's own idiom exactly (PATTERNS § ConnectKeyStep):
    1. Interface: add `passphraseSecret?: boolean;` beside `passphraseLabel?` /
       `passphrasePlaceholder?` / `passphraseHelper?` in the venue-config type (~:64-70).
    2. Defaulted read (~:390-402): `const passphraseSecret = activeExchange?.passphraseSecret ?? true;`
       — default TRUE so OKX and every venue that omits the key render exactly as today. D-03's
       "OKX true" is satisfied by the default; do NOT edit the OKX config entry.
    3. Render (:697 only): change `type={showSecret ? "text" : "password"}` to
       `type={passphraseSecret && !showSecret ? "password" : "text"}` — when the venue declares the
       slot non-secret (MT5), the input is always plain text; when secret (default), the existing
       showSecret toggle behaviour is preserved bit-for-bit.
    4. MT5 config entry (:129-149, inside the MT5_UI_ENABLED spread): add `passphraseSecret: false,`
       beside `passphraseLabel: "Broker server"`.
    Nothing else changes: not the label, not the placeholder, not the helper copy (D-04 keeps the
    existing "copy the server name exactly as it appears in your MT5 terminal" helper), not
    MultiKeyConnectStep.tsx, not autoComplete, not the required attribute.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "passphraseSecret" 'src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx'` ≥ 3 (interface key, defaulted read, MT5 entry).
    - `grep -c "passphraseSecret" 'src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx'` = 0 (file untouched; `git diff --stat` shows zero changes to it).
    - The OKX config entry is byte-identical to HEAD (`git diff` shows no hunk touching the okx object).
    - Existing ConnectKeyStep.test.tsx suite passes unmodified before Task 2 adds cases (`npx vitest run` on the file).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>MT5 broker-server renders unmasked via a defaulted per-venue flag; OKX path bit-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the render contract in both test files, then run the SC-7 falsification</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx — the existing
      contrast pair: :181 "shows NO passphrase field for Deribit but DOES for OKX", :185/:188
      queryByLabelText(/passphrase/i) asserts, and the MT5 block at :504-506
      (`getByLabelText("Broker server")`). New cases sit beside these.
    - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx — has NO OKX
      passphrase render assertion today (grep-verified). The passphrase input under test carries
      `data-testid={`key-${index}-passphrase`}` (component :1490-1505).
  </read_first>
  <action>
    1. ConnectKeyStep.test.tsx — two new cases in the existing describe blocks:
       (a) MT5 block: `expect(screen.getByLabelText("Broker server")).toHaveAttribute("type", "text")`.
       (b) OKX contrast beside :188: the OKX passphrase input has attribute `type="password"` (and
       stays password when showSecret is untoggled — mirror however the file drives showSecret).
    2. MultiKeyConnectStep.test.tsx — one new regression (~5 lines, VALIDATION row 2, decided at
       plan time to WRITE it rather than move the row): render with an OKX key row and assert
       `getByTestId("key-0-passphrase")` has `type="password"` and label text "OKX Passphrase".
       This is the cross-file half of SC-7: MultiKeyConnectStep hardcodes OKX, so this pins that a
       future "unify the venue arrays" refactor cannot silently unmask it.
    3. **SC-7 falsification run (record in SUMMARY):** temporarily set `passphraseSecret: false` on
       the OKX entry in ConnectKeyStep.tsx → run both specs → the OKX contrast assert MUST go RED →
       revert (git checkout of the hunk) → both specs green. Paste the failing assertion text into
       the SUMMARY under "Falsifiability evidence: SC-7". A skipped mutation is recorded as skipped,
       never as caught.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx' 'src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx'</automated>
  </verify>
  <acceptance_criteria>
    - ConnectKeyStep.test.tsx asserts `toHaveAttribute("type", "text")` for "Broker server" AND `toHaveAttribute("type", "password")` for the OKX passphrase — exact attribute assertions, not snapshot diffs.
    - MultiKeyConnectStep.test.tsx contains a new OKX passphrase assert using `key-0-passphrase` testid with type="password".
    - SC-7 mutation was RUN: SUMMARY carries the pasted failing assertion from the OKX case under the mutated config, and the working tree is restored (git diff shows no leftover mutation).
    - Both spec files green; `npm run typecheck && npm run lint` clean on touched files.
  </acceptance_criteria>
  <done>Both halves of the D-03 contract are pinned by attribute assertions, and SC-7 is Observed with pasted evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form → credential display | a masking decision on inputs that share one render slot |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.2-04 | Information disclosure | OKX passphrase unmasked by a global change | mitigate | per-venue flag with default true (byte-identity by defaulting, not by editing OKX); SC-7 mutation run as acceptance |
| T-142.2-05 | Information disclosure | broker server treated as un-encrypted because it displays as text | accept | storage path unchanged — still routed through encrypt_credentials; plan states display-only explicitly |
| T-142.2-SC | Tampering | package installs | accept | no installs in this plan |
</threat_model>

<verification>
- `npx vitest run` on both spec files green; typecheck + lint clean.
- `git diff --stat` shows exactly 3 files changed, none of them MultiKeyConnectStep.tsx.
</verification>

<success_criteria>
- MT5 broker-server input is legible text; OKX masked in both wizard steps; SC-7 Observed.
</success_criteria>

<output>
Create `.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-02-SUMMARY.md` when done.
</output>
