---
phase: 142.2
plan: 06
type: execute
wave: 2
depends_on: ["142.2-01"]
files_modified:
  - src/lib/strategyGate.ts
  - src/lib/strategyGate.test.ts
  - src/lib/strategyGate.invariant.test.ts
  - src/lib/database.types.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/api/admin/strategy-review/route.ts
  - src/app/api/admin/strategy-review/route.test.ts
  - src/__tests__/audit-fanout-integration.test.ts
autonomous: true
requirements: [MT5-11, MT5-12]

must_haves:
  truths:
    - "THE ACCEPTANCE TEST: a keyed perp with seriesCompleteness='fill_derived_unproven', 135 csv rows, 0 trades is REFUSED (INSUFFICIENT_TRADES) — MT5 passing is not the test; that perp still failing is (D-15, SC-1)"
    - "seriesCompleteness null/absent → trade branch (fail CLOSED); the branch is a positive ALLOW-LIST — a deny-list admits every value nobody thought of, including NULL (SC-2)"
    - "isLedgerBackedExchange is DELETED, not widened; zero venue literals remain in strategyGate.ts; the stale keep-in-lockstep docblock is deleted, not amended (SC-3)"
    - "The admin TOCTOU re-check calls the SAME exported predicate as the first pass — the hand-duplicated copy at strategy-review/route.ts:377-380 is deleted (the third site; two-of-three fixes re-arm the drift, SC-4)"
    - "Composites (apiKeyId null) with 'composite_stitched' remain approvable; a keyed MT5 strategy with 'ledger_complete' + 135 rows + 0 trades PASSES; keyless CSV with 'user_supplied' still passes on ≥7 rows"
  artifacts:
    - path: "src/lib/strategyGate.ts"
      provides: "Gate reads a persisted verdict; exported shared predicate; no venue knowledge"
      contains: "seriesCompleteness"
    - path: "src/lib/strategyGate.invariant.test.ts"
      provides: "Source-reading pin: zero venue literals, with anti-vacuity floor and scanner SELF-TESTs"
    - path: "src/lib/database.types.ts"
      provides: "series_completeness hand-patched into strategy_analytics Row/Insert/Update"
      contains: "series_completeness"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      to: "strategy_analytics.series_completeness"
      via: "widened select in the existing Promise.all member (:1174-1180) — zero new queries"
      pattern: "series_completeness"
    - from: "src/app/api/admin/strategy-review/route.ts"
      to: "src/lib/strategyGate.ts"
      via: "both gate sites (first pass :265-284 AND TOCTOU re-check :377-380) invoke the exported predicate"
      pattern: "isDailyReturnsSourced"
---

<objective>
MT5-11 through MT5-12 (D-13, LOCKED): delete TypeScript's opinion about venue trust. The gate reads
`strategy_analytics.series_completeness` — a verdict it cannot compute — through a positive
allow-list. `isLedgerBackedExchange` is DELETED (a standalone widening was explicitly rejected by
the founder: it re-arms the identical drift for the next venue).

Purpose: the venue list was a proxy for "is this series complete?", enforced only by a comment that
already failed once (Python widened to {deribit, sfox, mt5}; TS stayed at "deribit"; a 135-day MT5
account landed on GATE_INSUFFICIENT_TRADES, unwinnable, with a draft-destroying remedy). The stale
comment is part of the defect — divergence must become DETECTABLE (the invariant test + the deleted
second copy), not re-instructed.
Output: gate rewrite, both consumers on one exported predicate, both vi.mock sites fixed, types
patched, invariant test.

**Allow-list contract (LOCKED, hand-typed literals here and in tests — never imported):**
`{"ledger_complete", "user_supplied", "composite_stitched"}`. NULL / "fill_derived_unproven" /
"sampled_gapped" / anything unrecognized → trade branch. `composite_stitched` is in the allow-list
because composites pass today via the `!input.apiKeyId` term this plan deletes; the stitch job
stamps it (plan 142.2-05). A5 is FALSE for the admin approve path — the path that actually
publishes — so composite approvability is a first-class acceptance criterion below.

⚠️ Deletion blast radius (grep-verified): the export is imported/mocked at SyncPreviewStep.tsx:15/1325,
admin/strategy-review/route.ts:11 (5 refs incl. local `let isLedgerBacked`),
strategy-review/route.test.ts:109 (vi.mock), src/__tests__/audit-fanout-integration.test.ts:837
(a SECOND vi.mock nothing in RESEARCH names), strategyGate.test.ts (4 cases). All are updated in
this plan — the repo typechecks clean at plan end, not before.
⚠️ `_LEDGER_BACKED_SOURCES` in long_fetch.py:63 answers a DIFFERENT question (does this adapter
fetch fills — an adapter capability). Deleting it breaks ingestion. Leave it (Pitfall 2); MT5-12 is
satisfied by deleting the TypeScript trust mirror.
⚠️ Coverage ratchet: deleting isLedgerBackedExchange and its tests removes covered lines — the
replacement tests must more than cover them (lines 82 / statements 80 / functions 74 / branches 72).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-PATTERNS.md
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite strategyGate.ts on the verdict, patch database.types.ts, rewrite the gate unit suite</name>
  <files>src/lib/strategyGate.ts, src/lib/strategyGate.test.ts, src/lib/database.types.ts</files>
  <read_first>
    - src/lib/strategyGate.ts — full file. What changes: isLedgerBackedExchange (:73-77) + its
      docblock DELETED; the input field `isLedgerBacked?: boolean` and its 12-line docblock (:52-63)
      DELETED; the branch at :181-184 and its stale 17-line docblock (:164-180) REPLACED. What stays:
      the StrategyGateUnevaluableError refusal idiom (:108-117) and its message discipline (message
      carries only integers the caller counted); ⚠️ the NO_DATA_SOURCE guard at :154 ALSO keys off
      `!input.apiKeyId` and is UNTOUCHED — apiKeyId leaves the daily-branch predicate, not the input type.
    - src/lib/strategyGate.test.ts — :174, :188, :206/:210, :430 pass isLedgerBacked today; all rewritten.
    - src/lib/database.types.ts:2477-2570 — strategy_analytics Row/Insert/Update; the analog is the
      three prior hand-patch commits (48b12210, 84d65d93, 3d40d925). There is NO regeneration script.
  </read_first>
  <action>
    strategyGate.ts:
    1. New input field: `seriesCompleteness: string | null` (required, nullable — forcing every
       caller to state what it read; a caller that "forgot" is a compile error, not a silent
       fail-open). Docblock: the producer decides (broker_dailies combiners / analytics_runner /
       stitch job); TS only reads; NULL means never-examined and MUST refuse (fail closed).
    2. Export the shared predicate both callers will use, e.g.
       `export function isDailyReturnsSourced(input: { tradeCount: number; csvRowCount: number; seriesCompleteness: string | null }): boolean`
       implemented as: tradeCount === 0 && csvRowCount > 0 &&
       SERIES_TRUSTED_FOR_DAILY_BRANCH.has(input.seriesCompleteness ?? "") — where
       SERIES_TRUSTED_FOR_DAILY_BRANCH is a module-level ReadonlySet with EXACTLY the three
       allow-list literals. Positive allow-list, never a deny-list. checkStrategyGate's internal
       branch calls this same function (one implementation, three call sites total).
    3. Delete isLedgerBackedExchange and BOTH stale docblocks. Add a comment where the branch lives:
       why NULL refuses (a NULL/absent verdict coerced by `?? ""` lands OUTSIDE the allow-list —
       here the coercion's safe direction is refusal; say so, per the PATTERNS shared-pattern note).
    database.types.ts:
    4. Hand-patch `series_completeness: string | null` into strategy_analytics Row, and
       `series_completeness?: string | null` into Insert and Update (three sites — the file's every
       column appears three times with different optionality). ⚠️ PLANNER DECISION, stated for
       review: patch ONLY this column. The three pre-existing omissions (computing_started_at,
       computation_warned, metrics_json_by_basis) are REAL but out of scope (Rule 3 surgical);
       plan 142.2-08 logs them to TODOS.md with the no-regen-script problem named. Never `as any` /
       `@ts-expect-error` near a strategy_analytics select (Pitfall 5).
    strategyGate.test.ts:
    5. Rewrite the ~4 isLedgerBacked cases and add, expected values/allow-list as hand-typed
       literals (Oracle Independence — never import the Set):
       (a) **THE ACCEPTANCE TEST (SC-1, TS half):** keyed perp, apiKeyId "key-1", tradeCount 0,
       computationStatus "complete" (the pipeline SUCCEEDED — that is the trap), csvRowCount 135,
       seriesCompleteness "fill_derived_unproven" → passed false, code INSUFFICIENT_TRADES. Comment
       the fixture as the D-15 economic situation: a funding-only series that would publish an
       understated record as verified.
       (b) keyed MT5, "ledger_complete", 135 rows, 0 trades → PASSES.
       (c) seriesCompleteness null, keyed, 135 rows → trade branch → refused (SC-2 fail-closed).
       (d) "sampled_gapped" → refused.
       (e) keyless CSV: apiKeyId null, "user_supplied", ≥7 rows → passes.
       (f) composite: apiKeyId null, "composite_stitched", rows > 0 → passes; composite with null →
       refused (documents the new admin-approve dependency on the stitch stamp).
       (g) NO_DATA_SOURCE guard unchanged: apiKeyId null, 0 trades, 0 rows → same refusal as HEAD.
    6. **SC-1 (TS) + SC-2 mutation runs (record in SUMMARY):** SC-1 — flip case (a)'s fixture
       verdict handling by temporarily adding "fill_derived_unproven" to the allow-list → (a) goes
       RED → revert. SC-2 — temporarily replace the allow-list membership with a deny-list
       (`!== "sampled_gapped"`) → case (c) goes RED → revert. Paste both failing assertions.
  </action>
  <verify>
    <automated>npx vitest run src/lib/strategyGate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isLedgerBackedExchange\|isLedgerBacked" src/lib/strategyGate.ts` = 0.
    - `grep -c "deribit" src/lib/strategyGate.ts` = 0 (comment-stripped or not — zero total).
    - The allow-list Set contains exactly three literals; the branch uses `.has(...)`, no negation-based membership.
    - The NO_DATA_SOURCE guard at the equivalent of :154 still reads `!input.apiKeyId` (git diff shows no hunk on it).
    - database.types.ts diff touches exactly the three strategy_analytics blocks, one column each.
    - All seven listed cases exist and pass; SC-1(TS) + SC-2 Observed with pasted evidence in SUMMARY.
    - `npx vitest run src/lib/strategyGate.test.ts` green. (Repo-wide typecheck completes in Task 2 — consumers still reference the deleted export until then.)
  </acceptance_criteria>
  <done>The gate reads a verdict, refuses absence, exports one predicate, and the D-15 acceptance test is red-provable.</done>
</task>

<task type="auto">
  <name>Task 2: Move both consumers onto the verdict + fix both vi.mock sites + composite approvability</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/api/admin/strategy-review/route.ts, src/app/api/admin/strategy-review/route.test.ts, src/__tests__/audit-fanout-integration.test.ts</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:1165-1345 — the 7-member
      Promise.all; the select to WIDEN at :1174-1180 (add series_completeness to the column string —
      zero new queries); the gate call at :1310-1326 (the isLedgerBacked term + its two-line comment
      go; ⚠️ the keyRowResult member STAYS — keyRow?.exchange is used at ~:1366 for an unrelated
      purpose); the import at :15. ⚠️ The composite read at :933-935 (metrics_json_by_basis,
      data_quality_flags) is a DIFFERENT path that never routes through checkStrategyGate (:923) —
      do not touch it.
    - src/app/api/admin/strategy-review/route.ts — first-pass strategy_analytics select :160
      ("computation_status, computation_error" — widen), gate call :265-284, the api_keys exchange
      lookup + fail-loud 503 :238-256 (REVIEW_SOURCE_READ_FAILED — the idiom for any new read
      failure), TOCTOU re-check select :337 ("computation_status" — widen; it is .single() with an
      existing isReadFailure guard at :362 — do NOT add a second read), the hand-duplicated
      predicate :377-380 and its "must never diverge" comment (DELETE — the comment was the entire
      enforcement mechanism and it failed).
    - src/app/api/admin/strategy-review/route.test.ts — vi.mock of the deleted export at :109; gate
      cases :678-733; the composite documentation case at :1073.
    - src/__tests__/audit-fanout-integration.test.ts:837 — the second vi.mock.
  </read_first>
  <action>
    SyncPreviewStep.tsx:
    1. Widen the :1174 select with `series_completeness`; pass
       `seriesCompleteness: analytics?.series_completeness ?? null` into checkStrategyGate with a
       comment naming the deliberate null-coercion as the SAFE direction (refusal). Remove the
       isLedgerBackedExchange import and its call/term. Keep the keyRowResult Promise.all member.
    admin/strategy-review/route.ts:
    2. Widen BOTH strategy_analytics selects (:160 and :337) with series_completeness. First-pass
       gate call passes seriesCompleteness. Replace the :377-380 inline predicate with the exported
       `isDailyReturnsSourced({ tradeCount: recheckTradeCount, csvRowCount: recheckCsvCount ?? 0,
       seriesCompleteness: <recheck read> })` — noting the existing recheckCsvCount null-guard
       semantics must be preserved, not silently changed. Delete the local `let isLedgerBacked`
       resolution and — ONLY IF keyRow.exchange serves no other purpose in this file (grep within
       the file first) — the api_keys exchange lookup that fed it, including its now-orphaned 503
       arm. If exchange IS used elsewhere, keep the lookup and delete only the isLedgerBacked
       assignment.
    Tests:
    3. route.test.ts — delete/replace the :109 vi.mock (mock the strategy_analytics read to return
       verdicts instead); rewrite gate cases :678-733 onto verdict fixtures. NEW acceptance cases:
       (a) composite (api_key_id null) whose recheck reads series_completeness "composite_stitched"
       → approvable end-to-end through the route; (b) same composite with NULL verdict → refused —
       this pair is the SC-4 oracle: a hand-copy retaining the old `!approveApiKeyId` term passes
       (b) wrongly, so (b) red-pins the shared-predicate call.
    4. audit-fanout-integration.test.ts:837 — replace the vi.mock of the deleted export with
       whatever the fanout flow now needs (likely a strategy_analytics verdict fixture); keep the
       test's original intent intact — do not delete assertions to make it pass.
    5. **SC-4 mutation run (record in SUMMARY):** temporarily restore the hand-duplicated
       three-line predicate (old form with `!approveApiKeyId || isLedgerBacked`) in place of the
       shared-predicate call → composite-NULL case (b) MUST go RED → revert. Paste evidence.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/admin/strategy-review/route.test.ts src/__tests__/audit-fanout-integration.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "isLedgerBackedExchange" src/` returns ZERO hits (production and test code both).
    - Admin route contains exactly ONE textual implementation of the daily-branch predicate — the import from strategyGate — verified by `grep -c "recheckTradeCount === 0" src/app/api/admin/strategy-review/route.ts` = 0 (the inline form is gone).
    - Both strategy_analytics selects in the admin route include series_completeness; SyncPreviewStep's :1174 select includes it; `grep -rn "from(\"strategy_analytics\")" src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx | wc -l` is unchanged from HEAD (zero NEW queries).
    - Composite approvable/refused pair passes; SC-4 Observed with pasted evidence.
    - `npm run typecheck && npm run lint` clean repo-wide; both spec files green.
  </acceptance_criteria>
  <done>One predicate, three call sites, zero venue knowledge in TS, composites still publishable.</done>
</task>

<task type="auto">
  <name>Task 3: strategyGate.invariant.test.ts — the no-venue-literal pin (SC-3)</name>
  <files>src/lib/strategyGate.invariant.test.ts</files>
  <read_first>
    - src/lib/seam-venue-vocabulary.invariant.test.ts — the analog (353 lines; the smallest complete
      example of the idiom — NOT seam-budgets, which is 1,190 lines of budget arithmetic). Copy: the
      `// @vitest-environment node` pragma + stripCommentsPreserveLines import from ./source-scan
      (:1-10); the predicate-in-prose section (:72-93); hand-typed rosters never `[...derived]`
      (:155-184); the anti-vacuity floor (:186-195, :201-213); the SELF-TEST cases (:301-350).
    - src/lib/source-scan.ts — the shared comment-stripper.
    - src/lib/strategyGate.ts (post-Task-1)
  </read_first>
  <action>
    Create src/lib/strategyGate.invariant.test.ts:
    1. Read src/lib/strategyGate.ts from disk, strip comments via stripCommentsPreserveLines, and
       assert ZERO occurrences of each venue literal in a hand-typed list:
       ["deribit", "sfox", "mt5", "binance", "bybit", "okx"] — the gate must hold no venue knowledge
       of any kind (MT5-12: "no hardcoded venue set governs gate routing").
    2. ⚠️ A zero-assertion is vacuous by definition (green if the file is empty, missing, or
       misnamed). Add the POSITIVE floor: (a) the stripped source is non-empty (length above a
       hand-typed literal floor); (b) it contains "checkStrategyGate" and "isDailyReturnsSourced"
       and all three allow-list literals ("ledger_complete", "user_supplied", "composite_stitched" —
       hand-typed here, NOT imported, so widening the allow-list in source without updating this
       test reddens); (c) SELF-TESTs feeding synthetic source through the same scanner: a venue
       literal in code IS found; the same literal inside a comment is NOT counted after stripping.
    3. Prose section stating the predicate so the count is reproducible without reading the code.
    4. **SC-3 mutation run (record in SUMMARY):** temporarily re-add `exchange === "deribit"` as an
       extra `||` term (with a plumbed-through input or a dead-code form — whatever compiles) in
       strategyGate.ts → this test MUST go RED naming "deribit" → revert. Paste evidence. The
       mutation only counts because the SELF-TESTs prove the scanner is alive.
  </action>
  <verify>
    <automated>npx vitest run src/lib/strategyGate.invariant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The venue-literal roster and the allow-list expectation are hand-typed literals; nothing is imported from strategyGate.ts.
    - Positive floor + at least two SELF-TEST cases present and passing.
    - SC-3 Observed with pasted failing assertion in SUMMARY; tree restored.
    - Full-file vitest green; `npm run typecheck` clean.
  </acceptance_criteria>
  <done>A future "quick fix" cannot reintroduce a venue literal without a red test naming it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB verdict → gate decision | a persisted enum decides publication admissibility |
| browser (owner RLS) → strategy_analytics | wizard reads the verdict client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.2-15 | Tampering/Repudiation | understated track record published as api_verified (D-15) | mitigate | positive allow-list; NULL refuses; SC-1/SC-2 mutations observed as acceptance |
| T-142.2-16 | Tampering | predicate drift re-armed at the third site (admin TOCTOU hand copy) | mitigate | copy deleted; both sites call one export; SC-4 composite-NULL oracle |
| T-142.2-17 | Tampering | venue literal reintroduced by a future patch | mitigate | invariant test with anti-vacuity floor + SELF-TESTs (SC-3) |
| T-142.2-18 | DoS (user-inflicted) | destructive remedy on refusal (onTryAnotherKey fires handleDeleteDraft) | accept (this phase) | pre-existing; MT5-12 removes the UNWINNABLE case for MT5; general destructive remedy logged to TODOS.md by plan 142.2-08 |
| T-142.2-19 | Elevation of privilege | new column readable beyond owner | mitigate | zero RLS changes; A2 grant check executed in plan 142.2-04; client read rides the proven owner-RLS path (SyncPreviewStep :600-605 precedent) |
| T-142.2-SC | Tampering | package installs | accept | no installs in this plan |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test` green repo-wide (full TS suite at plan end — this plan deletes an export with five consumer files).
- Coverage thresholds still clear (lines 82 / statements 80 / functions 74 / branches 72).
- Reproduce any CI-only vitest failure under `PATH=/opt/homebrew/opt/node@22/bin` before calling it a flake (CI is Node 22, local 25).
- SC-1(TS), SC-2, SC-3, SC-4 Observed with pasted evidence in SUMMARY.
</verification>

<success_criteria>
- The fills-gapped perp is still refused; NULL refuses; composites approvable; zero venue literals; one predicate at all three former sites.
</success_criteria>

<output>
Create `.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-06-SUMMARY.md` when done.
</output>
