---
phase: 142.2
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql
  - supabase/tests/test_strategy_analytics_series_completeness.sql
autonomous: true
requirements: [MT5-12]

must_haves:
  truths:
    - "A migration file exists that adds strategy_analytics.series_completeness as text, nullable, no default, no CHECK, no RLS change, no backfill — and self-verifies those properties with RAISE EXCEPTION"
    - "A supabase/tests/test_*.sql gate (the only DB gate that counts in CI) asserts the column is nullable with no default, and SKIPs gracefully when the migration has not yet reached the shared TEST DB"
    - "No UPDATE of existing rows appears anywhere in the migration — NULL is the honest verdict for series whose inputs were never examined (Pitfall 6)"
  artifacts:
    - path: "supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql"
      provides: "The MT5-12 verdict carrier: additive nullable column on strategy_analytics"
      contains: "ADD COLUMN IF NOT EXISTS series_completeness"
    - path: "supabase/tests/test_strategy_analytics_series_completeness.sql"
      provides: "CI-counting SQL gate pinning nullable/no-default"
      contains: "series_completeness"
  key_links:
    - from: "supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql"
      to: "strategy_analytics"
      via: "ALTER TABLE public.strategy_analytics"
      pattern: "ADD COLUMN IF NOT EXISTS series_completeness"
---

<objective>
Create the persisted carrier for the MT5-12 completeness verdict: a new nullable
`strategy_analytics.series_completeness` column, plus the CI-counting SQL gate that pins its shape.

Purpose: the gate (TS) must read a verdict the Python producer stamped; the verdict must outlive the
job. D-16 superseded the per-row `csv_daily_returns` sketch — completeness is a property of the
series, and the series' identity is the strategy (RESEARCH § Alternatives Considered).
Output: one migration file + one SQL gate file. **Nothing is applied by this plan** — MCP apply to
TEST is plan 142.2-04 (orchestrator-only); PROD application happens only via merge to main.

⛔ Merging `supabase/migrations/**` to main AUTO-APPLIES TO PRODUCTION. This file is the
highest-risk artefact of the phase. It must be additive, nullable, no default, no constraint on
existing rows, no backfill, and must NOT add or alter any RLS policy (V4: if it appears to need
one, the column is on the wrong table). No `DELETE` of compute_jobs anywhere. Never `cron.unschedule(9)`.

**Value-set contract (LOCKED for the whole plan set — copy verbatim, do not re-derive):**
- `series_completeness` values: `ledger_complete` | `sampled_gapped` | `fill_derived_unproven` | `user_supplied` | `composite_stitched`
- The value set is defined in exactly ONE registry: Python (`broker_dailies.py`, plan 142.2-03).
- ⛔ **No SQL CHECK constraint on the values.** A CHECK would be a second hand-maintained copy of the
  value set — the same two-registries-drift class MT5-12 exists to delete. The Python fail-loud
  assert (plan 142.2-05) is the enforcement. State this reasoning in the column COMMENT.
- `composite_stitched` exists because `run_stitch_composite_job` is the second `csv_daily_returns`
  producer (PATTERNS § Class-Fix Site Ledger item 3): without a composite verdict, deleting the
  `!input.apiKeyId` gate term makes every composite read NULL → INSUFFICIENT_TRADES → un-approvable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-CONTEXT.md
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the additive-nullable series_completeness migration</name>
  <files>supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql</files>
  <read_first>
    - supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql — the analog
      (most recent additive nullable column on this exact table). Copy the DDL shape at :215-222
      (nullable, NO DEFAULT, NO SET NOT NULL, IF NOT EXISTS, reasoning inline) and the
      self-verification tail at :546-560 (RAISE EXCEPTION on: column missing, NOT NULL, has DEFAULT).
      Do NOT copy its index or cron — neither is needed here.
    - .planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-PATTERNS.md
      § "supabase/migrations/<ts>_strategy_analytics_series_completeness.sql"
  </read_first>
  <action>
    Create `supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql`
    (timestamp verified strictly greater than the newest existing migration, 20260803130000).
    Content, per the analog's shape:
    1. Header comment: MT5-12, the D-16 verdict carrier; name the five values and state that the
       value registry lives in `analytics-service/services/broker_dailies.py` ONLY — no SQL CHECK by
       design (a CHECK is a second registry; the derive-seam assert in job_worker.py is the
       enforcement). State: additive, nullable, no default, no backfill, no RLS change.
    2. `ALTER TABLE public.strategy_analytics ADD COLUMN IF NOT EXISTS series_completeness text;`
    3. `COMMENT ON COLUMN public.strategy_analytics.series_completeness IS ...` — name the five
       values, the producer-decides principle, that NULL means "never examined — the gate refuses
       it" (fail-closed, D-15/SC-2), and that a backfill is forbidden (a backfill fabricates a trust
       claim about series whose inputs no longer exist — Pitfall 6).
    4. Self-verification `DO` block, copying the analog's three RAISE EXCEPTION probes reworded for
       this column: (a) column exists; (b) `is_nullable = 'YES'` (a NOT NULL is a 23502 timebomb
       against every existing writer); (c) `column_default IS NULL` (a DEFAULT would stamp a verdict
       on rows nobody examined).
    Absolutely NOT in this file: any `UPDATE`, any `CREATE POLICY`/`ALTER POLICY`, any CHECK
    constraint, any index, any cron, any DELETE.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS series_completeness" supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql && ! grep -viE "^--" supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql | grep -qiE "UPDATE public\.|CREATE POLICY|ALTER POLICY|CHECK \(|CREATE INDEX|cron\.|DELETE FROM"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at the exact path with timestamp 20260803150000 (greater than 20260803130000).
    - Contains exactly one DDL statement: `ALTER TABLE public.strategy_analytics ADD COLUMN IF NOT EXISTS series_completeness text;` plus a COMMENT ON COLUMN.
    - The comment-stripped body contains zero occurrences of: `UPDATE public.`, `CREATE POLICY`, `ALTER POLICY`, `CHECK (`, `CREATE INDEX`, `cron.`, `DELETE FROM` (case-insensitive).
    - Self-verification DO block RAISEs on: column absent, `is_nullable <> 'YES'`, `column_default IS NOT NULL` — three distinct RAISE EXCEPTION arms, each naming series_completeness.
    - Header names all five values verbatim: ledger_complete, sampled_gapped, fill_derived_unproven, user_supplied, composite_stitched — and states the Python-only-registry / no-CHECK rationale.
  </acceptance_criteria>
  <done>Migration file exists, additive-nullable-only, self-verifying, with the value set documented but not constrained.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CI-counting SQL gate for the column's shape</name>
  <files>supabase/tests/test_strategy_analytics_series_completeness.sql</files>
  <read_first>
    - supabase/tests/test_allocator_equity_pre_terminus_flag.sql — the analog. Copy four properties:
      (a) `BEGIN;` whole-test-rolls-back header; (b) the shared-TEST-DB lag skip (SELECT from
      information_schema.columns; if the column is absent, RAISE NOTICE 'SKIP: migration
      20260803150000 not yet applied here...' and RETURN); (c) numbered assertions with RAISE
      EXCEPTION interpolating the observed value; (d) `RAISE NOTICE 'Assertion N OK: ...'` per pass.
      ⚠️ INVERT THE POLARITY: that analog asserts NOT NULL DEFAULT false; this column must assert
      `is_nullable = 'YES'` and `column_default IS NULL`. Copy the structure, not the predicates.
    - supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql (Task 1 output)
  </read_first>
  <action>
    Create `supabase/tests/test_strategy_analytics_series_completeness.sql` asserting, against
    `information_schema.columns` for `public.strategy_analytics.series_completeness`:
    1. `data_type = 'text'`
    2. `is_nullable = 'YES'` — failure message must say a NOT NULL here is a 23502 timebomb.
    3. `column_default IS NULL` — failure message must say a DEFAULT stamps a verdict on unexamined rows.
    4. A negative registry probe: assert `pg_constraint` holds NO CHECK constraint whose definition
       (`pg_get_constraintdef`) mentions `series_completeness` — pinning the "no second registry in
       SQL" decision so a future 'helpful' CHECK reddens CI.
    Include the lag-skip so a PR branch does not redden before the migration reaches the shared
    TEST DB, and wrap in BEGIN;/ROLLBACK;. This is a schema-shape gate only — do NOT assert on row
    contents (workers will legitimately populate values after deploy).
  </action>
  <verify>
    <automated>grep -c "is_nullable" supabase/tests/test_strategy_analytics_series_completeness.sql && grep -c "SKIP" supabase/tests/test_strategy_analytics_series_completeness.sql && grep -c "pg_get_constraintdef\|pg_constraint" supabase/tests/test_strategy_analytics_series_completeness.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists in supabase/tests/ (the ONLY DB-gate location that runs in CI).
    - Asserts data_type='text', is_nullable='YES', column_default IS NULL — each a numbered RAISE EXCEPTION arm interpolating the observed value.
    - Asserts zero CHECK constraints referencing series_completeness.
    - Contains the lag-skip (RAISE NOTICE 'SKIP: ...' + RETURN when the column is absent) naming migration 20260803150000.
    - Whole file rolls back (BEGIN; ... ROLLBACK;). Zero assertions about row values.
  </acceptance_criteria>
  <done>SQL gate exists, polarity-inverted from the analog, lag-safe, and pins both the nullable shape and the no-CHECK decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrations/** → PROD | merging to main auto-applies this SQL to production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.2-01 | Tampering/DoS | the migration itself | mitigate | additive nullable, no default, no constraint, no backfill, no RLS change; self-verifying DO block; TEST-first apply in plan 142.2-04 before any merge |
| T-142.2-02 | Tampering | a future backfill fabricating trust (`UPDATE ... SET series_completeness`) | mitigate | Pitfall-6 prohibition stated in the column COMMENT; acceptance grep forbids UPDATE in this file |
| T-142.2-03 | Information disclosure | verdict channel carrying magnitudes | mitigate | column carries an enum-like text verdict only — no row counts, gap-day counts, or money (T-73-02 discipline); enforced downstream in plans 03/05 |
| T-142.2-SC | Tampering | package installs | accept | this plan installs nothing (RESEARCH § Package Legitimacy Audit: no packages in phase scope) |
</threat_model>

<verification>
- Both files exist; `git add` includes both (`.planning` and supabase files are tracked).
- The forbidden-token grep in Task 1 passes (no UPDATE/POLICY/CHECK/INDEX/cron/DELETE outside comments).
- Do NOT attempt to apply the migration anywhere from this plan — MCP is orchestrator-only (plan 142.2-04).
</verification>

<success_criteria>
- Migration + SQL gate committed, shape-pinned, unapplied.
- The no-CHECK / Python-single-registry decision is stated in both files, so it cannot be re-litigated silently.
</success_criteria>

<output>
Create `.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-01-SUMMARY.md` when done.
</output>
