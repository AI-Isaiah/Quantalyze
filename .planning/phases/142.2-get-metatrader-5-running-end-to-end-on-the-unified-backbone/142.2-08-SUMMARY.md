---
phase: 142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone
plan: 08
subsystem: planning-records
tags: [phase-close, falsifiability-ledger, backlog, human-uat, mt5, honesty-boundary]

# Dependency graph
requires:
  - "142.2-02 .. 142.2-07 (all seven prior SUMMARYs — the evidence this plan transcribes)"
provides:
  - "TODOS.md — 11 DEF-142.2-NN entries; the founder-observed MT5 wizard defects marked CLOSED"
  - "142.2-VALIDATION.md — ledger 10/10 closed with pasted assertions; three fiction classes corrected; frontmatter status: executed"
  - "142.2-HUMAN-UAT.md — MT5-05 persisted as the phase's OPEN post-deploy human item"
  - "The reachable-not-correct boundary written into both the ledger and the UAT checklist"
affects:
  - "142.3 (inherits MT5-06..10 and the number-correctness gate this phase explicitly did NOT close)"
  - "any future reader of TODOS.md (deferred-items.md content is now promoted; no new tracker created)"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Transcribe the mutation ACTUALLY RUN, with substitutions and prior-run provenance stated, never the row's original text"
    - "Describe a marker string rather than quoting it when a raw grep is the acceptance criterion"
    - "An e2e seed audit may honestly conclude NO EDIT — a dishonest fixture verdict to satisfy a checkbox is worse than an unwidened seed"

key-files:
  created:
    - .planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-HUMAN-UAT.md
  modified:
    - TODOS.md
    - .planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md

key-decisions:
  - "e2e/helpers/seed-test-project.ts NOT edited — the audit found no wizard spec drives the gate's daily branch (the composite arm never routes through checkStrategyGate), so widening would have written dishonest verdicts into fixtures to satisfy a checkbox"
  - "npm run test:e2e recorded as skipped-with-reason, never as green — the seed-gated specs self-skip without TEST_SUPABASE_* and a run that skips is not evidence"
  - "SC-5 recorded as observed-by-a-prior-run and explicitly NOT re-verified; not upgraded to first-hand"
  - "SC-6's oracle recorded as the every-payload SCANS, not the stamp test, which stayed GREEN under the mutation"
  - "MT5-05 persisted as PENDING, never approved — nothing is deployed and a green suite is not evidence"

requirements-completed: [MT5-01, MT5-02]
requirements-pending: [MT5-05]

# Metrics
duration: ~55min
completed: 2026-08-04
---

# Phase 142.2 Plan 08: Honest phase close Summary

**Every deferral folded into the one backlog, all ten falsifiability rows closed on the mutation
that was actually run — including a documented substitution and one row that a stalled run observed
and the recovery run never re-verified — three ledger fictions struck, and MT5-05 persisted as the
phase's open human item rather than approved untested.**

## Performance

- **Duration:** ~55 min
- **Tasks:** 2 of 3 executed; **task 3 deliberately NOT satisfied** (see below)
- **Files created:** 1 · **modified:** 2 · **code files touched: 0**

## Task Commits

| Task | Commit | What |
|---|---|---|
| 1 | `94d43b31` | TODOS.md — 11 `DEF-142.2-NN` entries; founder-observed MT5 section closed |
| 2 | `abe6b296` | VALIDATION.md — ledger backfilled, three fiction classes corrected, checklists closed, frontmatter set |
| 3 | `8df1025c` | `142.2-HUMAN-UAT.md` — MT5-05 persisted as **PENDING**, not approved |

---

## ⛔ Task 3 (MT5-05) is PENDING — human, post-deploy. It was NOT approved.

**MT5-05 requires a founder to complete the MT5 connect flow on PRODUCTION, and nothing from this
phase has been deployed.** It could not be satisfied by this run and was not pretended to be.

Persisted as `142.2-HUMAN-UAT.md` (`status: pending`, `gate: blocking-human`) in the 142.1 pattern —
four checks, run after `/ship` + `/land-and-deploy` (deploy order: migration auto-applies on merge →
Railway worker → Vercel). It states the pass condition (**no step requires knowing an internal error
code, a server name, or a flag**), that any point where the founder must ask what a code means is a
**FAILURE fixed in-phase**, and that it proves **reachability, NOT number-correctness**.

⚠️ **A green suite is explicitly not evidence for it.** v1.15 shipped 6/6 phases green with both
open items intact — that is the failure mode MT5-05 exists to catch, and it is why this plan did not
infer approval from the 10,580 passing TS tests below.

---

## Task 1 — one backlog, complete deferral record

**11 `DEF-142.2-NN` entries** in `TODOS.md`, each naming its source decision. **No new tracker file
was created** — `deferred-items.md` is the GSD per-phase convention and its content was **promoted**
into the one ground truth rather than left in a second place.

Highlights where the entry carries more than a title:

- **`DEF-142.2-01` (D-04, typeahead)** — filed with **the data-source problem named**, so it is not
  re-opened as a simple UI task: zero repo hits for `broker_server`/`server_name`, no public
  canonical registry, and all three candidate sources (curated static list / learn-from-connections
  / public registry) recorded as considered and **rejected with reasons**.
- **`DEF-142.2-02` (D-06)** — **9 sites, not 11.** Re-measured at HEAD by this plan:
  `keys/validate-and-encrypt` **4**, `verify-strategy` **5**. The old 11 counted comment prose (the
  same delta that made the in-scope routes read 14 instead of 12).
- **`DEF-142.2-09` (Pitfall 6)** — the healing population with **all three per-producer remedies**
  mapped to their counts: **1 keyed → re-derive**, **4 keyless CSV → re-run
  `compute_analytics_from_csv`**, **1 composite → re-stitch**, **0 published composites**. ⛔ Never a
  backfill `UPDATE`. The keyless remedy is called out as the one that is easy to omit and covers the
  largest group — "re-derive or re-stitch" alone would be an *impossible instruction* for 4 of the 6.
- **`DEF-142.2-07` (Pitfall 7)** — deribit `twr_chain_broken` tightening booked with plan 04's PROD
  census attached: **0, total and published**. Tightening would affect nothing today; plan 03 shipped
  behaviour-preserving. **Marked deferred to the founder, not taken.**
- **`DEF-142.2-10`** — the Vercel Workflow-DevKit recommendation, **declined**, with the reasoning
  intact: these are the two **secret-bearing** routes, so durable execution puts raw credentials
  across a **new persistence boundary** — a threat-model question, not a refactor.
- **`DEF-142.2-06`** — `database.types.ts` drift: three pre-existing missing columns and **no
  regeneration script**, so it needs hand-patching; plan 06 patched only `series_completeness`.
  Marked as absorbing the narrower Phase-142 entry so they are not fixed twice.

**Founder-observed MT5 wizard section updated, not deleted.** Both defects marked ✅ CLOSED —
defect 1 by MT5-01 (env flip + redeploy 2026-08-03, an **env** fix, never a code defect), defect 2 by
plan 07's class fix. ⛔ **D-05's rejection of server-string passthrough is recorded there** so it is
not re-opened as an unfinished half.

**No duplication:** the `strategy_analytics (*)` anon splat (commit `d935fa61`) is cross-referenced,
not re-entered.

---

## Task 2 — the ledger, and the three fictions

### 10/10 rows closed, none left unrun

Every SC row now carries the mutation **as actually run**, a **pasted failing assertion**, and its
owning plan and date. Three of them carry a provenance qualifier that a careless transcription would
have erased:

**SC-1 — a documented SUBSTITUTION on the TS side, recorded as both halves.** The PY half (plan 03)
ran this row's original text — stamp `ledger_complete` on the realized-empty perp path. The TS half
(plan 06) ran **allow-list widening instead**, and plan 06's SUMMARY states the substitution in those
words for exactly this transcription. Recording the row's original wording for the TS half would have
credited evidence to an edit nobody made.

**SC-5 — observed by plan 05's FIRST, stalled run; NOT re-run by the recovery run.** Plan 05's own
SUMMARY labels it taken-on-trust. It is recorded here as **observed-by-a-prior-run**, with the
assertion quoted from that run's commit message and the non-re-verification stated. ⛔ Not upgraded
to first-hand. What the recovery run *did* verify first-hand is recorded alongside it (whole suite
green at that HEAD; the seam check at `job_worker.py:4434` genuinely preceding the reconcile DELETE
constructed at `:4563` / executed at `:4580`).

**SC-6 — the oracle is the every-payload SCANS, not the stamp assertion.** Under the mutation the
keyless stamp test stayed **GREEN** (the conditional stamp overwrites the injected `None`). A row
citing the stamp test would have recorded a mutation as caught by a test that structurally cannot
catch it.

### Three fiction classes corrected

**(a) "each of the 14 causes" → 12.** The map rows now read 12; the correction note records that
**24 sites (12+12)** were in scope and **9 (4+5)** deferred — not the research's 28 and 11.

**(b) ⛔ The "type-level | mypy --strict" guarantee DOES NOT EXIST.** Combiners return
`dict[str, Any]` and `NavTWRMeta` is `total=False`, so mypy can see **neither an omitted stamp nor a
spurious one**. **Three plans reported this independently (03, 05, 06's handoff)** — it was not
softened. The row is rewritten to the enforcement that exists: the **runtime seam assert**, with the
three empty-path exemptions named and each citing `job_worker.py:4340`.

**(c) SC-5's Must-turn-RED column no longer names mypy** — struck for the same reason, leaving the
seam test as the sole oracle, with a one-line note saying why.

Also corrected as the same fiction: the Test-Infrastructure **full-suite command**, which read
`mypy --strict .` — not this repo's gate, and pre-existing red at ~5483 errors (~all in `tests/`,
untyped by design). Replaced with the real CI gate.

### Checklists and boundary

Wave-0 (6/6 delivered, each with its artefact and commit), Oracle Independence (6 boxes with evidence
pointers **plus the one deliberate boundary exclusion named and independently covered**), Sign-Off
including **A1 / A2 / A4** — A1 confirmed **executed** through the real PostgREST path, A2 confirmed
via a **corrected** check (the plan's original expected a count that structurally can never be zero),
and **A4 resolved FALSE → three producers**.

A **Phase boundary** section states, in the file that governs this phase's proof, that 142.2 delivers
MT5 **reachable and honest** and that **MT5's rendered NUMBERS remain UNVERIFIED pending Phase
142.3** — naming the near-midnight offset failure mode as the reason a rendered strategy is not
evidence of a correct one. It also records MT5-01/02 as **pre-delivered 2026-08-03 ahead of
planning**, with **no file in this phase implementing them**, so requirement coverage stays honest.

---

## The e2e seed audit — a finding, not an edit

VALIDATION.md's Sampling Rate demanded `npm run test:e2e` for the wizard specs, and this plan owned
the seed audit that had to precede it. **Both are recorded honestly rather than checked off.**

**Audited at HEAD:** `e2e/helpers/seed-test-project.ts` — four `strategy_analytics` inserts at
`:565`, `:1029`, `:1292`, `:1486`, **none** carrying `series_completeness`.

**Finding: no wizard e2e spec exercises the gate's daily branch, so no seed needed widening.**

- `SyncPreviewStep.tsx:920-926` — the **composite arm never routes through `checkStrategyGate`** and
  never queries `trades`, by design (a composite has 0 fills; the single-key gate would false-fail
  `INSUFFICIENT_TRADES`). `composite-onboarding.spec.ts`, the one spec that walks the wizard to a
  *passing* preview, takes that arm — so the `:1029` composite seed's NULL verdict is never read by
  a gate.
- `checkStrategyGate` has exactly two call sites (`SyncPreviewStep.tsx:1312` single-key arm;
  `admin/strategy-review/route.ts` approve path) and **no e2e spec reaches either**.
  `wizard-sync-regression.spec.ts` self-skips without `PLAYWRIGHT_TEST_STRATEGY_ID` and stubs
  `/api/keys/sync`; `wizard-axe` / `wizard-hydration-probe` never leave the connect step; no
  admin-approve spec exists.

⛔ **The seed was therefore NOT edited.** Blanket-stamping `ledger_complete` on four fixtures that
are not ledger-complete would have written a **dishonest verdict into a fixture to satisfy a
checkbox** — the exact class this phase exists to close.

⚠️ **`npm run test:e2e` was NOT executed**, and is recorded as **skipped-with-reason**: the
seed-gated specs self-skip without `TEST_SUPABASE_URL` / `TEST_SUPABASE_SERVICE_ROLE_KEY` (absent
from this worktree), and a run that skips is not evidence. The gate-relevant question it existed to
answer was answered by the audit.

---

## Verification

| Gate | Result |
|---|---|
| `npm run typecheck` | ✅ clean |
| `npm run lint` | ✅ **0 errors**, 1 pre-existing warning (`EquityChart.tsx:1119`) — booked `DEF-142.2-11` |
| `npm run test` (full TS) | ✅ **737 files / 10,580 tests passed**, 19 + 287 skipped |
| `pytest` (full PY, **from `analytics-service/`**) | ✅ **4875 passed, 96 skipped** in 70.35 s |
| `mypy --strict --follow-imports=silent services/ routers/ models/` | ✅ **Success: no issues found in 89 source files** |
| `mypy --strict .` | ⛔ not run — **not this repo's gate**, pre-existing red |
| `npm run test:e2e` | ⚠️ **skipped-with-reason** (see above) — not claimed green |
| `grep -c "DEF-142.2" TODOS.md` | **13** (11 entries + 2 in-prose references); plan required ≥ 8 |
| Unrun-marker count in VALIDATION.md | **0** |
| `grep -q "nyquist_compliant: true"` | ✅ present |
| `git status` | only `TODOS.md` + the two planning docs; **no new tracker file, no seed edit** |

⚠️ `pytest` was run **only** from `analytics-service/` — a repo-root run misses the VCR
`cassette_library_dir` and makes **live broker calls**. It initially failed with
`ModuleNotFoundError: pandas` because the PATH `pytest` is not the interpreter carrying the deps; re-run
as `python3 -m pytest` from `analytics-service/`, which resolves them. Not a code failure — a local
interpreter-selection artifact, recorded so the next runner does not read it as one.

⚠️ **Every gate above is advisory at merge** — `main` carries no branch protection (founder decision,
deferred until paying clients). These read as *"the suite would have caught it"*, never *"CI stopped
it"*.

## Deviations from Plan

**None on substance.** Three points worth recording, none of which changed scope:

**1. [Rule 1 — Bug in my own output] My prose quoted the unrun-row marker, which kept the acceptance
grep reporting the class open.** The plan's automated verify is a raw `grep -c` with no prose
exclusion, so two sentences *claiming* zero unrun rows made the count read 2. Both were reworded to
**describe** the marker instead of quoting it. This is the identical failure plan 07 hit with the
`Client-side` clause, met from the other direction — and it is cited as such in the file.

**2. [Plan-sanctioned] The e2e seed was not edited.** The plan explicitly permits this: *"If no
wizard spec exercises the daily branch, record that finding in the SUMMARY instead of editing the
seed."* The finding is recorded in both this SUMMARY and VALIDATION.md.

**3. The Test-Infrastructure `mypy --strict .` row was corrected too**, not only the two rows the
plan enumerated. It is the same fiction (plan 03 flagged it and asked that plans 05/08 fix it);
leaving it would have kept a command in the file that cannot pass.

## Issues Encountered

None blocking. No auth gates, no package installs, no architectural questions. One mechanical note:
the worktree carries no `node_modules` and no `.env*` files — Node resolution walks up to the parent
repo (byte-identical `package-lock.json`), so `vitest`/`tsc`/`eslint` run; the absent env files are
why the seed-gated e2e specs cannot produce evidence here.

## Known Stubs

None. This plan writes no code. The one deliberately-unfinished artefact is `142.2-HUMAN-UAT.md`,
which is **designed** to be open — MT5-05 is a human outcome check that cannot be satisfied before
deploy, and marking it otherwise would be the fabrication this plan exists to prevent.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern, or trust-boundary schema change; no
code was modified.

Register dispositions:
- **T-142.2-23** (*"verified" claims without run evidence — the 142 ledger failure*) — **mitigated.**
  Every row carries a pasted assertion; the substituted mutation is recorded as run **with the
  substitution named**; the prior-run row is recorded as such and not upgraded; the row whose stamp
  test cannot fail cites the scans instead.
- **T-142.2-24** (*142.2 close implying MT5 numbers are correct — the v1.15 failure shape*) —
  **mitigated.** The reachable-not-correct boundary is written into VALIDATION.md's Phase-boundary
  section, the HUMAN-UAT checklist, and the TODOS.md section header, each naming 142.3 as the gate.
- **T-142.2-SC** (package installs) — **accepted**; none attempted.

## Next Phase Readiness

- **Owed before close:** `/ship` + `/land-and-deploy`, then the founder runs `142.2-HUMAN-UAT.md`.
  **The phase is not closed until MT5-05 is discharged by a human**, and it may re-open work: any
  failure point in that checklist is fixed **in-phase**.
- **Deploy order is migration → worker → frontend.** The NULL window is self-healing and named; the
  6-strategy affected population and its three remedies are booked as `DEF-142.2-09`.
- **142.3 inherits** MT5-06..10 with D-07..D-11 preserved verbatim in `142.2-CONTEXT.md`. ⛔ Nothing
  in 142.2 is evidence about MT5's numbers.
- **STATE.md / ROADMAP.md deliberately untouched** — the orchestrator owns them in parallel mode.

## Self-Check: PASSED

| Claim | Verified |
|---|---|
| `TODOS.md` modified, 11 `DEF-142.2-NN` entries | FOUND (`grep -c "DEF-142.2"` → 13) |
| `142.2-VALIDATION.md` modified, 560 lines, frontmatter intact | FOUND (`status: executed`, `nyquist_compliant: true`, `wave_0_complete: true`) |
| `142.2-HUMAN-UAT.md` created, `status: pending` | FOUND |
| Commit `94d43b31` (Task 1) | FOUND |
| Commit `abe6b296` (Task 2) | FOUND |
| Commit `8df1025c` (Task 3 persistence) | FOUND |
| Zero unrun ledger rows | CONFIRMED (count 0) |
| No new tracker file; `e2e/helpers/seed-test-project.ts` unedited | CONFIRMED (`git status` clean of both) |
| Zero deletions in any commit | CONFIRMED |
| STATE.md / ROADMAP.md untouched | CONFIRMED |
| MT5-05 **not** marked complete anywhere | CONFIRMED — recorded PENDING in this SUMMARY, VALIDATION.md and the UAT file |

---
*Phase: 142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone*
*Completed: 2026-08-04 — ⛔ MT5 is REACHABLE and HONEST, not verified-correct. Numbers are 142.3.*
