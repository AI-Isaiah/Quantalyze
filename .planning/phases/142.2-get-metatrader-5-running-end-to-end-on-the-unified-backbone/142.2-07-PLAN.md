---
phase: 142.2
plan: 07
type: execute
wave: 2
depends_on: ["142.2-02"]
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/lib/wizardErrors.invariant.test.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
autonomous: true
requirements: [MT5-04]

must_haves:
  truths:
    - "Every one of the 24 emitting sites (12 + 12 — NOT the research's 28; the delta is comment prose) returns an honest cause-specific code; KEY_INVALID_FORMAT remains at exactly ONE site per route (the api_secret length<8 ccxt arm). Scope is the TWO wizard routes only; validate-and-encrypt and verify-strategy are deliberately out (D-06)"
    - "Guard expressions, error strings, and HTTP statuses are byte-identical at every site — ONLY the code literal changes (V5: changing validation logic under cover of a copy fix changes the security posture)"
    - "The KEY_INVALID_FORMAT cause copy no longer claims a client-side check — it was false at all 24 sites"
    - "Rendering is copy-by-code ONLY — D-05 REJECTED surfacing the server's error string; do not re-open"
    - "SC-8, SC-9, SC-10 mutations observed RED"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "Four new honest codes in the union + registry entries; corrected KEY_INVALID_FORMAT cause"
      contains: "KEY_VENUE_NOT_ENABLED"
    - path: "src/lib/wizardErrors.invariant.test.ts"
      provides: "Source-reading pin: every emitted code ∈ union ∧ the emitting route's roster; literal counts 12/12; exactly one KEY_INVALID_FORMAT emitter per route"
  key_links:
    - from: "src/app/api/strategies/create-with-key/route.ts"
      to: "src/lib/wizardErrors.ts"
      via: "code literals resolved by the client's copy registry"
      pattern: "KEY_MISSING_REQUIRED_FIELD"
    - from: "src/lib/wizardErrors.ts"
      to: "ConnectKeyStep.tsx KNOWN_CREATE_WITH_KEY_CODES + MultiKeyConnectStep.tsx KNOWN_ADD_KEY_CODES"
      via: "a new code is inert unless it lands in all three registries"
      pattern: "KEY_INPUT_TOO_LONG"
---

<objective>
MT5-04 (D-05/D-06, LOCKED): split `KEY_INVALID_FORMAT` into honest codes across the two wizard
routes. Distinct causes get distinct codes; `KEY_INVALID_FORMAT` keeps only actual format failures,
which makes its existing copy true again.

Purpose: the founder submitted a complete MT5 form and was told their "API key format is wrong" with
Binance hex-length advice — the code bucketed ~12 unrelated causes and the copy's "Client-side
format check" sentence was factually false (every site is a server-side route guard).
Output: 4 new codes in three registries, 24 site edits, corrected copy, route tests, invariant test.

**LOCKED boundaries:**
- Scope is the TWO wizard routes only. `keys/validate-and-encrypt` (4 emitting sites) and
  `verify-strategy` (5) are OUT — plan 142.2-08 logs those 9 sites to TODOS.md (not 11 — the
  research's counts were raw greps that included comment prose).
- Copy-by-code ONLY. D-05 explicitly rejected rendering the server's `error` string.
- The MT5-gate arm (create-with-key :149) is UNREACHABLE after MT5-01 — split it anyway; the value
  is in the class, not the instance. The composite add-key MT5 arm (:161) is UI-unreachable
  (MultiKeyConnectStep has no MT5 card) — split it for class-consistency, test at route level,
  and do NOT claim UI-test coverage of it.
- Do NOT change validation LOGIC. No cap "tidying", no guard reordering, no new validation.

**New codes (LOCKED names):** `KEY_MISSING_REQUIRED_FIELD`, `KEY_UNSUPPORTED_VENUE`,
`KEY_VENUE_NOT_ENABLED`, `KEY_INPUT_TOO_LONG`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-PATTERNS.md
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Registry first — union, copy entries, honesty fix, both rosters</name>
  <files>src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx</files>
  <read_first>
    - src/lib/wizardErrors.ts — the union (:37..:84-86 region); the entry analog KEY_MT5_WRONG_SERVER
      (:460-476): four-field shape { title, cause, fix: [...], docsHref, actions } — imitate it; the
      false sentence at :477-487 ("Client-side format check failed before sending the key to the
      exchange…").
    - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx:185-277 — the
      KNOWN_CREATE_WITH_KEY_CODES docblock argues the two rosters are DELIBERATELY not shared
      (routes emit different sets). Do NOT "simplify" them into one.
    - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx:214-229 —
      KNOWN_ADD_KEY_CODES; the MT5 codes at :228-229 show the add-a-code pattern.
    - src/lib/wizardErrors.test.ts — EXISTS (Wave-0 correction: extend, do not create).
  </read_first>
  <action>
    1. wizardErrors.ts: add the four codes to the WizardErrorCode union. Add four registry entries
       imitating the KEY_MT5_WRONG_SERVER shape. Copy requirements: user-actionable, zero internal
       vocabulary (no env-var names, no flag names, no seam/service words — the scrubSeamError
       discipline applies to anything derived from a seam error; these are static strings, so just
       write them clean):
       - KEY_MISSING_REQUIRED_FIELD: a required field is empty or the request was malformed; fix =
         fill every field shown for this exchange and resubmit.
       - KEY_UNSUPPORTED_VENUE: this exchange is not supported; fix = pick one of the listed exchanges.
       - KEY_VENUE_NOT_ENABLED: this exchange is not yet available on Quantalyze; fix = contact/try
         later ("request_call" action fits).
       - KEY_INPUT_TOO_LONG: a field exceeds its maximum length; fix = re-copy the value without
         extra characters.
    2. The one-line honesty fix: in KEY_INVALID_FORMAT's cause, replace "Client-side format check
       failed" with wording true for the one remaining emitter (a server-side format check of the
       API secret) — keep the Binance/OKX/Bybit format guidance, which becomes true again once only
       genuine format failures carry the code.
    3. Add all four codes to BOTH rosters: KNOWN_CREATE_WITH_KEY_CODES and KNOWN_ADD_KEY_CODES
       (both routes emit all four). Keep the rosters separate.
    4. wizardErrors.test.ts: extend — (a) KEY_INVALID_FORMAT.cause does NOT contain "Client-side"
       (exact string assert); (b) each new code resolves to an entry with non-empty title, cause,
       and ≥1 fix step; (c) no new entry's title/cause/fix contains any of a hand-typed
       internal-vocabulary denylist (e.g. "MT5_ENABLED", "flag", "env", "seam", "gate") — literals
       in the test.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The union and both rosters each gain exactly the four LOCKED code names (grep each file for each literal).
    - `grep -c "Client-side" src/lib/wizardErrors.ts` = 0.
    - wizardErrors.test.ts green with the three new assertion groups; typecheck clean.
    - `git diff` on ConnectKeyStep/MultiKeyConnectStep touches ONLY the roster arrays (no render, no config changes — MT5-03 already landed in plan 02).
  </acceptance_criteria>
  <done>New codes exist in all three registries with honest copy before any route emits them.</done>
</task>

<task type="auto">
  <name>Task 2: Split the 24 emitting sites — code literal only, everything else byte-identical</name>
  <files>src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/composite/add-key/route.ts, src/app/api/strategies/composite/add-key/route.test.ts</files>
  <read_first>
    - node_modules/next/dist/docs/ — the route-handlers guide (AGENTS.md: this Next.js differs from
      training data; the edits reuse existing in-file idioms, but read before asserting any API).
    - src/app/api/strategies/create-with-key/route.ts — the 12 emitting sites.
    - src/app/api/strategies/composite/add-key/route.ts — the mirror twelve at :87, :103, :134,
      :148, :161, :179, :186, :200, :207, :214, :220, :226 (guard-for-guard identical).
    - Both route.test.ts files — existing rejection-case structure to extend.
  </read_first>
  <action>
    Apply this LOCKED site→code map to create-with-key (and its structural mirror in add-key —
    same guard, same new code):
    | Line (HEAD) | Guard | New code |
    |---|---|---|
    | :72 | !body / not object | KEY_MISSING_REQUIRED_FIELD |
    | :88 | !isSupportedExchange | KEY_UNSUPPORTED_VENUE |
    | :120 | api_key missing/blank | KEY_MISSING_REQUIRED_FIELD |
    | :133 | sFOX server gate | KEY_VENUE_NOT_ENABLED |
    | :149 | MT5 server gate (unreachable post-D-01 — split anyway) | KEY_VENUE_NOT_ENABLED |
    | :168 | api_secret missing (sfox/mt5 arm) | KEY_MISSING_REQUIRED_FIELD |
    | :175 | api_secret.length < 8 (ccxt arm) | **KEY_INVALID_FORMAT — UNCHANGED, the one genuine format failure** |
    | :189 | OKX passphrase missing | KEY_MISSING_REQUIRED_FIELD |
    | :196 | wizard_session_id !isUuid | KEY_MISSING_REQUIRED_FIELD |
    | :203 :209 :215 | three length caps | KEY_INPUT_TOO_LONG |
    Rules: ONLY the `code:` literal changes. The `error` string, the status, and the guard
    expression stay byte-identical at every site. The two comment-prose mentions per file
    (create-with-key :98,:112; add-key :113,:126 — the MT5 short-login carve-out) may be updated for
    accuracy but are NOT emitters and must not be counted as such.
    Tests (extend both route.test.ts):
    - One case per emitting site asserting the exact (status, code) pair — 12 per route. For sites
      with existing cases, update the expected code; for uncovered sites, add the case. The add-key
      MT5 arm (:161) is tested at route level with an explicit comment that it is UI-unreachable.
    - A negative pin: the :175/:186 short-secret case still returns KEY_INVALID_FORMAT.
    **SC-8 + SC-9 mutation runs (record in SUMMARY):** SC-8 — revert ONE non-format site in
    create-with-key (e.g. :203 length cap) to KEY_INVALID_FORMAT → its route test MUST go RED →
    revert. SC-9 — revert a DIFFERENT class member in add-key (e.g. the unsupported-venue site) →
    its route test MUST go RED → revert. Second-member-of-class on purpose. Paste both.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/composite/add-key/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'code: "KEY_INVALID_FORMAT"' src/app/api/strategies/create-with-key/route.ts` = 1, and same = 1 for composite/add-key/route.ts.
    - `git diff` on both route files shows hunks touching ONLY `code:` literals (and optionally comment prose) — zero guard-expression, error-string, or status changes.
    - 12 (status, code) route-test cases per route pass; the short-secret negative pin passes.
    - SC-8 and SC-9 Observed with pasted failing assertions in SUMMARY; tree restored.
    - `npm run typecheck && npm run lint` clean.
  </acceptance_criteria>
  <done>24 honest emitters, one true KEY_INVALID_FORMAT per route, zero validation-behaviour change.</done>
</task>

<task type="auto">
  <name>Task 3: wizardErrors.invariant.test.ts — registry-drift pin (SC-10)</name>
  <files>src/lib/wizardErrors.invariant.test.ts</files>
  <read_first>
    - src/lib/seam-venue-vocabulary.invariant.test.ts — the analog idiom: @vitest-environment node,
      stripCommentsPreserveLines from ./source-scan (comment-strip BEFORE counting — the "28 sites"
      error was exactly a raw grep counting comment prose), predicate-in-prose, hand-typed roster
      never [...derived], anti-vacuity floor (~60% of measured population), SELF-TESTs.
    - src/lib/source-scan.ts
    - Both route files + wizardErrors.ts + both roster arrays (post Task 1/2 state)
  </read_first>
  <action>
    Create src/lib/wizardErrors.invariant.test.ts, all expectations hand-typed literals:
    1. Derive from disk (comment-stripped) the set of `code: "KEY_*"` literals emitted by each of
       the two wizard routes. Assert each emitted code is a member of (a) the WizardErrorCode union
       source and (b) the emitting route's roster (create-with-key → KNOWN_CREATE_WITH_KEY_CODES;
       add-key → KNOWN_ADD_KEY_CODES) — derived-actual vs hand-typed-expected direction.
    2. Pin the literal counts (Oracle Independence: a size pinned as derived.size cannot fail):
       create-with-key rejection-emitting sites = 12; add-key = 12; KEY_INVALID_FORMAT emitters =
       exactly 1 per route. State the counting predicate in prose so the numbers are reproducible.
    3. Anti-vacuity floor: hand-typed DERIVED_FLOOR (≈60% of 24) with the reason in a comment; the
       derivation length must exceed it.
    4. SELF-TESTs through the same scanner: a code in real syntax IS found; a code inside a comment
       is NOT (the 14-vs-12 lesson, cite it in the comment).
    5. **SC-10 mutation run (record in SUMMARY):** temporarily add a new code to the WizardErrorCode
       union AND emit it from one route site, but omit it from that route's roster → this test MUST
       go RED → revert. Paste evidence.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.invariant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Counts pinned as the literals 12, 12, and 1-per-route — never 14, never len()/size of the derivation.
    - Comment-strip applied before counting; SELF-TEST proves a comment mention is not counted.
    - Union-membership AND roster-membership both asserted per emitted code.
    - SC-10 Observed with pasted failing assertion; tree restored; full TS suite green at plan end.
  </acceptance_criteria>
  <done>A code added to one registry but not the others reddens CI; the counts assert reality, not the research's fiction.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client submit → route validation | untrusted wizard input classified by server guards |
| route code → user-facing copy | rejection causes rendered to end users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.2-20 | Tampering | validation LOGIC changed under cover of the copy fix (V5) | mitigate | byte-identical guard/status/error-string rule enforced by acceptance-criteria diff review; only `code:` literals change |
| T-142.2-21 | Information disclosure | new copy leaking internal vocabulary (V7) | mitigate | static-string denylist assert in wizardErrors.test.ts; copy-by-code only (server strings never rendered, per D-05) |
| T-142.2-22 | Repudiation | a rejection cause reverting to the dishonest bucket | mitigate | per-site (status, code) route tests; SC-8/SC-9 second-member mutations observed |
| T-142.2-SC | Tampering | package installs | accept | no installs in this plan |
</threat_model>

<verification>
- `npm run typecheck && npm run lint && npm run test` green; coverage thresholds clear.
- SC-8, SC-9, SC-10 Observed with pasted evidence in SUMMARY.
- Node-22 reproduction rule applies to any CI-only vitest failure.
</verification>

<success_criteria>
- Every wizard rejection tells the truth; the honest-code class is closed across both routes; registry drift is red-detectable.
</success_criteria>

<output>
Create `.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-07-SUMMARY.md` when done.
</output>
