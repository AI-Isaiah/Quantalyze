---
phase: 142.2
plan: 08
type: execute
wave: 3
depends_on: ["142.2-02", "142.2-03", "142.2-04", "142.2-05", "142.2-06", "142.2-07"]
files_modified:
  - TODOS.md
  - .planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md
autonomous: false
requirements: [MT5-01, MT5-02, MT5-05]

must_haves:
  truths:
    - "MT5-01 and MT5-02 are recorded as ALREADY DELIVERED (2026-08-03 Vercel env flip + redeploy, verified by the /security mt5-readonly curl) — NO task in this phase implements them; they are carried here so requirement coverage stays honest"
    - "Every deferred item lands in TODOS.md and NOWHERE else — the single ground-truth backlog"
    - "The Falsifiability Ledger SC-1..SC-10 is fully transcribed: every row Observed with pasted evidence from the plan SUMMARYs, or explicitly skipped-with-reason — never '⬜ pending' reported as closed (the 142 lesson)"
    - "MT5-05 is asserted as an OUTCOME by a human run on production — a green suite is explicitly not evidence (the v1.15 failure mode); and no artifact of this phase claims MT5's NUMBERS are verified (that is 142.3's gate)"
  artifacts:
    - path: "TODOS.md"
      provides: "All 142.2 deferrals + closure of the founder-observed MT5 wizard entries this phase fixed"
      contains: "DEF-142.2"
    - path: ".planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md"
      provides: "Ledger backfilled, Oracle Independence checklist closed, frontmatter nyquist_compliant: true"
      contains: "Observed"
  key_links:
    - from: "142.2-0*-SUMMARY.md falsifiability evidence"
      to: "142.2-VALIDATION.md Falsifiability Ledger"
      via: "transcription with pasted assertions"
      pattern: "SC-"
---

<objective>
Close the phase honestly: consolidate deferrals into TODOS.md, transcribe the falsifiability
evidence, record the two already-delivered requirements, and run the MT5-05 human outcome check.

autonomous: false — Task 3 is a human-verify checkpoint, and this plan runs orchestrator-side after
all wave-2 plans merge.

⛔ 142.2 closing means MT5 is REACHABLE, not correct. Nothing written by this plan may claim,
assert, or imply that MT5's rendered performance has been verified — proving the numbers is Phase
142.3 (MT5-06..10, decisions D-07..D-11 preserved [informational] in 142.2-CONTEXT.md as the
142.3 seed). Do not archive or advertise.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-CONTEXT.md
@TODOS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TODOS.md — log every deferral, close what this phase fixed</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md — § "MT5 wizard — founder-observed on live UI (added 2026-08-02)" (both defects; this
      phase resolves them) and § "Money-path correctness" (existing backbone-bypass entries — do not
      duplicate them).
    - 142.2-04-SUMMARY.md — the census numbers (deribit twr_chain_broken count; NULL-verdict
      draft/composite populations).
  </read_first>
  <action>
    Add, as DEF-142.2-NN entries (new deferred items go to TODOS.md ONLY — no new tracker files):
    1. **Broker-server typeahead (D-04)** — deferred for lack of ANY data source: zero repo hits for
       broker_server/server_name lists, no public canonical registry. Name the three candidate
       sources considered and rejected: curated static list (rots silently; field must stay
       free-text anyway), learn-from-successful-connections (empty until MT5 has users; leaks one
       user's broker choice to others), public registry (does not exist). NOT a simple UI task.
    2. **KEY_INVALID_FORMAT split, remaining routes (D-06)** — 9 emitting sites:
       keys/validate-and-encrypt/route.ts (4) + verify-strategy/route.ts (5). (Not 11 — the old
       count included comment prose.) Same defect class, different callers and copy contracts.
    3. **Destructive remedy on gate refusal** — GATE_INSUFFICIENT_TRADES offers "try another key";
       onTryAnotherKey fires handleDeleteDraft(), destroying the draft and every strategy_keys
       member. MT5-12 removed the UNWINNABLE case; the destructive remedy itself remains (RESEARCH
       security table, DoS user-inflicted).
    4. **ccxt data-driven verdict refinement** — stamping ledger_complete when realized records
       provably span the series would newly ADMIT keyed perps; blocked on fixing the known
       silent-truncation inputs first (OKX bills paginator; bybit funding cursor).
    5. **`_LEDGER_BACKED_SOURCES` → adapter.fetches_fills property** — optional follow-up removing
       the last venue literal from ingestion; NOT required by the MT5-12 invariant (it answers an
       adapter-capability question, not a trust question).
    6. **database.types.ts is drifting** — three pre-existing missing columns
       (computing_started_at, computation_warned, metrics_json_by_basis) and NO regeneration script
       in package.json; series_completeness was hand-patched in 142.2-06.
    7. **Deribit twr_chain_broken tightening** — decision deferred to founder WITH the census number
       from 142.2-04; this phase shipped behaviour-preserving ledger_complete. Include the remedy
       rule: if tightened, affected series get a RE-DERIVE, never a backfill.
    8. **Renaming csv_daily_returns** — cosmetic relative to the invariant (RESEARCH deferred list).
    9. If the 142.2-04 census found a non-trivial NULL-verdict pending population: an entry naming
       the re-derive/re-stitch remedy and the counts.
    Then UPDATE § "MT5 wizard — founder-observed on live UI": defect 1 (client-on/server-off) —
    resolved by MT5-01 (env flip + redeploy, 2026-08-03); defect 2 — code split shipped in 142.2-07;
    record that the "surface the server's error string" half was REJECTED by founder decision D-05
    (copy-by-code only) so it is not re-opened as an open item. Delete or annotate per the
    reverify-CLOSED-then-delete discipline — leave no entry that claims an open defect this phase closed.
  </action>
  <verify>
    <automated>grep -c "DEF-142.2" TODOS.md</automated>
  </verify>
  <acceptance_criteria>
    - ≥8 DEF-142.2 entries exist covering the list above; each names its source decision (D-04, D-06, Pitfall 7, ...).
    - The founder-observed MT5 wizard section no longer presents either defect as open; D-05's rejection of string-passthrough is recorded there.
    - No new tracker file created anywhere (`git status` shows only TODOS.md + planning docs).
  </acceptance_criteria>
  <done>One backlog, complete deferral record, closed items closed.</done>
</task>

<task type="auto">
  <name>Task 2: Backfill the Falsifiability Ledger + record MT5-01/02 + the honesty boundary</name>
  <files>.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md</files>
  <read_first>
    - .planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md
      — the SC-1..SC-10 ledger, Oracle Independence checklist, Sign-Off, frontmatter.
    - All seven prior SUMMARYs (142.2-01 .. 142.2-07) — the "Falsifiability evidence: SC-x" blocks.
  </read_first>
  <action>
    1. Transcribe each SC row from the SUMMARYs: mutation run, the pasted failing assertion, plan
       and date. Owner map: SC-1 → plans 03 (py) + 06 (ts); SC-2/SC-3/SC-4 → 06; SC-5/SC-6 → 05;
       SC-7 → 02; SC-8/SC-9/SC-10 → 07. Any row without pasted evidence: RUN IT NOW (the mutations
       are all local + revert) or mark skipped-with-reason — never "covered".
    2. Correct the two known count fictions in the Per-Task Verification Map: "each of the 14
       causes" rows → 12 (the PATTERNS-verified emitting-site count).
    3. Complete the Oracle Independence checklist (each box with a one-line pointer to the evidence)
       and the Validation Sign-Off, including the A1/A2/A4 confirmation line (A1/A2 from
       142.2-04-SUMMARY; A4 resolved as FALSE→three producers, covered by 142.2-05).
    4. Frontmatter: `status: executed`, `nyquist_compliant: true`, `wave_0_complete: true` — only if
       every Wave-0 item is confirmed delivered (strategyGate.invariant.test.ts,
       wizardErrors.invariant.test.ts, the economic fixture, the SQL gate; wizardErrors.test.ts and
       test_analytics_runner.py resolved to EXTEND per PATTERNS).
    5. Append a "Phase boundary" note: MT5-01/02 delivered 2026-08-03 ahead of planning (env flip +
       redeploy; verified via the /security mt5-readonly curl — no file in this phase implements
       them); MT5's rendered NUMBERS remain UNVERIFIED pending Phase 142.3; deploy order
       migration → worker → frontend with the self-healing NULL window named.
  </action>
  <verify>
    <automated>grep -c "⬜ pending" ".planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md" | grep -qx "0" && grep -q "nyquist_compliant: true" ".planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-VALIDATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - Zero `⬜ pending` rows remain; every SC row shows Observed-with-evidence or skipped-with-reason (pasted assertion text, not "the test covers it").
    - The "14 causes" rows read 12.
    - The phase-boundary note contains the sentence pattern "reachable, not correct" (or equivalent) and names 142.3 as the verification gate.
    - Frontmatter flags set; the file parses (frontmatter intact).
  </acceptance_criteria>
  <done>The phase's own proof is complete and honest — the 142 ledger failure mode cannot recur here.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: MT5-05 — founder completes the connect flow without internal knowledge</name>
  <files>(none — human verification on production)</files>
  <action>Present the verification steps below to the founder and wait. Do not perform any automated work in this task; everything automatable was done in plans 02–07.</action>
  <verify>Founder's resume signal ("approved" or a described failure point). If the phase executes before ship, persist this checkpoint to 142.2-HUMAN-UAT.md as the open UAT item instead of approving it untested.</verify>
  <done>The founder reached a rendered MT5 strategy on production without needing an internal error code, a server name, or a flag — or every failure point is logged and fixed in-phase.</done>
  <what-built>
    Plans 02–07 made MT5 reachable and honest: broker-server field legible, rejection copy
    cause-specific, and the gate reading the canonical series' own verdict — so a 135-day MT5
    account passes on its daily series and a fills-gapped perp still refuses.
  </what-built>
  <how-to-verify>
    ⚠️ This runs on PRODUCTION AFTER ship + land-and-deploy (deploy order: migration auto-applies on
    merge → Railway worker → Vercel). It cannot run locally: the MT5 gateway is Railway-internal and
    the TEST DB has no worker. If executing the phase before ship, record this checkpoint as the
    phase's open UAT item (the 142.1-HUMAN-UAT pattern) rather than approving it untested.
    1. On quantalyze.xyz, open the wizard → pick MT5. The Broker-server field must be legible plain
       text as you type (not dots); helper copy unchanged.
    2. Submit the real MT5 account (login / investor password / broker server). If anything rejects,
       the message must name the actual cause in plain words — any moment where you must ask what a
       code means, what a server name is, or which flag is set is a FAILURE: log it and it is fixed
       in-phase.
    3. Wait for sync (~10–15 min for a fresh key), reach the preview, pass the gate, and see a
       rendered strategy.
    4. Sanity-negative: an OKX connect attempt still shows a masked passphrase field.
    ⛔ Reaching a rendered strategy proves REACHABILITY only. Do NOT record the numbers as correct —
    that is Phase 142.3 against the terminal on a trading day.
  </how-to-verify>
  <resume-signal>Type "approved" if the flow completed without needing internal knowledge, or describe exactly where it demanded a code/server/flag</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase records → future planning | ledger/backlog claims steer later phases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.2-23 | Repudiation | "verified" claims without run evidence (the 142 ledger failure) | mitigate | transcription requires pasted assertions; unrun rows are run now or marked skipped |
| T-142.2-24 | Repudiation | 142.2 close implying MT5 numbers are correct (the v1.15 failure shape) | mitigate | the reachable-not-correct boundary written into VALIDATION.md and the checkpoint text |
| T-142.2-SC | Tampering | package installs | accept | no installs in this plan |
</threat_model>

<verification>
- Full gates before close: `npm run typecheck && npm run lint && npm run test` AND
  `cd analytics-service && pytest && mypy --strict .` — all green.
- TODOS.md is the only backlog touched; VALIDATION.md ledger complete.
</verification>

<success_criteria>
- Requirements MT5-01/02 honestly recorded as pre-delivered, MT5-05 human-verified (or persisted as the phase's open UAT item), every deferral logged once, every falsifiability row closed.
</success_criteria>

<output>
Create `.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-08-SUMMARY.md` when done.
</output>
