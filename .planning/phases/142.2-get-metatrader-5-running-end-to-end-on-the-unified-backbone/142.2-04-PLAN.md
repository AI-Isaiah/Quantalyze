---
phase: 142.2
plan: 04
type: execute
wave: 2
depends_on: ["142.2-01"]
files_modified: []
autonomous: false
requirements: [MT5-12]

must_haves:
  truths:
    - "Migration 20260803150000 is applied to TEST (qmnijlgmdhviwzwfyzlc) via MCP BEFORE any merge to main — and PROD (khslejtfbuezsmvmtsdn) receives zero writes of any kind"
    - "A1 is CONFIRMED by an executed TEST round trip, not inferred from a comment: an upsert that omits series_completeness leaves a previously-stamped value intact"
    - "A2 is CONFIRMED on TEST: the strategy_analytics grants/policies cover the new column for owner reads with zero policy changes"
    - "The deribit twr_chain_broken PROD population (A6) and the NULL-verdict draft/composite population (Pitfall 6 census) are counted read-only and recorded — the tightening decision is made on evidence, not assumption"
  artifacts: []
  key_links: []
---

<objective>
Orchestrator-only verification gate (autonomous: false — MCP tools are stripped from subagents,
upstream anthropics/claude-code#13898; this plan runs in the orchestrator session, mirroring
142.1-07). Convert RESEARCH assumptions A1, A2, A6 from beliefs into executed evidence, apply the
migration to TEST, and run the read-only PROD censuses that gate the Pitfall-7 decision and size
the NULL-verdict population.

Purpose: A1 is the load-bearing assumption of the whole carrier design — if PostgREST's upsert did
NOT preserve omitted columns, the analytics job would wipe the verdict and the gate would refuse
everything. A2 false would make the wizard read null and refuse everything. Both must be executed
"early" (phase context) — this plan runs at the top of wave 2, before the consumer work merges.
Output: evidence recorded in this plan's SUMMARY; census numbers for plan 142.2-08's TODOS entries.

⛔ PROD `khslejtfbuezsmvmtsdn` is READ-ONLY. SELECT only. Never write, never migrate.
⚠️ If A1 or A2 FAILS: STOP and surface to the phase orchestrator before plans 05/06 merge — the
carrier design (survival-by-omission) would need an explicit lift instead, which changes plan 05's
analytics_runner task. Fail loud; do not patch around it silently.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-RESEARCH.md
@supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply migration to TEST and run the SQL gate there</name>
  <files>(none — TEST DB state only)</files>
  <read_first>
    - supabase/migrations/20260803150000_strategy_analytics_series_completeness.sql (plan 01 output)
    - supabase/tests/test_strategy_analytics_series_completeness.sql (plan 01 output)
  </read_first>
  <action>
    Via Supabase MCP against project qmnijlgmdhviwzwfyzlc ONLY:
    1. Apply the migration body with `apply_migration`, name `strategy_analytics_series_completeness`.
       (Known drift: MCP apply stamps now() into the migrations ledger — acceptable on TEST; noted,
       not fixed here.)
    2. Confirm the migration's own self-verification DO block raised nothing.
    3. Execute the full body of supabase/tests/test_strategy_analytics_series_completeness.sql via
       execute_sql and confirm every 'Assertion N OK' NOTICE fires with zero exceptions (the
       lag-skip must NOT fire — the column now exists).
  </action>
  <verify>
    <automated>MISSING — MCP-only task; evidence = pasted execute_sql output in SUMMARY (information_schema row showing data_type=text, is_nullable=YES, column_default NULL)</automated>
  </verify>
  <acceptance_criteria>
    - `select data_type, is_nullable, column_default from information_schema.columns where table_schema='public' and table_name='strategy_analytics' and column_name='series_completeness'` on TEST returns exactly one row: text / YES / NULL — pasted into SUMMARY.
    - The SQL gate ran on TEST with all assertions OK and no SKIP.
    - Zero statements of any kind were executed against khslejtfbuezsmvmtsdn in this task.
  </acceptance_criteria>
  <done>TEST carries the column with the pinned shape; gate green against real schema.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the A1 upsert-survival round trip and the A2 RLS/grant check on TEST</name>
  <files>(none — TEST DB state only)</files>
  <read_first>
    - analytics-service/services/analytics_runner.py:1516-1530 — the metrics_json_by_basis
      survival-by-omission comment (the in-repo precedent A1 rides on) and the _clear_stale_by_basis
      counter-example (omission is a mechanism, not a guarantee).
  </read_first>
  <action>
    On TEST (qmnijlgmdhviwzwfyzlc):
    1. **A1 round trip.** Pick (or insert inside a transaction) a throwaway strategy_analytics row.
       UPDATE its series_completeness to 'ledger_complete'. Then perform an upsert that mimics
       `_mark_complete`'s shape WITHOUT the column. Preferred: a real PostgREST call against the
       TEST REST endpoint (test-role credentials are in macOS Keychain, service `quantalyze-test`,
       per the standing test-credentials note) — `POST /rest/v1/strategy_analytics` with
       `Prefer: resolution=merge-duplicates` and a payload of only {strategy_id, computation_status}.
       Fallback if REST credentials are unavailable: `INSERT ... ON CONFLICT (strategy_id) DO UPDATE
       SET computation_status = excluded.computation_status` via execute_sql, and record HONESTLY in
       the SUMMARY that this verifies the SQL half while the PostgREST payload-projection half rests
       on the executed pytest payload-scan (plan 05) plus the two in-repo comments. Then SELECT the
       row: series_completeness MUST still read 'ledger_complete'. Clean up the throwaway row.
    2. **A2 grant/policy check.** Run: (a) `select policyname, cmd, qual from pg_policies where
       tablename='strategy_analytics'` — record the set; (b) `select grantee, privilege_type from
       information_schema.role_table_grants where table_name='strategy_analytics'` and
       `select count(*) from information_schema.column_privileges where table_name='strategy_analytics'`
       — PG RLS is row-level, so a table-level SELECT grant with ZERO column-level carve-outs means
       the new column is covered with no policy change. If column-level grants exist, STOP and
       surface (the wizard's browser read would get null → every strategy refused).
  </action>
  <verify>
    <automated>MISSING — MCP-only task; evidence = pasted round-trip SELECT output + grant/policy listings in SUMMARY</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY shows the three-step A1 sequence with pasted outputs: stamped value → omitting upsert → value INTACT. If the REST path was used, the HTTP status and the follow-up SELECT are both pasted; if the SQL fallback was used, the SUMMARY says so in those words.
    - SUMMARY shows zero column-level privileges on strategy_analytics and the unchanged policy list — or a loud STOP escalation.
    - The throwaway row is deleted (TEST left clean).
  </acceptance_criteria>
  <done>A1 and A2 are executed evidence, or the phase has stopped loudly before building on them.</done>
</task>

<task type="auto">
  <name>Task 3: Read-only PROD censuses (A6 + Pitfall-6 population sizing)</name>
  <files>(none — SELECT-only against PROD)</files>
  <read_first>
    - .planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-RESEARCH.md
      § Pitfall 6 (the gate runs at exactly two moments — wizard SyncPreviewStep and admin approve;
      NULL cannot un-publish anything) and § Pitfall 7 (deribit tightening is census-gated).
  </read_first>
  <action>
    Against khslejtfbuezsmvmtsdn, SELECT only:
    1. **A6 census:** count strategy_analytics rows whose data_quality_flags carries
       twr_chain_broken, joined to strategies/api_keys where the venue is deribit; record total and
       how many belong to published vs draft strategies.
    2. **Pitfall-6 population:** count (a) non-composite draft strategies awaiting approval whose
       admissibility would now read a NULL verdict (strategies not yet published, with
       csv_daily_returns rows present); (b) composite strategies in the same pending state (these
       need a re-stitch to receive composite_stitched after deploy). Record both counts.
    3. Record the interpretation in the SUMMARY: NULL cannot un-publish anything already published;
       the affected population self-heals via re-derive/re-stitch. If either count is non-trivial,
       the remedy is a RE-DERIVE/RE-STITCH (which examines the inputs), never a backfill UPDATE —
       flag it for the founder in plan 142.2-08's TODOS entry rather than acting here.
  </action>
  <verify>
    <automated>MISSING — MCP-only task; evidence = pasted census counts in SUMMARY</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY carries all three counts with the SQL used (verifiably SELECT-only — no INSERT/UPDATE/DELETE/DDL token anywhere in the pasted statements).
    - The deribit tightening decision input is recorded: census number + the behaviour-preserving default shipped in plan 03 + "tightening deferred to founder" (feeds 142.2-08's TODOS entry).
  </acceptance_criteria>
  <done>Every assumption that gates a behaviour decision has a number attached, gathered without touching PROD state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP session → TEST/PROD databases | direct SQL execution against live projects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142.2-09 | Tampering/DoS | accidental write/migration against PROD | mitigate | PROD tasks are SELECT-only by acceptance criterion; migration applied to TEST project id only; plan is orchestrator-run, autonomous:false |
| T-142.2-10 | Repudiation | assumptions recorded as "verified" without execution | mitigate | every criterion demands pasted query output; the SQL-fallback arm must self-identify as partial |
| T-142.2-SC | Tampering | package installs | accept | no installs in this plan |
</threat_model>

<verification>
- All evidence pasted in SUMMARY; TEST clean of throwaway rows; zero PROD writes.
- If A1/A2 failed: plans 05/06 are HALTED pending replan — say so loudly in the SUMMARY and to the user.
</verification>

<success_criteria>
- Migration live on TEST; A1/A2 executed evidence; A6 + Pitfall-6 populations counted; no PROD writes.
</success_criteria>

<output>
Create `.planning/phases/142.2-get-metatrader-5-running-end-to-end-on-the-unified-backbone/142.2-04-SUMMARY.md` when done.
</output>
