---
phase: 146.2
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260819151000_csv_finalize_fold_guard1_null_safe.sql
  - supabase/tests/test_csv_finalize_atomic_fold.sql
  - supabase/tests/test_csv_finalize_double_submit.sql
  - supabase/schema/functions/finalize_csv_strategy_with_returns.sql
autonomous: false
requirements: [R5, W2, W3]
estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A NULL p_terminal_status is refused with SQLSTATE 22023 by GUARD 1 — the ERRCODE map's '(incl. NULL)' promise becomes true (R5)"
    - "The fold's no-handler invariant is pinned by a STANDING CI gate: a future CREATE OR REPLACE reintroducing an EXCEPTION handler reddens the recurring fold gate, not just migration-apply (W3)"
    - "service_role EXECUTE revocation is pinned in the recurring gate beside the existing anon check (carried 146.1 finding)"
    - "The double-submit gate's Part 3 header no longer claims count-based coverage of the catch-write-re-raise variant; it scopes to the swallow variant and points the re-raise variant at the prosrc pin (W2)"
  artifacts:
    - path: supabase/migrations/20260819151000_csv_finalize_fold_guard1_null_safe.sql
      provides: "CREATE OR REPLACE re-based on 20260819130000's body, GUARD 1 gains the IS NULL arm, no handler clause, COMMENT map + self-verify re-issued"
    - path: supabase/tests/test_csv_finalize_atomic_fold.sql
      provides: "NULL-terminal-status probe CALL expecting 22023; Part 1 prosrc no-handler assertion + service_role privilege pin"
    - path: supabase/schema/functions/finalize_csv_strategy_with_returns.sql
      provides: "regenerated snapshot (sql-function-snapshot CI reds without it)"
  key_links:
    - from: "GUARD 1 in the new migration"
      to: "GUARD 4/5's established NULL-safe shape in 20260819130000:285-296"
      via: "identical IS NULL OR + distinguishing-substring + 22023 pattern"
      pattern: "IS NULL OR"
    - from: "fold gate Part 1"
      to: "pg_proc prosrc of the deployed fold"
      via: "comment-stripped pg_get_functiondef assertion — the house prosrc-gate pattern (comments are stored verbatim; stripping is load-bearing)"
      pattern: "pg_get_functiondef"
---

<objective>
Close R5, W3, W2 — the fold-function cluster, one migration plus its recurring gates.

**R5:** GUARD 1 (`20260819130000_csv_finalize_fold_input_guards.sql:243-247`) is NULL-unsafe
inside the anti-NULL migration: NULL NOT IN (...) evaluates NULL, plpgsql takes the ELSE, the
guard passes, and the failure surfaces as 23502 from the strategies.status NOT NULL constraint —
while the re-issued COMMENT ON FUNCTION ERRCODE map promises 22023 "(incl. NULL)". GUARD 4
(:285) and GUARD 5 (:293) both carry the IS NULL OR arm; GUARD 1 is the omission. Direct-RPC
boundary class (the route cannot send NULL — it validates entry_context and passes literals).

**W3:** the fold's no-handler invariant — called THE mechanism of the fold migration — is pinned
only at migration-apply time. A future CREATE OR REPLACE adding an EXCEPTION handler applies
cleanly with no standing CI gate reddening. The house pattern exists (10+ SQL gates carry prosrc
assertions); one comment-stripped assertion in the recurring fold gate's Part 1 fixes it. Same
location gains the service_role EXECUTE pin (carried 146.1 finding: the REVOKE is asserted only
at apply time; the recurring gate checks anon but not service_role).

**W2:** `test_csv_finalize_double_submit.sql:224-228` over-claims — a catch-write-and-re-raise
handler is uncatchable by counts (the handler is a subtransaction; a re-raise rolls its writes
back, counts read clean either way). Prose re-cut only, honest per the 146.1-07 precedent, with
the re-raise variant pointed at W3's prosrc pin (which DOES catch any handler clause).

**One-way door:** the migration CREATE-OR-REPLACEs the PROD-resident fold body
(pg_proc) on merge. Explicit checkpoint before the writing task, per orchestrator directive.

Output: migration + regenerated snapshot + armed gates, REDs observed on throwaway PG16.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-CONTEXT.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-RESEARCH.md

Read before coding (PATTERNS.md section 3 quotes the guard + prosrc shapes):
- supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql — FULL read: it is the re-base source (GUARD block :240-300, COMMENT map :446-455, self-verify :520-560 incl. the comment-strip regexp at :545 and the proconfig search_path read, the service_role check at :531-534)
- supabase/tests/test_csv_finalize_atomic_fold.sql:90-200 (Part 1 pins + Part 3d probe shape :290-330 — a real CALL, not a grep; Part 2c honesty note :196-200 is W2's precedent)
- supabase/tests/test_wizard_session_idempotency.sql:160-200 (the prosrc comment-strip house pattern and its three-times-hit trap rationale)
- supabase/tests/test_csv_finalize_double_submit.sql:200-240 (W2's over-claiming header)
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Open the one-way door: CREATE OR REPLACE the PROD fold function, re-based on 20260819130000</decision>
  <context>The fold body lives in pg_proc on PROD; merging this migration replaces it with no human in the loop. The blast surface is every CSV finalize on PROD. The mitigation is transcription discipline: the memory rule requires grepping ALL migrations to confirm 20260819130000 is the LATEST definition and re-basing on ITS full body — a re-base onto a stale source has happened before in this repo (the 3f half-state assertion exists because of it). Exactly ONE clause changes (GUARD 1 gains the IS NULL arm). Undo requires another migration.</context>
  <options>
    <option id="proceed">
      <name>Proceed: one-clause CREATE OR REPLACE re-based on 20260819130000</name>
      <pros>Closes the R5 ERRCODE-map lie with the minimum diff; same-merge event as plan 04's migration so plan 08 rehearses/verifies both at once</pros>
      <cons>Any transcription slip ships to PROD's live finalize path — mitigated by the re-issued self-verify, the byte-diff check in the task, and plan 08's TEST rehearsal before merge</cons>
    </option>
    <option id="hold">
      <name>Hold R5 for a later migration batch</name>
      <pros>One fewer PROD function replacement this merge</pros>
      <cons>The shipped COMMENT map keeps promising 22023 for a case that raises 23502; the phase's SC roster carries R5 — holding it re-opens the finding</cons>
    </option>
  </options>
  <resume-signal>Select: proceed or hold</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Migration 20260819151000 — GUARD 1 gains the IS NULL arm; snapshot regenerated in the same commit</name>
  <reversibility rating="one-way">Replaces the PROD-resident fold body (pg_proc) on merge; undo requires another CREATE OR REPLACE migration.</reversibility>
  <files>supabase/migrations/20260819151000_csv_finalize_fold_guard1_null_safe.sql, supabase/schema/functions/finalize_csv_strategy_with_returns.sql</files>
  <action>
    FIRST re-base: grep ALL of supabase/migrations for the fold function name and confirm
    20260819130000 carries the LATEST full definition (memory rule — never re-base on a stale
    source). Create the new migration as a CREATE OR REPLACE carrying that body byte-preserved
    with exactly one change: GUARD 1 adopts GUARD 4's NULL-safe shape (the IS NULL OR arm ahead
    of the NOT IN list), keeping the distinguishing substring and ERRCODE 22023 verbatim so no
    existing gate anchor moves. Absolute constraints: (a) NO handler clause may appear anywhere
    in the body — W3's invariant, and the new prosrc pin in Task 3 will red on one; (b) re-issue
    the COMMENT ON FUNCTION ERRCODE map from 20260819130000 — its "(incl. NULL)" line is now
    TRUE for terminal status, keep the wording aligned; (c) re-issue the full self-verify block
    in the house shape (comment-stripped regexp over the rendered definition, search_path read
    from proconfig not re-rendered text, RAISE on drift) updated to assert the new IS NULL arm
    is present in the deployed body; (d) re-assert the ACL state the parent asserted
    (authenticated EXECUTE present, anon and service_role absent) — CREATE OR REPLACE preserves
    ACLs but the assertion is the house belt-and-braces. Then regenerate the snapshot with the
    repo script npm run schema:functions and commit the migration + snapshot in the SAME commit
    (the path-triggered sql-function-snapshot CI job reds otherwise — STATE item-12 precedent).
    Commit as feat(146.2-05).
  </action>
  <verify>
    <automated>bash -c 'f=supabase/migrations/20260819151000_csv_finalize_fold_guard1_null_safe.sql; test -s "$f" && grep -v "^[[:space:]]*--" "$f" | grep -c "p_terminal_status IS NULL" && git status --porcelain supabase/schema/functions/ | wc -l'</automated>
  </verify>
  <done>Migration exists with the NULL-safe GUARD 1 in non-comment SQL; snapshot regenerated and staged in the same commit; COMMENT map + self-verify re-issued; no handler clause in the body</done>
</task>

<task type="auto">
  <name>Task 3: Arm the recurring gates (R5 probe, W3 prosrc + service_role pins, W2 prose) and observe the REDs on throwaway PG16</name>
  <precondition>docker daemon available (docker info succeeds) — the RED observations need a disposable postgres:16; if unavailable, STOP and surface a checkpoint rather than skipping</precondition>
  <files>supabase/tests/test_csv_finalize_atomic_fold.sql, supabase/tests/test_csv_finalize_double_submit.sql</files>
  <action>
    Three gate edits, then the REDs. (1) R5 probe: add a real probe CALL to
    test_csv_finalize_atomic_fold.sql in the Part-3d shape — invoke the fold with p_terminal_status
    NULL, expect SQLSTATE 22023 and GUARD 1's distinguishing substring; the test comment states
    reachability honestly (the route validates entry_context and passes literals — this is the
    direct-RPC boundary class A1 closes). (2) W3 pins in Part 1: fetch the deployed definition
    via pg_get_functiondef, comment-strip with the house regexp (strip double-dash line comments
    FIRST — pg stores comments verbatim and the fold's own comments discuss handlers at length;
    an unstripped assertion is permanently red, the trap test_wizard_session_idempotency
    documents), then assert the stripped body contains no exception-handler clause using the
    word-bounded case-insensitive regexp from RESEARCH §W3; beside it, pin privileges: the
    has_function_privilege checks asserting service_role has NO EXECUTE (copy the apply-time
    check from 20260819130000:531-534) next to the existing anon pin. Failure style: RAISE
    EXCEPTION with TEST FAILED prefix and remedy prose, per the 3f analog. (3) W2: re-cut the
    Part 3 header prose in test_csv_finalize_double_submit.sql — scope the count-based claim to
    the swallow variant only, and point the catch-write-re-raise variant at the fold gate's new
    prosrc pin; no behavior change, mirror the 146.1-07 honesty-note register. THEN the REDs on
    a disposable postgres:16 container (plan-unique container name and scratch filenames): apply
    the fold from the NEW migration, run the gate green; (red 1) swap in the PRE-fix function
    from 20260819130000 and observe the NULL probe answer 23502 → gate red; (red 2) CREATE OR
    REPLACE the fold with a trivial appended handler that only re-raises → prosrc pin red;
    (red 3) GRANT service_role EXECUTE → privilege pin red. Restore the correct state after each,
    final full-gate green run, tear down. Record all three verbatim. Commit as test(146.2-05).
  </action>
  <verify>
    <automated>bash -c 'grep -c "pg_get_functiondef" supabase/tests/test_csv_finalize_atomic_fold.sql && grep -c "has_function_privilege" supabase/tests/test_csv_finalize_atomic_fold.sql'</automated>
  </verify>
  <done>Probe + both pins armed in the recurring gate; W2 prose re-cut; three REDs observed on PG16 and recorded verbatim in the SUMMARY; final green run on the post-migration fold; container removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| direct RPC caller → SECURITY DEFINER fold | non-route callers can send arbitrary arguments incl. NULL |
| migration merge → PROD pg_proc | the fold body is replaced on merge with no human in the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.2-09 | Elevation of Privilege | SECDEF fold ACLs | high | mitigate | service_role + anon no-EXECUTE now pinned in the RECURRING gate, not only at apply time |
| T-146.2-10 | Tampering | fold atomicity invariant | high | mitigate | standing prosrc no-handler pin — a future handler clause reddens CI, not just apply |
| T-146.2-11 | Denial of Service (input) | GUARD 1 NULL arm | medium | mitigate | 22023 refusal before any write; probe CALL pins it |
| T-146.2-SC | Tampering | package installs | low | accept | zero package installs in this phase |
</threat_model>

<verification>
- Comment-stripped greps confirm the guard and pins are live SQL, not prose.
- Full fold gate green on PG16 against the post-migration body; three REDs recorded.
- Snapshot staged in the migration commit (sql-function-snapshot CI stays green).
- Shared-TEST-DB trap (RESEARCH Pitfall 5): gates and migration land in ONE PR, so TEST arming happens at plan 08's rehearsal — do not apply anything to TEST from this plan.
</verification>

<success_criteria>
- R5: NULL terminal status answers 22023 with GUARD 1's substring; the ERRCODE map is truthful.
- W3 + carried finding: no-handler and service_role pins live in the recurring gate, each observed RED under its named neuter.
- W2: the double-submit gate's claim is scoped to what counts can actually catch.
- SC#5 satisfied for all three via the recorded PG16 REDs.
</success_criteria>

<output>
Create `.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-05-SUMMARY.md` when done.
Record: the re-base source confirmation (grep evidence), all three verbatim REDs, and the snapshot-regen commit hash.
</output>
