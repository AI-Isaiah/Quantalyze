# Phase 146.2 — R6 + escape live-flow UAT

**Run:** 2026-08-20, localhost:3000 (Next dev) + local analytics-service on :8002,
both against **TEST** (`qmnijlgmdhviwzwfyzlc`). Merged code under test: `0ba62be9`
(v0.68.0.0, PR #694). Driver: Claude (browser automation), authorized by the user
("run the uat test yourself with your access to the localhost via the browser").

This closes **human item 2** of `146.2-VERIFICATION.md` (the R6 + escape
live-flow UAT). Human items 1 (TEST-rehearsal DB claims) and 3 (CI green at
d2b1702b) were closed pre-merge; the merge CI ran green at the PR head.

## Result: PASS — both arms, every scripted assertion

### Arm (a) — killed connection after the fold commits, then resubmit

Method: `window.fetch` patched in-page so the FIRST `csv-finalize` request is
forwarded (fold commits server-side) and then thrown as `TypeError: Failed to
fetch` — the script's "kill the connection after the fold commits", without
touching the server.

- ✓ Failure state: "The server did not respond within 30 seconds. Your file is
  preserved — click Retry to try again." CTA stayed live (no disabled
  "Submitting…" button).
- ✓ Resubmit (same session, identical content) rendered the **amber echo notice
  in-step**: "An earlier attempt in this wizard session had already saved this
  strategy, so nothing new was written. We compared the saved track record's row
  count and its first and last dates against this file — not the individual
  daily values — so open the strategy to check it holds the numbers you meant to
  upload." plus an "Open the strategy →" link.
- ✓ Footer read **[Back] [Continue]** — no disabled button.
- ✓ Continue landed on /my-strategies with **exactly one** "R6 UAT probe bravo"
  row (echo did not duplicate).

### Arm (b) — CSV_SESSION_REUSED refusal → "Start a new strategy" escape

Method: same kill for submit #1 (burn the session server-side while the client
fence stays unaware), then a second in-page patch mutated `strategy_name` in the
outgoing resubmit body (`+" MUTATED"`) — simulating exactly the fence-failure /
divergent-content case the server arm exists for. The client fence itself
correctly prevents reaching this from pure UI (a material change re-mints the
session id), which is why the mutation is the honest way to drive the arm.

- ✓ 409 refusal panel rendered the W-1 sentence with the interpolated escape
  label: "This wizard session already created a strategy with a different track
  record, so we refused before writing anything of this submission. **Start a
  new strategy** to upload a different file."
- ✓ "Start a new strategy" button rendered **below the body, above the
  diagnostics** (correlation_id line).
- ✓ Clicking it cleared the refusal and showed: "This will be a new strategy.
  Press 'Add to my strategies' to upload this file." Submit CTA stayed live.
- ✓ Subsequent submit created a **second** "R6 UAT probe charlie" strategy from
  the file still in hand — **no re-upload**.

### Incidental confirmations

- The pre-fix upstream failure (analytics service down / 401) rendered as
  `CSV_UPSTREAM_FAIL` "This is on our side, not your data. Nothing was saved."
  — NOT as "your CSV is invalid" (the forwarded-4xx classification fix holds).
- A **fresh** wizard session re-uploading the same file creates a second
  strategy with no echo/refusal — correct: the dedupe keys on the wizard
  session, not on content.

## Residue on TEST (left deliberately; all Private, owner-only)

5 probe strategies under the test account: "R6 UAT probe alpha" ×2, "…bravo" ×1,
"…charlie" ×2, all `Private`/SYNCING (their compute jobs sit in TEST's normal
workerless pending backlog). Delete whenever convenient.

## Environment note

The UAT was unblocked by the `fix/local-analytics-never-drives-prod` branch
(analytics-service prefers `.env.qa-local`, prod-guard tripwire) plus starting
the Next dev server with `ANALYTICS_SERVICE_KEY`/`INTERNAL_API_TOKEN` exported
from `analytics-service/.env.qa-local` — the 401 seam mismatch is
`INTERNAL_API_TOKEN` (bearer on `/process-key`), not only `SERVICE_KEY`.
