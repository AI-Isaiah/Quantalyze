---
phase: 146.2
plan: 08
type: execute
wave: 3
depends_on: ["146.2-04", "146.2-05"]
files_modified: []
autonomous: false
requirements: [R3, R5]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Both new migrations were rehearsed on TEST before merge, and both SQL gate files EXECUTED armed-and-green against the post-migration TEST state"
    - "A human approved the merge AFTER seeing the rehearsal + neuter evidence (blocking checkpoint)"
    - "Post-merge PROD state verified: the live cron body carries the ceiling conjunct, one real sweep tick succeeded, the fold's GUARD 1 carries the IS NULL arm, ACLs and search_path intact, suspect_and_visible still 0"
  artifacts:
    - path: .planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-08-SUMMARY.md
      provides: "the rehearsal census, checkpoint record, and PROD verification evidence"
  key_links:
    - from: "TEST rehearsal"
      to: "PROD auto-apply on merge"
      via: "the same two migration files — rehearsal is the only pre-merge observation of their effect on a live database"
      pattern: "20260819150000|20260819151000"
---

<objective>
⛔ **ORCHESTRATOR-ONLY** — the 146.1-08 pattern. This plan CANNOT be delegated to a worktree
agent: it needs the Supabase MCP (TEST + PROD, stripped from subagents) and it gates the merge
itself. Both 146.2 migrations (plan 04's sweep ceiling `20260819150000`, plan 05's fold GUARD-1
fix `20260819151000`) AUTO-APPLY TO PROD when `supabase/migrations/**` merges to main. This plan
is the pre-merge rehearsal, the blocking human gate, and the post-merge PROD verification —
executed once for both migrations, which share the merge event.

Ordering trap (RESEARCH Pitfall 5): applying the migrations to TEST arms the new gate arms
(C5, the NULL probe, the prosrc/service_role pins) — the gates and migrations land in the SAME
PR, so other open PRs' sql-tests run their own older gate files and stay green; but never run
python and e2e-seeded suites concurrently against TEST, and prefer a quiet window.

Purpose: no unrehearsed SQL reaches PROD; the phase's SC#3 and R5 truths are verified on the
live databases, not inferred from green CI.
Output: 146.2-08-SUMMARY.md carrying the full evidence chain.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-04-SUMMARY.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-05-SUMMARY.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEST rehearsal — apply both migrations, execute both gate files armed</name>
  <precondition>Supabase MCP access to the TEST project (orchestrator session only); TEST is quiet (no concurrent python or e2e-seeded suite runs)</precondition>
  <files>none (read/apply against TEST only)</files>
  <action>
    Pre-census TEST (cron.job rows for the sweep, fold proconfig/grants, jobid bookkeeping), then
    apply 20260819150000 and 20260819151000 to TEST in timestamp order. Execute BOTH gate files
    (test_reconcile_dropped_enqueue_sweep.sql, test_csv_finalize_atomic_fold.sql) against the
    post-migration TEST state and confirm every arm — including the NEW ones (C5 + positive
    control, NULL probe, prosrc pin, service_role pin) — runs ARMED and green (a SKIP is not a
    pass; if any arm skips, the state-adaptive guard fired and the rehearsal is invalid —
    investigate before proceeding). Verify each migration's own self-verify block RAISEd nothing
    on apply. If the TEST PostgREST pool wedges (all-day PGRST003/504 with idle Postgres), apply
    the known remedy (terminate the postgrest backends) rather than misreading it as a migration
    failure. Record the full census before/after.
  </action>
  <verify>
    <automated>Both gate files executed on TEST with zero failures and zero skipped arms (transcript captured for the SUMMARY); both self-verify blocks passed on apply</automated>
  </verify>
  <done>TEST carries both migrations; all gate arms armed-green; census recorded</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING pre-merge gate — human reviews the rehearsal + neuter evidence and approves the merge</name>
  <what-built>Both 146.2 migrations rehearsed on TEST with every gate arm armed-green; the plan-04/05 SUMMARYs carry the throwaway-PG16 RED observations (C5 pre-ceiling red, B4-BLANKET reddening C2/C3, NULL probe answering 23502 pre-fix, prosrc-handler red, service_role re-grant red)</what-built>
  <how-to-verify>
    1. Read the rehearsal transcript in this plan's working notes: both migrations applied to TEST cleanly, self-verifies silent, all arms armed (none skipped).
    2. Read the RED roster in 146.2-04-SUMMARY.md and 146.2-05-SUMMARY.md — every named neuter has a verbatim observation.
    3. Confirm the ceiling N in the migration header matches the plan-04 checkpoint choice.
    4. Approve the merge of the phase PR (which carries both migrations — merging auto-applies them to PROD).
  </how-to-verify>
  <resume-signal>Type "approved — merge" to proceed, or name the item that blocks</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Post-merge PROD verification</name>
  <precondition>The phase PR is merged to main and the migration auto-apply has run (Supabase MCP access to PROD, read-only except none — this task performs NO writes)</precondition>
  <files>none (read-only against PROD)</files>
  <action>
    Read-only PROD census, mirroring the 146.1-08 shape: (1) the live cron.job command for
    reconcile_dropped_enqueue_sweep contains the ceiling conjunct with the ratified N as a
    word-bounded literal, and the job re-registered under its expected schedule; (2) at least one
    post-merge cron.job_run_details tick of the sweep with status succeeded; (3)
    pg_get_functiondef of the fold shows GUARD 1 with the IS NULL arm and NO exception-handler
    clause (comment-stripped check), proconfig still pins search_path to public, pg_catalog, and
    has_function_privilege confirms authenticated EXECUTE true / anon false / service_role false;
    (4) re-run the phase's exposure query: suspect_and_visible for csv strategies still 0 (the R1
    fix is forward-only — a non-zero here means the window opened mid-phase and must be surfaced,
    not silently absorbed); (5) do NOT call the fold with NULL on PROD — the prosrc read IS the
    verification, a probe CALL on PROD is a write-path risk for nothing. Record everything in the
    SUMMARY; any mismatch is a FAIL-LOUD stop reported to the developer, never patched inline.
  </action>
  <verify>
    <automated>All five census items recorded with query output in the SUMMARY; zero mismatches (or a fail-loud report)</automated>
  </verify>
  <done>PROD verified carrying both migrations with the promised semantics; exposure still zero; SUMMARY complete</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge event → PROD database | two migrations change live scheduled + SECDEF code with no further human action |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.2-16 | Tampering | PROD migration auto-apply | high | mitigate | TEST rehearsal + blocking human gate before merge + read-only PROD census after |
| T-146.2-SC | Tampering | package installs | low | accept | zero package installs in this phase |
</threat_model>

<verification>
- Every arm armed on TEST (no skips); human approval recorded; five-item PROD census clean.
</verification>

<success_criteria>
- ROADMAP 146.2 SC#3 and the R5 truth verified against the LIVE databases.
- The merge was human-gated with the full evidence chain in view.
</success_criteria>

<output>
Create `.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-08-SUMMARY.md` when done, carrying the TEST rehearsal transcript, the checkpoint record, and the PROD census verbatim.
</output>
