---
phase: 146.2
plan: 06
type: execute
wave: 2
depends_on: ["146.2-01"]
files_modified:
  - src/app/api/strategies/csv-finalize/route.ts
  - src/lib/audit.ts
  - src/__tests__/csv-finalize-c14-regression.test.ts
  - TODOS.md
autonomous: true
requirements: [R7]
estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A payload row dated before 1900-01-01 is refused 400 CSV_INVALID_FORMAT at the route boundary — never a fold 22023 surfaced as a 500 'safe to try again' (R7)"
    - "The two undriven fail-closed resolve arms (refetchErr, no-committed-row) are each pinned by a case observed RED under a step-tag neuter (absorbed item 5)"
    - "The c14 resolve-echo case again proves 'echoed, not fabricated' on its own — the committed status it seeds is distinguishable from any fallback (absorbed item 4)"
    - "csv-finalize emits an audit event on a fresh successful finalize; the @audit-skip placeholder is gone and the audit-coverage law enforces the call (absorbed item 1 — decided IN SCOPE)"
    - "The two known deferrals are re-recorded in TODOS.md — nothing absorbed from TODOS is silently dropped"
  artifacts:
    - path: src/app/api/strategies/csv-finalize/route.ts
      provides: "date lower-bound mirror in parseDailyReturnsSeries; logAuditEvent call on the fresh-create success path, @audit-skip removed"
    - path: src/lib/audit.ts
      provides: "strategy.csv_finalize action literal + entity-type map entry (compile-enforced 3-step)"
    - path: src/__tests__/csv-finalize-c14-regression.test.ts
      provides: "1899/1900 boundary cases; two fail-closed arm cases pinning the finalize-resolve-read-fail step tag; restored echo discrimination; fixtures declaring category_id/asset_class explicitly"
  key_links:
    - from: "parseDailyReturnsSeries lower-bound branch"
      to: "the fold's DATE 1900-01-01 fence (20260819130000:344-352)"
      via: "route-side 400 mirror ahead of the RPC — permanent input never reaches the retry-copy 500 arm"
      pattern: "1900-01-01"
    - from: "csv-finalize fold call site (route.ts:611)"
      to: "src/__tests__/audit-coverage.test.ts"
      via: "removing @audit-skip re-enters the call under the audit-coverage law; the law is the standing gate"
      pattern: "logAuditEvent"
---

<objective>
Wave-2 close-out on `csv-finalize/route.ts` (single-owner serial after plan 01) plus the phase's
bookkeeping obligations.

**R7:** the fold refuses rows dated before 1900-01-01 with 22023, and a fold 22023 is CLASS 1
"rolled-back" → 500 CSV_FINALIZE_FAIL with "safe to try again" copy — a permanently-failing
input answered with retry copy. `parseDailyReturnsSeries` (route.ts:211-238) validates regex,
calendar round-trip, and future dates, but has NO lower bound. Mirror it as a 400 (magnitude
parity is already mirrored; only the date lower bound is missing).

**Absorbed items (CONTEXT table — this phase is their only record):**
- Item 5: the fail-closed resolve arms at route.ts:963 (refetchErr) and :969-977 (no committed
  row) are undriven — no test exercises either 503, and the finalize-resolve-read-fail Sentry
  step tag (:930-933) is unpinned.
- Item 4: c14's resolve-echo case lost its "echoed, not fabricated" discrimination to A2. With
  plan 01's fallback removal, the discrimination is provable again: seed a committed status that
  differs from the old fallback constant and assert the echo reports the seeded value.
- Item 1 (audit event) — **DECIDED: IN SCOPE.** The taxonomy is a closed compile-enforced map
  (src/lib/audit.ts:575-579: add the literal to AuditAction, the entity_type, and the map entry —
  the compiler catches an omitted step). A strategy.csv_finalize action with entity_type strategy
  conforms to the existing strategy.* family. The @audit-skip at route.ts:609 was an honest
  placeholder; removing it re-enters the fold call under the audit-coverage law
  (src/__tests__/audit-coverage.test.ts), which becomes the standing gate.
- Plus the two deferrals plans 01/02 hand over, re-recorded in TODOS.md (founder rule: absorbed
  items are deleted from TODOS; a deferral must be re-recorded — silent drop forbidden).

Output: route fence + audit emission + pinned arms + honest TODOS, every behavior RED-first.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-CONTEXT.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-RESEARCH.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-01-SUMMARY.md

Read before coding:
- src/app/api/strategies/csv-finalize/route.ts:200-240 (parseDailyReturnsSeries + future-date arm), :600-615 (@audit-skip + fold call), :920-980 (failClosed + the two undriven arms), :1101-1104 (the lexicographic-comparison validity comment)
- src/lib/audit.ts:560-640 (the action/entity map + the 3-step extension instruction), src/app/api/preferences/route.ts:215-295 (logAuditEventAsUser call-site shape: admin client, JWT-immune)
- src/__tests__/csv-finalize-c14-regression.test.ts (NEW-C14-09/C14-10 parse describes; strategiesMaybeSingleMock + findCapture machinery; fixture rows at :400-403)
- src/__tests__/audit-coverage.test.ts (the law that will enforce the new call)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R7 — date lower-bound mirror, RED first</name>
  <precondition>node_modules present in the working checkout (npx vitest --version succeeds); in a fresh worktree run npm install first</precondition>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-c14-regression.test.ts</files>
  <behavior>
    - Parse unit case: a row dated 1899-12-31 → parse result not ok (row-indexed message naming the date bound, mirroring the future-date arm's register).
    - Boundary case: 1900-01-01 exactly → accepted.
  </behavior>
  <action>
    Add both cases to the existing parseDailyReturnsSeries unit describes (NEW-C14-09/C14-10 are
    the template), run, observe the 1899 case RED against current code (no lower bound exists —
    free red; record it). Then implement: in the same per-row loop as the future-date check,
    reject rows whose date string is lexicographically below the 1900-01-01 literal —
    lexicographic comparison is valid for the already-regex-validated date format, the property
    the file itself relies on at :1101-1104 — returning the same not-ok shape with a row-indexed
    message. While there, confirm (do not change) magnitude parity between the route's abs bound
    and the fold's BETWEEN fence. Green. Named neuter for the record: deleting the new branch
    reds the 1899 case (observe once, restore). Commit as feat(146.2-06) with the RED noted.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism 2>&1 | tail -10 (green after implementation; the 1899 RED captured before it; exit code captured directly)</automated>
  </verify>
  <done>1899 rejected / 1900 accepted, both pinned; RED observed and recorded; permanent input can no longer reach the retry-copy 500 arm</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the undriven fail-closed arms + restore c14's echo discrimination + declare fixtures</name>
  <files>src/__tests__/csv-finalize-c14-regression.test.ts</files>
  <behavior>
    - Case A: the resolve re-fetch resolves with an error object → 503 CSV_PERSIST_FAIL AND findCapture locates the Sentry capture whose step tag is the finalize-resolve-read-fail string (route.ts:930-933).
    - Case B: the re-fetch resolves data null with no error (the synthetic no-committed-row Error at :969-977) → same 503 + same step tag.
    - Echo discrimination: the resolve-echo case seeds a committed status of private and asserts the echoed body status equals private — a value distinct from the removed fallback constant, so 'echoed, not fabricated' is proven by this case alone again.
    - Fixtures: the resolve-arm fixture rows now DECLARE category_id and asset_class explicitly (post-plan-01 the select reads them; explicit declaration keeps every case's tri-state deterministic — classified fixtures declare strings, and any case relying on absence keeps the field absent ON PURPOSE with a comment saying so).
  </behavior>
  <action>
    Extend c14-regression per the behavior list using its existing machinery
    (strategiesMaybeSingleMock, findCapture). Both new cases red against a neutered step tag:
    the named neuter is retagging the step string in a scratch edit of route.ts — observe both
    cases red on the retag, restore, observe green (this proves the tests pin the TAG, not just
    the status code). The discrimination case needs no route change — it reds if anyone
    reintroduces a status fallback (its neuter, named for the SUMMARY: restore the fallback
    expression → red; already observed under plan 01's task 3, cite that observation rather than
    repeating the route edit if the SUMMARY of plan 01 recorded it). Commit as test(146.2-06).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-c14-regression.test.ts src/__tests__/csv-finalize-cross-submission-merge.test.ts --no-file-parallelism</automated>
  </verify>
  <done>Both fail-closed arms driven with the step tag pinned (retag neuter observed RED and restored); echo discrimination restored; fixtures explicit; both csv-finalize suites green</done>
</task>

<task type="auto">
  <name>Task 3: Audit event on the fresh finalize + TODOS re-records</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/lib/audit.ts, TODOS.md</files>
  <action>
    Audit (absorbed item 1, decided in scope): (1) in src/lib/audit.ts add the
    strategy.csv_finalize action literal with entity_type strategy via the documented 3-step
    (AuditAction literal, map entry; entity type strategy already exists) — the compiler enforces
    completeness; (2) in the route, on the FRESH-create success path (after the fold outcome is
    known fresh, near the enqueue — not on echoes, which write no new strategy), emit via
    logAuditEventAsUser with an admin client and args.userId, entity_id the new strategyId,
    metadata carrying fmt and the correlation id — copy the preferences route call-site shape
    (JWT-immune in deferred windows, fire-and-forget, console-warn fallback); (3) delete the
    @audit-skip marker and its now-false placeholder prose above the fold call (:600-611), keeping
    the RPC name on the .rpc( line (the law scans line-by-line — the comment there says why).
    Named neuter: with the marker gone, temporarily remove the logAuditEvent call → the
    audit-coverage law test reds (this is the standing gate doing its job); observe, restore,
    record. Add a behavioral case if the law alone does not execute the call path (follow how
    sibling routes' audit emissions are asserted — check audit-coverage.test.ts and the
    fanout-integration suite first; do not duplicate a law that already covers it).
    TODOS.md (founder rule — absorbed items were deleted there; deferrals must be re-recorded):
    append two items under a Phase 146.2 recorded-deferrals heading: (a) the A2-arm refusal
    sentence still reuses the default CSV_SESSION_REUSED literal whose wording does not describe
    the terminal-status case — deferred by plan 01 because c14 pins the default sentence and the
    stopping rule keeps copy-only findings non-blocking; the refuse() second parameter plan 01
    added makes the future fix a two-line change; (b) process-key-client's transport catch scrubs
    with header-derived values only, so a TRAP-1-shaped transport error could inline BODY
    credentials into a seam-level CONSOLE line (Sentry unaffected — the file has no
    captureToSentry; verified 2026-08-19). Commit as feat(146.2-06).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/audit-coverage.test.ts src/__tests__/csv-finalize-c14-regression.test.ts --no-file-parallelism && npx tsc --noEmit && grep -c "strategy.csv_finalize" src/lib/audit.ts</automated>
  </verify>
  <done>Audit event emitted on fresh finalize; audit-coverage law green with the marker gone and observed RED under the call-removal neuter; both TODOS deferrals recorded; tsc clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → csv-finalize route | untrusted date payload; permanent inputs must be classified at the boundary |
| route → audit log | forensic record of finalize writes crosses to the audit store |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.2-12 | Repudiation | csv finalize write path | medium | mitigate | strategy.csv_finalize audit event on every fresh finalize; audit-coverage law is the standing gate |
| T-146.2-13 | Denial of Service | retry loop on permanent input | low | mitigate | pre-1900 rows 400 at the boundary instead of a retryable 500 |
| T-146.2-14 | Repudiation | fail-closed resolve arms | medium | mitigate | both 503 arms pinned by step-tag, so silent regressions of the capture surface red |
| T-146.2-SC | Tampering | package installs | low | accept | zero package installs in this phase |
</threat_model>

<verification>
- Both csv-finalize suites + audit-coverage green under `--no-file-parallelism`; tsc clean.
- TODOS.md diff shows exactly the two recorded deferrals — no other backlog edits.
</verification>

<success_criteria>
- R7 closed with the boundary pair pinned; absorbed items 1/4/5 closed in-scope; the two deferrals durably re-recorded.
- SC#5: every fix here observed RED under its named neuter (1899 free red; step-retag; audit-call removal).
</success_criteria>

<output>
Create `.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-06-SUMMARY.md` when done, recording all neuter observations and quoting the two TODOS entries verbatim.
</output>
