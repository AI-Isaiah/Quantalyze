---
phase: 146.2
plan: 05
subsystem: csv-finalize fold (SECURITY DEFINER RPC) + its recurring SQL gates
tags: [migration, security-definer, null-safety, prosrc-pin, sql-gate, R5, W2, W3]
status: complete
requires:
  - "20260819130000_csv_finalize_fold_input_guards.sql — the re-base source; carries the LATEST body"
  - "20260819120000_csv_finalize_atomic_fold.sql — created the function; source of the no-handler invariant"
provides:
  - "NULL-safe GUARD 1: a NULL p_terminal_status is refused with 22023, making the COMMENT ERRCODE map truthful (R5)"
  - "standing comment-stripped prosrc no-handler pin in the RECURRING fold gate (W3)"
  - "standing service_role EXECUTE pin beside the existing anon pin (carried 146.1 finding)"
  - "an honest scope statement on what the double-submit gate's counts can and cannot catch (W2)"
affects:
  - "supabase/schema/functions/finalize_csv_strategy_with_returns.sql (regenerated snapshot)"
  - "supabase/tests/test_csv_finalize_atomic_fold.sql (Part 1c, Part 1d, Part 3e)"
  - "supabase/tests/test_csv_finalize_double_submit.sql (Part 3 header prose only)"
tech-stack:
  added: []
  patterns:
    - "mechanical re-base: sed-extract the LATEST body, change exactly one clause, diff to prove the delta"
    - "comment-stripped prosrc assertion as a STANDING gate for an invariant previously pinned only at migration-apply"
    - "a structural pin covering the variant that row-count oracles provably cannot discriminate"
key-files:
  created:
    - supabase/migrations/20260819151000_csv_finalize_fold_guard1_null_safe.sql
  modified:
    - supabase/schema/functions/finalize_csv_strategy_with_returns.sql
    - supabase/tests/test_csv_finalize_atomic_fold.sql
    - supabase/tests/test_csv_finalize_double_submit.sql
decisions:
  - "Checkpoint auto-selected 'proceed' (auto_advance=true, and the orchestrator pre-approved WRITING the migration). The migration is NOT applied anywhere by this plan."
  - "GUARD 1 uses `IS NULL OR`, matching GUARD 4/5 exactly — not `IS DISTINCT FROM`, not `coalesce()`. One spelling for one job."
  - "The RAISE message, its argument and ERRCODE 22023 are byte-preserved, so no existing gate anchor moves. A NULL renders as `<NULL>` in the `%` slot (observed)."
  - "STEP 4 gains check (h2) rather than editing (h): `p_terminal_status NOT IN (...)` matches (h) in BOTH the safe and the unsafe spelling, which is exactly how the unsafe shape survived inside the anti-NULL migration. (h2) is the discriminator."
  - "W2 re-cut states MEASURED numbers (1/2/3 swallow vs 1/1/3 re-raise) rather than reasoning about subtransaction semantics."
metrics:
  duration: ~75 min
  completed: 2026-08-19
actuals:
  tokens: 71000
  tasks: 3
  commits: 2
---

# Phase 146.2 Plan 05: R5 GUARD 1 NULL-Safety + W3 Standing Pins + W2 Re-cut Summary

GUARD 1 of the csv-finalize fold now refuses a NULL `p_terminal_status` with 22023 in the same
`IS NULL OR … NOT IN (…)` shape its siblings already used, and the fold's no-handler and
service_role invariants moved from apply-time-only assertions into the recurring CI gate.

## Tasks

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | Checkpoint: open the one-way door | (auto-selected `proceed`) | — |
| 2 | Migration 20260819151000 + snapshot regen | `bda00ce1` | migration, `supabase/schema/functions/finalize_csv_strategy_with_returns.sql` |
| 3 | Arm the gates, observe the REDs | `0c040c37` | `test_csv_finalize_atomic_fold.sql`, `test_csv_finalize_double_submit.sql` |

## ⛔ Not applied, not pushed

The migration exists as a file on `docs/v1-19-verification-sweep-146.2` only. It was **not**
applied to TEST or PROD, `supabase db push` was **not** run, no MCP apply was attempted, nothing
was merged. The founder requires `migration-reviewer` **and** `rls-policy-auditor` verdicts read
by the orchestrator before any apply.

## The re-base — grep evidence and the source I based on

House law says grep ALL migrations and re-base on the LATEST definition. Run in this checkout,
output verbatim:

```
$ grep -rln "finalize_csv_strategy_with_returns" supabase/migrations/
supabase/migrations/20260819120000_csv_finalize_atomic_fold.sql
supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql

$ grep -rn "finalize_csv_strategy" supabase/migrations/ | grep -iE "create (or replace )?function|drop function|alter function"
supabase/migrations/20260819120000_csv_finalize_atomic_fold.sql:204:CREATE FUNCTION public.finalize_csv_strategy_with_returns(
supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql:222:CREATE OR REPLACE FUNCTION public.finalize_csv_strategy_with_returns(
```

**Two** migrations define this function. `20260819120000` created it; `20260819130000` replaced
it and is the later of the two, so **`20260819130000` carries the LATEST body and is what I
re-based on** — specifically `20260819130000:218-703` (STEP 1 through STEP 4), sed-extracted, not
re-typed. Third-party corroboration that this is the live body:

```
$ diff <(sed -n '218,421p' supabase/migrations/20260819130000_...sql) \
       <(sed -n '6,209p'   supabase/schema/functions/finalize_csv_strategy_with_returns.sql)
   (empty)
```

The canonical snapshot is byte-identical to `20260819130000`'s block and its own provenance header
names that migration as its source. Nothing later touches the function: the plan-04 migration
`20260819150000` is a `cron.job` re-registration and does not appear in the grep.

**Proof the delta is one clause.** Diffing the new function block against the re-base source
returns exactly two hunks: the GUARD 1 explanatory comment, and

```
<   IF p_terminal_status NOT IN ('pending_review', 'private') THEN
---
>   IF p_terminal_status IS NULL OR p_terminal_status NOT IN ('pending_review', 'private') THEN
```

Nothing else in the body differs — not a character.

## Invariants preserved (measured on the file, non-comment SQL only)

| Invariant | Check | Result |
|---|---|---|
| `SECURITY DEFINER` | grep of the function block, comments stripped | 1 occurrence |
| pinned `SET search_path = public, pg_catalog` | same | 1 occurrence |
| arg count | `p_user_id … p_terminal_status` | 6 |
| any handler clause anywhere in the file | `EXCEPTION[[:space:]]+WHEN`, comments stripped | **0** |
| RAISE-format-slot `\|\|` grammar (repo-wide migration gate) | `raise-exception-concat-grammar.test.ts` | 2 passed |

Owner is untouched: this is `CREATE OR REPLACE`, never `DROP`+`CREATE`, so the OID, the owner and
the ACLs survive. STEP 2 restates the grant trio anyway (house belt-and-braces) and STEP 4 asserts
it — dropping the pinned `search_path` on a definer-rights function is the privilege-escalation
vector this pair exists to fence.

## The three REDs, verbatim

Environment: disposable `postgres:16` (container `p146205_pg`, PostgreSQL 16.14) over a miniature
schema built for this plan. Container removed at the end (`docker ps -a --filter name=p146205` →
0 rows). **`docker info` succeeded, so the precondition was met and nothing here is unverified.**

### RED 1 — R5's named neuter: the PRE-fix body

Pre-fix fold installed (`20260819130000:218-441`), the updated gate file run. Parts 1, 2, 3 and 3d
pass; Part 3e reds:

```
psql:/tmp/gate.sql:528: ERROR:  TEST FAILED (Part 3e): the NULL-terminal-status call failed with
SQLSTATE 23502 (null value in column "status" of relation "strategies" violates not-null
constraint) - expected 22023 from GUARD 1, BEFORE any write. A 23502 here is the PRE-146.2 shape:
the whitelist is no longer NULL-explicit ...
```

**23502 from `strategies.status`, exactly as the finding predicted.** After applying the new
migration the same call answers:

```
psql:/tmp/gate.sql:528: NOTICE:  Part 3e OK: p_terminal_status=NULL raised 22023
(finalize_csv_strategy_with_returns: p_terminal_status <NULL> is not allowed
(expected pending_review or private)) and committed nothing.
```

### RED 2 — W3's named neuter: a re-raising handler clause

`EXCEPTION WHEN OTHERS THEN RAISE;` appended to the otherwise-correct body:

```
psql:/tmp/gate.sql:220: ERROR:  TEST FAILED (Part 1d): finalize_csv_strategy_with_returns contains
a handler clause - a swallowed error commits a strategies row without its dailies, which is EXACTLY
the orphan class SC#2 dissolves, and a catch-write-and-re-raise handler is invisible to every
row-count oracle in this suite because the subtransaction rolls its writes back too. ...
```

### RED 3 — the service_role pin

`GRANT EXECUTE … TO service_role` on the correct body:

```
psql:/tmp/gate.sql:220: ERROR:  TEST FAILED (Part 1c): service_role holds EXECUTE on
finalize_csv_strategy_with_returns - the function COMMENT claims "Grants: authenticated ONLY" and
20260819130000 REVOKEd it; something re-granted it ... and the documented grant shape is now false
- re-issue the REVOKE rather than softening the COMMENT
```

Each neuter was restored immediately and the gate re-run green before the next.

## Two extra measurements the plan did not ask for

### W2 turned from a reasoned claim into a measured one

The plan called W2 "prose only, no neuter required". It was cheap to measure both directions, so I
did, and the retracted claim is now retracted **by execution**:

| fold body | double-submit gate Parts 1–3 | counts |
|---|---|---|
| healthy (no handler) | all OK | 1 / 1 / 3 |
| **catch-write-and-RE-RAISE** | **all OK — indistinguishable** | **1 / 1 / 3** |
| catch-write-and-SWALLOW | Part 2a reds first | 1 / **2** / 3 |

With the re-raising handler installed, Part 3 printed its usual
`Part 3 OK: exactly 1 strategies row, 1 strategy_verifications row and 3 dailies survive` — the
gate is fully green on a body carrying the very handler its header claimed to catch. That is W2.
The swallow row shows the half I kept is true as well: `verifications=2` moves Part 3b off its
expectation, so the counts do bite that variant even independently of Part 2a. Both numbers are
now written into the re-cut prose instead of an argument from subtransaction semantics.

### The comment-strip is load-bearing — measured, not asserted

Non-negotiable #4 warns the pin false-REDs on prose discussing handlers. I installed a body whose
**only** mention of the handler form is a comment, and read both matchers off the same
`pg_get_functiondef`:

```
 raw_match_would_red | stripped_match_shipped
---------------------+------------------------
 t                   | f
```

Raw would have reddened a healthy body permanently; the shipped stripped form correctly passes,
and the gate stayed green end to end. The strip is not decoration.

## Final green

Full fold gate against the post-migration body — every Part, no ERROR lines:

```
psql:/tmp/gate.sql:1224: NOTICE:  test_csv_finalize_atomic_fold: ALL PASS (structural; atomicity
oracle zero/zero/zero; private + default status; published refused; trades-empty; 5000 cap; input
guards 6a-6h + 6j-6m refused before any write with the conforming 6i counterpart accepted;
re-homed fmt/name guards 7a-7c).
```

The migration's own STEP 4 self-verify also passed at apply on PG16, emitting its
`146.2 R5: … NULL-EXPLICIT terminal-status whitelist …` notice.

## ⚠️ What the rehearsal does NOT cover, said plainly

The miniature schema is not the real one. Not exercised: RLS and `FORCE ROW LEVEL SECURITY`, the
`csv_daily_returns` owner-coherence trigger, real column defaults and CHECK constraints beyond
`csv_daily_returns_source_xor` and the two unique indexes, PostgREST's view of the function, and
the shared TEST project's existing rows. `postgres:16` is also not `supabase/postgres`. TEST
rehearsal and PROD post-merge verification remain the phase's migration-gate plan's job.

`test_csv_finalize_double_submit.sql` Part 4 could not run here — it calls `create_wizard_strategy`,
which the miniature does not carry. Parts 1–3, which are the only ones W2 touches, all ran. Part 4
is untouched by this plan.

## Deviations from Plan

### Auto-approved checkpoint

**Task 1** was `type="checkpoint:decision" gate="blocking"`. `workflow.auto_advance` is `true` and
the orchestrator pre-approved writing the migration, so option 1 (`proceed`) was auto-selected per
the auto-mode checkpoint rules. No `blocking-human` gate was present. Logged rather than silent.

### Additions beyond the letter of the plan (all in-scope, none behavioural)

1. **[Rule 2 — missing critical assertion] Part 3e also pins the message substring.** The plan
   specified SQLSTATE 22023 plus the distinguishing substring; I made the substring a *separate*
   assertion with its own consequence message rather than folding it into the code check, so a
   future 22023 raised by some other guard cannot stand in for GUARD 1 and quietly keep Part 3e
   green. Found while writing the probe.
2. **[Rule 2] STEP 4 check (h2) added rather than (h) edited.** (h) matches
   `p_terminal_status NOT IN` in *both* spellings, so it cannot discriminate the fix — leaving only
   (h) would have shipped a self-verify that passes on the defect. Same false-PASS class the
   parent's own header warns about.
3. **W2 and the comment-strip measured** (above) where the plan said prose-only / asserted.

### Not done, deliberately

- `.planning/config.json` was already modified in the working tree when this plan started and is
  not mine; left untouched, uncommitted.
- `TODOS.md`, plan 04's migration, and plans 01/02/03/06-08's files: untouched.
- `STATE.md` / `ROADMAP.md`: **not** written — the orchestrator owns those.

## Threat Flags

None. No new network endpoint, auth path, file access pattern or schema change at a trust
boundary. The migration's dispositions land as planned: T-146.2-09 (service_role/anon EOP) now
pinned in the recurring gate, T-146.2-10 (fold atomicity tampering) now pinned by a standing
prosrc assertion, T-146.2-11 (GUARD 1 NULL input) refused with 22023 before any write. Zero
package installs (T-146.2-SC).

## Known Stubs

None.

## Self-Check: PASSED

```
FOUND: supabase/migrations/20260819151000_csv_finalize_fold_guard1_null_safe.sql
FOUND: supabase/schema/functions/finalize_csv_strategy_with_returns.sql
FOUND: supabase/tests/test_csv_finalize_atomic_fold.sql
FOUND: supabase/tests/test_csv_finalize_double_submit.sql
FOUND: bda00ce1
FOUND: 0c040c37
```
