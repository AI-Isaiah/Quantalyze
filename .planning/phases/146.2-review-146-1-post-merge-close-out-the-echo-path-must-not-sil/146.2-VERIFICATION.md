---
phase: 146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil
verified: 2026-08-19T23:45:00Z
status: human_needed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "MERGE GATE (plan 08 Task 3): merge PR #694, then run the post-merge PROD verification — live cron body carries the `< 3` ceiling conjunct and ONE real sweep tick succeeds; fold GUARD 1 (22023 on NULL p_terminal_status) and GUARD 2 (42501 on NULL p_user_id) live on PROD with prosecdef/proconfig/ACLs intact; `suspect_and_visible` still 0; PROD schema_migrations tip advances to 20260819151000"
    expected: "All census values match the TEST rehearsal table in 146.2-08-SUMMARY.md; any drift is a stop-the-line finding"
    why_human: "Both migrations are applied to TEST only. PROD state cannot exist before the merge, and this verifier has no PROD access. Plan 08 explicitly reserves Task 3 as ORCHESTRATOR-ONLY post-merge work — it is the phase's own designed blocking checkpoint, not an oversight."
  - test: "R6 real-flow UAT (flagged by the plan-07 executor): submit a CSV, kill the connection after the fold commits, resubmit the same file per A3's copy"
    expected: "The amber notice renders the route's echo sentence verbatim in-step; the footer reads [Back] [Continue] with NO disabled 'Submitting…' button; Continue lands on the strategies list with the strategy present"
    why_human: "jsdom tests pin the render, the hold, and the Continue callback (10/10 pass), but the real wizard flow — visual placement, amber tone against DESIGN.md, and the WizardClient navigation handoff on a live server — is not reachable by grep or jsdom. The executor itself routed this to the phase verifier as a manual UAT note."
---

# Phase 146.2 Verification Report

**Phase Goal:** A CSV finalize that recovers via the resubmit path A3's own copy instructs lands the user's classification instead of silently defaulting it, and no raw exchange credential reaches Sentry.
**Verified:** 2026-08-19 (initial verification — no prior VERIFICATION.md)
**Status:** human_needed — all 5 success criteria verified in code; two items require post-merge / live-flow human action
**Branch:** `docs/v1-19-verification-sweep-146.2`, PR #694 open. Phase diff `c4c88496..HEAD` (36 commits).

Stance: every SUMMARY claim below was re-derived from the shipped code, from `git show` at the
phase base, or from a test run in this session. Where I could not independently re-derive a claim,
that is stated plainly in "What I could NOT verify".

## Goal Achievement — the 5 ROADMAP Success Criteria

| # | Success criterion | Verdict | Evidence (file:line, this session) |
|---|---|---|---|
| 1 | 23505 echo applies submitted `category_id`/`asset_class` when the committed row has none; REFUSES 409 `CSV_SESSION_REUSED` on conflict | **ACHIEVED** | `src/app/api/strategies/csv-finalize/route.ts:1349` (FILL keyed on `existingRow.category_id === null`), `:1435-1459` (conflict → `refuse(...)` 409), `:1520` (`fillClassification` on the outcome), `:2311` (gate widened to `outcome.fresh \|\| outcome.fillClassification`), `:2270` (`if (!outcome.ok) return outcome.response` — refusals exit before any write). 83/83 tests pass across the three csv-finalize suites (run this session). |
| 2 | `passphrase` scrubbed from every Sentry capture site on verify-strategy; docblock enumeration matches the array | **ACHIEVED** | `src/app/api/verify-strategy/route.ts:235-239` (`bodyCredentials` incl. `passphrase`), `:328` (`perRequestSecrets = Object.values(bodyCredentials)`), fed to ALL 4 `captureToSentry` sites (`:404`, `:460`, `:504`, `:525`) and ALL 3 `scrubSeamError` sites (`:464`, `:508`, `:529`) — full-file grep census, zero unscrubbed sites. Docblock `:311-328` names all three credentials AND the one-declaration-two-readers mechanism. Seam suite 6/6 pass (run this session). |
| 3 | B4 readmit predicate cannot drive an unbounded reaped-orphan retry loop | **ACHIEVED** (code + gates; PROD apply pending merge → human item 1) | `supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql:388-396` (`count(*) … last_error LIKE 'orphaned_running_reaped:%' … < 3` conjunct AND'ed into the batch CTE — can only shrink the candidate set), `:499-508` (word-bounded self-verify). Matched-pair gate arms C5/C5b in `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:850-870`; `sql-tests` CI job **pass** at HEAD (run 32303289670). Applied to TEST with measured body properties (146.2-08-SUMMARY Task 1). |
| 4 | RT-3 re-mint persists the NEW session id — no spent-id-with-cleared-burn window | **ACHIEVED** | `src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:599` (`const nextWizardSessionId = newWizardSessionId()`), `:608-614` (ONE `saveWizardState` carries `wizardSessionId: nextWizardSessionId` + `failedCsvSubmitSig: null`), `:615` (setter fed from the same local). Pair-regression test passes (run this session). Base `c4c88496` confirmed the defect: the save wrote the old closure `wizardSessionId` before the swap. |
| 5 | Each fix carries a regression test observed RED under a named neuter | **ACHIEVED** (with one honest falsification, correctly handled — see below) | All 7 fix plans record verbatim RED transcripts with exit codes. Free-red premises independently re-verified at base `c4c88496` this session: `perRequestSecrets = [api_key, api_secret]` (base `:285-288`), metadata gate `outcome.fresh` only (base `:1923`), re-mint saves old closure id (base `:590-600`), no `< 3` anywhere in the B4 migration, GUARD 1 NOT NULL-explicit (base `20260819130000:243`). Every named regression test exists at HEAD and passes. |

**Score: 5/5.** No truth rests on symbol presence alone — SC#1/2/4 were exercised by named test
runs in this session, SC#3 by the CI `sql-tests` job at HEAD, SC#5 by base-state falsification.

## The five hardest checks (per the verification brief)

### 1. Is the FILL reachable on the path the goal names? — YES, traced end to end

`route.ts:756` (`error?.code === "23505"`) → `:791` (`resolveExistingStrategyOrRefuse`) →
`:1121-1127` (re-fetch `.select("id, name, status, category_id, asset_class")` — the projection
carries the classification) → `:1349` tri-state → `fillClassification: true` at `:1426` →
returned on the outcome at `:1520` → `:2311` `outcome.fresh || outcome.fillClassification` →
awaited `applyCsvMetadataUpdate` → enqueue at `:2326` AFTER the awaited update (ordering
commented as load-bearing at `:2308-2310`).

**The discriminator is `category_id === null` and is NOT `asset_class`-based.** `:1331-1342`
carries the crux verbatim: `asset_class` is `NOT NULL DEFAULT 'traditional'`, so "never
classified" and "chose traditional" are indistinguishable on that column; the code comments
explicitly forbid an `asset_class`-present pre-check blocking the fill. `undefined` (absent
reading) is a distinct third state (`:1136-1140`, `:1461` arm (c)) that licenses nothing.

The empty-series (`fmt='trades'`) FILL is additionally gated on the clock-safety guard
(`:1354-1420`): an owner-scoped `strategy_analytics` read of exactly the seven columns
`getPercentiles` folds into rankings; any non-null → REFUSE; read error → failClosed 503. The
column set at `:974-982` matches `PERCENTILE_ANALYTICS_COLUMNS` (`src/lib/queries.ts:126-127`)
member-for-member — re-checked this session.

### 2. Does the REFUSE arm write NOTHING? — YES, structurally

`refuse()` (`:1155-1191`) constructs and returns the 409 `NextResponse` inside
`resolveExistingStrategyOrRefuse`; the whole resolve arm performs only two reads (`strategies`
re-fetch, `strategy_analytics` guard read) and zero mutations. Every refusal returns
`ok: false`, and the handler exits at `:2270` before the metadata gate and the enqueue. The
clock-safety REFUSE (`:1408-1420`) explicitly declines even a `category_id`-only partial fill,
with the discovery-promotion reason in the comment. The REFUSE test arms pin
`updateCalls == []` (plan-01 D2/D4, passing at HEAD).

### 3. Did the ECONOMIC ORACLE survive byte-unchanged? — YES, both extractions non-empty

Content-extracted by `it()` title from `git show c4c88496:` and from HEAD: **20 lines each,
both non-empty, `diff` EMPTY** (the empty-vs-empty vacuity the brief warns about is excluded —
the extracted arm contains the real fixture, both `expect` calls, and the WHY messages). The
single named test passes at HEAD (`1 passed | 34 skipped`, run this session). Non-vacuity:
plan 01's neuter (b) transcript shows this exact arm redding under an unconditional FILL, with
the oracle's own WHY message ("a Sharpe computed on 252 presented as a 365 crypto track
record").

### 4. Is `passphrase` scrubbed at EVERY capture site? — YES, 4+3 of 4+3

Full-file grep census of `src/app/api/verify-strategy/route.ts`: exactly four `captureToSentry`
sites (`:394`, `:457`, `:502`, `:523`) — each passes `secrets: perRequestSecrets` — and three
`scrubSeamError` console sites (`:464`, `:508`, `:529`) — each passes `perRequestSecrets`.
`perRequestSecrets` is `Object.values(bodyCredentials)` and `bodyCredentials` contains
`passphrase` (`:238`). The wire side derives from the SAME tuple (`:260-262` filtered spread
into `teaserContext`), so a credential cannot be sent without being scrubbed — and the WIRE
SIDE test proves the scrub is not achieved by no longer sending (it asserts all three raw
credentials still reach `/process-key`). Sibling census: exactly 2 `perRequestSecrets` files in
`src/`, per the settled "instance, not class" measurement — not re-litigated.

### 5. SC#5 — neuters observed RED, verbatim, incl. plan 07's falsified one

Every fix plan records verbatim vitest/psql RED output with directly-captured exit codes (no
`$?`-after-pipe; plan 07 even documents catching that exact mistake in-flight). Spot-audit:

- **Plans 01–03, 06:** free reds + named neuters with verbatim `AssertionError` output and
  failure counts; the free-red premises confirmed real at base this session (see SC table).
- **Plan 04:** C5 red under the pre-ceiling body, C5b red under `< 2` (the matched pair —
  neither vacuous), and **the carried 146.1 "B4-BLANKET neuter was never run" finding is
  discharged by execution** — the blanket widening reds C2, C2b AND C3 (one arm stronger than
  146.1 claimed). Verbatim psql errors recorded.
- **Plan 05:** six REDs (GUARD 1 pre-fix 23502→22023, no-handler pin, service_role pin, GUARD 2
  pre-fix 23502→42501, STEP 4 (h3) discriminator, proconfig pin with three neuter rows), each
  with verbatim psql output and an anti-vacuity control (probe C: a valid status passes GUARD 1).
- **Plan 07 — the falsified neuter, verified as CORRECT reporting:** the plan's W1 neuter
  (alter one word of a re-typed constant) ran GREEN because the constant is both the mocked
  wire and the expected DOM — altering it moves both sides. The SUMMARY records the green
  verbatim WITH the mechanism instead of laundering it, which is the honest outcome. The
  substitute neuter (e) — decoy mock body while assertions still demand the constants — reds
  BOTH arms by name (verbatim `TestingLibraryElementError` naming each sentence), and it
  genuinely discriminates the property the arms enforce: the panel renders the sentence the
  ROUTE sent. What the suite does NOT enforce — correspondence of the fixtures to `route.ts` —
  is stated in the file. I re-established that correspondence independently this session:
  both hand-typed concatenations, mechanically joined, occur **verbatim, exactly once** in
  `route.ts` at HEAD (`grep -c` = 1 each; programmatic `includes` check = true for both).
- **No neuter was claimed-but-not-run.** The only summarized-not-verbatim entries are plan 04's
  TS-sibling-gate neuters (described with red counts, not full transcripts) — corroborated by
  the gate file existing at HEAD with the pinned assertions and the `frontend-test` CI jobs
  green. Not a gap under the stopping rule.

## Additional criteria beyond the five hard checks

### R5/W2/W3 (plan 05) — fold guards and standing pins

- GUARD 1 `IS NULL OR` and GUARD 2 `IS DISTINCT FROM` present in the migration body
  (`20260819151000:240`, `:280`) AND the regenerated canonical snapshot
  (`supabase/schema/functions/finalize_csv_strategy_with_returns.sql:43`, `:83`) — the two are
  in agreement, and `snapshot-drift` CI is green.
- Standing pins now live in the RECURRING gate, not only at apply time:
  `supabase/tests/test_csv_finalize_atomic_fold.sql` Part 1b (`proconfig` search_path regex,
  `:180-185`), Part 1c (`service_role` EXECUTE shut out, `:214-216`), Part 1d (comment-stripped
  no-handler prosrc, `:262`), Part 3e (NULL terminal status → 22023). `sql-tests` green at HEAD.
- W2's over-claim in `test_csv_finalize_double_submit.sql` re-cut to measured numbers
  (1/2/3 swallow vs 1/1/3 re-raise — the re-raising handler is genuinely invisible to the
  counts, per the executor's own measurement table).

### R7 + absorbed items (plan 06)

- Route-side date fence: `MIN_DAILY_RETURN_DATE = "1900-01-01"` (`route.ts:120`), fence at
  `:288` — the fold's literal copied verbatim with the two-sided move obligation commented.
- Audit event: `logAuditEventAsUser(createAdminClient(), …, action: "strategy.csv_finalize")`
  at `route.ts:733-734`, fresh-create branch only; the `@audit-skip` pragma is gone, so the
  standing audit-coverage law now REQUIRES the emission; Python mirror in
  `analytics-service/services/audit.py` pinned by the parity test (CI `python` job green).
- Both undriven fail-closed resolve arms pinned by step tag AND separated by forensic payload
  (c14 suite, passing).

### SEAMPROSE-01 — the mid-phase CI blocker is CLOSED at HEAD

Plan 07 correctly recorded a pre-existing RED (four bare `file:line` citations in `route.ts`,
introduced by plans 01/06, off-limits to plan 07) in `deferred-items.md` instead of
cross-plan-writing. Commit `d0a4dab5` symbol-anchored all four. Verified this session:
`src/lib/seam-citations.invariant.test.ts` → **47/47 pass**. The deferral protocol worked.

### Requirements coverage

| Req | Plan | Status | Evidence |
|---|---|---|---|
| R1 (blocker) | 01 | SATISFIED | tri-state + clock guard + A2-residual fail-closed status (`route.ts:1349-1520`) |
| R2 (security) | 02 | SATISFIED | shared tuple, 7/7 sites scrubbed |
| R3 | 04 + 08 | SATISFIED (PROD apply pending merge) | ceiling conjunct + C5/C5b gates |
| R4 | 03 | SATISFIED | mint-first into a local, one save |
| R5 | 05 | SATISFIED | GUARD 1 (+GUARD 2, class-closed) NULL-safe |
| R6 | 07 | SATISFIED | success-path `human_message` consumer, step holds, explicit Continue (`CsvSubmitStep.tsx:481-485`) |
| R7 | 06 | SATISFIED | boundary 400 replaces the retryable 500 |
| W1 | 07 | SATISFIED | provenance re-typed from HEAD, independently re-verified |
| W2 | 05 | SATISFIED | measured re-cut |
| W3 | 05 | SATISFIED | standing prosrc + proconfig + service_role pins |

No orphaned requirements: ROADMAP maps R1–R7, W1–W3 to this phase; all appear in plan
`requirements-completed` fields. The five absorbed TODOS items each landed (1→06 implemented,
2→01, 3=R6→07, 4→06, 5→06) or were durably re-recorded.

### Anti-patterns

Zero `TBD`/`FIXME`/`XXX` across all 22 non-`.planning` files the phase touched (scanned this
session). No skipped tests introduced. No placeholder copy.

### CI at HEAD (`0f7fac91`)

Completed `ci.yml` run at exactly HEAD: **success**, including `sql-tests: success`,
`e2e-seeded: success`, `frontend: success` (aggregator). The re-triggered PR run showed the
same jobs green during this verification. Per the founder's 2026-08-03 call, branch protection
is off — these gates are advisory at merge, so "green" here means "would have caught", never
"did stop".

## Deferred items (recorded, not gaps)

All durably recorded in `TODOS.md` under "Phase 146.2 — recorded deferrals (logged 2026-08-19)"
(verified present this session): the A2 409-sentence mismatch (copy-only, two-line fix now that
`refuse()` takes a per-arm sentence), the `process-key-client` body-credential console-scrub
class note, two prose-citation defects in the 20260819150000 header, plus the orchestrator's
earlier entries (`getPercentiles` failed-KPI surfacing, `cron.job.username` class gap, silent
ceiling exhaustion). Known accepted residuals stated honestly in plan 04: the ceiling is per
rolling 90-day retention window (3 cycles/quarter, not 3 forever) and bounds only this sweep.

## What I could NOT verify (stated plainly)

1. **The neuter RED transcripts themselves.** Neuters were temporary edits, observed, restored —
   re-running them would mutate the codebase. What I verified instead: the pre-fix defect
   shapes are REAL at base `c4c88496` (five independent `git show` checks above), every named
   regression test exists at HEAD and passes, and the transcripts' failure messages match the
   WHY strings in the shipped test files. The transcripts are executor claims corroborated on
   both ends, not independently reproduced.
2. **TEST-database state.** Plan 08 Task 1's measured table (cron `username=postgres`, fold
   catalog properties, behavioural probes A/B/C on TEST) is a SUMMARY record; I have no DB
   access in this session. The recurring `sql-tests` CI gates cover the same assertions against
   a fresh database and are green, which is the standing (rather than one-shot) evidence.
3. **PROD.** Nothing of this phase is on PROD (by design — PR #694 unmerged). Plan 08 Task 3 is
   the pending PROD census: human item 1.
4. **The real-browser echo flow.** jsdom pins render/hold/Continue; the live wizard flow is
   human item 2 (the plan-07 executor's own UAT note).

## Verdict

All 5 success criteria are achieved in the shipped code with behavioral evidence; the goal
sentence — FILL on the never-classified echo, REFUSE on conflict with zero writes, passphrase
scrubbed at every capture site — is observably true at HEAD. Status is `human_needed`, not
`passed`, solely because the phase's own design leaves two items that no verifier can close
pre-merge: the plan-08 post-merge PROD census and the live-flow R6 UAT.

---

_Verified: 2026-08-19_
_Verifier: Claude (gsd-verifier)_
