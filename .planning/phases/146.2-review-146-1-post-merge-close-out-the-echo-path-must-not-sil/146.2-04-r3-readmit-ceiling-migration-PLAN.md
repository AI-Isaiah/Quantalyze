---
phase: 146.2
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql
  - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
autonomous: false
requirements: [R3]
estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The B4 readmit predicate cannot drive an unbounded reaped-orphan retry loop: a strategy with N or more orphaned_running_reaped marker rows is no longer readmitted (ROADMAP SC#3)"
    - "C2/C3's no-retry-loop reasons stay satisfied: genuine failed_final rows still exclude via the untouched IS TRUE-wrapped conjunct; the ceiling only STOPS readmission, never adds it; the refused blanket widening stays refused"
    - "Gate arm C5 pins the ceiling and was observed RED against the pre-ceiling body"
    - "The carried 'B4-BLANKET neuter was never run' claim is discharged by actually running it and recording the observation"
  artifacts:
    - path: supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql
      provides: "cron-body re-registration with ONE added ceiling conjunct, byte-preserved otherwise, self-verify re-issued with word-bounded literal"
    - path: supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
      provides: "arm C5: N marker rows => zero INSERT"
  key_links:
    - from: "ceiling conjunct (count of marker rows per strategy)"
      to: "terminalizer marker rows (status failed_final, last_error prefix orphaned_running_reaped:)"
      via: "each reap-readmit cycle strictly increments the marker-row count — the count IS the attempt counter, zero DDL"
      pattern: "orphaned_running_reaped"
---

<objective>
Close R3: the B4 sweep readmit (`20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql:338`)
has no attempt ceiling. Cycle: enqueue → worker dies (OOM/container kill) → stays running → reaped
at 4h with the marker → sweep readmits → repeat, unbounded. The migration's own comments name
"an hourly retry loop with no attempt ceiling" three times as the mode it must not cause, but
guard only the NULL-last_error case.

**The attempt signal ALREADY EXISTS — zero DDL (RESEARCH §R3, all verified):** the terminalizer
UPDATEs the running row in place to a persistent `failed_final` marker row
(`20260817120000:618-622`, rows "survive as failed_final"), and the sweep INSERTs a NEW row each
readmission (`20260819130500:368-373`). So the per-strategy count of marker rows strictly
increments per cycle — that count IS the counter. `compute_jobs.attempts/max_attempts/reclaim_count`
are per-JOB worker-retry fields reset on each sweep INSERT — they cannot carry the cross-row
cycle count; do not use them.

**One-way door:** the migration auto-applies to PROD on merge and re-registers the live
`reconcile_dropped_enqueue_sweep` cron body (`cron.job` holds the body — source edits alone
change nothing). Explicit checkpoint BEFORE the migration-writing task, per orchestrator directive.

**Known residual to STATE, not fix:** failed_final rows hit the 90-day retention wall
(`20260817120000:260-261`), after which the marker count resets — the bound is N cycles per ~90
days, not N forever. That is a ceiling, which is what R3 asks; record it in the migration header.

Purpose: ROADMAP 146.2 SC#3.
Output: migration + gate arm C5 + recorded RED observations (including the carried B4-BLANKET claim discharge).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-CONTEXT.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-RESEARCH.md

Read before coding (PATTERNS.md section 4 + RESEARCH §R3 carry the shapes):
- supabase/migrations/20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql — FULL read: the batch CTE predicate (:325-360), the transcription-discipline comment (:290-296), the self-verify block (:440-470), the C2/C3 rationale comments (:98, :127, :442)
- supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql:600-660 (the marker-writing terminalizer)
- supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:400-470 (arms C2/C2b/C3/C4 — the house arm shape C5 copies)
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <decision>Ratify the readmit ceiling value N and open the one-way migration door</decision>
  <context>This migration re-registers the LIVE PROD cron body on merge (auto-apply; the body lives in cron.job, PROD-active with hourly ticks). Undo means another migration. The ceiling value is the one open parameter: it bounds how many reap-readmit cycles (4h each) a strategy gets per ~90-day retention window before the sweep stops readmitting it. Too low strands a strategy after transient worker deaths; too high burns worker cycles on a poisoned series. RESEARCH suggests 3 (~12h of cycles).</context>
  <options>
    <option id="n-3">
      <name>N = 3 (recommended)</name>
      <pros>Three full 4h claim windows (~12h) absorbs any transient outage; matches compute_jobs.max_attempts default of 3 (the house per-job ceiling); a strategy that OOMs a worker three times is a poisoned input, not a flake</pros>
      <cons>A genuinely transient multi-day incident (e.g. a wedged gateway) exhausts the ceiling before resolution; recovery then waits for the 90d wall or a manual re-enqueue</cons>
    </option>
    <option id="n-6">
      <name>N = 6</name>
      <pros>~24h of cycles; survives a full-day incident</pros>
      <cons>Doubles wasted worker cycles on a poisoned series; no house precedent for 6</cons>
    </option>
  </options>
  <resume-signal>Select: n-3 or n-6 (the chosen literal is word-bound-pinned in the self-verify and named in the migration header with this derivation)</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Write migration 20260819150000 — one added ceiling conjunct, everything else byte-preserved</name>
  <reversibility rating="one-way">Re-registers the PROD cron body on merge; undoing the ceiling requires another migration against a live scheduled job.</reversibility>
  <files>supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql</files>
  <action>
    Create the migration following 20260819130500's own transcription discipline (its :290-296
    comment: gates anchor on exact spellings; TEST/PROD md5s already differ by whitespace —
    transcribe the CURRENT body byte-exact and change exactly one thing): unschedule/re-register
    the reconcile_dropped_enqueue_sweep job with the SAME body plus ONE added conjunct in the
    batch CTE — a scalar subquery counting this strategy's compute_jobs rows with status
    failed_final and last_error matching the reaped-orphan marker prefix, compared strictly less
    than the ratified N (RESEARCH §R3 sketches the clause). Constraints, all load-bearing:
    (a) the existing IS TRUE-wrapped conjunct and every other predicate byte-preserved — C2/C3's
    no-retry-loop gates must stay green and the REFUSED blanket status-list widening must not
    appear in any form; (b) the ceiling only STOPS readmission (a count-bound guard), it never
    adds an admission path; (c) header comment names N with its derivation (checkpoint choice),
    the 90-day-wall residual (bound is N per ~90d), and why the marker-row count is the counter
    (per-job attempts fields reset on each INSERT); (d) re-issue the self-verify block in the
    house shape — comment-stripped regexp over the deployed cron.job command, word-bounded
    anchors on the N literal (backslash-m/backslash-M bounds so a widened literal cannot pass as
    a prefix), RAISE on drift, and keep the existing checks for the dailies conjunct and the
    two compute_jobs mentions (which becomes three with the ceiling subquery — update that
    count assertion honestly and re-cut its message). Do NOT edit 20260819130500 itself.
    Commit as feat(146.2-04).
  </action>
  <verify>
    <automated>bash -c 'f=supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql; test -s "$f" && grep -v "^[[:space:]]*--" "$f" | grep -c "orphaned_running_reaped"'</automated>
  </verify>
  <done>Migration exists; the ceiling conjunct references the marker prefix in non-comment SQL (grep count >= 1 after comment-stripping); self-verify re-issued with word-bounded N; header carries N derivation + 90d residual</done>
</task>

<task type="auto">
  <name>Task 3: Gate arm C5 + RED observations on throwaway PG16 (including the carried B4-BLANKET discharge)</name>
  <precondition>docker daemon available (docker info succeeds) — the RED observations rehearse SQL against a throwaway postgres:16 container; if docker is unavailable, STOP and surface a checkpoint rather than skipping the observations</precondition>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <action>
    Add arm C5 to the gate file in the existing arm shape (C2/C4 at :442-466 are the template —
    the arms run the TRANSCRIBED sweep body from the test file, self-contained): seed a strategy
    with N marker rows (status failed_final, last_error carrying the reaped-orphan prefix), run
    the transcribed post-ceiling body, assert ZERO new INSERT for that strategy; plus a positive
    control at N-1 markers asserting the readmit still fires (so C5 cannot pass vacuously).
    Update the file's transcribed body to the post-ceiling text (byte-copy from the new
    migration). Then on a throwaway postgres:16 container (docker run, disposable, plan-unique
    container name and scratch filenames — sibling agents share the scratchpad), observe TWO reds
    and record both verbatim: (1) C5's named neuter — run C5 against the PRE-ceiling body (the
    20260819130500 transcription) and observe it red; (2) the carried 146.1 finding "the
    B4-BLANKET neuter was never run, yet two SUMMARYs name it as decisive" — run the blanket
    status-list widening variant against arms C2/C3 and observe them red, which retroactively
    makes the SUMMARYs' claim TRUE by execution rather than retraction. Restore the correct body,
    re-run all arms green, tear down the container. Commit as test(146.2-04).
  </action>
  <verify>
    <automated>bash -c 'grep -c "C5" supabase/tests/test_reconcile_dropped_enqueue_sweep.sql && docker ps >/dev/null 2>&1; echo "gate file updated; RED observations recorded in SUMMARY with verbatim psql output"'</automated>
  </verify>
  <done>C5 armed with a positive control; C5 observed RED against the pre-ceiling body; B4-BLANKET widening observed reddening C2/C3 (carried claim discharged by execution); all arms green on the final body; container removed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration merge → PROD cron | the migration re-registers a live scheduled job on merge with no human in the loop |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.2-07 | Denial of Service | compute worker pool | medium | mitigate | ceiling bounds reap-readmit cycles at N per ~90d window; C5 pins it; positive control prevents a vacuous pin |
| T-146.2-08 | Tampering | sweep predicate semantics | high | mitigate | byte-preserved transcription + re-issued word-bounded self-verify; C2/C3 stay green proving no settled failure is re-enqueued |
| T-146.2-SC | Tampering | package installs | low | accept | zero package installs in this phase |
</threat_model>

<verification>
- Comment-stripped grep confirms the ceiling conjunct is live SQL, not prose.
- All sweep-gate arms green on the throwaway PG16 against the final body; both RED observations recorded verbatim in the SUMMARY.
- TEST rehearsal and PROD post-merge verification are NOT this plan's job — plan 08 (the 146.1-08-pattern gate) owns them.
</verification>

<success_criteria>
- ROADMAP 146.2 SC#3: the readmit predicate cannot drive an unbounded loop; C2/C3 reasons intact; blanket widening still refused.
- SC#5: C5 observed RED under its named neuter (pre-ceiling body).
- Carried finding discharged: the B4-BLANKET neuter has now actually been RUN, observation recorded.
</success_criteria>

<output>
Create `.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-04-SUMMARY.md` when done.
Record: the ratified N and its derivation, both verbatim RED observations, and the 90d-wall residual statement.
</output>
