# 146.2 PLAN-CHECK — pre-execution verification (gsd-plan-checker, 2026-08-19)

**Verdict: FAIL — 1 BLOCKER (plan 01, trades-echo FILL arm), 3 WARNINGS, 2 INFO.**
7 of 8 plans are sound as written; the blocker is localized and has a small prescribed fix.

---

## PRIORITY CHALLENGE — answer: (b), with precision

**The plan's stated premise is FALSE, and the decisive sub-question resolves against the plan:
`computation_status='failed'` rows' KPIs ARE surfaced.** Evidence, traced in src/ this session:

1. **The premise "with no compute job, strategy_analytics has no row at all (route.ts:1640-1654)"
   is false two ways:**
   - The cited comment describes the WR-04 empty-series *rejection* rationale, not a structural
     invariant about analytics rows.
   - The route ITSELF writes `strategy_analytics` rows with `computation_status='failed'` and no
     job success: `writeFailedStrategyAnalyticsPlaceholder` (`route.ts:1229-1305`, upsert at
     `:1280-1293`). PROD confirms: 18/18 zero-dailies csv strategies carry an analytics row; 7
     carry non-null `sharpe` AND `cagr` (all `computation_status='failed'`, computed 2026-05-27
     under older code).

2. **Failed rows' KPIs are READ and DISPLAYED:**
   - `src/lib/utils.ts:171-176` `extractAnalytics` does NO status gating.
   - `src/components/strategy/StrategyTable.tsx:1091-1092` renders
     `formatNumber(s.analytics.sharpe)` UNGATED by `computation_status` (only the *chip* at
     `:896-904` is status-aware). This is the discovery/browse listing surface.
   - `src/lib/queries.ts:141-183` `getPercentiles` selects `sharpe` etc. with NO
     `computation_status` column at all and folds every published row into percentile ranks.
   - (Contrast: strategy-v2 detail `queries.ts:1197,1212` and both factsheet PDF routes DO gate
     on complete — the gating is per-surface, not systemic.)

3. **Why this bites the trades-FILL arm specifically:** FILL writes `category_id` — which
   PROMOTES a previously discovery-invisible row (WR-04) into the `discovery_categories!inner`
   listing join — together with `asset_class='crypto'`, while the empty-series arm fires NO
   recompute. If the row belongs to the historical failed-with-KPIs class, its √252-computed
   Sharpe is then displayed under a crypto label with nothing reconciling it. That is exactly
   the hazard constraint B (the ECONOMIC ORACLE) exists to forbid.

**Mitigating measurements (honest, but not an invariant):** the placeholder writer never writes
KPI columns (only status/error/flags, `route.ts:1283-1291`); under CURRENT code no new
zero-dailies strategy can acquire KPI values (both enqueuers are dailies-gated; the worker needs
a job); all 7 KPI-bearing failed rows are historical (pre-A4, 2026-05-27) and an echo against
them requires the same user resubmitting a ~3-month-dead wizard_session_id. So the hazard
population is small and shrinking — but "practically unreachable" is a census fact, not a
structural pin, and this codebase's own rule is that money-math must not rest on usually-true.

### BLOCKER-1 (plan 01, must_haves truth 4 + Task 1 trades-FILL arm + objective evidence chain)
- The sentence "`trades` strategies carry NO stored KPIs — structurally pinned" and "with no
  compute job, strategy_analytics has no row at all" must be DELETED — both are refuted by
  PROD and by `route.ts:1229ff`.
- **Fix (either):**
  (i) Condition the empty-series FILL on stored-KPI absence: before FILLing on an echo whose
  enqueue will not fire, read the strategy's `strategy_analytics` row; if it carries any
  non-null stored KPI (`sharpe`/`cagr`/…), REFUSE (or FILL `category_id` only, never the
  clock) — one extra owner-scoped read, and the trades-FILL test arm gains a sibling
  KPI-present → no-clock-move arm with its own neuter; OR
  (ii) FILL unconditionally but gate the `asset_class` component on "the recompute fires"
  (`dailyReturnsSeries.length > 0`), leaving `category_id`-only FILL on empty-series echoes —
  simplest, loses nothing economically (a truly KPI-less trades row has no clock reader today).
- Either way, re-ground the objective's FILL-safety chain on the TRUE premise: "no KPI-value
  writer exists without a completed compute job under current code (enqueuers dailies-gated;
  the route placeholder writes no KPI columns), and the historical failed-with-KPIs class is
  excluded by the guard above" — not on "no row exists".
- The currently-planned trades-FILL test arm (`FILL applied AND no enqueue`, unconditional)
  would PIN the hazardous behavior. It must be re-cut to match the chosen guard.

---

## Additional checks

### 1. SC coverage — PASS
SC#1→01; SC#2→02; SC#3→04+08; SC#4→03; SC#5→named neuters in every plan. Requirements roster
R1-R7/W1-W3 fully mapped: 01:[R1+A2-residual], 02:[R2], 03:[R4], 04:[R3], 05:[R5,W2,W3],
06:[R7+absorbed 1/4/5], 07:[R6,W1], 08:[R3,R5 live verification]. No orphan criterion.

### 2. Economic oracle survives — PASS (with WARNING-1)
Plan 01 Task 1 excludes `:1133-1152` from edits; Task 3 done-criteria require grep presence +
"git diff shows no hunk inside" the arm. Planned edits (rewrite `:1115`, add arms) cannot touch
it if the done-checks are enforced. **WARNING-1:** the check is LINE-anchored (`:1133-1152`)
but the new arms shift line numbers; the executor must verify byte-identity of the arm's
CONTENT (e.g. `git diff` hunk inspection by arm name "ECONOMIC ORACLE"), not line range. State
this in the plan's Task 3 done or the check can pass vacuously after line drift.

### 3. The `:1115` arm — PASS
Task 1 explicitly REWRITES it to "echo against a CLASSIFIED row issues NO metadata UPDATE"
(fixture gains explicit `category_id` string) — narrowed, not deleted.

### 4. Neuters — PASS (spot-checked)
- Free reds vs shipped HEAD verified real: R1 FILL/A2 (HEAD gates `:1923` on `outcome.fresh`,
  fallback at `:1165-1172` present), R2 (passphrase truly absent from `perRequestSecrets`,
  route.ts:285-288), R4 (shipped ordering persists old closure id, WizardClient:590-608).
- Task-3 neuters in plan 01 all genuinely red their targets, including (b) unconditional FILL →
  ECONOMIC ORACLE red (it would overwrite the oracle's present-and-different fixture).
- Plan 04 C5 has a positive control at N-1 (anti-vacuity ✓); B4-BLANKET carried claim
  discharged by execution ✓. Plan 05's three PG16 reds each target the exact pin ✓.
- Minor: the trades-FILL arm's no-enqueue half is green-on-HEAD (no enqueue happens either
  way); the arm as a whole reds via the FILL half — acceptable, but the arm is re-cut under
  BLOCKER-1 anyway.

### 5. Discriminator — PASS
FILL keys exclusively on `category_id IS NULL` (objective "MUST NOT key FILL on asset_class";
Task 2 note forbids an asset_class-present pre-check blocking FILL). No task violates it.

### 6. File-collision discipline — PASS (with INFO-1)
`csv-finalize/route.ts`: plan 01 (wave 1) → plan 06 (wave 2, depends_on 01) — single-owner
serial ✓. Test files disjoint: cross-submission-merge = 01 only; c14-regression = 06 (01 runs
it read-only with an explicit "fix within own files only" rule ✓); CsvSubmitStep* = 07 only.
**INFO-1:** plans 06 and 07 share wave 2; 07 hand-retypes two route.ts sentences "at
post-plan-01 HEAD" while 06 edits route.ts concurrently. 06's edit set (:600-611 audit,
parse loop, no refuse/persist-fail literals) is disjoint from the sentences 07 re-types, and
the A2-sentence change is explicitly deferred to TODOS — safe, but worktrees must fork from a
base containing plan 01's pushed commits (memory rule; plan 07 already depends_on 01).

### 7. Migrations — PASS
Plan 04: cron-body re-registration, zero table DDL ✓; blocking `checkpoint:decision` (ceiling
N) BEFORE the writing task ✓; `autonomous: false` ✓. Plan 05: `CREATE OR REPLACE` function
only, zero table DDL ✓; blocking checkpoint before the writing task ✓. Plan 08 adds the TEST
rehearsal + blocking human merge gate + PROD census — the 146.1-08 pattern intact.

### 8. Scope — PASS (with WARNING-2)
No plan touches the four struck-through CLOSED bookkeeping items ✓. All absorbed items have a
home (1→06 in-scope with documented decision; 2→01; 3=R6→07; 4→06; 5→06) and the two
deferrals are durably re-recorded in TODOS via 06 ✓. Carried findings (B4-BLANKET neuter,
service_role pin, A2 sentence) each land or defer explicitly ✓. Nothing beyond the roster.
**WARNING-2 (advisory, never blocking per ADR-2629):** plan 01's estimate is 90k tokens at
`confidence: low` — the heaviest of the set (total 460k across 8 plans). With the BLOCKER-1
re-cut adding an arm, watch plan 01 for overrun; if it degrades, split Task 3's
adjacent-suite sweep into plan 06.

### WARNING-3 — plan 01 SUMMARY mandate
The `<output>` block orders the SUMMARY to record "the FILL-safety evidence chain (trades ⇒ no
stored KPIs, structurally pinned)" — this sentence must be rewritten with the corrected
premise from BLOCKER-1, or the false claim gets laundered into the permanent record.

### INFO-2
Plan 01 cites the oracle at `:1133-1152`; CONTEXT says `:1132`. Off-by-one on the describe
line — harmless, but supports WARNING-1's content-anchoring point.

---

## Structured issues

```yaml
issues:
  - plan: "146.2-01"
    dimension: verification_derivation
    severity: blocker
    description: >-
      Trades-echo FILL arm rests on a false premise ("no job => no strategy_analytics row =>
      no stored KPIs"). PROD: 18/18 zero-dailies strategies have analytics rows, 7 with
      non-null sharpe/cagr (computation_status='failed'); route.ts:1229-1305 itself writes
      failed placeholder rows; StrategyTable.tsx:1091 and getPercentiles (queries.ts:141-183)
      surface failed-row sharpe ungated. FILL on an empty-series echo moves the annualization
      clock AND promotes the row into discovery (writes category_id) with no recompute.
    fix_hint: >-
      Guard the empty-series FILL: either refuse/skip the asset_class write when the analytics
      row carries any non-null stored KPI, or FILL category_id only when the enqueue will not
      fire. Re-cut the trades-FILL test arm to pin the guard; re-ground the objective's
      evidence chain and the SUMMARY mandate on the corrected premise.
  - plan: "146.2-01"
    dimension: task_completeness
    severity: warning
    description: "ECONOMIC ORACLE untouched-check is line-anchored (:1133-1152) but new arms shift lines"
    fix_hint: "Verify content byte-identity of the named arm, not a line range"
  - plan: "146.2-01"
    dimension: scope_sanity
    severity: warning
    description: "90k-token estimate at low confidence, heaviest plan, grows under BLOCKER-1 fix"
    fix_hint: "If overrunning, move Task 3 adjacent-suite sweep to plan 06"
  - plan: "146.2-01"
    dimension: verification_derivation
    severity: warning
    description: "SUMMARY output mandate orders recording the refuted 'structurally pinned' claim"
    fix_hint: "Rewrite the SUMMARY mandate alongside the objective"
```

## Recommendation
Return plan 01 to the planner for the BLOCKER-1 re-cut (objective evidence chain + trades-FILL
guard + test arm + SUMMARY mandate). Plans 02-08 need no changes and may be dispatched once
plan 01 is revised (06/07 depend on it).
