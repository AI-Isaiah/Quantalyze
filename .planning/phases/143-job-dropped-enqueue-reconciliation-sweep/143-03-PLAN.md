---
phase: 143-job-dropped-enqueue-reconciliation-sweep
plan: 03
type: execute
wave: 2
depends_on: [143-01, 143-02]
files_modified:
  - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
  - src/__tests__/reconcile-dropped-enqueue-sweep.test.ts
  - analytics-service/tests/test_main_worker.py
autonomous: true
requirements: [JOB-04]
tags: [sql-gate, vitest, pytest, ci, pg_cron, migration-content-gate]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "A CI-visible SQL gate EXECUTEs the REAL deployed cron body (read from cron.job.command, never retyped) and proves SC#1 heal, SC#2 idempotency + bound, and every SC#3 never-touch arm (D-14)"
    - "The SQL gate's structural part is DELIBERATELY UNGATED and FAILS when the migration is unapplied — its TDD RED arrives on the PR's first sql-tests run, before Plan 04 applies to TEST"
    - "A pure-text TS gate pins the deployed shape (MATERIALIZED, SKIP LOCKED, ON CONFLICT DO NOTHING, the 1-hour literal, the four excluded statuses, the composite conjunct) and negative-anchors the rejected shapes — scoped to the $cron$ body so header prose neither satisfies nor trips it"
    - "A pytest pins the cross-language marker contract: the $cron$ body's marker literals equal the literals main_worker.py reads (D-11) — with an anti-vacuity guard on the extraction"
    - "Every gate has been OBSERVED red under its neuter, not asserted (D-14, standing founder rule)"
  artifacts:
    - "supabase/tests/test_reconcile_dropped_enqueue_sweep.sql — Parts 1-4, per-part BEGIN/ROLLBACK, identity-scoped, century-backdated seeds"
    - "src/__tests__/reconcile-dropped-enqueue-sweep.test.ts — migration-content gate, no DB required"
    - "analytics-service/tests/test_main_worker.py — TestReconcileSweepMarkerContract + _SWEEP_MIGRATION_NAME/_sweep_migration_path()/_sweep_cron_body()"
  key_links:
    - "SQL gate oracle = EXECUTE of cron.job.command — never a re-typed predicate (a gate that re-implements the predicate passes when the DEPLOYED predicate is wrong)"
    - "TS/pytest gates -> $cron$ extraction with anti-vacuity assertion (an empty extraction must fail loudly, never pass negatives by default)"
    - "gate filename constants <-> migration filename 20260816140000_reconcile_dropped_enqueue_sweep.sql — must move together in one commit (RESEARCH P-7)"
---

<objective>
Build the three CI-visible gates for the sweep (D-14): the SQL gate that executes the deployed body
against seeded fixtures on the TEST project (the ONLY gate that can falsify the predicate), the
vitest migration-content gate (runs in every shard, no DB), and the pytest cross-language
marker-contract gate. Then observe every gate RED under its neuter using the Plan-02 throwaway
harness.

Purpose: D-19's lesson — every prior gate passed over a broken bound because it grepped for tokens;
only executing the body falsifies it. And the founder rule: a test that CANNOT FAIL is worse than none.
Output: three gate files; recorded neuter table; the SQL gate's designed RED staged for the PR's
first sql-tests run.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CONTEXT.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-PATTERNS.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-02-SUMMARY.md
@supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql
</context>

## Decision map (cited as D-NN below; source: 143-CONTEXT.md locked decisions)

| ID | Locked decision honored here |
|----|------------------------------|
| D-02 | The has-a-job never-touch arm seeds a RUNNING derive_broker_dailies job — proving ANY-kind scoping (kind-scoping the NOT EXISTS is the neuter that must redden it) |
| D-03 | The terminal-analytics arm's failure message NAMES the mass-re-enqueue incident |
| D-04 | A computing arm proves the non-racing split with 142's reaper (dropping 'computing' from the exclusion list must redden it) |
| D-09/DX-05 | A composite arm (strategy_keys member) proves composites are never touched |
| D-10 | Idempotency part: two EXECUTEs of the deployed body -> exactly one job row (SC#2) |
| D-11 | The marker-contract pytest pins source/reconcile-sweep/detected_at across SQL and Python |
| D-14 | Gates live in supabase/tests/test_*.sql + src/__tests__ + analytics-service/tests — the only CI-visible locations; every gate observed RED when neutered |

DX carried from Plan 02: migration filename `20260816140000_reconcile_dropped_enqueue_sweep.sql`,
jobname `reconcile_dropped_enqueue_sweep`, schedule `35 * * * *`, LIMIT 25, grace `interval '1 hour'`.
If Plan 02's Task 1 bumped the timestamp, use the bumped name EVERYWHERE in this plan (P-7: gate
constants and migration filename move together).

## Artifacts this phase produces

Same full inventory as 143-01-PLAN.md — this plan delivers
`supabase/tests/test_reconcile_dropped_enqueue_sweep.sql`,
`src/__tests__/reconcile-dropped-enqueue-sweep.test.ts`, and
`TestReconcileSweepMarkerContract` (+ helpers `_SWEEP_MIGRATION_NAME`, `_sweep_migration_path()`,
`_sweep_cron_body()`) in `analytics-service/tests/test_main_worker.py`.

<tasks>

<task type="auto">
  <name>Task 1: Author the SQL gate (Parts 1-4) against the deployed body</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <read_first>
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql (FULL FILE, 925 lines — the model: header discipline :33-130, per-part BEGIN/SET LOCAL lock_timeout/ROLLBACK framing :62-79, the ungated Part 1 anti-green-skip rule :45-60, FK seed chain :336-351, backdate-never-sleep :323/:359-366, EXECUTE v_command oracle :396-399, identity-scoped LIMIT part :454-554, failure-message register :407/:527, frozen-clock trap :114-123)
    - supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql (the object under test — anchors, jobname, LIMIT, grace literal, the four excluded statuses)
    - .github/workflows/ci.yml:833-1035 (sql-tests discovery of supabase/tests/test_*.sql, ON_ERROR_STOP, the preflight rejecting backslash meta-commands, concurrency shared-test-db)
    - supabase/migrations/20260710120000_strategy_keys.sql:1-120 (strategy_keys NOT NULLs + the owner-coherence trigger the composite arm's seed must satisfy — it requires a REAL api_keys row whose user_id matches)
    - output of: grep -l "CREATE TABLE.*api_keys" supabase/migrations/*.sql (derive the minimal api_keys INSERT column set for the composite arm's seed; check later migrations for added NOT NULL columns)
  </read_first>
  <action>
    Create supabase/tests/test_reconcile_dropped_enqueue_sweep.sql modeled part-for-part on the 142
    gate. NO whole-file BEGIN (psql nested BEGIN creates no savepoint — the first ROLLBACK would end
    the outer transaction and later parts would AUTOCOMMIT seeds onto the SHARED TEST project). Every
    writing part: own BEGIN; SET LOCAL lock_timeout = '5s'; ... ROLLBACK;. pgTAP is NOT installed —
    plain RAISE EXCEPTION assertions, NOTICEs on success. No backslash meta-commands (CI preflight
    rejects them). No sleeps — all ages are seed backdates (created_at minus 100 years), which is why
    DX-04 chose an INSERT-writable anchor.
    Part 1 (STRUCTURAL, deliberately UNGATED — must FAIL when the migration is unapplied; that is this
    file's TDD RED, observed on the PR's first sql-tests run before Plan 04 applies): cron job named
    reconcile_dropped_enqueue_sweep exists; schedule STRING-equals 35 * * * *; exactly one job row;
    deployed command positive anchors (the MATERIALIZED keyword, SKIP LOCKED, ON CONFLICT DO NOTHING,
    the 1-hour interval literal, public.strategy_keys, the reconcile-sweep marker literal, all three
    triangle tables schema-qualified) and negative anchors (no IN-SELECT-LIMIT shape via the regex
    from 20260803130000:188-190; neither rejected timestamp anchor column; not the enqueue RPC name).
    Parts 2+ gate ONLY on pg_cron absence (RAISE NOTICE SKIP, local-dev only) — NEVER green-skip on
    the object under test being absent; fetch v_command from cron.job and RAISE EXCEPTION if NULL
    while pg_cron is installed.
    Part 2 (DIRECTIONAL ARMS): one part, one FK seed chain (auth.users -> profiles -> strategies),
    v_seeded array, all assertions identity-scoped (= ANY (v_seeded), never global counts — the
    shared-TEST rule). Seed the arms from Plan 02's observed table: A orphan-past-grace analytics-
    ABSENT (dailies created_at = v_fresh - 100 years) -> after EXECUTE v_command exactly ONE
    compute_jobs row, status pending, kind compute_analytics_from_csv, metadata source =
    reconcile-sweep; A2 analytics at pending -> healed; B in-grace (dailies created_at = v_fresh) ->
    zero rows; C1 a running derive_broker_dailies job seeded -> zero NEW rows (message: kind-scoping
    the NOT EXISTS re-enqueues healthy in-flight chains — D-02); C2 only a failed_final job -> zero
    new rows; C3 only a done job -> zero new rows; D1-D4 analytics at complete /
    complete_with_warnings / failed / computing -> zero rows each. D1's failure message MUST name the
    consequence: with retention deleting done jobs at 30 days, healing a complete row means the sweep
    re-enqueues the entire historical corpus — the mass-re-enqueue incident (D-03). D4's message names
    the 142 reaper race (D-04). E composite: strategy_keys member row (seed a minimal REAL api_keys
    row for the same user to satisfy the FK + owner-coherence trigger) -> zero rows (message: enqueueing
    the csv kind on a composite overwrites a correct composite headline with divergent single-key
    math — a money surface, D-09). F status archived -> zero rows.
    Part 3 (IDEMPOTENCY, SC#2/D-10): re-seed arm A shape; EXECUTE v_command TWICE; exactly ONE job row
    for that strategy after both ticks.
    Part 4 (BOUND, D-08): seed 26 candidates with staggered century-old anchors (oldest first wins the
    ORDER BY budget by construction — isolation on the shared project, never cross-tenant UPDATEs);
    tick 1 -> exactly 25 healed, the youngest untouched; tick 2 -> the 26th healed (bounded AND
    progressing). Never compare two now()-derived values inside one part (frozen clock — use the
    seeded sentinel ages).
    Every failure message carries requirement id + arm + business consequence (the 142 register).
  </action>
  <verify>
    <automated>bash -c 'f=supabase/tests/test_reconcile_dropped_enqueue_sweep.sql; test -s "$f" && ! grep -nE "^[[:space:]]*\\\\(!|copy|COPY|o)" "$f" && grep -c "ROLLBACK;" "$f" && grep -c "SET LOCAL lock_timeout" "$f"</automated>
  </verify>
  <acceptance_criteria>
    - File exists; zero backslash meta-commands; every writing part paired BEGIN/SET LOCAL/ROLLBACK
      (counts match part count); no whole-file transaction.
    - Part 1 carries no pg_cron SKIP arm (it must red on an unapplied migration); Parts 2-4 skip ONLY
      on pg_cron absence with an explanatory NOTICE.
    - All Part 2-4 assertions are identity-scoped to the part's own seeded ids; zero global counts;
      zero cross-tenant UPDATEs; zero sleeps; zero now()-vs-now() comparisons.
    - The D1 (complete) failure message contains the words naming the mass-re-enqueue consequence; the
      D4 message names the 142 reaper split.
    - The oracle is EXECUTE of v_command in every behavioral part — the predicate is never re-typed.
  </acceptance_criteria>
  <done>The SQL gate exists, structurally sound and CI-discoverable, with the deployed-body oracle and all SC arms encoded.</done>
</task>

<task type="auto">
  <name>Task 2: TS migration-content gate + pytest marker-contract gate</name>
  <files>src/__tests__/reconcile-dropped-enqueue-sweep.test.ts, analytics-service/tests/test_main_worker.py</files>
  <precondition>node_modules resolvable in the working tree (worktree spawns have none — run `npm ci` first or execute in the main checkout; `npx vitest` in a bare worktree exits 1 with MODULE_NOT_FOUND)</precondition>
  <read_first>
    - src/__tests__/compute-jobs-kind-check-csv-2026-05-25.test.ts (FULL FILE, 137 lines — imports/path constants :1-36, local-literal oracle discipline :38-54, scoped-body assertion :77-94, self-verify assertion :96-102, forward-drift sweep over later migrations :104-136)
    - analytics-service/tests/test_main_worker.py:2194-2243 (_reaper_migration_path/_reaper_cron_body — the extraction + anti-vacuity idiom to mirror for the NEW migration; do NOT copy TestReaperThresholdDriftGate's SQL<->Python equality — a mirrored 1-hour Python constant would be a decorative drift gate, RESEARCH Don't-Hand-Roll)
    - analytics-service/main_worker.py (Plan 01's shipped emission — the exact marker literals the pytest pins)
    - supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql (the file under test)
  </read_first>
  <action>
    (A) src/__tests__/reconcile-dropped-enqueue-sweep.test.ts, on the compute-jobs-kind-check model:
    header comment with the incident narrative (the after() hole) + numbered assertion list + the
    pure-text-no-DB line. Constants: FIX_FILENAME = the migration filename; FIX_TS = its prefix.
    Extract the $cron$ body with a regex and expect(match).not.toBeNull() (anti-vacuity). On the BODY
    only (grep-gate hygiene — header prose must neither satisfy nor trip): positive anchors — the
    MATERIALIZED batch keyword, SKIP LOCKED, ON CONFLICT DO NOTHING, the 1-hour interval literal,
    LIMIT 25, all four excluded computation_status literals as a set, public.strategy_keys,
    public.csv_daily_returns, the reconcile-sweep marker with both keys source and detected_at, and
    cron-schedule assertions on the WHOLE file for jobname + the 35 * * * * string; negative anchors
    on the BODY — no IN-SELECT-LIMIT regex shape, neither rejected timestamp anchor column token, not
    the enqueue RPC name, no CREATE FUNCTION, no SECURITY DEFINER. Whole-file assertions: a DO block +
    RAISE EXCEPTION self-verify exists; the fail-loud pg_cron check exists (feature_not_supported).
    Forward-drift sweep: every migration with prefix > FIX_TS must not re-register jobname
    reconcile_dropped_enqueue_sweep (if one ever does, this test names P-7's move-together rule in its
    failure message). Declare all oracle literals LOCALLY with a comment naming the migration as the
    production source — never import them.
    (B) In analytics-service/tests/test_main_worker.py: add _SWEEP_MIGRATION_NAME (the filename
    constant), _sweep_migration_path() (parents[2] / supabase / migrations idiom), _sweep_cron_body()
    (regex extraction + anti-vacuity assert that the body contains the INSERT INTO
    public.compute_jobs statement — an empty extraction must fail loudly, never pass negatives by
    default), and class TestReconcileSweepMarkerContract with
    test_cron_body_marker_matches_worker_literal: the body contains the jsonb_build_object call with
    key literals source and detected_at and value reconcile-sweep, AND main_worker.py's source
    (read via inspect.getsource or pathlib) contains the matching reads of the same three literals —
    the cross-language contract (D-11): drift on either side silently kills the alert, and this test's
    docstring says so.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/reconcile-dropped-enqueue-sweep.test.ts 2>&1 | tail -3 && cd analytics-service && python3 -m pytest tests/test_main_worker.py -k "MarkerContract" -x -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Both gates GREEN against the committed migration + Plan 01's worker code.
    - The TS gate's body-scoped negatives cannot be tripped by header prose: temporarily append a
      header comment containing a rejected anchor column token, re-run, still GREEN, revert (observed,
      recorded — this proves the scoping).
    - The pytest extraction helper's anti-vacuity assert fires when pointed at a file with no $cron$
      block (observed via a temporary bogus-path run or equivalent, recorded).
    - `cd analytics-service && python3 -m mypy --strict --follow-imports=silent tests/test_main_worker.py`
      introduces zero new errors; no new type-ignore comments.
    - Coverage: the TS file is pure text assertions (raises, never lowers, the 82/80/74/72 thresholds).
  </acceptance_criteria>
  <done>Both content gates exist, green, body-scoped, with anti-vacuity guards proven live.</done>
</task>

<task type="auto">
  <name>Task 3: Observe every gate RED under its neuter (harness-driven)</name>
  <files>supabase/tests/test_reconcile_dropped_enqueue_sweep.sql</files>
  <precondition>Plan 02's throwaway harness and cluster recipe are available (.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql committed by Plan 02; local Postgres 16 toolchain present)</precondition>
  <read_first>
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md "### Neutering proof — what to break for each assertion" (the full Gate | Neuter | Expected RED inventory this task discharges)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql (the stub schema + fake cron the gate runs against locally)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-02-SUMMARY.md (which neuter signatures Plan 02 already observed against the BODY — do not duplicate; this task observes the GATES redden)
  </read_first>
  <action>
    Stand the throwaway cluster up from the committed harness + migration, then run the SQL GATE FILE
    itself against it with psql -v ON_ERROR_STOP=1 (fidelity caveat recorded: no RLS/triggers/real
    scheduler — the real-schema run is CI sql-tests post-apply, Plan 04). Observe and record each
    gate-neuter pair, restoring from committed text after each (never retype):
    (1) Migration absent (drop the cron.job row) -> Part 1 REDs (the anti-green-skip proof).
    (2) Deployed command with the exclusion list missing the complete status -> the D1 arm REDs with
    the mass-re-enqueue message.
    (3) Deployed command with the NOT EXISTS kind-scoped to compute_analytics_from_csv -> the C1
    running-derive arm REDs (D-02).
    (4) Deployed command with the composite conjunct removed -> arm E REDs (D-09).
    (5) Deployed command with the grace conjunct removed -> arm B REDs.
    (6) Deployed command with a bare INSERT (conflict clause removed) -> Part 3 REDs on
    unique_violation or the count assertion (D-10).
    (7) Deployed command with the MATERIALIZED keyword removed -> Part 4 REDs with 26 healed on tick 1
    (the D-19 signature, D-08).
    (8) TS gate: flip one excluded-status literal in a COPY of the migration -> vitest REDs naming the
    missing status; restore.
    (9) pytest contract gate: change the marker key source to src in a COPY -> pytest REDs; restore.
    Record all nine in the Gate | Neuter | Expected RED | Observed table. After all restores:
    clean-tree proof (git status clean, grep MUTANT -> 0) and one final green run of the SQL gate
    against the intact harness, the TS gate, and the pytest -k MarkerContract filter.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/reconcile-dropped-enqueue-sweep.test.ts 2>&1 | tail -2 && cd analytics-service && python3 -m pytest tests/test_main_worker.py -q 2>&1 | tail -2 && git status --porcelain | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - All nine neuter pairs OBSERVED (verbatim RED output captured for each) and restored; the table is
      complete in the SUMMARY with no asserted-only rows (D-14).
    - Final state: TS gate green, full test_main_worker.py green, SQL gate green against the intact
      harness, git tree clean, `grep -rn MUTANT` -> 0.
    - The SUMMARY states plainly what the harness run does NOT prove (cron role RLS posture, real
      scheduler) and points to Plan 04.
  </acceptance_criteria>
  <done>Every gate in the phase has an observed RED on record; all gates green on the intact artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI (sql-tests as psql user) -> shared TEST project | gate seeds/rollbacks on a DB shared with other PRs' jobs |
| gate files -> deployed cron body | the oracle boundary: gates must read the deployed artifact, never re-implement it |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-03 | DoS / Tampering | terminal-analytics conjunct | high | mitigate | Task 1 arm D1 + Task 3 neuter (2): dropping the complete status is OBSERVED to redden the gate whose message names the mass-re-enqueue incident (D-03) |
| T-143-04 | Tampering | composite money surface | high | mitigate | Task 1 arm E + Task 3 neuter (4) observe the composite guard reddening (D-09/DX-05) |
| T-143-02 | Tampering | cron role vs FORCE RLS | high | mitigate | Explicitly OUT of this plan's power: the sql-tests job connects as the psql user and cannot prove the cron role's posture — stated in the gate header and the SUMMARY; proof owned by Plan 04's live tick. The gate must not be worded to imply otherwise |
| T-143-13 | Tampering | shared-TEST pollution by the gate itself | medium | mitigate | Per-part BEGIN/ROLLBACK (no whole-file transaction — nested-BEGIN autocommit hazard), identity-scoped assertions, century-backdated seeds that win the LIMIT budget by construction, zero cross-tenant UPDATEs (the 142.1/D-18 deletion is not reintroduced) |
| T-143-14 | Information Disclosure | psql meta-command exfiltration | medium | mitigate | No backslash meta-commands (CI preflight ci.yml:951-1000 rejects them; Task 1 verify greps) |
| T-143-15 | Repudiation | vacuous gates | high | mitigate | Anti-vacuity asserts on every extraction; Part 1 deliberately ungated; all nine neuters OBSERVED not asserted (D-14) |
| T-143-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- SQL gate file passes CI preflight rules and encodes Parts 1-4 with the deployed-body oracle
- TS gate + pytest contract gate green; body-scoping and anti-vacuity proven live
- Nine observed neuter pairs recorded; tree clean; all gates green on intact artifacts
</verification>

<success_criteria>
- Every phase SC has a CI-visible gate that has been seen to fail: SC#1 (Part 2 arm A + marker
  contract), SC#2 (Parts 3-4), SC#3 (arms B/C/D/E/F).
- The PR's first sql-tests run will RED on Part 1 until Plan 04 applies the migration to TEST — the
  designed, free-standing TDD RED for the whole SQL gate.
</success_criteria>

<output>
Create `.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-03-SUMMARY.md` when done,
including the nine-row `Gate | Neuter | Expected RED | Observed` table.
</output>
