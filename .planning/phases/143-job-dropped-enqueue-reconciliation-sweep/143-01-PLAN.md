---
phase: 143-job-dropped-enqueue-reconciliation-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/main_worker.py
  - analytics-service/tests/test_main_worker.py
autonomous: true
requirements: [JOB-04]
tags: [sentry, python-worker, pg_cron, job-queue, alerting]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "main_worker.main() calls init_sentry() before any loop starts — without it every capture_* is a silent no-op and SC#1's 'a Sentry alert fires' is false in production while unit tests stay green (D-11 correction)"
    - "Claiming a compute_jobs row whose metadata.source == 'reconcile-sweep' emits exactly one Sentry warning event carrying strategy_id and detected_at (SC#1 alert half, D-11)"
    - "Claiming a job WITHOUT the marker emits no Sentry event"
    - "A Sentry transport failure never fails the job being dispatched"
    - "The main()-calls-init_sentry assertion goes RED when the init line is removed, while the capture test stays GREEN — proving the two tests are independently load-bearing (D-11)"
  artifacts:
    - "analytics-service/main_worker.py — module-level `import sentry_sdk`, init_sentry() in main(), marker-read emission in dispatch_tick()"
    - "analytics-service/tests/test_main_worker.py — TestMainWorkerSentryBootstrap + TestReconcileSweepAlert"
  key_links:
    - "main() -> init_sentry() (analytics-service/sentry_init.py:347) — the bootstrap every capture depends on"
    - "dispatch_tick claim loop (main_worker.py:605-626) -> job['metadata']['source'] — the Python half of the cross-language marker contract whose SQL half Plan 02's cron body writes"
    - "module-level `import sentry_sdk` -> monkeypatch.setattr(main_worker, 'sentry_sdk', spy) — the testability idiom; a from-import breaks it"
---

<objective>
Make SC#1's "a Sentry alert fires" TRUE at the Python layer: wire Sentry initialization into the
standalone worker process (which today has ZERO Sentry references — RESEARCH L-1) and emit a real
Sentry event when the worker claims a compute_jobs row carrying the reconcile-sweep metadata marker
that Plan 02's cron body stamps.

This implements D-11 (Sentry fires worker-side on claim, via metadata marker) INCLUDING its
2026-08-16 ⛔ CORRECTION: the worker has no Sentry wiring to hook into, so `init_sentry()` into
`main_worker.py::main()` is a mandatory prerequisite, and a SEPARATE assertion that `main()` calls
`init_sentry()` is load-bearing (the capture test mocks the SDK and stays green when init is removed).

Purpose: without this plan the sweep heals silently — the exact "alerting channel that fails
silently" defect class D-11 rejected pg_net for.
Output: main_worker.py bootstrap + emission; two new pytest classes; observed RED->GREEN plus the
init-removal neuter proof.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CONTEXT.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-PATTERNS.md
</context>

## Decision map (cited as D-NN below; source: 143-CONTEXT.md locked decisions)

| ID | Locked decision |
|----|-----------------|
| D-11 | Sentry fires worker-side on claim via `metadata = {source: 'reconcile-sweep', detected_at: <ts>}`; ⛔ CORRECTION: `init_sentry()` must be wired into `main_worker.main()` FIRST, a separate main()-calls-init assertion is load-bearing, and `SENTRY_DSN` on the worker Railway service must be verified (Plan 04). pg_net bridge and prose-only alerts REJECTED. |
| D-14 | CI-visible gates only; every gate observed RED when neutered (standing founder rule). |

Claude's-discretion choices made in this plan (documented per CONTEXT "Claude's Discretion"):
- **DX-07:** emission lives in the `dispatch_tick` claim loop — immediately after the
  `claim_token = job.get("claim_token")` read at main_worker.py:625 and BEFORE the `try:` at :626
  that owns the heartbeat task's try/finally. Rationale: the marker read must not sit inside the
  heartbeat's error handling, and claim-time (not handler entry) alerts even when dispatch fails.
- **DX-08:** test names are `TestMainWorkerSentryBootstrap::test_main_calls_init_sentry` and
  `TestReconcileSweepAlert::{test_marker_job_captures_sentry_event, test_unmarked_job_does_not_capture,
  test_capture_failure_does_not_fail_job}`. Neuter proofs are recorded as a
  `Gate | Neuter | Expected RED | Observed` table in the plan SUMMARY.

## Artifacts this phase produces (full phase inventory — this plan delivers the ⭐ rows)

| Artifact | Symbol / value |
|---|---|
| Migration (Plan 02) | `supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql` |
| Cron job (Plan 02) | `reconcile_dropped_enqueue_sweep` at `35 * * * *` |
| Metadata marker (Plan 02 writes, ⭐ this plan reads) | `{"source": "reconcile-sweep", "detected_at": <timestamptz>}` |
| ⭐ Worker bootstrap | `init_sentry()` call inside `main_worker.main()`; module-level `import sentry_sdk` |
| ⭐ Worker emission | marker-check + `sentry_sdk.capture_message("Dropped compute-job enqueue healed by reconciliation sweep", level="warning")` in `dispatch_tick()` |
| ⭐ pytest classes | `TestMainWorkerSentryBootstrap`, `TestReconcileSweepAlert` in `analytics-service/tests/test_main_worker.py` |
| pytest contract gate (Plan 03) | `TestReconcileSweepMarkerContract` + `_SWEEP_MIGRATION_NAME` / `_sweep_migration_path()` / `_sweep_cron_body()` |
| SQL gate (Plan 03) | `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` |
| TS gate (Plan 03) | `src/__tests__/reconcile-dropped-enqueue-sweep.test.ts` |
| Throwaway harness (Plan 02) | `.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql` |
| Census artifact (Plans 02/04) | `.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md` |
| TODOS entries (Plan 04) | composite non-coverage; wizard first-hop non-coverage |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing pytests for bootstrap + marker capture (observe RED)</name>
  <files>analytics-service/tests/test_main_worker.py</files>
  <precondition>`cd analytics-service && python3 -c "import sentry_sdk"` exits 0 (deps installed; worktree spawns may lack them — if it fails, `pip install -r requirements.txt --break-system-packages` or execute in the main checkout)</precondition>
  <read_first>
    - analytics-service/tests/test_main_worker.py:39-110 (TestDispatchTick — the mocked-supabase claim-loop harness to reuse: claim_chain/mark_chain rpc side_effect, patch of main_worker.get_supabase and main_worker.dispatch)
    - analytics-service/tests/test_secret_misconfig_signal.py:85-110 (_FakeSentry spy + its "must NOT silently invent attributes" discipline comment; the monkeypatch.setattr(module, "sentry_sdk", spy) idiom)
    - analytics-service/main_worker.py:587-680 (dispatch_tick — the claim loop the emission lands in) and :955-999 (main() — what must be patched to call it in a test)
    - analytics-service/main.py:16-20 (why sentry_sdk must be imported as a MODULE for the spy idiom to work)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-PATTERNS.md ("Analog C" — no existing test asserts main() calls anything; this test is written from first principles and its docstring must say so)
  </read_first>
  <behavior>
    - Test 1 (test_main_calls_init_sentry): with validate_kek_on_startup, asyncio.gather, the four loop
      coroutines (dispatch_loop, watchdog_loop, daily_enqueue_loop, start_healthz_server) and signal-handler
      registration patched out, awaiting main_worker.main() invokes init_sentry exactly once. Patch
      init_sentry AS IMPORTED IN main_worker's namespace (the patch target depends on the import style
      Task 2 chooses — patch "main_worker.init_sentry"). Docstring states this is the load-bearing
      companion to the capture test per D-11: the capture test mocks the SDK and stays green when init
      is removed, so THIS test is the only thing that makes the alert real.
    - Test 2 (test_marker_job_captures_sentry_event): a claimed job dict carrying
      "metadata": {"source": "reconcile-sweep", "detected_at": "2026-08-16T00:00:00Z"} drives dispatch_tick
      with a _FakeSentry-style spy installed via monkeypatch.setattr(main_worker, "sentry_sdk", spy);
      exactly one capture_message call is recorded with level="warning" and message
      "Dropped compute-job enqueue healed by reconciliation sweep"; the scope received tags
      surface="reconcile-sweep" and job_kind, and extras strategy_id and detected_at.
    - Test 3 (test_unmarked_job_does_not_capture): a claimed job with metadata None AND a second job with
      metadata {"source": "something-else"} produce zero capture_message calls.
    - Test 4 (test_capture_failure_does_not_fail_job): the spy's capture_message raises; dispatch_tick
      still dispatches the job and the job outcome is unchanged (DispatchResult DONE path completes).
  </behavior>
  <action>
    Add the two classes to analytics-service/tests/test_main_worker.py. Reuse TestDispatchTick's
    mocked-supabase harness verbatim (claim_chain returning the seeded jobs list, mark_chain for the
    status rpc, patched main_worker.dispatch returning DispatchResult(outcome=DispatchOutcome.DONE)).
    Build the Sentry spy on the tests/test_secret_misconfig_signal.py:85-110 pattern, including its
    no-invented-attributes discipline: the fake exposes new_scope (a context manager yielding a scope
    recorder with set_tag/set_extra) and capture_message ONLY. Do not import production literals into
    assertions — declare the expected message string and marker key literals locally in the test,
    naming main_worker.py as the production source they pin (oracle-independence convention,
    143-PATTERNS.md "Established Patterns"). Do NOT touch main_worker.py in this task — these tests
    must fail against the unmodified worker.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_main_worker.py -k "SentryBootstrap or ReconcileSweepAlert" -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - All four new tests FAIL against unmodified main_worker.py, and the RED output is captured verbatim
      for the SUMMARY (test_main_calls_init_sentry fails on the missing init_sentry symbol/patch target;
      the capture tests fail on zero captures). This is the phase's designed RED for the alert path.
    - No other test in tests/test_main_worker.py changed state: `python3 -m pytest tests/test_main_worker.py -q`
      shows the same pass count as baseline plus exactly 4 new failures.
    - pytest was run from analytics-service/ with python3 (repo-root runs miss VCR cassettes — RESEARCH P-8).
    - The four tests contain no sleep calls and no wall-clock assertions (the 142.1 item-6 lesson).
  </acceptance_criteria>
  <done>Four failing tests exist, RED observed and recorded; no production file touched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire init_sentry() into main() + marker emission into dispatch_tick (GREEN + neuter proof)</name>
  <files>analytics-service/main_worker.py</files>
  <read_first>
    - analytics-service/main_worker.py:1-40 (existing imports — where the module-level `import sentry_sdk` and the init_sentry import land), :605-630 (the exact insertion point: after `claim_token = job.get("claim_token")` at :625, before the `try:` at :626), :961-993 (main() — insertion after logging.basicConfig, before validate_kek_on_startup)
    - analytics-service/sentry_init.py:330-400 (init_sentry: DSN-absent early return, send_default_pii=False, _redact_before_send, environment resolution — and the StarletteIntegration/FastApiIntegration args relevant to the smoke below)
    - analytics-service/main.py:16-20 and :60-69 and :184-203 (module-import discipline; bootstrap placement; the new_scope tag-then-capture idiom wrapped in try/except so a transport failure never breaks the caller)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-PATTERNS.md (comment register: every non-obvious line in the claim loop carries a `# <REQ-ID>:` comment naming its incident)
  </read_first>
  <action>
    Three edits to analytics-service/main_worker.py, per D-11 and DX-07:
    (1) Module level: add `import sentry_sdk` (as a MODULE — a from-import breaks the
    monkeypatch.setattr spy idiom, main.py:16-20) and `from sentry_init import init_sentry`.
    (2) In main(): call init_sentry() after logging.basicConfig and before validate_kek_on_startup,
    with a JOB-04 comment stating the worker previously had NO Sentry client, that capture without init
    is a silent no-op, and that the call no-ops when SENTRY_DSN is unset.
    (3) In dispatch_tick's claim loop at the DX-07 insertion point: read
    `_meta = job.get("metadata") or {}`; when `_meta.get("source") == "reconcile-sweep"`, open
    sentry_sdk.new_scope(), set tag surface="reconcile-sweep", tag job_kind from job.get("kind"),
    extra strategy_id from job.get("strategy_id"), extra detected_at from _meta.get("detected_at"),
    then capture_message("Dropped compute-job enqueue healed by reconciliation sweep", level="warning").
    Wrap the whole emission in try/except Exception: pass — a Sentry transport failure must never fail
    the job (main.py:186-188 convention). Carry a JOB-04 comment naming the honest latency limitation
    (alert = sweep tick -> next worker claim; a fully-down worker means no alert, independently alarmed
    by healthz) so the reader never assumes instant paging (D-11).
    Pass only strategy_id (a UUID) — never user email or CSV contents (V7, threat T-143-06).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_main_worker.py -q 2>&1 | tail -3 && python3 -m mypy --strict --follow-imports=silent main_worker.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All 4 Task-1 tests GREEN; full tests/test_main_worker.py green; zero previously-passing tests broken.
    - NEUTER PROOF OBSERVED AND RECORDED (D-14): temporarily remove the init_sentry() call from main(),
      run the -k filter — test_main_calls_init_sentry goes RED while test_marker_job_captures_sentry_event
      stays GREEN (the recorded proof that the two tests are independently load-bearing, per the D-11
      correction). Restore via git checkout of the file (never retype), confirm git status clean on it,
      re-run GREEN. Record the pair in the SUMMARY neuter table.
    - Integrations-outside-ASGI smoke (RESEARCH A4): from analytics-service/,
      `SENTRY_DSN="https://public@o0.ingest.sentry.io/0" python3 -c "from sentry_init import init_sentry; init_sentry(); print('init-ok')"`
      prints init-ok with exit 0 — proving StarletteIntegration/FastApiIntegration construct without an
      ASGI app on sentry-sdk 2.64.0. If it raises, the fix is a worker-specific init path (still calling
      sentry_sdk.init with the same redaction args), NOT removing the init — record the deviation.
    - `python3 -m mypy --strict` on main_worker.py introduces zero NEW errors versus baseline; no new
      `# type: ignore` (fix via cast() if needed — RESEARCH P-9).
    - `grep -n "import sentry_sdk" analytics-service/main_worker.py` shows a module-level module import
      (not a from-import).
  </acceptance_criteria>
  <done>Worker initializes Sentry at boot and alerts on claiming a reconcile-sweep-marked job; RED->GREEN observed; init-removal neuter proof recorded; mypy --strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker (service-role) -> Sentry | operator telemetry leaves the tenant boundary; PII must not travel with it |
| cron body -> worker | the metadata marker is a cross-language contract; drift silently kills the alert |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-05 | Repudiation | main_worker.py Sentry path | high | mitigate | init_sentry() wired in main() (this plan, Task 2); SEPARATE main()-calls-init pytest whose RED is observed with init removed (Task 2); SENTRY_DSN on the worker Railway service verified in Plan 04 (unverifiable from repo — honestly manual) |
| T-143-06 | Information Disclosure | Sentry event payload | medium | mitigate | send_default_pii=False + _redact_before_send already in init_sentry (sentry_init.py:356-362); event carries strategy_id UUID + detected_at only — never email/CSV content (Task 2 action) |
| T-143-10 | Tampering | marker contract (SQL literal vs Python literal) | high | mitigate | Python side declares the exact literals "source"/"reconcile-sweep"/"detected_at"; Plan 03's TestReconcileSweepMarkerContract pins both halves against the migration's $cron$ body with an anti-vacuity guard |
| T-143-11 | DoS | dispatch_tick emission | medium | mitigate | emission wrapped in try/except; test_capture_failure_does_not_fail_job proves a Sentry failure never fails a job |
| T-143-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase (sentry-sdk[fastapi]==2.64.0 already declared, requirements.txt:222); no legitimacy checkpoint required |

The three phase-level threats named in the planning context (SECDEF surface, FORCE-RLS cron write,
mass re-enqueue) are owned by Plans 02/03/04 — see those registers.
</threat_model>

<verification>
- `cd analytics-service && python3 -m pytest tests/test_main_worker.py -q` green (full file)
- `cd analytics-service && python3 -m mypy --strict --follow-imports=silent main_worker.py` no new errors
- Neuter pair (init removed -> bootstrap test RED, capture test GREEN) observed and recorded
- Integrations-outside-ASGI smoke exit 0
</verification>

<success_criteria>
- SC#1 alert half is real at the Python layer: claim of a marked job emits one Sentry warning event
  through an initialized SDK; unmarked jobs emit nothing; transport failure is harmless.
- The D-11 correction is fully discharged in code + tests; only the Railway SENTRY_DSN check (Plan 04)
  and the live PROD event remain outside this plan.
</success_criteria>

<output>
Create `.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-01-SUMMARY.md` when done,
including the `Gate | Neuter | Expected RED | Observed` table for the four tests.
</output>
