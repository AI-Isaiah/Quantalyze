---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/restore-test-from-baseline.sh
  - scripts/restore-test-from-baseline-fixtures/old-test.sql
  - scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql
  - scripts/restore-test-from-baseline-fixtures/migrations/20260101000000_fixture_a.sql
  - scripts/restore-test-from-baseline-fixtures/migrations/20260102000000_fixture_b.sql
  - scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql
  - src/__tests__/restore-test-from-baseline.test.ts
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The restore is ONE transaction: a preflight run leaves the target database byte-for-byte unchanged (rolled back), and a restore run either commits the whole drop+replay+survivors+ledger or commits nothing."
    - "The script refuses, BEFORE any write, on: absent credential; NULL database marker; a marker that does not name TEST or that names PROD; baseline sha not matching BASELINE.md; a migration committed after the last baseline regeneration; the shared-TEST advisory mutex not held."
    - "Objects outside `public` that depend on `public` (the `auth.users` signup trigger, `storage.objects` policies calling public functions, realtime publication rows) are censused BEFORE the drop and re-created AFTER the replay, and the post-census must equal the pre-census by name or the transaction aborts."
    - "The ledger seed is CLI-shaped: one row per repo migration file, `version` = the 14-digit filename prefix, `name` = the description after the first underscore, `statements` = a provenance sentence naming the baseline sha — never the claim that the SQL was executed on TEST."
    - "Every red arm above is OBSERVED red by `--self-test` on a throwaway cluster, with a pinned arm count that fails when an arm disappears."
  artifacts:
    - "scripts/restore-test-from-baseline.sh — `--self-test`, `--run --mode preflight|restore`, `--help`; exports nothing but prints a counted self-test line"
    - "scripts/restore-test-from-baseline-fixtures/ — an 'old TEST' fixture (stray public objects, re-stamped ledger rows, an auth.users fixture with a trigger on a public function, a storage.objects fixture with a policy calling a public function, a database COMMENT marker) and a small baseline-shaped dump fixture"
    - "src/__tests__/restore-test-from-baseline.test.ts — pins the script's constants (EXPECTED_ARMS, the filtered-line pattern, the marker regexes) line-exact, and calibrates each pin on a mutated copy"
  key_links:
    - "scripts/restore-test-from-baseline.sh reads the expected sha from supabase/schema/BASELINE.md by the same table-row shape scripts/check-baseline-staleness.mjs reads (a second opinion is a second drift)"
    - "scripts/restore-test-from-baseline.sh boots its throwaway cluster with the binaries scripts/pg-lane/run.sh --print-pgbin names, and copies that lane's port allocation and EXIT-trap cleanup shape — it never touches TEST or PROD in --self-test"
    - "The transaction stream = preamble (BEGIN, lock_timeout, DROP SCHEMA public CASCADE, CREATE SCHEMA public) + the baseline dump with exactly one filtered line class + survivors DDL + ledger TRUNCATE/INSERT + DO-block assertions + NOTIFY pgrst + COMMIT-or-ROLLBACK"
---

<objective>
Build the restore mechanism — the script and its hermetic proof — that Wave 1 executes against
shared TEST. This plan writes NO byte to TEST or PROD; it produces a script whose every refusal
and every success is observed on a throwaway Postgres first.

Purpose: CONTEXT.md's SUPERSEDING DECISION (2026-09-08) reverses Area 2 Q1: TEST's schema is
rebuilt from the PROD dump `supabase/schema/baseline.sql` at the sha recorded in
`supabase/schema/BASELINE.md:54`, and its ledger is seeded to match PROD's, so that
`supabase db push` can run natively against TEST afterwards (Wave 2). The 40 re-stamped ledger
rows and the 63-vs-32 gap are REPLACED, not repaired. A cut date, a `TEST_APPLY_CUT` constant, a
bespoke forward-only applier, repairing ledger rows one by one, and bulk-applying the 31
baselined migrations are all DEAD SCOPE and do not appear here.

Output: `scripts/restore-test-from-baseline.sh` with `--self-test` (counted arms, real Postgres),
its fixtures, and a vitest that pins its constants. Plan 02 wires it into a dispatch-only workflow;
Plan 03 executes it behind a `checkpoint:decision`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-PATTERNS.md
@scripts/pg-lane/run.sh
@scripts/test-ledger-drift-check.sh
@scripts/prod-body-drift-check.sh
@scripts/check-baseline-staleness.mjs
@supabase/schema/BASELINE.md
</context>

<measured_facts>
Read off the working tree on 2026-09-08 (HEAD `f7ff8b27`); re-measure rather than trust if the
tree has moved:

- `supabase/schema/baseline.sql`: 15,169 lines, sha256
  `27826b7660026a5b6a7fbee9223265ab98f8fbf485b87237498881034c558ec7` (matches `BASELINE.md:54`),
  ZERO data statements, ZERO `CREATE SCHEMA` lines, ZERO mention of `supabase_migrations`.
  Every one of its 190 `CREATE TABLE/FUNCTION/VIEW/TYPE/SEQUENCE` objects is in `public`; every
  `OWNER TO` is `"postgres"` (191); grantees are only `postgres/anon/authenticated/service_role`.
  It carries `ALTER PUBLICATION "supabase_realtime" OWNER TO "postgres";` (`baseline.sql:13668`)
  and six `CREATE EXTENSION IF NOT EXISTS` lines (`pg_cron`, `pg_net`, `pg_stat_statements`,
  `pgcrypto`, `supabase_vault`, `uuid-ossp`). Schema grants and default privileges are restated
  at `baseline.sql:13677-13680` and `:15115-15138`.
- The dump does NOT contain the signup trigger: `on_auth_user_created` is `CREATE TRIGGER … ON
  auth.users` (`supabase/migrations/20260405061912_rls_policies.sql:81`) calling
  `public.handle_new_user()`. `DROP SCHEMA public CASCADE` removes that trigger and the dump
  does not put it back. Same class: `gdpr_exports_admin_read ON storage.objects`
  (`supabase/migrations/20260417110538_sanitize_user.sql:232`) calls
  `public.current_user_has_app_role`. These are the SURVIVORS this script must census and
  re-create.
- `scripts/local-stack/run.sh` `load_baseline()` (`:192-227`) feeds the dump to
  `psql -v ON_ERROR_STOP=1 -q -f` against a `supabase start` stack; `scripts/pg-lane/run.sh`
  (`resolve_pgbin()` `:91`, `alloc_port()` `:125`, role creation `:406`) is the hermetic
  Postgres this repo boots in CI. The pg-lane has pg_cron but NOT `pg_net`/`supabase_vault`, so
  the REAL dump cannot replay on it — the self-test uses a baseline-SHAPED fixture; the real
  dump's applicability to hosted TEST is what the workflow's `preflight` mode (Plan 02/03)
  measures, transactionally, before anyone decides.
- TEST ledger today (CONTEXT): 243 rows, 40 with a `version` matching no repo file; repo has 266
  migration files (`ls supabase/migrations/*.sql | wc -l`).
- Supabase CLI 2.84.2 locally / 2.98.2 pinned in CI: `db push --db-url`, `migration list --db-url`,
  `db dump --db-url --data-only -s <schema>` all exist (help text read this session).
</measured_facts>

<tasks>

<task type="tracer">
  <name>Task 1: `scripts/restore-test-from-baseline.sh` — one transaction, end to end, on a throwaway cluster</name>
  <files>scripts/restore-test-from-baseline.sh, scripts/restore-test-from-baseline-fixtures/old-test.sql, scripts/restore-test-from-baseline-fixtures/baseline-fixture.sql, scripts/restore-test-from-baseline-fixtures/migrations/20260101000000_fixture_a.sql, scripts/restore-test-from-baseline-fixtures/migrations/20260102000000_fixture_b.sql, scripts/restore-test-from-baseline-fixtures/migrations/20260103000000_fixture_c.sql</files>
  <read_first>
    - scripts/pg-lane/run.sh — `resolve_pgbin()` (:91), `alloc_port()` (:125), the D-04 EXIT/INT/TERM trap that stops and removes the cluster, `psqlq()` (:222), role creation (:406). Copy the boot/cleanup SHAPE for `--self-test`; do not call the lane's `--apply/--gate` contract (it is SQL-in/SQL-out and cannot drive a bash script).
    - scripts/test-ledger-drift-check.sh — `default_ledger_query()` (:133-165) for the psql invocation shape (`-X -q -A -t -v ON_ERROR_STOP=1`, `SET statement_timeout`), the SP-M01 rc-checked count idiom (:304-318), the charset refusal of migration basenames (:280-282), the credential assert (:237-246), the temp-dir split traps (:252-270), the self-test `run_arm` helper (:604-619) and `--expect-inverted` (:610-612), unknown-argv refusal (:675-680).
    - scripts/prod-body-drift-check.sh — `arm()` (:468-505): the `${6-…}` not `${6:-…}` trap, captured output grepped for dollar-quotes (redaction is CHECKED, not asserted).
    - scripts/check-baseline-staleness.mjs — how it reads the sha table row from BASELINE.md (copy the regex, cite it), and `EXPECTED_ASSERTIONS` (:154) as the pinned-count idiom.
    - supabase/schema/baseline.sql:1-30 (the SET preamble and `set_config('search_path','')`), :13668 (the publication OWNER line), :13677-13680, :15115-15138.
    - CLAUDE.md § "Which database am I on? (ask FIRST, every time)" (:328) — the marker query, verbatim.
  </read_first>
  <action>
Create `scripts/restore-test-from-baseline.sh` (bash, `set -euo pipefail`, header block pasting the three invocations VERBATIM so CI and a developer run the same program — mode identity, the `sql-gate-lint` idiom):

  bash scripts/restore-test-from-baseline.sh --self-test
  bash scripts/restore-test-from-baseline.sh --run --mode preflight
  bash scripts/restore-test-from-baseline.sh --run --mode restore

**Seams (env, every DB read/write goes through them, defaults are the real path):** `RESTORE_DB_URL` (the SESSION-mode DSN; absent ⇒ `::error::` naming `RESTORE_DB_URL` + exit 1, never a skip — the newest credential-assert shape, `migration-drift-check.yml:102-121`), `BASELINE_FILE` (default `supabase/schema/baseline.sql`), `BASELINE_DOC` (default `supabase/schema/BASELINE.md`), `MIGRATIONS_DIR` (default `supabase/migrations`), `FRESHNESS_TS_CMD` (default: `git log -1 --format=%ct -- <path>`; prints an epoch), `RESTORE_EXPECT_MARKER_RE` (default: case-insensitive, `test` as a whole word), `RESTORE_REFUSE_MARKER_RE` (default: case-insensitive `prod`), `RESTORE_OUT_DIR` (where census files are written; default a mktemp dir), `RESTORE_REQUIRE_MUTEX` (default `1`). `RESTORE_DB_URL` is NEVER echoed; psql stderr may be printed (it names SQL objects, not credentials), and the self-test greps every arm's captured output for a DSN-shaped string and for dollar-quoted SQL and FAILS the arm on a hit.

**`--run` ordering — refusals first, all before the first write, each its own `::error::` line and exit 1:**
1. credential present; 2. `shasum -a 256 "$BASELINE_FILE"` equals the sha in the `| sha256 |` table row of `$BASELINE_DOC` (read with the same regex `check-baseline-staleness.mjs` uses; cite it in a comment) — mismatch or no row ⇒ refuse; 3. freshness: `FRESHNESS_TS_CMD $BASELINE_FILE` must be ≥ `FRESHNESS_TS_CMD $MIGRATIONS_DIR` — a migration committed after the last baseline regeneration means the dump does not describe PROD, refuse and name both epochs; 4. enumerate `$MIGRATIONS_DIR/*.sql` non-recursively, refuse any basename outside `[A-Za-z0-9_.-]`, require ≥ 1, print the count; 5. the marker: run CLAUDE.md's `shobj_description(oid,'pg_database')` query; NULL ⇒ refuse ("re-set the COMMENT ON DATABASE marker before writing — CLAUDE.md § Which database am I on?"); not matching `RESTORE_EXPECT_MARKER_RE` or matching `RESTORE_REFUSE_MARKER_RE` ⇒ refuse; on success print ONLY `which_database: OK — marker names TEST and not PROD` (the marker text itself is not printed: public log); 6. if `RESTORE_REQUIRE_MUTEX=1`: `SELECT count(*) FROM pg_locks WHERE locktype='advisory' AND objid=61616158 AND granted` must be ≥ 1, else refuse ("the shared-TEST mutex is not held — this script runs INSIDE the workflow's Acquire step, never alone").

**Pre-census (read-only, written to `$RESTORE_OUT_DIR/pre-census.txt`, printed):** counts of `public` tables (`pg_tables`), functions (distinct `proname` in `public`), policies (`pg_policies` where schemaname='public'), extensions present, ledger row count and the count of ledger rows whose `version` is not 14 digits; a census of public objects NOT owned by `postgres` (relations and functions — the founder must see this before deciding, because `postgres` cannot drop what it does not own on hosted Supabase); and the SURVIVORS with their DDL: (a) non-internal triggers on tables outside `public` whose function is in `public` — key `schema.table:trigger`, DDL from `pg_get_triggerdef(oid)`; (b) policies on tables outside `public` whose `qual`/`with_check` text references `public.` — key `schema.table:policy`, DDL composed from `pg_policies` (permissive/restrictive, cmd, roles, USING, WITH CHECK, NULL-safe, identifiers via `format('%I')`); (c) `pg_publication_tables` rows whose schemaname='public' — key `pub:public.table`, DDL `ALTER PUBLICATION %I ADD TABLE public.%I`. Survivor keys and DDL are written to `$RESTORE_OUT_DIR/survivors.sql` and the key list to `survivors.keys`.

**Expected-shape numbers derived from the DUMP TEXT, not from the database:** tables = count of lines matching `^CREATE TABLE`, policies = count of `^CREATE POLICY`, function names = the distinct names `node scripts/sql-body-normalize.mjs --function-names` prints for the dump (the ONE shared normalizer; cite `test-ledger-drift-check.sh:85-87`). Print all three.

**The transaction stream, assembled in `$RESTORE_OUT_DIR/restore.sql` and fed to ONE psql session (`psql "$RESTORE_DB_URL" -X -v ON_ERROR_STOP=1 -f`), in this order:** `BEGIN;` · `SET LOCAL lock_timeout = '30s';` (fail loud rather than queue behind a foreign lock) · `SET LOCAL statement_timeout = 0;` · `DROP SCHEMA public CASCADE;` · `CREATE SCHEMA public;` · the dump, with exactly ONE line class filtered out: `^ALTER PUBLICATION "supabase_realtime" OWNER TO` (on hosted Supabase that publication is owned by `supabase_admin` and `postgres` may not reassign it; on the lane it would succeed, which is why the filter is asserted by COUNT — the script prints `filtered: N line(s) matching <pattern>` and the self-test pins N=1 on the fixture) · `survivors.sql` · the ledger: `CREATE SCHEMA IF NOT EXISTS supabase_migrations;` and `CREATE TABLE IF NOT EXISTS supabase_migrations.schema_migrations (version text NOT NULL PRIMARY KEY, statements text[], name text);` (the CLI's own DDL, `pkg/migration/history.go:15-17` in supabase/cli v2.98.2 per RESEARCH A.3(6)), `TRUNCATE supabase_migrations.schema_migrations;`, then one `INSERT INTO supabase_migrations.schema_migrations(version, name, statements) VALUES ('<14-digit prefix>', '<description after the first underscore>', ARRAY['<provenance>'])` per migration file, provenance = a single constant sentence: `SEEDED <YYYY-MM-DD> by scripts/restore-test-from-baseline.sh from supabase/schema/baseline.sql sha256 <sha>: schema restored from the PROD dump; this file's SQL was NOT executed on TEST` (basenames are charset-refused above so no quoting hazard; still escape single quotes) · assertions as `DO $restore$ … RAISE EXCEPTION … $restore$;` blocks that compare, INSIDE the transaction: public table count = expected, policy count = expected, distinct function-name count = expected, ledger row count = migration file count, ledger rows with a non-14-digit version = 0, and every survivor key present again (re-run the same three census queries and compare the key SET to `survivors.keys` — a missing key raises naming it) · `NOTIFY pgrst, 'reload schema';` · `COMMIT;` when `--mode restore`, `ROLLBACK;` when `--mode preflight`. Any error ⇒ psql exits 3 ⇒ the session ends ⇒ Postgres rolls back ⇒ the script prints `::error::restore aborted at <the psql error line>; the transaction was rolled back, TEST is unchanged` and exits 1. Never hand psql its own whole-file transaction flag (its COMMIT is unconditional; the MODE decides the terminator, and Task 2 pins that the flag's spelling is absent from the script — so do not spell it in a comment either).

**Post (both modes):** re-run the pre-census read-only, print it as `post-census`, and in `preflight` assert post == pre byte-for-byte (the rollback is MEASURED, not assumed — a preflight that changed anything is a defect); in `restore` assert post matches the expected shape and print one summary line: `restore: tables=<n> policies=<n> functions=<n> ledger_rows=<n> survivors=<k>/<k> filtered=<n> mode=restore` — Plan 03 pastes this line into the SUMMARY.

**`--self-test` (hermetic, pinned `EXPECTED_ARMS`):** boot a throwaway cluster the pg-lane way (`PGBIN` from `bash scripts/pg-lane/run.sh --print-pgbin`, port from the node one-liner, `initdb` + `pg_ctl -o "-c listen_addresses=127.0.0.1"`, EXIT trap stops and removes it — cite the lane's D-04 comment), create roles `anon authenticated service_role`, load `old-test.sql` (public: a stray table `e2e_leftover`, a function `public.fx_survivor()` and `public.fx_role()`; schema `auth` with table `users`; a trigger `on_auth_user_created` ON `auth.users` calling `public.fx_survivor()`; schema `storage` with table `objects` and a policy whose USING calls `public.fx_role()`; a publication `supabase_realtime` FOR TABLE `public.e2e_leftover`; `supabase_migrations.schema_migrations` with four rows, one of them re-stamped (`version` = an apply timestamp, `name` = a filename), one file-less; `COMMENT ON DATABASE postgres IS 'quantalyze TEST fixture'`), and hold the mutex from a background psql (`SELECT pg_advisory_lock(61616158); SELECT pg_sleep(600);`) exactly as `ci.yml:2610` does. `baseline-fixture.sql` is baseline-SHAPED: the same SET preamble as the real dump's first 12 lines, `CREATE EXTENSION IF NOT EXISTS "pgcrypto"`, two tables, one policy, two functions including `public.fx_survivor()` and `public.fx_role()` (so the survivors can be re-created), the `ALTER PUBLICATION "supabase_realtime" OWNER TO "postgres";` line (so the filter has something to count), and the schema GRANT USAGE lines. Three fixture migrations in `fixtures/migrations/`. A fixture `BASELINE.md`-shaped doc is generated in the temp dir with the fixture's real sha so arm "sha mismatch" can edit it.

Arms, each run through the REAL `--run` dispatch with `RESTORE_DB_URL` pointed at the lane, exit code AND a named output line asserted (`run_arm <label> <want-exit> …` shape from `test-ledger-drift-check.sh:604-619`, and ONE `--expect-inverted` calibration arm proving the harness can fail):
  1. RED credential absent ⇒ exit 1, output names `RESTORE_DB_URL`; 2. RED marker NULL ⇒ exit 1 before any write (assert the stray table still exists); 3. RED marker names PROD (`COMMENT … IS 'quantalyze PROD fixture'`) ⇒ exit 1; 4. RED sha mismatch (edit the fixture doc) ⇒ exit 1; 5. RED stale baseline (stub `FRESHNESS_TS_CMD` to return baseline < migrations) ⇒ exit 1; 6. RED mutex not held (release the background holder for this arm) ⇒ exit 1; 7. GREEN preflight ⇒ zero exit status, post-census == pre-census, stray table still present, ledger still 4 rows, output contains `mode=preflight`; 8. GREEN restore ⇒ zero exit status, output line `restore: tables=2 policies=1 functions=2 ledger_rows=3 survivors=3/3 filtered=1 mode=restore`, stray table GONE, `on_auth_user_created` present on `auth.users`, the storage policy present, the publication holds the fixture table that survived (or the key is reported dropped by name if its table is not in the fixture dump — pick the fixture so it survives), ledger rows exactly the three fixture files with 14-digit versions and description-only names; 9. RED survivor cannot be re-created (arm fixture: a trigger on `auth.users` calling a public function ABSENT from the baseline fixture) ⇒ exit 1, transaction rolled back (stray table still present), output names the survivor key; 10. RED assertion mismatch (stub the expected table count by pointing `BASELINE_FILE` at a copy with one extra `CREATE TABLE` line appended AFTER the doc sha is regenerated for it) ⇒ exit 1 and rolled back; 11. REDACTION: every arm's captured output is grepped for `postgres(ql)?://` and for `\$[A-Za-z_0-9]*\$` — a hit fails that arm; 12. unknown argv ⇒ exit 1.
Pin `EXPECTED_ARMS=12` as a literal constant beside a `MEASURED 2026-09-08` comment, assert `total -eq EXPECTED_ARMS` and `pass -eq total`, and print the closing line `restore-test-from-baseline: self-test OK (<pass>/<EXPECTED_ARMS> arms — six refusals fire before any write, preflight rolls back byte-for-byte, restore commits the full shape, survivors round-trip, redaction checked)`.

Threats: the pre-drop census is the ONLY record of survivors — write `survivors.sql` to `$RESTORE_OUT_DIR` before the transaction starts so Plan 02's backup artifact captures it even when the transaction aborts (T-164.8-03).
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&1 | tee /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/restore-selftest.log | grep -E 'self-test OK \(12/12 arms' && ! grep -aE 'postgres(ql)?://' /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/restore-selftest.log</automated>
  </verify>
  <done>`--self-test` prints `12/12 arms` and exits 0 on a throwaway cluster; each RED arm was observed red (the harness's `--expect-inverted` calibration proves the harness itself can fail); `git status --porcelain` is clean after the run (nothing leaked into the checkout); no DSN-shaped string or dollar-quoted SQL appears in the captured self-test output.</done>
</task>

<task type="auto">
  <name>Task 2: Observe the refusals RED on a NEUTERED scratch copy, then pin the constants in vitest</name>
  <files>src/__tests__/restore-test-from-baseline.test.ts</files>
  <read_first>
    - src/__tests__/local-stack-lane-wiring.test.ts:124-140 — the LINE-EXACT non-comment pin (whole-file `toContain` stays green with the line commented out; MEASURED 2026-09-08 twice in this repo).
    - src/__tests__/prod-prober-wiring.test.ts:13-17 (CALIBRATION: mutate a copy, assert the copy differs, assert the predicate flips) and :93-96 (`SOFTENING_TOKENS`).
    - src/__tests__/gate-family-meta.test.ts:18-30 — SC-9: every threshold constant needs a measurement token AND a date beside it.
    - src/__tests__/mutation-runner-floors.test.ts:94-111 (`maskJsComments`) — the idea, applied to bash: mask `#` comments before asserting a CALL exists.
  </read_first>
  <action>
Write `src/__tests__/restore-test-from-baseline.test.ts`. All reads through `node:fs` (never shell grep — this repo has a measured NUL-blind file). Slice the script text into its `--run` function body and its `self_test` body by the function-definition lines so pins are asserted against the REGION, not the whole file. Pins (each with a calibration twin that applies the mutation to a COPY of the text in memory and asserts the predicate flips):
  - `EXPECTED_ARMS=12` on a live (non-comment) line, exactly once, with `MEASURED 2026-09-08` on the same or the preceding line (SC-9);
  - the terminator is mode-selected: the `--run` region contains the literal `ROLLBACK;` and the literal `COMMIT;` each on a live line, and psql's whole-file transaction flag (the one spelled `--single-` followed by `transaction`) appears NOWHERE in the script — assemble the literal in the test from two halves so this plan and the test never carry it verbatim (a copy with the flag inserted flips the pin);
  - the ordering invariant: the line index of the marker query (`shobj_description`) is LOWER than the line index of `DROP SCHEMA public CASCADE` and LOWER than the first `TRUNCATE`, inside the `--run` region (masked of comments);
  - the survivors census runs before the DROP: index of `pg_get_triggerdef` < index of `DROP SCHEMA public CASCADE`;
  - the filtered-line pattern is present exactly once as a live line and names `supabase_realtime`;
  - the credential assert names `RESTORE_DB_URL` inside a `::error::` line and no softening token from the `SOFTENING_TOKENS` list appears in the `--run` region (comments masked) — the only `exit` in the credential branch is non-zero;
  - the ledger INSERT column list is exactly `(version, name, statements)` and the provenance sentence contains `NOT executed on TEST`;
  - the fixtures dir contains exactly three `*.sql` files under `migrations/` with 14-digit prefixes (the self-test's `ledger_rows=3` is derived from a DIRECTORY, so a fourth fixture silently changes the arm — pin the count with an AIM: the same glob applied to a synthetic listing of five names returns five).
Run the vitest file alone, then the two `--self-test` commands again, and paste the vitest line count into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/restore-test-from-baseline.test.ts</automated>
  </verify>
  <done>The vitest passes; every calibration twin demonstrates its pin flips on a mutated in-memory copy; the SUMMARY records for each pin the mutation that was applied and that it went RED.</done>
</task>

<task type="auto">
  <name>Task 3: Record the measured self-test and the fixture design in the script header and SUMMARY</name>
  <files>scripts/restore-test-from-baseline.sh</files>
  <action>
Finish the script header (comments only): what `--self-test` proves and what it does NOT — it proves the mechanism on a baseline-SHAPED fixture; it cannot replay the REAL dump because the pg-lane has no `pg_net`/`supabase_vault` (state this; the real dump's applicability to hosted TEST is measured by the workflow's `preflight` mode inside a rolled-back transaction, Plan 02/03). State the public-repo rule: names and counts only in logs, never a DSN, never a body. State the two things this script deliberately does NOT do: it does not back up (the workflow does, BEFORE calling it — Plan 02), and it does not take the mutex (it asserts the mutex is held by someone and refuses otherwise). Paste the verbatim closing self-test line and its date beside `EXPECTED_ARMS`. Re-run `--self-test` after the header edit to prove the header sentence about the marker did not add a matching literal to a `.sql` file (the marker regexes scan DB output, not files — but the SUMMARY must say the re-run was done). Commit.
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&1 | grep -E 'self-test OK \(12/12 arms' && git status --porcelain | grep -c . | grep -qx 0</automated>
  </verify>
  <done>Header states scope and non-scope; self-test still 12/12; working tree clean after the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → shared TEST Postgres | a destructive DDL transaction crosses here (Plan 02/03); this plan only builds the code that will cross it |
| repo file text → SQL | migration basenames and the dump text are interpolated into a SQL stream |
| public Actions log | every printed line is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-01 | Elevation | `--run` against the wrong database | critical | mitigate | marker query is the first DB statement; NULL / non-TEST / PROD-naming ⇒ refuse before any write; arm 2 and arm 3 prove it |
| T-164.8-02 | Tampering | a stale or unreviewed dump replayed onto TEST | high | mitigate | sha must equal BASELINE.md's row (arm 4); baseline must be at least as fresh as the last migration commit (arm 5) |
| T-164.8-03 | Denial of service | CASCADE drop silently removes `auth.users` trigger / storage policies / publication rows | high | mitigate | survivors census before the drop, DDL replayed after, key-set equality asserted INSIDE the transaction (arms 8, 9); census file written before the transaction so the backup captures it |
| T-164.8-04 | Tampering | SQL injection through a migration basename | medium | mitigate | charset refusal `[A-Za-z0-9_.-]` before interpolation (VAC-08 idiom), single quotes escaped anyway |
| T-164.8-05 | Information disclosure | DSN or SQL body in a public log | high | mitigate | DSN never echoed; self-test greps captured output for DSN shapes and dollar-quotes and fails on a hit (arm 11) |
| T-164.8-06 | Repudiation | a self-test that shrank but still prints PASSED | medium | mitigate | `EXPECTED_ARMS` pinned as a literal, `total -eq EXPECTED_ARMS` asserted, vitest pins the literal line-exact |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | no package is installed by this plan (bash + psql + node built-ins) |
</threat_model>

<verification>
- `bash scripts/restore-test-from-baseline.sh --self-test` → `12/12 arms`, exit 0, on macOS (local) — and, once Plan 02 wires it, on ubuntu inside the workflow's self-test step.
- `npx vitest run src/__tests__/restore-test-from-baseline.test.ts` green, with each pin's calibration twin recorded RED in the SUMMARY.
- `git status --porcelain` empty after the self-test (the script mutates only its scratch dir).
</verification>

<success_criteria>
1. Six refusals fire BEFORE any write and each is observed red (arms 1-6).
2. Preflight is provably a no-op: post-census equals pre-census byte-for-byte (arm 7).
3. Restore commits the full shape in one transaction and survivors round-trip by name (arm 8); a survivor that cannot be re-created aborts the whole transaction (arm 9).
4. The ledger seed is CLI-shaped and honest: 14-digit versions, description-only names, a provenance sentence that says the SQL was not executed on TEST.
5. What would make this RED: any arm count other than 12/12; a DSN in the captured output; psql's whole-file transaction flag appearing anywhere in the script; the marker query moving below the DROP.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-01-SUMMARY.md` when done, pasting the verbatim self-test closing line and the per-pin calibration record.
</output>
