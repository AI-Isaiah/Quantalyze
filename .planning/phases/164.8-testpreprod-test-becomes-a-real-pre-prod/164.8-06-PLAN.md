---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 06
type: execute
wave: 6
depends_on:
  - 164.8-05
files_modified:
  - TODOS.md
  - CLAUDE.md
  - docs/runbooks/shared-test-db-mutex.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE
  - 164.5-TEST-VAC08-DROP-UNAPPLIABLE

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Area 3 `TEST-NOT-APPLICABLE` pragma is recorded, in the TODOS entry that asked for it, as UNNECESSARY FOR THIS MIGRATION with the measurement that shows it — and the GENERAL case is recorded as OPEN because the restore is schema-only (B4), with the escape hatch booked as a named TODOS entry AND routed to a phase (founder rule 2026-09-08: every deferral names a phase)."
    - "Every TODOS entry this phase closes carries the run id / sha that closed it; the three entries this phase OPENS carry the mechanism, the trigger condition and the founder-owned escape hatches."
    - "The first SHA-bound `sql-tests` run after Plan 04's PR prints `ledger presence: 0 absent, all 0 baselined … 0 NEW drift.` and is recorded — the ROADMAP criterion that moved here from 164.5 is closed on what the tool PRINTS."
    - "CLAUDE.md's 'Which database am I on?' section and the mutex runbook describe the world AFTER this phase (two new holders; TEST mirrors PROD's CATALOGUE, not its data; apply-on-merge; the ref guard), dated, beside the older paragraphs kept as lineage — and never state the false general premise."
  artifacts:
    - "TODOS.md — closures with reasoning; new `[164.8-PUSH-RACE-VAC08]`, `[164.8-TEST-DATA-RESEEDED]` and `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` entries, the last routed to Phase 164.9"
    - "CLAUDE.md — a dated CURRENCY paragraph under 'Which database am I on?'"
    - "docs/runbooks/shared-test-db-mutex.md — the fourth and fifth holders named in § 1, with their TTLs in § 2's table"
  key_links:
    - "The pragma verdict cites Plan 04's read-only reading `create_allocator_connected_strategy overloads on TEST = 0` AFTER the restore and VAC-08's `0 absent` line — the object is in neither PROD, the dump, nor TEST, and the ledger row is present, so there is nothing for a pragma to declare FOR THAT MIGRATION; the same paragraph states why that does not generalise"
    - "`[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`'s trigger condition is the verdict job's failure cause (3) from Plan 05: `apply-test` RED on a data-reading RAISE while PROD's `plan` dry-run is clean"
---

<objective>
Close the books: record the verdicts and the SHA-bound readings, and update the three documents
that tell the next reader which database they are on and who holds the mutex.

Purpose: CONTEXT told planning to settle the pragma question explicitly and to treat the ratchet
file's emptying as a deliverable; the ROADMAP's requirement line names three TODOS entries this
phase exists to close. A closure without its evidence is the defect class this milestone
removes, so every sentence written here quotes a run id or a sha from Plans 04-05.

Revision 2026-09-08 (plan-checker REVISE, B4): the original verdict — "TEST mirrors PROD by
construction, so a DROP valid for PROD is valid for TEST; DEAD SCOPE" — rests on a premise that
is false in the DATA dimension. The restore is schema-only (`BASELINE.md`: 0 data statements);
TEST's public tables start empty and diverge from PROD's rows the moment e2e runs. This repo's
house style is data-reading DO blocks that `RAISE EXCEPTION`; one that reads a PROD-populated
table and refuses on an unexpected count applies cleanly to PROD and refuses on TEST, and under
Area 1 Q2 (LOCKED — not reopened here) that blocks the PROD apply. The retired pragma was
precisely the declared escape hatch. The verdict is therefore rewritten as "unnecessary for THIS
migration; general case OPEN", the false premise is shipped NOWHERE (not TODOS.md, not
CLAUDE.md, not the ratchet header — Plan 04 already carries the corrected wording), and the
escape hatch is booked as a named entry with its trigger condition AND routed to a phase.

Which phase (constraint: name a phase, and check the two unstarted ones before proposing a new
one): Phase 164.10 BODYDRIFT is about three PROD function BODIES and has no data-contract scope
— not the home. Phase 164.9 TESTISOLATION's goal is what a run may assume about TEST's rows
("the restore changes what a clean starting state even means", its own ROADMAP note), and a
data-reading migration refusing on TEST is exactly a claim about what TEST's rows are — so
164.9 is the structural home. ⚠️ FLAG FOR THE ORCHESTRATOR: add to Phase 164.9's ROADMAP entry,
via `/gsd-phase --edit 164.9`, the sentence "SCOPE ADDED 2026-09-08 from 164.8 (B4): the TEST
data contract — what a migration may assume about TEST's rows, and the disposition for a
PROD-valid data-reading migration that refuses on TEST (`[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`);
the retired `TEST-NOT-APPLICABLE` pragma is one candidate shape, a per-run seed contract is the
other — decide there, not by inertia." This plan's Task 2 VERIFIES that sentence is present and
applies it (scoped `Edit`) only if the orchestrator has not.

Output: TODOS.md, CLAUDE.md, the runbook, and the dated ROADMAP notes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@TODOS.md
@docs/runbooks/shared-test-db-mutex.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TODOS — close five entries with evidence, record the corrected pragma verdict, open three</name>
  <files>TODOS.md</files>
  <read_first>
    - Read Plan 04's and Plan 05's SUMMARYs (`164.8-04-SUMMARY.md`, `164.8-05-SUMMARY.md`) — every closure below quotes a run id or sha from them.
    - TODOS.md:1378-1410 — the 164.2-TEST-APPLY-PROVENANCE entry.
    - TODOS.md:1435-1462 — the 164.5-TEST-VAC08-DROP-UNAPPLIABLE entry.
    - TODOS.md:1474-1510 — the 164.7-TEST-APPLY-APPSETTINGS entry.
    - TODOS.md:1519-1548 — the 164.5-TEST-EXCEPT-DRIFT04 entry.
    - TODOS.md:2362-2390 — the CI-MIGRATE-01 section.
    - TODOS.md:6972-6990 — the VAC08-LEDGER-32 entry.
    - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql:925-935 — `SELECT enabled INTO v_seeded` followed by `RAISE EXCEPTION`: the data-reading DO-block-that-RAISEs house style the escape-hatch entry cites (a PROD-populated read that would refuse on an empty table).
    - The repo's closure style: project CLAUDE.md's 164.4.1 closing reading (`[REDUNDER-PGCRON]` / `[REDUNDER-LANEBLOCKED-BLIND]` closed WITH their reasoning, the second deliberately).
  </read_first>
  <action>
Mark each entry `[x]` and append a dated closure paragraph, quoting Plan 04/05 SUMMARY readings:
  - `[164.2-TEST-APPLY-PROVENANCE]` and `[164.7-TEST-APPLY-APPSETTINGS]`: the migrations reached TEST by the restore (run id, sha) — TEST's schema is the PROD dump that already contains them (BASELINE.md's 2026-09-07/08 regeneration tables list them by name) and the ledger holds their rows; the "record the first green run's SHA" step each entry asked for is satisfied by Task 2's reading. State that the hand-apply remedy steps are superseded by the `apply-test` job for every future migration.
  - `[164.5-TEST-VAC08-DROP-UNAPPLIABLE]`: closed — and the DISPOSITION IT ASKED FOR WAS NOT BUILT, for THIS migration, deliberately, with the general case left OPEN and routed. Write the verdict in two parts. (1) NARROW, evidenced: Area 3's pragma was designed for a TEST that was built from `supabase/migrations/` and therefore lacked the hand-created PROD function; after the restore TEST is a copy of PROD's catalogue, `create_allocator_connected_strategy` has 0 overloads on TEST (Plan 04 reading) exactly as on PROD, and the ledger row for `20260908120000` is present, so VAC-08 prints `0 absent` with no pragma. Record the three refused fixes as still refused (no `IF EXISTS`, no baseline line, no hand-apply) and that none was needed. (2) GENERAL, open (B4): the restore was SCHEMA-ONLY — `BASELINE.md` records 0 data statements — so TEST mirrors PROD's CATALOGUE and never its DATA; a migration in this repo's house style (a data-reading DO block that `RAISE EXCEPTION`s on an unexpected count, e.g. the one in `20260907130000_ledger_refresh_switch_to_system_flags.sql`) that reads a PROD-populated table applies cleanly to PROD and refuses on empty TEST, and under Area 1 Q2 that BLOCKS the PROD apply. The pragma was the declared escape hatch for exactly that moment. It is NOT built here because it has no consumer today (CONTEXT: a mechanism nobody needs is the vacuity this milestone removes), and a parser-only seam would be dead code behind a passing test (the reasoning that closed `[REDUNDER-LANEBLOCKED-BLIND]`). The disposition is booked as `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` below and routed to Phase 164.9. ⛔ Neither the PROD-validity-implies-TEST-validity sentence the checker quoted nor the words DEAD SCOPE may appear anywhere in this closure (the verify negative-greps the former).
  - `[164.5-TEST-EXCEPT-DRIFT04]`: closed — its own expiry condition ("stops being an exception when baseline.sql is next regenerated from PROD after this removal applies") was met on 2026-09-08 (BASELINE.md "pure deletion, 91 lines"), and Plan 04 restored TEST from that regeneration; there is no apply list in 164.8 to exclude it from because `supabase db push` runs natively against a ledger that already holds it.
  - `### CI-MIGRATE-01`: closed by `supabase-migrate.yml`'s `apply-test` job (Plan 05 run id, headSha) — with the precision that the founder chose apply-ON-MERGE, not apply-before-`sql-tests`: PR-time SQL gates with applied-ness probes stay red until merge (see the new entry below); the three named hazards are answered — (1) the apply holds the mutex, (2) the first apply surfaced nothing because the restore made TEST current first, (3) every previously-silent gate is now armed, and the first honest green is Task 2's SHA. Add: the dispatch path is ref-guarded to `main` (B3, Plan 05).
  - `[VAC08-LEDGER-32]`: closed — 31 → 0 by the restore, not by 31 hand-applies; the file is empty beside an AIM (Plan 04).
Open THREE entries under the phase's dated heading:
  - `[164.8-PUSH-RACE-VAC08]` (the text Plan 05's SUMMARY hands over: the merge-time mutex race between `sql-tests` and `apply-test`, Railway skipping on a red main, the founder-owned escape hatches; and the PR-time red for migration PRs by construction). Phase: this is an Area 1 Q1 consequence the founder accepted; route it to Phase 164.9 as well (same data/ordering contract) and say so.
  - `[164.8-TEST-DATA-RESEEDED]` (public-table data on TEST was destroyed by the restore and is NOT reversible — W6; record which e2e/seed paths re-created what on the first main CI run after Plan 04 — read off that run, and if any spec failed for a missing pre-existing row, name it here rather than re-seeding by hand). Phase: 164.9 (per-run seed contract).
  - Record WHY the GitHub `Test` environment was KEPT rather than deleted (founder decision 2026-09-08, after explicitly reconsidering): NOT for access control — measured, it adds none, since `workflow_dispatch` runs the file from the selected ref and both the branch policy and the guard job fall to the same branch edit — but for the durable **Deployments audit record** (actor / ref / sha / timestamp) of every destructive rebuild of shared TEST, which outlives workflow-log retention. Configured `main`-only, 0 reviewers, 0 secrets, 0 variables, so it adds no per-merge friction and cannot shadow the 39 repo-level `TEST_SUPABASE_DB_URL` references measured in `ci.yml` (33) and `mutex-probe.yml` (6).
  - `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` (B4) with: TRIGGER — `apply-test-verdict` reports failure cause (3): `apply-test` RED on a `RAISE` from a data-reading DO block while PROD's `plan` dry-run is clean; INTERIM REMEDY — revert the merge (Area 1 Q2 is locked; editing `supabase-migrate.yml` under deploy pressure is the failure mode this entry exists to prevent); CANDIDATE SHAPES — the retired `TEST-NOT-APPLICABLE` header pragma parsed by ONE piece of code both `apply-test` and VAC-08 read, with the three-arm proof CONTEXT Area 3 specified (RED unpragma'd-absent, GREEN pragma'd, RED pragma-whose-reason-is-false), OR a per-run seed contract that makes the read valid on TEST; PHASE — **164.9 TESTISOLATION** (the phase that defines what TEST's rows are; 164.10 BODYDRIFT was checked and is function-body scope only). State that this entry has an owner, a trigger and a phase because a TODOS line alone has none of the three (founder rule 2026-09-08).
  </action>
  <verify>
    <automated>grep -c '\[164.8-PUSH-RACE-VAC08\]' TODOS.md | grep -qv '^0$' && grep -c '\[164.8-TEST-DATA-RESEEDED\]' TODOS.md | grep -qv '^0$' && grep -c '\[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE\]' TODOS.md | grep -qv '^0$' && grep -n 'DATA-DEPENDENT-MIGRATION-ESCAPE' TODOS.md | grep -q '164.9' && grep -n 'TEST-NOT-APPLICABLE' TODOS.md | grep -qi 'not built' && ! grep -Eqi 'valid for PROD is valid for TEST|mirrors PROD by construction|TEST mirrors PROD[^.]{0,40}so a (DROP|migration)' TODOS.md</automated>
  </verify>
  <done>Five entries closed with run ids/shas; the pragma verdict is written where the pragma was requested, narrow-and-evidenced plus general-and-open; three new entries carry mechanisms, triggers, owners and a phase.</done>
</task>

<task type="auto">
  <name>Task 2: Record the SHA-bound first honest green of VAC-08, and update ROADMAP notes (including the 164.9 routing)</name>
  <files>.planning/ROADMAP.md</files>
  <precondition>Plan 04's PR (empty ratchet file, `ENTRY_COUNT = 0`) and Plan 05's PR are merged; at least one `ci.yml` run on `main` has completed since.</precondition>
  <action>
Find the first `main` CI run after Plan 04's merge: `gh run list --workflow=CI --branch main -L 5 --json databaseId,headSha,conclusion,createdAt`; read the `sql-tests` job log via `gh api …/jobs/<id>/logs` and quote the VAC-08 lines verbatim: `Repo migrations: 266`, `ledger presence: 0 absent, all 0 baselined (see …); 0 NEW drift.`, the body-comparison summary, and the job conclusion. Memory rule: the green must be SHA-bound (`headSha` quoted) — a settled board can belong to an ancestor. If that run is RED on `sql-tests` for any arm, record the arm by name: an applied-ness probe that is still red AFTER the restore means the dump did not contain what the probe asserts, which is a real finding (DRIFT class), not this phase's regression — book it, do not paper over it. Then, with `Edit` (scoped, never a whole-file write): (a) in the Phase 164.8 section, replace the `**Plans:** 0 plans` / `TBD` block with the SIX-plan list (the orchestrator's `update_roadmap` step may already have done this — check first, do not duplicate); (b) under Phase 164.5's criterion 5 (search the 164.5 section for "criterion 5" / the VAC-08 line rather than trusting a line number) append a dated sentence: the stronger goal it deferred to 164.8 — TEST genuinely holding these migrations — is met by the restore (run id), and VAC-08's SHA-bound reading is now `0 absent`; (c) in the Phase 164.8 goal block's SCOPE ADDED paragraph append a dated sentence pointing at TODOS `[164.5-TEST-VAC08-DROP-UNAPPLIABLE]`'s closure for the pragma verdict (narrow closed, general open); (d) B4 routing: check Phase 164.9's entry for the sentence the objective flagged for the orchestrator (`164.8-DATA-DEPENDENT-MIGRATION-ESCAPE` must appear in the 164.9 section); if absent, append it there with a scoped `Edit` in the exact wording given in this plan's objective, and record in the SUMMARY whether the orchestrator or this task wrote it.
  </action>
  <verify>
    <automated>grep -n '0 absent, all 0 baselined' .planning/ROADMAP.md | grep -q . && grep -n '164.8-01-PLAN.md' .planning/ROADMAP.md | grep -q . && grep -n '164.8-06-PLAN.md' .planning/ROADMAP.md | grep -q . && awk '/^### Phase 164.9:/{f=1} /^### Phase 164.10:/{f=0} f' .planning/ROADMAP.md | grep -q 'DATA-DEPENDENT-MIGRATION-ESCAPE'</automated>
  </verify>
  <done>The ROADMAP carries the SHA-bound VAC-08 reading, the six-plan list, and the 164.9 routing sentence; any residual red arm is named, not hidden.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md currency paragraph and the runbook's new holders</name>
  <files>CLAUDE.md, docs/runbooks/shared-test-db-mutex.md</files>
  <read_first>
    - CLAUDE.md:328-350 — the "Which database am I on?" section, and the dated-CURRENCY style of the SQL gate integrity section above it.
    - docs/runbooks/shared-test-db-mutex.md:20-73 — § 1, who holds the lock and how to spot each holder by application name.
    - docs/runbooks/shared-test-db-mutex.md:75-110 — § 2, the coupled numbers table.
    - docs/runbooks/shared-test-db-mutex.md:317-340 — § 5, the drill's "run when CI is quiet" advice.
  </read_first>
  <action>
CLAUDE.md, under "Which database am I on?", append a dated paragraph (keep every existing sentence): as of the restore run (id, sha) TEST's `public` schema is the PROD dump at `baseline.sql` sha `27826b76…` and its ledger is CLI-shaped (one row per repo file, `statements[]` a provenance sentence — NOT the applied SQL, unlike PROD's ledger, which the memory rule "PROD ledger stores the APPLIED SQL" describes; say the difference plainly so nobody reads TEST's `statements` as evidence of execution); ⚠️ B4, in these words: TEST mirrors PROD's CATALOGUE, never its DATA — every public table started EMPTY at the restore and holds only what e2e seeds since, so a data-reading migration that is valid on PROD can refuse on TEST and block the PROD apply by design (`[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`, Phase 164.9; interim remedy: revert the merge); every merge touching `supabase/migrations/**` now applies to TEST FIRST via `supabase-migrate.yml` `apply-test` (`environment: Test`, no reviewers, ref-guarded to `main` on dispatch) and PROD's `apply` waits on it AND on the `Production` environment's human reviewer (since 2026-09-08); the marker check is the first DB statement in both jobs; `scripts/vac08-ledger-baseline.txt` is EMPTY by measurement beside an AIM and grows only by founder decision; and the two NEW couplings — merge-time race with `sql-tests`, PR-time red for migration PRs — are booked as `[164.8-PUSH-RACE-VAC08]`. Also correct the section's sentence "The CLI and the browser SQL editor have no automated guard at all" with a dated addendum: the two CI jobs that write to TEST now carry the marker guard; the developer CLI and the browser editor still do not. ⛔ Do not write "TEST mirrors PROD by construction" without the catalogue/data qualifier.
Runbook § 1: add `apply-test` (`supabase-migrate.yml`, holds across marker → dry-run → push → list) and `restore` (`test-restore-from-baseline.yml`, dispatch-only, holds across backup → transaction → post-verify) as holders four and five, both with the same `PGAPPNAME` the ci.yml step sets (they copy the ci.yml step byte-for-byte, so they are indistinguishable from `sql-tests` in `pg_stat_activity` except by `query`/timing — say so, and say that is acceptable because the release step's dead-holder witness and the guarded terminate are the same). § 2: add both jobs' `timeout-minutes: 90` to the coupled-numbers table with the sentence that raising the acquire cap now moves FIVE TTLs. § 3 (manual unlock): add the one new consideration — a wedged holder now delays a PROD deploy, so the census in § 3 is the first thing to run when `apply-test-verdict` reports a timeout.
  </action>
  <verify>
    <automated>grep -n 'apply-test' docs/runbooks/shared-test-db-mutex.md | grep -q . && grep -n 'test-restore-from-baseline' docs/runbooks/shared-test-db-mutex.md | grep -q . && grep -n '164.8-PUSH-RACE-VAC08' CLAUDE.md | grep -q . && grep -n 'DATA-DEPENDENT-MIGRATION-ESCAPE' CLAUDE.md | grep -q . && ! grep -Eqi 'valid for PROD is valid for TEST|mirrors PROD by construction|TEST mirrors PROD[^.]{0,40}so a (DROP|migration)' CLAUDE.md</automated>
  </verify>
  <done>Both documents describe the post-phase world with dates and run ids, older paragraphs intact as lineage; the catalogue-not-data distinction is stated where TEST's parity is described.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → the next operator's decision | a stale or false sentence here sends a human to write to the wrong database, or to edit a workflow under deploy pressure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-21 | Repudiation | closures without evidence | medium | mitigate | every closure quotes a run id + sha from Plans 04/05; the VAC-08 line is pasted verbatim, SHA-bound |
| T-164.8-22 | Information disclosure | a DSN or marker text copied into docs | medium | mitigate | docs name jobs, keys, counts and run ids only |
| T-164.8-23 | Tampering | ROADMAP whole-file overwrite | high | mitigate | `Edit` scoped replacements only, never `Write` |
| T-164.8-28 | Repudiation | the false "valid for PROD is valid for TEST" premise reaching TODOS/CLAUDE.md (B4) | medium | mitigate | each verify negative-greps THREE phrasings via `grep -Eqi` (the literal sentence, `mirrors PROD by construction`, and the `TEST mirrors PROD … so a DROP/migration` reworded form), not one literal string — plan-check iteration 2 concern 2; the escape hatch is booked with a trigger and a phase |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `grep` gates above; a re-read of the three documents by the executor for any sentence the phase made false.
- Full `npm test` alone on the box before `/gsd-ship`.
</verification>

<success_criteria>
1. The pragma is settled in writing: unnecessary for `20260908120000` with its measurement; the general data-dependent case OPEN, booked with a trigger, an interim remedy and Phase 164.9.
2. VAC-08's first honest green is recorded SHA-bound, or its residual red is named.
3. The next reader of CLAUDE.md / the runbook sees five holders and a TEST that mirrors PROD's catalogue, not its data.
4. What would make this RED: a closure paragraph without a run id; the ROADMAP still saying `0 plans`; a runbook § 2 table that still lists three TTLs; the PROD-validity-implies-TEST-validity sentence in any of the three documents; a deferral with no phase.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-06-SUMMARY.md` when done.
</output>
