---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 04
type: execute
wave: 4
depends_on:
  - 164.8-03
files_modified:
  - scripts/vac08-ledger-baseline.txt
  - src/__tests__/vac08-ledger-dispositions.test.ts
  - .planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-RESTORE.log
autonomous: false
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE
  - 164.5-TEST-VAC08-DROP-UNAPPLIABLE

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The founder decided AT THE MOMENT OF EXECUTION, on the preflight's measured readings, and the restore ran only after that decision."
    - "After the restore, TEST's `public` schema matches the dump's shape (tables/policies/functions counts equal the dump-derived numbers), its ledger holds exactly one CLI-shaped row per repo migration file, and `supabase db push --dry-run --db-url` reports nothing pending (ledger SHAPE — W4)."
    - "`scripts/vac08-ledger-baseline.txt` is MEASURED empty, beside an AIM that proves the classifier still classifies, and `ENTRY_COUNT` moved to 0 in the same commit — never a bare empty file."
    - "The ratchet file's header states the CORRECTED premise (B4): TEST mirrors PROD's CATALOGUE by construction, never its DATA — the restore is schema-only — so a data-reading migration valid for PROD can still refuse on TEST, and that general case is OPEN, routed by Plan 06 to a phase."
    - "Every reading in the SUMMARY is SHA-bound and run-id-bound (workflow run ids for preflight and restore, head sha, the verbatim `restore:` summary line)."
  artifacts:
    - "164.8-04-RESTORE.log — the pasted preflight and restore logs (names and counts only), both run ids"
    - "scripts/vac08-ledger-baseline.txt — header only, dated, naming the restore run id; zero entries"
    - "src/__tests__/vac08-ledger-dispositions.test.ts — ENTRY_COUNT = 0 with the empty set asserted only after the AIM"
  key_links:
    - "VAC-08's ratchet (scripts/test-ledger-drift-check.sh, the STALE classification and its error exit) turns every one of the 31 entries STALE the moment the ledger is seeded — the emptying is the file's own header rule applied, and it lands in the SAME PR as this plan so main's `sql-tests` is red only between the restore and this PR's merge"
    - "The `[SEC]` arm of vac08-ledger-dispositions.test.ts loses its subjects — it is rewritten as an AIM over the synthetic corpus, not deleted"
---

<objective>
Execute the one-way door: preflight, founder decision, restore, measurement — then apply the
ratchet file's own rule to the 31 entries that the restore makes present.

Purpose: this is the act CONTEXT's SUPERSEDING DECISION ordered and every earlier plan built
toward. It is destructive on a database shared with other people's CI, so it runs behind a
`checkpoint:decision` and every number it produces is recorded with its run id. The pragma of
Area 3 is settled here by measurement FOR THIS MIGRATION: once TEST's ledger holds
`20260908120000` (seeded from the repo file set that PROD's green `plan` job proves PROD holds),
VAC-08's red for that migration is gone with no pragma at all. ⚠️ B4 (plan-checker,
2026-09-08): that settles the NARROW question only. The restore is SCHEMA-ONLY (`BASELINE.md`:
0 data statements), so TEST mirrors PROD's catalogue and never its data; a future migration that
reads a PROD-populated table and `RAISE`s on an unexpected count — this repo's house style, e.g.
the data-reading DO block in `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql`
— applies cleanly to PROD and refuses on empty TEST, and under Area 1 Q2 (LOCKED) that blocks
the PROD apply. The GENERAL case is therefore OPEN, not dead; Plan 06 records the verdict in
those words and books the escape hatch to a phase.

Output: a restored TEST, an evidence log, an emptied-beside-an-AIM baseline whose header states
the corrected premise.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@scripts/vac08-ledger-baseline.txt
@src/__tests__/vac08-ledger-dispositions.test.ts
@src/__tests__/mutation-annotation-parser.test.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Merge the Wave 1-3 PR, then dispatch PREFLIGHT and read it</name>
  <precondition>Plans 01, 02 and 03 are merged to `main` (a workflow_dispatch workflow is dispatchable only from the default branch). The premise the seed rests on — PROD's ledger equals the repo file set — is no longer a prose precondition: Plan 03's step `Assert PROD's ledger equals the repo file set` refuses the run unless the newest `supabase-migrate.yml` run on main is `success` at an ancestor of HEAD (W5). Read Plans 01-03's SUMMARYs (`164.8-01-SUMMARY.md`, `164.8-02-SUMMARY.md`, `164.8-03-SUMMARY.md`) before dispatching.</precondition>
  <action>Merge the PR carrying `164.8-01` + `164.8-02` + `164.8-03` to `main`. Everything after the merge — the preflight dispatch, the log read, the evidence file — the executor does itself, because a preflight commits nothing.</action>
  <instructions>
    Plans 01-03 built and pinned the restore script, its sixteen-arm proof and its dispatch-only workflow; nothing has touched TEST.
    The ONE thing requiring you: merge that PR (via `/gsd-ship`, reviewers before the pr-branch filter, per CLAUDE.md), then say "merged".
    On resume the executor will: confirm `gh workflow list` shows `test-restore-from-baseline`; run `gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight`; read the job log via `gh api repos/:owner/:repo/actions/jobs/<job-id>/logs` (never `gh run view --log`, which truncates); and write `164.8-04-RESTORE.log` (names and counts only) with the `which_database:` line, the ancestry step's three readings, the `pre-census` block (tables / functions / policies / ledger rows / non-14-digit versions / objects-not-owned-by-postgres / survivor keys INCLUDING every `pub:public.<table>` key), the `expected shape` block, `filtered: N`, the `post-census == pre-census` verdict, `mode=preflight`, the backup artifact name and sizes, the run id and head sha.
    If the preflight is RED the ROLLBACK already happened (or the refusal fired before any write): the error line names the class — `must be owner` on a public object; a missing extension; a survivor that cannot be re-created; a `pub:public.<t>` row for a table absent from the dump (W3: the remedy is a founder-run `ALTER PUBLICATION supabase_realtime DROP TABLE public.<t>` on TEST, recorded in the log with the marker check pasted first, then re-preflight — NOT a script change); or the derived census naming a non-public dependent of `public` that no survivor class covers (B2: a hosted-only object the fixture cannot see — the fix is a NEW survivor class in Plan 01's script WITH a new self-test arm and a raised `EXPECTED_ARMS`, then re-merge and re-preflight). Task 2 is never reached on a red preflight.
  </instructions>
  <verification>test -s .planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-RESTORE.log and the file contains `mode=preflight` and `post-census == pre-census`, and `gh run list --workflow test-restore-from-baseline.yml -L1` shows `success` on `main`.</verification>
  <resume-signal>Type "merged" once the Wave 1-3 PR is on main; the executor runs the preflight and returns with the readings.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: DECISION — restore TEST from the PROD dump now?</name>
  <precondition>Task 1's preflight was green on `main` and its readings are in `164.8-04-RESTORE.log`; the backup artifact `test-restore-backup-<run_id>` from that run is downloadable.</precondition>
  <decision>Rebuild shared TEST's `public` schema from `supabase/schema/baseline.sql` (sha `27826b76…`, the sha row of `supabase/schema/BASELINE.md`) and seed its ledger with one CLI-shaped row per repo migration file — a ONE-WAY act on a database shared with other people's CI.</decision>
  <context>
Present, from the preflight log: the survivors that will be dropped and re-created by name (expected: `auth.users:on_auth_user_created`, `storage.objects:gdpr_exports_admin_read`, `storage.objects:portfolio_docs_*` only if they reference public functions, and every `supabase_realtime` row as `pub:public.<table>`); the count of public objects NOT owned by `postgres` (must be 0 for the drop to succeed — the preflight proved it, or it would be red); the pre/expected shape numbers; the ledger delta (243 rows, 40 re-stamped → 266 rows, 0 re-stamped). State the CONSEQUENCES plainly: (1) every row in every `public` table on TEST is destroyed AND THAT IS NOT REVERSIBLE — no data dump is taken (W6); e2e specs re-seed their own data; hand-created rows and `public.profiles` for existing `auth.users` are gone; (2) `auth.users` and every non-public schema survive untouched — the derived `pg_depend` census proved no unlisted dependent exists, or the preflight would have been red (B2); (3) other people's CI runs that are queued will wait on the mutex (the run holds it for the whole act, ≈ minutes) and any run mid-flight without the mutex could see a torn schema — dispatch when `gh run list --workflow=CI --branch main` is quiet (runbook § 5's advice for the drill); (4) reversibility: SCHEMA AND LEDGER are reversible from the artifact (ledger CSV, schema dump, survivors) by a founder-run `psql -f` after a manual drop, not a button; DATA IS NOT; (5) the publication hazard (W3): ANY `supabase_realtime` row on TEST for a public table absent from the PROD dump makes the restore refuse before writing, every time, until that row is removed by hand — the preflight listed every `pub:` key, so if it was green there is none; (6) FANOUT-GLOBAL-01 stays as it is — the restore does not change the shared-TEST assertion caveat.
  </context>
  <options>
    <option id="restore">
      <name>RESTORE NOW</name>
      <pros>TEST becomes a copy of PROD's catalogue with a CLI-shaped ledger; Plan 05's `apply-test` can run natively; every applied-ness probe in `supabase/tests` is armed; the 31-entry ratchet collapses by measurement; the act is one atomic transaction with a schema+ledger backup artifact already uploaded by the preflight run.</pros>
      <cons>Every row in every `public` table on TEST is destroyed and cannot be restored (e2e re-seeds its own data; hand-created rows and existing users' `public.profiles` are gone); a foreign CI run mid-flight without the mutex could observe a torn schema only if it ignores the lock; the schema/ledger reversal is a founder-run `psql -f` from the artifact, not a button.</cons>
    </option>
    <option id="abort">
      <name>ABORT</name>
      <pros>Nothing on shared TEST changes.</pros>
      <cons>The phase stops here: `supabase db push` cannot run against TEST's current ledger (RESEARCH A.3(1), `ErrMissingLocal` on the 40 re-stamped rows), so Plan 05's apply-on-merge pipeline is unbuildable and `main` stays red on VAC-08 for `20260908120000`.</cons>
    </option>
  </options>
  <resume-signal>Reply "restore" or "abort". On "restore" the executor runs `gh workflow run test-restore-from-baseline.yml --ref main -f mode=restore -f confirm=<the token the preflight's confirm step printed>` — founder-approved at this moment (CONTEXT safety rule 4) — watches it to completion, and continues with Task 3. On "abort" it writes the SUMMARY with the reason and stops.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Measure the restored TEST, then empty the ratchet file BESIDE AN AIM with the corrected premise in its header</name>
  <files>scripts/vac08-ledger-baseline.txt, src/__tests__/vac08-ledger-dispositions.test.ts, .planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-RESTORE.log</files>
  <read_first>
    - scripts/vac08-ledger-baseline.txt:1-30 — its own rules: MUST ONLY SHRINK; a name listed here that later turns up PRESENT is a HARD FAILURE, delete the line; removing entries is a founder decision (the founder's decision is Task 2, recorded by run id).
    - scripts/test-ledger-drift-check.sh:361-406 — the ratchet: STALE = baselined but not measured missing; stale means an error exit; the green sentence "ledger presence: N absent, all N baselined; 0 NEW drift."
    - src/__tests__/vac08-ledger-dispositions.test.ts:42-49 — ENTRY_COUNT and its rule.
    - src/__tests__/vac08-ledger-dispositions.test.ts:128-140 — the both-directions pin.
    - src/__tests__/vac08-ledger-dispositions.test.ts:158-175 — the [SEC] arm over SEC_ENTRIES.
    - src/__tests__/vac08-ledger-dispositions.test.ts:178-190 — the printable-reading pins and unqualifiedStrongClaims.
    - src/__tests__/vac08-ledger-dispositions.test.ts:198-240 — the existing AIM block.
    - src/__tests__/mutation-annotation-parser.test.ts:2391-2421 — the AIM-FIRST ordering for a deliberately empty class (the AIM runs first and fails there, before the live corpus is asserted to be empty).
    - src/__tests__/drift-check-scripts.test.ts — search it for the baseline file name and for ENTRY_COUNT before editing; it scaffolds an EMPTY baseline per arm, so it should be unaffected — MEASURE that by running it.
  </read_first>
  <action>
**Measure first.** Paste into `164.8-04-RESTORE.log`: the restore run id and head sha, the verbatim `restore: tables=… policies=… functions=… ledger_rows=266 survivors=k/k filtered=1 mode=restore` line, the CLI post-verify output (nothing pending / every local version Remote — ledger SHAPE, W4), and the post-census. Then, read-only, against TEST through the SAME which-database check (a read of the marker first, proceed only when it names TEST): `SELECT count(*) FROM supabase_migrations.schema_migrations` (expect 266), `… WHERE version !~ '^[0-9]{14}$'` (expect 0), `SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND p.proname='create_allocator_connected_strategy'` (expect 0 — the DRIFT-04 function is in neither PROD nor the dump nor TEST; this is Plan 06's evidence that no pragma is needed FOR THIS MIGRATION), and `SELECT tgname FROM pg_trigger WHERE tgrelid='auth.users'::regclass AND NOT tgisinternal` (expect `on_auth_user_created`). Record each with its expected value beside it.

**Then the ratchet file.** Rewrite `scripts/vac08-ledger-baseline.txt` to header comments ONLY (zero entries). The header keeps its identity and rules ("a RATCHET, not a mute button", "MUST ONLY SHRINK", "do not hand-apply to TEST", "LEDGER ABSENCE IS NOT OBJECT ABSENCE") and adds a dated paragraph: on <date>, restore run <id> at head <sha> rebuilt TEST's schema from `baseline.sql` sha `27826b76…` and seeded the ledger with all 266 repo files, so all 31 entries turned up PRESENT — the file's own rule required their deletion, and this is the founder-decided apply-equivalent the header reserved (cite Task 2). State the reading the gate CAN now print: `ledger presence: 0 absent, all 0 baselined (see …); 0 NEW drift.` — and keep the file from spelling the phrase `zero unledgered migrations` (the `unqualifiedStrongClaims` pin stays as-is and the file must stay clean under it; say instead that the ledger holds a row for every repo file BY SEEDING, which VAC-08 measures as `0 absent`). Add, IN THESE TERMS (B4 — the false general premise the checker quoted, that validity on PROD implies validity on TEST, must not appear in the file; the verify negative-greps its wording): "⛔ From here an entry may be ADDED only by founder decision for a migration that genuinely cannot reach TEST. Expect additions to be rare but NOT impossible: the restore was SCHEMA-ONLY, so TEST mirrors PROD's CATALOGUE by construction and never its DATA — every public table here starts empty and diverges from PROD's rows the moment e2e runs. A migration that reads a PROD-populated table and refuses on an unexpected count (this repo's house style for data-reading DO blocks) applies cleanly to PROD and RAISEs on TEST; under the apply-on-merge pipeline that BLOCKS the PROD apply by design, and the interim remedy is a revert of the merge, never a YAML edit. The general disposition for that case is OPEN — TODOS `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` (Phase 164.8 Plan 06) routes it to a phase. The `TEST-NOT-APPLICABLE` pragma was not built in 164.8 because THIS migration (`20260908120000`) needed none: the object is in neither PROD, the dump, nor TEST, and its ledger row is present."

**Then the test, in the AIM-FIRST shape.** In `vac08-ledger-dispositions.test.ts`: `ENTRY_COUNT = 0` with the docblock rewritten (why it is 0, run id, date; "may only be raised by a founder decision recorded in the header"). Restructure the live-file describe so that the FIRST `it` is the AIM (move the existing AIM block's fixture re-derivation to run before any live-file assertion, mirroring the parser test's AIM-FIRST block), and only then `it("the live file is MEASURED empty — beside the AIM above")` asserting `entries.length === ENTRY_COUNT` with the both-directions message (a GREW message that says "an entry was added — that is a founder decision; record it in the header first"). Replace the `[SEC]` arm: `SEC_ENTRIES` becomes an empty tuple with a comment that the three security migrations reached TEST by the restore (name them and the run id), and the arm becomes an AIM: a synthetic corpus with one `[SEC]`-marked line lacking the "never received it" disclosure and one carrying it, asserting the classifier flags exactly the first — so the disclosure rule is still enforced the day an entry returns. Keep the "file states the reading the gate CAN print" pin (`0 NEW drift`) and the `unqualifiedStrongClaims` pin untouched.

**Observe RED before trusting:** on a scratch COPY of the test file (never `git checkout --`), set `ENTRY_COUNT` to 31 and run — it must fail naming `SHRANK to 0`; appending a synthetic entry to a COPY of the baseline is not available to this test (it reads the real path), so instead assert the AIM catches a stopped classifier by mutating `rederive` in the copy to return `[]` unconditionally — the AIM `it` must go red. Record both observations. Run `npx vitest run src/__tests__/vac08-ledger-dispositions.test.ts src/__tests__/drift-check-scripts.test.ts` and `bash scripts/test-ledger-drift-check.sh --self-test` (its arms use an empty baseline already — unchanged 5/5 expected). Commit the three files together (the ratchet file and its pin are ONE edit).
  </action>
  <verify>
    <automated>test "$(grep -v '^#' scripts/vac08-ledger-baseline.txt | grep -c .)" = 0 && grep -q 'export const ENTRY_COUNT = 0;' src/__tests__/vac08-ledger-dispositions.test.ts && grep -c 'DATA-DEPENDENT-MIGRATION-ESCAPE' scripts/vac08-ledger-baseline.txt | grep -qv '^0$' && ! grep -q 'valid for PROD is valid for TEST' scripts/vac08-ledger-baseline.txt && npx vitest run src/__tests__/vac08-ledger-dispositions.test.ts src/__tests__/drift-check-scripts.test.ts && bash scripts/test-ledger-drift-check.sh --self-test</automated>
  </verify>
  <done>The log holds the restore's run id, sha, summary line, CLI post-verify and four read-only catalog readings; the baseline has zero non-comment lines, a dated header naming the run, and the corrected catalogue-not-data premise with the TODOS route; `ENTRY_COUNT = 0` asserted AFTER an AIM that was observed to go red when the classifier was neutered on a copy; VAC-08 self-test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder decision → destructive dispatch | the only place a human authorises a write to shared TEST |
| shared TEST → other people's CI | a torn schema mid-restore is visible to any run not holding the mutex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-12 | Elevation | restore dispatched without the founder | critical | mitigate | `checkpoint:decision` before the dispatch; the `confirm` token; preflight readings presented first |
| T-164.8-13 | Denial of service | foreign CI mid-flight during the restore | high | mitigate | mutex held for the act; dispatch when CI is quiet (runbook § 5); the transaction is atomic so a foreign reader sees old-or-new, never torn |
| T-164.8-14 | Repudiation | readings restated from memory later | medium | mitigate | every number in `164.8-04-RESTORE.log` carries run id + sha; the SUMMARY quotes the log |
| T-164.8-15 | Tampering | emptying the ratchet as a mute button | high | mitigate | emptied only after the 31 were MEASURED present (the gate would otherwise print `31 … no longer MEASURED absent`), beside an AIM, pin moved in the same commit; header states the OPEN general case rather than a false "stays empty forever" |
| T-164.8-09 | Repudiation | data loss on TEST is irreversible | high | accept | stated in the checkpoint's context, cons and the workflow header; no data dump is taken (a data dump to a public artifact is the worse finding); e2e re-seeds; residue booked in Plan 06's `[164.8-TEST-DATA-RESEEDED]` |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- Preflight green then restore green, both on `main`, both run ids in the log.
- Read-only catalog readings match expectations (266 / 0 / 0 / `on_auth_user_created`).
- `sql-tests` on the next main run after this PR merges prints `ledger presence: 0 absent, all 0 baselined … 0 NEW drift.` (Plan 06 records the SHA-bound first green).
</verification>

<success_criteria>
1. The founder decided on measured preflight readings — with data loss stated as NOT reversible — and the restore ran after, not before.
2. TEST's ledger is CLI-consistent (CLI dry-run: nothing pending — shape) — the precondition Plan 05 needs.
3. The 31-entry ratchet is empty by measurement, beside an AIM, with its pin moved in the same commit, and its header carries the catalogue-not-data premise and the TODOS route for the open general case.
4. What would make this RED: a restore run id without a preceding preflight run id; `ledger_rows` ≠ the repo file count; a survivor key missing post-restore; a bare `toEqual([])` / `toBe(0)` on the live file without the AIM running first; the header carrying the PROD-validity-implies-TEST-validity sentence.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-SUMMARY.md` when done, quoting the log's run ids and the `restore:` line verbatim.
</output>
