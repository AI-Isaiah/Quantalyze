---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 04
type: execute
wave: 4
depends_on:
  - 164.8-03
files_modified:
  - .github/workflows/supabase-migrate.yml
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On every push to main touching supabase/migrations/**, a job bound to `environment: Test` applies the merged migrations to TEST with the pinned Supabase CLI BEFORE the PROD `apply` job can start (Area 1 Q1/Q3/Q4)."
    - "A TEST apply that fails OR is skipped blocks the PROD apply — a `skipped` upstream is judged as a FAULT by an `if: always()` verdict job that names both possible causes (Area 1 Q2; the `frontend` aggregator idiom, ci.yml:2207 and :2246-2284)."
    - "The TEST apply holds the shared-TEST advisory mutex (key 61616158) across marker check → dry-run → apply → post-verify (Area 4 Q3), and an absent credential is exit 1 naming the variable on EVERY event."
    - "The PROD apply now waits for a human (Production required_reviewers since 2026-09-08); the TEST apply does NOT (no reviewers on `Test` by founder decision) — the reviewer sees `apply-test` green before approving."
    - "The pipeline is proven end-to-end by a SHA-bound workflow_dispatch run in which apply-test → verdict → plan → apply all conclude success with nothing pending on either database."
  artifacts:
    - ".github/workflows/supabase-migrate.yml — new jobs `apply-test` and `apply-test-verdict`; `apply` gains `needs: [plan, apply-test]`"
    - "src/__tests__/supabase-migrate-test-first.test.ts — line-exact pins over the three job blocks with calibration twins; cross-file mutex suffix identity"
    - "src/__tests__/critical-regressions.test.ts — existing pins (:1033, :1078) still green with the new jobs present"
  key_links:
    - "`apply.needs` includes `apply-test` AND `apply.if` requires `needs.apply-test.result == 'success'` — the `needs:` blocks on failure by default, the `if:` blocks on skip, the verdict job makes the skip a named red"
    - "`apply-test` uses `TEST_SUPABASE_DB_URL` rewritten to the :5432 session DSN with `supabase db push --include-all --db-url` — the same CLI, same flags, same `migration list` reverted-grep as PROD (`supabase-migrate.yml:241-250`), a different credential MODE, stated in the job comment as the one deliberate deviation from PROD's shape"
---

<objective>
Make TEST the stage every migration crosses before PROD: the apply-on-merge pipeline.

Purpose: CONTEXT Area 1 (all four decisions) and Area 4 Q3/Q4. With TEST's ledger now
CLI-consistent (Plan 03), `supabase db push --include-all` runs natively against TEST —
no bespoke applier, no cut, no pragma (the founder's dead-scope list). The accepted coupling
(a foreign hung mutex holder now sits between a merge and the PROD deploy) is IMPLEMENTED as
decided, not softened: the acquire cap stays the runbook's 3600 s and the job TTL 90 min.

Output: `supabase-migrate.yml` with `apply-test` + `apply-test-verdict`, pins, and a SHA-bound
dispatch proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-PATTERNS.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-03-SUMMARY.md
@.github/workflows/supabase-migrate.yml
@.github/workflows/test-restore-from-baseline.yml
@src/__tests__/test-restore-workflow-wiring.test.ts
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `apply-test` + `apply-test-verdict` jobs; PROD `apply` gated on both</name>
  <files>.github/workflows/supabase-migrate.yml</files>
  <precondition>Plan 03's restore has run: `supabase db push --include-all --dry-run --db-url <TEST session DSN>` reported nothing pending in its post-verify (otherwise the CLI refuses TEST's ledger with `ErrMissingLocal`, RESEARCH A.3(1), and this job can never go green).</precondition>
  <read_first>
    - .github/workflows/supabase-migrate.yml — all 251 lines; especially :107-128 (plan job shape), :129-166 (the PROD credential check whose push-gate `critical-regressions.test.ts:1078-1137` pins — leave it byte-identical), :187-189 (`apply: needs: plan / if:`), :199-205 (CLI pin rationale), :241-250 (the apply + C-0331 post-verify body to twin), :40-49 (one-time setup header), :60-88 (the PR-Y2 comment whose item 2 at :65 — "Test is still 38 migrations BEHIND prod" — is now false and gets a dated correction, not a deletion).
    - .github/workflows/test-restore-from-baseline.yml (Plan 02) — the Acquire/Release steps, the credential assert and the marker step: COPY them byte-for-byte (Task 2 pins the suffix identity across all three files).
    - .github/workflows/ci.yml:2160-2300 — the `frontend` aggregator's `if: always()` result loop (:2207; the `sql-tests` row :2246-2284 with its two-cause `::error::`), and :2187-2191 (why a bare `needs:` chain is not enough).
    - 164.8-PATTERNS.md § 1 — shapes (a) vs (b); this plan uses BOTH: `if:` on `apply` for the block, a verdict job for the named red.
  </read_first>
  <action>
Edit `.github/workflows/supabase-migrate.yml` (surgical: the `plan` job and the PROD `apply` step bodies stay byte-identical except the `needs:`/`if:` lines named below).

**Job `apply-test`** (placed between `plan` and `apply`; `runs-on: ubuntu-latest`; `timeout-minutes: 90` with the runbook § 2 derivation comment — setup + 3600 s acquire cap + apply; `environment: Test`; NO `needs:` — it must not wait for the Production-environment `plan` job, which now waits for a human reviewer; the reviewer approves `plan`/`apply` after seeing this job green). Steps:
  1. checkout pinned, `persist-credentials: false`.
  2. `Assert TEST credential is configured` — NO `if:`, on every event: `TEST_SUPABASE_DB_URL` empty ⇒ `::error::` naming it, `HARD FAILURE, not a skip`, exit 1. Comment: the PROD job tolerates an unconfigured `workflow_dispatch` clone (D-02/R1); this job does not, because a green TEST apply that applied nothing is exactly "a freshly-deployed worker meets an OLD schema" one environment over (PATTERNS § 1).
  3. `supabase/setup-cli` pinned SHA, `version: 2.98.2` (same as PROD, same rationale comment).
  4. install `postgresql-client`.
  5. `Acquire shared-test-db mutex` — byte-identical to Plan 02's step (fail-loud credential prefix + the ci.yml suffix).
  6. `Which database am I on` — the marker step from Plan 02, verbatim; refuse on NULL / non-TEST / PROD-naming.
  7. `Plan — dry-run apply (TEST)` — derive the session DSN exactly as the mutex step does (never echoed), then `supabase db push --include-all --dry-run --db-url "$dsn"` (command-fidelity twin of the PROD plan step; a log record, not a gate).
  8. `Push migrations to TEST` — `set -euo pipefail`; `supabase db push --include-all --db-url "$dsn"`; then the C-0331 twin: `supabase migration list --db-url "$dsn" | tee "$RUNNER_TEMP/test-migration-list.txt"` and the same `reverted` grep ⇒ `::error::` + exit 1. Comment states the ONE deliberate deviation from PROD's shape: credential MODE is `--db-url` (the existing repo secret, session-mode rewrite, no `supabase link`, no access token) rather than project-ref + password — and that the DSN handed to the CLI must be percent-encoded (fix lives in the secret, never in YAML).
  9. `Release shared-test-db mutex (best effort)` — `if: always()`, verbatim.

**Job `apply-test-verdict`** (`needs: [apply-test]`, `if: always()`, `runs-on: ubuntu-latest`, `timeout-minutes: 5`): one step that prints `apply-test=${{ needs.apply-test.result }}` (through `env:`) and: `success` ⇒ zero exit status; `skipped` ⇒ `::error::` naming BOTH causes cheapest-first — (1) the job's `if:`/`needs:` were edited so it no longer runs on this event, (2) the workflow was cancelled while queued — exit 1; anything else ⇒ `::error::` naming (1) a mutex acquire timeout (runbook §§ 2-3: queue depth vs wedged holder — the ACCEPTED coupling of CONTEXT, a foreign holder now delays PROD), (2) the migration itself failed on TEST — read the `Push migrations to TEST` step, `At statement:` names the file — which is the phase's entire purpose, caught against a real Postgres instead of customers; exit 1. Comment: this job exists so a skipped or failed TEST apply is a RED check with a cause on the Actions board, not a grey `apply` that "did not run" (ci.yml:2187-2191 reasoning).

**PROD `apply`:** `needs: [plan, apply-test]` and `if: needs.plan.outputs.configured == 'true' && needs.apply-test.result == 'success'` — nothing else in the job changes. Comment beside it: a failed OR skipped TEST apply blocks PROD (Area 1 Q2, warn-only rejected); the verdict job is where the cause is printed.

**Header edits:** in "Required GitHub repo configuration" add `Environment Test` (exists 2026-09-08, no protection rules by founder decision — do not add reviewers) and `Secret TEST_SUPABASE_DB_URL` (repo-level, shared with ci.yml); in the "Safety story" add the line that TEST is applied FIRST and PROD waits on it; under the PR-Y2 comment item 2 (:65) add a dated `⚠️ CURRENCY` sentence: TEST was rebuilt from the PROD dump by Phase 164.8 (restore run id from Plan 03's SUMMARY) and is applied on every merge by the `apply-test` job below, so "38 BEHIND" no longer describes it. Also correct the "required-reviewer gates NOT yet configured" statements with a dated note that `Production` carries `required_reviewers` since 2026-09-08 (measured via `gh api …/environments`) — the `environment:` pins already honour it, no YAML change.

Run `npx vitest run src/__tests__/critical-regressions.test.ts` — the existing `plan`/`apply` slices (:1033-1063, :1078-1137) must still pass; if the job-slicing regex there stops at the next `  <name>:` line, the inserted `apply-test:` job is invisible to them by design — measure, do not assume.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts -t "supabase-migrate" && node -e 'const y=require("fs").readFileSync(".github/workflows/supabase-migrate.yml","utf8");const a=y.slice(y.indexOf("\n  apply:\n"));if(!/needs: \[plan, apply-test\]/.test(a))process.exit(1);if(!/needs\.apply-test\.result == .success./.test(a))process.exit(2);if(!/\n  apply-test-verdict:\n/.test(y))process.exit(3);console.log("gate wired")'</automated>
  </verify>
  <done>`apply-test`, `apply-test-verdict` exist; PROD `apply` needs both `plan` and `apply-test` and its `if:` requires `apply-test` success; the existing critical-regressions pins on `plan`/`apply` stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Pins — line-exact, block-sliced, cross-file mutex identity, verdict-loop semantics</name>
  <files>src/__tests__/supabase-migrate-test-first.test.ts, src/__tests__/critical-regressions.test.ts</files>
  <read_first>
    - src/__tests__/test-restore-workflow-wiring.test.ts (Plan 02) — reuse its slicing/calibration helpers by COPY (the repo's wiring tests are self-contained files; say so in a comment rather than creating a shared helper nobody else imports).
    - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts:27-36 — "a grep pin goes green the moment someone keeps the strings and guts the logic… so this test EXTRACTS the step's shell script and RUNS it with a stub on PATH, and asserts on exit codes."
    - src/__tests__/critical-regressions.test.ts:1290-1312 — the byte-identity regex; and :1033-1063 to add the `apply-test` environment pin in the SAME describe.
  </read_first>
  <action>
Write `src/__tests__/supabase-migrate-test-first.test.ts` (reads via `node:fs`; slices `apply-test:`, `apply-test-verdict:` and `apply:` blocks by `\n  <job>:\n`; every pin has a calibration twin on an in-memory mutated copy):
  - `environment: Test` is a live line inside `apply-test:` exactly once; `apply-test:` has NO `needs:` line (a `needs: plan` inserted flips it — it would make TEST wait on the human-gated Production job);
  - `timeout-minutes: 90` live in `apply-test:`;
  - the credential step has NO `if:` between its `- name:` and `run:`, contains `exit 1` and `::error::` naming `TEST_SUPABASE_DB_URL`; the `apply-test:` block, comments masked and the Release step sliced out, contains none of the softening tokens (`continue-on-error`, the double-pipe-true idiom, a zero-status exit, `::warning`, `set +e` — restate the prober test's list);
  - the three CLI commands are LIVE `run:` lines in `apply-test:`, in order: `supabase db push --include-all --dry-run --db-url "$dsn"` before `supabase db push --include-all --db-url "$dsn"` before `supabase migration list --db-url "$dsn"`; and the marker query (`shobj_description`) index < the dry-run index; the Acquire step index < the marker index;
  - `apply:` block: `needs: [plan, apply-test]` live line; the `if:` line contains both `needs.plan.outputs.configured == 'true'` and `needs.apply-test.result == 'success'`;
  - `apply-test-verdict:` block: `if: always()` live line; `needs:` names exactly `apply-test`; its script is EXTRACTED and RUN (the anti-skip-gate idiom) three times with `APPLY_TEST_RESULT` = `success` / `skipped` / `failure` set through the env the step reads, asserting exit statuses 0 / 1 / 1 and that the `skipped` and `failure` outputs each contain two numbered causes;
  - cross-file mutex suffix identity: the Acquire step suffix (from the `if ! command -v psql` line) in `supabase-migrate.yml` `apply-test` == `test-restore-from-baseline.yml` `restore` == `ci.yml` `sql-tests`; and the Release step is byte-identical across the three files;
  - the PROD `Push migrations to production` step body is byte-identical to its pre-edit form (pin the exact text of `supabase-migrate.yml:241-250` as a literal so this plan's edit is proven not to have touched it).
In `critical-regressions.test.ts` add, inside the `supabase-migrate plan/apply env-gate symmetry` describe (:1033), an `it` that the `apply-test` job declares `environment: Test` (job-sliced, same idiom as :1034-1046) and an `it` that `apply` needs `apply-test` — so the repo's central CI-hardening file knows the third job exists.
Observe RED on copies for each pin (record the mutation and the failing message in the SUMMARY); never `git checkout --`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/test-restore-workflow-wiring.test.ts</automated>
  </verify>
  <done>All pins green with recorded RED calibrations; the verdict script is executed (not grepped) under all three results; mutex Acquire/Release identical across the three workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the pipeline end-to-end with a SHA-bound dispatch (nothing pending on either side)</name>
  <files>.github/workflows/supabase-migrate.yml</files>
  <precondition>This plan's PR is merged to `main` (the `plan`/`apply` jobs read `environment: Production`, which now requires reviewer approval — the founder approves those two jobs; `apply-test` needs no approval).</precondition>
  <action>
After merge: `gh workflow run supabase-migrate.yml --ref main` (no migration is pending on PROD or TEST, so the run exercises the ORDER, the mutex, the marker, the credential and the verdict without applying anything — a clean-room proof of wiring). Watch it: `apply-test` must conclude `success` with the TEST dry-run and push both reporting nothing to apply and the `migration list` reverted-grep clean; `apply-test-verdict` `success`; then `plan` waits for the founder's environment approval; `apply` starts only after both `plan` and `apply-test` succeeded. Read job logs via `gh api …/jobs/<id>/logs`. Record in the SUMMARY: run id, head sha, each job's conclusion and wall clock, the per-job mutex acquire wait, and the verbatim `Which database` OK line from `apply-test`. If the CLI rejects the `--db-url` DSN (percent-encoding), stop and record it: the remedy is a re-encoded SECRET (a founder settings act) followed by a re-dispatch — never a YAML transform. Then book the two founder-visible consequences of Area 1 Q1 that this run makes concrete, as ONE TODOS entry `[164.8-PUSH-RACE-VAC08]` (Plan 05 owns TODOS.md — hand the text to it via the SUMMARY): (a) on a migration merge, `ci.yml sql-tests` (VAC-08 + the applied-ness probes inside `supabase/tests/*.sql`) and `supabase-migrate.yml apply-test` contend for the same mutex in unspecified order, so main's `frontend` can be red for that push when VAC-08 wins, and Railway then skips the analytics deploy until a re-run; (b) on every PR that adds a migration, `sql-tests` is red until merge by construction (apply-on-merge was chosen over apply-on-PR). Both were surfaced in RESEARCH Pitfall 3; the escape hatches are founder decisions, not planner ones.
  </action>
  <verify>
    <automated>RID=$(gh run list --workflow supabase-migrate.yml -L 1 --json databaseId,event --jq '.[] | select(.event=="workflow_dispatch") | .databaseId' | head -1); gh run view "$RID" --json jobs --jq '.jobs[] | "\(.name)=\(.conclusion)"' | grep -q 'apply-test=success' && gh run view "$RID" --json jobs --jq '.jobs[] | "\(.name)=\(.conclusion)"' | grep -q 'apply-test-verdict=success'</automated>
  </verify>
  <done>A SHA-bound dispatch run on main shows `apply-test=success`, `apply-test-verdict=success`, `plan=success`, `apply=success`, with `apply` starting after `apply-test`; the SUMMARY records ids, sha, timings, and the `[164.8-PUSH-RACE-VAC08]` text for Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged migration → shared TEST | untested-on-a-real-Postgres SQL crosses here FIRST, by design |
| `apply-test` result → PROD `apply` | the only thing standing between a merge and production DDL |
| foreign mutex holder → PROD deploy latency | the ACCEPTED coupling (CONTEXT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-16 | Elevation | PROD apply proceeds although TEST apply did not succeed | critical | mitigate | `needs: [plan, apply-test]` + `if: needs.apply-test.result == 'success'`; verdict job reddens a skip; pins execute the verdict script under all three results |
| T-164.8-17 | Elevation | `apply-test` resolves a PROD credential | critical | mitigate | it never reads `SUPABASE_PROJECT_REF`/`SUPABASE_DB_PASSWORD`; marker step refuses non-TEST before any write |
| T-164.8-18 | Denial of service | foreign wedged holder delays PROD up to the 3600 s cap | high | accept | CONTEXT's accepted consequence; runbook §§ 2-3 census; escape hatches are founder decisions — not softened here |
| T-164.8-19 | Tampering | a migration that is wrong for PROD but applies on TEST | medium | accept | TEST mirrors PROD by construction after Plan 03; the PROD-side pre-flight gates (`migration-drift-check.yml`, `migration-policy.yml`) are unchanged and still run against PROD |
| T-164.8-20 | Information disclosure | DSN in the public log | high | mitigate | derived by parameter expansion, never echoed; same redaction posture as ci.yml's mutex step |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | pinned `supabase/setup-cli` SHA + apt `postgresql-client`, both already in use |
</threat_model>

<verification>
- Vitest: `supabase-migrate-test-first.test.ts`, `critical-regressions.test.ts`, `test-restore-workflow-wiring.test.ts` green.
- A workflow_dispatch run on main with all four jobs `success` and `apply` starting after `apply-test`.
- Full `npm test` alone on the box before `/gsd-ship` (memory: never share the box).
</verification>

<success_criteria>
1. TEST is applied first, PROD waits — mechanically (`needs` + `if`), with a named red on skip/failure.
2. One mutex hold spans the TEST apply; credential absence is exit 1 on every event.
3. End-to-end proven by a SHA-bound run, not by reading YAML.
4. What would make this RED: `apply-test` gaining `needs: plan`; `apply.if` losing the `apply-test` clause; the verdict script exiting 0 on `skipped`; the mutex suffix drifting from ci.yml's; the PROD push step body changing by one byte.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-SUMMARY.md` when done, including the dispatch run id, sha, per-job conclusions, and the `[164.8-PUSH-RACE-VAC08]` text for Plan 05.
</output>
