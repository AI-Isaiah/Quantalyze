---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
verified: 2026-09-09T17:05:00Z
status: passed
score: 7/9 must-haves verified
behavior_unverified: 1
overrides_applied: 0
re_verification: 2026-09-09T18:20:00Z — both gaps CLOSED by the orchestrator in commit 2832e139; see the Orchestrator resolution section at the end of this file
gaps:
  - truth: "Every claim this phase leaves behind about what has and has NOT been exercised is TRUE at HEAD — the phase's own auditability standard, which plan 04 states as 'an unmeasured gate is recorded as NOT RUN, never as a pass — and it STAYS recorded that way after the fact'."
    status: failed
    reason: >-
      The Supabase CLI post-verify step DID execute and DID pass, in restore-mode run
      34330741339 (head a622df27, 2026-09-09T08:44Z, step 24 `Post-verify with the Supabase
      CLI — ledger SHAPE` -> success), printing `post-verify: the pinned CLI parses the
      seeded ledger and reports nothing pending — ledger SHAPE is consistent.` Three
      places in the repo still say it never has. The falsehood is carried FORWARD into the
      ROADMAP block that scopes Phase 164.9's work, so the next planner is sent to close a
      hole that measurement already closed. The direction of the error is conservative
      (the phase under-claims), but the defect class — a stated fact nobody re-measured —
      is the one this milestone exists to remove.
    artifacts:
      - path: ".planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/deferred-items.md"
        issue: "item 4 (`:90-91`) 'has NEVER run'; item 6 (`:169`) 'That step has now never executed in any run'"
      - path: ".planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-SUMMARY.md"
        issue: "coverage id D4b marked FAILED / 'has still never executed in any run'; `:355`; `:365`; `:379`"
      - path: ".planning/ROADMAP.md"
        issue: "Phase 164.9 ROUTED-HERE block, item 2: 'The Supabase CLI post-verify has NEVER executed … where the wording is INFERRED, not executed.' It was executed, against the pinned 2.98.2, and both asserted strings held."
    missing:
      - "Correct D4b to MET, citing run 34330741339, job step 24, head a622df27, and the verbatim `post-verify:` line."
      - "Close deferred-items item 4; narrow item 6 to the extension guard alone (its 'collateral damage' half is discharged)."
      - "Narrow the Phase 164.9 ROUTED-HERE item 2 to the guard's two-directional fix; delete the 'never executed / wording inferred' half."
    resolved: "CLOSED 2026-09-12 — RE-MEASURED, and every `missing:` clause above is SATISFIED. ⚠️ THE VERIFICATION WAS STALE, NOT WRONG: it was written 2026-09-09T17:05Z and the corrections were made the SAME DAY, after it. Measured now, file by file: (a) `164.8-04-SUMMARY.md` D4b carries a SECOND verification row with `status: pass` citing `gh run 34330741339 (head a622df27, mode=restore, 08:44Z) step 24 — conclusion SUCCESS`, and its old rationale is kept struck-through as lineage at `:110`; (b) `deferred-items.md` item 4 reads `✅ ITEM 4 IS DISCHARGED — CORRECTED 2026-09-09. The step RAN and PASSED under the pinned CLI`, and the `has NEVER run` sentence at `:102` survives only inside `~~strikethrough~~` marked superseded; (c) item 6 is already narrowed to the extension guard alone — its three numbered points are BLINDNESS, SCOPE and ORDERING of the guard, with the `{pg_net}` set difference measured from the run's own backup artifact rather than inferred, and no collateral-damage half remains; (d) `.planning/ROADMAP.md:1783` reads `~~The Supabase CLI post-verify has NEVER executed~~ — ✅ DISCHARGED 2026-09-09. IT EXECUTED AND PASSED; this item is closed and must not scope work here.` ⭐ The verifier was RIGHT that the falsehood was being carried forward into Phase 164.9's scope — that is precisely what got fixed, so the next planner is no longer sent to close a hole measurement had already closed."
  - truth: "A destructive act on shared TEST is auditable — the phase's own non-negotiable safety premise (CONTEXT Area 2 superseding decision, safety rules 1-4)."
    status: failed
    reason: >-
      TWO further dispatches of `test-restore-from-baseline.yml` ran on 2026-09-09 at head
      a622df27 — 34329459044 (preflight, 08:30Z) and 34330741339 (mode=restore, confirm
      token enforced, 08:44Z, ledger 266 -> 266, `restore: tables=62 policies=154
      functions=120 ledger_rows=266 survivors=2/2 filtered=1 mode=restore`). `grep -rn`
      over the whole of `.planning/` returns ZERO hits for either run id. Shared TEST's
      current state was set by the 08:44 restore, not by run 34274355596 the previous
      evening, yet every SUMMARY, the ROADMAP and CLAUDE.md attribute it to 34274355596.
      A second full DROP SCHEMA public CASCADE on a database other people's CI uses is
      recorded nowhere.
    artifacts:
      - path: ".planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-RESTORE.log"
        issue: "records only 34274355596 (and preflight 34258614075); the two 2026-09-09 runs are absent"
      - path: ".planning/ROADMAP.md"
        issue: "criterion-5 closure and the 164.8 entry both attribute TEST's state solely to run 34274355596"
    missing:
      - "Append 34329459044 and 34330741339 to 164.8-04-RESTORE.log with their readings and the actor."
      - "Repoint the 'TEST was restored by run X' sentences at 34330741339, the run that actually left TEST as it stands."
    resolved: "CLOSED 2026-09-12 — the audit trail now exists in every file that carried the wrong attribution, INCLUDING the one this item did not name. Measured: `164.8-04-RESTORE.log` carries `34329459044` (2 hits) and `34330741339` (3 hits) alongside `34274355596`, with the readings and the `failure`-despite-COMMIT disposition of the earlier run; `.planning/ROADMAP.md` carries `34330741339` (3 hits). ⭐ **FOUND AND FIXED TODAY, beyond this item's own `missing:` list: `CLAUDE.md` still attributed TEST's state SOLELY to run `34274355596` and had ZERO occurrences of either 2026-09-09 run id.** That is the most-read file in the repo — it is loaded into every session — so the single place the mis-attribution would have propagated furthest was the one place nobody had corrected. Its TESTPREPROD currency block now names `34329459044` (preflight, 08:30Z) and `34330741339` (mode=restore, 08:44Z, `restore: tables=62 policies=154 functions=120 ledger_rows=266 survivors=2/2 filtered=1`), states plainly that the 08:44Z restore is what left TEST as it stands, and splits the citation: `34274355596` for the LEDGER MOVE 243 → 266, `34330741339` for the SCHEMA STATE. ⭐ This item's premise — `a destructive act on shared TEST is auditable` — is a NON-NEGOTIABLE safety premise of the phase, and a second full DROP SCHEMA public CASCADE on a database other people's CI uses being recorded in one phase log and nowhere else is exactly the failure it names. It is now recorded in four places."
behavior_unverified_items:
  - truth: "On every push to main touching supabase/migrations/**, a job bound to `environment: Test` applies the merged migrations to TEST with the pinned CLI BEFORE the PROD `apply` job can start, and a TEST apply that fails or is skipped blocks the PROD apply."
    test: >-
      Land the next real migration PR to main and watch the push-triggered
      supabase-migrate.yml run. Read the `apply-test` job's `Post-apply applied-set
      verification (affirmative)` group.
    expected: >-
      `planned N migration version(s); push reported N.` with N > 0 and the two sets equal;
      at least one `Applying migration <version>...` line; `apply` runs only afterwards and
      only on `needs.apply-test.result == 'success'`.
    why_human: >-
      No push-triggered run has ever reached these jobs. The last push that actually applied
      a migration is 34200188676 (7146c574, 2026-09-08), which PREDATES `apply-test`. Both
      dispatch proofs ran with 266/266 on both ledgers, so `planned` and `applied` were both
      EMPTY and set equality held trivially — run 34367135073 printed, verbatim,
      `planned 0 migration version(s); push reported 0.` The non-empty branch and the
      partial-apply branch have run only against fixtures. Producing a real partial apply
      requires breaking a migration mid-batch on a SHARED database, which no automated check
      may do.
    resolved: "STILL OPEN, DELIBERATELY — and the attempt to close it on 2026-09-12 found a DEFECT, now booked as `[164.8-APPLYTEST-CANNOT-TELL-SEEDED-FROM-APPLIED]` (TODOS.md), owned by Phase 164.9 TESTISOLATION. ⛔ DO NOT MARK THIS MET FROM THE 2026-09-12 EVENTS — the non-empty `apply-test` branch STILL has not run in the wild, and the run that looks like it did is a different job. What happened: the Phase-164.8.6 merge push (run `34684247933`, head `ce7ac08a`) DID reach `apply-test`, which FAILED in 32 s at step `Push migrations to TEST` with `the TEST push applied ZERO migrations on a push-to-main run … Refusing to green-light the PROD apply on a stage nothing crossed`; `apply-test-verdict` then failed as designed and `apply` was CANCELLED — the TEST-first gate blocking PROD, i.e. the gate WORKING. ⭐ BUT THE REFUSAL WAS A FALSE POSITIVE, measured directly against TEST (marker query first: `✅ TEST (shared with other CI)`): `20260911120000` carries **6 real SQL statements** in its ledger row and `20260911130000` carries **10** — NOT the seeded provenance sentence `20260907130000` carries — and `match_engine_cron_tick` on TEST contains both `INTO STRICT` and `btrim`, the two things `20260911120000` exists to add. The migrations HAD crossed TEST; `db push --include-all` correctly had nothing to do; the guard read a correct empty set as a poisoned ledger. ⚠️ THE PROD APPLY THEN HAPPENED VIA THE DOCUMENTED ESCAPE: run `34686331921` (`workflow_dispatch`, `756469e6`) applied both migrations to PROD (`planned 2 migration version(s); push reported 2`) while its OWN `apply-test` printed `planned 0 … push reported 0` — so the `planned 2` line that reads like this item's evidence belongs to the PROD `apply` job, not to `apply-test`. ⛔ Reading it as closure would be citing the wrong job for the wrong database, which is why this stays open with the real state written down."
human_verification:
  - test: "Land the next real migration PR to main; read the `apply-test` applied-set group in the push-triggered run."
    expected: "planned N (>0) == applied N, at least one `Applying migration` line, `apply` gated behind it."
    why_human: "The non-empty applied-set branch has never executed in the wild; a partial apply cannot be manufactured on shared TEST."
    resolved: "STILL OPEN, DELIBERATELY — and the attempt to close it on 2026-09-12 found a DEFECT, now booked as `[164.8-APPLYTEST-CANNOT-TELL-SEEDED-FROM-APPLIED]` (TODOS.md), owned by Phase 164.9 TESTISOLATION. ⛔ DO NOT MARK THIS MET FROM THE 2026-09-12 EVENTS — the non-empty `apply-test` branch STILL has not run in the wild, and the run that looks like it did is a different job. What happened: the Phase-164.8.6 merge push (run `34684247933`, head `ce7ac08a`) DID reach `apply-test`, which FAILED in 32 s at step `Push migrations to TEST` with `the TEST push applied ZERO migrations on a push-to-main run … Refusing to green-light the PROD apply on a stage nothing crossed`; `apply-test-verdict` then failed as designed and `apply` was CANCELLED — the TEST-first gate blocking PROD, i.e. the gate WORKING. ⭐ BUT THE REFUSAL WAS A FALSE POSITIVE, measured directly against TEST (marker query first: `✅ TEST (shared with other CI)`): `20260911120000` carries **6 real SQL statements** in its ledger row and `20260911130000` carries **10** — NOT the seeded provenance sentence `20260907130000` carries — and `match_engine_cron_tick` on TEST contains both `INTO STRICT` and `btrim`, the two things `20260911120000` exists to add. The migrations HAD crossed TEST; `db push --include-all` correctly had nothing to do; the guard read a correct empty set as a poisoned ledger. ⚠️ THE PROD APPLY THEN HAPPENED VIA THE DOCUMENTED ESCAPE: run `34686331921` (`workflow_dispatch`, `756469e6`) applied both migrations to PROD (`planned 2 migration version(s); push reported 2`) while its OWN `apply-test` printed `planned 0 … push reported 0` — so the `planned 2` line that reads like this item's evidence belongs to the PROD `apply` job, not to `apply-test`. ⛔ Reading it as closure would be citing the wrong job for the wrong database, which is why this stays open with the real state written down."
---

# Phase 164.8: TESTPREPROD Verification Report

**Phase Goal:** The shared TEST project stops being a stale bystander and becomes the stage every migration crosses BEFORE production. Two halves: (1) bring TEST current; (2) apply on merge to TEST FIRST, then PROD, so a migration that cannot apply is caught against a real Postgres instead of against customers.

**Verified:** 2026-09-09T17:05:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

⚠️ **The 164.8 ROADMAP entry carries NO `**Success Criteria**:` block.** Measured:
`grep -n "Criteria" .planning/ROADMAP.md | awk -F: '$1>=1318 && $1<=1370'` returns nothing, and
`roadmap.get-phase 164.8` renders a Goal, a constraints table, a scope-addition block and a
Plans list with no criteria section. Must-haves are therefore derived per Option C from the
goal, merged with the `must_haves.truths` of all six PLAN frontmatters. No PLAN scope was
allowed to reduce the goal.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | TEST is brought current — its `public` schema and its migration ledger match PROD's shape | ✓ VERIFIED | Restore run `34274355596` (88581b8b) log: `backup: ledger_rows=243` → post-census `ledger_rows=266`, transaction COMMITTED. Re-confirmed green end-to-end by run `34330741339` (a622df27): `restore: tables=62 policies=154 functions=120 ledger_rows=266 survivors=2/2 filtered=1 mode=restore` |
| 2 | The restore mechanism is one transaction with pre-write refusals, and EVERY red path is observed, not asserted | ✓ VERIFIED | I ran `bash scripts/restore-test-from-baseline.sh --self-test` on a real throwaway PG16 cluster: **26/26 arms**, exit 0. Falsified by my own mutation — see Anti-Vacuity below |
| 3 | The restore workflow is dispatch-only, ref-guarded, confirm-token-gated, and backs TEST up before any write | ✓ VERIFIED | `on:` block is `workflow_dispatch:` alone (`:143-144`); `restore.if: github.ref == 'refs/heads/main'` (`:203`) with `dispatch-guard.if` its exact negation (`:183`). Run `34330741339` log `:271-281` shows the confirm token enforced; step 22 (backup) green BEFORE step 23 (script). Both mutations bite |
| 4 | On every push to main touching `supabase/migrations/**`, `apply-test` applies to TEST before PROD `apply`, and a failed OR skipped TEST apply blocks PROD | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Present and wired: `apply.needs: [plan, apply-test]` + `apply.if: … && needs.apply-test.result == 'success'` (`:1040-1041`); `apply-test-verdict` `if: always()` (`:949-951`); push trigger `branches: [main] paths: supabase/migrations/**`. All four gating mutations go RED. BUT no push-triggered run has ever reached it, and the one green dispatch proof ran on an EMPTY set — see below |
| 5 | The applied-set verification can actually fail (it compares against `Applying migration` lines, not the CLI's own prompt echo) | ✓ VERIFIED | `applied_versions_of` present at BOTH sites (`:832`, `:1129`). Reverting the TEST site to the old whole-stdout scrape turns **3 arms RED**, including the arm that asserts the old scrape called the same bytes "agree" |
| 6 | `scripts/vac08-ledger-baseline.txt` is MEASURED empty beside an AIM, `ENTRY_COUNT = 0` in the same commit | ✓ VERIFIED | 155 lines, **0** non-comment non-blank entries; `ENTRY_COUNT = 0` at `vac08-ledger-dispositions.test.ts:80`; the `[SEC]` arm rewritten as an AIM over a synthetic corpus, not deleted |
| 7 | The Area 3 `TEST-NOT-APPLICABLE` pragma verdict is recorded two-part, and the OPEN general case names a phase | ✓ VERIFIED | `TODOS.md:2740` `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` routed to Phase 164.9; ROADMAP `:1346` carries both halves and `:1369` explicitly corrects the superseded "DEAD SCOPE" wording. No pragma anywhere in the repo |
| 8 | CLAUDE.md and the mutex runbook describe the world AFTER this phase, dated, with the older paragraphs kept as lineage | ✓ VERIFIED | CLAUDE.md `:26` addendum, `:34` `### Currency 2026-09-09`, `:50` the CATALOGUE-not-DATA distinction, `:78-79` the push race. Runbook `§1` holders **4** (`apply-test`) and **5** (`restore`), `§2` TTL rows. Every anchor and count re-resolved — see Requirements/Anchors |
| 9 | Every claim this phase leaves behind about what has and has NOT been exercised is TRUE at HEAD | ✗ FAILED | Three files still say the CLI post-verify "has NEVER run". Run `34330741339` step 24 concluded **success**. See Gaps |
| 10 | A destructive act on shared TEST is auditable | ✗ FAILED | Two 2026-09-09 dispatches (one a full `mode=restore`) appear in ZERO `.planning/` artifacts. See Gaps |

**Score:** 7/9 truths verified (1 present, behaviour-unverified; 2 failed)

*(Truths 9 and 10 are counted as one "auditability of the record" must-have pair against the
9-item denominator: 7 VERIFIED, 1 PRESENT_BEHAVIOR_UNVERIFIED, and the two FAILED items
collapsed to the single failing must-have they share.)*

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `.github/workflows/supabase-migrate.yml` | `dispatch-ref-guard`, `apply-test`, `apply-test-verdict`; `apply` gains `needs: [plan, apply-test]` | ✓ VERIFIED | 1159 lines. All four jobs present at `:214`, `:353`, `:949`, `:1033`. `apply-test` steps: credential assert → session DSN → psql → mutex acquire → marker check → dry-run → push → mutex release |
| `.github/workflows/supabase-migrate.yml` (`applied_versions_of`) | at BOTH the TEST and PROD push step | ✓ VERIFIED | `:832` (TEST) and `:1129` (PROD), identical awk |
| `.github/workflows/test-restore-from-baseline.yml` | dispatch-only, ref-guarded, `environment: Test`, `timeout-minutes: 90` | ✓ VERIFIED | 1472 lines; `:144`, `:183`/`:203`, `:207`, `:216` |
| `scripts/restore-test-from-baseline.sh` | one-transaction restore, 9 pre-write refusals, `--self-test` | ✓ VERIFIED | 3013 lines; `EXPECTED_ARMS=26` at `:1695`; self-test run by me, 26/26 |
| `scripts/restore-test-from-baseline-fixtures/` | old-TEST fixture, baseline-shaped dump, per-arm overlays | ✓ VERIFIED | 8 `.sql` fixtures + `migrations/` + `refdata-allowlist.txt` |
| `src/__tests__/supabase-migrate-test-first.test.ts` | line-exact pins WITH calibration twins; verdict + ref guard EXECUTED | ✓ VERIFIED | 1922 lines, 28 tests. `runScript` executes the extracted shell for the ref guard, credential assert, DSN derivation, verdict, marker check and both applied-set sites |
| `src/__tests__/test-restore-workflow-wiring.test.ts` | line-exact pins over the job block, PROD-ancestry step under a stub `gh` | ✓ VERIFIED | 1381 lines, 33 tests |
| `src/__tests__/restore-test-from-baseline.test.ts` | pins `EXPECTED_ARMS` and the script constants line-exact | ✓ VERIFIED | 856 lines; `EXPECTED_ARMS=26` pinned with a MEASURED-date check and a moved-constant calibration |
| `src/__tests__/vac08-ledger-dispositions.test.ts` | `ENTRY_COUNT = 0`, empty set asserted only after the AIM | ✓ VERIFIED | 528 lines; `:80`, `:346-358` |
| `scripts/vac08-ledger-baseline.txt` | header only, dated, naming the restore run id; zero entries | ✓ VERIFIED | 0 entries |
| `TODOS.md` | closures with run ids; 3 new entries, one routed to 164.9 | ✓ VERIFIED | `:2634`, `:2692`, `:2740`, `:7460` |
| `CLAUDE.md` | dated CURRENCY paragraph under "Which database am I on?" | ✓ VERIFIED | `:26`, `:34-79` |
| `docs/runbooks/shared-test-db-mutex.md` | 4th and 5th holders in §1, TTLs in §2 | ✓ VERIFIED | `:72`, `:79`, `:207`, `:208` |
| `164.8-04-RESTORE.log` | pasted preflight + restore logs, both run ids | ⚠️ INCOMPLETE | Records `34258614075` and `34274355596` only. The two runs that actually left TEST as it stands are absent |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `apply` | `apply-test` | `needs:` + `if: needs.apply-test.result == 'success'` | ✓ WIRED | `:1040-1041`. Dropping either half turns a named arm RED |
| `apply-test` | shared TEST | `supabase db push --include-all --db-url "${dsn}"` under `set -euo pipefail` | ✓ WIRED | `:827` (push), `:752` (dry-run). `pipefail` is set, so the `\| tee` does not mask the CLI's exit status |
| `apply-test` | shared-TEST mutex | advisory key `61616158` | ✓ WIRED | 7 occurrences in `supabase-migrate.yml`, 26 in `ci.yml` — the exact counts CLAUDE.md `:78-79` states, re-measured |
| `apply-test` | database identity | `shobj_description(oid,'pg_database')` marker, NULL / non-TEST / PROD each a named refusal | ✓ WIRED | Step `Which database am I on`; executed under a stubbed `psql` by the pin at `:1176` |
| `restore` job | `Test` GitHub Environment | `environment: Test` + branch policy | ✓ WIRED | Live API: `Test` protection = `['branch_policy']`, branches = `main`. `Production` = `['required_reviewers']` — plan 05's asymmetry holds in the live config |
| restore script | `BASELINE.md` sha | same table-row shape as `check-baseline-staleness.mjs` | ✓ WIRED | Self-test arm 4 (`baseline sha != BASELINE.md`) fires RED |
| `164.8-06-PLAN.md` anchors | `CLAUDE.md` | `plan-anchor-verify` | ✓ WIRED | `node scripts/verify-plan-anchors.mjs --pending` → `claims: 17 checked` / `OK: 3 plan file(s), no stale claims.` (deferred-items item 3 is CLOSED) |

### Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
|---|---|---|---|
| The four phase test files pass at HEAD | `npx vitest run src/__tests__/{supabase-migrate-test-first,test-restore-workflow-wiring,restore-test-from-baseline,vac08-ledger-dispositions}.test.ts` | `Test Files 4 passed (4) / Tests 94 passed (94)` | ✓ PASS |
| The restore self-test runs on a real cluster | `bash scripts/restore-test-from-baseline.sh --self-test` | `self-test OK (26/26 arms …)` | ✓ PASS |
| The plan-anchor gate self-tests and scans clean | `node scripts/verify-plan-anchors.mjs --self-test` then `--pending` | `SELF-TEST OK` / `OK: 3 plan file(s), no stale claims.` | ✓ PASS |
| VAC-08 prints `0 absent` SHA-bound | `gh api …/jobs/102416204141/logs` (run 34335526540, b8951132) | `ledger presence: 0 absent, all 0 baselined (see scripts/vac08-ledger-baseline.txt); 0 NEW drift.` + `266 migration(s) checked` | ✓ PASS |
| The dispatch proof is green end-to-end at a real main sha | `gh run view 34367135073` | headSha `dbd13246` (on main); `plan`, `apply-test`, `apply-test-verdict`, `prod-credential-divergence-verdict`, `apply` all **success**; `dispatch-ref-guard` correctly **skipped** | ✓ PASS |
| The applied-set check on that proof | job `102518664316` log | `planned 0 migration version(s); push reported 0.` | ? SKIP — empty set, equality trivial (see truth 4) |
| The CLI post-verify against TEST | run `34330741339` step 24 | **success** — `post-verify: the pinned CLI parses the seeded ledger and reports nothing pending — ledger SHAPE is consistent.` | ✓ PASS (contradicts the phase's own record) |

### Anti-Vacuity — mutations I ran, and what went RED

Every mutation below was applied to a byte-backup I took myself, observed, then restored from
those pristine bytes. `git checkout --` was never used. Final `git status --porcelain` is empty
and all three sha256 sums match their pre-mutation values.

| # | Control | Mutation | Observed |
|---|---|---|---|
| M1 | PROD apply blocks on TEST apply | `apply.if` drops `&& needs.apply-test.result == 'success'` | RED — `Tests 1 failed \| 27 passed` |
| M2 | PROD apply blocks on TEST apply | `apply.needs: [plan, apply-test]` → `[plan]` | RED — arm *"apply needs [plan, apply-test] and requires apply-test success"* |
| M3 | `apply-test` is not a write path from an arbitrary ref | `apply-test.if` → `always()` | RED — 2 arms |
| M4 | A skipped TEST apply is a named red, not grey | `apply-test-verdict.if: always()` → `success()` | RED — 1 arm |
| M5 | The applied set is read from apply lines, not the prompt echo | TEST site `applied_versions_of` → `versions_of` (the pre-`dbd13246` scrape) | RED — **3 arms**, incl. *"agrees on a real apply, DISAGREES on a partial one the OLD scrape called green"* |
| M6 | The restore workflow is dispatch-only | add `push: branches: [main]` to `on:` | RED — *"the on: block declares workflow_dispatch and NOTHING else"* |
| M7 | The restore job runs only from main | `restore.if` → `always()` | RED — *"the guard job's if: is the exact negation of the restore job's"* |
| M8 | The restore refuses a PROD-named identity marker | `if printf … grep -Eiq "$RESTORE_REFUSE_MARKER_RE"` → `if false` | RED — self-test `FAIL 3 RED identity marker names PROD — refused, loudly (exit 1, expected 0)` |

**No control I probed was found unable to fail.** The suite's quality is materially above a pin
farm: `runScript()` extracts the shipped `run:` block out of the YAML and EXECUTES it against
stubbed `psql`/`supabase`/`gh`, and each executed arm carries a defanged twin that asserts the
step stops failing when its guard is deleted. The self-test boots a real PostgreSQL 16 cluster
from `scripts/pg-lane/run.sh --print-pgbin`, loads fixtures, and observes each refusal.

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|---|---|---|---|
| `CI-MIGRATE-01` | 01-06 | ✓ SATISFIED | `apply-test` exists, is bound to `environment: Test`, holds the mutex, and gates PROD. The mechanism that "nothing applies migrations to TEST" is closed structurally; the wild non-empty exercise is truth 4 |
| `164.2-TEST-APPLY-PROVENANCE` | 01-06 | ✓ SATISFIED | `TODOS.md:1378` closed `[x]`; VAC-08 `0 absent` at a SHA-bound run; the hand-apply-through-the-Management-API path is replaced by a workflow job |
| `164.5-TEST-VAC08-DROP-UNAPPLIABLE` | 04, 06 | ✓ SATISFIED | Two-part verdict recorded; narrow half closed by measurement (no pragma shipped, none needed); general half booked as `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` and routed to Phase 164.9 |

**Orphaned:** none. The ROADMAP `**Requirements**` line for 164.8 names exactly these three TODOS
entries plus "TBD (no v1.20 requirement IDs)".

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| — | — | `TBD` / `FIXME` / `XXX` | — | **NONE** across all nine phase files. The debt-marker gate is clean |
| `scripts/restore-test-from-baseline.sh` | 1554 | post-COMMIT extension guard is a strict scalar equality on a whole-database `count(*) FROM pg_extension` (`:331`), running after the COMMIT | ⚠️ WARNING | Still shipped at HEAD, unchanged. It reddened a CORRECT restore on run `34274355596` by reporting a legitimate GAIN (`pg_net`, 6→7) with a loss-shaped message. Deliberately deferred by founder instruction and routed to Phase 164.9. Not a blocker: it is post-commit, so it cannot corrupt a restore, and the phase records it honestly. Its one real cost — eating the post-verify step — is now discharged by run `34330741339` |

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | The extension guard's two-directional, per-schema fix and its two new self-test arms | Phase 164.9 | ROADMAP ROUTED-HERE block item 1: *"the remedy must be TWO-DIRECTIONAL and scoped to the schema it reasons about, with both directions observed RED before it is believed"* |
| 2 | `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` — a data-reading migration valid for PROD refusing on a schema-only TEST | Phase 164.9 | `TODOS.md:2740`; ROADMAP `:1512` |
| 3 | `[164.8-PUSH-RACE-VAC08]` — `sql-tests` and `apply-test` share one advisory key with nothing ordering them | Phase 164.9 | `TODOS.md:7460`; ROADMAP `:1539` |
| 4 | `[164.8-TEST-DATA-RESEEDED]` — reference rows destroyed by the schema-only restore | Phase 164.8.1 (SHIPPED, `06db9958`) | `TODOS.md:2692`; ROADMAP `:1468` |

These are correctly routed and do NOT count as gaps.

### Human Verification Required

#### 1. The non-empty applied-set branch, in the wild

**Test:** Land the next real migration PR to `main`. Watch the push-triggered
`supabase-migrate.yml` run and open the `apply-test` job's
`Post-apply applied-set verification (affirmative)` group.
**Expected:** `planned N migration version(s); push reported N.` with **N > 0** and the two
sets equal; at least one `Applying migration <version>...` line in the push log; `apply` runs
only after `apply-test` concludes success.
**Why human:** No push-triggered run has ever reached these jobs — the last push that applied a
migration (`34200188676`, `7146c574`) predates `apply-test`. Both dispatch proofs had 266/266 on
both ledgers, so `planned` and `applied` were both empty and equality held trivially:
`34367135073` printed `planned 0 migration version(s); push reported 0.` verbatim. A genuine
PARTIAL apply cannot be manufactured on a shared database.

**My judgment on whether the fixture evidence is sufficient (asked for explicitly).**
It is sufficient to call the control WIRED and FALSIFIABLE; it is not sufficient to call the
pipeline PROVEN. Two things distinguish this from a grep pin, and they matter:
(a) the pins EXTRACT the shipped `run:` block from the YAML and EXECUTE it, so the thing under
test is the shipped bytes and not a paraphrase; and (b) the fixture bodies are the measured
stdout of run `34200188676` under the same pinned CLI 2.98.2, including the confirmation-prompt
echo that made the old scrape unfalsifiable — and the suite asserts BOTH directions, so a revert
cannot read as a refactor. I confirmed (b) by reverting the TEST site and watching three arms go
red (M5). What remains genuinely unmeasured is one shape only: the CLI's real output when it
aborts mid-batch. Everything else about the check has been executed. I would not hold the phase
for it, and I would not let the next migration PR merge without a human reading that group.

### Gaps Summary

The phase's engineering is done and it holds up under adversarial probing. TEST genuinely holds
266 CLI-shaped ledger rows and PROD's catalogue; the restore is a real one-transaction mechanism
whose 26 arms I ran myself on a real cluster and whose refusals I falsified; the apply-on-merge
pipeline exists, is bound to `environment: Test`, holds the mutex, and gates PROD through both a
`needs:` and an `if:`, with every one of those gates going red under mutation. `VAC-08` prints
`0 absent … 0 NEW drift` at a SHA-bound run, the ratchet is empty beside an AIM, and the Area 3
pragma verdict is recorded two-part with its open half routed to a named phase.

What fails is the RECORD, and it fails in the direction this repo treats as a defect class rather
than a nicety.

**First**, three places still assert that the Supabase CLI post-verify "has NEVER executed" —
`deferred-items.md` items 4 and 6, `164.8-04-SUMMARY.md`'s coverage id `D4b`, and the ROADMAP's
Phase 164.9 ROUTED-HERE item 2. It executed. Run `34330741339`, step 24, concluded `success` and
printed `post-verify: the pinned CLI parses the seeded ledger and reports nothing pending`, with
both strings asserted positively against the pinned 2.98.2 — the exact uncertainty item 4 exists
to name. The claim is now carried forward as scope for Phase 164.9, so a false sentence is
routing real future work.

**Second**, and this is the one I would fix first: **a second full `mode=restore` of shared TEST
happened and is recorded nowhere.** `34329459044` (preflight, 08:30Z) and `34330741339`
(`mode=restore` with the confirm token, 08:44Z), both at head `a622df27`, both actor `AI-Isaiah`.
`grep -rn "34330741339\|34329459044" .planning/` returns zero hits. Shared TEST's present state
was set by the 08:44 run, while every artifact in the phase — and the ROADMAP's criterion-5
closure — attributes it to `34274355596` from the previous evening. This phase's own CONTEXT
makes auditability of a destructive act on a shared database non-negotiable (safety rules 1-4,
"back up … so the act is reversible", "founder-executed or founder-approved AT THE MOMENT OF
EXECUTION"). The act was performed with the machinery working correctly; only the ledger entry
is missing. That is cheap to fix and expensive to leave.

**I disagree with the SUMMARYs on one point of substance, plainly:** `164.8-04-SUMMARY.md`
reports `D4b` as FAILED and its Self-Check as *"PASSED (with D4b honestly reported FAILED — the
CLI post-verify was skipped and has never run)"*. That was true when written on 2026-09-08. It
was falsified the following morning, before plan 06 closed the books at 16:22Z, and plan 06 did
not re-measure it. The self-discipline the SUMMARY is exercising is the right one; the number it
is applied to went stale eight hours before the phase ended. Both gaps are corrections of record,
not re-engineering. Neither touches a shipped control.

---

_Verified: 2026-09-09T17:05:00Z_
_Verifier: Claude (gsd-verifier)_

---

## Orchestrator resolution — 2026-09-09T18:20:00Z

⛔ **The `status:` field above was changed from `gaps_found` to `passed` by the ORCHESTRATOR, not
by the verifier.** The verifier's own verdict, its score, its gap texts and its eight mutation
readings are untouched above and remain the record of what it found. This section states what
changed afterwards and who changed it, so nobody reads a `passed` and assumes the verifier wrote
it.

Both gaps were closed by commit `2832e139`, and each was re-measured by the orchestrator before
being written — the verifier's finding was the lead, not the evidence:

**Gap 1 — three files asserted the CLI post-verify had never executed.** Confirmed independently:
`gh run view 34330741339` → `conclusion=success`, head `a622df27`, step 24, under the pinned CLI
2.98.2. Corrected in `deferred-items.md` items 4 and 6, `164.8-04-SUMMARY.md` (`coverage.D4b`, its
`rationale`, its Blocked-on line and its `## Self-Check` heading) and the ROADMAP's Phase 164.9
ROUTED-HERE item 2, which was scoping downstream work on a false sentence.
⚠️ The claim stood in **five** places, not the three the verifier named. The extra two surfaced
only because a full grep was run after the named ones were fixed — one of them was the `rationale`
field sitting directly under a `coverage` entry that had just been corrected to `pass`, i.e. the
file contradicted itself in adjacent lines.

**Gap 2 — two destructive restores of shared TEST were recorded nowhere.** Confirmed:
`grep -rn "34330741339\|34329459044" .planning/` returned zero hits before the fix. Both runs are
now in `164.8-04-RESTORE.log` with readings and actor, and the ROADMAP's criterion-5 attribution
carries a dated amendment: run `34274355596` concluded `failure` (it committed, then exited 1 on
the post-COMMIT extension guard) and is **not** the run that left shared TEST as it stands.

**NOT closed, and deliberately so:**
- The applied-set comparison has still never run on a non-empty set. Carried in `164.8-UAT.md`
  test 5 as `blocked`, and in two SUMMARY `coverage` blocks as `human_judgment: true`. Only a
  merge that carries a migration creates the occasion.
- The post-COMMIT extension guard still fires on a legitimate gain. Routed to Phase 164.9 item 1.
- The five code-review Warnings are routed to **Phase 164.8.2 GATEHARDENING**, inserted for them.

_Resolution written by: Claude (orchestrator), not the verifier._
