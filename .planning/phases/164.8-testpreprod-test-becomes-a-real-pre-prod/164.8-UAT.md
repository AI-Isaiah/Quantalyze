---
status: partial
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
source: 164.8-01-SUMMARY.md, 164.8-02-SUMMARY.md, 164.8-03-SUMMARY.md, 164.8-04-SUMMARY.md, 164.8-05-SUMMARY.md, 164.8-06-SUMMARY.md
started: 2026-09-09T16:40:00Z
updated: 2026-09-09T16:40:00Z
---

⛔ **READ THIS BEFORE TRUSTING ANY `pass` BELOW. NO HUMAN OBSERVED ANY OF IT.**

This phase ships CI workflows, a destructive database-restore script and gate pins. There is no
UI, no user-facing surface and nothing to click, so conversational UAT in its normal sense does
not apply. Every result below carries an explicit `source:` and it is never `human`:

- `source: automated` — deterministically covered by the `coverage:` block of a SUMMARY whose
  named verification refs are passing. 22 of 27 deliverables.
- `source: measured` — the checkpoint asked whether something had ever RUN. It has now, and the
  run is named. This DISCHARGES the question the checkpoint was created to hold open; it does NOT
  substitute for a human judging whether the outcome is acceptable.
- `source: open` — genuinely unmet. Not passed, not skipped, not quietly dropped.

⚠️ Two checkpoints below (`E6`, `D4b`) were `human_judgment` because three phase artifacts assert
the Supabase CLI post-verify "has NEVER executed". That assertion was TRUE when written on
2026-09-08 and became FALSE at 2026-09-09T08:44Z. It is still shipped in `deferred-items.md`
items 4 and 6, `164.8-04-SUMMARY.md` coverage id `D4b`, and the ROADMAP's Phase 164.9 ROUTED-HERE
item 2 — where it is currently scoping downstream work that may not be needed.

## Current Test

[testing paused — 1 item outstanding, and it cannot be closed inside this phase]

## Tests

### 1. The seven pre-write refusals are coded and ordered ahead of the first write (D6, plan 01)
expected: Each refusal is a permanent RED arm of the self-test, not merely code that looks ordered — including the credential and publication refusals, which plan 01 could only exercise by hand.
result: pass
source: measured
evidence: |
  `bash scripts/restore-test-from-baseline.sh --self-test` → `self-test OK (26/26 arms ...)`,
  run by the phase verifier on a real PostgreSQL 16 cluster. The arm labels include
  `RED  credential absent` and `RED  publication row for a table the dump does not re-create (W3)`,
  so both hand-only refusals are now machine-exercised arms. `EXPECTED_ARMS=26` read from the
  script, not restated from prose.

### 2. In restore mode the CLI proves the ledger is SHAPE-consistent (E6, plan 03)
expected: The pinned CI CLI (2.98.2) — not the local 2.84.2 the strings were measured against — parses the seeded ledger and reports nothing pending.
result: pass
source: measured
evidence: |
  Run `34330741339` (head `a622df27`, `mode=restore`, 2026-09-09T08:44Z), step 24 → `success`,
  printing `post-verify: the pinned CLI parses the seeded ledger and reports nothing pending —
  ledger SHAPE is consistent.` Both asserted strings held under the pinned 2.98.2, which was the
  exact uncertainty the checkpoint existed to name.

### 3. CLI post-verify reports nothing pending (D4b, plan 04)
expected: The step executes at all — plan 04 recorded it as skipped because step 19 exited 1 on the extension guard before reaching it.
result: pass
source: measured
evidence: |
  Same run and step as test 2. `164.8-04-SUMMARY.md` reports `D4b` FAILED with "the CLI
  post-verify was skipped and has never run" — true when written 2026-09-08, falsified 08:44 the
  next morning, ~8 hours before plan 06 closed the books without re-measuring.
follow_up: The three artifacts still carrying the stale assertion must be corrected; see Gaps.

### 4. A second destructive restore of shared TEST is recorded (verifier finding)
expected: Every destructive rebuild of a database shared with other people's CI is attributable — actor, ref, sha, timestamp.
result: issue
source: measured
reported: "Runs 34329459044 (preflight, 08:30Z) and 34330741339 (mode=restore, 08:44Z), both at a622df27, actor AI-Isaiah, appear nowhere in .planning/ (0 hits before VERIFICATION.md was written). The ROADMAP attributes shared TEST's present state to run 34274355596 — which concluded FAILURE."
severity: major

### 5. The applied-set comparison exercised on a NON-EMPTY set in the wild (D3 plan 05 / M5 plan 06)
expected: A real `supabase db push` that applies at least one migration, where `planned` and `applied` are both non-empty and the comparison therefore decides something.
result: blocked
source: open
blocked_by: prior-phase
reason: |
  Both dispatch proofs (`34354619770` and `34367135073`) ran with 266/266 on both the TEST and the
  PROD ledger, so both sets were empty and equality held trivially. Neither plan 05 nor plan 06
  added a migration, so neither created an occasion. Only the next merge that actually carries a
  migration can exercise it. Until then the non-empty behaviour rests entirely on pins that
  extract the shipped step's shell and run it against fixtures measured from run `34200188676`.
  ⚠️ The phase verifier's judgement, recorded verbatim: "Sufficient to call the applied-set check
  wired and falsifiable; not sufficient to call the pipeline proven. What remains genuinely
  unmeasured is one shape: the CLI's real output when it aborts mid-batch."

## Summary

total: 5
passed: 3
issues: 1
pending: 0
skipped: 0
blocked: 1

auto_covered_not_presented: 22

## Gaps

- truth: "Every destructive rebuild of shared TEST is attributable from the planning record"
  status: failed
  reason: "Runs 34329459044 and 34330741339 are recorded nowhere in .planning/; the ROADMAP credits a run that concluded failure (34274355596)"
  severity: major
  test: 4
  root_cause: "Plan 04's artifacts were written 2026-09-08; the second restore ran 2026-09-09T08:44Z and no artifact was re-measured before plan 06 closed the books at 16:22Z"
  artifacts:
    - path: ".planning/ROADMAP.md"
      issue: "criterion-5 closure attributes TEST's state to run 34274355596, which concluded failure"
    - path: ".planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-RESTORE.log"
      issue: "does not record the 2026-09-09 preflight and restore runs"
    - path: ".planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/deferred-items.md"
      issue: "items 4 and 6 assert the CLI post-verify has never executed; it executed and passed"
    - path: ".planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-SUMMARY.md"
      issue: "coverage id D4b reported FAILED on the same stale basis"
  missing:
    - "Append 34329459044 and 34330741339 to 164.8-04-RESTORE.log with readings and actor"
    - "Repoint the 'TEST was restored by run X' sentences at 34330741339"
    - "Correct D4b to MET, citing run 34330741339 step 24 and the verbatim post-verify line"
    - "Remove the stale assertion from the ROADMAP's Phase 164.9 ROUTED-HERE item 2, so it stops scoping work that may not be needed"
  debug_session: ""
