---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 05
type: execute
wave: 5
depends_on:
  - 164.8-04
files_modified:
  - .github/workflows/supabase-migrate.yml
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/supabase-migrate-test-first.test.ts
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On every push to main touching supabase/migrations/**, a job bound to `environment: Test` applies the merged migrations to TEST with the pinned Supabase CLI BEFORE the PROD `apply` job can start (Area 1 Q1/Q3/Q4)."
    - "A TEST apply that fails OR is skipped blocks the PROD apply — a `skipped` upstream is judged as a FAULT by an `if: always()` verdict job that names every possible cause (Area 1 Q2; the `frontend` aggregator idiom in ci.yml)."
    - "`apply-test` is NOT a write path to shared TEST from an arbitrary ref (B3): it runs on `push` to main and on `workflow_dispatch` ONLY when `github.ref == 'refs/heads/main'`; a dispatch from any other ref runs a `dispatch-ref-guard` job that exits 1 naming the ref, and the verdict job names 'dispatched off main' as a cause of a skip — pinned line-exact, so a wrong-ref dispatch is a NAMED red."
    - "The TEST apply holds the shared-TEST advisory mutex (key 61616158) across marker check → dry-run → apply → post-verify (Area 4 Q3), and an absent credential is exit 1 naming the variable on EVERY event."
    - "The PROD apply now waits for a human (Production required_reviewers since 2026-09-08); the TEST apply does NOT (no reviewers on `Test` by founder decision) — the reviewer sees `apply-test` green before approving."
    - "The pipeline is proven end-to-end by a SHA-BOUND workflow_dispatch run — the run's `headSha` equals the merge commit on main — in which apply-test → verdict → plan → apply all conclude success with nothing pending on either database (W7)."
  artifacts:
    - ".github/workflows/supabase-migrate.yml — new jobs `dispatch-ref-guard`, `apply-test` and `apply-test-verdict`; `apply` gains `needs: [plan, apply-test]`"
    - "src/__tests__/supabase-migrate-test-first.test.ts — line-exact pins over the job blocks with calibration twins; cross-file mutex suffix identity; the verdict script and the ref guard executed, not grepped"
    - "src/__tests__/critical-regressions.test.ts — existing pins on the plan/apply env-gate symmetry still green with the new jobs present"
  key_links:
    - "`apply.needs` includes `apply-test` AND `apply.if` requires `needs.apply-test.result == 'success'` — the `needs:` blocks on failure by default, the `if:` blocks on skip, the verdict job makes the skip a named red"
    - "`apply-test.if` is `github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'` — the same guard shape Plan 03's restore workflow carries, because the exposure is the REF, not the database (the marker check confirms the right database, never the right code)"
    - "`apply-test` uses `TEST_SUPABASE_DB_URL` rewritten to the :5432 session DSN with `supabase db push --include-all --db-url` — the same CLI, same flags, same `migration list` reverted-grep as PROD's push step, a different credential MODE, stated in the job comment as the one deliberate deviation from PROD's shape"
---

<objective>
Make TEST the stage every migration crosses before PROD: the apply-on-merge pipeline.

Purpose: CONTEXT Area 1 (all four decisions) and Area 4 Q3/Q4. With TEST's ledger now
CLI-consistent (Plan 04), `supabase db push --include-all` runs natively against TEST —
no bespoke applier, no cut, no pragma for THIS migration (the founder's dead-scope list; the
general data-dependent case is open, Plan 06). The accepted coupling (a foreign hung mutex
holder now sits between a merge and the PROD deploy) is IMPLEMENTED as decided, not softened:
the acquire cap stays the runbook's 3600 s and the job TTL 90 min.

Revision 2026-09-08 (plan-checker REVISE): B3 — `supabase-migrate.yml` has a bare
`workflow_dispatch:` trigger with no branch restriction (line 88), and the original `apply-test`
had no ref guard, so anyone with dispatch permission could apply ANY branch's migrations to
shared TEST with no PR and no review. The restore workflow (Plan 03) already answers this with a
guard job; this plan carries the equivalent. NOT a defect: `apply-test` lacking `needs: plan` —
on push the code is already merged and `plan` is a dry-run audit record, not a review gate.
W7 — the end-to-end proof selects the run by `headSha`, not by event.

Output: `supabase-migrate.yml` with `dispatch-ref-guard` + `apply-test` + `apply-test-verdict`,
pins, and a SHA-bound dispatch proof.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-PATTERNS.md
@.github/workflows/supabase-migrate.yml
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `dispatch-ref-guard` + `apply-test` + `apply-test-verdict` jobs; PROD `apply` gated on both</name>
  <files>.github/workflows/supabase-migrate.yml</files>
  <precondition>Plan 04's restore has run and its CLI post-verify reported nothing pending for `supabase db push --include-all --dry-run --db-url <TEST session DSN>`. Read that for what it is (W4): proof of ledger SHAPE — the CLI parses TEST's ledger without `ErrMissingLocal` (RESEARCH A.3(1)) and finds no pending version — which is exactly the precondition this job needs to go green. It is NOT proof that any object exists on TEST; that came from the script's in-transaction shape assertions (Plan 04's `restore:` line) and is re-proven by the applied-ness probes on the next `sql-tests` run (Plan 06). Read Plan 03's SUMMARY (`164.8-03-SUMMARY.md`) and Plan 04's (`164.8-04-SUMMARY.md`) before editing; the restore workflow file `.github/workflows/test-restore-from-baseline.yml` and its wiring test `src/__tests__/test-restore-workflow-wiring.test.ts` exist on main by now — read both.</precondition>
  <read_first>
    - .github/workflows/supabase-migrate.yml:1-251 — the whole file, from its header to the `Push migrations to production` step. Sections: lines 107-128 (plan job shape); 129-166 (the PROD credential check whose push-gate critical-regressions pins — leave it byte-identical); 187-189 (apply needs plan / if); 199-205 (CLI pin rationale); 241-250 (the apply + C-0331 post-verify body to twin); 40-49 (one-time setup header); 60-88 (the PR-Y2 comment whose item 2 at line 65 says Test is still 38 migrations BEHIND prod — now false, gets a dated correction, not a deletion); line 88 the bare workflow_dispatch trigger (B3).
    - .github/workflows/test-restore-from-baseline.yml — Plan 03's Acquire/Release steps, credential assert, marker step and the `dispatch-guard` job: COPY them byte-for-byte (Task 2 pins the suffix identity across all three files and the guard's shape).
    - .github/workflows/ci.yml:2160-2300 — the frontend aggregator's if always() result loop (line 2207; the sql-tests row at 2246-2284 with its two-cause error), and 2187-2191 (why a bare needs chain is not enough).
    - 164.8-PATTERNS.md § 1 — shapes (a) vs (b); this plan uses BOTH: `if:` on `apply` for the block, a verdict job for the named red.
  </read_first>
  <action>
Edit `.github/workflows/supabase-migrate.yml` (surgical: the `plan` job and the PROD `apply` step bodies stay byte-identical except the `needs:`/`if:` lines named below).

**Job `dispatch-ref-guard`** (B3; `if: github.event_name == 'workflow_dispatch' && github.ref != 'refs/heads/main'`, `runs-on: ubuntu-latest`, `timeout-minutes: 5`): prints the ref through `env:` (never inline), explains that `apply-test` writes to a database shared with other people's CI and therefore applies only what is ALREADY MERGED, prints the correct dispatch (`gh workflow run supabase-migrate.yml --ref main`), exits 1 in all cases. Comment: mirrors Plan 03's `dispatch-guard`; the marker check downstream proves the right DATABASE, this job proves the right CODE. Note in the same comment that the PROD `plan`/`apply` jobs on an off-main dispatch are held by the `Production` environment's required reviewer (since 2026-09-08) — a human gate, which `Test` deliberately does not have, which is why `apply-test` needs this job.

**Job `apply-test`** (placed between `plan` and `apply`; `if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'` (B3); `runs-on: ubuntu-latest`; `timeout-minutes: 90` with the runbook § 2 derivation comment — setup + 3600 s acquire cap + apply; `environment: Test`; NO `needs:` — it must not wait for the Production-environment `plan` job, which now waits for a human reviewer; the reviewer approves `plan`/`apply` after seeing this job green). Steps:
  1. checkout pinned, `persist-credentials: false`.
  2. `Assert TEST credential is configured` — NO `if:`, on every event: `TEST_SUPABASE_DB_URL` empty ⇒ `::error::` naming it, `HARD FAILURE, not a skip`, exit 1. Comment: the PROD job tolerates an unconfigured `workflow_dispatch` clone (D-02/R1); this job does not, because a green TEST apply that applied nothing is exactly "a freshly-deployed worker meets an OLD schema" one environment over (PATTERNS § 1).
  3. `supabase/setup-cli` pinned SHA, `version: 2.98.2` (same as PROD, same rationale comment).
  4. install `postgresql-client`.
  5. `Acquire shared-test-db mutex` — byte-identical to Plan 03's step (fail-loud credential prefix + the ci.yml suffix).
  6. `Which database am I on` — the marker step from Plan 03, verbatim; refuse on NULL / non-TEST / PROD-naming.
  7. `Plan — dry-run apply (TEST)` — derive the session DSN exactly as the mutex step does (never echoed), then `supabase db push --include-all --dry-run --db-url "$dsn"` (command-fidelity twin of the PROD plan step; a log record, not a gate).
  8. `Push migrations to TEST` — `set -euo pipefail`; `supabase db push --include-all --db-url "$dsn"`; then the C-0331 twin: `supabase migration list --db-url "$dsn" | tee "$RUNNER_TEMP/test-migration-list.txt"` and the same `reverted` grep ⇒ `::error::` + exit 1. Comment states the ONE deliberate deviation from PROD's shape: credential MODE is `--db-url` (the existing repo secret, session-mode rewrite, no `supabase link`, no access token) rather than project-ref + password — and that the DSN handed to the CLI must be percent-encoded (fix lives in the secret, never in YAML).
  9. `Release shared-test-db mutex (best effort)` — `if: always()`, verbatim.

**Job `apply-test-verdict`** (`needs: [apply-test]`, `if: always()`, `runs-on: ubuntu-latest`, `timeout-minutes: 5`): one step that prints `apply-test=${{ needs.apply-test.result }}` (through `env:`) and: `success` ⇒ zero exit status; `skipped` ⇒ `::error::` naming THREE causes cheapest-first — (1) the workflow was dispatched from a ref other than main, so `apply-test`'s `if:` excluded it and `dispatch-ref-guard` already printed why (B3), (2) the job's `if:`/`needs:` were edited so it no longer runs on this event, (3) the workflow was cancelled while queued — exit 1; anything else ⇒ `::error::` naming (1) a mutex acquire timeout (runbook §§ 2-3: queue depth vs wedged holder — the ACCEPTED coupling of CONTEXT, a foreign holder now delays PROD), (2) the migration itself failed on TEST — read the `Push migrations to TEST` step, `At statement:` names the file — which is the phase's entire purpose, caught against a real Postgres instead of customers; and, B4, (3) the migration RAISEd from a data-reading DO block that is valid on PROD's rows and not on TEST's empty tables — the open case booked as `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`, whose interim remedy is to revert the merge, never to edit this workflow; exit 1. Comment: this job exists so a skipped or failed TEST apply is a RED check with a cause on the Actions board, not a grey `apply` that "did not run" (the ci.yml reasoning at 2187-2191).

**PROD `apply`:** `needs: [plan, apply-test]` and `if: needs.plan.outputs.configured == 'true' && needs.apply-test.result == 'success'` — nothing else in the job changes. Comment beside it: a failed OR skipped TEST apply blocks PROD (Area 1 Q2, warn-only rejected); the verdict job is where the cause is printed.

**Header edits:** in "Required GitHub repo configuration" add `Environment Test` (exists 2026-09-08, no protection rules by founder decision — do not add reviewers) and `Secret TEST_SUPABASE_DB_URL` (repo-level, shared with ci.yml); in the "Safety story" add the line that TEST is applied FIRST and PROD waits on it, and the B3 line that a dispatch off main never reaches TEST; under the PR-Y2 comment item 2 add a dated `⚠️ CURRENCY` sentence: TEST was rebuilt from the PROD dump by Phase 164.8 (restore run id from Plan 04's SUMMARY) and is applied on every merge by the `apply-test` job below, so "38 BEHIND" no longer describes it. Also correct the "required-reviewer gates NOT yet configured" statements with a dated note that `Production` carries `required_reviewers` since 2026-09-08 (measured via `gh api …/environments`) — the `environment:` pins already honour it, no YAML change.

Run `npx vitest run src/__tests__/critical-regressions.test.ts` — the existing `plan`/`apply` slices must still pass; if the job-slicing regex there stops at the next job-name line, the inserted jobs are invisible to them by design — measure, do not assume.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts -t "supabase-migrate" && node -e 'const y=require("fs").readFileSync(".github/workflows/supabase-migrate.yml","utf8");const a=y.slice(y.indexOf("\n  apply:\n"));if(!/needs: \[plan, apply-test\]/.test(a))process.exit(1);if(!/needs\.apply-test\.result == .success./.test(a))process.exit(2);if(!/\n  apply-test-verdict:\n/.test(y))process.exit(3);if(!/\n  dispatch-ref-guard:\n/.test(y))process.exit(4);const t=y.slice(y.indexOf("\n  apply-test:\n"),y.indexOf("\n  apply-test-verdict:\n"));if(!/if: github\.event_name != .workflow_dispatch. \|\| github\.ref == .refs\/heads\/main./.test(t))process.exit(5);console.log("gate + ref guard wired")'</automated>
  </verify>
  <done>`dispatch-ref-guard`, `apply-test`, `apply-test-verdict` exist; `apply-test` carries the ref guard `if:`; PROD `apply` needs both `plan` and `apply-test` and its `if:` requires `apply-test` success; the existing critical-regressions pins on `plan`/`apply` stay green.</done>
</task>

<task type="auto">
  <name>Task 2: Pins — line-exact, block-sliced, cross-file mutex identity, verdict-loop and ref-guard semantics EXECUTED</name>
  <files>src/__tests__/supabase-migrate-test-first.test.ts, src/__tests__/critical-regressions.test.ts</files>
  <read_first>
    - src/__tests__/test-restore-workflow-wiring.test.ts — Plan 03's file; reuse its slicing/calibration helpers by COPY (the repo's wiring tests are self-contained files; say so in a comment rather than creating a shared helper nobody else imports).
    - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts:27-36 — a grep pin goes green the moment someone keeps the strings and guts the logic, so that test EXTRACTS the step's shell script and RUNS it with a stub on PATH, and asserts on exit codes.
    - src/__tests__/critical-regressions.test.ts:1290-1312 — the byte-identity regex.
    - src/__tests__/critical-regressions.test.ts:1033-1063 — the plan/apply env-gate symmetry describe, where the new environment pin is added.
  </read_first>
  <action>
Write `src/__tests__/supabase-migrate-test-first.test.ts` (reads via `node:fs`; slices `dispatch-ref-guard:`, `apply-test:`, `apply-test-verdict:` and `apply:` blocks by their job-name lines; every pin has a calibration twin on an in-memory mutated copy):
  - `environment: Test` is a live line inside `apply-test:` exactly once; `apply-test:` has NO `needs:` line (a `needs: plan` inserted flips it — it would make TEST wait on the human-gated Production job);
  - B3, line-exact: `apply-test:` carries the live line `if: github.event_name != 'workflow_dispatch' || github.ref == 'refs/heads/main'` (a copy with the `||` clause removed, and a copy with the `if:` deleted, each flip it); `dispatch-ref-guard:` carries the live line `if: github.event_name == 'workflow_dispatch' && github.ref != 'refs/heads/main'` and its script is EXTRACTED and RUN with `GITHUB_REF=refs/heads/feature-x` in the env the step reads, asserting exit 1 and that the output names the ref and the correct `--ref main` dispatch; the two `if:` lines are each other's negation on both predicates (assert the four literals: `event_name != / ==`, `ref == / !=`);
  - `timeout-minutes: 90` live in `apply-test:`;
  - the credential step has NO `if:` between its `- name:` and `run:`, contains `exit 1` and `::error::` naming `TEST_SUPABASE_DB_URL`; the `apply-test:` block, comments masked and the Release step sliced out, contains none of the softening tokens (`continue-on-error`, the double-pipe-true idiom, a zero-status exit, `::warning`, `set +e` — restate the prober test's list);
  - the three CLI commands are LIVE `run:` lines in `apply-test:`, in order: `supabase db push --include-all --dry-run --db-url "$dsn"` before `supabase db push --include-all --db-url "$dsn"` before `supabase migration list --db-url "$dsn"`; and the marker query (`shobj_description`) index < the dry-run index; the Acquire step index < the marker index;
  - `apply:` block: `needs: [plan, apply-test]` live line; the `if:` line contains both `needs.plan.outputs.configured == 'true'` and `needs.apply-test.result == 'success'`;
  - `apply-test-verdict:` block: `if: always()` live line; `needs:` names exactly `apply-test`; its script is EXTRACTED and RUN (the anti-skip-gate idiom) three times with `APPLY_TEST_RESULT` = `success` / `skipped` / `failure` set through the env the step reads, asserting exit statuses 0 / 1 / 1, that the `skipped` output contains THREE numbered causes the first naming `main`, and that the `failure` output contains THREE numbered causes the third naming `DATA-DEPENDENT-MIGRATION-ESCAPE`;
  - cross-file mutex suffix identity: the Acquire step suffix (from the `if ! command -v psql` line) in `supabase-migrate.yml` `apply-test` == `test-restore-from-baseline.yml` `restore` == `ci.yml` `sql-tests`; and the Release step is byte-identical across the three files;
  - the PROD `Push migrations to production` step body is byte-identical to its pre-edit form (pin the exact text of the current push step, lines 241-250 of supabase-migrate.yml at HEAD, as a literal so this plan's edit is proven not to have touched it).
In `critical-regressions.test.ts` add, inside the `supabase-migrate plan/apply env-gate symmetry` describe, an `it` that the `apply-test` job declares `environment: Test` (job-sliced, same idiom as the existing plan/apply pins) and an `it` that `apply` needs `apply-test` — so the repo's central CI-hardening file knows the third job exists.
Observe RED on copies for each pin (record the mutation and the failing message in the SUMMARY); never `git checkout --`.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/supabase-migrate-test-first.test.ts src/__tests__/critical-regressions.test.ts src/__tests__/test-restore-workflow-wiring.test.ts</automated>
  </verify>
  <done>All pins green with recorded RED calibrations; the verdict script is executed (not grepped) under all three results and the guard script under an off-main ref; mutex Acquire/Release identical across the three workflows.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the pipeline end-to-end with a SHA-BOUND dispatch (nothing pending on either side)</name>
  <files>.github/workflows/supabase-migrate.yml</files>
  <precondition>This plan's PR is merged to `main` (the `plan`/`apply` jobs read `environment: Production`, which now requires reviewer approval — the founder approves those two jobs; `apply-test` needs no approval).</precondition>
  <action>
After merge: record `MERGE_SHA=$(git rev-parse origin/main)` FIRST, then `gh workflow run supabase-migrate.yml --ref main` (no migration is pending on PROD or TEST, so the run exercises the ORDER, the ref guard's inverse, the mutex, the marker, the credential and the verdict without applying anything — a clean-room proof of wiring). Select the run by SHA, never by event alone (W7, memory rule "CI green is meaningless unless SHA-bound"): `gh run list --workflow supabase-migrate.yml -L 5 --json databaseId,event,headSha,status` and take the run whose `headSha == MERGE_SHA` and `event == workflow_dispatch`; if none, MEASURE_FAIL — do not read a neighbouring run. Watch it: `dispatch-ref-guard` must be `skipped` (the ref IS main); `apply-test` must conclude `success` with the TEST dry-run and push both reporting nothing to apply and the `migration list` reverted-grep clean; `apply-test-verdict` `success`; then `plan` waits for the founder's environment approval; `apply` starts only after both `plan` and `apply-test` succeeded. Read job logs via `gh api …/jobs/<id>/logs`. Record in the SUMMARY: run id, `headSha` (== MERGE_SHA, quoted), each job's conclusion and wall clock, the per-job mutex acquire wait, and the verbatim `Which database` OK line from `apply-test`. If the CLI rejects the `--db-url` DSN (percent-encoding), stop and record it: the remedy is a re-encoded SECRET (a founder settings act) followed by a re-dispatch — never a YAML transform. Then book the two founder-visible consequences of Area 1 Q1 that this run makes concrete, as ONE TODOS entry `[164.8-PUSH-RACE-VAC08]` (Plan 06 owns TODOS.md — hand the text to it via the SUMMARY): (a) on a migration merge, `ci.yml sql-tests` (VAC-08 + the applied-ness probes inside `supabase/tests/*.sql`) and `supabase-migrate.yml apply-test` contend for the same mutex in unspecified order, so main's `frontend` can be red for that push when VAC-08 wins, and Railway then skips the analytics deploy until a re-run; (b) on every PR that adds a migration, `sql-tests` is red until merge by construction (apply-on-merge was chosen over apply-on-PR). Both were surfaced in RESEARCH Pitfall 3; the escape hatches are founder decisions, not planner ones.
  </action>
  <verify>
    <automated>MERGE_SHA=$(git rev-parse origin/main); RID=$(gh run list --workflow supabase-migrate.yml -L 5 --json databaseId,event,headSha --jq ".[] | select(.event==\"workflow_dispatch\" and .headSha==\"$MERGE_SHA\") | .databaseId" | head -1); test -n "$RID" || { echo "MEASURE_FAIL: no workflow_dispatch run at $MERGE_SHA"; exit 1; }; gh run view "$RID" --json headSha --jq .headSha | grep -qx "$MERGE_SHA" && gh run view "$RID" --json jobs --jq '.jobs[] | "\(.name)=\(.conclusion)"' | tee /dev/stderr | grep -q '^apply-test=success$' && gh run view "$RID" --json jobs --jq '.jobs[] | "\(.name)=\(.conclusion)"' | grep -q '^apply-test-verdict=success$'</automated>
  </verify>
  <done>A dispatch run on main whose `headSha` equals the merge commit shows `apply-test=success`, `apply-test-verdict=success`, `plan=success`, `apply=success`, `dispatch-ref-guard=skipped`, with `apply` starting after `apply-test`; the SUMMARY records ids, the sha, timings, and the `[164.8-PUSH-RACE-VAC08]` text for Plan 06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merged migration → shared TEST | untested-on-a-real-Postgres SQL crosses here FIRST, by design |
| workflow_dispatch from any ref → shared TEST | B3: closed by the ref guard; only `main`'s already-merged tree may reach TEST |
| `apply-test` result → PROD `apply` | the only thing standing between a merge and production DDL |
| foreign mutex holder → PROD deploy latency | the ACCEPTED coupling (CONTEXT) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-16 | Elevation | PROD apply proceeds although TEST apply did not succeed | critical | mitigate | `needs: [plan, apply-test]` + `if: needs.apply-test.result == 'success'`; verdict job reddens a skip; pins execute the verdict script under all three results |
| T-164.8-17 | Elevation | `apply-test` resolves a PROD credential | critical | mitigate | it never reads `SUPABASE_PROJECT_REF`/`SUPABASE_DB_PASSWORD`; marker step refuses non-TEST before any write |
| T-164.8-26 | Elevation | an unreviewed branch's migrations applied to shared TEST by `gh workflow run --ref <branch>` (B3) | critical | mitigate | `apply-test.if` excludes off-main dispatches; `dispatch-ref-guard` exits 1 naming the ref; verdict names it as cause (1) of a skip; the guard script is executed in the wiring test; PROD's own off-main path is held by the Production reviewer gate | ⚠️ CLARIFIED 2026-09-08: the `Test` environment now also carries a deployment branch policy allowing `main` only, so a wrong-ref dispatch is refused TWICE — once by this guard job and once by GitHub before the job starts. That is defence in depth against an ACCIDENTAL wrong-ref dispatch, NOT a stronger control: `workflow_dispatch` runs the workflow file from the SELECTED ref, so someone who controls a branch can delete the guard job and the `environment:` line alike. Both controls fall to the same edit. Do not describe the branch policy as closing a hole this guard leaves open.
| T-164.8-18 | Denial of service | foreign wedged holder delays PROD up to the 3600 s cap | high | accept | CONTEXT's accepted consequence; runbook §§ 2-3 census; escape hatches are founder decisions — not softened here |
| T-164.8-19 | Tampering | a migration that is wrong for PROD but applies on TEST | medium | accept | TEST mirrors PROD's catalogue by construction after Plan 04; the PROD-side pre-flight gates (`migration-drift-check.yml`, `migration-policy.yml`) are unchanged and still run against PROD |
| T-164.8-27 | Denial of service | a migration valid on PROD's data that RAISEs on TEST's empty tables blocks the PROD apply (B4) | medium | accept | Area 1 Q2 is LOCKED: the block is by design; interim remedy is revert-the-merge; verdict cause (3) names it; the general disposition is booked to a phase in Plan 06 |
| T-164.8-20 | Information disclosure | DSN in the public log | high | mitigate | derived by parameter expansion, never echoed; same redaction posture as ci.yml's mutex step |
| T-164.8-21 | Information disclosure | survivor DDL in the public Actions log | medium | mitigate | ⚠️ OPEN AT THE TIME OF WRITING — this row books the item, it does not record a control. `run_restore` cats the WHOLE pre-census to stdout (`cat "$RESTORE_OUT_DIR/pre-census.txt"`), and column 4 of every survivor row is full DDL: `pg_get_triggerdef(...)` and a reconstructed `CREATE POLICY … USING (<qual>)`. The step that invokes the script applies NO redaction to stdout — Plan 03's redaction covers the artifact's `.err/.log/.out` channels before upload, which is a different surface. Most of this DDL is already public (it comes from committed migrations), so the residual is anything HAND-APPLIED to TEST outside the repo — and TEST is shared with other people's CI, so that is not only our own. ⛔ Do NOT close this by widening the redaction: the fix is to print survivor KEYS to stdout and leave the DDL in `survivors.sql` inside the artifact, because Plan 04 reads the census block out of the job log and must keep a readable one. The script's header block already states the honest claim and must be re-pointed here when this lands: it currently ends "(Narrowing the cats is B-batch, in the workflow.)", which named work that was never scoped. |
| T-164.8-22 | Information disclosure | `[REDACT-HOSTNAME-01]` — the SHARED psql redaction pattern misses a DNS failure | medium | mitigate | ⚠️ MEASURED 2026-09-08 against real step bytes: the three-expression pattern copied from ci.yml (158-REVIEW WR-08) strips the DSN userinfo and `server at "…"`, but a name-resolution failure emits `could not translate host name "<host>" to address`, which NONE of the three match — the TEST pooler host survived into the output. The PASSWORD is stripped (verified: 0 occurrences); the HOST was not. Closed LOCALLY in the activity-gate step by a fourth expression, so this workflow ships no known host leak. NOT closed at the source: every other copy of the three-expression pattern (ci.yml and the other psql sites in this workflow) still has the gap. ⛔ Do not fix by editing this workflow's copies alone — that diverges from ci.yml, which is the thing the copies exist to mirror. The fix belongs in the shared pattern. |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | pinned `supabase/setup-cli` SHA + apt `postgresql-client`, both already in use |
</threat_model>

<verification>
- Vitest: `supabase-migrate-test-first.test.ts`, `critical-regressions.test.ts`, `test-restore-workflow-wiring.test.ts` green.
- A workflow_dispatch run on main at the merge sha with all four jobs `success` (guard `skipped`) and `apply` starting after `apply-test`.
- Full `npm test` alone on the box before `/gsd-ship` (memory: never share the box).
</verification>

<success_criteria>
1. TEST is applied first, PROD waits — mechanically (`needs` + `if`), with a named red on skip/failure.
2. No ref but `main` can reach TEST through this workflow; a wrong-ref dispatch is a named red.
3. One mutex hold spans the TEST apply; credential absence is exit 1 on every event.
4. End-to-end proven by a SHA-bound run, not by reading YAML and not by "the newest dispatch".
5. What would make this RED: `apply-test` gaining `needs: plan`; `apply-test.if` losing the ref clause; `apply.if` losing the `apply-test` clause; the verdict script exiting 0 on `skipped`; the mutex suffix drifting from ci.yml's; the PROD push step body changing by one byte; a proof run whose `headSha` is not the merge commit.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-05-SUMMARY.md` when done, including the dispatch run id, its `headSha`, per-job conclusions, and the `[164.8-PUSH-RACE-VAC08]` text for Plan 06.
</output>
