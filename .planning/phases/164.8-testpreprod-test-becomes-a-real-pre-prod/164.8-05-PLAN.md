---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 05
type: execute
wave: 5
depends_on:
  - 164.8-04
files_modified:
  - TODOS.md
  - CLAUDE.md
  - docs/runbooks/shared-test-db-mutex.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE
  - 164.5-TEST-VAC08-DROP-UNAPPLIABLE

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Area 3 `TEST-NOT-APPLICABLE` pragma is recorded as DEAD SCOPE with the measurement that killed it, in the TODOS entry that asked for it — not silently omitted."
    - "Every TODOS entry this phase closes carries the run id / sha that closed it; the two entries this phase OPENS carry the mechanism and the founder-owned escape hatches."
    - "The first SHA-bound `sql-tests` run after Plan 03's PR prints `ledger presence: 0 absent, all 0 baselined … 0 NEW drift.` and is recorded — the ROADMAP criterion that moved here from 164.5 is closed on what the tool PRINTS."
    - "CLAUDE.md's 'Which database am I on?' section and the mutex runbook describe the world AFTER this phase (two new holders; TEST mirrors PROD; apply-on-merge), dated, beside the older paragraphs kept as lineage."
  artifacts:
    - "TODOS.md — closures with reasoning; new `[164.8-PUSH-RACE-VAC08]` and `[164.8-CI-MIGRATE-PR-TIME]` entries"
    - "CLAUDE.md — a dated CURRENCY paragraph under 'Which database am I on?'"
    - "docs/runbooks/shared-test-db-mutex.md — the fourth and fifth holders named in § 1, with their TTLs in § 2's table"
  key_links:
    - "The pragma verdict cites Plan 03's read-only reading `create_allocator_connected_strategy overloads on TEST = 0` AFTER the restore and VAC-08's `0 absent` line — the object is in neither PROD, the dump, nor TEST, and the ledger row is present, so there is nothing for a pragma to declare"
---

<objective>
Close the books: record the verdicts and the SHA-bound readings, and update the three documents
that tell the next reader which database they are on and who holds the mutex.

Purpose: CONTEXT told planning to settle the pragma question explicitly and to treat the ratchet
file's emptying as a deliverable; the ROADMAP's requirement line names three TODOS entries this
phase exists to close. A closure without its evidence is the defect class this milestone
removes, so every sentence written here quotes a run id or a sha from Plans 03-04.

Output: TODOS.md, CLAUDE.md, the runbook, and two dated ROADMAP notes.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-03-SUMMARY.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-04-SUMMARY.md
@TODOS.md
@docs/runbooks/shared-test-db-mutex.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TODOS — close five entries with evidence, record the pragma verdict, open two</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md:1378-1410 `[164.2-TEST-APPLY-PROVENANCE]`, :1435-1462 `[164.5-TEST-VAC08-DROP-UNAPPLIABLE]`, :1474-1510 `[164.7-TEST-APPLY-APPSETTINGS]`, :1519-1548 `[164.5-TEST-EXCEPT-DRIFT04]`, :2362-2390 `### CI-MIGRATE-01`, :6972 `[VAC08-LEDGER-32]`.
    - The repo's closure style: project CLAUDE.md's 164.4.1 closing reading (`[REDUNDER-PGCRON]` / `[REDUNDER-LANEBLOCKED-BLIND]` closed WITH their reasoning, the second deliberately).
  </read_first>
  <action>
Mark each entry `[x]` and append a dated closure paragraph, quoting Plan 03/04 SUMMARY readings:
  - `[164.2-TEST-APPLY-PROVENANCE]` and `[164.7-TEST-APPLY-APPSETTINGS]`: the migrations reached TEST by the restore (run id, sha) — TEST's schema is the PROD dump that already contains them (BASELINE.md's 2026-09-07/08 regeneration tables list them by name) and the ledger holds their rows; the "record the first green run's SHA" step each entry asked for is satisfied by Task 2's reading. State that the hand-apply remedy steps are superseded by the `apply-test` job for every future migration.
  - `[164.5-TEST-VAC08-DROP-UNAPPLIABLE]`: closed — and the DISPOSITION IT ASKED FOR WAS NOT BUILT, deliberately. Write the verdict: Area 3's pragma was designed for a TEST that was built from `supabase/migrations/` and therefore lacked the hand-created PROD function; after the restore TEST is a copy of PROD's catalogue, `create_allocator_connected_strategy` has 0 overloads on TEST (Plan 03 reading) exactly as on PROD, and the ledger row for `20260908120000` is present, so VAC-08 prints `0 absent` with no pragma. Going forward TEST mirrors PROD by construction (apply-on-merge, TEST first), so a DROP valid for PROD is valid for TEST; the only way the two can diverge again is a hand-created object on ONE of them — which is DRIFT-04's own class and is owned by the baseline/VAC-04/DRIFT-05 gates, not by a TEST-side pragma. A mechanism with no consumer is the vacuity this milestone removes (CONTEXT's own sentence). Record the three refused fixes as still refused (no `IF EXISTS`, no baseline line, no hand-apply) and that none was needed.
  - `[164.5-TEST-EXCEPT-DRIFT04]`: closed — its own expiry condition ("stops being an exception when baseline.sql is next regenerated from PROD after this removal applies") was met on 2026-09-08 (BASELINE.md "pure deletion, 91 lines"), and Plan 03 restored TEST from that regeneration; there is no apply list in 164.8 to exclude it from because `supabase db push` runs natively against a ledger that already holds it.
  - `### CI-MIGRATE-01`: closed by `supabase-migrate.yml`'s `apply-test` job (Plan 04 run id) — with the precision that the founder chose apply-ON-MERGE, not apply-before-`sql-tests`: PR-time SQL gates with applied-ness probes stay red until merge (see the new entry below); the three named hazards are answered — (1) the apply holds the mutex, (2) the first apply surfaced nothing because the restore made TEST current first, (3) every previously-silent gate is now armed, and the first honest green is Task 2's SHA.
  - `[VAC08-LEDGER-32]`: closed — 31 → 0 by the restore, not by 31 hand-applies; the file is empty beside an AIM (Plan 03).
Open two entries under the phase's dated heading: `[164.8-PUSH-RACE-VAC08]` (the text Plan 04's SUMMARY hands over: the merge-time mutex race between `sql-tests` and `apply-test`, Railway skipping on a red main, the founder-owned escape hatches; and the PR-time red for migration PRs by construction) and `[164.8-TEST-DATA-RESEEDED]` (public-table data on TEST was destroyed by the restore; record which e2e/seed paths re-created what on the first main CI run after Plan 03 — read off that run, and if any spec failed for a missing pre-existing row, name it here rather than re-seeding by hand).
  </action>
  <verify>
    <automated>grep -c '\[164.8-PUSH-RACE-VAC08\]' TODOS.md | grep -qv '^0$' && grep -c '\[164.8-TEST-DATA-RESEEDED\]' TODOS.md | grep -qv '^0$' && grep -n 'TEST-NOT-APPLICABLE' TODOS.md | grep -qi 'not built\|dead scope'</automated>
  </verify>
  <done>Five entries closed with run ids/shas; the pragma verdict is written where the pragma was requested; two new entries carry mechanisms and owners.</done>
</task>

<task type="auto">
  <name>Task 2: Record the SHA-bound first honest green of VAC-08, and update ROADMAP notes</name>
  <files>.planning/ROADMAP.md</files>
  <precondition>Plan 03's PR (empty ratchet file, `ENTRY_COUNT = 0`) and Plan 04's PR are merged; at least one `ci.yml` run on `main` has completed since.</precondition>
  <action>
Find the first `main` CI run after Plan 03's merge: `gh run list --workflow=CI --branch main -L 5 --json databaseId,headSha,conclusion,createdAt`; read the `sql-tests` job log via `gh api …/jobs/<id>/logs` and quote the VAC-08 lines verbatim: `Repo migrations: 266`, `ledger presence: 0 absent, all 0 baselined (see …); 0 NEW drift.`, the body-comparison summary, and the job conclusion. Memory rule: the green must be SHA-bound (`headSha` quoted) — a settled board can belong to an ancestor. If that run is RED on `sql-tests` for any arm, record the arm by name: an applied-ness probe that is still red AFTER the restore means the dump did not contain what the probe asserts, which is a real finding (DRIFT class), not this phase's regression — book it, do not paper over it. Then, with `Edit` (scoped, never a whole-file write): (a) in the Phase 164.8 section, replace the `**Plans:** 0 plans` / `TBD` block with the five-plan list (the orchestrator's `update_roadmap` step may already have done this — check first, do not duplicate); (b) under Phase 164.5's criterion 5 (`ROADMAP.md:1299`) append a dated sentence: the stronger goal it deferred to 164.8 — TEST genuinely holding these migrations — is met by the restore (run id), and VAC-08's SHA-bound reading is now `0 absent`; (c) in the Phase 164.8 goal block's SCOPE ADDED paragraph append a dated sentence pointing at TODOS `[164.5-TEST-VAC08-DROP-UNAPPLIABLE]`'s closure for the pragma verdict.
  </action>
  <verify>
    <automated>grep -n '0 absent, all 0 baselined' .planning/ROADMAP.md | grep -q . && grep -n '164.8-01-PLAN.md' .planning/ROADMAP.md | grep -q .</automated>
  </verify>
  <done>The ROADMAP carries the SHA-bound VAC-08 reading and the plan list; any residual red arm is named, not hidden.</done>
</task>

<task type="auto">
  <name>Task 3: CLAUDE.md currency paragraph and the runbook's new holders</name>
  <files>CLAUDE.md, docs/runbooks/shared-test-db-mutex.md</files>
  <read_first>
    - CLAUDE.md:328-350 (the "Which database am I on?" section) and the dated-CURRENCY style of the SQL gate integrity section above it.
    - docs/runbooks/shared-test-db-mutex.md § 1 (:20-73, who holds the lock and how to spot each holder by `PGAPPNAME`), § 2 (:75, the coupled numbers table), § 5 (:317, the drill's "run when CI is quiet" advice).
  </read_first>
  <action>
CLAUDE.md, under "Which database am I on?", append a dated paragraph (keep every existing sentence): as of the restore run (id, sha) TEST's `public` schema is the PROD dump at `baseline.sql` sha `27826b76…` and its ledger is CLI-shaped (one row per repo file, `statements[]` a provenance sentence — NOT the applied SQL, unlike PROD's ledger, which the memory rule "PROD ledger stores the APPLIED SQL" describes; say the difference plainly so nobody reads TEST's `statements` as evidence of execution); every merge touching `supabase/migrations/**` now applies to TEST FIRST via `supabase-migrate.yml` `apply-test` (`environment: Test`, no reviewers) and PROD's `apply` waits on it AND on the `Production` environment's human reviewer (since 2026-09-08); the marker check is the first DB statement in both jobs; `scripts/vac08-ledger-baseline.txt` is EMPTY by measurement beside an AIM and stays empty unless a founder decides otherwise; and the two NEW couplings — merge-time race with `sql-tests`, PR-time red for migration PRs — are booked as `[164.8-PUSH-RACE-VAC08]`. Also correct the section's sentence "The CLI and the browser SQL editor have no automated guard at all" with a dated addendum: the two CI jobs that write to TEST now carry the marker guard; the developer CLI and the browser editor still do not.
Runbook § 1: add `apply-test` (`supabase-migrate.yml`, holds across marker → dry-run → push → list) and `restore` (`test-restore-from-baseline.yml`, dispatch-only, holds across backup → transaction → post-verify) as holders four and five, both with `PGAPPNAME=ci-shared-test-db-mutex` (they copy the ci.yml step byte-for-byte, so they are indistinguishable from `sql-tests` in `pg_stat_activity` except by `query`/timing — say so, and say that is acceptable because the release step's dead-holder witness and the guarded terminate are the same). § 2: add both jobs' `timeout-minutes: 90` to the coupled-numbers table with the sentence that raising the acquire cap now moves FIVE TTLs. § 3 (manual unlock): add the one new consideration — a wedged holder now delays a PROD deploy, so the census in § 3 is the first thing to run when `apply-test-verdict` reports a timeout.
  </action>
  <verify>
    <automated>grep -n 'apply-test' docs/runbooks/shared-test-db-mutex.md | grep -q . && grep -n 'test-restore-from-baseline' docs/runbooks/shared-test-db-mutex.md | grep -q . && grep -n '164.8-PUSH-RACE-VAC08' CLAUDE.md | grep -q .</automated>
  </verify>
  <done>Both documents describe the post-phase world with dates and run ids, older paragraphs intact as lineage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation → the next operator's decision | a stale sentence here sends a human to write to the wrong database |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-21 | Repudiation | closures without evidence | medium | mitigate | every closure quotes a run id + sha from Plans 03/04; the VAC-08 line is pasted verbatim, SHA-bound |
| T-164.8-22 | Information disclosure | a DSN or marker text copied into docs | medium | mitigate | docs name jobs, keys, counts and run ids only |
| T-164.8-23 | Tampering | ROADMAP whole-file overwrite | high | mitigate | `Edit` scoped replacements only, never `Write` |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- `grep` gates above; a re-read of the three documents by the executor for any sentence the phase made false.
- Full `npm test` alone on the box before `/gsd-ship`.
</verification>

<success_criteria>
1. The pragma is settled in writing as DEAD SCOPE with its measurement.
2. VAC-08's first honest green is recorded SHA-bound, or its residual red is named.
3. The next reader of CLAUDE.md / the runbook sees five holders and a TEST that mirrors PROD.
4. What would make this RED: a closure paragraph without a run id; the ROADMAP still saying `0 plans`; a runbook § 2 table that still lists three TTLs.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-05-SUMMARY.md` when done.
</output>
