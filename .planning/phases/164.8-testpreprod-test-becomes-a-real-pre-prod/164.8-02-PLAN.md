---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 02
type: execute
wave: 2
depends_on:
  - 164.8-01
files_modified:
  - scripts/restore-test-from-baseline.sh
  - scripts/restore-test-from-baseline-fixtures/arm-unqualified-policy.sql
  - scripts/restore-test-from-baseline-fixtures/arm-orphan-view.sql
  - scripts/restore-test-from-baseline-fixtures/arm-stray-publication.sql
  - scripts/restore-test-from-baseline-fixtures/arm-unrecreatable-trigger.sql
  - src/__tests__/restore-test-from-baseline.test.ts
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every refusal and every abort path of `scripts/restore-test-from-baseline.sh` is OBSERVED red by `--self-test` on a throwaway cluster, and every assertion the script makes has a NAMED mutation in this plan that turns its arm red (anti-vacuity: an arm nobody can falsify is not an arm)."
    - "`EXPECTED_ARMS=17` is pinned as a literal beside a MEASURED date; the self-test asserts `total -eq EXPECTED_ARMS` and `pass -eq total`; the vitest pins the literal line-exact with a calibration twin — an arm that disappears is a red, not a silently smaller PASSED."
    - "The census's search_path independence (B1) and the derived `pg_depend` closure (B2) are each a COUNTED arm with a fixture that would have slipped through the original three-class census."
    - "Redaction is a CHECK with a subject: every arm's captured output is grepped for a DSN shape and for dollar-quoted SQL, and arm 14 runs a deliberately leaking scratch copy of the script and asserts the grep FIRES."
  artifacts:
    - "scripts/restore-test-from-baseline.sh — `--self-test` with 16 counted arms and the pinned constant; header stating scope and non-scope"
    - "scripts/restore-test-from-baseline-fixtures/arm-*.sql — four per-arm overlays applied on top of `old-test.sql` by `fresh_db`"
    - "src/__tests__/restore-test-from-baseline.test.ts — pins the script's constants (EXPECTED_ARMS, the search_path line, the derived-census marker, the filtered-line pattern, the marker regexes, the terminator selection) line-exact, each calibrated on a mutated in-memory copy"
  key_links:
    - "Arm 12 (unqualified policy) and arm 13 (orphan view) are the two fixtures the checker's B1/B2 findings describe; both are RED on a scratch copy of Plan 01's script with the corresponding line neutered, and GREEN/RED as declared on the shipped script"
    - "The `run_arm` helper and its `--expect-inverted` flip are copied from scripts/test-ledger-drift-check.sh; the calibration is arm 16 and the whitelist tripwire arm 17, both COUNTED, so `EXPECTED_ARMS` and the arms actually listed agree (W1)"
---

<objective>
Finish the proof Plan 01 started: every RED path of the restore script observed on a throwaway
Postgres, the arm count pinned, the constants pinned in vitest, the header written.

Purpose: this script will run ONE destructive transaction on a database other people's CI uses.
CONTEXT's non-negotiable safety list is only worth something if every refusal is known to fire.
The plan-checker's REVISE (2026-09-08) found three arm-accounting defects in the original single
plan — an arm count that did not match the arms listed (W1), a "redaction arm" that was a predicate
over other arms' output with no mutation that could redden it (W2), and a publication fixture that
could not be green as specified (W3) — plus two census blockers (B1, B2). This plan resolves all
five with a DEFINITE arm list and a named falsifier per arm.

Output: `--self-test` printing `16/16 arms`, four arm-overlay fixtures, the vitest pin file, the
script header.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-01-PLAN.md
@scripts/test-ledger-drift-check.sh
@scripts/prod-body-drift-check.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: The sixteen arms — every refusal, every abort, the two census blockers, redaction with a subject, and the counted calibration</name>
  <files>scripts/restore-test-from-baseline.sh, scripts/restore-test-from-baseline-fixtures/arm-unqualified-policy.sql, scripts/restore-test-from-baseline-fixtures/arm-orphan-view.sql, scripts/restore-test-from-baseline-fixtures/arm-stray-publication.sql, scripts/restore-test-from-baseline-fixtures/arm-unrecreatable-trigger.sql</files>
  <read_first>
    - Read `scripts/restore-test-from-baseline.sh` as Plan 01 left it (the file this plan extends; it does not exist at planning time, so it is named here rather than as an @-reference) and Plan 01's SUMMARY (`164.8-01-SUMMARY.md`) for the skeleton's `fresh_db` / `hold_mutex` / `run_arm` names and the three RED observations it recorded — arms 12 and 13 below are those observations made permanent.
    - scripts/test-ledger-drift-check.sh:604-619 — the run_arm helper and the expect-inverted flip.
    - scripts/prod-body-drift-check.sh:468-505 — the arm helper's captured-output redaction grep.
    - scripts/check-baseline-staleness.mjs:150-160 — the pinned-count idiom (an EXPECTED_ASSERTIONS literal beside its measurement date).
  </read_first>
  <action>
Extend `--self-test` in `scripts/restore-test-from-baseline.sh`. `fresh_db` gains an optional overlay argument: after loading `old-test.sql` it loads `fixtures/arm-<name>.sql` when given. Every arm's combined stdout+stderr is captured to `$SELFTEST_DIR/arm-<n>.out`; after EVERY arm the harness greps that file for `postgres(ql)?://` and for `\$[A-Za-z_0-9]*\$` and FAILS the arm on a hit (this is the redaction CHECK; it is not itself an arm — arm 14 is its subject). Arms, each through the REAL `--run` dispatch with `RESTORE_DB_URL` pointed at the lane, exit code AND a named output line asserted; the FALSIFIER column is what the SUMMARY records having observed RED on a scratch copy (never `git checkout --`):

  1. RED credential absent (`RESTORE_DB_URL` unset) ⇒ exit 1, output names `RESTORE_DB_URL`. Falsifier: delete the credential assert on the copy ⇒ the script proceeds to the sha check and fails there with a DIFFERENT line ⇒ arm 1's named-line assertion fails.
  2. RED marker NULL (`COMMENT ON DATABASE … IS NULL` in the arm) ⇒ exit 1 before any write (assert the stray table still exists). Falsifier: make the NULL branch print the OK sentence on the copy ⇒ exit 0 ⇒ arm fails.
  3. RED marker names PROD (`COMMENT … IS 'quantalyze PROD fixture'`) ⇒ exit 1. Falsifier: empty `RESTORE_REFUSE_MARKER_RE`'s default on the copy.
  4. RED sha mismatch (edit the generated fixture doc) ⇒ exit 1 naming both shas' first 8 chars. Falsifier: compare only the first 4 chars on the copy after editing char 5.
  5. RED stale baseline (stub `FRESHNESS_TS_CMD` to return baseline < migrations) ⇒ exit 1 naming both epochs. Falsifier: flip the comparison on the copy.
  6. RED mutex not held (`release_mutex` for this arm) ⇒ exit 1, output contains `mutex is not held`. Falsifier: `RESTORE_REQUIRE_MUTEX` default flipped to 0 on the copy.
  7. RED stray publication row (overlay `arm-stray-publication.sql`: `ALTER PUBLICATION supabase_realtime ADD TABLE public.e2e_leftover` — a table the baseline fixture does NOT re-create) ⇒ exit 1 BEFORE the transaction (stray table still present, ledger still 4 rows), output names `pub:public.e2e_leftover` and the `ALTER PUBLICATION … DROP TABLE` remedy sentence. Falsifier: delete the dump-text check on the copy ⇒ the transaction runs and aborts INSIDE at the re-ADD with psql's own `relation "public.e2e_leftover" does not exist` — exit 1 still, but the named line is absent ⇒ arm fails on the line assertion (this is why the arm asserts the LINE, not just the exit).
  8. GREEN preflight (Plan 01's G-preflight, unchanged) ⇒ exit 0, `mode=preflight`, `post-census == pre-census`, stray table present, ledger 4 rows. Falsifier: preflight sends `COMMIT;` on the copy.
  9. GREEN restore (Plan 01's G-restore, unchanged) ⇒ exit 0, line `restore: tables=2 policies=1 functions=2 ledger_rows=3 survivors=3/3 filtered=1 mode=restore`, stray table gone, trigger + policy `qualified_ref` + publication row `public.fx_keep` present, ledger = the three fixture files. Falsifier: drop `survivors.sql` from the stream on the copy ⇒ the in-transaction key-set assertion raises ⇒ exit 1.
  10. RED survivor cannot be re-created (overlay `arm-unrecreatable-trigger.sql`: a second trigger on `auth.users` calling `public.fx_absent()`, a function the baseline fixture lacks) ⇒ exit 1, transaction rolled back (stray table still present), output names `auth.users:<trigger>`. Falsifier: wrap `survivors.sql` execution in a savepoint that swallows errors on the copy ⇒ exit 0 ⇒ arm fails.
  11. RED expected-shape mismatch (mutate the EXPECTED derivation, not the dump — a scratch COPY of the script, per the arm-14 idiom, whose `tables` derivation is changed from the `^CREATE TABLE` line count to that count PLUS ONE; run the copy with `--run --mode restore` against the unmodified fixture dump) ⇒ exit 1 and rolled back, output names `tables` and reports expected 3 vs actual 2. Falsifier: remove the table-count DO block from the scratch copy ⇒ exit 0 ⇒ arm fails. ⚠️ Do NOT instead append a `CREATE TABLE` line to a dump copy: the expected shape is derived FROM THE DUMP TEXT while the database is built BY REPLAYING THAT SAME DUMP, so an extra line raises expected and actual EQUALLY and the shape assertion cannot mismatch — and appending after the doc sha is regenerated makes arm 4 fire instead. That recipe was measured self-contradictory and inert at plan-check iteration 2; it is recorded here so it is not reintroduced.
  12. GREEN unqualified-policy survivor (B1; overlay `arm-unqualified-policy.sql`: `SET search_path = public; CREATE POLICY unqualified_ref ON storage.objects USING (fx_role());` — its stored expression, rendered under the default search_path, contains NO `public.`) ⇒ exit 0, line `… survivors=4/4 …`, and after the restore `pg_policies` shows `unqualified_ref` on `storage.objects`. Falsifier: delete the census session's `SET search_path = pg_catalog` on the copy ⇒ the census misses it ⇒ the derived closure names `storage.objects:unqualified_ref` and rolls back ⇒ exit 1 (and, with the derived check ALSO neutered, `survivors=3/3` ⇒ the line assertion fails) — either way the arm is red.
  13. RED derived-census orphan (B2; overlay `arm-orphan-view.sql`: `CREATE SCHEMA analytics; CREATE VIEW analytics.v_leftover AS SELECT * FROM public.fx_keep;` — a fourth class no hand-listed census knows) ⇒ exit 1, rolled back (stray table still present, `analytics.v_leftover` still present), output contains `analytics.v_leftover` and `NOT a censused survivor`. Falsifier: `RAISE EXCEPTION` → `RAISE NOTICE` in the closure block on the copy ⇒ exit 0 and the view is gone ⇒ arm fails (this is Plan 01's recorded observation (2), now permanent).
  14. Redaction check BITES (W2's subject): write a scratch COPY of the script with `echo "$RESTORE_DB_URL"` inserted directly after the credential assert; run the COPY with `--run --mode preflight`; the arm PASSES only if the harness's redaction grep over that arm's captured output FIRES (i.e. the DSN shape is found). Falsifier: weaken the grep to `postgresql://` only while the lane DSN is `postgres://` on the copy ⇒ the leak is missed ⇒ arm fails. (The grep over arms 1-13 stays a per-arm check; this arm is what proves the check can see a real leak.)
  15. RED unknown argv (`--frobnicate`) ⇒ exit 1, output contains `unknown argument`. Falsifier: drop the `*)` refusal case on the copy.
  16. Harness calibration, COUNTED: `run_arm` with `--expect-inverted` on arm 1's command declared with want-exit 0 — the flip makes the harness expect 1, the script exits 1, the arm PASSES; then the same call WITHOUT the flip is run inside a subshell and its `FAIL` result line is asserted present — proving the harness can report a failure at all. Falsifier: make `run_arm` ignore `want` on the copy ⇒ the subshell shows `ok` ⇒ arm 16 fails.
  17. RED unresolvable-class tripwire (the derived-census `refclassid` whitelist, plan-check iteration 2 concern 1; overlay `arm-public-operator.sql`: `CREATE OPERATOR public.### (LEFTARG = int, RIGHTARG = int, FUNCTION = int4pl);` — an object class the whitelist does NOT resolve) ⇒ exit 1 BEFORE `DROP SCHEMA public CASCADE`, rolled back, output names the count and the class `pg_operator`. Falsifier: delete the whitelist-exclusion DO block on the copy ⇒ exit 0 and the operator is silently CASCADE-dropped and never re-created ⇒ arm fails. This is what makes the whitelist CLOSED BY MEASUREMENT rather than by assumption: the excluded classes are pinned empty on the LIVE database, and the pin has a subject.

Pin `EXPECTED_ARMS=17` as a literal constant beside a `MEASURED 2026-09-08` comment, assert `total -eq EXPECTED_ARMS` and `pass -eq total`, print every result line, and print the closing line `restore-test-from-baseline: self-test OK (<pass>/<EXPECTED_ARMS> arms — seven refusals fire before any write, preflight rolls back byte-for-byte, restore commits the full shape, survivors round-trip search_path-independently, the derived census names an unlisted dependent, redaction is checked with a subject, the census whitelist refuses an unresolvable class, harness calibrated)`. Replace Plan 01's interim closing line (the one announcing the skeleton) with this one — it must NOT survive, and no comment may mention it by its capitalised word either, because Task 2 negative-pins that word.
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&1 | tee /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/restore-selftest.log | grep -E 'self-test OK \(16/16 arms' && ! grep -aE 'postgres(ql)?://' /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/restore-selftest.log && git status --porcelain | grep -c . | grep -qx 0</automated>
  </verify>
  <done>`--self-test` prints `16/16 arms` and exits 0 on a throwaway cluster; the SUMMARY lists, for each of the 16 arms, the falsifier applied to a scratch copy and the RED line observed; `git status --porcelain` is clean after the run; no DSN-shaped string appears in the harness's own captured log (arm 14's leaking copy writes to its arm file, which the harness deletes after asserting on it).</done>
</task>

<task type="auto">
  <name>Task 2: Pin the constants in vitest, calibrated on mutated in-memory copies</name>
  <files>src/__tests__/restore-test-from-baseline.test.ts</files>
  <read_first>
    - src/__tests__/local-stack-lane-wiring.test.ts:124-140 — the LINE-EXACT non-comment pin (whole-file toContain stays green with the line commented out; MEASURED 2026-09-08 twice in this repo).
    - src/__tests__/prod-prober-wiring.test.ts:13-17 — CALIBRATION: mutate a copy, assert the copy differs, assert the predicate flips.
    - src/__tests__/prod-prober-wiring.test.ts:93-96 — the softening-token list to restate.
    - src/__tests__/gate-family-meta.test.ts:18-30 — SC-9: every threshold constant needs a measurement token AND a date beside it.
    - src/__tests__/mutation-runner-floors.test.ts:94-112 — comment masking before asserting a CALL exists; apply the idea to bash `#` comments.
  </read_first>
  <action>
Write `src/__tests__/restore-test-from-baseline.test.ts`. All reads through `node:fs` (never shell grep — this repo has a measured NUL-blind file). Slice the script text into its `--run` function body and its `self_test` body by the function-definition lines so pins are asserted against the REGION, not the whole file. Pins (each with a calibration twin that applies the mutation to a COPY of the text in memory and asserts the predicate flips):
  - `EXPECTED_ARMS=17` on a live (non-comment) line, exactly once, with `MEASURED 2026-09-08` on the same or the preceding line (SC-9); Plan 01's interim closing line is ABSENT — assert on the capitalised word that line used, assembled in the test from two halves so this plan and the test never carry it verbatim (a copy with the line re-inserted flips);
  - B1 pin: the census session and the transaction stream each contain a live `search_path = pg_catalog` line, and BOTH indices are LOWER than the index of `pg_get_triggerdef` and of `pg_policies` in their region (the search_path is set BEFORE the catalog is read);
  - B2 pin: the `--run` region contains a live line with `pg_depend` and a live line with `NOT a censused survivor`, and the index of the `pg_depend` line is LOWER than the index of `DROP SCHEMA public CASCADE` (the closure is asserted BEFORE the drop);
  - the terminator is mode-selected: the `--run` region contains the literal `ROLLBACK;` and the literal `COMMIT;` each on a live line, and psql's whole-file transaction flag (the one spelled `--single-` followed by `transaction`) appears NOWHERE in the script — assemble the literal in the test from two halves so this plan and the test never carry it verbatim (a copy with the flag inserted flips the pin);
  - the ordering invariant: the line index of the marker query (`shobj_description`) is LOWER than the line index of `DROP SCHEMA public CASCADE` and LOWER than the first `TRUNCATE`, inside the `--run` region (masked of comments);
  - the survivors census runs before the DROP: index of `pg_get_triggerdef` < index of `DROP SCHEMA public CASCADE`; and the publication dump-text check (`pub:public.`) index < `BEGIN;` index (W3's refusal is pre-write);
  - the filtered-line pattern is present exactly once as a live line and names `supabase_realtime`;
  - the credential assert names `RESTORE_DB_URL` inside a `::error::` line and no softening token from the restated list appears in the `--run` region (comments masked) — the only `exit` in the credential branch is non-zero;
  - the ledger INSERT column list is exactly `(version, name, statements)` and the provenance sentence contains `NOT executed on TEST`;
  - the fixtures dir contains exactly three `*.sql` files under `migrations/` with 14-digit prefixes, and exactly four `arm-*.sql` overlays (the self-test's `ledger_rows=3` and the overlay arms are derived from DIRECTORY listings, so a stray file silently changes an arm — pin both counts with an AIM: the same glob applied to a synthetic listing of five names returns five).
Run the vitest file alone, then `--self-test` again, and paste the vitest test count into the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/restore-test-from-baseline.test.ts</automated>
  </verify>
  <done>The vitest passes; every calibration twin demonstrates its pin flips on a mutated in-memory copy; the SUMMARY records for each pin the mutation that was applied and that it went RED.</done>
</task>

<task type="auto">
  <name>Task 3: Header — what the self-test proves, what it cannot, and the two things the script deliberately does not do</name>
  <files>scripts/restore-test-from-baseline.sh</files>
  <action>
Finish the script header (comments only): what `--self-test` proves and what it does NOT — it proves the mechanism on a baseline-SHAPED fixture; it cannot replay the REAL dump because the pg-lane has no `pg_net`/`supabase_vault` (state this; the real dump's applicability to hosted TEST is measured by the workflow's `preflight` mode inside a rolled-back transaction, Plans 03/04). State why the census runs under `search_path = pg_catalog` (B1, one sentence) and why the closure is read from `pg_depend` rather than from cascade NOTICEs (the 100-dependent client truncation, one sentence). State the public-repo rule: names and counts only in logs, never a DSN, never a body. State the two things this script deliberately does NOT do: it does not back up (the workflow does, BEFORE calling it — Plan 03), and it does not take the mutex (it asserts the mutex is held by someone and refuses otherwise). State what a restore DESTROYS and what is NOT backed up: every row in every `public` table on TEST; the backup the workflow takes is schema and ledger — data is NOT reversible (W6; CONTEXT safety rule 3 names ledger and schema, and this header says so in those words). Paste the verbatim closing self-test line and its date beside `EXPECTED_ARMS`. Re-run `--self-test` after the header edit (the marker regexes scan DB output, not files — but the SUMMARY must say the re-run was done). Commit.
  </action>
  <verify>
    <automated>bash scripts/restore-test-from-baseline.sh --self-test 2>&1 | grep -E 'self-test OK \(16/16 arms' && grep -c 'data is NOT reversible' scripts/restore-test-from-baseline.sh | grep -qv '^0$' && git status --porcelain | grep -c . | grep -qx 0</automated>
  </verify>
  <done>Header states scope, non-scope, the two census design reasons, and the data-loss/non-reversibility sentence; self-test still 16/16; working tree clean after the run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| self-test harness → developer box / CI runner | boots a throwaway Postgres; must never reach TEST or PROD |
| scratch copies of the script → the checkout | neutered copies must live outside the tree and never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-05 | Information disclosure | DSN or SQL body in a public log | high | mitigate | per-arm redaction grep over captured output; arm 14 proves the grep fires on a real leak; the leaking copy's output file is deleted after the assertion |
| T-164.8-06 | Repudiation | a self-test that shrank but still prints PASSED | medium | mitigate | `EXPECTED_ARMS=17` literal, `total -eq EXPECTED_ARMS`, vitest line-exact pin with a calibration twin; arm 16 proves the harness can report FAIL; arm 17 pins the census whitelist's excluded classes as measured-empty |
| T-164.8-24 | Tampering | a neutered scratch copy leaking into the tree | medium | mitigate | copies live under the scratchpad dir; every verify ends with `git status --porcelain` empty |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | none (bash + psql + node built-ins) |
</threat_model>

<verification>
- `bash scripts/restore-test-from-baseline.sh --self-test` → `16/16 arms`, exit 0, on macOS (local) — and, once Plan 03 wires it, on ubuntu inside the workflow's self-test step.
- `npx vitest run src/__tests__/restore-test-from-baseline.test.ts` green, with each pin's calibration twin recorded RED in the SUMMARY.
- `git status --porcelain` empty after the self-test.
</verification>

<success_criteria>
1. Seventeen arms, each with a falsifier observed RED on a scratch copy; `EXPECTED_ARMS=17` matches the arms listed (W1 closed in the plan text, not by the executor).
2. Redaction has a subject (arm 14) — it is no longer a predicate with no mutation (W2).
3. The publication fixture is green because it names a table the dump re-creates, and a row for a stray table is a pre-write refusal with a remedy (W3).
4. B1 and B2 are each a counted arm (12, 13) whose fixture the original census would have lost silently.
5. What would make this RED: any arm count other than 16/16; Plan 01's interim closing line surviving; the search_path line moving below the catalog read; the `pg_depend` block moving below the DROP; a DSN in the harness log.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-02-SUMMARY.md` when done, pasting the verbatim closing self-test line and the per-arm falsifier record.
</output>
