---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 02
type: execute
wave: 2
depends_on:
  - 164.8-01
files_modified:
  - .github/workflows/test-restore-from-baseline.yml
  - src/__tests__/test-restore-workflow-wiring.test.ts
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE
user_setup:
  - service: github
    why: "The `Test` GitHub Environment binds the restore job (it EXISTS since 2026-09-08 with no protection rules — do NOT add reviewers to it; the founder's approval surface is the dispatch itself plus Plan 03's checkpoint:decision). TEST_SUPABASE_DB_URL is a REPO-level secret and is visible to environment-bound jobs."
    env_vars:
      - name: TEST_SUPABASE_DB_URL
        source: "already present as a repo secret (measured 2026-09-08 via gh api …/actions/secrets); the pooler DSN on :6543, rewritten to :5432 in-job"

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The workflow is dispatch-only: no push, schedule or pull_request trigger can ever run it."
    - "Its destructive mode cannot run by accident: it requires ref == main, `mode=restore`, and a `confirm` input equal to the first 8 hex chars of the baseline sha recorded in BASELINE.md; anything else runs the guard job and exits 1 in restore mode (a mis-dispatch is a judgeable red, never a silent green)."
    - "Before the script is called, TEST's current ledger, its public schema and the survivors census are uploaded as a CI artifact — the act is reversible."
    - "The shared-TEST advisory mutex is acquired once and held across backup, script and post-verify; an absent credential is exit 1 naming the variable, never a skip."
    - "In restore mode the job ends by proving the ledger is CLI-consistent: `supabase db push --include-all --dry-run --db-url <session DSN>` reports nothing pending."
  artifacts:
    - ".github/workflows/test-restore-from-baseline.yml — jobs: dispatch-guard, restore (environment: Test, timeout-minutes: 90)"
    - "src/__tests__/test-restore-workflow-wiring.test.ts — line-exact pins over the job block, with calibration twins"
  key_links:
    - "The Acquire/Release mutex steps are the ci.yml:2522 / ci.yml:3269 steps with ONE deliberate difference: the fork-PR early-exit branch is replaced by a fail-loud credential assert (this workflow never runs on a fork PR)"
    - "The backup step runs BEFORE the script step; the script step is the header's verbatim command; the post-verify step uses the same session-DSN derivation as the mutex step"
    - "scripts/check-baseline-staleness.mjs runs as its own step BEFORE the script (the co-edit gate, self-test first), so a sha drift is a named red in the workflow log, not only inside the script"
---

<objective>
Wire Plan 01's script into a founder-dispatched, dispatch-only GitHub workflow that backs TEST up,
holds the shared mutex for the whole act, runs `preflight` or `restore`, and proves the result with
the Supabase CLI. This plan still writes NO byte to TEST: it produces the workflow and its pins.
Plan 03 dispatches it.

Purpose: CONTEXT's non-negotiable safety list — marker check, whole-act mutex, reversible backup,
founder-executed at the moment of execution, sha-verified source — must be enforced by the
WORKFLOW, not by discipline. The repo's precedents: `supabase-migrate.yml` (environment-bound
apply, credential check, pinned CLI 2.98.2, post-apply `migration list` check), `mutex-probe.yml`
`dispatch-guard` (:90-103, a guard JOB that explains itself), `prod-prober.yml` typed
`workflow_dispatch` inputs (:66-78), and `migration-drift-check.yml`'s unconditional credential
assert (:102-121).

Output: `.github/workflows/test-restore-from-baseline.yml` + its wiring test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-01-SUMMARY.md
@.github/workflows/supabase-migrate.yml
@.github/workflows/mutex-probe.yml
@docs/runbooks/shared-test-db-mutex.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `test-restore-from-baseline.yml` — guard job + restore job, one path through every step</name>
  <files>.github/workflows/test-restore-from-baseline.yml</files>
  <read_first>
    - .github/workflows/ci.yml:2522-2727 (Acquire shared-test-db mutex: the fork-PR branch at :2527-2530 that MUST be replaced, the :6543→:5432 rewrite at :2543, keepalives, the holder at :2604-2612, the 3600 s cap loop, the two-cause `::error::` at :2726) and :3269-3330 (Release, `if: always()`, never non-zero).
    - .github/workflows/supabase-migrate.yml:107-128 (environment pin, `persist-credentials: false`), :167-171 (setup-cli pinned SHA + `version: 2.98.2`), :129-166 (the OLDER split credential check — do NOT copy its tolerant branch), :40-49 (the one-time-setup header block to mirror).
    - .github/workflows/migration-drift-check.yml:102-121 — the NEWER unconditional credential assert (no `if:` on the step, `HARD FAILURE, not a skip`).
    - .github/workflows/mutex-probe.yml:90-110 — `dispatch-guard` and the real job's inverse `if:`.
    - .github/workflows/prod-prober.yml:66-78 — typed `choice`/`string` inputs and the sentence that a narrowed run can never read as a pass.
    - .github/workflows/ci.yml:300 (`actions/upload-artifact@043fb46d…` v7.0.1 pinned SHA), :68-70 (`actions/setup-node@48b55a01…` node 22), :613 (`postgresql-client` install).
    - src/__tests__/critical-regressions.test.ts:540-660 — every workflow must SHA-pin every `uses:`, declare minimum `permissions:`, set `persist-credentials: false`, and declare a `concurrency:` group; the new file is auto-covered by those globs, so conform up front.
    - docs/runbooks/shared-test-db-mutex.md § 2 (:75) — the three coupled numbers (acquire cap 3600 s, job TTL 90 min, holder sleep 6000 s); copy them, do not re-derive.
  </read_first>
  <action>
Create `.github/workflows/test-restore-from-baseline.yml`:

**Header comment** (mirrors `supabase-migrate.yml:1-58` in shape): what it does (rebuild shared TEST's `public` schema from `supabase/schema/baseline.sql` at the sha in `BASELINE.md`, seed the ledger CLI-shaped, per CONTEXT's SUPERSEDING DECISION 2026-09-08), that it is DISPATCH-ONLY and founder-run, the required configuration (`Test` environment — exists, no protection rules by founder decision; repo secret `TEST_SUPABASE_DB_URL`), the public-repo log rule, and the reversibility story (the backup artifact, 90-day retention, how to restore FROM it: `psql -f` the schema file after a manual drop, `\copy` the ledger CSV back — written as prose, the reversal is a founder act).

**Triggers:** `on: workflow_dispatch:` ONLY, with typed inputs: `mode` (`type: choice`, options `preflight`, `restore`, default `preflight` — preflight runs the WHOLE transaction and rolls it back, so it is the measurement the founder decides on) and `confirm` (`type: string`, default empty, description: "restore mode only — type the first 8 hex chars of the baseline sha recorded in supabase/schema/BASELINE.md; a preflight ignores it"). `concurrency: { group: test-restore-from-baseline, cancel-in-progress: false }`. `permissions: { contents: read }`.

**Job `dispatch-guard`** (runs when `github.ref != 'refs/heads/main'`): prints WHY nothing ran and the correct dispatch (`gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight`), exits 1 in ALL cases (a restore workflow dispatched off main is a fault, not a no-op — differs from mutex-probe's green guard, say why in a comment: a wrong-ref dispatch here could restore from an unreviewed branch's dump).

**Job `restore`** (`if: github.ref == 'refs/heads/main'`, `runs-on: ubuntu-latest`, `environment: Test`, `timeout-minutes: 90` with the runbook derivation comment) — steps, in order, each a separate named step with a verbatim `run:` (no wrappers):
  1. checkout (pinned SHA, `persist-credentials: false`, `fetch-depth: 0` — the freshness check needs history).
  2. `Assert TEST credential is configured` — NO `if:`; `TEST_SUPABASE_DB_URL` empty ⇒ `::error::` naming it + `HARD FAILURE, not a skip` + exit 1.
  3. `Refuse restore without the confirm token` — computes the expected token from `supabase/schema/BASELINE.md`'s sha row (first 8 hex chars); if `inputs.mode == 'restore'` and `inputs.confirm` ≠ token ⇒ `::error::` printing the EXPECTED token (it is a public sha prefix, not a secret) and exit 1; in preflight print `confirm not required for preflight`. Inputs flow through `env:`, never inline `${{ }}` in `run:` (the repo's script-injection rule, `supabase-migrate.yml:50-54`).
  4. setup-node (pinned, 22) and `sudo apt-get install -y --no-install-recommends postgresql-client`.
  5. `Baseline co-edit gate` — `node scripts/check-baseline-staleness.mjs --self-test` then `node scripts/check-baseline-staleness.mjs` (two verbatim steps, self-test first).
  6. `Restore script self-test` — `bash scripts/restore-test-from-baseline.sh --self-test` (the pg-lane needs server binaries + pg_cron on ubuntu: copy the `Provision pg_cron` / postgresql server install steps ci.yml uses for `sql-mutation` — cite the step name you copied).
  7. `Acquire shared-test-db mutex` — the ci.yml:2522 step body with the fork-PR early-exit branch replaced by a fail-loud `::error::` + exit 1 (comment: this workflow never runs on a fork PR, an absent credential on a dispatch is a fault); everything from the psql-install line to the end byte-identical to ci.yml (Task 2 pins that suffix).
  8. `Which database am I on` — the CLAUDE.md marker query against the session DSN; refuse on NULL / non-TEST / PROD-naming, print only the OK sentence. (The script repeats this check; the step exists so the log shows the refusal BEFORE the backup runs, and the backup never touches PROD.)
  9. `Back up TEST before any write` — into `$RUNNER_TEMP/test-backup/`: (a) ledger CSV via `psql … -c "\copy (SELECT version, name, statements FROM supabase_migrations.schema_migrations ORDER BY version) TO 'ledger.csv' CSV HEADER"`; (b) schema via the pinned CLI: `supabase db dump --db-url "$dsn" -f schema-before.sql` (the same tool that made `baseline.sql`; runs pg_dump in docker on the ubuntu runner — if it fails, that is a red, not a skip); (c) the survivors census the script writes (`RESTORE_OUT_DIR` pointed here); (d) `pre-census.txt`. Print the row count and byte sizes only. Then `actions/upload-artifact` (pinned) `name: test-restore-backup-${{ github.run_id }}`, `path: $RUNNER_TEMP/test-backup`, `retention-days: 90`, `if-no-files-found: error`, `if: always()` so an aborted script still leaves the backup.
  10. `Run the restore script` — `bash scripts/restore-test-from-baseline.sh --run --mode "$MODE"` with `RESTORE_DB_URL` = the session DSN derived exactly as the mutex step derives it, `RESTORE_OUT_DIR=$RUNNER_TEMP/test-backup`.
  11. `Post-verify with the Supabase CLI` (restore mode only, `if: inputs.mode == 'restore'` on the STEP — this is a mode select, not a credential skip): setup-cli pinned `2.98.2`, then `supabase db push --include-all --dry-run --db-url "$dsn" | tee "$RUNNER_TEMP/dry-run.txt"` and assert the output contains no `Would push` / pending-migration line (grep the CLI's own wording; if the wording is unknown assert instead that `supabase migration list --db-url "$dsn"` shows every local version in the Remote column — implement the assertion as "count of local files == count of rows showing both Local and Remote", printed) — a pending set or any `Reverted` row is `::error::` + exit 1. This is the measurable end state: TEST's ledger is CLI-consistent with the repo, which is what `supabase-migrate.yml`'s PROD `plan` job already proves for PROD (last three runs 2026-09-06/07/08 all success, measured this session).
  12. `Release shared-test-db mutex (best effort)` — ci.yml:3269 verbatim, `if: always()`.
Step 11's `--db-url` is the FIRST time this repo hands the pooler DSN to the CLI ("must be percent-encoded" per `supabase db push --help`): if the CLI rejects the DSN while psql accepts it, the fix is to percent-encode the password INSIDE the secret (a settings act), never to transform it in YAML — write that sentence in the step comment so the first run's reader does not improvise.
  </action>
  <verify>
    <automated>node -e 'const y=require("fs").readFileSync(".github/workflows/test-restore-from-baseline.yml","utf8");const on=y.slice(y.indexOf("\non:"),y.indexOf("\njobs:"));if(/\n\s+(push|schedule|pull_request)\s*:/.test(on))process.exit(1);if(!/environment: Test\n/.test(y))process.exit(2);if(!/timeout-minutes: 90\n/.test(y))process.exit(3);console.log("trigger+env+ttl ok")' && npx vitest run src/__tests__/critical-regressions.test.ts -t "CI hardening"</automated>
  </verify>
  <done>The workflow file exists with dispatch-only triggers, typed inputs, the guard job, and the twelve ordered steps; `critical-regressions.test.ts`'s workflow-wide invariants (SHA pins, permissions, persist-credentials, concurrency) pass with the new file present.</done>
</task>

<task type="auto">
  <name>Task 2: Line-exact wiring pins with calibration twins</name>
  <files>src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <read_first>
    - src/__tests__/local-stack-lane-wiring.test.ts:50-83 (block slicing by `\n  <job>:\n`), :124-140 (LINE-EXACT non-comment pin), :153-158 (filter `!/^\s*#/` before asserting a `run:` literal is executed).
    - src/__tests__/prod-prober-wiring.test.ts:53-55, :62-67 (`bareRunLines`), :93-96 (`SOFTENING_TOKENS`), :99-109 (credential step has NO `if:`), :13-17 (calibration).
    - src/__tests__/critical-regressions.test.ts:1290-1312 — the byte-identity pin across the three ci.yml acquire steps (the regex that extracts a step body by its `- name:` line and the next step/comment at step indent).
  </read_first>
  <action>
Write `src/__tests__/test-restore-workflow-wiring.test.ts` reading both YAML files via `node:fs`. Slice the `restore:` job block. Pins, each with a calibration twin (mutate an in-memory copy, assert it differs, assert the predicate flips):
  - the top-level `on:` block contains ONLY `workflow_dispatch` (any of `push`, `schedule`, `pull_request`, `pull_request_target` as a trigger key flips it);
  - `environment: Test` is a live line inside the `restore:` block, exactly once;
  - `timeout-minutes: 90` is a live line inside the block;
  - the `mode` input is `type: choice` with options exactly `preflight` and `restore`, default `preflight` (a default of `restore` flips it);
  - the `Assert TEST credential is configured` step has NO `if:` between its `- name:` line and its `run:` line, and its body contains `exit 1` and an `::error::` naming `TEST_SUPABASE_DB_URL`;
  - the three verbatim commands are LIVE `run:` lines (non-comment): `node scripts/check-baseline-staleness.mjs`, `bash scripts/restore-test-from-baseline.sh --self-test`, `bash scripts/restore-test-from-baseline.sh --run --mode "$MODE"`, and the self-test line's index is lower than the run line's index (self-test first);
  - ordering: the `Back up TEST before any write` step's `- name:` index < the `Run the restore script` step's index < the `Post-verify` step's index; and the Acquire step's index < the backup step's index;
  - the backup upload step carries `retention-days: 90` and `if-no-files-found: error` and `if: always()`;
  - none of `SOFTENING_TOKENS` (import the list's VALUES by restating them — the prober test does not export them; say so) appears in the `restore:` block with comments masked, EXCEPT inside the Release step, which is `if: always()` by design (slice it out before the scan, as ci.yml's own pins do);
  - cross-file byte identity: extract the Acquire step body from ci.yml (`sql-tests` job) and from the new workflow with the critical-regressions regex, drop everything before the `if ! command -v psql` line in both, and assert the two suffixes are byte-identical; assert separately that the new workflow's prefix contains no zero-status early exit and does contain `exit 1`;
  - the guard job's `if:` is the negation of the restore job's `if:` on `refs/heads/main` (both literals present, line-exact).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/critical-regressions.test.ts</automated>
  </verify>
  <done>All pins green; each calibration twin recorded RED in the SUMMARY with the mutation used; the cross-file suffix identity holds so a future edit to ci.yml's mutex protocol reddens this file until the copy is re-synced.</done>
</task>

<task type="auto">
  <name>Task 3: Document the one-time configuration and the dispatch recipe; verify the workflow is registered</name>
  <files>.github/workflows/test-restore-from-baseline.yml</files>
  <action>
In the workflow header, write the dispatch recipe Plan 03 will run verbatim: (1) `gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight` and how to read its log (`pre-census`, `expected shape`, `filtered:`, `post-census == pre-census`, `mode=preflight`); (2) the founder's decision; (3) `gh workflow run test-restore-from-baseline.yml --ref main -f mode=restore -f confirm=27826b76` (state that the token is the sha prefix from BASELINE.md:54 at the time of writing and goes stale with any regeneration — the step computes it, the recipe just shows the shape); (4) how to read the restore log's single `restore:` summary line and the CLI post-verify. State explicitly what the restore DESTROYS: every row in every `public` table on TEST (e2e specs re-seed their own data per D-16 / `e2e/helpers/seed-test-project.ts`; pre-existing hand-created rows, and `public.profiles` rows for existing `auth.users`, are LOST — `auth.users` itself survives, and the re-created `on_auth_user_created` trigger covers only FUTURE signups). Note: a workflow_dispatch workflow is dispatchable only once its file is on the DEFAULT branch — Plan 03's first checkpoint is therefore "merge this PR". Run the repo's YAML-level checks (`npx vitest run src/__tests__/critical-regressions.test.ts`) once more and commit.
  </action>
  <verify>
    <automated>grep -c 'gh workflow run test-restore-from-baseline.yml --ref main -f mode=' .github/workflows/test-restore-from-baseline.yml | grep -qx 2 && npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts</automated>
  </verify>
  <done>Header carries both dispatch commands, the data-loss statement, and the merge-first note; wiring test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow_dispatch inputs → job env | founder-typed strings enter `run:` blocks (via `env:`, never inline) |
| GitHub `Test` environment → repo secret `TEST_SUPABASE_DB_URL` | the credential that writes to a database shared with other people's CI |
| CI artifact | TEST's pre-restore ledger and schema, downloadable by anyone with read access to a PUBLIC repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-07 | Elevation | accidental restore (wrong mode / wrong ref / wrong dump) | critical | mitigate | dispatch-only; guard job exits 1 off main; `confirm` must equal the BASELINE.md sha prefix; `mode` defaults to preflight; marker check before backup |
| T-164.8-08 | Denial of service | restore racing another CI job on shared TEST | high | mitigate | ci.yml's advisory-lock holder (key 61616158) held across backup → script → post-verify; fail-loud credential branch |
| T-164.8-09 | Repudiation | irreversible act | high | mitigate | backup artifact (ledger CSV, schema dump, survivors, pre-census) uploaded with `if: always()` BEFORE the script; 90-day retention; reversal recipe in the header |
| T-164.8-10 | Information disclosure | backup artifact on a public repo | medium | accept | schema and ledger are already public in `supabase/migrations/**` and `baseline.sql`; NO data dump of public tables is taken (stated in the header); the DSN is never written to the artifact |
| T-164.8-11 | Tampering | script-injection via inputs | medium | mitigate | inputs reach `run:` only through `env:`; `confirm` is compared, never executed |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | only apt `postgresql-client` and the pinned `supabase/setup-cli` SHA already used by three workflows |
</threat_model>

<verification>
- `npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/critical-regressions.test.ts` green.
- The workflow's `on:` block has exactly one trigger key.
- The Acquire step suffix is byte-identical to ci.yml's.
</verification>

<success_criteria>
1. Dispatch-only, ref-guarded, mode-defaulted-to-preflight, confirm-tokened destructive path — every one pinned line-exact with a calibration twin.
2. Backup precedes the script; the artifact survives an aborted run.
3. One mutex hold spans the whole act; credential absence is exit 1.
4. What would make this RED: a second trigger key; `environment: Test` commented out; the backup step moving below the script step; the fork-PR early-exit surviving in the credential branch.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-02-SUMMARY.md` when done.
</output>
