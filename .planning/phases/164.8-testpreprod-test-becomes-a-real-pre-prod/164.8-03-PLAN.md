---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 03
type: execute
wave: 3
depends_on:
  - 164.8-02
files_modified:
  - .github/workflows/test-restore-from-baseline.yml
  - src/__tests__/test-restore-workflow-wiring.test.ts
autonomous: true
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE
user_setup:
  - service: github
    why: "The `Test` GitHub Environment binds the restore job (it EXISTS since 2026-09-08 and carries ONE protection rule — a deployment branch policy allowing `main` ONLY (measured 2026-09-08: protection_rules=[branch_policy], branches=[main], required_reviewers=0, wait_timer=0, secrets=0, variables=0) — do NOT add reviewers to it, and do NOT set `protected_branches: true`: `main` carries NO branch protection on this repo (measured 404), so that setting would permit NOTHING; the founder's approval surface is the dispatch itself plus Plan 04's checkpoint:decision). TEST_SUPABASE_DB_URL is a REPO-level secret and is visible to environment-bound jobs."
    env_vars:
      - name: TEST_SUPABASE_DB_URL
        source: "already present as a repo secret (measured 2026-09-08 via gh api …/actions/secrets); the pooler DSN on :6543, rewritten to :5432 in-job"

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The workflow is dispatch-only: no push, schedule or pull_request trigger can ever run it."
    - "Its destructive mode cannot run by accident: it requires ref == main, `mode=restore`, and a `confirm` input equal to the first 8 hex chars of the baseline sha recorded in BASELINE.md; anything else runs the guard job and exits 1 in restore mode (a mis-dispatch is a judgeable red, never a silent green)."
    - "Before the script is called, TEST's current ledger, its public schema and the survivors census are uploaded as a CI artifact — SCHEMA AND LEDGER ARE REVERSIBLE; DATA IS NOT (every row in every public table on TEST is destroyed with no backup — W6), and the header, the checkpoint and the threat register all say so in those words."
    - "Before the seed can run, the workflow PROVES the premise the ledger seed rests on — that PROD's ledger equals the repo file set — by asserting the most recent `supabase-migrate.yml` run on `main` concluded `success` at a sha that is an ANCESTOR of the checked-out HEAD (W5); the assertion is executed under stubs in the wiring test, not merely grepped."
    - "The shared-TEST advisory mutex is acquired once and held across backup, script and post-verify; an absent credential is exit 1 naming the variable, never a skip."
    - "In restore mode the job ends by proving the ledger is CLI-consistent: `supabase db push --include-all --dry-run --db-url <session DSN>` reports nothing pending — which proves ledger SHAPE (the CLI parses it, no ErrMissingLocal, no pending version), NOT object presence; presence is proven by the script's in-transaction shape assertions and by the applied-ness probes on the next `sql-tests` run (W4)."
  artifacts:
    - ".github/workflows/test-restore-from-baseline.yml — jobs: dispatch-guard, restore (environment: Test, timeout-minutes: 90)"
    - "src/__tests__/test-restore-workflow-wiring.test.ts — line-exact pins over the job block, with calibration twins; the PROD-ancestry step executed under a stub gh"
  key_links:
    - "The Acquire mutex step is .github/workflows/ci.yml:2522 — `- name: Acquire shared-test-db mutex` (MEASURED at HEAD 6b100e74 and at origin/main 78cfaef4; 2523 is its env line) — and the Release step is .github/workflows/ci.yml:3269 — `- name: Release shared-test-db mutex (best effort)` — with ONE deliberate difference in the copy: the fork-PR early-exit branch is replaced by a fail-loud credential assert (this workflow never runs on a fork PR)"
    - "The backup step runs BEFORE the script step; the script step is the header's verbatim command; the post-verify step uses the same session-DSN derivation as the mutex step"
    - "scripts/check-baseline-staleness.mjs runs as its own step BEFORE the script (the co-edit gate, self-test first), so a sha drift is a named red in the workflow log, not only inside the script"
---

<objective>
Wire Plan 01's script into a founder-dispatched, dispatch-only GitHub workflow that backs TEST up,
holds the shared mutex for the whole act, runs `preflight` or `restore`, and proves the result with
the Supabase CLI. This plan still writes NO byte to TEST: it produces the workflow and its pins.
Plan 04 dispatches it.

Purpose: CONTEXT's non-negotiable safety list — marker check, whole-act mutex, reversible backup,
founder-executed at the moment of execution, sha-verified source — must be enforced by the
WORKFLOW, not by discipline. The repo's precedents: `supabase-migrate.yml` (environment-bound
apply, credential check, pinned CLI 2.98.2, post-apply `migration list` check), `mutex-probe.yml`
`dispatch-guard` (a guard JOB that explains itself), `prod-prober.yml` typed `workflow_dispatch`
inputs, and `migration-drift-check.yml`'s unconditional credential assert.

Revision 2026-09-08 (plan-checker REVISE): W5 — the ledger-seed premise was prose in a
precondition; it is now a workflow step that is executed under stubs. W6 — "reversible" is
narrowed to "schema and ledger are reversible; data is not", everywhere it is said. W9 — the
self-test step installs server binaries only; Plan 01's fixtures use no pg_cron. W4 — the CLI
post-verify is described as what it proves.

Output: `.github/workflows/test-restore-from-baseline.yml` + its wiring test.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.github/workflows/supabase-migrate.yml
@.github/workflows/mutex-probe.yml
@docs/runbooks/shared-test-db-mutex.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: `test-restore-from-baseline.yml` — guard job + restore job, one path through every step</name>
  <files>.github/workflows/test-restore-from-baseline.yml</files>
  <read_first>
    - Read Plan 01's and Plan 02's SUMMARYs (`164.8-01-SUMMARY.md`, `164.8-02-SUMMARY.md`) for the script's seams (`RESTORE_DB_URL`, `RESTORE_OUT_DIR`) and the self-test closing line the workflow step must reproduce.
    - .github/workflows/ci.yml:2522-2727 — Acquire shared-test-db mutex: the fork-PR branch at lines 2527-2530 that MUST be replaced, the port rewrite at 2543, keepalives, the holder at 2604-2612, the 3600 s cap loop, the two-cause error at 2726.
    - .github/workflows/ci.yml:3269-3330 — Release, if always(), never non-zero.
    - .github/workflows/supabase-migrate.yml:107-128 — environment pin and persist-credentials false.
    - .github/workflows/supabase-migrate.yml:167-171 — setup-cli pinned SHA and version 2.98.2.
    - .github/workflows/supabase-migrate.yml:129-166 — the OLDER split credential check; do NOT copy its tolerant branch.
    - .github/workflows/supabase-migrate.yml:40-49 — the one-time-setup header block to mirror.
    - .github/workflows/migration-drift-check.yml:102-121 — the NEWER unconditional credential assert (no if on the step; a hard failure, not a skip).
    - .github/workflows/mutex-probe.yml:90-110 — dispatch-guard and the real job's inverse if.
    - .github/workflows/prod-prober.yml:66-78 — typed choice/string inputs and the sentence that a narrowed run can never read as a pass.
    - .github/workflows/ci.yml:300 — upload-artifact pinned SHA (v7.0.1); :68-70 setup-node pinned (node 22); :613 the postgresql-client install line.
    - .github/workflows/ci.yml:1377-1400 — the step that proves the runner image's PostgreSQL SERVER binaries resolve; copy THAT (W9), not the pg_cron provisioning step at 1272, because Plan 01's fixtures create no cron job and the lane there needs no pg_cron.
    - src/__tests__/critical-regressions.test.ts:540-660 — every workflow must SHA-pin every uses, declare minimum permissions, set persist-credentials false, and declare a concurrency group; the new file is auto-covered by those globs, so conform up front.
    - docs/runbooks/shared-test-db-mutex.md:75-110 — the three coupled numbers (acquire cap 3600 s, job TTL 90 min, holder sleep 6000 s); copy them, do not re-derive.
  </read_first>
  <action>
Create `.github/workflows/test-restore-from-baseline.yml`:

**Header comment** (mirrors the shape of supabase-migrate.yml's header): what it does (rebuild shared TEST's `public` schema from `supabase/schema/baseline.sql` at the sha in `BASELINE.md`, seed the ledger CLI-shaped, per CONTEXT's SUPERSEDING DECISION 2026-09-08), that it is DISPATCH-ONLY and founder-run, the required configuration (`Test` environment — exists, no protection rules by founder decision; repo secret `TEST_SUPABASE_DB_URL`), the public-repo log rule, and the reversibility story IN THESE WORDS (W6): "SCHEMA AND LEDGER ARE REVERSIBLE from the backup artifact (90-day retention; `psql -f` the schema file after a manual drop, `\copy` the ledger CSV back — a founder act, written as prose). DATA IS NOT: every row in every `public` table on TEST is destroyed and no data dump is taken (a data dump of TEST to a public-repo artifact would be an information-disclosure finding of its own; e2e specs re-seed their own rows). CONTEXT safety rule 3 names ledger and schema; this is that rule, no wider."

**Triggers:** `on: workflow_dispatch:` ONLY, with typed inputs: `mode` (`type: choice`, options `preflight`, `restore`, default `preflight` — preflight runs the WHOLE transaction and rolls it back, so it is the measurement the founder decides on) and `confirm` (`type: string`, default empty, description: "restore mode only — type the first 8 hex chars of the baseline sha recorded in supabase/schema/BASELINE.md; a preflight ignores it"). `concurrency: { group: test-restore-from-baseline, cancel-in-progress: false }`. `permissions: { contents: read, actions: read }` (`actions: read` is for the W5 step below — say so in a comment; it is the minimum that lets `gh run list` read this repo's runs).

**Job `dispatch-guard`** (runs when `github.ref != 'refs/heads/main'`): prints WHY nothing ran and the correct dispatch (`gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight`), exits 1 in ALL cases (a restore workflow dispatched off main is a fault, not a no-op — differs from mutex-probe's green guard, say why in a comment: a wrong-ref dispatch here could restore from an unreviewed branch's dump).

**Job `restore`** (`if: github.ref == 'refs/heads/main'`, `runs-on: ubuntu-latest`, `environment: Test`, `timeout-minutes: 90` with the runbook derivation comment) — steps, in order, each a separate named step with a verbatim `run:` (no wrappers):
  1. checkout (pinned SHA, `persist-credentials: false`, `fetch-depth: 0` — the freshness check and the ancestry check need history).
  2. `Assert TEST credential is configured` — NO `if:`; `TEST_SUPABASE_DB_URL` empty ⇒ `::error::` naming it + `HARD FAILURE, not a skip` + exit 1.
  3. `Refuse restore without the confirm token` — computes the expected token from `supabase/schema/BASELINE.md`'s sha row (first 8 hex chars); if `inputs.mode == 'restore'` and `inputs.confirm` ≠ token ⇒ `::error::` printing the EXPECTED token (it is a public sha prefix, not a secret) and exit 1; in preflight print `confirm not required for preflight`. Inputs flow through `env:`, never inline `${{ }}` in `run:` (the repo's script-injection rule, stated in supabase-migrate.yml's header).
  4. `Assert PROD's ledger equals the repo file set` (W5 — the premise the seed rests on, ENFORCED): with `GH_TOKEN: ${{ github.token }}` in `env:`, `gh run list --workflow supabase-migrate.yml --branch main -L 1 --json status,conclusion,headSha` → require `status == completed`, `conclusion == success`, and `git merge-base --is-ancestor "$headSha" HEAD` (exit 0); any other reading ⇒ `::error::` naming which of the three failed and the sha, exit 1. Comment: the seed writes one ledger row per repo file CLAIMING parity with PROD; PROD's own green `plan`+`apply` at an ancestor sha is the only evidence that claim rests on, and the freshness check (script refusal 3) covers the window between that sha and HEAD. The three readings flow through `env:` (from a prior `id:` step's outputs), never inline. Placed BEFORE the mutex so a stale premise never holds the lock.
  5. setup-node (pinned, 22) and `sudo apt-get install -y --no-install-recommends postgresql-client`.
  6. `Baseline co-edit gate` — `node scripts/check-baseline-staleness.mjs --self-test` then `node scripts/check-baseline-staleness.mjs` (two verbatim steps, self-test first).
  7. `Restore script self-test` — `bash scripts/restore-test-from-baseline.sh --self-test`, preceded by the SERVER-binaries step copied from ci.yml (the probe/install step at 1377, cite its name in a comment). W9: do NOT copy ci.yml's pg_cron provisioning step (line 1272) — Plan 01's fixtures create no cron job, and Task 2 negative-pins that step's name, so do not mention it in a YAML comment either; the comment may say only that a future fixture needing pg_cron would provision it the way `sql-mutation` does.
  8. `Acquire shared-test-db mutex` — the Acquire step body from .github/workflows/ci.yml (line 2522 onward) with the fork-PR early-exit branch replaced by a fail-loud `::error::` + exit 1 (comment: this workflow never runs on a fork PR, an absent credential on a dispatch is a fault); everything from the psql-install line to the end byte-identical to ci.yml (Task 2 pins that suffix).
  9. `Which database am I on` — the CLAUDE.md marker query against the session DSN; refuse on NULL / non-TEST / PROD-naming, print only the OK sentence. (The script repeats this check; the step exists so the log shows the refusal BEFORE the backup runs, and the backup never touches PROD.)
  10. `Back up TEST before any write (schema + ledger; NOT data)` — into `$RUNNER_TEMP/test-backup/`: (a) ledger CSV via `psql … -c "\copy (SELECT version, name, statements FROM supabase_migrations.schema_migrations ORDER BY version) TO 'ledger.csv' CSV HEADER"`; (b) schema via the pinned CLI: `supabase db dump --db-url "$dsn" -f schema-before.sql` (the same tool that made `baseline.sql`; runs pg_dump in docker on the ubuntu runner — if it fails, that is a red, not a skip); (c) the survivors census the script writes (`RESTORE_OUT_DIR` pointed here); (d) `pre-census.txt`. Print the row count and byte sizes only. Then `actions/upload-artifact` (pinned) `name: test-restore-backup-${{ github.run_id }}`, `path: $RUNNER_TEMP/test-backup`, `retention-days: 90`, `if-no-files-found: error`, `if: always()` so an aborted script still leaves the backup. The step name says what is NOT in it.
  11. `Run the restore script` — `bash scripts/restore-test-from-baseline.sh --run --mode "$MODE"` with `RESTORE_DB_URL` = the session DSN derived exactly as the mutex step derives it, `RESTORE_OUT_DIR=$RUNNER_TEMP/test-backup`.
  12. `Post-verify with the Supabase CLI — ledger SHAPE` (restore mode only, `if: inputs.mode == 'restore'` on the STEP — this is a mode select, not a credential skip): setup-cli pinned `2.98.2`, then `supabase db push --include-all --dry-run --db-url "$dsn" | tee "$RUNNER_TEMP/dry-run.txt"` and assert the output contains no `Would push` / pending-migration line (grep the CLI's own wording; if the wording is unknown assert instead that `supabase migration list --db-url "$dsn"` shows every local version in the Remote column — implement the assertion as "count of local files == count of rows showing both Local and Remote", printed) — a pending set or any `Reverted` row is `::error::` + exit 1. W4, in the step comment: this proves the ledger's SHAPE — the CLI can parse it (no `ErrMissingLocal`) and finds nothing pending — which is the precondition Plan 05's `apply-test` needs. It does NOT prove any object exists: the seed writes one row per repo file, so nothing CAN be pending here. Object presence is proven by the script's in-transaction shape assertions (counts vs the dump text) and, for every gate that cares, by the applied-ness probes in `supabase/tests/*.sql` on the next `sql-tests` run (Plan 06 records that SHA-bound).
  13. `Release shared-test-db mutex (best effort)` — the Release step from .github/workflows/ci.yml (line 3269 onward) verbatim, including its `if: always()`.
Step 12's `--db-url` is the FIRST time this repo hands the pooler DSN to the CLI ("must be percent-encoded" per `supabase db push --help`): if the CLI rejects the DSN while psql accepts it, the fix is to percent-encode the password INSIDE the secret (a settings act), never to transform it in YAML — write that sentence in the step comment so the first run's reader does not improvise.
  </action>
  <verify>
    <automated>node -e 'const y=require("fs").readFileSync(".github/workflows/test-restore-from-baseline.yml","utf8");const on=y.slice(y.indexOf("\non:"),y.indexOf("\njobs:"));if(/\n\s+(push|schedule|pull_request)\s*:/.test(on))process.exit(1);if(!/environment: Test\n/.test(y))process.exit(2);if(!/timeout-minutes: 90\n/.test(y))process.exit(3);if(!/merge-base --is-ancestor/.test(y))process.exit(4);const ci=require("fs").readFileSync(".github/workflows/ci.yml","utf8").split("\n");const cron=(ci[1271]||"").replace(/^\s*- name:\s*/,"").trim();if(!cron||y.includes(cron))process.exit(5);console.log("trigger+env+ttl+ancestry ok")' && npx vitest run src/__tests__/critical-regressions.test.ts -t "CI hardening"</automated>
  </verify>
  <done>The workflow file exists with dispatch-only triggers, typed inputs, the guard job, and the thirteen ordered steps including the PROD-ancestry assert; no pg_cron provisioning; `critical-regressions.test.ts`'s workflow-wide invariants (SHA pins, permissions, persist-credentials, concurrency) pass with the new file present.</done>
</task>

<task type="auto">
  <name>Task 2: Line-exact wiring pins with calibration twins; the ancestry step EXECUTED under a stub gh</name>
  <files>src/__tests__/test-restore-workflow-wiring.test.ts</files>
  <read_first>
    - src/__tests__/local-stack-lane-wiring.test.ts:50-83 — block slicing of a job by its name line.
    - src/__tests__/local-stack-lane-wiring.test.ts:124-140 — the LINE-EXACT non-comment pin.
    - src/__tests__/local-stack-lane-wiring.test.ts:153-158 — filter comment lines before asserting a run literal is executed.
    - src/__tests__/prod-prober-wiring.test.ts:53-67 — the bare run-line extraction helper.
    - src/__tests__/prod-prober-wiring.test.ts:93-96 — the softening-token list.
    - src/__tests__/prod-prober-wiring.test.ts:99-109 — the credential step has NO if.
    - src/__tests__/prod-prober-wiring.test.ts:13-17 — calibration.
    - src/__tests__/critical-regressions.test.ts:1290-1312 — the byte-identity pin across the three ci.yml acquire steps (the regex that extracts a step body by its name line and the next step/comment at step indent).
    - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts:27-36 — extract a step's shell script and RUN it with a stub on PATH, asserting on exit codes.
  </read_first>
  <action>
Write `src/__tests__/test-restore-workflow-wiring.test.ts` reading both YAML files via `node:fs`. Slice the `restore:` job block. Pins, each with a calibration twin (mutate an in-memory copy, assert it differs, assert the predicate flips):
  - the top-level `on:` block contains ONLY `workflow_dispatch` (any of `push`, `schedule`, `pull_request`, `pull_request_target` as a trigger key flips it);
  - `environment: Test` is a live line inside the `restore:` block, exactly once;
  - `timeout-minutes: 90` is a live line inside the block;
  - the `mode` input is `type: choice` with options exactly `preflight` and `restore`, default `preflight` (a default of `restore` flips it);
  - the `Assert TEST credential is configured` step has NO `if:` between its `- name:` line and its `run:` line, and its body contains `exit 1` and an `::error::` naming `TEST_SUPABASE_DB_URL`;
  - W5, EXECUTED not grepped: extract the `Assert PROD's ledger equals the repo file set` step's script (the anti-skip-gate idiom) and run it in a temp git repo with a stub `gh` on PATH and the three readings injected through the env the step reads: (success, completed, ancestor sha) ⇒ exit 0; (failure, completed, ancestor) ⇒ exit 1 naming `conclusion`; (success, completed, a sha that is NOT an ancestor — a commit on a side branch of the temp repo) ⇒ exit 1 naming `ancestor`; also pin that the step's `- name:` index is LOWER than the Acquire step's index (a stale premise never holds the lock);
  - the four verbatim commands are LIVE `run:` lines (non-comment): `node scripts/check-baseline-staleness.mjs`, `bash scripts/restore-test-from-baseline.sh --self-test`, `bash scripts/restore-test-from-baseline.sh --run --mode "$MODE"`, and the self-test line's index is lower than the run line's index (self-test first);
  - W9: the name of ci.yml's pg_cron provisioning step (line 1272 — read it from ci.yml in the test rather than restating it here) does NOT appear in the workflow (a copy with that step pasted in flips it) and the server-binaries step's name (as copied from ci.yml) DOES appear before the self-test step;
  - ordering: the `Back up TEST before any write` step's `- name:` index < the `Run the restore script` step's index < the `Post-verify` step's index; and the Acquire step's index < the backup step's index;
  - the backup upload step carries `retention-days: 90` and `if-no-files-found: error` and `if: always()`; the backup step's name contains `NOT data` (W6 — the step says what it does not hold);
  - none of the softening tokens (restate the prober test's VALUES — it does not export them; say so) appears in the `restore:` block with comments masked, EXCEPT inside the Release step, which is `if: always()` by design (slice it out before the scan, as ci.yml's own pins do);
  - cross-file byte identity: extract the Acquire step body from ci.yml (`sql-tests` job) and from the new workflow with the critical-regressions regex, drop everything before the `if ! command -v psql` line in both, and assert the two suffixes are byte-identical; assert separately that the new workflow's prefix contains no zero-status early exit and does contain `exit 1`;
  - the guard job's `if:` is the negation of the restore job's `if:` on `refs/heads/main` (both literals present, line-exact).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/critical-regressions.test.ts</automated>
  </verify>
  <done>All pins green; each calibration twin recorded RED in the SUMMARY with the mutation used; the ancestry step observed exit 0/1/1 under the three stub readings; the cross-file suffix identity holds so a future edit to ci.yml's mutex protocol reddens this file until the copy is re-synced.</done>
</task>

<task type="auto">
  <name>Task 3: Document the one-time configuration and the dispatch recipe; verify the workflow is registered</name>
  <files>.github/workflows/test-restore-from-baseline.yml</files>
  <action>
In the workflow header, write the dispatch recipe Plan 04 will run verbatim: (1) `gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight` and how to read its log (`pre-census`, `expected shape`, `filtered:`, `post-census == pre-census`, `mode=preflight`, and — new — the survivors list with every `pub:public.<table>` key, because a publication row for a table absent from the dump is a pre-write REFUSAL the preflight will name (W3: the remedy is a founder-run `ALTER PUBLICATION supabase_realtime DROP TABLE public.<t>` recorded in the RESTORE log, then re-preflight)); (2) the founder's decision; (3) `gh workflow run test-restore-from-baseline.yml --ref main -f mode=restore -f confirm=27826b76` (state that the token is the sha prefix from BASELINE.md's sha row at the time of writing and goes stale with any regeneration — the step computes it, the recipe just shows the shape); (4) how to read the restore log's single `restore:` summary line and the CLI post-verify (and what the latter proves — shape, not presence). State explicitly what the restore DESTROYS and that it is NOT reversible: every row in every `public` table on TEST (e2e specs re-seed their own data per D-16 / `e2e/helpers/seed-test-project.ts`; pre-existing hand-created rows, and `public.profiles` rows for existing `auth.users`, are LOST — `auth.users` itself survives, and the re-created `on_auth_user_created` trigger covers only FUTURE signups). Note: a workflow_dispatch workflow is dispatchable only once its file is on the DEFAULT branch — Plan 04's first checkpoint is therefore "merge this PR". Run the repo's YAML-level checks (`npx vitest run src/__tests__/critical-regressions.test.ts`) once more and commit.
  </action>
  <verify>
    <automated>grep -c 'gh workflow run test-restore-from-baseline.yml --ref main -f mode=' .github/workflows/test-restore-from-baseline.yml | grep -qx 2 && grep -c 'DATA IS NOT' .github/workflows/test-restore-from-baseline.yml | grep -qv '^0$' && npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts</automated>
  </verify>
  <done>Header carries both dispatch commands, the data-loss / not-reversible statement, the publication-row hazard and its remedy, and the merge-first note; wiring test still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow_dispatch inputs → job env | founder-typed strings enter `run:` blocks (via `env:`, never inline) |
| GitHub `Test` environment → repo secret `TEST_SUPABASE_DB_URL` | the credential that writes to a database shared with other people's CI |
| CI artifact | TEST's pre-restore ledger and schema, downloadable by anyone with read access to a PUBLIC repo |
| GitHub Actions API → ancestry assert | a read of this repo's own run history with the job's default token |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-07 | Elevation | accidental restore (wrong mode / wrong ref / wrong dump) | critical | mitigate | dispatch-only; guard job exits 1 off main; `confirm` must equal the BASELINE.md sha prefix; `mode` defaults to preflight; marker check before backup |
| T-164.8-08 | Denial of service | restore racing another CI job on shared TEST | high | mitigate | ci.yml's advisory-lock holder (key 61616158) held across backup → script → post-verify; fail-loud credential branch |
| T-164.8-09 | Repudiation | irreversible act | high | mitigate | SCHEMA AND LEDGER: backup artifact (ledger CSV, schema dump, survivors, pre-census) uploaded with `if: always()` BEFORE the script; 90-day retention; reversal recipe in the header. DATA: NOT reversible, stated in the header, the step name, the checkpoint (Plan 04) and TODOS `[164.8-TEST-DATA-RESEEDED]` (Plan 06) |
| T-164.8-10 | Information disclosure | backup artifact on a public repo | medium | accept | schema and ledger are already public in `supabase/migrations/**` and `baseline.sql`; NO data dump of public tables is taken (stated in the header — a data dump would be the disclosure); the DSN is never written to the artifact |
| T-164.8-11 | Tampering | script-injection via inputs | medium | mitigate | inputs reach `run:` only through `env:`; `confirm` is compared, never executed |
| T-164.8-25 | Tampering | seeding a ledger that claims parity PROD does not have | high | mitigate | W5 step: newest `supabase-migrate.yml` run on main must be `success` at an ancestor of HEAD, executed under stubs in the wiring test; the script's freshness refusal covers the window after that sha |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | only apt `postgresql-client` / server binaries and the pinned `supabase/setup-cli` SHA already used by three workflows |
</threat_model>

<verification>
- `npx vitest run src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/critical-regressions.test.ts` green.
- The workflow's `on:` block has exactly one trigger key.
- The Acquire step suffix is byte-identical to ci.yml's.
- The ancestry step exits 0/1/1 under the three stub readings.
</verification>

<success_criteria>
1. Dispatch-only, ref-guarded, mode-defaulted-to-preflight, confirm-tokened destructive path — every one pinned line-exact with a calibration twin.
2. Backup (schema + ledger) precedes the script; the artifact survives an aborted run; data loss is stated as NOT reversible wherever reversibility is mentioned.
3. One mutex hold spans the whole act; credential absence is exit 1; the PROD-ledger premise is asserted before the lock is taken.
4. What would make this RED: a second trigger key; `environment: Test` commented out; the backup step moving below the script step; the fork-PR early-exit surviving in the credential branch; the ancestry step exiting 0 on a non-ancestor sha; a `Provision pg_cron` step.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-03-SUMMARY.md` when done.
</output>