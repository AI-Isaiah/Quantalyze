---
phase: 164.8-testpreprod-test-becomes-a-real-pre-prod
plan: 03
type: execute
wave: 3
depends_on:
  - 164.8-02
files_modified:
  - scripts/vac08-ledger-baseline.txt
  - src/__tests__/vac08-ledger-dispositions.test.ts
  - .planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-03-RESTORE.log
autonomous: false
requirements:
  - CI-MIGRATE-01
  - 164.2-TEST-APPLY-PROVENANCE
  - 164.5-TEST-VAC08-DROP-UNAPPLIABLE

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The founder decided AT THE MOMENT OF EXECUTION, on the preflight's measured readings, and the restore ran only after that decision."
    - "After the restore, TEST's `public` schema matches the dump's shape (tables/policies/functions counts equal the dump-derived numbers), its ledger holds exactly one CLI-shaped row per repo migration file, and `supabase db push --dry-run --db-url` reports nothing pending."
    - "`scripts/vac08-ledger-baseline.txt` is MEASURED empty, beside an AIM that proves the classifier still classifies, and `ENTRY_COUNT` moved to 0 in the same commit — never a bare empty file."
    - "Every reading in the SUMMARY is SHA-bound and run-id-bound (workflow run ids for preflight and restore, head sha, the verbatim `restore:` summary line)."
  artifacts:
    - "164.8-03-RESTORE.log — the pasted preflight and restore logs (names and counts only), both run ids"
    - "scripts/vac08-ledger-baseline.txt — header only, dated, naming the restore run id; zero entries"
    - "src/__tests__/vac08-ledger-dispositions.test.ts — ENTRY_COUNT = 0 with the empty set asserted only after the AIM"
  key_links:
    - "VAC-08's ratchet (`scripts/test-ledger-drift-check.sh:365,376-385`) turns every one of the 31 entries STALE the moment the ledger is seeded — the emptying is the file's own header rule applied, and it lands in the SAME PR as this plan so main's `sql-tests` is red only between the restore and this PR's merge"
    - "The `[SEC]` arm (`vac08-ledger-dispositions.test.ts:158`) loses its subjects — it is rewritten as an AIM over the synthetic corpus, not deleted"
---

<objective>
Execute the one-way door: preflight, founder decision, restore, measurement — then apply the
ratchet file's own rule to the 31 entries that the restore makes present.

Purpose: this is the act CONTEXT's SUPERSEDING DECISION ordered and every earlier plan built
toward. It is destructive on a database shared with other people's CI, so it runs behind a
`checkpoint:decision` and every number it produces is recorded with its run id. The pragma of
Area 3 is settled here by measurement: once TEST's ledger holds `20260908120000` (seeded from
the repo file set that PROD's green `plan` job proves PROD holds), VAC-08's red for that
migration is gone with no pragma at all — Plan 05 records the verdict (DEAD SCOPE) with this
plan's readings as its evidence.

Output: a restored TEST, an evidence log, an emptied-beside-an-AIM baseline.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-CONTEXT.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-01-SUMMARY.md
@.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-02-SUMMARY.md
@scripts/vac08-ledger-baseline.txt
@src/__tests__/vac08-ledger-dispositions.test.ts
@src/__tests__/mutation-annotation-parser.test.ts
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Merge the Wave 1 PR, then dispatch PREFLIGHT and read it</name>
  <precondition>Plans 01 and 02 are merged to `main` (a workflow_dispatch workflow is dispatchable only from the default branch); the latest `supabase-migrate.yml` run on main is `success` for both `plan` and `apply` (measured 2026-09-08: 07:36Z push run at `7146c574`), which is the proof that PROD's ledger equals the repo file set the seed will write.</precondition>
  <action>Merge the PR carrying `164.8-01` + `164.8-02` to `main` (a workflow_dispatch workflow is only dispatchable once its file is on the default branch). Everything after the merge — the preflight dispatch, the log read, the evidence file — the executor does itself, because a preflight commits nothing.</action>
  <instructions>
    Plans 01 and 02 built and pinned the restore script and its dispatch-only workflow; nothing has touched TEST.
    The ONE thing requiring you: merge that PR (via `/gsd-ship`, reviewers before the pr-branch filter, per CLAUDE.md), then say "merged".
    On resume the executor will: confirm `gh workflow list` shows `test-restore-from-baseline`; run `gh workflow run test-restore-from-baseline.yml --ref main -f mode=preflight`; read the job log via `gh api repos/:owner/:repo/actions/jobs/<job-id>/logs` (never `gh run view --log`, which truncates); and write `164.8-03-RESTORE.log` (names and counts only) with the `which_database:` line, the `pre-census` block (tables / functions / policies / ledger rows / non-14-digit versions / objects-not-owned-by-postgres / survivor keys), the `expected shape` block, `filtered: N`, the `post-census == pre-census` verdict, `mode=preflight`, the backup artifact name and sizes, the run id and head sha.
    If the preflight is RED the ROLLBACK already happened: the psql error line names the class (`must be owner` on a public object, a missing extension, a survivor that cannot be re-created — hosted-only differences Plan 01's fixture cannot see). The fix goes into Plan 01's script WITH a new self-test arm and a raised `EXPECTED_ARMS`, then re-merge and re-preflight. Task 2 is never reached on a red preflight.
  </instructions>
  <verification>test -s .planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-03-RESTORE.log and the file contains `mode=preflight` and `post-census == pre-census`, and `gh run list --workflow test-restore-from-baseline.yml -L1` shows `success` on `main`.</verification>
  <resume-signal>Type "merged" once the Wave 1 PR is on main; the executor runs the preflight and returns with the readings.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: DECISION — restore TEST from the PROD dump now?</name>
  <precondition>Task 1's preflight was green on `main` and its readings are in `164.8-03-RESTORE.log`; the backup artifact `test-restore-backup-<run_id>` from that run is downloadable.</precondition>
  <decision>Rebuild shared TEST's `public` schema from `supabase/schema/baseline.sql` (sha `27826b76…`, BASELINE.md:54) and seed its ledger with one CLI-shaped row per repo migration file — a ONE-WAY act on a database shared with other people's CI.</decision>
  <context>
Present, from the preflight log: the survivors that will be dropped and re-created by name (expected: `auth.users:on_auth_user_created`, `storage.objects:gdpr_exports_admin_read`, `storage.objects:portfolio_docs_*` only if they reference public functions, and any `supabase_realtime` rows); the count of public objects NOT owned by `postgres` (must be 0 for the drop to succeed — the preflight proved it, or it would be red); the pre/expected shape numbers; the ledger delta (243 rows, 40 re-stamped → 266 rows, 0 re-stamped). State the CONSEQUENCES plainly: (1) every row in every `public` table on TEST is destroyed — e2e specs re-seed their own data; hand-created rows and `public.profiles` for existing `auth.users` are gone; (2) `auth.users` and every non-public schema survive untouched; (3) other people's CI runs that are queued will wait on the mutex (the run holds it for the whole act, ≈ minutes) and any run mid-flight without the mutex could see a torn schema — dispatch when `gh run list --workflow=CI --branch main` is quiet (runbook § 5's advice for the drill); (4) reversibility: the artifact holds the ledger CSV, the schema dump and the survivors; reversal is a founder-run `psql -f` after a manual drop, not a button; (5) FANOUT-GLOBAL-01 stays as it is — the restore does not change the shared-TEST assertion caveat.
  </context>
  <options>
    <option id="restore">
      <name>RESTORE NOW</name>
      <pros>TEST becomes a copy of PROD's catalogue with a CLI-shaped ledger; Wave 2's `apply-test` can run natively; every applied-ness probe in `supabase/tests` is armed; the 31-entry ratchet collapses by measurement; the act is one atomic transaction with a backup artifact already uploaded by the preflight run.</pros>
      <cons>Every row in every `public` table on TEST is destroyed (e2e re-seeds its own data; hand-created rows and existing users' `public.profiles` are gone); a foreign CI run mid-flight without the mutex could observe a torn schema only if it ignores the lock; the reversal is a founder-run `psql -f` from the artifact, not a button.</cons>
    </option>
    <option id="abort">
      <name>ABORT</name>
      <pros>Nothing on shared TEST changes.</pros>
      <cons>The phase stops here: `supabase db push` cannot run against TEST's current ledger (RESEARCH A.3(1), `ErrMissingLocal` on the 40 re-stamped rows), so Plan 04's apply-on-merge pipeline is unbuildable and `main` stays red on VAC-08 for `20260908120000`.</cons>
    </option>
  </options>
  <resume-signal>Reply "restore" or "abort". On "restore" the executor runs `gh workflow run test-restore-from-baseline.yml --ref main -f mode=restore -f confirm=<the token the preflight's confirm step printed>` — founder-approved at this moment (CONTEXT safety rule 4) — watches it to completion, and continues with Task 3. On "abort" it writes the SUMMARY with the reason and stops.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Measure the restored TEST, then empty the ratchet file BESIDE AN AIM</name>
  <files>scripts/vac08-ledger-baseline.txt, src/__tests__/vac08-ledger-dispositions.test.ts, .planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-03-RESTORE.log</files>
  <read_first>
    - scripts/vac08-ledger-baseline.txt:1-30 — its own rules: "MUST ONLY SHRINK", "a name listed here that later turns up PRESENT is a HARD FAILURE — delete the line", "removing entries is a founder decision" (the founder's decision is Task 2, recorded by run id).
    - scripts/test-ledger-drift-check.sh:361-406 — the ratchet: `:365` STALE = baselined but not measured missing; `:376-385` stale ⇒ `::error::` + exit 1; `:404` the green sentence `ledger presence: N absent, all N baselined; 0 NEW drift.`
    - src/__tests__/vac08-ledger-dispositions.test.ts:42-49 (`ENTRY_COUNT` and its rule), :128-140 (both-directions pin), :158-175 (the `[SEC]` arm over `SEC_ENTRIES`), :178-190 (the printable-reading pins and `unqualifiedStrongClaims`), :198-240 (the existing AIM block).
    - src/__tests__/mutation-annotation-parser.test.ts:2391-2421 — the AIM-FIRST ordering for a deliberately empty class ("(a) THE AIM, FIRST … fails HERE — before the live corpus is asserted to be empty").
    - src/__tests__/drift-check-scripts.test.ts — grep it for `vac08-ledger-baseline` and `ENTRY_COUNT` before editing; it scaffolds an EMPTY baseline per arm (`LEDGER_BASELINE_FILE`), so it should be unaffected — MEASURE that by running it.
  </read_first>
  <action>
**Measure first.** Paste into `164.8-03-RESTORE.log`: the restore run id and head sha, the verbatim `restore: tables=… policies=… functions=… ledger_rows=266 survivors=k/k filtered=1 mode=restore` line, the CLI post-verify output (nothing pending / every local version Remote), and the post-census. Then, read-only, against TEST through the SAME which-database check (a read of the marker first, proceed only when it names TEST): `SELECT count(*) FROM supabase_migrations.schema_migrations` (expect 266), `… WHERE version !~ '^[0-9]{14}$'` (expect 0), `SELECT count(*) FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace WHERE n.nspname='public' AND p.proname='create_allocator_connected_strategy'` (expect 0 — the DRIFT-04 function is in neither PROD nor the dump nor TEST; this is Plan 05's evidence that no pragma is needed), and `SELECT tgname FROM pg_trigger WHERE tgrelid='auth.users'::regclass AND NOT tgisinternal` (expect `on_auth_user_created`). Record each with its expected value beside it.

**Then the ratchet file.** Rewrite `scripts/vac08-ledger-baseline.txt` to header comments ONLY (zero entries). The header keeps its identity and rules ("a RATCHET, not a mute button", "MUST ONLY SHRINK", "do not hand-apply to TEST", "LEDGER ABSENCE IS NOT OBJECT ABSENCE") and adds a dated paragraph: on <date>, restore run <id> at head <sha> rebuilt TEST's schema from `baseline.sql` sha `27826b76…` and seeded the ledger with all 266 repo files, so all 31 entries turned up PRESENT — the file's own rule required their deletion, and this is the founder-decided apply-equivalent the header reserved (cite Task 2). State the reading the gate CAN now print: `ledger presence: 0 absent, all 0 baselined (see …); 0 NEW drift.` — and keep the file from spelling the phrase `zero unledgered migrations` (the `unqualifiedStrongClaims` pin at `:185` stays as-is and the file must stay clean under it; say instead that the ledger holds a row for every repo file BY SEEDING, which VAC-08 measures as `0 absent`). Add: "⛔ From here an entry may be ADDED only by founder decision for a migration that genuinely cannot reach TEST — and since TEST now mirrors PROD by construction, a migration valid for PROD is valid for TEST, so expect this file to stay empty (Phase 164.8 Plan 05 records why the `TEST-NOT-APPLICABLE` pragma was not built)."

**Then the test, in the AIM-FIRST shape.** In `vac08-ledger-dispositions.test.ts`: `ENTRY_COUNT = 0` with the docblock rewritten (why it is 0, run id, date; "may only be raised by a founder decision recorded in the header"). Restructure the live-file describe so that the FIRST `it` is the AIM (move the existing AIM block's fixture re-derivation to run before any live-file assertion, mirroring `mutation-annotation-parser.test.ts:2399-2421`), and only then `it("the live file is MEASURED empty — beside the AIM above")` asserting `entries.length === ENTRY_COUNT` with the both-directions message (a GREW message that says "an entry was added — that is a founder decision; record it in the header first"). Replace the `[SEC]` arm: `SEC_ENTRIES` becomes an empty tuple with a comment that the three security migrations reached TEST by the restore (name them and the run id), and the arm becomes an AIM: a synthetic corpus with one `[SEC]`-marked line lacking the "never received it" disclosure and one carrying it, asserting the classifier flags exactly the first — so the disclosure rule is still enforced the day an entry returns. Keep `:178` ("the file states the reading the gate CAN print" — `0 NEW drift`) and `:185` untouched.

**Observe RED before trusting:** on a scratch COPY of the test file (never `git checkout --`), set `ENTRY_COUNT` to 31 and run — it must fail naming `SHRANK to 0`; append one synthetic entry line to a COPY of the baseline via `LEDGER_BASELINE_FILE`-style redirection is not available to this test (it reads the real path), so instead assert the AIM catches a stopped classifier by mutating `rederive` in the copy to return `[]` unconditionally — the AIM `it` must go red. Record both observations. Run `npx vitest run src/__tests__/vac08-ledger-dispositions.test.ts src/__tests__/drift-check-scripts.test.ts` and `bash scripts/test-ledger-drift-check.sh --self-test` (its arms use an empty baseline already — unchanged 5/5 expected). Commit the three files together (the ratchet file and its pin are ONE edit).
  </action>
  <verify>
    <automated>test "$(grep -v '^#' scripts/vac08-ledger-baseline.txt | grep -c .)" = 0 && grep -q 'export const ENTRY_COUNT = 0;' src/__tests__/vac08-ledger-dispositions.test.ts && npx vitest run src/__tests__/vac08-ledger-dispositions.test.ts src/__tests__/drift-check-scripts.test.ts && bash scripts/test-ledger-drift-check.sh --self-test</automated>
  </verify>
  <done>The log holds the restore's run id, sha, summary line, CLI post-verify and four read-only catalog readings; the baseline has zero non-comment lines and a dated header naming the run; `ENTRY_COUNT = 0` asserted AFTER an AIM that was observed to go red when the classifier was neutered on a copy; VAC-08 self-test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder decision → destructive dispatch | the only place a human authorises a write to shared TEST |
| shared TEST → other people's CI | a torn schema mid-restore is visible to any run not holding the mutex |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8-12 | Elevation | restore dispatched without the founder | critical | mitigate | `checkpoint:decision` before the dispatch; the `confirm` token; preflight readings presented first |
| T-164.8-13 | Denial of service | foreign CI mid-flight during the restore | high | mitigate | mutex held for the act; dispatch when CI is quiet (runbook § 5); the transaction is atomic so a foreign reader sees old-or-new, never torn |
| T-164.8-14 | Repudiation | readings restated from memory later | medium | mitigate | every number in `164.8-03-RESTORE.log` carries run id + sha; the SUMMARY quotes the log |
| T-164.8-15 | Tampering | emptying the ratchet as a mute button | high | mitigate | emptied only after the 31 were MEASURED present (the gate would otherwise print `31 … no longer MEASURED absent`), beside an AIM, pin moved in the same commit |
| T-164.8-SC | Tampering | npm/pip/cargo installs | low | accept | none |
</threat_model>

<verification>
- Preflight green then restore green, both on `main`, both run ids in the log.
- Read-only catalog readings match expectations (266 / 0 / 0 / `on_auth_user_created`).
- `sql-tests` on the next main run after this PR merges prints `ledger presence: 0 absent, all 0 baselined … 0 NEW drift.` (Plan 05 records the SHA-bound first green).
</verification>

<success_criteria>
1. The founder decided on measured preflight readings; the restore ran after, not before.
2. TEST's ledger is CLI-consistent (CLI dry-run: nothing pending) — the precondition Plan 04 needs.
3. The 31-entry ratchet is empty by measurement, beside an AIM, with its pin moved in the same commit.
4. What would make this RED: a restore run id without a preceding preflight run id; `ledger_rows` ≠ the repo file count; a survivor key missing post-restore; a bare `toEqual([])` / `toBe(0)` on the live file without the AIM running first.
</success_criteria>

<output>
Create `.planning/phases/164.8-testpreprod-test-becomes-a-real-pre-prod/164.8-03-SUMMARY.md` when done, quoting the log's run ids and the `restore:` line verbatim.
</output>
