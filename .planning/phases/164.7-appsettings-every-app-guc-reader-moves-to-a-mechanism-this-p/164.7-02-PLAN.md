---
phase: 164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p
plan: 02
type: execute
subsystem: supabase-migrations
tags: [vault, system_settings, match-engine, drift-02, secdef, rls, pg-lane-fixture, criterion-2]
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql
  - scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql
  - supabase/tests/test_analytics_service_settings_and_vault_tick.sql
  - src/lib/database.types.ts
  - supabase/schema/functions/match_engine_cron_tick.sql
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-02-NEUTER.log
autonomous: true
requirements: [SC-2, D-02, D-03, CRON-DRIFT-01-GUC]
provides:
  - "public.system_settings — the TEXT-valued sibling of system_flags (D-03), RLS-enabled, admin+service policies only, anon REVOKEd, seeded with analytics_service_url = the manifest literal"
  - "public.match_engine_cron_tick() — the repo's description of the mechanism PROD jobid 1 ALREADY uses (D-02 as corrected: DRIFT-02, not a key move): key from vault.decrypted_secrets, url from system_settings, RAISE on either absent, net.http_post async — SCHEDULES NOTHING"
  - "scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql — a vault schema stand-in so the RAISE paths are provable on the lane"
  - "A new SQL gate with 7 arms, each with a RED-UNDER-M twin, proven RED-first on the lane"
affects: [164.7-05, 164.7-06, 164.5]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Applying 20260907120000 on a vanilla lane (no vault, no pg_net) SUCCEEDS — no apply-time reference to vault.* or net.* exists in the file (TRAP D), only run-time references inside the plpgsql body"
    - "With the vault stand-in present but no analytics_service_key row, match_engine_cron_tick() RAISES naming the key; with the key present but no analytics_service_url row it RAISES naming the url; with both present it gets PAST both checks (observed by the error/return NOT being either RAISE)"
    - "anon cannot read system_settings; an authenticated non-admin cannot UPDATE it; service_role can — each proven by an arm whose twin reddens it on the lane"
    - "The file contains ZERO occurrences of the cron-schedule and cron-unschedule tokens and ZERO app-GUC reads, comments included (scope fence vs 164.5 item 7; gate 3a's raw-text scan once the window widens)"
    - "Every new arm was driven RED by its twin on the lane BEFORE the file was committed as GREEN, and the log carries the RED (identity ok) count"
  artifacts:
    - path: "supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql"
      provides: "BEGIN/lock_timeout/COMMIT wrapper; system_settings DDL + RLS + 2 policies + REVOKE from anon; url seed ON CONFLICT DO NOTHING; match_engine_cron_tick() SECDEF search_path=public,pg_catalog; REVOKE ALL FROM PUBLIC,anon,authenticated; COMMENT ON FUNCTION; house-style DO $verify$ block"
    - path: "scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql"
      provides: "CREATE SCHEMA vault; TABLE vault.decrypted_secrets(name TEXT PRIMARY KEY, decrypted_secret TEXT); FUNCTION vault.create_secret(text,text,text) inserting into it — header says why it exists and that it is never applied to TEST or PROD"
    - path: "supabase/tests/test_analytics_service_settings_and_vault_tick.sql"
      provides: "arms 0, V1, U1, C1, R1, R2, R3 with RED-UNDER-M twins and a RED-UNDER-SETUP apply list; sentinel ALL 7 ARMS EXECUTED (0,V1,U1,C1,R1,R2,R3)"
    - path: "src/lib/database.types.ts"
      provides: "system_settings Row/Insert/Update/Relationships block mirroring system_flags"
  key_links:
    - from: "match_engine_cron_tick() body"
      to: "vault.decrypted_secrets"
      via: "SELECT decrypted_secret INTO v_key … WHERE name = 'analytics_service_key' — the manifest jobid-1 idiom byte-for-byte"
      pattern: "vault\\.decrypted_secrets"
    - from: "match_engine_cron_tick() body"
      to: "public.system_settings"
      via: "SELECT s.value INTO v_url … WHERE s.key = 'analytics_service_url'"
      pattern: "analytics_service_url"
    - from: "supabase/tests/test_analytics_service_settings_and_vault_tick.sql RED-UNDER-SETUP"
      to: "scripts/pg-lane/fixtures/07,12,15,32 then the migration"
      via: "fixtures first (07 makes the anon REVOKE falsifiable), migration last"
      pattern: "RED-UNDER-SETUP"
---

<objective>
Ship the settled mechanism for the two `analytics_service_*` settings, as the CONTEXT's final correction frames it: **`app.analytics_service_key` is ALREADY on Vault in the live PROD row (jobid 1, captured verbatim in `scripts/prod-prober/cron-manifest.json`), and the repo does not describe that mechanism — this is DRIFT-02's shape, closed by making the repo describe what PROD runs. `app.analytics_service_url` has NO consumer anywhere; the live row hardcodes the literal.** Neither April migration defines a function; both are one-shot DO blocks that WROTE a cron row (`20260408215026` superseding `20260408113029`), and both get lineage headers in plan 05, not edits here.

So this plan creates, in ONE migration per D-02/D-03 (the two values are read side by side, so one file): `public.system_settings` (D-03's sibling table, cloned from `system_flags`' shape and policy set MINUS the public-read policy — nothing browser-side needs the URL) seeded with the manifest's literal URL, and `public.match_engine_cron_tick()` — a SECURITY DEFINER callable whose body IS the manifest command with the URL read from the table and the key read via the manifest's own Vault idiom, RAISING on either being absent (the null-header 401 outage of CRON-DRIFT-01 must convert into a failed `cron.job_run_details` row, never a silent 200-looking green). **It schedules nothing.** The live jobid-1 repoint is Phase 164.5 item 7 and RESEARCH Open Question 2 records the `vault-absent` hygiene-rule interaction that phase must resolve; this file must contain no cron-schedule or cron-unschedule token anywhere, comments included.

**Kill-switch route (Claude's discretion, decided):** no admin route is shipped in this phase. Criterion 4 needs the flip to WORK and be OBSERVED; the flip is a founder SQL-editor UPDATE inside a runbook that already requires the which-database marker first. A route would add a CSRF/limiter/audit surface to review for a switch that is thrown a handful of times a year. Recorded here so it is not re-litigated; a later phase may add one modelled on `kill-switch/route.ts`.

**Lane reality (RESEARCH Q1, TRAP D):** the pg-lane has no `vault` schema and no `pg_net`. Vault and net references live ONLY inside the plpgsql body (resolved at run time), never in apply-time SQL or the verify block, so the migration applies on the lane and on shared TEST. A stand-in fixture supplies `vault.decrypted_secrets` and `vault.create_secret` so the RAISE paths are provable; the happy path (an actual POST) is exercised only where pg_net exists and only against an unroutable URL.

Purpose: SC-2 for `analytics_service_key` and `analytics_service_url`, by name; the GUC half of CRON-DRIFT-01.
Output: migration A, fixture 32, the new gate, the types block, the neuter log.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-CONTEXT.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-RESEARCH.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-PATTERNS.md
@scripts/prod-prober/cron-manifest.json
@supabase/migrations/20260407164606_perfect_match.sql
@supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql
@supabase/migrations/20260906120000_computation_error_provenance.sql
@supabase/tests/test_profiles_privileged_columns_locked.sql
@supabase/tests/test_sync_status_curated_sentence_survives.sql
@scripts/pg-lane/fixtures/07-fixture-supabase-default-privileges.sql
@scripts/pg-lane/fixtures/15-fixture-auth-role.sql
@scripts/mutation-runner/GRAMMAR.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "the callable reads the table and the vault, and RAISES when either is empty" — migration + vault stand-in + one lane run</name>
  <files>supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql, scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql, supabase/schema/functions/match_engine_cron_tick.sql</files>
  <read_first>
    - `scripts/prod-prober/cron-manifest.json` jobid 1 `command` — the DO body to transcribe: the `SELECT decrypted_secret INTO v_key FROM vault.decrypted_secrets WHERE name = 'analytics_service_key'` read, the `IS NULL OR = ''` RAISE, the `net.http_post(url := …, headers := jsonb_build_object('Content-Type','application/json','X-Service-Key', v_key), body := '{}'::jsonb, timeout_milliseconds := 60000)` call
    - `supabase/migrations/20260407164606_perfect_match.sql` section "2. system_flags" (the four-column DDL, the `ON CONFLICT (key) DO NOTHING` seed) and section "8. RLS" (`system_flags_admin_all`, `system_flags_service_all` — clone these two; do NOT clone `system_flags_match_engine_public_read`)
    - `supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql` — the `BEGIN; SET lock_timeout = '5s'; … COMMIT;` wrapper, the SECDEF triple (`SECURITY DEFINER`, `SET search_path = public, pg_catalog`), the `REVOKE ALL ON FUNCTION … FROM PUBLIC, anon, authenticated;` line, and the `DO $verify$` block's DECLARE-everything note (42601) and `RAISE EXCEPTION 'Migration …: <what> — <why>'` house style; its TRADE-OFF note rejecting checks that are only true on PROD
    - `supabase/migrations/20260621120000_scenarios_table_and_rls.sql` — the `TO authenticated` policy scoping and the defense-in-depth `REVOKE … FROM anon` on a fresh table
    - PATTERNS § 2 CONFLICT 1 — admin predicate is `profiles.is_admin`, never `current_user_has_app_role` (the anon-EXECUTE trap)
    - RESEARCH § Code Examples (the callable shape and the system_settings DDL) and § Q1 "Failure mode when the secret is absent" (why the RAISE is load-bearing)
    - `scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql` header — the additive stand-in idiom and "never a second base"
  </read_first>
  <action>
    Write `scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql` FIRST (it is what makes the tracer runnable on the lane): a header in the fixture-12 voice stating that Supabase's `supabase_vault` extension is created by `supabase/schema/baseline.sql` and by NO migration, that the lane has no such extension, that this stand-in exists ONLY so the RAISE paths of run-time Vault readers are provable, and that it must never be applied to TEST or PROD. Body: `CREATE SCHEMA IF NOT EXISTS vault;` a TABLE `vault.decrypted_secrets (name TEXT PRIMARY KEY, decrypted_secret TEXT)`, and a FUNCTION `vault.create_secret(p_secret TEXT, p_name TEXT, p_description TEXT DEFAULT NULL) RETURNS uuid` that inserts and returns `gen_random_uuid()` — matching the real signature order (`secret, name, description`) so a gate can call it identically on TEST. Check that lint rules R5/R6 do not fire (no migration creates these objects, so no shadowing).

    Write the migration `supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql`. Header: title, phase/plan/criterion line, the ⚠️ OPS auto-apply sentence from the 20260906120000 precedent, a "⛔ what this file does NOT do" block (registers no schedule, repoints no cron row — 164.5 item 7 — and touches no vault SECRET: the secret `analytics_service_key` exists on PROD since 2026-08-25 per TODOS CRON-DRIFT-01), and a DRIFT-02 paragraph saying this file makes the repo describe the mechanism jobid 1 already runs, citing the manifest by path and jobid. Keep the header free of any cron-schedule/unschedule token and of any app-GUC read call — write "app-GUC read" in prose.

    STEP 1 — `public.system_settings`: `CREATE TABLE IF NOT EXISTS public.system_settings (key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at TIMESTAMPTZ NOT NULL DEFAULT now(), updated_by UUID REFERENCES public.profiles(id));` `ALTER TABLE … ENABLE ROW LEVEL SECURITY;` `REVOKE ALL ON TABLE public.system_settings FROM anon;` (defense in depth, scenarios precedent); policies `system_settings_admin_all` (FOR ALL TO authenticated, USING/WITH CHECK `EXISTS (SELECT 1 FROM public.profiles WHERE id = auth.uid() AND is_admin = true)`) and `system_settings_service_all` (FOR ALL, USING/WITH CHECK `auth.role() = 'service_role'`) — both `CREATE POLICY` guarded by a `DO` block `IF NOT EXISTS (SELECT 1 FROM pg_policies …)` so a re-apply is idempotent; seed `INSERT INTO public.system_settings (key, value) VALUES ('analytics_service_url', 'https://quantalyze-analytics-production.up.railway.app') ON CONFLICT (key) DO NOTHING;` with a comment that the literal is verbatim from the committed manifest (already public) and that DO NOTHING means a re-apply never overwrites an operator's value. `COMMENT ON TABLE` stating: non-secret operator configuration; secrets go to Vault, never here.

    STEP 2 — `CREATE OR REPLACE FUNCTION public.match_engine_cron_tick() RETURNS BIGINT LANGUAGE plpgsql SECURITY DEFINER SET search_path = public, pg_catalog AS $tick$ … $tick$;` Body, in order: read the key with the manifest idiom into `v_key`; `IF v_key IS NULL OR v_key = '' THEN RAISE EXCEPTION 'analytics_service_key missing from vault — refusing to send a null header'; END IF;` (message byte-identical to the manifest so the cron-obs/cron-drift vocabulary stays one); read `v_url` from `public.system_settings` where key = `analytics_service_url`; `IF v_url IS NULL OR v_url = '' THEN RAISE EXCEPTION 'analytics_service_url missing from system_settings — refusing to post to a null url'; END IF;` then `SELECT net.http_post(url := v_url || '/api/match/cron-recompute', headers := jsonb_build_object('Content-Type', 'application/json', 'X-Service-Key', v_key), body := '{}'::jsonb, timeout_milliseconds := 60000) INTO v_req; RETURN v_req;`. Comment inside the body: net.http_post is ASYNC — a returned request id is NOT an HTTP success; the cron-obs arm reads `net._http_response` (CRON-OBS-01). No identifier and no secret in any RAISE text (T-161.1-10). Then `REVOKE ALL ON FUNCTION public.match_engine_cron_tick() FROM PUBLIC, anon, authenticated;` and a `COMMENT ON FUNCTION` that says it is the CALLABLE half of jobid 1's mechanism and that the row repoint is Phase 164.5's.

    STEP 3 — `DO $verify$` block, every variable DECLAREd up front (42601 note copied): (1) function exists with `pronargs = 0`; (2) `prosecdef` true and `proconfig` carries `search_path=`; (3) `has_function_privilege('anon', …, 'EXECUTE')` and `('authenticated', …)` both FALSE; (4) `system_settings` exists, `relrowsecurity` true, both policy names present; (5) the seed row exists; (6) the function body text (`pg_get_functiondef`) contains `vault.decrypted_secrets` AND `analytics_service_url` — a text check is honest here because this file OWNS the body it just wrote. NO check touches `vault.*` or `net.*` as objects (TRAP D). Close with `RAISE NOTICE 'Migration 20260907120000: applied (system_settings seeded, match_engine_cron_tick defined, NOTHING scheduled)'`. `COMMIT;`.

    Run the tracer on the lane: build a gate SQL in the scratchpad that (a) calls the function with the stand-in EMPTY and asserts SQLERRM contains `analytics_service_key missing from vault`; (b) inserts a stand-in secret, DELETEs the url row, calls, asserts SQLERRM contains `analytics_service_url missing from system_settings`; (c) restores the url row (any value), calls, and asserts the failure is NOT one of the two RAISEs (on the lane `net.http_post` does not exist → SQLSTATE 42883 or 3F000). Apply list: fixtures 01, 02, 07, 12, 15, 32, then the migration. Assert the lane prints the migration's applied NOTICE and the tracer's OK line.

    Then run `npm run schema:functions` and READ what it does with the new function: if it emits `supabase/schema/functions/match_engine_cron_tick.sql`, commit it; if it does not, record why in the SUMMARY (the generator's selection is a measurement, not an assumption) and remove the path from this plan's `files_modified` in the SUMMARY's deviation note. Either way `npm run schema:functions:check` must exit 0 afterwards.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && M=supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql && test -f "$M" && test -f scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql && test "$(grep -a -c -i 'cron\.schedule\|cron\.unschedule' "$M")" = "0" && test "$(grep -a -c "current_setting('app\." "$M")" = "0" && grep -a -c 'vault.decrypted_secrets' "$M" | grep -v '^0$' && grep -a -c "SET search_path = public, pg_catalog" "$M" | grep -v '^0$' && grep -a -c 'REVOKE ALL ON FUNCTION public.match_engine_cron_tick() FROM PUBLIC, anon, authenticated' "$M" | grep -q '^1$' && test "$(grep -a -c 'system_settings_match_engine_public_read\|public_read' "$M")" = "0" && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p02-tracer && rm -rf "$S" && mkdir -p "$S" && printf "DO \$\$ DECLARE v_msg text; v_state text; BEGIN BEGIN PERFORM public.match_engine_cron_tick(); RAISE EXCEPTION 'TEST FAILED (T-a): returned with no secret'; EXCEPTION WHEN OTHERS THEN GET STACKED DIAGNOSTICS v_msg = MESSAGE_TEXT; IF v_msg !~ 'analytics_service_key missing from vault' THEN RAISE EXCEPTION 'TEST FAILED (T-a): %%', v_msg; END IF; END; PERFORM vault.create_secret('k','analytics_service_key','t'); DELETE FROM public.system_settings WHERE key='analytics_service_url'; BEGIN PERFORM public.match_engine_cron_tick(); RAISE EXCEPTION 'TEST FAILED (T-b): returned with no url'; EXCEPTION WHEN OTHERS THEN GET STACKED DIAGNOSTICS v_msg = MESSAGE_TEXT; IF v_msg !~ 'analytics_service_url missing from system_settings' THEN RAISE EXCEPTION 'TEST FAILED (T-b): %%', v_msg; END IF; END; INSERT INTO public.system_settings(key,value) VALUES ('analytics_service_url','http://127.0.0.1:9'); BEGIN PERFORM public.match_engine_cron_tick(); EXCEPTION WHEN OTHERS THEN GET STACKED DIAGNOSTICS v_msg = MESSAGE_TEXT, v_state = RETURNED_SQLSTATE; IF v_msg ~ 'missing from' THEN RAISE EXCEPTION 'TEST FAILED (T-c): %%', v_msg; END IF; END; RAISE NOTICE 'TRACER-02 OK'; END \$\$;\n" > "$S/gate.sql" && bash scripts/pg-lane/run.sh --workdir "$S/lane" --apply scripts/pg-lane/fixtures/01-fixture-core.sql scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql scripts/pg-lane/fixtures/07-fixture-supabase-default-privileges.sql scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql scripts/pg-lane/fixtures/15-fixture-auth-role.sql scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql "$M" --gate "$S/gate.sql" > "$S/out.log" 2>&1; rc=$?; grep -a 'Migration 20260907120000: applied' "$S/out.log" && grep -a 'TRACER-02 OK' "$S/out.log" && test "$rc" = "0" && npm run -s schema:functions:check && node scripts/lint-sql-gates.mjs --files scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql</automated>
  </verify>
  <done>The migration applies on a vanilla lane with the vault stand-in; the callable raises the key message with no secret, the url message with no url row, and neither once both exist; the file carries no cron-schedule/unschedule token and no app-GUC read; the snapshot check is clean; the fixture passes the SQL-gate lint.</done>
</task>

<task type="auto">
  <name>Task 2: The gate file — 7 arms, 7 twins, driven RED first on the lane</name>
  <files>supabase/tests/test_analytics_service_settings_and_vault_tick.sql</files>
  <read_first>
    - `supabase/tests/test_sync_status_curated_sentence_survives.sql` header (the ⭐ WHY EVERY IDENTITY CARRIES A DIGIT block), its `RED-UNDER-SETUP` line, and its arm-0 `sql`-kind twin — the newest gate in the corpus and this file's template
    - `supabase/tests/test_profiles_privileged_columns_locked.sql` lines with `set_config('request.jwt.claims', …)` / `SET LOCAL ROLE authenticated` / `RESET ROLE` — the role-switch idiom and its two traps (identity raises only on simple heads; wrap privileged probes so a bare `permission denied` cannot score NO-IDENTITY)
    - `supabase/tests/test_resync_retry_single_job.sql` assertion (b) — the `BEGIN … EXCEPTION … RAISE EXCEPTION 'TEST FAILED (x)'` wrap; lint rule R1 forbids DML/SELECT INTO inside the handler
    - `scripts/mutation-runner/GRAMMAR.md` "The four annotation rules" and "Schema" — markers line-start anchored; every `edit` carries MEASURED `occurrences`; no mutation may inject the identity literal; `sql` steps run on the live lane after the apply list
    - `.github/workflows/ci.yml` `sql-tests` sentinel read-back (the `ALL N ARMS EXECUTED` grep and the "roster must agree" comment)
    - RESEARCH § Q5 VAC-08 paragraph and Pitfall 4 — this file is RED on shared TEST until the migration is hand-applied there; that is named in the SUMMARY, never turned into a SKIP
  </read_first>
  <action>
    Create `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` in the corpus house style: header (what it proves, the shared-TEST caveats, the expected-red-until-applied sentence naming `20260907120000`, the `⚠️ C1 posts to 127.0.0.1:9` note), one `-- RED-UNDER-SETUP:` line whose apply list is fixtures `01, 02, 07, 12, 15, 32` then `supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql` (DISCOVERED by running it, not guessed — if the lane needs another fixture the run says so), then ONE `DO $$` block with every variable DECLAREd up front, ending in `ROLLBACK;` so nothing persists on shared TEST.

    Arms, each preceded by a `-- RED-UNDER:` prose line and a `-- RED-UNDER-M:` JSON twin on its own comment line, each failure a simple-head `RAISE EXCEPTION 'TEST FAILED (<id>): …'`:
    - **0** applied-ness (absence is failure, WR-03): function `match_engine_cron_tick` exists AND table `system_settings` exists, else `TEST FAILED (0)` naming the migration as cause (i) and a DROP as cause (ii). Twin: `{"kind":"sql","stmt":"DROP FUNCTION public.match_engine_cron_tick()"}`.
    - **V1** no secret ⇒ RAISES the key message: ensure no `analytics_service_key` row exists in `vault.decrypted_secrets` for this transaction (on TEST a committed secret may exist — if it does, `DELETE` it INSIDE this transaction; it rolls back; state that in the arm comment). Call inside BEGIN/EXCEPTION; a return is `TEST FAILED (V1)`; a raise whose MESSAGE_TEXT lacks `analytics_service_key missing from vault` is `TEST FAILED (V1)`. Twin: edit the migration, find `IF v_key IS NULL OR v_key = '' THEN` replace `IF FALSE THEN`, occurrences MEASURED (expected 1).
    - **U1** no url ⇒ RAISES the url message: `PERFORM vault.create_secret('arm-u1', 'analytics_service_key', 'test')` if absent; `DELETE FROM public.system_settings WHERE key = 'analytics_service_url'`; call; assert as V1 with the url message. Twin: find `IF v_url IS NULL OR v_url = '' THEN` replace `IF FALSE THEN`.
    - **C1** both present ⇒ PAST both checks: `INSERT … ('analytics_service_url', 'http://127.0.0.1:9') ON CONFLICT (key) DO UPDATE SET value = EXCLUDED.value`; call; accept EITHER a returned bigint (pg_net present: async, unroutable target, harmless) OR a raise whose SQLSTATE is `42883` or `3F000` (no pg_net on the lane) — and in BOTH cases `TEST FAILED (C1)` if the message contains `missing from`. Twin: find `WHERE name = 'analytics_service_key'` replace `WHERE name = 'analytics_service_key_never'` (the key read now misses → the key RAISE fires → C1 reddens). MEASURE occurrences: the literal appears in the body once and possibly in the verify block or comments — pick a `find` string that is unique or set `occurrences` to the measured number, per GRAMMAR rule 2.
    - **R1** anon cannot read: `set_config('request.jwt.claims', '{"role":"anon"}', true)`; `SET LOCAL ROLE anon`; wrapped `SELECT count(*) INTO v_cnt FROM public.system_settings` (permission denied → v_cnt := 0 via handler that only assigns/raises nothing else); `RESET ROLE`; `TEST FAILED (R1)` if v_cnt > 0. Twin (compound `sql`): `GRANT SELECT ON public.system_settings TO anon` then `CREATE POLICY ss_arm_r1 ON public.system_settings FOR SELECT TO anon USING (true)`.
    - **R2** authenticated non-admin cannot UPDATE: seed a non-admin profile via fixture-02's `profiles` (is_admin defaults false), set claims sub/role authenticated, `SET LOCAL ROLE authenticated`, wrapped `UPDATE public.system_settings SET value = 'r2' WHERE key = 'analytics_service_url'`, `RESET ROLE`, read the value back as postgres: `TEST FAILED (R2)` if it equals `'r2'`. Twin: `sql` `CREATE POLICY ss_arm_r2 ON public.system_settings FOR ALL TO authenticated USING (true) WITH CHECK (true)`.
    - **R3** service_role CAN update: claims role service_role, `SET LOCAL ROLE service_role`, `UPDATE … SET value = 'r3'`, `RESET ROLE`, `TEST FAILED (R3)` unless the value reads `'r3'`. Twin: `sql` `DROP POLICY system_settings_service_all ON public.system_settings`.
    Restore the url row to its seed value at the end of the block for readability (it rolls back anyway). Sentinel: `RAISE NOTICE 'ALL 7 ARMS EXECUTED (0,V1,U1,C1,R1,R2,R3) and passed — …'` — count AND roster.

    Drive it: `node scripts/mutation-runner/run.mjs --parse-only` must show every `occurrences` matching (fix any `MEASURE_FAIL` by re-measuring, never by guessing); then `node scripts/mutation-runner/run.mjs --file supabase/tests/test_analytics_service_settings_and_vault_tick.sql` (DIAGNOSTIC, never exits 0) and read the table: baseline GREEN, 7 × `RED (identity ok)`, each arm's first failure naming ITSELF. Any `no-red`, `NO-IDENTITY` or wrong-arm-first is fixed at the arm, not at the twin. Also `node scripts/lint-sql-gates.mjs` (bare) must stay clean — the new file is in its corpus.

    ⚠️ Between this commit and plan 05's floor move, the FULL runner (`node scripts/mutation-runner/run.mjs`) exits 1 with a `FILES_FLOOR` stale-ratchet defect (46 annotated > floor 45). That is the ratchet reporting growth, not a regression; the commit body carries `FLOOR-INTERVAL: annotated 46 > FILES_FLOOR 45 until 164.7-05 pins`.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && G=supabase/tests/test_analytics_service_settings_and_vault_tick.sql && P=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p02-parse.log && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p02-arms && rm -f "$P" "$S/diag.log" && mkdir -p "$S" && test -f "$G" && grep -a -c '^[ \t]*--[ \t]*RED-UNDER-M' "$G" | grep -q '^7$' && grep -a -c "ALL 7 ARMS EXECUTED (0,V1,U1,C1,R1,R2,R3)" "$G" | grep -q '^1$' && grep -a -c '^[ \t]*--[ \t]*RED-UNDER-SETUP' "$G" | grep -q '^1$' && grep -a -o 'RED-UNDER-SETUP: .*' "$G" | grep -a -c '32-fixture-vault-stand-in.sql.*20260907120000_analytics_service_settings_and_vault_tick.sql' | grep -q '^1$' && node scripts/lint-sql-gates.mjs && { node scripts/mutation-runner/run.mjs --parse-only > "$P" 2>&1 || true; } && grep -a -E '^coverage: files 46/73' "$P" && { node scripts/mutation-runner/run.mjs --file "$G" > "$S/diag.log" 2>&1 || true; } && red=$(grep -a -c 'RED (identity ok)' "$S/diag.log") && echo "red=$red" && test "$red" = "7" && test "$(grep -a -c -E 'no-red|NO-IDENTITY|wrong-arm' "$S/diag.log")" = "0" && git log -1 --format=%B | grep -a -c 'FLOOR-INTERVAL:' | grep -v '^0$'</automated>
  </verify>
  <done>The gate carries 7 twins and a 7-arm roster sentinel; parse-only measures every occurrence; the diagnostic run shows 7 RED (identity ok) with no no-red/NO-IDENTITY; lint-sql-gates stays clean; parse-only reports the corpus at 46/73 (this file is the 46th annotated, the 73rd file); the commit body carries the floor-interval marker. FALSIFIABILITY (revision 2, N1): one `&&` chain, both scratch logs `rm -f`'d first, the never-zero runner modes wrapped `{ … || true; }` — if parse-only stops printing `coverage: files 46/73` (e.g. a MEASURE_FAIL on this file), the grep fails and the chain STOPS before `--file` runs; and if `--file` reports fewer than 7 RED, `red=$(grep -c …)` is read from a log this run wrote, never a stale one. Previously the `;` let a skipped `--file` run pass on the previous iteration's `diag.log`.</done>
</task>

<task type="auto">
  <name>Task 3: Types block, neuter evidence for the gate itself, and the shared-TEST expectation named</name>
  <files>src/lib/database.types.ts, .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-02-NEUTER.log</files>
  <read_first>
    - `src/lib/database.types.ts` the `system_flags: {` block (Row/Insert/Update/Relationships with the `system_flags_updated_by_fkey` entries) — mirror it exactly for `system_settings`, `value: string` in place of `enabled: boolean`
    - RESEARCH § Q2 closing paragraph — no `gen types` script exists and the CLI is linked to PROD; the block is HAND-WRITTEN, and `npx tsc --noEmit` is the proof it is well-formed
    - The neuter protocol in plan 01 Task 3 (byte backup, sha256 BEFORE/AFTER, no `git checkout --`)
  </read_first>
  <action>
    Add the `system_settings` block to `src/lib/database.types.ts` immediately after `system_flags`, alphabetically consistent with the file's ordering, with a one-line comment that it was hand-written from migration `20260907120000` because no type-generation script exists here and the CLI is PROD-linked (cite RESEARCH). Run `npx tsc --noEmit` (Node 22 PATH if available) and confirm clean.

    Neuter the GATE (not the migration — the twins already prove the migration's arms): take a byte backup of the gate file with sha256; (N1) replace arm V1's `TEST FAILED (V1)` raise head with a NOTICE so the arm cannot fail, run `node scripts/mutation-runner/run.mjs --file <gate>` and observe the runner's neuter/identity machinery report the arm as not biting (record the exact defect kind printed); (N2) delete the `-- RED-UNDER-M:` line of arm R3 and observe `--parse-only` report the section without a twin (record the printed line). Restore from the byte backup after each, assert sha256 equality, assert `git status --porcelain <gate>` empty. Write `164.7-02-NEUTER.log` in the phase dir with the same block shape as plan 01's log, plus a final section `EXPECTED RED ON SHARED TEST` naming: `test_analytics_service_settings_and_vault_tick.sql → TEST FAILED (0)` until `20260907120000` reaches TEST (and, if TEST has no `vault` schema at all, the arm and SQLSTATE that will fire instead — write it as UNKNOWN-until-first-run, to be read off the first `sql-tests` log in plan 06). Commit.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && grep -a -c 'system_settings: {' src/lib/database.types.ts | grep -q '^1$' && awk '/system_settings: \{/,/Relationships/' src/lib/database.types.ts | grep -a -c 'value: string' | grep -v '^0$' && npx tsc --noEmit 2>&1 | tail -3 | grep -a -c 'error TS' | grep -q '^0$' && L=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-02-NEUTER.log && test -f "$L" && grep -a -c -E '^(BEFORE|AFTER) sha256: [0-9a-f]{64}' "$L" | grep -q '^4$' && grep -a -c 'EXPECTED RED ON SHARED TEST' "$L" | grep -q '^1$' && grep -a -c 'TEST FAILED (0)' "$L" | grep -v '^0$' && test "$(grep -a -c 'git checkout --' "$L")" = "0" && git status --porcelain supabase/tests/test_analytics_service_settings_and_vault_tick.sql | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <done>The types file carries exactly one system_settings block and tsc is clean; two neuters of the gate were observed caught by the runner and restored with hash equality; the expected shared-TEST red is named by arm before any PR exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator UPDATE → system_settings | a non-secret value an admin/service role may change; anon and ordinary users must not |
| function body → vault.decrypted_secrets | the secret crosses from Vault into an HTTP header inside a SECDEF body and must never be logged or stored |
| migration merge → PROD auto-apply | every statement runs on PROD with no deploy step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.7-05 | Information disclosure | system_settings read by anon | medium | mitigate | no public-read policy; table-level REVOKE from anon; arm R1 + twin |
| T-164.7-06 | Elevation | non-admin UPDATE of the URL (exfiltration target) | high | mitigate | admin policy `TO authenticated` on `profiles.is_admin`; arm R2 + twin; service policy arm R3 |
| T-164.7-07 | Information disclosure | secret in cron.job.command or in a RAISE | high | mitigate | key read at run time inside the body, never baked; RAISE texts carry no values; cron-drift hygiene rules stay in force |
| T-164.7-08 | Repudiation | absent secret → null header → silent 401s | high | mitigate | explicit RAISE on NULL/empty (arms V1/U1); async note points at cron-obs |
| T-164.7-09 | Tampering | a schedule shipped in a migration | high | mitigate | zero cron-schedule/unschedule tokens (verify grep); 164.5 owns the row |
| T-164.7-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Lane: migration applies with fixture 32; tracer OK; gate diagnostic 7 RED (identity ok).
- `node scripts/lint-sql-gates.mjs` clean; `npm run schema:functions:check` clean; `npx tsc --noEmit` clean.
- Parse-only: `coverage: files 46/73`; full runner RED only with the FILES_FLOOR stale ratchet until plan 05.
</verification>

<success_criteria>
- `analytics_service_key` and `analytics_service_url` are each dispositioned BY NAME with a migration that ships the mechanism (SC-2), and the file schedules nothing (scope fence).
- Every arm can fail and was seen failing; the shared-TEST red is named before the PR.
</success_criteria>

<output>
Create `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-02-SUMMARY.md` when done, recording the snapshot generator's behaviour for the new function and the diagnostic run's table verbatim.
</output>
