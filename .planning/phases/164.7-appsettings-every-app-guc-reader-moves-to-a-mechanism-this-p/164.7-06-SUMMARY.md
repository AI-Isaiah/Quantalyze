# 164.7 Plan 06 — SUMMARY

**Completed:** 2026-09-10
**Mode:** RECONSTRUCTION from evidence (founder decision, 2026-09-10)
**Status:** complete, with two limits recorded rather than papered over

## What this plan turned out to be

Plan 06 was authored as a **pre-merge obligation**: three reviewers on the working branch, then the
PR, then the VAC-04 observation and the earned ack. By the time it was executed, 164.7's migrations
had already merged (PR #756, `14b3b6c3`, 2026-09-07T17:35:36Z) and **the work had been done at ship
time and recorded in the PR body and `TODOS.md` instead of in the phase artifacts.**

So this execution did not redo the work. It **moved the record into the phase**, re-verifying every
claim against the primary source rather than transcribing it. The founder chose this over a
post-hoc reviewer re-run, because re-reviewing already-applied migrations cannot satisfy a
pre-merge ordering truth either, and would have required inventing per-reviewer attribution.

## Artifacts

| file | what it carries |
|---|---|
| `164.7-VAC04-OBSERVED.md` | the run (id, job id, head sha), the branch VAC-04 took proven from the log, the verbatim verdict block, and the ack verified byte-for-byte |
| `164.7-REVIEWS.md` | the findings that exist, by `TODOS.md` id and line, with the attribution limit stated |
| `.planning/WINDOWS.md` entry 25 | dispositioned `open` → `fixed` from measurement |

## The three must-haves

| # | claim | verdict |
|---|---|---|
| 1 | no migration reaches a PR before all three reviewers have run | ✅ **MET — PROVEN by timestamp.** ⛔ CORRECTED 2026-09-10 after `gsd-verifier`: the first grading said the ordering "cannot be independently proven" and that was too weak. ⛔ STRENGTHENED again 2026-09-10: the first upgrade cited `b4a01504`, which names only ONE reviewer while the must-have says all three. The commit that carries it is **`968b1f57` @ 14:26:03Z** — the first on the branch whose `TODOS.md` holds the `three-reviewer residuals` heading AND all six ids (predecessors hold zero) — **8m15s** before PR #756 existed at 14:34:18Z. |
| 2 | the pragma carries the PROD sha VAC-04 printed, and the two shas are identical | ✅ **VERIFIED byte-for-byte at write time.** `88e6af84…ae6e36` and `7c3d33e9…81ac4` in the log match `20260907130000:10-11` exactly. |
| 3 | the record names VAC-04's branch by line — the compare loop, NOT `:198`, NOT `:448` | ✅ **PROVEN from the log.** `Functions defined or replaced by this PR: 5` makes `:198` impossible; `Functions indexed in the PROD source: 120` makes `:448` impossible. Exactly RESEARCH Q5's prediction, now measured. |

## Gates re-run at HEAD 2026-09-10

⚠️ These measure the CURRENT tree (164.7's code plus everything merged since, including
164.8/164.8.1/164.8.2). They are a statement about today, not a reconstruction of the PR-time
reading.

```
lint-sql-gates      73 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s)
lint-app-guc        self-test OK (4 kinds, red+green each); corpus: 292 scanned, 5 annotated, 0 findings
schema:functions    snapshot current (120 functions); DRIFT-05 gate (a) 120 vs 120 — agree, 0 ratcheted
mutation-runner     --parse-only rc=0; coverage: files 46/73; arms 0/384/0; no MEASURE_FAIL
pytest              39 passed (test_ledger_refresh_gates.py, test_ledger_refresh_kind_scope_drift.py)
```

⭐ `schema:functions:check` reports **120** functions and VAC-04's log reported **120** indexed from
the PROD source. Two independent instruments, same count.

## Findings carried forward — none invented, none dropped

| id | state | who owns it now |
|---|---|---|
| `[164.7-VAULT-ABSENT-RULE]` | **OPEN — now blocking** | **Phase 164.5.1**, its first work |
| ⚠️ `[VAULTTICK-EMPTYKEY-01]` | **OPEN** | **Phase 164.5.1 must know it** — the whitespace-key hole is in the callable 164.5.1 repoints jobid 1 at |
| `[APPGUC-DETECT-DOUBLEQUOTE-01]` | OPEN | unrouted |
| `[APPGUC-UTF16-01]` | OPEN | unrouted |
| `[APPGUC-WARNING-UNINSTRUMENTED-01]` | OPEN | unrouted |
| `[164.7-PLAN03-EVIDENCE-01]` | OPEN | unrouted |
| `[164.7-CITATION-DRIFT-01]` | OPEN | unrouted |
| `GATE-COMMENT-D2-FALSE` | OPEN | red-team item, not a reviewer finding |
| `[164.7-TEST-APPLY-APPSETTINGS]` | CLOSED | expected-red by design, not a finding |

⛔ **CORRECTED 2026-09-10 — the table above was WRONG and `gsd-verifier` caught it.** The first
version said five of six findings were recoverable and the sixth was not identifiable. In fact
`TODOS.md` carries a heading reading `## Phase 164.7 (APPSETTINGS) — SQL-fixer + three-reviewer
residuals (logged 2026-09-07)` with **exactly six** entries under it, and the first version cited a
line INSIDE that section while missing the heading eight lines above. It also listed the wrong set:
two non-reviewer items in, **four real findings out**, including `[VAULTTICK-EMPTYKEY-01]` — a
defect in the Vault guard this phase shipped. The corrected six are in `164.7-REVIEWS.md`. ⭐ Kept
visible rather than silently rewritten: this is the rounding-off a reconstruction is most tempted
into, and it is the reason the verifier landed its blocker on the RECORD and not on the code.

## Deferred / not done

- ⛔ **The `supabase-migrate` plan-job dry-run reading (PATTERNS TRAP A)** — *"does a comment-only
  edit to an applied migration change what the CLI plans?"* No such reading was taken at ship time
  and none is reconstructible now. **Not silently dropped.** It is a live question for Phase
  164.5.1, whose criterion 6 proposes comment edits to two applied migrations — and whose research
  independently recommended annotating *beside* `20260907120000:380` rather than editing it, for
  exactly this reason.
- **Per-reviewer attribution** — unrecoverable; recorded as a limit in `164.7-REVIEWS.md`.

## Incidental fix

`.planning/WINDOWS.md`'s frontmatter carried **two `last_updated` keys**. A YAML parser takes the
last, which was the OLDER timestamp, so the ledger reported a stale value. Collapsed to one and set
to the edit time. One line, noticed while dispositioning entry 25.

## New trap, measured while executing this plan

⛔ **`gh api …/actions/jobs/<id>/logs` REFUSES a log containing terminal escape sequences** — it
exits **1 with EMPTY stdout**: *"the response contains terminal escape sequences; pass
--allow-escape-sequences to output it anyway."* Measured: without the flag `rc=1, bytes=0`; with it,
`rc=0, 681 lines`. This matters because the plan mandates this path over `gh run view --log`
(which truncates), and an empty log read as "no drift" is a false green of the exact shape this
project books. **Always pass `--allow-escape-sequences` and assert a non-zero line count before
concluding anything.**
