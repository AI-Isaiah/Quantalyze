---
phase: 164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p
plan: 04
type: execute
subsystem: sql-gates-mutation-corpus
tags: [red-under, mutation-twins, re-point, fail-closed-arms, ledger-refresh, trap-e]
wave: 2
depends_on: ["164.7-03"]
files_modified:
  - supabase/tests/test_ledger_refresh_fanout.sql
  - supabase/tests/test_ledger_refresh_composite_arm.sql
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-04-NEUTER.log
autonomous: true
requirements: [SC-2, D-01, D-05]
provides:
  - "Both ledger gate files activate the switch through the TABLE (INSERT … ON CONFLICT DO UPDATE inside the rolled-back transaction), abort loud on a COMMITTED TRUE row, extend arm 0 to require the table-backed body, and carry THREE fail-closed arms each: A (missing row ⇒ 0), K (row FALSE ⇒ 0), L (read raises ⇒ 0 without raising)"
  - "All 28 edit-kind RED-UNDER-M twins re-pointed at 20260907130000 with every `occurrences` re-MEASURED; the 2 staleness-view twins untouched; arm A's needle replaced by the new guard text"
  - "Both files driven through the diagnostic runner: every twin RED (identity ok) — 17 per file (arm 0 + A-L, H carrying four twins in the fanout file; the composite file's own roster) — zero no-red"
affects: [164.7-05, 164.7-06]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every edit-kind twin in both files names supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql as its `file`, and `--parse-only` measures every `occurrences` exactly (zero MEASURE_FAIL)"
    - "Arm A now proves the MISSING-ROW case returns 0 (C-03's pinned rejection of the fail-OPEN branch); arm K proves FALSE returns 0; arm L proves a RAISING read returns 0 and does not propagate — each with a twin that reddens it and only it"
    - "The anti-vacuity precondition reads the COMMITTED row and RAISES 'TEST ABORTED' if it is TRUE — it never UPDATEs a committed row on shared TEST"
    - "The sentinel reads `ALL 12 ARMS EXECUTED (A-L)` in both files — count AND roster, which ci.yml's sql-tests read-back requires to agree"
    - "`node scripts/mutation-runner/run.mjs --file <each>` shows one RED (identity ok) per twin — 17 per file after K and L join — the file's first failure naming its own arm, no no-red, no NO-IDENTITY"
  artifacts:
    - path: "supabase/tests/test_ledger_refresh_fanout.sql"
      provides: "apply list + fixture 31 before 20260825130000 and 20260907130000 last; new precondition; table activation; arms A/K/L; 14 re-pointed twins; sentinel A-L; DECLAREs for the new arms"
    - path: "supabase/tests/test_ledger_refresh_composite_arm.sql"
      provides: "the same, 12 re-pointed twins (the 2 staleness-view twins stay on 20260825120000)"
  key_links:
    - from: "RED-UNDER-SETUP apply lists"
      to: "20260907130000 applied LAST"
      via: "the CREATE OR REPLACE overwrites the 20260825 bodies on the lane, which is exactly why the twins must point at the new file"
      pattern: "20260907130000_ledger_refresh_switch_to_system_flags.sql\"\\]"
    - from: "arm A/K/L twins"
      to: "the guard text in 20260907130000 (unique per body because the find spans the function-naming NOTICE/WARNING line)"
      via: "RED-UNDER-M edit with occurrences 1"
      pattern: "IS DISTINCT FROM TRUE THEN"
---

<objective>
Close TRAP E (PATTERNS) / C-02 (CONTEXT): after plan 03, every edit-kind twin in the two ledger gate files mutates a 20260825 file whose body the new migration overwrites later in the apply list — the runner reports `no-red` on arm A of both files and the corpus exits 1. Re-point all 28 (14 fanout + 12 composite; the 2 that target the staleness VIEW migration stay), re-MEASURE every `occurrences` (GRAMMAR rule 2: the needles now live in a file with different surrounding text), replace arm A's needle with the new guard text, move the activation idiom from `set_config` to the table, and add the two fail-closed arms per file that D-01/C-03 require to be PINNED, not argued.

Precedent, followed exactly: `test_sync_status_curated_sentence_survives.sql` (phase 164.2 plan 07) points every twin at the SUPERSEDING migration `20260906120000`, never at the file that first defined the function.

**Why three fail-closed arms and not one:** A (missing row), K (row present and FALSE) and L (the read itself RAISES) are three different failure surfaces of the same guard, and a single mutation cannot redden all three — arm A's twin (`= FALSE`, opens on NULL) leaves K green; K's twin (`IS NULL`, opens on FALSE) leaves A green; L's twin (handler assigns TRUE) leaves A and K green. Three arms, three twins, each isolating one defect, is the runner's identity discipline applied to a truth table.

**Interval, stated:** the full runner still exits 1 after this plan — now for the stale FILES_FLOOR/ARMS_FLOOR ratchets (46 > 45 files; the arm count above 369) — until plan 05 pins. That is growth reported, not regression. Commit bodies carry `FLOOR-INTERVAL:`.

Purpose: SC-2 (the switch's fail-closed contract is machine-proven), D-05's "every gate must be able to fail" for the corpus this phase touches.
Output: the two rewritten gate files and the neuter log.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-CONTEXT.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-RESEARCH.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-PATTERNS.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-03-SUMMARY.md
@supabase/tests/test_ledger_refresh_fanout.sql
@supabase/tests/test_ledger_refresh_composite_arm.sql
@supabase/tests/test_sync_status_curated_sentence_survives.sql
@supabase/tests/test_resync_retry_single_job.sql
@supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
@scripts/mutation-runner/GRAMMAR.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end on the fanout gate — apply list, table activation, arms A/K/L, every edit-kind twin re-pointed, diagnostic run 17/17 RED</name>
  <files>supabase/tests/test_ledger_refresh_fanout.sql</files>
  <read_first>
    - `supabase/tests/test_ledger_refresh_fanout.sql` in full: the header's shared-TEST caveats and the `RED-UNDER-SETUP` line (~line 123), the DECLARE block with the 42601 note (~155-165), arm 0 (~168-180), the anti-vacuity precondition on arm A (~186-192), arm A and its twin (~323-338), the `set_config` activation (~343), every `-- RED-UNDER-M:` line (14 edit-kind + arm 0's `sql` kind, lines listed in the plan-04 objective's measurement), the sentinel (~707), the closing `ROLLBACK;`
    - `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql` — the exact guard text of the FANOUT body (its NOTICE line names `enqueue_ledger_refresh_for_strategies`, its WARNING line too); every other needle (source marker, INTERVAL '20 hours', is_stale, is_composite = FALSE, venue_rank, LIMIT 4, the three ak.* predicates) — grep each `find` string against the NEW file and COUNT
    - `supabase/tests/test_sync_status_curated_sentence_survives.sql` — apply list ending in the superseding migration; twins naming it
    - `supabase/tests/test_resync_retry_single_job.sql` assertion (b) — the BEGIN/EXCEPTION wrap that converts a raw error into `TEST FAILED (x)`; R1 forbids probes inside the handler
    - `scripts/mutation-runner/GRAMMAR.md` rules 1-4 and "Defect kinds the runner reports" (`no-red`, `NO-IDENTITY`, wrong-arm-first)
    - `.github/workflows/ci.yml` sql-tests sentinel read-back — count AND roster
  </read_first>
  <action>
    Apply list: insert `scripts/pg-lane/fixtures/31-fixture-system-flags.sql` immediately before `supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql` and append `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql` LAST. Update the header's discovery note (which fixtures, why 31 precedes the 20260825 file, why the new migration is last).

    DECLAREs: add `v_flag BOOLEAN;` and `v_ret_l INTEGER;` (and whatever arms K/L need) in the DECLARE block with a note under the existing 42601 warning.

    Arm 0: extend the applied-ness probe — after the existence check, `IF pg_get_functiondef('public.enqueue_ledger_refresh_for_strategies()'::regprocedure) !~ 'FROM public\.system_flags' THEN RAISE EXCEPTION 'TEST FAILED (0): … the body on this database still reads the retired app-GUC (migration 20260907130000 not applied here) …'` — cause (iii) in the message, so the shared-TEST red names the un-applied migration precisely. Its existing `sql` DROP twin still bites.

    Precondition (replaces the `current_setting` check): `SELECT enabled INTO v_flag FROM public.system_flags WHERE key = 'ledger_refresh_enabled';` `IF v_flag IS TRUE THEN RAISE EXCEPTION 'TEST ABORTED: the COMMITTED activation row is TRUE on this database, so arms A/K cannot test the dormant cases. Never UPDATE a committed row from a test on a SHARED project — an operator turned it on; find out why.'; END IF;` (`TEST ABORTED` is deliberately not an identity).

    Arm A (rewritten; identity stays `A`): with the candidate published as today, `DELETE FROM public.system_flags WHERE key = 'ledger_refresh_enabled';` (inside this rolled-back transaction — say so), call, assert 0 and zero compute_jobs for `s_a`. RED-UNDER prose: "make the guard open on a MISSING row — the send-intro branch (2) this phase REJECTS", PLUS the sentence that this twin (`= FALSE`) also reddens arm L — under it the handler's NULL (a raising read) falls through exactly like the missing-row NULL — and that the runner's identity discipline holds because A executes BEFORE L, so the file's first failure is A; a future reorder that puts L ahead of A would read as a wrong-arm-first defect on A's twin, and that is expected, not a bug. Twin: `{"arm":"A","apply":[{"kind":"edit","file":"supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql","find":"IF v_enabled IS DISTINCT FROM TRUE THEN\n    RAISE NOTICE 'enqueue_ledger_refresh_for_strategies: dormant","replace":"IF v_enabled = FALSE THEN\n    RAISE NOTICE 'enqueue_ledger_refresh_for_strategies: dormant","occurrences":1}]}` — the multi-line find spans the function-naming NOTICE so it is unique to the fanout body; MEASURE the whitespace (indentation, `\n`) against the real bytes with `--parse-only`.

    Arm K (new): `INSERT INTO public.system_flags (key, enabled) VALUES ('ledger_refresh_enabled', FALSE) ON CONFLICT (key) DO UPDATE SET enabled = FALSE;` call, assert 0 and no job. Twin: same find, replace `IF v_enabled IS NULL THEN\n    RAISE NOTICE …` (opens on FALSE, still closed on NULL — so arm A stays green under it).

    Arm L (new): ⛔ no savepoint-family statement of any spelling — this gate is one `DO $$ … END $$;` block and PL/pgSQL cannot parse transaction-control statements (TODOS `[REDUNDER-SAVEPOINT]`, class closed at exactly 3 migrations in Phase 164.4; `test_enqueue_compute_job_dedupe_non_terminal.sql`'s header records the same 42601). Use the raise-to-rollback idiom: a plpgsql EXCEPTION block is an implicit subtransaction, so `SET LOCAL lock_timeout = '2s';` then ONE nested block — `BEGIN ALTER TABLE public.system_flags RENAME TO system_flags_arm_l; BEGIN v_ret_l := public.enqueue_ledger_refresh_for_strategies(); EXCEPTION WHEN OTHERS THEN RAISE EXCEPTION 'TEST FAILED (L): the fan-out RAISED (SQLSTATE %) when its flag read failed — it must return 0 and stay dormant', SQLSTATE; END; RAISE EXCEPTION USING ERRCODE = 'P0164'; EXCEPTION WHEN SQLSTATE 'P0164' THEN NULL; END;` — the `P0164` raise unwinds the RENAME and releases its ACCESS EXCLUSIVE lock the instant the handler runs (an explicit rename-back would hold the lock to transaction end, which on SHARED TEST blocks other people's CI); `v_ret_l` survives because plpgsql local-variable assignments are not transactional; a `TEST FAILED (L)` raised by the inner block is not `P0164`, so it propagates as the arm's own red instead of being swallowed. Then `IF v_ret_l <> 0 THEN RAISE EXCEPTION 'TEST FAILED (L): the fan-out returned % when its flag read failed, expected 0', v_ret_l; END IF;` — the assertion sits AFTER the block (R1). Note in the arm's prose that on shared TEST the RENAME holds an ACCESS EXCLUSIVE lock on `system_flags` for milliseconds inside the rolled-back subtransaction and that `lock_timeout` makes a wait fail loud instead of wedging. Twin: `{"arm":"L","apply":[{"kind":"edit","file":"…20260907130000…","find":"'enqueue_ledger_refresh_for_strategies: activation flag read failed (SQLSTATE %); treating as dormant', SQLSTATE;\n    v_enabled := NULL;","replace":"'enqueue_ledger_refresh_for_strategies: activation flag read failed (SQLSTATE %); treating as dormant', SQLSTATE;\n    v_enabled := TRUE;","occurrences":1}]}` — under it the read fails, the handler opens the flag, the fan-out proceeds and enqueues (the candidate is still published) ⇒ non-zero ⇒ L reddens; A and K stay green because their reads do not fail.

    Activation for arms B-J: replace `PERFORM set_config(…)` with `INSERT INTO public.system_flags (key, enabled) VALUES ('ledger_refresh_enabled', TRUE) ON CONFLICT (key) DO UPDATE SET enabled = TRUE;` and rewrite the comment ("rolls back with the transaction; never a committed write on shared TEST").

    Re-point the remaining twins (B, E, C, D, F, G1, G2, H, H/inactive, H/revoked, H/disconnected — 11, plus A/K/L above = 14 edit-kind): change every `"file"` to the new migration; keep each `find`/`replace`; re-measure every `occurrences` — several needles (e.g. `INTERVAL '20 hours'`, `AND ak.sync_status IS DISTINCT FROM 'revoked'`) now appear in BOTH bodies of the one file, so either widen the find to a body-unique span or set the MEASURED count and accept that the composite body is mutated too (state which you did per twin in the RED-UNDER prose). `sql`-kind twins (0, I, J) are unchanged.

    Sentinel: `ALL 12 ARMS EXECUTED (A-L)`; update the header's own quotation of the sentinel and the per-lane timing note (re-measure over 3 runs, as the header did).

    Drive: `--parse-only` until zero MEASURE_FAIL; then `node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_fanout.sql > <scratch>/p04-fanout.log` and read: baseline GREEN; 17 × `RED (identity ok)` (one per twin marker — arm 0, A-L, with H's four); the runner's first-failure identity equals the twin's arm for every row; zero `no-red`. Fix at the arm. `node scripts/lint-sql-gates.mjs` clean. Commit with `FLOOR-INTERVAL:`.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && G=supabase/tests/test_ledger_refresh_fanout.sql && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad && rm -f "$S/p04-parse.log" "$S/p04-fanout.log" && test "$(grep -a '^[ \t]*--[ \t]*RED-UNDER-M' "$G" | grep -c '20260825130000_ledger_refresh_fanout_dormant.sql')" = "0" && test "$(node -e 'const t=require("fs").readFileSync(process.argv[1],"utf8");let n=0;for(const l of t.split("\n")){const m=l.match(/^\s*--\s*RED-UNDER-M:\s*(\{.*\})\s*$/);if(!m)continue;for(const s of JSON.parse(m[1]).apply){if(s.kind==="edit"&&s.file==="supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql")n++;}}console.log(n)' "$G")" = "16" && grep -a -c '^[ \t]*--[ \t]*RED-UNDER-M' "$G" | grep -q '^17$' && test "$(grep -a -v '^[[:space:]]*--' "$G" | grep -a -c -E '\bSAVEPOINT\b')" = "0" && grep -a -c "WHEN SQLSTATE 'P0164'" "$G" | grep -q '^1$' && grep -a -c "ALL 12 ARMS EXECUTED (A-L)" "$G" | grep -q '^1$' && test "$(grep -a -c "set_config('app.ledger_refresh_enabled'" "$G")" = "0" && test "$(grep -a -c "current_setting('app.ledger_refresh_enabled'" "$G")" = "0" && grep -a -o 'RED-UNDER-SETUP: .*' "$G" | grep -a -c '31-fixture-system-flags.sql.*20260825130000.*20260907130000_ledger_refresh_switch_to_system_flags.sql"\]' | grep -q '^1$' && grep -a -c 'TEST FAILED (K)' "$G" | grep -v '^0$' && grep -a -c 'TEST FAILED (L)' "$G" | grep -v '^0$' && node scripts/lint-sql-gates.mjs && { node scripts/mutation-runner/run.mjs --parse-only > "$S/p04-parse.log" 2>&1 || true; } && test "$(grep -a -c 'MEASURE_FAIL' "$S/p04-parse.log")" = "0" && { node scripts/mutation-runner/run.mjs --file "$G" > "$S/p04-fanout.log" 2>&1 || true; } && red=$(grep -a -c 'RED (identity ok)' "$S/p04-fanout.log") && echo "red=$red" && test "$red" = "17" && test "$(grep -a -c -E 'no-red|NO-IDENTITY' "$S/p04-fanout.log")" = "0" && git log -1 --format=%B | grep -a -c 'FLOOR-INTERVAL:' | grep -v '^0$'</automated>
  </verify>
  <done>Zero twins name the 20260825 fanout file; the 16 edit STEPS (14 existing + K + L) all name the new migration — counted by parsing each RED-UNDER-M JSON `apply` array, NOT by `grep -c`, because arm E carries 3 edit steps on ONE marker line (a line count reads 14 here; do not split E's marker to make grep agree); 17 twin markers total (15 + K + L); arm L uses the P0164 raise-to-rollback idiom and the file contains no SAVEPOINT token; sentinel A-L; no GUC set_config/current_setting left; apply list ordered fixture-31 → 20260825… → new migration last; parse-only measures cleanly; the diagnostic run shows 17 RED (identity ok) with no no-red. FALSIFIABILITY (revision 2, N1): one `&&` chain, `p04-parse.log`/`p04-fanout.log` `rm -f`'d first — if parse-only prints MEASURE_FAIL, the `= "0"` test fails and the chain STOPS; `--file` never runs and there is no log left for `red=` to read. Previously the `;` skipped `--file` and `red` was read from the stale `p04-fanout.log` the action's own iterate loop had written, passing at 17. Likewise any failing assertion before the runner (16 steps, 17 markers, SAVEPOINT-zero, sentinel) now fails the block instead of being masked by the tail.</done>
</task>

<task type="auto">
  <name>Task 2: The composite gate — same surgery, 12 edit steps re-pointed, 2 view twins untouched, arms A/K/L, 17/17 RED</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql</files>
  <read_first>
    - `supabase/tests/test_ledger_refresh_composite_arm.sql` in full — apply list (~130), DECLAREs, arm 0 (~178), precondition (~192), arm A + twin (~351), activation (~365), the twins for `D/precondition` and `E1/precondition` that target `20260825120000_ledger_refresh_staleness_view.sql` (these STAY — the view is not superseded), sentinel (~763)
    - The composite body's guard text in `20260907130000` (NOTICE/WARNING prefixed `enqueue_ledger_composite_refresh:`)
    - Task 1's finished fanout file — mirror its shapes, including the per-twin `occurrences` decisions
  </read_first>
  <action>
    Repeat Task 1 on the composite file with the composite body's identifiers: apply list (fixture 31 before `20260825130000`, new migration last); arm 0 extended to require `FROM public\.system_flags` in `pg_get_functiondef('public.enqueue_ledger_composite_refresh()'::regprocedure)`; precondition on the committed row; arm A rewritten to the missing-row case with the twin's multi-line find spanning `RAISE NOTICE 'enqueue_ledger_composite_refresh: dormant`; arms K and L with their twins (L's find spans the composite WARNING text); table activation; the 12 composite-body twins re-pointed with re-measured `occurrences`; the 2 staleness-view twins left byte-identical (assert this in the SUMMARY by diffing those two lines against HEAD~1); sentinel `ALL 12 ARMS EXECUTED (A-L)`; header notes updated. Drive with `--parse-only` and the `--file` diagnostic to 17 × RED (identity ok) — one per twin marker, arm 0 included — zero no-red. `node scripts/lint-sql-gates.mjs` clean. Commit with `FLOOR-INTERVAL:`.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && G=supabase/tests/test_ledger_refresh_composite_arm.sql && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad && rm -f "$S/p04-parse2.log" "$S/p04-composite.log" && test "$(grep -a '^[ \t]*--[ \t]*RED-UNDER-M' "$G" | grep -c '20260825140000_ledger_refresh_composite_arm.sql')" = "0" && test "$(node -e 'const t=require("fs").readFileSync(process.argv[1],"utf8");const c={};for(const l of t.split("\n")){const m=l.match(/^\s*--\s*RED-UNDER-M:\s*(\{.*\})\s*$/);if(!m)continue;for(const s of JSON.parse(m[1]).apply){if(s.kind==="edit")c[s.file]=(c[s.file]||0)+1;}}console.log((c["supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql"]||0)+"/"+(c["supabase/migrations/20260825120000_ledger_refresh_staleness_view.sql"]||0))' "$G")" = "14/2" && test "$(grep -a -v '^[[:space:]]*--' "$G" | grep -a -c -E '\bSAVEPOINT\b')" = "0" && grep -a -c "WHEN SQLSTATE 'P0164'" "$G" | grep -q '^1$' && grep -a -c "ALL 12 ARMS EXECUTED (A-L)" "$G" | grep -q '^1$' && test "$(grep -a -c "set_config('app.ledger_refresh_enabled'" "$G")" = "0" && test "$(grep -a -c "current_setting('app.ledger_refresh_enabled'" "$G")" = "0" && grep -a -o 'RED-UNDER-SETUP: .*' "$G" | grep -a -c '31-fixture-system-flags.sql.*20260907130000_ledger_refresh_switch_to_system_flags.sql"\]' | grep -q '^1$' && node scripts/lint-sql-gates.mjs && { node scripts/mutation-runner/run.mjs --parse-only > "$S/p04-parse2.log" 2>&1 || true; } && test "$(grep -a -c 'MEASURE_FAIL' "$S/p04-parse2.log")" = "0" && { node scripts/mutation-runner/run.mjs --file "$G" > "$S/p04-composite.log" 2>&1 || true; } && red=$(grep -a -c 'RED (identity ok)' "$S/p04-composite.log") && echo "red=$red" && test "$red" = "17" && grep -a -c '^[ \t]*--[ \t]*RED-UNDER-M' "$G" | grep -q '^17$' && test "$(grep -a -c -E 'no-red|NO-IDENTITY' "$S/p04-composite.log")" = "0"</automated>
  </verify>
  <done>Zero twins name the 20260825 composite file; 14 edit STEPS name the new migration (the 12 existing + K + L — counted by JSON parse of the `apply` arrays, because arm F carries 3 steps on ONE marker line and a `grep -c` reads 12; the SUMMARY carries the per-arm map) and exactly 2 steps still name the staleness view; arm L uses the P0164 raise-to-rollback idiom, no SAVEPOINT token; the 2 view twins are untouched; 17 twin markers; sentinel A-L; diagnostic 17 RED (identity ok). FALSIFIABILITY (revision 2, N1): one `&&` chain, `p04-parse2.log`/`p04-composite.log` `rm -f`'d first — if the `14/2` JSON-parse pin or the SAVEPOINT-zero check fails, the chain STOPS there instead of being masked by the tail; if parse-only prints MEASURE_FAIL, `--file` never runs and `red=` has no stale `p04-composite.log` to read. Previously the `;` let both pass.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter the three new arms in each file, observe the runner catch each, restore by hash</name>
  <files>.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-04-NEUTER.log</files>
  <read_first>
    - Plan 01 Task 3's protocol; `scripts/mutation-runner/GRAMMAR.md` "Neuter semantics" (what the runner does when an arm's own RAISE is neutered — the identity/first-failure machinery)
  </read_first>
  <action>
    For each gate file: byte backup + sha256; (N1) neuter arm K's `TEST FAILED (K)` raise into a NOTICE → the `--file` run must report K as not biting (record the defect kind and line); (N2) delete arm L's `-- RED-UNDER-M:` line → `--parse-only` must report the section without a twin; (N3) revert the precondition to a `NOTICE` instead of `TEST ABORTED` and seed a committed-looking TRUE row via a `sql`-step in a throwaway copy of the twin → arm A must go RED for the wrong reason (this proves the precondition guards A's meaning; record the observed line). Restore after each by `cp`, sha256 equal, `git status --porcelain` clean. Write `164.7-04-NEUTER.log` (both files, six blocks, hashes), ending `RESTORE VERIFIED: sha256 equal x6; git status clean`. Commit with `FLOOR-INTERVAL:`.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && L=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-04-NEUTER.log && test -f "$L" && grep -a -c -E '^(BEFORE|AFTER) sha256: [0-9a-f]{64}' "$L" | grep -q '^12$' && grep -a 'RESTORE VERIFIED: sha256 equal x6; git status clean' "$L" && grep -a -c 'TEST FAILED (K)' "$L" | grep -v '^0$' && test "$(grep -a -c 'git checkout --' "$L")" = "0" && git status --porcelain supabase/tests/test_ledger_refresh_fanout.sql supabase/tests/test_ledger_refresh_composite_arm.sql | wc -l | grep -q '^ *0$' && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^coverage: files 4[56]/7[23]'</automated>
  </verify>
  <done>Six neuters observed caught (three per file), six hash-verified restores, both gate files clean in git, parse-only still measures the corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate transaction → shared TEST | every write the gate makes must roll back; a committed operator row must be read, never overwritten |
| twin → production migration text | a twin edits the file that auto-applies to PROD on merge; it runs only on the throwaway lane and is restored by the runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.7-15 | Tampering | a test UPDATE persisting on shared TEST | high | mitigate | all writes inside the rolled-back DO transaction; precondition ABORTS on a committed TRUE row instead of flipping it |
| T-164.7-16 | Denial of Service | arm L's RENAME holding a lock on shared TEST | low | mitigate | `SET LOCAL lock_timeout = '2s'`; the RENAME sits in a nested BEGIN/EXCEPTION subtransaction unwound by a `P0164` raise, so the ACCESS EXCLUSIVE lock is released in milliseconds (no SAVEPOINT — PL/pgSQL cannot parse it); fails loud on contention |
| T-164.7-17 | Repudiation | a twin that cannot redden (no-red) | high | mitigate | re-pointed at the superseding file; diagnostic 17/17 RED per file (fanout: 16 edit steps to the new migration; composite: 14 to the new migration + 2 to the staleness view; 17 markers each); Task 3 neuters |
| T-164.7-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Both `--file` diagnostics: 17 RED (identity ok) each, zero no-red/NO-IDENTITY.
- `--parse-only`: zero MEASURE_FAIL; every twin `file` is the new migration except the 2 view twins.
- `node scripts/lint-sql-gates.mjs` clean.
</verification>

<success_criteria>
- The corpus no longer contains a twin that mutates a dead body.
- Missing row, FALSE row, and a raising read are each pinned to "returns 0" by an arm a machine has seen fail.
</success_criteria>

<output>
Create `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-04-SUMMARY.md` when done, with a per-arm table: arm → twin file → find (first 40 chars) → measured occurrences → RED observed.
</output>
