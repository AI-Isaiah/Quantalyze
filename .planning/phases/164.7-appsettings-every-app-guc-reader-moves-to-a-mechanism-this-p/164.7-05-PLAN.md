---
phase: 164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p
plan: 05
type: execute
subsystem: gates-ratchets-and-docs
tags: [floors, ratchet, lineage-headers, allowlist, runbook, criterion-1, criterion-5, d-04, d-05, currency]
wave: 3
depends_on: ["164.7-01", "164.7-02", "164.7-03", "164.7-04"]
files_modified:
  - scripts/mutation-runner/run.mjs
  - .github/workflows/ci.yml   # CONDITIONAL — Task 1's timeout rule: only if the pinned-value run's wall clock reached ~600 s (15 → 20 ONCE); record the reading either way
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - supabase/migrations/20260407164606_perfect_match.sql
  - supabase/migrations/20260408113029_cron_heartbeat.sql
  - supabase/migrations/20260408215026_schedule_match_cron_hourly.sql
  - supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql
  - supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql
  - scripts/lint-app-guc.mjs
  - src/__tests__/lint-app-guc.test.ts
  - docs/runbooks/ledger-refresh-go-live.md
  - docs/runbooks/match-engine.md
  - CLAUDE.md
  - TODOS.md
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-FLOORS.log
autonomous: true
requirements: [SC-1, SC-2, SC-5, D-04, D-05]
provides:
  - "FILES_FLOOR and ARMS_FLOOR pinned at the MEASURED separation point of the finished corpus (46 files; arms read off the run), WAIVED_CEILING still 0, dated RE-DERIVED blocks beside each, parser-test name pin at 46"
  - "Five dated lineage headers (D-04 for admin_email; D-05 for the four analytics_service_* / ledger files) with EXACT occurrence counts 1/5/4/1/1 and successor clauses naming the 164.7 migrations, mirrored in LINEAGE_ALLOWLIST — corpus scan 0 findings, 5 annotated files printed by name"
  - "docs/runbooks/ledger-refresh-go-live.md corrected at Step 1a-1d, Rollback FAST, the pointer paragraph and OQ-3 (closed by the 2026-09-05 measurement), plus the TWO-op activation and the manifest re-capture; docs/runbooks/match-engine.md item 1 corrected (same class)"
  - "CLAUDE.md currency paragraph for the 46-file corpus; TODOS entries for the expected shared-TEST reds and the roll-forward items"
affects: [164.7-06, 164.7-07, 164.5]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`node scripts/mutation-runner/run.mjs` (full corpus) exits 0 at this plan's final commit and prints `coverage: files 46/73`, `arms: N/N/0` with the two tallies agreeing and `✅ No defects` — and the floors were separated in BOTH directions on real full-corpus runs before being pinned"
    - "`node scripts/lint-app-guc.mjs` exits 0, prints `app-guc: findings 0` and names the five annotated files; `--self-test` still fires every kind"
    - "No executable statement of any applied migration changed: `git diff` on the five annotated files shows ONLY added `--` lines within the first 40 lines, and `npm run schema:functions:check` is clean (a header above the dollar tags does not change a body)"
    - "The runbook no longer instructs any statement that returns 42501: `grep -n 'ALTER DATABASE\\|ALTER ROLE' docs/runbooks/ledger-refresh-go-live.md` returns only lines inside the dated measurement note"
    - "The runbook says activation is TWO live ops (schedule registration is not on PROD: manifest has 14 jobs, none of them) and that the cron manifest must be re-captured afterwards or the cron-drift arm trips"
  artifacts:
    - path: "scripts/mutation-runner/run.mjs"
      provides: "FILES_FLOOR = 46; ARMS_FLOOR = <measured>; RE-DERIVED blocks (VALUE, DATE+HEAD, COMMAND with printed lines, SAMPLE SIZE, SEPARATION both directions, RECORD)"
    - path: "scripts/lint-app-guc.mjs"
      provides: "LINEAGE_ALLOWLIST with five entries {file, occurrences, successor, reason}"
    - path: "docs/runbooks/ledger-refresh-go-live.md"
      provides: "Step 1 rewritten to the system_flags UPDATE with same-session verify; 1b/1c deleted with a dated correction note; 1d records the 2026-09-05 table (OQ-3 closed); Step 2 gains the 'NOT registered on PROD' measurement and the manifest re-capture step; Rollback FAST rewritten; pointer paragraph names 20260907130000; P3-C carries the #747 note"
  key_links:
    - from: "LINEAGE_ALLOWLIST entries"
      to: "the APP-GUC-LINEAGE header in each of the five files"
      via: "file + occurrences + successor must agree in both places or the gate fires"
      pattern: "APP-GUC-LINEAGE: retired 2026-09-07"
    - from: "the two ledger files' headers"
      to: "20260907130000_ledger_refresh_switch_to_system_flags.sql"
      via: "successor clause; the successor must contain zero app-GUC reads (checked by the gate)"
      pattern: "successor: 20260907130000"
---

<objective>
Pin what the earlier plans measured, annotate what D-04/D-05 decided, and correct the documents whose privilege claims were falsified on PROD (criterion 5).

**Floors (TRAP F, CLAUDE.md procedure):** with plans 02 and 04 landed, the annotated corpus is 46 files and the arm count is whatever the run prints — expected 369 + 7 (new gate) + 4 (K and L in two files) = 380, but the plan does not pin a projection; it pins what `node scripts/mutation-runner/run.mjs` prints on the FINAL tree, after separating each floor by one in both directions on real full-corpus lane runs (`+1` ⇒ exactly the named regression, exit 1; pinned value ⇒ `✅ No defects`, exit 0). `WAIVED_CEILING` stays 0 — nothing in this phase is waived.

**Lineage headers (D-04 for `app.admin_email`, D-05 for the rest; ROADMAP criterion 2's SCOPE AMENDMENT 2026-09-07 already carries the reasoning):** the executable text of an applied migration is the permanent record of what ran on PROD and is not edited. A single `--` header line in the file's first 40 lines, parsed by `scripts/lint-app-guc.mjs`, carries the retirement date, the EXACT count the gate measures, the successor (or `none`), and the evidence — and the script's `LINEAGE_ALLOWLIST` pins the same five files and counts. Two edits in two places, both count-pinned. The `20260408113029` line-113 comment is one of the five occurrences of that file, by design: the gate counts comments.

**The runbook (criterion 5):** Steps 1a/1b/1c instruct `ALTER DATABASE … SET` / `ALTER ROLE … SET`, both `42501` on PROD (ROADMAP table, measured 2026-09-05 in the SQL editor as `postgres`); 1b/1c's "verify in a NEW session" is moot for a table read; 1d's "record which form worked" is OQ-3, closed by that measurement; Rollback FAST has the same 42501 and is the more dangerous half. Beyond correcting those, RESEARCH Q7 found two things the runbook does not say: the `ledger_refresh_fanout` schedule is NOT registered on PROD (`scripts/prod-prober/cron-manifest.json` holds 14 jobnames, none of them — activation is TWO live ops), and registering it makes the 164.1 `cron-drift` arm report an extra job until the manifest is re-captured with `node scripts/prod-prober/run.mjs --capture-manifest --out <path>` and committed. `docs/runbooks/match-engine.md` item 1 carries the identical falsified claim for `app.admin_email` — same class, fixed in the same commit.

Purpose: SC-1 closes (gate green on the annotated tree), SC-2's D-04 disposition lands, SC-5 lands, the corpus ratchets pin.
Output: floors + pins, five headers + allowlist, two runbooks, CLAUDE.md/TODOS currency, the floors log.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-CONTEXT.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-RESEARCH.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-PATTERNS.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-01-SUMMARY.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-02-SUMMARY.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-04-SUMMARY.md
@scripts/mutation-runner/run.mjs
@src/__tests__/mutation-annotation-parser.test.ts
@src/__tests__/mutation-runner-floors.test.ts
@scripts/lint-app-guc.mjs
@docs/runbooks/ledger-refresh-go-live.md
@docs/runbooks/match-engine.md
@CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Full-corpus run on the finished tree — separate both floors in both directions, then pin and re-derive</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/mutation-runner-floors.test.ts, .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-FLOORS.log</files>
  <read_first>
    - `scripts/mutation-runner/run.mjs` the dated blocks immediately above `export const FILES_FLOOR = 45;` and `export const ARMS_FLOOR = 369;` (the 164.2 plan-07 RE-DERIVED block is the template: VALUE / DATE+HEAD / COMMAND with the printed lines / SAMPLE SIZE / SEPARATION / RECORD), and `export const WAIVED_CEILING = 0;`
    - `src/__tests__/mutation-annotation-parser.test.ts` the `expect(corpus.filesAnnotated).toBe(45)` line and the `annotatedFiles` name list containing `"test_sync_status_curated_sentence_survives.sql"`, plus the `toHaveLength(45)` pins at the structured/prose counts
    - `src/__tests__/mutation-runner-floors.test.ts` header (annotated < floor = regression; annotated > floor = stale ratchet) — both directions are defects
    - CLAUDE.md "SQL gate integrity jobs" 164.2 currency paragraph — the procedure ("SEPARATED in both directions on real full-corpus lane runs before being pinned") and the `timeout-minutes` derivation (raise 15 → 20 ONCE when ~10 min is crossed; 451 legs ran in 567 s)
    - Memory `reference_agent_worktrees_exhaust_disk` — prune worktrees before a full run; and `reference_full_vitest_must_not_share_the_box` — nothing else runs while the corpus runs
  </read_first>
  <action>
    Confirm `git status --porcelain` is clean (the runner refuses a dirty tree) and no other lane or vitest is running. Run the FULL corpus: `node scripts/mutation-runner/run.mjs > <scratch>/p05-full-1.log 2>&1; echo exit=$?`. Expected on the unpinned tree: exit 1 with EXACTLY the stale-ratchet defects for FILES_FLOOR (46 > 45) and ARMS_FLOOR (N > 369) and nothing else — any OTHER defect kind (`no-red`, `NO-IDENTITY`, `lane-blocked-stale`, tallies disagreeing) is a regression from plans 02-04 and is fixed THERE before continuing. Read off the run's own lines: `coverage: files 46/73`, `arms: N/N/0`, `biting: N`, `lane-invocations: N` (must equal), `per-arm lane time`, wall clock.

    Separation, on real full-corpus runs (four runs total; budget ~10 min each at ~1.1 s/arm plus 46 baseline + 46 restore legs): (S1) set `FILES_FLOOR = 47` and `ARMS_FLOOR = N+1` → run → exactly two `floor` defects naming both regressions, exit 1; (S2) set `FILES_FLOOR = 46` and `ARMS_FLOOR = N` → run → `✅ No defects`, exit 0. (The stale-ratchet direction was already observed in run 1 at 45/369.) Record all three runs' printed lines and exit codes in `164.7-05-FLOORS.log` with date, HEAD sha, host, and the wall clock of each.

    Pin: `FILES_FLOOR = 46`, `ARMS_FLOOR = N`, `WAIVED_CEILING` unchanged at 0. Append a `⭐ RE-DERIVED 2026-09-07 BY MEASUREMENT (phase 164.7, plan 05)` block above EACH constant in the house shape: which files/arms joined (the new gate's 7 identities by name; K and L in each ledger file; the 28 re-pointed twins re-observed biting AFTER the re-point; arm A's needle replaced), the corpus growth 72 → 73, the class arithmetic `46 + 0 lane-blocked + 27 unreachable + 0 pending = 73`, the printed lines verbatim, the SEPARATION lines, RECORD = this plan's SUMMARY path. Note that every earlier block stays as dated lineage.

    Update the parser test: `filesAnnotated` 45 → 46; add `"test_analytics_service_settings_and_vault_tick.sql"` to the name list in sorted position; every `toHaveLength(45)` that counts annotated files → 46 (read each one's subject first — some count OTHER things; change only the ones that count annotated files, and say which in the commit). Update `mutation-runner-floors.test.ts` only if it pins a literal (measure by grep; it may read the exports). Run `npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts` → green.

    Timeout rule: if the pinned-value run's wall clock reached ~10 min (600 s), raise `sql-mutation`'s `timeout-minutes` 15 → 20 ONCE per the derivation beside it in ci.yml and record the reading; if it did not, leave 15 and record the reading. Either way the log carries the number.

    Commit (this commit ENDS the `FLOOR-INTERVAL` — say so in the body; the `APP-GUC-INTERVAL` ends at Task 2).
  </action>
  <verify>
    <automated>cd . && node -e 'import("./scripts/mutation-runner/run.mjs").then(m=>{console.log(m.FILES_FLOOR,m.ARMS_FLOOR,m.WAIVED_CEILING);if(m.FILES_FLOOR!==46||m.ARMS_FLOOR<=369||m.WAIVED_CEILING!==0)process.exit(1)})' && grep -a -c 'RE-DERIVED 2026-09-07 BY MEASUREMENT (phase 164.7, plan 05)' scripts/mutation-runner/run.mjs | grep -q '^2$' && grep -a -c 'expect(corpus.filesAnnotated).toBe(46)' src/__tests__/mutation-annotation-parser.test.ts | grep -q '^1$' && grep -a -c '"test_analytics_service_settings_and_vault_tick.sql"' src/__tests__/mutation-annotation-parser.test.ts | grep -v '^0$' && npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts 2>&1 | grep -a -E 'Test Files .* passed' && L=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-FLOORS.log && test -f "$L" && grep -a -c 'FILES_FLOOR regression: 46 annotated file(s) < floor 47' "$L" | grep -v '^0$' && grep -a -c 'ARMS_FLOOR regression' "$L" | grep -v '^0$' && grep -a -c 'No defects' "$L" | grep -v '^0$' && grep -a -c -E 'coverage: files 46/73' "$L" | grep -v '^0$' && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -E '^coverage: files 46/73' && node scripts/mutation-runner/run.mjs --self-test 2>&1 | grep -a 'SELF-TEST PASSED'</automated>
  </verify>
  <done>Floors pinned at the measured separation point with dated derivation blocks; parser pins at 46 with the new name; the floors log records the stale run, the +1 run (both regressions named) and the pinned run (no defects); self-test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Five lineage headers + the allowlist — the app-GUC gate goes from 12 findings to 0 by annotation, never by loosening</name>
  <files>supabase/migrations/20260407164606_perfect_match.sql, supabase/migrations/20260408113029_cron_heartbeat.sql, supabase/migrations/20260408215026_schedule_match_cron_hourly.sql, supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql, supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql, scripts/lint-app-guc.mjs, src/__tests__/lint-app-guc.test.ts</files>
  <read_first>
    - `scripts/lint-app-guc.mjs` (plan 01) — `parseLineageHeader` grammar, `LINEAGE_ALLOWLIST` entry shape, `HEADER_SCAN_LINES`, the successor zero-check
    - CONTEXT `<code_context>` "app.admin_email is NOT a live reader — it is history" and "CORRECTIONS" C-01 + "C-01 IS ITSELF CORRECTED" — the evidence each header must carry, in ONE sentence each
    - `supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql` D-01 header section, the "(An earlier revision of this comment claimed …)" parenthetical — the house shape for a dated in-place correction
    - PATTERNS TRAP A — `supabase-migrate.yml`'s `plan` job runs `supabase db push --include-all --dry-run` on the PR; whether editing an APPLIED file's comment changes anything the CLI cares about is UNVERIFIED here and is READ off that job's output in plan 06, not assumed
    - RESEARCH § Q3 last paragraph — a `--` header at the TOP of the ledger files sits outside the dollar tags, so `dump-sql-functions.ts` output and the twins' `find` strings are unaffected
  </read_first>
  <action>
    Add ONE header line (plus at most three `--` context lines directly beneath it, none of which may contain the app-GUC call) within the first 40 lines of each file, dated `2026-09-07`:
    - `20260407164606_perfect_match.sql`: `occurrences: 1; successor: none;` reason: one-shot DO block, ran once at apply 2026-04-07, `v_admin_email` read at line 31 has no second definition anywhere (`grep -rln 'perfect_match\|v_admin_email' supabase/migrations/` returns this file), nothing reads it — D-04 SCOPE AMENDMENT (ROADMAP criterion 2).
    - `20260408113029_cron_heartbeat.sql`: `occurrences: 5; successor: 20260907120000_analytics_service_settings_and_vault_tick.sql;` reason: two preflight reads inside a one-shot DO block + two reads baked into a cron command literal this file WROTE (superseded by 20260408215026, same jobname) + one mention in a `--` comment at line 113 which the gate counts because a grep cannot tell a comment from a statement; the mechanism PROD's jobid 1 actually runs (Vault) is described by the successor. State in the header that a FRESH apply of this file would still write the GUC-reading command — a latent-on-rebuild defect, NOT an ongoing outage — in exactly those words.
    - `20260408215026_schedule_match_cron_hourly.sql`: `occurrences: 4; successor: 20260907120000_…;` same reasoning; note it supersedes 20260408113029 and is itself superseded on PROD by the 2026-09-01 hand repair recorded in `scripts/prod-prober/cron-manifest.json` jobid 1; the live-row repoint is Phase 164.5 item 7.
    - `20260825130000_ledger_refresh_fanout_dormant.sql` and `20260825140000_ledger_refresh_composite_arm.sql`: `occurrences: 1; successor: 20260907130000_ledger_refresh_switch_to_system_flags.sql;` reason: Lock B re-based onto `public.system_flags` because the GUC could not be set here (42501 on PROD, measured 2026-09-05); this file remains the record of what applied on 2026-08-25.
    Place each header ABOVE any dollar-quoted body; for the two ledger files confirm `npm run schema:functions:check` is still clean afterwards and that plan 04's twins still measure (`--parse-only` zero MEASURE_FAIL).

    Fill `LINEAGE_ALLOWLIST` in `scripts/lint-app-guc.mjs` with the five entries, counts 1/5/4/1/1, successors as above, one-line reasons. Run `node scripts/lint-app-guc.mjs` → exit 0, `app-guc: findings 0`, `app-guc: annotated (lineage) 5 — <the five names>`. Run `--self-test` → still OK. Rewrite vitest arm (g) from "12 findings / 5 files" to "0 findings AND `annotated` is exactly the five names AND each allowlist entry's count equals `countReads` of its file" (read the real files — this is the pin that fails when someone adds a sixth read anywhere or removes one); keep a comment saying the 12/5 reading was the dated interim of plan 01. Add arm (h): temporarily remove one allowlist entry in-memory and assert exactly one `header-not-allowlisted`; add arm (i): append one app-GUC read to a temp copy of an annotated file scanned with the real allowlist and assert exactly one `header-count-mismatch`.

    Verify the diff discipline: `git diff --numstat` on the five migration files shows only additions, and `git diff` shows only lines beginning with `--`. Commit — this ends the `APP-GUC-INTERVAL`; say so in the body.
  </action>
  <verify>
    <automated>cd . && node scripts/lint-app-guc.mjs --self-test 2>&1 | grep -a 'self-test OK' && node scripts/lint-app-guc.mjs > /private/tmp/claude-501/-Users-<user>-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p05-guc.log 2>&1 && grep -a 'app-guc: findings 0' /private/tmp/claude-501/-Users-<user>-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p05-guc.log && grep -a 'app-guc: annotated (lineage) 5' /private/tmp/claude-501/-Users-<user>-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p05-guc.log && for f in supabase/migrations/20260407164606_perfect_match.sql supabase/migrations/20260408113029_cron_heartbeat.sql supabase/migrations/20260408215026_schedule_match_cron_hourly.sql supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql; do head -40 "$f" | grep -a -c '^[ \t]*-- APP-GUC-LINEAGE: retired 2026-09-07;' | grep -q '^1$' || exit 1; test "$(git diff origin/main -- "$f" | grep -a '^[-+]' | grep -a -v '^+++\|^---' | grep -a -v -c '^+--')" = "0" || { echo "non-comment change in $f"; exit 1; }; done && grep -a -c 'occurrences: 5; successor: 20260907120000_analytics_service_settings_and_vault_tick.sql' supabase/migrations/20260408113029_cron_heartbeat.sql | grep -q '^1$' && node -e 'import("./scripts/lint-app-guc.mjs").then(m=>{if(m.LINEAGE_ALLOWLIST.length!==5)process.exit(1);const s=m.LINEAGE_ALLOWLIST.reduce((a,e)=>a+e.occurrences,0);if(s!==12)process.exit(2);console.log("allowlist 5 entries, 12 occurrences")})' && npm run -s schema:functions:check && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -c 'MEASURE_FAIL' | grep -q '^0$' && npx vitest run src/__tests__/lint-app-guc.test.ts 2>&1 | grep -a -E 'Tests .* passed' && git log -1 --format=%B | grep -a -c 'APP-GUC-INTERVAL' | grep -v '^0$'</automated>
  </verify>
  <done>Five headers, each within 40 lines and comment-only (diff against origin/main is all `+--` lines); allowlist of five summing to 12; the gate reports 0 findings and 5 annotated files by name; self-test still fires; snapshot check and twin measurement unaffected; vitest pins the annotated state with two new anti-vacuity arms.</done>
</task>

<task type="auto">
  <name>Task 3: The runbooks whose privilege claims were falsified, plus CLAUDE.md and TODOS currency</name>
  <files>docs/runbooks/ledger-refresh-go-live.md, docs/runbooks/match-engine.md, CLAUDE.md, TODOS.md</files>
  <read_first>
    - `docs/runbooks/ledger-refresh-go-live.md` sections: the pointer paragraph under "What this activates" (names `20260825130000` and says "this pointer must move with it"), `## Step 1` (intro sentence, 1a, 1b, 1c, 1d), `## Step 2` (the schedule statement and "This statement lives HERE and NEVER in a migration"), `### P3 — venue enable flags` Part C (mt5 jobs `done` within 3 days), `## Rollback, part 1 of 2` FAST subsection, and the `### ⚠️ What a mis-ordered activation actually costs — CORRECTED by measurement` section (the house shape for an in-place correction: quote the falsified sentence, state the measurement with date and place, enumerate evidence, say why corrected in place)
    - `docs/runbooks/match-engine.md` line 20 (`ALTER DATABASE postgres SET app.admin_email = …`)
    - ROADMAP Phase 164.7 header table (the three 42501/✅ rows) — the measurement to cite verbatim
    - RESEARCH § Q7 — the written two-op procedure with the which-database marker first, the P4 BEFORE census, 2a/2b/2c/2d, and step 3's manifest re-capture; RESEARCH's `[ASSUMED — A1]` note (SECDEF owned by postgres reads Vault — for the match-engine runbook only)
    - CLAUDE.md "SQL gate integrity jobs" — the 164.2 currency paragraph (shape and the ⚠️ DATED-readings caveat) and `## Which database am I on?`
    - TODOS.md `[164.2-TEST-APPLY-PROVENANCE]` entry — the shape for naming an expected shared-TEST red BY ARM with ordered remedy steps
    - Memory `feedback_scope_amendment_touches_more_than_one_file` — D-04 already sits in ROADMAP criterion 2 and CONTEXT; the SUMMARY is the third place (plan 07 writes it)
  </read_first>
  <action>
    `docs/runbooks/ledger-refresh-go-live.md`, in the runbook's own correction voice:
    1. Pointer paragraph: the body now lives in `20260907130000_ledger_refresh_switch_to_system_flags.sql` (both arms); the 20260825 files are lineage; the table under "What this activates" gains that row.
    2. Step 1 intro: the guard now reads `public.system_flags` key `ledger_refresh_enabled` and opens ONLY on `enabled = TRUE`; a missing row, a FALSE row and a failed read are all dormant (cite the migration's Lock B by symbol). Replace 1a with the `UPDATE public.system_flags SET enabled = TRUE, updated_at = now() WHERE key = 'ledger_refresh_enabled';` and its expected `UPDATE 1` (UPDATE 0 ⇒ the seed row is missing ⇒ STOP, do not INSERT under incident pressure). Replace 1b with a same-session `SELECT key, enabled, updated_at …` (a table read is not session-cached — say why the old "new session" rule no longer applies). DELETE 1c (role-level fallback) and its pg_cron-username trap, replacing both with a dated correction block quoting the falsified statements and the ROADMAP measurement table (2026-09-05, SQL editor as `postgres`: both ALTER forms `42501`, session `SET` ✅ — which is why a session GUC can never be an out-of-band switch). Rewrite 1d: OQ-3 is CLOSED by that measurement; the execution record now records the `system_flags` row's `updated_at` and `updated_by` instead of "which form worked".
    3. Pre-flight: add to P2 the `guard_is_table_backed` probe (`pg_get_functiondef(...) ~ 'system_flags'`) and the seed-row check (`enabled = false` expected), from RESEARCH Q7 step 1; add the which-database marker as P0 (from CLAUDE.md) if the runbook does not already open with it. In P3-C add a dated note: issue #747 (MT5 `-6 authorization failed`, 2026-09-06) was CLOSED the same day by founder VNC re-login and prober run 34027776532 read `initialize=true` — but P3-C is a BEHAVIOURAL probe and must be re-run at activation time regardless of the issue's state; a recurrence blocks criterion 3 by the runbook's own ABORT rule.
    4. Step 2: state the MEASUREMENT that the schedule is NOT registered on PROD (manifest, 14 jobnames, none is `ledger_refresh_fanout` — cite the file and the 161.1-VERIFICATION reading of 2026-08-28), so activation is TWO live ops; keep the statement and the "NEVER in a migration" rule; add step 2e: re-capture the cron manifest (`node scripts/prod-prober/run.mjs --capture-manifest --out <path>`, review, commit) IMMEDIATELY after registering, because the 164.1 `cron-drift` arm compares jobname sets and will report the new job as drift until then.
    5. Observation (criterion 3): keep the existing "check the view, not the job" text and add RESEARCH Q7's 2c queries; criterion 4: add 2d (flip FALSE, next tick with the schedule STILL registered enqueues 0, `count(*) … created_at > <flip timestamp>` = 0).
    6. Rollback FAST: `UPDATE public.system_flags SET enabled = FALSE, updated_at = now() WHERE key = 'ledger_refresh_enabled';` — `UPDATE 1`; verify with a SELECT in the same session; "confirm the next tick enqueues nothing" stays; delete the role-level reset sentence with a one-line dated note.
    Nowhere in the runbook may a statement that returns 42501 remain outside the dated correction block.

    `docs/runbooks/match-engine.md` item 1: replace the `ALTER DATABASE … app.admin_email` instruction with a dated note that the statement returns 42501 on this platform, that `app.admin_email` was read once by a one-shot DO block in `20260407164606_perfect_match.sql` and has no live reader (D-04, ROADMAP criterion 2 amendment), and that the match-engine cron's mechanism is now Vault + `system_settings` via `public.match_engine_cron_tick()` (`20260907120000`), with the live-row repoint owned by Phase 164.5 item 7 and the `[ASSUMED — A1]` Vault-privilege note carried forward.

    CLAUDE.md "SQL gate integrity jobs": append a `⭐ CURRENCY 2026-09-07 (Phase 164.7 APPSETTINGS plan 05)` paragraph in the 164.2 paragraph's shape: the corpus grew by one gate file (`test_analytics_service_settings_and_vault_tick.sql`, 7 arms) and by 4 arms in the two ledger files (K, L each), 28 twins re-pointed at the superseding migration, the printed lines off the pinned run (`coverage: files 46/73`, `arms: N/N/0`, tallies agree, `✅ No defects`, exit 0), `FILES_FLOOR` 45 → 46, `ARMS_FLOOR` 369 → N, `WAIVED_CEILING` still 0, separation in both directions, the ⚠️ DATED-readings caveat (read the constants by SYMBOL), and — new for this phase — that `sql-gate-lint` now ALSO runs `scripts/lint-app-guc.mjs` (self-test first) and that `sql-tests`/VAC-08 are expected RED on the 164.7 PR for the arms TODOS names until the founder hand-applies to shared TEST. Do not restate any number as a live constant.

    TODOS.md: add `[164.7-TEST-APPLY-APPSETTINGS]` in the `[164.2-TEST-APPLY-PROVENANCE]` shape — the two migrations, the expected reds BY ARM (`test_analytics_service_settings_and_vault_tick.sql → TEST FAILED (0)`; `test_ledger_refresh_fanout.sql → TEST FAILED (0)` cause (iii); `test_ledger_refresh_composite_arm.sql → TEST FAILED (0)` cause (iii); VAC-08 `NOT baselined` for both new migration ledger rows), the ordered remedy (which-database marker → `psql "$TEST_SUPABASE_DB_URL" -f` each migration in timestamp order → never `db push`), and the UNKNOWN to be read off the first `sql-tests` log: whether TEST has a `vault` schema at all (RESEARCH OQ-3). Also add `[164.7-VAULT-ABSENT-RULE]` recording RESEARCH Open Question 2 for Phase 164.5 (the `vault-absent` hygiene rule requires the literal `vault.decrypted_secrets` in jobid 1's command TEXT, so a repoint to `SELECT public.match_engine_cron_tick()` must amend the rule, the manifest and the row in ONE change). Update `CRON-DRIFT-01`'s entry with a dated line: the GUC half is closed by 164.7 (lineage headers + the callable), the LIVE-row repair stays open for 164.5 item 7.
  </action>
  <verify>
    <automated>cd . && R=docs/runbooks/ledger-refresh-go-live.md && test "$(grep -a -n 'ALTER DATABASE\|ALTER ROLE' "$R" | grep -a -v -c '42501\|CORRECTED\|measured')" = "0" && grep -a -c "UPDATE public.system_flags SET enabled = TRUE" "$R" | grep -v '^0$' && grep -a -c "UPDATE public.system_flags SET enabled = FALSE" "$R" | grep -v '^0$' && grep -a -c '20260907130000_ledger_refresh_switch_to_system_flags.sql' "$R" | grep -v '^0$' && grep -a -c 'capture-manifest' "$R" | grep -v '^0$' && grep -a -c 'TWO live op' "$R" | grep -v '^0$' && grep -a -c '#747' "$R" | grep -v '^0$' && grep -a -c 'OQ-3' "$R" | grep -v '^0$' && test "$(grep -a -c 'ALTER DATABASE postgres SET app.admin_email' docs/runbooks/match-engine.md | grep -v '^0$' | wc -l)" = "1" && grep -a -c '42501' docs/runbooks/match-engine.md | grep -v '^0$' && grep -a -c 'CURRENCY 2026-09-07 (Phase 164.7 APPSETTINGS plan 05)' CLAUDE.md | grep -q '^1$' && grep -a -c 'coverage: files 46/73' CLAUDE.md | grep -v '^0$' && grep -a -c '\[164.7-TEST-APPLY-APPSETTINGS\]' TODOS.md | grep -q '^1$' && grep -a -c '\[164.7-VAULT-ABSENT-RULE\]' TODOS.md | grep -q '^1$' && grep -a -c 'TEST FAILED (0)' TODOS.md | grep -v '^0$' && npx tsx scripts/check-planning-hygiene.ts</automated>
  </verify>
  <done>No 42501-returning statement survives in the ledger runbook outside its dated correction; the two-op activation, the manifest re-capture, the P3-C/#747 note and the OQ-3 closure are written; the match-engine runbook's identical claim is corrected; CLAUDE.md carries the dated currency paragraph; TODOS names the expected shared-TEST reds by arm and the 164.5 hand-off; planning hygiene passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| annotated migration → gate exemption | a comment line exempts a file; the count and allowlist keep it honest |
| runbook → founder's hands on PROD | every statement the runbook prints will be pasted into the PROD SQL editor |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.7-18 | Tampering | executable text of an applied migration changed under cover of a header | high | mitigate | diff discipline in Task 2's verify (only `+--` lines); snapshot check; twins still measure |
| T-164.7-19 | Repudiation | floors pinned from prose instead of a run | medium | mitigate | separation in both directions on real runs, logged with exit codes |
| T-164.7-20 | Denial of Service | a runbook statement that fails under incident pressure | high | mitigate | zero 42501 statements outside the correction block; FAST rollback is a single UPDATE with a same-session verify |
| T-164.7-21 | Information disclosure | the runbook instructing a write without the which-database marker | high | mitigate | P0 marker step; every block preceded by it (RESEARCH Q7) |
| T-164.7-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Full corpus exit 0 at pinned floors; parser/floors vitest green; `--self-test` passes.
- App-GUC gate 0 findings / 5 annotated; self-test OK; diff on the five files is comment-only.
- Runbook greps as in Task 3; planning hygiene check passes.
</verification>

<success_criteria>
- SC-1 green on the annotated tree with a gate proven able to fire; D-04's third disposition recorded in file, ROADMAP and (plan 07) SUMMARY.
- SC-5: every falsified privilege claim corrected in place with the measurement, and the two facts research added (two ops; re-capture) written where the operator will read them.
</success_criteria>

<output>
Create `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-SUMMARY.md` when done, quoting the pinned run's `coverage:`/`arms:` lines and the app-GUC gate's summary lines verbatim.
</output>
