# 164.7 Plan 07 — ACTIVATION RECORD

**Founder decision:** 2026-09-10, at the `checkpoint:decision` gate of plan 07.

## DEFERRED

```
DEFERRED 2026-09-10
Pre-flight that failed:  P3-C (mt5 compute_jobs `done` within 3 days)
Measured value:          newest mt5 job ~2026-09-07 04:0xZ — MT5 broke at
                         04:05:08, five minutes after that day's 04:00Z tick,
                         and the 05:00Z tick never had a working terminal.
                         => ~3.5 days old, OUTSIDE the 3-day window.
Verdict:                 criterion 3 BLOCKED by an operational fault outside this phase
Re-entry condition:      after the 04:00Z and 05:00Z enqueue ticks on 2026-09-11,
                         re-run P3-C. MT5 has been authorized again since
                         2026-09-10 ~16:10Z (issue #753 closed, prober run
                         34500455961 clean).
```

⛔ **NOTHING WAS WRITTEN TO PRODUCTION.** No `UPDATE`, no `cron.schedule`, no manifest re-capture.
`scripts/prod-prober/cron-manifest.json` is untouched at 14 jobs and `ledger_refresh_fanout` is
still absent from it.

## Why the gate was not met — the arithmetic, not a guess

| fact | value | source |
|---|---|---|
| `poll-allocator-positions` schedule | `0 4 * * *` (daily 04:00Z) | committed manifest, jobid 15 |
| `refresh-allocator-equity` schedule | `0 5 * * *` (daily 05:00Z) | committed manifest, jobid 17 |
| MT5 terminal broke | 2026-09-07 **04:05:08** | terminal Journal, `Invalid account` |
| last tick before the break | 2026-09-07 04:00Z — **5 minutes earlier** | schedule above |
| P3-C window | 3 days | `docs/runbooks/ledger-refresh-go-live.md` P3 Part C |
| gap at decision time (2026-09-10 ~16:20Z) | **~3.5 days** | arithmetic |

Both enqueue jobs are **daily, not hourly**. So no amount of waiting inside 2026-09-10 could have
brought a fresh mt5 job into the window; the next natural opportunity is the 2026-09-11 04:00Z and
05:00Z ticks. ⭐ The founder considered forcing the enqueue by calling the two functions by hand and
**declined on blast radius**: `enqueue_refresh_allocator_equity_for_all()` and
`enqueue_poll_allocator_positions_for_all_keys()` enqueue for **every key at every venue**, so a
full cross-venue day-run would have been triggered to produce one mt5 row for a pre-flight. Waiting
~12 h costs nothing — the activation closes a gap, not an outage.

## The P0 marker

⚠️ **There is no marker OUTPUT in this record, and that is correct, not an omission.** The
which-database query heads every block that WRITES, and **no block was run**:

```sql
SELECT shobj_description(oid, 'pg_database') AS which_database
  FROM pg_database WHERE datname = current_database();
```

⛔ Do not read this section as evidence that the marker was checked. It is the statement that will
head every block at re-entry. A DEFER path produces no PROD statements and therefore no
`which_database` reading.

## Criteria verdicts

| criterion | verdict |
|---|---|
| 3 — mt5 cohort staleness falls after activation | **BLOCKED by an operational fault outside this phase** |
| 4 — kill switch proven with the schedule still firing | **BLOCKED** — it is downstream of criterion 3's activation; nothing was registered to flip |

⛔ Neither is reported as failed engineering. Both are blocked on an MT5 gateway outage that ran
from 2026-09-07 04:05:08 to 2026-09-10 16:10Z and had nothing to do with this phase's code.

## What is NOT claimed here

- No claim that `system_flags.ledger_refresh_enabled` is FALSE right now beyond the migration's own
  apply-time NOTICE (`applied DORMANT … NOTHING scheduled`, apply run 34148251493). Nothing since is
  recorded as flipping it, but that is an argument from absence and P2/1b settle it at re-entry.
- No claim that the worker is healthy. P5 exists for that and requires a PROD read.
- ⛔ No claim about `[A1]` (a `SECURITY DEFINER` wrapper reading the real Vault). It is still HELD,
  not proven, and this deferral does not touch it.
