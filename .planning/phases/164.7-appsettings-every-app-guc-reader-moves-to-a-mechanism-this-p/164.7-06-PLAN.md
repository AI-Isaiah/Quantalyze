---
phase: 164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p
plan: 06
type: execute
subsystem: pr-gates-and-review
tags: [three-reviewers, vac-04, earned-ack, drift-02, vac-08, expected-red, dry-run, criterion-6, criterion-7]
wave: 4
depends_on: ["164.7-05"]
files_modified:
  - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
  - scripts/mutation-runner/run.mjs   # CONDITIONAL — Task 1 only: re-pin FILES_FLOOR/ARMS_FLOOR if a reviewer fix moved the arm count; otherwise untouched (plan 05 owns the pin)
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEWS.md
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-VAC04-OBSERVED.md
  - .planning/WINDOWS.md
autonomous: false
requirements: [SC-6, SC-7, D-07, D-08, VAC04-ARMS-OBSERVE]
provides:
  - "Three reviewer reports (migration-reviewer, rls-policy-auditor, silent-failure-hunter) over the two migrations and their gates, with every finding either fixed or recorded with its reason — BEFORE the PR exists"
  - "The VAC-04 step's own output from THIS PR's migration-drift-check run, quoted verbatim with run id and head sha, the branch it took named by line reference, and the prod-body-ack EARNED (origin/main snapshot sha == PROD sha proven locally with the gate's own normaliser) before the pragma is written"
  - "The supabase-migrate plan job's dry-run output read and recorded (TRAP A: does a comment-only edit to an applied migration change what the CLI plans?)"
  - "WINDOWS.md entry 25 dispositioned from a measurement"
affects: [164.7-07]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "No migration of this phase reaches a PR before all three reviewers have run on the working branch and their actionable findings are fixed (D-07; CLAUDE.md: reviewers run BEFORE the pr-branch filter)"
    - "The prod-body-ack pragma in 20260907130000 carries the PROD sha VAC-04 printed, the origin/main snapshot sha computed locally, and the run id — and the two shas are IDENTICAL; if they were not, the difference was folded into the migration and NO pragma was written"
    - "The SUMMARY-bound record names the VAC-04 branch by line: the compare loop (DRIFT for the two ledger names, advisory 'no PROD counterpart' for match_engine_cron_tick), NOT the :198 short-circuit and NOT the :448 legitimate-zero — or, if the output says otherwise, it says exactly what the output says"
    - "The expected `sql-tests` / VAC-08 reds on the PR are the ones TODOS names by arm, and nothing else is red for a reason this phase introduced"
  artifacts:
    - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEWS.md"
      provides: "per-reviewer findings table (id, severity, file:symbol, disposition, fix commit) — three sections"
    - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-VAC04-OBSERVED.md"
      provides: "the verbatim VAC-04 step log excerpt (via `gh api …/jobs/<id>/logs`, never `gh run view --log`), run id, head sha, the branch by line reference, the local --diff-bodies output, the dry-run job's output excerpt, and the observed sql-tests red list"
  key_links:
    - from: "supabase/migrations/20260907130000 header"
      to: "scripts/prod-body-drift-check.sh pragma grep (`-- prod-body-ack: ${live_hash}`)"
      via: "exact live-hash match, so the ack silences ONLY the drift it names"
      pattern: "prod-body-ack: [0-9a-f]{64}"
    - from: "scripts/sql-body-normalize.mjs --diff-bodies <origin/main snapshot> <HEAD snapshot>"
      to: "the PROD sha in the VAC-04 log"
      via: "equality of the origin/main snapshot sha and PROD's sha proves DRIFT-02's shape is absent"
      pattern: "diff-bodies"
---

<objective>
The PR-time obligations, in the order the repo's own rules impose.

**Reviewers first, on the WORKING branch (D-07; criterion 6).** Merging `supabase/migrations/**` to `main` auto-applies to PROD. The three named reviewers run here, before any PR: `migration-reviewer` and `rls-policy-auditor` exist at `.claude/agents/`; `silent-failure-hunter` is the `pr-review-toolkit` plugin agent (measured at planning: `~/.claude/plugins/cache/claude-plugins-official/pr-review-toolkit/*/agents/silent-failure-hunter.md`). silent-failure-hunter is load-bearing, not routine: D-01 converts an infallible read into a fallible one, and the handler's `RAISE WARNING` + `NULL` is exactly the shape that reviewer exists to interrogate.

**Then the PR, then VAC-04 (D-08; criterion 7).** `migration-drift-check.yml` runs on `pull_request` only, with the real PROD credential, so the VAC-04 observation cannot be taken before the PR exists — this is why the plan carries a human-action checkpoint for `/gsd-ship` between the reviewers and the ack. RESEARCH Q5 predicts the compare loop (this PR adds migrations, so the `:198` short-circuit is impossible; it defines functions, so the `:448` legitimate-zero is impossible), with DRIFT for the two ledger names and an advisory for the new callable. The plan does not assume the prediction; it READS the log via `gh api …/jobs/<id>/logs` (memory: `gh run view --log` TRUNCATES — a clean failure reads as a hang) and quotes it. The ack is EARNED by the 164.2 recipe (commit `22566fbe`, header of `20260906120000_computation_error_provenance.sql`): the origin/main snapshot sha must equal PROD's printed sha; only then is the pragma written, with both hashes and the run id beside it. Any mismatch is a DRIFT-02 finding to FOLD into the migration, never to acknowledge.

**Expected reds, named before anyone debugs them (constraint 8):** `sql-tests` runs every gate against shared TEST and nothing applies migrations there (`SKIP-01`, `CI-MIGRATE-01`); VAC-08 errors on repo migrations absent from TEST's ledger and not baselined. Both are RED on this PR by design for exactly the arms `[164.7-TEST-APPLY-APPSETTINGS]` lists, until the founder hand-applies. The ack commit lands on the WORKING branch and the `-pr` branch is rebuilt (CLAUDE.md "Reviewers run BEFORE the filter"); the deletion guard runs before every push of a `-pr` branch.

Purpose: SC-6, SC-7, D-08's observation obligation; the dry-run reading closes PATTERNS TRAP A by measurement.
Output: reviews record, VAC-04 observation record, the earned pragma, WINDOWS entry 25 disposition.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-CONTEXT.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-RESEARCH.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-SUMMARY.md
@supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql
@supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
@supabase/migrations/20260906120000_computation_error_provenance.sql
@scripts/prod-body-drift-check.sh
@scripts/sql-body-normalize.mjs
@.github/workflows/migration-drift-check.yml
@.github/workflows/supabase-migrate.yml
@.planning/WINDOWS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Three reviewers on the working branch — findings fixed at the source, recorded with reasons</name>
  <files>.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEWS.md</files>
  <read_first>
    - `.claude/agents/migration-reviewer.md` and `.claude/agents/rls-policy-auditor.md` (their briefs and what they need pointed at)
    - the `silent-failure-hunter` agent definition under the pr-review-toolkit plugin cache (measure its path with `ls ~/.claude/plugins/cache/claude-plugins-official/pr-review-toolkit/*/agents/`)
    - Memory `feedback_run_three_reviewers_before_any_migration_apply`; `feedback_fix_everything_topic_batched_parallel_fixers` (the orchestrator re-runs the gates itself after any fixer; never trust a fixer's claim); `feedback_review_blast_radius_bar_and_stopping_rule` (what BLOCKS vs what is booked)
  </read_first>
  <action>
    Dispatch the three reviewers (they are not gsd-* agents, so the Agent tool is the right vehicle here) against the phase diff: `git diff origin/main --stat` scoped to `supabase/migrations/20260907*`, `supabase/tests/test_analytics_service_settings_and_vault_tick.sql`, the two ledger gate files, `scripts/pg-lane/fixtures/31-*`, `32-*`, and the runbook. Brief each with the specific questions this phase raises: migration-reviewer — re-base fidelity of both ledger bodies against the snapshots (statement-level diff must be ONLY Lock B + the DECLARE type + the COMMENT), the seed's `ON CONFLICT DO NOTHING`, the verify block's RAISE on a TRUE row at apply time, `search_path`, REVOKEs, the absence of schedule tokens, apply-ability on a cluster without vault/pg_net; rls-policy-auditor — `system_settings` policy set vs `system_flags` (no public read; `TO authenticated` on the admin policy; `auth.role()` service predicate; table-level REVOKE from anon), whether the SECDEF readers need any policy (they do not — owner exemption), the arm R1-R3 twins; silent-failure-hunter — the Lock B handler (WARNING carries SQLSTATE only; NULL ⇒ dormant; is any failure now INVISIBLE that was visible before?), the callable's RAISE-on-absent texts, the async `net.http_post` note, arm L's swallowed exception in the gate, and the runbook's "UPDATE 0 ⇒ STOP" branch.

    Fix every actionable finding at the source, file-disjoint fixers if parallel, then RE-RUN the gates yourself: `node scripts/mutation-runner/run.mjs --parse-only`, the two `--file` diagnostics for any gate touched, `node scripts/lint-sql-gates.mjs`, `node scripts/lint-app-guc.mjs`, `npm run schema:functions:check`, pytest from `analytics-service/`, and — if any migration body changed — the full corpus run (floors must still separate; re-pin if the arm count moved, with a dated note). Write `164.7-REVIEWS.md`: three sections, one table each (id, severity, file + symbol, disposition fixed/booked/accepted with reason, fix commit sha). Booked items go to TODOS.md under the phase tag. If any reviewer cannot be dispatched (plugin missing), STOP and report — do not substitute a fourth reviewer for a named one.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && R=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEWS.md && test -f "$R" && grep -a -c '^## migration-reviewer' "$R" | grep -q '^1$' && grep -a -c '^## rls-policy-auditor' "$R" | grep -q '^1$' && grep -a -c '^## silent-failure-hunter' "$R" | grep -q '^1$' && test "$(grep -a -c -i 'disposition: *open\|| open |' "$R")" = "0" && node scripts/lint-sql-gates.mjs && node scripts/lint-app-guc.mjs && npm run -s schema:functions:check && node scripts/mutation-runner/run.mjs --parse-only 2>&1 | grep -a -c 'MEASURE_FAIL' | grep -q '^0$' && (cd analytics-service && python3 -m pytest tests/test_ledger_refresh_gates.py tests/test_ledger_refresh_kind_scope_drift.py -q 2>&1 | tail -1 | grep -a -E '[0-9]+ passed') && git status --porcelain | wc -l | grep -q '^ *0$'</automated>
  </verify>
  <done>Three reviewer sections exist with no open disposition; every gate this phase touches is green again after the fixes; the tree is clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Ship the PR from the working branch's filtered `-pr` twin, then resume</name>
  <action>
    Founder runs the repo's ship flow (`/gsd-ship` → `/gsd-pr-branch` → the deletion guard → PR from `<branch>-pr`), per CLAUDE.md "PR branches — always filter transient planning artifacts". Then paste back: the PR number, the PR head sha, and the `migration-drift-check` run id once it has completed (it runs on `pull_request`; it needs the credential, and it exits 1 if the credential is absent — that is a fault to fix, not a skip).
  </action>
  <instructions>
    Already automated (Task 1): three reviewers run on the working branch, findings fixed, every gate re-run green, tree clean.
    The ONE thing requiring the founder: open the PR through the ship flow so that `migration-drift-check` runs on `pull_request` with the real PROD credential. Steps: (1) `/gsd-ship` on the working branch; (2) `/gsd-pr-branch` builds `<branch>-pr`; (3) run the deletion guard from CLAUDE.md (`git diff --diff-filter=D --name-only main..<branch>-pr -- .planning/` must print nothing — any output is a STOP); (4) push `<branch>-pr` and open the PR from it; (5) wait for `migration-drift-check` to complete and copy its run id (`gh run list --workflow migration-drift-check.yml --limit 1 --json databaseId,headSha,conclusion`). Expect `sql-tests` RED for exactly the arms TODOS `[164.7-TEST-APPLY-APPSETTINGS]` names — that is by design until the TEST hand-apply.
  </instructions>
  <verification>An open PR from `<branch>-pr` whose head sha matches the pasted value; a completed `migration-drift-check` run at that head (conclusion read in Task 3, not assumed green here); `git branch --show-current` back on the working branch.</verification>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && gh pr list --state open --head "$(git branch --show-current)-pr" --json number,headRefOid --jq '.[0] | .number, .headRefOid' | grep -a -c . | grep -q '^2$'</automated>
  </verify>
  <done>An open PR exists from the `-pr` branch; its number, head sha and the drift-check run id are known.</done>
  <resume-signal>Reply with `PR=<n> HEAD=<sha> DRIFT_RUN=<id>`</resume-signal>
</task>

<task type="auto">
  <name>Task 3: READ VAC-04's output, EARN the ack, read the dry-run, name the reds, disposition WINDOWS 25</name>
  <files>supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql, .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-VAC04-OBSERVED.md, .planning/WINDOWS.md</files>
  <read_first>
    - `scripts/prod-body-drift-check.sh`: the `:198` notice text, the `:448` LEGITIMATE-ZERO exit, the compare-loop verdict strings (`MATCH`, `committed body has no PROD counterpart (advisory)`, the DRIFT error and the `-- prod-body-ack: ${live_hash}` grep), the absurdity floor block (`SNAPSHOT_MIN`)
    - `supabase/migrations/20260906120000_computation_error_provenance.sql` header — the ack block to reproduce, value for value
    - `scripts/sql-body-normalize.mjs` usage: `--diff-bodies <snapshot.sql> <live.sql>` (emits names and shas, never body text)
    - Memory `reference_gh_run_view_log_truncates` — use `gh api repos/{owner}/{repo}/actions/jobs/<job_id>/logs`; compare the last log timestamp vs the job's `completedAt`
    - Memory `reference_ci_green_must_be_sha_bound` — re-read `headRefOid` before trusting any check
    - `.planning/WINDOWS.md` entry 25 row (`unrun-verify | scripts/prod-body-drift-check.sh`)
    - `.github/workflows/supabase-migrate.yml` `plan` job (the `--dry-run` step) — read its output on this PR for TRAP A
    - CLAUDE.md "Reviewers run BEFORE the filter" measured incidents 1-3 — the ack commit goes on the WORKING branch, the `-pr` branch is REBUILT, and `git branch --show-current` is checked after every filter
  </read_first>
  <action>
    1. Resolve the drift-check run's job id (`gh run view <DRIFT_RUN> --json jobs`) and fetch the FULL job log with `gh api`. Extract the `VAC-04 — repo-vs-PROD function body drift` step's lines verbatim into `164.7-VAC04-OBSERVED.md` with run id, job id, head sha (must equal the PR head from Task 2), and the job's `completedAt` vs the last log timestamp. Name the branch it took BY LINE REFERENCE against `scripts/prod-body-drift-check.sh` at the PR head: if the log carries the `:198` notice → say so and record the roll-forward to 164.5 (do NOT claim the arms ran); if it carries the `:448` legitimate-zero → say so (and explain how, since this PR defines functions — that would be a gate defect to book); if it reached the compare loop → list each name and its verdict (expected: `enqueue_ledger_refresh_for_strategies` DRIFT, `enqueue_ledger_composite_refresh` DRIFT, `match_engine_cron_tick` advisory/no counterpart). Record whether the absurdity floor printed anything. This section is what criterion 7 quotes.
    2. EARN the ack, per name: `git show origin/main:supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql > <scratch>/main-fanout.sql` and the same for the composite; run `node scripts/sql-body-normalize.mjs --diff-bodies <scratch>/main-fanout.sql supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql` (and composite) and record the two shas each prints. The origin/main sha MUST equal the PROD sha VAC-04 printed for that name. If equal for both: write `-- prod-body-ack: <PROD sha>` lines (one per name — measure whether the script expects one pragma per hash; it greps per live hash) into the reserved block of `20260907130000`, filling every `<measured in plan 06>` field with the run id, head sha, both shas and the differing-line count, and the ⭐ "PROD's live body is EXACTLY the repository's last committed snapshot — DRIFT-02's shape is ABSENT" sentence ONLY if that is what the hashes say. If NOT equal for either name: STOP, book a DRIFT-02 finding, fold PROD's difference into the migration (the DRIFT-02a procedure: transcribe `pg_get_functiondef` and prove by md5), and do not write a pragma — return `## CHECKPOINT REACHED` to the orchestrator with the two shas. ⚠️ On that branch this task's `<verify>` is EXPECTED to fail at its `prod-body-ack` grep: the pragma's absence is the DRIFT-02 checkpoint state, not a task failure, and the verify must NOT be satisfied by writing a pragma to make it pass. The orchestrator reads `## CHECKPOINT REACHED` plus the two unequal shas as the outcome; the verify is re-run only after the fold lands and the ack is earned.
    3. Commit the ack on the WORKING branch (`git branch --show-current` must NOT end in `-pr`), rebuild the `-pr` branch via the ship flow's filter, run the deletion guard (`git diff --diff-filter=D --name-only main..<pr-branch> -- .planning/` must print nothing), push, and re-read the drift-check run on the NEW head: the compare loop must now print the acknowledged warning for both names and exit 0.
    4. Read the `supabase-migrate.yml` `plan` job's dry-run output on this PR (TRAP A): does the CLI plan ONLY the two new migrations, and does it say anything about the five comment-edited applied files (hash mismatch, re-apply, nothing)? Quote the relevant lines into the observation file. If it reports a hash mismatch for an applied file, STOP — that is a merge-blocking fact the founder decides on (repair the ledger hash vs revert the header), returned as `## CHECKPOINT REACHED`.
    5. Read the `sql-tests` job log (same `gh api` method) and list every `TEST FAILED (…)` line and VAC-08 `::error::` line; diff that list against `[164.7-TEST-APPLY-APPSETTINGS]` in TODOS — any red NOT on the list is a regression to fix now; any listed red that did NOT appear is a claim to correct in TODOS. Record the UNKNOWN that this run answers: does TEST have a `vault` schema (which arm fired in the new gate).
    6. `.planning/WINDOWS.md` entry 25: set its disposition from the measurement in step 1 (closed with run id if the compare loop ran; otherwise carried to 164.5 with the reason), one row edit, scoped `Edit`.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && O=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-VAC04-OBSERVED.md && test -f "$O" && grep -a -c -E 'run id: [0-9]{9,}' "$O" | grep -v '^0$' && grep -a -c -E 'head sha: [0-9a-f]{40}' "$O" | grep -v '^0$' && grep -a -c -E 'branch taken: .*(:198|:448|compare loop)' "$O" | grep -q '^1$' && grep -a -c 'diff-bodies' "$O" | grep -v '^0$' && grep -a -c 'dry-run' "$O" | grep -v '^0$' && grep -a -c 'sql-tests' "$O" | grep -v '^0$' && M=supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql && grep -a -c -E '^-- prod-body-ack: [0-9a-f]{64}$' "$M" | grep -v '^0$' && test "$(grep -a -c '<measured in plan 06>' "$M")" = "0" && grep -a -c -E 'origin/main snapshot sha256 +[0-9a-f]{8}' "$M" | grep -v '^0$' && test "$(git branch --show-current | grep -c -- '-pr$')" = "0" && grep -a '^| 25 ' .planning/WINDOWS.md | grep -a -v -c '| open |' | grep -q '^1$' && PR=$(gh pr list --state open --head "$(git branch --show-current)-pr" --json number --jq '.[0].number') && gh pr checks "$PR" --json name,state,link 2>/dev/null | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const j=JSON.parse(s);const d=j.find(c=>/migration-drift-check|drift/i.test(c.name));if(!d){console.log("no drift check found");process.exit(1)};console.log(d.name,d.state);if(d.state!=="SUCCESS")process.exit(1)})'</automated>
  </verify>
  <done>The observation file quotes VAC-04's own lines with run id and sha and names the branch by line; the pragma(s) carry the PROD sha proven equal to the origin/main snapshot sha; the drift check on the new head is SUCCESS; the dry-run and sql-tests readings are recorded; WINDOWS entry 25 is no longer `open`; the working branch is not the `-pr` branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR merge → PROD auto-apply | the last human-reviewable point before two SECDEF bodies are replaced on PROD |
| ack pragma → VAC-04 silence | a pasted pragma would silence exactly the drift DRIFT-02 exists to catch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.7-22 | Repudiation | pragma written on sight | critical | mitigate | sha equality proven locally with the gate's own normaliser; mismatch ⇒ fold, never ack (Task 3 step 2) |
| T-164.7-23 | Tampering | fixes landing on the `-pr` head | medium | mitigate | branch check after every filter; ack commit on the working branch (Task 3 step 3) |
| T-164.7-24 | Tampering | `-pr` branch deleting base `.planning/` files | high | mitigate | deletion guard before push (CLAUDE.md) |
| T-164.7-25 | Repudiation | a truncated log read as a hang | medium | mitigate | `gh api …/jobs/<id>/logs`; completedAt vs last timestamp |
| T-164.7-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Three reviewer sections, no open findings; all gates re-run green by the orchestrator.
- VAC-04 branch named by line; pragma earned; drift check SUCCESS on the new head, SHA-bound.
- Dry-run and sql-tests readings recorded; reds match TODOS by arm.
</verification>

<success_criteria>
- SC-6 and SC-7 satisfied by artifacts a reviewer can re-derive from run ids and shas, not by assertion.
</success_criteria>

<output>
Create `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-06-SUMMARY.md` when done; it must quote the VAC-04 step output verbatim (criterion 7) and name the branch by line reference.
</output>
