---
phase: 164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p
plan: 05
subsystem: gates-ratchets-and-docs
tags: [floors, ratchet, lineage-headers, allowlist, runbook, criterion-1, criterion-5, d-04, d-05, currency]

requires:
  - phase: 164.7-01
    provides: "scripts/lint-app-guc.mjs — the hermetic scanner whose LINEAGE_ALLOWLIST this plan fills, and the measured interim census (12 findings / 5 files, 1/5/4/1/1) this plan drives to 0 by annotation"
  - phase: 164.7-02
    provides: "supabase/tests/test_analytics_service_settings_and_vault_tick.sql — the 46th annotated gate file and 7 of the 11 new arms"
  - phase: 164.7-03
    provides: "20260907130000_ledger_refresh_switch_to_system_flags.sql — the successor two lineage headers name, and the fail-CLOSED system_flags body the runbook now activates"
  - phase: 164.7-04
    provides: "arms K and L in both ledger gates (4 of the 11 new arms) and the 27 re-pointed twins whose continued biting is what makes the floor move +11 rather than less"
provides:
  - "FILES_FLOOR = 46 and ARMS_FLOOR = 380, each pinned at a separation point observed on real full-corpus lane runs, with dated RE-DERIVED blocks; WAIVED_CEILING still 0"
  - "The MEASURED correction that this ratchet is separated in TWO LAYERS — run.mjs reports only the regression direction; the stale direction is the vitest ratchet — falsifying the plan's own expectation"
  - "Five dated `-- APP-GUC-LINEAGE:` headers + five count-pinned LINEAGE_ALLOWLIST entries summing to 12, landed in ONE commit; the app-GUC gate goes 12 findings -> 0 by annotation, never by loosening"
  - "164.7-05-NEUTER.log — five neuters of the exemption, all observed RED, all restored by `cp` with sha256 equality"
  - "docs/runbooks/ledger-refresh-go-live.md with zero 42501-returning statements outside its dated correction blocks, the TWO-op activation, the manifest re-capture, the #747 note and OQ-3 closed"
  - "docs/runbooks/match-engine.md item 1 corrected (same class, same cause); CLAUDE.md + TODOS currency"
  - "deferred-items.md — `npm run lint` is RED on this branch from 86 pre-existing planning-hygiene violations in the phase's OWN artifacts, proven to predate this plan"
affects: [164.7-06, 164.7-07, 164.5]

actuals:
  tokens: 47000   # chars/4 over the realized diff, d193e4cd..68a049b8
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A ratchet separated in TWO LAYERS: the runner reports the regression direction (`biting < floor`), the vitest reports the stale direction (`annotated > floor`). Naming which layer reports which is what stops a stale ratchet reading as a green gate."
    - "A count-pinned exemption proven by moving the count ONE in each of its two independent edits, and again by appending one real occurrence — three neuters that isolate three different halves of the same pin"
    - "Correcting a runbook IN PLACE by quoting the falsified statement beside the measurement, rather than deleting it — a founder-facing document that silently drops a claim teaches nothing"

key-files:
  created:
    - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-FLOORS.log
    - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-NEUTER.log
    - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/deferred-items.md
  modified:
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/mutation-runner-floors.test.ts
    - scripts/lint-app-guc.mjs
    - src/__tests__/lint-app-guc.test.ts
    - supabase/migrations/20260407164606_perfect_match.sql
    - supabase/migrations/20260408113029_cron_heartbeat.sql
    - supabase/migrations/20260408215026_schedule_match_cron_hourly.sql
    - supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql
    - supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql
    - .github/workflows/ci.yml
    - docs/runbooks/ledger-refresh-go-live.md
    - docs/runbooks/match-engine.md
    - CLAUDE.md
    - TODOS.md

key-decisions:
  - "The plan's prediction that run 1 would exit 1 on stale-low floors was FALSIFIED by measurement and the correction was written into the derivation rather than the expectation quietly dropped. run.mjs gates on `annotated < floor` and `biting < floor`, so a stale ratchet is invisible to it; the vitest catches it. Both readings are in the floors log."
  - "The two separation runs measured the tree that ALREADY carried Task 2's comment-only headers, and run 1 measured the same corpus WITHOUT them at the same 380 arms. That ordering buys a proof the plan's ordering does not: a masthead comment demonstrably moved no arm."
  - "ci.yml was committed with Task 2 rather than Task 1 even though the timeout reading belongs to Task 1 — splitting one file across the two commits would have put a sentence claiming the app-GUC interval was closed into history one commit BEFORE the annotations that close it."
  - "The two `toHaveLength(45)` pins that count annotations WITHIN test_strategy_shares_rls.sql were left UNCHANGED. They count a different thing; bumping every 45 in the file would have been the drive-by this ratchet exists to catch."
  - "The D-05 comment arm no longer pins line 113 by literal. This plan's own header shifted it, and a hard-coded line number that must be re-typed by whoever next edits the masthead becomes a chore and then a lie; it is re-derived from the bytes instead."
  - "86 planning-hygiene violations across 13 of the phase's OWN plan/summary/neuter files were NOT fixed. The remedy edits three PENDING plans two future executors are about to read — deviation Rule 4, not Rule 3 — and it predates this plan, proven by an empty `git diff --stat d193e4cd HEAD` over those files."

patterns-established:
  - "When a plan predicts a gate reading and the gate disagrees, the DERIVATION records the refutation beside the value, so the next plan inherits the measurement and not the expectation"
  - "A cross-reference to a TODOS entry is written WITHOUT the entry's bracketed key, so the key stays a unique definition token a count-based assertion can pin"

requirements-completed: [SC-1, SC-2, SC-5, D-04, D-05]

coverage:
  - id: D1
    description: "FILES_FLOOR and ARMS_FLOOR pinned at the measured separation point of the finished corpus, WAIVED_CEILING still 0"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "164.7-05-FLOORS.log run 2 (47/381) -> exit 1 with exactly two floor defects; run 3 (46/380) -> exit 0, No defects"
        status: pass
      - kind: unit
        ref: "npx vitest run src/__tests__/mutation-annotation-parser.test.ts src/__tests__/mutation-runner-floors.test.ts — 187/187"
        status: pass
    human_judgment: false
  - id: D2
    description: "Both directions are separated, and WHICH LAYER reports each is measured rather than assumed"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "164.7-05-FLOORS.log run 1 (45/369) -> exit 0, No defects — the runner does NOT report a stale ratchet"
        status: pass
      - kind: unit
        ref: "164.7-05-FLOORS.log VITEST READING A — `RATCHET STALE: 46 of 73 … FILES_FLOOR is still 45` plus the paired ARMS_FLOOR message, 7 failures"
        status: pass
    human_judgment: false
  - id: D3
    description: "The app-GUC gate reports 0 findings and names five annotated files, reached by ANNOTATION and not by loosening the rule"
    requirement: "D-04, D-05"
    verification:
      - kind: integration
        ref: "node scripts/lint-app-guc.mjs — `app-guc: findings 0`, `app-guc: annotated (lineage) 5 — <the five>`; --self-test still OK"
        status: pass
      - kind: unit
        ref: "src/__tests__/lint-app-guc.test.ts — 28/28, including the re-measurement of all five counts against countReads() and their sum of 12"
        status: pass
    human_judgment: false
  - id: D4
    description: "The exemption is FALSIFIABLE — moving either edit's count by one, appending one read, or deleting the allowlist entry each turns the gate RED"
    requirement: "D-05"
    verification:
      - kind: other
        ref: "164.7-05-NEUTER.log N1-N5 — five neuters, five `cp` restores, sha256 equal on both files, no `git checkout --` anywhere"
        status: pass
    human_judgment: false
  - id: D5
    description: "No executable statement of any applied migration changed"
    requirement: "D-04"
    verification:
      - kind: unit
        ref: "git diff origin/main on the five files — 20 insertions, 0 deletions, every line `+--`; npm run schema:functions:check current (120 functions); --parse-only zero MEASURE_FAIL; 39 pytest ledger-gate arms pass"
        status: pass
    human_judgment: false
  - id: D6
    description: "The runbook no longer instructs a statement that returns 42501, and carries the two facts research added"
    requirement: "SC-5"
    verification:
      - kind: unit
        ref: "grep: every `ALTER DATABASE`/`ALTER ROLE` line in the ledger runbook sits inside a dated correction; zero `current_setting('app.` remain; `TWO live op`, `capture-manifest`, `#747`, `OQ-3` all present"
        status: pass
    human_judgment: false
  - id: D7
    description: "The corrected runbook actually works when a founder runs it on PROD"
    verification: []
    human_judgment: true
    rationale: "Criteria 3 and 4 are a USER GATE by CONTEXT decision D-06 — two production writes plus a manifest re-capture. Nothing in this plan touched a database. The runbook is now executable as written; whether the activation succeeds is measured at the gate, and P3-C may still block it for reasons outside this phase."

duration: 172min
completed: 2026-09-07
status: complete
---

# Phase 164.7 Plan 05: floors, lineage headers and the falsified runbooks Summary

**The ratchet is pinned at 46 files / 380 arms on separation points a machine was
watched failing at, the app-GUC gate went from 12 findings to 0 by annotating five
applied migrations rather than by loosening anything, and the two runbooks that
instructed operators to run statements returning `42501` now instruct statements
that execute.**

## Performance

- **Duration:** ~172 min (2026-09-07, 11:15Z → 14:07Z)
- **Tasks:** 3 of 3
- **Files:** 3 created, 15 modified
- **Lane time:** three full-corpus runs, 531 s + 528 s + 614 s

## The pinned run, quoted verbatim

`node scripts/mutation-runner/run.mjs`, exit **0**:

```
coverage: files 46/73
lane-blocked: 0 file(s) probe pg_extension for pg_cron and are not yet annotated — (none; the lane hosts pg_cron since Phase 164.4.1, TODOS [REDUNDER-PGCRON] retired 2026-09-05)
lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires
  pending: 0 idiom file(s) without RED-UNDER — 
arms: 380/380/0   (executed/annotated/waived)
biting: 380   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 380   (arm lanes actually spawned — tallied inside runLane, independent of the 380 the verdict loop counted; plus 46 baseline / 46 restore leg(s))
per-arm lane time: mean 1.3s over 380 arm run(s)
✅ No defects. Every annotated arm bit its own arm first.
```

`46 + 0 lane-blocked + 27 unreachable + 0 pending = 73`. The two independent
tallies agree. `FILES_FLOOR` 45 → **46**, `ARMS_FLOOR` 369 → **380**,
`WAIVED_CEILING` still **0**.

## The app-GUC gate, quoted verbatim

`node scripts/lint-app-guc.mjs`, exit **0**:

```
app-guc: files scanned 291
app-guc: annotated (lineage) 5 — supabase/migrations/20260407164606_perfect_match.sql, supabase/migrations/20260408113029_cron_heartbeat.sql, supabase/migrations/20260408215026_schedule_match_cron_hourly.sql, supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql, supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql
app-guc: findings 0
```

`--self-test`:

```
lint-app-guc self-test OK: 4 finding kinds, red+green each (5 red, 2 green fixtures; 2 kind(s) vitest-covered by declaration).
```

⛔ The twelve sites did not go anywhere. The five allowlist `occurrences` sum to
**12** — the same twelve the plan-01 census reported — and the vitest re-measures
each against `countReads()` of the real file's bytes.

## Accomplishments

- **Both floors separated in both directions on REAL full-corpus lane runs before
  being pinned.** At `47/381` the runner named both regressions and exited 1; at
  `46/380` it exited 0 with no defects. Three runs, all logged with exit codes,
  wall clocks and printed lines.
- **The plan's own expectation about the stale direction was falsified and the
  refutation was written into the derivation.** See Deviations 1.
- **Five applied migrations annotated without one executable byte changing.**
  `git diff origin/main` over them is 20 insertions and 0 deletions, every line
  `+--`.
- **The exemption was broken five ways and watched to fail each time**, then
  restored by `cp` from byte backups with sha256 equality — including one neuter
  of the committed VITEST, which proved the census arm is coupled to the real
  file's bytes rather than to a number typed beside it.
- **The ledger runbook's Step 1 and its Rollback FAST both execute now.** The
  falsified statements are quoted beside the 2026-09-05 measurement rather than
  deleted, and OQ-3 is closed with the answer NEITHER.
- **Two facts research found that the runbook never said** are now where the
  operator reads them: the schedule is NOT registered on PROD (14 jobnames, none
  of them), so activation is TWO live ops; and registering it trips the 164.1
  `cron-drift` arm until the manifest is re-captured, which is step 2e.

## Task Commits

1. **Task 1 (tracer): floors pinned at the measured separation point** — `9aa08bc7` (chore) — ends `FLOOR-INTERVAL`
2. **Task 2: five lineage headers + the allowlist, one commit** — `e12ef4c6` (docs) — ends `APP-GUC-INTERVAL`
3. **Task 3: the two runbooks, CLAUDE.md, TODOS, deferred items** — `68a049b8` (docs)

## Deviations from Plan

### 1. [Rule 1 — Bug in the plan's stated expectation] The stale-ratchet direction is NOT reported by `run.mjs`

- **Found during:** Task 1, run 1.
- **The plan said:** run 1 would "exit 1 with EXACTLY the stale-ratchet defects
  for FILES_FLOOR (46 > 45) and ARMS_FLOOR (N > 369)", and that "any OTHER defect
  kind … is a regression from plans 02-04".
- **Measured:** run 1 exited **0** with `✅ No defects` at floors 45/369 on a
  46-file / 380-arm corpus. The runner's gate paths are
  `if (annotatedFiles < filesFloor)` and `if (bitingArms < armsFloor)`, so a floor
  BELOW the corpus is invisible to it by construction.
- **Counterfactual that justified acting on this rather than around it:** had I
  taken the plan literally, the only ways forward were to declare a regression in
  plans 02-04 that does not exist, or to pin the floors having observed only ONE
  direction. Both put a number into a ratchet with evidence it does not have.
- **Fix:** the stale direction was measured where it is actually enforced —
  `src/__tests__/mutation-runner-floors.test.ts` on the same tree, which failed
  with `RATCHET STALE: 46 of 73 gate files are now annotated but FILES_FLOOR is
  still 45` and the paired ARMS_FLOOR message (7 failures in total). Both readings
  are in `164.7-05-FLOORS.log`, and both RE-DERIVED blocks now name which layer
  reports which direction, with a ⚠️ telling the next plan not to expect otherwise.
- **Files:** `scripts/mutation-runner/run.mjs`, `164.7-05-FLOORS.log`
- **Commit:** `9aa08bc7`

### 2. [Rule 3 — Blocking] The `<automated>` blocks `cd` to the MAIN checkout, not this worktree

- **Found during:** Task 1.
- **Issue:** every verify chain opens `cd /Users/<user>/claude-projects/quantalyze`.
  This executor ran in `.claude/worktrees/agent-abc52246958c98fda`. Run literally,
  the chains would have measured a tree containing none of this plan's work.
  ⚠️ The danger is the PASSING case: `grep -c … | grep -q '^0$'`-shaped legs pass
  vacuously against a tree that never had the change.
- **Fix:** the `cd` TARGET and only the `cd` target was pointed at the worktree
  root. Every other byte verbatim. The chains were run as `bash <file>` because
  the worktree isolation guard refuses inline compounds of that length; that does
  not change their semantics. Booked as `[PLANVERIFY-CD-01]`; all four prior
  executors deviated identically.
- **Verification:** T1 exit 0, T2 exit 0, T3 exit 0 (see below).

### 3. [Rule 3 — Blocking] Task 1's `--self-test` leg needs `PGBIN`, and fails misleadingly without it

- **Found during:** Task 1 verify.
- **Issue:** `run.mjs --self-test` spawns real lanes. Without
  `PGBIN=/opt/homebrew/opt/postgresql@16/bin` the PATH `pg_ctl` is the PG18
  shim, which carries no pg_cron, and the self-test failed with 11 assertions
  including `lane-unrunnable` and `baseline` defects — a reading that looks like
  a corpus regression and is a toolchain miss.
- **Fix:** `PGBIN` exported in the verify script. With it: `SELF-TEST PASSED`.
  Same environment note plan 02 recorded.

### 4. [Rule 3 — Blocking] Task 3's verify uses `wc -l` output that BSD `wc` pads

- **Found during:** Task 3 verify.
- **Issue:** `test "$(… | wc -l)" = "1"` fails on macOS because BSD `wc` prints
  `       1`. MEASURED: the leg failed while `grep -c` returned exactly 1.
- **Fix:** the leg was re-run with `| tr -d "[:space:]"` and passed. Its INTENT —
  the falsified `ALTER DATABASE … app.admin_email` statement must SURVIVE in
  `docs/runbooks/match-engine.md`, quoted inside its correction — is satisfied.
  Recorded in `deferred-items.md` D-164.7-05-2; on GNU `wc` (CI) the leg passes
  as written.

### 5. [Rule 2 — Missing critical functionality] `ci.yml`'s DATED expected-RED note would have become a lie

- **Found during:** Task 2.
- **Issue:** `ci.yml` carried "the corpus step below is EXPECTED RED between this
  commit and phase 164.7 plan 05's annotation commit". Leaving it after plan 05
  lands leaves a sentence in CI telling the next reader that a RED app-GUC step is
  expected — the exact instruction that would let a real regression through.
  Plan 01's own "Next Phase Readiness" assigns the deletion to plan 05.
- **Fix:** closed in place with the rule that a red step from here is a
  REGRESSION and is never cleared by widening the allowlist or relaxing
  `DETECT_RE`. `ci.yml` is in plan 05's `files_modified` (conditionally, for the
  timeout); this uses that slot.
- **Commit:** `e12ef4c6`

### 6. [Rule 3 — Blocking, PLAN PREMISE STALE] The `timeout-minutes` rule has no 15 left to raise

- **Found during:** Task 1.
- **Issue:** the plan says "raise `sql-mutation`'s `timeout-minutes` 15 → 20 ONCE
  … otherwise leave 15". Measured: it is **already 20**. The one permitted raise
  was taken on 2026-09-05 (ubuntu run 33973362161, 646 s = 10.8 min), and 20 is a
  declared CEILING whose next crossing is answered by `[REDUNDER-SUBSET-SPLIT]`.
- **Fix:** no change; the READING is recorded in `ci.yml` beside the existing
  derivation and in the floors log — 472 legs at 531 s / 528 s (mean 1.1 s/arm)
  plus one contaminated 614 s run, none of them an ubuntu number.

### 7. [Rule 4 — NOT fixed, recorded] `npm run lint` is RED from 86 pre-existing planning-hygiene violations

- **Found during:** Task 3 verify's last leg.
- **Issue:** `npx tsx scripts/check-planning-hygiene.ts` reports **86 violations
  across 13 files**, every one a `.planning/phases/164.7-…/` artifact written by
  the planner or by plans 01-04 — hardcoded `/Users/<user>/…` `cd` targets and
  quoted agent scratchpad directory names, leaking the local username on a public
  repo. It is the last leg of `npm run lint` (`package.json:11`), so the branch's
  lint gate is red. eslint itself is clean here: 0 errors, 2 pre-existing warnings.
- **PROVEN to predate this plan:** `git diff --stat d193e4cd HEAD` over those 13
  files is EMPTY — plan 05 modified none of them.
- **Why not fixed:** the remedy edits three PENDING plans (05/06/07) whose
  `<automated>` blocks two future executors are about to read. That is a scope
  decision about other agents' inputs — Rule 4, not Rule 3.
- **Recorded:** `deferred-items.md` D-164.7-05-1, with the exact remedy, and in
  `.planning/WINDOWS.md`. Owner: plan 06, whose own must-have truth is that
  "nothing else is red for a reason this phase introduced".

---

**Total deviations:** 6 auto-fixed (1 × Rule 1, 4 × Rule 3, 1 × Rule 2) + 1
recorded-not-fixed (Rule 4).

## Neuter evidence (Task 2)

Full log: `164.7-05-NEUTER.log`. Byte backups in the session scratchpad, never in
the repo. **`git checkout --` appears zero times.**

| # | What was broken | Observed RED | Exit |
|---|---|---|---|
| N1 | header `occurrences` 5 → 6, allowlist untouched | `header-count-mismatch` + `header-not-allowlisted` (2 findings — the two-edit design bites twice) | 1 |
| N2 | allowlist `occurrences` 5 → 6, header untouched | `header-not-allowlisted` naming both counts | 1 |
| N3 | one MORE app-GUC read appended to an annotated file | `header-count-mismatch`, message: "fix it, do not raise the count" | 1 |
| N4 | allowlist entry deleted, header left in place | `header-not-allowlisted` (T-164.7-01) | 1 |
| N5 | the same append, run against the committed VITEST | 3 arms fell, incl. the anti-vacuity half of the census arm | 1 |

`BEFORE` == `AFTER` sha256 on both files. ⚠️ N4's first attempt ABORTED on a guard
that matched the script's own doc prose rather than an allowlist entry; the abort
is recorded rather than edited out, and the guard was re-cut to count entries.

## Verification Status

| Check | Result |
|---|---|
| Task 1 `<automated>` verify, at final HEAD | **exit 0** |
| Task 2 `<automated>` verify, at the Task 2 commit | **exit 0** |
| Task 2 verify re-run at final HEAD | all legs pass except the time-dependent `git log -1 \| grep APP-GUC-INTERVAL` (HEAD is now Task 3); the token is in `e12ef4c6`, verified |
| Task 3 `<automated>` verify | all legs pass except the pre-existing hygiene gate (deviation 7) and the BSD `wc` leg (deviation 4) |
| `node scripts/mutation-runner/run.mjs` full corpus | exit 0, 46/73, 380/380/0, no defects |
| `node scripts/mutation-runner/run.mjs --self-test` | SELF-TEST PASSED (with `PGBIN`) |
| `node scripts/lint-app-guc.mjs` / `--self-test` | findings 0, five named / self-test OK |
| `npx vitest run` parser + floors | 187/187 |
| `npx vitest run src/__tests__/lint-app-guc.test.ts` | 28/28 |
| `npx vitest run src/__tests__/contracts` | 110/110 |
| `npx tsc --noEmit` | clean |
| `npm run schema:functions:check` | current (120 functions) |
| `node scripts/lint-sql-gates.mjs` | 73 files, 0 findings |
| `pytest tests/test_ledger_refresh_gates.py tests/test_ledger_refresh_kind_scope_drift.py` | 39 passed |
| `python3 yaml.safe_load('.github/workflows/ci.yml')` | parses |
| `npm run lint` | **RED** — 86 pre-existing planning-hygiene violations (deviation 7); eslint 0 errors |

⚠️ **Not taken, and owed:** a SHA-bound ubuntu run of the 46-file / 380-arm
corpus, and the first ubuntu run of the two `sql-gate-lint` app-GUC steps. Plan 01
deferred that to this commit; it cannot be taken from a worktree executor and
belongs to whoever opens the PR (plan 06).

## Known Stubs

None. `LINEAGE_ALLOWLIST` was plan 01's declared stub and this plan fills it.

## Threat Flags

None. This plan added no network surface, no database access, no endpoint and no
package. The one security-relevant CHANGE is that five applied migrations became
exempt from a static gate — which is why the exemption takes two independent
count-pinned edits and was broken five ways and watched to fail.

## Self-Check: PASSED

All 3 created paths and all 15 modified paths exist on disk; all 3 commit hashes
(`9aa08bc7`, `e12ef4c6`, `68a049b8`) resolve in `git log`. Verified 2026-09-07.
