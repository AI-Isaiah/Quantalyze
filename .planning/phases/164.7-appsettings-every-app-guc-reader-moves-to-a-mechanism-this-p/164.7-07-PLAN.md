---
phase: 164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p
plan: 07
type: execute
subsystem: prod-activation
tags: [founder-gate, prod, ledger-refresh, activation, kill-switch, cron-manifest, criterion-3, criterion-4, 161.1]
wave: 5
depends_on: ["164.7-06"]
files_modified:
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-RECORD.md
  - scripts/prod-prober/cron-manifest.json
  - .planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-VERIFICATION.md
  - docs/runbooks/ledger-refresh-go-live.md
  - TODOS.md
autonomous: false
requirements: [SC-3, SC-4, D-06, 161.1-ACTIVATION]
provides:
  - "A MEASURED pre-flight an agent can take without PROD write access: merge/apply state SHA-bound, issue #747 state and the latest prober's mt5 line, the manifest's jobname set (ledger_refresh_fanout absent until the founder registers it), and the runbook's P3-C query handed to the founder verbatim"
  - "The founder gate: activation as TWO live ops with the which-database marker first, observed via ledger_refresh_staleness (never compute_jobs.status), then the kill-switch proof (flip FALSE, schedule still firing, next tick enqueues 0), then the manifest re-capture — or an explicit, dated DEFER with the blocker named"
  - "The post-activation bookkeeping: committed manifest carries the new job, cron-drift arm green SHA-bound, 161.1-VERIFICATION's human item closed by measurement, the runbook's execution record filled"
affects: [161.1, 164.5]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Criterion 3 is claimed ONLY from ledger_refresh_staleness: the mt5 cohort's `is_stale` count and max `days_since_last_return` went DOWN and strategy_analytics.computed_at advanced — a green compute_jobs row is not cited as success anywhere in the record"
    - "Criterion 4 is claimed ONLY from an observed tick: with the schedule still registered, after the row was set FALSE, the next tick's job_run_details row is `succeeded` and compute_jobs with source ledger-refresh created after the flip timestamp is 0"
    - "P3-C was MEASURED at activation time (mt5 jobs done within 3 days), not inferred from issue #747's state — and if it was stale, the record says ABORTED at P3-C and criterion 3 is reported BLOCKED by an operational fault outside this phase"
    - "Every PROD statement in the record is preceded by the which-database marker's output naming PROD"
    - "The cron manifest was re-captured and committed after registration, and the cron-drift arm read green on the new manifest, SHA-bound"
  artifacts:
    - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md"
      provides: "the agent-measurable pre-flight with dates, run ids and shas"
    - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-RECORD.md"
      provides: "the founder's pasted outputs: marker, P1-P5, BEFORE census, 2a/2b/2c/2d outputs, AFTER census, manifest re-capture run — or the DEFER block"
    - path: "scripts/prod-prober/cron-manifest.json"
      provides: "15 jobs including ledger_refresh_fanout (and the composite job if registered), captured from PROD by --capture-manifest, reviewed"
  key_links:
    - from: "public.system_flags row ledger_refresh_enabled = TRUE"
      to: "compute_jobs rows with metadata.source = 'ledger-refresh'"
      via: "pg_cron 'ledger_refresh_fanout' → enqueue_ledger_refresh_for_strategies() — registered by the founder, never by a migration"
      pattern: "ledger_refresh_fanout"
    - from: "compute_jobs → worker → strategy_analytics.computed_at"
      to: "ledger_refresh_staleness.days_since_last_return"
      via: "the ONLY success signal (criterion 3's own warning)"
      pattern: "days_since_last_return"
---

<objective>
Criteria 3 and 4 are PRODUCTION WRITES and therefore a founder gate (D-06), not an executor step — an agent's PROD read was already denied by the auto-mode classifier during discuss. This plan does the three things an agent CAN do around that gate: measure everything measurable before it, hand the founder a procedure that cannot stall at step one, and verify the aftermath.

**Two live ops, not one (RESEARCH Q7; CONTEXT "NEW BLOCKERS"):** `scripts/prod-prober/cron-manifest.json` — captured from PROD by 164.1 plan 06 — holds 14 jobnames and none is `ledger_refresh_fanout`; `161.1-VERIFICATION.md` measured the same on 2026-08-28. Throwing the switch alone changes nothing. The founder registers the schedule (runbook Step 2, the statement that "lives HERE and NEVER in a migration"), sets the flag, observes, flips back, observes, and re-captures the manifest — because the 164.1 `cron-drift` arm compares jobname sets and reports the new job as drift until the committed manifest carries it.

**P3-C and #747, measured not assumed:** the runbook's HARD GATE P3 Part C requires mt5 `refresh_allocator_equity_daily` / `poll_allocator_positions` jobs `done` within 3 days. Issue #747 (MT5 `initialize()` `-6 authorization failed`, prober run 34018874984, 2026-09-06) was measured at planning time as CLOSED the same day — founder VNC re-login; prober run 34027776532 read `initialize=true connected=true trade_allowed=true`. That is the issue's state, not the gateway's state tomorrow. Task 1 re-measures the issue and the LATEST prober run; the founder runs P3-C itself. If P3-C is stale the runbook says ABORT, and this plan then reports criterion 3 BLOCKED by an operational fault outside this phase — in those words — rather than skipping P3.

**The success signal:** `ledger_refresh_staleness`, never `compute_jobs.status` (criterion 3's own warning: the defect 161.1 fixes wore a green badge for weeks).

Purpose: SC-3, SC-4; closes the 161.1 ACTIVATION human-verification item that this phase exists to unblock.
Output: pre-flight record, activation record, re-captured manifest, closed verification item, runbook execution record.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-CONTEXT.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-RESEARCH.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-06-SUMMARY.md
@docs/runbooks/ledger-refresh-go-live.md
@scripts/prod-prober/cron-manifest.json
@scripts/prod-prober/run.mjs
@.planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-VERIFICATION.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure everything an agent can measure before the gate — merge/apply state, #747, the latest prober, the manifest set</name>
  <files>.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md</files>
  <precondition>The 164.7 PR is MERGED to main and `supabase-migrate.yml`'s apply job has run on that merge commit (both migrations reached PROD) — checked SHA-bound by this task's first step; if either is false the task halts and reports which.</precondition>
  <read_first>
    - `docs/runbooks/ledger-refresh-go-live.md` as corrected by plan 05: P0-P5, Step 1 (UPDATE), Step 2 (register + 2e re-capture), observation queries, Rollback FAST
    - `scripts/prod-prober/run.mjs` header (`--capture-manifest --out <p>`, "a capture is a reviewed file move") and `.github/workflows/prod-prober.yml` (how to find the latest run)
    - `.planning/phases/161.1-…/161.1-VERIFICATION.md` `human_verification` item (the ACTIVATION test and its `why_human`)
    - Memory `reference_ci_green_must_be_sha_bound`; `reference_pg_net_async_cron_success_is_not_http_success`; `project_ledger_venues_no_recurring_strategy_refresh` (last_sync_at LIES — read strategy_analytics.computed_at)
  </read_first>
  <action>
    Write `164.7-ACTIVATION-PREFLIGHT.md` with dated, sourced measurements: (1) `gh pr view <n> --json mergedAt,mergeCommit` and `gh run list --workflow supabase-migrate.yml --limit 3 --json databaseId,headSha,conclusion` — the apply run's `headSha` must equal the merge commit and its conclusion `success`; quote the apply job's log lines naming `20260907120000` and `20260907130000` (via `gh api …/jobs/<id>/logs`); (2) `gh issue view 747 --json state,closedAt` and the LATEST `prod-prober.yml` run's `mtsa`/`mt5:` line (via the job log) — record both, with the sentence that P3-C is a behavioural probe the founder still runs; (3) the committed manifest's jobname set (`node -e` over `scripts/prod-prober/cron-manifest.json`) — assert and record that `ledger_refresh_fanout` is ABSENT (if it is present, someone registered it already: STOP and report); (4) the founder's statement pack, copied VERBATIM from the corrected runbook in execution order with the which-database marker at the head of every block: P0 marker; P1-P5 (P2 with the `guard_is_table_backed` probe and the seed-row check; P3 A/B/C with the C query); P4 BEFORE census; 2a UPDATE TRUE + same-session SELECT; 2b `cron.schedule('ledger_refresh_fanout','25 * * * *', $$SELECT public.enqueue_ledger_refresh_for_strategies();$$)` + the cron.job read-back (and the composite arm's registration as an OPTIONAL second block, the census has one deribit composite); 2c the first-tick observation queries and the AFTER census with the explicit success definition; 2d the flip FALSE + next-tick zero queries; 2e `node scripts/prod-prober/run.mjs --capture-manifest --out scripts/prod-prober/cron-manifest.json` then review the diff (exactly the new row(s), 14 → 15/16 jobs, no command text changed) and commit. State next to 2b that the schedule statement is typed by the founder into the SQL editor and appears in NO migration (gate 3a), that it is the 161.1 runbook's own Step 2 — the founder operation criteria 3/4 require ("with the schedule still registered and firing") — and is therefore INSIDE this phase's scope fence, and that jobid 1 (`match_engine_cron`, the Vault-backed row) is touched by NO 164.7 operation: nothing in this pack or in any plan repoints, unschedules or reschedules it. Add the DEFER template: date, which pre-flight failed, the measured value, "criterion 3 BLOCKED by an operational fault outside this phase", and the re-entry condition.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && P=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-PREFLIGHT.md && test -f "$P" && grep -a -c -E 'apply run: [0-9]{9,} .*headSha: [0-9a-f]{40} .*success' "$P" | grep -v '^0$' && grep -a -c '20260907130000' "$P" | grep -v '^0$' && grep -a -c 'issue #747' "$P" | grep -v '^0$' && grep -a -c 'ledger_refresh_fanout: ABSENT from committed manifest' "$P" | grep -q '^1$' && grep -a -c 'shobj_description' "$P" | grep -v '^0$' && grep -a -c 'days_since_last_return' "$P" | grep -v '^0$' && grep -a -c 'capture-manifest' "$P" | grep -v '^0$' && grep -a -c 'BLOCKED by an operational fault outside this phase' "$P" | grep -v '^0$' && node -e 'const m=require("./scripts/prod-prober/cron-manifest.json");const j=(m.jobs||m).map(x=>x.jobname);if(j.includes("ledger_refresh_fanout")){console.log("already registered");process.exit(1)};console.log("manifest jobs",j.length)'</automated>
  </verify>
  <done>The pre-flight file carries the SHA-bound apply reading, the #747 and latest-prober readings, the manifest absence, and the founder's verbatim statement pack with the marker at the head of every block and the DEFER template.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Activate the 161.1 ledger-refresh switch on PROD now (two live ops + observation + kill-switch proof + manifest re-capture), or DEFER with the blocker named</decision>
  <context>
    Criteria 3 and 4 are production writes (D-06). The founder runs the statement pack in 164.7-ACTIVATION-PREFLIGHT.md in the PROD SQL editor, starting every block with the which-database marker. P3-C is a HARD GATE: if mt5 jobs are not `done` within 3 days, the runbook says ABORT — pick DEFER and paste the P3-C output. Success for criterion 3 is the AFTER census: mt5 rows' `is_stale` count and max `days_since_last_return` DOWN and `strategy_analytics.computed_at` advanced — not a green compute_jobs row. Criterion 4 is the 2d observation: schedule still registered, flag FALSE, next tick enqueues 0. Then 2e: re-capture the manifest and commit, or the cron-drift prober trips on its next hourly run.
  </context>
  <options>
    <option id="activate">
      <name>Activate now</name>
      <pros>Closes criteria 3 and 4 and the 161.1 human item; the four stale mt5 strategies start refreshing; the kill switch is proven real</pros>
      <cons>Two live ops on PROD; ~2 h of observation across two ticks; requires P3-C fresh</cons>
    </option>
    <option id="defer">
      <name>Defer — pre-flight failed</name>
      <pros>Nothing is written to PROD against a failed gate; the blocker is recorded with its measurement</pros>
      <cons>Criterion 3 reported BLOCKED by an operational fault outside this phase; the phase closes with that sentence and a re-entry condition, not with a green</cons>
    </option>
  </options>
  <resume-signal>Reply `ACTIVATED` and paste every block's output (marker, P1-P5, BEFORE, 2a, 2b, 2c, AFTER, 2d, 2e diff + commit sha) — or `DEFERRED` with the failed pre-flight's output</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record the outcome, verify the manifest and the prober SHA-bound, close what the measurement closes</name>
  <files>.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-RECORD.md, scripts/prod-prober/cron-manifest.json, .planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-VERIFICATION.md, docs/runbooks/ledger-refresh-go-live.md, TODOS.md</files>
  <read_first>
    - The founder's pasted outputs from Task 2 (the resume message)
    - `scripts/prod-prober/arms/cron-drift.mjs` header (jobname set equality; the hygiene rules a new command must pass — the ledger command is a bare `SELECT public.enqueue_…();`, so no secret-shape rule applies)
    - `.planning/phases/161.1-…/161.1-VERIFICATION.md` frontmatter `human_verification` list and `status: human_needed`
    - The runbook's execution-record section (1d as rewritten by plan 05)
  </read_first>
  <action>
    Write `164.7-ACTIVATION-RECORD.md` from the pasted outputs, block by block, each headed by its marker output. ACTIVATED path: (1) verify the committed manifest diff is exactly the new row(s) with `command` = the runbook statement and no other row changed; commit it if the founder did not; (2) dispatch the prober (`gh workflow run prod-prober.yml`) or wait for the next hourly run, then read its job log SHA-bound: the `cron-drift` arm must be green on the new manifest and `cron-obs` must show no new defect; quote the lines; (3) compute the criterion-3 verdict FROM the two censuses (BEFORE/AFTER per mt5 row: `days_since_last_return`, `is_stale`, and `strategy_analytics.computed_at` advanced) and the criterion-4 verdict from 2d (`succeeded` tick + 0 rows after the flip timestamp) — write each as a table with the founder's values, and the word PASSED only where the numbers say so; (4) `161.1-VERIFICATION.md`: mark the ACTIVATION human item done with the date, the record's path and the two verdict tables' one-line summaries; flip `status` only if every human item is now closed (read the list); (5) runbook execution record (1d): the row's `updated_at`/`updated_by`, the registration timestamp, the tick times, the manifest commit sha; (6) TODOS: if the founder registered only the single-key arm, book `[164.7-COMPOSITE-ARM-UNREGISTERED]` naming the deribit composite and the statement; note in `[164.7-TEST-APPLY-APPSETTINGS]` whether the TEST hand-apply happened. DEFERRED path: write the DEFER block verbatim, leave the manifest untouched, add `[164.7-ACTIVATION-DEFERRED]` to TODOS with the failed pre-flight, its measured value, and the re-entry condition; the SUMMARY reports criteria 3 and 4 as BLOCKED by an operational fault outside this phase, in those words. Either path: the SUMMARY carries the D-04 scope amendment sentence (the third of its three required places), the CRON-DRIFT-01 GUC-half closure, and the 164.5 hand-off (live-row repoint; `vault-absent` rule; composite arm if unregistered).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && R=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-ACTIVATION-RECORD.md && test -f "$R" && grep -a -c 'which_database' "$R" | grep -v '^0$' && if grep -a -q '^## ACTIVATED' "$R"; then grep -a -c 'days_since_last_return' "$R" | grep -v '^0$' && grep -a -c -E 'criterion 3: (PASSED|FAILED)' "$R" | grep -q '^1$' && grep -a -c -E 'criterion 4: (PASSED|FAILED)' "$R" | grep -q '^1$' && test "$(grep -a -c -i 'compute_jobs.status.*success' "$R")" = "0" && node -e 'const m=require("./scripts/prod-prober/cron-manifest.json");const j=(m.jobs||m);const r=j.find(x=>x.jobname==="ledger_refresh_fanout");if(!r||!/enqueue_ledger_refresh_for_strategies/.test(r.command))process.exit(1);console.log("manifest carries",r.jobname,r.schedule)' && grep -a -c -E 'cron-drift: .*green.*run [0-9]{9,}.*sha [0-9a-f]{40}' "$R" | grep -v '^0$' && grep -a -c 'ACTIVATION' .planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-VERIFICATION.md | grep -v '^0$' && grep -a -c '164.7-ACTIVATION-RECORD.md' .planning/phases/161.1-ledger-refresh-recurring-strategy-refresh-for-ledger-backed/161.1-VERIFICATION.md | grep -v '^0$'; else grep -a -q '^## DEFERRED' "$R" && grep -a -c 'BLOCKED by an operational fault outside this phase' "$R" | grep -v '^0$' && grep -a -c '\[164.7-ACTIVATION-DEFERRED\]' TODOS.md | grep -q '^1$' && git diff --quiet origin/main -- scripts/prod-prober/cron-manifest.json; fi</automated>
  </verify>
  <done>Either: the record carries both verdict tables computed from the founder's censuses, the manifest carries the new job, the prober read green SHA-bound on it, and 161.1's human item points at the record; or: the DEFER block, the untouched manifest and the TODOS entry say exactly why, and the SUMMARY reports criteria 3/4 as blocked in the required words.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder SQL editor → PROD | the only human write path; no automated guard exists (CLAUDE.md), the marker is the guard |
| pg_cron schedule → cross-tenant compute | a registered recurring job enqueuing SECDEF work for every eligible tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.7-26 | Tampering | writing to the wrong database | critical | mitigate | which-database marker at the head of EVERY block; NULL ⇒ stop |
| T-164.7-27 | Denial of Service | activation with MT5 unable to authorise (downgrade stamps) | high | mitigate | P3-C measured at activation; ABORT ⇒ DEFER path |
| T-164.7-28 | Repudiation | a green compute_jobs row cited as success | high | mitigate | verdict computed from the view + computed_at only; verify greps the record for the forbidden claim |
| T-164.7-29 | Tampering | manifest drift after registration | medium | mitigate | 2e re-capture reviewed and committed; prober read SHA-bound |
| T-164.7-30 | Elevation | a schedule statement leaking into a migration | high | mitigate | typed by the founder into the editor; gate 3a scans the window; no migration touched by this plan |
| T-164.7-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Pre-flight file SHA-bound to the apply run; manifest absence asserted before the gate.
- Activation record with marker outputs and verdict tables, or the DEFER block.
- Manifest carries the job and the prober read green on it, SHA-bound.
</verification>

<success_criteria>
- SC-3 and SC-4 are claimed only from the view and the observed tick; or they are reported BLOCKED in the required words with the measurement that blocked them.
- The 161.1 ACTIVATION human item is closed by a record a reviewer can re-derive, or explicitly carried with its re-entry condition.
</success_criteria>

<output>
Create `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-07-SUMMARY.md` when done — the phase SUMMARY that carries the D-04 scope amendment (third place), the VAC-04 quotation from plan 06, the per-setting disposition table (4 settings by name → mechanism → migration/header), and the 164.5 hand-off list.
</output>
