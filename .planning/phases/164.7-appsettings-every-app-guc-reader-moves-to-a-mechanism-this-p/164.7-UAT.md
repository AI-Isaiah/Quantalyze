---
status: complete
phase: 164.7-appsettings
source: 164.7-01-SUMMARY.md, 164.7-02-SUMMARY.md, 164.7-03-SUMMARY.md, 164.7-04-SUMMARY.md, 164.7-05-SUMMARY.md, 164.7-06-SUMMARY.md, 164.7-07-SUMMARY.md
started: 2026-09-22T01:30:14Z
updated: 2026-09-22T01:30:14Z
verified_at_sha: 89cc03588d7716cb2394efea897fdf2374e888d6
---

## Current Test

[testing complete]

## Why this UAT exists, and what kind of UAT it is

⭐ **Nothing was failing.** `164.7-VERIFICATION.md` has carried `status: passed` since
2026-09-12, and all 7 plans have summaries. `phase.uat-passed` returned `false` for
exactly one reason — `no_uat_artifacts: true`. The artifact was simply absent. This file
is it.

⛔ **This is a VERIFICATION-OF-RECORD exercise, not a user-facing walkthrough**, because
Phase 164.7 has no user-visible surface: it moves database GUC readers onto mechanisms
the platform actually grants. Every test below is scored from the repository at the sha
in the frontmatter. ⛔ No test was invented to fill a template slot, and no database
command was run against any remote — this checkout's Supabase CLI is linked to
PRODUCTION, and this phase is *about* database settings, so that restraint is load-bearing
rather than ceremonial.

## Tests

### 1. Criterion 1 — the GUC gate EXISTS, is WIRED into CI, and CAN FIRE
expected: a gate over `supabase/migrations/**` that refuses a reintroduced app-GUC read,
reachable in CI, and carrying a RED fixture proving it fires — *"a gate that cannot fire
is this milestone's named defect"*
result: pass

- **Exists:** `scripts/lint-app-guc.mjs`.
- **Wired:** `.github/workflows/ci.yml` invokes it as **two separate steps, the self-test
  FIRST and the corpus scan second** — so a corpus pass is never trusted before the gate
  has proven it can fail.
- **Can fire — this is the criterion's whole point, and it is satisfied by construction:**
  `node scripts/lint-app-guc.mjs --self-test` →
  `lint-app-guc self-test OK: 4 finding kinds, red+green each (11 red, 2 green fixtures;
  2 kind(s) vitest-covered by declaration).` Exit 0.
- **Corpus verdict:** `app-guc: findings 0`, exit 0.

⛔ Recorded as a VERDICT and a COUNT. The pattern the gate searches for is deliberately
not reproduced here: this repo has a dated case of a proof-of-absence publishing the very
thing it proved absent, and criterion 1's subject is literally a search pattern.

### 2. Criterion 2 — each of the four settings is dispositioned BY NAME, none silently dropped
expected: `analytics_service_key` → Vault; `analytics_service_url` → the settings table;
`ledger_refresh_enabled` → the existing `system_flags` table; each with a migration that
moves it
result: pass

| setting | mechanism | migration carrying the move |
|---|---|---|
| `analytics_service_key` | Vault | `20260911120000_vault_tick_hardening.sql` |
| `analytics_service_url` | settings table | `20260907120000_analytics_service_settings_and_vault_tick.sql` |
| `ledger_refresh_enabled` | `system_flags` | `20260907130000_ledger_refresh_switch_to_system_flags.sql` |

⭐ The Vault move is not a bare lookup: the reader refuses rather than guessing when the
secret is not unique, raising on a multi-row match instead of silently picking one. That
is the fail-loud posture this milestone asks for, present at the point it matters.
⛔ No credential value was read, echoed or reconstructed at any point — only the existence
of the mechanism was established.

### 3. The `app.admin_email` D-04 exemption is honoured as ANNOTATED — not migrated, not deleted
expected: the scope amendment of 2026-09-07 (D-04) is respected: a dated, commented
lineage block, which is the exemption criterion 1 itself defines
result: pass

The occurrences in `20260407164606_perfect_match.sql` sit in `--` comment lines, and the
one-shot `DO $$ … END $$` block begins below them — matching D-04's measured
justification exactly. The block ran once on 2026-04-07 and cannot run again.

⭐ **This is the test most likely to be mis-scored by a later reader, so the reasoning is
recorded rather than the verdict alone.** Migrating it would re-point code that will never
execute; deleting it would edit an APPLIED migration so the repo would no longer describe
the statements that ran against PROD — a cosmetic win paid for with a false permanent
record. ⛔ It is NOT a miss, and the gate agrees: the corpus scan is at 0 findings with
this annotation in place.

⚠️ Criterion 1's gate is a grep, and a grep cannot tell a comment from a statement — the
original census miscounted a commented mention in `20260408113029_cron_heartbeat.sql` as a
read site. That miscount is noted here so it is not inherited a third time.

### 4. The two `regressions:` entries in the verification record are CLOSED at HEAD
expected: the two NEW DEFECT entries recorded at re-verification time are either still
live (a finding) or since closed (a stale record)
result: pass

Both were re-measured at the frontmatter sha and both are closed:

1. The claim that the whitespace key *"is sent as the Authorization header"* — **0
   occurrences of that phrase remain** in `164.7-REVIEWS.md`, and
   `supabase/schema/functions/match_engine_cron_tick.sql` sends `'X-Service-Key'`.
2. The claim that `[VAULTTICK-EMPTYKEY-01]`'s routing did not exist — it is now present
   in `164.7-07-SUMMARY.md` (1), `TODOS.md` (5) and `.planning/ROADMAP.md` (2).

⇒ **The `regressions:` and `gaps:` blocks are a HISTORICAL record of the state at
re-verification time, not live defects**, which is consistent with
`re_verification.gaps_remaining: []` and with this repo's rule that completion is
historical while drift is a separate signal. ⛔ Neither was reopened.

⚠️ **Stated plainly because the frontmatter invites the opposite reading:**
`164.7-VERIFICATION.md` carries `status: passed` beside `score: 5/7`, one gap at
`status: failed`, and two `gaps_partially_closed`. Those co-exist because the score and
gap blocks describe the FIRST verification pass and the `re_verification` block describes
what the fix commit closed. A reader who stops at `score: 5/7` will conclude the phase
failed; a reader who stops at `status: passed` will never notice the question. Both halves
are needed, and this test is the record that someone checked.

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

None. No test produced an issue, so this section stays empty by measurement rather than
by omission.

## Scope note carried forward, not resolved here

⚠️ Two `gaps_partially_closed` entries from the original verification remain *partial* in
the record — the timeline's F1-F6 rows, and `[164.7-MARKER-GREP-VACUOUS]`'s owning phase
not carrying it in the ROADMAP while running after the leg where it bites. ⛔ This UAT does
NOT close them and does not claim to: they were routed to named owners at the time, which
is what flipped the status, and re-litigating a routed deferral here would be exactly the
silent scope creep this repo's deferral rule forbids.
