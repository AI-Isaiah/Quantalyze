---
phase: 164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p
plan: 03
type: execute
subsystem: supabase-migrations
tags: [ledger-refresh, system_flags, fail-closed, secdef, re-base, snapshot, pytest-gates, criterion-2]
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
  - scripts/pg-lane/fixtures/31-fixture-system-flags.sql
  - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
  - supabase/schema/functions/enqueue_ledger_composite_refresh.sql
  - analytics-service/tests/test_ledger_refresh_gates.py
  - analytics-service/tests/test_ledger_refresh_kind_scope_drift.py
  - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-03-NEUTER.log
autonomous: true
requirements: [SC-2, D-01, D-07, 161.1-ACTIVATION]
provides:
  - "20260907130000 — CREATE OR REPLACE of enqueue_ledger_refresh_for_strategies() and enqueue_ledger_composite_refresh(), each re-based on its committed snapshot body, with Lock B rewritten as a fail-CLOSED read of public.system_flags (missing row ⇒ dormant, read error ⇒ dormant, FALSE ⇒ dormant) that stays the FIRST statement; seeds ('ledger_refresh_enabled', FALSE) ON CONFLICT DO NOTHING"
  - "Regenerated snapshots, so VAC-04 will report DRIFT on the PR for exactly these two names (the earned-ack occasion, plan 06)"
  - "The pytest static gates re-anchored on the NEW read, with the three-plus-two invariants that make the fail-closed shape falsifiable by text"
  - "scripts/pg-lane/fixtures/31-fixture-system-flags.sql — the lane stand-in for the table 20260407164606 creates (that migration cannot enter an apply list)"
affects: [164.7-04, 164.7-05, 164.7-06, 164.7-07]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "On a lane with fixture 31 and no seeded row, enqueue_ledger_refresh_for_strategies() returns 0 with a published candidate present (missing row ⇒ dormant) — the C-03 rejection of send-intro's fail-OPEN branch, pinned"
    - "With the row FALSE it returns 0; with the row TRUE and a candidate it returns ≥ 1; with the table RENAMED it returns 0 and does NOT raise — all four observed on ONE lane run (D-01: fail-closed on ANY failure)"
    - "The guard is the first statement of each body: nothing but comments and whitespace sits between the body's BEGIN and the guard's inner BEGIN (pytest gate 4, re-anchored)"
    - "`npm run schema:functions:check` is clean at HEAD and the two snapshot files now carry `-- source migration: 20260907130000_…`"
    - "`python3 -m pytest tests/test_ledger_refresh_gates.py tests/test_ledger_refresh_kind_scope_drift.py` passes from analytics-service/, and each re-anchored assertion was NEUTERED and seen RED before being committed"
    - "The migration contains ZERO cron-schedule/unschedule tokens (gate 3a scans it raw once the window widens) and ZERO app-GUC reads"
  artifacts:
    - path: "supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql"
      provides: "header (no-schedule block, D-01 rationale, C-03 divergence from send-intro stated), seed, two CREATE OR REPLACE bodies keeping the $fanout$/$composite$ dollar tags, REVOKEs, COMMENT ON FUNCTION rewritten, DO $verify$ block; a reserved slot for the prod-body-ack pragma with a ⛔ 'earned, not pasted' note (plan 06 fills it)"
    - path: "scripts/pg-lane/fixtures/31-fixture-system-flags.sql"
      provides: "CREATE TABLE system_flags (key TEXT PRIMARY KEY, enabled BOOLEAN NOT NULL, updated_at, updated_by UUID REFERENCES profiles(id)); ENABLE ROW LEVEL SECURITY; header explaining why no policies (the SECDEF reader bypasses RLS; owner exemption is arm J's subject) and never-a-second-base"
    - path: "analytics-service/tests/test_ledger_refresh_gates.py"
      provides: "_FANOUT_MIGRATION_NAME/_COMPOSITE_MIGRATION_NAME → the new file; window END 20260908000000; _PHASE_MIGRATION_NAMES + new file; 3b/10c re-specified to an EXACT two-name set with the newest as the definer; gate 4 re-anchored (read exactly once, inside EXCEPTION WHEN OTHERS whose handler only WARNs+assigns NULL, IS DISTINCT FROM TRUE, RETURN 0, first statement, no app-GUC read left in the body)"
  key_links:
    - from: "20260907130000 both bodies"
      to: "public.system_flags key 'ledger_refresh_enabled'"
      via: "SELECT sf.enabled INTO v_enabled inside BEGIN … EXCEPTION WHEN OTHERS"
      pattern: "FROM public.system_flags sf"
    - from: "supabase/schema/functions/*.sql"
      to: "scripts/prod-body-drift-check.sh compare loop"
      via: "snapshot now holds the NEW body while PROD holds the OLD → DRIFT → ack required (plan 06)"
      pattern: "source migration: 20260907130000"
    - from: "analytics-service/tests/test_ledger_refresh_gates.py function_body()"
      to: "the $fanout$ block of 20260907130000"
      via: "_FANOUT_MIGRATION_NAME pointer — the runbook's own 'pointer must move with it' rule"
      pattern: "_FANOUT_MIGRATION_NAME"
---

<objective>
The one REAL code change of the phase: `app.ledger_refresh_enabled` — the only setting with LIVE readers — moves from a GUC no operator can set (42501 measured on PROD 2026-09-05) to the EXISTING `public.system_flags` table (D-01), in both fan-out bodies, fail-CLOSED.

**The fail-closed contract, exactly (D-01 + C-03):** the guard stays the FIRST statement of each body. It reads `sf.enabled` from `public.system_flags` for key `ledger_refresh_enabled` inside `BEGIN … EXCEPTION WHEN OTHERS` whose handler does exactly two things — `RAISE WARNING '<fn>: activation flag read failed (SQLSTATE %); treating as dormant', SQLSTATE;` and `v_enabled := NULL;` — then `IF v_enabled IS DISTINCT FROM TRUE THEN RAISE NOTICE '<fn>: dormant (system_flags.ledger_refresh_enabled is not TRUE); enqueued 0'; RETURN 0; END IF;`. `IS DISTINCT FROM TRUE` is the whole point: with `v_enabled` NULL (missing row, read error) both `<> TRUE` and `NOT v_enabled` evaluate NULL and the IF falls THROUGH — they would OPEN the flag on exactly the failure path. A missing row is DORMANT here; that is the deliberate INVERSE of `send-intro/route.ts` branch (2), whose missing-row branch is fail-OPEN by design for a kill switch that defaults ON. Copy its error branch and its `enabled=false` branch; reject its missing-row branch; say so in the body comment.

**Re-base discipline (memory: re-base on the LATEST definition; DRIFT-02):** the LEFT side is `supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql` and `…composite_refresh.sql` (the committed snapshots VAC-04 reads), not the 20260825 files — identical today, but the snapshot is canonical. Everything outside Lock B is copied verbatim: the advisory lock, the candidate query with `LIMIT 4` / `venue_rank <= 2` / `INTERVAL '20 hours'`, the per-candidate handler, the unlock-before-re-raise arm. The `$fanout$` / `$composite$` dollar tags are KEPT — the pytest extractor depends on them.

**What this plan deliberately leaves broken until plan 04:** the two SQL gate files still activate via `set_config` and their 28 edit-kind twins still point at the 20260825 files; on the lane, those twins mutate a body the new migration then overwrites, so arm A reports `no-red` in both files. That is TRAP E doing its job. The full runner exits 1 across this interval; plan 04 re-points, plan 05 pins. Every commit body carries `TWIN-INTERVAL: ledger twins point at 20260825 until 164.7-04 re-points`.

Purpose: SC-2 for `ledger_refresh_enabled`, by name; unblocks 161.1 ACTIVATION (plan 07).
Output: migration B, fixture 31, regenerated snapshots, re-anchored pytest gates, neuter log.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-CONTEXT.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-RESEARCH.md
@.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-PATTERNS.md
@supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
@supabase/schema/functions/enqueue_ledger_composite_refresh.sql
@supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql
@supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql
@supabase/migrations/20260906120000_computation_error_provenance.sql
@analytics-service/tests/test_ledger_refresh_gates.py
@analytics-service/tests/test_ledger_refresh_kind_scope_drift.py
@src/app/api/admin/match/send-intro/route.ts
@scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "missing row ⇒ 0, FALSE ⇒ 0, renamed table ⇒ 0 without raising, TRUE ⇒ enqueues" — migration + fixture + one lane run + snapshots</name>
  <files>supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql, scripts/pg-lane/fixtures/31-fixture-system-flags.sql, supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql, supabase/schema/functions/enqueue_ledger_composite_refresh.sql</files>
  <read_first>
    - Both snapshot files in full — they ARE the bodies to re-base (228 and 232 lines); note the `-- source migration:` line the generator writes
    - `supabase/migrations/20260825130000_ledger_refresh_fanout_dormant.sql` Lock B block (the DECLARE `v_enabled TEXT`, the comment enumerating `'1'`, `'on'`, `'TRUE'`, `'true '`, the `COALESCE(current_setting(…))` read and `IF v_enabled <> 'true' THEN`), the per-candidate `EXCEPTION WHEN OTHERS` handler with its `RAISE WARNING … (SQLSTATE %)` line, the unlock-before-re-raise arm, `COMMENT ON FUNCTION` (says "DORMANT until the app.ledger_refresh_enabled database setting is exactly ''true''" — must be rewritten), the `DO $verify$` block checks (1)-(4) including the owner `rolsuper OR rolbypassrls` predicate (2b) and the ⚠️ TRADE-OFF note, the `BEGIN; SET lock_timeout = '5s';` wrapper
    - `supabase/migrations/20260825140000_ledger_refresh_composite_arm.sql` the same regions for the composite body (its NOTICE names `enqueue_ledger_composite_refresh`)
    - `supabase/migrations/20260906120000_computation_error_provenance.sql` header — the VAC-04 ack block shape (`-- prod-body-ack: <sha>` + MEASURED evidence) and the `-- STEP 2: … re-based on <file>:<range>` citation idiom
    - `src/app/api/admin/match/send-intro/route.ts` the three-branch comment above the system_flags lookup — branches (1) and (3) transfer, branch (2) is REJECTED here
    - `scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql` header — the additive stand-in idiom; `scripts/lint-sql-gates.mjs` rules R5/R6/R7 (a fixture must not shadow a migration's table — the new migration must only INSERT into system_flags, never CREATE it)
    - RESEARCH § Q2 "The repo's own idiom" (the guard text) and § Q2 RLS paragraph (no policy needed: SECDEF owner is RLS-exempt by the migration's own RAISE)
  </read_first>
  <action>
    Write `scripts/pg-lane/fixtures/31-fixture-system-flags.sql`: header in the fixture-12 voice — `20260407164606_perfect_match.sql` creates `system_flags` in production but cannot enter an apply list (reads `auth.users`, FKs `strategies`, creates four unrelated tables), so this additive stand-in mirrors the four columns; RLS is ENABLED with NO policies because the only lane reader is a SECDEF body whose owner bypasses RLS (arm J of the ledger gates proves that exemption) and a policy here would shadow nothing but could mask a future migration's own (R7); never a second base. Body: the CREATE TABLE with `updated_by UUID REFERENCES profiles(id)` (profiles exists from fixture 02) and `ALTER TABLE system_flags ENABLE ROW LEVEL SECURITY;`. No seed row — the migration seeds.

    Write the migration `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql`:
    Header — title; phase/plan/criterion; the ⚠️ OPS auto-apply sentence; a "⛔ what this file does NOT do" block (registers NO schedule — activation is the runbook's Step 2, criterion 3 is a founder op; repoints nothing); a D-01 paragraph (why the EXISTING table, Rule 7); a C-03 paragraph (the send-intro divergence, quoted above); a reserved block titled `VAC-04 ACKNOWLEDGEMENT — to be EARNED on the PR (plan 06)` containing the 20260906120000 evidence-block skeleton with every value written as `<measured in plan 06>` and the ⛔ "had the hashes not matched, FOLD the difference in, never paste the pragma" sentence — and NO `prod-body-ack:` pragma line yet (the gate greps for the pragma with the live hash; a placeholder cannot match anything, but leaving the pragma line absent is the honest state). Keep the header free of cron-schedule/unschedule tokens and of the app-GUC call — write "the app-GUC read" in prose; the migration is inside gate 3a's window after Task 2 widens it and is scanned RAW.

    `BEGIN; SET lock_timeout = '5s';`
    STEP 1 — seed: `INSERT INTO public.system_flags (key, enabled) VALUES ('ledger_refresh_enabled', FALSE) ON CONFLICT (key) DO NOTHING;` with the comment that DO NOTHING means a re-apply never flips a switch an operator set, and that seeding FALSE keeps "merging changes ZERO production behaviour" true. Do NOT `CREATE TABLE IF NOT EXISTS system_flags` (R5).
    STEP 2 — `CREATE OR REPLACE FUNCTION public.enqueue_ledger_refresh_for_strategies() … AS $fanout$ … $fanout$;` re-based on the snapshot, citing `-- re-based on supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql (source migration 20260825130000), Lock B replaced` in the STEP header. DECLARE `v_enabled BOOLEAN;` (was TEXT). Lock B exactly as the objective states, with a rewritten comment: the BOOLEAN NOT NULL column makes the `'1'/'on'/'TRUE'/'true '` class UNREPRESENTABLE rather than rejected (keep the list, say why it no longer needs a comparison), missing row ⇒ dormant ON PURPOSE (the send-intro inverse), read error ⇒ dormant, `IS DISTINCT FROM TRUE` because the NULL-unsafe forms open the flag on the failure path, and "resetting the row is the incident-pressure kill switch: the next tick returns 0 with no schedule operation, no deploy, no migration". Everything else verbatim from the snapshot. Then `REVOKE ALL ON FUNCTION … FROM PUBLIC, anon, authenticated;` and `COMMENT ON FUNCTION` rewritten to name `public.system_flags` key `ledger_refresh_enabled` = TRUE as the activation condition.
    STEP 3 — the same for `public.enqueue_ledger_composite_refresh()` with `$composite$` tags, its own NOTICE/WARNING prefix `enqueue_ledger_composite_refresh:`, re-based on its snapshot.
    STEP 4 — `DO $verify$`: DECLARE everything (42601 note copied); checks per body: exists with `pronargs = 0`; `prosecdef`; `proconfig` has `search_path=`; owner `rolsuper OR rolbypassrls` (2b, the corrected predicate); anon/authenticated EXECUTE both FALSE; `pg_get_functiondef` contains `FROM public.system_flags` AND `IS DISTINCT FROM TRUE` AND does NOT contain the app-GUC call (strip `--` comment lines first, then assert on the executable text — the D-05 caveat about matching an in-body comment); the seed row exists AND `enabled = FALSE` at apply time ("Lock B is still closed after merge" — RAISE if TRUE with the message that an operator flipped it before this migration applied, which is a state to STOP and read, not to overwrite). House style `RAISE EXCEPTION 'Migration 20260907130000: <what> — <why>'`. Closing `RAISE NOTICE 'Migration 20260907130000: applied DORMANT (system_flags.ledger_refresh_enabled = false; NOTHING scheduled)'`. `COMMIT;`.

    Tracer run on the lane — write the gate to `/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p03-tracer/gate.sql`, `--workdir` under that same directory, and capture stdout+stderr to `…/p03-tracer/out.log` (the verify reads that exact path); apply list = the fanout gate's current `RED-UNDER-SETUP` list PLUS fixture 31 inserted before `20260825130000` PLUS this migration appended last: (a) seed a published candidate the way arm A of `test_ledger_refresh_fanout.sql` does (read that arm's seeding statements and copy them); DELETE the flag row; call → must return 0; (b) INSERT the row FALSE; call → 0; (c) SAVEPOINT; `ALTER TABLE public.system_flags RENAME TO system_flags_gone`; call inside BEGIN/EXCEPTION — a raise is `TEST FAILED (T-c)`; return must be 0; ROLLBACK TO SAVEPOINT; (d) UPDATE the row TRUE; call → must return ≥ 1 and a `compute_jobs` row with `metadata->>'source' = 'ledger-refresh'` must exist. Print `TRACER-03 OK` with the four values. Assert the migration's applied NOTICE printed.

    Then `npm run schema:functions` and confirm BOTH snapshot files now say `-- source migration: 20260907130000_ledger_refresh_switch_to_system_flags.sql` and carry the new Lock B; `npm run schema:functions:check` exits 0. Commit with `TWIN-INTERVAL:` in the body.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && M=supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql && test -f "$M" && test -f scripts/pg-lane/fixtures/31-fixture-system-flags.sql && test "$(grep -a -c -i 'cron\.schedule\|cron\.unschedule' "$M")" = "0" && test "$(grep -a -v '^\s*--' "$M" | grep -a -c "current_setting('app\.")" = "0" && grep -a -c 'IS DISTINCT FROM TRUE' "$M" | grep -q '^[2-9]' && grep -a -c 'FROM public.system_flags sf' "$M" | grep -q '^2$' && grep -a -c '\$fanout\$' "$M" | grep -q '^2$' && grep -a -c '\$composite\$' "$M" | grep -q '^2$' && grep -a -c "ON CONFLICT (key) DO NOTHING" "$M" | grep -v '^0$' && test "$(grep -a -c 'CREATE TABLE IF NOT EXISTS system_flags\|CREATE TABLE IF NOT EXISTS public.system_flags' "$M")" = "0" && test "$(grep -a -c '^-- prod-body-ack:' "$M")" = "0" && grep -a -c 'source migration: 20260907130000' supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql supabase/schema/functions/enqueue_ledger_composite_refresh.sql | grep -c ':1$' | grep -q '^2$' && npm run -s schema:functions:check && S=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/11a6e3f6-eb0f-46ac-ba88-a3d6767e5dd8/scratchpad/p03-tracer && test -f "$S/out.log" && grep -a 'Migration 20260907130000: applied DORMANT' "$S/out.log" && grep -a 'TRACER-03 OK' "$S/out.log" && git log -1 --format=%B | grep -a -c 'TWIN-INTERVAL:' | grep -v '^0$'</automated>
  </verify>
  <done>The migration re-defines both bodies with the fail-closed table read as first statement, seeds FALSE, schedules nothing, carries no pragma yet; the lane tracer observed 0/0/0-without-raise/≥1 across the four states; both snapshots regenerated to the new source; the snapshot check is clean.</done>
</task>

<task type="auto">
  <name>Task 2: Re-anchor the pytest static gates on the NEW read — every constant that pinned the old design, moved in this commit</name>
  <files>analytics-service/tests/test_ledger_refresh_gates.py, analytics-service/tests/test_ledger_refresh_kind_scope_drift.py</files>
  <read_first>
    - `analytics-service/tests/test_ledger_refresh_gates.py`: `_FANOUT_MIGRATION_NAME`, `_COMPOSITE_MIGRATION_NAME`, `_ACTIVATION_SETTING`, `_PHASE_MIGRATION_WINDOW_START/END`, `_PHASE_MIGRATION_NAMES`, `_PHASE_IDENTIFIER_TOKENS`, `function_body()` / `composite_body()` (the `$fanout$`/`$composite$` extractors), `test_3a_no_schedule_in_any_phase_migration` (floors 1/2/2b/3 — read the whole method), `test_3b_fanout_function_name_appears_in_exactly_one_migration` and its `10c` composite mirror, `_ACTIVATION_READ_RE`, `_assert_activation_is_exact_lowercase_true`, `TestGate4ActivationLock.test_activation_is_exact_lowercase_string_equality` and the `10d` composite mirror, `_strip_sql_comments`
    - `analytics-service/tests/test_ledger_refresh_kind_scope_drift.py` `_FANOUT_MIGRATIONS`
    - RESEARCH § Q2 table rows for this file (what breaks, what to do) — followed here row by row
    - Memory `reference_pytest_must_run_from_analytics_service_dir` (run from `analytics-service/`, `python3`)
  </read_first>
  <action>
    Before editing, MEASURE the blast radius: from the repo root, `grep -rln "20260825130000\|20260825140000\|app.ledger_refresh_enabled" analytics-service/tests src/__tests__ scripts docs .github` and record the list in the SUMMARY; every hit outside the two files this task owns and the runbook (plan 05) must be either explained as unaffected or added to this task's edit set (that is the "move in the same commit" rule — a stale pointer is a gate that guards nothing).

    In `test_ledger_refresh_gates.py`, in ONE commit: (1) point `_FANOUT_MIGRATION_NAME` and `_COMPOSITE_MIGRATION_NAME` at `20260907130000_ledger_refresh_switch_to_system_flags.sql` (both bodies live in one file; the extractors still find `$fanout$`/`$composite$`), keeping the two 20260825 names as new constants `_FANOUT_ORIGINAL_MIGRATION_NAME` / `_COMPOSITE_ORIGINAL_MIGRATION_NAME` for the lineage assertions below; (2) `_PHASE_MIGRATION_WINDOW_END = "20260908000000"` with a dated comment (why widened, which files enter: 20260907120000 and 20260907130000 — both scanned RAW for schedule tokens by floor 3, which is exactly why neither may carry one even in prose); (3) `_PHASE_MIGRATION_NAMES` gains the new file; (4) replace `_ACTIVATION_SETTING` with `_ACTIVATION_FLAG_KEY = "ledger_refresh_enabled"` and re-derive `_PHASE_IDENTIFIER_TOKENS` so every token still resolves in at least one in-window migration (floor 2) — keep a lineage token for the retired GUC name only if a scanned migration still carries it (the 20260825 files do, so floor 2 holds either way; decide, and say which in the comment); (5) re-specify 3b and 10c: `hits` must equal EXACTLY the two-name set `{original, new}` (assert the set, not the length), the NEW one must contain `CREATE OR REPLACE FUNCTION public.<name>` and the original must too (both define; the newest wins by timestamp order — say so), and NEITHER may contain the schedule tokens or a `PERFORM public.enqueue_…` call; rewrite the docstring/message which today anticipates only "REGISTERS A SCHEDULE or CALLS it"; (6) gate 4: rename to `test_activation_is_fail_closed_table_read` (and the 10d mirror), re-anchor `_ACTIVATION_READ_RE` on `SELECT\s+sf\.enabled\s+INTO\s+(\w+)\s+FROM\s+public\.system_flags\s+sf\s+WHERE\s+sf\.key\s*=\s*'ledger_refresh_enabled'` and assert, on comment-stripped body text, ALL of: exactly one such read; the read sits inside a `BEGIN … EXCEPTION WHEN OTHERS THEN … END;` block whose handler text contains exactly one `RAISE WARNING` (carrying `SQLSTATE`) and one `<var> := NULL;` and nothing else (no SELECT INTO, no DML — the R1 discipline at the source); the first statement after the outer `BEGIN` is that inner `BEGIN` (regex over the stripped text: `BEGIN\s+BEGIN\s+SELECT sf\.enabled` — "first statement" made literal); the comparison that follows is `IF <var> IS DISTINCT FROM TRUE THEN` followed within the IF by `RETURN 0;`; the body contains NO app-GUC read (negative assertion on stripped text); and, for the F16 lesson, add the two NULL-unsafe spellings (`IF NOT <var> THEN`, `IF <var> <> TRUE THEN`) as an explicit "must NOT appear" list with the sentence explaining they open the flag on the failure path; (7) keep every other gate (LIMIT 4, per-venue cap, INTERVAL '20 hours', REVOKE, owner exemption …) untouched — they must pass on the new body unchanged, which is itself the proof the re-base preserved them.

    In `test_ledger_refresh_kind_scope_drift.py`, add the new migration path to `_FANOUT_MIGRATIONS` (tuple of three) with a comment that the newest is the live body and the originals are lineage.

    Run from `analytics-service/`: `python3 -m pytest tests/test_ledger_refresh_gates.py tests/test_ledger_refresh_kind_scope_drift.py -q` → all pass. Then the neuter proof for the re-anchored gate 4, in the scratchpad against a COPY of the migration (never the tracked file, never `git checkout --`): monkeypatch `_migrations_dir()` or copy the tree, and confirm gate 4 goes RED for each of: (i) `IS DISTINCT FROM TRUE` → `<> TRUE`; (ii) the handler assigning TRUE instead of NULL; (iii) a statement inserted before the inner BEGIN; (iv) the read deleted (0 reads); (v) `IF NOT v_enabled THEN`. Record each RED's assertion message in `164.7-03-NEUTER.log` (Task 3 finalises the log). Also run `python3 -m mypy --strict tests/test_ledger_refresh_gates.py` if the suite's mypy config covers tests (measure by reading `analytics-service/pyproject.toml`/`mypy.ini`); record the result either way.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze/analytics-service && python3 -m pytest tests/test_ledger_refresh_gates.py tests/test_ledger_refresh_kind_scope_drift.py -q 2>&1 | tail -3 | grep -a -E '[0-9]+ passed' && grep -a -c '20260907130000_ledger_refresh_switch_to_system_flags.sql' tests/test_ledger_refresh_gates.py | grep -v '^0$' && grep -a -c '_PHASE_MIGRATION_WINDOW_END: Final\[str\] = "20260908000000"' tests/test_ledger_refresh_gates.py | grep -q '^1$' && grep -a -c 'def test_activation_is_fail_closed_table_read' tests/test_ledger_refresh_gates.py | grep -v '^0$' && grep -a -c 'IS DISTINCT FROM TRUE' tests/test_ledger_refresh_gates.py | grep -v '^0$' && test "$(grep -a -c 'def test_activation_is_exact_lowercase_string_equality' tests/test_ledger_refresh_gates.py)" = "0" && grep -a -c '20260907130000' tests/test_ledger_refresh_kind_scope_drift.py | grep -v '^0$' && cd .. && python3 - <<'PY'
import re,pathlib
src=pathlib.Path("analytics-service/tests/test_ledger_refresh_gates.py").read_text()
assert "assert len(hits) == 1" not in src, "3b/10c still a bare count"
print("3b/10c re-specified")
PY</automated>
  </verify>
  <done>Both pytest files pass on the new body; every pointer constant names the new migration; the window is widened; 3b/10c assert an exact two-name set; gate 4 asserts the fail-closed shape (read once, inside the handler, NULL on error, IS DISTINCT FROM TRUE, first statement, no GUC read, no NULL-unsafe spelling) and was seen RED under five neuters.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter the MIGRATION's guard on the lane and the verify block's own check — watch each go RED, restore by hash</name>
  <files>.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-03-NEUTER.log</files>
  <read_first>
    - Plan 01 Task 3's protocol (byte backup, sha256 BEFORE/AFTER, `git checkout --` banned, restore verified by grepping the newest edit as well as by hash)
    - The Task 1 tracer gate in the scratchpad (re-usable as the oracle here)
  </read_first>
  <action>
    Byte-back-up the migration with sha256. Neuters, each restored and hash-verified before the next: (N1) `IS DISTINCT FROM TRUE` → `= FALSE` in the fanout body ONLY (edit the unique text including the fanout NOTICE line that follows) — rerun the Task 1 tracer gate on the lane: state (a) missing row must now go RED (`TEST FAILED (T-a)`: returned non-zero); (N2) handler `v_enabled := NULL;` → `v_enabled := TRUE;` (fanout) — state (c) renamed table must go RED; (N3) move the guard BELOW the advisory-lock block — the migration's OWN verify block must NOT catch this (it is a text check on presence, not position; say so) but pytest gate 4 must: run it and record the RED; (N4) delete the verify block's `enabled = FALSE` seed check and set the seed to TRUE — the tracer's state (b) must go RED. After each: `cp` back, sha256 equal, `grep -c 'IS DISTINCT FROM TRUE'` back to the committed count, lane re-run GREEN once at the end. Finalise `164.7-03-NEUTER.log` with Task 2's five pytest neuters and these four lane neuters (command, exit, RED line verbatim, BEFORE/AFTER hashes), ending `RESTORE VERIFIED: sha256 equal x4; git status clean`. Commit with the interval marker.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && L=.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-03-NEUTER.log && test -f "$L" && grep -a -c 'TEST FAILED (T-a)' "$L" | grep -v '^0$' && grep -a -c 'TEST FAILED (T-c)' "$L" | grep -v '^0$' && grep -a -c 'TEST FAILED (T-b)' "$L" | grep -v '^0$' && grep -a -c -E '^(BEFORE|AFTER) sha256: [0-9a-f]{64}' "$L" | grep -q '^8$' && grep -a 'RESTORE VERIFIED: sha256 equal x4; git status clean' "$L" && grep -a -c 'pytest' "$L" | grep -v '^0$' && test "$(grep -a -c 'git checkout --' "$L")" = "0" && git status --porcelain supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql | wc -l | grep -q '^ *0$' && npm run -s schema:functions:check</automated>
  </verify>
  <done>Four migration neuters each observed RED by the lane tracer or the pytest gate, four hash-verified restores, the log names every one, the migration is clean in git, and the snapshot check is still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| system_flags row → cross-tenant enqueue | one boolean row opens a SECDEF fan-out that enqueues compute for every eligible tenant |
| read failure → guard outcome | a transient fault must land on the closed side, never the open side |
| migration merge → PROD auto-apply | CREATE OR REPLACE of two live SECDEF bodies with no deploy step |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.7-10 | Elevation | fail-OPEN on NULL (missing row / read error) | critical | mitigate | `IS DISTINCT FROM TRUE`; handler assigns NULL; lane tracer states (a)(c); pytest gate 4 negative list; plan 04 arms A/K/L |
| T-164.7-11 | Tampering | a re-apply flipping an operator's switch | high | mitigate | seed `ON CONFLICT DO NOTHING`; verify block RAISES if the row is TRUE at apply time |
| T-164.7-12 | Elevation | REVOKE/owner exemption lost in the re-base | high | mitigate | verify checks (2)(2b)(3) copied; pytest REVOKE/owner gates unchanged and passing on the new body |
| T-164.7-13 | Repudiation | a NOTICE/WARNING carrying an identifier | low | mitigate | counts and SQLSTATE only (T-161.1-10) |
| T-164.7-14 | Tampering | a schedule token entering an in-window migration | high | mitigate | zero tokens (verify grep); gate 3a raw scan after the widen |
| T-164.7-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Lane tracer: four states observed with the migration applied last.
- `npm run schema:functions:check` clean; pytest gates green from `analytics-service/`.
- `164.7-03-NEUTER.log`: nine REDs (five pytest, four lane), all restored by hash.
</verification>

<success_criteria>
- `ledger_refresh_enabled` is dispositioned by name with a migration that moves it to `system_flags`, fail-closed on every path the C-03 correction enumerates.
- Every text gate that pinned the old design moved in the same commit as the body it guards.
</success_criteria>

<output>
Create `.planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-03-SUMMARY.md` when done, with the blast-radius grep result and the tracer's four printed values.
</output>
