# 164.7 Plan 07 — ACTIVATION PRE-FLIGHT

**Measured:** 2026-09-10. Every figure below is bound to a run id, a sha or a file read today.
**Scope:** everything an agent CAN measure without PROD write access. The activation itself is a
founder gate (D-06 / D4) and **nothing in this file touches production.**

## ⛔ HEADLINE — read this before the statement pack

**P3-C is expected to FAIL, and if it does the runbook says ABORT.** The MT5 terminal is failing
authorization again *right now*, and P3-C is a behavioural probe that depends on it.

If the founder runs P3-C and the reading is stale, the honest outcome is to record:

> **criterion 3 BLOCKED by an operational fault outside this phase**

— and to DEFER the activation, not to activate around it. That is the runbook's own instruction and
this plan's own must-have #3.

## 1. Merge and apply state — SHA-bound

| item | value |
|---|---|
| PR | #756, merged 2026-09-07T17:35:36Z |
| merge commit | `14b3b6c34aa555542ac09dfdcdd50d172787f229` |
| apply run: 34148251493 event push headSha: 14b3b6c34aa555542ac09dfdcdd50d172787f229 conclusion success | ✅ headSha EQUALS the merge commit |
| apply job | 101824885370 (`apply`), success; sibling `plan` job 101824819648, success |

Verbatim from the apply job log (fetched with `gh api …/jobs/<id>/logs --allow-escape-sequences`):

```
 • 20260907120000_analytics_service_settings_and_vault_tick.sql
 • 20260907130000_ledger_refresh_switch_to_system_flags.sql
Applying migration 20260907120000_analytics_service_settings_and_vault_tick.sql...
NOTICE (00000): Migration 20260907120000: applied (system_settings seeded and destination-constrained,
                TRUNCATE revoked from authenticated, match_engine_cron_tic…
Applying migration 20260907130000_ledger_refresh_switch_to_system_flags.sql...
NOTICE (00000): Migration 20260907130000: applied DORMANT
                (system_flags.ledger_refresh_enabled = false; NOTHING scheduled)
```

Ledger rows confirm both: `20260907120000 | 2026-09-07 12:00:00` and `20260907130000 | 2026-09-07 13:00:00`.

⭐ **Both migrations ARE on PROD, and 20260907130000 applied DORMANT with nothing scheduled** — the
precondition holds, and the "nothing scheduled" half is stated by the migration itself.

⛔ **Trap, measured today:** `gh api …/jobs/<id>/logs` REFUSES a log carrying terminal escape
sequences, exiting **1 with EMPTY stdout**. An empty log read as "no findings" is a false green.
Always pass `--allow-escape-sequences` and assert a non-zero line count.

## 2. MT5 / issue #747 / the latest prober — the gate at risk

| item | measured 2026-09-10 |
|---|---|
| issue #747 (`-6 authorization failed`, raised 2026-09-06) | **CLOSED** 2026-09-06T10:36:18Z |
| ⛔ issue **#753** `prod-prober: 1 defect(s)` | **OPEN**, created 2026-09-07T07:10, **last updated 2026-09-10T12:31** |
| latest `prod-prober.yml` runs | **34477143827 failure** (12:30), 34451054069 failure (07:39), 34428590952 failure (02:13) — all 2026-09-10 |

The open defect, verbatim from #753:

```
  KIND                        ARM         SUBJECT
  mt5-terminal-error          mt5         -6
      MT5 initialize() failed with code -6, which is neither -10004 nor -10005 —
      the IPC transport is not implicated, so neither IPC remedy applies.
```

⚠️ **#747 being closed is a statement about 2026-09-06 and discharges nothing.** The runbook says
so itself: *"a closed issue is a statement about the past — the MT5 gateway is on record wedging
three times in a single day. Run the query at activation time and read its answer."* The runbook's
dated 2026-09-07 note (terminal re-logged, prober run 34027776532 read `initialize=true`) is now
**itself superseded by measurement**: the `-6` is back and has been firing hourly for three days.

⭐ Side note, not a blocker: the remedy text above says *"the bridge is fine and the fault is inside
MT5 itself. Read the reported code against the MT5 error table"* — sending the operator to an error
table when the real cause is that the terminal has no authorized account. **That misdirection is
exactly what Phase 164.8.3 PROBERAUTH exists to fix.**

**P3-C is a BEHAVIOURAL probe over `compute_jobs` and only the founder can run it.** It is in the
statement pack below as P3-C.

## 3. The committed manifest

```
count: 14
ledger_refresh_fanout: ABSENT from committed manifest
```

Jobnames: `api_key_rotation_reminder, audit_log_cold_purge, audit_log_hot_to_cold,
compute_bridge_outcome_deltas, match_engine_cron, poll-allocator-positions,
reap_strategy_analytics_stuck_computing, reconcile_dropped_enqueue_sweep,
refresh-allocator-equity, resend_message_correlation_retention_90d, retention_compute_jobs_done,
retention_compute_jobs_failed, retention_compute_jobs_orphaned_running,
retention_notification_dispatches`

✅ Nobody has registered it already. If a future run of this check finds it PRESENT, **STOP** — the
schedule was registered out of band and this pre-flight's assumptions are void.

⛔ **`match_engine_cron` (jobid 1) is touched by NO 164.7 operation.** Nothing in this pack and
nothing in any 164.7 plan repoints, unschedules or reschedules it. That row is **Phase 164.5.1's**
subject, and the two must not be run in the same session.

## 4. The founder's statement pack — verbatim, in execution order

⛔ **P0 goes at the head of EVERY session that writes.** `current_database()` is `postgres` on both
projects and proves nothing.

### P0 — which database is this? (BLOCKING, FIRST)

```sql
SELECT shobj_description(oid, 'pg_database') AS which_database
  FROM pg_database WHERE datname = current_database();
```

**Expected:** the hand-set marker naming **PROD**. **Abort if** it names anything else or comes back
NULL — a NULL means the `COMMENT ON DATABASE` was lost; re-set it rather than proceed on a guess.
The CLI and the browser SQL editor have **no** automated production guard; this marker is the only one.

### P3-C — the hard gate, and the one expected to fail

```sql
SELECT cj.kind,
       count(*)          AS done_jobs,
       max(cj.completed_at) AS newest
  FROM compute_jobs cj
  JOIN api_keys ak ON ak.id = cj.api_key_id
 WHERE ak.exchange = 'mt5'
   AND cj.kind IN ('refresh_allocator_equity_daily', 'poll_allocator_positions')
   AND cj.status = 'done'
   AND cj.completed_at > now() - INTERVAL '3 days'
 GROUP BY cj.kind;
```

**Expected:** both kinds present, healthy counts, `newest` within hours (2026-08-25 pre-tracer
reading: 12 `done` of each, newest 5 h old).
**ABORT if:** either kind is absent, or `newest` is stale by more than a day.

Run P1, P2, P4 (BEFORE census) and P5 from the runbook (`docs/runbooks/ledger-refresh-go-live.md`,
sections P1 :165, P2 :181, P3 :208, P4 :269, P5 :287) — they are unchanged and are not reproduced
here to avoid a second copy that can rot.

### Step 1 — the activation flag (LIVE op 1 of 2)

Runbook 1a (:350) throw it, 1b (:365) verify **in the SAME session**, 1d (:389) execution record.

### Step 2 — the schedule (LIVE op 2 of 2)

```sql
SELECT cron.schedule(
  'ledger_refresh_fanout',
  '25 * * * *',
  $$SELECT public.enqueue_ledger_refresh_for_strategies();$$
);
```

Read-back:

```sql
SELECT jobname, schedule, active FROM cron.job WHERE jobname = 'ledger_refresh_fanout';
```

**Expected:** one row, `25 * * * *`, `active = t`.

⛔ **This statement is typed by the founder into the SQL editor and appears in NO migration**
(gate 3a). Minute 25 is chosen, not arbitrary: it clears the `*/15` reaper grid, `match_engine_cron`
at `:00`, `reconcile_dropped_enqueue_sweep` at `:35`, the terminalizer at `:50`, and every occupied
daily slot.

### Observation — criterion 3's ONLY success signal

⛔ **Claim criterion 3 ONLY from `ledger_refresh_staleness`** — the mt5 cohort's `is_stale` count and
max **`days_since_last_return`** going DOWN, and `strategy_analytics.computed_at` advancing.
**NEVER from `compute_jobs.status`**: the defect 161.1 fixes wore a green badge for weeks, and
`last_sync_at` LIES — read `strategy_analytics.computed_at`.

### Kill-switch proof — criterion 4

With the schedule **still registered and firing**: set the flag FALSE, then show the next tick's
`job_run_details` row is `succeeded` AND `compute_jobs` with source `ledger-refresh` created after
the flip timestamp is **0**. Runbook :529.

### 2e — re-capture the manifest, IMMEDIATELY after registering

```bash
node scripts/prod-prober/run.mjs --capture-manifest --out scripts/prod-prober/cron-manifest.json
```

**Expected:** the diff ADDS exactly one job — `ledger_refresh_fanout`, `25 * * * *`, active — and
changes nothing else. **Abort if** any other job moved.
⚠️ Read the `command` text of every job in the diff before committing: the manifest is a committed,
world-readable file in a **public** repository.

## 5. DEFER template — use this if P3-C is stale

```
DEFERRED <date>
Pre-flight that failed:  P3-C (mt5 compute_jobs done within 3 days)
Measured value:          <paste the query's actual output>
Verdict:                 criterion 3 BLOCKED by an operational fault outside this phase
Re-entry condition:      MT5 terminal authorized (issue #753 closed, prod-prober mt5 arm green),
                         then re-run P3-C and resume at Step 1.
```

## 6. What this pre-flight does NOT establish

- **P1, P2, P3-A/B, P4, P5** — all require PROD reads; the founder runs them.
- **Whether `system_flags.ledger_refresh_enabled` is FALSE right now.** The migration applied it
  dormant on 2026-09-07 and no operation since is recorded as flipping it, but that is an argument
  from absence. The founder's P2/1b reads settle it.
- ⛔ Nothing here proves the worker deploy is healthy. Railway skips a deploy when the merge
  commit's check-suite is red — P5 exists for that and is not reconstructible from here.
