# 164.7 Plan 07 — SUMMARY

**Completed:** 2026-09-10 (DEFERRED path)
**Outcome:** the gate was reached with everything measurable measured, and the founder deferred on a
failed pre-flight. Nothing was written to production.

## Verdict

**Criteria 3 and 4 are BLOCKED by an operational fault outside this phase.**

Criterion 3 (the mt5 cohort's staleness falls after activation) could not be attempted because its
own hard gate, P3-C, was not met. Criterion 4 (the kill switch proven with the schedule still
firing) is downstream of the activation — nothing was registered, so there was nothing to flip.

⛔ Neither is failed engineering. The blocker is an MT5 gateway outage that ran from **2026-09-07
04:05:08** to **2026-09-10 ~16:10Z** and has nothing to do with this phase's code.

## What was done

| task | outcome |
|---|---|
| 1 — measure everything measurable before the gate | ✅ `164.7-ACTIVATION-PREFLIGHT.md` |
| 2 — founder decision | ✅ **DEFERRED**, with the failed pre-flight and its measurement |
| 3 — record the outcome | ✅ `164.7-ACTIVATION-RECORD.md`, `[164.7-ACTIVATION-DEFERRED]` booked |

**Pre-flight facts, SHA-bound:** apply run **34148251493**, `headSha`
`14b3b6c34aa555542ac09dfdcdd50d172787f229` (= PR #756's merge commit), conclusion success; both
migrations quoted from the apply job log, with `20260907130000` reporting `applied DORMANT
(system_flags.ledger_refresh_enabled = false; NOTHING scheduled)`. The committed manifest holds 14
jobs and `ledger_refresh_fanout` is **absent**, so nobody had registered it out of band.

**Why P3-C failed:** both enqueue crons are **daily** — `poll-allocator-positions` at `0 4 * * *`,
`refresh-allocator-equity` at `0 5 * * *`. MT5 broke five minutes after the 2026-09-07 04:00Z tick,
so the newest usable mt5 job was ~3.5 days old against a 3-day window. No wait inside 2026-09-10
could close that; the 2026-09-11 ticks will.

⭐ **Forcing was considered and declined on blast radius.** The two enqueue functions are
`…_for_all()` and `…_for_all_keys()` — they enqueue for every key at every venue. Triggering a full
cross-venue day-run to manufacture one mt5 row for a pre-flight is a far larger action than the
question warrants, and the activation closes a reproducibility gap rather than an outage.

## The MT5 incident, since it is this deferral's whole cause

Root cause came from the terminal's own Journal, not from the prober:

```
2026.09.07 04:05:03  '26547876': disconnected from VantageMarkets-Live 5
2026.09.07 04:05:08  '34043761': authorization on VantageMarkets-Live 14 failed (Invalid account)
2026.09.10 15:48:25  '34043761': authorization on VantageMarkets-Live 14 failed (Invalid account)
```

The terminal dropped a working session and re-attached to an account it could not authorize.
Resolved 2026-09-10 ~16:10Z by re-entering valid credentials (`authorized … through AS05`,
`investor mode` on trading and balance management). Verified by prober run **34500455961**: its
`Open or update the prod-prober issue` step was **SKIPPED**, and that step is gated on a `^❌` line
in the runner log, so skipped means zero defects. ⚠️ The run's own `conclusion: success` proves
nothing here — the workflow deliberately exits 0 even on a real production defect so Railway's
wait-for-CI is not blocked. Issue #753 closed.

Two instrument defects this exposed were entered into **Phase 164.8.3 PROBERAUTH** as criteria 7 and
8 (`/gsd-phase --edit`, 2026-09-10): the probe records `terminal_info()` as present/absent rather
than as `connected` and `trade_allowed` — the two fields the go-live runbook names as THE
verification — and the narrowed `--arm` diagnostic publishes nothing at all.

## D-04 — the scope amendment sentence, third and final placement

**`app.admin_email` is ANNOTATED, not migrated and not deleted.** The executable text of an applied
migration is the permanent record of what ran on PROD and is not edited; a single `--` lineage
header line in the file's first 40 lines, parsed by `scripts/lint-app-guc.mjs`, carries the
retirement date, the exact count the gate measures and the successor. ROADMAP criterion 2's SCOPE
AMENDMENT of 2026-09-07 carries the reasoning. This is the third of the three required places
(ROADMAP criterion 2 and `164.7-05-PLAN.md` are the other two).

## CRON-DRIFT-01 — the GUC half is CLOSED

Measured at HEAD 2026-09-10: `node scripts/lint-app-guc.mjs` → **`app-guc: findings 0`** across 292
files scanned, with 5 files carrying dated lineage annotations
(`20260407164606`, `20260408113029`, `20260408215026`, `20260825130000`, `20260825140000`). No
`current_setting('app.…')` read remains unannotated anywhere in the tree.

⛔ **The LIVE-ROW half is NOT closed by this phase and never was in its scope.** It is Phase 164.5.1.

## Hand-off to Phase 164.5.1

1. **The live-row repoint.** PROD's `cron.job` jobid 1 still runs the hand-repaired 2026-09-01
   Vault-backed command. `match_engine_cron_tick()` was `measured absent` from PROD at PR time and
   reached PROD when #756 merged and auto-applied — so 164.5.1 points the row at a callable that is
   now present.
2. ⛔ **The `vault-absent` rule is 164.5.1's FIRST work, not a follow-up.** Reproduced by execution:
   `hygieneViolations("match_engine_cron", "SELECT public.match_engine_cron_tick();")` →
   `[vault-absent]`. `cron-drift.mjs:329` demands the literal `vault.decrypted_secrets` in the
   COMMAND TEXT, which the repaired command no longer carries, so `captureManifest` refuses
   (`run.mjs:2497-2512`, exit 1, nothing written) and criterion 2 cannot be met until the rule is
   widened. Booked as `[164.7-VAULT-ABSENT-RULE]`.
3. **The composite arm is UNREGISTERED — and so is the single-key arm.** Because the activation was
   deferred, nothing at all was registered; `[164.7-COMPOSITE-ARM-UNREGISTERED]` is therefore NOT
   booked. It becomes a live question only if a future activation registers the single-key arm alone.
4. ⚠️ **Ordering.** Both a future 164.7 activation and 164.5.1's repoint mutate
   `scripts/prod-prober/cron-manifest.json`. Whichever lands second must re-capture afterward, or
   the `cron-drift` arm reports drift on every hourly run.

## Re-entry

After the 2026-09-11 04:00Z and 05:00Z ticks, re-run P3-C from the pre-flight (P0 marker first). If
both kinds are present with a `newest` within hours, the gate is met and Task 2 can be answered
`ACTIVATED`. The statement pack, observation queries, kill-switch proof and 2e re-capture are all
already written.
