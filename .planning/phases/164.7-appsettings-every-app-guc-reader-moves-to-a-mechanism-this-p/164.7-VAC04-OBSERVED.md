# 164.7 — VAC-04 OBSERVED

**Written:** 2026-09-10, RECONSTRUCTED from the live run rather than from recollection.
**Status:** the observation obligation of plan 06 (D-08, SC-7) is MET, and the evidence below was
re-fetched at write time — it is not a transcription of what someone remembered.

⛔ **Read the provenance note before the tables.** Plan 06 was authored as a PRE-MERGE obligation.
It was never executed as a plan: 164.7's migrations merged on 2026-09-07 as PR #756, and the VAC-04
observation was performed and recorded **in the PR body** instead of here. This file moves that
record into the phase artifacts and re-verifies every claim in it against the run. Nothing here is
reconstructed by inference; every line is quoted from the job log or read from the committed file.

## The run

| field | value |
|---|---|
| workflow | `migration-drift-check.yml` (runs on `pull_request` only) |
| run id | **34146946050** |
| job id | 101820921298 (`check`) |
| head sha | **7c9aea647332549621e3f0d036a2bfc36c41196d** — PR #756's exact head |
| merge commit | `14b3b6c34aa555542ac09dfdcdd50d172787f229` |
| created | 2026-09-07T17:16:55Z |
| conclusion | **success** |

⚠️ **Fetched with `gh api …/actions/jobs/<id>/logs`, per the plan** — `gh run view --log` TRUNCATES
and a clean failure then reads as a hang.
⛔ **NEW TRAP, measured 2026-09-10 while writing this file:** `gh api` REFUSES a log containing
terminal escape sequences, exiting **1 with EMPTY stdout** and the message *"the response contains
terminal escape sequences; pass --allow-escape-sequences to output it anyway"*. An empty log read
as "no drift" would be a false green of exactly the shape this project books. **Use
`--allow-escape-sequences`** and assert the line count is non-zero before drawing any conclusion.
Measured: without the flag `rc=1, bytes=0`; with it, `rc=0, 681 lines`.

## The branch VAC-04 took — must_have #3

The plan required naming the branch **by line reference**, and specifically that it was the compare
loop and **NOT** the `:198` short-circuit and **NOT** the `:448` legitimate-zero. Confirmed from the
log — the gate performed real comparisons against a populated PROD index, which neither of those
two branches does:

```
Functions defined or replaced by this PR: 5
Functions indexed in the PROD source: 120 (union of two independent readings)
```

`:198` (short-circuit on a changed set with no `CREATE … FUNCTION`) is impossible — this PR defines
5. `:448` (legitimate zero) is impossible — the PROD index holds 120 names. This is what
`164.7-RESEARCH.md` Q5 predicted; the prediction is now MEASURED, not assumed.

## The verdict, verbatim

```
VAC-04 repo-vs-PROD function-body drift gate: enqueue_ledger_composite_refresh/0 DRIFTS from PROD
  (snapshot 15e3ba963fa0349eec352e1a5f8a3be5258fe8f34ff996926e99b9d2f72996b5
   vs prod 7c3d33e96f1cbe5a864750ff083670e23ab4c586b8529d48da005006b4d81ac4,
   15 differing line(s)) — acknowledged by a matching '-- prod-body-ack:' pragma.

VAC-04 repo-vs-PROD function-body drift gate: enqueue_ledger_refresh_for_strategies/0 DRIFTS from PROD
  (snapshot adeb6d168187b2dfb22fa4798eb14972e233342e8721d7221323a9311053705f
   vs prod 88e6af8472e4e48175a62b1ee189f7411407b14827d948c81aa015f511ae6e36,
   15 differing line(s)) — acknowledged by a matching '-- prod-body-ack:' pragma.

  latest_cron_success/1: MATCH (125c3c9ba764231fd9805b7a6c2d1606764f0a5cba273c0d81454fd69a546b38)
  match_engine_cron_tick: measured absent — not in the PROD source's 120-name index.
                          Treated as a NEW function (pass).
  send_intro_with_decision/6: MATCH (0e81bb452b719a3d441b199f00d734123f15efc4e163a48c28d02356d50557dc)

VAC-04 repo-vs-PROD function-body drift gate: 4 body comparison(s) — 2 match, 2 acknowledged drift,
                                              1 measured-absent (new) function(s).
VAC-04 repo-vs-PROD function-body drift gate: no unacknowledged repo-vs-PROD body drift.
```

## The ack is EARNED — must_have #2, re-verified byte-for-byte at write time

The pragma must carry **the PROD sha VAC-04 printed**, not a hash copied from anywhere else.
Compared the committed file against the log above, 2026-09-10:

| function | PROD sha in the log | committed pragma | line | match |
|---|---|---|---|---|
| `enqueue_ledger_refresh_for_strategies/0` | `88e6af84…ae6e36` | `-- prod-body-ack: 88e6af84…ae6e36` | `20260907130000:10` | ✅ |
| `enqueue_ledger_composite_refresh/0` | `7c3d33e9…81ac4` | `-- prod-body-ack: 7c3d33e9…81ac4` | `20260907130000:11` | ✅ |

⭐ **And the DRIFT-02 question was settled by REPRODUCTION, not assertion.** Per PR #756's body, the
gate's own normalizer was pointed at `origin/main`'s snapshot as its `live` argument and returned
**the same two hashes and the same 15 differing lines** CI reported against real PROD. So PROD's
live bodies are the repository's last-known committed snapshots on main, and DRIFT-02's shape — an
out-of-band patch the repo has no record of — is **ABSENT**.

## What this says about Phase 164.5.1

`match_engine_cron_tick` was **measured absent** from PROD at PR time (2026-09-07T17:17Z) and
treated as a new function. It reached PROD when this PR merged and auto-applied. Phase 164.5.1
therefore points the live cron row at a callable that is now present — consistent with that phase's
own reading, and now with a dated measurement behind it.

## Limits of this record

- The **live `supabase db dump` path** half of WINDOWS entry 25 is what this run exercised; the
  entry is dispositioned `fixed` on that basis (see `.planning/WINDOWS.md`).
- The `supabase-migrate` **plan-job dry-run** reading (PATTERNS TRAP A — "does a comment-only edit
  to an applied migration change what the CLI plans?") is **NOT** covered here. No such reading was
  taken at ship time and none is reconstructible from an expired context. ⛔ Recorded as still-open
  rather than quietly dropped — see the SUMMARY's deferrals.
