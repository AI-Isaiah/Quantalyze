---
phase: 164.7-appsettings
verified: 2026-09-10T18:45:00Z
status: gaps_found
score: 5/7 ROADMAP success criteria met
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: none
gaps:
  - truth: "Criterion 6's evidence artifact records the reviewer findings accurately"
    status: failed
    reason: >
      164.7-REVIEWS.md and 164.7-06-SUMMARY.md both assert that only five of PR #756's
      six reviewer findings are recoverable and that "the sixth is not identifiable from
      the surviving record". Re-derived: all SIX are a closed, named set under one heading
      at TODOS.md:7710 (at commit fcdbcdab, the very commit that wrote REVIEWS.md) — eight
      lines above the first item REVIEWS.md itself cites at :7726. Worse than a miscount:
      three of the five rows REVIEWS.md lists are NOT reviewer findings at all, and four
      genuine reviewer findings are missing, including [VAULTTICK-EMPTYKEY-01], a real
      defect in the Vault guard this phase shipped.
    artifacts:
      - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEWS.md"
        issue: "Findings table is the wrong set; the 'sixth is unidentifiable' claim is false"
      - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-06-SUMMARY.md"
        issue: "Repeats the same false count and the same wrong set"
    missing:
      - "Replace the table with the six entries under TODOS.md heading 'Phase 164.7 (APPSETTINGS) — SQL-fixer + three-reviewer residuals (logged 2026-09-07)': [APPGUC-DETECT-DOUBLEQUOTE-01], [APPGUC-UTF16-01], [APPGUC-WARNING-UNINSTRUMENTED-01], [VAULTTICK-EMPTYKEY-01], [164.7-PLAN03-EVIDENCE-01], [164.7-CITATION-DRIFT-01]"
      - "Move [164.7-VAULT-ABSENT-RULE], [GATE-COMMENT-D2-FALSE] and [164.7-TEST-APPLY-APPSETTINGS] to a separate 'related, not reviewer-sourced' section — GATE-COMMENT-D2-FALSE's own heading calls it 'phase 164.7 red-team' and commit 6913fc37 shows it was BRIEFED TO the reviewers, not found by them"
      - "Delete the sentence 'The sixth is not identifiable from the surviving record'"
  - truth: "Must-have #1's ordering grade is honest"
    status: partial
    reason: >
      164.7-REVIEWS.md grades the pre-merge ordering "MEASURED, NOT PROVEN" and asserts
      "nothing written in 2026-09-10 could prove it". One `gh api
      repos/AI-Isaiah/Quantalyze/pulls/756/commits` call proves it. That is an UNDERCLAIM,
      not an overclaim — but it is a false statement in an evidence artifact.
    artifacts:
      - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-REVIEWS.md"
        issue: "Asserts unprovability of a fact that is provable from the PR's own commit list"
    missing:
      - "Record the timeline: 6913fc37 11:41:48Z (brief the wave-4 reviewers, founder request) -> b4a01504 13:09:29Z (silent-failure-hunter B1/B2) -> f551e265/551d73c1/19694320 13:32-13:33Z (F1-F6) -> 3c1136c6 13:35:14Z (merge reviewer fixes) -> PR #756 CREATED 14:34:18Z -> merged 17:35:36Z"
      - "Narrow the recorded limit to per-reviewer ATTRIBUTION of F1-F6 only (silent-failure-hunter IS attributable by commit message)"
  - truth: "Plan 07's DEFER-branch verify leg can fail"
    status: failed
    reason: >
      The <automated> block at 164.7-07-PLAN.md:149 opens with
      `grep -a -c 'which_database' "$R" | grep -v '^0$'`, applied to BOTH the ACTIVATED and
      the DEFERRED branch. Its truth condition is string presence, so pasting the QUERY
      satisfies it exactly as well as recording an OUTPUT. On the DEFER path — the path
      actually taken — the leg cannot fail and would have passed identically for a
      dishonest record. This is the milestone's own named defect class and it is booked
      nowhere (grep of TODOS.md and the SUMMARYs finds no entry).
    artifacts:
      - path: ".planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-07-PLAN.md"
        issue: "line 149 — precondition outside the branch it belongs to; presence-only oracle"
    missing:
      - "Book the vacuity as a TODOS entry with an owning phase"
      - "At re-entry, move the precondition inside the ACTIVATED branch, or require an OUTPUT-shaped match (e.g. `which_database:[[:space:]]*[^[:space:]]`)"
deferred:
  - truth: "Criteria 3 and 4 — the 161.1 activation thrown on PROD and the kill switch proven"
    addressed_in: "Phase 164.7 plan 07, re-entered at the same founder gate after the 2026-09-11 04:00Z/05:00Z ticks"
    evidence: >
      TODOS.md:1609-1634 [164.7-ACTIVATION-DEFERRED], Owner line: "Phase 164.7 plan 07,
      re-entered at the same gate." ⚠️ This is a SELF-owned deferral, not a forward phase —
      it does not satisfy Step 9b's later-phase test and is listed here for visibility only.
      The blocker itself was independently corroborated (see criterion 3 below).
human_verification:
  - test: "After the 2026-09-11 04:00Z and 05:00Z enqueue ticks, re-run P3-C from 164.7-ACTIVATION-PREFLIGHT.md (P0 marker query FIRST, and record its OUTPUT this time), then decide the founder gate again."
    expected: "Both mt5 kinds present with `newest` within hours; then the two live ops, the view-based observation (ledger_refresh_staleness.days_since_last_return DOWN for the mt5 cohort), the kill-switch proof, and the cron-manifest re-capture."
    why_human: "PROD write access behind a founder checkpoint:decision. This checkout's Supabase CLI is linked to PRODUCTION; no agent may perform it."
  - test: "Decide whether criteria 3 and 4 stay owned by 164.7 plan 07 or are re-routed to a named forward phase."
    expected: "A phase owns them, per the founder rule that every deferral names a PHASE."
    why_human: "Roadmap/queue decision, and the current owner is the deferring phase itself."
---

# Phase 164.7: APPSETTINGS Verification Report

**Phase Goal:** Every `current_setting('app.…')` reader in `supabase/migrations/**` moves to a
configuration mechanism this platform actually grants, and a machine stops the next one being written.
**Verified:** 2026-09-10T18:45:00Z
**Status:** gaps_found (5/7 criteria met; 2 correctly deferred; 3 record defects, one of them material)
**Re-verification:** No — initial verification.

⛔ **Nothing in this verification wrote to any database.** All evidence is file reads, read-only
lint/test scripts, `git`, and `gh api`.

## Goal Achievement — the ROADMAP's seven Success Criteria

| # | Criterion | Status | Evidence I personally re-derived |
|---|---|---|---|
| 1 | Zero `current_setting('app.` outside a dated lineage block, proven by a CI grep with a RED fixture | ✅ **MET** | 12 occurrences remain, all inside the 5 files carrying `-- APP-GUC-LINEAGE:` headers whose declared counts (1+4+5+1+1) sum to exactly 12. `node scripts/lint-app-guc.mjs` → `files scanned 292 / annotated 5 / findings 0`, rc=0. `--self-test` → `OK: 4 finding kinds, red+green each (5 red, 2 green fixtures)`; 7 fixture files present in `scripts/lint-app-guc-fixtures/`. `npx vitest run src/__tests__/lint-app-guc.test.ts` → **28/28 pass**. Wired at `.github/workflows/ci.yml:977` (self-test) and `:979` (corpus) inside `sql-gate-lint`, which the `frontend` aggregator gates. |
| 2 | Each of the 4 settings dispositioned BY NAME with its mechanism and a migration that moves it | ✅ **MET** | `analytics_service_key` → Vault: `20260907120000:322` `FROM vault.decrypted_secrets`, with a post-verify at `:549-550` that RAISEs if the shipped body does not read it. `analytics_service_url` → `public.system_settings` (`CREATE TABLE` `:167`), destination-pinned by CHECK constraint `:214`. `ledger_refresh_enabled` → **existing** `public.system_flags`, seeded FALSE at `20260907130000:188-189`, read at `:252` and `:511` in both fan-outs. `admin_email` → ANNOTATED per the 2026-09-07 D-04 scope amendment (`successor: none` in its lineage header). `grep -n "cron.schedule" ` over both migrations → **0 hits**: neither schedules anything. |
| 3 | ⭐ The 161.1 activation switch is THROWN on PROD and observed working | ❌ **UNMET — DEFERRED at the founder gate; blocker independently corroborated as external** | See "Criterion 3 — I checked the arithmetic myself" below. |
| 4 | The out-of-band kill switch proven by measurement, schedule still firing | ❌ **UNMET — downstream of 3; nothing was registered, so nothing existed to flip** | `git diff origin/main -- scripts/prod-prober/cron-manifest.json` → **0 lines**. Manifest holds **14** jobs; `ledger_refresh_fanout` **absent**. The DEFER path left production alone, as claimed. |
| 5 | `ledger-refresh-go-live.md` corrected at Step 1a/1b/1c and Rollback; OQ-3 closed | ✅ **MET** | `:332-333` carry the 42501 table naming "(old 1a)" and "(old 1c)"; `:206` replaces the flag check with a `system_flags` row read and forbids hand-INSERT; `:389-392` — *"Open question OQ-3 … is closed by measurement, and the answer is NEITHER"*; `:583` records the Rollback rewrite, naming what the old text would have done under incident pressure. |
| 6 | Three reviewers before any migration applies | ✅ **MET in substance — and provable, contrary to the phase's own grade.** Its evidence ARTIFACT is defective (gap 1 + gap 2). | Ordering proof from PR #756's own commit list: `6913fc37` **11:41:48Z** *"brief the wave-4 reviewers on GATE-COMMENT-D2-FALSE (founder request)"* → `b4a01504` **13:09:29Z** *"repair verify legs that could not fail (**silent-failure-hunter** B1/B2)"* → `f551e265`/`551d73c1`/`19694320` **13:32–13:33Z** (F1–F6) → `3c1136c6` **13:35:14Z** *"merge(164.7): reviewer fixes — F1 key-exfil constraint, F2 TRUNCATE, F3 owner check, F4/F5/F6"* → **PR CREATED 14:34:18Z** → **merged 17:35:36Z**. Every reviewer-attributed commit precedes PR creation, which precedes the merge — and the merge is the only event that applies to PROD. The six findings are present in `TODOS.md` at PR head `7c9aea64`. |
| 7 | `VAC04-ARMS-OBSERVE` — the SUMMARY quotes VAC-04's own output verbatim and names the branch by line | ✅ **MET — re-derived from the job log, not from the record** | See "Criterion 7 — I fetched the log myself" below. |

**Score: 5/7 met.** Two unmet (3, 4), both at a founder PROD gate, both with the blocker measured
and corroborated externally.

## Criterion 7 — I fetched the log myself, and the ack is EARNED

`gh api repos/AI-Isaiah/Quantalyze/actions/jobs/101820921298/logs --allow-escape-sequences`
→ **rc=0, 681 lines**. (Without the flag: rc=1, 0 bytes — the record's new trap is real, and an
empty read taken as "no drift" would be a false green of exactly the shape this project books.)

| function | PROD sha printed by the gate (log line) | committed pragma | line | byte-for-byte |
|---|---|---|---|---|
| `enqueue_ledger_refresh_for_strategies/0` | `88e6af8472e4e48175a62b1ee189f7411407b14827d948c81aa015f511ae6e36` (:654) | same, in full | `20260907130000:10` | ✅ identical |
| `enqueue_ledger_composite_refresh/0` | `7c3d33e96f1cbe5a864750ff083670e23ab4c586b8529d48da005006b4d81ac4` (:653) | same, in full | `20260907130000:11` | ✅ identical |

**The hashes match byte-for-byte.** `164.7-VAC04-OBSERVED.md` is genuinely sourced; it is not a
restatement of the PR body.

**The branch claim holds, and its line references check out at the right sha.** Log `:650`
`Functions defined or replaced by this PR: 5` and `:651` `Functions indexed in the PROD source: 120
(union of two independent readings)`. `git show 7c9aea64:scripts/prod-body-drift-check.sh` puts the
*"this PR changes no migration files — nothing to compare against PROD"* short-circuit at **:198**
and the `── (a) LEGITIMATE ZERO ──` block at **:442-448**, exactly as cited. ⚠️ In the CURRENT tree
those same branches sit at `:647` and `:1357` — the record's citations are correct *relative to PR
head*, which is the right frame, and are a live instance of its own `[164.7-CITATION-DRIFT-01]`.

Run metadata independently confirmed via `gh api`: run **34146946050**, `Migration Drift Check`,
event `pull_request`, `head_sha 7c9aea647332549621e3f0d036a2bfc36c41196d`, conclusion `success`,
created `2026-09-07T17:16:55Z`; job `101820921298` (`check`), success. `Drift-check credentials
present.` appears **twice** in the log, so the credential-absent skip did not fire.
`.planning/WINDOWS.md` entry 25 is dispositioned `fixed` on that measurement.

⚠️ **One citation inconsistency worth a clause.** The ack comment block inside
`20260907130000:22-27` cites run **34138679709** at `51f576ef`, while `164.7-VAC04-OBSERVED.md`
cites run **34146946050** at `7c9aea64`. Both are real runs on the same PR printing the same
hashes, but a reader chasing the migration's own citation lands on a different run id than the
phase record's.

## Criterion 3 — I checked the arithmetic myself; the "external fault" claim is ACCURATE

| fact | claimed | re-derived by me | verdict |
|---|---|---|---|
| jobid 15 `poll-allocator-positions` | `0 4 * * *` | `0 4 * * *` (read from `cron-manifest.json`) | ✅ daily |
| jobid 17 `refresh-allocator-equity` | `0 5 * * *` | `0 5 * * *` | ✅ daily |
| P3-C window | 3 days | `AND cj.completed_at > now() - INTERVAL '3 days'` — `docs/runbooks/ledger-refresh-go-live.md:248` | ✅ |
| gap at decision time | ~3.5 d | break 09-07 04:05:08 is 5 min AFTER that day's 04:00Z tick and BEFORE the 05:00Z tick; decision ~09-10 16:20Z ⇒ 3 d 12 h | ✅ outside the window |
| "no wait inside 2026-09-10 could close it" | — | next ticks are 09-11 04:00Z / 05:00Z | ✅ arithmetically forced |
| manifest untouched | yes | `git diff origin/main -- scripts/prod-prober/cron-manifest.json` = **0 lines**; 14 jobs; `ledger_refresh_fanout` absent | ✅ |

**External corroboration I obtained independently of the phase's own prose:** issue **#753**
`prod-prober: 1 defect(s) on 2026-09-07`, closed `2026-09-10T16:13:58Z`; prober run **34500455961**
created `2026-09-10T16:10:56Z`, and its `Open or update the prod-prober issue` step conclusion is
**`skipped`** (I read the step conclusion from the jobs API, not from the record). And on the merge
commit's own check-run set, the **only** failures are four `probe` runs starting `2026-09-07T18:30Z`
— the outage, on the SHA, from a third instrument.

⛔ **So this is not a phase failure dressed as an external one.** The blocker is real, measured,
corroborated three ways, and the DEFER branch was pre-authorized by the ROADMAP's own plan-07 line
(*"or DEFER with the blocker named"*). ⚠️ But criteria 3 and 4 are still, factually, **UNMET**, and
the ROADMAP's criterion 3 says in terms that *"this phase is not done when the code changes"*. The
deferral's owner is **the deferring phase itself** (`TODOS.md:1633` — *"Owner: Phase 164.7 plan 07,
re-entered at the same gate"*), not a forward phase, so it does not qualify as a Step-9b deferral
and is surfaced for a human routing decision.

⚠️ **One structural caveat, stated because it is the shape of a self-serving escape hatch.** The
runbook sentence that pre-authorizes this exact outcome — `:264-267`, *"criterion 3 is then blocked
by an operational fault outside this activation, and the honest outcome is to say so, not to
activate around it"* — was **written by this phase** (plan 05, 2026-09-07). It reads correctly and
it predates the outcome by three days, and the ROADMAP independently authorized the DEFER branch,
so I do not treat it as circular. It is noted so a later reader does not mistake it for an
independent authority.

## The anti-vacuity question you asked, answered in two parts

**`164.7-ACTIVATION-RECORD.md`'s `which_database` section is an HONEST DISCLOSURE sitting on top of
a VACUOUS GATE.** Both halves are true and they must not be collapsed.

*The artifact is honest.* It states, in bold and behind a ⛔: *"There is no marker OUTPUT in this
record, and that is correct, not an omission… Do not read this section as evidence that the marker
was checked. It is the statement that will head every block at re-entry."* Nothing is claimed that
did not happen.

*The gate is vacuous.* `164.7-07-PLAN.md:149` opens its `<automated>` block with
`grep -a -c 'which_database' "$R" | grep -v '^0$'` **before** the ACTIVATED/DEFERRED branch, so it
applies to both. Its truth condition is string presence. A record that pasted the query, a record
that recorded a real marker reading, and a record that fabricated one are indistinguishable to it.
On the DEFER path it **cannot fail** — it would have passed for a dishonest record just as readily.
That is precisely "an assertion that passes if its subject is ABSENT", and it is booked nowhere.
Recorded as gap 3.

**Other presence-only assertions on the DEFER path.** `grep -a -c 'BLOCKED by an operational fault
outside this phase'` is a self-asserted string with the same property — though it is a record-SHAPE
check, which is a weaker claim. By contrast `git diff --quiet origin/main -- …cron-manifest.json`
and `grep -c '\[164.7-ACTIVATION-DEFERRED\]' TODOS.md | grep -q '^1$'` are real oracles that can
fail, and I confirmed both pass on their merits.

## The findings count — I found the sixth

`164.7-REVIEWS.md` and `164.7-06-SUMMARY.md` both say:

> *"PR #756's body says six findings; five are recoverable by id at HEAD. The sixth is not
> identifiable from the surviving record. Counted honestly rather than padded to match the prose."*

**That is false.** At commit `fcdbcdab` — the commit that wrote REVIEWS.md — `TODOS.md:7710` carries
the heading

> `## Phase 164.7 (APPSETTINGS) — SQL-fixer + three-reviewer residuals (logged 2026-09-07)`

whose own body reads *"…and the three reviewers (migration-reviewer, rls-policy-auditor,
silent-failure-hunter) read the result."* Under it, **exactly six** `- [ ]` entries — I counted them
mechanically at that commit (`COUNT=6`) and again at HEAD:

| # | id | line @ `fcdbcdab` | in REVIEWS.md? |
|---|---|---|---|
| 1 | `[APPGUC-DETECT-DOUBLEQUOTE-01]` | 7718 | ❌ missing |
| 2 | `[APPGUC-UTF16-01]` | 7720 | ❌ missing |
| 3 | `[APPGUC-WARNING-UNINSTRUMENTED-01]` | 7722 | ❌ missing |
| 4 | `[VAULTTICK-EMPTYKEY-01]` | 7724 | ❌ missing |
| 5 | `[164.7-PLAN03-EVIDENCE-01]` | 7726 | ✅ |
| 6 | `[164.7-CITATION-DRIFT-01]` | 7728 | ✅ |

The section header sits **eight lines above** the first entry REVIEWS.md itself cites by line
(`TODOS.md:7726`). The author was reading inside the answer.

**This is worse than a miscount — REVIEWS.md lists the wrong SET.** Three of its five rows are not
reviewer findings: `[GATE-COMMENT-D2-FALSE]` is labelled *"phase 164.7 **red-team**"* in its own
`TODOS.md:3597` heading and commit `6913fc37` shows it was **briefed TO** the wave-4 reviewers, not
found by them; `[164.7-VAULT-ABSENT-RULE]` and `[164.7-TEST-APPLY-APPSETTINGS]` are phase
deferrals booked elsewhere in the same merge. Meanwhile four genuine reviewer findings are dropped
from the phase's evidence trail — including `[VAULTTICK-EMPTYKEY-01]`, a live defect in the Vault
guard **this phase shipped** (`v_key = ''` instead of `btrim(v_key) = ''`, so a whitespace secret is
sent as the `Authorization` header). A file whose stated purpose is *"to be evidence"* dropped a
security-adjacent finding about its own migration.

## Where the record overclaims or misstates — quoted

1. `164.7-REVIEWS.md`: *"⚠️ PR #756's body says **six**; five are recoverable by id at HEAD. **The
   sixth is not identifiable from the surviving record.** Counted honestly rather than padded."* —
   **False.** All six are in one named section; the set listed is the wrong one. (gap 1)
2. `164.7-06-SUMMARY.md`: *"PR #756's body says **six** findings; **five** are recoverable by id.
   The sixth is not identifiable from the surviving record — counted honestly rather than padded."*
   — same defect, repeated. (gap 1)
3. `164.7-REVIEWS.md`: *"The available evidence says the reviewers ran and their findings were
   booked before the merge; it does not independently prove the ordering, and **nothing written in
   2026-09-10 could**."* — **UNDERCLAIM.** One `gh api …/pulls/756/commits` call proves it. (gap 2)
4. `164.7-06-SUMMARY.md` must-have #1: *"⚠️ **MEASURED, NOT PROVEN**"* — the correct grade for the
   ORDERING is **PROVEN**; only per-reviewer attribution of F1–F6 is unrecoverable, and even that
   is partly recoverable (`silent-failure-hunter` is named in commit `b4a01504`). Neither
   overclaiming nor a wrong call about attribution — it under-grades a fact the primary record
   settles. (gap 2)

Everything else I tested held. Nothing in these artifacts overclaims a PROD action, a hash, a run
id, or a gate result.

## Gates re-run at HEAD by me (the SUMMARY's claims, independently reproduced)

| gate | SUMMARY claimed | I measured | match |
|---|---|---|---|
| `lint-app-guc --self-test` | self-test OK (4 kinds, red+green each) | `OK: 4 finding kinds, red+green each (5 red, 2 green fixtures; 2 vitest-covered)` | ✅ |
| `lint-app-guc` corpus | 292 scanned, 5 annotated, 0 findings | `files scanned 292 / annotated 5 / findings 0`, rc=0 | ✅ |
| `lint-sql-gates --self-test` | — | `OK: 7 rules, red+green each.` | ✅ |
| `lint-sql-gates` corpus | 73 files / 0 findings | `scanned 73 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s)` | ✅ |
| `dump-sql-functions --check` | 120 functions; gate (a) 120 vs 120 | `snapshot is current (120 functions)`; `baseline 120 vs migration-replay 120 — agree, 0 ratcheted` | ✅ |
| `mutation-runner --parse-only` | rc=0, no MEASURE_FAIL | `✅ No static defects…` rc=0 (and it correctly prints *"STATIC PARSE ONLY — ZERO arms executed. This is NOT the gate"*) | ✅ |
| `vitest src/__tests__/lint-app-guc.test.ts` | vitest pin | **28 passed** | ✅ |

⭐ Two independent instruments agree on **120**: `dump-sql-functions --check` here, and VAC-04's
`Functions indexed in the PROD source: 120` in the 2026-09-07 job log.

## Merge-commit CI, SHA-bound

`gh api …/commits/14b3b6c3…/check-runs`: **24 success**, 2 skipped (`version-gate`,
`plan-anchor-verify`), **4 failure — all named `probe`**, started `2026-09-07T18:30Z`, `21:51Z`,
`23:52Z`, `2026-09-08T02:11Z`. Those are post-merge hourly `prod-prober` runs, i.e. the MT5 outage,
not this phase's CI. `sql-tests` and `frontend` are **success** on the merge SHA — the expected
shared-TEST reds were cleared by the hand-apply recorded in commits `334b37a4` / `51f576ef`.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `164.7-07-PLAN.md` | 149 | Presence-only `which_database` grep applied to both branches | ⚠️ Warning | The DEFER-branch verify leg cannot fail. Artifact honest, gate vacuous. Unbooked. |
| `164.7-REVIEWS.md` | findings table | Assertion of unidentifiability contradicted by the file it cites | 🛑 Blocker | Four real reviewer findings dropped from the phase's evidence trail |
| `.planning/ROADMAP.md` | 508-514, 1881 | 7/7 plans executed, all still `- [ ]`; progress row still `0/?  Queued 2nd` | ⚠️ Warning | Record currency — today's 4 commits touched only `.planning/phases/164.7-*`, `WINDOWS.md`, `TODOS.md` |
| `20260907130000` | 22-27 | Ack block cites run `34138679709`@`51f576ef`; the phase record cites `34146946050`@`7c9aea64` | ℹ️ Info | Both real, same hashes; a reader chasing one citation lands elsewhere |

⭐ **No debt markers.** `grep -nE "TBD|FIXME|XXX"` over the artifacts written today: 0 unreferenced
hits. No stub, no hardcoded-empty, no console-only implementation in the phase's code.

## Gaps Summary

The phase's ENGINEERING is sound and its most load-bearing evidence survived adversarial
re-derivation: I fetched VAC-04's job log myself and the two `prod-body-ack` shas match the
committed pragmas byte-for-byte; the branch claim and its line references check out against the
file at PR head; the gate is wired, fires, and is pinned by 28 passing tests; both migrations move
their settings to mechanisms this platform grants and schedule nothing; the runbook is corrected;
and the DEFER path demonstrably left production untouched. Criteria 3 and 4 are unmet for a reason
I corroborated three independent ways, and the phase said so rather than dressing it up.

What fails is a piece of the RECORD, and it fails in the one place a reconstruction is most
tempted to round off: the reviewer findings. The claim that a sixth finding is "not identifiable"
is false, and the five listed are not the six that exist — the substitution quietly drops
`[VAULTTICK-EMPTYKEY-01]`, a defect in this phase's own Vault guard. Alongside it, must-have #1 is
graded as unprovable when the PR's own commit list proves it, and plan 07's DEFER-branch verify leg
is a control that cannot fail, disclosed honestly in prose but booked nowhere. All three are
minutes of work to close, and none of them requires touching production.

---

_Verified: 2026-09-10T18:45:00Z_
_Verifier: Claude (gsd-verifier) — goal-backward, adversarial. No database was written._
