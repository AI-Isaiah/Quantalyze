# 164.7 — REVIEWS

**Written:** 2026-09-10, RECONSTRUCTED from evidence. Founder decision, same date.

## ⛔ Provenance — read this before the table, it is the point of the file

Plan 06 required three reviewers (`migration-reviewer`, `rls-policy-auditor`,
`silent-failure-hunter`) on the working branch **before** any migration reached a PR, each with its
own findings section. **That review happened; this file did not.** 164.7's migrations merged on
2026-09-07 as PR #756, whose body records *"Six non-blocking reviewer findings booked in
`TODOS.md`"* — so the gate ran and its output was routed to the backlog, not to a phase artifact.

⚠️ **THE PER-REVIEWER ATTRIBUTION WAS NOT PRESERVED AND IS NOT RECONSTRUCTED HERE.** Plan 06 asked
for three sections, one per reviewer. The booked items carry a phase tag, not a reviewer name.
Writing three tidy sections would mean **guessing which reviewer found what** — inventing structure
the evidence does not support, in a file whose whole purpose is to be evidence. One table, sourced,
is the honest artifact. That is a recorded LIMIT, not an oversight.

⛔ **Plan 06's must_have #1 cannot be satisfied retroactively by anyone.** *"No migration of this
phase reaches a PR before all three reviewers have run"* is a claim about ordering. The available
evidence says the reviewers ran and their findings were booked before the merge; it does not
independently prove the ordering, and nothing written in 2026-09-10 could. Stated as measured, not
upgraded to proven.

## Findings, by TODOS id — the record that does exist

Every row below was read from `TODOS.md` at HEAD on 2026-09-10, with its line number.

| id | class | disposition | evidence |
|---|---|---|---|
| `[164.7-VAULT-ABSENT-RULE]` | **design conflict — now blocking Phase 164.5.1** | **OPEN** | `TODOS.md:1733` |
| `[164.7-PLAN03-EVIDENCE-01]` | evidence not re-derivable | OPEN | `TODOS.md:7726` |
| `[164.7-CITATION-DRIFT-01]` | citation drift (~4 lines) | OPEN | `TODOS.md:7728` |
| `GATE-COMMENT-D2-FALSE` | prose asserts a FALSE measurement | OPEN | `TODOS.md:3597` |
| `[164.7-TEST-APPLY-APPSETTINGS]` | expected-red on shared TEST, by design | **CLOSED** | `TODOS.md:1610` |

⚠️ PR #756's body says **six**; five are recoverable by id at HEAD. The sixth is not identifiable
from the surviving record. Counted honestly rather than padded to match the prose.

## The two findings that matter now, and why

### `[164.7-VAULT-ABSENT-RULE]` — it graduated from a note to a blocker

Booked here as a non-blocking design conflict. It is now the **first work of Phase 164.5.1**, and
that phase cannot meet its criterion 2 until it is fixed. Reproduced by execution 2026-09-10:

```
hygieneViolations("match_engine_cron", "SELECT public.match_engine_cron_tick();")  =>  [vault-absent]
```

`scripts/prod-prober/arms/cron-drift.mjs:329` requires the literal `vault.decrypted_secrets` in the
job's COMMAND TEXT. Phase 164.7's own callable moves that read INSIDE the function body, so the
rule **fires against a correct repair** and `captureManifest` refuses (`run.mjs:2497-2512`, exit 1,
nothing written). ⭐ This is 164.7 shipping a mechanism whose own hygiene rule had not caught up —
the booking predicted it precisely, and it is the reason finalizing 164.7 before 164.5.1 was the
right order.

### `GATE-COMMENT-D2-FALSE` — the item the founder personally routed to the reviewers

Two gate comments and a SUMMARY justify a comment-stripping probe by asserting the migration's body
comments contain the needle; measured `needle_in_comments = 0` for both functions. The CHANGE is
correct and mandatory (`sql-gate-lint` rule `[R2-functiondef-comment-strip]` rejects a raw
`pg_get_functiondef` match, and over-stripping is fail-CLOSED). Only the STATED REASON is false.
Still **OPEN** at `TODOS.md:3597`. Prose defect by the stopping rule, so it never blocked the PR —
but `[SEAMPROSE-01]` exists because claims like this rot into fact.

## Gates, re-run at HEAD 2026-09-10

See `164.7-06-SUMMARY.md` for the commands and their output. Re-running them now measures the
CURRENT tree, which is 164.7's code plus everything merged since — it is a statement about today,
not a reconstruction of the PR-time reading, and is labelled that way.
