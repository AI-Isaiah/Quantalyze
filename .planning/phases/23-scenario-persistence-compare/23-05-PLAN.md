---
phase: 23-scenario-persistence-compare
plan: 05
type: execute
wave: 4
depends_on: ["23-02", "23-03", "23-04"]
files_modified:
  - src/app/(dashboard)/allocations/components/SavedScenariosList.tsx
  - src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComparePanel.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
autonomous: true
requirements: [PERSIST-03, PERSIST-04]
must_haves:
  truths:
    - "The Scenario tab shows a Saved scenarios list (name + timestamp + Open/Rename/Delete) RLS-scoped to the allocator, with an EmptyStateCard when none exist"
    - "Rename does an inline edit → PATCH; Delete does a small inline danger confirm (not a modal) → DELETE"
    - "Selection checkboxes + a Live book pseudo-row feed a Compare selected CTA enabled at >= 2 selections"
    - "Compare selected computes each selected draft + the live book via computeMetricsForDraft and renders ScenarioCompareTable in an in-tab panel"
    - "Open routes the row's draft to the composer's codec-trichotomy Open handler (Plan 04)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/SavedScenariosList.tsx"
      provides: "list rows (Open/Rename/Delete + selection checkbox), EmptyStateCard, Compare-selected CTA"
      min_lines: 60
    - path: "src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx"
      provides: "in-tab panel: computes metrics per selection + live book, mounts ScenarioCompareTable"
      min_lines: 30
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      provides: "mounts SavedScenariosList + ScenarioComparePanel adjacent to ScenarioComposer on the scenario tab"
      contains: "SavedScenariosList"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/SavedScenariosList.tsx"
      to: "/api/allocator/scenario/saved"
      via: "GET list, PATCH rename, DELETE"
      pattern: "scenario/saved"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx"
      to: "computeMetricsForDraft"
      via: "per-selection metrics + synthetic live-book draft → ScenarioCompareTable"
      pattern: "computeMetricsForDraft"
    - from: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      to: "SavedScenariosList"
      via: "rendered in the activeTab === scenario branch with props payload + allocatorId"
      pattern: "SavedScenariosList"
---

<objective>
Wire the feature together on the Scenario tab: `SavedScenariosList` (list rows with Open/Rename/Delete + selection checkboxes + EmptyStateCard + Compare-selected CTA), `ScenarioComparePanel` (computes each selection + the live book via `computeMetricsForDraft` and mounts `ScenarioCompareTable`), and mount both adjacent to `ScenarioComposer` in `AllocationsTabs.tsx`.

Purpose: PERSIST-03 (list/rename/delete) + PERSIST-04 (compare). This is the integration plan — the components from Plans 02/03/04 become a working surface on the existing Scenario tab (no new authenticated route).
Output: `SavedScenariosList` + `ScenarioComparePanel` + tests; `AllocationsTabs.tsx` mount.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-scenario-persistence-compare/23-PATTERNS.md
@.planning/phases/23-scenario-persistence-compare/23-UI-SPEC.md
@.planning/phases/23-scenario-persistence-compare/23-02-SUMMARY.md
@.planning/phases/23-scenario-persistence-compare/23-03-SUMMARY.md
@.planning/phases/23-scenario-persistence-compare/23-04-SUMMARY.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

Scenario tab mount point (AllocationsTabs.tsx:710-721):
  {activeTab === "scenario" && ( … <ScenarioComposer payload={props} allocatorId={props.allocator_id} /> … )}
  → SavedScenariosList + ScenarioComparePanel mount in THIS branch, adjacent to the composer (space-y-6 gap),
    handed the SAME `props` payload (payload.strategies, holdingsSummary, holdingReturnsByScopeRef, symbolByHoldingId)
    and props.allocator_id. No second fetch path for compute (reuse the SSR-lifted payload).

CRUD endpoints (Plan 02): GET /api/allocator/scenario/saved (list: id,name,schema_version,created_at,updated_at);
  PATCH /api/allocator/scenario/saved/[id] (rename); DELETE /api/allocator/scenario/saved/[id].
  Open does NOT fetch here — the row's draft is passed to the composer's Open handler (Plan 04) which runs the codec.
  NOTE: the list GET returns metadata only (no draft / no N). To Open or Compare a row, fetch its draft — either include
  draft in the list select, or GET the single row's draft on demand. Planner's discretion; default: include draft in the
  list payload so Open + Compare have it without a second round-trip (RESOLVED: list rows do NOT stamp N/overlap —
  no cheap N without running computeScenario; N is stamped per COLUMN in the compare table where the engine runs).

computeMetricsForDraft(draft, liveInputs) + buildLiveBookDraft(holdingsSummary)  (Plan 03, scenario-compare.ts).
ScenarioCompareTable  (Plan 03) — takes per-column { name, metrics: ComputedMetrics }.
ScenarioComposer Open handler + loadedScenarioId  (Plan 04) — Open(row) applies the codec trichotomy.

List-row shell self-analog: ScenarioComposer.tsx:1386 (flex items-center justify-between gap-3 rounded-md border border-border p-3).
EmptyStateCard (src/components/ui/EmptyStateCard.tsx) — heading MUST match body (#509 lesson).
Button (src/components/ui/Button.tsx) — variants primary/secondary/ghost/danger.

UI-SPEC copy (§Copywriting): list heading "Saved scenarios"; affordances Open(ghost)/Rename/Delete(danger inline confirm);
  empty heading "No saved scenarios yet" + body "Compose a draft above, then choose \"Save scenario\" to keep it here. …";
  "Compare selected" (secondary, disabled <2); under-selection "Select 2 or more scenarios (or the live book) to compare.";
  Live book pseudo-row "Live book"; Delete confirm "Delete \"{name}\"?" + Delete(danger)/Cancel(ghost) inline (NOT a modal).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SavedScenariosList (list rows + rename/delete + selection + CTA + empty state)</name>
  <files>src/app/(dashboard)/allocations/components/SavedScenariosList.tsx, src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx</files>
  <read_first>
    - src/components/ui/EmptyStateCard.tsx (empty state — heading must match body)
    - src/components/ui/Button.tsx (variants per UI-SPEC)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1386 (list-row shell self-analog)
    - .planning/phases/23-scenario-persistence-compare/23-UI-SPEC.md §Component Inventory 2 + §Copywriting + §Accessibility (exact tokens, copy, focus rings, keyboard reachability)
    - src/app/api/allocator/scenario/saved/route.ts + [id]/route.ts (Plan 02 — list/rename/delete endpoints)
  </read_first>
  <behavior>
    - Renders one row per saved scenario: [checkbox] {name} (14px) + 12px text-text-muted saved/updated timestamp; right side Open(ghost) · Rename · Delete.
    - Empty list → EmptyStateCard with heading "No saved scenarios yet" + the UI-SPEC body (heading matches body).
    - Rename: clicking Rename reveals an inline edit input; submit PATCHes /api/allocator/scenario/saved/[id] with the trimmed name; empty/>120 shows the validation copy and does not PATCH.
    - Delete: clicking Delete reveals a small inline "Delete \"{name}\"?" confirm with Delete(danger)/Cancel(ghost) — NOT a modal; confirm DELETEs the row and removes it from the list.
    - Selection checkboxes are keyboard-focusable + labeled by the scenario name; a "Live book" pseudo-row participates in selection.
    - "Compare selected" CTA is disabled until >= 2 rows (incl. Live book) are checked (reuse Button's disabled opacity-50 pointer-events-none); enabled, it raises the selected set to the parent.
  </behavior>
  <action>
    RED first: write `src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx` (mock fetch for list/PATCH/DELETE) encoding the behavior block — assert the empty-state heading/body match, rename PATCH fires with the trimmed name, delete shows an inline confirm (no modal) then removes the row, and Compare-selected is disabled <2 / enabled >=2. Run it — MUST fail. GREEN: create `src/app/(dashboard)/allocations/components/SavedScenariosList.tsx`. Props: the saved-scenario rows (fetched from GET, including draft for Open/Compare), an onOpen(row) callback (delegates to the composer's Open handler — Plan 04), and an onCompare(selectedRows + includeLiveBook) callback (raises selection to the parent panel). Rows mirror the composer list-row shell; use EmptyStateCard for empty; Button variants per UI-SPEC; inline rename + inline danger delete confirm (no modal). Selection checkboxes keyboard-focusable + name-labeled with focus:ring-accent/50; a Live book pseudo-row participates. Compare-selected disabled <2. Use ONLY UI-SPEC tokens/copy; no new icons. REFACTOR; keep green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - RED commit: test fails before the component exists.
    - Empty state heading matches body (EmptyStateCard, #509 lesson).
    - Rename → inline edit → PATCH with trimmed name; bad length shows validation copy + no PATCH.
    - Delete → small inline danger confirm (NOT a modal) → DELETE → row removed.
    - Selection checkboxes keyboard-focusable + name-labeled; Live book pseudo-row participates.
    - Compare-selected disabled <2, enabled >=2.
    - Only UI-SPEC tokens/copy; no new icons.
  </acceptance_criteria>
  <done>SavedScenariosList green; list/rename/delete/selection/empty-state all per UI-SPEC.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ScenarioComparePanel (compute selections + live book → ScenarioCompareTable)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx, src/app/(dashboard)/allocations/components/ScenarioComparePanel.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-compare.ts (Plan 03 — computeMetricsForDraft + buildLiveBookDraft)
    - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx (Plan 03 — the render target + its props shape)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:460-518 (how the composer derives liveInputs from payload — reuse the SAME derivation, no second fetch)
    - .planning/phases/23-scenario-persistence-compare/23-UI-SPEC.md §Component Inventory 3 + §Copywriting (Sharpe-leader callout, under-selection hint)
  </read_first>
  <behavior>
    - Given the selected rows (each with its draft) + an includeLiveBook flag + the live payload, computes one ComputedMetrics per selection via computeMetricsForDraft, and the live-book column via computeMetricsForDraft over buildLiveBookDraft(holdingsSummary).
    - Passes the per-column { name, metrics } array to ScenarioCompareTable (Live book column labeled "Live book").
    - With < 2 selections, renders the under-selection hint "Select 2 or more scenarios (or the live book) to compare." (no fabricated table).
    - A degenerate selection (null metrics) flows through to ScenarioCompareTable's em-dash path — the panel does NOT coerce to 0.
    - The live payload is reused (no second fetch); leverage = 1 (never persisted).
  </behavior>
  <action>
    RED first: write `src/app/(dashboard)/allocations/components/ScenarioComparePanel.test.tsx` encoding the behavior block (fixture payload + two selected drafts incl. one degenerate; assert the live-book column is present and labeled "Live book", the under-selection hint shows <2, and a degenerate column reaches the table as null metrics — not 0). Run it — MUST fail. GREEN: create `src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx`. Props: selectedRows (with drafts), includeLiveBook, and the live payload (payload.strategies, holdingsSummary, holdingReturnsByScopeRef, symbolByHoldingId). Derive liveInputs from the payload using the SAME derivation the composer uses (ScenarioComposer.tsx:460-518) — no second fetch. Compute each column's metrics via computeMetricsForDraft; compute the live-book column via computeMetricsForDraft(buildLiveBookDraft(holdingsSummary), liveInputs). Render ScenarioCompareTable with the { name, metrics } columns (in-tab panel, default — not a modal). Under-selection → the UI-SPEC hint. Do NOT add a `?? 0` anywhere. Use ONLY UI-SPEC tokens/copy. REFACTOR; keep green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComparePanel.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - RED commit: test fails before the panel exists.
    - Each selection + the live book computed via computeMetricsForDraft over the SAME live payload (no second fetch).
    - Live-book column present and labeled "Live book"; computed via the synthetic all-on draft (all six metrics populate).
    - < 2 selections → under-selection hint; no fabricated table.
    - A degenerate column reaches ScenarioCompareTable as null metrics (no `?? 0` in the panel).
    - Only UI-SPEC tokens/copy.
  </acceptance_criteria>
  <done>ScenarioComparePanel green; per-selection + live-book metrics via one engine path; degenerate → null preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Mount SavedScenariosList + ScenarioComparePanel on the Scenario tab</name>
  <files>src/app/(dashboard)/allocations/AllocationsTabs.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx:710-721 (the activeTab === "scenario" branch where ScenarioComposer renders with payload={props} allocatorId={props.allocator_id})
    - src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx (existing scenario-tab test — extend / do not regress)
    - src/app/(dashboard)/allocations/components/SavedScenariosList.tsx + ScenarioComparePanel.tsx (Tasks 1-2)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (Plan 04 Open handler + loadedScenarioId — the onOpen target)
  </read_first>
  <action>
    In `AllocationsTabs.tsx` within the `activeTab === "scenario"` branch (710-721), render SavedScenariosList + (when a compare selection is active) ScenarioComparePanel adjacent to ScenarioComposer with a `space-y-6` gap, handing both the SAME `props` payload + `props.allocator_id`. Hold the saved-scenario rows + the compare selection in this branch's state (fetch the list from GET on mount; refetch after a Save/Update/rename/delete so the list stays consistent). Wire SavedScenariosList's onOpen → the composer's Open handler (Plan 04) so Open hydrates through the codec trichotomy; wire onCompare → set the selection that mounts ScenarioComparePanel. Match the Phase 21 V2/ScenarioStub rollback gating already in this branch (only the V2 composer path gets the new surfaces; the ScenarioStub rollback path is untouched). Do NOT add a new authenticated route. Extend the existing scenario-tab test to assert the list mounts on the V2 path.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.test.tsx" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - SavedScenariosList renders on the scenario tab (V2 composer path), handed props payload + props.allocator_id.
    - ScenarioComparePanel mounts in-tab when a >=2 selection is active (no new authenticated route).
    - onOpen delegates to the composer's codec-trichotomy Open handler (Plan 04).
    - List refetches after Save/Update/rename/delete so it stays consistent.
    - ScenarioStub rollback path untouched; existing scenario-tab tests still green.
    - npm run typecheck clean.
  </acceptance_criteria>
  <done>List + compare panel mounted on the Scenario tab; Open delegates to the composer; rollback path untouched.</done>
</task>

</tasks>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComparePanel.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx"` green.
- `npm run typecheck` + `npm run lint` clean.
- Full suite `npm test` green at the wave merge; coverage gate (frontend-coverage) not lowered.
- Note for /ship: bump VERSION + package.json together (critical-regressions test) — not a wave task.
</verification>

<success_criteria>
- Saved scenarios list (Open/Rename/Delete + selection) on the Scenario tab, RLS-scoped, with an honest empty state.
- Compare selected (>=2, incl. Live book) renders ScenarioCompareTable via one engine path; degenerate → "—".
- Open delegates to the composer's codec-trichotomy hydrate; no new authenticated route.
</success_criteria>

<output>
Create `.planning/phases/23-scenario-persistence-compare/23-05-SUMMARY.md` when done.
</output>
