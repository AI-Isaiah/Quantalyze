---
phase: 23-scenario-persistence-compare
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-compare.ts
  - src/app/(dashboard)/allocations/lib/scenario-compare.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx
autonomous: true
requirements: [PERSIST-04]
must_haves:
  truths:
    - "Each saved draft re-resolves its return series from the live payload and runs through computeScenario → ComputedMetrics (leverage = 1, never persisted)"
    - "A degenerate draft (n below usable / null metric) yields null metrics that render as an em-dash, never a fabricated 0"
    - "Each compare column stamps its OWN methodologyLine(n) — heterogeneous windows are allowed; no single shared-window header"
    - "The live-book column is computed through the same engine path (synthetic all-on draft) so all six metrics populate honestly"
    - "Winner highlighting + the Sharpe-leader callout match CompareTable's findWinner behavior"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-compare.ts"
      provides: "pure computeMetricsForDraft(draft, liveInputs) → ComputedMetrics + the synthetic live-book draft builder"
      exports: ["computeMetricsForDraft"]
    - path: "src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx"
      provides: "CompareTable-mirrored grid: 6 metric rows × columns, per-column methodologyLine, em-dash, winner ✓, Sharpe leader"
      min_lines: 60
  key_links:
    - from: "src/app/(dashboard)/allocations/lib/scenario-compare.ts"
      to: "computeScenario"
      via: "adapter → projectionState → collapseAliasedHoldingStrategies → buildDateMapCache → computeScenario"
      pattern: "computeScenario"
    - from: "src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx"
      to: "methodologyLine"
      via: "per-column independent stamp"
      pattern: "methodologyLine"
---

<objective>
Build the compare engine + render: a pure `computeMetricsForDraft(draft, liveInputs)` helper that re-resolves a saved draft's series from the live payload and runs the frozen `computeScenario`, plus `ScenarioCompareTable` mirroring `CompareTable` with per-column `methodologyLine(n)`, em-dash honesty, winner highlighting, and the Sharpe-leader callout.

Purpose: PERSIST-04. This is the honesty-critical surface — degenerate columns must show "—" (never a fabricated 0), each column stamps its OWN window (heterogeneous windows are correct; common-window alignment is Phase 24's job), and the live-book column runs through the SAME engine path so all six metrics populate.
Output: `scenario-compare.ts` + tests; `ScenarioCompareTable.tsx` + tests. No routes, no DB — pure TS over the existing payload + frozen engine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-scenario-persistence-compare/23-PATTERNS.md
@.planning/phases/23-scenario-persistence-compare/23-RESEARCH.md
@.planning/phases/23-scenario-persistence-compare/23-UI-SPEC.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

The composer's adapter→compute chain to extract (ScenarioComposer.tsx ~460-630):
  payload.strategies → addedStrategyReturnsLookup / addedStrategyMetadataLookup
  → buildStrategyForBuilderSet(holdingsSummary, disabledHoldingRefs, draft.addedStrategies, holdingReturnsByScopeRef, …)  [scenario-adapter.ts:87]
  → overlay draft.toggleByScopeRef / draft.weightOverrides into projectionState
  → collapseAliasedHoldingStrategies(strategies, state, symbolByHoldingId)  [scenario-dealias.ts:60]
  → buildDateMapCache(strategies)  [scenario.ts]
  → computeScenario(strategies, state, dateMapCache) → ComputedMetrics  [scenario.ts:132]

ComputedMetrics fields used: n, twr, cagr, sharpe, sortino, max_drawdown, volatility. computeScenario already
returns null-metric ComputedMetrics for degenerate sets (n<10 / NaN-poisoned) — caller renders "—", NEVER `?? 0`.

CompareTable analog (src/components/strategy/CompareTable.tsx):
  METRICS rows + higherIsBetter flags (:27-37); formatValue(value,format) returns "—" for null (:44);
  findWinner(items,key,higherIsBetter) skips nulls, returns best idx (:51); winner cell text-accent font-bold + " ✓" (:104-120).
  Phase 23 six rows from ComputedMetrics: Cumulative Return(twr,%,true), CAGR(%,true), Sharpe(number,true),
  Sortino(number,true), Max Drawdown(%,false), Volatility(%,false).

methodologyLine(n)  [scenario-history.ts:41] — per-column window+N stamp (heterogeneous windows expected).
evaluateSampleFloor / SAMPLE_FLOOR_OVERLAPPING_DAYS=60  [sample-floor.ts] — below-floor gate copy (neutral, no red/amber).
formatPercent / formatNumber  [src/lib/utils.ts] — return "—" on null/non-finite (em-dash for free).

Live-book column: compute via the SAME engine path over a synthetic "all live holdings, equity-weight" draft
  (RESOLVED decision — NOT payload.liveBaselineMetrics, which nulls cagr/sortino/volatility → 3 unintended em-dashes).
  An em-dash on a genuinely-degenerate live book is then honest, not an artifact of a thin baseline shape.

Leverage: ephemeral useState in the composer (ScenarioComposer.tsx:361-367), NEVER persisted. The helper runs
  every leg at leverage 1. Saved drafts carry no leverage field.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD computeMetricsForDraft + synthetic live-book draft (scenario-compare.ts)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-compare.ts, src/app/(dashboard)/allocations/lib/scenario-compare.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:460-630 (the adapter→projectionState→deAliased→computeScenario chain to extract)
    - src/lib/scenario.ts:85-132 (computeScenario, buildDateMapCache, ComputedMetrics — the null-metric degenerate path)
    - src/app/(dashboard)/allocations/lib/scenario-adapter.ts:87 (buildStrategyForBuilderSet)
    - src/lib/scenario-dealias.ts:60 (collapseAliasedHoldingStrategies)
    - src/lib/scenario.test.ts (the existing engine pins — do NOT regress; the new test mirrors its fixture style)
  </read_first>
  <behavior>
    - A saved draft round-trips to the SAME ComputedMetrics the composer would show for that draft over the same live inputs.
    - A degenerate draft (overlap n below usable / NaN-poisoned) yields ComputedMetrics with null metric fields — the helper does NOT coerce to 0.
    - Two drafts with heterogeneous overlap windows each report their OWN n (the helper does not force a common window).
    - The synthetic live-book draft (all live holdings enabled, equity-weight, no added strategies, no leverage) computes all six metrics (twr, cagr, sharpe, sortino, max_drawdown, volatility) non-null when the live book has ≥ usable overlap.
    - Every leg runs at leverage 1 (no leverage field consulted).
  </behavior>
  <action>
    RED first: write `src/app/(dashboard)/allocations/lib/scenario-compare.test.ts` encoding the behavior block (mirror scenario.test.ts fixture style; build minimal liveInputs = { holdingsSummary, addedStrategyReturnsLookup, addedStrategyMetadataLookup, holdingReturnsByScopeRef, symbolByHoldingId }). Run it — it MUST fail (no module yet). GREEN: create `src/app/(dashboard)/allocations/lib/scenario-compare.ts` exporting `computeMetricsForDraft(draft: ScenarioDraft, liveInputs): ComputedMetrics` that extracts the composer's chain verbatim: build addedStrategy lookups from payload.strategies, buildStrategyForBuilderSet over holdingsSummary + draft.addedStrategies + holdingReturnsByScopeRef, overlay draft.toggleByScopeRef/draft.weightOverrides into projectionState (NO leverage — leverage is ephemeral, never persisted; every leg at leverage 1), collapseAliasedHoldingStrategies, buildDateMapCache, computeScenario. Also export a `buildLiveBookDraft(holdingsSummary)` (or equivalent) that produces the synthetic all-on equity-weight draft so the live-book column is computed through the SAME helper (RESOLVED decision — never payload.liveBaselineMetrics). Do NOT add a `?? 0` on any metric — let computeScenario's null path flow through. REFACTOR if needed; keep tests green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-compare.test.ts" src/lib/scenario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - RED commit: the test fails before the module exists (no false-green).
    - computeMetricsForDraft extracts the existing composer chain; no new compute algorithm invented.
    - Degenerate draft → null metrics (no `?? 0`); test proves a null metric, not a 0.
    - Heterogeneous-window drafts each report their own n (test asserts distinct n).
    - Synthetic live-book draft populates all six metrics non-null on a healthy live book.
    - Leverage never consulted (every leg leverage 1).
    - src/lib/scenario.test.ts still green (engine not regressed).
  </acceptance_criteria>
  <done>computeMetricsForDraft + live-book draft builder green; degenerate→null, heterogeneous windows, leverage=1 all pinned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TDD ScenarioCompareTable (CompareTable-mirrored grid + per-column honesty)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx, src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx</files>
  <read_first>
    - src/components/strategy/CompareTable.tsx (full — table scaffold :84-95, METRICS+higherIsBetter :27-37, formatValue null→"—" :44, findWinner :51, winner cell styling :104-120, empty-selection state :66-68)
    - src/lib/scenario-history.ts:41 (methodologyLine)
    - src/lib/sample-floor.ts (evaluateSampleFloor, SAMPLE_FLOOR_OVERLAPPING_DAYS=60, builder copy)
    - .planning/phases/23-scenario-persistence-compare/23-UI-SPEC.md §Component Inventory 3 + §Copywriting (exact tokens + copy)
    - src/app/(dashboard)/allocations/lib/scenario-compare.ts (Task 1 — the metrics source)
  </read_first>
  <behavior>
    - Renders six metric rows (Cumulative Return, CAGR, Sharpe, Sortino, Max Drawdown, Volatility) × N scenario columns + the "Live book" column.
    - A column whose metrics are null renders "—" in every value cell (via formatValue/formatPercent/formatNumber) — never "0", "0.00", "0%", or "N/A".
    - Each column renders its OWN methodologyLine(n) caption — two columns with different n show DIFFERENT stamps (no single shared-window header).
    - The per-metric winner cell carries text-accent font-bold + " ✓" via findWinner (skips nulls); higherIsBetter false for Max Drawdown + Volatility.
    - A "Best Sharpe: {name}" callout (neutral text-text-secondary) names the Sharpe leader.
    - A column whose whole window is below SAMPLE_FLOOR_OVERLAPPING_DAYS (60) is gated to the neutral sample-floor copy (no red/amber, no role="alert").
    - Under-selection state renders the UI-SPEC hint "Select 2 or more scenarios (or the live book) to compare."
  </behavior>
  <action>
    RED first: write `src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx` encoding the behavior block (render with fixture ComputedMetrics arrays incl. a degenerate/null column and two heterogeneous-window columns). Run it — MUST fail. GREEN: create `src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx` mirroring CompareTable EXACTLY for scaffold + winner logic (copy formatValue + findWinner behavior; reuse formatPercent/formatNumber from utils for em-dash), reading keys from ComputedMetrics (not StrategyAnalytics). The load-bearing DIVERGENCE from CompareTable: each column stamps its OWN methodologyLine(scenarioMetrics.n) in a caption sub-row/footer (12px, N in Geist Mono via font-metric) — do NOT render a single shared-window header (Pitfall 5). Gate a whole below-floor column with evaluateSampleFloor + the builder copy (neutral). Add the "Best Sharpe: {name}" callout. Use ONLY the UI-SPEC tokens/copy (no new tokens, no icons). Props take the per-column { name, metrics: ComputedMetrics } produced by Plan 04's panel (computed via computeMetricsForDraft + the live-book draft). REFACTOR; keep tests green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - RED commit: the component test fails before the component exists.
    - Six metric rows × columns + Live book column render from ComputedMetrics.
    - Degenerate/null column renders "—" everywhere (test asserts "—", and asserts NO "0"/"0.00%"/"N/A" appears for that column).
    - Two heterogeneous-window columns render DIFFERENT methodologyLine stamps (test asserts distinct stamps; no shared header).
    - Winner cell = text-accent font-bold + " ✓" via findWinner; Max Drawdown + Volatility use higherIsBetter=false.
    - "Best Sharpe: {name}" callout names the Sharpe leader (neutral styling).
    - Below-floor column gated to neutral sample-floor copy (no red/amber, no role="alert").
    - Under-selection hint matches the UI-SPEC copy exactly.
    - Only UI-SPEC tokens/copy used; no new icons or tokens.
  </acceptance_criteria>
  <done>ScenarioCompareTable green; em-dash, per-column independent windows, winner ✓, Sharpe leader, below-floor neutrality all pinned.</done>
</task>

</tasks>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-compare.test.ts" "src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx" src/lib/scenario.test.ts` green.
- `npm run typecheck` + `npm run lint` clean.
- Coverage not lowered (each new file carries its test).
</verification>

<success_criteria>
- Each saved draft + the live book run through one engine path (computeMetricsForDraft); all six metrics populate honestly.
- Degenerate columns show "—" (test-proven, never a fabricated 0).
- Each column stamps its OWN methodologyLine(n); heterogeneous windows allowed.
- Winner highlight + Sharpe-leader callout match CompareTable behavior.
</success_criteria>

<output>
Create `.planning/phases/23-scenario-persistence-compare/23-03-SUMMARY.md` when done.
</output>
