---
phase: 23
slug: scenario-persistence-compare
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-21
---

# Phase 23 — UI Design Contract

> Visual and interaction contract for Scenario Persistence & Compare. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **This phase is INCREMENTAL UI on an existing surface.** It adds a "Saved scenarios" list, a save/update control, and an in-tab compare panel to the allocations **Scenario tab** (surfaced in Phase 21). Every token below is inherited verbatim from `DESIGN.md` and the existing scenario components — nothing new is invented. The contract's job is to pin REUSE, not redesign.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom token system; `components.json` absent — NOT shadcn) |
| Preset | not applicable |
| Styling | Tailwind v4 via `@theme inline` in `src/app/globals.css` (no `tailwind.config.*`); all color vars use the `--color-*` prefix (DESIGN.md decision 2026-05-06) |
| Component library | none (hand-rolled primitives) |
| Icon library | none — identity carried by border + text only (DESIGN.md anti-pattern: no icons-in-colored-circles). No new icons this phase. |
| Font | DM Sans (body/UI), Instrument Serif (display only), Geist Mono (numbers/tabular-nums) — loaded via `next/font/google` in `src/app/layout.tsx` |

**Primitives this phase reuses (do not author new variants):**

| Primitive | Path | Use in Phase 23 |
|-----------|------|-----------------|
| `Button` | `src/components/ui/Button.tsx` | variants `primary` / `secondary` / `ghost` / `danger`; sizes `sm` / `md` / `lg`. `min-h-[44px]` is built into `md`/`lg` (touch target). `sm` = `px-3 py-1.5 text-xs`. |
| `EmptyStateCard` | `src/components/ui/EmptyStateCard.tsx` | "No saved scenarios yet" + the honest below-data states. Heading MUST match body (the #509 lesson). |
| `CompareTable` pattern | `src/components/strategy/CompareTable.tsx` | the compare panel MIRRORS this exact metric-rows × columns layout + `findWinner` highlighting — see §Component Inventory. |
| `formatPercent` / `formatNumber` | `src/lib/utils.ts` | both already return `"—"` on `null` or `!Number.isFinite` — the em-dash honesty invariant is free; do not re-implement. |
| `methodologyLine(n)` | `src/lib/scenario-history.ts:41` | each compare column stamps its OWN overlap window + N with this. |
| `EmptyStateCard` builders + `evaluateSampleFloor` | `src/lib/sample-floor.ts` | below-floor / degenerate gate copy. |
| PROJECTED badge + coverage caveat + fingerprint-mismatch banner | `ScenarioComposer.tsx:992-1044` | reopen surfaces reuse these verbatim; do not re-style. |

---

## Spacing Scale

DESIGN.md base unit 4px, "Comfortable" density. Declared values (all multiples of 4):

| Token | Value | Usage in Phase 23 |
|-------|-------|-------------------|
| xs | 4px | Inline gaps inside a saved-scenario row (`gap-1`) |
| sm | 8px | Checkbox-to-label gap, badge-to-value gap (`gap-2`) |
| md | 16px | Card padding (`px-4`), row vertical rhythm, save-input-to-button gap |
| lg | 24px | Gap between the composer, the saved-scenarios list, and the compare panel (`space-y-6`) |
| xl | 32px | Major section break above the compare panel when expanded |

Card padding follows the DESIGN.md Card pattern: 16px (`px-4`) for the list rows and compare panel shell. Table cell padding mirrors `CompareTable`: `px-4 py-2.5` (header `py-3`), `py-2` for the methodology-stamp sub-row.

**Exceptions:** one documented exception. The compare-panel body cells use `py-2.5` (10px) verbatim from the existing `CompareTable.tsx` primitive that this phase explicitly mirrors. This is a pre-existing CompareTable convention reused for exact visual parity on the Scenario tab, NOT a new spacing declaration; altering it would create visual inconsistency with the established component. (No 44px touch-target exception needed — `Button` `md`/`lg` already carry `min-h-[44px]`; the inline `sm` rename/delete affordances live inside ≥44px table rows per DESIGN.md "Table row height ~44px".)

---

## Typography

Inherits the v2 scenario surface's tight 4-size / 2-weight contract (DESIGN.md decision 2026-04-29). Weights: **2** in net-new chrome — 400 regular and 600 semibold; sub-headings differentiate via `uppercase tracking-wider`. **One documented weight exception:** the per-metric *winner* cell in the compare table uses `font-bold` (700) verbatim from the existing `CompareTable.tsx` primitive this phase mirrors (`CompareTable.tsx:116`) — reused for exact visual parity, NOT a new weight introduced for this surface (same parity rationale as the `py-2.5` spacing exception). All numbers use **Geist Mono tabular-nums** (`font-metric`).

| Role | Size | Weight | Line Height | Applied to |
|------|------|--------|-------------|------------|
| Section heading | 16px (`text-base` / panel-H2 tier) | 600 semibold | 1.2 | "Saved scenarios", "Compare scenarios" panel titles (DM Sans) |
| Body / label | 14px (`text-sm`) | 400 regular | 1.5 | Scenario name in list rows, button labels, compare metric-row labels (DM Sans) |
| Caption / metadata | 12px (`text-xs`) | 400 regular | 1.5 | Saved/updated timestamp, the per-column `methodologyLine` stamp, sub-headings via `uppercase tracking-wider` (DM Sans for prose; Geist Mono for the N) |
| Data / metric value | 12px (`text-xs font-metric`) | `font-bold` (700) for the per-metric winner (inherited from `CompareTable`; see documented exception above), 400 for the rest | 1.5 | Compare-table cell values (Geist Mono tabular-nums) — mirrors `CompareTable` exactly |

The em-dash `"—"` for a degenerate cell renders in the same Geist Mono data tier as a real value (no special styling — it reads as honest absence, not an error).

---

## Color

DESIGN.md restrained palette: 1 accent + 3 semantic + neutrals. Light mode only.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#F8F9FA` (`--color-page`) | Page background, table-row hover (`hover:bg-page/50` per CompareTable) |
| Secondary (30%) | `#FFFFFF` (`--color-surface`) | List rows, compare-panel shell, EmptyStateCard surface, cards |
| Accent (10%) | `#1B6B5A` (`--color-accent`) | See reserved-for list below |
| Destructive | `#DC2626` (`--color-negative`) | Delete confirm affordance + `Button variant="danger"` only |

**Accent (`#1B6B5A`) reserved for — exhaustively:**
1. The per-metric **winner value** in the compare table (`text-accent font-bold` + " ✓"), exactly as `CompareTable.findWinner` does today.
2. The primary **"Save scenario" / "Update scenario"** CTA (`Button variant="primary"` = `bg-accent text-white`).
3. Focus ring on the inline name input and checkboxes (`focus:ring-accent/50`, `border-focus` = accent on focused inputs).

Accent is NOT used for: list-row backgrounds, the "Open"/"Compare selected"/"Save as new scenario" secondary actions (those are `secondary`/`ghost`), timestamps, the Sharpe-leader callout text, or selection checkboxes' resting state.

**Semantic usage constraints (load-bearing honesty):**
- **Negative `#DC2626`** — ONLY the delete affordance (hover→negative inline confirm) and a hard save/load failure error envelope. Never for a degenerate/em-dash cell (absence is neutral, not an error).
- **No warning amber** in net-new Phase 23 chrome, EXCEPT the reused fingerprint-mismatch banner (`ScenarioComposer.tsx:1026`) which is rendered verbatim on reopen — its existing `border-warning` styling is inherited, not re-specified here.
- **Degenerate / below-floor columns and empty states are NEUTRAL** — `text-text-muted` / `EmptyStateCard` muted card, NO `role="alert"`, NO red/amber. Honest absence is calm (DESIGN.md + EmptyStateCard contract).

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (no scenario open) | **Save scenario** (`Button variant="primary"`, in/near the composer header toolbar adjacent to the "Scenario" H2 at `ScenarioComposer.tsx:993`) |
| Primary CTA (a saved scenario is open) | **Update scenario** (writes back to the open row, touches `updated_at`) — with secondary **Save as new scenario** (`Button variant="secondary"`) beside it |
| Save name input | inline text input (NOT a modal), placeholder **Name this scenario** ; on-submit validates trim + 1–120 chars |
| Save validation error (empty) | **Enter a name to save this scenario.** (inline, below the input, `text-xs text-negative`) |
| Save validation error (too long) | **Scenario names are limited to 120 characters.** |
| List section heading | **Saved scenarios** (16px DM Sans semibold) |
| List row affordances | **Open** (`ghost`), **Rename** (inline edit), **Delete** (inline `danger`-tinted) |
| Empty state heading | **No saved scenarios yet** |
| Empty state body | **Compose a draft above, then choose "Save scenario" to keep it here. Saved scenarios reopen into the composer and can be compared side-by-side.** |
| Compare entry CTA | **Compare selected** (`Button variant="secondary"`, disabled until ≥2 rows checked; reuse the disabled `opacity-50 pointer-events-none` built into `Button`) |
| Compare empty/under-selection hint | **Select 2 or more scenarios (or the live book) to compare.** (mirrors CompareTable's existing "Select strategies to compare." line) |
| Live-book pseudo-row label | **Live book** (your current actual blend) |
| Compare Sharpe-leader callout | **Best Sharpe: {name}** (neutral `text-text-secondary`; the winning cell itself carries the accent ✓) |
| Degenerate compare cell | **—** (em-dash, via `formatPercent`/`formatNumber` null/non-finite path — never a fabricated `0`) |
| Below-sample-floor compare column | reuse `EmptyStateCard` / `sample-floor.ts` builder copy: heading + body naming the SPECIFIC shortfall (overlap N below 60). No invented metric. |
| Reopen — fingerprint drift | reuse EXISTING banner verbatim (`ScenarioComposer.tsx:1028`): "Your live holdings have changed since you last edited the scenario." — do not author new copy |
| Reopen — schema `reset` (older incompatible format) | **This saved scenario uses an older format and can't be reopened.** (honest notice; do NOT silently load an empty composer) |
| Reopen — schema `readonly` (newer build wrote it) | **This scenario was saved by a newer version and is read-only here.** (block edits honestly) |
| Load/save failure (hard error) | route through the canonical error path; problem + next step: **Couldn't save this scenario. Check your connection and try again.** / **Couldn't open this scenario. Try again.** |
| Destructive confirmation — Delete | **Delete "{name}"?** inline confirm with **Delete** (`danger`) + **Cancel** (`ghost`); small inline confirm, NOT a full modal (CONTEXT decision). Deletes the allocator's own row only. |

**Honesty authoring rules (non-negotiable, carried from #509 / Phase 21–22):**
- Every empty-state heading MUST match its body (no heading that implies data the body contradicts).
- A degenerate scenario renders `"—"`, never `0`, `0.00`, `0%`, or `N/A`.
- Each compare column stamps its OWN `methodologyLine(n)` (heterogeneous windows allowed — do NOT render a single shared-window header; benchmark window alignment is Phase 24).
- No fabricated values, no false precision, no silent recompute on drift.

---

## Component Inventory (Phase 23 net-new surfaces)

All new surfaces live on the allocations **Scenario tab** (`AllocationsTabs.tsx`, `TabKey="scenario"`). No new authenticated route.

**Focal point:** The primary visual anchor on the Scenario tab is the **"Save scenario" / "Update scenario"** CTA in the composer header toolbar; the saved-scenarios list and compare panel are subordinate, rendered below it.

### 1. Save / Update control (in the composer header toolbar)
- Lives in the `flex flex-wrap items-center gap-3` header row beside the "Scenario" H2 (`ScenarioComposer.tsx:992`).
- Default state: single `Button variant="primary"` **Save scenario** → click reveals an inline `Name this scenario` text input + a confirm `Button`. No modal.
- When a saved scenario is open (composer tracks the loaded scenario id): primary becomes **Update scenario**, with secondary **Save as new scenario**.
- Validation (trim + 1–120) inline; error copy per §Copywriting.

### 2. Saved-scenarios list (Scenario tab, adjacent to composer, `space-y-6` gap)
- Section heading **Saved scenarios** (16px semibold).
- Each row: white surface, 1px `#E2E8F0` border, 8px radius (Card pattern), `px-4`, ≥44px tall — mirrors the existing composer list-row shell (`ScenarioComposer.tsx:1386` `flex items-center justify-between gap-3 rounded-md border border-border p-3`).
  - Left: `[checkbox] {name}` (14px DM Sans) + a 12px `text-text-muted` timestamp line, and when cheaply available, an inline N + overlap-window stamp.
  - Right: **Open** (`ghost`) · **Rename** (inline edit → PATCH) · **Delete** (`danger` inline confirm).
- Empty state: `EmptyStateCard` with the §Copywriting heading/body.

### 3. Compare panel (in-tab, default; planner may choose panel vs modal)
- Triggered by **Compare selected** (enabled at ≥2 checked rows; a "Live book" pseudo-row participates).
- MIRRORS `CompareTable` EXACTLY:
  - `overflow-x-auto` wrapper → `<table className="w-full text-sm">`.
  - `thead`: first col `Metric` (`text-left px-4 py-3 text-xs font-semibold text-text-muted w-40`); each scenario column header right-aligned `text-xs font-semibold text-text-primary` showing the scenario name (+ "Live book").
  - `tbody` metric rows (`border-b border-border/50 hover:bg-page/50`), label cell `px-4 py-2.5 text-xs text-text-muted`, value cells right-aligned `px-4 py-2.5`, value span `text-xs font-metric`, winner = `text-accent font-bold` + " ✓" via `findWinner`.
  - **Metric rows (from `ComputedMetrics`):** Cumulative Return (`twr`, %), CAGR (%), Sharpe (number), Sortino (number), Max Drawdown (%), Volatility (%). `higherIsBetter`: false for Max Drawdown & Volatility, true for the rest — matches CompareTable's existing `METRICS` flags.
  - **Per-column honesty stamp:** a caption sub-row under each column (or a footer row) renders that column's own `methodologyLine(n)` (12px; N in Geist Mono). Heterogeneous windows are expected and correct.
  - **Sharpe leader callout:** **Best Sharpe: {name}** above/below the table (neutral text); PERSIST-04 ranking = Sharpe primary / return improvement.
- Degenerate column: cells render `"—"`; if the whole column is below the sample floor, the column is gated to honest absence copy (no fabricated row).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| (none) | (none) | not applicable — project does not use shadcn (`components.json` absent); all primitives are hand-rolled and already in-repo |

No third-party registries declared. No `npx shadcn view` vetting required. No new npm dependency this phase (CONTEXT: "No new dependencies / no Python").

---

## Accessibility Minimums (inherited from DESIGN.md DESIGN-05)

- Selection checkboxes are keyboard-focusable and labeled by the scenario name (`<label>`/`aria-label`); focus ring `focus:ring-accent/50`.
- Delete inline-confirm: the confirm/cancel pair is keyboard-reachable; destructive action is never a single-key activation.
- Honest empty states use `EmptyStateCard` (neutral, NO `role="alert"`); hard errors use the canonical error path with `role="alert"`.
- Compare table: text contrast ≥4.5:1 against `#FFFFFF`/`#F8F9FA` — accent `#1B6B5A`, `text-text-muted` `#64748B`, and negative `#DC2626` all already AA-pinned in DESIGN.md.
- `accessibilityLayer={false}` is NOT relevant here (no Recharts chart added; compare is a table). Any reused chart (reopened composer equity/drawdown) keeps the codebase-wide opt-out.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
