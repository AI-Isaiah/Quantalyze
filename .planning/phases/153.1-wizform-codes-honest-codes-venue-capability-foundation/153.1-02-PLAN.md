---
phase: 153.1-wizform-codes-honest-codes-venue-capability-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.test.ts
  - TODOS.md
autonomous: true
requirements: [WIZFORM-03]
must_haves:
  truths:
    - "Every supported venue has a capability row, and adding a seventh venue without one is a compile error"
    - "An unresolved venue is still scope-probed — the security control fails toward probing"
    - "sFOX's submit-time behaviour is byte-unchanged: its row asserts no capability at all"
    - "The description lower bound is one constant, not a literal in a route"
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "VENUE_CAPABILITIES + the three predicates + MAGNITUDE_CAPS.MIN_DESCRIPTION_CHARS"
      contains: "VENUE_CAPABILITIES"
    - path: "src/lib/closed-sets.test.ts"
      provides: "totality, null-direction and byte-unchanged-sFOX assertions; the MIN pin line"
      contains: "MIN_DESCRIPTION_CHARS"
  key_links:
    - from: "src/lib/closed-sets.ts"
      to: "SupportedExchange"
      via: "as const satisfies Record<SupportedExchange, VenueCapabilities>"
      pattern: "satisfies Record<SupportedExchange"
---

<objective>
Mint the two foundation values three later sub-phases read: the **per-venue capability record**
(so venue-shaped behaviour is a class rule, never `venue === "mt5"`) and
**`MIN_DESCRIPTION_CHARS`** (so the field guard, the submit guard and the server arm cannot drift
apart again).

Purpose: D-17's "never advise switching exchange to an MT5 user" and D-14's probe skip are the
same shape of question asked by two different layers. Answering it once, in the one isomorphic
module both layers already import, is what makes the fix a class fix.

Output: `VENUE_CAPABILITIES` + three predicates + `MAGNITUDE_CAPS.MIN_DESCRIPTION_CHARS` in
`src/lib/closed-sets.ts`, each pinned in `closed-sets.test.ts`.

⛔ **This plan adds NO venue to any offered set.** `UI_EXCHANGE_CODES`, `EXCHANGES`,
`FUNDING_EXCHANGES` and `CRYPTO_EXCHANGES` are byte-unchanged — Phase 153.2 owns the MT5 chip-set
widening and the `closed-sets.mt5-flag` pin re-cut (D-16, D-20). `mt5` is already a member of
`SUPPORTED_EXCHANGES` (`:38`) and `EXCHANGE_DISPLAY` (`:53`), so giving it a capability row adds
no membership anywhere.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-CONTEXT.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-PATTERNS.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-RESEARCH.md

<interfaces>
<!-- Extracted from src/lib/closed-sets.ts at HEAD 0c4f01d8. -->

export const SUPPORTED_EXCHANGES = ["binance","okx","bybit","deribit","sfox","mt5"] as const;  // :38
export type SupportedExchange = (typeof SUPPORTED_EXCHANGES)[number];                          // :39

export const EXCHANGE_DISPLAY = { binance:"Binance", okx:"OKX", bybit:"Bybit",
  deribit:"Deribit", sfox:"sFOX", mt5:"MT5" } as const satisfies Record<SupportedExchange,string>; // :48-55

// Analog C — subset registry + predicate, INCLUDING its null handling (:280-301)
export const CRYPTO_EXCHANGES = ["binance","okx","bybit","deribit","sfox"] as const satisfies readonly SupportedExchange[];
export function isCryptoExchange(exchange: string | null | undefined): boolean {
  if (!exchange) return false;
  return (CRYPTO_EXCHANGES as readonly string[]).includes(exchange.toLowerCase());
}

export const MAGNITUDE_CAPS = {   // :529-546
  MAX_NAME_CHARS: 80,
  MAX_DESCRIPTION_CHARS: 5000,
  …
} as const;

// Module import graph, VERIFIED at HEAD: the ONLY import is `zod` (:1); every module-load env
// read is NEXT_PUBLIC (:75, :98, :124) and therefore build-inlined; the non-public reads
// (`SFOX_ENABLED` :151, `MT5_ENABLED` :179) are inside functions. No `server-only`.
// ⇒ this module is safe to import from the isomorphic `wizardErrors.ts` (153.1-03 relies on
//    this measurement — record it if it drifts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mint VENUE_CAPABILITIES and its three predicates (D-17, D-22)</name>
  <files>src/lib/closed-sets.ts</files>
  <read_first>
    - `src/lib/closed-sets.ts` `:20-56` (the allowlist + `EXCHANGE_DISPLAY` — the
      `as const satisfies Record<SupportedExchange, …>` discipline to copy), `:100-124`
      (`MT5_UI_ENABLED` and the explicit "mt5 stays OUT of UI_EXCHANGE_CODES / EXCHANGES /
      FUNDING_EXCHANGES / CRYPTO_EXCHANGES" reasoning this plan must not disturb), `:280-301`
      (`CRYPTO_EXCHANGES` + `isCryptoExchange` — the predicate + null-handling analog).
    - `.planning/phases/153-…/153-PATTERNS.md` §⭐3 Analogs A, B and C, including the ⚠️ "fail
      direction inverts for the probe gate" note.
    - `.planning/phases/153-…/153-RESEARCH.md` §Pattern 3 (the recommended shape), §Pitfall 4,
      §Security Domain ASVS **V4** row, §Open Questions **Q2** (sFOX).
    - `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx:38-81` — the
      `requiresSecret?` / `passphraseSecret?` absent-means-incumbent precedent.
  </read_first>
  <action>
    Insert immediately AFTER `EXCHANGE_DISPLAY` / `ExchangeDisplay` (`:56`) and BEFORE the
    `SFOX_UI_ENABLED` flag block (`:58`). That placement keeps this plan's diff physically
    separate from the `UI_EXCHANGE_CODES` region (`:190-250`) Phase 153.2 edits, so the two
    sub-phases cannot collide in the same hunk.

    Declare an exported `VenueCapabilities` interface with three OPTIONAL booleans, each
    documented in the voice of `ExchangeOption`'s `passphraseSecret?` docblock — the docblock
    must state the default and WHY the default is the incumbent behaviour:
    - `scopeProbeSupported?` — absent ⇒ **true**. The venue answers a ccxt per-key permissions
      probe. `mt5` sets `false`: MT5 read-only is enforced structurally (`Mt5Client` composes
      only read methods) and behaviourally (`order_check` master-login rejection), so the probe
      has nothing to ask. **Consumer: Phase 153.2's `finalize-wizard` probe gate — not this
      sub-phase.**
    - `substitutable?` — absent ⇒ **true**. There exists another venue the user could plausibly
      use instead, so "switch to a different exchange" is a real remedy. `mt5` sets `false`: the
      account IS the venue (D-17; same unwinnable-remedy class as MT5-13). **Consumer: 153.1-03's
      copy filter.**
    - `serialized?` — absent ⇒ **false**. `mt5` sets `true`: every gateway call funnels through
      one terminal lease, so a wait is queueing rather than slowness. **Consumer: Phase 153.4's
      long-wait copy (ROADMAP §153.4 Depends-on names it).**

    Declare `VENUE_CAPABILITIES` as `as const satisfies Record<SupportedExchange,
    VenueCapabilities>` with **all six** members present. Five of them are the empty object `{}`
    — including `sfox` (D-22: sFOX keeps the submit-time scope probe byte-unchanged; only `mt5`
    opts out). Put the RESEARCH Q2 question in `sfox`'s inline comment: sFOX asserts
    `read_only=True` structurally for the same reason MT5 does, but changing its submit path is
    outside this phase's requirements — the question is logged, not answered here.
    Only `mt5` carries `{ scopeProbeSupported: false, substitutable: false, serialized: true }`.

    Export three predicates mirroring `isCryptoExchange`'s signature
    (`string | null | undefined`, lowercase the input, no throw). **Their null answers differ and
    that asymmetry is load-bearing — write the reason at each one or a reviewer will "unify"
    them:**
    - `venueSupportsScopeProbe(v)` → null/unknown/absent-row ⇒ **`true`**. This gate is a
      SECURITY control (ASVS V4, Pitfall 4): an unresolved venue must still be probed. The local
      precedent for "null ⇒ do the conservative thing" is `finalize-wizard/route.ts:864-872`.
    - `venueIsSubstitutable(v)` → null/unknown ⇒ **`true`**. Incumbent copy is preserved when the
      caller did not name a venue; suppressing venue-shaped copy everywhere would be a repo-wide
      copy regression D-17 did not ask for.
    - `venueIsSerialized(v)` → null/unknown ⇒ **`false`**. Never claim queueing we cannot observe
      (the TRAP-3 "a surface must not invent a specific fact" rule).

    ⛔ Do NOT add `mt5` to `CRYPTO_EXCHANGES`, `UI_EXCHANGE_CODES`, `EXCHANGES` or
    `FUNDING_EXCHANGES`. ⛔ Do NOT gate `VENUE_CAPABILITIES` on `MT5_UI_ENABLED`: a capability is
    a fact about the venue, not an offer, and flag-gating it would make the copy layer's
    behaviour depend on a UI flag.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean, and the `satisfies` totality is real: deleting the `mt5:` row
      must be a compile error. Prove it — delete the row, paste the `tsc` error, restore it.
    - Source assertion: `grep -n 'UI_EXCHANGE_CODES\|CRYPTO_EXCHANGES\|FUNDING_EXCHANGES' <(git
      diff -U0 src/lib/closed-sets.ts)` returns nothing — this plan touches no offered set.
    - `git diff src/lib/closed-sets.ts` shows the new block sits above the `SFOX_UI_ENABLED`
      declaration.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>`VENUE_CAPABILITIES` exists with six rows (five empty, `mt5` carrying three false/true capabilities) and three predicates with three deliberately different null answers, each documented.</done>
</task>

<task type="auto">
  <name>Task 2: Mint MAGNITUDE_CAPS.MIN_DESCRIPTION_CHARS and pin it (D-23)</name>
  <files>src/lib/closed-sets.ts, src/lib/closed-sets.test.ts</files>
  <read_first>
    - `src/lib/closed-sets.ts:529-546` — `MAGNITUDE_CAPS`, and the one-line docblock style each
      member carries.
    - `src/lib/closed-sets.test.ts:319-331` — the hand-typed magnitude-caps pin block and the
      `MAX_DOLLAR_VALUE_USD > MAX_TICKET_SIZE_USD` relational assertion below it.
    - `.planning/phases/153-…/153-RESEARCH.md` §Finding 8 (the grep showing the lower bound is a
      naked `10` in exactly one route).
  </read_first>
  <action>
    Add `MIN_DESCRIPTION_CHARS: 10` to `MAGNITUDE_CAPS`, adjacent to `MAX_DESCRIPTION_CHARS` so
    the pair reads as a bound pair. Its docblock states the three consumers that must read it —
    the `finalize-wizard` server arm (re-pointed in 153.1-05), the `MetadataStep` inline field
    guard and the `handleSubmit` predicate (both Phase 153.2, D-11/D-12/D-13) — and WHY: the
    upper bound has been single-sourced since `:535` while the lower bound was a naked literal in
    one route, which is how the client and server rules drifted into the 3-failed-submit incident.

    ⛔ Do not change the value. `10` is what the server enforces today at
    `finalize-wizard/route.ts:389`; this task single-sources the existing rule, it does not
    retune it.

    Extend the pin block at `closed-sets.test.ts:320-330` with a `MIN_DESCRIPTION_CHARS` line, and
    add one relational assertion beside the existing dollar-cap one: `MIN_DESCRIPTION_CHARS <
    MAX_DESCRIPTION_CHARS`, with a message naming what an inverted pair would do (refuse every
    description at the server while the client believes the field is valid).
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/closed-sets.test.ts` green; paste the summary line.
    - Source assertion: `grep -n "MIN_DESCRIPTION_CHARS" src/lib/closed-sets.ts src/lib/closed-sets.test.ts`
      returns exactly one declaration line and at least two test lines.
    - The pinned value is the hand-typed literal `10` on the test side, never
      `MAGNITUDE_CAPS.MIN_DESCRIPTION_CHARS` compared to itself.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.test.ts</automated>
  </verify>
  <done>`MAGNITUDE_CAPS.MIN_DESCRIPTION_CHARS` is `10`, pinned as a literal, with a relational assertion against the max.</done>
</task>

<task type="auto">
  <name>Task 3: Pin the capability record's properties, and log the deferred sFOX question</name>
  <files>src/lib/closed-sets.test.ts, TODOS.md</files>
  <read_first>
    - `src/lib/closed-sets.test.ts` in full (337 lines) — the describe-block layout and the
      failure-message voice used by the existing set pins.
    - `.planning/phases/153-…/153-VALIDATION.md` §Falsifiability Ledger row **SC-3** ("flip the
      `scopeProbeSupported` default from `true` to `false`" must red the fail-toward-probing
      test).
    - `TODOS.md` — the existing entry format (heading, date, owner) so the two logged items match
      house style.
  </read_first>
  <action>
    Add a `describe("venue capabilities")` block asserting the PROPERTIES, not the record's own
    shape read back at itself:
    - **Totality at runtime**: every member of `SUPPORTED_EXCHANGES` has a row in
      `VENUE_CAPABILITIES`. Iterate `SUPPORTED_EXCHANGES` (the oracle) and index the record (the
      subject) — never `Object.keys(VENUE_CAPABILITIES)` compared to its own length.
    - **sFOX is byte-unchanged (D-22)**: `VENUE_CAPABILITIES.sfox` has NO `scopeProbeSupported`
      key at all (`"scopeProbeSupported" in VENUE_CAPABILITIES.sfox === false`), and
      `venueSupportsScopeProbe("sfox") === true`. A message naming D-22 and RESEARCH Q2 — if
      someone opts sFOX out later, that is a decision, and this assertion is where it gets made.
    - **mt5 is the only opt-out**: exactly one venue in `SUPPORTED_EXCHANGES` answers
      `venueSupportsScopeProbe === false`, exactly one answers `venueIsSubstitutable === false`,
      exactly one answers `venueIsSerialized === true`, and in each case it is `mt5`. Assert the
      COUNT as a hand-typed `1`, not as a filter length compared to itself — a second
      non-probing venue arriving silently is exactly the SC-3 mutation the ledger is watching for.
    - **The three null directions**, each its own `it` with its own reason in the message:
      `venueSupportsScopeProbe(null | undefined | "" | "kraken") === true` (fail TOWARD probing —
      ASVS V4; this is the SC-3 falsifiability target),
      `venueIsSubstitutable(null | "kraken") === true`,
      `venueIsSerialized(null | "kraken") === false`.
    - **Case insensitivity**: `venueIsSubstitutable("MT5") === false`, mirroring
      `isCryptoExchange`'s `.toLowerCase()` contract, because `canonicalizeExchange` returns the
      display form `"MT5"` for an MT5 key (RESEARCH Finding 5).

    Then append two lines to `TODOS.md` (both explicitly out of scope here, per RESEARCH §Open
    Questions):
    - **Q2** — should sFOX also opt out of the submit-time scope probe? It asserts `read_only`
      structurally for the same reason MT5 does; unknown whether the ccxt probe currently
      succeeds for it or has been silently failing. Owner: unassigned. Reference: 153.1 D-22.
    - **Q5 / D-21** — `Validating…` (U+2026) at `CsvUploadStep.tsx:751` is inconsistent with the
      ASCII `Validating...` at the four other live sites; the ASCII form is the recorded
      superseding decision (`MultiKeyConnectStep.test.tsx:19-21`). A repo-wide copy sweep is not
      in this phase's scope. Owner: unassigned.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/closed-sets.test.ts` green; paste the summary line.
    - Falsifiability, RUN and pasted (SC-3): temporarily flip `venueSupportsScopeProbe`'s
      null/unknown answer from `true` to `false`; the fail-toward-probing `it` must RED. Paste the
      failing assertion, then revert and re-run green.
    - Falsifiability, RUN and pasted (class-not-instance): temporarily add
      `substitutable: false` to the `sfox` row; the "exactly one non-substitutable venue"
      assertion must RED. Paste it, revert, re-run green.
    - `TODOS.md` carries the two new entries; `git diff TODOS.md` shows additions only.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.test.ts</automated>
  </verify>
  <done>The capability record's properties are pinned by count and by null direction; both falsifiability mutations were run and observed red; the two deferred questions are in TODOS.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared lib → API route (Phase 153.2) | `venueSupportsScopeProbe` will gate `runScopeBroadeningProbe`, a scope-broadening SECURITY control (ASVS V4). This plan mints the decision data; the enforcement site is 153.2's. |
| shared lib → browser bundle | `closed-sets.ts` is imported by client components; nothing added here reads a non-public env var or a secret. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.1-04 | Elevation of Privilege | `venueSupportsScopeProbe` default direction | mitigate | A `false` default (or a `false` answer for an unresolved venue) silently disables the scope-broadening probe for every venue the resolver could not name — a key broadened to trade/withdraw between Connect and Submit would be promoted. Mitigated by: absent ⇒ `true`, null/unknown ⇒ `true`, an `it` per null form, and the SC-3 mutation RUN in Task 3. |
| T-153.1-05 | Elevation of Privilege | sFOX drifting into the opt-out | mitigate | D-22 pins sFOX byte-unchanged. Asserted structurally (`"scopeProbeSupported" in …sfox === false`) plus a hand-typed count of exactly ONE opt-out venue, so a second opt-out cannot arrive silently. |
| T-153.1-06 | Information Disclosure | none | accept | No secret, credential, env var or user datum is introduced. `MIN_DESCRIPTION_CHARS` and the capability booleans are public product facts already visible in server behaviour. |
| T-153.1-07 | Spoofing (venue identity) | predicate input handling | mitigate | Predicates take `string \| null \| undefined` and lowercase before lookup; an attacker-supplied venue string that matches no row falls to the conservative answer (probe = yes). No value is interpolated into a key, a query or a breaker name (T-140-01). |
| T-153.1-SC | Tampering | npm/pip/cargo installs | n/a | Zero package installs (RESEARCH §Package Legitimacy Audit, [VERIFIED]). No legitimacy checkpoint owed. |
</threat_model>

<verification>
- `npx vitest run src/lib/closed-sets.test.ts` green.
- `npx tsc --noEmit` clean.
- `npx vitest run src/lib/closed-sets.mt5-flag.test.ts` still green — this plan must not disturb
  the no-widening pin Phase 153.2 will re-cut.
- `git diff --stat` shows exactly `src/lib/closed-sets.ts`, `src/lib/closed-sets.test.ts`,
  `TODOS.md`.
</verification>

<success_criteria>
1. `VENUE_CAPABILITIES` is total over `SupportedExchange` at compile time (proved by deleting a
   row) and at runtime (proved by the iteration test).
2. `mt5` is the ONLY venue that opts out of anything, asserted as a hand-typed count of 1 for each
   of the three capabilities.
3. `venueSupportsScopeProbe` answers `true` for null, undefined, empty and unknown venues, and the
   SC-3 mutation was run and observed red.
4. `MAGNITUDE_CAPS.MIN_DESCRIPTION_CHARS === 10`, pinned as a literal, with a relational assertion
   against the max.
5. No offered venue set changed; `closed-sets.mt5-flag.test.ts` still green.
</success_criteria>

<output>
Create `.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-02-SUMMARY.md` when done.
Record the exact exported names (interface, record, three predicates) — 153.1-03 and Phases 153.2
and 153.4 import them by name.
</output>
