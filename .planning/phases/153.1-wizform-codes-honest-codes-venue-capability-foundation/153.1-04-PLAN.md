---
phase: 153.1-wizform-codes-honest-codes-venue-capability-foundation
plan: 04
type: execute
wave: 3
depends_on: ["153.1-03"]
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
autonomous: true
requirements: [WIZFORM-02, WIZFORM-03]
must_haves:
  truths:
    - "Every field the wizard can refuse has a code with copy that names the field and the rule"
    - "A field-level refusal offers no Retry control — retrying the same payload cannot change the outcome"
    - "The two failures that reach SubmitStep with no wizard member now have honest copy instead of UNKNOWN"
    - "A deadline the client itself imposed is classified as such, and offers no Retry"
    - "The table-size guard moved at BOTH sites, with its reasoning re-run"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "ten new WizardErrorCode members + copy entries + charCount/budgetSeconds context"
      contains: "SEAM_DEADLINE_EXCEEDED"
    - path: "src/lib/wizardErrors.test.ts"
      provides: "EXPECTED_TABLE_SIZE moved at both sites with re-run reasoning; per-member assertions"
      contains: "EXPECTED_TABLE_SIZE"
  key_links:
    - from: "src/lib/wizardErrors.ts"
      to: "src/lib/envelope.ts RECOVERABLE_ACTIONS"
      via: "actions arrays that deliberately omit clear_and_retry / try_another_key"
      pattern: "actions:"
---

<objective>
Mint the vocabulary the rest of this milestone spends: seven field-level metadata codes, the two
live `UNKNOWN` residuals, and `SEAM_DEADLINE_EXCEEDED`.

Purpose: WIZFORM-02's criterion is "no wizard failure renders `code: UNKNOWN` when the server DID
classify it". A code with no copy entry falls through to `UNKNOWN` exactly as a missing code does
— so the members must exist BEFORE the route starts emitting them (153.1-05) and before the
derived coverage assertion can be satisfied (153.1-06).

Output: ten new `WizardErrorCode` members with copy, two new context fields, and
`EXPECTED_TABLE_SIZE` moved at both sites with its reasoning re-run.

⛔ No route file is touched by this plan. The route starts emitting these in 153.1-05, which
also admits them to `KNOWN_FINALIZE_CODES` in the same commit (Shared Pattern D).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-CONTEXT.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-RESEARCH.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-UI-SPEC.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-PATTERNS.md
@.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-03-SUMMARY.md

<the_ten_members>
Derived from RESEARCH **Table A** + Open Question **Q3** (mint a member only where a FIELD-LEVEL
message must route back to a SPECIFIC field; arms 1, 2 and 8 are never user-typed and take the
existing `VALIDATION_FAILED`), plus RESEARCH **Finding 4**'s two live `UNKNOWN` residuals, plus
UI-SPEC Gate A's `SEAM_DEADLINE_EXCEEDED`.

| # | Member | Comes from | Field it routes to (153.2) |
|---|---|---|---|
| 1 | `METADATA_NAME_INVALID` | Table A arm 3 (`route.ts:383`) | codename `<Select>` |
| 2 | `METADATA_DESCRIPTION_TOO_SHORT` | Table A arm 4 (`:396`), short side | description |
| 3 | `METADATA_DESCRIPTION_TOO_LONG` | Table A arm 4, long side | description |
| 4 | `METADATA_CATEGORY_REQUIRED` | Table A arm 5 (`:405`) | category `<Select>` |
| 5 | `METADATA_AUM_INVALID` | Table A arm 6 (`:428`) | AUM input |
| 6 | `METADATA_CAPACITY_INVALID` | Table A arm 7 (`:439`) | capacity input |
| 7 | `METADATA_CAPITAL_OWNERSHIP_INVALID` | Table A arm 9 (`:503`) | the OWN-03 radio group |
| 8 | `SEAM_DEADLINE_EXCEEDED` | UI-SPEC Gate A | — (whole-panel) |
| 9 | `COMPOSITE_UNSUPPORTED_UNIFIED` | Finding 4 — emitted at `route.ts:1782`, renders UNKNOWN today | — |
| 10 | `DRAFT_STATE_INVALID` | Finding 4 — emitted lowercase at `route.ts:1319`, renders UNKNOWN today | — |

⚠️ `METADATA_DESCRIPTION_REQUIRED` **already exists** (union `:167`, entry `:882`). The
missing/non-string description case reuses it — do not mint an eighth metadata member.
⇒ `EXPECTED_TABLE_SIZE` moves **64 → 74**. That arithmetic is the planner's; the executor
RUNS the guard and reconciles (RESEARCH: "the final number must be computed from the actual
member list, not copied").
</the_ten_members>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The seven field-level metadata members (WIZFORM-02, Table A + Q3)</name>
  <files>src/lib/wizardErrors.ts</files>
  <read_first>
    - `src/lib/wizardErrors.ts` `:37-200` (the `WizardErrorCode` union — note the existing
      `METADATA_DESCRIPTION_REQUIRED` at `:167`), `:882-900` (that entry, the voice and shape to
      match), `:343-370` (`WizardErrorAction`, `WizardErrorCopy`), `:1564-1592`
      (`ALLOCATION_NOT_ALLOCATABLE` — the most recent additive member and the template for the
      structural Retry suppression).
    - `src/lib/envelope.ts:54-57` — `RECOVERABLE_ACTIONS = { clear_and_retry, try_another_key }`.
    - `.planning/phases/153-…/153-UI-SPEC.md` §"The description field — the rule that cost 3
      submits" (the four states table; the under-10 and over-5000 sentences VERBATIM) and
      §Copywriting Contract.
    - `.planning/phases/153-…/153-PATTERNS.md` §`src/lib/wizardErrors.ts` and §Shared Pattern **B**.
  </read_first>
  <action>
    Add the seven members to the `WizardErrorCode` union and seven entries to
    `WIZARD_ERROR_COPY`, each with `title`, `cause`, `fix`, `docsHref`, `actions`.

    **Recoverability — decide once, for the class:** a field-level refusal is NOT recoverable.
    Give every one of the seven an `actions` array containing **no member of
    `RECOVERABLE_ACTIONS`**, so `buildEnvelope` derives `recoverable: false` and no Retry control
    renders. Reason, written in the first entry's comment and cross-referenced from the rest:
    resubmitting the identical payload fails identically; the remedy is on the form. Start from
    `["expand_log"]`; if a table-walk sweep in `wizardErrors.test.ts` demands a second control
    (e.g. the destructive-action pairing guard), add `leave_and_return` — never
    `clear_and_retry`, which would wipe the user's typing, and never `try_another_key`, which is
    about credentials and has nothing to do with a description.

    **Copy rules** (UI-SPEC §Copywriting Contract): no raw exception, no HTTP status, no Python
    identifier, no module path, no env-var name. Name the field in the user's language, state the
    rule as a positive instruction, and say that nothing was saved where that is true.
    - `METADATA_DESCRIPTION_TOO_SHORT` / `_TOO_LONG` interpolate the user's current count. Add
      `WizardErrorContext.charCount?: number` and interpolate it in `formatKeyError` following
      the `CSV_FILE_TOO_LARGE` / `SIZE_MB_PLACEHOLDER` const-then-replace precedent (`:352-359`,
      `:1683-1688`). ⚠️ When `charCount` is ABSENT the sentence must NOT name a number — mirror
      `retryAfterSeconds`'s docblock exactly: absence means "we were not told", never "zero"
      (TRAP-3). Write the table's `title`/`cause` so they read correctly with no count, and let
      the interpolation arm produce the specific form.
      ⓘ Adding a per-code interpolation arm here is NOT the instance-not-class defect 153.1-03
      removed: interpolation is per-code by nature (five precedents), whereas requirement
      FILTERING is per-class and stays the single filter. Say this in a comment next to the new
      arm so a later reviewer does not "unify" them.
    - The other five state their rule plainly ("Pick a codename from the list", "Choose a
      category", "Enter AUM as a number of dollars, or leave it blank", …).
    - Bounds must read from `MAGNITUDE_CAPS` where they appear in copy, never as a typed `10` or
      `5000` (D-23's whole point). Format 5000 per the Numbers Contract (`5,000`).
    - D-21: any ellipsis authored here is ASCII `...`, never U+2026 — the ASCII form is the
      recorded superseding decision (`MultiKeyConnectStep.test.tsx:19-21`) and the incumbent at
      four of five live sites. (The `CsvUploadStep.tsx:751` inconsistency is logged in TODOS.md by
      153.1-02, not fixed here.)
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean (the union and the record are total by construction).
    - `npx vitest run src/lib/wizardErrors.test.ts` — expected to RED only on
      `EXPECTED_TABLE_SIZE` until Task 3. Paste the failure and confirm it names the size guard
      and nothing else. Any OTHER red is a copy-contract violation and must be fixed here.
    - Behaviour assertion, run and pasted: `buildEnvelope("METADATA_DESCRIPTION_TOO_SHORT",
      "corr-x").recoverable === false` for all seven members.
    - Behaviour assertion, run and pasted:
      `formatKeyError("METADATA_DESCRIPTION_TOO_SHORT")` (no context) contains no digit sequence
      that could be read as a character count; with `{ charCount: 2 }` it names `2`.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Seven field-level members exist with non-recoverable copy; the description pair interpolates `charCount` and says nothing numeric without it.</done>
</task>

<task type="auto">
  <name>Task 2: SEAM_DEADLINE_EXCEEDED + the two live UNKNOWN residuals (Finding 4)</name>
  <files>src/lib/wizardErrors.ts</files>
  <read_first>
    - `src/lib/wizardErrors.ts` `:1564-1592` (`ALLOCATION_NOT_ALLOCATABLE` — the ⛔ comment
      explaining that the ABSENCE of a recoverable action IS the behaviour being shipped),
      `:2130-2160` (the alias-table docblock, including the sentence that names
      `draft_state_invalid` and `COMPOSITE_UNSUPPORTED_UNIFIED` as deliberately out of scope),
      `:2157-2192` (`SEAM_CODE_TO_WIZARD_CODE`).
    - `src/app/api/strategies/finalize-wizard/route.ts` `:1313-1324` (the 409 whose code is
      lowercase `draft_state_invalid`) and `:1765-1790` (the 409 `COMPOSITE_UNSUPPORTED_UNIFIED`,
      including the `strategy_analytics` failed-stamp written just above it — the copy must be
      true of that state).
    - `.planning/phases/153-…/153-UI-SPEC.md` §Surface 3 Gate A — the `SEAM_DEADLINE_EXCEEDED`
      field table VERBATIM, and its ⛔ "do not replace Retry with a second button".
    - `.planning/phases/153-…/153-RESEARCH.md` §Finding 4.
  </read_first>
  <action>
    **`SEAM_DEADLINE_EXCEEDED`** — author from the UI-SPEC Gate A table. `actions` are
    `["request_call", "expand_log"]`: neither `clear_and_retry` nor `try_another_key`, so
    `recoverable` derives `false` and the panel renders NO Retry control. That absence IS the fix
    (Shared Pattern B) — a deadline inversion fails identically on every attempt, and the
    2026-08-08 panel offered Retry against exactly that. `docsHref: "/security#sync-timing"`.
    The UI-SPEC's `cause` interpolates `{budget}` seconds. Add
    `WizardErrorContext.budgetSeconds?: number` with a docblock copied in spirit from
    `retryAfterSeconds` (`:1616-1630`): OPTIONAL, and absence means "no budget was named" —
    never zero, never "immediately". Write the table `cause` WITHOUT a number so it is true when
    nothing is passed, and interpolate the specific sentence in `formatKeyError` when
    `budgetSeconds` is present. **Phase 153.4 is the consumer** (ROADMAP §153.4 Depends-on names
    this member) — say so in the entry's comment so it does not read as dead copy.

    **`COMPOSITE_UNSUPPORTED_UNIFIED`** — the route already emits it (`:1782`) and SubmitStep
    renders `UNKNOWN` for it today. Mint the member with copy that is true of the state the route
    just wrote: a multi-key strategy cannot finalize on this path, the analytics row is stamped
    failed, and the remedy is to contact us — not to retry. Non-recoverable.

    **`DRAFT_STATE_INVALID`** — mint the UPPER_SNAKE member. The route's literal is lowercase
    `draft_state_invalid` at `:1319`; **153.1-05 uppercases it in the same commit it starts being
    a wizard member** — this task only mints the member and its copy. Copy states the 409's truth
    ("this draft has moved on since this page loaded; reload and try again") and is
    non-recoverable: a Retry of the same POST cannot help, the page must reload.
    ⚠️ This is a behaviour change from today's `UNKNOWN`, whose copy IS recoverable and DOES
    render a Retry. Removing a control is a decision — write it in the entry's comment, naming
    what the control did and why it was a false affordance.

    **Rewrite the alias-table docblock at `:2149-2151` in this same edit.** Its sentence — "both
    are deliberately out of scope here and are recorded in the TS-35 ledger row, NOT silently
    absorbed" — becomes false the moment these members exist. Replace it with what is now true:
    both are wizard members as of Phase 153.1, minted because WIZFORM-02's criterion covers them
    by its own words (they ARE classified and DID render UNKNOWN). Leaving stale prose green while
    its premise breaks is the Pitfall-1 class this whole sub-phase exists to close.
    ⛔ Do NOT add either code to `SEAM_CODE_TO_WIZARD_CODE`. They are route-minted WIZARD codes,
    not wire codes needing translation; aliasing them would be the "vocabulary starts lying"
    failure that docblock warns about. `CIRCUIT_OPEN` stays an alias, unchanged.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean.
    - Behaviour assertions, run and pasted: `buildEnvelope(c, "corr").recoverable === false` for
      all three of `SEAM_DEADLINE_EXCEEDED`, `COMPOSITE_UNSUPPORTED_UNIFIED`,
      `DRAFT_STATE_INVALID`.
    - Behaviour assertion, run and pasted: `formatKeyError("SEAM_DEADLINE_EXCEEDED").cause`
      contains no digit; with `{ budgetSeconds: 120 }` it names `120`.
    - Source assertion: `grep -n "deliberately out of scope here" src/lib/wizardErrors.ts`
      returns nothing — the stale docblock sentence is gone.
    - Source assertion: `grep -c "COMPOSITE_UNSUPPORTED_UNIFIED\|DRAFT_STATE_INVALID"` inside the
      `SEAM_CODE_TO_WIZARD_CODE` map body is 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Three more members exist, all non-recoverable, and the alias-table docblock no longer claims two of them are out of scope.</done>
</task>

<task type="auto">
  <name>Task 3: Move EXPECTED_TABLE_SIZE at BOTH sites, with the reasoning re-run</name>
  <files>src/lib/wizardErrors.test.ts</files>
  <read_first>
    - `src/lib/wizardErrors.test.ts` `:1415-1446` (the first `EXPECTED_TABLE_SIZE`, its docblock,
      and the guard) and `:1620-1660` (the second — ⚠️ a DIFFERENT `describe` with its own copy of
      the constant; the planner verified both read `64` at HEAD).
    - `.planning/phases/153-…/153-PATTERNS.md` §"Table-size pin" — "the docblock RE-RUNS the
      reasoning over the new entries each time the number moves. Do the same, per new member —
      do not just bump the integer."
    - `.planning/phases/153-…/153-VALIDATION.md` — the `EXPECTED_TABLE_SIZE` test-map row naming
      both `:1437` and `:1649`.
  </read_first>
  <action>
    Move BOTH literals from `64` to the number the guard actually reports. The planner's
    arithmetic is `64 + 10 = 74`; **run the test first, read the actual count out of the failure
    message, and reconcile.** If it is not 74, a member was double-counted or one of the ten
    already existed — resolve that before touching the number. ⛔ Never replace either literal
    with `Object.keys(WIZARD_ERROR_COPY).length`; that is how the guard stops being able to fail
    (the docblock says so).

    Re-run each docblock's reasoning over the ten new entries — for each, one clause saying
    whether it belongs to the class that docblock's surrounding assertions scan (the first site
    guards a destructive-action pairing scan; the second guards a different sweep — read each and
    write the clause that site needs, not a copy of the other). ⚠️ Both sites are half of one
    fact: moving one and not the other is a silent half-fix (this sub-phase's stated trap #5).

    Add one assertion that the two sites cannot silently diverge again: a single `it` that
    asserts the table size equals the same hand-typed literal it is pinned to in BOTH describes.
    If extracting a shared module-level constant is cleaner, do that instead — but it must remain
    a hand-typed literal, declared once, referenced twice.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/wizardErrors.test.ts` GREEN. Paste the summary line.
    - Source assertion, pasted: `grep -n "EXPECTED_TABLE_SIZE" src/lib/wizardErrors.test.ts`
      shows the new value at every site and no remaining `64`.
    - Falsifiability, RUN and pasted: delete ONE of the ten new copy entries (and its union
      member); the size guard must RED at BOTH sites. Paste both failures, restore, re-run green.
    - `npx vitest run src/lib/envelope.test.ts src/app/\(dashboard\)/strategies/new/wizard/WizardErrorEnvelope.test.tsx`
      still green — no pre-existing envelope changed shape.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/envelope.test.ts</automated>
  </verify>
  <done>Both `EXPECTED_TABLE_SIZE` sites carry the measured number with re-run reasoning, and deleting a member reds both.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server error path → user-visible copy | Ten new copy entries reach the browser through `buildEnvelope`. |
| context → copy interpolation | `charCount` and `budgetSeconds` are numbers supplied by a caller and rendered into a sentence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.1-12 | Information Disclosure | new copy entries | mitigate | ASVS **V7**: no raw exception text, HTTP status, Python identifier, module path or env-var name in any of the ten entries. Enforced by the existing whole-table sweeps in `wizardErrors.test.ts` (the `blob` scans at `:741`, `:856`, `:1026`, `:1131`, `:1699`, `:1784`, `:1876`, `:2122`), which now walk the new entries automatically — the acceptance criteria require the file GREEN, not merely the size guard. |
| T-153.1-13 | Tampering (copy lies) | `charCount` / `budgetSeconds` absence | mitigate | TRAP-3: a surface must never name a duration or a count it did not receive. Both fields are optional; the table sentence is written to be true with NO number, and the specific sentence is produced only by the interpolation arm. Asserted by the two "contains no digit" behaviour checks. |
| T-153.1-14 | Elevation of Privilege | none | accept | No decision changes. These are copy entries; the server's refusal (ASVS V5) is unchanged and is made in 153.1-05's arms. |
| T-153.1-15 | Denial of Service (false affordance) | `actions` on the ten members | mitigate | A Retry control against a condition that fails identically every time is a user-facing DoS on their own time and the incident class that produced this phase (the founder clicked Retry five times). Mitigated structurally: no member of `RECOVERABLE_ACTIONS` on any of the ten; asserted via `buildEnvelope(...).recoverable === false`. |
| T-153.1-SC | Tampering | npm/pip/cargo installs | n/a | Zero package installs (RESEARCH §Package Legitimacy Audit, [VERIFIED]). |
</threat_model>

<verification>
- `npx vitest run src/lib/wizardErrors.test.ts src/lib/envelope.test.ts` green.
- `npx vitest run src/app/\(dashboard\)/strategies/new/wizard/WizardErrorEnvelope.test.tsx` green.
- `npx tsc --noEmit` clean; `npm run lint` clean.
- `git diff --stat` shows exactly `src/lib/wizardErrors.ts` and `src/lib/wizardErrors.test.ts`.
- Backward copy sweep (memory `project_milestone_v1_10_backbone_unification`): no string this plan
  changed is asserted anywhere in `e2e/`. Run
  `grep -rn "deliberately out of scope here" e2e/ || echo CLEAR` and record the result.
</verification>

<success_criteria>
1. Ten new `WizardErrorCode` members exist with copy entries; the union and the record are total.
2. All ten derive `recoverable: false` — no false Retry affordance anywhere in the new vocabulary.
3. `charCount` and `budgetSeconds` never appear in copy when absent from the context.
4. `EXPECTED_TABLE_SIZE` moved at BOTH sites, to a number READ FROM the guard's own failure, with
   each docblock's reasoning re-run over the new entries.
5. The stale alias-table sentence claiming two of these codes are out of scope is gone.
</success_criteria>

<output>
Create `.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-04-SUMMARY.md` when done.
Record the FINAL table size measured (not the planner's 74 unless it matched) and the exact
member names — 153.1-05 emits them and 153.1-06 asserts the roster admits them.
</output>
