---
phase: 153.1-wizform-codes-honest-codes-venue-capability-foundation
plan: 06
type: execute
wave: 5
depends_on: ["153.1-01", "153.1-05"]
files_modified:
  - src/lib/wizardErrors.invariant.test.ts
autonomous: true
requirements: [WIZFORM-02]
must_haves:
  truths:
    - "A code emitted by finalize-wizard and missing from SubmitStep's roster reddens CI by name"
    - "The next code added to the route cannot silently fall to UNKNOWN"
    - "A wire code the alias table translates is admitted through the alias, not by weakening the assertion"
    - "The two incumbent routes' pinned facts are unchanged by the third entry"
    - "Reordering one arm back to `{ error, code }` reddens an assertion — the derivation cannot go blind"
  artifacts:
    - path: "src/lib/wizardErrors.invariant.test.ts"
      provides: "third ROUTES entry, per-route site literals, alias-aware coverage law, raised vacuity floor"
      contains: "finalize-wizard"
  key_links:
    - from: "src/lib/wizardErrors.invariant.test.ts"
      to: "src/app/api/strategies/finalize-wizard/route.ts"
      via: "ROUTES entry with the widened status predicate"
      pattern: "finalize-wizard"
    - from: "src/lib/wizardErrors.invariant.test.ts"
      to: "SEAM_CODE_TO_WIZARD_CODE"
      via: "alias-aware admission for CIRCUIT_OPEN"
      pattern: "CIRCUIT_OPEN"
---

<objective>
Close D-09(a): the `finalize-wizard` ↔ `SubmitStep` roster stops being a hand-listed convention
and becomes **derived from the emitting sites**, with a coverage assertion that names the missing
code and a vacuity floor sized against the reordered total.

Purpose: the two-route mechanism has existed and been CI-wired since 142.2-07. WIZFORM-02's class
fix is one more `ROUTES` entry — but the entry is only honest if the incumbent per-route literals
stop being route-uniform, the coverage law consults the alias table, and the floor is sized so
that a single arm going blind reddens something.

Output: a third `ROUTES` entry, per-route site literals, an alias-aware admission rule, a raised
`DERIVED_FLOOR`, and both SC-2 falsifiability mutations RUN.

⛔ Test file only. No production source is touched by this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-CONTEXT.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-PATTERNS.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-VALIDATION.md
@.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-01-SUMMARY.md
@.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-05-SUMMARY.md

<what_a_third_entry_actually_breaks>
⚠️ Adding a `ROUTES` entry is NOT a one-line change. Four existing assertions are written as
per-route sweeps with ROUTE-UNIFORM expectations, verified at HEAD `0c4f01d8`:

| Assertion | Line | What the third entry does to it |
|---|---|---|
| `"%s: the site count is the LITERAL 12"` (`it.each(ROUTES…)`) | `:246-265` | REDS — finalize-wizard has 25 sites, not 12. The literal must become per-route. |
| `"%s: KEY_INVALID_FORMAT survives at exactly ONE guard"` | `:297-312` | REDS — finalize-wizard emits it ZERO times. This is a 142.2-07 fact about the two ccxt routes; scope it to them. |
| `"the emitted vocabulary is the hand-typed split set — no more, no less"` (flatMaps ALL routes) | `:322-330` | REDS — finalize-wizard's vocabulary is not `EXPECTED_SPLIT_CODES`. Scope it to the two split routes. |
| `"the two routes emit the SAME set of codes"` (`const [a, b] = derived`) | `:314-320` | Stays green by accident (it destructures the first two) — but its name and comment become false. Name the two routes explicitly instead of relying on array position. |

And one that reds for a REAL reason, which must be fixed properly, not weakened:

| `"%s: every emitted code is a member of the WizardErrorCode union"` | `:267-279` | REDS on `CIRCUIT_OPEN` — a WIRE code deliberately kept OUT of `WizardErrorCode` and translated by `SEAM_CODE_TO_WIZARD_CODE` (`wizardErrors.ts:2163`). RESEARCH Finding 4: "the derivation must consult the alias table, exactly as `MultiKeyConnectStep.tsx:201-207` describes (coverage-law row 1)." |
</what_a_third_entry_actually_breaks>

<interfaces>
<!-- The entry shape, verbatim from wizardErrors.invariant.test.ts:103-127, plus 153.1-01's field -->
interface RouteUnderTest {
  readonly label: string;
  readonly route: string;
  readonly rosterFile: string;
  readonly rosterName: string;
  readonly statusRe: string;   // added by 153.1-01; "400" for the two incumbents
}
const WIZARD_STEPS = join(REPO,"src","app","(dashboard)","strategies","new","wizard","steps");  // :60-69

<!-- The third entry's coordinates -->
route:      join(REPO, "src/app/api/strategies/finalize-wizard/route.ts")
rosterFile: join(WIZARD_STEPS, "SubmitStep.tsx")
rosterName: "KNOWN_FINALIZE_CODES"
statusRe:   "[45]\\d\\d"

<!-- The alias table to consult (import from ./wizardErrors if exported; otherwise derive it
     from source the same way the union is derived — do NOT re-type its contents) -->
const SEAM_CODE_TO_WIZARD_CODE: ReadonlyMap<string, WizardErrorCode>;  // wizardErrors.ts:2157-2192
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Third ROUTES entry + per-route literals + the alias-aware coverage law</name>
  <files>src/lib/wizardErrors.invariant.test.ts</files>
  <read_first>
    - `src/lib/wizardErrors.invariant.test.ts` in full (422 lines at HEAD, plus 153.1-01's
      additions) — especially `:169-203` (the three literal oracles and the doctrine in their
      docblocks) and `:216-330` (every assertion the third entry touches).
    - `src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx:195-215` — the
      "coverage law" docblock that states how a wire code is admitted through the alias table
      rather than by adding a non-union member to the roster.
    - `src/lib/wizardErrors.ts:2130-2192` — the alias-table docblock and
      `SEAM_CODE_TO_WIZARD_CODE`.
    - `.planning/phases/153.1-…/153.1-05-SUMMARY.md` — the measured site and roster counts to pin.
  </read_first>
  <action>
    **(a) Add the third entry** to `ROUTES` with the coordinates in `<interfaces>`.

    **(b) Move the site-count literal onto the entry.** Replace the module-level
    `EXPECTED_SITES_PER_ROUTE = 12` with an `expectedSites` field on `RouteUnderTest`: `12` for
    both incumbents (byte-identical to today's expectation) and the measured count for
    finalize-wizard — the planner measured **25** (14 reordered + 11 arms); use 153.1-05's
    recorded number and reconcile if it differs. Keep the docblock's doctrine verbatim in spirit:
    hand-typed literals, NEVER `derived.length`; a size compared against its own derivation cannot
    fail. Keep the failure message's two branches (a guard was ADDED → give it an honest code and
    bump this literal; a guard was REMOVED → that is a bigger question than this test).

    **(c) Scope the two 142.2-07 facts to the routes they are facts about.** The
    `KEY_INVALID_FORMAT`-exactly-once assertion and the "emitted vocabulary is the hand-typed
    split set" assertion describe the ccxt key-validation split. Drive both off an explicit list
    of the two split-route labels rather than off `ROUTES` wholesale, and say in the comment that
    finalize-wizard is deliberately outside them because it validates wizard METADATA, not key
    material. ⛔ Do not delete either assertion and do not widen `EXPECTED_SPLIT_CODES` to absorb
    the new vocabulary — that would retire a guard to make a new route fit.

    **(d) Name the mirror pair explicitly.** Change `const [a, b] = derived` to look the two split
    routes up by label, so the assertion cannot silently start comparing the wrong pair when a
    fourth route lands.

    **(e) The alias-aware coverage law.** Both the union assertion and the roster assertion must
    admit a code that `SEAM_CODE_TO_WIZARD_CODE` translates: a derived code is admitted if it is
    a union member, OR the alias table maps it to a union member (and for the roster, if the
    roster admits the TRANSLATED code). `CIRCUIT_OPEN` → `SERVICE_UNAVAILABLE_RETRY` is the live
    case and the only one today.
    ⛔ Get the alias table from the module (import it if exported; otherwise derive its pairs from
    source, bounded to the `new Map<…>([ … ])` block, the same way `deriveUnionMembers` is
    bounded). **Never re-type its contents into the test** — a hand-copied alias list is a second
    source of truth that goes stale silently, which is the failure this whole file exists to
    prevent. Add a SELF-TEST that the alias derivation is non-empty and contains the
    `CIRCUIT_OPEN` pair; an empty alias table would make the widened admission rule a no-op that
    passes for the wrong reason.
    Write the rule's reason in the assertion's failure message: a wire code is not a wizard
    member on purpose, and admitting it by adding a non-union string to a
    `ReadonlySet<WizardErrorCode>` would not compile and would be wrong if it did.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/wizardErrors.invariant.test.ts` GREEN with THREE `ROUTES` entries.
      Paste the full test-name list — ⛔ never "assume the scanner reaches it".
    - Behaviour proof, pasted from the run: the per-route site counts reported are `12`, `12` and
      the finalize-wizard number, and the finalize-wizard roster parsed non-empty. If
      finalize-wizard's derived count is `0` or suspiciously low, STOP — that is the Pitfall-3
      warning sign, not a passing test.
    - Falsifiability, RUN and pasted (**SC-2, arm #7 not #1**): delete `code:` from the SEVENTH of
      the eleven `validatePayload` arms. The per-route site-count assertion must RED. Paste it,
      revert, re-run green.
    - Falsifiability, RUN and pasted (alias law): temporarily remove the `CIRCUIT_OPEN` pair from
      the alias derivation's input; the union assertion must RED naming `CIRCUIT_OPEN`. Paste it,
      revert.
    - Non-regression: both incumbent routes' assertions are unchanged in expectation — `git diff`
      shows no change to the value `12` for either, and `EXPECTED_SPLIT_CODES` is untouched.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.invariant.test.ts</automated>
  </verify>
  <done>Three routes are covered; the two incumbents' pinned facts are unchanged; `CIRCUIT_OPEN` is admitted through the alias table, derived from source, with a SELF-TEST against a vacuous alias map.</done>
</task>

<task type="auto">
  <name>Task 2: Size the vacuity floor against the REORDERED total, and prove it can fail (D-34)</name>
  <files>src/lib/wizardErrors.invariant.test.ts</files>
  <read_first>
    - `src/lib/wizardErrors.invariant.test.ts` `:193-203` (`DERIVED_FLOOR = 14` and the docblock
      explaining "~60% of 24 measured sites" and why an empty derivation is green forever) and
      `:230-250` (the vacuity assertion and its PREDICATE prose).
    - `.planning/phases/153.1-…/153.1-01-SUMMARY.md` — the measured finalize-wizard count at HEAD
      **before** the reorder (it will be near zero). That number is the receipt that the reorder
      was load-bearing.
    - `.planning/phases/153-…/153-VALIDATION.md` §Falsifiability Ledger rows **SC-2** (both), and
      §Oracle Independence's closing paragraph on the site-count floor.
  </read_first>
  <action>
    Raise `DERIVED_FLOOR` from `14` to a hand-typed value sized against the **combined reordered
    total** — 24 (the two incumbents) plus finalize-wizard's measured count (planner: 25) ⇒ ~49
    measured, floor at roughly 60%, i.e. about **29**. ⛔ Sizing it against the nine new arms, or
    leaving it at 14, is the trap the ROADMAP names: with 49 sites available a floor of 14 is
    satisfied even if the entire finalize-wizard derivation collapses to zero.
    Rewrite the docblock's arithmetic sentence so it states the NEW measured total and the NEW
    percentage. A docblock that still says "~60% of 24" while guarding 49 is the Pitfall-1 class.

    Extend the vacuity test's PREDICATE prose (`:233-244`) to describe the per-route status
    predicate — "status 400 for the two key-validation routes; any 4xx/5xx for finalize-wizard,
    which answers 400/403/404/409/502/503" — and extend its per-route roster-non-empty loop so it
    covers `KNOWN_FINALIZE_CODES`. ⚠️ That loop's `toBeGreaterThan(10)` bound was written for the
    two 24-member rosters; confirm the finalize-wizard roster clears it and record the measured
    size.

    Add ONE assertion the file does not have: a per-route MINIMUM that fails when a single route's
    derivation collapses while the combined total still clears the floor. The per-route
    `expectedSites` literal from Task 1 already does this exactly — so instead of duplicating it,
    write a comment at `DERIVED_FLOOR` stating the division of labour explicitly (the floor
    catches a TOTAL scanner break; the per-route literals catch a SINGLE site or route going
    blind) so a future reader does not "simplify" one into the other.

    Then RUN the second SC-2 mutation and record which assertion catches it. VALIDATION says "if
    only the roster assertion reds, the floor is too low" — with per-route literals the honest
    answer is that the per-route count reds first. Record that, do not paper over it.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/wizardErrors.invariant.test.ts` GREEN. Paste the summary line.
    - Falsifiability, RUN and pasted (**SC-2, reorder**): reorder ONE `validatePayload` arm back
      to `{ error: "…", code: "…" }`. Record EVERY assertion that reds (the per-route site count
      must be among them). Revert, re-run green.
    - Falsifiability, RUN and pasted (total collapse): temporarily break the emitter regex (e.g.
      require a code literal that cannot occur) so the whole derivation goes to zero; the
      `DERIVED_FLOOR` assertion must RED. Paste it, revert.
    - Source assertion: `grep -n "DERIVED_FLOOR" src/lib/wizardErrors.invariant.test.ts` shows the
      new value, and the docblock beside it states the new measured total — not `24`.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.invariant.test.ts</automated>
  </verify>
  <done>The floor is sized against the reordered total with a docblock that says so; both SC-2 mutations were run and the assertions that caught them are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Test file only. This plan changes no request path, response body or persisted value. What it protects is the honesty of the wizard's error vocabulary, which is a **detective** control over the boundaries 153.1-05 crosses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.1-21 | Repudiation (a guard that cannot fail) | the derived coverage assertion | mitigate | The named failure mode of this whole mechanism: a derivation that matches nothing satisfies every "missing is empty" assertion. Mitigated by three independent levers — the per-route `expectedSites` literals, the raised `DERIVED_FLOOR`, and the roster/union non-empty guards — plus THREE mutations RUN (SC-2 ×2 and the total-collapse one) with their catching assertions recorded. |
| T-153.1-22 | Tampering | the alias-aware admission rule | mitigate | Widening admission is exactly how a real gap gets "fixed" into invisibility. Mitigated by deriving the alias table from source rather than re-typing it, by a SELF-TEST that reds on a vacuous alias map, and by keeping the admission scoped to codes the table actually translates (one today). |
| T-153.1-23 | Tampering | scoping the two 142.2-07 assertions | mitigate | Scoping an assertion to fewer routes could quietly retire it. Mitigated by requiring `EXPECTED_SPLIT_CODES` and the `12` literals to be untouched in the diff, and by naming the two split routes explicitly rather than by array position. |
| T-153.1-SC | Tampering | npm/pip/cargo installs | n/a | Zero package installs (RESEARCH §Package Legitimacy Audit, [VERIFIED]). |
</threat_model>

<verification>
- `npx vitest run src/lib/wizardErrors.invariant.test.ts` green with three routes.
- `npm test` (full vitest) green — this is the sub-phase's last plan and the point at which the
  whole TypeScript suite must be clean.
- `npx tsc --noEmit` clean; `npm run lint` clean.
- `git diff --stat` shows exactly one file.
- ⚠️ Do not run the python and e2e-seeded suites concurrently against the shared TEST DB
  (VALIDATION §Test Infrastructure).
</verification>

<success_criteria>
1. `ROUTES` holds three entries; the derived coverage assertion reds BY NAME when a
   `finalize-wizard` code is missing from `KNOWN_FINALIZE_CODES`.
2. `CIRCUIT_OPEN` is admitted through a source-derived alias table, with a SELF-TEST that reds if
   that table parses empty.
3. Site counts are per-route hand-typed literals: `12`, `12`, and the measured finalize-wizard
   number.
4. `DERIVED_FLOOR` is sized against the reordered combined total, with a docblock stating the new
   measurement and the division of labour against the per-route literals.
5. Three mutations were RUN — SC-2 code-removal (arm #7), SC-2 reorder, and a total scanner
   collapse — each with the catching assertion pasted.
</success_criteria>

<output>
Create `.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-06-SUMMARY.md` when done.
Record the final measured numbers (per-route sites, `DERIVED_FLOOR`, roster sizes) and, for each
of the three mutations, WHICH assertion caught it. Phase 153.2 adds codes to this same route and
will read those numbers.
</output>
