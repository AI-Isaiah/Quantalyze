# Phase 153.1: WIZFORM-CODES — Honest codes + the venue-capability foundation - Context

**Gathered:** 2026-08-08
**Status:** Ready for planning
**Source:** Inherited from the Phase 153 split (founder-approved 2026-08-08)

<domain>
## Phase Boundary

**Delivers.** No wizard failure renders `code: UNKNOWN` when the server DID classify it, and
the copy layer gains a per-venue **capability record** so venue-shaped remedies are filtered
by a *class* rule rather than by stacked instance checks.

Concretely:

- The nine code-less `validatePayload` 400 arms in `finalize-wizard/route.ts` each carry a
  `code` (D-09b).
- The client rosters stop being hand-listed and become **derived from the emitting sites**,
  with a coverage assertion and a non-vacuous site-count floor (D-09a).
- The six pre-existing `{ error, code }` / lowercase arms are **reordered** so the emitter
  scanner can see them once the predicate widens (D-34).
- `VENUE_CAPABILITIES` is minted in `src/lib/closed-sets.ts` — the one isomorphic home the
  server probe gate, the copy gating and the client all read (D-17, D-22).
- `MIN_DESCRIPTION_CHARS` is minted so the field guard, the submit guard and the server arm
  read one constant (D-23).
- The A-25 **derived** breaker assertion lands in `seam-constants.pin.test.ts` as a **Wave-0
  gate, green at HEAD** (D-19's mechanism, so 153.4's budget raise cannot pass a pin that
  cannot fail).
- The `Validating...` (ASCII) spelling is settled in the copy layer (D-21).

**Requirements:** WIZFORM-02, WIZFORM-03.

**Owns:** `src/lib/closed-sets.ts`(+test), `src/lib/wizardErrors.ts`(+test, +invariant test),
`finalize-wizard/route.ts` (**validatePayload only**), `SubmitStep.tsx` (**roster only**),
`seam-constants.pin.test.ts` (**Wave-0 A-25 assertion only**).

**Does NOT deliver — and who owns each excluded item:**

- Inline field validation, the `MetadataStep` / `AllocateDialog` field guards, the
  `disabled`-removal, and the submit-guard widening — **Phase 153.2** (WIZFORM-FIELD). 153.1
  only mints the `MIN_DESCRIPTION_CHARS` constant those guards read.
- The `finalize-wizard` **probe gate** (skipping the ccxt scope probe for MT5) and the
  `KEY_NETWORK_TIMEOUT` **catch-all** — **Phase 153.2**. 153.1 only mints the capability
  record (`scopeProbeSupported`) the gate reads; 153.1 touches `finalize-wizard/route.ts`
  inside `validatePayload` (and the D-34 reorder) and nowhere else.
- The MT5 **chip-set widening** in `closed-sets.ts` (`UI_EXCHANGE_CODES` / `EXCHANGES`) and
  the `closed-sets.mt5-flag` pin re-cut — **Phase 153.2** (D-16, D-20). 153.1 owns the
  *file* for the `VENUE_CAPABILITIES` addition; the MT5 set-membership edit is 153.2's.
- Every Python change — the end-to-end probe deadline, the `initialize()` timeout inversion,
  the terminal lease, `terminal_info()` fail-closed, per-stage instrumentation —
  **Phase 153.3** (WIZFORM-GW).
- The `120_000` ms budget row, `BREAKER_LOCK_TOMBSTONE_S` 60 → 90, `budgetKeyFor`, the 16 pin
  sites, the long-wait card and `Stop waiting` — **Phase 153.4** (WIZFORM-BUDGET). 153.1
  lands only the *derived A-25 assertion itself*, green at HEAD; 153.4 raises the budget
  against it.

</domain>

<decisions>
## Implementation Decisions

### Honest codes (WIZFORM-02) — ⚠️ SCOPE CORRECTED BY SOURCE OBSERVATION

- **D-08: The "SECOND LIVE INSTANCE" roster gap is ALREADY CLOSED — the 3-member
  stopgap landed.** Verified at source 2026-08-08: `KNOWN_CREATE_WITH_KEY_CODES`
  (`ConnectKeyStep.tsx:312-314`) and `KNOWN_ADD_KEY_CODES` (`MultiKeyConnectStep.tsx`)
  BOTH now contain `SERVICE_UNREACHABLE`, `KEY_MISSING_READ_SCOPE` and
  `KEY_PERMISSION_DENIED` (22 members each, identical sets). ⛔ Do not re-fix this.
  REQUIREMENTS.md still describes it as open ("A 3-member stopgap may land earlier via
  hotfix; the class fix stays here") — the stopgap is the thing that landed.
- **D-09: What REMAINS is the CLASS fix, and it is two distinct problems.**
  **(a)** Both rosters are still **hand-listed** `new Set([...])` literals — the header
  even says "enumerated from the route rather than remembered", which is a convention,
  not a mechanism. The sweep must be **derived from the emitting sites** with a coverage
  assertion, so the next added code cannot silently fall to `UNKNOWN`.
  **(b)** **All nine `validatePayload` 400 arms in `finalize-wizard/route.ts` are still
  code-less** — verified at source: `:347` ("Invalid request body"), `:374`, `:383`,
  `:396` ("description must be 10-5000 characters" — the arm that cost 3 submits),
  `:405`, `:428`, `:439`, `:474`, `:503`.
- **D-10: Re-derive every line number from source; the ones in REQUIREMENTS.md have
  drifted.** The requirement cites `:338-346`, `:345`, `:298/:324/:333/:355/:381/:392/:427`;
  phases 150–152 shifted the file. The requirement itself says the 2026-07-08 stored
  learning is STALE and must be re-derived — that instruction now also applies to its own
  line numbers.

- **D-34: ⚠️ the emitter-order trap is SIX PRE-EXISTING sites, not one — and widening the
  status predicate makes them ALL invisible at once.** Found by the planner, verified at
  HEAD 2026-08-08. `wizardErrors.invariant.test.ts:100-101` requires `code:` as the FIRST
  key and matches `[A-Z][A-Z0-9_]*`. In `finalize-wizard/route.ts` the existing coded arms
  are written the WRONG way round at **`:573`** (`CIRCUIT_OPEN`), **`:617`**
  (`KEY_NETWORK_TIMEOUT`), **`:767`** and **`:1293`** (`GATE_DRAFT_GONE`), **`:1310`**
  (`GUARD_BLOCKED`) — all `{ error, code }` — and **`:1319`** (`draft_state_invalid`) is
  `code:`-first but **lowercase**, so it fails the literal class as well.
  ⛔ The danger is compound: today `EMITTER_RE` also gates on `status: 400`, so these six
  are simply out of scope. The moment the third `ROUTES` entry lands **and** the predicate
  widens past 400, all six become sites the scanner *should* see and silently does not —
  the coverage assertion goes blind on the **pre-existing** arms, not merely on the nine
  new ones. **Reordering these six is IN SCOPE**, and the site-count vacuity floor must be
  sized against the reordered total, not against the nine.

### Venue-shaped copy (WIZFORM-03)

- **D-17: "Switch to a different exchange" must never render for MT5.** Unwinnable-remedy
  class — the account IS the venue. Same family as MT5-13 and the deleted "0 trades"
  message.

### Foundation constants and the copy layer

- **D-21: `Validating...` (ASCII) wins; the UI-SPEC's `Validating…` is CORRECTED, not
  blended.** Rule 7 — there is a recorded superseding decision plus four live call sites.
  One spelling, chosen, everywhere.
- **D-22: sFOX keeps the submit-time scope probe byte-unchanged.** Only `mt5` opts out,
  via the capability. The probe is a security control: it must fail *toward* probing when
  the venue is unknown/null.
- **D-23: mint a `MIN_DESCRIPTION_CHARS` constant.** The `10` is a bare literal at
  `finalize-wizard/route.ts:389`; the field guard, the submit guard and the server arm
  must all read the same constant or they will drift apart again.

**Corrections to earlier decisions, verified at source by research — the substantive
claims survive, the numbers do not:**
- D-08 says "22 members"; the true count is **24** (a `[A-Z_]+` regex misses
  `KEY_MT5_MASTER_PASSWORD` / `KEY_MT5_WRONG_SERVER`). All three stopgap codes are
  present and the two rosters are identical, as D-08 claims.
- D-16 says the `closed-sets.mt5-flag` pin covers four sets; it asserts **two**
  (`UI_EXCHANGE_CODES`, `EXCHANGES`).

### Superseded decisions — carried for the record, NOT to be implemented

- **D-18 (SUPERSEDED by D-24 — kept for the record):** an earlier reading set the budget at
  `90_000` ms because that was the largest value needing only ONE constant changed under
  the A-25 breaker invariant. **That reasoning was backwards** — it let a test invariant
  pick a production timeout. Founder challenge 2026-08-08: *"I don't know if 90s is the
  right number… maybe you do some research how much is enough time."* Superseded.

- **D-28: `close()` must be bounded separately.** Every one of the 9 failures was followed
  by a *second* full 30.02–30.03 s in `Mt5Client.close: shutdown() raised` — so a failed
  attempt costs ~60 s of server wall clock, already 2× the client budget before any broker
  slowness. The `finally`-close (`exchange.py:448-465`) runs on every path and must keep
  doing so; it needs its own bound, NOT inclusion inside the end-to-end deadline (that
  would leak the RPyC session).

  ⚠️ **SUPERSEDED by D-30**, which takes `shutdown()` off the request path entirely, so
  there is no per-request close to bound. Recorded here because the A-25 sizing
  conversation references it; the Python change itself is **Phase 153.3**.

### Inherited but NOT owned here

- **D-16 / D-20 (owner: Phase 153.2)** — the MT5 chip-set widening and the
  `closed-sets.mt5-flag` pin re-cut edit `src/lib/closed-sets.ts`, the file this sub-phase
  otherwise owns. 153.1 must add `VENUE_CAPABILITIES` in a way that does not pre-empt or
  collide with that widening, and must leave `CRYPTO_EXCHANGES` mt5-free.
- **D-11 / D-12 / D-13 (owner: Phase 153.2)** — the inline-validation behaviour that
  consumes `MIN_DESCRIPTION_CHARS`. The constant must be exported in the shape those
  guards need (one module, one value, importable client-side).
- **D-14 (owner: Phase 153.2)** — the probe gate and the `KEY_NETWORK_TIMEOUT` catch-all
  read `scopeProbeSupported` from the record minted here.
- **D-19 / D-26 (owner: Phase 153.4)** — 153.4 raises the budget to `120_000` and moves
  `BREAKER_LOCK_TOMBSTONE_S` 60 → 90 in one commit; the **derived** A-25 assertion this
  sub-phase lands is the gate that must be able to fail when they do not move together.
- **D-05 / D-29 (owner: Phases 153.4 / 153.3)** — the long-wait copy and the "waiting for
  the connection" state consume the copy table this sub-phase owns; leave room for them
  rather than closing the table's shape against them.

### Claude's Discretion

- The mechanism for the derived roster in D-09(a) (generated union, exported const from
  the route module, or a build-time assertion) — the requirement mandates the property
  ("driven from the emitting sites"), not the technique.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-CONTEXT.md`
  — the parent's full D-01..D-34 (this file carries only this sub-phase's slice)
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-RESEARCH.md`
  — **Finding 3** (the derived-roster mechanism already exists and is CI-wired),
  **Finding 4** (two live UNKNOWN instances the derivation will surface),
  **Finding 6** (`buildEnvelope` is a pass-through, so `fix[]` gating must live in
  `formatKeyError`), **Finding 8** (there is no `MIN_DESCRIPTION_CHARS` constant),
  **Finding 9** (the typographic-ellipsis conflict D-21 settles),
  **Finding 10** (roster member-count correction), **Table A** (the nine code-less
  `validatePayload` 400 arms with proposed codes), **Pattern 3** (per-venue capability,
  DEFAULT preserves today's behaviour — the `VENUE_CAPABILITIES` shape),
  **Pitfall 3** (the emitter regex will not see `{ error, code }` order),
  **Pitfall 1** (a hand-typed pin that restates a property cannot see the property break)
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-PATTERNS.md`
  — **⭐2 The derived-roster invariant test — `ROUTES` entry shape, verbatim**;
  **⭐3 Per-venue CAPABILITY (class), never `venue === "mt5"` (instance)**;
  the `src/lib/wizardErrors.ts` (shared registry, transform) section;
  the `src/app/api/strategies/finalize-wizard/route.ts` (route handler) section;
  the `SubmitStep.tsx` (roster + field-level code routing) section;
  **Shared Pattern C** (a hand-typed pin is a MANUAL checklist item when it restates a
  table fact), **D** (a new code is admitted to its roster IN THE SAME COMMIT the route
  starts emitting it), **E** (every error path scrubs through the ONE shared leaf)
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-VALIDATION.md`
  — Wave 0 items owned here: *"Vacuity floor + SELF-TEST for the widened emitter predicate
  in `wizardErrors.invariant.test.ts` — WIZFORM-02"*, *"Class-level sweep: no
  `substitutable:false` venue receives a substitution bullet — WIZFORM-03"*,
  *"Surface-absent ⇒ bullet suppressed — WIZFORM-03"*, and *"⭐ A-25 derived-longest-budget
  assertion in `src/lib/seam-constants.pin.test.ts`"* (landed here green-at-HEAD).
  Test-map rows: the four WIZFORM-02 rows (derived-roster, non-vacuity, emitter SELF-TEST,
  nine arms return their `code`), the `EXPECTED_TABLE_SIZE` row (`:1437`, `:1649`), and
  both WIZFORM-03 rows. Falsifiability rows **SC-2** (remove `code:` from the 7th arm;
  reorder one arm so `EMITTER_RE` goes blind) and **SC-5** (add a substitution bullet to a
  `substitutable:false` entry). Also §**Oracle Independence** — this sub-phase lands the
  derivation-vs-hand-typed assertion that section is written about.
- `.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-UI-SPEC.md`
  — **no UI surface** in this sub-phase. Read only §Surface 3 **Gate C** ("venue-shaped
  remedies are gated on a venue CAPABILITY, not on a venue name") and §Copywriting
  Contract, because the copy table and the capability record this sub-phase mints are what
  Gate C is implemented against by 153.2/153.4.
- `153-EVIDENCE-mt5-platform.md` / `153-EVIDENCE-mt5-latency.md` — **not required here**
  (Phases 153.3 and 153.4 own the evidence-driven work).

</canonical_refs>

<specifics>
## Specific Ideas

**Binding traps (verbatim from `.planning/ROADMAP.md` §Phase 153.1):**

- ⛔ **D-34: reordering is not cosmetic.** Six PRE-EXISTING arms (`:573 :617 :767 :1293 :1310` written `{ error, code }`, plus `:1319` lowercase) are invisible to `EMITTER_RE`. They are out of scope only because it gates on `status: 400`; the moment the third `ROUTES` entry lands **and** the predicate widens, the coverage assertion goes blind on them. Size the vacuity floor against the **reordered total**, never against the nine.
- ⚠️ The A-25 **derived** assertion lands here as a Wave-0 gate (green at HEAD) so that 153.4's budget raise cannot pass a pin that cannot fail.

**File:line anchors this sub-phase must not lose** (all copied from the parent CONTEXT /
ROADMAP; D-10 requires re-deriving each from source before editing):

- The nine code-less `validatePayload` 400 arms: `finalize-wizard/route.ts` `:347`, `:374`,
  `:383`, `:396`, `:405`, `:428`, `:439`, `:474`, `:503`.
- The six reorder sites: `finalize-wizard/route.ts` `:573`, `:617`, `:767`, `:1293`,
  `:1310`, `:1319`.
- The bare `10` that becomes `MIN_DESCRIPTION_CHARS`: `finalize-wizard/route.ts:389`.
- `wizardErrors.invariant.test.ts:100-101` — `code:`-first + `[A-Z][A-Z0-9_]*`.
- `src/lib/wizardErrors.ts` — the `WizardErrorCode` union and copy table
  (`EXPECTED_TABLE_SIZE` guards its size; Phase 151 moved it 63→64).
- `ConnectKeyStep.tsx:265-315` — `KNOWN_CREATE_WITH_KEY_CODES` (stopgap already applied);
  `MultiKeyConnectStep.tsx:214+` — `KNOWN_ADD_KEY_CODES`.
- `src/lib/closed-sets.ts:105-125` — `MT5_UI_ENABLED` and the no-widening reasoning (the
  file `VENUE_CAPABILITIES` joins).
- `seam-constants.pin.test.ts:713-718` — the literal-vs-literal A-25 pin the derived
  assertion is added *beside* (D-19; the existing one is kept, not replaced).
- `classifyKeyValidationError` (shared) — already the single classifier the rosters claim
  to enumerate; the natural source of truth for a DERIVED roster (D-09a).
- `DESIGN.md` — read before any visual decision.

</specifics>

<deferred>
## Deferred Ideas

- Inline field validation, the `disabled` removal, the submit-guard widening, the
  `AllocateDialog` amount field — **Phase 153.2** (D-11, D-12, D-13; UI-SPEC Surface 2).
- The `finalize-wizard` probe gate and the `KEY_NETWORK_TIMEOUT` catch-all — **Phase 153.2**
  (D-14). Minting the capability is here; *reading* it at the gate is there.
- MT5 chip-set widening, the `closed-sets.mt5-flag` re-cut, the MT5 preselect —
  **Phase 153.2** (D-15, D-16, D-20).
- Any Python edit whatsoever (`exchange.py`, `mt5_client.py`, `mt5_validation.py`,
  `mt5_concurrency.py`) — **Phase 153.3**.
- Raising the `validate-key` budget, `budgetKeyFor`, the 16 pin sites,
  `BREAKER_LOCK_TOMBSTONE_S`, the long-wait card and `Stop waiting` — **Phase 153.4**
  (D-01, D-04, D-05, D-19, D-26).
- Whether **sFOX** should also opt out of the scope probe — explicitly an OPEN QUESTION,
  not a finding (RESEARCH §Open Questions Q2). D-22 pins sFOX byte-unchanged in this phase;
  route the question to `TODOS.md`.
- Wizard draft-aware entry chooser, stale-screen root cause, token-less credential dedup —
  **Phase 154** (WIZCONT/STALE).
- Live MT5 numeric verification against an external oracle on a trading day — **Phase 155**
  (MT5-VERIFY), human- and calendar-gated.
- ⚠️ `size_at_decision_usd` recorded on a NOTIONAL basis while the composer sizes on an
  EQUITY basis — an open founder decision carried from the v0.54.0.0 land, logged in
  `TODOS.md`, **not this phase**.

</deferred>

---

*Phase: 153.1-wizform-codes-honest-codes-venue-capability-foundation*
*Context inherited: 2026-08-08 from the Phase 153 split*
