---
phase: 168-drboptions-a-deribit-options-account-can-be-ingested-end-to
plan: 03
type: execute
wave: 3
depends_on: ["168-01", "168-02"]
files_modified: []
autonomous: false
requirements: [DERIBIT-ASSIGNMENT-UNCLASSIFIED]
user_setup: []

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low
  # estimate-calibration: 0 samples, factor 1, confidence low (derived, not self-rated).

must_haves:
  truths:
    - "D-06: after plans 01 and 02 are merged and the analytics service is deployed at the merge commit, the founder (not an agent) retries the Deribit options strategy whose stitch job failed on 2026-09-23 on the assignment refusal; no agent reads Deribit, reads or echoes a Deribit credential, or writes to PROD for this observation, and no live census read is performed."
    - "D-08: the founder's report records the job's terminal status, the return-point count, and the CLASS of any refusal or error that appears, named by type only (for example an unknown-type refusal for exercise or expiry, the D-02 co-occurrence refusal, the windowed-crawl refusal, or a missing position/commission guard) — never a job id, correlation id, account, strategy name, instrument name or change value."
    - "D-08: an assignment refusal no longer appears for that strategy; if a different refusal class appears (exercise, expiry, or a missing-field guard), it is recorded here and routed to its own phase via /gsd-phase --insert, not fixed in this phase (phase-size rule)."
    - statement: "A Deribit options account is observed to ingest end to end in production (the TODOS [DERIBIT-ASSIGNMENT-UNCLASSIFIED] close condition)."
      verification: backstop
  artifacts: []
  key_links: []
  prohibitions:
    - statement: "No agent performs the retry, reads the production analytics log for it, or queries PROD; the observation is founder-owned end to end."
      status: unverified
      flagged: true
      verification: "The SUMMARY attributes the report to the founder's resume message and quotes only counts, statuses and refusal classes."
---

<objective>
Close the phase on a real observation: after deploy, the founder retries the Deribit options
strategy that failed on 2026-09-23 and reports what happened, by counts and classes only (D-06,
D-08).

Purpose: the phase goal is that a Deribit options account ingests end to end. Unit and crawl-level
tests in plans 01 and 02 prove the classifier; only a production retry proves the account. Agents
may not touch Deribit or PROD, so this is a founder checkpoint, and it happens AFTER merge and
deploy, not during execution.

Scheduling (post-land step, expected pause): this plan runs AFTER `/gsd-ship` and
`/land-and-deploy` of plans 01 and 02, not inside the execute run. `/gsd-execute-phase 168` is
EXPECTED to stop at wave 3 on this checkpoint with plans 01 and 02 complete; that pause is the
designed hand-off, not a failure. The orchestrator then runs verification, ship and land, and the
checkpoint is resolved when the founder's report arrives after the deploy. The deploy check stays
founder step 1 (not a `<precondition>`), because no agent may read Railway, PROD or Deribit here.

No migration is needed; this plan changes no file.

Output: the founder's counts-only report, recorded in this plan's SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/168-drboptions-a-deribit-options-account-can-be-ingested-end-to/168-CONTEXT.md
Read once they have landed: 168-01-SUMMARY.md and 168-02-SUMMARY.md in this phase directory.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: founder retries the failed Deribit options strategy after deploy and reports counts and classes only</name>
  <files>none (observation only)</files>
  <read_first>
    - 168-CONTEXT.md D-06 and D-08
    - 168-01-SUMMARY.md and 168-02-SUMMARY.md (what shipped; the refusal classes the code can now raise)
  </read_first>
  <action>
    This is a post-land step: when execute-phase reaches it, stop here and report the expected pause; do not wait inside the execute run for the deploy. Resume only after the phase is shipped, landed and deployed. Pause and hand to the founder (per D-06 and D-08). The agent does not perform any step of the retry and does not read PROD, the analytics log or Deribit.
    What was built: `assignment` is classified cash-bearing in the measured census shape on both twins; the co-occurring shape, a missing instrument name and a windowed crawl each refuse loudly; the option book reads one shared event vocabulary.
    Founder steps: (1) confirm the deployed analytics commit is the merge commit; (2) in the app, retry the Deribit options strategy whose stitch job failed on 2026-09-23 (the wizard's Retry, or the owner's resync); (3) wait for the job to reach a terminal status; (4) report back, counts and classes only: the terminal status (for example complete, complete_with_warnings, failed_final), the return-point count on the strategy, whether an assignment refusal appeared (yes/no), and the class of any other refusal or error (unknown type named by its type such as exercise or expiry, co-occurrence refusal, windowed-crawl refusal, missing position or commission, NAV reconciliation breach, other). No ids, names, instrument strings or change values.
    On resume: record the report verbatim in the SUMMARY. If the status is terminal-success with a nonzero return-point count and no refusal, the close condition is met. If a different refusal class appeared, record it as the next topic for `/gsd-phase --insert` (exercise/expiry classification needs its own census) and do not change code in this phase. A NAV reconciliation breach belongs to its existing owner, not to this phase.
  </action>
  <what-built>Deribit `assignment` classified cash-bearing in the 2026-09-23 census shape on both twins (D-01); the co-occurring shape, a missing instrument name and a windowed crawl refuse loudly (D-02); the six option-book sites read `_OPTION_BOOK_EVENT_TYPES` (D-07); the D-03 evidence file; all pinned by tests seen RED then GREEN.</what-built>
  <how-to-verify>
    1. Confirm the Railway analytics service's deployed commitHash is the merge commit and /health is green.
    2. In the app, retry the Deribit options strategy whose stitch job failed on 2026-09-23.
    3. Wait for the job's terminal status.
    4. Reply with: terminal status; return-point count; assignment refusal yes/no; class of any other refusal or error, by type only. No ids, names, instrument strings or change values.
  </how-to-verify>
  <verify>
    <human-check>The founder's resume message states the terminal status, the return-point count, whether an assignment refusal appeared, and the class of any other refusal, with no identifiers.</human-check>
  </verify>
  <acceptance_criteria>
    - The SUMMARY quotes the founder's report with terminal status, return-point count, assignment-refusal yes/no, and any other refusal class.
    - The SUMMARY contains no job id, correlation id, account, strategy name, instrument name or change value.
    - If a new refusal class appeared, the SUMMARY names it as a proposed new phase and records that no code was changed for it here.
  </acceptance_criteria>
  <resume-signal>Founder replies with the four report fields, or "blocked: reason".</resume-signal>
  <done>The phase's close condition is observed in production by the founder, or the next blocking refusal class is named and routed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder report -> tracked .planning/ SUMMARY (public repo) | anything quoted into the SUMMARY becomes world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-11 | Information disclosure | identifiers from the production retry reaching the public SUMMARY | medium | mitigate | report is counts and classes only (D-08); acceptance criterion forbids ids, names, instrument strings and change values |
| T-168-12 | Elevation of privilege | an agent touching PROD or Deribit to "help" the observation | high | mitigate | the retry is founder-owned (D-06); the task's action forbids any agent PROD, log or broker read |
| T-168-SC | Tampering | package installs | low | accept | none in this plan |
</threat_model>

<verification>
- The founder's report is recorded in the SUMMARY with no identifiers.
</verification>

<success_criteria>
- The founder has observed the retried Deribit options strategy reach a terminal status with the assignment refusal gone, or the next refusal class is named and routed to its own phase.
</success_criteria>

## Artifacts this phase produces

| Symbol | Kind | File |
|---|---|---|
| founder's counts-only retry report | phase record | .planning/phases/168-drboptions-a-deribit-options-account-can-be-ingested-end-to/168-03-SUMMARY.md |

<output>
Create `.planning/phases/168-drboptions-a-deribit-options-account-can-be-ingested-end-to/168-03-SUMMARY.md` when done
</output>
