---
phase: 158-ops-ci-a-merge-means-a-deploy
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx
autonomous: true
requirements: [OPS-11]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "OPS-11 closes reproduction-FIRST: a documented shuffle-seed × ordering × Node-22 sweep at HEAD precedes any change; closure is EITHER a pinned RED→GREEN regression fix at the leak source OR a mechanism citation (Phase 140.5 config fence + env snapshot + leak canary) backed by the sweep evidence — never a green rerun alone (edge: OPS-11 reproduction-first)"
    - "If a leak source is found, the fix applies the DEF-16-1 class remedy (vi.spyOn + restoreAllMocks; vi.unstubAllGlobals / module-mock restoration) at the LEAKING file, not a retry, not a test reordering, not an increased timeout"
    - "158-OPS11-EVIDENCE.md records every seed, ordering mode, and environment attempted, with per-run outcomes — the closure claim is reproducible from the artifact"
  artifacts:
    - path: ".planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md"
      provides: "Reproduction-attempt matrix + closure rationale (fix or mechanism)"
      contains: "seed"
  key_links:
    - from: "158-OPS11-EVIDENCE.md"
      to: "vitest.config.ts:68-69 + src/test-setup.leak-canary.test.ts"
      via: "mechanism-closure branch cites the config-level unstubGlobals/unstubEnvs fence and the falsifiable canary (SC-HARNESS-1 / SC-ENV-1) as the standing regression guard"
      pattern: "unstubGlobals"
  prohibitions:
    - statement: "The flake must not be retried away — no retry annotation, no test.retry, no CI rerun-as-fix, no reordering of specs to dodge the failing order"
      status: unverified
      flagged: true
      verification: "Diff review: no retry/reorder-shaped change lands; the evidence artifact shows either a leak-source fix or a mechanism closure with sweep data"
---

<objective>
Root-cause (or honestly close) the `MultiKeyConnectStep` order-sensitive vitest flake recorded
2026-07-30 (44/44 green in isolation, order/shard-sensitive in the full suite). The harness
gained the Phase 140.5 config-level fence (`unstubGlobals: true`, `unstubEnvs: true`, env
snapshot restore, leak canary) AFTER the flake was recorded, and the spec file itself is
hygienic at HEAD — so the defect is a dated claim that must be re-measured before any fix.

Purpose: `MultiKeyConnectStep` passes under any test ordering, closed on mechanism, never on
retries (ROADMAP 158 SC-5).
Output: reproduction-evidence artifact + (only if reproduced) a leak-source fix with a pinned
RED→GREEN regression demonstration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-PATTERNS.md
@vitest.config.ts
@src/test-setup.leak-canary.test.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

- `.github/workflows/mutex-probe.yml` (plan 01); `.github/workflows/ci.yml` changes (plans 01, 06)
- `.github/workflows/main-ci-cancelled-watcher.yml` + label `main-ci-cancelled` (plan 02)
- `docs/runbooks/shared-test-db-mutex.md` + README index entry (plan 02)
- `scripts/drain-test-compute-backlog.ts` (plan 03)
- `e2e/my-strategies.spec.ts` (plan 05)
- `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md` (plan 03), `…/158-OPS11-EVIDENCE.md` (this plan), `…/158-DB-TYPES-DECISION.md` (plan 06)
- Advisory lock key constant: `61616158` (plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Attempt reproduction at HEAD under shuffled orders and the CI-Node22 environment</name>
  <precondition>node_modules is installed in the working tree (GSD worktrees fork WITHOUT node_modules — run npm ci first if node_modules/.bin/vitest is absent) and /opt/homebrew/opt/node@22/bin exists for the Node-22 PATH variant</precondition>
  <files>.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md</files>
  <read_first>
    - vitest.config.ts (lines ~50-125: the unstub pair at :68-69, its cannot-be-shadowed comment, project/shard layout)
    - src/test-setup.leak-canary.test.ts (docblock: DEF-16-1 statement, the 81/38/54 measurements, SC-HARNESS-1 / SC-ENV-1 ledger rows)
    - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx lines 1-100 (the file-level vi.mock at :37, global afterEach with restoreAllMocks + cleanup at :93-96)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md §OPS-11 (harness state, reproduction recipe, the two closure branches)
  </read_first>
  <action>
    Attempt to reproduce the order-sensitive failure at HEAD. Environment rules (all are
    established repo findings, not suggestions): a FULL local suite reds ~274 tests BY DESIGN
    when .env.test.local is present (HAS_LIVE_DB un-skips) — run the sweep in a tree WITHOUT
    the .env files (the phase worktree qualifies if no .env.test.local was copied in; verify
    with ls before starting); CI is Node 22 vs local Node 25 — run at least half the sweep
    with PATH=/opt/homebrew/opt/node@22/bin:$PATH (this reproduces CI-only reds); prefer
    --no-file-parallelism where the goal is a deterministic ordering (known local-flake
    reducer).

    Sweep matrix (record EVERY run: command, seed, node version, duration, outcome):
    1. Baseline: the two target files alone —
       npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts"
       — expect green (the recorded flake was full-suite-context only).
    2. Shuffle sweep: >=10 seeds of `npx vitest run --sequence.shuffle --sequence.seed=<n>`
       over the full suite, at least 5 of them under the Node-22 PATH. Distinct seeds
       (e.g. 1..10), outcomes per seed.
    3. The exact CI shard invocation for the shard containing the file (read the frontend-test
       shard command from .github/workflows/ci.yml and reproduce it locally under Node 22).
    A run is a REPRODUCTION only if a MultiKeyConnectStep case fails while the same case
    passes in isolation. Unrelated reds (live-DB env leakage, pre-existing flakes) are
    recorded but are NOT a reproduction — classify each red honestly.

    Start `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md` with the
    full matrix table, environment fingerprints (node -v for both PATHs, git SHA), and a
    verbatim-command log so any future session can re-run the sweep.
  </action>
  <verify>
    <automated>grep -qiE 'seed' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md && grep -q 'node' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md && [ "$(grep -cE 'sequence\.seed=' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md)" -ge 10 ]</automated>
  </verify>
  <acceptance_criteria>
    - The evidence file lists >=10 distinct shuffle-seed full-suite runs (>=5 under Node 22), the isolation baseline, and the CI-shard invocation, each with an explicit outcome
    - Every red run is classified: reproduction (target case failed in-context, passes isolated) vs unrelated (named cause)
    - The sweep ran in a tree without .env.test.local (recorded in the artifact)
  </acceptance_criteria>
  <done>The dated 2026-07-30 flake claim has been re-measured at HEAD with a documented, re-runnable sweep — the closure branch (fix vs mechanism) is now a matter of evidence, not opinion.</done>
</task>

<task type="auto">
  <name>Task 2: Close on the evidence — leak-source fix with pinned RED→GREEN, or mechanism closure</name>
  <files>.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx</files>
  <read_first>
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md (Task 1 matrix)
    - src/test-setup.ts (the env-snapshot restore mechanism — one of the two fences)
  </read_first>
  <action>
    BRANCH A — reproduced (any seed shows the target case failing in-context):
    1. Pin the failing seed. Bisect the co-scheduled file set: rerun the failing seed while
       excluding halves of the preceding files (vitest include lists) until the single leaking
       sibling file is identified. The known fence gaps are the leads: vi.mock module-registry
       state, direct process.env writes that predate the snapshot, jsdom document residue —
       the config fence only covers vi.stubGlobal/vi.stubEnv.
    2. Fix AT THE LEAKING FILE with the DEF-16-1 class remedy: vi.spyOn + restoreAllMocks
       (never a stubbed global left registered), vi.unstubAllGlobals where stubs are
       legitimate, module-mock restoration (vi.unmock / vi.resetModules) where the registry is
       the leak. If MultiKeyConnectStep.test.tsx itself needs a defensive change, keep it
       minimal and justified — the file is hygienic at HEAD.
    3. Regression demonstration (founder rule — a test that cannot fail is worse than none):
       the failing seed observed RED before the fix, GREEN after, both runs recorded verbatim
       in the evidence file. Then re-run 3 additional seeds green.
    4. Scan for the same leak pattern in sibling files of the identified leaker (close the
       class): record any additional fixes in the same pass.

    BRANCH B — not reproduced after the honest sweep:
    The closure IS the Phase 140.5 mechanism. Append to the evidence file: the flake was
    recorded 2026-07-30; the config-level fence (vitest.config.ts unstubGlobals/unstubEnvs,
    :68-69), the src/test-setup.ts env-snapshot restore, and the falsifiable leak canary
    (src/test-setup.leak-canary.test.ts, ledger rows SC-HARNESS-1 / SC-ENV-1) landed at/after
    that date and are the identified, in-tree, falsifiable mechanism; the sweep (N seeds,
    orders, Node 22) could not reproduce at HEAD. State explicitly: closed on MECHANISM with
    reproduction-attempt evidence, not on a green rerun. No production or test code changes in
    this branch.

    Either branch: finish the evidence file with a one-paragraph closure statement naming the
    branch taken.
  </action>
  <verify>
    <automated>grep -qiE '(RED.*GREEN|mechanism)' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS11-EVIDENCE.md && npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - Branch A: the leaking file is named; the fix uses the DEF-16-1 pattern; the failing seed's RED-before / GREEN-after outputs are excerpted verbatim in the evidence file; 3 further seeds green
    - Branch B: the evidence file cites vitest.config.ts:68-69, the env-snapshot restore, and the leak canary by ledger row, plus the sweep summary; zero code changes shipped
    - The target files pass in isolation at close (verify command green)
    - The closure paragraph explicitly states which branch closed OPS-11 and why a green rerun alone would not have sufficed
  </acceptance_criteria>
  <done>OPS-11 is closed the only two honest ways: a pinned, falsifiable fix at the leak source, or a mechanism citation carried by reproduction-attempt evidence — and the artifact makes the closure reproducible.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → shared worker state | leaked globals/env/mocks cross test-file boundaries within a vitest worker |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-14 | Tampering | test-state leakage between files (the defect itself) | low | mitigate | DEF-16-1 remedy at the source if reproduced; 140.5 fence + canary as the standing guard otherwise |
| T-158-15 | Repudiation | closure without falsifiable evidence ("it passed when we reran it") | medium | mitigate | reproduction-first protocol + RED→GREEN pin or mechanism citation, both encoded as acceptance criteria |
</threat_model>

<verification>
- Task gates above (evidence-matrix greps with the >=10-seed count, target-file isolation runs).
- Wave gate: full vitest in a clean worktree (no .env files) green before merge, per the
  phase-wide sampling rules.
</verification>

<success_criteria>
- ROADMAP 158 SC-5: MultiKeyConnectStep passes under any tested ordering; the root cause is
  either fixed at the leak source with a pinned regression, or the 140.5 mechanism closure is
  recorded with the sweep evidence. No retry-shaped change anywhere.
</success_criteria>

<output>
Create `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-04-SUMMARY.md` when done,
recording: branch taken, the evidence-artifact path, and (Branch A) the leaking file + fix
commits.
</output>
