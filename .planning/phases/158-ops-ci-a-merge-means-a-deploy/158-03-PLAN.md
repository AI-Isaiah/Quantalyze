---
phase: 158-ops-ci-a-merge-means-a-deploy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_compute_jobs_fencing.py
  - scripts/drain-test-compute-backlog.ts
  - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md
autonomous: true
requirements: [OPS-04]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both direct UPDATEs in test_defer_compute_job_token_fence and test_defer_compute_job_null_token_backcompat stamp claimed_at with a CURRENT timestamp, so a row stranded by a dying test is reapable by reset_stalled_compute_jobs (whose predicate requires claimed_at IS NOT NULL)"
    - "The drain hard-rejects any target URL that lacks the TEST ref qmnijlgmdhviwzwfyzlc or contains the PROD ref khslejtfbuezsmvmtsdn, exiting non-zero BEFORE any network call (edge: OPS-04 drain boundary — PROD hard-reject)"
    - "The drain terminalizes stale rows to a terminal failed state and performs zero row deletions (WR-02 doctrine symmetry: terminalize, never DELETE)"
    - "OPS-04 closes on MEASURED row counts: before/after counts by (kind, status) on TEST recorded in 158-OPS04-DRAIN-EVIDENCE.md — never on 'the 10 reds are gone' (PR #674's p_kind_include scoping already structurally fixed the deterministic red on 2026-08-12)"
    - "The drain is idempotent: an immediate re-run reports zero additional rows terminalized"
    - "pytest tests/test_compute_jobs_fencing.py passes from analytics-service/ after the stamps"
  artifacts:
    - path: "analytics-service/tests/test_compute_jobs_fencing.py"
      provides: "claimed_at stamps in the two direct running-flip UPDATEs"
      contains: "claimed_at"
    - path: "scripts/drain-test-compute-backlog.ts"
      provides: "Guarded, re-runnable TEST-only drain tool (interlock + terminalize + measure)"
      contains: "qmnijlgmdhviwzwfyzlc"
    - path: ".planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md"
      provides: "Before/after row-count evidence + recorded semantics decisions"
      contains: "before"
  key_links:
    - from: "scripts/drain-test-compute-backlog.ts"
      to: "TEST Supabase compute_jobs"
      via: "supabase-js service-role client, env NEXT_PUBLIC_SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY (same names as scripts/seed-demo-data.ts)"
      pattern: "SUPABASE_SERVICE_ROLE_KEY"
    - from: "analytics-service/tests/test_compute_jobs_fencing.py"
      to: "reset_stalled_compute_jobs reaper predicate"
      via: "claimed_at stamped current-time so stranded rows age into the claimed_at < now() - threshold window"
      pattern: "claimed_at"
  prohibitions:
    - statement: "The drain is never a migration — no file under supabase/migrations/** may be created or modified (merging migrations auto-applies them to PROD)"
      status: unverified
      flagged: true
      verification: "git diff --stat for this plan shows zero paths under supabase/migrations/"
    - statement: "Never cron.unschedule(9) — the TEST pg_cron fan-out job is read-only context for this phase; the drain must not touch cron schedules in any way"
      status: unverified
      flagged: true
      verification: "The drain script contains no cron.unschedule / cron.alter / cron.schedule call and no SQL string mentioning the cron schema; grep of scripts/drain-test-compute-backlog.ts"
---

<objective>
Close OPS-04 both halves: (1) the two direct UPDATEs in
`analytics-service/tests/test_compute_jobs_fencing.py` that flip seeded rows to `running`
without `claimed_at` — leaving permanently-unreapable rows on the shared TEST project if a
test dies mid-flight — get current-time stamps; (2) the accumulated TEST `compute_jobs`
backlog (thousands of permanently-`running` rows + the daily `derive_broker_dailies` pending
refill) is drained TEST-only by a guarded, re-runnable script, closed on measured row counts.

Purpose: the shared TEST project stops accumulating unreapable state, and the fencing tests
can no longer strand invisible rows (ROADMAP 158 SC-4).
Output: stamped test file, `scripts/drain-test-compute-backlog.ts` (new), drain evidence
artifact with before/after measurements.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-PATTERNS.md
@scripts/seed-demo-data.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

- `.github/workflows/mutex-probe.yml` — NEW workflow; jobs `contend` / `assert-serialization` (plan 01)
- `.github/workflows/ci.yml` — mutex adoption + `sql-tests` aggregator gating + batch lists (plans 01, 06)
- `.github/workflows/main-ci-cancelled-watcher.yml` — NEW workflow; job `report-cancelled`; label `main-ci-cancelled` (plan 02)
- `docs/runbooks/shared-test-db-mutex.md` + README index entry (plan 02)
- `scripts/drain-test-compute-backlog.ts` — NEW guarded TEST-only drain tool (this plan)
- `e2e/my-strategies.spec.ts` — NEW seeded spec (plan 05)
- `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md` (this plan), `…/158-OPS11-EVIDENCE.md` (plan 04), `…/158-DB-TYPES-DECISION.md` (plan 06)
- Advisory lock key constant: `61616158` (plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Stamp claimed_at in the two direct running-flip UPDATEs</name>
  <precondition>The analytics-service test environment resolves to the TEST project: the configured Supabase URL used by tests contains qmnijlgmdhviwzwfyzlc (echo the env the test config reads; never run these live-DB tests against a PROD-flavored URL)</precondition>
  <files>analytics-service/tests/test_compute_jobs_fencing.py</files>
  <read_first>
    - analytics-service/tests/test_compute_jobs_fencing.py lines 1100-1290 (the two target functions test_defer_compute_job_token_fence ~:1127 and test_defer_compute_job_null_token_backcompat ~:1187, plus the in-file backdate-stamp precedent at ~:1108-1110 and ~:1277-1279)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md §OPS-04 (the reaper predicate: claimed_at IS NOT NULL AND claimed_at < now() - threshold, migration 20260516104201:667-668)
  </read_first>
  <action>
    In `analytics-service/tests/test_compute_jobs_fencing.py`, add a `"claimed_at"` key
    carrying a CURRENT UTC ISO-8601 timestamp (e.g. via datetime.now(timezone.utc).isoformat(),
    matching the file's existing import style) to the two direct UPDATE payloads that flip a
    seeded row to running:
    1. in `test_defer_compute_job_token_fence` (the payload currently setting status /
       claim_token=real_token / attempts=1, ~:1148-1152)
    2. in `test_defer_compute_job_null_token_backcompat` (the payload currently setting
       status / claim_token=uuid4 / attempts=1, ~:1200-1204)
    CURRENT time, not backdated: these rows must age INTO the reaper window only if the test
    dies; a backdated stamp would make the reaper race a healthy test run.

    Audit (read-only unless a same-class omission is found) the other direct UPDATEs the
    research enumerates (~:1108, :1277, :1361, :1726, :1807 backdate claimed_at deliberately;
    ~:2914/:2924 rotate token/status on rows claimed via _claim_one): confirm each either
    stamps or operates on an already-claimed row. If a third status-to-running flip WITHOUT a
    claimed_at stamp exists, fix it in the same pass (close the class, not the instance) and
    record it in the SUMMARY.

    Run pytest ONLY from analytics-service/ with python3 (repo-root runs miss VCR cassettes
    and go live).
  </action>
  <verify>
    <automated>cd analytics-service && awk '/def test_defer_compute_job_token_fence/,/def test_defer_compute_job_null_token_backcompat/' tests/test_compute_jobs_fencing.py | grep -q '"claimed_at"' && awk '/def test_defer_compute_job_null_token_backcompat/,/^    def |^def |^class /' tests/test_compute_jobs_fencing.py | grep -q '"claimed_at"' && python3 -m pytest tests/test_compute_jobs_fencing.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - Region-scoped greps confirm a "claimed_at" key inside BOTH target functions' UPDATE payloads, valued from a current-time expression (not a string literal date)
    - `cd analytics-service && python3 -m pytest tests/test_compute_jobs_fencing.py -q` exits 0
    - The audit result for the other direct UPDATEs is recorded in the SUMMARY (each classified: deliberate backdate / already-claimed / fixed-as-class)
  </acceptance_criteria>
  <done>A fencing test that dies between its running-flip and its finally-cleanup now strands a row the reaper can see and reclaim, instead of a permanently-invisible (running, NULL claimed_at) row.</done>
</task>

<task type="auto">
  <name>Task 2: Build the guarded TEST-only drain script</name>
  <files>scripts/drain-test-compute-backlog.ts</files>
  <read_first>
    - scripts/seed-demo-data.ts lines 630-700 (the TEST-only interlock to copy: env presence check, prod-URL regex hard-reject, SEED_CONFIRM_STAGING opt-in — extend it)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md §OPS-04 (drain shapes table, refill source, WR-02 note, TEST/PROD refs)
  </read_first>
  <action>
    Create `scripts/drain-test-compute-backlog.ts` (run via `npx tsx`, supabase-js
    service-role client, env names `NEXT_PUBLIC_SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` —
    identical to seed-demo-data.ts so CI/local wiring is uniform). Header doc = the operating
    runbook: purpose, guards, modes, idempotency, and the WR-02 semantics decision.

    INTERLOCK (all four, checked before ANY network call, each with its own error message):
    (1) both env vars present; (2) hard-reject if the URL matches /\b(prod|production)\b/i;
    (3) hard-reject if the URL contains the PROD ref khslejtfbuezsmvmtsdn; (4) REQUIRE the URL
    to contain the TEST ref qmnijlgmdhviwzwfyzlc; plus (5) explicit opt-in env
    DRAIN_CONFIRM_TEST=true or refuse.

    MODE 1 (default, `--dry-run` flag supported): measure, then terminalize.
    - Measure: counts of compute_jobs grouped by (kind, status) — print as a table (this is
      the before/after evidence source).
    - Target set: rows with kind = 'derive_broker_dailies' AND status IN ('pending','running')
      AND the row's age exceeds 24 hours (use claimed_at for running rows when present, else
      created_at/next_attempt_at — inspect actual column availability first). The age guard
      keeps concurrent CI-seeded fresh rows untouched.
    - PLANNER DECISION (recorded): TERMINALIZE, never delete — set status to 'failed' and
      write a drain provenance note into the error-text column the worker already uses
      (inspect one existing failed row or the migration to learn the exact column name before
      writing; do not invent a column). WR-02's terminalize-doctrine is PROD-born; applying it
      on TEST preserves audit shape at trivial cost and resolves the drain-boundary edge probe.
    - Page the work: select matching ids in pages (1000), then update by `.in('id', chunk)` —
      never one giant unbounded mutation.
    - `--dry-run` prints the measurement and the would-be-terminalized count, mutating nothing.

    MODE 2 (`--flip-eligibility`, additionally gated by DRAIN_CONFIRM_ELIGIBILITY=true):
    durable refill reduction — the daily pg_cron fan-out (jobid 9) enqueues one row per
    ELIGIBLE api_key (is_active AND sync_status IS DISTINCT FROM 'revoked' AND disconnected_at
    IS NULL). This mode: (a) measures the eligible-key population and prints owner/created_at
    aggregates; (b) computes an ALLOWLIST of keys that live seeded specs depend on (at
    minimum: keys belonging to the seed-demo-data.ts demo strategies and the sfox-badge /
    mt5-badge seed fixtures — derive by reading those fixtures' seed identifiers, not by
    guessing); (c) sets disconnected_at = now() ONLY for keys older than 7 days AND outside
    the allowlist; (d) prints proposed count BEFORE mutating and requires the confirm env.
    Never touch cron itself.
  </action>
  <verify>
    <automated>NEXT_PUBLIC_SUPABASE_URL="https://khslejtfbuezsmvmtsdn.supabase.co" SUPABASE_SERVICE_ROLE_KEY="x" DRAIN_CONFIRM_TEST=true npx tsx scripts/drain-test-compute-backlog.ts --dry-run; test $? -ne 0 && NEXT_PUBLIC_SUPABASE_URL="https://qmnijlgmdhviwzwfyzlc.supabase.co" SUPABASE_SERVICE_ROLE_KEY="x" npx tsx scripts/drain-test-compute-backlog.ts --dry-run; test $? -ne 0 && ! grep -q 'delete(' scripts/drain-test-compute-backlog.ts && ! grep -qi 'cron\.' scripts/drain-test-compute-backlog.ts</automated>
  </verify>
  <acceptance_criteria>
    - Invocation with a PROD-ref URL exits non-zero with a refusal message BEFORE any network call (observed)
    - Invocation without DRAIN_CONFIRM_TEST exits non-zero with a refusal message (observed)
    - Source contains zero row-deletion calls (negative grep passes) and zero cron-schema references
    - The TEST ref requirement, PROD ref rejection, prod-word regex, and both confirm envs are all present in source
    - `--dry-run` performs only SELECTs (read-back of the code path)
  </acceptance_criteria>
  <done>A re-runnable, four-guard TEST-only drain tool exists whose unsafe invocations were OBSERVED to refuse, and whose semantics (terminalize, never delete; cron untouched) are encoded in code, not discipline.</done>
</task>

<task type="auto">
  <name>Task 3: Execute the drain against TEST and record measured evidence</name>
  <precondition>TEST service-role credentials are available in the local environment under the names NEXT_PUBLIC_SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY (e.g. sourced from .env.test.local) and the URL host contains qmnijlgmdhviwzwfyzlc</precondition>
  <reversibility rating="costly">Terminalized TEST rows cannot be flipped back to their prior pending/running states in bulk; the rows are worthless stale fan-out artifacts and the daily cron refills pending, but the mutation itself is not undoable — hence dry-run first, age guard, and TEST-only interlock.</reversibility>
  <files>.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md</files>
  <read_first>
    - scripts/drain-test-compute-backlog.ts (as built in Task 2)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md §OPS-04 (the PR #674 correction — acceptance is row counts, not test colors; measured 2026-08-11 baseline: 2320 pending / 2325 running)
  </read_first>
  <action>
    Run the drain against TEST in this order, capturing full output:
    1. `--dry-run` — record the BEFORE table (counts by kind, status).
    2. Real MODE 1 run — record rows terminalized.
    3. Immediate second MODE 1 run — must report 0 additional (idempotency).
    4. MODE 2 measurement (without the confirm env — it prints the eligible population and
       proposed flip set). If the allowlist derivation is unambiguous, run the flip with
       DRAIN_CONFIRM_ELIGIBILITY=true and record before/after eligible-key counts. If fixture
       ambiguity prevents a safe allowlist (missing information, not difficulty), DO NOT
       guess: record the measurement, skip the flip, and add a TODOS.md entry
       `[158-OPS-04] eligibility flip deferred: <reason + measured counts>` so the deferral
       is explicit, never silent.
    5. AFTER table — counts by (kind, status).

    Write `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md`:
    BEFORE/AFTER tables, terminalized counts, idempotency proof, the MODE 2 outcome, the
    recorded semantics decision (terminalize / never delete / cron untouched), and the ledger
    correction paragraph: the exactly-10 deterministic red was structurally fixed by PR #674
    (p_kind_include scoping, 2026-08-12) — this drain closes the HYGIENE debt on measured
    counts, and any claim that it "fixed the 10 reds" would be false.

    ⚠️ Repo is PUBLIC and .planning/ is tracked: the evidence file carries counts and refs
    only — no keys, no user emails, no DSNs.
  </action>
  <verify>
    <automated>grep -qi 'before' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md && grep -qi 'after' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md && grep -q '674' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md && grep -qiE 'idempoten' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md</automated>
  </verify>
  <acceptance_criteria>
    - The evidence file contains BEFORE and AFTER (kind, status) count tables from real runs, a terminalized-row count, and the second-run zero-delta idempotency line
    - The stale derive_broker_dailies backlog (rows older than 24h in pending/running) measures 0 in the AFTER table
    - The MODE 2 outcome is one of: flip executed with before/after eligible counts, OR an explicit recorded deferral with a TODOS.md entry — never silence
    - The PR #674 correction paragraph is present (closure on measurement, not on test colors)
    - No secret value, email, or DSN appears in the evidence file
  </acceptance_criteria>
  <done>The TEST backlog is drained and MEASURED (before/after on disk), the drain is proven idempotent, and the refill-reduction outcome is explicitly recorded either way.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator env → drain script | a misrouted DSN/URL would aim the mutation at PROD |
| drain script → TEST compute_jobs/api_keys | bulk mutation of shared-environment state |
| .planning evidence → public repo | measurements are world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-09 | Tampering | drain aimed at PROD via misrouted env | critical | mitigate | four-guard interlock (TEST-ref REQUIRED + PROD-ref reject + prod-word regex + confirm env), each observed refusing in the task gate |
| T-158-10 | Repudiation | drain destroying audit trail | medium | mitigate | terminalize-only (status update with provenance note); zero deletion calls (source gate) |
| T-158-11 | Denial of Service | eligibility flip breaking live seeded specs (sfox/mt5 badges) | medium | mitigate | fixture-derived allowlist + 7-day age cutoff + measure-print-confirm flow + explicit deferral path |
| T-158-12 | Tampering | drain racing concurrent CI runs' fresh rows | medium | mitigate | 24h age guard scopes the target set to stale artifacts only |
| T-158-13 | Information Disclosure | evidence artifact leaking secrets in a public repo | high | mitigate | counts and refs only; explicit no-secrets rule in the task action + acceptance criterion |
</threat_model>

<verification>
- Task gates above (region-scoped stamp greps + green pytest from analytics-service/; observed
  interlock refusals; evidence-file content greps).
- Deferred-to-verifier: one deliberate mid-test abort demonstration (kill the fencing test
  between flip and cleanup, confirm the stranded row now carries claimed_at and ages into the
  reaper window) is OPTIONAL per RESEARCH ("or at minimum the payload includes claimed_at");
  the payload greps + pytest are the floor.
</verification>

<success_criteria>
- ROADMAP 158 SC-4 holds: TEST stale-pending backlog drained TEST-only (no migration, cron
  untouched), closed on measured before/after row counts; both direct UPDATEs stamp
  claimed_at; the fencing suite is green from analytics-service/.
</success_criteria>

<output>
Create `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-03-SUMMARY.md` when done,
recording: the audit classification of every direct UPDATE, the drain run outputs, and the
MODE 2 outcome (flip or explicit deferral).
</output>
