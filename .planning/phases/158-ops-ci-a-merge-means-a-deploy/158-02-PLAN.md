---
phase: 158-ops-ci-a-merge-means-a-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/main-ci-cancelled-watcher.yml
  - docs/runbooks/shared-test-db-mutex.md
  - docs/runbooks/README.md
autonomous: true
requirements: [OPS-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A cancelled main-branch CI push-run conclusion produces a loud, dedup'd GitHub issue labeled main-ci-cancelled (create on first detection, comment on subsequent) — the silence mechanism of #616 is gone"
    - "The watcher never reds a main-HEAD check: every code path exits 0; the loud signal is the issue, never the check status (analytics-deploy-verify.yml:17-24 doctrine — a red check makes Railway skip the very deploy the watcher protects)"
    - "A workflow_run event whose conclusion is not cancelled (or not main, or not a push) is a clean no-op — the gate condition excludes it before any issue API call (edge: OPS-01 empty — watcher on a non-cancelled run is a no-op)"
    - "The watcher is testable without forcing a cancellation on main: workflow_dispatch with a run_id input evaluates the same predicate against a fetched historical run (31273384829)"
    - "docs/runbooks/shared-test-db-mutex.md documents holder discovery, pg_terminate_backend manual unlock, TTL semantics, the fork no-op arm, and the probe drill — referencing secret NAMES only (repo is PUBLIC)"
    - statement: "Post-merge, dispatching the watcher with run_id 31273384829 creates the dedup'd issue on the first run and comments on the second, after which the issue is closed by the operator"
      verification: backstop
  artifacts:
    - path: ".github/workflows/main-ci-cancelled-watcher.yml"
      provides: "OPS-01 detection layer: cancelled main-run conclusion → dedup'd issue"
      contains: "workflow_run"
    - path: "docs/runbooks/shared-test-db-mutex.md"
      provides: "Manual-unlock runbook (adoption requirement of the mutex, not a follow-up)"
      contains: "pg_terminate_backend"
    - path: "docs/runbooks/README.md"
      provides: "Runbook index entry"
      contains: "shared-test-db-mutex"
  key_links:
    - from: ".github/workflows/main-ci-cancelled-watcher.yml"
      to: "ci.yml (workflow name 'CI')"
      via: "on: workflow_run with workflows: [\"CI\"], types: [completed], branches: [main]"
      pattern: "workflows:\\s*\\[\"CI\"\\]"
    - from: ".github/workflows/main-ci-cancelled-watcher.yml"
      to: "GitHub Issues"
      via: "actions/github-script dedup'd-issue block (label-keyed listForRepo → createComment | create), env-injected context"
      pattern: "main-ci-cancelled"
  prohibitions:
    - statement: "The watcher must never conclude failure on the main-HEAD check for a detection — a red check on main HEAD makes Railway's wait-for-CI skip the analytics deploy, recreating the exact #616 failure it watches for"
      status: unverified
      flagged: true
      verification: "Read every step of main-ci-cancelled-watcher.yml: no path sets a non-zero exit on detection; detection ends in issue create/comment followed by normal step completion"
    - statement: "This phase creates no supabase/migrations/** file"
      status: unverified
      flagged: true
      verification: "git diff --stat for this plan shows only the watcher workflow and docs/runbooks files"
---

<objective>
Build the detection half of OPS-01: a `workflow_run` watcher that turns a `cancelled`
main-branch CI conclusion (grey — nobody triages grey) into a loud dedup'd GitHub issue, plus
the manual-unlock runbook that is a hard adoption requirement of the mutex shipped in plan
158-01.

Purpose: a forced `cancelled` main-run conclusion raises a loud signal instead of a
silently-skipped Railway deploy (ROADMAP 158 SC-1, detection half).
Output: `.github/workflows/main-ci-cancelled-watcher.yml` (new),
`docs/runbooks/shared-test-db-mutex.md` (new), `docs/runbooks/README.md` index entry.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-PATTERNS.md
@.github/workflows/analytics-deploy-verify.yml
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

- `.github/workflows/mutex-probe.yml` — NEW workflow; jobs `contend` / `assert-serialization` (plan 01)
- `.github/workflows/ci.yml` — mutex adoption + `sql-tests` aggregator gating + batch lists (plans 01, 06)
- `.github/workflows/main-ci-cancelled-watcher.yml` — NEW workflow; job `report-cancelled`; issue label `main-ci-cancelled` (this plan)
- `docs/runbooks/shared-test-db-mutex.md` + `docs/runbooks/README.md` index entry (this plan)
- `scripts/drain-test-compute-backlog.ts` (plan 03)
- `e2e/my-strategies.spec.ts` (plan 05)
- `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md`, `…/158-OPS11-EVIDENCE.md`, `…/158-DB-TYPES-DECISION.md` (plans 03, 04, 06)
- Advisory lock key constant: `61616158`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the cancelled-conclusion watcher workflow</name>
  <reversibility rating="reversible">Purely additive workflow file; deleting it restores prior behavior with no data or contract impact.</reversibility>
  <files>.github/workflows/main-ci-cancelled-watcher.yml</files>
  <read_first>
    - .github/workflows/analytics-deploy-verify.yml (FULL file: header doctrine :17-24, permissions :45-51, dedup'd-issue github-script block :111-160, env-into-script injection hygiene)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-PATTERNS.md (watcher section — trigger shape, checkout convention, new label)
    - .github/workflows/ci.yml line 1 (workflow name is exactly `CI` — the workflow_run reference must match it byte-for-byte)
  </read_first>
  <action>
    Create `.github/workflows/main-ci-cancelled-watcher.yml`, copying the
    `analytics-deploy-verify.yml` skeleton and dedup'd-issue pattern (the third in-repo use of
    that block — nightly.yml and cassette-refresh.yml mirror it):

    - Header comment: state the exit-0 doctrine verbatim in spirit — this workflow attaches
      its check to the main-HEAD SHA via workflow_run; it MUST always exit 0; the loud signal
      is the dedup'd issue, never a red check (cite the 2026-06-21 deadlock precedent from
      analytics-deploy-verify.yml).
    - Triggers: `on: workflow_run: { workflows: ["CI"], types: [completed], branches: [main] }`
      plus `workflow_dispatch` with a required `run_id` input (description: exercise the issue
      path against a historical cancelled run, e.g. 31273384829, without forcing a
      cancellation on main).
    - `permissions: { contents: read, issues: write }`. PLANNER DECISION (Claude's discretion,
      recorded): the watcher is ISSUE-ONLY — no auto-rerun, because a rerun re-enters the same
      contention window and the requirement's "issue or rerun" wording permits issue-only;
      therefore no Actions-API write permission is uplifted.
    - `concurrency: { group: main-ci-cancelled-watcher, cancel-in-progress: false }`.
    - Single job `report-cancelled` on ubuntu-latest with the SHA-pinned checkout
      (`actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0`,
      `persist-credentials: false` — C-0293 applies to every workflow).
    - Detection gate: for workflow_run events, proceed only when
      `github.event.workflow_run.conclusion == 'cancelled' && github.event.workflow_run.head_branch == 'main' && github.event.workflow_run.event == 'push'`
      — expressed as a step-level `if:` (plus event_name == 'workflow_dispatch' as the OR-arm).
      For the dispatch arm, a github-script step fetches the run by `inputs.run_id` via
      `github.rest.actions.getWorkflowRun` and evaluates the SAME predicate on the fetched
      run's conclusion/head_branch/event, so the historical-run test exercises the real logic.
    - Issue step: `actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3  # v9.0.0`,
      dedup label `main-ci-cancelled`; pass run URL / run id / head SHA into the script via
      `env:` only — never interpolate `${{ }}` expressions inside the script body (injection
      hygiene; workflow_run payload fields are attacker-influenced strings). Behavior:
      `listForRepo({state:"open", labels:"main-ci-cancelled", per_page:1})` → if an open issue
      exists, `createComment` with the new occurrence (date, run URL); else `create` with
      title "Main CI concluded cancelled — deploy may have been skipped — <date>", body
      explaining the mechanism (a cancelled main run means Railway's wait-for-CI saw no green
      and SKIPPED the analytics deploy; link the run; point at
      docs/runbooks/shared-test-db-mutex.md for triage), labels `["p1","main-ci-cancelled"]`.
    - Every step ends exit 0 on detection; the only allowed failures are GitHub API errors
      surfaced as step failures on the WATCHER's own run (which is not a main-CI check gate).
    - One-time label setup (executor, via gh CLI, not in the workflow):
      `gh label create main-ci-cancelled --color B60205 --description "Main CI run concluded cancelled; analytics deploy may have been skipped" || true`
      (idempotent; the REST issues.create call does not auto-create labels for the token).
  </action>
  <verify>
    <automated>node -e "const y=require('js-yaml');const d=y.load(require('fs').readFileSync('.github/workflows/main-ci-cancelled-watcher.yml','utf8'));if(!d.on.workflow_run||!d.on.workflow_dispatch)process.exit(1);console.log('yaml ok')" && grep -q 'issues: write' .github/workflows/main-ci-cancelled-watcher.yml && ! grep -q 'actions: write' .github/workflows/main-ci-cancelled-watcher.yml && grep -q 'main-ci-cancelled' .github/workflows/main-ci-cancelled-watcher.yml && grep -q 'persist-credentials: false' .github/workflows/main-ci-cancelled-watcher.yml && gh label list --json name --jq '.[].name' | grep -qx 'main-ci-cancelled'</automated>
  </verify>
  <acceptance_criteria>
    - The workflow parses as YAML; `on:` carries workflow_run (workflows ["CI"], types [completed], branches [main]) AND workflow_dispatch with a run_id input
    - Permissions are exactly contents: read + issues: write; the negative grep for the Actions-API write scope in the verify command passes (issue-only decision holds)
    - The github-script issue step receives all run context via env: — zero `${{` occurrences inside the script body string
    - Both actions are SHA-pinned with version comments; checkout sets persist-credentials: false
    - The detection predicate requires conclusion cancelled AND head_branch main AND event push (workflow_run arm), and the dispatch arm fetches the run and evaluates the same predicate
    - The `main-ci-cancelled` label exists in the repo (gh label list)
    - No step can exit non-zero on a successful detection (read-back of every run/script step)
  </acceptance_criteria>
  <done>A cancelled main CI conclusion now produces a loud dedup'd issue with zero risk of red-checking main HEAD, and the path is exercisable on demand via dispatch against run 31273384829.</done>
</task>

<task type="auto">
  <name>Task 2: Write the shared-test-db mutex runbook and index it</name>
  <files>docs/runbooks/shared-test-db-mutex.md, docs/runbooks/README.md</files>
  <read_first>
    - docs/runbooks/seam-breaker.md (structure/tone precedent for ops runbooks)
    - docs/runbooks/README.md (index format)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md §OPS-01 "Fix shape" item 6 (runbook contents) and "Watcher half"
  </read_first>
  <action>
    Create `docs/runbooks/shared-test-db-mutex.md` with these sections (secret NAMES only —
    the repo is PUBLIC; never a DSN value, password, or username):

    1. **Mechanism** — the three DB-touching CI jobs (sql-tests, python, e2e-seeded) serialize
       cross-run via Postgres session advisory lock key `61616158` on the TEST project,
       acquired by a background psql session with application_name `ci-shared-test-db-mutex`
       over the session-mode DSN (port 5432 variant of the secret named TEST_SUPABASE_DB_URL).
       Why: GitHub's concurrency-group layer holds one pending slot and EVICTS (issue #616).
    2. **TTL / steal semantics** — `timeout-minutes: 60` on each job bounds the hold; a
       killed/cancelled job's TCP session drops and Postgres releases the lock automatically.
    3. **Manual unlock** — find the holder:
       `SELECT pid, application_name, state, query_start FROM pg_stat_activity WHERE application_name = 'ci-shared-test-db-mutex';`
       (cross-check via a pg_locks join on locktype='advisory' and the key), then
       `SELECT pg_terminate_backend(<pid>);` — the next FIFO waiter proceeds immediately.
       Note this is the same primitive as the wedged-PostgREST remedy already used on TEST.
    4. **Fork-PR arm** — the acquire step no-ops when the secret is withheld; fork DB work
       self-skips independently, so an unserialized fork run cannot touch the TEST project.
    5. **Probe drill** — `gh workflow run mutex-probe.yml` ×3 back-to-back post-merge; all
       three must conclude `success` with non-overlapping lock windows (assert the string
       `success`, never "not failure" — grey merges). ⚠️ Never run a deliberately-failing
       probe variant via dispatch on main: a red check on a main-HEAD SHA makes Railway skip
       the analytics deploy. Neuter drills belong on `ci-probe/**` branches only.
    6. **Watcher triage** — an issue labeled `main-ci-cancelled` means a main CI run concluded
       cancelled: check whether Railway deployed the main-HEAD commit (compare
       `/health` commitHash), rerun the cancelled run via `gh run rerun <id>` manually if
       needed, then close the issue.

    Add a one-line entry for the new runbook to `docs/runbooks/README.md` following the
    existing index format (scoped Edit — do not restructure the index).
  </action>
  <verify>
    <automated>grep -q 'pg_terminate_backend' docs/runbooks/shared-test-db-mutex.md && grep -q '61616158' docs/runbooks/shared-test-db-mutex.md && grep -q 'ci-shared-test-db-mutex' docs/runbooks/shared-test-db-mutex.md && grep -q 'shared-test-db-mutex' docs/runbooks/README.md && ! grep -qE 'postgresql://[^ ]*:[^ ]*@' docs/runbooks/shared-test-db-mutex.md</automated>
  </verify>
  <acceptance_criteria>
    - The runbook contains all six sections (mechanism, TTL/steal, manual unlock, fork arm, probe drill, watcher triage) with the exact holder-discovery SQL and pg_terminate_backend command
    - No credentialed DSN pattern (the negative grep in verify) and no secret VALUE anywhere in the file; the DB secret is referenced by the name TEST_SUPABASE_DB_URL only
    - docs/runbooks/README.md indexes the new runbook in the existing format
    - The docs link-check job's glob (docs/runbooks/**/*.md) will pick the file up — internal links in the new file resolve (relative paths valid)
  </acceptance_criteria>
  <done>The mutex ships WITH its manual-unlock runbook (an adoption requirement, not a follow-up), and operators have a documented drill, unlock primitive, and watcher triage path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow_run payload → github-script | attacker-influenced strings (branch names, titles) enter a secrets-capable workflow context |
| watcher check → main HEAD SHA | the watcher's conclusion is visible to Railway's wait-for-CI |
| public repo ← runbook content | everything written is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-05 | Elevation of Privilege | github-script issue step (expression injection from workflow_run payload) | high | mitigate | all payload values passed via env:, zero `${{ }}` inside script bodies (analytics-deploy-verify pattern) |
| T-158-06 | Denial of Service | watcher redding main-HEAD check → Railway skips deploy | high | mitigate | exit-0-on-detection doctrine in header + structure; loud signal is the issue only |
| T-158-07 | Elevation of Privilege | GITHUB_TOKEN scope on the watcher | low | mitigate | contents: read + issues: write only; issue-only decision means no Actions-API write scope |
| T-158-08 | Information Disclosure | runbook leaking DSN/usernames in a PUBLIC repo | high | mitigate | secret names only; negative grep for credentialed DSN patterns in the task gate |
| T-158-SC | Tampering | supply chain — third-party actions | low | accept | only the two already-in-repo SHA-pinned actions (checkout, github-script); zero package installs |
</threat_model>

<verification>
- Task gates above (YAML parse, permission greps, label existence, runbook greps).
- Phase-gate (post-merge, verifier/orchestrator — the backstop truth):
  `gh workflow run main-ci-cancelled-watcher.yml -f run_id=31273384829` → first run creates
  the `main-ci-cancelled` issue; second identical dispatch comments on it instead of creating
  a duplicate; operator closes the issue. Record both watcher-run URLs.
- Read-back that the watcher file exists on main before relying on workflow_run (the trigger
  only fires from the default branch — it is inert until the phase PR merges; note this in
  the SUMMARY).
</verification>

<success_criteria>
- ROADMAP 158 SC-1 detection half: a forced cancelled main-run conclusion raises a loud
  dedup'd issue instead of silent grey; the watcher itself can never recreate #616 via a red
  main-HEAD check.
- The mutex's manual-unlock runbook exists, indexed, with holder discovery and
  pg_terminate_backend — satisfying the adoption requirement stated in CONTEXT and ROADMAP.
</success_criteria>

<output>
Create `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-02-SUMMARY.md` when done,
recording: the issue-only decision rationale, the label setup command output, and the note
that workflow_run activation awaits the merge to main (with the post-merge dispatch test
commands for the verifier).
</output>
