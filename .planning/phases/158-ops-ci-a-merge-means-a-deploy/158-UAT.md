---
status: complete
phase: 158-ops-ci-a-merge-means-a-deploy
source: [158-VERIFICATION.md]
started: 2026-08-21T00:05:00Z
updated: 2026-08-21T05:10:00Z
---

## Current Test

none — all 4 items resolved 2026-08-21 (PR #697 merged as 2544cba8).

## Tests

### 1. Phase-PR live CI run
expected: frontend aggregator both-polarity proof; ≥1 executed case per wired spec (api-key-flow, sync-analytics-flow, full-flow, csv-upload-flow, my-strategies); acquire steps + stamped fencing tests execute
result: |
  PASSED 2026-08-21. Evidence, all four sub-criteria:
  (a) GREEN polarity — PR #697 run 32424762495: all 21 checks pass incl.
      sql-tests (1m27s) and the frontend aggregator.
  (b) RED polarity — drill PR #698 (branch drill/158-uat1-aggregator-red,
      planted supabase/tests/test_zz_planted_red_drill_158.sql on the identical
      #697 tree, run 32426772489): sql-tests FAILED on the planted
      RAISE EXCEPTION (psql line 10) and the frontend aggregator FAILED with
      "sql-tests result=failure" + the runbook pointer; every other check
      green, so the red is attributable solely to the plant. Drill PR closed
      unmerged, branch deleted.
  (c) ≥1 executed case per wired spec — CI dot reporter cannot attribute
      per-spec, so each spec was run individually at HEAD with
      --reporter=line --workers=1 under its batch's env (unseeded pair under
      the e2e job's placeholder env incl. NEXT_PUBLIC_ALLOWED_ORIGINS; seeded
      trio against TEST via the 158-05 env wrapper): api-key-flow 3/12
      executed, sync-analytics-flow 4/18, full-flow 4/10, csv-upload-flow 2/4
      (2 skips = HAS_ANALYTICS_SERVICE-gated, correct CI mirror),
      my-strategies 1/1. Zero failures. Evidence file:
      scratchpad uat1-per-spec-evidence-158.md + per-spec logs.
  (d) In-situ acquires + fencing — all three mutex acquire steps ran on run
      32424762495; e2e-seeded CONTENDED for real (waited 75s while python
      held key 61616158, then acquired). tests/test_compute_jobs_fencing.py
      executed ~30 cases in the python job.
  Note: two skip-count integers in 158-05-SUMMARY's after-repair table
  (api-key-flow "3/8", sync-analytics "4/12") are transcription slips — the
  executed-test titles and pass counts match exactly; totals at HEAD are 12
  and 18.

### 2. Post-merge mutex probe ×3
expected: `gh workflow run mutex-probe.yml` dispatched 3×, all conclude `success`, per-run windows non-overlapping
result: |
  PASSED 2026-08-21. Dispatched on ci-probe/mutex-drill (at main HEAD 2544cba8)
  per the CR-01 ref gate — a bare main-ref dispatch lands in dispatch-guard and
  proves nothing. Sequential (same-ref dispatches cancel each other): runs
  32448377859, 32448628304, 32448874335 all `success`, plus the branch-creation
  push-run 32448078841 (bonus, also `success`). Success IS the non-overlap
  proof: each run's assert-serialization job reds on any window overlap.
  Sample (32448377859): windows abut exactly — c1 1787287921–966,
  c3 …966–011, c2 …011–056, 45s holds, zero overlap. Probe branch deleted
  after the drill.

### 3. Post-merge watcher dispatch ×2
expected: `gh workflow run main-ci-cancelled-watcher.yml -f run_id=31273384829 -f attempt=1` (never omit `attempt` — bare run_id is a silent no-op) → dedup'd issue created; second dispatch comments on it; close it after
result: |
  PASSED 2026-08-21. Both dispatches carried run_id=31273384829 attempt=1
  (the workflow's own documented immutable cancelled-attempt vector).
  Run 32448117561 `success` → created dedup issue #699 with the
  Railway-skip warning body; run 32448154607 `success` → commented on #699
  instead of duplicating. #699 closed with a drill explanation.

### 4. FIFO fairness (A1) — informational
expected: acquisition order under contention logged by the probe; fairness is DISCLAIMED, not asserted — record observation only, nothing depends on it
result: |
  RECORDED 2026-08-21 (observation only). Run 32448377859: arrival order
  (runner clock) [1, 2, 3] vs acquisition order (db clock) [1, 3, 2] — the
  advisory lock did NOT grant in arrival order, a live confirmation that
  fairness is correctly disclaimed. Mutual exclusion held regardless
  (windows abut with zero overlap). The probe's own log carries the same
  caveat: the barrier collapses arrival spread below timestamp resolution,
  so this is not an ordering assertion.

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
