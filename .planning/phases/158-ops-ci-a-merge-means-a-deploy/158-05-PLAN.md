---
phase: 158-ops-ci-a-merge-means-a-deploy
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/api-key-flow.spec.ts
  - e2e/sync-analytics-flow.spec.ts
  - e2e/full-flow.spec.ts
  - e2e/csv-upload-flow.spec.ts
  - e2e/my-strategies.spec.ts
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Each of the four named orphan specs (api-key-flow, sync-analytics-flow, full-flow, csv-upload-flow) executes at least one non-skipped case under its target batch environment — a wired spec whose every case self-skips is the same false-coverage state as orphanhood"
    - "Every case disabled during repair carries an explicit test.skip with a recorded reason string — a spec whose required env is absent records a VISIBLE skip decision, never silent orphanhood (edge: OPS-03 orphan adoption)"
    - "full-flow's authed half is a RECORDED skipped-by-design decision (no E2E_TEST_EMAIL / E2E_TEST_PASSWORD secrets exist in CI and none are provisioned — decision in the spec comment + SUMMARY); its anon cases execute"
    - "e2e/my-strategies.spec.ts covers the NAV-01 surface: a seeded owner sees their OWN prefixed strategy listed at /my-strategies and a second seeded user's prefixed strategy is absent — both assertions scoped to THIS spec's own seeds (PR #654 lesson: never assert global DB state), selectors scoped by href/route/data-testid, never bare 'My Strategies' heading text (DEF-149-B: /strategies renders the same h1)"
    - "The new spec follows the seeded-spec contract: HAS_SEED_ENV self-skip constant, seedTestAllocator + loginViaForm, NAME_PREFIX cleanup guarded by HAS_SEED_ENV"
  artifacts:
    - path: "e2e/my-strategies.spec.ts"
      provides: "NAV-01 surface e2e coverage (grep -rn my-strategies e2e/ returned nothing at HEAD — this is a NEW file)"
      contains: "HAS_SEED_ENV"
    - path: "e2e/full-flow.spec.ts"
      provides: "Repaired, executable anon cases + recorded authed-half skip decision"
      contains: "test.skip"
  key_links:
    - from: "e2e/my-strategies.spec.ts"
      to: "e2e/helpers/seed-test-project.ts"
      via: "seedTestAllocator + loginViaForm + cleanupStrategiesByNamePrefix (wizard-resume.spec.ts pattern)"
      pattern: "seedTestAllocator"
    - from: "e2e/my-strategies.spec.ts"
      to: "/my-strategies route"
      via: "href-scoped navigation (a[href=\"/my-strategies\"] Sidebar precedent), never heading-text selectors"
      pattern: "my-strategies"
  prohibitions:
    - statement: "No new CI secrets are provisioned for these specs — the E2E_TEST_EMAIL path is refused in favor of the in-repo seedTestAllocator pattern; a spec must not depend on env that exists nowhere in CI without a visible skip"
      status: unverified
      flagged: true
      verification: "grep the five spec files: no reference to E2E_TEST_EMAIL/E2E_TEST_PASSWORD outside full-flow's pre-existing self-skip guard; the new spec uses seed helpers only"
---

<objective>
Close the authoring half of OPS-03: repair the four named never-run orphan specs so each
actually executes (run → repair → then plan 06 wires), and author the missing NAV-01 surface
spec `e2e/my-strategies.spec.ts` (measured at HEAD: no e2e file references my-strategies).
Wiring never-run specs straight into a blocking batch converts spec-rot into a red frontend
aggregator on unrelated PRs — repair happens here, wiring happens in plan 06 after this plan
lands.

Purpose: the orphaned e2e specs (incl. the NAV-01 surface) can execute honestly in a CI batch
(ROADMAP 158 SC-3, authoring half).
Output: four repaired specs + one new seeded spec, each proven to execute locally.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-PATTERNS.md
@e2e/wizard-resume.spec.ts
@e2e/helpers/seed-test-project.ts
@playwright.config.ts
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

- `.github/workflows/mutex-probe.yml` (plan 01); `.github/workflows/ci.yml` changes (plans 01, 06)
- `.github/workflows/main-ci-cancelled-watcher.yml` + label `main-ci-cancelled` (plan 02)
- `docs/runbooks/shared-test-db-mutex.md` + README index entry (plan 02)
- `scripts/drain-test-compute-backlog.ts` (plan 03)
- `e2e/my-strategies.spec.ts` — NEW seeded spec, the NAV-01 surface (this plan)
- `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md` (plan 03), `…/158-OPS11-EVIDENCE.md` (plan 04), `…/158-DB-TYPES-DECISION.md` (plan 06)
- Advisory lock key constant: `61616158` (plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Run and repair the four named orphan specs</name>
  <precondition>A local Playwright run is possible: node_modules installed (npm ci if absent), Playwright browsers installed, and the app server strategy defined by playwright.config.ts (webServer or manual dev server) is available</precondition>
  <files>e2e/api-key-flow.spec.ts, e2e/sync-analytics-flow.spec.ts, e2e/full-flow.spec.ts, e2e/csv-upload-flow.spec.ts</files>
  <read_first>
    - playwright.config.ts (how specs get a server + env; what the unseeded batch's placeholder env means locally)
    - e2e/api-key-flow.spec.ts, e2e/sync-analytics-flow.spec.ts, e2e/full-flow.spec.ts, e2e/csv-upload-flow.spec.ts (FULL files — these have NEVER executed in CI; expect drift)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md §OPS-03 (orphan census rows for the four named specs; the load-bearing wiring pitfall)
  </read_first>
  <action>
    For EACH of the four named specs, in order: run it locally
    (`npx playwright test e2e/<name>.spec.ts`), classify every failure as spec-rot (stale
    selector/copy/route — repair) vs missing-surface (feature moved/removed — replace the case
    or skip with a reason) vs missing-env (self-skip correctly), then repair:

    - `api-key-flow.spec.ts` (2 pre-existing test.skip(true) cases; RESEARCH notes :212
      matches copy by regex luck): update selectors/copy to the current wizard surface; keep
      its authed self-skips; make its anon path assert current reality.
    - `sync-analytics-flow.spec.ts` (3 pre-existing skips): same treatment.
    - `full-flow.spec.ts`: PLANNER DECISION (recorded, Claude's discretion): its authed
      describes require E2E_TEST_EMAIL / E2E_TEST_PASSWORD which exist NOWHERE in ci.yml —
      the decision is skipped-by-design; do NOT provision secrets. Add a dated comment at the
      authed describe explaining the decision (phase 158 / OPS-03: authed half skipped-by-design,
      no CI secrets; anon cases are the coverage). Ensure the anon cases run and pass.
    - `csv-upload-flow.spec.ts` (Phase 15 CSV wizard branch): repair against the current CSV
      wizard surface; determine its true env need — if it requires a real DB/auth surface that
      the placeholder env cannot serve, convert it to the seeded-spec contract (HAS_SEED_ENV
      constant + seed helpers) and record that plan 06 must wire it into the SEEDED list;
      otherwise keep it placeholder-safe for the unseeded list. Record the batch verdict for
      EACH spec in the SUMMARY (plan 06 consumes it).

    Repair rules: any case disabled gets `test.skip(true, "<reason — 158/OPS-03 disposition>")`
    with a real reason string, never a bare skip; do not delete cases silently; do not weaken
    an assertion to make it pass (a test that cannot fail is worse than none) — if an
    assertion's surface is gone, the honest move is skip-with-reason or rewrite against the
    real surface. After repair, each spec must report >=1 executed (passed, non-skipped) case
    in the local run (use the line/JSON reporter counts).
  </action>
  <verify>
    <automated>set -o pipefail; npx playwright test e2e/api-key-flow.spec.ts e2e/sync-analytics-flow.spec.ts e2e/full-flow.spec.ts e2e/csv-upload-flow.spec.ts --reporter=line 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - The combined local run exits 0 with >=1 passed (non-skipped) case attributable to EACH of the four files (reporter output per file recorded in the SUMMARY)
    - Every skip added or retained carries a reason string; zero bare `test.skip(true)` without a reason remain in the four files
    - full-flow's authed describe carries the dated skipped-by-design decision comment referencing phase 158 / OPS-03
    - The SUMMARY records a batch verdict (unseeded vs seeded) for each of the four specs, derived from what each spec's repaired form actually requires
    - No spec references secrets that do not exist in CI without a self-skip guard
  </acceptance_criteria>
  <done>All four named orphans are repaired, each proven to execute >=1 real case locally, with every disabled case a visible reasoned decision and each spec's batch destination recorded for plan 06.</done>
</task>

<task type="auto">
  <name>Task 2: Author e2e/my-strategies.spec.ts — the NAV-01 surface</name>
  <precondition>Seeded-e2e env available locally: the env vars named by the HAS_SEED_ENV constant in e2e/wizard-resume.spec.ts resolve, pointing at the TEST project (qmnijlgmdhviwzwfyzlc)</precondition>
  <files>e2e/my-strategies.spec.ts</files>
  <read_first>
    - e2e/wizard-resume.spec.ts (the canonical seeded-spec shape: HAS_SEED_ENV, seedTestAllocator role "both" at :82, loginViaForm :111, NAME_PREFIX cleanup :70-73)
    - e2e/helpers/seed-test-project.ts (available seed helpers + cleanup functions; what a seeded strategy looks like)
    - src/app/(dashboard)/my-strategies (the page under test — its actual DOM: list container, row link targets, empty state, data-testids if any)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-PATTERNS.md (my-strategies section: binding assertion + selector rules)
  </read_first>
  <action>
    Create `e2e/my-strategies.spec.ts` on the wizard-resume.spec.ts pattern:

    - `HAS_SEED_ENV` constant + `test.skip(!HAS_SEED_ENV, ...)` (the MA-8 joining rule's
      second half — plan 06 adds the list half).
    - Unique NAME_PREFIX (e.g. `e2e-my-strategies-`), cleanup via the helpers'
      cleanup-by-prefix function in beforeEach/afterAll guarded by HAS_SEED_ENV.
    - Seed: allocator A (role "both") with one strategy named under the prefix; allocator B
      with one differently-prefixed strategy (both minted by THIS spec — own-seed rule).
    - Cases (each an own-seed assertion, PR #654 lesson — the TEST project is polluted by
      construction, so never assert counts, emptiness, or any global state):
      1. A logs in via loginViaForm, navigates via the sidebar link scoped
         `a[href="/my-strategies"]` (DEF-149-B: /strategies and /my-strategies both render an
         h1 reading "My Strategies" — NEVER select by heading text), and sees A's prefixed
         strategy name rendered.
      2. On the same page, B's prefixed strategy name is NOT present (RLS/ownership boundary,
         still own-seed-scoped — both names are this spec's mints).
      3. A's row links toward that strategy's detail/factsheet surface (assert the href
         shape, click-through optional).
    - Selector discipline throughout: route/href/data-testid scoping; no bare heading-text or
      ambiguous copy selectors.

    Run it locally with the seed env until green; confirm the Playwright report shows its
    cases EXECUTED (not skipped) under the seed env, and skipped (not failed) without it
    (unset the env for one run to prove the self-skip arm — both polarities observed).
  </action>
  <verify>
    <automated>set -o pipefail; npx playwright test e2e/my-strategies.spec.ts --reporter=line 2>&1 | tail -8 && grep -q 'HAS_SEED_ENV' e2e/my-strategies.spec.ts && grep -q 'seedTestAllocator' e2e/my-strategies.spec.ts && grep -q 'a\[href="/my-strategies"\]' e2e/my-strategies.spec.ts && ! grep -q "getByRole('heading', { name: 'My Strategies' })" e2e/my-strategies.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Seeded local run: exit 0 with all cases executed (non-skipped); env-less run: cases report skipped, not failed (both runs' reporter tails in the SUMMARY)
    - The spec contains HAS_SEED_ENV, seedTestAllocator, prefix cleanup, and the href-scoped navigation selector; the negative grep for a heading-text selector passes
    - Assertions reference only names minted by this spec's own seeds — no count/empty-state/global assertions anywhere in the file
    - Two seeded users: owner-visible and non-owner-absent assertions both present
  </acceptance_criteria>
  <done>The NAV-01 surface has a real, seeded, own-seed-scoped e2e spec that executes locally, self-skips visibly without seed env, and is ready for plan 06 to wire into the seeded batch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local spec runs → shared TEST project | seeded runs write real rows into the shared DB |
| spec source → public repo | selectors/fixtures are world-readable; no credentials may appear |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-16 | Tampering | seeded spec polluting shared TEST state | low | mitigate | unique NAME_PREFIX + guarded cleanup; own-seed assertions only |
| T-158-17 | Information Disclosure | credentials in spec files (public repo) | high | mitigate | seed helpers mint throwaway users; no E2E_TEST_EMAIL plumbing; secrets referenced by name only in comments |
| T-158-18 | Spoofing | ownership-boundary regression on /my-strategies | medium | mitigate | the non-owner-absent assertion pins the RLS-scoped read |
</threat_model>

<verification>
- Task gates above (local Playwright runs with per-file executed-case evidence, structural greps).
- Handoff contract: the SUMMARY's per-spec batch verdicts are the direct input to plan 06's
  wiring task — plan 06 must not re-derive them.
</verification>

<success_criteria>
- Four repaired specs each executing >=1 real case; the new my-strategies spec green seeded
  and visibly-skipping unseeded; every disabled case a reasoned, dated decision; zero new
  secrets required.
</success_criteria>

<output>
Create `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-05-SUMMARY.md` when done,
recording: per-spec run results, the batch verdict table (spec → unseeded/seeded), the
full-flow authed-half decision, and any case skipped with its reason.
</output>
