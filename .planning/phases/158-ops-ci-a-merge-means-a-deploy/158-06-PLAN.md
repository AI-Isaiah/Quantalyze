---
phase: 158-ops-ci-a-merge-means-a-deploy
plan: 06
type: execute
wave: 2
depends_on: [158-01, 158-05]
files_modified:
  - .github/workflows/ci.yml
  - TODOS.md
  - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-DB-TYPES-DECISION.md
autonomous: true
requirements: [OPS-03]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All four named orphan specs plus e2e/my-strategies.spec.ts appear in exactly ONE ci.yml Playwright batch list each (unseeded at ~:1404 or seeded at ~:1774-1797 per plan 158-05's batch verdicts), honoring the MA-8 joining rule for every seed-gated spec (list entry + in-spec HAS_SEED_ENV constant)"
    - "Each of the 15 remaining orphan specs has a one-line recorded disposition in TODOS.md tagged [158-OPS-03] — OPS-03 closes as a CLASS decision (wire-later / defer / delete-candidate), never a silent partial fix"
    - "DB-types drift carries an explicit recorded decision NOT to gate, with the soundness blockers (hand-patched scenarios block that a regen against a target missing migration 20260621120000 silently reverts; TEST not schema-authoritative; broken local shadow replay) and the compensating control (extend src/lib/database.types.test.ts pins when columns are added; regen-when-touched convention) in 158-DB-TYPES-DECISION.md"
    - statement: "On the phase PR's CI run, every newly wired spec reports >=1 executed (non-skipped) case in its batch's Playwright output — wired-but-all-skip is the same false-coverage state as orphanhood"
      verification: backstop
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Batch-list wiring for the five specs"
      contains: "e2e/my-strategies.spec.ts"
    - path: ".planning/phases/158-ops-ci-a-merge-means-a-deploy/158-DB-TYPES-DECISION.md"
      provides: "The explicitly recorded decision OPS-03 permits in place of a regen gate"
      contains: "20260621120000"
    - path: "TODOS.md"
      provides: "15 orphan-spec dispositions, class closure"
      contains: "[158-OPS-03]"
  key_links:
    - from: ".github/workflows/ci.yml seeded list"
      to: "e2e/my-strategies.spec.ts"
      via: "MA-8 joining rule — list entry here + HAS_SEED_ENV constant in the spec (landed in plan 158-05)"
      pattern: "e2e/my-strategies\\.spec\\.ts"
  prohibitions:
    - statement: "No new needs: edges among sql-tests / python / e2e-seeded while wiring the batches — batch-list membership changes only, never job-graph changes"
      status: unverified
      flagged: true
      verification: "git diff of ci.yml for this plan touches only the two npx playwright test invocation lists and adjacent comments; needs: blocks unchanged"
    - statement: "This phase creates no supabase/migrations/** file — the DB-types outcome is a recorded decision, and any future regen gate proposal must solve the scenarios hand-patch + schema-authority blockers first"
      status: unverified
      flagged: true
      verification: "git diff --stat shows zero paths under supabase/migrations/; the decision artifact exists instead of a gate workflow"
---

<objective>
Close OPS-03 as a class: wire the five repaired/authored specs (plan 158-05) into the correct
ci.yml Playwright batch lists on top of plan 158-01's ci.yml (both plans own that file —
hence wave 2), record a one-line disposition for each of the 15 remaining orphan specs in
TODOS.md, and record the DB-types decision the requirement explicitly permits in place of a
regeneration gate.

Purpose: orphaned e2e specs execute in a CI batch and DB-types drift has an explicit recorded
decision (ROADMAP 158 SC-3, closure half).
Output: updated ci.yml batch lists, 15 TODOS dispositions, DB-types decision artifact.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-PATTERNS.md
@.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces (phase-wide, plans 01–06)

- `.github/workflows/mutex-probe.yml` (plan 01); `.github/workflows/ci.yml` — mutex + aggregator (plan 01) + batch lists (this plan)
- `.github/workflows/main-ci-cancelled-watcher.yml` + label `main-ci-cancelled` (plan 02)
- `docs/runbooks/shared-test-db-mutex.md` + README index entry (plan 02)
- `scripts/drain-test-compute-backlog.ts` (plan 03)
- `e2e/my-strategies.spec.ts` (plan 05)
- `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-OPS04-DRAIN-EVIDENCE.md` (plan 03), `…/158-OPS11-EVIDENCE.md` (plan 04), `…/158-DB-TYPES-DECISION.md` (this plan)
- TODOS.md: 15 `[158-OPS-03]` orphan-spec disposition entries (this plan)
- Advisory lock key constant: `61616158` (plan 01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Wire the five specs into the correct ci.yml batch lists</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-05-SUMMARY.md (the per-spec batch verdict table — do NOT re-derive it)
    - .github/workflows/ci.yml — the unseeded invocation (~:1404, with the both-lists warning at :1379-1384) and the seeded invocation (~:1774-1797, with the MA-8 comment at :1757-1762); anchors may have drifted after plan 158-01 — locate by the npx playwright test invocations
  </read_first>
  <action>
    Using plan 158-05's batch verdict table verbatim:
    - Append each placeholder-safe spec (expected: api-key-flow, sync-analytics-flow,
      full-flow, and csv-upload-flow unless 158-05 converted it to seeded) to the UNSEEDED
      single-line `npx playwright test` invocation in the `e2e` job.
    - Append `e2e/my-strategies.spec.ts \` (and any spec 158-05 converted to the seeded
      contract) to the SEEDED multi-line list in the `e2e-seeded` job, before the
      `--timeout 60000` line, matching the existing backslash-continuation format.
    - Honor MA-8 for every seeded addition: the spec must carry the HAS_SEED_ENV constant
      (verify by grep — 158-05 authored it; if a converted spec lacks it, that is a 158-05
      regression to fix there, not here).
    - Update BOTH lists' maintenance comments (the "Update BOTH lists" warning and the MA-8
      comment) with a one-line Phase 158 note that the four long-orphaned specs + the NAV-01
      spec are now wired.
    - Touch ONLY the two invocation lists and adjacent comments — no job graph, no needs:,
      no concurrency, no if: changes (plan 158-01 owns those regions and has landed; do not
      reformat them).
  </action>
  <verify>
    <automated>for s in api-key-flow sync-analytics-flow full-flow csv-upload-flow my-strategies; do [ "$(grep -c "e2e/$s.spec.ts" .github/workflows/ci.yml)" -eq 1 ] || exit 1; done && grep -q 'HAS_SEED_ENV' e2e/my-strategies.spec.ts && node -e "const y=require('js-yaml');y.load(require('fs').readFileSync('.github/workflows/ci.yml','utf8'));console.log('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Each of the five spec filenames appears EXACTLY once across the two batch invocations (the per-spec count check in verify), in the batch plan 158-05's verdict table assigned
    - Every seeded-list addition's spec file contains the HAS_SEED_ENV constant (MA-8 both-halves)
    - The diff for this task touches only the two playwright invocations and comments — needs:/concurrency:/if: blocks byte-identical to plan 158-01's output
    - js-yaml parse of ci.yml succeeds
  </acceptance_criteria>
  <done>The four named orphans and the NAV-01 spec are wired into real CI batches per their repaired forms — the never-runs now run.</done>
</task>

<task type="auto">
  <name>Task 2: Record the 15 orphan dispositions and the DB-types decision</name>
  <reversibility rating="reversible">Both artifacts are prose records; amending them later is a one-file edit.</reversibility>
  <files>TODOS.md, .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-DB-TYPES-DECISION.md</files>
  <read_first>
    - TODOS.md (the single ground-truth backlog — match its entry format; add entries, never restructure)
    - .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-RESEARCH.md §OPS-03 (the 20-orphan census table — the 15 non-named specs with their seed/skip classification — and the DB-types measured reality: header, freshness vs ledger, the PERSIST-01 hand-patch landmine, partial guards)
    - src/lib/database.types.test.ts (the C-0156 / PERSIST-01 docblocks — the compensating control being cited)
  </read_first>
  <action>
    1. TODOS.md: add one line per remaining orphan spec, tagged `[158-OPS-03]`, naming the
       spec and a disposition with a one-clause rationale. The 15 specs (from the RESEARCH
       census): admin-csv-status-axe, discovery-sparkline-regression, discovery-watchlist,
       for-quants-landing, discovery, for-quants-onboarding, bridge-flow, mandate-form,
       match-queue, security-page, simulator-flow, strategy-detail-tabs,
       wizard-hydration-probe, sync-flow-queue, wizard-sync-regression — plus a 16th line for
       portfolio-pdf-demo's non-@nightly cases (its @nightly cases already run in
       nightly.yml:109). Dispositions are one of: wire-later (seed-gated, candidate for the
       seeded list once run-and-repaired), defer (tracked, low value now), delete-candidate
       (surface gone — deletion needs its own reviewed change, NOT done here). Use the
       census's seed-gated classification to inform each call; every line must be a decision,
       not a description.
    2. Create `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-DB-TYPES-DECISION.md`
       recording, with citations: DECISION — no DB-types regeneration gate is added (the
       option OPS-03 explicitly permits). Reasons (all measured in 158-RESEARCH.md): (a)
       src/lib/database.types.ts contains a HAND-PATCHED scenarios block (migration
       20260621120000) that a regen against a target missing that migration SILENTLY REVERTS
       — a naive diff gate self-destructs or commits the reversion; (b) TEST is not
       schema-authoritative (migrations auto-apply to PROD on merge; TEST caught up manually)
       and the local shadow-DB replay is broken (P156-IN-01), so no CI-reachable
       schema-authoritative target exists; (c) the file is fresher than the ledger claimed
       (computation_warned + metrics_json_by_basis present since commit a6a2dee8).
       Compensating controls (standing): extend the src/lib/database.types.test.ts type-level
       pins when columns are added (C-0156 / PERSIST-01 pattern — a stale regen that DROPS
       pinned columns fails the build), and the regen-when-touched convention. Revisit
       trigger: a gate becomes sound only when a schema-authoritative CI-reachable target
       exists AND the scenarios hand-patch is reconciled into regenerable state. Note
       explicitly that src/lib/types.ts is a separate hand-written file OUTSIDE this
       decision's "generated types" scope.
  </action>
  <verify>
    <automated>[ "$(grep -c '\[158-OPS-03\]' TODOS.md)" -ge 16 ] && grep -q '20260621120000' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-DB-TYPES-DECISION.md && grep -q 'database.types.test.ts' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-DB-TYPES-DECISION.md && grep -qi 'decision' .planning/phases/158-ops-ci-a-merge-means-a-deploy/158-DB-TYPES-DECISION.md</automated>
  </verify>
  <acceptance_criteria>
    - TODOS.md gains >=16 lines tagged [158-OPS-03] (15 orphans + portfolio-pdf-demo's non-@nightly cases), each carrying a disposition verb (wire-later / defer / delete-candidate) and a one-clause rationale; no existing TODOS entries modified or removed
    - The decision artifact contains: the DECISION statement, all three measured reasons with the migration id 20260621120000 and the schema-authority blocker, both compensating controls, the revisit trigger, and the types.ts scope note
    - No workflow, script, or migration file is created for DB-types — the decision replaces the gate
  </acceptance_criteria>
  <done>OPS-03's class half is closed: every orphan has a recorded decision, and DB-types drift has the explicitly recorded, reasoned decision the requirement permits — with its compensating control named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ci.yml batch lists → CI execution | list membership decides what gates PRs; a mis-wired spec reds unrelated PRs |
| TODOS.md / .planning → public repo | dispositions and decisions are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-19 | Denial of Service | wiring a still-rotten spec reddening the frontend aggregator on unrelated PRs | medium | mitigate | wiring consumes plan 158-05's repaired-and-locally-proven verdicts only; backstop truth re-checks executed-case counts on the phase PR |
| T-158-20 | Tampering | accidental job-graph edits while touching ci.yml lists | medium | mitigate | diff-scope acceptance criterion: only the two invocation lists + comments change |
| T-158-21 | Information Disclosure | decision artifacts leaking non-public details in a public repo | low | mitigate | artifacts carry migration ids, file paths and reasoning only — no secrets, no usernames |
</threat_model>

<verification>
- Task gates above (per-spec exactly-once wiring counts, TODOS tag count, decision-artifact greps, yaml parse).
- Phase-gate (backstop truth, verifier on the phase PR): the PR's e2e + e2e-seeded job logs
  show >=1 executed (non-skipped) case for each newly wired spec — Playwright's per-spec
  reporting makes this a read-off, not a judgment call.
</verification>

<success_criteria>
- ROADMAP 158 SC-3 fully closed: the orphaned specs (incl. the NAV-01 surface) execute in CI
  batches, the remaining orphans carry recorded class dispositions, and DB-types drift has an
  explicit recorded decision with a named compensating control.
</success_criteria>

<output>
Create `.planning/phases/158-ops-ci-a-merge-means-a-deploy/158-06-SUMMARY.md` when done,
recording: the final batch assignments, the TODOS disposition lines added, and the decision
artifact path.
</output>