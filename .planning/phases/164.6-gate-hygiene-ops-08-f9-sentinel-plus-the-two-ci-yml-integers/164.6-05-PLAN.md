---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 05
type: execute
wave: 3
depends_on:
  - "164.6-01"
  - "164.6-02"
  - "164.6-03"
  - "164.6-04"
files_modified:
  - docs/runbooks/ledger-refresh-go-live.md
  - docs/runbooks/match-engine.md
autonomous: true
requirements:
  - OPS-08-F2
  - 161.1-D13

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The go-live runbook tells the founder how to read a failing tick's 'candidate_enqueue_failed' row, selecting counts only and never the target list, and says the fan-out's return value still means jobs INSERTED (D-10, D-11)"
    - "The go-live runbook says any 'is it dormant?' reading of cron_runs under ledger_refresh_fanout must filter on error or metadata cause, because since Phase 164.6 a row there may be a per-candidate failure row (D-11)"
    - "The match-engine runbook's deliberate-exercise paragraph says a failed candidate now ALSO leaves one counted cron_runs row beside the unchanged WARNING (D-11)"
    - "The go-live runbook carries a named BLOCKING precondition, [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT], that forbids registering any schedule for public.enqueue_ledger_composite_refresh() until the Python run_stitch_composite_job guard re-reads the live compute_jobs row instead of the claim-time metadata snapshot; the Python change itself is NOT made in this phase (D-01; routes the Plan 02 residual per the orchestrator decision)"
    - "With all five plans in the tree, the full vitest suite, typecheck, lint and pending plan-anchor verification are green"
  artifacts:
    - path: docs/runbooks/ledger-refresh-go-live.md
      provides: "failure-row reading query, dormancy discrimination sentence, composite-schedule blocking precondition"
      contains: "candidate_enqueue_failed"
    - path: docs/runbooks/match-engine.md
      provides: "one sentence on the counted failure row"
      contains: "candidate_enqueue_failed"
  key_links:
    - from: docs/runbooks/ledger-refresh-go-live.md
      to: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
      via: "the runbook query reads the metadata keys failed_count and enqueued_count and the error literal the migration writes"
      pattern: "candidate_enqueue_failed"
    - from: docs/runbooks/ledger-refresh-go-live.md
      to: analytics-service/services/job_worker.py
      via: "the composite-schedule precondition names run_stitch_composite_job and _refresh_marker_still_on_row by symbol"
      pattern: "run_stitch_composite_job"
  prohibitions:
    - requirement_id: OPS-08-F2
      category: privacy
      status: resolved
      verification: judgment
      statement: "A runbook query must never select the per-target list, because runbook output gets pasted into this public repo."
    - requirement_id: 161.1-D13
      category: safety
      status: resolved
      verification: judgment
      statement: "The composite fan-out must never be scheduled while the Python composite guard reads a claim-time snapshot; the runbook states this as a blocking precondition, not advice."
---

<objective>
Tell the runbooks' readers how to read Plan 03's new failure row (OPS-08-F2, ROADMAP Phase 164.6 success criterion 3), give the Plan 02 data-integrity residual a destination (161.1-D13), and run the phase-level full-suite pass once every plan is in the tree.

Scope (D-01): criteria 2, 3 and 4 only. This plan edits two public runbooks and runs gates. It does NOT change `analytics-service/services/job_worker.py`: the Python claim-time-snapshot residual (RESEARCH Open Question 1, RESOLVED) is outside D-01, and per the orchestrator's decision its destination is a named blocking precondition on the COMPOSITE fan-out's activation in the go-live runbook. The composite fan-out is dormant and unscheduled, and TODOS `161.1-D13` already says the composite twin must be closed before that founder-gated go-live op, so the precondition sits exactly where the risk becomes reachable. The orchestrator additionally reports it as an OPEN founder item that needs a phase.

Created in revision 1 by splitting the old Plan 04 (per-plan budget); the runbook task and the phase-level pass moved here unchanged in substance.

Purpose: an operator reading a failing tick sees counts, a "dormant?" reader is not misled, and nobody can schedule the composite fan-out over a known suppressed-failure path.
Output: two runbook edits and a green phase-level suite.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-CONTEXT.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-RESEARCH.md

Executor constraints (this repo, this run):
- Anchors BY SYMBOL or heading text. Line numbers in `.planning/` artifacts are forbidden (plan-anchor-verify).
- The runbooks are PUBLIC: never paste a strategy id, a job id, the target list, a home path or a username into them.
- Do NOT touch VERSION, the package.json version, CHANGELOG.md (no ship in this phase); do not stage `.planning/milestone.lock`, `.planning/state.json`, `.planning/phases/164.5.3-*`. No CI skip token in any commit message. No home path or username in `.planning/`.
- Python is never edited or executed by this plan.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — the go-live runbook reads the migration's failure row end-to-end, carries the composite-schedule precondition, and match-engine names the counted row</name>
  <precondition>Plan 03 is in this worktree: `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` exists (or the stamp Plan 03's SUMMARY records; use that stamp everywhere below).</precondition>
  <files>docs/runbooks/ledger-refresh-go-live.md, docs/runbooks/match-engine.md</files>
  <read_first>
    - docs/runbooks/ledger-refresh-go-live.md — `## Watching it`, `### Did the tick itself run, and how much did it enqueue?`, where the return integer is read back (D-10), `## Step 2 — the schedule (LIVE op 2 of 2)` and its `### ⛔ **This statement lives HERE and NEVER in a migration.**`, and `## What this runbook deliberately does not cover` with its `**The deribit composite.**` bullet
    - docs/runbooks/match-engine.md — the paragraph beginning `**The deliberate exercise.**`
    - supabase/migrations/20260924120000_ledger_fanout_failure_count.sql (cause string and metadata keys, from Plan 03)
    - analytics-service/services/job_worker.py — `run_stitch_composite_job` (its `job.get("metadata")` read before the composite-marker comparison) and `_refresh_marker_still_on_row` (read only)
    - TODOS.md entry `161.1-D13` (the "must be closed BEFORE that founder-gated go-live op" sentence)
    - .planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-02-SUMMARY.md and 164.6-03-SUMMARY.md (written by Plans 02 and 03: the residual as recorded, and the migration stamp actually used)
    - .planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-RESEARCH.md `## Open Questions (RESOLVED)` item 1
  </read_first>
  <action>
    Precondition steps: run `node scripts/verify-plan-anchors.mjs` on this PLAN.md path and halt on non-zero; assert the `<precondition>`.

    1. ledger-refresh-go-live.md, under `### Did the tick itself run, and how much did it enqueue?` (D-10, D-11): add a short subsection with RESEARCH Code Example 4's admin-SQL-editor query selecting `completed_at`, `error`, `metadata->>'function'`, `(metadata->>'failed_count')::int` and `(metadata->>'enqueued_count')::int` from `public.cron_runs` where `cron_name = 'ledger_refresh_fanout' AND error = 'candidate_enqueue_failed'`, newest first, limit 5. Say in one sentence that the per-target list is deliberately not selected because runbook output gets pasted into this public repo. Add one sentence that since Phase 164.6 a row under `ledger_refresh_fanout` may be a per-candidate failure row rather than dormancy, so any "is it dormant?" reading must filter on `error` (or `metadata->>'cause'`), and that the function's return value still means jobs INSERTED (D-10).
    2. ledger-refresh-go-live.md, composite precondition (orchestrator decision; D-01 keeps the Python change out of this phase). The runbook has no composite activation steps today (the composite is listed under `## What this runbook deliberately does not cover`), so add a new section `## ⛔ Before the COMPOSITE fan-out is ever scheduled — BLOCKING precondition [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` immediately BEFORE `## What this runbook deliberately does not cover`. It states, in this runbook's voice: (a) `public.enqueue_ledger_composite_refresh()` is dormant and has no `cron.schedule` registration; (b) BLOCKING: no schedule naming it may be registered until `run_stitch_composite_job` in `analytics-service/services/job_worker.py` re-reads the LIVE `compute_jobs` row's `metadata->>'source'` before honouring the `ledger-refresh-composite` marker, the way the single-key honour sites do through `_refresh_marker_still_on_row`, instead of the claim-time `job.get("metadata")` snapshot it reads today; (c) why: a user-initiated composite resync that dedups onto an already-claimed fan-out job is retracted by the TypeScript sites (Phase 164.6) but that run's Python guard still sees the stale marker and suppresses the failure, while a retraction before claim is fully effective; (d) how to check it is met: `run_stitch_composite_job` calls a live re-read before its composite-marker comparison, with a test that goes RED without it; (e) that Phase 164.6 did NOT implement it, it is an OPEN founder item awaiting a phase, and TODOS `161.1-D13` already requires the composite twin closed before this go-live op. Then append one sentence to the `**The deribit composite.**` bullet pointing to the new section by its bracketed id. Name no strategy or job id.
    3. match-engine.md: in `**The deliberate exercise.**`, add one sentence that a failed candidate now ALSO leaves one counted `cron_runs` row (`error = 'candidate_enqueue_failed'`) beside the unchanged WARNING (D-11).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const d=fs.readFileSync('docs/runbooks/ledger-refresh-go-live.md','utf8');const m=fs.readFileSync('supabase/migrations/20260924120000_ledger_fanout_failure_count.sql','utf8');const need=['candidate_enqueue_failed','failed_count','enqueued_count'];const miss=need.filter(k=>!d.includes(k)||!m.includes(k));const pre=d.indexOf('[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]'),not=d.indexOf('## What this runbook deliberately does not cover');if(miss.length||pre<0||not<0||pre>not||!d.includes('run_stitch_composite_job')){console.error('FAIL',{miss,pre,not});process.exit(1)}console.log('OK runbook reads the migration keys and carries the precondition')"</automated>
    <fails_when>non-zero exit or output lacks "OK runbook reads the migration keys"</fails_when>
    <automated>grep -c 'candidate_enqueue_failed' docs/runbooks/match-engine.md</automated>
    <fails_when>prints 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'candidate_enqueue_failed' docs/runbooks/ledger-refresh-go-live.md` >= 1 and `grep -c 'candidate_enqueue_failed' docs/runbooks/match-engine.md` >= 1.
    - The added go-live query's SELECT list names exactly the five columns in action step 1 and does not select `failed_targets`: `grep -c "metadata->'failed_targets'" docs/runbooks/ledger-refresh-go-live.md` = 0.
    - `grep -c '164.6-COMPOSITE-CLAIMTIME-SNAPSHOT' docs/runbooks/ledger-refresh-go-live.md` >= 2 (the section heading and the pointer in the deribit composite bullet).
    - `git diff --stat -- analytics-service/` is empty (the Python change is not made here).
  </acceptance_criteria>
  <done>Both runbooks describe the new row using the migration's own keys; the composite fan-out cannot be scheduled from this runbook without first meeting the named precondition.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-level full-suite pass with all five plans in the tree</name>
  <files>none (verification only; a failure is fixed in the plan that owns the failing file, then this task re-runs)</files>
  <read_first>
    - .planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-04-SUMMARY.md (the moved pins)
  </read_first>
  <action>
    Run each command in `<verify>` as its own step and record the summary line of each in the SUMMARY. If one fails, name the failing file and the plan that owns it, fix the cause in that file (never by lowering a floor, widening an allowlist or adding a waiver), and re-run the whole list. Record the still-EXPECTED reds from Plan 03 (baseline-content-drift: exactly the two fan-out findings; TEST applied-ness read by the job Plan 03's SUMMARY names) as recorded, not suppressed.
  </action>
  <verify>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or the vitest summary reports any failed test file or test</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, an eslint error, or a check-planning-hygiene finding</fails_when>
    <automated>node scripts/verify-plan-anchors.mjs --pending</automated>
    <fails_when>non-zero exit or any MISS line</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm test`, typecheck, lint and `verify-plan-anchors --pending` all exit 0; SUMMARY quotes each summary line.
  </acceptance_criteria>
  <done>The full vitest suite, typecheck, lint and anchor verification are green with all five plans in the tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public runbook → operator paste | runbook queries produce output that may be pasted into the public repo |
| runbook → live pg_cron schedule | the runbook is the only path by which the composite fan-out gets scheduled |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6-18 | I (Information disclosure) | runbook query | low | mitigate | the added query selects counts only, never the target list; one sentence states why; acceptance greps that `failed_targets` is not selected |
| T-164.6-20 | T (Tampering / integrity) | composite fan-out go-live | high | mitigate | named BLOCKING precondition `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` placed before any composite schedule can be registered from the runbook; OPEN founder item reported by the orchestrator for a phase |
| T-164.6-SC | Tampering | npm/pip installs | low | accept | no package installed |
</threat_model>

<verification>
- The runbook tracer check prints `OK runbook reads the migration keys and carries the precondition`.
- `npm test`, `npm run typecheck`, `npm run lint`, `node scripts/verify-plan-anchors.mjs --pending` green.
- Still EXPECTED red, recorded not suppressed: `baseline-content-drift` (2 fan-out findings until the founder's post-apply re-dump), and TEST applied-ness of the new migration until merge, read by the job Plan 03's SUMMARY names (`sql-tests` before Phase 164.4.2 merges; `test-db-drift`/VAC-08 after).
</verification>

<success_criteria>
- The runbooks tell the founder how to read a failing tick; the composite fan-out's activation carries the named precondition for the Python claim-time-snapshot residual; the whole phase's suite is green.
</success_criteria>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Symbol / content |
|---|---|---|
| `docs/runbooks/ledger-refresh-go-live.md` | modified | failure-row reading query; dormancy discrimination sentence; `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` blocking precondition section |
| `docs/runbooks/match-engine.md` | modified | one sentence on the counted row |

## Owed after this phase (outside executor reach, recorded)
- OPEN founder item: the Python `run_stitch_composite_job` live re-read that satisfies `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` needs a phase (orchestrator reports it).
- D-14: migration-reviewer + rls-policy-auditor + silent-failure-hunter before push, every finding fixed (orchestrator-dispatched).
- Post-merge: founder's credentialed `baseline.sql` re-dump clears `baseline-content-drift`.
- Ship-time (not this phase): VERSION/package.json/CHANGELOG per CLAUDE.md CHANGELOG discipline.

<assumption_delta_decision>
Detector `detected: true` from prose about dropped criteria; this plan edits documentation and runs gates. Decision: `no-change`.
</assumption_delta_decision>

<output>
Create `.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-05-SUMMARY.md` when done.
</output>
