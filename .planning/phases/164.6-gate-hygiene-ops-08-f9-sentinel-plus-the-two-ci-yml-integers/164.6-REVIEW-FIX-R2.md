---
phase: 164.6
round: 2
status: all_fixed
sources: [164.6-REVIEW-R2.md, 164.6-REVIEW-SFH-R2.md]
date: 2026-09-24
---

# 164.6 round-2 fix report

This round was split into two fixers, SQL and prober, working in separate worktrees; their branches were merged at `7d9ec9db7`. Two earlier fixer attempts stalled and committed nothing. The first one's uncommitted SQL edits were checked and kept.

## Commits

| Commit | Scope |
|---|---|
| `a87cdeeee` | WR-01 / N2 / N3 / migration MEDIUM: the all-fail RAISE is removed from both fan-out bodies, so the failure row always commits and the 20 h cooldown always engages. L3: only 40001 is a lost race. |
| `51d82f4c4` | RLS LOW: `cron_runs` refuses a failure-row INSERT from anon and from a non-admin user (arms ANON 2 / USER 2, each with a WITH CHECK-widening twin). |
| `25412fc74` | Census: `ARMS_FLOOR` 445 → 449, plus the registry, parser pins, GREEN_LOG and deliberate mismatches (still off by one). |
| `2ee0e28cb` | N1 / N2 / N4 / N5 / WR-02: cron-obs step (4) now judges by the success shape. It adds stuck and too-few-runs checks and a count-only failure-row read, and always runs. Its constants are bound to the manifest and the snapshots. New self-test fixtures bring the total to 91 scenarios. |
| `7a9694525` | L2: a retraction that fails after its 5 s budget is reported under a `_late` tag. |
| `a1080564e` | WR-03 / IN-01 / IN-05 / L1: the runbooks describe the committed failure rows, the four prober counts, the unwatched composite fan-out (now a blocking scheduling precondition), 40P01 as a failure, L1 as silent, and IN-01 as an assumption the first PROD prober run measures. |

## Findings

| Finding | Status |
|---|---|
| WR-01 / HIGH-2 / N2 / N3 / migration MEDIUM | Fixed (root fix; arm U inverted in both gates to prove the committed row and the next-tick cooldown) |
| WR-02 | Fixed (wiring test; each constant neutered, red observed) |
| WR-03 | Fixed (runbook, blocking precondition 6) |
| N1, N4, N5 | Fixed |
| L2, L3 | Fixed |
| RLS LOW | Fixed |
| IN-01 | Recorded as an assumption, measured on the first PROD prober run |
| IN-02, IN-03 | Fixed (same as N4 and N5) |
| IN-04 | Fixed (the composite W/deadlock sub-arm reads `lost_race_count = 1`) |
| IN-05 | Fixed |
| L1 | Recorded: a failed failure-row write on a tick that did enqueue is silent (two independent faults) |

## Gates, re-run by the orchestrator at `7d9ec9db7`

- `node scripts/prod-prober/run.mjs --self-test`: `SELF-TEST PASSED: 91/91 scenarios`
- vitest (prober wiring, keys/sync, ledger-refresh-marker, mutation-runner floors, gate-family-meta, audit-coverage): `Test Files 7 passed (7)`, `Tests 327 passed (327)`
- `node scripts/lint-sql-gates.mjs --self-test`: `OK: 7 rules, red+green each.`
- `tsx scripts/dump-sql-functions.ts --check`: `SQL function snapshot is current (121 functions).`
- eslint on the touched TS: exit 0
- Full mutation runner, run by the SQL fixer at `25412fc74` (the merge touched no SQL): `arms: 449/449/0`, `biting: 449`, `lane-blocked: 0`, `✅ No defects`, exit 0
