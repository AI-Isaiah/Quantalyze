---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 04
subsystem: ci-gates
tags: [mutation-runner, sql-gates, ci, floors, ratchet, vitest]

requires:
  - phase: 164.6-03
    provides: "Arm N in both ledger gates (fan-out 19 sentinel arms / 24 twins, composite 16 sentinel arms / 21 twins)"
provides:
  - "ARMS_FLOOR = 428 in scripts/mutation-runner/run.mjs, set from a full lane run, with a dated derivation comment"
  - "Every arm-count census pin re-derived: armsSeen 428, stepsSeen 443, needles 443, totalAnchored 428, GREEN_LOG ledger rows 21/24 and aggregates 428, gate-family-meta row ARMS_FLOOR=428"
  - "ci.yml sql-tests sentinel rows 16 / 19, SENTINEL_FLOOR 11, ARMS_FLOOR 215, and the drift-check literal mirror at 215"
affects: [164.6-05]

actuals:
  tokens: 8100
  tasks: 2
  commits: 2
plan_head_before: 98feb7720c981809007d24a96d6fcac26ceff40d

tech-stack:
  added: []
  patterns:
    - "Census pins moved by a substitution script that measures each needle at exactly one occurrence and refuses otherwise"

key-files:
  created: []
  modified:
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/gate-family-meta.test.ts
    - .github/workflows/ci.yml
    - src/__tests__/drift-check-scripts.test.ts

key-decisions:
  - "ARMS_FLOOR set to 428, the `biting:` count of a full run. The run that is quoted as the gate was taken with the constant already at 428, so it also shows the floor holding"
  - "The three deliberate mismatches in the floors test keep their gap of one: lane-invocations 429 against 428, biting 429 against executed 428, and rows summing to 427 against 428"
  - "Host lane lock taken by hand as the directory the 164.4.2 plans name under the git common dir, following the orchestrator note and Plan 03's precedent. The prompt supplied no literal path"

requirements-completed: []  # OPS-08-F2 is also declared by Plan 05; it is marked complete when the last declaring plan lands

coverage:
  - id: D1
    description: "ARMS_FLOOR equals the measured full-run biting count (428); FILES_FLOOR 49 and WAIVED_CEILING 0 unchanged"
    requirement: OPS-08-F2
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs (full lane run: arms 428/428/0, biting 428, lane-invocations 428, No defects, exit 0)"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts (99 passed; RED at ARMS_FLOOR=426, GREEN at 428)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Parser census, totalAnchored, GREEN_LOG and gate-family-meta pins re-derived from measurement"
    requirement: OPS-08-F2
    verification:
      - kind: unit
        ref: "npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts (228 passed)"
        status: pass
    human_judgment: false
  - id: D3
    description: "ci.yml sentinel table 16 / 19, SENTINEL_FLOOR 11, ARMS_FLOOR 215; the drift-check literal mirror moved with it"
    requirement: OPS-08-F2
    verification:
      - kind: unit
        ref: "npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts src/__tests__/drift-check-scripts.test.ts (673 passed)"
        status: pass
      - kind: other
        ref: "npm run typecheck (exit 0)"
        status: pass
    human_judgment: false

duration: ~26min
completed: 2026-09-24
status: complete
---

# Phase 164.6 Plan 04: OPS-08-F2 floors and pins Summary

**Every pin that Plan 03's two arm N additions moved now reads its measured value. `ARMS_FLOOR` is 428 from a full lane run with no defects. The parser, floors and registry censuses read 428 arms and 443 steps and needles. The ci.yml sentinel table reads 16 and 19, and its `ARMS_FLOOR` reads 215.**

## Performance

- **Duration:** about 26 min
- **Started:** 2026-09-24T05:02Z
- **Completed:** 2026-09-24T05:28Z
- **Tasks:** 2, both committed
- **Files modified:** 6

## Accomplishments

- **Full mutation run (the gate).** `node scripts/mutation-runner/run.mjs` was run under the host lane lock with `ARMS_FLOOR` already at 428. It exited 0. The lines it printed:
  - `scope: FULL 49/49 annotated files`
  - `coverage: files 49/76`
  - `arms: 428/428/0   (executed/annotated/waived)`
  - `biting: 428`
  - `lane-invocations: 428 … plus 49 baseline / 49 restore leg(s)` (the two tallies agree)
  - `lane-blocked: 0 file(s)`, `lane-probe: pg_cron AVAILABLE`, `unreachable: 27 file(s)`, `pending: 0`
  - `per-arm lane time: mean 1.1s over 428 arm run(s)`
  - `✅ No defects. Every annotated arm bit its own arm first.`
  - Per-file: `test_ledger_refresh_composite_arm.sql: sections 21 / judged 21 / annotated 21 / waived 0 / biting 21` and `test_ledger_refresh_fanout.sql: sections 24 / judged 24 / annotated 24 / waived 0 / biting 24`. Both arms N scored `RED (identity ok)`.
- **Static census.** `--parse-only` printed `coverage: files 49/76`, `arms: 0/428/0` and `✅ No static defects`.
- **`scripts/mutation-runner/run.mjs`.** `ARMS_FLOOR` moved 426 → 428, with a dated derivation comment in the house style. `FILES_FLOOR` stays 49 and `WAIVED_CEILING` stays 0.
- **`src/__tests__/mutation-annotation-parser.test.ts`.** Each value was measured by its own run:
  - `armsSeen` 426 → 428 (`expected 428 to be 426`)
  - `stepsSeen` 441 → 443 (`expected 443 to be 441`, re-run after `armsSeen` moved)
  - `needles.length` 441 → 443
  - Each arm N carries one `edit` step whose `find` is the failure-count increment, so the walk sees it. The earlier `sql`-step arms were not seen.
- **`src/__tests__/mutation-runner-floors.test.ts`.**
  - `totalAnchored` 428.
  - GREEN_LOG ledger rows 20 → 21 and 23 → 24. No row was added and the per-file leg counts (49) did not move.
  - Aggregates are 428 across the `arms:`, `biting:`, `lane-invocations:` and `per-arm lane time` lines and the `arm lane(s) spawned` assertion.
  - Calibrations: row sum 427 against 428; severed `executed=428 lane-invocations=0 biting=428`; extra lane 429 against 428; garbled and waived needles at 428; the zero arm `0/428/0`; spliced `biting (429) exceeds executed (428)`; FULL `biting arms 428 >= 428`.
  - All 20 substitutions went through one script. It measured each needle at exactly 1 occurrence before replacing and would have refused otherwise.
  - A dated `⭐ CURRENCY 2026-09-24` note sits beside each moved literal.
- **`src/__tests__/gate-family-meta.test.ts`.** The row key is now `scripts/mutation-runner/run.mjs :: ARMS_FLOOR=428`. A `MEASURED + 2026-09-24` entry was prepended and the whole previous row follows it as `Prior:`. `grep -c 'ARMS_FLOOR=426"'` = 0.
- **`.github/workflows/ci.yml`** (sql-tests step only):
  - The two rows now read `test_ledger_refresh_composite_arm.sql 16` and `test_ledger_refresh_fanout.sql 19`, measured with `grep -m1 -o 'ALL [0-9]* ARMS EXECUTED'` on each gate.
  - A dated one-line note sits above the block.
  - `SENTINEL_FLOOR=11` is unchanged.
  - `ARMS_FLOOR=215     # 13 + 9 + 6 + 16 + 19 + 8 + 7 + 4 + 103 + 7 + 23`
  - The diff touches only the note, the two rows and the `ARMS_FLOOR` line. No `timeout-minutes` changed.
- **`src/__tests__/drift-check-scripts.test.ts`.** The title and the `grep -ac` argv moved to `ARMS_FLOOR=215`, with a dated note.

## Task Commits

1. **Task 1 (tracer): full measurement drives ARMS_FLOOR and every arm-count pin.** Commit `72f76d68d` (test). Tracer gate: `--parse-only` and the three vitest files were re-run green after the commit. ⚡ Tracer verified end-to-end — expanding.
2. **Task 2: ci.yml sentinel table and floors, and the drift-check literal pin.** Commit `d2403b824` (ci).

## Anti-vacuity (stale-low direction)

1. `run.mjs` was copied byte for byte to a scratch backup.
2. `ARMS_FLOOR` was set back to 426 and the floors vitest was run. It failed 2 tests:
   - `equals the number of NON-WAIVED twins`: `The corpus declares 428 twin(s) of which 0 are waivers, so a green run bites 428. ARMS_FLOOR is 426. … expected 426 to be 428`
   - `GREEN: a FULL log passes …`: `expected '…' to contain 'biting arms 428 >= 428'`
3. The file was restored with `cp`. `cmp` printed nothing, and the re-run showed 99 passed.

## Files Created/Modified

- `scripts/mutation-runner/run.mjs`: `ARMS_FLOOR` 428 and its lineage comment.
- `src/__tests__/mutation-runner-floors.test.ts`: `totalAnchored`, GREEN_LOG, calibrations.
- `src/__tests__/mutation-annotation-parser.test.ts`: `armsSeen`, `stepsSeen`, `needles.length`.
- `src/__tests__/gate-family-meta.test.ts`: `ARMS_FLOOR=428` registry row.
- `.github/workflows/ci.yml`: the sentinel rows, the note and `ARMS_FLOOR=215`.
- `src/__tests__/drift-check-scripts.test.ts`: the literal mirror at 215.

## Decisions Made

- The floor's value comes only from the runner's `biting:` line (428). The static `arms: 0/428/0` agreed but was not the source.
- No floor was lowered and no waiver or allowlist entry was added. `WAIVED_CEILING` stays 0 and no timeout moved.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] The first full run reported `dirty-checkout` because I edited files while it ran**
- **Found during:** Task 1, step 2.
- **Issue:** While the first full run was in progress I edited four pin files: the parser test, the floors test, ci.yml and drift-check. The runner compares `git status --porcelain` before and after, so it recorded `❌ 1 defect(s): dirty-checkout` and exited 1. Every measured line in that run already agreed with the final values: `arms: 428/428/0`, `biting: 428`, `lane-invocations: 428`, both ledger rows 21/24, and both arms N `RED (identity ok)`. The only defect was the one I caused.
- **Fix:** I set `ARMS_FLOOR` to 428 first. Then I took the host lock again and ran the full corpus a second time without editing anything during the run. That run exited 0 with `✅ No defects`, and it is the one quoted above. I added the "No defects, exit 0" line to the `run.mjs` comment after that run finished.
- **Files modified:** none beyond the plan's list.
- **Commit:** `72f76d68d`.

**2. [Rule 3 - Blocking] The prompt supplied no literal `<HOST_LOCK_DIR>`**
- **Found during:** Task 1 precondition.
- **Issue:** The plan says to halt if the prompt does not supply the path. The orchestrator note instead said to take the host lock by hand under the git common dir.
- **Fix:** I followed the orchestrator note and Plan 03's precedent. The lock was the directory name the 164.4.2 plans already use for this purpose under the git common dir. It was taken with `mkdir` and released with `rmdir`, each as its own command, around each full run, and it was released after both runs. The path is not written into any `.planning/` artifact.

---

**Total deviations:** 2 (both blocking). **Impact:** none on the delivered values. The first one cost one extra full run.

## Issues Encountered

- The floor's anti-vacuity failure is not literally worded `RATCHET STALE`. It is the `equals the number of NON-WAIVED twins` assertion, which is the same stale-low check under this file's current wording.

## Expected-red gates (by construction, recorded, never suppressed)

- **`baseline-content-drift`:** 2 findings, both on the fan-out functions. They clear only when the founder re-dumps `baseline.sql` from PROD after the apply. Plan 03 measured this; this plan did not touch it.
- **TEST applied-ness in `test-db-drift` (VAC-08):** red until apply-on-merge. At this base, `sql-tests` replays the new migration on its private lane (164.4.2 DECISION F).

## Known Stubs

None.

## Threat Flags

None. Floors only rose to measured values, which is the T-164.6-17 mitigation. Lineage entries were prepended and the `Prior:` chain was kept, which is the T-164.6-19 mitigation.

## User Setup Required

None.

## Next Phase Readiness

- Plan 05 is ready to run. It covers the runbook changes that read the failure row, the composite-schedule BLOCKING precondition and the phase-level full-suite pass.
- The D-14 three reviewers are still owed before push.

---
*Phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers*
*Completed: 2026-09-24*

## Self-Check: PASSED

- FOUND: every modified file listed under key-files
- FOUND: commits 72f76d68d, d2403b824
- Re-run at close: the five vitest files (901 passed), `--parse-only` (no static defects), `npm run typecheck` (exit 0)
