---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 05
subsystem: docs
tags: [runbook, cron_runs, pg_cron, ledger-refresh, composite, vitest]

requires:
  - phase: 164.6-03
    provides: "migration 20260924120000: the candidate_enqueue_failed cron_runs row with failed_count / enqueued_count / failed_targets"
  - phase: 164.6-02
    provides: "the TS retraction at keys/sync and finalize-wizard, and the recorded residual [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]"
  - phase: 164.6-04
    provides: "every floor, census and sentinel pin at its measured value, so the full suite can be read green"
provides:
  - "ledger-refresh-go-live runbook: counts-only reading of the failure row, the return-value and dormancy discrimination sentences"
  - "ledger-refresh-go-live runbook: BLOCKING precondition [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT] on scheduling the composite fan-out, owner Phase 164.6.7 COMPOSITECLAIMSNAPSHOT"
  - "match-engine runbook: the deliberate exercise names the counted failure row"
  - "phase-level suite reading with all five plans in the tree"
affects: [164.6 review fix round, 164.6.7 COMPOSITECLAIMSNAPSHOT, ledger-refresh go-live]

actuals:
  tokens: 2026
  tasks: 2
  commits: 1
plan_head_before: 06aa57c5aeedeaa86c4b40838be01c2801866546

tech-stack:
  added: []
  patterns:
    - "A runbook describes current behaviour a pending fix will change, and marks each such point \"changes in the 164.6 review fix\" instead of guessing the fixed behaviour"

key-files:
  created: []
  modified:
    - docs/runbooks/ledger-refresh-go-live.md
    - docs/runbooks/match-engine.md

key-decisions:
  - "The failure-row query sits as a sub-subsection under 'Did the tick itself run, and how much did it enqueue?', so it is read beside the tick checks it complements"
  - "The composite precondition names Phase 164.6.7 COMPOSITECLAIMSNAPSHOT as owner, and cites it as inserted on main by PR #849: this feature branch's ROADMAP does not carry 164.6.7 yet (measured: 0 hits here, heading present on origin/main)"
  - "Three behaviours the review fix is expected to change (no alert, poisoned candidate re-selected every tick, 40001 counted as a failure) are stated as current and marked, not pre-described"
  - "The deribit-composite bullet points to the precondition by its bracketed id in plain text, not by a markdown anchor, because a slug generated from an emoji heading is fragile"

patterns-established: []

requirements-completed: [OPS-08-F2, 161.1-D13]

coverage:
  - id: D1
    description: "The go-live runbook reads the failure row by count only (never the target list), says the return value still means jobs INSERTED, and says a dormancy reading must filter on error or metadata cause"
    requirement: OPS-08-F2
    verification:
      - kind: other
        ref: "Task 1 tracer node check (prints OK runbook reads the migration keys and carries the precondition); grep -c \"metadata->'failed_targets'\" docs/runbooks/ledger-refresh-go-live.md = 0"
        status: pass
    human_judgment: false
  - id: D2
    description: "Named BLOCKING precondition [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT] before 'What this runbook deliberately does not cover', naming run_stitch_composite_job and _refresh_marker_still_on_row, with the deribit composite bullet pointing to it"
    requirement: 161.1-D13
    verification:
      - kind: other
        ref: "Task 1 tracer node check (precondition index < 'does not cover' index); grep -c '164.6-COMPOSITE-CLAIMTIME-SNAPSHOT' = 2; git diff --stat -- analytics-service/ empty"
        status: pass
    human_judgment: false
  - id: D3
    description: "match-engine runbook: a failed candidate also leaves one counted cron_runs row beside the unchanged WARNING"
    requirement: OPS-08-F2
    verification:
      - kind: other
        ref: "grep -c 'candidate_enqueue_failed' docs/runbooks/match-engine.md = 1"
        status: pass
    human_judgment: false
  - id: D4
    description: "Whether the runbook text reads correctly to an operator and stays true after the review fix round"
    requirement: OPS-08-F2
    verification: []
    human_judgment: true
    rationale: "Prose accuracy against a fix that has not been written yet is a reviewer's judgment; no test asserts runbook wording"
  - id: D5
    description: "Phase-level suite with all five plans in the tree: vitest, typecheck, lint, pending plan anchors"
    verification:
      - kind: unit
        ref: "npm test (3 shell-spawning files timed out under load 32, each green when re-run alone, see Issues Encountered)"
        status: pass
      - kind: other
        ref: "npm run typecheck; npm run lint; node scripts/verify-plan-anchors.mjs --pending (all exit 0)"
        status: pass
    human_judgment: false

duration: ~9 min
completed: 2026-09-24
status: complete
---

# Phase 164.6 Plan 05: runbooks for the fan-out failure row and the composite-schedule block Summary

**The go-live runbook now reads Plan 03's `candidate_enqueue_failed` row with a counts-only query and tells a "dormant?" reader to filter on `error`. It also carries a BLOCKING precondition, `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]`, that forbids scheduling the composite fan-out until `run_stitch_composite_job` re-reads the live row. That work is owned by Phase 164.6.7. The match-engine runbook names the counted row, and the phase-level suite is green with all five plans in the tree.**

## Performance

- **Duration:** about 9 min
- **Started:** 2026-09-24T05:31:22Z
- **Completed:** 2026-09-24T05:40:10Z
- **Tasks:** 2 (Task 1 committed; Task 2 ran gates only and changed no file)
- **Files modified:** 2

## Accomplishments

- **`docs/runbooks/ledger-refresh-go-live.md`, new `#### Did a candidate fail to enqueue this tick? (Phase 164.6, OPS-08-F2)`** under `### Did the tick itself run, and how much did it enqueue?`:
  - The admin-SQL-editor query selects `completed_at`, `error`, `metadata->>'function'`, `failed_count` and `enqueued_count` from `public.cron_runs` where `cron_name = 'ledger_refresh_fanout' AND error = 'candidate_enqueue_failed'`, newest first, limit 5. It does not select `failed_targets`, and a sentence says why: runbook output gets pasted into this public repository.
  - It says the function's return value still means jobs INSERTED (D-10), and that the row, not the return value, is the failure signal.
  - It says a row under `ledger_refresh_fanout` is no longer proof of dormancy, so any "is it dormant?" reading must filter on `error` or `metadata->>'cause'`.
  - Three current behaviours are stated and each is marked "changes in the 164.6 review fix": nothing alerts on the row; a poisoned candidate leaves no `compute_jobs` row, so the attempt cooldown misses it and it is selected every tick; a lost `40001` race is counted in `failed_count`. All three were measured against the migration body (the cooldown predicate reads `compute_jobs`, and the per-candidate handler is `EXCEPTION WHEN OTHERS`).
- **New section `## ⛔ Before the COMPOSITE fan-out is ever scheduled — BLOCKING precondition [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]`**, placed immediately before `## What this runbook deliberately does not cover`. It says:
  - (a) `public.enqueue_ledger_composite_refresh()` is dormant and has no schedule;
  - (b) no schedule may be registered until `run_stitch_composite_job` re-reads the live `metadata->>'source'` before it honours the `ledger-refresh-composite` marker, as the single-key sites do via `_refresh_marker_still_on_row`;
  - (c) why: a retraction that lands after the claim does not stand down the claim-time-snapshot guard;
  - (d) how to check it is met: a live re-read before the comparison, plus a test that goes RED without it;
  - (e) the owner is Phase 164.6.7 COMPOSITECLAIMSNAPSHOT, the block holds until that phase ships, and TODOS `161.1-D13` already requires this.
  The `**The deribit composite.**` bullet points to it by id. No strategy id and no job id appear.
- **`docs/runbooks/match-engine.md`, `**The deliberate exercise.**`:** one sentence says a failed candidate also leaves one counted `cron_runs` row for its tick beside the unchanged `WARNING`, and points to the go-live query.

## Task Commits

1. **Task 1 (tracer): runbooks read the failure row and block the composite schedule.** Commit `1c8e3da84` (docs). Tracer gate: the tracer `<verify>` was re-run green before Task 2. ⚡ Tracer verified end-to-end — expanding.
2. **Task 2: phase-level full-suite pass.** No commit. It ran commands only and no file changed.

## Task 2 readings (all five plans in the tree, HEAD `1c8e3da84`)

| Command | Summary line | Exit |
|---|---|---|
| `npm test` (full suite, run 1) | `Test Files  3 failed \| 866 passed \| 19 skipped (888)` / `Tests  3 failed \| 15382 passed \| 280 skipped (15665)` | 1 |
| `npm run typecheck` | `tsc --noEmit`, no `error TS` line | 0 |
| `npm run lint` | `[check-admin-route-manifest] OK — 20 admin routes`, `[check-route-contract] OK — 58 page routes`, `[check-planning-hygiene] OK — 6935 tracked files scanned` | 0 |
| `node scripts/verify-plan-anchors.mjs --pending` | `claims: 14 checked` / `OK: 4 plan file(s), no stale claims.` | 0 |

All three full-suite failures were `Error: Test timed out in 5000ms` in files that spawn shells. Load average was 32 at the first re-run. Each file was re-run alone and both readings are recorded here. No timeout was raised.

| File (test) | Full run | Alone |
|---|---|---|
| `src/__tests__/test-restore-workflow-wiring.test.ts` (B4, F-R2-02 undecidable predicate) | timed out | `Tests  117 passed (117)` |
| `src/__tests__/contracts/analytics-deploy-tree-compare.contract.test.ts` (every path exits 0) | timed out | `Tests  9 passed (9)` |
| `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` (non-loopback DB_URL refusal) | timed out | 1st alone at load 32: `1 failed \| 32 passed (33)`; 2nd alone at load 17: `Tests  33 passed (33)` |

**Still EXPECTED red, recorded and not suppressed:**
- `baseline-content-drift` keeps its 2 fan-out findings until the founder's post-apply re-dump.
- The new migration's applied-ness on TEST stays red until merge. `test-db-drift` / VAC-08 reads it now that Phase 164.4.2 has merged.
- Neither can run locally: no remote was touched.

## Files Created/Modified

- `docs/runbooks/ledger-refresh-go-live.md` — failure-row query, dormancy discrimination, composite-schedule BLOCKING precondition, pointer from the deribit composite bullet
- `docs/runbooks/match-engine.md` — one sentence on the counted failure row

## Decisions Made

See `key-decisions` in the frontmatter. The only one of substance: Phase 164.6.7 is cited as "inserted into the ROADMAP on `main` by PR #849". It is on `origin/main`, but this branch's ROADMAP does not carry it yet, so "in the ROADMAP" alone would have been false in this tree.

## Deviations from Plan

None. The plan ran as written. The three marked current-behaviour points follow the orchestrator's note about the review fix round, sit inside step 1's subsection, and add no scope.

## Issues Encountered

- The three full-suite timeouts above. They came from concurrent host load (load average 32), and each file is green alone. The anti-skip contract file needed a second solo run because the first ran while load was still at 32.
- The eslint cache in `npm run lint` writes under `node_modules/.cache`, which the prescribed symlink points into the main checkout. It is a cache only. The symlink was removed before the commit.

## Known Stubs

None.

## Threat Flags

None. T-164.6-18 is mitigated: the query selects counts only (`grep -c "metadata->'failed_targets'"` = 0). T-164.6-20 is mitigated by the named BLOCKING precondition.

## User Setup Required

None.

## Next Phase Readiness

- All five plans of Phase 164.6 have SUMMARYs. Next come the 164.6 review fix round for the plan 03 migration findings, then verification.
- If the fix round adds an alert, a cooldown or separate 40001 counting, it must update the three bullets marked "changes in the 164.6 review fix" in `docs/runbooks/ledger-refresh-go-live.md`.
- `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` stays BLOCKING until Phase 164.6.7 COMPOSITECLAIMSNAPSHOT ships.

## Self-Check: PASSED

- FOUND: docs/runbooks/ledger-refresh-go-live.md, docs/runbooks/match-engine.md
- FOUND: commit 1c8e3da84
- Acceptance re-run: tracer check OK; `candidate_enqueue_failed` go-live 3 / match-engine 1; `failed_targets` selected 0; precondition id 2; `analytics-service/` diff empty.

---
*Phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers*
*Completed: 2026-09-24*
