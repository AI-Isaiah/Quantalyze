---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
  - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
  - supabase/schema/functions/enqueue_ledger_composite_refresh.sql
  - analytics-service/tests/test_ledger_refresh_gates.py
  - supabase/tests/test_ledger_refresh_fanout.sql
  - supabase/tests/test_ledger_refresh_composite_arm.sql
autonomous: true
requirements:
  - OPS-08-F2

estimate:
  tokens: 170000
  raw_tokens: 170000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "One new forward migration re-bases BOTH public.enqueue_ledger_refresh_for_strategies() and public.enqueue_ledger_composite_refresh() on their committed snapshots with CREATE OR REPLACE; signature, SECURITY DEFINER, search_path, owner-only EXECUTE and RETURNS INTEGER are unchanged (D-09)"
    - "Each fan-out's per-candidate EXCEPTION WHEN OTHERS handler does assignments only (v_failed increment + append {strategy_id, sqlstate} to v_failed_targets) and keeps its RAISE WARNING line byte-identical (D-11)"
    - "When v_failed > 0 after the loop, exactly ONE public.cron_runs row is written AFTER the success-path advisory unlock: cron_name 'ledger_refresh_fanout', status 'error', error 'candidate_enqueue_failed', metadata {function, cause, failed_count, enqueued_count, failed_targets}; the write is best-effort wrapped and the tick is never rolled back to report a failure (D-10, D-11)"
    - "The return value still means jobs actually INSERTED this tick; the final RAISE NOTICE names the failed count beside it and names no strategy id (D-10, D-12)"
    - "On the pg-lane, gate arm N in test_ledger_refresh_fanout.sql forces ONE candidate's enqueue to raise and asserts: return = 1, the healthy sibling has a job, the poisoned one has none, and exactly one 'candidate_enqueue_failed' row for this function whose failed_targets contains the poisoned id and not the healthy id (D-13)"
    - "The same proof holds for the composite arm in test_ledger_refresh_composite_arm.sql arm N (D-13)"
    - "Boundary edge (OPS-08-F2): a tick with zero failures writes no 'candidate_enqueue_failed' row — arm N re-calls the fan-out after dropping its poison trigger and asserts no new row; exactly one failure gives failed_count = 1; failed_targets is bounded by the per-tick LIMIT (4 single-key, 2 composite)"
    - "Precision edge (OPS-08-F2): in the failure row, (metadata->>'failed_count')::int = jsonb_array_length(metadata->'failed_targets') and (metadata->>'enqueued_count')::int equals the tick's return value; both are exact integers, no rounding"
    - "Every existing edit twin on the superseded bodies is re-pointed to the new migration in the same commit as its apply-list entry, and both gates' narrowed mutation runs print 'No defects in the narrowed scope.' (D-13)"
    - "The Python static gate registers the new file as the live definer of both fan-outs, adds the superseded files, and adds a one-second window 5; window 4 is not widened"
    - "Each new guard is observed to bite on the lane from a neutered scratch copy while the repo migration stays byte-identical (cmp): the $verify$ ordering check refuses a body whose failure block precedes the last advisory unlock, the failure-instrument needle refuses a body missing the failed_targets key, and arm 0 fails first with Cause (v) when the new migration is left out of the apply list"
  artifacts:
    - path: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
      provides: "forward migration: both fan-out bodies with the failure instrument, catalogue-only $verify$, two prod-body-ack lines"
      contains: "candidate_enqueue_failed"
    - path: supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
      provides: "regenerated snapshot naming the new source migration"
    - path: supabase/schema/functions/enqueue_ledger_composite_refresh.sql
      provides: "regenerated snapshot naming the new source migration"
    - path: supabase/tests/test_ledger_refresh_fanout.sql
      provides: "arm N (poisoned + healthy), cause (v) applied-ness probe, re-pointed twins, sentinel 19"
      contains: "ALL 19 ARMS EXECUTED"
    - path: supabase/tests/test_ledger_refresh_composite_arm.sql
      provides: "arm N (poisoned + healthy composite), cause (v), re-pointed twins, sentinel 16"
      contains: "ALL 16 ARMS EXECUTED"
    - path: analytics-service/tests/test_ledger_refresh_gates.py
      provides: "_FANOUT_MIGRATION_NAME/_COMPOSITE_MIGRATION_NAME moved, superseded sets extended, _PHASE_MIGRATION_WINDOW_5_START/_END"
      contains: "_PHASE_MIGRATION_WINDOW_5_START"
  key_links:
    - from: supabase/tests/test_ledger_refresh_fanout.sql
      to: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
      via: "RED-UNDER-SETUP apply list (last entry) + every RED-UNDER-M edit step's file"
      pattern: "20260924120000_ledger_fanout_failure_count\\.sql"
    - from: supabase/tests/test_ledger_refresh_composite_arm.sql
      to: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
      via: "RED-UNDER-SETUP apply list (last entry) + every RED-UNDER-M edit step's file"
      pattern: "20260924120000_ledger_fanout_failure_count\\.sql"
    - from: analytics-service/tests/test_ledger_refresh_gates.py
      to: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
      via: "_FANOUT_MIGRATION_NAME and _COMPOSITE_MIGRATION_NAME"
      pattern: "20260924120000_ledger_fanout_failure_count\\.sql"
    - from: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
      to: public.cron_runs
      via: "post-unlock best-effort INSERT under cron_name ledger_refresh_fanout, error candidate_enqueue_failed"
      pattern: "candidate_enqueue_failed"
  prohibitions:
    - requirement_id: OPS-08-F2
      category: safety
      status: resolved
      verification: judgment
      statement: "Reporting a failed candidate must never roll back the enqueues that succeeded in the same tick, and must never leave the fan-out's advisory lock held."
    - requirement_id: OPS-08-F2
      category: privacy
      status: resolved
      verification: judgment
      statement: "A failed candidate's strategy id must never appear in RAISE / NOTICE / WARNING text; it is recorded only in cron_runs.metadata, which RLS limits to platform admins and service_role."
---

<objective>
Close requirement OPS-08-F2 (ROADMAP Phase 164.6 success criterion 3): the two pg_cron fan-out functions stop reporting a clean tick over a swallowed per-candidate enqueue failure. The failed target id is recorded and a non-zero failure count is surfaced from the tick, beside the unchanged "jobs inserted" return value, proven on the private pg-lane with one target forced to raise.

Scope (D-01): the founder's 2026-09-17 SCOPE PRUNE reduced Phase 164.6 to criteria 2, 3 and 4; only OPS-08-TS, OPS-08-F2 and 161.1-D13 are in scope (the ROADMAP's longer requirements string lists TODOS ids of DROPPED criteria). This plan owns OPS-08-F2. The mutation-runner floor and census pins that the two new arms move are Plan 04's.

⛔ SCHEMA-GATE OVERRIDE (orchestrator decision, recorded here): the generic schema-gate contribution would inject a `[BLOCKING]` remote schema-push task. That is FORBIDDEN in this repo: this checkout's Supabase CLI is linked to PRODUCTION, and merging `supabase/migrations/**` auto-applies to shared TEST and then to PROD via `supabase-migrate.yml`. It is replaced by Task 3, a `[BLOCKING]` task that applies the migration on the private throwaway pg-lane (`scripts/pg-lane/run.sh`, 127.0.0.1 only) and runs both ledger gates and the mutation runner against it. No Supabase CLI, psql, or any remote-database command appears anywhere in this plan.

Purpose: a per-candidate failure in a production cron tick becomes a counted, readable row instead of a WARNING nobody keeps.
Output: one forward migration, two regenerated snapshots, one Python gate registration, one new arm per ledger gate with all superseded twins re-pointed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-CONTEXT.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-RESEARCH.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-PATTERNS.md
@supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
@supabase/schema/functions/enqueue_ledger_composite_refresh.sql
@supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql
@supabase/migrations/20260917120000_ledger_fanout_admit_private.sql
@scripts/mutation-runner/GRAMMAR.md

Discretion choices fixed by this plan (CONTEXT "Claude's Discretion"):
- Migration file: `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql`. The stamp must be strictly newer than every migration at the executor's base (it is `20260922120000` at planning time). If it is not, choose a newer stamp and use it consistently in every file below (apply lists, twin `file` fields, Python constants, window 5).
- Cause string: `candidate_enqueue_failed` (distinct from the dormancy causes `flag_read_failed` and `flag_row_invisible_or_absent`).
- Locals: `v_failed INTEGER := 0;` and `v_failed_targets JSONB := '[]'::jsonb;`.
- Arm id `N` in both gates (unused in both at planning time; confirm with the arm-id census). Fixture names `lrf N1` (poisoned) / `lrf N2` (healthy) and `lrc N1` / `lrc N2`. Poison functions `pg_temp.lrf_poison_n()` / `pg_temp.lrc_poison_n()`, triggers `lrf_poison_n_trg` / `lrc_poison_n_trg` (arm R's `pg_temp.lrf_poison()` is created without OR REPLACE and never dropped, so a new name is required).

Executor constraints (this repo, this run):
- ⛔ Phase 164.4.2 is NOT merged into this base. Every count (`occurrences`, `nth`, twin totals, sentinel roster) is MEASURED with `grep -c -F` / `grep -o` on the final bytes at your base — never taken from RESEARCH or from this plan.
- Two absolute paths are SUPPLIED IN YOUR EXECUTOR PROMPT and written here only as placeholder tokens: `<HOST_LOCK_DIR>` (the host-wide lane lock directory) and `<MAIN_VENV_PYTHON>` (the main checkout's `analytics-service` venv interpreter; this worktree has no venv). Substitute the literal absolute path the prompt gives you. ⛔ Never derive either with command substitution or from a `.git` path, and never write the substituted path into any `.planning/` artifact. If the prompt does not supply one of them, HALT and report it; do not guess.
- Every pg-lane or mutation-runner run is done under the host lock: `mkdir <HOST_LOCK_DIR>` as its own single command before, `rmdir <HOST_LOCK_DIR>` as its own single command after (release it even when the run fails). If the `mkdir` fails, another lane run holds it: wait and retry, never delete it.
- One command per verification step. Lane scratch lives under your scratchpad (e.g. `p1646-03-lane`), is removed after use, and is never quoted into `.planning/`.
- Anchors BY SYMBOL only. Applied migrations (20260826150000, 20260911130000, 20260917120000) are history and are NOT edited.
- Never lower `FILES_FLOOR`/`ARMS_FLOOR`, never raise `WAIVED_CEILING` (0), never add a waiver, never widen `CONTENT_DRIFT_ALLOWLIST` or any allowlist. Never add `UPDATE strategy_analytics` to a gate (`test_sql_gate_scoped_updates.py` pins it) — seed with INSERT only.
- Python: pytest runs ONLY with `analytics-service` as the working directory (repo-root runs miss cassettes). The command is `cd analytics-service && <MAIN_VENV_PYTHON> -m pytest ...` with the prompt-supplied absolute interpreter path substituted: a plain `&&` chain of `cd` and an absolute path, with no command substitution and no `.git` path, so a wrong path exits 127 loudly instead of false-greening. Never start uvicorn.
- ⚠️ After this plan, `src/__tests__/mutation-annotation-parser.test.ts`, `src/__tests__/mutation-runner-floors.test.ts`, `src/__tests__/gate-family-meta.test.ts`, `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` and `src/__tests__/drift-check-scripts.test.ts` are EXPECTED RED on their arm/sentinel pins until Plan 04 moves them. Do not edit them here.
- Do NOT touch VERSION, the package.json version, CHANGELOG.md; do not stage `.planning/milestone.lock`, `.planning/state.json`, `.planning/phases/164.5.3-*`. No CI skip token in any commit message. No home path or username in any `.planning/` artifact.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — the migration, its snapshots and Python registration, proven on the lane through the single-key gate's new arm N</name>
  <reversibility rating="one-way">Merging supabase/migrations/** auto-applies to shared TEST and then PROD with no reviewer gate, and the single-key fan-out is LIVE on PROD (scheduled as ledger_refresh_fanout since Phase 164.5.1). The irreversible act is the MERGE, which is outside this phase's execution: the orchestrator stops before merge/ship, the three-reviewer pass (migration-reviewer + rls-policy-auditor + silent-failure-hunter, D-14) runs before push, and the founder performs the merge. Authoring the file on the branch is reversible until then, so by orchestrator decision no checkpoint:decision blocks this task.</reversibility>
  <files>supabase/migrations/20260924120000_ledger_fanout_failure_count.sql, supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql, supabase/schema/functions/enqueue_ledger_composite_refresh.sql, analytics-service/tests/test_ledger_refresh_gates.py, supabase/tests/test_ledger_refresh_fanout.sql</files>
  <read_first>
    - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql (body source, `$fanout$`) and supabase/schema/functions/enqueue_ledger_composite_refresh.sql (body source, `$composite$`)
    - supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql — two-body skeleton and the `DO $verify$` `FOREACH v_fn IN ARRAY ARRAY[...]` loop, its concatenated-needle DECLARE block, the comment strip `regexp_replace(v_def_raw, ...)`
    - supabase/migrations/20260917120000_ledger_fanout_admit_private.sql — header sections (OPS, FORWARD MIGRATION, VAC-04 ACKNOWLEDGEMENT, RE-BASE DISCIPLINE (DRIFT-02), DISJOINTNESS, NO GATE `find` STRING, BLAST RADIUS), its verify checks 8-10, the COMMENT ON FUNCTION and REVOKE tail
    - supabase/tests/test_ledger_refresh_fanout.sql — `RED-UNDER-SETUP`, every `RED-UNDER-M` line, arm 0 causes (iii)/(iv), arm R (the poison mechanism), arm M1 (the cron_runs count shape), the three `ALL 18 ARMS EXECUTED` mentions
    - analytics-service/tests/test_ledger_refresh_gates.py — `_FANOUT_MIGRATION_NAME`, `_COMPOSITE_MIGRATION_NAME`, `_FANOUT_SUPERSEDED_MIGRATION_NAMES`, `_COMPOSITE_SUPERSEDED_MIGRATION_NAMES`, `_PHASE_MIGRATION_WINDOW_4_START`, `_PHASE_MIGRATION_WINDOWS`
    - scripts/mutation-runner/GRAMMAR.md, scripts/lint-sql-gates.mjs (R1, R2), scripts/lint-app-guc.mjs, scripts/lint-migration-data-dependence.mjs, scripts/sql-body-normalize.mjs header
    - supabase/schema/baseline.sql — the `cron_runs` table, `cron_runs_status_check`, policies `cron_runs_admin_read` / `cron_runs_service_role` (for the D-12 header note)
  </read_first>
  <action>
    Precondition steps: run `node scripts/verify-plan-anchors.mjs` on this PLAN.md path and halt on non-zero; confirm the lane has pg_cron with `bash scripts/pg-lane/run.sh --print-pgbin` (non-empty dir on stdout).

    A. Migration (D-09, D-10, D-11, D-12). Create `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql`: header, `BEGIN;`, STEP 1 the single-key body, STEP 2 the composite body (this order keeps the composite gate's `nth: 2` convention), one `DO $verify$`, `COMMIT;`. Copy each body BYTE FOR BYTE from its committed snapshot (DRIFT-02 re-base discipline: from the snapshot, not migration text), then apply ONLY these edits, each enumerated in the header's RE-BASE DISCIPLINE section:
      1. DECLARE gains `v_failed` and `v_failed_targets` (see `<context>`).
      2. The per-candidate `EXCEPTION WHEN OTHERS` handler keeps its `RAISE WARNING ... one candidate failed to enqueue (SQLSTATE %); continuing` line byte-identical (arm R's twin and `docs/runbooks/match-engine.md` read it) and gains ASSIGNMENTS only: `v_failed := v_failed + 1;` and an append of `jsonb_build_array(jsonb_build_object('strategy_id', v_row.strategy_id, 'sqlstate', SQLSTATE))` to `v_failed_targets` (use the loop record's actual variable name from the snapshot). No DML in the handler (lint R1 shape).
      3. AFTER the success-path `PERFORM pg_advisory_unlock(hashtext(...))` (never inside the lock-holding outer block, RESEARCH Pitfall 8), an `IF v_failed > 0 THEN BEGIN INSERT INTO public.cron_runs (cron_name, status, completed_at, error, metadata) VALUES ('ledger_refresh_fanout', 'error', now(), 'candidate_enqueue_failed', jsonb_build_object('function', '<this function name>', 'cause', 'candidate_enqueue_failed', 'failed_count', v_failed, 'enqueued_count', v_enqueued, 'failed_targets', v_failed_targets)); EXCEPTION WHEN OTHERS THEN RAISE WARNING '<fn>: failure instrument write failed (SQLSTATE %)', SQLSTATE; END; END IF;` — copying the dormancy instrument's wrap. Both bodies use cron_name `ledger_refresh_fanout`, distinguished by `metadata.function`. The lock is therefore released on every path by construction, and a failed INSERT cannot roll back the good enqueues (D-10 rejected RAISE-at-tick-end for exactly that reason).
      4. The final `RAISE NOTICE` names `v_failed` beside `v_enqueued` — counts only, never an id (T-161.1-10 discipline, D-12). `RETURN v_enqueued` is unchanged (D-10).
      5. Re-issue `COMMENT ON FUNCTION` for both with one added sentence about the failure row, and re-issue `REVOKE ALL ON FUNCTION public.<fn>() FROM PUBLIC, anon, authenticated, service_role;` for both with the "RE-ISSUED rather than trusted to survive CREATE OR REPLACE" note. Both definitions are spelled exactly `CREATE OR REPLACE FUNCTION public.<name>()` with the `$fanout$` / `$composite$` tags (the Python extractors find bodies by tag).
    The `DO $verify$` block loops both names (20260911130000's `FOREACH` shape) and reads the CATALOGUE ONLY (no application-table read, no row count — `lint-migration-data-dependence.mjs`). Carry forward checks: one overload with `pronargs = 0`; `prosecdef` and the whitespace-stripped search_path VALUE; owner `rolsuper OR rolbypassrls`; comment-stripped length >= 500; the concatenated needles for the activation read, the NULL-safe guard, the dormancy instrument INSERT, the row-count read, the retired GUC absent, the lifecycle conjunct, `'draft'`/`'archived'` absent in executable text (grep the stripped text first); EXECUTE grantee set == owner via aclexplode over `COALESCE(proacl, acldefault('f', proowner))`. ADD: the failure-instrument needle, concatenated so the literal never appears whole in the verify text (e.g. the quoted `failed_targets` key built from two pieces); and an ordering check that the failure-instrument block sits after the LAST `pg_advisory_unlock` in the stripped body. DROP 20260911130000's TRUNCATE post-condition (check 10). Every verify needle must be one NO twin `find` string touches (Pitfall 6).
    Header (copy 20260917120000's section headings): OPS paragraph stating the file applies to shared TEST first and then AUTO-APPLIES to PROD with no reviewer gate once TEST succeeds — do NOT copy 20260917120000's sentence about a human reviewer gate; "IT IS A FORWARD MIGRATION"; VAC-04 ACKNOWLEDGEMENT with TWO `-- prod-body-ack: <sha256>` lines, one per function, each the `live` column of `node scripts/sql-body-normalize.mjs --diff-bodies <the regenerated HEAD snapshot> <origin/main's copy of the same file written into your scratch dir via git show>` — NEVER `--hash` of a file (RESEARCH measured e7e99017...ce30c9 and d1ca6102...b76152 at origin/main 9fd60115; re-measure and ack what you measure); RE-BASE DISCIPLINE (the five edits above); DISJOINTNESS (verify needles vs twin finds); "NO GATE `find` STRING IS REPRODUCED ANYWHERE IN THIS FILE" (the handler's new increment statement and every re-pointed twin needle must occur only in executable body text, never in comments); catalogue reads only; BLAST RADIUS; and a D-12 note quoting the `cron_runs` RLS facts from baseline.sql (RLS on, no FORCE; admin SELECT policy; service_role policy; anon/authenticated hold grants but no admitting policy so read zero rows; FORCE RLS would silence both instruments) so the rls-policy-auditor can confirm who reads `failed_targets`. Forbidden tokens (see acceptance criteria) must not appear anywhere in the file, comments included.

    B. Snapshots (D-14): run `npm run schema:functions`; confirm both files' `-- source migration:` lines now name the new file; never hand-edit them.

    C. Python registration (same commit): in `analytics-service/tests/test_ledger_refresh_gates.py` move `_FANOUT_MIGRATION_NAME` and `_COMPOSITE_MIGRATION_NAME` to the new file with a dated `⛔ MOVED ...` comment in the existing style; add `20260917120000_ledger_fanout_admit_private.sql` to `_FANOUT_SUPERSEDED_MIGRATION_NAMES` and `20260911130000_ledger_fanout_grantees_and_dormancy.sql` to `_COMPOSITE_SUPERSEDED_MIGRATION_NAMES` (enumerated, never dropped); add `_PHASE_MIGRATION_WINDOW_5_START = "20260924120000"` / `_PHASE_MIGRATION_WINDOW_5_END = "20260924120001"` (half-open, one second) with a window-4-style comment carrying its measured schedule-verb count (0), and append the pair to `_PHASE_MIGRATION_WINDOWS`. Never widen window 4.

    D. Fan-out gate (`supabase/tests/test_ledger_refresh_fanout.sql`), same commit (TRAP E / C-02, Pitfall 4):
      1. Append the new migration LAST in `-- RED-UNDER-SETUP: {"apply":[...]}`.
      2. Re-point EVERY `RED-UNDER-M` edit step whose `"file"` is `supabase/migrations/20260917120000_ledger_fanout_admit_private.sql` to the new file (RESEARCH counted 20 steps with `grep -o`; re-measure). Steps on other files (e.g. arm Q's on `20260716130000_...`, view/constraint migrations) stay. For each re-pointed needle, `grep -c -F` it on the final migration bytes and set `occurrences` to the measured count; where a needle now occurs in both bodies (e.g. the dormancy cause assignment, the interval, the in-flight status list), set `"occurrences":2,"nth":1` so it still mutates the single-key body (Pitfall 5).
      3. Arm 0: add cause (v) after cause (iv), in its style — `IF v_body !~ 'failed_targets' THEN RAISE EXCEPTION 'TEST FAILED (0): ... Cause (v): migration 20260924120000_ledger_fanout_failure_count.sql has not been applied to THIS database. Apply it and re-run; expect this exactly once, on the PR that introduces it, because migrations reach TEST only on merge.'`.
      4. Arm N, placed AFTER arm R and before the sentinel NOTICE (Pitfall 7: before R it would steal R's first failure under R's twin). Seed via INSERT only two draft strategies on `k_led` (`lrf N1` poisoned, `lrf N2` healthy) with moderately stale `strategy_analytics` like arms P/R (`CURRENT_DATE - 2x` style dates, never century-old); create `pg_temp.lrf_poison_n()` keyed on `name LIKE 'lrf N1%'` and `lrf_poison_n_trg BEFORE INSERT ON compute_jobs`; publish both; call the fan-out inside `BEGIN ... EXCEPTION WHEN OTHERS THEN RAISE EXCEPTION 'TEST FAILED (N): ...' END;` (escape becomes the arm's own identity; the poison's RAISE is not a `TEST FAILED (` literal); `DROP TRIGGER`. Then OUTSIDE any handler (lint R1) and directly in the DO body (GRAMMAR 3c) assert: return = 1; `lrf N2` has 1 job and `lrf N1` has 0; exactly one row with `cron_name = 'ledger_refresh_fanout' AND error = 'candidate_enqueue_failed' AND metadata->>'function' = 'enqueue_ledger_refresh_for_strategies' AND metadata->'failed_targets' @> jsonb_build_array(jsonb_build_object('strategy_id', <N1 id>))` (narrow by YOUR target: arm R's all-poisoned tick now writes such a row too); in it `failed_count` = 1 = `jsonb_array_length(failed_targets)`, `enqueued_count` = 1, and the N2 id is NOT contained. Boundary: record the count of this function's `candidate_enqueue_failed` rows, call the fan-out again with no poison, assert the count is unchanged (a zero-failure tick writes nothing) and assert the second return equals the value you measure on the lane (expected 1: `lrf N1` is now enqueueable — if it is not 1, investigate candidate competition; never loosen the assertion to pass). Re-park both to `draft`.
      5. Annotate arm N: a prose `-- RED-UNDER:` line saying which body edit reddens it, and a `-- RED-UNDER-M: {"arm":"N","apply":[{"kind":"edit","file":"supabase/migrations/20260924120000_ledger_fanout_failure_count.sql","find":"<the increment statement>","replace":"<the same statement adding 0>","occurrences":<measured>,"nth":1}]}` (with both bodies holding the statement, measured occurrences is expected to be 2). Prose `RED-UNDER:` count must equal twin count per file (grammar parity).
      6. Move the sentinel to `ALL 19 ARMS EXECUTED (A, B, C, D, E, F, G, H, I, J, K, L, M1, M2, N, P, Q, R, S1)` (order as the file lists arms; measure) in ALL THREE places — the header prose, the `the same lane prints ...` comment, and the final `RAISE NOTICE` (CI reads the FIRST with `head -1`, Pitfall 9).
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only</automated>
    <fails_when>non-zero exit, or output lacks "No static defects", or reports occurrence-mismatch / parity / duplicate-arm</fails_when>
    <automated>node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_fanout.sql</automated>
    <fails_when>exit code other than 2, or output lacks "No defects in the narrowed scope.", or any arm reports no-red / wrong-first-failure / NO-IDENTITY (run under the host lock)</fails_when>
    <automated>node scripts/lint-sql-gates.mjs</automated>
    <fails_when>non-zero exit or any finding reported</fails_when>
    <automated>node scripts/lint-app-guc.mjs</automated>
    <fails_when>non-zero exit or any finding reported</fails_when>
    <automated>node scripts/lint-migration-data-dependence.mjs</automated>
    <fails_when>non-zero exit or the new migration named in a refusal</fails_when>
    <automated>npm run schema:functions:check</automated>
    <fails_when>non-zero exit or output lacks "SQL function snapshot is current"</fails_when>
    <automated>cd analytics-service && <MAIN_VENV_PYTHON> -m pytest -q tests/test_ledger_refresh_gates.py</automated>
    <fails_when>non-zero exit or any "failed" / "error" count in the pytest summary</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'CREATE OR REPLACE FUNCTION public.enqueue_ledger_refresh_for_strategies()' supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` lists exactly one line, and the same command for `public.enqueue_ledger_composite_refresh()` lists exactly one line.
    - `grep -n 'prod-body-ack:' supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` lists exactly two lines, each followed by a 64-hex-digit hash.
    - `grep -cE 'cron\.(un)?schedule' supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` == 0.
    - `grep -c "current_setting('app\." supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` == 0.
    - `grep -ci 'Production environment' supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` == 0.
    - `grep -n 'source migration: 20260924120000_ledger_fanout_failure_count.sql' supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql supabase/schema/functions/enqueue_ledger_composite_refresh.sql` lists one line in each file.
    - `grep -c '"file":"supabase/migrations/20260917120000_ledger_fanout_admit_private.sql"' supabase/tests/test_ledger_refresh_fanout.sql` == 0.
    - `grep -n 'ALL 19 ARMS EXECUTED' supabase/tests/test_ledger_refresh_fanout.sql` lists three lines (header prose, the lane comment, the final RAISE NOTICE).
    - `grep -c 'ALL 18 ARMS EXECUTED' supabase/tests/test_ledger_refresh_fanout.sql` == 0.
    - `grep -c '_PHASE_MIGRATION_WINDOW_5_START' analytics-service/tests/test_ledger_refresh_gates.py` >= 2 and `_PHASE_MIGRATION_WINDOW_4_END` still reads `"20260917120001"`.
    - The narrowed runner on the fan-out gate exits 2 with `No defects in the narrowed scope.` and lists arm N as `RED (identity ok)`.
    - pytest, schema check and the three linters are green.
  </acceptance_criteria>
  <done>The migration exists, is registered, its snapshots are current, and the single-key gate proves on the lane that one poisoned candidate yields exactly one counted failure row naming it while its sibling enqueues; every fan-out twin still bites.</done>
</task>

<task type="auto">
  <name>Task 2: Composite gate — apply the new migration, re-point its twins, add arm N with the h_set park</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql</files>
  <read_first>
    - supabase/tests/test_ledger_refresh_composite_arm.sql — `RED-UNDER-SETUP` (it ends at `20260911130000_...` and does NOT carry `20260917120000_...`), every `RED-UNDER-M` line (twins use `nth: 2`), arm 0 causes (iii)/(iv), arm B's composite seeding (`api_key_id NULL` + `strategy_keys` members), arm H (`h_set`, five composites left published), arms M1/M2, the three `ALL 15 ARMS EXECUTED` mentions
    - supabase/tests/test_ledger_refresh_fanout.sql arm N (from Task 1; the pattern to adapt)
    - supabase/migrations/20260924120000_ledger_fanout_failure_count.sql (from Task 1; measure needles on its bytes)
  </read_first>
  <action>
    1. Append `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` LAST in this gate's `RED-UNDER-SETUP`. Do not add `20260917120000_...`: the new file's single-key body is self-contained and this gate never tested it; add it only if the lane apply measurably fails without it, and record why.
    2. Re-point EVERY `RED-UNDER-M` edit step whose `"file"` is `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql` to the new file (RESEARCH counted 16 with `grep -o`; re-measure). Keep `nth: 2` where the needle occurs in both bodies; re-measure every `occurrences:1` needle on the new file's bytes (e.g. `'ledger-refresh-composite'`, `LIMIT 2`, the composite-only predicates) and correct `occurrences`/`nth` to the measured values so each still hits the composite body. The two view steps on `20260825120000_...` stay.
    3. Arm 0: add cause (v) in the Task 1 style, naming this function.
    4. Arm N after the file's last arm and before the sentinel NOTICE: first park arm H's cohort (`UPDATE strategies SET status = 'draft' WHERE id = ANY(h_set)`, Pitfall 12) so it cannot consume the `LIMIT 2`; seed via INSERT two composites `lrc N1` (poisoned) and `lrc N2` (healthy), each `api_key_id NULL` with at least one eligible `strategy_keys` member on `k_led` (not the deferred venue), with moderately stale analytics; create `pg_temp.lrc_poison_n()` keyed on `name LIKE 'lrc N1%'` and `lrc_poison_n_trg BEFORE INSERT ON compute_jobs`; publish both; wrapped call; `DROP TRIGGER`; then the same assertions as Task 1's arm N with `metadata->>'function' = 'enqueue_ledger_composite_refresh'` and the composite ids, including the precision equalities and the zero-failure second call (assert no new `candidate_enqueue_failed` row for this function; assert the second return equals the lane-measured value — if the composite body legitimately withholds `lrc N1` on the second tick, assert the measured value and record the body clause that explains it). Re-park N1/N2.
    5. Annotate arm N with a prose `-- RED-UNDER:` and a `-- RED-UNDER-M:` edit on the new file flipping the increment to add 0, with `"occurrences":<measured>,"nth":2` (the composite body is second). Keep prose/twin parity.
    6. Move the sentinel to `ALL 16 ARMS EXECUTED (A, B, C, D, E, F, G, H, I, J, K, L, M1, M2, N, S1)` (file order; measure) in ALL THREE places.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only</automated>
    <fails_when>non-zero exit or output lacks "No static defects"</fails_when>
    <automated>node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_composite_arm.sql</automated>
    <fails_when>exit code other than 2, or output lacks "No defects in the narrowed scope.", or any arm reports no-red / wrong-first-failure / occurrence-mismatch (run under the host lock)</fails_when>
    <automated>node scripts/lint-sql-gates.mjs</automated>
    <fails_when>non-zero exit or any finding reported</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '"file":"supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql"' supabase/tests/test_ledger_refresh_composite_arm.sql` == 0.
    - `grep -n 'ALL 16 ARMS EXECUTED' supabase/tests/test_ledger_refresh_composite_arm.sql` lists three lines.
    - `grep -c 'ALL 15 ARMS EXECUTED' supabase/tests/test_ledger_refresh_composite_arm.sql` == 0.
    - `grep -c 'h_set' supabase/tests/test_ledger_refresh_composite_arm.sql` increased by at least 1 (the park inside arm N).
    - `grep -c 'UPDATE strategy_analytics' supabase/tests/test_ledger_refresh_composite_arm.sql` is unchanged from base.
    - The narrowed runner exits 2 with `No defects in the narrowed scope.`; arm N is `RED (identity ok)`.
  </acceptance_criteria>
  <done>The composite gate applies the new migration, every one of its twins still bites on the new body, and arm N proves the composite failure row on the lane.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Apply the migration on the private pg-lane and run both ledger gates plus the mutation runner against it (replaces the forbidden remote schema push)</name>
  <files>supabase/migrations/20260924120000_ledger_fanout_failure_count.sql (verification only; edited only if a lane run exposes a defect, followed by re-running Tasks 1-2 verifies)</files>
  <read_first>
    - scripts/pg-lane/run.sh header (CLI contract: `--workdir`, `--apply <files...>`, `--gate`; exit codes)
    - the `RED-UNDER-SETUP` lines of both ledger gates (the apply lists to pass)
    - scripts/baseline-content-drift-check.mjs header and src/__tests__/baseline-content-drift-allowlist.test.ts (`PINNED_SIZE`, list may only shrink)
  </read_first>
  <action>
    This is the phase's schema-application gate. Nothing proceeds to Plan 04 or to review until every step below is green. One command per step, each lane step under the host lock (`mkdir` before, `rmdir` after, as separate commands).
    1. For each ledger gate: print its apply list (`grep -a '^-- RED-UNDER-SETUP:' <gate>`), then run ONE command `bash scripts/pg-lane/run.sh --workdir <scratch>/p1646-03-lane --apply <each file from the list, in order, as separate arguments> --gate <gate>`; require exit 0 and the new sentinel (`ALL 19 ARMS EXECUTED` / `ALL 16 ARMS EXECUTED`) in its output; remove the scratch lane dir afterwards.
    2. Re-run both narrowed mutation runs and `--parse-only` (exact commands in `<verify>`).
    3. Re-measure both `prod-body-ack` values with `--diff-bodies` (as in Task 1) and confirm the file carries exactly those two `live` hashes.
    3b. Anti-vacuity for the three NEW guards this plan adds (CONTEXT `<specifics>`: every new guard is neutered, observed RED, restored byte-identically). The repo migration is NEVER edited for this: `cp` it into a `p1646-03-bak` directory under your scratchpad as the byte backup, and build each neutered variant as a SEPARATE scratch copy (`p1646-03-neuter/`), passing that copy's path to `--apply` IN PLACE of the repo file (same position in the apply list). One command per step, each lane run under the host lock:
      (i) `$verify$` ordering check (T-164.6-11 mitigation): in a scratch copy, move the single-key body's failure-instrument `IF v_failed > 0 THEN ... END IF;` block to just BEFORE that body's last `pg_advisory_unlock`. Run the fan-out gate's lane apply with the copy; require a NON-zero exit and the ordering check's own RAISE text in the output (the apply must refuse, not the gate). Record the refusal line.
      (ii) failure-instrument needle: in a fresh scratch copy, rename the `failed_targets` metadata key in the single-key body only (e.g. to `failed_tgts`). Run the same lane apply; require a non-zero exit and the needle check's RAISE text naming that function. Record it.
      (iii) arm 0 cause (v): run the fan-out gate's lane with the apply list MINUS the new migration (every other entry unchanged). Require a non-zero exit whose FIRST `TEST FAILED (` line is arm 0's and contains `Cause (v)`. Record the line. Repeat for the composite gate and record its arm 0 cause (v) line.
      Then `cmp` the repo migration against the `p1646-03-bak` backup (no output required), remove both scratch dirs, and re-run step 1's two green lane runs. Record all four observations (neuter in words, the refusal or failure line, silent `cmp`) in the SUMMARY without any absolute path.
    4. Run `node scripts/baseline-content-drift-check.mjs` and RECORD its output in the SUMMARY: it is EXPECTED to exit 1 with exactly two findings naming `enqueue_ledger_refresh_for_strategies` and `enqueue_ledger_composite_refresh` (RESEARCH Pitfall 11). The only legal clearance is a post-apply `baseline.sql` re-dump from PROD, a credentialed founder act outside this phase. ⛔ Never widen `CONTENT_DRIFT_ALLOWLIST`. Also record in the SUMMARY WHICH CI job reads this migration's TEST applied-ness at YOUR base, measured with `grep -c '^  test-db-drift:' .github/workflows/ci.yml`: if 0 (Phase 164.4.2 not in your base), shared-TEST `sql-tests` reads arm 0 cause (v) red on the PR until `apply-test` lands the migration on merge; if 1 (164.4.2 in your base, DECISION F), `sql-tests` replays the new migration on its private local stack so cause (v) is green there, and TEST applied-ness is read by `test-db-drift` (its VAC-08 step), which sees the migration only after apply-on-merge. Name the job you measured, and the count.
    5. Record in the SUMMARY: D-14's three reviewers (migration-reviewer, rls-policy-auditor, silent-failure-hunter) are owed BEFORE push and are the orchestrator's to dispatch; D-12's rls question for the auditor is "who can read `cron_runs.metadata->'failed_targets'`" with the baseline RLS facts quoted in the migration header.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_fanout.sql</automated>
    <fails_when>exit code other than 2, or output lacks "No defects in the narrowed scope."</fails_when>
    <automated>node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_composite_arm.sql</automated>
    <fails_when>exit code other than 2, or output lacks "No defects in the narrowed scope."</fails_when>
    <automated>node scripts/baseline-content-drift-check.mjs</automated>
    <fails_when>it reports a finding for any function OTHER than the two fan-outs, or reports zero findings (meaning the migration's bodies did not reach the chain), or its finding count is not exactly 2</fails_when>
  </verify>
  <acceptance_criteria>
    - Both direct lane runs exit 0 and print their new sentinels; SUMMARY quotes the sentinel lines.
    - Both narrowed runner runs exit 2 with `No defects in the narrowed scope.`.
    - `baseline-content-drift-check` output recorded with exactly two findings, both fan-outs; `git diff --stat -- scripts/baseline-content-drift-check.mjs src/__tests__/baseline-content-drift-allowlist.test.ts` is empty.
    - No Supabase CLI, psql or remote-DB command was run (SUMMARY states it).
    - SUMMARY records step 3b's four anti-vacuity observations: the ordering-check refusal, the needle-check refusal, and arm 0's `Cause (v)` first failure in each gate, each followed by a silent `cmp` of the repo migration against its byte backup.
    - SUMMARY names the CI job measured in step 4 (`sql-tests` or `test-db-drift`) with the `grep -c` count that decided it.
  </acceptance_criteria>
  <done>The migration has been applied and exercised on the private lane by both ledger gates and the mutation runner; the two by-construction reds are recorded, not suppressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron tick (definer, table owner) → public.cron_runs | the fan-out writes failure rows by ownership (no FORCE RLS) |
| cron_runs readers → failed_targets | admin SELECT policy and service_role policy; anon/authenticated have grants but no admitting policy |
| branch merge → TEST → PROD | merging this migration auto-applies it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6-10 | I (Information disclosure) | cron_runs.metadata.failed_targets | medium | mitigate | strategy ids only in metadata, readable only by platform admins and service_role (baseline RLS quoted in the header, D-12); never in RAISE text; rls-policy-auditor confirms before push (D-14) |
| T-164.6-11 | D (Denial of Service) | advisory lock on a failing instrument write | high | mitigate | failure INSERT placed after the success-path unlock and best-effort wrapped; apply-time ordering check refuses the migration if the block moves inside the lock-held block |
| T-164.6-12 | T (Tampering / integrity) | successful enqueues in a tick with a failure | high | mitigate | no RAISE at tick end (D-10); handler assignments only; arm N asserts the healthy sibling's job exists |
| T-164.6-13 | E (Elevation) | SECURITY DEFINER search_path hijack | high | mitigate | `SET search_path = public, pg_catalog` value-checked at apply (check 2); REVOKE re-issued; EXECUTE grantee == owner check (check 11) |
| T-164.6-14 | D (Denial of Service) | unbounded failure rows | low | mitigate | one row per failing tick; failed_targets bounded by LIMIT 4 / LIMIT 2 |
| T-164.6-15 | T / R (Repudiation) | evidence erasure of failure rows | low | accept | TRUNCATE already revoked from anon/authenticated (20260911130000); DELETE scoped by RLS; FORCE RLS deliberately not added (it would silence the instruments) |
| T-164.6-16 | T (Tampering) | production apply | high | mitigate | irreversible act is the merge; three-reviewer pass before push (D-14); founder merges; no remote DB command in this phase |
| T-164.6-SC | Tampering | npm/pip installs | low | accept | no package installed |
</threat_model>

<verification>
- `node scripts/mutation-runner/run.mjs --parse-only` → `No static defects`.
- Both narrowed runner runs → exit 2, `No defects in the narrowed scope.`.
- Both direct lane runs → exit 0 with the new sentinels.
- `node scripts/lint-sql-gates.mjs`, `node scripts/lint-app-guc.mjs`, `node scripts/lint-migration-data-dependence.mjs`, `npm run schema:functions:check`, ledger pytest → green.
- `node scripts/baseline-content-drift-check.mjs` → exactly the two expected fan-out findings, recorded.
</verification>

<success_criteria>
- OPS-08-F2 closed at the SQL layer: both fan-outs record the failed target id and a non-zero failure count per failing tick, proven on the pg-lane with one target forced to raise, with every superseded twin re-pointed and biting.
</success_criteria>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Symbol / content |
|---|---|---|
| `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` | new migration | both fan-out bodies; locals `v_failed`, `v_failed_targets`; cause `candidate_enqueue_failed`; `DO $verify$`; two `prod-body-ack:` lines |
| `supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql` | regenerated | source migration = new file |
| `supabase/schema/functions/enqueue_ledger_composite_refresh.sql` | regenerated | source migration = new file |
| `analytics-service/tests/test_ledger_refresh_gates.py` | modified | `_FANOUT_MIGRATION_NAME`, `_COMPOSITE_MIGRATION_NAME`, superseded sets, `_PHASE_MIGRATION_WINDOW_5_START` / `_END` |
| `supabase/tests/test_ledger_refresh_fanout.sql` | modified | arm N, cause (v), re-pointed twins, `ALL 19 ARMS EXECUTED` |
| `supabase/tests/test_ledger_refresh_composite_arm.sql` | modified | arm N, cause (v), re-pointed twins, `ALL 16 ARMS EXECUTED` |

## Expected-red gates (by construction, recorded — never suppressed)
- `sql-gate-lint` → `baseline-content-drift`: 2 findings until a founder's post-apply `baseline.sql` re-dump.
- TEST applied-ness, named by the job the executor measures at its base (Task 3 step 4): before Phase 164.4.2 merges, `sql-tests` against shared TEST reads cause (v) red until `apply-test` applies the migration on merge; after 164.4.2 (DECISION F), `sql-tests` replays the migration on its private stack and is green, and `test-db-drift` (VAC-08) is the job that sees the migration only after apply-on-merge.
- Until Plan 04 lands: the parser census, floors, gate-family-meta, ci-anti-skip contract and drift-check literal vitest pins.

## Budget note (scope_sanity)
This plan's uncalibrated estimate exceeds the per-plan budget and it is deliberately NOT split. RESEARCH requires the migration, both regenerated snapshots, the Python registration, the two new arms, every superseded twin re-point and the arm-0 causes to land in ONE commit: any intermediate commit that carries the migration without its twin re-points leaves the old twins mutating a superseded file, which the runner reports as no-red, and `WAIVED_CEILING` is 0, so there is no legal green intermediate state. Splitting across plans would therefore ship a knowingly red board between plans. The mitigation is inside the plan instead: Task 1 is the tracer on one gate, Task 2 repeats a proven pattern on the second, and Task 3 only runs commands.

<assumption_delta_decision>
Detector `detected: true` from prose about dropped criteria. The new failure row reuses the existing cron_name `ledger_refresh_fanout` and is discriminated by `error`/`metadata.cause`, the same identity the dormancy instrument already uses. Decision: `no-change` (Plan 05's runbook edit tells future "dormant?" readers to discriminate on `error`).
</assumption_delta_decision>

<output>
Create `.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-03-SUMMARY.md` when done.
</output>
