---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/supabase/retry-serialization-failure.ts
  - src/lib/supabase/retry-serialization-failure.test.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/strategies/csv-finalize/route.test.ts
  - src/app/api/allocator/holdings/sync/route.ts
  - src/app/api/allocator/holdings/sync/route.test.ts
autonomous: true
requirements:
  - OPS-08-TS

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A 40001 from enqueue_compute_job in csv-finalize's enqueueCsvAnalyticsAfter is retried exactly once with no sleep; on success no failed-analytics placeholder is written and captureToSentry is not called (D-03, D-06)"
    - "A 40001 from request_allocator_holdings_sync in the holdings sync POST is retried exactly once; 40001 then success answers 200 and emits the audit event once (D-03)"
    - "When the retry is also a 40001, csv-finalize still writes ENQUEUE_LOST_RACE_USER_COPY through the placeholder and captures to Sentry exactly once, and holdings sync still answers its existing 500 (D-04)"
    - "Any non-40001 error (42501, PGRST301, any other code, or a null code) makes exactly one RPC call and takes the existing branch unchanged (D-04)"
    - "Idempotency edge (OPS-08-TS): a retry re-issues the WHOLE RPC through a factory, never re-awaits a builder, so the call count observed at the mock is exactly 2 on a retried 40001 and exactly 1 otherwise; the re-issued RPC is retry-safe because a 40001 aborts request_allocator_holdings_sync's whole transaction (it catches unique_violation only) and enqueue_compute_job dedups"
    - "Concurrency edge (OPS-08-TS): the helper holds no module-level state, so two concurrent invocations each get their own single-retry budget — a helper test runs two invocations under Promise.all, each seeing 40001 first, and asserts each factory is called exactly twice"
    - "Each site's retry is observed RED when neutered and restored byte-identically (cmp against a byte backup) (D-07)"
    - "csv-finalize's prose no longer claims nothing in the repo retries a 40001; the user copy literal is unchanged (D-08)"
  artifacts:
    - path: src/lib/supabase/retry-serialization-failure.ts
      provides: "retryOnceOnSerializationFailure — call a factory; on error.code 40001 call onRetry then the factory exactly once more"
      exports: ["retryOnceOnSerializationFailure"]
    - path: src/lib/supabase/retry-serialization-failure.test.ts
      provides: "helper unit tests incl. concurrency case"
    - path: src/app/api/strategies/csv-finalize/route.ts
      provides: "enqueueCsvAnalyticsAfter wraps admin.rpc enqueue_compute_job in the helper"
      contains: "retryOnceOnSerializationFailure"
    - path: src/app/api/allocator/holdings/sync/route.ts
      provides: "POST wraps supabase.rpc request_allocator_holdings_sync in the helper"
      contains: "retryOnceOnSerializationFailure"
  key_links:
    - from: src/app/api/strategies/csv-finalize/route.ts
      to: src/lib/supabase/retry-serialization-failure.ts
      via: "import of retryOnceOnSerializationFailure around the enqueue_compute_job rpc factory"
      pattern: "retryOnceOnSerializationFailure\\(\\s*\\(\\)\\s*=>\\s*admin\\.rpc"
    - from: src/app/api/allocator/holdings/sync/route.ts
      to: src/lib/supabase/retry-serialization-failure.ts
      via: "import of retryOnceOnSerializationFailure around the request_allocator_holdings_sync rpc factory"
      pattern: "retryOnceOnSerializationFailure\\(\\s*\\(\\)\\s*=>\\s*supabase\\.rpc"
  prohibitions:
    - requirement_id: OPS-08-TS
      category: safety
      status: resolved
      verification: judgment
      statement: "One user request must never execute the RPC more than twice, and a retry must never be inserted behind a sleep, backoff loop or any error code other than 40001."
    - requirement_id: OPS-08-TS
      category: transparency
      status: resolved
      verification: judgment
      statement: "The owner-facing lost-race copy must never promise an automatic retry; it is written only after the single retry is exhausted and says what the owner can do next."
---

<objective>
Close requirement OPS-08-TS (ROADMAP Phase 164.6 success criterion 2): a `40001` (`serialization_failure`, the lost-enqueue-race code raised by `_enqueue_compute_job_internal` since migration 20260826150000) is retried exactly once at BOTH named call sites — csv-finalize's `enqueueCsvAnalyticsAfter` and the holdings sync POST's `request_allocator_holdings_sync` RPC — through one small shared helper.

Scope (D-01): the founder's 2026-09-17 SCOPE PRUNE reduced Phase 164.6 to criteria 2, 3 and 4. The ROADMAP's long requirements string lists many TODOS ids for dropped criteria; only OPS-08-TS, OPS-08-F2 and 161.1-D13 are in scope, and this plan owns OPS-08-TS. Per D-02 the retry lands at these two sites ONLY; the six other `enqueue_compute_job` callers (keys/sync, finalize-wizard x2, intro, two crons) are a recorded deferred idea and are NOT touched.

Purpose: a lost MVCC race currently reaches a paying user as an error; after this plan it self-heals once.
Output: `src/lib/supabase/retry-serialization-failure.ts` (+ test), two wrapped call sites, updated route tests, corrected prose.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-CONTEXT.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-RESEARCH.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-PATTERNS.md
@src/lib/supabase/cast.ts
@src/app/api/strategies/csv-finalize/route.ts
@src/app/api/allocator/holdings/sync/route.ts

Interface the helper exposes (the only export; `knip` flags anything else):
- `retryOnceOnSerializationFailure<T extends { error: { code?: string | null } | null }>(call: () => PromiseLike<T>, onRetry: (first: T) => void): Promise<T>`
- Behaviour: await `call()`; if `result.error?.code !== "40001"` return it; else invoke `onRetry(first)` and return `await call()` — exactly one more time, no sleep.

Executor constraints (this repo, this run):
- Anchors are cited BY SYMBOL. Locate with `grep -n '<symbol>' <file>` at execution time; never write a `path:line` anchor into any `.planning/` artifact.
- Anti-vacuity = byte backup (`cp` into a per-plan directory named `p1646-01-bak` under your scratchpad), neuter, observe RED with the named assertion, restore from the backup, `cmp` backup vs file. NEVER `git checkout --`, `git restore`, or `git stash`. Re-take the backup after every edit.
- One command per verification step; do not chain commands with `&&` in a step that names a `.git` path.
- No `npm install`/`pip install`. No credential anywhere. `.planning/` is public: no home paths or usernames in any artifact (run `npm run check:planning-hygiene` after writing the SUMMARY).
- Do NOT touch VERSION, the package.json version, or CHANGELOG.md. Do not stage `.planning/milestone.lock`, `.planning/state.json`, or anything under `.planning/phases/164.5.3-*`.
- Commit messages never contain a CI skip token, not even to deny one.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — helper plus csv-finalize retries a 40001 once, end-to-end through the route's after() callback</name>
  <files>src/lib/supabase/retry-serialization-failure.ts, src/lib/supabase/retry-serialization-failure.test.ts, src/app/api/strategies/csv-finalize/route.ts, src/app/api/strategies/csv-finalize/route.test.ts</files>
  <read_first>
    - src/lib/supabase/cast.ts (shape to copy: pure module, file-level JSDoc, one JSDoc'd export, no `server-only`)
    - src/lib/supabase/numeric-precision.test.ts (co-located test shape)
    - src/app/api/strategies/csv-finalize/route.ts — symbols `enqueueCsvAnalyticsAfter`, `ENQUEUE_LOST_RACE_USER_COPY`, `enqueueLostRace`, the `// @audit-skip:` pragma above `admin.rpc("enqueue_compute_job"`
    - src/app/api/strategies/csv-finalize/route.test.ts — `adminRpcMock`, `describe("[WR-07] ...")`, `runScheduledEnqueue`, `copyWrittenToUser`, the `⭐ RED DEMO` docblock, the case titled `a 40001 lost race → the owner reads curated copy`
    - src/__tests__/audit-coverage.test.ts header (the pragma must stay within 8 lines above the chain start)
    - supabase/migrations/20260826150000_destrict_enqueue_internal_10param.sql header (read only; D-08 forbids editing it)
  </read_first>
  <behavior>
    - Helper: factory returns 40001 then `{ error: null }` → factory called 2x, `onRetry` called 1x with the first result, second result returned.
    - Helper: 40001 twice → factory 2x, `onRetry` 1x, the SECOND 40001 result returned (no third call).
    - Helper: `"42501"`, `"PGRST301"`, `{ error: null }` and an error with `code: null` → factory 1x, `onRetry` never called.
    - Helper concurrency: two invocations under `Promise.all`, each factory yielding 40001 then ok → each factory called exactly 2x (no shared budget).
    - Helper: uses no timer — assert under `vi.useFakeTimers()` that the retried promise resolves without advancing timers.
    - csv-finalize: 40001 then ok → `copyWrittenToUser()` empty (no placeholder), `adminRpcMock` called 2x for `enqueue_compute_job`, `captureToSentry` not called.
    - csv-finalize: 40001 twice → curated `ENQUEUE_LOST_RACE_USER_COPY` written once, `adminRpcMock` 2x, `captureToSentry` exactly 1x.
    - csv-finalize: the existing `CONTROL — a NON-40001 ...` case additionally asserts `adminRpcMock` 1x.
  </behavior>
  <action>
    Precondition step: run `node scripts/verify-plan-anchors.mjs` on this PLAN.md path and halt on a non-zero exit.

    1. Create `src/lib/supabase/retry-serialization-failure.ts` exporting only `retryOnceOnSerializationFailure` with the interface in `<context>` (D-05). It takes a FACTORY, never a builder: a supabase-js builder is a thenable, so re-awaiting it is not a second request (RESEARCH Pitfall 3). File JSDoc states the policy and its evidence: retry exactly once, only on `error.code === "40001"`, no sleep (D-03) because the 40001 is raised only after the race's winner has already advanced past the in-flight statuses; the caller logs the retried attempt with `console.warn` and never sends it to Sentry (D-06). No imports, no `server-only`, no options beyond `call` and `onRetry` (D-05: must not grow options nobody uses). Do NOT place it under `src/lib/retry/` (client backoff primitives) and do not route through `src/lib/seam-retry-registry.ts` (Railway HTTP seams).
    2. Create `src/lib/supabase/retry-serialization-failure.test.ts` with the helper cases from `<behavior>`, a docblock saying WHY each matters, asserting call counts on the factory `vi.fn()`.
    3. In `enqueueCsvAnalyticsAfter`, replace the awaited `admin.rpc("enqueue_compute_job", {...})` with `retryOnceOnSerializationFailure(() => admin.rpc("enqueue_compute_job", {...}), (first) => console.warn(...))`. The retry warn line uses the existing `${opts.logPrefix}` prefix and `[correlation_id=${opts.correlationId}]` suffix and says a 40001 lost race is being retried once, so it is distinguishable from the existing failure warn; it does NOT call `captureToSentry` (D-06). Keep the `// @audit-skip:` pragma within 8 lines above the chain start. Everything from `if (enqueueErr)` down stays byte-identical (D-04): the exhausted 40001 still sets `enqueueLostRace` and still reaches `ENQUEUE_LOST_RACE_USER_COPY` through `writeFailedStrategyAnalyticsPlaceholder`.
    4. D-08 prose: reword the `ENQUEUE_LOST_RACE_USER_COPY` JSDoc paragraph that begins `⛔ IT MUST NOT PROMISE AN AUTOMATIC RETRY` so it says the route now retries a 40001 once and this copy is written only after that retry is exhausted, so "Retry the sync" remains true and the copy still must not promise an automatic retry. Keep the `// WR-07:` comment above `let enqueueLostRace` only as dated history (prefix it as the pre-164.6 state). The `ENQUEUE_LOST_RACE_USER_COPY` string literal is NOT changed. Applied migration 20260826150000 is NOT edited.
    5. Tests (RESEARCH Pitfall 1 — this case flips RED when the retry lands): in the `[WR-07]` describe, convert the case titled `a 40001 lost race → the owner reads curated copy` to TWO `mockResolvedValueOnce` 40001 results and add `expect(adminRpcMock).toHaveBeenCalledTimes(2)` plus `captureToSentry` called exactly once. Reword its failure message (the one calling an automatic retry a FALSE PROMISE because no retry existed) and the describe docblock line that claims no retry exists, to say the single retry is exhausted before this copy is written. Keep the `not.toMatch(/automatic/i)` assertion. Add the 40001-then-ok case and the CONTROL call-count assertion from `<behavior>`. Filter `adminRpcMock.mock.calls` by name `enqueue_compute_job` if other RPCs share the mock.
  </action>
  <verify>
    <automated>npx vitest run src/lib/supabase/retry-serialization-failure.test.ts src/app/api/strategies/csv-finalize/route.test.ts</automated>
    <fails_when>non-zero exit, or the summary line reports any failed test, or reports "No test files found"</fails_when>
    <automated>npx vitest run src/__tests__/audit-coverage.test.ts</automated>
    <fails_when>non-zero exit (the wrapped enqueue chain lost its @audit-skip pragma within 8 lines)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'export' src/lib/supabase/retry-serialization-failure.ts` shows exactly one exported function, `retryOnceOnSerializationFailure`, and the file contains no `setTimeout`, `sleep`, or `import "server-only"`.
    - `grep -n 'retryOnceOnSerializationFailure(' src/app/api/strategies/csv-finalize/route.ts` returns exactly one call site inside `enqueueCsvAnalyticsAfter`.
    - The stale claim is gone even where it wraps across comment lines (a plain `grep` cannot see it: at base the phrase spans two JSDoc lines and `grep -ci` already prints 0). Run `node -e "const s=require('fs').readFileSync('src/app/api/strategies/csv-finalize/route.ts','utf8');const j=s.split('\n').map(l=>l.replace(/^\s*(\/\/+|\*+|\/\*+)\s?/,'')).join(' ').replace(/\s+/g,' ');console.log((j.match(/nothing in this repo retries a 40001/gi)||[]).length)"`: it prints `1` at base (measured at planning base `93d2ab394`; confirm at yours before editing) and MUST print `0` after this task.
    - The `ENQUEUE_LOST_RACE_USER_COPY` literal text is byte-identical to the base (compare the assigned string with `git show HEAD:src/app/api/strategies/csv-finalize/route.ts`).
    - Both vitest files pass; the retried-then-ok csv case asserts 2 calls and 0 Sentry calls; the 40001-twice case asserts 2 calls and exactly 1 Sentry call.
  </acceptance_criteria>
  <done>csv-finalize retries a 40001 exactly once through the helper; both vitest files and the audit-coverage test are green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Holdings sync retries a 40001 once; 403/500/audit branches unchanged</name>
  <files>src/app/api/allocator/holdings/sync/route.ts, src/app/api/allocator/holdings/sync/route.test.ts</files>
  <read_first>
    - src/app/api/allocator/holdings/sync/route.ts — the `POST` handler, its header JSDoc, the `supabase.rpc("request_allocator_holdings_sync"` call, the `"42501"` branch, the 500 branch, `logAuditEvent`
    - src/app/api/allocator/holdings/sync/route.test.ts — the hoisted `mockRpc` / `mockLogAuditEvent`, the `vi.mock("@/lib/supabase/server"` factory, the case `returns 500 when RPC raises an unexpected error`
    - supabase/schema/functions/request_allocator_holdings_sync.sql (read only: its `EXCEPTION WHEN unique_violation` does not catch 40001, so a 40001 aborts the RPC's whole transaction including the `api_keys` UPDATE — the whole-RPC retry is safe)
    - src/lib/supabase/retry-serialization-failure.ts (from Task 1)
  </read_first>
  <behavior>
    - 40001 then success → 200, `mockRpc` called 2x, `mockLogAuditEvent` called 1x.
    - 40001 twice → 500 with the existing body, `mockRpc` 2x, `mockLogAuditEvent` 0x.
    - `PGRST301` → 500, `mockRpc` 1x.
    - `42501` → 403, `mockRpc` exactly 1x (never retried).
  </behavior>
  <action>
    Wrap ONLY the `supabase.rpc("request_allocator_holdings_sync", { p_api_key_id: api_key_id })` call as `retryOnceOnSerializationFailure(() => supabase.rpc(...), (first) => console.warn(...))` with a `[allocator/holdings/sync]` prefix naming the user id and key id already used by the existing `console.error`, and no Sentry call (D-06). Keep the user-scoped `createClient()` client (the RPC is granted to `authenticated` and gates on `auth.uid()`); do NOT switch to the admin client. The `"42501"` → 403, the other-error → 500, and the `logAuditEvent` success path stay byte-identical (D-04). Extend the route header JSDoc with one numbered step: a 40001 lost enqueue race is retried once with no sleep (D-03), and the whole RPC is retry-safe because the 40001 aborts its own transaction. In the test file add the four cases in `<behavior>` using the existing hoisted `mockRpc` harness (`mockResolvedValueOnce`); leave the existing `returns 500 when RPC raises an unexpected error` case unchanged as the non-40001 control but add a `toHaveBeenCalledTimes(1)` assertion if absent.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/allocator/holdings/sync/route.test.ts</automated>
    <fails_when>non-zero exit, or any failed test in the summary, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'retryOnceOnSerializationFailure(' src/app/api/allocator/holdings/sync/route.ts` returns exactly one hit; `grep -c 'createAdminClient' src/app/api/allocator/holdings/sync/route.ts` is unchanged from base.
    - The test file contains a 42501 case asserting `toHaveBeenCalledTimes(1)` and a 40001-then-ok case asserting `toHaveBeenCalledTimes(2)` on `mockRpc`.
    - vitest run is green.
  </acceptance_criteria>
  <done>Holdings sync retries a 40001 exactly once; 403/500/audit behaviour unchanged; test file green.</done>
</task>

<task type="auto">
  <name>Task 3: Anti-vacuity — neuter each site's retry, observe its test RED, restore byte-identically; repo gates</name>
  <files>src/app/api/strategies/csv-finalize/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/csv-finalize/route.test.ts — the `⭐ RED DEMO` docblock at the end of the `[WR-07]` describe (dated house record format)
    - src/app/api/strategies/csv-finalize/route.ts and src/app/api/allocator/holdings/sync/route.ts (post-Task-1/2 state)
  </read_first>
  <action>
    Per D-07, for EACH site in turn (csv-finalize first, then holdings sync), one command per step:
    (a) `cp` the route file to the `p1646-01-bak` backup directory;
    (b) neuter the retry by replacing that site's `retryOnceOnSerializationFailure(() => X, ...)` with a direct `X` (single call);
    (c) run that site's vitest file and record the named failing test title(s) and the AssertionError line (expected: the 40001-then-ok case fails on the call count 1 vs 2);
    (d) restore by `cp` from the backup;
    (e) `cmp` backup vs file and require no output;
    (f) re-run that site's vitest file green.
    Also neuter the helper itself once (return `first` unconditionally) and observe the helper test RED, restore, `cmp`.
    Append one dated `⭐ RED DEMO` entry to the csv-finalize `[WR-07]` docblock in the house format (date 2026-09-24 or the execution date, the neuter described in words, the observed failing title, the restored pass count). Record the holdings-sync and helper observations in the SUMMARY (do not quote any home path). Then run the repo gates listed in `<verify>`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/supabase/retry-serialization-failure.test.ts src/app/api/strategies/csv-finalize/route.test.ts src/app/api/allocator/holdings/sync/route.test.ts</automated>
    <fails_when>non-zero exit or any failed test after restoration</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or the vitest summary reports any failed test file or test (this covers every suite that scans or exercises the touched routes, e.g. the csv-finalize-*, seam-log-coverage, seam-budgets, seam-ratelimit-posture, resilient-fetch.wiring and phase-150-capital-ownership-invariant tests)</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, or eslint reports an error, or check-planning-hygiene reports a finding</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY records, per site and for the helper: the neuter, the named RED test title, and a `cmp` with no output after restore.
    - The csv-finalize `⭐ RED DEMO` docblock has one new dated entry for the 164.6 retry.
    - Record this plan's starting commit sha before Task 1; `git diff --stat START_SHA^..HEAD` (START_SHA = this plan's first commit) lists only the six files in `files_modified` (plus this plan's SUMMARY).
    - typecheck and lint exit 0; `npm test` exits 0 with no failed test file.
  </acceptance_criteria>
  <done>Both retries and the helper were each observed RED when neutered and restored byte-identically; typecheck and lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → route handler | authenticated owner triggers csv-finalize or a holdings sync |
| route → PostgREST RPC | the route re-issues an RPC after a 40001 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6-01 | D (Denial of Service) | retryOnceOnSerializationFailure | medium | mitigate | exactly one retry, only on code 40001, no loop; call count pinned 2 by tests at both sites and in the helper test |
| T-164.6-02 | T (Tampering / integrity) | request_allocator_holdings_sync re-issue | low | mitigate | whole-RPC retry: the 40001 aborts the RPC transaction including the api_keys UPDATE, so the re-issue starts clean; user-scoped client keeps auth.uid() gating (no switch to admin) |
| T-164.6-03 | R (Repudiation) | holdings sync audit | low | mitigate | logAuditEvent emitted once on the eventual success, zero times on exhausted failure; asserted in tests |
| T-164.6-04 | I (Information disclosure) | console.warn on retry | low | accept | the retry warn carries only ids already logged by the existing branches; no error body beyond what the existing warn logs |
| T-164.6-SC | Tampering | npm/pip installs | low | accept | this plan installs no package (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `npx vitest run src/lib/supabase/retry-serialization-failure.test.ts src/app/api/strategies/csv-finalize/route.test.ts src/app/api/allocator/holdings/sync/route.test.ts src/__tests__/audit-coverage.test.ts` green.
- `npm test` (full suite), `npm run typecheck` and `npm run lint` green.
- Anti-vacuity recorded for both sites and the helper.
</verification>

<success_criteria>
- OPS-08-TS closed: both named sites retry a 40001 exactly once; the exhausted path and every non-40001 path are unchanged; each site's test goes RED when its retry is removed.
</success_criteria>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Symbol / content |
|---|---|---|
| `src/lib/supabase/retry-serialization-failure.ts` | new module | `retryOnceOnSerializationFailure` |
| `src/lib/supabase/retry-serialization-failure.test.ts` | new test | helper cases incl. concurrency and no-timer |
| `src/app/api/strategies/csv-finalize/route.ts` | modified | `enqueueCsvAnalyticsAfter` wrapped; `ENQUEUE_LOST_RACE_USER_COPY` JSDoc reworded |
| `src/app/api/strategies/csv-finalize/route.test.ts` | modified | `[WR-07]` cases converted/added; new `⭐ RED DEMO` entry |
| `src/app/api/allocator/holdings/sync/route.ts` | modified | `POST` wrapped; header JSDoc step |
| `src/app/api/allocator/holdings/sync/route.test.ts` | modified | four retry cases |

## Out of scope / deferred (recorded, not done)
- 40001 retry at keys/sync, finalize-wizard x2, intro and the two crons (D-02, CONTEXT Deferred Ideas).
- Editing applied migration 20260826150000's header prose (D-08: applied history).

<assumption_delta_decision>
The assumption-delta detector returned `detected: true`, but every signal it cites comes from prose about DROPPED criteria. This plan changes no identity model. Decision: `no-change`.
</assumption_delta_decision>

<output>
Create `.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-01-SUMMARY.md` when done.
</output>
