# Phase 164.6: GATE-HYGIENE - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-23
**Phase:** 164.6-gate-hygiene
**Mode:** `--auto` (founder asleep; every choice taken from repo evidence, none asked)
**Areas discussed:** Scope, 40001 retry policy, fan-out failure surface, composite marker retraction

---

## Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Criteria 2, 3, 4 as pruned 2026-09-17 | The founder's prune | ✓ |
| Also criterion 17 (kill-switch comments) | Routed in 2026-09-14, then dropped by the later prune | |
| Retry at every TS enqueue site | Class closure beyond criterion 2's two named sites | |

**Choice:** [auto] the prune. The later founder decision governs; widening is deferred.

## 40001 retry policy

| Option | Description | Selected |
|--------|-------------|----------|
| Retry once, 40001 only, no sleep | TODOS OPS-08-TS's stated fix | ✓ |
| Retry N times with backoff | No evidence a second race is plausible once the winner has advanced | |

**Choice:** [auto] retry once. Sentry only on final failure.

## Fan-out failure surface

| Option | Description | Selected |
|--------|-------------|----------|
| cron_runs row beside the unchanged INTEGER return | Reuses the dormancy instrument's shape | ✓ |
| Change the return type | Needs DROP FUNCTION; breaks the scheduled command and gates | |
| RAISE at end of a failing tick | Rolls back the enqueues that succeeded | |

**Choice:** [auto] the cron_runs row; the id goes in metadata only, never in RAISE text.

## Composite marker retraction

| Option | Description | Selected |
|--------|-------------|----------|
| Mirror Python's helper in TS, union of markers, best-effort + Sentry | Parity with the shipped Python half | ✓ |
| Fail the user's request on retraction failure | Stricter than Python; the enqueue already succeeded | |
| SQL merge arm in enqueue_compute_job | 161.1-D11; every enqueue path, its own review | |

**Choice:** [auto] mirror Python.

## Workflow note

The `--auto` chain's `auto_advance` step (persist `_auto_chain_active`, then launch
`gsd-plan-phase --auto`) was NOT taken: the orchestrating instruction runs plan-phase and
execute-phase (`--no-transition`) as separate, explicit steps and stops before verify/ship.

## Claude's Discretion

Helper names, the fan-out cause string, how the pg-lane arm forces one target to raise, plan split.

## Deferred Ideas

- 40001 retry at the other user-request enqueue sites.
- 161.1-D11 merge arm.
