---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 04
type: execute
wave: 2
depends_on:
  - "164.6-03"
files_modified:
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - .github/workflows/ci.yml
  - src/__tests__/drift-check-scripts.test.ts
autonomous: true
requirements:
  - OPS-08-F2

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A full `node scripts/mutation-runner/run.mjs` over the corpus WITH Plan 03's two new arms exits 0 with `No defects`, and ARMS_FLOOR in scripts/mutation-runner/run.mjs equals the biting count that run measured (expected 428); FILES_FLOOR is unchanged and WAIVED_CEILING stays 0 (D-13)"
    - "Every census and calibration pin that tracks the arm count is re-derived from the measured run — armsSeen, stepsSeen, needles.length, totalAnchored, GREEN_LOG per-file rows and totals, the gap-of-one calibrations and the gate-family-meta ARMS_FLOOR row — and those vitest files are green"
    - "ci.yml's sql-tests sentinel derivation table reads 16 for test_ledger_refresh_composite_arm.sql and 19 for test_ledger_refresh_fanout.sql, SENTINEL_FLOOR stays 11, ARMS_FLOOR moves to the new measured sum (expected 215) with its sum comment, and the ci-anti-skip contract and drift-check literal pin are green"
    - "The floors vitest goes RED when ARMS_FLOOR is left at its stale value and GREEN at the measured value (the stale-low direction observed, then restored byte-identically)"
  artifacts:
    - path: scripts/mutation-runner/run.mjs
      provides: "export const ARMS_FLOOR at the measured value with a dated derivation comment"
      contains: "export const ARMS_FLOOR"
    - path: src/__tests__/gate-family-meta.test.ts
      provides: "value-keyed row scripts/mutation-runner/run.mjs :: ARMS_FLOOR=<measured> with dated lineage prepended"
    - path: .github/workflows/ci.yml
      provides: "sentinel derivation rows 16 / 19 and ARMS_FLOOR=<measured sum>"
  key_links:
    - from: scripts/mutation-runner/run.mjs
      to: src/__tests__/mutation-runner-floors.test.ts
      via: "the floors test's RATCHET STALE check reads ARMS_FLOOR by symbol against the re-derived corpus"
      pattern: "ARMS_FLOOR"
    - from: .github/workflows/ci.yml
      to: src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts
      via: "contract re-derives the sentinel table from each gate file's ALL N ARMS EXECUTED sentinel"
      pattern: "test_ledger_refresh_fanout\\.sql\\s+19"
  prohibitions:
    - requirement_id: OPS-08-F2
      category: safety
      status: resolved
      verification: judgment
      statement: "No floor may be lowered, WAIVED_CEILING may not rise above 0, no waiver may be added, no allowlist may be widened, and no timeout may be relaxed to make this plan's gates pass."
---

<objective>
Finish requirement OPS-08-F2 (ROADMAP Phase 164.6 success criterion 3) by moving every pin that Plan 03's two new gate arms move — measured, never predicted (D-13). The runbook edits and the phase-level full-suite pass moved to Plan 05 in revision 1 to keep this plan inside the per-plan budget.

Scope (D-01): the founder's 2026-09-17 SCOPE PRUNE reduced Phase 164.6 to criteria 2, 3 and 4 (only OPS-08-TS, OPS-08-F2 and 161.1-D13 are in scope; the ROADMAP's longer requirements string lists TODOS ids of DROPPED criteria). ⚠️ Although the phase directory's slug still reads "OPS-08-F9 sentinel plus the two ci.yml integers", criterion 1 (OPS-08-F9/F8) was DROPPED; this plan moves the `ci.yml` integers ONLY because Plan 03's new arms change the ledger gates' sentinels, not to deliver OPS-08-F9 (no `test_enqueue_internal_destrict.sql` sentinel is added).

Purpose: the SQL-gate censuses are independent ratchets; leaving any one at the old value is a red board (floors may only RISE to MEASURED values, D-13).
Output: moved floors and pins, with each moved pin's own vitest file green.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-CONTEXT.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-RESEARCH.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-PATTERNS.md

Pins at planning time (base `93d2ab394`, read by symbol; RE-MEASURE at your base — Phase 164.4.2 is not merged here and also edits run.mjs, the floors test and ci.yml): `export const ARMS_FLOOR` = 426, `export const FILES_FLOOR` = 49, `export const WAIVED_CEILING` = 0; `expect(armsSeen).toBe(426)`, `expect(stepsSeen).toBe(441)`, `expect(needles.length).toBe(441)`; `expect(totalAnchored).toBe(426)`; gate-family-meta row `"scripts/mutation-runner/run.mjs :: ARMS_FLOOR=426"`; ci.yml rows `test_ledger_refresh_composite_arm.sql 15` / `test_ledger_refresh_fanout.sql 18`, `SENTINEL_FLOOR=11`, `ARMS_FLOOR=213`; drift-check-scripts title `ci.yml still declares SENTINEL_FLOOR=11 and ARMS_FLOOR=213 at HEAD` and its `grep -ac` argv. Expected after (+2 arms, +2 edit steps): 428 / 443 / 443 / 428 / 16 / 19 / 11 / 215 — confirm each by measurement.

Executor constraints (this repo, this run):
- ⛔ `supabase/tests` censuses are GREPPED, not counted, and never blanket-replaced N→N+1: the floors test holds DELIBERATE off-by-one calibrations (`rows sum to 425 ... reports 426`, `lane-invocations: 427`, `biting: 427 ... exceeds executed (426)`, `executed=426 lane-invocations=0`) whose GAP must be preserved relative to the new value (RESEARCH Pitfall 10).
- `<HOST_LOCK_DIR>` is an absolute path SUPPLIED IN YOUR EXECUTOR PROMPT (the host-wide lane lock directory); substitute it literally. ⛔ Never derive it with command substitution or from a `.git` path, and never write the substituted path into `.planning/`. If the prompt does not supply it, HALT and report it.
- Full or narrowed runner runs happen under the host lock: `mkdir <HOST_LOCK_DIR>` before and `rmdir <HOST_LOCK_DIR>` after, each its own single command (release it even when the run fails). The full run takes several minutes: run it in the background writing its log to your scratchpad (`p1646-04-full.log`) and wait for its completion notification before reading it. Never act on a partial log.
- Anti-vacuity = byte backup (`cp` to `p1646-04-bak` under your scratchpad), neuter, observe RED, restore by `cp`, `cmp`. NEVER `git checkout --` / `git restore` / `git stash`.
- Anchors BY SYMBOL. Line numbers in `.planning/` artifacts are forbidden (plan-anchor-verify). The runbooks are public: do not paste strategy ids or the target list into them.
- Do NOT touch VERSION, the package.json version, CHANGELOG.md (no ship in this phase); do not stage `.planning/milestone.lock`, `.planning/state.json`, `.planning/phases/164.5.3-*`. No CI skip token in any commit message. No home path or username in `.planning/`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer [BLOCKING] — full mutation-runner measurement drives ARMS_FLOOR and every arm-count pin</name>
  <precondition>Plan 03 is merged into this worktree: `grep -c 'ALL 19 ARMS EXECUTED' supabase/tests/test_ledger_refresh_fanout.sql` prints 3 and `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` exists (or the stamp Plan 03's SUMMARY records).</precondition>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/mutation-annotation-parser.test.ts, src/__tests__/gate-family-meta.test.ts</files>
  <read_first>
    - scripts/mutation-runner/run.mjs — `export const ARMS_FLOOR` and its dated derivation comment, `export const FILES_FLOOR`, `export const WAIVED_CEILING`
    - src/__tests__/mutation-runner-floors.test.ts — `expect(totalAnchored).toBe(`, `const GREEN_LOG` (per-file rows for both ledger gates, `arms:`, `biting:`, `lane-invocations:`, `per-arm lane time:` lines), every calibration literal that names 425/426/427, and the dated `⭐ CURRENCY` comment style
    - src/__tests__/mutation-annotation-parser.test.ts — `expect(armsSeen).toBe(`, `expect(stepsSeen).toBe(`, `expect(needles.length).toBe(`
    - src/__tests__/gate-family-meta.test.ts — the `"scripts/mutation-runner/run.mjs :: ARMS_FLOOR=` row and its `MEASURED + <date> (... ) Prior: ...` lineage format
    - .planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-03-SUMMARY.md (the narrowed per-file arm counts Plan 03 measured)
  </read_first>
  <action>
    Precondition steps: run `node scripts/verify-plan-anchors.mjs` on this PLAN.md path and halt on non-zero; assert the `<precondition>`.
    1. Static census: run `node scripts/mutation-runner/run.mjs --parse-only` and record its `arms:` line (annotated count; expected `0/428/0`) and step/needle totals.
    2. Under the host lock, run the FULL `node scripts/mutation-runner/run.mjs` in the background to `p1646-04-full.log`; after completion require exit 0 and `No defects`, and record `coverage: files`, `arms:`, `biting:`, `lane-invocations:` (the two tallies must AGREE), `lane-blocked`, `lane-probe`, `unreachable`, `pending`, and the per-file rows for both ledger gates. With ARMS_FLOOR still at the old value the run exits 0 (the runner only catches below-floor); that is expected.
    3. Set `export const ARMS_FLOOR` to the measured biting count, prepending a dated derivation comment in the existing style (164.6 plans 03/04: arm N in each ledger gate, both in already-annotated files so FILES_FLOOR stays; WAIVED_CEILING stays 0). Do not touch FILES_FLOOR or WAIVED_CEILING.
    4. Re-derive each pin from the measurements (D-13): `armsSeen`, `stepsSeen`, `needles.length` from the parse-only census; `totalAnchored`; in `GREEN_LOG` the two ledger per-file rows (sections/judged/annotated/biting to the measured per-file values) and every aggregate line; each calibration literal re-derived so its deliberate gap to the aggregate is preserved (e.g. the row-sum stays exactly ONE below the aggregate, the over-count stays exactly ONE above). Add a dated `⭐ CURRENCY 2026-09-24 (Phase 164.6 ...)` comment beside each moved literal in the house style. Prepend the new `MEASURED + <date>` entry to the gate-family-meta `ARMS_FLOOR=` row (renaming the row key to the measured value) and keep the `Prior:` chain intact.
    5. Anti-vacuity (stale-low direction): `cp` run.mjs to the backup dir, set ARMS_FLOOR back to the old value, run the floors vitest and record the `RATCHET STALE` (or equivalent) failing assertion, restore by `cp`, `cmp` with no output, re-run green.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --parse-only</automated>
    <fails_when>non-zero exit or output lacks "No static defects"</fails_when>
    <automated>npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/gate-family-meta.test.ts</automated>
    <fails_when>non-zero exit, any failed test, or "No test files found"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^export const (FILES_FLOOR|ARMS_FLOOR|WAIVED_CEILING)' scripts/mutation-runner/run.mjs` shows ARMS_FLOOR = the biting count quoted from `p1646-04-full.log` in the SUMMARY, FILES_FLOOR unchanged from base, WAIVED_CEILING = 0.
    - SUMMARY quotes the full run's `arms:`, `biting:`, `lane-invocations:` lines and its exit code 0.
    - `grep -c 'ARMS_FLOOR=426"' src/__tests__/gate-family-meta.test.ts` = 0 as a row key (the old value survives only inside the `Prior:` lineage prose).
    - The three vitest files are green; the stale-low anti-vacuity observation is recorded with a silent `cmp`.
  </acceptance_criteria>
  <done>ARMS_FLOOR and every arm-count pin equal the measured full-run values; the floors ratchet was observed RED at the stale value.</done>
</task>

<task type="auto">
  <name>Task 2: ci.yml sentinel table and floors, and the drift-check literal pin</name>
  <files>.github/workflows/ci.yml, src/__tests__/drift-check-scripts.test.ts</files>
  <read_first>
    - .github/workflows/ci.yml — the `sql-tests` step's sentinel derivation table (rows `#   test_ledger_refresh_composite_arm.sql` / `#   test_ledger_refresh_fanout.sql`, a contiguous, equally-indented block), `SENTINEL_FLOOR=` and `ARMS_FLOOR=` with its `# 13 + 9 + ...` sum comment
    - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts (the row regex, `SENTINEL_FLOOR == table.length`, `ARMS_FLOOR == sum`)
    - src/__tests__/drift-check-scripts.test.ts — `it("ci.yml still declares SENTINEL_FLOOR=11 and ARMS_FLOOR=213 at HEAD"` and its `["-ac", "ARMS_FLOOR=213", ...]` argv
  </read_first>
  <action>
    Read each ledger gate's FIRST `ALL N ARMS EXECUTED` sentinel (`grep -m1 -o 'ALL [0-9]* ARMS EXECUTED' <gate>`) and set the two table rows to those values (expected 16 and 19), keeping the block contiguous and the numeric column aligned exactly as the neighbouring rows (the contract parses them as one block). Add a dated one-line "Re-measured 2026-09-24 (Phase 164.6): composite 15 -> 16, fan-out 18 -> 19 (arm N each)" note ABOVE the block, never between rows. `SENTINEL_FLOOR` stays 11 (no new sentinel file). Set `ARMS_FLOOR` to the new sum of the table rows (expected 215) and rewrite its sum comment to the new addends. Do not touch `timeout-minutes` or any other job. In drift-check-scripts, move the test title and the `grep -ac` argv literal to the new ARMS_FLOOR value, with a dated comment.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>non-zero exit, any failed test, or "No test files found"</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -nE '^\s+#\s+test_ledger_refresh_(composite_arm|fanout)\.sql\s+[0-9]+' .github/workflows/ci.yml` shows 16 and 19.
    - `grep -c 'SENTINEL_FLOOR=11' .github/workflows/ci.yml` >= 1; `grep -c 'ARMS_FLOOR=213' .github/workflows/ci.yml` = 0.
    - `git diff .github/workflows/ci.yml` touches only the sentinel note, the two rows, the `ARMS_FLOOR=` line and its comment (no `timeout-minutes` change).
    - Both vitest files green.
  </acceptance_criteria>
  <done>ci.yml's sentinel table and floor agree with the gate files; the contract and literal pin are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate configuration → merge | the floors and sentinel integers decide whether a weakened gate can merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6-17 | T (Tampering) | ARMS_FLOOR / sentinel floors | medium | mitigate | floors only rise to MEASURED values from a full run; stale-low direction observed RED; WAIVED_CEILING stays 0; contract test cross-checks table vs files |
| T-164.6-19 | R (Repudiation) | pin lineage | low | mitigate | dated CURRENCY / MEASURED entries prepended, Prior chain kept |
| T-164.6-SC | Tampering | npm/pip installs | low | accept | no package installed |
</threat_model>

<verification>
- Full `node scripts/mutation-runner/run.mjs` exit 0, `No defects`, tallies agree; ARMS_FLOOR = measured.
- Floors, parser, gate-family-meta, ci-anti-skip contract and drift-check vitest files green.
- `npm run typecheck` green (the full `npm test`, lint and anchor pass run in Plan 05, after every plan is in the tree).
- Still EXPECTED red, recorded not suppressed: `baseline-content-drift` (2 fan-out findings until the founder's post-apply re-dump), and TEST applied-ness of the new migration until merge, read by the job Plan 03's SUMMARY names for the executor's base (`sql-tests` arm 0 cause (v) before Phase 164.4.2 merges; `test-db-drift`/VAC-08 after it).
</verification>

<success_criteria>
- Every pin the two new arms move agrees with a measured full run, and each pin's vitest file is green.
</success_criteria>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Symbol / content |
|---|---|---|
| `scripts/mutation-runner/run.mjs` | modified | `export const ARMS_FLOOR` (measured) + dated derivation comment |
| `src/__tests__/mutation-runner-floors.test.ts` | modified | `totalAnchored`, `GREEN_LOG`, calibrations re-derived |
| `src/__tests__/mutation-annotation-parser.test.ts` | modified | `armsSeen`, `stepsSeen`, `needles.length` |
| `src/__tests__/gate-family-meta.test.ts` | modified | `run.mjs :: ARMS_FLOOR=<measured>` row |
| `.github/workflows/ci.yml` | modified | sentinel rows 16 / 19, `ARMS_FLOOR=<sum>` |
| `src/__tests__/drift-check-scripts.test.ts` | modified | literal `ARMS_FLOOR=<sum>` pin |

## Owed after this phase (outside executor reach, recorded)
- D-14: migration-reviewer + rls-policy-auditor + silent-failure-hunter before push, every finding fixed (orchestrator-dispatched).
- Post-merge: founder's credentialed `baseline.sql` re-dump clears `baseline-content-drift`.
- Ship-time (not this phase): VERSION/package.json/CHANGELOG per CLAUDE.md CHANGELOG discipline.

<assumption_delta_decision>
Detector `detected: true` from prose about dropped criteria; this plan moves counters only. Decision: `no-change`.
</assumption_delta_decision>

<output>
Create `.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-04-SUMMARY.md` when done.
</output>
