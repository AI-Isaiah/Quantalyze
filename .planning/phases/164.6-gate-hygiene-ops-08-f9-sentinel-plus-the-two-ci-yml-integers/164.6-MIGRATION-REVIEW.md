---
phase: 164.6
reviewed_at_sha: 98feb7720
migration: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
reviewers: [migration-reviewer, rls-policy-auditor, silent-failure-hunter]
date: 2026-09-24
---

# 164.6 — three-reviewer pass on the plan 03 migration (D-14)

All three ran read-only against the branch at `98feb7720` (plans 01–03). No DB command against any remote.

## migration-reviewer — SAFE TO MERGE, 0 BLOCKER / 0 HIGH / 0 MEDIUM / 1 LOW

- Filename orders after the newest migration on origin/main; no collision; no existing migration edited.
- Re-based on the latest definitions: single-key fan-out from `20260917120000_ledger_fanout_admit_private.sql`, composite from `20260911130000_ledger_fanout_grantees_and_dormancy.sql`. Old-vs-new body diff shows only the four stated edits; signature, SECURITY DEFINER, `search_path = public, pg_catalog`, candidate query, per-tick limits, lock keys and activation guard are byte-identical.
- Grants: `REVOKE ALL … FROM PUBLIC, anon, authenticated, service_role` re-issued exactly as the predecessors; no GRANT; owner-only EXECUTE asserted by the verify block. No ACL drift.
- The failure-row INSERT sits in its own `EXCEPTION WHEN OTHERS` block after the success-path unlock; check 10c enforces the ordering.
- The `$verify$` block reads only the catalogue (no application table, never calls the functions), so it cannot refuse on TEST's data-less copy (`[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`).
- Return value unchanged. Snapshots current (`dump-sql-functions --check`: 121 functions; DRIFT-05 0 disagreements).
- `prod-body-ack` hashes match `sql-body-normalize --diff-bodies` output. **Condition:** VAC-04 on the PR must report the same PROD hashes; if not, fold PROD's drift into the migration — never edit the pragma.
- **LOW:** a failed enqueue writes no `compute_jobs` row, so the 20 h cooldown never applies to a poisoned candidate — one `cron_runs` row per hourly tick, indefinitely, and `cron_runs` has no purge.

## rls-policy-auditor — SAFE TO MERGE, 0 findings above threshold

- `cron_runs` (RLS on, no FORCE): `cron_runs_admin_read` (caller's own `profiles.is_admin`) and `cron_runs_service_role` only. anon and non-admin authenticated read zero rows. `is_admin` is not user-writable (column grant + `prevent_profile_privileged_change` trigger; `admin_role_mutate` is service_role only). No view reads `cron_runs`; `latest_cron_success` returns only a timestamp. So `metadata->'failed_targets'` (strategy ids, possibly of private strategies) is visible only to platform admins and service_role — no cross-tenant path.
- EXECUTE on both fan-outs: owner only, unchanged; zero-argument functions.
- search_path pinned and asserted; BYPASSRLS/owner assumptions asserted; the new write works because the owner owns `cron_runs`.
- Below threshold, to fix anyway: (a) no `supabase/tests` arm asserts anon / non-admin authenticated read zero `cron_runs` rows; (b) a carried comment cites a stale `baseline.sql` line number for "no FORCE on cron_runs" — cite by symbol.

## silent-failure-hunter — 2 High / 2 Medium / 3 Low

- **HIGH-1** — no automated reader of the new row: a tick where every candidate fails (signature drift 42883, 42501, constraint drift, 53xxx, 55P03) returns 0, pg_cron records `succeeded`, and the only signal is a `cron_runs` row nothing queries (`prod_prober_cadence_check` watches `prod_prober` only). User-facing (stale ledger factsheets, no alert). Fix: an observer shaped like `prod_prober_cadence_check`, and/or re-raise after unlock when `v_enqueued = 0 AND v_failed > 0` or any failed SQLSTATE is class 42/53/58/XX.
- **HIGH-2** — poisoned-candidate starvation (same root as the migration-reviewer LOW, pre-existing): stalest-first ordering + no cooldown row ⇒ a poisoned candidate takes a slot every tick; two per venue starve that venue, two composites starve the whole composite cohort. Fix: make a failed attempt engage the cooldown (e.g. exclude ids in a recent `failed_targets`), and cover it in the observer.
- **MEDIUM-1** — if the instrument INSERT itself fails, only a WARNING remains; no gate arm proves the wrap. Fix: re-raise when `v_enqueued = 0`; add an arm forcing the INSERT to fail.
- **MEDIUM-2** — a benign lost enqueue race (40001) / deadlock (40P01) is counted as a failed candidate. Fix: `WHEN serialization_failure` before `OTHERS`, counted separately and not writing an `error` row.
- **LOW-1** — `retractInheritedRefreshMarker` does not verify its UPDATE hit a row, and an unreadable just-enqueued job reads as "not a marker".
- **LOW-2** — keys/sync logs only the generic message (PostgREST code sits in `err.cause`); the final error after a retry does not say a retry happened.
- **LOW-3** — verify check (3) uses `< 2` while its message says "expected 2"; use `<> 2`.
- Confirmed correct: handler scope (57014 / assert_failure reach the outer handler), lock not dropped by subtransaction rollback, retry helper only retries 40001 once with a factory at two rollback-safe call sites, both retraction sites log + capture under their own tag.

## Disposition

Every finding goes to the 164.6 fix round (fix-everything policy); none is waived. The merge itself remains the founder's (plan 03's reversibility note).
