---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
verified: 2026-09-24T13:40:00Z
status: human_needed
score: 11/11 must-haves verified
verified_at_sha: 9df87d147b7cdcf290c585dfc1c87be0eb0ea1bb
drift_subjects:
  - src/lib/supabase/retry-serialization-failure.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/allocator/holdings/sync/route.ts
  - src/lib/ledger-refresh-marker.ts
  - src/app/api/keys/sync/route.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
  - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
  - supabase/schema/functions/enqueue_ledger_composite_refresh.sql
  - supabase/tests/test_ledger_refresh_fanout.sql
  - supabase/tests/test_ledger_refresh_composite_arm.sql
  - supabase/tests/test_cron_runs_rls.sql
  - scripts/prod-prober/arms/cron-obs.mjs
  - scripts/prod-prober/run.mjs
  - scripts/mutation-runner/run.mjs
  - .github/workflows/ci.yml
  - analytics-service/services/job_worker.py
  - docs/runbooks/ledger-refresh-go-live.md
  - docs/runbooks/match-engine.md
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "PROD applies migration 20260924120000_ledger_fanout_failure_count.sql after merge (supabase-migrate.yml: apply-test, then apply)"
    expected: "apply-test and apply both succeed; the migration's own verify block passes on TEST and PROD; the PROD ledger carries a 20260924120000 row"
    why_human: "Post-merge by construction: the migration is unmerged and nothing here may touch a remote. Owner: orchestrator at land time (PROD auto-applies once apply-test succeeds; there is no reviewer stop)."
  - test: "VAC-04 on the PR (migration-drift-check.yml) reports the same PROD body hashes the migration's two prod-body-ack lines record"
    expected: "No new body drift for enqueue_ledger_refresh_for_strategies or enqueue_ledger_composite_refresh; if PROD drifted, fold PROD's drift into the migration, never edit the pragma"
    why_human: "Needs the PROD credential in CI; the migration-reviewer made it a merge condition. Owner: orchestrator at ship time."
  - test: "VAC-08 in the test-db-drift job, on the merge push after apply-test"
    expected: "0 NEW drift once TEST carries 20260924120000; red-until-merge on the PR itself is by construction (apply-on-merge)"
    why_human: "Reads shared TEST's ledger, which moves only on merge. Owner: orchestrator at land time."
  - test: "Founder re-dumps supabase/schema/baseline.sql (and regenerates baseline-carried-migrations.txt in the same commit)"
    expected: "baseline-content-drift stops reporting its 2 expected findings for the two re-based fan-out bodies"
    why_human: "The re-dump is a founder action against PROD. Until it happens the 2 findings are expected, not a defect. Owner: founder."
  - test: "First PROD prod-prober run after deploy: read the cron-obs line for ledger_refresh_fanout (IN-01)"
    expected: "A succeeded PROD run's return_message is one of the success forms the step counts ('1 row' or 'SELECT 1'); runs >= 2 in 3h; errored 0; stuck 0"
    why_human: "The success-message shape was measured only on the local lane (pg_cron 1.6.4). If PROD writes a different success form, every healthy run reads as errored (loud, not silent). Owner: orchestrator, first prober run after land."
  - test: "keys/sync marker-retraction budget under a real slow database (IN-04, flagged by the round-1 fixer as needing human verification)"
    expected: "No keys-sync.composite_refresh_marker_retract_timeout or _late events in Sentry in normal operation; if one fires, the 202 still went out"
    why_human: "Covered by a fake-timer test only; real latency is observable only after deploy. Owner: orchestrator, Sentry check after land."
  - test: "Read the two runbook sections as an operator (plan 05 D4): ledger-refresh-go-live.md failure-row reading and the BLOCKING precondition [164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]; match-engine.md deliberate exercise"
    expected: "The text matches the shipped round-2 behaviour (failure row always commits, 20 h cooldown, 40001 counted apart, 40P01 a failure, four prober counts)"
    why_human: "Prose accuracy; no test asserts runbook wording. Owner: founder."
---

# Phase 164.6: GATE-HYGIENE (pruned to criteria 2, 3, 4) Verification Report

**Phase Goal (after the founder's 2026-09-17 SCOPE PRUNE, CONTEXT D-01):** ROADMAP criteria 2 (OPS-08-TS),
3 (OPS-08-F2) and 4 (161.1-D13 TS half). Criterion 1 (OPS-08-F9 sentinel plus the first-failure-exit
loop), 5, 7 and 10 to 17 are DROPPED in the ROADMAP and were not verified as scope. The phase
directory's name still carries the pre-prune criterion 1 title. What this phase does move in
`ci.yml` is the `sql-tests` sentinel rows and the SQL `ARMS_FLOOR` for its own new arms (plan 04),
and those are verified below as a must-have.
**Verified:** 2026-09-24T13:40:00Z at `9df87d147` on `feat/164.6-gatehygiene`
**Status:** human_needed. Every must-have is verified in code and by execution. The open items all
happen after merge, or are founder judgement calls.
**Re-verification:** No. This is the initial verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | **Criterion 2**: both TS call sites retry on 40001, each with a test that fails when that site's retry is removed | ✓ VERIFIED | `retryOnceOnSerializationFailure` retries only `code === "40001"`, once, with no sleep, and takes a factory. Wired at `enqueueCsvAnalyticsAfter` (csv-finalize) and at the `request_allocator_holdings_sync` call (holdings sync). **Neutered by the verifier, one site at a time:** the helper call was replaced with a single call and the file restored from a byte backup (sha256 equal after). csv-finalize went RED with 2 failures; holdings sync went RED with 2 failures. Restored, all 6 files passed (249 tests). |
| 2 | **Criterion 3**: a failed fan-out target produces a non-zero failure count from the tick, proven on the pg-lane with one target forced to raise | ✓ VERIFIED | Both bodies count `v_failed` and `v_failed_targets` in the `WHEN OTHERS` handler, which does assignments only. They write ONE `cron_runs` row (`candidate_enqueue_failed`, with `failed_count`, `enqueued_count`, `lost_race_count` and `failed_targets`) after the advisory unlock, and name the count in the final NOTICE. **Verifier's own pg-lane runs:** `test_ledger_refresh_fanout.sql` baseline exit 0 and arms 32/32/0 biting 32. `test_ledger_refresh_composite_arm.sql` baseline exit 0 and arms 29/29/0 biting 29. Arms N, N2, N3, T and U each went RED under their mutant. |
| 3 | **Criterion 4**: the composite-stamp twin's TS half is closed, with a test that fails when the retraction is removed at either enqueue site | ✓ VERIFIED | `retractInheritedRefreshMarker` is wired at the keys/sync composite kickoff (bounded by `MARKER_RETRACTION_BUDGET_MS`) and at the finalize-wizard composite enqueue, which now captures `jobId`. **Neutered by the verifier:** the call was replaced with `Promise.resolve({retracted:false})`. keys/sync went RED with 6 failures; finalize-wizard went RED with 3. Both were restored byte-identically. |
| 4 | D-10: the return value still means jobs INSERTED; signature, return type and ACL unchanged | ✓ VERIFIED | Both bodies declare `RETURNS INTEGER` and return `v_enqueued`. The migration's `$verify$` block asserts 0 args, SECURITY DEFINER, a pinned `search_path` and owner-only EXECUTE. |
| 5 | D-12: a failed target's id goes only into `cron_runs.metadata`, never into RAISE text, and `cron_runs` is admin-only | ✓ VERIFIED | No RAISE in the migration interpolates a strategy id; the RAISE texts carry counts and SQLSTATE only. The verifier ran `test_cron_runs_rls.sql`: baseline exit 0 and arms 5/5/0. ADMIN 1, ANON 1, USER 1, ANON 2 and USER 2 each went RED under their policy-widening mutant. |
| 6 | D-13: floors only rise and `WAIVED_CEILING` stays 0 | ✓ VERIFIED | Diff against the merge base: `FILES_FLOOR` 49→50 and `ARMS_FLOOR` 426→449. `WAIVED_CEILING` is still 0. `mutation-runner-floors.test.ts` is green. |
| 7 | Plan 04: the `ci.yml` sql-tests sentinel rows and the SQL `ARMS_FLOOR` match the gates' sentinels, and the contract test passes | ✓ VERIFIED | Gate sentinels are `ALL 26 ARMS EXECUTED` (fan-out) and `ALL 23 ARMS EXECUTED` (composite). `ci.yml` has rows 26 and 23, `SENTINEL_FLOOR=11` and `ARMS_FLOOR=229`. `ci-anti-skip-gate.contract.test.ts` and `drift-check-scripts.test.ts` are green. |
| 8 | D-14: the migration passed three reviewers, every finding was fixed, and the snapshots were regenerated | ✓ VERIFIED | MIGRATION-REVIEW, then REVIEW-FIX: 17/17. REVIEW-R2 plus SFH-R2, then REVIEW-FIX-R2: all fixed, with IN-01 and L1 recorded rather than fixed. `dump-sql-functions --check` reports the snapshot current (121 functions). DRIFT-05 gate (a) reports 0 disagreements. |
| 9 | D-15/D-16: the TS marker set equals Python's `LEDGER_REFRESH_JOB_SOURCES` and is pinned by a parity test | ✓ VERIFIED | `ledger-refresh-marker.test.ts` parses `job_worker.py` (and, since the WR-01/IN-01 fixes, the SQL lists) and is green. |
| 10 | D-08: the TS prose that became false is corrected, and the user copy stays | ✓ VERIFIED | 0 hits for "nothing in this repo retries" in `src/`. `ENQUEUE_LOST_RACE_USER_COPY` still reads "Retry the sync, or contact support if this persists." |
| 11 | PD-2: the claim-time-snapshot residual has a destination, and the composite schedule is BLOCKED | ✓ VERIFIED | The runbook section `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` is marked BLOCKING, names Phase 164.6.7 as owner, and adds item 6: the composite runs must be watched before scheduling. The `### Phase 164.6.7` heading exists in this branch's ROADMAP. |

**Score:** 11/11 truths verified (0 present, behavior-unverified).

### Required Artifacts

| Artifact | Status | Details |
|---|---|---|
| `src/lib/supabase/retry-serialization-failure.ts` | ✓ VERIFIED | Substantive, and imported and used at both criterion-2 sites |
| `src/lib/ledger-refresh-marker.ts` | ✓ VERIFIED | Imported and used at both criterion-4 sites |
| `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql` | ✓ VERIFIED | Applied on the pg-lane by both ledger gates' RED-UNDER-SETUP; sorts after the newest migration on origin/main |
| both `supabase/schema/functions/enqueue_ledger_*.sql` snapshots | ✓ VERIFIED | Current, per `dump-sql-functions --check` |
| `supabase/tests/test_cron_runs_rls.sql` | ✓ VERIFIED | New gate, 5 biting arms |
| `scripts/prod-prober/arms/cron-obs.mjs` step (4) | ✓ VERIFIED (wiring) | `LEDGER_FANOUT_SQL` and `parseLedgerFanoutRow` are called on the live path; self-test 91/91. Its PROD reading is a human item (IN-01). |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| csv-finalize `enqueueCsvAnalyticsAfter` | `enqueue_compute_job` RPC | `retryOnceOnSerializationFailure(() => admin.rpc(...))` | WIRED (neuter goes RED) |
| holdings sync route | `request_allocator_holdings_sync` RPC | `retryOnceOnSerializationFailure(() => supabase.rpc(...))` | WIRED (neuter goes RED) |
| keys/sync composite kickoff | `compute_jobs.metadata` | `retractInheritedRefreshMarker(admin, rpcData, correlation_id)` | WIRED (neuter goes RED) |
| finalize-wizard composite enqueue | `compute_jobs.metadata` | `retractInheritedRefreshMarker(admin, jobId, correlationId)` | WIRED (neuter goes RED) |
| both fan-outs | `public.cron_runs` | failure-row INSERT after unlock, plus a cooldown `NOT EXISTS` read | WIRED (pg-lane arms N, T and U bite) |
| cron-obs | `cron.job_run_details` + `cron_runs` | `LEDGER_FANOUT_SQL` | WIRED (self-test) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Route and helper tests (6 files) | vitest | 249/249 | ✓ PASS |
| Census, contract and prober tests (8 files: prober wiring, floors, parser, gate-family-meta, ci-anti-skip contract, drift-check, lint-sql-gates, audit-coverage) | vitest | 1115/1115 | ✓ PASS |
| Retry neuter at each of 2 sites | vitest after a byte-backup edit | RED 2 + RED 2, restored with sha256 equal | ✓ PASS |
| Retraction neuter at each of 2 sites | vitest after a byte-backup edit | RED 6 + RED 3, restored with sha256 equal | ✓ PASS |
| Fan-out gate on the pg-lane | `mutation-runner --file test_ledger_refresh_fanout.sql` | baseline exit 0; 32/32 biting; no defects | ✓ PASS |
| Composite gate on the pg-lane | `mutation-runner --file test_ledger_refresh_composite_arm.sql` | baseline exit 0; 29/29 biting; no defects | ✓ PASS |
| cron_runs RLS gate on the pg-lane | `mutation-runner --file test_cron_runs_rls.sql` | baseline exit 0; 5/5 biting; no defects | ✓ PASS |
| Prober self-test | `node scripts/prod-prober/run.mjs --self-test` | 91/91 | ✓ PASS |
| SQL gate lint | `lint-sql-gates --self-test`, then the corpus | 7 rules OK; 77 files, 0 findings | ✓ PASS |
| Function snapshot | `tsx scripts/dump-sql-functions.ts --check` | current, 121 functions | ✓ PASS |
| Mutation static parse | `mutation-runner --parse-only` | No static defects | ✓ PASS |

The full mutation run (449/449/0, exit 0 at `25412fc74`) was **not** re-run. No file under
`supabase/`, `scripts/mutation-runner/` or `scripts/pg-lane/` changed between `25412fc74` and HEAD
(measured with `git diff --stat`), and the three narrowed runs above re-executed every gate this
phase touched. The Python `test_ledger_refresh_gates.py` was not re-run: it is not in this
verification's permitted gate set.

### Requirements Coverage

| Requirement | Source Plan | Status | Evidence |
|---|---|---|---|
| OPS-08-TS | 01 | ✓ SATISFIED | Truth 1 |
| 161.1-D13 (TS half) | 02, 05 | ✓ SATISFIED | Truths 3, 9, 11 |
| OPS-08-F2 | 03, 04, 05 | ✓ SATISFIED | Truths 2, 4 to 8 |
| OPS-08-F9, OPS-08-F8, PROC-02, PROC-03, H-0001, and criteria 7 and 10 to 17 | none | DROPPED | The ROADMAP SCOPE PRUNE of 2026-09-17 dropped them (fix-or-drop). Not orphaned, because the founder decision removed them from scope. |

### Anti-Patterns Found

None. The lines this branch adds to the 35 non-planning files it changed carry no unreferenced
TBD, FIXME, XXX or HACK. The only matches are prose citations of `TODOS.md` entry ids.

Info only: the branch does not yet carry the CHANGELOG entry or the VERSION bump. Both belong to
the ship step.

### Recorded residuals (carried by the fix reports, not gaps)

- L1: if the failure-row write fails on a tick that did enqueue, the failure is silent. That needs
  two independent faults, and it is recorded rather than fixed.
- The keys/sync retraction timeout does not cancel the in-flight retraction. A late rejection is
  now LOUD under its `_late` tag.
- The Python composite guard reads its claim-time snapshot. Phase 164.6.7 owns that fix, and the
  runbook blocks scheduling the composite fan-out until then.
- D-02: the 40001 retry is deliberately not added at the other `enqueue_compute_job` callers.

### Human Verification Required

Every item happens after merge or needs founder judgement. None is marked passed.

1. **PROD applies the migration.** Owner: orchestrator at land. `apply-test` runs first, then `apply` auto-applies; there is no reviewer stop.
2. **VAC-04 hashes on the PR** match the `prod-body-ack` lines. This is the migration-reviewer's merge condition. Owner: orchestrator at ship.
3. **VAC-08 in `test-db-drift`** reads 0 NEW drift after `apply-test`. Owner: orchestrator at land.
4. **Founder re-dumps the baseline.** Until then `baseline-content-drift` shows its 2 expected findings. Owner: founder.
5. **First PROD prober run** measures the success-message assumption (IN-01). Owner: orchestrator after land.
6. **Real-latency behaviour of the keys/sync retraction budget** (IN-04), checked in Sentry. Owner: orchestrator after land.
7. **Runbook prose read-through** (plan 05 D4). Owner: founder.

### Gaps Summary

No gaps. All three surviving ROADMAP criteria are met, and each was proven by the verifier's own
execution: the four TS guards were neutered and went RED, and the pg-lane gates came back green with
every arm biting. The status is `human_needed` only because the migration's PROD apply, the
TEST-side drift gates, the baseline re-dump and the first PROD prober reading all happen after
merge.

---

_Verified: 2026-09-24T13:40:00Z_
_Verifier: Claude (gsd-verifier)_
