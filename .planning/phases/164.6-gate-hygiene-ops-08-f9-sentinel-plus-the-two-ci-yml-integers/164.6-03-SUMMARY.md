---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 03
subsystem: database
tags: [postgres, plpgsql, pg_cron, security-definer, cron_runs, mutation-runner, pg-lane]

requires:
  - phase: 164.5.1.1
    provides: "20260917120000, the live single-key fan-out body (lifecycle admits `private`)"
  - phase: 164.8.6
    provides: "20260911130000, the live composite body, the dormancy instrument and its cron_runs write shape"
provides:
  - "Forward migration 20260924120000_ledger_fanout_failure_count.sql: both fan-outs count failed candidate enqueues and write ONE cron_runs row per failing tick (error candidate_enqueue_failed, metadata {function, cause, failed_count, enqueued_count, failed_targets}) after the advisory unlock"
  - "Arm N in both ledger gates: one poisoned candidate beside a healthy one, proven on the pg-lane"
  - "All 36 superseded edit-kind twins re-pointed to the new migration with measured occurrences/nth"
affects: [164.6-04, 164.6-05, ledger-refresh-go-live runbook, match-engine runbook]

actuals:
  tokens: 39107
  tasks: 3
  commits: 2
plan_head_before: 47fe37cded422154d4a92fe66619139719d06a76

tech-stack:
  added: []
  patterns:
    - "Failure instrument: handler does assignments only; one best-effort-wrapped cron_runs write AFTER the success-path advisory unlock"
    - "Apply-time ordering check: the failure block must follow the LAST pg_advisory_unlock in the comment-stripped body"
    - "Counted presence (>= 2) for a statement shape that now occurs twice per body"

key-files:
  created:
    - supabase/migrations/20260924120000_ledger_fanout_failure_count.sql
  modified:
    - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
    - supabase/schema/functions/enqueue_ledger_composite_refresh.sql
    - analytics-service/tests/test_ledger_refresh_gates.py
    - supabase/tests/test_ledger_refresh_fanout.sql
    - supabase/tests/test_ledger_refresh_composite_arm.sql

key-decisions:
  - "Migration stamp 20260924120000 kept: newest migration on every local and remote ref was 20260922120000 (measured), so the stamp is strictly in order"
  - "Verify check 7 COUNTS the cron_runs INSERT (>= 2) instead of testing presence: with two writes per body a presence test would pass with the dormancy write deleted"
  - "Fan-out arm N seeds its fixtures inside the arm (INSERT only), so no earlier arm can see them"
  - "Composite gate does NOT add 20260917120000 to its apply list; the lane applied cleanly without it"
  - "Host lane lock taken by hand as a directory under the git common dir, per the orchestrator note (the prompt named no literal path)"

patterns-established:
  - "Arm N shape: seed poisoned + healthy, pg_temp BEFORE INSERT trigger keyed by name, wrapped call, assert return / sibling job / exactly one failure row keyed by the poisoned id / exact counts / clean second tick writes nothing"

requirements-completed: []  # OPS-08-F2 is also declared by Plan 04 (pins); marked complete only when Plan 04 lands

coverage:
  - id: D1
    description: "Single-key fan-out: one poisoned candidate yields exactly one candidate_enqueue_failed row naming it, the healthy sibling enqueues, return = 1, counts exact, a clean tick writes nothing"
    requirement: OPS-08-F2
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --apply <gate apply list> --gate supabase/tests/test_ledger_refresh_fanout.sql (ALL 19 ARMS EXECUTED, exit 0)"
        status: pass
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_fanout.sql (24/24 biting, arm N RED identity ok)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Composite fan-out: the same proof, under metadata.function = enqueue_ledger_composite_refresh, with arm H's cohort parked"
    requirement: OPS-08-F2
    verification:
      - kind: integration
        ref: "bash scripts/pg-lane/run.sh --apply <gate apply list> --gate supabase/tests/test_ledger_refresh_composite_arm.sql (ALL 16 ARMS EXECUTED, exit 0)"
        status: pass
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_composite_arm.sql (21/21 biting, arm N RED identity ok)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Migration registered and current: Python gate pointers + window 5, snapshots regenerated, three SQL linters clean"
    requirement: OPS-08-F2
    verification:
      - kind: unit
        ref: "analytics-service: python -m pytest -q tests/test_ledger_refresh_gates.py (37 passed)"
        status: pass
      - kind: other
        ref: "npm run schema:functions:check; node scripts/lint-sql-gates.mjs; node scripts/lint-app-guc.mjs; node scripts/lint-migration-data-dependence.mjs"
        status: pass
    human_judgment: false
  - id: D4
    description: "Who can read cron_runs.metadata->'failed_targets' (D-12) and the PROD blast radius of an auto-applying SECURITY DEFINER change"
    requirement: OPS-08-F2
    verification: []
    human_judgment: true
    rationale: "D-14's three pre-push reviewers (migration-reviewer, rls-policy-auditor, silent-failure-hunter) own this judgment; they are the orchestrator's to dispatch before push"

duration: ~20min
completed: 2026-09-24
status: complete
---

# Phase 164.6 Plan 03: OPS-08-F2 fan-out failure count Summary

**Both ledger fan-outs now write one `cron_runs` row when a candidate's enqueue fails. The row names the failed strategy ids and SQLSTATEs and counts them beside the enqueued count, and the write happens after the advisory lock is released. Arm N in each gate proves this on the private pg-lane with one candidate forced to raise.**

## Performance

- **Duration:** about 20 min
- **Started:** 2026-09-24T04:40Z
- **Completed:** 2026-09-24T05:00Z
- **Tasks:** 3 (Tasks 1 and 2 committed; Task 3 ran commands only and changed no file)
- **Files modified:** 6 (1 created, 5 modified)

## Accomplishments

- **New migration `20260924120000_ledger_fanout_failure_count.sql`.** It re-bases BOTH bodies byte for byte on their committed snapshots and applies the same four edits to each:
  - (a) two locals: the failure count and the failed-target JSONB list.
  - (b) in the per-candidate handler, **assignments only**: increment the count and append `{strategy_id, sqlstate}`. The WARNING line stays byte-identical.
  - (c) after the success-path unlock, `IF v_failed > 0` runs one INSERT inside its own exception block into `cron_runs`, with `cron_name 'ledger_refresh_fanout'`, `status 'error'` and `error 'candidate_enqueue_failed'`.
  - (d) the closing NOTICE reports the failure count as a number and names no id.
  - `RETURN v_enqueued` is unchanged. The signature, SECURITY DEFINER and the `search_path` pin are unchanged. The COMMENT and the REVOKE are re-issued.
- **Catalogue-only `DO $verify$` over both functions.** It carries the house checks (1–11) forward and adds:
  - a `'failed_targets'` key needle;
  - a counted `cron_runs` write check that requires at least 2 writes;
  - an ordering check that requires an unlock before the failure block and none after it.
  - 20260911130000's TRUNCATE check was dropped.
- **Two `prod-body-ack` lines**, each the `live` column of `--diff-bodies`, re-measured after the snapshots were regenerated:
  - `e7e99017…ce30c9` (single-key)
  - `d1ca6102…b76152` (composite)
- **Python gate.**
  - Both pointers moved to the new file.
  - `20260917120000` was added to the fan-out superseded set and `20260911130000` to the composite set.
  - One-second window 5 is `[20260924120000, 20260924120001)`. Window 4 was not widened.
- **Fan-out gate.**
  - The new migration is appended last in the apply list.
  - All **20** twins that pointed at `20260917120000` were re-pointed. On the new file's bytes 11 needles measured at 2 and now carry `occurrences:2, nth:1`; the other 9 measured at 1.
  - Added arm 0 cause (v) and a new arm N.
  - Sentinel is `ALL 19 ARMS EXECUTED (A, B, C, D, E, F, G, H, I, J, K, L, M1, M2, N, P, Q, R, S1)`, updated in all three places.
- **Composite gate.**
  - All **16** twins that pointed at `20260911130000` were re-pointed. Every occurrence/nth value was re-measured and none changed.
  - Added arm 0 cause (v) and arm N, which parks `h_set` first.
  - Sentinel is `ALL 16 ARMS EXECUTED (A, B, C, D, E, F, G, H, I, J, K, L, M1, M2, N, S1)` in all three places.

## Task Commits

1. **Task 1 (tracer): migration, snapshots, Python registration, fan-out gate arm N.** Commit `a03e008ed` (feat). Tracer gate: every `<verify>` was re-run green before expanding. ⚡ Tracer verified end-to-end — expanding.
2. **Task 2: composite gate, apply entry, twin re-point, arm N.** Commit `42a1f6e94` (test).
3. **Task 3 [BLOCKING]: pg-lane application, mutation runs, anti-vacuity, drift.** No commit. It ran commands only; the migration was not edited and the `cmp` below is silent.

## Task 3 evidence (the schema-application gate that replaces the forbidden remote push)

**No Supabase CLI, no psql against any remote and no remote-database command was run in this plan.** All SQL ran on `scripts/pg-lane/run.sh` (throwaway cluster, 127.0.0.1), under the host lane lock.

- **Direct lane runs.** Both exit 0 with zero `TEST FAILED` lines:
  - fan-out: `ALL 19 ARMS EXECUTED (A, B, C, D, E, F, G, H, I, J, K, L, M1, M2, N, P, Q, R, S1) and passed — …`
  - composite: `ALL 16 ARMS EXECUTED (A, B, C, D, E, F, G, H, I, J, K, L, M1, M2, N, S1) and passed — …`
  - Arm N's NOTICE stream on the fan-out lane shows `enqueued 1 … ; 1 candidate(s) failed to enqueue` followed by the clean tick `enqueued 1 … ; 0 candidate(s) failed`. The composite lane shows the same pair. The second-tick value of 1 was measured on the lane, not assumed.
- **Narrowed mutation runs.** Both exit 2 with `No defects in the narrowed scope.`:
  - fan-out: `arms: 24/24/0`, `biting: 24`, `lane-invocations: 24`, arm N `RED (identity ok)`.
  - composite: `arms: 21/21/0`, `biting: 21`, `lane-invocations: 21`, arm N `RED (identity ok)`.
  - `--parse-only`: `✅ No static defects`.
- **VAC-04 acks.** `--diff-bodies` re-measured after the regeneration: `live` = `e7e99017…ce30c9` and `d1ca6102…b76152`. The file carries exactly those two.
- **Anti-vacuity (3b).** Every neuter was a SEPARATE scratch copy passed to `--apply` in the repo file's position. The repo file was never edited.
  - (i) **Ordering check.** The single-key failure block was moved to just before that body's last `pg_advisory_unlock`. The apply refused with lane exit 3: `ERROR: P0001: Migration 20260924120000: public.enqueue_ledger_refresh_for_strategies's failure-instrument block ("IF v_failed > 0 THEN") is not after the LAST advisory unlock ("pg_advisory_unlock(") …`
  - (ii) **Needle check.** `'failed_targets'` was renamed to `'failed_tgts'` in the single-key body only. The apply refused with lane exit 3: `ERROR: P0001: Migration 20260924120000: public.enqueue_ledger_refresh_for_strategies no longer records the failed-target list in its failure row (looked for "'failed_targets'") …`
  - (iii) **Arm 0 cause (v).** Each gate was run with its apply list minus the new migration. Both exited 3, and in each the first `TEST FAILED (` line was arm 0's and contained `Cause (v)`:
    - fan-out: `TEST FAILED (0): public.enqueue_ledger_refresh_for_strategies writes the dormancy instrument but its executable body never records a failed candidate … Cause (v): migration 20260924120000_ledger_fanout_failure_count.sql has not been applied to THIS database …`
    - composite: the same message for `public.enqueue_ledger_composite_refresh`.
  - Then `cmp` of the repo migration against its byte backup printed nothing (identical). Both scratch dirs were removed, and both green lane runs above were repeated after the neuters.
- **`node scripts/baseline-content-drift-check.mjs`.** Exit 1 with **exactly 2 findings**, both expected: `DRIFT enqueue_ledger_composite_refresh/0` and `DRIFT enqueue_ledger_refresh_for_strategies/0` (21 differing lines each, `MATCH 118, DRIFT 5`, 3 allowlisted rows untouched). `git diff --stat` on `scripts/baseline-content-drift-check.mjs` and `src/__tests__/baseline-content-drift-allowlist.test.ts` is empty. The only legal clearance is a post-apply `baseline.sql` re-dump from PROD, which is a credentialed founder act.
- **Which CI job reads TEST applied-ness at this base.** `grep -c '^  test-db-drift:' .github/workflows/ci.yml` = **1**, so Phase 164.4.2 is in this base (DECISION F):
  - `sql-tests` replays the new migration on its private local stack. `node scripts/check-baseline-currency.mjs --replay-set` prints `baseline-replay: 1 migration(s) newer than the dump: 20260924120000_ledger_fanout_failure_count.sql`, so cause (v) is green there.
  - Shared-TEST applied-ness is read by **`test-db-drift`** (its VAC-08 step). That job sees the migration only after `apply-test` lands it on merge.
- **D-14 reviewers are owed BEFORE push**, and dispatching them is the orchestrator's job: migration-reviewer, rls-policy-auditor and silent-failure-hunter. Merging auto-applies to TEST and then PROD with no human stop. D-12's question for the rls-policy-auditor is **"who can read `cron_runs.metadata->'failed_targets'`"**. The baseline RLS facts are quoted in the migration header:
  - RLS is enabled with no FORCE.
  - `cron_runs_admin_read` admits admins, and `cron_runs_service_role` admits service_role.
  - anon and authenticated hold table grants, but no policy admits them.

## Files Created/Modified

- `supabase/migrations/20260924120000_ledger_fanout_failure_count.sql`: the forward migration (both bodies, COMMENT/REVOKE, `$verify$`, two acks).
- `supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql`, `…/enqueue_ledger_composite_refresh.sql`: regenerated; `-- source migration:` now names the new file.
- `analytics-service/tests/test_ledger_refresh_gates.py`: pointers, superseded sets, window 5.
- `supabase/tests/test_ledger_refresh_fanout.sql`: apply entry, 20 re-pointed twins with dated prose, cause (v), arm N, sentinel 19.
- `supabase/tests/test_ledger_refresh_composite_arm.sql`: apply entry, 16 re-pointed twins with dated prose, cause (v), arm N, sentinel 16.

## Decisions Made

- The plan's check 7 said to carry the needle forward as a presence test. I made it a **count** instead: each body now has two `cron_runs` writes, so a presence test would still pass with the dormancy write deleted. The check refuses fewer than 2.
- The fan-out arm N seeds inside the arm instead of in the file's SEED block, so its rows do not exist while any earlier arm runs.
- `20260917120000` stays out of the composite apply list. The plan allowed it only if the apply failed without it, and the apply did not fail.
- Prose that described the old twin target was updated to the new file. This covered the RED-UNDER lines, the seven identical `NO nth` blocks in the fan-out gate, the S1 and R disjointness notes, and the header lineage. The older text is kept as dated lineage and not deleted.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical] Verify check 7 counts the `cron_runs` write**
- **Found during:** Task 1, while writing the verify block.
- **Issue:** Carried forward as written, check 7 is a presence test. After this migration each body carries two INSERTs into `cron_runs`, so the check would pass with the dormancy instrument deleted.
- **Fix:** `v_instr_count` is computed by counting occurrences in the stripped body, and the check refuses fewer than 2. No twin mutates either INSERT, so the change stays disjoint from the twins (the narrowed runs are all biting).
- **Files modified:** the new migration.
- **Committed in:** `a03e008ed`.

**2. [Rule 3 - Blocking] The prompt supplied neither placeholder path**
- **Found during:** Task 1 precondition.
- **Issue:** The plan says `<HOST_LOCK_DIR>` and `<MAIN_VENV_PYTHON>` are supplied in the executor prompt and to halt if they are absent. The prompt supplied neither as a literal. Its orchestrator note instead said to take the host lock by hand "under git-common-dir" and to run pytest only from `analytics-service/`.
- **Fix:** Following the orchestrator's note:
  - The lock was a directory under the git common dir, taken with `mkdir` and released with `rmdir`, each as its own command, around every lane and runner session.
  - The interpreter was the main checkout's `analytics-service` venv python, invoked by its literal absolute path from `analytics-service/`. There was no command substitution, and the existence of the path was verified first.
  - Neither path is written into any `.planning/` artifact.
- **Verification:** pytest printed `37 passed`, and a wrong path would have exited 127.

---

**Total deviations:** 2 (1 missing-critical, 1 blocking-input).
**Impact on plan:** The first makes the apply-time check stricter. The second is an input the orchestrator had already resolved. There was no scope creep.

## Issues Encountered

- The first lane invocation from zsh failed with `apply file not found`, because zsh does not word-split an unquoted variable. It was re-run through a small bash wrapper using `mapfile`, which is RESEARCH Q4's recipe. The migration and gates were not involved.
- **Not run: the Docker local-stack lane (`scripts/local-stack/run.sh up`).** The plan mandates only the pg-lane. The disk had about 12 GiB free, and CI's `sql-tests` replays this migration on that lane anyway. What remains unproven locally is the apply against the PROD-shaped `baseline.sql` catalogue, as opposed to the pg-lane fixtures. The first `sql-tests` run on the PR is that proof.

## Expected-red gates (by construction, recorded, never suppressed)

- **`baseline-content-drift`:** 2 findings (above) until a founder re-dumps `baseline.sql` after the PROD apply.
- **Vitest pins that Plan 04 owns**, measured here with 6 failing tests across 3 files; everything else in the related set passed (1195 passed):
  - `mutation-annotation-parser.test.ts`: `armsSeen` 426 → **428**, needle census 441 → **443**.
  - `mutation-runner-floors.test.ts`: two cases.
  - `contracts/ci-anti-skip-gate.contract.test.ts`: two cases, because the ci.yml per-file table still reads 15 and 18.
  - `drift-check-scripts.test.ts` and `gate-family-meta.test.ts` stayed green at this base, because ci.yml still reads `ARMS_FLOOR=213` and run.mjs still reads `ARMS_FLOOR=426`. Plan 04 moves those to their measured values.
- **TEST applied-ness in `test-db-drift`:** red until apply-on-merge.

## Known Stubs

None.

## Threat Flags

None beyond the plan's register. The only new surface is the `cron_runs.metadata.failed_targets` column. It is T-164.6-10, and the rls-policy-auditor confirms it before push.

## User Setup Required

None.

## Next Phase Readiness

- Plan 04 can proceed. It must move the arm and needle pins to **428** and **443**, and read `ARMS_FLOOR` from a full runner run, not from these numbers. It must also update the ci.yml sentinel table to 19 and 16 and move `ARMS_FLOOR=213` → 215.
- Plan 05 needs to cover two things. The runbooks should discriminate `cron_runs` rows by `error`, because a row under `ledger_refresh_fanout` no longer proves dormancy. The composite-schedule BLOCKING precondition also belongs to Plan 05.
- The three D-14 reviewers must run before push.

---
*Phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers*
*Completed: 2026-09-24*

## Self-Check: PASSED

- FOUND: supabase/migrations/20260924120000_ledger_fanout_failure_count.sql, both gate files, analytics-service/tests/test_ledger_refresh_gates.py
- FOUND: commits a03e008ed, 42a1f6e94
