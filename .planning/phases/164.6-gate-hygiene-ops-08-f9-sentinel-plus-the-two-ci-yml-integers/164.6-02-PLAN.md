---
phase: 164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ledger-refresh-marker.ts
  - src/lib/ledger-refresh-marker.test.ts
  - src/app/api/keys/sync/route.ts
  - src/app/api/keys/sync/route.test.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
autonomous: true
requirements:
  - 161.1-D13

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "After keys/sync's composite stitch_composite enqueue returns a job id whose compute_jobs.metadata.source is 'ledger-refresh' or 'ledger-refresh-composite', the row's metadata is rewritten WITHOUT source, WITH refresh_marker_retracted = the old marker and correlation_id = this request's id (D-15)"
    - "finalize-wizard's composite stitch_composite enqueue captures the RPC's returned job id and performs the same retraction (D-15, D-17)"
    - "A row whose metadata carries no ledger-refresh marker (another source, null metadata, or no row) receives no update (D-15)"
    - "A failed retraction (read error or update error) never changes the response: keys/sync still answers 202 and finalize-wizard's enqueue side effect still resolves; the failure is console.error'd and sent to captureToSentry under a distinct tag that is not the enqueue's tag (D-18)"
    - "The TS marker set LEDGER_REFRESH_JOB_SOURCES equals Python's LEDGER_REFRESH_JOB_SOURCES in analytics-service/services/job_worker.py, pinned by a vitest parity test that reads the Python file and throws at module scope if it cannot parse it (D-16)"
    - "Each site's retraction and the parity pin are observed RED when neutered and restored byte-identically (D-20)"
    - statement: "Concurrency edge (161.1-D13) — a concurrent metadata writer between the retraction's read and its write can be overwritten; this is the inherited read-modify-write residual 161.1-D12, its real closure (a merge arm in enqueue_compute_job, 161.1-D11) is out of scope, and the residual is stated in the helper's JSDoc (D-19)"
      verification: backstop
  artifacts:
    - path: src/lib/ledger-refresh-marker.ts
      provides: "LEDGER_REFRESH_JOB_SOURCES constant and retractInheritedRefreshMarker(admin, jobId, correlationId)"
      exports: ["LEDGER_REFRESH_JOB_SOURCES", "retractInheritedRefreshMarker"]
    - path: src/lib/ledger-refresh-marker.test.ts
      provides: "unit cases per marker + static parity against job_worker.py"
    - path: src/app/api/keys/sync/route.ts
      provides: "composite branch retracts an inherited marker after the enqueue, before the 202"
      contains: "retractInheritedRefreshMarker"
    - path: src/app/api/strategies/finalize-wizard/route.ts
      provides: "composite enqueue captures the job id and retracts inside its own try/catch"
      contains: "retractInheritedRefreshMarker"
  key_links:
    - from: src/app/api/keys/sync/route.ts
      to: src/lib/ledger-refresh-marker.ts
      via: "await retractInheritedRefreshMarker(admin, rpcData, correlation_id)"
      pattern: "retractInheritedRefreshMarker\\("
    - from: src/app/api/strategies/finalize-wizard/route.ts
      to: src/lib/ledger-refresh-marker.ts
      via: "await retractInheritedRefreshMarker(admin, jobId, correlationId) in the enqueue_sync_trades_job side effect"
      pattern: "retractInheritedRefreshMarker\\("
    - from: src/lib/ledger-refresh-marker.test.ts
      to: analytics-service/services/job_worker.py
      via: "readFileSync + regex over LEDGER_REFRESH_SINGLE_KEY_SOURCE / LEDGER_REFRESH_COMPOSITE_SOURCE / LEDGER_REFRESH_JOB_SOURCES"
      pattern: "job_worker\\.py"
  prohibitions:
    - requirement_id: 161.1-D13
      category: safety
      status: resolved
      verification: judgment
      statement: "A failed marker retraction must never fail or delay-fail the user's sync request; the enqueue already succeeded."
    - requirement_id: 161.1-D13
      category: transparency
      status: resolved
      verification: judgment
      statement: "A failed marker retraction must never be silent, and must never be reported under the enqueue's own failure tag."
    - requirement_id: 161.1-D13
      category: values
      status: resolved
      verification: judgment
      statement: "The retraction must never rewrite metadata on a job whose source is not one of the ledger-refresh markers."
---

<objective>
Close requirement 161.1-D13, TS half (ROADMAP Phase 164.6 success criterion 4): the two TypeScript `stitch_composite` enqueue sites — `src/app/api/keys/sync/route.ts` and `src/app/api/strategies/finalize-wizard/route.ts` — retract an inherited ledger-refresh marker exactly the way Python's `_retract_refresh_marker_on_reuse` (`analytics-service/services/ingestion/long_fetch.py`) already does, so a user-initiated composite sync that dedups onto a fan-out job is no longer treated as a background refresh.

Scope (D-01): the founder's 2026-09-17 SCOPE PRUNE reduced Phase 164.6 to criteria 2, 3 and 4; only OPS-08-TS, OPS-08-F2 and 161.1-D13 are in scope (the ROADMAP's longer requirements string names TODOS ids for DROPPED criteria). This plan owns 161.1-D13. `grep -rn '"enqueue_compute_job"' src` shows no other TS site enqueues `stitch_composite`.

Purpose: a marker Python honours must also be honoured on the TS path.
Output: `src/lib/ledger-refresh-marker.ts` (+ test with parity), two call sites wired, their tests extended.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-CONTEXT.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-RESEARCH.md
@.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-PATTERNS.md
@analytics-service/services/ingestion/long_fetch.py
@src/app/api/keys/sync/route.ts

Interfaces this plan creates:
- `export const LEDGER_REFRESH_JOB_SOURCES = ["ledger-refresh", "ledger-refresh-composite"] as const;`
- `export async function retractInheritedRefreshMarker(admin: SupabaseClient, jobId: string | null | undefined, correlationId: string): Promise<{ retracted: false } | { retracted: true; marker: string }>` — throws an `Error` (carrying the PostgREST error as `cause`) on a read or update error; the CALLER owns `console.error` + `captureToSentry` (D-18). `SupabaseClient` is a type-only import from `@supabase/supabase-js`.

Python semantics to mirror line for line (`_retract_refresh_marker_on_reuse`): no job id → return; read `compute_jobs.metadata` by id (maybe-single); return unless metadata is an object whose `source` is in the marker set; write back metadata minus `source`, plus `refresh_marker_retracted = <marker>` and `correlation_id = <correlationId>`, via `update({ metadata }).eq("id", jobId)`.

Executor constraints (this repo, this run):
- Anchors are cited BY SYMBOL; locate with `grep -n`. Never write a `path:line` anchor into `.planning/`.
- Anti-vacuity = byte backup (`cp` into a per-plan directory named `p1646-02-bak` under your scratchpad), neuter, observe RED, restore from backup, `cmp`. NEVER `git checkout --` / `git restore` / `git stash`.
- One command per verification step. No installs. No credential. `.planning/` is public: no home paths/usernames (run `npm run check:planning-hygiene` after writing the SUMMARY).
- Python is never executed by this plan. Never start uvicorn.
- Do NOT touch VERSION, the package.json version, CHANGELOG.md; do not stage `.planning/milestone.lock`, `.planning/state.json`, `.planning/phases/164.5.3-*`. No CI skip token in any commit message.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Tracer — marker helper with parity pin, wired end-to-end at keys/sync's composite branch</name>
  <files>src/lib/ledger-refresh-marker.ts, src/lib/ledger-refresh-marker.test.ts, src/app/api/keys/sync/route.ts, src/app/api/keys/sync/route.test.ts</files>
  <read_first>
    - analytics-service/services/ingestion/long_fetch.py — `_retract_refresh_marker_on_reuse` and its docstring (the three reasoning blocks to carry)
    - analytics-service/services/job_worker.py — `LEDGER_REFRESH_SINGLE_KEY_SOURCE`, `LEDGER_REFRESH_COMPOSITE_SOURCE`, `LEDGER_REFRESH_JOB_SOURCES`
    - src/app/api/strategies/csv-finalize/route.test.ts — `const CURATED_DEFAULT_COPY` (parse-at-module-scope, throw on miss)
    - src/lib/seam-venue-vocabulary.invariant.test.ts (`// @vitest-environment node`, `readFileSync` over a `.py`)
    - src/app/api/keys/sync/route.ts — the composite branch: `getCorrelationId`, `rpcData`, the `enqueued stitch_composite job=` console.log, `logAuditEventAsUser` with `kind: "stitch_composite"`, and the `assetClassErr` capture shape
    - src/app/api/keys/sync/route.test.ts — the admin `from: (table)` mock (fallthrough `return { upsert: mockUpsert }`), the `strategy_analytics` and `strategies` branches, `mockRpc`, `TEST_JOB_ID`, `TEST_CORRELATION_ID`, the `@/lib/sentry-capture` mock
    - src/__tests__/audit-coverage.test.ts header (`findImportedMutatorCalls`, pragma within 8 lines, never grow `H_0001_UNCOVERED_ALLOWLIST`)
  </read_first>
  <behavior>
    - Helper: row with `source: "ledger-refresh"` → one update whose payload has no `source`, `refresh_marker_retracted: "ledger-refresh"`, `correlation_id` set, other keys preserved; returns `{ retracted: true, marker }`.
    - Helper: same for `source: "ledger-refresh-composite"` (Pitfall 16: the UNION, not one value).
    - Helper: `source: "keys/sync"`, `metadata: null`, `data: null` → no update, `{ retracted: false }`.
    - Helper: `jobId` null/undefined/empty → no read at all.
    - Helper: read error → throws; update error → throws.
    - Parity: TS `LEDGER_REFRESH_JOB_SOURCES` as a Set equals the two Python literals; anti-vacuity `it` pins size 2 and both literal values; the module-scope parse throws if either literal or the frozenset line naming both symbols is absent.
    - keys/sync: composite enqueue returning `TEST_JOB_ID` whose row carries each marker → the update spy is called with the retracted metadata and `eq("id", TEST_JOB_ID)`; response 202.
    - keys/sync: row without a marker → update spy not called; 202.
    - keys/sync: update returns `{ error }` → 202 unchanged AND `captureToSentry` called with tag `op: "keys-sync.composite_refresh_marker_retract"`.
  </behavior>
  <action>
    Precondition step: run `node scripts/verify-plan-anchors.mjs` on this PLAN.md path and halt on a non-zero exit.

    1. Create `src/lib/ledger-refresh-marker.ts` with the interface in `<context>` (D-15). The chain is `admin.from("compute_jobs").select("metadata").eq("id", jobId).maybeSingle()`, then destructure `{ source, ...rest }` and `admin.from("compute_jobs").update({ metadata: { ...rest, refresh_marker_retracted: source, correlation_id: correlationId } }).eq("id", jobId)`. Put an `// @audit-skip:` pragma directly above the `.update(` chain (job-row provenance metadata, not a user-visible mutation). No `server-only` import. JSDoc carries, adapted from the Python docstring: why the write targets `metadata->>'source'` (two readers: the honour sites in `analytics-service/services/job_worker.py` and `sync_strategy_analytics_status`'s `is_protected` predicate); why the UNION of both markers is the safe direction (retraction is the loud direction); and ⚠️ the RESIDUAL read-modify-write (161.1-D12) with its closure 161.1-D11 out of scope (D-19). The constant carries a comment that it is parity-pinned to Python's `LEDGER_REFRESH_JOB_SOURCES` (D-16).
    2. Create `src/lib/ledger-refresh-marker.test.ts` headed `// @vitest-environment node`, with a module-scope IIFE that reads `analytics-service/services/job_worker.py` via `readFileSync(resolve(process.cwd(), ...))`, regex-extracts the two `LEDGER_REFRESH_*_SOURCE: Final[str] = "<v>"` literals, confirms the `LEDGER_REFRESH_JOB_SOURCES` frozenset line names both symbols, and THROWS with a re-point-do-not-delete message on any miss (copy the `CURATED_DEFAULT_COPY` shape). Then the unit cases from `<behavior>` using a small local fake admin client whose `from("compute_jobs")` exposes `select→eq→maybeSingle` and `update→eq` spies.
    3. keys/sync route: in the composite branch, between the `enqueued stitch_composite job=` console.log and `logAuditEventAsUser`, add an awaited call `retractInheritedRefreshMarker(admin, rpcData, correlation_id)` inside its own try/catch. The catch does `console.error` with `[keys/sync]` prefix, the strategy id and `scrubSeamError(err)`, then `captureToSentry(err, { tags: { op: "keys-sync.composite_refresh_marker_retract" }, extra: { strategy_id, job_id: rpcData, correlation_id } })` (D-18: loud, distinct tag). The 202 response is unchanged on failure. Add an `// @audit-skip:` pragma directly above the call ("job-row provenance metadata; user intent is audited by the sync.start event below"). Import the helper.
    4. keys/sync test: add an explicit `compute_jobs` branch to the admin `from: (table)` mock BEFORE the fallthrough (otherwise `.select` is undefined and the TypeError lands in the new catch, letting a test pass vacuously), with hoisted configurable `{ data: { metadata }, error }` for the read and a spy-backed `update(patch)→eq(col,val)→Promise<{ error }>`, reset in `beforeEach`. Add the three keys/sync cases from `<behavior>` (one per marker value for the positive case). Do not change `route.seam.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ledger-refresh-marker.test.ts src/app/api/keys/sync/route.test.ts</automated>
    <fails_when>non-zero exit, any failed test, "No test files found", or a module-scope parity parse Error thrown at import</fails_when>
    <automated>npx vitest run src/__tests__/audit-coverage.test.ts</automated>
    <fails_when>non-zero exit (a helper-mediated compute_jobs update reached a call site without a pragma or same-body audit)</fails_when>
  </verify>
  <acceptance_criteria>
    - `src/lib/ledger-refresh-marker.ts` exports exactly `LEDGER_REFRESH_JOB_SOURCES` and `retractInheritedRefreshMarker`, contains both strings `"ledger-refresh"` and `"ledger-refresh-composite"`, contains the text `161.1-D12`, and has no `import "server-only"`.
    - `grep -n 'compute_jobs' src/app/api/keys/sync/route.test.ts` shows the new explicit branch.
    - `grep -n 'keys-sync.composite_refresh_marker_retract' src/app/api/keys/sync/route.ts` returns one hit.
    - Both vitest files and audit-coverage are green.
  </acceptance_criteria>
  <done>The helper, its parity pin and keys/sync's retraction work end-to-end; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: finalize-wizard captures the composite job id and retracts inside its own try/catch</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.ts — `runLegacyFinalize`, its `after(async () => {` block, the `sideEffects` entry `label: "enqueue_sync_trades_job"`, the composite branch (`p_kind: "stitch_composite"`, `const { error: enqueueErr } = await admin.rpc(`), `logAuditEventAsUser` with `kind: "stitch_composite"`, the `keyLinkErr` capture (`surface: "finalize-wizard-after"`, `side_effect: "api_key_id_lookup"`), the `Promise.allSettled` loop
    - src/app/api/strategies/finalize-wizard/route.test.ts — the `@/lib/supabase/admin` mock ending in `throw new Error(\`unexpected admin from(${table})\`)`, `STATE.adminRpcCalls`, `STATE.captureToSentryCalls`, `STATE.runAfterCallback`, `STATE.afterPromise`, the existing `beforeEach`
    - src/app/api/keys/sync/route.test.ts — its `vi.mock("@/lib/correlation-id", ...)` factory (copy it)
    - src/lib/correlation-id.ts (`getCorrelationId`, `import "server-only"`, `headers()`)
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/after.md (AGENTS.md: read the installed Next.js docs before route code)
    - src/lib/ledger-refresh-marker.ts (from Task 1)
  </read_first>
  <behavior>
    - composite path, row carrying each marker → `STATE.computeJobsUpdates` holds one payload with no `source`, `refresh_marker_retracted` = marker, `correlation_id` = the mocked id, updating the id the RPC returned (`"fake-job-id"`).
    - composite path, row without marker → no update.
    - composite path, update error → the route still returns its success envelope, `STATE.captureToSentryCalls` holds a call with `side_effect: "composite_refresh_marker_retract"`, and NO call carries `side_effect: "enqueue_sync_trades_job"`.
  </behavior>
  <action>
    Route (D-17): change the composite enqueue destructure to `const { data: jobId, error: enqueueErr } = await admin.rpc(...)`. Resolve `const correlationId = await getCorrelationId()` in request scope in `runLegacyFinalize` immediately before `after(...)` is scheduled and close over it (deterministic; does not depend on after-context header semantics). Between the `if (enqueueErr) throw` and `logAuditEventAsUser`, add an awaited `retractInheritedRefreshMarker(admin, jobId, correlationId)` inside its OWN try/catch (RESEARCH Pitfall 15: an escaping error would reject the `enqueue_sync_trades_job` side effect and be captured under the enqueue's tag). The catch does `console.error` with the `[strategies/finalize-wizard]` prefix and `scrubSeamError(err)`, then `captureToSentry(err, { tags: { surface: "finalize-wizard-after", side_effect: "composite_refresh_marker_retract" }, extra: { strategy_id: resolvedId, job_id: jobId, correlation_id: correlationId } })` (D-18). Add `// @audit-skip:` directly above the call. Import `getCorrelationId` from `@/lib/correlation-id` and the helper; no other new imports. Add one comment line above the call stating the read-modify-write residual is inherited (161.1-D12, D-19) and pointing at the helper's JSDoc.

    Test (Pitfalls 13 and 14): add a `compute_jobs` branch to the admin `from()` mock BEFORE the throw, backed by `STATE.computeJobsRow`, `STATE.computeJobsReadError`, `STATE.computeJobsUpdateError` and a `STATE.computeJobsUpdates` array, all reset in the existing `beforeEach` beside `STATE.adminRpcCalls = []`. Add `vi.mock("@/lib/correlation-id", () => ({ getCorrelationId: vi.fn().mockResolvedValue(<const>), CORRELATION_HEADER: "x-correlation-id" }))` copied from keys/sync's test. Add the cases in `<behavior>` (positive case once per marker value), driving `after()` with `STATE.runAfterCallback = true` and awaiting `STATE.afterPromise`. Run the whole file: a missing branch shows up as many composite tests failing at once.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts</automated>
    <fails_when>non-zero exit, any failed test, or an "unexpected admin from(compute_jobs)" error in output</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n 'data: jobId' src/app/api/strategies/finalize-wizard/route.ts` returns the composite enqueue destructure.
    - `grep -n 'composite_refresh_marker_retract' src/app/api/strategies/finalize-wizard/route.ts` returns one hit.
    - `grep -n 'correlation-id' src/app/api/strategies/finalize-wizard/route.test.ts` shows the new mock.
    - The failure-case test asserts no Sentry call carries `side_effect: "enqueue_sync_trades_job"`.
    - The full finalize-wizard test file is green.
  </acceptance_criteria>
  <done>finalize-wizard captures the job id, retracts best-effort and loudly under its own tag; test file green.</done>
</task>

<task type="auto">
  <name>Task 3: Anti-vacuity for both sites and the parity pin; repo gates</name>
  <files>src/lib/ledger-refresh-marker.test.ts</files>
  <read_first>
    - src/app/api/keys/sync/route.ts, src/app/api/strategies/finalize-wizard/route.ts, src/lib/ledger-refresh-marker.ts (post-Task-1/2 state)
    - src/app/api/strategies/csv-finalize/route.test.ts `⭐ RED DEMO` docblock (house record format)
  </read_first>
  <action>
    Per D-20, one command per step, for each of: (i) keys/sync — comment out the `retractInheritedRefreshMarker` call; (ii) finalize-wizard — same; (iii) the helper — make it retract only `"ledger-refresh-composite"` (drop the single-key marker from the membership check); (iv) the parity pin — change one TS marker literal: (a) `cp` the file to the `p1646-02-bak` directory; (b) neuter; (c) run the relevant vitest file and record the named failing test title(s) (expected: the marker positive case for (i)/(ii), the `"ledger-refresh"` case for (iii), the parity `it` for (iv)); (d) restore by `cp`; (e) `cmp` with no output; (f) re-run green. Add a short dated `RED DEMO` docblock at the end of `src/lib/ledger-refresh-marker.test.ts` recording all four observations in the house format (no paths outside the repo). Then run the gates in `<verify>`.
  </action>
  <verify>
    <automated>npx vitest run src/lib/ledger-refresh-marker.test.ts src/app/api/keys/sync/route.test.ts src/app/api/keys/sync/route.seam.test.ts src/app/api/strategies/finalize-wizard/route.test.ts src/__tests__/audit-coverage.test.ts</automated>
    <fails_when>non-zero exit or any failed test after restoration</fails_when>
    <automated>npm test</automated>
    <fails_when>non-zero exit, or the vitest summary reports any failed test file or test (this covers every suite that scans or exercises the touched routes, e.g. the csv-finalize-*, seam-log-coverage, seam-budgets, seam-ratelimit-posture, resilient-fetch.wiring and phase-150-capital-ownership-invariant tests)</fails_when>
    <automated>npm run typecheck</automated>
    <fails_when>non-zero exit or any "error TS" line</fails_when>
    <automated>npm run lint</automated>
    <fails_when>non-zero exit, an eslint error, or a check-planning-hygiene finding</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY records four neuter → named RED → restore → silent `cmp` observations.
    - `src/lib/ledger-refresh-marker.test.ts` carries a dated RED DEMO docblock.
    - Record this plan's starting commit sha before Task 1; `git diff --stat START_SHA^..HEAD` (START_SHA = this plan's first commit) lists only the six files in `files_modified` (plus this plan's SUMMARY).
    - typecheck and lint exit 0; `npm test` exits 0 with no failed test file.
    - SUMMARY names the Python claim-time-snapshot residual and points to `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` (Plan 05) as its destination.
  </acceptance_criteria>
  <done>Every new guard in this plan was observed RED when neutered and restored byte-identically; gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route (service-role admin client) → compute_jobs | the route rewrites job metadata with BYPASSRLS privileges |
| Python worker / SQL readers ← compute_jobs.metadata.source | the rewrite changes what the honour sites and `is_protected` read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6-05 | E (Elevation) / T | retractInheritedRefreshMarker target id | high | mitigate | the job id is the one the server-side `enqueue_compute_job` RPC returned for a strategy whose ownership the route already verified; no caller-supplied id reaches the helper |
| T-164.6-06 | T (Tampering / integrity) | metadata read-modify-write | medium | accept | inherited residual 161.1-D12, stated in the helper JSDoc (D-19); closure 161.1-D11 is out of scope; the write only removes `source` and adds two keys, preserving every other key |
| T-164.6-07 | R (Repudiation) | failed retraction | medium | mitigate | console.error + captureToSentry under a distinct tag at each site (D-18); tests assert the tag and that the enqueue tag is not used |
| T-164.6-08 | I (Information disclosure) | Sentry extra / logs | low | mitigate | logged errors pass through `scrubSeamError`; extra carries only strategy id, job id, correlation id |
| T-164.6-09 | T (integrity) | marker set drift TS vs Python | medium | mitigate | static parity test reads job_worker.py and throws at module scope on a parse miss (D-16) |
| T-164.6-SC | Tampering | npm/pip installs | low | accept | no package installed |
</threat_model>

<verification>
- The five vitest files in Task 3's first verify command are green, and the full `npm test` suite is green.
- `npm run typecheck`, `npm run lint` green.
- Four anti-vacuity observations recorded.
</verification>

<success_criteria>
- 161.1-D13 (TS half) closed: both TS `stitch_composite` sites retract an inherited ledger-refresh marker with Python's semantics; a failed retraction is loud and never fails the request; each site's test goes RED when its retraction is removed.
</success_criteria>

## Artifacts this phase produces (this plan)

| Artifact | Kind | Symbol / content |
|---|---|---|
| `src/lib/ledger-refresh-marker.ts` | new module | `LEDGER_REFRESH_JOB_SOURCES`, `retractInheritedRefreshMarker` |
| `src/lib/ledger-refresh-marker.test.ts` | new test | unit cases, module-scope parity parse of `job_worker.py`, RED DEMO docblock |
| `src/app/api/keys/sync/route.ts` | modified | composite branch retraction, Sentry tag `op: "keys-sync.composite_refresh_marker_retract"` |
| `src/app/api/keys/sync/route.test.ts` | modified | explicit `compute_jobs` mock branch + cases |
| `src/app/api/strategies/finalize-wizard/route.ts` | modified | `data: jobId` capture, `correlationId` resolved before `after()`, retraction with `side_effect: "composite_refresh_marker_retract"` |
| `src/app/api/strategies/finalize-wizard/route.test.ts` | modified | `compute_jobs` mock branch, correlation-id mock, cases |

## Out of scope / deferred (recorded, not done)
- **OPEN founder item (RESEARCH Open Question 1), NOT implemented here:** Python's `run_stitch_composite_job` composite guard in `analytics-service/services/job_worker.py` reads the CLAIM-TIME `job.get("metadata")` snapshot rather than re-reading the live row, so a TS retraction that lands AFTER the worker claimed a deduped composite job does not stand down that run's Python guard (it does stand down `is_protected`; a retraction before claim is fully effective). It is data-integrity class and outside D-01's scope (criterion 4 is the TS half). **Destination (orchestrator decision, RESEARCH Open Question 1 RESOLVED):** Plan 05 adds the named BLOCKING precondition `[164.6-COMPOSITE-CLAIMTIME-SNAPSHOT]` to `docs/runbooks/ledger-refresh-go-live.md`, forbidding any schedule for the dormant composite fan-out until this guard re-reads the live row; the orchestrator reports it as an OPEN founder item needing a phase. This plan's SUMMARY names the residual and points to that precondition by its id. The Python change is NOT made in Phase 164.6.
- The merge arm in `enqueue_compute_job` (161.1-D11) that removes the read-modify-write.

<assumption_delta_decision>
Detector `detected: true`; all signals come from prose about dropped criteria. The marker set is not a new identity — it mirrors an existing Python set. Decision: `no-change`.
</assumption_delta_decision>

<output>
Create `.planning/phases/164.6-gate-hygiene-ops-08-f9-sentinel-plus-the-two-ci-yml-integers/164.6-02-SUMMARY.md` when done.
</output>
