---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/pg-lane/fixtures/33-fixture-cron-runs.sql
  - supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql
autonomous: true
requirements:
  - 164.7-WR02-SERVICE-ROLE-EXECUTE
  - APPGUC-WARNING-UNINSTRUMENTED-01
  - 164.7-DORMANCY-UNINSTRUMENTED
tags: [sql-migration, pg-lane, cron-runs, dormancy-instrument]

estimate:
  tokens: 130000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Both ledger fan-outs (`enqueue_ledger_refresh_for_strategies`, `enqueue_ledger_composite_refresh`) are re-defined from their COMMITTED SNAPSHOTS with the SAME dormancy guard text, plus an instrument: `GET DIAGNOSTICS … ROW_COUNT` after the flag read and a `v_read_failed` flag set after the handler's assignment, and in the dormant branch ONE counted `public.cron_runs` row naming the cause for the two causes that are invisible today (`flag_row_invisible_or_absent`, `flag_read_failed`) — TODOS APPGUC-WARNING-UNINSTRUMENTED-01 (:7873) and 164.7-DORMANCY-UNINSTRUMENTED (:7950), WR-10's three causes"
    - "EXECUTE on both fan-outs is revoked from PUBLIC, anon, authenticated AND service_role, and the migration's DO block asserts the WHOLE grantee set equals the owner via aclexplode — ROADMAP criterion 4, TODOS 164.7-WR02 (:7944)"
    - "The DO block asserts each body by the sibling's variable-bound needles (`FROM public.system_flags`, `v_enabled IS DISTINCT FROM TRUE`) plus the new `INSERT INTO public.cron_runs`, keeps the CONCATENATED absent-GUC needle, and reads NO table rows (criterion 7)"
    - "A lane fixture `33-fixture-cron-runs.sql` mirrors `public.cron_runs` exactly so the fan-out gates' lanes can apply the new migration (the real `20260408113029` cannot enter those lists: its policies resolve `profiles.is_admin` and `auth.role()`, neither in the lists — PATTERNS correction)"
    - "The three gate-arm find strings inside the guard (A/K adjacency `IF v_enabled IS DISTINCT FROM TRUE THEN` + NOTICE line; L's handler WARNING + `v_enabled := NULL;`) survive byte-for-byte in both new bodies"
  artifacts:
    - "scripts/pg-lane/fixtures/33-fixture-cron-runs.sql"
    - "supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql"
  key_links:
    - "Fixture 33 precedes the new migration in both fan-out apply lists (plan 03) — the bodies' INSERT resolves `public.cron_runs` at CALL time, but the DO's needle check is catalogue-only so the apply never needs a row"
    - "Cause strings are assigned ONCE per body (`v_cause := '<cause>';`) so plan 03's instrument twins can mutate one occurrence with `nth`"
    - "Migration needles vs gate finds stay DISJOINT (PATTERNS § Disjointness); the instrument cause strings are gate-mutable and are NOT needles"
---

<objective>
Ship the LEDGER half of the phase's repair: a second forward migration that widens the two fan-outs' REVOKE to the whole role set, asserts the whole grantee set, and closes the dormancy blind spot with ONE SQL instrument writing a counted `cron_runs` row for the two causes nobody can see today — plus the lane fixture that lets the fan-out gates apply it.

Why a second file (recorded, not silent): the fan-out gates' lanes carry the ledger stack and no vault stand-in; the tick gate's lane is the reverse (RESEARCH §Q4). One file cannot apply on either lane. Plan 01 + plan 02 = one shipped repair, one PR (plan 06).

Purpose: ROADMAP criterion 4 for the fan-outs; APPGUC-WARNING-UNINSTRUMENTED-01 + 164.7-DORMANCY-UNINSTRUMENTED (re-routed here 2026-09-10 because the instrument is SQL in the `20260907130000` bodies; a prober-side `system_flags` read-back was REJECTED by measurement — it cannot distinguish WR-10's third cause).
Output: `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql`, `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-CONTEXT.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md
@supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
@supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
@supabase/schema/functions/enqueue_ledger_composite_refresh.sql
@scripts/pg-lane/fixtures/31-fixture-system-flags.sql
</context>

<interfaces>
Created here, consumed by plan 03 (gate arms) and plan 04 (acks, snapshots):

- Migration file: `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql`.
- Fixture: `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` creating `cron_runs` (unqualified, like fixture 31) with RLS ON and no policies.
- Per-body local variables: `v_found INTEGER := NULL;` `v_read_failed BOOLEAN := FALSE;` `v_cause TEXT;`
- Cause strings (assigned exactly ONCE per body, so exactly TWICE in the file): `v_cause := 'flag_row_invisible_or_absent';` and `v_cause := 'flag_read_failed';`. Cause 2 (`v_enabled = FALSE`, healthy-dormant) writes NO row (decision, see task 2).
- Instrument row: `INSERT INTO public.cron_runs (cron_name, status, completed_at, error, metadata)` with `cron_name = 'ledger_refresh_fanout'`, `status = 'error'`, `error = v_cause`, `metadata = jsonb_build_object('function', '<fn>', 'cause', v_cause)`.
- Gate-find strings that MUST survive byte-for-byte in BOTH bodies (PATTERNS § ledger migration):
  - `IF v_enabled IS DISTINCT FROM TRUE THEN` immediately followed (next line, 4-space indent) by `RAISE NOTICE '<fn>: dormant` (A and K finds)
  - `'<fn>: activation flag read failed (SQLSTATE %); treating as dormant', SQLSTATE;` followed on the next line by `v_enabled := NULL;` (L find)
  - every other `find` in PATTERNS' re-point tables (e.g. G2's `       LIMIT 4` with seven leading spaces, `INTERVAL '20 hours'` twice)
- Needles: `FROM public.system_flags`, `v_enabled IS DISTINCT FROM TRUE`, `INSERT INTO public.cron_runs`, and the concatenated absent-GUC needle from `20260907130000:776-783`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Lane fixture 33 — a `cron_runs` stand-in the fan-out lanes can apply</name>
  <files>scripts/pg-lane/fixtures/33-fixture-cron-runs.sql</files>
  <read_first>
    - scripts/pg-lane/fixtures/31-fixture-system-flags.sql (the whole file — the house pattern for "a table whose creating migration cannot enter an apply list": header explains why, mirrors the columns EXACTLY, RLS ON with no policies, "never a second base")
    - supabase/migrations/20260408113029_cron_heartbeat.sql:31-39 (the column contract: `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `cron_name TEXT NOT NULL`, `started_at`, `completed_at`, `status TEXT NOT NULL DEFAULT 'running' CHECK (status IN ('running', 'ok', 'error'))`, `error TEXT`, `metadata JSONB`), :52 (RLS), :54-64 (the two policies referencing `profiles.is_admin` and `auth.role()` — the reason the real migration cannot enter the fan-out lists)
    - scripts/lint-sql-gates.mjs:176-215 (R5/R6/R7 — a fixture must not shadow a table/policy a later list entry creates)
    - scripts/mutation-runner/GRAMMAR.md (rule 4: no twin may target a fixture)
  </read_first>
  <action>
    Create `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` on the fixture-31 pattern. Header comment names: the production creator `20260408113029_cron_heartbeat.sql:31-39` (`CREATE TABLE IF NOT EXISTS cron_runs`); WHY that migration cannot enter the fan-out apply lists (its `cron_runs_admin_read` policy resolves `profiles.is_admin`, provided on the lane only by fixture 12, and its `cron_runs_service_role` policy resolves `auth.role()`, provided only by fixture 15 — policy creation resolves columns and functions at creation time, so the apply would abort with 42703; neither fixture is in `test_ledger_refresh_fanout.sql:194` or `test_ledger_refresh_composite_arm.sql:206`, both `RED-UNDER-SETUP` lists); that the ONLY lane writer is a SECURITY DEFINER body whose owner is RLS-exempt (arm J asserts that exemption), so RLS is ENABLED with NO POLICIES deliberately; that this file creates one table and drops nothing (01-fixture-core.sql remains the only destructive fixture); and that on shared TEST the real table exists (baseline restore 2026-09-08). Body: `CREATE TABLE cron_runs (…)` mirroring the seven columns, types, defaults and the status CHECK exactly; `ALTER TABLE cron_runs ENABLE ROW LEVEL SECURITY;`. No index, no policy, no data. Unqualified table name exactly like fixture 31 (the lane's search_path is public). Do not add a `-- APP-GUC-LINEAGE:` header (no app-GUC token in this file).
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && F=scripts/pg-lane/fixtures/33-fixture-cron-runs.sql && test -f "$F" && grep -qF 'CREATE TABLE cron_runs' "$F" && grep -qF "CHECK (status IN ('running', 'ok', 'error'))" "$F" && grep -qF 'ALTER TABLE cron_runs ENABLE ROW LEVEL SECURITY;' "$F" && test "$(grep -ciF 'CREATE POLICY' "$F")" = 0 && test "$(grep -ciE '^\s*(DROP|TRUNCATE|DELETE|INSERT)\b' "$F")" = 0 && grep -qF 'metadata' "$F" && grep -qF 'gen_random_uuid()' "$F" && node scripts/lint-app-guc.mjs && echo FIXTURE33-OK</automated>
    <fails_when>The file is missing, any required DDL string is absent, a `CREATE POLICY` or destructive statement is present (count must be 0), `lint-app-guc.mjs` reports a finding or exits non-zero, or `FIXTURE33-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` exists, git-tracked after the commit.
    - Contains `CREATE TABLE cron_runs` with columns `id`, `cron_name`, `started_at`, `completed_at`, `status`, `error`, `metadata` and the exact CHECK `status IN ('running', 'ok', 'error')`.
    - Contains `ALTER TABLE cron_runs ENABLE ROW LEVEL SECURITY;` and ZERO `CREATE POLICY`, ZERO DROP/TRUNCATE/DELETE/INSERT statements.
    - Header names `20260408113029_cron_heartbeat.sql`, `profiles.is_admin`, `auth.role()` and the two fan-out apply-list line numbers as the reason the real migration cannot be listed.
  </acceptance_criteria>
  <done>The fan-out lanes have a `cron_runs` provider that mirrors the production contract, carries no policy, and is lint-clean.</done>
</task>

<task type="auto">
  <name>Task 2: The ledger migration — whole-set REVOKE + dormancy instrument in both fan-out bodies, catalogue-only verify</name>
  <files>supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql</files>
  <read_first>
    - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql (267 lines) and supabase/schema/functions/enqueue_ledger_composite_refresh.sql (251 lines) — the LEFT side of both re-bases; copy byte-for-byte
    - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql — structure (header :1-60, acks :6-11 and :133-152, re-base :154-160), handler rule :243-245, the guards :250-261 and :509-520, REVOKEs :470-471 and :735-736, verify DECLARE :740-783 (incl. the CONCATENATED absent-GUC needle :776-783), FOREACH loop :784-924, and :926-933 (the flag-row DATA read that MUST NOT be re-run — `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`'s named specimen)
    - supabase/migrations/20260408215026_schedule_match_cron_hourly.sql:37-38 (the house `cron_runs` INSERT shape from a definer that runs as postgres)
    - supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql:441 (`GET DIAGNOSTICS v_rows = ROW_COUNT;` precedent)
    - supabase/migrations/20260515205431_sec_def_public_execute_guard.sql:86-90, :116 (aclexplode idiom; service_role REVOKE precedent)
    - scripts/lint-app-guc.mjs:138-142 (`DETECT_RE`; the linter scans migrations RAW — see its header :1-20 — and a new file cannot be allowlisted, :279+)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md § ledger migration (the three finds that must survive; where cause-branching may go) and § Disjointness
  </read_first>
  <action>
    Create `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql`, structurally a copy of `20260907130000` (header -> TWO ack blocks -> re-base note -> two CREATE OR REPLACE + COMMENT + REVOKE -> one `DO $verify$` with a FOREACH over both names). If a later-stamped migration lands on main first, re-stamp later and keep the timestamp greater than plan 01's file.

    HEADER: phase/criteria; the two VAC-04 ack blocks in the `20260907130000:133-152` shape with the pragma lines DELIBERATELY ABSENT (plan 04 earns and adds them) and the "EARNED, not pasted" paragraph; the re-base note (LEFT side = the committed snapshots, never the 20260907 migration text); the apply-time prohibition (this block must never call either function); BOTH disjointness lists from PATTERNS with the sentence that the instrument cause strings are gate-mutable and deliberately NOT needles; the WR-10 three-cause table (1 read raised -> WARNING then dormant; 2 row FALSE -> NOTICE dormant; 3 row present but invisible -> `SELECT INTO` NULL -> NOTICE byte-identical to 2) and the DECISION recorded as Claude's discretion (CONTEXT § Claude's Discretion; RESEARCH A4): rows are written for causes 1 and 3 ONLY — the two that are invisible today — so a healthy dormant platform writes nothing (no `cron_runs` purge exists; 2 functions x 24 ticks/day would otherwise accrue 48 rows/day); the retention note.

    BODIES: copy each snapshot body byte-for-byte and change ONLY: (a) DECLARE gains `v_found INTEGER := NULL;`, `v_read_failed BOOLEAN := FALSE;`, `v_cause TEXT;`; (b) inside the guard's `BEGIN … EXCEPTION` block, immediately AFTER the `SELECT sf.enabled INTO v_enabled …` statement and BEFORE `EXCEPTION WHEN OTHERS THEN`, add `GET DIAGNOSTICS v_found = ROW_COUNT;`; (c) in the handler, AFTER the existing `v_enabled := NULL;` line, add `v_read_failed := TRUE;` (an assignment, which 130000:243-245 permits; L's find ends at `v_enabled := NULL;` so the needle is intact); (d) inside `IF v_enabled IS DISTINCT FROM TRUE THEN`, keep `RAISE NOTICE '<fn>: dormant …'` as the FIRST statement (A/K adjacency), then add the cause branch AFTER the NOTICE and BEFORE `RETURN 0;`: `IF v_read_failed THEN v_cause := 'flag_read_failed'; ELSIF v_found = 0 THEN v_cause := 'flag_row_invisible_or_absent'; ELSE v_cause := NULL; END IF;` followed by `IF v_cause IS NOT NULL THEN INSERT INTO public.cron_runs (cron_name, status, completed_at, error, metadata) VALUES ('ledger_refresh_fanout', 'error', now(), v_cause, jsonb_build_object('function', '<fn>', 'cause', v_cause)); END IF;`. Schema-qualify `public.cron_runs` (the bodies pin search_path). Each cause literal appears exactly ONCE per body. Everything else — every `find` in PATTERNS' two re-point tables, the NOTICE/WARNING texts, `       LIMIT 4`, `INTERVAL '20 hours'` — stays byte-identical; a drifted byte is an `occurrence-mismatch` in plan 03.

    COMMENT ON FUNCTION for both (re-state the snapshot comments; add one sentence naming the instrument row).

    REVOKE both: `REVOKE ALL ON FUNCTION public.<fn>() FROM PUBLIC, anon, authenticated, service_role;` — no GRANT; the scheduler runs as the owner (cron-manifest.json rows are all `postgres`).

    `DO $verify$` — catalogue only, `RAISE EXCEPTION 'Migration 20260911130000: …'` by name. DECLARE the needles as variables with derivation comments: `v_flag_needle := 'FROM public.system_flags'`, `v_null_needle := 'v_enabled IS DISTINCT FROM TRUE'`, `v_instrument_needle := 'INSERT INTO public.cron_runs'`, and the CONCATENATED absent-GUC needle copied verbatim from 130000:776-783 (do NOT tidy it into one literal — `lint-app-guc.mjs` scans this file RAW and a new file cannot be allowlisted). FOREACH over both function names: (1) present, `pronargs = 0`; (2) `prosecdef` and `proconfig` search_path; (3) owner `rolsuper OR rolbypassrls`; (4) strip `regexp_replace(v_def_raw, '--[^\n]*', '', 'g')`, length sanity `< 500`; (5) `position(v_flag_needle IN v_def) = 0 -> RAISE`; (6) same for `v_null_needle`; (7) same for `v_instrument_needle` (message: the dormancy instrument is gone — a decline would again reach no counted row); (8) `position(v_guc_needle IN v_def) <> 0 -> RAISE` (the retired app-namespace setting is still absent); (9) grantee set == owner via `aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner)))`, `string_agg(CASE WHEN a.grantee = 0 THEN 'PUBLIC' ELSE pg_get_userbyid(a.grantee) END, ',' ORDER BY 1)` compared `IS DISTINCT FROM pg_get_userbyid(p.proowner)`; a NULL aggregate is its own RAISE. Also carry the comment-strip residual note (`/* */` not stripped; measured 0 occurrences) and the CORRECTED direction note: for the ABSENCE check (8) a `--` inside a string literal eating code would be a false PASS, for the presence checks (5)-(7) a false FAILURE — measured 0 code lines carry a trailing `--` in either body. NEVER re-run the flag-row read at 130000:926-933; NEVER read rows back from the instrument table or the flags table at apply time — the ONLY table reads in the DO are `pg_proc`/`pg_namespace`/`pg_roles` plus `pg_get_functiondef` (criterion 7: shared TEST tables are EMPTY; a refusal on TEST blocks the PROD apply; remedy is to revert the merge, never to edit supabase-migrate.yml).
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && F=supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql && test -f "$F" && test "$(grep -cF "v_cause := 'flag_row_invisible_or_absent';" "$F")" = 2 && test "$(grep -cF "v_cause := 'flag_read_failed';" "$F")" = 2 && test "$(grep -cF 'GET DIAGNOSTICS v_found = ROW_COUNT;' "$F")" = 2 && test "$(grep -cF 'v_read_failed := TRUE;' "$F")" = 2 && test "$(grep -v '^\s*--' "$F" | grep -cF 'INSERT INTO public.cron_runs')" = 2 && test "$(grep -v '^\s*--' "$F" | grep -cF 'FROM public.system_flags')" = 2 && test "$(grep -v '^\s*--' "$F" | grep -cF 'v_enabled IS DISTINCT FROM TRUE')" = 3 && test "$(grep -cF 'FROM PUBLIC, anon, authenticated, service_role;' "$F")" = 2 && grep -qF "aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner)))" "$F" && grep -qF "v_instrument_needle" "$F" && grep -qF "'current_' || 'setting(''app.' || 'ledger_refresh_enabled'''" "$F" && test "$(grep -v '^\s*--' "$F" | grep -cF 'v_seeded')" = 0 && test "$(grep -v '^\s*--' "$F" | grep -ciE 'FROM public\.cron_runs|SELECT .*INTO v_seeded')" = 0 && grep -A1 -F 'IF v_enabled IS DISTINCT FROM TRUE THEN' "$F" | grep -cF "RAISE NOTICE '" | grep -qx 2 && node scripts/lint-app-guc.mjs && echo LEDGER-SHAPE-OK</automated>
    <fails_when>Any pinned count differs (each cause assignment, `GET DIAGNOSTICS`, `v_read_failed := TRUE;`, the INSERT needle, `FROM public.system_flags`, the four-role REVOKE must occur exactly TWICE in the file; `v_enabled IS DISTINCT FROM TRUE` exactly THREE times in non-comment text — once per body plus the needle declaration; `v_seeded` and any apply-time SELECT from `public.cron_runs` ZERO times), the A/K adjacency check finds fewer than 2 `RAISE NOTICE` lines directly following the guard `IF`, `lint-app-guc.mjs` prints a finding or exits non-zero, or `LEDGER-SHAPE-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - The migration exists, git-tracked after the commit; both `CREATE OR REPLACE FUNCTION public.enqueue_ledger_refresh_for_strategies()` and `…enqueue_ledger_composite_refresh()` are present.
    - Each body contains exactly one `GET DIAGNOSTICS v_found = ROW_COUNT;`, one `v_read_failed := TRUE;`, one `v_cause := 'flag_read_failed';`, one `v_cause := 'flag_row_invisible_or_absent';`, one `INSERT INTO public.cron_runs`.
    - `IF v_enabled IS DISTINCT FROM TRUE THEN` is immediately followed by the `RAISE NOTICE '<fn>: dormant` line in both bodies (A/K finds intact); the handler's `v_enabled := NULL;` line directly follows the WARNING line in both (L find intact).
    - Both REVOKEs name `PUBLIC, anon, authenticated, service_role`; no GRANT on either function.
    - The DO block declares `v_flag_needle`, `v_null_needle`, `v_instrument_needle` and the concatenated `v_guc_needle`, and contains the aclexplode whole-set comparison against `pg_get_userbyid(p.proowner)`.
    - Non-comment text contains ZERO `v_seeded` and ZERO apply-time reads of `public.cron_runs` or of `system_flags` rows (only `pg_get_functiondef`/`pg_proc` reads).
    - `node scripts/lint-app-guc.mjs` reports 0 findings (the concatenated needle keeps `DETECT_RE` quiet).
  </acceptance_criteria>
  <reversibility rating="costly">Reversible until merge (the one-way door is the merge, gated by plan 06's checkpoint:decision). Costly because 26 gate arms (plan 03) and two earned acks (plan 04) bind to this file's exact bytes.</reversibility>
  <done>Both fan-outs are re-based with the instrument and the widened REVOKE, the verify block is catalogue-only with four variable-bound needles and the whole-set check, and the shape gate prints LEDGER-SHAPE-OK.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 02's share)

- NEW `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` — lane stand-in for `public.cron_runs`, RLS ON, no policies.
- NEW `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql` — both fan-outs re-based with `v_found`/`v_read_failed`/`v_cause`, the `public.cron_runs` instrument row (`cron_name = 'ledger_refresh_fanout'`, `status = 'error'`, causes `flag_read_failed` / `flag_row_invisible_or_absent`), four-role REVOKEs, `DO $verify$` with `v_flag_needle`, `v_null_needle`, `v_instrument_needle`, `v_guc_needle` and the aclexplode whole-set check. Its two `-- prod-body-ack:` lines are added by plan 04.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB roles -> EXECUTE on the fan-outs | browser roles and service_role must not invoke the SECURITY DEFINER fan-outs |
| definer body -> `public.cron_runs` | the instrument writes a row through RLS by owner exemption; row content must carry a cause, never a value |
| migration merge -> PROD | auto-apply after shared TEST |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-07 | Elevation of Privilege | fan-out EXECUTE ACLs | high | mitigate | REVOKE from service_role + whole-set assertion; gate arm S1 (plan 03) proves a GRANT reddens |
| T-164.8.6-08 | Repudiation | dormancy causes 1 and 3 leave no counted trace | medium | mitigate | `cron_runs` row per invisible cause; asserted by needle at apply and by arms M1/M2 at run time |
| T-164.8.6-09 | Denial of Service (storage) | unbounded `cron_runs` growth while healthy-dormant | low | mitigate | no row for cause 2 (`flag_false_by_design`); retention note recorded |
| T-164.8.6-10 | Denial of Service (deploy) | DATA read in the DO block refuses on empty shared TEST | high | mitigate | catalogue-only; ZERO `v_seeded`; acceptance grep |
| T-164.8.6-11 | Tampering | fixture 33 shadowing a real table on TEST | low | accept | fixtures apply only on the throwaway lane (fixture 32 header precedent); never applied to TEST/PROD |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | no packages installed by this phase |
</threat_model>

<verification>
- Task 1 verify prints `FIXTURE33-OK`; task 2 verify prints `LEDGER-SHAPE-OK`.
- `node scripts/lint-app-guc.mjs` stays at 0 findings with the new migration present.
- Lane proof of this migration happens in plan 03 (the fan-out gates' narrowed runs), which is the only lane that can apply it.
</verification>

<success_criteria>
- Criterion 4 implemented for both fan-outs; APPGUC/DORMANCY instrument present in both bodies; the three gate finds and every re-point `find` string survive byte-for-byte; verify block is catalogue-only.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-02-SUMMARY.md` when done; record the cause-2 no-row decision and the retention note there.
</output>
