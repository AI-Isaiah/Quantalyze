---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 03
type: execute
wave: 2
depends_on:
  - 164.8.6-02
files_modified:
  - supabase/tests/test_ledger_refresh_fanout.sql
  - supabase/tests/test_ledger_refresh_composite_arm.sql
autonomous: true
requirements:
  - 164.7-WR02-SERVICE-ROLE-EXECUTE
  - APPGUC-WARNING-UNINSTRUMENTED-01
  - 164.7-DORMANCY-UNINSTRUMENTED
tags: [sql-gate, mutation-runner, pg-lane, dormancy-instrument]

estimate:
  tokens: 150000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Both fan-out gates apply `33-fixture-cron-runs.sql` then `20260911130000_ledger_fanout_grantees_and_dormancy.sql` LAST, and every `edit` twin that targets a function BODY (14 in the fan-out gate, 12 in the composite gate) is re-pointed at the new migration with re-measured `occurrences`/`nth`; the two composite precondition arms that edit `20260825120000` (the VIEW) and every `sql` twin (0, I, J) stay as they are"
    - "Each gate gains three RED-UNDER-M arms: `S1` (service_role GRANT -> whole-set RAISE, placed AFTER arm J), `M1` (missing flag row -> exactly one `cron_runs` row with cause `flag_row_invisible_or_absent`, reusing arm A's state), `M2` (raising read -> exactly one row with cause `flag_read_failed`, probed INSIDE arm L's outer block body before the P0164 unwind)"
    - "Narrowed runner runs over both gates report `No defects in the narrowed scope.` — every arm bites its own arm first, no waiver"
  artifacts:
    - "supabase/tests/test_ledger_refresh_fanout.sql (apply list :194 extended; 14 re-points; arms S1/M1/M2; roster `ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)`)"
    - "supabase/tests/test_ledger_refresh_composite_arm.sql (apply list :206 extended; 12 re-points; arms S1/M1/M2; roster `ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)`)"
  key_links:
    - "Apply-list coupling: an edit twin may only target a file in that gate's RED-UNDER-SETUP list (scripts/mutation-runner/run.mjs:4085-4094, `bad-file-ref`); a later CREATE OR REPLACE overwrites any mutation of the earlier file (Pitfall 1)"
    - "Instrument twins mutate `v_cause := '<cause>';` (occurrences 2 in the new file: fan-out body first, composite body second -> `nth` 1 in the fan-out gate, `nth` 2 in the composite gate); the migration needle is `INSERT INTO public.cron_runs`, so the mutation never aborts the apply"
    - "S1 after J: J's twin grants EXECUTE ON ALL FUNCTIONS to a drift role and changes the owner; a whole-set arm before J would be the first failure under J's twin"
---

<objective>
Re-point and extend both ledger gates so the new ledger migration is the definition their arms run against, and add the three arms per gate that make criterion 4 (whole grantee set) and the dormancy instrument falsifiable on the lane.

Purpose: the mutation-runner's apply-list topology is the real constraint of this phase — 26 body-editing arms silently un-bite the moment plan 02's file is appended unless each is re-pointed (RESEARCH Pattern 3, Pitfall 1). This plan pays that cost explicitly, arm by arm.
Output: two gate files, each green on its lane with 15 arms, and two narrowed runner reports pasted into the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md
# read at execution when present (produced by an earlier plan of this phase): .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-02-SUMMARY.md
@supabase/tests/test_ledger_refresh_fanout.sql
@supabase/tests/test_ledger_refresh_composite_arm.sql
# read at execution (produced by plan 02): supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql
@scripts/mutation-runner/GRAMMAR.md
</context>

<interfaces>
From plan 02 (consumed here): migration path `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql`; fixture path `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql`; cause assignments `v_cause := 'flag_row_invisible_or_absent';` and `v_cause := 'flag_read_failed';` (each occurs TWICE in the file: fan-out body first, composite body second); instrument row `cron_name = 'ledger_refresh_fanout'`, `metadata->>'function'` = the function name.

Created here: arm ids `S1`, `M1`, `M2` in BOTH gates (unused in either file today — existing ids are A-L, G1, G2, H/inactive, H/revoked, H/disconnected, D/precondition, E1/precondition, E1/mixed, E2/all); roster sentinels `ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)`; RAISE texts `TEST FAILED (S1)`, `TEST FAILED (M1)`, `TEST FAILED (M2)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `test_ledger_refresh_fanout.sql` — apply list, 14 re-points, arms S1/M1/M2, roster 12 -> 15</name>
  <files>supabase/tests/test_ledger_refresh_fanout.sql</files>
  <read_first>
    - supabase/tests/test_ledger_refresh_fanout.sql — whole file once: header :150-192 (sentinel pinned to ONE occurrence; :182-186 why the last entry matters), apply list :194, arm A :476-509, arm K :517-526, arm L :578-604 (the P0164 unwind at :589-604), arm I :943-955, arm J :1010-1025, roster :1027
    - supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql (plan 02 output — measure every `find` against THIS file)
    - scripts/mutation-runner/GRAMMAR.md:73-77 (`occurrences` measured; `nth` selects the occurrence), :100-118, :540-552
    - scripts/lint-sql-gates.mjs:121-133 (`R1-exception-handler-probe`: no DML or SELECT-INTO inside an EXCEPTION handler — the M2 probe goes in the block BODY)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md § fan-out gates (re-point table, arm-I analog, arm-A analog for M1, arm-L analog for M2, ORDER rule for S1)
  </read_first>
  <action>
    1. APPLY LIST (`:194`): append, in this order, `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` then `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql` (LAST). Add the prose line: fixture 33 precedes the new migration the way fixture 31 precedes its reader (`:174-181`), and the new migration is applied LAST because its CREATE OR REPLACE is the definition every edit twin below now names.

    2. RE-POINT all 14 body-editing twins from `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql` to the new migration — `file` changes, `find`/`replace` stay, `occurrences` (and `nth` where present) are RE-MEASURED against the new file with `grep -cF` before writing (GRAMMAR rule 2: `occurrence-mismatch` is a MEASURE_FAIL). The enumerated set, by current line: A `:496`, K `:526`, L `:588`, B `:620`, E `:671` (3 steps, `nth:1`), C `:701` (`nth:1`), D `:733`, F `:769` (`nth:1`), G1 `:802`, G2 `:835` (find `       LIMIT 4`, seven leading spaces), H `:866` (`nth:1`), H/inactive `:885` (`nth:1`), H/revoked `:900` (`nth:1`), H/disconnected `:915` (`nth:1`). Twins that STAY as they are: 0 `:253`, I `:948`, J `:1012` (`sql` steps). After the edit, ZERO `RED-UNDER-M` lines in this file may name `20260907130000`.

    3. ARM M1 (missing-row cause; APPGUC/DORMANCY): immediately after arm A's last assertion (A has already DELETEd the flag row and called the fan-out), add `SELECT count(*) INTO v_cnt_m1 FROM public.cron_runs WHERE cron_name = 'ledger_refresh_fanout' AND error = 'flag_row_invisible_or_absent' AND metadata->>'function' = 'enqueue_ledger_refresh_for_strategies'` and `IF v_cnt_m1 <> 1 THEN RAISE EXCEPTION 'TEST FAILED (M1): the fan-out declined with NO activation row and wrote % instrument row(s) naming flag_row_invisible_or_absent, expected exactly 1 — the decline reached no counted instrument (WR-10 cause 3 / APPGUC-WARNING-UNINSTRUMENTED-01) …'`. Reading a row this transaction wrote is fine (not an apply-time read). Prose `-- RED-UNDER:` line + twin on ONE line: `{"arm":"M1","apply":[{"kind":"edit","file":"supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql","find":"v_cause := 'flag_row_invisible_or_absent';","replace":"v_cause := NULL;","occurrences":2,"nth":1}]}` — under it the fan-out body writes no row (the `IF v_cause IS NOT NULL` gate skips the INSERT) and only M1 reddens; the composite body (nth 2) is untouched. The cause string is NOT a migration needle, so the apply survives.

    4. ARM M2 (raising-read cause): inside arm L's OUTER `BEGIN` (`:589-604`), AFTER the inner call block's `END;` and BEFORE the `RAISE EXCEPTION USING ERRCODE = 'P0164'` unwind, add `SELECT count(*) INTO v_cnt_m2 FROM public.cron_runs WHERE cron_name = 'ledger_refresh_fanout' AND error = 'flag_read_failed' AND metadata->>'function' = 'enqueue_ledger_refresh_for_strategies'` and `IF v_cnt_m2 <> 1 THEN RAISE EXCEPTION 'TEST FAILED (M2): the flag read RAISED and the fan-out wrote % instrument row(s) naming flag_read_failed, expected exactly 1 …'`. It MUST sit in the block BODY (lint R1 flags handlers, not bodies) and BEFORE the unwind (the P0164 rollback discards the row; the plpgsql local survives — `:571-573`); its P0001 is not swallowed by the P0164-only handler (`:575-577`). Twin: `{"arm":"M2","apply":[{"kind":"edit","file":"supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql","find":"v_cause := 'flag_read_failed';","replace":"v_cause := NULL;","occurrences":2,"nth":1}]}`.

    5. ARM S1 (whole grantee set; 164.7-WR02): AFTER arm J (`:1010-1025`), before the roster NOTICE. `sql` twin (an edit of the migration's REVOKE would abort the apply under the new whole-set check — the arm-I reasoning at `:943-947`): `{"arm":"S1","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.enqueue_ledger_refresh_for_strategies() TO service_role"}]}`. Assertion: the aclexplode SET query from the migration (grantee string vs `pg_get_userbyid(p.proowner)`), raising `TEST FAILED (S1): EXECUTE on enqueue_ledger_refresh_for_strategies is held by [%], expected exactly the owner [%] …`. ORDER rationale in the prose: J's twin grants EXECUTE ON ALL FUNCTIONS to `lrf_owner_drift` and changes the owner; placed before J, S1 would be the first failure under J's twin (`wrong-first-failure`); after J, J fails first under its own twin and S1 reddens alone under its own. Arm I's `has_function_privilege` probes stay. Pre-merge note: RED on shared TEST by construction (the OLD REVOKE left service_role its EXECUTE) — CLAUDE.md coupling (b).

    6. DECLARE: add `v_cnt_m1 INTEGER`, `v_cnt_m2 INTEGER`, `v_grantees TEXT`, `v_owner_name TEXT` (check the existing DECLARE for name collisions — `v_owner` is already used by arm J as a rolname) in the same edit.

    7. ROSTER: `:1027` -> `RAISE NOTICE 'ALL 15 ARMS EXECUTED (A-L,M1,M2,S1) and passed — …'` with one clause each for M1, M2, S1; update the header mention at `:155` to 15. Exactly ONE `RAISE NOTICE '…ALL 15 ARMS EXECUTED (…)` line (ci.yml:3163 reads the first match).
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && G=supabase/tests/test_ledger_refresh_fanout.sql && M=supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql && grep -qF '"scripts/pg-lane/fixtures/33-fixture-cron-runs.sql","'"$M"'"]}' "$G" && test "$(grep -c 'RED-UNDER-M:.*"file":"supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql"' "$G")" = 0 && test "$(grep -c 'RED-UNDER-M:.*"kind":"edit","file":"'"$M"'"' "$G")" = 16 && grep -qF '{"arm":"M1","apply":[{"kind":"edit","file":"'"$M"'","find":"v_cause := '"'"'flag_row_invisible_or_absent'"'"';","replace":"v_cause := NULL;","occurrences":2,"nth":1}]}' "$G" && grep -qF '{"arm":"M2","apply":[{"kind":"edit","file":"'"$M"'","find":"v_cause := '"'"'flag_read_failed'"'"';","replace":"v_cause := NULL;","occurrences":2,"nth":1}]}' "$G" && grep -qF '{"arm":"S1","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.enqueue_ledger_refresh_for_strategies() TO service_role"}]}' "$G" && test "$(grep -c "RAISE NOTICE 'ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)" "$G")" = 1 && test "$(grep -n '"arm":"S1"' "$G" | cut -d: -f1)" -gt "$(grep -n '"arm":"J"' "$G" | cut -d: -f1)" && node scripts/lint-sql-gates.mjs && node scripts/mutation-runner/run.mjs --parse-only && echo FANOUT-GATE-OK</automated>
    <fails_when>The apply list does not end with fixture 33 then the new migration; any RED-UNDER-M still names 20260907130000 (count must be 0); the count of edit twins naming the new file is not 16 (14 re-points + M1 + M2); the M1/M2/S1 twin JSON does not match byte-for-byte; the roster NOTICE count is not exactly 1; S1's line number is not greater than J's; lint-sql-gates or `--parse-only` exits non-zero (an `occurrence-mismatch` or `bad-file-ref` row); or `FANOUT-GATE-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Apply list at `:194` ends `…,"scripts/pg-lane/fixtures/33-fixture-cron-runs.sql","supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql"]}`.
    - 0 RED-UNDER-M twins name `20260907130000`; 16 edit twins name the new migration (A, K, L, B, E, C, D, F, G1, G2, H, H/inactive, H/revoked, H/disconnected, M1, M2); 0, I, J, S1 are `sql` twins.
    - M1 sits after arm A's assertions; M2 sits inside arm L's outer BEGIN body before the `P0164` raise (its line number is between the inner block's `END;` and the `USING ERRCODE = 'P0164'` line); S1 sits after J.
    - `TEST FAILED (M1)`, `TEST FAILED (M2)`, `TEST FAILED (S1)` RAISE texts present; DECLARE has `v_cnt_m1`, `v_cnt_m2`, `v_grantees`.
    - Exactly one `RAISE NOTICE 'ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)`; header mention updated.
    - `node scripts/lint-sql-gates.mjs` and `node scripts/mutation-runner/run.mjs --parse-only` exit 0.
  </acceptance_criteria>
  <done>The fan-out gate names the new migration in every body-editing twin, carries M1/M2/S1 with the twins above, and passes both static gates.</done>
</task>

<task type="auto">
  <name>Task 2: `test_ledger_refresh_composite_arm.sql` — apply list, 12 re-points, arms S1/M1/M2, roster 12 -> 15</name>
  <files>supabase/tests/test_ledger_refresh_composite_arm.sql</files>
  <read_first>
    - supabase/tests/test_ledger_refresh_composite_arm.sql — whole file once: header :165-205, apply list :206, arm 0 :255-300 (the `v_body` comment-strip precedent for R2), arm A :518, K :547, L :604 (P0164 unwind), D/precondition :766 and E1/precondition :836 (edit `20260825120000` — the VIEW — UNCHANGED), I :967, J :1038, roster :1053
    - supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql (measure every `find` against it; the composite body is the SECOND body, so shared finds use `nth:2`)
    - scripts/mutation-runner/GRAMMAR.md:73-77
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md § fan-out gates (composite re-point table)
  </read_first>
  <action>
    Mirror task 1 for the composite gate. 1. APPLY LIST (`:206`): append fixture 33 then the new migration LAST, with the same prose. 2. RE-POINT the 12 body-editing twins from `20260907130000` to the new migration, re-measuring `occurrences`/`nth` against the new file: A `:518`, K `:547`, L `:604`, B `:636`, F `:704` (3 steps, `nth:2`), C `:734` (`nth:2`), D `:802`, E `:853`, E1/mixed `:863`, E2/all `:873`, G `:899` (`nth:2`), H `:928`. STAY as they are: D/precondition `:766` and E1/precondition `:836` (they edit `20260825120000`, the view), 0 `:261`, I `:967`, J `:1038` (`sql`). After the edit ZERO RED-UNDER-M lines name `20260907130000`; exactly TWO still name `20260825120000`. 3. ARM M1 after arm A's assertions, filtering `metadata->>'function' = 'enqueue_ledger_composite_refresh'`, twin `{"arm":"M1","apply":[{"kind":"edit","file":"<new migration>","find":"v_cause := 'flag_row_invisible_or_absent';","replace":"v_cause := NULL;","occurrences":2,"nth":2}]}` (`nth:2` — the composite body is second in the file). 4. ARM M2 inside arm L's outer block body before the P0164 unwind, filtering the composite function name, twin with `find` `v_cause := 'flag_read_failed';`, `occurrences:2`, `nth:2`. 5. ARM S1 after J: `{"arm":"S1","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.enqueue_ledger_composite_refresh() TO service_role"}]}` with the aclexplode set assertion and `TEST FAILED (S1)`; same ORDER rationale and pre-merge note as task 1. 6. DECLARE additions as in task 1 (check collisions). 7. ROSTER `:1053` -> `ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)`; header mention `:170` updated; exactly one such NOTICE.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && G=supabase/tests/test_ledger_refresh_composite_arm.sql && M=supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql && grep -qF '"scripts/pg-lane/fixtures/33-fixture-cron-runs.sql","'"$M"'"]}' "$G" && test "$(grep -c 'RED-UNDER-M:.*"file":"supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql"' "$G")" = 0 && test "$(grep -c 'RED-UNDER-M:.*"kind":"edit","file":"'"$M"'"' "$G")" = 14 && test "$(grep -c 'RED-UNDER-M:.*"file":"supabase/migrations/20260825120000' "$G")" = 2 && grep -qF '{"arm":"M1","apply":[{"kind":"edit","file":"'"$M"'","find":"v_cause := '"'"'flag_row_invisible_or_absent'"'"';","replace":"v_cause := NULL;","occurrences":2,"nth":2}]}' "$G" && grep -qF '{"arm":"M2","apply":[{"kind":"edit","file":"'"$M"'","find":"v_cause := '"'"'flag_read_failed'"'"';","replace":"v_cause := NULL;","occurrences":2,"nth":2}]}' "$G" && grep -qF '{"arm":"S1","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.enqueue_ledger_composite_refresh() TO service_role"}]}' "$G" && test "$(grep -c "RAISE NOTICE 'ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)" "$G")" = 1 && test "$(grep -n '"arm":"S1"' "$G" | cut -d: -f1)" -gt "$(grep -n '"arm":"J"' "$G" | cut -d: -f1)" && node scripts/lint-sql-gates.mjs && node scripts/mutation-runner/run.mjs --parse-only && echo COMPOSITE-GATE-OK</automated>
    <fails_when>The apply list tail is wrong; any twin still names 20260907130000; the edit-twin count naming the new file is not 14 (12 re-points + M1 + M2); the two `20260825120000` precondition twins are not exactly 2; M1/M2/S1 JSON mismatch (note `nth:2`); roster NOTICE count not 1; S1 not after J; lint or `--parse-only` non-zero; or `COMPOSITE-GATE-OK` absent.</fails_when>
  </verify>
  <acceptance_criteria>
    - Apply list at `:206` ends with fixture 33 then the new migration.
    - 0 twins name `20260907130000`; 14 edit twins name the new migration; exactly 2 twins name `20260825120000` (D/precondition, E1/precondition); 0, I, J, S1 are `sql` twins.
    - M1/M2 twins carry `"occurrences":2,"nth":2`; S1 sits after J; M2 sits in arm L's outer block body before the `P0164` raise.
    - `TEST FAILED (M1)`, `(M2)`, `(S1)` present; exactly one `ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)` NOTICE.
    - lint-sql-gates and `--parse-only` exit 0.
  </acceptance_criteria>
  <done>The composite gate mirrors task 1 with `nth:2` instrument twins and passes both static gates.</done>
</task>

<task type="auto">
  <name>Task 3: Lane proof — narrowed runner runs over both ledger gates</name>
  <files>supabase/tests/test_ledger_refresh_fanout.sql, supabase/tests/test_ledger_refresh_composite_arm.sql</files>
  <read_first>
    - scripts/mutation-runner/run.mjs:1-30 (narrowed runs exit 2 on a clean run), :4500-4560
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-01-SUMMARY.md (the tick gate's narrowed-run block — the shape to reproduce)
  </read_first>
  <action>
    With `export PGBIN=/opt/homebrew/opt/postgresql@16/bin` exported, run each gate GREEN on a lane first (`bash scripts/pg-lane/run.sh --workdir "$SCRATCH/lane-fanout" --apply <the 15 apply-list files in order> --gate supabase/tests/test_ledger_refresh_fanout.sql`, exit 0 with `ALL 15 ARMS EXECUTED` in the stream; same for the composite gate with its 14-entry list). Then `node scripts/mutation-runner/run.mjs --file supabase/tests/test_ledger_refresh_fanout.sql` and `… --file supabase/tests/test_ledger_refresh_composite_arm.sql`: each must end `No defects in the narrowed scope.` with exit 2 and 20 twins biting first (17 existing + M1 + M2 + S1). Diagnose by defect kind: `no-red` on a re-pointed arm = a missed re-point or a `find` that now occurs in BOTH bodies without `nth`; `occurrence-mismatch` = a byte drifted in plan 02's re-base (fix the migration, and note that plan 02's shape gate must still pass); `wrong-first-failure` on S1 = it is before J; on M2 = the probe is after the unwind. Never add a waiver. Paste both final blocks into the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && export PGBIN=/opt/homebrew/opt/postgresql@16/bin && for g in supabase/tests/test_ledger_refresh_fanout.sql supabase/tests/test_ledger_refresh_composite_arm.sql; do OUT="$(node scripts/mutation-runner/run.mjs --file "$g" 2>&1)"; RC=$?; printf '%s\n' "$OUT" | tail -12; test "$RC" = 2 || exit 1; printf '%s\n' "$OUT" | grep -qF 'No defects in the narrowed scope.' || exit 1; printf '%s\n' "$OUT" | grep -qE 'no-red|wrong-first-failure|occurrence-mismatch|bad-file-ref|waiver' && exit 1; done; echo LEDGER-LANES-OK</automated>
    <fails_when>Either narrowed run exits with a code other than 2, omits `No defects in the narrowed scope.`, or prints any `no-red`/`wrong-first-failure`/`occurrence-mismatch`/`bad-file-ref`/`waiver` row; or `LEDGER-LANES-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Both gates exit 0 on `scripts/pg-lane/run.sh` with `ALL 15 ARMS EXECUTED` in the psql stream.
    - Both narrowed runner runs exit 2 with `No defects in the narrowed scope.` and no defect rows of the five named kinds.
    - SUMMARY carries both final blocks (arm counts, lane-invocations, exit codes).
  </acceptance_criteria>
  <done>The ledger migration is proven on both lanes: 26 re-pointed arms and 6 new arms bite first, zero waivers.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 03's share)

- MODIFIED `supabase/tests/test_ledger_refresh_fanout.sql` — apply list + fixture 33 + new migration; 14 twins re-pointed; NEW arms `M1`, `M2`, `S1`; DECLARE `v_cnt_m1`, `v_cnt_m2`, `v_grantees`; roster `ALL 15 ARMS EXECUTED (A-L,M1,M2,S1)`.
- MODIFIED `supabase/tests/test_ledger_refresh_composite_arm.sql` — same shape with `nth:2` instrument twins.
- New gate RAISE texts: `TEST FAILED (M1)`, `TEST FAILED (M2)`, `TEST FAILED (S1)` (x2 files).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gate transaction -> shared TEST | gates run on shared TEST in CI; every write is inside a transaction ending in ROLLBACK |
| pg-lane -> mutation runner | the runner applies mutated migrations to a throwaway cluster only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-12 | Tampering (control integrity) | a re-pointed arm that no longer bites (`no-red`) | high | mitigate | narrowed runs per gate (task 3) + full-corpus floor separation (plan 05); WAIVED_CEILING 0 |
| T-164.8.6-13 | Elevation of Privilege | service_role EXECUTE on the fan-outs re-granted out of band | high | mitigate | arm S1 in both gates asserts the whole set on TEST every CI run |
| T-164.8.6-14 | Repudiation | instrument rows missing after a decline | medium | mitigate | arms M1/M2 count exactly one cause row in the gate's own transaction |
| T-164.8.6-15 | Denial of Service (shared TEST) | arm L's RENAME of `system_flags` blocking other CI | low | accept | pre-existing arm with `lock_timeout = '2s'` and a P0164 unwind; unchanged by this plan |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Task 1 and 2 verifies print `FANOUT-GATE-OK` / `COMPOSITE-GATE-OK` (static: counts, JSON byte-match, lint, parse-only).
- Task 3 verify prints `LEDGER-LANES-OK` (dynamic: both narrowed runs clean, exit 2).
</verification>

<success_criteria>
- 26 body-editing arms re-pointed with measured occurrences; 6 new arms; both rosters 15; both lanes clean; zero waivers.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-03-SUMMARY.md` when done, carrying both narrowed-run final blocks verbatim.
</output>
