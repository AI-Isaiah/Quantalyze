---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 06
status: complete
completed: 2026-09-12
---

# 164.8.6-06 — SUMMARY (release + the SQL PR)

Written after the fact, 2026-09-12. Plan 06's three tasks were carried out
interactively during the ship rather than by a dispatched executor, so the
artifacts exist and this record was the only thing missing. Recorded here so
`execute-phase` does not re-run a plan whose one-way door is already through.

## What shipped

- **Release commit** — `VERSION` and `package.json` both at the byte-equal
  4-digit `0.77.34.0`, with the unified CHANGELOG entry built by CLAUDE.md's
  commit-checklist mechanism. `npm version` was not used.
- **PR #778** — the SQL wave (plans 01-05), filtered branch
  `phase-164.8.6-vaulttickfix-pr`, 19 of 32 commits picked; the 13 dropped
  carry ZERO non-filtered files, verified per commit. CODE IDENTICAL
  5069 == 5069. Merged as squash **`ce7ac08a`**.
- **The checkpoint:decision** gating the PR opening was taken by the founder.

## Verdicts at merge

26 pass / 3 structural fails, all three predicted in the PR body by
construction ([164.8-PUSH-RACE-VAC08] coupling (b) — applied-ness probes are
RED on a PR that ADDS a migration). VAC-04: 3 real body comparisons, 3 acked,
0 unacked. DRIFT-05 (b): 120 == 120 names both directions.

## What plan 06 did NOT predict, and cost a follow-up PR

`apply-test`'s affirmative verifier had **never once counted a migration**.
`supabase db push` writes its plan bullets and `Applying migration …` progress
to **stderr**, and all four capture sites piped stdout only, so the parser fed
on an empty file: the set-equality half compared 0 == 0 and passed by
tolerance on `workflow_dispatch` while failing closed on push-to-main. An
anti-vacuity INVERTED case — a control that could not SUCCEED. Fixed in
**PR #779** (squash `756469e6`, v0.77.34.1), which added `2>&1` to all four
sites including the PROD pair, and re-pointed 8 of 9 `db push` anchors in
`src/__tests__/supabase-migrate-test-first.test.ts`. The three
`migration list` anchors were deliberately NOT moved — that command writes to
stdout.

## Production

Dispatched run `34686331921` closed the split: `apply-test` reported
`planned 0 … reported 0` (TEST already current) and `apply` reported
**`planned 2 migration version(s); push reported 2`** with both server-side
NOTICEs. main, TEST and PROD are aligned.

## Carried forward

- `sql-gate-lint` is RED on main pending a `baseline.sql` regeneration from
  PROD — needs credentials not held locally; `BASELINE.md` calls it a separate
  reviewed act.
- The three `prod-body-ack` pragmas now name PROD's superseded bodies.
  MEASURED inert: `prod-body-drift-check.sh:1300` consults the ack only inside
  the `DRIFT)` branch, and greps only `CHANGED_FILES`. They must NOT be edited
  in place — a comment-only edit to an applied migration was this phase's one
  reviewer Critical.
