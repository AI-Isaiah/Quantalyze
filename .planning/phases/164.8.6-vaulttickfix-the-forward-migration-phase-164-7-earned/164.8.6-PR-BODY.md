# Phase 164.8.6 VAULTTICKFIX — the forward migration Phase 164.7 earned

`v0.77.34.0` · 30 commits · branch `phase-164.8.6-vaulttickfix`

164.7's review found real defects in the hourly tick and the ledger fan-out, and 164.7 could not
fix them: it was a review phase, and the fixes are DDL. This is that DDL.

## ⛔ Read this before merging — the merge is a one-way door

Merging anything under `supabase/migrations/**` **auto-applies to PRODUCTION**, via
`apply-test` (shared TEST first) then `apply` (behind the `Production` environment's human
reviewer gate). There is no preview and no dry run past that point.

**If `apply-test` refuses on shared TEST, the remedy is to REVERT THE MERGE.** Never edit
`supabase-migrate.yml` to get a deploy out — that is exactly the failure mode
`[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` exists to prevent.

## Why two migration files is one repair, not two

They are one shipped repair split by **lane topology**: the tick migration is exercised by the
`test_analytics_service_settings_and_vault_tick.sql` lane, the ledger migration by the
`test_ledger_refresh_fanout.sql` and `test_ledger_refresh_composite_arm.sql` lanes. A single
file would force one lane to carry arms for gates it does not own, and the mutation runner's
`edit` arms may only target a file in that gate's own apply list.

## Expect RED checks on this PR before merge. That is by construction.

`V2`, `G1`, `S1` (x2), `M1` (x2) and `M2` (x2) all probe **applied-ness** — and these migrations
apply *on merge*, not on PR. That is the documented `[164.8-PUSH-RACE-VAC08]` coupling **(b)**:
apply-on-merge was chosen over apply-on-PR, so a PR that ADDS a migration has red
applied-ness gates until it lands. `sql-tests` may also contend with `apply-test` for advisory
key `61616158` on the merge push — coupling **(a)**. Both were accepted at decision time and are
routed to Phase 164.9.

`VAC-08` carries a ledger-frontier exemption (PR #767) that narrows (b) for its own verdict, and
for nothing else.

## What changed

### Tick — `supabase/migrations/20260911120000_vault_tick_hardening.sql`

| defect | repair |
|---|---|
| `164.7-WR01-VAULT-NOT-STRICT` | `SELECT … INTO STRICT` — a missing or duplicated Vault secret now raises instead of silently taking the first row |
| `VAULTTICK-EMPTYKEY-01` | an empty decrypted key no longer falls through as if it were a valid credential |
| `164.7-WR02-SERVICE-ROLE-EXECUTE` | `service_role` EXECUTE granted explicitly rather than inherited |

Plus a **check 0** that aborts on an overloaded catalogue row count, and a value-based
`search_path` assertion replacing the old `LIKE 'search_path=%'` prefix match.

### Ledger — `supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql`

- Whole-set `REVOKE TRUNCATE, REFERENCES, TRIGGER ON public.cron_runs FROM anon, authenticated`.
- A dormancy instrument recording **why** a fan-out found no candidates, carrying `v_sqlstate`
  into `metadata`.
- Both instrument INSERTs wrapped in `BEGIN … EXCEPTION WHEN OTHERS THEN RAISE WARNING … END;`
  so instrumentation can never take the refresh down with it.

### Gates

- `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` — a `cron_runs` stand-in that is
  deliberately **stricter** than production (RLS on, no policies, no grants), so a gate cannot
  pass on the lane by being more permissive than the real table.
- Both ledger gates re-pointed onto the new migration, arms M1/M2/S1 each; tick gate gains V2
  and G1. Rosters fully enumerated — `ci.yml`'s F12 parser rejects the `A-L,M1,M2,S1` range form.
- `ARMS_FLOOR` **384 → 392**, separated in both directions on real lanes.

## ⚠️ The one honest gap this PR ships with

**Criteria 2 and 3 are closed in the repo and OPEN in production.** They land in
`match_engine_cron_tick()` — which **no PROD cron job calls**. Job 1 `match_engine_cron` still
inlines the unguarded Vault read and the `v_key = ''` fallthrough. Re-pointing that job row is
Phase 164.5.1's live production DDL, not this phase's work. The migration carries a loud header
saying so. This is a disclosure, not a silent gap.

## The three VAC-04 acknowledgements were EARNED, not pasted

VAC-04 diffs each migration's function bodies against what is live on PROD. Where the repo and
PROD legitimately differ, the migration carries a `prod-body-ack:` pragma naming PROD's live body
hash. **These three were regenerated from a CI run's own output, never transcribed by hand:**

| file | ack |
|---|---|
| `20260911120000_vault_tick_hardening.sql:95` | `323330b4…c943d9` |
| `20260911130000_ledger_fanout_grantees_and_dormancy.sql:93` | `adeb6d16…53705f` |
| `20260911130000_ledger_fanout_grantees_and_dormancy.sql:94` | `15e3ba96…2996b5` |

⛔ **If VAC-04 disagrees with any of these on this PR, FOLD — do not re-paste.** A mismatch means
PROD's live body is not what the ack claims, and the right response is to re-derive the hash from
the failing run's output and understand why it moved — never to copy the expected value out of the
error message to make the check go quiet.

## Why `20260907130000` is NOT in this diff, and why that matters to the merge

`.github/workflows/supabase-migrate.yml:846-851` refuses a push-to-main run whose TEST apply moved
nothing:

> the TEST push **applied ZERO migrations** on a push-to-main run … Refusing to green-light the
> PROD apply on a stage nothing crossed.

A **comment-only** edit inside an already-applied migration is exactly that no-op, so such a change
cannot ship alone on a push — it would trip this guard. This PR sidesteps the problem rather than
bundling around it: the earlier comment edit to
`20260907130000_ledger_refresh_switch_to_system_flags.sql` was **reverted** after the
`migration-reviewer` graded it Critical, and both corrections now ride as header prose inside the
new `20260911130000`. `git diff main` on `20260907130000` is byte-empty. Two genuinely new
migration files apply on merge, so `applied_n` is non-zero and the guard stays silent.

## Verification at `bce4b3b0`

| gate | result |
|---|---|
| `scripts/mutation-runner/run.mjs` | `arms: 392/392/0` · `biting: 392` · `lane-invocations: 392` (tallies agree) · `lane-blocked: 0` · `lane-probe: pg_cron AVAILABLE` · `✅ No defects` · exit 0 |
| `lint-sql-gates.mjs --self-test` / corpus | `7 rules, red+green each.` / `73 files, 0 findings` · exit 0 |
| `lint-app-guc.mjs --self-test` / corpus | `4 finding kinds, red+green each` / `294 files, 0 findings` · exit 0 |
| `run.mjs --parse-only` | `✅ No static defects.` |
| `npm run lint` | 0 errors (2 pre-existing warnings, neither file touched here) |
| `npm run typecheck` | clean |

## Review

Two rounds, three independent reviewers each — `migration-reviewer`, `rls-policy-auditor`,
`silent-failure-hunter`, all on opus.

- **Round 1:** 1 Critical, 2 High, 7 Medium → three sequential, file-disjoint fix passes
  (`13f3ece6`, `14462a48`, `bce4b3b0`).
- **Round 2:** **0 Critical**, 2 High, 4 Medium — stopping rule met.

Two reviewer claims did not survive independent re-measurement and are worth recording: the
"check 9" finding was the *sibling* file's numbering, and the grantor-mismatch `REVOKE` emits
**no warning at all** (it is completely silent, and a superuser is not the escape —
`SET ROLE <grantor>` is). The second one is why the `REVOKE` now carries a
`has_table_privilege` post-condition: nothing else can observe it.

## Carry-forward

The **all-candidates-failed** branch of the ledger fan-out is unreachable today (the fan-out is
dormant) and becomes reachable the moment Phase 161.1's recurring ledger refresh is activated.
Exercise it deliberately at activation.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_01LctzSPnLGDas8G5AgRJyS6
