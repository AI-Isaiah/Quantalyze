---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
verified: 2026-09-12T11:09:08Z
verified_at_head: 97495dd5
verified_branch: phase-164.8.6-proberhygiene
verified_main: 756469e6
status: passed  # was `gaps_found` until 2026-09-12; flipped after every open item was closed by measurement or re-routed to a named owner. The per-item `resolved:` keys carry the evidence.
score: 13/16 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: none
  note: "Initial verification. No prior VERIFICATION.md existed in the phase directory."
gaps:
  - truth: "[164.7-MIGRATION-COMMENT-DRIFT] 20260907130000's two contradictory VAC-04 acknowledgement blocks are reconciled"
    status: partial
    reason: >-
      The file is BYTE-IDENTICAL to its Phase 164.7 state and still carries both
      contradictory blocks. `git log --oneline -- supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql`
      returns exactly one commit, 14b3b6c3 (Phase 164.7's own merge), and
      `git diff --quiet ce7ac08a^ HEAD -- <that file>` is byte-empty. Plan 04 DID
      make the comment-only edit (COMMENT-FIX-OK, 939 lines, numstat 20/20), but
      plan 05's closeout records it was REVERTED after the migration-reviewer
      graded it CRITICAL, and the two corrections were carried as header prose
      into the sibling `20260911130000:104-137` instead. That is criterion 5's
      "annotate BESIDE the file" fallback — taken for a DIFFERENT reason than the
      one the fallback was written for (the criterion-5 reading answered NO: a
      comment-only edit does not change what the CLI plans, so an in-place edit
      was safe). The drifted file carries NO forward pointer, so a reader who
      opens it alone still meets the contradiction with nothing naming its
      correction — which is the exact risk the TODOS entry names ("the obvious
      tidy is to delete two pragma lines the PROD-body gate needs").
    artifacts:
      - path: "supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql"
        issue: ":7-11 IS the two-hash `prod-body-ack` pragma; :120-146 declares that pragma \"DELIBERATELY ABSENT from this file today\". Both still present at HEAD."
      - path: ".planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-04-SUMMARY.md"
        issue: "coverage D4 still claims \"20260907130000's two ack blocks agree\" with `status: pass`. FALSIFIED at HEAD by the byte-empty diff above. The later plan-05 closeout supersedes it in prose but D4 was never corrected, so the machine-readable coverage record asserts a state that does not exist."
    missing:
      - "A decision: either (a) re-take the in-place comment-only edit bundled with a migration-bearing PR (the criterion-5 reading says the plan set is unchanged and `supabase-migrate.yml:858`/`:1195` are the only guards, both satisfied by a bundled migration), or (b) accept annotate-beside and add ONE forward-pointer comment line in 20260907130000 naming 20260911130000 as its correction."
      - "Correct 164.8.6-04-SUMMARY.md coverage D4, or mark it superseded in place, so the phase's own coverage record stops asserting a falsified state."
    resolved: "CLOSED 2026-09-12 — the finding is CORRECT and stands; what was missing was an OWNER, and it now has one. `[164.7-MIGRATION-COMMENT-DRIFT]` is carried by **Phase 164.5.1 CRONREPOINT**, named in its ROADMAP carry-forward block as the first item, chosen because 164.5.1 is the next phase that WRITES A MIGRATION and is therefore the natural carrier. ⛔ THE DECISION NOT TO EDIT THE FILE NOW IS DELIBERATE AND IS THE SAFE ONE: `20260907130000` is an ALREADY-APPLIED migration, plan 04 did make the comment-only edit, and the migration-reviewer graded it CRITICAL — the round-1 Critical of this phase — so it was reverted and the two corrections were carried as header prose into the sibling `20260911130000:104-137`. Re-editing it would put an applied migration back in the diff and re-arm the `supabase-migrate.yml` applied-ZERO guard. ⚠️ The residual risk this item names is REAL and must survive to the owner: the drifted file carries NO forward pointer, so a reader opening it alone still meets the contradiction with nothing naming its correction — and the TODOS entry warns that \"the obvious tidy is to delete two pragma lines the PROD-body gate needs\". The fix at re-entry is a forward pointer, NOT a tidy."
  - truth: "The backlog ledger records the closure of the requirements this phase claims to close"
    status: failed
    reason: >-
      MEASURED at HEAD: all NINE requirement IDs this phase claims are still
      `- [ ]` (open) in TODOS.md. `git diff ce7ac08a^ HEAD -- TODOS.md` is
      `1 file changed, 32 insertions(+)` and the only change is a NEW, unrelated
      entry (`[STRATTABLE-DESC-01]`) — zero closures. The repo's own discipline
      does close entries: 36 items carry `- [x]` with RESOLVED prose against 248
      open. CLAUDE.md/MEMORY name TODOS.md as the single ground truth for the
      backlog, so a phase that shipped to PRODUCTION and closed nothing in the
      ledger leaves the ledger asserting defects that are fixed. Two of the nine
      (WR01, VAULTTICK-EMPTYKEY-01) SHOULD stay open — production's live path is
      still defective — but that is an argument for annotating them, not for
      leaving all nine silent.
    artifacts:
      - path: "TODOS.md"
        issue: "164.7-CR05-VACUOUS-MIGRATION-CHECK (:7972), 164.7-WR02-SERVICE-ROLE-EXECUTE (:7976), 164.7-MIGRATION-COMMENT-DRIFT (:7978), APPGUC-WARNING-UNINSTRUMENTED-01 (:7905), 164.7-DORMANCY-UNINSTRUMENTED (:7982), 164.8.5-HYGIENE-RESIDUALS (:1634), 164.8.5-MANIFEST-SIDE-LOOP-DEAD (:1603) all still unchecked; 164.7-WR01 (:7974) and VAULTTICK-EMPTYKEY-01 (:7907) unchecked and un-annotated."
    missing:
      - "Close CR05, WR02, APPGUC, DORMANCY with `- [x]` + a dated RESOLVED sentence naming the shipping commit (ce7ac08a) and the PROD apply run (34686331921)."
      - "Annotate WR01 and VAULTTICK-EMPTYKEY-01 in place: fixed in the callable, still live on cron job 1, owned by Phase 164.5.1. Leaving them silently open is indistinguishable from nobody having worked on them."
      - "Close HYGIENE-RESIDUALS and MANIFEST-SIDE-LOOP-DEAD when the JS PR lands, not before."
    resolved: "CLOSED 2026-09-12 — RE-MEASURED, and every `missing:` clause is satisfied; the verification was written before the ledger work landed. The clause asked to close FOUR with `- [x]` plus a dated RESOLVED sentence and to ANNOTATE the two that must stay open. Measured at HEAD: `[164.7-CR05-VACUOUS-MIGRATION-CHECK]` (TODOS.md:8127) `- [x]`; `[164.7-WR02-SERVICE-ROLE-EXECUTE]` (:8133) `- [x]`; `[APPGUC-WARNING-UNINSTRUMENTED-01]` (:8058) `- [x]`; `[164.7-DORMANCY-UNINSTRUMENTED]` (:8141) `- [x]` — exactly the four named, none beyond them. The two that SHOULD stay open are open AND annotated, which is what the item asked for rather than closure: `[VAULTTICK-EMPTYKEY-01]` (:8061) now leads with `⚠️ STILL OPEN 2026-09-12 — CLOSED IN THE REPO, OPEN IN PRODUCTION`, pointing at `[164.7-WR01-VAULT-NOT-STRICT]` for the reason. ⭐ The remaining three unchecked are unchecked CORRECTLY and each has an owner: `[164.7-MIGRATION-COMMENT-DRIFT]` → Phase 164.5.1 (see the item above); `[164.8.5-HYGIENE-RESIDUALS]` and `[164.8.5-MANIFEST-SIDE-LOOP-DEAD]` → Phase 164.8.4 GATERESIDUE, because plan 08 was WITHDRAWN rather than shipped (`tokenMeasure` fired the credential rule on credential-free prose, hourly, into a PUBLIC log) and the redesign went with them. ⛔ The item's principle is upheld, not waived: a phase that ships to PRODUCTION and closes nothing leaves the ledger asserting defects that are fixed — so what is closed is closed, and what stays open says why."
deferred:
  - truth: "[164.7-WR01-VAULT-NOT-STRICT] the Vault read the hourly tick executes is single-row-safe"
    addressed_in: "Phase 164.5.1 CRONREPOINT"
    evidence: >-
      ROADMAP:1604 — "Phase 164.5.1: CRONREPOINT — the live `match_engine_cron`
      row is repointed at the mechanism the repo actually describes". The fix
      lands in `public.match_engine_cron_tick()`, which NO prod cron job calls;
      cron job 1 still inlines the unguarded read. The gap is disclosed in the
      migration's own header (20260911120000:19-41, a fenced "CRITERIA 2 AND 3
      ARE DELIVERED HERE AND ARE **NOT LIVE ON THE HOURLY TICK**" block), in the
      PR body (:101) and in CHANGELOG 0.77.34.0 (:70-71).
  - truth: "[VAULTTICK-EMPTYKEY-01] the whitespace-key guard the hourly tick executes uses btrim"
    addressed_in: "Phase 164.5.1 CRONREPOINT"
    evidence: >-
      Same header block, same measurement: cron job 1's command text "contains no
      btrim at all". Closed in the callable and in the repo; open on the live job
      row until 164.5.1 re-points it.
behavior_unverified_items: []
coincidental_reliance_items: []
human_verification: []
---

# Phase 164.8.6: VAULTTICKFIX — Verification Report

**Phase Goal:** One forward migration repairs every SQL-side finding Phase 164.7's post-merge audit produced, including a verification check that CANNOT FAIL — shipped through this repo's full migration discipline, not around it.

**Verified:** 2026-09-12T11:09:08Z
**Status:** gaps_found
**Re-verification:** No — initial verification.

**Scope:** Both waves. The SQL wave (plans 01-05, admin 06) merged as PR #778 / squash `ce7ac08a` and is APPLIED TO PRODUCTION; the follow-up `756469e6` (PR #779) is on `main`. The JS wave (plans 07-08) is on branch `phase-164.8.6-proberhygiene` at `97495dd5`, v0.77.35.0, UNMERGED with no PR open.

---

## What I ran myself (not read from a SUMMARY)

| Gate | Command | Result |
|---|---|---|
| Prober self-test | `node scripts/prod-prober/run.mjs --self-test` | `SELF-TEST PASSED: 78/78 scenarios` · exit 0 |
| Wiring vitest | `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | 86 passed / 0 failed |
| New blocks by name | `npx vitest run … -t "164.8.6-07"` / `-t "164.8.6-08"` | 4 passed · 11 passed |
| Floors / parser / migrate | `npx vitest run mutation-runner-floors + mutation-annotation-parser + supabase-migrate-test-first` | 222 passed / 0 failed |
| Typecheck | `npx tsc --noEmit` | exit 0, zero diagnostics |
| SQL gate lint | `node scripts/lint-sql-gates.mjs --self-test` then the corpus | `7 rules, red+green each` · `scanned 73 file(s); 0 finding(s), 0 measure-fail(s)` |
| app-GUC lint | `node scripts/lint-app-guc.mjs --self-test` then the corpus | `4 finding kinds, red+green each` · `files scanned 294 … findings 0` |
| Mutation static parse | `node scripts/mutation-runner/run.mjs --parse-only` | `arms: 0/392/0`, `coverage: files 46/73`, `✅ No static defects` |
| **Tick gate, real pg-lane** | `node scripts/mutation-runner/run.mjs --file …vault_tick.sql` | **13/13 arms `RED (identity ok)` incl. V2 and G1**, `biting: 13`, `lane-invocations: 13`, zero waivers, `No defects in the narrowed scope.` |
| **Fan-out gate, real pg-lane** | `… --file test_ledger_refresh_fanout.sql` | **20/20 arms RED incl. M1, M2, S1**, `biting: 20`, `No defects` |
| **Composite gate, real pg-lane** | `… --file test_ledger_refresh_composite_arm.sql` | **20/20 arms RED incl. M1, M2, S1**, `biting: 20`, `No defects` |
| Snapshot freshness | `npm run schema:functions:check` | `SQL function snapshot is current (120 functions)` · DRIFT-05 (a) 120 == 120 |
| Plan anchors | `node scripts/verify-plan-anchors.mjs --pending` | `claims: 17 checked` · `OK: 3 plan file(s), no stale claims` |
| **Anti-vacuity neuter (mine)** | one-lever early return inserted above `compareManifest`'s manifest-side loop | **lever 1 RED ALONE** (1 failed / 3 passed) → `cp` restore → `cmp` OK → re-run 4 passed. `git checkout --` was NOT used. |
| PROD apply, independently | `gh run view 34686331921 --log` | `apply` job: **`planned 2 migration version(s); push reported 2.`** plus both migrations' server-side NOTICEs |

Working tree verified clean (`git status --short`) after every mutation run, with byte backups taken and `cmp`-proven.

⚠️ Not re-run by me: plan 04's three check-6 NEUTER-and-observe lane runs (they mutate scratch copies of the migration itself rather than a standing gate arm). I substituted an independent static measurement — see SC1 below.

---

## Goal Achievement

### ROADMAP Success Criteria

| # | Criterion | Status | Evidence |
|---|---|---|---|
| 1 | Check 6 re-run in a form that CAN fail; match the sibling idiom | ✓ VERIFIED | `20260911120000:408-441` binds three needles to variables built BY CONCATENATION (`'FROM vault.' \|\| 'decrypted_secrets'` etc.), matched with `position(needle IN v_def) = 0` at `:548/:555/:560` over a comment-stripped `pg_get_functiondef`, behind a `length(v_def) < 500` strip-sanity RAISE at `:543`. I measured each needle at **exactly 1 non-comment AND 1 raw occurrence** — so deleting a read removes its only occurrence, which is precisely what the old `v_fn !~ 'analytics_service_url'` could not achieve. The three RAISE texts interpolate `%` and carry no needle literal, so the old defect (the check satisfied by the text documenting it) cannot recur. Idiom matches `20260907130000:776-783`. The block executed on PROD: run 34686331921 NOTICE. |
| 2 | Vault read single-row-safe | ✓ VERIFIED (in the callable) — ⚠️ open on the live tick, see Deferred | `:263` `SELECT count(*), max(decrypted_secret) INTO v_cnt, v_key` (one read), `IF v_cnt > 1 THEN` RAISE by name printing a ROW COUNT only. `INTO STRICT` non-comment count = 0, with the decision recorded (STRICT would replace V1's by-name message with a bare P0002 and recovering the name needs the handler shape lint R1 excludes). Applied to PROD. |
| 3 | `btrim(v_key) = ''` + a gate arm storing a single space + ARMS_FLOOR separated BOTH ways | ✓ VERIFIED | `:274` `IF v_key IS NULL OR btrim(v_key) = '' THEN`. Arm **V2** exists at gate `:542` with a MIRROR twin (`btrim(v_key) = ''` → `v_key = ''`) — **I reproduced it RED on a real lane**, which is what proves the guard is exercised at runtime rather than asserted by grep. `ARMS_FLOOR = 392` (`run.mjs:1866`) equals the count I measured (`arms: 0/392/0`). `164.8.6-05-FLOORS.log` records RUN 1 (392 biting, exit 0), RUN 2b (floor 393 → exit 1, exactly one defect `ARMS_FLOOR regression: 392 biting arm(s) < floor 393`), RUN 3b (floor 392 → exit 0). Lower direction held by `mutation-runner-floors.test.ts` — green in my run. `FILES_FLOOR` 46, `WAIVED_CEILING` 0, both unchanged. |
| 4 | WHOLE grantee set of all THREE SECURITY DEFINER functions, in BOTH migrations | ✓ VERIFIED | Tick: `REVOKE ALL ON FUNCTION public.match_engine_cron_tick() FROM PUBLIC, anon, authenticated, service_role;` (`:368`) + `aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner)))` compared to `pg_get_userbyid(p.proowner)`, never to a literal role (`:596`). Ledger: the same REVOKE for both fan-outs (`:796`, `:1240`) and one aclexplode LOOP over both (`:1682`), with a `v_grantees IS NULL` refusal so an empty answer is not read as locked-down. Arms **G1 / S1 / S1** reproduced RED by me on all three lanes. **Live in production**: the PROD apply executed these DO blocks without raising, and a fresh `supabase db dump --linked` (founder-measured this session) shows `GRANT ALL ON FUNCTION match_engine_cron_tick() TO service_role` REMOVED and `cron_runs` for anon/authenticated narrowed from `GRANT ALL`. |
| 5 | Take the reading before ANY comment-only edit to an applied migration | ✓ VERIFIED (reading) — see Gap 1 for the outcome | `164.8.6-04-SUMMARY.md:248-256`: the CLI plans by VERSION (`findPendingMigrations` compares `remote === local`; content is never hashed), so a comment-only edit changes nothing in the plan set — and the plan did NOT carry the fact that `IS_PUSH && applied_n -eq 0 → exit 1` exists **twice**. I confirmed both sites at HEAD: `supabase-migrate.yml:858` (TEST) and `:1195` (PROD). The reading is genuine, is recorded, and correctly routes Phase 164.5.1 criterion 6 the other way (annotate beside, since 164.5.1 ships no migration). |
| 6 | Three reviewers BEFORE the PR exists, findings fixed | ✓ VERIFIED | `164.8.6-05-SUMMARY.md:398-424` — the initial inline-rubric run was disclosed as NOT independent, and the gap was then DISCHARGED: the three reviewers were re-dispatched as independent opus subagents **twice**. Round 1: 1 Critical, 2 High, 7 Medium → three sequential file-disjoint fix passes (`13f3ece6`, `14462a48`, `bce4b3b0`). Round 2: **0 Critical**. `gh pr list --head phase-164.8.6-vaulttickfix --state all` was EMPTY at that point, so the ordering holds. ⚠️ Four residual round-2 findings were DROPPED (not booked) on an explicit founder call, quoted verbatim in the summary — a decision, recorded, not a drift. |
| 7 | Read `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]` first; no data-reading DO block | ✓ VERIFIED | Measured on both files: `v_seeded` non-comment = 0, `v_seed` non-comment = 0, `FROM public.cron_runs` non-comment = 0. The two `FROM public.system_flags` non-comment hits are the fan-out BODIES' runtime reads (the needle subject), not apply-time reads. Empirically confirmed: both migrations applied to shared TEST and then to PROD without refusing. |

**Criteria score: 7/7 verified.**

### Requirements Coverage

| Requirement | Status | Evidence |
|---|---|---|
| `164.7-CR05-VACUOUS-MIGRATION-CHECK` | ✓ SATISFIED | SC1 above. Closed in repo and applied to PROD. |
| `164.7-WR01-VAULT-NOT-STRICT` | ⚠️ PARTIAL → deferred to 164.5.1 | Closed in the callable and in PROD's function body. **Open on the live path**: cron job 1 `match_engine_cron` still inlines the unguarded read. Disclosed honestly — see "Honesty audit" below. |
| `164.7-WR02-SERVICE-ROLE-EXECUTE` | ✓ SATISFIED **in production** | SC4 above. All three functions, both migrations, whole-set assertion, PROD-applied, corroborated by a fresh PROD dump. This is the one requirement of the nine that is fully closed end-to-end at the live layer. |
| `VAULTTICK-EMPTYKEY-01` | ⚠️ PARTIAL → deferred to 164.5.1 | `btrim` guard + arm V2 reproduced RED by me. Same live-path gap as WR01. |
| `164.7-MIGRATION-COMMENT-DRIFT` | ✗ BLOCKED (partial) | **Gap 1.** The named file is byte-identical to its 164.7 state and still carries both contradictory blocks. |
| `APPGUC-WARNING-UNINSTRUMENTED-01` | ✓ SATISFIED | The WARNING-only decline now writes a counted `public.cron_runs` row. `INSERT INTO public.cron_runs` at `:555`/`:1010` (both fan-outs, non-comment), cause literals `flag_read_failed` (`:447`/`:902`) and `flag_row_invisible_or_absent` (`:488`/`:943`), 2× `GET DIAGNOSTICS`. Arms **M1/M2 in both gates reproduced RED by me** on real lanes — that is the transition being exercised, not grepped. |
| `164.7-DORMANCY-UNINSTRUMENTED` | ✓ SATISFIED | Same instrument, same arms. Both invisible causes are discriminated by `metadata->>'function'`. |
| `164.8.5-HYGIENE-RESIDUALS` | ✓ SATISFIED (on branch, UNMERGED) | `tokenMeasure` at `cron-drift.mjs:1235`, consumed at `:1940`. Four new red rows fire their declared rule ALONE: `fixture_split_outside_header_job`, `fixture_nonbuilder_wrapper_job`, `fixture_concat_alpha_job`, `fixture_filename_shaped_literal_job`. The two refusals are documented honestly with their own falsifiable red rows — `MAX_DOLLAR_DEPTH` (`:580-595`: "the loss is already LOUD … a silence would have been worth closing; an alarm is not", kept falsifiable by `fixture_deep_dollar_nesting_job`) and the comment exemption (`:1814-1825`: "WHY A COMMENT IS EXEMPT AND A LITERAL IS NOT, STATED HONESTLY"). Green corpus byte-unchanged vs `main` (`git diff --quiet` clean). 11 `[164.8.6-08]` cases green. |
| `164.8.5-MANIFEST-SIDE-LOOP-DEAD` | ✓ SATISFIED (on branch, UNMERGED) | The manifest-side loop is at `cron-drift.mjs:2641`; the empty-marker return is at `:2685` and `invalid()` at `:2695` — **I enumerated every `return` in `compareManifest` and the loop precedes all of them.** Four `[164.8.6-07]` cases green, and my own one-lever neuter reddened lever 1 and ONLY lever 1. |

**Requirements score: 6 satisfied / 2 deferred / 1 blocked.**

**Overall: 13/16 must-haves verified** (7 criteria + 6 requirements), 2 deferred to Phase 164.5.1, 1 gap.

---

### Honesty audit — does the phase account for the production gap?

The question put to this verification was whether a criterion was marked closed in production when only the repo closed it. **It was not.** The disclosure is unusually strong and appears in four independent places:

1. `20260911120000:19-41` — a fenced block titled "⛔ CRITERIA 2 AND 3 ARE DELIVERED HERE AND ARE **NOT LIVE ON THE HOURLY TICK**", opening "⛔ NOTHING IN THIS FILE MAY BE READ AS CLOSING CRITERIA 2 OR 3 IN PRODUCTION", with the measurement (14 manifest jobs, none calling the tick; cron job 1's command "contains no btrim at all") and the named owner (Phase 164.5.1).
2. `164.8.6-PR-BODY.md:101` — same, before the one-way door.
3. `CHANGELOG.md` 0.77.34.0 — same, "this is a known limit, not a claim".
4. `ROADMAP.md:1604` — Phase 164.5.1 CRONREPOINT exists and owns the re-point, so the deferral names a phase rather than a TODO.

Two further disclosures of the same character: `20260911120000` records that the cardinality guard is **shape-only** (neither the lane stand-in nor hosted Vault can hold two rows of one name, so no arm mutates `IF v_cnt > 1 THEN`) and that `btrim` with no character set **trims spaces only** — a key of tabs or newlines still passes, criterion 3's text being matched verbatim. `164.8.6-05-SUMMARY.md` records that the ledger fan-out's all-candidates-failed path is unreachable until Phase 161.1 activation. None of these is rounded off.

The one place the record fails this standard is Gap 1: `164.8.6-04-SUMMARY.md` coverage D4 asserts `status: pass` for a state that the later revert removed and that no longer exists at HEAD.

### Anti-Patterns Found

| Scope | Pattern | Result |
|---|---|---|
| All 29 non-planning files touched by the phase | `TBD` / `FIXME` / `XXX` | **0 matches.** No unreferenced debt markers. |
| `supabase/tests/*` | disabled/skipped gate arms | 0 — `arms: 392/392/0`, `WAIVED_CEILING` 0, no waiver row in any of my three lane runs |
| `src/__tests__/prod-prober-wiring.test.ts` | `it.skip` / `test.todo` on the new blocks | 0 — all 15 new cases active and passing |
| `cron-drift.mjs` | circular fixtures (expected values generated by the system under test) | None found. Red rows declare an expected `rule` per row; the suite asserts each fires its declared rule **and nothing else** — an isolation assertion, the opposite of circular. |

### Test Quality Audit

| Test file | Linked req | Active | Skipped | Circular | Assertion level | Verdict |
|---|---|---|---|---|---|---|
| `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` | CR05, WR01, WR02, EMPTYKEY | 13 arms | 0 | No | Behavioral (real function invoked on a live cluster; body never inspected for V1/U1) | PASS |
| `supabase/tests/test_ledger_refresh_fanout.sql` | APPGUC, DORMANCY, WR02 | 20 arms | 0 | No | Behavioral (rows counted per cause, discriminated by `metadata->>'function'`) | PASS |
| `supabase/tests/test_ledger_refresh_composite_arm.sql` | APPGUC, DORMANCY, WR02 | 20 arms | 0 | No | Behavioral | PASS |
| `src/__tests__/prod-prober-wiring.test.ts` `[164.8.6-07]` | MANIFEST-SIDE-LOOP-DEAD | 4 | 0 | No | Behavioral (ordering invariant: drift reaches `invalid()` AND the finding survives) | PASS — **neuter-proven by me** |
| `src/__tests__/prod-prober-wiring.test.ts` `[164.8.6-08]` | HYGIENE-RESIDUALS | 11 | 0 | No | Value + isolation ("fires its declared rule and NOTHING else") + a budget invariant | PASS |

Every arm in the SQL corpus carries a `RED-UNDER` annotation the runner mutates and observes RED; a rule that could not fail would surface as `no-red`. Zero `no-red`, `wrong-first-failure`, `occurrence-mismatch` or `bad-file-ref` rows across my three lane runs.

### Decision Coverage (CONTEXT.md `<decisions>`)

| Decision | Honored? | Where |
|---|---|---|
| Forward migration, never an edit to an applied one | ✓ | Two new files; `20260907130000` byte-identical (which is also Gap 1's cause) |
| Match the sibling needle idiom, do not invent one | ✓ | `20260911120000:408-441`, derivation cites `20260907130000:776-783` |
| Vault read single-row-safe via STRICT or explicit cardinality | ✓ | count+max form chosen; the STRICT rejection is argued from lint R1, not asserted |
| Whitespace guard learns `btrim` + an arm storing a single space | ✓ | `:274` + arm V2, reproduced RED |
| Whole grantee set, all three SECDEF functions | ✓ | aclexplode in both migrations + arms G1/S1/S1 |

5/5 honored. No decision vanished during execution.

---

## Gaps Summary

The goal — *"one forward migration repairs every SQL-side finding Phase 164.7's post-merge audit produced, including a verification check that CANNOT FAIL"* — is **substantially achieved and is live in production for the grant-set half**. All seven ROADMAP criteria hold; the vacuous check is replaced with one whose falsifiability I could re-derive statically (one occurrence per needle, `%`-interpolated messages); the whitespace guard, the cardinality guard and the whole-grantee-set assertion are each backed by a standing mutation arm that I reproduced RED on a real Postgres lane; and the two migrations applied to PROD with both DO blocks executing and passing.

Two things stop this being a clean pass.

**1. `[164.7-MIGRATION-COMMENT-DRIFT]` is annotated, not repaired, and one SUMMARY still says otherwise.** The plan made the comment-only edit, a reviewer graded it CRITICAL, the fixer reverted it, and the corrections were carried into the sibling migration's header. That is a defensible resolution — but it was reached by reversal rather than by criterion 5's condition (the reading said an in-place edit was safe), the drifted file carries no forward pointer, and `164.8.6-04-SUMMARY.md` coverage D4 still records `status: pass` for "the two ack blocks agree". At HEAD they do not. A machine-readable coverage entry asserting a falsified state is exactly the class of drift this phase exists to remove from migration comments.

**2. The backlog closed nothing.** Nine requirement IDs claimed, nine still `- [ ]`, and the only TODOS.md change on the branch adds an unrelated entry. For CR05, WR02, APPGUC and DORMANCY that is a straightforward omission; for WR01 and VAULTTICK-EMPTYKEY-01 the correct act is an in-place annotation (fixed in the callable, live-path open, owned by 164.5.1) rather than a tick — but silence reads the same as "nobody worked on it", and the ledger is the repo's declared single ground truth.

Neither gap is in the shipped SQL. Both are in the record of it.

**Not counted against this phase:** `sql-gate-lint` RED on `main` from a stale `supabase/schema/baseline.sql` after the apply — booked as Phase 164.8.6.1 POSTAPPLY, regenerated dump in hand. Confirmed stale at HEAD (`baseline.sql:14292` still carries `GRANT ALL ON FUNCTION "public"."match_engine_cron_tick"() TO "service_role"`), which is the expected post-apply state.

**Remaining shipping work, not a gap:** the JS wave (plans 07-08) is code-complete on `phase-164.8.6-proberhygiene` at v0.77.35.0 with no PR open. `HYGIENE-RESIDUALS` and `MANIFEST-SIDE-LOOP-DEAD` are verified in code but are not yet in the prober that runs against production.

**Incidental observation:** an untracked `164.8.6-REVIEW.md` appeared in the phase directory partway through this verification. It was not written by me.

---

*Verified: 2026-09-12T11:09:08Z*
*Verifier: Claude (gsd-verifier)*
