---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260911120000_vault_tick_hardening.sql
  - supabase/tests/test_analytics_service_settings_and_vault_tick.sql
autonomous: true
requirements:
  - 164.7-CR05-VACUOUS-MIGRATION-CHECK
  - 164.7-WR01-VAULT-NOT-STRICT
  - 164.7-WR02-SERVICE-ROLE-EXECUTE
  - VAULTTICK-EMPTYKEY-01
tags: [sql-migration, mutation-runner, pg-lane, vault]

estimate:
  tokens: 140000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A NEW forward migration re-defines public.match_engine_cron_tick() with a one-read count+max Vault read, an `IF v_cnt > 1 THEN` cardinality RAISE by name, and a `btrim(v_key) = ''` whitespace guard whose RAISE text is byte-identical to today's (`analytics_service_key missing from vault`) — ROADMAP criteria 2 and 3, TODOS 164.7-WR01 / VAULTTICK-EMPTYKEY-01"
    - "The migration's own DO block asserts the tick body by VARIABLE-BOUND STATEMENT-SHAPE needles (`FROM vault.decrypted_secrets`, `FROM public.system_settings`, `IF v_cnt > 1 THEN`) over a comment-stripped pg_get_functiondef, matching the sibling idiom at 20260907130000:766-775 / :896-903 — ROADMAP criterion 1, TODOS 164.7-CR05"
    - "EXECUTE on public.match_engine_cron_tick() is revoked from PUBLIC, anon, authenticated AND service_role, and the DO block asserts the WHOLE grantee set equals exactly the owner via aclexplode — ROADMAP criterion 4, TODOS 164.7-WR02"
    - "The tick gate applies the new migration LAST, every body-editing arm (V1, U1, C1, C2, C3) is re-pointed at it, R3 stays on 20260907120000, and two new RED-UNDER-M arms exist: V2 (single-space key -> RAISE by name, MIRROR twin) and G1 (service_role GRANT -> whole-set RAISE, sql twin)"
    - "A narrowed mutation-runner run over the tick gate on the pg-lane reports `No defects in the narrowed scope.` with every re-pointed and new arm biting its own arm first"
  artifacts:
    - "supabase/migrations/20260911120000_vault_tick_hardening.sql"
    - "supabase/tests/test_analytics_service_settings_and_vault_tick.sql (apply list :190 extended; arms V1/U1/C1/C2/C3 re-pointed; arms V2 and G1 added; roster 11 -> 13)"
  key_links:
    - "Gate apply list :190 -> new migration appended LAST (scripts/mutation-runner/run.mjs:4085-4094, `bad-file-ref`, only allows edit steps on files in that list; a later CREATE OR REPLACE overwrites any mutation of the earlier file)"
    - "Migration needles {FROM vault.decrypted_secrets, FROM public.system_settings, IF v_cnt > 1 THEN} DISJOINT from gate find strings {IF v_key IS NULL OR btrim(v_key) = '' THEN, IF v_url IS NULL OR v_url = '' THEN, WHERE s.key = 'analytics_service_url', IF v_url !~ c_url_allowed THEN, C3 message suffix} — WAIVED_CEILING is 0 (scripts/mutation-runner/run.mjs:1882, `WAIVED_CEILING`)"
    - "V1's RAISE text unchanged -> V1's assertion at :259 (`v_msg !~ 'analytics_service_key missing from vault'`) and V2's assertion share one message"
---

<objective>
The TRACER for the SQL wave: wire ONE path — a new forward migration that hardens `public.match_engine_cron_tick()` — through every layer this phase touches (migration file -> mutation-runner apply list -> gate arms -> pg-lane run) and prove it end-to-end with a narrowed runner run before anything else is built out.

⛔ **Two migration files, not one.** The ROADMAP goal sentence says "One forward migration". That phrasing is DISPROVEN by lane topology (measured in RESEARCH §Q4): the tick gate's lane applies fixtures 01/02/07/12/15/32 + `20260907120000` and none of the ledger stack; the fan-out gates' lanes apply the ledger stack and no vault stand-in. One file that `CREATE OR REPLACE`s all three functions cannot apply on either lane. This plan ships the TICK file; plan 02 ships the LEDGER file; both merge in ONE PR (plan 06). "One shipped repair" is satisfied; "one file" is not, and that is recorded here rather than silently satisfied.

Purpose: ROADMAP criteria 1-4 for the tick function (TODOS.md:7940, :7942, :7944, :7875).
Output: `supabase/migrations/20260911120000_vault_tick_hardening.sql` + the tick gate extended and re-pointed, green on the lane.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-CONTEXT.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md
@supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql
@supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
@supabase/schema/functions/match_engine_cron_tick.sql
@supabase/tests/test_analytics_service_settings_and_vault_tick.sql
@scripts/mutation-runner/GRAMMAR.md
</context>

<interfaces>
Symbols and strings other plans depend on (created or pinned here):

- Migration file: `supabase/migrations/20260911120000_vault_tick_hardening.sql` (plans 04, 05, 06 read it; plan 04 adds its earned `-- prod-body-ack:` line).
- Function body strings that MUST exist byte-for-byte in the new body because gate arms `find` them (PATTERNS § tick migration):
  - `IF v_key IS NULL OR btrim(v_key) = '' THEN` (V1 and V2 finds)
  - `IF v_url IS NULL OR v_url = '' THEN` (U1)
  - `WHERE s.key = 'analytics_service_url';` (C1)
  - `IF v_url !~ c_url_allowed THEN` (C2)
  - `read it with an admin session. Allowed: an https host under .up.railway.app';` (C3)
- Verification needles (DO block, `position(needle IN v_def)`): `FROM vault.decrypted_secrets`, `FROM public.system_settings`, `IF v_cnt > 1 THEN`.
- RAISE texts: `analytics_service_key missing from vault — refusing to send a null header` (UNCHANGED, V1/V2 assert on `analytics_service_key missing from vault`); NEW `analytics_service_key is not unique in vault (% rows) — refusing to pick one`.
- New gate arm ids: `V2`, `G1`; setup sub-arm `V2-SETUP`; roster sentinel `ALL 13 ARMS EXECUTED (0,V1,U1,U2,C1,C2,C3,V2,R1,R2,R3,T1,G1)`.
</interfaces>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "harden match_engine_cron_tick" — the new migration, one path only</name>
  <files>supabase/migrations/20260911120000_vault_tick_hardening.sql</files>
  <read_first>
    - supabase/schema/functions/match_engine_cron_tick.sql (the LEFT side of the re-base — lines 9-76 are the body; copy them, change only what is named below)
    - supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql:300-403 (body, COMMENT, REVOKE at :388, the apply-time prohibition at :395-398) and :460-555 (STEP 3 checks to carry forward in KIND; :534-536 is the seed-row read that MUST NOT be re-run; :546-554 is the vacuous check 6 to quote as the defect)
    - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql:1-60 (header + ack shape), :135-152 (why a placeholder ack is ABSENT until measured), :154-160 (re-base discipline), :243-245 (why nothing but an assignment goes in a handler), :741-746 (a missing DECLARE aborts the block), :766-775 and :858-903 (the variable-bound needle idiom + comment strip)
    - supabase/migrations/20260515205431_sec_def_public_execute_guard.sql:80-116 (`aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner)))` idiom and the REVOKE precedent naming service_role)
    - scripts/pg-lane/fixtures/07-fixture-supabase-default-privileges.sql:21-22 (`GRANT ALL ON FUNCTIONS TO anon, authenticated, service_role;` — why the REVOKE is falsifiable on the lane)
    - scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql (lane `vault.decrypted_secrets` is a TABLE with `name TEXT PRIMARY KEY`; `create_secret(secret, name, description)`)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md § tick migration and § Disjointness table
  </read_first>
  <action>
    Create `supabase/migrations/20260911120000_vault_tick_hardening.sql` (timestamp later than the newest file on disk, `20260908120000`; if a later-stamped migration lands on main before merge, re-stamp this one later still and update the gate apply list in the same commit). Structure it exactly like `20260907130000`: header -> VAC-04 ack block -> re-base note -> CREATE OR REPLACE -> COMMENT ON FUNCTION -> REVOKE -> `DO $verify$`.

    HEADER must carry, as comments: (a) the phase/criteria line; (b) the VAC-04 ack block in the `20260907130000:135-152` shape — the `prod-body-ack` pragma line is DELIBERATELY ABSENT in this task (plan 04 earns it from origin/main's snapshot hash and adds it); state that absence and why, copying the "EARNED, not pasted" paragraph; (c) the quoted vacuous check from `20260907120000:546-554` labelled as THE DEFECT this file replaces, with the explanation that the strip removes comments but not string literals and the needle token sits in the function's own RAISE text at :332 and :348; (d) BOTH disjointness lists pasted verbatim from PATTERNS § Disjointness (migration needles vs gate find strings) with the sentence that `WAIVED_CEILING` is 0 so an intersection is not waivable; (e) the apply-time prohibition from `20260907120000:395-398` (this block must never call the function — a real net.http_post would fire at merge); (f) the "two files, one repair" note from this plan's objective; (g) the shape-only note for the cardinality guard: neither the lane stand-in (PRIMARY KEY on name) nor hosted Vault lets a gate insert two rows of one name, so criterion 2 is asserted by needle, not exercised.

    CREATE OR REPLACE FUNCTION public.match_engine_cron_tick(): copy the committed snapshot body (`supabase/schema/functions/match_engine_cron_tick.sql:9-76`, whose Vault read ends at `WHERE name = 'analytics_service_key';`) byte-for-byte and change ONLY: (1) DECLARE gains `v_cnt INTEGER;` (a missing DECLARE raises 42601 and aborts the whole block — 130000:741-746); (2) the Vault read becomes ONE statement reading count and value from the same scan: `SELECT count(*), max(decrypted_secret) INTO v_cnt, v_key` on its own lines followed by `FROM vault.decrypted_secrets` on its own line (this exact spelling is the needle AND `src/__tests__/prod-prober-wiring.test.ts:1754` asserts the snapshot matches `/FROM\s+vault\.decrypted_secrets/i`) and `WHERE name = 'analytics_service_key';`; (3) immediately after it, the cardinality guard `IF v_cnt > 1 THEN` raising `analytics_service_key is not unique in vault (% rows) — refusing to pick one` with `v_cnt` as the argument, `END IF;`; (4) the whitespace guard line becomes `IF v_key IS NULL OR btrim(v_key) = '' THEN` with the EXISTING RAISE text unchanged (`analytics_service_key missing from vault — refusing to send a null header`). Per CONTEXT "Claude's Discretion": the count form is chosen over the STRICT form because STRICT replaces V1's by-name message with a bare P0002 and recovering the name needs the handler shape 130000:243-245 keeps out of source (RESEARCH §Q3, Pitfall 8). `btrim(v_key)` is the criterion-3 text verbatim; record the tab/newline residual (`btrim` default trims spaces only) in a comment beside it. Every other line — in particular the five gate-find strings listed in `<interfaces>` — survives unchanged; a one-byte drift there is an `occurrence-mismatch` MEASURE_FAIL in plan 01 task 2.

    COMMENT ON FUNCTION: re-state the snapshot's COMMENT (pg_get_functiondef does not return it, so it is not a needle hazard).

    REVOKE: `REVOKE ALL ON FUNCTION public.match_engine_cron_tick() FROM PUBLIC, anon, authenticated, service_role;` (20260515205431:116 precedent) and NO GRANT — the scheduler runs as `postgres` = owner (`scripts/prod-prober/cron-manifest.json:53`); say so in the comment as 20260907120000:389 does. Note assumption A3 (no out-of-repo service_role caller; grep at HEAD found none) in the comment.

    `DO $verify$` — CATALOGUE READS ONLY (criterion 7; shared TEST has PROD's catalogue and EMPTY tables — a data read that RAISEs on TEST blocks the PROD apply, and the remedy is to revert the merge, never to edit supabase-migrate.yml). Checks, each `RAISE EXCEPTION 'Migration 20260911120000: …'` by name, never a NOTICE-skip: (1) function present, `pronargs = 0`; (2) `prosecdef` true and `proconfig` pins `search_path` (carry 120000:472-485 in kind); (3) owner `rolsuper OR rolbypassrls`; (4) comment strip `regexp_replace(v_def_raw, '--[^\n]*', '', 'g')` into `v_def` and the length sanity check `< 500` with the "strip is broken" message (130000:896-898); (5) three variable-bound needles declared with derivation comments in the 130000:766-775 style — `v_vault_needle := 'FROM vault.decrypted_secrets'`, `v_settings_needle := 'FROM public.system_settings'`, `v_cardinality_needle := 'IF v_cnt > 1 THEN'` — each asserted with `position(needle IN v_def) = 0 -> RAISE` naming the needle; the settings-needle comment MUST say why NOT the bare setting-key token (it is in the RAISE text at 120000:332/:348) and why NOT the C1 `WHERE` line (arm C1 mutates it); (6) whole grantee set: `string_agg(CASE WHEN a.grantee = 0 THEN 'PUBLIC' ELSE pg_get_userbyid(a.grantee) END, ',' ORDER BY 1)` over `aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner)))` filtered to `privilege_type = 'EXECUTE'`, compared `IS DISTINCT FROM pg_get_userbyid(p.proowner)` — never against the literal role name `postgres` (the lane owner is whatever run.sh boots as); a NULL aggregate is its own RAISE; the message says service_role's grant survived 164.7 because only anon/authenticated were probed. Do NOT name `vault.*` or `net.*` as objects in the DO (neither exists on the lane, 120000:400-403). Do NOT re-run the seed-row read at 120000:534-536. Do NOT strip `/* */` — record the residual as 130000:882-886 does.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && F=supabase/migrations/20260911120000_vault_tick_hardening.sql && test -f "$F" && grep -qF "v_vault_needle" "$F" && grep -qF "v_settings_needle" "$F" && grep -qF "v_cardinality_needle" "$F" && grep -qF "FROM PUBLIC, anon, authenticated, service_role;" "$F" && grep -qF "aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner)))" "$F" && grep -qF "IF v_key IS NULL OR btrim(v_key) = '' THEN" "$F" && grep -qF "IF v_url IS NULL OR v_url = '' THEN" "$F" && grep -qF "WHERE s.key = 'analytics_service_url';" "$F" && grep -qF "IF v_url !~ c_url_allowed THEN" "$F" && grep -qF "read it with an admin session. Allowed: an https host under .up.railway.app';" "$F" && test "$(grep -v '^\s*--' "$F" | grep -cF 'FROM vault.decrypted_secrets')" = 1 && test "$(grep -v '^\s*--' "$F" | grep -cF 'FROM public.system_settings')" = 1 && test "$(grep -v '^\s*--' "$F" | grep -cF 'INTO STRICT')" = 0 && test "$(grep -v '^\s*--' "$F" | grep -cF 'v_seed')" = 0 && test "$(grep -v '^\s*--' "$F" | grep -ciF 'PERFORM public.match_engine_cron_tick')" = 0 && node scripts/lint-app-guc.mjs && echo TRACER-SHAPE-OK</automated>
    <fails_when>Any grep in the chain exits non-zero (a required string is absent), any counted occurrence differs from the pinned number (the two needles must occur exactly ONCE in non-comment text; `INTO STRICT`, `v_seed` and an apply-time PERFORM must occur ZERO times), `lint-app-guc.mjs` prints a finding or exits non-zero, or the final line `TRACER-SHAPE-OK` is missing from the output.</fails_when>
  </verify>
  <acceptance_criteria>
    - `supabase/migrations/20260911120000_vault_tick_hardening.sql` exists and is git-tracked after the commit (`git ls-files -- <path>` non-empty).
    - Non-comment text contains exactly ONE `FROM vault.decrypted_secrets` and exactly ONE `FROM public.system_settings` (the needles are falsifiable: deleting the read removes its only occurrence).
    - The five gate-find strings from `<interfaces>` are each present byte-for-byte.
    - `REVOKE ALL ON FUNCTION public.match_engine_cron_tick() FROM PUBLIC, anon, authenticated, service_role;` is present; no executable GRANT statement targets the tick function (`grep -v '^\s*--' <file> | grep -c 'GRANT.*match_engine_cron_tick'` = 0).
    - The DO block declares `v_vault_needle`, `v_settings_needle`, `v_cardinality_needle` as TEXT with the values named in the action, and contains `aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner)))` and `pg_get_userbyid(p.proowner)`.
    - Non-comment text contains ZERO `INTO STRICT`, ZERO `v_seed`, ZERO apply-time PERFORM of the function, ZERO `current_setting('app.` (lint-app-guc corpus scan prints 0 findings for the new file).
    - The header contains a `prod-body-ack` PARAGRAPH stating the pragma line is absent until measured, and NO line beginning `-- prod-body-ack: ` (that line is added by plan 04 after the hash is earned).
    - The header contains both disjointness lists and the quoted defect (`v_fn !~ 'analytics_service_url'` appears in the header comment, quoted as the defect, and nowhere in executable text).
  </acceptance_criteria>
  <reversibility rating="costly">The FILE is reversible until merge; the one-way door is the merge (auto-apply to PROD via supabase-migrate.yml), gated by the checkpoint:decision in plan 06 before the PR opens. Rewriting this body later cascades into every re-pointed gate arm's `find`/`occurrences` and the earned ack hash, which is why it is costly rather than free.</reversibility>
  <done>The new migration exists in the sibling's structure, carries the three variable-bound needles, the count-form Vault read, the btrim guard with V1's RAISE text unchanged, the four-role REVOKE and the aclexplode whole-set check, contains no data read and no apply-time call, and the shape gate above prints TRACER-SHAPE-OK.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the tick gate — apply list, five re-points, arms V2 and G1, roster 11 -> 13</name>
  <files>supabase/tests/test_analytics_service_settings_and_vault_tick.sql</files>
  <read_first>
    - supabase/tests/test_analytics_service_settings_and_vault_tick.sql — whole file once: header :44-56 (no NOTICE-skip), :107-121 (arm-id digit rule; SETUP sub-arms not twinned), :160 and :606 (roster sentinel, header mention + NOTICE), :190 (apply list), :194-204 (DECLARE), arms at :214, :255, :283, :326, :387, :431, :444, :475, :515, :547, :596, V1 shape :229-267, C2-SETUP :398-412
    - scripts/mutation-runner/GRAMMAR.md:73-77 (`occurrences` is REQUIRED and MEASURED), :100-118 (a mutation never rewrites an identity), :540-552 (waivers; ceiling 0)
    - scripts/mutation-runner/run.mjs:4085-4094 (edit steps only on files in the apply list)
    - supabase/tests/test_ledger_refresh_fanout.sql:517-525 (the MIRROR-mutation argument for arm K — V2's twin copies it)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md § tick gate (re-point table, V2 twin CORRECTION, G1 shape)
    - .github/workflows/ci.yml:3156-3220 (how the `ALL N ARMS EXECUTED` sentinel and its roster are read: from the `RAISE NOTICE` line, first match)
  </read_first>
  <action>
    1. APPLY LIST (`:190`): append `supabase/migrations/20260911120000_vault_tick_hardening.sql` as the LAST entry of the `RED-UNDER-SETUP` JSON array. Add a prose line above it in the fan-out gate's words (`test_ledger_refresh_fanout.sql:182-186`): applied LAST because its CREATE OR REPLACE must be the definition the arms run against; every body-editing twin below now names it.

    2. RE-POINT the five body-editing arms (PATTERNS re-point table; R3 at `:547` STAYS on `20260907120000` because it edits the table REVOKE, not the body):
       - V1 `:255`: `file` -> the new migration; `find` -> `IF v_key IS NULL OR btrim(v_key) = '' THEN`; `replace` stays `IF FALSE THEN`; `occurrences` re-measured against the NEW file (expected 1).
       - U1 `:283`, C1 `:387`, C2 `:431`, C3 `:444`: `file` -> the new migration; `find` unchanged; `occurrences` re-measured (expected 1 each — measure with `grep -cF`, never assume).
       - C1's prose at `:376-386` says the mutant survives the migration's own body check because the mutated key still contains the setting-key token — update that sentence to name the NEW needle (`FROM public.system_settings`) and state that the C1 mutation leaves the needle intact by construction, so the apply survives and the arm is the first failure.

    3. ARM V2 (VAULTTICK-EMPTYKEY-01): insert AFTER arm C3's last assertion and BEFORE the `ARM R1` banner. Placement rationale, written into the arm's prose: C3 leaves the url row at the disallowed value set by C2-SETUP (`:405-407`), so on shared TEST BEFORE merge — where the OLD body is live and a whitespace key sails past `v_key = ''` — the callable still refuses on the allow-list re-test before building any request (no egress), and after merge the NEW body's btrim guard fires first. No later arm calls the function, so V2 leaves the blank secret in place; the file's closing ROLLBACK (`:609`) restores TEST's real row. Shape = copy of V1 (`:229-267`): `V2-SETUP` guarded `DELETE FROM vault.decrypted_secrets WHERE name = 'analytics_service_key'` raising `TEST FAILED (V2-SETUP)` by name on any error; then `PERFORM vault.create_secret(' ', 'analytics_service_key', 'phase 164.8.6 gate fixture')` (parameter order secret, name, description — fixture 32:44-50) inside the same guarded block; then invoke the tick function exactly as V1 does at `:257` under an EXCEPTION WHEN OTHERS that only captures `v_raised := true` and `GET STACKED DIAGNOSTICS v_msg = MESSAGE_TEXT` (nothing else in the handler — lint R1); assert `v_raised` (`TEST FAILED (V2): … RETURNED with a single-space analytics_service_key …`) and `v_msg ~ 'analytics_service_key missing from vault'` (`TEST FAILED (V2): … its message does not name analytics_service_key — it reads: %`). Twin, ONE line, the MIRROR mutation and NOT `IF FALSE THEN` (which is V1's twin and would make V1 the first failure — `wrong-first-failure`): `-- RED-UNDER-M: {"arm":"V2","apply":[{"kind":"edit","file":"supabase/migrations/20260911120000_vault_tick_hardening.sql","find":"IF v_key IS NULL OR btrim(v_key) = '' THEN","replace":"IF v_key IS NULL OR v_key = '' THEN","occurrences":1}]}`. Write the `-- RED-UNDER:` prose line explaining the mirror (under this twin V1 stays GREEN because NULL is still caught and only V2 reddens; under V1's twin both redden and V1 is first — the arm-K argument at fanout:517-525). Add the pre-merge note: on shared TEST before the merge V2 is RED BY CONSTRUCTION (old body) — CLAUDE.md coupling (b).

    4. ARM G1 (164.7-WR02): insert AFTER arm T1 (`:596-600`) as the last arm. A `sql`-step twin (never an edit of the migration's REVOKE — that would abort the apply under the new whole-set check; same reason as arm 0 at `:210-214` and T1 at `:586-595`): `-- RED-UNDER-M: {"arm":"G1","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.match_engine_cron_tick() TO service_role"}]}`. Assertion: the SAME aclexplode set query the migration uses (grantee set string vs `pg_get_userbyid(p.proowner)`), raising `TEST FAILED (G1): EXECUTE on match_engine_cron_tick is held by [%], expected exactly the owner [%] …`. Add DECLARE variables `v_owner TEXT` and `v_grantees TEXT` at `:194-204` in the SAME edit. Pre-merge note: RED on shared TEST by construction (the OLD REVOKE left service_role its EXECUTE).

    5. ROSTER: change the `RAISE NOTICE` at `:606` to begin `ALL 13 ARMS EXECUTED (0,V1,U1,U2,C1,C2,C3,V2,R1,R2,R3,T1,G1):` and extend its prose with one clause each for V2 (a single-space secret is refused by name) and G1 (the EXECUTE grantee set is exactly the owner, service_role included in the refusal). Update the header mention at `:160` to the same count. ci.yml:3163 reads the sentinel off the `RAISE NOTICE` line (first match) — keep exactly ONE `RAISE NOTICE '…ALL 13 ARMS EXECUTED (…)` line.

    Identity rule: every new arm id carries a digit (`V2`, `G1`); every `-- RED-UNDER:` prose line is line-start anchored with its `-- RED-UNDER-M:` JSON on the very next line, on ONE line.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && G=supabase/tests/test_analytics_service_settings_and_vault_tick.sql && M=supabase/migrations/20260911120000_vault_tick_hardening.sql && grep -qF "\"$M\"]}" "$G" && test "$(grep -cF '"arm":"V2"' "$G")" = 1 && test "$(grep -cF '"arm":"G1"' "$G")" = 1 && grep -qF '"arm":"V2","apply":[{"kind":"edit","file":"'"$M"'","find":"IF v_key IS NULL OR btrim(v_key) = '"'"''"'"' THEN","replace":"IF v_key IS NULL OR v_key = '"'"''"'"' THEN","occurrences":1}]}' "$G" && grep -qF '"arm":"G1","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.match_engine_cron_tick() TO service_role"}]}' "$G" && test "$(grep -c 'RED-UNDER-M:.*"kind":"edit","file":"'"$M"'"' "$G")" = 6 && test "$(grep -c 'RED-UNDER-M:.*"kind":"edit","file":"supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql"' "$G")" = 1 && test "$(grep -c "RAISE NOTICE 'ALL 13 ARMS EXECUTED (0,V1,U1,U2,C1,C2,C3,V2,R1,R2,R3,T1,G1)" "$G")" = 1 && node scripts/lint-sql-gates.mjs --self-test | grep -qF '7 rules, red+green each' && node scripts/lint-sql-gates.mjs && node scripts/mutation-runner/run.mjs --parse-only && echo GATE-SHAPE-OK</automated>
    <fails_when>Any pinned count is off (6 edit twins must name the new file — V1, U1, C1, C2, C3, V2; exactly 1 must still name 20260907120000 — R3; exactly one roster NOTICE with the 13-id list), the V2 or G1 twin JSON does not match byte-for-byte, `lint-sql-gates.mjs` prints a rule violation or exits non-zero, `run.mjs --parse-only` reports a parse/bad-file-ref/occurrence defect or exits non-zero, or `GATE-SHAPE-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `RED-UNDER-SETUP` apply list at `:190` ends with `supabase/migrations/20260911120000_vault_tick_hardening.sql`.
    - Exactly 6 `RED-UNDER-M` edit twins target the new migration (V1, U1, C1, C2, C3, V2) and exactly 1 (R3) still targets `20260907120000`.
    - V2's twin is the MIRROR mutation (`replace` = `IF v_key IS NULL OR v_key = '' THEN`), occurrences 1; G1's twin is the `sql` GRANT to service_role.
    - `TEST FAILED (V2-SETUP)`, `TEST FAILED (V2)`, and `TEST FAILED (G1)` each appear at least once as RAISE texts; no `RAISE NOTICE 'SKIP` anywhere in the file.
    - `v_owner` and `v_grantees` are declared in the DECLARE block.
    - Exactly one `RAISE NOTICE 'ALL 13 ARMS EXECUTED (0,V1,U1,U2,C1,C2,C3,V2,R1,R2,R3,T1,G1)` line; the header mention at :160 says 13.
    - `node scripts/lint-sql-gates.mjs` (7 rules) and `node scripts/mutation-runner/run.mjs --parse-only` both exit 0.
  </acceptance_criteria>
  <done>The tick gate applies the new migration last, every body-editing arm names it with re-measured occurrences, V2 and G1 exist with the twins named above, the roster reads 13, and both static gates (lint + parse-only) are green.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the tracer on the pg-lane — narrowed runner run over the tick gate</name>
  <files>supabase/tests/test_analytics_service_settings_and_vault_tick.sql, supabase/migrations/20260911120000_vault_tick_hardening.sql</files>
  <read_first>
    - scripts/mutation-runner/run.mjs:1-30 (header: `--file` is a NARROWED DIAGNOSTIC run that exits 2 on a defect-free run, never 0) and :4500-4560 (the `No defects in the narrowed scope.` line)
    - scripts/pg-lane/run.sh:15-45 (usage: `--apply <file.sql> [<file.sql> ...]`; exit 0 iff the gate passed) and scripts/pg-lane/install-pg-cron-macos.sh (the PGBIN refusal)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md § Environment Availability (PATH `pg_ctl` is 18.6 without pg_cron; `export PGBIN=/opt/homebrew/opt/postgresql@16/bin` is mandatory)
  </read_first>
  <action>
    With `export PGBIN=/opt/homebrew/opt/postgresql@16/bin` exported in the shell (never a one-shot prefix — the check script refuses the mismatch verbatim), first run the gate GREEN on a lane: `bash scripts/pg-lane/run.sh --workdir "$SCRATCH/lane-tick" --apply <the seven files from the apply list at :190, in order> --gate supabase/tests/test_analytics_service_settings_and_vault_tick.sql` and confirm exit 0 with the `ALL 13 ARMS EXECUTED` notice in the psql stream. Then run the narrowed mutation run `node scripts/mutation-runner/run.mjs --file supabase/tests/test_analytics_service_settings_and_vault_tick.sql` and read its report: every one of the 13 twins (0, V1, U1, U2, C1, C2, C3, V2, R1, R2, R3, T1, G1) must bite its own arm FIRST; the expected closing line is `No defects in the narrowed scope.` with EXIT 2 (a narrowed run never exits 0 — do not "fix" that). If the run reports `no-red` for V1/U1/C1/C2/C3, the re-point missed one; `occurrence-mismatch` means a `find` string drifted in the re-base (task 1) — fix the SOURCE, never add a waiver (`WAIVED_CEILING` is 0 and stays 0). If V2 reports `wrong-first-failure: V1`, the twin is not the mirror mutation. Paste the runner's final block (arms line, `lane-invocations`, defects line, exit code) into the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && export PGBIN=/opt/homebrew/opt/postgresql@16/bin && OUT="$(node scripts/mutation-runner/run.mjs --file supabase/tests/test_analytics_service_settings_and_vault_tick.sql 2>&1)"; RC=$?; printf '%s\n' "$OUT" | tail -25; test "$RC" = 2 && printf '%s\n' "$OUT" | grep -qF 'No defects in the narrowed scope.' && printf '%s\n' "$OUT" | grep -qF 'NARROWED DIAGNOSTIC RUN' && ! printf '%s\n' "$OUT" | grep -qE 'no-red|wrong-first-failure|occurrence-mismatch|bad-file-ref|waiver' && echo TICK-LANE-OK</automated>
    <fails_when>Exit code is anything other than 2 (0 means the run was not narrowed as intended; 1 means defects), the line `No defects in the narrowed scope.` is absent, any of `no-red`, `wrong-first-failure`, `occurrence-mismatch`, `bad-file-ref` or `waiver` appears in the report, or `TICK-LANE-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `bash scripts/pg-lane/run.sh … --gate supabase/tests/test_analytics_service_settings_and_vault_tick.sql` exits 0 and its psql stream contains `ALL 13 ARMS EXECUTED`.
    - `node scripts/mutation-runner/run.mjs --file supabase/tests/test_analytics_service_settings_and_vault_tick.sql` exits 2 and prints `No defects in the narrowed scope.`; the report contains no `no-red`, `wrong-first-failure`, `occurrence-mismatch`, `bad-file-ref` or `waiver` row.
    - The SUMMARY carries the pasted final block of the narrowed run (arm count 13, lane-invocations, exit code 2).
  </acceptance_criteria>
  <done>The single path — new migration applied last on the lane, 13 arms each biting first — is proven end-to-end and committed; the SQL wave's architecture (apply-list coupling, disjoint needles, mirror twin) is validated before plans 02-04 build out.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 01's share)

- NEW `supabase/migrations/20260911120000_vault_tick_hardening.sql` — re-based `public.match_engine_cron_tick()` (count+max Vault read, `IF v_cnt > 1 THEN`, `btrim(v_key) = ''`), four-role REVOKE, `DO $verify$` with `v_vault_needle` / `v_settings_needle` / `v_cardinality_needle` and the aclexplode whole-set check. Its `-- prod-body-ack:` line is added by plan 04.
- MODIFIED `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` — apply list + new migration; arms V1/U1/C1/C2/C3 re-pointed; NEW arms `V2` (sub-arm `V2-SETUP`) and `G1`; DECLARE `v_owner`, `v_grantees`; roster sentinel `ALL 13 ARMS EXECUTED (0,V1,U1,U2,C1,C2,C3,V2,R1,R2,R3,T1,G1)`.
- New RAISE text: `analytics_service_key is not unique in vault (% rows) — refusing to pick one`.
- New gate RAISE texts: `TEST FAILED (V2-SETUP)`, `TEST FAILED (V2)`, `TEST FAILED (G1)`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vault -> SECURITY DEFINER body | the secret value crosses from `vault.decrypted_secrets` into an outbound header; the body must refuse blank/ambiguous values by name |
| DB roles -> EXECUTE on the tick | browser-reachable roles and service_role must not be able to invoke a function that posts the analytics service key |
| migration merge -> PROD | anything in `supabase/migrations/**` auto-applies to PROD after shared TEST |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-01 | Elevation of Privilege | `public.match_engine_cron_tick()` EXECUTE ACL | high | mitigate | REVOKE from service_role + DO block asserts grantee set == {owner} via aclexplode; gate arm G1 proves a GRANT reddens |
| T-164.8.6-02 | Tampering | duplicate `analytics_service_key` row selects an arbitrary key | medium | mitigate | `IF v_cnt > 1 THEN` RAISE by name; asserted by needle (shape-only, not constructible on lane/TEST — recorded) |
| T-164.8.6-03 | Denial of Service / Repudiation | whitespace key -> 401 hidden behind a green cron history | medium | mitigate | `btrim(v_key) = ''` guard; arm V2 stores a single space and asserts RAISE by name |
| T-164.8.6-04 | Information Disclosure | RAISE text echoing a secret or URL value | medium | mitigate | count-guard message prints a ROW COUNT only; C3's "name the setting, never its value" line preserved byte-for-byte |
| T-164.8.6-05 | Tampering (verification integrity) | a verification check satisfied by its own prose | high | mitigate | variable-bound statement-shape needles over comment-stripped body; falsifier task in plan 04 |
| T-164.8.6-06 | Denial of Service (deploy) | a DATA read in the DO block refuses on empty shared TEST and blocks the PROD apply | high | mitigate | catalogue-only reads; acceptance criteria grep ZERO `v_seed`; criterion 7 |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | this phase installs NO packages (RESEARCH § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Shape gate on the migration (task 1 verify) prints `TRACER-SHAPE-OK`.
- Static gates on the tick gate (task 2 verify) print `GATE-SHAPE-OK`: lint-sql-gates 7 rules, `--parse-only` clean.
- Lane proof (task 3 verify): gate exit 0 with `ALL 13 ARMS EXECUTED`; narrowed runner exit 2 with `No defects in the narrowed scope.`.
</verification>

<success_criteria>
- ROADMAP criteria 1, 2, 3, 4 are implemented for `match_engine_cron_tick` in a NEW migration (no applied migration edited for a fix).
- Needle set and gate `find` set are disjoint (both pasted in the migration header).
- 13 arms bite first on the lane; zero waivers.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-01-SUMMARY.md` when done, carrying the narrowed run's final block verbatim.
</output>
