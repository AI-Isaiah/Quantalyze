---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 08
type: execute
wave: 6
depends_on:
  - 164.8.6-07
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - scripts/prod-prober/fixtures/cron-drift/hygiene-red.json
  - scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json
  - src/__tests__/prod-prober-wiring.test.ts
  - VERSION
  - package.json
  - CHANGELOG.md
autonomous: true
requirements:
  - 164.8.5-HYGIENE-RESIDUALS
tags: [prod-prober, javascript, hygiene, fixtures, release]

estimate:
  tokens: 150000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "ONE whole-token measure (`tokenMeasure(span)`) replaces per-shape exemptions: it splits each executable span on whitespace AND operators, joins adjacent string-literal operands across `||` and across ANY callee's argument list (not only `BUILDERS`), applies ONE derived-length threshold with the URL/UUID/JWT exemptions already hoisted (`:1471-1490`), and drops the digit requirement ONLY for tokens whose derived length came from concatenation — TODOS.md:1602-1635 (`whole-token measure`), 164.8.5-HYGIENE-RESIDUALS"
    - "Shapes 2 (a `||`-split value outside a header region; a non-`BUILDERS` wrapper), 3 (alphabetic-only 32+ token, CONCATENATION-derived) and 4 (a `MIGRATION_FILENAME_RE`-shaped token inside a string LITERAL) each have a red row in `hygiene-red.json` that fires its rule ALONE; `hygiene-green.json` and `hygiene-bypass.json` stay clean (the zero-false-positive budget); `fixture_raise_prose_job` (34-char alphabetic prose token) stays GREEN"
    - "Shapes 1 (too-deep DO -> `command-unjudgeable`, LOUD) and the comment-side of shape 4 (`provenance` exemption is comment-only; a comment cannot execute and gitleaks scans repo text) are RECORDED as deliberate refusals with corrected docstrings, not silently left"
    - "The committed `cron-manifest.json`'s 14 commands remain at ZERO violations; `node scripts/prod-prober/run.mjs --self-test` green; the JS PR opens ONLY after the SQL PR is merged and touches no `supabase/**` path"
  artifacts:
    - "scripts/prod-prober/arms/cron-drift.mjs (`tokenMeasure`, rule text at :1492-1495 updated, docstrings corrected)"
    - "scripts/prod-prober/fixtures/cron-drift/hygiene-red.json (+3 rows with `note`s, assembled operands)"
    - "scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json (`expect_any_of` admitted ids if a bypass shape's firing rule changes)"
    - "src/__tests__/prod-prober-wiring.test.ts (`[164.8.6-08]` describe)"
    - "Release commit v0.77.34.1 + the JS PR"
  key_links:
    - "`HYGIENE_RULE_IDS` (`cron-drift.mjs:292`) must equal the set of rule ids in `hygiene-red.json` member for member (self-test, scripts/prod-prober/run.mjs:2252-2257, `HYGIENE_RULE_IDS`): a new id needs a red row; a new shape under an existing id needs a red row tagged with that id"
    - "`hygiene-green.json` is the false-positive budget — `fixture_raise_prose_job` is green BY DESIGN; shape 3's red row must be concatenation-derived, never a bare prose token (PATTERNS conflict resolution)"
    - "Repo is PUBLIC and CI runs gitleaks: red rows use assembled short operands, never a whole credential-shaped token"
---

<objective>
JS WAVE, PART 2 — the whole-token measure for the five hygiene residuals, with a red row per unreported shape and the green/bypass corpora unchanged. Then the JS PR, after the SQL PR has landed.

Purpose: TODOS.md:1602-1635 (`164.8.5-HYGIENE-RESIDUALS`) — the DECISION recorded there: a whole-token measure rather than a sixth exemption, because every narrowing risks the zero-false-positive budget the arm depends on.
Output: `tokenMeasure`, three red rows, corrected docstrings, a `[164.8.6-08]` describe, release commit, JS PR.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md
# read at execution when present (produced by an earlier plan of this phase): .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-07-SUMMARY.md
@scripts/prod-prober/arms/cron-drift.mjs
@scripts/prod-prober/fixtures/cron-drift/hygiene-red.json
@scripts/prod-prober/fixtures/cron-drift/hygiene-green.json
@scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json
</context>

<interfaces>
Consumed (by line, at HEAD): `MAX_DOLLAR_DEPTH = 4` (`:566`, shape 1 — unchanged); `BUILDERS` (`:685-686`) and the "skipped whole" residual (`:900-903`, `:939`); `headerRegions` (`:1028`); `derivedLiteralLength` (`:909`); `TOKEN_MIN = HEADERS_LITERAL_MAX = 32` (`:378`); the digit test `if (!/\d/.test(token)) continue;` (`:1464`); `MIGRATION_FILENAME_RE` (`:488`) honoured only with `provenance` (`:1488`); the `long-token-anywhere` rule text (`:1492-1495`); `HYGIENE_RULE_IDS` (`:292`); `UNJUDGEABLE_RULE_IDS` (`:1752`).
Produced: `tokenMeasure(span)` (module-level function, exported for the wiring test); red rows `fixture_split_outside_header_job`, `fixture_nonbuilder_wrapper_job`, `fixture_concat_alpha_job`, `fixture_filename_shaped_literal_job` (names are Claude's discretion; keep the `fixture_*_job` convention); describe `[164.8.6-08] whole-token measure`; version `0.77.34.1`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: `tokenMeasure` — one derived-length measure over executable spans, replacing shape-specific exemptions</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs</files>
  <read_first>
    - scripts/prod-prober/arms/cron-drift.mjs:283-320 (`HYGIENE_RULE_IDS` — rule-id contract and self-test coupling), :378 (`TOKEN_MIN`), :488 (`MIGRATION_FILENAME_RE`), :563-566 (`MAX_DOLLAR_DEPTH`), :685-686 (`BUILDERS`), :900-939 (`derivedLiteralLength` and the "skipped whole" branch), :1028 (`headerRegions`), :1455-1495 (`long-token-anywhere`: the digit test at :1464, the hoisted URL/UUID/JWT exemptions :1471-1490, the rule text :1492-1495), :1332-1342 and :1752 (`command-unjudgeable` routing — unchanged)
    - scripts/prod-prober/fixtures/cron-drift/hygiene-green.json (every row is a false-positive-budget row; read `fixture_raise_prose_job`'s note)
    - scripts/prod-prober/run.mjs:2252-2257 (rule-id set equality self-test)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md §Q7 "Remedy" (i)-(iv) and PATTERNS § cron-drift.mjs (the shape-3 conflict and its resolution)
  </read_first>
  <action>
    Implement `export function tokenMeasure(span)` returning, per candidate token, `{ token, derivedLength, fromConcat, literalOperands }`: (i) tokenize the executable span on whitespace AND SQL operators/punctuation (`||`, `(`, `)`, `,`, `:=`, `=`), keeping string literals intact; (ii) join ADJACENT string-literal operands across `||` and across ANY callee's argument list — not only `BUILDERS` — into one derived token (`fromConcat = true` when more than one operand contributed); (iii) apply ONE threshold, `derivedLength >= TOKEN_MIN`, with the existing URL/UUID/JWT exemptions applied to the DERIVED token (reuse the hoisted predicates at :1471-1490, do not duplicate them); (iv) require a digit ONLY when `fromConcat` is false — a single short literal like `'application/json'` keeps the digit guard that protects the green corpus, while an alphabetic 32+ token assembled by concatenation fires. Route `tokenMeasure` findings through the existing `long-token-anywhere` id for shapes 2 and 3 (they are the same claim: a credential-length token in executable text) and through `long-literal-in-headers` when the derived token sits inside a header region; for shape 4, a `MIGRATION_FILENAME_RE`-matching token inside a string LITERAL is judged by `tokenMeasure` like any other literal (the `provenance` exemption at :1488 stays COMMENT-ONLY — narrow its predicate so it applies only to comment spans, and correct its docstring: a filename-shaped token in a comment is exempt because a comment cannot execute and gitleaks scans repo text; the sentence "the prefix and suffix stop this being a one-step off-switch" is replaced by that honest statement). Keep `derivedLiteralLength`/`headerRegions` for the header rule but make the "skipped whole" branch (:900-903, :939) delegate to `tokenMeasure` instead of skipping. Update the rule text at :1492-1495 so the predicate sentence matches the new predicate ("containing a digit, or assembled by concatenation"). Shape 1 stays a refusal: do not change `MAX_DOLLAR_DEPTH` or the `command-unjudgeable` routing; add one docstring sentence recording that as the decision. No new rule id unless a shape genuinely has no honest home in the existing ids — if one is added, it goes in `HYGIENE_RULE_IDS` with a red row (task 2). A GUARD THAT REFUSES MUST BE ADDITIVE, NEVER SUBSTITUTIVE (164.8.5 doctrine): `tokenMeasure` adds findings; it never suppresses one an existing rule produced.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && F=scripts/prod-prober/arms/cron-drift.mjs && grep -qE '^export function tokenMeasure\(' "$F" && grep -qF 'fromConcat' "$F" && grep -qE 'export const MAX_DOLLAR_DEPTH = 4;|const MAX_DOLLAR_DEPTH = 4;' "$F" && grep -qF 'assembled by concatenation' "$F" && node -e "import('./scripts/prod-prober/arms/cron-drift.mjs').then(m => { const r = m.tokenMeasure(\"jsonb_build_object('X-Auth', 'FAKEKEYABCDEFGHIJ' || 'KLMNOPQRSTUVWXYZ')\"); const hit = r.find(t => t.fromConcat && t.derivedLength >= 32); if (!hit) { console.error('no concat-derived 32+ token'); process.exit(1); } const g = m.tokenMeasure(\"SELECT 'application/json'\"); if (g.some(t => t.derivedLength >= 32)) { console.error('short literal measured long'); process.exit(1); } })" && node scripts/prod-prober/run.mjs --self-test && echo TOKEN-MEASURE-OK</automated>
    <fails_when>`tokenMeasure` is not exported, lacks the `fromConcat` field, `MAX_DOLLAR_DEPTH` changed from 4, the rule-text predicate was not updated, the inline probe finds no concatenation-derived 32+ token for the split alphabetic key (or measures `'application/json'` as long), the prober self-test exits non-zero (rule-id set mismatch or a green row went red), or `TOKEN-MEASURE-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `export function tokenMeasure(span)` exists; results carry `token`, `derivedLength`, `fromConcat`, `literalOperands`.
    - `tokenMeasure("jsonb_build_object('X-Auth', 'FAKEKEYABCDEFGHIJ' || 'KLMNOPQRSTUVWXYZ')")` yields a token with `fromConcat === true` and `derivedLength >= 32`; `tokenMeasure("SELECT 'application/json'")` yields none `>= 32`.
    - `MAX_DOLLAR_DEPTH` is still 4; `UNJUDGEABLE_RULE_IDS` routing unchanged; the `provenance` exemption applies to comment spans only and its docstring states why.
    - The `long-token-anywhere` rule text names the new predicate.
    - `node scripts/prod-prober/run.mjs --self-test` passes (green and bypass corpora unchanged and clean).
  </acceptance_criteria>
  <done>One measure decides what a token is; exemptions are hoisted, not multiplied; refusals are recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Red rows for shapes 2, 3, 4; green/bypass unchanged; `[164.8.6-08]` describe</name>
  <files>scripts/prod-prober/fixtures/cron-drift/hygiene-red.json, scripts/prod-prober/fixtures/cron-drift/hygiene-bypass.json, src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - scripts/prod-prober/fixtures/cron-drift/hygiene-red.json (row shape `{rule, jobname, command, note?}`; analog rows `fixture_nonanchored_split_job`, `fixture_var_key_job`, `fixture_unbalanced_headers_job` and their `note` discipline)
    - scripts/prod-prober/fixtures/cron-drift/hygiene-green.json (`fixture_raise_prose_job` must stay green), hygiene-bypass.json:1-12 (`expect_any_of`)
    - scripts/prod-prober/run.mjs:2240-2270 (how the self-test consumes the red/green/bypass corpora; each red row must fire its rule ALONE)
    - src/__tests__/prod-prober-wiring.test.ts:1798-1830 (`DRIFT_FIXTURES`, the fixture loading pattern) and the `[164.8.6-07]` describe (plan 07)
  </read_first>
  <action>
    Add to `hygiene-red.json`, each with a `note` naming the shape, the TODOS id, the mechanism that fires it and why no other rule fires, using assembled short operands (never a whole credential-shaped token — public repo, gitleaks): shape 2a `fixture_split_outside_header_job` — a `||`-split value OUTSIDE any header region (e.g. assigned to a local in a DO body, operands each < 32), `rule: long-token-anywhere`; shape 2b `fixture_nonbuilder_wrapper_job` — the same split inside a callee NOT in `BUILDERS` (e.g. `coalesce(...)`), `rule: long-token-anywhere`; shape 3 `fixture_concat_alpha_job` — an alphabetic-only 32+ token DERIVED BY CONCATENATION (`'FAKEKEYABCDEFGHIJ' || 'KLMNOPQRSTUVWXYZ'`), `rule: long-token-anywhere`; shape 4 `fixture_filename_shaped_literal_job` — a `20260101000000_<32 lowercase/digit chars>.sql`-shaped token INSIDE a string literal in executable text, `rule: long-token-anywhere`. Do NOT add a bare 32+ alphabetic prose token (that is `fixture_raise_prose_job`'s green shape by design). If `tokenMeasure` changes WHICH rule fires for an existing bypass row, admit the new id in that row's `expect_any_of` and record why in its note — never delete a bypass row. Add describe `[164.8.6-08] whole-token measure (TODOS 164.8.5-HYGIENE-RESIDUALS)` to the wiring test: (a) each of the four new red rows produces exactly its declared rule (import `judgeRow` or the exported judge the self-test uses) and no other credential rule; (b) every `hygiene-green.json` row still produces zero violations (the budget, asserted by name for `fixture_raise_prose_job`); (c) the committed `scripts/prod-prober/cron-manifest.json`'s 14 commands produce zero violations (the STATE.md invariant "stayed at ZERO violations through every narrowing and widening"); (d) a RED control: `tokenMeasure` with `fromConcat` forced false on the shape-3 command yields no 32+ token — proving the concat carve-out is what makes shape 3 fire. Run `node scripts/prod-prober/run.mjs --self-test` — it fails loud on a red row whose rule never fires or on a rule id without a red row; do not bump `SELF_TEST_SCENARIOS` (no scenario added; if the executor adds one anyway, bump `scripts/prod-prober/run.mjs:149` AND `prod-prober-wiring.test.ts:1774` in the same commit).
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && R=scripts/prod-prober/fixtures/cron-drift/hygiene-red.json && node -e "const r=require('./$R'); const need=['fixture_split_outside_header_job','fixture_nonbuilder_wrapper_job','fixture_concat_alpha_job','fixture_filename_shaped_literal_job']; for (const n of need) { const row=r.find(x=>x.jobname===n); if(!row){console.error('missing '+n);process.exit(1);} if(!row.note){console.error('no note on '+n);process.exit(1);} } const g=require('./scripts/prod-prober/fixtures/cron-drift/hygiene-green.json'); if(!g.some(x=>x.jobname==='fixture_raise_prose_job')){console.error('green calibration row gone');process.exit(1);}" && node scripts/prod-prober/run.mjs --self-test && grep -qF '[164.8.6-08]' src/__tests__/prod-prober-wiring.test.ts && grep -qF 'fixture_raise_prose_job' src/__tests__/prod-prober-wiring.test.ts && grep -qF 'cron-manifest.json' src/__tests__/prod-prober-wiring.test.ts && npx vitest run src/__tests__/prod-prober-wiring.test.ts && grep -qE 'SELF_TEST_SCENARIOS\).toBe\(78\)' src/__tests__/prod-prober-wiring.test.ts && grep -qE '^export const SELF_TEST_SCENARIOS = 78;' scripts/prod-prober/run.mjs && echo RED-ROWS-OK</automated>
    <fails_when>Any of the four red rows is missing or lacks a `note`; the green calibration row was removed; the prober self-test exits non-zero (a red row's rule does not fire alone, a green/bypass row went red, or the rule-id sets differ); the `[164.8.6-08]` describe, its green-budget assertion or its manifest-zero assertion is missing; the wiring test fails; the scenario pins moved without a scenario; or `RED-ROWS-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Four new red rows with the named jobnames, each carrying `note`, each firing its declared rule alone under the self-test.
    - `hygiene-green.json` unchanged (`git diff --quiet -- scripts/prod-prober/fixtures/cron-drift/hygiene-green.json`); bypass rows never deleted (row count unchanged).
    - `[164.8.6-08]` describe asserts: per-row rule, green budget by name, committed-manifest zero violations, and the `fromConcat` red control.
    - `node scripts/prod-prober/run.mjs --self-test` exits 0 at 78 scenarios; wiring test green.
  </acceptance_criteria>
  <done>Every previously unreported executable shape has a red row that fires alone; the false-positive budget is intact and asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Release commit v0.77.34.1 and the JS PR — after the SQL PR has landed, touching no `supabase/**` path</name>
  <files>VERSION, package.json, CHANGELOG.md</files>
  <read_first>
    - CLAUDE.md § CHANGELOG discipline (the six-step mechanism)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-06-SUMMARY.md (the SQL PR number)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-07-SUMMARY.md (the lineage note to carry into the PR body)
  </read_first>
  <precondition>The SQL PR from plan 06 is MERGED (`gh pr view <N> --json state --jq .state` prints `MERGED`) and `main` has been fetched; the JS branch is rebased onto it (`git merge-base --is-ancestor origin/main HEAD`).</precondition>
  <action>
    Rebase the JS wave onto `origin/main` (which now carries the SQL PR). CHECKLIST: `git log origin/main..HEAD --oneline` (count), `git diff origin/main...HEAD` (read); assert that the name-only diff against origin/main lists no path under the supabase directory — `supabase-migrate.yml` must not trigger on this PR. Write CHANGELOG entry `## [0.77.34.1] - <date>`: `### Fixed` (manifest-side hygiene loop hoisted — 164.8.5-MANIFEST-SIDE-LOOP-DEAD, ordering half; lineage: re-animated by PR #776), `### Added` (`tokenMeasure`; four red rows; `[164.8.6-07]`/`[164.8.6-08]` describes), `### Notes` (shape 1 and the comment-side of shape 4 are recorded refusals; green corpus unchanged; 14 manifest commands at zero; 164.5.1's re-capture clause partly moot). Cross-check every commit maps to a bullet. Set VERSION and package.json to `0.77.34.1` by hand (byte-equal). Commit `chore(release): v0.77.34.1 — VAULTTICKFIX JS wave` with trailers. Push and `gh pr create --base main --title "VAULTTICKFIX (164.8.6) JS wave: manifest-side hygiene un-skippable + whole-token measure" --body` stating: separate wave by founder decision (no `supabase/**` path; migration discipline does not apply); the PR #776 lineage; the red controls; the footer `🤖 Generated with [Claude Code](https://claude.com/claude-code)`. Record the PR number in the SUMMARY. Do not merge.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && git fetch -q origin main && test "$(git diff origin/main...HEAD --name-only | grep -c '^supabase/')" = 0 && V="$(tr -d '[:space:]' < VERSION)" && test "$V" = "$(node -p "require('./package.json').version")" && test "$V" = "0.77.34.1" && grep -qF '## [0.77.34.1]' CHANGELOG.md && grep -qF '164.8.5-HYGIENE-RESIDUALS' CHANGELOG.md && grep -qF '164.8.5-MANIFEST-SIDE-LOOP-DEAD' CHANGELOG.md && npx vitest run src/__tests__/critical-regressions.test.ts && N="$(gh pr list --state open --search 'VAULTTICKFIX (164.8.6) JS wave' --json number --jq '.[0].number')" && test -n "$N" && gh pr view "$N" --json body --jq .body | grep -qF 'Generated with [Claude Code]' && echo "JS-PR-OPEN #$N"</automated>
    <fails_when>The diff against origin/main lists any `supabase/` path (count not 0 — the migrate workflow would trigger); VERSION and package.json differ or are not `0.77.34.1`; the CHANGELOG lacks the heading or either TODOS id; `critical-regressions.test.ts` fails; no open PR matches the JS-wave title; the body lacks the footer; or `JS-PR-OPEN` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Precondition held: the SQL PR state is `MERGED` before this task ran (recorded in the SUMMARY with its number).
    - `git diff origin/main...HEAD --name-only` contains no `supabase/` path.
    - `VERSION` == package.json version == `0.77.34.1`; CHANGELOG entry `## [0.77.34.1]` with the commit-to-bullet cross-check table in the SUMMARY.
    - An open PR with the JS-wave title exists; its number is recorded in the SUMMARY; no merge performed.
  </acceptance_criteria>
  <done>The JS wave is its own reviewed PR, opened only after the SQL PR landed, carrying no migration path.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 08's share)

- MODIFIED `scripts/prod-prober/arms/cron-drift.mjs` — `export function tokenMeasure(span)`; comment-only `provenance` exemption with corrected docstring; updated `long-token-anywhere` rule text; shape-1 refusal recorded.
- MODIFIED `scripts/prod-prober/fixtures/cron-drift/hygiene-red.json` — rows `fixture_split_outside_header_job`, `fixture_nonbuilder_wrapper_job`, `fixture_concat_alpha_job`, `fixture_filename_shaped_literal_job` (with notes).
- POSSIBLY MODIFIED `hygiene-bypass.json` (`expect_any_of` admissions only; no row deleted).
- MODIFIED `src/__tests__/prod-prober-wiring.test.ts` — describe `[164.8.6-08] whole-token measure`.
- Release commit `chore(release): v0.77.34.1 — VAULTTICKFIX JS wave`; the JS PR (number in the SUMMARY).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD `cron.job` command text -> hygiene judge | a credential assembled from short operands must be measured whole |
| fixture corpus -> public repo | red rows must not themselves be credential-shaped tokens |
| false-positive budget | an arm that cries wolf gets switched off; the green corpus is the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-30 | Information Disclosure | a credential split by `||` or wrapped in a non-BUILDERS callee evading every length rule | high | mitigate | `tokenMeasure` joins operands across `||` and any callee; red rows for shapes 2a/2b/3/4 |
| T-164.8.6-31 | Denial of Service (control) | a false positive on a green row disabling the arm in practice | high | mitigate | digit guard kept for non-concatenated tokens; green corpus asserted unchanged; committed manifest at zero |
| T-164.8.6-32 | Information Disclosure | a fixture row that is itself a real-looking credential in a public repo | medium | mitigate | assembled short operands only; gitleaks in CI |
| T-164.8.6-33 | Repudiation | a refusal (`command-unjudgeable`) mistaken for a clean verdict | medium | accept | LOUD by design (`UNJUDGEABLE_RULE_IDS` -> measure-fail); recorded as a deliberate refusal |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- `TOKEN-MEASURE-OK` (task 1), `RED-ROWS-OK` (task 2), `JS-PR-OPEN #N` (task 3); prober self-test 78 scenarios green throughout.
</verification>

<success_criteria>
- One whole-token measure; four red rows firing alone; green/bypass corpora intact; manifest at zero; JS PR opened after the SQL PR landed with no `supabase/` path.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-08-SUMMARY.md` when done.
</output>