---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 05
type: execute
wave: 3
depends_on:
  - 164.8.6-03
  - 164.8.6-04
files_modified:
  - scripts/mutation-runner/run.mjs
  - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-FLOORS.log
autonomous: true
requirements:
  - VAULTTICK-EMPTYKEY-01
  - 164.7-WR02-SERVICE-ROLE-EXECUTE
  - 164.7-CR05-VACUOUS-MIGRATION-CHECK
  - 164.7-WR01-VAULT-NOT-STRICT
  - APPGUC-WARNING-UNINSTRUMENTED-01
  - 164.7-DORMANCY-UNINSTRUMENTED
  - 164.7-MIGRATION-COMMENT-DRIFT
tags: [mutation-runner, floors, ci-gates, reviewers]

estimate:
  tokens: 110000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`ARMS_FLOOR` is re-pinned to the MEASURED biting-arm count N of a full-corpus lane run at the final SQL commit, separated in BOTH directions: N+1 -> `ARMS_FLOOR regression: N biting arm(s) < floor N+1` exit 1; N -> `✅ No defects` exit 0; `src/__tests__/mutation-runner-floors.test.ts` green (the stale-low direction) — ROADMAP criterion 3's ⚠️, TODOS VAULTTICK-EMPTYKEY-01"
    - "`FILES_FLOOR` stays 46 (no new gate file) and `WAIVED_CEILING` stays 0; a dated CURRENCY block beside `ARMS_FLOOR` records the three runs with exit codes and wall clocks and cites `164.8.6-05-FLOORS.log`"
    - "Every quick gate is green on the SQL tree: lint-sql-gates (7 rules), lint-app-guc (0 findings), `npm run lint`, `npm run typecheck`, `npm run schema:functions:check`, the floors/wiring/contract vitest files"
    - "The three reviewers (migration-reviewer, rls-policy-auditor, silent-failure-hunter) ran over the SQL diff BEFORE any PR exists and every finding is fixed or explicitly dispositioned in the SUMMARY — ROADMAP criterion 6"
  artifacts:
    - "scripts/mutation-runner/run.mjs (ARMS_FLOOR re-pinned + CURRENCY 2026-09-xx block)"
    - ".planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-FLOORS.log"
    - "164.8.6-05-SUMMARY.md § Reviewers (three verdicts, findings, dispositions)"
  key_links:
    - "run.mjs gates `bitingArms < armsFloor` (upper); the floors test pins `ARMS_FLOOR === twins - waivers` (lower) — two layers by design (CLAUDE.md § Two floors, two layers)"
    - "`sql-mutation`'s `timeout-minutes` is a declared CEILING of 20 (ci.yml:1386, `timeout-minutes: 20`) — never raised; a crossing is answered by [REDUNDER-SUBSET-SPLIT]"
---

<objective>
Pin the floor the new arms moved, prove the whole SQL tree green on every gate CI will run, and run the three reviewers BEFORE a PR exists (criterion 6 is an ORDERING constraint, not a checklist item).

Purpose: ROADMAP criterion 3's floor clause, criterion 6, and the phase-gate row of VALIDATION.md (full suite green on a clean tree, SHA-bound).
Output: `ARMS_FLOOR` re-pinned with its derivation; `164.8.6-05-FLOORS.log`; reviewer verdicts with every finding fixed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-VALIDATION.md
# read at execution when present (produced by an earlier plan of this phase): .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-03-SUMMARY.md
# read at execution when present (produced by an earlier plan of this phase): .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-04-SUMMARY.md
@scripts/mutation-runner/run.mjs
@src/__tests__/mutation-runner-floors.test.ts
</context>

<interfaces>
Read by symbol, never from prose: scripts/mutation-runner/run.mjs:877 (`FILES_FLOOR` = 46), scripts/mutation-runner/run.mjs:1768 (`ARMS_FLOOR` = 384), scripts/mutation-runner/run.mjs:1882 (`WAIVED_CEILING` = 0). Expected move: +8 (V2, G1 in the tick gate; S1, M1, M2 in each of two ledger gates) — a PREDICTION to be checked against the measurement, never pinned from it.
Produced: `ARMS_FLOOR = N` (measured), the CURRENCY block, `164.8.6-05-FLOORS.log`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Separate `ARMS_FLOOR` in both directions on real full-corpus lane runs, then pin it with a dated derivation</name>
  <files>scripts/mutation-runner/run.mjs, .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-FLOORS.log</files>
  <read_first>
    - scripts/mutation-runner/run.mjs:860-880 (`FILES_FLOOR` and the ⛔ block above it — the stale direction is not the runner's to report), :1736-1768 (ARMS_FLOOR's CURRENCY blocks — the latest, 380 -> 384, is the exact shape to append), :1882 (WAIVED_CEILING)
    - src/__tests__/mutation-runner-floors.test.ts:550-570 (`ARMS_FLOOR` must equal `twins - waivers`)
    - .planning/phases/164.7-appsettings-every-app-guc-reader-moves-to-a-mechanism-this-p/164.7-05-SUMMARY.md and 164.7-05-FLOORS.log if present on this checkout (the precedent record; if the directory is absent on this branch, the CURRENCY block at scripts/mutation-runner/run.mjs:1752-1768 quotes it — `RE-SEPARATED on real full-corpus lane runs`)
    - .github/workflows/ci.yml:1386 (`timeout-minutes: 20` — a declared CEILING; the sql-mutation job begins at :1196. ⚠️ CLAUDE.md cites :1259 / :1069 — stale by measurement 2026-09-11; cite the measured lines in the CURRENCY block)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md §Q6 (the six-step procedure)
  </read_first>
  <action>
    On a CLEAN tree at the final SQL commit (plans 01-04 merged into the working branch; `git status --porcelain` empty except this task's own edits), with `export PGBIN=/opt/homebrew/opt/postgresql@16/bin` exported, and each run launched with a 600 s tool timeout or in the background with output tee'd (a full run took ≈514 s for 384 arms; expect ≈530 s):
    RUN 1 (measurement, floor still 384): `node scripts/mutation-runner/run.mjs 2>&1 | tee -a <phase dir>/164.8.6-05-FLOORS.log`. Read `arms: N/N/0`, `biting: N`, `lane-invocations: N` (the two tallies MUST agree), `lane-blocked: 0 file(s)`, `lane-probe: pg_cron AVAILABLE`, `WAIVED 0`, `✅ No defects`, exit 0. N is the measurement; predicted 392, but the log decides. If N < 392, an arm did not bite — go back to plans 01/03, never lower the expectation.
    RUN 2 (upper separation): set `export const ARMS_FLOOR = N+1;`, re-run, tee; EXPECT exit 1 and the line `ARMS_FLOOR regression: N biting arm(s) < floor N+1`.
    RUN 3 (pin): set `ARMS_FLOOR = N`, re-run, tee; EXPECT exit 0, `✅ No defects`.
    LOWER direction: `npx vitest run src/__tests__/mutation-runner-floors.test.ts` green (at 384 it would fail with `The corpus declares N twin(s) of which 0 are waivers … ARMS_FLOOR is 384`; record that failing line by running the test ONCE at 384 before RUN 2 — it is the proof the lower layer bites).
    Write the CURRENCY block immediately above `export const ARMS_FLOOR`, in the :1752-1768 shape, dated, naming: the phase, the eight new twins by id and file, the three runs with exit codes and wall clocks, the `regression` line verbatim, the floors-test line at 384 verbatim, the two agreeing tallies, and `RECORD` pointing at this plan's SUMMARY beside `164.8.6-05-FLOORS.log`. Also record in the block the ubuntu ceiling reasoning: per-arm lane time (mean from the run) x N stays under the 20-minute `timeout-minutes` CEILING at ci.yml:1386 (`timeout-minutes: 20`) — if the merge-time sql-mutation job crosses it, the answer is `[REDUNDER-SUBSET-SPLIT]`, never a raise. `FILES_FLOOR` stays 46 (no new gate file) and `WAIVED_CEILING` stays 0 — do not touch either line.
    The log file goes in the phase directory (tracked under `.planning/`, like `164.7-05-FLOORS.log`); redact nothing — the runner prints no secrets.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && L=.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-FLOORS.log && test -s "$L" && N="$(grep -aoE '^biting: [0-9]+' "$L" | tail -1 | grep -oE '[0-9]+')" && test -n "$N" && test "$N" -ge 392 && grep -aqF "ARMS_FLOOR regression: $N biting arm(s) < floor $((N+1))" "$L" && test "$(grep -ac '✅ No defects' "$L")" -ge 2 && grep -aq 'lane-invocations: '"$N" "$L" && grep -aq 'lane-blocked: 0 file(s)' "$L" && grep -aq 'lane-probe: pg_cron AVAILABLE' "$L" && grep -qE "^export const ARMS_FLOOR = $N;" scripts/mutation-runner/run.mjs && grep -qE '^export const FILES_FLOOR = 46;' scripts/mutation-runner/run.mjs && grep -qE '^export const WAIVED_CEILING = 0;' scripts/mutation-runner/run.mjs && grep -qF '164.8.6-05-FLOORS.log' scripts/mutation-runner/run.mjs && npx vitest run src/__tests__/mutation-runner-floors.test.ts && echo FLOORS-PINNED</automated>
    <fails_when>The log is missing or empty; no `biting: N` line or N below 392 (an arm did not bite); the `ARMS_FLOOR regression: N biting arm(s) < floor N+1` line is absent (the upper separation was not taken); fewer than two `✅ No defects` runs; `lane-invocations` disagrees with `biting`; `lane-blocked` or `lane-probe` lines wrong; the constant in run.mjs is not exactly N, FILES_FLOOR is not 46, WAIVED_CEILING is not 0, the CURRENCY block does not cite the log; the floors test fails; or `FLOORS-PINNED` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `164.8.6-05-FLOORS.log` contains three full runs: run 1 exit 0 at floor 384, run 2 exit 1 with `ARMS_FLOOR regression: N biting arm(s) < floor N+1`, run 3 exit 0 at floor N; `biting`, `lane-invocations` and `arms: N/N/0` agree; `WAIVED 0`.
    - `export const ARMS_FLOOR = N;` with N >= 392 (measured); `FILES_FLOOR = 46`; `WAIVED_CEILING = 0` unchanged.
    - The CURRENCY block above the constant is dated, lists the eight new twin ids, both separation lines verbatim, wall clocks, the ceiling reasoning, and `RECORD` -> this SUMMARY + the log.
    - `npx vitest run src/__tests__/mutation-runner-floors.test.ts` passes; the SUMMARY quotes the failing line observed at 384 before the pin.
  </acceptance_criteria>
  <done>The floor is a measured separation point in both directions, recorded in the runner's own derivation style with the full log beside the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Every quick gate green on the SQL tree (the gates CI runs, run locally, SHA-bound)</name>
  <files>scripts/mutation-runner/run.mjs</files>
  <read_first>
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-VALIDATION.md § Test Infrastructure (quick set; `npm run typecheck` is SEPARATE from `npm run lint`; a file-scoped vitest run cannot clear a directory-scanning contract test)
    - CLAUDE.md § SQL gate integrity jobs (sql-mutation, sql-gate-lint incl. lint-app-guc, plan-anchor-verify) and § Test Coverage
    - src/__tests__/critical-regressions.test.ts (VERSION/package.json byte-equality — relevant to plan 06, run here as a baseline)
  </read_first>
  <action>
    Run, in this order, each pasted verbatim into the SUMMARY with its exit code and the HEAD sha (`git rev-parse HEAD`) it was measured at: `node scripts/lint-sql-gates.mjs --self-test` (expect `lint-sql-gates self-test OK: 7 rules, red+green each.`) then `node scripts/lint-sql-gates.mjs`; `node scripts/lint-app-guc.mjs --self-test` then `node scripts/lint-app-guc.mjs` (0 findings); `node scripts/mutation-runner/run.mjs --parse-only`; `npm run schema:functions:check`; `node scripts/verify-plan-anchors.mjs --pending`; `npm run lint`; `npm run typecheck`; `npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/prod-prober-wiring.test.ts src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/refdata-allowlist.test.ts src/__tests__/critical-regressions.test.ts`; then the full `npm run test:coverage` (background, tee'd; thresholds read from `vitest.config.ts`, never restated). Any red is fixed at the SOURCE (a gate, a migration, a fixture), never by widening an allowlist, relaxing `DETECT_RE`, adding a waiver, or editing `supabase-migrate.yml`. If a fix touches a migration or a gate, re-run task 1's RUN 3 (the floor pin must be measured at the FINAL bytes) and re-run this task.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && node scripts/lint-sql-gates.mjs --self-test | grep -qF '7 rules, red+green each' && node scripts/lint-sql-gates.mjs && node scripts/lint-app-guc.mjs --self-test && node scripts/lint-app-guc.mjs && node scripts/mutation-runner/run.mjs --parse-only && npm run schema:functions:check && node scripts/verify-plan-anchors.mjs --pending && npm run lint && npm run typecheck && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/prod-prober-wiring.test.ts src/__tests__/test-restore-workflow-wiring.test.ts src/__tests__/refdata-allowlist.test.ts src/__tests__/critical-regressions.test.ts && echo QUICK-GATES-GREEN</automated>
    <fails_when>Any command in the chain exits non-zero — a lint rule violation, an app-GUC finding, a parse/occurrence defect, a `stale:` snapshot, a broken plan anchor, an eslint or manifest-check error, a `tsc` error, or a failing vitest file — or `QUICK-GATES-GREEN` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Every command listed in the action exits 0; the SUMMARY records each with its exit code and the HEAD sha.
    - `npm run test:coverage` exits 0 (thresholds from `vitest.config.ts`) and its summary lines are pasted.
    - `git diff main...HEAD --name-only` does not list `.github/workflows/supabase-migrate.yml` or `scripts/lint-app-guc.mjs`, and `git diff main...HEAD -- scripts/mutation-runner/run.mjs` changes no line containing the waiver-ceiling constant (only the arms-floor constant and its CURRENCY block).
  </acceptance_criteria>
  <done>The SQL tree is green on every gate CI will run, measured locally at a named sha.</done>
</task>

<task type="auto">
  <name>Task 3: Run the three reviewers over the SQL diff BEFORE any PR exists; fix every finding (criterion 6)</name>
  <files>scripts/mutation-runner/run.mjs</files>
  <read_first>
    - .claude/agents/migration-reviewer.md and .claude/agents/rls-policy-auditor.md (project reviewer agents — their brief and output format)
    - the `silent-failure-hunter` agent shipped by the `pr-review-toolkit` plugin (dispatch name `pr-review-toolkit:silent-failure-hunter`)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-CONTEXT.md § Specific Ideas (criterion 6 is an ORDERING constraint)
    - .planning/STATE.md `last_activity_desc` (the 164.8.5 doctrine: a guard that refuses must be ADDITIVE, never substitutive — the class these reviewers look for)
  </read_first>
  <action>
    Dispatch the three reviewers as subagents with `model="opus"` (founder rule 2026-09-08: reviewers on Opus; `.planning/config.json` is not the authority), each over the SQL-wave diff `git diff main...HEAD -- supabase/ scripts/pg-lane/ scripts/mutation-runner/` plus the two new migrations in full: (1) `migration-reviewer`, (2) `rls-policy-auditor`, (3) `pr-review-toolkit:silent-failure-hunter`. Brief each with the phase's hard constraints (catalogue-only reads in DO blocks; needles disjoint from gate finds; no handler probes; TEST mirrors PROD's catalogue never its data). Collect findings; FIX every one at the source (policy: fix everything; block only on user-facing or data-integrity findings); where a finding is rejected, write the measurement that refutes it. After fixes, re-run task 2's chain, and if a migration or gate changed, re-run task 1's RUN 3 so the pin is measured at the final bytes. Record in the SUMMARY under `## Reviewers`: agent name, model, findings by severity, each finding's disposition (fixed at <file:line> / refuted by <measurement>), and the sha the review ran against. No `gh pr create` happens in this plan.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && S=.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-SUMMARY.md && test -f "$S" && grep -qF '## Reviewers' "$S" && grep -qF 'migration-reviewer' "$S" && grep -qF 'rls-policy-auditor' "$S" && grep -qF 'silent-failure-hunter' "$S" && test "$(grep -ciE 'unresolved (critical|high)' "$S")" = 0 && test -z "$(gh pr list --head "$(git branch --show-current)" --json number --jq '.[].number' 2>/dev/null)" && echo REVIEWERS-BEFORE-PR</automated>
    <fails_when>The SUMMARY lacks a `## Reviewers` section naming all three agents, contains an `unresolved critical/high` line, a PR already exists for this branch (`gh pr list` non-empty — criterion 6 ordering violated), or `REVIEWERS-BEFORE-PR` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY `## Reviewers` lists three agents with model `opus`, the sha reviewed, findings by severity, and a disposition per finding.
    - Zero unresolved Critical or High findings; every fix re-verified by task 2's chain (and task 1's RUN 3 if bytes under the runner changed).
    - `gh pr list --head <branch>` is empty at the end of this plan.
  </acceptance_criteria>
  <done>Three reviewers have run before the PR exists and every finding is fixed or refuted by measurement — criterion 6 satisfied in the order it demands.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 05's share)

- MODIFIED `scripts/mutation-runner/run.mjs` — `export const ARMS_FLOOR = N;` (measured, expected 392) + dated CURRENCY block citing `164.8.6-05-FLOORS.log`.
- NEW `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-FLOORS.log` — three full-corpus runs.
- SUMMARY sections `## Quick gates` (commands, exit codes, sha) and `## Reviewers` (three verdicts).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| measured run -> pinned constant | a floor pinned from prose instead of a run is a control that cannot fail |
| reviewer findings -> source | findings must change the source, not the PR body |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-20 | Tampering (control integrity) | `ARMS_FLOOR` set below the corpus (stale-low) | high | mitigate | floors test observed RED at 384 then GREEN at N; both directions logged |
| T-164.8.6-21 | Tampering | a waiver or allowlist widening used to clear a red gate | high | mitigate | acceptance grep of the branch diff for WAIVED_CEILING / DETECT_RE / supabase-migrate.yml edits |
| T-164.8.6-22 | Repudiation | reviewers "ran" without a recorded sha or disposition | medium | mitigate | SUMMARY schema pinned by the verify grep; PR must not exist yet |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- `FLOORS-PINNED`, `QUICK-GATES-GREEN`, `REVIEWERS-BEFORE-PR` printed by the three verifies.
</verification>

<success_criteria>
- Floor separated both ways and pinned from measurement; all CI gates green locally at a named sha; three reviewers before the PR with zero unresolved Critical/High.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-SUMMARY.md` when done.
</output>
