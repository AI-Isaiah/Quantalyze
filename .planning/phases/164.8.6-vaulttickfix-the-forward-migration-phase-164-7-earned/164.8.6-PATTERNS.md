# Phase 164.8.6: VAULTTICKFIX - Pattern Map

**Mapped:** 2026-09-11
**Files analyzed:** 11 (8 named in RESEARCH § Wave 0 Gaps + 3 the gaps imply: the two fan-out snapshots, `ARMS_FLOOR`, and a lane stand-in for `cron_runs`)
**Analogs found:** 11 / 11 (every analog verified `git ls-files`-tracked at HEAD; no `.gsd/capabilities` mirror paths anywhere in this file)

Every excerpt below was read from the file at HEAD this session, not from RESEARCH. Where this map CONTRADICTS RESEARCH it says so with the measurement (two places: § V2's twin, § `cron_runs` on the fan-out lanes).

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|-------------------|------|-----------|----------------|---------------|
| `supabase/migrations/<ts>_vault_tick_hardening.sql` (NEW) | migration (CREATE OR REPLACE + self-verifying `DO`) | catalogue-assert (DDL; reads `pg_proc`/`pg_get_functiondef` only) | `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql` (verify idiom, ack header, re-base discipline) + `20260907120000_analytics_service_settings_and_vault_tick.sql` (the body it re-bases) + `20260515205431_sec_def_public_execute_guard.sql` (`aclexplode`) | exact |
| `supabase/migrations/<ts>_ledger_fanout_grantees_and_dormancy.sql` (NEW) | migration (two CREATE OR REPLACE + `DO`) | catalogue-assert; bodies WRITE `cron_runs` at RUN time | `20260907130000` (it IS a re-base of the same two bodies, one migration earlier) + `20260408215026_schedule_match_cron_hourly.sql:37-38` (`cron_runs` INSERT shape) + `20260826140000_compute_jobs_error_kind_orphaned.sql:441` (`GET DIAGNOSTICS … ROW_COUNT`) | exact |
| `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` (MODIFY) | test gate (psql, `RED-UNDER-M` twins) | transactional probe + ROLLBACK | itself: arm V1 (`:229-267`) for V2; arms R1/T1 `sql`-step twins (`:475`, `:596`) for G1 | exact |
| `supabase/tests/test_ledger_refresh_fanout.sql` (MODIFY) | test gate | transactional probe + ROLLBACK | itself: arm A (`:476-509`) and arm L (`:578-600`) for the dormancy-instrument arms; arm I (`:948-955`) for the whole-set grant arm; arm J (`:1012+`) for ORDERING | exact |
| `supabase/tests/test_ledger_refresh_composite_arm.sql` (MODIFY) | test gate | transactional probe + ROLLBACK | itself, same arms at `:518`, `:604`, `:967`, `:1038` | exact |
| `supabase/schema/functions/{match_engine_cron_tick,enqueue_ledger_refresh_for_strategies,enqueue_ledger_composite_refresh}.sql` (REGENERATED) | generated snapshot | build artifact | `npm run schema:functions` (`package.json:25-26`); header at `match_engine_cron_tick.sql:1-5` | n/a (never hand-edited) |
| `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` (NEW, recommended — see § cron_runs) | lane fixture (stand-in) | DDL only | `scripts/pg-lane/fixtures/31-fixture-system-flags.sql` | exact |
| `scripts/mutation-runner/run.mjs` `ARMS_FLOOR` (MODIFY constant + dated block) | config constant | n/a | itself, `:1751-1768` CURRENCY block | exact |
| `scripts/prod-prober/arms/cron-drift.mjs` (MODIFY) | utility script (pure JS transform) | transform (rows in, defects out) | itself: section (0) loop `:2194-2227` is the shape the hoisted section (2) loop `:2373-2408` must mirror | exact |
| `scripts/prod-prober/fixtures/cron-drift/hygiene-red.json` (MODIFY) | test fixture | data | its own rows `fixture_nonanchored_split_job`, `fixture_var_key_job`, `fixture_raise_prose_job` (in `hygiene-green.json`) | exact |
| `src/__tests__/prod-prober-wiring.test.ts` (MODIFY) | vitest | request-response over `compareManifest` | describe `[164.8.5-02] compareManifest totality (CR-04)` at `:1798-1956` (`invalidsFor`, `driftDetailWithProdCommand`); self-test scenario in `scripts/prod-prober/run.mjs:1560-1600` | exact |

## Pattern Assignments

### `supabase/migrations/<ts>_vault_tick_hardening.sql` (migration, catalogue-assert)

**Analog:** `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql` for STRUCTURE (header → ack → re-base note → CREATE OR REPLACE → COMMENT → REVOKE → `DO $verify$`), `20260907120000` for the BODY it re-bases (`:301-369` = the snapshot `supabase/schema/functions/match_engine_cron_tick.sql:9-76` byte for byte).

**Header + VAC-04 ack pattern** (`20260907130000:6-46`; the pragma the gate greps as a FIXED STRING, `scripts/prod-body-drift-check.sh:1300`):
```sql
-- prod-body-ack: 88e6af8472e4e48175a62b1ee189f7411407b14827d948c81aa015f511ae6e36
--
-- The gate compares the COMMITTED SNAPSHOT (supabase/schema/functions/) against
-- PROD's live body. …
--   MEASURED 2026-09-07, workflow run 34138679709 at 51f576ef:
--     enqueue_ledger_refresh_for_strategies/0
--       PROD live sha256            88e6af84...ae6e36   (15 differing lines)
--       committed snapshot at HEAD  adeb6d16...53705f
--   and, reproduced LOCALLY with the gate's own normalizer, aiming its `live`
--   argument at origin/main's snapshot rather than at PROD:
--     node scripts/sql-body-normalize.mjs --diff-bodies \
--       supabase/schema/functions/<fn>.sql <origin/main's copy of the same file>
--   returned, for BOTH functions, the SAME two hashes and the SAME 15 differing
--   lines the gate reported against PROD.
```
One `prod-body-ack:` line for `match_engine_cron_tick` (expected PROD hash = today's snapshot hash `3a3fdd65…`, RESEARCH §Q4). It is EARNED by reproducing the hash from origin/main's snapshot; `20260907130000:142-150` records why a placeholder hash is worse than absence.

**Re-base discipline** (`20260907130000:154-160`): "The LEFT side of both re-bases is the COMMITTED SNAPSHOT under supabase/schema/functions/ … not the 20260825 migration text." Copy `supabase/schema/functions/match_engine_cron_tick.sql:9-76` (the snapshot) and change ONLY the Vault read and the key guard. Lines that MUST survive verbatim because gate arms `find` them (see § Disjointness):
```sql
  IF v_url IS NULL OR v_url = '' THEN                                      -- U1 find
   WHERE s.key = 'analytics_service_url';                                  -- C1 find
  IF v_url !~ c_url_allowed THEN                                           -- C2 find
    … read it with an admin session. Allowed: an https host under .up.railway.app';   -- C3 find
```

**The Vault read to replace** (`20260907120000:321-326` = snapshot `:29-34`):
```sql
  -- The Vault read, byte-for-byte the idiom the live job row already runs.
  SELECT decrypted_secret INTO v_key
    FROM vault.decrypted_secrets
   WHERE name = 'analytics_service_key';
  IF v_key IS NULL OR v_key = '' THEN
    RAISE EXCEPTION 'analytics_service_key missing from vault — refusing to send a null header';
  END IF;
```
Replacement shape is RESEARCH §Q3's count form (one read, `SELECT count(*), max(decrypted_secret) INTO v_cnt, v_key`, then `IF v_cnt > 1 THEN RAISE …` then `IF v_key IS NULL OR btrim(v_key) = '' THEN RAISE 'analytics_service_key missing from vault — …'`). Keep the by-name message BYTE-IDENTICAL: arm V1 asserts `v_msg !~ 'analytics_service_key missing from vault'` (`test_analytics…:259`). Keep `FROM vault.decrypted_secrets` on its own line in that exact spelling: it is the needle AND `src/__tests__/prod-prober-wiring.test.ts:1754` asserts the snapshot matches `/FROM\s+vault\.decrypted_secrets/i`. Declare `v_cnt INTEGER` in the DECLARE — `20260907130000:741-746` records that a missing DECLARE raises 42601 and stops the WHOLE block.

**Why not `STRICT` + handler** (`20260907130000:243-245`, the source-side rule that lint R1 mirrors in the corpus):
```sql
  -- ⚠️ NOTHING but the assignment goes in the handler. A probe or a second read
  -- inside an EXCEPTION block is the shape lint rule R1 flags in the gate corpus;
  -- keeping it clean at the source is what stops a gate copying the bad shape.
```

**REVOKE pattern, whole set** (`20260515205431:116`, the only migration that already revokes `service_role`):
```sql
REVOKE ALL ON FUNCTION public._assert_no_public_execute(TEXT) FROM PUBLIC, anon, authenticated, service_role;
```
replacing `20260907120000:388`'s `REVOKE ALL ON FUNCTION public.match_engine_cron_tick() FROM PUBLIC, anon, authenticated;`. No GRANT follows: the scheduler runs as `postgres` = owner (`cron-manifest.json:53`), and `20260907120000:389` already says "The scheduler runs as superuser; no additional GRANT is required."

**Verify block — the falsifiable-needle idiom** (`20260907130000:766-775` DECLARE half):
```sql
  -- The two positive needles, held in variables so the loop states each one once.
  v_flag_needle TEXT := 'FROM public.system_flags';
  -- ⚠️ BOUND TO THE GUARDED VARIABLE, not floating. `IS DISTINCT FROM TRUE`
  -- alone asserts only that the token appears SOMEWHERE in the stripped body —
  -- it is satisfied by a null-safe comparison of any other variable, in any
  -- other statement, while the activation guard itself has been rewritten to
  -- `NOT v_enabled`. Naming the variable is what makes check 5 an assertion
  -- about the flag. Measured before tightening: `v_enabled IS DISTINCT FROM
  -- TRUE` occurs exactly ONCE per body (and nowhere else in this file), so the
  -- widened needle costs nothing today and refuses the drift tomorrow.
  v_null_needle TEXT := 'v_enabled IS DISTINCT FROM TRUE';
```
and the assertion half (`20260907130000:896-903`):
```sql
    v_def := regexp_replace(v_def_raw, '--[^\n]*', '', 'g');

    IF v_def IS NULL OR length(v_def) < 500 THEN
      RAISE EXCEPTION 'Migration 20260907130000: the comment-stripped definition of public.% is % character(s) — the strip is broken, so checks (4)-(6) below would pass over nothing', v_fn, COALESCE(length(v_def), 0);
    END IF;

    -- 4. the guard READS THE TABLE.
    IF position(v_flag_needle IN v_def) = 0 THEN
      RAISE EXCEPTION 'Migration 20260907130000: public.% does not read the activation flag from the settings table (looked for "%") — the fail-closed switch is gone and this function would fan out unconditionally', v_fn, v_flag_needle;
    END IF;
```
The tick file's needles (RESEARCH §Q2, all measured to occur ONCE in code and ZERO times in any literal of the body): `v_vault_needle := 'FROM vault.decrypted_secrets'`, `v_settings_needle := 'FROM public.system_settings'`, `v_cardinality_needle := 'IF v_cnt > 1 THEN'`. Each needle comment must carry the derivation the way `:768-775` does — for the settings needle, WHY NOT `'analytics_service_url'` (it is in the function's own RAISE text at `20260907120000:332` and `:348`) and WHY NOT `WHERE s.key = 'analytics_service_url'` (arm C1 mutates it).

**The VACUOUS counter-example this replaces** (`20260907120000:546-554`) — quote it in the new file's header as the defect, never copy its shape:
```sql
  SELECT regexp_replace(pg_get_functiondef('public.match_engine_cron_tick()'::regprocedure),
                        '--[^\n]*', '', 'g')
    INTO v_fn;
  IF v_fn !~ 'vault\.decrypted_secrets' THEN
    RAISE EXCEPTION 'Migration 20260907120000: match_engine_cron_tick does not read vault.decrypted_secrets. …';
  END IF;
  IF v_fn !~ 'analytics_service_url' THEN
    RAISE EXCEPTION 'Migration 20260907120000: match_engine_cron_tick does not read analytics_service_url. …';
  END IF;
```
Its own comment at `:539-545` claims "A text check is honest here only because this file OWNS the body it just wrote" — and the body it wrote carries the needle in prose at `:332`/`:348`. That is `R2-functiondef-comment-strip` one layer over (literals, not comments; `scripts/lint-sql-gates.mjs:135-147`).

**Whole-grantee-set check** — `aclexplode` idiom (`20260515205431:86-90`):
```sql
  SELECT COUNT(*) INTO v_leaks
    FROM pg_proc p,
         LATERAL aclexplode(COALESCE(p.proacl, acldefault('f', p.proowner))) a
   WHERE p.oid = v_oid
     AND a.grantee = 0
     AND a.privilege_type = 'EXECUTE';
```
widened to the SET form in RESEARCH § Code Examples (`string_agg(CASE WHEN a.grantee = 0 THEN 'PUBLIC' ELSE pg_get_userbyid(a.grantee) END …)` compared to `pg_get_userbyid(p.proowner)`). Never compare to the literal `postgres`: the lane owner is whatever `run.sh` boots as. Falsifiable on the lane only because `scripts/pg-lane/fixtures/07-fixture-supabase-default-privileges.sql:21-22` grants `ALL ON FUNCTIONS TO anon, authenticated, service_role` first — say so in the check's comment the way `20260907120000:487-490` does.

**Checks to carry forward from `20260907120000` STEP 3 unchanged in kind (catalogue only):** `prosecdef`, `proconfig` (search_path), `pronargs = 0`, owner `rolsuper OR rolbypassrls` (`:472-485`). **Checks that MUST NOT be re-run** (criterion 7): `:534-536` seed-row read. Do not name `vault.*` or `net.*` as OBJECTS in the `DO` (`:400-403`: neither exists on the lane).

**Apply-time prohibition** (`20260907120000:395-398`, verbatim in the new header):
```sql
-- ⛔ THIS BLOCK MUST NEVER CALL match_engine_cron_tick(). An apply-time
-- invocation would fire a real HTTP POST at the analytics service the moment
-- this migration merges. Assert the SHAPE, never the behaviour; behaviour is
-- covered by supabase/tests/test_analytics_service_settings_and_vault_tick.sql.
```

---

### `supabase/migrations/<ts>_ledger_fanout_grantees_and_dormancy.sql` (migration, catalogue-assert; bodies write `cron_runs` at run time)

**Analog:** `20260907130000` — structurally identical (two re-based bodies, two acks, one FOREACH verify loop). Two acks (`:10-11` shape); expected PROD hashes `cc77a2b9…` and `81760333…` (RESEARCH §Q4). LEFT side of the re-base = `supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql` (267 lines) and `…composite_refresh.sql` (251 lines).

**The guard being instrumented** (`20260907130000:250-261`; composite twin at `:509-520` differs only in the function name inside the WARNING/NOTICE text):
```sql
  BEGIN
    SELECT sf.enabled INTO v_enabled
      FROM public.system_flags sf
     WHERE sf.key = 'ledger_refresh_enabled';
  EXCEPTION WHEN OTHERS THEN
    RAISE WARNING 'enqueue_ledger_refresh_for_strategies: activation flag read failed (SQLSTATE %); treating as dormant', SQLSTATE;
    v_enabled := NULL;
  END;
  IF v_enabled IS DISTINCT FROM TRUE THEN
    RAISE NOTICE 'enqueue_ledger_refresh_for_strategies: dormant (system_flags.ledger_refresh_enabled is not TRUE); enqueued 0';
    RETURN 0;
  END IF;
```
⛔ Three strings in that block are gate-arm `find`s and must survive byte-for-byte in the new bodies (re-pointed arms A, K, L in BOTH gates — `test_ledger_refresh_fanout.sql:496,526,588`; `…composite_arm.sql:518,547,604`):
- A/K: `"IF v_enabled IS DISTINCT FROM TRUE THEN\n    RAISE NOTICE '<fn>: dormant"` — the `IF` line and the NOTICE line must stay ADJACENT with exactly 4-space indent, or `occurrences` reads 0 (`occurrence-mismatch`, MEASURE_FAIL). Any cause-branching the instrument adds goes AFTER the NOTICE inside the `IF`, or between the handler's `END;` and the `IF` — never between the `IF` and the `RAISE NOTICE`.
- L: `"'<fn>: activation flag read failed (SQLSTATE %); treating as dormant', SQLSTATE;\n    v_enabled := NULL;"` — the handler stays exactly one WARNING + one assignment (also lint R1 / `:243-245`). Set a cause flag AFTER the block, not inside the handler: e.g. `GET DIAGNOSTICS v_found = ROW_COUNT;` immediately after the `END;` is unreachable when the read raised, so "handler ran" is `v_enabled IS NULL AND v_found IS NULL` if `v_found` is initialised NULL and assigned only on the success path — or simpler, keep a `v_read_failed BOOLEAN := FALSE` that the handler sets AFTER `v_enabled := NULL;`. ⚠️ Adding a line INSIDE the handler after `v_enabled := NULL;` keeps L's needle intact (the needle ends at `v_enabled := NULL;`), so a second assignment there is grammatically safe; `:243-245` forbids a PROBE or READ in the handler, not an assignment.

**`GET DIAGNOSTICS` precedent** (`20260826140000_compute_jobs_error_kind_orphaned.sql:441`, one of 35 migrations using it):
```sql
  GET DIAGNOSTICS v_rows = ROW_COUNT;
```
Row-count 0 after the `SELECT … INTO` = cause `flag_row_invisible_or_absent`; `v_enabled = FALSE` = `flag_false_by_design`; handler ran = `flag_read_failed`.

**`cron_runs` INSERT shape** (`20260408215026_schedule_match_cron_hourly.sql:37-38`, the house instrument already writing this table from a `DO` that runs as `postgres`):
```sql
    INSERT INTO cron_runs (cron_name, status, completed_at, error)
    VALUES ('match_engine_cron', 'error', now(), 'pg_cron extension missing at migration 015');
```
Table contract (`20260408113029_cron_heartbeat.sql:31-39`): `status TEXT NOT NULL DEFAULT 'running' CHECK (status IN ('running', 'ok', 'error'))`, `error TEXT`, `metadata JSONB`; RLS ON (`:52`) — the SECURITY DEFINER owner is RLS-exempt (asserted by arm J and by `20260825130000`'s own block), which is why the body can write it. Schema-qualify as `public.cron_runs` in the new bodies (the fan-outs `SET search_path`); the needle the verify block asserts is `INSERT INTO public.cron_runs` (catalogue, via `pg_get_functiondef`) — NEVER `count(*) FROM cron_runs` at apply time (criterion 7).

**REVOKEs to widen** (`20260907130000:470-471` and `:735-736`):
```sql
REVOKE ALL ON FUNCTION public.enqueue_ledger_refresh_for_strategies()
  FROM PUBLIC, anon, authenticated;
```
→ `FROM PUBLIC, anon, authenticated, service_role;` (`20260515205431:116` precedent).

**Verify loop skeleton** (`20260907130000:784-800`):
```sql
  FOREACH v_fn IN ARRAY ARRAY[
    'enqueue_ledger_refresh_for_strategies',
    'enqueue_ledger_composite_refresh'
  ]
  LOOP
    -- 1. the function landed, and it takes ZERO arguments (T-161.1-06/-15).
    SELECT p.prosecdef, p.proconfig, p.pronargs, pg_get_functiondef(p.oid)
      INTO v_secdef, v_config, v_nargs, v_def_raw
      FROM pg_proc p
      JOIN pg_namespace n ON n.oid = p.pronamespace
     WHERE n.nspname = 'public'
       AND p.proname = v_fn;

    IF v_nargs IS NULL THEN
      RAISE EXCEPTION 'Migration 20260907130000: public.% is missing after its own CREATE OR REPLACE — the re-base did not land', v_fn;
    END IF;
```
Needles for this file: `'FROM public.system_flags'`, `'v_enabled IS DISTINCT FROM TRUE'` (both already ONCE per body, `:762-775`), plus `'INSERT INTO public.cron_runs'`. Keep the concatenated absent-needle exactly as `:776-783`:
```sql
  -- ⛔ ASSEMBLED BY CONCATENATION, DELIBERATELY, and do NOT "tidy" it into one
  --    literal. …
  v_guc_needle  TEXT := 'current_' || 'setting(''app.' || 'ledger_refresh_enabled''';
```
`scripts/lint-app-guc.mjs` scans `supabase/migrations/**` RAW (`:3-8`); its `DETECT_RE` (`:162-163`) matches `current_setting(` followed by `'app.` — the snapshot's comment at `enqueue_ledger_refresh_for_strategies.sql:30` ("in the `app.` namespace") does NOT match it (measured: no `current_setting` token in any of the three snapshots), so copying the snapshot prose is safe. A NEW file cannot be allowlisted (the allowlist pins retired lineage only, `:279+`); a red there is fixed in the file.

**Do NOT re-run** `20260907130000:926-933` (the `v_seeded` flag-row read) — it is `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`'s named specimen.

---

### `supabase/tests/test_analytics_service_settings_and_vault_tick.sql` (test gate)

**Apply list to extend** (`:190`) — append the new tick migration LAST:
```
-- RED-UNDER-SETUP: {"apply":["scripts/pg-lane/fixtures/01-fixture-core.sql","scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql","scripts/pg-lane/fixtures/07-fixture-supabase-default-privileges.sql","scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql","scripts/pg-lane/fixtures/15-fixture-auth-role.sql","scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql","supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql"]}
```
Why the last entry matters, in the fan-out gate's own words (`test_ledger_refresh_fanout.sql:182-186`): "applied LAST, because its `CREATE OR REPLACE` must be the definition the arms run against. That is precisely why every edit-kind twin below names it: the 20260825 bodies are overwritten before arm 0 executes, so mutating them would be mutating dead text (TRAP E / C-02)."

**Re-point table (measured at HEAD, `edit` arms only; `sql` arms 0/U2/R1/R2/T1 unchanged):**

| Arm | Line | Current `find` (file `20260907120000`) | After |
|---|---|---|---|
| V1 | `:255` | `IF v_key IS NULL OR v_key = '' THEN` → `IF FALSE THEN` | file → new tick migration; `find` → `IF v_key IS NULL OR btrim(v_key) = '' THEN`; re-measure `occurrences` |
| U1 | `:283` | `IF v_url IS NULL OR v_url = '' THEN` | file → new; find unchanged |
| C1 | `:387` | `WHERE s.key = 'analytics_service_url'` → `…_never'` | file → new; find unchanged. Its prose at `:376-386` says the mutant "survives the migration's own STEP 3 body check because `analytics_service_url_never` still contains `analytics_service_url`" — still true under the new needle `FROM public.system_settings`; update the prose to name the new needle |
| C2 | `:431` | `IF v_url !~ c_url_allowed THEN` | file → new; find unchanged |
| C3 | `:444` | `read it with an admin session. Allowed: an https host under .up.railway.app';` | file → new; find unchanged |
| R3 | `:547` | `REVOKE ALL ON TABLE public.system_settings FROM anon;` | STAYS on `20260907120000` (table REVOKE, not the body) |

**Arm V1 — the setup-guard + call + assert shape to COPY for V2** (`:229-267`):
```sql
  v_setup := NULL;
  BEGIN
    DELETE FROM vault.decrypted_secrets WHERE name = 'analytics_service_key';
  EXCEPTION WHEN OTHERS THEN
    GET STACKED DIAGNOSTICS v_msg = MESSAGE_TEXT, v_state = RETURNED_SQLSTATE;
    v_setup := v_state || ' ' || v_msg;
  END;
  IF v_setup IS NOT NULL THEN
    RAISE EXCEPTION 'TEST FAILED (V1-SETUP): could not remove the analytics_service_key secret for the duration of this transaction (%), …', v_setup;
  END IF;

  v_raised := false;
  BEGIN
    PERFORM public.match_engine_cron_tick();
  EXCEPTION WHEN OTHERS THEN
    v_raised := true;
    GET STACKED DIAGNOSTICS v_msg = MESSAGE_TEXT;
  END;
  …
  IF NOT v_raised THEN
    RAISE EXCEPTION 'TEST FAILED (V1): match_engine_cron_tick() RETURNED with no analytics_service_key in the vault. …';
  END IF;
  IF v_msg !~ 'analytics_service_key missing from vault' THEN
    RAISE EXCEPTION 'TEST FAILED (V1): the callable did raise with no secret present, but its message does not name analytics_service_key — it reads: %. …', COALESCE(v_msg, 'NULL');
  END IF;
```
V2 = the same shape with `PERFORM vault.create_secret(' ', 'analytics_service_key', 'phase 164.8.6 gate fixture')` after the guarded DELETE — parameter order `(secret, name, description)` per `scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql:44-50` (`ON CONFLICT (name) DO UPDATE`, so the DELETE is optional on the lane but keeps TEST's real row out of the way). Placement: AFTER C1's fixture has set the url to `http://127.0.0.1:9` (`:355-357`), so a whitespace key that sails past the OLD body on shared TEST cannot leave the host (`:64-71`). Note V2 asserts `v_msg ~ 'analytics_service_key missing from vault'` — the SAME message as V1, because the `btrim` guard shares V1's RAISE.

⛔ **V2's twin — CORRECTION to RESEARCH §Q6.** RESEARCH proposes `{"find":"IF v_key IS NULL OR btrim(v_key) = '' THEN","replace":"IF FALSE THEN"}` for V2, "the same `find` V1 must be re-pointed to". That is V1's mutation. Under it V1 executes first (single-space is not the only thing `IF FALSE` lets through — NULL is too), so the runner reports `wrong-first-failure: V1` for V2 (`GRAMMAR.md` defect table: "Red-anywhere is not success"). V2 needs the MIRROR mutation that reddens V2 ALONE — revert the guard to its pre-phase spelling:
```json
{"arm":"V2","apply":[{"kind":"edit","file":"supabase/migrations/<ts>_vault_tick_hardening.sql","find":"IF v_key IS NULL OR btrim(v_key) = '' THEN","replace":"IF v_key IS NULL OR v_key = '' THEN","occurrences":1}]}
```
Under this, V1 stays GREEN (NULL is still caught) and V2 reddens (a single space passes `v_key = ''`). Under V1's twin (`IF FALSE THEN`) both redden and V1 is first — correct. This is exactly the fan-out gates' arm A / arm K argument (`test_ledger_refresh_fanout.sql:517-525`):
```sql
  -- ⚠️ Deliberately the MIRROR of arm A's twin, and that is the proof the two
  --    arms are not redundant: under this mutation arm A stays GREEN (NULL is
  --    still dormant) and only K reddens, while under arm A's twin K stays
  --    green. Neither mutation can redden both.
```
Neither V1's nor V2's `find` is a migration needle (§ Disjointness), so neither aborts the apply.

**Arm G1 (whole grantee set) — the `sql`-step twin shape** (`:475`, arm R1; and `:596`, arm T1, with the reason a `sql` step is chosen over an edit at `:586-595`):
```sql
  -- RED-UNDER: hand the privilege back on the live lane. A `sql` step and NOT
  --            an edit of the migration's REVOKE line, because that REVOKE
  --            names three privileges in one statement and weakening it by text
  --            invites a needle that removes REFERENCES or TRIGGER instead —
  -- RED-UNDER-M: {"arm":"T1","apply":[{"kind":"sql","stmt":"GRANT TRUNCATE ON public.system_settings TO authenticated"}]}
```
G1: `{"arm":"G1","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.match_engine_cron_tick() TO service_role"}]}`. An `edit` of the migration's REVOKE would abort the apply (the new verify block asserts the set) — the same reason `:210-214` gives for arm 0. The gate's assertion is the `aclexplode` SET query from § tick migration, compared to the owner, raising `TEST FAILED (G1)`. Place G1 after T1 (T1's twin TRUNCATEs the table; a catalogue arm after it is unaffected). ⚠️ Pre-merge on shared TEST, G1 is RED by construction (the OLD REVOKE left `service_role` its EXECUTE) — CLAUDE.md coupling (b); say so in the PR body.

**Identity rule** (`:107-115`): every arm id carries a DIGIT (`V2`, `G1`), and setup guards are `V2-SETUP` (a sub-arm, not separately twinned, `:117-121`). Update the DECLARE (`:194-204`) for any new variable in the SAME edit and the `ALL 11 ARMS EXECUTED (…)` roster at `:606` (→ 13).

---

### `supabase/tests/test_ledger_refresh_fanout.sql` and `supabase/tests/test_ledger_refresh_composite_arm.sql` (test gates)

**Apply lists** (`fanout:194`, 13 entries; `composite:206`, 12 entries) — both end in `20260907130000`; append the `cron_runs` provider (§ cron_runs below) and then the new ledger migration LAST.

**Re-point table (measured at HEAD by parsing every `RED-UNDER-M`):**

| Gate | `edit` arms on `20260907130000` → new file | Stay as-is |
|---|---|---|
| `test_ledger_refresh_fanout.sql` | A `:496`, K `:526`, L `:588`, B `:620`, E `:671` (3 steps, `nth:1`), C `:701` (`nth:1`), D `:733`, F `:769` (`nth:1`), G1 `:802`, G2 `:835`, H `:866` (`nth:1`), H/inactive `:885` (`nth:1`), H/revoked `:900` (`nth:1`), H/disconnected `:915` (`nth:1`) — **14** | 0 `:253`, I `:948`, J `:1012` (`sql` steps) |
| `test_ledger_refresh_composite_arm.sql` | A `:518`, K `:547`, L `:604`, B `:636`, F `:704` (3 steps, `nth:2`), C `:734` (`nth:2`), D `:802`, E `:853`, E1/mixed `:863`, E2/all `:873`, G `:899` (`nth:2`), H `:928` — **12** | D/precondition `:766` and E1/precondition `:836` (edit `20260825120000`, the VIEW — unchanged), 0 `:261`, I `:967`, J `:1038` |

⚠️ Every `occurrences`/`nth` pair is a MEASUREMENT of the OLD file (e.g. `INTERVAL '20 hours'` occurs 2× in `20260907130000` because it holds both bodies). If the new file also holds both bodies, the counts carry over — but re-measure each one against the new file (`GRAMMAR.md` rule 2, `:73-77`: `occurrence-mismatch` is a MEASURE_FAIL). The `find` strings themselves are the strongest reason to re-base from the snapshots byte-for-byte: `G2`'s find is `"       LIMIT 4"` (7 leading spaces).

**Whole-set grant arm — analog arm I** (`fanout:943-955`):
```sql
  -- RED-UNDER: `GRANT EXECUTE … TO anon` on the live lane. It is a `sql` step
  --            rather than an edit to the REVOKE in 20260825130000 because that
  --            migration's own DO block asserts the same privilege and would
  --            ABORT THE APPLY, so the gate would never run. The lane's
  --            --post-apply hook exists for exactly this shape.
  -- RED-UNDER-M: {"arm":"I","apply":[{"kind":"sql","stmt":"GRANT EXECUTE ON FUNCTION public.enqueue_ledger_refresh_for_strategies() TO anon"}]}
  IF has_function_privilege('anon', 'public.enqueue_ledger_refresh_for_strategies()', 'EXECUTE') THEN
    RAISE EXCEPTION 'TEST FAILED (I): role anon can EXECUTE enqueue_ledger_refresh_for_strategies. …';
  END IF;
```
New arm per gate: `GRANT EXECUTE ON FUNCTION public.<fn>() TO service_role` + the `aclexplode` set assertion. ⛔ **ORDER: place it AFTER arm J.** J's twin (`fanout:1038`, `composite:1038`) runs `GRANT EXECUTE ON ALL FUNCTIONS IN SCHEMA public TO lrf_owner_drift` and `ALTER FUNCTION … OWNER TO lrf_owner_drift` — a whole-set arm placed BEFORE J would be the first failure under J's twin (`wrong-first-failure`). After J, under J's twin J fails first (correct); under the new arm's twin J is green and the new arm reddens alone. Arm I's `has_function_privilege` probes stay (they are not vacuous when the function is gone, `:940-942`).

**Dormancy-instrument arms — analog arm A for the missing-row cause** (`fanout:476-509`):
```sql
  -- RED-UNDER-M: {"arm":"A","apply":[{"kind":"edit","file":"supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql","find":"IF v_enabled IS DISTINCT FROM TRUE THEN\n    RAISE NOTICE 'enqueue_ledger_refresh_for_strategies: dormant","replace":"IF v_enabled IS NOT NULL AND v_enabled IS DISTINCT FROM TRUE THEN\n    RAISE NOTICE 'enqueue_ledger_refresh_for_strategies: dormant","occurrences":1}]}
  UPDATE strategies SET status = 'published' WHERE id = s_a;

  DELETE FROM public.system_flags WHERE key = 'ledger_refresh_enabled';

  v_ret := public.enqueue_ledger_refresh_for_strategies();
  IF v_ret <> 0 THEN
    RAISE EXCEPTION 'TEST FAILED (A): the fan-out returned % with NO activation row at all, expected 0 — …', v_ret;
  END IF;
  SELECT count(*) INTO v_cnt FROM compute_jobs WHERE strategy_id = s_a;
```
The missing-row instrument arm reuses arm A's state (the row is already deleted and the function already called): immediately after A's assertions, `SELECT count(*) INTO v_cnt FROM public.cron_runs WHERE cron_name = '<instrument name>' AND error = 'flag_row_invisible_or_absent'` and `IF v_cnt <> 1 THEN RAISE EXCEPTION 'TEST FAILED (<id>): …'`. Reading a row the gate's own transaction wrote is fine (it is not an apply-time read). Twin: an `edit` on the new migration that changes the cause string written for that branch (e.g. `'flag_row_invisible_or_absent'` → `'flag_row_invisible_or_absent_drifted'`, `occurrences` = 2 if both bodies carry it, with `nth`) — that string is NOT a migration needle (the needle is `INSERT INTO public.cron_runs`), so the apply survives.

**Read-failed cause — analog arm L** (`fanout:589-604`):
```sql
  SET LOCAL lock_timeout = '2s';
  BEGIN
    ALTER TABLE public.system_flags RENAME TO system_flags_arm_l;
    BEGIN
      v_ret_l := public.enqueue_ledger_refresh_for_strategies();
    EXCEPTION WHEN OTHERS THEN
      RAISE EXCEPTION 'TEST FAILED (L): the fan-out RAISED (SQLSTATE %) …', SQLSTATE;
    END;
    RAISE EXCEPTION USING ERRCODE = 'P0164', MESSAGE = 'arm L: unwinding the RENAME (not a failure)';
  EXCEPTION WHEN SQLSTATE 'P0164' THEN
    NULL;
  END;
  IF v_ret_l <> 0 THEN
```
⚠️ The P0164 unwind ROLLS BACK everything inside that block — including the `cron_runs` row the function just wrote. So the read-failed instrument probe (`SELECT count(*) INTO v_cnt_rf FROM public.cron_runs WHERE error = 'flag_read_failed'`) must sit INSIDE the outer BEGIN, after the inner call block and BEFORE the `RAISE … 'P0164'` — in the block BODY, not in a handler (lint `R1-exception-handler-probe` flags DML/`SELECT INTO` inside an `EXCEPTION WHEN` handler, `scripts/lint-sql-gates.mjs:122-133`; the body is not a handler). The plpgsql local survives the rollback (`:571-573`). Its `TEST FAILED` raise is P0001 and is not swallowed by the P0164-only handler (`:575-577`).

**Arm ids:** both files already use `A`–`L`; fanout also `G1`, `G2`, `H/inactive`, `H/revoked`, `H/disconnected`; composite also `D/precondition`, `E1/precondition`, `E1/mixed`, `E2/all`. RESEARCH's `D1`/`D2` and `G-*` collide with existing `D`/`G1`/`G2` in reading (not in the parser — `sectionOfIdentity` keeps a trailing digit). Pick ids unused in BOTH files, each carrying a digit (`test_analytics…:107-115`), e.g. `M1`/`M2` for the two instrument arms and `S1` for the service_role whole-set arm. Update each file's `ALL 12 ARMS EXECUTED (A-L)` roster (`fanout:1027`) — the header says the sentinel is pinned to ONE occurrence (`:190-192`).

---

### `scripts/pg-lane/fixtures/33-fixture-cron-runs.sql` (NEW lane fixture) — the `cron_runs` provider for the fan-out lanes

⛔ **CORRECTION to RESEARCH § Dormancy ("append `20260408113029_cron_heartbeat.sql` … or a fixture").** Appending the migration does NOT apply on the fan-out lanes, measured at HEAD:
- `20260408113029:54-60` `CREATE POLICY "cron_runs_admin_read" … EXISTS (SELECT 1 FROM profiles p WHERE p.id = auth.uid() AND p.is_admin = true)` — `profiles.is_admin` exists on the lane only via `12-fixture-profiles-is-admin.sql:15` (`ADD COLUMN IF NOT EXISTS`), which is in the TICK gate's list and in NEITHER fan-out list (`fanout:194`, `composite:206`). `CREATE POLICY` resolves columns at creation → 42703, apply aborts.
- `:62-64` `CREATE POLICY "cron_runs_service_role" … auth.role() = 'service_role'` — `auth.role()` comes from `15-fixture-auth-role.sql` (`:1-4`), also absent from both fan-out lists.
- Its `DO` (`:120-183`) is lane-tolerant (pg_net absent → INSERTs an error row and RETURNs), but the file carries a count-pinned `APP-GUC-LINEAGE` header (`:1`, `occurrences: 5`) and 2 `cron.schedule` literals — nothing to gain.

**Analog: `scripts/pg-lane/fixtures/31-fixture-system-flags.sql`** — the house pattern for "a table whose creating migration cannot enter an apply list":
```sql
-- Additive stand-in: `public.system_flags`. 20260407164606_perfect_match.sql:44-49
-- creates it in production; that migration cannot enter an apply list (it reads
-- auth.users, carries FKs onto `strategies`, and creates four unrelated tables
-- alongside this one), so this fixture mirrors its four columns EXACTLY —
-- key/enabled/updated_at/updated_by, same types, same PRIMARY KEY, same FK
-- target.
…
-- RLS is ENABLED with NO POLICIES, deliberately. The only lane reader is a
-- SECURITY DEFINER body whose owner is RLS-exempt (arm J of the ledger gates
-- asserts that exemption, …)
…
-- ⛔ Never a second base: 01-fixture-core.sql remains the only destructive
-- fixture. This file creates one table and enables RLS on it; it drops nothing.
CREATE TABLE system_flags (
  key        TEXT PRIMARY KEY,
  enabled    BOOLEAN NOT NULL,
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_by UUID REFERENCES profiles(id)
);

ALTER TABLE system_flags ENABLE ROW LEVEL SECURITY;
```
Fixture 33 mirrors `20260408113029:31-39` exactly (`id UUID PRIMARY KEY DEFAULT gen_random_uuid()`, `cron_name TEXT NOT NULL`, `started_at`, `completed_at`, `status … CHECK (status IN ('running','ok','error'))`, `error TEXT`, `metadata JSONB`), RLS ON with no policies, and the header names the two lane-absent dependencies above as the reason. Lint `R5-fixture-shadows-migration-table` (`lint-sql-gates.mjs:176-188`) does not fire because `20260408113029` is not in the list; `GRAMMAR.md` rule 4: no twin may target a fixture. Insert it immediately before the new ledger migration in both lists (like 31 sits before its reader, `fanout:174-181`). On shared TEST the real table exists (baseline restore, RESEARCH §Q5).

**Alternative, if the planner prefers the real migration:** insert `12-fixture-profiles-is-admin.sql`, `15-fixture-auth-role.sql`, then `20260408113029_cron_heartbeat.sql` before the new migration in both lists, and re-run `node scripts/lint-sql-gates.mjs` (R5-R7) — measured cost: two more fixtures per lane and a migration whose only in-scope content is the table.

---

### `supabase/schema/functions/*.sql` (REGENERATED)

No analog to copy; the header is the contract (`match_engine_cron_tick.sql:1-5`):
```sql
-- @generated by scripts/dump-sql-functions.ts — DO NOT EDIT BY HAND.
-- Canonical current body of this function, replayed from supabase/migrations/**.
-- Regenerate with `npm run schema:functions`. See tech-debt #2.

-- source migration: 20260907120000_analytics_service_settings_and_vault_tick.sql
```
After the new migrations, `-- source migration:` (`scripts/dump-sql-functions.ts:303`) names the new files. `npm run schema:functions` then `npm run schema:functions:check` (`package.json:25-26`); RESEARCH Pitfall 9: a stale snapshot makes VAC-04 compare the OLD body and report a false clean.

---

### `scripts/mutation-runner/run.mjs` — `ARMS_FLOOR` (MODIFY)

**Analog:** the constant's own latest CURRENCY block (`:1752-1768`):
```js
//                ⚠️ CURRENCY 2026-09-07 (SQL-fixer pass): 380 -> 384. The fixer
//                grew test_analytics_service_settings_and_vault_tick.sql from 7
//                arms to 11 (C3, R3, T1 and the U2 constraint arm), so the
//                corpus bites 384. RE-SEPARATED on real full-corpus lane runs:
//                  ARMS_FLOOR=385 (one higher) -> defect ->
//                    `ARMS_FLOOR regression: 384 biting arm(s) < floor 385`
//                  ARMS_FLOOR=384 (this value) -> 0 defects, EXIT 0
//                So 384 is exactly the separation point, not a value below it.
//                The STALE direction is NOT this runner's to report — see the
//                ⛔ block above FILES_FLOOR. …
//   RECORD       .planning/phases/164.7-appsettings-every-app-guc-reader-moves-
//                to-a-mechanism-this-p/164.7-05-SUMMARY.md, beside
//                164.7-05-FLOORS.log, which carries all three runs with their
//                exit codes, wall clocks and printed lines.
export const ARMS_FLOOR = 384;
```
Values at HEAD by symbol: `FILES_FLOOR = 46` (`:877`), `ARMS_FLOOR = 384` (`:1768`), `WAIVED_CEILING = 0` (`:1882`). Expected move: +1 per new `RED-UNDER-M` twin (V2, G1, one whole-set arm per fan-out gate, two instrument arms per fan-out gate → up to +8); `FILES_FLOOR` unchanged (no new gate file). Lower bound: `src/__tests__/mutation-runner-floors.test.ts:565-569` pins `ARMS_FLOOR` to `twins - waivers` exactly. Apply-list coupling that makes the re-points mandatory (`:4085-4094`):
```js
      // Static check: an annotation may only edit files this gate declares.
      …
          if (step.kind === "sql") continue;
          if (!corpusRels.includes(step.file)) {
            addDefect(
              "bad-file-ref",
              ann.arm,
              gateRel,
              `step targets ${step.file}, which is not in this gate's RED-UNDER-SETUP apply list`,
```

---

### `scripts/prod-prober/arms/cron-drift.mjs` (utility, transform)

**Analog for the hoist: section (0)'s PROD-side loop, `:2194-2227`** — the manifest-side loop at `:2373-2408` is already its mirror; the fix is POSITION, not shape. Section (0)'s own placement rule (`:2091-2103`):
```js
  // (0) PROD-SIDE HYGIENE, ABOVE EVERY `return` IN THIS FUNCTION.
  //
  // ⛔ DO NOT MOVE THIS BELOW ANY RETURN. MEASURED on the reverted repair
  // `84b21cb5`: with a PROD command carrying an inline JWT, appending a tidy
  // ` -- comment` to the MANIFEST's copy of that command collapsed the whole
  // result to `manifest-invalid` and the `cron-secret-in-command` finding
  // VANISHED for all fourteen jobs. …
  // This loop reads `prodRows` ONLY. It needs nothing from the oracle, so no
  // oracle state — absent, unparsable, schema-bumped, hand-edited, or captured
  // from a different database — can silence it.
```
The loop body to keep verbatim when moving (`:2194-2227`):
```js
  const rows = Array.isArray(prodRows) ? prodRows : [];
  const prodHygiene = new Map();
  for (const row of rows) {
    const judged = judgeRow(row.jobname, row.command);
    recordVerdict(prodHygiene, row.jobname, judged);
    if (judged.error !== null) {
      defects.push({ kind: "measure-fail", subject: `prod:${row.jobname}`, detail: judged.error });
      const partial = partialCredentialDefect("prod", row.jobname, judged);
      if (partial !== null) defects.push(partial);
      continue;
    }
    const v = judged.violations;
    const { credential, unjudgeable } = splitHygiene(v);
    if (credential.length > 0) {
      defects.push({
        kind: "cron-secret-in-command",
        subject: `prod:${row.jobname}`,
        detail: `the PROD cron.job row for ${shownName(row.jobname)} fails ${credential.length} hygiene rule(s): ${credential.join(" ")}`,
      });
    }
```
The early returns the manifest loop currently sits below: `:2244` (empty live marker), `invalid()` at `:2249-2252` used by the shape checks through `:2340`, `:2342-2346` (no `match_engine_cron`), `:2366-2373` (marker mismatch). Hoist section (2) (`:2373-2408`) to directly after `:2227` and before (0b) at `:2229`, guarding the input the shape checks have not yet validated: `for (const row of (Array.isArray(manifest?.jobs) ? manifest.jobs : []))` with the existing `if (typeof row.command !== "string") continue;` (`:2381`). `manifestHygiene` (`:2380`) must stay declared before its consumer (the withholding logic later in the function reads both maps — `hygieneWithholds`, `:1896` test). Leave (1a)'s sha binding (`:2312-2325`) where it is. Keep the comment block at `:2373-2379` and add the MEASURED line in section (0)'s style.

**The five hygiene shapes, by line (for the whole-token measure):** `MAX_DOLLAR_DEPTH = 4` (`:566`, shape 1 — leave as a refusal); `BUILDERS` (`:685-686`) and the "skipped whole" residual (`:900-903`) plus `headerRegions` (`:1028`) — shape 2; the digit test (`:1464`, `if (!/\d/.test(token)) continue;`) — shape 3; `MIGRATION_FILENAME_RE = /^[0-9]{14}_[a-z0-9_]+\.sql$/` (`:488`) honoured only with `provenance` (`:1488`) — shape 4; `captureManifest`'s `return 3` on `malformed` (`scripts/prod-prober/run.mjs:4240-4249`) and `countMismatch` (`:4251-4260`) — shape 5 (documented refusal; the loud change is to print hygiene findings for readable rows before returning 3).

⚠️ **CONFLICT the planner must resolve, not average (shape 3 vs. a green calibration row).** `hygiene-green.json`'s `fixture_raise_prose_job` is a 34-character alphabetic-only token that is GREEN BY DESIGN: its note reads "clean only because it carries no digit. Rename that job with a numeric suffix and the hourly prober starts crying wolf; the false-positive-budget scenario is the tripwire." Dropping the digit requirement wholesale turns that row red and breaks the zero-false-positive budget. RESEARCH §Q7's remedy already scopes it — "drop the digit requirement only for tokens whose derived length came from concatenation" — so shape 3's red row must be a CONCATENATION-derived alphabetic token (e.g. `'FAKEKEYABCDEFGHIJ' || 'KLMNOPQRSTUVWXYZ'`), never a bare prose token. The `long-token-anywhere` rule text at `:1492-1495` says "containing a digit"; if the rule's predicate changes, its sentence changes with it.

**Rule-id contract** (`:283-292`, `:292-314`): every id in `HYGIENE_RULE_IDS` needs ≥1 row in `hygiene-red.json` whose rule fires ALONE, and the self-test asserts the id sets are EXACTLY equal (`scripts/prod-prober/run.mjs:2252-2257`). A new id → a new red row; a new shape under an existing id → a red row tagged with that id (the file already has 7 rows tagged `x-service-key-literal`).

---

### `scripts/prod-prober/fixtures/cron-drift/hygiene-red.json` (fixture)

**Analog rows** (same file):
- Split OUTSIDE the anchored header (shape 2's nearest existing red): `fixture_nonanchored_split_job` — `"rule": "long-literal-in-headers", "command": "… jsonb_build_object('X-Auth', 'FAKE-key-0123456789' || 'abcdef0123456789') …"`.
- A value moved into a variable outside any header region (the `long-token-anywhere` red): `fixture_var_key_job` — its `note` explains WHY it fires one rule alone ("that is the Q2 partition working, not luck"). Copy the `note` discipline: every non-obvious row carries a `note` naming the mechanism and the reason no other rule fires.
- Assembled operands, never a whole credential-shaped token ("Split into short operands so gitleaks sees no credential-shaped token", `fixture_unbalanced_headers_job` note) — the repo is PUBLIC and CI runs gitleaks.
New rows: shape 2 = `||`-split of operands each < 32 outside every header region (through a non-`BUILDERS` callee or bare); shape 3 = concatenation-derived alphabetic 32+ token (see the conflict above); shape 4 = a `20260101000000_sk_live_lookalike.sql`-shaped token INSIDE a string literal (the `provenance` exemption must stay comment-only). `hygiene-green.json` and `hygiene-bypass.json` must stay clean; `hygiene-bypass.json` rows carry `"expect_any_of"` (`:5-8`) — if the whole-token measure changes WHICH rule fires for a bypass shape, that array is where the new id is admitted.

---

### `src/__tests__/prod-prober-wiring.test.ts` (vitest)

**Analog: describe `[164.8.5-02] compareManifest totality (CR-04)` (`:1798-1956`)** — drives `compareManifest` DIRECTLY with fixture JSON, deep-copies, mutates ONE thing, asserts the defect by kind+subject, and ends with a CALIBRATION test proving the untouched pair is clean. Imports and constants (`:1800-1802`):
```ts
  const DRIFT_FIXTURES = join(PROBER_DIR, "fixtures", "cron-drift");
  const MANIFEST = JSON.parse(readFileSync(join(DRIFT_FIXTURES, "manifest.json"), "utf8"));
  const PROD_OK = JSON.parse(readFileSync(join(DRIFT_FIXTURES, "prod-ok.json"), "utf8"));
  const SUBJECT_JOB = "match_engine_cron";
```
The mutate-one-thing helper with its vacuity guard (`:1811-1819`):
```ts
  const withoutField = (field: string) => {
    const copy = JSON.parse(JSON.stringify(MANIFEST));
    const row = copy.jobs.find((j: { jobname: string }) => j.jobname === SUBJECT_JOB);
    if (!row || !(field in row)) throw new Error(`fixture drift: ${SUBJECT_JOB} has no ${field} to delete`);
    delete row[field];
    if (JSON.stringify(copy) === JSON.stringify(MANIFEST)) throw new Error(`the ${field} mutant is identical to the original`);
    return copy;
  };

  const invalidsFor = (manifest: object | null) =>
    compareManifest(manifest, PROD_OK, { liveMarker: MANIFEST.database_marker }).defects.filter(
      (d: { kind: string }) => d.kind === "manifest-invalid",
    );
```
Existing manifest-side subject assertion (`:1896`): `all.some((d) => d.kind === "measure-fail" && d.subject === \`manifest:${SUBJECT_JOB}\`)`. Calibration closer (`:1945-1955`): "the committed fixture pair must be CLEAN or the four tests above prove nothing".

New cases (the 164.8.5 red control): load `manifest-inline-key.json` (`:1-16`; `normalization: "ws-collapse-v2"`, `match_engine_cron` carries `FAKE-inline-key-…`), deep-copy, then ONE of: `normalization = "ws-collapse-v1"`, `schema_version = 2`, or pass `liveMarker: "quantalyze-fixture-OTHER"`. Assert BOTH the early-return defect (`manifest-invalid`, or `measure-fail` on `database marker`) AND `cron-secret-in-command` with `subject === "manifest:match_engine_cron"`, and that no detail contains `FAKE-inline-key` (the self-test's own rule, `run.mjs:1594-1597`). Use `PROD_OK` rows so the only credential finding is manifest-side. Three separate `it()`s, one lever each (`:1789-1796`: "A single test covering all three would credit one RED to three controls").

**If a self-test scenario is added instead/as well** — analog `scripts/prod-prober/run.mjs:1560-1600`:
```js
    const prod = loadFixture("cron-drift", "prod-inline-key.json");
    const man = loadFixture("cron-drift", "manifest-inline-key.json");
    …
      const r = await driftRun(prod.data, driftFixturePath("manifest-inline-key.json"), quiet);
      const secrets = r.defects.filter((x) => x.kind === "cron-secret-in-command");
      pass =
        expect(r.exitCode === 1, `an inline key exits 1 even with matching shas (got ${r.exitCode})`) &&
        expect(
          secrets.some((x) => x.subject === "manifest:match_engine_cron"),
          "and so is the committed manifest — a manifest captured from a dirty state is itself a finding",
        ) &&
```
and the marker-mismatch scenario at `:1985-2022` (the `otherMarker` lever, asserting the PROD credential "is STILL reported"). ⚠️ Adding a scenario bumps `SELF_TEST_SCENARIOS` (`run.mjs:149`, currently 78) AND the deliberate literal pin `expect(SELF_TEST_SCENARIOS).toBe(78)` at `prod-prober-wiring.test.ts:1774`.

---

## Shared Patterns

### Verification needles vs. gate `find` strings — the DISJOINT sets (Pattern 3, enumerated)
**Apply to:** both new migrations and every re-pointed/new arm. Paste both lists into each migration's header.

| Migration needles (assert presence by `position(needle IN v_def)`) | Gate `find` strings (mutated by twins) |
|---|---|
| `FROM vault.decrypted_secrets` | `IF v_key IS NULL OR btrim(v_key) = '' THEN` (V1 → `IF FALSE THEN`; V2 → `IF v_key IS NULL OR v_key = '' THEN`) |
| `FROM public.system_settings` | `IF v_url IS NULL OR v_url = '' THEN` (U1) |
| `IF v_cnt > 1 THEN` (shape-only; no arm) | `WHERE s.key = 'analytics_service_url'` (C1) |
| `FROM public.system_flags` | `IF v_url !~ c_url_allowed THEN` (C2) |
| `v_enabled IS DISTINCT FROM TRUE` | `read it with an admin session. Allowed: an https host under .up.railway.app';` (C3) |
| `INSERT INTO public.cron_runs` | the A/K/L/B/C/D/E/F/G/H finds listed in the re-point tables; the instrument cause strings |

A/K's twins are designed to PRESERVE `IS DISTINCT FROM TRUE` for exactly this reason (`fanout:482-488`: "that removes the token 20260907130000's OWN verification block checks for at apply time … so the migration ABORTS, the gate never runs, and no arm can be the first failure"). `WAIVED_CEILING = 0` (`run.mjs:1882`), so an intersection is not waivable.

### House style for verification blocks
**Source:** `20260907130000:738-747`; `20260907120000:392-403`
**Apply to:** both migrations
```sql
-- House style: RAISE EXCEPTION, never a silent NOTICE-skip.
--
-- ⛔ This block must NEVER call either function. …
-- Assert the SHAPE, not the behaviour; behaviour is
-- covered by the pg-lane tracer and by supabase/tests/test_ledger_refresh_*.sql.
```
Catalogue reads only (`pg_proc`, `pg_get_functiondef`, `aclexplode`, `pg_roles`, `pg_policies`, `pg_constraint`); never `SELECT … FROM public.<table>` at apply time (criterion 7; shared TEST is empty and a TEST refusal blocks the PROD apply).

### Gate-arm grammar
**Source:** `scripts/mutation-runner/GRAMMAR.md:73-77` (measured `occurrences` REQUIRED), `:100-118` (a mutation may never inject or rewrite an identity), `:540-552` (waivers exist so an unfailable arm is visible — and the ceiling is 0); `test_analytics…:107-121` (identity digit rule; SETUP sub-arms not twinned).
**Apply to:** every new/re-pointed arm. Prose `-- RED-UNDER:` line-start anchored, adjacent `-- RED-UNDER-M: {…}` JSON on ONE line; `sql` steps for anything the migration's own verify would abort on (arm 0 `:210-214`, U2 `:317-326`, I `:943-947`); `edit` steps only on files in the gate's apply list.

### Setup guards that RAISE by name
**Source:** `test_analytics…:229-238` (`TEST FAILED (V1-SETUP)`), `:345-354` (`U2-SETUP`), `:403-405` (`C2-SETUP`), `fanout:589` (`SET LOCAL lock_timeout = '2s'` before a DDL on a shared table)
**Apply to:** V2-SETUP (the `create_secret` of a single space), the instrument arms' `cron_runs` reads. Never a `RAISE NOTICE` skip (`test_analytics…:44-56`).

### Lint rules the new arms must satisfy
**Source:** `scripts/lint-sql-gates.mjs:121-215` (7 rules); regenerate the count with `node scripts/lint-sql-gates.mjs --self-test`.
- R1: no DML / `SELECT INTO` inside an `EXCEPTION WHEN` handler — probes go AFTER the block (or in the block BODY before an unwind).
- R2: any regex/LIKE over a `pg_get_functiondef` result passes through `regexp_replace(…, '--[^\n]*', '', 'g')` first (composite arm 0 does this at `:255-300` with `v_body`).
- R5/R6/R7: a new fixture must not re-create a table/policy a later list entry creates (fixture 33 vs. `20260408113029` — only one may be in a list).

### VAC-04 ack is earned, not pasted
**Source:** `20260907130000:6-46` (worked derivation), `:142-150` (why a placeholder is absent until measured), `scripts/prod-body-drift-check.sh:1300` (`grep -aqF -- "-- prod-body-ack: ${live_hash}"`)
**Apply to:** three acks across the two migrations. Derivation: `node scripts/sql-body-normalize.mjs --diff-bodies <HEAD snapshot> <origin/main snapshot>`; the origin/main hash must equal what VAC-04 reports for PROD, else FOLD the difference.

### Floors move in both directions, from measurement
**Source:** `run.mjs:1751-1768` (procedure quoted above); `src/__tests__/mutation-runner-floors.test.ts:565-569`
**Apply to:** the `ARMS_FLOOR` re-pin after the final commit, with `export PGBIN=/opt/homebrew/opt/postgresql@16/bin` (RESEARCH Pitfall 6). Three runs (N+1 → exit 1 with the `ARMS_FLOOR regression` line; N → exit 0; floors test green), logged beside the SUMMARY.

## No Analog Found

None. Every file has a same-role, same-flow analog in the repo; the two places where RESEARCH's proposal had no working analog (V2's twin, `20260408113029` on the fan-out lanes) are resolved above by the A/K mirror-mutation pattern and the fixture-31 stand-in pattern respectively.

## Metadata

**Analog search scope:** `supabase/migrations/` (the 20260907* pair, `20260515205431`, `20260408113029`, `20260408215026`, `20260826140000`), `supabase/tests/` (the three gate files, every `RED-UNDER-M` parsed), `supabase/schema/functions/` (3 snapshots), `scripts/pg-lane/fixtures/` (07, 12, 15, 31, 32 + directory listing), `scripts/mutation-runner/` (`run.mjs`, `GRAMMAR.md`), `scripts/lint-sql-gates.mjs`, `scripts/lint-app-guc.mjs`, `scripts/prod-body-drift-check.sh`, `scripts/dump-sql-functions.ts`, `scripts/prod-prober/` (`arms/cron-drift.mjs`, `run.mjs`, 4 fixtures), `src/__tests__/prod-prober-wiring.test.ts`, `package.json`
**Files scanned:** 27 (all confirmed `git ls-files`-tracked)
**Pattern extraction date:** 2026-09-11
