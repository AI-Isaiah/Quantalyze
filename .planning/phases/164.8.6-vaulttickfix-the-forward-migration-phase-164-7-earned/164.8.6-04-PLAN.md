---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 04
type: execute
wave: 2
depends_on:
  - 164.8.6-01
  - 164.8.6-02
files_modified:
  - supabase/migrations/20260911120000_vault_tick_hardening.sql
  - supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql
  - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
  - supabase/schema/functions/match_engine_cron_tick.sql
  - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql
  - supabase/schema/functions/enqueue_ledger_composite_refresh.sql
autonomous: true
requirements:
  - 164.7-CR05-VACUOUS-MIGRATION-CHECK
  - 164.7-WR01-VAULT-NOT-STRICT
  - 164.7-MIGRATION-COMMENT-DRIFT
tags: [anti-vacuity, pg-lane, vac-04, schema-snapshot, comment-drift]

estimate:
  tokens: 120000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The replacement for check 6 is PROVEN able to fail: on a scratch copy of the tick migration, deleting the settings read while keeping both RAISE texts makes the lane apply RED with the by-name `does not read public.system_settings` line; the same for the Vault read and the cardinality guard; the source file is byte-identical before and after (cp/cmp, never git checkout --) — ROADMAP criterion 1's own proof obligation, TODOS 164.7-CR05, and criterion 2's shape-only assertion, TODOS 164.7-WR01"
    - "`20260907130000`'s two VAC-04 ack blocks no longer contradict each other and its comment-strip direction note is no longer inverted for its own absence check 6; the edit is comment-only, line-count-NEUTRAL, and merges in the SAME PR as the two new migrations — the criterion-5 reading (RESEARCH §Q1) says the CLI plans by version so the plan set is unchanged, and supabase-migrate.yml:848-851 (`applied ZERO migrations`) hard-fails a push with an empty applied set, so a comment-only merge is safe ONLY bundled with a new migration — TODOS 164.7-MIGRATION-COMMENT-DRIFT"
    - "The three function snapshots are regenerated (`-- source migration:` names the new files), `npm run schema:functions:check` is clean, and each new migration carries an EARNED `-- prod-body-ack: <hash>` line whose hash was reproduced from origin/main's snapshot with the gate's own normaliser (expected today: 3a3fdd65f0412ad8bce83af7c79e0e3d210ecfd6165b27fac620fa2f947d6de3 / cc77a2b93a92d0ae2c84d10b6ea014702b4f93a3c6f1e99014e5ef476c63c93c / 817603330e933568d95e91b8445359ad27d7541e30b24b6b9d367c2e659ef9e2), never pasted off a log"
  artifacts:
    - "supabase/migrations/20260911120000_vault_tick_hardening.sql (+ one prod-body-ack line and its derivation)"
    - "supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql (+ two prod-body-ack lines and derivations)"
    - "supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql (comment-only reconciliation, 939 lines before and after)"
    - "supabase/schema/functions/{match_engine_cron_tick,enqueue_ledger_refresh_for_strategies,enqueue_ledger_composite_refresh}.sql (regenerated)"
  key_links:
    - "scripts/prod-body-drift-check.sh:1300 greps `-- prod-body-ack: ${live_hash}` as a FIXED STRING from the changed files — the hash must equal what VAC-04 reports for PROD on the PR; if it differs, PROD drifted out of band and the difference is FOLDED, never pasted (assumption A1)"
    - "A stale snapshot makes VAC-04 compare the OLD body and report a false clean (Pitfall 9); `src/__tests__/prod-prober-wiring.test.ts:1754` asserts the tick snapshot still matches its `decrypted_secrets` regex"
    - "Comment edits shift `file:line` anchors that pending PLAN.md files cite; `plan-anchor-verify` re-resolves them — the fix is line-count-neutral so this phase's own anchors into 20260907130000 stay valid"
---

<!-- planner-discipline-allow: FROM public.system_settings -->

<objective>
Close the three items that sit between "the migrations exist" and "the migrations are honest": prove the new check 6 can fail (the reason this phase exists), reconcile the sibling's contradictory ack blocks now that criterion 5's reading is taken, and regenerate the snapshots so the VAC-04 acks are earned from origin/main rather than transcribed.

Purpose: ROADMAP criteria 1 (proof), 5 (reading applied), and the ack/snapshot discipline VAC-04 enforces on the PR.
Output: three neuter proofs pasted in the SUMMARY; a line-count-neutral comment fix; regenerated snapshots; three earned ack lines.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md
# read at execution when present (produced by an earlier plan of this phase): .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-01-SUMMARY.md
# read at execution when present (produced by an earlier plan of this phase): .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-02-SUMMARY.md
@supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql
@scripts/prod-body-drift-check.sh
@scripts/sql-body-normalize.mjs
@scripts/dump-sql-functions.ts
</context>

<interfaces>
Consumed: the two new migrations (plans 01, 02); the tick gate's apply list at `supabase/tests/test_analytics_service_settings_and_vault_tick.sql:190` (the `RED-UNDER-SETUP` line; task 1 uses the same seven files with the scratch copy swapped in).
Produced: `-- prod-body-ack: <sha256>` lines (one in the tick file, two in the ledger file) — the fixed strings `scripts/prod-body-drift-check.sh:1300` greps (`prod-body-ack: ${live_hash}`). Snapshot headers `-- source migration: 20260911120000_vault_tick_hardening.sql` and `… 20260911130000_ledger_fanout_grantees_and_dormancy.sql`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: FALSIFIER — neuter the tick migration on a scratch copy, observe RED by name, prove the source untouched</name>
  <files>supabase/migrations/20260911120000_vault_tick_hardening.sql</files>
  <read_first>
    - supabase/migrations/20260911120000_vault_tick_hardening.sql (plan 01 output — locate the three statements to neuter: the settings read `SELECT s.value INTO v_url … FROM public.system_settings s … WHERE s.key = 'analytics_service_url';`, the Vault read `SELECT count(*), max(decrypted_secret) INTO v_cnt, v_key … FROM vault.decrypted_secrets … WHERE name = 'analytics_service_key';`, and the `IF v_cnt > 1 THEN … END IF;` block)
    - scripts/pg-lane/run.sh:15-45 (usage: `--workdir`, `--apply` in order, `--gate`; exit = failing psql step's status)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md §Q2 "Criterion 1's own proof obligation" and § Code Examples "Neuter proof"
    - CLAUDE.md § Destructive-edit safety (memory): NEVER `git checkout --` in a neuter harness; byte backups go stale — re-take after every edit; verify the revert by grepping the newest fix, not by hash alone
  </read_first>
  <action>
    Work ONLY on copies under the session scratchpad (`$SCRATCH`); the tracked file is never edited by this task. Steps, each recorded in the SUMMARY with its printed line:
    0. `cp` the tracked migration to `$SCRATCH/mig-a.pristine.sql`. Positive control: run `bash scripts/pg-lane/run.sh --workdir "$SCRATCH/lane-f0" --apply <the six fixtures from the tick gate's apply list> "$SCRATCH/mig-a.pristine.sql" --gate supabase/tests/test_analytics_service_settings_and_vault_tick.sql` with `PGBIN` exported — expect exit 0 (the pristine copy applies and the gate passes with 13 arms).
    1. NEUTER A (criterion 1's exact obligation — TODOS 164.7-CR05): `cp` pristine to `$SCRATCH/mig-a.neuterA.sql`; delete the THREE lines of the settings read (the SELECT … INTO v_url, the FROM public.system_settings line, the WHERE line) and NOTHING else — both RAISE texts that name the setting key stay. Prove the edit LANDED: `grep -cF 'FROM public.system_settings' "$SCRATCH/mig-a.neuterA.sql"` (non-comment) must be 0 while `grep -c 'analytics_service_url' …` is still > 2 (the prose survives). Apply on a fresh lane with the neutered copy in the seventh slot. EXPECT: non-zero exit and the stream contains `Migration 20260911120000:` followed by the settings-needle message naming `FROM public.system_settings`. If the apply is GREEN, the check is vacuous and plan 01 task 1 is wrong — stop and fix the migration, never this task.
    2. NEUTER B: `cp` pristine to `…neuterB.sql`; delete the Vault read statement (its SELECT/FROM/WHERE lines); prove `FROM vault.decrypted_secrets` count (non-comment) is 0; apply; EXPECT RED with the vault-needle message.
    3. NEUTER C (TODOS 164.7-WR01, shape-only assertion made falsifiable): `cp` pristine to `…neuterC.sql`; delete the `IF v_cnt > 1 THEN … END IF;` block (all its lines); prove `IF v_cnt > 1 THEN` count is 0; apply; EXPECT RED with the cardinality-needle message.
    4. Prove the SOURCE is untouched: `cmp "$SCRATCH/mig-a.pristine.sql" supabase/migrations/20260911120000_vault_tick_hardening.sql` exits 0 and `git status --porcelain -- supabase/migrations/20260911120000_vault_tick_hardening.sql` prints nothing.
    Paste, per neuter, the grep count proving the edit landed, the RED line, and the exit code into the SUMMARY under a `## Falsifier` heading. The scratch lanes are removed by run.sh's EXIT trap; delete the scratch copies at the end.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && export PGBIN=/opt/homebrew/opt/postgresql@16/bin && S="${SCRATCH:?set SCRATCH to the session scratchpad}" && M=supabase/migrations/20260911120000_vault_tick_hardening.sql && G=supabase/tests/test_analytics_service_settings_and_vault_tick.sql && FX="scripts/pg-lane/fixtures/01-fixture-core.sql scripts/pg-lane/fixtures/02-fixture-sanitize-tables.sql scripts/pg-lane/fixtures/07-fixture-supabase-default-privileges.sql scripts/pg-lane/fixtures/12-fixture-profiles-is-admin.sql scripts/pg-lane/fixtures/15-fixture-auth-role.sql scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql" && cp "$M" "$S/mig-a.pristine.sql" && grep -v '^\s*--' "$S/mig-a.pristine.sql" | grep -vF 'FROM public.system_settings' > "$S/mig-a.neuterA.sql" && test "$(grep -cF 'FROM public.system_settings' "$S/mig-a.neuterA.sql")" = 0 && OUT="$(bash scripts/pg-lane/run.sh --workdir "$S/lane-vA" --apply $FX "$S/mig-a.neuterA.sql" --gate "$G" 2>&1)"; RC=$?; printf '%s\n' "$OUT" | grep -a 'Migration 20260911120000' | head -3; test "$RC" != 0 && printf '%s\n' "$OUT" | grep -aq 'Migration 20260911120000:.*FROM public.system_settings' && cmp "$S/mig-a.pristine.sql" "$M" && test -z "$(git status --porcelain -- "$M")" && echo FALSIFIER-A-RED-SOURCE-INTACT</automated>
    <fails_when>The lane apply of the neutered copy exits 0 (the check did NOT fail — vacuous), the stream lacks a `Migration 20260911120000:` line naming `FROM public.system_settings`, the neuter did not land (count not 0), `cmp` reports the tracked file differs from the pristine copy, `git status` shows the migration modified, or `FALSIFIER-A-RED-SOURCE-INTACT` is not printed. (This automated check covers neuter A; the executor records B and C by the same procedure in the SUMMARY.)</fails_when>
  </verify>
  <acceptance_criteria>
    - SUMMARY `## Falsifier` section lists, for neuters A, B and C: the `grep -c` proving the needle count in the scratch copy is 0, the exact RED line beginning `Migration 20260911120000:` (settings / vault / cardinality message respectively), and the non-zero exit code.
    - Positive control recorded: pristine copy applies and the gate exits 0.
    - `cmp` of pristine vs tracked exits 0; `git status --porcelain` for the migration is empty; the recorded procedure restores by `cp`/`cmp` only.
  </acceptance_criteria>
  <done>Check 6's replacement is proven able to fail three ways, and the tracked migration is proven byte-identical to its pre-task copy.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile `20260907130000`'s ack blocks and its inverted direction note — comment-only, line-count-neutral, bundled</name>
  <files>supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql</files>
  <read_first>
    - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql:1-60 (block ONE: the two `prod-body-ack:` pragmas at :10-11 and the worked derivation :13-46), :133-152 (block TWO: the "measured in plan 06" placeholders and the paragraph saying the pragma is absent — the contradiction TODOS.md:7946 names), :858-895 (the comment-strip direction note; :880-885 claims a `--` inside a string literal "can only cause a FALSE FAILURE"), :899-916 (checks 4/5 are PRESENCE needles, check 6 at :912-916 is an ABSENCE needle)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md §Q1 (criterion 5's reading: CLI plans by VERSION; `supabase-migrate.yml:848-851` fails a push with zero applied; comment edit inside a body changes the snapshot; VAC-04/VAC-08 are comment-blind)
    - .github/workflows/supabase-migrate.yml:817-823, :848-851 (the zero-applied guard, verbatim)
    - scripts/verify-plan-anchors.mjs:1-40 (`--pending` re-resolves every anchor in pending plans)
  </read_first>
  <action>
    Criterion 5 gate, recorded BEFORE editing (write it into the SUMMARY): the reading is TAKEN (RESEARCH §Q1) — a comment-only edit does not change the plan set (the CLI's `findPendingMigrations` compares versions, never content), so this edit is safe to make IN PLACE provided it merges in the same PR as plans 01/02's new migrations (then `applied_n >= 1` and the `:848` guard is not taken). It must NEVER ship on its own push to main. Phase 164.5.1's criterion 6 is answered by the same reading (its comment edits ship with NO migration and would go red — it should annotate beside the files); say so in the SUMMARY.

    Edit ONE: block TWO (`:133-152`). Replace the six angle-bracketed "measured in plan 06" placeholder values with the values block ONE already records (the two hashes at :10-11; the run id, sha and differing-line count from :37-46), and replace the paragraph at :142-146 that declares the pragma deliberately absent until measured with a paragraph of the SAME line count stating that the pragmas live at :10-11 above, were EARNED as block ONE derives, and that this block previously contradicted block ONE (booked as 164.7-MIGRATION-COMMENT-DRIFT, fixed by 164.8.6 comment-only, bundled with 20260911120000/130000 so the zero-applied guard is not taken). Keep the "EARNED, not pasted" sentence.

    Edit TWO: the direction note at `:880-885`. Its sentence "The other direction is safe by construction — a `--` inside a string literal makes the strip eat real code, which can only cause a FALSE FAILURE, and a false failure is loud" is TRUE for the presence checks (4)/(5) and INVERTED for the ABSENCE check (6) at :912-916, where eaten code containing the retired call is a false PASS. Rewrite those lines (same line count) to state both directions per check kind and the measurement that makes it moot today (0 code lines in either body carry a trailing `--`). This is the WR-03/WR-08 finding TODOS.md:7946 cites.

    LINE-COUNT NEUTRALITY is a hard requirement: the file is 939 lines before and MUST be 939 lines after, and no line outside the two edited regions may change — pending PLAN.md files in this phase cite `20260907130000:766`, `:896`, `:250-261`, `:509-520`, `:776-783`, `:926-933` and `plan-anchor-verify` re-resolves them. Comment-only: the executable text is unchanged, so `npm run schema:functions:check` stays clean and VAC-04/VAC-08 see no drift (both comment-blind).
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && F=supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql && test "$(wc -l < "$F")" = 939 && test "$(grep -c '<measured in plan 06>' "$F")" = 0 && test "$(grep -c 'DELIBERATELY ABSENT' "$F")" = 0 && test "$(grep -c '^-- prod-body-ack: ' "$F")" = 2 && grep -qF '164.7-MIGRATION-COMMENT-DRIFT' "$F" && grep -qiF 'false PASS' "$F" && git diff --numstat -- "$F" | awk '{ if ($1 != $2) exit 1 }' && test "$(git diff -U0 -- "$F" | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[-+]\s*--' | wc -l | tr -d ' ')" = 0 && npm run schema:functions:check && node scripts/verify-plan-anchors.mjs --pending && echo COMMENT-FIX-OK</automated>
    <fails_when>The file is not exactly 939 lines; a `<measured in plan 06>` placeholder or the `DELIBERATELY ABSENT` phrase survives; the pragma count is not 2; the TODOS id or the corrected `false PASS` wording is missing; `git diff --numstat` shows added != deleted lines (not neutral); any changed line is NOT a `--` comment line (count must be 0); `schema:functions:check` prints `stale:`/`ratchet-stale:` or exits non-zero; `verify-plan-anchors.mjs --pending` reports a broken anchor; or `COMMENT-FIX-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `wc -l` of the file = 939 before and after; `git diff --numstat` added == deleted.
    - Every changed line begins with `--` (comment-only), verified by the diff filter above.
    - Block TWO carries the two measured hashes and no placeholder; the "absent" paragraph is gone; the TODOS id `164.7-MIGRATION-COMMENT-DRIFT` appears in the file.
    - The direction note distinguishes presence checks (4)/(5) from absence check (6) and uses the phrase `false PASS` for the absence case.
    - `npm run schema:functions:check` and `node scripts/verify-plan-anchors.mjs --pending` exit 0.
    - SUMMARY records the criterion-5 reading and the "bundle, never standalone" rule, plus the note for Phase 164.5.1.
  </acceptance_criteria>
  <done>The sibling's two ack blocks agree, its direction note is correct per check kind, the edit is provably comment-only and line-count-neutral, and every pending plan anchor still resolves.</done>
</task>

<task type="auto">
  <name>Task 3: Regenerate the three snapshots and EARN the three VAC-04 acks from origin/main</name>
  <files>supabase/schema/functions/match_engine_cron_tick.sql, supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql, supabase/schema/functions/enqueue_ledger_composite_refresh.sql, supabase/migrations/20260911120000_vault_tick_hardening.sql, supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql</files>
  <read_first>
    - scripts/dump-sql-functions.ts:295-310 (`-- source migration:` header), :500-550 (`--check` and its `stale:` output)
    - scripts/sql-body-normalize.mjs:40-55 (usage: `--hash [file]`, `--diff-bodies <snapshot.sql> <live.sql>`)
    - scripts/prod-body-drift-check.sh:13-30 (`comment-stripped and whitespace-normalized`, both sides), scripts/prod-body-drift-check.sh:1290-1305 (the fixed-string grep for `-- prod-body-ack: ${live_hash}`)
    - supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql:6-46 (the worked ack derivation to copy in shape)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md §Q4 (expected hashes at HEAD; assumption A1) and Pitfall 5, Pitfall 9
  </read_first>
  <action>
    1. `npm run schema:functions` then `npm run schema:functions:check` (must print no `stale:`). Confirm each regenerated snapshot's `-- source migration:` header names the NEW migration (`20260911120000_vault_tick_hardening.sql` for the tick; `20260911130000_ledger_fanout_grantees_and_dormancy.sql` for both fan-outs) and that the tick snapshot still matches `FROM vault.decrypted_secrets` (`src/__tests__/prod-prober-wiring.test.ts:1754`, the `decrypted_secrets` regex).
    2. EARN each ack (never paste): for each of the three functions, `git show origin/main:supabase/schema/functions/<fn>.sql > "$SCRATCH/om-<fn>.sql"` then `node scripts/sql-body-normalize.mjs --hash "$SCRATCH/om-<fn>.sql"` — this is the hash VAC-04 will report for PROD if PROD equals origin/main's snapshot (assumption A1). Expected today: tick `3a3fdd65f0412ad8bce83af7c79e0e3d210ecfd6165b27fac620fa2f947d6de3`, strategies fan-out `cc77a2b93a92d0ae2c84d10b6ea014702b4f93a3c6f1e99014e5ef476c63c93c`, composite `817603330e933568d95e91b8445359ad27d7541e30b24b6b9d367c2e659ef9e2`. Also run `node scripts/sql-body-normalize.mjs --diff-bodies supabase/schema/functions/<fn>.sql "$SCRATCH/om-<fn>.sql"` and record the two hashes and the differing-line count it prints (names only, no body text — the repo is public).
    3. Write into each new migration's ack block (plans 01/02 left the pragma line absent by design): the line `-- prod-body-ack: <origin/main hash>` (one in the tick file, two in the ledger file, one per function), followed by the derivation in the `20260907130000:37-46` shape: the origin/main sha (`git rev-parse origin/main`), the two hashes, the differing-line count, and the sentence that the ack is EARNED only if VAC-04 on the PR reports the SAME PROD hash — if it differs, PROD drifted out of band and the difference must be FOLDED into the migration, never acknowledged. The ack comment must not contain any gate `find` string (plan 03's `occurrences` are measured on the ledger file).
    4. Run `npx vitest run src/__tests__/prod-prober-wiring.test.ts` (snapshot assertion at :1754) and the two shape gates from plans 01/02 again (their `<verify>` chains) — the added comment lines must not disturb any pinned count.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && S="${SCRATCH:?}" && npm run schema:functions:check && grep -qF -- '-- source migration: 20260911120000_vault_tick_hardening.sql' supabase/schema/functions/match_engine_cron_tick.sql && grep -qF -- '-- source migration: 20260911130000_ledger_fanout_grantees_and_dormancy.sql' supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql && grep -qF -- '-- source migration: 20260911130000_ledger_fanout_grantees_and_dormancy.sql' supabase/schema/functions/enqueue_ledger_composite_refresh.sql && grep -qiE 'FROM\s+vault\.decrypted_secrets' supabase/schema/functions/match_engine_cron_tick.sql && for fn in match_engine_cron_tick enqueue_ledger_refresh_for_strategies enqueue_ledger_composite_refresh; do git show "origin/main:supabase/schema/functions/$fn.sql" > "$S/om-$fn.sql"; H="$(node scripts/sql-body-normalize.mjs --hash "$S/om-$fn.sql")"; case "$fn" in match_engine_cron_tick) F=supabase/migrations/20260911120000_vault_tick_hardening.sql;; *) F=supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql;; esac; grep -qF -- "-- prod-body-ack: $H" "$F" || { echo "ACK MISSING for $fn ($H)"; exit 1; }; done && test "$(grep -c '^-- prod-body-ack: ' supabase/migrations/20260911120000_vault_tick_hardening.sql)" = 1 && test "$(grep -c '^-- prod-body-ack: ' supabase/migrations/20260911130000_ledger_fanout_grantees_and_dormancy.sql)" = 2 && npx vitest run src/__tests__/prod-prober-wiring.test.ts && echo SNAPSHOTS-ACKS-OK</automated>
    <fails_when>`schema:functions:check` prints `stale:` or exits non-zero; a snapshot's `-- source migration:` header does not name the new file; the tick snapshot no longer matches the Vault-read regex; for any function the hash recomputed from origin/main's snapshot is NOT present as a `-- prod-body-ack:` fixed string in the owning migration (`ACK MISSING` printed); the pragma counts are not 1 and 2; the wiring test fails; or `SNAPSHOTS-ACKS-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `npm run schema:functions:check` exits 0 with no `stale:` line; the three snapshots name the new migrations as source.
    - Each ack hash in the migrations equals `node scripts/sql-body-normalize.mjs --hash` of the origin/main snapshot of that function (recomputed in the verify, not read from RESEARCH); the tick file has exactly 1 pragma line, the ledger file exactly 2.
    - Each ack block records the origin/main sha, both hashes and the differing-line count from `--diff-bodies`, and the FOLD-never-paste rule.
    - `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes; the plan 01 and plan 02 shape gates still print `TRACER-SHAPE-OK` / `LEDGER-SHAPE-OK`.
  </acceptance_criteria>
  <done>Snapshots are current, and the three acks are earned from origin/main with their derivations recorded beside them, ready for VAC-04 to confirm on the PR.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 04's share)

- `-- prod-body-ack: <sha256>` lines: 1 in `20260911120000_vault_tick_hardening.sql`, 2 in `20260911130000_ledger_fanout_grantees_and_dormancy.sql`, each with a derivation block.
- REGENERATED `supabase/schema/functions/match_engine_cron_tick.sql`, `enqueue_ledger_refresh_for_strategies.sql`, `enqueue_ledger_composite_refresh.sql` (source headers name the new migrations).
- MODIFIED (comment-only, 939 -> 939 lines) `supabase/migrations/20260907130000_ledger_refresh_switch_to_system_flags.sql`.
- SUMMARY sections: `## Falsifier` (three neuters, RED lines, exit codes, cmp proof) and `## Criterion 5 reading` (the rule for this phase and for 164.5.1).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo snapshot -> PROD body (VAC-04) | the ack asserts PROD's live body equals origin/main's snapshot; a pasted hash silences the gate without evidence |
| scratch lane -> tracked source | neuter proofs must never leak into the tracked migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-16 | Repudiation | a `prod-body-ack` transcribed off a CI log | high | mitigate | hash recomputed from origin/main's snapshot inside the verify; FOLD rule recorded in the block |
| T-164.8.6-17 | Tampering | a neuter harness restoring with `git checkout --` and destroying uncommitted work | medium | mitigate | scratch copies only; `cmp` + `git status` prove the source untouched |
| T-164.8.6-18 | Denial of Service (deploy) | comment-only edit merged ALONE -> `supabase-migrate.yml:848` (`applied_n`) red -> PROD apply skipped | high | mitigate | bundled in the same PR as two new migrations (plan 06); rule recorded in SUMMARY |
| T-164.8.6-19 | Information Disclosure | `--diff-bodies` output pasted with body text into a public repo | low | mitigate | the tool prints names and hashes only; SUMMARY records hashes and counts |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- Task 1: `FALSIFIER-A-RED-SOURCE-INTACT` printed; B and C recorded in the SUMMARY by the same procedure.
- Task 2: `COMMENT-FIX-OK` (939 lines, comment-only diff, anchors resolve, snapshot check clean).
- Task 3: `SNAPSHOTS-ACKS-OK` (hashes recomputed from origin/main, pragma counts 1 and 2, wiring test green).
</verification>

<success_criteria>
- Criterion 1's proof obligation met three ways with the source proven untouched; criterion 5's reading applied (bundled comment fix, line-count-neutral); VAC-04 acks earned, snapshots current.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-04-SUMMARY.md` when done.
</output>
