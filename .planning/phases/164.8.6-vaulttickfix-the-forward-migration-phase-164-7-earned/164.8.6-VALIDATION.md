---
phase: "164.8.6"
slug: "vaulttickfix-the-forward-migration-phase-164-7-earned"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-11"
---

# Phase 164.8.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Seeded from `164.8.6-RESEARCH.md` § Validation Architecture. Every command below is
> quoted from that section, not re-invented here.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.10 (TS) + SQL gates `supabase/tests/test_*.sql`, run on shared TEST (`ci.yml:2606`) and on the throwaway pg-lane by `scripts/mutation-runner/run.mjs` |
| **Config file** | `vitest.config.ts`; `scripts/mutation-runner/GRAMMAR.md` |
| **Quick run command** | `npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/prod-prober-wiring.test.ts` · `node scripts/lint-sql-gates.mjs --self-test && node scripts/lint-sql-gates.mjs` · `node scripts/lint-app-guc.mjs --self-test && node scripts/lint-app-guc.mjs` · `npm run schema:functions:check` |
| **Full suite command** | `export PGBIN=/opt/homebrew/opt/postgresql@16/bin && node scripts/mutation-runner/run.mjs` (≈ 9 min) + `npm run test:coverage` |
| **Estimated runtime** | ~120 s quick · ~9 min full lane |

⛔ **`PGBIN` is mandatory, measured:** PATH's `pg_ctl` is 18.6 and has no pg_cron; the lane's own
check script refuses the mismatch verbatim. A lane run without it is not a green run.

⚠️ **`npm run typecheck` is a SEPARATE gate from `npm run lint`** (lint = eslint + 3 manifest
checks). Both belong in the phase gate. Recorded 2026-09-11 after a phase shipped with 7 unseen
TS errors because every "gates green" report omitted `tsc --noEmit`.

⚠️ **A file-scoped vitest run cannot clear a directory-scanning contract test.**
`src/__tests__/test-restore-workflow-wiring.test.ts` scans ALL of `src/__tests__`, so the matching
`--shard` must run before any green claim.

---

## Sampling Rate

- **After every task commit:** the quick-run set above (< 2 min)
- **After every plan wave:** full-corpus lane run + `npm run test:coverage`
- **Before `/gsd-verify-work`:** full suite green on a clean tree, SHA-bound in the SUMMARY
- **Max feedback latency:** 120 s (quick), 540 s (full lane)
- ⛔ **Phase gate, ordering:** the three reviewers (migration-reviewer, rls-policy-auditor,
  silent-failure-hunter) run BEFORE `gh pr create` and their findings are fixed — criterion 6

---

## Per-Task Verification Map

| Req ID | Behavior | Test Type | Automated Command | File Exists | Status |
|--------|----------|-----------|-------------------|-------------|--------|
| 164.7-CR05-VACUOUS-MIGRATION-CHECK | new check goes RED when the settings read is deleted while the `RAISE` prose stays | lane neuter proof (apply-time) | scratch-copy apply per RESEARCH § Code Examples | ❌ one-off, recorded in SUMMARY | ⬜ pending |
| 164.7-WR01-VAULT-NOT-STRICT | `v_cnt > 1` cardinality guard present by needle | migration `DO`, shape-only (no gate arm) | apply on lane via the gate's apply list | ❌ Wave 0: migration | ⬜ pending |
| VAULTTICK-EMPTYKEY-01 | single-space key ⇒ RAISE by name | SQL gate arm V2 + `RED-UNDER-M` twin | `node scripts/mutation-runner/run.mjs` | ❌ Wave 0: arm | ⬜ pending |
| 164.7-WR02-SERVICE-ROLE-EXECUTE | grantee set = `{owner}`; a `service_role` GRANT makes it RED | migration `DO` + gate arm G1 | runner | ❌ Wave 0 | ⬜ pending |
| APPGUC-WARNING-UNINSTRUMENTED-01 + 164.7-DORMANCY-UNINSTRUMENTED | an invisible dormancy cause writes a `cron_runs` row naming the cause | gate arms D1/D2 in BOTH fan-out gates | runner | ❌ Wave 0 | ⬜ pending |
| 164.7-MIGRATION-COMMENT-DRIFT | ack blocks consistent; function snapshot not stale | `npm run schema:functions:check` + VAC-04 on the PR | CI | ✅ | ⬜ pending |
| 164.8.5-MANIFEST-SIDE-LOOP-DEAD | the manifest-side finding survives every early return | vitest | `npx vitest run src/__tests__/prod-prober-wiring.test.ts` | ✅ file / ❌ new cases | ⬜ pending |
| 164.8.5-HYGIENE-RESIDUALS | red rows for shapes 2-4; green + bypass corpora unchanged | prober self-test + vitest | `node scripts/prod-prober/run.mjs --self-test` | ✅ fixtures / ❌ new rows | ⬜ pending |
| floors | `ARMS_FLOOR` separated in BOTH directions | runner (upper) + `mutation-runner-floors.test.ts` (lower) | see RESEARCH § Q6 | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `supabase/migrations/<ts>_vault_tick_hardening.sql` — CR05, WR01, WR02 (tick), EMPTYKEY
- [ ] `supabase/migrations/<ts>_ledger_fanout_grantees_and_dormancy.sql` — WR02 (fan-outs), APPGUC, DORMANCY
- [ ] arms V2, G1 (+ re-pointed V1/U1/C1/C2/C3) in `test_analytics_service_settings_and_vault_tick.sql`
- [ ] arms G-*, D1/D2 (+ 26 re-pointed) in the two fan-out gates; `20260408113029` added to their apply lists
- [ ] vitest cases for the hoisted manifest loop; red fixture rows for hygiene shapes 2-4

⛔ **TWO migration files, not one — measured, and it contradicts the phase goal sentence.** The
tick gate's lane lacks the ledger stack and the fan-out lanes lack the vault stand-in, so a single
file cannot apply on either lane. One PR, two files. The ROADMAP goal says *"One forward
migration"*; that phrase is disproven by the lane topology and is recorded here rather than
silently satisfied.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| check 6's replacement is genuinely falsifiable | 164.7-CR05 | the proof deletes a statement from a migration and applies the mutant to a scratch lane; it is an apply-time one-off, not a repeatable gate arm | copy the migration to a scratch file, delete the settings read, KEEP the `RAISE` text, apply to the pg-lane, observe the RAISE. ⛔ `cp`-restore and `cmp`; NEVER `git checkout --` |
| PROD apply | all SQL reqs | merging `supabase/migrations/**` auto-applies to PROD behind a human reviewer gate | founder approves the `Production` environment gate in the GitHub UI after `apply-test` is green |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or a Wave 0 dependency
- [ ] Sampling continuity: no 3 consecutive tasks without an automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120 s (quick)
- [ ] ⛔ No check added by this phase reads DATA (row counts) rather than CATALOGUE — criterion 7.
      Never re-run `120000:534` (seed row) or `130000:926` (flag row): shared TEST's
      `system_settings` / `system_flags` are EMPTY, so such a check applies on PROD and REFUSES on
      TEST, and a failed TEST apply BLOCKS the PROD apply
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
