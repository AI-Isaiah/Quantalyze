---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 05
subsystem: testing
tags: [mutation-runner, arms-floor, ci-gates, reviewers, planning-hygiene, sentinel-floor]

# Dependency graph
requires:
  - phase: 164.8.6 (plan 03)
    provides: "both ledger gates re-pointed at 20260911130000 with arms S1/M1/M2 each, and the measured claim that the corpus stands at 392 annotations / 411 steps"
  - phase: 164.8.6 (plan 04)
    provides: "the three earned prod-body-ack lines, the regenerated function snapshots, and the measured correction that the acked hash is --diff-bodies' `live` column"
  - phase: 164.7
    provides: "the ARMS_FLOOR derivation block at 384 whose shape this plan appends to, and the two-layer floor design (runner gates the upper direction, the vitest ratchet the lower)"
provides:
  - "`ARMS_FLOOR = 392`, separated in BOTH directions on real full-corpus pg-lane runs, with a dated derivation block naming all eight new twins and citing the full log"
  - "`164.8.6-05-FLOORS.log` — FIVE complete runs with exit codes and wall clocks, including two contaminated runs KEPT rather than deleted, each with its cause named"
  - "SEVEN stale corpus-tracking artifacts found by RUNNING and re-pinned, of which SIX were named by no plan in this phase"
  - "Two repo-wide gates greened that were red on arrival and are not this plan's doing: 46 planning-hygiene violations (a public-repo local-identity leak across all eight PLAN.md files) and 6 stale plan anchors"
  - "Three reviewer rubrics executed over the SQL diff BEFORE any PR exists, with every finding dispositioned by measurement — and an honest account of the ONE way the dispatch differed from the plan"
affects: [164.8.6-06 (the PR — the reviewer record and the no-PR-yet ordering are its preconditions), 164.8.6-07, 164.8.6-08]

actuals:
  tokens: 86133
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "A floor is pinned from a RUN and separated in both directions; the stale-low direction is proven by observing the OTHER layer go red first"
    - "A contaminated measurement is KEPT beside its clean re-take, with the contamination's cause named — deleting it would hide that the clean-tree assertion works"
    - "When a number about the corpus is mirrored somewhere else, moving the corpus means moving EVERY mirror in the same commit; only the full suite finds them all"

key-files:
  created:
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-FLOORS.log
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-SUMMARY.md
  modified:
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/mutation-annotation-parser.test.ts
    - src/__tests__/gate-family-meta.test.ts
    - src/__tests__/drift-check-scripts.test.ts
    - .github/workflows/ci.yml
    - .planning/phases/164.8.6-.../164.8.6-01..08-PLAN.md (absolute home paths -> repo-relative)

key-decisions:
  - "ARMS_FLOOR pinned at 392 from a RUN. The plan PREDICTED 392 and the run agreed exactly (+8: V2, G1, and S1/M1/M2 in each of two ledger gates) — but the number was taken from `biting: 392`, never from the prediction."
  - "TWO runs were re-taken rather than accepted. RUN 2 tripped the runner's clean-tree assertion on this executor's own mid-run edit; RUN 3 tripped it on a CONCURRENT session's .planning commits. Both are kept in the log with their causes; a separation record carrying an unexplained co-defect is not a separation record."
  - "SEVEN stale pins, SIX unnamed by any plan. The plan named one file; the corpus is mirrored in seven places. Only `npm run test:coverage` found the last four — every file-scoped run was green."
  - "The 46 planning-hygiene violations were fixed as a WHOLE CLASS across all eight PLAN.md files, not point-fixed on the two this plan touched. 21 of 23 pre-date this plan; leaving them would have kept `npm run lint` red for whoever ran it next."
  - "⛔ THE THREE REVIEWERS WERE NOT DISPATCHED AS SUBAGENTS. No Task/Agent tool exists in this executor's context. Their shipped rubrics were executed inline instead. The ORDERING criterion 6 demands is satisfied (no PR exists); the INDEPENDENCE of three fresh contexts is NOT. Stated as a gap, not papered over."
  - "The one MEDIUM finding (an uncounted candidate-failure path) was NOT fixed: it is inherited from 20260907130000, outside this task's scope boundary, and fixing it would add an arm and move the floor again at the end of a gate-locked phase."

patterns-established:
  - "Pattern: keep the contaminated run beside the clean one — it is the evidence that the clean-tree assertion bites"
  - "Pattern: a `.replace()`-derived RED arm needs `.not.toBe(GREEN)` beside it, because a pattern that stops matching makes the arm pass on nothing"
  - "Pattern: never spell a script PATH in a CI job body that does not run it — substring-based job-coverage derivations read the mention as an invocation"

requirements-completed:
  - VAULTTICK-EMPTYKEY-01
  - 164.7-WR02-SERVICE-ROLE-EXECUTE
  - 164.7-CR05-VACUOUS-MIGRATION-CHECK
  - 164.7-WR01-VAULT-NOT-STRICT
  - APPGUC-WARNING-UNINSTRUMENTED-01
  - 164.7-DORMANCY-UNINSTRUMENTED
  - 164.7-MIGRATION-COMMENT-DRIFT

coverage:
  - id: D1
    description: "ARMS_FLOOR re-pinned to the MEASURED biting-arm count 392, separated in BOTH directions on real full-corpus lane runs"
    requirement: "VAULTTICK-EMPTYKEY-01"
    verification:
      - kind: integration
        ref: "164.8.6-05-FLOORS.log RUN 1 — `arms: 392/392/0`, `biting: 392`, `lane-invocations: 392`, `✅ No defects`, exit 0, 546.3 s"
        status: pass
      - kind: integration
        ref: "164.8.6-05-FLOORS.log RUN 2b — floor 393, exit 1, EXACTLY one defect: `ARMS_FLOOR regression: 392 biting arm(s) < floor 393`"
        status: pass
      - kind: integration
        ref: "164.8.6-05-FLOORS.log RUN 3b — floor 392, `✅ No defects`, exit 0, 539.2 s, at the final bytes"
        status: pass
      - kind: unit
        ref: "src/__tests__/mutation-runner-floors.test.ts — 81 passed; observed RED at 384 first with `The corpus declares 392 twin(s) of which 0 are waivers ... ARMS_FLOOR is 384.`"
        status: pass
    human_judgment: false
  - id: D2
    description: "FILES_FLOOR stays 46 and WAIVED_CEILING stays 0; exactly one non-comment line changed in run.mjs across the whole branch"
    verification:
      - kind: other
        ref: "git diff main...HEAD -- scripts/mutation-runner/run.mjs | grep -v '^[+-]//' => only `ARMS_FLOOR = 384` -> `392`"
        status: pass
      - kind: integration
        ref: "run's own `coverage: files 46/73`"
        status: pass
    human_judgment: false
  - id: D3
    description: "Every corpus-mirroring pin re-pinned from the SAME measurement — parser test (armsSeen/stepsSeen/needles), floors test (totalAnchored, GREEN_LOG fixture and its RED-arm regexes), gate-family threshold registry, ci.yml derivation table, and its exact-literal mirror"
    verification:
      - kind: unit
        ref: "npm run test:coverage — 853 files passed, 14752 tests passed, 0 failed, exit 0"
        status: pass
    human_judgment: false
  - id: D4
    description: "Every quick gate green on the SQL tree at a named sha"
    verification:
      - kind: other
        ref: "the plan's task-2 chain run verbatim under bash — `QUICK-GATES-GREEN`, chain exit 0, at HEAD 6a1b8118"
        status: pass
    human_judgment: false
  - id: D5
    description: "Two repo-wide gates that were RED on arrival, greened at the source: check-planning-hygiene (46 violations) and plan-anchor-verify (6 stale claims)"
    verification:
      - kind: other
        ref: "npm run lint — `6291 tracked files scanned, none carry the local username or an absolute home path`, exit 0"
        status: pass
      - kind: other
        ref: "node scripts/verify-plan-anchors.mjs --pending — `claims: 122 checked / OK: 7 plan file(s), no stale claims`, exit 0"
        status: pass
    human_judgment: false
  - id: D6
    description: "Three reviewer rubrics executed over the SQL diff before any PR exists; every finding dispositioned"
    verification:
      - kind: other
        ref: "gh pr list --head phase-164.8.6-vaulttickfix --state all => EMPTY"
        status: pass
    human_judgment: true
    rationale: "The rubrics were executed INLINE by this executor, not by three independently-spawned opus subagents — no Task/Agent tool exists in this context. The ordering criterion 6 demands holds, but the independence of three fresh contexts does not. A human must decide whether that independence is required before the PR opens."

# Metrics
duration: 84 min
completed: 2026-09-11
status: complete
---

# Phase 164.8.6 Plan 05: ARMS_FLOOR, the gate chain, and the three reviewers — Summary

**`ARMS_FLOOR` pinned at a measured 392 and separated both ways on real pg-lanes, seven stale corpus mirrors re-pinned (six of them unnamed by any plan), two repo-wide gates greened that were red on arrival, and three reviewer rubrics run before any PR exists.**

## Performance

- **Duration:** 84 min
- **Tasks:** 3
- **Files modified:** 15
- **Full-corpus lane runs:** 5 (≈45 min of lane time)

## The floor: three runs of record, five runs taken

`N = 392`, taken from `biting: 392` — never from the plan's prediction, which happened to agree.

| Run | Floor | Exit | Wall | Verdict |
|---|---|---|---|---|
| RUN 1 | 384 | **0** | 546.3 s | measurement: `arms: 392/392/0`, `biting: 392`, `lane-invocations: 392`, `✅ No defects` |
| RUN 2 | 393 | **1** | 547.4 s | ⚠️ **contaminated, kept** — 2 defects: the floor regression AND `dirty-checkout` from this executor's own mid-run edit |
| RUN 2b | 393 | **1** | 537.8 s | upper separation, clean: **exactly one** defect |
| RUN 3 | 392 | **1** | 542.6 s | ⚠️ **contaminated, kept** — zero arm defects; `dirty-checkout` from a CONCURRENT session's `.planning` commits |
| RUN 3b | 392 | **0** | 539.2 s | the pin, clean: `✅ No defects` at the final bytes |

**Both directions, verbatim:**

- upper — `ARMS_FLOOR regression: 392 biting arm(s) < floor 393`
- lower, observed at 384 BEFORE the pin — `The corpus declares 392 twin(s) of which 0 are waivers, so a green run bites 392. ARMS_FLOOR is 384. Update the floor in scripts/mutation-runner/run.mjs from a MEASURED run, never from this number.`

The two layers behaved exactly as designed: at 384 the **runner exits 0** and it is `mutation-runner-floors.test.ts` that fails. A floor below the corpus is invisible to the runner by construction.

**The eight new twins**, measured against `main` rather than read from the plan:

| Gate | Arms | Migration |
|---|---|---|
| `test_analytics_service_settings_and_vault_tick.sql` | V2, G1 | `20260911120000` |
| `test_ledger_refresh_fanout.sql` | S1, M1, M2 | `20260911130000` |
| `test_ledger_refresh_composite_arm.sql` | S1, M1, M2 | `20260911130000` |

`FILES_FLOOR` stays **46** (`coverage: files 46/73`), `WAIVED_CEILING` stays **0**, waivers **0**. Across the whole branch, `scripts/mutation-runner/run.mjs` has **exactly one** non-comment changed line.

**Ceiling check:** 392 arms × 1.1 s = 431 s of lane time; slowest full run 546 s = 9.1 min against the declared **20-minute** ceiling on `sql-mutation`. ~2.2× headroom. The ceiling is not raised; a future crossing is `[REDUNDER-SUBSET-SPLIT]`.

## Seven stale pins — one named by the plan, six not

The plan named **one** file. The corpus turns out to be mirrored in **seven** places. Each was found by RUNNING, never by reasoning.

| # | Artifact | Pin | Named by the plan? | Found by |
|---|---|---|---|---|
| 1 | `mutation-annotation-parser.test.ts` | `armsSeen` 384→392 | ✅ yes | file-scoped run |
| 2 | same | `stepsSeen` 406→**411** | ✅ yes | ran only after #1 was unblocked |
| 3 | same | `needles.length` 406→**411** | ✅ yes | file-scoped run |
| 4 | `mutation-runner-floors.test.ts` | `totalAnchored` 384→392 | ❌ **no** | task-1's own verify |
| 5 | same | `GREEN_LOG` fixture + **every RED-arm regex** | ❌ **no** | after #4 was fixed |
| 6 | `gate-family-meta.test.ts` | `KNOWN_THRESHOLD_SITES` by VALUE | ❌ **no** | full `test:coverage` |
| 7 | `.github/workflows/ci.yml` + `drift-check-scripts.test.ts` | sentinel `ARMS_FLOOR` 195→**203** | ❌ **no** | full `test:coverage` |

Three things here are worth more than the counts:

- **The brief predicted `expected 411 to be 406` was `stepsSeen`. It was `needles.length`.** They are different derivations pinned at the same number; `stepsSeen` only surfaced once `armsSeen` stopped failing first. All three were then taken from one scan: `arms=392 waivers=0 fileSteps=411 sqlSteps=116 totalSteps=527 needles=411`.
- **Pin #5 is the dangerous one.** The `GREEN_LOG` RED arms mutate the fixture with `.replace(/^arms: 384\/384\/0 /m, …)`. Once the fixture moved to 392 those patterns stopped matching, so each "RED" arm ran against an **unmutated** log — a neuter that does not apply reads as GREEN. The `.not.toBe(GREEN_LOG)` assertions beside them are what caught it, and they are the reason the class was visible at all.
- **Pin #7 is a second, independent floor.** `ci.yml`'s sentinel `ARMS_FLOOR` counts `ALL N ARMS EXECUTED` sentinels, not RED-UNDER-M twins — and it moved by the same **+8**. Two different countings agreeing is evidence each new arm got *both* a sentinel and a biting twin.

## Quick gates — every command, its exit code, at `HEAD 6a1b8118`

Run under `bash` (the repo's verify commands are GNU/bash-shaped; `PIPESTATUS` is empty under zsh, which silently blanked the first attempt's exit codes).

| Command | Exit | Output |
|---|---|---|
| `lint-sql-gates.mjs --self-test` | **0** | `lint-sql-gates self-test OK: 7 rules, red+green each.` |
| `lint-sql-gates.mjs` | **0** | `scanned 73 file(s); 0 finding(s), 0 measure-fail(s), 0 allowlist error(s).` |
| `lint-app-guc.mjs --self-test` | **0** | `4 finding kinds, red+green each (11 red, 2 green fixtures…)` |
| `lint-app-guc.mjs` | **0** | `files scanned 294` · `annotated (lineage) 5` · `findings 0` |
| `run.mjs --parse-only` | **0** | `✅ No static defects.` |
| `schema:functions:check` | **0** | `snapshot is current (120 functions)` · `DRIFT-05 gate (a): 120 vs 120 — agree` |
| `verify-plan-anchors.mjs --pending` | **0** | `claims: 122 checked` · `OK: 7 plan file(s), no stale claims.` (**was exit 1**) |
| `npm run lint` | **0** | `6291 tracked files scanned, none carry the local username or an absolute home path` (**was exit 1**) |
| `npm run typecheck` | **0** | `tsc --noEmit` clean |
| the six-file vitest set | **0** | `6 passed` / `520 tests` |
| `npm run test:coverage` | **0** | **853 files passed, 19 skipped · 14752 passed, 280 skipped · 0 failed** |

Full chain, run verbatim: `QUICK-GATES-GREEN`, exit **0**.

**Acceptance on the branch diff:** neither `supabase-migrate.yml` nor `scripts/lint-app-guc.mjs` appears; no non-comment `WAIVED_CEILING` line changed.

### Two gates were RED on arrival — neither is this plan's doing

**`npm run lint` — 46 violations (23 LOCAL-USERNAME + the same 23 as ABSOLUTE-HOME-PATH).** **21 of 23 pre-date this plan**: every PLAN.md of this phase, 01 through 08, opened its `<automated>` verify with `cd <absolute home path> && `. The repo is **public** and `.planning/` is **tracked**, so each was a world-readable local identifier (Phase 163 SEC-02). The gate itself prescribes the fix for an in-repo path — rewrite it repo-relative — so the prefix was deleted and every command is otherwise **byte-identical** and still runnable from the repo root. The remaining 2 are this plan's own: the runner prints the absolute scope dir in its defect table, so only the *username segment* in `164.8.6-05-FLOORS.log` was redacted to `<user>`, leaving the lines otherwise verbatim. Fixed as a **whole class**, not point-fixed on the two files this plan touched.

**`plan-anchor-verify --pending` — 6 stale claims, two causes.** Three were **this plan invalidating its own anchors by executing** (the CURRENCY block moved `ARMS_FLOOR` :1768→:1866 and `WAIVED_CEILING` :1882→:1980). Three were plans 07/08's TODOS.md ranges, shifted by the concurrent session's `[STRATTABLE-DESC-01]` commit; re-measured against the real entry spans.

## Anchor drift the brief warned about — measured

| Anchor | Plan/RESEARCH says | Measured | Verdict |
|---|---|---|---|
| `FILES_FLOOR` | `run.mjs:877` | **877** | resolves |
| `ARMS_FLOOR` | `run.mjs:1768` | **1768** (pre-edit) → **1866** after | resolved, then moved by this plan |
| `WAIVED_CEILING` | `run.mjs:1882` | **1882** → **1980** after | same |
| `sql-mutation` job / timeout | plan `:1196` / `:1386`; **CLAUDE.md + RESEARCH §Q6 say `:1259`/`:1069`** | **1196** / **1386** | plan correct, **CLAUDE.md and RESEARCH stale** — `[164.7-CITATION-DRIFT-01]` |
| parser-test CURRENCY | `:745-810`, `:1575-1590` | `armsSeen` decl at **689**, expect at **809**; needles expect at **1586** | ranges approximate; corrected in-place |

## Reviewers

**sha reviewed: `6a1b8118`** (SQL diff `main...HEAD -- supabase/ scripts/pg-lane/ scripts/mutation-runner/`, plus both new migrations in full). **`gh pr list --head phase-164.8.6-vaulttickfix --state all` is EMPTY** — criterion 6's ordering holds.

### ⛔ A gap in the mechanism, stated plainly

**The three reviewers were NOT dispatched as independent subagents.** This executor's context exposes no `Task`/`Agent` tool, so the plan's `model="opus"` subagent dispatch was impossible. Instead each agent's **shipped rubric was read from disk and executed inline** (`.claude/agents/migration-reviewer.md`, 21 invariants; `.claude/agents/rls-policy-auditor.md`; and the silent-failure-hunter's class, taken from the 164.8.5 doctrine in `STATE.md`).

What this does and does not buy:
- ✅ **Ordering** — the reviews ran before any PR exists, which is what criterion 6 actually constrains.
- ✅ **Rubric coverage** — every invariant was applied, and every disposition below rests on a command, not an impression.
- ❌ **Independence** — three fresh contexts would each re-derive the diff without this executor's priors. One context reviewing work it just committed is weaker evidence, and the founder's rule puts reviewers on Opus *for that independence*.

**Recommendation for plan 06:** re-dispatch the three agents properly from the orchestrator (which does have `Task`) before opening the PR. Nothing below should be treated as a substitute for that.

### 1. migration-reviewer (rubric: `.claude/agents/migration-reviewer.md`, 21 invariants)

`Findings: 0 CRITICAL, 0 HIGH, 0 MEDIUM, 0 LOW above threshold.`

| Invariant | Measurement | Disposition |
|---|---|---|
| 1 filename convention | both new files match `^[0-9]{14}_[a-z0-9_]+\.sql$` | PASS |
| 2 backdated guard | `20260911*` > remote tip `20260908120000` | PASS |
| 3 SECDEF `search_path` | 3 real `CREATE FUNCTION`, each with `SECURITY DEFINER` + `SET search_path = public, pg_catalog` (the apparent 4th at `:1007` is a comment) | PASS |
| 5 / 18 CONCURRENTLY | 0 occurrences in both files | N/A |
| 6 23502 timebomb | no `ALTER TABLE` / `ADD COLUMN` / `SET NOT NULL` | N/A |
| **11 edit of an applied migration** | `20260907130000` **is** modified — but **20/20 changed lines are comments, 939 → 939 lines, ZERO non-comment changed lines** | **REFUTED by measurement** (see the criterion-5 note below) |
| 14 BEGIN/COMMIT | both files carry top-level `BEGIN;`/`COMMIT;` | **REFUTED by measurement** — **180 of 268** migrations and **11 of the last 12** use it. Project convention; per "surface conflicts, don't average them", the convention wins and the rubric's generic advice is overridden explicitly. |
| 16 LLM template artifacts | `grep -cE '</?(content\|invoke\|function_calls\|antml:\|parameter)'` = **0** in all three files | PASS |
| **17 trigger-on-SECDEF REVOKE breaker** | `REVOKE ALL … FROM PUBLIC, anon, authenticated, service_role` on 3 live SECDEF functions. The predecessors revoked from **three** roles — **`service_role` is revoked here for the first time** | **REFUTED by measurement** — see below |
| 19 EXECUTE-to-authenticated probe-oracle | **0** `GRANT EXECUTE … TO authenticated/anon/PUBLIC` | PASS |
| 21 RAISE format single literal | awk scan for `\|\|` in the format slot: **0 hits** in both files | PASS |

**Invariant 17 in full, because it was the one real production-breaker candidate.** Revoking `service_role` EXECUTE from a live function breaks every caller in that role with `42501`, and these migrations auto-apply to PROD on merge. Four measurements clear it:

1. No callers in `src/`, `analytics-service/` or `supabase/functions/` — only test files and comments.
2. No `CREATE TRIGGER … EXECUTE FUNCTION` reaches any of the three.
3. The only production invoker is pg_cron, and `scripts/prod-prober/cron-manifest.json` shows **`match_engine_cron` (jobid 1) runs as `username: postgres`** — as do all 14 jobs. `postgres` owns these functions, so the ACL does not gate it.
4. The REVOKE is the *point*: `164.7-WR02-SERVICE-ROLE-EXECUTE`, and arm **G1** exists to turn the gate RED if `service_role` EXECUTE is handed back.

**Project-specific check the rubric does not carry — `[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`.** A data-reading `DO` block that `RAISE`s on an unexpected count applies to PROD and **refuses on the empty shared TEST**, and a failed TEST apply blocks the PROD apply. Both files' `DO $verify$` blocks (173 and 235 lines, 12 and 13 `RAISE EXCEPTION`s) read **`pg_proc`, `pg_roles`, `pg_get_functiondef`, `unnest(proconfig)`, `aclexplode`** and **no `public.*` data table**. Catalogue-only, so an empty TEST cannot make them refuse. ⚠️ Recorded because it nearly fooled this pass: the blocks open `DO $verify$`, not `DO $$`, so the first scan returned a **false clean of 0 DO blocks**.

⚠️ **Criterion 5 remains OPEN and is not this plan's to close.** The branch ships a comment-only edit to the applied migration `20260907130000`. Criterion 5 requires a *blocking measurement* — does a comment-only edit change what `supabase-migrate`'s plan job plans? — before such an edit is acceptable, with "annotate BESIDE the file" as the fallback. The edit is **provably comment-only**, but that measurement is a different question and is owned elsewhere in the phase. **Plan 06 must not open the PR without it.**

### 2. rls-policy-auditor (rubric: `.claude/agents/rls-policy-auditor.md`)

`Findings: 0 CRITICAL, 0 HIGH, 0 MEDIUM, 0 LOW above threshold.`

Surface scan of both migrations: `CREATE POLICY` **0**, `DROP POLICY` **0**, `CREATE VIEW` **0**, `CREATE TABLE` **0**, `ENABLE ROW LEVEL SECURITY` **0**, `USING (true)`/`WITH CHECK (true)` **0**, `request.jwt.claims` **0**. The single `auth.role()` hit is inside a comment. So the whole no-op-policy / recursion / asymmetric-USING / `security_invoker` family is **not applicable** — there is no policy surface.

The one RLS-adjacent dependency is real and is handled well: **both fan-out bodies now write `public.cron_runs`, an RLS-enabled table.** Three measurements:

- The migration **asserts the owner is RLS-exempt and fails LOUD** if not: `IF NOT (COALESCE(v_bypass, FALSE) OR COALESCE(v_super, FALSE)) THEN RAISE EXCEPTION …`. The predicate is `rolsuper OR rolbypassrls` and the file explains why `rolbypassrls` alone is wrong (superuser implies bypass without carrying the explicit attribute). The failure text names the consequence — "both fail CLOSED, silently, which is byte-identical to *nothing was stale*".
- `IF v_owner IS NULL THEN RAISE` — an unresolvable owner is **refused**, not read as a pass.
- The lane fixture `33-fixture-cron-runs.sql` reproduces the table with **exact 7-column parity** and `ALTER TABLE cron_runs ENABLE ROW LEVEL SECURITY`, but without the two production policies. RLS-enabled-with-no-policies denies every non-exempt role, so the lane is **stricter** than production — a conservative approximation, and the divergence is derived in the fixture's own header.

The tick migration carries the same BYPASSRLS reasoning about reading the RLS-enabled settings table as its definer, and RAISEs with "A true-looking error about a false cause".

### 3. silent-failure-hunter (class: the 164.8.5 doctrine — *a guard that refuses must be ADDITIVE, never SUBSTITUTIVE*)

`Findings: 0 CRITICAL, 0 HIGH, 1 MEDIUM (conf 8, pre-existing, NOT fixed — routed).`

**Six `EXCEPTION WHEN OTHERS` handlers** in the ledger migration, every one read in full. None swallows:

| Lines | Shape | Verdict |
|---|---|---|
| 304 / 636 | flag-read failure → `RAISE WARNING … SQLSTATE %`, `v_enabled := NULL`, `v_read_failed := TRUE` | **ADDITIVE** — the cause is recorded here and **written as a counted `cron_runs` row** on the dormant path. Assignment only; the file cites lint rule **R1-exception-handler-probe** forbidding DML/`SELECT INTO` in a handler. |
| 514 / 852 | per-candidate failure → `RAISE WARNING … SQLSTATE %`, continue | Documented (one poisoned row must not abort the tick); carries SQLSTATE but **deliberately no identifier** (T-161.1-10/19). |
| 524 / 862 | outer → `PERFORM pg_advisory_unlock(...)` **then bare `RAISE;`** | Correct, and the file names it "the part authors drop" — releasing before re-raising, or the session holds the lock and every later tick skips. |

**Empty-reads-as-pass shapes checked:** `string_agg` over the grantee set returns NULL on empty, and **both** migrations refuse it explicitly — *"could not read the EXECUTE grantee set … an empty answer here is indistinguishable from a locked-down one unless it is refused"*. That is the doctrine applied at exactly the right place.

**Gate-side anti-vacuity is machine-proven**: 392/392 arms `RED (identity ok)` with **zero** `no-red` / `wrong-first-failure` / `occurrence-mismatch` defects. That is also the proof of the briefed "needles disjoint from gate finds" constraint — a needle colliding with a `find` would surface as `wrong-first-failure`, and the gate files document the disjointness where it is subtle (M1's cause literal vs the migration's concatenated `INSERT INTO public.` ‖ `cron_runs` needle). Arm **0** additionally distinguishes "instrument broken" from "migration not applied" (cause iv), so M1/M2's count-of-0 cannot be misread. `lint-sql-gates` (rule R1 included) is at **0 findings over 73 files**.

**MEDIUM (conf 8) — NOT FIXED, routed.** *A tick where every candidate fails to enqueue is indistinguishable from a tick with no work.* Candidate failures at `:514`/`:852` are warned but **not counted**, and write **no** `cron_runs` row — only the dormant path does (`:339`, `:671`). Ten failures therefore report `enqueued 0`, byte-identical to "nothing to do". This is the same class as `164.7-DORMANCY-UNINSTRUMENTED` for a different cause.

**Why it is not fixed here:** it is **inherited, not introduced** — `20260907130000` carries the shape twice and `20260825130000` once; this migration carries it forward unchanged. That places it outside this task's scope boundary. It is also not user-facing and not data-integrity (nothing is corrupted; work silently does not happen), so under the review policy it does not block. Fixing it would add a counted row → a new arm → the floor moves again, at the end of a gate-locked phase.

⚠️ **This needs a phase, and this executor must not assign one** (`ROADMAP.md` is out of bounds for this plan, and every deferral must be entered via `/gsd-phase --edit`, never hand-edited). **Handing it to the orchestrator to route.**

## Task Commits

1. **Task 1: pin `ARMS_FLOOR` at the measured 392** — `66b9f86c` (test)
2. **Task 2 (part 1): green the two repo-wide gates** — `6b4005a8` (fix)
3. **Task 2 (part 2) + Task 3: carry the +8 through every corpus mirror** — `6a1b8118` (test)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Four unnamed stale pins blocked the plan's own acceptance criteria**
- **Found during:** Tasks 1 and 2
- **Issue:** `floors.test.ts:499` `totalAnchored`, its `GREEN_LOG` fixture and RED-arm regexes, `gate-family-meta.test.ts`'s `KNOWN_THRESHOLD_SITES`, and `ci.yml`'s sentinel derivation table + its exact-literal mirror all pin the corpus at 384/195. The plan named none of them, yet its verifies require them green.
- **Fix:** re-pinned each from the same measurement, with a dated block in each file's own style.
- **Verification:** `npm run test:coverage` exit 0 (853 files, 14752 tests).
- **Committed in:** `66b9f86c`, `6a1b8118`

**2. [Rule 2 - Missing Critical / Security] 46 planning-hygiene violations, 21 pre-existing**
- **Found during:** Task 2 (`npm run lint`)
- **Issue:** every PLAN.md of this phase leaked an absolute home path on a **public** repo with `.planning/` tracked (Phase 163 SEC-02).
- **Fix:** rewritten repo-relative exactly as the gate prescribes; commands otherwise byte-identical. The log's 2 occurrences had only the username segment redacted.
- **Verification:** `6291 tracked files scanned, none carry the local username or an absolute home path`.
- **Committed in:** `6b4005a8`

**3. [Rule 3 - Blocking] 6 stale plan anchors, 3 self-inflicted and 3 from a concurrent session**
- **Fix:** all six re-measured and corrected. **Committed in:** `6b4005a8`

**4. [Rule 1 - Bug] A real-corpus test timing out in the full suite only**
- **Issue:** the identity walk took **6330 ms** against vitest's 5000 ms default inside `test:coverage`, while passing file-scoped in 2.19 s. Work is proportional to 392 arms × 411 steps; the budget is fixed.
- **Fix:** an explicit 30 s timeout with the measurement recorded beside it. **No assertion changed** — a harness budget, not a gate threshold. This is the suite's first per-test timeout, so it is flagged as a deliberate new shape.
- **Committed in:** `6a1b8118`

**5. [Rule 1 - Bug] A CI comment turned a job-coverage arm red**
- **Issue:** the ci.yml note spelled `scripts/mutation-runner/run.mjs` verbatim; `lint-sql-gates.test.ts` matches those paths as substrings against each job body, so a **mention** inside `sql-tests` made it look like a mutation-runner job.
- **Fix:** the comment yielded, not the test — the arm is right that a job running these scripts must be pinned. A ⛔ note now warns the next author.
- **Committed in:** `6a1b8118`

---

**Total deviations:** 5 auto-fixed (2 blocking, 2 bugs, 1 missing-critical/security).
**Impact:** no scope creep into the SQL corpus — **zero bytes** changed under `supabase/`, `scripts/pg-lane/` or `scripts/mutation-runner/` since the floor commit, so RUN 3b still measures the final corpus. Every fix was at the source; no allowlist widened, no `DETECT_RE` relaxed, no waiver added, `supabase-migrate.yml` untouched.

## Issues Encountered

### A. A concurrent session committed onto this branch mid-run — 9 minutes of lane time lost

During RUN 3 another session committed `9458e152` and `a0867c41`, moving HEAD `48b91f34` → `a0867c41`. A mutation run asserts the working tree is unchanged across its own execution, so **even a `.planning`-only commit turns a green full-corpus run into exit 1**. The measurement survives, and that is proven rather than assumed: `git diff --stat 48b91f34..a0867c41 -- supabase/ scripts/ src/` is **EMPTY**.

### B. This executor contaminated RUN 2 itself

Writing a CURRENCY block while RUN 2 was in flight tripped the same assertion. Both contaminated runs are **kept in the log** — deleting them would hide that the clean-tree assertion works.

### C. `PIPESTATUS` is empty under zsh

The first quick-gate pass reported **blank** exit codes; a later `| tail -6` masked a vitest failure and printed `FLOORS-PINNED` over a **red** test. Both re-run under `bash` with exit codes preserved. The plan's own verify chains are correct as written; the corruption was in this executor's wrapping.

### D. Two open items this plan does not close

1. **Criterion 5** (the comment-only-edit measurement) — blocking for plan 06's PR.
2. **The MEDIUM silent-failure finding** — needs a phase via `/gsd-phase --edit`.

### E. Stale citations found

`CLAUDE.md` and `164.8.6-RESEARCH.md` §Q6 both cite `ci.yml:1259`/`:1069` for the `sql-mutation` timeout; measured today it is the `sql-mutation:` job key at **:1196** and `timeout-minutes: 20` at **:1386** — the `[164.7-CITATION-DRIFT-01]` class.

## User Setup Required

None.

## Next Phase Readiness

**Ready for plan 06 (the PR), with three preconditions stated rather than assumed:**

1. ⛔ **Re-dispatch the three reviewers as independent subagents.** Ordering is satisfied; independence is not.
2. ⛔ **Criterion 5's blocking measurement must be taken** before a PR carrying a comment-only edit to the applied `20260907130000`.
3. ⚠️ **Route the MEDIUM silent-failure finding** to a phase via `/gsd-phase --edit`.

Also carried forward for the PR body: **V2 is RED on shared TEST before merge by construction** (the old body is live until the migration applies) — the documented `[164.8-PUSH-RACE-VAC08]` coupling (b). Expect one red `sql-tests` on the PR.

**State at `6a1b8118`:** corpus **392 annotations / 411 steps**, all three gates biting (13/20/20), zero waivers, zero defects; every quick gate and the full suite green; **no PR exists**.

## Self-Check: PASSED

- `164.8.6-05-FLOORS.log` exists, 2835 lines, 5 runs — FOUND
- `164.8.6-05-SUMMARY.md` — FOUND
- `66b9f86c`, `6b4005a8`, `6a1b8118` — all FOUND in `git log`
- `export const ARMS_FLOOR = 392;` / `FILES_FLOOR = 46;` / `WAIVED_CEILING = 0;` — all confirmed
- `gh pr list --head phase-164.8.6-vaulttickfix --state all` — EMPTY

---
*Phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned*
*Completed: 2026-09-11*
