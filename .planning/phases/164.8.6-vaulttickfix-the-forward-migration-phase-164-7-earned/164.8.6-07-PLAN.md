---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 07
type: execute
wave: 6
depends_on:
  - 164.8.6-06
files_modified:
  - scripts/prod-prober/arms/cron-drift.mjs
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: true
requirements:
  - 164.8.5-MANIFEST-SIDE-LOOP-DEAD
tags: [prod-prober, javascript, vitest, hygiene]

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`compareManifest`'s MANIFEST-side hygiene loop runs ABOVE every early return — directly after section (0)'s PROD-side loop and before the `:2244` empty-marker return — so a credential in committed manifest text is reported even when the manifest is `manifest-invalid` (normalization or schema_version drift) or the database marker mismatches — TODOS.md:1571-1600, 164.8.5-MANIFEST-SIDE-LOOP-DEAD"
    - "Three separate vitest cases, one lever each (normalization `ws-collapse-v1`, `schema_version` bump, liveMarker mismatch), each assert BOTH the early-return defect AND `cron-secret-in-command` with `subject === 'manifest:match_engine_cron'`, and no defect detail contains the fixture credential; a RED control (pre-hoist file swapped in by `cp`) shows all three failing"
    - "The SUMMARY records: the loop was RE-ANIMATED by PR #776 (`3412f3f9`, run 34611594511, `cron-manifest.json:3` = `ws-collapse-v2`) before this phase; 164.8.6 made it un-skippable by ordering; 164.5.1's 'no capture tool today' text is stale (`captureManifest`, scripts/prod-prober/run.mjs:4217)"
  artifacts:
    - "scripts/prod-prober/arms/cron-drift.mjs (section (2) hoisted; MEASURED comment)"
    - "src/__tests__/prod-prober-wiring.test.ts (three new `it()` cases in a `[164.8.6-07]` describe)"
  key_links:
    - "The hoisted loop iterates `Array.isArray(manifest?.jobs) ? manifest.jobs : []` and judges only rows with `typeof row.command === 'string'` — it must not assume the shape checks (:2249-2340) have run"
    - "(1a)'s sha binding (:2312-2325) stays where it is; a hygiene finding on text whose sha does not match is still a finding about committed text"
    - "`manifestHygiene` stays declared before its consumers (the withholding logic, `hygieneWithholds`)"
---

<objective>
JS WAVE, PART 1 — separate from the migration wave by founder decision (ROADMAP: "Plan them as a separate wave … do not let the migration's discipline be read as applying to them"). No `supabase/**` path is touched; no reviewer trio, no TEST-before-PROD, no marker query applies. This wave ships as its OWN PR after the SQL PR lands (one PR at a time).

Hoist the manifest-side hygiene loop above `compareManifest`'s early returns so the check that PR #776 silently re-animated cannot be skipped again by the next oracle drift.

Purpose: TODOS.md:1571-1600 (`164.8.5-MANIFEST-SIDE-LOOP-DEAD`, the ORDERING half — 164.5.1 owns the re-capture half; whichever lands first says so, and this phase is first).
Output: hoisted loop + three red-controlled vitest cases.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-RESEARCH.md
@.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md
@scripts/prod-prober/arms/cron-drift.mjs
@src/__tests__/prod-prober-wiring.test.ts
@scripts/prod-prober/fixtures/cron-drift/manifest-inline-key.json
</context>

<interfaces>
Consumed: `compareManifest(manifest, prodRows, opts)` (`cron-drift.mjs:2085`, `export function compareManifest`); `hygieneWithholds` (exported, test :52); fixtures `manifest.json`, `prod-ok.json`, `manifest-inline-key.json` under `scripts/prod-prober/fixtures/cron-drift/`; the self-test rule that no detail may contain the fixture credential (`scripts/prod-prober/run.mjs:1594-1597`, `FAKE-inline-key`).
Produced: describe block `[164.8.6-07] manifest-side hygiene survives every early return` with three `it()`s; a `// (2) MANIFEST-SIDE HYGIENE, ABOVE EVERY return` comment block carrying a MEASURED line.
Not produced: a new `--self-test` scenario (the vitest cases carry the red control; `SELF_TEST_SCENARIOS` = 78 and its pin at `prod-prober-wiring.test.ts:1774` stay untouched — if a scenario IS added later, both move in one commit).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Hoist section (2) — the manifest-side loop — to directly after section (0), before the first return</name>
  <files>scripts/prod-prober/arms/cron-drift.mjs</files>
  <read_first>
    - scripts/prod-prober/arms/cron-drift.mjs:2085-2110 (`compareManifest` head; section (0)'s placement rule "ABOVE EVERY return … MEASURED on the reverted repair 84b21cb5"), :2194-2233 (the PROD-side loop body — the shape section (2) already mirrors), :2229 ((0b) begins), :2244 (first return: empty live marker), :2249-2340 (`invalid()` and the eight shape checks), :2312-2325 ((1a) sha binding — stays), :2342-2346, :2366-2373 (more returns), :2373-2408 (section (2), the loop to MOVE — keep its body and its comment block, :2381 `if (typeof row.command !== "string") continue;`)
    - scripts/prod-prober/run.mjs:1560-1600 (the inline-key self-test scenario that asserts `manifest:match_engine_cron` is reported — must stay green)
    - src/__tests__/prod-prober-wiring.test.ts:1864 and :1936-1945 (`hygieneWithholds` is ONE predicate called twice — `manifestHygiene` must remain populated for the withholding logic)
  </read_first>
  <action>
    Before editing, `cp scripts/prod-prober/arms/cron-drift.mjs "$SCRATCH/cron-drift.prehoist.mjs"` (task 2's RED control needs the pre-hoist bytes; a later `git show` is not equivalent once this task is committed). Then MOVE section (2) (`:2373-2408`, the `manifestHygiene` map declaration + the `for` loop + its comment block) to immediately after section (0)'s loop ends (`:2227`) and before (0b) (`:2229`). Change the loop's input to `Array.isArray(manifest?.jobs) ? manifest.jobs : []` (the shape checks have NOT run yet at this position — a non-array `jobs` must not throw) and keep `if (typeof row.command !== "string") continue;` as the per-row guard; keep `judgeRow`/`recordVerdict`/`splitHygiene`/`partialCredentialDefect` calls exactly as they are, `subject: \`manifest:${row.jobname}\``. Leave (1a)'s sha binding at :2312-2325 in place. Leave nothing of section (2) at its old position (no duplicate loop, no second `manifestHygiene` declaration). Extend the moved comment block in section (0)'s style with a MEASURED line: "MEASURED 2026-09-11 (TODOS 164.8.5-MANIFEST-SIDE-LOOP-DEAD): with the loop below the `:2244`/`invalid()`/marker returns, `normalization ws-collapse-v1 -> 0 manifest-side credential defects`; PR #776's re-capture (`ws-collapse-v2`) re-animated it as a side effect — this position makes it un-skippable." Confirm `node scripts/prod-prober/run.mjs --self-test` stays green (78 scenarios) — no scenario is added by this task.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && F=scripts/prod-prober/arms/cron-drift.mjs && L2="$(grep -nF 'const manifestHygiene = new Map' "$F" | head -1 | cut -d: -f1)" && LR="$(awk 'NR>2085 && /^  if \(.*liveMarker/ {print NR; exit}' "$F")" && LP="$(grep -nF 'const prodHygiene = new Map' "$F" | head -1 | cut -d: -f1)" && test -n "$L2" && test -n "$LR" && test -n "$LP" && test "$L2" -gt "$LP" && test "$L2" -lt "$LR" && test "$(grep -cF 'const manifestHygiene = new Map' "$F")" = 1 && grep -qF 'Array.isArray(manifest?.jobs) ? manifest.jobs : []' "$F" && grep -qF '164.8.5-MANIFEST-SIDE-LOOP-DEAD' "$F" && node scripts/prod-prober/run.mjs --self-test && npx vitest run src/__tests__/prod-prober-wiring.test.ts && echo HOIST-OK</automated>
    <fails_when>`manifestHygiene` is declared more than once or its line number is not between the PROD-side map declaration and the first `liveMarker` return (the loop is not hoisted); the shape-safe iteration expression or the MEASURED tag is missing; the prober self-test or the wiring test exits non-zero; or `HOIST-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - Exactly one `const manifestHygiene = new Map` in the file, positioned after `const prodHygiene = new Map` and before the first `liveMarker` return inside `compareManifest`.
    - The loop iterates `Array.isArray(manifest?.jobs) ? manifest.jobs : []` and keeps the `typeof row.command !== "string"` guard.
    - No loop over `manifest.jobs` remains below the marker-mismatch return (the old position is empty).
    - The comment block carries the MEASURED line naming the TODOS id and PR #776.
    - `node scripts/prod-prober/run.mjs --self-test` passes at 78 scenarios; `npx vitest run src/__tests__/prod-prober-wiring.test.ts` passes.
    - `$SCRATCH/cron-drift.prehoist.mjs` exists (byte copy taken before the edit).
  </acceptance_criteria>
  <done>The manifest-side loop is the second thing `compareManifest` does, before any return, and every existing control is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Three one-lever vitest cases + the RED control (pre-hoist file swapped in by cp, observed failing, restored, cmp)</name>
  <files>src/__tests__/prod-prober-wiring.test.ts</files>
  <read_first>
    - src/__tests__/prod-prober-wiring.test.ts:1789-1830 (the "one lever per test" rule at :1789-1796; the `[164.8.5-02]` describe, fixtures and `withoutField`/`invalidsFor` helpers), :1896 (existing manifest-side `measure-fail` subject assertion), :1945-1955 (the calibration closer)
    - scripts/prod-prober/fixtures/cron-drift/manifest-inline-key.json:1-16 (`"normalization": "ws-collapse-v2"`, `match_engine_cron` carries `FAKE-inline-key-…`)
    - scripts/prod-prober/run.mjs:1594-1597 (`FAKE-inline-key` must never appear in a detail) and scripts/prod-prober/run.mjs:1985-2022 (the `otherMarker` lever precedent)
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PATTERNS.md § prod-prober-wiring.test.ts (the exact case design)
  </read_first>
  <action>
    Add a describe `[164.8.6-07] manifest-side hygiene survives every early return (TODOS 164.8.5-MANIFEST-SIDE-LOOP-DEAD)` beside `[164.8.5-02]`, loading `manifest-inline-key.json` and `prod-ok.json` (PROD rows clean, so the ONLY credential finding is manifest-side). Deep-copy the manifest per test; ONE lever each, three `it()`s: (1) `normalization = "ws-collapse-v1"`; (2) `schema_version` bumped by one; (3) unchanged manifest but `liveMarker: "quantalyze-fixture-OTHER"`. Each asserts: the early-return defect is present (`manifest-invalid` for 1 and 2; `measure-fail` whose detail names the database marker for 3), AND `defects.some(d => d.kind === "cron-secret-in-command" && d.subject === "manifest:match_engine_cron")`, AND `defects.every(d => !String(d.detail).includes("FAKE-inline-key"))`. Add a vacuity guard per mutant (throw if the copy equals the original), and a calibration `it` proving the untouched inline-key pair still yields exactly the manifest-side finding and no early-return defect. Then the RED CONTROL, recorded in the SUMMARY with output: `cp scripts/prod-prober/arms/cron-drift.mjs "$SCRATCH/cron-drift.hoisted.mjs"`; `cp "$SCRATCH/cron-drift.prehoist.mjs" scripts/prod-prober/arms/cron-drift.mjs`; run `npx vitest run src/__tests__/prod-prober-wiring.test.ts -t "164.8.6-07"` — EXPECT the three lever tests FAILING (the calibration test still passes: that is the proof the lever, not the fixture, is what the hoist fixes); `cp "$SCRATCH/cron-drift.hoisted.mjs" scripts/prod-prober/arms/cron-drift.mjs`; `cmp` the two; re-run — GREEN. Never `git checkout --` for either swap.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && T=src/__tests__/prod-prober-wiring.test.ts && grep -qF '[164.8.6-07]' "$T" && test "$(awk '/\[164.8.6-07\]/,0' "$T" | grep -cE '^\s*it\(')" -ge 4 && grep -qF 'ws-collapse-v1' "$T" && grep -qF 'quantalyze-fixture-OTHER' "$T" && grep -qF '"manifest:match_engine_cron"' "$T" && grep -qF 'FAKE-inline-key' "$T" && npx vitest run "$T" -t "164.8.6-07" && S="${SCRATCH:?}" && test -f "$S/cron-drift.prehoist.mjs" && cp scripts/prod-prober/arms/cron-drift.mjs "$S/cron-drift.hoisted.mjs" && cp "$S/cron-drift.prehoist.mjs" scripts/prod-prober/arms/cron-drift.mjs && ( npx vitest run "$T" -t "164.8.6-07" >/dev/null 2>&1; RC=$?; cp "$S/cron-drift.hoisted.mjs" scripts/prod-prober/arms/cron-drift.mjs; cmp "$S/cron-drift.hoisted.mjs" scripts/prod-prober/arms/cron-drift.mjs && test "$RC" != 0 ) && npx vitest run "$T" -t "164.8.6-07" && echo RED-CONTROL-OK</automated>
    <fails_when>The describe or any lever literal is missing; fewer than 4 `it(` cases under it; the cases fail on the hoisted file; the pre-hoist copy is missing; the cases PASS with the pre-hoist file swapped in (RC 0 — the control cannot fail, which is the defect class this phase exists for); `cmp` shows the restore did not land; the final green run fails; or `RED-CONTROL-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - A `[164.8.6-07]` describe with three lever `it()`s + one calibration `it()`; each lever test asserts the early-return defect, the `manifest:match_engine_cron` credential finding, and the no-credential-in-detail rule.
    - With the pre-hoist file swapped in by `cp`, `npx vitest run … -t "164.8.6-07"` exits non-zero (three lever failures, calibration green) — pasted in the SUMMARY; after `cp` restore + `cmp`, it exits 0.
    - `SELF_TEST_SCENARIOS` (scripts/prod-prober/run.mjs:149) and its pin at `prod-prober-wiring.test.ts:1774` (`toBe(78)`) are unchanged.
    - SUMMARY states: re-animated by PR #776 on 2026-09-11 (`3412f3f9`, run 34611594511); 164.8.6 landed the ordering fix first; 164.5.1's "no capture tool" text is stale (`captureManifest` exists — scripts/prod-prober/run.mjs:4217, `export async function captureManifest`).
  </acceptance_criteria>
  <done>The ordering fix is covered by three controls that were each observed RED against the pre-hoist file and GREEN against the hoisted one.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 07's share)

- MODIFIED `scripts/prod-prober/arms/cron-drift.mjs` — section (2) manifest-side hygiene loop hoisted above every return; MEASURED comment.
- MODIFIED `src/__tests__/prod-prober-wiring.test.ts` — describe `[164.8.6-07] manifest-side hygiene survives every early return` (3 lever cases + calibration).
- SUMMARY `## Red control` (pre-hoist swap output) and `## Lineage` (PR #776 re-animation; 164.5.1 note).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed manifest text -> public Actions log | a credential in `cron-manifest.json` is repo text; the loop reports it as a subject, never as detail text |
| oracle state -> control liveness | oracle drift must not silence a credential check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-27 | Tampering (control integrity) | manifest-side loop skipped by an early return | high | mitigate | hoist above every return; three lever tests with a RED control |
| T-164.8.6-28 | Information Disclosure | credential text echoed in a defect detail | high | mitigate | every new test asserts no detail contains the fixture key (self-test rule scripts/prod-prober/run.mjs:1594-1597, `FAKE-inline-key`) |
| T-164.8.6-29 | Denial of Service | non-array `manifest.jobs` throwing before the shape checks | medium | mitigate | `Array.isArray(manifest?.jobs) ? … : []` + per-row `typeof` guard |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- `HOIST-OK` (task 1) and `RED-CONTROL-OK` (task 2) printed; prober self-test 78 scenarios green; full wiring test file green.
</verification>

<success_criteria>
- The manifest-side loop cannot be skipped by any early return; three one-lever controls proven able to fail.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-07-SUMMARY.md` when done.
</output>
