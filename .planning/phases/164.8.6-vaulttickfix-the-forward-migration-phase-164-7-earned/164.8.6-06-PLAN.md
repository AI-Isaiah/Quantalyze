---
phase: 164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned
plan: 06
type: execute
wave: 5
depends_on:
  - 164.8.6-05
files_modified:
  - VERSION
  - package.json
  - CHANGELOG.md
  - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PR-BODY.md
autonomous: false
requirements:
  - 164.7-CR05-VACUOUS-MIGRATION-CHECK
  - 164.7-WR01-VAULT-NOT-STRICT
  - 164.7-WR02-SERVICE-ROLE-EXECUTE
  - VAULTTICK-EMPTYKEY-01
  - 164.7-MIGRATION-COMMENT-DRIFT
  - APPGUC-WARNING-UNINSTRUMENTED-01
  - 164.7-DORMANCY-UNINSTRUMENTED
tags: [release, changelog, pull-request, one-way-door]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The release commit bumps VERSION and package.json to the SAME 4-digit string and writes ONE unified CHANGELOG entry built by the commit-checklist mechanism (every commit on the branch maps to a bullet) — CLAUDE.md § CHANGELOG discipline"
    - "The PR body states, in words: two migration files = one shipped repair (lane topology); the `20260907130000` comment fix rides this PR because a comment-only merge alone is red at supabase-migrate.yml:848-851 (`applied ZERO migrations`); V2, G1, S1 (x2), M1 (x2), M2 (x2) are RED on shared TEST BEFORE merge by construction (coupling (b)) and `sql-tests` may race `apply-test` on the merge push (coupling (a)); the founder approves the `Production` environment gate after `apply-test` is green; if `apply-test` refuses on TEST the remedy is to REVERT THE MERGE"
    - "A checkpoint:decision gates the PR opening because the merge is a ONE-WAY door: `supabase/migrations/**` auto-applies to PRODUCTION"
  artifacts:
    - "VERSION, package.json (byte-equal 4-digit version), CHANGELOG.md (unified entry)"
    - "164.8.6-PR-BODY.md (the PR body, committed in the phase dir)"
    - "The SQL PR number recorded in the SUMMARY"
  key_links:
    - "src/__tests__/critical-regressions.test.ts asserts VERSION == package.json version (never `npm version`)"
    - "migration-drift-check.yml (VAC-04) on the PR must report PROD hashes equal to the three earned acks; a mismatch means FOLD, not paste"
    - "The JS wave (plans 07-08) opens its OWN PR after this one lands — founder's one-PR-at-a-time rule"
---

<objective>
Turn the reviewed SQL tree into the release commit and the PR — behind a decision gate, because merging is the one irreversible step of this phase (auto-apply to PROD after shared TEST).

Purpose: ship criteria 1-7 through "this repo's full migration discipline, not around it" — VERSION/CHANGELOG mechanism, reviewers already done (plan 05), TEST-before-PROD by the workflow, human Production gate by the founder.
Output: release commit, PR opened, PR number in the SUMMARY.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
# read at execution when present (produced by an earlier plan of this phase): .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-05-SUMMARY.md
@CHANGELOG.md
@VERSION
@CLAUDE.md
</context>

<interfaces>
Consumed: everything plans 01-05 committed on the working branch. Produced: version string `0.77.34.0` (a phase = minor bump from `0.77.33.3`; decided here, not asked), CHANGELOG heading `## [0.77.34.0] - <date>`, PR body file, PR number.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Release commit — VERSION/package.json bump and the unified CHANGELOG entry via the commit checklist; write the PR body</name>
  <files>VERSION, package.json, CHANGELOG.md, .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PR-BODY.md</files>
  <read_first>
    - CLAUDE.md § CHANGELOG discipline (the six-step mechanism; section vocabulary `### Fixed/Added/Changed/Removed/Tests/Notes/Security/Root cause/Why`; entry BEFORE the push in the SAME commit as the VERSION bump; VERSION and package.json byte-equal 4-digit; never `npm version`)
    - CHANGELOG.md:1-80 (the newest entries — match their voice: infrastructure-facing, names phases and TODOS ids)
    - VERSION, package.json:82 (current `0.77.33.3`)
    - src/__tests__/critical-regressions.test.ts (the byte-equality assertion)
    - CLAUDE.md § Which database am I on / § Currency 2026-09-09 (the apply-test -> apply flow, couplings (a) and (b), the revert-the-merge remedy — all quoted in the PR body)
  </read_first>
  <action>
    1. CHECKLIST: `git log main..HEAD --oneline` — count the commits; `git diff main...HEAD --stat` — read the full diff. Group by theme (new capability / behaviour change / bug fix / infrastructure-tests / refactor). 2. Write ONE entry `## [0.77.34.0] - <today>` at the top of CHANGELOG.md using the repo's own sections: `### Fixed` (CR05 vacuous check replaced by statement-shape needles; WR01 cardinality guard; EMPTYKEY btrim; WR02 whole grantee set incl. service_role; MIGRATION-COMMENT-DRIFT reconciled), `### Added` (dormancy instrument rows for causes 1 and 3 — APPGUC-WARNING-UNINSTRUMENTED-01 + 164.7-DORMANCY-UNINSTRUMENTED; fixture 33; arms V2/G1/S1/M1/M2; ARMS_FLOOR N), `### Tests` (26 re-points, the three falsifiers, floors separated both ways), `### Notes` (two files = one repair; criterion 5 reading; pre-merge reds expected; the tab residual of btrim; the shape-only cardinality assertion; the JS wave ships separately as the next PR). CROSS-CHECK: every commit from step 1 maps to at least one bullet — an unmapped commit is a defect, not "minor". 3. Set VERSION to `0.77.34.0` and package.json `"version": "0.77.34.0"` by hand (byte-equal); never `npm version`. 4. Write `164.8.6-PR-BODY.md` in the phase directory carrying, in prose: the seven criteria and where each is satisfied (file:line); the two-files-one-repair note with the lane-topology reason; the criterion-5 reading and why the comment fix is bundled (cite .github/workflows/supabase-migrate.yml:848-851 and quote its `applied ZERO migrations` guard text verbatim); the list of arms that are RED on shared TEST before merge by construction with the CLAUDE.md coupling (b) citation and the (a) race note; the three earned ack hashes and the FOLD rule if VAC-04 disagrees; the merge flow (apply-test -> Production reviewer gate -> apply) and the ⛔ remedy (revert the merge; never edit supabase-migrate.yml); the falsifier results (three RED lines); the reviewer verdicts summary; and the footer `🤖 Generated with [Claude Code](https://claude.com/claude-code)`. 5. Commit as `chore(release): v0.77.34.0 — VAULTTICKFIX` (with the Co-Authored-By and Claude-Session trailers) containing exactly VERSION, package.json, CHANGELOG.md and the PR body file. Do NOT push yet — the decision gate comes first.
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && V="$(cat VERSION | tr -d '[:space:]')" && P="$(node -p "require('./package.json').version")" && test "$V" = "$P" && test "$V" = "0.77.34.0" && grep -qF '## [0.77.34.0]' CHANGELOG.md && grep -qF 'VAULTTICKFIX' CHANGELOG.md && grep -qF '164.7-CR05-VACUOUS-MIGRATION-CHECK' CHANGELOG.md && grep -qF 'APPGUC-WARNING-UNINSTRUMENTED-01' CHANGELOG.md && B=.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PR-BODY.md && test -f "$B" && grep -qF 'applied ZERO migrations' "$B" && grep -qiF 'revert the merge' "$B" && grep -qF 'prod-body-ack' "$B" && grep -qF 'Generated with [Claude Code]' "$B" && git log -1 --format=%s | grep -qF 'chore(release): v0.77.34.0' && npx vitest run src/__tests__/critical-regressions.test.ts && echo RELEASE-COMMIT-OK</automated>
    <fails_when>VERSION and package.json differ or are not `0.77.34.0`; the CHANGELOG lacks the `## [0.77.34.0]` heading or the named ids; the PR body lacks the `:848` guard citation, the revert remedy, the ack mention or the footer; HEAD's subject is not the release commit; `critical-regressions.test.ts` fails; or `RELEASE-COMMIT-OK` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `VERSION` == package.json `version` == `0.77.34.0` (byte-equal, 4 digits).
    - CHANGELOG top entry `## [0.77.34.0] - <date>` with `### Fixed`, `### Added`, `### Tests`, `### Notes`; the SUMMARY carries the commit-to-bullet cross-check table (N commits, all mapped).
    - `164.8.6-PR-BODY.md` exists with the seven listed elements; HEAD is `chore(release): v0.77.34.0 — VAULTTICKFIX`.
  </acceptance_criteria>
  <done>The release commit exists locally with a mechanism-built CHANGELOG entry and the PR body written; nothing is pushed.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: DECISION — open the SQL PR (the one-way door: merge auto-applies to PRODUCTION)</name>
  <decision>Authorise opening the SQL-wave PR against `main`. Merging it applies `20260911120000_vault_tick_hardening.sql` and `20260911130000_ledger_fanout_grantees_and_dormancy.sql` to shared TEST and then — behind your `Production` environment approval — to PROD. There is no rollback file; the reverse is a further forward migration.</decision>
  <context>
    Evidence to weigh (all in the plan SUMMARIES): plan 01/03 narrowed lane runs clean; plan 04 falsifiers RED three ways with the source intact; plan 05 full-corpus floor N pinned both ways, all quick gates green at sha <from 164.8.6-05-SUMMARY>, three reviewers with zero unresolved Critical/High. Expected on the PR: `migration-drift-check` (VAC-04) reports PROD hashes equal to the three earned acks (if not: FOLD, do not merge); `sql-tests` on shared TEST is RED for V2, G1, S1 x2, M1 x2, M2 x2 BY CONSTRUCTION until merge (CLAUDE.md coupling (b)) — that red is expected and documented in the PR body, not a defect. After merge: `apply-test` (TEST) must be green before you approve `Production`; if it refuses, REVERT THE MERGE — never edit `supabase-migrate.yml`.
  </context>
  <options>
    <option id="A">Open the PR from the working branch as-is (`.planning/` artifacts included — the repo is public and `.planning/` is tracked).</option>
    <option id="B">First create a `.planning`-filtered branch via `/gsd-pr-branch` (the `-pr` convention used for PRs #749/#774), then open the PR from it; the release commit's VERSION/package.json/CHANGELOG survive the filter.</option>
    <option id="C">Hold — name the evidence that is missing; the executor stops here.</option>
  </options>
  <recommendation>B — it matches the last three shipped phases and keeps the review diff to source files; A is acceptable if speed matters more than diff size. I'd take B.</recommendation>
  <resume-signal>Reply with A, B or C. On A or B, task 3 pushes and opens the PR; on C, the plan stops and the SUMMARY records the hold reason.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Push and open the SQL PR with the prepared body; record the PR number</name>
  <files>.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PR-BODY.md</files>
  <read_first>
    - .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PR-BODY.md
    - the founder's answer to task 2 (A or B)
  </read_first>
  <precondition>The task-2 decision is A or B (recorded in the SUMMARY); `git branch --show-current` is not `main`; `gh auth status` succeeds.</precondition>
  <action>
    On A: `git push -u origin <branch>` then `gh pr create --base main --head <branch> --title "VAULTTICKFIX (164.8.6): forward migrations — falsifiable check 6, single-row-safe Vault read, btrim guard, whole grantee set, dormancy instrument" --body-file <PR body path>`. On B: create the filtered branch per the `/gsd-pr-branch` convention first (the orchestrator runs that skill; the executor does not hand-roll the filter), verify `git diff <branch>..<filtered> -- ':!.planning' ` is empty (CODE IDENTICAL) and that VERSION/package.json/CHANGELOG.md are present on the filtered branch, then push and `gh pr create` from it. Record the PR number and URL in the SUMMARY, and paste the initial check board (`gh pr checks <n>`) noting which reds are the documented pre-merge reds. Do NOT merge, do NOT approve any environment, do NOT re-run `apply-test` — those are the founder's actions in the GitHub UI. Do NOT open the JS PR yet (plans 07-08 wait for this one to land).
  </action>
  <verify>
    <automated>cd /Users/helios-mammut/claude-projects/quantalyze && N="$(gh pr list --state open --search 'VAULTTICKFIX (164.8.6)' --json number --jq '.[0].number')" && test -n "$N" && gh pr view "$N" --json body --jq .body | grep -qF 'applied ZERO migrations' && gh pr view "$N" --json body --jq .body | grep -qF 'Generated with [Claude Code]' && grep -qF "PR #$N" .planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-06-SUMMARY.md && echo "SQL-PR-OPEN #$N"</automated>
    <fails_when>No open PR matches the title (`N` empty), the PR body lacks the `:848` citation or the footer, the SUMMARY does not record `PR #N`, or `SQL-PR-OPEN` is not printed.</fails_when>
  </verify>
  <acceptance_criteria>
    - An open PR against `main` with the VAULTTICKFIX title exists; its body equals `164.8.6-PR-BODY.md`.
    - The SUMMARY records `PR #N`, the URL, the head sha, and the initial `gh pr checks` board with the expected pre-merge reds annotated.
    - No merge, no environment approval, no JS PR performed by this task.
  </acceptance_criteria>
  <reversibility rating="one-way">Opening the PR is reversible (close it); MERGING it is not — `supabase/migrations/**` auto-applies to PROD via supabase-migrate.yml behind the founder's Production gate. The checkpoint:decision immediately before this task is the gate for that door.</reversibility>
  <done>The SQL PR is open with the prepared body; the founder owns merge, TEST verdict and Production approval from here.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces (plan 06's share)

- Release commit `chore(release): v0.77.34.0 — VAULTTICKFIX` (VERSION, package.json, CHANGELOG.md `## [0.77.34.0]`).
- NEW `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-PR-BODY.md`.
- The SQL PR (number recorded in `164.8.6-06-SUMMARY.md`).
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR merge -> shared TEST -> PROD | the only irreversible step; human-gated twice (checkpoint here, Production environment in GitHub) |
| public repo | PR body and `.planning/` are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.8.6-23 | Denial of Service (deploy) | `apply-test` refuses on TEST after merge, blocking PROD | high | mitigate | catalogue-only DO blocks (plans 01/02); PR body carries the revert-the-merge remedy |
| T-164.8.6-24 | Tampering | merging with a VAC-04 hash mismatch (PROD drifted) | high | mitigate | decision context: FOLD, do not merge; acks are earned not pasted |
| T-164.8.6-25 | Information Disclosure | secrets or body text in the PR body | medium | mitigate | body carries hashes, counts and file:line only |
| T-164.8.6-26 | Repudiation | a ship without a CHANGELOG entry | medium | mitigate | commit-checklist mechanism; verify greps the entry and ids |
| T-164.8.6-SC | Tampering | npm/pip/cargo installs | low | accept | none installed |
</threat_model>

<verification>
- `RELEASE-COMMIT-OK` (task 1), founder decision recorded (task 2), `SQL-PR-OPEN #N` (task 3).
</verification>

<success_criteria>
- Release commit with byte-equal version and a mechanism-built CHANGELOG; PR open with the documented expectations; the one-way door explicitly gated.
</success_criteria>

<output>
Create `.planning/phases/164.8.6-vaulttickfix-the-forward-migration-phase-164-7-earned/164.8.6-06-SUMMARY.md` when done (record the decision letter, PR #N, URL, head sha, check board).
</output>
