---
phase: 149-nav-my-strategies-a-ranking-at-discovery-parity
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.my-strategies.test.ts
  - src/components/ui/Badge.tsx
  - src/components/ui/Badge.test.tsx
autonomous: true
requirements: [NAV-01]
tags: [supabase-query, visibility-predicate, badge, anti-join]

must_haves:
  truths:
    - "getMyStrategies(userId) returns own rows at EVERY status via .eq('user_id', userId) — never withPublishedOrOwner, never a category inner join"
    - "getStrategiesByCategory still wraps withPublishedOnly — the public server predicate is untouched"
    - "A key covered ONLY via strategy_keys (the Alpha Centauri shape) yields NO placeholder; a truly bare active key yields exactly one"
    - "Badge maps 'private' to label 'Private' with muted classes — no longer the draft-styled raw lowercase fallback"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "getMyStrategies, getStrategylessActiveKeys, deriveStrategylessKeys (pure, @internal-exported), shared shapeRankingRows"
      exports: ["getMyStrategies", "getStrategylessActiveKeys", "deriveStrategylessKeys"]
    - path: "src/lib/queries.my-strategies.test.ts"
      provides: "pure-function anti-join falsifier incl. the strategy_keys second-link case"
      min_lines: 50
    - path: "src/components/ui/Badge.tsx"
      provides: "private entries in statusMap + statusLabelMap"
      contains: "private"
    - path: "src/components/ui/Badge.test.tsx"
      provides: "Badge status-domain spec (new file — did not exist)"
  key_links:
    - from: "src/lib/queries.ts::getMyStrategies"
      to: "strategies table"
      via: "own-only predicate"
      pattern: "\\.eq\\(\"user_id\", userId\\)"
    - from: "src/lib/queries.ts::getStrategylessActiveKeys"
      to: "strategy_keys"
      via: "owner-scoped link read"
      pattern: "\\.eq\\(\"owner_id\", userId\\)"
    - from: "src/lib/queries.ts::getMyStrategies"
      to: "shapeRankingRows"
      via: "shared row-shaper (trust signals + extractAnalytics + EMPTY_ANALYTICS fallback)"
      pattern: "shapeRankingRows"
---

<objective>
Add the two server fetchers the my-strategies page needs, and fix the live Badge `private` defect.

**Documented ROADMAP deviation (binding orchestrator/founder ruling, record verbatim in SUMMARY):**
ROADMAP SC-3 and CONTEXT name `withPublishedOrOwner` as the predicate. That helper is
`published OR own` (`visibility.ts:130-133`) — on a page titled "My Strategies" it would render
the ENTIRE published universe (research Pitfall 2). The page query uses OWN-ONLY
`.eq("user_id", user.id)` at every status. RLS sanctions own-row reads (`strategies_read` =
`status='published' OR user_id = auth.uid()`, migration 20260405061912:28; `analytics_read` is
its EXISTS-mirror). `.eq("user_id", …)` is strictly NARROWER than the named helper and cannot
leak. The ROADMAP wording's intent ("own including unpublished") is satisfied; 148's gate still
powers the row → factsheet links. Lint-clean by construction: `no-owner-or-on-admin-client`
matches only the raw `.or(...)` shape, `no-raw-published-predicate` only
`.eq("status","published")`.

**Placeholder derivation (founder decision + PROD census 2026-08-05):** founder account = 8
active keys → 4 strategies (Alpha Centauri carries 3 keys via `strategy_keys`; three others via
direct `strategies.api_key_id`) → exactly 2 bare keys. Coverage MUST consider BOTH link forms —
an `api_key_id`-only anti-join fabricates 2 spurious placeholders on the founder's own account.
Active-key predicate = the `isPerKeyDailiesEligibleKey` semantics (`queries.ts:2436-2443`):
`is_active && sync_status !== "revoked" && disconnected_at == null` — reuse the exported helper,
do not re-derive.

**Badge fix (declared scope, research Pitfall 8):** the `private` mapping improves TWO existing
surfaces where the defect is LIVE today (`(dashboard)/strategies/page.tsx:177`,
`StrategyHeader.tsx:24`); the three `contact_requests` consumers are unaffected (`private` is not
in that status domain). Declared, not silent widening.

Output: `getMyStrategies`, `getStrategylessActiveKeys`, `deriveStrategylessKeys` (pure), shared
`shapeRankingRows`, Badge maps + two new test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-CONTEXT.md
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-PATTERNS.md (§3, §6, Pinned Literals table)
@src/lib/queries.ts (:91-260 percentiles + category loader; :2040-2110 getUserApiKeys + SUPPORTED_EXCHANGE_SET; :2410-2450 isPerKeyDailiesEligibleKey)
@src/lib/visibility.ts
@src/components/ui/Badge.tsx

<interfaces>
Row-shaper to EXTRACT (verbatim from getStrategiesByCategory, queries.ts:243-254):

```ts
const signals = await readPublicVerificationSignals(strategies.map((s) => (s as unknown as Strategy).id));
return strategies.map((s) => {
  const strat = s as unknown as Strategy;
  return {
    ...strat,
    trust_tier: (signals.get(strat.id)?.trust_tier ?? null) as Strategy["trust_tier"],
    analytics: extractAnalytics(s.strategy_analytics) ?? { ...EMPTY_ANALYTICS, strategy_id: s.id },
  };
});
```

The `?? { ...EMPTY_ANALYTICS, strategy_id: s.id }` fallback is load-bearing for Delta 4
(analytics-less rows render em-dashes, never crash).

Active-key predicate — ALREADY EXPORTED, reuse (queries.ts:2436-2443):
`isPerKeyDailiesEligibleKey({ is_active, sync_status, disconnected_at }): boolean`

api_keys projection — MANDATORY (constants.ts SEC-005 note: any other projection silently
returns NULL for revoked columns on a user client):
`API_KEY_USER_COLUMNS` = `"id, user_id, exchange, label, is_active, sync_status, last_sync_at, account_balance_usdt, created_at, sync_error, last_429_at, disconnected_at"`

strategy_keys columns (migration 20260710120000): `strategy_id, api_key_id, owner_id, …` —
RLS gate is `owner_id = auth.uid()`; N keys → 1 composite; a key may hold TWO disjoint windows
(no (strategy_id, api_key_id) uniqueness) → de-dupe linked key ids with a Set.

Venue display map (closed-sets.ts:48-55) — covers ALL SIX venues incl. deribit/mt5 (the founder
census needs them; queries.ts's private VENUE_DISPLAY and SyncBadge's map are 3-venue and WRONG
for this): `EXCHANGE_DISPLAY: { binance: "Binance", okx: "OKX", bybit: "Bybit", deribit: "Deribit", sfox: "sFOX", mt5: "MT5" }`

Badge maps (Badge.tsx:13-35): `statusMap` / `statusLabelMap`; fallbacks `?? statusMap.draft` (:46)
and `?? label` (:49). The `archived` muted-ink pairing to clone: `"bg-badge-other/10 text-text-muted"` / `"Archived"`.

New export contracts (downstream plans 03/04 code against these — define EXACTLY):

```ts
export type StrategylessKey = { id: string; exchange: SupportedExchange; label: string };
export async function getMyStrategies(userId: string): Promise<StrategyWithAnalytics[]>;
export async function getStrategylessActiveKeys(userId: string): Promise<StrategylessKey[]>;
/** @internal Exported for unit testing only (the isPerKeyDailiesEligibleKey precedent). */
export function deriveStrategylessKeys(
  keys: Pick<ApiKeyUserView, "id" | "exchange" | "label" | "is_active" | "sync_status" | "disconnected_at">[],
  directKeyIds: readonly (string | null)[],
  linkedKeyIds: readonly string[],
): StrategylessKey[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: getMyStrategies + getStrategylessActiveKeys + pure anti-join with its falsifier</name>
  <files>src/lib/queries.ts, src/lib/queries.my-strategies.test.ts</files>
  <read_first>src/lib/queries.ts (:204-255 getStrategiesByCategory — the body to split; :119-146 getPercentiles error idiom; :2044-2109 getUserApiKeys; :2410-2450 eligibility helper), 149-PATTERNS.md §3 + §No Analog row 1</read_first>
  <behavior>
    `queries.my-strategies.test.ts` — pure-function spec for `deriveStrategylessKeys`, literal fixtures, no supabase mock:
    - Founder-census shape: 8 eligible keys; directKeyIds covers 3; linkedKeyIds covers 3 MORE (the Alpha Centauri keys, absent from directKeyIds) → result is EXACTLY the 2 remaining ids. This is the falsifier for the api_key_id-only anti-join (dropping the linked union must turn it RED with 5 ≠ 2).
    - Ineligible keys never yield placeholders: a key with `is_active: false`, one with `sync_status: "revoked"`, one with `disconnected_at: "2026-01-01T00:00:00Z"` → all excluded even when uncovered.
    - `directKeyIds` nulls ignored (CSV strategies carry `api_key_id: null`).
    - A key linked twice in strategy_keys (two windows) counts as covered once — no duplicate suppression artifacts.
    - Output preserves the input key order and projects exactly `{ id, exchange, label }`.
    - 10-keys-0-strategies shape (the PROD keys-without-strategies account): all 10 eligible keys returned.
  </behavior>
  <action>
    In `queries.ts` (ADD new functions AFTER getStrategiesByCategory; do NOT reorder any existing
    declaration — the phase-84 gate slices the dashboard join block by literal anchors, and the
    phase-63/147 gates scan this file):
    1. Extract the :243-254 body into `async function shapeRankingRows(strategies: <the existing supabase row type>): Promise<StrategyWithAnalytics[]>` (module-private) and call it from `getStrategiesByCategory` — the diff to that function is predicate-preserving: `withPublishedOnly(` stays verbatim at :221.
    2. `getMyStrategies(userId)`: `createClient()`, then `.from("strategies").select("*, strategy_analytics (*)").eq("user_id", userId)`. NO `discovery_categories!inner` (Pitfall 4: `category_id` is nullable, PostgREST drops rows on an `!inner` miss) and NO status predicate. Error → fail-soft `[]` with `console.error("[queries.getMyStrategies] supabase error:", …)` + `captureToSentry(error, { tags: { op: "getMyStrategies" }, level: "error" })` (the getPercentiles idiom — honest empty state, never a fabricated row; state the fail-soft-vs-getUserApiKeys-fail-loud choice in a comment, Rule 7). Return `shapeRankingRows(data ?? [])`. Carry the deviation comment: NOT `withPublishedOrOwner` (published OR own = the whole published universe here); `.eq("user_id", …)` is strictly narrower; RLS `strategies_read` (20260405061912:28) sanctions it; per founder ruling 2026-08-05.
    3. `deriveStrategylessKeys(keys, directKeyIds, linkedKeyIds)`: `covered = new Set([...directKeyIds.filter(Boolean), ...linkedKeyIds])`; return `keys.filter(isPerKeyDailiesEligibleKey).filter(k => !covered.has(k.id)).map(({ id, exchange, label }) => ({ id, exchange: exchange as SupportedExchange, label }))`.
    4. `getStrategylessActiveKeys(userId)`: `Promise.all` of three OWNER-SCOPED reads on the user client — `from("api_keys").select(API_KEY_USER_COLUMNS).eq("user_id", userId)`, `from("strategies").select("id, api_key_id").eq("user_id", userId)`, `from("strategy_keys").select("api_key_id").eq("owner_id", userId)`. Any error → fail-soft `[]` + captureToSentry (`op: "getStrategylessActiveKeys"`). Drop unknown-exchange rows via `SUPPORTED_EXCHANGE_SET` BEFORE deriving (the getUserApiKeys trust-boundary idiom, with the same warn+Sentry escalation). Return `deriveStrategylessKeys(validKeys, strategies.map(s => s.api_key_id), strategyKeys.map(r => r.api_key_id))`.
    Write the Task-1 behavior spec alongside; observe it RED against a deliberately direct-only intermediate ONLY if written test-first (preferred: write test → implement → green).
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-strategies.test.ts src/__tests__/phase-147-series-resolution-guards.test.ts src/__tests__/phase-84-asset-class-flow.test.ts src/__tests__/phase-63-series-space-guards.test.ts --no-file-parallelism && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>New spec green; phase-147/84/63 gates still green (the queries.ts edit did not disturb their pinned literals — `strategy_analytics (*)` is a splat, not a bare `daily_returns` select); tsc clean; `grep -n 'withPublishedOrOwner' src/lib/queries.ts` → 0 hits inside getMyStrategies/getStrategylessActiveKeys bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Badge `private` mapping + the file's first test</name>
  <files>src/components/ui/Badge.tsx, src/components/ui/Badge.test.tsx</files>
  <read_first>src/components/ui/Badge.tsx (whole file — 55 lines), 149-PATTERNS.md §6, DESIGN.md (semantic-color gates: muted, never red/amber for non-published status), src/app/(dashboard)/allocations/components/CoverageStateChip.test.tsx (:45-56 class-assertion idiom)</read_first>
  <behavior>
    `Badge.test.tsx` (NEW — verified absent), literal expectations only:
    - `<Badge type="status" label="private" />` renders text `Private` with className containing `bg-badge-other/10` AND `text-text-muted` — and NOT containing `text-badge-other` (the draft ink it wrongly falls back to today; this is the RED-first case).
    - `<Badge type="status" label="draft" />` → `Draft` + `text-badge-other` (regression pin).
    - `<Badge type="status" label="published" />` → `Published` + `text-positive`.
    - Unknown status (`label="bogus_status"`) still falls back to draft styling with the raw label — the fallback CONTRACT is preserved, only `private` leaves it.
    - Contact-request domain untouched: `<Badge type="status" label="pending" />` renders `Pending` (or the existing mapped label — read the map and pin the actual literal), proving the shared-map fix did not disturb the other domain.
  </behavior>
  <action>
    Add to `statusMap`: `private: "bg-badge-other/10 text-text-muted",` (the exact `archived`
    muted-ink pairing) and to `statusLabelMap`: `private: "Private",` — both placed in the
    strategies-status section ABOVE the contact_request comment divider (keep the divider).
    Do not touch the `:46`/`:49` fallbacks. Comment names Phase 149 Delta 3 + the two LIVE
    surfaces this fixes (`strategies/page.tsx:177`, `StrategyHeader.tsx:24` — Pitfall 8 declared
    blast radius). Write the spec RED-first (the `private` case fails on today's maps), then add
    the two map entries and observe GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/Badge.test.tsx --no-file-parallelism</automated>
  </verify>
  <done>Badge.test.tsx green with the five case groups; the `private` case was observed RED before the map edit (record output); `grep -c 'private' src/components/ui/Badge.tsx` ≥ 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC server query → Supabase (user client) | own-only predicate is the isolation layer; RLS is the backstop (visibility.ts header contract) |
| api_keys / strategy_keys read → page render | key metadata (exchange, label) flows to HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-04 | Information Disclosure / EoP | `getMyStrategies` predicate | mitigate | `.eq("user_id", userId)` where userId comes ONLY from `auth.getUser()` at the call site (plan 04); strictly narrower than withPublishedOrOwner — cannot return OTHER users' unpublished rows; RLS `strategies_read`/`analytics_read` backstop (security_requirement a). Pinned by plan-05 gate assertion 5 |
| T-149-05 | Information Disclosure | `getStrategylessActiveKeys` | mitigate | All three reads owner-scoped on the USER client (`.eq("user_id"/"owner_id", userId)`); `API_KEY_USER_COLUMNS` projection only (SEC-005 — no secrets in the grant); rendered fields limited to exchange+label of the SESSION OWNER's own keys (security_requirement c) |
| T-149-06 | Information Disclosure | percentile population | mitigate | `getPercentiles` NOT touched by this plan (zero-diff on :119-197); own drafts structurally cannot enter its published-only map (security_requirement d). Asserted by plan-05 gate |
| T-149-07 | Repudiation (masked infra failure) | fail-soft `[]` returns | accept | Fail-soft chosen over getUserApiKeys' throw: an empty ranking is honest and non-money-display; captureToSentry preserves ops signal (V7 idiom, reason stated in code comment) |
| T-149-SC | Tampering | package installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/queries.my-strategies.test.ts src/components/ui/Badge.test.tsx --no-file-parallelism` green; phase-63/84/147 gates green.
- `grep -n 'discovery_categories!inner' src/lib/queries.ts` — present in getStrategiesByCategory/getPercentiles/getStrategyDetail bodies ONLY; absent from the two new fetchers.
- `npm run lint` clean (no-owner-or / no-raw-published rules untouched by construction).
</verification>

<success_criteria>
Own-only fetcher + census-correct placeholder derivation exist with pure-function falsifiers (the strategy_keys second-link case observed); public server predicate untouched; Badge `private` defect closed with the file's first spec.
</success_criteria>

<output>
Create `.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-02-SUMMARY.md` when done. Record the ROADMAP-deviation prose and the observed RED outputs.
</output>
