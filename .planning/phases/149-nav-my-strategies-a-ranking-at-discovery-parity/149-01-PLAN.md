---
phase: 149-nav-my-strategies-a-ranking-at-discovery-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/strategy/StrategyTable.tsx
  - src/components/strategy/StrategyFilters.tsx
  - src/components/strategy/StrategyTable.visibility.test.tsx
autonomous: true
requirements: [NAV-01]
tags: [ranking, visibility-predicate, react-table, discovery-parity]

must_haves:
  truths:
    - "StrategyTable with visibility='owner-all-statuses' RENDERS a status:'private' row; with the default it DROPS it"
    - "/discovery/[slug] and /browse/[slug] pass no visibility prop, so their render is byte-identical to today"
    - "On the owner surface the Grid view is unreachable: the view toggle is not rendered and viewMode can never resolve to 'grid'"
    - "Own unpublished rows never render a Simulate Impact button (the simulator service rejects non-published candidates)"
  artifacts:
    - path: "src/components/strategy/StrategyTable.tsx"
      provides: "visibility prop (closed union, literal default 'published-only'), effectiveViewMode derivation, status-gated SimulateImpactButton"
      contains: "visibility = \"published-only\""
    - path: "src/components/strategy/StrategyFilters.tsx"
      provides: "showViewToggle?: boolean prop (default true) hiding the table/grid toggle group"
    - path: "src/components/strategy/StrategyTable.visibility.test.tsx"
      provides: "RED-first behavioral falsifier for Pitfall 1 + parity + link + toggle assertions"
      min_lines: 80
  key_links:
    - from: "src/components/strategy/StrategyTable.tsx"
      to: "strategies.filter((s) => s.status === \"published\")"
      via: "the filtered useMemo first line, now branched on visibility"
      pattern: "visibility === \"published-only\""
    - from: "src/components/strategy/StrategyTable.tsx"
      to: "src/components/strategy/StrategyFilters.tsx"
      via: "showViewToggle={visibility !== \"owner-all-statuses\"}"
      pattern: "showViewToggle"
---

<objective>
Parameterize the ONE blocking defect of the phase: `StrategyTable.tsx:331` filters to
`status === "published"` IN THE COMPONENT (research Pitfall 1 — invisible to `withPublishedOnly`,
to the AST lint rule, and to every existing test). Add a closed-union `visibility` prop with the
literal default `"published-only"` so both public consumers are byte-unchanged, make Grid view
unreachable on the owner surface (orchestrator/founder ruling: HIDE the view toggle — the
`rowLinkMode` grid-link fix is OUT OF SCOPE; grid stays discovery-only), and gate the
per-row Simulate Impact button on `status === "published"` (plan-time verification:
`analytics-service/routers/simulator.py:288-290` fetches the candidate with
`.eq("status","published")` and rejects non-published candidates — a button on an own draft row
would fail on every click; the gate is behavior-invariant on public surfaces where every row is
already published).

UI-SPEC alignment note (required by the ruling, record in SUMMARY): the UI-SPEC's Inherited-anatomy
table lists "view modes (table/grid)" as inherited. The founder ruling supersedes it for THIS
surface only: grid view is a `notFound()` dead-end for own unpublished rows
(`StrategyGrid.tsx:52-53` → `getStrategyDetail` → `withPublishedOnly` → 404, research Pitfall 3),
and hiding the toggle is the simplest correct resolution with no dead-end reachable. Public
surfaces keep both view modes unchanged.

Purpose: without this, the my-strategies page renders "No strategies match your filters." for
every private/draft row (SC-1 dead on arrival), and the shared-component reuse is the leak
vector the roadmap trap names.
Output: parameterized StrategyTable + StrategyFilters, and the RED-first behavioral spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-CONTEXT.md
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-PATTERNS.md (§4, §5, §11, Pinned Literals table)
@src/components/strategy/StrategyTable.tsx
@src/components/strategy/StrategyFilters.tsx
@src/components/strategy/StrategyTable.test.tsx (fixture factory :72-102, leaf stubs :104-121 — copy, do NOT edit this file)

<interfaces>
Current signature to extend (StrategyTable.tsx:175-183):

```tsx
export function StrategyTable({
  strategies,
  categorySlug,
  basePath = "/discovery",
  portfolioId = null,
  userId,
  initialWatchedSet,
  percentiles,
}: StrategyTableProps) {
```

The site to parameterize (StrategyTable.tsx:330-331, FIRST line of the `filtered` useMemo):

```tsx
  const filtered = useMemo(() => {
    let result = strategies.filter((s) => s.status === "published");
```

⚠️ `result.sort(...)` at :418 mutates in place and `strategies` is in the memo dep array (:428) —
the owner arm MUST hand back a copy (`strategies.slice()`), never the prop array.

View-mode consumers to route through `effectiveViewMode`:
- :415-416 `const effectiveSortKey = viewMode === "table" ? tableSortKey : sortKey;` (and Dir)
- :458 scroll-cue effect dep `viewMode`
- :547 render branch `viewMode === "table" ?`
- :259 hydration effect `setViewMode(prefs.view)`

Simulate button render site (StrategyTable.tsx:829-835): `<SimulateImpactButton candidateStrategyId={s.id} … portfolioId={portfolioId} />`.

StrategyFilters view toggle (StrategyFilters.tsx:375-395): two buttons with
`aria-label="Table view"` / `aria-label="Grid view"`; props `viewMode` :73, `onViewModeChange` :74.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-first visibility spec — the falsifier for Pitfall 1</name>
  <files>src/components/strategy/StrategyTable.visibility.test.tsx</files>
  <read_first>src/components/strategy/StrategyTable.test.tsx (:72-124 — `makeStrategy`/`makeAnalytics` factories, leaf `vi.mock` stubs for SimulateImpactButton/Sparkline, `installFetchMock()` beforeEach, and the CI-flake note about `discovery_view_preferences` keys), 149-PATTERNS.md §11</read_first>
  <behavior>
    New spec file (do NOT edit StrategyTable.test.tsx — its `status: "published"` factory default at :89 is a pinned literal). Clone the factory + leaf stubs; use a distinct `categorySlug` (`"vis-spec"`) to dodge prefs-key collisions. Cases, all with literal expectations:
    - SC-1c RED-first: render `<StrategyTable strategies={[private row, draft row, published row]} visibility="owner-all-statuses" …/>` → all three names in the document. Same fixture with NO visibility prop → only the published name renders. ⛔ This file MUST be observed RED against today's :331 before Task 2 (the private/draft arm fails), and the RED output pasted into the eventual commit message body.
    - SC-2a parity: one shared published fixture rendered once with the discovery prop recipe and once with `visibility="owner-all-statuses"` → identical rendered column-header label set (Strategy, Return %, CAGR, Sharpe, Max DD, Volatility, 6 Month, AUM), identical header sort buttons, and a leading `#1` rank cell in both.
    - SC-2b: with `percentiles={{ [id]: { sharpe: 82, … } }}` and default sharpe sort, the mapped row shows text `P82`; an unmapped row shows NO `P` suffix; with `percentiles` undefined no suffix anywhere.
    - SC-4c: an own private row's metric cells equal the same `formatPercent`/`formatNumber` output as the published fixture with identical analytics (no reduced column set) — assert an exact formatted cell string for one metric.
    - SC-5a: for a `status:"private"` row, the name cell link href is exactly `/factsheet/{id}`.
    - Toggle-hide: with `visibility="owner-all-statuses"`, `screen.queryByLabelText("Grid view")` is null AND `screen.queryByLabelText("Table view")` is null; with the default recipe both exist.
    - Simulate gate: mock `SimulateImpactButton` to render a probe testid; with `visibility="owner-all-statuses"` and rows [private, published] + `portfolioId="p-1"`, exactly ONE probe renders (the published row's).
  </behavior>
  <action>
    Write the spec per the behavior block. Import `StrategyTable` only; expected values are string literals in the test (Oracle Independence — never import `COLUMNS` or formatters to compute expectations). Run it and OBSERVE the failures: the owner-all-statuses cases fail on today's source (unknown prop is ignored by React, so private/draft rows are dropped); the toggle-hide and simulate-gate cases fail too. Do not mark the task done on a fully-green first run — that means the spec is vacuous.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.visibility.test.tsx --no-file-parallelism 2>&1 | tail -20</automated>
  </verify>
  <done>Spec file exists with all seven case groups; run output shows RED on the owner-arm cases (recorded); default-recipe cases (parity render, published-only drop) are green — proving the spec measures the delta, not the world.</done>
</task>

<task type="auto">
  <name>Task 2: Parameterize visibility, suppress grid, gate Simulate — then GREEN</name>
  <files>src/components/strategy/StrategyTable.tsx, src/components/strategy/StrategyFilters.tsx</files>
  <read_first>src/components/strategy/StrategyTable.tsx (:110-137 props interface, :175-193 destructuring+state, :234-266 prefs hydration, :330-428 filtered memo, :413-431 sort/paging, :491-547 StrategyFilters mount + render branch, :829-845 actions cell + empty row, :865-874 grid mount), src/components/strategy/StrategyFilters.tsx (:64-80 props, :375-395 toggle), 149-PATTERNS.md §4 + Shared Pattern C/F</read_first>
  <action>
    In `StrategyTable.tsx`:
    1. Add to `StrategyTableProps` (matching the file's WHY-comment prop idiom, Shared Pattern F):
       `visibility?: "published-only" | "owner-all-statuses";` with a doc comment stating: DEFAULT `"published-only"` reproduces the behavior /discovery/[slug] and /browse/[slug] have had since 2eef614a; `"owner-all-statuses"` is passed ONLY by the owner-scoped /my-strategies surface whose SERVER query already narrowed the set to `user_id = <session id>` — this prop does not widen anything, it stops the component re-filtering an already-owner-scoped set. Pinned by `src/__tests__/phase-149-my-strategies-parity.test.ts` (per D: StrategyTable's in-component published filter must be PARAMETERIZED with the published default — discovery surfaces provably unchanged; same idiom as `basePath`'s literal default at :178).
    2. Destructure with the literal default: `visibility = "published-only",` — the default expression IS the public invariant the phase gate pins; do not hoist it into a constant.
    3. Replace :331 with:
       `let result = visibility === "published-only" ? strategies.filter((s) => s.status === "published") : strategies.slice();`
       (`.slice()` mandatory — :418 sorts in place; add `visibility` to the memo dep array at :428.)
    4. Derive ONCE, right after the destructured state: `const effectiveViewMode = visibility === "owner-all-statuses" ? "table" : viewMode;` and use `effectiveViewMode` at :415-416 (effectiveSortKey/Dir), in the :458 effect dep array, and at the :547 render branch. Leave the :259 hydration `setViewMode(prefs.view)` untouched — the derivation is the single enforcement point, so a stale grid pref cannot resurrect the dead-end. Comment WHY: grid links resolve `${basePath}/${categorySlug}/${id}` → `getStrategyDetail` → `withPublishedOnly` → `notFound()` for own unpublished rows (Pitfall 3); founder ruling 2026-08-05 keeps grid discovery-only rather than adding `rowLinkMode`.
    5. Pass `showViewToggle={visibility !== "owner-all-statuses"}` to `<StrategyFilters …>` at :493.
    6. Wrap the SimulateImpactButton at :830-834: render it only when `s.status === "published"`; when the row is not published render nothing in that cell. Comment: verified `analytics-service/routers/simulator.py:288-290` — the service fetches the candidate `.eq("status","published")` and 404s otherwise; on public surfaces every row is published so this branch is behavior-invariant (never a button that fails on every click — the no-disabled-buttons UAT direction).
    In `StrategyFilters.tsx`: add `showViewToggle?: boolean;` to `StrategyFiltersProps` (doc comment: default true; false ONLY on the owner-scoped my-strategies surface where grid view is deliberately unreachable — Phase 149 ruling), destructure `showViewToggle = true`, and wrap the toggle group (:375-395, the container div holding both aria-labelled buttons) in `{showViewToggle && (…)}`.
    Do NOT touch: the watchlist/example/search/advanced filter chain, `emptyRowColSpan`, the grid mount props, `StrategyGrid.tsx`, or `StrategyTable.test.tsx`.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.visibility.test.tsx src/components/strategy/StrategyTable.test.tsx --no-file-parallelism && npx tsc --noEmit -p tsconfig.json 2>&1 | tail -5</automated>
  </verify>
  <done>Task-1 spec fully green; the pre-existing 912-line StrategyTable.test.tsx suite green UNCHANGED (zero edits to it — its green-through proves the default preserved public behavior); tsc clean. `grep -n 'visibility = "published-only"' src/components/strategy/StrategyTable.tsx` → exactly 1 hit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client component | server-fetched strategy rows (possibly unpublished) cross into a SHARED client component also mounted on anon surfaces |
| client → analytics-service | SimulateImpactButton posts a candidate id to a published-gated Python service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-01 | Information Disclosure | `StrategyTable` visibility default | mitigate | Literal default `"published-only"` in the destructuring; RED-first behavioral spec (this plan) + structural gate pin (plan 05) make the default the asserted public invariant — an unpublished row's metrics cannot reach anon/non-owner via the shared path (security_requirement b) |
| T-149-02 | Information Disclosure (oracle via dead-end) | grid view on owner surface | mitigate | `effectiveViewMode` derivation + hidden toggle: grid unreachable by construction, no 404 oracle, no dead-end (SC-5) |
| T-149-03 | Denial of Service (self-inflicted UX) | SimulateImpactButton on own drafts | mitigate | `s.status === "published"` row gate; service-side `.eq("status","published")` (simulator.py:288-290) remains the backstop |
| T-149-SC | Tampering | npm installs | accept | This phase installs ZERO packages (RESEARCH §Package Legitimacy Audit — vacuous, not skipped) |
</threat_model>

<verification>
- `npx vitest run src/components/strategy/StrategyTable.visibility.test.tsx src/components/strategy/StrategyTable.test.tsx --no-file-parallelism` green.
- `git diff --stat` touches ONLY the three files in `files_modified`.
- `grep -c 'owner-all-statuses' src/components/strategy/StrategyTable.tsx` ≥ 2 (prop union + branch), and `grep -rn 'visibility=' 'src/app/(dashboard)/discovery' src/app/browse --include='*.tsx'` → 0 hits.
</verification>

<success_criteria>
Pitfall 1 is closed and pinned RED-first; public surfaces provably pass no new prop; grid unreachable on the owner recipe; Simulate gated to published rows; existing 912-line suite untouched and green.
</success_criteria>

<output>
Create `.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-01-SUMMARY.md` when done. Record: the observed RED output, the UI-SPEC toggle-deviation note, and the simulator.py verification citation.
</output>
