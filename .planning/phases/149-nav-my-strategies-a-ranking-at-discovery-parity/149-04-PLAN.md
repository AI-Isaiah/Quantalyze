---
phase: 149-nav-my-strategies-a-ranking-at-discovery-parity
plan: 04
type: execute
wave: 3
depends_on: ["149-01", "149-02", "149-03"]
files_modified:
  - src/app/(dashboard)/my-strategies/page.tsx
  - src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx
  - src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.tsx
  - src/app/(dashboard)/my-strategies/page.test.tsx
  - src/lib/routing/route-contract-manifest.ts
  - src/app/(dashboard)/requireRolePage-wiring.test.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Sidebar.test.tsx
autonomous: true
requirements: [NAV-01]
tags: [rsc-page, nav, role-gate, route-contract]

must_haves:
  truths:
    - "An allocator session reaches /my-strategies from the sidebar and sees own rows at every NON-ARCHIVED status ranked at discovery parity — each own row carrying a Pnn scored against the published population (founder scorer ruling) — plus placeholder rows for bare keys"
    - "A manager-only session never sees the nav entry and is redirected off the page by requireRolePage"
    - "The comparison-set line states the REAL N = the populationSize returned by getOwnRowPercentiles (byte-equal to the old Object.keys(getPercentiles()).length), the <5 threshold copy otherwise, and the K bare-keys sentence when K>0 — ONE published-universe fetch on this page (I-2)"
    - "The empty state (zero strategies AND zero bare keys) opens ContributionWizardOverlay — never a Link into the manager-guarded /strategies subtree"
  artifacts:
    - path: "src/app/(dashboard)/my-strategies/page.tsx"
      provides: "RSC page: noStore → auth → requireRolePage('allocator') → 3 parallel fetches + single-fetch own/published scorer (I-2) → note + section | empty state"
      contains: "requireRolePage"
    - path: "src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx"
      provides: "client host: StrategyTable(owner recipe) + ContributionWizardOverlay + onFinishSetup"
    - path: "src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.tsx"
      provides: "page-level empty state with its own overlay mount (AllocationsTabs precedent)"
    - path: "src/lib/routing/route-contract-manifest.ts"
      provides: "route entry { route: '/my-strategies', class: 'private' } (same commit as page.tsx — lint Rules 1+4)"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "My Strategies href entry inside showsAllocatorWorkspace, above Add a Strategy"
  key_links:
    - from: "src/app/(dashboard)/my-strategies/page.tsx"
      to: "src/lib/queries.ts::getMyStrategies"
      via: "Promise.all with user.id from auth.getUser()"
      pattern: "getMyStrategies\\(user\\.id\\)"
    - from: "src/app/(dashboard)/my-strategies/page.tsx"
      to: "getOwnRowPercentiles"
      via: "the SINGLE published-universe fetch (I-2): { ownMap, publishedMap, populationSize } — Pnn from ownMap, N from populationSize; NO getPercentiles call on this page (drafts DO get Pnn — founder scorer ruling 2026-08-05)"
      pattern: "getOwnRowPercentiles\\("
    - from: "src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx"
      to: "StrategyTable"
      via: "visibility=\"owner-all-statuses\" + placeholderKeys + onFinishSetup + percentiles (the OWN-scored map)"
      pattern: "owner-all-statuses"
---

<objective>
Assemble the vertical slice: route, page shell, comparison-set line, client section hosting the
table + wizard overlay, empty state, route-contract entry, role-gate wiring pin, and the sidebar
entry. Every piece is an analog clone (149-PATTERNS §1, §2, §8, §10, §12); the only genuinely new
copy is the comparison-set line, whose literals the UI-SPEC locks.

Binding rulings applied here: `getPercentiles` keeps its exact signature but is NOT called on
this page (I-2 single-fetch ruling): `getOwnRowPercentiles(strategies)` fetches the published
universe ONCE (un-scoped — own rows span categories / null category; a scoped population would
rank against the wrong peer set) and returns `{ ownMap, publishedMap, populationSize }`. The Pnn
suffixes for OWN rows come from `ownMap` (plan 02 — the founder scorer ruling, self-inclusive
per W-A): own rows scored against the SAME published population via the ONE extracted core,
never joining it. getPercentiles remains the discovery-surface caller of that same core — the
gate's "both callers use the core" pin still holds. The approved comparison-set copy stays
VERBATIM — it is now TRUE: drafts DO get Pnn ("Pnn compares the row against those N published
strategies"). N = `populationSize` (byte-equal to the old `Object.keys(getPercentiles()).length`
— never a second COUNT query, never hardcoded, never the own map's
size); the labeled comparison line carries the #n disambiguation copy verbatim; the empty state
mounts its OWN ContributionWizardOverlay (the two existing local-useState precedents); Sidebar
entry = "My Strategies", `BarChartIcon`, above "Add a Strategy", inside the allocator branch —
⚠️ the phase-32 gate does a whole-file `not.toContain("/scenarios")` on Sidebar.tsx, so no
comment or href may contain that substring (`/my-strategies` is safe).

Surface parity note (iteration-2 checker I-2 — distinct from this iteration's I-2 single-fetch ruling; one line for the SUMMARY): the customize-columns / view-prefs
affordances absent here match the /browse variant of this surface — deliberate parity with the
closest sibling, not an omission.

Purpose: SC-1 (way in + full coverage), SC-2 (parity + honest comparison set + own-row Pnn),
SC-5 (rows open the 148 owner lane).
Output: the page subtree + nav, with the RSC wiring spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-UI-SPEC.md (Deltas 1, 2, 5 + Copywriting Contract + States)
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-PATTERNS.md (§1, §2, §8, §10, §12)
@DESIGN.md
@src/app/(dashboard)/discovery/[slug]/page.tsx
@src/app/(dashboard)/recommendations/page.tsx (:2-5, :33-47 — noStore + auth + role-gate ORDER)
@src/app/(dashboard)/allocations/AllocationsTabs.tsx (:1010-1018 — overlay mount precedent)
@src/app/(dashboard)/requireRolePage-wiring.test.tsx
@src/lib/routing/route-contract-manifest.ts
@src/components/layout/Sidebar.tsx (:103-145)
@src/components/layout/Sidebar.test.tsx (:41-95, :115-160)

<interfaces>
Contracts landed by earlier waves (code against, do not re-derive):
- `getMyStrategies(userId): Promise<RankedStrategyRow[]>` (archived excluded via `.neq("status","archived")` — W-4) · `getStrategylessActiveKeys(userId): Promise<StrategylessKey[]>` (plan 02, `@/lib/queries`)
- `getOwnRowPercentiles(ownRows): Promise<{ ownMap: PercentileMap; publishedMap: PercentileMap; populationSize: number } | null>` (plan 02, I-2 single fetch) — ONE published-universe query serving both the own-row Pnns (ownMap, self-inclusive per W-A) and the honesty-copy N (populationSize = Object.keys(publishedMap).length); mirrors getPercentiles' <5 thresholds, so its null-branch and the N-copy branch flip together
- `StrategyTable` props: `visibility="owner-all-statuses"`, `placeholderKeys: PlaceholderKeyRow[]`, `onFinishSetup: () => void` (plans 01/03); `PlaceholderKeyRow = { id, exchangeLabel, keyLabel }`
- `EXCHANGE_DISPLAY` (`@/lib/closed-sets`) — all 6 venues; format placeholder rows server-side: `{ id: k.id, exchangeLabel: EXCHANGE_DISPLAY[k.exchange], keyLabel: k.label }`
- `ContributionWizardOverlay` props: `{ isOpen: boolean; onClose: () => void; onSuccess?: (strategyId: string) => void }` — portals to body, null while closed, own Esc/focus handling. NO preselect prop.
- `requireRolePage(supabase, user, "allocator")` — throws NEXT_REDIRECT; call OUTSIDE any try/catch.
- `getRealPortfolio(user.id)` — existing `@/lib/queries` export. (`getPercentiles` is NOT imported or called here — I-2; it stays the discovery-surface caller of the same core.)

Copy literals (UI-SPEC Copywriting Contract — verbatim, including the #n disambiguation):
- Map branch: `Ranked against {N} published strategies. # is the row's position in this list; Pnn compares the row against those {N} published strategies. Private and draft rows are visible only to you and do not affect public rankings.`
- Null branch: `Percentile ranks appear once 5 strategies are published in the comparison set. Private and draft rows are visible only to you.`
- K sentence (appended when K>0): `{K} of your keys have no strategy yet.`
- Empty state: heading `No strategies yet.` · body `Connect an exchange API key or upload a CSV to see your strategies ranked here. First metrics arrive ~10–15 minutes after a key connects.` · CTA `Add a Strategy` · anatomy `border border-border bg-surface rounded-lg px-6 py-8`, no icon.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: /my-strategies page subtree + manifest + role-wiring pin (one commit)</name>
  <files>src/app/(dashboard)/my-strategies/page.tsx, src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx, src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.tsx, src/app/(dashboard)/my-strategies/page.test.tsx, src/lib/routing/route-contract-manifest.ts, src/app/(dashboard)/requireRolePage-wiring.test.tsx</files>
  <read_first>149-PATTERNS.md §1 (imports + auth order + parallel-fetch shape, incl. the "do not copy the key prop" delta), §2 (overlay mount + why a Link is forbidden), §10 (SURFACES entry + chain-recording supabase double), §12 (manifest rules), DESIGN.md, src/app/(dashboard)/recommendations/page.tsx:33-47</read_first>
  <behavior>
    `page.test.tsx` (mock `@/lib/supabase/server` with a CHAIN-RECORDING double — record `.from().select().eq()/.neq()` args so predicates are ASSERTABLE (the double must record `.neq` alongside `.eq`); an identity stub makes SC-1b vacuous, the 148 Pitfall-5 lesson. The double routes payloads by QUERY SHAPE, not call order (I-3): a `strategies` chain recording `.eq("user_id", …)` (the `*, strategy_analytics (*)` own-rows select) gets the own fixture; a `strategies` chain with the population select shape (`id, strategy_analytics (…)` — PERCENTILE_ANALYTICS_COLUMNS, no user_id eq) gets the population fixture; `api_keys`/`strategy_keys` route by table name. Leaf stubs (I-3): `vi.mock` SimulateImpactButton → null and Sparkline → null (the StrategyTable.test.tsx idiom); `installFetchMock()` in beforeEach (row-level fetches must never hit the network); `useDiscoveryPrefs` needs NO mock — it is localStorage-only and jsdom provides it. Mock `@/lib/auth/requireRolePage` and `next/cache`'s noStore as pass-through spies):
    - SC-1b: rendering the page records a `strategies` query whose recorded eq-chain contains `["user_id", "<session-user-id>"]`, whose recorded neq-chain contains `["status", "archived"]` (W-4 — archived rows excluded from the ranked list), and does NOT contain an eq `["status", "published"]`; the api_keys/strategy_keys reads record `["user_id", uid]` / `["owner_id", uid]`.
    - SC-2c map branch: with a published-population double of 7 rows with analytics and 3 own rows, the rendered `[data-testid="comparison-set-note"]` text contains the literal `Ranked against 7 published strategies` and the full disambiguation sentence. The double records EXACTLY ONE population-shaped `strategies` query (the I-2 single-fetch pin — a second identical published-universe fetch is a regression).
    - SC-2c null branch: population double returns <5 rows → note text contains `Percentile ranks appear once 5 strategies are published`.
    - SC-2d own-row Pnn (the scorer-ruling wire): with the 7-row published population double carrying sharpe values [1,2,3,4,5,6,7] and one own DRAFT row with sharpe 7.5, the rendered own row shows the literal `P100` (hand-computed, SELF-INCLUSIVE per W-A: 7 population values ≤ 7.5 plus the subject itself → 8 of 8 → 100; higher-is-better, no inversion — the P100 fixture is unchanged by the ruling). The single population fixture serves BOTH outputs of getOwnRowPercentiles — publishedMap/populationSize (the N copy) and ownMap (this Pnn) — one fetch, both observable.
    - K sentence: 2 bare keys → note text contains `2 of your keys have no strategy yet.`; 0 bare keys → sentence absent.
    - Empty branch: 0 strategies AND 0 bare keys → `No strategies yet.` heading renders, comparison-set note is ABSENT.
    - Placeholders-only branch: 0 strategies, 2 bare keys → table section renders (not the empty state) and the note text is exactly the K sentence.
    - noStore called; requireRolePage called with the literal `"allocator"`.
  </behavior>
  <action>
    Write page.test.tsx first (RED — module doesn't exist), then build:
    1. `page.tsx` (RSC): copy the recommendations auth block ORDER — `noStore()` (re-target the C-0016 comment: owner-rendered rows must never be cached across users) → `createClient()` → `auth.getUser()` → `if (!user) redirect("/login?redirect=/my-strategies")` → `await requireRolePage(supabase, user, "allocator")` outside any try/catch. Then `const [strategies, bareKeys, portfolio] = await Promise.all([getMyStrategies(user.id), getStrategylessActiveKeys(user.id), getRealPortfolio(user.id)])` — THREE fetches, no getPercentiles (I-2 single-fetch ruling; comment it: the published universe is fetched ONCE, inside getOwnRowPercentiles; getPercentiles stays the discovery-surface caller of the same core). Then `const own = strategies.length > 0 ? await getOwnRowPercentiles(strategies) : null;` (sequential — it consumes the fetched rows; comment: FOUNDER SCORER RULING + W-A 2026-08-05 — own rows scored self-inclusively AGAINST the published population via the ONE extracted core; drafts get Pnn; the population is never widened; N = own.populationSize, byte-equal to the old Object.keys(getPercentiles()).length — the number of strategies that entered the ranking, which is what the copy claims; per-metric n can be smaller — do NOT build a per-metric N). `const placeholderRows = bareKeys.map((k) => ({ id: k.id, exchangeLabel: EXCHANGE_DISPLAY[k.exchange], keyLabel: k.label }))`. Shell: `<div className="mx-auto max-w-[1920px]">` (copy the fluid-fill comment), `<PageHeader title="My Strategies" />`. Branches: both empty → `<MyStrategiesEmptyState />`; else render `<p data-testid="comparison-set-note" className="mb-6 text-small text-text-secondary">{noteText}</p>` + `<MyStrategiesSection …/>` where noteText = (strategies.length > 0 ? (own ? MAP_COPY(own.populationSize) : NULL_COPY) : "") + (K > 0 ? K_SENTENCE : "") joined with a space — literals from the interfaces block (N is ALWAYS populationSize — the PUBLISHED universe; the own map's size is not the comparison set). OMIT userId/initialWatchedSet/getMyWatchlist (browse variant — starring your own upload is meaningless; the customize/prefs absence likewise matches the /browse variant of this surface, deliberate — iteration-2 checker I-2) and OMIT the `key={…}` remount prop (static route).
    2. `MyStrategiesSection.tsx` ("use client"): props `{ strategies, placeholderKeys, portfolioId, percentiles }` — type `strategies: RankedStrategyRow[]` (import the type from `@/lib/queries`; W-C: `analyticsPresent` is REQUIRED on the owner path so plan 03's chip coercion always has its signal — the shared StrategyTable prop stays optional), and `percentiles` receives the OWN-scored map (`own?.ownMap ?? null`), not the published map: only own rows render here, and their Pnn must come from the scorer helper. Local `const [wizardOpen, setWizardOpen] = useState(false)`; `const router = useRouter()`. Renders `<StrategyTable strategies={strategies} categorySlug="my-strategies" portfolioId={portfolioId} percentiles={percentiles ?? undefined} visibility="owner-all-statuses" placeholderKeys={placeholderKeys} onFinishSetup={() => setWizardOpen(true)} />` (categorySlug is a prefs-scope key, NOT a category — Pitfall 6 comment) + `<ContributionWizardOverlay isOpen={wizardOpen} onClose={() => setWizardOpen(false)} onSuccess={() => { setWizardOpen(false); router.refresh(); }} />`. Comment: opens FRESH — no preselect seam exists (founder ruling; follow-up in TODOS).
    3. `MyStrategiesEmptyState.tsx` ("use client"): the UI-SPEC empty-state anatomy + copy verbatim; primary `Button` `Add a Strategy` toggling its OWN locally-mounted `ContributionWizardOverlay` (AllocationsTabs:1010-1018 precedent; quote the Sidebar:129-136 comment on why a Link into /strategies is forbidden — without the banned substring issue, that comment is about the wizard route). `onSuccess` → close + `router.refresh()`.
    4. `route-contract-manifest.ts`: insert `{ route: "/my-strategies", class: "private", notes: "Phase 149 NAV-01 — allocator-only ranking of own strategies at every status; requireRolePage('allocator') + own-only user_id predicate." }` in ALPHABETICAL position.
    5. `requireRolePage-wiring.test.tsx`: add the 8th SURFACES entry `{ label: "my-strategies/page", need: "allocator", invoke: async () => (await import("./my-strategies/page")).default() }` and update ALL THREE "7 surfaces" prose sites — lines 6, 16, and 77 — to 8 (I-4; afterwards `grep -c '7 surfaces'` on the file → 0, so no prose drift survives).
    All six files in ONE commit — the manifest entry without the page is a STALE lint failure and the page without the entry fails lint (Rules 1+4); the wiring entry in the same commit or the suite is green-while-unguarded.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/my-strategies/page.test.tsx" "src/app/(dashboard)/requireRolePage-wiring.test.tsx" --no-file-parallelism && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <done>page.test green after observed RED; wiring spec green with 8 surfaces and zero "7 surfaces" prose remnants (I-4); `npm run lint` clean (route contract satisfied); noteText branches all literal-asserted; the SC-2d P100 case proves the own-scorer map reaches the rendered row and exactly ONE population-shaped query was recorded (I-2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sidebar entry + role-matrix pin</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/Sidebar.test.tsx</files>
  <read_first>src/components/layout/Sidebar.tsx (:103-145 allocator branch — note the existing comment deliberately avoids the phase-32 banned substring), src/components/layout/Sidebar.test.tsx (:41-95 allocator block, :115-160 manager block), 149-UI-SPEC.md Delta 1, DESIGN.md (light-rail nav)</read_first>
  <behavior>
    Extend Sidebar.test.tsx (existing role-matrix idiom; ⚠️ scope selectors by href, never bare text — Pitfall 10: the manager branch has a "Strategies" entry and `(dashboard)/strategies/page.tsx` shares the h1 text):
    - Allocator view: an `<a>` with `href="/my-strategies"` and accessible text `My Strategies` renders; it appears in DOM order AFTER the `Decks` link and BEFORE the `Add a Strategy` button.
    - Manager-only view (`isAllocator={false} isManager={true}` per the existing block's props): `document.querySelector('a[href="/my-strategies"]')` is null (T-110-16 role-leak pin).
    - The entry is an `<a>`, NOT a `<button>` (this is a page, not an overlay action).
  </behavior>
  <action>
    Write the three cases (RED), then in Sidebar.tsx insert between the
    Recommendations/Compare/Decks push and the "Add a Strategy" push:
    `workspaceItems.push({ label: "My Strategies", href: "/my-strategies", icon: BarChartIcon });`
    with a comment matching the branch's convention: names Phase 149 / NAV-01 (the founder's
    "see my strategies" gap — the workspace was write-only), notes it lives INSIDE
    showsAllocatorWorkspace so it never leaks to a manager (T-110-16), and that BarChartIcon is
    shared with the manager "Strategies" entry deliberately (different branch, different label).
    ⚠️ The comment must NOT contain the literal retired-route substring the phase-32 frozen-spine
    guard whole-file-matches on Sidebar.tsx (write it as "the retired composer slug" if needed).
    Do NOT touch `buildPrimaryMobileNav` (:298-301 — cap of 5; the drawer mounts the full Sidebar
    so mobile is free, Pitfall 7). `BarChartIcon` is already imported in this file.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Sidebar.test.tsx src/__tests__/phase-32-frozen-spine-guards.test.ts --no-file-parallelism</automated>
  </verify>
  <done>Sidebar spec green after observed RED; phase-32 frozen-spine gate green (no banned substring introduced); manager-view negative case green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session → page | owner id must come from auth.getUser(), never a param |
| RSC render → HTTP cache | owner-rendered HTML must never be cached across users |
| nav construction → role branches | entry must not leak to manager-only sessions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-11 | Elevation of Privilege | page owner id | mitigate | `getMyStrategies(user.id)` with `user` from `auth.getUser()` only; the page takes NO params/searchParams (V5 vacuous); chain-recording spec pins the predicate incl. the archived exclusion (security_requirement a) |
| T-149-12 | Information Disclosure | cross-user HTML cache | mitigate | `noStore()` first line (recommendations C-0016 precedent); no `'use cache'` anywhere in the subtree; route class `private` in the manifest |
| T-149-13 | Information Disclosure | nav role leak | mitigate | entry inside `showsAllocatorWorkspace` + manager-view negative spec (T-110-16 class) |
| T-149-14 | Information Disclosure | placeholder key data | mitigate | rows built from `getStrategylessActiveKeys(user.id)` — session-owner keys only, exchange+label only, formatted server-side (security_requirement c) |
| T-149-15 | Information Disclosure | percentile map | mitigate | the published universe is fetched ONCE inside `getOwnRowPercentiles` (withPublishedOnly by construction, un-scoped — I-2); N = populationSize from the PUBLISHED map; own Pnns come from ownMap, scored self-inclusively against the population without joining it (plan-02 core spec makes the invariant executable) — own drafts can neither enter the population other rows see nor inflate N (security_requirement d) |
| T-149-SC | Tampering | package installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/my-strategies/page.test.tsx" src/components/layout/Sidebar.test.tsx "src/app/(dashboard)/requireRolePage-wiring.test.tsx" --no-file-parallelism` green.
- `npm run lint` and `npx tsc --noEmit` clean.
- `grep -rn "'use cache'\|\"use cache\"" 'src/app/(dashboard)/my-strategies'` → 0.
</verification>

<success_criteria>
The founder's ask is reachable end to end: sidebar → ranking (all non-archived statuses, each
own row Pnn-scored against the published universe, + placeholders) → factsheet links via the 148
owner lane; role gates pinned; comparison-set honesty literal-asserted (N = populationSize from
the single published-universe fetch — I-2); route contract satisfied in the same commit as the
page.
</success_criteria>

<output>
Create `.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-04-SUMMARY.md` when done. Record the iteration-2 I-2 parity note (customize/prefs absence matches the /browse variant — deliberate).
</output>
