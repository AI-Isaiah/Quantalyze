---
phase: 149-nav-my-strategies-a-ranking-at-discovery-parity
plan: 03
type: execute
wave: 2
depends_on: ["149-01"]
files_modified:
  - src/components/strategy/StrategyTable.tsx
  - src/components/strategy/StrategyTable.pending-chip.test.tsx
autonomous: true
requirements: [NAV-01]
tags: [ranking, honest-pending, placeholder-rows, chips]

must_haves:
  truths:
    - "A non-published own row shows a muted status marker (Private/Draft/…); published rows carry NO marker"
    - "A row with computed_at null shows the Syncing or No data chip with em-dash cells — never 0 / 0.00 / +0.0%"
    - "One unranked placeholder row renders per bare key, below ranked rows, outside #n numbering, with a working Finish setup action"
    - "Public surfaces render zero markers, zero chips, zero placeholders (props absent → branches dead)"
  artifacts:
    - path: "src/components/strategy/StrategyTable.tsx"
      provides: "status marker (Delta 3), pending chip (Delta 4), placeholderKeys/onFinishSetup props + placeholder <tr> class (Delta 5)"
      contains: "placeholderKeys"
    - path: "src/components/strategy/StrategyTable.pending-chip.test.tsx"
      provides: "chip-state + placeholder-row falsifiers with literal class/copy assertions"
      min_lines: 80
  key_links:
    - from: "src/components/strategy/StrategyTable.tsx"
      to: "src/lib/closed-sets.ts::deriveEmptySeriesState"
      via: "the ONE chip-state derivation site"
      pattern: "deriveEmptySeriesState"
    - from: "src/components/strategy/StrategyTable.tsx"
      to: "onFinishSetup"
      via: "placeholder Actions-cell button"
      pattern: "onFinishSetup"
---

<objective>
Add the three owner-surface row treatments to the now-parameterized StrategyTable: the Delta-3
status marker, the Delta-4 honest pending chip (147 chip family verbatim), and the Delta-5
unranked placeholder rows for strategy-less keys. All three are dead branches on public surfaces:
the marker can never fire on a published-only row set, and the chip/placeholder branches are
gated on props the public pages do not pass — the UI-SPEC "byte-identical to today" invariant.

Delta-5 link ruling (binding): `ContributionWizardOverlay` has NO preselect seam — the
"Finish setup →" action opens the wizard FRESH via an `onFinishSetup` callback (client→client
function prop supplied by plan 04's client section; RSC pages never pass it). The preselect
follow-up goes to TODOS.md in plan 05, not built here.

Purpose: SC-4 (never zeros, no invented data) and the founder's per-KEY coverage half of SC-1.
Output: StrategyTable deltas + the row-states spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-UI-SPEC.md (Deltas 3, 4, 5 + Copywriting Contract)
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-PATTERNS.md (§6 placement, §7, §No Analog rows 1-3)
@DESIGN.md
@src/components/strategy/StrategyTable.tsx
@src/app/(dashboard)/allocations/components/CoverageStateChip.tsx
@src/lib/closed-sets.ts (:455-456 SERIES_STATES, :491-509 deriveEmptySeriesState)

<interfaces>
Chip contract (CoverageStateChip.tsx — copy TOKENS, not the component):
- BASE: `"inline-flex items-center rounded-sm px-2 py-0.5 text-fixed-11 font-medium uppercase tracking-wide"`
- Syncing: `"text-warning bg-warning-bg border border-warning-border"` · No data / No strategy yet: `"text-text-muted bg-track"`
- Rationale comment to reuse: amber = transient-recoverable, muted = honest steady-state absence, NEVER red.

State derivation (closed-sets.ts:491): `deriveEmptySeriesState(status: string | null, strategyCreatedAt: string | null, nowMs?: number): "available" | "computing" | "empty"` — returns only "computing" | "empty" for the inputs used here. ONE derivation site (its own header demands it; the plan-05 gate pins exactly one call in StrategyTable).

Name-cell second line (the empty slot, StrategyTable.tsx:741-748): `SyncBadge` returns null when
`computedAt` null (SyncBadge.tsx:28) — the chip occupies that slot with no collision.

Delta-3 slot (first line, after the name Link + api-verified check, :726-739); Badge render idiom :743-745.

New props (plan 04 codes against these — define EXACTLY):
```tsx
export type PlaceholderKeyRow = { id: string; exchangeLabel: string; keyLabel: string };
placeholderKeys?: PlaceholderKeyRow[];   // server-formatted (EXCHANGE_DISPLAY applied upstream); public pages pass nothing
onFinishSetup?: () => void;              // client→client only; opens ContributionWizardOverlay in the section host
```

Copy literals (UI-SPEC): chip `Syncing` + `title="First metrics arrive in ~10–15 min"` +
`aria-label="Syncing — first metrics arrive in ~10–15 min"`; chip `No data`; placeholder chip
`No strategy yet`; placeholder action `Finish setup →`; placeholder row bg `bg-surface-subtle`;
placeholder name `{exchangeLabel} · {keyLabel}` in `text-small text-text-muted`, NO link.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delta 3 status marker + Delta 4 pending chip (RED-first)</name>
  <files>src/components/strategy/StrategyTable.tsx, src/components/strategy/StrategyTable.pending-chip.test.tsx</files>
  <read_first>src/components/strategy/StrategyTable.tsx (post-plan-01 state: props interface, :726-748 name cell, :64-84 formatters), 149-UI-SPEC.md Deltas 3+4, DESIGN.md (semantic-color gates, Numbers Contract), src/app/(dashboard)/allocations/components/CoverageStateChip.tsx + its test (:45-56)</read_first>
  <behavior>
    New spec `StrategyTable.pending-chip.test.tsx` (clone the makeStrategy/makeAnalytics factories + leaf stubs; distinct categorySlug `"chip-spec"`; all rows rendered with `visibility="owner-all-statuses"`):
    - SC-4b marker: a `status:"private"` row renders a badge with text `Private` and className containing `text-text-muted`; a `status:"draft"` row renders `Draft`; a `status:"published"` row renders NEITHER (queryByText null). Marker classes never contain `text-negative` or `text-warning`.
    - SC-4a Syncing: a row with `analytics.computed_at: null`, `analytics.computation_status: "pending"` renders a chip with text `Syncing`, `title="First metrics arrive in ~10–15 min"`, `aria-label="Syncing — first metrics arrive in ~10–15 min"`, className containing `bg-warning-bg`; its metric cells contain `—` and the row NEVER contains the strings `0.00` or `+0.0%`.
    - SC-4a No data: `computed_at: null`, `computation_status: "failed"`, `created_at` older than the computing window → chip text `No data` with className containing `bg-track`; still `—` cells; row still clickable (`/factsheet/{id}` link present).
    - Computed rows: `computed_at` set → NO chip (queryByText("Syncing") and ("No data") both null) and SyncBadge branch preserved.
    - Public invariance: the same pending fixture rendered with NO visibility prop (published row, computed_at null) renders NO chip — the chip is owner-surface-only.
  </behavior>
  <action>
    Write the spec first, observe RED. Then in StrategyTable.tsx:
    1. Delta 3 — in the name cell FIRST line (after the api-verified span, :733-739 block): `{s.status !== "published" && (<Badge type="status" label={s.status} />)}`. No visibility gate needed (a published-only row set can never fire it) — comment WHY. Muted-only rendering comes from plan 02's Badge maps.
    2. Delta 4 — in the second line (:741-748), after `<SyncBadge …/>`: when `visibility === "owner-all-statuses" && !s.analytics.computed_at`, derive `const chipState = deriveEmptySeriesState(s.analytics.computation_status ?? null, s.created_at ?? null)` (the ONE call site — import from `@/lib/closed-sets`) and render a span with the 147 BASE classes + the state's class/label pair: `"computing"` → `Syncing` (+ the title/aria-label literals), `"empty"` → `No data`. `rounded-sm` (4px) is DELIBERATE vs the adjacent Badge's `rounded-md` — carry the UI-SPEC "do NOT harmonize" comment. Gate on `visibility` so public surfaces are byte-identical (a published row awaiting recompute must not grow a chip on /discovery — the UI-SPEC States invariant).
    3. Keep the derivation inline in the row map (one site); do NOT add a helper component file.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.pending-chip.test.tsx src/components/strategy/StrategyTable.visibility.test.tsx src/components/strategy/StrategyTable.test.tsx --no-file-parallelism</automated>
  </verify>
  <done>New spec green after observed RED; visibility spec + the untouched 912-line suite still green; `grep -c 'deriveEmptySeriesState' src/components/strategy/StrategyTable.tsx` → exactly 1.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delta 5 placeholder rows for strategy-less keys</name>
  <files>src/components/strategy/StrategyTable.tsx, src/components/strategy/StrategyTable.pending-chip.test.tsx</files>
  <read_first>src/components/strategy/StrategyTable.tsx (:430-431 paged/totalPages, :460-466 emptyRowColSpan hand-count, :684-845 tbody incl. the :839-845 filter-empty row, :876-898 pagination footer), 149-UI-SPEC.md Delta 5, 149-PATTERNS.md §No Analog rows 1-2</read_first>
  <behavior>
    Extend the pending-chip spec with a placeholder describe block (fixtures: 2 ranked published rows + `placeholderKeys=[{id:"k1",exchangeLabel:"Deribit",keyLabel:"Zavara main"},{id:"k2",exchangeLabel:"MT5",keyLabel:"Acct 7001"}]` + a recorded `onFinishSetup`):
    - Two rows with className containing `bg-surface-subtle` render BELOW the ranked rows; their rank cells contain `—` and never a `#` digit; ranked rows above still show `#1`/`#2` (numbering unaffected).
    - Placeholder name cell shows `Deribit · Zavara main` and contains NO `<a>` (no factsheet link) plus a chip `No strategy yet` with className containing `bg-track`.
    - Metric cells of a placeholder are `—` and untinted; the strings `0.00`/`+0.0%` appear nowhere in the placeholder rows.
    - Clicking the `Finish setup →` button fires `onFinishSetup` exactly once; the control is a `<button>`, not a link into `/strategies`.
    - With `placeholderKeys` omitted, zero `bg-surface-subtle` rows render and the DOM equals the pre-Delta-5 shape (public invariance).
    - Filter-empty interplay: `strategies=[]` + 2 placeholders → the "No strategies match your filters." row does NOT render, the placeholders do.
  </behavior>
  <action>
    Write the spec first (RED), then:
    1. Add `placeholderKeys?: PlaceholderKeyRow[]` and `onFinishSetup?: () => void` to the props interface (export `PlaceholderKeyRow` from this file), each with a WHY-comment naming Phase 149 Delta 5 + the founder census (8 keys → 4 strategies → 2 bare keys) and stating that public pages pass neither.
    2. `const placeholders = placeholderKeys ?? [];` and `const showPlaceholders = placeholders.length > 0 && page >= totalPages - 1;` (last page, and the `totalPages === 0` empty-set case since `0 >= -1`). Placeholders live OUTSIDE `filtered`/`paged`/`rank` — `#n`, `totalPages`, and the pagination footer counts must not read them (UI-SPEC subordination rule; comment it).
    3. After the `paged.map(...)` block and BEFORE the filter-empty row, render when `showPlaceholders`: one `<tr key={p.id} className="bg-surface-subtle border-b border-border last:border-0" style={{ height: "var(--row-h)" }}>` per placeholder, mirroring the ranked-row td sequence: sticky rank td with `—` (same classes as :706, `bg-surface-subtle` variant is acceptable — keep sticky positioning classes); sticky name td containing `<span className="text-small text-text-muted">{p.exchangeLabel} · {p.keyLabel}</span>` on line 1 and the `No strategy yet` chip (147 BASE + `text-text-muted bg-track`) on line 2; five metric tds + the three `@max-3xl:hidden` tds each rendering `<span className="text-text-muted">—</span>` right-aligned; two empty sparkline tds; an empty details td; and the Actions td rendering `<button type="button" onClick={onFinishSetup} className="text-small text-accent underline underline-offset-2">Finish setup →</button>`. Match the ranked row's td class skeleton so columns align — copy the class strings, drop the hover tint and pctSuffix.
    4. Change the :839 filter-empty condition to `paged.length === 0 && placeholders.length === 0` (public pages: placeholders is `[]` → behavior unchanged).
    5. Do NOT touch `emptyRowColSpan` (:466) — placeholders render full td sequences, not a colSpan row.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.pending-chip.test.tsx src/components/strategy/StrategyTable.visibility.test.tsx src/components/strategy/StrategyTable.test.tsx --no-file-parallelism && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>Placeholder block green after observed RED; all three StrategyTable spec files green; tsc clean; `grep -n 'placeholderKeys' 'src/app/(dashboard)/discovery/[slug]/page.tsx' src/app/browse/[slug]/page.tsx` → 0 hits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server-formatted key metadata → client render | placeholder rows carry exchange/label of api_keys rows |
| shared component → public surfaces | new branches must be dead when props absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-08 | Information Disclosure | placeholder rows on public surfaces | mitigate | `placeholderKeys` is a prop the public pages do not pass (asserted by the invariance case + plan-05 gate); the data itself is the session owner's own keys, formatted server-side in plan 04 (security_requirement c) |
| T-149-09 | Information Disclosure | pending chip leaking compute state to anon | mitigate | chip branch gated on `visibility === "owner-all-statuses"` — a public row with computed_at null renders exactly as today (spec case) |
| T-149-10 | Tampering (invented data) | metric cells for uncomputed rows | mitigate | em-dash literal assertions + "never 0.00/+0.0%" negative assertions (SC-4, no-invented-data class) |
| T-149-SC | Tampering | package installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- All three StrategyTable spec files + tsc green.
- `git diff` on StrategyTable.tsx contains no change inside the `filtered` useMemo other than plan-01's (deltas are render-layer only).
- No new import of ContributionWizardOverlay in StrategyTable.tsx (`grep -c ContributionWizardOverlay src/components/strategy/StrategyTable.tsx` → 0 — the overlay stays out of the shared public bundle).
</verification>

<success_criteria>
Owner rows carry honest status/pending treatments with literal-pinned copy and classes; bare keys render census-correct unranked placeholders with a working wizard action; every new branch provably dead on public surfaces.
</success_criteria>

<output>
Create `.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-03-SUMMARY.md` when done.
</output>
