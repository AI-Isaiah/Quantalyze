---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/self-referential-oracle.test.ts
  - scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.red.ts
  - scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.green.ts
  - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md
autonomous: true
requirements: [SC-5]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The Primitive-D machine check was OBSERVED flagging src/__tests__/lint-sql-gates.test.ts:182-186 at HEAD, BEFORE that site was fixed, and the observation is a committed artifact quoting the exact command and its verbatim output — not a claim in a summary (SC-5)."
    - "The rule can fire: the SRO-01 red fixture is flagged, the green fixture is clean, and a neutered detector reddens the fixture test — observed RED, restored, restore verified by shasum (never by git checkout --)."
    - "The HEAD-wide finding count across src/__tests__/**/*.test.ts is recorded verbatim with the command that produced it — reported honestly however large, never tuned to a comfortable number — for plan 164.3.1-08 to size the blocking allowlist against."
    - "The rule is report-only in this plan by phase design: the new test file is green at HEAD (no corpus-count assertion), and NO flagged site was modified — plan 164.3.1-08 owns the :182-186 fix and the blocking flip."
    - "Member probes recorded: which of [AUDCOV-01], [MUT-W02], and the removed SP-C04 shape the rule catches and which it misses — a miss reported as a finding, not tuned away."
  artifacts:
    - "src/__tests__/self-referential-oracle.test.ts"
    - "scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.red.ts"
    - "scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.green.ts"
    - ".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md"
  key_links:
    - "self-referential-oracle.test.ts reads both fixtures via node:fs from scripts/self-referential-oracle-fixtures/ and pins the fixture-ID set exactly — a missing or extra fixture REDs the suite (house rule-quality contract, scripts/lint-sql-gates.mjs:33-40)"
    - "the corpus scan walks src/__tests__/**/*.test.ts via node:fs (NUL-safe), non-vacuity floor >= 100 files scanned (measured 127 on 2026-09-01 at 4752920d)"
    - "164.3.1-02-CALIBRATION.md is the measurement plan 164.3.1-08 consumes to size the measured-exception allowlist and to prove the rule was calibrated on the real instance before the fix"
---

<objective>
Build the Primitive-D machine check — a TypeScript-AST rule shipped as a vitest gate that flags an `expect(SUBJ)` whose subject resolves to a `const`/literal declared in the SAME block — in REPORT-ONLY mode, then CALIBRATE it at HEAD: observe and record it flagging the live self-referential instance at `src/__tests__/lint-sql-gates.test.ts:182-186` BEFORE that site is fixed, and measure the HEAD-wide RED count across `src/__tests__/**/*.test.ts` to size plan 164.3.1-08's allowlist.

**This plan fixes NOTHING.** The ordering is the point: SC-5 requires the rule to be observed firing on the real instance at HEAD, before the fix. A rule written after the fix and never observed firing on the real instance is unproven — the exact "close the example, declare the class closed" failure this phase exists to stop. Plan 164.3.1-08 consumes this plan's recorded count, sizes the allowlist, fixes the `:182-186` site, and flips the rule to blocking (PLAN-OUTLINE.md row 08).

Implements the CONTEXT.md locked decision "Primitive D — the machine check for self-referential oracles": TypeScript-AST rule as a vitest gate (regex heuristics explicitly REJECTED — a line-based reader with quote/comment blind spots is Primitive A's own defect); `typescript` ^6 already a devDependency, no new dependency; calibration is part of the deliverable.

Purpose: SC-5 second half — "the machine check for primitive D flags the `lint-sql-gates.test.ts:182-186` shape at HEAD before it is fixed."
Output: the report-only rule + red/green fixture pair + the committed calibration artifact `164.3.1-02-CALIBRATION.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-PATTERNS.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-VALIDATION.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-PLAN-OUTLINE.md
@src/lib/seam-log-coverage.test.ts
@scripts/lint-sql-gates.mjs
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

**`src/__tests__/self-referential-oracle.test.ts`** (CREATE) — the Primitive-D vitest gate, report-only in this plan. Exported symbols (plan 164.3.1-08 imports/extends these in place):
- `type SelfRefFinding` — `{ file: string; expectLine: number; subjectName: string | null; declLine: number | null; kind: "same-block-const" | "inline-literal" }`
- `findSelfReferentialExpects(fileName: string, code: string): SelfRefFinding[]` — the detector (pure; `ts.createSourceFile` + `ts.forEachChild` walk)
- `listCorpusFiles(): string[]` — the `src/__tests__/**/*.test.ts` walker (node:fs, recursive, sorted)
- `SRO_FIXTURE_DIR` — `scripts/self-referential-oracle-fixtures/`
- `SRO_FIXTURE_IDS` — exact pinned fixture-ID set, `["SRO-01-same-block-const"]` in this plan
- Report line prefixes (exported string consts so plan 08 and the artifact grep the same tokens): `SRO_FINDING_PREFIX = "SELF-REF-ORACLE finding:"`, `SRO_SUMMARY_PREFIX = "SELF-REF-ORACLE report-only summary:"`

**`scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.red.ts`** (CREATE) — the shape the rule MUST flag.
**`scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.green.ts`** (CREATE) — the repaired idiom the rule MUST pass.

**`.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md`** (CREATE) — the committed calibration record: HEAD sha, exact commands, verbatim output, the flagged `:182-186` site, the HEAD-wide count, the member-probe table, the fire-proof neuter/restore record. This is the plan's deliverable and plan 08's sizing input.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the report-only Primitive-D AST rule with its red/green fixture pair</name>
  <files>src/__tests__/self-referential-oracle.test.ts, scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.red.ts, scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.green.ts</files>
  <read_first>
    - src/lib/seam-log-coverage.test.ts:6 (import ts), src/lib/seam-log-coverage.test.ts:708-720 (parseStripped — the `ts.createSourceFile(file, code, ts.ScriptTarget.Latest, true, ts.ScriptKind.TS)` wrapper and the `ts.forEachChild` recursive collector; the repo's ONLY AST-walking vitest gate — copy this shape), :1010 (comment: `ts.createSourceFile` RECOVERS from malformed input — parse is total, never throws)
    - scripts/lint-sql-gates.mjs:33-40 (the house rule-quality contract: every rule ships a fixture PAIR — one it MUST flag, one it MUST pass; "A rule without a firing fixture does not merge")
    - 164.3.1-PATTERNS.md §8 (the composite analog: AST walk + exact-set allowlist + the :182-186 verbatim target + fixture contract)
    - src/__tests__/lint-sql-gates.test.ts:180-188 — READ ONLY, do NOT edit (plan 164.3.1-08 owns the fix). This is the live shape the red fixture models: line 182 `const banner = "-- RED FIXTURE (see the rule for the mechanism).\n";` then lines 183 and 184-186 assert `banner` directly.
    - vitest.config.ts:22-23 (INCLUDE is `src/**/*.test.{ts,tsx}` — the new gate is collected automatically; the fixtures under scripts/ are NOT, because they do not match `*.test.ts`)
    - tsconfig.json (include is `**/*.ts` excluding only node_modules/supabase/tools — MEASURED this session: the `.red.ts`/`.green.ts` fixtures under scripts/ WILL enter the `npx tsc --noEmit` program, so both fixtures must be VALID TypeScript with real vitest imports, not bare snippets)
  </read_first>
  <behavior>
    - Test "SRO-01 red fixture is flagged": parsing scripts/self-referential-oracle-fixtures/SRO-01-same-block-const.red.ts yields >= 1 finding of kind "same-block-const" whose subjectName matches the fixture's const and whose declLine/expectLine are both resolved (non-null, declLine < expectLine). This assertion CAN fail — it is the fire proof.
    - Test "SRO-01 green fixture is clean": parsing the green fixture yields exactly 0 findings.
    - Test "fixture-ID set is pinned exactly": the set of fixture basenames on disk (stripped of .red.ts/.green.ts) equals SRO_FIXTURE_IDS exactly, and every ID has BOTH members of its pair — an unpaired or unregistered fixture REDs by name (mirrors the lint-sql-gates.mjs RULES-registry discipline).
    - Test "report-only corpus scan": listCorpusFiles() returns >= 100 files (non-vacuity floor; comment in code: measured 127 files under src/__tests__/**/*.test.ts on 2026-09-01 at 4752920d — a broken walker scores 0, ordinary churn stays far above 100); every file parses (parse is total); every finding is printed one line each via SRO_FINDING_PREFIX plus a final SRO_SUMMARY_PREFIX line; findings sorted by file then expectLine for deterministic output. NO assertion on the finding count — report-only by phase design (PLAN-OUTLINE row 08 flips it blocking; this is deliberate sequencing, not scope reduction).
  </behavior>
  <action>
    Create `src/__tests__/self-referential-oracle.test.ts` implementing the CONTEXT.md locked decision "Primitive D — the machine check for self-referential oracles", copying the seam-log-coverage.test.ts parse/walk shape (its parseStripped wrapper at :708-712 and forEachChild collector at :714-720).

    Detector spec — `findSelfReferentialExpects(fileName, code)`: parse with ts.createSourceFile (ScriptTarget.Latest, setParentNodes true, ScriptKind.TS — parents are REQUIRED for the enclosing-block walk). Walk every node with ts.forEachChild. A candidate is a CallExpression whose expression is the bare identifier `expect` with at least one argument; the SUBJECT is argument 0 (the repo uses the two-argument `expect(subject, message)` form — message is ignored). Flag when either: (a) the subject is itself a StringLiteral, NoSubstitutionTemplateLiteral, or NumericLiteral — kind "inline-literal", declLine null; or (b) the subject is an Identifier that resolves, searching ONLY the statements of the nearest enclosing ts.Block (or SourceFile top level for top-level expects) that PRECEDE the expect's enclosing statement, to a `const` VariableDeclaration of that name whose initializer subtree is literal-only — kind "same-block-const" with declLine. Literal-only means: the initializer contains no Identifier, CallExpression, PropertyAccessExpression, or ElementAccessExpression anywhere in its subtree (a TemplateExpression qualifies only if every span expression is itself literal-only). Resolve all lines 1-based via sourceFile.getLineAndCharacterOfPosition(node.getStart()). Sort findings by file then expectLine.

    Scoping choice, documented in the file header per the house closing-the-example discipline (RESEARCH anti-pattern 5 — every control states what it does NOT cover): the rule resolves SAME-BLOCK declarations only, matching the detection shape the CONTEXT decision specifies and both measured instances of the class (lint-sql-gates.test.ts:182-186 and SP-C04). It does NOT resolve outer-scope or module-level constants, does NOT trace dataflow through helper calls, and does NOT inspect the matcher side — state all three in the header. Subjects that are calls into the system under test are out of scope by design (that is what makes the green fixture green).

    Corpus scan — `listCorpusFiles()`: walk `src/__tests__` recursively via node:fs readdirSync with the recursive option, filter names ending `.test.ts`, sort, return repo-relative paths. Read every file with readFileSync (node:fs is NUL-safe; the repo's known NUL-carrying file src/lib/wizardErrors.test.ts is outside this population, keep the property anyway). The scan test prints each finding as one SRO_FINDING_PREFIX line of the exact shape `SELF-REF-ORACLE finding: <relpath>:<expectLine> expect(<subjectName or "<literal>">) — <kind> declared :<declLine or "-">` and one final `SELF-REF-ORACLE report-only summary: <N> finding(s) in <F> file(s), <M> file(s) scanned` line. Assert only the non-vacuity floor (>= 100 files scanned) and finding-shape sanity (every finding has file and expectLine populated) — the count itself is NOT asserted. Ensure the gate's own expects use derived subjects (call results, property accesses) so the file does not appear in its own report.

    Fixtures, per the scripts/lint-sql-gates-fixtures/ contract (lint-sql-gates.mjs:33-40 — `rule without a firing fixture does not merge`) transposed to TS: create `scripts/self-referential-oracle-fixtures/`. Both fixtures MUST be valid TypeScript importing `expect` and `it` from "vitest" (tsconfig's `**/*.ts` include puts them in the `npx tsc --noEmit` program — measured this session; they are NOT collected by vitest because INCLUDE at vitest.config.ts:22-23 only matches `*.test.{ts,tsx}` under src/). RED fixture: an `it` block declaring a const string literal (model it on the measured :182-186 shape — a banner-style string) then asserting that same const with toContain, with a comment naming [VAC-SELFREF-01] and the modeled site. GREEN fixture: the repaired idiom — the expect subject derives from bytes produced OUTSIDE the block via an import (readFileSync of a real fixture file, e.g. scripts/lint-sql-gates-fixtures/R1-exception-handler-probe.red.sql), so the assertion can fail when the system under test changes; comment says exactly that.

    Export SelfRefFinding, findSelfReferentialExpects, listCorpusFiles, SRO_FIXTURE_DIR, SRO_FIXTURE_IDS, SRO_FINDING_PREFIX, SRO_SUMMARY_PREFIX (plan 164.3.1-08 flips this same file to blocking and needs the detector and prefixes by name). No new dependency: `typescript` ^6 and `vitest` ^4.1.2 are already installed (RESEARCH § Standard Stack; § Package Legitimacy Audit: no packages installed by this phase).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/self-referential-oracle.test.ts</automated>
    <fails_when>exit code is non-zero, or the run output does not contain a line starting "SELF-REF-ORACLE report-only summary:" naming at least 100 scanned files</fails_when>
    <automated>npx tsc --noEmit</automated>
    <fails_when>exit code is non-zero (the two fixture .ts files under scripts/ enter the tsc program via tsconfig's **/*.ts include — an invalid fixture breaks the house phase gate)</fails_when>
    <automated>npx vitest run src/__tests__/contracts</automated>
    <fails_when>exit code is non-zero — contract tests scan ALL of src/, and the new test file is in their scope; a file-scoped run of the new gate alone cannot clear them</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files` newly tracks all three created files after the task commit; the fixture pair lives under scripts/self-referential-oracle-fixtures/ (NOT under src/ — contract tests scan src/ and coverage instruments it).
    - The red-fixture test asserts kind "same-block-const" with resolved declLine and expectLine — not merely findings.length > 0.
    - The corpus-scan test has NO assertion on the finding count (report-only), and its files-scanned floor carries the measured-value comment (127 on 2026-09-01 at 4752920d) per the SC-9 measured-threshold convention.
    - The file header states the three non-coverage bounds (same-block only; no outer-scope resolution; no dataflow through calls).
    - src/__tests__/lint-sql-gates.test.ts and src/__tests__/audit-coverage.test.ts are NOT modified by this task (`git diff --name-only HEAD` does not list them) — plans 08 and 06 own those files.
  </acceptance_criteria>
  <reversibility rating="reversible">Three new files, report-only, nothing depends on them yet; removal restores HEAD behavior exactly.</reversibility>
  <done>Rule file and fixture pair committed; file-scoped vitest green with the red fixture flagged and the green fixture clean; tsc --noEmit green; contracts green; summary line printed with >= 100 files scanned; no fix applied to any flagged site.</done>
</task>

<task type="auto">
  <name>Task 2: Fire-proof — neuter the detector, observe the red-fixture test RED, restore sha-verified</name>
  <files>src/__tests__/self-referential-oracle.test.ts (temporarily neutered, restored byte-identical — net zero diff)</files>
  <read_first>
    - 164.3.1-VALIDATION.md § Anti-Vacuity Contract (byte backups verified by shasum, re-taken after every edit; ⛔ NEVER `git checkout --` in a neuter/restore harness — it restores to HEAD and silently destroys uncommitted work)
    - 164.3.1-VALIDATION.md § Per-gate neuter/restore map, row "Primitive-D AST rule" (this task delivers the plan-02 half: the rule proven able to fire; the post-fix planted-fixture half belongs to plan 164.3.1-08)
  </read_first>
  <action>
    Prove the rule can fail — a control that cannot fail is itself a Primitive-D instance (SC-1's recursive discipline, applied to this plan's own gate).

    1. Create a scratch directory OUTSIDE the repo tree (the session scratchpad; `mktemp -d` as fallback — never inside the checkout, a stray backup dirties parallel Wave-1 executors' status checks). Copy src/__tests__/self-referential-oracle.test.ts to `<scratch>/sro-backup.ts` and record `shasum -a 256` of the original into `<scratch>/sro-backup.sha`.
    2. NEUTER: edit the rule file so `findSelfReferentialExpects` returns an empty array as its first statement — the named, concrete neuter (detector blind, walker intact).
    3. Run `npx vitest run src/__tests__/self-referential-oracle.test.ts 2>&1 | tee <scratch>/sro-fireproof-red.log`. REQUIRED observation: exit code non-zero and the failing test named in output is the red-fixture test ("SRO-01 red fixture is flagged"). The corpus floor test stays green (proving the neuter hit the detector, not the walker).
    4. RESTORE by copying `<scratch>/sro-backup.ts` back over the rule file. Verify `shasum -a 256` of the restored file equals the recorded digest EXACTLY. Do NOT use `git checkout --`.
    5. Re-run file-scoped vitest, tee to `<scratch>/sro-fireproof-green.log`; must be green.
    6. Keep both logs and the digest pair in the scratch directory — Task 3 quotes them verbatim into the calibration artifact. Use the sro- filename prefix (the scratchpad is shared with concurrent agents; plan-scoped names avoid collisions).
  </action>
  <verify>
    <automated>diff <(shasum -a 256 src/__tests__/self-referential-oracle.test.ts | awk '{print $1}') <(awk '{print $1}' "$SCRATCH/sro-backup.sha")</automated>
    <fails_when>diff exits non-zero — the restored file's digest differs from the pre-neuter recording (restore incomplete or backup stale)</fails_when>
    <automated>grep -aE "SRO-01 red fixture" "$SCRATCH/sro-fireproof-red.log" && grep -acE "FAIL|✕|failed" "$SCRATCH/sro-fireproof-red.log"</automated>
    <fails_when>either grep exits non-zero — the neuter window never produced a recorded RED naming the red-fixture test (the fire proof did not happen)</fails_when>
    <automated>npx vitest run src/__tests__/self-referential-oracle.test.ts</automated>
    <fails_when>exit code is non-zero after restore — the gate is not GREEN at HEAD state</fails_when>
  </verify>
  <acceptance_criteria>
    - The RED observation exists on disk in `<scratch>/sro-fireproof-red.log` with the red-fixture test named as the failure, and the corpus-floor test green within the same RED run.
    - The restore is verified by shasum equality against the digest recorded BEFORE the neuter — not by re-reading the file or by hash of the backup alone.
    - `git status --porcelain -- src/__tests__/self-referential-oracle.test.ts` is empty after restore (byte-identical to the task-1 commit).
    - `git checkout --` appears nowhere in the executed commands.
  </acceptance_criteria>
  <done>Neuter observed RED naming the red-fixture test; restore sha-verified byte-identical; file-scoped suite green again; both verbatim logs retained for Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Calibrate at HEAD — record the :182-186 flag, the HEAD-wide count, and the member probes as a committed artifact</name>
  <files>.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md</files>
  <precondition>src/__tests__/lint-sql-gates.test.ts:182 still begins `const banner = "-- RED FIXTURE` (assert with `sed -n '182p'` before measuring). Plan ordering guarantees plans 01/03/04 do not touch this file and plan 08 — which fixes it — depends on this plan; if the line has drifted, HALT and surface it: the calibration target moved and the phase's measure-before-fix ordering is broken. Do not hunt for a new location silently.</precondition>
  <read_first>
    - 164.3.1-RESEARCH.md P12 (the target verbatim at :182-186), Open Question 3 (the HEAD-wide RED count is genuinely UNMEASURED — this task answers it), § Pattern 4 ("Calibration order is load-bearing")
    - 164.3.1-PLAN-OUTLINE.md row 164.3.1-08 (the consumer: allowlist sized from THIS count, exact-set, fail-by-name, each entry carrying its measurement)
    - .planning/phases/164.3-vacuity-a-control-that-cannot-fail-must-be-caught-by-machine/164.3-SHIP-SPECIALISTS.md:50-56 (SP-C04's shape quoted verbatim — the reconstruction source for the probe; the pre-fix file is NOT in git history, it was squash-landed already fixed in 7bcddd94)
    - src/__tests__/lint-sql-gates.test.ts:335-341 — READ ONLY ([MUT-W02] probe target: `ci` at :336 is readFileSync bytes, the single-spelling pin at :337/:341)
  </read_first>
  <action>
    Produce the plan's deliverable: the committed calibration record. The artifact carries evidence, not conclusions — every claim in it is a quoted command plus its verbatim output.

    1. Assert the precondition (sed -n '182p' shows the const). Record `git rev-parse HEAD` and `git rev-parse --short HEAD` at measurement time.
    2. THE CALIBRATION RUN: `npx vitest run src/__tests__/self-referential-oracle.test.ts 2>&1 | tee <scratch>/sro-head-scan.log`. The log MUST contain SRO findings for src/__tests__/lint-sql-gates.test.ts at expect lines 183 and 184, subject `banner`, kind same-block-const, declared :182. If the rule does NOT flag the target: the detector is wrong — fix the detector until it catches the REAL instance and re-run (that is what calibration means), then re-execute Task 2's fire-proof against the corrected detector. The opposite tuning is FORBIDDEN: never adjust the rule to shrink the corpus-wide count — a rule tuned to produce a pleasing number is a self-referential oracle. Report the count as measured, however large; if it is large, that is plan 08's sizing input, not this plan's problem to massage.
    3. MEMBER PROBES (per the phase directive: report which members of the primitive the rule catches and which it misses — a miss is a finding, not a failure):
       a. [AUDCOV-01] — read the scan log's entries (or absence of entries) for src/__tests__/audit-coverage.test.ts. The shipped SP-I01 single-line-string arms pass their consts THROUGH calls into the system under test, so the expected outcome is a MISS on those exact sites; record what was actually observed and the mechanism.
       b. [MUT-W02] — the pin at lint-sql-gates.test.ts:337-341: the subject `ci` is file-read bytes (:336), not a same-block literal; expected MISS; record observed outcome and mechanism.
       c. SP-C04 — reconstruct the removed shape verbatim from 164.3-SHIP-SPECIALISTS.md:51-52 (a const `prose` string literal asserted by two expects two lines below) as a TEMPORARY probe file `src/__tests__/spc04-probe.test.ts` (valid TS, vitest imports, the reconstructed it-block). Re-run the file-scoped gate, tee to `<scratch>/sro-spc04-probe.log`, record whether the scan flags the probe file (expected CATCH — same shape as the target). Then DELETE the probe file. It is a measurement probe, never committed: the permanent Primitive-D corpus entries are plan 164.3.1-12's deliverable, and asserting a catch here would contradict "a miss is a finding, not a failure."
    4. WRITE `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md` with sections: (i) HEAD sha + date + exact command; (ii) THE CALIBRATION OBSERVATION — the verbatim SRO finding lines for lint-sql-gates.test.ts:183 (`expect(banner).toContain("RED FIXTURE");`) and :184 plus the quoted :182-186 source, stated as observed BEFORE any fix; (iii) HEAD-WIDE COUNT — the verbatim summary line plus the full sorted finding list (every file:line), and the sentence plan 08 needs: this list is the allowlist-sizing population, each future allowlist entry must carry its measurement; (iv) MEMBER-PROBE TABLE — target / [AUDCOV-01] / [MUT-W02] / SP-C04, each row: caught or missed, the mechanism, and the probe command; (v) FIRE-PROOF RECORD — Task 2's verbatim RED excerpt naming the red-fixture test, the backup and restored shasum digests, and the statement that git checkout -- was not used; (vi) NON-COVERAGE — the rule's three stated bounds, so plan 08 sizes the allowlist knowing what the rule cannot see.
    5. NO-FIX GATE: confirm this plan modified no flagged site — `git diff --name-only HEAD` (and the final commit's file list) must contain neither src/__tests__/lint-sql-gates.test.ts nor src/__tests__/audit-coverage.test.ts; the spc04 probe must be gone. Commit the artifact (`.planning/` is tracked — git add it).
  </action>
  <verify>
    <automated>grep -a "lint-sql-gates.test.ts:183" .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md && grep -a "lint-sql-gates.test.ts:184" .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md</automated>
    <fails_when>either grep exits non-zero — the artifact does not record the rule flagging both expect lines of the calibration target at HEAD</fails_when>
    <automated>grep -a "SELF-REF-ORACLE report-only summary:" .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md && grep -acE "SP-C04|MUT-W02|AUDCOV-01" .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md</automated>
    <fails_when>either grep exits non-zero — the artifact lacks the verbatim HEAD-wide summary line or the three member-probe rows</fails_when>
    <automated>git diff --name-only HEAD -- src/__tests__/lint-sql-gates.test.ts src/__tests__/audit-coverage.test.ts | grep -a . ; test $? -eq 1</automated>
    <fails_when>the pipeline exits non-zero because the grep found output — a plan-08/06-owned file was modified by this plan (the no-fixes rule is broken)</fails_when>
    <automated>git status --porcelain | grep -a "spc04" ; test $? -eq 1</automated>
    <fails_when>the pipeline exits non-zero because grep found a match — the temporary SP-C04 probe file was left behind</fails_when>
  </verify>
  <acceptance_criteria>
    - The artifact is committed and quotes commands + verbatim output for every claim: the calibration observation (both expect lines, subject, declLine), the summary line with the exact HEAD-wide count, the full sorted finding list, three member-probe rows with mechanisms, and the fire-proof digests.
    - The artifact states explicitly that the observation was made at the recorded HEAD BEFORE any fix to any flagged site, and names plan 164.3.1-08 as the consumer that sizes the allowlist from the recorded list.
    - The HEAD-wide count is reported as measured — the SUMMARY of this plan repeats the number verbatim from the artifact, and no detector change was made after the final calibration run without re-running Task 2's fire-proof.
    - src/__tests__/spc04-probe.test.ts does not exist in the tree or in any commit (`git log --all --oneline -- src/__tests__/spc04-probe.test.ts` is empty).
  </acceptance_criteria>
  <done>164.3.1-02-CALIBRATION.md committed with the recorded :182-186 flag at HEAD, the honest HEAD-wide count, the member-probe table, and the fire-proof record; no flagged site fixed; no probe residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan ships CI test tooling with no user-facing surface, no network egress, no database access, and no secrets. The honest boundaries are internal to the control system itself:

| Boundary | Description |
|----------|-------------|
| future test authors → the gate | hand-authored test code (the class Primitive D recurs through) crosses into the scanned corpus |
| the measurement → the record | the calibration's evidentiary value depends on it being taken at HEAD before the fix — a post-fix recording would be unfalsifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-02-01 | Tampering | future self-referential oracles in src/__tests__ | low | accept | The rule detects but does not block in this plan — by phase design, not omission: PLAN-OUTLINE row 08 flips it blocking with the allowlist sized from THIS plan's measurement. Acceptance window is one wave. |
| T-164.3.1-02-02 | Repudiation | 164.3.1-02-CALIBRATION.md | medium | mitigate | Artifact records `git rev-parse HEAD` at measurement time plus verbatim command output; Task 3's no-fix gate proves neither lint-sql-gates.test.ts nor audit-coverage.test.ts was touched by this plan, so the "before the fix" claim is machine-checkable from the git history. |
| T-164.3.1-02-03 | Information Disclosure | scanned test-file contents echoed into a tracked artifact | low | accept | The repo is public and .planning/ is tracked already; the artifact quotes only file:line findings and already-public test source. No secrets are in the scan population. |
| T-164.3.1-02-SC | Tampering | npm/pip/cargo installs | low | accept | NO packages are installed by this plan (RESEARCH § Package Legitimacy Audit: none; `typescript` ^6 and `vitest` ^4.1.2 already present). No install task exists, so no legitimacy checkpoint is required. |

No threat reaches `high`; nothing blocks under ASVS L1.
</threat_model>

<verification>
Overall phase checks for this plan, run after Task 3 (VALIDATION.md § Sampling Rate):

1. `npx vitest run src/__tests__/self-referential-oracle.test.ts` — green (report-only gate holds at HEAD). Fails when: non-zero exit.
2. `npx vitest run src/__tests__/contracts` — green (src/ was touched; file-scoped runs cannot clear the contracts). Fails when: non-zero exit.
3. `npx tsc --noEmit` — green (fixtures are in the tsc program). Fails when: non-zero exit.
4. Node-22 pass on the changed test file (house rule — CI is Node 22, local is 25): `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run src/__tests__/self-referential-oracle.test.ts`. Fails when: non-zero exit under Node 22 while green under local node.
5. The calibration artifact exists, is committed, and its recorded HEAD sha matches a commit in which lint-sql-gates.test.ts:182 still carries `const banner` (`git show <sha>:src/__tests__/lint-sql-gates.test.ts | sed -n '182p'`). Fails when: the sed line does not begin with `const banner`.

Serialization note: do not run the full vitest suite concurrently with other Wave-1 executors (contention manufactures timeout regressions — measured); the file-scoped + contracts runs above are this plan's lane.
</verification>

<success_criteria>
- SC-5 (second half, measure-first portion): the Primitive-D machine check exists as a vitest gate and WAS OBSERVED flagging src/__tests__/lint-sql-gates.test.ts:182-186 at HEAD before the site was fixed — evidenced by the committed 164.3.1-02-CALIBRATION.md quoting the command and verbatim output, with the fix verifiably absent from this plan's commits.
- The HEAD-wide RED count across src/__tests__/**/*.test.ts is recorded honestly (Research Open Question 3 answered by measurement), with the full finding list plan 164.3.1-08 sizes its exact-set allowlist from.
- The rule ships with a red and a green fixture proving it can fire, per the house contract, and the fire-proof (neuter → RED naming the red fixture → sha-verified restore) is on record.
- Member probes for [AUDCOV-01], [MUT-W02], and SP-C04 are recorded with catch/miss and mechanism — misses reported as findings, not tuned away.
- Nothing this plan touched overlaps Wave-1 siblings: no changes to scripts/mutation-runner/**, src/__tests__/audit-coverage.test.ts, scripts/sql-function-names-naive.mjs, scripts/sql-body-normalize.mjs, or src/__tests__/lint-sql-gates.test.ts.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-SUMMARY.md` when done. The SUMMARY must repeat the HEAD-wide finding count verbatim from the calibration artifact and link plan 164.3.1-08 to `164.3.1-02-CALIBRATION.md` as its allowlist-sizing input.
</output>
