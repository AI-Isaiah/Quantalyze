---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "06"
type: execute
subsystem: audit-coverage
tags: [audcov-01, quote-state, live-regression, primitive-d]
wave: 2
depends_on: ["164.3.1-03"]
files_modified:
  - src/__tests__/audit-coverage.test.ts
autonomous: true
requirements: [SC-5]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "Case A — a multi-line template literal containing /* on a continuation line with a real mutation below it — now finds its site: re-running plan 03's committed, UNEDITED harness prints AUDCOV-CAL A=[5] B=[5] C=[3], where the recorded before-state was A=[] B=[5] C=[3]. Movement on exactly one row, measured with the same instrument (SC-5)."
    - "The fix is the prescribed one: quote state carried ACROSS lines through unmatchedBlockOpen/stripBlockComments, with the carried quote forced to null at end-of-line for single and double quotes — only the backtick legally spans lines in TS (ROADMAP deliverable 4, locked shape)."
    - "The A/B/C arms ship IN the test file as multi-line real-shape inputs calling the file's own real functions — case B is the calibration control (still [5] post-fix) and case C fences the shipped single-line behavior (still [3]) — so 'A finds its site' is falsifiable in both directions (Pitfall 3)."
    - "Post-fix, the quote-carry was NEUTERED (per-line state restored), case A observed regressing to [], then restored sha-verified — the fix is proven load-bearing, not incidental (VALIDATION row 'stripBlockComments fix')."
    - "The full audit-coverage suite is green: the H-0001 gate over the real src/ corpus either finds no new sites, or every newly-visible uncovered mutation site is dispositioned by name (covered, or added to H_0001_UNCOVERED_ALLOWLIST with its justification) — a less-blind stripper widening the gate's real findings is a FINDING to disposition, never a reason to weaken the fix."
  artifacts:
    - "src/__tests__/audit-coverage.test.ts — cross-line quote state in unmatchedBlockOpen/stripBlockComments + the three-arm A/B/C table with B as control"
  key_links:
    - "post-fix movement proof ← .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs re-run UNEDITED (instrument identity is what makes before→after comparable)"
    - "before-state ← 164.3.1-03-CALIBRATION-AUDCOV.md (committed by plan 03: A=[] B=[5] C=[3] with byte bindings)"
---

<objective>
Fix [AUDCOV-01] — the one item in this phase carrying a LIVE regression on main. The SP-I01 fix
made `stripBlockComments` per-line: `unmatchedBlockOpen`'s quote state is discarded at every
newline, so line 2's opened template literal is forgotten, line 3's interior `/*` opens a phantom
block, and everything below is blanked — main's H-0001 audit-coverage gate is BLINDER than the
pre-fix code, which found site A.

The fix is locked (ROADMAP deliverable 4): carry string state across lines, with `quoteAtEnd`
forced to null for `'` and `"` (they cannot legally span a line in TS; only the backtick can).
Plan 164.3.1-03 committed the before-state at pre-fix HEAD — `AUDCOV-CAL A=[] B=[5] C=[3]`,
measured through the file's own extracted bytes with blob-hash bindings — and a verdict-free
harness this plan re-runs UNEDITED for the after-leg. A post-fix table against a reconstructed
"before" would be exactly the unchecked claim this phase refuses.

Purpose: SC-5 first half — "stripBlockComments finds the multi-line-template site (case A) while
the B control still passes."
Output: the cross-line quote-state fix, the shipped three-arm A/B/C table, the before→after
movement record, and the neuter/restore proof that the quote-carry is load-bearing.

Scope fence: this plan edits `src/__tests__/audit-coverage.test.ts` and NOTHING else. Plan 03's
harness and calibration artifact are read and re-run, never edited. Wave-2 siblings own
`scripts/mutation-runner/**` + `scripts/pg-lane/**` (05), `scripts/prod-body-drift-check.sh` +
`src/__tests__/drift-check-scripts.test.ts` (07), and `src/__tests__/lint-sql-gates.test.ts` +
the SRO rule (08) — touch none of them.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@src/__tests__/audit-coverage.test.ts

Read at execution time — created by plan 03, so they do not exist at planning time and are
deliberately not @-referenced (plan-anchor-verify checks pending plans against the current tree):
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-CALIBRATION-AUDCOV.md
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-SUMMARY.md

Measured anchors at pre-phase HEAD b4bdfc1e (this file is untouched by Wave 1, so they hold at
this plan's start; re-grep before editing regardless): unmatchedBlockOpen at
`src/__tests__/audit-coverage.test.ts:159-206` (`stripBlockComments` begins at 180; its per-line
call of unmatchedBlockOpen sits at 195), findMutations at 239, stripLineComment at 432, the
shipped SP-I01 string-arm at 1167-1181, H_0001_UNCOVERED_ALLOWLIST at 1495 with its exact-set
check at 1625-1650.
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL (ROADMAP deliverable 4) | Quote state across lines, quoteAtEnd forced null for ' and "; three-arm table with B as calibration control | Tasks 1-2 |
| REQ [SC-5] | Case A finds its site while B still passes; C fences shipped behavior | Task 1 (fix + arms), Task 2 (movement + neuter proof) |
| CONTEXT § Specifics | [AUDCOV-01] must not merge as-is; measured A/B/C = []/[5]/[3] | Task 1 |
| RESEARCH Pitfall 3 | Arms are multi-line real-shape inputs; fixtures must not agree by construction | Task 1 |
| VALIDATION row "stripBlockComments / unmatchedBlockOpen" | Post-fix neuter of the quote-carry → A regresses to [], observed, restored | Task 2 |
| Plan 03 contract | The after-leg re-runs the SAME committed instrument unedited | Task 2 |

Excluded (not gaps): the Primitive-D machine check over this file's arms (plan 08's rule scans it;
plan 02 recorded the member probe); corpus consolidation (plan 12).
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cross-line quote state in unmatchedBlockOpen/stripBlockComments + the shipped A/B/C arm table</name>
  <files>src/__tests__/audit-coverage.test.ts</files>
  <precondition>Plan 164.3.1-03's artifact exists and records the before-state: grep -F "AUDCOV-CAL A=[] B=[5] C=[3]" .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-CALIBRATION-AUDCOV.md exits 0. If it does not, HALT — the measure-before-fix ordering is broken; do not reconstruct a before-state from memory.</precondition>
  <read_first>src/__tests__/audit-coverage.test.ts:130-206 (the SP-I01 header comment explaining the old restriction, unmatchedBlockOpen's per-line quote state — the defect — and stripBlockComments' line loop) and :425-442 (stripLineComment, which also quote-tracks per line and must stay consistent with the carried state) and :1167-1181 (the shipped string-arm whose behavior case C fences); 164.3.1-03-CALIBRATION-AUDCOV.md (the exact case A/B/C line arrays — reuse them verbatim as the arm inputs); 164.3.1-RESEARCH.md Premise P11 (the measured mechanism: phantom block from forgotten backtick state) and § Architectural Responsibility Map row "Audit-coverage stripper fix"</read_first>
  <behavior>
    - New arm "case A ([AUDCOV-01]): a /* inside a MULTI-LINE template no longer blanks the file" — the plan-03 case-A line array driven through the file's own findMutations → sites [5]. This arm FAILS at pre-fix HEAD (sites []) — write it first, observe RED, record the verbatim failure text.
    - New arm "case B (calibration control): the same input without the /* still finds its site" — plan-03 case-B array → sites [5], passing both before AND after the fix (its value is that it cannot move).
    - New arm "case C (shipped single-line behavior fenced): a /* inside a single-line template stays quoted" — plan-03 case-C array → sites [3].
    - All pre-existing describes in the file still pass, including the SP-I01 string arms at :1167-1181 and the H-0001 exact-set checks.
  </behavior>
  <action>
    TDD order: add the three arms (a new describe adjacent to the existing SP-I01 arms; inputs are
    the verbatim line arrays from the plan-03 artifact, joined with newline), run the file-scoped
    suite, observe case A RED with sites [] — the live regression reproduced inside its own gate —
    and capture the verbatim failure text for the SUMMARY. Cases B and C must be green pre-fix
    (they are the controls; if either is red pre-fix, STOP — the before-state has drifted and
    plans 03/06 need re-grounding).

    Then implement the locked fix. Rework unmatchedBlockOpen so quote state ENTERS and EXITS the
    function: it accepts the quote state carried from the previous line and returns both its
    unmatched-open index (or -1) and the quote state at end-of-line. stripBlockComments threads
    that state through its line loop; after each line, force the carried state to null when it is
    a single or double quote — only the backtick legally spans lines in TS, so a dangling ' or "
    at end-of-line is a within-line artifact, not an open span (state this rationale in a comment
    beside the forcing, citing [AUDCOV-01] and the pre-fix blindness it closes). Keep the
    escape-handling (backslash skip) semantics. Update the SP-I01 header comment at :136-152: the
    old text justifies the per-line design this task removes — append the 2026-09 supersession
    with the measured A/B/C mechanism, preserving the history. stripLineComment (:432) keeps its
    own within-line quote tracking — it strips trailing -- er, line comments on a single line and
    receives already-block-stripped text; confirm and state in a comment that its per-line scope
    remains correct because block state is resolved before it runs.

    Then run the FULL audit-coverage suite. A less-blind stripper may surface uncovered mutation
    sites in real src/ files that the blind stripper missed — the H-0001 exact-set check will name
    them. Every newly-visible site is a FINDING: disposition each by name (write the covering
    audit assertion, or add it to H_0001_UNCOVERED_ALLOWLIST with a justification comment carrying
    the discovery mechanism — found by the [AUDCOV-01] fix, date), and record the list in the
    SUMMARY. Weakening the fix to avoid new findings is forbidden — that is closing the example
    while re-opening the class.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/audit-coverage.test.ts</automated>
    <fails_when>exit code is non-zero or any test fails — the A/B/C arms, every pre-existing SP-I01 arm, and the H-0001 exact-set checks must all pass together post-fix</fails_when>
    <automated>npx vitest run src/__tests__/contracts</automated>
    <fails_when>exit code is non-zero — src/ was touched and contract tests scan all of src/; a file-scoped run cannot clear them</fails_when>
  </verify>
  <acceptance_criteria>
    - Case A was observed RED (sites []) at pre-fix state with verbatim failure text captured, and is GREEN (sites [5]) post-fix; B and C unchanged in both states
    - unmatchedBlockOpen carries quote state in and out; stripBlockComments threads it; the null-forcing for ' and " carries its rationale comment naming [AUDCOV-01]
    - The SP-I01 header comment is appended-to, not rewritten — the measured history survives
    - Any newly-visible H-0001 site is dispositioned by name with its justification; none silently absorbed; the fix was not weakened
    - git status --porcelain shows changes ONLY in src/__tests__/audit-coverage.test.ts
  </acceptance_criteria>
  <done>The stripper carries quote state across lines, case A finds its site while B and C hold, the arms ship in the gate itself, and every consequence of the widened vision is dispositioned by name.</done>
</task>

<task type="auto">
  <name>Task 2: Before→after movement via plan 03's unedited instrument + the quote-carry neuter/restore proof</name>
  <files>src/__tests__/audit-coverage.test.ts</files>
  <read_first>164.3.1-03-CALIBRATION-AUDCOV.md § "What plan 164.3.1-06 must show" (the expected movement: A [] → [5], B stays [5], C stays [3], measured with the same unedited instrument); .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs (the instrument — confirm it is byte-identical to plan 03's commit before running: git status --porcelain on it must be empty); 164.3.1-VALIDATION.md § Anti-Vacuity Contract (byte backups verified by shasum, re-taken after every edit; never git checkout -- in a neuter/restore harness)</read_first>
  <action>
    AFTER-LEG: run the plan-03 harness UNEDITED from the repo root and capture stdout verbatim to
    the session scratchpad. REQUIRED: the summary line reads AUDCOV-CAL A=[5] B=[5] C=[3]. If it
    reads anything else, STOP and record the discrepancy as a blocker in the SUMMARY — either the
    fix is wrong (A not [5]), the control broke (B moved — the stripper now over-returns), or the
    shipped behavior regressed (C moved). Do not edit the instrument to make the numbers agree;
    instrument identity is the point.

    NEUTER/RESTORE (VALIDATION row — proves the quote-carry is the load-bearing mechanism): take a
    byte backup of src/__tests__/audit-coverage.test.ts, record shasum -a 256. Neuter by severing
    the carry — make stripBlockComments pass a fresh null quote state to every line (the named,
    minimal neuter; leave everything else intact). Run the file-scoped suite: REQUIRED observation
    — the case-A arm REDs (sites [] again) while case B stays green in the same run (proving the
    neuter hit the carry, not the detector). Capture the verbatim RED. Restore from the byte
    backup; verify by shasum equality AND by re-running the harness (A=[5] again) — confirm the
    restore by observing the newest fix live, not by hash alone. Never git checkout -- here.

    Record in the SUMMARY: the before line (quoted from the plan-03 artifact), the after line
    (from this task's run), the neuter RED excerpt, the shasum pair, and the statement that the
    instrument was re-run byte-identical (its git status clean).
  </action>
  <verify>
    <automated>OUT="$(node .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs)" && printf '%s\n' "$OUT" | grep -F "AUDCOV-CAL A=[5] B=[5] C=[3]"</automated>
    <fails_when>node exits non-zero (extraction failed against the edited file — a declaration was renamed or removed) or grep exits 1 — the post-fix measurement does not show A=[5] B=[5] C=[3]: the movement claim fails in one of its three falsifiable directions</fails_when>
    <automated>[ -z "$(git status --porcelain -- .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs)" ] && npx vitest run src/__tests__/audit-coverage.test.ts</automated>
    <fails_when>the bracket test fails (the instrument was edited — before/after are no longer comparable) or vitest exits non-zero (the restored file is not green — the neuter harness left residue)</fails_when>
  </verify>
  <acceptance_criteria>
    - The after-leg used the byte-identical plan-03 instrument (porcelain clean on the harness) and printed A=[5] B=[5] C=[3]
    - The quote-carry neuter was observed reverting case A to [] with case B green in the same run; restore proven by shasum AND a green re-run of both the suite and the harness
    - The SUMMARY carries before + after lines, the neuter RED verbatim, and the shasum pair — the full movement record SC-5 needs
  </acceptance_criteria>
  <done>Movement is proven with the same committed instrument on both legs, and the fix is proven load-bearing by neuter and sha-verified restore.</done>
</task>

</tasks>

<threat_model>
This plan edits one vitest gate file. No network, no database, no packages, no user-facing surface.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future src/ code → the H-0001 audit-coverage gate | the gate's stripper decides which mutation sites are VISIBLE to coverage auditing; a blind spot converts directly into unaudited write paths |
| the before-record → the movement claim | the fix's evidence rests on plan 03's committed byte-bound artifact; an edited instrument would make movement unfalsifiable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-06-01 | Tampering | stripBlockComments blind spot hiding real mutation sites | high | mitigate | the cross-line quote-carry fix, pinned by the in-gate A/B/C arms (A observed RED first) and proven load-bearing by the neuter/restore in task 2 |
| T-164.3.1-06-02 | Repudiation | movement claimed against a reconstructed before-state | medium | mitigate | before-leg is plan 03's committed artifact with blob-hash bindings; after-leg re-runs the byte-identical instrument (porcelain-checked) |
| T-164.3.1-06-03 | Tampering | fix weakened to dodge newly-visible H-0001 findings | medium | mitigate | task 1 forbids weakening; every new site dispositioned by name via the exact-set allowlist discipline, recorded in the SUMMARY |
| T-164.3.1-06-SC | Tampering (supply chain) | npm installs | low | accept | zero packages installed by this plan; no install task exists |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 06)

| Artifact | Kind | Location | Consumers |
|---|---|---|---|
| Cross-line quote state in `unmatchedBlockOpen` / `stripBlockComments` | fixed gate internals (signatures extended to carry quote state) | `src/__tests__/audit-coverage.test.ts` | the H-0001 gate itself; plan 12's instance→arm registry |
| A/B/C arm table (A = the [AUDCOV-01] pin, B = calibration control, C = shipped-behavior fence) | new describe with three arms | `src/__tests__/audit-coverage.test.ts` | CI vitest shards; plan 12 references these as the [AUDCOV-01] corpus entry |
| Before→after movement record + neuter/restore proof | SUMMARY evidence block | `164.3.1-06-SUMMARY.md` | phase verification for SC-5 |
| No new files, flags, or dependencies | — | — | — |
</artifacts_produced>

<verification>
1. `npx vitest run src/__tests__/audit-coverage.test.ts` — green post-fix (A/B/C + all pre-existing arms + H-0001 exact-set). Fails when: non-zero exit.
2. `node .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs` prints `AUDCOV-CAL A=[5] B=[5] C=[3]`, instrument byte-identical. Fails when: any other summary line, or porcelain non-empty on the harness.
3. `npx vitest run src/__tests__/contracts` — green (src/ touched). Node-22 pass on the changed file (`PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run src/__tests__/audit-coverage.test.ts`). Fails when: non-zero exit under either runtime.
4. Fence: this plan's commits touch only `src/__tests__/audit-coverage.test.ts` (plus `.planning/` summary) — `git show --stat` proves it.
</verification>

<success_criteria>
- SC-5 first half delivered: case A finds its site ([5]) while the B control still returns [5] and
  shipped arm C still returns [3] — movement on exactly one row, proven against plan 03's
  committed before-state with the same unedited instrument.
- The quote-carry is proven load-bearing (neuter → A regresses to [] with B green → restore
  sha-verified), and the arms ship inside the gate so the regression class reds in CI next time.
- The H-0001 gate is now STRICTLY less blind than main: no newly-visible finding silently
  absorbed, none dodged by weakening.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-06-SUMMARY.md`
when done. It must quote: the before line from plan 03's artifact and the after line from this
plan's run; case A's pre-fix RED and the neuter-RED verbatim; the shasum restore pair; and the
disposition list for any newly-visible H-0001 sites. If the harness printed anything other than
A=[5] B=[5] C=[3] post-fix, that is a BLOCKER entry, not a pass with a caveat.
</output>