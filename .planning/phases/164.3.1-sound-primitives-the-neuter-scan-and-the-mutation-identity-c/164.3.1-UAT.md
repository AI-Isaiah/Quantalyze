---
status: complete
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
source: [164.3.1-VERIFICATION.md]
started: 2026-09-02T09:17:06Z
updated: 2026-09-02T16:05:00Z
---

## Current Test

[testing complete]

## Tests

### 1. First real `sql-mutation` run on its ubuntu host
Read the job log for `SELF-TEST 12/12`, the corpus summary lines (`coverage:` / `arms:` / `biting:` / `lane-invocations:`), and the count-recheck step's closing sentence.
expected: 12/12 self-test PASSED; `arms: 30/30/0`, `biting: 30`, `lane-invocations: 30`; count-recheck echoes "both floors hold and the runner's two tallies agree". A differing psql grammar on ubuntu must surface as a named MEASURE_FAIL, never a quiet pass. (WINDOWS.md 28 — never executed on ubuntu.)
result: pass
observed: "Driven by the orchestrator 2026-09-02: `gh workflow run ci.yml --ref phase-164.3.1-sound-primitives` → run 33620169220 at 89cbef8b, ubuntu-latest, job sql-mutation id 100215130019 conclusion=success. Log: `SELF-TEST 1/12`..`12/12` each ok, `SELF-TEST PASSED: both floor modes, the wrong-identity mode and MEASURE_FAIL all fire, and the 164.3.1-11 regression corpus holds`; `coverage: files 1/71`; `arms: 30/30/0`; `biting: 30`; `lane-invocations: 30 (… plus 1 baseline / 1 restore leg(s))`; count-recheck: `Coverage print is present, both floors hold and the runner's two tallies agree: files 1 >= 1, biting arms 30 >= 30 (of 30 executed, 30 arm lane(s) spawned).` No MEASURE_FAIL emitted. First execution of this job on its real host — closes WINDOWS.md 28.

⚠️ **SHA-BINDING CORRECTED 2026-09-02T16:23Z (re-verification at HEAD e30f7c33).** The run cited above is at `89cbef8b`, which **PREDATES the entire ship-review fix pass** (CR-01, WR-01..04, IN-01..09 — `run.mjs` +751, `prod-body-drift-check.sh` +364, `parse.mjs` +145). Carrying it forward as this criterion's evidence would be the ancestor-SHA trap: a green board that belongs to an ancestor, not to the shipped tree. The CORRECT binding for the code that actually shipped is **CI run 33643046061 at `60b0722d`** (PR #730), where `git diff 60b0722d..HEAD` outside `.planning/` is empty — and whose `sql-mutation` job reports **SELF-TEST 15/15**, not 12/12, the three extra modes being the ones the review pass added. The 89cbef8b run remains valid for what it uniquely proves (first-ever execution on the ubuntu host, closing WINDOWS.md 28); it is NOT the evidence that the shipped primitives are sound. Corpus figures are unchanged between the two runs (`files 1/71`, `arms 30/30/0`, `biting 30`, `lane-invocations 30`, tallies agree), independently re-measured at HEAD by the verifier on a throwaway pg-lane cluster." 

### 2. First live VAC-04 (`migration-drift-check.yml`, real PROD credential) and VAC-08 (`sql-tests` with `TEST_SUPABASE_DB_URL`) runs
expected: VAC-04: migration PR with no function → the D-13 `::notice:: … the zero is LEGITIMATE` path; PR with a visible definition → normal compare verdict; absurdity floor SILENT (index ≥ half the 118-body snapshot). VAC-08: `self-test OK (5/5)`, then a ledger verdict with the shape diagnostic, floor silent on the real TEST ledger. ⛔ Do not merge a migration PR between 164.3.1 landing and 164.4 landing — a block at the fail-closed zero path is the gate working. (WINDOWS.md 25/26.)
result: pass
observed: "Both gates ran live on the phase's ship PR #730 (`phase-164.3.1-sound-primitives-pr` → main, merged 2026-09-02T14:59:55Z, merge 35c0418f). Read by the orchestrator 2026-09-02.

**VAC-08 — COMPLETE.** ci.yml `sql-tests` job 100297358657 (run 33643046061), conclusion success, real `TEST_SUPABASE_DB_URL`. Emitted: `ledger presence: 32 absent, all 32 baselined (see scripts/vac08-ledger-baseline.txt); 0 NEW drift.`; `advisory: 2 ledger row(s) have no repo file (squashes / CLI-era rows). Not a failure.`; the shape diagnostic `VAC-08 repo-vs-TEST ledger and body drift gate: 262 migration(s) checked against the ledger, 4 body comparison(s) over 3 named function(s).`; `::notice::… ledger and body checks clean.` Both floors SILENT on the real TEST ledger — `All 71 SQL self-tests passed … 8 completion sentinel(s) verified, covering 166 declared arms` against SENTINEL_FLOOR=8 / ARMS_FLOOR=166. `self-test OK (5/5)` did NOT appear in CI: ci.yml:2051 invokes the script bare (`check`), so the `--self-test` arm never runs there. Run by the orchestrator locally at HEAD instead — `bash scripts/test-ledger-drift-check.sh --self-test` → all five arms `ok` (missing-ledger-row RED, body-mismatch RED, empty-body-check-list RED, zero-comparisons RED, green path), `self-test OK (5/5 arms — all four red modes fire and the green path passes)`, exit 0. CI wiring gap booked to TODOS.md.

**VAC-04 — PARTIAL, and the residue is named.** migration-drift-check run 33643046189, event `pull_request`, head 60b0722d, conclusion success. It ran against the REAL PROD credential — `::notice::Drift-check credentials present.` with `SUPABASE_DB_PASSWORD: ***` in env — which closes the WINDOWS.md 25 window (the gate had never executed against its real credential). Also reached `::notice::No drift - Remote database is up to date.` ⚠️ It then took the EARLIEST short-circuit, `prod-body-drift-check.sh:198`: `::notice::VAC-04 repo-vs-PROD function-body drift gate: this PR changes no migration files — nothing to compare against PROD.` PR #730 touched no `supabase/migrations/**` (it matched the paths filter on VAC-04's own script entries), so the three arms this test names were NOT exercised: the D-13 LEGITIMATE-ZERO branch (:448 `no functions defined, and the zero is LEGITIMATE`), the normal compare verdict, and the absurdity floor (:608+). Those need a migration-bearing PR. Booked to TODOS.md. The standing instruction on this line still holds: do NOT merge a migration PR between 164.3.1 landing and 164.4 landing." 

### 3. Read the 17 DIAGNOSTIC_FIRST_ALLOWLIST reasons and the 2 SRO_ALLOWLIST entries
Files: gate-family-meta.test.ts (classes PRECONDITION / REDACTED / EMPTY-BY-COND / READER-STDERR); self-referential-oracle.test.ts:150-196; types-design-tests.test.ts:27/:63.
expected: Each reason is TRUE of its site, not merely plausible; READER-STDERR/REDACTED entries are acceptable only because the grep_rc follow-up (capture the child's exit code) is booked — it is NOT booked yet (VERIFICATION W1).
result: pass
observed: "Read at 89cbef8b by the orchestrator. All 17 DIAGNOSTIC_FIRST_ALLOWLIST reasons checked against their sites: the 4 READER-STDERR entries (:207 `--function-names`, :221 naive, :428 `--function-qualified-names`, :430 naive `--qualified`) each redirect ONLY stdout to $TMP and leave stderr on the log before `|| fail` — true; the REDACTED index entries (:472/:475) redirect stderr to `$TMP/prod-names.err` and the test-ledger presence query (:301) withholds psql output — true; the EMPTY-BY-COND entries (:482/:484, BODY_CHECK_FUNCTIONS) sit behind a measured-empty branch — true; PRECONDITION entries name the unset variable/binary — true. SRO_ALLOWLIST: `empty` (types-design-tests.test.ts:27) and `_kinds` (:63) are genuinely type-annotation carriers whose runtime expect cannot fail — reasons true, `measured` cites 164.3.1-02-CALIBRATION § III. Caveat holds as stated: the READER-STDERR follow-up (capture child exit code with the grep_rc idiom at the 7 `|| fail` sites) is NOT yet booked in TODOS.md — booked by the orchestrator in this session (see TODOS.md entry added after the plan-13 review fix pass)." 

### 4. (Optional, repeatable) Re-run plan 10's severed-tally harness
Byte-backup scripts/mutation-runner/run.mjs, change `laneTally[leg] += 1` → `if (leg !== "arm") laneTally[leg] += 1`, run the full corpus, restore from the backup, confirm sha256 matches HEAD.
expected: exit 1 with an `absurdity` defect `executed=30 lane-invocations=0 biting=30` while `arms:`/`biting:` still print healthy; restore → exit 0.
result: pass
observed: "Run by the orchestrator 2026-09-02 at 539dd87a (post review-fix tree; the tally line moved to run.mjs:1396). Byte backup, pre sha256 e6e6042d414a6a53…; neuter = the plan-10 one-liner. FIRE: exit 1; `arms: 30/30/0`, `biting: 30` printed healthy, `lane-invocations: 0`, then `❌ 1 defect(s)` kind `absurdity`: `MEASURE_FAIL — … the verdict loop counted 30 executed arm(s) but the lane runner spawned only 0 arm lane(s) (executed=30 lane-invocations=0 biting=30)`. Restore from backup: post sha256 e6e6042d414a6a53… identical, `git status --short` clean, 0 `NEUTER(` markers. SILENT: exit 0, `lane-invocations: 30`, `✅ No defects`. Local throwaway cluster via scripts/pg-lane (Homebrew PG16), no shared DB." 

## Summary

total: 4
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps
