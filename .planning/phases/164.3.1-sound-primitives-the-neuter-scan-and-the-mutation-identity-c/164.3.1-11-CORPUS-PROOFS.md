# 164.3.1-11 — Recursive SC-1 proofs: every corpus entry RED under the neuter of its fix

**Requirement:** SC-1 — *every measured instance across the primitives is an executable arm in the
regression corpus, **each proven to RED by neutering its fix** and observing the failure.* CONTEXT
§ Specifics makes it recursive: *a corpus entry that cannot fail is itself a Primitive-D instance.*

This file is the record. Everything below was OBSERVED on a real throwaway cluster, not argued.
Each cycle: byte backup → neuter applied by an exact-once replacement (refused otherwise) →
narrowed debug run (the runner's own verdict lines) → full `--self-test` → RED-set parse →
restore from the backup → `shasum -a 256` equality **and** newest-edit grep **and** zero
`NEUTER(` markers → green re-run. Never `git checkout --`. Harness and neuter specs lived in the
session scratchpad; the four specs are quoted verbatim below.

---

## 1. Provenance

| Field | Value |
|---|---|
| Tree at proof time | `95109cba` (Task 1 `8b407031`, Task 2 `42f088bd`, fixture hygiene `95109cba`) |
| Date | 2026-09-01 / 02 (cycles ran across midnight local) |
| Host | macOS, arm64; PostgreSQL 16.13 (Homebrew) via `scripts/pg-lane/run.sh` throwaway `initdb` clusters — **never** shared TEST, **never** PROD; no `supabase` CLI command was run |
| `run.mjs` sha256 — GREEN, identical before and after **every** cycle | `c1d247fbae9a1b769d629e99fab8378f7dda0d66a181a2e38ebb4fea3e7f05e3` |
| Self-test denominator | 12 scenarios; the four corpus entries are 9, 10, 11, 12 |

The RED-set of a run is the set of scenario numbers followed by a `SELF-TEST FAIL:` line. A cycle
is accepted only when its entry's scenario is in the set **and** every scenario the neuter should
not touch is absent from it — the specificity half is measured, not assumed.

| Cycle | Entry | Fix neutered (file · symbol) | `run.mjs` sha256 neutered | RED scenarios | Required | Forbidden-but-red |
|---|---|---|---|---|---|---|
| N1 | `compound-head-gate.sql` (9) | `run.mjs` · `neuterArm`, the `shares` refusal (a head must BEGIN its line) | `9b3ff299f5e8ccf9915d16b06576ae5f3332883e0b931fd55cc50a325cb607d3` | **{9}** | 9 | none |
| N2 | `comment-parity-gate.sql` (10) | `run.mjs` · `neuterArm`, tokenizer RAISE end → the deleted quote-only walker | `f2d01e0f1fb93cc214919bcec7340b098bf4f2b78f4cbd66af9347c98fde560a` | **{10}** | 10 | none |
| N3 | `current-query-forge-gate.sql` (11) | `run.mjs` · `judgeBlock`, legs (a)(b)(c) all skipped | `5c9ae46ec6a547049d6e663e6dda6a5caee0c917a65d2f4f160d004a72560ccd` | **{8, 11, 12}** | 11 | (none forbidden — recorded) |
| N4 | `nested-execute-forge-gate.sql` (12) | `run.mjs` · `judgeBlock`, leg (b) ONLY → first-frame-only | `704daae7ff3ed9465e019093df99c84bcc2f27e8548678ec358aad3c4611c38d` | **{12}** | 12 | none — **11 stayed green** |

Restore after each cycle: sha256 back to `c1d247fb…`, `NEUTER(` markers 0, the newest edit
(`SELF-TEST 12/12:` header) present exactly once, `git status --porcelain` empty, then
`--self-test` **exit 0, 0 FAIL lines, PASSED** before the next cycle began.

---

## 2. Cycle N1 — the compound-head entry (Primitive A, [R4-C01] / RESEARCH P3)

**Neuter** — the minimal named removal of plan 01's rule that a branch head must begin its line
(the statement sharing the line BEFORE the head is no longer refused):

```diff
-      if (shares !== -1 && statements[shares].endLine === stmt.startLine) {
-        return refuse(statements[shares]);
-      }
+      void shares; // NEUTER(164.3.1-11 task 3, N1): line-local head acceptance re-introduced — [R4-C01]'s hole
```

`git diff --stat`: 1 file changed, 1 insertion(+), 3 deletions(-). Applied exactly once.

**The runner's own verdicts under the neuter** (narrowed run on the fixture, verbatim):

```
  baseline  scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql — exit 0 (1.5s)
  arm BEHIND P1                exit   3  RED (identity ok)  (1.7s)
  arm BEHIND HEAD              exit   3  RED (identity ok)  (1.6s)
  restore   scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql — exit 0 (2.0s)
arms: 2/2/0   (executed/annotated/waived)
biting: 2   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
```

That second line is the hole itself: the neuter of `COMPOUND HEAD` was **accepted**, the lane ran
with `SET ROLE postgres;` live (the lane's superuser exists, so it succeeded silently), and
`BEHIND HEAD` was credited as **biting** — the exact false attestation [R4-C01] describes. Under the
shipped rule the same run reports `neuter-missed … (line 135: SET ROLE postgres;)` and `biting: 1`.

**`--self-test` under the neuter — RED, verbatim:**

```
=== SELF-TEST 9/12: [R4-C01] the P3 compound HEAD must be REFUSED as `neuter-missed` naming `SET ROLE postgres;`, beside an ACCEPTED P1-shape neuter ===
SELF-TEST FAIL: exit code is 1 (got 2)
=== SELF-TEST FAILED ===
```

self-test exit 1, 2 `SELF-TEST FAIL` lines (the scenario's and the trailer's). RED set **{9}** —
scenarios 1–8 and 10–12 all green, so the neuter touched exactly the entry that pins it. (`got 2`
is the narrowed-green code: the runner found **no defect** where a refusal was required.)

**Restore:** `c1d247fb…` — MATCHES green · `NEUTER(` markers 0 · newest-edit grep 1 ·
`git status` clean · green re-run: exit 0, 0 FAIL lines, `=== SELF-TEST PASSED`.

---

## 3. Cycle N2 — the comment-parity entry (Primitive A, [MUT-I01] P4 + P5)

**Neuter** — the DELETED single-quote-only forward walker re-introduced in place of the
tokenizer's statement end, in both the sub-line and the whole-line splice paths:

```diff
   const spanStart = statements[startIdx].start;
-  const spanEnd = raiseStmt.end;
+  // NEUTER(164.3.1-11 task 3, N2): the DELETED single-quote-only forward walker, re-introduced.
+  let spanEnd = -1;
+  {
+    let q = false;
+    for (let c = raiseStmt.start; c < text.length; c += 1) {
+      const ch = text[c];
+      if (ch === "'") {
+        if (q && text[c + 1] === "'") { c += 1; continue; }
+        q = !q;
+      } else if (ch === ";" && !q) { spanEnd = c + 1; break; }
+    }
+  }
+  if (spanEnd === -1) return { text, found: false, reason: `could not find the end of the RAISE statement for "${arm}"` };
+  const neuterEndLine = text.slice(0, spanEnd).split("\n").length;
…
-    const end = raiseStmt.endLine - 1;
+    const end = neuterEndLine - 1; // NEUTER(164.3.1-11 task 3, N2)
```

`git diff --stat`: 1 file changed, 15 insertions(+), 2 deletions(-). Both replacements applied
exactly once.

**The runner's own verdicts under the neuter** (narrowed run, verbatim):

```
  baseline  scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql — exit 0 (1.7s)
  arm BEHIND EVEN              exit   3  WRONG-ARM(SURVIVOR LOST)  (1.8s)
  restore   scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql — exit 0 (1.6s)
arms: 1/2/0   (executed/annotated/waived)
biting: 0   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
  wrong-first-failure   BEHIND EVEN               scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql
      first failure was "SURVIVOR LOST", not "BEHIND EVEN" — red-anywhere is not success
  neuter-missed         BEHIND ODD                scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql
      could not neuter "PARITY ODD": could not find the end of the RAISE statement for "PARITY ODD"
```

Both directions, by name, in one run: **P5** (even parity, SILENT) — the walker ran past the
real terminator to the `PERFORM set_config(…);` and commented the survivor out, which the fixture
converts into the loud `SURVIVOR LOST` first failure; **P4** (odd parity, LOUD) — the walker ran to
end of file on `-- don't worry` and refused a legal arm with the measured text
`could not find the end of the RAISE statement`. `BEHIND ODD` never laned (`arms: 1/2/0`).

**`--self-test` under the neuter — RED, verbatim (defect JSON carried in the message):**

```
=== SELF-TEST 10/12: [MUT-I01] an apostrophe in a `--` comment inside a RAISE must neither refuse the neuter (P4) nor over-neuter the statement after it (P5) ===
SELF-TEST FAIL: narrowed run is GREEN: exit code 2 with an empty defect table (got 1; defects: [{"kind":"wrong-first-failure","arm":"BEHIND EVEN","file":"scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql","detail":"first failure was \"SURVIVOR LOST\", not \"BEHIND EVEN\" — red-anywhere is not success"},{"kind":"neuter-missed","arm":"BEHIND ODD","file":"scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql","detail":"could not neuter \"PARITY ODD\": could not find the end of the RAISE statement for \"PARITY ODD\""}])
=== SELF-TEST FAILED ===
```

RED set **{10}** — scenario 9 (whose `P1 SEVEN` neuter also runs through the forward walk) stayed
green, as did 1–8 and 11–12: the walker only breaks where a comment carries an apostrophe.

⚠️ **Fixture hardening forced by this proof (commit `95109cba`).** Tracing the re-introduced
walker by hand before the cycle showed two prose apostrophes in comments AFTER `PARITY ODD`'s
raise (`authenticated's`, `PARITY ODD's`) that happened to cancel — the P4 refusal was going to
be observed by luck, not by construction. Both were rewritten so the fixture's stated invariant
(every apostrophe after `-- don't worry` is paired) is literally true; measured after the edit:
82 apostrophes after that line, even. Recorded as a rework of the entry, not hidden.

**Restore:** `c1d247fb…` — MATCHES · markers 0 · newest-edit 1 · clean · green re-run exit 0,
PASSED.

---

## 4. Cycle N3 — the current_query() entry (Primitive B, [R4-C02])

**Neuter** — the attribution accepts any ERROR/P0001 message carrying `TEST FAILED (X)`: legs (a)
gate-file prefix, (b) chain length and (c) line resolution are all skipped:

```diff
+  return { identity, arm: identity, why: "attributed", seen }; // NEUTER(164.3.1-11 task 3, N3): any TEST FAILED text attributes — legs (a)(b)(c) skipped
   // ── leg (a): the psql prefix names THIS lane's gate scratch file ──────────
```

`git diff --stat`: 1 file changed, 1 insertion(+). Applied exactly once.

**The runner's own verdicts under the neuter** (narrowed run, verbatim):

```
  baseline  scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql — exit 0 (1.5s)
  arm CTRL 1                   exit   3  RED (identity ok)  (1.5s)
  arm FORGE 1                  exit   3  RED (identity ok)  (1.5s)
  restore   scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql — exit 0 (1.4s)
arms: 2/2/0   (executed/annotated/waived)
biting: 2   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
```

`FORGE 1` — the trigger that reads `current_query()` and re-raises the identity, for an arm guarded
by `IF FALSE` — is credited as **biting**. That is the nonce-era verdict ([R4-C02]: `RED (identity
ok)`, `biting: 1` for an arm that never ran) reproduced against the neutered attribution.

**`--self-test` under the neuter — RED, verbatim:**

```
=== SELF-TEST 11/12: [R4-C02] a current_query() trigger re-raising the identity must be SYNTHESISED, with the genuine arm RED (identity ok) beside it ===
SELF-TEST FAIL: exit code is 1 (got 2)
```

self-test exit 1, 4 FAIL lines. RED set **{8, 11, 12}** — expected and recorded: N3 removes the
whole attribution, so scenario 8 (the `format()`-spelled synthesis, refused by leg (a)) and
scenario 12's `FORGE 3` (refused by leg (b)) fall with it; `FORGE 2` alone stays refused because
its echoed `SQL statement` field is judged before `judgeBlock` is ever reached. Nothing was
forbidden for this cycle; the breadth is the neuter's, and it is why N4 exists as a separate,
narrower cycle.

**Restore:** `c1d247fb…` — MATCHES · markers 0 · newest-edit 1 · clean · green re-run exit 0,
PASSED.

---

## 5. Cycle N4 — the nested-EXECUTE entry (Primitive B, the strongest known attack)

**Neuter** — leg (b) ONLY, plan 05's exact neuter (164.3.1-05-ATTRIBUTION.md § 6): the chain is
judged by its FIRST frame, its length ignored:

```diff
-  const frame = SINGLE_DO_FRAME_RE.exec(chain.trim());
+  const frame = SINGLE_DO_FRAME_RE.exec(chain.trim().split("\n")[0].trim()); // NEUTER(164.3.1-11 task 3, N4): FIRST frame only, chain length ignored
```

`git diff --stat`: 1 file changed, 1 insertion(+), 1 deletion(-). Applied exactly once.

**The runner's own verdicts under the neuter** (narrowed run, verbatim):

```
  baseline  scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql — exit 0 (1.9s)
  arm CTRL 1                   exit   3  RED (identity ok)  (2.0s)
  arm FORGE 2                  exit   3  SYNTHESISED(FORGE 2)  (1.8s)
  arm FORGE 3                  exit   3  RED (identity ok)  (1.7s)
  restore   scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql — exit 0 (1.6s)
arms: 3/3/0   (executed/annotated/waived)
biting: 2   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
  synthesised-identity  FORGE 2                   scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql
      MEASURE_FAIL: the lane emitted TEST FAILED (FORGE 2), which this runner's gate file did not raise. WHY IT IS NOT ATTRIBUTABLE: carried by the CONTEXT field of a ERROR block, not by its message. WHAT WAS SEEN: BEGIN RAISE EXCEPTION 'TEST FAILED (FORGE 2): the arm under test fired.'; END $d$". …
```

Exactly the § 6 table from plan 05, re-observed on the permanent fixture: `FORGE 3` — the echo-free
forgery whose forged frame reads `inline_code_block line 5 at RAISE`, the genuine arm's own line —
flips to a **false `RED (identity ok)`** and `biting` rises 1 → 2. `FORGE 2` does NOT flip: with the
chain rule gone its refusal reason changes from the chain to *carried by the CONTEXT field … not by
its message* — the second, independent control that made a proof on FORGE 2 alone vacuous (§ 5 of
the ATTRIBUTION record), now visible in the runner's own words.

**`--self-test` under the neuter — RED, verbatim:**

```
=== SELF-TEST 12/12: the nested-EXECUTE DO forgery AIMED at the genuine raise line must be SYNTHESISED by chain LENGTH alone, with the genuine arm RED (identity ok) beside it ===
SELF-TEST FAIL: the defect table names BOTH FORGE 2 and FORGE 3 with kind "synthesised-identity"
```

RED set **{12}**. ⭐ **Scenario 11 stayed GREEN under N4** — `FORGE 1`'s first frame is
`PL/pgSQL function forge_fn() line 1 at RAISE`, which a first-frame-only judge still refuses. So the
two B entries fail under DIFFERENT neuters: N3 reds both; N4 reds only the nested-EXECUTE entry.
That is what makes them two corpus entries and not one.

**Restore:** `c1d247fb…` — MATCHES · markers 0 · newest-edit 1 · clean · green re-run exit 0,
PASSED.

---

## 6. The neuter specs, verbatim (scratchpad `neuter-N{1..4}.json`, consumed by the harness)

Each spec names the entry, the fix, the narrowed debug fixture, the REQUIRED red scenario, the
FORBIDDEN set, and the exact-once `find`/`replace` pairs quoted in §§ 2–5. The harness refused to
proceed unless every needle occurred exactly once, restored before exiting on any anomaly, and
asserted required ⊆ RED and forbidden ∩ RED = ∅ before the green re-run.

| Spec | required | forbidden |
|---|---|---|
| N1 | 9 | 1–8, 10–12 |
| N2 | 10 | 1–9, 11–12 |
| N3 | 11 | — (breadth recorded, § 4) |
| N4 | 12 | 1–11 |

All four cycles reported `CYCLE Nn COMPLETE`; no cycle tripped the ⛔ PRIMITIVE-D branch and no
cycle tripped a forbidden-scenario warning.

---

## 7. Final all-green self-test on the restored tree

After cycle N4's restore (sha `c1d247fb…`, tree clean), the green re-run:

```
self-test exit=0   SELF-TEST FAIL lines: 0   PASSED: 1
=== SELF-TEST PASSED: both floor modes, the wrong-identity mode and MEASURE_FAIL all fire, and the 164.3.1-11 regression corpus holds ===
```

with all twelve headers present (`SELF-TEST 1/12` … `SELF-TEST 12/12`), each expectation `ok`.
The plan-level re-run recorded in `164.3.1-11-SUMMARY.md` repeats this on the committed tree.

---

## 8. What this record does NOT claim

1. **Host:** every cycle ran on macOS / PostgreSQL 16.13. The `sql-mutation` job has never executed
   on its ubuntu host (WINDOWS.md 28); these arms ride that unobserved job like every other
   self-test scenario. Inherited, restated, not absorbed.
2. **N3 is broad by construction** — it is the "any text attributes" neuter the plan asked for, and
   it reds three scenarios. The narrow, single-leg proof for Primitive B is N4. Both are recorded
   because SC-1 asks for *its fix*, and the current_query() entry's fix is the attribution as a
   whole, not one leg of it.
3. **`biting` under N1 and N3/N4 rose to 2** in the narrowed runs — the quantity `ARMS_FLOOR`
   bounds was inflated by an arm whose own logic never ran, in each case. That is the number these
   entries exist to keep honest.
