---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "11"
subsystem: mutation-runner
tags: [regression-corpus, selftest-fixtures, primitive-a, primitive-b, sc-1, anti-vacuity, neuter-restore]

requires:
  - phase: 164.3.1-01
    provides: "the statement tokenizer and the two rules the A entries pin — a branch head must BEGIN its line ([R4-C01]) and the RAISE ends where its STATEMENT ends ([MUT-I01]) — plus the corrected P1/P3 spellings this plan's fixtures embed"
  - phase: 164.3.1-05
    provides: "source-location attribution (judgeBlock legs a/b/c) and 164.3.1-05-ATTRIBUTION.md §3, the fixture texts the B entries promote VERBATIM (CTRL 1, FORGE 1, FORGE 2, FORGE 3) plus the carry-forwards (promote FORGE 3; the k=4 padding is derived)"
  - phase: 164.3.1-09
    provides: "the unmoved baseline the main corpus must still report after the additions: files 1/71, arms 30/30/0, biting 30"
  - phase: 164.3.1-10
    provides: "laneTally / lane-invocations and the absurdity cross-check every new scenario runs under (each asserts its own laneInvocations)"
provides:
  - "scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql — the [R4-C01] P3 compound HEAD (`SET ROLE postgres; IF NOT ok THEN`) as a lane-driven neuter target refused by name, beside the seven-line P1 EXCEPTION-compound shape whose neuter is ACCEPTED (the passing control); self-test scenario 9/12"
  - "scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql — [MUT-I01] P4 (apostrophe in a `--` comment inside the RAISE) and P5 (the statement after the terminator must survive; an over-neuter surfaces as `wrong-first-failure: SURVIVOR LOST`, never as a green run); scenario 10/12"
  - "scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql — [R4-C02] promoted verbatim: FORGE 1 refused `synthesised-identity` by its first frame `forge_fn()`, CTRL 1 RED (identity ok) in the same run; scenario 11/12"
  - "scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql — the strongest known attack on the attribution: FORGE 2 (echoing, double-guarded) and FORGE 3 (echo-free, refused by chain LENGTH alone, aimed at `line 5`), CTRL 1 RED beside them; scenario 12/12"
  - "run.mjs --self-test is 12 scenarios; the narrowed-run exit-2 contract is asserted explicitly; the `N/M` header set is documented as a COUNTED set"
  - "164.3.1-11-CORPUS-PROOFS.md — four neuter→RED→restore cycles with verbatim runner verdicts, verbatim SELF-TEST FAIL lines, the RED-set per cycle, sha256 pairs, and the proof that the two B entries fail under DIFFERENT neuters"
  - "`neuter-missed` is now an exercised self-test kind (floors test lockstep)"
affects: [164.3.1-12, 164.4, sql-mutation, frontend-aggregator]

actuals:
  tokens: 17724
  tasks: 3
  commits: 5

tech-stack:
  added: []
  patterns:
    - "A corpus entry carries its PASSING CONTROL in the same file and the same run: one arm whose neuter/attribution must be ACCEPTED beside the arm whose neuter/attribution must be REFUSED, so a rule that refuses everything cannot pass the scenario"
    - "Make the SILENT direction loud INSIDE the fixture: a survivor statement records that it ran, and a second raise with its own identity fires when the branch was entered but the record is missing — the runner's ordinary verdict then names the over-neuter, and the scenario needs no lane output"
    - "Every proof cycle asserts SPECIFICITY as well as sensitivity: required ⊆ RED-set AND forbidden ∩ RED-set = ∅, so 'this neuter reds only this entry' is measured, not assumed"
    - "Per-commit denominator consistency: renumber 8→10 in the task that adds two scenarios and 10→12 in the task that adds two more, each pre-counted, so no commit's self-test lies about its own coverage"

key-files:
  created:
    - scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql
    - scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql
    - scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql
    - scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-11-CORPUS-PROOFS.md
  modified:
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-runner-floors.test.ts
    - .github/workflows/ci.yml

key-decisions:
  - "The A entries pin the runner's VERDICT TABLE, not neuterArm's return value: the compound-head refusal is observed as a `neuter-missed` defect naming `SET ROLE postgres;` through a real lane, which is the layer plan 01's vitest pins could not reach"
  - "P5's silent direction is made loud by the fixture itself (`set_config('mut.survivor', 'ran', false)` + a `SURVIVOR LOST` raise) rather than by exposing lane output through runCorpus — the runner's public result is the observable, and no new field was added to it"
  - "Scenario 10 asserts exit 2 WITH an empty defect table, because a defect-free narrowed run exits 2 by the runner's own contract (a subset run must never read as a passing gate); 0 there would mean that guard had been lost. The `armsFloor` each new scenario states is documented as INERT in narrowed mode, and the control is asserted DIRECTLY on bitingArms"
  - "Both FORGE 2 and FORGE 3 are promoted (plan 05's carry-forward): FORGE 2 is the documented recipe and its `(+1 further unattributable sighting)` echo is asserted so the double guard stays visible; FORGE 3 is the arm the chain-length proof actually rests on"
  - "The B entries' neuters are two: N3 (the whole attribution) for the current_query() entry, whose fix IS the attribution, and N4 (leg (b) only) for the nested-EXECUTE entry — and N4 leaving scenario 11 green is what proves they are two entries"

patterns-established:
  - "Fixture hygiene is part of the proof: a quote-only walker re-introduced for a neuter must run out of file BY CONSTRUCTION, so no unpaired prose apostrophe may follow the P4 raise — traced by hand before the cycle, fixed, and measured (82 apostrophes after the line, even)"

requirements-completed: [SC-1]

coverage:
  - id: D1
    description: "Primitive-A entry 1: the [R4-C01] compound-head hole is a permanent lane-driven arm (scenario 9/12) with its passing control; proven to RED under the line-local head acceptance (N1)"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test (scenario 9/12: 6 ok lines; exit 0, 12/12)"
        status: pass
      - kind: manual_procedural
        ref: "164.3.1-11-CORPUS-PROOFS.md §2 — N1: BEHIND HEAD RED (identity ok) with SET ROLE postgres; live, biting 2; SELF-TEST FAIL: exit code is 1 (got 2); RED set {9}; restore c1d247fb…"
        status: pass
    human_judgment: false
  - id: D2
    description: "Primitive-A entry 2: [MUT-I01] both directions are permanent lane-driven arms (scenario 10/12); proven to RED under the re-introduced quote-only forward walker (N2), P4 and P5 each by name"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test (scenario 10/12: 4 ok lines)"
        status: pass
      - kind: manual_procedural
        ref: "164.3.1-11-CORPUS-PROOFS.md §3 — N2: WRONG-ARM(SURVIVOR LOST) + neuter-missed 'could not find the end of the RAISE statement for \"PARITY ODD\"'; RED set {10}"
        status: pass
    human_judgment: false
  - id: D3
    description: "Primitive-B entry 1: the [R4-C02] current_query() trigger is a permanent arm with CTRL 1 RED in the same run (scenario 11/12); proven to RED under the attribution neuter (N3)"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test (scenario 11/12: 5 ok lines)"
        status: pass
      - kind: manual_procedural
        ref: "164.3.1-11-CORPUS-PROOFS.md §4 — N3: FORGE 1 RED (identity ok), biting 2; RED set {8, 11, 12}"
        status: pass
    human_judgment: false
  - id: D4
    description: "Primitive-B entry 2: the nested-EXECUTE forgery (FORGE 2 + echo-free FORGE 3) is a permanent arm with CTRL 1 RED beside it (scenario 12/12); proven to RED under the chain-length-only neuter (N4) while scenario 11 stays green — the two B entries fail under DIFFERENT neuters"
    requirement: "SC-1"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs --self-test (scenario 12/12: 7 ok lines, incl. the aimed `inline_code_block line 5 at RAISE` frame and the single-sighting check)"
        status: pass
      - kind: manual_procedural
        ref: "164.3.1-11-CORPUS-PROOFS.md §5 — N4: FORGE 3 RED (identity ok), FORGE 2 still SYNTHESISED (field-carried), biting 2; RED set {12}, 11 green"
        status: pass
    human_judgment: false
  - id: D5
    description: "The main corpus is untouched by the additions and every count-naming site moved in lockstep: full run exit 0 files 1/71 arms 30/30/0 biting 30 lane-invocations 30; no stale `/8` or `/10` header; floors test exact lists and ci.yml comment updated; contracts + Node 22 + tsc green"
    verification:
      - kind: integration
        ref: "node scripts/mutation-runner/run.mjs (Task 2 verify: exit 0, files 1/71, 30/30/0, biting 30, lane-invocations 30)"
        status: pass
      - kind: unit
        ref: "npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/contracts (7 files, 218 passed); Node 22: 109 passed; npx tsc --noEmit exit 0"
        status: pass
      - kind: other
        ref: "bash -c '! grep -aqE \"SELF-TEST [0-9]+/8:\" scripts/mutation-runner/run.mjs' (exit 0); grep -acE 'SELF-TEST [0-9]+/12:' → 12"
        status: pass
    human_judgment: false
  - id: D6
    description: "The four scenarios behave identically on the sql-mutation job's real ubuntu host"
    verification: []
    human_judgment: true
    rationale: "The sql-mutation job has never executed on ubuntu (WINDOWS.md 28). Every lane in this plan ran on macOS / PostgreSQL 16.13; the first real ubuntu run is the measurement nothing in this repo can substitute for"

duration: 8h 31m (wall clock, includes an idle gap; active execution ≈ 2h)
completed: 2026-09-02
status: complete
---

# Phase 164.3.1 Plan 11: Regression corpus — primitives A and B as permanent lane-driven arms Summary

**Every measured Primitive-A and Primitive-B instance is now a permanent executable arm under `fixtures/selftest/` driven by `run.mjs --self-test` on every PR — the [R4-C01] compound head (scenario 9), both [MUT-I01] parities (10), the [R4-C02] `current_query()` trigger (11) and the nested-EXECUTE forgery (12) — each carrying its passing control in the same run, and each proven to RED by neutering its fix in `run.mjs` and observing the failure verbatim, with `run.mjs` sha-verified back to `c1d247fb…` after every cycle and the main corpus unmoved at `files 1/71 · arms 30/30/0 · biting 30`.**

## Performance

- **Duration:** 8h 31m wall clock (2026-09-01T21:38:52Z → 2026-09-02T06:10Z); the harness runs themselves total ≈ 20 min of lane time
- **Tasks:** 3
- **Files modified:** 8 (5 created, 3 modified)

## The four entries and their scenario numbers

| # | Fixture | Scenario | Pins | Passing control (same run) |
|---|---|---|---|---|
| 1 | `compound-head-gate.sql` | **9/12** | [R4-C01] P3 `SET ROLE postgres; IF NOT ok THEN` refused as `neuter-missed` naming `SET ROLE postgres;`; the refused arm never lanes | `BEHIND P1` RED (identity ok) behind the seven-line P1 EXCEPTION-compound shape, whose neuter is ACCEPTED through the sub-line splice |
| 2 | `comment-parity-gate.sql` | **10/12** | [MUT-I01] P4: no spurious `neuter-missed`; P5: the survivor statement runs (else `wrong-first-failure: SURVIVOR LOST`) | `BEHIND ODD` and `BEHIND EVEN` both RED (identity ok), 2/2 biting, exit 2 with an empty defect table |
| 3 | `current-query-forge-gate.sql` | **11/12** | [R4-C02] `FORGE 1` → `synthesised-identity`, detail naming `forge_fn()` and `not EXACTLY ONE` | `CTRL 1` RED (identity ok), biting 1 |
| 4 | `nested-execute-forge-gate.sql` | **12/12** | `FORGE 2` and `FORGE 3` → `synthesised-identity`; FORGE 3 aimed (`inline_code_block line 5 at RAISE`) and echo-free (no `further … sighting`); FORGE 2's echo asserted | `CTRL 1` RED (identity ok), biting 1 |

The B fixtures are the texts of `164.3.1-05-ATTRIBUTION.md` §3, promoted unchanged (arm names, trigger SQL, `NEW.id <> 90N` guards, `repeat(E'\\n', 4)` padding); their headers cite GRAMMAR.md §3c's authoring rule and the 2026-09-01 adversarial finding.

## One RED excerpt per entry (from `164.3.1-11-CORPUS-PROOFS.md`)

**Entry 1 — N1, line-local head acceptance re-introduced.** The runner's own verdicts under the neuter, then the scenario:

```
  arm BEHIND P1                exit   3  RED (identity ok)  (1.7s)
  arm BEHIND HEAD              exit   3  RED (identity ok)  (1.6s)
biting: 2   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
…
=== SELF-TEST 9/12: [R4-C01] the P3 compound HEAD must be REFUSED as `neuter-missed` naming `SET ROLE postgres;`, beside an ACCEPTED P1-shape neuter ===
SELF-TEST FAIL: exit code is 1 (got 2)
```

`BEHIND HEAD` credited as biting with `SET ROLE postgres;` live — the hole verbatim. RED set {9}.

**Entry 2 — N2, the deleted quote-only forward walker re-introduced:**

```
  arm BEHIND EVEN              exit   3  WRONG-ARM(SURVIVOR LOST)  (1.8s)
  wrong-first-failure   BEHIND EVEN     first failure was "SURVIVOR LOST", not "BEHIND EVEN" — red-anywhere is not success
  neuter-missed         BEHIND ODD      could not neuter "PARITY ODD": could not find the end of the RAISE statement for "PARITY ODD"
```

P5's silent over-neuter and P4's spurious refusal, both by name, in one run. RED set {10}.

**Entry 3 — N3, attribution legs (a)(b)(c) skipped:**

```
  arm CTRL 1                   exit   3  RED (identity ok)  (1.5s)
  arm FORGE 1                  exit   3  RED (identity ok)  (1.5s)
biting: 2
…
=== SELF-TEST 11/12 … ===
SELF-TEST FAIL: exit code is 1 (got 2)
```

The nonce-era [R4-C02] verdict reproduced: a trigger's raise credited for an `IF FALSE` arm. RED set {8, 11, 12}.

**Entry 4 — N4, chain length reduced to first-frame-only:**

```
  arm CTRL 1                   exit   3  RED (identity ok)  (2.0s)
  arm FORGE 2                  exit   3  SYNTHESISED(FORGE 2)  (1.8s)
  arm FORGE 3                  exit   3  RED (identity ok)  (1.7s)
biting: 2
…
SELF-TEST FAIL: the defect table names BOTH FORGE 2 and FORGE 3 with kind "synthesised-identity"
```

RED set **{12}** — scenario 11 stayed green (its first frame is `forge_fn()`), so the two B entries fail under DIFFERENT neuters.

## Final self-test tally and the unchanged main corpus, verbatim

`node scripts/mutation-runner/run.mjs --self-test` on the committed tree: **exit 0, 12 headers, 46 `ok` lines, 0 `SELF-TEST FAIL` lines**, ending

```
=== SELF-TEST PASSED: both floor modes, the wrong-identity mode and MEASURE_FAIL all fire, and the 164.3.1-11 regression corpus holds ===
```

`node scripts/mutation-runner/run.mjs` after all four fixture additions (Task 2 verify; `run.mjs` is byte-identical at every later commit — sha `c1d247fb…` — and the only later change is a comment in a selftest fixture outside the `supabase/tests` scan):

```
coverage: files 1/71
arms: 30/30/0   (executed/annotated/waived)
biting: 30   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 30   (arm lanes actually spawned — tallied inside runLane, independent of the 30 the verdict loop counted; plus 1 baseline / 1 restore leg(s))
per-arm lane time: mean 1.7s over 30 arm run(s)

✅ No defects. Every annotated arm bit its own arm first.
```

exit 0. The plan-09/10 floor numbers hold.

## Task Commits

1. **Task 1: Primitive-A entries + scenarios 9/10, denominators 8→10, floors-test lockstep** — `8b407031` (feat)
2. **Task 2: Primitive-B entries + scenarios 11/12, denominators 10→12** — `42f088bd` (feat)
3. **Task 3 (prep): comment-parity fixture apostrophe hygiene forced by the proof trace** — `95109cba` (fix)
4. **Task 3: the recursive SC-1 proof record** — `7d6f934b` (docs; amended once, message-only, to correct a session-trailer typo)

**Plan metadata:** the docs commit carrying this file.

## Files Created/Modified

- `scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql` — four arms: `P1 SEVEN` (the corpus's `EXCEPTION WHEN OTHERS THEN v_raised := true; END;` + one-line head/cleanup/raise/END IF), `BEHIND P1` (control), `COMPOUND HEAD` (exact-set pin with the P3 line), `BEHIND HEAD` (the refused neuter). Header states the P1-vs-P3 distinction from plan 01 §7.
- `scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql` — `PARITY EVEN`/`BEHIND EVEN` (P5 + `SURVIVOR LOST`), `PARITY ODD`/`BEHIND ODD` (P4); order chosen so a quote-only walker runs out of file on the P4 raise.
- `scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql` — `CTRL 1` + `FORGE 1`, promoted verbatim.
- `scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql` — `CTRL 1` + `FORGE 2` + `FORGE 3`, promoted verbatim; header documents the derived k=4 padding and why FORGE 3 exists.
- `scripts/mutation-runner/run.mjs` — scenarios 9–12; all headers `N/12`; the self-test block header names the COUNTED-set discipline; the PASSED line names the corpus. No executable change outside `selfTest()`.
- `src/__tests__/mutation-runner-floors.test.ts` — `neuter-missed` moved from the exact not-covered list to the exact exercised list, with the reason.
- `.github/workflows/ci.yml` — the self-test step's dated count comment re-dated (`8/8 … 12/12 after 164.3.1-11`); no step changed.
- `164.3.1-11-CORPUS-PROOFS.md` — new: provenance, the four cycles with diffs, verdicts, FAIL lines, RED sets, sha pairs, restores, the final green run, and what the record does not claim.

## Decisions Made

See `key-decisions` in the frontmatter. The one worth restating: a corpus entry's observable is the runner's **verdict table**, and where the defect under test is silent by nature (P5), the fixture converts silence into a named first failure rather than the scenario reaching into lane output. That keeps `runCorpus`'s result surface unchanged and keeps the scenario reading the same thing CI reads.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] `mutation-runner-floors.test.ts` exact kind lists moved in lockstep**
- **Found during:** Task 1 (read_first)
- **Issue:** the floors test pins an EXACT list of defect kinds the self-test does NOT exercise, and `neuter-missed` was on it. Scenario 9 asserts `kind === "neuter-missed"`, which the test derives from `selfTest()`'s source — the pin would have gone red by name.
- **Fix:** `neuter-missed` moved to the exercised list with the reason (a refused neuter inside the entry's own file, without breaking the shared fixture corpus). The file is not in the plan's `files_modified` but is named in its verification step 3.
- **Verification:** floors test green (Node 25 and Node 22); the exact-set discipline is preserved (both lists still exact).
- **Committed in:** `8b407031`

**2. [Rule 2 - Missing Critical] Fixture hygiene forced by the proof trace**
- **Found during:** Task 3, tracing the N2 walker by hand before running it
- **Issue:** two prose apostrophes in comments after `PARITY ODD`'s raise cancelled each other, so the P4 refusal would have been observed by luck rather than by the fixture's stated construction.
- **Fix:** both comment lines rewritten apostrophe-free; the invariant (every apostrophe after `-- don't worry` paired) is now literally true and was measured (82, even).
- **Verification:** narrowed run 2/2 RED before commit; N2 then observed the exact P4 refusal text; recorded in CORPUS-PROOFS §3.
- **Committed in:** `95109cba`

**3. [Rule 1 - Bug, in this plan's own scenario] Scenario 10 first asserted `exitCode === 0`**
- **Found during:** Task 1 verify (first self-test run: `SELF-TEST FAIL: exit code is 0 (got 2; defects: [])`)
- **Issue:** a defect-free NARROWED run exits **2** by the runner's own contract (`run.mjs` header + `:1939`) — never 0. My first draft of the scenario had adopted scenario 6's exit-0 shape, which is only right for a whole-directory run. The same read showed narrowed runs enforce NO floors, so the `armsFloor` reasons I had written for scenarios 9 and 10 ("a floor of 1 makes a lost control a floor defect") were false.
- **Fix:** scenario 10 asserts `exitCode === 2 && defects.length === 0` with the contract stated inline; every new scenario's `armsFloor` comment says it is INERT in narrowed mode and the control is asserted directly on `bitingArms`. Fixed before the Task 1 commit.
- **Verification:** self-test 10/10 then 12/12 green.
- **Committed in:** `8b407031`

---

**Total deviations:** 3 auto-fixed (1 blocking, 1 missing-critical, 1 bug in new code). **Impact:** no scope creep — one lockstep pin, one hardening the proof itself demanded, one correction to a claim this plan would otherwise have shipped as false.

## Issues Encountered

1. **A stale claim in the plan.** Task 1 says a comment in `src/__tests__/mutation-annotation-parser.test.ts` "cites scenario 8 by its old denominator". The pre-count (`grep -a` for `/8`, `8/8`, `scenario 8`, `self-test` across `src/__tests__`, `.github/workflows`, `scripts/mutation-runner`) found **no such comment** — the full `/8` set was the eight `run.mjs` headers plus the dated measurement comment at `ci.yml:991`. Recorded rather than silently "updated": the parser test was not touched.
2. **`ci.yml` was touched (comment only).** Not in `files_modified`, but the plan's read_first names `.github/workflows` as part of the count set. The measurement comment now reads `8/8, in seconds; 12/12 after 164.3.1-11 added the Primitive-A and -B regression corpus (2026-09-01)` — lineage kept, currency added. The floors test's ci.yml text pins are unaffected (81 → still passing inside the 218).
3. **The N3 neuter is broad.** Skipping the whole attribution reds scenarios 8 and 12 as well as 11. Expected and recorded (CORPUS-PROOFS §4, §8): the current_query() entry's fix IS the attribution; the single-leg proof for Primitive B is N4, which reds 12 alone.
4. **STATE.md bookkeeping** — see the state-update notes below the self-check; `state.advance-plan` was expected to refuse with `ambiguous_position_phase` per the executor notes.

## Known Stubs

None. No placeholder values, no skipped tests, no unrun `<verify>` command. Every scenario was observed RED under its neuter before being called a corpus entry.

## Threat Flags

None. The fixtures ARE the attacks, kept on a leash inside `fixtures/selftest/` (outside the `supabase/tests` scan); no new network endpoint, auth path, file-access pattern or schema surface. Zero packages installed (`T-164.3.1-11-SC` accepted as planned). T-164.3.1-11-01/-02/-03 mitigated as the register states: four permanent scenarios in the sql-mutation self-test step, recursive neuter proofs, and a committed record with verbatim REDs and sha pairs.

## User Setup Required

None.

## Next Phase Readiness

- **Plan 12 (instance → arm registry):** the four entries are `compound-head-gate.sql` → 9/12, `comment-parity-gate.sql` → 10/12, `current-query-forge-gate.sql` → 11/12, `nested-execute-forge-gate.sql` → 12/12; cite `164.3.1-11-CORPUS-PROOFS.md` §§2–5 for each proof. The self-test denominator is 12; anyone adding a scenario must pre-count `N/12` across `run.mjs` and `ci.yml:991-992`.
- **Phase 164.4:** the authoring rule these fixtures attack is GRAMMAR.md §3c; both forge fixtures state that a shape change must recompute k. `FILES_FLOOR`'s "Phase 164.4 raises FILES_FLOOR as it backfills the other 70 files." sentence is untouched (run.mjs was edited only inside `selfTest()` and its header comment).
- **Carried residual, not closed:** every lane in this plan ran on macOS / PostgreSQL 16.13; the ubuntu host is still unobserved (WINDOWS.md 28). Coverage entry D6 is `human_judgment: true` for that reason.

## Self-Check: PASSED

| Claim | Result |
|---|---|
| `scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql` | FOUND |
| `scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql` | FOUND |
| `scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql` | FOUND |
| `scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql` | FOUND |
| `164.3.1-11-CORPUS-PROOFS.md` | FOUND |
| commit `8b407031` | FOUND |
| commit `42f088bd` | FOUND |
| commit `95109cba` | FOUND |
| commit `7d6f934b` | FOUND |
| `run.mjs` sha256 after all cycles | `c1d247fb…`, 0 `NEUTER(` markers, `git status` clean |
| file deletions in any commit | none |

---
*Phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c*
*Completed: 2026-09-02*
