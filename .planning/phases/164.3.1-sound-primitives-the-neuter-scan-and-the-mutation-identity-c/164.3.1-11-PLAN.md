---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "11"
type: execute
subsystem: mutation-runner
tags: [regression-corpus, selftest-fixtures, primitive-a, primitive-b, sc-1]
wave: 5
depends_on: ["164.3.1-05", "164.3.1-10"]
files_modified:
  - scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql
  - scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql
  - scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql
  - scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql
  - scripts/mutation-runner/run.mjs
  - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-11-CORPUS-PROOFS.md
autonomous: true
requirements: [SC-1]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "Every measured Primitive A and Primitive B instance is a PERMANENT, lane-driven executable arm under fixtures/selftest: the seven-line compound-head shape (R4-C01/P3), the [MUT-I01] parity pair (P4 spurious refusal + P5 silent over-neuter), the R4-C02 current_query() trigger, and the nested-EXECUTE DO forgery — each driven by a numbered run.mjs self-test scenario against a real throwaway cluster (SC-1)."
    - "SC-1 is RECURSIVE and honoured literally: each corpus entry was proven to RED by NEUTERING ITS FIX (the statement predicate for the A entries; the attribution/chain rule for the B entries) and observing the scenario fail, with verbatim failure text recorded and restores verified by shasum — a corpus entry that cannot fail is itself a Primitive-D instance."
    - "The B-instance scenarios carry their PASSING CONTROL in the same fixture run: a genuine direct DO-body arm scores RED (identity ok) beside the forge arms scoring their defect verdicts — an attribution that rejects everything would red the control, so the scenarios cannot pass vacuously."
    - "Every self-test scenario denominator and count-referencing comment was PRE-COUNTED before editing (grep for the current N/8 spellings first) and updated consistently — no stale count survives to make the self-test lie about its own coverage."
    - "The main corpus run is untouched by the new fixtures: fixtures/selftest is outside the supabase/tests scan, so coverage stays files 1/71 and the plan-09/10 floor numbers hold — asserted by a full run after the additions."
  artifacts:
    - "scripts/mutation-runner/fixtures/selftest/ — four new *-gate.sql fixtures with RED-UNDER-M annotations"
    - "scripts/mutation-runner/run.mjs — four new numbered self-test scenarios with explicit armsFloor and reason, denominators updated"
    - ".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-11-CORPUS-PROOFS.md — per-entry neuter→RED→restore records with shasums and verbatim failure text"
  key_links:
    - "fixture texts ← 164.3.1-05-ATTRIBUTION.md (the R4-C02 and nested-EXECUTE fixtures plan 05 demonstrated on scratch and recorded in full for promotion) and 164.3.1-01-SUMMARY.md (the corrected seven-line spelling: EXCEPTION-compound lines are P1; SET ROLE postgres; IF NOT ok THEN is the P3 head shape)"
    - "scenario shape ← the existing selftest scenarios in run.mjs (SELFTEST_DIR + runCorpus onlyFile + expect(condition, message) + explicit armsFloor with inline reason)"
    - "consumer → the sql-mutation CI job (its self-test step runs every scenario on every PR, inside the frontend aggregator)"
---

<objective>
Regression corpus, primitives A + B (SC-1): promote every measured instance to a PERMANENT
executable arm driven by the runner's own self-test against a real throwaway cluster, so instance
five of either family reds in CI rather than in a review.

The four entries and their measured lineage:
1. compound-head-gate.sql — the R4-C01/P3 shape: a compound statement line directly above a raise
   (the exact hole where the old predicate accepted the whole line as a branch head and an
   accepted neuter left a superuser session live). Plan 01 closed it; this entry keeps it closed.
2. comment-parity-gate.sql — the [MUT-I01] pair: P4 (a multi-line RAISE with an apostrophe inside
   a line comment must neuter cleanly, no spurious refusal) and P5 (the even-parity shape must
   neuter ONLY the raise; the statement that must survive stays live).
3. current-query-forge-gate.sql — the R4-C02 attack: a sql-step trigger reading current_query()
   and raising the arm's message. Under the nonce it scored RED with biting 1; under attribution
   it must score its defect verdict, with a genuine arm passing beside it.
4. nested-execute-forge-gate.sql — the strongest known attack on the chosen design: a trigger
   EXECUTE-ing a DO string with padded newlines to spoof an inline_code_block frame. The
   single-frame chain rule must refuse it.

SC-1's recursion is the acceptance bar: each entry is proven able to fail by neutering its fix and
observing the failure, restores sha-verified. Plan 08's machine check runs over everything this
plan writes — any self-referential shortcut in the proof arms fails CI by name.

Prerequisites honored: plan 05 recorded the B fixtures' full texts in 164.3.1-05-ATTRIBUTION.md
(promote, do not re-derive); plan 10's count cross-check and floors are live, so the new scenarios
run under the hardened runner.

⚠️ Anchor discipline: run.mjs has been rewritten by plans 01/05/09/10 — locate the self-test
block and all counts by SYMBOL and grep, never by stale line numbers. ⚠️ Gate tokens and counts
must be counted PRE-EDIT (the N/8 denominators, any comment naming scenario numbers — grep for
them across run.mjs, src/__tests__, and .github/workflows BEFORE renumbering).

⛔ Safety: only the pg-lane throwaway cluster is written to. No supabase CLI commands.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@scripts/mutation-runner/run.mjs
@scripts/mutation-runner/GRAMMAR.md
@scripts/mutation-runner/fixtures/selftest/synthesised-identity-gate.sql

Read at execution time — created by plans 01 and 05, so they do not exist at planning time and
are deliberately not @-referenced (plan-anchor-verify checks pending plans against the tree):
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-05-ATTRIBUTION.md
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-01-SUMMARY.md
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL (ROADMAP deliverable 6) | Regression corpus carrying every measured instance as executable arms — A and B families | Tasks 1-2 |
| REQ [SC-1] | Each entry proven to RED by neutering its fix, observed, restored sha-verified | Task 3 |
| CONTEXT § Specifics | Criterion 1 recursive: a corpus entry that cannot fail is a Primitive-D instance | Task 3 |
| RESEARCH § Pattern 5 | Corpus entries slot into runner self-test arms (A/B instances need a lane) | Tasks 1-2 |
| RESEARCH § Pattern 2 | The nested-EXECUTE forgery added as the SECOND corpus entry beside R4-C02 | Task 2 |
| VALIDATION § Wave 0 | Seven-line replica, forgery fixture, R4-C02 trigger annotation — the fixture list | Tasks 1-2 |
| GRAMMAR.md authoring rule (plan 05) | Fixtures conform: genuine arms raise directly in DO bodies; the forge arms are the documented refusal shapes | Tasks 1-2 |

Excluded (not gaps): C+D instances and the VAC-08 instances (plan 12, parallel in this wave,
file-disjoint); the vitest-side P1-P5 pins (plan 01, standing — this plan adds the LANE-driven
layer).
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Primitive-A entries — compound-head-gate.sql and comment-parity-gate.sql with their scenarios</name>
  <files>scripts/mutation-runner/fixtures/selftest/compound-head-gate.sql, scripts/mutation-runner/fixtures/selftest/comment-parity-gate.sql, scripts/mutation-runner/run.mjs</files>
  <precondition>bash scripts/pg-lane/run.sh --self-test exits 0 (throwaway cluster available), and no concurrent agent edits this checkout during lane runs.</precondition>
  <read_first>scripts/mutation-runner/fixtures/selftest/synthesised-identity-gate.sql and nonbiting-gate.sql (fixture anatomy: the RED-UNDER-M annotation block, the gate DO bodies, how a fixture encodes its expected defect); scripts/mutation-runner/run.mjs — the selfTest block (grep "SELFTEST_DIR": scenario structure, expect helper, explicit armsFloor with inline reason, the final PASSED/FAILED prints) and the current denominators (grep for the /8 spellings — PRE-COUNT them and everything else that names a scenario count across run.mjs, src/__tests__ and .github/workflows before editing); 164.3.1-01-SUMMARY.md (the corrected spellings: the seven P1 lines are EXCEPTION-compound; the P3 head is SET ROLE postgres; IF NOT ok THEN — do not re-import the outline's conflation); 164.3.1-RESEARCH.md Premises P3-P5 (the exact measured shapes the fixtures must embed)</read_first>
  <action>
    compound-head-gate.sql: a small gate file whose annotated arm embeds the P3 compound-head
    shape (a SET ROLE line compounded with an open IF ... THEN, directly above the arm's TEST
    FAILED raise) plus one of the seven-line EXCEPTION-compound P1 shapes elsewhere in the file,
    with a RED-UNDER-M annotation whose neuter targets the arm above the compound head. The
    scenario asserts the SHIPPED behavior: the runner classifies the compound statements
    correctly and the neuter path behaves per plan 01's rule (the refusal-or-correct-decompose
    outcome plan 01 pinned — read its SUMMARY for which; assert THAT, with the defect table or
    verdict as the observable). armsFloor explicit with the reason inline.

    comment-parity-gate.sql: two annotated arms — the P4 shape (multi-line RAISE spanning a line
    comment containing an apostrophe; the arm must neuter cleanly and score its normal verdict)
    and the P5 shape (even parity across two comments with a PERFORM-style statement after the
    raise terminator that MUST survive; the scenario asserts the lane output shows the surviving
    statement executed — e.g. its side effect or a NOTICE the fixture emits — and the verdict is
    normal, proving no silent over-neuter). Follow GRAMMAR.md rule 2 (measured occurrences) and
    the plan-05 authoring rule (all genuine raises directly in DO bodies).

    Add the two numbered scenarios to selfTest following the house shape (runCorpus with
    onlyFile per fixture, expect(condition, message) per assertion, quiet log). Renumber ALL
    denominators consistently (the pre-counted set from read_first — every N/8 header plus any
    comment naming a scenario count, including the one in src/__tests__/
    mutation-annotation-parser.test.ts that cites scenario 8 by its old denominator).
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --self-test</automated>
    <fails_when>exit code is non-zero or output contains "SELF-TEST FAIL" — the two new scenarios and all pre-existing ones must pass together with consistent numbering</fails_when>
    <automated>bash -c '! grep -aqE "SELF-TEST [0-9]+/8:" scripts/mutation-runner/run.mjs'</automated>
    <fails_when>exits non-zero — a scenario header still carries the stale /8 denominator after the renumbering (the pre-count discipline failed); the pattern is scoped to the header shape so prose comments cannot trip it</fails_when>
  </verify>
  <acceptance_criteria>
    - Both fixtures exist with RED-UNDER-M annotations conforming to GRAMMAR.md and the plan-05 authoring rule; the P1/P3 spelling distinction from plan 01's SUMMARY is respected in fixture comments
    - Both scenarios assert concrete observables (verdict strings, defect kinds, the surviving-statement evidence for P5) with explicit armsFloor and reasons
    - All scenario denominators and count-naming comments updated from the pre-counted list; no stale count anywhere
  </acceptance_criteria>
  <done>The A-family instances are permanent lane-driven arms: the compound-head hole and both [MUT-I01] directions each have a scenario that runs on every PR.</done>
</task>

<task type="auto">
  <name>Task 2: Primitive-B entries — current-query-forge-gate.sql and nested-execute-forge-gate.sql with in-run passing controls</name>
  <files>scripts/mutation-runner/fixtures/selftest/current-query-forge-gate.sql, scripts/mutation-runner/fixtures/selftest/nested-execute-forge-gate.sql, scripts/mutation-runner/run.mjs</files>
  <read_first>164.3.1-05-ATTRIBUTION.md (the two fixture texts recorded in full for promotion — start from these, do not re-derive the trigger SQL or the EXECUTE padding); scripts/mutation-runner/run.mjs — the attribution verdict kinds plan 05 shipped (grep "SYNTHESISED" and "NO-IDENTITY" for the exact strings the scenarios must assert) and the defect-kind names; scripts/mutation-runner/GRAMMAR.md — the rewritten 3c section and the authoring rule (the forge fixtures are its documented refusal shapes; cite the section in the fixture headers)</read_first>
  <action>
    current-query-forge-gate.sql: promote plan 05's scratch fixture — an annotated arm whose
    mutation installs the current_query()-reading trigger (the R4-C02 attack), PLUS a genuine
    direct DO-body arm in the same fixture. Scenario asserts BOTH in one run: the forge arm
    scores its defect verdict (the exact kind/verdict strings from plan 05's shipped code) and
    exits 1, AND the genuine arm scores RED (identity ok) — the passing control that stops the
    scenario passing vacuously.

    nested-execute-forge-gate.sql: promote the nested-EXECUTE DO forgery fixture the same way —
    trigger EXECUTE-ing a padded DO string targeting the genuine arm's raise line; scenario
    asserts the forge arm is refused (defect named) with the genuine arm's RED beside it. The
    fixture header cites GRAMMAR.md's single-frame authoring rule and RESEARCH's adversarial
    finding date, so a future editor knows this file IS the attack the rule exists for.

    Add both numbered scenarios; keep denominators consistent (they were renumbered in task 1 —
    extend, re-grep the count set to confirm nothing else names it).
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --self-test</automated>
    <fails_when>exit code is non-zero or output contains "SELF-TEST FAIL" — both forge scenarios (defect verdicts + passing controls) and all prior scenarios must pass together</fails_when>
    <automated>node scripts/mutation-runner/run.mjs</automated>
    <fails_when>exit code is non-zero, or the output lacks "files 1/71" — the selftest fixtures leaked into the main corpus scan or a floor moved; the main run must be untouched by the additions</fails_when>
  </verify>
  <acceptance_criteria>
    - Both fixtures are promoted from the plan-05 record (texts traceable to 164.3.1-05-ATTRIBUTION.md), with GRAMMAR.md citations in their headers
    - Each scenario asserts the forge defect AND the genuine arm's RED in the same run — the control is inside the arm, not a separate claim
    - Main corpus run unchanged (files 1/71, floors green) after all four fixture additions
  </acceptance_criteria>
  <done>Both B-family attacks are permanent lane-driven arms with in-run passing controls: the strongest known attacks on the attribution design run on every PR.</done>
</task>

<task type="auto">
  <name>Task 3: Recursive SC-1 proofs — neuter each entry's fix, observe RED, restore sha-verified, record</name>
  <files>.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-11-CORPUS-PROOFS.md</files>
  <precondition>No concurrent agent edits this checkout during the neuter/restore windows (each involves full self-test runs against real clusters).</precondition>
  <read_first>164.3.1-VALIDATION.md § Anti-Vacuity Contract and § Per-gate neuter/restore map row "Regression corpus (criterion 1, recursive)" (the 164.3 fix-report format: verbatim failure text + shasum restore); scripts/mutation-runner/run.mjs and scripts/mutation-runner/parse.mjs — the fix sites to neuter (the statement predicate path for A; the attribution chain/prefix checks for B), located by symbol</read_first>
  <action>
    For EACH of the four entries, run the neuter→RED→restore cycle with byte backups of the
    edited file(s) verified by shasum -a 256, re-taken after every edit, never git checkout --:

    1. compound-head entry: neuter the statement-based classification (re-introduce the
       line-local head acceptance in the backward-scan path — the minimal named neuter of plan
       01's fix) → run the self-test → the compound-head scenario REDs; capture verbatim.
    2. parity entry: re-introduce single-quote-only forward tracking → both parity assertions
       RED (P4 spurious refusal, P5 over-neuter detected); capture.
    3. current-query entry: neuter the attribution so any TEST FAILED text attributes (accept
       without the prefix/chain/line checks) → the forge arm flips toward RED (identity ok) and
       the scenario REDs on its defect expectation; capture.
    4. nested-execute entry: neuter ONLY the chain-length leg (first-frame-only) → this scenario
       REDs while the current-query scenario still passes (the named-frame check still refuses
       it) — record that the two B entries fail under DIFFERENT neuters, which is what makes
       them two entries and not one.

    After each: restore, shasum-verify, re-run the self-test fully green before the next cycle.
    A corpus entry that stays green under its neuter is a Primitive-D instance — if any does,
    STOP: the entry's assertion is not coupled to its fix; rework the scenario until the neuter
    reds it, and note the rework in the record.

    Write 164.3.1-11-CORPUS-PROOFS.md: per entry — the fix neutered (file + symbol), the
    verbatim RED excerpt, the shasum pair, the restore confirmation (hash AND a green re-run),
    and the final all-green self-test output. Commit it (.planning/ is tracked).
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --self-test</automated>
    <fails_when>exit code is non-zero or output contains "SELF-TEST FAIL" — the restored tree must be fully green after all four cycles; residue from any neuter lands here</fails_when>
    <automated>ART=".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-11-CORPUS-PROOFS.md" && [ "$(grep -ac "shasum\|sha256" "$ART")" -ge 4 ] && grep -aq "compound" "$ART" && grep -aq "parity" "$ART" && grep -aqi "current.query\|current_query" "$ART" && grep -aqi "nested" "$ART"</automated>
    <fails_when>any check fails — the record lacks a per-entry restore hash or one of the four entries' proof sections; an unrecorded proof did not happen</fails_when>
  </verify>
  <acceptance_criteria>
    - All four entries observed RED under their fix's neuter with verbatim text in the committed record; the two B entries proven to fail under DIFFERENT neuters
    - Every restore verified by shasum AND a green re-run; no git checkout -- used
    - Any entry that survived its neuter was reworked until coupled, with the rework noted
  </acceptance_criteria>
  <done>SC-1 is discharged for the A+B families: every corpus entry is proven able to fail, on record, with sha-verified restores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture SQL → runner verdicts | the fixtures ARE the attacks — hostile annotations kept on a leash as regression arms |
| the proof record → SC-1's verdict | an unrecorded or unfalsifiable proof converts the corpus into decoration |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-11-01 | Tampering | a future predicate/attribution regression reopening a closed hole | high | mitigate | four permanent lane-driven scenarios in the sql-mutation job's self-test step, inside the frontend aggregator |
| T-164.3.1-11-02 | Tampering | a corpus entry that agrees with its fix by construction | medium | mitigate | task 3's recursive neuter proofs; plan 08's blocking SRO rule scans nothing here (fixtures are .sql) but the scenario assertions use runner output, not fixture-echoed constants |
| T-164.3.1-11-03 | Repudiation | proof claims without evidence | medium | mitigate | committed CORPUS-PROOFS record with verbatim REDs and shasum pairs, greppable by the verifier |
| T-164.3.1-11-SC | Tampering (supply chain) | npm installs | low | accept | zero packages installed; fixtures and scenarios only |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 11)

| Artifact | Kind | Location | Consumers |
|---|---|---|---|
| `compound-head-gate.sql`, `comment-parity-gate.sql`, `current-query-forge-gate.sql`, `nested-execute-forge-gate.sql` | lane-driven fixtures with RED-UNDER-M annotations | `scripts/mutation-runner/fixtures/selftest/` | run.mjs self-test; sql-mutation CI job; plan 12's instance→arm registry |
| Four numbered self-test scenarios (denominators renumbered consistently) | self-test arms | `scripts/mutation-runner/run.mjs` | every PR via the sql-mutation job |
| `164.3.1-11-CORPUS-PROOFS.md` | committed recursive-proof record | phase directory | phase verifier (SC-1); plan 12's registry cites it |
</artifacts_produced>

<verification>
Serialized:
1. `node scripts/mutation-runner/run.mjs --self-test` — all scenarios green, consistent numbering.
2. `node scripts/mutation-runner/run.mjs` — exit 0, files 1/71, floors green (main corpus untouched by fixture additions).
3. `npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-annotation-parser.test.ts` — green (count-citing comments updated in lockstep).
4. If any src/ file changed: contracts suite + Node-22 pass.
5. The CORPUS-PROOFS record is committed and covers all four entries.
</verification>

<success_criteria>
- SC-1 delivered for primitives A and B: every measured instance is a permanent executable arm
  driven through real lanes on every PR, and each was proven to RED by neutering its fix with
  sha-verified restores — the recursive criterion honoured literally, on record.
- The B entries carry in-run passing controls and fail under DIFFERENT neuters.
- The main corpus baseline and all floor numbers are unmoved by the additions.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-11-SUMMARY.md`
when done. It must name all four fixtures and their scenario numbers, quote one RED excerpt per
entry from the CORPUS-PROOFS record, and state the final self-test tally and the unchanged main
corpus numbers verbatim.
</output>