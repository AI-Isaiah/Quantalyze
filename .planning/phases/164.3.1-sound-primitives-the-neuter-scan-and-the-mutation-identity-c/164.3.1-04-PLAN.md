---
phase: 164.3.1-sound-primitives
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/sql-function-names-naive.mjs
  - scripts/sql-body-normalize.mjs
  - src/__tests__/vac04-reader-guards.test.ts
autonomous: true
requirements: [SC-4, SC-7]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "Invoking either VAC-04 union member through a symlinked path or a path containing a space executes `main()` exactly as a plain-path invocation does — `--self-test` prints its OK line, and no-args naive exits 2 with its usage error — where at HEAD `4752920d` BOTH silently exit 0 having run nothing (re-measured 2026-09-01 with a passing control). [VAC04-C2], SC-4 contribution."
    - "The P10 non-ASCII-identifier input (`CREATE OR REPLACE FUNCTION public.fúnc_é(p uuid)…`) makes EACH reader exit non-zero printing WHAT IT SAW — the identifier prefix it read, the offending character with its U+ codepoint, and the input file — where at HEAD naive prints the truncated name `f` and the normalizer prints nothing, both exit 0 (re-measured 2026-09-01). [VAC04-C4], SC-7 diagnostic-first."
    - "Truncated-or-dropped names are impossible at the LIBRARY level too: `naiveFunctionDefs` and `extractFunctionDefs` throw a marked refusal on any unquoted identifier leaving `[A-Za-z0-9_$]`, so no import path — CLI, vitest, or future caller — can silently receive the wrong subject."
    - "The union's DESIGNED `$` disagreement survives untouched: `sanitize_user$v2` is still read by the naive member and still silently DROPPED (never refused) by the normalizer — `$` is INSIDE the unquoted charset — and quoted identifiers may still contain any character. drift-check-scripts.test.ts's SP-C05 arms (disagreement, independence, corpus parity, e2e RED/GREEN) all stay green."
    - "The refusal is SILENT on the legitimate corpus: the standing 380-file parity arm in drift-check-scripts.test.ts passes post-fix — fires-on-P10 / silent-on-corpus, the two-directional shape SC-8 demands of every new loud path."
    - "Reader failure diagnostics never echo function body text — the refusal also fires on PROD dumps (BODY_NAME_INDEX_CMD runs the normalizer over a PROD dump), and this repo's CI logs are public. The normalizer header's 'prints no body text in any reporting mode' contract is preserved."
    - "SC-4 remains PARTIAL by founder decision (CONTEXT.md Open Tension): this plan closes [VAC04-C2] and [VAC04-C4] reader-side ONLY. [VAC04-C1]/[VAC04-C3], the zero-path warn, the flip arm, and the absurdity floor are plan 164.3.1-07's. Verification must not record SC-4 as met on the strength of this plan."
  artifacts:
    - "scripts/sql-function-names-naive.mjs (realpath guard + charset refusal + 1 new self-test check → 13)"
    - "scripts/sql-body-normalize.mjs (realpath guard + charset refusal with documented $ exemption + 1 new self-test check → 9)"
    - "src/__tests__/vac04-reader-guards.test.ts (new — guard pins symlink/space/direct + charset pins, both members driven through the SAME probes)"
  key_links:
    - "reader exit ≠ 0 → prod-body-drift-check.sh's EXISTING `|| fail` wrappers at :207/:221/:272/:274/:313/:316/:450 (code-read 2026-09-01) — a refusal already fails the gate loudly with NO gate-script change; plan 07 owns that file and needs no propagation work for this"
    - "guard idiom ← check-banned-packages.mjs:443-452, DUPLICATED verbatim per member, never extracted to a shared module — the naive reader's node:-builtins-only independence is pinned at drift-check-scripts.test.ts:612-638, and shared guard code would recreate the one-mechanism-both-members defect [VAC04-C2] names"
    - "silent-direction proof ← drift-check-scripts.test.ts's standing corpus-parity arm (~:712, 'the two readings agree on the ENTIRE real corpus') — it imports BOTH library functions over all 380 .sql files, so a refusal that fires on legitimate input reds it by construction"
    - "symlink-pin harness pattern ← check-banned-packages.test.ts:637-662 ('the exec guard still runs the gate via an absolute path through a symlink')"
---

<objective>
Harden BOTH VAC-04 union readers — `scripts/sql-function-names-naive.mjs` and
`scripts/sql-body-normalize.mjs` — against the two measured reader-side vacuity members:

- **[VAC04-C2]** lift the realpath main-module guard from `check-banned-packages.mjs:443-452`
  into BOTH members, replacing the URL-string guard that no-ops on any symlinked path or path
  containing a space (measured at HEAD: symlinked `--self-test` exits 0 printing NOTHING on both
  members, while the direct run prints its OK line — the two "independent" derivations fail
  together through ONE mechanism).
- **[VAC04-C4]** refuse-rather-than-truncate on any unquoted identifier leaving `[A-Za-z0-9_$]`,
  in BOTH members: the P10 input must make each reader ERROR with a diagnostic-first message
  (SC-7), where at HEAD naive prints the truncated name `f` and the normalizer prints nothing,
  both exit 0 — so the gate can compare the WRONG function body and report MATCH for a
  different subject.

Purpose: both members carrying the identical broken guard is what makes VAC-04's "Two
independent readings agree" a lie rather than a redundancy — a fail-OPEN guard over PRODUCTION
function bodies means a green gate that read nothing. Fixing only one member would be worse than
fixing neither: it would manufacture the appearance of independence while the composing failure
mechanism stays live. Both members are fixed in the same tasks, driven by the same probes.

**Scope fence (binding):** this plan touches the two READER scripts and one new test file, and
NOTHING else. `scripts/prod-body-drift-check.sh` — its zero-path warn, grep exit-code
branching, flip arm, and absurdity floor — is plan 164.3.1-07's; do not edit it. The refusal
exit codes ALREADY propagate: every reader invocation in the gate is `|| fail`-wrapped
(:207/:221/:272/:274/:313/:316/:450, code-read this session), so a non-zero reader exit fails
the gate loudly today with no gate change. File-disjoint from wave-1 siblings 01
(`scripts/mutation-runner/**`), 02 (`src/__tests__/lint-sql-gates.test.ts` + AST rule), and
03 (phase-directory artifacts only). Do not touch `src/__tests__/audit-coverage.test.ts` (06),
`src/__tests__/drift-check-scripts.test.ts` (run it, never edit it), or any `.github/workflows/`
file.

⚠️ **SC-4 stays PARTIAL by founder decision** — VAC-04's zero path WARNS until Phase 164.4
lands (CONTEXT.md Open Tension). Nothing in this plan may make a currently-warning path fail
closed, and neither the SUMMARY nor verification may record SC-4 as fully met.

⛔ **Safety:** this checkout's Supabase CLI is linked to PRODUCTION. Nothing in this plan
touches any database — all probes run the two reader scripts on scratch files. Never
`supabase db push` / `db reset` / `--project-ref` / `--db-url` from this directory.

Output: both readers hardened with pins that drive symlink AND direct invocation, charset
refusal proven fires-on-P10 and silent-on-corpus, diagnostics that print what they saw without
echoing body text, and the pre-fix REDs recorded verbatim.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@scripts/sql-function-names-naive.mjs
@scripts/sql-body-normalize.mjs
@scripts/check-banned-packages.mjs
@src/__tests__/drift-check-scripts.test.ts
@src/__tests__/check-banned-packages.test.ts

Measured anchors, re-driven 2026-09-01 at HEAD `4752920d` (RESEARCH measured at `fa2291d2`;
re-grep before editing if HEAD has moved — `plan-anchor-verify` will also catch drift):

- Broken guard, verbatim-identical in both members: `sql-function-names-naive.mjs:223-227` and
  `sql-body-normalize.mjs:622-626` — `const invokedDirectly = process.argv[1] &&
  import.meta.url === \`file://${process.argv[1]}\`;`
- Correct idiom: `check-banned-packages.mjs:443-450` (`invokedDirectly()` comparing
  `realpathSync(process.argv[1])` to `fileURLToPath(import.meta.url)`, `resolve()` fallback in
  catch), call site `:452`, imports at `:39-41`.
- **C2 RED re-driven this session:** symlinked `--self-test` on BOTH members → exit 0, empty
  stdout. Passing control: direct naive `--self-test` → exit 0, `sql-function-names-naive
  self-test OK (12 checks)`; direct normalizer `--self-test` → exit 0, `sql-body-normalize
  self-test OK (8 checks)`. **Pre-edit self-test counts: naive 12, normalizer 8.**
- **C4 RED re-driven this session:** `CREATE OR REPLACE FUNCTION public.fúnc_é(p uuid)…` →
  naive prints `f`, exit 0 (chain regex stops at the `ú` byte); normalizer `--function-names`
  prints NOTHING, exit 0 (`readQualifiedName` returns name `f`… then `extractFunctionDefs`
  drops the def at the `masked[j] !== "("` check, `:313`).
- Naive truncation site: `DEF_RE` at `sql-function-names-naive.mjs:85-86` (chain charset
  `[A-Za-z0-9_$]` / `"…"`); defs loop `:111-126`.
- Normalizer drop site: `readQualifiedName:240-272` (ident loop `/[A-Za-z0-9_]/` at `:261` —
  note: NO `$`), consumed at `extractFunctionDefs:309`, drop at `:313`.
- SP-C05 union pins that MUST stay green (read, never edit): `drift-check-scripts.test.ts:591`
  (normalizer must EXIT 0 and print nothing for `sanitize_user$v2` — the designed
  disagreement), `:612` (naive imports node: builtins only), `:642`/`:655` (e2e RED/GREEN gate
  arms), `~:712` (380-file corpus parity, imports both library functions).
- Symlink-pin harness analog: `check-banned-packages.test.ts:637-662`.
- Gate propagation (read-only, plan 07's file): every reader invocation in
  `prod-body-drift-check.sh` is `|| fail`-wrapped — `:207-208`, `:221-222`, `:272-275`,
  `:313-317`, `:450`.
- Node 22 present locally at `/opt/homebrew/opt/node@22/bin/node` (v22.22.1) — CI runs Node 22,
  local default is v25.8.1.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [VAC04-C2] — lift the realpath guard into BOTH union members, pins drive symlink AND direct</name>
  <files>scripts/sql-function-names-naive.mjs, scripts/sql-body-normalize.mjs, src/__tests__/vac04-reader-guards.test.ts</files>
  <precondition>Repo devDependencies are installed — `npx vitest --version` from the repo root prints a 4.x version resolved from the repo's own node_modules (the pins run under the repo's vitest, not a downloaded one).</precondition>
  <read_first>
    - scripts/sql-function-names-naive.mjs:205-227 — main() and the broken guard (note the no-args exit-2 usage error at :209-214: it is the passing control that proves main ran)
    - scripts/sql-body-normalize.mjs:543-626 — main() and the identical broken guard
    - scripts/check-banned-packages.mjs:39-41 and :436-453 — the imports and the correct `invokedDirectly()` idiom to lift, including its header comment explaining WHY realpaths (import.meta.url is realpath-resolved, argv[1] is not) and why the catch falls back to `resolve()` rather than throwing
    - src/__tests__/check-banned-packages.test.ts:637-662 — the house symlink-probe harness (mkdtemp + symlinkSync + spawnSync)
    - src/__tests__/drift-check-scripts.test.ts:612-638 — the independence pin: the naive reader's module specifiers must ALL start with `node:` (this is why the guard is duplicated, never shared)
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md — Premise Ledger rows P6/P7 and the "realpath guard to lift" code block
  </read_first>
  <behavior>
    New file `src/__tests__/vac04-reader-guards.test.ts`, describe block "main-module guard
    ([VAC04-C2])", parameterized over the two members
    ({ script: "scripts/sql-function-names-naive.mjs", okMarker: "sql-function-names-naive self-test OK (" }
    and { script: "scripts/sql-body-normalize.mjs", okMarker: "sql-body-normalize self-test OK (" }):

    - Arm 1 (passing control, per member): `spawnSync("node", [script, "--self-test"])` →
      status 0 AND stdout contains okMarker. Proves the probe can distinguish ran from
      didn't-run.
    - Arm 2 (symlink, per member): symlink `resolve(script)` into a fresh
      `mkdtempSync(join(tmpdir(), "vac04-guard-"))`, spawn `node <link> --self-test` →
      status 0 AND stdout contains okMarker. AT HEAD THIS ARM IS RED: stdout is empty because
      `main()` never runs — that is the recorded pre-fix observation.
    - Arm 3 (space-in-path, per member): `cpSync` the script into
      `mkdtempSync(join(tmpdir(), "vac04 guard sp-"))` (template contains spaces), spawn the
      copy with `--self-test` → status 0 AND stdout contains okMarker. Isolates the
      space-breaks-the-URL-string mechanism (the copy's own import.meta.url percent-encodes
      the space; the old guard's `file://` concatenation does not). Both scripts import only
      node: builtins, so a copy runs from anywhere.
    - Arm 4 (exit-code-flipping arm, naive only): spawn the SYMLINKED naive with NO args →
      status 2 AND stderr contains "at least one file is required". At HEAD: status 0, empty
      output. This is the arm where the EXIT CODE itself flips, not just stdout.
  </behavior>
  <action>
    TDD order, and the pre-fix RED is part of the deliverable:

    1. Write the guard pins above in the NEW file `src/__tests__/vac04-reader-guards.test.ts`
       (imports: vitest, node:child_process spawnSync, node:fs mkdtempSync/symlinkSync/cpSync,
       node:os tmpdir, node:path join/resolve — follow the check-banned-packages.test.ts:637
       harness shape). Run `npx vitest run src/__tests__/vac04-reader-guards.test.ts` at HEAD
       and CAPTURE the failing output verbatim — arms 2/3/4 must be RED (silent exit 0 /
       missing marker) while arm 1 is green. This recorded RED is the [VAC04-C2] pre-fix
       observation the SUMMARY must quote; also re-drive the raw shell probe once
       (`ln -s "$PWD/scripts/sql-function-names-naive.mjs" <tmp>/link.mjs && node <tmp>/link.mjs --self-test; echo $?`)
       and record it. Store scratch output under the session scratchpad, not /tmp.
    2. In EACH member, replace the two-line guard (`sql-function-names-naive.mjs:223-227`,
       `sql-body-normalize.mjs:622-626`) with the `invokedDirectly()` function idiom from
       `check-banned-packages.mjs:443-452`, lifted VERBATIM in structure: `if
       (!process.argv[1]) return false;` then `try { return realpathSync(process.argv[1]) ===
       fileURLToPath(import.meta.url); } catch { return resolve(process.argv[1]) ===
       fileURLToPath(import.meta.url); }`, called as `if (invokedDirectly()) {
       process.exit(main(process.argv.slice(2))); }`. Add the needed imports per member:
       naive extends its existing `node:fs` import with `realpathSync` and adds
       `fileURLToPath` from `node:url` and `resolve` from `node:path`; the normalizer does the
       same alongside its existing `node:fs`/`node:crypto` imports. All are node: builtins, so
       the independence pin (drift-check-scripts.test.ts:612) stays green.
       DUPLICATE the function into each file — do NOT extract a shared helper module: the two
       members must share no code (a shared guard would recreate the
       one-mechanism-fails-both-members defect this task closes, and the naive member's
       node:-builtins-only import contract is machine-pinned). Carry a short comment on each
       guard stating why realpaths (symlink + space-in-path both no-op the URL-string
       comparison — measured 2026-09-01), why the catch falls back toward RUNNING the gate
       (fail toward running, never toward skipping), and why `!process.argv[1]` returns false
       (both files are imported by vitest suites; import must not trigger main's exit).
    3. Do NOT touch either selfTest() in this task — the restore proof is that both self-test
       counts are UNCHANGED (pre-edit measured: naive 12 checks, normalizer 8 checks;
       re-measure both counts yourself BEFORE editing, per the count-tokens-pre-edit rule).
    4. Re-run the pins — all arms green. If any restore of a partially-edited file is ever
       needed mid-task, use byte backups verified by `shasum -a 256`, re-taken after every
       edit; NEVER `git checkout --` in this harness.
  </action>
  <reversibility rating="reversible">Guard replacement in two scripts plus one new test file; reverting is restoring two small code blocks and deleting the file. No data, schema, or workflow surface changes.</reversibility>
  <verify>
    <automated>npx vitest run src/__tests__/vac04-reader-guards.test.ts && node scripts/sql-function-names-naive.mjs --self-test | grep -F "self-test OK (12 checks)" && node scripts/sql-body-normalize.mjs --self-test | grep -F "self-test OK (8 checks)"</automated>
    <fails_when>vitest exits non-zero naming a failing guard arm (a symlinked or space-path invocation still silently skips main, or the no-args symlinked naive did not exit 2), or either self-test pipeline fails — non-zero exit, or grep exits 1 because the printed check count moved off the pre-edit 12/8 (the guard task altered self-test behavior it must not touch).</fails_when>
  </verify>
  <acceptance_criteria>
    - Both members contain the lifted idiom — `grep -c "realpathSync(process.argv\[1\])" scripts/sql-function-names-naive.mjs scripts/sql-body-normalize.mjs` reports exactly 1 per file — and each carries its own copy with the why-comment; no shared guard module exists. ([VAC04-C2]; CONTEXT.md code-insight "realpath idiom … to be lifted into BOTH union members")
    - The pre-fix RED is recorded: the SUMMARY quotes the verbatim HEAD-state failing vitest arms AND the raw symlink probe (exit 0, empty stdout) beside the passing direct control — the anti-vacuity contract's observe-RED-then-fix, satisfied by the defect being live at HEAD rather than by an artificial neuter.
    - Post-fix, symlink and space-path invocations of BOTH members print their self-test OK line with status 0, and the symlinked no-args naive exits 2 with its usage error — main() runs on every invocation shape.
    - `drift-check-scripts.test.ts`'s independence pin still passes (naive's module specifiers all start with `node:`) — checked in Task 3's full run, but no non-builtin import may be introduced here.
    - Self-test counts unchanged at 12/8 (this task adds no checks); `git status --porcelain` shows changes ONLY in the three declared files.
  </acceptance_criteria>
  <done>Both union members execute main() under direct, symlinked, and space-containing invocation paths (pinned by committed vitest arms that were observed RED at HEAD first); self-test behavior is byte-for-byte equivalent (12/8 checks); pre-fix RED recorded verbatim for the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [VAC04-C4] — refuse-rather-than-truncate in BOTH readers, diagnostic-first, with the $ exemption preserved</name>
  <files>scripts/sql-function-names-naive.mjs, scripts/sql-body-normalize.mjs, src/__tests__/vac04-reader-guards.test.ts</files>
  <read_first>
    - scripts/sql-function-names-naive.mjs:79-126 — DEF_RE (the chain stops at the first byte outside `[A-Za-z0-9_$]`/quotes; the follower byte is where truncation is detectable) and naiveFunctionDefs
    - scripts/sql-body-normalize.mjs:237-272 and :294-355 — FN_RE, readQualifiedName (ident loop `/[A-Za-z0-9_]/` at :261 — deliberately narrower than the Postgres charset: NO `$`), and extractFunctionDefs (the `masked[j] !== "("` drop at :313; note `masked` is in scope — the follower check must read MASKED text so a comment butted against the name in the LEXER path, e.g. `foo/*c*/(…)`, is still masked to a space and stays legal)
    - scripts/sql-body-normalize.mjs:25-31 — the SCOPE BOUNDARY header: "prints no body text in any reporting mode … this repository is PUBLIC and a PROD body can contain a surgical patch that exists nowhere in it" — the refusal diagnostic is bound by this
    - src/__tests__/drift-check-scripts.test.ts:591-610 — the pin that DEFINES the $ exemption: the normalizer must EXIT 0 printing NOTHING for `sanitize_user$v2`; a refusal that fires on `$` breaks this arm, the e2e arms at :642/:655, and the union's whole design
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md — Premise Ledger row P10 and Architecture Pattern 3(c)
  </read_first>
  <behavior>
    Extend `src/__tests__/vac04-reader-guards.test.ts` with describe "charset refusal
    ([VAC04-C4])". Fixture files are written to fresh temp dirs per test. The P10 fixture is
    `CREATE OR REPLACE FUNCTION public.fúnc_é(p uuid) RETURNS void LANGUAGE plpgsql AS $$ …
    $$;` whose dollar-quoted body carries one distinctive body-only sentinel token chosen by
    the executor (used ONLY to assert non-leakage; it must appear nowhere but the fixture
    body).

    - Arm 1: naive CLI on the P10 file → status 1, stderr contains "identifier leaves the
      unquoted charset" AND "U+00FA" (ú). AT HEAD: status 0, stdout `f` — the recorded RED.
    - Arm 2: normalizer `--function-names` on the P10 file → status 1, stderr contains the
      same two markers. AT HEAD: status 0, empty stdout — the recorded RED.
    - Arm 3: normalizer `--function-qualified-names` on the same file → status 1 (the
      qualified mode VAC-04's schema check drives goes through the same throw site).
    - Arm 4 (union covers naive's blind spot): a leading-non-ASCII definition
      `CREATE FUNCTION únc(p int) …` → normalizer status 1 with the charset markers (naive
      cannot even see this line — DEF_RE requires a charset byte after FUNCTION — so the
      normalizer's refusal is what keeps the union sound for this shape; assert naive status 0
      and empty stdout on it, the documented miss).
    - Arm 5 (legal non-ASCII stays legal): `CREATE OR REPLACE FUNCTION public."fúnc"(p int)
      … $$ … $$;` → BOTH readers status 0, stdout contains `fúnc` — quoted identifiers may
      contain any character; the refusal concerns only UNQUOTED identifiers.
    - Arm 6 ($ exemption): a file whose only definition is
      `CREATE OR REPLACE FUNCTION public.sanitize_user$v2(p uuid) …` → naive status 0 stdout
      `sanitize_user$v2`; normalizer `--function-names` status 0 and EMPTY stdout — dropped,
      NOT refused. `$` is inside `[A-Za-z0-9_$]`; the normalizer's own $-blindness is
      SP-C05's measured limitation, covered by the union.
    - Arm 7 (no body leakage): for arms 1 and 2, stderr does NOT contain the fixture's body
      sentinel token — the diagnostic names the identifier and codepoint, never body text.
    - Arm 8 (library-level closure): importing the two modules directly,
      `naiveFunctionDefs(p10Sql)` and `extractFunctionDefs(p10Sql)` each throw with a message
      matching /identifier leaves the unquoted charset/ — no import path receives a truncated
      or silently-narrowed name.
  </behavior>
  <action>
    TDD order again: write the arms, run them at HEAD, capture the RED verbatim (arms 1-4 and
    8 fail; arm 6 already passes — it is the pre-existing behavior being fenced), re-drive the
    two raw CLI probes on the P10 file and record `f`/exit 0 and empty/exit 0, THEN implement:

    1. **Naive member** (`naiveFunctionDefs`): track the line number while iterating (index of
       the split). After a DEF_RE match, examine the follower byte `line[m[0].length]`. Legal
       followers: end-of-line (undefined), whitespace (`/\s/` — covers `\r` on CRLF files), or
       `(`. Anything else means the identifier chain was cut mid-token: throw an Error carrying
       a marker property (e.g. `err.charsetRefusal = { line: <1-based>, prefix: <the matched
       chain m[1]>, offender: <the byte>, codepoint: "U+XXXX" }`) with message including
       "identifier leaves the unquoted charset [A-Za-z0-9_$]". In `main()`, wrap the per-file
       loop: on a marked error print a `::error::` diagnostic naming the FILE, the 1-based
       line number, the identifier prefix read so far, and the offending character with its
       U+ codepoint — then return 1 (2 stays reserved for usage errors). Unmarked errors
       rethrow — an internal crash must stay a loud crash, never a formatted pass.
    2. **Normalizer member** (`extractFunctionDefs`): after the `readQualifiedName` call
       (`:309`), check the follower byte in MASKED text — `masked[afterName]`. Legal:
       undefined (EOF), whitespace, `(`, or `$`. The `$` exemption MUST carry a comment stating
       all three legs: `$` is inside Postgres' unquoted-identifier charset; this reader's own
       inability to read past it is SP-C05's measured limitation (the whole reason the naive
       union member exists); and the drop-not-refuse behavior is machine-pinned by
       drift-check-scripts.test.ts's disagreement arm — refusing `$` would break the union's
       design, not harden it. Any other follower byte: throw the same marked-error shape
       (compute the 1-based line from the offset for the diagnostic). In `main()`, wrap each
       mode's extraction work (`--function-names`, `--function-qualified-names`,
       `--extract-fn`, `--diff-bodies` — one throw site covers all four since they all call
       extractFunctionDefs): marked error → `::error::` diagnostic naming file, line,
       identifier prefix, offending character + codepoint → return 1; unmarked → rethrow.
       `--normalize`/`--hash` read no identifiers and are untouched (VAC-08 drives those on
       migrations; their behavior must not change).
    3. **Diagnostic bound (both members):** the refusal prints identifier-and-position facts
       ONLY — never the surrounding source line, never any slice of a function body. The
       normalizer's index run executes over a PROD dump in a public-log CI job; echoing the
       line would leak PROD body text. This is the SC-7 shape: print what you SAW (the
       identifier, the byte, the position), not what surrounds it.
    4. **Self-tests:** add EXACTLY ONE new check to each member's selfTest() asserting the P10
       shape is refused (try/catch around the library call, checking the marker), bringing
       counts to naive 13 / normalizer 9. The check message must state the refusal exists so
       the gate cannot compare the wrong subject ([VAC04-C4]).
    5. Re-run all arms green, plus `npx vitest run src/__tests__/drift-check-scripts.test.ts`
       — the SP-C05 disagreement arm, independence arm, both e2e arms, and the 380-file
       corpus-parity arm must ALL still pass. The corpus-parity arm doubles as the
       silent-direction proof: the refusal fires zero times across every real migration and
       schema file. If ANY corpus file trips the refusal, STOP — do not widen the exemption
       ad hoc; record the exact file, identifier, and byte in the SUMMARY as a blocker (the
       corpus premise has drifted and the follower rule needs re-grounding against it).
    6. Over-refusal on degenerate-but-conceivable shapes (e.g. a comment butted directly
       against the name in the NAIVE reader's line-anchored view, `foo/*c*/(…)` — the lexer
       path masks it legal, the naive path refuses) is the ACCEPTED direction: loud false
       refusal over silent wrong-subject comparison. State this in a comment beside the naive
       follower check, citing the corpus-parity arm as the zero-firings evidence.
  </action>
  <reversibility rating="reversible">Adds throw-and-catch refusal paths and two self-test checks; reverting restores the prior silent-narrowing behavior. No schema, data, or CI-workflow change. The exit-code contract (1 = refusal, 2 = usage) matches existing conventions.</reversibility>
  <verify>
    <automated>npx vitest run src/__tests__/vac04-reader-guards.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/drift-check-scripts.test.ts && node scripts/sql-function-names-naive.mjs --self-test | grep -F "self-test OK (13 checks)" && node scripts/sql-body-normalize.mjs --self-test | grep -F "self-test OK (9 checks)"</automated>
    <fails_when>vitest exits non-zero — a charset arm red (a reader still exits 0 on the P10 input, or the diagnostic lacks the charset phrase or the U+00FA codepoint, or leaked the body sentinel), an SP-C05 union arm red (the refusal fired on the `$` identifier and broke the designed disagreement or the e2e gate arms), or the 380-file corpus-parity arm red (the refusal fired on legitimate corpus input) — or either self-test pipeline fails: non-zero exit, `SELF-TEST FAIL` printed, or grep exits 1 because the count is not exactly 13/9 (the one-new-check-per-reader contract was violated).</fails_when>
  </verify>
  <acceptance_criteria>
    - The P10 input makes EACH reader exit 1 with a diagnostic carrying: the input file, the 1-based line, the identifier prefix actually read, the offending character with its U+ codepoint, and the phrase "identifier leaves the unquoted charset" — diagnostic-first per SC-7 (the failure names what was measured, not only a conclusion). Pre-fix RED (naive `f`/exit 0; normalizer silent/exit 0) recorded verbatim in the SUMMARY.
    - `naiveFunctionDefs` and `extractFunctionDefs` THROW on charset-leaving unquoted identifiers (arm 8) — the silent-narrowing path no longer exists at any call depth. ([VAC04-C4] closed at the library boundary, not just the CLI)
    - The `$` exemption is implemented in the normalizer ONLY, commented with its three-leg justification, and arm 6 plus drift-check-scripts.test.ts:591 both pass — `sanitize_user$v2` still produces the designed union disagreement.
    - Quoted identifiers containing any character remain accepted by both readers (arm 5).
    - Refusal diagnostics contain no body text: arm 7's sentinel assertion passes, and the normalizer's "prints no body text in any reporting mode" header contract is intact.
    - The corpus-parity arm passes post-fix: zero refusals, zero disagreements across all 380 real .sql files — the silent direction of the two-directional proof, via the standing committed arm rather than a new instrument.
    - Self-test counts are exactly 13 and 9; `git status --porcelain` shows changes ONLY in the three declared files.
  </acceptance_criteria>
  <done>Both readers ERROR loudly — with what-I-saw diagnostics — on any unquoted identifier leaving `[A-Za-z0-9_$]`, at CLI and library level; the $ disagreement and the whole SP-C05 arm family stay green; fires-on-P10 / silent-on-corpus both observed and recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Restore-direction sweep — contracts, Node 22, scope fence, and the evidence record</name>
  <files>src/__tests__/vac04-reader-guards.test.ts</files>
  <precondition>Node 22 toolchain present at `/opt/homebrew/opt/node@22/bin/node` (measured v22.22.1 this session) — CI runs Node 22 while local default is 25; a CI-only failure is reproducible only through this PATH.</precondition>
  <read_first>
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-VALIDATION.md — the per-gate neuter/restore map rows "VAC-04 main-module guards" and "VAC-04 charset refusal in readers" (this task assembles exactly that evidence) and the Open Tension section (SC-4 PARTIAL wording the SUMMARY must carry)
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-PLAN-OUTLINE.md — row 164.3.1-07 (the downstream consumer of the hardened readers) and the file-disjointness paragraph
  </read_first>
  <action>
    No production-code edits in this task (the only permitted diff is a test-file polish if the
    contracts or Node-22 runs surface one — any reader-script change belongs to a re-run of
    Tasks 1/2, not here).

    1. Run the contracts suite (`src/__tests__/contracts/` scans ALL of `src/`, so the new
       test file is in its population) and the Node-22 pass on the three affected test files
       — the automated command below. Full vitest is NOT run here (house rule: the full suite
       runs serialized and alone at the phase gate, not per-plan).
    2. Scope-fence proof: `git status --porcelain -- scripts/mutation-runner
       scripts/prod-body-drift-check.sh src/__tests__/lint-sql-gates.test.ts
       src/__tests__/audit-coverage.test.ts .github/workflows` must be EMPTY, and `git show
       --stat` over this plan's commits must list only the three declared files (plus
       `.planning/` artifacts — `.planning/` is tracked; `git add` it).
    3. Assemble the SUMMARY evidence block: (a) the verbatim pre-fix REDs from Tasks 1 and 2
       (failing vitest arms at HEAD + raw probes: symlinked --self-test exit 0/empty on both
       members; P10 → naive `f` exit 0, normalizer empty exit 0); (b) the post-fix green runs
       with the new self-test counts 13/9; (c) the corpus-parity arm's pass as the
       silent-direction proof, with the arm's own printed population facts (>200 files, >100
       definitions asserted inside it); (d) the propagation note for plan 07 — reader exit 1
       already fails the gate via the existing `|| fail` wrappers, no gate change needed;
       (e) the SC-4 PARTIAL statement, verbatim from must_haves truth 7: this plan closes
       C2+C4 reader-side only; C1/C3/zero-path/flip-arm/floor are plan 07's, and the zero
       path still WARNS by founder decision until 164.4.
    4. If anything in step 1 fails: fix forward only within this plan's three files; a failure
       that implicates a sibling plan's file is a STOP — record it in the SUMMARY as a
       blocker, do not cross the fence.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts && PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run src/__tests__/vac04-reader-guards.test.ts src/__tests__/sql-body-normalize.test.ts src/__tests__/drift-check-scripts.test.ts && [ -z "$(git status --porcelain -- scripts/mutation-runner scripts/prod-body-drift-check.sh src/__tests__/lint-sql-gates.test.ts src/__tests__/audit-coverage.test.ts .github/workflows)" ]</automated>
    <fails_when>the contracts suite exits non-zero (a contract scanning all of src/ was reddened by the new test file — even a comment can do it), or the Node-22 vitest pass exits non-zero (a CI-only incompatibility that local Node 25 hides), or the bracketed test fails because porcelain printed a path — an edit leaked across the wave-1 file fence into a sibling plan's territory.</fails_when>
  </verify>
  <acceptance_criteria>
    - Contracts suite green with the new test file present in src/__tests__/.
    - All three affected test files green under Node 22 (the CI runtime), not just local Node 25.
    - Fence intact: zero changes — staged, unstaged, or committed by this plan — to `scripts/mutation-runner/**`, `scripts/prod-body-drift-check.sh`, `src/__tests__/lint-sql-gates.test.ts`, `src/__tests__/audit-coverage.test.ts`, or `.github/workflows/**`.
    - The SUMMARY carries: both verbatim pre-fix REDs, the post-fix greens with counts 13/9, the corpus-parity silent-direction proof, the plan-07 propagation note, and the SC-4 PARTIAL statement — so the phase verifier can mark SC-4 PARTIAL with evidence rather than absorbing the gap.
  </acceptance_criteria>
  <done>All suites green on both Node versions, the fence is proven intact, and the SUMMARY's evidence block lets the verifier reproduce every claim — RED, GREEN, silent-direction, and the deliberate SC-4 partiality — without re-deriving anything.</done>
</task>

</tasks>

<threat_model>
This plan hardens two CLI readers that a CI gate runs over PR-authored migration files and a
PRODUCTION function-body dump, in a PUBLIC repository whose CI logs are public. No database is
touched by the plan itself; no packages are installed.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR-authored migrations → readers | untrusted input crosses into the parsers whose output decides "new function (pass)" vs "compare against PROD" |
| PROD dump → normalizer index/fetch | sensitive body text flows through the reader whose failure diagnostics land in public CI logs |
| invocation path (argv[1]) → main-module guard | the runner's filesystem layout (symlinks, spaces) decides whether the gate's readers execute AT ALL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-04-01 | Tampering | fail-OPEN main-module guard in BOTH union members — a symlinked or spaced invocation path makes each exit 0 having read NOTHING over PROD bodies: a green gate that measured nothing, through one shared mechanism | high | mitigate | Task 1: realpath guard lifted into both members (duplicated, never shared), pinned by symlink + space + direct arms observed RED at HEAD first; catch falls back toward RUNNING, never skipping |
| T-164.3.1-04-02 | Spoofing | subject confusion via identifier truncation — a charset-leaving identifier makes the gate look up and compare the WRONG function body (naive reads `f` for `fúnc_é`) and can report MATCH for a different subject | high | mitigate | Task 2: refuse-rather-than-truncate as a library-level throw in both readers, exit 1 at the CLI; propagates through the gate's existing `\|\| fail` wrappers with no gate change |
| T-164.3.1-04-03 | Information Disclosure | refusal diagnostics echoing PROD body text into public CI logs (the normalizer's index run executes over a PROD dump) | medium | mitigate | diagnostic bound to identifier-and-position facts only (file, line, prefix, offending byte + codepoint); body-sentinel non-leakage arm; the module's "prints no body text in any reporting mode" header contract preserved |
| T-164.3.1-04-04 | Denial of Service | over-refusal wedging legitimate migration PRs on legal-but-unusual shapes (e.g. a comment butted against the name in the naive line-anchored view) | low | accept | loud false refusal is this phase's chosen direction over silent wrong-subject comparison; the 380-file corpus-parity arm proves zero firings on every real file, and a future firing names its exact file, identifier, and byte |
| T-164.3.1-04-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan; RESEARCH's Package Legitimacy Audit records none for the whole phase; the banned-package gate itself (whose guard idiom this plan lifts) is untouched |
</threat_model>

<verification>
From the repo root, after all three tasks:

1. `npx vitest run src/__tests__/vac04-reader-guards.test.ts` — green: guard arms (direct, symlink, space, no-args exit-2) and charset arms (P10 refusals with codepoint diagnostics, qualified-mode refusal, leading-non-ASCII refusal, quoted-identifier acceptance, $ exemption, no-body-leak sentinel, library-level throws) all pass for BOTH members.
2. `node scripts/sql-function-names-naive.mjs --self-test` prints `self-test OK (13 checks)`, exit 0; `node scripts/sql-body-normalize.mjs --self-test` prints `self-test OK (9 checks)`, exit 0 — and BOTH also do so when invoked through a symlink.
3. `npx vitest run src/__tests__/drift-check-scripts.test.ts` — green: the SP-C05 disagreement arm (normalizer exit 0 + empty on `sanitize_user$v2`), the node:-builtins independence arm, both e2e gate arms, and the 380-file corpus-parity arm (zero refusals on legitimate input — the silent direction).
4. `npx vitest run src/__tests__/sql-body-normalize.test.ts` and `npx vitest run src/__tests__/contracts` — green; the Node-22 pass (`PATH=/opt/homebrew/opt/node@22/bin:$PATH`) on the three affected test files — green.
5. Fence: `git status --porcelain -- scripts/mutation-runner scripts/prod-body-drift-check.sh src/__tests__/lint-sql-gates.test.ts src/__tests__/audit-coverage.test.ts .github/workflows` empty; `git show --stat` on this plan's commits lists only the three declared files plus `.planning/` artifacts.
6. The SUMMARY quotes both verbatim pre-fix REDs (symlink silent-exit-0 probes; P10 `f`/empty probes) beside the passing controls, and records SC-4 as PARTIAL with the founder-decision reason — never as met.
</verification>

<success_criteria>
- [VAC04-C2] closed both-members: symlinked, spaced, and direct invocations all execute main(), pinned by arms observed RED at HEAD before the fix — the one-mechanism-fails-both-members shape cannot recur silently.
- [VAC04-C4] closed both-members at library AND CLI level: the P10 input errors loudly with what-I-saw diagnostics (identifier prefix, offending byte, U+ codepoint, file, line); no path can compare the wrong subject silently.
- SC-7 satisfied for these two gates: every new failure path PRINTS ITS EVIDENCE, and prints no body text.
- The union's design intact: `$` disagreement preserved, quoted identifiers unrestricted, corpus parity zero-disagreement, naive independence pinned — proven by the standing drift-check-scripts.test.ts arms, unedited.
- Two-directional proof on the new loud path: fires on P10 (both readers), silent on all 380 real corpus files.
- SC-4 recorded as PARTIAL (reader-side members C2+C4 only; C1/C3/zero-path/flip/floor → plan 164.3.1-07; fail-closed flip deferred to 164.4 by founder decision).
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Path | Consumer |
|----------|------|----------|
| Hardened naive union member (realpath guard, charset refusal, 13 self-test checks) | `scripts/sql-function-names-naive.mjs` | `prod-body-drift-check.sh` via `NAIVE_NAMES`/`BODY_NAME_INDEX_XCHECK_CMD` (plan 07 builds its zero-path warn and floors on top of readers that can no longer silently no-op or mis-subject) |
| Hardened normalizer union member (realpath guard, charset refusal with documented $ exemption, 9 self-test checks) | `scripts/sql-body-normalize.mjs` | `prod-body-drift-check.sh` (`NORMALIZER`, all four extract modes) and `test-ledger-drift-check.sh` (`--normalize`/`--hash`, untouched behavior); plan 07 |
| Reader guard + charset pin suite | `src/__tests__/vac04-reader-guards.test.ts` | `frontend` aggregator via the vitest shards (zero new CI wiring — SC-1's corpus plans 11/12 may reference its arms); phase verifier |
| Pre-fix RED record (verbatim probes + failing arms at HEAD `4752920d`) | `164.3.1-04-SUMMARY.md` evidence block | Phase verification (SC-4 PARTIAL evidence; the neuter/restore map rows for both reader gates) |

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-04-SUMMARY.md` when done. The SUMMARY must quote both pre-fix REDs verbatim, record the post-fix self-test counts (13/9), cite the corpus-parity arm as the silent-direction proof, carry the plan-07 propagation note (reader exit 1 already fails the gate through the existing `|| fail` wrappers), and record SC-4 as PARTIAL with the founder-decision reason — a criterion marked passed on a deferred promise is the exact shape this phase exists to stop. If the corpus-parity arm reds post-fix, that is a BLOCKER entry (corpus premise drift), not a pass with a caveat.
</output>
