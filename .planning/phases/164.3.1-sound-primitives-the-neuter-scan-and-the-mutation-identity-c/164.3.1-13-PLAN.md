---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "13"
type: execute
subsystem: vac04-gate
tags: [vac04, gap-closure, sc-4, gate-level, realpath-guard, charset-refusal, instance-registry, anti-vacuity]
wave: 1
depends_on: []
files_modified:
  - src/__tests__/drift-check-scripts.test.ts
  - src/__tests__/gate-family-meta.test.ts
autonomous: true
gap_closure: true
requirements: [SC-4]   # the ROADMAP criterion this gap closes; no REQUIREMENTS.md row maps to this phase (plans 04/07 claimed SC-4 the same way)

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low   # derived: estimate-calibration sample_count 0 < min_samples 3 (factor 1, not applied)

must_haves:
  truths:
    - "SC-4 (the gap, VERIFICATION.md gaps[0]): each of [VAC04-C1]..[VAC04-C4] is driven END-TO-END THROUGH THE REAL GATE — scripts/prod-body-drift-check.sh itself, never a reader CLI — and observed on its own measured input: C1 and C3 by the standing plan-07 arms; [VAC04-C2] by new arms in which a reader reached through a SYMLINK and through a SPACE-containing path still RUNS, so the gate reaches the readers-ran line `Functions defined or replaced by this PR: 1`, with EACH union member individually load-bearing; [VAC04-C4] by a new arm in which a changed migration defining `public.fúnc_é(p uuid)` makes the gate exit NON-ZERO naming `U+00FA` and `leaves the unquoted charset` (diagnostic-first, SC-7) with no body text in its output."
    - "Every new arm is proven able to fail TWO ways: a STANDING calibration leg in the same describe (scratch reader copies carrying the pre-fix idiom — the URL-string main-module guard for C2, both charset refusals disabled for C4 — drive the SAME gate wiring to the opposite outcome on every run), and a RECORDED neuter → RED → restore cycle on the REAL readers (byte backups, sha256 re-taken after every edit, never `git checkout --`) whose RED set is exactly the load-bearing cases and nothing else."
    - "The C2 arm's PASS condition is the readers-ran line, never the D-13 tripwire refusal: the refusal is what the NEUTERED wiring produces (both readers `-> 0 name(s)`, then MEASURE_FAIL), so the arm proves the realpath guard fix and not [VAC04-C1]'s tripwire (VERIFICATION gaps[0] 'Note for the fixer')."
    - "INSTANCE_ARM_REGISTRY binds VAC04-C2 and VAC04-C4 to BOTH the reader-level arms (kept) and the new gate-level arms by name via `also:`; every needle resolves in its file's bytes; the reader-vs-gate distinction is visible in the entry itself; the registry fire-proof shows a renamed gate-level needle REDs by instance name."
    - "No gate script, reader script, or plan 01-12 artifact is changed outside a sha-verified neuter window: `git diff --quiet HEAD -- scripts/prod-body-drift-check.sh scripts/sql-function-names-naive.mjs scripts/sql-body-normalize.mjs` exits 0 at the end of every task; no database is contacted (injected commands and scratch temp dirs only; never a supabase CLI command)."
  artifacts:
    - "src/__tests__/drift-check-scripts.test.ts — two new describe blocks placed directly after the `[VAC04-C1]` describe: `[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH THE REAL GATE: a reader reached through a symlink or a space path must RUN` (fixture calibration + four fixed-leg cases + the pre-fix-guard calibration leg) and `[VAC04-C4] GATE-LEVEL — the charset refusal driven THROUGH THE REAL GATE: a non-ASCII identifier must be REFUSED, never compared as a different function` (refusal arm + the disabled-refusal calibration leg + the stated-bound `it`)"
    - "src/__tests__/gate-family-meta.test.ts — the VAC04-C2 and VAC04-C4 entries of INSTANCE_ARM_REGISTRY carrying an `also:` binding to the new describe titles and a citation extended with this plan and its SUMMARY"
    - "164.3.1-13-SUMMARY.md — the recorded cycles C2-N1 (naive guard only), C2-N2 (normalizer guard only), C4-N1 (both refusals) and the registry fire-proof, each with verbatim RED text and the sha256 pair"
  key_links:
    - "gate-level arms → scripts/prod-body-drift-check.sh through `NORMALIZER` / `NAIVE_NAMES` (env-injectable, :143-144) pointing at symlinks and space-path copies under the test's temp dir; the PROD side stays on the standing real-wiring dump commands (word-split on IFS — never handed a space-containing path)"
    - "C2 RED direction → the D-13 zero path (:336-412): the neutered wiring prints both readers' `name(s)` evidence lines at zero (:384, :388) and then the blind-zero MEASURE_FAIL (:379) — observed as the RED, never used as the pass condition"
    - "C4 → :207-208 (`node \"$NORMALIZER\" --function-names … || fail`): `reportCharsetRefusal` (sql-body-normalize.mjs:564-573) writes the `identifier leaves the unquoted charset` diagnostic with the offending codepoint (`${r.codepoint}`) to stderr, which the gate does not redirect, so it reaches the gate output; the naive call at :221 is unreachable on this input (stated non-coverage)"
    - "registry `also:` needles → the two new describe titles, resolved by `countOf(read(b.file), b.needle)` in gate-family-meta.test.ts's binds-by-name `it.each` (:319-332); the non-vacuity arm (:299-317) enforces NEEDLE_MIN_LENGTH 16, uniqueness and the citation regexes"
---

<objective>
Close the one gap in 164.3.1-VERIFICATION.md (SC-4 partial). [VAC04-C2] (realpath main-module
guard) and [VAC04-C4] (charset refusal) were proven on the two reader CLIs only; the gate-level
propagation rested on a code-read of `|| fail` wrappers. This plan drives BOTH through the real
gate, `scripts/prod-body-drift-check.sh`, with its env-injectable reader paths, proves each new
arm able to fail (a standing scratch-copy calibration leg AND a recorded neuter of the real
readers), and binds the new arms into INSTANCE_ARM_REGISTRY beside the reader-level ones.

Scope is EXACTLY the three `missing:` items of VERIFICATION gaps[0]. Test-only: no gate script,
no reader script, no plan 01-12 artifact is edited except inside sha-verified neuter windows.

Measured at planning time (HEAD 0564024c, scratchpad only, injected commands, no database):
- A migration whose only definition is `public.sanitize_user$v2(...)` at line start is seen by the
  naive reader ONLY; `SELECT 1; CREATE OR REPLACE FUNCTION public.mid_fn(a int) …` on one line is
  seen by the normalizer ONLY. Each fixture makes exactly one union member load-bearing — which
  matters because on a definition BOTH readers see, the union masks a single reader's silent zero
  and the gate still prints the readers-ran line.
- Fixed readers reached through a symlink and through a copy under a directory named with a
  space: `Functions defined or replaced by this PR: 1`, `<name>: measured absent`, exit 0.
- Scratch copies with the pre-fix URL-string guard reached through symlinks: both evidence lines
  read zero names, `MEASURE_FAIL — NOTHING WAS COMPARED.`, exit 1 (the D-13 blind-zero refusal).
- Real readers on the P10 input: exit 1, `sql-body-normalize: <file>:2: identifier leaves the
  unquoted charset [A-Za-z0-9_$] — read 'public.f' then hit 'ú' (U+00FA)`, then the gate's own
  `could not extract function names from the changed migrations.`; body sentinel absent.
- Scratch copies with BOTH refusals disabled on P10: `Functions defined or replaced by this PR: 1`,
  `f: measured absent`, `no unacknowledged repo-vs-PROD body drift`, exit 0 — the wrong-subject
  pass. With ONLY the normalizer's refusal disabled the naive refusal still names U+00FA through
  :221's `|| fail` (exit 1) — so the C4 recorded neuter must disable both members.
- One gate run ≈ 0.8 s; vitest.config.ts sets no testTimeout (default 5 s), so every new arm that
  spawns the gate more than once carries an explicit 30_000 timeout (vac04-reader-guards.test.ts's
  convention).

Planner-contribution checkpoints, each stated rather than ignored: assumption-delta does not fire
(test-only gap; no singular→plural / required→optional / derived→chosen transition; no
`<assumption_delta_decision>` needed). schema-gate does not fire (no `supabase/migrations/**` in
scope — every `.sql` fixture lives under the test's mkdtemp dir; no push task). ai-integration
API-coverage does not fire (no external API/SDK; no COVERAGE.md). Security: `<threat_model>` at
ASVS L1, block-on-high — every high threat below is mitigated.

⛔ Safety: never any supabase CLI command from this checkout (linked to PROD). Every arm drives
injected commands over scratch fixtures. TRACER_MODE is moot for a two-arm test gap — no tracer
slice is invented; the tasks are sequential on one file.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-VERIFICATION.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-04-SUMMARY.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-07-SUMMARY.md
@scripts/prod-body-drift-check.sh
@scripts/sql-function-names-naive.mjs
@scripts/sql-body-normalize.mjs
@src/__tests__/drift-check-scripts.test.ts
@src/__tests__/vac04-reader-guards.test.ts
@src/__tests__/gate-family-meta.test.ts

Anchor discipline: drift-check-scripts.test.ts is edited by this plan — locate its arms by TEST
NAME, never by line. The gate script and both readers are edited by NO task here (only inside
neuter windows that are restored byte-for-byte), so their line anchors below hold at execution.
Pre-edit gate tokens, counted at HEAD 0564024c before any edit (the rule: count PRE-edit):
`[VAC04-C2]`, `[VAC04-C4]`, `GATE-LEVEL`, `symlink`, `U+00FA` each occur 0 times in
drift-check-scripts.test.ts; `also: [` occurs 4 times and `GATE-LEVEL` 0 times in
gate-family-meta.test.ts; `realpathSync(process.argv[1])` occurs exactly once in each reader.
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| VERIFICATION gaps[0].missing[1] | Gate-level [VAC04-C2] arm: NORMALIZER/NAIVE_NAMES through a SYMLINKED path and a SPACE path; visible definition; assert the readers-ran line; calibration leg with the pre-fix guard idiom in scratch copies producing zero names | Task 1 |
| VERIFICATION gaps[0].missing[2] | Gate-level [VAC04-C4] arm: real gate on `public.fúnc_é(p uuid)`; exit 1; output names `U+00FA` / `leaves the unquoted charset`; no body text; a neuter with the refusal disabled flips it to the truncated-name path | Task 2 |
| VERIFICATION gaps[0].missing[3] | VAC04-C2 / VAC04-C4 registry entries bind the gate-level arms by name via `also:`, keeping the reader-level bindings | Task 3 |
| VERIFICATION gaps[0] "Note for the fixer" (D-13) | The C2 arm isolates the guard fix — expects the readers-ran line, not the tripwire refusal | Task 1 (pass condition + calibration leg) |
| GOAL (ROADMAP SC-4) | "each of [VAC04-C1]..[VAC04-C4] is driven end-to-end through the real gate" | Tasks 1-2 (C2/C4); C1/C3 already stand (plan 07) |
| CONTEXT D-12 / SC-7 | Diagnostic-first: assertions on failure output check the evidence lines (`U+00FA`, the `name(s)` lines, the file path), not only the exit code | Tasks 1-2 |
| Founder rule (a test that cannot fail is worse than none) | Standing calibration legs + recorded neuter cycles with RED-set specificity, byte backups, sha256, never `git checkout --` | Tasks 1-3 |
| RESEARCH Pitfall 6 (test the wiring, not the helper) | Every arm invokes scripts/prod-body-drift-check.sh; reader CLIs are spawned only to CALIBRATE fixtures and neuters | Tasks 1-2 |

Excluded (not gaps, per VERIFICATION `deferred:` and the scope fence): [MUT-I02]/[MUT-I03]
(TODOS.md); FILES_FLOOR raise (Phase 164.4); REVIEW.md WR-04's two bare-grep sites at
prod-body-drift-check.sh :667 (gate-script behaviour — outside this gap; route to TODOS.md if not
already there, do not fix here); the naive reader's refusal at gate level on the P10 input
(unreachable — the normalizer refuses first at :207; stated non-coverage in the C4 arm's comment).
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate-level [VAC04-C2] — readers reached through a SYMLINK and a SPACE path still run through the real gate; the pre-fix guard idiom is proven to produce zero names</name>
  <reversibility rating="reversible">Test-only edit to one vitest file; undo is deleting one describe block.</reversibility>
  <precondition>`node_modules/.bin/vitest` resolves in this checkout (GSD worktrees get no node_modules — symlink it before starting) AND `node scripts/sql-function-names-naive.mjs --self-test` prints `self-test OK (13 checks)` while `node scripts/sql-body-normalize.mjs --self-test` prints `self-test OK (9 checks)` — both readers are in their FIXED state before any neuter window opens.</precondition>
  <files>src/__tests__/drift-check-scripts.test.ts</files>
  <read_first>scripts/prod-body-drift-check.sh:143-153 (the `NORMALIZER`/`NAIVE_NAMES` env defaults, the `-f` existence checks, and the same-file refusal at :151 — the two injected paths must have DIFFERENT basenames or the gate refuses before either reader runs); :204-247 (the two reader invocations under `|| fail` at :207-208 and :221-222, the union at :223, the naive-only warning at :238-244, `NAME_COUNT` at :246); :336-412 (the D-13 zero path: the per-reader `name(s)` evidence lines at :384 and :388, the legitimate-zero notice, the blind-zero refusal at :379 — what the NEUTERED wiring produces); :415 (`Functions defined or replaced by this PR: ${NAME_COUNT}` — the readers-ran line, the arm's pass condition). src/__tests__/drift-check-scripts.test.ts — the `[VAC04-C1]` describe (locate by title): its `scaffold` (real-wiring dump commands + `CHANGED_MIGRATIONS` + `SNAPSHOT_DIR`), its CALIBRATION-first ordering, and the `REOPEN PIN` arm's evidence assertions; the SP-C05 describe's `realWiring()` and its arm "the same input is still classified honestly when PROD does NOT hold it: measured absent, and the disagreement is REPORTED" (the exact `Functions defined or replaced by this PR: 1` + `measured absent` + `ZERO bodies compared` shape the fixed leg reproduces); `withTempDir`, `run`, `FAKE_CREDS` at the top of the file. src/__tests__/vac04-reader-guards.test.ts:63-116 (the symlink shape `symlinkSync(resolve(script), link)` and the space shape `mkdtempSync(join(tmpdir(), "vac04 guard sp-"))` + `cpSync` — reuse the SHAPES; do not restate the reader-level assertions). scripts/sql-function-names-naive.mjs:337-348 and scripts/sql-body-normalize.mjs:764-775 (the duplicated `invokedDirectly()` realpath guard — the two comparison lines each neutered scratch copy replaces); scripts/sql-function-names-naive.mjs:247-250 (the `LIMITATION 1` self-test — the mid-line blind spot fixture B relies on). 164.3.1-VERIFICATION.md gaps[0] (the D-13 note: pass condition = readers-ran line, never the tripwire refusal).</read_first>
  <behavior>
    - CALIBRATION (first, like the C1 describe): fixture A (`CREATE OR REPLACE FUNCTION public.sanitize_user$v2(p uuid) RETURNS void LANGUAGE plpgsql AS $fn$ BEGIN END; $fn$;` at line start) is seen by the naive reader ONLY (lexer stdout empty, naive stdout `sanitize_user$v2`, both exit 0); fixture B (`SELECT 1; CREATE OR REPLACE FUNCTION public.mid_fn(a int) RETURNS int LANGUAGE sql AS $$ SELECT a $$;` on one line) is seen by the normalizer ONLY (lexer `mid_fn`, naive empty). Without this, "the gate reached 1" could hold with both readers seeing the fixture, and a single-member neuter would not RED.
    - FIXED LEG, four cases as separate `it`s (RED-set specificity), each driving the REAL gate with the standing real-wiring PROD commands (dump = the `some_other_fn` definition, so the PR's name is measured absent), an empty `snapshot/` dir, and `NORMALIZER`/`NAIVE_NAMES` injected as: case 1 naive via a copy under a SPACE-named directory + normalizer via a symlink, fixture A; case 2 naive via a symlink + normalizer via a space copy, fixture A; case 3 normalizer via a symlink + naive via a space copy, fixture B; case 4 normalizer via a space copy + naive via a symlink, fixture B. Each asserts exit 0, `Functions defined or replaced by this PR: 1`, `<name>: measured absent` for its fixture's name, and NOT `MEASURE_FAIL`; fixture-A cases additionally assert the naive-only warning text `the independent name reader found function definition(s) the normalizer's parser did not` (proof the naive reader ran through its injected path).
    - CALIBRATION LEG (the standing RED direction): a by-name pin that each REAL reader source carries `realpathSync(process.argv[1])` at least once; scratch copies of both readers written to the temp dir with the two guard comparison lines replaced by the pre-fix URL-string idiom (assert the copy carries ZERO occurrences of the realpath idiom and at least one of the pre-fix idiom — a neuter not proven applied makes this leg vacuous); the gate driven through SYMLINKS to the neutered copies on fixture A and on fixture B → exit non-zero, `MEASURE_FAIL`, the two evidence lines naming the injected paths each ending in `0 name(s)`, and NOT the readers-ran line. This is the D-13 blind-zero refusal — observed as the RED, never the pass condition.
  </behavior>
  <action>
    Add a new describe titled exactly `[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH
    THE REAL GATE: a reader reached through a symlink or a space path must RUN` (one line in the
    source; this title is the registry needle Task 3 binds) directly after the `[VAC04-C1]`
    describe closes. Header comment: what it proves (SC-4's "driven through the real gate" for
    C2), why the fixtures are per-reader-visible (the union masks a single reader's silent zero on
    a both-visible definition — measured 2026-09-02), why the pass condition is the readers-ran
    line and not the tripwire refusal (D-13), and the stated bound that PROD-side commands are
    word-split so only `NORMALIZER`/`NAIVE_NAMES` — the left-hand readers — take the injected
    shapes. Add `symlinkSync`, `cpSync` (node:fs) and `resolve` (node:path) to the file's imports.

    Write a local `scaffold` mirroring the C1 describe's: temp `snapshot/` dir, a dump file holding
    the `some_other_fn` definition, the standing real-wiring `BODY_FETCH_CMD` /
    `BODY_NAME_INDEX_CMD` / `BODY_NAME_INDEX_XCHECK_CMD` on the canonical repo reader paths, the
    migration file from the fixture text, `FAKE_CREDS`, plus `NORMALIZER` and `NAIVE_NAMES` taken
    from a `readers` argument. Write a `readerPath(shape, script, dir)` helper: `symlink` →
    `symlinkSync(resolve(script), join(dir, "links", basename(script)))`; `space` → `cpSync` into
    `join(dir, "reader copies", basename(script))` (the directory name carries the space; keep the
    two readers' basenames distinct so :151's same-file check passes). Implement the behavior
    block's calibration arm, the four fixed-leg cases (an `it.each` over a case table naming the
    load-bearing reader, its shape, the other reader's shape and the fixture — titles must name
    the reader and the shape so a neuter's RED set reads correctly), and the calibration leg.
    Derive the neutered copies by string replacement of BOTH comparison lines inside
    `invokedDirectly()` (the `realpathSync(...) === fileURLToPath(import.meta.url)` line and the
    `resolve(...) === fileURLToPath(import.meta.url)` fallback) with the pre-fix idiom; write them
    with `writeFileSync` under the temp dir and reach them through symlinks. Pass an explicit
    30_000 timeout on every `it` that spawns the gate. Keep fixture constants at describe scope and
    never `expect()` a same-block const (the SRO blocking rule).

    RECORDED PROOF on the REAL readers, after the file is green (byte backups via `cp`, `shasum
    -a 256` re-taken after every edit, never `git checkout --`, `git stash` or `git clean`):
    cycle C2-N1 — replace the realpath comparison in scripts/sql-function-names-naive.mjs ONLY with
    the pre-fix idiom, run `npx vitest run src/__tests__/drift-check-scripts.test.ts -t "VAC04-C2"`,
    REQUIRED: cases 1 and 2 (naive load-bearing) RED with the blind-zero refusal in their assertion
    output, cases 3 and 4 GREEN, and the calibration leg RED by name (its realpath pin finds 0 in
    the naive source); restore, sha-verify, re-run green. Cycle C2-N2 — the same on
    scripts/sql-body-normalize.mjs ONLY: cases 3 and 4 RED, 1 and 2 GREEN, calibration RED by name;
    restore, sha-verify, green. Capture every RED verbatim for the SUMMARY. Confirm both readers'
    `--self-test` counts (13 / 9) after the restores.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>exit code is non-zero or any test is reported failed — the new C2 calibration arm, the four fixed-leg cases, the pre-fix-guard calibration leg, and every pre-existing VAC-04 / SP-C05 / VAC-08 arm must pass together on the RESTORED tree</fails_when>
    <automated>bash -c 'grep -aF "[VAC04-C2] GATE-LEVEL" src/__tests__/drift-check-scripts.test.ts >/dev/null && [ "$(grep -ac "realpathSync(process.argv\[1\])" scripts/sql-function-names-naive.mjs)" -eq 1 ] && [ "$(grep -ac "realpathSync(process.argv\[1\])" scripts/sql-body-normalize.mjs)" -eq 1 ]'</automated>
    <fails_when>exits non-zero — the gate-level C2 describe is missing from the test file (0 hits at HEAD 0564024c pre-edit), or either reader's realpath-guard line count is not exactly 1 (measured 1 per reader at HEAD 0564024c, both code lines inside `invokedDirectly()`; 0 means a real reader was left carrying the neutered guard). The two bracket tests, one per reader, make the count part of the exit status, so the command itself exits 1 on the signal</fails_when>
    <automated>git diff --quiet HEAD -- scripts/prod-body-drift-check.sh scripts/sql-function-names-naive.mjs scripts/sql-body-normalize.mjs</automated>
    <fails_when>exits non-zero — a gate or reader script carries an uncommitted change, i.e. a neuter window was not restored byte-for-byte or the scope fence was broken</fails_when>
  </verify>
  <acceptance_criteria>
    - The new describe's fixture calibration proves fixture A is naive-only-visible and fixture B is normalizer-only-visible (reader stdout asserted both ways), so each fixed-leg case has exactly one load-bearing member
    - Four fixed-leg cases cover naive{symlink, space} and normalizer{symlink, space}; each asserts exit 0 AND the readers-ran line `Functions defined or replaced by this PR: 1` AND `<name>: measured absent` AND no `MEASURE_FAIL`; fixture-A cases assert the naive-only warning text
    - The calibration leg pins `realpathSync(process.argv[1])` present in both REAL reader sources, proves the scratch copies carry 0 realpath idioms and ≥1 pre-fix idioms, and drives the gate through symlinks to them on both fixtures → exit non-zero, `MEASURE_FAIL`, both injected paths each followed by `0 name(s)`, and NOT the readers-ran line
    - Cycles C2-N1 and C2-N2 recorded with verbatim RED text, the RED set (exactly the two load-bearing cases plus the by-name calibration) and sha256 pairs; the readers' self-tests print 13 / 9 after the restores
    - Every gate-spawning `it` in the new describe carries an explicit 30_000 timeout; no same-block-const assertion subject was introduced
  </acceptance_criteria>
  <done>[VAC04-C2] is driven through the real gate: a symlinked or space-path reader reaches the gate's name count on every CI run, each union member is individually proven load-bearing, and the pre-fix guard idiom is shown — standing and recorded — to turn the same wiring into zero names and a refusal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate-level [VAC04-C4] — the real gate exits non-zero on `public.fúnc_é`, names U+00FA, prints no body text; with both refusals disabled the gate compares the WRONG subject and passes</name>
  <reversibility rating="reversible">Test-only edit to one vitest file; undo is deleting one describe block.</reversibility>
  <files>src/__tests__/drift-check-scripts.test.ts</files>
  <read_first>scripts/prod-body-drift-check.sh:204-222 (`node "$NORMALIZER" --function-names` at :207-208 is the FIRST reader call and the ONLY site the P10 input reaches — its `|| fail` message is `could not extract function names from the changed migrations.`; the naive call at :221-222 is never executed on this input); :428-431 (the qualified-name calls — unreachable on this input); :718 (the measured-absent pass line the NEUTERED wiring reaches: `Treated as a NEW function (pass).`). scripts/sql-body-normalize.mjs:362-370 (the follower-byte check on MASKED text and the `throw charsetRefusal(sql, j,` line at :369 — the neutered copy turns that throw into the pre-fix `continue`, i.e. the silent DROP) and :564-573 (`reportCharsetRefusal` — the exact diagnostic: `identifier leaves the unquoted charset [A-Za-z0-9_$]`, the `U+` codepoint, `<file>:<line>:`). scripts/sql-function-names-naive.mjs:170-171 (`const follower = line[m[0].length];` and the `if` condition the neutered copy replaces with a constant false, reproducing the truncated `f`) and :296-310 (the per-file catch in `main()` that formats the refusal and returns 1). src/__tests__/vac04-reader-guards.test.ts:162-173 (`BODY_SENTINEL` and `P10_SQL` — a header comment on line 1, the definition on line 2, the sentinel inside the body; reuse the text verbatim) and :293-309 (the reader-level non-leakage arm — the analog, not to be restated). The `[VAC04-C1]` describe's `scaffold` in drift-check-scripts.test.ts (the wiring to reuse).</read_first>
  <behavior>
    - REFUSAL ARM: the REAL gate (canonical reader paths, real-wiring PROD commands on a `some_other_fn` dump, empty snapshot dir) on a changed migration holding the P10 text → exit 1; output contains `leaves the unquoted charset`, `U+00FA`, the migration path followed by `:2:` (the 1-based line is computed, not hardcoded), the prefix actually read `public.f`, and the gate's wrapper `could not extract function names from the changed migrations`; output does NOT contain the body sentinel (calibration: the fixture text does contain it), NOT `Functions defined or replaced by this PR`, NOT `measured absent`, NOT `no unacknowledged repo-vs-PROD body drift`.
    - CALIBRATION LEG (the standing RED direction): scratch copies of BOTH readers with the refusal disabled — naive: the follower-byte `if` condition replaced by a constant false; normalizer: the `throw charsetRefusal(sql, j,` line replaced by `continue;` — each copy proven neutered by ABSENCE only (the replaced refusal text is absent from the copy; deliberately NOT a `source !== copy` check — under the recorded C4-N1 cycle the real sources are already neutered, the replacements no-op and the copies come out byte-identical, so a differs-from-source assertion would RED this leg for a reason unrelated to the gate; the leg's teeth are its gate-outcome assertions below, which RED the moment a copy still refuses); the gate driven with `NORMALIZER`/`NAIVE_NAMES` pointing at the copies on the same P10 migration → exit 0, `Functions defined or replaced by this PR: 1`, `f: measured absent`, `no unacknowledged repo-vs-PROD body drift`, and NOT `U+00FA` — the wrong-subject pass, measured verbatim at planning time.
    - STATED BOUND (comment + its OWN `it`, separate from the calibration leg): with ONLY the normalizer's refusal disabled (neutered normalizer copy + the REAL naive reader), the gate still exits 1 and names `U+00FA` under the `sql-function-names-naive:` prefix — the naive refusal reaches the verdict through :221's `|| fail` (measured: exit 1). That is why the recorded neuter disables both members, and why the naive refusal's gate-level reachability on THIS input is a stated non-coverage rather than a claim. It is its own `it` because it depends on the REAL naive reader: under the recorded C4-N1 cycle it REDs (the real naive no longer refuses, so the gate exits 0), while the calibration leg — built from copies only — stays green; folding it into the calibration leg would make that leg RED under C4-N1 and blur the RED set.
  </behavior>
  <action>
    Add a second new describe titled exactly `[VAC04-C4] GATE-LEVEL — the charset refusal driven
    THROUGH THE REAL GATE: a non-ASCII identifier must be REFUSED, never compared as a different
    function` (the registry needle for Task 3) directly after the C2 describe. Header comment: the
    measured pre-fix gate behaviour (naive truncates to `f`, normalizer drops, the gate looks up
    `f` and passes), which site the input reaches (:207 only) and the stated non-coverage for the
    naive call at :221, and why body text must never appear (public CI log; the normalizer's index
    run reads a PROD dump). Declare the P10 fixture text and the body sentinel at describe scope,
    copied verbatim from vac04-reader-guards.test.ts.

    Reuse Task 1's scaffold shape (a local copy or a hoisted helper — do not modify the C1
    describe). Implement the refusal arm and the calibration leg from the behavior block. Build
    the neutered copies by string replacement (naive :171 condition → constant false; normalizer
    :369 throw → `continue;`), assert each replacement applied by ABSENCE (the replaced refusal
    text is absent from the copy — not a `source !== copy` check, which would RED the calibration
    leg under the recorded C4-N1 cycle where the real sources are already neutered and the copies
    come out byte-identical; see the behavior block), write them under the temp dir each named
    `basename(script)` (`sql-function-names-naive.mjs` / `sql-body-normalize.mjs` — DISTINCT
    basenames, so :151's same-file refusal does not fire before either reader runs; the same rule
    Task 1 states), and point `NORMALIZER`/`NAIVE_NAMES` at them directly (no symlink needed here
    — the guard is not the subject). Add the stated bound as its OWN third `it`, never inside the
    calibration leg: with only the normalizer copy neutered and the REAL naive reader, the gate
    still exits 1 and names `U+00FA` under the `sql-function-names-naive:` prefix (this `it` is
    the one member besides the refusal arm that depends on the real readers, which is why it REDs
    under C4-N1 while the calibration leg does not). Explicit 30_000 timeouts on gate-spawning `it`s;
    no same-block-const assertion subjects.

    RECORDED PROOF on the REAL readers (byte backups, sha256 after every edit, never
    `git checkout --`): cycle C4-N1 — apply BOTH neuters to the real files (naive :171 condition →
    constant false; normalizer :369 throw → `continue;`), run
    `npx vitest run src/__tests__/drift-check-scripts.test.ts -t "VAC04-C4"`, REQUIRED RED set,
    exactly: {the refusal arm — RED with `expected 0 to be 1` and `f: measured absent` in the
    captured output; the stated-bound `it` — RED because the REAL naive reader no longer refuses,
    so the gate exits 0 and its output carries no `U+00FA`}. The calibration leg stays GREEN (its
    copies are derived by absence from an already-neutered source, so they are byte-identical to
    it and the gate still takes the wrong-subject pass — record that this is expected, not a
    defect). Any other member RED, or the calibration leg RED, is a defect in the arm, not a
    proof; restore both, sha-verify, re-run green, and
    confirm `node scripts/sql-function-names-naive.mjs --self-test` prints 13 checks and
    `node scripts/sql-body-normalize.mjs --self-test` prints 9 checks (the readers' own +1 charset
    checks prove the restore). Capture the RED verbatim for the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>exit code is non-zero or any test is reported failed — the C4 refusal arm, its calibration leg, the stated-bound assertion, Task 1's arms, and every pre-existing arm must pass together on the RESTORED tree</fails_when>
    <automated>bash -c 'grep -aF "[VAC04-C4] GATE-LEVEL" src/__tests__/drift-check-scripts.test.ts >/dev/null && node scripts/sql-function-names-naive.mjs --self-test | grep -F "self-test OK (13 checks)" && node scripts/sql-body-normalize.mjs --self-test | grep -F "self-test OK (9 checks)"'</automated>
    <fails_when>exits non-zero — the gate-level C4 describe is missing (0 hits pre-edit), or a reader's self-test does not print exactly `self-test OK (13 checks)` / `self-test OK (9 checks)` (measured at HEAD 0564024c). The pipe into `grep -F` makes each count part of the exit status, so a real reader left with its charset refusal disabled — whose own +1 charset check then fails — reddens the command itself, not only the prose</fails_when>
    <automated>git diff --quiet HEAD -- scripts/prod-body-drift-check.sh scripts/sql-function-names-naive.mjs scripts/sql-body-normalize.mjs</automated>
    <fails_when>exits non-zero — a reader or the gate script carries an uncommitted change after the C4-N1 window; the restore was not byte-for-byte</fails_when>
  </verify>
  <acceptance_criteria>
    - The refusal arm drives scripts/prod-body-drift-check.sh (not a reader CLI) on the P10 migration and asserts exit 1 plus the evidence: `leaves the unquoted charset`, `U+00FA`, `<migration path>:2:`, `public.f`, and the gate's wrapper sentence; and asserts the absence of the body sentinel (with a calibration that the fixture contains it), of the readers-ran line, of `measured absent`, and of the success notice
    - The calibration leg proves both scratch neuters applied and drives the same wiring to exit 0 with `Functions defined or replaced by this PR: 1`, `f: measured absent` and the success notice, with no `U+00FA` — the wrong-subject pass
    - The stated bound (normalizer-only neuter + the real naive reader → naive refusal still names `U+00FA` under `sql-function-names-naive:`, exit 1) is asserted in its OWN `it`, separate from the calibration leg, and its consequence — the recorded neuter disables both members — is written in the describe's comment
    - The neutered copies are named `basename(script)` (distinct basenames) and proven neutered by absence of the replaced refusal text, never by a source-vs-copy difference
    - Cycle C4-N1 recorded with the verbatim REDs, the RED set exactly {refusal arm, stated-bound `it`} with the calibration leg green, the sha256 pairs for both readers, and the post-restore self-test counts 13 / 9
  </acceptance_criteria>
  <done>[VAC04-C4] is driven through the real gate: `public.fúnc_é` cannot reach a comparison — the gate exits 1 naming the codepoint and the file with no body text — and the truncated-name pass the fix removed is reproduced, standing and recorded, the moment both refusals are disabled.</done>
</task>

<task type="auto">
  <name>Task 3: Bind the gate-level arms in INSTANCE_ARM_REGISTRY via `also:` (reader-level bindings kept), fire-proof the binding, and run the cross-cutting gates</name>
  <reversibility rating="reversible">Two registry entries gain an `also:` binding and a longer citation; undo is removing those lines.</reversibility>
  <files>src/__tests__/gate-family-meta.test.ts</files>
  <read_first>src/__tests__/gate-family-meta.test.ts:96-112 (`RegistryEntry` — `also` is a list of further `{ file, needle }` bindings, each of which must resolve), :201-227 (the VAC04-C2 and VAC04-C4 entries: reader-level `file`/`armNeedle` and their citations), :295-317 (the non-vacuity arm: `NEEDLE_MIN_LENGTH` 16, needle uniqueness across ALL bindings, the citation regexes — the plan token must match `164\.3(\.1)?-\d\d` and the record must match `CORPUS-PROOFS|SUMMARY|CALIBRATION`), :319-332 (the binds-by-name `it.each` that resolves every `also` binding with `countOf`). 164.3.1-12-CORPUS-PROOFS.md — the registry fire-proof section (the renamed-needle cycle format to reproduce).</read_first>
  <action>
    Edit ONLY the VAC04-C2 and VAC04-C4 entries of INSTANCE_ARM_REGISTRY. Keep `file` and
    `armNeedle` (the reader-level arms from plan 04) unchanged. Add `also:` with one binding each:
    `file: "src/__tests__/drift-check-scripts.test.ts"`, `needle:` = the exact describe title
    substring `[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH THE REAL GATE` and
    `[VAC04-C4] GATE-LEVEL — the charset refusal driven THROUGH THE REAL GATE` respectively
    (quoted from the file bytes written in Tasks 1-2 — read them, do not retype from memory).
    Extend each `citation` so it names this plan and its record, e.g. appending
    `+ 164.3.1-13 (gate-level arm through scripts/prod-body-drift-check.sh); proof:
    164.3.1-13-SUMMARY.md § cycles C2-N1/C2-N2` (and `§ cycle C4-N1` for C4). Add a one-line
    comment per entry stating the two levels explicitly: reader-level (plan 04, the CLIs) and
    gate-level (this plan, SC-4's "driven through the real gate") — the reader-vs-gate distinction
    must be visible in the entry, not only in a citation. Do not touch PHASE_TAXONOMY, the
    meta-arms, their allowlists, or KNOWN_THRESHOLD_SITES.

    FIRE-PROOF (byte backup of gate-family-meta.test.ts, sha256 re-taken after every edit, never
    `git checkout --`): alter one character inside the new VAC04-C2 `also` needle in the REGISTRY
    (not in the target file), run `npx vitest run src/__tests__/gate-family-meta.test.ts`,
    REQUIRED: the `VAC04-C2 (primitive C) → … binds by name` case REDs with the message naming
    VAC04-C2, the altered needle and src/__tests__/drift-check-scripts.test.ts, while every other
    registry case stays green; restore, sha-verify, re-run green. Capture verbatim for the SUMMARY.

    Then run the cross-cutting gates listed in `<verification>` (contracts, the SRO blocking scan,
    vac04-reader-guards, both changed files under Node 22, `tsc --noEmit`, eslint on the two
    changed files) and record each result in the SUMMARY. Write the SUMMARY with the four
    recorded cycles (C2-N1, C2-N2, C4-N1, registry fire-proof): what was neutered, the verbatim
    RED, the RED set, the sha256 pair, and the restore confirmation (hash AND green re-run AND the
    readers' self-test counts).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/gate-family-meta.test.ts</automated>
    <fails_when>exit code is non-zero or any test fails — a new needle does not resolve in drift-check-scripts.test.ts, is shorter than 16 chars or duplicates another needle, a citation lacks the plan token or the record token, or the exact set / primitive letters moved</fails_when>
    <automated>bash -c 'grep -aF "[VAC04-C2] GATE-LEVEL" src/__tests__/gate-family-meta.test.ts >/dev/null && grep -aF "[VAC04-C4] GATE-LEVEL" src/__tests__/gate-family-meta.test.ts >/dev/null && [ "$(grep -ac "also: \[" src/__tests__/gate-family-meta.test.ts)" -ge 6 ]'</automated>
    <fails_when>exits non-zero — one of the two gate-level needles is absent from the registry, or fewer than 6 lines carry `also: [` (measured 4 at HEAD 0564024c pre-edit, plus the 2 added here). The bracket test makes the floor part of the exit status, so the command itself exits 1 on the signal</fails_when>
    <automated>npx vitest run src/__tests__/contracts src/__tests__/self-referential-oracle.test.ts src/__tests__/vac04-reader-guards.test.ts</automated>
    <fails_when>exit code is non-zero — a contract scan reddened on the new bytes (file-scoped runs cannot clear src/__tests__/contracts, so this run is required), the SRO blocking scan found a new self-referential oracle (its summary must still report exactly the 2 allowlisted findings), or a reader-level arm broke after the neuter windows</fails_when>
    <automated>git diff --quiet HEAD -- scripts/prod-body-drift-check.sh scripts/sql-function-names-naive.mjs scripts/sql-body-normalize.mjs .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-04-PLAN.md .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-07-PLAN.md .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-12-PLAN.md</automated>
    <fails_when>exits non-zero — a gate script, a reader, or a plan 01-12 artifact carries an uncommitted change; the scope fence ("test + registry edits only") was broken</fails_when>
  </verify>
  <acceptance_criteria>
    - VAC04-C2 and VAC04-C4 entries keep their reader-level `file`/`armNeedle` and each gains exactly one `also:` binding to the gate-level describe title in src/__tests__/drift-check-scripts.test.ts, with a citation naming 164.3.1-13 and 164.3.1-13-SUMMARY.md and a comment naming both levels
    - The registry fire-proof is recorded: an altered gate-level needle REDs the VAC04-C2 binds-by-name case naming the instance and the file; restore sha-verified, green
    - The SUMMARY records cycles C2-N1, C2-N2, C4-N1 and the fire-proof with verbatim REDs, RED sets and sha256 pairs; and records the cross-cutting gate results (contracts, SRO summary unchanged at 2 findings, vac04-reader-guards 15 passed, Node 22 pass on both changed files, tsc clean, eslint clean)
    - SC-4 is stated in the SUMMARY as fully driven through the real gate for all four members, with the migration-PR ordering constraint (hold until 164.3.1 and 164.4 both land) restated unchanged
  </acceptance_criteria>
  <done>The corpus's anti-decay gate binds C2 and C4 to both their reader-level and gate-level arms by name, proven to red on a renamed needle, and the whole family plus the contracts scans are green on the restored tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| injected reader paths → gate verdict | `NORMALIZER`/`NAIVE_NAMES` decide what the gate reads on the PR side; a reader that silently no-ops through an unusual path is a green gate that read nothing |
| PR-authored migration → reader name extraction → PROD lookup | an identifier the readers narrow makes the gate fetch and compare a DIFFERENT function |
| test fixtures → public CI log | the gate and its readers run in a public log; body text must never be printed |
| registry needles → corpus completeness | a gate-level arm bound by a needle that does not resolve is a binding to nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-13-01 | Tampering | a gate-level C2 arm that passes for the wrong reason — the union masking one reader's silent zero on a both-visible definition | high | mitigate | per-reader-visible fixtures A/B with a calibration arm proving visibility, four cases naming the load-bearing reader, and single-member neuter cycles C2-N1/C2-N2 whose RED sets are exactly the two load-bearing cases (Task 1) |
| T-164.3.1-13-02 | Tampering | a C2 arm that proves C1's tripwire instead of the guard fix (pass condition = refusal) | high | mitigate | pass condition is the readers-ran line; the refusal is asserted only in the calibration leg as the NEUTERED outcome (Task 1, D-13 note) |
| T-164.3.1-13-03 | Tampering | a C4 neuter that cannot RED because the other member's refusal still reaches the verdict | medium | mitigate | the stated-bound assertion (normalizer-only neuter → naive refusal names U+00FA) and the recorded cycle disabling BOTH members, observed to exit 0 on the wrong subject (Task 2) |
| T-164.3.1-13-04 | Information Disclosure | the P10 fixture's function body echoed into the gate output / CI log | medium | mitigate | body-sentinel non-leakage assertion at gate level, calibrated against the fixture text (Task 2) |
| T-164.3.1-13-05 | Repudiation | registry `also:` needle typed from memory and never resolving, or a citation to no record | medium | mitigate | needles quoted from the written file bytes; the binds-by-name it.each and the citation regexes; the renamed-needle fire-proof (Task 3) |
| T-164.3.1-13-06 | Tampering | a neuter window on the real readers left unrestored, shipping a truncating reader | high | mitigate | byte backups + sha256 after every edit, `git diff --quiet` verifies on every task, the readers' own self-test counts (13 / 9) re-asserted after each restore |
| T-164.3.1-13-SC | Tampering (supply chain) | npm/pip/cargo installs | low | accept | zero packages installed; the arms use node: builtins and the already-installed vitest |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 13)

| Artifact | Kind | Location | Consumers |
|---|---|---|---|
| `[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH THE REAL GATE: …` describe: fixture calibration, 4 fixed-leg cases (naive/normalizer × symlink/space), pre-fix-guard calibration leg | vitest arms (driven through scripts/prod-body-drift-check.sh) | `src/__tests__/drift-check-scripts.test.ts` | CI vitest shards / frontend aggregator; the registry; the SC-4 verifier |
| `[VAC04-C4] GATE-LEVEL — the charset refusal driven THROUGH THE REAL GATE: …` describe: refusal arm, disabled-refusal calibration leg, stated-bound `it` (its own case) | vitest arms (driven through the real gate) | `src/__tests__/drift-check-scripts.test.ts` | same |
| `also:` gate-level bindings + extended citations on the VAC04-C2 / VAC04-C4 registry entries | registry edit | `src/__tests__/gate-family-meta.test.ts` | the registry's binds-by-name and non-vacuity checks; future refactors of the family |
| Recorded cycles C2-N1, C2-N2, C4-N1, registry fire-proof (verbatim RED, RED set, sha256 pairs) | committed proof record | `164.3.1-13-SUMMARY.md` | the phase verifier (SC-1 recursion, SC-4); the registry citations |

No new files beyond the SUMMARY. No fixture files under `scripts/` — every `.sql` fixture is written into the test's mkdtemp directory at run time.
</artifacts_produced>

<verification>
1. `npx vitest run src/__tests__/drift-check-scripts.test.ts` — green on the restored tree (280 arms at HEAD plus the 9 or so added here). Fails when: non-zero exit.
2. `npx vitest run src/__tests__/gate-family-meta.test.ts` — green: both new needles resolve, exact set unchanged (15 instances), citations pass both regexes. Fails when: non-zero exit.
3. `npx vitest run src/__tests__/contracts src/__tests__/self-referential-oracle.test.ts src/__tests__/vac04-reader-guards.test.ts` — green; the SRO blocking summary still reports exactly 2 findings. Fails when: non-zero exit or a third finding.
4. Node 22 pass on the two changed files: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts` after confirming `node --version` on that PATH reads v22 (CI runs Node 22; local is 25; the arms use only `spawnSync`, `symlinkSync`, `cpSync`, `mkdtempSync` — no Node-25-only API). Fails when: non-zero exit, or the v22 binary is absent (say so in the SUMMARY rather than skipping).
5. `npx tsc --noEmit` clean and `npx eslint src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts` clean. Fails when: non-zero exit.
6. `node scripts/sql-function-names-naive.mjs --self-test` → `13 checks`; `node scripts/sql-body-normalize.mjs --self-test` → `9 checks`; `git diff --quiet HEAD -- scripts/` exits 0 — every neuter window restored. Fails when: other counts or a non-empty diff.
7. Fence: this plan's commits touch only the two declared test files plus `.planning/` — `git show --stat` proves it; no plan 01-12 file, no gate script, no reader, no `.github/workflows/**` change.
8. ⚠️ Full-suite note for the executor: file-scoped runs are the per-task gate; run the full vitest suite ALONE on the box (never concurrently with pytest/mypy — contention fakes regressions) before hand-off if the orchestrator's protocol calls for it.
</verification>

<success_criteria>
- SC-4 fully discharged by mechanism: [VAC04-C2] and [VAC04-C4] are each driven end-to-end through
  scripts/prod-body-drift-check.sh on their measured inputs — C2 reaching the readers-ran line under
  both invocation shapes for both union members, C4 exiting non-zero with the codepoint named and
  no body text — joining the standing C1/C3 arms.
- Every new arm is proven able to fail on every run (standing scratch-copy calibration legs) and
  in the record (real-reader neuter cycles with RED-set specificity and sha256 pairs).
- INSTANCE_ARM_REGISTRY binds both instances at both levels by name and reds on a renamed needle.
- Scope fence intact: test + registry edits only; gate and readers byte-identical to HEAD; plans
  01-12 untouched; no database contacted.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-13-SUMMARY.md`
when done. It must: name the two new describe titles and every `it` inside them; quote the
planning-time measurements it reproduced (readers-ran line under symlink/space; `0 name(s)` twice
then MEASURE_FAIL under the pre-fix guard; `U+00FA` with no body text; `f: measured absent` exit 0
under the disabled refusals); record cycles C2-N1, C2-N2, C4-N1 and the registry fire-proof with
verbatim RED text, the RED set, sha256 pairs and the post-restore self-test counts; record the
cross-cutting gate results (contracts, SRO 2 findings, reader-guards 15 passed, Node 22, tsc,
eslint); state SC-4 as MET by mechanism for all four members; and restate the migration-PR ordering
constraint (hold until 164.3.1 and 164.4 have both landed) unchanged.
</output>
