# 164.3.1-12 — Recursive SC-1 proofs: primitives C + D, and the registry's own fire-proof

**Requirement:** SC-1 — *every measured instance across the primitives is an executable arm in the
regression corpus, **each proven to RED by neutering its fix** and observing the failure.* CONTEXT
§ Specifics makes it recursive: *a corpus entry that cannot fail is itself a Primitive-D instance.*

This file is the record for the arms plan 12 owns: the two VAC-08 instances (§2–§4) and the
instance→arm registry (§5). The C/D arms plans 04/06/07/08 shipped carry their own proofs in their
SUMMARY evidence blocks; the registry BINDS them (§5) and does not re-prove them.

Discipline, identical to `164.3.1-11-CORPUS-PROOFS.md`: byte backup in the session scratchpad →
neuter applied by an exact-once replacement (`neuter.mjs`, refuses on any other count) → the arm
run under vitest → verbatim failure captured → restore by copying the backup back → `shasum -a 256`
equality **and** a grep for the newest edit **and** zero `NEUTER(` markers → green re-run. Never
`git checkout --`, never `git stash`. No database was contacted at any point: every VAC-08 arm
injects `LEDGER_QUERY_CMD` / `BODY_FETCH_CMD`, and no `supabase` CLI command was run.

---

## 1. Provenance

| Field | Value |
|---|---|
| Tree at proof time | `e59f231e` (plan 11's docs commit) + the uncommitted Task-1 edit to `src/__tests__/drift-check-scripts.test.ts` |
| Date | 2026-09-02 |
| Host | macOS arm64, Node v25.8.1 (Node 22 v22.22.1 parity re-run recorded in the SUMMARY) |
| `scripts/test-ledger-drift-check.sh` sha256 — GREEN, identical before and after **every** cycle | `c9857646ffcc56d00b70048efd9ced7ecd67cd0ff6f9aa9adbe7b5dc8774179a` |
| Precondition (`bash scripts/test-ledger-drift-check.sh --self-test`) | exit 0, `self-test OK (5/5 arms)` — before the first neuter window and after the last restore |
| Baseline `vitest run src/__tests__/drift-check-scripts.test.ts` before Task 1 | 277 passed |

The gate script is edited by **no plan** in this phase. Every window below opens on the byte
backup's hash and closes on the same hash.

---

## 2. Cycle 1 — VAC08-JOIN: a load-bearing convention clause removed

**Entry under proof:** `VAC08-JOIN: a ledger row matching under EACH convention is not reported
missing; one matching under NONE still is, by name` (new in this plan, `drift-check-scripts.test.ts`,
describe *VAC08-JOIN — the join key is the union of every ledger naming convention*).

**Neuter (the minimal named one):** delete clause 3 — the bare-timestamp convention, the one the
shape diagnostic found on CI run 33276251646 after the first two cut false-missing from 253 to 56 —
from the `missing` direction only. Applied by exact-once replacement of the four-line predicate
head anchored on `schema_migrations m`:

```diff
                    SELECT 1 FROM supabase_migrations.schema_migrations m
                     WHERE m.name = r.fname
                        OR (m.version || '_' || m.name) = r.fname
-                       OR m.name = split_part(r.fname, '_', 1)
                        OR m.name = substr(r.fname, strpos(r.fname, '_') + 1));" \
```

`git diff --stat` on the script: 1 file, 0 insertions, 1 deletion. sha256 under the neuter:
`24f90ca8eb52267059610c2cae91f870427d6fccbbacabb3e6280e23236d0765`.

### 2a. The first draft did NOT red — a vacuity caught by this cycle, recorded rather than hidden

The first version of the arm built its fixture rows **from the clause list it had just read out
of the script**. Under the neuter it read three clauses, built three rows, drove the gate, and
the gate — correctly — found all three. Only the sibling symmetry arm went red:

```
 ❯ |jsdom| src/__tests__/drift-check-scripts.test.ts (279 tests | 1 failed | 277 skipped)
       × the ADVISORY extra direction uses the SAME clause set as the missing direction — one predicate, two readings (:62-67)
AssertionError: the two directions disagree BY CONSTRUCTION again — … expected [ 'm.name = r.fname', …(3) ] to deeply equal [ 'm.name = r.fname', …(2) ]
      Tests  1 failed | 1 passed | 277 skipped (279)
```

That is exactly the Primitive-D shape this phase exists to close — an arm whose fixture follows
the artifact it is checking cannot observe that artifact losing a clause. The arm was rewritten so
that **rows come from the fixed convention table and only the predicate comes from the script**,
and the cycle was re-run. The corrected arm carries this measurement in its comment.

### 2b. RED, verbatim (corrected arm, same neuter, sha `24f90ca8…`)

```
 FAIL  |jsdom| src/__tests__/drift-check-scripts.test.ts > VAC-08 — scripts/test-ledger-drift-check.sh > VAC08-JOIN — the join key is the union of every ledger naming convention > VAC08-JOIN: a ledger row matching under EACH convention is not reported missing; one matching under NONE still is, by name
AssertionError: the gate reported a migration MISSING although a ledger row matches it under one of its own conventions — a clause was removed and the false-253 path is open again:
Repo migrations: 4
::error::VAC-08 repo-vs-TEST ledger and body drift gate: 1 repo migration(s) are not present in the TEST ledger and are NOT baselined:
::error::  20260103000000_join_bare_ts
::error::--- TEST ledger shape (diagnostic, does not decide pass/fail) ---
::error::  rows_total=stub
::error::If name_null is large, the join key is wrong for older rows and the
::error::fix belongs in the gate, NOT in a hand-apply to TEST. Do not apply
::error::migrations to TEST to make this gate green — TEST is shared.
  advisory: 1 ledger row(s) have no repo file (squashes / CLI-era rows). Not a failure.
  demo_fn/1: TEST body matches the committed body (56f691a0fe5ea06d2cf0e9af260bbc6db7e9c16dc6dfcd547353f07d09349468)
VAC-08 repo-vs-TEST ledger and body drift gate: 4 migration(s) checked against the ledger, 1 body comparison(s) over 1 named function(s).
: expected 1 to be +0 // Object.is equality

 FAIL  … > the ADVISORY extra direction uses the SAME clause set as the missing direction — one predicate, two readings (:62-67)
AssertionError: the two directions disagree BY CONSTRUCTION again — before 2026-08-30 the advisory direction joined on `name` alone and reported 224 phantom extras while the missing direction used all the clauses: expected [ 'm.name = r.fname', …(3) ] to deeply equal [ 'm.name = r.fname', …(2) ]
      Tests  2 failed | 277 skipped (279)
```

The gate named **exactly the row that only the deleted clause could match**
(`20260103000000_join_bare_ts`), and the symmetry arm reddened beside it because the `extra`
direction still carried four clauses. RED set: both VAC08-JOIN arms; nothing else in the file was
run under this neuter (filtered `-t "VAC08-JOIN"`).

### 2c. Restore

| Point | sha256 of `scripts/test-ledger-drift-check.sh` |
|---|---|
| byte backup, pre-neuter | `c9857646ffcc56d00b70048efd9ced7ecd67cd0ff6f9aa9adbe7b5dc8774179a` |
| after `cp` from the backup | `c9857646ffcc56d00b70048efd9ced7ecd67cd0ff6f9aa9adbe7b5dc8774179a` |

Newest-edit grep: `split_part(r.fname, '_', 1)` occurs **2** times (missing + extra) —
restored, not merely hashed. `git status --porcelain -- scripts/test-ledger-drift-check.sh` empty.
Green re-run, filtered: `Tests 2 passed | 277 skipped (279)`.

---

## 3. Cycle 1b — a MEASUREMENT, not a required RED: clause 2 is subsumed by clause 4

While building the fixture rows it became visible that the `version_name` row the ledger really
stores (`version=<ts> name=<desc>`, the "old rows" shape at script :39-40) **also satisfies the
desc-only clause** (`m.name = substr(r.fname, strpos(r.fname,'_')+1)` — `m.name` *is* the
description). So for every underscore-free `version`, clause 2 implies clause 4, and removing
clause 2 cannot change the gate's verdict on any honest row. The script header's line :44-47
("BOTH clauses are required; neither is redundant, and dropping either reproduces a known-false
verdict") was written when only two clauses existed and is stale for clause 2 now that four do.

Rather than assert a runtime consequence that does not exist, the arm **pins the measured
subsumption structure** (`EXPECTED_SPECIFICITY`) so it cannot rot silently, and this cycle
measures it.

**Neuter:** delete clause 2 (`OR (m.version || '_' || m.name) = r.fname`) from the `missing`
direction only. sha256 under the neuter: `e50e1d50eeb5096094bbc57c3e0a292091f48e329653d5e75f139ef131d75db3`.

**Observed, verbatim** (filtered to the join arm alone):

```
 FAIL  … > VAC08-JOIN: a ledger row matching under EACH convention is not reported missing; one matching under NONE still is, by name
AssertionError: the clause-subsumption structure changed. If a clause became load-bearing (or stopped being), update EXPECTED_SPECIFICITY WITH the measurement — do not loosen this to `toBeTruthy`: expected { 'name-only': [ 'name-only' ], …(3) } to deeply equal { 'name-only': [ 'name-only' ], …(3) }
- Expected
+ Received
@@ -8,8 +8,7 @@
    "version_name": [
      "desc-only",
-     "version_name",
    ],
      Tests  1 failed | 278 skipped (279)
```

Two things this shows, both as predicted: (1) **both driven runs passed** — the gate did not name
`20260102000000_join_version_name`, because the desc-only clause still matched its row: the
subsumption is real at runtime, not reasoned; (2) the arm still reds on the removal, through the
**structural pin** rather than the verdict. So even the subsumed clause cannot be dropped
silently — but the record says truthfully *why* it reds. Reported in the SUMMARY as a follow-up
for the script's header comment (the script is not edited in this phase).

**Restore:** `cp` from backup → sha256 `c9857646…` (identical); newest-edit grep
`(m.version || '_' || m.name) = r.fname` occurs **2** times.

---

## 4. Cycle 2 — VAC08-253: the absurdity floor disabled

**Entry under proof:** the standing 253-shape family, confirmed present by name before the window:
`RED: a populated ledger matching under half the repo is MEASURE_FAIL, not drift` + its two
controls `CONTROL: the same populated ledger with everything matched stays silent` and
`CONTROL: a SMALL ledger with nothing matched is ordinary drift, not MEASURE_FAIL` (describe
*distinguishes a broken instrument from a drift finding*).

**Neuter:** the floor's comparison replaced by a constant, exact-once:

```diff
-  if [ -n "$ledger_rows" ] && [ "$ledger_rows" -ge 50 ] && [ $(( matched * 2 )) -lt "${#repo_names[@]}" ]; then
+  if false; then # NEUTER(164.3.1-12 task 1 cycle 2): absurdity-floor comparison disabled
```

sha256 under the neuter: `b147a809d9916d36ae3c8c0b160edefec3dd168968a1b459ff05be48bd7a5555`.

**RED, verbatim:**

```
 FAIL  |jsdom| src/__tests__/drift-check-scripts.test.ts > VAC-08 — scripts/test-ledger-drift-check.sh > distinguishes a broken instrument from a drift finding > RED: a populated ledger matching under half the repo is MEASURE_FAIL, not drift
AssertionError: expected 'Repo migrations: 1\n::error::VAC-08 r…' to contain 'MEASURE_FAIL'
- Expected
+ Received
- MEASURE_FAIL
+ Repo migrations: 1
+ ::error::VAC-08 repo-vs-TEST ledger and body drift gate: 1 repo migration(s) are not present in the TEST ledger and are NOT baselined:
+ ::error::  20260829120000_demo
+ ::error::--- TEST ledger shape (diagnostic, does not decide pass/fail) ---
+ ::error::  rows_total=stub
+ ::error::If name_null is large, the join key is wrong for older rows and the
+ ::error::fix belongs in the gate, NOT in a hand-apply to TEST. Do not apply
+ ::error::migrations to TEST to make this gate green — TEST is shared.
      Tests  1 failed | 2 passed | 276 skipped (279)
```

With the floor gone, a 239-row ledger matching 0 of 1 repo migrations is reported as **drift** —
the 253-of-262 sentence, the one that points a reader at hand-applying migrations to a shared
database — instead of `MEASURE_FAIL`. The two controls stayed **green in the same run**
(`2 passed`): the neuter hit the floor, not the harness. RED set: exactly the RED arm.

**Restore:**

| Point | sha256 |
|---|---|
| byte backup, pre-neuter | `c9857646ffcc56d00b70048efd9ced7ecd67cd0ff6f9aa9adbe7b5dc8774179a` |
| after `cp` from the backup | `c9857646ffcc56d00b70048efd9ced7ecd67cd0ff6f9aa9adbe7b5dc8774179a` |

`NEUTER(` markers: **0**; the floor line `matched * 2 )) -lt` present: **1**; porcelain empty.
`bash scripts/test-ledger-drift-check.sh --self-test` → `self-test OK (5/5 arms)`, exit 0.
Full file: `Tests 279 passed (279)` on Node 25 **and** on Node 22.

---

## 5. Registry fire-proof — the instance→arm registry can fail, in both directions

**Subject:** `src/__tests__/gate-family-meta.test.ts` (Task 2). GREEN before the window:
`Tests 17 passed (17)`. Byte backup sha256:
`d7a9d23930d540b09a64b64b11d6d8c5d2351c1efdb5cbfe4f6122baeaebac7b`. Neuters are applied to the
**registry**, never to a target file — the point is that the registry notices a binding that no
longer resolves, not that a target arm was harmed.

### 5a. Direction 1 — a needle renamed inside the registry (the "arm renamed/deleted" shape)

Exact-once replacement of AUDCOV-01's `armNeedle`, appending `-RENAMED-FOR-FIRE-PROOF` to the
quoted title. sha256 under the neuter: `a3f5e4945189d0e7d6e0c5aaeba9839a55c93c76efc6b7ccf019639d580d75ea`.

```
     × 'AUDCOV-01' (primitive 'D') → 'src/__tests__/audit-coverage.test.ts' binds by name 4ms
AssertionError: AUDCOV-01: its arm "case A ([AUDCOV-01]): a `/*` inside a MULTI-LINE template no longer blanks the FILE-RENAMED-FOR-FIRE-PROOF" is no longer in src/__tests__/audit-coverage.test.ts. The arm was renamed or deleted. If renamed, update the needle HERE with the new title; if deleted, the corpus lost a measured instance — restore it (see 164.3.1-06; proof: 164.3.1-06-SUMMARY.md § Case A's pre-fix RED and § Neuter/restore proof). Do NOT drop the registry entry.: expected 0 to be greater than or equal to 1
      Tests  1 failed | 16 passed (17)
```

RED **by instance name**, with the citation carried into the message. The other 14 bindings and
the exact-set arm stayed green in the same run — the neuter hit one binding, not the reader.

Restore: `cp` from backup → sha256 `d7a9d239…` (identical); `FIRE-PROOF` markers: **0**.

### 5b. Direction 2 — a registry entry dropped (the "instance forgotten" shape)

The whole `MUT-W02` entry removed by a node splice (refused unless the entry was found once).
sha256 under the neuter: `e9b61e651584f8045bdc755e1ab985ca01f3282b70dda99849fe6345c710d0f5`.

```
     × EXACT SET, both directions: registry instances === phase taxonomy, failing by name 7ms
AssertionError: measured instance(s) with NO arm in the corpus: MUT-W02. SC-1 says every measured instance is an executable arm — ship the arm, prove it RED under the neuter of its fix, then register it here with its citation. Do NOT delete the taxonomy entry.: expected [ 'MUT-W02' ] to deeply equal []
      Tests  1 failed | 15 passed (16)
```

RED by instance name through the taxonomy → registry direction. (The registry → taxonomy
direction is the same `toEqual([])` shape one assertion later, with its own do-not-silence text.)

Restore: `cp` from backup → sha256 `d7a9d239…` (identical); `instance: "MUT-W02"` occurs **2**
times (taxonomy + registry); `git status --porcelain` shows the file only as the untracked new
file it is. Green re-run: `Tests 17 passed (17)`.

### 5c. The meta-arms' predicate is load-bearing (Task 3)

Backup sha256 of `gate-family-meta.test.ts` at `5aa5038b`'s content:
`ce09a09a47c5c9318d7d5d03b15299fa408d86f7dc56542c4468d54e120e9754`. Neuter: `carriesRuntimeValue`
made to `return true` (every emission reads as carrying a quantity). sha256 under the neuter:
`7dd99b3c5cd8512f7c31a3b0a70efe1619c75476055ef0f81838a446d942ae76`.

```
META diagnostic-first: 68 emission(s) over 2 gate(s), 0 bare, 17 allowlisted
     × diagnostic-first: the RED fixture's bare-conclusion block AND bare fail are flagged; the GREEN fixture is clean and really read
     × every failure emission prints a runtime value — exact against DIAGNOSTIC_FIRST_ALLOWLIST, both directions
AssertionError: the rule must flag the fixture's bare ::error:: block and its bare fail call; findings were []: expected [] to deeply equal [ 'error-block', 'fail-call' ]
AssertionError: allowlist entr(y/ies) no longer match a bare emission — either the site now prints its quantity (delete the entry) or the walker stopped seeing it (a regression in this rule; re-run the fixture arm before touching the list): expected [ …(17) ] to deeply equal []
      Tests  2 failed | 19 passed (21)
```

Two independent detectors of a blinded rule, both firing: the red fixture (the SP-L02 half) and
the exact-set allowlist's discharge direction (all 17 entries "no longer flagged" — the same
second tripwire plan 08 observed on the SRO rule). The bare-measurement arm and the registry stayed
green in the same run. The TDD RED before implementation (commit `03585b88`) showed the same two
fixture assertions failing against stub predicates: `findings were []` / `sites were []`.

Restore: `cp` from backup → sha256 `ce09a09a…` (identical); `NEUTER(` markers **0**; `Tests 21
passed (21)`.

---

## 6. What this record does NOT claim

- It does not re-prove the arms plans 04/06/07/08/11 shipped. The registry binds them by name;
  their proofs are in the records each entry cites.
- The VAC08-JOIN arm proves the gate's verdict follows the script's clause list as READ by a JS
  interpreter of four known clause shapes. It does not execute the SQL; a clause the interpreter
  does not know fails the arm by text rather than being evaluated (§2, header of the describe).
- Cycle 1b is a measurement of subsumption, not a required RED of the gate's verdict. The arm
  still reds on the removal, through its structural pin, and the record says so (§3).
- Nothing here was run against a database. Both VAC-08 windows drove injected commands over
  scratch fixtures; the gate script's real `psql` path was never invoked.
