---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
verified: 2026-09-02T09:12:33Z
verifier: gsd-verifier (re-verification at HEAD 092003fb, branch phase-164.3.1-sound-primitives; previous report at a30ce097)
status: human_needed
score: 9/9 success criteria verified
behavior_unverified: 0
overrides_applied: 0

re_verification:
  previous_status: gaps_found
  previous_score: 8/9
  previous_head: a30ce097
  gaps_closed:
    - "SC-4 — [VAC04-C2] and [VAC04-C4] are now driven END-TO-END THROUGH THE REAL GATE scripts/prod-body-drift-check.sh (9 new arms in src/__tests__/drift-check-scripts.test.ts, bound at gate level in INSTANCE_ARM_REGISTRY via `also:`)"
  gaps_remaining: []
  regressions: []

# ─────────────────────────────────────────────────────────────────────────────
# What I RAN MYSELF at HEAD 092003fb — every line is a command this verifier
# executed in this checkout, serialized, never a supabase CLI command, no
# database contacted, the tree never mutated (git status --porcelain on
# scripts/ and src/ empty before and after). SUMMARY claims are cross-checked
# BELOW, not trusted.
# ─────────────────────────────────────────────────────────────────────────────
independent_measurements:
  - command: "git diff --stat a30ce097..HEAD ; git diff --name-only a30ce097..HEAD | grep -v '^.planning/'"
    result: "Only src/__tests__/drift-check-scripts.test.ts (+482/−2) and src/__tests__/gate-family-meta.test.ts (+22/−2) changed outside .planning/ since the previous verification"
    proves: "Every previously-VERIFIED artifact (gate script, both readers, run.mjs, parse.mjs, pg-lane, fixtures, the other 9 test files, ci.yml) is byte-identical to what a30ce097 verified — regression scope is exactly the two test files."
  - command: "git cat-file -t 6c23ae13 b8d79162 dc47efd4 092003fb ; git show --stat <each>"
    result: "all four commits exist; the three task commits touch ONLY the two declared test files (275+1 / 206 / 22+2 lines)"
    proves: "Plan 13's scope fence (test + registry edits only; no gate, reader, workflow or plan 01-12 file) is a git fact."
  - command: "node_modules/.bin/vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts  (Node 25.8.1, then again with --reporter=verbose, then again under /opt/homebrew/opt/node@22/bin v22.22.1)"
    result: "exit 0 — 2 files / 310 passed (289 + 21) on Node 25 AND on Node 22; META diagnostic-first: 68 emission(s) over 2 gate(s), 17 bare, 17 allowlisted; META bare-measurement: 6 threshold site(s) over 5 file(s). Verbose listing shows all 9 new arms by name under the two GATE-LEVEL describes (C2: CALIBRATION, FIXED LEG cases 1-4, CALIBRATION LEG; C4: REFUSAL, CALIBRATION LEG, STATED BOUND), 150-830 ms each"
    proves: "The new gate-level arms execute and pass on the CI runtime, not only locally; the registry's exact set, needle resolution and both meta-arms are green with the new `also:` bindings."
  - command: "grep -aF -c '[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH THE REAL GATE' src/__tests__/drift-check-scripts.test.ts ; same for the C4 needle ; grep -ac 'also: \\[' src/__tests__/gate-family-meta.test.ts"
    result: "1 ; 1 ; 6 (was 4)"
    proves: "Both registry needles resolve exactly once in the target file's bytes; the two `also:` bindings were added and nothing else was removed."
  - command: "shasum -a 256 scripts/sql-function-names-naive.mjs scripts/sql-body-normalize.mjs"
    result: "c844ed06aff760611fffd7c96b8c49c6f15e7ca0fca6744a4e4d01a6773abe03 ; 226969cdc60cf8bfdda4dd29b49328617b2de75f70795fac44184bb8d269f87e — identical to the SUMMARY's recorded pre-cycle and post-restore hashes"
    proves: "The recorded neuter cycles C2-N1 / C2-N2 / C4-N1 started from and returned to exactly the bytes at HEAD; no neutered reader shipped."
  - command: "grep -an of the neuter targets: naive.mjs `if (follower !== undefined && !/\\s/.test(follower) && follower !== \"(\") {` ; normalize.mjs `throw charsetRefusal(sql, j,` ; `realpathSync(process.argv[1])` count per reader ; the `resolve(process.argv[1]) === fileURLToPath` fallback"
    result: "naive.mjs:171 ; normalize.mjs:369 ; 1 and 1 ; naive:342 / normalize:769"
    proves: "The lines the SUMMARY says were neutered exist at the recorded line numbers, and the string-replacement targets the standing calibration legs use (`FIXED_RETURN_REALPATH`, `FIXED_RETURN_FALLBACK`, `NAIVE_REFUSAL_CONDITION`, the `throw charsetRefusal(sql, j,` regex) match the real bytes — so the scratch-copy neuters cannot silently no-op."
  - command: "node scripts/sql-function-names-naive.mjs --self-test ; node scripts/sql-body-normalize.mjs --self-test"
    result: "self-test OK (13 checks) ; self-test OK (9 checks)"
    proves: "Both readers carry their +1 charset check — the restore after C4-N1 is real."
  - command: "git diff --quiet HEAD -- scripts/ .github/ ; git status --porcelain -- scripts/ src/"
    result: "clean ; empty (only .planning/milestone.lock is dirty — orchestrator bookkeeping)"
    proves: "No neuter window was left open; nothing outside the two test files is uncommitted."
  - command: "SCRATCH reproduction of the SPACE shape outside the tree: copy naive.mjs into '<scratchpad>/vac04 space check/fixed.mjs'; derive prefix.mjs by replacing both guard lines with the pre-fix `import.meta.url === \\`file://${process.argv[1]}\\`` idiom (0 realpath idioms after); run each on fixture A"
    result: "fixed via space path -> `sanitize_user$v2` exit 0 ; pre-fix via space path -> exit 0, stdout EMPTY ; pre-fix via a plain no-space path (control) -> `sanitize_user$v2` exit 0"
    proves: "The standing C2 calibration leg drives only the SYMLINK shape (see W6); the SPACE shape's RED direction rests on the recorded cycles (cases 1 and 4). I reproduced that direction independently: the pre-fix guard is blind to a space path and sighted on a plain path, exactly the [VAC04-C2] finding, so the recorded `expected 1 to be +0` REDs for cases 1/2 (C2-N1) and 3/4 (C2-N2) are consistent with the code."
  - command: "node_modules/.bin/vitest run src/__tests__/contracts src/__tests__/self-referential-oracle.test.ts src/__tests__/vac04-reader-guards.test.ts"
    result: "exit 0 — 7 files / 130 passed; SELF-REF-ORACLE blocking summary: 2 finding(s) in 1 file(s), 131 file(s) scanned (the 2 allowlisted types-design-tests sites, unchanged)"
    proves: "The contract scans (which read all of src/ and could redden on the new bytes) and the Primitive-D blocking rule are green over the 482 new test lines; no new same-block-const oracle was introduced; the reader-level [VAC04-C2]/[VAC04-C4] arms (15) still pass."
  - command: "node_modules/.bin/eslint <both files> ; node_modules/.bin/tsc --noEmit"
    result: "exit 0 ; exit 0 (0 lines)"
    proves: "Lint and type gates clean on the new arms."
  - command: "node scripts/lint-sql-gates.mjs --self-test ; bash scripts/test-ledger-drift-check.sh --self-test ; node scripts/verify-plan-anchors.mjs --pending"
    result: "4 rules, red+green each ; self-test OK (5/5 arms) ; OK: 2 plan file(s), no stale claims"
    proves: "Sibling gates undisturbed; no pending plan anchors went stale."
  - command: "regression markers: parse.mjs exports ; run.mjs floors ; nonce helpers ; gate markers ; pg-lane verbose ; fixture dirs"
    result: "tokenizeStatements :822 / maskNonCode :833 / BRANCH_HEAD_KEYWORDS :504 ; `export const ARMS_FLOOR = 30` :197 with `RE-DERIVED 2026-09-01` :195, `FILES_FLOOR = 1` :129 ; makeIdentityNonce/stampIdentities absent ; VAC04-ZERO-PATH-FAILS-CLOSED :248, TRIPWIRE :296, readers-ran line :415, SNAPSHOT_MIN=50 :547, evidence lines :379/:384/:388 ; VERBOSITY=verbose run.sh:153 ; 9 selftest fixtures, 4 SRO fixtures, 2 MW02 fixtures present"
    proves: "Quick regression pass over the previously-VERIFIED artifacts (SC-1..3, 5..9) — all anchors the a30ce097 report cited still resolve; combined with the empty source diff this is sufficient without re-running the pg-lane corpus."
  - command: "grep -anE 'TBD|FIXME|XXX' and 'TODO|HACK|PLACEHOLDER' over the two modified files"
    result: "0 and 0"
    proves: "Debt-marker gate clean on plan 13's files."
  - command: "grep -anE 'WR-04|bare[- ]grep|:667|grep_rc|READER-STDERR|BOTH clauses' TODOS.md"
    result: "no entry for REVIEW.md WR-04 (the only WR-04 hit, :3832, is an unrelated closed ConnectKeyStep item) and none for the two plan-12 follow-ups"
    proves: "164.3.1-13-SUMMARY.md's sentence 'REVIEW.md WR-04's bare-grep sites … (already in TODOS.md)' is FALSE — see W5. Non-blocking (CI-gate hygiene, reviewer-rated Warning), but a routing claim that is not true."

gaps: []

deferred:
  - truth: "[MUT-I02] header denominator (114 vs 262) and [MUT-I03] concurrent-agent dirty-checkout note"
    addressed_in: "TODOS.md (explicitly EXCLUDED by the ROADMAP goal text: 'EXCLUDES only two prose items, which stay in TODOS.md')"
    evidence: "TODOS.md:5131-5132 carry both items open; ROADMAP 164.3.1 goal names them as out of scope"
  - truth: "FILES_FLOOR raised from 1 as the other 70 gate files are annotated"
    addressed_in: "Phase 164.4"
    evidence: "run.mjs:129 `export const FILES_FLOOR = 1` with the 'Phase 164.4 raises FILES_FLOOR as it backfills the other 70 files' sentence; ROADMAP Phase 164.4 REDUNDER-BACKFILL"

coincidental_reliance_items: []

human_verification:
  - test: "First real `sql-mutation` run on its ubuntu host: read the job log for `SELF-TEST 12/12`, the corpus summary lines (`coverage:` / `arms:` / `biting:` / `lane-invocations:`), and the count-recheck step's closing sentence"
    expected: "12/12 self-test PASSED; `arms: 30/30/0`, `biting: 30`, `lane-invocations: 30`; the count-recheck echoes 'both floors hold and the runner's two tallies agree'. If psql's verbose grammar differs on ubuntu, the runner must emit a named MEASURE_FAIL — never a quiet pass"
    why_human: "Every lane in this phase (and in both verifications) ran on macOS / PostgreSQL 16.13. .planning/WINDOWS.md 28: the job has never executed on ubuntu. Nothing in the repo can observe that host — the plans themselves mark this human_judgment (05-D6, 10-D4, 11-D6)"
  - test: "First live `migration-drift-check.yml` run (VAC-04 with the real PROD credential) and first `sql-tests` run with `TEST_SUPABASE_DB_URL` (VAC-08) on this branch or its successor"
    expected: "VAC-04: on a migration PR defining no function, the D-13 `::notice:: … the zero is LEGITIMATE` path; on a PR with a visible definition, the normal compare verdict; the absurdity floor SILENT (index ≥ half the 118-body snapshot). VAC-08: `self-test OK (5/5)` step, then a ledger verdict with the shape diagnostic, floor silent on the real TEST ledger"
    why_human: "Both gates are proven only against injected commands (WINDOWS.md 25/26) — plan 13's new arms included (FAKE_CREDS + injected BODY_*_CMD); the real credential path has never been observed. ⛔ Standing ordering constraint: do not merge a migration PR between 164.3.1 landing and 164.4 landing — a block at the fail-closed zero path is the gate working"
  - test: "Read the 17 DIAGNOSTIC_FIRST_ALLOWLIST reasons (gate-family-meta.test.ts, classes PRECONDITION / REDACTED / EMPTY-BY-COND / READER-STDERR) and the 2 SRO_ALLOWLIST entries (self-referential-oracle.test.ts:150-196, types-design-tests.test.ts:27/:63)"
    expected: "Each reason is TRUE of its site, not a plausible sentence; the READER-STDERR/REDACTED entries are accepted only because their follow-up (capture the child's exit code with the grep_rc idiom) is booked — it is NOT booked yet, see W1"
    why_human: "The machine asserts a reason STRING exists per entry (exact set, both directions); whether the reason is legitimate is a reading judgment. Plans 08 (D6) and 12 (D7) mark exactly this human_judgment"
  - test: "(Optional, repeatable) Re-run plan 10's severed-tally harness: byte-backup run.mjs, change `laneTally[leg] += 1` → `if (leg !== \"arm\") laneTally[leg] += 1`, run the full corpus, restore from the backup, confirm the sha256 matches HEAD"
    expected: "exit 1 with an `absurdity` defect `executed=30 lane-invocations=0 biting=30` while `arms:`/`biting:` still print healthy; restore → exit 0"
    why_human: "The FIRE direction on the REAL wiring is a recorded harness (164.3.1-10-SUMMARY § Task 2, sha pairs). The standing pins cover the pure `absurdityViolations` function and the CI step's extracted `run:` block, and the a30ce097 verification's own corpus run confirmed the SILENT direction on real wiring; neither verification mutated the tree to re-observe the fire"
---

# Phase 164.3.1: SOUND-PRIMITIVES Verification Report (re-verification)

**Phase Goal:** All FOUR cycling control primitives closed BY CONSTRUCTION — A (statement-level SQL tokenizer, one definition of "code"), B (arm identity moved to source-location attribution where executing SQL cannot observe it), C (a gate's verdict bounded by what it measured: VAC-04 fail-closed zero path + realpath guards + grep exit branching + charset refusal; absurdity floors on VAC-04 and the runner), D (machine check for self-referential oracles; [AUDCOV-01] fixed with the A/B/C table; [MUT-W02] structural). Plus the regression corpus (6), ARMS_FLOOR re-derived (7), diagnostic-first (8), and the sample-size/coverage RULE (9). HARD DEPENDENCY for 164.4.
**Verified:** 2026-09-02T09:12:33Z at HEAD `092003fb` (8 commits over the previous verification HEAD `a30ce097`; 4 of them plan 13's)
**Status:** human_needed — 9/9 success criteria verified; the one gap (SC-4 partial) is CLOSED; four human items carried forward unchanged (external hosts/credentials and allowlist-reading judgments no automated check can observe)
**Re-verification:** Yes — after gap closure (plan 164.3.1-13)

## Re-verification scope

The a30ce097 report returned `gaps_found` with exactly one gap: SC-4 was met for [VAC04-C1] and [VAC04-C3] but [VAC04-C2] (realpath main-module guard) and [VAC04-C4] (charset refusal) were proven only on the two reader CLIs, with the gate-level propagation resting on a code-read of `|| fail` wrappers. Plan 13 claims to close it with test-only edits. Per the re-verification protocol the failed item got full three-level + behavioural verification; the eight passed criteria got a regression check, which here is unusually strong: `git diff --name-only a30ce097..HEAD` outside `.planning/` is exactly the two test files, so every artifact the previous report verified is byte-identical, and I re-resolved every cited anchor plus re-ran the cheap gates rather than the pg-lane corpus.

## Goal Achievement

### Observable Truths (the ROADMAP's SC-1..SC-9 — the contract; plan 13's must-haves merged under SC-4)

| # | Truth | Status | Evidence (mine unless marked "record") |
| --- | --- | --- | --- |
| SC-1 | Every measured instance across all four primitives is an EXECUTABLE arm in the regression corpus, each proven to RED by neutering its fix | ✓ VERIFIED (regression) | Registry exact-set (15 instances) green in my run, now with VAC04-C2/C4 bound at BOTH levels; the recursive half for the two new gate-level arms is the standing calibration legs (run on every CI run, both green) plus the recorded cycles C2-N1 / C2-N2 / C4-N1 whose pre/post sha256 I confirmed equal HEAD and whose RED text matches the assertion messages in the code (`the gate did not pass — a reader reached through a space/symlink path did not RUN … expected 1 to be +0`; `no longer carries the realpath guard … expected 0 to be greater than or equal to 1`; `the gate did not refuse a non-ASCII identifier … expected +0 to be 1`; `the naive refusal did not reach the gate's verdict`). Registry fire-proof RED text matches gate-family-meta.test.ts:346 verbatim (record). All other SC-1 evidence unchanged from a30ce097 (source diff empty). |
| SC-2 | Neuter classifier is statement-based; seven compound lines classify correctly; no accepted neuter leaves `SET ROLE postgres;` live | ✓ VERIFIED (regression) | parse.mjs/run.mjs/test files byte-identical to a30ce097; `tokenizeStatements` :822 still exported; prior evidence stands. |
| SC-3 | Arm identity cannot be produced by any SQL the gate executes | ✓ VERIFIED (regression) | run.mjs/pg-lane byte-identical; nonce helpers still absent; `VERBOSITY=verbose` run.sh:153; prior 12/12 self-test evidence stands. |
| SC-4 | VAC-04 cannot exit 0 having compared nothing; zero path fails closed; **each of [VAC04-C1]..[C4] driven end-to-end through the real gate and observed to exit non-zero on its own measured input** | ✓ VERIFIED (gap closed) | Zero path / C1 / C3: unchanged, gate script byte-identical (markers :248/:296/:379-388/:415/:547). **C2 — gate-driven now:** `[VAC04-C2] GATE-LEVEL` describe spawns `PROD_GATE = "scripts/prod-body-drift-check.sh"` with `NORMALIZER`/`NAIVE_NAMES` (env-injectable, gate :143-144) pointing at a symlink (`links/<basename>`) and a copy under `reader copies/`; four fixed-leg cases each with ONE load-bearing union member (calibration arm proves fixture A naive-only, fixture B normalizer-only by reader stdout); **pass condition is the readers-ran line** `Functions defined or replaced by this PR: 1` + `<name>: measured absent` + `not.toContain("MEASURE_FAIL")` (test :222-224) — NOT the D-13 refusal, satisfying the previous report's "Note for the fixer"; fixture-A cases additionally require the naive-only warning, proving the naive reader ran through ITS path. Standing calibration leg pins `realpathSync(process.argv[1])` ≥1 in both REAL readers, derives pre-fix copies (asserted 0 realpath / ≥1 pre-fix idioms), and drives the same wiring through symlinks on both fixtures → non-zero, `MEASURE_FAIL`, both injected paths `-> 0 name(s)`, readers-ran line absent. **C4 — gate-driven now:** REFUSAL arm drives the real gate on the P10 migration (`public.fúnc_é(p uuid)` on a computed line 2) → `toBe(1)`, output contains `leaves the unquoted charset`, `U+00FA`, `<migration>:2:`, `read 'public.f'`, the gate wrapper `could not extract function names from the changed migrations`; body sentinel calibrated present in the input and asserted ABSENT in output; readers-ran line / `measured absent` / success notice all absent. Calibration leg (both refusals disabled in scratch copies, proven by absence of the replaced text) → exit 0, `f: measured absent`, success notice, no `U+00FA` — the wrong-subject pass the fix removed. STATED BOUND is its own `it` (normalizer-only neuter + REAL naive → exit 1, `/sql-function-names-naive: [^\n]*U\+00FA/`). All 9 arms green under Node 25 and Node 22. Registry: both `also:` needles resolve once in the file bytes; the binds-by-name `it.each` iterates `[{file, needle: armNeedle}, ...also]` (gate-family-meta.test.ts:340-346). |
| SC-5 | `stripBlockComments` finds case A while B passes; the machine check flagged `lint-sql-gates.test.ts:182-186` at HEAD before the fix | ✓ VERIFIED (regression) | audit-coverage / lint-sql-gates / SRO files byte-identical; SRO blocking summary still exactly 2 findings over 131 files in my run; calibration ordering is a git fact (807702ff ancestor of b7a80598, unchanged). |
| SC-6 | ARMS_FLOOR and biting re-derived under the sound primitives; movement recorded | ✓ VERIFIED (regression) | `export const ARMS_FLOOR = 30` run.mjs:197 with `RE-DERIVED 2026-09-01` :195; `FILES_FLOOR = 1` :129; run.mjs byte-identical to the 30/30/30 corpus run at a30ce097. |
| SC-7 | Every gate in the family PRINTS ITS EVIDENCE on failure; an arm asserts it | ✓ VERIFIED (regression + extended) | `META diagnostic-first: 68 emission(s) over 2 gate(s), 17 bare, 17 allowlisted` green; plan 13's arms assert the evidence lines, not only exit codes (injected paths `-> 0 name(s)`; `U+00FA`, file:line, prefix read). |
| SC-8 | Absurdity floor on VAC-04, the runner and VAC-08, each proven to fire AND silent; thresholds record measurement + separation | ✓ VERIFIED (regression) | Gate/runner byte-identical; `SNAPSHOT_MIN=50` :547; floor arms within the 289-test drift-check file green. |
| SC-9 | Every measured key/threshold/join carries SAMPLE SIZE + COVERAGE and a full-corpus arm; no bare ones | ✓ VERIFIED (regression) | `META bare-measurement: 6 threshold site(s) over 5 file(s)` all ok in my run; KNOWN_THRESHOLD_SITES untouched by plan 13 (diff shows only the two registry entries changed). |

**Score:** 9/9 truths verified (0 present-but-behavior-unverified; 0 gaps)

### Plan 13 must-haves (the gap-closure contract) — each checked against the bytes

| Must-have | Status | Evidence |
| --- | --- | --- |
| C2 driven through the real gate to the readers-ran line, each union member individually load-bearing; C4 makes the gate exit non-zero naming U+00FA + `leaves the unquoted charset` with no body text | ✓ VERIFIED | Table above; verbose vitest listing shows the 9 arms under the two GATE-LEVEL titles. |
| Every new arm fails two ways: standing calibration leg + recorded neuter with RED-set specificity and sha256 pairs | ✓ VERIFIED | Standing legs present and green; recorded cycles' sha256 pairs bracket HEAD's bytes exactly; RED messages in the record are the literal `expect()` messages in the code; my scratch reproduction of the space shape agrees with cases 1/4 REDing. Note: the standing C2 leg covers the symlink shape only (W6). |
| C2 pass condition = readers-ran line, never the D-13 tripwire | ✓ VERIFIED | drift-check-scripts.test.ts (new block) `expect(out).toContain(READERS_RAN)` and `expect(out, "the D-13 refusal is the NEUTERED outcome, never the pass").not.toContain("MEASURE_FAIL")`; the refusal is asserted only in the calibration leg with `.not.toBe(0)`. |
| Registry binds C2/C4 at both levels via `also:`, needles resolve, distinction visible in the entry, fire-proof REDs by instance name | ✓ VERIFIED | `also:` count 4→6; READER-LEVEL / GATE-LEVEL comments in both entries; both needles count 1 in the target; citations carry `164.3.1-13` + `164.3.1-13-SUMMARY.md` (pass the non-vacuity regexes — arm green); fire-proof RED text matches :346 (record). |
| No gate/reader/plan 01-12 artifact changed outside a sha-verified neuter window; no database contacted | ✓ VERIFIED | Task commits touch only the two test files; `git diff --quiet HEAD -- scripts/ .github/` clean; reader hashes = recorded pre/post; arms use `FAKE_CREDS` (`stub-ref`/`stub-token`/`stub-password`) and injected `BODY_*_CMD` on a temp dump — no supabase CLI, no network. |

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | [MUT-I02] naive-reader header denominator 114 vs 262; [MUT-I03] concurrent-agent dirty-checkout note | TODOS.md (excluded by the ROADMAP goal) | TODOS.md:5131-5132 open; ROADMAP: "EXCLUDES only two prose items" |
| 2 | `FILES_FLOOR` raised as the other 70 gate files get annotations | Phase 164.4 | run.mjs:129 sentence; ROADMAP Phase 164.4 REDUNDER-BACKFILL |

### Required Artifacts (plan 13's, three levels; plans 01-12's artifacts unchanged since a30ce097 — see that table, carried forward)

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `src/__tests__/drift-check-scripts.test.ts` (3044 lines) | two new describes after `[VAC04-C1]`: C2 (calibration + 4 fixed-leg cases + calibration leg), C4 (refusal + calibration leg + stated bound) | ✓ VERIFIED | present at the declared position (`@@ -1283 +1285` in the diff, before the VAC-04 ABSURDITY FLOOR block); every gate-spawning `it` carries `30_000`; imports `symlinkSync`, `cpSync`, `resolve` added; 289 tests green; wired: spawns `scripts/prod-body-drift-check.sh` (the real gate), not a reader CLI, in every gate arm |
| `src/__tests__/gate-family-meta.test.ts` (841 lines) | VAC04-C2 / VAC04-C4 entries with `also:` gate-level bindings + extended citations | ✓ VERIFIED | diff is exactly those two entries (+22/−2); 21 tests green; wired: binds-by-name `it.each` resolves `also` needles via `countOf(read(b.file), b.needle)` |
| `164.3.1-13-SUMMARY.md` | recorded cycles C2-N1, C2-N2, C4-N1 + registry fire-proof with verbatim RED, RED sets, sha256 pairs | ✓ VERIFIED | present; hashes bracket HEAD; RED text = code's assertion messages; one false routing sentence (W5) |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| gate-level C2 arms | `scripts/prod-body-drift-check.sh` | `NORMALIZER` / `NAIVE_NAMES` env (gate :143-144) → symlink / space-dir copies; PROD side on canonical paths (word-split, never a space) | ✓ WIRED | fixed leg reaches `:415` readers-ran line; `:151` same-file refusal avoided by distinct basenames |
| C2 RED direction | D-13 zero path (gate :379/:384/:388) | pre-fix guard → both readers `-> 0 name(s)` → `MEASURE_FAIL` | ✓ WIRED | calibration leg asserts all three lines with the INJECTED paths |
| C4 refusal | gate :207-208 `node "$NORMALIZER" --function-names … \|\| fail` | `reportCharsetRefusal` stderr (not redirected) → gate output; wrapper sentence | ✓ WIRED | refusal arm asserts diagnostic + wrapper; naive call :221 unreachable on this input — stated non-coverage bounded by the STATED BOUND `it` |
| registry `also:` needles | the two describe titles | `countOf` over file bytes | ✓ WIRED | 1 hit each; renamed-needle fire-proof RED text matches :346 (record) |
| reader exit ≠ 0 / silent 0 | gate verdict | `\|\| fail` wrappers + D-13 tripwire | ✓ WIRED (was PARTIAL) | now exercised at gate level for all four members |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| C2 arms `out` / `status` | `run(PROD_GATE, env)` → `spawnSync` of the real gate | real gate process over temp fixtures + injected reader paths | Yes — readers-ran line / `0 name(s)` lines observed | ✓ FLOWING |
| C4 arms `out` / `status` | same | real gate + real readers on P10 | Yes — `U+00FA` diagnostic / wrong-subject pass observed | ✓ FLOWING |
| registry `bindings` | `[{file, needle: armNeedle}, ...also]` | node:fs read of target files | Yes — 6 `also:` bindings resolved | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| 9 new gate-level arms + registry | vitest (2 files), Node 25 | 310 passed | ✓ PASS |
| same under the CI runtime | vitest (2 files), Node 22.22.1 | 310 passed | ✓ PASS |
| contracts + SRO blocking + reader-level arms over the new bytes | vitest (7 files) | 130 passed, SRO 2 findings | ✓ PASS |
| lint / types | eslint (2 files), tsc --noEmit | clean / clean | ✓ PASS |
| reader self-tests after the recorded cycles | `--self-test` ×2 | 13 / 9 checks | ✓ PASS |
| space-shape blindness of the pre-fix guard (RED-direction consistency) | scratch copies, tree untouched | pre-fix: empty stdout exit 0; control: name | ✓ PASS |
| sibling gates | lint-sql-gates / ledger-drift / plan-anchors | 4 rules / 5/5 / no stale claims | ✓ PASS |
| C2/C4 through the REAL gate | (was: no arm) | 9 arms, green | ✓ PASS (previously ✗ — the gap) |

Not re-run (source byte-identical to a30ce097, where they were run and passed): `run.mjs --self-test` 12/12, the full pg-lane corpus 30/30/30, the tokenizer/parser/floors/neuter/audit-coverage/lint-sql-gates test files.

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist; the family's probes are the script self-tests run above (naive 13, normalize 9, ledger-drift 5/5, lint-sql-gates 4 rules, plan-anchors clean).

### Requirements Coverage

No REQUIREMENTS.md row maps to this phase (`grep -c '164.3.1' .planning/REQUIREMENTS.md` = 0; ROADMAP says `Requirements: TBD`). Every plan's `requirements:` field cites ROADMAP SC ids only; plan 13 cites `[SC-4]` and states the absence of a REQUIREMENTS row explicitly. No orphaned requirement can exist for this phase; the SC cross-reference is:

| Criterion | Claimed by plans | Completed per SUMMARY | Status |
| --------- | ---------------- | --------------------- | ------ |
| SC-1 | 11, 12 | 11, 12 | ✓ SATISFIED |
| SC-2 | 01 | 01 | ✓ SATISFIED |
| SC-3 | 05 | 05 | ✓ SATISFIED |
| SC-4 | 04, 07, **13** | 07, **13** (04: `[]`, superseded PARTIAL note) | ✓ SATISFIED (was PARTIAL) |
| SC-5 | 02, 03, 06, 08 | 02, 03, 06, 08 | ✓ SATISFIED |
| SC-6 | 09 | 09 | ✓ SATISFIED |
| SC-7 | 04, 05, 07, 10, 12 | 05, 07, 10, 12 | ✓ SATISFIED |
| SC-8 | 07, 10 | 07, 10 | ✓ SATISFIED |
| SC-9 | 07, 08, 09, 10, 12 | 07, 08, 09, 10, 12 | ✓ SATISFIED |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (2 plan-13 files) | — | TBD / FIXME / XXX / TODO / HACK / PLACEHOLDER | none found | — |
| `src/__tests__/audit-coverage.test.ts` | 470, 1035, 1079 | `TODO` (unchanged since a30ce097) | ℹ️ Info | fixture strings for the "TODO-comment is not coverage" detector — test inputs, not debt |

### Human Verification Required

See the `human_verification` frontmatter (4 items, carried forward unchanged from a30ce097 — plan 13 touched nothing they observe): first ubuntu `sql-mutation` run; first real-credential VAC-04 / VAC-08 runs (with the ⛔ migration-PR ordering constraint); a reading of the 17 + 2 allowlist justifications; the optional severed-tally re-run.

### Warnings (non-blocking — route, do not absorb)

- **W1 (carried, still open) — Two plan-12 follow-ups are recorded nowhere but in 164.3.1-12-SUMMARY.md.** (a) `scripts/test-ledger-drift-check.sh:44-47` "BOTH clauses are required" is stale (four clauses; clause 2 measured subsumed by clause 4). (b) Seven `|| fail` sites print no child exit code and are allowlisted under READER-STDERR/REDACTED with "a named follow-up". TODOS.md still carries neither (grep at HEAD: none). Add both to TODOS.md.
- **W2 (carried) — SUMMARY test-count drift.** Now consistent at 289 (280 + 9) per SUMMARY 13; SUMMARYs 07/12 still say 273/279 — stale prose only.
- **W3 (carried) — `gate-family-meta.test.ts` is not in its own `THRESHOLD_BEARING_FILES`**, so its `EMISSION_FLOOR=40` / `NEEDLE_MIN_LENGTH=16` are outside the machine audit.
- **W4 (carried) — SC-7's meta-arm scans shell gates only** (`FAMILY_SHELL_GATES` = 2).
- **W5 (new) — 164.3.1-13-SUMMARY.md makes a FALSE routing claim.** Its SC-4 statement says REVIEW.md WR-04's bare-grep sites at `prod-body-drift-check.sh:548-555` (find/grep under `pipefail`) and `:667` (`! grep -aqE '[^[:space:]]' "$live"` — a grep exit ≥ 2 read as "empty body") "were not touched (already in TODOS.md)". TODOS.md has no such entry (its only `WR-04` line, :3832, is an unrelated closed ConnectKeyStep item). WR-04 is reviewer-rated Warning and outside SC-4's letter (SC-4 names the *name-index* grep, fixed at :689-704), so it does not reopen the criterion; but it is a [VAC04-C3]-shaped site one branch earlier in a gate over PROD bodies, and it is currently routed to nothing. Add it to TODOS.md (or fix it in 164.4's gate work) — a deferral routed to nothing is the parent phase's G2 precedent.
- **W6 (new, informational) — The standing C2 calibration leg exercises only the SYMLINK shape.** The pre-fix copies are reached through `pre-fix-links/`; no standing leg reaches a pre-fix copy through a space-named directory. The space shape's RED direction is proven by the recorded cycles (cases 1 and 4 RED under C2-N1/C2-N2) and independently by my scratch reproduction, so nothing is unproven — but if someone later removes the URL-encoding sensitivity from the guard without touching realpath, the standing leg would not notice. One extra loop iteration (`space` shape on the pre-fix copies) would make the leg cover both shapes on every CI run.

### Gaps Summary

None remaining. The single gap from a30ce097 — SC-4 met by scope for two of four members — is closed by mechanism: [VAC04-C2] and [VAC04-C4] are each driven through `scripts/prod-body-drift-check.sh` itself, with the C2 pass condition being the readers-ran line (isolating the realpath fix from C1's tripwire), the C4 arm asserting non-zero exit + `U+00FA` + file:line + no body text, standing calibration legs that reproduce the pre-fix outcome on every run, recorded neuter cycles whose sha256 pairs bracket HEAD's bytes exactly, and both instances bound at both levels in the registry with the needles resolving. Everything else in the phase is byte-identical to the previously verified state and its anchors still resolve. Status is `human_needed` only because the four external-host / credential / judgment items no automated check can observe remain open, as they did before.

---

_Verified: 2026-09-02T09:12:33Z_
_Verifier: Claude (gsd-verifier)_
