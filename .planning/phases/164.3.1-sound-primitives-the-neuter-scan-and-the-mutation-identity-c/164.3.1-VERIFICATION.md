---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
verified: 2026-09-02T16:23:33Z
verifier: gsd-verifier (re-verification at HEAD e30f7c33, branch phase-164.3.1-sound-primitives; previous report at 092003fb, the one before at a30ce097)
status: passed
score: 9/9 success criteria verified
behavior_unverified: 0
overrides_applied: 0

re_verification:
  previous_status: human_needed
  previous_score: 9/9
  previous_head: 092003fb
  # Why this re-run was NOT redundant: 25 files changed outside .planning/ after
  # the 092003fb report — the entire ship-review fix pass (14/14 findings,
  # cbf29f95..2217688e, plus e8c5c422, 5d8f068f, 33b7f447, b13b36f9). run.mjs
  # alone took +751 lines and prod-body-drift-check.sh +364. The prior report's
  # regression argument ("the source diff outside .planning is empty") was true
  # at 092003fb and is FALSE at e30f7c33, so every criterion was re-measured
  # against the code rather than carried forward.
  gaps_closed:
    - "W5 (prior report) — 164.3.1-13-SUMMARY.md's FALSE claim that WR-04's bare-grep sites were 'already in TODOS.md'. CORRECTED at 13-SUMMARY.md:267 by commit 2217688e (IN-09), which states plainly that the earlier sentence was false, names the real fix commit (d958e54e) and routes the three remaining sites to TODOS.md § 'Phase 164.3.1 review-fix — bounded residuals' (TODOS.md:2466, present at HEAD)."
    - "W1 (prior report) — the READER-STDERR / grep_rc follow-up was accepted into DIAGNOSTIC_FIRST_ALLOWLIST on the strength of a follow-up that was NOT booked. It is now booked: TODOS.md:2467 at HEAD, naming the seven `node \"$READER\" … || fail` sites, the four allowlist entries that depend on it, and the exact `set +e; …; _rc=$?; set -e` idiom to use."
    - "UAT item 1 (ubuntu sql-mutation) — SUPERSEDED AND STRENGTHENED. The UAT record cites run 33620169220 at 89cbef8b with SELF-TEST 12/12, which predates the whole review-fix pass. I re-bound it: CI run 33643046061 ran at 60b0722d, and `git diff 60b0722d..HEAD` outside .planning/ is EMPTY, so that run is SHA-bound to the exact bytes I verified. Its sql-mutation job (100290699311) is `success` with SELF-TEST 15/15 PASSED, coverage files 1/71, arms 30/30/0, biting 30, lane-invocations 30, '✅ No defects'."
    - "UAT item 2 (VAC-04 / VAC-08 real credential) — VAC-08 fully closed on the real TEST credential (job 100297358657). VAC-04's real-credential window (WINDOWS.md 25) retired; its three unreached branches are booked as [VAC04-ARMS-UNRUN] and appear under `deferred` below rather than as an open human item."
    - "UAT items 3 and 4 — read/driven by the orchestrator and recorded pass; item 4 is additionally now pinned IN-PROCESS on every run as SELF-TEST 14/15, which is stronger than the manual harness it replaces."
  gaps_remaining: []
  regressions: []
  # Regression sweep result: NONE. Every review fix I drove myself either
  # strengthened an existing control or left it behaviourally identical. The two
  # changes with real regression potential were tested head-on and both hold —
  # see independent_measurements entries 8 (5d8f068f narrowed the naive reader's
  # refusal; `ú` still refuses through the real gate) and 12 (33b7f447 made the
  # tripwire unconditional; comment-only text still takes the LEGITIMATE-zero
  # exit 0, a real definition still fires).

# ─────────────────────────────────────────────────────────────────────────────
# Commands I EXECUTED MYSELF in this checkout at HEAD e30f7c33. Serial, never
# parallel. No `supabase` CLI command, no shared database — the only PostgreSQL
# contact was the throwaway cluster scripts/pg-lane/run.sh initdb's into a temp
# dir. The repository was never mutated: `git status --porcelain` returned the
# same two pre-existing user edits (164.3.1-UAT.md, TODOS.md) before and after.
# All reader-neutering was done on COPIES under the scratchpad.
# ─────────────────────────────────────────────────────────────────────────────
independent_measurements:
  - command: "git status --porcelain (before and after the whole run); git rev-parse HEAD"
    result: "Both times exactly ` M …/164.3.1-UAT.md` and ` M TODOS.md` — the orchestrator's own in-flight edits, present before I started. HEAD e30f7c33 throughout."
    proves: "Nothing I ran mutated the tree. The two dirty files are pre-existing and are the UAT record plus its TODOS bookings, not verification residue."

  - command: "git diff --stat 092003fb..HEAD -- . ':(exclude).planning' ; git log --oneline 092003fb..HEAD"
    result: "26 files, +3205/−633, over 24 commits. Source of substance: run.mjs +751, prod-body-drift-check.sh +364, drift-check-scripts.test.ts +623, parse.mjs +145, plus 8 other src/__tests__ files, 3 selftest fixtures, ci.yml, GRAMMAR.md, pg-lane/run.sh, both readers and a new MW02 red fixture."
    proves: "The prior report is materially stale. Its central regression argument (empty source diff) does not hold at this HEAD, so no criterion could be carried forward on it."

  - command: "gh pr view 730 --json headRefOid ; git diff --stat 60b0722d..HEAD -- . ':(exclude).planning'"
    result: "PR head 60b0722d; the diff is EMPTY."
    proves: "The shipped PR's CI board is SHA-bound to the exact bytes I verified — CI green here is evidence about THIS code, not an ancestor (the trap named in reference_ci_green_must_be_sha_bound)."

  - command: "gh run view 33643046061 --json jobs ; gh run view --job 100290699311 --log"
    result: "21/21 jobs success at 60b0722d, including sql-mutation, sql-gate-lint, plan-anchor-verify, sql-tests, frontend-coverage, e2e, e2e-seeded. sql-mutation log: `SELF-TEST 1/15`..`15/15`, `SELF-TEST PASSED: both floor modes, the waiver ceiling, the wrong-identity mode, MEASURE_FAIL, the absurdity floor and the 164.3.1-11 regression corpus all fire`; `coverage: files 1/71`; `arms: 30/30/0`; `biting: 30`; `lane-invocations: 30`; `✅ No defects.`"
    proves: "SC-1/2/3/6/8 execute green on ubuntu at these exact bytes. Supersedes the UAT's 12/12-at-89cbef8b record, which was taken before the review pass added three self-test modes."

  - command: "node scripts/mutation-runner/run.mjs --self-test    (local, macOS, real pg-lane cluster)"
    result: "15/15, every named arm `ok`, exit 0. Includes 9/15 [R4-C01] compound HEAD refused as `neuter-missed` naming `SET ROLE postgres;` beside an ACCEPTED P1 neuter; 10/15 [MUT-I01] P4/P5; 11/15 [R4-C02] current_query() trigger SYNTHESISED with the genuine arm RED (identity ok); 12/15 nested-EXECUTE AIMED forgery refused by chain LENGTH; 13/15 [F1] message-embedded CONTEXT+LOCATION forgery refused by field DUPLICATION; 14/15 severed tally `absurdity` executed=1 lane-invocations=0; 15/15 `lane-unrunnable` ENOENT never scored biting."
    proves: "SC-2 and SC-3's fire directions, in-process, on real SQL. Each mode ships a PASSING CONTROL in the same run, so none of them can pass by refusing everything."

  - command: "node scripts/mutation-runner/run.mjs    (full corpus, local throwaway pg-lane cluster)"
    result: "30 arms each `exit 3  RED (identity ok)`; `coverage: files 1/71`; `arms: 30/30/0`; `biting: 30`; `lane-invocations: 30`; `per-arm lane time: mean 1.6s`; `✅ No defects. Every annotated arm bit its own arm first.`"
    proves: "SC-6 re-derived by me — ARMS_FLOOR=30 is the measured biting count, and the runner's two INDEPENDENT tallies (verdict loop 30, in-lane spawn counter 30) agree. Also SC-8's SILENT direction on the mutation-runner absurdity floor."

  - command: "node --input-type=module … tokenizeStatements(test_profiles_privileged_columns_locked.sql) over lines 96/101/106/111/116/121/132"
    result: "Every one of the seven decomposes into SEPARATE statements: `EXCEPTION WHEN OTHERS THEN` (head) | `v_raised := true;` | `END;`. Not one line-level classification among them."
    proves: "SC-2's literal text — the seven compound lines the ROADMAP names are classified correctly by the shipped tokenizer, statement-wise. Measured on the real file, not a fixture."

  - command: "node --input-type=module … tokenizeStatements on `… END LOOP; … SET ROLE postgres; IF NOT ok THEN …` and on String.raw`SELECT E'a\\'b'; SET ROLE postgres;`"
    result: "head flags: `END LOOP;` false, `SET ROLE postgres;` false, `IF NOT ok THEN` true, `FOR r IN SELECT 1 LOOP` true. E-string: ONE literal `SELECT E'a\\'b';` then a separate `SET ROLE postgres;`."
    proves: "CR-01 and WR-01 — the two review findings that touched Primitive A — are genuinely closed, verified by driving the shipped tokenizer myself rather than reading the fix report. A block closer is not a head, and a backslash-escaped quote inside E'…' does not terminate the literal (which is what would otherwise mis-split the statement that follows)."

  - command: "REAL GATE, C4: CHANGED_MIGRATIONS=<my migration defining public.fúnc_é(p uuid)>, FAKE creds + injected BODY_* commands, bash scripts/prod-body-drift-check.sh"
    result: "exit 1. `identifier leaves the unquoted charset [A-Za-z0-9_$] — read 'public.f' then hit 'ú' (U+00FA). Refusing rather than dropping: a dropped definition is absent from the function-name index, so VAC-04 classifies it 'absent from PROD, therefore NEW, therefore a pass'.` plus the gate wrapper `could not extract function names from the changed migrations`."
    proves: "SC-4 member [VAC04-C4] end-to-end through the real gate, at THIS HEAD. This is the highest-regression-risk item in the phase: commit 5d8f068f deliberately LOOSENED the naive reader ('no longer refuses prose after a name'). The loosening is scoped to name-terminating punctuation (`,` `.` `;` `)`) only — `ú` still refuses, and the refusal still reaches the gate's verdict."

  - command: "REAL GATE, calibration control: same invocation with plain ASCII public.indep_ctrl(p uuid)"
    result: "`Functions defined or replaced by this PR: 1` — the readers-ran line. No refusal, no U+00FA."
    proves: "The C4 arm is not passing by refusing every input. The refusal discriminates."

  - command: "REAL GATE, zero path: a migration containing only `ALTER TABLE … ADD COLUMN`"
    result: "exit 0, `::notice::… no functions defined, and the zero is LEGITIMATE — nothing to compare.` with the file listed and the masking rationale printed."
    proves: "SC-4's D-13 legitimate-zero branch is reachable and honest — the gate distinguishes 'measured absence of functions' from 'could not measure'."

  - command: "REAL GATE, tripwire both directions: (i) `CREATE OR REPLACE FUNCTION` text present ONLY inside `--` and `/* */` comments; (ii) a real `CREATE OR REPLACE FUNCTION \"wéird quoted\"()`"
    result: "(i) exit 0, LEGITIMATE zero — no false tripwire. (ii) exit 1, refusing the name as a filename component, naming the traversal/collision risk."
    proves: "Commit 33b7f447 ('textual tripwire runs unconditionally with comments masked') is correct in BOTH directions — it did not turn every commented mention into a false failure, and it still catches a real definition the structural readers do not surface."

  - command: "REAL GATE, [VAC04-C1] fail-closed: two DISTINCT injected readers that emit nothing and exit 0, over a file with a plainly visible definition"
    result: "exit 1. `MEASURE_FAIL — a function definition is present that NEITHER reader can see.` then `WHAT THE TWO READERS SAW (evidence first — D-12/SC-7):` listing the changed file, `blindA.mjs --function-names -> 0 name(s) / (none)`, `blindB.mjs -> 0 name(s) / (none)`, `crude textual scan (comments masked) … -> MORE definitions than the readers' union in: …: textual 1 definition(s) vs structural 0`, and `[VAC04-C1] Two independent readings agreeing is not evidence of absence when their blind spots OVERLAP`."
    proves: "The single most important claim in the phase, measured by me: VAC-04 CANNOT exit 0 having compared nothing. The composed-blindness class is caught by a THIRD, independent reading, and the failure prints its evidence rather than its conclusion (SC-4 + SC-7 in one observation). Note the gate separately REFUSED my first attempt because NORMALIZER and NAIVE_NAMES resolved to the same file — 'two readings that share an implementation agree by construction'."

  - command: "REAL GATE, [VAC04-C2] FIXED direction: readers reached via a SYMLINK dir and via a dir with a SPACE in its name (`sp ace/`)"
    result: "Both print `Functions defined or replaced by this PR: 1` — the readers RAN through both path shapes."
    proves: "SC-4 member [VAC04-C2]. `realpathSync(process.argv[1]) === fileURLToPath(import.meta.url)` at sql-body-normalize.mjs:771 and sql-function-names-naive.mjs:368 survives both shapes."

  - command: "REAL GATE, [VAC04-C2] RED direction: pre-fix copies derived in the SCRATCHPAD (guard reverted to ``import.meta.url === `file://${process.argv[1]}` ``; realpath idioms 1 -> 0 in each), reached via symlink"
    result: "exit 1, `a function definition is present that NEITHER reader can see`, `prefixlinks/norm.mjs --function-names -> 0 name(s)`, `prefixlinks/naive.mjs -> 0 name(s)`."
    proves: "Two-directional proof of C2 that I performed myself. The pre-fix guard demonstrably goes blind through a symlink, so the C2 arm CAN fail — and, notably, even that blindness now fails CLOSED at the tripwire instead of exiting 0. The repository was never touched: both neutered copies live under the scratchpad."

  - command: "REAL GATE, SC-8 FIRE on VAC-04's absurdity floor: injected name index holding 1 name against the 118-body committed snapshot"
    result: "exit 1 with `WHAT WAS READ (evidence first — D-12/SC-7): PROD function-name index : 1 name(s) … committed snapshot bodies: 118 … absurdity floor: the index must hold at least half the snapshot population (59 name(s)) once that population is >= 50 … first names actually read: idx`."
    proves: "SC-8 fire direction on VAC-04, with the threshold, the population and the sample all printed."

  - command: "REAL GATE, SC-8 SILENT on VAC-04's absurdity floor: same run with a 130-name index"
    result: "Floor silent; the run proceeded to per-name comparison and failed later for an unrelated (deliberately fake) fetcher reason."
    proves: "SC-8's second direction on VAC-04 — the floor does not fire unconditionally, which is exactly the failure mode the ROADMAP warns about ('a floor that fires unconditionally also passes its own RED arm')."

  - command: "REAL GATE, zero-comparison floor: the C1 shape with a healthy index"
    result: "exit 1, `MEASURE_FAIL — ZERO bodies compared, and only 0 of 1 function(s) were measured absent from PROD. The rest reached no comparison at all, so this run has nothing to report clean.`"
    proves: "The union readers now SEE `sanitize_user$v2` (the C1 blind spot is closed at reader level) AND the gate independently refuses to report clean on zero comparisons — defence in depth, both layers observed."

  - command: "sed of scripts/prod-body-drift-check.sh:765-800 — the [VAC04-C3] name-index branch"
    result: "`set +e; grep -aqxF …; _idx_rc=$?; set -e` then THREE exhaustive legs: `>= 2` MEASURE_FAIL printing `grep exited : N (0 = present, 1 = measured absent, >= 2 = ERROR)`; `== 0` extraction-failure; else the single silent pass, reachable only from a grep that RAN."
    proves: "SC-4 member [VAC04-C3]. The fail-OPEN else the ROADMAP describes is gone; the three legs are exhaustive over grep's status."

  - command: "sed of prod-body-drift-check.sh:396-402 and :604-614 (WR-04's three remaining bare sites)"
    result: "The textual hit list (`_hits_rc`), the snapshot walk (`_find_rc`) and the snapshot count (`_snap_rc`) each capture status and fail with `MEASURE_FAIL: could not read/enumerate … (exited N)`."
    proves: "WR-04 is genuinely fixed in the script, not merely reported fixed. The prior report's W5 concerned the ROUTING claim about these sites, which IN-09 corrected."

  - command: "node_modules/.bin/vitest run — FOUR serial batches, never parallel: (1) drift-check-scripts + gate-family-meta + lint-sql-gates + mutation-annotation-parser; (2) mutation-runner-floors + mutation-runner-neuter + sql-statement-tokenizer + vac04-reader-guards + self-referential-oracle; (3) src/__tests__/contracts; (4) audit-coverage"
    result: "exit 0 every batch. 454 + 265 + 109 + 26 = 854 tests passed, 0 failed. Diagnostics printed: `META diagnostic-first: 75 emission(s) over 2 gate(s), 17 bare, 17 allowlisted`; `META bare-measurement: 8 threshold site(s) over 6 file(s)` all `ok`; `SELF-REF-ORACLE blocking summary: 2 finding(s) in 1 file(s), 131 file(s) scanned` (both allowlisted, unchanged); `MW02 result-loop parse: 11 condition(s), 3 job arm(s)`; `MW02 executed posture: 12 job(s) × 8 guard combination(s)`."
    proves: "Every changed test file is green at HEAD, and the repo-wide contract scans (which read all of src/) did not redden on the ~1,800 new test lines. SC-5, SC-7 and SC-9's machine checks all execute."

  - command: "node --input-type=module enumeration of INSTANCE_ARM_REGISTRY + read of the EXACT SET guard (gate-family-meta.test.ts:361-380)"
    result: "15 instances: R4-C01/P3, MUT-I01-P4, MUT-I01-P5, R4-C02, NESTED-EXECUTE, VAC04-C1..C4, VAC08-253, VAC08-JOIN, VAC-SELFREF-01, SP-C04, AUDCOV-01, MUT-W02. 6 `also:` gate-level bindings. The guard fails BY NAME in both directions against PHASE_TAXONOMY (unbound instance, and orphan registry entry)."
    proves: "SC-1 — every instance the ROADMAP goal names has a registered executable arm, and neither list can drift from the other silently."

  - command: "sed of src/__tests__/audit-coverage.test.ts:1296-1332 (the A/B/C table) + vitest run of that file"
    result: "case A `[AUDCOV-01]` expects `[5]` (the site is FOUND — the pre-fix code returned `[]`); case B labelled `(calibration control)` expects `[5]`; case C expects `[3]`. 26/26 pass."
    proves: "SC-5's first half — the multi-line-template regression is fixed and the B control is present and load-bearing, so case A's result is evidence rather than coincidence."

  - command: "sed of src/__tests__/lint-sql-gates.test.ts:178-196"
    result: "The [VAC-SELFREF-01] site carries `FIXED HERE — Phase 164.3.1 plan 08`, states that the old assertions read a string literal declared one line above and 'could not fail', and records that the machine check flagged this file at `:183` and `:184` AT HEAD before the fix, verbatim in 164.3.1-02-CALIBRATION.md § II."
    proves: "SC-5's second half — the primitive-D machine check demonstrably flagged the named shape at HEAD before it was fixed, and the calibration is recorded rather than asserted."

  - command: "node scripts/sql-function-names-naive.mjs --self-test ; node scripts/sql-body-normalize.mjs --self-test ; node scripts/lint-sql-gates.mjs --self-test ; bash scripts/test-ledger-drift-check.sh --self-test ; node scripts/verify-plan-anchors.mjs --pending"
    result: "`self-test OK (14 checks)` (was 13 — 5d8f068f added the prose-after-name arm); `self-test OK (9 checks)`; `4 rules, red+green each`; `self-test OK (5/5 arms — all four red modes fire and the green path passes)` incl. `zero-comparisons RED`; `OK: 2 plan file(s), no stale claims` over 7 claims."
    proves: "Every sibling gate in the family is intact and each carries a working fire-proof. VAC-08's zero-comparison arm is SC-8's third floor."

  - command: "node scripts/mutation-runner/run.mjs --fixture-corpus --parse-only"
    result: "`⚠️ STATIC PARSE ONLY — ZERO arms executed. This is NOT the gate: it cannot observe a non-biting annotation. Run \\`node scripts/mutation-runner/run.mjs\\`.` Also rejects `--parse-only` combined with `--file`/`--arm` and rejects an unknown `--scope-dir`."
    proves: "The parse-only mode cannot be mistaken for the gate — it announces its own non-measurement. Relevant to WR-02 (mode identity) and to SC-8's 'unknown is not zero' discipline."

  - command: "git diff 092003fb..HEAD -- .github/workflows/ci.yml"
    result: "Adds WAIVED_CEILING to the floors read, a numeric MEASURE_FAIL guard on it, an `arms_waived > waived_ceiling` hard fail, and extends the closing sentence to `both floors and the waiver ceiling hold and the runner's two tallies agree … waived N <= C`. Self-test comment updated 12/12 -> 15/15. Mode identity (`bash` the bare command) unchanged."
    proves: "The CI wiring was STRENGTHENED, not loosened. The hole it closes is directly relevant to Phase 164.4: a prose marker paired with a waiver twin would raise annotated coverage across all 70 unannotated files while adding zero arms and lowering no floor. That is now bounded from above."

  - command: "Debt-marker gate — grep -anE '\\b(TBD|FIXME|XXX)\\b' over all 20 changed source/workflow files, excluding lines carrying a formal follow-up reference"
    result: "ZERO. The only TODO/HACK/PLACEHOLDER hits are 4 pre-existing descriptive uses of 'PLACEHOLDER env' in ci.yml at :652/:4205/:4265/:4290, all outside this phase's diff hunks."
    proves: "No unreferenced debt markers were introduced. Completion is auditable."

  - command: "TODOS.md at HEAD: grep for the prior report's W1/W5 routing; git diff TODOS.md for the uncommitted delta"
    result: "HEAD carries TODOS.md:2466 § 'Phase 164.3.1 review-fix — bounded residuals' and :2467 the grep_rc follow-up. The UNCOMMITTED working-tree delta adds `[VAC08-SELFTEST-CI]` (ci.yml:2051 invokes the ledger gate bare, so its `--self-test` never runs in CI) and `[VAC04-ARMS-UNRUN]` (three VAC-04 branches unreached on the real credential)."
    proves: "Both prior-report warnings are routed, and the two UAT-2 residues are booked — though the bookings for the latter are still UNCOMMITTED. See Anti-Patterns / observation O-2."

  - command: "Adversarial SC-9 probe — read findThresholdSites (gate-family-meta.test.ts:560-570), then scanned all 7 family files for CEILING/MAX/LIMIT-named numeric constants"
    result: "The detector matches only `[A-Z_]*(FLOOR|MIN)[A-Z0-9_]*` constants and shell `-ge/-gt/-lt/-le <2+ digits>` comparisons. Exactly ONE family threshold escapes that naming: `WAIVED_CEILING = 0` (run.mjs:226) — added by this phase's own review pass. It is NOT among the arm's 8 enumerated sites. I read run.mjs:199-226 and it DOES carry its justification inline: 'PINNED 2026-09-02 BY MEASUREMENT (164.3.1 red team), not chosen', with the sample (`--parse-only` -> arms 0/30/0, 0 waivers), an independent node:fs re-derivation, and the pin in mutation-runner-floors.test.ts."
    proves: "SC-9's SUBSTANCE holds — I grepped the family for justifying measurements and found no bare ones, WAIVED_CEILING included. But the ARM that enforces SC-9 by construction has a naming-scoped blind spot. Recorded as observation O-1: non-blocking, since the one site in that class is justified and re-derived on every run, but on-theme for the phase's own thesis."

  - command: "pgrep -fl 'postgres.*pg-lane|pg-lane'"
    result: "No matches."
    proves: "No orphaned postmaster survived my corpus and self-test runs — pg-lane's D-04 trap (made loud by 33b7f447) cleaned up after both."

gaps: []

deferred:
  - truth: "[MUT-I02] header denominator (114 vs 262) and [MUT-I03] concurrent-agent dirty-checkout note"
    addressed_in: "TODOS.md — EXPLICITLY EXCLUDED by the ROADMAP goal ('EXCLUDES only two prose items, which stay in TODOS.md')"
    evidence: "ROADMAP 164.3.1 goal names both as out of scope; both carried open in TODOS.md."
  - truth: "FILES_FLOOR raised from 1 as the other 70 gate files are annotated"
    addressed_in: "Phase 164.4 REDUNDER-BACKFILL"
    evidence: "run.mjs:129 `export const FILES_FLOOR = 1`; measured coverage at HEAD is `files 1/71`; ROADMAP Phase 164.4's goal is 'Coverage moves from 1 file to all of them, behind a floor that cannot regress.'"
  - truth: "[VAC04-ARMS-UNRUN] — VAC-04's D-13 legitimate-zero branch (:448), its normal compare verdict, and its absurdity floor (:608+) have never executed against the REAL PROD credential"
    addressed_in: "The next genuine migration-bearing PR (booked in TODOS.md, working-tree delta)"
    evidence: "migration-drift-check run 33643046189 DID run on a real pull_request with the real credential (`::notice::Drift-check credentials present.`), retiring WINDOWS.md 25; but PR #730 touched no supabase/migrations/**, so the gate took its earliest short-circuit at :198. I drove all three of those branches myself against the real gate script with injected data (measurements 11, 12, 16, 17 above), so the LOGIC is verified — what remains unobserved is only the live PROD data source. ⛔ The standing instruction not to merge a migration PR between 164.3.1 and 164.4 landing still holds."
  - truth: "[VAC08-SELFTEST-CI] — VAC-08's five-arm self-test is never invoked by any workflow (ci.yml:2051 calls the script bare)"
    addressed_in: "TODOS.md § FIX MID-TERM (working-tree delta)"
    evidence: "The gate itself runs and produces a real verdict in CI (job 100297358657); what is missing is the machine proof that its four RED modes still fire. I ran `--self-test` myself at HEAD: 5/5 arms ok. Guard hygiene, not user-facing — non-blocking per the stopping rule."

behavior_unverified_items: []

coincidental_reliance_items: []

human_verification: []
---

# Phase 164.3.1: SOUND-PRIMITIVES Verification Report (re-verification #2)

**Phase Goal:** All FOUR cycling control primitives closed BY CONSTRUCTION — A (a SQL-aware statement tokenizer carrying quote / dollar-quote / block-comment state across lines, so the file has ONE definition of what is code), B (arm identity moved where executing SQL cannot observe it), C (a gate's verdict bounded by what it actually measured, in BOTH directions), D (a machine check for oracles that agree with themselves by construction). Plus the executable regression corpus (6), ARMS_FLOOR re-derived (7), diagnostic-first (8), and the sample-size/coverage RULE (9).

**Verified:** 2026-09-02T16:23:33Z at HEAD `e30f7c33`
**Status:** ✅ **passed** — 9/9 success criteria verified, no gaps, no open human items
**Re-verification:** Yes — #2, after the ship-review fix pass (24 commits, 26 files, +3205/−633 outside `.planning/`)

## Why this re-run was necessary

The `092003fb` report justified carrying eight of its nine criteria forward on a single strong fact: `git diff --name-only a30ce097..HEAD` outside `.planning/` was exactly two test files. That argument is **void at this HEAD**. The review-fix pass rewrote the load-bearing code — `run.mjs` +751 lines, `prod-body-drift-check.sh` +364, `parse.mjs` +145 — and touched the tokenizer (CR-01, WR-01), the VAC-04 readers (5d8f068f), the VAC-04 tripwire (33b7f447), the CI wiring and both union readers' refusal logic. Two of those changes **loosen** behaviour that a criterion depends on. So every criterion was re-measured against the code; nothing was carried forward on the prior report's authority.

I also re-bound the CI evidence. The UAT record cites `SELF-TEST 12/12` at `89cbef8b`, which predates the whole fix pass — carrying that forward would have been the ancestor-SHA trap. The correct binding is CI run `33643046061` at `60b0722d`, where `git diff 60b0722d..HEAD` outside `.planning/` is **empty**: that board is about these exact bytes, and its `sql-mutation` job reports **15/15**, not 12/12.

## Goal Achievement

### Observable Truths — the ROADMAP's nine Success Criteria

| # | Success Criterion | Status | Evidence I measured myself |
| --- | --- | --- | --- |
| SC-1 | Every measured instance across all four primitives is an EXECUTABLE arm in the regression corpus, each proven to RED by neutering its fix | ✓ VERIFIED | `INSTANCE_ARM_REGISTRY` holds 15 instances spanning A (R4-C01/P3, MUT-I01-P4, MUT-I01-P5), B (R4-C02, NESTED-EXECUTE), C (VAC04-C1..C4, VAC08-253, VAC08-JOIN) and D (VAC-SELFREF-01, SP-C04, AUDCOV-01, MUT-W02), with 6 `also:` gate-level bindings. The EXACT SET guard fails by name in **both** directions against `PHASE_TAXONOMY`. Fire proofs executed in front of me: self-test 15/15 locally and on ubuntu. RED proofs I performed myself: the C2 pre-fix guard goes blind through a symlink (both readers `-> 0 name(s)`), and the C4 refusal fires on `ú` while the ASCII control passes. |
| SC-2 | Neuter classifier is statement-based; the seven compound lines in `test_profiles_privileged_columns_locked.sql` classify correctly; no accepted neuter leaves `SET ROLE postgres;` live | ✓ VERIFIED | I drove `tokenizeStatements` over the **real file** at all seven named lines (96/101/106/111/116/121/132): each decomposes into `EXCEPTION WHEN OTHERS THEN` (head) \| `v_raised := true;` \| `END;` — statements, not lines. Self-test 9/15 refuses the compound HEAD as `neuter-missed` **naming `SET ROLE postgres;`**, with an ACCEPTED P1-shape neuter beside it as the calibration control. 10/15 covers [MUT-I01] in both the spurious-miss (P4) and silent-over-neuter (P5) directions. I separately confirmed the two review fixes on this path: `END LOOP;` is `head=false` (CR-01) and `E'a\'b'` is consumed as ONE literal (WR-01). |
| SC-3 | Arm identity cannot be produced by any SQL the gate executes — demonstrated by re-running the R4-C02 `current_query()` trigger and observing NO-IDENTITY, or the reachability proof recorded | ✓ VERIFIED | The runtime nonce is gone; identity is now source-location attribution (`run.mjs:820+`, "THE REPLACEMENT (CONTEXT D-01, locked)") reading the error's CONTEXT chain and LOCATION field, with `IDENTITY_RE` spelled once and every reader derived from `IDENTITY_RE.source` (IN-03). Three forgery classes are refused, each with a genuine arm scoring RED (identity ok) **in the same run**: 11/15 the R4-C02 `current_query()` trigger (the ROADMAP's own demonstration), 12/15 a nested-EXECUTE forgery AIMED at the genuine raise line, refused by chain LENGTH alone, 13/15 the review-discovered [F1] message-embedded CONTEXT+LOCATION pair, refused by field DUPLICATION and explicitly *not* by the chain-length rule it satisfies. |
| SC-4 | VAC-04 cannot exit 0 having compared nothing: the zero path fails closed, and each of [VAC04-C1]..[C4] is driven end-to-end through the real gate and observed to exit non-zero on its own measured input | ✓ VERIFIED | **Seven** independent drives of `scripts/prod-body-drift-check.sh` by me at this HEAD. C1: two distinct blind readers over a visible definition → exit 1, `a function definition is present that NEITHER reader can see`, with both readers' outputs and the textual-vs-structural count printed. C2: readers via symlink AND via a space-bearing path both RAN; pre-fix copies via symlink went blind → exit 1. C3: read the three-way exhaustive `_idx_rc` branch (`>=2` MEASURE_FAIL / `0` extraction-failure / `1` the one silent pass, reachable only from a grep that ran). C4: `public.fúnc_é` → exit 1 naming `read 'public.f' then hit 'ú' (U+00FA)`. Zero path: no-function migration → the D-13 LEGITIMATE notice; comment-only mention → same; a quoted non-ASCII definition → refused. Zero-comparison floor → `MEASURE_FAIL — ZERO bodies compared`. The gate additionally refuses two readers that resolve to the same file. |
| SC-5 | `stripBlockComments` finds the multi-line-template site (case A) while the B control still passes, and the primitive-D machine check flags the `lint-sql-gates.test.ts:182-186` shape at HEAD before it is fixed | ✓ VERIFIED | The A/B/C table ships as three executable `it`s (`audit-coverage.test.ts:1306/1316/1326`), B explicitly labelled `(calibration control)`: A `[5]` (pre-fix returned `[]`), B `[5]`, C `[3]`; 26/26 green. `quoteAtEnd` carries string state across lines with backtick handling at `:252`. The machine check is BLOCKING: `self-referential-oracle.test.ts` scanned 131 files and reports 2 findings, both allowlisted with `measured` citations. The `:182-186` site is fixed and its comment records that the machine — not a review round — flagged it at `:183`/`:184` at HEAD, verbatim in `164.3.1-02-CALIBRATION.md § II`. [MUT-W02] is re-expressed structurally: `MW02 result-loop parse: 11 condition(s), 3 job arm(s)` plus an EXECUTED posture over `12 job(s) × 8 guard combination(s)`, so any spelling of a tolerance is caught. |
| SC-6 | ARMS_FLOOR and the biting count re-derived under the sound primitives, any movement from 30/30/0 recorded with the reason | ✓ VERIFIED | My own full corpus run on a throwaway pg-lane cluster: `coverage: files 1/71`, `arms: 30/30/0`, `biting: 30`, `lane-invocations: 30`, `✅ No defects`. Identical on ubuntu at the SHA-bound run. `ARMS_FLOOR = 30` (run.mjs:197) and `FILES_FLOOR = 1` (:129) each carry a `MEASURED 2026-08-29` justification; `164.3.1-09-REDERIVATION.md` records the derivation. No movement — and the number is now bounded from three sides (floor, the independent lane tally, and the new `WAIVED_CEILING = 0`). |
| SC-7 | Every gate in the family PRINTS ITS EVIDENCE on failure, demonstrated by driving each to its failure path; an arm asserts this | ✓ VERIFIED | I drove VAC-04 to **five distinct** failure paths and read the output each time — every one leads with a named evidence block (`WHAT THE TWO READERS SAW (evidence first — D-12/SC-7)`, `WHAT WAS READ`) carrying counts, thresholds, the names actually read, and the file:line of the offending token. The mutation runner's 15 self-test modes each print what they saw (e.g. the absurdity defect prints `executed=1 lane-invocations=0 biting=0` and states "this is the GATE failing, not the corpus"). VAC-08's self-test drives all four RED modes. The enforcing arm is exact-set in both directions: `META diagnostic-first: 75 emission(s) over 2 gate(s), 17 bare, 17 allowlisted`. |
| SC-8 | The absurdity floor exists on VAC-04 and on the mutation runner as well as VAC-08, each proven able to fire AND proven silent on a legitimate finding | ✓ VERIFIED | **VAC-04** — FIRE: 1-name index vs the 118-body snapshot → exit 1 printing the population, the threshold (`at least half … 59 name(s)`) and the names read. SILENT: 130-name index → floor quiet, run proceeds. Both measured by me. **Mutation runner** — FIRE: self-test 14/15 severs the tally in-process (`executed=1 lane-invocations=0`) while `arms:`/`biting:` still print healthy. SILENT: my full corpus run, 30 executed / 30 lanes / no defect. **VAC-08** — FIRE: `zero-comparisons RED` and `empty-body-check-list RED` in the 5/5 self-test I ran. SILENT: the real TEST run in CI (`262 migration(s) checked … 4 body comparison(s)`, floors quiet). Every threshold records its measurement and separation. |
| SC-9 | Every gate whose key, threshold or join rests on a measurement carries that measurement's SAMPLE SIZE and COVERAGE beside it, plus an arm asserting the key against the full corpus. Verified by grepping the family and finding no bare ones | ✓ VERIFIED | The enforcing arm reports `META bare-measurement: 8 threshold site(s) over 6 file(s)`, all `ok`, each with its measurement token and date within an 80-line window: `ledger_rows -ge 50`, `SNAPSHOT_MIN=50`, `FILES_FLOOR=1`, `ARMS_FLOOR=30`, `RESULT_LOOP_CONDITION_FLOOR=8`, `CORPUS_FLOOR=100`, `NEEDLE_MIN_LENGTH=16`, `EMISSION_FLOOR=40`. Full-corpus assertions exist for the keys: MW02 over all 12 CI jobs × 8 guard combinations, VAC-08's join over 262 migrations, the SRO scan over 131 files. I grepped the family myself for a bare threshold and found none — `WAIVED_CEILING = 0`, the one site the arm's detector does not enumerate, carries a full measured justification with sample and an independent re-derivation. See observation **O-1** for the detector's naming scope. |

**Score: 9/9 truths verified** (0 present-but-behaviour-unverified; 0 overrides). Every behaviour-dependent criterion — SC-2, SC-3, SC-4, SC-6, SC-7, SC-8 — is backed by a test or gate I executed and read the output of, not by symbol presence.

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `scripts/mutation-runner/parse.mjs` | The single definition of "what is code" | ✓ VERIFIED | `tokenizeStatements` :846 and `maskNonCode` :857 documented as "two views of ONE state machine" (:585); `BRANCH_HEAD_KEYWORDS` :491 is the single exported table. Driven by me on the real corpus file and on E-string / block-closer edge cases. |
| `scripts/mutation-runner/run.mjs` | Neuter path routed through the tokenizer; identity attribution; floors | ✓ VERIFIED | `isBranchHead` :420 now reads `statement.head` (a tokenizer property, not a line regex); `BLOCK_CLOSER_RE` :1347 with `blockCloserKind` :1349; floors :129/:197/:226; 15-mode self-test. |
| `scripts/prod-body-drift-check.sh` | VAC-04 fail-closed, C1–C4, absurdity floor, diagnostic-first | ✓ VERIFIED | Zero-path block :250, tripwire :298, C3 branch :765-800, snapshot walk/count with captured status :604-614, `SNAPSHOT_MIN=50` :596. Seven end-to-end drives by me. |
| `scripts/sql-body-normalize.mjs` / `scripts/sql-function-names-naive.mjs` | Two INDEPENDENT readers, realpath guards, charset refusal | ✓ VERIFIED | `realpathSync(process.argv[1]) === fileURLToPath(import.meta.url)` at :771 and :368 respectively; `charsetRefusal` in the naive reader :132-193. Self-tests 9 and 14 checks. The gate refuses them if they resolve to the same file. |
| `scripts/pg-lane/run.sh` | Throwaway cluster only; loud cleanup | ✓ VERIFIED | PGBIN resolution :64-75 (ubuntu + homebrew); loud `pg_ctl stop` failure with orphan-pid diagnostics :116-126. No orphan postmaster after my two runs. |
| `src/__tests__/gate-family-meta.test.ts` | Instance registry + SC-7/SC-9 meta-arms | ✓ VERIFIED | 15 instances, exact-set both ways, 6 `also:` bindings; both META arms green. |
| `src/__tests__/self-referential-oracle.test.ts` | The primitive-D machine check, BLOCKING | ✓ VERIFIED | 131 files scanned, 2 allowlisted findings, `CORPUS_FLOOR=100` measured. |
| `src/__tests__/audit-coverage.test.ts` | [AUDCOV-01] fix + A/B/C table | ✓ VERIFIED | Cross-line `quoteAtEnd`; three arms with B as the calibration control; 26/26. |
| `.github/workflows/ci.yml` | `sql-mutation` count-recheck with all floors + tally cross-check | ✓ VERIFIED | Now also reads and enforces `WAIVED_CEILING`; mode identity preserved; green at the SHA-bound run. |
| `src/__tests__/helpers/branch-head-bare-code.ts` | ONE bare-code table for both branch-head oracles (IN-04) | ✓ VERIFIED | 7 keyword spellings keyed to `BRANCH_HEAD_KEYWORDS` + 4 `BLOCK_CLOSERS` pinned as non-heads; both oracles assert the key set matches the implementation's export. |

### Key Link Verification

| From | To | Via | Status |
| --- | --- | --- | --- |
| `run.mjs` neuter path | `parse.mjs` tokenizer | `isBranchHead(statement) => statement.head === true` (:420) — no line regex remains on the decision path | ✓ WIRED |
| `run.mjs` verdict loop | `runLane` spawn counter | two independent tallies, cross-checked in-process AND re-asserted in ci.yml | ✓ WIRED (30 = 30 measured twice) |
| `prod-body-drift-check.sh` | both union readers | `NORMALIZER` / `NAIVE_NAMES` (:143-144), with a same-file refusal | ✓ WIRED (I injected both) |
| `prod-body-drift-check.sh` zero path | the textual tripwire | unconditional, comments masked (33b7f447) | ✓ WIRED (both directions driven) |
| `ci.yml` sql-mutation | `run.mjs` floors | `import(...).then(m => console.log(m.FILES_FLOOR, m.ARMS_FLOOR, m.WAIVED_CEILING))` | ✓ WIRED |
| `gate-family-meta.test.ts` registry | the arms themselves | needle resolution in the named file + `also:` gate-level bindings | ✓ WIRED (21 needles, all resolve) |

### Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
| --- | --- | --- | --- |
| Runner's 15 exit-1 modes all fire | `node scripts/mutation-runner/run.mjs --self-test` | 15/15, exit 0 | ✓ PASS |
| Full corpus, real SQL, throwaway cluster | `node scripts/mutation-runner/run.mjs` | 30/30/0, biting 30, lanes 30, no defects | ✓ PASS |
| Seven compound lines classify as statements | `tokenizeStatements` on the real file | all 7 split correctly | ✓ PASS |
| Block closer is not a branch head (CR-01) | tokenizer driver | `END LOOP;` head=false | ✓ PASS |
| E-string backslash escape (WR-01) | tokenizer driver | one literal, correct split after | ✓ PASS |
| VAC-04 C4 charset refusal | real gate, `fúnc_é` migration | exit 1, U+00FA named | ✓ PASS |
| VAC-04 ASCII control | real gate | readers-ran line, no refusal | ✓ PASS |
| VAC-04 legitimate zero | real gate, no-function migration | exit 0, D-13 notice | ✓ PASS |
| VAC-04 comment-masked tripwire | real gate | exit 0, no false fire | ✓ PASS |
| VAC-04 C1 composed blindness | real gate, two blind readers | exit 1 with full evidence | ✓ PASS |
| VAC-04 C2 fixed / pre-fix | real gate via symlink + space | ran / went blind | ✓ PASS (both directions) |
| VAC-04 absurdity floor fire / silent | real gate, 1-name vs 130-name index | exit 1 / quiet | ✓ PASS (both directions) |
| Sibling gate self-tests | 5 scripts | 14 / 9 / 4-rules / 5-of-5 / anchors OK | ✓ PASS |
| All changed test files, 4 serial batches | vitest | 854 passed, 0 failed | ✓ PASS |

### Probe Execution

| Probe | Command | Result | Status |
| --- | --- | --- | --- |
| Mutation runner self-test | `node scripts/mutation-runner/run.mjs --self-test` | 15/15 PASSED | PASS |
| SQL gate lint | `node scripts/lint-sql-gates.mjs --self-test` | 4 rules, red+green each | PASS |
| VAC-08 ledger gate | `bash scripts/test-ledger-drift-check.sh --self-test` | 5/5 arms | PASS |
| Naive name reader | `node scripts/sql-function-names-naive.mjs --self-test` | OK (14 checks) | PASS |
| Body normalizer | `node scripts/sql-body-normalize.mjs --self-test` | OK (9 checks) | PASS |
| Plan anchors | `node scripts/verify-plan-anchors.mjs --pending` | 7 claims, no stale | PASS |

### Requirements Coverage

ROADMAP records `**Requirements**: TBD` for this phase, and no plan declares a `requirements:` ID. Nothing is orphaned: `grep -E "Phase 164.3.1" .planning/REQUIREMENTS.md` maps no requirement ID here. Traceability for this phase runs through the nine Success Criteria, which is what I verified.

### Anti-Patterns and Observations

| ID | File | Severity | Finding |
| --- | --- | --- | --- |
| — | 20 changed source/workflow files | — | **Debt-marker gate CLEAN.** Zero unreferenced `TBD`/`FIXME`/`XXX`. The 4 `PLACEHOLDER` hits in `ci.yml` are pre-existing descriptive prose about placeholder env values, outside this phase's diff hunks. |
| **O-1** | `src/__tests__/gate-family-meta.test.ts:560-570` | ⚠️ WARNING (non-blocking) | **The SC-9 enforcement arm is naming-scoped and cannot see a `CEILING`.** `findThresholdSites` matches only constants named `*FLOOR*`/`*MIN*` (plus shell `-ge/-gt/-lt/-le <2+ digits>`). `WAIVED_CEILING = 0` — a threshold introduced by this phase's own review pass — is therefore absent from the arm's 8 enumerated sites, and a *bare* threshold in the `CEILING`/`MAX`/`LIMIT` spelling would pass the "no bare measurements" arm silently. **Why this is not a gap:** I grepped the family myself; `WAIVED_CEILING` is the only site in that class and it is fully justified inline (`PINNED 2026-09-02 BY MEASUREMENT`, sample recorded, independently re-derived by `mutation-runner-floors.test.ts` on every run). SC-9 as written is met. **Why it still matters:** the phase's thesis is soundness "not by which spelling happens to be in the corpus today", and this is precisely a spelling-scoped check. Recommend widening the regex to `(FLOOR|MIN|CEILING|MAX|LIMIT)` and adding `WAIVED_CEILING=0` to `KNOWN_THRESHOLD_SITES`. Not booked in TODOS.md yet. |
| **O-2** | `TODOS.md`, `164.3.1-UAT.md` | ⚠️ WARNING (mechanical) | **The bookings that close UAT-2's residues are UNCOMMITTED.** `[VAC08-SELFTEST-CI]` and `[VAC04-ARMS-UNRUN]` exist only in the working tree. They are the routing for the two deferred items below; if the working tree is discarded, both residues become unrouted — the exact failure IN-09 had to correct once already in this phase. Orchestrator action: commit them with the verification bundle. |
| **O-3** | `src/__tests__/gate-family-meta.test.ts` | ℹ️ INFO | The three self-test modes added during the review pass ([F1] message-embedded forgery, injected-lane absurdity FIRE, `lane-unrunnable`) are executable arms proven to fire, but they are **not** entries in `PHASE_TAXONOMY` / `INSTANCE_ARM_REGISTRY`, so the exact-set guard does not bind them by name. SC-1 is satisfied (the arms exist and fire, verified 15/15), but a future rename would not fail by name the way the registered 15 do. Worth registering when 164.4 next touches this file. |

### Human Verification Required

**None.** All four items from the `092003fb` report are resolved in `164.3.1-UAT.md` (4/4 pass), and I found no new item requiring human judgment:

- The ubuntu item is not merely closed but **superseded** — I re-bound it to a run at bytes identical to HEAD, showing 15/15 rather than the UAT's stale 12/12.
- The VAC-04 real-credential item is now a bounded, booked **deferred** item, not an open question: the credential window is retired, the three unreached branches are named, and I verified their logic myself against the real gate script.
- The allowlist-reading item was performed and its follow-up is booked at `TODOS.md:2467`.
- The severed-tally item is now pinned in-process as `SELF-TEST 14/15`, which runs on every CI invocation — strictly stronger than the manual harness.

O-1 and O-2 are observable, mechanically resolvable findings, not judgment calls, so they are recorded as warnings rather than routed to a human test.

## HARD DEPENDENCY: is Phase 164.4 safe to start?

**Yes — 164.4 REDUNDER-BACKFILL is safe to start.**

The dependency exists because 164.4 annotates ~70 files, each asserting that a machine proved the arm bites; against unsound primitives those would be 70 false attestations. Each primitive that could produce a false attestation is closed and, critically, closed **by construction** rather than by example:

- **A (would falsely accept a neuter):** the classifier is statement-based over a single tokenizer state machine. I verified it on the exact seven compound lines in the very directory 164.4 backfills, and on the two edge cases the review found (block closers, E-string escapes). A neuter that leaves `SET ROLE postgres;` live is refused **by name**, with an accepted neuter beside it as the control.
- **B (would count an arm as biting without executing it):** identity moved off the wire entirely. Three distinct forgery classes — including one discovered during this very review — are refused, each beside a genuine arm scoring RED in the same run.
- **C (would let a gate report more than it measured):** VAC-04 fails closed on every zero I could construct, and three absurdity floors are proven in both directions. Two independent tallies of arm execution are cross-checked in-process and re-asserted in CI.
- **D (would ship a corpus entry that cannot fail):** the machine check is blocking over 131 files with a measured allowlist, so a self-referential arm added during the backfill reds rather than passing.

Two additions from the review pass make the backfill **safer than it was at the prior verification**:

1. `WAIVED_CEILING = 0`, enforced both in-process and in `ci.yml`. This closes a hole aimed directly at 164.4: a prose marker paired with a waiver twin satisfies parity, raises annotated coverage across all 70 files, spawns no lane, lowers no floor and exits 0. Coverage can no longer be inflated with zero new arms.
2. `lane-unrunnable` (self-test 15/15): a lane that could not be *run* is now named as not-judged, never scored as `wrong-first-failure` and never counted biting. Across 70 newly annotated files, spawn failures are the likely noise, and they will now be loud instead of being absorbed into a tally.

**One standing constraint carries into 164.4, unchanged:** ⛔ do not merge a migration PR between 164.3.1 landing and 164.4 landing. VAC-04's three real-credential branches remain unobserved live (`[VAC04-ARMS-UNRUN]`), and a block at the fail-closed zero path would be the gate working, not a defect. 164.4 touches `supabase/tests/*.sql`, not `supabase/migrations/**`, so it does not conflict with this constraint.

## Gaps Summary

No gaps. All nine ROADMAP Success Criteria are verified against the code as it exists at `e30f7c33`, by measurements I executed in this checkout — not by SUMMARY claims and not by carrying the prior report forward. The ship-review fix pass introduced **no regression**: the two changes with genuine regression potential (`5d8f068f` narrowing the naive reader's refusal, `33b7f447` making the tripwire unconditional) were each tested head-on in both directions and both hold. Two non-blocking warnings are recorded above (O-1, a naming-scoped blind spot in the SC-9 enforcement arm; O-2, uncommitted TODOS bookings), plus one informational note (O-3). Four items are deferred with explicit routing, two of them excluded by the ROADMAP goal itself.

The working tree was left exactly as I found it: `git status --porcelain` reports the same two pre-existing orchestrator edits before and after. Every neuter I performed to prove an arm could fail was done on copies under the scratchpad; the repository was never mutated, and no `supabase` CLI command was run.

---

_Verified: 2026-09-02T16:23:33Z at HEAD `e30f7c33`_
_Verifier: Claude (gsd-verifier) — re-verification #2, adversarial/goal-backward_
