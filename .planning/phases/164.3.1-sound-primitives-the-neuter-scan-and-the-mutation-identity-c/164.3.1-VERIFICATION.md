---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
verified: 2026-09-02T07:30:00Z
verifier: gsd-verifier (independent re-measurement at HEAD a30ce097, branch phase-164.3.1-sound-primitives, merge-base c2251b6d)
status: gaps_found
score: 8/9 success criteria verified
behavior_unverified: 0
overrides_applied: 0

# ─────────────────────────────────────────────────────────────────────────────
# What I RAN MYSELF at HEAD a30ce097 — every line is a command this verifier
# executed in this checkout, serialized (never two gates concurrently), never a
# supabase CLI command, only the pg-lane throwaway cluster written to.
# SUMMARY.md / CORPUS-PROOFS claims are cross-referenced BELOW, not trusted.
# ─────────────────────────────────────────────────────────────────────────────
independent_measurements:
  - command: "node scripts/mutation-runner/run.mjs --self-test"
    result: "exit 0 — SELF-TEST 1/12 … 12/12 all ok, 0 `SELF-TEST FAIL` lines, `SELF-TEST PASSED … the 164.3.1-11 regression corpus holds`"
    proves: "Scenarios 9–12 (the A/B regression corpus: R4-C01 compound head refused as neuter-missed naming SET ROLE postgres;, MUT-I01 P4+P5, R4-C02 current_query() trigger SYNTHESISED with CTRL 1 RED beside it, nested-EXECUTE forgery SYNTHESISED by chain length) execute against real throwaway clusters under my own hand. SC-1 (A+B entries), SC-2, SC-3."
  - command: "node scripts/mutation-runner/run.mjs"
    result: "exit 0 — coverage: files 1/71 · arms: 30/30/0 · biting: 30 · lane-invocations: 30 (plus 1 baseline / 1 restore) · 30× `RED (identity ok)` · No defects; `git status --porcelain` empty afterwards"
    proves: "SC-6 re-derivation reproduced independently: ARMS_FLOOR=30 and FILES_FLOOR=1 hold under the sound primitives; the plan-10 absurdity cross-check is SILENT on the legitimate corpus (executed = lane-invocations = 30)."
  - command: "node_modules/.bin/vitest run src/__tests__/gate-family-meta.test.ts src/__tests__/mutation-runner-floors.test.ts src/__tests__/self-referential-oracle.test.ts"
    result: "3 files / 65 passed (Node 25) AND 65 passed under Node 22.22.1. Printed: `META diagnostic-first: 68 emission(s) over 2 gate(s), 17 bare, 17 allowlisted`; `META bare-measurement: 6 threshold site(s) over 5 file(s)`, all `ok`; `SELF-REF-ORACLE blocking summary: 2 finding(s) in 1 file(s), 131 file(s) scanned`"
    proves: "Instance→arm registry exact-set (15 instances) green; SC-7 and SC-9 meta-arms green with their fixtures; Primitive-D rule is BLOCKING with exactly the 2 measured allowlist entries."
  - command: "node_modules/.bin/vitest run src/__tests__/mutation-runner-neuter.test.ts src/__tests__/sql-statement-tokenizer.test.ts src/__tests__/mutation-annotation-parser.test.ts"
    result: "3 files / 201 passed"
    proves: "Tokenizer span contract, the seven real compound lines (GENERALISED arm over real bytes), P3/P4/P5, sub-line splice, the 5 measured psql shapes incl. the nested-EXECUTE forgery refused by chain length ONLY, MEASURE_FAIL on unreadable grammar, no nonce export."
  - command: "node_modules/.bin/vitest run src/__tests__/drift-check-scripts.test.ts"
    result: "1 file / 280 passed"
    proves: "VAC04-C1 reopen pin (execution + by-name), VAC04-C3 grep-exit-2 MEASURE_FAIL + control, D-13 LEGITIMATE ZERO / CORPUS / CALIBRATION arms, VAC-04 absurdity floor FIRES + 2× SILENT + INERT-announces, VAC08-253 floor RED + 2 controls, VAC08-JOIN driven arm + specificity + symmetry — all driving the REAL gate scripts with injected commands."
  - command: "node_modules/.bin/vitest run src/__tests__/audit-coverage.test.ts src/__tests__/lint-sql-gates.test.ts src/__tests__/vac04-reader-guards.test.ts"
    result: "3 files / 84 passed. Printed: `MW02 result-loop parse: 11 condition(s), 3 job arm(s) [e2e-seeded, sql-tests, plan-anchor-verify], tolerance-bearing: e2e-seeded(is_fork_pr), sql-tests(is_fork_pr+is_dispatch), plan-anchor-verify(is_pr)`"
    proves: "AUDCOV-01 case A/B/C arms; [MUT-W02] structural parse with floor 8 vs measured 11 and the MW02 red/green pair; VAC04-C2 (symlink/space/direct ×2 readers + no-args exit 2) and VAC04-C4 (P10 refusal, qualified mode, leading non-ASCII, `$` exemption, body non-leakage, library throws) at READER level."
  - command: "node scripts/sql-function-names-naive.mjs --self-test ; node scripts/sql-body-normalize.mjs --self-test"
    result: "`self-test OK (13 checks)` ; `self-test OK (9 checks)` — both exit 0"
    proves: "The +1 charset check per reader is present (12→13, 8→9 as claimed)."
  - command: "bash scripts/test-ledger-drift-check.sh --self-test ; node scripts/lint-sql-gates.mjs --self-test ; node scripts/verify-plan-anchors.mjs --pending"
    result: "`self-test OK (5/5 arms)` ; `4 rules, red+green each` ; `OK: 2 plan file(s), no stale claims` — all exit 0"
    proves: "Sibling gates undisturbed; no stale plan anchors remain pending."
  - command: "node .planning/phases/164.3.1-…/164.3.1-03-audcov-extract.mjs"
    result: "`AUDCOV-CAL A=[5] B=[5] C=[3]`, subject sha256 ce51e2fb…"
    proves: "SC-5 stripper half: A finds its site, B control and C fence unchanged, measured with plan 03's unedited instrument against the committed bytes."
  - command: "scratch-copy fire proof (tree untouched): copy audit-coverage.test.ts + the plan-03 instrument into the scratchpad with a node_modules symlink; run; sed `unmatchedBlockOpen(code, quote)`→`(code, null)` in the COPY; run again"
    result: "control `A=[5] B=[5] C=[3]` → neutered `A=[] B=[5] C=[3]`; real tree sha256 ce51e2fb… == `git show HEAD:` blob, porcelain empty"
    proves: "One recursive SC-1 entry (AUDCOV-01) independently observed to RED under the neuter of its fix, exactly as 164.3.1-06-SUMMARY records — without mutating the checkout."
  - command: "git merge-base --is-ancestor 807702ff b7a80598 ; git show 807702ff:src/__tests__/lint-sql-gates.test.ts | sed -n 178,188p"
    result: "ancestor = YES; the pre-fix blob carries `const banner = \"-- RED FIXTURE …\"` at :182 with `expect(banner)` at :183/:184"
    proves: "D-06 / SC-5 calibration ORDERING is a git fact, not a prose claim: the detector commit precedes the site fix and the site was self-referential at that commit (164.3.1-02-CALIBRATION.md § II records the flagging at 807702ff)."
  - command: "grep -an ABSORBABLE_CLEANUP scripts/mutation-runner/run.mjs"
    result: "`/^[ \\t]*RESET[ \\t]+ROLE[ \\t]*;[ \\t]*$/i` — RESET ROLE only"
    proves: "`SET ROLE postgres;` is not absorbable, so a neuter whose abort branch carries it is REFUSED (the mechanism behind SC-2's second clause), not accepted."
  - command: "grep -arn 'SET ROLE postgres;[ \\t]*[A-Za-z]' supabase/tests/ ; sed -n 94,98p supabase/tests/test_profiles_privileged_columns_locked.sql"
    result: "no `SET ROLE postgres; <code>` line exists in the corpus; lines 96/101/…/132 read `EXCEPTION WHEN OTHERS THEN v_raised := true; END;`"
    proves: "Plan 01's correction of the ROADMAP premise is right: the seven real lines are the EXCEPTION-compound P1 shape (pinned by content, ≥7), and the `SET ROLE postgres; IF NOT ok THEN` P3 head shape lives only in the selftest fixture — the class is closed by mechanism, and the corpus-file clause of SC-2 is trivially bounded by today's single annotated file."
  - command: "ci.yml aggregator `needs:` vs result loop (read)"
    result: "12 entries in needs, 12 rows in the loop, identical sets incl. sql-gate-lint / sql-mutation / plan-anchor-verify; branch diff on ci.yml = +37/−3 lines, count-recheck step only"
    proves: "D-11: every arm this phase shipped rides `frontend-test` (vitest shards) or `sql-mutation` (self-test), both in the aggregator's needs AND loop; no new job outside the aggregator."
  - command: "TBD/FIXME/XXX scan over all 27 phase-modified files"
    result: "0 debt markers"
    proves: "Debt-marker gate clean."

gaps:
  - truth: "SC-4 — each of [VAC04-C1]..[VAC04-C4] is driven END-TO-END THROUGH THE REAL GATE and observed to exit non-zero on its own measured input"
    status: partial
    severity: BLOCKER-by-letter (small, ~2 arms)
    reason: >
      The load-bearing half of SC-4 is VERIFIED: the zero path fails closed (REOPEN PIN drives
      scripts/prod-body-drift-check.sh on the P8 composing fixture → evidence block then exit 1),
      and [VAC04-C1] and [VAC04-C3] are each driven through the real gate by arms I ran. But
      [VAC04-C2] (symlinked / space-containing reader path → main() never ran → silent exit 0) and
      [VAC04-C4] (non-ASCII identifier → truncated `f` / dropped) are driven ONLY through the two
      reader CLIs in src/__tests__/vac04-reader-guards.test.ts. MEASURED: `grep -i
      'symlink|fúnc|U+00FA|VAC04-C2|VAC04-C4' src/__tests__/drift-check-scripts.test.ts` returns
      no test arm (the single 'charset' hit at :1927 is about migration FILENAMES). The gate-level
      propagation rests on a CODE-READ of the `|| fail` wrappers (164.3.1-04-SUMMARY § Propagation
      note), and plan 04's own must-have truth 7 fenced this out as "reader-side ONLY" without plan
      07 picking it up — a PLAN-level scope reduction of a ROADMAP success criterion, which is the
      exact shape this phase exists to stop ("testing the helper, not the wiring", RESEARCH Pitfall 6).
      Note for the fixer: since D-13, a C2-style silent-zero on a real function is ALSO caught by
      C1's textual tripwire — so a gate-level C2 arm must isolate the guard fix (symlinked reader +
      a definition both readers CAN see → expect `Functions defined or replaced by this PR: 1`, not
      the tripwire refusal), or it proves the wrong control.
    artifacts:
      - path: "src/__tests__/drift-check-scripts.test.ts"
        issue: "no arm drives scripts/prod-body-drift-check.sh with NORMALIZER/NAIVE_NAMES resolved through a symlinked or space-containing path ([VAC04-C2]); no arm drives it with a changed migration defining `public.fúnc_é(...)` ([VAC04-C4])"
      - path: "src/__tests__/gate-family-meta.test.ts"
        issue: "registry binds VAC04-C2 and VAC04-C4 to vac04-reader-guards.test.ts (reader-level arms) — correct as a binding, but the registry's citation is the only place the reader-vs-gate distinction is visible"
    missing:
      - "Gate-level [VAC04-C2] arm: scaffold the gate with `NORMALIZER`/`NAIVE_NAMES` (env-injectable, prod-body-drift-check.sh:143-144) pointing at a SYMLINKED copy path and at a path containing a space; drive a migration with a visible definition; assert the gate reaches `Functions defined or replaced by this PR: 1` (readers ran) — and a calibration leg that the same wiring with the pre-fix guard idiom (a scratch reader copy carrying `import.meta.url === \\`file://${process.argv[1]}\\``) produces zero names, so the arm is proven able to fail"
      - "Gate-level [VAC04-C4] arm: drive the real gate on a changed migration containing `CREATE OR REPLACE FUNCTION public.fúnc_é(p uuid)`; assert exit 1 AND that the output names `U+00FA` / `leaves the unquoted charset` (diagnostic-first) AND contains no body text; a neuter (reader refusal disabled in a scratch copy) must flip it to the truncated-name path"
      - "Update the VAC04-C2 / VAC04-C4 entries in INSTANCE_ARM_REGISTRY (`also:`) to bind the new gate-level arms by name, keeping the reader-level bindings"

deferred:
  - truth: "[MUT-I02] header denominator (114 vs 262) and [MUT-I03] concurrent-agent dirty-checkout note"
    addressed_in: "TODOS.md (explicitly EXCLUDED by the ROADMAP goal text: 'EXCLUDES only two prose items, which stay in TODOS.md')"
    evidence: "TODOS.md:5131-5132 carry both items open; ROADMAP 164.3.1 goal names them as out of scope"
  - truth: "FILES_FLOOR raised from 1 as the other 70 gate files are annotated"
    addressed_in: "Phase 164.4"
    evidence: "run.mjs:128 sentence 'Phase 164.4 raises FILES_FLOOR as it backfills the other 70 files.' (verbatim, keyed on by plan 07's arm); ROADMAP Phase 164.4 REDUNDER-BACKFILL"

coincidental_reliance_items: []

human_verification:
  - test: "First real `sql-mutation` run on its ubuntu host: read the job log for `SELF-TEST 12/12`, the corpus summary lines (`coverage:` / `arms:` / `biting:` / `lane-invocations:`), and the count-recheck step's closing sentence"
    expected: "12/12 self-test PASSED; `arms: 30/30/0`, `biting: 30`, `lane-invocations: 30`; the count-recheck echoes 'both floors hold and the runner's two tallies agree'. If psql's verbose grammar differs on ubuntu, the runner must emit a named MEASURE_FAIL — never a quiet pass"
    why_human: "Every lane in this phase (and in my verification) ran on macOS / PostgreSQL 16.13. .planning/WINDOWS.md 28: the job has never executed on ubuntu. Nothing in the repo can observe that host — the plans themselves mark this human_judgment (05-D6, 10-D4, 11-D6)"
  - test: "First live `migration-drift-check.yml` run (VAC-04 with the real PROD credential) and first `sql-tests` run with `TEST_SUPABASE_DB_URL` (VAC-08) on this branch or its successor"
    expected: "VAC-04: on a migration PR defining no function, the D-13 `::notice:: … the zero is LEGITIMATE` path; on a PR with a visible definition, the normal compare verdict; the absurdity floor SILENT (index ≥ half the 118-body snapshot). VAC-08: `self-test OK (5/5)` step, then a ledger verdict with the shape diagnostic, floor silent on the real TEST ledger"
    why_human: "Both gates are proven only against injected commands (WINDOWS.md 25/26); the real credential path has never been observed. ⛔ Standing ordering constraint: do not merge a migration PR between 164.3.1 landing and 164.4 landing — a block at the fail-closed zero path is the gate working"
  - test: "Read the 17 DIAGNOSTIC_FIRST_ALLOWLIST reasons (gate-family-meta.test.ts:621-716, classes PRECONDITION / REDACTED / EMPTY-BY-COND / READER-STDERR) and the 2 SRO_ALLOWLIST entries (self-referential-oracle.test.ts:171-196, types-design-tests.test.ts:27/:63)"
    expected: "Each reason is TRUE of its site, not a plausible sentence; the READER-STDERR/REDACTED entries are accepted only because their follow-up (capture the child's exit code with the grep_rc idiom) is booked — see Warnings"
    why_human: "The machine asserts a reason STRING exists per entry (exact set, both directions); whether the reason is legitimate is a reading judgment. Plans 08 (D6) and 12 (D7) mark exactly this human_judgment"
  - test: "(Optional, repeatable) Re-run plan 10's severed-tally harness: byte-backup run.mjs, change `laneTally[leg] += 1` → `if (leg !== \"arm\") laneTally[leg] += 1`, run the full corpus, restore from the backup, confirm sha256 75fa5442-equivalent at current HEAD"
    expected: "exit 1 with an `absurdity` defect `executed=30 lane-invocations=0 biting=30` while `arms:`/`biting:` still print healthy; restore → exit 0"
    why_human: "The FIRE direction on the REAL wiring is a recorded harness (164.3.1-10-SUMMARY § Task 2, sha pairs). The standing pins cover the pure `absurdityViolations` function and the CI step's extracted `run:` block, and my own runs confirm the SILENT direction on real wiring; I did not mutate the tree to re-observe the fire"
---

# Phase 164.3.1: SOUND-PRIMITIVES Verification Report

**Phase Goal:** All FOUR cycling control primitives closed BY CONSTRUCTION — A (statement-level SQL tokenizer, one definition of "code"), B (arm identity moved to source-location attribution where executing SQL cannot observe it), C (a gate's verdict bounded by what it measured: VAC-04 fail-closed zero path + realpath guards + grep exit branching + charset refusal; absurdity floors on VAC-04 and the runner), D (machine check for self-referential oracles; [AUDCOV-01] fixed with the A/B/C table; [MUT-W02] structural). Plus the regression corpus (6), ARMS_FLOOR re-derived (7), diagnostic-first (8), and the sample-size/coverage RULE (9). HARD DEPENDENCY for 164.4.
**Verified:** 2026-09-02T07:30:00Z at HEAD `a30ce097` (81 commits over merge-base `c2251b6d`)
**Status:** gaps_found — 8/9 success criteria verified; SC-4 PARTIAL (two of four members not driven through the real gate)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (the ROADMAP's SC-1..SC-9 — the contract; PLAN must-haves merged underneath)

| # | Truth | Status | Evidence (mine unless marked "record") |
| --- | --- | --- | --- |
| SC-1 | Every measured instance across all four primitives is an EXECUTABLE arm in the regression corpus, each proven to RED by neutering its fix | ✓ VERIFIED | Registry exact-set (15 instances A/B/C/D) green in my run; every bound arm executed green across my 630 vitest tests + 12/12 self-test; VAC-08 253-of-262 and three-convention-join arms present and driving the real script. Recursive half: one entry (AUDCOV-01) independently reproduced RED on a scratch copy (`A=[]` under the carry-neuter, `A=[5]` restored); the other cycles are in 164.3.1-11/12-CORPUS-PROOFS.md with verbatim REDs, RED-set specificity, and sha256 pairs (record). Registry fire-proof both directions (record §5a/5b). |
| SC-2 | Neuter classifier is statement-based; the seven compound lines classify correctly; no accepted neuter leaves `SET ROLE postgres;` live on any corpus file | ✓ VERIFIED | `tokenizeStatements` exported (parse.mjs:822) and consumed at run.mjs:99/491/853/1197; `inQuote`/`statementEndLine`/`IGNORABLE_LINE` gone from the neuter path; GENERALISED arm over the real bytes of lines 96/101/106/111/116/121/132 green; self-test 9/12 refuses the P3 head as `neuter-missed` naming `SET ROLE postgres;` beside an ACCEPTED P1 neuter; `ABSORBABLE_CLEANUP` = RESET ROLE only. Corpus: 104 identities / 103 scans / 4 standing refusals / 0 added (REAL CORPUS arms green). |
| SC-3 | Arm identity cannot be produced by any SQL the gate executes — R4-C02 `current_query()` trigger observed NO-IDENTITY with a passing control | ✓ VERIFIED | Self-test 11/12 under my run: FORGE 1 (the R4-C02 trigger) SYNTHESISED, CTRL 1 RED (identity ok) in the same lane run; 12/12: nested-EXECUTE forgery aimed at the genuine line refused by chain LENGTH alone. Nonce exports absent (`makeIdentityNonce`/`stampIdentities`/… not in run.mjs); pg-lane `-v VERBOSITY=verbose` on every leg (run.sh:153); legs (a)(b)(c) at run.mjs:1063-1151. Reachability proof recorded with constructs covered: GRAMMAR.md § 3c + authoring rule (:205-300). |
| SC-4 | VAC-04 cannot exit 0 having compared nothing; zero path fails closed; **each of [VAC04-C1]..[C4] driven end-to-end through the real gate** | ⚠️ PARTIAL → gap | Fail-closed zero path: VERIFIED (marker `VAC04-ZERO-PATH-FAILS-CLOSED` at :248, evidence block :379-407, D-13 tripwire :296-355 with `grep_rc`; REOPEN PIN + LEGITIMATE ZERO + CORPUS arms green, all driving the real script). C1 ✓ gate-driven, C3 ✓ gate-driven (grep exit 2 → MEASURE_FAIL). **C2 and C4: reader-level only** — realpath guard (naive.mjs:337-346, normalize.mjs) and charset refusal proven by 15 arms on the reader CLIs; no arm drives the gate itself through a symlinked reader path or a `fúnc_é` migration (grep of drift-check-scripts.test.ts: none). See Gaps. |
| SC-5 | `stripBlockComments` finds case A while B still passes; the machine check flagged `lint-sql-gates.test.ts:182-186` at HEAD before it was fixed | ✓ VERIFIED | Plan-03 instrument, unedited, prints `A=[5] B=[5] C=[3]` against `ce51e2fb…`; before-state `A=[] B=[5] C=[3]` bound to blob `dff9d1a1` in 164.3.1-03-CALIBRATION-AUDCOV.md. Calibration ordering is a git fact: detector `807702ff` is an ancestor of fix `b7a80598`, and the `807702ff` blob has `const banner` at :182 with `expect(banner)` at :183/:184 — the CALIBRATION artifact records the rule flagging exactly those two lines at that HEAD. Rule now BLOCKING, 2 findings / 131 files under my run; SRO-01/SRO-02 red+green pairs green. |
| SC-6 | ARMS_FLOOR and biting re-derived under the sound primitives; movement recorded | ✓ VERIFIED | My full corpus run: `30/30/0 · biting 30 · lane-invocations 30 · exit 0`, 30× `RED (identity ok)`. Matches 164.3.1-09-REDERIVATION.md (per-arm table, 0/30 moved, gate blob `5ae6855f` byte-identical at base and HEAD, `git log base..HEAD -- test_strategy_shares_rls.sql` empty). `ARMS_FLOOR = 30` with `RE-DERIVED 2026-09-01` two lines above it (run.mjs:195-197); floors test untouched per lockstep. |
| SC-7 | Every gate in the family PRINTS ITS EVIDENCE on failure; an arm asserts it | ✓ VERIFIED | `META diagnostic-first: 68 emission(s) over 2 gate(s), 17 bare, 17 allowlisted` (exact set, red/green fixtures) green in my run; VAC-04 zero path prints both readers' outputs + changed files before refusing; VAC-04 floor prints both counts + sample; runner absurdity prints `executed= lane-invocations= biting=`; attribution refusals print `WHAT WAS SEEN` / `EXPECTED … (statement A-B, so psql prefix :B and CONTEXT line N)` (run.mjs:74, 129-132; record §4 of ATTRIBUTION.md); reader refusals print prefix + byte + `U+XXXX` + file:line, no body text (non-leakage arm green). Note: the meta-arm scans SHELL gates only (`FAMILY_SHELL_GATES` = 2 files); the runner's evidence-first shape is pinned by its own tests, not by the meta-arm. |
| SC-8 | Absurdity floor on VAC-04 AND the mutation runner (and VAC-08), each proven to fire AND silent; every threshold records its measurement + separation | ✓ VERIFIED | VAC-04 floor (`SNAPSHOT_MIN=50`, ratio ≥ half the snapshot, :498-590): FIRES (3-name index) / SILENT (118-name index, pass) / SILENT (genuine drift) / INERT-announced — all four arms drive the real script, green. VAC-08 floor (`ledger_rows -ge 50 && matched*2 < repo`): RED arm + 2 controls green; disabled-floor neuter recorded (CORPUS-PROOFS-12 §4). Runner: `absurdityViolations` exact relation (run.mjs:1400-1425) wired at :1901 into the defect table; both-direction pure pins + CI extract-and-run pins green; SILENT on real wiring in my corpus run; FIRE on real wiring is a recorded severed-tally harness (see Human #4). Separation stated beside each rule (VAC-04: 0 vs 118-122 around 59; runner: exact — one lane fires). |
| SC-9 | Every gate whose key/threshold/join rests on a measurement carries SAMPLE SIZE + COVERAGE beside it, plus an arm asserting the key against the full corpus; verified by grepping the family and finding no bare ones | ✓ VERIFIED | `META bare-measurement: 6 threshold site(s) over 5 file(s)`, all `ok` (token + date within 80 lines), exact against `KNOWN_THRESHOLD_SITES`; red fixture (`ROWS_FLOOR=7` / `-ge 25`) fires. Full-corpus key arms: VAC08-JOIN reads the four clauses from the script and drives one row per convention; VAC-04 corpus-parity arm over all 380 `.sql` files (readers agree, refusal silent); `RESULT_LOOP_CONDITION_FLOOR=8` vs measured 11; `CORPUS_FLOOR=100` vs 131; `EMISSION_FLOOR=40` vs measured 68 (dated 2026-09-02 at 03585b88, gate-family-meta.test.ts:717-725). Minor: gate-family-meta.test.ts is not in its own `THRESHOLD_BEARING_FILES`, so its `EMISSION_FLOOR` is justified in prose but not machine-audited (Warning W3). |

**Score:** 8/9 truths verified (0 present-but-behavior-unverified; 1 partial → gap)

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | [MUT-I02] naive-reader header denominator 114 vs 262; [MUT-I03] concurrent-agent dirty-checkout note | TODOS.md (excluded by the ROADMAP goal) | TODOS.md:5131-5132 open; ROADMAP: "EXCLUDES only two prose items" |
| 2 | `FILES_FLOOR` raised as the other 70 gate files get annotations | Phase 164.4 | run.mjs:128 sentence verbatim; ROADMAP 164.4 REDUNDER-BACKFILL |

### Required Artifacts (all 12 plans' `artifacts`, three levels + data flow)

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `scripts/mutation-runner/parse.mjs` (857 lines) | `tokenizeStatements`, `maskNonCode`, `BRANCH_HEAD_KEYWORDS` | ✓ VERIFIED | exports at :504/:822/:833; imported and used by run.mjs (4 call sites) and the tokenizer test |
| `scripts/mutation-runner/run.mjs` (2469 lines) | statement-routed neuter path; `attributeIdentities`; `absurdityViolations`; `laneTally`; 12 self-test scenarios; nonce helpers removed | ✓ VERIFIED | all present; `SELF-TEST 1/12…12/12`; no nonce export; `ARMS_FLOOR=30`/`FILES_FLOOR=1` with measured comments |
| `scripts/pg-lane/run.sh` | `VERBOSITY=verbose` on every leg | ✓ VERIFIED | :153 `psqlq()` |
| `scripts/mutation-runner/GRAMMAR.md` | § 3c source-location + 164.4 authoring rule | ✓ VERIFIED | :205 rule, :225 table, :251 nonce lineage, :273 authoring rule |
| `scripts/mutation-runner/fixtures/selftest/{compound-head,comment-parity,current-query-forge,nested-execute-forge}-gate.sql` | 4 lane-driven corpus fixtures | ✓ VERIFIED | 171/174/87/142 lines; driven by scenarios 9-12 in my self-test run; outside the `supabase/tests` scan (corpus still 1/71) |
| `src/__tests__/mutation-runner-neuter.test.ts`, `sql-statement-tokenizer.test.ts`, `mutation-annotation-parser.test.ts`, `mutation-runner-floors.test.ts` | pins for A, span contract, B parser, floors + absurdity + CI extract-and-run | ✓ VERIFIED | 201 + (floors within 65) passed, Node 25 and 22 |
| `src/__tests__/self-referential-oracle.test.ts` + `scripts/self-referential-oracle-fixtures/SRO-0{1,2}-*.{red,green}.ts` | BLOCKING AST rule, exact-set `SRO_ALLOWLIST` with per-entry `measured:` | ✓ VERIFIED | :171-196 two entries, each with justification + measurement citation; 131 files scanned; fixtures 24/27/42/29 lines |
| `scripts/sql-function-names-naive.mjs`, `scripts/sql-body-normalize.mjs` | realpath guard (duplicated per member), charset refusal, self-tests 13/9 | ✓ VERIFIED | `invokedDirectly()` realpath at naive:337-346; `realpathSync` imported in normalize:54; `charsetRefusal` + `U+` codepoint; my self-test runs 13/9 |
| `src/__tests__/vac04-reader-guards.test.ts` (327 lines) | 15 arms symlink/space/direct + charset | ✓ VERIFIED (reader-level) | green; ⚠️ does not reach the gate script — see gap |
| `scripts/prod-body-drift-check.sh` (843 lines) | grep exit branching, fail-closed zero path + marker, D-13 tripwire, absurdity floor, ordering constraint | ✓ VERIFIED | `VAC04-ZERO-PATH-FAILS-CLOSED` :248; `VAC04-ZERO-PATH-TRIPWIRE` :296; `DO NOT "IMPROVE" THIS INTO A PARSER` :298; `164.3.1 AND Phase 164.4 have both landed` :407; `ABSURDITY FLOOR … MEASURED 2026-09-01 at 15ab417b` :498-499; `SNAPSHOT_MIN=50` :547; old fail-open sentence gone |
| `src/__tests__/drift-check-scripts.test.ts` (2564 lines) | C1/C3 arms, reopen pins, D-13 arms, VAC-04 floor arms, VAC08-JOIN + 253 arms | ✓ VERIFIED | 280 passed (SUMMARY 12 says 279 — count drift only, no commit touched the file after 5aa5038b) |
| `src/__tests__/audit-coverage.test.ts` | cross-line quote state; A/B/C arms | ✓ VERIFIED | `unmatchedBlockOpen(code, quoteIn): {open, quoteAtEnd}` :194-217; carry forced null for `'`/`"` :245-252; arms :1306/:1316/:1325; H-0001 allowlist still exactly 6 |
| `src/__tests__/lint-sql-gates.test.ts` | :182-186 fixed; `extractResultLoopConditions`; `RESULT_LOOP_CONDITION_FLOOR=8`; MW02 arms | ✓ VERIFIED | fix comment block + real-bytes degradation; parser :395; floor :473-480; MW02 arms :659/:689 |
| `scripts/aggregator-tolerance-fixtures/MW02-{alternate-spelling.red,current-spelling.green}.yml` | red carries `[[ $name == 'sql-mutation' ]]`; green mirrors ci.yml | ✓ VERIFIED | 66/80 lines; red-blind-to-old-regex arm + fidelity arm green |
| `.github/workflows/ci.yml` | count-recheck step parses `lane-invocations:`; aggregator untouched | ✓ VERIFIED | branch diff +37/−3 in the sql-mutation step only; needs (12) == loop (12) |
| `src/__tests__/gate-family-meta.test.ts` (819 lines) | taxonomy (15) + registry + 2 meta-arms + 4 embedded fixtures | ✓ VERIFIED | 21 tests green; every `armNeedle` resolves in its file's bytes (the arm asserts it) |
| `164.3.1-02-CALIBRATION.md`, `-03-CALIBRATION-AUDCOV.md`, `-03-audcov-extract.mjs`, `-05-ATTRIBUTION.md`, `-09-REDERIVATION.md`, `-11-CORPUS-PROOFS.md`, `-12-CORPUS-PROOFS.md` | committed measurement/proof records with commands, verbatim output, sha pairs | ✓ VERIFIED | all present; numbers cited in them (104/103/4, 30/30/30, 380/359/122/118, 23→4→2, A/B/C, 68/17, 6/6, 11/8) match the constants and printed outputs in code and in my runs |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | --- | --- | ------ | ------- |
| run.mjs | parse.mjs | `import { tokenizeStatements }` | ✓ WIRED | :99 import; used :491 (neuterArm), :853 (gateAttributionRecords), :1197 (failureBranches) |
| run.mjs | pg-lane/run.sh | verbose grammar (P0001 / CONTEXT / LOCATION sentinel) | ✓ WIRED | run.sh:153 flag; run.mjs:118 asserts the LOCATION sentinel bounds the chain |
| mutation-annotation-parser.test.ts | run.mjs | `attributeIdentities` driven with measured PG 16.13 bytes | ✓ WIRED | 201 passed incl. "ONLY the chain rule refuses" |
| mutation-runner-neuter.test.ts | test_profiles_privileged_columns_locked.sql | `readFileSync` of the seven real lines by content | ✓ WIRED | lines 96…132 exist verbatim; GENERALISED arm green |
| reader exit ≠ 0 / silent 0 | prod-body-drift-check.sh | `\|\| fail` wrappers + D-13 tripwire | ⚠️ PARTIAL | wrappers present (:207/:221/:428/:430 continue to `\|\| fail`); no gate-level arm exercises them for C2/C4 — the gap |
| self-referential-oracle.test.ts | scripts/self-referential-oracle-fixtures/ | node:fs, fixture-ID set pinned | ✓ WIRED | "stray files" arm + red/green arms green |
| lint-sql-gates.test.ts | ci.yml frontend result loop | literal markers → `extractResultLoopConditions` | ✓ WIRED | parse prints 11 conditions / 3 arms; population arm agrees with `AGGREGATED_JOBS` |
| gate-family-meta registry | 8 target files | `armNeedle` substring over file bytes | ✓ WIRED | binds-by-name arms green for all 15 |
| sql-mutation CI step | run.mjs output | `lane-invocations:` line parsed, MEASURE_FAIL on absence | ✓ WIRED | ci.yml:1142-1153; floors-test extract-and-run of the real `run:` block green |
| every phase arm | `frontend` aggregator | vitest shards (`frontend-test`) or `sql-mutation` self-test | ✓ WIRED | both jobs in needs AND loop |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| run.mjs `biting` / `armsExecuted` / `laneInvocations` | verdict loop + `laneTally.arm` at the spawn | real pg-lane runs | Yes — 30/30/30 in my run | ✓ FLOWING |
| run.mjs `attributeIdentities` | psql stderr blocks | real lane output | Yes — 30 attributions + 3 forgeries refused | ✓ FLOWING |
| prod-body-drift-check.sh `NAME_COUNT` / `SNAPSHOT_BODY_COUNT` | both readers + `ls` of the snapshot dir | injected in tests; real files in the corpus-parity arm | Yes | ✓ FLOWING |
| gate-family-meta `sites` / `emissions` | `findThresholdSites` / `findFailureEmissions` over real file bytes | node:fs | Yes — 6 / 68 | ✓ FLOWING |
| VAC08-JOIN rows | FIXED `JOIN_CONVENTIONS` table, predicate from the script bytes | node:fs + injected `LEDGER_QUERY_CMD` | Yes (first draft that derived rows from the script was caught and fixed — record §2a) | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Corpus A/B entries red-able and green | `run.mjs --self-test` | 12/12 PASSED, exit 0 | ✓ PASS |
| Floors hold under sound primitives | `run.mjs` (full corpus, alone) | 30/30/0 · biting 30 · lanes 30 · exit 0 | ✓ PASS |
| Registry + meta-arms + SRO blocking | vitest (3 files) | 65 passed (Node 25 + Node 22) | ✓ PASS |
| A tokenizer / B parser pins | vitest (3 files) | 201 passed | ✓ PASS |
| C arms through real gate scripts | vitest drift-check-scripts | 280 passed | ✓ PASS |
| D arms + readers | vitest (3 files) | 84 passed | ✓ PASS |
| AUDCOV movement with unedited instrument | plan-03 extract | `A=[5] B=[5] C=[3]` | ✓ PASS |
| AUDCOV entry REDs under neuter (scratch copy) | sed on a COPY + extract | `A=[] B=[5] C=[3]`, tree untouched | ✓ PASS |
| C2/C4 through the REAL gate | (no arm exists) | — | ✗ FAIL (gap) |

### Probe Execution

No `scripts/*/tests/probe-*.sh` exist; this family's probes are the script self-tests, all run above: `run.mjs --self-test` PASS · `sql-function-names-naive.mjs --self-test` PASS (13) · `sql-body-normalize.mjs --self-test` PASS (9) · `test-ledger-drift-check.sh --self-test` PASS (5/5) · `lint-sql-gates.mjs --self-test` PASS (4 rules) · `verify-plan-anchors.mjs --pending` PASS (0 stale).

### Requirements Coverage

No REQUIREMENTS.md rows map to this phase (`Requirements: TBD`; `requirements.mark-complete SC-*` → not_found is expected). Cross-reference of every SC id claimed in PLAN frontmatter against the ROADMAP list and the SUMMARYs' `requirements-completed`:

| Criterion | Claimed by plans | Completed per SUMMARY | Status | Evidence |
| --------- | ---------------- | --------------------- | ------ | -------- |
| SC-1 | 11, 12 | 11, 12 | ✓ SATISFIED | above |
| SC-2 | 01 | 01 | ✓ SATISFIED | above |
| SC-3 | 05 | 05 | ✓ SATISFIED | above |
| SC-4 | 04, 07 | 07 (04: `[]`, superseded PARTIAL note) | ⚠️ PARTIAL | C2/C4 not gate-driven |
| SC-5 | 02, 03, 06, 08 | 02, 03, 06, 08 | ✓ SATISFIED | above |
| SC-6 | 09 | 09 | ✓ SATISFIED | above |
| SC-7 | 04, 05, 07, 10, 12 | 05, 07, 10, 12 | ✓ SATISFIED | above |
| SC-8 | 07, 10 | 07, 10 | ✓ SATISFIED | above |
| SC-9 | 07, 08, 09, 10, 12 | 07, 08, 09, 10, 12 | ✓ SATISFIED | above |

No orphaned criterion; every SC-1..9 is claimed by ≥1 plan and every plan's claim resolves to a completed SUMMARY.

### CONTEXT decisions honoured (D-01..D-13)

D-01/D-02 source-location attribution with control in the same run ✓ · D-03 hand-rolled tokenizer, no dependency ✓ (`tech-stack.added: []` everywhere; `git diff` shows no package.json change) · D-04/D-05/D-06 AST rule, RED-by-default with measured allowlist, calibrated at HEAD before the fix ✓ · D-07 zero path fails closed IN this phase ✓ · D-08 standing reopen pin (two independent directions) ✓ · D-09/D-10 floors on VAC-04 + runner, thresholds by measurement with stated separation ✓ · D-11 corpus in the aggregator's needs AND loop ✓ · D-12 diagnostic-first ✓ · D-13 legitimate-vs-blind zero tripwire, measured 108/262 pass vs 3/262 block, 7→3 correction recorded ✓.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| (27 phase-modified files) | — | TBD / FIXME / XXX | none found | — |
| `src/__tests__/audit-coverage.test.ts` | 470, 1035, 1079 | `TODO` | ℹ️ Info | fixture strings and a doc comment for the "TODO-comment is not coverage" detector — test inputs, not debt |

### Human Verification Required

See the `human_verification` frontmatter (4 items): first ubuntu `sql-mutation` run; first real-credential VAC-04 / VAC-08 runs (with the ⛔ migration-PR ordering constraint); a reading of the 17 + 2 allowlist justifications; and the optional re-run of the runner's severed-tally fire harness.

### Warnings (non-blocking — route, do not absorb)

- **W1 — Two plan-12 follow-ups are recorded nowhere but in 164.3.1-12-SUMMARY.md.** (a) `scripts/test-ledger-drift-check.sh:44-47` "BOTH clauses are required; neither is redundant" is stale now that four clauses exist and clause 2 is measured subsumed by clause 4 (CORPUS-PROOFS-12 §3). (b) Seven `|| fail` sites (`prod-body-drift-check.sh` :208/:222/:429/:431/:470/:473, `test-ledger-drift-check.sh` :302) print no child exit code and are allowlisted under READER-STDERR/REDACTED in `DIAGNOSTIC_FIRST_ALLOWLIST` with "a named follow-up". `gsd windows append` refused (ledger count mismatch — known, not re-reported) and TODOS.md carries neither (grep: none). Per the parent phase's G2 precedent, a deferral routed to nothing is a bookkeeping gap: add both to TODOS.md.
- **W2 — SUMMARY test-count drift.** `drift-check-scripts.test.ts` is 280 tests at HEAD; SUMMARY 07 says 273 (pre-D-13), SUMMARY 12 says 279. No commit touched the file after `5aa5038b`; the numbers are stale prose, the file is green.
- **W3 — The SC-9 meta-arm does not scan its own file.** `THRESHOLD_BEARING_FILES` = 2 shell gates + run.mjs + lint-sql-gates.test.ts + self-referential-oracle.test.ts; `gate-family-meta.test.ts`'s own `EMISSION_FLOOR=40` (measured 68, dated) and `NEEDLE_MIN_LENGTH=16` (unmeasured sanity constant) are outside the machine audit. Substance holds (EMISSION_FLOOR carries its measurement); the self-exclusion is worth one line when the list is next touched.
- **W4 — SC-7's meta-arm covers shell gates only.** `FAMILY_SHELL_GATES` has 2 entries; the runner's evidence-first output is pinned by its own tests, not by the family scan. A future JS gate in the family would not be caught by the meta-arm.

### Gaps Summary

The phase's four primitives are closed by the mechanisms the ROADMAP asked for, and I reproduced the load-bearing measurements myself: the statement tokenizer is the single definition of code in the neuter path and refuses the compound-head shape by name; the identity nonce is gone and the `current_query()` trigger scores SYNTHESISED beside a RED control on a real cluster; VAC-04's zero path refuses with evidence first and now separates a legitimate zero from a blind one; the Primitive-D rule blocks with a two-entry measured allowlist; ARMS_FLOOR=30 re-earns itself at 30/30/30; and 15 measured instances are bound to executable arms by an exact-set registry with two meta-arms enforcing diagnostic-first and no-bare-thresholds.

**One criterion is met by scope rather than by mechanism, which is this phase's own named disease:** SC-4 requires each of [VAC04-C1]..[C4] "driven end-to-end through the real gate". C1 and C3 are; C2 (realpath guard) and C4 (charset refusal) are proven on the reader CLIs and the gate-level propagation is a code-read of `|| fail` wrappers. Plan 04's must-have truth 7 fenced this out as "reader-side ONLY" and no later plan picked it up — a PLAN reducing a ROADMAP success criterion. The fix is two arms in `drift-check-scripts.test.ts` (the gate's reader paths are env-injectable at :143-144), each with a neuter leg, and a registry `also:` binding. Until then, 164.4's ~70 attestations would rest on a gate whose two reader hardenings have never been observed to reach the verdict.

---

_Verified: 2026-09-02T07:30:00Z_
_Verifier: Claude (gsd-verifier)_
