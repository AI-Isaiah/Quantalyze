---
phase: 164.3.1-sound-primitives
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs
  - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-CALIBRATION-AUDCOV.md
autonomous: true
requirements: [SC-5]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 2
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "Running the committed harness against the pre-fix bytes of src/__tests__/audit-coverage.test.ts prints case A = [] (the [AUDCOV-01] blindness), control B = [5], and shipped arm C = [3] — measured through functions EXTRACTED from the file's own bytes at runtime, never a retyped approximation (SC-5's 'before' leg)."
    - "The calibration artifact carries the exact command and the verbatim harness output, plus the branch HEAD, the subject file's git blob hash, and its SHA-256 — so plan 164.3.1-06's post-fix table shows movement against a recorded, reproducible before-state instead of a reconstructed claim."
    - "src/__tests__/audit-coverage.test.ts is byte-identical to its committed blob at measurement time — no fix, no widening of unmatchedBlockOpen, no quote-carry. The fix belongs to plan 164.3.1-06."
    - "The harness fails loud — exit 1 naming the missing declaration — if any of the six extracted declarations cannot be located; it can never silently measure the wrong subject."
    - "The harness is verdict-free: it prints what it measured and a machine-greppable summary line, with no baked-in expected values, so plan 164.3.1-06 can re-run the SAME instrument post-fix (when A moves to [5]) without editing it."
  artifacts:
    - ".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs"
    - ".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-CALIBRATION-AUDCOV.md"
  key_links:
    - "harness → src/__tests__/audit-coverage.test.ts via ts.createSourceFile + node.getText() extraction of the six real declarations (the anti-retype guarantee)"
    - "CALIBRATION-AUDCOV.md → plan 164.3.1-06's post-fix A/B/C table (this artifact IS the 'before' column)"
    - "CALIBRATION-AUDCOV.md → the exact bytes measured, via git blob hash + SHA-256 of the subject file recorded beside the output"
---

<objective>
Record the [AUDCOV-01] live-regression "before" state at HEAD, prior to any fix: drive cases
A/B/C through `src/__tests__/audit-coverage.test.ts`'s REAL extracted bytes and observe
`[]` / `[5]` / `[3]`, committing the observation as an artifact carrying the exact command and
its verbatim output.

Purpose: [AUDCOV-01] is the one item in this phase carrying a live regression on main — the
SP-I01 fix made `stripBlockComments` blind to a mutation call below a multi-line template
literal containing `/*` (a phantom block opens because `unmatchedBlockOpen`'s quote state is
per-line and resets at every newline). The PRE-FIX code found site A, so main's H-0001
audit-coverage gate is currently BLINDER than it was. Plan 164.3.1-06 owns the fix (quote state
carried across lines, `quoteAtEnd` forced null for `'` and `"`) and needs a real recorded
"before" to show movement against — a post-fix table with a reconstructed-from-memory before is
exactly the unchecked claim this phase refuses. Case B is the calibration control: it must
return a site both before AND after the fix; without it, "A now returns a site" is
unfalsifiable, because a stripper that returns every line would also pass.

**This plan fixes NOTHING.** It reads `src/__tests__/audit-coverage.test.ts`; it never edits it.
No `src/` file is modified, so no vitest or contracts run is required — the harness run is the
verification. File-disjoint from wave-1 siblings 01 (`scripts/mutation-runner/**`),
02 (`src/__tests__/lint-sql-gates.test.ts` + AST rule), and
04 (`scripts/sql-function-names-naive.mjs` / `scripts/sql-body-normalize.mjs`) — this plan
creates only two files under the phase directory.

Output: a verdict-free extraction harness (reusable by plan 06 for the "after" leg) and the
committed calibration artifact `164.3.1-03-CALIBRATION-AUDCOV.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@src/__tests__/audit-coverage.test.ts

Measured anchors, re-grepped 2026-09-01 at HEAD `4752920d` (RESEARCH measured at `fa2291d2`;
all anchors held across the drift — re-grep again before editing if HEAD has moved):

- `MUTATOR_CALL_RE` — `src/__tests__/audit-coverage.test.ts:115`
- `FROM_CALL_RE` — `:118`
- `unmatchedBlockOpen` — `:159` (its per-line `quote` state is the defect; explanatory SP-I01 header comment at `:136-152`)
- `stripBlockComments` — `:180` (call site of `unmatchedBlockOpen` at `:195`)
- `findMutations` — `:239` (self-contained given the five names above; its `Mutation[]` return annotation erases on transpile)
- `stripLineComment` — `:432`
- Shipped SP-I01 single-line-template arm (case C's input shape, verbatim source) — `:1161-1180`
- RESEARCH Premise P11 (the measured A/B/C = `[]`/`[5]`/`[3]` at HEAD, driven through real extracted bytes) and Pitfall 3 (why B is the control and why the fixtures must be multi-line real-shape bytes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the verdict-free extract-and-run calibration harness</name>
  <files>.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs</files>
  <precondition>Repo devDependencies are installed — `node -p "require('typescript').version"` from the repo root prints a 6.x version (the harness imports the repo's own `typescript`).</precondition>
  <read_first>
    - src/__tests__/audit-coverage.test.ts:110-300 — the six declarations to extract and their exact shapes (constants :115/:118, unmatchedBlockOpen :159-178, stripBlockComments :180-206, findMutations :239-297)
    - src/__tests__/audit-coverage.test.ts:425-442 — stripLineComment
    - src/__tests__/audit-coverage.test.ts:1161-1180 — the shipped single-line-template arm whose input shape case C reuses verbatim
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md — Premise Ledger row P11 and Common Pitfalls "Pitfall 3" (the extract-and-run method this task reproduces)
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-PATTERNS.md — §7 ([AUDCOV-01] defect verbatim) and §8(a) (the house ts.createSourceFile walk shape from src/lib/seam-log-coverage.test.ts)
  </read_first>
  <action>
    Create `164.3.1-03-audcov-extract.mjs` in the phase directory. It is a standalone Node ESM
    script, run from the repo root, importing only `typescript` (repo devDep, resolved by
    walking up to the root `node_modules`), `node:fs`, `node:vm`, `node:path`, and
    `node:crypto` (or shell out to `shasum` from Task 2 instead — executor's choice). Per the
    binding constraint it drives the cases through the file's REAL extracted bytes, reproducing
    RESEARCH P11's method — never a hand-retyped copy of the functions.

    Extraction: read `src/__tests__/audit-coverage.test.ts` via `node:fs` `readFileSync`
    (NUL-safe by nature; house rule). Parse with `ts.createSourceFile(path, source,
    ts.ScriptTarget.Latest, true, ts.ScriptKind.TS)`. Walk the SourceFile's TOP-LEVEL
    statements only and collect, by name, the source text (`stmt.getText(sourceFile)`) of
    exactly six declarations: FunctionDeclarations named `unmatchedBlockOpen`,
    `stripBlockComments`, `stripLineComment`, `findMutations`, and VariableStatements whose
    first declaration name is `MUTATOR_CALL_RE` or `FROM_CALL_RE`. If ANY of the six is not
    found, print which name(s) are missing and exit 1 — never fall back to an inline
    approximation; a silent fallback would calibrate the wrong subject, which is itself a
    primitive-D shape.

    Evaluation: concatenate the six extracted texts (declaration order is irrelevant —
    function declarations hoist and the two consts are only read at call time), transpile with
    `ts.transpileModule` (target ES2022; the snippet has no imports/exports so the output is a
    plain script; type annotations like `Mutation[]` and `string | null` erase without needing
    their declarations), and evaluate via `node:vm` — e.g. `vm.runInContext(transpiled +
    ";({ findMutations })", vm.createContext({}))`, taking the completion value to obtain the
    real `findMutations`. Do NOT use dynamic import of the test file (its vitest imports would
    execute) and do NOT re-implement any function.

    Cases: define the three inputs EXACTLY as specified in <behavior> below — copy the line
    arrays verbatim, `join("\n")`. Case A is the multi-line template carrying `/*` on a
    continuation line; case B is byte-identical to A except the two-character sequence `/*`
    (and its trailing space) is removed from line 3; case C is the shipped single-line arm's
    input shape from `:1161-1180` with the insert on line 3.

    Output (diagnostic-first, house standard): for each case print the case label, the input
    with 1-based line numbers, and the full JSON of `findMutations("case-<X>.ts", src)`. Then
    print sites as `sites = JSON.stringify(mutations.map(m => m.line))` per case, and end with
    ONE machine-greppable summary line in exactly this shape:
    `AUDCOV-CAL A=<sitesA> B=<sitesB> C=<sitesC>` (e.g. `AUDCOV-CAL A=[] B=[5] C=[3]`).

    The harness is VERDICT-FREE: it must NOT assert or bake in expected values and must exit 0
    whenever extraction and evaluation succeed, regardless of what the sites are. Rationale:
    plan 164.3.1-06 re-runs this SAME instrument after the fix, when case A's sites move to
    [5] — an instrument with baked-in "before" expectations would have to be edited between
    the before and after legs, breaking instrument identity. Expectations live in this plan's
    verify gate and in plan 06's, not in the instrument. State this rationale in the script's
    header comment, along with: what it extracts, from where, and that plan 164.3.1-06
    consumes it for the post-fix leg.

    Prohibitions: do not edit `src/__tests__/audit-coverage.test.ts` (plan 06 owns it; this
    plan only reads it). Do not touch `scripts/mutation-runner/**`,
    `src/__tests__/lint-sql-gates.test.ts`, `scripts/sql-function-names-naive.mjs`, or
    `scripts/sql-body-normalize.mjs` (wave-1 siblings own those). No new npm packages.
  </action>
  <behavior>
    Case inputs (copy verbatim; each is an array of lines joined with "\n").

    CASE A — the [AUDCOV-01] blindness: a multi-line template literal opened on line 2, a `/*`
    on continuation line 3 (inside the template), a real same-line-anchored mutation on line 5.
    At HEAD, line 3 is scanned with fresh per-line quote state, so its `/*` opens a phantom
    block and lines 4+ are blanked; expected sites at HEAD: [].

        "export async function POST() {",
        "  const tpl = `a multi-line template",
        "   a glob like /* appears mid-template",
        "   and it ends here`;",
        "  const { error } = await supabase.from('trades').insert(batch);",
        "  return Response.json({ ok: !error });",
        "}",

    CASE B — the calibration control: identical to A except line 3 drops the opener, i.e.
    line 3 is exactly "   a glob like appears mid-template". No phantom block can open;
    expected sites at HEAD: [5]. B must keep returning [5] after plan 06's fix — that is what
    makes "A now returns a site" falsifiable.

    CASE C — the shipped single-line arm still passes: the `:1161-1180` shape, insert on
    line 3. The single-line template's backtick pair opens and closes within one line, so the
    interior `/*` is correctly seen as quoted; expected sites at HEAD: [3].

        "export async function POST() {",
        "  const tpl = `c /* d`;",
        "  const { error } = await supabase.from('trades').insert(batch);",
        "  return Response.json({ ok: !error });",
        "}",

    Mechanism sanity (why these numbers): stripBlockComments processes lines independently at
    HEAD; A's line 2 opens a backtick that is forgotten at the newline; A's line 3 then reads
    `/*` outside any (per-line) quote → `unmatchedBlockOpen` returns its index → `inBlock =
    true` → lines 4-7 blank → findMutations sees nothing → []. B's line 3 has no `/*` → line 5
    survives → same-line `.from(` + `.insert(` → [5]. C's line 2 quote-tracks within the line
    → no phantom block → [3].
  </behavior>
  <reversibility rating="reversible">Adds one instrument script under the tracked phase directory; deleting it reverts fully. The verdict-free design choice is recorded in the script header and can be revisited by plan 06 if it needs a stricter instrument.</reversibility>
  <verify>
    <automated>OUT="$(node .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs)" && printf '%s\n' "$OUT" | grep -F "AUDCOV-CAL A=[] B=[5] C=[3]"</automated>
    <fails_when>node exits non-zero (extraction missed a declaration — the script names which — or evaluation threw), or the summary line `AUDCOV-CAL A=[] B=[5] C=[3]` is absent from stdout (grep exits 1): either the harness printed different site values than the measured HEAD before-state, or it printed no summary line at all.</fails_when>
  </verify>
  <acceptance_criteria>
    - The harness extracts exactly six named top-level declarations from `src/__tests__/audit-coverage.test.ts` via the TypeScript compiler API and evaluates the transpiled REAL bytes — the script contains no reimplementation of `unmatchedBlockOpen`, `stripBlockComments`, `stripLineComment`, or `findMutations`. (SC-5 before-leg; CONTEXT.md Primitive D locked decision: calibration is part of the deliverable.)
    - Missing any of the six declarations → exit 1 with the missing name(s) printed; no fallback path exists.
    - Output includes, per case: numbered input lines and the full mutations JSON (diagnostic-first), then the single summary line `AUDCOV-CAL A=[] B=[5] C=[3]` at pre-fix HEAD.
    - The harness contains no baked-in expected site values and exits 0 on any successful measurement — verified by reading the script: expectations appear only in this plan's verify gate.
    - `git status --porcelain -- src/__tests__/audit-coverage.test.ts` is empty after the task: the subject was read, never written.
  </acceptance_criteria>
  <done>Harness committed in the phase directory (`.planning/` is tracked — `git add` it); running it from the repo root at pre-fix HEAD exits 0 and prints `AUDCOV-CAL A=[] B=[5] C=[3]`; the subject test file is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Record the calibration artifact — command, verbatim output, byte bindings</name>
  <files>.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-CALIBRATION-AUDCOV.md</files>
  <read_first>
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs — the instrument Task 1 built (its printed output is what gets recorded verbatim)
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md — "Specific Ideas" section ([AUDCOV-01] must-not-merge-as-is; B is the calibration control) and the anti-vacuity contract framing
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-PLAN-OUTLINE.md — row 164.3.1-06 (the consumer: its post-fix table shows movement against THIS artifact)
  </read_first>
  <action>
    Run the harness ONCE, capturing combined stdout verbatim (redirect to a file in the session
    scratchpad, not /tmp), and write `164.3.1-03-CALIBRATION-AUDCOV.md` in the phase directory
    with these sections, in order:

    1. **What this is** — the [AUDCOV-01] live-regression "before" record for SC-5, taken at
       HEAD before any fix; states that plan 164.3.1-06 consumes it as the "before" column of
       its post-fix A/B/C table, and that this plan fixed nothing.
    2. **Provenance** — recorded by RUNNING each command and pasting its output: branch name
       and `git rev-parse HEAD` at measurement time; `git rev-parse
       HEAD:src/__tests__/audit-coverage.test.ts` (the subject's git BLOB hash — stable across
       this plan's own commits, the durable byte-binding); `shasum -a 256
       src/__tests__/audit-coverage.test.ts` of the working tree; the blob-equality proof —
       `git show HEAD:src/__tests__/audit-coverage.test.ts | shasum -a 256` pasted beside the
       working-tree shasum with the two hash fields visibly equal (this PROVES the measured
       bytes are the committed pre-fix bytes, not a locally-edited file); `git status
       --porcelain -- src/__tests__/audit-coverage.test.ts` output shown empty; `node
       --version`.
    3. **The exact command** — the verbatim invocation from the repo root:
       `node .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs`
    4. **Verbatim output** — the FULL captured stdout, unedited, including every per-case
       numbered input listing, the mutations JSON, and the `AUDCOV-CAL A=[] B=[5] C=[3]`
       summary line. Not a paraphrase, not an excerpt.
    5. **Before-state table** — three rows (A/B/C): input shape, sites at HEAD, and meaning
       (A `[]` = the blindness — a phantom block opened by a `/*` inside a multi-line template
       blanks the real mutation below it; B `[5]` = the control proving the harness and
       detector work; C `[3]` = the shipped single-line arm still passes).
    6. **What plan 164.3.1-06 must show** — after the quote-carry fix (quote state survives
       newlines; `quoteAtEnd` forced null for `'` and `"`): A moves `[]` → `[5]`, B stays
       `[5]`, C stays `[3]` — movement on exactly one row, measured with this same committed
       instrument. Note explicitly that the instrument is verdict-free by design so 06 re-runs
       it unedited.

    Every number in the artifact must come from a command run in this task — no value carried
    from this plan's own text or from RESEARCH by memory. If the harness prints anything other
    than `A=[] B=[5] C=[3]`, STOP: do not write an artifact recording an expectation the
    machine did not produce — surface the discrepancy in the SUMMARY as a blocker instead
    (the before-state has drifted and plans 03/06 need re-grounding).

    No neuter/restore harness is needed in this plan — the subject file is never edited, so
    there is nothing to back up or restore (and per the phase contract, `git checkout --` is
    forbidden in any such harness regardless). The porcelain check in Provenance is the proof
    of non-modification.
  </action>
  <reversibility rating="reversible">Adds one artifact file under the tracked phase directory; reverting is deletion. The recorded measurement itself is reproducible from the committed harness at the recorded blob hash.</reversibility>
  <verify>
    <automated>ART=".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-CALIBRATION-AUDCOV.md" && grep -F "AUDCOV-CAL A=[] B=[5] C=[3]" "$ART" && grep -F "$(git rev-parse HEAD:src/__tests__/audit-coverage.test.ts)" "$ART" && [ -z "$(git status --porcelain -- src/__tests__/audit-coverage.test.ts)" ]</automated>
    <fails_when>first grep exits 1 (the artifact does not carry the verbatim summary line — the recorded output is missing or was paraphrased), or second grep exits 1 (the artifact does not record the subject file's current git blob hash — the byte-binding between the recorded measurement and the committed pre-fix bytes is broken), or the bracketed test fails because `git status --porcelain` printed anything for the subject file (a fix or edit leaked into the calibration plan).</fails_when>
  </verify>
  <acceptance_criteria>
    - The artifact carries the exact command AND its full verbatim output — the anti-vacuity contract's committed-artifact requirement, not a claim in a summary. (SC-5)
    - Provenance section binds the measurement to bytes: branch HEAD, subject blob hash (`git rev-parse HEAD:src/__tests__/audit-coverage.test.ts`), working-tree SHA-256, and the pasted blob-equality proof showing working tree == committed blob.
    - The before-state table reads A=`[]`, B=`[5]`, C=`[3]`, each with its meaning, and the "What plan 164.3.1-06 must show" section states the expected post-fix movement (A→`[5]`, B and C unchanged) using this same unedited instrument.
    - `src/__tests__/audit-coverage.test.ts` remains byte-identical to `HEAD` — no fix was smuggled into this plan (checked by the porcelain gate in verify).
    - Both new files are `git add`ed and committed (`.planning/` is tracked in this repo).
  </acceptance_criteria>
  <done>`164.3.1-03-CALIBRATION-AUDCOV.md` is committed beside the harness, carrying command + verbatim output + byte bindings; plan 164.3.1-06 can show before→after movement against it without reconstructing anything from memory.</done>
</task>

</tasks>

<threat_model>
This plan ships local calibration tooling for a CI gate family. It has no user-facing surface,
no network I/O, no database access (neither shared TEST nor PROD is reachable from anything
here), and installs zero packages. The honest trust boundary is narrow: repo-tracked source
(`src/__tests__/audit-coverage.test.ts`) is extracted and executed in a local `node:vm` context
— the same trust domain as running the repo's own vitest suite on this machine.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-tracked test source → local vm evaluation | extracted declarations from a file this checkout already executes under vitest; no privilege change |
| recorded artifact → plan 06's fix verification | a falsified or unbound "before" would let the fix claim unearned movement — the integrity boundary this plan exists to close |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-03-01 | Tampering | harness extraction (measuring a retyped copy instead of the real bytes) | medium | mitigate | AST extraction by name from the file's own bytes; exit 1 naming any missing declaration; no fallback path; acceptance criterion forbids reimplementation |
| T-164.3.1-03-02 | Repudiation | calibration artifact (an unverifiable "before" claim) | medium | mitigate | artifact carries exact command, full verbatim output, branch HEAD, subject blob hash, and the working-tree-vs-committed-blob shasum equality proof |
| T-164.3.1-03-03 | Elevation of Privilege | `vm.runInContext` of extracted code | low | accept | executed code is the repo's own tracked test file, already run locally under vitest; vm adds no new privilege and the context is empty |
| T-164.3.1-03-SC | Tampering | npm/pip/cargo installs | low | accept | zero packages installed by this plan; RESEARCH's Package Legitimacy Audit records none for the whole phase; the banned-package gate is untouched |
</threat_model>

<verification>
From the repo root, at pre-fix HEAD (before plan 164.3.1-06 executes):

1. `node .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs` exits 0 and its stdout contains the line `AUDCOV-CAL A=[] B=[5] C=[3]` plus per-case numbered inputs and mutations JSON.
2. `164.3.1-03-CALIBRATION-AUDCOV.md` exists, contains that same summary line verbatim inside the recorded output block, and contains the output of `git rev-parse HEAD:src/__tests__/audit-coverage.test.ts` run now (byte-binding intact).
3. `git status --porcelain -- src/__tests__/audit-coverage.test.ts` prints nothing, and `git log --oneline -- src/__tests__/audit-coverage.test.ts` shows no commit from this plan: the subject file was never modified.
4. `git ls-files` lists both new phase-directory files (committed, since `.planning/` is tracked).
5. Negative scope check: this plan's commits touch no file under `src/`, `scripts/`, or `.github/` (`git show --stat` on this plan's commits) — the fix, the tokenizer, the AST rule, and the VAC-04 readers all belong to sibling/downstream plans.
</verification>

<success_criteria>
- SC-5's "before" leg is a committed, reproducible, byte-bound record: cases A/B/C measured through the file's real extracted bytes at pre-fix HEAD read exactly `[]` / `[5]` / `[3]`, with the exact command and verbatim output in the artifact.
- Case B (`[5]`) is recorded as the calibration control and case C (`[3]`) as the shipped-arm control, so plan 06's post-fix claim "A finds its site" is falsifiable in both directions.
- Plan 164.3.1-06 can compute its before→after table from this artifact plus a re-run of the SAME committed, verdict-free instrument — nothing reconstructed from memory.
- Nothing was fixed: `unmatchedBlockOpen` is not widened, no quote-carry exists, and `src/__tests__/audit-coverage.test.ts` is byte-identical to its committed blob.
</success_criteria>

## Artifacts this phase produces (this plan's contribution)

| Artifact | Path | Consumer |
|----------|------|----------|
| Extraction harness (verdict-free instrument) | `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-audcov-extract.mjs` | Plan 164.3.1-06 (re-run unedited for the post-fix leg); phase verifier (reproduces the before-state) |
| [AUDCOV-01] calibration "before" record | `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-CALIBRATION-AUDCOV.md` | Plan 164.3.1-06 (the "before" column of its post-fix A/B/C table); phase verification for SC-5 |

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-03-SUMMARY.md` when done. If the harness measured anything other than `A=[] B=[5] C=[3]`, the SUMMARY must flag it as a BLOCKER (before-state drift — plans 03 and 06 need re-grounding), not record it as a pass.
</output>
