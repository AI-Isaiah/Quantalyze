---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "07"
type: execute
subsystem: vac04-gate
tags: [vac04, fail-closed, absurdity-floor, reopen-pin, primitive-c]
wave: 2
depends_on: ["164.3.1-04"]
files_modified:
  - scripts/prod-body-drift-check.sh
  - src/__tests__/drift-check-scripts.test.ts
autonomous: true
requirements: [SC-4, SC-7, SC-8, SC-9]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "[VAC04-C3]: the name-index membership grep's exit code is captured explicitly — exit 0 → present path, exit 1 → measured-absent path, exit >= 2 → hard MEASURE_FAIL naming the file, the pattern and the code. An unreadable index can no longer land in the 'measured absent — Treated as a NEW function (pass)' branch. Proven by a driven arm that stubs grep to exit 2 (SP-M01 idiom) and observes the MEASURE_FAIL."
    - "[VAC04-C1] zero path FAILS CLOSED (amended D-07): driving P8's composing input (a mid-line $-identifier definition BOTH readers miss) through the real gate prints BOTH readers' outputs and the changed-file list — evidence BEFORE refusal, per D-12/SC-7 — and then exits NON-ZERO. A gate over PRODUCTION function bodies can no longer exit 0 having compared nothing."
    - "The closed state is pinned by a standing REOPEN PIN (amended D-08): an executable arm that REDs if the zero path ever returns to exiting 0. Proven two-directionally per the VALIDATION map — neuter by reverting the refusal to `exit 0` → the pin REDs, observed; restore → the pin is silent, sha-verified. There is no future flip left to guarantee; the pin is a standing regression machine, never a TODO."
    - "The VAC-04 absurdity floor exists, templated on VAC-08's shape: a PROD name index below a measured floor is a BROKEN READER, not an empty database — MEASURE_FAIL with the shape diagnostic, never an all-new pass. The threshold records its measurement (repo-side proxy 359 definitions / 380 files / 122 distinct names, measured 2026-09-01) with sample size and coverage beside it, and states both separation directions (SC-8, SC-9)."
    - "The floor is proven TWO-DIRECTIONALLY: an injected tiny index fires it with evidence printed, and a legitimate run (real-shaped index, ordinary drift) leaves it SILENT — because a floor that fires unconditionally also passes its own RED arm."
    - "SC-4 is MET outright at phase end: the zero path fails closed inside this phase (amended D-07, 2026-09-01, reversing the same-day deferral to 164.4). This plan's SUMMARY and the phase verification record SC-4 as MET — never PARTIAL. The wedge risk is handled by ORDERING, not compromise: migration PRs are HELD until 164.3.1 and 164.4 have both landed; a block at the fail-closed zero path is the gate working, not a defect to route around."
  artifacts:
    - "scripts/prod-body-drift-check.sh — grep exit-code branching, fail-closed zero path with both readers' evidence and the VAC04-ZERO-PATH-FAILS-CLOSED marker, absurdity floor with measured thresholds"
    - "src/__tests__/drift-check-scripts.test.ts — driven arms: grep-exit-2 MEASURE_FAIL, P8 evidence-then-refusal (exit non-zero), floor fires / floor silent, and the reopen pin (REDs if the zero path returns to exit 0; silent on the restored gate)"
  key_links:
    - "reopen pin → the real gate's bytes and exit code: it drives the P8 composing fixture through scripts/prod-body-drift-check.sh and requires a non-zero exit plus the VAC04-ZERO-PATH-FAILS-CLOSED marker — reverting the refusal to exit 0 REDs it by execution, and deleting or rerouting the branch REDs it by name"
    - "floor template ← scripts/test-ledger-drift-check.sh:320-350 (the VAC-08 floor: quantity, wide-separation comment, shape diagnostic, exit 1) and the grep-rc discipline at :311-318"
    - "driven arms ← the scaffold()/stub-command idioms already in drift-check-scripts.test.ts (SP-M01 delegate-except-one-file grep stub; injected BODY_NAME_INDEX_CMD)"
---

<objective>
Make VAC-04's gate script honest about what it measured: close [VAC04-C3] (grep exit 2 currently
lands in the fail-OPEN "measured absent (pass)" branch), convert the [VAC04-C1] zero path at
`scripts/prod-body-drift-check.sh:255-256` to FAIL CLOSED — per amended D-07: it prints both
readers' evidence loudly and THEN exits non-zero, refusing to pass having compared nothing
(evidence BEFORE refusal — SC-7 diagnostic-first; the gate must not ship a bare conclusion) —
pin the closed state with a standing REOPEN PIN (amended D-08: an executable arm that REDs if the
zero path ever returns to exiting 0 — a regression machine, never a TODO),
and extend the absurdity floor to VAC-04 (D-09: absurdity floors on VAC-04 and the mutation runner,
alongside VAC-08's existing one — this plan owns the VAC-04 half) — a gate
that can currently report "every function is new — pass" when its name reader breaks.

Every threshold is set BY MEASUREMENT with wide separation, never by taste, templated on VAC-08's
floor (fires 9/262, silent 232/262 — both directions stated beside the rule). Denominators already
measured for this work: the repo-side index proxy is 359 definitions across 380 .sql files, 122
distinct repo function names (RESEARCH § Pattern 3).

Plan 04 hardened the two READERS (realpath guards, charset refusal) — reader exit 1 already fails
this gate through its existing fail-wrapped invocations, so this plan needs no propagation work.
This plan owns the GATE SCRIPT and its driven arms.

**SC-4 is MET outright in this phase** (CONTEXT § Amendment 2026-09-01 — the same-day deferral to
164.4 is REVERSED; every instruction to record SC-4 as PARTIAL is withdrawn). The wedge risk that
justified deferring is handled by ORDERING, which is load-bearing and lives in this plan, not only
in CONTEXT: ⛔ **migration PRs are HELD until 164.3.1 AND 164.4 have both landed.** An executor or
reviewer hitting a block at the fail-closed zero path is watching the gate WORK — route the
ordering, never the gate.

⚠️ **RESUME STATE for a re-dispatched executor: Task 1 is EFFECTIVELY DONE.** It landed as commit
`ab99ab99` ("[VAC04-C3] an unreadable name index is a MEASURE_FAIL, not 'measured absent'"), on
branch `worktree-agent-af16f49b7215565f4`, not yet merged to the phase branch. That work is
orthogonal to this amendment and is KEPT. Start by making `ab99ab99` reachable from your base
(`git merge-base --is-ancestor ab99ab99 HEAD`; if it fails, merge or fork from that commit —
never re-implement), re-run Task 1's verify green, then proceed directly to Task 2.

⛔ Safety: VAC-04 diffs repo function bodies against PROD by design — this plan touches only READ
paths and drives everything through injected commands and scratch fixtures. No database is
contacted by any task; never any supabase CLI command from this checkout.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-PATTERNS.md
@scripts/prod-body-drift-check.sh
@scripts/test-ledger-drift-check.sh
@src/__tests__/drift-check-scripts.test.ts

Read at execution time — created by plan 04, so it does not exist at planning time and is
deliberately not @-referenced (plan-anchor-verify checks pending plans against the current tree):
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-04-SUMMARY.md

Measured anchors at HEAD b4bdfc1e (only this plan edits these files in this phase, so they hold
at execution; re-grep before editing regardless — plan-anchor-verify also checks):
- Zero path: `scripts/prod-body-drift-check.sh:255-256` — the notice text then `exit 0`.
- Unbranched membership grep: line 425 (a bare if over `grep -aqxF` — exits 1 AND >= 2 both fall
  to the else) with the fail-open print at line 434. ⚠️ This describes the PHASE-BRANCH head
  (what the pending-plan anchor check reads); it is already FIXED in Task 1's landed commit
  `ab99ab99` — after the resume merge, locate the three-way branching by its MEASURE_FAIL text,
  not these lines.
- Already-guarded zero-compare split (match its diagnostic style): lines 545-552.
- Legitimate zero path (do NOT touch): lines 198-199 (no migration files changed).
- VAC-08 floor template: `scripts/test-ledger-drift-check.sh:320-350` (`ABSURDITY FLOOR`); the
  grep-rc discipline at lines 311-318; the shape diagnostic emission at line 346.
- Existing VAC-04 driven arms: `src/__tests__/drift-check-scripts.test.ts:182-544` — the
  `scaffold` idiom, stub commands, the empty-index arm at line 315, the erroring-index arm at
  line 327.
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL (ROADMAP deliverable 3) | Zero path fails closed rather than trusting composed blind spots (amended D-07 — the warn-then-flip deferral is reversed); explicit grep exit branching with >= 2 a hard fail | Tasks 1-2 |
| GOAL (ROADMAP deliverable 9) | Absurdity floor extended to VAC-04, thresholds by measurement with wide separation | Task 3 |
| REQ [SC-4] | [VAC04-C1] and [VAC04-C3] driven end-to-end through the real gate — MET outright, recorded as MET | Tasks 1-3 + SUMMARY |
| REQ [SC-7] | The refusal's evidence block and every new failure print what they saw (both readers' outputs, counts, samples) BEFORE the conclusion | Tasks 1-3 |
| REQ [SC-8] | Floor proven able to FIRE and proven SILENT on a legitimate finding | Task 3 |
| REQ [SC-9] | Thresholds carry sample size + coverage; the key asserted against the full corpus (380-file parity stands from plan 04) | Task 3 |
| CONTEXT § Amendment 2026-09-01 (amended D-07/D-08, locked) | Fail closed NOW; the closed state pinned by a standing reopen arm, never a TODO; migration PRs held until 164.3.1 + 164.4 both land | Task 2 |
| RESEARCH Open Question 1 | MOOT under amended D-07 — there is no deferred flip to observe; the reopen pin keys on the gate's own bytes and exit code, not on a 164.4 observable | Task 2 (recorded in the pin's comment) |
| VALIDATION rows "VAC-04 grep exit branching", "VAC-04 zero-path FAIL-CLOSED + reopen pin", "absurdity floors (VAC-04 half)" | Each row's neuter and restore leg discharged | Tasks 1-3 |

Excluded (not gaps): reader-side members C2/C4 (plan 04, shipped); the mutation-runner floor half
(plan 10); corpus consolidation and the VAC-08 instances (plan 12).
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: [VAC04-C3] — explicit exit-code branching on the name-index grep; >= 2 is a hard MEASURE_FAIL</name>
  <files>scripts/prod-body-drift-check.sh, src/__tests__/drift-check-scripts.test.ts</files>
  <precondition>Plan 164.3.1-04 is merged: node scripts/sql-function-names-naive.mjs --self-test prints "self-test OK (13 checks)" (the hardened readers this gate now calls).</precondition>
  <read_first>scripts/prod-body-drift-check.sh:405-455 (the comparison loop around the :425 grep — the bare if, the present path, and the :434 fail-open else) and :300-320 (how the name index is built and the SP-C05 warnings around it); scripts/test-ledger-drift-check.sh:311-318 (the set +e / grep_rc / set -e capture discipline to reproduce — the house template, verbatim in PATTERNS §5); src/__tests__/drift-check-scripts.test.ts:1176-1218 (the SP-M01 arm — a PATH-shim grep that delegates everywhere except one target file and exits 2 there; the harness idiom this task's arm reuses against prod-body-drift-check.sh)</read_first>
  <behavior>
    - New driven arm "VAC04-C3 RED: a grep that ERRORS on the name index is MEASURE_FAIL, not measured-absent": scaffold a run whose name index exists, shim grep to exit 2 only when reading the prod-names file → gate exits 1 with a MEASURE_FAIL naming the index file, the searched name, and exit code 2 — and the output does NOT contain the measured-absent pass text. AT HEAD this arm is RED in the defect direction: the gate prints the measured-absent pass line and continues — capture that pre-fix observation verbatim.
    - Control arm (existing behavior fenced): a name genuinely absent from a READABLE index still takes the measured-absent pass path; a name present but unfetchable still takes the extraction-failure exit 1 (both shapes already have arms at :271 and :290 — they must stay green, proving the branching changed only the error leg).
  </behavior>
  <action>
    ⚠️ ALREADY LANDED (amendment 2026-09-01): this task executed and landed as commit `ab99ab99`
    on branch `worktree-agent-af16f49b7215565f4` — orthogonal to the D-07/D-08 amendment and
    KEPT. If that commit is reachable from your base, do NOT redo the work: run this task's
    automated verify green, confirm the acceptance criteria against the landed diff, and move
    to Task 2. Re-implement only if the commit is genuinely absent from your tree.

    Write the driven arm first (reuse the SP-M01 PATH-shim idiom from :1176 pointed at
    prod-body-drift-check.sh's scaffold), observe the pre-fix fail-open behavior, capture it. Then
    fix the gate: replace the bare if-grep at :425 with the VAC-08 capture discipline — disable
    errexit around the grep, capture the status, restore errexit, then branch three ways: status 0
    → the present path (unchanged); status 1 → the measured-absent path (unchanged); status >= 2 →
    MEASURE_FAIL exit 1 printing what was attempted (the index file path, the function name
    searched, the exit code) and the sentence that an unreadable index is not a measurement —
    diagnostic-first per SC-7, matching the style at :545-552. Keep the -aqxF flags (NUL-safe
    fixed-string exact-line — the reason each flag exists is already the house idiom). Re-run the
    arm green, and re-run the two control arms.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>exit code is non-zero or any test fails — the new C3 arm, both control arms, and every pre-existing VAC-04/SP-C05/VAC-08 arm must pass together</fails_when>
  </verify>
  <acceptance_criteria>
    - The gate script branches on the captured grep status three ways; the >= 2 leg exits 1 naming file, pattern and code — verified by the driven arm, with the pre-fix fail-open observation recorded verbatim for the SUMMARY
    - The measured-absent pass path and the extraction-failure path are byte-for-byte unchanged in behavior (their standing arms green)
    - No reader script is modified (plan 04's files untouched: git status --porcelain on scripts/sql-function-names-naive.mjs and scripts/sql-body-normalize.mjs is empty)
  </acceptance_criteria>
  <done>Exit 2 can no longer impersonate "measured absent": the fail-closed leg exists, fires under the driven arm, and the legitimate legs are fenced unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: [VAC04-C1] — the zero path FAILS CLOSED: both readers' evidence printed, THEN a non-zero exit; the closed state pinned by an executable reopen pin</name>
  <files>scripts/prod-body-drift-check.sh, src/__tests__/drift-check-scripts.test.ts</files>
  <read_first>scripts/prod-body-drift-check.sh:240-260 (the zero path being converted: the union-agreement comment block and the :255-256 notice + exit 0) and :198-199 (the OTHER zero path — no migration files changed — which is legitimate and must not change); 164.3.1-CONTEXT.md § Amendment 2026-09-01 and the amended D-07/D-08 (the authority for this task: fail closed NOW, a standing reopen pin instead of a flip arm, and the migration-PR ordering constraint); 164.3.1-VALIDATION.md § Amendment 2026-09-01 and the neuter/restore map row "VAC-04 zero-path FAIL-CLOSED + reopen pin" (the exact two-directional proof this task discharges); 164.3.1-RESEARCH.md § Pattern 3 "Zero path" (the P8 composing-shape mechanics — read its "flip arm" half as historical: superseded by amended D-08)</read_first>
  <behavior>
    - New driven arm "VAC04-C1: the composing zero path prints evidence, then REFUSES": scaffold a changed migration whose only definition is P8's composing shape (a mid-line $-identifier definition — SELECT 1; then CREATE OR REPLACE FUNCTION on the same line — which the line-anchored naive reader cannot see and the normalizer drops at the $) → the gate prints an evidence block FIRST — the changed-file list, the naive reader's output for those files (empty), the normalizer's output (empty), and the sentence that agreement between readers with overlapping blind spots is not evidence of absence — and THEN exits NON-ZERO (amended D-07). AT HEAD the same input produces only the one-line ::notice:: and exit 0 — capture the before.
    - Passing control (a gate that refuses everything also passes its own refusal test): a run whose changed migration defines a function BOTH readers can see proceeds PAST the zero path to a normal verdict — the standing e2e GREEN gate arm already crosses this path; confirm it stays green post-change and cite it as the control, adding a dedicated control arm only if its scaffold does not exercise the zero-path branch.
    - Reopen pin (amended D-08), two parts: (a) the driven refusal arm above IS the executable direction — reverting the refusal to exit 0 flips its expected non-zero exit and REDs it by execution; (b) a bytes assertion via node:fs that scripts/prod-body-drift-check.sh contains the VAC04-ZERO-PATH-FAILS-CLOSED marker, so deleting or rerouting the refusal branch also fails BY NAME. Fire-proof both directions per the VALIDATION map: neuter (revert the refusal to exit 0) → pin REDs, observed; restore → pin silent, sha-verified.
  </behavior>
  <action>
    Convert the zero path at :255-256 per amended D-07: replace the single ::notice:: + exit 0
    with a diagnostic-first evidence block followed by a REFUSAL. Print WHAT BOTH READERS SAW
    first — the changed migration file list the gate already holds, each reader's verbatim
    (empty) name output for those files, and the explicit caveat that this zero is unverified
    absence (two readers with overlapping blind spots agreeing is not a measurement), citing
    [VAC04-C1] and SP-C05 — THEN exit non-zero. Evidence before refusal: the gate must never
    ship a bare conclusion (SC-7, D-12). Mark the refusal branch with the marker token
    VAC04-ZERO-PATH-FAILS-CLOSED on its own comment line, stating the amended founder decision
    (2026-09-01, reversing the same-day warn deferral) and the ORDERING CONSTRAINT beside it, so
    a blocked reviewer reads it at the point of refusal: migration PRs are held until 164.3.1
    and 164.4 have both landed — a block here is the gate working, not a defect to route around.

    Write the REOPEN PIN in drift-check-scripts.test.ts: the driven refusal arm (the P8
    composing fixture through the real gate → evidence block present AND exit non-zero) plus a
    bytes assertion via node:fs that the gate script contains VAC04-ZERO-PATH-FAILS-CLOSED
    (fail-by-name if the branch is deleted or rerouted; node:fs, never shell grep — this repo
    carries a measured NUL-blind file). In the pin's comment, record that the superseded
    FILES_FLOOR flip-coupling design was retired with amended D-08: there is no deferred flip
    left to guarantee, so the pin guards the closed state itself. Discharge the VALIDATION
    map's two directions: neuter the refusal back to exit 0 (byte backup, shasum -a 256
    re-taken after every edit, never git checkout --) → observe the pin RED, capture verbatim;
    restore, shasum-verify, observe the pin green. Confirm the passing control from the
    behavior block so the refusal is proven to fire ONLY on the compared-nothing state.

    Do not touch the :198-199 zero path (no migration files changed — a legitimate nothing-to-do,
    it keeps its exit 0), and do not touch the :545-552 measured-zero split beyond matching its
    diagnostic style.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>exit code is non-zero or any test fails — the evidence-then-refusal arm (evidence block present, exit NON-zero), the passing control (a legitimate comparison still reaches its normal verdict), and the reopen pin's bytes assertion must pass together with every pre-existing arm</fails_when>
    <automated>bash -c 'grep -ac "VAC04-ZERO-PATH-FAILS-CLOSED" scripts/prod-body-drift-check.sh'</automated>
    <fails_when>exits non-zero or prints 0 — the fail-closed marker the reopen pin keys on is missing from the gate script</fails_when>
  </verify>
  <acceptance_criteria>
    - Driving P8's composing input through the real gate prints the evidence block (changed-file list and BOTH readers' outputs) and exits NON-ZERO — the before (one-line notice, exit 0) and after captured for the SUMMARY
    - The reopen pin exists (driven refusal arm + marker bytes assertion) and its two-directional proof is recorded: refusal reverted to exit 0 → pin observed RED; restored → pin green, shasum-verified
    - The passing control is green: a legitimate non-empty comparison still reaches its normal verdict — the refusal fires only on the compared-nothing state
    - The legitimate :198-199 zero path is unchanged (still a quiet exit 0)
    - The ordering constraint (hold migration PRs until 164.3.1 and 164.4 both land) is recorded beside the refusal in the gate script AND restated in the SUMMARY
    - Every text the SUMMARY records about SC-4 says MET, never PARTIAL
  </acceptance_criteria>
  <done>The zero path can no longer exit 0 having compared nothing: it prints both readers' evidence and refuses (amended D-07), the closed state is pinned by an executable reopen arm that REDs on any revert (amended D-08), and the migration-PR ordering that absorbs the wedge risk is written where a blocked reader will find it.</done>
</task>

<task type="auto">
  <name>Task 3: The VAC-04 absurdity floor — measured thresholds, wide separation, proven two-directionally</name>
  <files>scripts/prod-body-drift-check.sh, src/__tests__/drift-check-scripts.test.ts</files>
  <read_first>scripts/test-ledger-drift-check.sh:320-350 (the template: quantity, the wide-separation comment stating BOTH measured directions, the shape diagnostic, exit 1) — copy its structure, not its numbers; scripts/prod-body-drift-check.sh:300-340 (where PROD_NAME_COUNT is computed from the index — the floor's insertion point is after the index is read and before the comparison loop); src/__tests__/drift-check-scripts.test.ts:1013-1055 (the VAC-08 floor's three-arm proof set — RED fires, matched control silent, small-population control silent — the exact arm shape to reproduce for VAC-04); 164.3.1-RESEARCH.md § Pattern 3 "Absurdity floor (VAC-04)" (candidate quantity and the measured denominators: 359 definitions / 380 files / 122 distinct names)</read_first>
  <action>
    MEASURE FIRST, in this task, before writing the rule: run the real index-building pipeline
    shape over the repo corpus (node scripts/sql-body-normalize.mjs --function-names across
    supabase/migrations plus the schema snapshot the workflow feeds it, per the
    BODY_NAME_INDEX_CMD wiring quoted in PATTERNS §10) and record the measured index size at HEAD
    beside the RESEARCH proxies (359 definitions / 380 files / 122 distinct names). Pick the floor
    threshold from the measurement with WIDE separation — a broken reader scores 0 or single
    digits (the measured C2/C4 failure shapes produced empty output), the real index scores in the
    hundreds; choose a threshold with at least an order of magnitude to each side and write the
    wide-separation comment stating BOTH directions with their measured values, plus sample size
    and coverage (which corpus, how many files, date) per SC-9.

    Implement the floor in prod-body-drift-check.sh after the PROD name index is read: if the
    index row count is below the measured floor, MEASURE_FAIL exit 1 — this is the GATE failing,
    not a database with no functions — printing the observed count, the floor with its measured
    justification, and a sample of what WAS read (head of the index), diagnostic-first per SC-7,
    in the VAC-08 template's exact structure. The floor must sit so it fires BEFORE any
    measured-absent pass can be reached (a tiny index is what makes every name absent).

    Prove it TWO-DIRECTIONALLY with driven arms mirroring :1013-1055: (fires) inject a
    BODY_NAME_INDEX_CMD yielding a plausible-but-tiny index (a handful of names) with a PR that
    defines a real function → gate exits 1 with the floor's MEASURE_FAIL and evidence, never the
    all-new pass; (silent) a realistic index (hundreds of names, generated from the repo corpus)
    with ordinary drift → the floor stays silent and the run proceeds to its normal verdict.
    Keep the existing empty-index arm at :315 green — the floor subsumes empty but must not
    change that arm's contract.

    Confirm the standing 380-file corpus-parity arm (plan 04's silent-direction proof) still
    passes — it doubles as SC-9's key-against-the-full-corpus assertion for the readers feeding
    this floor.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts && npx vitest run src/__tests__/contracts</automated>
    <fails_when>either run exits non-zero — the floor's fire arm, silent arm, empty-index arm, corpus-parity arm, or a contracts scan reddened; a floor that cannot fire or that fires on legitimate input both land here</fails_when>
    <automated>bash -c 'grep -a -A3 "ABSURDITY FLOOR" scripts/prod-body-drift-check.sh | grep -aqi "measured"'</automated>
    <fails_when>exits non-zero — the floor block lacks a measured-threshold comment beside the rule (SC-9's record is missing)</fails_when>
  </verify>
  <acceptance_criteria>
    - The floor threshold was picked from a measurement RUN IN THIS TASK, recorded in the comment with both separation directions, sample size and coverage — not copied from RESEARCH by memory
    - Fire direction: tiny-index injection → MEASURE_FAIL with observed count, floor justification, and an index sample printed; silent direction: realistic index + ordinary drift → no floor output — both arms committed
    - The floor precedes every measured-absent pass; the empty-index and corpus-parity arms stay green
    - SUMMARY records SC-4 as MET (fail-closed zero path per amended D-07, reopen pin in place per amended D-08), SC-8 as met for the VAC-04 half, and the threshold's measurement record for SC-9
  </acceptance_criteria>
  <done>VAC-04 can no longer report "every function is new — pass" off a broken reader: the floor fires on the absurd state with evidence, stays silent on legitimate findings, and carries its measurement beside the rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR-authored migrations → gate verdict | untrusted input decides which functions are compared against PROD bodies |
| PROD dump → name index → pass/fail branches | a broken instrument on this path converts directly into a green gate over production function bodies |
| future edits to the zero path → the fail-closed contract | a quiet revert to exit 0 would reopen Primitive C's canonical case; the reopen pin is the machine watching for it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-07-01 | Tampering | fail-OPEN grep branch treating exit 2 as measured-absent | high | mitigate | task 1's three-way branching with the >= 2 MEASURE_FAIL, proven by the grep-stub driven arm |
| T-164.3.1-07-02 | Tampering | broken name reader yielding an all-new pass over PROD bodies | high | mitigate | task 3's absurdity floor with measured thresholds, proven fires + silent |
| T-164.3.1-07-03 | Tampering | the fail-closed zero path silently reverted to exit 0 | high | mitigate | task 2's reopen pin: the driven refusal arm (REDs by execution on a revert) plus the fail-by-name marker assertion, in the aggregator-gated vitest suite |
| T-164.3.1-07-04 | Information Disclosure | refusal/floor diagnostics echoing PROD body text into public CI logs | medium | mitigate | evidence blocks print reader NAME outputs, file lists, counts and index samples (names only) — never body text; the normalizer's no-body-text contract (plan 04) is upstream of everything printed |
| T-164.3.1-07-SC | Tampering (supply chain) | npm installs | low | accept | zero packages installed by this plan; no install task exists |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 07)

| Artifact | Kind | Location | Consumers |
|---|---|---|---|
| Three-way grep branching (C3 fix) | gate-script control flow | `scripts/prod-body-drift-check.sh` | migration-drift-check.yml runs; plan 12's instance→arm registry |
| Fail-closed zero path (evidence block, then exit non-zero) + `VAC04-ZERO-PATH-FAILS-CLOSED` marker + the migration-PR ordering note | gate-script branch + greppable token | `scripts/prod-body-drift-check.sh` | the reopen pin; migration PRs (held until 164.3.1 + 164.4 both land, by ordering) |
| Reopen pin (driven refusal arm + fail-by-name marker assertion) | vitest arms | `src/__tests__/drift-check-scripts.test.ts` | CI vitest shards / frontend aggregator; any future editor of the zero path |
| VAC-04 absurdity floor with measured-threshold comment | gate-script rule | `scripts/prod-body-drift-check.sh` | migration PRs; plan 12's bare-measurement audit (it must find this threshold justified) |
| C3 / refusal / floor driven arms (fire + silent) | vitest arms | `src/__tests__/drift-check-scripts.test.ts` | plan 12's corpus registry |
</artifacts_produced>

<verification>
1. `npx vitest run src/__tests__/drift-check-scripts.test.ts` — green: new C3/refusal/floor/reopen-pin arms plus every pre-existing VAC-04, SP-C05 and VAC-08 arm. Fails when: non-zero exit.
2. `npx vitest run src/__tests__/contracts` — green (src/ touched). Node-22 pass on the changed test file. Fails when: non-zero exit under either runtime.
3. `bash scripts/test-ledger-drift-check.sh --self-test` — exit 0 (proves this plan's floor work did not disturb the sibling gate it templated from). Fails when: non-zero exit.
4. Fence: this plan's commits touch only the two declared files plus `.planning/` — `git show --stat` proves it; `scripts/sql-function-names-naive.mjs`, `scripts/sql-body-normalize.mjs`, `.github/workflows/**` untouched.
5. The SUMMARY's SC-4 statement says MET (fail-closed zero path per amended D-07), names the reopen pin as the standing enforcement, and restates the migration-PR ordering constraint (hold until 164.3.1 + 164.4 both land).
</verification>

<success_criteria>
- [VAC04-C3] closed: exit >= 2 on the membership grep is a hard MEASURE_FAIL naming file, pattern
  and code — driven end-to-end through the real gate on its measured input (SC-4 contribution).
- [VAC04-C1]'s zero path FAILS CLOSED with both readers' evidence printed BEFORE the refusal
  (amended D-07) — and the closed state is pinned by an executable reopen arm that reds if the
  zero path ever returns to exiting 0 (amended D-08).
- The VAC-04 absurdity floor exists, templated on VAC-08's, threshold measured in-task with wide
  separation recorded both directions, proven able to FIRE and proven SILENT (SC-8), sample size
  and coverage beside every number (SC-9).
- SC-4 recorded as MET everywhere it is mentioned — the deferral is REVERSED (CONTEXT § Amendment
  2026-09-01), and the wedge risk is carried by the load-bearing ordering constraint: migration
  PRs held until 164.3.1 and 164.4 have both landed.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-07-SUMMARY.md`
when done. It must quote: the pre-fix fail-open grep observation and the post-fix MEASURE_FAIL
(from Task 1's landed commit `ab99ab99` — cite, do not re-derive); the before (one-line notice,
exit 0) and after (evidence block, then non-zero exit) of the zero path; the reopen pin's
two-directional proof (refusal reverted → pin RED, restored → pin green, sha-verified); the
floor's measured threshold with both separation directions; the SC-4 MET statement (amended
D-07, 2026-09-01); and the migration-PR ordering constraint (hold until 164.3.1 + 164.4 both
land) so the verifier and every downstream reader inherit it. A gate whose verdict exceeds what
it measured is the exact shape this phase exists to stop.
</output>