---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "12"
type: execute
subsystem: regression-corpus
tags: [regression-corpus, vac08, meta-arms, instance-registry, sc-1]
wave: 5
depends_on: ["164.3.1-06", "164.3.1-07", "164.3.1-08", "164.3.1-10"]
files_modified:
  - src/__tests__/drift-check-scripts.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - .github/workflows/ci.yml # conditional — task 3 edits it ONLY if the symmetry pins find a miss; declared so the wave file-overlap guard sees the 10→12 serialization
  - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-12-CORPUS-PROOFS.md
autonomous: true
requirements: [SC-1, SC-7, SC-9]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "The two VAC-08 instances are driven arms against the real test-ledger-drift-check.sh: the 253-of-262 absurd verdict (the populated-ledger-matching-under-half MEASURE_FAIL, with its matched and small-ledger controls) and the THREE-CONVENTION JOIN (a ledger row matching under each alternative naming convention — name-only, version_name, and the prefix/suffix split conventions — is NOT reported missing). Each is proven to RED by neutering its fix in the gate script (floor removed; extra OR clauses removed), observed, restored sha-verified — instance four of the Primitive-C family reds in CI, not in a review (SC-1, ROADMAP deliverable 9)."
    - "An INSTANCE→ARM REGISTRY exists as an exact-set machine check: every measured instance in this phase's taxonomy (A: R4-C01/P3, MUT-I01-P4, MUT-I01-P5; B: R4-C02, NESTED-EXECUTE; C: VAC04-C1..C4, VAC08-253, VAC08-JOIN; D: VAC-SELFREF-01, SP-C04, AUDCOV-01, MUT-W02) maps to a named arm in a named file, and the check verifies each referenced arm name exists in the referenced file's bytes — a renamed or deleted arm fails BY NAME, and an instance added to the taxonomy without an arm fails the exact set (SC-1's corpus-completeness made mechanical)."
    - "META-ARM diagnostic-first (SC-7): a machine check over the family's shell gates asserts every failure-emission block prints at least one runtime-interpolated quantity — a gate in this family cannot ship a bare conclusion — shipped with a red fixture snippet proving the rule fires and an exact-set allowlist for justified exceptions."
    - "META-ARM bare-measurement audit (SC-9): a machine check greps the family for threshold rules and asserts each carries a measured-justification comment beside it — the criterion-9 rule executed as an arm, with a planted-bare-threshold red fixture proving it can fire and a non-vacuity floor on the number of thresholds found."
    - "The frontend aggregator's needs list and result loop are confirmed SYMMETRIC for every job carrying this phase's arms, by running the standing machine pins (presence pins + the plan-08 structural parser + the population check) — a job in needs but absent from the result loop passes quietly, and branch protection gates on this aggregator (binding constraint 8)."
  artifacts:
    - "src/__tests__/drift-check-scripts.test.ts — the three-convention join arm (new) beside the standing 253-shape floor arms"
    - "src/__tests__/gate-family-meta.test.ts — the instance→arm registry + both meta-arms with their fixtures"
    - ".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-12-CORPUS-PROOFS.md — the recursive neuter→RED→restore records for the arms this plan owns"
  key_links:
    - "driven arms → scripts/test-ledger-drift-check.sh (the real gate, driven via the injected-command idiom the standing VAC-08 arms already use; the script itself is never edited except inside sha-verified neuter windows)"
    - "join conventions ← scripts/test-ledger-drift-check.sh's missing-query OR clauses (name = fname; version||'_'||name = fname; name = split_part(fname,'_',1); name = substr(fname, strpos+1)) and the convention-lineage comment at :37-70 (253 → 56 → third convention found by the shape diagnostic)"
    - "registry targets ← the arm names shipped by plans 01/02/04/06/07/08/11 (each plan's artifacts-produced table names its arms; the registry reads the files' bytes via node:fs)"
---

<objective>
Regression corpus, primitives C + D, plus the corpus's own completeness check and the family-wide
meta-arms (SC-1, SC-7, SC-9 close-out).

Three deliverables:
1. **The VAC-08 instances as driven arms.** The 253-of-262 verdict — a gate reporting a finding
   it never really measured, phrased so confidently it pointed a reader at hand-applying 253
   migrations to a SHARED database — and the three-convention join whose key was validated on 12
   rows, applied to 262, and wrong for 253. The absurdity-floor arm family already stands in
   drift-check-scripts.test.ts (the populated-ledger MEASURE_FAIL plus two controls); this plan
   adds the JOIN-CONVENTION arm (each convention proven matchable, so removing a convention
   clause reds CI instead of resurrecting the false-253) and discharges SC-1's recursion for
   both: neuter the fix in the real gate script, observe the arm RED, restore sha-verified.
2. **The instance→arm registry.** SC-1 says EVERY measured instance is an executable arm. Plans
   01-11 shipped the arms; nothing yet asserts the mapping stays complete. The registry is an
   exact-set machine check binding each instance ID to its arm by file + name, failing by name in
   both directions — the corpus's own anti-decay gate.
3. **The meta-arms.** SC-7's arm (D-12: DIAGNOSTIC-FIRST is a hard requirement — every gate in
   this family prints what it saw on failure, and the meta-arm is the machine asserting it): a
   gate in this family cannot ship a bare conclusion — checked
   over the family's failure emissions, with a red fixture. SC-9's arm: no bare thresholds — every
   floor rule carries its measurement beside it, checked with a red fixture. Both with exact-set
   allowlists and honest non-coverage statements (a static shape check sees emission structure,
   not runtime truth — the per-gate driven arms carry the runtime half).

Plus: the frontend aggregator's needs/result-loop symmetry confirmed by running the standing
machine pins for every carrying job (D-11: the regression corpus's every measured instance is an
executable arm wired into the frontend aggregator's needs list AND its result loop, so a failure
fails the aggregate rather than passing quietly).

⚠️ Anchor discipline: drift-check-scripts.test.ts was edited by plan 07 — reference its arms by
TEST NAME, never by line. test-ledger-drift-check.sh is edited by NO plan in this phase, so its
current anchors hold (floor :320-350, grep-rc :311-318, conventions comment :37-70, the
missing-query OR clauses in run_ledger_query). ci.yml was edited by plan 10 — reference it by
job/step names and drive it through the standing pins only; this plan edits ci.yml ONLY if the
symmetry confirmation finds a miss (serialized after 10, so ownership is clean).

⛔ Safety: no database is contacted — the VAC-08 arms inject commands (the standing idiom); the
gate script's real DB path is never invoked. Never any supabase CLI command.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-VALIDATION.md
@scripts/test-ledger-drift-check.sh
@src/__tests__/drift-check-scripts.test.ts
@src/__tests__/lint-sql-gates.test.ts
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL (ROADMAP deliverable 9) | VAC-08 instances carried into the corpus as executable arms — 253-of-262 and the three-convention join | Task 1 |
| REQ [SC-1] | Corpus completeness across ALL FOUR primitives; each new arm proven to RED by neutering its fix | Tasks 1-2 |
| REQ [SC-7] | The diagnostic-first meta-arm: a future gate cannot ship a bare conclusion | Task 3 |
| REQ [SC-9] | The bare-measurement audit: grep the family for justifying measurements, find no bare ones — executed as an arm | Task 3 |
| CONTEXT § Blast radius (locked) | Corpus wired into the frontend aggregator's needs AND result loop, Phase 164.3 pattern | Task 3 (symmetry confirmation via standing pins) |
| VALIDATION row "Regression corpus (criterion 1, recursive)" | Per-entry neuter/RED/restore with verbatim text + shasum | Tasks 1-2, recorded in CORPUS-PROOFS |
| Binding constraint 8 | A job in needs but absent from the result loop passes quietly | Task 3 |

Excluded (not gaps): A+B corpus entries (plan 11, parallel, file-disjoint); the per-gate fix
arms themselves (plans 04/06/07/08 shipped them with their own RED-first proofs — the registry
BINDS them, it does not re-prove them).
</source_audit>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VAC-08 instances — the three-convention join arm, plus recursive proofs for both instances against the real gate script</name>
  <files>src/__tests__/drift-check-scripts.test.ts, .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-12-CORPUS-PROOFS.md</files>
  <precondition>bash scripts/test-ledger-drift-check.sh --self-test exits 0 (the gate's own harness is green before any neuter window opens).</precondition>
  <read_first>scripts/test-ledger-drift-check.sh:20-70 (the 253-of-262 story and the convention lineage: two conventions stated, the third found by the shape diagnostic AFTER the first two cut false-missing from 253 to 56) and the run_ledger_query missing/extra queries (the four OR clauses — the join under test) and :320-350 (the absurdity floor — the fix under recursive proof); src/__tests__/drift-check-scripts.test.ts — the VAC-08 describe (locate by name: the "distinguishes a broken instrument from a drift finding" describe with its RED arm and two CONTROL arms, and the injected-command idiom every VAC-08 arm uses to avoid a real database); 164.3.1-VALIDATION.md § Anti-Vacuity Contract (byte backups, shasum re-taken after every edit, never git checkout --)</read_first>
  <behavior>
    - New arm "VAC08-JOIN: a ledger row matching under EACH convention is not reported missing": inject a ledger whose rows cover all the join's conventions for distinct repo migration names (a name-only row, a version_name-composite row, and rows matching the split conventions) → the gate reports zero missing for those names. This arm REDs if any OR clause is removed — the false-253 resurrection path.
    - Contrast sub-assertion: a row matching under NO convention is still reported missing and NAMED (the join is not so wide it matches everything — the arm is falsifiable in both directions).
    - The standing 253-shape arms (the populated-ledger MEASURE_FAIL RED arm + matched control + small-ledger control) are confirmed present by name and green — they are the VAC08-253 corpus entry; if any is missing, that is a BLOCKER, not something to quietly re-add.
  </behavior>
  <action>
    Write the join arm using the injected-command idiom the standing VAC-08 arms use (stub the
    ledger queries; never a real database). Derive the convention list by READING the
    run_ledger_query missing-query's OR clauses at execution time — the arm's fixture rows are
    built per-clause, and the arm asserts one matched name PER CLAUSE, so a future fourth
    convention automatically widens the arm's required coverage (derive-from-the-artifact, the
    house discipline). Keep the contrast sub-assertion in the same arm.

    RECURSIVE PROOFS (SC-1), each with byte backups of scripts/test-ledger-drift-check.sh
    verified by shasum -a 256, re-taken after every edit, never git checkout --:
    1. VAC08-JOIN: remove ONE convention OR clause from the missing query (the minimal named
       neuter) → run the vitest arm → REQUIRED: the join arm REDs naming the now-missing name;
       capture verbatim. Restore, shasum-verify, re-run green.
    2. VAC08-253: neuter the absurdity floor (disable its comparison) → REQUIRED: the standing
       populated-ledger RED arm fails (the MEASURE_FAIL it expects never comes — the gate
       reports the absurd drift finding instead); capture verbatim. Restore, shasum-verify,
       re-run green, and run the gate's own --self-test green.

    Open 164.3.1-12-CORPUS-PROOFS.md and record both cycles: fix neutered (file + what), verbatim
    RED, shasum pair, restore confirmation (hash AND green re-run). Task 2 appends to it.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/drift-check-scripts.test.ts</automated>
    <fails_when>exit code is non-zero or any test fails — the new join arm (both directions), the standing 253-shape arms, and every other VAC-04/VAC-08/SP-C05 arm must pass together on the restored tree</fails_when>
    <automated>bash scripts/test-ledger-drift-check.sh --self-test</automated>
    <fails_when>exit code is non-zero — the gate script is not byte-restored to working order after the neuter windows</fails_when>
  </verify>
  <acceptance_criteria>
    - The join arm builds its rows from the query's own OR clauses read at runtime (one matched name per clause) and carries the not-matching contrast; removing any clause reds it — observed in proof cycle 1
    - The 253-shape arm family is confirmed by name and its floor proven load-bearing in proof cycle 2
    - Both cycles recorded in CORPUS-PROOFS with verbatim REDs and shasum pairs; the gate script byte-identical to its pre-task state (git status --porcelain clean on it)
  </acceptance_criteria>
  <done>Instance four of the Primitive-C family — both VAC-08 shapes — reds in CI: the join cannot silently narrow and the floor cannot silently vanish.</done>
</task>

<task type="auto">
  <name>Task 2: The instance→arm registry — exact-set corpus completeness across all four primitives</name>
  <files>src/__tests__/gate-family-meta.test.ts, .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-12-CORPUS-PROOFS.md</files>
  <read_first>The artifacts-produced tables of every prior plan's PLAN/SUMMARY in this phase directory (the arm names and files the registry binds: plan 01's neuter pins, plan 02/08's SRO rule and fixtures, plan 04's vac04-reader-guards arms, plan 06's A/B/C arms, plan 07's C3/refusal/floor/reopen-pin arms, plan 11's four selftest scenarios and fixtures, this plan's task-1 arms); src/__tests__/audit-coverage.test.ts — the H_0001 exact-set discipline (both-direction equality, fail-by-name, do-not-silence message) the registry reproduces</read_first>
  <behavior>
    - The registry is an exported exact set: entries of { instance, primitive, file, armNeedle } for every measured instance ID — R4-C01/P3, MUT-I01-P4, MUT-I01-P5, R4-C02, NESTED-EXECUTE, VAC04-C1, VAC04-C2, VAC04-C3, VAC04-C4, VAC08-253, VAC08-JOIN, VAC-SELFREF-01, SP-C04, AUDCOV-01, MUT-W02 — where armNeedle is a distinctive string from the arm's name (a test title, a scenario header, or a fixture filename).
    - The check reads each referenced file via node:fs (NUL-safe; never shell grep — this repo carries a measured NUL-blind test file) and asserts the needle exists in its bytes; a missing needle fails naming the instance and the file.
    - The set is exact in BOTH directions against the phase taxonomy list (declared beside the registry with its source citation): an instance without an entry and an entry without an instance both fail by name, with a do-not-silence message.
    - A non-vacuity self-check: the registry's own needles are verified non-trivial (minimum length, no empty strings) — a needle that matches everything binds nothing.
  </behavior>
  <action>
    Create src/__tests__/gate-family-meta.test.ts with the registry from the behavior block.
    Populate armNeedle values by READING each target file first and quoting a stable distinctive
    string (test titles and scenario headers; for fixtures, the filename checked via existsSync).
    Where an instance's arm lives in a selftest scenario (the plan-11 entries), the needle is the
    scenario's header text in run.mjs plus the fixture path. For SP-C04, bind to the SRO red
    fixture and the blocking rule (the same-block-const shape is the instance; plan 02's
    calibration record is the citation). Each entry carries a one-line citation comment (which
    plan shipped it, which record proves it can fail — plan 11's and this plan's CORPUS-PROOFS,
    plans 04/06/07/08's SUMMARY evidence blocks). The registry's header states what it does and
    does NOT check: it binds names to files so the corpus cannot silently decay; it does not
    re-prove the arms fail (the recursive proofs live in the committed records it cites).

    Append the registry's own fire-proof to CORPUS-PROOFS: temporarily rename one needle in the
    registry (not the target file) → the check REDs naming the instance → restore (in-memory edit
    of the test file, byte-backup + shasum discipline) → green. Record verbatim.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/gate-family-meta.test.ts</automated>
    <fails_when>exit code is non-zero or any test fails — every registry needle must resolve in its file and the exact-set equality must hold in both directions</fails_when>
    <automated>bash -c 'for id in "R4-C01" "MUT-I01" "R4-C02" "NESTED" "VAC04-C1" "VAC04-C2" "VAC04-C3" "VAC04-C4" "VAC08" "SELFREF" "SP-C04" "AUDCOV-01" "MUT-W02" ; do grep -aq "$id" src/__tests__/gate-family-meta.test.ts || { echo "missing $id"; exit 1; }; done'</automated>
    <fails_when>exits non-zero printing the missing instance ID — the registry does not cover the full phase taxonomy; a silently dropped instance is exactly what this check exists to prevent</fails_when>
  </verify>
  <acceptance_criteria>
    - Every instance in the taxonomy has a registry entry whose needle resolves in its declared file; both-direction exact-set equality enforced with fail-by-name messages
    - Every entry carries its citation comment (shipping plan + the record proving the arm can fail)
    - The registry's own fire-proof (renamed needle → RED naming the instance → restore) is recorded in CORPUS-PROOFS
  </acceptance_criteria>
  <done>The corpus has an anti-decay gate: renaming, deleting, or forgetting any measured instance's arm fails CI by instance name.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Meta-arms — diagnostic-first (SC-7) and the bare-measurement audit (SC-9) — plus aggregator symmetry confirmation</name>
  <files>src/__tests__/gate-family-meta.test.ts</files>
  <read_first>scripts/test-ledger-drift-check.sh:320-350 (the exemplar both meta-arms must PASS: a failure block interpolating its numbers, a threshold with its wide-separation measured comment) and scripts/prod-body-drift-check.sh (the plan-07 floor and fail-closed zero-path evidence blocks — further green exemplars); scripts/mutation-runner/run.mjs — the floor constants' measured comments (plan 09's re-cut shape); src/__tests__/lint-sql-gates.test.ts — the needs+loop presence pins, the plan-08 structural tolerance parser, and the population-derivation check (the standing pins task 3 runs for the symmetry confirmation — locate by test name)</read_first>
  <behavior>
    - Diagnostic-first arm: over the declared family file set (scripts/test-ledger-drift-check.sh, scripts/prod-body-drift-check.sh — the shell gates whose failure output is their only interface), every failure-emission block (contiguous error-line groups and fail-helper call sites) contains at least one runtime-interpolated value (a shell variable expansion in the emitted text). Exceptions live in an exact-set allowlist with per-entry justifications. A RED fixture (an embedded bare-conclusion error block string) is flagged by the same predicate — the rule can fire.
    - Bare-measurement audit arm: over the family set plus scripts/mutation-runner/run.mjs, every threshold site (floor constants and numeric comparison rules in floor/absurdity blocks, located by the family's own naming conventions — FLOOR tokens and the absurdity-block markers) has a measured-justification comment within its surrounding block (tokens indicating measurement and a date). A RED fixture (a planted bare-threshold snippet) is flagged — the rule can fire. A non-vacuity floor asserts the scan FOUND at least the known threshold count, measured in this task and recorded beside the floor (the audit auditing itself).
    - Symmetry confirmation: the standing lint-sql-gates pins all green (presence in needs AND result loop for each aggregated job; tolerance posture per the structural parser; population equality) — executed here as this plan's gate, with any red a BLOCKER (fix in ci.yml is in-scope, serialized after plan 10).
  </behavior>
  <action>
    Implement both meta-arms in gate-family-meta.test.ts. Read family files via node:fs. Keep
    both predicates deliberately structural and state their honest bounds in the file header:
    they check emission SHAPE (a variable expansion present; a measured-comment present), not
    runtime truth — the runtime half lives in the per-gate driven arms (cite the registry). The
    family file list is an exported constant with a comment stating the inclusion rule (shell
    gates whose only interface is their output; the runner's floor constants) so a future gate
    author knows to add theirs — and the diagnostic-first arm's allowlist starts EMPTY if the
    family is already clean (measure first; if a block legitimately carries no quantity, that is
    an allowlist entry with a justification, not a rule change).

    Fixture discipline: both red fixtures are embedded string constants clearly labeled as
    fixtures INSIDE the meta test file, fed to the same predicate functions the real scan uses —
    the SP-L02 discipline (same code path, mutilated input). Note: these fixture strings are the
    known, accepted exception to the same-block-const shape — they are inputs to the predicate
    under test, not assertion subjects; confirm the SRO blocking scan (plan 08) does not flag
    them (subjects here are predicate call results), and if it does, route via the SRO allowlist
    with justification rather than weakening either rule.

    TDD order: red fixtures + predicates first (observe both rules fire on their fixtures), then
    the family scan (observe green or populate allowlists with justified entries), then the
    non-vacuity floor with its in-task measured count.

    Run the symmetry confirmation (the standing pins) and record the result in the SUMMARY. If
    any pin reds: fix the asymmetry in ci.yml (this plan is serialized after plan 10 and owns
    ci.yml conditionally for exactly this), re-run, and record what was found — a quiet-pass job
    is the defect class this phase exists to close.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/gate-family-meta.test.ts src/__tests__/lint-sql-gates.test.ts && npx vitest run src/__tests__/contracts</automated>
    <fails_when>any run exits non-zero — a meta-arm red (a family gate ships a bare conclusion or a bare threshold), a fixture failed to fire its rule, a symmetry pin red, or a contracts scan reddened</fails_when>
    <automated>bash -c 'grep -ac "measured\|MEASURED" src/__tests__/gate-family-meta.test.ts'</automated>
    <fails_when>exits non-zero or prints 0 — the meta file's own non-vacuity floor carries no measured count; the audit would be auditing by taste</fails_when>
  </verify>
  <acceptance_criteria>
    - Both meta-arms exist with red fixtures proven to fire through the same predicate the real scan uses; allowlists exact-set with per-entry justifications (or empty with the clean measurement recorded)
    - The bare-measurement audit found the known thresholds (count measured in-task, recorded beside its floor) and every one carries its justification — SC-9's grep executed as a permanent arm
    - The symmetry confirmation ran the standing pins green (or the found-and-fixed asymmetry is recorded); every job carrying this phase's arms is in BOTH the needs list and the result loop
    - The file header states both predicates' honest bounds and cites the registry for the runtime half
  </acceptance_criteria>
  <done>A future gate in this family that ships a bare conclusion or a bare threshold fails CI, the corpus cannot silently decay, and every carrying job is proven inside the aggregate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future gate authors → the family's evidentiary standard | the meta-arms are the machine form of the phase's strongest measured lesson (the shape diagnostic that found two conventions four review rounds missed) |
| ci.yml job set → branch protection | a job outside the aggregator's result loop is advisory at merge; the symmetry pins are the control |
| the registry → corpus completeness | without it, SC-1's "every instance" claim decays silently as files are refactored |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-12-01 | Tampering | a join-convention clause silently removed, resurrecting the false-253 | high | mitigate | the derive-from-the-query join arm — one matched name per clause — proven RED under clause removal (task 1) |
| T-164.3.1-12-02 | Tampering | corpus decay: an instance's arm renamed or deleted without notice | medium | mitigate | the exact-set instance→arm registry failing by instance name, with its own fire-proof (task 2) |
| T-164.3.1-12-03 | Repudiation | a future family gate shipping bare conclusions or taste-based thresholds | medium | mitigate | both meta-arms with red fixtures and exact-set allowlists (task 3) |
| T-164.3.1-12-04 | Tampering | a carrying job quietly outside the aggregator's result loop | high | mitigate | the standing presence/parser/population pins executed as this plan's gate; any red is a blocker fixed in ci.yml (task 3) |
| T-164.3.1-12-SC | Tampering (supply chain) | npm installs | low | accept | zero packages installed; predicates are hand-rolled over node:fs reads per the family's no-new-deps rule |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 12)

| Artifact | Kind | Location | Consumers |
|---|---|---|---|
| VAC08-JOIN arm (per-clause coverage + contrast) | driven vitest arm | `src/__tests__/drift-check-scripts.test.ts` | CI vitest shards; the registry |
| Instance→arm registry (exact set, 15 instances, fail-by-name) | exported set + machine check | `src/__tests__/gate-family-meta.test.ts` | phase verifier (SC-1 completeness); every future refactor of the family |
| Diagnostic-first meta-arm + red fixture + allowlist | machine check | `src/__tests__/gate-family-meta.test.ts` | SC-7's permanent enforcement |
| Bare-measurement audit meta-arm + red fixture + non-vacuity floor | machine check | `src/__tests__/gate-family-meta.test.ts` | SC-9's permanent enforcement |
| `164.3.1-12-CORPUS-PROOFS.md` | committed recursive-proof record (VAC08 cycles + registry fire-proof) | phase directory | phase verifier (SC-1) |
</artifacts_produced>

<verification>
1. `npx vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/lint-sql-gates.test.ts` — green. Fails when: non-zero exit.
2. `bash scripts/test-ledger-drift-check.sh --self-test` — exit 0 (gate script byte-restored). Fails when: non-zero exit.
3. `npx vitest run src/__tests__/contracts` — green; Node-22 pass on both changed test files. Fails when: non-zero exit under either runtime.
4. CORPUS-PROOFS committed covering: both VAC-08 cycles + the registry fire-proof, each with verbatim RED and shasum pair.
5. Phase-gate note for the verifier: SC-4 is MET (amended D-07, 2026-09-01 — the zero path fails closed in plan 07, pinned by the standing reopen arm); this plan's registry and meta-arms do not re-prove it — plan 07's record carries the evidence, and the migration-PR ordering constraint (hold until 164.3.1 + 164.4 both land) travels with it.
</verification>

<success_criteria>
- SC-1 closed corpus-wide: the VAC-08 instances are driven arms proven able to fail; the
  instance→arm registry makes "every measured instance is an executable arm" a machine-checked
  invariant across all four primitives, with every recursive proof on record in committed
  artifacts (plans 04/06/07/08 evidence blocks, plan 11's and this plan's CORPUS-PROOFS).
- SC-7 closed as an arm: a family gate that fails without printing what it saw reds CI, proven
  able to fire.
- SC-9 closed as an arm: the family greps clean of bare thresholds, the audit carries its own
  measured floor, proven able to fire.
- Aggregator symmetry confirmed for every carrying job via the standing machine pins.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-12-SUMMARY.md`
when done. It must: name the join arm and quote its clause-removal RED; list the registry's 15
instances with their arm files; quote both meta-arms' fixture-fire observations; record the
symmetry confirmation result; and note SC-4 MET (amended D-07 — the fail-closed zero path with
its reopen pin live in plan 07) so the phase verifier reads the criterion from plan 07's record,
not from the withdrawn PARTIAL instruction.
</output>