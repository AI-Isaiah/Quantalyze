---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "13"
subsystem: vac04-gate
tags: [vac04, gap-closure, sc-4, gate-level, realpath-guard, charset-refusal, instance-registry, anti-vacuity]

# Dependency graph
requires:
  - phase: 164.3.1-04
    provides: "the two hardened readers (realpath main-module guard, charset refusal) and the reader-level arms this plan lifts to gate level"
  - phase: 164.3.1-07
    provides: "the standing [VAC04-C1] / [VAC04-C3] gate-level arms, the D-13 tripwire and the scaffold shape the new describes mirror"
  - phase: 164.3.1-12
    provides: "INSTANCE_ARM_REGISTRY with its `also:` binding shape and the binds-by-name / non-vacuity meta-arms"
provides:
  - "src/__tests__/drift-check-scripts.test.ts: `[VAC04-C2] GATE-LEVEL` describe — fixture calibration, four fixed-leg cases (naive/normalizer × symlink/space) driving scripts/prod-body-drift-check.sh to the readers-ran line, and a standing pre-fix-guard calibration leg producing the D-13 blind-zero refusal"
  - "src/__tests__/drift-check-scripts.test.ts: `[VAC04-C4] GATE-LEVEL` describe — the real gate exits 1 on `public.fúnc_é` naming U+00FA and file:line with no body text; a standing both-refusals-disabled calibration leg reproducing the wrong-subject pass; the stated bound as its own `it`"
  - "src/__tests__/gate-family-meta.test.ts: VAC04-C2 / VAC04-C4 registry entries bound at BOTH levels (reader-level kept, gate-level via `also:`), with the level visible in the entry"
  - "This SUMMARY: recorded cycles C2-N1, C2-N2, C4-N1 and the registry fire-proof with verbatim REDs, RED sets and sha256 pairs"
affects: [164.4, VAC-04, SC-4, migration-drift-check, phase-verification]

# Actuals (#2632) — chars/4 over `git diff 4bce215f..HEAD` (2 files, 503 insertions / 3 deletions, 29,489 chars)
actuals:
  tokens: 7372
  tasks: 3
  commits: 3

tech-stack:
  added: []
  patterns:
    - "Gate-level arms use per-reader-visible fixtures so each union member is individually load-bearing — on a both-visible definition the union masks a single reader's silent zero"
    - "A gate-level arm's pass condition is the wiring's positive evidence line (readers-ran), never a downstream refusal that another control produces (D-13)"
    - "Scratch-copy neuters are proven applied by ABSENCE of the replaced text, never by source≠copy, so the standing calibration leg stays green under a recorded neuter of the real source"
    - "Registry entries carry a READER-LEVEL / GATE-LEVEL comment per binding so the distinction is visible in the entry, not only in a citation"

key-files:
  created:
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-13-SUMMARY.md
  modified:
    - src/__tests__/drift-check-scripts.test.ts
    - src/__tests__/gate-family-meta.test.ts

key-decisions:
  - "The C2 fixed leg's pass condition is `Functions defined or replaced by this PR: 1` and the case additionally asserts `<name>: measured absent` and the absence of MEASURE_FAIL; the D-13 refusal is asserted only in the calibration leg as the NEUTERED outcome — otherwise the arm would prove C1's tripwire, not the guard fix"
  - "Fixture A (`sanitize_user$v2` at line start, naive-only) and fixture B (`SELECT 1; CREATE OR REPLACE FUNCTION public.mid_fn…` one line, normalizer-only) were calibrated by reader stdout FIRST in the describe, because a both-visible definition would let the fixed leg pass with one reader silent and no single-member neuter could RED"
  - "The C4 stated bound (normalizer-only neuter + REAL naive → still exit 1 naming U+00FA) is its own `it`, separate from the calibration leg: it depends on the real naive reader and therefore REDs under C4-N1 while the copies-only calibration leg stays green — folding them would blur the RED set"
  - "C4 scratch copies are proven neutered by absence of the replaced refusal text, deliberately not by `source !== copy`, so under C4-N1 (real sources already neutered, copies byte-identical) the calibration leg cannot RED for a reason unrelated to the gate — confirmed: it stayed green in the recorded cycle"
  - "The recorded C4 neuter disables BOTH readers' refusals: measured, a normalizer-only neuter still exits 1 through :221's `|| fail` (the naive refusal reaches the verdict), so a single-member neuter cannot flip the gate"
  - "No TDD RED commit was made for these test-only tasks: the behaviour under test already stands in the tree, so a committed RED would have meant committing a neutered reader, which the scope fence forbids. The RED gates are the recorded neuter cycles below (see § TDD Gate Compliance)"

patterns-established:
  - "Gate-level lift of a reader-level instance: same input, injected reader paths, assert the gate's own evidence lines; standing calibration leg from scratch copies + recorded neuter of the real files"

requirements-completed: [SC-4]

coverage:
  - id: D1
    description: "[VAC04-C2] driven through the real gate: a reader reached through a symlink or a space path RUNS, the gate reaches `Functions defined or replaced by this PR: 1`, each union member individually load-bearing; the pre-fix guard idiom turns the same wiring into `0 name(s)` twice + MEASURE_FAIL (standing calibration leg + recorded cycles C2-N1/C2-N2)"
    requirement: "SC-4"
    verification:
      - kind: unit
        ref: "src/__tests__/drift-check-scripts.test.ts#[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH THE REAL GATE (6 arms: CALIBRATION, FIXED LEG cases 1-4, CALIBRATION LEG)"
        status: pass
      - kind: other
        ref: "recorded cycles C2-N1 / C2-N2 in this SUMMARY (RED sets exactly the two load-bearing cases + the by-name pin)"
        status: pass
    human_judgment: false
  - id: D2
    description: "[VAC04-C4] driven through the real gate: `public.fúnc_é(p uuid)` makes scripts/prod-body-drift-check.sh exit 1 naming U+00FA, `leaves the unquoted charset`, `<migration>:2:`, `read 'public.f'`, with no body text; with both refusals disabled the gate counts `f`, measures it absent and passes (standing calibration leg + recorded cycle C4-N1)"
    requirement: "SC-4"
    verification:
      - kind: unit
        ref: "src/__tests__/drift-check-scripts.test.ts#[VAC04-C4] GATE-LEVEL — the charset refusal driven THROUGH THE REAL GATE (3 arms: REFUSAL, CALIBRATION LEG, STATED BOUND)"
        status: pass
      - kind: other
        ref: "recorded cycle C4-N1 in this SUMMARY (RED set exactly {REFUSAL, STATED BOUND}, calibration leg green)"
        status: pass
    human_judgment: false
  - id: D3
    description: "INSTANCE_ARM_REGISTRY binds VAC04-C2 and VAC04-C4 at both levels by name; a renamed gate-level needle REDs the instance's binds-by-name case"
    requirement: "SC-4"
    verification:
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#$instance (primitive $primitive) → $file binds by name (VAC04-C2, VAC04-C4) + non-vacuity + EXACT SET"
        status: pass
      - kind: other
        ref: "registry fire-proof in this SUMMARY (GATE→GATF in the C2 `also` needle → exactly that case RED; restore sha-identical)"
        status: pass
    human_judgment: false

# Metrics
duration: ~16 min (first tool call ≈ 2026-09-02T08:44Z; last task commit 2026-09-02T08:59:48Z)
completed: 2026-09-02
status: complete
---

# Phase 164.3.1 Plan 13: Gap closure — [VAC04-C2] and [VAC04-C4] driven through the real gate Summary

**[VAC04-C2] and [VAC04-C4] now have gate-level arms that spawn `scripts/prod-body-drift-check.sh` itself — a symlinked / space-path reader reaches the gate's readers-ran line with each union member individually load-bearing, and `public.fúnc_é` makes the gate exit 1 naming `U+00FA` with no body text — each proven able to fail by a standing scratch-copy calibration leg AND a recorded neuter of the real readers, and both bound at both levels in INSTANCE_ARM_REGISTRY.**

## Performance

- **Duration:** ~16 min
- **Started:** ≈ 2026-09-02T08:44Z (first read; not stamped precisely)
- **Completed:** 2026-09-02T08:59:48Z (last task commit)
- **Tasks:** 3
- **Files modified:** 2 (both test files) + this SUMMARY

## Accomplishments

- SC-4 is now MET by mechanism for all four members: [VAC04-C1] and [VAC04-C3] by the standing plan-07 arms, [VAC04-C2] and [VAC04-C4] by the two new describes, every one of which invokes `scripts/prod-body-drift-check.sh` (never a reader CLI as the subject — readers are spawned only to calibrate fixtures).
- Every new arm fails two ways: a standing calibration leg in the same describe (scratch reader copies carrying the pre-fix idiom drive the SAME gate wiring to the opposite outcome on every CI run) and a recorded neuter → RED → restore cycle on the REAL readers with RED-set specificity and sha256 pairs (below).
- The registry binds VAC04-C2 / VAC04-C4 to both the reader-level arm (plan 04, kept) and the gate-level arm (this plan) via `also:`, with the level visible in the entry; the fire-proof shows one altered character in a gate-level needle REDs exactly that instance's binds-by-name case.
- Scope fence intact: gate script, both readers and every plan 01-12 artifact are byte-identical to HEAD `4bce215f` (`git diff --quiet` rc=0 at the end of every task); no database was contacted (injected commands and mkdtemp fixtures only; no supabase CLI command).

## Task Commits

Each task was committed atomically:

1. **Task 1: Gate-level [VAC04-C2]** — `6c23ae13` (test)
2. **Task 2: Gate-level [VAC04-C4]** — `b8d79162` (test)
3. **Task 3: Registry `also:` bindings + fire-proof + cross-cutting gates** — `dc47efd4` (test)

**Plan metadata:** the `docs(164.3.1-13)` commit that carries this SUMMARY (see git log).

## The two new describes and every `it` inside them

**`[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH THE REAL GATE: a reader reached through a symlink or a space path must RUN`** (drift-check-scripts.test.ts, directly after the `[VAC04-C1]` describe)

1. `CALIBRATION: fixture A is visible to the NAIVE reader only, fixture B to the NORMALIZER only — each case has ONE load-bearing member`
2. `FIXED LEG case 1: NAIVE reader load-bearing through a SPACE path (normalizer through a symlink) — fixture A: the real gate reaches the readers-ran line`
3. `FIXED LEG case 2: NAIVE reader load-bearing through a SYMLINK (normalizer through a space path) — fixture A: …`
4. `FIXED LEG case 3: NORMALIZER load-bearing through a SYMLINK (naive through a space path) — fixture B: …`
5. `FIXED LEG case 4: NORMALIZER load-bearing through a SPACE path (naive through a symlink) — fixture B: …`
6. `CALIBRATION LEG (standing RED direction): readers carrying the PRE-FIX guard, reached through symlinks, give ZERO names and the MEASURE_FAIL refusal — the readers-ran line never prints`

**`[VAC04-C4] GATE-LEVEL — the charset refusal driven THROUGH THE REAL GATE: a non-ASCII identifier must be REFUSED, never compared as a different function`** (directly after the C2 describe)

1. `` REFUSAL: the real gate exits 1 on `public.fúnc_é`, naming U+00FA and the file:line — and prints NO body text, no readers-ran line, no verdict ``
2. `` CALIBRATION LEG (standing RED direction): with BOTH refusals disabled, the gate counts a truncated `f`, measures it absent and PASSES — the wrong-subject pass ``
3. `STATED BOUND: with only the normalizer's refusal disabled, the REAL naive reader's refusal still reaches the verdict through :221 — exit 1 naming U+00FA under its own prefix`

Every gate-spawning `it` carries an explicit `30_000` timeout. Fixture constants live at describe scope; no `expect()` subject is a same-block string-literal const (SRO summary unchanged at 2 findings, below).

## Planning-time measurements reproduced at HEAD (scratchpad, injected commands, no database)

- Fixture A (`CREATE OR REPLACE FUNCTION public.sanitize_user$v2(p uuid) …` at line start): normalizer stdout empty / naive `sanitize_user$v2`, both exit 0. Fixture B (`SELECT 1; CREATE OR REPLACE FUNCTION public.mid_fn(a int) …` one line): normalizer `mid_fn` / naive empty, both exit 0.
- Fixed readers, naive via a copy under `reader copies/` + normalizer via `links/…` (and the mirror shapes): `Functions defined or replaced by this PR: 1`, `  sanitize_user$v2: measured absent — not in the PROD source's 1-name index. Treated as a NEW function (pass).` (resp. `mid_fn: measured absent`), `::notice::… no unacknowledged repo-vs-PROD body drift.`, exit 0. Fixture-A runs additionally print `::warning::… the independent name reader found function definition(s) the normalizer's parser did not:` / `::warning::  sanitize_user$v2`.
- Scratch copies with the pre-fix `import.meta.url === \`file://${process.argv[1]}\`` guard (both comparison lines replaced; copies carry 0 realpath idioms / 2 pre-fix idioms), reached through symlinks, fixture A and B:
  ```
  ::error::VAC-04 repo-vs-PROD function-body drift gate: MEASURE_FAIL — NOTHING WAS COMPARED.
  ::error::  <tmp>/neutlinks/sql-body-normalize.mjs --function-names -> 0 name(s)
  ::error::    (none)
  ::error::  <tmp>/neutlinks/sql-function-names-naive.mjs -> 0 name(s)
  ::error::    (none)
  ::error::  crude textual scan for 'CREATE [OR REPLACE] FUNCTION' -> HIT in: …
  ```
  exit 1 — the D-13 blind-zero refusal, observed as the RED direction, never used as a pass condition.
- Real readers on the P10 migration through the gate:
  ```
  ::error::sql-body-normalize: <tmp>/20260902120000_p10.sql:2: identifier leaves the unquoted charset [A-Za-z0-9_$] — read 'public.f' then hit 'ú' (U+00FA). Refusing rather than dropping: …
  ::error::VAC-04 repo-vs-PROD function-body drift gate: could not extract function names from the changed migrations.
  ```
  exit 1; the body sentinel `ZZ_VAC04_BODY_SENTINEL_ZZ` is absent from the output.
- Scratch copies with BOTH refusals disabled on P10: `Functions defined or replaced by this PR: 1`, `  f: measured absent — … Treated as a NEW function (pass).`, `no unacknowledged repo-vs-PROD body drift`, exit 0 — the wrong-subject pass.
- Normalizer-only disabled + REAL naive on P10: `::error::sql-function-names-naive: <tmp>/…_p10.sql:2: identifier leaves the unquoted charset [A-Za-z0-9_$] — read 'public.f' then hit 'ú' (U+00FA). …` then `::error::…: the independent name reader failed on the changed migrations.`, exit 1 — so the recorded C4 neuter must disable both members.

## Recorded neuter → RED → restore cycles (REAL readers; byte backups via `cp`, sha256 after every edit, never `git checkout --`)

Pre-cycle sha256 of the real readers (identical to HEAD `4bce215f`):
- `scripts/sql-function-names-naive.mjs` `c844ed06aff760611fffd7c96b8c49c6f15e7ca0fca6744a4e4d01a6773abe03`
- `scripts/sql-body-normalize.mjs` `226969cdc60cf8bfdda4dd29b49328617b2de75f70795fac44184bb8d269f87e`

### Cycle C2-N1 — naive reader ONLY, realpath guard → pre-fix URL-string idiom

Neuter: in `scripts/sql-function-names-naive.mjs`, both comparison lines inside `invokedDirectly()` (`return realpathSync(process.argv[1]) === fileURLToPath(import.meta.url);` and the `resolve(...)` fallback) replaced by `` return import.meta.url === `file://${process.argv[1]}`; `` — realpath idiom count 0. Neutered sha256 `fd29f93bfdb82759d0998566a86ec9beb5f1d18d9bce778cba9b88efc5f62c0e`.

Run: `npx vitest run src/__tests__/drift-check-scripts.test.ts -t "VAC04-C2"` → exit 1, `Tests  3 failed | 3 passed | 280 skipped (286)`. RED set exactly {case 1, case 2, calibration leg}; CALIBRATION, case 3 and case 4 green. Verbatim:

```
 × FIXED LEG 'case 1: NAIVE reader load-bearing thr…': the real gate reaches the readers-ran line 298ms
 × FIXED LEG 'case 2: NAIVE reader load-bearing thr…': the real gate reaches the readers-ran line 289ms
 × CALIBRATION LEG (standing RED direction): readers carrying the PRE-FIX guard, reached through symlinks, give ZERO names and the MEASURE_FAIL refusal — the readers-ran line never prints 1ms
AssertionError: the gate did not pass — a reader reached through a space/symlink path did not RUN
::error::VAC-04 repo-vs-PROD function-body drift gate: MEASURE_FAIL — NOTHING WAS COMPARED.
::error::have passed here with a notice; this one did not.
: expected 1 to be +0 // Object.is equality
AssertionError: the gate did not pass — a reader reached through a symlink/space path did not RUN
::error::VAC-04 repo-vs-PROD function-body drift gate: MEASURE_FAIL — NOTHING WAS COMPARED.
: expected 1 to be +0 // Object.is equality
AssertionError: scripts/sql-function-names-naive.mjs no longer carries the realpath guard `realpathSync(process.argv[1])` — [VAC04-C2] is reopened in the real reader: expected 0 to be greater than or equal to 1
```

Restore: `cp` from the byte backup → sha256 `c844ed06…` (identical to pre); `git diff --quiet HEAD -- scripts/sql-function-names-naive.mjs` rc=0.

### Cycle C2-N2 — normalizer ONLY, the same neuter

Neuter: `scripts/sql-body-normalize.mjs` `invokedDirectly()` both comparison lines → pre-fix idiom; realpath idiom count 0. Neutered sha256 `242cd69c7dc4408845e4d2e90b35a442f02aaf47bc7f6363546f1cfd8d516880`.

Run: same command → exit 1, `Tests  3 failed | 3 passed | 280 skipped (286)`. RED set exactly {case 3, case 4, calibration leg}; CALIBRATION, case 1 and case 2 green. Verbatim:

```
 × FIXED LEG 'case 3: NORMALIZER load-bearing throu…': the real gate reaches the readers-ran line 291ms
 × FIXED LEG 'case 4: NORMALIZER load-bearing throu…': the real gate reaches the readers-ran line 295ms
 × CALIBRATION LEG (standing RED direction): readers carrying the PRE-FIX guard, reached through symlinks, give ZERO names and the MEASURE_FAIL refusal — the readers-ran line never prints 1ms
AssertionError: the gate did not pass — a reader reached through a space/symlink path did not RUN
: expected 1 to be +0 // Object.is equality
AssertionError: the gate did not pass — a reader reached through a symlink/space path did not RUN
: expected 1 to be +0 // Object.is equality
AssertionError: scripts/sql-body-normalize.mjs no longer carries the realpath guard `realpathSync(process.argv[1])` — [VAC04-C2] is reopened in the real reader: expected 0 to be greater than or equal to 1
```

Restore: `cp` → sha256 `226969cd…` (identical); `git diff --quiet` rc=0. Post-restore: full file `npx vitest run src/__tests__/drift-check-scripts.test.ts` → `Tests  286 passed (286)`; `node scripts/sql-function-names-naive.mjs --self-test` → `self-test OK (13 checks)`; `node scripts/sql-body-normalize.mjs --self-test` → `self-test OK (9 checks)`.

### Cycle C4-N1 — BOTH readers, charset refusal disabled

Neuter: naive.mjs:171 `if (follower !== undefined && !/\s/.test(follower) && follower !== "(") {` → `if (false) {`; normalize.mjs:369 `throw charsetRefusal(sql, j, …);` → `continue;`. Neutered sha256: naive `f5bdd4135ab4515859666b11234bdde8642dae036682844c21dd1fcf768c1195`, normalizer `23c8c3da6e069697698d9def4cb1cda3476a776f576623df964ee72c70b37176`. Under the neuter the readers' own self-tests fail their +1 charset check (`SELF-TEST FAIL: an UNQUOTED identifier leaving [A-Za-z0-9_$] must be REFUSED, not truncated to \`f\` …` / `… not dropped …`) — the restore proof below is those counts returning.

Run: `npx vitest run src/__tests__/drift-check-scripts.test.ts -t "VAC04-C4"` → exit 1, `Tests  2 failed | 1 passed | 286 skipped (289)`. RED set exactly {REFUSAL, STATED BOUND}; the CALIBRATION LEG stayed GREEN — expected, not a defect: its copies are derived by absence from an already-neutered source, come out byte-identical, and the gate still takes the wrong-subject pass. Verbatim:

```
 × REFUSAL: the real gate exits 1 on `public.fúnc_é`, naming U+00FA and the file:line — and prints NO body text, no readers-ran line, no verdict 834ms
 × STATED BOUND: with only the normalizer's refusal disabled, the REAL naive reader's refusal still reaches the verdict through :221 — exit 1 naming U+00FA under its own prefix 820ms
AssertionError: the gate did not refuse a non-ASCII identifier
: expected +0 to be 1 // Object.is equality
AssertionError: the naive refusal did not reach the gate's verdict
: expected +0 to be 1 // Object.is equality
```

The captured gate output inside the REFUSAL assertion carries `f: measured absent` (2 occurrences in the run log) and no `U+00FA`.

Restore: `cp` both → sha256 naive `c844ed06…`, normalizer `226969cd…` (identical to pre); `git diff --quiet HEAD -- <both>` rc=0; self-tests `13 checks` / `9 checks`; full file → `Tests  289 passed (289)`.

### Registry fire-proof (gate-family-meta.test.ts; byte backup, sha256)

Pre sha256 `a0f69d90d8c752d5ebcf7c9c8b1a106126220330ee58554bbe527c4a66b30dab`. Altered ONE character inside the new VAC04-C2 `also` needle in the REGISTRY (`…THROUGH THE REAL GATE` → `…THROUGH THE REAL GATF`; target file untouched) → sha256 `b85179630b987f1cfedcaa54cf2b8f527b122295b39b52a829ef2d7bc39c4869`. Run `npx vitest run src/__tests__/gate-family-meta.test.ts` → exit 1, `Tests  1 failed | 20 passed (21)`:

```
 × 'VAC04-C2' (primitive 'C') → 'src/__tests__/vac04-reader-guards.tes…' binds by name 4ms
AssertionError: VAC04-C2: its arm "[VAC04-C2] GATE-LEVEL — the realpath guard driven THROUGH THE REAL GATF" is no longer in src/__tests__/drift-check-scripts.test.ts. The arm was renamed or deleted. If renamed, update the needle HERE with the new title; if deleted, the corpus lost a measured instance — restore it (see 164.3.1-04 task 1; proof: 164.3.1-04-SUMMARY.md § Pre-fix RED / [VAC04-C2] + 164.3.1-13 task 1 (gate-level arm through scripts/prod-body-drift-check.sh); proof: 164.3.1-13-SUMMARY.md § cycles C2-N1/C2-N2). Do NOT drop the registry entry.: expected 0 to be greater than or equal to 1
```

Every other registry case (including VAC04-C4's) green. Restore: `cp` → sha256 `a0f69d90…` (identical), `GATF` count 0, re-run `Tests  21 passed (21)`.

## Cross-cutting gate results (all on the restored tree, serialized — never two concurrently)

| Gate | Result |
|---|---|
| `npx vitest run src/__tests__/drift-check-scripts.test.ts` | 289 passed (280 at HEAD + 9 new) |
| `npx vitest run src/__tests__/gate-family-meta.test.ts` | 21 passed; exact set unchanged (15 instances); both new needles resolve; citations pass both regexes |
| Task 3 bracket (`grep -aF` both `GATE-LEVEL` needles in the registry + `also: [` ≥ 6) | rc=0 (`also: [` count 6; measured 4 pre-edit) |
| `npx vitest run src/__tests__/contracts src/__tests__/self-referential-oracle.test.ts src/__tests__/vac04-reader-guards.test.ts` | 7 files / 130 passed; `SELF-REF-ORACLE blocking summary: 2 finding(s) in 1 file(s), 131 file(s) scanned` (unchanged); vac04-reader-guards 15 passed |
| Node 22 (`/opt/homebrew/opt/node@22/bin`, `v22.22.1`) on both changed files | 2 files / 310 passed |
| `npx tsc --noEmit` | clean (rc=0) |
| `npx eslint src/__tests__/drift-check-scripts.test.ts src/__tests__/gate-family-meta.test.ts` | clean (rc=0) |
| Reader self-tests | `sql-function-names-naive self-test OK (13 checks)`, `sql-body-normalize self-test OK (9 checks)` |
| Fence `git diff --quiet HEAD -- scripts/prod-body-drift-check.sh scripts/sql-function-names-naive.mjs scripts/sql-body-normalize.mjs .planning/…/164.3.1-04-PLAN.md …-07-PLAN.md …-12-PLAN.md` | rc=0 at the end of every task |
| `git show --stat` of the three task commits | only `src/__tests__/drift-check-scripts.test.ts` and `src/__tests__/gate-family-meta.test.ts`; no gate script, reader, workflow or plan 01-12 file |
| Full vitest suite | NOT run in this plan (file-scoped runs are the per-task gate per the plan's verification note 8; the orchestrator's protocol decides whether to run the full suite ALONE on the box before hand-off) |

## SC-4 statement

**SC-4 is MET by mechanism for all four members.** [VAC04-C1] (fail-closed zero path; plan 07 REOPEN PIN + D-13 arms), [VAC04-C3] (three-way grep exit branching; plan 07), [VAC04-C2] (this plan: readers through a symlink / a space path reach `Functions defined or replaced by this PR: 1` on every CI run, each union member individually load-bearing, the pre-fix guard shown standing and recorded to produce `0 name(s)` twice then MEASURE_FAIL) and [VAC04-C4] (this plan: the gate exits 1 on `public.fúnc_é` naming `U+00FA` and `<file>:2:` with no body text; both refusals disabled → the truncated-`f` wrong-subject pass, standing and recorded) are each driven end-to-end through `scripts/prod-body-drift-check.sh` on their measured inputs.

Stated non-coverage (unchanged): the naive reader's refusal at gate level on the P10 input is unreachable — the normalizer refuses first at :207 — and is bounded by the STATED BOUND `it`, not claimed. REVIEW.md WR-04's bare-grep sites at prod-body-drift-check.sh:667 are gate-script behaviour outside this gap and were not touched (already in TODOS.md).

⛔ **Ordering constraint, restated unchanged:** migration PRs are HELD until Phase 164.3.1 AND Phase 164.4 have both landed (founder decision, amended D-07, 2026-09-01). A block at the fail-closed zero path is the gate working.

## Files Created/Modified

- `src/__tests__/drift-check-scripts.test.ts` — two new describes (above); `symlinkSync`, `cpSync` (node:fs) and `resolve` (node:path) added to the imports. 2564 → 2838 → 3044 lines.
- `src/__tests__/gate-family-meta.test.ts` — VAC04-C2 / VAC04-C4 entries: READER-LEVEL / GATE-LEVEL comments, one `also:` binding each, citations extended with `164.3.1-13` and `164.3.1-13-SUMMARY.md § cycles …`. PHASE_TAXONOMY, meta-arms, allowlists and KNOWN_THRESHOLD_SITES untouched.
- `.planning/phases/…/164.3.1-13-SUMMARY.md` — this record.

## Decisions Made

See `key-decisions` in the frontmatter (pass condition = readers-ran line; per-reader-visible fixtures calibrated first; the stated bound as its own `it`; absence-only neuter proof; both-member C4 neuter; no TDD RED commit for test-only tasks).

## Deviations from Plan

None — plan executed exactly as written. (The pre-existing uncommitted `.planning/STATE.md`, `.planning/milestone.lock` and `.planning/state.json` working-tree changes at start belong to the orchestrator's phase-start bookkeeping and were left alone by the task commits.)

## TDD Gate Compliance

Both TDD-flagged tasks are test-only: the readers' fixed behaviour already stands in the tree, so a `test(...)` RED commit would have required committing a neutered reader, which the plan's scope fence (`git diff --quiet HEAD -- scripts/…` at the end of every task) forbids. The RED gates were therefore executed and recorded as the neuter cycles C2-N1 / C2-N2 / C4-N1 above (RED observed on the real wiring, then restored) rather than as separate commits; each task landed as a single `test(164.3.1-13)` commit on the green, restored tree. There is no `feat(...)` commit because no production code changed.

## Issues Encountered

None. One scratch-measurement re-run was needed because a shell variable was not exported into a `node -e` helper (no effect on the tree).

## Known Stubs

None.

## Threat Flags

None — no new network endpoint, auth path, file-access pattern or schema surface; every fixture lives under `mkdtemp` and is removed by `withTempDir`.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- 164.3.1-VERIFICATION.md gaps[0] (SC-4 partial) is closed by mechanism; re-verification can re-run the two describes and the registry and re-read the cycles above.
- Phase 164.4 (REDUNDER-BACKFILL) is unblocked as before; the migration-PR hold stands until both phases land.
- Human verification items from VERIFICATION.md (first real `sql-mutation` / `migration-drift-check` / `sql-tests` runs on their hosts) are unchanged by this plan.

---
*Phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c*
*Completed: 2026-09-02*

## Self-Check: PASSED

- SUMMARY on disk; commits 6c23ae13 / b8d79162 / dc47efd4 present in git log; scope fence rc=0.
