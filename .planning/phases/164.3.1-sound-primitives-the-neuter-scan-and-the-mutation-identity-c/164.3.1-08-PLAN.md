---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "08"
type: execute
subsystem: primitive-d-gate
tags: [self-referential-oracle, ast-rule, blocking, mut-w02, primitive-d]
wave: 2
depends_on: ["164.3.1-02"]
files_modified:
  - src/__tests__/self-referential-oracle.test.ts
  - src/__tests__/lint-sql-gates.test.ts
  - scripts/aggregator-tolerance-fixtures/MW02-alternate-spelling.red.yml
  - scripts/aggregator-tolerance-fixtures/MW02-current-spelling.green.yml
autonomous: true
requirements: [SC-5, SC-9]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "The Primitive-D rule is BLOCKING: the corpus scan asserts the finding set equals the measured-exception allowlist EXACTLY — a new self-referential oracle anywhere in src/__tests__/**/*.test.ts fails BY NAME, and a fixed site still on the allowlist also fails (the stale-entry direction). The allowlist is sized from plan 02's committed CALIBRATION list, minus the sites fixed in this plan, and EVERY entry carries the measurement that justifies it — an unexplained entry is itself a primitive-D instance (SC-5, CONTEXT locked decision)."
    - "The lint-sql-gates.test.ts:182-186 site is FIXED: the two assertions now describe the red fixture's REAL bytes (the text already read from disk two lines above), so they can fail when the fixture or the rule changes — and the rule's post-fix scan no longer flags the site."
    - "The post-fix half of the VALIDATION Primitive-D row is discharged: with the site fixed, the rule's same-block resolution was NEUTERED and a planted self-referential fixture observed staying GREEN (the blindness reproduced), then restored sha-verified — the blocking rule is proven still able to fail."
    - "[MUT-W02] is re-expressed structurally: the aggregator's result-loop script block is EXTRACTED from ci.yml and its per-job branch conditions enumerated by a parser — the tolerance set must equal the AGGREGATED_JOBS table exactly, with a NON-VACUITY FLOOR (the parser must find at least the known arm count; an empty parse is MEASURE_FAIL). An equivalently-spelled tolerance arm is now VISIBLE where the single-spelling regex pin was blind — proven by the red fixture."
    - "The MW02 red/green fixture pair exists per the house contract: the red fixture carries an alternate-spelling tolerance arm the parser MUST find (and the old literal pin provably missed), the green fixture carries the current real spelling; the fixture-ID set is pinned exactly."
  artifacts:
    - "src/__tests__/self-referential-oracle.test.ts — blocking corpus assertion + SRO_ALLOWLIST exact set with per-entry measurements"
    - "src/__tests__/lint-sql-gates.test.ts — the :182-186 fix + the structural branch-condition parser replacing the single-spelling pins"
    - "scripts/aggregator-tolerance-fixtures/ — MW02 red/green pair"
  key_links:
    - "allowlist sizing ← .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md (the recorded HEAD-wide finding list; this plan must not re-measure from memory)"
    - "branch-condition parser → .github/workflows/ci.yml frontend aggregator result loop (extracted by its literal markers, the SP-L02 idiom already used elsewhere in lint-sql-gates.test.ts)"
    - "exact-set discipline ← audit-coverage.test.ts H_0001_UNCOVERED_ALLOWLIST (:1495, :1625-1650): fails by name in BOTH directions, with the do-not-silence failure message"
---

<objective>
Flip the Primitive-D machine check from report-only to BLOCKING, fix the live self-referential
instance it was calibrated on, and re-express [MUT-W02] as a structural parse of the frontend
aggregator's branch conditions.

Plan 02 built the rule, proved it fires (red/green fixtures + neuter fire-proof), observed it
flagging `src/__tests__/lint-sql-gates.test.ts:182-186` at HEAD BEFORE any fix, and recorded the
HEAD-wide finding list in 164.3.1-02-CALIBRATION.md. This plan consumes that record: the exact-set
allowlist is sized from the recorded list (minus what this plan fixes), each entry carrying its
justifying measurement. The ordering was the point — the rule is proven against the real instance,
so flipping it blocking is calibrated enforcement, not hope.

[MUT-W02]'s defect: the per-job tolerance pin at lint-sql-gates.test.ts asserts ONE literal
spelling — a regex over the exact bytes of one bracket-test shape — so an equivalently-written
tolerance arm (different quoting, spacing, or operator) widens the aggregator unseen. The
re-expression parses the result-loop's shell conditions structurally with its own non-vacuity
floor, so the assertion is about the SET of tolerance arms, not one spelling of one arm.

Purpose: SC-5 second half (the machine check, now enforcing) + SC-9 (allowlist entries and the
parser floor each carry their measurements).
Output: the blocking rule with its measured allowlist, the fixed :182-186 site, the structural
[MUT-W02] parser with its red/green fixture pair, and the post-fix neuter proof.

Scope fence: this plan owns `src/__tests__/lint-sql-gates.test.ts` and the SRO gate file. It does
NOT touch `.github/workflows/ci.yml` (read-only extraction; plan 10 owns ci.yml edits),
`scripts/lint-sql-gates.mjs`, or any Wave-2 sibling's files (05: mutation-runner + pg-lane;
06: audit-coverage.test.ts; 07: prod-body-drift-check.sh + drift-check-scripts.test.ts).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@src/__tests__/lint-sql-gates.test.ts
@src/__tests__/audit-coverage.test.ts

Read at execution time — created by plan 02, so they do not exist at planning time and are
deliberately not @-referenced (plan-anchor-verify checks pending plans against the current tree):
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-CALIBRATION.md
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-02-SUMMARY.md
src/__tests__/self-referential-oracle.test.ts (the plan-02 gate file this plan flips blocking)

Measured anchors at HEAD b4bdfc1e (lint-sql-gates.test.ts is read-only to Wave 1, so these hold
at this plan's start; re-grep before editing): the self-referential site at
`src/__tests__/lint-sql-gates.test.ts:182-186` (the banner const and its two assertions; the real
fixture bytes are already read into a variable two lines above at :181); the AGGREGATED_JOBS
table at `:314-318`; the needs+loop presence pins at `:320-331`; the single-spelling tolerance
pins being replaced at `:333-349` (the bracket-test regex construction, the null-tolerance
negative assertion at :341, and the literal skipped-condition pin at :349); the exact-set
population check at `:376`. Exact-set allowlist analog: `audit-coverage.test.ts:1495-1650`
(`H_0001_UNCOVERED_ALLOWLIST` and its exact-set check at 1625-1650).
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL (ROADMAP deliverable 5) | Machine check for primitive D flags same-block-constant assertions; [MUT-W02] re-expressed as a branch-condition parse with a non-vacuity floor | Tasks 1-3 |
| REQ [SC-5] | The rule flagged :182-186 at HEAD before the fix (plan 02); this plan fixes the site and flips the rule blocking | Tasks 1-2 |
| REQ [SC-9] | Allowlist entries carry measurements; the parser floor carries its measured count; findings asserted against the full corpus population | Tasks 1, 3 |
| CONTEXT § Primitive D (locked) | RED by default, measured-exception allowlist, unexplained entry is itself an instance | Task 1 |
| VALIDATION row "Primitive-D AST rule" (post-fix half) | Neuter the same-block resolution → planted fixture stays green, observed, restored | Task 2 |
| VALIDATION row "[MUT-W02] branch-condition parse" | Alternate spelling in a ci.yml copy: old pin green (the defect), new parse REDs | Task 3 |
| PLAN-OUTLINE row 08 | Red/green TS fixture pair per the fixtures contract | Task 3 (MW02 pair) + plan 02's SRO pair (standing) |

Excluded (not gaps): the calibration itself (plan 02, shipped); ci.yml edits (plan 10); corpus
consolidation and the SP-C04 permanent entry (plan 12).
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Fix the :182-186 site, size the exact-set allowlist from plan 02's record, flip the rule BLOCKING</name>
  <files>src/__tests__/lint-sql-gates.test.ts, src/__tests__/self-referential-oracle.test.ts</files>
  <precondition>164.3.1-02-CALIBRATION.md exists, records the rule flagging lint-sql-gates.test.ts at expect lines 183 and 184, and carries the full sorted HEAD-wide finding list (grep -a "lint-sql-gates.test.ts:183" on the artifact exits 0). If absent or incomplete, HALT — blocking cannot be sized without the calibration record.</precondition>
  <read_first>164.3.1-02-CALIBRATION.md (the finding list this task's allowlist is sized from, the member-probe table, and the rule's three stated non-coverage bounds); src/__tests__/self-referential-oracle.test.ts (plan 02's exports: findSelfReferentialExpects, listCorpusFiles, SRO_FINDING_PREFIX and the report-only corpus test this task converts); src/__tests__/lint-sql-gates.test.ts:175-195 (the site in context: the fixture bytes read at :181, the banner const at :182, the two assertions at :183-186, and the mutation drive below them); audit-coverage.test.ts:1495 and :1625-1650 (the exact-set allowlist shape to reproduce: typed entries with justifications, both-direction set equality, the do-not-silence message)</read_first>
  <action>
    FIX THE SITE FIRST: at lint-sql-gates.test.ts:182-186, delete the banner const and point both
    assertions at the REAL fixture bytes already in scope (the variable read from disk at :181):
    the red fixture's text must contain the RED-FIXTURE banner phrase, and must not contain the
    first rule's attribution. Now both assertions can fail when the fixture files or the
    attribution helper change — the subject is the system under test, not a constant defined two
    lines above. Keep the assertion messages' intent; update them to name what is now asserted.
    Run the file-scoped suite to confirm the fixture really carries the banner (if it does not,
    the assertion was ALWAYS vacuous — record what the real bytes hold and assert what is true of
    them, which is the point of the fix).

    SIZE THE ALLOWLIST: in self-referential-oracle.test.ts, add an exported SRO_ALLOWLIST — an
    exact typed set keyed by file + subjectName + kind (stable across line drift; do NOT key on
    line numbers, which move under unrelated edits), following the H_0001 shape. Populate it from
    plan 02's recorded finding list MINUS the entries this plan fixes (the two :182-186 findings).
    Every entry carries: the measurement that justifies it (quoted from or citing the calibration
    artifact's member-probe/finding record), the date, and why it is acceptable (e.g. a test
    asserting a deliberate literal contract where the constant IS the spec). An entry without a
    justification comment is FORBIDDEN — the review standard is that an unexplained entry is
    itself a primitive-D instance. If the recorded list is large, the allowlist is large — sizing
    honestly from the measurement is the contract; shrinking by re-tuning the detector is
    forbidden (any detector change requires re-running plan 02's fire-proof and re-recording).

    FLIP BLOCKING: convert the corpus-scan test from report-only to exact-set enforcement — the
    finding set (as file+subjectName+kind keys) must equal SRO_ALLOWLIST exactly, failing BY NAME
    in both directions with a message mirroring the H_0001 discipline: a NEW finding names the
    site and says fix it or justify it (never silence it); a STALE entry names the allowlist row
    that must be removed. Keep the diagnostic printing (every finding still printed with
    SRO_FINDING_PREFIX; the summary line stays) — the gate's failure output remains
    diagnostic-first (SC-7 family standard). Keep the >= 100 files-scanned non-vacuity floor with
    its measured comment. Update the file header: the rule is now blocking, sized 2026-09 from the
    calibration record; the three non-coverage bounds stand.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/self-referential-oracle.test.ts src/__tests__/lint-sql-gates.test.ts</automated>
    <fails_when>exit code is non-zero or any test fails — the blocking scan must be green with the fixed site absent from findings, the allowlist exact in both directions, and every pre-existing lint-sql-gates pin still passing</fails_when>
    <automated>bash -c 'grep -ac "justif" src/__tests__/self-referential-oracle.test.ts'</automated>
    <fails_when>exits non-zero or prints 0 — no justification text exists near the allowlist; entries without measurements violate the locked decision</fails_when>
  </verify>
  <acceptance_criteria>
    - The :182-186 assertions now read the fixture bytes variable (subject = system under test); the banner const is gone; the post-fix scan does not flag the site
    - SRO_ALLOWLIST is an exported exact set keyed on file+subjectName+kind, populated from the calibration record minus the fixed entries, every entry carrying measurement + date + reason
    - The corpus test fails by name in BOTH directions (new finding; stale entry) with do-not-silence messages, and still prints its findings and summary line
    - No detector logic changed in this task (the calibration remains valid); git diff on findSelfReferentialExpects shows no functional edit
  </acceptance_criteria>
  <done>The rule enforces: the live instance is fixed, the exceptions are measured and named, and the next self-referential oracle written in this repo fails CI by name.</done>
</task>

<task type="auto">
  <name>Task 2: Post-fix neuter proof — the blocking rule can still fail</name>
  <files>src/__tests__/self-referential-oracle.test.ts</files>
  <read_first>164.3.1-VALIDATION.md § Per-gate neuter/restore map row "Primitive-D AST rule" (the post-fix half this task discharges: neuter the rule's same-block resolution → a planted fixture stays green, observed, restored) and § Anti-Vacuity Contract (byte backups by shasum, re-taken after every edit; never git checkout --); 164.3.1-02-CALIBRATION.md § fire-proof record (plan 02's pre-fix half, for the SUMMARY's combined record)</read_first>
  <action>
    With the site fixed and the rule blocking, prove the rule is still able to fail — SC-1's
    recursive discipline applied to the phase's own machine check.

    1. Plant a probe: write a TEMPORARY test file in the scanned population (a src/__tests__/
       sro-plant-probe.test.ts carrying the same-block-const shape the rule must flag). Run the
       gate: REQUIRED — the blocking scan REDs naming the planted file (proves blocking bites on
       a NEW site, the enforcement direction). Capture verbatim.
    2. NEUTER: byte-backup self-referential-oracle.test.ts (shasum -a 256 recorded, backup
       outside the checkout in the session scratchpad); edit the detector's same-block resolution
       so it never resolves a declaration (the named minimal neuter). Run the gate with the plant
       still present: REQUIRED — the scan stays GREEN (the plant invisible, the blindness
       reproduced) while the SRO-01 red-fixture test REDs (the fixture pin catches the neuter —
       record that the pair fails together, which is the fixture contract working).
    3. RESTORE from the byte backup; verify shasum equality; delete the plant; run the full
       file-scoped suite green. Confirm the plant is gone from tree and index (git status
       --porcelain shows no sro-plant path; never committed).
    4. Record all three observations verbatim for the SUMMARY beside plan 02's pre-fix fire-proof,
       so the rule's lifecycle (fires at HEAD → calibrated → blocking → still falsifiable) is one
       continuous evidence chain.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/self-referential-oracle.test.ts</automated>
    <fails_when>exit code is non-zero after restore — the gate is not green at its enforced state, or the harness left residue</fails_when>
    <automated>bash -c 'git status --porcelain | grep -a "sro-plant"; test $? -eq 1'</automated>
    <fails_when>the pipeline exits non-zero because grep found a match — the planted probe file was left behind in the tree</fails_when>
  </verify>
  <acceptance_criteria>
    - The plant was observed failing the BLOCKING scan by name (enforcement direction), the neutered detector was observed missing it while the red-fixture pin failed (falsifiability direction), and the restore is shasum-verified
    - The plant file never appears in any commit (git log --all --oneline -- on its path is empty)
    - All three verbatim observations recorded for the SUMMARY
  </acceptance_criteria>
  <done>The blocking rule is proven to bite on new sites and proven still able to fail — the machine check is not itself a control that cannot fail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: [MUT-W02] — structural parse of the aggregator's branch conditions with a non-vacuity floor and the MW02 red/green fixture pair</name>
  <files>src/__tests__/lint-sql-gates.test.ts, scripts/aggregator-tolerance-fixtures/MW02-alternate-spelling.red.yml, scripts/aggregator-tolerance-fixtures/MW02-current-spelling.green.yml</files>
  <read_first>src/__tests__/lint-sql-gates.test.ts:300-380 (the AGGREGATED_JOBS table, the needs+loop presence pins at :320-331 which STAY, the single-spelling tolerance pins at :333-349 being replaced, and the population-derivation arm at :376 whose discipline the parser extends); .github/workflows/ci.yml — the frontend aggregator's result-loop script block (locate by its literal for-loop over the job=result pairs; read-only — this plan must NOT edit ci.yml); 164.3.1-RESEARCH.md Premise P16 (the measured defect: the bracket-test regex at one spelling; an equivalently-written arm is invisible) and § Pattern 4 [MUT-W02] re-expression; scripts/lint-sql-gates.mjs:33-40 (the house fixture-pair contract the MW02 pair follows)</read_first>
  <behavior>
    - Parser unit pins: extractResultLoopConditions(yamlText) returns the per-job branch conditions found in the aggregator's result-loop script — for the real ci.yml it finds the known tolerance shape for the is_pr-tolerant job and the skipped-condition family, with a count >= the measured known-arm floor; for a script block with NO recognizable conditions it reports a MEASURE_FAIL sentinel (empty parse is never a pass).
    - Red-fixture pin: parsing MW02-alternate-spelling.red.yml — a result-loop snippet whose tolerance arm is spelled differently (double-bracket test, different quoting/spacing) but is semantically a per-job tolerance — the parser FINDS it and classifies the job as tolerance-bearing. A companion assertion demonstrates the OLD single-spelling regex does NOT match the red fixture's bytes (the recorded defect, kept as a permanent contrast pin).
    - Green-fixture pin: parsing MW02-current-spelling.green.yml — the current real spelling extracted from ci.yml — yields exactly the same classification as parsing the real file (fixture fidelity check).
    - Replacement enforcement pin: the tolerance-bearing job set derived by the parser from the REAL ci.yml equals the AGGREGATED_JOBS table's tolerance column EXACTLY — a job growing ANY spelling of a tolerance arm, or losing its required one, fails by name.
  </behavior>
  <action>
    Write the fixtures first: create scripts/aggregator-tolerance-fixtures/ with the red member (a
    minimal result-loop script snippet carrying an alternate-spelling per-job tolerance arm — vary
    the test syntax and quoting from the current literal so the old regex provably misses it) and
    the green member (the current real result-loop condition lines, quoted from ci.yml with a
    provenance comment). Fixtures are .yml snippets, NOT under scripts/lint-sql-gates-fixtures/
    — that directory's registry is lint-sql-gates.mjs's own contract and foreign files would
    violate its pairing rules; the new directory carries its own README-style header comment in
    each file naming [MUT-W02] and the pair contract. Pin the fixture-ID set exactly in the test
    (an unpaired or unregistered fixture fails by name).

    Implement the parser inside lint-sql-gates.test.ts (a local function; no new file, no YAML
    dependency — extract the result-loop script block from the ci.yml bytes by its literal
    markers, the SP-L02 extract idiom already used in this file, then parse the SHELL conditions
    line-wise for the bracket-test family over the loop's name/result variables, normalizing
    quoting and spacing). The parser returns per-job condition records; a parse finding fewer
    conditions than the NON-VACUITY FLOOR is a MEASURE_FAIL — set the floor by measuring the real
    ci.yml in this task (count the known skipped-condition arms and the is_pr tolerance arm;
    record the count, the command, and the date in the floor's comment per SC-9).

    Replace the single-spelling tolerance pins at :333-349 with the parser-driven enforcement
    from the behavior block, keeping the needs+loop presence pins at :320-331 and the :376
    population check untouched. Keep ONE contrast assertion: the old literal regex applied to the
    red fixture matches nothing — the permanent record of WHY the re-expression exists (this is a
    fixture-scoped assertion about the defect, not a live pin on ci.yml). TDD order: write the
    red-fixture pin, observe it fail against the OLD regex-based logic (the defect direction),
    then implement the parser and watch the set-equality enforcement go green against the real
    ci.yml.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lint-sql-gates.test.ts && npx vitest run src/__tests__/contracts</automated>
    <fails_when>either run exits non-zero — the parser pins, fixture pins, set-equality enforcement, the :320-331 presence pins, or a contracts scan reddened; an empty parse passing (floor not firing) also lands here</fails_when>
    <automated>bash -c 'ls scripts/aggregator-tolerance-fixtures/MW02-alternate-spelling.red.yml scripts/aggregator-tolerance-fixtures/MW02-current-spelling.green.yml'</automated>
    <fails_when>exits non-zero — the fixture pair is missing a member; a rule without a firing fixture does not merge (house contract)</fails_when>
  </verify>
  <acceptance_criteria>
    - The parser enumerates branch conditions structurally from the extracted result-loop block; its non-vacuity floor carries the in-task measured count with command and date (SC-9)
    - The red fixture is FOUND by the parser and provably MISSED by the old single-spelling regex (both asserted); the green fixture matches the real ci.yml classification
    - The tolerance-set equality against AGGREGATED_JOBS replaces the :333-349 literal pins; the :320-331 needs+loop pins and the :376 population check are untouched
    - ci.yml itself is unmodified (git status --porcelain on .github/workflows/ is empty — plan 10 owns that file)
  </acceptance_criteria>
  <done>[MUT-W02] is closed structurally: any spelling of a tolerance arm is visible to the parser, the assertion is about the set not the bytes, the floor forbids a vacuous empty parse, and the defect's mechanism is preserved as a fixture-pinned contrast.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future test authors → the SRO gate | hand-authored test code written fast to close findings is the class Primitive D recurs through — now blocked by machine, not review |
| ci.yml edits → the aggregator's effective gate set | a tolerance arm in any spelling widens what branch protection tolerates; the parser is the control |
| plan 02's record → this plan's allowlist | the blocking flip's honesty rests on sizing from the committed calibration, not from a fresh, tunable measurement |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-08-01 | Tampering | a new self-referential oracle entering the corpus | high | mitigate | exact-set blocking with fail-by-name in both directions (task 1), proven to bite on a planted site and proven still falsifiable under neuter (task 2) |
| T-164.3.1-08-02 | Tampering | an equivalently-spelled tolerance arm widening the frontend aggregator unseen | high | mitigate | structural condition parse with set-equality against AGGREGATED_JOBS and a measured non-vacuity floor; red fixture proves the old blindness and the new visibility (task 3) |
| T-164.3.1-08-03 | Repudiation | allowlist entries with no measurement (silent scope growth) | medium | mitigate | every entry carries measurement + date + reason; the grep gate in task 1's verify asserts justification text exists; review standard names an unexplained entry a primitive-D instance |
| T-164.3.1-08-SC | Tampering (supply chain) | npm installs | low | accept | zero packages installed; the parser is hand-rolled in the test per the no-YAML-dep decision (RESEARCH § Don't Hand-Roll) |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 08)

| Artifact | Kind | Location | Consumers |
|---|---|---|---|
| `SRO_ALLOWLIST` | exported exact set (file+subjectName+kind keys, per-entry measurements) | `src/__tests__/self-referential-oracle.test.ts` | the blocking scan; plan 12's bare-measurement audit |
| Blocking corpus enforcement (both-direction set equality) | converted test | `src/__tests__/self-referential-oracle.test.ts` | CI vitest shards / frontend aggregator |
| Fixed :182-186 site (assertions over real fixture bytes) | fix | `src/__tests__/lint-sql-gates.test.ts` | the SRO scan (no longer flagged); plan 12's registry |
| `extractResultLoopConditions` + tolerance-set enforcement | local parser + pins | `src/__tests__/lint-sql-gates.test.ts` | aggregator integrity; plan 12's wiring confirmation |
| `scripts/aggregator-tolerance-fixtures/` MW02 red/green pair | fixture files | new directory | the parser pins; plan 12's registry |
</artifacts_produced>

<verification>
1. `npx vitest run src/__tests__/self-referential-oracle.test.ts src/__tests__/lint-sql-gates.test.ts` — green: blocking scan exact against the allowlist, fixed site unflagged, parser + fixture pins, presence pins intact. Fails when: non-zero exit.
2. `node scripts/lint-sql-gates.mjs --self-test` — exit 0 (its fixture registry untouched by the new fixture directory). Fails when: non-zero exit.
3. `npx vitest run src/__tests__/contracts` — green; Node-22 pass on both changed test files. Fails when: non-zero exit under either runtime.
4. `npx tsc --noEmit` — green (the SRO fixture .ts files remain in the tsc program; nothing this plan adds breaks it). Fails when: non-zero exit.
5. Fence: commits touch only the four declared paths plus `.planning/` — ci.yml, scripts/lint-sql-gates.mjs, audit-coverage.test.ts, drift-check-scripts.test.ts, mutation-runner all untouched.
</verification>

<success_criteria>
- SC-5 second half delivered end-to-end: the machine check that was proven firing on the real
  instance at HEAD (plan 02) is now BLOCKING with a measured exact-set allowlist, the instance is
  fixed, and the rule is proven to bite on new sites and to remain falsifiable.
- [MUT-W02] re-expressed: tolerance posture asserted structurally over ALL spellings with a
  measured non-vacuity floor and a red/green fixture pair — the single-spelling blindness is
  closed and its mechanism preserved as a contrast pin.
- SC-9 within scope: every allowlist entry and the parser floor carry their measurements with
  sample size, command and date.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-08-SUMMARY.md`
when done. It must record: the allowlist size and its derivation from the calibration record
(entries added = recorded findings minus fixed sites, with the arithmetic shown); the planted-site
enforcement RED and the neuter/restore observations verbatim with shasums; the parser's measured
floor count and command; and the red-fixture contrast (old regex misses, parser finds) verbatim.
</output>