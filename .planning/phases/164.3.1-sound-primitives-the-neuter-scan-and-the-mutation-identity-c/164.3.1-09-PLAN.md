---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "09"
type: execute
subsystem: mutation-runner
tags: [arms-floor, re-derivation, measure-first, sc-6]
wave: 3
depends_on: ["164.3.1-05"]
files_modified:
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
  - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-09-REDERIVATION.md
autonomous: true
requirements: [SC-6, SC-9]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low   # derived: 0 calibration samples (factor 1, not applied)

must_haves:
  truths:
    - "ARMS_FLOOR and the biting count are RE-DERIVED under the sound primitives — the statement tokenizer (plan 01) and the source-location attribution (plan 05) — by running the real corpus and the real gate file, not by asserting the old numbers still apply. Today's 30/30/0 was correct by SCOPE, not by mechanism; after the mechanism changed, the number must be re-earned (SC-6)."
    - "The R3-C01 measurement discipline is reproduced against the real gate file: the identity count and backward-scan count are re-measured (104 identities / 103 backward scans expected from the pre-phase baseline), agreements and refusals enumerated, refusals each dispositioned loudly — never silently absorbed."
    - "ANY movement from the recorded 30/30/0 baseline is recorded WITH ITS REASON — an arm that scores differently under attribution than under the nonce is a finding about the mechanism, named per-arm. Zero movement is also a recorded result, not an assumed one."
    - "Every floor constant's comment is re-cut with the re-derived measurement: value, date, sample size (arms/files measured), coverage (which corpus), and the derivation command — the SC-9 shape, so no floor rests on a superseded mechanism's number."
  artifacts:
    - "scripts/mutation-runner/run.mjs — ARMS_FLOOR (and FILES_FLOOR comment if touched) re-cut with the re-derived measured comments; values changed ONLY if the measurement moved them"
    - "src/__tests__/mutation-runner-floors.test.ts — its measured pins updated in lockstep IF the re-derivation moved a number (its anchored-identity pin carries a measured value)"
    - ".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-09-REDERIVATION.md — the committed re-derivation record: commands, verbatim outputs, per-arm movement table, refusal dispositions"
  key_links:
    - "baseline ← plan 01's SUMMARY (104/103 tokenizer re-measurement) and plan 05's SUMMARY (full-run summary lines under attribution) — this plan compares against RECORDED baselines, not memory"
    - "consumer → plan 164.3.1-10 (the absurdity-floor thresholds are set from THIS plan's re-derived numbers)"
---

<objective>
Measure-first (SC-6): re-derive ARMS_FLOOR and the biting count under the sound primitives, and
record any movement from the measured 30/30/0 with the reason. Plans 01 and 05 changed the
MECHANISM that produces these numbers — statement-based classification and source-location
attribution. Both plans were required to preserve the baseline exactly (any movement there was a
defect), so what remains is the honest question this plan answers: measured from scratch under the
new mechanism, are the floors still pinned at the right values, and does every arm still score for
the same reason?

This is its own plan by outline design — a measure-first task folded into a fix plan gets tuned to
the fix's convenience. Nothing here fixes anything: it measures, records, and re-cuts comments
(and values ONLY if the measurement moved them).

Purpose: SC-6 — "ARMS_FLOOR and the biting count are re-derived under the sound primitives, and
any movement from today's 30/30/0 is recorded with the reason." SC-9 — the re-cut comments carry
sample size and coverage.
Output: the committed re-derivation artifact and the re-cut measured comments in run.mjs.

⚠️ Anchor discipline: plans 01 and 05 rewrote run.mjs — locate everything by SYMBOL (ARMS_FLOOR,
FILES_FLOOR, the predicate measured-comment block), never by stale line numbers.

⛔ Safety: only the pg-lane throwaway cluster is written to. No supabase CLI commands.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-CONTEXT.md
@.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-RESEARCH.md
@scripts/mutation-runner/run.mjs
@src/__tests__/mutation-runner-floors.test.ts

Read at execution time — created by plans 01 and 05, so they do not exist at planning time and
are deliberately not @-referenced (plan-anchor-verify checks pending plans against the tree):
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-01-SUMMARY.md
.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-05-SUMMARY.md
</context>

<source_audit>
| Source | Item | Coverage |
|---|---|---|
| GOAL (ROADMAP deliverable 7) | Re-derive ARMS_FLOOR and the biting count; record movement — 30/30/0 is correct by scope, not mechanism | Tasks 1-2 |
| REQ [SC-6] | Re-derivation performed and movement recorded with reason | Tasks 1-2 |
| REQ [SC-9] | Re-cut comments carry sample size + coverage + command | Task 2 |
| RESEARCH § Pattern 1 non-regression / Pitfall 2 | R3-C01 discipline: 104/103 against the real gate file, refusals enumerated | Task 1 |
| PLAN-OUTLINE row 09 | Measure-first as its own plan, never folded into a fix | whole plan |

Excluded (not gaps): the absurdity floor itself (plan 10 consumes this plan's numbers); fixture
corpus additions (plan 11).
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Re-derive — full corpus run, per-arm verdict table, 104/103 re-measurement, movement analysis</name>
  <files>.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-09-REDERIVATION.md</files>
  <precondition>Plans 01 and 05 are merged (node -e "import('./scripts/mutation-runner/parse.mjs').then(m => process.exit(typeof m.tokenizeStatements === 'function' ? 0 : 1))" exits 0 and node -e "import('./scripts/mutation-runner/run.mjs').then(m => process.exit(typeof m.attributeIdentities === 'function' ? 0 : 1))" exits 0), and no concurrent agent edits this checkout during the ~2-minute full runs.</precondition>
  <read_first>scripts/mutation-runner/run.mjs — the ARMS_FLOOR and FILES_FLOOR blocks with their measured-comment headers, and the predicate measured-comment plan 01 re-cut (locate all by symbol/grep); 164.3.1-01-SUMMARY.md (the recorded 104/103 table and the corrected seven-line spelling — the tokenizer-side baseline); 164.3.1-05-SUMMARY.md (the recorded full-run summary lines under attribution — the attribution-side baseline); supabase/tests/test_strategy_shares_rls.sql (the annotated real corpus file — read via node:fs, never bare shell grep; this repo carries a NUL-blind file and the habit matters); 164.3-REVIEW-FIX-R3.md if present in .planning/milestones or phase archives (the R3-C01 record format to reproduce — plan 01's SUMMARY cites it)</read_first>
  <action>
    Run the full corpus: node scripts/mutation-runner/run.mjs, capturing complete output. Extract
    the per-arm verdict lines (the runner prints one line per arm with exit code and verdict) and
    build the PER-ARM TABLE: arm identity, verdict now, verdict at the pre-phase baseline
    (RED (identity ok) for all 30 per the recorded 30/30/0 with biting 30), moved or not. For any
    moved arm, derive and record the REASON from the diagnostic output (which attribution leg or
    classification decision changed its scoring) — a movement without a mechanism explanation is
    an open finding that BLOCKS this plan's completion, not a footnote.

    Re-run the R3-C01 measurement against the real gate file: the identity count and
    backward-scan count over supabase/tests/test_strategy_shares_rls.sql under the shipped
    statement predicate (the executable form lives in the real-corpus describe of
    src/__tests__/mutation-runner-neuter.test.ts — run it, and also record the raw counts with
    the command used). Expected from plan 01's record: 104 identities / 103 backward scans /
    0 refusals. Enumerate every agreement/refusal; a refusal is dispositioned loudly (named, with
    the statement text and the arm that proves it) — never silently absorbed.

    Derive the floor verdict: with biting measured under the sound primitives, state whether
    ARMS_FLOOR's pinned value still equals the measured biting count. If the measurement moved,
    the floor moves WITH the measurement (a floor above measured reds honest runs; below it,
    regressions pass) — record old value, new value, reason. If nothing moved, record that as the
    measured result with the run outputs proving it.

    Write 164.3.1-09-REDERIVATION.md: (i) HEAD sha, date, commands; (ii) the full-run summary
    lines verbatim; (iii) the per-arm table with the movement column and reasons; (iv) the
    104/103 re-measurement with refusal dispositions; (v) the floor verdict with its arithmetic;
    (vi) the handoff sentence: plan 164.3.1-10 sets its absurdity-floor thresholds from THESE
    numbers. Commit it (.planning/ is tracked).
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs</automated>
    <fails_when>exit code is non-zero — the corpus is not green under the sound primitives; a red arm must be dispositioned before any floor conclusion is recorded</fails_when>
    <automated>ART=".planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-09-REDERIVATION.md" && grep -aq "biting" "$ART" && grep -aq "104" "$ART" && grep -aqi "movement\|no movement" "$ART"</automated>
    <fails_when>any grep exits non-zero — the artifact lacks the biting record, the 104-identity re-measurement, or an explicit movement statement (zero movement must be stated, not implied)</fails_when>
  </verify>
  <acceptance_criteria>
    - The per-arm table covers every executed arm with a movement column; every moved arm carries a mechanism reason; zero movement is explicitly recorded as measured
    - The 104/103 re-measurement is recorded with its command; refusal count and dispositions listed (expected 0, but the enumeration is the deliverable)
    - The floor verdict states the measured biting count and whether ARMS_FLOOR's value holds, with arithmetic
    - The artifact is committed and names plan 10 as its consumer
  </acceptance_criteria>
  <done>The numbers are re-earned under the new mechanism, per-arm, with every movement (or its absence) recorded and reasoned in a committed artifact.</done>
</task>

<task type="auto">
  <name>Task 2: Re-cut the measured comments (and values only if moved); lockstep the floors test; leave the tree green</name>
  <files>scripts/mutation-runner/run.mjs, src/__tests__/mutation-runner-floors.test.ts</files>
  <read_first>scripts/mutation-runner/run.mjs — the ARMS_FLOOR header comment (pinned by measurement, dated) and the FILES_FLOOR header (which states 164.4 raises it — that sentence must SURVIVE, plan 07's flip arm keys on the mechanism it describes); src/__tests__/mutation-runner-floors.test.ts — its measured pins (the anchored-identity count pin carries a hardcoded measured value with a dated comment; grep for the measured-comment discipline) and its header explaining why it deliberately does not import the parser</read_first>
  <action>
    Re-cut the ARMS_FLOOR measured comment with task 1's re-derivation: the value (changed only if
    task 1's verdict moved it), the date, the derivation command, sample size (30 arms executed, 1
    annotated file of 71) and coverage (which corpus, which gate file) — the SC-9 shape. Append to
    the comment's measured history (the 2026-08-29 pin stays as lineage), never overwrite it.
    Do the same for the FILES_FLOOR comment ONLY if task 1 touched anything it states — its
    "Phase 164.4 raises FILES_FLOOR" sentence is load-bearing for plan 07's flip arm and must
    survive verbatim.

    If a value moved: update the constant, update mutation-runner-floors.test.ts's corresponding
    measured pin in the SAME commit (its hardcoded anchored-count expectation carries a measured
    comment — re-cut it with the new measurement, date and command), and state in the artifact's
    movement section that both sites moved in lockstep. If no value moved: the floors test is
    untouched, and this task's diff is comments-only in run.mjs — say so in the SUMMARY.

    Leave the tree green: full runner run, runner self-test, floors test, neuter test — the
    serialized plan-level gate below.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --self-test && npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-runner-neuter.test.ts</automated>
    <fails_when>either command exits non-zero — a re-cut comment or a moved value broke a self-test scenario or a floors/neuter pin; comment and pin must move in lockstep or not at all</fails_when>
    <automated>bash -c 'grep -a -B2 -A8 "export const ARMS_FLOOR" scripts/mutation-runner/run.mjs | grep -aqi "2026-09"'</automated>
    <fails_when>exits non-zero — the ARMS_FLOOR block carries no re-derivation record dated this phase; the floor still rests on the superseded mechanism's measurement</fails_when>
  </verify>
  <acceptance_criteria>
    - ARMS_FLOOR's comment carries the re-derived measurement (value, date, command, sample size, coverage) with the prior measurement preserved as history
    - The FILES_FLOOR 164.4 sentence survives verbatim (plan 07's flip arm dependency)
    - Value changes (if any) are mirrored in mutation-runner-floors.test.ts in the same commit; no change → no touch
    - Full runner + self-test + floors + neuter suites all green after the re-cut
  </acceptance_criteria>
  <done>Every floor constant's justification is current to the sound primitives, histories preserved, consumers in lockstep, tree green.</done>
</task>

</tasks>

<threat_model>
This plan measures and documents; it changes no gate logic. The honest boundary is the record
itself.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the re-derivation → every downstream threshold | plan 10's floors and 164.4's ratchets inherit these numbers; a tuned or unrecorded measurement here corrupts everything above it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.3.1-09-01 | Repudiation | movement absorbed silently (a changed verdict recorded as noise) | medium | mitigate | per-arm table with a mandatory reason column; an unexplained movement BLOCKS completion; zero movement stated as a measured result |
| T-164.3.1-09-02 | Tampering | floor value drifting from its pin without its consumer test | low | mitigate | lockstep rule: constant + floors-test pin move in the same commit, or neither |
| T-164.3.1-09-SC | Tampering (supply chain) | npm installs | low | accept | zero packages installed; measurement-only plan |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces (plan 09)

| Artifact | Kind | Location | Consumers |
|---|---|---|---|
| `164.3.1-09-REDERIVATION.md` | committed measurement record (per-arm table, 104/103, floor verdict) | phase directory | plan 10 (threshold source); phase verifier (SC-6) |
| Re-cut ARMS_FLOOR measured comment (+ value iff moved) | constant + comment | `scripts/mutation-runner/run.mjs` | the runner's floor check; the sql-mutation CI step; plan 10 |
| Lockstepped floors-test pin (iff a value moved) | test pin | `src/__tests__/mutation-runner-floors.test.ts` | CI vitest shards |
| No new functions, flags, fixtures or dependencies | — | — | — |
</artifacts_produced>

<verification>
Serialized (the runner must not share the box):
1. `node scripts/mutation-runner/run.mjs` — exit 0; summary lines match the artifact verbatim.
2. `node scripts/mutation-runner/run.mjs --self-test` — 8/8, exit 0.
3. `npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/mutation-runner-neuter.test.ts` — green.
4. If any src/ test file changed: `npx vitest run src/__tests__/contracts` + Node-22 pass.
5. The artifact is committed and its floor verdict matches the constants in run.mjs.
</verification>

<success_criteria>
- SC-6 delivered: ARMS_FLOOR and biting re-derived by running the real corpus under the sound
  primitives; the per-arm movement table exists; every movement reasoned, zero-movement stated as
  measured; the 104/103 discipline reproduced with refusals dispositioned.
- SC-9 within scope: every re-cut comment carries value, date, command, sample size and coverage,
  with prior measurements preserved as history.
- Plan 10 has a committed, honest number source — not a stale mechanism's pin.
</success_criteria>

<output>
Create `.planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-09-SUMMARY.md`
when done. It must repeat the full-run summary lines and the floor verdict verbatim from the
artifact, state movement-or-none explicitly, and name plan 164.3.1-10 as the threshold consumer.
</output>