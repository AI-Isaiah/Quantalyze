---
phase: 164.3.1-sound-primitives
plan: 04
subsystem: testing
tags: [vac-04, sql-gates, ci, node, esm, main-module-guard, parser-hardening, anti-vacuity]

# Dependency graph
requires:
  - phase: 164.3-vacuity
    provides: "VAC-04 (repo-vs-PROD function body diff) and its two-reader union (SP-C05) — the readers this plan hardens"
provides:
  - "scripts/sql-function-names-naive.mjs: realpath main-module guard + charset refusal; self-test 12 -> 13 checks"
  - "scripts/sql-body-normalize.mjs: realpath main-module guard + charset refusal with a documented $ exemption; self-test 8 -> 9 checks"
  - "src/__tests__/vac04-reader-guards.test.ts: 15 arms pinning both members through direct/symlink/space invocation and the charset refusal in both directions"
  - "Measured evidence that VAC-04's reader exit codes already propagate through the gate's existing `|| fail` wrappers — plan 07 needs no propagation work"
affects: [164.3.1-07, 164.4, VAC-04, VAC-08, SC-4, SC-7]

actuals:
  tokens: 15224   # chars/4 over the three files actually changed (60,895 chars). Realized-diff-only figure: 8,938 (35,752 chars).
  tasks: 3
  commits: 3      # 2 code + 1 plan-metadata

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "realpath main-module guard (realpathSync(argv[1]) === fileURLToPath(import.meta.url)), DUPLICATED per union member rather than shared"
    - "refuse-rather-than-narrow: a marked error thrown at the LIBRARY boundary, formatted to exit 1 at the CLI, unmarked errors rethrown"
    - "follower-byte check as the truncation tell (end-of-input | whitespace | `(`), evaluated on MASKED text in the lexer member"
    - "diagnostic-first but body-text-bounded: name the identifier, byte, codepoint, file and line — never the surrounding source"

key-files:
  created:
    - src/__tests__/vac04-reader-guards.test.ts
  modified:
    - scripts/sql-function-names-naive.mjs
    - scripts/sql-body-normalize.mjs

key-decisions:
  - "The guard function is DUPLICATED verbatim into each reader, never extracted to a shared module — a shared guard would recreate the one-mechanism-fails-both-members defect [VAC04-C2] names, and would break the naive member's machine-pinned node:-builtins-only import contract."
  - "`$` is EXEMPT from the normalizer's charset refusal. It is inside Postgres' unquoted charset; the normalizer's blindness to it is SP-C05's measured limitation and the entire reason the naive member exists; and drop-not-refuse is machine-pinned by the designed-disagreement arm. Refusing `$` would break the union's design while claiming to harden it."
  - "The naive reader's over-refusal on degenerate-but-legal shapes (a block comment butted against the name in its line-anchored view) is ACCEPTED, not worked around: a loud false refusal names its file, line and byte; a silent wrong-subject comparison reports a pass."
  - "The stale plan-anchor-verify claims caused by this fix are resolved by this SUMMARY existing (a plan with a sibling SUMMARY leaves the pending set), NOT by rewriting the PLAN's anchors — editing them to describe the fixed code would destroy the record of the measured defect."
  - "Exit code 1 for a refusal, 2 reserved for usage errors — matching both readers' existing convention, so the gate's `|| fail` wrappers need no change."

patterns-established:
  - "Two-directional proof for every new loud path: prove it FIRES on the measured input and prove it is SILENT on the whole legitimate corpus, using a STANDING committed arm for the silent direction rather than a new instrument."
  - "Every probe ships a passing CONTROL in the same arm family, so a probe that cannot distinguish `ran` from `did not run` is itself detectable."
  - "Refusal diagnostics in public-CI-log paths are bounded to identifier-and-position facts, pinned by a body-sentinel non-leakage arm."

requirements-completed: []   # SC-7 satisfied for these two gates; SC-4 is PARTIAL by founder decision — see "SC-4 is PARTIAL" below. Neither is claimed complete here.

coverage:
  - id: D1
    description: "[VAC04-C2] Both VAC-04 union readers execute main() under direct, symlinked, and space-containing invocation paths — the fail-open guard that made a green gate read nothing is closed in BOTH members."
    requirement: "SC-4"
    verification:
      - kind: unit
        ref: "src/__tests__/vac04-reader-guards.test.ts#main-module guard ([VAC04-C2]) — every invocation shape must actually RUN the reader (7 arms)"
        status: pass
      - kind: integration
        ref: "node scripts/sql-function-names-naive.mjs --self-test through a symlink => 'self-test OK (13 checks)', exit 0; symlinked no-args => exit 2"
        status: pass
    human_judgment: false
  - id: D2
    description: "[VAC04-C4] Both readers refuse rather than truncate/drop on an unquoted identifier leaving [A-Za-z0-9_$], at CLI and library level, with what-I-saw diagnostics carrying no body text."
    requirement: "SC-7"
    verification:
      - kind: unit
        ref: "src/__tests__/vac04-reader-guards.test.ts#charset refusal ([VAC04-C4]) — a reader that cannot read the name must REFUSE, never narrow it (8 arms)"
        status: pass
      - kind: integration
        ref: "node scripts/sql-body-normalize.mjs --self-test => 'self-test OK (9 checks)'; node scripts/sql-function-names-naive.mjs --self-test => 'self-test OK (13 checks)'"
        status: pass
    human_judgment: false
  - id: D3
    description: "The union's design is intact: the `$` disagreement survives, quoted identifiers stay unrestricted, the naive member's node:-builtins independence holds, and the refusal fires zero times on the real 380-file corpus (the silent direction)."
    verification:
      - kind: unit
        ref: "src/__tests__/drift-check-scripts.test.ts (248 arms, incl. 'the two readings genuinely DISAGREE', the node:-builtins independence arm, both e2e gate arms, and 'the two readings agree on the ENTIRE real corpus')"
        status: pass
    human_judgment: false
  - id: D4
    description: "SC-4 remains PARTIAL: this plan closes the two READER-SIDE members only. [VAC04-C1]/[VAC04-C3], the zero-path warn, the flip arm and the absurdity floor are plan 164.3.1-07's, and the fail-closed flip is deferred to Phase 164.4 by founder decision."
    verification: []
    human_judgment: true
    rationale: "A deferred criterion recorded as met is the exact defect shape this phase exists to stop. A human must carry the partiality forward into phase verification rather than a machine absorbing it."

# Metrics
duration: 20min
completed: 2026-09-01
status: complete
---

# Phase 164.3.1 Plan 04: VAC-04 Reader Hardening Summary

**Both VAC-04 union readers now actually RUN on every invocation shape (realpath main-module guard, duplicated per member) and REFUSE rather than silently narrow a charset-leaving identifier — closing the one shared mechanism that made "two independent readings agree" a lie rather than a redundancy.**

## Performance

- **Duration:** ~20 min
- **Started:** 2026-09-01T17:50:00Z
- **Completed:** 2026-09-01T18:10:00Z
- **Tasks:** 3
- **Files modified:** 3 (2 modified, 1 created)

## Accomplishments

- **[VAC04-C2] closed in BOTH members.** The URL-string guard `import.meta.url === \`file://${process.argv[1]}\`` is false on any symlinked path and on any path containing a space, so `main()` never ran, stdout was empty, and the process exited 0 — VAC-04 reading NOTHING over PROD function bodies while reporting success. Replaced in each reader with the `invokedDirectly()` realpath idiom lifted from `check-banned-packages.mjs:443-450`, **duplicated verbatim rather than shared**.
- **[VAC04-C4] closed in BOTH members, at the library boundary.** `naiveFunctionDefs` and `extractFunctionDefs` now THROW a marked `charsetRefusal` on any unquoted identifier leaving `[A-Za-z0-9_$]`; the CLIs format it to exit 1. No import path — CLI, vitest, or future caller — can silently receive a truncated or dropped name.
- **Two-directional proof on the new loud path.** It FIRES on the P10 input in both readers (and on the leading-non-ASCII shape the naive member is blind to, which the normalizer's refusal now covers). It is SILENT on the entire real corpus: measured 2026-09-01, all **362 definitions across all 380 `.sql` files** are followed by `(` in **both** readers, so the refusal fires zero times.
- **The union's design survives untouched.** `sanitize_user$v2` is still read by the naive member and still silently DROPPED (never refused) by the normalizer.
- **Zero gate-script changes needed.** Code-read this session: every reader invocation in `prod-body-drift-check.sh` (`:207`, `:221`, `:272`, `:274`, `:313`, `:316`, `:450`) and in `test-ledger-drift-check.sh` (`:492`) is already `|| fail`-wrapped, so a refusal fails the gate loudly today.

## Task Commits

1. **Task 1: [VAC04-C2] realpath guard into both union members** — `860ce007` (fix)
2. **Task 2: [VAC04-C4] refuse-rather-than-truncate in both readers** — `e7f1ae6a` (fix)
3. **Task 3: Restore-direction sweep** — verification-only, **no code diff** (the plan permits a diff here only if the contracts or Node-22 runs surface a polish; neither did). Its evidence is this SUMMARY.

**Plan metadata:** see the `docs(164.3.1-04)` commit carrying this file.

## Pre-fix RED, recorded verbatim

Both defects were live at this plan's base `e0660031`. Nothing was artificially neutered — the RED below is the tree's own state.

### [VAC04-C2] — the guard no-ops, identically in both members

Raw shell probe, with a **passing control** so the probe is provably able to distinguish "ran" from "did not run":

```
=== CONTROL: direct naive --self-test ===
sql-function-names-naive self-test OK (12 checks)
exit=0
=== CONTROL: direct normalizer --self-test ===
sql-body-normalize self-test OK (8 checks)
exit=0
=== PROBE: symlinked naive --self-test ===
exit=0
=== PROBE: symlinked normalizer --self-test ===
exit=0
=== PROBE: symlinked naive with NO ARGS (exit-code-flipping arm) ===
exit=0
=== PROBE: space-in-path naive --self-test ===
exit=0
=== PROBE: space-in-path normalizer --self-test ===
exit=0
```

Every PROBE line above printed **nothing at all** and exited **0**. The no-args symlinked naive must exit **2**.

Committed pins at HEAD before the fix — `npx vitest run src/__tests__/vac04-reader-guards.test.ts`:

```
 ❯ src/__tests__/vac04-reader-guards.test.ts (7 tests | 5 failed) 836ms
     × scripts/sql-function-names-naive.mjs: through a SYMLINK, main() still runs
     × scripts/sql-function-names-naive.mjs: from a path containing a SPACE, main() still runs
     × scripts/sql-body-normalize.mjs: through a SYMLINK, main() still runs
     × scripts/sql-body-normalize.mjs: from a path containing a SPACE, main() still runs
     × the symlinked naive reader with NO ARGS exits 2 — the arm where the EXIT CODE itself flips

AssertionError: a symlinked invocation that prints NOTHING is a reader that read nothing
while reporting success: expected '' to contain 'sql-function-names-naive self-test OK ('
AssertionError: stdout= stderr=: expected +0 to be 2 // Object.is equality

 Tests  5 failed | 2 passed (7)
```

The 2 passing were the direct-invocation controls.

### [VAC04-C4] — truncate-or-drop, in two different directions

Raw shell probe on the P10 input `CREATE OR REPLACE FUNCTION public.fúnc_é(p uuid) …`:

```
=== PROBE: naive on P10 ===
f
exit=0
=== PROBE: normalizer --function-names on P10 ===
exit=0
=== PROBE: normalizer --function-qualified-names on P10 ===
exit=0
=== PROBE: normalizer --function-names on leading-non-ASCII ===
exit=0
```

`f` is a **different function** from `fúnc_é`. VAC-04 fetches a PROD body by that name, so the truncated read compares the wrong subject and can report `MATCH` for it; the dropped read is absent from the name index, so the gate concludes "absent from PROD, therefore NEW, therefore a pass".

Committed pins at HEAD before the fix:

```
 ❯ src/__tests__/vac04-reader-guards.test.ts (15 tests | 6 failed) 1867ms
     × naive: the P10 input EXITS 1 naming what it saw, instead of printing the truncated `f`
     × normalizer --function-names: the P10 input EXITS 1 instead of silently dropping the definition
     × normalizer --function-qualified-names: the mode VAC-04's SCHEMA check drives refuses too
     × a LEADING non-ASCII identifier is caught by the normalizer — the union covering the naive reader's blind spot
     × refusal diagnostics leak NO body text — the normalizer's index run reads a PROD dump into a public CI log
     × both LIBRARY functions throw — no import path can receive a truncated or dropped name

AssertionError: stdout=f
 stderr=: expected +0 to be 1 // Object.is equality
```

The `$`-exemption arm and the quoted-identifier arm passed **before** the fix — they fence pre-existing behavior that must not change.

## Post-fix GREEN

### Self-test counts — 13 and 9

```
sql-function-names-naive self-test OK (13 checks)     # was 12; +1 charset check
sql-body-normalize self-test OK (9 checks)            # was 8;  +1 charset check
```

Task 1 deliberately left both counts at 12/8 (it added no checks); Task 2 added exactly one per reader.

### Guard, post-fix — every invocation shape now runs

```
=== PROBE: symlinked naive --self-test ===
sql-function-names-naive self-test OK (13 checks)
exit=0
=== PROBE: symlinked normalizer --self-test ===
sql-body-normalize self-test OK (9 checks)
exit=0
=== PROBE: symlinked naive with NO ARGS (exit-code-flipping arm) ===
::error::sql-function-names-naive: at least one file is required. …
exit=2
=== PROBE: space-in-path naive --self-test ===
sql-function-names-naive self-test OK (13 checks)
exit=0
=== PROBE: space-in-path normalizer --self-test ===
sql-body-normalize self-test OK (9 checks)
exit=0
```

### Refusal, post-fix — diagnostic-first (SC-7), with no body text

```
::error::sql-function-names-naive: /…/p10.sql:1: identifier leaves the unquoted charset
[A-Za-z0-9_$] — read 'public.f' then hit 'ú' (U+00FA). Refusing rather than truncating:
a truncated name makes VAC-04 fetch and compare a DIFFERENT function's body, and report
MATCH for it. Quote the identifier ("...") if that character is intended.
exit=1

::error::sql-body-normalize: /…/p10.sql:1: identifier leaves the unquoted charset
[A-Za-z0-9_$] — read 'public.f' then hit 'ú' (U+00FA). Refusing rather than dropping:
a dropped definition is absent from the function-name index, so VAC-04 classifies it
'absent from PROD, therefore NEW, therefore a pass'. Quote the identifier ("...") if that
character is intended.
exit=1
```

Each names **what it saw** — the file, the 1-based line, the identifier prefix actually read, and the offending character with its `U+` codepoint — and **no source line, no body text**. A body-sentinel arm pins the non-leakage; the normalizer's header contract ("prints no body text in any reporting mode") is intact, which matters because `BODY_NAME_INDEX_CMD` runs this reader over a PROD dump in a **public** CI log.

### Suite runs

| Run | Result |
|---|---|
| `vitest run src/__tests__/vac04-reader-guards.test.ts` | **15 passed** |
| `vitest run src/__tests__/drift-check-scripts.test.ts src/__tests__/sql-body-normalize.test.ts` | **286 passed** |
| `vitest run src/__tests__/contracts` | **109 passed** (5 files) |
| Node 22 (`/opt/homebrew/opt/node@22/bin`, v22.22.1) on the three affected suites | **301 passed**, self-tests 13/9 |
| `tsc --noEmit` | clean |
| `eslint src/__tests__/vac04-reader-guards.test.ts` | clean |

## The silent direction — the corpus-parity arm

The plan requires proving the refusal is *silent on legitimate input* via a **standing committed arm**, not a new instrument. `drift-check-scripts.test.ts`'s "the two readings agree on the ENTIRE real corpus" arm imports **both** library functions and runs them over every `.sql` file under `supabase/migrations/` and `supabase/schema/functions/`. Because the refusal is a `throw`, a single firing on any corpus file would red that arm by construction. It passes post-fix.

Its own printed population facts (asserted inside the arm): `files > 200`, `definitions > 100`. Independently measured this session before implementing, so the direction was known rather than hoped:

```
corpus files: 380
naive DEF_RE matches: 362
  NAIVE follower "(": 362
lexer FN_RE matches: 362
  LEXER follower "(": 362
```

Every one of the 362 real definitions is followed by `(` in **both** readers. **Zero** would trip the refusal.

## Propagation note for plan 164.3.1-07

**No gate-script change is required, and none was made.** `scripts/prod-body-drift-check.sh` is plan 07's file and was not touched. Code-read this session, every reader invocation in it is already `|| fail`-wrapped:

| Site | Invocation |
|---|---|
| `:207-208` | `node "$NORMALIZER" --function-names …` |
| `:221-222` | `node "$NAIVE_NAMES" …` |
| `:272-275` | `--function-qualified-names` + `$NAIVE_NAMES --qualified` |
| `:313-317` | `$BODY_NAME_INDEX_CMD` + `$BODY_NAME_INDEX_XCHECK_CMD` (PROD dump) |
| `:450` | `node "$NORMALIZER" --diff-bodies …` |

A reader exit of 1 therefore fails the gate loudly today. The same holds for VAC-08: `test-ledger-drift-check.sh:492` wraps `--diff-bodies` with `|| fail`. Note the two PROD-index sites deliberately withhold reader stderr from the public log — which is a second, independent reason the refusal diagnostic carries no body text.

## ⚠️ SC-4 is PARTIAL — do not record it as met

Quoting this plan's own `must_haves` truth 7, and CONTEXT.md's Open Tension:

> SC-4 remains PARTIAL by founder decision: this plan closes **[VAC04-C2]** and **[VAC04-C4]** **reader-side ONLY**. **[VAC04-C1]/[VAC04-C3]**, the zero-path warn, the flip arm, and the absurdity floor are plan **164.3.1-07's**. Verification must not record SC-4 as met on the strength of this plan.

VAC-04's zero path still **WARNS** and does not block; the fail-closed flip is deferred to **Phase 164.4** (founder call D-07, 2026-09-01, taken to avoid wedging migration PRs mid-flight). A warn-only zero path *does* exit 0 having compared nothing, so criterion 4 is met in two steps and the second step is not in this phase. Nothing in this plan was made to fail closed that the founder deferred.

**SC-7** *is* satisfied for these two gates: every new failure path prints its evidence, and prints no body text.

## Files Created/Modified

- `scripts/sql-function-names-naive.mjs` — realpath `invokedDirectly()` guard (+ `realpathSync`/`resolve`/`fileURLToPath`, all `node:` builtins so the independence pin holds); follower-byte charset refusal in `naiveFunctionDefs` with a `charsetRefusal` marker; per-file `try/catch` in `main()` emitting a `::error::` diagnostic and returning 1; +1 self-test check (12 → 13).
- `scripts/sql-body-normalize.mjs` — the same guard, duplicated; `charsetRefusal()` helper; follower-byte check on **masked** text in `extractFunctionDefs` with the documented three-leg `$` exemption; `reportCharsetRefusal()` wired into `--function-names`, `--function-qualified-names`, `--extract-fn` and `--diff-bodies` (with file attribution for the two-file mode); `--normalize`/`--hash` untouched (VAC-08 drives those); +1 self-test check (8 → 9).
- `src/__tests__/vac04-reader-guards.test.ts` — **new**, 15 arms: 7 guard arms (direct control / symlink / space-path per member, plus the no-args exit-code flip) and 8 charset arms (P10 refusal per member, qualified mode, leading-non-ASCII union coverage, quoted-identifier acceptance, `$` exemption, body-sentinel non-leakage, library-level throws).

## Decisions Made

See `key-decisions` in the frontmatter. The two that most constrain future work:

1. **The guard is duplicated, not shared.** Extracting it would rebuild the exact coupling `[VAC04-C2]` names — one mechanism failing both "independent" derivations — and would break the naive member's machine-pinned `node:`-builtins-only import contract. Any future refactor that DRYs these two guards reopens the defect.
2. **`$` is exempt in the normalizer only.** It is inside Postgres' unquoted charset, so hitting one is not a charset violation but this reader stopping early — which is SP-C05's measured limitation and the reason the naive member exists. Refusing `$` would break the designed disagreement and the e2e gate arms.

## Deviations from Plan

None — plan executed as written. Two things worth surfacing that are *not* deviations:

- **Task 3 produced no diff.** The plan permits a test-file polish there only if the contracts or Node-22 runs surface one. Both were green on the first run, so the correct outcome was no edit.
- **The plan's line anchors for the base state are now stale by design** (see below). That is the intended consequence of the fix, not a deviation from the plan.

## Issues Encountered

**`plan-anchor-verify` went RED after the fix — resolved by this SUMMARY, deliberately not by editing the PLAN.**

`node scripts/verify-plan-anchors.mjs --pending` reported 4 stale claims, **all in `164.3.1-04-PLAN.md`** and all caused by this plan's own fix:

```
MISS …164.3.1-04-PLAN.md:103 [quote-absent]        const invokedDirectly =
MISS …164.3.1-04-PLAN.md:104 [quote-absent]        process.argv[1] && import.meta.url ===
MISS …164.3.1-04-PLAN.md:120 [quote-outside-range] [A-Za-z0-9_$]   (DEF_RE shifted 85-86 -> 87-88)
MISS …164.3.1-04-PLAN.md:190 [quote-absent]        const invokedDirectly =
FAIL: 4 stale claim(s) across 14 plan file(s).
```

These anchors describe the **pre-fix** code — they are the record of the measured defect. The script's own contract (`verify-plan-anchors.mjs:90`) is *"PENDING PLANS ONLY … A plan with a sibling SUMMARY has been executed; its claims are historical."* So the correct resolution is this SUMMARY existing, which removes plan 04 from the pending set. **Rewriting the PLAN's anchors to point at the fixed code would have destroyed the evidence of what was measured** — the exact substitution this phase exists to prevent.

Verified after writing this file: the 4 misses clear, and no sibling plan's anchors were affected (the RED run named only plan 04, never 01/02/03/05-12).

## Scope Fence

Intact. `git status --porcelain` over sibling-plan territory (`scripts/mutation-runner`, `scripts/prod-body-drift-check.sh`, `src/__tests__/lint-sql-gates.test.ts`, `src/__tests__/audit-coverage.test.ts`, `.github/workflows`) is **empty**, and `git show --stat` over both code commits lists only the three declared files:

```
860ce007  scripts/sql-body-normalize.mjs | scripts/sql-function-names-naive.mjs | src/__tests__/vac04-reader-guards.test.ts
e7f1ae6a  scripts/sql-body-normalize.mjs | scripts/sql-function-names-naive.mjs | src/__tests__/vac04-reader-guards.test.ts
```

No database was touched. `src/__tests__/drift-check-scripts.test.ts` was run repeatedly and never edited.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- **Plan 164.3.1-07 is unblocked and owes no propagation work.** It builds its zero-path warn, flip arm and absurdity floor on readers that can no longer silently no-op ([VAC04-C2]) or mis-subject ([VAC04-C4]), and the gate's existing `|| fail` wrappers already carry a reader refusal to a loud failure.
- **Carry SC-4 forward as PARTIAL**, with the founder-decision reason. The fail-closed flip and its pinning arm land in Phase 164.4.
- **Known limitation, stated rather than hidden:** the naive reader now over-refuses on legal-but-degenerate shapes its line-anchored view cannot resolve (e.g. a block comment butted directly against the name, `foo/*c*/(…)`, which the lexer member masks and accepts). This is the accepted direction and costs nothing on today's corpus (zero firings across 380 files). If a future migration trips it, the diagnostic names the exact file, identifier and byte — **do not widen the exemption ad hoc**; that would be corpus-premise drift requiring the follower rule to be re-grounded.
- **Do not DRY the two guard functions.** A shared guard reopens [VAC04-C2].

## Self-Check: PASSED

Every file and commit this SUMMARY claims was verified to exist on disk and in the log.

| Claim | Result |
|---|---|
| `scripts/sql-function-names-naive.mjs` | FOUND |
| `scripts/sql-body-normalize.mjs` | FOUND |
| `src/__tests__/vac04-reader-guards.test.ts` | FOUND |
| `.planning/phases/164.3.1-…/164.3.1-04-SUMMARY.md` | FOUND |
| commit `860ce007` (Task 1) | FOUND in `git log` |
| commit `e7f1ae6a` (Task 2) | FOUND in `git log` |
| fork base `e0660031` | confirmed as the parent of `860ce007` |
| `node scripts/verify-plan-anchors.mjs --pending` | `OK: 13 plan file(s), no stale claims` (167 claims checked) |
| `tsx scripts/check-planning-hygiene.ts` | OK — no local username or absolute home path |
| deletion check on both commits | no tracked file deleted |

---
*Phase: 164.3.1-sound-primitives*
*Plan: 04*
*Completed: 2026-09-01*
