# Phase 164.3.1 — Plan Outline

**Phase:** SOUND-PRIMITIVES — all FOUR cycling primitives closed by construction
**Mode:** chunked (outline-only) · TRACER_MODE=true · 12 plans · 5 waves
**Requirement set:** the nine ROADMAP success criteria (SC-1..SC-9) — `Requirements: TBD` in ROADMAP, so SC numbers are the acceptance surface.

## Plan Outline

| Plan ID | Objective | Wave | Depends On | Requirements |
|---|---|---|---|---|
| 164.3.1-01 | TRACER + Primitive A: hand-rolled statement tokenizer in `scripts/mutation-runner/parse.mjs` (`'…'`/`"…"`/`$tag$…$tag$`/`/* */`/`--` state ACROSS lines, emitting statements with start/end lines); route `executableText`, `isBranchHead`, `neuterArm`'s forward scan and `statementEndLine` through it — ONE definition of code. Seven P1 compound lines in `test_profiles_privileged_columns_locked.sql` classify correctly; [MUT-I01] closed both directions (P4 spurious refusal, P5 silent over-neuter); full corpus run stays green. Records the assumption-delta promotion line→statement. | 1 | — | [SC-2] |
| 164.3.1-02 | Measure-first (calibration at HEAD): build the Primitive-D AST rule REPORT-ONLY (`ts.createSourceFile` walk per `seam-log-coverage.test.ts` shape); observe and RECORD it flagging `src/__tests__/lint-sql-gates.test.ts:182-186` at HEAD, before that site is fixed; measure the HEAD-wide RED count across `src/__tests__/**/*.test.ts` to size the allowlist. No fixes in this plan. | 1 | — | [SC-5] |
| 164.3.1-03 | Calibration at HEAD: record [AUDCOV-01] cases A/B/C = `[]`/`[5]`/`[3]` driven through `audit-coverage.test.ts`'s real extracted bytes, before any fix — the live-regression "before" the post-fix table needs. No fixes in this plan. | 1 | — | [SC-5] |
| 164.3.1-04 | VAC-04 union readers hardened: lift the `check-banned-packages.mjs:443-452` realpath guard into BOTH members ([VAC04-C2], symlink + direct pins); refuse-rather-than-truncate on identifiers leaving `[A-Za-z0-9_$]` in both readers ([VAC04-C4], P10 input must ERROR); diagnostic-first error output. | 1 | — | [SC-4, SC-7] |
| 164.3.1-05 | Primitive B: source-location attribution replaces the in-query nonce (drop `stampIdentities`/`stampedIdentity`/`firstFailureArm`/`unstampedIdentities`, covering all three read sites incl. NOTICE-carried text); verbose psql in pg-lane; rule = gate-file `psql:` prefix + CONTEXT chain of EXACTLY ONE `inline_code_block … at RAISE` frame + tokenizer-resolved line; R4-C02 `current_query()` trigger scores NO-IDENTITY with a genuine-arm passing control in the same run; nested-`EXECUTE DO` forgery refused; GRAMMAR.md authoring rule recorded. Records the assumption-delta promotion nonce→source-location. | 2 | 164.3.1-01 | [SC-3, SC-7] |
| 164.3.1-06 | [AUDCOV-01] fix: quote state carried across lines in `unmatchedBlockOpen`/`stripBlockComments` with `quoteAtEnd` forced null for `'` and `"`; case A finds its site while B control still returns `[5]` and shipped arm C still `[3]`; A/B/C shipped as multi-line real-shape arms extracted at runtime (calibration "before" from 164.3.1-03). | 2 | 164.3.1-03 | [SC-5] |
| 164.3.1-07 | VAC-04 gate script: explicit exit-code branching on the name-index grep with `>= 2` a hard MEASURE_FAIL ([VAC04-C3]); zero path FAILS CLOSED with both readers' evidence printed before it refuses ([VAC04-C1]; AMENDED 2026-09-01 — blocks in THIS phase); the closed state pinned by an executable reopen pin; VAC-04 absurdity floor templated on VAC-08 `:320-350` with measured thresholds (359 definitions / 380 files / 122 names), proven two-directionally; sample size + coverage recorded beside every threshold. | 2 | 164.3.1-04 | [SC-4, SC-7, SC-8, SC-9] |
| 164.3.1-08 | Primitive-D rule goes BLOCKING with the measured-exception allowlist sized from 164.3.1-02's HEAD-wide count (exact-set, fail-by-name, each entry carries its measurement); fix the `:182-186` self-referential site; re-express [MUT-W02] as a structural parse of the `frontend` aggregator's branch conditions with a non-vacuity floor; red/green TS fixture pair per the `lint-sql-gates-fixtures/` contract. | 2 | 164.3.1-02 | [SC-5, SC-9] |
| 164.3.1-09 | Measure-first (SC-6): re-derive `ARMS_FLOOR` and the biting count under the sound primitives (R3-C01 discipline — re-measure the 104 identities / 103 backward scans against the real gate file); record any movement from today's measured 30/30/0 with the reason; update floor constants with measured-threshold comments. | 3 | 164.3.1-05 | [SC-6, SC-9] |
| 164.3.1-10 | Mutation-runner absurdity floor: internal cross-check `armsExecuted` vs actual lane invocations, `biting <= armsExecuted`, printed AND asserted in the `sql-mutation` job's count-recheck step; thresholds set from 164.3.1-09's re-derived numbers with sample size + coverage beside them; floor proven able to FIRE and proven SILENT on a legitimate finding. | 4 | 164.3.1-09 | [SC-7, SC-8, SC-9] |
| 164.3.1-11 | Regression corpus, primitives A+B: lane-driven `fixtures/selftest/*-gate.sql` arms — seven-line compound-head replica, [MUT-I01] parity pair, R4-C02 `current_query()` trigger, nested-`EXECUTE DO` forgery — each entry proven to RED by neutering its fix and observing the failure (recursive SC-1), restores sha-verified. | 5 | 164.3.1-05, 164.3.1-10 | [SC-1] |
| 164.3.1-12 | Regression corpus, primitives C+D + the VAC-08 instances (the 253-of-262 verdict and the three-convention join) as driven arms against `test-ledger-drift-check.sh`; meta-arms: diagnostic-first assertion (a gate in the family cannot ship a bare conclusion) and the bare-measurement audit (grep the family for justifying measurements, none bare); `frontend` aggregator `needs:` + result-loop wiring confirmed symmetric for every carrying job. | 5 | 164.3.1-06, 164.3.1-07, 164.3.1-08, 164.3.1-10 | [SC-1, SC-7, SC-9] |

## Constraint verification

- **SC coverage (all nine, no silent drops):** SC-1 → 11, 12 · SC-2 → 01 · SC-3 → 05 · SC-4 → 04, 07 (PARTIAL by founder decision — flip arm in 07, verification must record PARTIAL) · SC-5 → 02, 03, 06, 08 · SC-6 → 09 · SC-7 → 04, 05, 07, 10, 12 · SC-8 → 07, 10 · SC-9 → 07, 08, 09, 10, 12.
- **A before B:** tokenizer (01, Wave 1) precedes attribution (05, Wave 2) — B's line resolution consumes the tokenizer's statement spans.
- **Calibrations at HEAD in Wave 1, before their fixes:** 02 (rule flags `:182-186`) before 08; 03 (A/B/C = `[]`/`[5]`/`[3]`) before 06.
- **Measure-first as their own plans:** 02 (primitive-D HEAD-wide RED count, before blocking) and 09 (ARMS_FLOOR/biting re-derivation) — neither folded into a fix.
- **Corpus distinct and downstream of all four primitives:** 11 + 12 depend (directly or transitively) on 01 (A), 05 (B), 04/07 (C), 02/08 (D).
- **File-disjoint waves:** `parse.mjs`/`run.mjs` owned sequentially by 01 → 05 → 09 → 10 → 11 (never parallel); `ci.yml` by 10 → 12; `audit-coverage.test.ts` by 06 only (03 reads); `lint-sql-gates.test.ts` by 08 only (02 reads); VAC-04 readers by 04, gate script by 07. Wave-1 plans 01/02/03/04 and Wave-2 plans 05/06/07/08 and Wave-5 plans 11/12 are pairwise file-disjoint.
- **Out of scope honored:** [MUT-I02], [MUT-I03] stay in TODOS.md; Phase 164.4 backfill untouched.

## OUTLINE COMPLETE
