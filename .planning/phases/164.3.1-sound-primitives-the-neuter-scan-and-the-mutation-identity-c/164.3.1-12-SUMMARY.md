---
phase: 164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c
plan: "12"
subsystem: regression-corpus
tags: [regression-corpus, vac08, meta-arms, instance-registry, sc-1, sc-7, sc-9, anti-vacuity, neuter-restore]

# Dependency graph
requires:
  - phase: 164.3.1-06
    provides: "the [AUDCOV-01] case A/B/C arms the registry binds"
  - phase: 164.3.1-07
    provides: "the VAC04-C1 reopen pin, VAC04-C3 arm and the VAC-04 absurdity floor with its measured comment (an exemplar both meta-arms must pass)"
  - phase: 164.3.1-08
    provides: "the blocking SRO scan (SP-C04 / VAC-SELFREF-01), the MW02 fixture pair, and the standing aggregator pins the symmetry confirmation runs"
  - phase: 164.3.1-10
    provides: "the run.mjs absurdity-floor measured block and the ci.yml count-recheck step as left — ci.yml was not edited by this plan"
  - phase: 164.3.1-11
    provides: "self-test scenarios 9–12 and the four selftest fixtures the A/B registry entries bind, with their CORPUS-PROOFS"
provides:
  - "src/__tests__/drift-check-scripts.test.ts: the VAC08-JOIN arm — clauses read out of the gate script at run time, a fixed convention table for the rows, one ledger row per convention driven through the real gate, a no-convention contrast, an exact specificity pin (clause 2 measured subsumed by clause 4), and a missing/extra clause-set symmetry arm"
  - "src/__tests__/gate-family-meta.test.ts: PHASE_TAXONOMY (15) + INSTANCE_ARM_REGISTRY binding every measured instance to a named arm by file bytes, exact set both directions, fail-by-name with citations"
  - "gate-family-meta.test.ts: diagnostic-first meta-arm (SC-7) over FAMILY_SHELL_GATES with red/green fixtures and a 17-entry measured exact-set allowlist in four stated classes"
  - "gate-family-meta.test.ts: bare-measurement audit meta-arm (SC-9) over THRESHOLD_BEARING_FILES with red/green fixtures and KNOWN_THRESHOLD_SITES (6, all justified)"
  - "164.3.1-12-CORPUS-PROOFS.md: VAC-08 cycles 1 / 1b / 2, the registry fire-proof in both directions, the meta-arm predicate fire-proof — verbatim REDs, sha pairs, restores"
affects: [164.4, sql-tests, migration-drift-check, frontend-aggregator, phase-verification]

# Actuals (#2632) — chars/4 over git diff e59f231e..HEAD (3 files, 1319 insertions incl. CORPUS-PROOFS)
actuals:
  tokens: 18769
  tasks: 3
  commits: 4

tech-stack:
  added: []
  patterns:
    - "Rows from a FIXED table, predicate from the ARTIFACT: a driven arm whose fixture follows the artifact it checks cannot observe the artifact losing a clause (measured in cycle 1, first draft)"
    - "A structural pin records subsumption instead of asserting a runtime consequence that does not exist — the arm still reds on the change, and the record says why"
    - "Meta-arm allowlists are classified BEFORE the keys are transcribed from the scan, and the class is written per entry"
    - "A blinded predicate is caught twice: by its red fixture and by its exact-set allowlist's discharge direction"

key-files:
  created:
    - src/__tests__/gate-family-meta.test.ts
    - .planning/phases/164.3.1-sound-primitives-the-neuter-scan-and-the-mutation-identity-c/164.3.1-12-CORPUS-PROOFS.md
  modified:
    - src/__tests__/drift-check-scripts.test.ts

key-decisions:
  - "The join arm evaluates the script's clauses with a JS interpreter of exactly the four documented clause shapes and REFUSES any other by text — it cannot run SQL, and an unevaluated clause must red rather than be skipped"
  - "Clause 2 (version||'_'||name) is measured SUBSUMED by clause 4 (desc-only) for underscore-free versions; the arm pins that structure exactly instead of pretending its removal has a runtime effect, and the stale script header (:44-47) is reported, not patched"
  - "The registry binds names to files and cites proofs; it does not re-prove arms — stated in the file header"
  - "Diagnostic-first excludes ${GATE} from what counts as a runtime value: counted, the rule would be satisfied by every line and bind nothing"
  - "KNOWN_THRESHOLD_SITES is an exact set (both directions) rather than a >= floor: a threshold leaving the family is a decision worth a red"
  - "ci.yml left byte-identical: the standing symmetry pins ran green, so the conditional ownership was never exercised"

patterns-established:
  - "Instance→arm registry entry: { instance, primitive, file, armNeedle, also?, fixture?, citation } — every entry names its shipping plan and the record proving the arm can fail"

requirements-completed: [SC-1, SC-7, SC-9]

coverage:
  - id: D1
    description: "VAC08-JOIN is a driven arm proven to RED under a removed clause (cycle 1) and pinned against silent subsumption drift (cycle 1b)"
    requirement: "SC-1"
    verification:
      - kind: unit
        ref: "src/__tests__/drift-check-scripts.test.ts#VAC08-JOIN: a ledger row matching under EACH convention is not reported missing; one matching under NONE still is, by name"
        status: pass
      - kind: manual_procedural
        ref: "164.3.1-12-CORPUS-PROOFS.md §2 (RED naming 20260103000000_join_bare_ts; restore c9857646) and §3"
        status: pass
    human_judgment: false
  - id: D2
    description: "VAC08-253's absurdity-floor family is confirmed by name and proven load-bearing: floor disabled → the RED arm fails with the drift finding in place of MEASURE_FAIL, controls green"
    requirement: "SC-1"
    verification:
      - kind: unit
        ref: "src/__tests__/drift-check-scripts.test.ts#RED: a populated ledger matching under half the repo is MEASURE_FAIL, not drift"
        status: pass
      - kind: manual_procedural
        ref: "164.3.1-12-CORPUS-PROOFS.md §4 (cycle 2; restore c9857646; --self-test 5/5)"
        status: pass
    human_judgment: false
  - id: D3
    description: "Instance→arm registry: 15 instances across A/B/C/D bound by file bytes, exact set both directions, fail-by-name; fire-proof in both directions"
    requirement: "SC-1"
    verification:
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#EXACT SET, both directions: registry instances === phase taxonomy, failing by name"
        status: pass
      - kind: manual_procedural
        ref: "164.3.1-12-CORPUS-PROOFS.md §5a/§5b (needle renamed → RED naming AUDCOV-01; entry dropped → RED naming MUT-W02; restore d7a9d239)"
        status: pass
    human_judgment: false
  - id: D4
    description: "Diagnostic-first meta-arm over the family's shell gates with a red fixture proven to fire and a 17-entry measured exact-set allowlist"
    requirement: "SC-7"
    verification:
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#every failure emission prints a runtime value — exact against DIAGNOSTIC_FIRST_ALLOWLIST, both directions"
        status: pass
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#diagnostic-first: the RED fixture's bare-conclusion block AND bare fail are flagged; the GREEN fixture is clean and really read"
        status: pass
    human_judgment: false
  - id: D5
    description: "Bare-measurement audit over the family's thresholds: 6 sites found, every one carrying a measurement token and a date within 80 lines; red fixture proven to fire"
    requirement: "SC-9"
    verification:
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#every threshold carries a measurement token and a date within the window, and the found set is exactly KNOWN_THRESHOLD_SITES"
        status: pass
      - kind: unit
        ref: "src/__tests__/gate-family-meta.test.ts#bare-measurement: the RED fixture's unjustified floor and literal comparison are flagged; the GREEN fixture's measured floor is not"
        status: pass
    human_judgment: false
  - id: D6
    description: "Aggregator symmetry confirmed for every carrying job by the standing machine pins, on an untouched ci.yml"
    verification:
      - kind: unit
        ref: "src/__tests__/lint-sql-gates.test.ts (43 passed: $job is BLOCKING — in the aggregator's needs AND in its result loop ×3; parse 11 conditions / 3 job arms; tolerance set exactly the three)"
        status: pass
    human_judgment: false
  - id: D7
    description: "Whether each of the 17 allowlisted bare emissions is a legitimate exception (PRECONDITION / REDACTED / EMPTY-BY-COND / READER-STDERR) rather than an excuse"
    verification: []
    human_judgment: true
    rationale: "The machine asserts that a reason string exists per entry; that the reason is TRUE of the site is a reading judgment. The classes were fixed before the keys were transcribed, and the READER-STDERR/REDACTED entries carry a named follow-up (exit code not captured) rather than a claim of adequacy"

duration: 26min
completed: 2026-09-02
status: complete
---

# Phase 164.3.1 Plan 12: Regression corpus C + D, instance→arm registry, and the family meta-arms Summary

**Both VAC-08 instances are now driven arms proven able to fail — the join clauses are read out of `test-ledger-drift-check.sh` at run time and driven through the real gate one convention per row (a deleted clause reds naming its row; the absurdity floor disabled reds the standing populated-ledger arm) — and the corpus has its anti-decay gate: an exact-set registry binding all 15 measured instances across A/B/C/D to named arms by file bytes, beside two meta-arms that make SC-7 (no bare conclusions: 68 emissions scanned, 17 justified exceptions) and SC-9 (no bare thresholds: 6 found, 6 measured) permanent machine checks, each with red fixtures observed firing.**

## Performance

- **Duration:** 26 min
- **Started:** 2026-09-02T06:12:58Z
- **Completed:** 2026-09-02T06:39:00Z
- **Tasks:** 3
- **Files modified:** 3 (2 created, 1 modified); `scripts/test-ledger-drift-check.sh` and `.github/workflows/ci.yml` byte-identical to HEAD before and after

## Accomplishments

- **VAC08-JOIN arm** (`drift-check-scripts.test.ts`, describe *VAC08-JOIN — the join key is the union of every ledger naming convention*). Reads the `missing` predicate's OR-clauses from the script bytes; an interpreter of the four documented clause shapes refuses any unknown clause by text; rows come from a FIXED convention table (one per convention, each in the shape the ledger really stores) and are driven through the real gate via the injected-command idiom. Direction 1: every row found (`ledger presence: 0 absent`). Direction 2: a row matching under no convention is still named absent. Plus an exact specificity pin and a missing/extra clause-set symmetry arm.
- **Clause-removal RED, quoted** (cycle 1, bare-ts clause deleted from the missing query): `::error::VAC-08 repo-vs-TEST ledger and body drift gate: 1 repo migration(s) are not present in the TEST ledger and are NOT baselined: 20260103000000_join_bare_ts` → `expected 1 to be +0`. The symmetry arm reddened beside it (`expected [ 'm.name = r.fname', …(3) ] to deeply equal [ 'm.name = r.fname', …(2) ]`).
- **VAC08-253 proven load-bearing** (cycle 2): floor comparison replaced by `false` → the standing RED arm fails with the drift sentence in place of `MEASURE_FAIL`; its two controls stayed green in the same run.
- **Instance→arm registry** with 15 entries, every needle quoted from the target file's bytes (verified once each), citations to the proving record per entry; fire-proof in both directions.
- **Two meta-arms** with embedded red/green fixtures fed to the same predicates as the family scan; TDD RED observed against stubs (`findings were []` / `sites were []`), GREEN through the real predicates; predicate blinded → red fixture AND allowlist discharge both fire.
- **Symmetry confirmation** ran the standing pins green; `ci.yml` untouched.

## The registry's 15 instances

| Primitive | Instance | Arm file | Binding |
|---|---|---|---|
| A | R4-C01/P3 | `scripts/mutation-runner/run.mjs` (+ `mutation-runner-neuter.test.ts`) | scenario 9/12 header; fixture `compound-head-gate.sql` |
| A | MUT-I01-P4 | `scripts/mutation-runner/run.mjs` (+ neuter test) | scenario 10/12 P4 half; fixture `comment-parity-gate.sql` |
| A | MUT-I01-P5 | `scripts/mutation-runner/run.mjs` (+ neuter test) | scenario 10/12 P5 half; same fixture |
| B | R4-C02 | `scripts/mutation-runner/run.mjs` | scenario 11/12; fixture `current-query-forge-gate.sql` |
| B | NESTED-EXECUTE | `scripts/mutation-runner/run.mjs` | scenario 12/12; fixture `nested-execute-forge-gate.sql` |
| C | VAC04-C1 | `src/__tests__/drift-check-scripts.test.ts` | REOPEN PIN (execution direction) |
| C | VAC04-C2 | `src/__tests__/vac04-reader-guards.test.ts` | main-module guard describe |
| C | VAC04-C3 | `src/__tests__/drift-check-scripts.test.ts` | VAC04-C3 RED arm |
| C | VAC04-C4 | `src/__tests__/vac04-reader-guards.test.ts` | charset refusal describe |
| C | VAC08-253 | `src/__tests__/drift-check-scripts.test.ts` | populated-ledger MEASURE_FAIL arm |
| C | VAC08-JOIN | `src/__tests__/drift-check-scripts.test.ts` | the new join arm |
| D | VAC-SELFREF-01 | `src/__tests__/self-referential-oracle.test.ts` (+ `lint-sql-gates.test.ts` fixed site) | SRO-01 red fixture arm; fixture `SRO-01-same-block-const.red.ts` |
| D | SP-C04 | `src/__tests__/self-referential-oracle.test.ts` | BLOCKING corpus scan; same fixture |
| D | AUDCOV-01 | `src/__tests__/audit-coverage.test.ts` | case A arm |
| D | MUT-W02 | `src/__tests__/lint-sql-gates.test.ts` | MW02 red-fixture arm; fixture `MW02-alternate-spelling.red.yml` |

## The meta-arms' fixture-fire observations, quoted

**Diagnostic-first** — blinded predicate (`carriesRuntimeValue` → `true`), 164.3.1-12-CORPUS-PROOFS §5c:

```
META diagnostic-first: 68 emission(s) over 2 gate(s), 0 bare, 17 allowlisted
     × diagnostic-first: the RED fixture's bare-conclusion block AND bare fail are flagged; …
     × every failure emission prints a runtime value — exact against DIAGNOSTIC_FIRST_ALLOWLIST, both directions
AssertionError: the rule must flag the fixture's bare ::error:: block and its bare fail call; findings were []: expected [] to deeply equal [ 'error-block', 'fail-call' ]
AssertionError: allowlist entr(y/ies) no longer match a bare emission — … expected [ …(17) ] to deeply equal []
```

**Bare-measurement** — TDD RED against the stub (commit `03585b88`):

```
AssertionError: the scan must find both the ROWS_FLOOR constant and the literal `-ge 25` comparison; sites were []: expected [] to deeply equal [ 'ROWS_FLOOR=7', 'other -ge 25' ]
```

and the GREEN scan over the family, printed by the arm on every run:

```
META bare-measurement: 6 threshold site(s) over 5 file(s)
  ok   scripts/test-ledger-drift-check.sh :: ledger_rows -ge 50 (line 339) — "scored" + 2026-08-29 within 80 lines
  ok   scripts/prod-body-drift-check.sh :: SNAPSHOT_MIN=50 (line 547) — "measured" + 2026-09-01 within 80 lines
  ok   scripts/mutation-runner/run.mjs :: FILES_FLOOR=1 (line 129) — "MEASURED" + 2026-08-29 within 80 lines
  ok   scripts/mutation-runner/run.mjs :: ARMS_FLOOR=30 (line 197) — "MEASURED" + 2026-08-29 within 80 lines
  ok   src/__tests__/lint-sql-gates.test.ts :: RESULT_LOOP_CONDITION_FLOOR=8 (line 480) — "measured" + 2026-09-01 within 80 lines
  ok   src/__tests__/self-referential-oracle.test.ts :: CORPUS_FLOOR=100 (line 133) — "MEASURED" + 2026-09-01 within 80 lines
```

The diagnostic-first allowlist was **measured first with the list empty** (68 emissions, 17 bare), the four exception classes decided, then the keys transcribed. Non-vacuity floor `EMISSION_FLOOR = 40` against measured 68 (wide separation, D-10).

## Symmetry confirmation (binding constraint 8)

`npx vitest run src/__tests__/lint-sql-gates.test.ts` → 43 passed, including `$job is BLOCKING — in the aggregator's needs AND in its result loop` for `sql-gate-lint`, `sql-mutation`, `plan-anchor-verify`; the structural parse printed `11 condition(s), 3 job arm(s) [e2e-seeded, sql-tests, plan-anchor-verify], tolerance-bearing: e2e-seeded(is_fork_pr), sql-tests(is_fork_pr+is_dispatch), plan-anchor-verify(is_pr)`; the population-derivation arm agreed with `AGGREGATED_JOBS`. **No asymmetry found; `ci.yml` was not edited** (`git status --porcelain -- .github/workflows/ci.yml` empty throughout).

## ⛔ SC-4 is MET (amended D-07) — read it from plan 07's record

Success criterion 4 — the VAC-04 zero path fails closed — is **MET**, per CONTEXT.md § Amendment 2026-09-01 and `164.3.1-07-SUMMARY.md` (§ *SC-4 is MET — never PARTIAL*; the D-13 follow-up narrows which PRs are held without retiring the refusal). This plan's registry binds VAC04-C1 to plan 07's reopen pin and does not re-prove it. The withdrawn PARTIAL instruction in `164.3.1-04-SUMMARY.md` is superseded. The migration-PR ordering constraint travels with it: **hold migration PRs until 164.3.1 and 164.4 have both landed.**

## Task Commits

1. **Task 1: VAC08-JOIN arm + recursive proofs (cycles 1, 1b, 2)** — `3a9cb144` (test)
2. **Task 2: instance→arm registry + fire-proof both directions** — `26163834` (feat)
3. **Task 3 RED: meta-arm fixtures against stub predicates** — `03585b88` (test)
4. **Task 3 GREEN: both meta-arms, measured allowlist and threshold set** — `5aa5038b` (feat)

**Plan metadata:** the docs commit carrying this file and the §5c append to CORPUS-PROOFS.

## TDD Gate Compliance

- Task 3 (`tdd="true"`): RED `03585b88` (`test(...)`) → GREEN `5aa5038b` (`feat(...)`), in order; no refactor commit needed.
- Task 1 (`tdd="true"`): the fix under test (the four-clause join) predates the arm, so a pre-fix RED could not exist; the RED was observed **under the neuter** of the fix (CORPUS-PROOFS §2b) before the arm was committed as `test(...)` (`3a9cb144`). No production code changed, so there is no `feat` commit for Task 1 — the test commit is the whole delivery. Stated here rather than left for the gate scanner to infer.

## Files Created/Modified

- `src/__tests__/drift-check-scripts.test.ts` — the *VAC08-JOIN* describe: `JOIN_CONVENTIONS`, `readJoinClauses`, `conventionsOf` (refuses unknown clauses), `scaffoldJoinCase`, the driven arm with contrast and `EXPECTED_SPECIFICITY`, the clause-set symmetry arm. 279 tests (was 277).
- `src/__tests__/gate-family-meta.test.ts` — new: header with both predicates' honest bounds; `PHASE_TAXONOMY`, `INSTANCE_ARM_REGISTRY`, the registry arms; `FAMILY_SHELL_GATES`, `findFailureEmissions`, `carriesRuntimeValue`, `diagnosticFirstViolations`, `emissionKey`, `DIAGNOSTIC_FIRST_ALLOWLIST`, `EMISSION_FLOOR`; `THRESHOLD_BEARING_FILES`, `findThresholdSites`, `KNOWN_THRESHOLD_SITES`; four embedded fixtures. 21 tests.
- `164.3.1-12-CORPUS-PROOFS.md` — new: provenance, cycles 1 / 1b / 2, registry fire-proof 5a/5b, meta-arm fire-proof 5c, non-claims.

## Verification

| Check | Result |
|---|---|
| `vitest run drift-check-scripts + gate-family-meta + lint-sql-gates` | 3 files, **344 passed** |
| `bash scripts/test-ledger-drift-check.sh --self-test` | **OK 5/5**, exit 0 (before the first window and after the last restore) |
| `vitest run src/__tests__/contracts` | 5 files, **109 passed** |
| `vitest run self-referential-oracle.test.ts` (blocking SRO scan incl. the new file) | 6 passed; **2 findings / 1 file / 131 scanned** — unchanged, no new self-referential oracle |
| Node 22 (`v22.22.1`): drift-check-scripts 279/279; gate-family-meta 21/21 | pass |
| `tsc --noEmit` | exit 0 |
| `eslint` on both test files | clean |
| `grep -ac "measured\|MEASURED" gate-family-meta.test.ts` | 31 |
| taxonomy grep (13 IDs) | OK |
| `git status --porcelain` on the gate script and ci.yml | empty throughout |
| `check-planning-hygiene` | OK — no local username or home path in the new planning file |

## Decisions Made

See `key-decisions`. The one worth restating: when the measurement showed clause 2 is subsumed by clause 4, the arm was made to pin the **measured structure** rather than to assert a runtime consequence that does not exist. It still reds on the clause's removal (cycle 1b), and the record says truthfully *why*.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug, in this plan's own arm] The first join arm built its rows from the script's clause list**
- **Found during:** Task 1, cycle 1 (the neuter window)
- **Issue:** rows derived from the clauses just read → deleting a clause deleted its row → the arm stayed GREEN under the exact neuter it exists to catch (only the symmetry arm reddened). A Primitive-D shape inside a Primitive-C arm.
- **Fix:** rows from the fixed `JOIN_CONVENTIONS` table; only the predicate comes from the script. Re-ran the cycle: RED naming the row. The arm's comment carries the measurement.
- **Files modified:** `src/__tests__/drift-check-scripts.test.ts`
- **Verification:** CORPUS-PROOFS §2a (first draft) and §2b (corrected RED)
- **Committed in:** `3a9cb144` (fixed before the task commit)

**2. [Rule 2 - Missing critical] Structural specificity pin added beside the driven verdict**
- **Found during:** Task 1
- **Issue:** the plan's "removing any clause reds it" is false at runtime for clause 2 (subsumed). Without a pin, clause 2 could be dropped with no signal, and the arm's claim would be quietly wrong.
- **Fix:** `EXPECTED_SPECIFICITY` exact pin (measured); cycle 1b proves it fires.
- **Committed in:** `3a9cb144`

**3. [Rule 2 - Missing critical] Registry fire-proof done in BOTH directions, and the meta-arm predicate fire-proof added**
- **Found during:** Tasks 2 and 3
- **Issue:** the plan asked for the needle-rename direction only, and for fixture arms only. An exact set has two directions and a red fixture proves the predicate on one input; the blinded-predicate run is what shows the family scan itself can fail.
- **Fix:** §5b (entry dropped → RED naming MUT-W02) and §5c (predicate blinded → fixture + discharge both RED).
- **Committed in:** evidence-only (restores byte-identical); recorded in CORPUS-PROOFS

---

**Total deviations:** 3 auto-fixed (1 bug in new code, 2 missing-critical). **Impact:** all strengthen the anti-vacuity properties the plan asked for; no file outside the declared set was touched.

## Issues Encountered

1. **`test-ledger-drift-check.sh` header :44-47 is stale.** "BOTH clauses are required; neither is redundant" was true of two clauses and is false of clause 2 now that four exist (measured, cycle 1b). Not patched: the script is edited by no plan in this phase. **Follow-up:** amend the comment beside the four-clause block.
2. **Seven `|| fail` sites print no exit code.** `prod-body-drift-check.sh` :208/:222/:429/:431 (reader stderr passes through) and :470/:473, `test-ledger-drift-check.sh` :302 (stderr withheld by the public-log rule) — the child's exit CODE is neither captured nor printed. Allowlisted with their class and this note. **Follow-up:** capture rc with the `grep_rc` idiom and name it in the `fail` text.
3. **`gsd windows append` refused both follow-ups above:** `Ledger counts disagree with entries: frontmatter open/waived/fixed/total=26/0/2/28 but entries yield 34/0/2/36`. `.planning/WINDOWS.md` is outside this plan's file set and the ledger is best-effort; nothing was written. The mismatch itself (8 entries unaccounted in the frontmatter) needs a hand fix by whoever next touches the ledger.
4. **STATE.md `state.advance-plan`** — expected to refuse with `ambiguous_position_phase` per the executor notes; recorded in the state-update notes below.

## Known Stubs

None. No placeholder, skipped test or unrun `<verify>`; every new arm was observed RED (against a stub or under a neuter) before being called an arm.

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema surface. Both test files read repo files via node:fs and drive shell gates over scratch fixtures with injected commands; no database, no `supabase` CLI, zero packages installed (T-164.3.1-12-SC accepted as planned). T-164.3.1-12-01..04 mitigated as the register states: clause-removal RED (01), registry fail-by-name both directions (02), meta-arms with fixtures and exact-set allowlists (03), symmetry pins green on an untouched ci.yml (04).

## User Setup Required

None.

## Next Phase Readiness

- **Phase verification:** SC-1 corpus-wide (registry + the C/D proofs here, A/B in plan 11), SC-7 and SC-9 as permanent arms; **SC-4 MET** from plan 07's record. Every arm in the corpus is in a file the frontend vitest shards run, or in the `sql-mutation` self-test the aggregator gates.
- **Phase 164.4:** a new gate in this family must (a) print a runtime value in every failure emission or add a justified `DIAGNOSTIC_FIRST_ALLOWLIST` entry, (b) register any new threshold in `KNOWN_THRESHOLD_SITES` with its measurement beside it, (c) if it fixes a measured instance, add it to `PHASE_TAXONOMY` and the registry with a citation. Raising `FILES_FLOOR`/`ARMS_FLOOR` changes the `KNOWN_THRESHOLD_SITES` labels (`FILES_FLOOR=1` → `FILES_FLOOR=N`) — update them in the same commit.
- **Carried residuals:** the three follow-ups in *Issues Encountered*; the family gates have still never been observed on their real hosts (WINDOWS.md 25/26/28).

## Self-Check: PASSED

- files: gate-family-meta.test.ts, drift-check-scripts.test.ts, 164.3.1-12-CORPUS-PROOFS.md, this SUMMARY — FOUND
- commits 3a9cb144, 26163834, 03585b88, 5aa5038b — FOUND in git log
- no tracked file deleted in any commit (diff-filter=D empty each time)
- scripts/test-ledger-drift-check.sh and .github/workflows/ci.yml — porcelain empty
