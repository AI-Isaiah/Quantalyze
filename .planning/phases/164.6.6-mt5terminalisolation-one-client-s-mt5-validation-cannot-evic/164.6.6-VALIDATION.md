---
phase: "164.6.6"
slug: "mt5terminalisolation-one-client-s-mt5-validation-cannot-evic"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-22"
---

# Phase 164.6.6 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

⚠️ **Seeded from `164.6.6-RESEARCH.md` § Validation Architecture. Every file path below was
re-verified to exist by the orchestrator on 2026-09-22 before this file was written** — this repo
has a dated case of a planner authoring an unrunnable verify command.

⛔ **THIS PHASE IS NOT EXECUTABLE YET (D-13).** 164.6.5 ships first by ROADMAP order
(*"availability first"*). These commands are the contract for when it does run; running them now
verifies nothing about this phase's unwritten work.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (Python) — `analytics-service/pytest.ini` ✓, `analytics-service/pyproject.toml` ✓ |
| **Config file** | `analytics-service/pytest.ini` |
| **Quick run command** | `cd analytics-service && ./.venv/bin/python -m pytest tests/test_mt5_session_episodes.py tests/test_mt5_concurrency.py tests/test_mt5_session_monitor.py -x` |
| **Full suite command** | `cd analytics-service && ./.venv/bin/python -m pytest` |
| **Estimated runtime** | quick ~30-60 s; full suite several minutes |

⛔ **RUN ONLY FROM `analytics-service/`, via that directory's `./.venv/bin/python`.** A repo-root
invocation causes cassette misses and **LIVE broker calls**. ⛔ A run reporting `0 collected` is a
LOUD FAILURE, never a pass.

⛔ **Never start a local `uvicorn main:app`** — it claims REAL production compute jobs.

---

## Sampling Rate

- **After every task commit:** the quick run command above (scoped to the three touched MT5 test files)
- **After every plan wave:** the full suite command
- **Before `/gsd-verify-work`:** full suite must be green
- **Max feedback latency:** ~60 seconds

---

## Per-Task Verification Map

⚠️ Task IDs are not yet assigned — the planner has not run. This maps the phase's four ROADMAP
criteria to their verification shape so the planner fills IDs against a fixed contract rather than
inventing one.

| Criterion | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---|---|---|---|---|---|---|---|---|---|
| C1 — eviction prevented or made harmless | TBD | TBD | ROADMAP 164.6.6 #1 | — | One client's validate cannot destroy another's authorized session | unit, neuter-calibrated | `cd analytics-service && ./.venv/bin/python -m pytest tests/test_mt5_concurrency.py -x` | ✅ exists, needs extension | ⬜ pending |
| C2 — account list never enumerated | TBD | TBD | ROADMAP 164.6.6 #2 | Information Disclosure | No code path enumerates or logs the Navigator account list; checks report COUNTS and VERDICTS only | unit (assert absence) | new test, name assigned by planner | ❌ W0 | ⬜ pending |
| C3 — an outage reaches a human | TBD | TBD | ROADMAP 164.6.6 #3 | — | A dark/stale session escalates within the blind-run threshold | unit, neuter-calibrated | `cd analytics-service && ./.venv/bin/python -m pytest tests/test_mt5_session_episodes.py -x` | ⚠️ file exists; the escalation test does not | ⬜ pending |
| C4 — the ownership decision is recorded | TBD | TBD | ROADMAP 164.6.6 #4 | — | D-10 recorded with the reason it beat the other two | manual-only — a decision record, not a behavior | N/A | N/A | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

⛔ **C4 IS MANUAL BY NATURE AND MUST NOT BE GIVEN A FAKE AUTOMATED CHECK.** Asserting that a
decision record exists tests the file system, not the decision. It closes at the plan's
`checkpoint:decision`.

---

## Wave 0 Requirements

- [ ] A test asserting no code path in this phase's new work enumerates or logs the Navigator
      account list (covers C2). ⛔ The test itself reports a COUNT or a BOOLEAN — it must never
      capture the list in order to assert on it, or the test becomes the disclosure (D-05).
- [ ] A neuter-calibrated test for the new escalation at the `blind = (...)` site in
      `analytics-service/services/mt5_session_episodes.py` (covers C3), structured like
      `analytics-service/tests/test_prober_cadence_alert.py` ✓ — which already calibrates the
      sibling `_escalate_prober_cadence_gap` pattern. ⭐ Read that file as the template rather
      than re-deriving the shape.
- [ ] Whichever D-10 option is chosen may need its own test file if it introduces new modules
      (a restore-on-release path inside `mt5_concurrency.py`'s `finally`, or an ephemeral-terminal
      lifecycle module). ⛔ Cannot be named more specifically until C4 is decided — that is a real
      dependency, not an omission.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|---|---|---|---|
| The D-10 ownership decision and its reasoning | ROADMAP 164.6.6 #4 | A recorded decision is not a runtime behavior; the ROADMAP names it a founder decision that must not be taken silently | Confirm the chosen option is recorded with the reason it beat the other two, inside the D-02/D-03/D-05 fence, at the plan's `checkpoint:decision` |
| Anything requiring a live `mt5-gateway` op | — | ⛔ A live MT5 op is a FOUNDER act — no redeploy, restart or `railway ssh` by an agent | Plan as `checkpoint:human-action` with a written procedure; ⚠️ reading Railway logs is read-only and permitted |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] ⭐ **Every new assertion proven able to FAIL** — neuter → observe RED → restore from a `cp`
      byte backup verified with `cmp`. ⛔ Never `git checkout -- <path>` / `git restore` /
      `git stash`. A test that cannot fail is worse than none.
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
