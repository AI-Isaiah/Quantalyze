---
phase: 164.6.6
plan: 04
type: execute
wave: 2
depends_on: ["164.6.6-01", "164.6.6-03"]
files_modified:
  - .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md
  - .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-CONTEXT.md
  - .planning/ROADMAP.md
  - TODOS.md
autonomous: false
requirements: ["164.6.6-C1", "164.6.6-C2", "164.6.6-C4"]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The terminal-ownership model is chosen BY THE FOUNDER at a blocking checkpoint, with the reason it beat the other two recorded — never taken silently and never defaulted to by an agent (D-10, ROADMAP criterion 4)."
    - "The costing the founder sees is re-measured at execution time, not inherited: `164.6.5`'s D-05 state is re-read at HEAD before any option is priced, because D-12 makes the two decisions coupled and this phase's research has a stated expiry."
    - "Whichever option is chosen is recorded against how it satisfies D-02 (restoration), D-03 (the occupant that holds no lease) and D-05 (never an inventory). An option that satisfies only two of the three is not eligible."
    - "The chosen answer lands in BOTH `164.6.6-CONTEXT.md` and `.planning/ROADMAP.md`. An amendment touching one file leaves the refused claim alive in the other."
    - "Criterion 2's accumulation remedy is recorded as INHERITING the chosen answer, naming which one — never planned standalone (D-06)."
    - "`MT5-WEDGE-OBS-01` is cross-linked and REFINED for the label-dedup salience defect, never reopened (D-09). `[MT5-VERDICT-SINK-01]` stays deferred and named (D-14)."
  artifacts:
    - ".planning/phases/164.6.6-.../164.6.6-DECISION-D10.md — the dossier: the re-measured inputs, the three options priced against them, and the recorded outcome"
    - ".planning/phases/164.6.6-.../164.6.6-CONTEXT.md — D-10 moved from OPEN to CLOSED with the reason"
    - ".planning/ROADMAP.md — § Phase 164.6.6 criterion 4 carrying the same answer"
    - "TODOS.md — the `MT5-WEDGE-OBS-01` refinement for the label-dedup defect"
  key_links:
    - "`164.6.5-CONTEXT.md` D-05 and `164.6.5-02-PLAN.md` -> this dossier's costing of options 1 and 2. If 164.6.5 pays the image-ownership cost, options 1/2 get materially cheaper; if it does not, they stay at full cost. ⛔ Read its answer; do not re-derive it."
    - "`164.6.6-01`'s scalar alarm shape -> the cost of options 1 and 2. The episode recorder is SINGULAR by construction; a plural terminal invalidates its counter, its stamp and its new alarm at once."
    - "`164.6.6-03`'s worker-count pin -> the cost of option 2. A pool that is serialized only within one event loop inherits the same process-local limit the pin now observes."
    - "This checkpoint -> plans 05+. The criterion-1 remedy and criterion 2's accumulation remedy CANNOT be authored until it resolves; that is a real dependency, not an omission."
---

<objective>
Close the one decision this phase deliberately left open, and close it the way the ROADMAP says
it must be closed: by a founder, with the reason it beat the other two recorded.

⛔ **THIS PLAN DOES NOT IMPLEMENT AN OWNERSHIP MODEL.** It assembles the evidence, presents the
three candidates at a blocking `checkpoint:decision`, and records the outcome in every place
this repo tracks decisions. The remedy itself is authored afterwards, by re-running
`/gsd-plan-phase 164.6.6`, because its files, its tests and its risks are all different under
each answer and a plan written before the answer would be fiction.

⛔ **AN AGENT MUST NOT CLOSE D-10 BY DEFAULT.** It is rated one-way: options 1 and 2 make this
repo the builder and operator of a container image it only consumes today, and unwinding that
means re-pointing the deploy and re-establishing an upstream pin the broker's terminal
self-update already makes unreliable.

⚠️ **D-13 — THIS PHASE IS PLANNABLE NOW, NOT EXECUTABLE NOW.** Phase 164.6.5 ships first by
ROADMAP order (*"availability first: this phase changes the terminal's ownership model, which is
only safe once validation stops wedging it"*) and is parked behind founder gates. This plan is
written to that gate. Do not execute it ahead of 164.6.5, and do not edit, merge or rebase the
`feat/164.6.5-mt5validatewedge` branch from here.

⭐ The phase tracer is `164.6.6-01` Task 1. This plan is the decision that gates the remaining
expansion.

Purpose: ROADMAP criterion 4 — a founder decision, not to be taken silently.
Output: a dossier, a recorded choice, and the same answer in every ledger that carries it.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-CONTEXT.md
@.planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-RESEARCH.md
@.planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-PATTERNS.md
@analytics-service/services/mt5_concurrency.py
@analytics-service/services/mt5_client.py
@deploy/mt5-gateway/railway-gateway.md
</context>

## What was already measured at planning time — ⛔ INPUTS, NOT CONCLUSIONS. Re-take every one.

Recorded here so the dossier starts from a stated position rather than a blank page, and so a
re-measurement that DISAGREES is visible as a disagreement. `164.6.6-RESEARCH.md` gives itself a
7-14 day validity for exactly this reason.

| # | Measured 2026-09-22 | Why it prices an option |
|---|---|---|
| M1 | `164.6.5-CONTEXT.md` D-05 still reads `DELIBERATELY LEFT OPEN` at branch head `65b7b986` (research checked it at `89cc0358`; this is a second, later reading of the same file). | D-12: options 1/2 are cheap only if 164.6.5 has already paid the image-ownership cost. |
| M2 | `164.6.5-02-PLAN.md` is where D-05 closes, behind its own checkpoint. Its `files_modified` are `analytics-service/services/mt5_client.py`, `analytics-service/tests/test_mt5_client_contract.py`, `deploy/mt5-gateway/railway-gateway.md` — **no Dockerfile** — and its must_haves require the recovery to restart the terminal PROCESS inside the same container, under the same Wine prefix and the same named volume. No SUMMARY exists for it, so the checkpoint has NOT been taken. | 164.6.5's PLANNED shape does not pay the image-ownership cost. On current evidence options 1/2 stay at full cost. ⚠️ A plan is not a decision — re-check for a `164.6.5-02-SUMMARY.md` before relying on this. |
| M3 | No Dockerfile exists anywhere under `deploy/`. The gateway runs the third-party prebuilt image via Railway *Deploy from Docker Image*; `164.6.6-PATTERNS.md` confirms no in-repo analog for wrapping it, and `analytics-service/Dockerfile` (a plain Python app image) is not one. | Options 1 and 2 are genuinely new infrastructure ownership, not an edit to something that exists. |
| M4 | An ephemeral terminal has no persistent Wine prefix, and that prefix is what makes unattended auto-login work — `164.6.5` D-07 rates it one-way. | Option 1 trades a one-way property for a one-way property. |
| M5 | `analytics-service/services/mt5_session_episodes.py` is SINGULAR by construction: `record_mt5_session_reading` takes no terminal identity, `_CONSECUTIVE_NOT_MEASURED_READINGS` is a scalar `int`, `_LAST_MEASURED_READING_MONOTONIC` a scalar `float`, and `164.6.6-01`'s new alarm window is a scalar too, deliberately. The `assumption-delta` detector FIRED on this phase with a `pluralization` signal, correctly. | Options 1 and 2 make the terminal plural and invalidate the counter, the stamp AND the criterion-3 alarm at once. That rework is a cost of those options and belongs in their column. |
| M6 | `bump_mt5_terminal_epoch` advances on EVERY lease exit — normal, raising and cancelled — and is called from `mt5_terminal_lease`'s `finally` plus the worker's raw-lock release paths; the registry is a process-local `dict[str, int]` cleared only by a test-only reset. | Input to the falsifiable test in task 1. |
| M7 | `deploy/mt5-gateway/docker-compose.yml` and `deploy/mt5-gateway/railway-gateway.md` describe the gateway; `docs/runbooks/mt5-go-live.md` § Step 1 pins BOTH services to exactly one replica and says a scale-up is a correctness change. | Option 2 (pooling) needs a placement rule nothing in this repo has, and inherits the process-local serialization limit `164.6.6-03` now pins. |

<tasks>

<task type="auto">
  <name>Task 1: Build the decision dossier — re-measure the inputs, then price the three options</name>
  <files>.planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md</files>
  <read_first>
    - .planning/phases/164.6.6-.../164.6.6-CONTEXT.md — D-02, D-03, D-05, D-06, D-10, D-11, D-12 in full
    - .planning/phases/164.6.6-.../164.6.6-RESEARCH.md — the Architecture Patterns eviction diagram, Pitfall 2, and Open Questions 1 and 3
    - analytics-service/services/mt5_client.py — `bump_mt5_terminal_epoch`, `_reset_mt5_epochs_for_tests`, `begin_mt5_lease_occupancy`, `end_mt5_lease_occupancy`, `assert_session_authorized` and its docstring
    - analytics-service/services/mt5_concurrency.py — `mt5_terminal_lease` in full including its `finally` block and the ORDER IS LOAD-BEARING comment
    - deploy/mt5-gateway/railway-gateway.md and deploy/mt5-gateway/docker-compose.yml — the image provenance and the persistent volume
  </read_first>
  <action>
Create `164.6.6-DECISION-D10.md` in the phase directory. It has four sections and is written for
a founder who will read it once and decide.

**Section 1 — Re-measured inputs.** Re-take M1 through M7 from the table above AT HEAD and
record each as CONFIRMED or CORRECTED with what was actually read. For M1 and M2 specifically,
re-read `164.6.5-CONTEXT.md` D-05 and check for a `164.6.5-02-SUMMARY.md`, using read-only `git`
against the `164.6.5` branch or the merged copy if it has since landed on `main`. ⛔ Do NOT edit,
merge or rebase `feat/164.6.5-mt5validatewedge`. If any input has CHANGED, say so loudly — the
costing below depends on it and `164.6.6-RESEARCH.md` gives itself a short validity for this
exact reason.

**Section 2 — The falsifiable test RESEARCH.md assigned to this plan (Open Question 1).** Read
the epoch and occupancy mechanism end to end and answer, in writing: *can the epoch value at the
time a client's lease released be compared at a later tick against the current epoch to produce
a true/false "somebody else has held this terminal since I left" signal, with zero new credential
and zero account-identifying data?* Record the verdict as YES or NO with the evidence.

⚠️ The expected answer, derived from M6 and stated here so the measurement is a CHECK rather than
an open-ended hunt, is **NO**, for two independent reasons: the epoch advances on every
legitimate handover including a client's own retry and every one of the three job call sites, so
the signal is true almost always and carries no eviction information; and more fundamentally the
evicted party holds NO lease at all (D-03), so there is no "my epoch" for the comparison to start
from. ⛔ Confirm or REFUTE this against source. If the measurement disagrees with the expectation,
the measurement wins and the dossier says so — a cheap pre-D-10 detection building block would be
a genuinely valuable find and must not be lost to a matching expectation.

Record the same for `Mt5Client.assert_session_authorized`: it is credential-free by design and
answers only "is ANY account authorized", never WHICH. ⛔ It must not be extended to identify an
account — that would need either a credential (defeating the whole point of a detector used to
DECIDE whether to send one) or an account-list read, which collides with D-05.

⛔ **Before Section 3, state D-01 at the top of the dossier in one sentence, because it is the
finding that disqualifies the obvious answer:** the mutex is NOT missing. The single-source
per-terminal lock registry exists, the interactive validate path already takes it, and the
eviction happened with the lease correctly held — so "add or strengthen a lock" is not among the
options and a second registry would serialize nothing (`MT5CONC-02`). A founder reading the
three options must know the fourth one they might imagine has already been measured and refused.

**Section 3 — The three options, each priced against the re-measured inputs.** One subsection
per option. Each MUST state, explicitly:
  - how it satisfies **D-02** (what restores the terminal's resting state between leases),
  - how it satisfies **D-03** (what becomes of an authorized session that holds no lease),
  - how it satisfies **D-05** (how it avoids ever enumerating the account list),
  - what it costs against M3, M4, M5 and M7,
  - and what it does to `164.6.6-01`'s scalar alarm and `164.6.6-03`'s worker-count pin.
An option that fails ANY of D-02, D-03 or D-05 is recorded as INELIGIBLE with the reason, not
softened.

⛔ **Option 3 (serialize-and-restore) is NOT the cheap option and the dossier must not present it
as one (D-11).** Restoring the prior occupant means a re-login on every lease release, and a
login IS an account change — precisely the operation 164.6.5 is investigating for wedging the
terminal, and precisely the operation `scripts/mt5-diag.sh`'s own header warns re-clears the
algo-trading flag while the account-change re-clear is armed. Price it as roughly DOUBLING the
rate of the known-dangerous operation, in its own words, in its own subsection.

⚠️ **Whether the account-change re-clear is CURRENTLY armed on the live terminal is NOT this
plan's to measure, and the dossier must say so rather than leave the reader wondering.**
`164.6.6-RESEARCH.md` logs it as Assumption A1 (carried forward, not re-verified) — and
`164.6.5-06-PLAN.md` already OWNS it: its must_haves require the setting to be pinned by a check
that fails loudly on its observable consequence, and require the repo's recorded restatements of
the re-clear behaviour to be RECONCILED with the founder's own reading rather than averaged.
Record that ownership by name in the dossier and do not re-derive it. ⛔ Do not reach for
`railway ssh` or `scripts/mt5-diag.sh` to settle it — a live MT5 operation is a founder act.
⭐ Note for the founder that A1's uncertainty is asymmetric in the SAFE direction: if the setting
has since been armed, option 3's cost is HIGHER than priced here, never lower.

**Section 4 — The recorded outcome.** Leave it as a stub with the headings the checkpoint will
fill: the option chosen, the reason it beat the other two, the date, and the named residuals
routed onward. ⛔ Do not pre-fill a choice, do not rank the options, and do not write a
recommendation sentence. The dossier presents; the founder decides.

⛔ **PUBLIC-REPO DISCLOSURE, and this file is the sharpest case in the milestone.** Write no MT5
account number, broker server name, credential, connection string, project ref, key, absolute
home path or local username anywhere in it. Synthetic placeholders only — `<account A>`,
`<broker X>` — exactly as the ROADMAP entry itself does. Timestamps, durations, position counts
and error codes are safe; identifiers are not. Truncate any UUID to 8 characters.

⛔ Run NO database command and NO live MT5 operation. Every measurement in this task is a
read-only source or `git` read. Reading Railway logs is permitted and read-only; a redeploy,
restart or `railway ssh` against `mt5-gateway` is a FOUNDER act and is not in this plan.
  </action>
  <verify>
    <automated>test -f .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md && grep -c '^## ' .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md</automated>
    <fails_when>non-zero exit (the file is absent), or a count below 4 (a section is missing — the dossier has four top-level sections and a founder reading three of them is deciding on an incomplete picture)</fails_when>
    <automated>git add .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md && npm run check:planning-hygiene</automated>
    <fails_when>non-zero exit, or the OK line is absent from the output. ⚠️ The count line and the OK line are DIFFERENT lines — never read this result with `tail -1`. The scan covers TRACKED files only, which is why the `git add` precedes it.</fails_when>
  </verify>
  <acceptance_criteria>
    - The dossier has four top-level sections: re-measured inputs, the Open Question 1 verdict, the three priced options, and a stub outcome
    - Every one of M1-M7 is recorded as CONFIRMED or CORRECTED with what was read
    - The Open Question 1 verdict is an explicit YES or NO with evidence, not a summary of the question
    - Each option subsection states its D-02, D-03 and D-05 answers separately, and its effect on the scalar alarm and the worker-count pin
    - Option 3's subsection states the doubling of the known-dangerous operation in its own words
    - Section 4 contains no chosen option, no ranking and no recommendation sentence
    - No account number, broker server name, credential, project ref, home path, username or untruncated UUID appears anywhere in the file
  </acceptance_criteria>
  <done>A founder can read one file and decide, with every input re-measured at HEAD, every option priced against the same five questions, and no option pre-selected.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: FOUNDER DECISION — the MT5 terminal ownership model (D-10, ROADMAP criterion 4)</name>
  <read_first>
    - .planning/phases/164.6.6-.../164.6.6-DECISION-D10.md — the dossier written in task 1, in full, especially its re-measured Section 1
  </read_first>
  <decision>Which ownership model does the shared MT5 terminal move to — per-validation ephemeral, pooled, or serialize-and-restore on the one terminal?</decision>
  <context>
    ⛔ **This is a ONE-WAY decision.** Options 1 and 2 make this repo the builder and operator of
    a container image it only consumes today. Unwinding that means re-pointing the deploy and
    re-establishing an upstream pin the broker's terminal self-update already makes unreliable.

    ⛔ **An agent must not answer this.** The ROADMAP names it a founder decision that must not
    be taken silently, and `164.6.6-CONTEXT.md` leaves it deliberately open for that reason.
    `gate="blocking-human"` means `workflow.auto_advance` does not apply here.

    ⛔ **Whichever is chosen MUST satisfy D-02 (what restores the terminal's resting state
    between leases), D-03 (what becomes of an authorized session that holds no lease) and D-05
    (never an inventory).** An option failing any of the three is ineligible, not merely worse.

    ⚠️ **Read the dossier's re-measured Section 1 before answering (D-12).** At planning time,
    `164.6.5`'s D-05 was still OPEN and its plan 02 — the plan that closes it — listed no
    Dockerfile among its files, so options 1 and 2 stood at FULL image-ownership cost. If D-05
    has resolved since, the relative costs have MOVED and the pros/cons below are stale by
    construction.

    ⭐ The measured incident this decides: `04:08:06 '<account A>': disconnected` →
    `04:08:07 '<account B>': authorized`. A clean, successful, one-second handover that
    destroyed a live session holding 11 open positions, with no error and no record against it.
    The lease was correctly held throughout. Nothing here is a lock bug.
  </context>
  <options>
    <option id="ephemeral">
      <name>Option 1 — per-validation ephemeral terminal</name>
      <pros>Structurally prevents BOTH the eviction (D-03) and the accumulation (D-05/D-06): a terminal that does not outlive the validation registers nothing that outlives it either. It is the only option that answers criterion 2 without a second mechanism.</pros>
      <cons>Costs the most. This repo does not build the terminal image today (third-party prebuilt, no in-repo Dockerfile anywhere under `deploy/`, no analog to copy). An ephemeral terminal has NO persistent Wine prefix — the exact property `164.6.5` D-07 rates one-way, because that prefix is what makes unattended auto-login work. It also makes the terminal PLURAL, invalidating the episode recorder's scalar counter, scalar stamp and the criterion-3 alarm shipped in `164.6.6-01` at once.</cons>
    </option>
    <option id="pooled">
      <name>Option 2 — pooled terminals</name>
      <pros>Bounds the blast radius without paying the one-per-validation cost on every validation; a pool of N caps how many clients can be disturbed by one onboarding.</pros>
      <cons>Inherits the image-ownership problem N times over. Needs a placement rule nothing in this repo has today. Inherits the process-local serialization limit that `164.6.6-03`'s worker-count pin now observes — a pool serialized only within one event loop is one `--workers` flag from serializing nothing. Also makes the terminal plural, with the same episode-recorder rework as option 1. Does not by itself prevent accumulation.</cons>
    </option>
    <option id="serialize-restore">
      <name>Option 3 — serialize-and-restore on the one terminal</name>
      <pros>Cheapest in INFRASTRUCTURE: no image ownership, no pool, no placement rule, no plural rework. The insertion point already exists — `mt5_terminal_lease`'s `finally` block — and its ordering discipline is already documented and test-pinned.</pros>
      <cons>⛔ NOT the cheap option, and must not be picked as one (D-11). Restoring the prior occupant means a re-login on EVERY lease release, and a login IS an account change — precisely the operation 164.6.5 is investigating for wedging the terminal, and precisely the operation `scripts/mt5-diag.sh`'s own header warns re-clears the algo-trading flag while the account-change re-clear is armed. It roughly DOUBLES the rate of the known-dangerous operation. It also does not prevent accumulation, so criterion 2 needs its own purge or assertion on top (D-06).</cons>
    </option>
  </options>
  <resume-signal>Name one option id — `ephemeral`, `pooled` or `serialize-restore` — AND the reason it beat each of the other two. ⛔ A bare option id is not sufficient: the ROADMAP requires the reasoning to be recorded, and task 3 writes exactly what is said here into three ledgers.</resume-signal>
  <done>The founder has named one option and the reason it beat the other two. ⛔ `workflow.auto_advance` does not apply: `gate="blocking-human"` is never auto-approved.</done>
</task>

<task type="auto">
  <name>Task 3: Record the answer in every ledger that carries it — and nowhere half of it</name>
  <files>.planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md, .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-CONTEXT.md, .planning/ROADMAP.md, TODOS.md</files>
  <read_first>
    - .planning/phases/164.6.6-.../164.6.6-CONTEXT.md — the D-10 entry and the `### Claude's Discretion` paragraph that states why it was left open
    - .planning/ROADMAP.md — § `### Phase 164.6.6`, success criterion 4 and criterion 2
    - TODOS.md — the `### MT5-WEDGE-OBS-01` section and the `[MT5-VERDICT-SINK-01]` entry, both in full
    - .planning/phases/164.6.6-.../164.6.6-DECISION-D10.md — Section 4 as filled at the checkpoint
  </read_first>
  <reversibility rating="one-way">Recording the chosen ownership model commits this repo to it; under options 1 and 2 it also commits the repo to building and operating a container image it only consumes today, which cannot be unwound without re-pointing the deploy and re-establishing an upstream pin the terminal's own self-update makes unreliable.</reversibility>
  <action>
Write the outcome down four times, in four voices, and never only once.

**FIRST, before any `Edit`:** record `wc -l < .planning/ROADMAP.md` in the SUMMARY as the
pre-edit line count. The verify below compares against it, and without a recorded baseline that
check is unfalsifiable.

**THE SHARED MARKER.** Three of the four files below must each carry the literal phrase
`164.6.6 D-10 CLOSED` on the line that opens their record of the outcome. It is the token the
verify greps for across all three at once, and it is what makes "the answer landed everywhere"
checkable rather than assertable. `TODOS.md` is deliberately NOT one of the three — its edit is
the D-09 refinement, a different subject.

**`164.6.6-DECISION-D10.md`** — fill Section 4: the option chosen, the reason it beat each of the
other two named individually, the date, and every residual routed onward by name. Open it with
the shared marker.

**`164.6.6-CONTEXT.md`** — move D-10 from OPEN to CLOSED. Keep the original OPEN text as lineage
(this repo's house style is to supersede, not to overwrite) and append a dated block opening with
the shared marker and carrying the choice and the reason. Update the `### Claude's Discretion`
paragraph so it no longer asserts D-10 is deliberately left open, and say in one line that the
closure was taken by the founder at this plan's checkpoint. ⛔ Use `Edit` with scoped
replacements; never `Write` the whole file.

**`.planning/ROADMAP.md`** — in § `### Phase 164.6.6`, record the same answer against success
criterion 4 on a line opening with the shared marker, and record against criterion 2 which answer
its accumulation remedy INHERITS (D-06: per-validation isolation prevents accumulation
structurally; serialize-and-restore does not and needs its own purge or assertion). ⛔ Use `Edit`
with scoped replacements ONLY. NEVER `Write` a whole-file replacement of `.planning/ROADMAP.md` —
a whole-file write destroys every phase entry outside the diff window, and this repo has a dated
record of exactly that.

**`TODOS.md`** — REFINE the existing `### MT5-WEDGE-OBS-01` section with the label-dedup salience
defect: the issue dedups by label, so a fresh P1 lands under a title still carrying an older
defect date and reads as stale while the Actions view reads green. Record that `164.6.6-01`
answers the salience half through a different channel (a rate-limited Sentry escalation that
does not ride the prober's exit code, per D-08), and that the dedup behaviour itself remains a
known limitation of the filing path. ⛔ Cross-link and REFINE; do NOT reopen it (D-09) — its
modal-dialog mechanism is proven and still correct for its own cause. Separately, confirm
`[MT5-VERDICT-SINK-01]` is still present, still open and still named, and add one line recording
that this phase considered and DECLINED to absorb it: it is a CAPABILITY verdict with no durable
sink, while `164.6.6-01` addresses a LIVENESS signal with no reader — different subjects, founder
call, stays deferred (D-14).

Finally, in the SUMMARY, state plainly that the criterion-1 eviction remedy and criterion 2's
accumulation remedy are NOT in this plan and are authored by re-running
`/gsd-plan-phase 164.6.6` now that the checkpoint has resolved. ⛔ That is a real dependency, not
an omission: the remedy's files, tests and risks are all different under each answer.

⛔ Never stage, commit, revert or `git checkout` `.planning/milestone.lock`,
`.planning/state.json` or `.planning/phases/164.5.3-*/164.5.3-VERIFICATION.md`. All three are
modified on purpose and belong to other phases.

⛔ Write no MT5 account number, broker server name, credential, project ref, absolute home path,
local username or untruncated UUID into any of the four files.
  </action>
  <verify>
    <automated>git add -A .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/ && npm run check:planning-hygiene</automated>
    <fails_when>non-zero exit, or the OK line is absent from the output. ⚠️ The count line and the OK line are DIFFERENT lines — never read this result with `tail -1`.</fails_when>
    <automated>grep -al -e '164.6.6 D-10 CLOSED' .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md .planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-CONTEXT.md .planning/ROADMAP.md</automated>
    <fails_when>the output does not list ALL THREE paths — the answer landed in some ledgers and not others, which is the refused-claim-survives-elsewhere defect this task exists to prevent. ⚠️ `grep -a` is mandatory: plain `grep` is silently blind to a NUL-bearing text file and would report a real hit as absent.</fails_when>
    <automated>wc -l < .planning/ROADMAP.md</automated>
    <fails_when>the count is LOWER than the pre-edit count the action requires be recorded before the first `Edit`. A scoped edit that ADDS a correction can only grow the file; a file that shrank is the signature of a whole-file write that destroyed phase entries outside the diff window, and this repo has a dated record of exactly that.</fails_when>
  </verify>
  <acceptance_criteria>
    - The pre-edit `wc -l < .planning/ROADMAP.md` value is recorded in the SUMMARY before the first `Edit`, and the post-edit count is greater than or equal to it
    - All three of the dossier, `164.6.6-CONTEXT.md` and `.planning/ROADMAP.md` carry the shared marker phrase; `grep -al` over the three lists all three paths
    - `164.6.6-DECISION-D10.md` Section 4 names the chosen option, the reason it beat each other option individually, and the date
    - `164.6.6-CONTEXT.md` D-10 carries a dated CLOSED block AND retains its original OPEN text as lineage
    - `164.6.6-CONTEXT.md` `### Claude's Discretion` no longer asserts D-10 is open
    - `.planning/ROADMAP.md` § Phase 164.6.6 records the answer against criterion 4 AND records against criterion 2 which answer it inherits, and no other phase section is touched
    - `TODOS.md` `### MT5-WEDGE-OBS-01` is refined with the label-dedup defect and is NOT reopened; `[MT5-VERDICT-SINK-01]` is still open, still named, with the declined-to-absorb line added
    - No identifier of any kind appears in any of the four files
  </acceptance_criteria>
  <done>The chosen ownership model, and the reason it beat the other two, is readable from the decision dossier, from CONTEXT.md, and from the ROADMAP; the two named TODOS entries are refined and confirmed-deferred respectively; and the SUMMARY names re-running `/gsd-plan-phase 164.6.6` as the next step.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this repo -> the public internet | The repo is PUBLIC and `.planning/` is TRACKED. Every word of the dossier is world-readable, and its subject matter IS client MT5 account numbers and broker server names. |
| a decision -> the ledgers that record it | A decision recorded in one file and not another leaves the refused claim alive; this repo has a dated milestone-audit finding of exactly that. |
| an agent -> a one-way infrastructure commitment | An agent that answered D-10 would commit the repo to building and operating a container image, irreversibly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.6-14 | Information Disclosure | `164.6.6-DECISION-D10.md` and the three edited ledgers | high | mitigate | Synthetic placeholders only, enforced by acceptance criteria on all four files plus `npm run check:planning-hygiene` run after `git add` (it scans TRACKED files only). |
| T-164.6.6-15 | Elevation of Privilege (an agent taking a founder's one-way decision) | the D-10 checkpoint | high | mitigate | `type="checkpoint:decision"` with `gate="blocking-human"`; `workflow.auto_advance` explicitly does not apply; the dossier is forbidden a recommendation sentence or a ranking. |
| T-164.6.6-16 | Repudiation (a decision recorded in one ledger only) | CONTEXT.md / ROADMAP.md / the dossier | medium | mitigate | A `git diff --stat` verify that fails when fewer than three ledgers changed, plus per-file acceptance criteria. |
| T-164.6.6-17 | Tampering (a scoped edit that becomes a whole-file rewrite) | `.planning/ROADMAP.md` | high | mitigate | `Edit`-only instruction, plus a deletion-count verify with a stated ceiling. |
| T-164.6.6-18 | Denial of Service (a live MT5 op taken by an agent) | `mt5-gateway` | high | mitigate | No task in this plan performs any live MT5 operation; every measurement is a read-only source or `git` read, and the plan says so in both the objective and task 1's action. |
| T-164.6.6-SC | Tampering | npm/pip/cargo installs | — | not applicable (visible skip) | ⭐ VISIBLE SKIP: this plan installs nothing and writes no code. |
</threat_model>

## Artifacts this phase produces (this plan's share)

**New file:** `.planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-DECISION-D10.md`
— four sections: re-measured inputs (M1-M7), the Open Question 1 verdict, the three priced
options, the recorded outcome.

**Modified ledgers:**
- `164.6.6-CONTEXT.md` — D-10 CLOSED block (original OPEN text retained as lineage); `### Claude's Discretion` corrected
- `.planning/ROADMAP.md` — § Phase 164.6.6 criteria 4 and 2
- `TODOS.md` — `### MT5-WEDGE-OBS-01` refined; `[MT5-VERDICT-SINK-01]` confirmed deferred and named

**No source file, no test, no package, no migration, no CI workflow change.**

⛔ **NOT produced by this plan, by design:** the criterion-1 eviction remedy and criterion 2's
accumulation remedy. Both are downstream of the checkpoint above and are authored by re-running
`/gsd-plan-phase 164.6.6` afterwards.

<verification>
- `npm run check:planning-hygiene` green after `git add`, with the OK line present (⚠️ never read it with `tail -1`)
- `git diff --stat` names exactly four files: the new dossier, `164.6.6-CONTEXT.md`, `.planning/ROADMAP.md` and `TODOS.md`. ⛔ `.planning/milestone.lock`, `.planning/state.json` and `.planning/phases/164.5.3-*/164.5.3-VERIFICATION.md` are modified on purpose by other phases — never stage, commit, revert or `git checkout` any of them
- No source file under `analytics-service/`, `scripts/` or `deploy/` is modified by this plan
- `feat/164.6.5-mt5validatewedge` is untouched: `git log --oneline -1 feat/164.6.5-mt5validatewedge` matches its value at the start of the plan
</verification>

<success_criteria>
- D-10 is closed by the founder, at a blocking checkpoint, with the reason it beat the other two recorded
- The costing the founder saw was re-measured at HEAD, and any input that had moved was flagged as moved
- Open Question 1 has a written YES/NO verdict with evidence, whichever way it fell
- The answer appears in the dossier, in CONTEXT.md and in the ROADMAP — never in only some of them
- `MT5-WEDGE-OBS-01` is refined and not reopened; `[MT5-VERDICT-SINK-01]` is still deferred and still named
- The next step — re-run `/gsd-plan-phase 164.6.6` to author the remedy plans — is stated in the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/164.6.6-mt5terminalisolation-one-client-s-mt5-validation-cannot-evic/164.6.6-04-SUMMARY.md` when done.
⛔ Record the chosen option, the reason, the re-measurement verdicts for M1-M7, the Open Question 1 answer, and the explicit statement that plans 05+ remain to be authored.
</output>
