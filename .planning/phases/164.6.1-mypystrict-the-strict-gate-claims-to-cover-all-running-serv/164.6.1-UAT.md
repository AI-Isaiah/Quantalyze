---
status: complete
phase: 164.6.1-mypystrict
source: [164.6.1-VERIFICATION.md]
started: 2026-09-24
updated: 2026-09-24
---

# Phase 164.6.1 — UAT (human verification items)

## Current Test

None. All tests passed.

## Tests

### 1. The `python` CI job goes RED on a `main.py` type error, then GREEN after the restore (D-06b)
expected: on the phase PR, a commit that drops `[None]` from `_crash_handler`'s `asyncio.Task[None]` produces a run bound to its head SHA. In that run the step "Type gate - mypy strict over the running-service surface" logs a `main.py:` `type-arg` error and exits 1. The restore commit then produces a green run bound to its SHA.
owner: orchestrator (ship time)
result: pass
evidence:
  - red: CI run 35939703086 at 2409925c42d5dbc78ab6c52da3908fe629057670. `python` step "Type gate - mypy strict over the running-service surface" = failure. The log reads `main.py:351: error: Missing type arguments for generic type "Task"  [type-arg]` / `Found 1 error in 1 file (checked 100 source files)` / exit code 1.
  - green: CI run 35940019949 at e53b40c4b7695aea5a0171d20dcfa0e376537a3f (the restore, byte-equal to the release commit's `main.py`). The same step logs `Success: no issues found in 100 source files`. The PR board shows 29/29 checks passing.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0

## Gaps

None. The local half (both neuters) was reproduced by the verifier.
