# Phase 164.6.1: MYPYSTRICT - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-09-23
**Phase:** 164.6.1-mypystrict
**Mode:** `--auto` — every choice below was auto-selected (founder asleep); evidence is in CONTEXT.md.
**Areas discussed:** Strict surface, Fixing the errors, Coverage statements, Neuter proof

---

## Strict surface

| Option | Description | Selected |
|--------|-------------|----------|
| `main.py` only | The ROADMAP deliverable, literally; comment then disclaims `main_worker.py` / `sentry_init.py` | |
| All four top-level service modules | `main.py` + the three modules it imports at runtime (worker merged into the API process) | ✓ |

**Choice:** [auto] all four — `main_worker.py` is already followed silently via `services/mt5_session_monitor.py`, so it is reached but unchecked; leaving it out keeps the defect class open. Plus a disk-derived vitest pin so a new top-level module cannot silently fall outside.

## Fixing the errors

| Option | Description | Selected |
|--------|-------------|----------|
| Real annotations; narrowly-scoped justified ignore only if a real type is impossible | | ✓ |
| Blanket ignores / `Any` casts | Forbidden by the brief | |

## Coverage statements

| Option | Description | Selected |
|--------|-------------|----------|
| Correct only the `ci.yml` comment | Criterion 2 minimum | |
| Correct every statement of the gate's surface (`ci.yml` comment + Makefile `typecheck`/`lint` help) | Makefile claims byte-equality with CI and would go false | ✓ |

## Neuter proof

| Option | Description | Selected |
|--------|-------------|----------|
| Local proof only | Does not observe the `python` JOB go RED | |
| `workflow_dispatch` on a throwaway branch now | Whole corpus + shared-TEST mutex holds + full-history gitleaks red | |
| Local proof now + CI observation on the phase PR at ship time | Criterion 3 stays open until the PR observation | ✓ |

## Claude's Discretion

- Annotation spelling for the `main_worker.py` closures; contract-test location; plan/wave split.

## Deferred Ideas

- `analytics-service/tests/` strict scope stays with its `TODOS.md` policy entries.
