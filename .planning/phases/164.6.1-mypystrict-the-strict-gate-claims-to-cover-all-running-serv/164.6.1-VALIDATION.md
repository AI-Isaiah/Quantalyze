---
phase: "164.6.1"
slug: "mypystrict-the-strict-gate-claims-to-cover-all-running-serv"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: "2026-09-23"
---

# Phase 164.6.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Commands are taken from `164.6.1-RESEARCH.md` `## Validation Architecture`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | mypy 2.2.0 (type gate) · vitest (contract test) · pytest (behaviour, `analytics-service/` only) |
| **Config file** | `analytics-service/pyproject.toml` `[tool.mypy]` · `vitest.config.ts` · `analytics-service/pytest.ini` |
| **Quick run command** | from `analytics-service/`: `<venv python> -m mypy --strict --follow-imports=silent services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py`; from repo root: `node_modules/.bin/vitest run src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts src/__tests__/contracts/contracts-registry.test.ts` |
| **Full suite command** | from `analytics-service/` ONLY: `<venv python> -m pytest --cov=services --cov=routers --cov=main_worker --cov-report=term-missing --cov-fail-under=80` (the `ci.yml` `python` job command) · from repo root: `node_modules/.bin/vitest run src/__tests__/contracts/` |
| **Estimated runtime** | quick ~5 s; full pytest ~3–8 min |

`<venv python>` is the analytics-service virtualenv interpreter (the CI pin set); it is never run from the repo root.

---

## Sampling Rate

- **After every task commit:** the widened mypy command + the contract test + the focused pytest subset (`tests/test_health.py tests/test_main_worker.py tests/test_sentry_init.py tests/test_verify_service_key_middleware.py`).
- **After every plan wave:** full pytest from `analytics-service/` + `vitest run src/__tests__/contracts/` + `tsx scripts/check-planning-hygiene.ts`.
- **Before verify:** all of the above green; local neuter evidence pasted verbatim; D-06b recorded as OPEN.
- **Max feedback latency:** 10 s (quick set).

---

## Per-Task Verification Map

| Requirement | Behavior | Test Type | Automated Command | File Exists | Status |
|-------------|----------|-----------|-------------------|-------------|--------|
| SC-1 / MYPY-MAINPY-01 | `main.py` strict-clean | type gate | `<venv python> -m mypy --strict main.py` → `Success` | ✅ | ⬜ pending |
| SC-1 / D-01 | widened invocation clean, 100 files | type gate | widened quick command → exit 0, `checked 100 source files` | ✅ | ⬜ pending |
| SC-2 / D-05 / D-08 | no surviving false surface claim | contract + grep | new contract test; `git grep -n "the current CI gate\|There is no mypy gate" -- analytics-service .github` → 0 hits | ❌ wave 2 | ⬜ pending |
| SC-3 local / D-06a | widened gate reds on `main.py` AND `main_worker.py`; old gate green on the same neuter | neuter | on a scratch copy: old cmd exit 0, new cmd exit 1 naming both files; `git status --porcelain analytics-service` empty afterwards | ✅ | ⬜ pending |
| SC-3 CI / D-06b | `python` job RED naming `main.py`, SHA-bound | manual at ship | neuter commit on the PR → job log has `main.py:` + non-zero exit; restore → green | ⏳ | OPEN until PR |
| SC-4 / D-02 / D-03 / D-09 | CI set == disk-derived set == Makefile set | contract | new contract test incl. calibration legs + non-vacuity control | ❌ wave 2 | ⬜ pending |
| D-04 / D-10 | no runtime change; `/health` + OpenAPI identical | unit + probe | full pytest; OpenAPI sha256 before == after | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements (created in plan 02, wave 2 — same task as its verify)

- [ ] `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` — SC-2 / SC-4 / D-03 / D-09, with calibration legs.
- [ ] `src/__tests__/contracts/contracts-registry.test.ts` row + floor bump; `src/__tests__/contracts/REGISTRY.md` prose row.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CI `python` job goes RED naming `main.py` | SC-3 (D-06b) | CI runs only on a PR to `main`, a push to `main`, or a dispatch; no PR exists at execute time | At ship: push one neuter commit (drop `[None]` on `_crash_handler`), bind the red run to its head SHA, then push the restore and bind the green run |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
