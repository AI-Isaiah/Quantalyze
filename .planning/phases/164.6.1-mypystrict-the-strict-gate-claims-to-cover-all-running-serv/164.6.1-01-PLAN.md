---
phase: 164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/main.py
  - analytics-service/main_worker.py
  - analytics-service/sentry_init.py
  - .github/workflows/ci.yml
  - analytics-service/Makefile
  - analytics-service/services/audit.py
  - analytics-service/services/ingestion/__init__.py
autonomous: true
requirements:
  - MYPY-MAINPY-01

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The ci.yml `python` job's mypy step runs `mypy --strict --follow-imports=silent services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py` and it exits 0 with `Success: no issues found in 100 source files` (D-01, D-02)"
    - "`python -m mypy --strict main.py` from analytics-service/ reports `Success: no issues found in 1 source file` where it reported 5 errors (ROADMAP SC-1, D-04)"
    - "The 15 strict errors are fixed with real annotations only: no `# type: ignore` added, no `cast(Any, ...)` added; the five Supabase-call closures in main_worker.py are `-> Any` per repo convention (D-04)"
    - "No runtime behaviour change: the full pytest suite from analytics-service/ passes and the `main.app.openapi()` sha256 prefix is equal before and after; `/health` carries `response_model=None` so the service still boots (D-04, D-10)"
    - "Every in-repo sentence that states the gate's surface is true after the change: ci.yml comment, Makefile header/help/typecheck/ci, services/audit.py comment, services/ingestion/__init__.py docstring (ROADMAP SC-2, D-05, D-07, D-08)"
    - "Makefile `typecheck` recipe names exactly the CI path set, and `make ci` runs `typecheck` rather than `lint` (D-05, D-07)"
    - "On a scratch copy of the committed tree, a neutered `_crash_handler` Task type-arg plus an un-annotated `_mark_done` leave the OLD invocation green (96 files) and turn the NEW invocation RED naming both `main.py:` and `main_worker.py:` (D-06a); the tracked working tree is never edited for the proof"
    - "The CI-side RED observation (D-06b) is recorded as OPEN in the SUMMARY with its ship-time procedure; it is NOT marked closed"
    - "D-06: both halves are honoured — (a) is proven locally on a scratch copy in this plan, (b) is carried OPEN to ship time; ROADMAP SC-3 is not marked closed by the local proof"
  artifacts:
    - path: "analytics-service/main.py"
      provides: "annotated lifespan, _crash_handler, verify_service_key, health (+ response_model=None)"
      contains: "RequestResponseEndpoint"
    - path: "analytics-service/main_worker.py"
      provides: "nine annotated helper closures"
      contains: "ErrorKind"
    - path: "analytics-service/sentry_init.py"
      provides: "_redact_before_send typed with sentry_sdk.types Event/Hint"
      contains: "from sentry_sdk.types import Event, Hint"
    - path: ".github/workflows/ci.yml"
      provides: "named, widened mypy step + corrected comment"
      contains: "main_worker_healthz.py sentry_init.py"
    - path: "analytics-service/Makefile"
      provides: "typecheck recipe mirroring CI; ci: typecheck test"
      contains: "ci: typecheck test"
  key_links:
    - from: ".github/workflows/ci.yml python job mypy step"
      to: "analytics-service/{main,main_worker,main_worker_healthz,sentry_init}.py"
      via: "named on the mypy command line (checked, not followed-silently)"
      pattern: "run: mypy --strict --follow-imports=silent services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py"
    - from: "analytics-service/Makefile typecheck"
      to: ".github/workflows/ci.yml mypy step"
      via: "identical path list (flags from pyproject.toml [tool.mypy])"
      pattern: "\\$\\(MYPY\\) services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py"
---

<objective>
Put the service's real entry surface under the `mypy --strict` gate and make every sentence that states the gate's coverage true (TODOS `[MYPY-MAINPY-01]`, ROADMAP SC-1, SC-2, SC-4, and the local half of SC-3).

This plan implements D-01 (widen to the four top-level modules), D-02 (before/after file set 96 → 100 stated verbatim), D-04 (real annotations only), D-05 / D-07 / D-08 (correct every surface claim, `make ci` runs `typecheck`), D-06a (local neuter proof on a scratch copy), and D-10 (`response_model=None` on `/health`). D-06b is carried as an OPEN ship-time step, not closed.

Purpose: the gate's comment claims coverage of ALL running-service code while `main.py` (the `uvicorn main:app` module) is outside it and `main_worker.py` is followed with its errors suppressed. A gate that asserts coverage it does not have is this milestone's named defect class.
Output: annotated `main.py`, `main_worker.py`, `sentry_init.py`; a named, widened ci.yml mypy step with a corrected comment; a Makefile that mirrors CI; two corrected in-code comments; SUMMARY evidence (mypy lines, OpenAPI hashes, neuter output).

Spec-less probe fallback: skipped. This phase has no v1.20 requirement IDs to probe (only the TODOS id MYPY-MAINPY-01), so no probe predicates are generated this run (visible skip, recorded here).
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-CONTEXT.md
@.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-RESEARCH.md
@.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-PATTERNS.md
@CLAUDE.md

## Environment facts (read before any command)

- `PY` below means the analytics-service virtualenv interpreter of the MAIN checkout: `"$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python"`. It carries the CI pin set (mypy 2.2.0). Your worktree has no `.venv`; do not create one, do not pip install.
- Run every mypy / pytest / `import main` command with cwd = YOUR worktree's `analytics-service/` directory (`cd "$(git rev-parse --show-toplevel)/analytics-service"`). ⛔ NEVER run pytest from the repo root (cassettes miss → live broker calls). ⛔ NEVER start `uvicorn main:app` (it claims real PROD compute jobs). `import main` without uvicorn is safe (no lifespan runs).
- ⛔ `git checkout --`, `git restore`, `git stash` are FORBIDDEN in this repo. Never use them to undo an edit.
- Git steps run ONE AT A TIME (no compound shell command naming a `.git` path). Stage by explicit path only. Never stage `.planning/milestone.lock`, `.planning/state.json`, anything under `.planning/phases/164.5.3-*`, or `node_modules`.
- Commit messages must not contain any CI-skip trailer token, not even in a sentence denying it.
- Repo is PUBLIC and `.planning/` is tracked: nothing you write under `.planning/` may contain a home path or the local username. Redact to `<home>` / `<user>` before pasting output.
- Cite by SYMBOL in all new prose and comments; never `ci.yml:NNNN` or `main.py:NNN` line numbers.
- Do NOT touch `VERSION`, `package.json`, `CHANGELOG.md`.
- Why verify commands start with `cd "$(git rev-parse --show-toplevel)..."` rather than an absolute path: each executor runs in its own harness worktree, so any literal worktree path would point at the wrong checkout; `git rev-parse --show-toplevel` resolves to the executor's own worktree root every time.
- `scripts/check-planning-hygiene.ts` enumerates files with `git ls-files -z`, so it CANNOT see an untracked file. A SUMMARY must be staged (`git add -- <summary path>`, as its own command) BEFORE the check runs, and is additionally grepped directly for the home-directory basename (derived at runtime with `basename "$HOME"`, never typed literally), the macOS home-directory prefix (built at runtime with `printf '\057Users\057'` and never typed literally, because `scripts/check-planning-hygiene.ts` flags that literal as ABSOLUTE-HOME-PATH in any tracked file, this PLAN.md included), `/home/` and `/private/tmp/`.

## Symbol claims (re-resolved by `scripts/verify-plan-anchors.mjs`)

<!-- verify-symbol: analytics-service/main.py _crash_handler -->
<!-- verify-symbol: analytics-service/main.py verify_service_key -->
<!-- verify-symbol: analytics-service/main.py async def lifespan -->
<!-- verify-symbol: analytics-service/main.py @app.get("/health" -->
<!-- verify-symbol: analytics-service/main_worker.py def _mark_done -->
<!-- verify-symbol: analytics-service/main_worker.py async def _safe_mark -->
<!-- verify-symbol: analytics-service/sentry_init.py def _redact_before_send -->
<!-- verify-symbol: analytics-service/services/mt5_session_monitor.py from main_worker import SHUTDOWN -->
<!-- verify-symbol: analytics-service/services/job_worker.py ErrorKind -->
<!-- verify-symbol: analytics-service/routers/process_key.py response_model=None -->
<!-- verify-symbol: .github/workflows/ci.yml mypy --strict --follow-imports=silent services/ routers/ models/ -->

## Interfaces the executor needs (extracted; verified by RESEARCH.md against installed pins)

- starlette 0.46.2 `starlette.middleware.base.RequestResponseEndpoint` = `Callable[[Request], Awaitable[Response]]` (already used by `services/logging_config.py` middleware `dispatch`).
- `services/mt5_concurrency.py::mt5_terminal_lease` is the in-repo `@asynccontextmanager ... -> AsyncIterator[None]` precedent.
- `routers/process_key.py::process_key` is the in-repo `response_model=None` + `-> dict[str, Any] | JSONResponse` precedent.
- `services/job_worker.py`: `ErrorKind = Literal["transient", "permanent", "unknown"]`; `DispatchResult.error_message: str | None`, `DispatchResult.error_kind: ErrorKind | None`.
- `main_worker.py`: `ClaimedJob.id: str`; `claim_token: str | None`; `services/db.py::db_execute(fn: Callable[[], _T]) -> _T`.
- sentry-sdk 2.64.0: `sentry_sdk.types` exports `Event`, `Hint` (bound to `Any` at runtime, real types under TYPE_CHECKING); `before_send` expects `EventProcessor = Callable[[Event, Hint], Optional[Event]]`.
- FastAPI 0.139: a Union return annotation on a route WITHOUT `response_model=None` raises `FastAPIError: Invalid args for response field!` at `import main` (boot failure).
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): main.py enters the gate end-to-end — baseline, annotate, name and widen the ci.yml step and the Makefile recipe to include main.py</name>
  <files>analytics-service/main.py, .github/workflows/ci.yml, analytics-service/Makefile</files>
  <read_first>
    - analytics-service/main.py (imports block; `lifespan`; `_crash_handler` and the `add_done_callback` loop inside `lifespan`; `verify_service_key`; `health` and its `@app.get("/health")` decorator)
    - .github/workflows/ci.yml (the `  python:` job block; the step whose run line starts `mypy --strict --follow-imports=silent` and the comment block above it)
    - analytics-service/Makefile (`typecheck:` target)
    - analytics-service/services/logging_config.py (middleware `dispatch` signature — analog)
    - analytics-service/services/mt5_concurrency.py (`mt5_terminal_lease` — analog)
    - analytics-service/routers/process_key.py (`process_key` decorator and return annotation — analog)
    - .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-RESEARCH.md (sections "Code Examples → main.py", "Pitfall 1", "Expected mypy lines for the SUMMARY")
  </read_first>
  <action>
    STEP -1 — run `node scripts/verify-plan-anchors.mjs` on this PLAN.md from the worktree root and HALT on a non-zero exit (repo convention: a stale symbol claim means the plan no longer describes the tree).

    STEP 0 — BASELINE, before editing anything (D-02, D-10). From your worktree's `analytics-service/` with `PY`, record verbatim into your notes for the SUMMARY:
    (i) the OLD invocation `PY -m mypy --strict --follow-imports=silent services/ routers/ models/` — expected last line `Success: no issues found in 96 source files`;
    (ii) the D-01 widened invocation `PY -m mypy --strict --follow-imports=silent services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py` — expected `Found 15 errors in 3 files (checked 100 source files)`; keep the full error list;
    (iii) `PY -m mypy --strict main.py` — expected 5 errors;
    (iv) the OpenAPI hash probe: `PY -c` running `import json,hashlib,main` then printing the first 16 hex chars of `hashlib.sha256(json.dumps(main.app.openapi(),sort_keys=True).encode()).hexdigest()` — RESEARCH measured `8103da730102c4fc`; record whatever you measure as BEFORE.
    If any of (i)–(iii) differs from the expected line, STOP and report (the research premise has moved; do not improvise).

    STEP 1 — annotate `main.py` (D-04, D-10). Real annotations only; no `# type: ignore`, no casts; function bodies untouched:
    - add `from collections.abc import AsyncIterator` in the stdlib import group (keep the file's existing ordering style) and `from starlette.middleware.base import RequestResponseEndpoint` beside the existing `from starlette.responses import Response`;
    - `lifespan(_app: FastAPI)` gains `-> AsyncIterator[None]`;
    - `_crash_handler(task: asyncio.Task)` becomes `task: asyncio.Task[None]` (every coroutine in the `tasks` list returns None — RESEARCH reveal_type `list[Task[None]]`);
    - `verify_service_key(request: Request, call_next)` becomes `call_next: RequestResponseEndpoint` with `-> Response`, wrapped over lines the way `services/logging_config.py` wraps `dispatch`;
    - per D-10: the `/health` decorator becomes `@app.get("/health", response_model=None)` and `health()` gains `-> JSONResponse | dict[str, Any]` (mirrors `routers/process_key.py::process_key`). ⛔ The annotation without `response_model=None` crashes `import main` — both edits land together.

    STEP 2 — the tracer's gate wiring (D-01, partial; widened to the full set in Task 2). In `.github/workflows/ci.yml`, `python` job: turn the bare mypy `- run:` step into a named step. Use EXACTLY this step name (Plan 02's contract test anchors on it): `Type gate - mypy strict over the running-service surface`. Today the step is the single line `      - run: mypy --strict --follow-imports=silent services/ routers/ models/` (6 spaces before `-`). Replace that ONE line with exactly these TWO lines — line 1: 6 spaces, then `- name: Type gate - mypy strict over the running-service surface`; line 2: 8 spaces, then `run: mypy --strict --follow-imports=silent services/ routers/ models/ main.py` (the `run:` key aligns under `name:`; the value stays on one line; `working-directory` stays job-level; no other key is added). Leave the comment block above it for Task 3. In `analytics-service/Makefile`, the `typecheck:` recipe becomes `$(MYPY) services/ routers/ models/ main.py` (TAB-indented recipe line; keep it one line).

    STEP 3 — verify end-to-end by executing the run line ci.yml actually carries (see verify), then the `main.py`-only strict run, the focused pytest subset, and the OpenAPI probe AFTER (must equal BEFORE).

    Commit (paths explicitly): `analytics-service/main.py`, `.github/workflows/ci.yml`, `analytics-service/Makefile`. Message e.g. `feat(164.6.1-01): main.py enters the mypy --strict gate with real annotations`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && ARGS=$(sed -n 's/^ *run: mypy //p' ../.github/workflows/ci.yml) && echo "ci.yml mypy args: $ARGS" && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -m mypy $ARGS</automated>
    <fails_when>non-zero exit; or the printed args line is empty or does not end in `main.py`; or the last line is not `Success: no issues found in 97 source files`</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -m mypy --strict main.py</automated>
    <fails_when>non-zero exit, or output other than `Success: no issues found in 1 source file`</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -m pytest -q tests/test_health.py tests/test_verify_service_key_middleware.py tests/test_service_key_log_companion.py tests/test_local_worker_prod_guard.py tests/test_correlation_middleware.py</automated>
    <fails_when>non-zero exit, or any `failed` / `error` count in the summary line, or a collection error importing `main`</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -c "import json,hashlib,main;print(hashlib.sha256(json.dumps(main.app.openapi(),sort_keys=True).encode()).hexdigest()[:16])"</automated>
    <fails_when>non-zero exit (e.g. `FastAPIError: Invalid args for response field!`), or the printed 16-hex prefix differs from the STEP 0 BEFORE value</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'response_model=None' analytics-service/main.py` prints a number at least 1 greater than before the edit, and `grep -n '@app.get("/health", response_model=None)' analytics-service/main.py` prints exactly 1 line
    - `grep -c 'asyncio.Task\[None\]' analytics-service/main.py` prints at least 1; `grep -c 'call_next: RequestResponseEndpoint' analytics-service/main.py` prints 1; `grep -c -- '-> AsyncIterator\[None\]' analytics-service/main.py` prints at least 1
    - with `<sha>` = this task's own commit (record it with `git rev-parse HEAD` immediately after committing): `git diff --name-only -G 'type: ignore' <sha>^..<sha> -- analytics-service/main.py` prints nothing, and `git diff --name-only -G 'cast\(' <sha>^..<sha> -- analytics-service/main.py` prints nothing
    - `grep -c '^      - name: Type gate - mypy strict over the running-service surface$' .github/workflows/ci.yml` prints 1
    - `grep -cE '^ *run: mypy ' .github/workflows/ci.yml` prints 1, and `grep -cE '^        run: mypy --strict --follow-imports=silent services/ routers/ models/ main\.py$' .github/workflows/ci.yml` prints 1 (8-space `run:` key directly under the 6-space `- name:` line)
    - OpenAPI prefix AFTER equals BEFORE (both recorded for the SUMMARY)
  </acceptance_criteria>
  <done>`main.py` is strict-clean, named on the CI command line and in the Makefile recipe, the service still imports and its OpenAPI document is unchanged, and the baseline mypy lines (96 / 15-in-3 / 100) are recorded.</done>
  <reversibility rating="reversible">Annotation-only edits and a one-step CI change; a follow-up commit reverts them.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: main_worker.py and sentry_init.py enter the gate — annotate, widen ci.yml and the Makefile recipe to the full D-01 set</name>
  <files>analytics-service/main_worker.py, analytics-service/sentry_init.py, .github/workflows/ci.yml, analytics-service/Makefile</files>
  <read_first>
    - analytics-service/main_worker.py (import block incl. the `from services.job_worker import ...` line; `_safe_mark`; closures `_claim_priority`, `_claim_legacy`, `_mark_done`, `_mark_failed`, `_mark_failed_fallback`, `_reset`, `_enqueue`, `_query`; class `ClaimedJob`)
    - analytics-service/sentry_init.py (imports; `_redact_before_send`, the breadcrumbs branch; `init_sentry`; every other use of `Any` in the file)
    - analytics-service/services/job_worker.py (`ErrorKind`, `DispatchResult`)
    - analytics-service/services/equity_reconstruction.py (`_upsert`, `_sel` closures typed `-> Any` — analog)
    - .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-RESEARCH.md ("Code Examples → main_worker.py", "Code Examples → sentry_init.py", Pitfalls 2 and 3)
  </read_first>
  <action>
    Per D-04, real annotations only, bodies untouched except the one sentry binding below.

    `main_worker.py` (9 errors; file has `from __future__ import annotations`):
    - add `from collections.abc import Callable` in the stdlib group; extend the existing `from services.job_worker import DispatchOutcome, JobStatus, Priority, dispatch` line with `ErrorKind` in alphabetical position (a plain import — no TYPE_CHECKING block);
    - `_safe_mark`'s `invoke_rpc` parameter becomes `invoke_rpc: Callable[[], object]` (the result is discarded, so `object` is the honest widest return);
    - `_claim_priority`, `_claim_legacy`, `_reset`, `_enqueue`, `_query` gain `-> Any`. JUSTIFICATION to copy into the SUMMARY: this matches the repo convention for Supabase-call closures (RESEARCH counted 21 `-> Any` vs 7 concrete in services/+routers/, e.g. `services/equity_reconstruction.py` `_upsert`/`_sel`), it equals the return type mypy already infers for an unannotated def, it is an explicit return type rather than a cast, and the concrete `SingleAPIResponse`/`APIResponse` types were MEASURED to cascade into 7 new errors (the `jobs: list[ClaimedJob]` assignment and the `rows[0].get("created_at")` freshness check) that could only be cleared by editing runtime code, which D-04 forbids;
    - default-arg closures: `_mark_done(jid: str = job["id"], tok: str | None = claim_token) -> None`; `_mark_failed(jid: str = job["id"], err: str | None = result.error_message, kind: ErrorKind | None = result.error_kind, tok: str | None = claim_token) -> None`; `_mark_failed_fallback(jid: str = job["id"], err: str = str(exc)[:500], tok: str | None = claim_token) -> None`. Keep default expressions byte-identical.

    `sentry_init.py` (1 error):
    - add `from sentry_sdk.types import Event, Hint` beside the other `sentry_sdk` imports (direct import; the SDK binds both to `Any` at runtime);
    - signature becomes `_redact_before_send(event: Event, hint: Hint | None) -> Event` (keep `Hint | None` — tests call it with `None`; return `Event`, never None);
    - in the breadcrumbs branch, bind `breadcrumbs = event.get("breadcrumbs")` once, test `isinstance(breadcrumbs, dict)`, and read `breadcrumbs.get("values")` — same object, same mutations, same control flow (clears the `union-attr` on the SDK's `Annotated` union). Leave the `typing` import alone unless `Any` becomes genuinely unused in the file (then remove only `Any`).
    - No `# type: ignore` anywhere. If mypy still reports an error that only an ignore would clear, STOP and report — D-04 requires a written reason why a real type is impossible, and research measured that none is needed.

    Widen (D-01, final): ci.yml step `Type gate - mypy strict over the running-service surface` run line becomes `mypy --strict --follow-imports=silent services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py`; the Makefile `typecheck:` recipe becomes `$(MYPY) services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py` — same tokens, same order, one line each.

    Commit the four paths explicitly, e.g. `feat(164.6.1-01): main_worker.py and sentry_init.py enter the strict gate; gate names the full service surface`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && ARGS=$(sed -n 's/^ *run: mypy //p' ../.github/workflows/ci.yml) && echo "ci.yml mypy args: $ARGS" && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -m mypy $ARGS</automated>
    <fails_when>non-zero exit; or the printed args line does not end in `main.py main_worker.py main_worker_healthz.py sentry_init.py`; or the last line is not `Success: no issues found in 100 source files`</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -m pytest -q tests/test_main_worker.py tests/test_sentry_init.py tests/test_health.py tests/test_verify_service_key_middleware.py tests/test_service_key_log_companion.py tests/test_local_worker_prod_guard.py tests/test_correlation_middleware.py tests/test_redact.py</automated>
    <fails_when>non-zero exit, or any `failed` / `error` count in the summary line</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && grep -E '^\s+\$\(MYPY\) ' Makefile</automated>
    <fails_when>the typecheck recipe line printed is not exactly `$(MYPY) services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py` after the tab (the `lint` recipe line with `--strict --follow-imports=silent services/ingestion/` is the other expected line)</fails_when>
  </verify>
  <acceptance_criteria>
    - with `<sha>` = this task's own commit (from `git rev-parse HEAD` right after committing): `git diff --name-only -G 'type: ignore' <sha>^..<sha> -- analytics-service/main_worker.py analytics-service/sentry_init.py` prints nothing, and the same command with `-G 'cast\('` prints nothing
    - `grep -c -- '-> Any:' analytics-service/main_worker.py` is at least 5 greater than before the edit (the five Supabase closures)
    - `grep -c 'from sentry_sdk.types import Event, Hint' analytics-service/sentry_init.py` prints 1
    - `grep -c 'services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py' .github/workflows/ci.yml` prints 1 and the same grep on `analytics-service/Makefile` prints 1
    - the full-set mypy run prints `Success: no issues found in 100 source files`
  </acceptance_criteria>
  <done>The D-01 invocation is green at 100 files in ci.yml and the Makefile, with zero ignores and zero casts, and the focused behaviour tests pass.</done>
  <reversibility rating="reversible">Type annotations plus one behaviour-identical local binding; revertible by a follow-up commit.</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 3: correct every surface claim, then prove the widened gate can go RED on a scratch copy (D-06a); full-suite and OpenAPI re-check</name>
  <files>.github/workflows/ci.yml, analytics-service/Makefile, analytics-service/services/audit.py, analytics-service/services/ingestion/__init__.py</files>
  <read_first>
    - .github/workflows/ci.yml (the comment block immediately above the step `Type gate - mypy strict over the running-service surface`)
    - analytics-service/Makefile (header comment; `help` target echo lines for `lint`, `typecheck`, `ci`; comment above `typecheck:`; `ci:` target)
    - analytics-service/services/audit.py (comment immediately above `AuditAction = Literal[`)
    - analytics-service/services/ingestion/__init__.py (the docstring NOTE about `@runtime_checkable`)
    - analytics-service/services/mt5_session_monitor.py (the `from main_worker import SHUTDOWN` line — why main_worker.py was followed-silently)
    - .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-RESEARCH.md ("Surface-Claim Inventory" rows 1–10, "Pitfall 4", "Pitfall 5")
  </read_first>
  <action>
    PART A — surface-claim corrections (D-05, D-07, D-08). Surgical: change only the sentences named here; no reflow of neighbours; cite by symbol, no line numbers.
    - ci.yml comment above the step (SC-2): keep the `@runtime_checkable` paragraph but replace its final sentence (the one saying the follow-imports flag keeps strictness local to the gated packages) with the plain consequence: a module reached only by import is FOLLOWED with its errors SUPPRESSED, so a green run cannot tell "checked" from "reached"; that is why the entry modules are named on the command line — `main_worker.py` was in exactly that state, reached via `services/mt5_session_monitor.py`'s import of `SHUTDOWN`. In the B-mypy paragraph, keep the history (ingestion → services part g → routers part h → models part i, the CI-pinned supabase/postgrest versions) but remove the unqualified all-of-the-running-service coverage claim (it was false: the entry module was never on the command line) and add a Phase 164.6.1 clause that names the four top-level modules (`main.py` = the `uvicorn main:app` entry, `main_worker.py` merged into the API process, `main_worker_healthz.py`, `sentry_init.py`), and states the before/after set (96 → 100 source files as mypy counts them). Do NOT name the surface-pin contract test in this comment: that file does not exist yet, and a comment must never name a pin that does not exist. Plan 02 adds the one-sentence citation in the same commit that creates the test. Keep the `scripts/` and `tests/` reasons verbatim.
    - Makefile header comment: the paragraph claiming the strict pass applies only to the ingestion package is false; rewrite it to say the `lint` target is a narrow historical subset (the Phase 19 MC-3 scope) and the CI gate is the `typecheck` target's path set, which mirrors the ci.yml `python` job's mypy step.
    - Makefile `help`: the `lint` line keeps `mypy --strict on services/ingestion/` but its parenthetical becomes "MC-3 scope; a subset of the CI gate"; the `typecheck` line names the full set (`services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py`) and says it is the CI gate's path set; the `ci` line becomes `full local CI gauntlet: typecheck + test`.
    - Makefile comment above `typecheck:`: replace the claim of being the byte-identical CI command with "the same paths as the ci.yml mypy step; the flags come from pyproject.toml [tool.mypy], which ci.yml also repeats on its command line". Do not name a contract test here either (Plan 02 adds that citation together with the test).
    - Makefile `ci:` target (D-07): `ci: typecheck test`. Keep the `lint` target itself.
    - `services/audit.py` comment above `AuditAction` (D-08): correct ONLY the first clause — this file IS under the CI mypy step (the `python` job's type gate); keep the point that annotations are static and have no runtime teeth, so the sync test is what makes the contract enforceable.
    - `services/ingestion/__init__.py` docstring NOTE (D-08): point at the ci.yml `python` job's step `Type gate - mypy strict over the running-service surface` (which covers this package as part of `services/`), not at the Makefile `lint` target. Keep the MC-3 / 19-REVIEWS.md provenance and the signature-drift rationale.
    - Do NOT edit `analytics-service/pyproject.toml` (D-05 default; its header is historical) or anything under `.planning/` history.
    Commit the four paths explicitly, e.g. `docs(164.6.1-01): every statement of the mypy gate's surface now names the real surface`.

    PART B — local neuter proof (D-06a), on a SCRATCH COPY ONLY. Precondition: `git status --porcelain analytics-service .github` prints nothing (Part A committed).
    1. Create a directory unique to this run inside YOUR session scratchpad (from your environment — parallel agents share it, so suffix the name with a timestamp) and `rsync -a` your worktree's `analytics-service/` into it, excluding `.venv`, `.mypy_cache`, `__pycache__`, `tests`, `scripts`.
    2. In the COPY only: in `main.py` change `task: asyncio.Task[None]` on `_crash_handler` to `task: asyncio.Task`; in `main_worker.py` strip the annotations from `_mark_done` (back to `_mark_done(jid=job["id"], tok=claim_token):`). Use a scripted replace (python or perl) and confirm each replacement matched exactly once.
    3. From the COPY's directory with `PY`: run the OLD invocation (`services/ routers/ models/`) — expect exit 0 and `Success: no issues found in 96 source files` (the neuter is invisible to the old gate); run the NEW D-01 invocation — expect exit 1, output containing a `main.py:` line (`type-arg`, Task) and a `main_worker.py:` line (`no-untyped-def`), and `Found 2 errors in 2 files (checked 100 source files)`. Record both exit codes (`echo "exit=$?"`).
    4. Restore by re-running the same `rsync` from the worktree over the copy, `cmp` the copy's `main.py` and `main_worker.py` against the worktree's (must be identical), re-run the NEW invocation in the copy — expect exit 0 and `Success: no issues found in 100 source files`.
    5. `git status --porcelain analytics-service` in your worktree prints nothing (the tracked tree was never touched). Delete the scratch directory.
    Paste steps 3–5 output VERBATIM into the SUMMARY under "D-06a local neuter proof", redacting any home path to `<home>` and the username to `<user>`.

    PART C — no-behaviour-change re-check (D-04, D-10): run the full pytest (exact ci.yml `python` job command, see verify) and the OpenAPI probe; AFTER must equal Task 1's BEFORE.

    SUMMARY requirements (write `164.6.1-01-SUMMARY.md`): (1) the four mypy lines verbatim — OLD 96, widened-before-fix `Found 15 errors in 3 files (checked 100 source files)`, widened-after `Success: no issues found in 100 source files`, `main.py` alone 1-file success (D-02, SC-4: before set = services/ routers/ models/, after set = that plus exactly the four modules, nothing left); (2) the `-> Any` justification from Task 2 verbatim; (3) a line `# type: ignore added: none` (or, if any was unavoidable, each by symbol with its reason); (4) OpenAPI hash BEFORE and AFTER; (5) the D-06a output; (6) a section "D-06b — OPEN (ship-time)" stating that ROADMAP SC-3 is NOT closed by the local proof and giving the procedure: on the phase PR push ONE commit that drops `[None]` from `_crash_handler`'s `asyncio.Task[None]` (message worded plainly, no CI-skip token), bind the run to the head SHA with `gh api "repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<sha>"`, read the `python` job's step `Type gate - mypy strict over the running-service surface` log for a `main.py:` error line and exit code 1, then push the restore commit and bind a green run to its SHA. Redact every scratchpad path in pasted output (the scratchpad path embeds the username) to `<scratch>`. After writing the SUMMARY: stage it with `git add -- .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-01-SUMMARY.md` as its own command (the hygiene check reads `git ls-files -z` and cannot see an untracked file), THEN run the hygiene check and the direct grep in verify — both BEFORE the SUMMARY is committed.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -m pytest --cov=services --cov=routers --cov=main_worker --cov-report=term-missing --cov-fail-under=80</automated>
    <fails_when>non-zero exit; any `failed` or `error` in the final summary line; or `Required test coverage of 80% not reached`</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && for f in .github/workflows/ci.yml analytics-service/Makefile analytics-service/services/audit.py analytics-service/services/ingestion/__init__.py; do test -f "$f" || { echo "MISSING target file: $f"; exit 2; }; done && { git grep -nE 'the current CI gate|There is no mypy gate|byte-for-byte the|applies ONLY|covers ALL running-service code|ci: lint test' -- .github/workflows/ci.yml analytics-service/Makefile analytics-service/services/audit.py analytics-service/services/ingestion/__init__.py; rc=$?; test "$rc" -eq 1; }</automated>
    <fails_when>non-zero exit: exit 2 with `MISSING target file` (a pathspec that does not exist would otherwise look like a clean result); any printed line (a surviving false surface claim, git grep rc 0); or any git grep error (rc other than 0/1). Exit 0 happens ONLY when all four files exist and git grep reports no match (rc 1).</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && git ls-files --error-unmatch .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-01-SUMMARY.md && node_modules/.bin/tsx scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit: `error: pathspec ... did not match` means the SUMMARY was not staged, so the hygiene check would not have scanned it; or the hygiene check reports any `LOCAL-USERNAME:` / absolute-home-path violation (if `node_modules/.bin/tsx` is absent in your worktree, run the same script with `npx --no-install tsx`)</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && S=.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-01-SUMMARY.md && test -f "$S" && U=$(basename "$HOME") && test -n "$U" && H=$(printf '\057Users\057') && test "${#H}" -eq 7 && { grep -nF -e "$U" -e "$H" -e /home/ -e /private/tmp/ "$S"; rc=$?; test "$rc" -eq 1; }</automated>
    <fails_when>non-zero exit: the SUMMARY is missing; or any printed line (the home-directory basename or an absolute home/scratch path survived redaction, grep rc 0); or a grep error (rc 2). Exit 0 only on no match (rc 1).</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'main_worker_healthz.py' .github/workflows/ci.yml` prints at least 2 (comment + run line) and the comment block above the step contains the string `164.6.1`
    - `grep -cE 'ci\.yml:[0-9]' .github/workflows/ci.yml analytics-service/Makefile` reports 0 for both files (measured 0 before this plan; no line-number citations introduced)
    - `grep -n '^ci: typecheck test$' analytics-service/Makefile` prints 1 line
    - the SUMMARY contains `Found 2 errors in 2 files (checked 100 source files)`, `Success: no issues found in 96 source files`, both a `main.py:` and a `main_worker.py:` error line, and a heading containing `D-06b` with the word `OPEN`
    - `git status --porcelain analytics-service` printed nothing after the neuter proof
    - the SUMMARY was staged before the hygiene check ran, the hygiene check exits 0, and the direct basename / absolute-path grep of the SUMMARY finds nothing
    - `grep -c 'ci-mypy-strict-surface' .github/workflows/ci.yml analytics-service/Makefile` reports 0 for both files at the end of this plan (the citation of the pin lands in Plan 02, with the pin)
  </acceptance_criteria>
  <done>No in-repo sentence outside `.planning/` history misstates the gate's surface; the widened gate is observed RED on both newly named modules while the old gate stays green on the same neuter; the full suite and OpenAPI hash confirm no runtime change; D-06b is recorded OPEN with its procedure.</done>
  <reversibility rating="reversible">Comment and dev-tooling edits; the neuter proof never touches tracked files.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway boot → FastAPI route registration | `import main` builds every route; an annotation FastAPI cannot model aborts boot |
| HTTP client → `verify_service_key` middleware | service-key auth gate; only its annotations change |
| service → Sentry | `_redact_before_send` scrubs credentials before events leave the process |
| agent output → tracked public `.planning/` | SUMMARY evidence could carry home paths / username |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.1-01 | D (Denial of service) | `main.py::health` route registration | high | mitigate | `response_model=None` lands in the same edit as the return annotation (D-10); Task 1 and Task 3 run `import main` + the OpenAPI sha256 probe and require AFTER == BEFORE; full pytest imports `main` |
| T-164.6.1-02 | E/T (Elevation / Tampering) | `main.py::verify_service_key` | medium | mitigate | annotation-only edit, body untouched; `test_verify_service_key_middleware.py` and `test_service_key_log_companion.py` run in Tasks 1–2 and the full suite in Task 3 |
| T-164.6.1-03 | I (Information disclosure) | `sentry_init.py::_redact_before_send` | medium | mitigate | the only body change is binding the same `breadcrumbs` object once; `test_sentry_init.py` and `test_redact.py` run in Task 2 and in the full suite |
| T-164.6.1-04 | T (Tampering, gate integrity) | ci.yml mypy step | medium | mitigate | D-06a scratch-copy neuter proves the widened step reds on `main.py` and `main_worker.py`; D-06b recorded OPEN for the CI-side observation; Plan 02's contract test pins the path set |
| T-164.6.1-05 | I (Information disclosure) | `164.6.1-01-SUMMARY.md` (public repo) | low | mitigate | redact home path/username before pasting; `check-planning-hygiene.ts` in Task 3 verify |
</threat_model>

<verification>
- The D-01 invocation, read out of ci.yml and executed from `analytics-service/`, prints `Success: no issues found in 100 source files`.
- `mypy --strict main.py` prints `Success: no issues found in 1 source file`.
- Full pytest (ci.yml `python` job command) green from `analytics-service/`; OpenAPI prefix unchanged.
- `git grep` for the retired false phrases over the four corrected files returns nothing (other files legitimately use some of those words, so the grep is file-scoped).
- D-06a output pasted; D-06b recorded OPEN.
</verification>

<success_criteria>
- ROADMAP SC-1 met (main.py 0 errors, covered in ci.yml).
- ROADMAP SC-2 met (comment names the real surface; Makefile, audit.py, ingestion docstring corrected).
- ROADMAP SC-3 LOCAL half met; CI half explicitly OPEN (D-06b).
- ROADMAP SC-4 met in prose (96 → 100, set stated); mechanised by Plan 02.
</success_criteria>

## Artifacts this phase produces

| Symbol / artifact | Kind | Where |
|---|---|---|
| step `Type gate - mypy strict over the running-service surface` | CI step name (new anchor) | `.github/workflows/ci.yml`, `python` job |
| D-01 path list `services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py` | CI invocation + Makefile `typecheck` recipe | ci.yml, `analytics-service/Makefile` |
| `ci: typecheck test` | Makefile target | `analytics-service/Makefile` |
| annotated `lifespan`, `_crash_handler`, `verify_service_key`, `health` (+ `response_model=None`) | annotations | `analytics-service/main.py` |
| annotated `_safe_mark`, `_claim_priority`, `_claim_legacy`, `_mark_done`, `_mark_failed`, `_mark_failed_fallback`, `_reset`, `_enqueue`, `_query` | annotations | `analytics-service/main_worker.py` |
| `_redact_before_send(event: Event, hint: Hint \| None) -> Event` | annotation | `analytics-service/sentry_init.py` |
| `164.6.1-01-SUMMARY.md` | evidence (mypy lines, OpenAPI hashes, D-06a output, D-06b OPEN) | phase dir |

<output>
Create `.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-01-SUMMARY.md` when done.
</output>
