---
phase: 164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv
verified: 2026-09-24T00:45:00Z
status: human_needed
score: 12/13 must-haves verified
verified_at_sha: 2163990e4ec0a766c65e1a2a1f87f9b4fb8c0e0f
drift_subjects:
  - analytics-service/main.py
  - analytics-service/main_worker.py
  - analytics-service/sentry_init.py
  - analytics-service/pyproject.toml
  - analytics-service/Makefile
  - analytics-service/services/audit.py
  - analytics-service/services/ingestion/__init__.py
  - .github/workflows/ci.yml
  - src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts
  - src/__tests__/contracts/contracts-registry.test.ts
  - src/__tests__/contracts/REGISTRY.md
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "ROADMAP SC-3 CI half (D-06b). On the phase PR, push ONE commit dropping `[None]` from `_crash_handler`'s `asyncio.Task[None]` in analytics-service/main.py (plain message, no CI-skip token of any kind). Bind the run to that head SHA via the actions/runs?head_sha API and COUNT the runs (empty is not a pass). Then push the restore commit and bind a green run to its SHA."
    expected: "The `python` job's step `Type gate - mypy strict over the running-service surface` exits 1 with a `main.py:` `type-arg` line naming `Task` on the neuter SHA, and exits 0 (`Success: no issues found in 100 source files`) on the restore SHA."
    why_human: "Requires a GitHub Actions run on the PR; CI does not run on this branch until the PR exists. The local scratch-copy neuter (D-06a) was reproduced by this verifier, but SC-3 explicitly requires the JOB to be observed RED. Owner: orchestrator (ship time)."
---

# Phase 164.6.1: MYPYSTRICT Verification Report

**Phase Goal:** the `mypy --strict` gate claims to cover all running-service code and does not cover the module that IS the service. Deliver: annotate `main.py`, add it to the ci.yml mypy invocation, and correct the comment so it names the real surface.
**Verified:** 2026-09-24
**Verified at:** `2163990e4ec0a766c65e1a2a1f87f9b4fb8c0e0f` (after the origin/main merge at `95d52ab0`)
**Status:** human_needed. The only open item is SC-3's CI half (D-06b), which is a ship-time step by design.
**Re-verification:** No. This is the initial verification.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | SC-1: `mypy --strict` covers `main.py` in ci.yml, and `mypy --strict main.py` reports 0 errors | ✓ VERIFIED | The run line of the ci.yml `python` job step `Type gate - mypy strict over the running-service surface` names `main.py main_worker.py main_worker_healthz.py sentry_init.py` after `services/ routers/ models/`. The step runs under job `defaults.run.working-directory: analytics-service`. The verifier ran that exact command with the pinned mypy 2.2.0: `Success: no issues found in 100 source files`. `mypy --strict main.py` gave `Success: no issues found in 1 source file`. |
| 2 | SC-2: the comment above the invocation names the real surface, with no residual claim of coverage the command does not deliver | ✓ VERIFIED | The comment block above the step names the four top-level modules and states the before/after sets (96, then 100). It explains the `--follow-imports=silent` "reached vs checked" trap and says why `scripts/` and `tests/` stay out. `git grep -i "ALL running"` over `.github`, `analytics-service`, `src` and `scripts` finds no live claim; the only hits are the contract test and registry quoting the OLD claim as history. The Makefile help, the `typecheck` header, the `services/audit.py` comment, the `services/ingestion/__init__.py` docstring and the pyproject `[tool.mypy]` header were all corrected. |
| 3 | SC-3: proven by neutering, with the `python` job observed RED naming `main.py` | ? HUMAN (ship time) | **Local half reproduced by this verifier:** on a scratch rsync copy, `_crash_handler(task: asyncio.Task)` left the OLD command green (`Success ... 96 source files`). The NEW command gave `main.py:351: error: Missing type arguments for generic type "Task" [type-arg]` and exited 1. A second independent neuter (the `-> ...` return type stripped from `health`) also went RED on the NEW command (`main.py:935 ... [no-untyped-def]`). **The CI half (D-06b) is OPEN** and is routed to Human Verification with owner "orchestrator (ship time)". It is NOT counted as passed. |
| 4 | SC-4: no file silently enters or leaves strict scope; the before/after set is stated | ✓ VERIFIED | The verifier measured the OLD command at `96 source files` and the NEW one at `100`. The difference is exactly the four named modules, which are the only tracked top-level `*.py` files under `analytics-service/`. The set is stated in the ci.yml comment and is also mechanised by the contract test's surface equality. |
| 5 | Real annotations only: no `# type: ignore`, no `cast(` added; Supabase closures `-> Any` | ✓ VERIFIED | `git diff 9d03ff87..HEAD -- analytics-service/` contains no added line matching `type: ignore` or `cast(`. The five `main_worker.py` closures carry `-> Any`. `_mark_done`, `_mark_failed` and `_mark_failed_fallback` are typed with `str`, `str \| None` and `ErrorKind \| None`. `main.py` uses `AsyncIterator[None]`, `asyncio.Task[None]`, `RequestResponseEndpoint -> Response` and `JSONResponse \| dict[str, Any]`. `sentry_init._redact_before_send` uses `Event`/`Hint`. |
| 6 | No runtime behaviour change; `/health` carries `response_model=None` so the service boots | ✓ VERIFIED | The decorator reads `@app.get("/health", response_model=None)`. The verifier ran the focused pytest from `analytics-service/` (test_health, test_verify_service_key_middleware, test_main_worker, test_sentry_init, test_redact): `145 passed`, and these import `main`. The only non-signature code change is `sentry_init`'s single `breadcrumbs = event.get(...)` binding (same object, same control flow). The OpenAPI hash equality (`8103da730102c4fc` before and after) is the executor's measurement and was not re-run here. |
| 7 | Makefile `typecheck` names exactly the CI path set; `make ci` runs `typecheck` | ✓ VERIFIED | The recipe is `$(MYPY) --config-file=pyproject.toml services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py`. `ci: typecheck test` is present. Strictness comes from the pyproject `strict = true`, and the contract test pins both. |
| 8 | D-06a scratch-copy neuter: OLD green, NEW RED; tracked tree never edited | ✓ VERIFIED | Independently reproduced (see #3). The verifier's copy lived in the session scratchpad; `git status` of the worktree shows only the two pre-existing unrelated modifications. |
| 9 | D-06b recorded OPEN, not closed | ✓ VERIFIED | Both SUMMARYs, the TODOS `[MYPY-MAINPY-01]` closure line ("STILL OPEN: ROADMAP SC-3's CI half, D-06b") and the contract test's declared limits all name it OPEN. |
| 10 | The contract test pins CI set == disk set == Makefile set, exactly one invocation, with calibration legs | ✓ VERIFIED | `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` reads the real ci.yml, Makefile and pyproject, and derives the surface from `git ls-files`. The verifier ran it with the registry test: `2 passed, 135 tests passed`. Calibration legs run the real `surfaceProblems` over mutated in-memory copies, and the HISTORICAL leg (f) feeds the pre-phase command and gets all four modules named. The round-2 fix report records a 47/47 arm neuter sweep (not re-run here). Leg (g2) was re-anchored to `test-db-drift` in `2163990e`. Its own guard asserts the job after `python` is `test-db-drift`, and it passes. |
| 11 | Registered: a CONTRACT_GUARDS row with a declared limit, floor 60 → 61, and a REGISTRY.md row | ✓ VERIFIED | The registry row exists, `toBeGreaterThanOrEqual(61)` is present, 61 `{ path:` rows were counted, and the REGISTRY.md row cites `164.6.1 / MYPY-MAINPY-01`. |
| 12 | TODOS `[MYPY-MAINPY-01]` closed in code with D-06b OPEN; tests/ and scripts/ policy not carried | ✓ VERIFIED | The checkbox is `[x]` with a dated closure naming D-06b as still open. `tests`/`scripts` appear only as the test's `EXCLUDED` record with reasons. |
| 13 | D-11: not added to `vitest.node-env.ts` | ✓ VERIFIED | `grep -c ci-mypy-strict-surface vitest.node-env.ts` = 0. |

**Score:** 12/13 truths verified (0 present, behaviour-unverified; 1 routed to human at ship time: SC-3's CI half).

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `analytics-service/main.py` | annotated lifespan, `_crash_handler`, `verify_service_key`, health | ✓ VERIFIED | Contains `RequestResponseEndpoint`; strict-clean on its own |
| `analytics-service/main_worker.py` | nine annotated closures | ✓ VERIFIED | Imports `ErrorKind`; strict-clean in the gate |
| `analytics-service/sentry_init.py` | Event/Hint typing | ✓ VERIFIED | `from sentry_sdk.types import Event, Hint` |
| `.github/workflows/ci.yml` | named, widened step and corrected comment | ✓ VERIFIED | One step, one run line, and the pin is cited once |
| `analytics-service/Makefile` | typecheck mirrors CI; `ci: typecheck test` | ✓ VERIFIED | |
| `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` | surface pin | ✓ VERIFIED | Contains `surfaceProblems`; green on the real tree |
| `src/__tests__/contracts/contracts-registry.test.ts` / `REGISTRY.md` | row and floor 61 | ✓ VERIFIED | |

### Key Link Verification

| From | To | Via | Status |
|------|----|-----|--------|
| ci.yml `python` job mypy step | the four top-level modules | named on the command line (checked, not silently followed) | ✓ WIRED. The neuter on the NEW command names `main.py` while the OLD command stays green |
| Makefile `typecheck` | ci.yml step | identical path list | ✓ WIRED. Pinned by the contract test |
| contract test | ci.yml step | `STEP_NAME` equals the step name | ✓ WIRED |
| CONTRACT_GUARDS | contract test | row plus floor 61 | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command (from `analytics-service/`) | Result | Status |
|----------|---------|--------|--------|
| CI gate green over the widened set | the ci.yml run line | `Success: no issues found in 100 source files` | ✓ PASS |
| `main.py` strict-clean alone | `mypy --strict main.py` | `Success: no issues found in 1 source file` | ✓ PASS |
| OLD command set size | `mypy --strict --follow-imports=silent services/ routers/ models/` | `96 source files` | ✓ PASS |
| Neuter `_crash_handler` (scratch copy) | OLD vs NEW | OLD green; NEW `main.py:351 [type-arg]`, exit 1 | ✓ PASS |
| Neuter `health` return type (scratch copy) | NEW | `main.py:935 [no-untyped-def]` | ✓ PASS |
| Touched-module behaviour | focused pytest (5 files) | `145 passed` | ✓ PASS |
| Surface pin and registry | vitest over the two files | `135 passed` | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| MYPY-MAINPY-01 | 01, 02 | TODOS id: the gate's comment claims coverage it lacks, and `main.py` is outside it | ✓ SATISFIED in code; SC-3 CI half OPEN | Truths 1–13 |

MYPY-MAINPY-01 is a TODOS.md id, not a REQUIREMENTS.md id: the ROADMAP says "TBD (no v1.20 requirement IDs) + TODOS `MYPY-MAINPY-01`", and REQUIREMENTS.md has 0 hits for either the id or the phase. No REQUIREMENTS.md id maps to this phase, so no requirement is orphaned.

### Anti-Patterns Found

| File | Symbol | Pattern | Severity | Impact |
|------|--------|---------|----------|--------|
| `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` (header "DECLARED LIMITS" bullet on the `python` job's `if:`); the matching sentence in the `CONTRACT_GUARDS` row `invariant` in `contracts-registry.test.ts`; the `REGISTRY.md` row "Declared limits" | prose | **Stale job name after the origin/main merge.** The text says a skipped `python` job is caught because "`sql-tests` needs `python`, and the `frontend` aggregator reds a `sql-tests` skip". Since Phase 164.4.2, `sql-tests` has NO `needs: python` edge; the ci.yml `sql-tests` comment records that the edge moved away. The job that carries `needs: [python]` is now `test-db-drift`, and the `frontend` aggregator reds a trusted-event skip of **`test-db-drift`**. The mechanism still holds, but the prose names the wrong job, which is this phase's own defect class (a restated claim that went stale). The (g2) re-anchor fixed the calibration but missed these three sentences. | ⚠️ Warning | This is not a coverage claim about the gate command, so SC-2 holds. Recommend replacing `sql-tests` with `test-db-drift` in all three places before ship. |

No `TBD`/`FIXME`/`XXX` markers were added. No stubs were found.

### Human Verification Required

#### 1. SC-3 / D-06b: the `python` job observed RED on the PR, then GREEN

**Owner:** orchestrator (ship time)
**Test:** On the phase PR, push one commit dropping `[None]` from `_crash_handler`'s `asyncio.Task[None]` in `analytics-service/main.py`. Word its message plainly and include no CI-skip token of any kind. Bind the run to the head SHA and count the runs. Then push the restore commit and bind a green run to its SHA.
**Expected:** On the neuter SHA, step `Type gate - mypy strict over the running-service surface` logs `main.py:` `type-arg` `Task` and exits 1. On the restore SHA it logs `Success: no issues found in 100 source files`.
**Why human:** It needs a GitHub Actions run on the PR. A green run alone cannot distinguish "covered" from "not reached", which is the point of SC-3.

### Gaps Summary

No blocking gaps. All code deliverables are verified against the tree at `2163990e`:
- `main.py` and the other three top-level modules are named on the ci.yml command line and are strict-clean with real annotations.
- The false coverage comment is corrected along with every other surface statement.
- The before/after set is stated and mechanically pinned.
- The widened gate goes RED on a neuter that the old gate misses, as this verifier reproduced independently.

The phase stays `human_needed` solely for SC-3's CI half (D-06b), which is by design a ship-time observation. One non-blocking warning: three declared-limit sentences still name `sql-tests` as the job that needs `python`. After the 164.4.2 merge, that job is `test-db-drift`.

---

_Verified: 2026-09-24_
_Verifier: Claude (gsd-verifier)_
