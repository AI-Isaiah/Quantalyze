---
phase: 164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv
plan: 02
type: execute
wave: 2
depends_on:
  - 164.6.1-01
files_modified:
  - src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts
  - src/__tests__/contracts/contracts-registry.test.ts
  - src/__tests__/contracts/REGISTRY.md
  - TODOS.md
  - .github/workflows/ci.yml
  - analytics-service/Makefile
autonomous: true
requirements:
  - MYPY-MAINPY-01

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A vitest contract test extracts the mypy command from the ci.yml `python` job's step `Type gate - mypy strict over the running-service surface` and asserts its path set EQUALS the service surface derived from disk: every directory directly under analytics-service/ carrying an `__init__.py` minus the hand-typed exclusions {tests, scripts}, plus every top-level analytics-service/*.py file (D-03, D-09, ROADMAP SC-4)"
    - "The same test asserts the Makefile `typecheck` recipe names the same set, and that exactly ONE mypy --strict invocation exists in the `python` job (D-03, D-05)"
    - "The test is proven able to fail: calibration legs on mutated COPIES of the real ci.yml / Makefile text / disk listing each produce a problem naming the dropped or phantom member, a HISTORICAL leg feeds the pre-phase command and gets all four top-level modules named, a non-vacuity control passes the unmutated inputs through the same function, and a final arm proves ci.yml and the Makefile are byte-unchanged on disk (D-03 anti-vacuity)"
    - "The ci.yml comment above the mypy step and the Makefile comment above `typecheck:` name the surface-pin test in the SAME commit that creates it, so no comment ever names a pin that does not exist; the run line, step name and recipe line are untouched"
    - "The test is registered: a `CONTRACT_GUARDS` row with a DECLARED LIMIT, the hand-typed floor RAISED 60 → 61 with the predicate quoted beside it, and a REGISTRY.md prose row"
    - "TODOS.md `[MYPY-MAINPY-01]` is marked closed IN CODE by Phase 164.6.1 with D-06b (the CI `python` job observed RED on the PR) named as still OPEN; the two `tests/`-policy entries quoting the old command each gain one dated CORRECTED line; no original text is rewritten (D-08)"
    - "D-11: the new contract test is not added to `vitest.node-env.ts`; like its `ci-*.contract.test.ts` siblings it runs under the default environment"
    - "The `analytics-service/tests/` and `analytics-service/scripts/` strict-scope questions are NOT carried — both stay outside the gate and appear only as the test's declared exclusions"
  artifacts:
    - path: "src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts"
      provides: "surface pin: CI set == disk set == Makefile set, with calibration"
      contains: "surfaceProblems"
    - path: "src/__tests__/contracts/contracts-registry.test.ts"
      provides: "registry row + floor 61"
      contains: "ci-mypy-strict-surface.contract.test.ts"
    - path: "src/__tests__/contracts/REGISTRY.md"
      provides: "prose registry row"
      contains: "164.6.1 / MYPY-MAINPY-01"
    - path: "TODOS.md"
      provides: "[MYPY-MAINPY-01] closed in code; D-06b OPEN; two dated CORRECTED lines"
      contains: "164.6.1"
  key_links:
    - from: "src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts"
      to: ".github/workflows/ci.yml python job step"
      via: "STEP_NAME constant equal to the ci.yml step name"
      pattern: "Type gate - mypy strict over the running-service surface"
    - from: "src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts"
      to: "analytics-service/Makefile typecheck recipe"
      via: "recipe line parsed after $(MYPY)"
      pattern: "typecheck:"
    - from: "src/__tests__/contracts/contracts-registry.test.ts CONTRACT_GUARDS"
      to: "src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts"
      via: "registry row path + floor 61"
      pattern: "toBeGreaterThanOrEqual\\(61\\)"
---

<objective>
Mechanise the gate's surface claim so it cannot silently go stale again (D-02 mechanised, D-03, D-09), register the new guard (floor 60 → 61), and close the ledger entry honestly (D-08): `[MYPY-MAINPY-01]` closed IN CODE, with the CI-side RED observation (D-06b) named OPEN.

Purpose: every restated-scope correction in this repo that shipped as prose alone went stale. The corrected ci.yml comment from Plan 01 is exactly that shape; this test turns "a new top-level module or package was added without widening the gate" into a RED check rather than a false comment.
Output: one new contract test, a registry row + floor bump, a REGISTRY.md row, TODOS.md closure lines, SUMMARY.

Spec-less probe fallback: skipped (no v1.20 requirement IDs; only the TODOS id MYPY-MAINPY-01) — visible skip, recorded here.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-CONTEXT.md
@.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-RESEARCH.md
@.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-PATTERNS.md
@CLAUDE.md

## Environment facts (read before any command)

- vitest from YOUR worktree root: `node_modules/.bin/vitest run <file>`. If `node_modules/.bin/vitest` is absent in your worktree, use `npx --no-install vitest run <file>` (resolves by walking up). Never install packages. Never stage or commit `node_modules`.
- `PY` = `"$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python"`, run only with cwd = your worktree's `analytics-service/`. ⛔ Never pytest from the repo root; never start `uvicorn main:app`.
- ⛔ `git checkout --`, `git restore`, `git stash` are FORBIDDEN. Git steps ONE AT A TIME; stage by explicit path; never stage `.planning/milestone.lock`, `.planning/state.json`, `.planning/phases/164.5.3-*`, `node_modules`. No CI-skip trailer token in any commit message.
- Repo is PUBLIC; nothing under `.planning/` may contain a home path or the local username.
- Cite by SYMBOL in all new prose and code comments; no `ci.yml:NNNN` line numbers.
- Do NOT touch `VERSION`, `package.json`, `CHANGELOG.md`.
- Why verify commands start with `cd "$(git rev-parse --show-toplevel)..."` rather than an absolute path: each executor runs in its own harness worktree, so any literal worktree path would point at the wrong checkout; `git rev-parse --show-toplevel` resolves to the executor's own worktree root every time.
- `scripts/check-planning-hygiene.ts` enumerates files with `git ls-files -z`, so it CANNOT see an untracked file. A SUMMARY must be staged (`git add -- <summary path>`, as its own command) BEFORE the check runs, and is additionally grepped directly for the home-directory basename (derived at runtime with `basename "$HOME"`, never typed literally), the macOS home-directory prefix (built at runtime with `printf '\057Users\057'` and never typed literally, because `scripts/check-planning-hygiene.ts` flags that literal as ABSOLUTE-HOME-PATH in any tracked file, this PLAN.md included), `/home/` and `/private/tmp/`.
- Per D-11 the new contract test is NOT added to `vitest.node-env.ts`; its `ci-*.contract.test.ts` siblings run under the default environment and so does it.

## Symbol claims

<!-- verify-symbol: src/__tests__/contracts/ci-docs-path-filter.contract.test.ts function extractRunScript -->
<!-- verify-symbol: src/__tests__/contracts/ci-docs-path-filter.contract.test.ts CALIBRATION -->
<!-- verify-symbol: src/__tests__/contracts/contracts-registry.test.ts toBeGreaterThanOrEqual( -->
<!-- verify-symbol: src/__tests__/contracts/contracts-registry.test.ts analytics-deploy-tree-compare.contract.test.ts -->
<!-- verify-symbol: src/__tests__/contracts/REGISTRY.md ci-docs-path-filter.contract.test.ts -->
<!-- verify-symbol: TODOS.md [MYPY-MAINPY-01] -->
<!-- verify-symbol: TODOS.md --follow-imports=silent services/ routers/ models/`, clean at 91 files) -->

## Facts carried from Plan 01 (confirm against `164.6.1-01-SUMMARY.md` in this phase directory, which Plan 01 writes)

- ci.yml `python` job step name: `Type gate - mypy strict over the running-service surface`; single-line `run:` value `mypy --strict --follow-imports=silent services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py`.
- `analytics-service/Makefile` `typecheck:` recipe (TAB-indented, one line): `$(MYPY) services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py`.
- Disk today: top-level `*.py` = `main.py`, `main_worker.py`, `main_worker_healthz.py`, `sentry_init.py`; directories with `__init__.py` = `models`, `routers`, `scripts`, `services`, `tests`.
- Pre-phase command (for the HISTORICAL calibration leg): `mypy --strict --follow-imports=silent services/ routers/ models/`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1 (tracer): the surface-pin contract test, wired end-to-end — disk → ci.yml → Makefile → assertion → registered guard</name>
  <files>src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts, src/__tests__/contracts/contracts-registry.test.ts, src/__tests__/contracts/REGISTRY.md, .github/workflows/ci.yml, analytics-service/Makefile</files>
  <read_first>
    - src/__tests__/contracts/ci-docs-path-filter.contract.test.ts (header doc-comment four-part shape; `extractRunScript` fail-loud miss; `JOBS_KEY_IDX` / `JOB_KEY_POSITIONS` / `jobBlockLines`; the CALIBRATION describe block with `mutate`, NON-VACUITY CONTROL and the byte-unchanged final arm)
    - src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts (secondary loader shape)
    - src/__tests__/contracts/contracts-registry.test.ts (`CONTRACT_GUARDS` rows — last row is `analytics-deploy-tree-compare.contract.test.ts`; the floor comment paragraphs and `toBeGreaterThanOrEqual(60)`)
    - src/__tests__/contracts/REGISTRY.md (the table; the `ci-docs-path-filter.contract.test.ts` row as the shape; the last table row)
    - .github/workflows/ci.yml (the `  python:` job block and the step `Type gate - mypy strict over the running-service surface`)
    - analytics-service/Makefile (`typecheck:` target)
    - .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-PATTERNS.md (section for the new contract test, registry, REGISTRY.md)
  </read_first>
  <action>
    First, run `node scripts/verify-plan-anchors.mjs` on this PLAN.md from the worktree root and HALT on a non-zero exit (repo convention). Confirm the carried Plan 01 facts against the working tree (`grep -n 'Type gate - mypy strict' .github/workflows/ci.yml`); if the step name or run line differs, STOP and report rather than adapting the test to it.

    Create `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` implementing D-03 and D-09. Follow the analog's conventions exactly; line-based text extraction, no YAML library (none is a dependency). Imports: vitest `describe`/`expect`/`it`, and from `node:fs` only `readFileSync`, `readdirSync`, `existsSync`; `join` from `node:path`. No `spawnSync`, no tempdir, no file writes.

    Header doc-comment, four parts (copy the analog's shape): (1) why it is NOT a grep pin — a `toContain("main.py")` passes when the comment names the module and the command drops it, so it parses the command line itself; (2) WHAT IT PINS — CI path set == disk-derived service surface == Makefile `typecheck` set, one invocation only, and the before/after record for D-02 (before: `services/ routers/ models/`, 96 source files as mypy counts them; after: plus exactly `main.py main_worker.py main_worker_healthz.py sentry_init.py`, 100); (3) WHAT IT DOES NOT PIN, stated rather than implied — it does not prove mypy is green (the `python` job does), and it does not prove the job goes RED on GitHub (D-06b, OPEN until the phase PR); (4) PLACEMENT — lives in `src/__tests__/contracts/`, so it runs in `contracts.yml` (no paths filter) and the `frontend-test` shards; a docs-only PR cannot change ci.yml or the Makefile. Describe titles use the prefix `[164.6.1 / MYPY-MAINPY-01]`.

    Constants: `ROOT = process.cwd()`; `STEP_NAME = "Type gate - mypy strict over the running-service surface"`; paths to `.github/workflows/ci.yml`, `analytics-service/Makefile`, `analytics-service/`; `EXCLUDED` = a hand-typed record `{ tests: <reason: untyped by design, the open B-mypy part j policy question in TODOS.md>, scripts: <reason: one-off operational tooling, not the running service> }`.

    Pure functions (each throws a remediation message on a missing anchor, like `extractRunScript`):
    - `pythonJobLines(ymlText)` — the `jobs:` key, then the `  python:` job block up to the next two-space job key (reuse the analog's `JOB_KEY_POSITIONS` approach, but parameterised on the text so calibration can feed mutated copies).
    - `mypyInvocationCount(jobLines)` — number of non-comment lines (trimmed line not starting with `#`) matching `\bmypy\b` together with `--strict`.
    - `ciMypyArgs(ymlText)` — inside the python block, find the line whose trim equals `- name: ${STEP_NAME}`, then the next line whose trim starts with `run: `; its value must start with `mypy `; return the whitespace-split tokens after `mypy`. Throw if the step, the run line, or the `mypy` prefix is missing.
    - `makefileTypecheckArgs(makefileText)` — the line equal to `typecheck:`, then the next TAB-indented recipe line; tokens after `$(MYPY)`. Throw if absent.
    - `pathSet(tokens)` — drop tokens starting with `--`; strip one trailing `/`; return a Set. `flagSet(tokens)` — the `--` tokens.
    - `readListing(dir)` — `readdirSync(dir, { withFileTypes: true })` mapped to `{ name, kind: "file" | "dir", hasInit }` where `hasInit` is `existsSync(join(dir, name, "__init__.py"))` for dirs.
    - `diskSurface(listing, excluded)` — top-level files ending `.py` plus dirs with `hasInit` whose name is not a key of `excluded`.
    - `surfaceProblems(ymlText, makefileText, listing, excluded)` returning `string[]`: invocation count must be exactly 1; CI flags must include `--strict` and `--follow-imports=silent`; every disk member missing from CI → a problem naming the member and "ci.yml"; every CI path not in the disk surface → a problem naming it; the same two checks for the Makefile set, each naming "Makefile"; every `excluded` key must exist in the listing as a dir → otherwise a problem naming the stale exclusion. Tokenise on whitespace, never substring-match (`main.py` is a substring of `main_worker.py`).

    Real arm: `surfaceProblems(realYml, realMakefile, readListing(analytics-service), EXCLUDED)` equals `[]`, with the joined problems as the assertion message.

    CALIBRATION describe block (copy the analog's `mutate(target, replacement, label)` helper with its three self-checks — target present, mutated differs, target gone). Every leg feeds MUTATED COPIES of the real TEXT or listing through `surfaceProblems`, so extraction runs too:
    - (a) drop the ` main.py` token from the ci.yml `run:` line (mutate the exact full run-line string to the same string without ` main.py`) → a problem containing `main.py` and `ci.yml`;
    - (b) add a phantom file `newmod.py` to the listing → a problem containing `newmod.py`;
    - (c) drop ` main_worker.py` from the Makefile recipe line → a problem containing `main_worker.py` and `Makefile`;
    - (d) add a phantom dir `newpkg` with `hasInit: true` to the listing → a problem containing `newpkg` (D-09);
    - (e) append a second, partial `mypy --strict --follow-imports=silent services/` step line inside the python block copy → a problem about the invocation count;
    - (f) HISTORICAL leg — replace the run-line value with the pre-phase command `mypy --strict --follow-imports=silent services/ routers/ models/` → problems naming each of `main.py`, `main_worker.py`, `main_worker_healthz.py`, `sentry_init.py` (this is the exact defect this phase removes);
    - (g) remove `scripts` from the listing → a stale-exclusion problem naming `scripts`.
    - NON-VACUITY CONTROL: the unmutated inputs yield `[]` through the same function, the derived disk surface contains `main.py` and at least 3 package directories, and `ciMypyArgs` returns at least 7 path tokens.
    - Final arm: ci.yml and the Makefile read from disk now are byte-equal to the texts read at module load.

    Registration (same commit): in `contracts-registry.test.ts` append a `CONTRACT_GUARDS` row after the `analytics-deploy-tree-compare.contract.test.ts` row: path `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts`, batch `"164.6.1 / MYPY-MAINPY-01"`, invariant: the mypy --strict invocation in the ci.yml `python` job is pinned, by EXTRACTION rather than grep, to equal the service surface derived from disk and the Makefile `typecheck` set, so a new top-level module or package added without widening the gate is RED; with a DECLARED LIMIT: it proves the command NAMES the surface, not that mypy is green, and the CI-side RED observation is D-06b, open until the phase PR. Add a dated paragraph above the floor literal in the existing style: `── 164.6.1 / MYPY-MAINPY-01 — RAISED 60 → 61`, quoting the predicate `grep -c '^  { path:' src/__tests__/contracts/contracts-registry.test.ts` with its measured BEFORE (60, against a floor of 60) and AFTER (61) values, and noting the previous dated paragraph stops at 59 while the literal was already 60, so the 60 baseline is stated as measured, not re-derived. Change the literal to `61` (hand-typed; never `CONTRACT_GUARDS.length`). In `REGISTRY.md` append a table row after the current last row, mirroring the `ci-docs-path-filter.contract.test.ts` row: bold one-line invariant, why it exists (the comment claimed ALL running-service code while the entry module was off the command line), both polarities (calibration legs), declared limit (D-06b).

    Cite the pin where the gate is stated (same commit as the test, so no comment ever names a pin that does not exist — Plan 01 deliberately left this out): in `.github/workflows/ci.yml`, add ONE comment sentence to the Phase 164.6.1 clause of the comment block above the step `Type gate - mypy strict over the running-service surface`, naming `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` as what pins this command's path set to the on-disk service surface and to the Makefile `typecheck` set. In `analytics-service/Makefile`, add ONE comment line to the comment above `typecheck:` naming the same test as the pin. Comment lines only: the `run:` line, the step name and the recipe line are untouched (the new test's real arm re-reads them, and its byte-unchanged arm compares disk to what was read at module load, so these comment edits must be saved BEFORE the vitest run).

    Commit the five paths explicitly, e.g. `test(164.6.1-02): pin the mypy strict invocation to the on-disk service surface`.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts src/__tests__/contracts/contracts-registry.test.ts</automated>
    <fails_when>non-zero exit; any `failed` count in the summary; or `Test Files` shows fewer than 2 passed; or the new file reports fewer than 10 passed tests (real arm + 7 calibration legs + non-vacuity control + byte-unchanged arm)</fails_when>
    <automated>grep -c '^  { path:' src/__tests__/contracts/contracts-registry.test.ts</automated>
    <fails_when>prints anything other than 61</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'toBeGreaterThanOrEqual(61)' src/__tests__/contracts/contracts-registry.test.ts` prints 1 and `grep -c 'toBeGreaterThanOrEqual(60)' src/__tests__/contracts/contracts-registry.test.ts` prints 0
    - `grep -c 'ci-mypy-strict-surface.contract.test.ts' src/__tests__/contracts/REGISTRY.md` prints at least 1
    - `grep -c 'Type gate - mypy strict over the running-service surface' src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` prints at least 1
    - `grep -cE 'spawnSync|writeFileSync|js-yaml|from "yaml"' src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` prints 0
    - `grep -cE 'ci\.yml:[0-9]' src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` prints 0
    - `grep -c 'ci-mypy-strict-surface.contract.test.ts' .github/workflows/ci.yml` prints at least 1 and the same grep on `analytics-service/Makefile` prints at least 1, and both land in the SAME commit that creates the test file (`git show --name-only <sha>` of this task's commit lists all five paths)
    - `grep -c 'mypy --strict --follow-imports=silent services/ routers/ models/ main.py main_worker.py main_worker_healthz.py sentry_init.py' .github/workflows/ci.yml` still prints 1 (the run line was not touched)
    - vitest output lists each calibration leg (a)–(g), the NON-VACUITY CONTROL and the byte-unchanged arm as passed
  </acceptance_criteria>
  <done>The surface pin runs green on the real tree, every calibration leg reds on its mutated copy, the guard is registered with the floor at 61, and ci.yml / the Makefile are unchanged on disk.</done>
  <reversibility rating="reversible">Adds one test file and registry lines; removable by a follow-up commit (with the floor lowered in the same commit).</reversibility>
</task>

<task type="auto" tdd="false">
  <name>Task 2: close the ledger honestly and run the phase-wide gate sweep</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md (the `[MYPY-MAINPY-01]` entry under `## Phase 146 close-out notes`; the `tests/`-policy entry directly above it quoting `services/ routers/ models/` "clean at 91 files"; the Phase 142 bullet quoting "the gate is deliberately `mypy --strict --follow-imports=silent services/ routers/ models/`"; an existing `⛔ **CORRECTED 2026-09-11 —` line and the `[164.11-DEPLOYGATE-CANCELLED]` `- [x]` closure as shape analogs)
    - .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-01-SUMMARY.md (the measured mypy lines and the D-06b procedure to cite)
  </read_first>
  <action>
    TODOS.md (D-08; lineage kept — never rewrite original text; use today's date 2026-09-23 or the actual execution date):
    - `[MYPY-MAINPY-01]`: flip `- [ ]` to `- [x]` and append ONE dated closure line in the repo's bold-lead style stating: CLOSED IN CODE by Phase 164.6.1 — the ci.yml `python` job's step `Type gate - mypy strict over the running-service surface` now names `main.py`, `main_worker.py`, `main_worker_healthz.py`, `sentry_init.py` (96 → 100 source files, 0 errors, real annotations, no ignores); the false coverage comment, the Makefile, `services/audit.py` and `services/ingestion/__init__.py` statements were corrected; `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` pins the invocation to the on-disk surface; the local neuter proof (D-06a) was observed. Then, explicitly: ⚠️ ROADMAP SC-3's CI half (D-06b — the `python` job observed RED naming `main.py` on the phase PR, SHA-bound, then restored) is STILL OPEN and is a ship-time step. Note also that the line numbers quoted in the original body are stale; cite by symbol. Do not edit the original body.
    - The two `tests/`-policy entries that quote the pre-phase command (the Phase 146 close-out bullet "clean at 91 files" and the Phase 142 bullet "the gate is deliberately ..."): append one dated `⛔ **CORRECTED 2026-09-23 —` line to each, stating the gate command now also names the four top-level modules (100 source files) as of Phase 164.6.1, that `tests/` and `scripts/` remain outside by the same stated reasons (the policy question is unchanged and NOT carried by 164.6.1), and that the quoted command is kept as lineage. Do not touch the quoted old command text.

    Phase-wide sweep (no edits; evidence into the SUMMARY): run every verify command below and paste the key output lines. Then write `164.6.1-02-SUMMARY.md` including: the vitest counts, the registry count 61, the TODOS lines added (quoted), a section "D-06b — OPEN (ship-time)" repeating Plan 01's procedure verbatim, and a closing statement that ROADMAP SC-1, SC-2, SC-4 are met and SC-3 is met LOCALLY only. Redact any home path, scratchpad path or username in pasted output to `<home>` / `<scratch>` / `<user>`. After writing the SUMMARY: stage it with `git add -- .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-02-SUMMARY.md` as its own command (the hygiene check reads `git ls-files -z` and cannot see an untracked file), THEN run the hygiene check and the direct grep in verify — both BEFORE the SUMMARY is committed.

    Commit `TODOS.md` explicitly, e.g. `docs(164.6.1-02): close MYPY-MAINPY-01 in code; CI-side RED observation stays open`.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/__tests__/contracts/</automated>
    <fails_when>non-zero exit, or any `failed` count in the summary line</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)/analytics-service" && ARGS=$(sed -n 's/^ *run: mypy //p' ../.github/workflows/ci.yml) && "$HOME/claude-projects/quantalyze/analytics-service/.venv/bin/python" -m mypy $ARGS</automated>
    <fails_when>non-zero exit, or the last line is not `Success: no issues found in 100 source files`</fails_when>
    <automated>node scripts/verify-plan-anchors.mjs .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-02-PLAN.md</automated>
    <fails_when>non-zero exit, or output lacking `no stale claims`</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && git ls-files --error-unmatch .planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-02-SUMMARY.md && node_modules/.bin/tsx scripts/check-planning-hygiene.ts</automated>
    <fails_when>non-zero exit: `error: pathspec ... did not match` means the SUMMARY was not staged, so the hygiene check would not have scanned it; or any `LOCAL-USERNAME:` or absolute-home-path violation line (if `node_modules/.bin/tsx` is absent, use `npx --no-install tsx`)</fails_when>
    <automated>cd "$(git rev-parse --show-toplevel)" && S=.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-02-SUMMARY.md && test -f "$S" && U=$(basename "$HOME") && test -n "$U" && H=$(printf '\057Users\057') && test "${#H}" -eq 7 && { grep -nF -e "$U" -e "$H" -e /home/ -e /private/tmp/ "$S"; rc=$?; test "$rc" -eq 1; }</automated>
    <fails_when>non-zero exit: the SUMMARY is missing; or any printed line (the home-directory basename or an absolute home/scratch path survived redaction, grep rc 0); or a grep error (rc 2). Exit 0 only on no match (rc 1).</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c '\[MYPY-MAINPY-01\]' TODOS.md` is unchanged from before the edit (entry not duplicated) and the entry's checkbox line starts `- [x] **\`[MYPY-MAINPY-01]\``
    - `grep -c 'CORRECTED 2026-09-23' TODOS.md` increased by at least 2
    - `grep -c 'clean at 91 files' TODOS.md` is unchanged (original text kept)
    - the closure line contains both `164.6.1` and `D-06b` and the word `OPEN`
    - with `<sha>` = this task's own commit (from `git rev-parse HEAD` right after committing), `git diff --numstat <sha>^..<sha> -- TODOS.md` shows `1` in the deleted-lines column (only the `[MYPY-MAINPY-01]` checkbox line is replaced; every other change is an addition)
    - the SUMMARY contains a heading with `D-06b` and `OPEN`
    - the SUMMARY was staged before the hygiene check ran, the hygiene check exits 0, and the direct basename / absolute-path grep of the SUMMARY finds nothing
  </acceptance_criteria>
  <done>The ledger says exactly what is true — closed in code, CI-side observation open — and the phase-wide gates (contracts dir, widened mypy, plan anchors, hygiene) are green.</done>
  <reversibility rating="reversible">Ledger additions only.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer edit → ci.yml / Makefile | a later edit can narrow the gate or add a module without widening it |
| agent output → tracked public `.planning/` and `TODOS.md` | evidence could carry home paths / username or overstate closure |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.1-06 | T (Tampering, gate integrity) | `ci-mypy-strict-surface.contract.test.ts` | medium | mitigate | parse the command line (not grep), exactly-one invocation, disk-derived surface, calibration legs (a)–(g) + non-vacuity control + byte-unchanged arm |
| T-164.6.1-07 | R (Repudiation — overstated closure) | TODOS.md `[MYPY-MAINPY-01]` | medium | mitigate | closure line must name D-06b OPEN; acceptance criteria grep for `D-06b` + `OPEN` |
| T-164.6.1-08 | T (silent guard removal) | `CONTRACT_GUARDS` roster | low | mitigate | hand-typed floor raised 60 → 61 in the same commit, predicate quoted |
| T-164.6.1-09 | I (Information disclosure) | `164.6.1-02-SUMMARY.md` | low | mitigate | `check-planning-hygiene.ts` in Task 2 verify |
</threat_model>

<verification>
- `vitest run src/__tests__/contracts/` green, including the new file and the registry test at 61 rows.
- Widened mypy (read out of ci.yml) still prints `Success: no issues found in 100 source files`.
- `verify-plan-anchors.mjs` and `check-planning-hygiene.ts` exit 0.
- TODOS.md: `[MYPY-MAINPY-01]` closed in code with D-06b OPEN; two dated CORRECTED lines; lineage intact.
</verification>

<success_criteria>
- ROADMAP SC-4 mechanised: the before/after set is pinned by a test that reds on drift in either direction.
- D-03 / D-09 proven able to fail by in-test calibration.
- Ledger closure matches reality (SC-3 CI half OPEN).
</success_criteria>

## Artifacts this phase produces

| Symbol / artifact | Kind | Where |
|---|---|---|
| `STEP_NAME`, `EXCLUDED`, `pythonJobLines`, `mypyInvocationCount`, `ciMypyArgs`, `makefileTypecheckArgs`, `pathSet`, `flagSet`, `readListing`, `diskSurface`, `surfaceProblems` | test-local functions/constants | `src/__tests__/contracts/ci-mypy-strict-surface.contract.test.ts` (NEW) |
| `CONTRACT_GUARDS` row `164.6.1 / MYPY-MAINPY-01`; floor literal `61` | registry | `src/__tests__/contracts/contracts-registry.test.ts` |
| REGISTRY.md row for the new guard | docs | `src/__tests__/contracts/REGISTRY.md` |
| `[MYPY-MAINPY-01]` closure line; two `CORRECTED 2026-09-23` lines | ledger | `TODOS.md` |
| `164.6.1-02-SUMMARY.md` | evidence | phase dir |

<output>
Create `.planning/phases/164.6.1-mypystrict-the-strict-gate-claims-to-cover-all-running-serv/164.6.1-02-SUMMARY.md` when done.
</output>
