---
phase: "164.6.1"
slug: "mypystrict"
status: verified
# threats_open = count of OPEN threats at or above workflow.security_block_on severity (the blocking gate)
threats_open: 0
asvs_level: 1
created: "2026-09-24"
verified_at_sha: 2163990e4ec0a766c65e1a2a1f87f9b4fb8c0e0f
---

# Phase 164.6.1 — Security

> Per-phase security contract: threat register, accepted risks and audit trail. The register was authored at plan time in plans 01 and 02. `gsd-security-auditor` verified every mitigation at the SHA above. It modified no file and ran no database command.

---

## Trust Boundaries

| Boundary | Description | Data Crossing |
|----------|-------------|---------------|
| Type annotations → running service | The phase changes signatures in the process that serves every request (`main.py`, `main_worker.py`, `sentry_init.py`). A wrong annotation can change FastAPI route registration or middleware behaviour. | Request/response handling, service-key auth, Sentry event redaction |
| CI gate → merge decision | The strict type gate's path set decides which service code is type-checked before merge. | Gate integrity |
| Phase artifacts → public repo | Planning files are tracked in a public repository. | Home paths, usernames |

---

## Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation (verified by FILE + SYMBOL) | Status |
|-----------|----------|-----------|----------|-------------|------------|--------|
| T-164.6.1-01 | D | `main.py` `health` route registration | high | mitigate | `@app.get("/health", response_model=None)` landed in the same commit as the return annotation. `tests/test_health.py` passes, and the OpenAPI sha256 is equal before and after. | closed |
| T-164.6.1-02 | E/T | `main.py` `verify_service_key` | medium | mitigate | Only the signature changed; the body is untouched. The service-key middleware tests pass. | closed |
| T-164.6.1-03 | I | `sentry_init.py` `_redact_before_send` | medium | mitigate | Only a single binding of `breadcrumbs` and the `Event`/`Hint` types changed. `test_sentry_init.py` and `test_redact.py` pass. | closed |
| T-164.6.1-04 | T | ci.yml `python` type-gate step | medium | mitigate | The exact CI command reports 0 issues in 100 source files. The local neuter proof is recorded. D-06b (the CI red, then green) is declared OPEN until ship time. The path set is pinned by the plan 02 contract test. | closed |
| T-164.6.1-05 | I | 164.6.1-01-SUMMARY.md | low | mitigate | `check-planning-hygiene` OK over the tracked tree. | closed |
| T-164.6.1-06 | T | `ci-mypy-strict-surface.contract.test.ts` | medium | mitigate | Token parse, not grep. The surface is derived from `git ls-files`, exactly one invocation is allowed, and calibration legs plus non-vacuity controls are included: 135 tests pass. The plan's "byte-unchanged arm" was removed in review because it could not fail. | closed |
| T-164.6.1-07 | R | TODOS.md `[MYPY-MAINPY-01]` | medium | mitigate | The closure text names D-06b as still OPEN and states that it does not discharge it. | closed |
| T-164.6.1-08 | T | `CONTRACT_GUARDS` roster | low | mitigate | `contracts-registry.test.ts` floor moved 60 → 61 in the same commit as the new guard row. | closed |
| T-164.6.1-09 | I | 164.6.1-02-SUMMARY.md | low | mitigate | Same hygiene run: OK. | closed |

---

## Accepted Risks Log

No accepted risks.

---

## Advisories (non-blocking)

1. **Stale prose after the 164.4.2 merge, fixed.** Three places said a skipped `python` job is caught because "`sql-tests` needs `python`". Since Phase 164.4.2, the job that needs `python` is `test-db-drift`. Corrected in commit `971c035b`; the two contract files pass 135/135.
2. **D-06b is a declared ship-time step.** The CI `python` job must be seen going RED on `main.py` at a head SHA, then GREEN after the restore.

---

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Run By |
|------------|---------------|--------|------|--------|
| 2026-09-24 | 9 | 9 | 0 | gsd-security-auditor (verdict SECURED, ASVS 1, block_on high) at 2163990e |

---

## Sign-Off

- [x] All threats have a disposition (mitigate / accept / transfer)
- [x] Accepted risks documented in the Accepted Risks Log
- [x] `threats_open: 0` confirmed
- [x] `status: verified` set in frontmatter

**Approval:** verified 2026-09-24
