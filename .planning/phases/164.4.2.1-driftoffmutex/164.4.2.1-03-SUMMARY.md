---
phase: 164.4.2.1-driftoffmutex
plan: "03"
subsystem: ci-shared-test-db
tags: [docs, runbook, shared-test-db, mutex, currency]
status: complete
requires:
  - 164.4.2.1-02 (test-db-drift off the key, commit 15d8bfd77)
provides:
  - dated 2026-09-25 holder-set corrections in CLAUDE.md, the mutex runbook, the deploy-verify triage message and the wait script's comments
  - the Option A choice and its reason recorded in runbook section 0 (SC-1)
affects:
  - ship (CHANGELOG entry and VERSION bump, one commit)
  - the post-merge AFTER measurement (D-05)
tech-stack:
  added: []
  patterns: [dated CORRECTED notes beside kept lineage]
key-files:
  created: []
  modified:
    - CLAUDE.md
    - docs/runbooks/shared-test-db-mutex.md
    - .github/workflows/analytics-deploy-verify.yml
    - scripts/wait-for-test-schema-apply.sh
decisions:
  - "The runbook's section-0 choice paragraph cites 164.4.2.1-MEASUREMENT.md for numbers and restates none. CLAUDE.md carries the two headline figures (20m31s wait, 2-5 s of VAC-08 work) because the plan's action asks for them there. Both were checked against MEASUREMENT's quoted Run 1-5 table."
  - "The stale 'The acquire step below installs psql conditionally too' line in ci.yml's test-db-drift wait step was NOT changed. This plan forbids editing ci.yml, and plan 02 pinned that step's region sha256. It is recorded below."
metrics:
  duration: 5m
  completed: 2026-09-25
actuals:
  tokens: 4028
  tasks: 2
  commits: 1
plan_head_before: b579f0110eaa3ed94b2010b49be8052520f52a8a
---

# Phase 164.4.2.1 Plan 03: holder-set currency after test-db-drift left the key Summary

`CLAUDE.md`, the shared-test-db mutex runbook, the deploy-verify triage message and the wait
script's comments now carry dated 2026-09-25 corrections. They say the `ci.yml` holders are `python`
and `e2e-seeded`, that `test-db-drift` is ordered only by the schema-apply wait, and they give the
re-measured counts. Every older sentence is kept as lineage.

## Commit

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | Re-measure counts, write the dated addenda | 579ab9c33 (single plan commit) | the four files |
| 2 | Sweep, gates, commit | 579ab9c33 | the four files |

`579ab9c33 docs(164.4.2.1-03): holder-set currency after test-db-drift left the key` touches exactly
the four files (+89 / -4). The 4 deletions are three runbook lines split to insert a note, with their
text continuing unchanged after the note, and the one triage message line. Nothing was pushed.

## Re-measured at the plan-02 commit (b579f0110), before any edit

| Measure | Value | Plan-02 SUMMARY |
|---|---|---|
| `grep -c 61616158 ci.yml` (and `grep -o … \| wc -l`) | 17 / 17 | 17, agrees |
| per job: `python` / `e2e-seeded` / `test-db-drift` / `sql-tests` | 9 / 8 / 0 / 0 | test-db-drift 0, agrees |
| `supabase-migrate.yml` / `test-restore-from-baseline.yml` / `mutex-probe.yml` / `analytics-deploy-verify.yml` | 7 / 8 / 5 / 1 | n/a (they match the 2026-09-23 census, unchanged) |
| `ci.yml` jobs carrying `- name: Acquire shared-test-db mutex` | `python`, `e2e-seeded` | agrees (DB_JOBS) |
| wait self-test check count, before edit | 46 | 46, agrees |
| actionlint on `analytics-deploy-verify.yml`, before | no output, exit 0 | n/a |

## What changed

- **`CLAUDE.md`:** two ⛔ CORRECTED 2026-09-25 (Phase 164.4.2.1) notes.
  - Under the 2026-09-23 coupling (a) correction: the holders are now `python` and `e2e-seeded`, the
    Option A reason, the 17× count with its per-job split, and 7× in `supabase-migrate.yml`.
  - Under the `## SQL gate integrity jobs` correction: `test-db-drift` keeps the secret, the wait and
    `needs: python`, and no longer takes the mutex.
- **Runbook:**
  - Section 0 opens with a dated block giving the current state (two `ci.yml` holders, FOUR in total),
    the choice (Option A), its reason, the rejected B / C / C′ / D, and a pointer to
    `164.4.2.1-MEASUREMENT.md` with no numbers restated.
  - Dated notes were added beside: the section 1 acquire list, the "FIVE HOLDERS" heading, the
    `pg_stat_activity` indistinguishability note, the section 2 lock-time lineage, the section 2
    cap-exhaustion waiters, the 7.2 census and 7.3.
- **`analytics-deploy-verify.yml`:** the triage message's advisory-lock bullet now lists `python` /
  `e2e-seeded`. The parenthetical says `test-db-drift` held the key until Phase 164.4.2.1. Only that
  string changed.
- **`wait-for-test-schema-apply.sh`:** two comment blocks.
  - Under the header: the invokers are now `python`, `e2e-seeded` and `test-db-drift`, and the wait is
    `test-db-drift`'s only ordering control. This also records that `sql-tests` stopped invoking it,
    which the header had not said since Phase 164.4.2.
  - Under the "NOT A CLAIM THAT THIS WAS THE ONLY DEFENCE" comment: the mutex no longer covers
    `test-db-drift`, and its overlaps fail loudly, never green.

## Gates (verbatim)

Task 1 `<automated>` verify:
```
wait-for-test-schema-apply self-test OK (46 checks) — the DISPATCHER (all three outcomes named; a held flag beats both an absent and a concluded run) AND the two READS (probe_apply_state over fixture API bytes; the flag classification and the port rewrite). ⚠️ NOT covered: the psql call itself, which only a real TEST run exercises.
DOCS-CURRENCY-OK
```
- Script diff: no added or removed line other than `#` comments; the check printed nothing.
- Workflow diff: exactly 2 changed lines (one `-`, one `+`).
- actionlint after the edit: no output, exit 0.

Tests that read these files, found by
`git grep -l -e 'shared-test-db-mutex.md' -e 'analytics-deploy-verify' -e 'wait-for-test-schema-apply' -- src/__tests__`.
That found 4 test files plus `contracts/REGISTRY.md`, which is not a test. I added
`live-db-execution-ledger.contract.test.ts`, the one test that reads `CLAUDE.md` by path:
```
 Test Files  5 passed (5)
      Tests  358 passed (358)
exit=0
```
Hygiene gate after `git add`:
`[check-planning-hygiene] OK — 7078 tracked files scanned, none carry the local username or an absolute home path (rule 1 needle: derived at runtime from the home-directory basename).`
- Commit-message skip-token sweep: `SKIP-SWEEP-0`.
- Task 2 `<automated>` verify: `PLAN03-COMMIT-OK`.
- The `node_modules` symlink was removed before staging.
- `.planning/milestone.lock` and `.planning/state.json` were not staged.
- The commit deletes no file.

## Sweep: `git grep -n test-db-drift -- ':!.planning' ':!.github/workflows/ci.yml' ':!src/__tests__'`

Every hit falls into one of these groups:
- current;
- dated lineage with a 2026-09-25 correction beside it;
- historical (`CHANGELOG.md` release entries, which are records of past releases);
- unrelated to the key (`scripts/aggregator-tolerance-fixtures/MW02-current-spelling.green.yml`, which
  covers the aggregator row, and runbook section 4's fork-author `if:`, which is still true).

**Found but not owned (not edited, outside this plan's `files_modified`):**
- `TODOS.md` `[164.4.2-REMAINING-KEY-HOLDERS]` still reads, in the present tense, "`python`,
  `e2e-seeded` and now `test-db-drift` still hold the shared-TEST advisory key". This phase is the
  move that entry anticipated for `test-db-drift`. Whoever owns the TODOS entry, most likely the ship
  or verify step, should update or narrow it.
- `.github/workflows/ci.yml`, `test-db-drift`'s `Wait for the TEST schema apply to conclude (merge
  pushes only)` step: its `run:` body still says "The acquire step below installs psql conditionally
  too". That is stale for this job, which has no acquire step now (carry-over from plan 02). It was
  not changed because this plan forbids editing `ci.yml` and plan 02 pinned the step byte-identical by
  region sha256. Changing it needs a scoped ci.yml edit, with the region hash re-recorded.

## Deviations from Plan

- **Test list widened by one file.** The plan's `git grep` does not match `CLAUDE.md`, and
  `CLAUDE.md` is an edited file. `live-db-execution-ledger.contract.test.ts` reads it by path, so it
  was added to the vitest run.
- **One scratch file went outside the scratchpad.** The per-job census span files were written to
  `/tmp/x_*` and deleted in the same command. They held only workflow text, no secret.

Otherwise, the plan was executed as written.

## Known Stubs

None.

## Hand-off

At ship time, write the CHANGELOG entry and the VERSION bump in one commit (CLAUDE.md "CHANGELOG
discipline"). After the merge, filling the five-run AFTER in `164.4.2.1-MEASUREMENT.md` is a
verification obligation (D-05). It needs admissible merge-push runs, so it cannot be done before the
push.

## Self-Check: PASSED

- FOUND: all four modified files; commit `579ab9c33`; `git rev-list --count b579f0110..HEAD` = 1 at
  the time of writing.
