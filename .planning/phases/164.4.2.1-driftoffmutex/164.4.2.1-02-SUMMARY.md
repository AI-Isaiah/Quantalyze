---
phase: 164.4.2.1-driftoffmutex
plan: "02"
subsystem: ci-shared-test-db
tags: [ci, shared-test-db, vac-08, mutex, pins]
status: complete
requires:
  - 164.4.2.1-01 (MEASUREMENT committed at d04f580a2, an ancestor of this plan's commit)
provides:
  - test-db-drift runs VAC-08 holding no shared-test-db key
  - holder-set pin DB_JOBS = [python, e2e-seeded], a test-db-drift negative pin and an ordering pin
affects:
  - plan 03 (docs currency addenda read the re-measured counts below)
  - the post-merge AFTER measurement (D-05)
tech-stack:
  added: []
  patterns: [a read-only gate orders itself by fact (the wait step), not by lock]
key-files:
  created: []
  modified:
    - .github/workflows/ci.yml
    - src/__tests__/critical-regressions.test.ts
    - src/__tests__/mutex-dead-holder-verdict.test.ts
decisions:
  - "The holder detector is lifted into one measureHolders helper that both the holder-set it and the new test-db-drift negative call, so the two cannot disagree about what a holder is"
  - "Lineage sentence 'the \"Acquire shared-test-db mutex\" step below' in the test-db-drift block reworded to 'the shared-test-db mutex's \"Acquire\" step below', because the plan's own Task 1 verify requires 0 occurrences of that step name in the job span"
metrics:
  duration: 12m
  completed: 2026-09-25
actuals:
  tokens: 12845
  tasks: 3
  commits: 1
plan_head_before: c79e1e0c6baa75f1f4526bffcd4958ca6e6e031d
---

# Phase 164.4.2.1 Plan 02: test-db-drift leaves the shared-test-db key Summary

`test-db-drift` no longer holds or waits on the shared-test-db advisory key. Its three mutex steps
are deleted, the key number no longer appears anywhere in the job, and the pins that measured it as
a holder were moved in the same commit. Each new or moved pin was seen RED under its own neuter.
Everything VAC-08 depends on is byte-identical: the ordering wait, the VAC-08 step, `needs`, `if`,
`timeout-minutes` and `permissions`.

## Commit

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | Delete the three mutex steps and make the re-measured pins green | 15d8bfd77 (single plan commit) | ci.yml, both tests |
| 2 | Comment currency, dated, lineage kept | 15d8bfd77 | ci.yml, critical-regressions.test.ts |
| 3 | Neuter every pin, run every gate, commit once | 15d8bfd77 | the same three files |

`15d8bfd77 ci(164.4.2.1-02): test-db-drift stops waiting on the shared-test-db key` touches
exactly the three files (+167 / -330). Nothing was pushed.

## What changed

- **`ci.yml` `test-db-drift`:** deleted `Acquire shared-test-db mutex`,
  `Release shared-test-db mutex (best effort)` and `Verdict — shared-test-db mutex holder died mid-job`,
  with their comment blocks. These were the two removed hunks (220 and 88 lines). No other
  non-comment line was added or removed; `git diff -U0` shows 0 added non-comment lines.
  - The four key literals in the job's comments now read "the shared-test-db key" (D-08).
  - Dated 2026-09-25 notes were added: a ⛔ CORRECTED header at the top of the job, the TTL
    comment, above the wait step, and in the VAC-08 block.
- **`python` / `e2e-seeded`:** comment-only dated notes. "Third taker", "all three", "identical in
  test-db-drift…" and "copy of the same verdict step at the test-db-drift site" now say two holders.
  Pointers to deleted `test-db-drift` steps now name the surviving twin in the other job by step
  name. No line inside an acquire or release step changed, and no key literal was added.
- **`critical-regressions.test.ts`:**
  - `DB_JOBS = ["python", "e2e-seeded"]`.
  - New `it("test-db-drift holds and names no shared-test-db key (Phase 164.4.2.1 D-02)")`.
  - New `it("test-db-drift runs the ordering wait BEFORE VAC-08 and keeps its TTL and needs: python …")`
    with 9 distinct messages.
  - Per-holder titles changed from "three" to "every holder"; no assertion logic changed.
  - A dated re-subjection paragraph was added to the describe's leading comment.
- **`mutex-dead-holder-verdict.test.ts` Test 2:** the floor went 5 → 4 in the argument, the message
  and the title, with a dated D-07 reason.

## Measured values

| Measure | Value |
|---|---|
| Key-number matches in ci.yml, before (`grep -o … \| wc -l`) | 29 |
| …of which inside `test-db-drift`, before | 12 |
| Key-number matches in ci.yml, after | **17** (= 29 − 12, equals the prediction) |
| Key-number matches in `test-db-drift`, after | 0 |
| `- name: Release shared-test-db mutex (best effort)` count | 2 (the lane drill extracts the first, `python`'s) |
| actionlint, before / after | 0 findings / 0 findings (exit 0 both) |

Region sha256 values of `test-db-drift`, identical before and after (first 16 hex digits):
```
needs       c08420d8d4ad4d66
if          6938d16ac4961e6c
timeout     9b33a307b83143a9
permissions 4dec1d782f3524f0
wait        67e5ecff9d7a2452   (16 lines, name line through run body)
vac08       5a1c012fba4299fa   (7 lines)
REGIONS-IDENTICAL
```

## Neuter → RED → restore (every restore `cmp`-proven against a `mktemp -d` backup)

| Neuter | RED `it` | First line of its message |
|---|---|---|
| 1a comment `- name: Acquire shared-test-db mutex` in test-db-drift | holder-set it AND `test-db-drift holds and names no shared-test-db key (Phase 164.4.2.1 D-02)` (2 failed) | "…jobs that hold (or name) shared-test-db key 61616158 are [e2e-seeded, python, test-db-drift], but DB_JOBS pins [e2e-seeded, python]…" / "ci.yml test-db-drift acquires (or names) shared-test-db key 61616158 again — Phase 164.4.2.1 D-02 took it off the key…" |
| 1b comment carrying the key number in test-db-drift | the same two `it`s | the same two messages |
| 1c the key number in `changed-paths` | ONLY the holder-set it (1 failed; the test-db-drift negative stayed GREEN) | "…are [changed-paths, e2e-seeded, python], but DB_JOBS pins [e2e-seeded, python]…" |
| 2a wait step moved after VAC-08 | ordering it | "ci.yml test-db-drift runs VAC-08 BEFORE the ordering wait…" |
| 2b wait step deleted | ordering it | "ci.yml test-db-drift lost its `Wait for the TEST schema apply…` step…" |
| 2c wait `if:` → `github.event_name == 'never'` | ordering it | "…ordering wait no longer carries its merge-push `if:` exactly…" |
| 2d invocation → `wait-for-test-schema-apply-x.sh` | ordering it | "…ordering wait step no longer invokes scripts/wait-for-test-schema-apply.sh…" |
| 2e `bash scripts/test-ledger-drift-check-x.sh` | ordering it | "…VAC-08 step no longer runs `bash scripts/test-ledger-drift-check.sh` bare…" |
| 2f `timeout-minutes: 120` | ordering it | "ci.yml test-db-drift lost (or changed) `timeout-minutes: 90`…" |
| 2g `- python` removed from needs | ordering it | "ci.yml test-db-drift dropped `needs: python`…" |
| 2h `- changed-paths` removed from needs | ordering it | "ci.yml test-db-drift dropped `needs: changed-paths`…" |
| 3 Test 2 floor back to 5 | `Test 2: at least 4 dead-holder annotations …` | "aggregate dead-holder annotation count across the three files is 4 …: expected 4 to be greater than or equal to 5". The measured corpus is 4. |

Each ordering neuter fired only its own assertion's message. Every restore printed `restore cmp OK`.

## Gates (verbatim)

All 17 files that reference `workflows/ci.yml` (`git grep -l 'workflows/ci.yml' -- src/__tests__`,
re-measured, 17):
```
exit=0
files=17 tests=1469 passed=1452 failed=0 pending=17 todo=0
```
⚠️ The 17 pending tests are env-gated `skipIf`s and not skips added here: 16 in `retention-crons.test.ts`
(`!HAS_LIVE_DB || !HAS_INTROSPECTION`; no live DB is used locally, per the constraints) and 1 in
`gitleaks-allowlist.test.ts` (the `skipIf(HAS_GITLEAKS)` twin). `critical-regressions.test.ts`: 168
passed. Quick run (two files): `Tests 186 passed (186)`.

Self-tests:
```
VAC-08 repo-vs-TEST ledger and body drift gate: self-test OK (13/13 arms — every red mode fires, all FOUR green paths pass, and the harness is calibrated).
wait-for-test-schema-apply self-test OK (46 checks) — the DISPATCHER (all three outcomes named; a held flag beats both an absent and a concluded run) AND the two READS (probe_apply_state over fixture API bytes; the flag classification and the port rewrite). ⚠️ NOT covered: the psql call itself, which only a real TEST run exercises.
mutex-dead-holder-verdict self-test OK (5 checks)
lint-sql-gates self-test OK: 7 rules, red+green each.
```
`actionlint .github/workflows/ci.yml`: no output, exit 0.

Task 3 `<automated>` verify: `PLAN02-COMMIT-OK`. Hygiene gate after `git add`:
`[check-planning-hygiene] OK — 7077 tracked files scanned, none carry the local username or an absolute home path …`.
Commit-message skip-token sweep: `SKIP-SWEEP-0`. The `node_modules` symlink was removed before
staging. `.planning/milestone.lock` and `.planning/state.json` were not staged, and the commit
deletes no file.

## Deviations from Plan

**1. [Rule 3 - Blocking] One lineage phrase reworded so the plan's own Task 1 verify can pass**
- **Found during:** Task 1
- **Issue:** The ⛔ lineage block in `test-db-drift` said `the "Acquire shared-test-db mutex" step below`.
  Task 1's verify requires 0 occurrences of `Acquire shared-test-db mutex` in the job span, while
  D-08 keeps the block as lineage.
- **Fix:** Reworded it to `the shared-test-db mutex's "Acquire" step below`. No other word changed.
  The new ⛔ CORRECTED header marks the whole block as lineage.
- **Commit:** 15d8bfd77

**2. [Structure] One shared detector, not an inline copy**
- To satisfy "reuse the same detection expression; do not write a second detector", the holder
  detection was lifted verbatim into a `measureHolders` helper in the describe. Both the holder-set
  `it` and the new negative `it` call it. The regex and body-slicing logic are unchanged.

**3. [Environment] Branch-namespace check not applicable**
- This checkout is the orchestrator's dedicated worktree on `feat/164.4.2.1-driftoffmutex`, not an
  `agent-*` branch. The orchestrator named that branch explicitly, and it is not a protected branch.
  The commit was made there as instructed.

## Known and deliberately left

- The wait step's `run:` body still says "The acquire step below installs psql conditionally too".
  That is stale for `test-db-drift`, but the step must stay byte-identical (its sha256 region is
  pinned above), so the line was left as it is, as the plan specified.
- Phrases that are still true were not changed: "These three jobs' `if:` conditions diverge" and
  "IN ALL THREE SITES". They count the three jobs that carry the ordering wait, and `test-db-drift`
  still carries it.

## Known Stubs

None.

## Self-Check: PASSED

- FOUND: `.github/workflows/ci.yml`, `src/__tests__/critical-regressions.test.ts`, `src/__tests__/mutex-dead-holder-verdict.test.ts` (modified)
- FOUND: commit `15d8bfd77`, and `git rev-list --count c79e1e0c6..15d8bfd77` = 1
