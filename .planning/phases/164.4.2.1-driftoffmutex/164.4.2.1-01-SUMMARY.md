---
phase: 164.4.2.1-driftoffmutex
plan: "01"
subsystem: ci-measurement
tags: [measurement, sc-3, ci, shared-test-db, vac-08]
status: complete
requires:
  - 164.4.2-MEASUREMENT.md (## Recipe, ## BEFORE table, ### Verdict rule, Runs 1-5)
provides:
  - 164.4.2.1-MEASUREMENT.md, the BEFORE, the empty AFTER protocol, the clause texts and the prediction, committed before any ci.yml change
affects:
  - plan 02 (its ci.yml commit must descend from d04f580a2)
  - the phase VERIFICATION (may not read passed before five admissible runs are graded)
tech-stack:
  added: []
  patterns: [decided-before-built measurement (164.4.2 Decision-E discipline)]
key-files:
  created:
    - .planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md
  modified: []
decisions:
  - "Clause (c) ceiling fixed at 8m25s (7m00s + 1m25s spread); both inputs re-read from the 164.4.2 BEFORE table and Verdict rule, and they agree with the plan"
  - "The overlapped-a-sibling column is recorded for every run, but the degenerate classification stays 164.4.2's python-acquire-wait rule, so both phases are graded on one criterion"
metrics:
  duration: 2m
  completed: 2026-09-25
actuals:
  tokens: 4200
  tasks: 2
  commits: 1
plan_head_before: 844a39953003c0ab6f38093f377d9bc1c5983761
---

# Phase 164.4.2.1 Plan 01: Measurement protocol and prediction Summary

The speed claim for DRIFTOFFMUTEX can now be refuted from run ids alone. It is committed as
`164.4.2.1-MEASUREMENT.md` at `d04f580a2`, before plan 02 touches `ci.yml`. The file carries:

- the BEFORE bands, quoted by heading from 164.4.2's file
- 164.4.2's five AFTER runs, the ones this phase exists to fix
- clause (a) graded on the founder's Goal reading (D-01), with the strict reading recorded for Phase 164.9
- a non-vacuous clause (c) with a fixed 8m25s ceiling (D-06)
- a separate migration-push stratum
- the prediction, quoted verbatim from the research

## What was built

| Task | Name | Commit | Files |
|---|---|---|---|
| 1 | Write 164.4.2.1-MEASUREMENT.md end to end | d04f580a2 (with Task 2) | `.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md` |
| 2 | Hygiene-check and commit before any code | d04f580a2 | the same single file |

The file has eight sections, in the plan's order: `## Recipe`, `## BEFORE`, `## Which runs
count`, `## Empty AFTER table`, `## Verdict rule`, `## Prediction — decided before the change` and
`## Obligation`, after a header that quotes D-01's "Judge by the goal" answer.

### Numbers re-read from the source (not copied from the plan)

- 164.4.2 `## BEFORE table`, non-degenerate runs, `python` + `e2e-seeded` acquire-wait: 7m00s,
  6m55s, 5m35s, 6m25s. The range is 5m35s–7m00s, which matches the `### Verdict rule` sentence.
  From that, spread = **1m25s** and ceiling = **8m25s**, the same values the plan carried, so
  nothing was recomputed.
- Bands: run-total 16m18s–18m50s (full spread 16m18s–62m20s); three-holder combined wait
  12m05s–14m56s; `sql-tests` job-total 8m31s–9m23s.
- 164.4.2 AFTER runs `35939061930`, `35943402509`, `35943407413`, `35957479474`, `35958026743`:
  run-total, degenerate flag, overlap and `test-db-drift` acquire-wait (6m31s, 20m21s, 14m06s,
  20m31s, 0m05s), quoted per run.

## Verification (gate output verbatim)

Task 1 shape check:
```
MEASUREMENT-SHAPE-OK
```

Task 2 hygiene gate (main checkout's `tsx`) and commit shape:
```
[check-planning-hygiene] OK — 7076 tracked files scanned, none carry the local username or an absolute home path (rule 1 needle: derived at runtime from the home-directory basename).
exit=0
COMMITTED-OK
```
The skip-token sweep of the commit message printed `SKIP-SWEEP-0`. `git diff --cached --name-only`
listed exactly the MEASUREMENT file. `.planning/milestone.lock` and `.planning/state.json` were not
staged, and no file was deleted.

## Deviations from Plan

None. The plan was executed as written. One detail: the 164.4.2 file does not record an overlap
for its Run 1, so that cell reads "none recorded" rather than "no". It was not re-measured,
because this plan makes no network calls.

## Not done here (by design)

- The AFTER table is empty. Filling it is a post-merge verification obligation (D-05), not a
  plan task.
- `STATE.md` and `ROADMAP.md` were not touched, and no gsd-tools state or roadmap handler was run
  (orchestrator constraint).
- Nothing was pushed.

## Known Stubs

None. The empty AFTER cells are the protocol's intended shape and are marked `PROTOCOL ONLY`.

## Self-Check: PASSED

- FOUND: `.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md`
- FOUND: commit `d04f580a2`
