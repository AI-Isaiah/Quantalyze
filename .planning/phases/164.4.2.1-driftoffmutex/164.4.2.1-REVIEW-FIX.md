---
phase: 164.4.2.1-driftoffmutex
fixed_at: 2026-09-25T00:00:00Z
review_path: .planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-REVIEW.md (+ 164.4.2.1-REVIEW-SFH.md)
iteration: 1
note: round 1 was split across topic fixers that append their own sections below; the counts in each section are that topic's only.
---

# Phase 164.4.2.1: Code Review Fix Report (round 1)

**Source reviews:** `164.4.2.1-REVIEW.md` (gsd-code-reviewer) and `164.4.2.1-REVIEW-SFH.md` (silent-failure-hunter).
**Iteration:** 1

## Round 1 — Topic B (docs, scripts, messages)

**Scope:** `CLAUDE.md`, `docs/runbooks/shared-test-db-mutex.md`, `TODOS.md`, `scripts/wait-for-test-schema-apply.sh` (comments), `scripts/test-ledger-drift-check.sh` (two failure messages), `scripts/restore-test-from-baseline.sh` (the `RESTORE_REQUIRE_MUTEX` refusal text only), `.github/workflows/test-restore-from-baseline.yml` and `.github/workflows/analytics-deploy-verify.yml` (comments and message strings only). `ci.yml` and the two regression test files belong to the parallel topic fixer and were not touched.

**Summary:**
- Findings in scope: 6 (SFH-01 / WR-01 as one, SFH-04, SFH-05, WR-03, IN-03, IN-06). SFH-06 is folded into the SFH-01 text, as its recommendation asks.
- Fixed: 6
- Skipped: 0

Every correction uses the dated `⛔ CORRECTED 2026-09-25` lineage style and keeps the old sentence. Citations are by symbol.

### SFH-01 / WR-01: PR-run VAC-08 has no ordering against a TEST writer

**Files modified:** `docs/runbooks/shared-test-db-mutex.md`, `CLAUDE.md`, `scripts/wait-for-test-schema-apply.sh`, `scripts/test-ledger-drift-check.sh`
**Commit:** f180241cd
**Applied fix:**
- Runbook section 0 has a dated correction under "The choice, and its reason". It says "The key bought it nothing" holds for merge pushes only. On a `pull_request` run the wait does not run, so VAC-08 has no ordering against `apply-test` or a dispatched restore. It names the three new reds and the merge-push TOCTOU window (SFH-06), says why none turns a real drift green, and gives the triage: re-run the check once both writers are idle, and do not widen the wait's `if:`. Section 7.3's "the ONLY ordering control" gets the same caveat.
- `CLAUDE.md`: both 2026-09-25 notes (half (a) and the VAC-08 note) get a one-sentence PR-event caveat.
- The wait script's header and its `wait_for_apply` note both get the PR-event correction.
- In `check()` in `scripts/test-ledger-drift-check.sh`, the ledger-presence `fail` and the `FRONTIER_EXEMPT_CEILING exceeded` message now name "a TEST writer (apply-test or a restore) was running concurrently" as a possible cause, with the re-run advice. The substrings the self-test and vitest assert (`FRONTIER_EXEMPT_CEILING exceeded`, `ledger presence query failed`) are unchanged.

### SFH-05: the wait-script note overstated how an overlap resolves

**Files modified:** `scripts/wait-for-test-schema-apply.sh`
**Commit:** 2f5549f8b
**Applied fix:** A dated correction in `wait_for_apply` replaces "fail loudly, never pass falsely" and "blocks them until COMMIT", keeping both as lineage. The restore takes ACCESS EXCLUSIVE on the ledger only at its late `TRUNCATE`. Before that point, reads see the pre-restore committed state and can pass, and an apply overlap can pass through the frontier exemption. The real outcomes are stated: an overlap either reads a committed state that really existed, or fails loudly. It cannot pass on a state that never committed.

### SFH-04: two restore safety texts denied the read-only exception

**Files modified:** `.github/workflows/test-restore-from-baseline.yml`, `scripts/restore-test-from-baseline.sh`
**Commit:** 72d12cf9c
**Applied fix:**
- THE ACTIVITY GATE comment has a dated correction beside "There is no window". `test-db-drift` is the one job the held mutex no longer keeps out. This is accepted because it only reads, and every writer still holds the mutex.
- The `RESTORE_REQUIRE_MUTEX` refusal now says the mutex keeps other CI *writers* out, and names the read-only exception. The substring that the self-test's arm (d) greps (`would bypass the shared-TEST mutex check, and this is --mode restore`) is unchanged, so the pg-lane restore self-test was not re-run.

### WR-03: TODOS `[164.4.2-REMAINING-KEY-HOLDERS]` listed `test-db-drift` as a holder

**Files modified:** `TODOS.md`
**Commit:** 92d3499d7
**Applied fix:** The title now names `python` and `e2e-seeded`, with "(`test-db-drift` left 2026-09-25)". A dated correction keeps the old title wording as lineage and points at `164.4.2.1-MEASUREMENT.md` for the AFTER table.

### IN-03: the NOT-ANNOUNCED restore error's reader list was stale

**Files modified:** `.github/workflows/test-restore-from-baseline.yml`
**Commit:** a72f2d809
**Applied fix:** The could-not-announce `::error::` now names `python` and `e2e-seeded` as the readers that queue into the DROP SCHEMA, plus a merge-push `test-db-drift` as a keyless reader. A dated comment in the `*)` case branch keeps the old wording ("sql-tests, python or e2e-seeded") as lineage. It also notes that `test-db-drift` reads without queueing and does not consult the flag on a PR.

### IN-06: the deploy-verify issue text did not say what reds `test-db-drift`

**Files modified:** `.github/workflows/analytics-deploy-verify.yml`
**Commit:** 003dd466d
**Applied fix:** The advisory-lock bullet adds: "A red `test-db-drift` is its ordering wait (`wait-outcome: wait-exhausted`) or VAC-08 itself, never the lock (runbook 7.3)."

### Verification (Topic B)

**Where the gates ran:** in the pinned phase checkout (the main working tree of this branch), not an isolated fixer worktree. `node_modules` was symlinked from the primary checkout and removed before each commit.

- `bash scripts/wait-for-test-schema-apply.sh --self-test`: `self-test OK (46 checks)`, after each of the two edits to the script.
- `bash scripts/test-ledger-drift-check.sh --self-test`: `self-test OK (13/13 arms — every red mode fires, all FOUR green paths pass, and the harness is calibrated)`.
- `bash -n` on each of the three touched shell scripts: clean.
- `actionlint`: `test-restore-from-baseline.yml` reports the same 4 shellcheck `info` findings as the committed HEAD copy, and none are new. `analytics-deploy-verify.yml` reports 0 findings, both before and after the edit.
- vitest over every test that reads a touched file:
  - 19 files / 1056 tests passed. These are the readers of `CLAUDE.md`, the VAC-08 script and the wait script, including `live-db-execution-ledger.contract.test.ts`, `drift-check-scripts`, `vac08-ledger-dispositions`, `gate-family-meta` and `critical-regressions`.
  - 8 files / 297 tests passed. These are the readers of `TODOS.md`.
  - Readers of the restore script and the two workflows: 8 of 9 files passed (417 of 418 tests). The one failure is `test-restore-workflow-wiring.test.ts` › "no scanned file feeds a raw index into a narrowing call". It flags `src/__tests__/critical-regressions.test.ts` at `const dir = path.slice(0, path.lastIndexOf("/"))`. That line is in the parallel topic fixer's **uncommitted** edit to `critical-regressions.test.ts`: it is absent from HEAD, and it is present in the working-tree diff. This is not caused by Topic B, and the owning fixer needs to route that index through a checked helper.
- `check-planning-hygiene`: OK before every commit.

**Not changed, and outside this topic's file list:**
- The `A5` comment above the refusal in `restore-test-from-baseline.sh` ("the ONE thing keeping other people's CI out") carries the same premise as the refusal text. The brief limited this topic to the refusal text.
- The describe-block comment in `src/__tests__/test-restore-workflow-wiring.test.ts` repeats "There is no window".

## Round 1 — Topic A (ci.yml + regression pins)

**Scope:** `.github/workflows/ci.yml`, `src/__tests__/critical-regressions.test.ts`, `src/__tests__/mutex-dead-holder-verdict.test.ts`. The docs, `TODOS.md`, the scripts and the other workflows belong to Topic B and were not touched.

**Summary:**
- Findings in scope: 8. They are WR-02, WR-04, WR-05 and SFH-03 (counted as one), SFH-02 and IN-05 (one), IN-01, IN-04, IN-02, and the `ci.yml` half of SFH-01, which covers SFH-06.
- Fixed: 8
- Skipped: 0

### WR-02: the wait step named an acquire step that no longer follows it, and carried a dead psql install

**Files modified:** `.github/workflows/ci.yml`
**Commit:** 244a1cde4
**Applied fix:** This change touches only `test-db-drift`'s copy of the `Wait for the TEST schema apply to conclude (merge pushes only)` run body. The "acquire step below installs psql conditionally" comment and the conditional `apt-get` block are removed. A dated note replaces them: the job's unconditional `Install psql client` step already runs before this one. The `python` and `e2e-seeded` copies are unchanged, because the sentence is still true there. No test pins a region hash of this step (checked by grep across `src/__tests__` and `scripts`), so there was no hash to re-record.

### WR-04: the job's CORRECTED header said the wait "already ran BEFORE the acquire"

**Files modified:** `.github/workflows/ci.yml`
**Commit:** 244a1cde4
**Applied fix:** The header now reads "which runs immediately before VAC-08".

### SFH-01 (ci.yml half) and SFH-06: the ci.yml comments said nothing about PR runs

**Files modified:** `.github/workflows/ci.yml`
**Commit:** 244a1cde4
**Applied fix:**
- The `test-db-drift` CORRECTED header now has a ⚠️ note. On a `pull_request` run the wait does not run, so VAC-08 has no ordering against `apply-test` or a dispatched restore. On a merge push, a window of a few seconds remains between the wait's exit and VAC-08's first read. The note gives the reason this is accepted (an overlap can only go loudly red), points to runbook section 0, and forbids widening the wait's `if:` to PRs.
- The wait step's 2026-09-25 parenthetical now says the pin holds the wait fail-closed, and that on a PR there is no ordering control at all.
- The wait's `if:` is unchanged.

### WR-05 / SFH-03: the ordering pin did not prove the wait fails closed

**Files modified:** `src/__tests__/critical-regressions.test.ts`
**Commit:** b3e86e58e
**Applied fix:** The `it` named "test-db-drift runs the ordering wait BEFORE VAC-08 …" now asserts all of the following:
- There is no `continue-on-error` on the wait step, on the VAC-08 step, or at job level.
- Both run bodies contain `set -euo pipefail`.
- The wait invocation is anchored end-of-line, so `|| true` fails the pin.
- No command follows either invocation. A `run:` block's exit status is its last command's.
The step-level `continue-on-error` checks sit at 8-space indent, which is where step keys live.

### IN-01: the TTL reason assumed an acquire cap that no longer exists, and VAC-08's connects were unbounded

**Files modified:** `.github/workflows/ci.yml`, `src/__tests__/critical-regressions.test.ts`
**Commit:** b3e86e58e
**Applied fix:**
- The VAC-08 step's `env:` now sets `PGCONNECT_TIMEOUT: "15"`. libpq reads it from the environment, so every psql call in `scripts/test-ledger-drift-check.sh` is bounded without editing the script, which belongs to Topic B.
- 15 seconds is the same value the mutex acquire and the ordering-wait probe already use.
- The TTL comment keeps its old derivation as lineage. It adds a "WHAT 90m NOW BOUNDS" paragraph: setup, plus `WAIT_BUDGET_SECONDS` (900 s), plus VAC-08. VAC-08's statements have a 30 s `statement_timeout` and a `LEDGER_QUERY_ATTEMPT_BUDGET`, and its connects are now bounded. That sums to well under 30 minutes.
- `timeout-minutes: 90` is unchanged (D-04).
- The ordering pin asserts the connect bound.

### IN-04: the `python` and `e2e-seeded` pointers to the mutex note

**Files modified:** `.github/workflows/ci.yml`
**Commit:** 244a1cde4
**Applied fix:** Every pointer was read and each one is still accurate, because the ⛔ block stays in `test-db-drift` as lineage (D-08). The lineage block now opens with a dated line: for a reader arriving from `python` or `e2e-seeded`, the "Acquire" step and "this job" it describes are theirs, and its ⛔ constraints still bind them. The block was not moved; D-08 keeps it in place.

### SFH-02 / IN-05: `measureHolders` saw only the literal step name or the literal key

**Files modified:** `src/__tests__/critical-regressions.test.ts`
**Commit:** 6806e1c9e
**Status:** fixed: requires human verification. This is detector logic: a wrong rule would either miss a holder or red on a job that holds nothing.
**Applied fix:** In a job's own YAML, any of the following marks the job as a holder:
- the literal key;
- any `Acquire…mutex` step;
- a `MUTEX_KEY` assignment;
- the mutex's PGAPPNAME;
- any advisory-lock call other than one on a different numeric literal. A call on a variable argument counts.

The detector also walks every `scripts/*.sh` on the job's non-comment lines, plus the files those scripts `.`/`source`. Inside a script, only these count as taking the key:
- `MUTEX_KEY=`;
- the mutex's PGAPPNAME;
- an advisory-lock call on the key literal or on `MUTEX_KEY`.

A mention does not count as a take, and that distinction is load-bearing:
- The wait's sourced `shared-test-db-keys.sh` names the key as a comparison operand and try-locks the in-flight flag key.
- `test-ledger-drift-check.sh` names the key in a comment.

`scripts/pg-lane/` and `scripts/local-stack/` are exempt because each runs a private cluster on the runner. The pg-lane drill `mutex-dead-holder-lane.sh` takes the literal key on purpose; without the exemption, `sql-mutation` reads as a holder.

A new calibration `it` runs the detector on synthetic jobs that call the repo's real scripts:
- Must be seen: four spellings, plus a key taken only through a sourced file.
- Must not be seen: `test-db-drift`'s two real scripts, a commented-out invocation, and the pg-lane drill.

The Topic B report flagged the raw-index directory computation. It now uses `posix.dirname`, and `test-restore-workflow-wiring.test.ts` passes (117/117).

### IN-02: the dead-holder floor comment

**Files modified:** `src/__tests__/mutex-dead-holder-verdict.test.ts`
**Commit:** 3bfd6d12e
**Applied fix:** This is a comment-only change. It records the per-file `grep -c` reading (ci.yml 2, supabase-migrate.yml 1, test-restore-from-baseline.yml 1 = 4), notes that the floor has zero slack, and says the next move must be re-measured the same way. No assertion changed.

### Neuter → RED → restore (every new guard)

Each neuter was applied to the fixed file. The test run then went RED with the named message, and the file was restored with `cp` and confirmed byte-identical with `cmp`.

| Neuter | RED in | Message fragment |
|---|---|---|
| `continue-on-error: true` on the wait step | ordering `it` | "ordering wait carries continue-on-error" |
| `\|\| true` after the wait invocation | ordering `it` | "invocation is no longer bare" |
| `echo done` after the wait invocation | ordering `it` | "runs a command AFTER scripts/wait-for-test-schema-apply.sh" |
| `set -euo pipefail` dropped from the wait step | ordering `it` | "ordering wait lost `set -euo pipefail`" |
| `continue-on-error: true` on VAC-08 | ordering `it` | "VAC-08 step carries continue-on-error" |
| job-level `continue-on-error: true` | ordering `it` | "carries a JOB-level continue-on-error" |
| `PGCONNECT_TIMEOUT` dropped | ordering `it` | "VAC-08 step lost `PGCONNECT_TIMEOUT`" |
| `echo done` after the VAC-08 invocation | ordering `it` | "runs a command AFTER scripts/test-ledger-drift-check.sh" |
| `set -euo pipefail` dropped from VAC-08 | ordering `it` | "VAC-08 step lost `set -euo pipefail`" |
| `test-db-drift` step running `test-only-normalize-analytics-url.sh` | holder-set pin **and** D-02 negative | "are [e2e-seeded, python, test-db-drift]" / "test-db-drift acquires (or names)" |
| script walk disabled | calibration `it` | "missed a job that runs scripts/test-only-normalize-analytics-url.sh" |
| private-lane exemption removed | calibration `it` **and** holder-set pin | "counted a private-lane drill" / "are [e2e-seeded, python, sql-mutation]" |
| sourced files not followed (re-run after the `posix.dirname` change) | calibration `it` | "does not follow a `.`-sourced helper" |
| a mention treated as a take | calibration `it`, holder-set pin, D-02 negative | "counted test-db-drift's two REAL scripts" / "are [e2e-seeded, python, sql-gate-lint, test-db-drift]" |

### Verification (Topic A)

**Where the gates ran:** in the pinned phase checkout (this branch's main working tree), not an isolated fixer worktree. `node_modules` was symlinked from the primary checkout and removed before each commit.

While the loop was running, Topic B removed the shared symlink for its own commit. One vitest run then created a real `node_modules/.vite` cache directory, which I deleted before re-linking. That run's result was discarded and re-run.

- vitest was run once per file over every test that reads `ci.yml` (29 files, found with `grep -rl 'ci\.yml' src/__tests__`), plus `test-restore-workflow-wiring.test.ts`. Every file passed. Selected counts: `critical-regressions` 169, `mutex-dead-holder-verdict` 18, `test-restore-workflow-wiring` 117, `drift-check-scripts` 644, `supabase-migrate-test-first` 33, `mutation-runner-floors` 99, `local-stack-lane-wiring` 56, `prod-prober-wiring` 110, `lint-sql-gates` 74, and all six `contracts/*` files. The skips are unchanged: `gitleaks-allowlist` 1 skipped, `gdpr-export` 1 skipped, `retention-crons` 16 skipped.
- Each intermediate commit was re-tested at its own content: C1 186/186, C2 303/303 (three files), C3 304/304 (three files), C4 18/18.
- `actionlint .github/workflows/ci.yml` exited with rc=0.
- `bash scripts/wait-for-test-schema-apply.sh --self-test` reported `self-test OK (46 checks)`.
- `npx tsc --noEmit -p .` reported no errors in either touched test file.
- `grep -c 61616158 .github/workflows/ci.yml` is still 17, so the CLAUDE.md and runbook counts are unaffected.
- `check-planning-hygiene` reported OK before every commit, and the skip-token sweep came back as 0 on every message file.
