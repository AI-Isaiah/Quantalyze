---
phase: "164.4.2.1"
slug: "driftoffmutex"
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
status: draft
nyquist_compliant: true
wave_0_complete: false
created: "2026-09-25"
---

# Phase 164.4.2.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Sourced from `164.4.2.1-RESEARCH.md` `## Validation Architecture`, mapped onto the three plans as
> revised after plan-check (planning head `42dba0e48`). Every count a self-test prints is read off the
> run at execution time; none is restated here as a gate.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest (`package.json` `"test": "vitest run"`) plus the offline bash `--self-test` harnesses of `scripts/test-ledger-drift-check.sh`, `scripts/wait-for-test-schema-apply.sh` and `scripts/mutex-dead-holder-verdict.sh` |
| **Config file** | `vitest.config.ts` |
| **Quick run command** | `node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts src/__tests__/mutex-dead-holder-verdict.test.ts` (with the `node_modules` symlink described in plan 02's context; the run must report a non-zero test count) |
| **Full suite command** | `node_modules/.bin/vitest run $(git grep -l 'workflows/ci.yml' -- src/__tests__)` plus the three self-tests, `node scripts/lint-sql-gates.mjs --self-test` and `actionlint`; `npm test` and CI on the PR, SHA-bound and counted |
| **Estimated runtime** | ~60 seconds for the quick run; a few minutes for the full list |

---

## Sampling Rate

- **After every task:** the task's own `<automated>` verify (below). Plan 02's Tasks 1 and 2 do not commit; their verify still runs after each.
- **After every plan wave:** wave 1 — the MEASUREMENT shape check and the hygiene gate; wave 2 — the full suite command; wave 3 — the tests that read the four edited docs plus the hygiene gate.
- **Before `/gsd-verify-work`:** full suite green and CI on the PR green, bound to the head SHA with the run count checked.
- **SC-3 (speed):** sampled only AFTER the merge, from five admissible merge-push runs (D-05). It is a verification obligation, not a task, and VERIFICATION may not read `passed` before it is filled.
- **Max feedback latency:** 60 seconds per task verify.

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 164.4.2.1-01-01 | 01 | 1 | SC-3 (D-01, D-05, D-06) | T-164.4.2.1-02 | Prediction, clause texts and the fixed clause (c) ceiling (8m25s) exist before any `ci.yml` change | doc-shape | Plan 01 Task 1 `<automated>`: heading loop + `grep -q` for `Judge by the goal`, `16m18s`, `12m05s`, `1m25s`, `8m25s`, `by construction`, `supabase-migrate.yml` and the five 164.4.2 run ids | ❌ created by this task | ⬜ pending |
| 164.4.2.1-01-02 | 01 | 1 | SC-3 (D-05) | T-164.4.2.1-01 | No identifier in a tracked public file; the measurement commit is an ancestor of the code commit | hygiene + commit-shape | `"$MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts` and `git log -1 --name-only` equals the MEASUREMENT path | ✅ | ⬜ pending |
| 164.4.2.1-02-01 | 02 | 2 | SC-1, SC-2 (D-02, D-07, D-08) | T-164.4.2.1-03 | `test-db-drift` has no mutex step and no key literal; holder set is `python` + `e2e-seeded`; wait precedes VAC-08 and is not disabled | unit (source-shape) | awk job-span counts `= 0` for the key and the three step names, then the quick run command | ✅ files; ❌ new negative `it` and ordering `it` (created in this task) | ⬜ pending |
| 164.4.2.1-02-02 | 02 | 2 | SC-1, SC-4 (D-02, D-04, D-08) | T-164.4.2.1-04 | Comments only; no test title still claims three holders; YAML unchanged beyond Task 1 | unit + diff-shape | job-span key count `= 0`, `grep -q 'Phase 164.4.2.1'` on `ci.yml`, no `it("all three`/`it("the three` title, then the quick run command | ✅ | ⬜ pending |
| 164.4.2.1-02-03 | 02 | 2 | SC-1, SC-2, SC-4 (D-07) | T-164.4.2.1-06, T-164.4.2.1-05 | Every new or moved pin observed RED under its own neuter (holder pins 1a-1c, ordering pins 2a-2h, floor 3), restore `cmp`-proven; single commit; key count = baseline minus the job's count | neuter/restore + commit-shape + self-tests | Plan 02 Task 3 `<automated>` (file set of the one commit, `HEAD^` key arithmetic, the three `--self-test`s); the neuter REDs are recorded in the SUMMARY | ✅ | ⬜ pending |
| 164.4.2.1-03-01 | 03 | 3 | SC-1, SC-2 (D-02, D-09) | T-164.4.2.1-08, T-164.4.2.1-09 | Dated addenda in all four docs; triage message no longer names `test-db-drift`; wait script behaviour unchanged | doc-shape + self-test + lint | Plan 03 Task 1 `<automated>` (`grep -q` addenda, negative grep on the triage list, `wait-for-test-schema-apply.sh --self-test`, `bash -n`, `actionlint` on the edited workflow) | ✅ | ⬜ pending |
| 164.4.2.1-03-02 | 03 | 3 | SC-1, SC-2 (D-09) | T-164.4.2.1-10 | No stale present-tense holder claim left unowned; no identifier; one commit of exactly four files | hygiene + commit-shape | Plan 03 Task 2 `<automated>` (no symlink left, hygiene gate OK, commit file set) | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

### Criterion → verification

| Criterion | Verified by |
|-----------|-------------|
| SC-1 `test-db-drift` holds no key; choice recorded with its reason | 02-01 (awk counts, holder-set `it`, `test-db-drift` negative `it`), 02-03 neuters 1a-1c, 03-01 (Option A reason in runbook and `CLAUDE.md`) |
| SC-2 VAC-08 still reads TEST after `apply-test`'s schema apply | 02-01 ordering `it` (order, wait not disabled, invocation inside the wait step, drift-check line, `needs:`), 02-03 neuters 2a-2h, `test-ledger-drift-check.sh --self-test` and `wait-for-test-schema-apply.sh --self-test` (unchanged harnesses) |
| SC-3 speed (Goal reading per D-01; clause (c) per D-06) | 01-01 / 01-02 before the code; the five-run AFTER after merge (manual, below) |
| SC-4 no timeout raised, no gate skipped, aggregator unchanged | ordering `it` TTL assertion + neuter 2f; existing aggregator pin left unedited and green in the 02-03 full run |

---

## Wave 0 Requirements

- [ ] New `test-db-drift` negative holder `it` in `src/__tests__/critical-regressions.test.ts` — written in plan 02 Task 1, observed RED in Task 3 (neuters 1a, 1b) and shown independent of the `DB_JOBS` equality (neuter 1c).
- [ ] New ordering `it` in the same file — written in plan 02 Task 1, each assertion observed RED on its own in Task 3 (neuters 2a-2h).

No framework install is needed; vitest and the self-test harnesses already exist. The only environment step is the `node_modules` symlink from the main checkout (plan 02 context), removed before each commit.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Merge pushes fall back inside or below the 16m18s-18m50s band; clause (c) combined `python` + `e2e-seeded` acquire-wait ≤ 8m25s; strict clause (a) recorded for Phase 164.9 | SC-3 | Only merge-push runs after the merge can measure it (D-05) | Fill `164.4.2.1-MEASUREMENT.md`'s AFTER from five admissible non-degenerate, non-migration-stratum runs using its `## Recipe`; record every degenerate and migration-stratum run separately; grade by its `## Verdict rule` |
| `test-db-drift` job log shows no `Acquire shared-test-db mutex` step | SC-1 (structural reading) | Needs a real CI run log | Read the job's step list on the first admissible run and paste it into the AFTER block |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (both new `it`s are written and neutered inside plan 02)
- [x] No watch-mode flags (`vitest run` only)
- [x] Feedback latency < 60s per task verify
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
