---
phase: 164.4.2.1-driftoffmutex
plan: "03"
type: execute
wave: 3
depends_on: ["164.4.2.1-02"]
files_modified:
  - CLAUDE.md
  - docs/runbooks/shared-test-db-mutex.md
  - .github/workflows/analytics-deploy-verify.yml
  - scripts/wait-for-test-schema-apply.sh
autonomous: true
requirements: [SC-1, SC-2]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Per D-09, every doc that states the shared-test-db holder set or the ci.yml key count now says, in a DATED addendum that keeps the old sentence as lineage, that test-db-drift left the key in Phase 164.4.2.1 and the ci.yml holders are python and e2e-seeded."
    - "Per D-02 (SC-1 'the choice is recorded with its reason'), the runbook and CLAUDE.md name the choice (Option A) and its reason (the ordering wait, not the key, orders VAC-08 after apply-test)."
    - "Every count written is RE-MEASURED at execution (grep -c on the live tree at the plan-02 commit), never copied from the research or this plan."
    - "The operator triage message in analytics-deploy-verify.yml no longer sends a reader to test-db-drift for an advisory-lock red."
    - "No behaviour changes: the wait script's self-test prints the same check count before and after, and actionlint reports no new finding on the edited workflow."
  artifacts:
    - CLAUDE.md
    - docs/runbooks/shared-test-db-mutex.md
    - .github/workflows/analytics-deploy-verify.yml
    - scripts/wait-for-test-schema-apply.sh
  key_links:
    - "CLAUDE.md's coupling (a) paragraph names the ci.yml job that contends with apply-test; after this phase that is python and e2e-seeded, not test-db-drift."
    - "The runbook's section 0 and section 1 holder lists and its section 7 census table are the places a key-change planner reads."
---

<objective>
Make every document that states who holds the shared-test-db key, or how many times `ci.yml` names it,
agree with what plan 02 shipped, as dated addenda with lineage kept (D-09).

Purpose: this repo has a dated record of prose drifting from the mechanism it describes. After plan 02,
`CLAUDE.md` (read by every session), the mutex runbook (read during an incident) and an operator triage
message would each send a reader to a `test-db-drift` acquire step that no longer exists.

Output: four files edited, comments and prose only. One commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-CONTEXT.md
@.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-RESEARCH.md

Also read `164.4.2.1-02-SUMMARY.md` in this phase directory at execution time: it records the MEASURED
`ci.yml` key count and each self-test's printed check count. It is named in plain text, not as an
@-reference, because plan 02 writes it, so it does not exist while this plan is pending. If it does not
exist, stop and report.

<!-- verify-symbol: docs/runbooks/shared-test-db-mutex.md test-db-drift -->
<!-- verify-symbol: .github/workflows/analytics-deploy-verify.yml test-db-drift -->
<!-- verify-symbol: scripts/wait-for-test-schema-apply.sh APPEAR_GRACE_SECONDS -->

⛔ Constraints:
- ⛔ Dated ⛔ CORRECTED / ⭐ ADDENDUM notes only (date 2026-09-25, "Phase 164.4.2.1"). Never silently
  rewrite an older sentence; keep it as lineage beside the correction.
- ⛔ Cite by SYMBOL (job name, step name, section heading). No line numbers in prose.
- ⛔ `scripts/wait-for-test-schema-apply.sh`: COMMENT lines only. `.github/workflows/analytics-deploy-verify.yml`:
  the one triage message string only. Both are non-planning paths and run in CI.
- ⛔ Do NOT edit `.github/workflows/ci.yml` (plan 02 owns it), `scripts/test-ledger-drift-check.sh` or
  `scripts/restore-test-from-baseline.sh` (D-09: already dated / regenerate-rather-than-trust).
- ⛔ Do NOT edit `VERSION`, `package.json` or `CHANGELOG.md`: the release entry is written at ship time,
  in the same commit as the VERSION bump (CLAUDE.md "CHANGELOG discipline").
- ⛔ Repo is PUBLIC: no DSN, host, username or absolute path. No CI skip token in the commit message,
  not even to deny it. Never `git checkout --` / `git restore` / `git stash`. Never stage
  `.planning/milestone.lock` or `.planning/state.json`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Re-measure the counts and write the dated addenda in all four files</name>
  <files>CLAUDE.md, docs/runbooks/shared-test-db-mutex.md, .github/workflows/analytics-deploy-verify.yml, scripts/wait-for-test-schema-apply.sh</files>
  <read_first>
    - `164.4.2.1-02-SUMMARY.md` (measured key count, self-test counts)
    - `CLAUDE.md` section `## Which database am I on? (ask FIRST, every time)`, the bullet on the TWO NEW COUPLINGS (`[164.8-PUSH-RACE-VAC08]`) with its 2026-09-23 correction, and section `## SQL gate integrity jobs (v0.77.0.0, Phase 164.3)` where it says `test-db-drift` keeps the secret, the schema-apply wait, the mutex and the `needs: python` stagger
    - `docs/runbooks/shared-test-db-mutex.md` sections 0 and 1, the holder-count argument ("five holders"), the section-7 key-occurrence census with its 2026-09-23 correction, and `### 7.3 The ordering wait, and its three outcomes`; find every other holder list with `grep -n test-db-drift`
    - `.github/workflows/analytics-deploy-verify.yml` the triage message naming `python` / `test-db-drift` / `e2e-seeded` and the advisory lock
    - `scripts/wait-for-test-schema-apply.sh` its header comment (which reader jobs invoke it) and the comment beginning "NOT A CLAIM THAT THIS WAS THE ONLY DEFENCE"
  </read_first>
  <action>
Per D-02 and D-09.

First measure, from the repo root at the plan-02 commit, and write the numbers into your notes:
`grep -c 61616158 .github/workflows/ci.yml`; the per-job count for `test-db-drift`, `python`,
`e2e-seeded` and `sql-tests` (awk over each job span, from the job's two-space key to the next two-space
job key); `grep -c 61616158` for `supabase-migrate.yml`, `test-restore-from-baseline.yml`,
`mutex-probe.yml`, `analytics-deploy-verify.yml`; the list of `ci.yml` jobs whose body carries
`- name: Acquire shared-test-db mutex`. These must agree with the plan-02 SUMMARY; if not, stop and report.
Also record the check count `bash scripts/wait-for-test-schema-apply.sh --self-test` prints BEFORE any edit.

1. `CLAUDE.md` — two dated ⛔ CORRECTED 2026-09-25 (Phase 164.4.2.1) notes, each directly under the
   2026-09-23 correction it supersedes, originals kept:
   (a) under the coupling (a) correction: the `ci.yml` jobs that hold the key are now `python` and
   `e2e-seeded`; `test-db-drift` holds none and is ordered after `apply-test` by the schema-apply wait
   alone (Option A, D-02, and why: VAC-08 is read-only and waited up to 20m31s for 2-5 s of work); the
   re-measured `ci.yml` count with its per-job split; so on a merge push the contenders for the key with
   `apply-test` are `python` and `e2e-seeded`.
   (b) under the `## SQL gate integrity jobs` correction: `test-db-drift` keeps the secret, the
   schema-apply wait and the `needs: python` edge, and no longer takes the mutex.
2. `docs/runbooks/shared-test-db-mutex.md` — a dated ⛔ CORRECTED 2026-09-25 note beside each place that
   lists `test-db-drift` as a holder or counts its key literals (sections 0 and 1, the holder-count argument,
   the section-7 census, `### 7.3`): holders in `ci.yml` are `python` and `e2e-seeded`, so FOUR holders in
   total with `apply-test` and the restore; the re-measured census; `test-db-drift` runs the ordering wait and
   no acquire, so for it the wait is the only ordering control, which `critical-regressions.test.ts` now pins.
   Add one short paragraph in section 0 recording the choice and its reason (SC-1): Option A, the rejected
   options in one line each, and that the measurement lives in
   `.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md` (read the numbers there, none restated).
3. `.github/workflows/analytics-deploy-verify.yml` — in the triage message, drop `test-db-drift` from the list
   of jobs that can go red on the advisory lock, and extend the existing parenthetical about `sql-tests` so it
   also says `test-db-drift` held it until Phase 164.4.2.1 and holds none now. Change nothing else in the file.
4. `scripts/wait-for-test-schema-apply.sh` — COMMENTS only: under the header sentence naming the reader jobs,
   a dated note that the invokers are now `python` and `e2e-seeded` (before their acquire) and `test-db-drift`
   (which has no acquire since Phase 164.4.2.1, so this wait is its only ordering control); under the
   "NOT A CLAIM THAT THIS WAS THE ONLY DEFENCE" comment, a dated qualifier that the mutex still separates
   `python` and `e2e-seeded` from a writer but no longer `test-db-drift`, whose reads are read-only and fail
   loud, never green, on an overlap (research interleaving table).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q '2026-09-25 (Phase 164.4.2.1' CLAUDE.md && grep -q 'Phase 164.4.2.1' docs/runbooks/shared-test-db-mutex.md && grep -q 'Phase 164.4.2.1' scripts/wait-for-test-schema-apply.sh && ! grep -q '`python` / `test-db-drift` / `e2e-seeded`' .github/workflows/analytics-deploy-verify.yml && bash scripts/wait-for-test-schema-apply.sh --self-test && bash -n scripts/wait-for-test-schema-apply.sh && actionlint .github/workflows/analytics-deploy-verify.yml && echo DOCS-CURRENCY-OK</automated>
  </verify>
  <acceptance_criteria>
    - Every addendum is dated 2026-09-25, names Phase 164.4.2.1, and sits beside a kept original.
    - Every count written equals the re-measured value and matches the plan-02 SUMMARY.
    - `git diff scripts/wait-for-test-schema-apply.sh` shows only lines starting with `#` (after indentation) added.
    - `git diff .github/workflows/analytics-deploy-verify.yml` changes exactly the one message line.
    - The wait script's self-test prints the same check count as before the edit; actionlint reports no finding on the edited workflow that it did not report before.
  </acceptance_criteria>
  <done>No doc in the repo still tells a reader that `test-db-drift` takes the key, except as dated lineage.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep for any missed holder claim, run the gates, commit</name>
  <files>CLAUDE.md, docs/runbooks/shared-test-db-mutex.md, .github/workflows/analytics-deploy-verify.yml, scripts/wait-for-test-schema-apply.sh</files>
  <read_first>
    - `git diff` of Task 1
  </read_first>
  <action>
Sweep: `git grep -n test-db-drift -- ':!.planning' ':!.github/workflows/ci.yml' ':!src/__tests__'` and read
every hit. Any remaining sentence that states in the present tense that `test-db-drift` holds or acquires the
key, outside a dated lineage block, gets a dated correction in THIS plan only if its file is in this plan's
`files_modified`; otherwise list it in the SUMMARY as a found-but-not-owned stale claim (do not widen scope
silently). Then run the tests that read these files:
`git grep -l -e 'shared-test-db-mutex.md' -e 'analytics-deploy-verify' -e 'wait-for-test-schema-apply' -- src/__tests__`
and run that list with vitest (symlink `node_modules` from the main checkout exactly as plan 02 describes,
and remove the symlink before committing). Run the planning hygiene gate:
`MAIN="$(dirname "$(git rev-parse --git-common-dir)")"; "$MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts`
and require OK. Stage the four files by explicit path, confirm `git diff --cached --name-only` lists exactly
those four, and commit `docs(164.4.2.1-03): holder-set currency after test-db-drift left the key`, ending
with the attribution lines the orchestrator supplies.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test ! -e node_modules && MAIN="$(dirname "$(git rev-parse --git-common-dir)")" && "$MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts && L="$(git log -1 --name-only --format=)" && test "$(printf '%s\n' "$L" | LC_ALL=C sort | tr '\n' ' ')" = '.github/workflows/analytics-deploy-verify.yml CLAUDE.md docs/runbooks/shared-test-db-mutex.md scripts/wait-for-test-schema-apply.sh ' && echo PLAN03-COMMIT-OK</automated>
  </verify>
  <acceptance_criteria>
    - The sweep's every hit is either current, dated lineage, or listed in the SUMMARY as not owned.
    - The tests that read these files pass with a non-zero count; the hygiene gate prints OK.
    - Exactly one commit touching exactly the four files.
  </acceptance_criteria>
  <done>The docs and the mechanism agree, and the one commit is on the branch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked docs → public repo | every byte is world-readable on push |
| operator reading the runbook mid-incident | a stale holder list sends triage to a step that does not exist |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2.1-08 | Repudiation (stale runbook) | `docs/runbooks/shared-test-db-mutex.md`, `CLAUDE.md` | medium | mitigate | dated corrections beside every holder list and census; counts re-measured, matched against the plan-02 SUMMARY |
| T-164.4.2.1-09 | Tampering (behaviour change hidden in a doc edit) | `scripts/wait-for-test-schema-apply.sh`, `analytics-deploy-verify.yml` | medium | mitigate | comment-only / one-string diff checked in acceptance; wait self-test check count unchanged; actionlint on the edited workflow |
| T-164.4.2.1-10 | Information disclosure | all four files | low | mitigate | no identifiers; hygiene gate OK before commit |
</threat_model>

<verification>
Task 1's currency check and Task 2's sweep, tests, hygiene gate and single-commit check all pass.
</verification>

<success_criteria>
`CLAUDE.md`, the runbook, the triage message and the wait script's header all say, dated, that the
`ci.yml` holders are `python` and `e2e-seeded`, with re-measured counts and the Option A reason.
</success_criteria>

<output>
Create `.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-03-SUMMARY.md` when done. Hand-off, not a task:
at ship time the CHANGELOG entry and VERSION bump are written in one commit per CLAUDE.md; after the merge
the five-run AFTER in `164.4.2.1-MEASUREMENT.md` is filled as a verification obligation (D-05).
</output>
