---
phase: 164.4.2.1-driftoffmutex
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md
autonomous: true
requirements: [SC-3]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Per D-05, the phase's speed claim is falsifiable before any ci.yml line changes: the BEFORE, the admissibility rule, the clause texts and the prediction are committed first, so the change cannot be graded on its own narrative."
    - "Per D-01, clause (a) is graded against the Goal reading (non-degenerate run-total inside or below 16m18s-18m50s), and the strict reading (below 16m18s) is still recorded per run and booked as evidence for Phase 164.9, never as this phase's pass/fail."
    - "Per D-06, clause (c) cannot pass vacuously: the test-db-drift acquire-wait is written as zero by construction and never evidence; the graded term is python + e2e-seeded, and its ceiling is fixed in advance at 8m25s (7m00s + the 1m25s BEFORE spread)."
    - "A merge push that carries a supabase-migrate.yml run is recorded as its own stratum, never averaged into the others."
  artifacts:
    - .planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md
  key_links:
    - "The new file cites 164.4.2-MEASUREMENT.md's `## Recipe`, `## BEFORE table` and `### Verdict rule` sections BY HEADING and quotes the bands from them; it does not restate a remembered variant."
    - "The prediction section is written before plan 02's commit exists, so `git log` shows the MEASUREMENT commit as an ancestor of the ci.yml commit."
---

<objective>
Write `164.4.2.1-MEASUREMENT.md`: the BEFORE this phase is judged against, an empty AFTER protocol
for five admissible merge-push runs, the clause texts per D-01 and D-06, and the prediction, all
committed before plan 02 touches `ci.yml` (D-05).

Purpose: Phase 164.4.2 shipped a change whose AFTER showed it made merge pushes slower. This phase's
own claim ("the 20-minute `test-db-drift` tail goes; runs fall back inside the BEFORE band") must be
stated in a form a later reader can refute from run ids alone.

Output: one planning document. No code, no network call, no database command.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-CONTEXT.md
@.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-RESEARCH.md
@.planning/phases/164.4.2-subsetsplit-sql-mutation-runs-only-the-changed-gate-files-on/164.4.2-MEASUREMENT.md

⛔ Constraints:
- ⛔ `.planning/` is TRACKED and this repository is PUBLIC: run ids and SHAs only. No DSN, project
  ref, host, absolute path or username.
- ⛔ No `gh` write, no workflow dispatch, no DB command. This plan needs no network at all: every
  number it cites is already in the 164.4.2 file or the research.
- ⛔ Do NOT modify `.planning/milestone.lock`, `.planning/state.json`, or any other phase's files.
- ⛔ Never write a CI skip token in the commit message, not even inside a sentence denying it.
- ⛔ Never use `git checkout --`, `git restore` or `git stash`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write 164.4.2.1-MEASUREMENT.md end to end (BEFORE, protocol, clauses, prediction)</name>
  <files>.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md</files>
  <read_first>
    - `164.4.2-MEASUREMENT.md` in full: `## Recipe`, `## Ancestry proof`, `## BEFORE table`, `## Prediction`, `## Refutation condition — decided in advance (DECISION E)`, and every subsection of `## AFTER — to be filled from merge-push runs at the new head` (`### Which runs count`, `### Commands`, `### The four jobs and what each quantity means now`, the Runs 1-5 blocks, `### Verdict rule`, `### What a WIN looks like`)
    - `164.4.2.1-RESEARCH.md` `## Measurement plan (Question 4)` and `### Pitfall 3` / `### Pitfall 4`
    - `164.4.2.1-CONTEXT.md` D-01, D-05, D-06
  </read_first>
  <action>
Create the file with these sections, in this order, using the Write tool.

1. A title and a blockquote stating it was captured before any `ci.yml` change in this phase, per
   D-05, and that the Goal reading of clause (a) is the founder's decision D-01 (quote the verbatim
   answer "Judge by the goal" and the date 2026-09-25).
2. `## Recipe` — cite `164.4.2-MEASUREMENT.md` `## Recipe` Steps 1-3 BY HEADING and paste them
   verbatim, with the dated four-job `select` that file's `### Commands` section added on 2026-09-24
   (`sql-tests`, `python`, `e2e-seeded`, `test-db-drift`). Add one further read per run: whether a
   `supabase-migrate.yml` run exists for the head SHA (`gh run list --workflow=supabase-migrate.yml
   --commit <sha> --json databaseId,conclusion`). State that durations are computed by a Node script
   (`new Date(b) - new Date(a)`, rounded to the second), never by hand.
3. `## BEFORE` — the 164.4.2 BEFORE bands, quoted from that file's `## BEFORE table` and
   `### Verdict rule`: non-degenerate run-total 16m18s-18m50s (full spread 16m18s-62m20s);
   three-holder combined acquire-wait 12m05s-14m56s; `python` + `e2e-seeded` alone 5m35s-7m00s.
   Then the 164.4.2 AFTER rows this phase exists to fix, quoted from that file's Runs 1-5 blocks by
   run id (35939061930, 35943402509, 35943407413, 35957479474, 35958026743): run-total, degenerate
   flag, and `test-db-drift`'s acquire-wait per run. Say that these are what the change is measured
   against and that the 164.4.2 file is the source; re-read every number from it, do not copy them
   from this plan.
4. `## Which runs count` — concluded `push` runs of `ci.yml` on `main` whose head has this phase's
   merge commit as an ancestor (`git merge-base --is-ancestor <merge> <head> && echo AFTER-CONFIRMED`,
   pasted per run); the first 5 non-degenerate, plus EVERY degenerate run recorded and marked. Bind
   each to its SHA and COUNT that SHA's workflow runs (`gh api "repos/AI-Isaiah/Quantalyze/actions/runs?head_sha=<sha>" -q '.workflow_runs | length'`).
   Degenerate = 164.4.2's rule (`python` acquire-wait not near 0m05s) PLUS a recorded
   "overlapped a sibling run?" column (start time inside another admissible run's span), because
   164.4.2's Runs 2 and 4 passed the old rule while overlapping. A run with a `supabase-migrate.yml`
   run for its SHA is its own STRATUM (Pitfall 4: the ordering wait inherits `apply-test`'s queue) and
   is reported separately, never averaged in. State explicitly: a migration-stratum run does NOT count
   toward "the first 5 non-degenerate" — the five graded runs are all non-migration pushes; stratum runs
   are recorded in their own table beside them, in addition to the five.
5. `## Empty AFTER table` — one block per run (copy for runs 2-5) with cells for: run id, head SHA,
   ancestry line, workflow-run count, sibling overlap, `supabase-migrate.yml` run present;
   `test-db-drift`: acquire-wait written literally as "0 — no acquire step (by construction, never
   evidence)", ordering-wait step duration, VAC-08 step duration, job-total, and
   `end(test-db-drift) − end(e2e-seeded)` (negative = off the critical path); `python` and
   `e2e-seeded`: acquire-wait, useful work, job-total; `sql-tests` job-total; run-total.
6. `## Verdict rule` — clause (a) per D-01: GRADED = non-degenerate run-total inside or below
   16m18s-18m50s; RECORDED, NOT GRADED = strict run-total below 16m18s, with the instruction that its
   result is booked as evidence for Phase 164.9 TESTISOLATION. Clause (c) per D-06: combined =
   `python` + `e2e-seeded` acquire-wait, the `test-db-drift` term written as 0 by construction; PASS =
   below 12m05s AND not above 5m35s-7m00s by more than the two jobs' measured BEFORE spread. Write the
   spread and the ceiling as FIXED NUMBERS here, before any code, so the bar cannot move after the AFTER
   is read: spread = 7m00s − 5m35s = **1m25s**; ceiling = 7m00s + 1m25s = **8m25s**. So clause (c) PASS
   per run = combined ≤ 8m25s (which is also below 12m05s). Re-read 5m35s and 7m00s from the 164.4.2
   `### Verdict rule` first; if either differs from this plan, recompute from the file's values and say so. Clause (b)
   (`sql-tests` job-total below 8m31s) is carried as a regression guard, since this phase must not
   undo 164.4.2's one passing clause. Clause (d) as in 164.4.2: a faster wall clock with (c) failing is
   luck, not a confirmation. Plus SC-1's structural reading: the `test-db-drift` job log shows no
   `Acquire shared-test-db mutex` step at all.
7. `## Prediction — decided before the change` — verbatim from the research's `## Measurement plan`
   last bullet: `test-db-drift` job-total about 2 min; it ends before `e2e-seeded` on every run;
   clause (c) PASS on non-overlapping runs (about 0m05s + 6m20s-6m50s); strict clause (a) FAIL
   (non-degenerate run-total about 17m25s-18m30s, `e2e-seeded`-bound); the Goal reading PASS;
   overlapping-sibling runs stay slow because `e2e-seeded` itself queues (164.4.2 Run 2 pattern),
   which is Phase 164.9's. State that a result better than predicted is recorded as MISSED
   prediction too, as 164.4.2's `### What a WIN looks like` did.
8. `## Obligation` — filling this AFTER is a verification obligation after the merge, not a plan
   task (D-05); the phase VERIFICATION may not read `passed` before five admissible runs are in.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && F=.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md && for h in '## Recipe' '## BEFORE' '## Which runs count' '## Empty AFTER table' '## Verdict rule' '## Prediction' '## Obligation'; do grep -q "^$h" "$F" || { echo "MISSING $h"; exit 1; }; done && grep -q 'Judge by the goal' "$F" && grep -q '16m18s' "$F" && grep -q '12m05s' "$F" && grep -q '1m25s' "$F" && grep -q '8m25s' "$F" && grep -q 'by construction' "$F" && grep -q 'supabase-migrate.yml' "$F" && for r in 35939061930 35943402509 35943407413 35957479474 35958026743; do grep -q "$r" "$F" || { echo "MISSING run $r"; exit 1; }; done && echo MEASUREMENT-SHAPE-OK</automated>
  </verify>
  <acceptance_criteria>
    - All eight sections exist in the order given.
    - Every band quoted equals the band in `164.4.2-MEASUREMENT.md` (re-read, not copied from this plan).
    - The clause (a) text has TWO readings, one GRADED (D-01 Goal reading) and one RECORDED for Phase 164.9.
    - The `test-db-drift` acquire-wait cell is pre-written as zero by construction, never evidence.
    - Clause (c)'s spread (1m25s) and ceiling (8m25s) are written as fixed numbers.
    - The file says migration-stratum runs are excluded from the first 5 graded runs.
  </acceptance_criteria>
  <fails_when>A heading, a quoted band, the fixed clause (c) ceiling, or one of the five 164.4.2 run ids is missing — the shape check prints `MISSING …` and exits 1.</fails_when>
  <done>MEASUREMENT.md exists with BEFORE, protocol, clause texts and prediction; no AFTER number is filled.</done>
</task>

<task type="auto">
  <name>Task 2: Hygiene-check and commit the measurement before any code</name>
  <files>.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md</files>
  <read_first>
    - The file written in Task 1 (to confirm no identifier crept in)
  </read_first>
  <action>
Stage ONLY the MEASUREMENT file by explicit path. Run the planning hygiene gate from the repo root
with the main checkout's `tsx`: set `MAIN` to the parent directory of `git rev-parse
--git-common-dir`, then run `"$MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts` and
require it to print OK. If it flags anything in this file, fix the file (never widen an allowlist)
and re-run. Confirm `git diff --cached --name-only` lists exactly this one file (never
`.planning/milestone.lock` or `.planning/state.json`). Commit with message
`docs(164.4.2.1-01): measurement protocol and prediction before the change`, ending with the
attribution lines the orchestrator supplies.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && MAIN="$(dirname "$(git rev-parse --git-common-dir)")" && "$MAIN/node_modules/.bin/tsx" scripts/check-planning-hygiene.ts && L="$(git log -1 --name-only --format=)" && test "$L" = '.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-MEASUREMENT.md' && echo COMMITTED-OK</automated>
  </verify>
  <acceptance_criteria>
    - The hygiene gate prints OK.
    - The commit touches exactly the MEASUREMENT file.
    - No `.github/`, `src/` or `scripts/` path is changed by this plan.
  </acceptance_criteria>
  <fails_when>The hygiene gate finds an identifier, or the last commit touches any path other than the MEASUREMENT file.</fails_when>
  <done>The measurement is committed and is an ancestor of whatever plan 02 commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tracked `.planning/` → public repo | every byte written here is world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2.1-01 | Information disclosure | MEASUREMENT.md | medium | mitigate | run ids and SHAs only; hygiene gate (`check-planning-hygiene.ts`) must print OK before the commit |
| T-164.4.2.1-02 | Repudiation | the speed claim | medium | mitigate | prediction and clause texts committed before the ci.yml change (D-05); strict clause (a) recorded even though not graded (D-01), so no reading is hidden |
</threat_model>

<verification>
The Task 1 shape check and the Task 2 hygiene + commit check both pass.
</verification>

<success_criteria>
`164.4.2.1-MEASUREMENT.md` is committed, carries the D-01 grading, the D-06 non-vacuous clause (c),
the migration-push stratum, the prediction, and an AFTER table with no numbers in it.
</success_criteria>

<output>
Create `.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-01-SUMMARY.md` when done.
</output>
