---
phase: 164.4.2.1-driftoffmutex
verified: 2026-09-26T05:56:48Z
status: human_needed
score: 3/4 roadmap success criteria verified (SC-3 is a post-merge measurement, not yet takeable)
verified_at_sha: c98f1cb348b858bd01746790cf7f465e6e1bb1b3
drift_subjects:
  - .github/workflows/ci.yml
  - .github/workflows/test-restore-from-baseline.yml
  - .github/workflows/analytics-deploy-verify.yml
  - scripts/wait-for-test-schema-apply.sh
  - scripts/test-ledger-drift-check.sh
  - scripts/restore-test-from-baseline.sh
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/mutex-dead-holder-verdict.test.ts
  - src/__tests__/test-restore-workflow-wiring.test.ts
  - docs/runbooks/shared-test-db-mutex.md
behavior_unverified: 0
overrides_applied: 0
re_verification: null
human_verification:
  - test: "SC-3 / D-05 — fill the five-run AFTER table in 164.4.2.1-MEASUREMENT.md from admissible merge-push runs whose head carries this phase's merge commit, and grade each run with that file's own `## Verdict rule`."
    expected: "Five non-degenerate, non-migration-stratum runs. Clause (a) on the Goal reading (D-01): run-total inside or below 16m18s–18m50s. Clause (c): python + e2e-seeded acquire-wait below 12m05s and not above 8m25s. The strict below-16m18s reading is recorded per run as evidence for Phase 164.9 and is not a pass/fail gate here."
    why_human: "Only runs after the merge can measure it (D-05). The table is PROTOCOL ONLY at this SHA; no pre-merge evidence can stand in for it. Owner: the session that lands the merge, as the post-merge re-verification of this phase (founder UAT queue, group C wait-for-event). This VERIFICATION may not be flipped to passed until the table is filled and graded."
  - test: "Ratify or reject the PR-run exposure the round-1 review found (SFH-01 / WR-01). On a pull_request run the ordering wait does not run, so VAC-08 now has NO ordering against a concurrent apply-test or a dispatched restore; before this phase the key provided it."
    expected: "A founder decision recorded in CONTEXT and ROADMAP (CLAUDE.md deviation policy). Accept as documented (new outcomes are LOUD reds only, triage in runbook section 0), or reject and book follow-up work."
    why_human: "D-02's reasoning covered merge pushes only. The acceptance of the PR-run case was written by the round-1 fixer, not decided by the founder, and it changes VAC-08's possible verdicts on PR runs (a transient loud red where the key used to make it wait). No code can decide whether that trade is acceptable."
---

# Phase 164.4.2.1: DRIFTOFFMUTEX Verification Report

**Phase Goal:** A merge push to `main` is no slower than before Phase 164.4.2. `test-db-drift` (VAC-08) stops queueing behind `python` and `e2e-seeded` for the advisory key, without weakening VAC-08's verdict and without breaking the ordering against `supabase-migrate.yml`'s `apply-test`.
**Verified:** 2026-09-26T05:56:48Z at `c98f1cb34` (branch `feat/164.4.2.1-driftoffmutex`)
**Status:** human_needed
**Re-verification:** No. This is a respawn of an initial verification whose previous run died mid-way and wrote no report.

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria, then plan must-haves)

| # | Truth | Status | Evidence |
|---|---|---|---|
| SC-1 | `test-db-drift` no longer holds or waits for the shared-test-db key for VAC-08, and the choice is recorded with its reason | ✓ VERIFIED | The non-comment body of the `test-db-drift` job at HEAD, diffed against the base `01dcf1cc7`, loses exactly the `Acquire`, `Release` and dead-holder `Verdict` steps. `grep -c 61616158` inside the job gives 0. The choice (Option A) and its reason are in CONTEXT D-02 and in runbook section 0, with B, C, C′ and D rejected. **Neuter N1 (mine):** I added a `pg_advisory_lock(<key>)` line to the job and 2 pins went RED, the holder-set `it` and `test-db-drift holds and names no shared-test-db key`. Restored, `cmp` OK. |
| SC-2 | VAC-08's verdict is unchanged: it reads TEST after `apply-test`'s schema apply, a red stays red, and a missing credential exits 1 | ✓ VERIFIED (merge pushes; see the human item on PR runs) | **Ordering:** the wait step still runs before VAC-08 with its merge-push `if:`. Neuter N2 (mine, the wait's `if:` disabled) turned the ordering pin RED. **Red stays red:** `scripts/test-ledger-drift-check.sh --self-test` passed 13/13 arms. Its only changes this phase are two failure-message strings and a comment, and the verdict logic is untouched. **Credential:** with `TEST_SUPABASE_DB_URL` unset the script exits 1 (observed locally, before any psql call). **Masking:** neuter N3 (mine, `if: false` on the VAC-08 step) was caught RED. **Added:** `PGCONNECT_TIMEOUT: "15"` on the VAC-08 step bounds only the connect, and a failed connect exits non-zero, so it can add a loud red but never a green. |
| SC-3 | Five non-degenerate merge-push runs after the merge meet 164.4.2's clauses (a) (Goal reading, D-01) and (c) (D-06) | ? UNCERTAIN, post-merge (human_needed) | `164.4.2.1-MEASUREMENT.md` has the BEFORE, the verdict rule, the prediction and an **empty** AFTER marked `PROTOCOL ONLY`. It was committed at `d04f580a2`, an ancestor of the `ci.yml` change `15d8bfd77` (checked with `git merge-base --is-ancestor`). D-05 forbids `passed` until the table is filled. Not deferred to a later phase: it is this phase's own obligation, and its owner is named in the human-verification item. |
| SC-4 | No timeout raised, no gate skipped, no job leaves the `frontend` aggregator | ✓ VERIFIED | `timeout-minutes: 90` unchanged, and neuter N4 (mine, 90 → 120) turned RED. `test-db-drift` is still in `frontend`'s `needs:` and result loop, and neuter N5 (mine, dropped from `needs:`) turned RED. Across all of `ci.yml`, the only added non-comment lines are `PGCONNECT_TIMEOUT` and the aggregator's red-message text. No step `if:` was added. The deleted dead-holder `Verdict` step judged a holder this job no longer has, so it is a removed mechanism, not a skipped gate. |
| P-1 | The holder-set pins moved by measurement in the same commit as the `ci.yml` change (D-07) | ✓ VERIFIED | `DB_JOBS = ["python", "e2e-seeded"]`, measured as 9 + 8 key hits in those jobs and 0 in `test-db-drift` and `sql-tests`. `mutex-dead-holder-verdict.test.ts` Test 2's floor went from 5 to 4 with a dated reason. The run passed 18/18 tests. |
| P-2 | Docs currency: dated addenda with counts re-measured, old sentences kept as lineage (D-09) | ✓ VERIFIED | My counts of `grep -c 61616158` per workflow: ci.yml 17, test-restore-from-baseline.yml 8, supabase-migrate.yml 7, mutex-probe.yml 5, analytics-deploy-verify.yml 1. They equal the figures in the CLAUDE.md addendum and runbook section 7.2. The runbook's "FOUR holders" (python, e2e-seeded, apply-test, restore) is consistent with its own enumeration. |

**Score:** 3/4 roadmap SCs verified, plus 2/2 plan must-haves. SC-3 cannot be taken before the merge. 0 truths are present-but-behavior-unverified: every behaviour-dependent pin was exercised by a neuter I ran myself, and every one went RED.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `.github/workflows/ci.yml` job `test-db-drift` | no mutex steps; wait before VAC-08; needs/if/timeout/permissions kept | ✓ VERIFIED | Diffed its non-comment lines against the base. The wait step lost its conditional psql install (the `Install psql client` step before it installs psql unconditionally). |
| `scripts/wait-for-test-schema-apply.sh` | the ordering wait, unchanged in outcome | ✓ VERIFIED | The one functional addition is a named `wait-outcome: psql-missing` exit 1, covered by 2 new self-test checks (48 in total). All three invokers (`python`, `test-db-drift`, `e2e-seeded`) install psql before their wait step. I checked each by line order. |
| `src/__tests__/critical-regressions.test.ts` | holder-set, negative and ordering pins | ✓ VERIFIED | Five neuters, five REDs, each restored and `cmp` proven. |
| `164.4.2.1-MEASUREMENT.md` | BEFORE, protocol, prediction, committed before code | ✓ VERIFIED (protocol), AFTER pending | See SC-3. |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `test-db-drift` wait step | `scripts/wait-for-test-schema-apply.sh` | `bash "${GITHUB_WORKSPACE}/scripts/wait-for-test-schema-apply.sh"` under the merge-push `if:` | WIRED (pinned; neuter N2 went RED) |
| `test-db-drift` VAC-08 step | `scripts/test-ledger-drift-check.sh` | bare `bash scripts/test-ledger-drift-check.sh`, no step `if:` | WIRED (pinned; neuter N3 went RED) |
| `frontend` aggregator | `test-db-drift` | `needs:` + `"test-db-drift=${{ needs.test-db-drift.result }}"` result loop | WIRED (neuter N5 went RED) |

### Behavioral Spot-Checks (all run by me at `c98f1cb34`)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Every test that reads `ci.yml`, plus the mutex verdict and restore wiring tests | `xargs vitest run` over the 17 `git grep -l 'workflows/ci.yml' -- src/__tests__` files, `mutex-dead-holder-verdict.test.ts` and `test-restore-workflow-wiring.test.ts` (18 files) | `Test Files  18 passed (18)` / `Tests  1471 passed \| 17 skipped (1488)`. The 17 skips are env-gated `skipIf`s (live DB, gitleaks twin). `lint-sql-gates` and `ci-anti-skip-gate` passed in this run with no 5 s timeout, so no solo re-run was needed. | ✓ PASS |
| actionlint | `actionlint .github/workflows/ci.yml` and `.../analytics-deploy-verify.yml` | no output, exit 0 for both | ✓ PASS |
| actionlint, restore workflow | `actionlint .github/workflows/test-restore-from-baseline.yml` | 4 shellcheck `info` findings. The base `01dcf1cc7` has the same 4, and this phase added comments and one message string only. | ✓ PASS (pre-existing) |
| Wait self-test | `bash scripts/wait-for-test-schema-apply.sh --self-test` | `wait-for-test-schema-apply self-test OK (48 checks)` | ✓ PASS |
| VAC-08 self-test | `bash scripts/test-ledger-drift-check.sh --self-test` | `self-test OK (13/13 arms — every red mode fires, all FOUR green paths pass, and the harness is calibrated)` | ✓ PASS |
| Other self-tests | `mutex-dead-holder-verdict.sh --self-test`; `lint-sql-gates.mjs --self-test` | `OK (5 checks)`; `OK: 7 rules, red+green each.` | ✓ PASS |
| VAC-08 missing credential | `env -u TEST_SUPABASE_DB_URL bash scripts/test-ledger-drift-check.sh` | exit 1 (fails before any psql call, touches no database) | ✓ PASS |

### Probe Execution

Step 7c: SKIPPED. The phase declares no `probe-*.sh`. The runnable checks are the self-tests above.

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|---|---|---|---|
| 164.4.2 speed goal, clause (a) | critical path back inside or below the BEFORE band (Goal reading) | ? NEEDS HUMAN (post-merge) | SC-3 |
| 164.4.2 speed goal, clause (c) | combined lock-wait, made non-vacuous (D-06) | ? NEEDS HUMAN (post-merge) | SC-3 |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| (all non-planning files in the phase diff) | — | TBD / FIXME / XXX / TODO / HACK in added lines | none found | — |
| `scripts/test-ledger-drift-check.sh` | `check()` failure messages | D-09 said "not edited"; round-1 review fix SFH-01 edited two message strings and a comment | ℹ️ Info | The verdict logic and the substrings its tests assert are unchanged, and the self-test is 13/13. This deviates from D-09, and it is recorded in REVIEW-FIX. |
| `ci.yml` `test-db-drift` | wait step, VAC-08 step | Plan 02 pinned both regions byte-identical by sha256; review fixes later changed them (removed the redundant conditional psql install, added `PGCONNECT_TIMEOUT: "15"`) | ℹ️ Info | Neither weakens ordering or the verdict. The recorded region hashes in 02-SUMMARY are now historical. |

### Human Verification Required

#### 1. SC-3: the five-run AFTER (D-05)

**Test:** After the merge, fill `164.4.2.1-MEASUREMENT.md`'s AFTER from five admissible merge-push runs, with the recipe and verdict rule written in that file.
**Expected:** Clause (a) on the Goal reading and clause (c) PASS on non-degenerate runs. Each run's strict (a) reading is recorded for Phase 164.9.
**Why human:** It needs runs that happen after the merge. **Owner:** the session that lands the merge (post-merge re-verification of 164.4.2.1; founder UAT queue group C). This report stays `human_needed` until then.

#### 2. Ratify the PR-run ordering loss (SFH-01 / WR-01)

**Test:** Decide whether it is acceptable that a `pull_request` run's VAC-08 has no ordering against a concurrent `apply-test` or dispatched restore.
**Expected:** A dated founder decision in CONTEXT and ROADMAP.
**Why human:** The fixer accepted it, not the founder, and D-02's reasoning covered merge pushes only. The documented outcomes are loud reds only, and nothing I found turns a real drift green.

### Gaps Summary

No gaps block the code change. `test-db-drift` is off the key and the ordering, credential, timeout and aggregator contracts all hold. Each is pinned, and I watched each pin go RED under a neuter. The phase cannot read `passed` yet for two reasons. First, its speed claim (SC-3) can be measured only by merge-push runs after the merge, and D-05 forbids `passed` before the table is filled. Second, a review-round acceptance of a new PR-run failure mode was never put to the founder.

---

_Verified: 2026-09-26T05:56:48Z_
_Verifier: Claude (gsd-verifier)_
