---
phase: 164.4.2.1-driftoffmutex
plan: "02"
type: execute
wave: 2
depends_on: ["164.4.2.1-01"]
files_modified:
  - .github/workflows/ci.yml
  - src/__tests__/critical-regressions.test.ts
  - src/__tests__/mutex-dead-holder-verdict.test.ts
autonomous: true
requirements: [SC-1, SC-2, SC-4]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per D-02, test-db-drift neither holds nor waits for the shared-test-db key: its Acquire, Release and dead-holder Verdict steps are gone, and no literal of the key number remains anywhere in the job's body."
    - "Per D-02 and SC-2, VAC-08's inputs and ordering are unchanged: needs [python, changed-paths], the job if:, timeout-minutes 90, permissions, the ordering-wait step and the VAC-08 step are byte-identical to before this plan, and the ordering wait still precedes VAC-08."
    - "Per D-07, the holder set is re-measured and re-pinned in the SAME commit: DB_JOBS is exactly [python, e2e-seeded]; a negative pin in its OWN it reds if test-db-drift takes or names the key again; an ordering pin reds if the wait moves after VAC-08, is deleted, is DISABLED (its if: no longer the push-only condition), stops invoking the wait script, if the VAC-08 drift-check line changes, or if test-db-drift's timeout-minutes, needs: python or needs: changed-paths changes; the dead-holder anti-vacuity floor is 4 with a dated reason."
    - "Every new or moved pin was observed RED under a neuter and GREEN after a byte-verified restore."
    - "Per D-04 and SC-4, no timeout is raised, no gate is skipped and no job leaves the frontend aggregator (the existing aggregator pin stays green unedited)."
  artifacts:
    - .github/workflows/ci.yml
    - src/__tests__/critical-regressions.test.ts
    - src/__tests__/mutex-dead-holder-verdict.test.ts
  key_links:
    - "critical-regressions.test.ts detects holders with /- name: Acquire shared-test-db mutex|61616158/ over each job body INCLUDING comments, so every comment inside test-db-drift must name the key by name, not by number (D-08)."
    - "The acquire and release step bodies of python and e2e-seeded stay byte-identical to each other; the byte-identity pin compares them."
    - "scripts/pg-lane/mutex-dead-holder-lane.sh extracts the FIRST `Release shared-test-db mutex (best effort)` step in ci.yml (python's), so deleting test-db-drift's copy does not move what it executes."
---

<objective>
Take `test-db-drift` off the shared-test-db key (D-02) and move every pin that measured it as a
holder, in ONE atomic commit, with each new or moved pin proven able to fail (D-07).

Purpose: `test-db-drift` waited up to 20m31s on the key for 2-5 s of read-only VAC-08 work, and that
wait put merge pushes above Phase 164.4.2's BEFORE band. The ordering VAC-08 needs is already
delivered by the wait step that runs before the acquire; the key adds nothing VAC-08 relies on.

Output: one commit touching `ci.yml` and two test files. ⛔ Tasks 1 and 2 do NOT commit; Task 3 makes
the plan's single commit, so `main` never carries a `ci.yml` whose pins are red.

Why Task 2's comment-currency work stays here rather than in plan 03 (plan-check W4, decided):
it edits the same two files this plan's single commit already owns, so keeping it here means no commit
ever carries `ci.yml` YAML with two holders beside prose saying three; moving it to plan 03 would put
six files in plan 03 (above the five-file bound) and make two plans own `ci.yml`. Plan 03 keeps the
docs outside `ci.yml` and the tests.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
<!-- planner-discipline-allow: Acquire shared-test-db mutex -->
@.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-CONTEXT.md
@.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-RESEARCH.md

<!-- verify-symbol: .github/workflows/ci.yml wait-for-test-schema-apply.sh -->
<!-- verify-symbol: .github/workflows/ci.yml test-ledger-drift-check.sh -->
<!-- verify-symbol: src/__tests__/critical-regressions.test.ts DB_JOBS -->
<!-- verify-symbol: src/__tests__/mutex-dead-holder-verdict.test.ts DEAD_HOLDER_ANNOTATION -->

**Environment (executors need node_modules).** This checkout has no `node_modules`. Before any
vitest run: `MAIN="$(dirname "$(git rev-parse --git-common-dir)")"`, then from the repo root
`[ -e node_modules ] || ln -s "$MAIN/node_modules" node_modules`. Confirm a real run (the test count
in vitest's output is non-zero) before trusting a green — research Pitfall 5 observed the main
checkout's `.bin/vitest` fail to load this checkout's config without the symlink. Before committing,
remove it with `[ -L node_modules ] && rm node_modules`, and confirm `git status --short` does not
list it.

**Byte backups, not git, for every neuter.** Take `cp` backups into a directory from `mktemp -d`
(NEVER a fixed name in the shared scratchpad — parallel agents share it). Re-take the backup after
every intended edit. Restore by `cp` and prove the restore with `cmp`. ⛔ Never `git checkout --`,
`git restore` or `git stash`: they destroy uncommitted work.

⛔ Constraints:
- ⛔ No DB command, no workflow dispatch, no push.
- ⛔ Do NOT edit any line inside the `Acquire shared-test-db mutex` or
  `Release shared-test-db mutex (best effort)` steps of `python` or `e2e-seeded`.
- ⛔ Do NOT add or remove a literal of the key number anywhere OUTSIDE `test-db-drift`: the file-wide
  count is a measured prediction (see Task 3).
- ⛔ Do NOT touch `scripts/test-ledger-drift-check.sh` (D-09), `timeout-minutes` (D-04) or the
  `frontend` aggregator.
- ⛔ Cite by symbol in every comment you write. No line numbers in prose that outlives the commit.
- ⛔ Repo is PUBLIC: no DSN, host, username or absolute path in any file or commit message.
- ⛔ Never write a CI skip token in the commit message, not even to deny it.
- ⛔ Never stage `.planning/milestone.lock` or `.planning/state.json`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End to end — delete test-db-drift's three mutex steps and make the re-measured pins green</name>
  <files>.github/workflows/ci.yml, src/__tests__/critical-regressions.test.ts, src/__tests__/mutex-dead-holder-verdict.test.ts</files>
  <read_first>
    - `.github/workflows/ci.yml` job `test-db-drift` IN FULL (from its `  test-db-drift:` key to the `  e2e:` key)
    - `src/__tests__/critical-regressions.test.ts` describe `shared-test-db serialization via the advisory-lock mutex (D-05 / Phase 158)` in full, including its helpers `jobSlice`, `expectMatch`, `findOrFail`, `readText`
    - `src/__tests__/mutex-dead-holder-verdict.test.ts` Test 1 and Test 2
    - `164.4.2.1-RESEARCH.md` `## Tests and counts that must move (Question 3)` and `## Code Examples`
  </read_first>
  <action>
Per D-02 and D-07.

Before editing, record the baselines you will compare against, into the executor's own notes (not
into a tracked file): `grep -o 61616158 .github/workflows/ci.yml | wc -l`; the per-job match count for
`test-db-drift` (awk over the job span, from the `  test-db-drift:` line to the next two-space job key);
the output of `actionlint .github/workflows/ci.yml` (so Task 3 can require NO NEW finding rather than
zero findings); and a sha256 of each of these regions of `test-db-drift`, so Task 3 can prove them
byte-identical: the `needs:` block, the `if:` line, the `timeout-minutes:` line, the `permissions:`
block, the `Wait for the TEST schema apply to conclude (merge pushes only)` step from its `- name:` line
through the end of its `run:` body, and the `VAC-08 - repo-vs-TEST ledger and function body drift` step
from its `- name:` line through the end of its `run:` body.

1. In `ci.yml`, delete from `test-db-drift` exactly three steps with their preceding comment blocks:
   `Acquire shared-test-db mutex`, `Release shared-test-db mutex (best effort)` and
   `Verdict — shared-test-db mutex holder died mid-job`. Nothing else in the job is deleted.
2. Replace every remaining literal of the key number inside `test-db-drift` (the job header comments
   and the ⛔ lineage block) with its NAME, "the shared-test-db key", changing no other word of those
   sentences (D-08). The job's literal count must then be 0.
3. In `critical-regressions.test.ts`: change `DB_JOBS` to `["python", "e2e-seeded"]`. Add the
   `test-db-drift` negative as its OWN `it`, placed directly after the holder-set `it` (not inside it):
   vitest reports only the FIRST failing `expect` in an `it`, so a negative placed after the `DB_JOBS`
   equality could never be seen failing on its own — any neuter that trips it also trips the equality
   first. The new `it` computes the holder set the same way the holder-set `it` does (reuse the same
   detection expression and helpers; do not write a second detector) and asserts
   `holders.includes("test-db-drift")` is false, with a message naming Phase 164.4.2.1 D-02 and why
   (VAC-08 is read-only, it is ordered by the `Wait for the TEST schema apply` step, and holding the key
   made it wait up to 20m31s for 2-5 s of work). Title it so it says `test-db-drift` holds and names no
   shared-test-db key. Leave the holder-set `it`'s title and its `sql-tests` assertion unchanged (the
   title names only `sql-tests`, which stays true).
4. Add a new `it` in the same describe, titled so it states that `test-db-drift` runs the ordering wait
   BEFORE VAC-08 and keeps its TTL and `needs: python` (its only ordering control since 164.4.2.1). Using
   `jobSlice(readText(".github/workflows/ci.yml"), "test-db-drift")`, assert: the index of
   `- name: Wait for the TEST schema apply to conclude (merge pushes only)` is found; the index of
   `- name: VAC-08 - repo-vs-TEST ledger and function body drift` is found; the wait index is smaller.
   Then SLICE THE WAIT STEP itself: from the wait's `- name:` index to the next line that starts a step
   (six spaces then `- `), or the end of the job. On that slice assert (so a DISABLED wait fails, not only
   a moved or deleted one): its `if:` line equals, exactly, the `if:` line the step carries today (read it
   from the live file in Task 1's baseline — it ends in `github.event_name == 'push' }}`); and the slice
   (not merely the job body) contains the invocation `bash "${GITHUB_WORKSPACE}/scripts/wait-for-test-schema-apply.sh"`.
   On the job body assert: a line that is exactly ten spaces then `bash scripts/test-ledger-drift-check.sh`;
   `timeout-minutes: 90` at four-space indent; the `needs:` block lists `- python` and `- changed-paths`.
   Each assertion is its own `expect` with its OWN distinct message, so the message a neuter produces
   names the one condition it broke.
   Every assertion carries a message saying what regressed and why it matters (SC-2 ordering, SC-4 no
   timeout raised). The research's `## Code Examples` sketch is the shape; use the file's existing
   helpers, not new ones.
5. In `mutex-dead-holder-verdict.test.ts` Test 2, lower the floor from 5 to 4 in BOTH the
   `toBeGreaterThanOrEqual` argument and the message, and add a dated comment above it:
   2026-09-25, Phase 164.4.2.1 D-07 — a holder LEFT (`test-db-drift` no longer takes the key, so its
   release step and dead-holder annotation went with it); the floor follows the measured corpus and this
   is not a relaxation. Keep the title's "anti-vacuity floor" words; change only the number in it.
6. Symlink `node_modules` (see context), then run the two test files. Both must be green, and the run
   must report a non-zero test count.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(awk '/^  test-db-drift:$/{f=1;next} f&&/^  [a-z0-9_-]+:$/{f=0} f' .github/workflows/ci.yml | grep -c 61616158)" = 0 && test "$(awk '/^  test-db-drift:$/{f=1;next} f&&/^  [a-z0-9_-]+:$/{f=0} f' .github/workflows/ci.yml | grep -c -e 'Acquire shared-test-db mutex' -e 'Release shared-test-db mutex' -e 'Verdict — shared-test-db mutex')" = 0 && node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts src/__tests__/mutex-dead-holder-verdict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test-db-drift` carries no acquire, release or verdict step, and no literal of the key number.
    - `DB_JOBS` has exactly two entries; the new negative assertion and the new ordering `it` exist.
    - Test 2's floor reads 4 with a dated reason.
    - Both files pass with a non-zero test count. Nothing is committed yet.
  </acceptance_criteria>
  <fails_when>Any mutex step or key literal survives in `test-db-drift`, or either test file is red or reports a zero test count.</fails_when>
  <done>The single path ci.yml → holder-set pin → ordering pin → floor pin is green end to end, uncommitted.</done>
</task>

<task type="auto">
  <name>Task 2: Bring ci.yml's prose and the test titles current (comments only, dated, lineage kept)</name>
  <files>.github/workflows/ci.yml, src/__tests__/critical-regressions.test.ts</files>
  <read_first>
    - `.github/workflows/ci.yml` the `python` job header, and the comments immediately ABOVE its `Wait for the TEST schema apply…`, `Acquire shared-test-db mutex`, `Release shared-test-db mutex (best effort)` and `Verdict — shared-test-db mutex holder died mid-job` steps; the same four places in `e2e-seeded`; the `e2e-seeded` job header. Find each with `grep -n` on `test-db-drift`, `all three`, `third taker` and `copy of the same verdict step`, restricted to the `python` and `e2e-seeded` job spans.
    - `164.4.2.1-RESEARCH.md` `### What stays true, and why` and `### Prose/runtime text to bring current`
  </read_first>
  <action>
Per D-02, D-04 and D-08. Comments only; no YAML key, value or `run:` line changes in this task.

1. `test-db-drift` header: add a dated block at the TOP of the job's comment, headed
   `⛔ CORRECTED 2026-09-25 (Phase 164.4.2.1 DRIFTOFFMUTEX, D-02): this job no longer holds the
   shared-test-db key.` State in it: VAC-08 is read-only; its ordering after `apply-test` is the
   `Wait for the TEST schema apply…` step (an Actions-API fact plus the in-flight flag), which ran before
   the acquire already; the key only made it queue behind `python` and `e2e-seeded` (up to 20m31s for
   2-5 s of work, 164.4.2-MEASUREMENT); `needs: [python, changed-paths]` is KEPT because it makes the
   same-commit `supabase-migrate.yml` run register long before the wait's appearance grace expires and
   because the mypy contract names it as the downstream backstop for a skipped `python`; the long ⛔ block
   below and the "WHY THIS JOB STILL HOLDS THE KEY" / "THE HONEST CONSEQUENCE" paragraphs are LINEAGE and
   stay in place because `python` and `e2e-seeded` reference the ⛔ block by name (D-08). Do not delete or
   rewrite those older paragraphs beyond the Task 1 literal-to-name substitution.
2. `test-db-drift` TTL comment above `timeout-minutes: 90`: append a dated line saying there is no mutex in
   this job since Phase 164.4.2.1, the value is left unchanged per D-04 (SC-4 forbids raising it; lowering
   it is out of scope), and the pg_sleep sentence now applies to `python` and `e2e-seeded` only.
3. `test-db-drift` comments ABOVE the wait step and ABOVE `actions/setup-node` (the VAC-08 block): append a
   dated correction to each where it says the mutex orders or encloses the work ("The mutex below
   guarantees…", "IT SITS BEFORE THE ACQUIRE", "Placed AFTER the mutex acquisition…"): in this job there
   is no acquire since 164.4.2.1 and the wait is the only ordering control. ⛔ The wait step itself (from its
   `- name:` line through its `run:` body) and the VAC-08 step stay byte-identical, including the stale
   "The acquire step below installs psql conditionally too" comment inside the wait's `run:` body — record
   that stale line in the SUMMARY as known and deliberately left.
4. `python` and `e2e-seeded`: where a comment says "all three", "the third taker is now `test-db-drift`",
   "identical in `test-db-drift`, `python` and `e2e-seeded`", or "copy of the same verdict step at the
   test-db-drift" job, append a dated `(2026-09-25, Phase 164.4.2.1: `test-db-drift` left the key; the
   holders in ci.yml are `python` and `e2e-seeded`)` note, keeping the original words as lineage. Where a
   note points the reader at a `test-db-drift` step that no longer exists, re-point it to the
   surviving twin in the other job BY STEP NAME. ⛔ Only comments ABOVE a step's `- name:` line or in a job
   header; never a line inside the acquire or release steps. Add no literal of the key number.
5. `critical-regressions.test.ts`: update test titles and failure messages that say "all three" or
   "three" about the DB_JOBS holders ("all three DB-touching jobs acquire the mutex…", "…carry the mutex
   TTL…", "the three Acquire shared-test-db mutex steps are byte-identical…", "all three release steps…",
   "…in all three acquire steps", and the comments near them) to say "both" or "every holder" — no
   assertion logic changes. Add one dated paragraph at the end of the describe's leading comment block:
   Re-subjected by Phase 164.4.2.1 (D-02 / D-07), 2026-09-25 — `test-db-drift` left the key; `DB_JOBS` is
   `python` and `e2e-seeded`; the ordering pin now carries SC-2 for `test-db-drift`, and its TTL and
   `needs: python` moved into that pin because the per-holder TTL loop no longer covers it.
6. Re-run the two test files; still green.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(awk '/^  test-db-drift:$/{f=1;next} f&&/^  [a-z0-9_-]+:$/{f=0} f' .github/workflows/ci.yml | grep -c 61616158)" = 0 && grep -q 'Phase 164.4.2.1' .github/workflows/ci.yml && ! grep -nE 'it\("(all three|the three) ' src/__tests__/critical-regressions.test.ts && node_modules/.bin/vitest run src/__tests__/critical-regressions.test.ts src/__tests__/mutex-dead-holder-verdict.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every changed `ci.yml` line in this task is a comment line (check with `git diff -U0 .github/workflows/ci.yml` — no added or removed non-comment line beyond Task 1's three step deletions).
    - No `it(` title in the describe still claims three holders.
    - The acquire and release steps of `python` and `e2e-seeded` are unchanged (the byte-identity pin is green).
  </acceptance_criteria>
  <fails_when>A test title still claims three holders, a key literal re-enters `test-db-drift`, or a non-comment line changed.</fails_when>
  <done>Prose in ci.yml and the test file tells the same story as the YAML: two holders, and test-db-drift ordered by the wait alone.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter → RED → restore every new or moved pin, run every gate, make the single commit</name>
  <files>.github/workflows/ci.yml, src/__tests__/critical-regressions.test.ts, src/__tests__/mutex-dead-holder-verdict.test.ts</files>
  <read_first>
    - The diff of Tasks 1 and 2 (`git diff`), to confirm the byte-identity baselines recorded in Task 1
  </read_first>
  <action>
Per D-07 (a pin that cannot fail is worse than none). Take fresh `cp` backups of `ci.yml` and both
test files into a `mktemp -d` directory FIRST. Each neuter below is applied, the named test is run
and observed RED with the named message, then the file is restored by `cp` and `cmp` proves the restore
before the next neuter. Record each RED (the failing test title and the first line of its message) in
the SUMMARY.

1. Holder pins — what vitest can show: it reports the first failing `expect` of EACH `it`, so two
   separate `it`s can both be seen RED, but two assertions inside one `it` cannot.
   (a) Insert one comment line containing `- name: Acquire shared-test-db mutex` into the `test-db-drift`
   job → TWO `it`s go RED: the holder-set `it` (the `DB_JOBS` equality message) AND the new
   `test-db-drift` negative `it` (its own D-02 message). Record both titles. Restore.
   (b) Repeat with a comment line containing only the key number → the same two `it`s RED. Restore.
   (c) Equality alone: insert one comment line containing only the key number into the `changed-paths`
   job → ONLY the holder-set `it` is RED (equality message); the `test-db-drift` negative `it` stays
   GREEN. Record that. Restore.
2. Ordering pin — one neuter per assertion, each observed RED with THAT assertion's own message (if a
   different assertion's message fires, the neuter hit two conditions; redo it narrower):
   (a) move the whole `Wait for the TEST schema apply…` step to after the VAC-08 step → order message;
   (b) delete the wait step → wait-not-found message;
   (c) in the wait step's `if:`, replace `github.event_name == 'push'` with `github.event_name == 'never'`
   → `if:` message (the wait is DISABLED, not moved);
   (d) change the wait step's invocation path to `scripts/wait-for-test-schema-apply-x.sh` → invocation message;
   (e) change the VAC-08 `run:` line `bash scripts/test-ledger-drift-check.sh` to `bash scripts/test-ledger-drift-check-x.sh`
   → drift-check-line message;
   (f) change `test-db-drift`'s `timeout-minutes: 90` to `120` → TTL message;
   (g) delete `- python` from `test-db-drift`'s `needs:` → needs-python message;
   (h) delete `- changed-paths` from `test-db-drift`'s `needs:` → needs-changed-paths message.
   Restore by `cp` and prove with `cmp` after each.
3. Floor: set Test 2's floor back to 5 → RED, and its message reports the measured count as 4 (this proves
   the floor equals the measured corpus). Restore. If the measured count is not 4, STOP and report.
4. After the last restore, `cmp` all three files against the backups taken at the start of this task.

Then run every gate, and require each:
- `node_modules/.bin/vitest run` over every test file that reads ci.yml: the list is
  `git grep -l 'workflows/ci.yml' -- src/__tests__` (measured at planning: 17 files; re-measure and use
  the live list). All green, non-zero test count.
- `bash scripts/test-ledger-drift-check.sh --self-test`, `bash scripts/wait-for-test-schema-apply.sh --self-test`,
  `bash scripts/mutex-dead-holder-verdict.sh --self-test`, `node scripts/lint-sql-gates.mjs --self-test`.
  All exit 0; record each script's own printed check count in the SUMMARY (do not restate a count from the
  research).
- `actionlint .github/workflows/ci.yml` reports no finding that was not in Task 1's baseline.
- The file-wide count of key-number MATCHES (`grep -o 61616158 .github/workflows/ci.yml | wc -l`, which counts
  matches where `grep -c` counts lines) equals the Task 1 baseline minus the Task 1 per-job `test-db-drift`
  count (predicted, on the research's line counts, 29 − 12 = 17). Record the measured value. If it differs, find why before
  committing; do not adjust the prediction to fit.
- The six Task 1 region hashes of `test-db-drift` (needs, if, timeout-minutes, permissions, wait step, VAC-08
  step) are unchanged.
- `grep -c -- '- name: Release shared-test-db mutex (best effort)' .github/workflows/ci.yml` is 2 (the lane
  drill extracts the first, `python`'s).

Remove the `node_modules` symlink. Stage the three files by explicit path; confirm
`git diff --cached --name-only` lists exactly those three. Commit ONCE:
`ci(164.4.2.1-02): test-db-drift stops waiting on the shared-test-db key`, with a body stating what was
deleted, what stayed byte-identical, which pins moved and the measured key count, ending with the
attribution lines the orchestrator supplies.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test ! -e node_modules && L="$(git log -1 --name-only --format=)" && test "$(printf '%s\n' "$L" | LC_ALL=C sort | tr '\n' ' ')" = '.github/workflows/ci.yml src/__tests__/critical-regressions.test.ts src/__tests__/mutex-dead-holder-verdict.test.ts ' && PREV="$(git show HEAD^:.github/workflows/ci.yml)" && B="$(printf '%s\n' "$PREV" | grep -o 61616158 | wc -l)" && J="$(printf '%s\n' "$PREV" | awk '/^  test-db-drift:$/{f=1;next} f&&/^  [a-z0-9_-]+:$/{f=0} f' | grep -o 61616158 | wc -l)" && A="$(grep -o 61616158 .github/workflows/ci.yml | wc -l)" && test "$J" -ge 1 && test "$A" -eq "$((B - J))" && bash scripts/test-ledger-drift-check.sh --self-test >/dev/null && bash scripts/wait-for-test-schema-apply.sh --self-test >/dev/null && bash scripts/mutex-dead-holder-verdict.sh --self-test >/dev/null && echo PLAN02-COMMIT-OK</automated>
  </verify>
  <acceptance_criteria>
    - Every neuter in steps 1-3 was observed RED with the message of the assertion it targets, neuter 1(c) left the `test-db-drift` negative GREEN, and every restore was proven by `cmp`.
    - All listed gates pass; the key count is measured and equals the prediction (or the discrepancy is explained before commit).
    - Exactly one commit, touching exactly the three files; no symlink, lock or state file staged.
  </acceptance_criteria>
  <fails_when>By design, this verify FAILS LOUD if Tasks 1 and 2 were committed separately: it reads the pre-change key count from `HEAD^`, so an earlier partial commit makes `J` 0 and `test "$J" -ge 1` exits 1. Do not weaken that check to make a split history pass; squash the plan's work into one commit instead. It also fails on a wrong file set, a key count that is not baseline minus the job's count, or a red self-test.</fails_when>
  <done>The change and its pins are in one commit, and each pin has been seen to fail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → shared TEST database | `test-db-drift` reads TEST with `TEST_SUPABASE_DB_URL`; TEST is shared with other people's CI |
| workflow log → public | this repo is public; every log line is world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.4.2.1-03 | Tampering (gate integrity) | VAC-08 verdict without the key | high | mitigate | no interleaving yields a false GREEN (research interleaving table); the ordering wait is kept byte-identical and newly PINNED before VAC-08 (Task 1 step 4), neutered RED in Task 3 |
| T-164.4.2.1-04 | Repudiation | a silently skipped VAC-08 | high | mitigate | `if:`, `needs:` and the aggregator row unchanged; existing aggregator pin stays green; new pin reds on a changed TTL or a lost `needs: python` |
| T-164.4.2.1-05 | Information disclosure | DSN in logs | medium | mitigate | no psql call is added; the Release step that carried the DSN into a server-side reap is deleted, so the secret's exposure SHRINKS |
| T-164.4.2.1-06 | Tampering (test vacuity) | moved pins | medium | mitigate | neuter → RED → `cmp`-proven restore for each (Task 3) |
| T-164.4.2.1-07 | Denial of service | a loud false RED when a later apply or a restore overlaps VAC-08 | low | accept | pre-existing under arbitrary lock order; fails loud; D-03 revisits only if observed |
</threat_model>

<verification>
Task 3's gate list, all green, bound to the single commit this plan makes.
</verification>

<success_criteria>
`test-db-drift` holds no key, keeps every other line of its YAML byte-identical, and the pins that
say so can each be shown to fail.
</success_criteria>

<output>
Create `.planning/phases/164.4.2.1-driftoffmutex/164.4.2.1-02-SUMMARY.md` when done, carrying the
measured key count, each self-test's printed check count, and every neuter's observed RED.
</output>
