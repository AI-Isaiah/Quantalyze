---
phase: 164.6.4
plan: 04
type: execute
wave: 3
depends_on: ["164.6.4-02"]
files_modified:
  - analytics-service/services/mt5_session_episodes.py
  - analytics-service/tests/test_mt5_session_episodes.py
autonomous: true
requirements: ["164.6.4-C1", "164.6.4-C2"]
coupling_justified: ["164.6.4-03: this plan IMPORTS `_heal_guard_defects` and `_HEAL_GUARD_MUTANTS` from `tests/test_mt5_relogin.py`, which plan 03 WRITES in the same wave — reader/writer on a shared mutable file with no files_modified overlap. Order-independent by construction: both imported names, and the `required_names` parameter this plan relies on, are established in WAVE 2 by plan 02, and plan 03's action forbids editing `_heal_source` or the shared predicate's existing behaviour — it only ADDS sibling feeders and a second predicate. Either order yields the same import surface."]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A row is written ONLY on a state TRANSITION — a tick that observes the state already recorded writes nothing at all (criterion 1, D-5)."
    - "The open episode is re-read from the database on every observation, so an analytics deploy cannot truncate a session lifetime; there is no in-process episode state to reset."
    - "At most ONE open row survives per `cron_name`, restored by every write — two overlapping containers on a Railway deploy, or a crash between a close and its paired open, can never leave an orphan that nothing will ever close and that no other assertion in this plan can see."
    - "A SUPERSEDED close is excluded from the lifetime dataset a successor would compute, and a genuine close is included — proven by computing that dataset in the test rather than by asserting a field exists."
    - "A `not_measured` reading — a busy skip, a budget abandon, or a `code=0` sentinel — closes NOTHING, because it did not measure that the session recovered."
    - "A `code=0` fault is recorded `unattributed`, never `authorized` and never `dark`: `-6` is only observable through the second `last_error()` round-trip, so when THAT is broken the fault classifies as 0 and is healed not at all (IN-02)."
    - "Every row carries the attribution limit IN THE ROW: the detector cannot tell WHOSE account is authorized (criterion 1, researcher C-6)."
    - "A sink failure never changes what the caller observes — the recorder cannot raise and cannot alter a verdict or its log line (T-153.3-24)."
    - "No credential, account number, gateway host, port or terminal-supplied free text appears in ANY field of ANY written row (T-164.6.2-12)."
    - "A run of consecutive `not_measured` readings that exceeds the independent instrument's own window escalates its log level, because a blind instrument reporting nothing looks exactly like a healthy one."
  artifacts:
    - path: "analytics-service/services/mt5_session_episodes.py"
      provides: "the transition-only, stateless, never-raising episode recorder with its honesty fields and blind-window escalation"
      contains: "record_mt5_session_reading"
    - path: "analytics-service/tests/test_mt5_session_episodes.py"
      provides: "the dataset-honesty gates, each calibrated"
      contains: "def test_"
  key_links:
    - from: "analytics-service/services/mt5_session_episodes.py"
      to: "analytics-service/services/db.py :: db_execute"
      via: "every Supabase call runs on the bounded executor so the event loop is never blocked"
      pattern: "db_execute"
    - from: "analytics-service/tests/test_mt5_session_episodes.py"
      to: "analytics-service/tests/test_mt5_relogin.py :: _heal_guard_defects"
      via: "the ONE shared never-raises predicate, called with an empty required-names tuple"
      pattern: "_heal_guard_defects"
---

<objective>
The dataset is the deliverable, and it has a hole that must be recorded IN THE ROWS.

This phase exists so a LATER decision can choose the keepalive interval against MORE THAN ONE
measured session lifetime. Everything that makes those rows trustworthy is this plan: that a row
means a TRANSITION and not a tick; that an analytics deploy cannot silently shorten a lifetime; that
a reading which measured nothing never closes an episode; and — the biggest threat to the whole
dataset — that the limit on what a reading can attribute travels WITH the row rather than living in
prose somebody will not read.

⛔ The attribution hole (researcher C-6): the allocator and derive paths call `Mt5Client.login(...)`
with PER-CUSTOMER credentials on the SAME shared terminal, and `close()` never calls `shutdown()`, so
an "authorized" reading may be a customer login rather than the env account. The TRIGGER stays
correct — `-6` means nobody is authorized — but the LIFETIME figure is the part at risk. ⛔ Do NOT
close it by reading `account_info()`: that is an extra read this phase is forbidden to add, and it
would put the account number into a log on a PUBLIC repo. Put the limit in the row.

Purpose: a successor reading these rows must be able to tell a measured lifetime from a truncated
one, an unmeasured tick from a healthy one, and an attributable reading from one that merely looked
authorized.

Output: the recorder's invariants gated with teeth, and its own safety made structural.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-CONTEXT.md
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-RESEARCH.md
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-PATTERNS.md
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-02-SUMMARY.md

⛔ Do NOT `@`-inline source files. Cite BY SYMBOL, never `file:line`.
⚠️ `pytest` runs ONLY from `analytics-service/`; use `analytics-service/.venv/bin/python`.
⛔ Never probe PROD from here: A4 (does the service-role INSERT actually land?) is verified on the
first deploy by reading the row back, and that check lives in plan 02's post-deploy human checks.
⚠️ The executor's shell is zsh — calibrate every gate under zsh, not just bash.
</context>

<tasks>

<task type="auto">
  <name>Task 1: A row means a TRANSITION, and every row carries what it cannot claim</name>
  <files>analytics-service/services/mt5_session_episodes.py, analytics-service/tests/test_mt5_session_episodes.py</files>
  <read_first>
    `analytics-service/services/mt5_session_episodes.py` in full — it is this phase's own new module.
    `supabase/migrations/20260408113029_cron_heartbeat.sql`: the `cron_runs` columns, the `status`
    CHECK, `idx_cron_runs_running`, the RLS policies and `latest_cron_success`.
    `analytics-service/services/mt5_relogin.py`: `_VERDICT_ALREADY_AUTHORIZED`, `_VERDICT_HEALED`,
    `_VERDICT_NOT_HEALED_PREFIX`, `_not_healed`'s composed reasons, and the outcome type plan 02 added.
    `analytics-service/tests/test_mt5_relogin.py`: `_install_client`, `_FakeMt5` — the double style to
    copy, in particular that it APPENDS every remote call to a round-trip list so drift is caught by
    a count rather than by a reading.
  </read_first>
  <action>
Write `analytics-service/tests/test_mt5_session_episodes.py` and harden
`analytics-service/services/mt5_session_episodes.py` against what it finds. Drive the recorder
directly against an in-memory fake Supabase table that RECORDS every write it receives — the write
LIST is the lever that catches per-tick drift, exactly as the round-trip list catches budget drift in
the shipped heal tests.

(a) PER TRANSITION, NEVER PER TICK. Seed an open AUTHORIZED run, feed an `authorized` reading, and
assert ZERO writes. Seed an open DARK run, feed a `dark` reading, assert ZERO writes. Then feed the
opposite reading in each case and assert EXACTLY TWO writes in order — the close of the old run and
the open of the new one — with the right `metadata.state` on each. ⭐ The anti-vacuity leg matters
more than the positive one: assert the write list is EMPTY, not merely that it is short. At a
ten-minute cadence a per-tick write is about 144 rows a day, forever, into a shared and gated table,
for no information.

(b) THE OPEN EPISODE IS RE-READ, NOT REMEMBERED. Seed an open run, run a reading, then DISCARD every
piece of module state the recorder holds and run another reading — the second must still continue the
seeded run rather than opening a second one. ⭐ The property being gated is that the DATABASE is the
state: Railway deploys analytics on every green main CI, so a process-memory `authorized_since` would
be reset several times a day and every recorded lifetime would be biased SHORT — corrupting the exact
number this phase exists to let a successor choose. Assert there is no module-level episode state at
all, so the boot re-read is satisfied BY CONSTRUCTION rather than by remembering to do it.

⛔ (b2) A SECOND CONCURRENT WRITER, AND THE ORPHAN IT LEAVES. Statelessness is precisely the design
that must tolerate TWO writers, and Railway OVERLAPS CONTAINERS ON DEPLOY — the old one drains while
the new one boots — so two monitor loops can observe the same terminal for a window. Two opens, or a
crash between a close and its paired open, leaves an older `running` row that NOTHING WILL EVER
CLOSE: a read that takes only the newest self-heals and never looks back, the orphan persists forever
with a null `completed_at`, and the episode it represents is LOST from the very lifetime dataset
criterion 1 exists to build. ⚠️ No assertion elsewhere in this plan can see it — a per-transition
check, an honesty-field check and a `not_measured` check are all satisfied by a table carrying
orphans. Gate it here:

  - Seed TWO open rows for the `cron_name` and run one observation. Assert the NEWEST is treated as
    live, that the older is CLOSED rather than left open, and — the anti-vacuity leg — that a
    subsequent read finds EXACTLY ONE open row. ⛔ Assert the surviving open count is 1, not merely
    that "an" open row exists.
  - Assert the read is not `limit 1`: with three open rows seeded, ALL the stale ones close, not just
    the second-newest. A read that looked at one row would leave the third orphaned and still pass a
    two-row test.
  - Assert the invariant is restored by the WRITE and not only by a lucky read: seed one open row,
    drive an observation that OPENS a new run, and assert the pre-existing row was closed in the same
    operation.
  - Simulate the crash case: a CLOSED row with no open row. Assert the next observation opens one
    carrying `started_at_is_lower_bound`, and writes no second row.

⭐ AND THE HALF THAT KEEPS THE FIX FROM CORRUPTING THE DATASET IN A NEW WAY: a superseded close must
stay distinguishable from a genuine dark-window end. Assert `close_is_measured` is present on EVERY
closed row, `false` exactly on the superseded ones and `true` exactly on those closed by an observed
opposite reading; assert a superseded close carries the superseded closing kind and the error status
REGARDLESS of the run's state, so `status='ok'` keeps meaning "an authorized run whose end we
MEASURED"; and assert its `completed_at` is the time it was NOTICED rather than a measured
transition. ⛔ Then the falsifier that makes all of that worth having: compute a lifetime dataset the
way a successor would — filtering on `close_is_measured` — and assert a superseded row is EXCLUDED
from it while a genuine one is included. A discriminator nothing filters on is decoration.

(c) A `not_measured` READING CLOSES NOTHING. Seed an open authorized run and feed, in turn, a busy
skip, a budget abandon, and a `code=0` sentinel. Each must write NOTHING and leave the seeded run
OPEN. ⛔ This is the one that a naive implementation gets wrong: it did not measure that the session
recovered, so it may never close an episode. ⚠️ And do not copy the boot heal's busy-skip rationale —
"a busy terminal is a terminal somebody is already successfully using, which is itself evidence the
session is fine" is cheap for a once-per-boot heal and UNSOUND for a loop: it is a standing claim
about state the tick DID NOT MEASURE, and a terminal held by a FAILING job is exactly where it is
most wrong. Assert the recorded class is `not_measured`, never `authorized`.

(d) `code=0` IS RECORDED `unattributed`, NEVER GUESSED. `_raise_last` uses `0` as the sentinel for
THREE distinct faults — an unknown `last_error`, a malformed `last_error` shape, and a transport raise
converted by its own except arm. ⚠️ IN-02 and it is load-bearing for this phase: `-6` is ONLY ever
observable through the SECOND `last_error()` round-trip, so whenever THAT is the broken one the fault
classifies as `0` and the session is healed not at all — the one fault this whole mechanism exists to
catch becomes undetectable. Assert the row says `unattributed`; assert it says neither `authorized`
nor `dark`.

(e) THE HONESTY FIELDS, ASSERTED PRESENT ON EVERY WRITTEN ROW, each with its own named assertion so a
partial regression says which one vanished: `reading_attributes_account` is `false` and the
attribution sentence is present; `schema_version`; `state`; `opened_by`/`closed_by`;
`opening_kind`/`closing_kind` and `opening_code`/`closing_code`; `poll_interval_s`;
`since_previous_reading_s` with `first_reading_after_boot` true exactly when it is `None`; and
`started_at_is_lower_bound` true exactly when there was no open run to continue. ⭐ Add a
`[REDACTED]`-style anti-vacuity leg: assert at least one field was actually written, so a row that is
empty or unrelated cannot satisfy a list of presence checks.

(f) `status` IS OVERLOADED AND `metadata.state` IS THE DISCRIMINATOR. The `cron_runs` CHECK admits
only `running`, `ok` and `error`, so a dark run must close as `error` with the real meaning in
metadata. Assert the mapping in both directions — a closed AUTHORIZED run is `ok`, a closed DARK run
is `error`, an open run of either kind is `running` — and assert `metadata.state` is present on every
row, because a reader who infers the state from `status` alone will mis-infer. ⛔ Never add
`FORCE ROW LEVEL SECURITY` to `cron_runs` while hardening this: the heartbeat migration's own note
calls it "the one clause under which owning a table stops being an exemption" and it would silently
drop the dormancy rows.

(g) CALIBRATION, for every assertion added. Byte-backup the file to the scratchpad first; ⛔ NEVER
`git checkout --` to restore — it restores to HEAD and silently destroys concurrent uncommitted work.
Neuter, RUN, observe RED, restore, re-run green, and record every observation. ⚠️ A neuter that does
not APPLY reads as GREEN — assert the mutated text differs before believing a red.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_session_episodes.py -q</automated>
    <automated>cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <done>
A tick that observes the already-recorded state writes NOTHING (asserted empty, not merely short); a
transition writes exactly the close and the open; a discarded-module-state re-run still continues the
seeded run; two and three seeded open rows both collapse to EXACTLY ONE with the stale ones closed as
`superseded`, an open restores the invariant in the same operation, and a superseded close is proven
EXCLUDED from a successor-shaped lifetime computation while a genuine close is included; a busy skip,
a budget abandon and a `code=0` each write nothing and close nothing; a
`code=0` row says `unattributed`; every honesty field is asserted present by name with an
anti-vacuity leg; the `status`/`metadata.state` mapping is asserted in both directions; every
assertion has a recorded neuter → RED → restore observation.
  </done>
</task>

<task type="auto">
  <name>Task 2: The instrument cannot harm what it measures, and it cannot go blind quietly</name>
  <files>analytics-service/services/mt5_session_episodes.py, analytics-service/tests/test_mt5_session_episodes.py</files>
  <read_first>
    `analytics-service/services/mt5_client.py`: `emit_mt5_stage_event` — the posture to copy
    ("instrumentation must not change what a caller observes", T-153.3-24) and its never-raises shape.
    `analytics-service/tests/test_mt5_relogin.py`: `_heal_guard_defects` and the `required_names`
    widening plan 02 added; `_assert_no_credential_value_escaped`; `_log_configuration_fault_once`'s
    throttle in `analytics-service/services/mt5_relogin.py`.
    ⚠️ MEASURED at HEAD: `analytics-service/tests/__init__.py` exists and
    `from tests.test_mt5_relogin import _heal_guard_defects` imports cleanly under `pythonpath = .`.
    Import the predicate; ⛔ do not copy it.
  </read_first>
  <action>
(a) THE RECORDER IS STRUCTURALLY NEVER-RAISING. Feed its source to the shared `_heal_guard_defects`
with an EMPTY required-names tuple — it has no kill switch of its own, being instrumentation disabled
by its caller not running — and assert no defects. Run the shipped mutant table over its source too,
so the assertion is calibrated rather than merely present; give it its own anchors if the shared
needles do not resolve, and keep the rule that every mutant must PARSE and produce a NAMED defect.
⚠️ `except BaseException` must never wrap an `await` here: `asyncio.CancelledError` is NOT an
`Exception` subclass on this venv, so `except Exception` correctly lets shutdown cancellation through
while `except BaseException` would swallow it and hang `lifespan`'s 10-second gather. ⚠️ And never
`except OSError` on this path — `asyncio.TimeoutError is TimeoutError` and its MRO runs through
`OSError`, so an `OSError` handler silently eats a budget expiry.

(b) A SINK FAILURE CHANGES NOTHING. Make the Supabase read raise, then the write raise, then both;
each time assert the recorder returns `None`, raises nothing, and emits exactly one warning. Then
assert the CALLER-VISIBLE half at the recorder boundary: the return value and the exception behaviour
are identical to the healthy case, so a sink outage cannot alter a verdict, a log line, or a
severity. ⭐ A DB write inside a never-raising path is a NEW raise source; this is the property that
keeps it from becoming one.

(c) NO SECRET IN ANY FIELD, EVER. Drive a full transition with the fake credential fixtures and a
fake gateway host and port in scope, then scan EVERY field of EVERY written row — including nested
metadata values and the `error` column — for each literal INDIVIDUALLY, so a partial leak names which
value escaped rather than reporting that something did. ⛔ The row must carry the verdict CLASS and
the CODE, never the composed verdict string: its tail is terminal-supplied text a broker is free to
echo the submitted account or server into. Add the anti-vacuity leg — assert the row was non-empty,
or the absences pass on nothing.

(d) A BLIND INSTRUMENT MUST NOT LOOK LIKE A HEALTHY ONE. Consecutive `not_measured` readings mean the
mechanism has measured nothing for that many intervals, and at INFO forever that is indistinguishable
from a quiet, healthy system — which is the defect class this entire phase exists to remove, pointed
at the fix. Escalate the log level once the consecutive-blind RUN has covered at least as long as the
independent hourly instrument's own window, deriving the threshold from the cadence so a retune
carries through rather than stranding a hand-typed count. Reset on any measured reading. ⚠️ The
counter is in-process and resets on deploy; say so at the site and say why it is acceptable — it
gates a LOG LEVEL, never a dataset value, and the dataset's own state lives in the database.
Assert the threshold is DERIVED (recompute it from the cadence in the test) and assert the escalation
in both directions: below the threshold it stays at the quiet level, at and above it escalates, and a
single measured reading resets it.

(e) THE MODULE HEADER EARNS ITS PLACE. Record what this sink IS and what it deliberately is NOT: that
`public.cron_runs` was chosen by founder decision (D-5) because it needs no migration, no reviewer
gauntlet and no floor ratchets while the dark window stays open; that a dedicated table is the
semantically exact successor and why it was deferred; that the table's `COMMENT` says "Heartbeat rows
written by cron jobs" and this is a stretch of that, stated rather than hidden; and that
`latest_cron_success` takes a NAME and does not enumerate them, so a new `cron_name` raises no false
stale alerts. ⛔ Never add `FORCE ROW LEVEL SECURITY` to the table.

(f) CALIBRATION DISCIPLINE, as Task 1(g).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_session_episodes.py -q</automated>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_session_episodes.py tests/test_mt5_relogin.py tests/test_mt5_session_monitor.py -q</automated>
    <automated>cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <done>
The recorder satisfies the shared never-raises predicate with an empty required-names tuple and its
own calibrated mutant run; a raising read, a raising write and both together each leave the recorder
returning `None` with exactly one warning and nothing raised; no credential, host or port literal
appears in any field of any row, asserted individually with an anti-vacuity leg; the blind-window
escalation threshold is DERIVED from the cadence and asserted in both directions plus the reset; the
module header records the sink choice, its named successor, its semantic stretch and the
`FORCE ROW LEVEL SECURITY` prohibition; every observation recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 terminal → the episode row | `last_error()` text and MT5 error codes cross into a durable record that outlives every log. |
| analytics worker → Supabase (`public.cron_runs`) | A service-role write into a SHARED, gated table whose dormancy instrument and grant posture were last edited days ago. |
| the recorder → its caller | Instrumentation must not change what the caller observes; a new DB write inside a never-raising path is a new raise source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.4-18 | Information disclosure | A credential, account number, gateway host, port, or terminal-supplied `last_error()` tail written into a durable row on a service the founder queries from a browser | high | mitigate | Task 2(c) — the row carries the verdict CLASS and the CODE only; every field of every row is scanned per literal with an anti-vacuity leg. |
| T-164.6.4-19 | Repudiation | Silently corrupted lifetimes — a deploy truncating an open episode, a per-tick write flooding the table, or a `not_measured` reading closing an episode it never measured | high | mitigate | Task 1(a)(b)(c) — statelessness asserted, empty-write-list asserted, and `not_measured` proven to close nothing. |
| T-164.6.4-19b | Repudiation | Two concurrent recorders during a Railway container overlap, or a crash between a close and its paired open, leaving an orphaned `running` row that no read will ever revisit and no other assertion in the plan can see | high | mitigate | Task 1(b2) — all open rows read (never `limit 1`), the newest treated as live, every older one closed as `superseded`, the invariant restored by every WRITE, and the surviving open count asserted to be exactly 1. |
| T-164.6.4-19c | Tampering | A superseded close read back as a measured dark-window end or a measured session lifetime — corrupting the dataset in a NEW way while fixing the old one | high | mitigate | Task 1(b2) — `close_is_measured` on every closed row, a superseded close forced to the error status and the superseded kind regardless of state, and a falsifier that computes the successor's lifetime dataset and asserts the superseded row is EXCLUDED from it. |
| T-164.6.4-20 | Spoofing | A reading recorded as attributable to the env account when a per-customer login on the same terminal produced it | high | mitigate | Task 1(e) — the attribution limit rides in every row and is asserted present by name. ⛔ Not mitigated by an extra `account_info()` read. |
| T-164.6.4-21 | Tampering | A `code=0` sentinel — three distinct faults under one code — recorded as a session state on a guess | medium | mitigate | Task 1(d) — recorded `unattributed`, asserted to be neither `authorized` nor `dark`. |
| T-164.6.4-22 | Denial of service | The recorder raising into the heal's guard, or blocking the event loop with a synchronous Supabase call | high | mitigate | Task 2(a)(b) — the shared never-raises predicate with a calibrated mutant run, plus every Supabase call on the bounded `db_execute` executor. |
| T-164.6.4-23 | Repudiation | A blind instrument at INFO forever, indistinguishable from a quiet healthy system | medium | mitigate | Task 2(d) — a cadence-DERIVED consecutive-blind threshold, asserted in both directions with a reset case. |
| T-164.6.4-24 | Elevation of privilege | Widening `cron_runs` access, or adding `FORCE ROW LEVEL SECURITY` while "hardening" the new writer | medium | mitigate | Tasks 1(f) and 2(e) — the existing service-role-write / admin-read posture is inherited unchanged and the FORCE prohibition is written into the module header. |
| T-164.6.4-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase; RESEARCH.md records the Package Legitimacy Audit as NOT APPLICABLE. |
</threat_model>

<verification>
Run from `analytics-service/`:
- `.venv/bin/python -m pytest tests/test_mt5_session_episodes.py tests/test_mt5_relogin.py tests/test_mt5_session_monitor.py -q`
- `.venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/`


⛔ A MISSING PATH IN A VERIFY LEG ABORTS COLLECTION — MEASURED 2026-09-15, zsh and bash
IDENTICALLY: pytest exits 4 with `no tests ran`, so EVERY suite listed beside the bad path silently
does NOT run. ⛔ The repair is to FIX THE PATH, never to drop it: dropping it shrinks the verified set
while the gate turns green, which is exactly how one dead path hid two whole suites in this phase’s
first cut. ⚠️ A leg naming `tests/test_mt5_session_monitor.py` or `tests/test_mt5_session_episodes.py`
does not resolve until the task that CREATES that file has run — expected at plan time, and the ONE
legitimate reason a leg here fails before its creating task completes. Any OTHER unresolved path is a
defect in the plan, not in the tree.

⛔ Nothing here is closed by relaxing an assertion, by narrowing a scan, or by widening an exemption.
Where a gate reds against the module, fix the MODULE.
</verification>

<success_criteria>
- A tick that observes the already-recorded state writes nothing; a transition writes exactly two rows.
- The recorder holds no episode state; the boot re-read is satisfied by construction and asserted.
- A `not_measured` reading closes nothing, and a `code=0` fault is recorded `unattributed`.
- Every row carries the attribution limit and the full honesty-field set, asserted individually with an anti-vacuity leg.
- No credential, host or port literal appears in any field of any row.
- The recorder is structurally never-raising, calibrated by mutants, and a sink failure leaves the caller's observation unchanged.
- The consecutive-blind escalation threshold is derived from the cadence and asserted in both directions plus reset.
- Every new assertion carries a recorded neuter → RED → restore-from-byte-backup observation.
</success_criteria>

<output>
Create `.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-04-SUMMARY.md` when done.
</output>
