---
phase: 164.6.4
plan: 02
type: execute
wave: 2
depends_on: ["164.6.4-01"]
files_modified:
  - analytics-service/services/mt5_session_episodes.py
  - analytics-service/services/mt5_session_monitor.py
  - analytics-service/services/mt5_relogin.py
  - analytics-service/main.py
  - analytics-service/tests/test_mt5_relogin.py
  - analytics-service/tests/test_mt5_session_monitor.py
autonomous: true
requirements: ["164.6.4-C1", "164.6.4-C2", "164.6.4-C3", "MT5-GATEWAY-LOGIN-01"]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every `authorized ↔ dark` transition of the terminal's broker session is written to `public.cron_runs` under a new `cron_name`, ONE ROW PER TRANSITION and never per tick (criterion 1, D-5)."
    - "The open episode is re-read from the database on every observation, so an analytics redeploy cannot truncate a session lifetime — there is no in-process episode state to reset (criterion 1, D-5)."
    - "At most ONE open row exists per `cron_name`, and that invariant is restored by every write: two overlapping containers on a Railway deploy, or a crash between a close and its paired open, can never leave an orphaned `running` row that nothing will ever close."
    - "A row closed because it was SUPERSEDED stays distinguishable from one closed by a measured opposite reading — `close_is_measured` is present on every closed row — so an anomaly can never be read as a measured session lifetime or a measured dark window."
    - "Every row carries the ATTRIBUTION LIMIT in the row itself: the detector cannot tell WHOSE account is authorized (criterion 1, researcher C-6)."
    - "A reading that measured nothing — a busy skip, a budget abandon, or a `code=0` sentinel — is recorded as `not_measured` and CLOSES NOTHING; a `code=0` fault is recorded `unattributed`, never guessed (IN-02)."
    - "Within one DETECTION POLL INTERVAL of the session lapsing, the analytics worker notices and drives the heal — with no human and no unrelated restart (criterion 2)."
    - "The monitor never logs in to measure: the only reading it takes is the credential-free detector, and the credentialed verb runs only after a `-6` (criterion 3, D-1)."
    - "The monitor loop is the SIXTH `main.lifespan` task, named and crash-tracked, and its body cannot reach `_crash_handler` (D-2)."
    - "No log line says `boot` on the monitor path — the heal's sentences name the caller that actually made them."
  artifacts:
    - path: "analytics-service/services/mt5_session_episodes.py"
      provides: "the durable, stateless, never-raising episode recorder over public.cron_runs"
      contains: "record_mt5_session_reading"
    - path: "analytics-service/services/mt5_session_monitor.py"
      provides: "the never-raising one-shot tick and the thin scheduling loop, plus the DETECTION POLL INTERVAL knob"
      contains: "mt5_session_monitor_loop"
    - path: "analytics-service/tests/test_mt5_session_monitor.py"
      provides: "the end-to-end loop behaviour tests"
      contains: "def test_"
  key_links:
    - from: "analytics-service/main.py :: lifespan"
      to: "analytics-service/services/mt5_session_monitor.py :: mt5_session_monitor_loop"
      via: "the sixth asyncio.create_task, named mt5_session_monitor"
      pattern: "create_task\\(mt5_session_monitor_loop\\(\\)"
    - from: "analytics-service/services/mt5_session_monitor.py :: run_mt5_session_monitor_tick"
      to: "analytics-service/services/mt5_relogin.py :: heal_mt5_terminal_session"
      via: "delegation — the tick adds NO second detector and NO second lease site"
      pattern: "heal_mt5_terminal_session\\("
    - from: "analytics-service/services/mt5_relogin.py :: heal_mt5_terminal_session"
      to: "analytics-service/services/mt5_session_episodes.py :: record_mt5_session_reading"
      via: "the recorder is called beside the verdict that produces it, inside the existing top-level guard"
      pattern: "record_mt5_session_reading\\("
---

<objective>
THE PHASE'S TRACER, then the expansion that makes it periodic.

Nothing triggers a recovery of the MT5 terminal's broker session, so the terminal sat ≥2h34m
confirmed dark — inside a ≈8h05m bracket — while recovery itself took at most 00:03:43. The boot heal
shipped in Phase 164.6.2 is deployed, armed and idle: it fires once at analytics startup, and
analytics startup is not correlated with the terminal losing its session.

Task 1 is the TRACER: the thinnest end-to-end path through every layer this phase touches —
credential-free detector → verdict → durable row in `public.cron_runs` — proved on the heal that is
ALREADY wired. After it, the founder can query session-state transitions that previously existed
only as log lines that scroll past (`[MT5-VERDICT-SINK-01]`, measured: `railway logs` returned ZERO
matching lines on 2026-09-05).

Task 2 is the expansion: the same path, now driven on a DETECTION POLL CADENCE by a sixth
`main.lifespan` task, so the system notices without waiting on an unrelated restart.

⛔ THIS PHASE DOES NOT CHOOSE THE KEEPALIVE INTERVAL (criterion 1, D-3, D-06 of Phase 164.6.2).
Wave 5 produced exactly ONE observation of each quantity and an interval set from n=1 is the guess
D-06 exists to prevent. What ships here is a DETECTION POLL CADENCE — a different number, derived
from instrument bounds and ZERO lifetime observations — and the dataset that lets a successor choose
the keepalive interval against n>1.

Output: two new `services/` modules, a sixth lifespan task, and one row per transition in a table
that already exists.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-CONTEXT.md
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-RESEARCH.md
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-PATTERNS.md

⛔ Do NOT `@`-inline the source files. Grep by SYMBOL and Read only those regions:
`analytics-service/services/mt5_relogin.py` is 900 lines, `analytics-service/tests/test_mt5_relogin.py`
is 2565, `analytics-service/main.py` is 942. Cite BY SYMBOL, never `file:line`
(`[164.7-CITATION-DRIFT-01]`).

⚠️ `pytest` runs ONLY from `analytics-service/`; `python` is not on PATH — use
`analytics-service/.venv/bin/python`. ⛔ Never start a local `uvicorn main:app`; never `railway up`
from local; ⛔ no VNC, no login by any instrument, no `services/mt5_probe.py` (its body IS a login).

⚠️ The executor's shell is zsh. `$VAR` does not word-split, `set -- $x` leaves `$2` empty, and
`test "$a" \> "$b"` is not portable. Calibrate every gate under zsh, not just bash — this bit Phase
164.6.2 four times.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a session-state transition lands in a durable row" — the boot heal path only</name>
  <files>analytics-service/services/mt5_session_episodes.py, analytics-service/services/mt5_relogin.py, analytics-service/tests/test_mt5_relogin.py</files>
  <read_first>
    `analytics-service/services/mt5_relogin.py`: `heal_mt5_terminal_session`, `_heal_blocking`,
    `_not_healed`, `_describe_exception_for_log`, `_log_configuration_fault_once`, and the verdict
    constants `_VERDICT_ALREADY_AUTHORIZED` / `_VERDICT_HEALED` / `_VERDICT_NOT_HEALED_PREFIX`.
    `analytics-service/services/db.py`: `get_supabase`, `db_execute`.
    `analytics-service/services/mt5_client.py`: `emit_mt5_stage_event` — copy its posture verbatim
    ("instrumentation must not change what a caller observes", T-153.3-24) and its never-raises shape.
    `analytics-service/services/job_worker.py`: `_upsert_report` — the writer analog
    (`db_execute(lambda-free closure)` over a `supabase.table(...)` call).
    `supabase/migrations/20260408113029_cron_heartbeat.sql` — the `cron_runs` columns, the partial
    index `idx_cron_runs_running`, the RLS policies, and `latest_cron_success`.
    `analytics-service/tests/test_mt5_relogin.py`: `_FakeMt5`, `_install_client`,
    `_install_lease_counter`, `_set_full_env`, `_records`, `_outcome_records`,
    `_assert_no_credential_value_escaped`, `_STARTING_FRAGMENT`.
  </read_first>
  <action>
Create `analytics-service/services/mt5_session_episodes.py` and wire it into the shipped heal so that
one real transition reaches the database end-to-end. ⛔ Fully annotated — `services/` is inside the
`mypy --strict --follow-imports=silent` gate — and it must earn real coverage, because `services/`
is in the `--cov` set behind `--cov-fail-under=80`.

(a) THE SINK, AND WHY THIS ONE. Per D-5 (founder, 2026-09-15) the sink is `public.cron_runs` under a
NEW `cron_name` — NO MIGRATION. Verified: the table is service-role writable by its existing
`cron_runs_service_role` policy; `status='running'` IS the open episode and `idx_cron_runs_running`
already supports reading it back; `latest_cron_success(p_cron_name)` takes a NAME and does not
enumerate them, so a new name raises no false stale alerts.

⚠️ THE GATE CLAIM, RESTATED PRECISELY, because the loose version is wrong in a way that would mislead
whoever checks it. It is NOT true that the ledger gates "never count `cron_runs` rows": migration
`20260911130000` DOES insert into the table and four SQL gates DO count it. What is true — and it is
the operative fact, VERIFIED at HEAD — is that every one of those counts is NARROWED by `cron_name`
(and in places by `error` and a metadata key), and the migration forbids ITSELF any unfiltered count
over `public.cron_runs`, on the stated reasoning that the instrument is asserted by the presence of
its INSERT in the body text and never by a row a tick may or may not have written yet. So a NEW
`cron_name` cannot perturb any existing gate. ⛔ Re-measure that narrowing before relying on it rather
than trusting this paragraph, and ⛔ never introduce an unfiltered count yourself. ⛔ NEVER add
`FORCE ROW LEVEL SECURITY` to `cron_runs` — the heartbeat migration's own note calls it "the one
clause under which owning a table stops being an exemption", and it would silently drop the dormancy
rows. Name a successor in the module header: a dedicated `mt5_session_episodes` TABLE is the
semantically exact home and was deliberately deferred because it drags a migration, three reviewers,
a founder apply gate and two floor ratchets, while the dark window stays open meanwhile.

(b) THE RECORD MODEL — one row per observed STATE-RUN, because that is what yields BOTH numbers the
successor needs. An AUTHORIZED run's duration is the session LIFETIME (what chooses the keepalive
interval); a DARK run's duration is the EXPOSURE this phase exists to collapse. `started_at` is when
the run was first observed, `completed_at` when the opposite state was first observed,
`status='running'` while open. On close, `status` is `ok` for an authorized run and `error` for a
dark run. ⛔ `status` is OVERLOADED — its CHECK admits only `running`/`ok`/`error` — so the real
discriminator is `metadata.state` ∈ {`authorized`, `dark`}; say so at the write site or a reader
will mis-infer.

`metadata` carries, and nothing else: `schema_version`; `state`; `opened_by` / `closed_by` (the
source that made the observation); `opening_kind` / `closing_kind` (the verdict CLASS, not the
composed verdict string); `opening_code` / `closing_code`; `poll_interval_s` in effect;
`since_previous_reading_s`; `first_reading_after_boot`; `started_at_is_lower_bound`;
`reading_attributes_account` (always `false`) and a one-sentence `attribution_limit`.
⛔ NO login, NO password, NO server, NO gateway host, NO port, NO terminal key, and NO free-text
detail from `last_error()`. Names only (T-164.6.2-12), and the endpoint is not even a name. Put the
verdict CLASS and the CODE in `error` when closing a dark run — never the composed verdict string,
whose tail is terminal-supplied text. RESEARCH Open Question 5 settled: omit `terminal_key`; one
terminal exists and a second is a schema problem for the phase that adds one.

⭐ `reading_attributes_account: false` is the biggest threat to the n>1 dataset this phase exists to
start (researcher C-6) and criterion 1 requires it IN THE ROWS, not only in prose: the allocator and
derive paths call `Mt5Client.login(...)` with PER-CUSTOMER credentials on the SAME shared terminal
and `close()` never calls `shutdown()`, so an "authorized" reading may be a customer login rather
than the env account. The TRIGGER stays correct — `-6` means nobody is authorized — but the LIFETIME
figure is the part at risk. ⛔ Do NOT "fix" it by reading `account_info()`: that is an extra read the
phase is forbidden to add, and it would put the account number into a log on a PUBLIC repo.

(c) STATELESS BY CONSTRUCTION, WHICH IS HOW THE BOOT RE-READ IS SATISFIED. `record_mt5_session_reading`
reads the open row from the database on EVERY observation — `cron_name` matches, `status='running'`,
newest `started_at` — and decides from that row alone. There is therefore NO in-process episode state
to reset, and an analytics redeploy (Railway deploys on every green main CI) cannot bias a lifetime
short. Write that reasoning at the site: D-5's warning is answered by having no memory, not by
remembering harder.

Decision table, exactly:
  - reading is `not_measured` → write NOTHING, close NOTHING, return. It did not measure that the
    session recovered, so it may never close an episode.
  - an open row exists and its `metadata.state` equals the observed state → write NOTHING. ⭐ This is
    what makes the sink per-TRANSITION rather than per-tick; at a ten-minute cadence a per-tick write
    would be ~144 rows/day forever into a shared, gated table, for no information.
  - an open row exists and differs → close it, then open a run for the observed state.
  - no open row exists → open one, with `started_at_is_lower_bound: true`, because its start is when
    we first LOOKED, not when the state began.

⛔ (c2) MORE THAN ONE OPEN ROW IS REACHABLE AND MUST BE HANDLED, NOT ASSUMED AWAY. A stateless
recorder is exactly the design that has to tolerate TWO WRITERS, and Railway OVERLAPS CONTAINERS ON
DEPLOY — the old one drains while the new one boots — so two monitor loops can observe the same
terminal for a window. Two opens, or a crash between a close and its paired open, leaves an older
`running` row that NOTHING WILL EVER CLOSE: a read that takes the newest row self-heals and never
looks back, so the orphan persists forever with a null `completed_at` and the episode it represents
is LOST from the very lifetime dataset criterion 1 exists to build.

  - ⛔ Read ALL open rows for the `cron_name`, never `limit 1`. One row is the normal path; ZERO and
    MORE-THAN-ONE are both real and both have defined behaviour.
  - More than one → the NEWEST by `started_at` is the live one; close every OLDER one as
    `superseded` and log at WARNING, because it is an anomaly and not a normal path.
  - On OPENING any row, first close any pre-existing `running` row for that name as `superseded`. The
    invariant "at most one open row" is then restored by every WRITE, not only by a read that
    happened to look.

⭐ A SUPERSEDED ROW MUST STAY DISTINGUISHABLE FROM A GENUINE DARK-WINDOW END, or this fix corrupts
the dataset in a new way instead of an old one. Every CLOSED row carries `close_is_measured`: `true`
when the close was driven by an OBSERVED opposite reading, `false` when it was superseded. A
superseded close additionally sets `closing_kind` to the superseded value and `status` to the error
value REGARDLESS of the run's state — so `status='ok'` keeps meaning "an authorized run whose end we
MEASURED", and a successor computing lifetimes filters on `close_is_measured` and can never read an
anomaly as a measured session lifetime or a measured dark window. Its `completed_at` is the time we
NOTICED, not a measured transition, exactly as `started_at_is_lower_bound` marks the mirror case.

(d) THE MEASURED LATENCY BOUND (criterion 2's "MEASURED rather than assumed"). True detection latency
is unknowable — nothing observes the lapse. What IS measurable, and what the criterion should be read
as demanding, is a BOUND. Hold ONE module-level monotonic timestamp of the previous MEASURED reading
and record `since_previous_reading_s` on each transition. That number is MEASURED wall-clock, not a
restatement of the configured cadence. On the first reading after a process start it is `None` and
`first_reading_after_boot` is `true` — ⛔ do not substitute the cadence constant for it; an assumed
number wearing a measured name is the defect class this phase is about.

(e) NEVER RAISES, AND NEVER CHANGES THE VERDICT. The recorder's whole body sits inside ONE top-level
`try` / `except Exception`, whose handler body is itself exactly one nested `try` with an
ARGUMENT-FREE `except BaseException` fallback log — the same IN-06 shape `heal_mt5_terminal_session`
carries and for the same reason (`logger.warning`'s arguments are evaluated before logging is
entered). ⚠️ Bind the caught exception `as exc` in every `except Exception` arm written in this phase:
`tests/test_mt5_relogin.py::_HEAL_GUARD_MUTANTS` anchors its splices on that exact line shape, and an
arm written without it turns the mutant table's "the anchor is no longer in the source" assertion red
rather than gating anything.

⭐ SO THE SHARED PREDICATE CAN REACH THIS MODULE, widen
`tests/test_mt5_relogin.py::_heal_guard_defects` with a keyword-only `required_names` tuple
defaulting to the single kill-switch name it hard-codes today. The default keeps every existing call
site byte-identical; the recorder is checked with an EMPTY tuple because it carries no kill switch of
its own — it is instrumentation, disabled by its caller not running. ⛔ Widen the ONE predicate;
⛔ never copy it — a copied predicate is the drift shape this repo has a dated record of, and the copy
is what rots. Add a calibration case proving the new parameter BITES: the predicate must name a
defect when a required name it was given is absent from the guard body.

⛔ `except BaseException` must NEVER wrap an `await`: `asyncio.CancelledError` is NOT an
`Exception` subclass on this venv (measured, Python 3.12.13), so `except Exception` correctly lets
shutdown cancellation through while `except BaseException` would swallow it and hang `lifespan`'s
10-second gather. ⛔ And never `except OSError` on this path: `asyncio.TimeoutError is TimeoutError`
and its MRO runs through `OSError`, so an `OSError` handler silently eats a budget expiry. A sink
failure must leave the heal's verdict and log line byte-identical — copy `emit_mt5_stage_event`'s
posture. All Supabase work goes through `services.db.db_execute` so the event loop is never blocked.

(f) STRUCTURED OUTCOME INSTEAD OF STRING PARSING. `_heal_blocking` currently returns the composed
verdict `str`. Change it to return a `NamedTuple` whose `.verdict` field is that string BYTE-UNCHANGED
— every existing log line and the `startswith(_VERDICT_NOT_HEALED_PREFIX)` severity branch keep
reading `.verdict`, so every log-content assertion in `tests/test_mt5_relogin.py` is unaffected
(MEASURED at HEAD: no test asserts `_heal_blocking`'s return value; the tests that touch it
monkeypatch it with a raiser). The other fields carry the FIRST probe's reading and code and the
FINAL reading and code, plus the verdict CLASS. ⛔ Do not parse the verdict string for the class —
that is the string-shape dependency plan 01 just removed from `initialize_with_credentials`.

Map reading from the CODE, not from the kind, and write the table at the site:
  - class `already_authorized` or `healed` → `authorized`
  - code `-6` → `dark`. It is the ONE code meaning the bridge ANSWERED and NO ACCOUNT IS AUTHORIZED.
  - any other code, including `0` → `not_measured`, and when the code is `0` also mark the row
    `unattributed`. ⚠️ IN-02 and it is load-bearing: `-6` is only ever observable through the SECOND
    `last_error()` round-trip, so when THAT is the broken one the fault classifies as `0` and is
    healed not at all. The record must say `unattributed`, never `authorized` and never `dark`.
  - a bounded-acquire expiry (`Mt5TerminalBusyError`) and a budget abandon (`asyncio.TimeoutError`)
    → `not_measured`.

⛔ Do NOT copy the boot heal's busy-skip RATIONALE. It reasons that "a busy terminal is a terminal
somebody is already successfully using, which is itself evidence that the session is fine" — cheap
and defensible for a once-per-boot heal, and UNSOUND for a loop: it is a standing claim about session
state the tick did not measure, and a terminal held by a FAILING job is exactly where it is most
wrong. Still SKIP (never queue ahead of real work) and still log at INFO, but record `not_measured`.

(g) TWO OBSERVATIONS PER TICK WHEN A HEAL RUNS, and this is what makes the dark window measurable at
all: report the FIRST probe's reading, then — only if the credentialed heal actually ran — the FINAL
reading. A tick that finds `-6` and heals it produces exactly TWO transitions (authorized→dark,
dark→authorized) and therefore exactly two rows, while a tick that finds the session healthy produces
ZERO. Call the recorder from inside `heal_mt5_terminal_session`'s guard body and from inside its
nested `Mt5TerminalBusyError` and `asyncio.TimeoutError` handlers. ⛔ NEVER from the outer catch-all:
the never-raises AST gate requires that handler's body to be EXACTLY one nested `try`, and an extra
statement there is a named defect.

(h) THE CALLER'S NAME IN THE RECORD. Give `heal_mt5_terminal_session` a KEYWORD-ONLY `source`
parameter defaulting to the boot value, plus a module-level label mapping so each in-heal log line
names its actual caller. Bind the label as the FIRST statement inside the guard, beside the existing
`credentials` binding, so the outer handler can never hit an unbound local; make the lookup total,
naming an unknown source rather than silently mislabelling it. `main.lifespan`'s existing call and
the "returns None on every path" contract are unchanged — the keyword default keeps them
byte-identical. ⚠️ The SEVEN messages emitted by the module-scope configuration helpers still say
"boot heal"; they are TRUE until a second caller exists, and Task 2 owns making them honest.

(i) THE TRACER'S OWN TEST, in `tests/test_mt5_relogin.py`, driving the shipped heal against the
existing `_FakeMt5` doubles with the recorder pointed at an in-memory fake sink: a `-6` scenario that
heals writes exactly TWO rows in the right order with the right states, codes and classes; an
`already_authorized` scenario against a seeded open authorized row writes ZERO rows; a busy skip and
a budget abandon each write ZERO rows and close nothing; and `_assert_no_credential_value_escaped`
plus a field-by-field scan prove no credential, host or port value reached any written row.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_relogin.py -q</automated>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_relogin.py -q -k "never_raises_predicate or body_sits_entirely or handler_body_cannot_escape"</automated>
    <automated>cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <done>
`services/mt5_session_episodes.py` exists, is fully annotated, never raises, and writes one row per
transition into `public.cron_runs` under the new `cron_name`; the shipped boot heal drives it
end-to-end; a `-6`-then-heal tick produces exactly two rows and a healthy tick produces none; every
row carries `reading_attributes_account: false` and the attribution sentence; no credential, host or
port value appears in any field of any row; the existing never-raises AST gate over
`heal_mt5_terminal_session` is still green; mypy --strict is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: The same path, now on a DETECTION POLL CADENCE — the sixth lifespan task</name>
  <files>analytics-service/services/mt5_session_monitor.py, analytics-service/main.py, analytics-service/services/mt5_relogin.py, analytics-service/tests/test_mt5_session_monitor.py, analytics-service/tests/test_mt5_relogin.py</files>
  <read_first>
    `analytics-service/main_worker.py`: `dispatch_loop`, `watchdog_loop`, `daily_enqueue_loop`,
    `SHUTDOWN` — the house periodic idiom.
    `analytics-service/main.py`: `lifespan`'s `tasks` list, `_bridge_healthz`, `_crash_handler`, and
    the shutdown `finally` block with its 10-second gather.
    `analytics-service/services/mt5_relogin.py`: `_env_float`, `_relogin_budget_s`,
    `_relogin_lease_wait_s`, `_MT5_RELOGIN_ROUND_TRIPS`, `_MT5_RELOGIN_CONNECT_SLACK_S`,
    `_MT5_RELOGIN_BUDGET_CEILING_S`, `_log_configuration_fault_once`, and the seven module-scope
    configuration messages that name the boot heal.
    `analytics-service/services/closed_sets.py`: `mt5_enabled_server`.
    `analytics-service/tests/test_mt5_relogin.py`: `_lifespan_node`, `_heal_task_lines`,
    `_create_task_lines`, `_named_call_lines`, `_LIFESPAN_CREATE_TASK_COUNT_AT_164_6_2`,
    `test_CRITERION_1_lifespan_starts_the_heal_exactly_once_as_a_task`,
    `test_the_heal_task_is_named_and_tracked_like_the_worker_loops`,
    `_excise_the_heal_entry`, `test_the_criterion_1_predicate_reds_on_a_mutant_with_the_entry_excised`.
  </read_first>
  <action>
Create `analytics-service/services/mt5_session_monitor.py` with TWO symbols and wire the loop into
`main.lifespan` as the SIXTH task (D-2). ⛔ Fully annotated; `services/` is inside `mypy --strict`
and the coverage set.

(a) TWO SYMBOLS, AND THE STRUCTURAL REASON FOR TWO. `run_mt5_session_monitor_tick()` is the
never-raising ONE-SHOT, shaped exactly like `heal_mt5_terminal_session`: whole body inside one
top-level `try` with a single `except Exception` whose handler body is itself one nested `try` with an
argument-free `except BaseException`, no `finally:` and no `else:`, and the kill-switch read FIRST
before anything is constructed. `mt5_session_monitor_loop()` is the thin scheduler whose `while` lives
INSIDE its own single top-level guard. Two symbols, not one, because the AST predicate's rule is
"exactly ONE top-level statement and it must be the `try`" — inlining the tick into the loop would
make the loop's guard body carry both the tick and the `while`, and the per-tick survival property
would stop being separately statable.

⚠️ NAMING, and it is a deliberate correction to CONTEXT D-2's suggested `probe_mt5_session_once`:
that name claims a read-only probe, and this function may send a credential after a `-6`. A symbol
that names the wrong thing is precisely this repo's recorded defect class, and criterion 3 is about
exactly that distinction. The SHAPE D-2 locks (two symbols; the loop's `while` inside its own single
guard) is honoured exactly; only the name is corrected, and the reason is recorded at the site.

(b) THE TICK DELEGATES; IT DOES NOT RE-IMPLEMENT. `run_mt5_session_monitor_tick()` reads
`mt5_enabled_server()` first, and on true awaits `heal_mt5_terminal_session(source=<the monitor
source>)`. ⛔ Do NOT write a second detector, a second lease acquisition, a second budget or a second
verdict vocabulary. The shipped heal already does, in order: kill switch → credentials → gateway
endpoint → BOUNDED lease by `mt5_terminal_key` → `to_thread` under a budget → credential-free probe →
branch on code → heal ONLY `-6` → RE-PROBE → classified verdict, and its five-round-trip budget is
already gated by `test_the_budget_covers_every_round_trip_the_worst_case_path_makes`. ⭐ Delegating
adds ZERO entries to `_PRODUCTION_LEASE_SITES` and zero new `Mt5Client` lifetimes to reason about —
write that as the argument, because it is the reason the roster stays at six. When the kill switch is
off, SKIP the tick and keep sleeping (so flipping it on takes effect without a redeploy); log once
through the existing throttle, NAMING the variable. ⛔ Never `return` from the loop on a disabled
switch — that would end the loop for the life of the process.

(c) THE DETECTION POLL CADENCE, AND IT IS NOT THE KEEPALIVE INTERVAL. Add exactly ONE knob,
`MT5_SESSION_POLL_INTERVAL_S`, read PER CALL through the shipped `_env_float` from
`services.mt5_relogin` (import it under its leading underscore, with a comment saying why, exactly as
`mt5_relogin` imports `_redact_credential_values` from `mt5_client`: it is the SHIPPED control and a
second copy is the drift its own gate exists to prevent). Its properties come from `_env_float`
unchanged — absent/blank/non-numeric/zero/negative/nan/inf all fall back to the default,
`math.isfinite` is tested FIRST, the effective window can never be empty from either end, and the
VALUE is never logged, only the NAME and the fault class.

State the derivation so a reader can check it rather than trust it, and state that NO session-lifetime
observation enters it — which is exactly why choosing it does not violate criterion 1:
  - FLOOR = the heal's declared budget CEILING. A tick must never still be running when the next one
    starts, or the loop would overlap itself against a shared terminal.
  - CEILING = 3600 seconds, the cadence of the independent hourly prod-prober. At or above it the
    loop measures nothing that instrument does not already measure, so it adds contention without
    adding information.
  - DEFAULT = 600 seconds, inside that window. Against the measured exposure (≥2h34m confirmed dark)
    any cadence in the ten-to-fifteen-minute band collapses the exposure by about one and a half
    orders of magnitude, and the difference between 600 and 900 is immaterial to that. Worst-case
    duty cycle is the tick budget over the cadence — about 27% in the pathological case where every
    tick both times out AND heals, and roughly one round-trip in the overwhelmingly common
    already-authorized case.

⛔ SHIP NO SECOND KNOB "ready for later". The keepalive interval is a DIFFERENT number, chosen in a
follow-up against n>1, and no symbol, env var, constant or default for it may exist anywhere in
`analytics-service/services/`. The first reader to find one will set it. ⛔ Do not introduce any
symbol or file name containing the token `keepalive` — the module is named for the SESSION MONITOR
precisely so the two numbers cannot be conflated by a later reader. (Plan 03 adds the gate that
enforces this; this task must not leave it work to do.)

(d) THE LOOP'S SHAPE. `while not SHUTDOWN.is_set():` inside the single top-level guard. EVERY
statement of the `while` body sits inside a per-tick `try` whose handler catches bare `Exception`,
logs through the IN-06 nested-fallback shape, and CONTINUES — it must contain no `break` and no
`raise`, because a handler that ends the loop is a keepalive that dies silently on tick 3 behind a
green worker, which is this phase's own defect class arriving inside the fix. Sleep with the house
idiom — `await asyncio.wait_for(SHUTDOWN.wait(), timeout=<the cadence>)`, `break` on set,
`except asyncio.TimeoutError: pass` on elapse — ⛔ never a bare `asyncio.sleep`, which `lifespan`'s
10-second gather would force-cancel on every deploy. Put the sleep INSIDE a guard too, so a sleep
that fails cannot end the loop either. ⚠️ Order the handlers so `asyncio.TimeoutError` cannot be
mistaken for "the interval elapsed" when it came from the tick: `asyncio.TimeoutError is TimeoutError`
whose MRO runs through `OSError` and `Exception`, so a tick-raised timeout reaching a sleep-shaped
handler would produce a HOT loop with no delay at all.

⛔ The loop is deliberately STRICTER than its three neighbours. `dispatch_loop`, `watchdog_loop` and
`daily_enqueue_loop` guard only their tick; their `while` header, sleep block and exit log all sit
outside any handler. For a dispatch loop that is arguably right — one that cannot sleep SHOULD take
the worker down. For a best-effort MT5 instrument it is not: `_crash_handler` calls `SHUTDOWN.set()`
on ANY background-task exception, which stops dispatch, watchdog and enqueue behind a green
`/health`. Write that asymmetry at the site in this repo's voice rather than leaving it to be
discovered.

(e) THE LIFESPAN WIRING. Add the loop to `main.lifespan`'s `tasks` list as a NAMED `create_task`
beside the existing five, so it inherits `_crash_handler` — a CONTAINMENT contract, not a crash
contract: the loop's own guard is what makes the callback inert. Put the reasoning in the entry's
comment the way the boot heal's entry does.

⛔ `_LIFESPAN_CREATE_TASK_COUNT_AT_164_6_2 = 5` in `tests/test_mt5_relogin.py` WILL RED the moment
`main.py` gains the sixth entry. That is the pin working. It is the ANTI-VACUITY leg of the
criterion-1 pin, measured independently of the symbol NAME so a silently-stopped predicate cannot
take both halves down together, and its own message says "Re-cut it deliberately." Re-cut it to 6 in
the SAME commit with the reasoning recorded; ⛔ never weaken it to `>=`. Its mutant-calibration
sibling asserts the count falls by EXACTLY ONE on the excised mutant — re-run
`test_the_criterion_1_predicate_reds_on_a_mutant_with_the_entry_excised` and confirm that arithmetic
still holds. Add a name-matched pin for the new symbol beside `_heal_task_lines`, asserting the
monitor loop is started exactly once, as a task, never awaited inline, and named.

(f) THE SEVEN SENTENCES THAT BECOME FALSE HERE. `mt5_relogin`'s module-scope configuration helpers —
the two `_env_float` fault messages, the two credential-read messages and the two gateway-endpoint
messages, plus the port one — all begin "mt5 boot heal:". They were TRUE until this task created a
second caller; they are wrong now, and `_env_float` is about to be reached from the monitor's own
cadence knob. Re-prefix them to a caller-NEUTRAL sentence that describes what they actually are —
configuration faults of the MT5 session path, not a heal attempt — and move every test assertion that
reads those fragments in the same commit. ⛔ Correct the SENTENCE, never the behaviour: the throttle,
the names-only rule and the fault classes are unchanged.

⛔ D-08's "STARTUP ONLY" MUST BE AMENDED, DELIBERATELY, IN THIS REPO'S VOICE — and this is the item
most likely to be forgotten, because nothing reds when a docstring goes false. Two sites say the heal
runs at startup and NOWHERE ELSE: the opening line of `heal_mt5_terminal_session`'s docstring, and the
`mt5_boot_heal` entry's comment in `main.lifespan`. This task is CHARTERED to make both false. Amend
them the way D-07 was amended rather than quietly contradicted, and record WHY the original hazard
does not transfer — VERIFIED two independent ways: the terminal epoch binds on FIRST TOUCH rather
than at construction (a preflight-built client has no epoch until the job's own login inside its own
lease), and the construction fence is a ContextVar that preflight never carries, which the module
itself calls "the honest encoding of 'this construction is not happening under anyone's lease'".
⚠️ A reader who finds the sentence false and the reason unwritten has found drift, which is the
record-versus-reality class this milestone exists to remove.

⛔ `[164.6.2-KILLSWITCH-COMMENT-DRIFT]` is PHASE 164.6's, not yours. Three comment blocks claim a
trailing-space "TRUE " reads the switch OFF while the shipped code strips before lowering, so it
reads ON. Do not touch them, and ⛔ never "fix the code to match the comment" — that would fail-CLOSE
a live go-live flag on a trailing space.

(g) THE END-TO-END LOOP TEST, in a new `tests/test_mt5_session_monitor.py`: the loop runs N ticks
against the `_FakeMt5` doubles with a tiny cadence and a fake sink, and a dark reading drives the
heal with no human and no restart; a tick that raises does NOT end the loop and tick N+1 still runs;
the loop exits on `SHUTDOWN` well inside the 10-second gather; the kill switch off skips the tick
without ending the loop; and — criterion 3 — on an already-authorized terminal the fake records NO
credentialed call at all (model it on `test_an_already_authorized_terminal_is_never_sent_a_credential`).

(h) CALIBRATION for every assertion added here. Byte-backup the file to the scratchpad first; ⛔ NEVER
`git checkout --` to restore — it restores to HEAD and silently destroys concurrent uncommitted work.
Neuter, RUN, observe RED, restore, re-run green, and record each observation in the SUMMARY. ⚠️ A
neuter that does not APPLY reads as GREEN — assert the mutated text differs before believing a red.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_session_monitor.py tests/test_mt5_relogin.py -q</automated>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_concurrency.py tests/test_mt5_client_contract.py -q</automated>
    <automated>cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/</automated>
    <human-check>On the FIRST deploy after merge (assumption A4 — MEASURED at HEAD: no Python code writes to `public.cron_runs` today, so the service-role INSERT has never executed): read the row back from PROD rather than assuming it landed. Confirm at least one row exists under the new `cron_name`, that its `metadata.state` and `reading_attributes_account` fields are present, and that no credential, host, port or account value appears in any field. ⛔ Run the marker query (`shobj_description` on `pg_database`) before touching any database from a SQL editor — `current_database()` is `postgres` on BOTH projects and proves nothing.</human-check>
    <human-check>Assumption A2, falsifiable post-ship at zero cost and with NO login: the `mt5linux` rpyc server is assumed to reap per-connection threads on close, so per-tick connect/close churn is bounded. If it does not, a ten-minute cadence adds roughly 144 connections a day to a container already observed with a three-digit thread counter. Read the gateway's thread counter in two prod-prober runs several hours apart after this ships and record both numbers. ⛔ Do not open a VNC session and do not log in to check.</human-check>
    <human-check>Criterion 2's cross-check, forward-dated and therefore post-deploy: compare the first recorded `dark` transition against the independent hourly prod-prober's reading of the SAME terminal over the same window. Two instruments agreeing (or disagreeing) is itself the measurement. ⛔ Do NOT mine the prober's archives backwards — they are polluted by self-test fixtures that print the not-authorized kind by name and read authorized-looking in both directions.</human-check>
  </verify>
  <done>
`services/mt5_session_monitor.py` exists with the never-raising one-shot and the thin loop; the loop
is the sixth named `create_task` in `main.lifespan`; `_LIFESPAN_CREATE_TASK_COUNT_AT_164_6_2` reads 6
with the re-cut reasoning recorded and its mutant sibling still green; a name-matched pin asserts the
monitor loop is started exactly once as a task and never awaited inline; exactly one cadence knob
exists and it is the DETECTION POLL INTERVAL, with its floor, ceiling and default derived from
instrument bounds and zero lifetime observations; no symbol or file name in `analytics-service/`
contains the keepalive-interval token; the configuration messages name the session path rather than
a boot heal, with every moved assertion re-run; mypy --strict clean; every new assertion has a
recorded neuter → RED → restore observation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 terminal → analytics worker | `last_error()` text and the terminal's authorization state cross here; the terminal is shared with live per-customer job processing. |
| analytics worker → Supabase (`public.cron_runs`) | A NEW writer crosses this boundary for the first time in Python, with the service-role key. |
| analytics worker → public log / Sentry | Exception messages and log lines reach a PUBLIC Actions log. |
| RPyC bridge (`gateway.railway.internal:8001`) | An unauthenticated arbitrary-remote-code channel. This phase adds NO new transport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.4-06 | Denial of service | `mt5_session_monitor_loop` raising into `main.lifespan._crash_handler`, which calls `SHUTDOWN.set()` and stops dispatch, watchdog and enqueue behind a green `/health` | critical | mitigate | Task 2(d) — the whole loop body inside one top-level `except Exception` with the IN-06 nested fallback, plus a per-tick guard that cannot `break` or `raise`. Plan 03 adds the calibrated AST gates for both properties. |
| T-164.6.4-07 | Denial of service | The periodic tick starving live job processing for the ONE shared terminal — jobs acquire with an UNBOUNDED wait, so a tick can make a job wait, now on a cadence and forever | high | mitigate | Delegation keeps the heal's BOUNDED acquire (skip, never queue) and its five-round-trip budget; the cadence FLOOR is the budget ceiling so ticks cannot overlap. Plan 05 proves it with a test that fails if the tick can steal the terminal. |
| T-164.6.4-08 | Information disclosure | A credential, account number, gateway host or port written into a `cron_runs` row on a database whose rows outlive every log | high | mitigate | Task 1(b) — the row carries the verdict CLASS and the CODE only, never the composed verdict's terminal-supplied tail, and a field-by-field scan asserts the three fake credential literals plus the fake host and port are absent from every written row. |
| T-164.6.4-09 | Spoofing / repudiation | An "authorized" reading attributed to the env account when it may be a per-customer login on the same terminal, silently corrupting the lifetime dataset a successor will choose an interval from | high | mitigate | Task 1(b) — `reading_attributes_account: false` and the attribution sentence ride in EVERY row. ⛔ Not mitigated by reading `account_info()`: that is a forbidden extra read and would log the account number on a public repo. |
| T-164.6.4-10 | Tampering | A `code=0` sentinel — three distinct faults sharing one code — recorded as `dark` or `authorized` on a guess | medium | mitigate | Task 1(f) — code-driven mapping; `0` records `not_measured` and `unattributed`, closing nothing. |
| T-164.6.4-11 | Elevation of privilege | Reconnaissance value of session-freshness rows | low | accept | The sink inherits `cron_runs`' existing RLS posture — service-role write, admin read, never `anon`/`authenticated` — whose own migration argues that case explicitly. ⛔ Never add `FORCE ROW LEVEL SECURITY` to the table. |
| T-164.6.4-12 | Information disclosure | Probing session freshness as an account change — a bare `initialize()` re-clearing the Expert-Advisors options | medium | accept | Falsified on THREE dated production readings behind the prober's bare `initialize()`, all showing trade permission intact. ⚠️ Scoped honestly to the BARE call; question THREE stays LIVE for the heal arm, which is why criterion 3 forbids logging in to measure and D-1 keeps detection credential-free. |
| T-164.6.4-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase; every dependency is stdlib or an existing first-party `analytics-service/services/` module. RESEARCH.md records the Package Legitimacy Audit as NOT APPLICABLE. |
</threat_model>

<verification>
Run from `analytics-service/`:
- `.venv/bin/python -m pytest tests/test_mt5_session_monitor.py tests/test_mt5_relogin.py tests/test_mt5_concurrency.py tests/test_mt5_client_contract.py -q`
- `.venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/`
- Full suite before the phase gate: `.venv/bin/python -m pytest -q` (⛔ from `analytics-service/` only — a repo-root run misses cassettes and can make LIVE broker calls).


⛔ A MISSING PATH IN A VERIFY LEG ABORTS COLLECTION — MEASURED 2026-09-15, zsh and bash
IDENTICALLY: pytest exits 4 with `no tests ran`, so EVERY suite listed beside the bad path silently
does NOT run. ⛔ The repair is to FIX THE PATH, never to drop it: dropping it shrinks the verified set
while the gate turns green, which is exactly how one dead path hid two whole suites in this phase’s
first cut. ⚠️ A leg naming `tests/test_mt5_session_monitor.py` or `tests/test_mt5_session_episodes.py`
does not resolve until the task that CREATES that file has run — expected at plan time, and the ONE
legitimate reason a leg here fails before its creating task completes. Any OTHER unresolved path is a
defect in the plan, not in the tree.

⚠️ EXPECTED REDS DURING THIS PLAN, each a pin working rather than a regression:
- `_LIFESPAN_CREATE_TASK_COUNT_AT_164_6_2 = 5` reds the moment `main.py` gains the sixth `create_task`. Re-cut to 6 deliberately, in the same commit; ⛔ never weaken it to `>=`.
- Any assertion reading the "mt5 boot heal:" fragment of the module-scope CONFIGURATION messages reds when Task 2(f) makes them caller-neutral. Move the assertion with the sentence.
⛔ Nothing here is closed by relaxing an assertion or lowering a floor.
</verification>

<success_criteria>
- A session-state transition reaches `public.cron_runs` end-to-end, one row per transition, and a healthy tick writes nothing.
- The open episode is re-read from the database on every observation; there is no in-process episode state that a redeploy could reset.
- Every row carries the attribution limit and no credential, host, port or account value.
- The monitor is the sixth named, crash-tracked `main.lifespan` task; it delegates to the shipped heal and adds ZERO new lease sites.
- Exactly one cadence knob exists, it is the DETECTION POLL INTERVAL, and no keepalive-interval symbol, env var, constant or default exists anywhere in `analytics-service/services/`.
- `mypy --strict --follow-imports=silent services/ routers/ models/` clean; the full pytest suite green from `analytics-service/`.
- Every new assertion carries a recorded neuter → RED → restore-from-byte-backup observation.
</success_criteria>

<output>
Create `.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-02-SUMMARY.md` when done.
</output>
