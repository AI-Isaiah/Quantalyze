---
phase: 164.6.4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/mt5_client.py
  - analytics-service/tests/test_mt5_client_contract.py
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: ["164.6.4-C5"]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A broker-echoed account or server literal arriving in `last_error()` text cannot reach an exception message from ANY credential-carrying arm of `Mt5Client` — the transport-raise arm and the falsy arm are now SYMMETRIC (criterion 5, D-4)."
    - "The original MT5 error code survives the redaction on the falsy arm — a heal that lost `-6` is undebuggable from a log."
    - "`Mt5Client.assert_session_authorized` still passes NO credential to `_raise_last`, and a future edit that gives it one is a named red (criterion 3)."
    - "`Mt5SessionAbandoned` raised by `_raise_last`'s own `_assert_live` still escapes UNTOUCHED (D-42) — it is never re-classified as a credential fault."
    - "`[164.6.2-RAISE-LAST-SHAPE-ONLY]` is marked CLOSED in all three places it actually lives; there is no TODOS row (measured 0 hits at HEAD, 2026-09-15)."
  artifacts:
    - path: "analytics-service/services/mt5_client.py"
      provides: "`_raise_last` parameterised by an OPTIONAL keyword-only credential triple, redacting by value at the one shared site"
      contains: "_raise_last"
    - path: "analytics-service/tests/test_mt5_client_contract.py"
      provides: "the falsy-arm SYMMETRY gate, the transport-raise gate widened to both arms, and the detector-carries-no-credential fence"
      contains: "CREDENTIAL_REDACTION"
  key_links:
    - from: "analytics-service/services/mt5_client.py :: Mt5Client.login"
      to: "analytics-service/services/mt5_client.py :: Mt5Client._raise_last"
      via: "both falsy arms now pass credentials=(login, password, server)"
      pattern: "_raise_last\\(credentials="
    - from: "analytics-service/services/mt5_client.py :: Mt5Client._raise_last"
      to: "analytics-service/services/mt5_client.py :: _redact_credential_values"
      via: "the shared by-value redactor, applied to all three raise points"
      pattern: "_redact_credential_values"
---

<objective>
Close criterion 5 — `[164.6.2-RAISE-LAST-SHAPE-ONLY]`, ROUTED IN from Phase 164.6.2 criterion 9
by founder decision on 2026-09-15.

`Mt5Client._raise_last` builds its detail from the TERMINAL's own `last_error()` text and raises
`Mt5ClientError(code, text)`, which is shape-scrubbed at construction and nothing more. That scrub
is a MEASURED no-op on the `mt5linux` kwargs-repr shape, so a broker that echoes the submitted
account or server back into `last_error()` discloses it — on a PUBLIC repo with Sentry attached.

Purpose: `_raise_last` is SHARED. `login()` reaches it and so does `initialize_with_credentials()`,
the verb plan 02's monitor drives ON A CADENCE. This plan is therefore a PRECONDITION for the rest
of the phase, not a nicety: without it the phase would knowingly multiply how often a known
disclosure path can fire, forever.

⚠️ This criterion is deliberately its OWN plan so that, if it turns out to be what holds the
keepalive back, it can be dropped to a successor without re-opening the phase's scope.

⛔ NAME WHAT THE ESCAPE HATCH COSTS, so nobody discovers it at the moment they need it. This plan is
WAVE 1 and plan 02 carries `depends_on: ["164.6.4-01"]`, with 03, 04 and 05 behind 02 — that edge is
the precondition above, encoded. So invoking the hatch is NOT free: dropping this plan strands the
whole phase until 02's `depends_on` is emptied and 02–05 are re-waved (02 → wave 1, 03/04/05 → wave
2). That is a deliberate, cheap, four-line edit rather than a re-plan, and it is written here so the
choice is made with its price visible. The alternative — running this plan in parallel with 02 —
would let the monitor's scheduled call site be written into a world where the shared raise site is
still unredacted, which is the exposure this criterion exists to close.

Output: a parameterised shared raise site; a symmetry gate where an asymmetry gate stood; a
structural fence keeping the credential-free detector credential-free; and the booked item closed
in the three prose sites that are its only home.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-CONTEXT.md
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-RESEARCH.md
@.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-PATTERNS.md

⛔ Do NOT `@`-inline `analytics-service/services/mt5_client.py` (1833 lines) or
`analytics-service/tests/test_mt5_client_contract.py` (3572 lines). Grep for the named symbols and
Read only those regions. Every symbol below was read at HEAD on 2026-09-15 and is cited BY SYMBOL,
never by `file:line` — line numbers drift (`[164.7-CITATION-DRIFT-01]`).

⚠️ `pytest` runs ONLY from `analytics-service/`; `python` is not on PATH — use
`analytics-service/.venv/bin/python`. ⛔ Never start a local `uvicorn main:app` (it claims REAL
prod compute jobs); never `railway up` from local.
</context>

<tasks>

<task type="auto">
  <name>Task 1: `_raise_last` redacts BY VALUE at the one shared site, parameterised deliberately</name>
  <files>analytics-service/services/mt5_client.py</files>
  <read_first>
    In `analytics-service/services/mt5_client.py`, read these symbols only: `_raise_last`,
    `login`, `assert_session_authorized`, `initialize_with_credentials`, `_redact_credential_values`,
    `_credential_renderings`, `Mt5ClientError.__init__`, `_assert_live`, `Mt5SessionAbandoned`.
    In `analytics-service/services/mt5_relogin.py` read `_describe_exception_for_log` — its
    two-armed shape (by value when the triple is known, shape-scrub when it is not) is the model
    this task mirrors, including the reason its `None` arm is CORRECT rather than a weakening.
  </read_first>
  <action>
Implement D-4 / criterion 5 at the shared site.

(a) SIGNATURE. Change `Mt5Client._raise_last` to take an OPTIONAL KEYWORD-ONLY credential triple —
`credentials: tuple[int, str, str] | None = None` — keeping its `NoReturn` return. ⛔ Do NOT cache
credentials on `Mt5Client`; the client stores none today and must continue to store none.

Write the NAME argument at the site, because the parameter name decides which gates fire and this
must be a decision rather than an accident. `tests/test_mt5_client_contract.py::_credentialed_client_methods`
classifies every `Mt5Client` method from source: positional params exactly the login/password/server
triple with no keyword-only params make a method DRIVABLE, and the redaction driver then tries to
make its credential-carrying transport call raise — `_raise_last` makes none, so `assert len(carrying) == 1`
would red. Naming the password parameter any other way makes the method RESIDUAL, and
`test_CREDENTIAL_REDACTION_no_password_carrying_method_escapes_the_driver` asserts RESIDUAL is empty
with the message "EXTEND THE DRIVER", which is not extendable here for the same reason. A keyword-only
`credentials` tuple is NEITHER, and that is correct rather than a dodge: the gate's class is "methods
that hand a credential to a transport call", and `_raise_last` hands credentials to nothing — it only
formats. KEYWORD-ONLY specifically so a positional mistake at any of the eight call sites is a
TypeError rather than a silent mis-bind.

(b) BEHAVIOUR. Add a small private module-level helper beside `_redact_credential_values` with the
same two arms as `mt5_relogin._describe_exception_for_log`: with a triple in scope, return
`_redact_credential_values(text, login, password, server)`; with `None`, return
`scrub_freeform_string(text)` — byte-identical to today. Apply it to ALL THREE raise points inside
`_raise_last`: the converted transport raise, the empty-`last_error` sentinel, and the final
`Mt5ClientError(code, text)`. ⛔ Reuse the shipped helper; a second copy of the loop is the drift its
own gate exists to prevent, and a raw-literal-only loop was a MEASURED live disclosure
(`[164.6.2-LOGIN-ESCAPE-BLIND]`).

(c) CALL SITES. Pass the triple from `Mt5Client.login`'s TWO falsy arms (after `initialize` and
after `login`) and from `Mt5Client.initialize_with_credentials`'s falsy arm. Leave the other five
callers — `assert_session_authorized`, `account_info`, `terminal_info`, `history_deals_get`,
`order_check` — calling it with no argument: they have no credential in scope and there is nothing
for them to redact by. ⚠️ CONTEXT D-4 says `_raise_last` is reached by two callers; MEASURED at HEAD
it is reached by EIGHT (researcher C-2). The decision is unaffected — fixing at the shared site is
still right — but restate the promise honestly at the site as "every credential-CARRYING caller
benefits, at one site, by construction", not "every caller benefits".

(d) SIMPLIFY THE NOW-DEAD UNWRAP. `initialize_with_credentials`'s falsy arm currently catches its
own `_raise_last`, strips the typed prefix with `removeprefix`, re-redacts and re-raises. With the
shared site redacting, that whole block collapses to one parameterised call. Delete it. Two
consequences to state in the replacement comment: the ORIGINAL code is preserved because
`_raise_last` raises the terminal's own code and nothing re-wraps it (strictly better than the
re-wrap it replaces, and it removes a comment that already admitted `removeprefix` degrades to a
no-op if the format changes); and `Mt5SessionAbandoned` raised by `_raise_last`'s own `_assert_live`
now escapes UNTOUCHED, which is exactly what D-42 requires — the narrow `except Mt5ClientError` arm
existed to make that structural, and deleting the whole catch makes it structural more cheaply.

⛔ KEEP `initialize_with_credentials`'s `except Exception` transport-raise arm and its DIRECT
`_redact_credential_values` call, and keep `login`'s. `tests/test_mt5_client_contract.py::_methods_routed_through_the_shared_redactor`
derives the escape-aware roster by looking for that NAME inside each method body; removing the direct
call would drop the verb out of `_ESCAPE_AWARE` and red `ESCAPE_AWARE_METHOD_FLOOR`. Re-measure
rather than trust this: before and after the edit, import the test module and print `sorted(_DRIVABLE)`,
`sorted(_ESCAPE_AWARE)` and `_RESIDUAL`, and paste both readings into the SUMMARY. They must be
`['initialize_with_credentials', 'login']`, `['initialize_with_credentials', 'login']` and `{}`.

(e) PROSE THAT BECOMES FALSE IN THE SAME COMMIT. `initialize_with_credentials`'s docstring states
that its by-value redaction is "strictly more than the shared `_raise_last` path gives `login()`'s
falsy arm" and that "that asymmetry is measured and deliberate: closing it would mean editing
`login()`, which D-07 forbids". Both sentences are false the moment this ships. Rewrite them to
record that the asymmetry is CLOSED, by what, and under which founder decision (criterion 5, routed
in 2026-09-15) — D-07's freeze was about keeping a shipped live-path method out of a REFACTOR, and
parameterising the shared site is not one. ⚠️ MEASURED at HEAD: `_redact_credential_values`'s own
docstring does NOT carry an asymmetry-freeze paragraph (PATTERNS §6 says it does; it does not) — its
D-07 paragraph is about `[164.6.2-LOGIN-ESCAPE-BLIND]`, which is CLOSED and stays true. Read it and
leave it alone unless it actually contradicts the new world.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_client_contract.py -q</automated>
    <automated>cd analytics-service && .venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <done>
`_raise_last` takes an optional keyword-only credential triple and redacts by value at all three of
its raise points when the triple is present; `login`'s two falsy arms and
`initialize_with_credentials`' falsy arm pass it; the other five callers are unchanged;
`initialize_with_credentials`' `removeprefix` unwrap is gone; the derived rosters still read
`['initialize_with_credentials', 'login']` / `['initialize_with_credentials', 'login']` / `{}` and
both readings are pasted into the SUMMARY; mypy --strict is clean.
⚠️ `tests/test_mt5_client_contract.py::test_CREDENTIAL_REDACTION_the_falsy_arm_asymmetry_is_measured_not_assumed`
IS EXPECTED TO RED at the end of this task. That is the pin working — it asserts the disclosure in
both directions and its own message says to delete that half and close the booked item. Task 2 does
exactly that. ⛔ Do NOT relax it here to get a green run.
  </done>
</task>

<task type="auto">
  <name>Task 2: the asymmetry gate becomes a SYMMETRY gate, the transport gate widens to both arms, and the booked item is closed where it actually lives</name>
  <files>analytics-service/tests/test_mt5_client_contract.py, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    In `analytics-service/tests/test_mt5_client_contract.py`, read only:
    `_credentialed_client_methods`, `_methods_routed_through_the_shared_redactor`, the module-level
    `_DRIVABLE` / `_RESIDUAL` / `_ESCAPE_AWARE` derivations, `CREDENTIALED_METHOD_FLOOR`,
    `ESCAPE_AWARE_METHOD_FLOOR` and the comment above it, `_FAKE_LOGIN` / `_FAKE_PASSWORD` /
    `_FAKE_SERVER`, the `_make` scenario helper, and these four tests:
    `test_CREDENTIAL_REDACTION_the_falsy_arm_asymmetry_is_measured_not_assumed`,
    `test_CREDENTIAL_REDACTION_a_transport_raise_discloses_none_of_the_three`,
    `test_CREDENTIAL_REDACTION_no_password_carrying_method_escapes_the_driver`,
    `test_initialize_with_credentials_falsy_return_discloses_nothing_and_keeps_the_code`.
  </read_first>
  <action>
(a) ASYMMETRY becomes SYMMETRY. Rename
`test_CREDENTIAL_REDACTION_the_falsy_arm_asymmetry_is_measured_not_assumed` to
`test_CREDENTIAL_REDACTION_the_falsy_arm_is_SYMMETRIC_across_both_drivable_verbs`, delete the
`_FAKE_SERVER in legacy_msg` assertion together with the docstring paragraph describing the
asymmetry as deliberate, and replace it with the positive form already asserted for the healing arm:
the server literal absent, the login literal absent, the redaction marker present, and
`legacy.value.code == -6`. ⛔ KEEP both password legs — that half was never asymmetric and never may
be. Rewrite the docstring to record that the criterion-5 fix CLOSED the asymmetry and that this case
now pins the symmetry in both directions.
⚠️ Before renaming, grep the repo for the OLD test name outside this file (CI selectors, scripts,
docs, `.planning/`). If any live selector names it, either keep the old name as the canonical one or
update the selector in the same commit — a renamed test that a `-k` selector no longer matches is a
gate that silently stops running.

(b) THE DERIVED GATE WIDENS RATHER THAN NARROWS. `test_CREDENTIAL_REDACTION_a_transport_raise_discloses_none_of_the_three`
is scoped to the transport-raise arm by a docstring paragraph that becomes false here. Parametrize it
over the product of `sorted(_DRIVABLE)` and the two arms, DERIVING the scenario for each rather than
hand-mapping it: the raise arm keeps today's `f"{carrying[0]}_raises"` key, and the falsy arm is
`{carrying[0]: False, "last_error": (-6, detail)}` where `detail` echoes the fake server and password
back the way a broker would. Rename it so the name says both arms. On BOTH arms assert the three
literals absent INDIVIDUALLY (so a partial redaction names which one escaped) and the marker present
(so an empty message cannot satisfy the absences vacuously); on the falsy arm ALSO assert the code is
preserved. Turning the criterion-5 fix into a STRENGTHENING of the signature-derived gate rather
than a deletion is the point — a third credentialed verb written later is fenced on both arms for
free.

(c) A NEW FENCE FOR CRITERION 3, because the detector is what plan 02's monitor calls on a cadence.
Add `test_CREDENTIAL_REDACTION_the_credential_free_detector_passes_no_credential_to_the_shared_raise`:
AST-parse the client source, find `Mt5Client.assert_session_authorized`, and assert that every
`_raise_last` call inside it has ZERO arguments and ZERO keywords. Its own docstring already states
that a detector carrying a credential could not be used to DECIDE whether to send one — it would
already have sent it. This makes that structural instead of remembered. Calibrate it: splice a
`credentials=` keyword into a COPY of the source (never the file on disk), assert the predicate names
a defect, and assert the mutated text differs from the original so a splice that failed to apply
cannot read as a pass.

(d) THE STALE COMMENT ABOVE `ESCAPE_AWARE_METHOD_FLOOR`. It reads "MEASURED 1 on 2026-09-14:
`initialize_with_credentials`. `login` carries its OWN copy of the by-value loop (D-07 keeps it
byte-unchanged), so it is NOT in this set." MEASURED at HEAD, `login` IS in the set — the D-07
amendment routed it through the shared helper — so the prose contradicts both the constant beside it
and the code. ⛔ Fix the COMMENT; the constant is right. Same class as `[164.7-CITATION-DRIFT-01]`.

(e) `test_initialize_with_credentials_falsy_return_discloses_nothing_and_keeps_the_code` — prose
update only; its assertions still hold. Its warning paragraph names the booked item and describes
the asymmetry as live.

(f) CLOSE THE BOOKED ITEM WHERE IT ACTUALLY LIVES. MEASURED at HEAD 2026-09-15:
`grep -anc "MT5-GATEWAY-LOGIN" TODOS.md` is 0 and there is no `RAISE-LAST` row either, so there is
NO TODOS row to close — the item exists only in `.planning/ROADMAP.md`, in `.planning/STATE.md`, and
in the docstring deleted in (a).

⛔ COUNT THEM; DO NOT CLOSE "BOTH PHASES" AND STOP. MEASURED at HEAD 2026-09-15 with
`grep -ao` (occurrences) and `grep -ac` (lines): `.planning/ROADMAP.md` carries the token on
**4 lines, 4 occurrences**; `.planning/STATE.md` carries it on **5 lines, 6 occurrences** — one STATE
line names it twice, which is exactly how a "two sentences" reading leaves seven sites stale while a
`done` criterion reads satisfied. Re-measure both counts BEFORE editing (they may have moved), visit
EVERY occurrence, and re-grep AFTER editing so the post-edit reading is recorded beside the pre-edit
one. ⛔ A site left claiming the item is open is the record-versus-reality class this milestone exists
to remove. Mark each CLOSED with the closing commit named. ⛔ Use SCOPED `Edit` calls only — never
`Write` on `ROADMAP.md` or `STATE.md`, and never a gsd-tools state handler here; both files are
large and a whole-file write destroys every section outside the diff window. After editing, run
`git diff --stat -- .planning/ROADMAP.md .planning/STATE.md` and confirm the changed-line count
matches the sentences you meant to touch; revert any collateral change before committing.

(g) CALIBRATION, for every assertion this task adds or strengthens. Take a BYTE BACKUP of the file
first (`cp` to the scratchpad). ⛔ NEVER `git checkout --` to restore — it restores to HEAD and
silently destroys concurrent uncommitted work in this repo's own mutation harnesses. Neuter each new
or changed assertion in turn, RUN it, OBSERVE red, restore from the byte backup, re-run green, and
record each observation in the SUMMARY. ⚠️ A neuter that does not APPLY reads as GREEN — assert the
mutated text differs from the original before believing a red.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_client_contract.py -q</automated>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mt5_abandon_fence.py tests/test_mt5_abandon_roster.py tests/test_mt5_relogin.py tests/test_mt5_concurrency.py -q</automated>
    <human-check>Post-merge, confirm on the PUBLIC repo that neither `.planning/ROADMAP.md` nor `.planning/STATE.md` gained any new credential, host, port or account value in this commit — the repo is public and `.planning/` is tracked.</human-check>
  </verify>
  <done>
The falsy-arm case asserts SYMMETRY in both directions; the derived transport gate covers both arms
for every drivable verb with the scenario derived, not hand-mapped; the credential-free detector has
a structural, calibrated fence against ever being handed a credential; the stale
`ESCAPE_AWARE_METHOD_FLOOR` comment is corrected without touching the constant;
`[164.6.2-RAISE-LAST-SHAPE-ONLY]` reads CLOSED at EVERY occurrence in `ROADMAP.md` and `STATE.md` —
pre-edit and post-edit `grep -ao`/`grep -ac` counts BOTH recorded in the SUMMARY, with zero
occurrences left claiming the item is open (the pre-edit reading at plan time was ROADMAP 4 lines /
4 occurrences and STATE 5 lines / 6 occurrences; re-measure rather than trust it) — and
`git diff --stat` shows no collateral edits; every new assertion has a recorded neuter → RED →
restore observation.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MT5 terminal → analytics worker | The terminal's own `last_error()` TEXT crosses here. A broker is free to echo the submitted account or server back into it; the worker treats it as untrusted freeform. |
| analytics worker → public log / Sentry | Exception messages cross into a PUBLIC GitHub Actions log and a Sentry project. This is the sink criterion 5 exists for. |
| repo working tree → public GitHub | `.planning/` is TRACKED on a PUBLIC repo — every plan, record and roadmap sentence is world-readable on push. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.6.4-01 | Information disclosure | `Mt5Client._raise_last` falsy arm reached by `login` and `initialize_with_credentials` | high | mitigate | Task 1 — by-value redaction at the shared site through the shipped `_redact_credential_values`, applied to all three raise points; Task 2 pins it in both directions with a derived gate covering both arms. |
| T-164.6.4-02 | Information disclosure | `mt5linux` 0.1.9 f-string-interpolates arguments into remotely-eval'd source (T-134-01), so a raw rpyc traceback carries login/password/server verbatim | high | mitigate | Unchanged control: both credentialed verbs keep their DIRECT `_redact_credential_values` call on the transport-raise arm; Task 1 explicitly forbids removing it and Task 2 re-measures the escape-aware roster. |
| T-164.6.4-03 | Information disclosure | The credential-free detector `assert_session_authorized` growing a credential by drift | medium | mitigate | Task 2(c) — a calibrated AST fence asserting every `_raise_last` call inside it carries zero arguments. |
| T-164.6.4-04 | Tampering | `Mt5SessionAbandoned` (D-42) being absorbed into a scrubbed `Mt5ClientError` and re-read as a user credential fault | medium | mitigate | Task 1(d) — deleting the unwrap makes the escape structural rather than dependent on a narrow `except`; the existing D-42 tests re-run in the verify set. |
| T-164.6.4-05 | Information disclosure | A credential, host, port or account value written into `.planning/` prose while closing the booked item | medium | mitigate | Task 2(f) restricts the edits to named sentences; the `<human-check>` re-reads the committed diff on the public repo. |
| T-164.6.4-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager install task exists in this phase. RESEARCH.md records the Package Legitimacy Audit as NOT APPLICABLE — every dependency is stdlib or an existing first-party `analytics-service/services/` module. If any plan later adds a package, the full legitimacy gate applies before it reaches a requirements file. |
</threat_model>

<verification>
Run from `analytics-service/`:
- `.venv/bin/python -m pytest tests/test_mt5_client_contract.py tests/test_mt5_abandon_fence.py tests/test_mt5_abandon_roster.py tests/test_mt5_relogin.py tests/test_mt5_concurrency.py -q`

⛔ THE D-42 REGRESSION SET IS NAMED DELIBERATELY AND MUST NOT SHRINK. Task 1(d) DELETES the
`except Mt5ClientError` unwrap whose narrowness is what makes `Mt5SessionAbandoned` escape untouched.
`tests/test_mt5_abandon_fence.py` and `tests/test_mt5_abandon_roster.py` are where that fence's
coverage actually lives (measured at HEAD: 16 passed together, and both were absent from every verify
set in the first cut of this phase's plans). ⛔ If any path in this leg does not resolve, FIX THE
PATH — never drop it: pytest aborts at COLLECTION on a missing file, so one bad path silently
un-runs every suite beside it, and dropping the path shrinks the verified set while the gate goes
green.
- `.venv/bin/mypy --strict --follow-imports=silent services/ routers/ models/`

⚠️ EXPECTED REDS DURING THIS PLAN, and each is a pin working rather than a regression:
- after Task 1 and before Task 2, `test_CREDENTIAL_REDACTION_the_falsy_arm_asymmetry_is_measured_not_assumed` reds on its `_FAKE_SERVER in legacy_msg` half. Its own message says to delete that half and close the booked item.
⛔ Nothing here is closed by relaxing an assertion, lowering a floor, or narrowing a derivation.
</verification>

<success_criteria>
- Criterion 5 closed: both drivable verbs redact the three values BY VALUE on BOTH arms, with the code preserved on the falsy arm, proven by a signature-DERIVED gate rather than a hand-listed one.
- The derived rosters did not collapse: `_DRIVABLE` and `_ESCAPE_AWARE` both read `['initialize_with_credentials', 'login']`, `_RESIDUAL` is empty, and both readings are pasted into the SUMMARY.
- `mypy --strict --follow-imports=silent services/ routers/ models/` is clean.
- Every new or changed assertion carries a recorded neuter → RED → restore-from-byte-backup observation.
- `[164.6.2-RAISE-LAST-SHAPE-ONLY]` reads CLOSED in `.planning/ROADMAP.md` (Phase 164.6.2 criterion 9 and Phase 164.6.4 criterion 5) and `.planning/STATE.md`, with no collateral edits.
</success_criteria>

<output>
Create `.planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-01-SUMMARY.md` when done.
</output>
