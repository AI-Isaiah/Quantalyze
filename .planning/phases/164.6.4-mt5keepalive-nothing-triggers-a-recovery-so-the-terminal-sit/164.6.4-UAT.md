---
status: complete
phase: 164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit
source:
  - 164.6.4-01-SUMMARY.md
  - 164.6.4-02-SUMMARY.md
  - 164.6.4-03-SUMMARY.md
  - 164.6.4-04-SUMMARY.md
  - 164.6.4-05-SUMMARY.md
started: 2026-09-16T05:05:36Z
updated: 2026-09-22T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. A4 — read the first production episode row back after deploy
expected: |
  At least one row under `cron_name = 'mt5_session_episode'`; `metadata.state` present;
  `metadata.reading_attributes_account` present and FALSE; `metadata.attribution_limit`
  present; and NO credential, host, port or account value in any field.
result: passed
reason: |
  MEASURED IN PROD 2026-09-16, after the deploy of c14665ac (analytics /health reported that
  sha at 06:53:27Z). Marker query run FIRST and it named PRODUCTION; an earlier attempt landed
  on TEST and was discarded, which is the marker doing its job.
  Row `5ef851c3-8129-4dad-95ef-c6381fdd3eaf`, started_at 2026-09-16T06:52:45.617Z,
  status `running`, completed_at NULL: `state` = `dark`, `opened_by` = `boot`,
  `opening_code` = -6, `reading_attributes_account` = false, `attribution_limit` present.
  ⭐ ALL THREE VERBS ARE NOW PROVEN, not just the INSERT: the boot re-read issued three
  filtered SELECTs that returned 200 OK, and `_confirm_row` has since written
  `metadata.last_confirmed_at`, so the compare-and-set UPDATE path ran too. The untested
  surface was the TABLE + POLICY pair and it is fully exercised.
  DISCLOSURE CHECK CLEAN: `password`, `passwd`, `server=`, `host` and `port` all match FALSE
  against `metadata::text`. A single 6-digit run matched a blunt `[0-9]{6,}` probe and was
  traced to its key — `last_confirmed_at`, i.e. the microsecond fraction of an ISO timestamp
  written by `_confirm_row`. ⛔ Not a leak; the probe needed a word boundary. `metadata_keys`
  matches the eleven keys the INSERT payload declares, and that payload carries an explicit
  fence: no login, no password, no server, no gateway host, no port, no terminal key, no
  free-text `last_error()` detail.
ledger_row: 51

### 2. A2 — does the gateway reap per-connection rpyc threads? (criterion CORRECTED 2026-09-16)
expected: |
  ⛔ THE ORIGINAL CRITERION WAS UNPASSABLE AND IS REPLACED. It read "the counter does NOT
  climb monotonically with tick count" — but CPython names threads from a globally monotonic
  counter and rpyc's ThreadedServer spawns one thread per connection, so the number climbs
  once per connection whether or not threads are reaped. A healthy server fails it.
  THE REAL FALSIFIER is `welcome` versus `goodbye` in the mt5-gateway log, scoped to the
  CURRENT process (everything after the last `server started on [0.0.0.0]:8001`): a leak is
  `welcome` running ahead of `goodbye` with the gap GROWING across two readings hours apart.
  ⚠️ A container restart resets the counter to `Thread-1` and invalidates the pair — that
  looks like a pass and is not one. Re-scope to the newest process before comparing.
result: passed
reason: |
  RESOLVED 2026-09-22 by the hours-apart reading the skip was waiting on. Readings 1 and 2 were
  27 minutes apart and cleared nothing; reading 3 is 6 days 18h 46m after the baseline.
  ⭐ THE PAIR IS VALID: the process did NOT restart in between, so the counter was never reset
  and the baseline is still comparable. Scoping evidence — exactly ONE `server started on
  [0.0.0.0]:8001` line exists in the whole window searched (from 2026-09-15T00:00Z), at
  2026-09-15T10:46:55Z, on a single deployment `9117450b` still reporting SUCCESS. ⚠️ Note the
  2026-09-21 wedge incident (Phase 164.6.5) did NOT restart this process: that recovery restarted
  `terminal64.exe` inside the container, and the rpyc server above it ran straight through.
  READING 3 at 2026-09-22T01:36:42Z — max `Thread-1039`, frontier `welcome` and `goodbye` both
  present for that same thread, 0 unclosed. Cumulative connections since the baseline: ~1012.
  Two independently sampled windows, each checked for CONTIGUITY of thread ids rather than only
  for equal totals, since a matched count can still hide one leaked id and one double-close:
    - 2026-09-21T00:00Z-06:00Z: welcome ids Thread-856..902 (47), goodbye ids Thread-856..902
      (47). Contiguous, no gaps, 0 unclosed.
    - 2026-09-22T00:00Z-01:37Z: welcome ids Thread-1020..1039 (20), goodbye ids Thread-1020..1039
      (20). Contiguous, no gaps, 0 unclosed.
  VERDICT: the gap is 0 at the baseline and 0 at reading 3. ⭐ IT IS NOT GROWING, which is the
  stated falsifier, and the sample is now adequate to the concern reading 2 raised: the
  one-in-twenty leak that would have been invisible in 3 connections would have left ~50
  unclosed threads in 1012. NO LEAK. The gateway reaps per-connection rpyc threads.
  ⚠️ Recorded as verdicts and counts only. The log lines carry peer addresses and ports; those
  are NOT reproduced here and this repository is PUBLIC.
ledger_row: 52

### 3. Criterion 2 cross-check against the independent hourly prod-prober
expected: |
  The first recorded `dark` transition agrees with the prober's reading of the same terminal
  over the same window — so the new instrument is not the only witness to its own claims.
result: skipped
reason: |
  POST-DEPLOY BY CONSTRUCTION — requires a real session lapse in production.
  ⛔ Do NOT satisfy this by mining the prober's archives backwards: they are polluted by
  self-test fixtures. Carried as broken-windows ledger row 53. Founder decision 2026-09-16.
ledger_row: 53

## Issues

None BLOCKING. A4 passed on first contact with production. One finding was raised by the
deploy and is NOT a defect in this phase's code — see A3 under Notes.

## Notes

⚠️ **AMENDED 2026-09-16, POST-DEPLOY.** This file previously said the UAT session "verified
NOTHING about production behaviour" and that all three tests were unrun. That was true when it
was written and is FALSE now — the deploy of `c14665ac` happened and A4 was measured. The old
wording is kept here as lineage, not as a live claim.

**A4 — PASSED.** The table + policy pair is fully exercised in PROD: INSERT, filtered SELECT
(3x 200 OK) and the `_confirm_row` compare-and-set UPDATE. No credential disclosure; the only
regex hit traced to a timestamp's microsecond fraction. Ledger row 51 closed.

**A2 — CLOSED 2026-09-22. PASSED.** Its criterion was WRONG and was replaced: the original
test could never pass on any server, healthy or leaking. Against the corrected welcome/goodbye
criterion the owed hours-apart reading has now been taken, 6 days 18h after the baseline and on
the SAME process (verified, not assumed — one `server started` line in the window). ~1012
connections, thread ids contiguous on both sampled windows, 0 unclosed, gap 0 -> 0. Ledger row 52
closes. ⚠️ The gateway reaps its threads; this was never the eviction defect, which stays with
Phase 164.6.6.

**C2 — STILL OPEN.** It needs a real transition. The episode opened at `dark` and stays open
until the session state changes; that transition is what C2 cross-checks against the hourly
prod-prober. ⛔ Still do NOT mine the prober archives backwards.

⭐ **THE FINDING THAT OUTRANKS ALL THREE — assumption A3, surfaced 2.4 seconds after boot.**
The boot heal reported `not_healed:credential_refused:code=-6: Terminal: Authorization failed`,
and the episode row records it as `state: dark`, `opening_code: -6`. A3 was carried in from a
prior phase as *"the -6 is the saved account being refused, not a broker-side block"*, still
open, with the note that if it IS broker-side then **no code change helps** and the heal returns
`-6` forever. RESEARCH predicted this detector would be what first makes A3 reachable and that
the episode row would say so. It did, on the first breath.
⚠️ **Unexplained disagreement worth chasing:** at 06:52:43.709Z a stage event reported
`ok=true stage="session_authorized"`, and 1.3 s later the heal reported authorization FAILED
on the same terminal. Either `ok=true` means "the stage ran" rather than "the session is
authorized", or the heal is attempting a credentialed re-login against a terminal that was
already fine. One sample is not a diagnosis. This is exactly the class of disagreement C2 exists
to catch, and it appeared before C2 had a lapse to test.

**What that means for the ship:** the mechanism was built, reviewed over three rounds (0 Critical),
secured (32/32 threats closed, 0 open) and green (5819 passed / 89 skipped serially, mypy --strict
clean on 95 files), and shipped UNPROVEN IN PRODUCTION by explicit founder decision. The first
deploy was the experiment. It wrote its first row, proved its own sink, and immediately produced
a real production finding.

---

## ⭐ A3 IS CLOSED, AND R2 IS TAKEN — founder VNC session, 2026-09-16 ~19:50Z

A founder-performed re-login on the gateway terminal settled three of this phase's open
readings in one event. ⛔ The founder performed every credentialed step; no instrument
triggered a login, which is what `164.6.2-06` requires.

### A3 — the `-6` is a REFUSED SAVED ACCOUNT, not a broker-side block

**MEASURED, from the terminal's own Journal tab** (the only place that separates the two —
neither `scripts/mt5-diag.sh` nor the prober can, and both say so):

- `04:06:20.222` and `06:52:45.016` — `authorization on <a Vantage live server> failed
  (Invalid account)`, twice, on one saved account. **`Invalid account` is the SERVER's own
  verdict**, returned over a working connection.
- `04:01:11.184`, minutes earlier — a DIFFERENT saved account `authorized` on a DIFFERENT
  Vantage live server, `ping: 7.57 ms`, then `terminal synchronized … 48 positions, 0 orders,
  1185 symbols`, then `trading has been disabled - investor mode`.

So the network was fine and the broker answered; one stored credential was rejected while
another was accepted seconds apart. **A3's carried-in alternative — "if it IS broker-side then
no code change helps and the heal returns `-6` forever" — is REFUTED.** A re-login fixed it:
`./scripts/mt5-diag.sh` went from
`{"initialize": false, "last_error": [-6, "Terminal: Authorization failed"], "terminal_info": null}`
to `{"initialize": true, "last_error": [1, "Success"], "trade_allowed": true,
"tradeapi_disabled": false, "connected": true, "build": 6182}` — **both** `connected` and
`trade_allowed` true, which is the remedy's own stated confirmation bar.

### R2 / question THREE (D-09) — a login did NOT re-clear the Expert-Advisors options

Read BEFORE the login, read again immediately AFTER it, nothing changed in between:

| option | before | after |
|---|---|---|
| Allow algorithmic trading | ✅ on | ✅ on |
| Disable … when the **account** has been changed | ☐ off | ☐ off |
| Disable … when the **profile** has been changed | ☐ off | ☐ off |
| Disable … when the charts symbol or period has been changed | ☐ off | ☐ off |
| Disable algorithmic trading via external Python API | ☐ off | ☐ off |

A REAL per-account authorization happened between the two readings (`19:50:16.567` — authorized,
synchronized, `trading has been disabled - investor mode`). **The options are byte-for-byte the
same.**

⭐ **D-09 in its UNCONDITIONAL form — "a login is an account change and MT5 re-clears the
Expert-Advisors options on every one" — does not hold.** The hazard is GATED by the
`Disable … when the account has been changed` checkbox, which is OFF on this terminal. That is
consistent with `scripts/mt5-diag.sh`'s own header, which conditions the warning on
`[Experts] Account=1` being *armed*.

⚠️ **TWO LIMITS, stated so nobody reads more into this than it holds.** (a) ONE login, one
terminal — enough to refute an "every one" claim, not enough to assert the converse. (b) The
EVIDENCE FORM DIFFERS from what `164.6.2-06` specifies: it asks for R2 "evidenced by an analytics
log line"; this is the terminal's own Journal plus a before/after reading of the options dialog.
Arguably the stronger oracle — it is the source rather than a reflection — but it is not the
artefact the plan item named. **Whether that satisfies `164.6.2-06` is a founder call, not this
record's.**

### ⭐ THE `ok=true stage="session_authorized"` CONTRADICTION IS RESOLVED — by contrast, not by one sample

This UAT recorded the disagreement as unexplained and said *"one sample is not a diagnosis."*
It now has five, and the healthy one is what settles it.

| tick | stage sequence | verdict line |
|---|---|---|
| 19:19:51, 19:29:51, 19:39:52, 19:49:52 (pre-login) | `session_authorized` → `last_error` → `initialize_credentialed` → `last_error` → `close` | `not_healed:credential_refused:code=-6` |
| 19:59:53 (post-login) | `session_authorized` → `close` | `already_authorized` |

`ok=true stage="session_authorized"` is emitted on BOTH paths. **It marks that the stage RAN; it
is not an outcome.** On the healthy path the monitor stops right there; on the broken path it
reads `last_error`, attempts `initialize_credentialed`, reads `last_error` again, and only then
reports the refusal. The UAT's first horn is the correct one, and the second — "the heal is
attempting a credentialed re-login against a terminal that was already fine" — is refuted: when
the terminal IS fine, no `initialize_credentialed` stage appears at all.

### What this does NOT settle

- **C2** still needs its cross-check against the hourly prod-prober. The transition itself is now
  in the analytics log (`already_authorized` at `19:59:53.708Z`); whether the episode row closed,
  and whether the prober agrees, is unread here. ⛔ Still do NOT mine the prober archives backwards.
- Whether the refused account should be repaired or retired is an operational decision, not a
  finding. The terminal currently runs the accepted investor account with `Save password` set, so
  the login survives a restart — ⛔ and a redeploy would NOT have fixed any of this: the saved
  login lives on the persistent volume.
- ⚠️ The KasmVNC browser client crashed mid-session (`Cannot read properties of undefined
  (reading 'lastActiveAt')`). MEASURED immediately after: `connected: true`, `trade_allowed: true`
  — the viewer died, the terminal did not. Do not read a VNC client error as a terminal fault.
