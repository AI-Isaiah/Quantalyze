---
status: complete
phase: 164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit
source:
  - 164.6.4-01-SUMMARY.md
  - 164.6.4-02-SUMMARY.md
  - 164.6.4-03-SUMMARY.md
  - 164.6.4-04-SUMMARY.md
  - 164.6.4-05-SUMMARY.md
started: 2026-09-16T05:05:36Z
updated: 2026-09-16T05:05:36Z
---

## Current Test

[testing complete]

## Tests

### 1. A4 — read the first production episode row back after deploy
expected: |
  At least one row under `cron_name = 'mt5_session_episode'`; `metadata.state` present;
  `metadata.reading_attributes_account` present and FALSE; `metadata.attribution_limit`
  present; and NO credential, host, port or account value in any field.
  ⛔ Run the `shobj_description` marker query FIRST — `current_database()` is `postgres`
  on BOTH projects and proves nothing.
result: skipped
reason: |
  POST-DEPLOY BY CONSTRUCTION — not testable before this ships, and deliberately not
  simulated green. No Python has ever written to `public.cron_runs`; the untested surface
  is the TABLE + POLICY pair, and the service-role INSERT cannot be exercised locally.
  ⛔ The obligation is NOT discharged by this skip — it is carried as broken-windows ledger
  row 51, which /gsd-ship's own gate reads, and in VERIFICATION.md's human_verification block.
  Founder decision 2026-09-16.
ledger_row: 51

### 2. A2 — read the gateway's rpyc thread counter in two prod-prober runs several hours apart
expected: |
  The counter does NOT climb monotonically with tick count. At the 600 s default the monitor
  opens ≈144 rpyc connections/day against a container already observed with a three-digit
  thread counter.
result: skipped
reason: |
  POST-DEPLOY BY CONSTRUCTION. Whether `mt5linux`'s rpyc server reaps per-connection threads
  on close is not observable locally (⛔ no VNC, no login).
  ⭐ THE MOST URGENT OF THE THREE: the verifier ranked this ABOVE A4. A4 fails semi-loudly —
  the heal still works and only the dataset goes missing, with logging. A2 would degrade the
  gateway SILENTLY over days, and this is the container with a history of wedging into -10005.
  Carried as broken-windows ledger row 52. Founder decision 2026-09-16.
ledger_row: 52

### 3. Criterion 2 cross-check against the independent hourly prod-prober
expected: |
  The first recorded `dark` transition agrees with the prober's reading of the same terminal
  over the same window — so the new instrument is not the only witness to its own claims.
result: skipped
reason: |
  POST-DEPLOY BY CONSTRUCTION — requires a real session lapse in production.
  ⛔ Do NOT satisfy this by mining the prober's archives backwards: they are polluted by
  self-test fixtures. Carried as broken-windows ledger row 53. Founder decision 2026-09-16.
ledger_row: 53

## Issues

None. No test reported a defect; all three are unrun-by-construction and tracked in the ledger.

## Notes

⚠️ **This UAT session verified NOTHING about production behaviour, and says so deliberately.**
All three tests are post-deploy. They are recorded as `skipped` WITH REASONS — a definitive
disposition — rather than `blocked`, because "blocked" would imply someone could unblock and
test them before deploy, which is false.

**What that means for the ship:** the mechanism is built, reviewed over three rounds (0 Critical),
secured (32/32 threats closed, 0 open) and green (5819 passed / 89 skipped serially, mypy --strict
clean on 95 files) — but it is **UNPROVEN IN PRODUCTION**, and the phase has said so since wave 2.
The first deploy is the experiment. Rows 51/52/53 are the record of what must be read back.
