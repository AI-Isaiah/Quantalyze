---
phase: 164.6.4-mt5keepalive
fixed_at: 2026-09-15T21:45:00Z
review_path: .planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-REVIEW-3.md
iteration: 3
findings_in_scope: 8
fixed: 8
skipped: 0
status: all_fixed
---

# Phase 164.6.4 MT5KEEPALIVE: Code Review Fix Report — ROUND 3 (THE LAST ONE)

**Fixed at:** 2026-09-15T21:45:00Z
**Source review:** `164.6.4-REVIEW-3.md` (standard reviewer — WR-13..WR-16, IN-10..IN-13) + the
parallel `silent-failure-hunter` findings (B1–B4) transcribed into the launch prompt, per the
launch instructions ("this is their source of record").
**Iteration:** 3
**Branch:** `phase-164.6.4-fold-raiselast`, HEAD `223b1b7f` at launch. MAIN checkout — no worktree
(required so `analytics-service/.venv/bin/python` resolves for pytest/mypy).

**Summary:**
- Findings in scope (per the launch brief's explicit approved list — Section A + Section B): 8
  (WR-13, WR-14, WR-15, WR-16, B1, B2, B3, B4)
- Fixed: 8
- Skipped: 0

⛔ **Scope note, stated rather than silently applied.** `164.6.4-REVIEW-3.md` also carries IN-10,
IN-11, IN-12 and IN-13 (the standard reviewer's Info findings). The launch brief's explicit
"Non-negotiable rules" section named exactly eight approved items (Section A: WR-13/14/15/16;
Section B: B1/B2/B3/B4) and stated "THIS ROUND MUST NOT ADD MECHANISM" with a deliberately
low-risk shape ("one deletion, one comment correction, and small changes that COPY patterns
already present"). IN-10..IN-13 were not in that approved list, so — per Rule 12 (fail loud rather
than silently drop) — they are recorded here as **NOT ROUTED THIS ROUND**, not silently skipped:
- IN-10 (a close in another container erases confirmations) — MOOT: B3 deleted the
  `confirmations` counter entirely, so the two-writer erasure this finding described no longer
  has a `confirmations` field to erase. `last_confirmed_at`/`last_confirmed_by` still get
  overwritten by an interleaved close's read-modify-write of the whole `metadata` object, but the
  review's own "Fix: none required for correctness" verdict already covered that residual.
- IN-11 (nothing pins the tick-deadline arm records `KIND_TICK_DEADLINE` specifically) — not
  fixed this round; genuinely a small, low-risk test-only addition, but it was not in the approved
  list and fix_scope's "all" default is superseded here by the launch brief's explicit
  enumeration.
- IN-12 (the WR-08 floor's "BY CONSTRUCTION" wording overstates a boundary case) — not fixed this
  round; comment-only, but likewise not in the approved list.
- IN-13 (two test docstrings misstate why `sink` is installed on the tick-deadline test) — not
  fixed this round; same reasoning. Note this docstring text is DISTINCT from the "ONE
  non-measuring exit" text B1 corrected — B1's target was
  `services/mt5_session_monitor.py`'s inline comment on the deadline arm; IN-13's target is
  `tests/test_mt5_session_monitor.py`'s own docstring reasoning about the `sink` fixture, a
  different sentence with a different false claim.

These four remain open for a future phase/round if the founder wants them routed.

**Baseline:** `5813 passed, 89 skipped` serial, `mypy --strict` clean on 95 files.
**After this round:** `5819 passed, 89 skipped` (`.venv/bin/python -m pytest -q`, serial, exit 0,
run TWICE for repeat-flake surfacing — both runs identical), `mypy --strict --follow-imports=silent
services/ routers/ models/` clean on 95 files (same file count as baseline — no scanned-tree
drift). The +6 passed are new/split tests added by this round's fixes:
`test_WR_15_a_confirm_that_LOST_its_race_leaves_the_SETTLED_row_untouched` (+1),
`test_the_kill_switch_reading_is_DISABLED_for_a_deliberate_value_and_UNPARSEABLE_otherwise` (+1),
the new `kill-switch-unparseable` parametrize leg on
`test_a_reading_that_MEASURED_NOTHING_writes_nothing_and_closes_nothing` (+1), and
`test_WR_10_a_MISCONFIGURATION_kind_still_escalates_past_the_threshold` going from a single test
to four parametrized legs (+3). 1 + 1 + 1 + 3 = 6, matching exactly.

## Fixed Issues

### B3: DELETE the `confirmations` counter; keep `last_confirmed_at`

**Files modified:** `analytics-service/services/mt5_session_episodes.py`,
`analytics-service/tests/test_mt5_session_episodes.py`, `analytics-service/tests/test_mt5_relogin.py`
**Commit:** `3fc98d34`
**Applied fix:** Deleted `metadata["confirmations"] = int(metadata.get("confirmations") or 0) + 1`
from `_confirm_row` (a read-modify-write of a stale snapshot that undercounts under the
two-container Railway overlap this module tolerates — verified by grep that nothing in
`services/`, `src/` or `supabase/` reads `confirmations`). Updated the two tests that asserted
`confirmations` 1→2 to assert `last_confirmed_at` ADVANCES on a second confirm instead, with a
deterministic `_now_iso` stub (two-value iterator) to avoid a same-microsecond flake on a
real-time-free async test.
**Verified:** neutered (re-inserted the deleted line), confirmed sha changed, ran the covering
test — RED with the named assertion message. Restored from byte backup, sha matched original,
test GREEN again.
**Test:** `test_a_tick_that_observes_the_ALREADY_RECORDED_state_CONFIRMS_the_open_row`
(`tests/test_mt5_session_episodes.py`), `test_TRACER_a_healthy_tick_CONFIRMS_the_open_row`
(`tests/test_mt5_relogin.py`).

### WR-13: `_confirm_row`'s metadata keys were outside every closed-set disclosure gate

**Files modified:** `analytics-service/tests/test_mt5_session_episodes.py`
**Commit:** `a224d36c`
**Applied fix:** Declared `_CONFIRM_METADATA_KEYS = frozenset({"last_confirmed_at",
"last_confirmed_by"})` (two keys, not three — B3 above deleted `confirmations` first). Drove it
two ways: added a closed-set equality assertion to the CONFIRMS test itself, and extended
`test_no_credential_host_or_port_reaches_ANY_field_of_ANY_row` to drive an actual CONFIRM (via a
repeated `KIND_HEALED` observation after its transition) so the disclosure scan — which had never
walked a confirmed row before — now does, with its `<=` union widened to include the confirm keys.
**Verified (MUTATION, gate hole per launch brief Section A):** injected
`metadata["verdict_tail"] = "Terminal: Authorization failed for Broker-Demo-2 account <redacted>"`
inside `_confirm_row`. Confirmed sha changed. Both gates caught it: the CONFIRMS test's closed-set
assertion REDs with the drift message, AND `test_no_credential_host_or_port_reaches_ANY_field_of_ANY_row`
independently REDs on the disclosure scan itself (`AssertionError: the episode row disclosed
'Broker-Demo-2' in column 'metadata'`). Restored from byte backup, sha matched, suite green again.
**Test:** `test_a_tick_that_observes_the_ALREADY_RECORDED_state_CONFIRMS_the_open_row`,
`test_no_credential_host_or_port_reaches_ANY_field_of_ANY_row` (both
`tests/test_mt5_session_episodes.py`).

### B2: `_confirm_row` now observes its CAS outcome instead of assuming it

**Files modified:** `analytics-service/services/mt5_session_episodes.py`
**Commit:** `b52f3bb4` (bundled with WR-15 — see note below)
**Applied fix:** Copied `_close_row`'s established pattern exactly: `.update(..., count=
CountMethod.exact)`, read `response.count` with the same `isinstance(matched, int) and not
isinstance(matched, bool)` guard, return `bool`. On a lost CAS, logs a WARNING naming the row id
and the state measured (mirroring `_close_row`'s own lost-race WARNING, one function below). The
call site (`record_mt5_session_reading`) does not need to consume the return value — its own
comment already establishes the confirm's loss is a harmless no-op for the dataset; what changed
is that the loss is now OBSERVED (logged) rather than silent.
**No new mechanism:** every piece of this fix is a direct copy of code and a guard clause already
present in `_close_row`, in the same file, three lines below.
**Verification:** see WR-15 below — the same test drives both the compare-and-set predicate and
the new WARNING log.

### WR-15: `_confirm_row`'s `status='running'` predicate was pinned by nothing

**Files modified:** `analytics-service/tests/test_mt5_session_episodes.py`
**Commit:** `b52f3bb4`
**Applied fix:** Added `test_WR_15_a_confirm_that_LOST_its_race_leaves_the_SETTLED_row_untouched`,
reusing the existing `_OrderedCronRuns.after_select` interleave hook (already used for the
close-path race in `test_a_LOST_compare_and_set_on_the_MEASURED_close_does_not_claim_one`) to land
a genuine close between the confirm caller's READ and its own UPDATE. Asserts the settled row is
left untouched (`last_confirmed_at`/`last_confirmed_by` absent, `completed_at` unchanged) AND — via
B2 above — that the new lost-CAS WARNING fires.
**Verified (MUTATION, gate hole per launch brief Section A):** deleted the `.eq("status",
"running")` line from `_confirm_row`. Confirmed sha changed. Test REDs: `AssertionError: a confirm
stamped a settled episode — its last_confirmed_at would post-date its own completed_at`. Restored
from byte backup, sha matched, suite green again (327 passed, +1 over the B3 baseline of 326 for
this file's own run).
**Test:** `test_WR_15_a_confirm_that_LOST_its_race_leaves_the_SETTLED_row_untouched`
(`tests/test_mt5_session_episodes.py`).

_(B2 and WR-15 are reported as two entries but landed in one commit — they modify the exact same
function's exact same lines, and WR-15's test exercises B2's new count-reading logic to correctly
observe `matched=0`. Splitting the diff would have left one half's code uncovered by any test in
the intervening commit.)_

### B4: `_confirm_row`'s "durably and queryably" framing overclaimed automatic detection

**Files modified:** `analytics-service/services/mt5_session_episodes.py`
**Commit:** `b52f3bb4` (bundled — see WR-15 note above; this is a docstring-only addition in the
same function touched by B2)
**Applied fix:** Added a paragraph to `_confirm_row`'s docstring stating plainly that the confirm
makes a dead loop distinguishable ON INSPECTION, not DETECTABLE AUTOMATICALLY: nothing reads
`last_confirmed_at` today, and `latest_cron_success` deliberately does not enumerate
`MT5_SESSION_EPISODE_CRON_NAME` (D-5) — a stale confirm raises no alert on its own.
**Verified:** Tier 1 (re-read) + Tier 3 (docstring-only, no syntax-relevant change) — full suite
run confirmed no regression.

### WR-16: the minimal-returning fake volunteered `.count` regardless of the request

**Files modified:** `analytics-service/tests/test_mt5_session_episodes.py`
**Commit:** `f538d0bd`
**Applied fix:** `_MinimalReturningCronRuns._execute` used to set `response.count =
len(response.data)` unconditionally on every UPDATE. Changed it to defer to the base fake
(`_FakeQuery.execute`, `tests/test_mt5_relogin.py`), which already sets `.count` ONLY when
`count="exact"` was requested — matching real postgrest's `Prefer: count=exact` behaviour — and
simply ASSERT that already happened (`response.count is not None`) rather than recompute it a
second, request-blind way.
**Verified (MUTATION, gate hole per launch brief Section A):** dropped `count=CountMethod.exact`
from `_close_row`'s `.update()` call. Confirmed sha changed. Both WR-12 tests REDs via the new
harness assertion (`the UPDATE did not request count=exact`). Restored from byte backup, sha
matched, suite green again.
**Test:** `test_WR_12_a_WON_compare_and_set_is_read_from_COUNT_not_inferred_from_rows`,
`test_WR_12_a_LOST_compare_and_set_still_reads_LOST_via_count` (both
`tests/test_mt5_session_episodes.py`).
**Residual, not applied (documented rather than silently dropped):** the review's ⚠️ note also
suggested changing the BASE fake's own `self._count_requested == "exact"` comparison
(`tests/test_mt5_relogin.py`) to compare against `CountMethod.exact` by symbol rather than the
string literal, as future-proofing against `CountMethod` ever stopping being a `str` Enum. Not
applied this round: it touches a shared fixture used by dozens of other tests across three files,
which is out of proportion to a documented, currently-inert (`isinstance(CountMethod.exact, str)
is True` on postgrest 2.31.0) future risk, and the round's own "no new mechanism, minimal risk"
directive weighed against it.

### WR-14: the kill-switch reading now distinguishes a decision from a misconfiguration

**Files modified:** `analytics-service/services/closed_sets.py`,
`analytics-service/services/mt5_session_episodes.py`,
`analytics-service/services/mt5_session_monitor.py`,
`analytics-service/tests/test_mt5_session_episodes.py`,
`analytics-service/tests/test_mt5_session_monitor.py`
**Commit:** `1a72a686`
**Applied fix:** Chose the review's Option — FAIL LOUD (record a distinct, escalating kind) on a
value `mt5_enabled_server()` cannot recognise as a decision — over silently widening
`mt5_enabled_server()` itself, because that function is a shared, deliberately fail-closed go-live
gate consumed by five OTHER call sites (`routers/exchange.py`, `routers/process_key.py`,
`services/allocator_positions.py`, `services/job_worker.py`,
`services/ingestion/long_fetch.py`) whose own byte-for-byte-mirrors-`sfox_enabled_server` contract
this round must not touch. Added `mt5_enabled_is_deliberate()` (`closed_sets.py`, scoped to the
ONE call site that needs the distinction: `""`/`false` only) and a new
`KIND_KILL_SWITCH_UNPARSEABLE` constant (deliberately kept OUT of `_DECIDED_BLIND_KINDS`).
Corrected the three sibling sites the review named as now-false: `KIND_DISABLED`'s own comment,
the kill-switch arm's comment in `mt5_session_monitor.py`, and
`test_the_disabled_kill_switch_reading_is_COUNTED_not_merely_logged`'s docstring — all now say
only the COUNTER, not the escalation, is guaranteed for `KIND_DISABLED`. Also closed the "related
gate gap" the review flagged: `test_WR_10_a_MISCONFIGURATION_kind_still_escalates_past_the_threshold`
previously drove only `KIND_BUSY_SKIP`; parametrized it over all four kinds outside
`_DECIDED_BLIND_KINDS` (busy-skip, credentials-not-configured, gateway-not-configured,
kill-switch-unparseable) so a future widening of that set would be caught.
**Verified (MUTATION, gate hole per launch brief Section A):** reverted the classification to
always record `KIND_DISABLED` regardless of `mt5_enabled_is_deliberate()`. Confirmed sha changed.
New test REDs (`assert False` on the `kill_switch_unparseable` leg — only `kind=disabled` appeared
in both ticks). Restored from byte backup, sha matched, suite green again (332 passed).
**Test:** `test_the_kill_switch_reading_is_DISABLED_for_a_deliberate_value_and_UNPARSEABLE_otherwise`,
`test_the_disabled_kill_switch_reading_is_COUNTED_not_merely_logged` (both
`tests/test_mt5_session_monitor.py`);
`test_a_reading_that_MEASURED_NOTHING_writes_nothing_and_closes_nothing`,
`test_WR_10_a_MISCONFIGURATION_kind_still_escalates_past_the_threshold` (both
`tests/test_mt5_session_episodes.py`).

### B1: corrected the "ONE non-measuring exit" claim on `KIND_TICK_DEADLINE`

**Files modified:** `analytics-service/services/mt5_session_monitor.py`
**Commit:** `d74ed688`
**Applied fix:** Replaced the false claim with an enumeration of the three actual remaining
non-measuring exits and WHY each is excluded: (1) `heal_mt5_terminal_session`'s own kill-switch arm
(the BOOT heal's, distinct from this tick's) doesn't record at all, but it is the single boot-only
caller so `blind_run_escalation_threshold(None)` is always `None` and a recording there could never
escalate regardless; (2) and (3) the outer `except Exception` in both `heal_mt5_terminal_session`
and this module's own `run_mt5_session_monitor_tick` are structurally barred from recording by the
never-raises AST gate's "exactly one nested `try`" requirement — the same reason
`_stamp_reading_and_measure_gap`/`_count_blind_reading` are separate functions in
`mt5_session_episodes.py`. Comment only; no restructuring of either handler (which would fight the
gate).
**Verified:** Tier 1 (re-read, comment-only) + Tier 2 (`mypy --strict` clean) + full suite green,
no test changes needed or made (nothing asserted on the old comment text).

## Skipped Issues

None — all 8 in-scope findings were fixed. (IN-10 through IN-13 were explicitly out of this
round's approved scope; see the Scope note above rather than "Skipped Issues" — they were never
attempted, not attempted-and-abandoned.)

---

_Fixed: 2026-09-15T21:45:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 3_
