---
phase: 164.6.4-mt5keepalive
fixed_at: 2026-09-15T21:15:00Z
review_path: .planning/phases/164.6.4-mt5keepalive-nothing-triggers-a-recovery-so-the-terminal-sit/164.6.4-REVIEW-2.md
iteration: 2
findings_in_scope: 11
fixed: 10
skipped: 0
status: all_fixed
---

# Phase 164.6.4 MT5KEEPALIVE: Code Review Fix Report — ROUND 2

**Fixed at:** 2026-09-15T21:15:00Z
**Source review:** `164.6.4-REVIEW-2.md` (standard reviewer) + the parallel `silent-failure-hunter`
findings transcribed into the launch prompt (no file of record for the latter — treated the
prompt as source, per the launch instructions).
**Iteration:** 2
**Branch:** `phase-164.6.4-fold-raiselast`, MAIN checkout (no worktree — required so
`analytics-service/.venv/bin/python` resolves for pytest/mypy).

**Summary:**
- Findings in scope: 11 (WR-07, WR-08, WR-12, WR-09 [two facets: boot-window liveness +
  steady-state confirm], WR-10, WR-11, IN-06, IN-07, IN-08, IN-09, plus the un-numbered "Info"
  item — `_open_row`'s discarded INSERT result and the shared-counter docstring note)
- Fixed: 10
- No change needed: 1 (IN-09 — the review itself says "No action owed in this phase")
- Skipped: 0

**Baseline:** `5792 passed, 89 skipped`, `mypy --strict` clean on 95 files.
**After this round:** `5813 passed, 89 skipped` (`pytest -q -n auto --dist loadgroup`, exit 0),
`mypy --strict --follow-imports=silent services/ routers/ models/` clean on 95 files (same file
count as baseline — no scanned-tree drift). The +21 passed are new/split tests added by this
round's fixes; skipped count is unchanged (no new skips introduced).

⚠️ **Surfaced rather than hidden:** a REPEAT full-suite run (same command, immediately after)
showed 3 failures — `test_limiter_identity.py::TestClassClosure::test_every_router_limiter_is_the_one_singleton`,
`test_portfolio_compute_integration.py::TestAlertFailureKeepsAnalyticsComplete::test_alert_exception_does_not_mark_row_failed`,
`test_process_key_auth_order.py::test_unset_internal_token_rejects_empty_bearer_with_500`.
Verified these are PRE-EXISTING and UNRELATED to this round: none of the three files reference
`mt5` (`grep -l mt5` on all three: empty); `test_unset_internal_token_rejects_empty_bearer_with_500`
reds even run completely ALONE with `env -i` (a fully clean environment) — an environmental
dependency in this sandbox, not a regression this round introduced. The full MT5 suite
(`test_mt5_session_episodes.py` + `test_mt5_relogin.py` + `test_mt5_session_monitor.py` +
`test_mt5_concurrency.py`, 356 tests) was re-run standalone under the same `-n auto --dist
loadgroup` flags used for the full suite and passed clean (0 failures) on every run. Not
investigated further — out of this round's scope (findings WR-07 through IN-09 only) and
pre-dates this round's HEAD (`bb5c9920` already carried the same three files unmodified).

## Fixed Issues

### WR-07: the uncounted tick deadline

**Files modified:** `services/mt5_session_episodes.py`, `services/mt5_session_monitor.py`,
`tests/test_mt5_session_monitor.py`
**Commit:** `9c7fa01c`
**Applied fix:** Added `KIND_TICK_DEADLINE` and record it (still `not_measured`, writes no row)
from the tick's own `except asyncio.TimeoutError` arm, after the log line so the deadline
itself cannot cancel the recorder. This closes the one non-measuring exit that escaped
`_CONSECUTIVE_NOT_MEASURED_READINGS` after WR-06 (round 1) added the outer deadline.
**Test:** `test_a_tick_that_outruns_its_own_cadence_is_BOUNDED_and_logged_distinctly` (updated
to install a fake sink and assert `_CONSECUTIVE_NOT_MEASURED_READINGS == 1` and `sink.rows ==
[]` after the deadline fires) — installing the `sink` fixture was itself necessary because the
new code path makes a real DB call that would otherwise hit the shared TEST Supabase project.
Calibrated: reverting the record call reds the counter assertion.

### WR-08: the cadence floor omitted the bounded lease acquire

**Files modified:** `services/mt5_session_monitor.py`, `tests/test_mt5_session_monitor.py`
**Commit:** `b6e0db8c`
**Applied fix:** `_MT5_SESSION_POLL_INTERVAL_FLOOR_S` is now
`_MT5_RELOGIN_BUDGET_CEILING_S + _MT5_RELOGIN_LEASE_WAIT_CEILING_S` (330.0, was 300.0) rather
than the budget ceiling alone. Also corrected the WR-02 comment to state that "the boot heal
owns the boot window uncontested" now holds by construction (`interval >= floor >= actual_lease_wait
+ actual_budget` for every value each knob's own window admits) rather than only for some
configurations.
**Test:** New `test_the_FLOOR_bounds_the_heals_WORST_CASE_wall_clock_not_the_budget_alone`
(pure arithmetic over the two ceilings), plus updated
`test_EXACTLY_ONE_cadence_knob_exists_and_it_is_the_DETECTION_POLL_INTERVAL` and
`test_the_declared_cadence_BOUNDS_are_DERIVATIONS_and_not_free_numbers` to assert the new
derivation. Calibrated: neutering the floor back to the budget ceiling alone (300.0) reds all
three against the lease-wait ceiling's default (30.0).

### WR-12: the CAS oracle inferred from row shape rather than observed

**Files modified:** `services/mt5_session_episodes.py`, `tests/test_mt5_relogin.py`,
`tests/test_mt5_session_episodes.py`, `tests/test_mt5_session_monitor.py`
**Commit:** `9bb0ad65`
**Applied fix:** `_close_row`'s UPDATE now requests `count=CountMethod.exact` explicitly and
reads `response.count` (falling back to `len(rows(response))` for a transport that doesn't set
it — the same guard `equity_reconstruction._result_row_count` uses), instead of inferring a WON
compare-and-set from `bool(rows(response))`, a shape that exists only because postgrest-py
currently defaults `returning='representation'`. Both shipped test fakes' `.update()` were
updated to accept the `count` kwarg (required so the real call doesn't raise against them).
**Test:** New `_MinimalReturningCronRuns` transport + two tests
(`test_WR_12_a_WON_compare_and_set_is_read_from_COUNT_not_inferred_from_rows`,
`test_WR_12_a_LOST_compare_and_set_still_reads_LOST_via_count`) simulating
`returning='minimal'` directly. Calibrated: reverting to `bool(rows(response))` (and dropping
the `count=` kwarg) reds the WON-case test.

### WR-09 (facet 1 — boot window) + Info (real loop-death content)

**Files modified:** `services/mt5_session_monitor.py`, `tests/test_mt5_session_monitor.py`
**Commit:** `654c0423`
**Applied fix:** Added a `logger.info("mt5 session monitor: started — …")` line inside the
loop's guard, before the `while`, naming the first-tick delay — closing the "byte-identical to
a monitor that does not exist" window for one full poll interval after every deploy. Also gave
the outer "THE LOOP ITSELF STOPPED" handler real content: `scrub_freeform_string(str(exc))`
alongside `exc_class`, since no credential is ever in scope in that handler (unlike
`mt5_relogin`'s by-value `_describe_exception_for_log`, which exists because that path carries
a live broker password).
**Test:** New `test_WR_09_the_loop_logs_that_it_STARTED_before_its_first_tick`; extended
`test_A_FAILURE_OUTSIDE_THE_PER_TICK_GUARD_STILL_CANNOT_END_THE_WORKER` to assert the
exception's own message text reaches the log. Calibrated: reverting both reds their respective
assertions.

### WR-09 (facet 2 — steady state, HIGH-2)

**Files modified:** `services/mt5_session_episodes.py`, `tests/test_mt5_session_episodes.py`,
`tests/test_mt5_relogin.py`
**Commit:** `46190411`
**Applied fix:** Added `_confirm_row` — an UPDATE of the open row's `last_confirmed_at` /
`confirmations` / `last_confirmed_by`, scoped to `status='running'` — called from the
matching-state branch of `record_mt5_session_reading` instead of a bare `return`. This does
**not** add a new row (INSERT-side per-transition behavior is unchanged — still one row per
transition, not ~144/day); it fixes the separate defect that a healthy session wrote zero rows
forever, making a dead loop and a live healthy one identical in the durable record
(`[MT5-VERDICT-SINK-01]`, this module's own reason for existing, left unapplied to itself).
Updated the module docstring's decision table and `record_mt5_heal_outcome`'s "produces ZERO"
claim to say "zero new rows" precisely.
**Test:** Renamed the two tests that asserted a matching-state tick writes nothing at all
(`..._writes_NOTHING` → `..._INSERTS_NOTHING`, keeping the still-true INSERT-side assertion) and
added direct new tests for the confirm behavior at both the recorder level
(`test_a_tick_that_observes_the_ALREADY_RECORDED_state_CONFIRMS_the_open_row`) and end-to-end
through the real heal (`test_TRACER_a_healthy_tick_CONFIRMS_the_open_row`). Calibrated:
reverting to the bare `return` reds both CONFIRMS tests.

### WR-10: `KIND_DISABLED` escalated a deliberate decision to WARNING forever

**Files modified:** `services/mt5_session_episodes.py`, `tests/test_mt5_session_episodes.py`
**Commit:** `cbb32dfd`
**Applied fix:** Added `_DECIDED_BLIND_KINDS = frozenset({KIND_DISABLED})`; the `blind`
computation now excludes any kind in that set from raising the log LEVEL, while still extending
the counter (so a later genuine fault's own escalation is not reset by a preceding decided
run). `KIND_NOT_CONFIGURED`'s successors are deliberately NOT added — an unset Railway variable
is the misconfiguration WR-03 was raised for.
**Test:** New `test_WR_10_a_DECIDED_kill_switch_is_COUNTED_but_NEVER_escalates_the_level` and
`test_WR_10_a_MISCONFIGURATION_kind_still_escalates_past_the_threshold` (the guard-rail case).
Calibrated: reverting the `blind` computation reds the first test after 13 consecutive readings
past the threshold.

### WR-11: the two WR-03 kinds (round 1) had no test coverage under the "closes nothing" control

**Files modified:** `tests/test_mt5_session_episodes.py`
**Commit:** `cbb32dfd` (combined with WR-10, same commit — both touch the same parametrize
block and were verified together)
**Applied fix:** Added `_DISABLED_READING` and `_TICK_DEADLINE_READING` (WR-07's new kind, added
at introduction rather than left to drift the same way) to
`test_a_reading_that_MEASURED_NOTHING_writes_nothing_and_closes_nothing`'s parametrize list.
The two `KIND_CREDENTIALS_NOT_CONFIGURED` / `KIND_GATEWAY_NOT_CONFIGURED` kinds (IN-07's
successors to `KIND_NOT_CONFIGURED`) were added to the same list in the IN-07 commit below.
**Test:** Coverage itself is the fix; verified green in the WR-07/WR-10/IN-07 commits' test
runs.

### IN-06: P-inner's escape scan did not cover the per-tick `try`'s own `else:`/`finally:`

**Files modified:** `tests/test_mt5_session_monitor.py`
**Commit:** `5f3a5e9e`
**Applied fix:** `_loop_survival_defects` now also scans `stmt.orelse` and `stmt.finalbody` for
`Break`/`Raise`/`Return`/`Continue` nodes, alongside the existing `handler.body` scan — closing
the gap where a `finally: break` or `else: return` on the per-tick guard itself (not inside any
`except` handler) was invisible to the predicate despite its own defect message already
claiming the broader property.
**Test:** New calibrating mutant `per-tick-guard-FINALLY-ends-the-loop` in
`_LOOP_SURVIVAL_MUTANTS`, driven through both `test_P_INNER_REDS_on_every_loop_death_shape` and
`test_P_OUTER_IS_BLIND_to_every_loop_death_shape_P_INNER_catches`. Calibrated: removing the new
scan reds the P-INNER test for that mutant (confirmed — the shipped module itself is untouched
by this fix, only the test-side predicate changed).

### IN-07: `KIND_NOT_CONFIGURED` collapsed two distinct operator faults into one class

**Files modified:** `services/mt5_session_episodes.py`, `services/mt5_relogin.py`,
`tests/test_mt5_relogin.py`, `tests/test_mt5_session_episodes.py`
**Commit:** `1214cbd3`
**Applied fix:** Split into `KIND_CREDENTIALS_NOT_CONFIGURED` and `KIND_GATEWAY_NOT_CONFIGURED`,
used at their respective call sites in `heal_mt5_terminal_session`. Both stay module `Final[str]`
constants — no env-var value ever enters a kind.
**Test:** New `test_IN_07_the_two_configuration_arms_record_DISTINGUISHABLE_kinds` (asserts the
two arms record different kinds in the recurring per-tick log evidence) plus both kinds added
to the WR-11 "closes nothing" parametrize list. Calibrated: reverting the gateway arm's kind
back to the credentials one reds the gateway-absent/port-not-numeric cases of the new test.

### IN-08: the WR-02 negative check raced the wall clock

**Files modified:** `tests/test_mt5_session_monitor.py`
**Commit:** `8c7458e2`
**Applied fix:** Split
`test_the_first_ITERATION_waits_BEFORE_ticking_so_the_boot_heal_owns_the_window`'s flaky
pre-tick check (`asyncio.sleep(cadence_s * 0.25)` against a 0.2s cadence — a 0.05s margin under
`-n auto` scheduler load) into a new dedicated test driven against a 300s cadence (a ~6000x
safety margin against the real ~0.05s sleep), per the review's "drive the clock rather than the
wall" suggestion. The original test keeps only the direction that is safe under load (the first
tick fires at approximately the right time — load can only make it later, never earlier).
**Test:** `test_the_first_ITERATION_does_NOT_tick_before_a_LARGE_cadence_elapses`. Calibrated:
neutering WR-02's actual wait-first ordering in the source (ticking immediately) reds the new
test.

### Info: `_open_row`'s discarded INSERT result; shared blind-counter docstring

**Files modified:** `services/mt5_session_episodes.py`, `tests/test_mt5_session_episodes.py`
**Commit:** `e2257ecb`
**Applied fix:** `_open_row` now checks the INSERT's response and logs an ERROR (never raises —
contract unchanged) if it returned zero rows — the one write the whole lifetime dataset rests
on, previously discarded entirely. Also documented on `_count_blind_reading` that
`_CONSECUTIVE_NOT_MEASURED_READINGS` is shared with the boot heal (which calls the same
function with `poll_interval_s=None`, so it can increment but never escalate), so the monitor's
first blind run after boot can inherit a non-zero starting count.
**Test:** New `test_INFO_an_open_INSERT_that_returns_no_rows_is_LOGGED_not_silent` via a
`_NoRowsOnInsert` transport subclass. Calibrated: reverting the check reds the test.

## No Change Needed

### IN-09: the tick-return-interval shape is a legitimate improvement, but the review itself scopes it out

**Files:** `services/mt5_session_monitor.py`
**Evidence:** The review's own text: "⚠️ Recorded so the follow-up phase that picks IN-03 up
does not re-derive the blocker from the fix report and conclude the change is wider than it is.
**No action owed in this phase.**" This is IN-03's own carve-out restated with a cheaper
implementation path (returning the interval rather than changing the call signature), not a new
requirement on this round. No code change made.

---

_Fixed: 2026-09-15T21:15:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
