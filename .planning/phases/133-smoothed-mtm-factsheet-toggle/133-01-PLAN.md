---
phase: 133-smoothed-mtm-factsheet-toggle
plan: 01
type: execute
wave: 1
depends_on: [132-01]
files_modified:
  - src/lib/types.ts
  - src/lib/factsheet/types.ts
  - src/lib/factsheet/build-payload.ts
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/factsheet/composite-read-path.test.ts
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/factsheet/[id]/v2/basis-context.tsx
  - src/app/factsheet/[id]/v2/basis-context.test.tsx
  - src/app/factsheet/[id]/v2/basis-context.leverage.test.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.tsx
  - src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx
  - src/app/factsheet/[id]/v2/factsheet-context.tsx
  - src/app/factsheet/[id]/v2/MasterBrush.basis.test.tsx
autonomous: true
requirements: [SMTM-01, SMTM-04]
must_haves:
  truths:
    - "The factsheet basis SegmentedControl offers THREE options — Cash / MTM / Smoothed MTM — wherever the basis toggle renders today"
    - "Selecting Smoothed MTM overlays scalars from metrics_json_by_basis.smoothed_mtm and swaps the chart series to the persisted smoothed bundle (same two-layer mechanism as mark_to_market) — on COMPOSITE and SINGLE-KEY factsheets alike (the single-key smoothed series is read in page.tsx and threaded into the payload, wiring-guarded)"
    - "When smoothed_mtm is absent from metrics_json_by_basis (marks missing / not yet recomputed / non-options venue), the Smoothed MTM segment is honest-disabled with a mapped reason — never hidden, never silently falling back to another basis's numbers"
    - "Every basis-consuming render-surface site under src/app/factsheet + src/lib — the `=== \"mark_to_market\"` / `!== \"mark_to_market\"` switches AND the bundle-READ sites (e.g. the factsheet-context brush x-range clamp) — has a DECIDED, test-pinned smoothed posture — eyebrow label, chart-caption three-state, suppressRelative, leverage re-pin + eligibility, peer-rank note, brush-clamp reachability; no surface renders smoothed numbers under a wrong basis label, silently falls into a cash arm, or clips the smoothed axis"
    - "On the flagship options-book case (MTM bundle absent, smoothed serving the charts), a smoothed axis LONGER than cash keeps its recent days brush-reachable (the F2.3 clamp consults the smoothed bundle length)"
    - "A {smoothed_mtm}-only metrics_json_by_basis row (MTM degraded reason-lessly) still yields the smoothed gate/metrics/toggle — never silently dropped by the :366 early-return"
    - "Cash remains the default basis on every fresh view; cash and mark_to_market render byte-identically pre/post"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "SmoothedMtmDailyReturnsSeriesPayload — per-kind payload type mirroring MtmDailyReturnsSeriesPayload (:565-571) with basis literal \"smoothed_mtm\""
      contains: "smoothed_mtm"
    - path: "src/lib/factsheet/types.ts"
      provides: "smoothed_mtm key on metricsByBasis/seriesByBasis + smoothed gate typing"
      contains: "smoothed_mtm"
    - path: "src/lib/factsheet/composite-read-path.ts"
      provides: "readSmoothedSeries + parser sibling + single-key smoothed threading via singleKeyBasisOpts + widened :366 early-return"
      contains: "readSmoothedSeries"
    - path: "src/app/factsheet/[id]/v2/page.tsx"
      provides: "Single-key smoothed series read + threading into buildOpts (sibling of the :130-147 MTM read)"
      contains: "readSmoothedSeries"
    - path: "src/app/factsheet/[id]/v2/basis-context.tsx"
      provides: "Basis union third member + smoothed overlay/series-view arms + leverage re-pin/eligibility"
      contains: "smoothed_mtm"
    - path: "src/app/factsheet/[id]/v2/factsheet-context.tsx"
      provides: "F2.3 brush x-range clamp widened with a third max() term over seriesByBasis?.smoothed_mtm?.dates.length"
      contains: "smoothed_mtm"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "Third SegmentedControl segment with honest-disabled reason copy + smoothed postures at every basis switch site"
  key_links:
    - from: "src/app/factsheet/[id]/v2/basis-context.tsx"
      to: "payload.metricsByBasis.smoothed_mtm"
      via: "useBasisMetrics overlay arm"
      pattern: "smoothed_mtm"
    - from: "src/lib/factsheet/composite-read-path.ts"
      to: "strategy_analytics.metrics_json_by_basis.smoothed_mtm"
      via: "server read → payload.metricsByBasis + gate"
      pattern: "smoothed_mtm"
    - from: "src/app/factsheet/[id]/v2/page.tsx"
      to: "src/lib/factsheet/composite-read-path.ts"
      via: "shouldReadSingleKeySmoothedSeries → readSmoothedSeries → singleKeyBasisOpts threading (the :130-147 MTM pattern's smoothed sibling)"
      pattern: "readSmoothedSeries"
    - from: "src/app/factsheet/[id]/v2/factsheet-context.tsx"
      to: "payload.seriesByBasis.smoothed_mtm"
      via: "setXRange upper-clamp third max() term"
      pattern: "smoothed_mtm"
---

<objective>
The user-facing third toggle: "Smoothed MTM" on the factsheet basis SegmentedControl, reading
the Phase-132-persisted `metrics_json_by_basis.smoothed_mtm` scalars and smoothed series bundle,
honest-disabled with a mapped reason where the smoothed basis is absent.

Purpose: closes the phase goal — a Deribit options book renders a smoothed_mtm factsheet with
no session-lump spikes; the basis on which MTM was honestly gated OFF becomes selectable.
Output: TS types + payload plumbing (BOTH read paths — composite AND the single-key page.tsx
series read, per plan-check HIGH-1) + basis-context third arm (incl. leverage re-pin +
eligibility) + SegmentedControl third segment + a decided smoothed posture at EVERY
basis-consuming render site — the `mark_to_market` switch sites AND the bundle-read sites
(the factsheet-context brush clamp, per plan-check HIGH-2) — all vitest-proven. Pure
TS/vitest — no Python in this plan.

Constraint carried through the phase: additive. `cash_settlement` stays the default and the
headline; the `mark_to_market` toggle, gate, and copy are byte-untouched. NO scope reduction:
the segment reads REAL persisted data — no static placeholders. None of the smoothed-posture
sites may alter cash/MTM byte-identity — every hunk is a smoothed sibling arm beside an
existing site.

Execution note (plan-check MEDIUM-3): the files_modified list is 14 files — at the top of the
single-plan budget but a single concern (one basis, sibling arms). NO split. The executor
CHECKPOINTS after each task: run that task's verify gate to green, summarize state (done /
verified / remaining), THEN start the next task — never carry a red gate forward.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-CONTEXT.md
@.planning/phases/132-smoothed-mtm-worker-persistence/132-01-SUMMARY.md
@DESIGN.md

Read-first anchors (verified 2026-07-22 against current code):
- src/lib/types.ts:565-571 (MtmDailyReturnsSeriesPayload — schema/basis-literal/rows/
  gap_spans/conventions shape; the discriminated per-kind payload union comment at :573)
- src/lib/factsheet/types.ts:560-580 (metricsByBasis + mtmGate typing — the omission
  contract: absent key, never JSON null; reason closed-set includes
  "unsmoothed_options_book"), :604-611 (seriesByBasis)
- src/lib/factsheet/build-payload.ts:427-431 (seriesByBasis assembly), :496-497
  (metricsByBasis/mtmGate passthrough)
- src/lib/factsheet/composite-read-path.ts:35 (parseMtmSeriesPayload), :95-114
  (readMtmSeries), :196 (metrics_json_by_basis read), :261 (composite mtmSeries read),
  :283 + :382-383 (mtmGate + metricsByBasis construction — the availability = key-present
  pattern to clone), :343 (singleKeyBasisOpts), :350-366 (the by-basis extraction + the
  `mtm === undefined && reason === undefined → {}` early return — the MEDIUM-2 edge),
  :402 (shouldReadSingleKeyMtmSeries)
- src/app/factsheet/[id]/v2/page.tsx:130-147 (single-key MTM series read+threading:
  shouldReadSingleKeyMtmSeries → readMtmSeries → 4th arg into singleKeyBasisOpts — the
  smoothed sibling lands HERE, plan-check HIGH-1)
- src/app/factsheet/[id]/v2/basis-context.tsx:33 (Basis union), :59 (default cash), :100-118
  (useBasisMetrics overlay via overlayBasisScalars), :216-232 (useBasisSeriesView bundle
  swap), :325 (levered persisted-scalar re-pin `basis !== "mark_to_market" || L <= 0`),
  :417-427 (leverageEligibleFor)
- src/app/factsheet/[id]/v2/factsheet-context.tsx:207-237 (Phase-103 F2.3 brush x-range
  clamp: setXRange upper bound = max(cash axis, MTM bundle axis) − 1; the :216-219 comment
  documents the "recent days permanently unreachable" harm the widening prevents — the
  smoothed bundle needs the SAME treatment, plan-check HIGH-2)
- src/app/factsheet/[id]/v2/MasterBrush.basis.test.tsx (the existing F2.3 clamp pin
  harness — "widened clamp: an MTM index past the cash length is REACHABLE (neuter clamp →
  RED)"; the smoothed reachability pin is its sibling case)
- src/app/factsheet/[id]/v2/FactsheetView.tsx:339 (mtmBundlePresent predicate),
  :343/:419-421 (toggle-availability predicate), :505-515 (chart-caption three-state
  role=status copy), :804 (suppressRelative), :841 (composite basis eyebrow label ternary),
  :1128-1152 (mtmAvailable/mtmDisabledReasonCopy/mtmReasonTone), :1261-1273
  (SegmentedControl render with disabled/disabledReason)
- src/app/factsheet/[id]/v2/BatchDPanels.tsx:131 (peer-rank "computed on cash-settlement
  basis" note gated `basis === "mark_to_market"`)
- src/lib/factsheet/basis-metrics.ts (overlayBasisScalars + BASIS_KPI_MAP — basis-agnostic
  scalar overlay; expect NO change, verify)
</context>

<interfaces>
From Phase 132 (132-01-SUMMARY.md — the ACTUAL persisted server truth this plan renders):
- `strategy_analytics.metrics_json_by_basis.smoothed_mtm: Record<string, number>` — present
  ONLY when the smoothed pass completed; absent (never null) otherwise — the SAME omission
  contract as mark_to_market (types.ts:563-565)
- Persisted smoothed basis series under kind "smoothed_mtm_daily_returns"
  (`KIND_SMOOTHED_MTM` in basis_series.py) — for composites it reaches the payload through
  the same read path that builds seriesByBasis.mark_to_market; for SINGLE-KEY it must be
  read in page.tsx and threaded (Task 1 — the MTM sibling at page.tsx:130-147 is the
  template; this was the plan-check HIGH-1 gap)
- The persisted smoothed series payload may carry an OPTIONAL `nan_dates` key
  (basis_series.py:360-361 — Phase-105 D1 composite-only key, emitted only when the derive
  surfaced guard-NaN dates under zero_fill). The smoothed parser sibling must TOLERATE it
  exactly as parseMtmSeriesPayload does — never reject a payload for carrying it
- The MTM gate reason "unsmoothed_options_book" is UNCHANGED — on an options book the MTM
  segment stays disabled with its existing copy while Smoothed MTM is enabled
- Worker invariant (relied on in Task 3, stated per plan-check MEDIUM-2): whenever the
  worker persists `smoothed_mtm`, an MTM STORY is also present — either the
  `mark_to_market` key or a persisted `mtm_gated_reason` (132-01: degraded MTM persists
  null + reason, pinned by `test_degraded_mtm_persists_null_and_reason`; smoothing "OPENS
  what MTM keeps closed" always alongside the MTM gate decision). Task 1 nonetheless
  hardens the frontend read defensively (the :366 extension) so a reason-less
  {smoothed_mtm}-only row cannot be silently dropped
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Types + payload plumbing — smoothed_mtm through BOTH read paths (incl. single-key page.tsx)</name>
  <files>src/lib/types.ts, src/lib/factsheet/types.ts, src/lib/factsheet/build-payload.ts, src/lib/factsheet/composite-read-path.ts, src/lib/factsheet/composite-read-path.test.ts, src/app/factsheet/[id]/v2/page.tsx</files>
  <behavior>
    - src/lib/types.ts (plan-check HIGH-1): a per-kind payload type
      `SmoothedMtmDailyReturnsSeriesPayload` mirroring MtmDailyReturnsSeriesPayload
      (:565-571) — same schema/rows/gap_spans/conventions shape, `basis: "smoothed_mtm"`
      literal (matching what the Phase-132 worker persists — verify against
      132-01-SUMMARY.md before pinning); the discriminated per-kind union (audit M-0581
      comment at :573) gains the "smoothed_mtm_daily_returns" kind
    - src/lib/factsheet/types.ts: metricsByBasis gains optional smoothed_mtm (same omission
      contract doc as mark_to_market at :563-565); seriesByBasis gains optional smoothed_mtm
      BasisSeriesBundle; a smoothed gate shape mirroring mtmGate (available = key present;
      reason from a small closed set, e.g. "smoothed_basis_unavailable" |
      "mtm_basis_unavailable_for_venue" — reuse the venue reason where it applies, add ONE
      new reason for options-book-not-yet-recomputed/marks-missing; never a bare string
      invention per row)
    - composite-read-path: `readSmoothedSeries` sibling of readMtmSeries (:95-114) querying
      kind "smoothed_mtm_daily_returns" via the same service-role handle + degrade-to-null
      posture; a parser sibling of parseMtmSeriesPayload (:35) that validates the smoothed
      payload's `basis` literal (malformed/wrong-basis → null + warn, same defensive
      coercion — never render a mislabeled series) AND tolerates the OPTIONAL `nan_dates`
      key (basis_series.py:360-361) exactly as the MTM parser does — a payload carrying it
      parses fine; test pins a nan_dates-bearing fixture
    - composite-read-path: metrics_json_by_basis.smoothed_mtm present → payload
      metricsByBasis.smoothed_mtm populated and smoothed gate available:true; absent →
      key OMITTED and gate {available:false, reason} (clone the :382-383 pattern); composite
      series read mirrors the :261 mtmSeries site; mark_to_market handling byte-identical
      (existing tests unmodified)
    - EARLY-RETURN EDGE (plan-check MEDIUM-2): the singleKeyBasisOpts `:364-366` early
      return currently yields `{}` when `mtm === undefined && reason === undefined` — a
      naive clone would silently DROP a {smoothed_mtm}-only row (MTM degraded
      reason-lessly): no gates, no toggle, no error. Extend the predicate to ALSO check the
      smoothed key: return `{}` ONLY when mtm, reason, AND smoothed are ALL absent (the
      non-options fast path stays byte-identical — such rows have none of the three).
      Structural consequence to note in a code comment: past the extended early return the
      gates are ALWAYS constructed, so a payload with ANY by-basis story carries mtmGate —
      which is what lets FactsheetView :419-421 stay unwidened (Task 3)
    - build-payload: seriesByBasis carries the smoothed bundle when the persisted series
      exists (clone the :427-431 mark_to_market assembly); smoothed gate/metrics passthrough
      via opts (:496-497 pattern)
    - SINGLE-KEY READ + THREADING (plan-check HIGH-1 — the missing scope): page.tsx:130-147
      gains the smoothed sibling of the MTM-04 pattern — a
      `shouldReadSingleKeySmoothedSeries` sibling predicate (clone
      shouldReadSingleKeyMtmSeries :402: cheap key-present + status check so the hot
      non-options path stays roundtrip-free) → `readSmoothedSeries` → threaded into
      `singleKeyBasisOpts` (:343) exactly as mtmSeries rides as the 4th arg (extend the
      signature/opts with the smoothed sibling; `{}` for every non-options single-key
      strategy → byte-identical). A failed/malformed row degrades to no-bundle (charts stay
      cash) — same posture as MTM
    - WIRING-GUARD (plan-checks HIGH-1 + MEDIUM-2): a test that REDDENS when the page.tsx
      smoothed threading is neutered — pin singleKeyBasisOpts output: with the smoothed
      series supplied, buildOpts carries the smoothed bundle; with the threading removed
      (smoothed arg absent/null while metrics key present), the bundle is ABSENT — proving
      single-key options charts FOLLOW the basis rather than silently staying cash. The
      wiring-guard FIXTURE uses `metrics_json_by_basis = { smoothed_mtm }` WITHOUT
      `mark_to_market` and WITHOUT `mtm_gated_reason` — pinning the MEDIUM-2 edge in the
      same guard (the un-extended :366 predicate drops this fixture → RED). Test the
      wiring, not just the helper: assert the predicate gates the read AND the read's
      result reaches the opts (the call-site contract, per the wiring-guard discipline)
  </behavior>
  <action>
    Failing vitest FIRST (composite-read-path.test.ts sibling cases: parser incl. the
    nan_dates-tolerance fixture, readSmoothedSeries, gate/metrics construction incl. the
    {smoothed_mtm}-only :366 edge, singleKeyBasisOpts smoothed threading + wiring-guard,
    plus a build-payload case). Then make the type/plumbing changes above, finishing with
    the page.tsx:130-147 smoothed sibling read (mirror the MTM comment block's contract
    notes — same admin handle, same RLS posture, no visibility widening). Surgical: every
    hunk adds a smoothed sibling beside an existing mark_to_market site; the ONLY existing
    predicate edited is the :366 early return, and its extension is a pure additive check
    (byte-identical outcome for every row with no smoothed key). TS strict; absent means
    OMITTED, never null (types.ts:565 contract). Checkpoint: verify gate green + state
    summary before starting Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/composite-read-path.test.ts src/lib/factsheet/build-payload.arithmetic.test.tsx --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>smoothed_mtm flows server→payload on BOTH read paths — composite and single-key page.tsx (present and absent cases pinned, wiring-guard reddens when threading is neutered, {smoothed_mtm}-only fixture survives the extended :366 early return, nan_dates-bearing payload parses); per-kind payload type in src/lib/types.ts with the "smoothed_mtm" basis literal; mark_to_market paths byte-identical with existing tests unmodified; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: basis-context third arm — Basis union, scalar overlay, series swap, leverage re-pin + eligibility</name>
  <files>src/app/factsheet/[id]/v2/basis-context.tsx, src/app/factsheet/[id]/v2/basis-context.test.tsx, src/app/factsheet/[id]/v2/basis-context.leverage.test.tsx</files>
  <behavior>
    - Basis union (:33) gains "smoothed_mtm"; default stays "cash_settlement" (:59, D5 —
      pin with a fresh-provider test)
    - useBasisMetrics (:100-118): basis === "smoothed_mtm" → overlayBasisScalars with
      payload.metricsByBasis?.smoothed_mtm ?? {} (exact clone of the mark_to_market arm —
      same seven mapped scalars, α/IR and unmapped keys untouched)
    - useBasisSeriesView (:216-232): smoothed basis with payload.seriesByBasis.smoothed_mtm
      present → serve the smoothed bundle; bundle ABSENT under smoothed → return the base
      payload unchanged (same graceful posture as the MTM arm — the toggle gate in Task 3
      prevents reaching this state, but the hook must not crash)
    - LEVERED PERSISTED-SCALAR RE-PIN (plan-check HIGH-2, basis-context.tsx:325): the
      strategyMetrics re-pin currently early-returns on `basis !== "mark_to_market" || L <= 0`
      — under smoothed with L > 0 it must re-pin Sharpe/Sortino from
      payload.metricsByBasis?.smoothed_mtm (exact structural mirror of the MTM arm, incl.
      the L=0 honest-zeros carve-out), avoiding the Phase-107 L=1↔L≠1 Sharpe/Sortino jump
      under the smoothed basis; test: levered smoothed view pins the persisted smoothed
      Sharpe/Sortino, and L=0 still yields the honest derived zeros
    - LEVERAGE ELIGIBILITY (plan-check HIGH-2, basis-context.tsx:417-427):
      leverageEligibleFor gains the smoothed clause mirroring the MTM one — under smoothed,
      eligible ⇔ payload.seriesByBasis?.smoothed_mtm AND payload.metricsByBasis?.smoothed_mtm
      are BOTH present (gate leverage on smoothed bundle/metrics presence; absent-bundle
      smoothed views are leverage-INeligible, never a re-pin against missing scalars); tests
      for present/absent both ways
    - cash_settlement and mark_to_market arms byte-identical — every existing
      basis-context test green UNMODIFIED (incl. the leverage variants)
  </behavior>
  <action>
    Failing tests FIRST in basis-context.test.tsx (clone the mark_to_market overlay/series
    cases for smoothed) and basis-context.leverage.test.tsx (re-pin + eligibility smoothed
    cases). Then extend the union and add the smoothed arms as exact structural clones of the
    MTM arms — overlay, series view, the :325 re-pin, and the :417-427 eligibility clause.
    basis-metrics.ts (overlayBasisScalars/BASIS_KPI_MAP) is basis-agnostic — expect zero
    change there; if a change seems needed, STOP and re-read, that is a design smell. Update
    the :28-33 and :89-99 doc comments to name three bases (surgical — only the sentences
    that enumerate bases). Checkpoint: verify gate green + state summary before starting
    Task 3.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/basis-context.test.tsx" "src/app/factsheet/[id]/v2/basis-context.leverage.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>Third arm overlays scalars + swaps series from failing-first; default-cash pinned; levered smoothed re-pin + eligibility pinned (no L=1↔L≠1 jump, no leverage on absent bundle); all existing basis-context tests green unmodified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SegmentedControl third segment + a decided smoothed posture at EVERY basis-consuming site (switches AND bundle reads)</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/BatchDPanels.tsx, src/app/factsheet/[id]/v2/factsheet-context.tsx, src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx, src/app/factsheet/[id]/v2/MasterBrush.basis.test.tsx</files>
  <behavior>
    - The basis SegmentedControl (:1261-1273) renders THREE segments wherever it renders
      today (same availability predicate :419-421 — deliberately NOT widened: after the
      Task-1 :366 extension, ANY by-basis story — mtm key, reason, or smoothed key —
      constructs mtmGate, so `payload.mtmGate != null` already covers the
      {smoothed_mtm}-only edge structurally; the worker invariant (smoothed ⇒ MTM story
      present, 132-01 `test_degraded_mtm_persists_null_and_reason`) makes that edge
      near-unreachable in production anyway. Pin it: a {smoothed_mtm}-only payload renders
      the toggle with MTM disabled and Smoothed enabled); labels per DESIGN.md conventions
      (read DESIGN.md before wording/casing)
    - Smoothed segment enabled ⇔ the smoothed gate is available (metrics key present);
      disabled → disabledReason from a smoothedDisabledReasonCopy mapping (clone
      mtmDisabledReasonCopy :1147 — closed-set reasons from Task 1, honest copy like
      "Smoothed basis not yet computed for this strategy" for the recompute-pending/marks
      case; NEVER an empty tooltip, NEVER hiding the segment)
    - THE PHASE'S RENDER PROOF: options-book fixture where mtmGate =
      {available:false, reason:"unsmoothed_options_book"} AND smoothed gate available →
      MTM segment disabled with its EXISTING copy, Smoothed segment ENABLED; selecting it
      shows the smoothed scalars (overlay pinned via a KPI-strip assertion)
    - Selecting Smoothed with the gate available but no series bundle: KPIs overlay, charts
      keep base series, no crash (the Task-2 graceful arm — pinned)
    - RENDER-SURFACE ENUMERATION (plan-check HIGH-2 — each site gets a decided smoothed
      posture AND a test; none may silently fall into a cash/else arm under smoothed):
      · FactsheetView.tsx:841 composite basis EYEBROW label — the binary ternary becomes
        three-way: under smoothed it must say a smoothed label (e.g. "SMOOTHED
        MARK-TO-MARKET" per DESIGN.md casing), NOT "CASH SETTLEMENT"; test pins the label
        under all three bases
      · FactsheetView.tsx:339 bundle-present predicate gains a smoothed sibling
        (smoothedBundlePresent over seriesByBasis.smoothed_mtm), and the :505-515 chart
        CAPTION three-state honesty extends per-basis: smoothed + bundle present →
        "Charts show the smoothed mark-to-market series." (exact copy per DESIGN.md);
        smoothed + bundle ABSENT → the honest fallback "Charts show the cash-settlement
        series. Smoothed mark-to-market applies to summary metrics only."; cash → ""
        unchanged; MTM copy byte-identical; tests for the smoothed-present and
        smoothed-absent captions
      · FactsheetView.tsx:804 suppressRelative — smoothed + ABSENT smoothed bundle must
        ALSO suppress α/IR (never render the cash joint-series relatives under the smoothed
        story); test: smoothed absent-bundle fixture hides α/IR, smoothed present-bundle
        shows them
      · BatchDPanels.tsx:131 peer-rank note — the "Peer rank is computed on the
        cash-settlement basis; the cohort is not recomputed per basis" note (F6
        no-invented-data) must ALSO render under smoothed (extend the
        `basis === "mark_to_market"` gate to include "smoothed_mtm"; cash still renders
        nothing extra); test pins the note under smoothed
      · factsheet-context.tsx:222-237 BRUSH X-RANGE CLAMP (plan-check HIGH-2 — the missed
        BUNDLE-READ site, invisible to a `=== "mark_to_market"` grep): the Phase-103 F2.3
        setXRange upper clamp sizes to `max(payload.dates.length,
        seriesByBasis?.mark_to_market?.dates.length ?? 0)` — it consults ONLY the MTM
        bundle. On the FLAGSHIP options-book case (MTM bundle absent/gated, smoothed
        serving the charts), a smoothed axis LONGER than cash would have its recent days
        PERMANENTLY brush-unreachable — the exact harm the site's own :216-219 comment
        documents for MTM. Add `payload.seriesByBasis?.smoothed_mtm?.dates.length ?? 0` as
        a THIRD max() term (and to the useCallback dependency array at :236) — strictly
        additive: max over an extra `?? 0` term is a NO-OP for every cash/MTM payload
        (byte-identity preserved; `fullRange` stays cash-sized, untouched). Extend the
        F2.3 comment block to name the smoothed bundle. PIN in
        MasterBrush.basis.test.tsx as the sibling of the existing "widened clamp: an MTM
        index past the cash length is REACHABLE (neuter clamp → RED)" case: a fixture with
        smoothed bundle LONGER than cash (and NO mark_to_market bundle — the flagship
        shape) keeps a smoothed index past the cash length reachable; reverting the third
        term → RED
      · (basis-context.tsx:325 + :417-427 are Task 2's sites — already pinned there)
    - GREP MANDATE (plan-checks HIGH-2 + MEDIUM-1 closure — the `=== "mark_to_market"`
      grep is how the brush-clamp site slipped: it misses `!==` sites AND bundle-READ
      sites): run `grep -rn 'mark_to_market' src/app/factsheet src/lib`, filter out
      comment-only lines and test files, and classify EACH remaining hit — switch sites
      (`===`/`!==`), bundle/metrics READ sites (`seriesByBasis`/`metricsByBasis` access),
      type/literal declarations — with its smoothed posture: either one of the sites
      enumerated above (this task + Tasks 1-2) or an explicitly-decided,
      comment-documented smoothed posture with a pinning test. List the full classified
      audit in the SUMMARY; a non-comment hit with no decided posture is a defect
    - Two-basis venues (perp/USD, gate unavailable): Cash/MTM behavior byte-identical —
      existing FactsheetBody.basis, MasterBrush.basis, and kpistrip tests green
      UNMODIFIED; NONE of the site changes above touches cash/MTM byte-identity (every
      hunk is a smoothed additional arm or an additive max() term)
  </behavior>
  <action>
    Failing tests FIRST in FactsheetBody.basis.test.tsx (three-segment render, the
    MTM-disabled-smoothed-enabled options case, the {smoothed_mtm}-only toggle-render case,
    disabled-reason copy case, plus the enumerated-site pins: eyebrow label, caption
    three-state × smoothed present/absent, suppressRelative × present/absent, peer-rank
    note under smoothed) and in MasterBrush.basis.test.tsx (smoothed-longer-than-cash
    reachability sibling). Then extend FactsheetView: derive
    smoothedAvailable/smoothedReason/smoothedBundlePresent beside the mtm equivalents
    (:339, :1128-1152), add the third segment to the SegmentedControl items with
    disabled/disabledReason, make the :841 eyebrow three-way, extend the :505-515 caption
    and :804 suppressRelative, extend the BatchDPanels.tsx:131 gate, add the
    factsheet-context.tsx third max() term + dependency, and update only the comments that
    enumerate the basis story. Read DESIGN.md before choosing the segment label ("Smoothed
    MTM" unless DESIGN.md dictates otherwise) and the eyebrow/caption copy — flag any
    deviation rather than inventing. Do not touch mtmDisabledReasonCopy/mtmReasonTone
    behavior. Finish with the broadened grep-every-hit audit. Then run the full frontend
    gates.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2" src/lib/factsheet --no-file-parallelism && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Three segments render with honest-disabled smoothed reason; the MTM-gated/smoothed-open options case AND the {smoothed_mtm}-only case pinned; eyebrow/caption/suppressRelative/peer-rank-note/brush-clamp smoothed postures each pinned by a test (incl. the smoothed-longer-than-cash reachability RED-provable pin); the broadened `grep -rn 'mark_to_market'` audit classifies every non-comment hit with a decided posture; full factsheet vitest surface + tsc + lint green with zero pre-existing test modifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server payload → public factsheet render | persisted by-basis numbers render to anonymous visitors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-08 | Spoofing/Info | fabricated numbers on missing basis | mitigate | honest-disabled segment; absent key ⇒ no overlay, no synthesized data (no-invented-data rule); reasons from a typed closed set; parser rejects wrong-basis payloads (never a mislabeled series) |
| T-131-09 | Tampering | overlay leaking across bases | mitigate | overlayBasisScalars maps only the seven pinned scalars; α/IR untouched — pinned by existing + new overlay tests; suppressRelative extended so absent-bundle smoothed never shows cash relatives under a smoothed label |
| T-131-SC | Tampering | package installs | accept | no new packages in this phase |
</threat_model>

<verification>
- `npx vitest run` green on the factsheet v2 + lib/factsheet surfaces; `npx tsc --noEmit`
  clean; `npm run lint` clean; coverage thresholds hold (CI gate)
- `git diff` audit: every hunk is a smoothed sibling beside an existing mark_to_market site,
  an additive predicate/max() term (the :366 early-return extension, the brush-clamp third
  term), or the new copy mapping — zero behavior change on cash/MTM paths
- `grep -rn 'mark_to_market' src/app/factsheet src/lib` (filter comment-only lines and
  test files): EVERY remaining hit — switch sites, bundle/metrics READ sites, type
  declarations — classified with a decided, test-pinned smoothed posture (HIGH-2 +
  MEDIUM-1 audit — enumerate the classification in the SUMMARY)
- No pre-existing test modified or deleted
- Executor checkpointed after each task's verify gate (MEDIUM-3 — 14 files, single plan,
  no red gate carried forward)
</verification>

<success_criteria>
- Three-basis SegmentedControl live: Cash (default, headline) / MTM (existing gate + copy
  byte-identical) / Smoothed MTM (enabled ⇔ persisted smoothed_mtm present, honest-disabled
  with mapped reason otherwise)
- On an options book: MTM disabled ("unsmoothed_options_book" copy unchanged), Smoothed
  selectable, rendering the redistributed no-spike daily series and overlaid scalars — on
  single-key AND composite factsheets (single-key charts follow via the page.tsx threading,
  wiring-guarded), with the FULL smoothed axis brush-reachable (recent days never clipped
  by the cash-sized clamp)
- Every basis-dependent render surface (eyebrow, caption, suppressRelative, leverage
  re-pin/eligibility, peer-rank note, brush x-range clamp) has a decided smoothed posture
  pinned by a test — no smoothed numbers under a wrong label, no silent cash fallthrough,
  no unreachable smoothed days
- A {smoothed_mtm}-only by-basis row (reason-less MTM degradation) flows through the
  extended :366 early return to a rendered toggle — pinned at the read path AND the render
- Phase close-out after this plan: gsd-code-reviewer + gsd-verifier per founder review
  policy; live verification (Phoenix re-onboard + Zav2 + perp-only byte-identity spot check,
  83-PLAN Task 10) is the named follow-up, blocked on re-onboarding the deleted key
</success_criteria>

<output>
Create `.planning/phases/133-smoothed-mtm-factsheet-toggle/133-01-SUMMARY.md` when done.
</output>
