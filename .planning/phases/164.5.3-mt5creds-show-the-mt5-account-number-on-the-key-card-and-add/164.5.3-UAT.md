---
status: testing
phase: 164.5.3-mt5creds-show-the-mt5-account-number-on-the-key-card-and-add
source: [164.5.3-VERIFICATION.md]
started: 2026-09-20T14:17:46Z
updated: 2026-09-20T14:17:46Z
---

## Current Test

number: 1
name: DESIGN.md conformance of the MT5 identifier line and the update-password dialog
expected: |
  The identifier and button render per DESIGN.md's card/typography contract, are visually
  distinct from the existing Reconnect affordance, and no copy anywhere implies the venue
  CONFIRMED the identifier — only that it is the login this key is connected with.
awaiting: user response

## Tests

### 1. DESIGN.md conformance of the MT5 identifier line and the update-password dialog
expected: Visual QA of the `MT5 account {id}` line (font metric, em-dash when NULL) and the
  "Update password" button/dialog on BOTH `ApiKeyManager` and `AllocatorExchangeManager`, in the
  active AND disconnected sections, against DESIGN.md.
  The identifier and button render per DESIGN.md's card/typography contract, are visually
  distinct from the existing Reconnect affordance, and no copy anywhere implies the venue
  CONFIRMED the identifier — only that it is the login this key is connected with.
result: [pending]
why_human: Visual appearance and copy tone are judgment calls; DESIGN.md conformance is a human
  decision, not a programmatic one.

### 2. Live end-to-end credential update against a real MT5 account
expected: Founder-only. ⛔ This repo's standing rule forbids any agent from entering, reading,
  decrypting or driving a real credential — so this test cannot be delegated. Identify or connect
  an MT5 key whose password is wrong, such that the allocator worker has already written
  `sync_status` of `revoked` or `error`, then use the new "Update password" affordance with the
  correct password.
  The PATCH returns 200; the card immediately shows the corrected status (no "Key revoked"
  pill); and on the NEXT ledger-refresh / allocator-poll tick the key is picked up by the
  automated fan-out again — not merely that the DB row now reads `sync_status='idle'`. That
  tick is the real-world confirmation of the CR-01 fix, which the verifier could only confirm
  at the persisted-payload level through mocked tests.
result: [pending]
why_human: Requires a live broker credential and observing a live cron/worker tick — real-time
  external-service behaviour that no agent may exercise.

### 3. Ship-time deploy sequencing of the GRANT migration
expected: ⚠️ This one is time-boxed to the ship itself, not to a later QA pass. Confirm migration
  `20260920120000` is applied and GREEN on PROD — or that the two deploy targets are otherwise
  sequenced — BEFORE the Vercel production promotion carrying the `constants.ts` / `types.ts`
  projection change lands. The alternative is splitting the merge into two PRs.
  No window exists in which a deployed frontend requests `venue_account_id` from PostgREST
  before the GRANT is live. The migration's own header documents this ordering; nothing in the
  code enforces it, because it spans two independent pipelines.
result: [pending]
why_human: Deploy ordering across two independent pipelines — Supabase's auto-apply behind the
  Production human reviewer gate, and the Vercel build/promote — cannot be enforced from inside
  either artifact. The migration's own header names it as a human ship-time decision.

## Summary

total: 3
passed: 0
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps

[none — the verifier scored 9/9 must-haves with ZERO code gaps. These three items are
`human_needed` because they are outside an agent's reach, not because anything is missing.]
