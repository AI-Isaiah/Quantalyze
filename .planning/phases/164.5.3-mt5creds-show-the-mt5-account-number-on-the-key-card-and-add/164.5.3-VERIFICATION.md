---
phase: 164.5.3-mt5creds-show-the-mt5-account-number-on-the-key-card-and-add
verified: 2026-09-20T16:20:00Z
status: human_needed
score: 9/9 must-haves verified (0 present-behavior-unverified)
verified_at_sha: 2b8098052c82e92e51b31ecbf77cec2957a93f7f
drift_subjects:
  - supabase/migrations/20260920120000_api_keys_venue_account_id_grant.sql
  - scripts/pg-lane/fixtures/35-fixture-api-keys-venue-account-id.sql
  - src/lib/constants.ts
  - src/lib/types.ts
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/exchanges/AllocatorExchangeManager.tsx
  - src/app/api/keys/validate-and-encrypt/route.ts
  - analytics-service/routers/internal.py
  - src/app/api/keys/[id]/rotate-secret/route.ts
  - src/lib/resilient-fetch.ts
  - src/lib/analytics-schemas.ts
  - src/components/strategy/UpdateMt5SecretDialog.tsx
  - src/lib/wizardErrors.ts
  - src/lib/audit.ts
  - src/lib/queries.ts
overrides_applied: 0
human_verification:
  - test: "Visual QA of the 'MT5 account {id}' line (font-metric, em-dash on NULL) and the 'Update password' button/dialog on BOTH ApiKeyManager and AllocatorExchangeManager (active + disconnected sections), against DESIGN.md."
    expected: "The identifier and button render per DESIGN.md's card/typography contract, are visually distinct from the existing Reconnect affordance, and no copy anywhere implies the venue confirmed the identifier (only 'the login this key is connected with')."
    why_human: "Visual appearance and copy-tone judgment are not programmatically verifiable; DESIGN.md conformance is a human call."
  - test: "End-to-end live flow, founder-only (per this repo's standing rule: no agent enters, decrypts, or drives a real credential): connect or identify an MT5 key with a wrong password so the allocator worker has already written sync_status='revoked' or 'error', then use the new 'Update password' affordance with the correct password."
    expected: "The PATCH returns 200; the card immediately shows the corrected status (no 'Key revoked' pill); and, on the NEXT ledger-refresh/allocator-poll tick, the key is picked up by the automated fan-out again (not just that the DB row now reads sync_status='idle') — the real-world confirmation of the CR-01 fix, which this report can only confirm at the persisted-payload level via mocked tests."
    why_human: "Requires a live MT5 broker credential and observing a live cron/worker tick — no agent may hold or exercise real credentials, and the fan-out tick is real-time external-service behavior."
  - test: "At ship time: confirm migration 20260920120000 is applied and GREEN on PROD (or the founder has otherwise sequenced the two deploy targets) BEFORE the Vercel production promotion that ships the constants.ts/types.ts projection change lands."
    expected: "No window exists where a deployed frontend requests `venue_account_id` from PostgREST before the GRANT is live — the migration's own header (CR-01, review finding CR-02) now documents this ordering requirement, but nothing in the code enforces it; this is a one-time human ship-sequencing action, not a code truth this report can verify."
    why_human: "Deploy ordering across two independent CI/CD pipelines (Supabase auto-apply + human Production reviewer gate, and the Vercel build/promote) cannot be enforced from inside either artifact and is explicitly named in the migration's own header as requiring a human ship-time decision."
---

# Phase 164.5.3 (MT5CREDS): Verification Report

**Phase Goal:** A founder (and a first-time client) can tell which MT5 account a key card belongs
to, and can correct a wrong password without deleting the key.
**Verified:** 2026-09-20
**Status:** human_needed
**Re-verification:** No — initial verification.

## Summary

All 9 derived must-have truths (spanning decision IDs D-01-PRIME, D-02-PRIME, D-03, D-04, D-05,
D-06, D-07 — cross-referenced against every plan's `requirements` field and CONTEXT.md's locked
decisions, including the `⛔ AMENDMENT` section that supersedes D-01/D-02) are **code-verified**:
read directly against the committed source at HEAD (not inferred from SUMMARY.md prose), and
exercised by real, non-vacuous, currently-passing tests I ran myself in this session (not merely
cited from SUMMARY/REVIEW). This included re-running the migration's own self-verify DO block on a
throwaway pg-lane cluster, the full Python and TypeScript test suites scoped to every file this
phase touched, `mypy --strict` and `tsc --noEmit`, and reading every "fixed" defect from
`164.5.3-REVIEW.md`'s Round 1 (3 critical, 4 warning findings) directly in the code to confirm the
fix landed — not merely trusting Round 2's "CLEAN" verdict.

**The phase is code-complete and the goal is achieved in the codebase.** Status is `human_needed`
rather than `passed` solely because three items are outside any agent's verification power in this
repo (visual/DESIGN.md conformance judgment, a live-broker end-to-end flow no agent may drive per
this repo's own credential-handling rule, and a deploy-sequencing action across two CI/CD pipelines
that cannot be enforced from inside either artifact) — none of the three indicates a code defect.

## Goal Achievement

### Observable Truths

| # | Truth | Decision | Status | Evidence |
|---|-------|----------|--------|----------|
| 1 | A founder viewing an MT5 key's card (either component) sees the MT5 login it was connected with — `.font-metric`, un-separated, em-dash on NULL, MT5-only gate | D-01-PRIME, D-02-PRIME, D-06 | ✓ VERIFIED | `venue_account_id` field present in `src/lib/constants.ts` (`API_KEY_USER_COLUMNS_ARR` + derived literal), `src/lib/types.ts` (`ApiKey` + `ApiKeyRowSchema`); rendered at `ApiKeyManager.tsx:504-507` and `AllocatorExchangeManager.tsx:797-801`/`898-902`, both `exchange === "mt5"`-gated, `key.venue_account_id ?? "—"`, no `toLocaleString`/comma grouping anywhere near it. `npx vitest run` on both component test files: 139/139 pass (ran this session). |
| 2 | `authenticated` can SELECT `venue_account_id`; `anon` cannot; no new column, no row read/written | D-01-PRIME, D-02-PRIME | ✓ VERIFIED | Migration `20260920120000_api_keys_venue_account_id_grant.sql` read in full: catalog-only guarded pre-check + `GRANT SELECT (venue_account_id)` + self-verify DO block, no `ALTER TABLE`/`ADD COLUMN`. **Ran the migration myself** on a throwaway pg-lane cluster this session (`scripts/pg-lane/run.sh --gate ...`): exit 0, `RAISE NOTICE 'Migration 20260920120000: authenticated can SELECT ...; anon cannot.'` printed twice (initial pass + gate re-run). |
| 3 | An MT5 key connected via "Add Key" (`validate-and-encrypt`, not just the wizard) gets `venue_account_id` populated at creation | D-01-PRIME, D-02-PRIME | ✓ VERIFIED | `src/app/api/keys/validate-and-encrypt/route.ts:762-763,786`: `const venueAccountId = isMt5 ? api_key.trim() : null;` stamped as an explicit INSERT field. `npx vitest run` on this route's test file: 106/106 pass (ran this session). |
| 4 | The credential-update path accepts ONLY the new password — never a login or broker server field | D-03 | ✓ VERIFIED | `src/app/api/keys/[id]/rotate-secret/route.ts:137-139`: `interface RotateSecretBody { new_secret?: unknown; }` — no other credential field exists in the parsed body type anywhere in the file. `analytics-service/routers/internal.py:579-580`: `class RotateSecretRequest(BaseModel): new_secret: str` (plus an optional `user_id` for owner-scoping defense-in-depth, not a credential field). |
| 5 | The new password is re-validated against the live broker BEFORE persisting; a failed validation persists NOTHING | D-04 | ✓ VERIFIED (behavior-dependent, exercised by real tests) | Python: `_validate_mt5_key`'s exceptions propagate uncaught before any `encrypt_credentials` call (`internal.py:735-761`); 5 Python tests assert `encrypt_credentials`/`_validate_mt5_key` were NOT called on each failure path — ran `pytest tests/test_mt5_rotate_secret_internal.py` myself: **15/15 passed**, exit 0. TS: the admin UPDATE is reached only after the seam call's `catch` block is NOT entered; route test suite proves this with not-called assertions — ran `npx vitest run` on the route test file myself as part of the 139/139 pass above. |
| 6 | A validated success clears `sync_error` and re-admits the key into the automated fan-out (not just clearing the UI's error pill) | D-05 | ✓ VERIFIED (behavior-dependent, exercised by a real test; the CR-01 review finding, confirmed fixed in code) | `route.ts:435-436`: `sync_error: null, sync_status: "idle"` written in the SAME UPDATE statement as the ciphertext, with a detailed code comment tracing the exact defect (a `'revoked'` key permanently excluded from both ledger-refresh and allocator-poll enqueuers) and its precedent fix (`reconnect_allocator_api_key`). Confirmed by direct grep of `src/lib/queries.ts:3545`: the eligibility predicate is `is_active && sync_status !== "revoked" && disconnected_at == null` — `"idle"` satisfies it. The route's happy-path test asserts the exact `sync_status: "idle"` in the UPDATE payload (part of the 139/139 pass). |
| 7 | The persist write is admin-client, ownership-scoped by an explicit filter (never RLS alone, since UPDATE is fully revoked) | D-07 | ✓ VERIFIED | `route.ts:373,444-446`: `createAdminClient()` then `.eq("id", id).eq("user_id", user.id).eq("exchange", "mt5")` on every UPDATE call, pre-verified via a user-scoped ownership read at step 6 of the handler. |
| 8 | A venue-identity collision on the backfill does NOT discard an otherwise-successful correction (WR-02, review finding) | D-04, D-07 | ✓ VERIFIED (behavior-dependent, exercised by a real, non-trivial test) | `route.ts:466-473`: on a 23505 naming `VENUE_IDENTITY_CONSTRAINT`, the route retries the SAME statement WITHOUT `venue_account_id`. Test `"WR-02: retries WITHOUT venue_account_id on a first-attempt collision, and the password fix + status clear still land"` asserts the retry's payload carries the ciphertext + `sync_status: "idle"` and returns 200 (not 409) — read and confirmed this test in full; part of the 139/139 pass. |
| 9 | The route's own rate-limit refusal renders an honest, recognizable error — not the "we cannot tell you whether your last action took effect" UNKNOWN envelope with a Retry, for a cap the route itself imposed (CR-03, review finding) | D-06 (ErrorEnvelope contract) | ✓ VERIFIED | `route.ts:264-271`: `rateLimitDenyJson(rl, { throttledBody: { code: "RATE_LIMITED", ... }, misconfiguredBody: { code: "SEAM_MISCONFIGURED", ... } })`. Confirmed `"RATE_LIMITED"` is rostered in `DASHBOARD_DIALOG_ROUTE_CODES` for `"keys/[id]/rotate-secret"` (`src/lib/wizardErrors.ts:5393`) via direct grep. |

**Score:** 9/9 truths verified (0 present-behavior-unverified, 0 overrides)

### Additional review findings independently confirmed fixed in code (not just cited from REVIEW.md)

`164.5.3-REVIEW.md` Round 1 found 3 critical + 4 warning issues; Round 2 (a separate `gsd-code-reviewer` + `silent-failure-hunter` pass at head `8d9f8cdb`) reported the fix round clean. I did not take Round 2's "CLEAN" verdict on faith — I independently re-read the fixed code for every Round 1 finding:

| Finding | Fix confirmed in code at HEAD |
|---|---|
| CR-01 (sync_status stranded) | `sync_status: "idle"` in the persist payload, `queries.ts` eligibility predicate confirmed to admit it |
| CR-02 (migration/deploy-race outage) | DEPLOY ORDER header added to the migration file (documentation fix — the review's own stated remedy, since ordering cannot be enforced from inside either artifact; carried into human_verification below) |
| CR-03 (rate-limit UNKNOWN envelope) | `throttledBody`/`misconfiguredBody` with `code` supplied; `RATE_LIMITED` rostered |
| WR-01 (seam code discarded) | `seamCode` carried as an own property on the thrown Error, read from `seamErrorCode(errBody)` |
| WR-02 (collision discards correction) | retry-without-backfill implemented and tested (Truth #8 above) |
| WR-03 (unconditional reconnect) | `disconnected_at` is NOT touched anywhere in the persist payload — a stronger fix than the review's suggested option (a), confirmed by direct code read and by the route's own extensive code comment explaining why (grepped `analytics-service/**`: the only writer of `disconnected_at` is the user-initiated Disconnect RPC, never a worker/cron path) |
| WR-04 (untrimmed backfill identifier) | `analytics-service/routers/internal.py:774`: `"venue_account_id": (login or "").strip() or None`, with a test proving the ciphertext still uses the UNTRIMMED login (`test_rotate_secret_trims_the_returned_identifier_but_not_the_encrypted_login`, confirmed passing in my own pytest run) |

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/migrations/20260920120000_api_keys_venue_account_id_grant.sql` | GRANT-extend migration, 068-shaped, self-verifying | ✓ VERIFIED | Read in full; ran the self-verify on a throwaway pg-lane cluster this session, exit 0 |
| `scripts/pg-lane/fixtures/35-fixture-api-keys-venue-account-id.sql` | pg-lane stand-in column | ✓ VERIFIED | Exists, consumed by the gate command I ran |
| `src/lib/constants.ts` / `src/lib/types.ts` | Three-way sync (`API_KEY_USER_COLUMNS_ARR`, `ApiKey`, `ApiKeyRowSchema`) | ✓ VERIFIED | Grepped; `venue_account_id` present in all three |
| `src/components/strategy/ApiKeyManager.tsx` | Renders identifier + Update password button | ✓ VERIFIED | Both present, MT5-gated |
| `src/components/exchanges/AllocatorExchangeManager.tsx` | Same, both active + disconnected sections | ✓ VERIFIED | Both present in both sections |
| `analytics-service/routers/internal.py` (`rotate_key_secret`) | Decrypt/re-validate/re-encrypt, plaintext never leaves Python | ✓ VERIFIED | Read in full; 15/15 Python tests pass; `mypy --strict` clean on this file (ran this session) |
| `src/app/api/keys/[id]/rotate-secret/route.ts` | PATCH route, owner-scoped, MT5-only, D-03..D-07 | ✓ VERIFIED | Read in full; part of the 139/139 vitest pass |
| `src/components/strategy/UpdateMt5SecretDialog.tsx` | Shared dialog, password-only, ErrorEnvelope on failure | ✓ VERIFIED | Read; wired into both cards; part of the 139/139 pass |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| Migration GRANT | `API_KEY_USER_COLUMNS_ARR` → `ApiKey` → card render | Three-way sync, confirmed by grep across all three files | ✓ WIRED |
| `ApiKeyManager` / `AllocatorExchangeManager` "Add Key" | `validate-and-encrypt` INSERT | Both cards POST to this route with `persist: true`; the route stamps `venue_account_id` | ✓ WIRED |
| `UpdateMt5SecretDialog` | `PATCH /api/keys/[id]/rotate-secret` | `fetch` call in the dialog, confirmed by test asserting the exact URL/method | ✓ WIRED |
| Rotate-secret route | `POST /internal/keys/{id}/rotate-secret` | `resilientFetch("keys-rotate-secret", ...)`, seam registered in `SEAM_BUDGETS`/`SEAM_ROUTE_BUDGETS`, confirmed via `seam-constants.pin.test.ts`/`seam-budgets.invariant.test.ts` (both passing, ran this session) | ✓ WIRED |
| Rotate-secret persist | `queries.ts` fan-out eligibility | `sync_status: "idle"` write confirmed to satisfy `is_active && sync_status !== "revoked" && disconnected_at == null` | ✓ WIRED |

### Behavioral Spot-Checks / Tests Run This Session

| Check | Command | Result | Status |
|---|---|---|---|
| Python rotate-secret suite | `pytest tests/test_mt5_rotate_secret_internal.py -v` (main-checkout `.venv`) | 15 passed, exit 0 | ✓ PASS |
| Python mypy strict | `mypy --strict --follow-imports=silent routers/internal.py` | Success: no issues found in 1 source file | ✓ PASS |
| TS rotate-secret route + dialog + both cards | `npx vitest run` on the 4 files | 139 passed, 0 failed | ✓ PASS |
| TS validate-and-encrypt route | `npx vitest run src/app/api/keys/validate-and-encrypt/route.test.ts` | 106 passed | ✓ PASS |
| TS seam/dialog/wizard-error census suite | `npx vitest run` on 7 census/invariant files | 630 passed | ✓ PASS |
| TS typecheck | `npm run typecheck` | clean (`tsc --noEmit`, no output) | ✓ PASS |
| Migration self-verify | `pg-lane --gate` on a throwaway cluster | exit 0, both RAISE NOTICE lines printed | ✓ PASS |
| Debt markers | `grep -n -E "TBD\|FIXME\|XXX"` across all 14 touched source files | 0 matches in every file | ✓ PASS |
| Planning hygiene | `npm run check:planning-hygiene` | OK — 6705 tracked files, no local-username/home-path leak | ✓ PASS |

### Requirements Coverage

This is an INSERTED phase with no `.planning/REQUIREMENTS.md` entries (confirmed by grep — zero
hits for `164.5.3`, `MT5CREDS`, or any `D-0[1-7]` ID outside one unrelated line at
`REQUIREMENTS.md:357`). Per `164.5.3-RESEARCH.md`'s own framing and the ROADMAP's phase header, the
seven decision IDs (D-01-PRIME, D-02-PRIME, D-03, D-04, D-05, D-06, D-07) ARE the requirement set.
All seven are covered by the truths table above; no orphaned requirements.

### Anti-Patterns Found

None. Debt-marker scan (`TBD`/`FIXME`/`XXX`) across all 14 source files this phase touched: zero
matches. No stub returns, no hardcoded empty data reaching render, no console.log-only
implementations found in the files read for this report.

### Deferred Items

| # | Item | Addressed In | Evidence |
|---|---|---|---|
| 1 | `KEY_UNDECRYPTABLE` degrades to the generic UNKNOWN/500 envelope on this route (no `VENUE_WIRE_CODE_TO_VERDICT` row) | Phase 164.5.4 MT5RECON-GAP | ROADMAP.md's Phase 164.5.4 goal explicitly names this: "`KEY_UNDECRYPTABLE` is discarded at the language seam... Phase 164.5.3 FALSIFIED that premise... Why it belongs HERE rather than in 164.5.3: it is the same defect CLASS as (2)." Also documented at the throw site (`route.ts:323-331`, "⚠️ RESIDUAL, not closed by this fix") and in commit `8d9f8cdb` as a founder instruction to route it there rather than force it into this phase's fix round. |

### Human Verification Required

See the `human_verification` frontmatter block above for the full test/expected/why_human detail on
each of the three items (visual/DESIGN.md conformance, the live-broker end-to-end flow no agent may
drive, and the deploy-sequencing action across two independent CI/CD pipelines). None of these
indicates a code defect — all nine derived truths are code-verified with real, currently-passing
tests I ran myself this session, not inferred from SUMMARY.md claims.

### Gaps Summary

No gaps. Every must-have truth derived from the phase's locked decisions (D-01-PRIME through D-07)
is verified against the actual codebase at HEAD, not against SUMMARY.md prose. Every Round-1 review
finding (3 critical, 4 warning) was independently re-confirmed fixed by reading the code directly,
not by trusting Round 2's "CLEAN" verdict. All test suites I ran myself this session pass. The
status is `human_needed`, not `passed`, solely because of the three items above that are structurally
outside any agent's verification power in this repository (this repo's own standing rule: "No agent
enters, reads, logs or echoes a credential. The founder enters credentials.").

---

_Verified: 2026-09-20_
_Verifier: Claude (gsd-verifier)_
