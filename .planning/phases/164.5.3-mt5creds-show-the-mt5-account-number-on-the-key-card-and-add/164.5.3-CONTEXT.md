# Phase 164.5.3 — MT5CREDS: Context

**Phase:** 164.5.3 MT5CREDS — show the MT5 account number on the key card and add a credential-update path
**Discussed:** 2026-09-20 · branch `feat/164.5.3-mt5creds` from `main` @ `76542eee`
**Mode:** autonomous — grey areas decided from repo evidence and recorded here, per the founder's standing delegation. No `checkpoint:decision` was emitted for ordinary design calls.

---

## Goal

A founder (and a first-time client) can tell **which MT5 account** a key card belongs to, and can **correct a wrong password without deleting the key** — preserving the row, its id, and its sync history.

---

## Premises, re-measured at HEAD (not inherited)

| # | Claim | Verdict |
|---|---|---|
| 1 | No update path exists | ✅ **CONFIRMED.** Every key-touching route under `src/app/api/**` exports only `POST` or `GET` — `keys/validate-and-encrypt`, `keys/sync`, `keys/[id]/permissions`, `strategies/create-with-key`, `strategies/composite/add-key`, `debug-key-flow`. **Zero `PATCH`/`PUT`.** A wrong password is fixable only by Delete + Add Key. |
| 2 | The card cannot identify the account | ✅ **CONFIRMED.** `API_KEY_USER_COLUMNS_ARR` (`src/lib/constants.ts`) does not expose any account identifier, so the card falls back to `label` — sourced from the OPTIONAL nickname field. Several MT5 accounts render indistinguishably. |
| 3 | The exposure idiom is established | ✅ **CONFIRMED and it is the fourth instance.** Migration 027 (`20260410225608`, SEC-005) REVOKEs `SELECT ON api_keys FROM anon, authenticated` at table level, then GRANTs an allowlist back. Migrations **066** (`sync_error`), **068** (`last_429_at`) and **075** (`disconnected_at`) each added exactly ONE column to the GRANT *and* to the ARR, each with an inline reason comment. |

### ⭐ The measurement that reshaped the design

⛔ **There is no plaintext login column to expose.** `api_key_encrypted` is, per migration 027's own rationale block, a **"Fernet ciphertext blob of the credential JSON"**. `api_secret_encrypted` and `passphrase_encrypted` are **legacy and currently NULL**. The MT5 slot mapping documented in `validate-and-encrypt` — login → `api_key`, investor password → `api_secret`, broker server → `passphrase` — describes the *request payload*, which is then sealed into that single blob alongside `dek_encrypted` / `nonce`.

**Consequence:** the account identifier cannot be surfaced by reading an existing column, and decrypting to populate one is exactly what the locked design forbids. A **new display column, written at the moment the plaintext is legitimately in hand server-side**, is the only route that neither decrypts nor relaxes a REVOKE. This is not a preference — it follows from the storage shape.

---

## Locked decisions

### D-01 — A GENERAL display column, populated only by MT5 for now
**Decision:** add one nullable text column holding the venue's non-secret account identifier, named generically (not `mt5_login`). Only the MT5 path populates it initially.
**Why:** the repo's own precedent settles this. All three predecessor columns are generally named and populated only where applicable — `last_429_at` is stamped solely on ccxt 429s and is NULL for venues that never rate-limit that way. A venue-specific name would force a rename migration the first time a second venue has an identifier worth showing, and an applied migration is never edited.
**Refused:** `mt5_login`. ⛔ Also refused: populating it for other venues in this phase — that is speculative work with no measured need.

### D-02 — No backfill migration. The column arrives NULL and the update path is itself the backfill.
**Decision:** the migration adds the column and extends the GRANT. **Nothing reads data.** Existing rows stay NULL until their next *successful* credential write — either a fresh create or the new update path this phase adds.
**Why:** ⛔ A data-reading `RAISE EXCEPTION` in a migration is forbidden (`[164.8-DATA-DEPENDENT-MIGRATION-ESCAPE]`): shared TEST holds PROD's catalogue and never its data, so such a migration can pass PROD and REFUSE TEST, and a refused TEST apply BLOCKS the PROD apply. Beyond that, a backfill would have to decrypt — the one thing the locked design rules out.
⭐ **The pleasing consequence:** the act of fixing a wrong password is also the act that makes the card identifiable. The two halves of this phase feed each other rather than sitting side by side.
**Deferred:** an out-of-band service-role backfill for keys whose password is already correct. It needs credentials, so it is founder-only; see Deferred Ideas.

### D-03 — The update path accepts the PASSWORD ONLY
**Decision:** the credential-update route changes the secret and nothing else. It does not accept a new login, and it does not accept a new broker server.
**Why:** the login *is* the identity the display column advertises, and the broker server qualifies it. Allowing either to change would silently re-point a row — and its entire sync history — at a different account, while the card kept showing the old one. That is a worse version of the defect this phase exists to remove. Changing account is what Delete + Add Key legitimately means.

### D-04 — Re-validate against the broker BEFORE persisting. Always.
**Decision:** the update path validates the new credential against the live broker and persists only on success. A failed validation persists nothing and mutates nothing.
**Why:** `keys/validate-and-encrypt` already establishes validate-then-encrypt as this codebase's shape, and a silent accept of a still-wrong password would reproduce precisely the defect being fixed — the user would believe they had corrected it. ⛔ This is a data-integrity and user-facing path, so its gates BLOCK.

### D-05 — Clear the failure state only on a successful validation, never on submission
**Decision:** on a validated, persisted update, clear the auth-failure state (`sync_error`, and lift a soft `disconnected_at`). On a failed validation, leave every status field exactly as it was.
**Why:** clearing an error because someone *attempted* a fix asserts something untrue about the account's state. The status fields are read by the exchange-status pill, so a premature clear renders a healthy pill over a still-broken key. D-04 makes success a measured fact rather than an assumption, which is what earns the right to clear.

### D-06 — UI follows DESIGN.md, with one deliberate, recorded deviation
**Decision:**
- The key card is a **card** primitive per DESIGN.md § Component Patterns — white surface, 1px `#E2E8F0`, 8px radius, subtle shadow. ⛔ Not a data panel; do not conflate.
- The account identifier renders in **Geist Mono with `tabular-nums` via `.font-metric`**, per the Numbers Contract's Typeface row.
- ⭐ **DEVIATION, deliberate:** the Numbers Contract's *Integers* row prescribes thousands separators. ⛔ **Not applied here.** An MT5 login is an **identifier, not a quantity** — rendering it `1,234,567` would be wrong and unrecognisable to the person matching it against their broker terminal. The contract's own framing is that "numbers are the product"; an account number is a name that happens to be numeric. Recorded here because the contract binds by default and a silent departure would read as drift.
- **Absent value renders `—`**, per the Numbers Contract's Null rule: *"em-dash. Never `0`, never blank, never a fabricated value."* ⭐ This is not a degradation workaround — it is the product-wide contract, and it is honest about absence, which is exactly right for a key whose identifier is not yet known (D-02).
- Update failures render through **`ErrorEnvelope`** (`src/components/error/ErrorEnvelope.tsx`), sourcing copy via `buildEnvelope()`, per DESIGN.md § Error Envelope. ⛔ No bespoke error UI.

---

## Consequences this phase carries

- ⚠️ **It ships a migration** ⇒ ⛔ three reviewers before any apply (`migration-reviewer`, `rls-policy-auditor`, `silent-failure-hunter`), then merge → `apply-test` on shared TEST → the PROD `apply` behind the **`Production` human reviewer gate**. ⭐ That gate is a REAL STOP the founder must approve; it is planned for, never routed around.
- ⚠️ **User-facing and credential-adjacent** ⇒ anti-vacuity BLOCKS; review findings here BLOCK rather than being fix-or-drop.
- ⛔ **Never widen the REVOKE on the encrypted columns.** The new column is additive and narrow; `api_key_encrypted`, `api_secret_encrypted`, `passphrase_encrypted`, `dek_encrypted` and `nonce` stay service-role only.
- ⚠️ The GRANT allowlist and `API_KEY_USER_COLUMNS_ARR` **must stay in sync** — migration 027 says so explicitly and the three precedent migrations each moved both together.
- ⚠️ Next.js here carries breaking changes versus training data — read `node_modules/next/dist/docs/` before writing route or component code.

---

## Deferred ideas (captured, not acted on)

| Idea | Why deferred |
|---|---|
| Out-of-band backfill of the display column for keys whose password is already correct | Requires decrypting with the service role ⇒ founder-only, credentials involved. Not blocking: D-02 populates on next write and `—` is honest meanwhile. |
| Allowing the broker server to change (genuine broker migrations happen) | Changes account identity — same objection as D-03. Its own phase if a real need is measured. |
| Populating the display column for non-MT5 venues | Speculative; no measured need. The general column name (D-01) leaves the door open at zero cost. |

---

## Non-negotiable for every downstream agent

⛔ **The repo is PUBLIC, `.planning/` is TRACKED, and this phase's subject is an account number.** Never write a real MT5 account number, broker server name, credential or credential-shaped literal into any plan, test fixture, migration, CHANGELOG entry or summary. **Synthetic placeholders only.** Record verdicts, never specimens — a proof-of-absence can publish the thing it proves absent.
⛔ No agent enters, reads, logs or echoes a credential. The founder enters credentials.

---

# ⛔ AMENDMENT — 2026-09-20, after research + pattern mapping

⛔ **THE CENTRAL PREMISE ABOVE IS FALSE AT HEAD, AND D-01 / D-02 ARE SUPERSEDED.** Two agents found this independently and the orchestrator confirmed it by direct read. It is recorded here rather than silently rewritten, because the reasoning that produced the wrong answer is worth keeping.

## What was wrong

The premise block claims *"there is no plaintext login column to expose… a new display column is the only route."* **`api_keys.venue_account_id` already exists** — migration `20260812083206` (Phase 154 / WIZCONT-02): nullable text, generically named, MT5-only in practice, holding the broker login as a non-secret identity. That is D-01's shape, already shipped.

**How the miss happened, so it is not repeated:** the scout measured `API_KEY_USER_COLUMNS_ARR` and the REVOKE migration — i.e. *"what is exposed"* — and never asked *"does an identifier column already exist, unexposed?"* Absence from the exposure allowlist was read as absence from the table. ⚠️ A column can exist and be invisible to every query that asks what the client can see.

## What is true at HEAD (measured, each traced to a source)

| Fact | Evidence |
|---|---|
| `venue_account_id` exists, nullable, MT5-only today | migration `20260812083206`; the ccxt adapter's `ValidationResult` carries no account-identity field, so ccxt venues stay NULL |
| It is NOT exposed | no GRANT for it; absent from `API_KEY_USER_COLUMNS_ARR` and from the `ApiKey` interface |
| It is populated ONLY by the wizard RPC | `create_wizard_strategy` stamps it inside the SECURITY DEFINER body; `validate-and-encrypt` and `composite/add-key` do **not** |
| Its server-side derivation | `venueAccountId = isMt5 ? api_key.trim() : null` in `strategies/create-with-key` |
| **Reachability forgery: CLOSED** | Phase 156 / CONNECT-02, CONNECT-05. The call rides `rpcAdmin`, and `20260814120000` withdrew `authenticated`'s EXECUTE, so the RPC is unreachable from a browser. *"The ONLY value this parameter can carry is the one the server derived."* |
| **Provenance: STILL OPEN** | *"156 RESTATED IT, it did not close it… The stored value is 'what the server passed', NEVER 'what the venue confirmed'."* No in-database oracle exists. Residual CR-01. |
| `api_keys` UPDATE is fully revoked | `REVOKE UPDATE ON public.api_keys FROM anon, authenticated` (`20260810120000`), **no re-grant anywhere** |
| **Three-way sync, not two** | every precedent column also gets a field + comment in the `ApiKey` interface (`src/lib/types.ts`) |
| Migration size template | of 066 / 068 / 075, only **068** (`last_429_at`) is a clean standalone column+GRANT+self-verify; 066 and 075 bundle theirs into larger multi-feature migrations |

## D-01 (SUPERSEDED) → D-01′ — Reuse `venue_account_id` and publish it. **Founder decision, 2026-09-20.**

⛔ **This OVERRIDES an explicit prohibition in shipped code**, and that is why it was escalated rather than decided here. Migration `20260812083206`'s `COMMENT ON COLUMN` reads: *"⛔ Never echo this value to the browser (UI-SPEC): **non-secret is not the same as publish**."* — a direct refutation of the "the login is not a secret, so expose it" reasoning this phase started from.

**Why the override is sound, stated so a later reader can re-judge it:**
- The prohibition's own stated harm is a **browser session forging the value and collapsing two different accounts onto one row**. That vector is **closed** — the RPC is unreachable from a browser and the value can only be the one the server derived.
- What survives is **integrity, not confidentiality**: the database cannot *prove* the value. But the row is the owner's own, RLS-scoped, so publishing discloses nothing across a tenant boundary.
- The server derives it from the same credential MT5 validation probe-asserts (`assert_expected_login`).

⛔ **The obligation this creates:** the card must present it as *the login this key was connected with*, **never** as a venue-verified account. ⛔ No copy anywhere may imply confirmation. Publishing a value the database cannot vouch for is acceptable **only** while the UI does not overclaim it.
⚠️ The CR-01 provenance residual stays open and is NOT closed by this phase. Do not record it as closed.

## D-02 (SUPERSEDED) → D-02′ — No new column, and the backfill coupling is dissolved

The column already carries values for wizard-created MT5 keys, so the card is useful immediately for those rows rather than after a re-validation cycle.
⚠️ **But population is NOT universal.** `validate-and-encrypt` and `composite/add-key` never stamp it, so keys created by those paths are NULL. ⛔ The plan must either populate at every live create chokepoint or state plainly which paths stay NULL — a column that is silently NULL on some paths is the failure mode D-02 was written to avoid.
⭐ **Consequence:** D-02's elegance ("the update path is itself the backfill") is **gone**, and with it the coupling between this phase's two halves. They are now genuinely separable. `—` still renders for NULL, per the Numbers Contract.

## D-04 — mechanism decided. **Founder decision, 2026-09-20: a new Python internal endpoint.**

Re-validation needs the **broker server**, which exists only inside the Fernet blob; no plaintext copy is readable from TypeScript. ⛔ Next.js must not decrypt, and the broker server must not round-trip through the browser.
**Decision:** add an internal analytics-service endpoint that decrypts, validates against the broker, and re-encrypts — mirroring `/internal/keys/{key_id}/permissions`, whose posture is that **plaintext never leaves Python**.
⚠️ This touches **two deploy targets** (Vercel for route + card, Railway for Python) and has **no in-repo precedent to copy**, since no update path exists anywhere today. Researcher confidence on the design: MEDIUM. The plan must treat it as design work, not transcription.

## D-07 (NEW) — the persist step cannot use a user-scoped client

`api_keys` UPDATE is revoked from `authenticated` with no re-grant, so a user-scoped `.update()` returns **42501**, not a benign "0 rows". Use `createAdminClient()` or a `SECURITY DEFINER` RPC. ⭐ The only existing UPDATE precedent on this table is **`reconnect_allocator_api_key`**, which is also the closest structural match to D-05's clear-on-success requirement.

## Route-skeleton note (supersedes any "copy `keys/[id]/permissions`" reading)

`keys/[id]/permissions` uses an OLDER manual URL-path id extraction that its own comment calls a compatibility shim. ⛔ Do not copy that. Use `strategies/[id]/name/route.ts`'s modern `params: Promise<{id}>` convention for the skeleton, and take the broker-revalidation call shape (`resilientFetch`, seam-error taxonomy, correlation id) from `keys/validate-and-encrypt`. ⚠️ Blend by responsibility; do not merge them into one borrowed file.
⚠️ Failure codes must extend `src/lib/wizardErrors.ts` (which already has `KEY_MT5_MASTER_PASSWORD`) — `buildEnvelope()` silently degrades an unknown code to a generic `UNKNOWN` envelope.

## DESIGN.md conflict, surfaced not blended

DESIGN.md § Numbers Contract requires one formatter module per surface family. The exchanges/key-card family does **not** follow it — `formatUsd` / `formatRelative` are in-file module-scope helpers in `AllocatorExchangeManager.tsx`, and no shared formatter module exists for that family. ⭐ Follow the **live, tested in-file precedent** for this phase's single new field; extracting a module for a two-line helper is over-abstraction. The gap is flagged for a future cleanup phase, per this repo's surface-the-conflict rule. ⛔ Not blended.
⭐ `AllocatorExchangeManager.tsx` already has the exact `.font-metric` + em-dash idiom D-06 requires; `ApiKeyManager.tsx` has neither and needs it net-new. ⚠️ **Both render a founder-visible key card — both must change or the fix is half-done.**

## Test-fixture pitfall

Existing `api_keys` row-shaped mocks (e.g. `ApiKeyManager.test.tsx`'s M-0456 projection block) will need the new field, or `npm run typecheck` fails. ⚠️ `typecheck` is SEPARATE from `npm run lint`.
