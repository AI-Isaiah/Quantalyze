---
phase: 126-factsheet-connected-key-api-verified-factsheet-render-blocki
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/strategy/[id]/page.tsx"          # candidate — only if the repro pins the bug here
  - "src/components/notes/StrategyNoteCard.tsx"  # candidate — H1 suspect subtree
autonomous: false   # the seeded repro leg needs the LOCAL dev env + test-Supabase creds; if they cannot be configured in-session the task records that honestly and falls back to the research hypothesis — it NEVER claims a repro it did not run
requirements: [FACTSHEET-01]
must_haves:
  truths:
    - "The exact SSR throw for the api_verified+connected-sfox-key /strategy/[id] render is pinned to a file:line and classified (transient-unavailable vs genuinely-wrong) — OR the SUMMARY explicitly states the repro could not run and records the H1 fallback hypothesis"
    - "If a genuine bug is pinned, it is fixed at source (never suppressed) and a regression test fails without the fix"
    - "No secrets (service-role keys, passwords) appear in any committed file or SUMMARY"
  artifacts:
    - path: ".planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-01-SUMMARY.md"
      provides: "Root-cause record: pinned throw file:line + classification, or explicit repro-not-run statement"
  key_links:
    - from: "e2e/sfox-badge.spec.ts admin/owner factsheet legs"
      to: "src/app/strategy/[id]/page.tsx"
      via: "authed SSR render of the legacy v1 route (the ONLY authed v1 render in the suite)"
      pattern: "goto\\(`/strategy/"
---

<objective>
Wave 0 (mandatory, Rule 6, locked decision): reproduce the `/strategy/[id]` SSR
throw for the seeded `api_verified` + connected-sfox-key strategy LOCALLY, pin
the exact throwing module:line, classify it (transient-unavailable → degrade in
126-02; genuinely-wrong → fix at source HERE), and land the source fix + a
regression test that fails without it.

Purpose: FACTSHEET-01 forbids fixing on a guess. Static analysis (126-RESEARCH.md)
ruled out the shared query/`auth.getUser()`/admin-client paths and ranks the
authed-only v1 sub-tree (`user_notes` fetch → `StrategyNoteCard` SSR subtree) as
H1 — but the repro is the decisive disambiguator. The sfox owner/admin e2e legs
are the ONLY authenticated SSR render of the legacy v1 `/strategy/[id]` page in
the entire suite (RESEARCH "Correction to prior memory"), which is why prod never
surfaced this.

Output: a pinned root cause (or an honest repro-not-run record + H1 fallback),
plus the source fix + failing-without-fix regression test when a genuine bug is
found.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-RESEARCH.md
@.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-PATTERNS.md
@src/app/strategy/[id]/page.tsx
@e2e/sfox-badge.spec.ts
@e2e/helpers/seed-test-project.ts
</context>

<interfaces>
<!-- Key contracts the executor needs — extracted this session; no exploration required. -->

From e2e/helpers/seed-test-project.ts (the ONLY sanctioned fixture — do not hand-roll seeds):
```typescript
export interface SeededSfoxVerifiedStrategy { /* strategyId, namePrefix, apiKeyId, owner:{email,password}, admin:{email,password} */ }
export async function seedSfoxVerifiedStrategy(opts?): Promise<SeededSfoxVerifiedStrategy>  // :1130
export async function cleanupSfoxVerifiedStrategy(seeded: Pick<SeededSfoxVerifiedStrategy,"namePrefix"|"apiKeyId">)  // :1263
// Requires TEST_SUPABASE_URL + TEST_SUPABASE_SERVICE_ROLE_KEY env (spec HAS_SEED_ENV gate, sfox-badge.spec.ts:54-56)
```

From src/app/strategy/[id]/page.tsx (the authed-only v1 sub-tree — H1 suspect):
```typescript
// lines 100-118: createClient() → supabase.auth.getUser() → user_notes
//   .select("content, updated_at").eq("user_id",user.id).eq("scope_kind","strategy")
//   .eq("scope_ref", strategy.id).maybeSingle()
// lines 200-206: {user && <StrategyNoteCard strategyId={strategy.id}
//   initialContent={initialNoteContent} initialLastSavedAt={initialNoteSavedAt} />}
```

From playwright.config.ts: non-CI runs auto-start `npm run dev` as webServer
(reuseExistingServer: true, baseURL http://localhost:3000).

From .github/workflows/ci.yml e2e-seeded job (:1338+): the reference env recipe —
seed via `SEED_CONFIRM_STAGING=true npx tsx scripts/seed-demo-data.ts` with
NEXT_PUBLIC_SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY = TEST project; Next build
needs NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY inlined at build
time (test project `qmnijlgmdhviwzwfyzlc`, NEVER prod `khslejtfbuezsmvmtsdn`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Seeded local repro — pin the exact SSR throw line (Wave 0, mandatory)</name>
  <files>none committed (repro evidence goes into the SUMMARY; a scratch env file, if any, stays in the session scratchpad and is NEVER committed)</files>
  <read_first>
    - .planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-RESEARCH.md — "Ranked hypotheses" H1-H4 table + "Common Pitfalls" 3 (repro on v1 `/strategy/[id]`, NOT `/v2`, while LOGGED IN)
    - e2e/sfox-badge.spec.ts — the two failing legs: "owner: the strategy factsheet shows the api_verified badge (+ axe)" (:131) and "admin: an elevated session reads the sfox strategy's api_verified tier" (:171)
    - e2e/helpers/seed-test-project.ts:1084-1280 — seedSfoxVerifiedStrategy / cleanupSfoxVerifiedStrategy contract
    - playwright.config.ts — local webServer auto-start (`npm run dev`)
    - .github/workflows/ci.yml:1338-1420 — the e2e-seeded job's test-env build recipe (which env vars the local run must mirror)
  </read_first>
  <action>
    ENV GATE FIRST: check whether the local session can reach the TEST Supabase
    project — TEST_SUPABASE_URL + TEST_SUPABASE_SERVICE_ROLE_KEY (seed helper)
    and NEXT_PUBLIC_SUPABASE_URL + NEXT_PUBLIC_SUPABASE_ANON_KEY pointed at the
    test project for the Next server (check existing `.env.local`/`.env.test*`
    files and the macOS Keychain service `quantalyze-test` per project memory).
    Point ONLY at the test project `qmnijlgmdhviwzwfyzlc`; the prod project
    `khslejtfbuezsmvmtsdn` must never receive seed writes (the seed script's
    SEED_CONFIRM_STAGING interlock + prod-URL probe are the guard — keep them).

    IF ENV AVAILABLE — run the repro: with the test env exported, run the two
    failing spec legs (`npx playwright test e2e/sfox-badge.spec.ts -g "admin"`
    is the minimal throw-repro: it asserts only the badge, no axe, and the spec
    seeds + cleans up itself via beforeAll/afterAll). Playwright auto-starts
    `npm run dev`; capture the SSR error stack from the dev-server stdout the
    moment the authed `/strategy/[id]` render throws. Record: (a) the exact
    throwing file:line, (b) the error class/name, (c) classification —
    TRANSIENT-UNAVAILABLE (a dependency such as the analytics service was
    unreachable → the fix is the 126-02 degradation boundary, plus possibly a
    CI env decision recorded for 126-03) vs GENUINELY-WRONG (an unguarded
    access / mis-wired call / bad data-shape handling → fix at source in Task
    2). Cross-check the pinned line against RESEARCH H1-H4 and say which
    hypothesis held. Optionally confirm on a prod-mode build (`npm run build &&
    npm run start` with the same env) if the dev stack is ambiguous. Do NOT
    change any production code in this task.

    IF ENV NOT AVAILABLE IN-SESSION — STOP the repro attempt and record, in the
    SUMMARY, the exact missing prerequisite (which env var / credential), state
    EXPLICITLY that the repro did NOT run, and adopt the research fallback:
    H1 (the authed `user_notes` fetch → `StrategyNoteCard` SSR sidecar) as the
    working hypothesis, with the CI `e2e-seeded` job (wired blocking in 126-03)
    as the disambiguator. NEVER claim the repro ran when it did not (Rule 12).

    Either way: never print or persist secret values; reference env vars by
    NAME only in all output and the SUMMARY.
  </action>
  <verify>
    <automated>npx playwright test e2e/sfox-badge.spec.ts -g "admin" 2>&1 | tail -20  # env present: exits non-zero pre-fix (the repro) with the throw visible in dev-server output; env absent: reports the HAS_SEED_ENV skip, which is the honest fallback signal</automated>
  </verify>
  <acceptance_criteria>
    - SUMMARY contains EITHER a "Pinned root cause" section with file:line +
      error name + transient/genuine classification + which of H1-H4 held, OR an
      explicit "Repro not run — env unavailable" section naming the missing
      prerequisite and adopting the H1 fallback. No third state.
    - No secret VALUE appears in the SUMMARY or any committed file (grep the
      SUMMARY for "eyJ" and "sbp_" and the seeded passwords → zero hits).
    - No production source file modified by this task (git status clean of src/ changes).
  </acceptance_criteria>
  <done>The throw is pinned + classified, or the repro-not-run state is honestly recorded with the H1 fallback adopted.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the pinned bug at source + regression test that fails without it</name>
  <files>the module Task 1 pinned (candidates: src/app/strategy/[id]/page.tsx, src/components/notes/StrategyNoteCard.tsx or its notes subtree) + a co-located regression test file</files>
  <read_first>
    - The Task 1 pinned module (whichever it is) IN FULL before editing
    - src/components/notes/StrategyNoteCard.tsx — the H1 client card (consumes NoteRender/useNoteAutoSave/NoteSaveStatus)
    - src/app/strategy/[id]/page.test.tsx — existing co-located test conventions (T31 insertion-order contract must keep passing)
    - src/app/error.test.tsx — the jsdom boundary-test harness pattern (`/** @vitest-environment jsdom */`, @testing-library/react, act)
  </read_first>
  <action>
    Branch on Task 1's classification — this task is conditional by design:

    GENUINELY-WRONG (a real bug pinned): fix it AT THE PINNED LINE, minimally
    and at source — e.g. a null/shape guard where the data contract actually
    allows the value, or correcting a mis-wired call. No try/catch-swallow, no
    suppression, no behavior change beyond the fix (Rule 3/6). Then write a
    regression test co-located with the fixed module (repo convention: every
    *.test.tsx sits beside its source; jsdom header per src/app/error.test.tsx)
    that reproduces the pinned failure input and FAILS without the fix — prove
    it by running the test against the pre-fix code (git stash the fix, run,
    observe red, unstash) and record the red/green pair in the SUMMARY (Rule 9,
    regression-first).

    TRANSIENT-UNAVAILABLE: there is no source bug to fix — record in the SUMMARY
    that the source-level remedy IS the 126-02 degradation boundary, and record
    the CI-env implication for 126-03 (RESEARCH Open Question 2: if the throw is
    the analytics dependency, degrading in code is the prod hardening AND the
    e2e env's missing ANALYTICS_SERVICE_URL becomes a transient-outage test
    case, not a gap). Make no speculative code change.

    REPRO-NOT-RUN: make NO speculative source change on a guess (Rule 6 — the
    planner/executor has no license to fix an unpinned bug). Record that the
    126-02 boundary + `<main>` land regardless (they are independently-verified
    defects). ⚠️ The REAL safety net is the W1 anti-mask assertion in 126-03,
    NOT a "post-boundary CI throw" — once the 126-02 boundary wraps the H1
    suspect the page no longer throws, so an e2e "throws again" disambiguator
    can NEVER fire and would let a genuinely-wrong note bug hide behind the
    amber fallback while the badge-only legs go green. Instead, the 126-03 owner
    leg asserts the note card actually renders on the healthy path (and the
    degraded fallback is absent): if that assertion RED-fails, the note bug is a
    genuine-wrong that the boundary masked → a follow-up gap plan re-enters with
    that failure (and the now-runnable seeded env — creds are in the macOS
    Keychain `quantalyze-test`) as the repro. NOTE: this branch is the
    exception; the seeded env IS reachable in-session (Keychain creds + test
    project qmnijlgmdhviwzwfyzlc), so the PINNED path above is expected.
  </action>
  <verify>
    <automated>npx vitest run --no-file-parallelism "src/app/strategy/[id]" 2>&1 | tail -5  # existing page tests stay green; if a regression test landed, it is in this slice (or run the pinned module's own test file) and exits 0 WITH the fix</automated>
  </verify>
  <acceptance_criteria>
    - If a fix landed: the regression test exists co-located with the fixed
      module, the SUMMARY records the observed RED (without fix) → GREEN (with
      fix) pair, and `npx vitest run --no-file-parallelism <that test file>`
      exits 0.
    - If no fix landed: the SUMMARY states which branch applied
      (transient-unavailable or repro-not-run) and why no source change is the
      correct outcome — never a silent skip.
    - `npm run lint` passes on touched files; page.test.tsx T31 (note-card
      insertion order) still green.
    - grep of the diff shows no bare try/catch newly wrapping the whole page
      render (no suppression): `git diff -U0 -- "src/app/strategy/[id]/page.tsx" | grep -v '^[+-][+-]' | grep -c "catch"` is 0 unless the SUMMARY justifies a targeted, classified catch.
  </acceptance_criteria>
  <done>The genuine bug (if any) is fixed at source with a failing-without-fix regression test; otherwise the no-change rationale is recorded honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local session → TEST Supabase project | service-role key crosses; seed writes must hit ONLY `qmnijlgmdhviwzwfyzlc` |
| server render → client HTML | any fix must not move server error text into client-rendered output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Information Disclosure | repro logs / SUMMARY | mitigate | reference secrets by NAME only; grep SUMMARY for token prefixes (eyJ, sbp_) before commit |
| T-126-02 | Tampering | seed writes | mitigate | env gate pins TEST project URL; keep the seed script's SEED_CONFIRM_STAGING + prod-URL probe intact |
| T-126-03 | Information Disclosure | any source fix touching the `user_notes` query | mitigate | do NOT widen the user_notes select/filters — RLS per-user privacy stays exactly as-is (RESEARCH Security Domain) |
| T-126-04 | Repudiation | classification record | mitigate | SUMMARY must contain exactly one of the three honest outcome states (pinned / transient / not-run) — a skipped repro is never claimed done |
</threat_model>

<verification>
- Task 1 outcome recorded in one of the three honest states; secrets absent from all artifacts.
- Task 2 branch executed per classification; vitest slice `src/app/strategy/[id]` green; lint green.
</verification>

<success_criteria>
- FACTSHEET-01 root-cause leg satisfied: the throw is pinned + classified via a real local repro, OR the repro-not-run state is explicitly recorded with the H1 fallback and the CI disambiguator named.
- If a genuine bug existed, it is fixed at source with a regression test proven to fail without the fix.
</success_criteria>

<output>
Create `.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-01-SUMMARY.md` when done (root-cause record is the load-bearing section).
</output>
