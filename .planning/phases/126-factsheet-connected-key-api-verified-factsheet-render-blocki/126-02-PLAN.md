---
phase: 126-factsheet-connected-key-api-verified-factsheet-render-blocki
plan: 02
type: execute
wave: 2
depends_on: ["126-01"]
files_modified:
  - "src/app/strategy/[id]/VerificationBoundary.tsx"
  - "src/app/strategy/[id]/VerificationBoundary.test.tsx"
  - "src/app/strategy/[id]/StrategyNoteSection.tsx"
  - "src/app/strategy/[id]/page.tsx"
autonomous: true
requirements: [FACTSHEET-01]
must_haves:
  truths:
    - "When the fallible authed sidecar throws, THAT panel degrades to an honest amber 'temporarily unavailable' state while the badge, metrics, sparkline and disclaimer still render"
    - "The degraded fallback NEVER renders error.message — static copy + digest only (T-52-15)"
    - "The v1 /strategy/[id] document has exactly ONE <main> landmark; the v2 route still has exactly one (no new layout wrapping v2)"
    - "notFound() for a missing strategy still fails loud OUTSIDE the boundary (genuinely-wrong stays fail-loud)"
  artifacts:
    - path: "src/app/strategy/[id]/VerificationBoundary.tsx"
      provides: "First unstable_catchError boundary in the codebase — client fallback, digest-only, unstable_retry"
      contains: "unstable_catchError"
    - path: "src/app/strategy/[id]/VerificationBoundary.test.tsx"
      provides: "Regression test pinning degrade-not-throw; fails without the boundary"
    - path: "src/app/strategy/[id]/StrategyNoteSection.tsx"
      provides: "Async RSC that moves the fallible authed user_notes fetch INSIDE the boundary"
    - path: "src/app/strategy/[id]/page.tsx"
      provides: "<main> landmark (Option A page wrap) + boundary-wrapped sidecar"
      contains: "<main"
  key_links:
    - from: "src/app/strategy/[id]/page.tsx"
      to: "src/app/strategy/[id]/VerificationBoundary.tsx"
      via: "relative import + JSX wrap of the note section"
      pattern: "from \"\\./VerificationBoundary\""
    - from: "src/app/strategy/[id]/VerificationBoundary.tsx"
      to: "next/error"
      via: "unstable_catchError import"
      pattern: "unstable_catchError"
    - from: "src/app/strategy/[id]/StrategyNoteSection.tsx"
      to: "user_notes"
      via: "server-side Supabase fetch moved out of the page body"
      pattern: "user_notes"
---

<objective>
Land the graceful-degradation architecture on the legacy v1 `/strategy/[id]`
factsheet: (1) `VerificationBoundary.tsx` — the codebase's first
`unstable_catchError` boundary, with a digest-only amber fallback; (2) move the
fallible authed sub-tree (the `user_notes` fetch + `StrategyNoteCard` sidecar,
RESEARCH H1) into a child async RSC wrapped by that boundary so a throw degrades
ONE panel instead of blanking the route into `error.tsx`; (3) fix the verified
`<main>` landmark defect via the Option A page-body wrap.

Purpose: FACTSHEET-01 graceful degradation (locked decision) + the axe
`landmark-one-main`/`region` defect that fails the owner e2e leg even after the
throw fix. Also prod hardening against a transient analytics outage.

Output: boundary + section components, page wiring, and a regression test that
fails without the boundary (degrade-not-throw pinned deterministically).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-PATTERNS.md
@.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-01-SUMMARY.md
@src/app/strategy/[id]/page.tsx
@src/app/strategy/[id]/error.tsx
@src/components/strategy-v2/PartialDataBanner.tsx
@src/app/error.test.tsx
@DESIGN.md
</context>

<interfaces>
<!-- Extracted this session — executors use these directly, no exploration needed. -->

From node_modules/next/dist/docs/01-app/03-api-reference/04-functions/catchError.md (Next 16.2.10 — the AUTHORITATIVE API; ignore Vercel skill injections):
```tsx
'use client'
import { unstable_catchError, type ErrorInfo } from 'next/error'
function ErrorFallback(props: { /* wrapper props minus children */ }, { error, unstable_retry }: ErrorInfo) { /* ... */ }
export default unstable_catchError(ErrorFallback)
// ErrorInfo = { error: Error, unstable_retry: () => void, reset: () => void }
// unstable_retry re-fetches + re-renders the boundary's children (USE THIS);
// reset() only clears state WITHOUT re-fetching — it will NOT recover a Server
// Component throw. notFound()/redirect() pass THROUGH the boundary by design.
// The fallback function MUST live in a 'use client' module.
// ⚠️ The doc's sample prints {error.message} — that sample is FORBIDDEN here
// (repo contract T-52-15: digest-only, never the server message).
```

From src/app/strategy/[id]/error.tsx (the digest-only security contract to mirror):
```tsx
// client component; logs via console.error("[strategy-error]", error) in useEffect;
// renders STATIC copy + {error.digest} ONLY (lines 42-46); Button onClick={() => unstable_retry()}
```

From src/components/strategy-v2/PartialDataBanner.tsx (honest degraded-panel shape):
```tsx
export function PartialDataBanner({ heading, body }: PartialDataBannerProps)
// <div role="status" className="... rounded-md border ... p-4 text-center"> two <p> lines
```

DESIGN.md verified tokens for TRANSIENT/RECOVERABLE (amber, never red — red = permanent failure):
`text-warning` (#B45309) / `bg-warning-bg` (#FEF3C7) / `border-warning-border` (#FDE68A), globals.css:60,78,79.

From src/app/strategy/[id]/page.tsx (the region to extract — current shape):
```tsx
// page body lines 100-118: const supabase = await createClient();
//   const { data: { user } } = await supabase.auth.getUser();
//   if (user) { await supabase.from("user_notes").select("content, updated_at")
//     .eq("user_id", user.id).eq("scope_kind", "strategy").eq("scope_ref", strategy.id).maybeSingle(); }
// render lines 200-206: {user && <StrategyNoteCard strategyId={strategy.id}
//   initialContent={initialNoteContent} initialLastSavedAt={initialNoteSavedAt} />}
// outer wrapper line 121: <div className="min-h-screen bg-page">  ← becomes <main> (Option A)
// import convention: '@/' for lib/components, './' for co-located route files (mirror ./error.tsx)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VerificationBoundary.tsx (unstable_catchError, digest-only amber fallback) + regression test</name>
  <files>src/app/strategy/[id]/VerificationBoundary.tsx, src/app/strategy/[id]/VerificationBoundary.test.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/catchError.md — full API (signature, ErrorInfo, unstable_retry vs reset, 'use client' requirement, notFound pass-through)
    - src/app/strategy/[id]/error.tsx — the digest-only / no-message-leak contract to mirror (T-52-15)
    - src/components/strategy-v2/PartialDataBanner.tsx — the honest role="status" degraded-panel shape
    - src/app/error.test.tsx — jsdom test harness pattern (`/** @vitest-environment jsdom */`, render + act)
    - DESIGN.md — semantic color rules: amber = transient/recoverable; red forbidden for recoverable states
  </read_first>
  <behavior>
    - Test 1 (degrade-not-throw): mounting VerificationBoundary wrapping a child that throws renders the fallback copy — the render does NOT throw. This test FAILS without the boundary (a bare throwing child crashes the render), which is the regression proof.
    - Test 2 (sibling isolation): a sibling element rendered NEXT TO the boundary (outside it) is still in the DOM after the child throws — the throw did not blank the subtree.
    - Test 3 (no-leak): the thrown Error's message string (use a distinctive sentinel like "SENTINEL_SECRET_SERVER_DETAIL") is NOT present anywhere in the rendered DOM; the static fallback copy IS.
    - Test 4 (digest + retry affordance): when the error carries a digest, the digest string renders; the "Try again" control invokes unstable_retry (assert via the boundary's retry behavior or a spy where mountable in jsdom).
  </behavior>
  <action>
    Create `src/app/strategy/[id]/VerificationBoundary.tsx` as a 'use client'
    module. Import unstable_catchError and type ErrorInfo from 'next/error'.
    Define a fallback function taking (props, { error, unstable_retry }) whose
    props allow optional `heading`/`body` copy overrides (mirroring
    PartialDataBanner's heading/body prop shape) with defaults "Verification
    temporarily unavailable" / "The rest of this factsheet is unaffected." —
    honest, static, no invented data, no fabricated metric. Render a
    role="status" aria-live="polite" panel using the amber transient tokens
    (border-warning-border, bg-warning-bg, text-warning — DESIGN.md: amber =
    transient/recoverable, red forbidden). Log the error client-side via
    console.error (tag "[verification-boundary]") inside useEffect, render
    error.digest ONLY when present (mirror error.tsx lines 42-46), and render a
    "Try again" affordance calling unstable_retry() — NOT reset(), which cannot
    recover a Server Component throw. NEVER render error.message (T-52-15 —
    the installed doc's {error.message} sample is explicitly overridden by the
    repo contract). Export default unstable_catchError(fallback).

    Create `src/app/strategy/[id]/VerificationBoundary.test.tsx` with the
    jsdom header + @testing-library/react harness copied from
    src/app/error.test.tsx, implementing the four behaviors above. Prove
    regression-first: run Test 1 with the boundary import swapped for a
    pass-through fragment (temporarily, in-session) and observe RED, then
    restore and observe GREEN; record the pair in the SUMMARY.

    W2 jsdom-mount contingency (record honestly, do not stall): no
    `unstable_catchError` boundary is unit-tested anywhere in this repo, so it
    is unknown whether the Next client boundary needs router/runtime context to
    MOUNT under vitest/jsdom. If Tests 1/2/4 (which require actually mounting
    the live `unstable_catchError` wrapper and triggering its catch) genuinely
    cannot run in jsdom after a reasonable attempt (e.g. the boundary throws on
    mount for missing Next runtime context, not because of the code under test),
    then: (a) keep Test 3 (no-leak) as a PURE-RENDER test of the fallback
    FUNCTION directly — call the exported fallback with a synthetic
    `{ error: new Error("SENTINEL_SECRET_SERVER_DETAIL"), unstable_retry: vi.fn() }`
    and assert the sentinel is absent + the static copy present; this needs no
    live boundary mount and MUST pass; and (b) record in the SUMMARY that the
    full degrade-not-throw regression is carried by the e2e leg
    (`e2e/sfox-badge.spec.ts`, the plan already names the e2e as the real
    end-to-end regression) rather than a hard vitest stall. Never delete a test
    to make the gate pass; downgrade explicitly and on the record only.
  </action>
  <verify>
    <automated>npx vitest run --no-file-parallelism "src/app/strategy/[id]/VerificationBoundary.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run --no-file-parallelism "src/app/strategy/[id]/VerificationBoundary.test.tsx"` exits 0. (If Tests 1/2/4 cannot mount the live boundary in jsdom, the file still exits 0 with Test 3 as a pure-render test of the fallback function, AND the SUMMARY records the W2 downgrade + names the e2e leg as the degrade-not-throw regression — never a skipped/failing file left as the gate.)
    - The no-leak gate holds in code, not just tests: `grep -v "^\s*//" "src/app/strategy/[id]/VerificationBoundary.tsx" | grep -c "error\.message"` outputs 0 (comment-filtered so a warning comment cannot self-trip the gate).
    - Test 3 (no-leak, pure-render) always runs and passes regardless of the jsdom-mount outcome — the sentinel message is absent, the static copy present.
    - The module contains `'use client'`, `unstable_catchError`, `unstable_retry`, `role="status"`, and the amber token classes (grep each token name present).
    - `reset(` is not invoked as the recovery affordance (grep shows no onClick reset call).
    - SUMMARY records EITHER the RED-without-boundary → GREEN-with-boundary proof (live mount worked) OR the explicit W2 downgrade (live mount not possible in jsdom → Test 3 pure-render passes + e2e carries the full regression).
  </acceptance_criteria>
  <done>The boundary exists with the digest-only amber fallback; the co-located test pins no-leak (pure-render, always) plus degrade-not-throw / sibling-isolation / retry where jsdom permits (else the e2e carries the full regression, recorded), and the vitest file exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Page wiring — extract StrategyNoteSection into the boundary + Option A &lt;main&gt; landmark</name>
  <files>src/app/strategy/[id]/StrategyNoteSection.tsx, src/app/strategy/[id]/page.tsx</files>
  <read_first>
    - src/app/strategy/[id]/page.tsx — full file (the extraction source; note import conventions and the T31 insertion-order contract: note card sits BETWEEN the sparkline card and the CTA card)
    - src/app/strategy/[id]/page.test.tsx — T31 insertion-order test that must keep passing
    - src/components/strategy-v2/StrategyV2Shell.tsx:54 and src/app/strategy/[id]/v2/error.tsx:23 — the v2 route's OWN <main>; the reason Option B (a /strategy layout) is FORBIDDEN here
    - src/app/browse/layout.tsx:38 — the proven <main> analog (for class/structure reference only; we are NOT adding a layout)
    - .planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-01-SUMMARY.md — the pinned root cause (informs whether the section needs an additional at-source guard already landed in 126-01)
  </read_first>
  <action>
    (1) Create `src/app/strategy/[id]/StrategyNoteSection.tsx` — a server-side
    async RSC (NO 'use client') accepting `strategyId: string`. Move the entire
    fallible authed sub-tree out of the page body into it: createClient() →
    supabase.auth.getUser() → when a user exists, the user_notes
    select("content, updated_at") filtered by user_id / scope_kind "strategy" /
    scope_ref strategyId with maybeSingle(), then render StrategyNoteCard with
    strategyId, initialContent, initialLastSavedAt exactly as the page does
    today (lines 100-118 + 200-206); return null for unauthenticated visitors.
    Preserve the query EXACTLY — do not widen the select or filters (RLS
    per-user privacy, T-126-13). Rationale: the boundary can only catch throws
    that happen while rendering its CHILDREN — the fetch must move inside a
    wrapped child, or a throw still fires in the page body and hits error.tsx.

    (2) Edit `src/app/strategy/[id]/page.tsx`: remove the now-extracted
    createClient/getUser/user_notes block and the `{user && <StrategyNoteCard/>}`
    render; in the same slot (BETWEEN the sparkline card and the CTA card — the
    T31 insertion-order contract), render the note section wrapped in the
    boundary imported via relative path from "./VerificationBoundary"
    (co-located route file convention, mirroring "./error.tsx"), passing
    honest copy props if the defaults do not describe the wrapped region.
    Keep `if (!result) notFound()` OUTSIDE the boundary — genuinely-wrong stays
    fail-loud, and unstable_catchError passes notFound() through by design.
    Keep the badge, metric grid, and Sparkline OUTSIDE the boundary so they
    always render.

    (3) Option A `<main>`: change the outer wrapper at page.tsx line 121 from a
    div with className "min-h-screen bg-page" to a `<main>` with the SAME
    className (and fix the closing tag). Do NOT create
    `src/app/strategy/layout.tsx` — Option B is FORBIDDEN in this codebase:
    a `/strategy` route layout would also wrap `/strategy/[id]/v2`, whose
    StrategyV2Shell (:54) and v2/error.tsx (:23) already render their own
    `<main>`, producing two `<main>` landmarks on v2 and breaking axe
    `landmark-one-main` there (this corrects 126-PATTERNS.md's "they do not
    collide" claim — route layouts nest). Exactly one `<main>` per document,
    added exactly once.
  </action>
  <verify>
    <automated>npx vitest run --no-file-parallelism "src/app/strategy/[id]" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "<main" "src/app/strategy/[id]/page.tsx"` outputs 1, and `test ! -f src/app/strategy/layout.tsx` succeeds (Option A only — `<main>` added exactly once).
    - page.tsx imports the boundary relatively: `grep -c 'from "./VerificationBoundary"' "src/app/strategy/[id]/page.tsx"` outputs 1, and the StrategyNoteSection render sits inside the VerificationBoundary JSX element (read the diff to confirm nesting).
    - The user_notes query moved verbatim: `grep -c "user_notes" "src/app/strategy/[id]/page.tsx"` outputs 0 and `grep -c "user_notes" "src/app/strategy/[id]/StrategyNoteSection.tsx"` outputs 1, with the same three .eq filters + maybeSingle().
    - `notFound()` remains in page.tsx OUTSIDE the boundary (grep confirms it is still called directly after the getStrategyDetail null-check).
    - `npx vitest run --no-file-parallelism "src/app/strategy/[id]"` exits 0 (T31 insertion-order + existing page/loading/error tests green) and `npx tsc --noEmit` exits 0.
    - `npm run lint` passes on the touched files.
  </acceptance_criteria>
  <done>The fallible authed sub-tree renders inside the boundary as a wrapped async RSC child, the v1 page has exactly one main landmark via the page wrap, and all existing co-located tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server render → client HTML | server error details must never reach client markup via the fallback |
| viewer session → user_notes rows | per-user RLS privacy on the note fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-11 | Information Disclosure | VerificationBoundary fallback | mitigate | static copy + error.digest ONLY; never error.message (T-52-15); comment-filtered grep gate + no-leak unit test with a sentinel message |
| T-126-12 | Tampering/Repudiation | boundary scope | mitigate | boundary wraps ONLY the note/provenance sidecar; notFound() and the strategy fetch stay outside → genuinely-wrong still fails loud into error.tsx (Rule 6/12) |
| T-126-13 | Information Disclosure | StrategyNoteSection user_notes query | mitigate | query moved byte-equivalent (same select/filters/maybeSingle); RLS unchanged; acceptance grep pins the filters |
| T-126-14 | Denial of Service (a11y regression) | double `<main>` on v2 | mitigate | Option A page wrap only; `test ! -f src/app/strategy/layout.tsx` gate |
</threat_model>

<verification>
- `npx vitest run --no-file-parallelism "src/app/strategy/[id]"` fully green; `npx tsc --noEmit` clean.
- Structural gates: exactly one `<main>` in page.tsx; no strategy/layout.tsx; boundary wired between sparkline and CTA; no `error.message` in the fallback (comment-filtered grep).
</verification>

<success_criteria>
- FACTSHEET-01 degradation leg satisfied: a throwing sidecar degrades to the honest amber panel while badge/metrics/sparkline/disclaimer render; the regression test fails without the boundary.
- The verified `<main>` defect is fixed exactly once with no v2 collision.
</success_criteria>

<output>
Create `.planning/phases/126-factsheet-connected-key-api-verified-factsheet-render-blocki/126-02-SUMMARY.md` when done.
</output>
