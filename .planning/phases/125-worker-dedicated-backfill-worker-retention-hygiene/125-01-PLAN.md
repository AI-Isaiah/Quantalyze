---
phase: 125-worker-dedicated-backfill-worker-retention-hygiene
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/test_retention_orphaned_running.sql
  - supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql
autonomous: true
requirements: [WORKER-04]

must_haves:
  truths:
    - "A pg_cron job named retention_compute_jobs_orphaned_running exists whose body DELETEs ONLY status='running' compute_jobs rows with claimed_at older than 2 hours"
    - "The 2-hour window exceeds the 40-minute max watchdog threshold (process_key_long) so a legitimately in-flight prod job can NEVER be deleted"
    - "A SQL guard test asserts the cron is registered with the exact predicate AND behaviorally proves the DELETE spares fresh running rows and non-running rows"
    - "The guard test NOTICE-skips (never reds) when the migration is not yet applied, matching the test-DB-lag presence-gate house style"
  artifacts:
    - path: "supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql"
      provides: "Recurring orphaned-running purge cron (WORKER-04 root-cause fix for the fence-test CI flake)"
      contains: "retention_compute_jobs_orphaned_running"
    - path: "supabase/tests/test_retention_orphaned_running.sql"
      provides: "Guard: cron registered + predicate correct + DELETE behavior (deletes >2h running, spares fresh/other-status)"
      contains: "retention_compute_jobs_orphaned_running"
  key_links:
    - from: "supabase/tests/test_retention_orphaned_running.sql"
      to: "supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql"
      via: "cron.job jobname lookup"
      pattern: "retention_compute_jobs_orphaned_running"
    - from: "supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql"
      to: "compute_jobs"
      via: "pg_cron scheduled DELETE (retention_delete_guard trigger backstops)"
      pattern: "status = 'running'"
---

<objective>
Land the WORKER-04 root-cause fix as code: a recurring pg_cron purge of orphaned
`running` compute_jobs (window 2h — safely above the 40-min max watchdog
threshold), plus its RED-guarded SQL test. This kills the recurring `python`
fence-test CI flake at its source: the `derive-allocator-key-dailies` cron
re-pollutes the WORKERLESS test project daily with `running` rows that collide
with fence-test seeds via the claim RPC's partition-dedupe arms
(`x.status IN ('running','done_pending_children')` NOT-EXISTS).

Purpose: WORKER-04 — orphaned `running` rows purged on a retention schedule
(test nightly + prod safety sweep) with ONE migration safe on BOTH projects.
Output: 1 migration + 1 SQL guard test. TEST-project MCP apply happens in plan
125-03 (BLOCKING there, before the guard is required green).

DELETE, never reset-to-pending (a reset row is re-claimed to `running` by the
next CI run — the collision returns). Never unschedule the derive cron
(`test_derive_allocator_keys_fanout.sql` assertion 6 requires it registered).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-RESEARCH.md
@.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-PATTERNS.md

<interfaces>
<!-- Verified against the repo 2026-07-19. Executor uses these directly. -->

From analytics-service/main_worker.py (why 2h is safe):
- WATCHDOG_PER_KIND_OVERRIDES (line 206) — max per-kind stale threshold is
  process_key_long = 40 minutes. On prod the interactive worker's watchdog
  resets stalled running→pending at each threshold, so a running row older
  than 2h is definitively orphaned (worker down), never a live job.

From supabase/migrations/20260515210200_retention_crons_high_hardening.sql
(the house-style analog — the retention_compute_jobs_failed job in this file
is the direct sibling):
- Envelope: `BEGIN; SET lock_timeout = '5s'; … COMMIT;` (lines 94-95, 322)
- pg_cron fail-loud presence guard: RAISE EXCEPTION USING ERRCODE =
  'feature_not_supported' when pg_extension lacks pg_cron (lines 141-152)
- Idempotent unschedule-then-schedule: IF EXISTS (SELECT 1 FROM cron.job
  WHERE jobname = …) THEN PERFORM cron.unschedule(…); then PERFORM
  cron.schedule(name, schedule, $cron$ DELETE … $cron$) (lines 248-259)
- Self-verifying terminal DO block: SELECT command INTO v_command FROM
  cron.job WHERE jobname = …; RAISE EXCEPTION if NULL or if command NOT
  ILIKE the intended predicate (lines 267-320)
- retention_delete_guard trigger (mig 121) caps per-statement DELETE at
  100k rows and backstops every retention cron body — cite it in the header,
  do NOT rebuild or bypass it (lines 84-87, 104)

From supabase/tests/test_derive_allocator_keys_fanout.sql (presence-gate
idiom for test-DB lag, lines 51-57):
- IF NOT EXISTS (SELECT 1 FROM cron.job WHERE jobname = …) THEN
  RAISE NOTICE 'SKIP: … migration not yet applied here.'; RETURN; END IF;

From supabase/tests/test_retention_crons_safe.sql:
- Cron-body assertion idiom: SELECT command INTO v_command FROM cron.job
  WHERE jobname = …; assert v_command ILIKE expected predicate fragments;
  pg_cron NOTICE-skip guard (lines 102-117)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SQL guard test (Wave 0 — presence-gated, lands before the migration)</name>
  <files>supabase/tests/test_retention_orphaned_running.sql</files>
  <read_first>
    - supabase/tests/test_retention_crons_safe.sql (cron-body/guard assertion idiom)
    - supabase/tests/test_derive_allocator_keys_fanout.sql (presence-gate + seed-assert-teardown shape, lines 33-172)
    - supabase/migrations/20260719073701_claim_kind_filter.sql (partition-dedupe arms lines 156-179 — the flake mechanism the test comment must cite)
    - analytics-service/main_worker.py (WATCHDOG_PER_KIND_OVERRIDES, line 206 — window rationale)
  </read_first>
  <action>
    Create supabase/tests/test_retention_orphaned_running.sql as plain PL/pgSQL
    DO blocks under psql -v ON_ERROR_STOP=1 (NO pgTAP), wrapped in BEGIN; …
    ROLLBACK; with the usage-comment header naming the run command (fanout
    analog lines 33-37). Structure:

    (1) Presence gates FIRST: NOTICE-skip + RETURN when pg_extension lacks
    pg_cron; NOTICE-skip + RETURN when cron.job has no row with
    jobname = 'retention_compute_jobs_orphaned_running' (message: assertions
    enforce once the test DB catches up — RESEARCH Pitfall 6).

    (2) Registration + predicate assertion: SELECT command INTO v_command FROM
    cron.job WHERE jobname = 'retention_compute_jobs_orphaned_running';
    RAISE EXCEPTION if v_command NOT ILIKE '%status = ''running''%', if NOT
    ILIKE '%interval ''2 hours''%', if NOT ILIKE '%claimed_at%', or if NOT
    ILIKE '%public.compute_jobs%'. Also assert the schedule hour parses into
    the safe 1-22 band (mirror fanout assertion 6's split_part idiom).

    (3) Behavioral seed-then-assert (inside the rolled-back txn): seed three
    compute_jobs rows — (a) status='running' with claimed_at = now() -
    interval '3 hours' (MUST be deleted), (b) status='running' with
    claimed_at = now() (MUST survive), (c) status='pending' or 'done' with
    created_at = now() - interval '3 hours' (MUST survive). Execute the
    registered cron body inline via EXECUTE v_command (running the REAL
    stored command, not a re-typed copy — the oracle is the deployed
    predicate, not the test author's transcription). Assert row (a) is GONE
    and rows (b) and (c) remain. Use realistic seed values satisfying
    compute_jobs NOT NULL / CHECK constraints (copy the fanout analog's seed
    shape for user/strategy/api_key scaffolding).

    (4) Encode the WHY (Rule 9) in a header comment: the flake mechanism is
    partition-dedupe collision (claim RPC NOT-EXISTS arms on
    status IN ('running','done_pending_children')) — DELETE not reset,
    because a reset row is re-claimed to running by the next CI run.
  </action>
  <verify>
    <automated>bash -c "grep -c 'retention_compute_jobs_orphaned_running' supabase/tests/test_retention_orphaned_running.sql | awk '{exit !($1>=3)}' && grep -q \"interval ''2 hours''\" supabase/tests/test_retention_orphaned_running.sql && grep -q 'ROLLBACK;' supabase/tests/test_retention_orphaned_running.sql && grep -qi 'RAISE NOTICE' supabase/tests/test_retention_orphaned_running.sql"</automated>
  </verify>
  <acceptance_criteria>
    - File exists at supabase/tests/test_retention_orphaned_running.sql, opens with BEGIN; and ends with ROLLBACK; (whole test rolls back)
    - Contains BOTH presence gates (pg_cron extension absent → NOTICE-skip; cron.job row absent → NOTICE-skip) — grep finds two distinct "RAISE NOTICE" skip messages before any RAISE EXCEPTION assertion
    - Predicate assertions use ILIKE against cron.job.command for the literal fragments: status = ''running'' / interval ''2 hours'' / claimed_at / public.compute_jobs
    - Behavioral section seeds exactly the three rows described, executes the stored cron.job.command via EXECUTE (grep -q "EXECUTE v_command" or equivalent variable), and asserts counts: orphaned-running row deleted, fresh-running row present, non-running row present
    - `psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_retention_orphaned_running.sql` exits 0 TODAY (NOTICE-skip path — migration not applied yet); if TEST_SUPABASE_DB_URL is unset locally, this leg is deferred to plan 125-03 Task 1 which runs it post-apply
    - No pgTAP functions used (grep -c "pgtap\|plan(" on non-comment lines = 0: `grep -v '^--' supabase/tests/test_retention_orphaned_running.sql | grep -ci pgtap` returns 0)
  </acceptance_criteria>
  <done>Guard test file committed; NOTICE-skips cleanly pre-migration; asserts registration + predicate + DELETE behavior once the migration is applied.</done>
</task>

<task type="auto">
  <name>Task 2: Write the retention purge migration (house-style, self-verifying)</name>
  <files>supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql</files>
  <read_first>
    - supabase/migrations/20260515210200_retention_crons_high_hardening.sql (WHOLE-file house style: envelope, pg_cron guard, unschedule-then-schedule, $cron$ body, terminal self-verify DO block)
    - supabase/migrations/20260717233529_allocator_equity_derived_surface.sql (the 30 5 * * * derive cron this purge must precede)
    - analytics-service/main_worker.py (WATCHDOG_PER_KIND_OVERRIDES line 206 — cite process_key_long=40min in the window-rationale comment)
  </read_first>
  <action>
    Create supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql
    (timestamp MUST sort after 20260719073701) following the 20260515210200
    house style exactly:

    - Envelope: BEGIN; SET lock_timeout = '5s'; … COMMIT;
    - Header comment: purpose (WORKER-04 — kill the fence-test flake root:
      the derive-allocator-key-dailies cron re-pollutes the workerless test
      project with orphaned running rows that collide via the claim RPC
      partition-dedupe arms); window rationale (2 hours >> process_key_long
      40-min max watchdog threshold — never touches a live in-flight prod
      job); cite the retention_delete_guard trigger (mig 121) as the
      100k-row DELETE backstop this cron inherits — do NOT disable, bypass,
      or re-implement it (no session_replication_role, no trigger disable).
    - Fail-loud pg_cron guard DO block: RAISE EXCEPTION USING ERRCODE =
      'feature_not_supported' if pg_extension lacks pg_cron (analog lines
      141-152 — fail-loud is mandatory, never a silent skip in a MIGRATION).
    - Idempotent schedule: IF EXISTS in cron.job THEN PERFORM
      cron.unschedule('retention_compute_jobs_orphaned_running'); then
      PERFORM cron.schedule('retention_compute_jobs_orphaned_running',
      '15 4 * * *', $cron$ … $cron$). Schedule rationale comment: daily,
      hour 4 inside the safe 1-22 band, BEFORE the 05:30 derive cron so each
      day starts from a clean slate.
    - Cron body (fixed literal, no interpolation, schema-qualified to pin
      resolution independent of the cron session search_path):
      DELETE FROM public.compute_jobs WHERE status = 'running' AND
      claimed_at IS NOT NULL AND claimed_at < now() - interval '2 hours';
    - Terminal self-verifying DO block (analog lines 267-320): re-select
      cron.job.command for the jobname; RAISE EXCEPTION if the row is
      missing, or if command NOT ILIKE '%status = ''running''%', NOT ILIKE
      '%interval ''2 hours''%', or NOT ILIKE '%public.compute_jobs%'; RAISE
      NOTICE success summary; COMMIT.

    Scope discipline: this migration touches ONLY the new cron job. It MUST
    NOT contain any cron.schedule/cron.unschedule for
    'derive-allocator-key-dailies' (WORKER-03 is a founder LIVE op — a
    migration reschedule would re-wedge prod per the runbook Step 6
    rationale) and MUST NOT alter compute_jobs DDL, RLS, or the claim RPC.
  </action>
  <verify>
    <automated>bash -c "f=supabase/migrations/20260719120000_retention_orphaned_running_compute_jobs.sql; grep -q \"status = 'running'\" $f && grep -q \"interval '2 hours'\" $f && grep -q 'public.compute_jobs' $f && grep -q \"'15 4 \\* \\* \\*'\" $f && grep -q 'feature_not_supported' $f && [ \"$(grep -v '^[[:space:]]*--' $f | grep -c 'derive-allocator-key-dailies')\" -eq 0 ] && grep -q 'COMMIT;' $f"</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists with timestamp 20260719120000 (sorts after 20260719073701) and the BEGIN/lock_timeout/COMMIT envelope
    - Cron body is EXACTLY the fixed literal: DELETE FROM public.compute_jobs WHERE status = 'running' AND claimed_at IS NOT NULL AND claimed_at < now() - interval '2 hours';
    - Schedule is '15 4 * * *' and jobname is retention_compute_jobs_orphaned_running
    - pg_cron-absent path RAISEs EXCEPTION with ERRCODE 'feature_not_supported' (grep -q feature_not_supported)
    - Terminal DO block asserts registration AND all three ILIKE predicate fragments; a deliberately-broken body would fail the migration itself
    - Non-comment lines contain ZERO occurrences of 'derive-allocator-key-dailies' (verified: `grep -v '^[[:space:]]*--' <file> | grep -c 'derive-allocator-key-dailies'` = 0) and zero DDL on compute_jobs (no ALTER TABLE/CREATE TRIGGER on it)
    - Header cites retention_delete_guard (mig 121) and the 40-min process_key_long watchdog rationale for the 2h window
  </acceptance_criteria>
  <done>Migration committed; self-verifying; guard test from Task 1 will flip from NOTICE-skip to full assertion once plan 125-03 MCP-applies it to TEST.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron → compute_jobs | Cron body executes as the job owner (superuser context) on a live prod table |
| migration auto-apply → PROD | supabase/migrations/** auto-applies to PROD on merge to main |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-01 | Tampering/DoS | purge cron DELETE deletes live in-flight prod jobs | mitigate | Window `interval '2 hours'` > 40-min max watchdog threshold (process_key_long) with 3x margin; predicate pinned to `status = 'running' AND claimed_at IS NOT NULL` — can NEVER touch non-running or younger rows; terminal self-verify DO block fails the migration if the deployed body drifts |
| T-125-02 | Elevation | cron body runs in superuser context | mitigate | Fixed-literal body (no dynamic SQL, no interpolation); table schema-qualified `public.compute_jobs` so resolution is independent of the cron session search_path; no GRANT widening anywhere in the migration |
| T-125-03 | DoS | unbounded DELETE volume in one statement | mitigate | Existing `retention_delete_guard` trigger (mig 121, 100k-row cap) backstops the body — respected, never disabled or bypassed (no session_replication_role, no ALTER TABLE DISABLE TRIGGER) |
| T-125-04 | Tampering | a cron reschedule of derive-allocator-key-dailies sneaking into the migration re-wedges prod on merge | mitigate | Explicit prohibition in Task 2 + grep gate asserting zero non-comment occurrences of 'derive-allocator-key-dailies' |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | N/A — this phase installs NO packages (RESEARCH Package Legitimacy Audit: no install tasks exist) |
</threat_model>

<verification>
- Both files exist; all automated grep gates pass.
- `psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_retention_orphaned_running.sql` exits 0 (NOTICE-skip pre-apply; full assertions post-apply in 125-03).
- `ls supabase/migrations/ | sort | tail -3` shows the new migration sorting last.
- CI sql-tests job auto-discovers the new test via the supabase/tests/test_*.sql glob (no wiring needed).
</verification>

<success_criteria>
- WORKER-04 code artifacts landed: recurring purge cron migration + RED-guarded behavioral SQL test.
- The migration is merge-safe on BOTH projects (2h window, prod-safe) and self-verifying.
- The guard test proves the DEPLOYED cron body (EXECUTE of cron.job.command) deletes >2h running rows and spares fresh/other-status rows.
- Zero prod behavior change beyond the new cron; no WORKER-03 reschedule smuggled in.
</success_criteria>

<output>
Create `.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-01-SUMMARY.md` when done.
</output>
