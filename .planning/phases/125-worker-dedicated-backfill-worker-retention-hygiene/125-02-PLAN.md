---
phase: 125-worker-dedicated-backfill-worker-retention-hygiene
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_worker_isolation_e2e.py
autonomous: true
requirements: [WORKER-02]

must_haves:
  truths:
    - "A deliberately-hung crawl times out via the real asyncio.wait_for bound END-TO-END: the dispatch classifies to FAILED/error_kind='transient' and the worker loop keeps ticking — never a crash, never an unbounded wedge"
    - "During a long-but-alive dispatch, the REAL healthz TCP server (main_worker_healthz.start_healthz_server) answers 200 over an actual socket because the heartbeat advances LAST_TICK_AT"
    - "The backfill and interactive claim payloads are provably disjoint: role backfill sends p_kind_include=BACKFILL_KINDS, role interactive sends p_kind_exclude=BACKFILL_KINDS"
  artifacts:
    - path: "analytics-service/tests/test_worker_isolation_e2e.py"
      provides: "End-to-end WORKER-02 proof: real dispatch_tick + real heartbeat + real healthz TCP server + hung-crawl timeout"
      min_lines: 80
  key_links:
    - from: "analytics-service/tests/test_worker_isolation_e2e.py"
      to: "analytics-service/main_worker_healthz.py"
      via: "binds the real start_healthz_server on an ephemeral PORT and probes over TCP"
      pattern: "start_healthz_server"
    - from: "analytics-service/tests/test_worker_isolation_e2e.py"
      to: "analytics-service/main_worker.py"
      via: "runs the real dispatch_tick (heartbeat task included)"
      pattern: "dispatch_tick"
---

<objective>
Prove WORKER-02 END-TO-END: the v1.12-landed `asyncio.wait_for` crawl bounds +
heartbeat/`LAST_TICK_AT` refresh actually keep the worker live and healthz
honest when composed together — not just unit-by-unit. The existing
`test_main_worker.py` heartbeat tests mock the dispatch and never bind the real
healthz TCP server; this plan closes exactly that gap (RESEARCH Pattern 2).

Purpose: WORKER-02 — a deliberately-hung crawl times out end-to-end, the worker
stays live, and healthz stays honest through a long backfill. This is the
regression proof the v1.11 FLIP rollback demanded (a hanging crawl froze
healthz for 12 min and the auto-restart never fired).
Output: one new test file. NO production code changes — main_worker.py,
main_worker_healthz.py, and job_worker.py are read-only in this plan
(re-implementing the landed groundwork is the phase's named failure mode).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-RESEARCH.md
@.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-PATTERNS.md

<interfaces>
<!-- Verified against the repo 2026-07-19. Executor uses these directly. -->

From analytics-service/main_worker.py:
- `async def dispatch_tick(worker_id: str) -> None` (line 437) — the real loop
  body; spawns the heartbeat task internally
- `_HEARTBEAT_INTERVAL_S` module constant (line 136, env-derived, validated
  in (0, STALE_THRESHOLD)) — monkeypatch the module attribute to shrink it
- `WORKER_CLAIM_ROLE` (line 160), `_claim_kind_args(role)` (line 163):
  role "interactive" → {"p_kind_exclude": list(BACKFILL_KINDS)};
  role "backfill" → {"p_kind_include": list(BACKFILL_KINDS)} (lines 170-172)
- `BACKFILL_KINDS` tuple (line 121): derive_broker_dailies, derive_allocator_equity

From analytics-service/main_worker_healthz.py:
- `LAST_TICK_AT: float = 0.0` module global (line 26) — save/restore in finally
- `STALE_THRESHOLD = 90.0` (line 29); healthy = age <= STALE_THRESHOLD (line 54)
- `async def start_healthz_server() -> None` (line 84): reads
  int(os.getenv("PORT", "8080")), asyncio.start_server(_handle_healthz,
  "0.0.0.0", port), serve_forever. Fresh body: "200 OK" with JSON
  {"status":"ok","last_tick_at":...}; stale: "503 Service Unavailable"

From analytics-service/services/job_worker.py:
- `class DispatchOutcome(str, Enum)`: DONE / FAILED / DEFERRED (line 291)
- `DispatchResult(outcome, error_message, error_kind, ...)` frozen dataclass
  (line 309); ErrorKind = Literal["transient","permanent","unknown"] (line 131)
- Real hung-crawl classification (lines 2319-2334): asyncio.wait_for(...,
  timeout=_BROKER_CRAWL_TIMEOUT_S) → except asyncio.TimeoutError → raise
  DeribitTransientReadError (retryable path) → dispatch classifies FAILED +
  error_kind="transient"
- `_BROKER_CRAWL_TIMEOUT_S` (line 239, env-derived module constant)

From analytics-service/tests/test_main_worker.py (analog idioms):
- TestDispatchTick.test_heartbeat_refreshes_last_tick_during_long_dispatch
  (lines 247-289): monkeypatch _HEARTBEAT_INTERVAL_S to 0.02; slow yielding
  dispatch; assert LAST_TICK_AT advanced past the captured start stamp
- Module-global save/restore idiom: _saved_tick =
  main_worker_healthz.LAST_TICK_AT; try/finally restore (every test)
- TestWorkerClaimRole (lines 1738-1811): patch("main_worker.WORKER_CLAIM_ROLE",
  ...) and inspect the claim RPC params
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Case B — long-but-alive backfill keeps the REAL healthz TCP server answering 200</name>
  <files>analytics-service/tests/test_worker_isolation_e2e.py</files>
  <read_first>
    - analytics-service/tests/test_main_worker.py (TestDispatchTick lines 247-289 heartbeat idiom; lines 16-30 imports; save/restore idiom)
    - analytics-service/main_worker_healthz.py (whole file — _handle_healthz response contract, start_healthz_server, STALE_THRESHOLD)
    - analytics-service/main_worker.py (dispatch_tick body around line 437-650 — how the heartbeat task is spawned and what dispatch seam the existing tests patch)
  </read_first>
  <behavior>
    - Test 1 (healthz_stays_200_through_long_backfill): with _HEARTBEAT_INTERVAL_S monkeypatched to 0.02 and the real start_healthz_server bound on an ephemeral port, a yielding dispatch that sleeps ~0.3s (≈15 heartbeat intervals) completes while an HTTP GET issued MID-dispatch over a real TCP socket returns status line "200 OK" with a JSON body whose last_tick_at is non-null
    - Test 2 (healthz_503_when_tick_stale): with LAST_TICK_AT forced to time.time() - (STALE_THRESHOLD + 10) and NO dispatch running, the same TCP probe returns "503 Service Unavailable" — proves the probe actually exercises the staleness contract, not a stub
  </behavior>
  <action>
    Create analytics-service/tests/test_worker_isolation_e2e.py with a
    module docstring naming WORKER-02 and the v1.11 root cause (hung crawl →
    healthz stale 12 min → restart never fired). Mirror test_main_worker.py's
    import set and asyncio_mode=auto style (pytest.ini already sets it).

    Bind the REAL server: pick a free ephemeral port (bind a temporary
    socket to port 0, read the assigned port, close it), monkeypatch env
    PORT to that value (monkeypatch.setenv), start
    main_worker_healthz.start_healthz_server() as an asyncio task, and probe
    with a raw asyncio.open_connection GET (send b"GET /healthz HTTP/1.1...",
    read the response bytes) — no HTTP client dependency. Cancel the server
    task in finally.

    Drive the REAL main_worker.dispatch_tick with the same claim/dispatch
    patch seams the existing TestDispatchTick tests use (patch the claim RPC
    to return one job, patch the dispatch callable with a slow YIELDING
    coroutine returning DispatchResult(outcome=DispatchOutcome.DONE)). While
    the dispatch sleeps, issue the TCP probe and capture the response;
    after dispatch_tick returns, assert the mid-dispatch response was 200
    and main_worker_healthz.LAST_TICK_AT advanced past the stamp captured at
    dispatch start (the analog's oracle).

    Save/restore main_worker_healthz.LAST_TICK_AT in try/finally in EVERY
    test (module-global pollution breaks order-independence — the analog's
    _saved_tick idiom). Do NOT modify any production file.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_worker_isolation_e2e.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analytics-service && python -m pytest tests/test_worker_isolation_e2e.py -x -q` exits 0
    - The test file imports and calls main_worker_healthz.start_healthz_server (grep -q "start_healthz_server" tests/test_worker_isolation_e2e.py) and probes via asyncio.open_connection — NOT by calling _handle_healthz directly and NOT via a mocked server
    - The 200-assertion happens on a response captured WHILE the slow dispatch is in flight (mid-dispatch), proven by capturing the response inside the dispatch window
    - The 503 test proves staleness honesty: forced-stale LAST_TICK_AT → b"503" in the response bytes
    - Every test restores main_worker_healthz.LAST_TICK_AT in a finally block (grep -c "finally" >= 2 in the file at this point)
    - `git diff --name-only` shows ONLY analytics-service/tests/test_worker_isolation_e2e.py (zero production-file edits)
  </acceptance_criteria>
  <done>Real-server healthz proof green: 200 mid-backfill (heartbeat honest), 503 when stale (contract real). No production code touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Case A — hung crawl times out to transient end-to-end + role-disjointness proof</name>
  <files>analytics-service/tests/test_worker_isolation_e2e.py</files>
  <read_first>
    - analytics-service/services/job_worker.py (lines 2310-2340 — the real wait_for → TimeoutError → DeribitTransientReadError classify-to-transient shape; the crawl awaitable to patch is fetch_deribit_native_account_state; the real dispatch entry points are dispatch() line 6785 → run_derive_broker_dailies_job() line 2028; DispatchOutcome/DispatchResult lines 285-315)
    - analytics-service/tests/test_main_worker.py (TestWorkerClaimRole lines 1738-1811 — claim payload inspection idiom; fallback-latch reset idiom lines 1836-1845)
    - analytics-service/main_worker.py (_claim_kind_args lines 163-172; BACKFILL_KINDS line 121; dispatch_tick loop-continuation behavior after a FAILED dispatch)
  </read_first>
  <behavior>
    - Test 3 (hung_crawl_times_out_worker_stays_live): a derive_broker_dailies job is dispatched through the REAL production dispatch path (services.job_worker.dispatch → run_derive_broker_dailies_job) with the exchange crawl patched to a NEVER-completing awaitable (an unset asyncio.Event().wait()) and services.job_worker._BROKER_CRAWL_TIMEOUT_S monkeypatched to ~0.05. PRODUCTION CODE — not the test — runs the wait_for at job_worker.py line ~2319, raises DeribitTransientReadError on asyncio.TimeoutError, and classifies the DispatchResult. The test ONLY asserts the RETURNED DispatchResult has outcome == FAILED and error_kind == "transient" (never an unhandled exception escapes); a SECOND dispatch_tick call immediately after still runs and bumps LAST_TICK_AT (the loop survived). NO wait_for is written in the test body — the oracle is the real seam, never a re-typed copy of it (P115 self-referential-oracle anti-pattern)
    - Test 4 (roles_never_contend): with patch("main_worker.WORKER_CLAIM_ROLE","backfill") the claim RPC params carry p_kind_include == list(BACKFILL_KINDS) and NO p_kind_exclude; with "interactive" they carry p_kind_exclude == list(BACKFILL_KINDS) and NO p_kind_include — the two workers structurally cannot claim the same kinds
  </behavior>
  <action>
    Extend tests/test_worker_isolation_e2e.py.

    Test 3: drive the hung crawl through the REAL production dispatch seam so
    the transient classification is produced BY job_worker, not by the test.
    The test must NOT contain its own asyncio.wait_for / except TimeoutError →
    transient logic — re-typing the classification in the test body pins a
    copy of the impl instead of the impl (the P115 self-referential-oracle
    anti-pattern that let money bugs survive review). Concretely:
      1. Patch the exchange crawl call the real handler awaits — patch
         services.job_worker.fetch_deribit_native_account_state (the awaitable
         wrapped by the wait_for at job_worker.py line ~2319) to return a
         genuinely-unbounded awaitable (await an asyncio.Event that is never
         set), so ONLY the production wait_for bound can end it.
      2. monkeypatch services.job_worker._BROKER_CRAWL_TIMEOUT_S to ~0.05 so
         the real bound fires quickly (shrink the real constant; do NOT
         introduce a new bound).
      3. Invoke the REAL path: build a minimal derive_broker_dailies job dict
         and call services.job_worker.dispatch(job) (which routes to
         run_derive_broker_dailies_job) — or call run_derive_broker_dailies_job
         directly if a lighter fixture context is needed. Either way the
         wait_for, the DeribitTransientReadError raise, and the DispatchResult
         classification all execute inside PRODUCTION code.
      4. ASSERT ONLY the returned DispatchResult: outcome == DispatchOutcome.FAILED
         and error_kind == "transient", and that no exception propagated out of
         the call. The test observes the result; it never computes it.
      5. Worker-stays-live oracle (genuinely e2e): after the timeout, call the
         real main_worker.dispatch_tick again (with a mocked claim returning
         one trivial DONE job) and assert main_worker_healthz.LAST_TICK_AT
         advanced — the loop survived the hang.
    Regression-first: the crawl awaitable is an Event that is never set, so if
    the production wait_for bound were removed the call would hang; guard the
    test body with an outer asyncio.wait_for(..., 5) so a genuine regression
    fails fast rather than hanging CI. Save/restore main_worker_healthz.LAST_TICK_AT
    in finally.

    Test 4: reuse the TestWorkerClaimRole payload-inspection idiom — patch
    main_worker.WORKER_CLAIM_ROLE per case, run the claim through
    dispatch_tick's claim seam (or _claim_kind_args directly PLUS one
    dispatch_tick-level assertion so the wiring is covered, not just the
    helper — test the wiring, not just the helper), assert the include/
    exclude payloads above and assert
    set(include_payload).isdisjoint(...) semantics: a kind in
    p_kind_include for backfill appears in p_kind_exclude for interactive.
    Reset any fallback-latch module globals the analog resets (lines
    1836-1845 idiom) for order-independence.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_worker_isolation_e2e.py -x -q && python -m pytest tests/test_main_worker.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analytics-service && python -m pytest tests/test_worker_isolation_e2e.py -x -q` exits 0 with >= 4 tests collected
    - Test 3's error_kind == "transient" / outcome == FAILED assertion is produced by PRODUCTION CODE: the test calls services.job_worker.dispatch (or run_derive_broker_dailies_job) and asserts the RETURNED DispatchResult — it does NOT contain its own asyncio.wait_for or its own except TimeoutError classification. Verify: `grep -c "wait_for" tests/test_worker_isolation_e2e.py` counts ONLY the outer 5s test-body guard(s), and `grep -c "TransientReadError\|error_kind *=" tests/test_worker_isolation_e2e.py` shows no test-authored construction of a transient DispatchResult (assertions compare against the returned value, never build it)
    - The hung crawl is created by patching the real awaitable the handler wraps (services.job_worker.fetch_deribit_native_account_state → an unset asyncio.Event().wait()), NOT by a test-side wait_for — grep confirms fetch_deribit_native_account_state is patched and no sleep-based fake hang that would end on its own
    - The bound that fires is job_worker's real module constant, shrunk via monkeypatch, not a literal re-implementation: grep -q "_BROKER_CRAWL_TIMEOUT_S" tests/test_worker_isolation_e2e.py and the test never passes its own timeout= to a wait_for wrapping the crawl
    - Worker-stays-live oracle (e2e): after the timeout, a real main_worker.dispatch_tick call advances main_worker_healthz.LAST_TICK_AT
    - Test 4 asserts p_kind_include == list(BACKFILL_KINDS) for backfill AND p_kind_exclude == list(BACKFILL_KINDS) for interactive, with the opposite key ABSENT in each payload
    - `cd analytics-service && python -m pytest tests/test_main_worker.py -x -q` still exits 0 (no cross-test pollution of module globals)
    - `git diff --name-only` still shows ONLY the new test file
  </acceptance_criteria>
  <done>WORKER-02 proven end-to-end: hung crawl → bounded transient, worker live, healthz honest; role payloads provably disjoint. Existing worker suite still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → live exchange APIs | Untrusted/slow external I/O is what the wait_for bound defends against |
| Railway healthcheck → healthz TCP server | Restart decisions ride the 200/503 contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-05 | DoS | hung exchange crawl wedges the worker event loop (the v1.11 wedge) | mitigate | This plan IS the mitigation proof: end-to-end regression test where an unbounded hang only ends via the real wait_for bound; fails (hangs → test-level timeout) if the bound is removed |
| T-125-06 | Repudiation | healthz lies (200 while wedged, or stale while healthy) → restart policy acts on false signal | mitigate | Real-TCP 200-mid-dispatch + forced-stale-503 tests pin the staleness contract against the deployed server code, not a mock |
| T-125-07 | Elevation | a worker claims out-of-role kinds (backfill jobs on the interactive loop) | mitigate | Test 4 pins the include/exclude payload disjointness at the dispatch wiring level |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed (test-only plan, stdlib + existing pytest) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_worker_isolation_e2e.py tests/test_main_worker.py -x -q` exits 0 (run SERIALLY — never -n auto for these).
- `git diff --name-only` contains exactly one file: analytics-service/tests/test_worker_isolation_e2e.py.
- Full-suite check at wave close: `cd analytics-service && python -m pytest -n auto` (fence tests excluded from parallel flake concerns until 125-03's cleanup lands — a fence flake here is the KNOWN WORKER-04 issue, not a regression from this plan).
</verification>

<success_criteria>
- WORKER-02 has an end-to-end automated proof: real dispatch_tick + real heartbeat + real healthz TCP server + genuinely-hung crawl, all composed.
- The proof is regression-first: it hangs/fails without the v1.12 wait_for + heartbeat groundwork.
- Zero production code changes.
</success_criteria>

<output>
Create `.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-02-SUMMARY.md` when done.
</output>
