---
phase: 125-worker-dedicated-backfill-worker-retention-hygiene
plan: 04
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - docs/runbooks/flipretry-derived-equity-go-live.md
autonomous: false
requirements: [WORKER-01, WORKER-03]

must_haves:
  truths:
    - "The runbook carries a complete, founder-executable env contract for the two-service split: existing worker → WORKER_CLAIM_ROLE=interactive, NEW second Railway service (same image, CMD python -m main_worker) → WORKER_CLAIM_ROLE=backfill"
    - "The runbook records that Railway REPLICAS cannot carry distinct roles — a distinct SERVICE is required (Pitfall 5) — and that RESEARCH Open Q1 (current API/worker service topology) must be founder-confirmed as cutover Step 0"
    - "The WORKER-03 cron reschedule remains a founder LIVE SQL op gated on runbook Step 6 preconditions — NEVER a migration; the runbook's Step 6 rationale is intact"
    - "The Phase-125 additions (purge migration, one-time TEST cleanup, op ordering) are documented so the founder sequence is unambiguous: purge migration merges → cutover Steps 1-2 → pilot/E2/backfill → Step 6 reschedule LAST"
    - "WORKER-01 activation (the live cutover) and WORKER-03 execution are verified by a human or remain explicitly OPEN — never silently claimed done"
  artifacts:
    - path: "docs/runbooks/flipretry-derived-equity-go-live.md"
      provides: "Extended founder runbook: dashboard env contract, topology-confirm Step 0 note, Phase-125 retention/ordering additions"
      contains: "WORKER_CLAIM_ROLE=backfill"
  key_links:
    - from: "docs/runbooks/flipretry-derived-equity-go-live.md"
      to: "analytics-service/main_worker.py"
      via: "documented env contract consumed by _validate_claim_role at worker startup"
      pattern: "WORKER_CLAIM_ROLE"
---

<objective>
Deliver WORKER-01 and WORKER-03 the only way runtime-state requirements can be
delivered: a precise, founder-executable runbook extension plus a blocking
human checkpoint for the LIVE ops. The claim-role code, the kind-filtered RPC
(prod-applied), and the runbook's cutover skeleton (Steps 1-2) and Step 6
reschedule already exist — this plan EXTENDS the runbook with the Phase-125
specifics and models the ops as human legs. It re-implements NOTHING.

Purpose: WORKER-01 — backfill jobs claimed ONLY by a dedicated worker, the
sequential prod worker structurally cannot claim them. WORKER-03 — the cron
reschedule is a founder LIVE SQL op with a written runbook step, never a
migration.
Output: runbook edits + a founder checkpoint (the cutover leg; the reschedule
leg stays gated on Step 6 preconditions and may remain human_needed-OPEN,
matching the 123-03 precedent).

Discretion decision (RESEARCH Open Q1, surfaced for founder override at the
checkpoint): document a DASHBOARD ENV CONTRACT in the runbook rather than
committing an `analytics-service/railway.worker.toml`. Rationale: the
interactive/backfill split is Railway service+env state that lives in the
dashboard, not git; a second toml cannot bind itself to a service and would
imply git controls topology it does not. If the founder prefers a committed
config file, that is a one-file follow-up — say so at the checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-RESEARCH.md
@.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-PATTERNS.md

<interfaces>
<!-- Verified against the repo 2026-07-19 -->

From docs/runbooks/flipretry-derived-equity-go-live.md (extend IN PLACE,
match the existing step format: numbered step, Env:/Verify:/Abort path:
sub-bullets, fenced railway/sql blocks):
- Step 1 (line 41): "Deploy the dedicated backfill worker" — second service,
  WORKER_CLAIM_ROLE=backfill, service-role env var is SUPABASE_SERVICE_KEY
  (NOT _ROLE_KEY)
- Step 2 (line 49): cut prod worker to interactive; documents the
  PROD-CRITICAL consequence (onboarding derive_broker_dailies rides the
  backfill worker from that moment) and the abort path (role back to all)
- Step 6 (line 105): cron reschedule LAST, LIVE SQL op, with the full
  not-a-migration rationale (lines 117) — KEEP VERBATIM
- Step 8: rollback (verbatim v1.11 recovery)

From analytics-service/main_worker.py:
- WORKER_CLAIM_ROLE validated LOUD at import: any value outside
  {all, interactive, backfill} raises ValueError at startup (lines 145-160)
- Default "all" = byte-identical to today's prod until BOTH roles are set
- Backfill worker on a legacy 2-arg claim REFUSES to claim
  (claim_rpc_fallback_backfill_refused, lines 488-505) — safety net

From analytics-service/railway.toml + Dockerfile:
- builder DOCKERFILE; deploy healthcheckPath /health, restartPolicyType
  ON_FAILURE, maxRetries 3; Dockerfile documents CMD override
  `python -m main_worker` for a worker service (default: uvicorn main:app)

Live Railway facts (RESEARCH): project quantalyze-analytics, env production,
ONE linked service visible via CLI — whether API and worker are one combined
service or two is dashboard state the founder must confirm (Open Q1 / A2).
Railway silently SKIPS deploys on red main CI — verify commitHash + /health.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the go-live runbook with the Phase-125 env contract, topology gate, and retention ordering</name>
  <files>docs/runbooks/flipretry-derived-equity-go-live.md</files>
  <read_first>
    - docs/runbooks/flipretry-derived-equity-go-live.md (WHOLE file — extend in place, match step format; Steps 1, 2, 6, 8 are load-bearing)
    - analytics-service/railway.toml + analytics-service/Dockerfile (CMD-override + healthcheck facts for the env contract)
    - analytics-service/main_worker.py (lines 145-172 — role validation + claim payload facts the contract must state)
  </read_first>
  <action>
    Edit docs/runbooks/flipretry-derived-equity-go-live.md IN PLACE (surgical
    additions — do not restructure or renumber existing steps; Step 6's
    not-a-migration rationale stays verbatim):

    (1) In "Preconditions (Step 0)": add a topology-confirmation item —
    founder confirms on the Railway dashboard whether the API and the
    durable worker currently run as ONE combined service or TWO, and names
    the exact service that will receive WORKER_CLAIM_ROLE=interactive
    (RESEARCH Open Q1/A2: the CLI shows one linked service
    quantalyze-analytics; the split may be dashboard-only). State the
    decision + alternative: the split is a documented DASHBOARD ENV
    CONTRACT (this runbook), not a committed railway.worker.toml — a second
    toml cannot bind itself to a Railway service; if the founder prefers a
    committed config file, record it as a follow-up.

    (2) In Step 1: tighten into an explicit env contract block for the NEW
    backfill service — same repo/image, CMD override `python -m main_worker`,
    WORKER_CLAIM_ROLE=backfill, SUPABASE_SERVICE_KEY (NOT _ROLE_KEY) + the
    same DB/exchange env the prod worker carries, optional
    WORKER_HEARTBEAT_INTERVAL_S validated in (0, 90), healthz on PORT
    (default 8080). Add: Railway REPLICAS cannot carry distinct roles
    (replicas share env + CMD) — a distinct SERVICE is required; and the
    fail-loud guarantee: a typo'd role fails startup with a LOUD ValueError,
    and a backfill worker on a legacy 2-arg claim REFUSES rather than
    claiming out-of-role (claim_rpc_fallback_backfill_refused).

    (3) Add a short "Phase 125 retention hygiene" subsection (before or
    after Step 6, clearly cross-referenced): the
    retention_compute_jobs_orphaned_running purge cron lands as a MIGRATION
    (safe both projects: 2h window > 40-min max watchdog; MCP-applied to
    TEST first per plan 125-03; auto-applies to prod at merge — founder
    watches); the one-time TEST cleanup (DELETE running > 1h on
    qmnijlgmdhviwzwfyzlc) ran alongside it; and the explicit ordering:
    purge migration merges → cutover Steps 1-2 → pilot/E2/full backfill
    (Steps 3-5) → Step 6 cron reschedule LAST. Contrast in one line WHY the
    purge is a migration but the reschedule is not (purge has no
    worker-readiness dependency; the reschedule re-wedges prod if the
    worker deploy is skipped).
  </action>
  <verify>
    <automated>bash -c "f=docs/runbooks/flipretry-derived-equity-go-live.md; grep -q 'WORKER_CLAIM_ROLE=backfill' $f && grep -q 'WORKER_CLAIM_ROLE=interactive' $f && grep -qi 'replicas' $f && grep -q 'retention_compute_jobs_orphaned_running' $f && grep -q 'SUPABASE_SERVICE_KEY' $f && grep -qi 'NOT a migration' $f && grep -q 'python -m main_worker' $f"</automated>
  </verify>
  <acceptance_criteria>
    - Runbook still contains Steps 1-8 in original order (grep '^## Step' count and order unchanged apart from the new subsection heading)
    - New content includes: topology-confirm precondition naming Open Q1; the replicas-cannot-split-roles fact; the full backfill-service env contract (CMD override, WORKER_CLAIM_ROLE=backfill, SUPABASE_SERVICE_KEY, PORT/healthz); the fail-loud role validation + legacy-claim refusal facts
    - The Phase-125 subsection names retention_compute_jobs_orphaned_running, the 2h window rationale, the TEST-first MCP apply, the one-time cleanup, and the explicit op ordering ending with Step 6 LAST
    - Step 6's existing not-a-migration rationale paragraph is byte-unchanged (git diff shows no modification to lines 105-121 content other than surrounding additions)
    - `git diff --name-only` shows ONLY docs/runbooks/flipretry-derived-equity-go-live.md
  </acceptance_criteria>
  <done>Runbook is founder-executable end-to-end for the Phase-125 ops with no ambiguity about topology, env, or ordering; WORKER-03's live-op-not-migration invariant preserved verbatim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Founder LIVE ops — two-worker cutover (WORKER-01) + reschedule gating (WORKER-03)</name>
  <files>(none — founder LIVE Railway/SQL ops, runtime state only)</files>
  <read_first>
    - docs/runbooks/flipretry-derived-equity-go-live.md (as extended by Task 1 — Steps 0-2 and Step 6)
  </read_first>
  <action>
    Pause for the founder LIVE ops. Present the runbook Steps 0-2 cutover
    procedure and the Step 6 gating note verbatim. Record the outcome
    per-leg: WORKER-01 cutover verified live, or OPEN; WORKER-03 reschedule
    executed with cron.job evidence, or OPEN (expected). Never mark either
    leg done without the founder's explicit confirmation and evidence.
  </action>
  <what-built>
    All Phase-125 code, tests, migration, and the extended runbook are landed.
    The claim-role split is merge-safe and DORMANT (WORKER_CLAIM_ROLE unset =
    "all" = byte-identical prod behavior). What remains is runtime state only:
    the Railway two-service cutover, and — gated later on runbook Step 6
    preconditions — the prod cron reschedule.
  </what-built>
  <how-to-verify>
    Cutover leg (WORKER-01 — executable now, per runbook Steps 0-2):
    1. Step 0 topology confirm: on the Railway dashboard, confirm whether API
       + worker are one service or two, and name the service that takes
       `interactive`. Also confirm (or override) the dashboard-env-contract
       decision vs a committed railway.worker.toml.
    2. Step 1: create the SECOND service (same repo/image, CMD
       `python -m main_worker`, WORKER_CLAIM_ROLE=backfill, standard worker
       env incl. SUPABASE_SERVICE_KEY). Verify its healthz is 200 and logs
       show role backfill (p_kind_include=BACKFILL_KINDS) — it claims nothing
       while no backfill jobs exist.
    3. Step 2: set WORKER_CLAIM_ROLE=interactive on the existing worker
       service and redeploy (Railway skips deploys on red main CI — verify
       commitHash + /health). Verify prod healthz stays fresh and a new-key
       connect still processes. From this moment the backfill worker is
       PROD-CRITICAL for onboarding — keep it healthy.
    4. Abort path: WORKER_CLAIM_ROLE=all on the existing worker restores
       single-worker behavior.

    Reschedule leg (WORKER-03 — do NOT run now):
    5. `cron.schedule('derive-allocator-key-dailies','30 5 * * *')` stays a
       founder LIVE SQL op gated on runbook Step 6 preconditions (Steps 1-5
       green — pilot/E2/full backfill, i.e. Phase 127/129 gates). It is
       EXPECTED to remain OPEN at this checkpoint; approving here records
       the cutover as done and the reschedule as explicitly OPEN — a skipped
       op is never claimed done (123-03 precedent).
  </how-to-verify>
  <resume-signal>Reply with one of: "cutover done" (WORKER-01 verified live; WORKER-03 recorded OPEN pending Step 6), "cutover done + rescheduled" (both legs verified — include the cron.job SELECT output), or describe issues / the railway.worker.toml override.</resume-signal>
  <verify>
    <automated>MISSING — human gate: Railway dashboard topology + prod pg_cron state cannot be automated from CI; post-cutover spot-checks are listed in how-to-verify (healthz 200 x2, role logs, cron.job SELECT)</automated>
  </verify>
  <acceptance_criteria>
    - The founder's reply explicitly states the per-leg outcome (cutover done/OPEN; reschedule done-with-cron.job-evidence/OPEN)
    - The SUMMARY records both leg states verbatim — an OPEN leg is carried forward as human_needed, never claimed done (123-03 precedent)
  </acceptance_criteria>
  <done>Founder replied with an explicit per-leg outcome; WORKER-01/WORKER-03 states recorded honestly (done with evidence, or OPEN).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway dashboard → worker runtime | Env values (WORKER_CLAIM_ROLE) set by hand control claim scope |
| founder SQL session → prod pg_cron | The reschedule op writes prod scheduler state directly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-11 | Elevation/Tampering | mis-typed WORKER_CLAIM_ROLE silently mis-scopes claims | mitigate | Landed `_validate_claim_role` LOUD ValueError at startup (worker refuses to boot); runbook documents it — no silent default anywhere |
| T-125-12 | DoS | cron rescheduled before the backfill worker is live → jobs nothing claims → v1.11 re-wedge | mitigate | WORKER-03 stays a LIVE op gated on Step 6 preconditions with the rationale preserved verbatim; grep gate in 125-01 keeps it out of migrations; checkpoint records OPEN state explicitly |
| T-125-13 | DoS | interactive cutover with a dead backfill worker strands onboarding jobs | mitigate | Runbook Step 2 PROD-CRITICAL warning + abort path (role back to `all`) verified at the checkpoint |
| T-125-14 | Repudiation | cutover/reschedule claimed done without evidence | mitigate | Blocking human-verify checkpoint with explicit two-leg resume signals; reschedule leg defaults to OPEN |
| T-125-SC | Tampering | npm/pip/cargo installs | accept | N/A — no packages installed (doc + ops plan) |
</threat_model>

<verification>
- Runbook grep gates pass; Step 6 rationale unchanged; only the runbook in the diff.
- Checkpoint resolved with an explicit two-leg outcome (cutover done; reschedule done or recorded OPEN).
- Post-cutover (when run): both services' healthz 200; interactive logs show p_kind_exclude=BACKFILL_KINDS; backfill logs show p_kind_include=BACKFILL_KINDS.
</verification>

<success_criteria>
- WORKER-01: the dedicated-worker split is founder-executable from the runbook alone and verified live at the checkpoint (or its OPEN state is recorded honestly).
- WORKER-03: the reschedule has a written runbook step, remains a LIVE op (never a migration), and its execution state is explicit — done with evidence, or OPEN.
- Zero production code changes; zero prod behavior change until the founder acts.
</success_criteria>

<output>
Create `.planning/phases/125-worker-dedicated-backfill-worker-retention-hygiene/125-04-SUMMARY.md` when done.
</output>
