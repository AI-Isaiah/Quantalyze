---
phase: 162-honest-what-the-user-sees-is-true
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/portfolios/[id]/page.tsx
  - src/app/(dashboard)/portfolios/[id]/equity-curve-series.test.tsx
  - src/lib/queries.ts
  - src/components/strategy/StrategyTable.tsx
  - src/components/strategy/StrategyGrid.tsx
  - src/components/strategy/StrategyTable.stale-analytics.test.tsx
autonomous: true
requirements: [HONEST-04, HONEST-03]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Per-strategy equity curves render only for isRankableAnalyticsRow constituents; a failed / non-terminal / absent-series constituent produces NO line — never a flat 1.0 line, never a dead run's series, never zeros (UI-SPEC C-3)"
    - "The hard-coded equityCurve null and the comment asserting the series is not selected are both gone from portfolios/[id]/page.tsx (C-3 stale-artefact clause)"
    - "When at least one constituent lacks a rankable curve, exactly one colorless caption renders below the chart: 'Equity curves shown for {n} of {m} strategies — {m−n} without computed analytics are omitted.'; no caption when n equals m; caption still renders when n is 0"
    - "Raw returns_series / daily_returns never cross the RSC boundary — only computed {date,value} wealth points ship (the _rs/_dr strip idiom; DEF-147-A payload caution)"
    - "is_example rows never render SyncBadge regardless of computation status, on both StrategyTable and StrategyGrid (UI-SPEC C-6 class guard; makes HONEST-03 immune to the demo rows drifting stale again)"
    - "Equity curves resolve server-side in the RSC — no new client loading state (UI-SPEC loading row)"
  artifacts:
    - path: "src/app/(dashboard)/portfolios/[id]/equity-curve-series.test.tsx"
      provides: "Wave-0 equity-series spec: success fixture with returns_series, failed fixture carrying best-in-class stale values (STALE-01 fixture discipline)"
      contains: "isRankableAnalyticsRow"
  key_links:
    - from: "src/app/(dashboard)/portfolios/[id]/page.tsx"
      to: "src/lib/closed-sets.ts"
      via: "isRankableAnalyticsRow called per constituent inside the curve builder — the REQUIRED shared analog; the gate must be SHOWN applied, not asserted"
      pattern: "isRankableAnalyticsRow"
    - from: "src/app/(dashboard)/portfolios/[id]/page.tsx"
      to: "src/lib/factsheet/resolve-series.ts"
      via: "resolveDailyReturnSeries + cumprod fold for CSV constituents (don't hand-roll series conversion)"
      pattern: "resolveDailyReturnSeries"
---

<objective>
HONEST-04: `buildEquityCurveSeries` serves real per-strategy wealth curves — the data is
already on the wire (`getPortfolioStrategies` selects `returns_series`/`daily_returns`;
the comment claiming otherwise is false and goes). Every new read path is gated with
`isRankableAnalyticsRow`, or this plan re-opens the exact class #712 hotfixed days ago
(162-CONTEXT standing constraint). Plus HONEST-03's residual code half: the `is_example`
SyncBadge class guard (D-162-1 repairs the DATA in plan 162-08; this guard makes the
requirement immune to the demo rows drifting stale again — UI-SPEC C-6).

Purpose: phase success criterion 3 (curves) and the guard half of criterion 2.
Output: gated curve building + C-3 disclosure caption + is_example badge guard, each
pinned by RED-witnessed specs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-PATTERNS.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-UI-SPEC.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-VALIDATION.md
</context>

<artifacts_this_phase_produces>
- Real `buildEquityCurveSeries` implementation (existing symbol, new behavior): per-constituent `{date,value}[]` wealth points or null
- The C-3 coverage caption element below `PortfolioEquityCurve`
- `equity-curve-series.test.tsx` — new Wave-0 spec
- `is_example` SyncBadge guard in `StrategyTable` + `StrategyGrid`, pinned in `StrategyTable.stale-analytics.test.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate-first curve building in buildEquityCurveSeries</name>
  <files>src/app/(dashboard)/portfolios/[id]/page.tsx, src/lib/queries.ts, src/app/(dashboard)/portfolios/[id]/equity-curve-series.test.tsx</files>
  <read_first>
    - `src/app/(dashboard)/portfolios/[id]/page.tsx:211-231` — buildEquityCurveSeries with the hard-coded null and the false comment
    - `src/lib/closed-sets.ts:747-790` — isComputedAnalytics + isRankableAnalyticsRow (verbatim in 162-PATTERNS; its docblock explains why a NULL check is NOT a substitute — failed rows still hold KPI corpses, measured 17/18 in PROD)
    - `src/lib/utils.ts:171-176` — extractAnalytics does NO status gating; these rows do NOT pass through shapeRowAnalytics
    - `src/lib/factsheet/resolve-series.ts:41-57` — resolveDailyReturnSeries + the returns_series/daily_returns shape docs (returns_series is the cumprod WEALTH curve; daily_returns is CSV-ingest-only)
    - `src/lib/scenario-blend-adapter.ts:136-140` — the cumprod fold precedent
    - `src/lib/queries.ts:2092-2103` (getPortfolioStrategies select) and `:2265-2278` (the _rs/_dr strip idiom)
    - `src/components/portfolio/PortfolioEquityCurve.tsx:31` (RETURN_FORMATTER expects wealth points, base 1) and `:78` (skips empty/null curves — component unchanged)
    - ⚠️ 162-PATTERNS §STALE-01 gate SI-01 note: `complete-status-scan.test.ts` greps raw source for exact-match status comparisons — call the predicate, never inline the status strings
  </read_first>
  <behavior>
    - Test 1: a constituent with terminal-success status and a returns_series renders a curve of sorted {date,value} wealth points identical (post-normalization) to its series
    - Test 2: a FAILED constituent whose analytics row carries best-in-class stale values (sharpe, cagr, AND a full returns_series — the STALE-01 fixture discipline) produces equityCurve null: the dead series must be sitting right there and still not render
    - Test 3: a CSV constituent (daily_returns only) renders the cumprod wealth curve derived via resolveDailyReturnSeries
    - Test 4: no raw returns_series/daily_returns key appears in the props object shipped across the RSC boundary
  </behavior>
  <action>
    Replace the hard-coded null: per constituent, `extractAnalytics` the row, then
    `isRankableAnalyticsRow(a)` decides — true: build wealth points (API rows: normalize
    `returns_series` directly to sorted, finite `{date,value}` points; CSV rows:
    `resolveDailyReturnSeries` then the cumprod fold precedent); false: null (the chart
    skips it). Remove the entire stale comment block justifying the old null — the C-3
    stale-artefact clause pins this: the comment asserts something that becomes false the
    moment this task lands. Strip raw series fields from anything crossing the RSC
    boundary using the established destructure idiom (compute points server-side, ship
    points only). If the strip belongs in `getPortfolioStrategies`, follow the file's own
    `_rs`/`_dr` shape and mind the phase-147 grep-gates that watch select widths — the
    SELECT itself does not narrow (the page needs the series server-side); only what is
    forwarded narrows. Status gating goes through the predicate call, never an inline
    status-literal comparison (SI-01 census).

    Write the four behavior tests first (new Wave-0 spec file); witness Test 2 RED by
    neutering the gate call (byte-copy restore, shasum before/after).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/portfolios/[id]/equity-curve-series.test.tsx" && npx tsc --noEmit && ! grep -q "Returns_series is not selected" "src/app/(dashboard)/portfolios/[id]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The gate is SHOWN applied: `isRankableAnalyticsRow(` appears in the curve-building path of page.tsx (cite the line in the summary) — not merely asserted
    - Test 2's failed-row fixture carries a full stale returns_series and still yields null (RED-witnessed against the neutered gate)
    - The false comment and the hard-coded null are gone; no inline status-literal comparison was added
    - Raw series fields do not cross the RSC boundary (Test 4)
  </acceptance_criteria>
  <done>Real gated curves build server-side; dead runs render nothing; spec green and falsifiable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: C-3 coverage caption — missing must look missing, with the number attached</name>
  <files>src/app/(dashboard)/portfolios/[id]/page.tsx, src/app/(dashboard)/portfolios/[id]/equity-curve-series.test.tsx</files>
  <read_first>
    - 162-UI-SPEC §C-3 Disclosure row — exact copy string, tokens, and the n===m / n===0 rules
    - DESIGN.md § Voice (state the limitation with the number attached) and § Semantic-color gates (absence is colorless)
  </read_first>
  <behavior>
    - Test 5: with m=3 constituents and n=2 rankable, one caption renders below the chart with the exact string "Equity curves shown for 2 of 3 strategies — 1 without computed analytics are omitted."
    - Test 6: n === m renders NO caption; n === 0 still renders the caption (composite line only + disclosure)
    - Test 7: the caption carries text-caption text-text-muted and no semantic color token
  </behavior>
  <action>
    Render the single caption line below the chart card content, left-aligned with it,
    `text-caption text-text-muted`, DM Sans, plain text in document flow (it is the
    accessible disclosure — the absent line is never the only signal). Copy is the exact
    C-3 template with real computed numbers. No new loading state (series resolves in the
    RSC; the page's existing skeleton covers it). Extend the Task-1 spec with tests 5–7;
    witness Test 5 RED by neutering the caption condition.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/portfolios/[id]/equity-curve-series.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exact caption copy per C-3, numbers computed from the same n/m the curve builder produced (one source of truth, no second count)
    - n===m and n===0 behaviors per contract (tests 6)
    - Colorless: no accent, no negative, no warning token on the caption
  </acceptance_criteria>
  <done>The chart discloses its own coverage; nothing about absence is silent or colored</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: is_example SyncBadge class guard (HONEST-03, UI-SPEC C-6)</name>
  <files>src/components/strategy/StrategyTable.tsx, src/components/strategy/StrategyGrid.tsx, src/components/strategy/StrategyTable.stale-analytics.test.tsx</files>
  <read_first>
    - `StrategyTable.tsx:969` (hasComputedAnalytics) and `:1158` (the STALE-01 badge gate this guard extends) and `:555` (is_example already on the row)
    - `StrategyGrid.tsx:84` (Example chip) and `:110` (ungated SyncBadge — no page consumer at HEAD, but guard it anyway or the shaper covers it; 162-RESEARCH says cover both components or the shaper once)
    - `StrategyTable.stale-analytics.test.tsx` — the STALE-01 spec family to extend
    - 162-UI-SPEC §C-6 — the contract: is_example rows never render SyncBadge regardless of status; the Example chip carries their identity
  </read_first>
  <behavior>
    - Test 8: an is_example row with terminal-success analytics and a fresh computed_at renders NO SyncBadge in StrategyTable (this is the post-recompute state D-162-1 will create — the guard must hold BEFORE the data repair lands)
    - Test 9: a non-example row with the same analytics still renders its SyncBadge (the guard is example-scoped, not a badge deletion)
    - Test 10: StrategyGrid's badge render is guarded identically
  </behavior>
  <action>
    Extend the existing badge gates so `is_example` suppresses SyncBadge on both
    components, per C-6's recommended-required default. Extend, never fork, the existing
    gate expressions (the STALE-01 discipline: one decided predicate per claim). If you
    find a founder surface that depends on example sync dates, STOP and surface the
    conflict per C-6's own instruction — do not blend. Extend the stale-analytics spec
    with tests 8–10; witness Test 8 RED by neutering the guard condition (byte-copy
    restore).
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.stale-analytics.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both render paths guarded (StrategyTable and StrategyGrid), or the shaper enforces it once — whichever, the choice and its coverage are stated in the summary
    - Test 8 RED-witnessed against the neutered guard, restored byte-identically
    - Non-example badge behavior byte-equivalent (test 9)
  </acceptance_criteria>
  <done>A sync-recency claim can never again render on a demo seed, whatever its status</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB analytics rows → public/authed render | dead-run series and stale badges are integrity claims to allocators |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-03-A | Tampering (integrity of displayed data) | buildEquityCurveSeries new read path | high | mitigate | isRankableAnalyticsRow gate per constituent, pinned by a fixture that carries a full dead series and asserts null (the STALE-01 class must not re-open through this new door) |
| T-162-03-B | Tampering (false freshness claim) | SyncBadge on example rows | medium | mitigate | is_example class guard on both render paths, RED-witnessed |
| T-162-03-C | Information Disclosure | RSC payload | low | accept | raw series stripped before the boundary anyway (payload-size motive); the series is already served to authed portfolio owners by design |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase |
</threat_model>

<verification>
- Both spec files green file-scoped; full vitest suite at the wave gate (contract tests scan all of src/)
- `npx tsc --noEmit` clean
- Every RED witness recorded with shasum-verified restore
- DESIGN.md conformance: caption colorless, text-caption tier, no new spacing/type values
</verification>

<success_criteria>
- Success criterion 3's curve half: real per-strategy curves for rankable rows, absence (not fabrication) for the rest, disclosed with the number attached
- HONEST-03's guard half: the badge class is closed independent of the data repair
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-03-SUMMARY.md` when done.
</output>
