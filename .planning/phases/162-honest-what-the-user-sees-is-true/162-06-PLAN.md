---
phase: 162-honest-what-the-user-sees-is-true
plan: 06
type: execute
wave: 2
depends_on: ["162-03", "162-05"]
files_modified:
  - src/components/strategy/StrategyTable.tsx
  - src/app/(dashboard)/my-strategies/page.tsx
  - src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx
  - src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.tsx
  - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.preselect.test.tsx
  - src/components/strategy/StrategyTable.pending-chip.test.tsx
autonomous: true
requirements: [HONEST-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "'Finish setup →' opens the wizard with the clicked key preselected: the saved-key summary shows the clicked row's exchange label + key label as text (UI-SPEC C-5)"
    - statement: "Preselect summary renders the clicked key's exchange+label for each of ≥2 bare keys (not first-key/last-key)"
      verification: backstop
    - "'Use a different key' reverts to the blank credential-entry form via the overlay's keyed remount — the preselected key id joins the remount key"
    - "Focus lands on the 'Continue with this key' primary CTA when the step mounts preselected (DESIGN-05 focus rule); the selected key is conveyed by text, never color alone"
    - "The summary renders NO masked credential fields and no fake dots — placeholder secrets are fabricated data"
    - "An owner clicking Finish setup on an orphaned key proceeds through the reuse path and is never routed into the KEY_ORPHANED refusal (population b); keys with a live draft resume that draft (population a); mid-sync keys keep the pending chip and never show Finish setup (population c, pinned)"
    - "Long key labels truncate with title= recovery carrying the full nickname (always the real value)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.preselect.test.tsx"
      provides: "Wave-0 preselect spec: step mounts with the clicked key chosen; ≥2-key discrimination; revert-to-blank-form"
      contains: "preselect"
  key_links:
    - from: "src/components/strategy/StrategyTable.tsx"
      to: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      via: "onFinishSetup(keyId) → mount props → overlay preselect prop (in the remount key) → WizardClient apiKeyId seam (:246-248)"
      pattern: "onFinishSetup"
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      to: "src/app/api/strategies/create-with-key/route.ts"
      via: "'Continue with this key' on an orphaned key POSTs reuse_api_key_id (contract pinned by plan 162-05) and resumes on the returned draft envelope"
      pattern: "reuse_api_key_id"
---

<objective>
HONEST-06's client half: thread the clicked key id from the placeholder row to the
wizard, and give the connect step its saved-key summary sub-state (UI-SPEC C-5) so the
user SEES which key was chosen and can change it. Preselect means REUSE of the existing
`api_keys` row — never a prefilled credential form (the measured KEY_ORPHANED loop:
a prefilled form still re-POSTs, still hits the venue_account_id unique, still refuses).
Orphaned keys ride the server path plan 162-05 pinned (D-162-3).

Purpose: phase success criterion 4 — "Finish setup →" opens the wizard with the clicked
key preselected, for every population.
Output: the id thread, the C-5 summary state, and the Wave-0 preselect spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-PATTERNS.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-UI-SPEC.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-05-SUMMARY.md
</context>

<artifacts_this_phase_produces>
- `onFinishSetup?: (keyId: string) => void` — widened StrategyTable callback signature
- Overlay preselect prop (e.g. `preselectKey?: { id: string; exchangeLabel: string; keyLabel: string }`) joining the `key={...}` remount idiom
- ConnectKeyStep saved-key summary sub-state (eyebrow SAVED KEY / identity line / primary + text CTAs)
- `ContributionWizardOverlay.preselect.test.tsx` — new Wave-0 spec
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread the clicked key id from the row to the overlay mounts</name>
  <files>src/components/strategy/StrategyTable.tsx, src/app/(dashboard)/my-strategies/page.tsx, src/app/(dashboard)/my-strategies/MyStrategiesSection.tsx, src/app/(dashboard)/my-strategies/MyStrategiesEmptyState.tsx</files>
  <read_first>
    - `StrategyTable.tsx:303` (the bare `onFinishSetup?: () => void` and the docblock above it recording "no preselect seam today") and `:1377-1383` (the invocation site — the row HAS the key id)
    - `my-strategies/page.tsx:94-98` — placeholderRows already carry `{ id, exchangeLabel, keyLabel }` (server-formatted labels: what the user clicked is what the summary must show)
    - `MyStrategiesSection.tsx:124-136` — the mount + the :124-127 comment recording the 2026-08-05 founder ruling (D-162-3 supersedes it; the comment must be updated in the same edit)
    - `MyStrategiesEmptyState.tsx:47` — the second mount
    - ⚠️ Wave note: plan 162-03 already modified StrategyTable.tsx in wave 1 — work from current HEAD of the phase branch, not from the pattern map's pre-phase line numbers
  </read_first>
  <behavior>
    - Test T-1: clicking Finish setup on a placeholder row invokes onFinishSetup with THAT row's key id (two bare keys in the fixture; each row passes its own id)
    - Test T-2 (population c, extend the pending-chip pin in Task 3): mid-sync keys still never render Finish setup
  </behavior>
  <action>
    Widen the callback to carry the key id, pass the clicked row's
    `{ id, exchangeLabel, keyLabel }` up through both mounts into overlay state, and
    replace the superseded founder-ruling comment with a one-liner citing D-162-3.
    "Finish setup →" keeps its exact visual treatment (existing accent link — C-5 Entry
    row). No behavior change for any non-placeholder row.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/strategy/StrategyTable.pending-chip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The callback signature is widened, both mounts thread the clicked row's triple, and the stale founder-ruling comment is gone (replaced with the D-162-3 citation)
    - No visual change to the link itself; pending-chip pins stay green
  </acceptance_criteria>
  <done>The clicked key's identity reaches the overlay mount as typed props</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Overlay preselect prop + WizardClient seam + the C-5 saved-key summary</name>
  <files>src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx, src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx</files>
  <read_first>
    - `ContributionWizardOverlay.tsx:49-54` (the prop seam to widen) and `:244-250` (the `key={`${source}:${draftId}`}` remount idiom — the preselect key id JOINS this key so toggling keys remounts cleanly)
    - `WizardClient.tsx:246-248` — the existing apiKeyId seam (seeded from a resumed draft today; the preselect seeds it too)
    - `ConnectKeyStep.tsx:431-457` — the credential-entry step (its props and state; the summary is a SUB-STATE of this step, composed from existing primitives — no new component family)
    - 162-UI-SPEC §C-5 — the full anatomy table (data-panel region: square, flat, hairline; eyebrow `SAVED KEY` Geist Mono uppercase text-micro tracking 0.18em muted; identity line `{Exchange label} — {key label}` text-small DM Sans; CTAs; truncate+title=; focus rule; NO masked fields)
    - 162-05-SUMMARY.md — the pinned reuse contract (request field + draft-arm envelope)
    - DESIGN.md § Cards-vs-Data-panels and § Typography (three voices)
  </read_first>
  <behavior>
    - Test O-1: opening the overlay with a preselect renders the summary (eyebrow + the CLICKED key's exchange + label) instead of the credential form
    - Test O-2: "Use a different key" reverts to the blank credential-entry form (preselect dropped, remount observed — no leftover summary state)
    - Test O-3: focus is on "Continue with this key" when the step mounts preselected
    - Test O-4: population (a) — preselected key with a live draft: Continue proceeds via the existing draft-resume plumbing (apiKeyId seeded, draft loaded)
    - Test O-5: population (b) — orphaned key: Continue POSTs reuse_api_key_id and, on the draft envelope, resumes exactly as population (a); the KEY_ORPHANED refusal is never rendered on this path
  </behavior>
  <action>
    Add the optional preselect prop to the overlay and fold the key id into the remount
    key. Seed WizardClient's `apiKeyId` from the preselect. Build the saved-key summary
    sub-state inside ConnectKeyStep per the C-5 anatomy — every token and copy string is
    in the contract table; primary CTA "Continue with this key" (accent Button — the one
    NEW accent element the UI-SPEC reserves), text button "Use a different key"
    (`text-small text-accent underline underline-offset-2`), truncation with `title=`
    carrying the full nickname, focus to the primary CTA on mount, identity conveyed by
    text. Absolutely no credential placeholders or masked dots.

    Continue-with-this-key routing: if a live draft exists for the key (the overlay's
    offered-read already answers this), resume it; else POST the reuse arm from plan
    162-05 and resume on the returned draft envelope — population (b) reduces to
    population (a) after one server call. Surface server refusals through the existing
    envelope renderer (no new copy here; plan 162-05 owns the refusal vocabulary).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <note>Behavior tests O-1..O-5 are Task 3's spec file (Wave-0 gap) — Task 3's verify runs them; this task's gate is compile-clean wiring only</note>
  </verify>
  <acceptance_criteria>
    - C-5 anatomy implemented exactly (eyebrow voice, identity line voice, both CTA treatments, no masked fields, focus rule) — cite the contract rows in the summary
    - The preselect id is part of the remount key (shown, not asserted)
    - Population (b) never routes into the KEY_ORPHANED refusal (test O-5) — the closed loop is the point of D-162-3
    - Behavior tests O-1..O-5 live in Task 3's spec file if not colocated here; either way they exist and pass before this plan closes
  </acceptance_criteria>
  <done>The wizard opens showing the clicked key and both CTAs behave per contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preselect spec (Wave 0) + pending-chip pin extension</name>
  <files>src/app/(dashboard)/allocations/components/ContributionWizardOverlay.preselect.test.tsx, src/components/strategy/StrategyTable.pending-chip.test.tsx</files>
  <read_first>
    - `StrategyTable.pending-chip.test.tsx:665-681` — the existing pin for when Finish setup shows at all (population c)
    - 162-UI-SPEC UI-Considerations backstop row: "Preselect summary renders the clicked key's exchange+label for each of ≥2 bare keys" — this spec is that backstop's evidence
  </read_first>
  <behavior>
    - The O-1..O-5 suite from Task 2, plus:
    - Test O-6 (the backstop): with two bare keys A and B, clicking A's Finish setup shows A's exchange+label; clicking B's shows B's — not first-key, not last-key
    - Test O-7: population (c) unchanged — mid-sync key renders the pending chip and no Finish setup control
  </behavior>
  <action>
    Write the overlay preselect spec covering O-1..O-6 and extend the pending-chip spec
    with O-7. Witness O-6 RED by neutering the id thread (hard-code the first key's
    triple at the mount), observe the failure, restore from a byte copy (never
    `git checkout --`), shasum before/after identical. Record the RED evidence in the
    summary — this test discriminates the exact bug class (wrong-key preselect) the
    backstop exists for, so a version that cannot fail is worth nothing.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.preselect.test.tsx" src/components/strategy/StrategyTable.pending-chip.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - O-6 witnessed RED against the neutered thread, restored byte-identically (shasum evidence)
    - All three populations covered by passing tests (a: O-4, b: O-5, c: O-7)
    - Full-suite reminder honored at the wave gate (contract tests scan all of src/)
  </acceptance_criteria>
  <done>The preselect behavior is pinned falsifiably for every population and every key count</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (key id in overlay state) → server | the threaded key id is selection intent only; the server re-proves ownership (plan 162-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-06-A | Elevation / IDOR | client-supplied key id | high | transfer | ownership enforcement lives server-side in plan 162-05's route arm + RPC assertion (tested there, RED-witnessed); the client never treats the id as authorization — it threads what the owner's own authed page payload already contains |
| T-162-06-B | Spoofing (wrong-key confusion) | preselect summary | medium | mitigate | the summary renders the clicked row's server-formatted labels; O-6 pins per-row discrimination across ≥2 keys |
| T-162-06-C | Information Disclosure | saved-key summary | low | accept | summary shows exchange + user's own nickname only — no credentials exist client-side on this path by construction |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase |
</threat_model>

<verification>
- Preselect + pending-chip specs green; tsc clean; full vitest at the wave gate
- O-6 RED witness recorded with shasum-verified restore
- DESIGN.md conformance: data-panel (square/flat/hairline), three voices respected, accent only on the reserved list
</verification>

<success_criteria>
- Phase success criterion 4 holds end-to-end: clicking "Finish setup →" opens the wizard with THAT key preselected, changeable, and workable for draft-holding, orphaned, and mid-sync keys alike
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-06-SUMMARY.md` when done.
</output>
