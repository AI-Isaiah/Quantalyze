---
phase: 162-honest-what-the-user-sees-is-true
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/routers/portfolio.py
  - analytics-service/tests/test_computation_error_curated.py
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.test.tsx
autonomous: true
requirements: [HONEST-01]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "classify_exception's bottom arms return curated fixed copy in the message slot; error_kind values are byte-unchanged ('unknown' still drives retry classification)"
    - "No writer stamps raw or scrubbed exception text into strategy_analytics.computation_error or portfolio_analytics.computation_error — raw detail goes to logs/Sentry only (D-162-4 strict, mapped at the WRITER per success criterion 1)"
    - "All 18 _stamp_strategy_analytics_failed call sites keep their typed human prefix sentence and lose the appended scrubbed suffix, via the :2874 choke point, not 18 point edits"
    - "The computation_error column value never renders inside the wizard failure envelope — the GATE_ANALYTICS_FAILED Details appendix is removed (UI-SPEC C-2)"
    - "Portfolio StaleWarning's structure is unchanged; it renders the column verbatim, which post-fix holds a curated sentence (mapping at the reader is the recorded anti-pattern)"
    - "No curated writer literal is string-equal to any wizardErrors title for codes that can co-occur on one screen (C-2 anti-double-render rule)"
  artifacts:
    - path: "analytics-service/tests/test_computation_error_curated.py"
      provides: "pytest pins: unknown-arm mapping, stamp-suffix stripping, portfolio _fail — each witnessed RED against its neutered fix"
      contains: "classify_exception"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "strategy_analytics.computation_error"
      via: "mark_compute_job_failed → compute_jobs.last_error → sync_strategy_analytics_status branch (b) verbatim copy — curating the Python writers makes a bridge migration unnecessary"
      pattern: "curated"
    - from: "analytics-service/routers/portfolio.py"
      to: "portfolio_analytics.computation_error"
      via: "_fail() writer at :692-700"
      pattern: "_fail"
---

<objective>
Close HONEST-01 as a class at every writer (D-162-4 strict, decided by the founder):
the `classify_exception` unknown arms, the ~18 prefixed-`scrubbed` stamp call sites via
their choke point, and the portfolio `_fail` catch-all all write curated fixed copy to
the user-readable `computation_error` columns; raw exception text goes to logs/Sentry
only. Then remove the wizard envelope's now-redundant `Details:` appendix (UI-SPEC C-2).

The SQL bridge needs NO change: `sync_strategy_analytics_status` copies what it is given,
and once every Python writer is curated, what it is given is curated. Prefer that — a
bridge edit auto-applies to PROD on merge (162-PATTERNS §SQL bridge).

Purpose: a computation failure renders mapped user copy, never a raw Python exception
string (phase success criterion 1). The str/None root-cause half of the criterion is
plan 162-01's verdict + plan 162-08's follow-through, deliberately not here.
Output: curated writers + RED-witnessed pytest pins + the envelope appendix removal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-PATTERNS.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-VALIDATION.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
- Curated user-copy literals for the unknown arms and `_fail` (module-level constants in `job_worker.py` / `portfolio.py`, exact strings from UI-SPEC C-2's table)
- A `detail=`-style split on `_stamp_strategy_analytics_failed` (stamped column gets the typed sentence only; scrubbed detail stays on the log/DispatchResult side)
- `analytics-service/tests/test_computation_error_curated.py` — new pytest module
- SyncPreviewStep: `GATE_ANALYTICS_FAILED` cause without the Details appendix
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Curate the classify_exception unknown arms and the stamp choke point (job_worker.py)</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_computation_error_curated.py</files>
  <read_first>
    - `job_worker.py:678-831` — classify_exception in full: the InvalidToken fixed-message arm (:694-702, the target shape), the curated_gateway_detail allow-list arm (:743-744 + docblock :723-733), and the two bottom arms (:827-831) returning the bare truncated exception string
    - `job_worker.py:2855-2877` — the `_stamp_strategy_analytics_failed` helper AND the anti-vacuity gate regions directly above it (:2855-2874). ⚠️ Count the gate's tokens BEFORE any edit — a token chosen by reading the finished file always passes (recorded project failure mode)
    - `job_worker.py:3699-3709` — one canonical stamp call site: typed sentence + appended scrubbed suffix, with the same scrubbed detail ALSO written to DispatchResult/logs at :3707-3709
    - 162-PATTERNS §job_worker (the 18 enumerated call-site lines) and §Curated-copy-at-writer
    - `analytics-service/tests/test_computing_started_at_stamp.py` — the AST census; payloads must stay dict literals at call sites (pitfall 6)
  </read_first>
  <behavior>
    - Test A: classify_exception on an unrecognized RuntimeError returns kind "unknown" and a message that is a fixed curated sentence per D-162-4 — the test FAILS if the message slot ever again contains the exception's own text (assert the raised exception's marker string is absent from the returned message)
    - Test B: same for the ccxt.BaseError bottom arm — kind unchanged, message curated
    - Test C: _stamp_strategy_analytics_failed writes a computation_error value that ends with the typed sentence — a sentinel marker passed as the exception text never reaches the stamped payload, but DOES reach the log/detail side
    - Test D (vacuity guard): each of A–C is witnessed RED against its own neutered fix before acceptance
  </behavior>
  <action>
    First, record the pre-edit token counts for the :2855-2874 gate regions (whatever
    identifiers/literals those indentation-bounded gates match on) — paste the counts into
    the work log before touching the file.

    Then implement per D-162-4 strict:
    1. Bottom arms of classify_exception: keep the tuple shape and the kind values
       exactly; replace the message slot with the curated sentence from UI-SPEC C-2
       ("Analytics could not complete due to an unexpected error. Retry the sync.").
       Log the raw exception (logger.exception or the module's established logging call)
       so operators lose nothing. Do NOT touch the already-curated arms (InvalidToken,
       curated_gateway_detail, and friends) — 162-PATTERNS lists the writers that must
       NOT be reworked.
    2. Choke-point edit: split `_stamp_strategy_analytics_failed` so the STAMPED column
       value is the typed sentence only, while the scrubbed exception detail is accepted
       as a separate argument that goes to logs/DispatchResult only (the helper already
       re-scrubs internally — mirror the 161.1 CR-02 choke-point discipline). Update the
       18 call sites to pass their sentence and their detail separately — mechanical,
       because every site already builds both strings adjacently. Enumerate all 18 sites
       (the PATTERNS line list, re-grepped at HEAD) and confirm each one's stamped payload
       no longer concatenates the scrubbed text.
    3. Keep payload dict literals literal at every call site (the JOB-01 AST census reds
       on payload indirection — analytics_runner.py:1340-1350 documents the measured
       failure of "cleaning this up").

    After edits: re-count the gate tokens from step 0 and show they are unchanged (or
    re-cut deliberately with reasoning in the same commit).

    Anti-vacuity protocol for every test: neuter the specific fixed-copy line (restore
    the raw-string expression), RUN the test, OBSERVE the failure, restore from a byte
    copy (never `git checkout --`), `shasum -a 256` before/after identical.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_computation_error_curated.py -x -q && python3 -m mypy --strict services/job_worker.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Kind values in classify_exception are byte-identical pre/post (retry classification untouched)
    - All 18 stamp sites route through the choke point with no stamped scrubbed suffix; the enumeration is shown in the summary, not asserted
    - Gate-token counts for :2855-2874 recorded PRE-edit and re-verified post-edit
    - Tests A–C each witnessed RED against the neutered fix, restored byte-identically (shasum evidence in summary)
    - mypy --strict clean on the touched files
  </acceptance_criteria>
  <done>job_worker.py writers curated at the choke point; pytest pins green and proven falsifiable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Curate the portfolio _fail catch-all (portfolio.py)</name>
  <files>analytics-service/routers/portfolio.py, analytics-service/tests/test_computation_error_curated.py</files>
  <read_first>
    - `portfolio.py:692-700` (`_fail` — the writer) and `:1191` (the catch-all composing type-name + truncated exception text) and `:1193` (logger.exception already present)
    - `src/app/(dashboard)/portfolios/[id]/page.tsx:137-140` — the StaleWarning reader; it renders the column verbatim and must NOT be changed (requirement locks the writer)
  </read_first>
  <behavior>
    - Test E: the portfolio catch-all writes the curated sentence ("Portfolio analytics could not complete due to an unexpected error. Retry the computation." per UI-SPEC C-2) to computation_error; a sentinel marker in the raised exception never reaches the written value
    - Test F: the raw type-name + message still reaches the logger (operators keep the diagnostic)
  </behavior>
  <action>
    Replace the f-string argument at the catch-all `_fail` call with the curated constant;
    keep `logger.exception` carrying the raw detail. Audit the OTHER `_fail` call sites in
    the router: any that already pass fixed typed copy stay untouched; any other site
    interpolating exception text gets the same treatment (close the class, not the
    instance — CLAUDE.md fix-campaign discipline). Do not modify the StaleWarning reader.
    Witness each new test RED against its neutered fix (same byte-copy restore protocol).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_computation_error_curated.py -x -q && python3 -m mypy --strict routers/portfolio.py 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - No `_fail` call site interpolates exception text into the written value (shown by enumeration of call sites in the summary)
    - StaleWarning reader byte-unchanged (`git diff --stat` shows no page.tsx change from this task)
    - Tests E–F witnessed RED, restored byte-identically
  </acceptance_criteria>
  <done>portfolio_analytics.computation_error can only receive curated copy; raw detail lives in logs</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove the wizard envelope's Details appendix (UI-SPEC C-2)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.test.tsx</files>
  <read_first>
    - `SyncPreviewStep.tsx:2103-2106` — the GATE_ANALYTICS_FAILED cause appending the column value into WizardErrorEnvelope (and its comment calling the value "server-scrubbed")
    - 162-UI-SPEC §C-2 — the final envelope shape: title/cause/fix from wizardErrors, no appendix, diagnostics accordion unchanged, anti-double-render rule
    - ⚠️ `src/lib/wizardErrors.test.ts` has a deliberate NUL byte at line 1572 — any sweep of wizard error surfaces uses `grep -a`, awk, or node, never plain grep
  </read_first>
  <behavior>
    - Test G: the GATE_ANALYTICS_FAILED render never includes the computation_error column value in the envelope body (fixture row carries a sentinel error string; assert it is absent from the rendered output)
    - Test H: title, cause sentence, fix lines, retry CTA, and diagnostics accordion all still render per the existing envelope contract
  </behavior>
  <action>
    Delete the appendix concatenation so the cause sentence stands alone; correct or
    remove the adjacent comment (its claim about scrubbing being sufficient is the exact
    misunderstanding this phase closes — scrubbing removes secrets, not internals). Do not
    add any reader-side mapping of column content (recorded anti-pattern). Verify the C-2
    anti-double-render rule: the curated Python literals from Tasks 1–2 must not be
    string-equal to any wizardErrors title for co-occurring codes — sweep with `grep -a`
    and record the check. Extend the SyncPreviewStep spec (or the file's existing spec
    family) with tests G–H; witness G RED by re-adding the appendix, restore byte-
    identically.
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Envelope body cannot render the column value for GATE_ANALYTICS_FAILED (test G, RED-witnessed)
    - Anti-double-render sweep recorded: no curated literal equals a wizardErrors title for co-occurring codes
    - Diagnostics accordion and retry CTA behavior unchanged (test H)
    - Full-suite note honored at the wave gate: file-scoped runs cannot clear src/__tests__/contracts/
  </acceptance_criteria>
  <done>The wizard failure envelope shows curated cause copy only; the column value has no render path into it</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker exception → user-readable DB column | computation_error columns render on wizard, portfolio dashboard, factsheets, exports |
| DB column → wizard envelope | SyncPreviewStep renders whatever the column holds |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-02-A | Information Disclosure | classify_exception bottom arms, _fail catch-all, 18 stamp sites | high | mitigate | curated-copy-at-writer (ASVS V8): fixed sentences in the column, raw text to logs/Sentry only; scrub_freeform_string stays as defense-in-depth but is not the mapping (pitfall 3) |
| T-162-02-B | Information Disclosure | wizard envelope Details appendix | medium | mitigate | appendix removed (C-2); column value has no render path into the envelope; diagnostics accordion stays PII-scrubbed and carries code + correlation_id only |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase (RESEARCH audit empty) |
</threat_model>

<verification>
- `cd analytics-service && python3 -m pytest -x -q` green from `analytics-service/` (never repo root — VCR cassettes)
- `python3 -m mypy --strict` clean on touched analytics-service files (memory rule: mypy before shipping any analytics-service change)
- Full vitest suite at the wave gate (contract tests scan all of src/)
- Every new test's RED witness recorded with shasum-verified byte-identical restore
- ⛔ Local analytics-service was never started (`uvicorn main:app` claims real PROD jobs)
</verification>

<success_criteria>
- Success criterion 1's writer half holds: no reachable writer can put raw or scrubbed exception text in front of a user
- The class is closed at BOTH tables (strategy_analytics + portfolio_analytics) and the envelope render path
- The SQL bridge is untouched, with the no-change decision recorded in the summary
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-02-SUMMARY.md` when done.
</output>
