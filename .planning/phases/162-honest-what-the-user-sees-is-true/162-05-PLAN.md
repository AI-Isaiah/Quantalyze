---
phase: 162-honest-what-the-user-sees-is-true
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260826130000_create_wizard_strategy_for_key.sql
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - supabase/tests/test_create_wizard_strategy_for_key.sql
  - src/lib/wizardErrors.ts
autonomous: false
requirements: [HONEST-06]

estimate:
  tokens: 95000
  raw_tokens: 95000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "POST create-with-key with reuse_api_key_id mints a draft strategy OVER the caller's own existing api_keys row and NEVER INSERTs into api_keys (D-162-3: reuse, never re-INSERT — the measured KEY_ORPHANED unwinnable loop)"
    - "Tenant scoping on the reuse arm is the server-session user id — the admin client bypasses RLS, so the .eq(user_id) filter IS the boundary and the uid never comes from the request body (create-with-key docblock :129-132, load-bearing)"
    - "A key id the caller does not own, or a disconnected key, is refused with no write; a CONNECTED key (a strategy already references it) is refused with honest copy"
    - "The new SQL function is SECURITY DEFINER with service-role-only EXECUTE plus an in-body role gate plus an in-body ownership assertion joining api_keys.user_id to the passed uid — the create_wizard_strategy discipline minus its api_keys INSERT"
    - "A repeat POST for the same key converges on the same draft (advisory-lock + select-existing idempotency fence copied from create_wizard_strategy)"
    - "The success envelope reuses the resolver's draft-arm shape ({ kind: 'draft', strategy_id, api_key_id }) so plan 162-06's client reduces the orphan population to the existing draft-resume path"
  artifacts:
    - path: "supabase/migrations/20260826130000_create_wizard_strategy_for_key.sql"
      provides: "the use-existing-key service-role writer (name/shape per the Task-1 decision)"
      contains: "SECURITY DEFINER"
    - path: "supabase/tests/test_create_wizard_strategy_for_key.sql"
      provides: "state-adaptive SQL gate: EXECUTE revoked from authenticated, in-body role refusal, ownership assertion, no api_keys INSERT"
      contains: "service_role"
  key_links:
    - from: "src/app/api/strategies/create-with-key/route.ts"
      to: "supabase/migrations/20260826130000_create_wizard_strategy_for_key.sql"
      via: "rpcAdmin.rpc(...) behind withAuth — the only caller; missing service-role credential fails LOUD and refuses the write (route :858 posture)"
      pattern: "rpc"
    - from: "162-06-PLAN.md (client thread)"
      to: "src/app/api/strategies/create-with-key/route.ts"
      via: "the reuse_api_key_id request field + draft-arm response envelope defined here are the contract plan 162-06 consumes"
      pattern: "reuse_api_key_id"
---

<objective>
D-162-3 (FULL, founder-decided): add the use-existing-key server path so "Finish setup →"
works for every key, including KEY_ORPHANED ones — the population it was added to help.
This is the phase's one security-boundary item: a NEW service-role writer at the tenant
boundary (ADR-0001/0003 territory, the D-162-3 fence). The path REUSES an existing
`api_keys` row and must NEVER re-INSERT one; the high-severity threat is cross-tenant
reuse of a key row the caller does not own.

The founder decided WHETHER (full scope). The remaining one-way door is HOW the writer is
shaped — a new RPC contract auto-applies to PROD on merge — so Task 1 is a
reversibility-gated decision checkpoint, then the tracer walks the chosen door end-to-end.

Purpose: the server half of phase success criterion 4. Plan 162-06 threads the client.
Output: migration + route arm + state-adaptive SQL gate + route tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-PATTERNS.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-RESEARCH.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-CONTEXT.md
</context>

<artifacts_this_phase_produces>
- SQL function `create_wizard_strategy_for_key(p_user_id uuid, p_api_key_id uuid, ...)` (final name/param set per Task-1 decision) — SECURITY DEFINER, service-role-only
- Route contract: optional `reuse_api_key_id` request field on POST `/api/strategies/create-with-key`; success envelope = the resolver's existing draft-arm shape
- `supabase/tests/test_create_wizard_strategy_for_key.sql` — state-adaptive SQL gate
- New refusal copy (only if no existing wizardErrors code is honest for the connected-key refusal) — inside the Phase-161 WIZERR fences
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decision gate — shape of the use-existing-key writer</name>
  <decision>Shape of the use-existing-key service-role writer (the one-way door D-162-3 walks through)</decision>
  <context>
    D-162-3 already decided FULL scope — this checkpoint does NOT re-ask that. It gates
    the writer's shape, because the chosen SQL contract auto-applies to PROD on merge and
    later phases inherit it (REVERSIBILITY_GATES; undoing a published SECURITY DEFINER
    contract is a migration plus a route rollback). PATTERNS found no exact analog — the
    nearest is create_wizard_strategy itself (quoted verbatim in 162-PATTERNS §NEW
    use-existing-key server path).
  </context>
  <options>
    <option id="new-rpc">
      <name>New function `create_wizard_strategy_for_key` (RECOMMENDED)</name>
      <pros>The battle-tested create_wizard_strategy stays byte-untouched (no re-base risk on the live wizard writer); the new function's smaller surface IS the security property — it physically contains no api_keys INSERT, so "reuse, never re-INSERT" holds by construction and the SQL gate can assert it structurally</pros>
      <cons>A second wizard-writer function to maintain; shared idempotency/lock discipline is copied, not shared</cons>
    </option>
    <option id="widen-existing">
      <name>Widen `create_wizard_strategy` with an optional `p_existing_api_key_id` branch</name>
      <pros>One writer, one lock discipline, one audit surface</pros>
      <cons>CREATE OR REPLACE of the LIVE wizard writer (must re-base on its latest definition across all migrations — project rule ⭐); the function then contains BOTH an api_keys INSERT arm and a reuse arm, so "never re-INSERT" becomes a branch condition instead of a structural guarantee; blast radius covers every existing wizard submit</cons>
    </option>
  </options>
  <resume-signal>Select: new-rpc or widen-existing</resume-signal>
</task>

<task type="tracer">
  <name>Task 2: Tracer — one orphaned key, end-to-end, through the chosen writer</name>
  <reversibility rating="one-way">The SECURITY DEFINER contract and its EXECUTE surface auto-apply to PROD on merge and plan 162-06's client builds on the envelope; undoing it is a migration plus a route+client rollback, not a refactor.</reversibility>
  <files>supabase/migrations/20260826130000_create_wizard_strategy_for_key.sql, src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts</files>
  <read_first>
    - `create-with-key/route.ts:116-132` — the service-role boundary contract (authenticated holds no EXECUTE; fn body refuses non-service_role; the CEILING sentence; the load-bearing .eq(user_id) docblock)
    - `route.ts:199-217` — resolveByVenueIdentity's ownership gate (the verbatim mandatory template) and `:248-252` (the RLS defense-in-depth note)
    - `route.ts:295-318` — the two-read orphan-verification discipline (both strategy reads empty ⇒ genuinely orphaned)
    - `route.ts:858` (fail-LOUD missing-credential posture) and `:881-882` (the rpcAdmin.rpc call shape)
    - Migrations `20260813150106` (A) and `20260814120000` (B) — the EXECUTE-revocation pattern to copy; grep ALL migrations for the latest create_wizard_strategy definition before writing SQL (project rule ⭐)
    - `src/lib/wizardErrors.ts:1780-1817` — the KEY_ORPHANED docblock recording the unwinnable loop this path closes
  </read_first>
  <action>
    Implement the thinnest complete path per the Task-1 decision (D-162-3):

    SQL (one migration): the writer takes the uid and key id, and inside the function
    body — (1) refuses when `auth.role()` is not service_role; (2) asserts the key row
    exists with `user_id = p_user_id AND disconnected_at IS NULL` (raise on failure — the
    in-body ownership assertion is the new part the pattern map calls for); (3) refuses
    when any strategy already references the key (connected-key refusal); (4) takes the
    advisory-lock + select-existing idempotency fence copied from create_wizard_strategy;
    (5) INSERTs the draft strategy row(s) exactly as create_wizard_strategy does MINUS
    any api_keys write, wiring the existing key id. EXECUTE granted to service_role only,
    revoked from authenticated/anon (Migration A/B pattern). Header documents the
    ADR-0001/0003 posture and states that merging applies it to PROD.

    Route: accept optional `reuse_api_key_id` (UUID-shape validated) on the POST body.
    When present: withAuth session uid only (never body uid); re-select the key
    `.eq("id", keyId).eq("user_id", userId).is("disconnected_at", null)` — and per the
    :248-252 note ALSO prove ownership through the user-scoped client where grants allow
    (defense-in-depth); re-verify orphanhood with the two-read discipline; refuse
    `connected` keys with honest copy; then call the writer via rpcAdmin with the
    fail-LOUD missing-credential posture. Success responds with the resolver's existing
    draft-arm envelope so the client resumes the wizard as if a draft had existed.
    Credential fields are ignored on this arm — nothing is validated against a venue,
    nothing is encrypted, nothing touches api_keys.

    Route tests (the tracer's real verify): owned-orphaned key → 200 draft envelope and
    the api_keys row count for that user is UNCHANGED; non-owner's key id → refusal, no
    write; disconnected key → refusal; repeat POST → same strategy id (idempotency).
    Witness the ownership test RED by neutering the user_id filter in the route's key
    re-select (byte-copy restore, shasum before/after).
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/create-with-key/route.test.ts" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - End-to-end: the route test drives request → ownership gate → RPC call shape → draft envelope, not per-layer stubs
    - The migration contains no INSERT targeting api_keys (structural, per the chosen shape)
    - The uid used in every filter comes from the withAuth session; the request body carries only selection intent (the key id)
    - The ownership refusal test was witnessed RED against the neutered filter
    - ⚠️ The migration's acceptance note states: merging supabase/migrations/** to main AUTO-APPLIES to PROD — reviewed on that basis
  </acceptance_criteria>
  <done>An owner's orphaned key can be turned into a draft strategy through one committed, tested, service-role-gated path that never touches api_keys</done>
</task>

<task type="auto">
  <name>Task 3: Hardening — state-adaptive SQL gate, refusal copy, boundary sweep</name>
  <files>supabase/tests/test_create_wizard_strategy_for_key.sql, src/lib/wizardErrors.ts, src/app/api/strategies/create-with-key/route.test.ts</files>
  <read_first>
    - Phase-156 state-adaptive SQL gates (plans 08/09 pattern, per STATE.md §Phase 156 close-out): gates SKIP on a pre-migration database and ARM after, so a green sql-tests run pre-application does not lie
    - 162-VALIDATION §Anti-Vacuity — the prose-satisfied-anchor trap: pg_get_functiondef RETURNS COMMENTS; strip comments before asserting, choose tokens by diffing versions
    - `.github` db-test wiring rule: SQL gates MUST live at supabase/tests/test_*.sql
    - Phase-161 curated-message fences in wizardErrors tests (new user-facing copy lives inside them); ⚠️ wizardErrors.test.ts NUL byte at line 1572 — sweep with grep -a/node
  </read_first>
  <action>
    Write `supabase/tests/test_create_wizard_strategy_for_key.sql`, state-adaptive
    (SKIP when the function is absent, ARM after), asserting: (1) EXECUTE is absent for
    authenticated and anon and present for service_role (information_schema/ACL read);
    (2) the function body — read via pg_get_functiondef WITH COMMENTS STRIPPED before any
    token match — contains the role refusal and the ownership predicate, tokens chosen by
    diffing against the function-absent state, not by reading the finished body; (3) the
    body contains no INSERT targeting api_keys (comment-stripped negative assertion);
    (4) calling as a non-service role fails.

    Refusal copy: prefer an EXISTING wizardErrors code that is honest for the
    connected-key refusal; mint a new code ONLY if none fits, inside the WIZERR fences
    with the curated-message tests extended. The KEY_ORPHANED refusal copy itself stays
    for the residual non-owner/anonymous population (UI-SPEC C-5: Phase-161-fenced,
    out of scope unless a fix[] line now lies for a reachable population — if you find
    that, surface it, do not silently edit).

    Boundary sweep: confirm the resolver's `orphaned` arm still carries no key id
    (T-154-06-C stands — the client threads the id from the owner's own page payload,
    not from the resolver); mirror the route's scrubSeamError posture on the new arm
    (never a raw Postgres error).
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/create-with-key/route.test.ts" && test -f supabase/tests/test_create_wizard_strategy_for_key.sql && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The SQL gate is state-adaptive (SKIP/ARM recorded in its header) and every body assertion strips comments first — no prose-satisfied anchor
    - Copy decision recorded: which existing code was reused, or why a new one was minted inside the fences
    - The resolver's orphaned arm is unchanged; the new arm's error path never forwards raw driver errors
  </acceptance_criteria>
  <done>The writer's boundary is machine-checked in CI and its refusals speak honest, fenced copy</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → create-with-key route | untrusted request body now carries a key id (selection intent) |
| route (admin client) → database | createAdminClient BYPASSES RLS; the .eq(user_id) filter IS the tenant boundary |
| migration merge → PROD | supabase/migrations/** auto-applies on merge to main |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-05-A | Elevation / IDOR (cross-tenant key reuse) | reuse arm in create-with-key + new RPC | high | mitigate | session-uid-only ownership filter on the admin client + user-scoped-client defense-in-depth re-read + in-RPC ownership assertion; refusal test RED-witnessed against the neutered filter |
| T-162-05-B | Tampering (api_keys re-INSERT re-opens KEY_ORPHANED loop) | new SQL writer | high | mitigate | writer contains no api_keys INSERT (structural under new-rpc); comment-stripped SQL gate asserts it; route arm never touches credential/encryption paths |
| T-162-05-C | Elevation | RPC EXECUTE surface | high | mitigate | service-role-only EXECUTE + in-body auth.role() gate (Migration A/B pattern), asserted by the state-adaptive SQL gate |
| T-162-05-D | Information Disclosure | reuse-arm error path | medium | mitigate | scrubSeamError posture mirrored; static envelopes only |
| T-162-05-E | Repudiation | new service-role write | medium | accept | inherits the standing ADR-0001/0003 service_role ceiling ("any server route holding createAdminClient can pass any uid") — documented in the migration header; SEC-03's audit-gate decision (Phase 163) will police RPC audit emission, noted for that phase |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase |
</threat_model>

<verification>
- Route tests green; SQL gate present and state-adaptive; tsc clean; full vitest at the wave gate
- Ownership RED witness recorded with shasum-verified restore
- Migration reviewed on the auto-applies-to-PROD basis (its header says so)
</verification>

<success_criteria>
- The server half of success criterion 4 exists: an owner's orphaned key becomes a draft strategy with zero re-entered credentials and zero api_keys writes
- The tenant boundary holds under test, with the failure mode (neutered filter) demonstrated to redden
- Plan 162-06 has a pinned contract (request field + draft-arm envelope) to build the client on
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-05-SUMMARY.md` when done.
</output>
