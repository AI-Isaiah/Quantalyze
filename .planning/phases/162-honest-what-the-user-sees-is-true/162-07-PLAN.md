---
phase: 162-honest-what-the-user-sees-is-true
plan: 07
type: execute
wave: 2
depends_on: ["162-01"]
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.recency-line.test.tsx
autonomous: true
requirements: [HONEST-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "IF the census verdict is flat-account: the factsheet v2 masthead shows 'Track record through {date}' directly below the FreshnessChip's date line — exact copy per D-162-2, {date} through the factsheet's OWN formatter (the same one the chip's date line uses; two formatters on neighbouring lines is the drift the one-formatter rule exists to prevent)"
    - "The date derives from the resolved return series' last point — the D-03-verdict signal ('a column only a real analytics run can advance') — NEVER computed_at, NEVER last_sync_at"
    - "Unknown series-end date: the line does not render at all — no em-dash sentence, no placeholder (a claim with no date fails the print test; the chip's 'Computed · not yet' covers the no-analytics case)"
    - "The line introduces NO staleness threshold and never demotes or changes the chip's tone; FreshnessChip logic, thresholds, and anatomy are byte-untouched (D-162-2: additive; no fourth freshness ladder)"
    - "The line is colorless (text-caption text-text-muted, DM Sans, sentence case) — a neutral dated fact; tone stays on the chip"
    - "IF the census verdict is derive-gap: the line does NOT ship; the routing (161.1 dormant fan-out runbook — a founder LIVE op — or the ccxt stored>0 filter) is recorded in this plan's SUMMARY for the phase close"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetView.recency-line.test.tsx"
      provides: "recency-line spec incl. the identical-date-formatter assertion (C-1 Test row) — created only on the flat-account arm"
      contains: "Track record through"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "src/lib/factsheet/resolve-series.ts"
      via: "resolveDailyReturnSeries over the payload's series columns; the last point's date is the line's only data source"
      pattern: "resolveDailyReturnSeries"
---

<objective>
HONEST-02's fix half, gated on plan 162-01's verdict (D-162-2: the series-recency line
"Track record through {date}" applies ONLY if the investigation concludes the account is
genuinely flat rather than suffering a derive gap). UI-SPEC C-1 is the visual contract;
DESIGN.md governs (D-162-2 fence).

⚠ PLANNER ASSUMPTION (UI-SPEC UI-Considerations row 17, status: unresolved — carried
explicitly, not dropped): whether C-1 ships at all is UNKNOWN at plan time. Both arms are
specified below; the executor branches on the committed census verdict, and neither arm
is silent — the derive-gap arm records its routing as a finding. This plan does NOT
pre-judge the verdict.

Purpose: phase success criterion 2's factsheet half — a strategy whose series ended
89 days ago cannot read FRESH without the surface also stating the series' end.
Output: the C-1 line + spec (flat arm), or the recorded routing (derive-gap arm).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-UI-SPEC.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- Flat arm only: the recency line element in the FactsheetView masthead freshness block + `FactsheetView.recency-line.test.tsx`
- Either arm: the HONEST-02 disposition record in this plan's SUMMARY (verdict, arm taken, and — derive-gap arm — the routing note the phase close must file)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Branch on the census verdict and implement the selected arm</name>
  <precondition>162-CENSUS.md contains a filled "HONEST-02 VERDICT:" line (plan 162-01's artifact) — halt if the verdict slot is empty or missing</precondition>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>
    - `.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md` — the verdict line AND the A2 note (if the subject IS one of the 15 example rows, HONEST-02 partially collapses into D-162-1's recompute — say so in the summary and still follow the verdict arm)
    - `FactsheetView.tsx:843-877` — FreshnessChip and the two-row chip layout (the analog; the new line stacks below its date line, mt-1, right-aligned like its siblings)
    - The chip's date line formatter in the same file/format module — the recency line MUST call the same formatter (C-1 Test row: assert identity in a test, not prose)
    - `src/lib/factsheet/resolve-series.ts:50-57` — resolveDailyReturnSeries (the payload already carries the series columns; last point's date = the line's date)
    - 162-UI-SPEC §C-1 — every row of the contract table (copy, data source, placement, typography, color, unknown-date omission, no-threshold rule, a11y)
  </read_first>
  <behavior>
    Flat arm only:
    - Test F-1: a payload whose resolved series ends on a known date renders exactly "Track record through {formatted date}" below the chip's date line, muted caption tier
    - Test F-2 (formatter identity): the rendered recency date string is byte-equal to what the chip's own date formatter produces for the same date — two dates for one fact may never diverge
    - Test F-3: a payload with no resolvable series end date renders NO recency line (queried by its text — absent entirely, no placeholder)
    - Test F-4: FreshnessChip renders byte-identically with and without the line (tone, thresholds, anatomy untouched)
  </behavior>
  <action>
    Read the verdict.

    FLAT-ACCOUNT arm: implement C-1 exactly per the contract table — plain text in
    normal document flow inside the masthead's right-aligned freshness block, directly
    below the chip's date line, `mt-1`, `text-caption text-text-muted`, DM Sans sentence
    case (NOT a mono eyebrow — the fixed copy string stays in one voice), no dot, no
    semantic color, no ARIA (static content). Date from the resolved series' last point
    via resolveDailyReturnSeries; formatted by the chip's own date formatter — never a
    new inline format. Unknown date: render nothing. Do not touch the chip's logic,
    thresholds (3d/7d), or the known chip-vs-computeFreshness ladder inconsistency
    (out of scope by contract).

    DERIVE-GAP arm: make NO FactsheetView change. Record in the SUMMARY: the verdict,
    the evidence pointer, and the routing — ledger venue → the 161.1 dormant fan-out's
    go-live runbook (`docs/runbooks/ledger-refresh-go-live.md`, a founder LIVE op, NOT
    executable by this phase) or ccxt venue → the `stored > 0` recompute filter
    (`routers/cron.py:471-472`, recorded as TODOS 0.2, unfixed by design there). State
    explicitly that the freshness surface becomes honest when the pipeline verdict is
    executed and the series advances. Flag the routing for the phase-close TODOS filing
    (plan 162-08 owns TODOS.md this phase — hand it the line via the SUMMARY).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -E "HONEST-02 VERDICT: (flat-account|derive-gap)" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - The arm taken matches the committed verdict, stated in the summary with the census line quoted
    - Flat arm: every C-1 contract row satisfied and cited; chip byte-untouched (F-4 is the proof, not a diff eyeball)
    - Derive-gap arm: zero code change in this plan; routing recorded loudly, including the founder-op boundary (the runbook is not run by an agent)
  </acceptance_criteria>
  <done>The selected arm is implemented (or recorded), with the census verdict as the cited authority (D-162-2)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the line (flat arm) or close out the record (derive-gap arm)</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.recency-line.test.tsx</files>
  <read_first>
    - Task 1's implementation and the existing FactsheetView spec family (place the new spec beside them; if the family's fixtures already build masthead payloads, extend rather than duplicate)
  </read_first>
  <action>
    FLAT arm: write tests F-1..F-4. Witness F-3 RED by neutering the unknown-date
    omission (force the line to render with a placeholder), observe, restore from a byte
    copy (never `git checkout --`), shasum before/after identical. Witness F-2 RED by
    swapping the recency line to a different date format. The formatter-identity test
    must compare rendered strings from BOTH elements for the same fixture date — not the
    implementation's own formatter against itself (self-referential-oracle trap in
    162-VALIDATION).

    DERIVE-GAP arm: no spec file is created (record why in the summary — the artifact
    row above is conditional). Instead, verify the no-change claim mechanically:
    `git diff --stat` for this plan shows no `src/` modification, and the SUMMARY carries
    the routing block Task 1 wrote.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Flat arm: F-2 and F-3 each witnessed RED and restored byte-identically (shasum evidence); F-2 compares two rendered surfaces, not the formatter against itself
    - Derive-gap arm: the plan's diff contains no src/ change, and the SUMMARY hands plan 162-08 the TODOS line verbatim
    - Full-suite reminder honored at the wave gate
  </acceptance_criteria>
  <done>HONEST-02's fix half is either pinned in a falsifiable spec or honestly recorded as a routed pipeline finding</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics series data → public factsheet render | freshness claims to allocators are integrity claims |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-07-A | Tampering (false freshness claim) | recency line data source | medium | mitigate | date keyed ONLY on the series' own last point (the D-03 signal); computed_at/last_sync_at structurally excluded by the data-source rule + F-2/F-3 pins; no timestamp is advanced to fix a badge (recorded anti-pattern) |
| T-162-07-B | Repudiation (silent scope drop) | conditional C-1 scope | medium | mitigate | both arms specified; the derive-gap arm must produce a recorded routing, never silence (UI-Considerations row 17 carried as an explicit assumption) |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase |
</threat_model>

<verification>
- Factsheet spec directory green; tsc clean; full vitest at the wave gate
- The arm taken is traceable: census verdict line → summary statement → diff
- Flat arm: RED witnesses recorded with shasum-verified restores
</verification>

<success_criteria>
- Success criterion 2's factsheet half: the freshness surface either states the series' end date (flat) or the recorded pipeline routing explains how the series will advance (gap) — in neither case does a dead series silently read FRESH
- D-162-2 honored exactly: additive, threshold-free, chip untouched
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-07-SUMMARY.md` when done.
</output>
