---
phase: 162-honest-what-the-user-sees-is-true
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md
autonomous: true
requirements: [HONEST-01, HONEST-02]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "162-CENSUS.md records an explicit HONEST-02 verdict line — flat-account or derive-gap — with the evidence rows that decided it (success criterion 2 hard-orders this investigation BEFORE any fix is chosen)"
    - "162-CENSUS.md records the HONEST-01 root-cause verdict — site-identified <path:line>, site-gone, or inconclusive — derived from PROD compute_jobs history, not from static reading alone"
    - "162-CENSUS.md enumerates the exception-shaped computation_error repair population (strategy/portfolio ids only) and the current status of all 15 example rows"
    - statement: "Every number in the census came from read-only queries against the PROD database, never TEST (162-RESEARCH: census-dependent claims; measuring TEST would decide PROD behavior on the wrong population)"
      verification: backstop
  artifacts:
    - path: ".planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md"
      provides: "HONEST-02 flat-vs-gap verdict, HONEST-01 root-cause verdict, repair census, example-row re-census"
      contains: "HONEST-02 VERDICT"
  key_links:
    - from: ".planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md"
      to: "162-07-PLAN.md and 162-08-PLAN.md"
      via: "wave barrier + preconditions — the C-1 recency line and the recompute/repair ops branch on this file's verdict lines"
      pattern: "VERDICT"
  prohibitions:
    - statement: "The census carries counts, dates, and strategy/portfolio ids ONLY — no emails, no auth uids, no user names, no key material, no production hostnames or project refs (repo is PUBLIC, .planning/ is tracked)"
      status: unverified
      flagged: true
      verification: "grep of 162-CENSUS.md finds no email-shaped token and no column outside each query's own output list"
---

<objective>
Run the two investigations this phase's success criteria hard-order BEFORE their fixes:
(1) the HONEST-02 flat-account-vs-derive-gap question for the factsheet freshness subject
(D-162-2's fence gates the recency line on this verdict), and (2) the HONEST-01
str/None root cause (the specific compare site is LOW-confidence by design in
162-RESEARCH — a fix for it may not be planned until somebody has observed it).
Also take the two piggyback censuses later plans consume: the exception-shaped
`computation_error` repair population (HONEST-01) and the current status of the 15
example rows (feeds D-162-1's recompute in plan 162-08).

Purpose: plans 162-07 (D-162-2 recency line) and 162-08 (D-162-1 recompute + str/None
follow-through) branch on this artifact. Nothing in this plan mutates anything.
Output: `.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md` with real PROD results and explicit verdict lines.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-RESEARCH.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-CONTEXT.md
@.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md
</context>

<artifacts_this_phase_produces>
- `162-CENSUS.md` — the phase's decision artifact: `HONEST-02 VERDICT:` line, `HONEST-01 ROOT-CAUSE:` line, repair-population table, example-row status table. No code symbols.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold 162-CENSUS.md with verbatim read-only query blocks and the decision tables</name>
  <files>.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</files>
  <read_first>
    - `.planning/phases/159-rank-public-ranking-integrity/159-CENSUS.md` — the template: header rules (PROD-ref confirmation recorded THERE, not repeated here; no-PII rule; read-only rule), query-block + results-table structure
    - `162-RESEARCH.md` §HONEST-02 (investigation protocol, 4 steps) and §HONEST-01 (diagnostic protocol + repair census patterns)
    - `analytics-service/routers/cron.py:370-373` (the sync_trades RPC write — confirm the actual trades table name from the RPC definition before writing the query; assumption A5)
  </read_first>
  <action>
    Create the census scaffold following the 159-CENSUS.md structure exactly: the
    no-PII header ("counts, dates, and strategy ids ONLY; repo is PUBLIC"), the
    read-only rule ("every statement is a SELECT; nothing here mutates PROD"), and
    the PROD-vs-TEST confirmation step referencing the project-ref record already
    committed in 159-CENSUS.md (do NOT re-embed the ref in this file or this plan).

    Recover the two subject strategies from the pre-scope snapshot at execution time —
    `git show ca3f0c5c2:TODOS.md` around L1939 (HONEST-01 subject: the strategy whose
    `computation_error` holds the bare str/None TypeError text) and around L1953
    (HONEST-02 subject: the "Phoenix Protocol" case, window ended 2026-05-06). Use the
    ids in queries; paste ids only into result tables.

    Scaffold these query blocks (SQL, each a single SELECT, copied into the file verbatim):
    1. HONEST-02 step 1 — the subject's `api_keys.exchange` (venue decides which
       structural gap applies: ledger fan-out vs the ccxt `stored > 0` filter at
       `routers/cron.py:471-472`).
    2. HONEST-02 step 2 — series end: max date inside `strategy_analytics.returns_series`
       for the subject (same `max((e->>'date')::date)` semantics as the
       `ledger_refresh_staleness` view, migration 20260825120000).
    3. HONEST-02 step 3 — trades/ledger rows for the subject's key AFTER that end date
       (table name confirmed against the sync_trades RPC definition first).
    4. HONEST-02 step 4 — the subject's `compute_jobs` history (kind, status, created_at,
       last_error) since the series end date.
    5. HONEST-01 — the HONEST-01 subject's full `compute_jobs` history (kind, last_error,
       created_at), newest first.
    6. Repair census — `strategy_analytics` AND `portfolio_analytics` rows whose
       `computation_error` matches exception-shaped patterns: `not supported between instances`,
       `Traceback`, and a leading Python-type-name-plus-colon prefix. Output: id, status,
       a boolean per pattern — never the full error text (it may embed user-adjacent detail).
    7. Example-row re-census (assumption A3) — the 15 `is_example` published strategies:
       id, computation_status, computed_at, source/venue columns needed by plan 162-08
       to pick the recompute mechanism (e.g. whether the row is CSV-ingested or API-keyed).
       Include a column answering assumption A2: is the HONEST-02 subject itself one of these 15?

    Below the query blocks, scaffold the decision tables with empty result slots:
    - `HONEST-02 VERDICT:` — decision rule written into the file: data-after-end →
      derive-gap; no-data-after-end → flat-account. Note the recorded wrinkle: under the
      ccxt `stored > 0` filter a flat account CAUSES the derive gap — the verdict is
      decided on the evidence rows, not pre-judged (success criterion 2).
    - `HONEST-01 ROOT-CAUSE:` — one of `site-identified <path:line>` / `site-gone` /
      `inconclusive`, plus the evidence trail.
  </action>
  <verify>
    <automated>test -f .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md && grep -c "HONEST-02 VERDICT" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - Scaffold contains all 7 query blocks, each a pure SELECT, plus both verdict slots
    - The no-PII rule and read-only rule are stated in the header, per the 159 template
    - No email, uid, hostname, or project ref appears anywhere in the file (per D-162 hard constraints; strategy ids are the only identifiers)
    - The trades-table name is confirmed against the sync_trades RPC definition, not assumed (A5)
  </acceptance_criteria>
  <done>162-CENSUS.md scaffold committed with verbatim query blocks and empty verdict slots</done>
</task>

<task type="auto">
  <name>Task 2: Execute the census read-only against PROD and record both verdicts</name>
  <precondition>PROD read access is available: the primary checkout's untracked `.env` (resolve its path via `git worktree list` — the first entry is the primary checkout) carries the PROD Supabase URL + service key for read-only PostgREST GETs; if it does not, halt with a precondition-unmet checkpoint so the orchestrator runs the verbatim SQL blocks itself (read-only, PROD ref confirmed per the 159-CENSUS header record — MCP tools are stripped from subagents)</precondition>
  <files>.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</files>
  <read_first>
    - The Task-1 scaffold (the queries to run — nothing improvised beyond them)
    - `162-RESEARCH.md` §HONEST-01 step "The str/None root cause is NOT statically determinable" — the verdict procedure
  </read_first>
  <action>
    Execute exactly the scaffolded queries, read-only. Two lanes, in order of preference:
    (a) a throwaway node script in the scratchpad (never committed) doing PostgREST GET
    requests with the service key from the primary checkout's `.env` — JSON post-processing
    (e.g. max date inside `returns_series`) happens client-side in the script; (b) if
    credentials are unreachable, the precondition halt above. Never lane-mix with TEST;
    never issue anything but reads.

    Paste result tables under each query block (ids/counts/dates only). Then decide and
    write the two verdict lines:

    - `HONEST-02 VERDICT: flat-account | derive-gap` per the decision rule in the file.
      Also record the A2 answer (subject is / is not one of the 15 example rows — if it
      IS, say so explicitly: HONEST-02 then partially collapses into D-162-1's recompute
      and plan 162-07 must read that note).
    - `HONEST-01 ROOT-CAUSE:` — from the job history (kinds + last_error shapes + dates),
      identify which pipeline stage raised the str/None TypeError, then grep the
      analytics-service source AT HEAD for reachable mixed str/None comparison candidates
      in that stage (sorted()/min/max over date-keyed rows with nullable fields). If a
      live site is identified, record `site-identified <path:line>` with the reasoning.
      If the failing code predates rewrites and no reachable site exists at HEAD, record
      `site-gone` with the commit evidence. If the job history cannot disambiguate,
      record `inconclusive` — plan 162-08 then closes it as documented-plus-repair, which
      162-RESEARCH endorses. Sentry is orchestrator-only; if its evidence is required and
      unavailable, note that in the trail rather than guessing.

    Fill the repair-population table and the example-row status table (plan 162-08's
    inputs). Commit the completed census.
  </action>
  <verify>
    <automated>grep -E "HONEST-02 VERDICT: (flat-account|derive-gap)" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md && grep -E "HONEST-01 ROOT-CAUSE: (site-identified|site-gone|inconclusive)" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - Both verdict lines are filled with a decided value and an evidence trail (a verdict with no pasted evidence rows is not a verdict)
    - Repair-population and example-row tables carry real PROD rows (ids/status/dates only)
    - The A2 answer is recorded explicitly
    - No mutation was issued: the lane used is recorded in the file, and it is a read lane
  </acceptance_criteria>
  <done>162-CENSUS.md committed with PROD results, both verdicts decided, and the two downstream tables filled</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .planning/ → public GitHub | everything committed here is world-readable on push |
| agent → PROD database | read-only service-key access to production data |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-01-A | Information Disclosure | 162-CENSUS.md (public repo) | high | mitigate | no-PII rule enforced in both tasks' acceptance criteria: ids/counts/dates only; error TEXT never pasted (query 6 outputs pattern booleans, not the string); no hostnames/refs |
| T-162-01-B | Tampering | PROD data via census queries | medium | mitigate | read-only contract: every scaffolded statement is a SELECT; lane recorded in the artifact; precondition halt instead of improvised access |
| T-162-SC | Tampering | package installs | low | accept | this phase installs no packages (162-RESEARCH Package Legitimacy Audit: empty); no install task exists |
</threat_model>

<verification>
- `162-CENSUS.md` exists, committed, with both verdict lines matching the automated greps
- A no-PII sweep of the artifact comes back clean
- No file outside `.planning/` was modified by this plan
</verification>

<success_criteria>
- The HONEST-02 investigation is COMPLETE before any HONEST-02 fix exists anywhere in the phase (success criterion 2's hard order)
- The HONEST-01 root-cause question has a decided, evidence-backed answer that plan 162-08 can branch on
- Plans 162-07 and 162-08 can execute without re-querying PROD for anything this census already answers
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-01-SUMMARY.md` when done.
</output>
