---
phase: 162-honest-what-the-user-sees-is-true
plan: 08
type: execute
wave: 2
depends_on: ["162-01", "162-02", "162-03"]
files_modified:
  - .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md
  - TODOS.md
autonomous: true
requirements: [HONEST-03, HONEST-01]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The 15 example rows reach terminal success via REAL recompute (fresh compute_jobs through the correct per-source mechanism), or are unpublished with the finding said out loud — NEVER synthesized values to make a row look computed (D-162-1 fence, hard constraint)"
    - "Recompute verification reads strategy_analytics.computed_at + computation_status transitions to terminal success — never last_sync_at (recorded project constraint: last_sync_at lies)"
    - "The HONEST-01 str/None verdict from 162-CENSUS.md has a follow-through: a RED-witnessed compare fix (site-identified), or a documented closure plus row repair (site-gone / inconclusive) — success criterion 1's root-cause clause is not left dangling"
    - "Exception-shaped computation_error rows from the repair census are re-enqueued where a mechanism exists, or explicitly recorded as awaiting their next terminal write (the curated writers from plan 162-02 stop NEW leaks; existing rows keep the old text until repaired)"
    - statement: "Live discovery renders no Synced badge on example rows and no stale badge anywhere"
      verification: backstop
  artifacts:
    - path: ".planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md"
      provides: "appended §Recompute results: per-row before/after status table + the str/None follow-through record"
      contains: "Recompute results"
  key_links:
    - from: "162-CENSUS.md example-row table (plan 162-01)"
      to: "PROD compute_jobs"
      via: "per-source enqueue mechanism discovered from the repo's own enqueue RPCs / job kinds — never an invented write shape"
      pattern: "compute_jobs"
---

<objective>
D-162-1 (founder-decided): recompute the 15 example rows to terminal success — the rows
have been `failed` since 2026-05-27; #712 made discovery honest-but-badge-free, and the
three-month failure is the bug. Unpublishing hides it; synthesizing values is forbidden.
Fence: if the rows cannot be recomputed, that IS the finding — fall back to unpublishing
and say so. Plus the two follow-throughs that branch on plan 162-01's census: the
HONEST-01 str/None verdict, and the exception-shaped `computation_error` repair rows.

Do NOT re-plan HONEST-03's badge fix — #712 (STALE-01) closed it across eleven surfaces
and plan 162-03 shipped the `is_example` class guard. This plan is the DATA half.

Purpose: phase success criterion 2's example-row half + criterion 1's root-cause clause.
Output: recomputed (or decidedly unpublished) example rows, the follow-through records,
and the TODOS filings the other plans handed off.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-CONTEXT.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-PATTERNS.md
</context>

<artifacts_this_phase_produces>
- 162-CENSUS.md §Recompute results — per-row before/after table (ids/status/dates only)
- The str/None follow-through: either a compare-site fix + regression test in analytics-service (conditional), or the documented closure record
- TODOS.md filings: any derive-gap routing handed off by plan 162-07's SUMMARY, plus any rows left awaiting repair
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Recompute the 15 example rows through their real per-source mechanism</name>
  <precondition>162-CENSUS.md carries the example-row table with per-row source/venue (plan 162-01), and PROD access is available via the primary checkout's untracked `.env` (resolve via `git worktree list`); if the write lane is unreachable, halt with a precondition-unmet checkpoint rather than improvising — enqueue writes are the one PROD mutation this phase makes</precondition>
  <files>.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</files>
  <read_first>
    - 162-CENSUS.md example-row table — per-row source (CSV-ingested vs API-keyed) decides the mechanism
    - The repo's enqueue surfaces AT HEAD before choosing one: grep `supabase/migrations` for the enqueue RPCs (e.g. the `_enqueue_compute_job_internal` family) and `analytics-service` for the job kinds each source's analytics run under — the mechanism must be one the worker actually consumes, never an invented compute_jobs INSERT shape
    - Memory constraint (162-PATTERNS §Example-row repair): ledger venues enqueue process_key_long ONLY at creation — a re-enqueue of that kind is provably a no-op; strategy-mode chain-tail kinds are the refresh route where applicable
    - ⛔ NEVER start a local analytics-service — `uvicorn main:app` claims REAL production compute jobs; the Railway worker consumes what you enqueue
  </read_first>
  <action>
    Per D-162-1: for each of the 15 rows, determine the correct recompute mechanism from
    the census source column + the repo's own enqueue definitions, enqueue a fresh
    compute job through it (service-key RPC call or the documented enqueue function —
    the write lane mirrors the census read lane), and record job ids/kinds in the census
    appendix. Then poll `strategy_analytics` (status + computed_at transitions) on a
    bounded schedule — e.g. every few minutes up to ~45 minutes total — and record the
    per-row outcome table: before-status → after-status, computed_at delta. Never judge
    by last_sync_at.

    FENCE (pre-authorized by D-162-1, execute without re-asking): any row with NO viable
    recompute mechanism for its source, or that fails to reach terminal success within
    the window after one retry, falls back to UNPUBLISH — update that row's strategy
    status through the same service-role lane, record it in the census table with the
    reason, and say so prominently in the SUMMARY. Do NOT write analytics values
    directly under any circumstances; a row that cannot be computed is unpublished, not
    decorated.

    Post-fix note for the record: rows that fail AFTER the phase's PR deploys will carry
    plan 162-02's curated copy; rows failing before deploy re-write the old raw shape —
    expected, noted, not a regression.
  </action>
  <verify>
    <automated>grep -c "Recompute results" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 15 rows lands in exactly one recorded terminal bucket: recomputed-to-success, retried-then-unpublished, or no-mechanism-then-unpublished — no row silently left `failed`-and-published
    - The enqueue mechanism used per source is named with its repo definition cited (no invented write shapes)
    - Verification cites computed_at + status transitions; last_sync_at appears nowhere in the evidence
    - All writes were enqueues or the pre-authorized unpublish — no direct analytics-value write occurred (state this explicitly)
  </acceptance_criteria>
  <done>The 15 example rows are in a decided, honest state on PROD, recorded per-row in the census</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HONEST-01 follow-throughs — str/None verdict + repair-census rows</name>
  <precondition>162-CENSUS.md carries a filled "HONEST-01 ROOT-CAUSE:" line and the repair-population table (plan 162-01)</precondition>
  <files>.planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</files>
  <read_first>
    - The census root-cause verdict + evidence trail
    - If site-identified: the named file/line at HEAD, plus the surrounding pipeline stage — and plan 162-02's summary (the same files may have moved under the wave-1 curation edits)
  </read_first>
  <behavior>
    Site-identified arm only:
    - Test S-1: a regression test reproducing the mixed str/None input at the identified site passes with the fix and was witnessed RED without it (neuter the specific guard line, observe, restore byte-identically — never git checkout)
  </behavior>
  <action>
    Branch on the verdict:
    - `site-identified <path:line>`: fix the compare (handle the nullable field
      explicitly — a deterministic key function or None-guard, matching the module's
      existing idioms), add the S-1 regression test beside the module's suite, run
      pytest from `analytics-service/` with python3, and mypy --strict the touched file.
      Note the fixed file in this plan's SUMMARY frontmatter/files list since it extends
      files_modified.
    - `site-gone` or `inconclusive`: write the documented closure into the census (what
      the evidence showed, why no code fix is planned — 162-RESEARCH endorses
      document-plus-repair for exactly this outcome), and ensure the affected row itself
      is repaired: enqueue its recompute via the Task-1 mechanism (it may already be in
      the repair census set).

    Repair-census rows: for each exception-shaped `computation_error` row, enqueue a
    recompute where Task 1's mechanism applies; rows without a mechanism are recorded as
    awaiting their next terminal write (which post-deploy carries curated copy). Append
    the disposition per row to the census.
  </action>
  <verify>
    <automated>grep -E "HONEST-01 ROOT-CAUSE: (site-identified|site-gone|inconclusive)" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md && grep -c "str/None follow-through" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - The follow-through record exists under the heading "str/None follow-through" and matches the verdict arm
    - Site-identified arm: S-1 witnessed RED, restored byte-identically, pytest + mypy --strict clean from analytics-service/
    - Every repair-census row has a recorded disposition (re-enqueued / awaiting-next-write) — none silently dropped
  </acceptance_criteria>
  <done>Success criterion 1's root-cause clause is closed on evidence, and the legacy raw-text rows have a decided repair path</done>
</task>

<task type="auto">
  <name>Task 3: Seam observation + TODOS filings + phase-close handoffs</name>
  <files>TODOS.md, .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</files>
  <read_first>
    - `162-07-SUMMARY.md` — a derive-gap routing line to file, if that arm was taken
    - `src/components/strategy/StrategyTable.stale-analytics.test.tsx` at HEAD — plan 162-03's guard pins (the seam evidence)
    - Memory rule: backlog has ONE ground truth — root TODOS.md; add items ONLY there
  </read_first>
  <action>
    Seam observation for the backstop truth: run the stale-analytics spec suite at HEAD
    (it now pins that is_example rows render no SyncBadge in any status) and record the
    run as the code-half evidence; pair it with the Task-1 census outcome (the data
    half: rows terminal-success or unpublished). Record both in the census under a
    "Discovery observation" heading, and note that the PROD render flips when the phase
    PR deploys — flag the post-deploy look as a `<human-check>` item below.

    TODOS filings (one edit, all lines): the derive-gap routing from 162-07's summary
    (if any), any repair-census rows left awaiting-next-write, and any unpublished
    example rows (so the founder sees discovery thinned and why). Cite the census in
    each line.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.stale-analytics.test.tsx && grep -c "Discovery observation" .planning/phases/162-honest-what-the-user-sees-is-true/162-CENSUS.md</automated>
    <human-check>After the phase PR deploys: open the public discovery page and confirm (1) no example row shows a Synced badge in any state, (2) recomputed rows show real KPI values with rank/percentile per the computed-cohort gates, (3) no stale "Synced Nd ago" claim anywhere on the table</human-check>
  </verify>
  <acceptance_criteria>
    - The backstop truth has explicit recorded evidence (spec run + census outcome) — an unevidenced backstop routes to human_needed at verify time, never a silent pass
    - Every handoff line landed in TODOS.md (or the census records that none existed)
    - The post-deploy PROD observation is queued as a human check, not claimed early
  </acceptance_criteria>
  <done>The phase's data repair is observed at the seam, filed in the one backlog, and queued for its post-deploy PROD look</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent → PROD database (write lane) | enqueue + unpublish writes through service-role — the phase's only PROD mutations |
| .planning/ + TODOS.md → public GitHub | records are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-08-A | Tampering (fabricated analytics) | example-row repair | high | mitigate | D-162-1 fence enforced in acceptance: writes are limited to real compute enqueues + the pre-authorized unpublish; direct analytics-value writes are prohibited and their absence explicitly attested |
| T-162-08-B | Tampering (wrong-mechanism enqueue corrupts state) | enqueue lane | medium | mitigate | mechanism chosen from the repo's own enqueue RPC definitions per source; ledger no-op re-enqueue trap named in read_first; job ids recorded for rollback tracing |
| T-162-08-C | Information Disclosure | census/TODOS records | medium | mitigate | ids/counts/dates only; error text never pasted |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase |
</threat_model>

<verification>
- Census carries §Recompute results, the str/None follow-through, and §Discovery observation
- All 15 rows in a decided bucket; repair rows dispositioned; TODOS filings landed
- Site-identified arm (if taken): pytest from analytics-service/ + mypy --strict clean, S-1 RED-witnessed
- Post-deploy PROD look queued as a human check
</verification>

<success_criteria>
- Success criterion 2's example-row half: discovery advertises nothing stale — rows are honestly computed, or honestly gone, and the badge class is sealed by 162-03's guard
- Success criterion 1's root-cause clause: closed on evidence, whichever arm the census chose
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-08-SUMMARY.md` when done.
</output>
