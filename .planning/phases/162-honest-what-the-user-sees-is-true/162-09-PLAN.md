---
phase: 162-honest-what-the-user-sees-is-true
plan: 09
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/connect/KeyPermissionBadge.tsx
  - src/components/connect/KeyPermissionBadge.test.tsx
autonomous: true
requirements: [HONEST-02]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "When perms.probe_error is set, NO scope is presented as detected: the Read/Trade/Withdraw chips render as unknown or are omitted — never as granted/denied fact (the chips are the load-bearing false claim, not the caption)"
    - "When perms.probe_error is set, the 'Detected {time} from the exchange' caption is not presented as fact — omitted or restated as unknown alongside the summary's existing honest message"
    - "A screen can never simultaneously say 'Could not contact the exchange to verify scopes' and 'Trade ✓ Withdraw ✓ — Detected just now from the exchange' — the observed PROD contradiction (2026-08-25 QA) has no render path"
    - "The unknown presentation reuses the phase's missing-must-look-missing vocabulary (UI-SPEC C-3/C-4: colorless absence, em-dash/unknown, no red — absence is not an error), not a parallel invention"
    - "The probe-error summary sentence at the top of the panel is byte-unchanged (it was already honest)"
  artifacts:
    - path: "src/components/connect/KeyPermissionBadge.test.tsx"
      provides: "regression pin: probe_error true + non-null read/trade/withdraw values → no scope presented as detected, chips and caption asserted SPECIFICALLY (the :266 summary-text assertion cannot stand in for this)"
      contains: "probe_error"
  key_links:
    - from: "src/components/connect/KeyPermissionBadge.tsx"
      to: "the probe result (perms.probe_error)"
      via: "the chips and footer caption gate on the SAME probe_error branch the plain-English summary already gates on — one fact, one gate, decided once"
      pattern: "probe_error"
---

<objective>
Coordinator-added in-scope finding (live PROD QA 2026-08-25T22:52Z, post-artifacts — not
in CONTEXT/RESEARCH/PATTERNS/UI-SPEC): the key-scope panel presents scopes it just said
it could not read. `KeyPermissionBadge.tsx` gates its plain-English summary on
`perms.probe_error` (:218, correctly) but renders the three scope chips (:251-253) and
the "Detected {time} from the exchange" caption (:255-260) UNCONDITIONALLY whenever
`perms` is present. Observed on one screen simultaneously: "Could not contact the
exchange to verify scopes." above "Read ✓ Trade ✓ Withdraw ✓ — Detected just now from
the exchange."

The severity is in the DIRECTION of the falsehood: the chips asserted Trade ✓ and
Withdraw ✓ directly beneath the form copy "Only read-only keys are accepted." A user who
believes the chips concludes their read-only key can withdraw funds; a user who believes
the copy concludes the app is broken. No reading leaves the user correctly informed.

This is exactly this phase's class — a rendered claim the data underneath contradicts —
structurally identical to HONEST-02's freshness problem one surface over; it is attached
to HONEST-02 as the nearest requirement ID and covered by the phase goal verbatim
("no freshness claim a dead series contradicts" → no scope claim a failed probe
contradicts). Scope enforcement itself is NOT in question (read-only IS enforced
server-side); the defect is the badge.

Purpose: the phase goal's rendered-claims class, closed on this surface.
Output: probe_error-gated chips + caption, with a RED-witnessed chip-specific pin.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-UI-SPEC.md
@src/components/connect/KeyPermissionBadge.tsx
</context>

<artifacts_this_phase_produces>
- probe_error-gated unknown state for the scope chips + footer caption in `KeyPermissionBadge.tsx`
- Chip-specific regression tests in `KeyPermissionBadge.test.tsx`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate the scope chips and the Detected caption on probe_error</name>
  <files>src/components/connect/KeyPermissionBadge.tsx, src/components/connect/KeyPermissionBadge.test.tsx</files>
  <read_first>
    - `KeyPermissionBadge.tsx:218` — the summary's existing (correct) probe_error branch: the gate to extend, not a new predicate
    - `KeyPermissionBadge.tsx:251-260` — the ungated chips + the ungated "Detected … from the exchange" caption
    - `KeyPermissionBadge.test.tsx:266` — the existing probe-error SUMMARY assertion. ⚠️ A naive new test could pass on this existing behaviour without touching the chips at all — the new assertions must target the CHIPS and the CAPTION specifically
    - 162-UI-SPEC §C-3/C-4 — the missing-must-look-missing vocabulary to reuse (colorless absence, unknown ≠ fact, no red on absence); DESIGN.md Numbers Contract (em-dash for a value that cannot be claimed; a `—` never carries semantic color)
  </read_first>
  <behavior>
    - Test K-1 (the pin): render with `probe_error` set AND non-null read/trade/withdraw values in `perms` — assert NO scope is presented as detected: no chip renders a granted (✓) or denied state, and the "Detected … from the exchange" caption is absent or restated as unknown. Assertions target the chip elements and the caption text, never the summary sentence
    - Test K-2: with a successful probe (no probe_error), chips and caption render exactly as today (byte-equivalent behavior — the gate is error-scoped, not a chip deletion)
    - Test K-3: the probe-error summary sentence still renders unchanged alongside the gated state (one honest message, no contradiction)
  </behavior>
  <action>
    Extend the SAME `probe_error` branch the summary already uses (one fact, one gate —
    the STALE-01 discipline: decided once) so that when the probe failed: the three
    scope chips render an unknown/indeterminate presentation or are omitted (whichever
    the existing Pill primitive supports cleanly — colorless, muted, no ✓, no ✗, no red:
    absence is not an error), and the footer caption is not presented as fact (omit it,
    or state the unknown per the C-3/C-4 voice: the limitation, plainly). Do NOT
    suppress only the caption — the chips are the load-bearing false claim. Do not touch
    the summary branch, the successful-probe render, or scope ENFORCEMENT anywhere
    (server-side enforcement is correct; this is the badge).

    Write K-1..K-3 first. Witness K-1 RED against its own neutered fix: neuter the new
    guard, RUN the test, OBSERVE the failure first-hand, restore byte-identically —
    `shasum -a 256` before/after; never `git checkout -- <path>` (it resets to HEAD and
    destroys uncommitted work).
  </action>
  <verify>
    <automated>npx vitest run src/components/connect/KeyPermissionBadge.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - K-1 asserts on the chips and the caption specifically (cite the assertion lines in the summary) — the existing :266 summary assertion is not the evidence
    - K-1 witnessed RED against the neutered guard, restored byte-identically (shasum evidence in summary)
    - Successful-probe rendering unchanged (K-2); summary sentence unchanged (K-3)
    - The unknown state is colorless per the C-3/C-4 vocabulary — no semantic color, no red, no new copy voice
  </acceptance_criteria>
  <done>A failed probe can no longer render scope facts; the observed PROD contradiction has no render path</done>
</task>

<task type="auto">
  <name>Task 2: Close the class — sweep sibling consumers of probe results</name>
  <files>src/components/connect/KeyPermissionBadge.test.tsx</files>
  <read_first>
    - The perms payload type consumed by KeyPermissionBadge (its import — find every other consumer of the same shape)
    - Project rule: close the WHOLE class across the surface, not the point instance (fix-campaign discipline)
  </read_first>
  <action>
    Grep for every other render site consuming the same permissions payload (the
    key-permissions cascade surfaces WIZERR-04 touched are candidates) and check each
    for the identical shape: scope facts or detected-at claims rendered without gating
    on the probe's own error field. For each hit: if it is the same component reused,
    the Task-1 fix already covers it — record the consumer list; if it is an independent
    ungated render, apply the identical gate + a chip-specific pin in that surface's
    spec (extend this plan's files_modified via the summary). If the sweep finds
    nothing, record the clean sweep with the grep evidence — a negative result stated
    loudly, not silence. ⚠️ Sweep with `grep -a`/node where wizard error surfaces are
    involved (the NUL-byte file trap).
  </action>
  <verify>
    <automated>npx vitest run src/components/connect/ && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The consumer sweep is recorded in the summary: every render site of the perms shape listed with its gating verdict
    - Any independent ungated instance found is fixed with the same one-gate discipline and its own pin, or explicitly filed — never silently left
  </acceptance_criteria>
  <done>The probe-error class is closed across every surface that renders probe results, with the sweep on record</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| probe result → key-connect UI | scope claims steer users handling exchange API keys with real fund permissions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-09-A | Tampering (integrity of displayed capability claims) | ungated scope chips under probe_error | high | mitigate | chips + caption gated on the summary's own probe_error branch; K-1 pins that non-null scope values under probe_error never render as fact (the direction of the falsehood — Trade ✓/Withdraw ✓ under "only read-only keys accepted" — is the severity, per the QA finding) |
| T-162-09-B | Spoofing (false freshness of detection) | "Detected just now from the exchange" caption | medium | mitigate | caption not presented as fact under probe_error — same claim-vs-data contract as HONEST-02, one surface over |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase |
</threat_model>

<verification>
- KeyPermissionBadge spec green file-scoped; full vitest at the wave gate (contract tests scan all of src/)
- K-1 RED witness recorded with shasum-verified byte-identical restore
- Consumer sweep recorded with grep evidence (clean or fixed, never silent)
</verification>

<success_criteria>
- The phase goal holds on this surface: no scope claim a failed probe contradicts can render, in either direction of the falsehood
- Scope ENFORCEMENT is untouched — the fix is presentation-layer only, as the finding specifies
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-09-SUMMARY.md` when done.
</output>
