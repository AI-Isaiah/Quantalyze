---
phase: 162-honest-what-the-user-sees-is-true
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/[id]/returns/route.ts
  - src/app/api/strategies/[id]/returns/route.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.added-metrics.test.tsx
autonomous: true
requirements: [HONEST-05]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Drawer-added legs render real CAGR/Sharpe once the lazy fetch settles — same row, same RLS, no new round-trip (the recorded WR-02 fix shape)"
    - "The widened /returns route withholds cagr/sharpe under the SAME isRankableAnalyticsRow predicate as the series — a failed row's scalars never leak through the new door (162-CONTEXT standing constraint)"
    - "In-flight fetch renders em-dash cells with NO absence note — the note is a settled claim and must not flash during load (undefined ≠ settled-null, UI-SPEC C-4)"
    - "Settled with BOTH null renders em-dashes plus the REVISED note: 'No computed metrics for this strategy — open the factsheet for detail.' (the old note's permanent-unreachability claim becomes false this phase)"
    - "Exactly one null renders an em-dash beside its live sibling with NO note (Phase-152 rule preserved)"
    - "A fetch error is treated as settled-absent: em-dash + note, never zeros, never stale neighbors, no red (absence is not an error)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.added-metrics.test.tsx"
      provides: "C-4 state-matrix spec: in-flight / settled-present / settled-both-null / one-null / fetch-error"
      contains: "addedMetricsById"
  key_links:
    - from: "src/app/api/strategies/[id]/returns/route.ts"
      to: "src/lib/closed-sets.ts"
      via: "isRankableAnalyticsRow decides scalar withholding exactly where deriveEmptySeriesState decides series withholding"
      pattern: "isRankableAnalyticsRow"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/app/api/strategies/[id]/returns/route.ts"
      via: "fetchAddedReturns settles addedMetricsById from the widened payload; purged in handleRemoveAdded"
      pattern: "addedMetricsById"
---

<objective>
HONEST-05: drawer-added strategies render CAGR/Sharpe like book rows. The fix shape was
recorded (WR-02) and re-verified at HEAD: widen `/api/strategies/[id]/returns` to
co-serve `cagr, sharpe` from the same row, and add an `addedMetricsById` lazy fallback
mirroring `addedProvenanceById`. The route was one of STALE-01's nine closed surfaces —
the co-served scalars MUST be withheld under the same predicate or this plan re-leaks
dead KPIs through a fresh door days after the 11-surface hotfix.

Purpose: phase success criterion 3's drawer half (a different surface from the
portfolio-page curves — do not conflate).
Output: widened gated route + composer fallback + the C-4 five-state contract, pinned.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/162-honest-what-the-user-sees-is-true/162-PATTERNS.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-UI-SPEC.md
@.planning/phases/162-honest-what-the-user-sees-is-true/162-VALIDATION.md
</context>

<artifacts_this_phase_produces>
- `/api/strategies/[id]/returns` payload gains `cagr: number|null, sharpe: number|null` (null unless the row is rankable)
- `addedMetricsById` state in ScenarioComposer (`Record<string, { cagr: number|null; sharpe: number|null }>`), settled by `fetchAddedReturns`, purged in `handleRemoveAdded`
- Revised C-4 absence-note copy
- `ScenarioComposer.added-metrics.test.tsx` — new spec (or extension of the file's existing spec family if one covers the detail panel; state which in the summary)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen /returns to co-serve gated cagr/sharpe</name>
  <files>src/app/api/strategies/[id]/returns/route.ts, src/app/api/strategies/[id]/returns/route.test.ts</files>
  <read_first>
    - `route.ts:243-262` — the select to widen AND the block comment above it (Phase 147 widened this same select and documented the RLS reasoning: analytics_read RLS is table-level, no column grants — the same sentence covers the scalar columns)
    - `route.ts:264-276` — the F5b error redaction (log + Sentry + static envelope; never a raw Postgres error) — mirror, do not weaken
    - `route.ts:322` and `:369` — the deriveEmptySeriesState routing for failed rows: the scalars gate sits exactly where the series gate sits
    - `route.test.ts` — the R4/R4b/R10 stale-analytics fixtures to extend (STALE-01 part 2 closed this route; commit dc7b17d3c's message names them)
    - 162-PATTERNS §STALE-01 gate (predicate verbatim + SI-01: call the predicate, never inline status literals)
  </read_first>
  <behavior>
    - Test R-A: terminal-success row → payload carries the row's cagr and sharpe
    - Test R-B: failed row whose analytics carry best-in-class stale cagr/sharpe values → payload carries null for both (the corpse is present in the fixture and still withheld)
    - Test R-C: response shape for existing consumers unchanged (series fields, computation_status, data_quality_flags byte-compatible)
  </behavior>
  <action>
    Add `cagr, sharpe` to the select; in the response assembly, emit them as null unless
    `isRankableAnalyticsRow({ computation_status: status })` holds — the same decision
    point that routes failed rows into `deriveEmptySeriesState`, decided once, not a
    second ladder. Extend the R4/R4b/R10 fixture family with tests R-A/R-B/R-C. Witness
    R-B RED by neutering the scalar gate (byte-copy restore, shasum before/after). Update
    the Phase-147 block comment to name the scalar columns in the same RLS sentence
    rather than adding a second explanation.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The gate is SHOWN applied at the value site (cite the route line in the summary), via the predicate call — no inline status-literal comparison (SI-01 census)
    - R-B's fixture holds real stale scalar values and the test was witnessed RED against the neutered gate
    - F5b redaction untouched; no new error copy minted
  </acceptance_criteria>
  <done>The route co-serves scalars for rankable rows only; the stale-analytics spec family pins it</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: addedMetricsById fallback + the C-4 five-state render contract</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.added-metrics.test.tsx</files>
  <read_first>
    - `ScenarioComposer.tsx:1134-1146` — addedProvenanceById: the template (declaration comment, settle writer, purge in handleRemoveAdded, presentation-only / never into the frozen engine)
    - `ScenarioComposer.tsx:2528-2532` — the consumption site: the metric pair sourced from the book-only payload, and the adjacent asset_class line showing the exact fallback shape to copy
    - 162-UI-SPEC §C-4 — the five states verbatim (in-flight, settled-present, settled-both-null + revised note, one-null, fetch-error)
    - DESIGN.md Numbers Contract (em-dash rule, .font-metric, formatPercent/formatNumber via the surface's formatter module — never inline toFixed)
    - TODOS/162-RESEARCH note on 159-BASIS-FLIP: out of scope; follow the purge-on-remove discipline so the new settle does not worsen it
  </read_first>
  <behavior>
    - Test C-A (in-flight): addedMetricsById[id] undefined → both cells em-dash, NO absence note in the panel
    - Test C-B (settled-present): real values render with existing formatting (CAGR signed 1dp, Sharpe 2dp), neutral ink
    - Test C-C (settled-both-null): em-dashes + exactly the revised note "No computed metrics for this strategy — open the factsheet for detail."; the old note string no longer renders
    - Test C-D (one-null): em-dash beside the live sibling, no note
    - Test C-E (fetch-error): treated as settled-both-null; no red, no zeros
    - Test C-F (purge): removing the added leg then re-adding starts clean (no stale settled entry)
  </behavior>
  <action>
    Declare `addedMetricsById` beside `addedProvenanceById`, copying its lifecycle
    exactly: settle from the widened `/returns` payload inside the same fetch that
    settles provenance, purge in `handleRemoveAdded`, presentation-only (never threaded
    into the frozen engine). Change the consumption site to the fallback chain the
    adjacent asset_class line already uses (`book value ?? addedMetricsById[id]?.x ?? null`).
    Preserve the undefined-vs-null distinction end-to-end: an unsettled fetch leaves the
    id ABSENT from the map; a settled fetch writes nulls explicitly; a fetch error writes
    settled nulls. Revise the absence-note copy per C-4 (same slot, same `text-xs
    text-text-muted`, single-note discipline; the factsheet link stays the panel's only
    accent element) and delete/correct the adjacent comment claiming permanent
    unreachability. Write tests C-A..C-F; witness C-C RED by neutering the fallback chain
    (byte-copy restore).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.added-metrics.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The five C-4 states each have a passing test; C-C was witnessed RED against the neutered fix
    - Purge-on-remove proven (C-F) — the settle family does not worsen 159-BASIS-FLIP (no new state survives leg removal)
    - Formatting goes through the surface's existing formatter module; no inline toFixed; em-dash cells colorless
    - The old note string has no remaining render path (swept with grep over the component)
  </acceptance_criteria>
  <done>Drawer-added legs render metrics like book rows, and every absence state says exactly what is true</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed API route → composer client | widened payload crosses to the browser; RLS is table-level (published OR owner) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-162-04-A | Tampering (dead-metrics resurrection) | widened /returns scalars | high | mitigate | isRankableAnalyticsRow withholding at the same decision point as the series; R-B fixture carries the corpse and pins the null |
| T-162-04-B | Information Disclosure | /returns error path | medium | mitigate | F5b redaction preserved: log + Sentry + static envelope; asserted untouched in Task 1 acceptance |
| T-162-04-C | Information Disclosure | scalar columns over RLS | low | accept | table-level analytics_read RLS already governs the row (published OR owner) — Phase 147's documented reasoning covers the new columns; no column grants exist to widen |
| T-162-SC | Tampering | package installs | low | accept | no packages installed this phase |
</threat_model>

<verification>
- Both spec files green file-scoped; full vitest at the wave gate (contract tests scan all of src/)
- `npx tsc --noEmit` clean
- RED witnesses recorded with shasum-verified restores
</verification>

<success_criteria>
- Success criterion 3's drawer half: a drawer-added leg is indistinguishable from a book row where data exists, and honestly em-dashed where it does not
- The STALE-01 class did not re-open through the widened route (R-B is the proof)
</success_criteria>

<output>
Create `.planning/phases/162-honest-what-the-user-sees-is-true/162-04-SUMMARY.md` when done.
</output>
