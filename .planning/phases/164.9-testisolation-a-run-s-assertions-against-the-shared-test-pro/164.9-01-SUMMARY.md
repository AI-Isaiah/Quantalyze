---
phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro
plan: 01
subsystem: planning-backlog
tags: [todos, claude-md, backlog-hygiene, fanout-global-01, advisory-key-census]

# Dependency graph
requires: []
provides:
  - "FANOUT-GLOBAL-01 as a real TODOS.md entry with owner, date, trigger and five family members"
  - "[164.9-SHARED-TEST-TRANSPORT-FLAKE], [164.9-MUTEX-HOLDER-DIED-UNSERIALIZED], [164.9-CREDENTIALED-TESTS-RED-AND-UNGATED] as booked TODOS.md entries carrying the ROADMAP evidence"
  - "Corrected advisory-key occurrence figure (7/27, dated 2026-09-21) consistent across CLAUDE.md and all three TODOS.md restatements"
affects: [164.9-02, 164.9-03, 164.9-04, 164.9-05, 164.9-06, 164.9-07, 164.9-08, 164.9-09, 164.9-10, 164.9-11]

# Actuals (#2632)
actuals:
  tokens: 4620
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Dated ⛔ CORRECTED / retro-link callouts appended in place, preserving the superseded sentence as lineage per this repo's own convention"
    - "Diff-scoped credential/host/path scan (git diff HEAD, not whole-file) so pre-existing credential-shaped fixture strings in TODOS.md don't self-invalidate a task's own verify"

key-files:
  created: []
  modified:
    - TODOS.md
    - CLAUDE.md

key-decisions:
  - "FANOUT-GLOBAL-01 name is KEPT, not retired — it is cited by name in CLAUDE.md, the ROADMAP Phase 164.9 entry, and multiple TODOS.md entries as a family label."
  - "Corrected the stale 26x ci.yml advisory-key figure everywhere it was restated (CLAUDE.md plus all three TODOS.md copies), not only at the one location the plan's automated verify targeted — one fact restated in more places than the plan anticipated, all wrong the same way, all fixed in the same pass."
  - "The [164.8-TEST-DATA-RESEEDED] routing discrepancy was recorded via a dated note, not resolved — its destination and disposition are unchanged, per the plan's explicit prohibition."

requirements-completed:
  - FANOUT-GLOBAL-01
  - "[164.9-SHARED-TEST-TRANSPORT-FLAKE]"
  - "[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]"
  - "[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"

coverage:
  - id: D1
    description: "FANOUT-GLOBAL-01 written as a real TODOS.md family entry (owner, date, trigger, five family members) and its three prior prose citations retro-linked to it"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: other
        ref: "Task 1 <verify> automated block (header count=1, total hits>=4, owner line present, diff-scoped credential scan=0) — re-run and confirmed passing"
        status: pass
    human_judgment: false
  - id: D2
    description: "The three 164.9-* ids each get a real TODOS.md entry carrying the ROADMAP criteria 11/12/13 evidence, an owner, a deliverable and a forbidden-closure clause"
    requirement: null
    verification:
      - kind: other
        ref: "Task 2 <verify> automated block (each id header count=1, section heading count=1, owner lines>=4, diff-scoped credential scan=0) — re-run and confirmed passing"
        status: pass
    human_judgment: false
  - id: D3
    description: "The two drifted facts (advisory-key ci.yml occurrence count, FANOUT-GLOBAL-01 '0 hits' claim) corrected in CLAUDE.md with lineage preserved, and the same occurrence figure corrected everywhere it was restated in TODOS.md, plus the [164.8-TEST-DATA-RESEEDED] routing note appended"
    requirement: null
    verification:
      - kind: other
        ref: "Task 3 <verify> automated block (26x-in-ci.yml absent from both files, 27x present, wider census named, dated, routing note present) — re-run and confirmed passing"
        status: pass
    human_judgment: false

# Metrics
duration: ~7min
completed: 2026-09-21
status: complete
---

# Phase 164.9 Plan 01: Book the four backlog ids and correct two drifted facts Summary

**Wrote the FANOUT-GLOBAL-01 family entry plus its three 164.9-\* member entries into `TODOS.md`, and corrected the stale advisory-key occurrence figure (26→27 for `ci.yml`) everywhere it was restated across `CLAUDE.md` and `TODOS.md`, not just at the single spot the plan's automated check targeted.**

## Performance

- **Duration:** ~7 min (first task commit 2026-09-21T09:45:20Z, plan completion ~2026-09-21T09:52Z)
- **Tasks:** 3 completed
- **Files modified:** 2 (`TODOS.md`, `CLAUDE.md`)

## Accomplishments
- `FANOUT-GLOBAL-01` now resolves to a real `TODOS.md` entry (owner: Phase 164.9 TESTISOLATION; trigger: any new assertion against shared TEST, or any new job/script taking a shared-TEST lock; five named family members), placed in `## 🟡 FIX MID-TERM` adjacent to the `[164.8-PUSH-RACE-VAC08]` / `[164.8.2-VAC08-FATAL-ON-TRANSIENT]` cluster. Its three prior prose citations (at the `[164.8.2-REDACT-HOSTNAME-01]` entry, the `[164.8-PUSH-RACE-VAC08]` entry, and the `FANOUT-FAILBRANCH-UNEXERCISED-01` section intro) are each retro-linked with a dated append-only clause.
- `[164.9-SHARED-TEST-TRANSPORT-FLAKE]`, `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]` and `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]` are now real `TODOS.md` entries under a new `## Phase 164.9 (TESTISOLATION) — ids booked at planning time (logged 2026-09-21)` heading, each carrying its ROADMAP-sourced evidence (run ids, failure signatures, the CONTEXT Area 4 correction that at least three of the five credentialed-class failure classes are baseline/fixture defects that follow the tests to any host), an owner, a deliverable, and an explicit forbidden-closure clause.
- The advisory-key `61616158` occurrence count for `.github/workflows/ci.yml` was stale at 26 (measured 2026-09-09) in **four** places — `CLAUDE.md`, and three separate restatements inside `TODOS.md` (the closed `CI-MIGRATE-01` entry, and two copies of the `[164.8-PUSH-RACE-VAC08]` entry, one of which is a near-duplicate of the other). All four are now corrected to the 2026-09-21 re-measurement (27), dated, with the superseded figure preserved as an inline lineage note rather than silently overwritten. `CLAUDE.md` additionally now names the three further workflows that also take the key (`test-restore-from-baseline.yml`, `mutex-probe.yml`, `analytics-deploy-verify.yml`).
- `CLAUDE.md`'s `FANOUT-GLOBAL-01` "0 hits, not a `TODOS.md` id" paragraph is kept verbatim as lineage, with a dated `⛔ CORRECTED 2026-09-21` block appended recording that the entry now exists, that the "0 hits" reading was stale in the letter and correct in the substance, and that the name was kept rather than retired.
- `[164.8-TEST-DATA-RESEEDED]` gets a dated routing-fact note: Phase 164.9's `ROADMAP.md` Requirements line does not cite it, this plan did not claim it, and its destination/disposition are unchanged — the discrepancy is recorded, not resolved, per the plan's explicit prohibition.

## Task Commits

Each task was committed atomically:

1. **Task 1: Write the FANOUT-GLOBAL-01 family entry and retro-link its three prose citations** - `72fe0885` (docs)
2. **Task 2: Write the three 164.9-\* entries from the ROADMAP evidence** - `5e9361a8` (docs)
3. **Task 3: Correct the two drifted facts in CLAUDE.md and the one restated inside TODOS.md** - `f65e33ae` (docs)

**Plan metadata:** committed alongside this SUMMARY.

## Files Created/Modified
- `TODOS.md` - added the `FANOUT-GLOBAL-01` family entry, the three `164.9-*` member entries under a new heading, retro-linked three prose citations, corrected the stale advisory-key figure in three places, and appended the `[164.8-TEST-DATA-RESEEDED]` routing note.
- `CLAUDE.md` - corrected the advisory-key occurrence figure and recorded the wider workflow census; appended a dated correction to the `FANOUT-GLOBAL-01` paragraph recording that the entry now exists.

## Decisions Made
- Kept the `FANOUT-GLOBAL-01` name (CONTEXT Area 2 locked decision) rather than retiring it in favour of a fresh id — it is cited by name in three places and retiring it would orphan those citations for no gain.
- Corrected the stale `26` figure in **every** place it was restated in `TODOS.md` (three locations), not only the one location Task 3's automated `<verify>` block directly targets. Reading before writing (`grep -n` over the whole file) surfaced two additional restatements — inside the closed `CI-MIGRATE-01` entry and inside a near-duplicate copy of `[164.8-PUSH-RACE-VAC08]` in the "Phase 164.8 (TESTPREPROD) — plan 05 residuals" section — that the plan's task description ("the same fact restated twice... wrong in both places") did not anticipate. Left uncorrected, those two would have stayed stale after this plan closed, which is exactly the kind of half-fix this repo's own history (the sql-gate-lint arm-count table, the FILES_FLOOR drift) shows recurs when a stale number is fixed in only the spot a script happens to check. Fixing all four instances of one fact in the same pass is the root-cause close, not a point-fix.
- Used a compact inline lineage note (`⛔ CORRECTED, superseding the 2026-09-09 reading of 26...`) for the three TODOS.md occurrence-figure corrections and the CLAUDE.md key-count sentence, versus the fuller "keep original sentence + append dated block" shape used for the FANOUT-GLOBAL-01 paragraph. The plan's own Edit 1 instruction said "replace... in place... do not add a new paragraph" for the key-count sentence, while Edit 2 explicitly asked for the fuller `⛔ CORRECTED` callout shape for the FANOUT-GLOBAL-01 paragraph; both forms satisfy the plan's Repudiation mitigation (T-164.9-01-02) that every correction stays dated and attributable.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Corrected two additional restatements of the same stale advisory-key figure that the plan's Task 3 did not name**
- **Found during:** Task 3, while reading the `[164.8-PUSH-RACE-VAC08]` entry per the task's own `<read_first>` instruction
- **Issue:** The plan's Task 3 describes "the `[164.8-PUSH-RACE-VAC08]` entry" (singular) restating the stale `26` figure "in the same pass" as `CLAUDE.md`, but `TODOS.md` actually carries this exact stale figure in three separate places: the closed `CI-MIGRATE-01` entry (line ~2962), the `[164.8-PUSH-RACE-VAC08]` entry inside `## 🟡 FIX MID-TERM` (line ~3348), and a near-duplicate `[164.8-PUSH-RACE-VAC08]` entry inside the "Phase 164.8 (TESTPREPROD) — plan 05 residuals" section (the one Task 3's `<verify>` block actually targets). Correcting only the targeted one would have left two stale restatements of the identical fact standing after this plan closed.
- **Fix:** Corrected all three TODOS.md restatements to 7/27 dated 2026-09-21, each with a compact inline lineage note recording the superseded 26 figure.
- **Files modified:** TODOS.md
- **Verification:** `grep -cF '26 in \`ci.yml\`' TODOS.md` and `grep -cF '26× in \`.github/workflows/ci.yml\`' TODOS.md` both return `0`; `grep -cF '26 times in \`.github/workflows/ci.yml\`' TODOS.md` (the plan's own targeted check) returns `0`.
- **Committed in:** f65e33ae (Task 3 commit)

---

**Total deviations:** 1 auto-fixed (1 bug — closing the whole class of a restated stale fact rather than the single instance the plan's automated check happened to target).
**Impact on plan:** Strictly additive correctness within Task 3's own stated scope (correcting a fact restated across files); no scope creep into other tasks or files.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `FANOUT-GLOBAL-01` and the three `164.9-*` ids are now booked items with owners — ROADMAP criterion 1 is met, and every downstream plan (164.9-02 through 164.9-11) that cites any of these four ids can now resolve them to a real `TODOS.md` entry instead of prose.
- The `[164.8-TEST-DATA-RESEEDED]` routing discrepancy remains open and unresolved by design — a future plan or founder decision should either explicitly claim it into Phase 164.9's scope or leave it where `TODOS.md` currently names it.
- No blockers for wave 2 (164.9-02 through 164.9-05, 07).

---
*Phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro*
*Completed: 2026-09-21*

## Self-Check: PASSED

- `TODOS.md` and `CLAUDE.md` both exist at the worktree root and carry the edits described above (confirmed via `grep -c` re-runs of every Task 1/2/3 `<verify>` assertion, all passing, after Task 3's edits landed).
- `git log --oneline --all --grep="164.9-01"` returns 0 because no commit message contains that literal token — commits are identified by hash instead: `72fe0885`, `5e9361a8`, `f65e33ae` all confirmed present via `git log -1 --format='%H %s'` immediately after each commit, and each commit's subject matched what this agent wrote (no sibling-agent scratchpad clobber observed, per this repo's own standing risk note).
- Re-ran the plan-level `<verification>` block: all four entry headers exist exactly once each; no id was retired or renamed; the occurrence figure (7/27, dated 2026-09-21) is identical in the four places it now appears; a diff-scoped scan (`git diff <plan-head-before>..HEAD`) for host-shaped, connection-string-shaped, and absolute-home-path strings over every added line returned 0 matches.
