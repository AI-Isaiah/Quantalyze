---
phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro
plan: 11
type: execute
wave: 6
depends_on: ["164.9-07", "164.9-10"]
files_modified:
  - .planning/ROADMAP.md
  - TODOS.md
autonomous: false
requirements:
  - "[164.8.1-REPLAY-INSERT-ONLY-SCOPE]"

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The routed restore-workflow item that remains open is discharged by a REAL dispatch whose run id is recorded — or it is explicitly deferred to a named phase with the reason written down."
    - "The sequencing constraint that makes this act post-merge is SURFACED to the founder as a decision, not resolved silently by the planner."
    - "Whatever is decided, the ROADMAP and the backlog say the same thing afterwards."
  artifacts:
    - .planning/ROADMAP.md
    - TODOS.md
  key_links:
    - "The dispatch exercises the two-directional extension guard and the value-pinning gate leg that plan 07 built — those guards are the SUBJECT of the run, not incidental to it."
  prohibitions:
    - "⛔ The executor never dispatches the restore. It is a destructive act on a database shared with other people's CI and it is a human act."
    - "⛔ The workflow's hard ref guard is never relaxed, worked around, or dispatched from a branch. It exists because a dispatch off the default branch would restore shared TEST from an unreviewed dump."
    - "⛔ The confirm token is never hard-coded into a planning document. It is derived at dispatch time from the tracked baseline record AT THE DISPATCHED REF, because it moves with that file."
    - "⛔ The discharged post-verify item from the same routed pair is NOT reopened. It executed and passed; it must not scope work here."
    - "⛔ If the item is deferred, its destination is a NAMED phase. A blank destination is not an option."
    - "No CI skip trailer token is written into any file or commit message, not even inside a sentence denying it."
---

<objective>
ROADMAP criterion 8: the two restore-workflow items routed here from Phase 164.8 are discharged by
ONE green restore dispatch, and its run id is recorded.

⚠️ One of that pair is ALREADY DISCHARGED — the pinned-CLI post-verify executed and passed, with its
run id recorded in the ROADMAP — and CONTEXT Area 6 is explicit that it must NOT scope work here.
So the remaining content of criterion 8 is: prove plan 07's two-directional extension guard and its
value-pinning gate leg against shared TEST itself, by one real dispatch.

⛔ **A SEQUENCING CONSTRAINT THE PLANNER WILL NOT RESOLVE SILENTLY, because it is a founder call.**
The restore workflow carries a HARD ref guard: a dispatch from anywhere but the default branch is
REFUSED, and the guard's own comment gives the reason — a dispatch off a branch would restore the
shared TEST database from an unreviewed dump, and a silent green would teach the operator that the
dispatch worked. That guard is correct and this plan does not touch it.

The consequence is unavoidable: **the dispatch runs the restore script AS IT EXISTS ON THE DEFAULT
BRANCH, so it cannot exercise this phase's new guards until this phase has merged.** Criterion 8 is
therefore a POST-MERGE act, and this plan makes that a decision the founder takes rather than a
surprise anyone discovers at ship time.

Purpose: close the criterion honestly, in whichever of the two legitimate ways the founder picks.

Output: a recorded decision, then either a recorded run id or a recorded deferral to a named phase —
with the ROADMAP and the backlog agreeing either way.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Decide how criterion 8 closes, given that the dispatch is post-merge by construction</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.github/workflows/test-restore-from-baseline.yml` — its header describing what a committing
      restore destroys, its dispatch guard and that guard's stated reason, and its confirm-token
      requirement.
    - `.planning/ROADMAP.md`, the `### Phase 164.9` entry's criterion 8 and the routed block above it
      — including the paragraph marking the second of the pair as discharged, which must not be
      reopened.
    - `.planning/phases/164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro/164.9-07-SUMMARY.md`
      — what plan 07 actually built and what it proved on a disposable cluster, so the decision is
      about what remains unproven rather than about everything.
  </read_first>
  <decision>How criterion 8 closes, given that the restore dispatch can only run from the default branch and therefore cannot exercise this phase's new guards until this phase has merged.</decision>
  <context>
    The restore workflow refuses any dispatch that is not from the default branch, on purpose,
    because a branch dispatch would restore shared TEST from an unreviewed dump. Plan 07's new
    guards therefore cannot be exercised against shared TEST until this phase has merged. Plan 07
    has already proven both of them on a disposable cluster with both directions observed RED; what
    a dispatch would add is the shared-TEST execution, not the falsification. ⚠️ The planner
    deliberately did not resolve this: both answers are defensible and the cost falls on the
    founder's own record-keeping discipline.
  </context>
  <options>
    <option id="ship-then-dispatch">
      <name>Option A — ship, then dispatch, then record</name>
      <pros>The falsification is already done; what remains is an execution whose only blocker is ordering. Nothing is re-deferred, so a routed item does not go back into the state this phase exists to end.</pros>
      <cons>The phase's own record is completed after its merge — the record-versus-reality gap this milestone exists to close. Only honest if the post-merge recording act is itself booked with an owner rather than intended.</cons>
    </option>
    <option id="defer-to-named-phase">
      <name>Option B — defer criterion 8 to a named phase</name>
      <pros>The phase completes with nothing owed after its merge, and the deferral is explicit, owned and triggered rather than implicit.</pros>
      <cons>Plan 07's guards ship proven only on a disposable cluster. Bounded — both directions were observed RED there, and the guard runs post-commit so it labels an outcome rather than preventing one — but it is a real gap.</cons>
    </option>
  </options>
  <resume-signal>Select: ship-then-dispatch or defer-to-named-phase. If ship-then-dispatch, also name where the post-merge recording act is booked and who owns it.</resume-signal>
  <action>
    Present the constraint and the two options above, with the recommendation, and record the
    answer.

    THE CONSTRAINT, stated in one paragraph for the founder: the restore workflow refuses any
    dispatch that is not from the default branch, on purpose, because a branch dispatch would
    restore shared TEST from an unreviewed dump. Plan 07's new guards therefore cannot be exercised
    against shared TEST until this phase has merged. Plan 07 has already proven both of them on a
    disposable cluster with both directions observed RED; what a dispatch would add is the
    shared-TEST execution, not the falsification.

    OPTION A — SHIP, THEN DISPATCH, THEN RECORD. The phase completes with criterion 8 marked as
    owed-at-merge; immediately after the merge a human runs the dispatch and the run id is recorded
    back into this plan's SUMMARY and the ROADMAP. Cost: the phase's own record is completed after
    its merge, which is the record-versus-reality gap this milestone exists to close, so the
    recording must be a named follow-up act and not a hope.

    OPTION B — DEFER CRITERION 8 TO A NAMED PHASE. The phase completes without the dispatch;
    criterion 8 is re-homed to a named successor phase with its own entry, owner and trigger. Cost:
    plan 07's guards ship proven only on a disposable cluster, which is a real but bounded gap —
    both directions were observed RED there, and the guard runs post-commit anyway, so it labels an
    outcome rather than preventing one.

    RECOMMENDATION to state in the prompt: **Option A**, because the falsification is already done
    and what remains is an execution whose only blocker is ordering — and because a deferral would
    put a routed item back in the state this whole phase exists to end. ⚠️ Option A is only honest
    if the post-merge recording is itself booked; make that part of the answer, not a promise.

    Record the founder's answer in the `### Phase 164.9` ROADMAP entry as a dated line under
    criterion 8, in the entry's own voice. ⛔ Use a scoped edit; do not rewrite the entry and do not
    touch any other phase's section.
  </action>
  <verify>
    <human-check>The founder states which option is taken, in their own words, and — if Option A — confirms that the post-merge recording act is booked somewhere with an owner, not merely intended.</human-check>
    <automated>grep -qF '2026-09-21' .planning/ROADMAP.md && test "$(grep -c '^### Phase 164.9' .planning/ROADMAP.md)" = 1 && D="$(git diff HEAD -- .planning/ROADMAP.md)" && test "$(printf '%s\n' "$D" | grep -c '^-')" -le 3 && echo DECISION-RECORDED-OK</automated>
    <fails_when>non-zero exit, or missing "DECISION-RECORDED-OK" in output — which happens when the phase heading is no longer unique (the entry was duplicated or another was damaged), or when the edit removed more than three lines, which would mean it rewrote the entry rather than appending a dated decision line.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `### Phase 164.9` ROADMAP entry carries a dated line under criterion 8 naming the option
      taken and its reason.
    - The ROADMAP diff is additive apart from at most three replaced lines; no other phase section
      appears in the diff.
    - The discharged post-verify item's paragraph is unchanged.
  </acceptance_criteria>
  <done>The sequencing constraint is on the record, the founder has chosen, and the choice is written where the next reader will find it.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: If Option A — the single dispatch, run by a human, against the default branch</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.github/workflows/test-restore-from-baseline.yml` header — the full recipe, what the
      committing mode destroys, and the confirm-token derivation.
    - `supabase/schema/BASELINE.md` — the recorded baseline hash row the confirm token is derived
      from. ⛔ Derive the token at dispatch time from the file AT THE DISPATCHED REF; it moves with
      that file and a value copied from a planning document will be stale.
    - `CLAUDE.md`'s database-safety section — shared TEST is used by other people's CI, and a write
      there is not private.
  </read_first>
  <action>Dispatch the restore workflow against the default branch — a preflight first, then the committing mode — so this phase's new restore guards are exercised against shared TEST.</action>
  <instructions>
    ⛔ **SKIP THIS TASK ENTIRELY IF TASK 1 CHOSE OPTION B.** Record the skip and its cause in the
    SUMMARY; do not run it "just in case".

    WHAT CLAUDE ALREADY AUTOMATED: plan 07 built both guards and proved each direction RED on a
    disposable cluster; the workflow, its confirm-token derivation and its recipe already exist.
    Nothing is left to build.

    THE ONE THING REQUIRING A HUMAN: dispatching a workflow that drops and rebuilds the public
    schema of a database other people's CI uses.

    ⛔ **THE EXECUTOR DOES NOT DISPATCH.** This drops and rebuilds the public schema of a database
    other people's CI uses. The executor prepares, presents and records; a human runs it.

    WHAT THE HUMAN DOES, in order:
    1. Confirm the phase's restore-script changes are on the default branch. ⛔ If they are not, STOP
       — the dispatch would run the OLD script and prove nothing about this phase's guards, while
       still destroying and rebuilding a shared schema. That is the worst of both outcomes.
    2. Derive the confirm token from the tracked baseline record at that ref.
    3. Dispatch a PREFLIGHT first. It runs the whole transaction and rolls back, so it exercises the
       new guards without committing. Read its verdict.
    4. Only if the preflight is green, dispatch the COMMITTING mode with the confirm token.
    5. Read the run's own printed summary line and the two new guards' output.

    WHAT THE EXECUTOR RECORDS in the SUMMARY afterwards:
    - the preflight run id and its conclusion;
    - the committing run id and its conclusion;
    - the summary line the run printed, verbatim;
    - what the two new guards reported — the schema-scoped extension comparison and the
      value-pinning leg — quoted as printed;
    - ⛔ if either guard did NOT run or did not report, say so plainly. A dispatch that concluded
      successfully without exercising the guards it was run to exercise has not discharged anything,
      and recording it as a discharge would be exactly the green-that-is-not-measuring-what-it-claims
      defect this whole phase exists to remove.
    ⛔ Record VERDICTS, never specimens: no hostname, no connection string, no project reference.
  </instructions>
  <verification>Read both runs' conclusions and their logs: the preflight must be green and must show both new guards reporting, and the committing run must print its summary line with both guards' output. A committing run whose logs do not show the guards reporting has discharged nothing and must not be recorded as a discharge.</verification>
  <resume-signal>Type "done" and paste the two run ids plus the committing run's summary line; or type "skipped — option B" if Task 1 chose the deferral.</resume-signal>
  <verify>
    <human-check>The human confirms: the phase's restore-script changes were on the default branch before the dispatch; the preflight was green BEFORE the committing dispatch; and the committing run's printed summary and both new guards' output were read. ⛔ A committing dispatch run without a green preflight first is a protocol breach and must be reported as one.</human-check>
    <automated>D="$(git diff HEAD -- .planning/ROADMAP.md)" && test "$(printf '%s\n' "$D" | grep '^+' | grep -caiE "supabase\.co|postgres(ql)?://|${HOME}")" = 0 && test "$(grep -c '^### Phase 164.9' .planning/ROADMAP.md)" = 1 && echo DISPATCH-RECORDED-OK</automated>
    <fails_when>non-zero exit, or missing "DISPATCH-RECORDED-OK" in output — which happens when a hostname, connection string or absolute home path appears on any line added to the ROADMAP, or when the phase heading is no longer unique.</fails_when>
  </verify>
  <acceptance_criteria>
    - If Option A: the SUMMARY records both run ids, both conclusions, the run's printed summary
      line verbatim, and both new guards' output.
    - If either guard did not report, the SUMMARY says so and the item is NOT recorded as
      discharged.
    - If Option B: the SUMMARY records that this task was skipped and why.
    - No hostname, connection string, project reference or absolute home path appears anywhere in
      the recorded output.
  </acceptance_criteria>
  <done>Either one real restore dispatch exercised this phase's new guards against shared TEST and its run ids are on the record, or the task is recorded as deliberately skipped under Option B.</done>
</task>

<task type="auto">
  <name>Task 3: Make the ROADMAP and the backlog say the same thing</name>
  <files>.planning/ROADMAP.md, TODOS.md</files>
  <read_first>
    - `.planning/ROADMAP.md`, the `### Phase 164.9` entry's criterion 8 and the routed block above it.
    - `TODOS.md` — the entries this phase booked in plan 01 and any entry covering the restore
      workflow items.
    - `.planning/phases/164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro/164.9-CONTEXT.md`
      — the standing rule that an item two places disagree about is owned by NEITHER until someone
      decides.
  </read_first>
  <action>
    Close the loop in both ledgers, matching whatever Task 1 decided and Task 2 produced.

    If Option A and the dispatch discharged the item: mark criterion 8 in the ROADMAP entry as
    discharged, with the run id, in the entry's own dated voice. Add or update the matching backlog
    entry so the two agree. ⛔ Use scoped edits; do not rewrite the ROADMAP entry and do not touch
    another phase's section.

    If Option A and the dispatch did NOT discharge it — a guard did not report, or the preflight was
    not green — treat that as the honest outcome it is: record what was measured, do NOT mark it
    discharged, and book the remainder with an id, a date, a trigger and a NAMED destination phase.

    If Option B: re-home criterion 8 to the named successor phase. Write the backlog entry with an
    id, a date, the trigger (this phase's restore-script changes reaching the default branch), the
    evidence already in hand (plan 07's disposable-cluster falsification, both directions observed
    RED), and the destination. ⛔ The destination is never blank.

    In every branch, state in the ROADMAP line what is NOT claimed. The restore guard runs after its
    transaction commits, so in committing mode it labels an outcome rather than preventing one —
    that limit is unchanged by any dispatch and must not be implied away by a green run id.
  </action>
  <verify>
    <automated>test "$(grep -c '^### Phase 164.9' .planning/ROADMAP.md)" = 1 && grep -qF 'Owner:' TODOS.md && D="$(git diff HEAD -- TODOS.md)" && test "$(printf '%s\n' "$D" | grep '^+' | grep -caiE "supabase\.co|postgres(ql)?://|${HOME}")" = 0 && D="$(git diff HEAD -- .planning/ROADMAP.md)" && test "$(printf '%s\n' "$D" | grep -c '^-')" -le 6 && NOW="$(grep -c '^### Phase ' .planning/ROADMAP.md)" && PREVRM="$(git show HEAD:.planning/ROADMAP.md)" && WAS="$(printf '%s\n' "$PREVRM" | grep -c '^### Phase ')" && echo "phase-sections now=$NOW was=$WAS" && test "$NOW" -ge "$WAS" && echo LEDGERS-AGREE-OK</automated>
    <fails_when>non-zero exit, or missing "LEDGERS-AGREE-OK" in output, or a printed line where "now" is lower than "was" — which would mean a scoped edit destroyed other phase entries, the known failure mode of writing the whole roadmap instead of editing a section. The baseline is DERIVED from the committed file, never a number restated here, because the phase count moves every time a phase is inserted.</fails_when>
  </verify>
  <acceptance_criteria>
    - The `### Phase 164.9` heading appears exactly once and the total count of phase sections in
      the ROADMAP is unchanged from before this task.
    - Criterion 8's status in the ROADMAP and the corresponding backlog entry state the same thing.
    - If anything remains open, it carries an id, a date, a trigger and a non-blank destination
      phase.
    - The ROADMAP line states explicitly what is NOT claimed: the guard labels rather than prevents
      in committing mode.
    - No hostname, connection string or absolute home path appears on any added line.
  </acceptance_criteria>
  <done>The ROADMAP and the backlog agree about criterion 8, and whatever is still open has an owner, a trigger and a named home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dispatch → shared TEST's public schema | A committing restore drops and rebuilds the public schema of a database other people's CI uses. |
| dispatched ref → what actually runs | The workflow runs the script as it exists on the dispatched ref, which is why the ref guard is the safety property. |
| recorded run id → a future reader's belief | A run id recorded as a discharge is taken as proof by everyone after. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9-11-01 | Denial of Service | A committing restore against a shared database | critical | mitigate | Human-run only, behind the workflow's existing confirm token, activity gate, mutex and backup artifact. A green preflight is required FIRST, and the executor is forbidden from dispatching. |
| T-164.9-11-02 | Tampering | A dispatch from a branch restoring from an unreviewed dump | critical | transfer | The workflow's existing hard ref guard owns this and exits non-zero off the default branch. This plan does not touch it and explicitly forbids working around it. |
| T-164.9-11-03 | Repudiation | A run id recorded as a discharge that exercised nothing | high | mitigate | The SUMMARY must quote both new guards' output; if either did not report, the item is explicitly NOT recorded as discharged. |
| T-164.9-11-04 | Information Disclosure | Run output quoted into a tracked public file | high | mitigate | Verdicts only; every added line in the ROADMAP and the backlog is scanned for host, connection-string and absolute-home-path shapes. |
| T-164.9-11-05 | Tampering | A whole-file write destroying other phase entries | high | mitigate | Scoped edits only, plus a phase-section count assertion that fails if the total falls — the measured failure mode this repository already records for roadmap writers. |
| T-164.9-11-SC | Tampering | npm/pip/cargo installs | high | accept | No package is installed and no manifest is touched. RESEARCH's Package Legitimacy Audit records "Not applicable". Gate evaluated; it does not fire. |
</threat_model>

<verification>
- The post-merge sequencing constraint is recorded and the founder has decided.
- Either both run ids and both guards' output are recorded, or the skip is recorded with its cause.
- Nothing is marked discharged on a run that did not exercise the guards it was run to exercise.
- The ROADMAP's phase-section count is unchanged and no other phase's section appears in the diff.
- Whatever remains open has an id, a date, a trigger and a named destination.

## AI integration gate

⛔ EVALUATED AND DID NOT FIRE. This phase integrates no external API, SDK or model service. Its
entire surface is this repository's own CI workflows, shell scripts, SQL gates and test corpus.
Recorded rather than silently omitted.
</verification>

<success_criteria>
ROADMAP criterion 8 is closed honestly: either by one real restore dispatch whose run id is recorded
and whose output shows this phase's new guards actually ran, or by an explicit, reasoned deferral to
a named phase. The already-discharged half of the routed pair was not reopened, and the workflow's
ref guard was neither relaxed nor worked around.
</success_criteria>

<output>
Create `.planning/phases/164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro/164.9-11-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| Criterion 8 decision line | new ROADMAP line | Dated, in the entry's voice, naming the option taken and its reason. |
| Restore dispatch run ids | recorded evidence | Preflight and committing, with both new guards' output quoted — or a recorded skip under Option B. |
| Residual backlog entry | new or updated `TODOS.md` entry | Id, date, trigger, destination phase. Never blank. |
</content>
