---
phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TODOS.md
  - CLAUDE.md
autonomous: true
requirements:
  - FANOUT-GLOBAL-01
  - "[164.9-SHARED-TEST-TRANSPORT-FLAKE]"
  - "[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]"
  - "[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Each of the four ids resolves to a real TODOS.md ENTRY HEADER, not a prose mention — the grep that measures entry-shape returns 1 for each."
    - "A reader who greps CLAUDE.md for the advisory-key occurrence count gets the 2026-09-21 measurement (7 and 27), not the superseded one."
    - "A reader who greps CLAUDE.md for the FANOUT-GLOBAL-01 paragraph is told the entry now EXISTS, with the lineage of why it did not preserved."
  artifacts:
    - TODOS.md
    - CLAUDE.md
  key_links:
    - "The three existing prose references to FANOUT-GLOBAL-01 inside TODOS.md point at the new family entry instead of at CLAUDE.md."
    - "The stale occurrence figure inside the existing [164.8-PUSH-RACE-VAC08] entry and the one inside CLAUDE.md are corrected in the SAME pass — they are one fact restated twice."
  prohibitions:
    - "The name FANOUT-GLOBAL-01 is NOT retired or renamed. CONTEXT Area 2 locked: WRITE the entry, KEEP the name."
    - "No entry is written with a blank or absent destination. Every new entry names an owner phase."
    - "No credential, connection string, project ref, hostname, absolute home path or local username appears in any entry. Record VERDICTS, never specimens."
    - "[164.8-TEST-DATA-RESEEDED] is NOT claimed by this phase. Its routing discrepancy is RECORDED, not resolved."
    - "No CI skip trailer token is written into any file or commit message, not even inside a sentence denying it."
---

<objective>
Make this phase's own subject a booked item. Four ids that this milestone cites by name have no
`TODOS.md` entry — no owner, no date, no trigger — and two facts recorded in `CLAUDE.md` about
those ids have drifted. Fix both before any code moves.

Purpose: ROADMAP criterion 1 is explicit that planning starts here and that everything below
depends on the phase's subject having a booked entry. CONTEXT Area 2 measured the id census on
2026-09-21 and found FOUR ids in this position, not one.

Output: four new `TODOS.md` entries, three retro-linked prose citations, and two dated
corrections in `CLAUDE.md`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Write the FANOUT-GLOBAL-01 family entry and retro-link its three prose citations</name>
  <files>TODOS.md</files>
  <read_first>
    - `TODOS.md` — read the existing `[164.8-PUSH-RACE-VAC08]` entry under the heading
      `## Phase 164.8 (TESTPREPROD) — plan 05 residuals (logged 2026-09-09)` for the entry-header
      convention, and the `### FANOUT-FAILBRANCH-UNEXERCISED-01 / 164.5.1-TODOS-DISPOSITION-OWED`
      section for the convention used when booking an item that existed ONLY as prose inside
      `.planning/`. That section already names this exact failure mode and is the shape to copy.
    - `.planning/ROADMAP.md`, the `### Phase 164.9` entry's **Goal:** paragraph — it carries the
      definition to reuse verbatim rather than re-deriving new wording.
    - `.planning/phases/164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro/164.9-CONTEXT.md`
      Area 1 and Area 2.
  </read_first>
  <action>
    Add a `FANOUT-GLOBAL-01` entry to `TODOS.md` as the FAMILY entry the other four already cite by
    name. Place it in the `## 🟡 FIX MID-TERM` section, immediately adjacent to the existing
    `[164.8-PUSH-RACE-VAC08]` / `[164.8.2-VAC08-FATAL-ON-TRANSIENT]` cluster so the family and its
    members read together.

    Header shape, matching the file's own convention:
    `- [ ] **` then the id in backticks, then a one-line past-tense summary of the defect, then
    `(booked 2026-09-21, Phase 164.9 TESTISOLATION)** — ` and the body.

    Body must carry, in this order:
    (a) the definition taken from the ROADMAP Goal paragraph — TEST is shared with other people's
        CI, so a GLOBAL assertion there is measuring other people's rows as well as ours and is
        unreliable by construction;
    (b) the MEASURED id census from CONTEXT Area 2, restated as a verdict: before this entry the id
        had three hits in `TODOS.md` and all three were PROSE references to it as a failure-mode
        FAMILY — no entry carried it as an id, so it had no owner, no date and no gate;
    (c) the naming decision and its reason: the name is KEPT rather than retired because it is
        cited by name in `CLAUDE.md`, in the `### Phase 164.9` ROADMAP entry and in at least two
        `TODOS.md` entries as a family label, and retiring it would orphan those citations for no
        gain;
    (d) the FAMILY MEMBERSHIP list, naming the ids that are instances of it:
        `[164.8-PUSH-RACE-VAC08]`, `[164.8.2-VAC08-FATAL-ON-TRANSIENT]`,
        `[164.9-SHARED-TEST-TRANSPORT-FLAKE]`, `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]`,
        `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]`;
    (e) the TRIGGER: any new assertion written against the shared TEST project, and any new job or
        script that takes a shared-TEST lock;
    (f) `**Owner:** Phase 164.9 TESTISOLATION.`

    Then retro-link the three existing prose citations. Each currently says the id is recorded in
    `CLAUDE.md` or names it as a family without a destination; append to each a short dated clause
    pointing at the new entry so the citation resolves inside this file rather than out of it. Do
    not rewrite the surrounding sentences — append only.

    Record a VERDICT of the census, never a specimen: no hostnames, no DSNs, no project refs.
  </action>
  <verify>
    <automated>test "$(grep -cF '- [ ] **`FANOUT-GLOBAL-01`' TODOS.md)" = 1 && test "$(grep -cF 'FANOUT-GLOBAL-01' TODOS.md)" -ge 4 && grep -qF 'Owner:** Phase 164.9 TESTISOLATION' TODOS.md && D="$(git diff HEAD -- TODOS.md)" && test "$(printf '%s\n' "$D" | grep '^+' | grep -caiE "supabase\.co|postgres(ql)?://|${HOME}")" = 0 && echo FAMILY-ENTRY-OK</automated>
    <fails_when>non-zero exit, or missing "FAMILY-ENTRY-OK" in output — which happens when the entry header is absent or duplicated, when the three prose citations were not retro-linked (total hits below 4), when no owner line was written, or when a hostname / DSN / absolute home path appears on any line THIS task added (the scan is diff-scoped on purpose: the file carries three pre-existing such strings and a whole-file scan would be self-invalidating).</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cF '- [ ] **`FANOUT-GLOBAL-01`' TODOS.md` returns exactly `1`.
    - `grep -cF 'FANOUT-GLOBAL-01' TODOS.md` returns at least `4` (the new entry plus the three
      retro-linked prose citations).
    - The entry body names all five family-member ids; `grep -cF '[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]' TODOS.md`
      is at least `1` after this task.
    - The entry carries a literal `Owner:` line naming Phase 164.9.
    - The diff of `TODOS.md` against HEAD is CAPTURED into a variable first, its status checked, and
      only then are its added lines scanned for host-shaped, connection-string-shaped and
      absolute-home-path strings. The scan returns no match.
      ⚠️ Diff-scoped deliberately: the file already carries three such strings
      from earlier entries, so a whole-file scan would fail for reasons this task did not cause.
  </acceptance_criteria>
  <done>`FANOUT-GLOBAL-01` resolves to a real entry with an owner, a date, a trigger and a family list, and its three prior prose citations point at it.</done>
</task>

<task type="auto">
  <name>Task 2: Write the three 164.9-* entries from the ROADMAP evidence</name>
  <files>TODOS.md</files>
  <read_first>
    - `.planning/ROADMAP.md`, the `### Phase 164.9` entry's success criteria 11, 12 and 13 — these
      carry the run ids, the dated measurements and the failure signatures. They are the source;
      do not re-derive or re-word them.
    - `TODOS.md` — the `[164.8.2-VAC08-FATAL-ON-TRANSIENT]` entry, for the
      BEFORE / AFTER / THE EXPOSURE THIS CREATES body shape used for
      consequence-of-a-correct-fix entries.
    - `.planning/phases/164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro/164.9-CONTEXT.md`
      Areas 4 and 5.
  </read_first>
  <action>
    Add three entries to `TODOS.md`, under a new heading
    `## Phase 164.9 (TESTISOLATION) — ids booked at planning time (logged 2026-09-21)`, placed
    directly after the `FANOUT-GLOBAL-01` entry from Task 1. Each uses the same header shape and
    each names `**Owner:** Phase 164.9 TESTISOLATION.`

    `[164.9-SHARED-TEST-TRANSPORT-FLAKE]` — carry from ROADMAP criterion 11: run `34763669052` at
    `e64b0811` on `main`, three attempts of one run at one commit; attempt 1 red on
    `tests/test_compute_jobs_fencing.py` with a PostgREST `504 Gateway Timeout`; attempt 2 red with
    a different signature (`httpx.ConnectError`, connection reset) on a different test set
    including `tests/test_drain_semantics.py`; attempt 3 green on identical code. State that
    non-deterministic victims across attempts is the proof that this is TRANSPORT, not logic.
    State the NOT-THIS clause: it is NOT the wedged-pool mechanism — a live probe returned three
    sub-second successes with all PostgREST backends idle, so the `pg_terminate_backend` remedy was
    correctly NOT fired at infrastructure shared with other people's CI. State the DELIVERABLE: a
    bounded retry at the transport boundary that keeps a transient fault DISTINGUISHABLE from a
    clean run, observed to EXHAUST. State the forbidden closure: never a bare re-run.

    `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]` — use the BEFORE / AFTER / EXPOSURE shape. BEFORE: the
    dead-holder branch of the best-effort mutex release step emits a GitHub log annotation naming
    the condition, and by the step's own documented design that annotation does not change exit
    status. AFTER (what this phase delivers): a separate always-run verdict step that reads a
    marker and exits non-zero. EXPOSURE: until that lands, a run can conclude GREEN on DB
    assertions its own harness has just declared untrustworthy. State the forbidden closures:
    deleting, softening or re-wording the warning, and adding an inline non-zero exit to the
    best-effort release step (that regresses the step's own invariant).

    `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]` — carry from ROADMAP criterion 13: measured at
    `f915bf49`, 16 files / 38 tests failed against the same tree's credential-free run reporting 0
    failures, with the skip-count delta 280 to 94 being the class itself. Name the five failure
    signatures by their PostgREST error codes and the two non-code ones, as the ROADMAP does.
    Then carry the CORRECTION from CONTEXT Area 4, which changes the entry's shape: at least three
    of the five classes are baseline- or fixture-level and follow the tests to ANY host — the
    function-overload ambiguity lives in `supabase/schema/baseline.sql` itself, the unexposed
    schema is set by `supabase/config.toml`'s exposed-schema list, and the missing-column failure
    is a stale fixture for a column that has never existed. State that the remaining per-file
    census is OWED and unmeasured. State the DELIVERABLE: the class runs somewhere it can go RED,
    or its drift is fixed with a gate proven to bite. State the forbidden closures: deleting the
    tests, skipping them permanently, arguing the credential-free green is sufficient, and adding
    TEST credentials to the vitest shards (that un-skips the live-DB class, shifts the coverage
    denominator and invalidates the ratchet baseline that a blocking gate rests on).

    Every figure written into these entries is a VERDICT taken from the ROADMAP. Truncate any
    identifier to at most 8 characters. No hostnames, no DSNs, no project refs, no local paths.
  </action>
  <verify>
    <automated>for ID in '164.9-SHARED-TEST-TRANSPORT-FLAKE' '164.9-MUTEX-HOLDER-DIED-UNSERIALIZED' '164.9-CREDENTIALED-TESTS-RED-AND-UNGATED'; do test "$(grep -cF "- [ ] **\`[$ID]\`" TODOS.md)" = 1 || { echo "MISSING-ENTRY $ID"; exit 1; }; done && test "$(grep -cF 'Phase 164.9 (TESTISOLATION) — ids booked at planning time' TODOS.md)" = 1 && test "$(grep -cF 'Owner:** Phase 164.9 TESTISOLATION' TODOS.md)" -ge 4 && D="$(git diff HEAD -- TODOS.md)" && test "$(printf '%s\n' "$D" | grep '^+' | grep -caiE "supabase\.co|postgres(ql)?://|${HOME}")" = 0 && echo THREE-IDS-OK</automated>
    <fails_when>non-zero exit, or output containing "MISSING-ENTRY", or missing "THREE-IDS-OK" — which happens when any of the three entry headers is absent or duplicated, when the section heading was not written, when fewer than four owner lines exist across the four new entries, or when a hostname / DSN / absolute home path appears on any line this plan added (diff-scoped for the same reason as Task 1).</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the three ids has exactly one entry header of the form
      `- [ ] **`[<id>]`` in `TODOS.md`.
    - The section heading `## Phase 164.9 (TESTISOLATION) — ids booked at planning time (logged 2026-09-21)`
      appears exactly once.
    - `grep -cF 'Owner:** Phase 164.9 TESTISOLATION' TODOS.md` is at least `4` (three new entries
      plus the family entry from Task 1).
    - Each entry body contains a clause naming what does NOT close it.
    - The diff of `TODOS.md` against HEAD is CAPTURED first, its status checked, and only then are
      its added lines scanned for host-shaped, connection-string-shaped and absolute-home-path
      strings. The scan returns no match.
  </acceptance_criteria>
  <done>The three `164.9-*` ids each resolve to a real entry carrying the ROADMAP evidence, an owner, a deliverable and an explicit non-closure clause.</done>
</task>

<task type="auto">
  <name>Task 3: Correct the two drifted facts in CLAUDE.md and the one restated inside TODOS.md</name>
  <files>CLAUDE.md, TODOS.md</files>
  <read_first>
    - `CLAUDE.md` — the section `## GSD orchestration rules (measured 2026-09-08 ...)` for the two
      target sentences, and any existing `⛔ CORRECTED` / `⭐ ADDENDUM` block for the dated-callout
      convention this file uses when a recorded fact changes.
    - `TODOS.md` — the `[164.8-PUSH-RACE-VAC08]` entry, which restates the same occurrence figure.
    - `.planning/phases/164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro/164.9-CONTEXT.md`
      Area 3 (the re-measurement) and the `<deferred>` block's routing-discrepancy paragraph.
  </read_first>
  <action>
    Three edits, all dated 2026-09-21.

    Edit 1 — `CLAUDE.md`, the sentence recording how many times advisory key `61616158` appears in
    each workflow. The recorded figure for `.github/workflows/ci.yml` is superseded. Replace the
    number and its measurement date in place with the 2026-09-21 measurement: seven occurrences in
    `.github/workflows/supabase-migrate.yml` and twenty-seven in `.github/workflows/ci.yml`. Keep
    the file's existing "measured DATE:" voice; do not add a new paragraph.
    ⭐ Also record, in the same sentence or the one after it, the wider census this phase measured
    and which the old sentence never mentioned: the key is ALSO taken by
    `.github/workflows/test-restore-from-baseline.yml`, `.github/workflows/mutex-probe.yml` and
    `.github/workflows/analytics-deploy-verify.yml`, so a reader planning a key change is not
    working from a two-file picture.

    Edit 2 — `CLAUDE.md`, the paragraph asserting that `FANOUT-GLOBAL-01` is not a `TODOS.md` id and
    that Phase 164.9 owns writing the real entry. Do not delete it. Follow this file's own
    `⛔ CORRECTED` callout convention: keep the original sentence as lineage and append a dated
    correction recording that the entry now EXISTS in `TODOS.md`, that the earlier "zero hits"
    reading was stale in the letter and correct in the substance (the three hits that existed were
    prose references to it as a family, not an entry), and that the name was KEPT.

    Edit 3 — `TODOS.md`, the `[164.8-PUSH-RACE-VAC08]` entry. It restates the same superseded
    occurrence figure for `ci.yml`. Correct it in the same pass, dated 2026-09-21, because it is
    one fact restated twice and was wrong in both places.

    Edit 4 — `TODOS.md`, the `[164.8-TEST-DATA-RESEEDED]` entry. Append one dated line recording a
    ROUTING FACT and nothing more: Phase 164.9's planning saw that this entry names Phase 164.9 as
    its destination while the Phase 164.9 ROADMAP Requirements line does not cite it, did NOT claim
    it, and left it for a decision. ⛔ Do not change the entry's destination, do not mark it done,
    do not absorb it into this phase's scope.
  </action>
  <verify>
    <automated>test "$(grep -cF '26× in `ci.yml`' CLAUDE.md)" = 0 && grep -qF '27× in `ci.yml`' CLAUDE.md && grep -qF 'test-restore-from-baseline.yml' CLAUDE.md && grep -qF '2026-09-21' CLAUDE.md && test "$(grep -cF '26 times in `.github/workflows/ci.yml`' TODOS.md)" = 0 && grep -qF '164.8-TEST-DATA-RESEEDED' TODOS.md && echo CORRECTIONS-OK</automated>
    <fails_when>non-zero exit, or missing "CORRECTIONS-OK" in output — which happens when the superseded occurrence figure still stands in either file, when the corrected figure or the wider workflow census was not written into CLAUDE.md, when no 2026-09-21 date was recorded there, or when the routing-fact note was written against a missing entry.</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -cF '26× in `ci.yml`' CLAUDE.md` returns `0` and `grep -qF '27× in `ci.yml`' CLAUDE.md`
      succeeds.
    - `CLAUDE.md` names `test-restore-from-baseline.yml`, `mutex-probe.yml` and
      `analytics-deploy-verify.yml` as further takers of the key.
    - `CLAUDE.md`'s `FANOUT-GLOBAL-01` paragraph still contains its original sentence AND a dated
      2026-09-21 correction recording that the entry now exists.
    - `grep -cF '26 times in `.github/workflows/ci.yml`' TODOS.md` returns `0`.
    - The `[164.8-TEST-DATA-RESEEDED]` entry carries a dated line stating this phase did not claim
      it, and its destination text is unchanged.
  </acceptance_criteria>
  <done>Both drifted facts are corrected in every place they are restated, with lineage preserved, and the one routing discrepancy is recorded rather than silently absorbed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → public internet | `.planning/`, `TODOS.md` and `CLAUDE.md` are TRACKED in a PUBLIC repository; anything written here is world-readable on push. |
| planner/executor prose → future reader | A recorded figure becomes an input to a later decision; a stale one misroutes work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.9-01-01 | Information Disclosure | New `TODOS.md` entries quoting measured evidence | high | mitigate | Record VERDICTS only — no hostname, DSN, project ref, key, absolute home path or local username. Every task's `<verify>` carries a negative scan for those shapes over the edited file. |
| T-164.9-01-02 | Repudiation | The corrected figures in `CLAUDE.md` / `TODOS.md` | medium | mitigate | Every correction is DATED and keeps the superseded sentence as lineage, per this repo's own callout convention, so the change is attributable rather than silent. |
| T-164.9-01-03 | Tampering | `[164.8-TEST-DATA-RESEEDED]`'s destination | medium | mitigate | The task appends a routing FACT and is explicitly forbidden from changing the destination, marking it done, or absorbing it. |
| T-164.9-01-SC | Tampering | npm/pip/cargo installs | high | accept | This plan installs no package and touches no manifest. RESEARCH's Package Legitimacy Audit records "Not applicable — no new npm, pip or cargo packages". Gate evaluated; it does not fire. |
</threat_model>

<verification>
- Four entry headers exist, one per id, each with an owner, a date and a trigger.
- No id is retired or renamed.
- The occurrence figure is identical in every place it appears, and every place is dated
  2026-09-21.
- Nothing credential-shaped, host-shaped or path-shaped was written into a tracked public file.
</verification>

<success_criteria>
ROADMAP criterion 1 is met: the phase's own subject, and the three ids whose evidence lived only in
the ROADMAP, are booked items with owners. The two facts `CLAUDE.md` records about them are
current, and their lineage is preserved.
</success_criteria>

<output>
Create `.planning/phases/164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro/164.9-01-SUMMARY.md` when done
</output>

## Artifacts this phase produces (this plan's contribution)

| Symbol / path | Kind | Notes |
|---|---|---|
| `FANOUT-GLOBAL-01` | `TODOS.md` entry id | NEW. Family entry. Name KEPT, not retired. |
| `[164.9-SHARED-TEST-TRANSPORT-FLAKE]` | `TODOS.md` entry id | NEW. |
| `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]` | `TODOS.md` entry id | NEW. |
| `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]` | `TODOS.md` entry id | NEW. |
| `## Phase 164.9 (TESTISOLATION) — ids booked at planning time (logged 2026-09-21)` | `TODOS.md` heading | NEW section. |
