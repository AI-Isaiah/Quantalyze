---
phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro
plan: 04
subsystem: sql-gate-integrity
tags: [mutation-testing, fanout-global-01, sql-gates, postgres, red-under-m, pg-lane]

# Dependency graph
requires:
  - phase: 164.9-01
    provides: "FANOUT-GLOBAL-01 as a real TODOS.md entry with owner, date, trigger and five family members"
provides:
  - "Foreign-row calibration proving the reaper, retention-orphaned-running and reconcile-dropped-enqueue-sweep gates verify THEIR OWN rows under the real deployed global sweep, even against a genuine competing foreign row"
  - "Three RED-UNDER-M arms, each observed to redden the new calibration when the deployed sweep's row budget is narrowed enough for the foreign row to crowd the run's own row out"
  - "test_prod_prober_cadence.sql explicitly excluded from this criterion, with the category-error reason recorded"
  - "ARMS_FLOOR raised 413 -> 416 with every one of the 15 files citing the symbol individually dispositioned"
affects: [164.9-05, 164.9-06, 164.9-07, 164.9-08, 164.9-09, 164.9-10, 164.9-11]

# Actuals (#2632)
actuals:
  tokens: 14756
  tasks: 3
  commits: 3

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Foreign-row calibration shape: seed a genuinely competing foreign row at a plausible-but-not-dominant position in the deployed sweep's ordering, seed this run's own rows at the file's established extreme-epoch position so they dominate, execute the real deployed body (never re-type the predicate), assert positively about the run's own rows, then assert the foreign row's presence didn't change that verdict and that it is a distinguishable participant."
    - "Per-gate budget sizing is NOT transliterable across sibling gates: the reconcile gate's own Part 2 seeds four simultaneously tied heal-eligible candidates, so its calibration needed 4 own rows (not 2) and a LIMIT-4 mutation (not LIMIT-2) to avoid crowding its own pre-existing seeds."
    - "A migration's own self-verify step can carry the identical word-bounded LIMIT presence check the calibration's mutation trips — when it does, the mutation must re-base that self-verify too (in the same arm's apply list), or the migration apply aborts before the gate ever runs."

key-files:
  created: []
  modified:
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
    - supabase/tests/test_retention_orphaned_running.sql
    - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/gate-family-meta.test.ts

key-decisions:
  - "test_prod_prober_cadence.sql is NOT touched. It asserts about a cron schedule singleton (prod_prober_cadence_check, a STALE/FRESH liveness check), not multi-tenant row data — per-run isolation is a category error there, exactly as the plan predicted."
  - "The reconcile gate's calibration uses 4 own rows + 1 foreign row under a LIMIT-4 mutation, not the 2+1/LIMIT-2 shape the other two gates use. First attempt (2+1/LIMIT-2) broke Part 2's own four simultaneously-tied heal-eligible seeds nondeterministically (observed WRONG-ARM). Redesigned so budget 4 exactly fits Part 2's own four candidates (no crowd-out there) while still crowding exactly one of the calibration's own five."
  - "The reconcile gate's arm also had to re-base its target migration's own self-verify step (identical LIMIT-25 presence check), or the migration apply aborted before the gate could run (observed NO-IDENTITY on the first attempt)."
  - "ARMS_FLOOR raised 413 -> 416 in the same commit as the three arms. FILES_FLOOR (48) and WAIVED_CEILING (0) are unchanged — all three arms landed in already-annotated files and nothing was waived."
  - "15 files reference the ARMS_FLOOR symbol (plan required >=14). Only 3 carry the real pin (run.mjs, mutation-runner-floors.test.ts, gate-family-meta.test.ts); the other 12 are either prose citing the symbol, ci.yml's dynamic re-derivation, or two unrelated same-named constants (ci.yml's own sentinel-floor ARMS_FLOOR=213, and scripts/prod-prober/run.mjs's own ARMS_FLOOR=4) — full table below."

requirements-completed:
  - FANOUT-GLOBAL-01

coverage:
  - id: D1
    description: "Reaper gate (test_strategy_analytics_stuck_computing_reaper.sql) carries a foreign-row calibration (Part 7) armed by RED-UNDER-M arm 7/FANOUT-GLOBAL-01, observed RED under a narrowed sweep budget"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql --arm \"7/FANOUT-GLOBAL-01\" -> RED (identity ok), exit 2; full-file run -> arms 29/29/0, 0 defects"
        status: pass
    human_judgment: false
  - id: D2
    description: "Retention gate (test_retention_orphaned_running.sql) carries a foreign-row calibration (Part 4) armed by RED-UNDER-M arm 4/FANOUT-GLOBAL-01, observed RED under a narrowed sweep budget"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_retention_orphaned_running.sql --arm \"4/FANOUT-GLOBAL-01\" -> RED (identity ok), exit 2; full-file run -> arms 25/25/0, 0 defects"
        status: pass
    human_judgment: false
  - id: D3
    description: "Reconcile gate (test_reconcile_dropped_enqueue_sweep.sql) carries a foreign-row calibration (Part 5) armed by RED-UNDER-M arm 5/FANOUT-GLOBAL-01, observed RED under a narrowed sweep budget"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file supabase/tests/test_reconcile_dropped_enqueue_sweep.sql --arm \"5/FANOUT-GLOBAL-01\" -> RED (identity ok), exit 2; full-file run -> arms 38/38/0, 0 defects"
        status: pass
    human_judgment: false
  - id: D4
    description: "test_prod_prober_cadence.sql excluded and untouched, with the category-error reason (schedule singleton, not multi-tenant data) recorded"
    requirement: null
    verification:
      - kind: other
        ref: "git diff --stat -- supabase/tests/test_prod_prober_cadence.sql (empty)"
        status: pass
    human_judgment: false
  - id: D5
    description: "ARMS_FLOOR raised 413 -> 416; FILES_FLOOR (48) and WAIVED_CEILING (0) unchanged; all 15 pin-surface sites individually dispositioned"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: unit
        ref: "npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/gate-family-meta.test.ts -- 102/102 passed"
        status: pass
      - kind: other
        ref: "Clean full-corpus mutation run (no concurrent git activity): arms 416/416/0, biting 416, coverage files 48/75, exit 0, No defects"
        status: pass
    human_judgment: false

# Metrics
duration: ~2h7m
completed: 2026-09-21
status: complete
---

# Phase 164.9 Plan 04: Foreign-row calibration for the three global-sweep SQL gates Summary

**Three SQL gates (reaper, retention-orphaned-running, reconcile-dropped-enqueue-sweep) now each seed a genuine competing foreign row alongside their own, execute the real deployed `ORDER BY ... LIMIT` sweep, and prove — under a mutation that narrows the sweep's budget enough for the foreign row to win — that the gate's verdict is still about its own rows; `ARMS_FLOOR` moved 413 -> 416 with all 15 citing files individually dispositioned.**

## Performance

- **Duration:** ~2h7m
- **Tasks:** 3
- **Files modified:** 6
- **Commits:** 3

## Accomplishments

- Added Part 7 to the reaper gate: seeds a foreign `strategy_analytics` row at a competitive-but-not-dominant epoch alongside two of this run's own rows at the file's established extreme-epoch discipline, executes the real deployed reap body read out of the cron job's command, and asserts positively that both own rows AND the foreign row reach `'failed'` under the real budget, then asserts the foreign row is a distinct id. Armed by `7/FANOUT-GLOBAL-01`, which narrows the deployed `LIMIT 25` to `LIMIT 2` in `supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql`.
- Added Part 4 to the retention gate: the same shape against `compute_jobs` rows (`kind='derive_broker_dailies'`), narrowing arm A's `LIMIT 100` to `LIMIT 2` in `supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql`.
- Added Part 5 to the reconcile gate: a 4-own + 1-foreign shape (not the sibling gates' 2+1), because this gate's own Part 2 already seeds four simultaneously heal-eligible tied candidates — budget 4 exactly accommodates those without crowding them while still crowding exactly the calibration's own least-dominant row. Narrows `LIMIT 25` to `LIMIT 4` in `supabase/migrations/20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql`, and re-bases that same migration's own self-verify step (which carries an identical word-bounded `LIMIT 25` presence check that would otherwise abort the apply before the gate runs).
- `test_prod_prober_cadence.sql` left untouched — it asserts about a cron schedule singleton, not multi-tenant row data, so per-run isolation is a category error there.
- `ARMS_FLOOR` raised 413 -> 416 (one per new arm). Grepped the entire repo for the `ARMS_FLOOR` symbol (15 files, exceeding the plan's >=14 floor) and individually dispositioned every site — full table below.

## Task Commits

Each task was committed atomically:

1. **Task 1: Foreign-row calibration in the reaper gate** - `c9a275c3` (test)
2. **Task 2: The same calibration in the retention and reconcile sweep gates** - `26912aa3` (test)
3. **Task 3: Move only the ratchets that must move** - `93b2dd9c` (chore)

**Plan metadata:** committed alongside this SUMMARY (docs: complete plan)

## Files Created/Modified

- `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` - Part 7 foreign-row calibration + arm `7/FANOUT-GLOBAL-01` (29 -> 30 `RED-UNDER-M` occurrences)
- `supabase/tests/test_retention_orphaned_running.sql` - Part 4 foreign-row calibration + arm `4/FANOUT-GLOBAL-01` (25 -> 26 `RED-UNDER-M` occurrences)
- `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` - Part 5 foreign-row calibration + arm `5/FANOUT-GLOBAL-01` (38 -> 39 `RED-UNDER-M` occurrences)
- `scripts/mutation-runner/run.mjs` - `ARMS_FLOOR` 413 -> 416, dated CURRENCY comment naming all three arms
- `src/__tests__/mutation-runner-floors.test.ts` - `totalAnchored` expectation and every `GREEN_LOG` aggregate/per-file field that tracks the live corpus moved 413 -> 416; the three deliberately-synthetic decoy lines (`unreachable`/`lane-blocked`/`pending` sample filenames) left untouched per the file's own documented convention
- `src/__tests__/gate-family-meta.test.ts` - `KNOWN_THRESHOLD_SITES` label and dated justification updated to `ARMS_FLOOR=416`

## Decisions Made

- **Reconcile gate needed a different calibration shape than its two siblings** (see key-decisions above) — discovered empirically via two failed pg-lane attempts (a `WRONG-ARM` from crowding Part 2's own tied seeds, then a `NO-IDENTITY` from the target migration's own self-verify aborting the apply), not assumed up front. Each sibling gate's calibration was written against that gate's actual deployed sweep and its actual pre-existing seed pattern, not transliterated from the reaper's design.
- **`test_prod_prober_cadence.sql` confirmed and left untouched.** Read its own header: it measures `prod_prober_cadence_check()`, a STALE/FRESH cron-liveness singleton — there is no per-tenant row to isolate a foreign competitor against, so this criterion does not apply.
- **Pin-surface disposition (Task 3).** `grep -rln 'ARMS_FLOOR' src/ scripts/ .github/` found 15 files. Disposition of each:

| # | File | Disposition |
|---|---|---|
| 1 | `scripts/mutation-runner/run.mjs` | **MOVED** — the canonical pin, 413 -> 416 |
| 2 | `src/__tests__/mutation-runner-floors.test.ts` | **MOVED** — `totalAnchored` + `GREEN_LOG` aggregate/per-file fields re-derived to match the live corpus |
| 3 | `src/__tests__/gate-family-meta.test.ts` | **MOVED** — `KNOWN_THRESHOLD_SITES` registry label + dated justification |
| 4 | `.github/workflows/ci.yml` | **NOT MOVED** — reads `FILES_FLOOR`/`ARMS_FLOOR`/`WAIVED_CEILING` dynamically via a Node import of `run.mjs` (auto-reflects 416). Separately also declares an UNRELATED same-named shell variable `ARMS_FLOOR=213` (a per-file completion-sentinel floor for a different roster of gate files that does not include any of the three files this plan touched) — deliberately not moved, different quantity, name collision only |
| 5 | `scripts/baseline-content-drift-check.mjs` | **NOT MOVED** — prose citing the symbol, no number restated |
| 6 | `scripts/mutation-runner/fixtures/selftest/synthesised-identity-gate.sql` | **NOT MOVED** — generic prose, no number |
| 7 | `scripts/mutation-runner/GRAMMAR.md` | **NOT MOVED** — historical Phase 164.4 lineage prose describing a past corpus state, not a live pin |
| 8 | `scripts/mutation-runner/VAC06-DEMOS.md` | **NOT MOVED** — prose, no number restated |
| 9 | `scripts/prod-prober/run.mjs` | **NOT MOVED** — its OWN, entirely unrelated `export const ARMS_FLOOR = 4;` for a separate prod-prober arm registry (pyapi06/cron-obs/cron-drift/mt5) — name collision only |
| 10 | `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` | **NOT MOVED** — re-derives ci.yml's UNRELATED sentinel `ARMS_FLOOR=213` dynamically via regex extraction + its own derivation-table sum; unaffected by this plan |
| 11 | `src/__tests__/drift-check-scripts.test.ts` | **NOT MOVED** — hardcoded expectation for ci.yml's UNRELATED sentinel `ARMS_FLOOR=213`, not the mutation-runner's constant |
| 12 | `src/__tests__/lint-sql-gates.test.ts` | **NOT MOVED** — historical lineage prose ("...moved the other two (FILES_FLOOR 47 -> 48 and ARMS_FLOOR 402 -> 412...)"), no live assertion depends on the old number |
| 13 | `src/__tests__/mutation-annotation-parser.test.ts` | **NOT MOVED** — explicit prose stating it never restates `FILES_FLOOR`/`ARMS_FLOOR` as a number |
| 14 | `src/__tests__/prod-prober-wiring.test.ts` | **NOT MOVED** — imports and asserts the prod-prober's OWN unrelated `ARMS_FLOOR` (value 4) |
| 15 | `src/__tests__/vac06-mechanism-arms.test.ts` | **NOT MOVED** — generic prose, no number |

3 moved, 12 deliberately left alone (prose-only, dynamically-derived, or a genuinely different constant sharing the name).

## Mutation run tallies (quoted verbatim from the clean full-corpus run)

```
coverage: files 48/75
unreachable: 27 file(s) raise outside the runner's identity idiom — test_anon_execute_current_user_has_app_role.sql test_api_key_delete_atomicity.sql test_claim_compute_jobs_dedupe_partition.sql test_claim_kind_filter.sql test_cleanup_orphaned_api_keys_sweep.sql test_cleanup_wizard_drafts_race.sql test_commit_scenario_batch_auth_input.sql test_commit_scenario_batch_fingerprint_precondition.sql test_commit_scenario_batch_p1956_range.sql test_commit_scenario_batch_p1957_divested.sql test_compute_analytics_kind_retired.sql test_compute_jobs_rpc_error_clear_and_fanin.sql test_cutover_strategy_metrics_keys_atomic.sql test_data_deletion_requests_fk_set_null.sql test_enqueue_internal_destrict.sql test_get_latest_portfolio_analytics_for_user.sql test_handle_new_user_role_allowlist.sql test_log_audit_event_service_ceiling.sql test_mt5_exchange_boundary.sql test_portfolio_recompute_inflight_unique.sql test_retention_crons_safe.sql test_sanitize_user_hardening.sql test_sfox_exchange_boundary.sql test_staff_role_both_backfill.sql test_sync_status_preserves_warnings.sql test_sync_status_supersede_failed_per_kind.sql test_upsert_strategy_analytics_series_batch_privilege.sql (TODOS [REDUNDER-NONIDIOM])
lane-blocked: 0 file(s) probe pg_extension for pg_cron and are not yet annotated — (none; the lane hosts pg_cron since Phase 164.4.1, TODOS [REDUNDER-PGCRON] retired 2026-09-05)
lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires
pending: 0 idiom file(s) without RED-UNDER —
arms: 416/416/0   (executed/annotated/waived)
biting: 416   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 416   (arm lanes actually spawned — tallied inside runLane, independent of the 416 the verdict loop counted; plus 48 baseline / 48 restore leg(s))
per-arm lane time: mean 4.5s over 416 arm run(s)

✅ No defects. Every annotated arm bit its own arm first.
```

Exit code: 0.

## Deviations from Plan

### Auto-fixed Issues

None — no Rule 1/2/3 auto-fixes were required; every change was within the plan's explicit scope.

### Process note (not a plan deviation, but recorded for the audit trail)

**Contaminated first full-corpus run, discarded.** The first `node scripts/mutation-runner/run.mjs` (no `--file`/`--arm` filter) had been launched in the background before this session's Task 3 commit landed. Because that commit (`93b2dd9c`) landed on disk while the run was still executing, the run's own `dirty-checkout` guard correctly detected that the working tree changed shape mid-run (from "3 files modified" at the run's start snapshot to "3 files committed, clean" by the time it finished) and reported `1 defect(s): dirty-checkout`. Every arm-level and coverage tally in that run was otherwise identical to the clean re-run (`arms: 416/416/0`, `biting: 416`, `coverage: files 48/75`) — the defect was entirely attributable to running `git commit` concurrently with the corpus scan, not to any gate or arm. Discarded that run's verdict and re-ran the full corpus with no concurrent git activity; the tallies quoted above are from that clean run (confirmed via `git status --short` showing no changes before, during, or after). This is itself a small confirmation that the runner's dirty-checkout guard works as designed.

---

**Total deviations:** 0 auto-fixed. One process note (self-inflicted measurement contamination, corrected by re-running cleanly).
**Impact on plan:** None on scope or correctness. All acceptance criteria met as specified.

## Issues Encountered

- **Reaper gate:** first mutation attempt under-neutered — the `3/arm E/JOB-02` identity has 4 separate `RAISE EXCEPTION` sites, and only 1 neuter entry was supplied. Fixed by expanding to 4 identical neuter entries (matching an existing precedent elsewhere in the same file).
- **Retention gate:** narrowing arm A's `LIMIT 100` also broke Part 1's own word-bounded `LIMIT 100` occurrence count. Fixed with 3 additional neuter entries for `1/JOB-05/D-19` (that identity's 4 raise sites; 3 sequential neuters were needed to progress past the harmless occurrences and reach the one that actually fires).
- **Reconcile gate:** the simpler 2-own+1-foreign/`LIMIT 2` shape broke Part 2's own four simultaneously-tied heal-eligible seeds nondeterministically (`WRONG-ARM`). Redesigned to 4-own+1-foreign under `LIMIT 4`. The redesigned arm then hit `NO-IDENTITY` because the target migration's own self-verify step carries an identical `LIMIT 25` presence check that aborted the apply; fixed by adding a second `apply` step re-basing that self-verify to `IF FALSE THEN`, following the same file's own pre-existing precedent for the identical situation.
- All three were resolved by iterating against the real pg-lane rather than assumed a priori — each fix was observed RED-then-fixed on the disposable cluster before being trusted.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three genuinely global-shaped sweep gates now carry a proven, armed foreign-row calibration; the fourth structurally-similar gate is confirmed and documented as a category-error exclusion.
- `ARMS_FLOOR` ratchet is current at 416 and every dependent pin/test/fixture that must track it does; no floor was relaxed, no ceiling widened, `WAIVED_CEILING` stays at 0.
- No blockers for downstream 164.9 plans.

---
*Phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro*
*Completed: 2026-09-21*

## Self-Check: PASSED

All 7 files confirmed present on disk (3 modified gate files, 3 modified ratchet/test files, this SUMMARY). All 3 task commit hashes (`c9a275c3`, `26912aa3`, `93b2dd9c`) confirmed present in `git log`.
