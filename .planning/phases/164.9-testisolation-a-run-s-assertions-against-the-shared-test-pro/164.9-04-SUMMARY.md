---
phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro
plan: 04
subsystem: sql-gate-integrity
tags: [mutation-testing, fanout-global-01, sql-gates, postgres, red-under-m, pg-lane]

# Dependency graph
requires:
  - phase: 164.9-01
    provides: "FANOUT-GLOBAL-01 as a real TODOS.md entry with owner, date, trigger and five family members"
provides:
  - "Foreign-row calibration proving the reaper, retention-orphaned-running and reconcile-dropped-enqueue-sweep gates verify THEIR OWN rows under the real deployed global sweep, even against a genuine competing foreign row"
  - "Nine RED-UNDER-M arms total (three original + six added in a fix round), each observed to redden its own calibration section when the deployed sweep's row budget is narrowed enough for the foreign row to crowd the run's own row out"
  - "test_prod_prober_cadence.sql explicitly excluded from this criterion, with the category-error reason recorded"
  - "ARMS_FLOOR raised 413 -> 416 -> 422 across two commits, with the full 26-file live pin surface (not the narrower 15-file scope an earlier grep found) individually dispositioned"
affects: [164.9-05, 164.9-06, 164.9-07, 164.9-08, 164.9-09, 164.9-10, 164.9-11]

# Actuals (#2632)
actuals:
  tokens: 19630
  tasks: 3
  commits: 4

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Foreign-row calibration shape: seed a genuinely competing foreign row at a plausible-but-not-dominant position in the deployed sweep's ordering, seed this run's own rows at the file's established extreme-epoch position so they dominate, execute the real deployed body (never re-type the predicate), assert positively about the run's own rows, then assert the foreign row's presence didn't change that verdict and that it is a distinguishable participant."
    - "Per-gate budget sizing is NOT transliterable across sibling gates: the reconcile gate's own Part 2 seeds four simultaneously tied heal-eligible candidates, so its calibration needed 4 own rows (not 2) and a LIMIT-4 mutation (not LIMIT-2) to avoid crowding its own pre-existing seeds."
    - "A migration's own self-verify step can carry the identical word-bounded LIMIT presence check the calibration's mutation trips — when it does, the mutation must re-base that self-verify too (in the same arm's apply list), or the migration apply aborts before the gate ever runs."
    - "A section-coverage vitest gate (164.4-02) requires EVERY distinct `TEST FAILED (...)` identity string in a gate file to have its own RED-UNDER-M twin naming that exact identity — a multi-assertion calibration part with sub-identities (`/own-1`, `/foreign`) needs one arm PER sub-identity, not one arm for the whole part."
    - "When a sandwiched row (never at an extreme rank under either ORDER BY direction, at any budget the file's other parts allow) cannot be isolated by LIMIT/direction alone, add a pure rank-shifter row with no assertion of its own so an EXISTING safe mutation excludes the target row too, differentiated from the existing arm only by which identity is neutered — cheaper and safer than inventing a new budget value."

key-files:
  created: []
  modified:
    - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
    - supabase/tests/test_retention_orphaned_running.sql
    - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
    - scripts/mutation-runner/run.mjs
    - src/__tests__/mutation-runner-floors.test.ts
    - src/__tests__/gate-family-meta.test.ts
    - src/__tests__/mutation-annotation-parser.test.ts

key-decisions:
  - "test_prod_prober_cadence.sql is NOT touched. It asserts about a cron schedule singleton (prod_prober_cadence_check, a STALE/FRESH liveness check), not multi-tenant row data — per-run isolation is a category error there, exactly as the plan predicted."
  - "The reconcile gate's base calibration uses 4 own rows + 1 foreign row under a LIMIT-4 mutation, not the 2+1/LIMIT-2 shape the other two gates use. First attempt (2+1/LIMIT-2) broke Part 2's own four simultaneously-tied heal-eligible seeds nondeterministically (observed WRONG-ARM). Redesigned so budget 4 exactly fits Part 2's own four candidates (no crowd-out there) while still crowding exactly one of the calibration's own five."
  - "The reconcile gate's base arm also had to re-base its target migration's own self-verify step (identical LIMIT-25 presence check), or the migration apply aborted before the gate could run (observed NO-IDENTITY on the first attempt)."
  - "ARMS_FLOOR raised 413 -> 416 in the plan's original three-task commit, then 416 -> 422 in a follow-up fix commit after a coordinator review found six uncovered sub-identities. FILES_FLOOR (48) and WAIVED_CEILING (0) are unchanged throughout — all nine arms landed in already-annotated files and nothing was waived."
  - "FIX ROUND (post-handback review): the section-coverage vitest gate (164.4-02 in mutation-annotation-parser.test.ts) found six `TEST FAILED (...)` sub-identities across the three new calibration parts with no RED-UNDER-M twin — `own-1`/`foreign` in reaper and retention, `own`/`foreign` in reconcile. Each got its own arm: reaper and retention's own-1/foreign pairs reverse the deployed ORDER BY (ASC->DESC) to exclude the most-dominant own row instead, or narrow the budget to 1 with the less-dominant-own identity neutered to exclude the foreign row. Reconcile's foreign identity is geometrically sandwiched between two own ranks at every LIMIT/direction pair a safe (>=4) budget allows, so a sixth own row (v_own_5, a pure rank-shifter with no assertion of its own) was added to make the SAME LIMIT-4 mutation the base arm already uses exclude the foreign row too."
  - "The original pin-surface grep (`src/ scripts/ .github/`) found 15 files and satisfied the plan's own >=14 acceptance threshold, but a repo-wide grep (excluding only node_modules/.git) finds 26 non-.planning files. Re-derived and dispositioned all 26 in the fix round: the 11 additional files are either dated historical CHANGELOG/TODOS/docs entries (append-only lineage, never edited) or pure prose citing the symbol, none restating a live number that needed to move."
  - "Two additional independent census pins were discovered stale in mutation-annotation-parser.test.ts during the fix round (`armsSeen`/`stepsSeen` at one test, `needles.length` at another) — missed by the original Task 3 sweep because neither restates the literal string `ARMS_FLOOR`, so a grep for that symbol never finds them. Both were run (not reasoned about) and corrected to their actual post-fix values (422 and 441 respectively)."

requirements-completed:
  - FANOUT-GLOBAL-01

coverage:
  - id: D1
    description: "Reaper gate (test_strategy_analytics_stuck_computing_reaper.sql) carries a foreign-row calibration (Part 7) with THREE armed identities: the base own-row crowd-out (7/FANOUT-GLOBAL-01), the most-dominant-own sanity check (7/FANOUT-GLOBAL-01/own-1), and the foreign-row check (7/FANOUT-GLOBAL-01/foreign) — all three observed RED under their own distinct mutation"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file <gate> --arm <each of the three arms> -> RED (identity ok), exit 2 each; full-file run -> arms 31/31/0, 0 defects"
        status: pass
    human_judgment: false
  - id: D2
    description: "Retention gate (test_retention_orphaned_running.sql) carries a foreign-row calibration (Part 4) with the same three-identity shape as the reaper gate, all three armed and observed RED"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file <gate> --arm <each of the three arms> -> RED (identity ok), exit 2 each; full-file run -> arms 27/27/0, 0 defects"
        status: pass
    human_judgment: false
  - id: D3
    description: "Reconcile gate (test_reconcile_dropped_enqueue_sweep.sql) carries a foreign-row calibration (Part 5) with three armed identities (5/FANOUT-GLOBAL-01, /own, /foreign), the latter two requiring a sixth rank-shifter own row (v_own_5) to make the foreign identity isolable under a Part-2-safe budget"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: other
        ref: "node scripts/mutation-runner/run.mjs --file <gate> --arm <each of the three arms> -> RED (identity ok), exit 2 each (foreign and own each re-run twice to confirm no flakiness against Part 2's own nondeterminism); full-file run -> arms 40/40/0, 0 defects (re-run twice)"
        status: pass
    human_judgment: false
  - id: D4
    description: "test_prod_prober_cadence.sql excluded and untouched, with the category-error reason (schedule singleton, not multi-tenant data) recorded"
    requirement: null
    verification:
      - kind: other
        ref: "git diff --stat -- supabase/tests/test_prod_prober_cadence.sql (empty)"
        status: pass
    human_judgment: false
  - id: D5
    description: "ARMS_FLOOR raised 413 -> 416 -> 422; FILES_FLOOR (48) and WAIVED_CEILING (0) unchanged; the full 26-file live pin surface individually dispositioned"
    requirement: "FANOUT-GLOBAL-01"
    verification:
      - kind: unit
        ref: "npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/gate-family-meta.test.ts -- 102/102 passed"
        status: pass
      - kind: unit
        ref: "npx vitest run src/__tests__/mutation-annotation-parser.test.ts -- 108/108 passed (the 164.4-02 section-coverage gate, and the two previously-stale armsSeen/stepsSeen/needles.length pins, all green)"
        status: pass
      - kind: other
        ref: "Clean full-corpus mutation run (no concurrent git activity): coverage files 48/75, arms 422/422/0, biting 422, lane-invocations 422, exit 0, No defects"
        status: pass
    human_judgment: false

# Metrics
duration: ~4h17m
completed: 2026-09-21
status: complete
---

# Phase 164.9 Plan 04: Foreign-row calibration for the three global-sweep SQL gates Summary

**Three SQL gates (reaper, retention-orphaned-running, reconcile-dropped-enqueue-sweep) each seed a genuine competing foreign row alongside their own, execute the real deployed `ORDER BY ... LIMIT` sweep, and prove — under three distinct mutations per gate (nine total) — that every one of the calibration's own assertions, not just the headline one, can independently fail; `ARMS_FLOOR` moved 413 -> 416 -> 422 across two commits, with the full 26-file live pin surface individually dispositioned.**

## Performance

- **Duration:** ~4h17m
- **Tasks:** 3 (plus one post-handback fix round)
- **Files modified:** 7
- **Commits:** 4

## Accomplishments

- Added Part 7 to the reaper gate: seeds a foreign `strategy_analytics` row at a competitive-but-not-dominant epoch alongside two of this run's own rows at the file's established extreme-epoch discipline, executes the real deployed reap body read out of the cron job's command, and asserts positively that both own rows AND the foreign row reach `'failed'` under the real budget, then asserts the foreign row is a distinct id. Three armed identities: `7/FANOUT-GLOBAL-01` (base own-row crowd-out, `LIMIT 25`->`2`), `7/FANOUT-GLOBAL-01/own-1` (ORDER BY `ASC`->`DESC` at the same budget), `7/FANOUT-GLOBAL-01/foreign` (`LIMIT 25`->`1`, neutering the base identity).
- Added Part 4 to the retention gate: the same three-identity shape against `compute_jobs` rows (`kind='derive_broker_dailies'`), against `supabase/migrations/20260826140000_compute_jobs_error_kind_orphaned.sql`.
- Added Part 5 to the reconcile gate: a 4-own + 1-foreign base shape (not the sibling gates' 2+1), because this gate's own Part 2 already seeds four simultaneously heal-eligible tied candidates — budget 4 exactly accommodates those without crowding them. A sixth own row (v_own_5, a pure rank-shifter) was added in the fix round so the foreign identity — geometrically sandwiched between two own ranks at every safe LIMIT/direction pair — could be armed without disturbing Part 2. Three armed identities: `5/FANOUT-GLOBAL-01`, `5/FANOUT-GLOBAL-01/own` (DESC reversal), `5/FANOUT-GLOBAL-01/foreign` (same LIMIT-4 mutation as the base arm, differentiated only by which identity is neutered).
- `test_prod_prober_cadence.sql` left untouched — it asserts about a cron schedule singleton, not multi-tenant row data, so per-run isolation is a category error there.
- `ARMS_FLOOR` raised 413 -> 416 (three base arms) then 416 -> 422 (six fix-round arms). Grepped the entire repo for the `ARMS_FLOOR` symbol twice — 15 files with the original narrower scope, 26 with a full repo-wide re-derivation in the fix round — and individually dispositioned every site both times.
- **Fix round:** a coordinator review ran the corpus's own section-coverage vitest gate (164.4-02), which the original three-task verify set never invoked, and found six `TEST FAILED (...)` sub-identities with no twin. Armed all six, discovered and corrected two additional independently-stale census pins in the same test file that an earlier sweep had missed (because neither restates the literal symbol `ARMS_FLOOR`), and re-verified the full corpus clean.

## Task Commits

Each task was committed atomically:

1. **Task 1: Foreign-row calibration in the reaper gate** - `c9a275c3` (test)
2. **Task 2: The same calibration in the retention and reconcile sweep gates** - `26912aa3` (test)
3. **Task 3: Move only the ratchets that must move** - `93b2dd9c` (chore)
4. **Fix round: arm the six section-coverage gaps the vitest gate found** - `e50c2705` (fix)

**Plan metadata:** `17768c2b` (docs: complete plan, written before the fix round; this SUMMARY was updated in place afterward rather than re-committed as a fifth commit, since GSD's SUMMARY.md convention is a single evolving artifact per plan)

## Files Created/Modified

- `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` - Part 7 foreign-row calibration, three armed identities (29 -> 32 `RED-UNDER-M` occurrences across both rounds)
- `supabase/tests/test_retention_orphaned_running.sql` - Part 4 foreign-row calibration, three armed identities (25 -> 28 `RED-UNDER-M` occurrences)
- `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` - Part 5 foreign-row calibration, three armed identities plus a sixth rank-shifter own row (38 -> 41 `RED-UNDER-M` occurrences)
- `scripts/mutation-runner/run.mjs` - `ARMS_FLOOR` 413 -> 416 -> 422, two dated CURRENCY comments
- `src/__tests__/mutation-runner-floors.test.ts` - `totalAnchored` expectation and every `GREEN_LOG` aggregate/per-file field moved twice (413->416->422); the three deliberately-synthetic decoy lines left untouched per the file's own documented convention
- `src/__tests__/gate-family-meta.test.ts` - `KNOWN_THRESHOLD_SITES` label and dated justification updated twice, now `ARMS_FLOOR=422`
- `src/__tests__/mutation-annotation-parser.test.ts` - fix round only: two independently-stale census pins corrected (`armsSeen`/`stepsSeen` 413/429 -> 422/441; `needles.length` 429 -> 441), each with a dated CURRENCY comment

## Decisions Made

- **Reconcile gate needed a different calibration shape than its two siblings, twice.** First for its base arm (see key-decisions above), discovered via a `WRONG-ARM` then a `NO-IDENTITY` failure. Then again in the fix round for its `/foreign` identity, discovered via a second `WRONG-ARM` (`2/arm C5b/...`, an unrelated Part-2 tie loser) when a naive `LIMIT 3` mutation was tried first.
- **`test_prod_prober_cadence.sql` confirmed and left untouched.** Read its own header: it measures `prod_prober_cadence_check()`, a STALE/FRESH cron-liveness singleton — there is no per-tenant row to isolate a foreign competitor against, so this criterion does not apply.
- **Rank-shifter row pattern (reconcile's v_own_5).** When a row cannot be isolated by LIMIT/direction alone because it is sandwiched between two OTHER ranks under every direction a safe budget allows, adding a rank-shifting row with no assertion of its own — chosen to be more dominant than the existing rows — lets an EXISTING, already-safe mutation exclude the target row too. The new arm differs from the existing one purely in which identity is neutered, not in the mutation itself. This avoided introducing any new LIMIT value below the floor Part 2 requires.
- **Pin-surface disposition, done twice.** First pass (`grep -rln 'ARMS_FLOOR' src/ scripts/ .github/`, matching the plan's own verify command) found 15 files, satisfying the plan's `>=14` threshold. A coordinator review pointed out the true repo-wide surface is 26 files (non-`.planning/`); re-derived with `grep -rln 'ARMS_FLOOR' . --exclude-dir=node_modules --exclude-dir=.git` and dispositioned every one of the 11 additional files. Full table below is the reconciled 26-file version, superseding the original 15-file table.

**Full pin-surface disposition (26 files, `grep -rln 'ARMS_FLOOR'` repo-wide minus `node_modules`/`.git`/`.planning`):**

| # | File | Disposition |
|---|---|---|
| 1 | `scripts/mutation-runner/run.mjs` | **MOVED** — the canonical pin, 413 -> 416 -> 422 |
| 2 | `src/__tests__/mutation-runner-floors.test.ts` | **MOVED** — `totalAnchored` + `GREEN_LOG` aggregate/per-file fields re-derived to match the live corpus, twice |
| 3 | `src/__tests__/gate-family-meta.test.ts` | **MOVED** — `KNOWN_THRESHOLD_SITES` registry label + dated justification, twice |
| 4 | `src/__tests__/mutation-annotation-parser.test.ts` | **MOVED** (fix round only) — never restates the literal symbol `ARMS_FLOOR`, so it was missed on the first pass; independently tracks the same corpus growth under local names `armsSeen`/`stepsSeen`/`needles.length`, both of which were stale and corrected |
| 5 | `.github/workflows/ci.yml` | **NOT MOVED** — reads `FILES_FLOOR`/`ARMS_FLOOR`/`WAIVED_CEILING` dynamically via a Node import of `run.mjs` (auto-reflects 422). Separately also declares an UNRELATED same-named shell variable `ARMS_FLOOR=213`, a per-file completion-sentinel floor for a different roster of gate files not touched by this plan — deliberately not moved, name collision only |
| 6 | `scripts/baseline-content-drift-check.mjs` | **NOT MOVED** — prose citing the symbol, no number restated |
| 7 | `scripts/mutation-runner/fixtures/selftest/synthesised-identity-gate.sql` | **NOT MOVED** — generic prose, no number |
| 8 | `scripts/mutation-runner/GRAMMAR.md` | **NOT MOVED** — historical Phase 164.4 lineage prose describing a past corpus state, not a live pin |
| 9 | `scripts/mutation-runner/VAC06-DEMOS.md` | **NOT MOVED** — prose, no number restated |
| 10 | `scripts/prod-prober/run.mjs` | **NOT MOVED** — its OWN, entirely unrelated `export const ARMS_FLOOR = 4;` for a separate prod-prober arm registry — name collision only |
| 11 | `src/__tests__/contracts/ci-anti-skip-gate.contract.test.ts` | **NOT MOVED** — re-derives ci.yml's UNRELATED sentinel `ARMS_FLOOR=213` dynamically; unaffected by this plan |
| 12 | `src/__tests__/drift-check-scripts.test.ts` | **NOT MOVED** — hardcoded expectation for ci.yml's UNRELATED sentinel `ARMS_FLOOR=213`, not the mutation-runner's constant |
| 13 | `src/__tests__/lint-sql-gates.test.ts` | **NOT MOVED** — historical lineage prose, no live assertion depends on the old number |
| 14 | `src/__tests__/prod-prober-wiring.test.ts` | **NOT MOVED** — imports and asserts the prod-prober's OWN unrelated `ARMS_FLOOR` (value 4) |
| 15 | `src/__tests__/vac06-mechanism-arms.test.ts` | **NOT MOVED** — generic prose, no number |
| 16 | `analytics-service/tests/test_mt5_relogin.py` | **NOT MOVED** — prose citing this repo's own dated `ARMS_FLOOR` correction as an analogy for its OWN documentation discipline; unrelated Python test |
| 17 | `analytics-service/tests/test_mt5_session_monitor.py` | **NOT MOVED** — same prose-analogy citation as above |
| 18 | `CHANGELOG.md` | **NOT MOVED** — dated, append-only historical release entries; never edited retroactively per this repo's own CHANGELOG discipline |
| 19 | `CLAUDE.md` | **NOT MOVED** — an explicit "read by symbol, never restate a number" instruction; deliberately carries no current value to go stale |
| 20 | `docs/sql-gate-lineage.md` | **NOT MOVED** — dated lineage history for Phases 164.3-164.7, explicitly nothing in it is a live constant |
| 21 | `TODOS.md` | **NOT MOVED** — dated, append-only backlog entries; historical readings preserved as lineage |
| 22 | `supabase/tests/test_enqueue_internal_destrict.sql` | **NOT MOVED** — prose citing the symbol, no number restated |
| 23 | `supabase/tests/test_ledger_refresh_composite_arm.sql` | **NOT MOVED** — prose ("read by symbol"), no number restated |
| 24 | `supabase/tests/test_ledger_refresh_fanout.sql` | **NOT MOVED** — prose ("read by symbol"), no number restated |
| 25 | `supabase/tests/test_sync_status_curated_sentence_survives.sql` | **NOT MOVED** — prose, no number restated |
| 26 | `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` | **NOT MOVED (for its ARMS_FLOOR mentions)** — this plan's own file, modified for its calibration content, but its three `ARMS_FLOOR` citations are pure prose naming the symbol, not a restated number |

4 moved (one discovered only in the fix round), 22 deliberately left alone (dated historical lineage, "read by symbol" instructions, dynamically-derived, or a genuinely different constant sharing the name).

## Mutation run tallies (quoted verbatim from the clean full-corpus run, fix round, no concurrent git activity)

```
coverage: files 48/75
unreachable: 27 file(s) raise outside the runner's identity idiom — test_anon_execute_current_user_has_app_role.sql test_api_key_delete_atomicity.sql test_claim_compute_jobs_dedupe_partition.sql test_claim_kind_filter.sql test_cleanup_orphaned_api_keys_sweep.sql test_cleanup_wizard_drafts_race.sql test_commit_scenario_batch_auth_input.sql test_commit_scenario_batch_fingerprint_precondition.sql test_commit_scenario_batch_p1956_range.sql test_commit_scenario_batch_p1957_divested.sql test_compute_analytics_kind_retired.sql test_compute_jobs_rpc_error_clear_and_fanin.sql test_cutover_strategy_metrics_keys_atomic.sql test_data_deletion_requests_fk_set_null.sql test_enqueue_internal_destrict.sql test_get_latest_portfolio_analytics_for_user.sql test_handle_new_user_role_allowlist.sql test_log_audit_event_service_ceiling.sql test_mt5_exchange_boundary.sql test_portfolio_recompute_inflight_unique.sql test_retention_crons_safe.sql test_sanitize_user_hardening.sql test_sfox_exchange_boundary.sql test_staff_role_both_backfill.sql test_sync_status_preserves_warnings.sql test_sync_status_supersede_failed_per_kind.sql test_upsert_strategy_analytics_series_batch_privilege.sql (TODOS [REDUNDER-NONIDIOM])
lane-blocked: 0 file(s) probe pg_extension for pg_cron and are not yet annotated — (none; the lane hosts pg_cron since Phase 164.4.1, TODOS [REDUNDER-PGCRON] retired 2026-09-05)
lane-probe: pg_cron AVAILABLE — lane-blocked class is empty, as a hosting lane requires
arms: 422/422/0   (executed/annotated/waived)
biting: 422   (executed arms that reddened their OWN arm first — the quantity ARMS_FLOOR bounds)
lane-invocations: 422   (arm lanes actually spawned — tallied inside runLane, independent of the 422 the verdict loop counted; plus 48 baseline / 48 restore leg(s))
per-arm lane time: mean 4.1s over 422 arm run(s)

✅ No defects. Every annotated arm bit its own arm first.
```

Exit code: 0. `git status --porcelain` confirmed empty immediately after.

### Verbatim RED messages for the six fix-round arms (per gate)

- `7/FANOUT-GLOBAL-01/own-1` (reaper): `TEST FAILED (7/FANOUT-GLOBAL-01/own-1): this run's own MOST-dominant stranded row was not reaped (got %). It should win a slot at any budget >= 1; if it did not, the seed itself is broken and this calibration proves nothing.`
- `7/FANOUT-GLOBAL-01/foreign` (reaper): `TEST FAILED (7/FANOUT-GLOBAL-01/foreign): the foreign competitor row was not reaped under the deployed budget (got %). A foreign row that never enters the sweep is a token, not a competitor, and this calibration would prove nothing about isolation under real contention.`
- `4/FANOUT-GLOBAL-01/own-1` (retention): `TEST FAILED (4/FANOUT-GLOBAL-01/own-1): this run's own MOST-dominant claimed-and-stale row was not terminalized (got %). It should win a slot at any budget >= 1; if it did not, the seed itself is broken and this calibration proves nothing.`
- `4/FANOUT-GLOBAL-01/foreign` (retention): `TEST FAILED (4/FANOUT-GLOBAL-01/foreign): the foreign competitor row was not terminalized under the deployed budget (got %). A foreign row that never enters the sweep is a token, not a competitor, and this calibration would prove nothing about isolation under real contention.`
- `5/FANOUT-GLOBAL-01/own` (reconcile): `TEST FAILED (5/FANOUT-GLOBAL-01/own): one of this run's own MORE-dominant grace-past candidates was not healed (got %, %, % sweep-marked jobs for own_1/own_2/own_3, expected 1 each). Each should win a slot at any budget >= 3; if one did not, the seed itself is broken and this calibration proves nothing.`
- `5/FANOUT-GLOBAL-01/foreign` (reconcile): `TEST FAILED (5/FANOUT-GLOBAL-01/foreign): the foreign competitor row was not healed under the deployed budget (% sweep-marked jobs, expected 1). A foreign row that never enters the sweep is a token, not a competitor, and this calibration would prove nothing about isolation under real contention.`

Each was confirmed via `node scripts/mutation-runner/run.mjs --file <gate> --arm "<identity>"` scoring `RED (identity ok)`, exit 2, `No defects in the narrowed scope.`

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Six calibration sub-identities carried no RED-UNDER-M twin, so their assertions could never be proven able to fail**
- **Found during:** Post-handback coordinator review, running the corpus's own section-coverage vitest gate (164.4-02 in `mutation-annotation-parser.test.ts`), which the original three-task `<verify>` set never invoked.
- **Issue:** The original three arms each armed only the FIRST `TEST FAILED (...)` identity in their calibration part. Each part also raises two OR MORE further identities under distinct sub-names (`/own-1`, `/foreign`) — assertions about the most-dominant own row and about the foreign row itself — none of which had their own mutation twin. A vacuous-assertion class this whole plan exists to eliminate, present in the plan's own output.
- **Fix:** Added one arm per sub-identity (six total): reaper and retention's `own-1` pairs reverse the deployed ORDER BY (ASC->DESC) at the same budget to exclude the most-dominant own row instead; their `foreign` pairs narrow the budget to 1 (ASC unchanged), neutering the identity that would otherwise fire first. Reconcile needed a structurally different fix (see Issues Encountered) because its foreign row is geometrically sandwiched between two own ranks under every direction a safe budget allows.
- **Files modified:** `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql`, `supabase/tests/test_retention_orphaned_running.sql`, `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql`, `scripts/mutation-runner/run.mjs`, `src/__tests__/mutation-runner-floors.test.ts`, `src/__tests__/gate-family-meta.test.ts`, `src/__tests__/mutation-annotation-parser.test.ts`
- **Verification:** All six arms individually confirmed `RED (identity ok)`, exit 2. Full-file re-runs on all three gates: 0 defects each (reconcile re-run twice against its own known nondeterminism). Full-corpus clean run: `arms: 422/422/0`, 0 defects, exit 0. `npx vitest run src/__tests__/mutation-annotation-parser.test.ts`: 108/108 passed.
- **Committed in:** `e50c2705`

---

**Total deviations:** 1 auto-fixed (Rule 1 — a genuine gap in the plan's own coverage, caught by the corpus's own gate and closed in the same session). Two process notes below, neither a scope deviation.
**Impact on plan:** Strengthens the plan's core claim — every assertion in every calibration part is now individually proven falsifiable, not just the headline one. No scope creep: all six new arms are within the plan's own `FANOUT-GLOBAL-01` objective.

### Process note 1 (not a plan deviation, but recorded for the audit trail)

**Contaminated first full-corpus run, discarded.** During the original three-task work, a background `node scripts/mutation-runner/run.mjs` (no `--file`/`--arm` filter) had been launched before the Task 3 commit landed. Because that commit landed on disk while the run was still executing, the run's own `dirty-checkout` guard correctly detected the working tree changed shape mid-run and reported `1 defect(s): dirty-checkout`. Every arm-level and coverage tally in that run was otherwise identical to a clean re-run — the defect was entirely attributable to running `git commit` concurrently with the corpus scan. Discarded that run's verdict and re-ran cleanly. The same discipline (no concurrent git activity during a full-corpus run) was followed for both full-corpus runs in the fix round.

### Process note 2 (not a plan deviation, but recorded for the audit trail)

**Two additional stale census pins discovered in the fix round, unrelated to the six-arm gap itself.** `src/__tests__/mutation-annotation-parser.test.ts` independently tracks corpus growth via locally-named variables `armsSeen`/`stepsSeen` (one test) and `needles.length` (a second test) — a SEPARATE derivation from `ARMS_FLOOR` per this repo's own documented "four independent censuses" pattern. Neither pin restates the literal string `ARMS_FLOOR`, so the original Task 3 grep-based sweep never found them, and they had silently gone stale reflecting a corpus state from BEFORE even this plan's original three arms (413/429, last touched at an earlier, unrelated phase). Running the section-coverage test after fixing the coverage gap surfaced both as failures (`expected 422 to be 413`, `expected 441 to be 429`); both were corrected to their measured actual values with dated CURRENCY comments, per this repo's own "RUN, not reasoned about" doctrine.

## Issues Encountered

- **Reaper gate (base arm):** first mutation attempt under-neutered — the `3/arm E/JOB-02` identity has 4 separate `RAISE EXCEPTION` sites, and only 1 neuter entry was supplied. Fixed by expanding to 4 identical neuter entries.
- **Retention gate (base arm):** narrowing arm A's `LIMIT 100` also broke Part 1's own word-bounded `LIMIT 100` occurrence count. Fixed with 3 additional neuter entries for `1/JOB-05/D-19`.
- **Reconcile gate (base arm):** the simpler 2-own+1-foreign/`LIMIT 2` shape broke Part 2's own four simultaneously-tied heal-eligible seeds nondeterministically (`WRONG-ARM`). Redesigned to 4-own+1-foreign under `LIMIT 4`, then hit `NO-IDENTITY` because the target migration's own self-verify step carries an identical `LIMIT 25` presence check; fixed by re-basing that self-verify in the same arm's apply list.
- **Reconcile gate (fix round, `/foreign` identity):** a naive `LIMIT 3` mutation (matching the shape used for `/own-1`/`/foreign` in the sibling gates) reddened `WRONG-ARM(2/arm C5b/JOB-04/R3/SC#3)` instead — Part 2's own four tied candidates need budget >= 4, and 3 crowds one of them out nondeterministically. Root cause: the foreign row's timestamp places it strictly between own_3 and own_4 in the deployed sort, so no LIMIT/direction pair on a safe (>=4) budget can exclude it alone. Fixed by adding a sixth own row (v_own_5, seeded more dominant than own_1, asserting nothing about itself) so the EXISTING base arm's exact LIMIT-4 mutation also excludes the foreign row, differentiated only by neutering the base identity for this arm.
- All arms across both rounds were resolved by iterating against the real pg-lane rather than assumed a priori — every fix was observed RED-then-fixed on the disposable cluster before being trusted, and the reconcile arms were re-run twice each in the fix round specifically to rule out flakiness from Part 2's documented nondeterminism.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- All three genuinely global-shaped sweep gates now carry a proven, armed foreign-row calibration where EVERY distinct assertion (not just the headline one) is individually falsifiable; the fourth structurally-similar gate is confirmed and documented as a category-error exclusion.
- `ARMS_FLOOR` ratchet is current at 422 and every dependent pin/test/fixture that must track it does, including two previously-stale pins outside the symbol's own literal grep surface that were found and fixed in this same session. No floor was relaxed, no ceiling widened, `WAIVED_CEILING` stays at 0.
- No blockers for downstream 164.9 plans.

---
*Phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro*
*Completed: 2026-09-21*

## Self-Check: PASSED

All 7 modified files confirmed present on disk. All 5 commit hashes on this branch since the plan's base — the 4 code-change commits counted in `actuals.commits` (`c9a275c3`, `26912aa3`, `93b2dd9c`, `e50c2705`) plus the docs/metadata commit (`17768c2b`) — confirmed present in `git log`. `git status --porcelain` confirmed empty at the end of the fix round.
