---
phase: 164.9-testisolation
verified: 2026-09-21T22:55:00Z
status: human_needed
score: 11/13 success criteria verified
verified_at_sha: ab33064d342b1dd62bc805cfbf50a6461add0fae
drift_subjects:
  - .github/workflows/ci.yml
  - .github/workflows/supabase-migrate.yml
  - .github/workflows/test-restore-from-baseline.yml
  - .github/workflows/mutex-probe.yml
  - scripts/shared-test-db-keys.sh
  - scripts/wait-for-test-schema-apply.sh
  - scripts/mutex-dead-holder-verdict.sh
  - scripts/pg-lane/mutex-dead-holder-lane.sh
  - scripts/test-ledger-drift-check.sh
  - scripts/restore-test-from-baseline.sh
  - scripts/test-only-normalize-analytics-url.sh
  - scripts/lint-migration-data-dependence.mjs
  - scripts/lint-migration-data-dependence-baseline.txt
  - scripts/live-db-execution-ledger.mjs
  - scripts/live-db-execution-ledger.txt
  - scripts/live-db-execution-reporter.mjs
  - scripts/live-db-fixture-drift-census.mjs
  - scripts/live-db-fixture-drift-baseline.txt
  - scripts/live-db-lane-corpus.mjs
  - scripts/local-stack/run.sh
  - scripts/mutation-runner/run.mjs
  - vitest.livedb.config.ts
  - vitest.livedb.globalsetup.ts
  - analytics-service/tests/live_db_transport.py
  - analytics-service/tests/conftest.py
  - supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql
  - supabase/tests/test_retention_orphaned_running.sql
  - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
  - supabase/tests/test_analytics_service_settings_and_vault_tick.sql
  - src/__tests__/mutex-dead-holder-verdict.test.ts
  - src/__tests__/live-db-execution-ledger.contract.test.ts
  - src/__tests__/lint-migration-data-dependence.test.ts
  - src/__tests__/live-db-fixture-drift.test.ts
  - src/__tests__/drift-check-scripts.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/restore-test-from-baseline.test.ts
behavior_unverified: 1
overrides_applied: 0
behavior_unverified_items:
  - truth: "Criterion 6 — shared TEST's cron demonstrably no longer reaches PROD compute, shown by reading the LIVE row"
    test: "Read `system_settings.analytics_service_url` on shared TEST through a TEST-scoped, read-only channel, after running the identity-marker query first. Or re-run `docs/runbooks/test-analytics-url.md` in DRY-RUN mode and confirm it REFUSES on an already-correct value."
    expected: "The row holds the loopback discard sink (an 18-character `http` value); the dry run refuses because the value is already correct."
    why_human: "The verifier is forbidden from running any command against a remote database from this checkout (the CLI is linked to PRODUCTION). The write itself was a founder act; the verifier can confirm the MECHANISM (gate arm D1, the remediation script, its 19-check self-test, the runbook) but cannot observe the live row."
human_verification:
  - test: "Criterion 8 — after this phase merges, dispatch `test-restore-from-baseline.yml` in `mode=preflight`, read the verdict, then dispatch the committing mode with the token derived AT THE MERGED REF from the tracked baseline record."
    expected: "Two run ids, both conclusions, the committing run's printed summary line verbatim, and evidence the two new guards ran — written back into `164.9-11-SUMMARY.md` and the ROADMAP criterion 8 block. A green preflight does NOT evidence `check_extension_guard`; only the committing run does."
    why_human: "POST-MERGE BY CONSTRUCTION (founder decision, plan 11 Task 1). The workflow's hard ref guard refuses any dispatch that is not from the default branch, so this phase's new restore guards cannot be exercised against shared TEST until it merges. A committing restore drops and rebuilds the `public` schema of a database other people's CI uses — a human act. Booked as `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]`, owner: THE FOUNDER."
  - test: "Criterion 6 — confirm the live `analytics_service_url` row on shared TEST (see `behavior_unverified_items` above), and then reconcile the three records that currently disagree about whether the write happened."
    expected: "The row holds the loopback discard sink; `164.9-10-SUMMARY.md`'s frontmatter D3 rationale, its `Tasks completed: 2 of 3` line and its `⛔ THIS PLAN IS NOT COMPLETE` headline are corrected to match its own appended tail; `[164.8.1-TEST-ANALYTICS-URL-PROD]` in `TODOS.md` stops reading `the WRITE is a founder action and is pending`."
    why_human: "Requires a read against shared TEST plus a founder decision on how to record the discharge (checkbox, amendment, or both)."
  - test: "Ship-time bookkeeping — the eleven `- [ ] 164.9-NN-PLAN.md` rows in the ROADMAP's Phase 164.9 plan list are all still unchecked, and the three planning-time ids this phase DELIVERS against (`[164.9-SHARED-TEST-TRANSPORT-FLAKE]`, `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]`, `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]`) still read `Owner: Phase 164.9 TESTISOLATION` on a phase that is closing."
    expected: "Each is checked off, amended with what shipped, or re-homed with a named owner — the treatment `[164.9-TEST-ANALYTICS-URL-REARM]` and `[164.9-DATA-DEPENDENT-GUARD-CENSUS]` already received."
    why_human: "Whether a partially-delivered id is closed, amended or re-homed is a founder judgment, not a machine one. `gsd-tools` state handlers are known to clobber these two files in this repo, so the edit is done by hand."
---

# Phase 164.9: TESTISOLATION Verification Report

**Phase Goal:** A CI run asserts only about the rows IT created on the shared TEST database, instead of making global assertions that measure other people's CI rows too; the shared-TEST contention and both `[164.8-PUSH-RACE-VAC08]` couplings are addressed; and the real `FANOUT-GLOBAL-01` backlog entry exists.

**Verified:** 2026-09-21T22:55:00Z
**Status:** human_needed
**Re-verification:** No — initial verification
**Requirement IDs:** none. This is an INSERTED phase with no v1.20 requirement IDs; `.planning/REQUIREMENTS.md` maps none to it and the ROADMAP entry records `**Requirements**: TBD (no v1.20 requirement IDs)`. The binding obligations are the 13 ROADMAP success criteria and the routed `TODOS.md` ids, verified below. **This is not a gap.**

## Goal Achievement

### Observable Truths (the 13 ROADMAP success criteria)

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | `FANOUT-GLOBAL-01` gets a REAL TODOS entry first | ✓ VERIFIED | Four entry headers exist in `TODOS.md`, each with an `Owner:` line, a booking date and a trigger: `FANOUT-GLOBAL-01` (kept, not retired; family membership enumerated), `[164.9-SHARED-TEST-TRANSPORT-FLAKE]`, `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]`, `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]`. The three pre-existing prose references now point at the family entry, and `CLAUDE.md` carries the dated correction (7×/27× advisory-key occurrence counts). |
| 2 | A run asserts only about its own rows — **read AS AMENDED** | ✓ VERIFIED (as amended) | All three deployed-sweep gates carry a new foreign-row calibration part, and all three carry the disclaimer the amendment quotes, in their own words — confirmed by a wrap-tolerant search returning exactly 1 hit per file for *"does NOT prove the deployed sweep is caller-scoped"*. I independently ran `node scripts/mutation-runner/run.mjs --file test_strategy_analytics_stuck_computing_reaper.sql --arm "7/FANOUT-GLOBAL-01"` → `biting: 1`, `No defects in the narrowed scope`. The known limit is booked as `[164.9-CALIBRATION-NARROWS-NOT-REPLACES]` with, deliberately, **no phase** and the founder decision stated. The amendment matches the code; it is dated and attributed, not a silent scope cut. |
| 3 | `[164.8-PUSH-RACE-VAC08]` + `[164.8.2-VAC08-FATAL-ON-TRANSIENT]` planned as ONE unit | ✓ VERIFIED | Both ids sit in a single plan's `requirements:` (`164.9-06-PLAN.md`). Two distinct keys now exist in `scripts/shared-test-db-keys.sh` — the mutual-exclusion key and a separate in-flight flag — and the script's own `--self-test` FAILS if they are ever made equal. Both schema writers announce the flag (`supabase-migrate.yml`, `test-restore-from-baseline.yml`); three reader jobs in `ci.yml` call `wait-for-test-schema-apply.sh`. No exclusion was removed and no taker was moved off the mutex key. |
| 4 | Fatal-on-transient NOT closed by restoring `\|\| echo ""` | ✓ VERIFIED | `scripts/test-ledger-drift-check.sh` carries a bounded retry whose status is RETURNED, not swallowed, with the silent-collapse fallback explicitly forbidden in-file. **Ran it:** `--self-test` → `self-test OK (13/13 arms)`, including `retry recovers a transient ledger read (GREEN)` and `retry EXHAUSTS loudly, and answer-level failures are not retried RED`. An unreadable input still reaches its named MEASURE_FAIL. |
| 5 | Data-dependent-migration escape has a MECHANISM, not a pragma | ✓ VERIFIED | `scripts/lint-migration-data-dependence.mjs` is an author-time refusal with 5 rules. **Ran it:** `--self-test` → `OK (5 refusals, red+green each. legs: 8 red, 11 green; floor 5)`; corpus → `272 migration(s), 345 anonymous block(s); 83 refusal(s), 83 allowlisted, 0 NEW`. Wired into `sql-gate-lint` (self-test step, corpus step, and a third step that captures stdout and MEASURE_FAILs when the census line is absent). `sql-gate-lint` is in the blocking `frontend` aggregator. No opt-out header or per-file escape hatch exists; `supabase-migrate.yml` was not edited to get a deploy out. |
| 6 | Shared TEST's cron no longer reaches PROD compute, by reading the LIVE row | ⚠️ PRESENT_BEHAVIOR_UNVERIFIED | Mechanism fully present and armed: arm D1 of `test_analytics_service_settings_and_vault_tick.sql` runs the identity-marker query FIRST, branches four ways with **no branch a skip**, RAISEs on a fall-through, and on the TEST branch READS the live row and raises if it is not the sink. `scripts/test-only-normalize-analytics-url.sh` (19-check self-test) plus `docs/runbooks/test-analytics-url.md`. `ARMS_FLOOR` 422→423. **The write itself is founder-attested, not verifier-observed** — the verifier may run no command against a remote DB from this checkout. See the record-disagreement warning below. |
| 7 | Third refdata gate leg pinning column VALUES | ✓ VERIFIED | `REFDATA_WRONGSTATE_{TABLE,ID,COL,EXPECTED}` exist in the **production path** of `scripts/restore-test-from-baseline.sh` with real defaults, guarded by the same interpolation refusals as the sibling seams, inside the same collect-first-raise-once block. **Ran the self-test on a disposable cluster:** `self-test OK (32/32 arms)`, which includes arms 28/29/30 (extension guard: public LOST / public GAINED with a different message / unchanged and QUIET) and arms 31/32 (value-pinning leg RED on a row restored in the WRONG STATE, QUIET when it holds its pinned value). |
| 8 | ONE green `mode=restore` dispatch, run id recorded | ⚠️ OPEN — POST-MERGE BY CONSTRUCTION (booking VERIFIED) | The ROADMAP itself records `STATUS: PENDING, NOT DISCHARGED`. The founder's binding condition is met: `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]` exists in `TODOS.md`, booked 2026-09-21, **owner: THE FOUNDER — the human who merges this phase**, with a trigger, a five-step act, an explicit closure definition (two run ids, both conclusions, the summary line verbatim, evidence the guards ran), and three named forbidden closures — including "NOT closed by a run id alone". The ref guard in `test-restore-from-baseline.yml` is intact and was not worked around. Routed to human verification, not scored as failed. |
| 9 | Every added/changed assertion proven able to fail, restored from a byte backup | ✓ VERIFIED | No `git checkout --` appears on any added line of the branch's non-planning diff. Falsification is machine-carried per surface: RED-UNDER/RED-UNDER-M arms for the SQL gates (executed by `sql-mutation`), red+green fixture pairs for both new linters, 32 named arms for the restore script, 13 for the VAC-08 gate, 18 for the execution ledger, 46 for the ordering wait, 5 for the dead-holder verdict, 19 for the analytics-URL remediation. |
| 10 | Nothing closed by widening an exemption, relaxing a floor, or asserting a smaller scope | ✓ VERIFIED | `ARMS_FLOOR` 413→**423** (ratchet UP), `FILES_FLOOR` 48 unchanged, `WAIVED_CEILING` **0** unchanged. `scripts/vac08-ledger-baseline.txt` still `ENTRY_COUNT = 0`. `scripts/live-db-fixture-drift-baseline.txt` **shrank 4 → 0** by repairing every finding at the call site, with the refused lazy repair (widening `supabase/config.toml` to expose `cron`) argued in-file. The two new ledgers (`lint-migration-data-dependence-baseline.txt` at 83, `live-db-execution-ledger.txt` at 18) are dated, shrink-only, carry per-entry destinations, state their AIM IS ZERO, and hard-fail in **both** directions — the `vac08-ledger-baseline.txt` idiom, copied rather than invented. |
| 11 | Shared-TEST transport flake answered — **read AS AMENDED (partially delivered)** | ✓ VERIFIED (as amended) | **Ran it:** `pytest tests/test_live_db_transport.py -q` from `analytics-service/` → `31 passed`, and the teardown counter printed unconditionally: `live-db transport: 11 retry/retries across 6 call(s)`. `_TERMINAL_WRITE_VERBS = {insert, upsert, update, delete, rpc}` is the idempotency boundary, with `rpc` on it **fail-closed** and the reason stated in-file. All four client factories return the retrying client; `conftest.py` registers the module sentinel plus `pytest_sessionfinish`/`pytest_terminal_summary`. **The three records AGREE**: the ROADMAP amendment, `[164.9-RPC-RETRY-NARROWED-SKIP-REOPENED]` in `TODOS.md` (destination Phase 164.9.1 JOBRPCTRUTH, interim owner the founder, two forbidden closures) and `164.9-05-SUMMARY.md`'s own `⛔ CORRECTED` block all say READS ONLY, all name the same residue, and all forbid putting `rpc` back on the retried side. |
| 12 | Dead-holder condition produces a NON-ZERO verdict, proven by killing a holder | ✓ VERIFIED | **I reproduced the drill independently** on a disposable local cluster: it killed a real holder holding the mutual-exclusion key, the release step's own body wrote the marker, then `=== RED OBSERVED: verdict exited 1, named the dead-holder condition, and read the SAME marker path ci.yml wrote ===` followed by `=== GREEN OBSERVED: verdict exited 0 with the marker absent ===`. Five paired verdict sites exist (`ci.yml` ×3, `supabase-migrate.yml`, `test-restore-from-baseline.yml`) and `src/__tests__/mutex-dead-holder-verdict.test.ts` pins the pairing per file, the `if: always()` shape, the ≥5 anti-vacuity floor, and that the marker path the release step WRITES is the one the verdict script READS. The warning text was added to, never softened. The drill is invoked from the `sql-mutation` job — the round-2 "zero call sites" defect is closed. |
| 13 | The credentialed live-DB class runs somewhere it can go RED | ✓ VERIFIED | `frontend-live-db-lane` stands the class up on a local stack and is wired into the `frontend` aggregator in **both** the `needs:` list and the result loop. Its corpus is DERIVED from the live-DB gate symbol (**ran it:** `corpus: 49 file(s)`), with three independent anti-vacuity controls — the tool's own empty-corpus exit, a preflight that refuses a non-numeric count by name, and `passWithNoTests: false`. The static census is a gate, not a document (**ran it:** `corpus 51 file(s); 0 finding(s) ... 69 unresolved`, the residue honestly printed and not claimed to be zero). Its ledger shrank to empty by repair. The execution ledger binds each run to its own artifact by nonce and refuses a vacuous run. The existing vitest shards' credential prohibition is untouched, so the coverage ratchet denominator is unaffected. |

**Score:** 11/13 criteria verified (1 present-but-behaviour-unverified, 1 open by founder decision)

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `scripts/shared-test-db-keys.sh` | Two keys, one source | ✓ VERIFIED | 18-check self-test; sourced by both schema writers and by the wait |
| `scripts/wait-for-test-schema-apply.sh` | Cross-workflow ordering wait | ✓ VERIFIED | 46-check self-test covering the DISPATCHER **and both READS**; 3 call sites |
| `scripts/mutex-dead-holder-verdict.sh` | Dead-holder → non-zero | ✓ VERIFIED | 5-check self-test; 5 paired sites; reproduced live |
| `scripts/pg-lane/mutex-dead-holder-lane.sh` | Falsification drill | ✓ VERIFIED | Invoked from `sql-mutation`; reproduced live, rc 0 |
| `scripts/test-ledger-drift-check.sh` | Bounded retry, no silent collapse | ✓ VERIFIED | 13/13 arms |
| `scripts/lint-migration-data-dependence.mjs` | Author-time refusal | ✓ VERIFIED | 5 refusals red+green; corpus 0 NEW; 3 ci.yml steps |
| `scripts/restore-test-from-baseline.sh` | 2-directional ext guard + value-pinning leg | ✓ VERIFIED | 32/32 arms on a disposable cluster |
| `scripts/live-db-execution-ledger.mjs` + `.txt` | Shrink-only execution ratchet | ✓ VERIFIED | 18 cases red+green; nonce freshness; vacuity refusal |
| `scripts/live-db-fixture-drift-census.mjs` + baseline | Static drift gate | ✓ VERIFIED | Ledger shrank 4 → 0 by call-site repair |
| `scripts/live-db-lane-corpus.mjs`, `vitest.livedb.{config,globalsetup}.ts` | Derived, refusing lane | ✓ VERIFIED | 49-file corpus; reporter registered; no coverage flag |
| `analytics-service/tests/live_db_transport.py` + `conftest.py` | Bounded read retry, visible | ✓ VERIFIED | 31 tests pass; counter printed at teardown |
| `supabase/tests/test_*` (3 sweep gates + analytics settings) | Calibration parts + arm D1 | ✓ VERIFIED | Disclaimer ×3; arm biting confirmed; D1 four-branch, no skip |
| `scripts/test-only-normalize-analytics-url.sh` + runbook | Guard-first TEST-only remediation | ✓ VERIFIED (artifact) | 19-check self-test; the live write is founder-attested |

### Key Link Verification

| From | To | Via | Status |
|---|---|---|---|
| `wait-for-test-schema-apply.sh` | `shared-test-db-keys.sh` | sourced, never re-implemented | ✓ WIRED |
| `supabase-migrate.yml` / `test-restore-from-baseline.yml` | in-flight flag | `pg_try_advisory_lock`, announced not blocking | ✓ WIRED |
| `ci.yml` DB-reader jobs (×3) | `wait-for-test-schema-apply.sh` | `run: bash …` | ✓ WIRED |
| 5 release steps carrying the dead-holder annotation | `mutex-dead-holder-verdict.sh` | marker file, one literal, pinned by a source-shape gate | ✓ WIRED |
| `frontend-live-db-lane` | `frontend` aggregator | `needs:` **and** result loop | ✓ WIRED |
| `vitest.livedb.config.ts` | `live-db-lane-corpus.mjs` | `liveDbLaneCorpus()` at config time | ✓ WIRED |
| `live-db-execution-ledger.mjs` | `live-db-execution-reporter.mjs` artifact | nonce-bound, run-end only | ✓ WIRED |
| `sql-gate-lint` | `lint-migration-data-dependence.mjs` | self-test + corpus + census-line MEASURE_FAIL | ✓ WIRED |
| `sql-mutation` | `mutex-dead-holder-lane.sh` | `run: bash …` (was zero call sites in round 2) | ✓ WIRED |

### Behavioural Spot-Checks (run by this verifier, not quoted from a SUMMARY)

| Behaviour | Command | Result | Status |
|---|---|---|---|
| Dead-holder verdict bites | `bash scripts/mutex-dead-holder-verdict.sh --self-test` | `OK (5 checks)` | ✓ PASS |
| Two keys stay distinct | `bash scripts/shared-test-db-keys.sh --self-test` | `OK (18 checks)` | ✓ PASS |
| Ordering wait covers dispatcher **and** reads | `bash scripts/wait-for-test-schema-apply.sh --self-test` | `OK (46 checks)` | ✓ PASS |
| Data-dependence linter can fail | `node scripts/lint-migration-data-dependence.mjs --self-test` | `OK (5 refusals, red+green each)` | ✓ PASS |
| Migration corpus has no NEW offender | `node scripts/lint-migration-data-dependence.mjs` | `272 / 345; 83 refusals, 83 allowlisted, 0 NEW` | ✓ PASS |
| Execution ledger red and green both observed | `node scripts/live-db-execution-ledger.mjs --self-test` | `OK: 18 cases` | ✓ PASS |
| Lane corpus is derived and non-empty | `node scripts/live-db-lane-corpus.mjs` | `corpus: 49 file(s)` | ✓ PASS |
| Fixture-drift census is clean and honest | `node scripts/live-db-fixture-drift-census.mjs` | `51 file(s); 0 finding(s) … 69 unresolved` | ✓ PASS |
| VAC-08 retry exhausts loudly | `bash scripts/test-ledger-drift-check.sh --self-test` | `13/13 arms` | ✓ PASS |
| Restore guards + value-pinning leg | `bash scripts/restore-test-from-baseline.sh --self-test` | `32/32 arms` | ✓ PASS |
| **Dead-holder drill, for real** | `bash scripts/pg-lane/mutex-dead-holder-lane.sh` | killed a real holder; `RED OBSERVED` then `GREEN OBSERVED`; PASSED | ✓ PASS |
| **The new calibration arm bites** | `node scripts/mutation-runner/run.mjs --file …reaper.sql --arm "7/FANOUT-GLOBAL-01"` | `biting: 1`, `No defects in the narrowed scope` | ✓ PASS |
| Python transport retry + visible counter | `pytest tests/test_live_db_transport.py -q` (from `analytics-service/`) | `31 passed`; `live-db transport: 11 retry/retries across 6 call(s)` | ✓ PASS |
| Phase contract tests | `npx vitest run` over the 7 phase-owned contract files | `7 files, 794 tests passed` | ✓ PASS |
| Planning hygiene | `npm run check:planning-hygiene` | `OK — 6818 tracked files scanned` | ✓ PASS |
| NUL-byte scan of the phase directory | byte count of `\000` per file | 0 in every file | ✓ PASS |
| Shared-TEST live row | — | — | ? SKIP — remote DB commands forbidden from this checkout |
| Restore dispatch against shared TEST | — | — | ? SKIP — post-merge by construction; destructive; human act |

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|---|---|---|---|
| — | Unreferenced `TBD`/`FIXME`/`XXX` on any added line | — | **None found** |
| — | `not yet implemented` / `coming soon` / stub returns | — | **None found** |
| `scripts/pg-lane/mutex-dead-holder-lane.sh`, `scripts/local-stack/run.sh` | `\|\| true` | ℹ️ Info | Teardown/cleanup paths only; not on any assertion path |
| test fixtures | `"…-placeholder"` literals | ℹ️ Info | Synthetic values, annotated as such because the repo is public |

### Warnings

**W1 — Three records disagree about whether criterion 6's write happened, inside the phase whose thesis is that they must not.**
Commit `9ec27f6a` appended `⭐ TASK 3 — RUN BY THE FOUNDER 2026-09-21. THE CHECKPOINT IS DISCHARGED.` to the **tail** of `164.9-10-SUMMARY.md`, with a before/after by shape and a second-connection confirmation — and left three other records saying the opposite:

- the same file's **machine-readable frontmatter**, whose `D3` rationale still ends `NOT DONE — this plan stopped at that checkpoint`;
- the same file's **headline**, `⛔ THIS PLAN IS NOT COMPLETE. IT STOPPED AT TASK 3`, and `Tasks completed: 2 of 3`;
- `[164.8.1-TEST-ANALYTICS-URL-PROD]` in `TODOS.md`, still `- [ ]`, whose last word on the subject is `the WRITE is a founder action and is pending`.

The only place outside that tail which records the discharge is a line inside the **next phase's** ROADMAP entry (164.9.1 JOBRPCTRUTH), not inside criterion 6. Criterion 6 itself carries no status annotation at all, unlike criteria 2, 8 and 11, each of which carries a dated amendment. This is the same defect class commit `be6eff89` ("stop three records disagreeing") was made to fix, reproduced one criterion over, and the frontmatter is what GSD tooling reads. **The substance is achieved; the auditability is not.** Neither review round raised it.

**W2 — Ship-time bookkeeping is outstanding.** All eleven `- [ ] 164.9-NN-PLAN.md` rows in the ROADMAP plan list are unchecked, and the three planning-time ids the phase delivers against still read `Owner: Phase 164.9 TESTISOLATION` on a closing phase — the state `[164.9-TEST-ANALYTICS-URL-REARM]` and `[164.9-DATA-DEPENDENT-GUARD-CENSUS]` were explicitly re-homed out of. `VERSION` is unmoved at `0.84.0.0` and `CHANGELOG.md` has no entry for this branch; per the repo's CHANGELOG discipline both belong in the release commit, which has not been made yet.

### Info

- **I looked specifically for the "PASS having measured nothing" shape the caller asked about, and did not find a new instance.** The three the review rounds found are each closed and I confirmed the closure independently: the ordering wait's self-test now names both READS as covered (46 checks) and states what it does *not* cover; the dead-holder drill now has a real call site inside `sql-mutation`; and the lane gate proves execution from a nonce-bound artifact written only at run end, refusing a vacuous run rather than reporting on files that never loaded. The nearest remaining candidate — the fixture-drift census printing `0 finding(s)` while 69 call sites are statically undecidable — is not one: the residue is printed on every run, restated twice in the baseline header, explicitly `NOT claimed to be zero`, and routed to execution in the lane that plan 08 stands up.
- **No requirement IDs are missing.** The phase has none by construction; verification was against the 13 criteria, as instructed.
- `TODOS.md` is deliberately **absent** from `drift_subjects` per the repo-owned rule, even though criterion 1 rests on it — a repo-global ledger every phase touches would make an unrelated backlog edit report drift that says nothing about this phase.

### Gaps Summary

**No gaps block the phase goal.** Eleven of the thirteen criteria are verified, and ten of those were confirmed by commands this verifier ran itself rather than by reading a SUMMARY — including the two hardest to fake: the dead-holder drill killing a real lock holder on a disposable cluster, and a newly added foreign-row calibration arm reddening its own section under mutation.

The two criteria not scored as verified are both **deliberate, dated and booked**, not oversights:

- **Criterion 8** stays OPEN by founder decision (Option A: ship, dispatch, record). The workflow's ref guard makes the dispatch post-merge by construction, and the founder's binding condition — that the recording act itself be booked before the merge, with a named owner — is met in full by `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]`.
- **Criterion 6**'s mechanism is verified and its write is founder-attested with an independent second-connection confirmation; this verifier is barred from running any command against a remote database and so records it as present-but-unobserved rather than as verified or failed.

The one thing worth fixing before this ships is **W1**: the phase built a great deal of machinery to stop a green reading from outliving its evidence, and then left its own frontmatter saying `NOT DONE` about work that is done.

---

_Verified: 2026-09-21T22:55:00Z_
_Verifier: Claude (gsd-verifier)_
