<!-- ⛔ CITATION CAVEAT — added by the orchestrator 2026-09-21, after measuring this file. -->

> ⛔ **THE `file:line` CITATIONS IN THIS FILE ARE UNRELIABLE. There are 33 of them, and they were
> written against an explicit instruction to cite BY SYMBOL.** This repo has a dated record of that
> exact failure class (`[164.7-CITATION-DRIFT-01]`): line numbers here rot within a commit or two,
> and this phase EDITS the very files most of them point into (`ci.yml`, `supabase-migrate.yml`,
> `scripts/test-ledger-drift-check.sh`), so they will be stale before the work lands.
>
> ⭐ **The ANALOGS and the REASONING in this file were spot-checked and are sound** — the line
> numbers are the only defective part. Use the file for *which* pattern to copy and *why*; re-resolve
> *where* by symbol (job key, function name, step name) at the moment you write it.
>
> ⛔ **Do NOT copy a `file:line` from this document into PLAN.md.** `plan-anchor-verify` in
> `.github/workflows/ci.yml` re-resolves every anchor a pending PLAN.md asserts and fails loud on a
> miss — a rotted anchor copied from here becomes a red gate, or worse, silently resolves to the
> wrong code.

# Phase 164.9: TESTISOLATION - Pattern Map

**Mapped:** 2026-09-21
**Files analyzed:** 15 (13 modified-existing, 2 net-new)
**Analogs found:** 14 / 15 (1 file has no analog and is flagged plainly below)

⚠️ This phase is CI/test-harness infrastructure only: 0 `.tsx`, 0 `src/app/`, 0 React components.
Every analog below is drawn from `.github/workflows/*.yml`, `scripts/*.sh`, `supabase/tests/*.sql`,
`TODOS.md`/`CLAUDE.md`, and one `.ts` test-fixture file. All paths below were verified
git-TRACKED via `git ls-files` this session — none are gitignored mirrors.

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `TODOS.md` (4 new entries: `FANOUT-GLOBAL-01`, `[164.9-SHARED-TEST-TRANSPORT-FLAKE]`, `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]`, `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]`) | config (backlog ledger) | batch (append-only doc) | `TODOS.md` itself — `[164.8-PUSH-RACE-VAC08]` and `[164.8.2-VAC08-FATAL-ON-TRANSIENT]` entries | exact (same file, same convention) |
| `CLAUDE.md` (correct stale "26" → 27, correct "0 hits" line for `FANOUT-GLOBAL-01`) | config | batch | `CLAUDE.md` itself — its own prior "CORRECTED"/"SHARPENED" dated-callout convention | exact |
| `.github/workflows/supabase-migrate.yml` (`apply-test`: distinct advisory key + explicit ordering signal) | config (CI workflow) | event-driven | `.github/workflows/ci.yml`'s `sql-tests` "Acquire shared-test-db mutex" step (`supabase-migrate.yml`'s own step is already a documented COPY of it) | exact |
| `.github/workflows/ci.yml` (`sql-tests`/`python`/`e2e-seeded`: distinct key(s) + ordering wait) | config (CI workflow) | event-driven | same file's own existing "Acquire shared-test-db mutex" step (three near-identical copies already exist at `sql-tests`/`python`/`e2e-seeded`) | exact |
| `.github/workflows/ci.yml` (new dead-holder verdict step, `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]`) | config (CI workflow) | event-driven | same file's existing "Release shared-test-db mutex (best effort)" step (the `::error::` annotation this new step must act on) | exact — same file, adjacent step |
| `.github/workflows/ci.yml` (new credentialed-test lane job, `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]`) | config (CI workflow) | request-response (test runner invocation) | `frontend-seam-redis` job (`.github/workflows/ci.yml:479-618`) | exact — repo's own documented precedent for this exact shape |
| `scripts/test-ledger-drift-check.sh` (bounded retry around `run_ledger_query`) | utility (bash) | transform / batch | same file's `run_ledger_query`/`LEDGER_QUERY_CMD` seam + its own `run_arm`/`--self-test` harness | exact — same file |
| `scripts/restore-test-from-baseline.sh` (extension guard: two-directional, `public`-scoped) | utility (bash) | batch | same file's existing extension-count guard (`pre_ext`/`post_ext`, `census_val`) | exact — same file, narrowed scope |
| `scripts/restore-test-from-baseline.sh` (third refdata gate leg: column VALUES) | utility (bash) | batch | same file's existing `v_empty`/`v_short` `DO` block | exact — same file, sibling aggregate |
| new `scripts/pg-lane/*.sh` (dead-holder-kill falsification script) | test (falsification harness) | event-driven | `scripts/pg-lane/run.sh` + `scripts/restore-test-from-baseline.sh`'s `run_arm` self-test harness | role-match — new file, established harness shape |
| new Python retry wrapper (transport-flake bounded retry, `analytics-service/` pytest live-DB layer — exact file **unmeasured**, see Open Question in RESEARCH.md) | utility (Python) | transform | **NONE — see "No Analog Found" below** | no analog |
| `src/__tests__/claim-dedupe-done-pending-children-guard.test.ts` (fixture fix: `key_hash` → `api_key_encrypted` + `label`) | test (vitest fixture) | CRUD (insert) | same file's own `.insert({...})` call — fix is a column-name/shape correction, not a new pattern | exact — same file |
| `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` (foreign-row calibration arm) | test (SQL gate) | batch | same file's existing "arm A: stranded A CENTURY" seeding block | exact — same file, sibling arm |
| `supabase/tests/test_retention_orphaned_running.sql` (foreign-row calibration arm) | test (SQL gate) | batch | `test_strategy_analytics_stuck_computing_reaper.sql`'s arm-A seeding-discipline pattern | role-match — cross-file, same discipline |
| `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql` (foreign-row calibration arm) | test (SQL gate) | batch | `test_strategy_analytics_stuck_computing_reaper.sql`'s arm-A seeding-discipline pattern | role-match — cross-file, same discipline |
| `docs/runbooks/shared-test-db-mutex.md` (update §1 for the new distinct-key scheme) | config (docs) | batch | same file's own §1 "Mechanism" section | exact — same file |

## Pattern Assignments

### `TODOS.md` — four new backlog entries

**Analog:** `TODOS.md:9077` (`[164.8-PUSH-RACE-VAC08]`) and `TODOS.md:3224` (`[164.8.2-VAC08-FATAL-ON-TRANSIENT]`)

**Entry-header pattern** (copy this shape for all four new ids):
```markdown
- [ ] **`[SOME-ID]` <one-line summary of the defect, past-tense-booked, dated> (booked
      YYYY-MM-DD, Phase NNN...).**
      **MECHANISM.** <what happens, with a measured/quoted fact>
      ...
      **Candidate fixes, none taken here:** <options, non-prescriptive>
```
Verbatim precedent (`TODOS.md:9077`):
```
- [ ] **[164.8-PUSH-RACE-VAC08] `sql-tests` (VAC-08) and `supabase-migrate.yml`'s `apply-test`
      share ONE advisory lock on the merge push, so their order is undefined — the frontier
      exemption removed the CONSEQUENCE, not the coupling (booked 2026-09-09)** — MEASURED
      2026-09-09 at this branch's HEAD: advisory key `61616158` appears 7 times in
      `.github/workflows/supabase-migrate.yml` and 26 times in `.github/workflows/ci.yml`. ...
```
⚠️ **Do not copy the stale "26" figure** — CONTEXT.md re-measured it as **27** in `ci.yml` on
2026-09-21; the new `FANOUT-GLOBAL-01`/`[164.9-*]` entries must cite 27, and the existing
`[164.8-PUSH-RACE-VAC08]` entry's own stale "26" (at `TODOS.md:9077` and `TODOS.md:3253-3254`)
should be corrected in the same pass since it is the same fact restated twice and now wrong in
both places.

**BEFORE/AFTER pattern** for consequence-of-a-fix entries (use for
`[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]`, which is exactly this shape — a correct detection whose
consequence is missing), verbatim precedent (`TODOS.md:3230-3237`):
```
**BEFORE.** `ledger_rows="$(run_ledger_query ledger_rows … 2>/dev/null || echo "")"` followed
by `[ -n "$ledger_rows" ] && …`. ...
**AFTER.** Three separated, named MEASURE_FAILs — query failed (with rc), exited 0 with NO
ROW, answer not a number — each exiting 1.
**THE EXPOSURE THIS CREATES.** ...
```

**Where to insert `FANOUT-GLOBAL-01` itself:** it is the FAMILY entry the other three (and
`[164.8-PUSH-RACE-VAC08]`, `[164.8.2-VAC08-FATAL-ON-TRANSIENT]`) already cite by name at
`TODOS.md:3181`, `TODOS.md:3300`, `TODOS.md:4193` — those three prose hits are the citations to
retro-link once the real entry exists. Its own header should state the definition already fixed
in `.planning/ROADMAP.md:2394` ("a GLOBAL assertion there is measuring other people's rows too")
rather than re-deriving new wording.

---

### `CLAUDE.md` — two corrections

**Analog:** `CLAUDE.md` itself, existing dated-correction convention (e.g. the "⛔ CORRECTED
2026-09-12" and "⭐ ADDENDUM 2026-09-09" blocks already in the file)

**Site 1** (`CLAUDE.md:93`) — currently reads:
```
measured 2026-09-09: key `61616158` appears 7× in `supabase-migrate.yml` and 26× in `ci.yml`.
```
Correct to 27×, dated 2026-09-21, in the same "measured N: ..." voice already used throughout
this file — do not add a new paragraph, edit the number and its date in place, per this file's
own instruction elsewhere to regenerate rather than trust a restated figure.

**Site 2** (`CLAUDE.md:101`) — currently reads:
```
⛔ `FANOUT-GLOBAL-01` is NOT a `TODOS.md` id (measured 2026-09-08: 0 hits). It exists only as
prose here and in the ROADMAP. **Phase 164.9 TESTISOLATION owns writing the real entry** — do not
send a planner looking for a spec that was never written.
```
Once `TODOS.md` gains the real entry (this phase's first task per CONTEXT Area 2), this sentence
becomes stale in the same way the "26" figure did — update it to record that the entry now
exists and point at it, following this file's own "⛔ CORRECTED" callout convention rather than
silently deleting the paragraph (the lineage — why it was written, why it changed — is the thing
this file's own style always preserves).

---

### `.github/workflows/supabase-migrate.yml` — `apply-test`'s advisory key + ordering signal

**Analog:** `.github/workflows/ci.yml`'s `sql-tests` "Acquire shared-test-db mutex" step (own file
already states at `supabase-migrate.yml:466` that its step is "a COPY of" that one)

**Current acquire pattern** (`.github/workflows/ci.yml:2948-3042`, identical shape reused in
`supabase-migrate.yml`):
```bash
- name: Acquire shared-test-db mutex
  env:
    TEST_SUPABASE_DB_URL: ${{ secrets.TEST_SUPABASE_DB_URL }}
  run: |
    set -euo pipefail
    if [ -z "${TEST_SUPABASE_DB_URL:-}" ]; then
      echo "fork PR / secret withheld — mutex skipped (DB work self-skips on forks)"
      exit 0
    fi
    ...
    start_holder() {
      : > "${log}"
      PGAPPNAME=ci-shared-test-db-mutex PGCONNECT_TIMEOUT=15 \
        nohup psql "${dsn}" -X -q -A -t -v ON_ERROR_STOP=1 \
          -c "SET statement_timeout = 0;" \
          -c "SET client_connection_check_interval = '30s';" \
          -c "SELECT 'HOLDER-BACKEND-PID ' || pg_backend_pid();" \
          -c "SELECT pg_advisory_lock(61616158);" \
          -c "SELECT 'MUTEX-ACQUIRED';" \
          -c "SELECT pg_sleep(6000);" \
          >> "${log}" 2>&1 &
      holder=$!
      echo "${holder}" > "${pidfile}"
    }
```
**Copy for the distinct-key half:** replace the literal `61616158` with `apply-test`'s OWN key in
this file only — every session-level GUC (`statement_timeout=0`,
`client_connection_check_interval='30s'`), the `HOLDER-BACKEND-PID` marker line, the `pidfile`/
`backendpidfile` bookkeeping, and the paired "Release shared-test-db mutex (best effort)" step
(same file, `ci.yml:3855-3894`, quoted below) must all be preserved verbatim — only the key
constant changes. **The ordering half is NOT present in this shape anywhere in the repo** — Q3 in
RESEARCH.md names three real options (cross-workflow polling wait modeled on the acquire loop
above, `workflow_run` trigger, or `workflow_call` reusable workflow) and explicitly does not
pre-select one; whichever the plan picks, CONTEXT requires "any plan touching
`supabase-migrate.yml` states in writing which of the two [isolation fix vs. red-clearing hack]
it is doing."

**Marker-query-first pattern** (already used first in this file, `supabase-migrate.yml:709`,
same idiom as `test-restore-from-baseline.sh`):
```
-c "SELECT shobj_description(oid, 'pg_database') FROM pg_database WHERE datname = current_database();" \
```
Any new ordering/verdict logic this phase adds to a DB-touching step must keep this as the FIRST
statement, per the standing repo convention.

---

### `.github/workflows/ci.yml` — dead-holder verdict step (`[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]`)

**Analog:** the existing "Release shared-test-db mutex (best effort)" step, three near-identical
copies at `ci.yml:3855-3894` / `4429-4458` / `5479-5508` (one per DB-touching job:
`sql-tests`/`python`/`e2e-seeded`)

**The exact cosmetic-only pattern to contrast against** (`ci.yml:3849-3885`, verbatim):
```yaml
# Best-effort EARLY release of the shared-test-db mutex (Phase 158 /
# OPS-01), identical in all three DB-touching jobs. Job teardown drops the
# psql session anyway; killing it here hands the lock to the next waiter a
# little sooner. `if: always()` + never exits non-zero: a failed release
# must not redden a job whose real work passed, and the teardown path is
# the guarantee.
- name: Release shared-test-db mutex (best effort)
  if: always()
  env:
    TEST_SUPABASE_DB_URL: ${{ secrets.TEST_SUPABASE_DB_URL }}
  run: |
    pidfile="${RUNNER_TEMP}/shared-test-db-mutex.pid"
    if [ ! -f "${pidfile}" ]; then
      echo "No mutex pid recorded (fork PR / acquire skipped) — nothing to release."
      exit 0
    fi
    holder="$(cat "${pidfile}")"
    if kill "${holder}" 2>/dev/null; then
      echo "Released the shared-test-db mutex (terminated psql pid ${holder})."
    else
      # Annotation only: this step is `if: always()` and must never redden a
      # job whose real work passed, and `echo ::error::` does not change the
      # exit status. Job teardown remains the guaranteed release path.
      echo "::error::mutex holder pid ${holder} died BEFORE this release step — the DB work after its death ran UNSERIALIZED. Do not trust this run's DB assertions; investigate (holder outlives the job by design, see the pg_sleep note in the acquire step)."
    fi
```
**Pattern to add (new step, do NOT edit the block above in place):** per RESEARCH.md Q5, write a
marker file (e.g. `"${RUNNER_TEMP}/shared-test-db-mutex-dead-holder"`) inside the `else` branch
above, immediately after the `echo "::error::..."` line, then add a SEPARATE, subsequent step
(also `if: always()`, so it evaluates even after an earlier failure) whose only job is:
```yaml
- name: Verdict — shared-test-db mutex holder died mid-job
  if: always()
  run: |
    if [ -f "${RUNNER_TEMP}/shared-test-db-mutex-dead-holder" ]; then
      echo "::error::this run's DB assertions were made without the shared-test-db mutex held — see the previous step's annotation."
      exit 1
    fi
```
This preserves the existing step's documented invariant ("must never redden a job whose real work
passed") while giving the dead-holder condition a real, job-reddening consequence, exactly as
RESEARCH.md's Pitfall 3 warns against doing inline. Repeat this two-step pattern once per
DB-touching job (three sites), matching the existing triplication.

---

### `.github/workflows/ci.yml` — new credentialed-test lane job (`[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]`)

**Analog:** `frontend-seam-redis`, `.github/workflows/ci.yml:479-618` — the repo's own documented
precedent, explicitly named in CONTEXT.md and RESEARCH.md as the template

**Full job shape to copy** (condensed, `ci.yml:479-550` + `620-670`):
```yaml
frontend-seam-redis:
  needs: [changed-paths]
  if: needs.changed-paths.outputs.docs_only != 'true'
  runs-on: ubuntu-latest
  services:
    # ... pinned-by-digest ephemeral service containers, not applicable to the DB-credentialed
    # lane unless it targets local-stack (see frontend-local-stack, ci.yml:620-762, for THAT
    # shape instead — it boots the Supabase CLI stack from supabase/schema/baseline.sql)
  env:
    # Deliberately set JOB-WIDE, which is the exact INVERSE of the
    # frontend-test warning ("DO NOT add NEXT_PUBLIC_SUPABASE_URL /
    # SUPABASE_SERVICE_ROLE_KEY env to this job"). It is safe HERE and only
    # here because this job runs `tests/redis/**` and nothing else: ...
    # Never widen this job's test selection.
    UPSTASH_REDIS_REST_URL: http://localhost:8079
    UPSTASH_REDIS_REST_TOKEN: ci-seam-breaker-token
  steps:
    - name: Preflight - the lane must have a non-empty test corpus
      run: |
        files=(tests/redis/*.test.ts)
        if [ ${#files[@]} -eq 0 ]; then
          echo "::error::No tests/redis/*.test.ts files found ..."
          exit 1
        fi
    - name: Run the seam breaker real-Redis lane
      run: npx vitest run --config vitest.redis.config.ts --reporter=dot  # NO --coverage
```
**What must differ for the credentialed-test lane:** (1) the preflight glob CANNOT be static —
RESEARCH.md measured only 2 of 49 `HAS_LIVE_DB`-gated files follow any filename convention, so
the corpus must be built dynamically: `grep -rl HAS_LIVE_DB src/ --include='*.test.ts'` (same
command RESEARCH used to measure the class), piped into the vitest invocation, so a newly-added
gated test is automatically picked up rather than silently never running in the new lane either
(this is `frontend-local-stack`'s own "Coverage that cannot execute is worse than absent
coverage" warning, restated for this lane); (2) whether the lane hosts against `frontend-local-stack`
(`ci.yml:620-762`, boots Postgres+PostgREST+GoTrue from `supabase/schema/baseline.sql` via
`scripts/local-stack/run.sh`, already proven for VAC-07) or against shared TEST is Claude's
Discretion per CONTEXT — but RESEARCH.md's Q2 finding is load-bearing: at least 3 of 5 named
failure signatures (the `claim_compute_jobs_with_priority` 2-arg/5-arg `PGRST203` overload
ambiguity at `baseline.sql:1534`/`:1625`, `cron` excluded from `supabase/config.toml`'s
`schemas = ["public", "graphql_public"]`, and the stale `key_hash` fixture in
`src/__tests__/claim-dedupe-done-pending-children-guard.test.ts`) reproduce on EITHER host, so this
job alone does not close the criterion — see that file's pattern assignment below; (3) `--coverage`
must never appear on this job's vitest invocation, matching both `frontend-seam-redis` and
`frontend-local-stack`'s explicit convention (protects the coverage-ratchet baseline
`frontend-test`'s own in-file prohibition exists to protect).

---

### `scripts/test-ledger-drift-check.sh` — bounded retry (`[164.9-SHARED-TEST-TRANSPORT-FLAKE]` + `[164.8.2-VAC08-FATAL-ON-TRANSIENT]`)

**Analog:** the file's own `run_ledger_query`/`LEDGER_QUERY_CMD` seam (`scripts/test-ledger-drift-check.sh:259-266`)

**The seam to wrap, verbatim:**
```bash
run_ledger_query() {
  if [ -n "${LEDGER_QUERY_CMD:-}" ]; then
    # shellcheck disable=SC2086
    $LEDGER_QUERY_CMD "$1"
  else
    default_ledger_query "$1" "$2"
  fi
}
```
**The three MEASURE_FAIL call sites this retry sits in front of** (`scripts/test-ledger-drift-check.sh:435,457,461,464`):
```bash
ledger_rows="$(run_ledger_query ledger_rows "$names_csv" 2>"$ledger_rows_err")"
...
fail "MEASURE_FAIL: could not read the TEST ledger row count (the ledger_rows query exited ${ledger_rows_rc}; ${err_lines} line(s) of stderr captured and WITHHELD — it can carry a DSN, host or username). ..."
```
**Error-message redaction convention (shared pattern, apply to any new retry-exhaustion message
too):** never interpolate raw query output, stderr, or a DSN into a `fail`/`echo` line — every
existing MEASURE_FAIL says "WITHHELD" and gives only a byte/line count. A new
"retry exhausted after N attempts" message must follow the same withholding convention.

**Self-test harness to extend** (`scripts/test-ledger-drift-check.sh:1153-1211`, `run_arm`/
`--self-test` conventions) — this is also the direct template for
`scripts/restore-test-from-baseline.sh`'s own `run_arm` at line 2302 (quoted below), which is
itself explicitly commented as "Copied from scripts/test-ledger-drift-check.sh:604-619". Build the
retry falsification as two new self-test arms: a `LEDGER_QUERY_CMD` shim that fails N-1 times then
succeeds (proves recovery — "keeps a transient fault distinguishable from a clean run"), and one
that always fails (proves the retry still reaches `fail "MEASURE_FAIL..."` after exhausting its
budget — CONTEXT's "must be observed to EXHAUST" requirement). Both are `cp`-backup-neutered,
observed RED, then restored, per this repo's standing anti-vacuity discipline.

⚠️ **The Python-side transport retry has NO analog in this repo** — see "No Analog Found" below.

---

### `scripts/restore-test-from-baseline.sh` — two-directional extension guard

**Analog:** the file's own existing (one-directional) extension-count guard

**Current one-sided pattern** (`scripts/restore-test-from-baseline.sh:1874-1877`):
```bash
local pre_ext post_ext class
pre_ext=$(census_val "$RESTORE_OUT_DIR/pre-census.txt" extensions)
post_ext=$(census_val "$RESTORE_OUT_DIR/post-census.txt" extensions)
[ "$post_ext" = "$pre_ext" ] || fail "post-census extensions=${post_ext}, pre-census had ${pre_ext}. An extension that lived in public was CASCADE-dropped and the dump did not put it back."
```
**The whole-database census query behind it** (`scripts/restore-test-from-baseline.sh:381`):
```sql
SELECT 'extensions=' || count(*)::text FROM pg_extension;
```
**Fix shape (per RESEARCH.md Q6a, not locked but the direct reading of the evidence):** re-scope
the census query to join `pg_namespace` and filter `n.nspname = 'public'`
(`SELECT count(*) FROM pg_extension e JOIN pg_namespace n ON e.extnamespace = n.oid WHERE
n.nspname = 'public'`), then keep TWO independently-provable directions instead of one bare `=`:
a loss (`post < pre` → RED, current message) and a gain (`post > pre` → RED, a NEW, distinct
message) — CONTEXT requires both directions independently neutered and observed RED. The sibling
`nonpostgres_rel`/`nonpostgres_fn` ownership-list comparison two lines below
(`scripts/restore-test-from-baseline.sh:1878-1882`) is the closest existing TWO-name-per-class
comparison idiom in the same function, useful as a structural sibling even though it compares
lists rather than a scoped count.

---

### `scripts/restore-test-from-baseline.sh` — third refdata gate leg (`[164.8.1-REPLAY-INSERT-ONLY-SCOPE]`)

**Analog:** the file's own existing `v_empty`/`v_short` `DO` block, same function

**Verbatim precedent** (`scripts/restore-test-from-baseline.sh:1370-1399`):
```sql
DO $restore$
DECLARE
  v_empty   text;
  v_short   text;
  v_missing text[];
BEGIN
  -- COLLECT FIRST, RAISE ONCE. ...
  SELECT string_agg(t.tbl, ', ' ORDER BY t.tbl) FILTER (WHERE t.n = 0),
         string_agg(format('%s (%s row(s) for %s statement(s))', t.tbl, t.n, t.expected), ', ' ORDER BY t.tbl)
           FILTER (WHERE t.n > 0 AND t.n < t.expected)
    INTO v_empty, v_short
    FROM (VALUES
      ${refdata_values}
    ) AS t(tbl, n, expected);

  IF v_empty IS NOT NULL THEN
    RAISE EXCEPTION 'restore aborted: reference table(s) % are EMPTY after the replay — ...', v_empty, coalesce(v_short, 'none');
  END IF;
  IF v_short IS NOT NULL THEN
    RAISE EXCEPTION 'restore aborted: reference table(s) are SHORT after the replay — %, ...', v_short;
  END IF;
```
**Copy this exact shape for the new leg:** add a sibling `v_wrong_state` aggregate to the SAME
`DECLARE`/`SELECT INTO`, scoped to the five known-affected rows CONTEXT names
(`20260521150000:23,28,33`, `20260716120000:72`, `20260530120000:291`, `20260521190243:32`,
`20260709130000:30`) — asserting each holds its POST-migration value, not its INSERT-time
default, with its own `IF v_wrong_state IS NOT NULL THEN RAISE EXCEPTION ...` following the same
COLLECT-FIRST-RAISE-ONCE discipline the comment above already documents. Bounded per CONTEXT: the
teaser trio only, no FK-/CHECK-bearing reference tables.

**Self-test harness for the new leg** (`scripts/restore-test-from-baseline.sh:2295-2329`,
`run_arm`):
```bash
run_arm() {
  local label="$1" want="$2"
  shift 2
  total=$((total + 1))
  local out="$SELFTEST_TMPD/${ARM_OUT_PREFIX}-${total}.out"
  local rc=0
  ( "$@" ) > "$out" 2>&1 || rc=$?
  ...
  # THE REDACTION CHECK (T-164.8-05). This log is public. ...
  local leaked=0
  if grep -aqE 'postgres(ql)?://' "$out" || grep -aqE '\$[A-Za-z_0-9]*\$' "$out"; then leaked=1; fi
```
New arms for the third leg must go through this same harness, including its DSN/dollar-quote
redaction check — a new `RAISE EXCEPTION` message must never interpolate a raw row value that
could be a credential (none of the five affected rows are credential-bearing per CONTEXT, but the
harness's blanket redaction scan applies regardless).

---

### `supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql` (+ `test_retention_orphaned_running.sql`, `test_reconcile_dropped_enqueue_sweep.sql`) — foreign-row calibration (criterion 2)

**Analog:** the reaper file's own existing "arm A: stranded A CENTURY" seeding block — this is the
ONE existing instance of the seeding-discipline this criterion must now PROVE, so it is both the
analog and (largely) the mechanism already in place; the task is calibration, not new mechanism.

**Verbatim precedent** (`supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:507-514`):
```sql
-- arm A: stranded A CENTURY (isolation by construction -- see the header: this
-- epoch is what makes the seed outrank any plausible foreign row for the
-- deployed `ORDER BY computing_started_at ASC LIMIT 25` budget, replacing the
-- deleted cross-tenant neutralizing UPDATE). warned marker SET (this is what
-- makes the SI-02 launder block observable), computed_at FRESH (SC#2
-- direction 1).
INSERT INTO public.strategy_analytics
  (strategy_id, computation_status, computation_warned, computing_started_at, computed_at)
```

**Oracle discipline this file's own header states, and which any new arm must not violate**
(`supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:33-43`):
```
ORACLE DISCIPLINE (the load-bearing property of this file)
The behavioral parts read the REAL deployed body out of cron.job.command and
`EXECUTE v_command` it. They NEVER re-type the predicate. A gate that
re-implements the predicate passes when the DEPLOYED predicate is wrong.
```
**Transaction framing convention** (`supabase/tests/test_strategy_analytics_stuck_computing_reaper.sql:62-72`):
```
Every part that writes opens its OWN `BEGIN;`, immediately sets
`SET LOCAL lock_timeout = '5s'`, and closes with `ROLLBACK;`. There is NO
outer whole-file transaction ...
```

**What the new calibration arm must add (not present today, per RESEARCH.md Q1):** a FOREIGN row
— one this run did not create, seeded with a plausible-but-not-dominant
`computing_started_at`/equivalent timestamp — seeded ALONGSIDE the existing extreme-timestamp
seeds, then a positive assertion that the run's OWN assertion (not the foreign row) is what the
deployed `ORDER BY ... LIMIT N` sweep surfaces. This is CONTEXT's non-negotiable calibration:
"seed a FOREIGN row, then show the new assertion stays GREEN where the old one would have gone
RED [without the seeding discipline]." Apply the identical arm shape to
`test_retention_orphaned_running.sql` and `test_reconcile_dropped_enqueue_sweep.sql` — both are
cross-file matches for the SAME discipline (their own transaction-framing lines,
`test_retention_orphaned_running.sql:54/191`, are already cited by the reaper file as an analog of
each other).

⛔ **`test_prod_prober_cadence.sql` is NOT in scope for this pattern** — RESEARCH.md confirms it is
a false positive (asserts about the cron SCHEDULE itself, a global singleton, not multi-tenant
data) and correctly stays global by design.

---

### `src/__tests__/claim-dedupe-done-pending-children-guard.test.ts` — stale-fixture fix

**Analog:** the same file's own `.insert({...})` call — the fix is a column-name/shape correction
against the real schema, not a new pattern to import from elsewhere.

**Current (broken) insert** (`src/__tests__/claim-dedupe-done-pending-children-guard.test.ts:299-309`):
```typescript
const { data: apiKey, error: akErr } = await admin
  .from("api_keys")
  .insert({
    user_id: userId,
    exchange: "okx",
    key_hash: `c39test${ts}`,
    status: "active",
    sync_status: "ok",
  })
  .select("id")
  .single<{ id: string }>();
```
**The real schema** (`supabase/schema/baseline.sql:9873-9899`): the column is
`"api_key_encrypted" "text" NOT NULL`, not `key_hash` (which has 0 hits anywhere in
`baseline.sql` or `supabase/migrations/*.sql` — it has never existed). A required `label` column
is also missing from this insert. Fix: rename `key_hash` → `api_key_encrypted` with a synthetic
placeholder value, and add the required `label` field — no new pattern, this is fixture-vs-schema
drift correction.

---

### `docs/runbooks/shared-test-db-mutex.md` — update for distinct keys

**Analog:** the file's own §1 "Mechanism" section

**Current text to update** (`docs/runbooks/shared-test-db-mutex.md`, §1):
```
`sql-tests`, `python` and `e2e-seeded` each acquire **session advisory lock key
`61616158`** (issue #616 / phase 158) before their first DB-touching step, and
hold it for the rest of the job.
```
Once criteria 3+4 land distinct keys, this sentence (and the file's own header note "Phase 158's
three became FIVE on 2026-09-09" listing `apply-test` and `restore`) needs the SAME kind of
dated-update paragraph this file already uses for its own prior revisions — follow its established
"kept as lineage, marked current" convention (visible in its own header: "Every 'three' below that
was not updated is a Phase 158 sentence kept as lineage").

## Shared Patterns

### The advisory-key acquire/release idiom
**Source:** `.github/workflows/ci.yml:2948-3042` (acquire) and `:3855-3894` (release), reused
verbatim (with only the DB-touching job's context differing) at `:4214-4302`/`:4429-4458` and
`:4962-5050`/`:5479-5508`, and copied into `.github/workflows/supabase-migrate.yml:483-571`.
**Apply to:** every file/job touched by criteria 3+4 and criterion 12 — `ci.yml`'s three
DB-touching jobs, `supabase-migrate.yml`'s `apply-test`. Any new key must preserve every GUC
(`statement_timeout=0`, `client_connection_check_interval='30s'`), the `HOLDER-BACKEND-PID`
marker, and the `pidfile`/`backendpidfile` bookkeeping unchanged — only the lock key constant and
(where relevant) an ordering wait are new.

### The `::error::` annotation vs. real exit-status idiom
**Source:** `.github/workflows/ci.yml:3881-3884`, the comment block explicitly stating `echo
::error::` "does not change the exit status."
**Apply to:** criterion 12's new verdict step (must be a SEPARATE step with a real `exit 1`, never
an edit to the annotation-only step) and any other place this phase adds a log-only warning for a
condition that CONTEXT requires to be gateable.

### Secret/output redaction convention ("WITHHELD", never interpolate raw query output)
**Source:** `scripts/test-ledger-drift-check.sh:457,461,464` (every `MEASURE_FAIL` message) and
`scripts/restore-test-from-baseline.sh:2318-2324` (`run_arm`'s DSN/dollar-quote redaction scan).
**Apply to:** any new `fail`/`echo`/`RAISE EXCEPTION`/`::error::` message this phase adds anywhere
— the retry-exhaustion message, the two-directional extension-guard messages, the new refdata leg
message, the dead-holder verdict step. This repo is PUBLIC; every existing analog names what was
withheld rather than printing it.

### `cp`-backup-verified-by-`cmp` neuter/restore discipline
**Source:** `scripts/restore-test-from-baseline.sh`'s `run_arm` self-test harness
(`:2302-2329`), and `scripts/test-ledger-drift-check.sh`'s own `--self-test` (`:1153-1211`).
**Apply to:** every new or changed assertion this phase adds (CONTEXT's non-negotiable Area 1
rule) — neuter, observe RED, restore from a `cp` byte backup verified with `cmp`, re-take the
backup after every edit. Never `git checkout --`/`git restore`/`git stash`.

### `frontend-seam-redis`'s "narrow lane, job-wide env, non-widening fence" shape
**Source:** `.github/workflows/ci.yml:479-618`, quoted in full above.
**Apply to:** the new credentialed-test lane job (criterion 13) exclusively — this shape is
explicitly NOT to be applied to the existing `frontend-test` shards (that job's own in-file
prohibition on `NEXT_PUBLIC_SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` must stay untouched).

## No Analog Found

| File | Role | Data Flow | Reason |
|---|---|---|---|
| New Python bounded-retry wrapper for the `python` job's live-DB pytest suite (`[164.9-SHARED-TEST-TRANSPORT-FLAKE]`'s Python half; exact target file inside `analytics-service/` unmeasured — RESEARCH.md's own Open Question) | utility | transform | Grepped `analytics-service/` (excluding `.venv`) for `tenacity`/`@retry`/`def retry` — **zero hits**. No retry/backoff pattern of any kind exists anywhere in this repo's Python code today, test or production. The bash-side seam (`run_ledger_query`/`LEDGER_QUERY_CMD`) is a real analog for the SHAPE (an injectable seam a retry wraps, falsifiable via override), but there is no Python precedent for the retry MECHANISM itself — RESEARCH.md's Don't-Hand-Roll table recommends a small hand-written decorator over pulling in a new pip dependency (`tenacity`, etc.), which would need its own dependency-legitimacy justification this repo does not currently carry. The planner's first task for this half is the file-location census RESEARCH.md flags as unmeasured (Open Question #2 sibling gap), before any pattern can be assigned. |

## Metadata

**Analog search scope:** `.github/workflows/ci.yml`, `.github/workflows/supabase-migrate.yml`,
`scripts/test-ledger-drift-check.sh`, `scripts/restore-test-from-baseline.sh`,
`scripts/pg-lane/run.sh`, `scripts/mutation-runner/run.mjs`, `supabase/tests/*.sql` (corpus-wide
grep + full read of the reaper file), `TODOS.md`, `CLAUDE.md`, `docs/runbooks/shared-test-db-mutex.md`,
`src/lib/test-helpers/live-db.ts`, `src/__tests__/claim-dedupe-done-pending-children-guard.test.ts`,
`supabase/schema/baseline.sql`, `analytics-service/` (Python retry-pattern grep only).
**Files scanned:** 15 target files/edit-sites + ~10 analog source files read directly this
session (all others reused from RESEARCH.md's own verified, line-cited excerpts from the same
2026-09-21 session, re-grepped here to confirm the cited symbols/line numbers still resolve).
**Pattern extraction date:** 2026-09-21

## PATTERN MAPPING COMPLETE
