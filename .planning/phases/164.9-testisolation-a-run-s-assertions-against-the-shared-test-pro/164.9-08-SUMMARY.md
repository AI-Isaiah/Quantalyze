---
phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro
plan: 08
subsystem: testing
tags: [ci-gate, vitest, supabase, local-stack, postgrest, fixture-drift, live-db]

# Dependency graph
requires:
  - phase: 164.9 plan 03
    provides: the static fixture-drift census, its four measured findings and the shrink-only
      ledger this plan empties
  - phase: 164.9 plan 06
    provides: the two-keys / ordering-wait / ledger-retry work this plan's wave depends on
provides:
  - All four static census findings repaired at the call site; the ledger is EMPTY
  - A run-time-derived live-DB lane corpus that cannot silently shrink
  - A lane config and global setup that refuse to boot in the production-linked context
  - frontend-live-db-lane, a blocking CI job wired into the aggregator in BOTH places
  - Reference-data replay in the local-stack boot, closing a 29-failure foreign-key class
  - The EXECUTION census the static census explicitly deferred to this lane
affects:
  - "164.9 remaining plans (own [164.9-LIVEDB-LANE-EXECUTION-CENSUS])"
  - ".github/workflows/ci.yml frontend aggregate (now waits on a second container job)"

# Actuals (#2632) — chars/4 over the realized diff, same scale as the plan's estimate.
actuals:
  tokens: 22000
  tasks: 3
  commits: 5
plan_head_before: eb8978594863d157b9e85ae047a21dab022a1778

tech-stack:
  added: []
  patterns:
    - "Derived-corpus lane: the include list is computed at config-evaluation time from the
       live-DB gate symbol, IMPORTED from the static census rather than re-implemented, so the
       population MEASURED and the population EXECUTED are the same population by construction."
    - "Refusals in `globalSetup`, never in the config body: a config body is executed by any tool
       that merely ENUMERATES vitest configs (the dead-code reporter does), so a throw there
       surfaces as an unrelated job's mystery error. Measured on the first run."
    - "Ask-the-script seams over pattern-matching: `run.sh --print-workdir` joins
       `--print-baseline-path`, because any ordinary re-spelling of an assignment reads as
       'unwired' to a substring match and would leave a green check measuring nothing."
    - "Reference-data replay from allowlisted migration BYTES (scripts/extract-reference-inserts.mjs)
       rather than hand-written seed rows, so a lane's reference tables cannot become a second,
       unaudited source of truth."

key-files:
  created:
    - scripts/live-db-lane-corpus.mjs
    - vitest.livedb.config.ts
    - vitest.livedb.globalsetup.ts
  modified:
    - scripts/live-db-fixture-drift-baseline.txt
    - scripts/live-db-fixture-drift-census.mjs
    - scripts/local-stack/run.sh
    - src/__tests__/claim-dedupe-done-pending-children-guard.test.ts
    - src/__tests__/claim-failed-retry-dedupe-migrations.test.ts
    - src/__tests__/retention-crons.test.ts
    - src/__tests__/contracts/ci-docs-path-filter.contract.test.ts
    - .github/workflows/ci.yml
    - package.json

key-decisions:
  - "The lane targets the LOCAL STACK, per the plan's own reasoning, and the host decision is now
     also supported by execution: the stack booted, loaded the committed baseline and ran 477
     tests. The stated cost stands and is written into both the lane config header and the CI job
     header — a local-stack lane cannot see drift that exists only on the shared project, which
     VAC-08 and VAC-04 own."
  - "`cron` was moved OFF the REST surface onto the Management-API path the same file already
     used, NOT closed by widening `[api].schemas`. `supabase/config.toml` is byte-unchanged. The
     seven arms that read it now carry the introspection gate as well — a narrower gate in
     appearance only, because before the repair they ran and asserted NOTHING: the PostgREST read
     answered `Invalid schema: cron` unconditionally and the helper's error branch turned that
     into a `null` every arm read as 'pg_cron absent'."
  - "The lane's refusals live in `globalSetup`, not the config body. The first draft threw at
     config load and turned the dead-code reporter's output into a confusing Supabase error;
     moving them means the refusal fires exactly when the lane is actually invoked."
  - "`walkCorpus` and `LIVE_DB_GATE_SYMBOL` are EXPORTED from the census and imported by the lane
     corpus tool. Two independent walks that agree today are two walks that can disagree
     tomorrow, and the disagreement would be invisible — each tool would keep reporting a
     complete-looking answer about its own half."
  - "Reference data is replayed into the local-stack baseline from the repo's existing allowlisted
     extractor. This was NOT in the plan; it is the measured root cause of 29 of the first run's
     50 failures, and the alternative (hand-written seed rows) would have created an unaudited
     second source of truth for the reference tables."
  - "The remaining 35 failures are BOOKED, not fixed here, and not hidden. The plan's own manual
     verification prescribes exactly this — 'a RED whose cause is not in the census ledger is a
     NEW finding and gets an entry, not a retry'."

requirements-completed: ["[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"]

coverage:
  - id: D1
    description: "All four static census findings repaired at the call site; the shrink-only
      ledger goes 4 entries to 0 with its recorded ENTRY_COUNT moved in the same commit"
    requirement: "[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"
    verification:
      - kind: command
        ref: "node scripts/live-db-fixture-drift-census.mjs (0 findings, exit 0, corpus 51)"
        status: pass
      - kind: unit
        ref: "src/__tests__/live-db-fixture-drift.test.ts (4 tests, incl. the ENTRY_COUNT ratchet)"
        status: pass
    human_judgment: false
  - id: D2
    description: "A run-time-derived lane corpus that refuses to be empty, sharing one derivation
      with the census"
    requirement: "[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"
    verification:
      - kind: command
        ref: "node scripts/live-db-lane-corpus.mjs (49 test files, exit 0); --root scripts (exit 1, named MEASURE_FAIL)"
        status: pass
    human_judgment: false
  - id: D3
    description: "A lane config that declares no merged-report configuration and refuses to boot
      in the production-linked context, both refusals observed RED"
    requirement: "[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"
    verification:
      - kind: command
        ref: "npm run test:live-db with no stack (exit 1, zero skips); with a neutered --print-workdir seam (exit 1, names the repo Supabase dir)"
        status: pass
    human_judgment: false
  - id: D4
    description: "frontend-live-db-lane is a blocking CI job, wired into the aggregator's
      dependency list AND its result loop, on the strict arm with no tolerance"
    requirement: "[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"
    verification:
      - kind: command
        ref: "python3 yaml.safe_load: job present, present in frontend.needs; named 4x in the file"
        status: pass
      - kind: unit
        ref: "src/__tests__/contracts/ci-docs-path-filter.contract.test.ts (filtered-set census pin, 16 -> 17)"
        status: pass
    human_judgment: false
  - id: D5
    description: "The live-DB class EXECUTES against a real database — 362 tests pass where the
      class previously asserted nothing — and the 35 remaining failures are classified into four
      families with destinations"
    requirement: "[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]"
    verification:
      - kind: command
        ref: "bash scripts/local-stack/run.sh up && npm run test:live-db (Tests 35 failed | 362 passed | 80 skipped)"
        status: pass
    human_judgment: true
    rationale: "The measurement is mechanical, but whether a BLOCKING job that is measured red
      should merge as-is, or land behind a shrink-only execution ledger, is a decision this
      executor must not take alone — see 'The decision this plan hands back'."

duration: 175min
completed: 2026-09-21
status: complete
---

# Phase 164.9 Plan 08: The Live-DB Class Gets a Lane It Can Go Red In Summary

**The ~49 specs gated on `HAS_LIVE_DB` — a class that skipped in every CI shard and could not be
un-skipped there without invalidating the coverage ratchet a blocking gate rests on — now execute
against a real Supabase stack in `frontend-live-db-lane`, a blocking job whose corpus is derived
at run time from the same gate symbol the static census measures; all four static census findings
are repaired and its ledger is empty; and the first real execution produced the per-file census
that was owed, with 362 tests passing where the class previously asserted nothing and 35 failures
classified into four named families.**

## Performance

- **Duration:** ~175 min
- **Tasks:** 3 (plus one measured root-cause repair outside the plan)
- **Commits:** 5 (measured: `git rev-list --count eb897859..HEAD`)
- **Files:** 12 changed, 816 insertions, 60 deletions

## Task Commits

1. **Task 1: Repair every finding the census named** — `01919a2d` (fix)
2. **Task 2: A derived lane corpus and a coverage-free lane config** — `e9bf8194` (feat)
3. **Task 3: Wire the lane as a blocking job, in both places** — `ffc215b4` (feat)
4. **Deviation (Rule 1): reference-data replay in the local-stack boot** — `d435cfe0` (fix)

## Accomplishments

### Task 1 — the static ledger goes to ZERO

All four findings plan 03 seeded were closed AT THE CALL SITE. Nothing was retargeted, nothing was
added, `supabase/config.toml` is byte-unchanged, no test file was deleted, and the count of
skip-forcing calls across `src/` did not rise (measured 0 → 0 in the three files touched; the
repo-wide total is 32 and unchanged because no other file moved).

| class | identifier | repair |
|---|---|---|
| absent-column | `api_keys.key_hash` | The column has never existed. The insert now names `label` and `api_key_encrypted` — what the catalogue declares, both NOT NULL with no default. |
| absent-column | `api_keys.status` | Replaced by `is_active`, the real column. |
| unexposed-schema | `cron` | Moved off the REST surface onto the Management-API raw-SQL path the same file already used for `cron.job.command`. |
| ambiguous-overload | `claim_compute_jobs_with_priority` | The call site now names `p_unified_backbone_active`, a parameter unique to the 5-arg declaration, passed NULL — behaviour-neutral, because the 5-arg body filters only on the two `IS NULL OR …`-guarded kind lists. |

**Per-class counts, before and after** (from the census's own summary line, not restated from
memory): `4 finding(s) (0 absent-table, 2 absent-column, 1 unexposed-schema, 1 ambiguous-overload)`
→ `0 finding(s) (0, 0, 0, 0)`. No class rose. Corpus size 51 both times. The `unresolved` count is
69 both times and is still not claimed to be zero.

### Task 2 — a corpus that cannot silently shrink, and a lane that refuses

`scripts/live-db-lane-corpus.mjs` prints every test file referencing the live-DB gate symbol (49)
and a count, and exits non-zero on an empty corpus. It **imports** `walkCorpus` and
`LIVE_DB_GATE_SYMBOL` from the census rather than re-implementing them. It spawns no process at
all — no glob, no text search — which matters because most of this corpus follows no filename
convention and because this repo has a measured search-blind file carrying a deliberate NUL byte.

`vitest.livedb.config.ts` derives its include list from that tool, declares nothing of the
merged-report kind (a search for the concept over the file returns 0), and carries the four claims
in its header. `vitest.livedb.globalsetup.ts` holds the refusals.

### Task 3 — blocking, in both places, on the strict arm

`frontend-live-db-lane` is modelled on `frontend-local-stack`: the docs-only filter on one physical
line plus the detector edge, an empty-corpus preflight before anything boots, the stack through
`run.sh` only, `npm run test:live-db` with no reporting flag, and the measure-or-fail teardown
assertion. It is in the aggregator's `needs:` list AND its result loop, with **no** per-row
tolerance branch — verified structurally: the extracted loop contains `elif [ "$name" = … ]`
branches for `e2e-seeded`, `sql-tests` and `plan-anchor-verify` only.

`frontend-test` is untouched: its 125-line block is `cmp`-identical to its committed form, and the
`ci.yml` diff adds zero references to a live-DB credential.

The filtered-set census pin fired first and named the job — `job(s) GAINED the docs-only conjunct
without moving the roster: frontend-live-db-lane` — which is exactly what that pin exists to force.
Roster, test title and the `.toBe(16)` → `.toBe(17)` count all moved in the same commit.

## Falsification — every assertion observed RED before it was believed

### The census gate (Task 1), RED then GREEN

The ledger was emptied FIRST, so the repairs had something to go green against. Observed RED,
verbatim:

```
::error file=src/__tests__/claim-dedupe-done-pending-children-guard.test.ts,line=300::[absent-column] api_keys.key_hash
::error file=src/__tests__/claim-dedupe-done-pending-children-guard.test.ts,line=300::[absent-column] api_keys.status
::error file=src/__tests__/claim-failed-retry-dedupe-migrations.test.ts,line=397::[ambiguous-overload] claim_compute_jobs_with_priority
::error file=src/__tests__/retention-crons.test.ts,line=96::[unexposed-schema] cron
live-db-fixture-drift: corpus 51 file(s); 4 finding(s) (0 absent-table, 2 absent-column, 1 unexposed-schema, 1 ambiguous-overload), 69 unresolved.
::error::live-db-fixture-drift FAILED. …
EXIT=1
```

GREEN after the repairs: `corpus 51 file(s); 0 finding(s) (0 absent-table, 0 absent-column, 0
unexposed-schema, 0 ambiguous-overload), 69 unresolved.` / `EXIT=0`.

⚠️ The ledger shrink and the repairs are ONE commit on purpose: the plan's verify compares the
working tree against `HEAD`, so committing the shrink first would have made its own `-lt` check
compare the new ledger with itself.

### The absent-stack refusal, RED

`npm run test:live-db` with no `.stack-env`: exit 1, and **zero** occurrences of the word
`skipped` in the whole output.

```
Error: LIVE-DB LANE ABSENT: …/scripts/local-stack/.stack-env is missing or incomplete, so the
Supabase stack this lane needs is not running (or was torn down mid-write). Boot it with:
bash scripts/local-stack/run.sh up   (then re-run). This lane FAILS rather than skips on an
absent stack: a live-DB corpus that skips silently is the condition ROADMAP criterion 13 exists
to end.
```

### The production-linked-workdir refusal, RED

`cp` byte backup taken and `cmp`-verified, then the `--print-workdir` seam was neutered to report
the repository's own `supabase/` directory. **The neuter was asserted to APPLY** (exactly one
occurrence replaced) before anything was believed — a neuter that does not apply reads as GREEN.

```
Error: LIVE-DB LANE REFUSED: the stack runner resolves its workdir to …/supabase, which is this
repository's own Supabase context. That context is linked to PRODUCTION in this checkout and it
governs production deploys. The lane must start from a derived, throwaway workdir instead.
Refusing to boot.
```

Restored from the byte backup and `cmp`-verified identical; the seam then reported
`…/scripts/local-stack/.stack` again. ⛔ No `git checkout --`, no `git restore`, no stash was used
anywhere in this plan.

### The empty-corpus refusal, RED

`node scripts/live-db-lane-corpus.mjs --root scripts` → `corpus: 0 file(s)`, a named
`EMPTY CORPUS … MEASURE_FAIL` on stderr, exit 1.

## ⭐ The execution census — what happened when the lane actually ran

The plan filed this under "Manual-only verification" on the grounds that it needs a CI run. A
container runtime was available (measured through `run.sh --assert-teardown`, which MEASURE_FAILs
rather than passing when `docker ps` cannot answer), so the lane was booted and run here instead —
strictly better evidence than shipping a blocking gate that has never executed its corpus once.

| run | Test Files | Tests |
|---|---|---|
| first, baseline as committed | 17 failed \| 31 passed \| 1 skipped (49) | **50 failed \| 347 passed** \| 80 skipped (477) |
| after the reference-data repair | 16 failed \| 32 passed \| 1 skipped (49) | **35 failed \| 362 passed** \| 80 skipped (477) |

⭐ **362 passing tests is the headline.** That class asserted nothing in CI before this plan.

### The root cause that was fixed (deviation, Rule 1)

29 of the first run's 50 failures were ONE foreign key: `compute_jobs.kind REFERENCES
compute_job_kinds(name)`, with `compute_job_kinds` EMPTY. The baseline is schema-only by design, so
every reference table a foreign key points at comes back empty. `scripts/local-stack/run.sh` now
replays the allowlisted reference inserts through
`scripts/extract-reference-inserts.mjs` — the repo's own audited generator, slicing the ORIGINAL
BYTES of allowlisted migration statements under `scripts/restore-test-refdata-allowlist.txt`, the
same mechanism the shared-project restore uses. A generator failure and an empty emission are both
FATAL. The existing `frontend-local-stack` lane still passes 2/2 afterwards.

### The 35 that remain, classified — booked as `[164.9-LIVEDB-LANE-EXECUTION-CENSUS]`

| family | count | nature | fixable test-side? |
|---|---|---|---|
| **F1** privilege state absent from the schema-only baseline | ~14 | Tests asserting a REVOKE/GRANT holds fail because the committed dump does not reproduce column- and function-level privileges. Two say so in their own words. | ⛔ **No.** Needs a change to how `supabase/schema/baseline.sql` is dumped, which `BASELINE.md` makes a human-run command against production. |
| **F2** the post-mig-117 claim-token fence | ~14 | `mark_compute_job_failed` / `mark_compute_job_done` now REQUIRE `p_claim_token` and raise `22023` without it; the call sites predate the fence. | Yes |
| **F3** NOT-NULL columns the fixture omits | 3 | `user_notes.scope_kind`, `match_decisions.decided_by` — the same class as this plan's `api_keys` repair. | Yes |
| **F4** two one-offs | 2 | A protocol-relative URL the harness cannot parse; and one `PGRST205` schema-cache failure. | Yes |

⭐ **Two of these families are invisible to the static census BY CONSTRUCTION**, which is the
argument for the lane existing. F2 is a required-versus-defaulted RPC argument question and the
census models parameter NAMES; F3 is a MISSING required column and the census checks whether a
SUPPLIED column exists. And F4 contains `PGRST205` — the one ROADMAP signature plan 03 recorded as
**not reproducible from tracked text**, now reproduced by execution exactly as predicted.

## ⚠️ The decision this plan hands back

`frontend-live-db-lane` is wired **BLOCKING and is measured RED**. Until F1–F4 close, the
`frontend` aggregate is red on merge — and a red main CI makes Railway SKIP the analytics deploy.
That is stated here rather than discovered later.

⛔ The escape hatches are all forbidden by criterion 13 and none was taken: no test was deleted, no
test was permanently skipped, the lane's corpus was not narrowed around the failures, the row was
not made advisory, and no tolerance arm was added.

The two honest options, for whoever takes this next:

1. **Close F2/F3/F4** (≈19 failures, all fixture-side and all tractable) and land F1 as the open
   item it is.
2. **Ship behind a dated, shrink-only EXECUTION ledger** of the `scripts/vac08-ledger-baseline.txt`
   kind: every test still EXECUTES, a failure outside the ledger still reddens, and a ledger entry
   that STOPS failing also reddens. ⛔ That is not a skip list, and it is not a reason to make the
   row advisory.

This executor deliberately did not choose between them: the plan's own manual verification says a
red whose cause is not in the ledger "gets an entry, not a retry", and inventing a tolerance
mechanism the plan explicitly prohibits ("no wrapper", "never a tolerance arm") is not an executor's
call to make alone.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] `api_keys.sync_status` was `"ok"`, which the CHECK constraint does not admit**
- **Found during:** Task 1
- **Issue:** A third defect in the same payload, invisible to the static census because a CHECK is
  not a column. `api_keys_sync_status_check` admits only
  idle/syncing/computing/complete/complete_with_warnings/error/revoked/rate_limited.
- **Fix:** `"idle"`. **Commit:** `01919a2d`

**2. [Rule 2 - Missing critical] `run.sh` had no seam to ask where the stack would start**
- **Found during:** Task 2
- **Issue:** The plan requires the lane to assert the runner's workdir is not the repo's own
  Supabase directory (T-164.9-08-01). The only alternative was pattern-matching the assignment
  line, which `run.sh`'s own header records as the wrong approach — any ordinary re-spelling reads
  as "unwired" and leaves the check green while measuring nothing.
- **Fix:** `--print-workdir`, beside the existing `--print-baseline-path`, documented in the usage
  block `usage()` prints. **Commit:** `e9bf8194`

**3. [Rule 2 - Missing critical] the census's gate symbol and corpus walk were not exported**
- **Found during:** Task 2
- **Issue:** The plan's key link requires the lane and the census to read the SAME gate symbol. Two
  independent walks would have satisfied it only by coincidence.
- **Fix:** Exported `LIVE_DB_GATE_SYMBOL` and `walkCorpus`; the lane corpus tool imports both.
  **Commit:** `e9bf8194`

**4. [Rule 1 - Bug] the lane config threw at CONFIG-LOAD time**
- **Found during:** Task 2, caught by running the dead-code reporter
- **Issue:** `ERROR: Error loading vitest.livedb.config.ts (LIVE-DB LANE ABSENT: …)`. A config body
  is executed by any tool that merely enumerates this repo's vitest configs, so an unrelated job's
  output became a confusing Supabase error.
- **Fix:** Moved both refusals into `vitest.livedb.globalsetup.ts`, so they fire only when the lane
  is actually invoked. Re-ran the reporter in CI's own mode: clean. **Commit:** `e9bf8194`

**5. [Rule 1 - Bug] the local-stack baseline left every reference table EMPTY**
- **Found during:** the first real lane run
- **Issue:** 29 of 50 failures were `compute_jobs_kind_fkey` (23503).
- **Fix:** Reference-data replay from the allowlisted extractor. 50 → 35 failures.
  **Commit:** `d435cfe0`

**6. [Rule 2 - Missing critical] the plan's Task 1 verify command mishandles a ZERO count**
- **Found during:** Task 1 verification
- **Issue:** `grep -c` exits 1 when the count is 0, so the `&&`-chained verify aborted once the
  ledger reached its target state of empty. The assertion was never wrong; only its exit-code
  handling was.
- **Fix:** `|| true` on the two counting sub-shells. No assertion was weakened — `entries=0
  recorded=0 prev=4`, `REPAIRS-OK`. Recorded rather than silently adapted.

**Total deviations:** 6 auto-fixed (3 bugs, 3 missing-critical). **Impact:** deviations 2–4 are
what make the plan's own threat mitigations measurable rather than asserted; deviation 5 is the
single largest correctness win in the plan.

## Verification

| plan-level check | result |
|---|---|
| ledger shrank, recorded count moved with it, census exits 0 | PASS — `entries=0 recorded=0 prev=4`, `REPAIRS-OK` |
| no test deleted; none gained a permanent skip | PASS — `git diff --diff-filter=D -- src/` empty; skip-forcing calls 0 → 0 in the touched files |
| corpus derived, non-empty, floored, refuses empty | PASS — 49 files (floor 40); `--root scripts` exits 1 |
| lane declares no merged-report config; adds no credential | PASS — `grep -cE 'coverage' vitest.livedb.config.ts` = 0; `ci.yml` diff adds 0 credential references |
| blocking in BOTH the dependency list and the result loop, no tolerance arm | PASS — YAML-parsed; no per-row branch for this name |
| exposed-schema list and the shards' prohibition unchanged | PASS — `git diff --stat -- supabase/config.toml` empty; `frontend-test` block `cmp`-identical |
| `npx tsc --noEmit` / `npm run lint` | PASS (exit 0 each) |
| the CI-structure and ci.yml-scanning suites (20 files) | PASS — 748 passed, 1 skipped |

**Manual-only verification — still owed.** The plan asks for the lane's first real *CI* run bound
to a head commit, with the run id recorded. That cannot exist before a PR. What replaces it here is
a full LOCAL execution of the same command sequence the job runs, reported above with both
readings. ⚠️ The CI-specific half — container boot on an ubuntu runner, the aggregator arm's live
behaviour, the 30-minute timeout's adequacy — remains unverified and is called out as headroom over
an unmeasured term in the job's own header.

## Known Stubs

None. No placeholder, no `TODO`, no unwired component was introduced.

## Threat Flags

None beyond the plan's own register. Every disposition there is mitigated as designed:
`T-164.9-08-01` by an asserted, RED-observed workdir refusal reached through the runner's own seam;
`-02` by a lane that declares nothing of that kind plus a `cmp`-identical `frontend-test` block;
`-03` by a derived corpus with three independent empty-corpus controls; `-04` by synthetic
placeholders only (no repaired call site carries a connection-string- or key-shaped literal);
`-05` by a byte-unchanged `supabase/config.toml`; `-06` by a shrink-only ledger that reached zero
with nothing dropped. `-SC` does not fire — no package was installed.

## Issues Encountered

The lane is measured RED and is wired blocking. See "The decision this plan hands back". Booked as
`[164.9-LIVEDB-LANE-EXECUTION-CENSUS]` in `TODOS.md` and recorded in `.planning/WINDOWS.md`.

## Next Phase Readiness

- Criterion 13's deliverable exists and executes: the class runs where it can go RED, backed by
  refusals proven to bite, and it was not closed by deleting tests, skipping them, narrowing the
  corpus, arguing the credential-free green is sufficient, or crediting the shards.
- The per-file census the parent backlog entry records as OWED is now produced, by execution.
- ⚠️ One blocker for the phase, not for this plan: the aggregate is red until F1–F4 are answered,
  and F1 needs a human-run baseline re-dump.

---
*Phase: 164.9-testisolation-a-run-s-assertions-against-the-shared-test-pro*
*Plan: 08*
*Completed: 2026-09-21*

## Self-Check: PASSED

Files claimed as created, verified on disk:
- FOUND: `scripts/live-db-lane-corpus.mjs`
- FOUND: `vitest.livedb.config.ts`
- FOUND: `vitest.livedb.globalsetup.ts`
- FOUND: `.planning/phases/164.9-.../164.9-08-SUMMARY.md`

Commits claimed, verified in this branch's history (5 lines from the plan base to HEAD):
- FOUND: `01919a2d` fix — the four census repairs, ledger to zero
- FOUND: `e9bf8194` feat — the corpus tool, the lane config and its refusals
- FOUND: `ffc215b4` feat — the blocking CI job, wired in both aggregator places
- FOUND: `d435cfe0` fix — reference-data replay in the local-stack boot
- FOUND: `72c8e886` docs — this SUMMARY, the TODOS entry and the WINDOWS record

⚠️ `commits: 5` in the frontmatter is MEASURED, not narrated: a revision count from the
`plan_head_before` recorded in the same frontmatter to HEAD returns `5`. The working tree is clean
at the point this was written — no plan change is sitting uncommitted.
