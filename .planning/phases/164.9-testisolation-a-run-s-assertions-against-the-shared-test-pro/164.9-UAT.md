---
status: complete
phase: 164.9-testisolation
source: 164.9-10-SUMMARY.md, 164.9-11-SUMMARY.md, 164.9-VERIFICATION.md
started: 2026-09-21T20:57:52Z
updated: 2026-09-21T20:57:52Z
---

## Current Test

[testing complete]

## Tests

### 1. Criterion 8 — the post-merge `mode=restore` dispatch and its recording
expected: Two run ids, both conclusions, the committing run's summary line verbatim, and evidence the two new guards ran — written back into `164.9-11-SUMMARY.md` and the ROADMAP criterion 8 block.
result: skipped
reason: |
  ⛔ NOT RUNNABLE BEFORE MERGE, BY CONSTRUCTION — and that is a recorded decision, not an
  omission. `test-restore-from-baseline.yml` carries a `dispatch-guard` job whose `if:` is
  `github.ref != 'refs/heads/main'`, so a dispatch from this branch is refused by design; the
  guard's own comment gives the reason. Verified intact at HEAD and NOT worked around.

  ⭐ RESOLVED BY A FOUNDER DECISION TAKEN BEFORE PLAN 11 RAN, not deferred by this session:
  the `### Phase 164.9` ROADMAP entry carries a dated block, **"DECIDED 2026-09-21 — OPTION A:
  SHIP, THEN DISPATCH, THEN RECORD"**, and records that Option B (re-home the criterion to a
  successor phase) was DECLINED because it would put a routed item back into the ownerless
  state this phase exists to end.

  ⭐ OPTION A'S HONESTY CONDITION IS MET, AND IT IS WHAT MAKES THIS A RESOLVED SKIP RATHER
  THAN A PROMISE — verified at HEAD: `[164.9-CRIT8-RESTORE-DISPATCH-RECORD]` exists in
  `TODOS.md`, booked 2026-09-21, **Owner: THE FOUNDER — the human who merges this phase.
  Not a phase. Not an agent.**, with a trigger (this phase's restore-script changes reaching
  the default branch), a five-step act, an explicit closure definition and three named
  forbidden closures — including **"NOT closed by a run id alone"**.

  ⚠️ The FALSIFICATION is already done and is not what the dispatch adds: plan 07 proved both
  directions of the schema-scoped extension guard RED on a disposable cluster, plus the
  value-pinning leg, at `32/32` self-test arms. What a dispatch adds is the shared-TEST
  EXECUTION.

  ⛔ This UAT did NOT dispatch the workflow. A committing restore drops and rebuilds the
  `public` schema of a database other people's CI uses; it is a founder act.

### 2. Criterion 6 — the live `analytics_service_url` row, and the records that describe it
expected: The row holds the loopback discard sink, and the four records that disagreed about whether the write happened now agree.
result: pass
source: founder-attested write + second-connection confirmation, records re-verified at HEAD
notes: |
  The founder ran the write on 2026-09-21 with their own credential. ⛔ No agent entered, read,
  decrypted, logged or echoed it, and no agent re-read the row: this checkout's Supabase CLI is
  linked to PRODUCTION, so no agent runs a database command against any remote.

  ⭐ The evidence is STRONGER than a single-channel read-back: the committing run's own success
  line is that run's account of itself, so the resulting state was re-read through a SECOND,
  INDEPENDENT connection holding its own auth and never seeing the DSN, against the same
  database proven by the same marker. Two unrelated clients, one reading.
  ⛔ Recorded BY SHAPE — length, scheme, is-the-sink — and never as a value. Verdict recorded,
  specimen not.

  ⚠️ THE PART THAT WAS ACTUALLY BROKEN, and it is the reason this item existed: the discharge
  was appended to the TAIL of `164.9-10-SUMMARY.md` and FOUR records were left saying the
  opposite — that file's frontmatter `D3` rationale (the machine-readable field the tooling
  reads), its `THIS PLAN IS NOT COMPLETE` headline, its `Tasks completed:` line, and an
  unchecked `[164.8.1-TEST-ANALYTICS-URL-PROD]` whose last word was "pending". Found by the
  verifier; NOT caught by either review round. All four verified in agreement at HEAD, each
  carrying a dated correction with the ORIGINAL WORDING KEPT AS LINEAGE — a discharge is a
  dated event, not a reason to pretend the plan always claimed one.

### 3. Ship-time bookkeeping — plan rows and the ids this phase delivered against
expected: The eleven plan rows are checked, and the three planning-time ids are closed, amended or re-homed with a named owner.
result: pass
notes: |
  Verified at HEAD: 11 of 11 `- [x] 164.9-NN-PLAN.md` rows, `**Plans:** 11/11 plans executed`.
  ⚠️ Run through the roadmap progress handler rather than hand-edited (try the tool first), and
  it injected blank lines into THREE unrelated phase sections — the collateral this repo has
  measured before. The file was restored byte-for-byte from a `cp` backup and only the 164.9
  block re-applied; the committed diff is 12 insertions / 12 deletions in one block and touches
  no other phase.

  All three ids are `- [x]` with an amended `**Owner:**` naming what shipped. ⭐ Two carry a
  residue and say so rather than closing over it:
  - `[164.9-SHARED-TEST-TRANSPORT-FLAKE]` — PARTIAL. The retry covers READS ONLY; `rpc` sits on
    the idempotency boundary fail-closed, so a sustained RPC read-timeout still reaches a skip.
    Residue `[164.9-RPC-RETRY-NARROWED-SKIP-REOPENED]`, destination 164.9.1.
  - `[164.9-CREDENTIALED-TESTS-RED-AND-UNGATED]` — the static census prints a non-zero
    unresolved residue every run and does NOT claim it is zero; routed to execution in the lane.
  - `[164.9-MUTEX-HOLDER-DIED-UNSERIALIZED]` — clean.

  ⚠️ `VERSION` at `0.84.0.0` with no `CHANGELOG.md` entry is NOT a gap here: this repo requires
  the bump and the entry in ONE release commit, made during `/gsd-ship`.

## Summary

total: 3
passed: 2
issues: 0
pending: 0
skipped: 1
blocked: 0

## Gaps

No gaps. The one skipped item is resolved by a recorded founder decision with a booked,
owner-named recording act — not an open question.

## ⛔ What this UAT does NOT claim

⛔ It does NOT claim criterion 8 is discharged. It claims the criterion is DEFERRED BY DECISION
with its recording act booked to a named human before the merge — which is the condition the
founder set for Option A being honest. Criterion 8 stays OPEN until
`[164.9-CRIT8-RESTORE-DISPATCH-RECORD]` closes, and a run id alone does not close it.

⛔ It does NOT claim an agent verified the live row on shared TEST. That reading is
founder-attested and second-connection confirmed.
