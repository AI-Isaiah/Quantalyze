---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 04
type: execute
wave: 2
depends_on: ["164.1.1-03"]
files_modified:
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_prober_cadence_alert.py
autonomous: true
requirements: [PROBER-CADENCE-UNDELIVERED-01]
tags: [analytics-service, alerting, fastapi, anti-vacuity, sentry]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The alarm's far end EXISTS: `POST /api/prober-cadence-alert` accepts the body `prod_prober_cadence_check()` sends, and it is guarded by the SAME `SERVICE_KEY` middleware that already guards `/api/match/cron-recompute` — no new credential, no new token, no carve-out."
    - "D-03 (CTX-03) is honoured at the delivery end: the escalation implemented here is the one the plan 03 checkpoint RECORDED, and the SUMMARY names the outcome letter it was built against."
    - "The route logs a structured line on EVERY occurrence, and escalates at most once per window — the rate shape `_alert_kek_unavailable` in `analytics-service/routers/internal.py` already established in this service, and for the reason it states: an unbounded capture becomes a flood that gets muted, which is indistinguishable from having no signal."
    - "An escalation-transport failure NEVER masks or crashes the path it reports on: the capture is wrapped, and the route still answers 200 so the async post is not left looking like a service outage."
    - "The route reports what it SAW. Its log line and its escalation message carry the gap in minutes and the ceiling as sent, so an operator reads the measurement rather than a verdict."
    - "A malformed or absent body is a 4xx naming the missing field, never a silently accepted alert that logs nothing — an alarm endpoint that swallows its own input is the phase's defect at the last hop."
    - { statement: "The tests FAIL when the escalation is deleted: each is calibrated by neutering the behaviour under test and observing RED before restoring, and the SUMMARY records what was observed.", verification: test }
  artifacts:
    - "analytics-service/routers/cron.py — `POST /api/prober-cadence-alert`, its request model, its rate-limited escalation helper and its reset hook for tests"
    - "analytics-service/tests/test_prober_cadence_alert.py — accepted body, rejected body, escalation fires once per window, a raising escalation transport does not change the response, and the log line carries the measurement"
  key_links:
    - "public.prod_prober_cadence_check() → net.http_post(system_settings.analytics_service_url || '/api/prober-cadence-alert') → SERVICE_KEY middleware → the new route → structured log + rate-limited escalation"
  prohibitions:
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: test, statement: "MUST NOT put the route on an unguarded path or under `/internal`. `/internal` is gated by `INTERNAL_API_TOKEN`, a DIFFERENT credential the PROD function does not hold; using it would require a new Vault secret and a founder act for no gain." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: test, statement: "MUST NOT claim, in code comments, in the SUMMARY, in the ROADMAP or in the CHANGELOG, that the alarm reaches a human — unless the plan 03 checkpoint recorded outcome (a) or (b). Under outcome (c) the honest sentence is that the terminal hop is a log line nobody is paged on." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: privacy, status: resolved, verification: test, statement: "MUST NOT log or capture any credential, any header value, or the request's own `X-Service-Key`. The alert body carries only a cron name, an integer, an interval rendered as text and a timestamp; nothing else may be echoed." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: reliability, status: resolved, verification: test, statement: "MUST NOT let an escalation-transport failure propagate. `net.http_post` is fire-and-forget, so a 5xx here is reported nowhere; the capture is wrapped and the route answers 200 after logging." }
---

<objective>
Give the alarm a far end that exists and can be shown to run.

Purpose: plan 01 proved the database side posts, with a usable key, at the expected path. That
proves the message LEAVES. It proves nothing about anything receiving it. Without this plan the
phase would ship an alarm whose last hop is a 404 — the same defect it exists to fix, moved one
layer out and harder to see, because a fire-and-forget post reports nothing about what answered.

Output: one guarded route in the existing `/api` router, the escalation the plan 03 checkpoint
recorded, and a calibrated test suite that goes red when the escalation is removed.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-CONTEXT.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-RESEARCH.md
@analytics-service/routers/cron.py
@analytics-service/routers/internal.py
@analytics-service/sentry_init.py

⭐ Read plan 03's SUMMARY (same phase directory, `164.1.1-03-SUMMARY.md`) FIRST and build the
escalation the checkpoint recorded — (a)/(b) Sentry capture plus structured log, or (c) structured
log only. It is deliberately not an `@` context ref: it does not exist until plan 03 runs, and
`plan-anchor-verify` fails a plan whose `@` ref resolves to nothing. ⛔ If that SUMMARY does not
exist or records no outcome letter, STOP — this plan has no input.

## Decision register — see plan 01's `<context>` for the full mapping

- D-03 = CTX-03 — measure the channel before choosing it. The recorded checkpoint answer IS the channel decision; this plan implements it and does not revisit it.
- D-08 (planner) — the destination is the existing allow-listed `analytics_service_url` plus a new `/api` route guarded by the existing `SERVICE_KEY` middleware. Zero new credentials.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The guarded route — accept the alert the database sends, log the measurement, escalate once per window</name>
  <files>analytics-service/routers/cron.py</files>
  <behavior>
    - A POST to `/api/prober-cadence-alert` carrying the body the PROD function sends (`cron_name`, `gap_minutes`, `ceiling`, `detected_at`) is accepted and answers 200.
    - A body missing `gap_minutes`, or carrying a non-integer one, is a 4xx naming the field — never a 200 that logged nothing.
    - EVERY accepted alert emits one structured `logger.error` line carrying the cron name, the gap in minutes and the ceiling as sent.
    - The escalation fires at most once per window; a second alert inside the window logs but does not escalate again.
    - An escalation that RAISES does not change the response and does not propagate — the route still answers 200 and the structured log line was already emitted.
    - No credential, no header value and no request `X-Service-Key` appears in any log line or escalation message.
  </behavior>
  <read_first>
    - analytics-service/routers/cron.py — the router declaration at `analytics-service/routers/cron.py:14` (`prefix="/api"`), and the existing `POST /cron-sync` handler for this file's response and error conventions.
    - analytics-service/routers/internal.py — `_alert_kek_unavailable` at `analytics-service/routers/internal.py:76-110` and the comment block immediately above it. This is the shape to clone: a window constant, a module-level last-fired timestamp, a test-only reset helper, a structured log on EVERY occurrence, the capture guarded by the window, `set_tag` before `capture_message`, and the whole capture wrapped in `try/except` so a transport failure never masks the path it reports on.
    - analytics-service/main.py — the `SERVICE_KEY` middleware and the list of paths it skips, to confirm a new `/api` route is guarded by default and needs no registration anywhere.
    - analytics-service/sentry_init.py — the no-op-when-unset contract, so the code's own comment states it rather than implying it.
    - analytics-service/services/error_contract.py — `service_error_response`, for the error envelope shape this service uses.
  </read_first>
  <action>
    Add `POST /prober-cadence-alert` to the existing `/api` router in `analytics-service/routers/cron.py`.
    It goes in this file and not in `internal.py`: `/internal/*` is gated by `INTERNAL_API_TOKEN`, a
    credential the PROD function does not hold and would need a new Vault secret and a founder act to
    obtain, whereas `/api/*` is guarded by the `SERVICE_KEY` middleware that already carries
    `match_engine_cron_tick()`'s posts. Say that at the route, so the next reader knows the placement
    was a decision.

    Declare a request model with the four fields the database sends: `cron_name` (string),
    `gap_minutes` (integer, nullable — the observer sends NULL when there has NEVER been a contact
    row, which is the loudest case and must not be rejected as malformed), `ceiling` (string, the
    interval rendered as text) and `detected_at` (timestamp). Validate through the model so a missing
    or wrongly-typed `cron_name` or `ceiling` is a 4xx naming the field. ⛔ Do not accept an arbitrary
    dict and log whatever arrives: an alarm endpoint that swallows its own input is this phase's
    defect at the last hop.

    Clone the alert shape from `_alert_kek_unavailable`, adapted:
    - a module-level window constant and a module-level last-escalated timestamp, plus a test-only
      reset helper, exactly as that file does;
    - a `logger.error` on EVERY accepted alert, before any window check, carrying the cron name, the
      gap in minutes (or an explicit "never" when it is null) and the ceiling as sent. State in a
      comment that the RATE of these lines is itself the operator's evidence, which is the reason the
      analog gives for logging unconditionally;
    - the escalation itself guarded by the window, with `set_tag` before the capture so events are
      greppable per-cause, and the whole capture wrapped in a bare `try/except` that swallows — with
      the analog's comment, in this route's terms: a transport failure must never mask or crash the
      path it is reporting on, and here that path is an async post nothing downstream would notice
      failing.

    Implement the escalation body according to the outcome plan 03 recorded. Under (a) or (b) that is
    the `sentry_sdk` capture above. Under (c) the escalation is the structured log ONLY, and the code
    carries a comment stating, dated, that the terminal hop is a Railway log line that no human is
    paged on, and naming the TODOS entry that owns closing it. ⛔ Under (c) do not write a comment,
    a docstring or a SUMMARY sentence claiming the alarm reaches anyone.

    Answer 200 with a small acknowledgement body in this file's own response style. Say in a comment
    why the response is 200 even when the escalation could not be delivered: `net.http_post` is
    fire-and-forget, so the status code reaches nobody, and a 5xx here would add a second silent
    failure rather than surfacing the first. Note in the same comment where an operator CAN see
    delivery — `net._http_response` on the database side, which is the distinction the `cron-obs`
    prober arm exists to make and the reason `cron.job_run_details.status` is never read as evidence
    in this repository.
  </action>
  <verify>
    <automated>cd analytics-service &amp;&amp; .venv/bin/python -m pytest tests/test_prober_cadence_alert.py -q</automated>
  </verify>
  <done>The route exists on the guarded `/api` prefix, validates its body, logs the measurement on every occurrence, escalates at most once per window per the recorded checkpoint outcome, never propagates an escalation failure, and answers 200 with a comment stating why.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Calibrate the tests — each one observed RED with the behaviour neutered, then restored</name>
  <files>analytics-service/tests/test_prober_cadence_alert.py</files>
  <behavior>
    - Accepted body ⇒ 200 and exactly one structured log line carrying the gap and the ceiling.
    - `gap_minutes` null (the never-contacted case) ⇒ accepted, and the log line says so explicitly rather than printing an empty value.
    - Missing or non-integer `gap_minutes`, or a missing `cron_name`, ⇒ 4xx naming the field, and NO escalation.
    - Two alerts inside one window ⇒ two log lines, ONE escalation.
    - Two alerts either side of the window ⇒ two escalations.
    - An escalation that raises ⇒ the response is still 200 and the exception does not propagate.
    - No log record and no escalation argument contains the request's `X-Service-Key` value.
  </behavior>
  <read_first>
    - analytics-service/tests/test_cron_router.py — this suite's conventions for driving a route on this router: client construction, how `SERVICE_KEY` is provided in tests, and how log records are captured.
    - analytics-service/routers/internal.py — the test-only reset helper pattern (`_reset_kek_alert`), and how the existing tests for that path drive the window rather than sleeping.
    - analytics-service/routers/cron.py — the route as task 1 left it.
  </read_first>
  <action>
    Write `analytics-service/tests/test_prober_cadence_alert.py` covering the behaviours above. Drive
    the window through the injectable clock or the reset helper task 1 added; ⛔ never `sleep` — a test
    that spends the window in real time is a test somebody eventually marks slow and skips.

    Capture log records and assert on their CONTENT, not merely that something was logged: assert the
    gap and the ceiling appear as sent. A test asserting only that a line exists passes against a
    line that says nothing.

    ⛔ **Then calibrate, and record what you observed.** For each of the four load-bearing behaviours
    — the unconditional log, the window, the escalation-failure containment, and the body validation
    — neuter that behaviour in `analytics-service/routers/cron.py`, run the suite, and CONFIRM the
    specific test goes RED naming that behaviour. Then restore the file and confirm green. Write into
    the plan SUMMARY, per behaviour, what you neutered and what the run said.

    Two calibration traps this repository has measured before, both of which make a neuter read as a
    false green: a neuter that does not APPLY (a `find` that matched nothing) leaves the suite green
    and looks like a passing calibration, so confirm the file actually changed before you run; and a
    neuter applied to the WRONG occurrence reddens loudly while measuring nothing, so confirm the RED
    test is the one you expected and not a neighbour.

    ⛔ Restore by re-applying the original text, not with `git checkout --`. This repository carries a
    measured incident where `git checkout --` in a neuter harness silently destroyed uncommitted work
    by restoring to HEAD.
  </action>
  <verify>
    <automated>cd analytics-service &amp;&amp; .venv/bin/python -m pytest tests/test_prober_cadence_alert.py -q</automated>
  </verify>
  <done>Every listed behaviour has a test; each of the four load-bearing ones was neutered, observed RED by name, and restored; the SUMMARY records the neuter and the observed failure per behaviour; the route file is byte-identical to its task-1 state afterwards.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PROD Postgres → Railway analytics-service | an unauthenticated-by-default HTTP surface carrying a service key in a header |
| the route → the escalation transport | a third-party SDK whose configuration lives outside this repository |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1.1-15 | Spoofing | `POST /api/prober-cadence-alert` | high | mitigate | The route lives under the `/api` prefix guarded by the existing `SERVICE_KEY` middleware; it introduces no carve-out and no new token, and a test asserts an unkeyed request is refused |
| T-164.1.1-16 | Denial of Service | escalation flooding | medium | mitigate | One escalation per window with a structured log on every occurrence — the shape `_alert_kek_unavailable` already argues for in this service, because a muted signal is indistinguishable from no signal |
| T-164.1.1-17 | Information Disclosure | log lines and escalation messages | high | mitigate | The body carries only a cron name, an integer, an interval rendered as text and a timestamp; a test asserts the request's own key value appears in no record |
| T-164.1.1-18 | Repudiation | an escalation that silently fails | high | mitigate | The structured log is emitted BEFORE the window check and before the capture, so the occurrence is recorded even when the escalation cannot be delivered |
| T-164.1.1-19 | Repudiation | a route that answers 200 for an alert it dropped | medium | accept | Accepted with its reason stated at the route: the post is fire-and-forget so no status code reaches anyone, and delivery is observable on the database side through `net._http_response`, never through `cron.job_run_details.status` |
| T-164.1.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package; `sentry_sdk` and `fastapi` are already dependencies of this service |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_prober_cadence_alert.py -q` is green.
- `cd analytics-service && .venv/bin/python -m pytest -q` is green (the whole service suite, so the new route did not perturb an existing one).
- `cd analytics-service && .venv/bin/python -m mypy .` reports no new error.
- The SUMMARY records the plan 03 checkpoint outcome letter this route was built against, and — per behaviour — the neuter applied and the RED observed.
- ⚠️ Run pytest ONLY from `analytics-service/` with that interpreter. From the repository root the cassettes miss and the suite makes LIVE broker calls.
</verification>

<success_criteria>
- The alarm has a far end that exists, is guarded by an existing credential, and is proven to run.
- The escalation is the one the checkpoint recorded, and nothing anywhere claims more reach than the checkpoint established.
- Every load-bearing behaviour has been observed failing with its own code neutered.
</success_criteria>

<output>
Create `.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-04-SUMMARY.md` when done.
</output>
