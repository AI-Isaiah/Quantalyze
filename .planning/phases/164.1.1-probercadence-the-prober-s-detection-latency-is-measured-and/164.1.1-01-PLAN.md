---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260918120000_prod_prober_cadence.sql
  - supabase/schema/functions/prod_prober_cadence_check.sql
  - scripts/dump-sql-functions.ts
  - scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql
  - supabase/tests/test_prod_prober_cadence.sql
  - scripts/prod-prober/run.mjs
  - scripts/mutation-runner/run.mjs
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
autonomous: true
requirements: [PROBER-CADENCE-UNDELIVERED-01]
tags: [prod-prober, pg-cron, observability, anti-vacuity, sql-gate, migration]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-02 (CTX-02): a prod-prober run writes ONE `public.cron_runs` row under `cron_name = 'prod_prober'` at the END of the run, and it writes it whether every arm was green, some arm was red, or every arm was credential-BLOCKED — the contact row measures that the PROBER RAN, never that production is healthy."
    - "D-01 (CTX-01): that write needs NO migration. The existing `cron_runs_service_role` policy covers it, the existing `idx_cron_runs_name_recent (cron_name, completed_at DESC NULLS LAST)` index already serves the read, and every existing SQL gate over `cron_runs` is narrowed by `cron_name`, so a new name cannot perturb one."
    - "D-04 (CTX-04): `public.prod_prober_cadence_check()` compares `max(completed_at)` for `cron_name = 'prod_prober'` against a ceiling DERIVED from the measured gap distribution, declared ONCE in the migration with its sample size and its date beside it, in the lineage-comment convention `FILES_FLOOR`/`ARMS_FLOOR` use in `scripts/mutation-runner/run.mjs`."
    - "The observer is FAIL-LOUD on an ABSENT contact row: no `prod_prober` row at all is treated as STALE and alarms. A NULL that compares false and silently reports healthy is the exact defect class this phase exists to remove."
    - "D-07: criterion 3 is OBSERVED, not asserted: on a pg-lane the STALE arm sees `prod_prober_cadence_check()` return a non-NULL request id and sees ONE recorded outbound post carrying a NON-NULL `X-Service-Key` header at the expected path; the FRESH arm sees it return NULL with ZERO recorded posts. Each arm carries a `RED-UNDER` prose twin and a machine-executable `RED-UNDER-M` twin that the mutation runner proves BITES."
    - "D-05: the schema stops carrying two unexplained answers to \"is this cron alive\". `latest_cron_success()` is KEPT (it has one real, manual reader at `docs/demos/pre-flight-checklist.md`) and the `COMMENT ON TABLE public.cron_runs` sentence that claims an automated alert nothing implements is REPLACED, in a forward migration, by one that names `prod_prober_cadence_check()` as the automated monitor and `latest_cron_success()` as an admin-only point query."
    - "`node scripts/prod-prober/run.mjs --self-test` still exits 0 and prints `=== SELF-TEST PASSED`, with `SELF_TEST_SCENARIOS` bumped in the SAME edit as the scenario that proves the contact write."
    - "The absurdity floor is NOT weakened and does NOT false-fire: the contact write's seam invocation is accounted for OUTSIDE the arm delta, so `armsExecuted === 0 && seamInvocations > 0` cannot fire merely because every arm was credential-blocked while the contact row was still written."
    - { statement: "The migration is FORWARD-ONLY and no applied migration file changes by one byte; nothing in this plan runs `supabase db push`, `db reset`, `--project-ref` or `--db-url`.", verification: judgment }
  artifacts:
    - "supabase/migrations/20260918120000_prod_prober_cadence.sql — `public.prod_prober_cadence_check()` (SECURITY DEFINER, `SET search_path`, parameterless, RETURNS BIGINT), the ceiling constant with its lineage comment, the REVOKE/GRANT block, and the corrected `COMMENT ON TABLE public.cron_runs`"
    - "supabase/schema/functions/prod_prober_cadence_check.sql — the committed snapshot, generated by `npm run schema:functions`, never hand-written"
    - "scripts/dump-sql-functions.ts — one new `NAME_SET_RATCHET` row for the new function name, dated, with `clearedBy` naming the exact condition that deletes it"
    - "scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql — a lane-only `net` schema stand-in that RECORDS calls, so the alarm's post is observable instead of inferred"
    - "supabase/tests/test_prod_prober_cadence.sql — the matched-pair gate, arms S (stale ⇒ posts) and G (fresh ⇒ does not), both `RED-UNDER`/`RED-UNDER-M` annotated"
    - "scripts/prod-prober/run.mjs — exports `PROD_PROBER_CRON_NAME` and `CONTACT_INSERT_SQL`; `recordProberContact` runs after the arm loop and after the arm-delta snapshot"
  key_links:
    - "prod-prober run → recordProberContact → seams.sql('record-contact', CONTACT_INSERT_SQL) → psql → public.cron_runs(cron_name='prod_prober', completed_at=now())"
    - "PROD pg_cron tick → public.prod_prober_cadence_check() → max(completed_at) WHERE cron_name='prod_prober' → gap vs the ceiling → self-row → (stale only) net.http_post(system_settings.analytics_service_url || the alert path)"
    - "supabase/tests/test_prod_prober_cadence.sql RED-UNDER-SETUP → fixtures 01/02/07/12/15/32/33/34 + migrations 20260907120000 + 20260911120000 + 20260918120000 → arms S and G"
  prohibitions:
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT put `cron.schedule(...)` — or any write to the live PROD `cron.job` catalog — inside a migration file. Registration is a runbook-driven live op (plan 05/06), which is this repo's settled convention." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: privacy, status: resolved, verification: test, statement: "MUST NOT echo a secret, a URL VALUE, or any `system_settings` value in a `RAISE` — name the SETTING only. `RAISE` text lands in `cron.job_run_details`, in Postgres logs and in downstream shippers, and this repository is PUBLIC with `.planning/` tracked." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: test, statement: "MUST NOT introduce an unfiltered `COUNT(*)`, scan or DELETE over `public.cron_runs`. Every read and every write this plan adds is narrowed by `cron_name`." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: test, statement: "MUST NOT widen the `system_settings_analytics_service_url_allowed` CHECK constraint, add a second allow-listed destination, or introduce a new Vault secret. The alarm reuses the destination and the key `match_engine_cron_tick()` already uses." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: test, statement: "MUST NOT let an absent contact row, a NULL `max(completed_at)`, or a failed Vault/settings read report HEALTHY. Every unknown resolves to the alarm branch or to a named `RAISE`, never to silence." }
---

<objective>
Lay the tracer: one prober run's contact reaches a PROD-side ceiling verdict, end to end, through
every layer this phase touches — Node prober → `public.cron_runs` → `prod_prober_cadence_check()`
→ the outbound post — and the whole path is proven on a disposable pg-lane by a matched pair of
arms that the mutation runner shows can go RED.

Purpose: prove the architecture on ONE path before any expansion. The dead-end this catches on its
first commit rather than its tenth is the one that has already bitten this repo twice — a writer
and a reader that disagree about the row (`cron_name`, column, or freshness semantics), so the
observer reports healthy forever while the thing it observes is gone.

Output: a forward migration carrying the function and the corrected table comment; the committed
function snapshot and its name-set ratchet row; a lane-only `net` stand-in; a two-arm annotated
gate; the prober's unconditional contact write with its own self-test scenario; and both mutation
floors moved from a MEASURED run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-CONTEXT.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-RESEARCH.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-PATTERNS.md
@CLAUDE.md

## Decision register — CTX ids from CONTEXT.md, numbered D-NN here for traceability

- D-01 = CTX-01 — last-prober-contact is a `public.cron_runs` row under `cron_name = 'prod_prober'`. No second table, no new helper.
- D-02 = CTX-02 — the PROBER writes that row, at the end of every run. Not a request-side trigger: a trigger measures REQUESTS and would report healthy while GitHub drops the prober, which is the failure this phase exists to close.
- D-03 = CTX-03 — measure the channel before choosing it, and report a dead alert as a FINDING rather than routing around it. Discharged by RESEARCH.md (`cron_runs` has a live PROD writer; `latest_cron_success()` has zero programmatic callers; the reachable destination is the already-allow-listed `system_settings.analytics_service_url`). The one question repo evidence could not answer — whether the terminal hop reaches a human — is plan 03's checkpoint.
- D-04 = CTX-04 — the ceiling is DERIVED from the measured distribution and ships with its sample size and its date beside it. A round number chosen because it looks tidy is refused.
- D-05 (planner, Claude's discretion) — `latest_cron_success()` is KEPT and its false claim is corrected at its own site. It is not dead code: `docs/demos/pre-flight-checklist.md` is a real manual reader. What is dead is the sentence in `COMMENT ON TABLE public.cron_runs` claiming an automated alert nothing implements. The forward migration replaces that sentence with one naming BOTH mechanisms and what each is for, so the schema carries one AUTOMATED monitor and one documented ADMIN point query rather than two unexplained answers to the same question.
- D-06 (planner) — the observer writes ONE `public.cron_runs` row per tick under `cron_name = 'prod_prober_cadence_check'`, unconditionally, so its OWN liveness is readable the same way the prober's is. Unconditional rather than alarm-only: an alarm-only row makes "healthy" and "the observer is dead" the same empty read, which is the defect one level up. Growth is 24 rows/day and is stated beside the write as an accepted, measured cost; the existing `mt5_session_episode` writer sets the precedent that this table is not pruned.
- D-07 (planner) — criterion 3 is proven by OBSERVING the post, not by asserting a predicate. The lane gets a `net` stand-in that RECORDS calls, so the stale arm can assert an actual post to the expected path with a non-NULL `X-Service-Key` header. A predicate-only gate would leave the CRON-DRIFT-01 null-header class (seven days of silent 401s behind a green cron history) open for the new function too.
- D-08 (planner) — the destination is the EXISTING `system_settings.analytics_service_url` plus a new `/api` route in analytics-service, gated by the `SERVICE_KEY` middleware that already guards `/api/match/cron-recompute`. Zero new credentials, zero allow-list widening. Plan 04 owns the route.
- D-09 (planner) — `cron.schedule(...)` is a live op described by a runbook, never a migration. Plans 05 and 06 own it, and the `cron-manifest.json` re-capture is a STEP of that same session.
- D-10 (planner) — criterion 5's turtle (nothing watches PROD `pg_cron` itself) is an ACCEPTED, NAMED RESIDUAL. Plan 05 records it.
- D-11 (planner) — criterion 4 is VERIFY-ONLY. The header was already corrected in commit `126517a8`; plan 03 pins it with a calibration twin and does not re-edit it.

## Names this phase fixes, once, here

| Thing | Name |
|---|---|
| contact `cron_name` (written by the prober) | `prod_prober` |
| observer `cron_name` (written by the check function) | `prod_prober_cadence_check` |
| the check function | `public.prod_prober_cadence_check()` |
| the migration | `supabase/migrations/20260918120000_prod_prober_cadence.sql` |
| the gate | `supabase/tests/test_prod_prober_cadence.sql` |
| the lane `net` stand-in | `scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql` |
| the alert route path (plan 04) | `/api/prober-cadence-alert` |
| the PROD job name (plans 05/06) | `prod_prober_cadence_check` |
</context>

<tasks>

<task type="tracer">
  <name>Task 1: End-to-end "a stale prober contact reaches an outbound alarm" — the migration, one path, nothing else</name>
  <reversibility rating="costly">The `cron_name` strings, the function name and its BIGINT return contract are what plans 02, 04, 05 and 06 and the committed cron manifest all build on; changing them later touches the gate, the route, the runbook and a live PROD `cron.job` row.</reversibility>
  <files>supabase/migrations/20260918120000_prod_prober_cadence.sql, supabase/schema/functions/prod_prober_cadence_check.sql, scripts/dump-sql-functions.ts</files>
  <read_first>
    - supabase/schema/functions/match_engine_cron_tick.sql — read in FULL. This is the shape to copy: Vault read with a uniqueness guard, `system_settings` read, the SAME allow-list regex re-tested inside the body, then `net.http_post` with named arguments. Its canonical body lives in `supabase/migrations/20260911120000_vault_tick_hardening.sql`.
    - supabase/migrations/20260408113029_cron_heartbeat.sql — the `cron_runs` DDL, both indexes, the two RLS policies, the REVOKE/GRANT block for `latest_cron_success`, and at `supabase/migrations/20260408113029_cron_heartbeat.sql:48-50` the exact `COMMENT ON TABLE` sentence this task replaces. Copy that literal out of the file; do not retype it from memory.
    - supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql — the `system_settings_analytics_service_url_allowed` CHECK constraint and the seeded row.
    - supabase/schema/functions/enqueue_ledger_refresh_for_strategies.sql — the counted-`cron_runs`-row idiom on a non-happy tick: one row, `cron_name` scoped, `metadata` naming the cause by NAME.
    - analytics-service/services/mt5_session_episodes.py — the D-5 (2026-09-15, founder) module docstring recording that a NEW `cron_name` in this table needs NO migration, and the four checks that were re-measured rather than assumed.
    - scripts/mutation-runner/run.mjs — the `FILES_FLOOR` / `ARMS_FLOOR` / `WAIVED_CEILING` declarations, for the lineage-comment convention ONLY (a dated sentence naming the measurement that produced the number, sitting immediately above the constant).
    - scripts/dump-sql-functions.ts — the `NAME_SET_RATCHET` export, its row type (`name`, `side`, `capturedAt`, `clearedBy`, `reason`) and the contract block above it.
  </read_first>
  <action>
    Write ONE forward migration, `supabase/migrations/20260918120000_prod_prober_cadence.sql`. It creates a
    function and corrects a comment. It registers nothing, schedules nothing, and alters no applied
    migration file.

    **Header block.** State, in this order: that this migration ships the PROD-side observer for the
    prod-prober's cadence (Phase 164.1.1, requirement `PROBER-CADENCE-UNDELIVERED-01`); that the
    `cron.schedule(...)` statement is DELIBERATELY absent and lives in
    `docs/runbooks/prod-prober-cadence-go-live.md` as a founder-gated live op, per D-09 and the
    precedent recorded in the Phase 164.5.1 PROD session; that the function is modelled on
    `public.match_engine_cron_tick()` and reuses its destination and its Vault key, so this migration
    adds no credential and widens no allow-list (D-08); and that per D-01 the contact rows it reads
    need no schema change, naming the four facts that were VERIFIED rather than assumed (the
    `cron_runs_service_role` policy is `FOR ALL` with a matching `WITH CHECK`; the
    `idx_cron_runs_name_recent` index already serves the read; `latest_cron_success` takes a NAME and
    does not enumerate them, so a new name is inert to it; every existing SQL gate over `cron_runs` is
    narrowed by `cron_name`).

    **The ceiling constant, and its lineage comment — this is D-04 and it is the one number in this
    phase.** Declare it inside the function as a `CONSTANT INTERVAL` named `c_contact_ceiling`, value
    ten hours and twenty-five minutes. Immediately above it, write the dated derivation, in the
    convention `scripts/mutation-runner/run.mjs` uses for its floors: the sample is 273.4 hours ending
    2026-09-18, measured twice independently with identical figures; GitHub delivered 75 of 273
    expected hourly ticks, a 27 per cent delivery rate; the median gap was 3.28 h, the maximum gap
    7.13 h, and two gaps exceeded six hours. The ceiling is the observed MAXIMUM gap plus the observed
    MEDIAN gap — 7.13 + 3.28 = 10.41 h — rounded UP to the whole minute, which is ten hours and
    twenty-five minutes. State both halves of why that rule and not another: below the observed
    maximum the alarm would fire on behaviour already measured as ordinary and would be muted within a
    week, and above maximum-plus-one-typical-interval a total stoppage would go unnamed for longer
    than any gap the sample contains. ⛔ Add a sentence forbidding the number from being restated in
    prose anywhere else in the tree — this repository carries a named recurring citation-drift defect
    class — and say that every other reference is by the symbol `c_contact_ceiling`.

    **The function.** `CREATE OR REPLACE FUNCTION public.prod_prober_cadence_check() RETURNS BIGINT
    LANGUAGE plpgsql SECURITY DEFINER SET search_path = public, pg_catalog`. Parameterless on purpose:
    it takes no external input, so it carries no injection surface, exactly as
    `match_engine_cron_tick()` does. Body, in this order:

    1. Read the contact: `SELECT max(completed_at) INTO v_last FROM public.cron_runs WHERE cron_name =
       'prod_prober'`. Narrowed by `cron_name`; never an unfiltered scan.
    2. Decide. `v_stale` is TRUE when `v_last IS NULL` OR `now() - v_last > c_contact_ceiling`. ⛔ The
       NULL leg is written FIRST and explicitly, with a comment saying why: an absent contact row is
       the loudest possible signal (the prober has never run, or its rows were removed), and a bare
       interval comparison against NULL yields NULL, which every `IF` in plpgsql treats as false —
       i.e. it reports healthy. That silent-healthy shape is the defect class this whole phase exists
       to remove, so it is refused at the one place it could enter.
    3. Compute `v_gap_minutes` as whole minutes since the last contact, or NULL when there has never
       been one. This is a DURATION, not a value read from configuration, so it is safe to record.
    4. Write the observer's own row (D-06), unconditionally, BEFORE any outbound call: one INSERT into
       `public.cron_runs` with `cron_name = 'prod_prober_cadence_check'`, `started_at = now()`,
       `completed_at = now()`, `status` of `'ok'` when not stale and `'error'` when stale, `error`
       NULL when not stale and otherwise a short sentence naming the condition and the gap in minutes
       (never a URL, never a key, never a setting value), and `metadata` a `jsonb_build_object`
       carrying only names and integers — the gap in minutes, the ceiling rendered as text, and the
       contact `cron_name`. Above it, record that this is 24 rows per day, that the growth is accepted
       and unpruned, and that the existing `mt5_session_episode` writer is the precedent.
    5. If NOT stale: `RETURN NULL`. Say in a comment that a NULL return means "no alarm was raised on
       this tick" and that the gate's fresh arm reads exactly that.
    6. If stale: resolve the key and the destination, in the SAME order and with the SAME guards as
       `match_engine_cron_tick()` — count and read `analytics_service_key` from
       `vault.decrypted_secrets`, `RAISE EXCEPTION` naming the SETTING if more than one row exists or
       if the value is NULL or blank after `btrim`; read `analytics_service_url` from
       `public.system_settings`, `RAISE EXCEPTION` naming the SETTING if NULL or empty; re-test the
       value against a `c_url_allowed CONSTANT TEXT` holding the SAME regex the
       `system_settings_analytics_service_url_allowed` CHECK constraint enforces, copied from
       `supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql`, and
       `RAISE EXCEPTION` naming the setting WITHOUT echoing the offending value if it does not match.
       Then `SELECT net.http_post(url := v_url || '/api/prober-cadence-alert', headers :=
       jsonb_build_object('Content-Type', 'application/json', 'X-Service-Key', v_key), body :=
       jsonb_build_object('cron_name', 'prod_prober', 'gap_minutes', v_gap_minutes, 'ceiling',
       c_contact_ceiling::text, 'detected_at', now()), timeout_milliseconds := 60000) INTO v_req;`
       and `RETURN v_req`.

    ⛔ Nothing in step 6 is wrapped in an exception handler that swallows. If `net.http_post` is
    absent or errors, the function RAISES and the tick is red. State why in a comment: an alarm that
    catches its own delivery failure and returns normally is an alarm that reports success for a
    message nobody received, which is this phase's subject one layer down. State the honest cost in
    the same comment — a raise rolls back the observer row written in step 4, so a tick that could not
    deliver leaves no row, and that absence is itself the next tick's evidence.

    **The comment correction (D-05).** Add a `COMMENT ON TABLE public.cron_runs` that REPLACES the
    sentence currently at `supabase/migrations/20260408113029_cron_heartbeat.sql:48-50` — read that
    literal out of the file rather than retyping it. The replacement says what is true, measured
    2026-09-18: heartbeat rows are written by several independent producers, each under its own
    `cron_name`; the AUTOMATED monitor is `public.prod_prober_cadence_check()`, which watches
    `cron_name = 'prod_prober'` and posts an alert when the gap crosses `c_contact_ceiling`;
    `public.latest_cron_success(p_cron_name)` is an ADMIN-ONLY point query with no programmatic
    caller, used by hand from the pre-flight checklist, and it raises no alert of its own. Add a
    `COMMENT ON FUNCTION public.latest_cron_success(TEXT)` saying the same thing from the other side
    and naming `docs/demos/pre-flight-checklist.md` as its one reader, so the two objects describe
    their relationship rather than leaving a reader to guess it. ⛔ Do not DROP
    `latest_cron_success()`: it has a real manual reader and an existing REVOKE/GRANT posture, and
    dropping a SECURITY DEFINER function with grants is a wider act than this phase needs.

    **Grants.** `REVOKE ALL ON FUNCTION public.prod_prober_cadence_check() FROM PUBLIC, anon,
    authenticated;` then `GRANT EXECUTE ... TO service_role;`, matching the posture
    `supabase/migrations/20260409133655_function_execute_hardening.sql` established for this family.

    **Self-verification block.** End the migration with a `DO` block that RAISES unless the function
    exists, is `SECURITY DEFINER`, has a non-default `search_path` in its `proconfig`, and carries no
    EXECUTE for `anon` or `authenticated` — the same posture the file above asserts for its own three
    functions.

    Then regenerate the committed snapshot with `npm run schema:functions` and commit
    `supabase/schema/functions/prod_prober_cadence_check.sql`. ⛔ Never hand-write that file.

    Finally, add ONE `NAME_SET_RATCHET` row to `scripts/dump-sql-functions.ts`. It is REQUIRED, not
    optional: DRIFT-05 gate (a) diffs the migration-replay snapshot's function names against
    `supabase/schema/baseline.sql`, a dated PROD dump, in BOTH directions, so a function that exists
    in a migration and not yet in PROD makes `npx tsx scripts/dump-sql-functions.ts --check` exit 1
    with a `snapshot-only` finding, and `.github/workflows/sql-function-snapshot.yml` runs that check
    on every PR touching `supabase/migrations/**`. The row's `name` is the new function, `side` is
    `snapshot-only`, `capturedAt` is today's date, `reason` states that the migration ships a function
    PROD does not hold until this phase merges and applies, and `clearedBy` names the exact condition
    that deletes it: a regeneration of `supabase/schema/baseline.sql` taken AFTER this migration has
    applied to PROD. Read the contract block above the export first and honour it — a row that has
    stopped describing a disagreement is a hard failure, so the `clearedBy` must be a condition a
    later reader can actually check.
  </action>
  <verify>
    <automated>npx tsx scripts/dump-sql-functions.ts --self-test &amp;&amp; npx tsx scripts/dump-sql-functions.ts --check</automated>
  </verify>
  <done>The migration exists, creates `public.prod_prober_cadence_check()` with the ceiling constant and its dated derivation, corrects both comments, and passes its own self-verification block; `supabase/schema/functions/prod_prober_cadence_check.sql` is generated and committed; `--check` exits 0 with the new `NAME_SET_RATCHET` row carrying a checkable `clearedBy`. No applied migration file changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The prober writes its own contact row — unconditionally, outside the arm loop, without perturbing the absurdity floor</name>
  <files>scripts/prod-prober/run.mjs</files>
  <behavior>
    - A run in which all four arms are GREEN issues exactly one contact write and the run still exits 0.
    - A run in which an arm reports a defect STILL issues the contact write — the contact row records that the prober RAN, not that production is healthy.
    - A run in which EVERY arm is credential-BLOCKED still issues the contact write, and the run does NOT report `absurdity`. This is the regression the scenario exists for: `absurdityViolations` fires on `armsExecuted === 0 && seamInvocations > 0`, so a contact write counted inside the arm delta would turn a credential outage into a false MEASURE_FAIL.
    - A contact write whose psql returns `measureFail`, or a non-zero status, or an `INSERT 0 0` row count, produces a `measure-fail` defect naming the contact step and makes the run exit 1. An INSERT that silently lands zero rows is itself a defect to surface.
    - No env VALUE reaches the log or the defect detail from the contact step — the existing scrubber covers it because it runs through the same `out`/`addDefect` path.
  </behavior>
  <read_first>
    - scripts/prod-prober/run.mjs — `runProber` (the function at `scripts/prod-prober/run.mjs:376-540`), specifically its step (2) tally snapshot, step (3) arm loop, the `delta` / `seamInvocations` / `tallyByArm` computation immediately after it, and step (5) where `absurdityViolations` is applied.
    - scripts/prod-prober/run.mjs — `absurdityViolations`, and in particular the `armsExecuted === 0 && seamInvocations > 0` leg.
    - scripts/prod-prober/run.mjs — `SELF_TEST_SCENARIOS`, `DEFECT_KINDS`, and the counted-header warning block above `selfTest`.
    - scripts/prod-prober/seams.mjs — `createSeams`, the `sql(arm, query, opts)` seam, `bump`, `tally` (including `byArm`), and `realSqlRunner` (the password travels as `PGPASSWORD` in the child env, never in the URL).
    - analytics-service/services/mt5_session_episodes.py — the payload shape written into `cron_runs` and the discipline that the one write the dataset rests on is CHECKED rather than discarded.
  </read_first>
  <action>
    Add the contact write to `scripts/prod-prober/run.mjs`, as a step SEPARATE from the four arms
    (D-02, and RESEARCH Finding 5: the arms enforce a read-only SQL discipline and a write folded into
    one of them would perturb their own self-test invariants).

    Export two constants so the gate in task 3 can assert against the SAME strings the prober ships
    rather than a retyped copy: `PROD_PROBER_CRON_NAME`, the string `prod_prober`; and
    `CONTACT_INSERT_SQL`, the single INSERT statement the step issues. Write the statement to name its
    columns explicitly — `cron_name`, `started_at`, `completed_at`, `status`, `error`, `metadata` —
    with `started_at` and `completed_at` both `now()`, `status` a literal, `error` NULL, and `metadata`
    a `jsonb_build_object` carrying only names and integers. ⛔ No parameter interpolation of any
    value that came from the environment: the statement is a constant, and the only thing that varies
    between the two shipped forms is the status literal. Append a `RETURNING 1` so the row count is
    readable from `-At` stdout and a zero-row INSERT is distinguishable from a successful one.

    Two status values, and the distinction matters: `'ok'` when the run produced no defects, `'error'`
    when it did. Write them as two constants derived from one template rather than two hand-typed
    statements, and export the template, so a future edit cannot move one and leave the other behind.

    Add `export async function recordProberContact({ seams, log, addDefect, hadDefects })`. It calls
    `seams.sql("record-contact", <the chosen statement>)` and judges the result: a `measureFail`, a
    non-zero `status`, or a `stdout` that does not read as exactly one returned row each produce
    `addDefect("measure-fail", "record-contact", null, ...)` with a sentence saying WHICH of the three
    it was and that the run's own cadence record is therefore missing — so the next
    `prod_prober_cadence_check()` tick will see a widening gap for a reason that is in this log.
    Reuse the existing `measure-fail` kind; do NOT add a new `DEFECT_KIND` (a new kind would move
    `EXPECTED_DEFECT_KINDS` in `src/__tests__/prod-prober-wiring.test.ts` and would need its own
    by-name self-test assertion, for no gain — "the prober could not record its contact" is precisely
    a failure to measure).

    ⛔ **Placement, and this is the load-bearing part.** Call `recordProberContact` inside
    `runProber`, AFTER the arm loop AND AFTER the `delta` / `seamInvocations` / `tallyByArm`
    computation that snapshots the arm-only seam activity. The contact write must therefore be
    invisible to `seamInvocations` and to `armsWithSeamActivity`. Say why in a comment at the call
    site: `absurdityViolations` reports a MEASURE_FAIL when `armsExecuted === 0 && seamInvocations >
    0`, so a run in which every arm is credential-blocked while the contact row is still written would
    otherwise report the instrument as broken when it is doing exactly what D-02 requires. Report the
    contact step on its own line instead — `contact: recorded` or `contact: NOT RECORDED` — placed
    beside the existing `arms:` and `seam-invocations:` lines, and add its own tally read so the line
    states the seam call it made rather than asserting an outcome it did not measure.

    Skip the contact write, and say so on that same line, only in a NARROWED `--arm` diagnostic run:
    a narrowed run never exits 0 and is not a cadence observation, so recording it as one would put a
    fresh contact row into PROD for a run that measured almost nothing. State that reason at the guard.

    Add ONE self-test scenario covering the behaviours listed above, and bump `SELF_TEST_SCENARIOS` in
    the SAME edit — the counted set is asserted against the number of headers actually printed, so a
    stale count makes the self-test lie about its own coverage. Drive it through the real `runProber`
    with a fixture `sqlRunner`, exactly as the existing scenarios do. Include the all-arms-blocked leg
    explicitly and assert that the returned defect list contains no `absurdity` entry — that leg is
    the calibration, and without it the placement above is an assertion rather than a measurement.
  </action>
  <verify>
    <automated>node scripts/prod-prober/run.mjs --self-test</automated>
  </verify>
  <done>`--self-test` exits 0 and prints `=== SELF-TEST PASSED` with the bumped count; the new scenario proves the contact write happens on a green run, a red run and an all-blocked run; the all-blocked leg reports no `absurdity`; a failing or zero-row INSERT produces a `measure-fail` naming `record-contact` and exit 1; `PROD_PROBER_CRON_NAME` and `CONTACT_INSERT_SQL` are exported.</done>
</task>

<task type="auto">
  <name>Task 3: The matched pair, observed — a stale contact posts and a fresh one does not, proven RED by the mutation runner</name>
  <files>scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql, supabase/tests/test_prod_prober_cadence.sql, scripts/mutation-runner/run.mjs, src/__tests__/mutation-runner-floors.test.ts, src/__tests__/gate-family-meta.test.ts, src/__tests__/mutation-annotation-parser.test.ts</files>
  <read_first>
    - supabase/tests/test_analytics_service_settings_and_vault_tick.sql — the closest analog, read in full. Note `supabase/tests/test_analytics_service_settings_and_vault_tick.sql:208` (the `RED-UNDER-SETUP` apply list, including fixtures 01/02/07/12/15/32 and both vault-tick migrations) and `supabase/tests/test_analytics_service_settings_and_vault_tick.sql, arm C1` (arm C1's tolerance of the pg_net-absent SQLSTATEs, which this gate REPLACES with a stand-in rather than inherits).
    - supabase/tests/test_ledger_refresh_fanout.sql — the matched-pair reasoning in its header, and the `RED-UNDER` / `RED-UNDER-M` pairs (for example those near `supabase/tests/test_ledger_refresh_fanout.sql:700` and `supabase/tests/test_ledger_refresh_fanout.sql:755`) for the exact twin JSON shape: `arm`, `apply`, `kind`, `file`, `find`, `replace`, `occurrences`.
    - scripts/pg-lane/fixtures/33-fixture-cron-runs.sql — read the whole header. It states the fixtures/ contract, why the real heartbeat migration cannot enter an apply list, why it carries NO seed row, and why it declares no policy. The new stand-in must argue itself the same way.
    - scripts/pg-lane/fixtures/32-fixture-vault-stand-in.sql — the stand-in idiom for an extension-provided schema.
    - scripts/lint-sql-gates.mjs — the seven static rules, in particular `R5-fixture-shadows-migration-table`, `R6-fixture-shadows-fixture-table` and `R7-fixture-shadows-policy`.
    - scripts/mutation-runner/run.mjs — `FILES_FLOOR`, `ARMS_FLOOR`, `WAIVED_CEILING` and their lineage comments.
    - src/__tests__/mutation-runner-floors.test.ts — the `FILES_FLOOR ratchet` and `ARMS_FLOOR ratchet` describes, and the diagnostic text each prints when it is stale in either direction.
    - src/__tests__/gate-family-meta.test.ts — `KNOWN_THRESHOLD_SITES`, whose entries embed the floor VALUES in their labels and are asserted as an EXACT set.
  </read_first>
  <action>
    **(a) The lane `net` stand-in.** Create `scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql`.
    It creates schema `net`, a lane-only recording table `net._lane_posts` (columns: an identity or
    uuid primary key, `url TEXT`, `headers JSONB`, `body JSONB`, `timeout_milliseconds INTEGER`,
    `called_at TIMESTAMPTZ DEFAULT now()`), and a function `net.http_post` that INSERTs one row and
    returns a `BIGINT`. ⛔ Declare the function's parameters with pg_net's own NAMES and defaults —
    `url`, `body`, `params`, `headers`, `timeout_milliseconds` — so a NAMED-argument call from the
    function under test resolves against the stand-in exactly as it resolves against pg_net on PROD.
    A stand-in whose parameter names differ would not be called at all, and the arm would then be
    measuring an overload-resolution failure while reporting on an alarm.

    Write its header in the same voice as fixture 33: say that it is the fixture author's MODEL of
    pg_net and not pg_net; that it exists so the alarm's post is OBSERVABLE rather than inferred from
    an `undefined_function` SQLSTATE, which is what the analog gate had to settle for; that it proves
    the caller builds the right url, path, header set and body, and proves NOTHING about pg_net's own
    asynchrony, retry or response handling; that `net._http_response` is deliberately NOT modelled,
    because reading a response back is the `cron-obs` arm's question against PROD and not this gate's;
    and that it lives only inside `scripts/pg-lane/run.sh`'s throwaway cluster, reached only by the
    gates that name it in their own `RED-UNDER-SETUP`.

    **(b) The gate.** Create `supabase/tests/test_prod_prober_cadence.sql` with TWO arms and the
    header discipline this corpus uses.

    Header: name the function under test and state, in the `⛔ WHICH FILE THE ARMS ACTUALLY MEASURE`
    form, that the arms measure `supabase/migrations/20260918120000_prod_prober_cadence.sql` and that
    every edit-kind twin below names that file, because a `CREATE OR REPLACE` elsewhere would mutate a
    body overwritten before the first assertion runs and the arm would report `no-red` — an arm that
    cannot fail, inside the machine built to find arms that cannot fail. Then state the matched-pair
    argument in this gate's own terms: an implementation that posts on EVERY tick passes the stale arm
    alone; one that posts on NO tick passes the fresh arm alone; only the pair pins the bound.

    `RED-UNDER-SETUP` apply list, in order: fixtures `01-fixture-core.sql`,
    `02-fixture-sanitize-tables.sql`, `07-fixture-supabase-default-privileges.sql`,
    `12-fixture-profiles-is-admin.sql`, `15-fixture-auth-role.sql`, `32-fixture-vault-stand-in.sql`,
    `33-fixture-cron-runs.sql`, `34-fixture-pg-net-stand-in.sql`, then migrations
    `20260907120000_analytics_service_settings_and_vault_tick.sql`,
    `20260911120000_vault_tick_hardening.sql`, and `20260918120000_prod_prober_cadence.sql`.
    ⛔ Do not trust that list — APPLY it on a lane before writing the arms and remove or add entries
    from what the lane actually says. A list that aborts mid-apply reports nothing and reads as a
    harness fault rather than as the missing dependency it is, which is the failure fixture 33's own
    header documents.

    Setup inside the gate's transaction: seed the Vault stand-in with an `analytics_service_key` and
    ensure `system_settings` holds an allow-listed `analytics_service_url`. Each setup step gets its
    own `RAISE EXCEPTION` on failure, phrased as the analog's `*-SETUP` raises are — saying which
    later arm would otherwise be measuring something other than its own claim.

    **ARM G (fresh ⇒ silent).** Insert one `public.cron_runs` row with `cron_name = 'prod_prober'`,
    `status = 'ok'` and `completed_at = now()`. Call the function. Assert THREE things together: it
    did not raise; it returned NULL; `net._lane_posts` holds ZERO rows. Then assert the
    self-observability row (D-06): exactly one row exists with `cron_name =
    'prod_prober_cadence_check'`, `status = 'ok'`, and a NULL `error`. Every failing branch RAISEs a
    sentence that prints what it SAW — the return value, the post count, the row count — before its
    verdict.
    `RED-UNDER` twin: invert the staleness comparison in the migration so a fresh row is judged stale.
    Name the exact source snippet and its mutant, set `occurrences` to the value MEASURED against the
    file rather than assumed, and write the prose half saying what a reader would have lost: an alarm
    that fires on every tick is muted within a week and is then indistinguishable from no alarm.

    **ARM S (stale ⇒ posts, with a usable key, at the right path).** Age the contact row past the
    ceiling — `UPDATE public.cron_runs SET completed_at = now() - INTERVAL '<comfortably past the
    ceiling>' WHERE cron_name = 'prod_prober'` — and clear `net._lane_posts`. Call the function.
    Assert: it did not raise; it returned a NON-NULL bigint; `net._lane_posts` holds EXACTLY ONE row;
    that row's `url` ends with the alert path; and its `headers ->> 'X-Service-Key'` is NOT NULL and
    NOT blank. ⛔ The header assertion is not decoration — a `jsonb_build_object` built over a NULL key
    produces a header with a null value, the service answers 401, and because the post is
    fire-and-forget the scheduler records the tick as succeeded. That is the CRON-DRIFT-01 outage
    exactly, and this arm is the only place in the tree that would catch it for this function. Say
    that in the RAISE text.
    `RED-UNDER` twin: widen the ceiling in the migration so a stale row is judged fresh and the post
    never happens. Again name the exact snippet, measure `occurrences`, and write the prose half.

    ⛔ Do NOT assert the observer's own `error` row on the stale branch in THIS plan. On PROD both
    land; on a lane the post succeeds against the stand-in so the row survives — but an arm that
    depends on the stand-in's success to observe the row is asserting two things through one
    mechanism. Plan 02 adds that assertion as its own arm with its own twin.

    **(c) The floors, from a MEASURED run, in lockstep.** Run `node scripts/mutation-runner/run.mjs`
    over the full corpus and read its own output. Raise `FILES_FLOOR` to the `files N/M` numerator the
    run prints, and raise `ARMS_FLOOR` to the `biting:` line the run prints. ⛔ Never compute either
    number by arithmetic from the value already in the file — the runner gates on `annotatedFiles <
    filesFloor` and `bitingArms < armsFloor`, so a floor set below the corpus is invisible to it by
    construction, and the stale-low direction is caught one layer up by
    `src/__tests__/mutation-runner-floors.test.ts`. Separate each new value in BOTH directions on real
    runs, as the existing lineage comments do, and record the exit codes you saw. Update the lineage
    comment above each constant with the date, this phase and plan, and what moved. Then move, in the
    same commit: the `KNOWN_THRESHOLD_SITES` entries in `src/__tests__/gate-family-meta.test.ts`
    (their labels embed the VALUES and the set is asserted exactly, in both directions), the pinned
    expectations and their surrounding lineage prose in `src/__tests__/mutation-runner-floors.test.ts`,
    the floor-naming comment in `src/__tests__/mutation-annotation-parser.test.ts`, and the derivation
    prose in `.github/workflows/ci.yml` if and only if a reading there names a value that moved.
    `WAIVED_CEILING` stays 0 — a run that exits non-zero is a regression and is fixed at the cause,
    never by a waiver.
  </action>
  <verify>
    <automated>node scripts/mutation-runner/run.mjs --file supabase/tests/test_prod_prober_cadence.sql &amp;&amp; node scripts/lint-sql-gates.mjs --self-test &amp;&amp; node scripts/lint-sql-gates.mjs &amp;&amp; npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/mutation-annotation-parser.test.ts</automated>
  </verify>
  <done>Both arms pass on a green lane and each is proven RED by its own `RED-UNDER-M` twin (the runner reports both as biting, and neither as `no-red`, `occurrence-mismatch` or a waiver); `lint-sql-gates` is green on the new gate and the new fixture; both mutation floors are raised to values read off a full-corpus run and separated in both directions, and every sibling pin moved in the same commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → PROD Postgres | the prober's `psql` session writes one row with a live PROD session-pooler credential |
| PROD Postgres → Railway analytics-service | `net.http_post` carries the Vault-held `analytics_service_key` out of the database |
| repo (PUBLIC) → the world | migration text, `RAISE` strings and the Actions log are world-readable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1.1-01 | Information Disclosure | `prod_prober_cadence_check()` `RAISE` text | high | mitigate | Every guard names the SETTING and never its value, the same discipline `match_engine_cron_tick()` states at both guard blocks; `RAISE` text lands in `cron.job_run_details`, Postgres logs and downstream shippers, and this repo is public |
| T-164.1.1-02 | Information Disclosure | the alert body posted to analytics-service | medium | mitigate | The body carries only a `cron_name`, an integer gap in minutes, the ceiling rendered as text and a timestamp — no url, no key, no `system_settings` value |
| T-164.1.1-03 | Tampering | `system_settings.analytics_service_url` | high | mitigate | Two layers, reused byte-identically: the existing `CHECK` constraint, plus the SAME regex re-tested inside the function body immediately before the header is built, so the destination control survives the constraint being dropped |
| T-164.1.1-04 | Elevation of Privilege | `EXECUTE` on the new SECURITY DEFINER function | high | mitigate | `REVOKE ALL FROM PUBLIC, anon, authenticated` then `GRANT EXECUTE TO service_role`, asserted by the migration's own self-verification `DO` block |
| T-164.1.1-05 | Spoofing | the alarm's outbound header | high | mitigate | Arm S asserts `headers ->> 'X-Service-Key'` is non-NULL and non-blank; a null-valued header is the CRON-DRIFT-01 shape (401s behind a green cron history) and this is the only place in the tree that would catch it for this function |
| T-164.1.1-06 | Denial of Service | `public.cron_runs` growth from the observer's own rows | low | accept | 24 rows/day, unpruned, stated beside the write; the existing `mt5_session_episode` writer already grows this table faster and nothing prunes it |
| T-164.1.1-07 | Repudiation | a tick that could not deliver | medium | accept | A raised post rolls back the observer's own row, so an undelivered alarm leaves NO row rather than a row claiming success; the absence is the next tick's evidence, and the alternative (swallowing the delivery failure) is the defect this phase exists to remove |
| T-164.1.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package. RESEARCH.md's Package Legitimacy Audit records the same finding for the whole phase |
</threat_model>

<verification>
- `npx tsx scripts/dump-sql-functions.ts --self-test` and `--check` both exit 0.
- `node scripts/prod-prober/run.mjs --self-test` exits 0 and prints `=== SELF-TEST PASSED` with the bumped scenario count.
- `node scripts/mutation-runner/run.mjs` over the FULL corpus exits 0, prints `✅ No defects`, and its `files` and `biting:` readings equal the floors now committed.
- `node scripts/lint-sql-gates.mjs --self-test` then `node scripts/lint-sql-gates.mjs` are both green, including over the new fixture.
- `npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/prod-prober-wiring.test.ts` is green.
- `git diff --stat` shows ZERO lines changed in any migration older than this plan's own.
- Nothing in this plan ran `supabase db push`, `supabase db reset`, `--project-ref` or `--db-url`.
</verification>

<success_criteria>
- A prod-prober run records its contact under `cron_name = 'prod_prober'` whether its arms were green, red or all credential-blocked, and a failed or zero-row write is a `measure-fail` that reddens the run.
- `public.prod_prober_cadence_check()` exists, is `SECURITY DEFINER` with a pinned `search_path` and service-role-only EXECUTE, treats an ABSENT contact row as stale, and carries the ceiling as one constant with its sample and its date beside it.
- The alarm is OBSERVED: a stale contact produces exactly one recorded post to the alert path with a non-blank `X-Service-Key`; a fresh contact produces none. Both arms are proven RED by the mutation runner.
- The schema no longer carries an unexplained second answer to "is this cron alive": the table comment names the automated monitor and describes `latest_cron_success()` as the admin point query it actually is.
- Both mutation floors were raised from a measured full-corpus run and every sibling pin moved in the same commit.
</success_criteria>

<output>
Create `.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-01-SUMMARY.md` when done.
</output>
