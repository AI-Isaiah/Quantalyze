---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 03
subsystem: testing
tags: [prod-prober, checkpoint, ci-gate, anti-vacuity, alerting, sentry]

# Dependency graph
requires:
  - phase: 164.1.1-plan-01
    provides: "The PROD-side observer, the alarm's live HTTP hop to Railway analytics-service, and the wiring test file this plan extends"
provides:
  - "A recorded, dated checkpoint answer naming the terminal channel plan 04 implements: option a-sentry-already-set (SENTRY_DSN IS set on Railway analytics-service production, founder-measured 2026-09-18)"
  - "A calibrated pin — with an OBSERVED RED calibration twin — proving criterion 4's already-corrected workflow header states the measured delivery figures, without re-deriving or re-editing them"
affects: [164.1.1-plan-04]

# Actuals (#2632)
actuals:
  tokens: 1214
  tasks: 2
  commits: 1

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Named-anchor slice pin with a scratch-copy calibration twin over a workflow header, following prod-prober-wiring.test.ts's own established convention (sliceBetweenAnchors + a CALIBRATION case that mutates a copy and asserts the predicate flips)."

key-files:
  created: []
  modified:
    - src/__tests__/prod-prober-wiring.test.ts

key-decisions:
  - "Checkpoint answered: SENTRY_DSN IS set on Railway → the analytics-service project → the production environment → Variables — option a-sentry-already-set, measured by the founder on 2026-09-18. Only presence was confirmed; no value is recorded anywhere. Plan 04 implements a rate-limited sentry_sdk capture plus a structured log, modelled on _alert_kek_unavailable in analytics-service/routers/internal.py."
  - "Carried-forward caveat (stated, not implied permanent): delivery still depends on a variable outside this repository, so a later unset of SENTRY_DSN would silently re-open the gap this phase exists to close. Nothing in this plan, or plan 04, may claim the alarm reaches a human without that caveat attached."
  - "Criterion 4 stays verify-only, per D-11: the workflow header's measured-delivery figures were already corrected in commit 126517a8. This plan pins them with a calibrated test and does not re-derive or re-edit the language. .github/workflows/prod-prober.yml is byte-unchanged."

requirements-completed: [PROBER-CADENCE-UNDELIVERED-01]

coverage:
  - id: D1
    description: "The alarm's last-hop question is answered, dated and measured by a human before any code commits to a terminal channel: SENTRY_DSN IS set on Railway analytics-service production (option a-sentry-already-set), founder-measured 2026-09-18, with the outside-this-repo delivery caveat written down beside it — never the variable's value."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification: []
    human_judgment: true
    rationale: "A checkpoint:decision outcome is inherently a fact only a human can establish by looking at the Railway dashboard — no test in this repository can substitute for that measurement, and the resume-signal answer was supplied directly by the orchestrator's checkpoint-already-resolved instruction (founder decision 2026-09-18), not inferred or guessed."
  - id: D2
    description: "Criterion 4 is pinned and calibrated, not re-done: a named-anchor slice test asserts the .github/workflows/prod-prober.yml schedule region states the measured delivery rate (27%), median gap (3.28h), max gap (7.13h), a date, and a reference to Phase 164.1.1; its calibration twin strips those figures from a scratch copy of the sliced region and asserts the predicate reports each one missing BY NAME — manually neutered and observed RED before being restored, confirming the pin is a measurement and not an assertion over a file nobody mutated."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: unit
        ref: "src/__tests__/prod-prober-wiring.test.ts#[164.1.1-03] criterion 4 — the schedule header states the measured delivery"
        status: pass
    human_judgment: false

# Metrics
duration: ~20min
completed: 2026-09-18
status: complete
---

# Phase 164.1.1 Plan 03: The checkpoint answered and criterion 4 pinned — Summary

**The alarm's last hop is a dated, founder-measured fact (Sentry, SENTRY_DSN confirmed set 2026-09-18) recorded before any code commits to it, and criterion 4's already-corrected workflow header is pinned with a calibration twin observed flipping RED and restored.**

## Performance

- **Duration:** ~20 min
- **Completed:** 2026-09-18T07:30:00Z
- **Tasks:** 2/2
- **Files modified:** 1

## Accomplishments
- Recorded the checkpoint answer: `SENTRY_DSN` IS set on Railway → analytics-service → production → Variables, measured by the founder on 2026-09-18 (option `a-sentry-already-set`). No credential value is recorded anywhere in this repository — presence only, per the plan's own privacy prohibition.
- Carried the outside-this-repo caveat forward explicitly: delivery still depends on a variable this repository cannot see, so a later unset would silently re-open the gap. Neither this SUMMARY nor any future plan may claim the alarm permanently reaches a human without that caveat attached.
- Added ONE new `describe` block to `src/__tests__/prod-prober-wiring.test.ts` pinning criterion 4: a named-anchor slice of the `.github/workflows/prod-prober.yml` schedule region, asserting it states the measured delivery rate, median gap, max gap, a date, and a Phase 164.1.1 reference — all read off the file itself, never restated from planning prose.
- Added the calibration twin immediately below: a scratch copy of the sliced region with every figure stripped, asserting the predicate reports each one missing BY NAME. Manually neutered the predicate function itself (returned `[]` unconditionally), ran the suite, observed the calibration test go RED with the exact expected diff (`- ["a date", "a reference to Phase 164.1.1", "measured delivery rate", "measured max gap", "measured median gap"]` vs `+ []`), then restored the real predicate and confirmed the file byte-identical to the intended version via `cmp`.
- `.github/workflows/prod-prober.yml` is byte-unchanged (`git diff` — 0 lines) — criterion 4 was verified and pinned, not re-derived.

## Task Commits

Each task was committed atomically:

1. **Task 1: CHECKPOINT — does the alarm's last hop reach a human?** — no code commit. `type="checkpoint:decision" gate="blocking-human"`; the decision was already answered by the founder before this executor ran (per the orchestrator's checkpoint-already-resolved instruction). Recorded in this SUMMARY's frontmatter (`key-decisions`, `coverage.D1`) rather than re-asked or re-opened.
2. **Task 2: Pin criterion 4 where it already holds** - `9a1b0a8b` (test)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `src/__tests__/prod-prober-wiring.test.ts` - new `describe("[164.1.1-03] criterion 4 — the schedule header states the measured delivery")` block: a `sliceBetweenAnchors`-based slice of the `on.schedule` region, a positive assertion over five named figures, and a calibration twin stripping them from a scratch copy

## Decisions Made
- Checkpoint answered as `a-sentry-already-set` — see `key-decisions` above. This is the load-bearing decision of the plan; it unblocks plan 04's escalation implementation without guessing or inferring from the existence of `docs/runbooks/sentry-triage.md` (that runbook proves the org uses Sentry, not that this service's variable is set — the plan's own explicit prohibition).
- No architectural or code changes were needed to answer the checkpoint; it is a recorded fact, not an implementation.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None for this plan. The plan's `user_setup` block (confirming `SENTRY_DSN` is set on Railway analytics-service production) was the substance of Task 1's checkpoint and is already resolved — the founder measured it directly in the Railway dashboard on 2026-09-18. No further action is needed before plan 04 proceeds, subject to the standing caveat that this is an external fact this repository cannot independently re-verify.

## Next Phase Readiness
- Plan 04 can implement the Sentry-based escalation route directly against the `a-sentry-already-set` answer: a rate-limited `sentry_sdk` capture plus a structured log on every occurrence, modelled on `_alert_kek_unavailable` in `analytics-service/routers/internal.py`, landing in the triage practice `docs/runbooks/sentry-triage.md` already documents. No new credential, no new dependency.
- Criterion 4 is closed for this phase: verified, pinned, calibrated. Nothing further is owed on it.
- Standing caveat for plan 04 and beyond: the phase must state, not imply, that delivery depends on a Railway environment variable outside this repository's control.

## Self-Check: PASSED

`src/__tests__/prod-prober-wiring.test.ts` confirmed on disk with the new describe block; commit `9a1b0a8b` confirmed in `git log --oneline --all`; `npx vitest run src/__tests__/prod-prober-wiring.test.ts` re-run clean at 102/102 passing after restoring the predicate; `git diff -- .github/workflows/prod-prober.yml` confirmed empty (0 lines).

---
*Phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and*
*Completed: 2026-09-18*
