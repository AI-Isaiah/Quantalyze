---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
verified: 2026-09-18T17:35:17Z
status: human_needed
score: 5/5 must-haves verified (plans 01-05 only; plan 06 out of scope — see human_verification)
behavior_unverified: 0
overrides_applied: 0
covered_files:
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-01-PLAN.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-01-SUMMARY.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-02-PLAN.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-02-SUMMARY.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-03-PLAN.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-03-SUMMARY.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-04-PLAN.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-04-SUMMARY.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-05-PLAN.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-05-SUMMARY.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-06-PLAN.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-CONTEXT.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-REVIEW.md
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-SECURITY.md
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_limiter_route_coverage.py
  - analytics-service/tests/test_prober_cadence_alert.py
  - docs/runbooks/README.md
  - docs/runbooks/prod-prober-cadence-go-live.md
  - scripts/dump-sql-functions.ts
  - scripts/mutation-runner/run.mjs
  - scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql
  - scripts/prod-prober/cron-manifest.json
  - scripts/prod-prober/run.mjs
  - src/__tests__/gate-family-meta.test.ts
  - src/__tests__/mutation-annotation-parser.test.ts
  - src/__tests__/mutation-runner-floors.test.ts
  - src/__tests__/prod-prober-wiring.test.ts
  - supabase/migrations/20260918120000_prod_prober_cadence.sql
  - supabase/schema/functions/prod_prober_cadence_check.sql
  - supabase/tests/test_prod_prober_cadence.sql
covered_digest: "v1:sha256:b4d548380633e1ac3907a8d0f1b70969ce820bb20b3bac37ef6a97014b4bc894"
human_verification:
  - test: "Plan 06 — run docs/runbooks/prod-prober-cadence-go-live.md end to end against live PROD: confirm the P0 database-marker query names PRODUCTION, run all five blocking pre-flight checks (P0-P4), execute `SELECT cron.schedule('prod_prober_cadence_check', '20 * * * *', $$SELECT public.prod_prober_cadence_check();$$);`, then in the SAME session run `node scripts/prod-prober/run.mjs --capture-manifest --out <scratch path>`, diff it against the committed `scripts/prod-prober/cron-manifest.json` (expect ONLY the new job row to differ), move it into the committed manifest and commit, then re-run `node scripts/prod-prober/run.mjs --preflight-repoint` and confirm it passes."
    expected: "A live PROD `cron.job` row named `prod_prober_cadence_check` exists, runs hourly at minute 20, and calls `SELECT public.prod_prober_cadence_check();`. `scripts/prod-prober/cron-manifest.json` carries that row (16 jobs total) in the same commit as the registration. `--preflight-repoint` exits 0, proving the committed manifest and live PROD agree again. At the first scheduled tick (minute 20), a new `public.cron_runs` row appears under `cron_name = 'prod_prober_cadence_check'` with `status = 'ok'` (if the prober has contacted within the ceiling) or `status = 'error'` plus one recorded post to `/api/prober-cadence-alert` (if stale) — recorded auditably in a new `164.1.1-PROD-SESSION.md`, per the runbook's own required artifact. `TODOS.md`'s `[PROBER-CADENCE-UNDELIVERED-01]` checkbox is then, and only then, closed with the date, the jobid, and a pointer to the session record."
    why_human: "Plan 06 is `autonomous: false` — a founder-gated live write to the linked-to-PRODUCTION Supabase CLI's `cron.job` catalog. Nothing in this repository can execute or simulate it: it requires a human to run the database-marker query, read its answer, and decide whether to proceed against a shared production database. The task dispatching this verification explicitly forbids verifying, simulating, or assuming plan 06's outcome. Confirmed absent on this branch: no `164.1.1-06-SUMMARY.md`, no `164.1.1-PROD-SESSION.md`, and `scripts/prod-prober/cron-manifest.json` carries 15 jobs with no `prod_prober_cadence_check` row."
---

# Phase 164.1.1: PROBERCADENCE Verification Report

**Phase Goal:** Give the prod-prober a detection latency that is MEASURED and ENFORCED rather than declared.
**Verified:** 2026-09-18T17:35:17Z
**Status:** human_needed
**Scope of this verification:** Plans 01–05 ONLY, per explicit dispatch instruction. Plan 06 (`autonomous: false`, the live PROD `cron.schedule(...)` registration session) has not run and is recorded below as the phase's one outstanding human-verification item — never as a gap, never as done.

## Goal Achievement

### Observable Truths (ROADMAP's 5 Success Criteria, plans 01-05 scope)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | PROD records last-prober-contact on every arm run, written by the PROBER itself, never inferred from GitHub's API | ✓ VERIFIED | `scripts/prod-prober/run.mjs` exports `PROD_PROBER_CRON_NAME`/`CONTACT_INSERT_SQL`; `recordProberContact()` runs after the arm loop, outside the seam-delta snapshot. Ran `node scripts/prod-prober/run.mjs --self-test` myself: **83/83 scenarios PASSED**, including the calibration leg proving the contact write fires unconditionally on green, red AND all-4-arms-credential-blocked runs without tripping the absurdity floor, and that a failed/zero-row write is itself a `measure-fail`. |
| 2 | A PROD-side check fails loud when the contact gap exceeds a stated ceiling; the ceiling is a MEASURED number with sample+date beside it | ✓ VERIFIED | `supabase/migrations/20260918120000_prod_prober_cadence.sql:58-78` declares `c_contact_ceiling CONSTANT INTERVAL := '10 hours 25 minutes'` with the dated derivation (273.4h sample, 27% delivery, median 3.28h, max 7.13h) immediately above it, per the `FILES_FLOOR`/`ARMS_FLOOR` lineage convention. The absent-row NULL leg is written first and explicit (`:103-110`) — refuses the "NULL reads as healthy" trap by construction. Confirmed migration is LIVE on PROD (`gh run view 35347643879`: `apply-test`, `apply`, `apply-test-verdict`, `prod-credential-divergence-verdict` all `success`, head `eec8a659`). |
| 3 | The alarm is proven to fire: observed RED with a stale/absent contact row and GREEN with a fresh one | ✓ VERIFIED | `supabase/tests/test_prod_prober_cadence.sql` — 7 arms (G1/S1 plan 01, O1/A1/N1/U1/V1 plan 02), each with a `RED-UNDER` prose twin AND a `RED-UNDER-M` machine-executable twin. Plan 02's SUMMARY records a full-corpus mutation run at `arms: 402/402/0, biting: 402, ✅ No defects`. I independently ran the four vitest pin files that gate this mechanism (`mutation-runner-floors`, `gate-family-meta`, `mutation-annotation-parser`, `prod-prober-wiring`): **312/312 passed**, `ARMS_FLOOR=402` confirmed at `scripts/mutation-runner/run.mjs:1979`. |
| 4 | `.github/workflows/prod-prober.yml`'s header stops claiming "within one tick"; states the measured delivery rate, date, and points at this phase | ✓ VERIFIED | Already corrected at commit `126517a8` (byte-unchanged by this phase, confirmed `git diff` empty per plan 03). Pinned by a calibrated test in `src/__tests__/prod-prober-wiring.test.ts` (`[164.1.1-03] criterion 4`), part of the 312/312 vitest run above; its calibration twin was manually neutered and observed RED per the plan 03 SUMMARY's own log. |
| 5 | The alarm's own reachability is stated honestly: pg_cron stopping is either covered or recorded as an accepted, named residual — no unexamined turtle | ✓ VERIFIED | `TODOS.md:3634` carries `[PGCRON-LIVENESS-UNWATCHED-01]` — measured, disposed as an accepted residual on two named grounds, manual path marked manual, left explicitly UNROUTED. Cross-referenced from `docs/runbooks/prod-prober-cadence-go-live.md:307`. A second, review-discovered residual (`[PROBER-ALERT-DELIVERY-UNVERIFIED-01]`, TODOS.md:3680 — nothing watches whether THIS alarm's own outbound post was delivered) was also disposed and cross-referenced from `analytics-service/routers/cron.py:1237-1241`. |

**Score:** 5/5 truths verified for the scope in play (plans 01-05). Plan 06 — the live registration that makes the observer actually run on a schedule — is not yet executed and is the phase's one open item (see Human Verification).

### Plan-level must-haves (spot-checked beyond the roadmap criteria)

| Must-have | Status | Evidence |
|---|---|---|
| Migration is forward-only, applied migrations untouched | ✓ VERIFIED | Confirmed live on PROD via CI run 35347643879; `supabase/schema/functions/prod_prober_cadence_check.sql` regenerated (`npx tsx scripts/dump-sql-functions.ts --self-test && --check` ran clean: `SELF-TEST PASS: 22 assertions`, `SQL function snapshot is current (121 functions)`, `DRIFT-05 gate (a)... 0 ratcheted disagreement(s)`). |
| EXECUTE revoked from PUBLIC/anon/authenticated/**service_role**, no GRANT follows | ✓ VERIFIED | `grep REVOKE\|GRANT supabase/migrations/20260918120000_prod_prober_cadence.sql` — line 295: `REVOKE ALL ... FROM PUBLIC, anon, authenticated, service_role;`, no `GRANT` anywhere in the file. Matches SECURITY.md's independently re-verified fix for the HIGH finding (`b409adbf`). |
| `COMMENT ON TABLE public.cron_runs` / `COMMENT ON FUNCTION public.latest_cron_success` corrected (D-05) | ✓ VERIFIED | `supabase/migrations/20260918120000_prod_prober_cadence.sql:225-263` — both comments present, guarded with `DO $$ IF EXISTS $$` for lane portability, naming the automated monitor and the admin-only point query correctly. |
| `POST /api/prober-cadence-alert` exists, guarded by existing `SERVICE_KEY` middleware, no new credential | ✓ VERIFIED | `analytics-service/routers/cron.py:1214` — route under `/api` prefix. Ran `cd analytics-service && .venv/bin/python -m pytest tests/test_prober_cadence_alert.py -q` myself: **12 passed**. |
| Escalation rate-limit window scoped by `cron_name` (WR-02 fix) | ✓ VERIFIED | `analytics-service/routers/cron.py:1161` — `_last_prober_cadence_alert_at: dict[str, float] = {}`, keyed by `alert.cron_name` at `:1199-1202`. Confirms SECURITY.md's re-verification of fix `17046cca`. |
| `NO_LIMITER_QUARANTINE` comment softened to match enforced behavior (WR-03 fix) | ✓ VERIFIED | `analytics-service/tests/test_limiter_route_coverage.py:341-348` — reads "gated by the SAME shared SERVICE_KEY... no additional caller restriction is enforced", matching SECURITY.md's re-verification. |
| Route docstring correctly attributes the delivery-watch gap rather than implying `cron-obs` covers it (WR-01 fix) | ✓ VERIFIED | `analytics-service/routers/cron.py:1226-1240` — docstring names `[PROBER-ALERT-DELIVERY-UNVERIFIED-01]` and states `cron-obs` does NOT watch this function's posts. |
| Runbook: blast radius before pre-flight, 5 blocking pre-flight checks, exact `cron.schedule` statement, same-session manifest re-capture, rollback, no forbidden CLI verbs, no secrets/absolute paths | ✓ VERIFIED | Read `docs/runbooks/prod-prober-cadence-go-live.md` in full (330 lines). Section order matches; P0 (database marker) is first and blocking; P1's query matches the one the orchestrator ran directly against live PROD verbatim. `grep -n "db push\|--project-ref\|--db-url\|db reset"` — 0 hits. `grep` for a home-path prefix, the local username, `password`, `secret_value` and `service_role_key` (pattern spelled escaped here on purpose — quoting it literally is itself the leak the rule forbids) — 0 hits. Both residual ids (`PGCRON-LIVENESS-UNWATCHED-01`, `PROBER-ALERT-DELIVERY-UNVERIFIED-01`) resolve in both the runbook and `TODOS.md`. Indexed in `docs/runbooks/README.md:31`. |
| `TODOS.md[PROBER-CADENCE-UNDELIVERED-01]` NOT closed by plans 01-05 | ✓ VERIFIED | `TODOS.md:3588` — checkbox still `[ ]`, carries a dated progress note (per plan 05) naming what's shipped and that only the live registration (plan 06) remains. |

## Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `supabase/migrations/20260918120000_prod_prober_cadence.sql` | Forward migration: function, ceiling, comment fixes, grants, self-verification | ✓ VERIFIED | Exists, applied to PROD, self-verification `DO` block present, all content matches plan/SUMMARY claims. |
| `supabase/schema/functions/prod_prober_cadence_check.sql` | Committed snapshot | ✓ VERIFIED | Exists; `dump-sql-functions --check` confirms currency and DRIFT-05 gate (a) agreement. |
| `scripts/pg-lane/fixtures/34-fixture-pg-net-stand-in.sql` | Lane-only `net.http_post` recording stand-in | ✓ VERIFIED | Exists (4.3KB), referenced by the gate's `RED-UNDER-SETUP` apply list. |
| `supabase/tests/test_prod_prober_cadence.sql` | 7-arm matched-pair SQL gate | ✓ VERIFIED | Exists (46KB, grown from plan 01's 2 arms to plan 02's 7); referenced in CI logs as executing on `sql-tests` (now correctly lane-excluded per Phase 164.1.1.1). |
| `scripts/prod-prober/run.mjs` — `recordProberContact`, `PROD_PROBER_CRON_NAME`, `CONTACT_INSERT_SQL` | Unconditional contact write | ✓ VERIFIED | Confirmed via `--self-test` run (83/83). |
| `analytics-service/routers/cron.py` — `POST /prober-cadence-alert` | Guarded route, rate-limited escalation | ✓ VERIFIED | Confirmed via direct read and `pytest` run (12/12). |
| `docs/runbooks/prod-prober-cadence-go-live.md` | Go-live runbook | ✓ VERIFIED | Read in full; matches all plan 05 must-haves. |
| `TODOS.md` — `PGCRON-LIVENESS-UNWATCHED-01`, `PROBER-ALERT-DELIVERY-UNVERIFIED-01`, progress note | Named residuals, open checkbox | ✓ VERIFIED | All three confirmed present at the cited line ranges. |

## Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `scripts/prod-prober/run.mjs` (`recordProberContact`) | `public.cron_runs(cron_name='prod_prober')` | `seams.sql('record-contact', CONTACT_INSERT_SQL)` | ✓ WIRED | Confirmed via self-test scenario 83. |
| PROD `pg_cron` tick (when scheduled) | `public.prod_prober_cadence_check()` | `max(completed_at)` read narrowed by `cron_name` | ✓ WIRED | Confirmed by reading the migration body directly; NOT yet invoked on a schedule (plan 06 pending). |
| `prod_prober_cadence_check()` | `POST /api/prober-cadence-alert` | `net.http_post` with Vault key + allow-listed destination | ✓ WIRED | Confirmed end-to-end on a disposable pg-lane per plan 01/02 SUMMARYs (`arms: 402/402/0`); route confirmed live via Railway deploy matching the merged SHA (per dispatch-provided fact) and via direct pytest run. |
| `POST /api/prober-cadence-alert` | Sentry (`sentry_sdk.capture_message`) | rate-limited `_escalate_prober_cadence_gap`, keyed by `cron_name` | ✓ WIRED | Confirmed via code read + pytest (window/no-window/failure-containment tests all passed). |
| Runbook Step 1 (`cron.schedule`) | Runbook Step 2 (`--capture-manifest`) | same-session re-capture, `cron-manifest.json` | ⚠️ NOT YET EXECUTED | Both steps exist and are documented correctly; neither has run. This is the plan 06 human-verification item, not a wiring defect. |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Migration self-verification & snapshot currency | `npx tsx scripts/dump-sql-functions.ts --self-test && --check` | `SELF-TEST PASS: 22 assertions`; `SQL function snapshot is current (121 functions)`; DRIFT-05 gate (a) 0 disagreements | ✓ PASS |
| Prober's unconditional contact write (green/red/all-blocked) | `node scripts/prod-prober/run.mjs --self-test` | `SELF-TEST PASSED: 83/83 scenarios` | ✓ PASS |
| Mutation-gate pin files (floors, meta, annotation parser, criterion-4 pin) | `npx vitest run src/__tests__/mutation-runner-floors.test.ts src/__tests__/gate-family-meta.test.ts src/__tests__/mutation-annotation-parser.test.ts src/__tests__/prod-prober-wiring.test.ts` | 4 files, 312/312 passed | ✓ PASS |
| Alert route (accept/reject/window/leak/auth) | `cd analytics-service && .venv/bin/python -m pytest tests/test_prober_cadence_alert.py -q` | 12 passed, 1 unrelated deprecation warning | ✓ PASS |
| Migration live on PROD | `gh run view 35347643879 --json conclusion,jobs` | `apply`, `apply-test`, `apply-test-verdict`, `prod-credential-divergence-verdict` all `success` at head `eec8a659` | ✓ PASS |
| Manifest state (15 jobs, no `prod_prober_cadence_check` row) | `node -e "...cron-manifest.json..."` | `jobs: 15`, `has prod_prober_cadence_check: false` | ✓ PASS (confirms un-registered, expected) |
| Branch CI health (SHA-bound, per CLAUDE.md's own trap warning) | `gh api .../actions/runs/{id}/jobs` at three successive SHAs | `eec8a659` (merge): `sql-gate-lint`/`sql-tests`/`frontend` all FAILED (baseline SNAPSHOT_MISSING + lane-only fixture on shared TEST); `eeeee320` (baseline regen): `sql-gate-lint` fixed, `sql-tests`/`frontend` still FAILED (`net._lane_posts does not exist` on shared TEST); `f04d224a` (Phase 164.1.1.1 LANEONLYGATES fix): `sql-gate-lint`, `sql-mutation`, `sql-tests`, `frontend` all `success` | ✓ PASS — confirms the dispatch's "sql-tests is green on shared TEST" claim by independently re-deriving it, and confirms the intermediate red states were real, understood, and are now closed |

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|---|---|---|---|---|
| `PROBER-CADENCE-UNDELIVERED-01` | 01, 02, 03, 04, 05, (06 pending) | The prod-prober's detection latency must be measured and alarmed from a scheduler that cannot silently drop it | ⚠️ PARTIALLY SATISFIED (by design at this checkpoint) | Plans 01-05 ship every artifact the requirement needs (observer, gate, route, runbook, disposed residuals) and TODOS.md explicitly records the requirement stays open until plan 06 lands the live registration. This is the correct, honest state — not a gap in plans 01-05's own delivery. |

No orphaned requirements found: `TODOS.md` names `PROBER-CADENCE-UNDELIVERED-01` as this phase's sole owned entry, and all six plans declare it in `requirements:`.

## Anti-Patterns Found

None blocking. Reviewed `164.1.1-REVIEW.md` (deep review, 18 files, 0 critical, 4 warnings, 3 info) and independently re-verified all four warnings are either informational-only (WR-04, a CI-timeout trend correctly routed to Phase 164.4.2, out of this phase's scope) or have landed fixes I confirmed myself in the current tree (WR-01, WR-02, WR-03 — see the must-haves table above). No `TBD`/`FIXME`/`XXX` markers found in any covered file. No placeholder/stub patterns found in the migration, the route, or the runbook.

## Human Verification Required

### 1. Plan 06 — the live PROD registration session

**Test:** Run `docs/runbooks/prod-prober-cadence-go-live.md` end to end against live PROD: confirm the P0 database-marker query names PRODUCTION, run all five blocking pre-flight checks (P0-P4), execute the `cron.schedule(...)` statement, then in the SAME session re-capture and commit the manifest, then confirm `--preflight-repoint` passes.

**Expected:** A live PROD `cron.job` row named `prod_prober_cadence_check` running hourly at minute 20; the committed manifest carries it (16/16 jobs agree); the first tick is observed and recorded in a new `164.1.1-PROD-SESSION.md`; `TODOS.md[PROBER-CADENCE-UNDELIVERED-01]` is closed with the date, jobid and a pointer to that record.

**Why human:** `autonomous: false` — a founder-gated live write to the linked-to-PRODUCTION Supabase CLI's `cron.job` catalog, requiring a human to run and interpret the database-marker query before any write. Nothing in this repository can execute, simulate, or substitute for that judgment. Per this dispatch's explicit scope boundary, plan 06 was not verified, simulated, or assumed complete. Confirmed absent on disk: no `164.1.1-06-SUMMARY.md`, no `164.1.1-PROD-SESSION.md`; `scripts/prod-prober/cron-manifest.json` carries 15 jobs with zero rows named `prod_prober_cadence_check`.

## Gaps Summary

None. Every truth, artifact and key link within the plans-01-05 scope this dispatch defined is present, substantive, wired, and — where a behavioral test existed — independently re-run and confirmed passing by this verifier (not merely read off a SUMMARY). The phase's only outstanding item is the founder-gated plan 06 live session, which is correctly un-executed, correctly un-claimed as done anywhere in the tree, and routed here as the phase's sole human-verification item rather than as a gap or a failure.

---

_Verified: 2026-09-18T17:35:17Z_
_Verifier: Claude (gsd-verifier)_
