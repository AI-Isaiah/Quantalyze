---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 05
subsystem: database
tags: [runbook, pg-cron, live-op, residual-risk, todos, prod-prober]

# Dependency graph
requires:
  - phase: 164.1.1-plan-02
    provides: "public.prod_prober_cadence_check(), all seven proven RED arms, FILES_FLOOR/ARMS_FLOOR at 47/402"
  - phase: 164.1.1-plan-04
    provides: "POST /api/prober-cadence-alert, guarded and calibrated, and the recorded checkpoint outcome a-sentry-already-set"
provides:
  - "docs/runbooks/prod-prober-cadence-go-live.md — the founder-run procedure that registers the PROD-side observer: blast radius before the pre-flight, five blocking pre-flight checks, the exact cron.schedule statement, the manifest re-capture as step 2 of the same session, first-tick expectations, watching it, and a two-part rollback"
  - "TODOS.md[PGCRON-LIVENESS-UNWATCHED-01] — criterion 5's turtle, named, measured, and disposed as an accepted residual with the rejected alternative recorded"
  - "A dated progress note under TODOS.md[PROBER-CADENCE-UNDELIVERED-01] recording what plans 01-05 shipped, with the checkbox still open"
affects: [164.1.1-plan-06]

# Actuals (#2632)
actuals:
  tokens: 5858
  tasks: 3
  commits: 3
  plan_head_before: 96e00e3f7703cffbd4ee87a8b4c6ea81fac59919

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Live PROD cron.schedule registration is always a founder-run runbook, never a migration — the third instance of this repo's settled convention (Phase 161.1 LEDGER-REFRESH, Phase 164.5.1 CRONREPOINT, now this)."
    - "A live cron.job registration's manifest re-capture is documented as step 2 of the SAME session, not a follow-up task, because the committed cron-drift oracle compares by jobname-set equality and reports the new job as drift until re-captured — the same coupling both prior go-live runbooks record."
    - "A criterion-5-style unexamined-turtle disposition is written as: measured evidence, the consequence stated without softening, an ACCEPTED verdict on two named grounds (narrowness + a considered-and-rejected alternative), and the manual path marked manual in the same sentence it is described in — never as coverage."

key-files:
  created:
    - docs/runbooks/prod-prober-cadence-go-live.md
  modified:
    - docs/runbooks/README.md
    - TODOS.md

key-decisions:
  - "The runbook's placeholder for the criterion-5 residual id was filled in by a THIRD, separate task/commit (task 3) rather than folded into task 1 — this repository has a measured incident of a scope amendment touching one file while the refused claim went on standing in the others (T-164.1.1-23), so making the two-surface reachability its own verified, committed act is the mitigation, not a formality."
  - "Minute 20 was verified against the CURRENT committed cron-manifest.json (read during this plan's execution, not assumed): existing hourly jobs sit at :00/:25/:35/:50, plus one job every 15 minutes on the quarter hours. Minute 20 collides with none of them."
  - "The runbook names NO literal 'db push' / 'db reset' / '--project-ref' / '--db-url' substring anywhere, including inside prohibition sentences, so the plan's own automated <verify> grep (which matches those exact substrings) cannot be defeated by a warning that quotes the very command it is warning against."

patterns-established:
  - "A go-live runbook's own 'what this runbook deliberately does not cover' section names its residual by TODOS id and one sentence, not by a forward reference — reachable from the operational surface without a TODOS.md grep, per the T-164.1.1-23 mitigation."

requirements-completed: [PROBER-CADENCE-UNDELIVERED-01]

coverage:
  - id: D1
    description: "docs/runbooks/prod-prober-cadence-go-live.md exists, follows the ledger-refresh-go-live.md section order (blast radius before pre-flight, blocking pre-flight, the live statement, the same-session manifest re-capture, first-tick expectations, watching it, rollback), is indexed in docs/runbooks/README.md, and contains none of the four forbidden CLI verb substrings."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: other
        ref: "test -f docs/runbooks/prod-prober-cadence-go-live.md && grep -q 'prod-prober-cadence-go-live' docs/runbooks/README.md && ! grep -rn \"db push|--project-ref|--db-url|db reset\" docs/runbooks/prod-prober-cadence-go-live.md"
        status: pass
    human_judgment: false
  - id: D2
    description: "Criterion 5's turtle is named, measured, and disposed as an ACCEPTED, NAMED residual with id [PGCRON-LIVENESS-UNWATCHED-01] in TODOS.md, recording the rejected fifth-arm alternative and marking the manual latest_cron_success() reading as manual, never coverage. [PROBER-CADENCE-UNDELIVERED-01] carries a dated progress note and stays an open checkbox."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: other
        ref: "grep -c 'PGCRON-LIVENESS-UNWATCHED-01' TODOS.md && grep -A2 'PROBER-CADENCE-UNDELIVERED-01' TODOS.md | grep -q '\\[ \\]'"
        status: pass
    human_judgment: false
  - id: D3
    description: "The criterion-5 disposition is reachable from BOTH surfaces a reader can land on: TODOS.md (the ledger) and the runbook itself (the operational surface, via its own id + one-sentence summary + the manual-reading caveat)."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: other
        ref: "grep -q 'PGCRON-LIVENESS-UNWATCHED-01' docs/runbooks/prod-prober-cadence-go-live.md && grep -q 'PGCRON-LIVENESS-UNWATCHED-01' TODOS.md"
        status: pass
    human_judgment: false
  - id: D4
    description: "Nothing in this plan executed anything against PROD, touched a migration file, or touched scripts/prod-prober/cron-manifest.json. This plan writes the runbook; plan 06 runs it."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: other
        ref: "git diff --name-only 7ae2fe98~1..33144af6 | grep -E 'supabase/migrations|cron-manifest.json' (expected: no output)"
        status: pass
    human_judgment: false

# Metrics
duration: ~50min
completed: 2026-09-18
status: complete
---

# Phase 164.1.1 Plan 05: The go-live runbook and the disposed residual — Summary

**A founder-run runbook (`docs/runbooks/prod-prober-cadence-go-live.md`) that registers the PROD-side prod-prober-cadence observer — blast radius before the pre-flight, five blocking pre-flight checks, the exact `cron.schedule` statement, the manifest re-capture as step 2 of the same session — plus criterion 5's turtle, named and disposed as an accepted residual (`TODOS.md[PGCRON-LIVENESS-UNWATCHED-01]`), reachable from both the ledger and the runbook itself.**

## Performance

- **Duration:** ~50 min
- **Tasks:** 3/3
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- Wrote `docs/runbooks/prod-prober-cadence-go-live.md`, shaped after `ledger-refresh-go-live.md`'s section order: what this activates, the schedule (minute 20, verified collision-free against the CURRENT committed manifest), blast radius stated before the pre-flight, five ALL-BLOCKING pre-flight checks (database marker, migration correctness, key/destination singularity, at-least-one-contact-row, manifest-agrees-with-PROD), the exact `cron.schedule('prod_prober_cadence_check', '20 * * * *', …)` statement with the reason it belongs in a runbook and never a migration, the manifest re-capture as step 2 of the SAME session, first-tick expectations, three "watching it" readings routed through the plan-03 checkpoint's `a-sentry-already-set` outcome, and a two-part rollback. Indexed in `docs/runbooks/README.md`.
- Named, measured and disposed criterion 5's turtle: `TODOS.md[PGCRON-LIVENESS-UNWATCHED-01]` records that nothing in this repository watches PROD `pg_cron`'s own liveness independently of any specific job's body, states the consequence exactly (this phase's own observer shares the failure mode of the thing it observes, one level down), and ACCEPTS it as a named residual on two grounds — narrowness (a total Postgres outage is a louder, whole-application incident) and a considered-and-rejected alternative (a fifth prober arm would share GitHub Actions' own unreliability, this phase's entire premise). The manual `latest_cron_success()` reading is stated and marked manual, never coverage. Left UNROUTED by explicit founder-facing design: criterion 5 asks the choice be stated, not the gap closed.
- Added a dated progress note under `TODOS.md[PROBER-CADENCE-UNDELIVERED-01]` recording what plans 01-05 shipped and that only the live registration (plan 06) remains; the checkbox stays open.
- Closed the two-surface reachability loop as its own separate, verified commit (task 3): replaced the runbook's placeholder with the real residual id and a one-sentence summary, and added the manual-reading caveat to the runbook itself — so a founder reading the runbook at 3am does not have to grep `TODOS.md` to know whether a quiet observer is a known gap.

## Task Commits

Each task was committed atomically:

1. **Task 1: The go-live runbook — blast radius first, blocking pre-flight, the statement, the manifest in the same act, rollback** - `7ae2fe98` (docs)
2. **Task 2: Criterion 5 — name the turtle, dispose of it, and give it an id** - `988463eb` (docs)
3. **Task 3: Make the criterion-5 disposition readable from the phase itself, not only from TODOS** - `33144af6` (docs)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `docs/runbooks/prod-prober-cadence-go-live.md` - the founder-run go-live procedure: blast radius, five blocking pre-flight checks, the live `cron.schedule` statement, the same-session manifest re-capture, first-tick expectations, watching it, rollback, and the criterion-5 residual with its manual-reading caveat
- `docs/runbooks/README.md` - new row in the Subsystems table indexing the runbook
- `TODOS.md` - new `[PGCRON-LIVENESS-UNWATCHED-01]` entry (criterion 5's disposed residual) and a dated progress note under `[PROBER-CADENCE-UNDELIVERED-01]`

## Decisions Made
See `key-decisions` in frontmatter: the placeholder-then-fill two-task structure for reachability (task 1 → task 3), minute 20 re-verified against the current manifest rather than trusted from the plan's own prose, and the forbidden-substring-avoidance discipline in the runbook's own warnings.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None for this plan. The plan's own text is explicit that nothing here executes against PROD — this plan writes the runbook; plan 06 is the founder-run session that executes it, behind its own decision checkpoint.

## Next Phase Readiness
- Plan 06 can run `docs/runbooks/prod-prober-cadence-go-live.md` directly: the exact statement, the pre-flight queries, and the manifest re-capture command are all present and were re-verified against the current tree during this plan's own execution (the manifest's current 15 jobs, the migration's actual grant/search_path shape, the function's actual Vault/destination reads).
- `[PROBER-CADENCE-UNDELIVERED-01]` closes only when plan 06 lands the live registration and the manifest agrees — not before. Its checkbox is deliberately still open.
- `[PGCRON-LIVENESS-UNWATCHED-01]` is closed for THIS phase's purposes (named, measured, disposed, reachable from two places) and stays UNROUTED until a founder decision routes it — no further action is owed on it by this milestone.

## Self-Check: PASSED

`docs/runbooks/prod-prober-cadence-go-live.md` and the updated `docs/runbooks/README.md`/`TODOS.md` confirmed on disk. All three task commit hashes (`7ae2fe98`, `988463eb`, `33144af6`) confirmed in `git log --oneline`. All four plan-level `<verify>`/`<verification>` assertions re-run clean: (1) runbook exists, is indexed, carries none of the four forbidden CLI substrings; (2) `PGCRON-LIVENESS-UNWATCHED-01` present in `TODOS.md`, `PROBER-CADENCE-UNDELIVERED-01` checkbox still `[ ]`; (3) the residual id resolves in both the runbook and `TODOS.md`; (4) `git diff --name-only` across the three task commits touches only `TODOS.md`, `docs/runbooks/README.md` and `docs/runbooks/prod-prober-cadence-go-live.md` — no migration, no `cron-manifest.json`.

---
*Phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and*
*Completed: 2026-09-18*
