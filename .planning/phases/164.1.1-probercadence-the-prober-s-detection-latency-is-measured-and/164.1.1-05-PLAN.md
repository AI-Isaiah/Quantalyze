---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 05
type: execute
wave: 3
depends_on: ["164.1.1-02", "164.1.1-04"]
files_modified:
  - docs/runbooks/prod-prober-cadence-go-live.md
  - docs/runbooks/README.md
  - TODOS.md
autonomous: true
requirements: [PROBER-CADENCE-UNDELIVERED-01]
tags: [runbook, pg-cron, live-op, residual-risk, todos]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "D-09: the `cron.schedule(...)` statement that registers the PROD job exists in a RUNBOOK and in no migration file. That is this repository's settled convention — a migration creates the function, a founder-gated live op writes the live `cron.job` catalog."
    - "⚠️ The manifest re-capture is a STEP OF THE SAME SESSION as the registration, not a follow-up. `scripts/prod-prober/arms/cron-drift.mjs` diffs live PROD against the COMMITTED `scripts/prod-prober/cron-manifest.json`, so a job registered without the manifest moving in the same act makes the prober's own `cron-drift` arm correctly report a fresh regression caused by this phase."
    - "The runbook states its BLAST RADIUS before its pre-flight, carries blocking pre-flight checks, and carries a rollback — the shape `docs/runbooks/ledger-refresh-go-live.md` established for the only comparable live op in this repository."
    - "The runbook forbids the two shortcuts by name: shortening the prober's own GitHub cron to 'fix' cadence (under the same throttling more attempts is not a bounded gap), and putting the schedule statement into a migration."
    - "Criterion 5's turtle is NAMED and DISPOSED: nothing in this repository watches PROD `pg_cron`'s own liveness, so the observer shares the failure mode it guards one level up. It is recorded as an ACCEPTED, NAMED residual with an id in TODOS.md, not solved and not implied away."
    - "The residual entry states its own narrowness honestly — a total Postgres outage is a whole-application incident with many other symptoms; what is uncovered is the narrow case of pg_cron degrading silently while the rest of Postgres works — and it states the MANUAL path that exists (the observer's own `cron_runs` rows, readable by hand through `latest_cron_success('prod_prober_cadence_check')`) without ever calling that path coverage."
    - "`[PROBER-CADENCE-UNDELIVERED-01]` is NOT closed by this plan. It closes when the job is live and the manifest agrees — plan 06."
  artifacts:
    - "docs/runbooks/prod-prober-cadence-go-live.md — blast radius, blocking pre-flight, the exact `cron.schedule(...)` statement, the manifest re-capture step, first-tick expectations, watching it, and rollback"
    - "docs/runbooks/README.md — the new runbook listed where this directory indexes the others"
    - "TODOS.md — a new named residual entry for the unwatched `pg_cron` liveness, and a progress note under `[PROBER-CADENCE-UNDELIVERED-01]` recording what is shipped and what remains"
  key_links:
    - "runbook step: cron.schedule('prod_prober_cadence_check', …) → live PROD cron.job → `node scripts/prod-prober/run.mjs --capture-manifest --out <path>` → scripts/prod-prober/cron-manifest.json → the cron-drift arm"
    - "criterion 5 residual → TODOS.md entry id → the phase SUMMARY and the ROADMAP criterion-5 line"
  prohibitions:
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT execute anything against PROD in this plan. This plan WRITES the runbook; plan 06 runs it behind a decision checkpoint." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT put `cron.schedule(...)` into a migration file, and MUST NOT instruct a reader to run `supabase db push`, `db reset --linked`, `--project-ref` or `--db-url` from this checkout — this checkout's Supabase CLI is linked to PRODUCTION." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: privacy, status: resolved, verification: judgment, statement: "MUST NOT put a connection string, a project ref, a key, an MT5 account number, a broker server name or an absolute home path into the runbook or into TODOS.md. Full UUIDs are truncated to eight characters. This repository is PUBLIC." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT close `[PROBER-CADENCE-UNDELIVERED-01]` here. Its own text says the observer must live where a scheduler that actually fires does; until the job is registered and the manifest agrees, nothing observes anything." }
---

<objective>
Write down the two things that cannot be automated: the live operation that registers the observer,
and the honest account of what the observer still cannot see.

Purpose: this repository treats a live PROD `cron.job` write as a founder-gated, runbook-driven act,
and it has a measured reason — the same act in Phase 164.5.1 also had to move a committed manifest
in the same session or the prober's own drift arm would report the change as a defect. And criterion
5 forbids an unexamined turtle: nothing here watches `pg_cron` itself, so the observer shares, one
level up, the failure mode it guards against. Naming that is the criterion; pretending otherwise
would be the phase committing its own defect in its closing sentence.

Output: a runbook shaped like the only comparable one this repository has, and a named residual with
an id.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-CONTEXT.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-RESEARCH.md
@docs/runbooks/ledger-refresh-go-live.md
@docs/runbooks/README.md
@TODOS.md
@CLAUDE.md

⭐ Read plans 01, 02 and 04's SUMMARYs (same phase directory) first — the function name, the job
name, the alert route path and the checkpoint outcome are all inputs here. They are deliberately not
`@` context refs: they do not exist until those plans run, and `plan-anchor-verify` fails a plan
whose `@` ref resolves to nothing.

## Decision register — see plan 01's `<context>` for the full mapping

- D-09 (planner) — `cron.schedule(...)` is a live op in a runbook, never a migration; the manifest re-capture is a STEP of that same session.
- D-10 (planner) — criterion 5's turtle is an ACCEPTED, NAMED residual.
- D-06 (planner) — the observer's own `cron_runs` rows are what make its liveness readable at all; they are the manual path the residual entry may describe but must never call coverage.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The go-live runbook — blast radius first, blocking pre-flight, the statement, the manifest in the same act, rollback</name>
  <files>docs/runbooks/prod-prober-cadence-go-live.md, docs/runbooks/README.md</files>
  <read_first>
    - docs/runbooks/ledger-refresh-go-live.md — the template, read in full for SHAPE. Its section order is the one to follow: what this activates, blast radius BEFORE the pre-flight, an all-blocking pre-flight, the live steps numbered and separated, first-tick expectations, watching it, a two-part rollback, and an explicit statement of what it deliberately does not cover.
    - .planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PROD-SESSION.md — the record of the only comparable live op this repository has performed, including the exact `cron.schedule(...)` statement that was run and the note that it UPSERTs on `(jobname, username)` and is therefore safe to re-run.
    - scripts/prod-prober/cron-manifest.json — the committed oracle, its `schema_version`, `normalization`, `captured_at`, `database_marker` and the field set each job row carries.
    - scripts/prod-prober/run.mjs — the `--capture-manifest --out <path>` and `--preflight-repoint [--manifest <path>]` verbs, their refusal conditions and their exit codes. The capture REFUSES a configuration that fails hygiene and writes nothing; the pre-flight ABORTS on a mismatched manifest.
    - scripts/prod-prober/arms/cron-drift.mjs — what the arm compares and why an unlisted live job is drift.
    - CLAUDE.md — the database-marker query that must run before any statement that writes, and the standing warning that this checkout's Supabase CLI is linked to PRODUCTION.
  </read_first>
  <action>
    Create `docs/runbooks/prod-prober-cadence-go-live.md`, following the ledger runbook's section
    order. Write it for a founder executing it once, months from now, with no memory of this phase.

    **What this activates.** One hourly PROD `pg_cron` job named `prod_prober_cadence_check` running
    `SELECT public.prod_prober_cadence_check();`. Say what it does in one paragraph: it reads the most
    recent `public.cron_runs` row under `cron_name = 'prod_prober'`, compares the gap to the ceiling
    the migration declares, writes its own row either way, and — only when the ceiling is crossed —
    posts to the analytics service. Name the migration that must have applied first, and say that if
    it has not, the job errors on every tick rather than doing nothing.

    **Schedule.** Minute 20 of every hour. Justify it from the committed manifest rather than
    asserting it: the existing hourly jobs sit at minutes 0, 25, 35 and 50, and one job runs every
    fifteen minutes on the quarter hours, so minute 20 collides with none of them. ⛔ Instruct the
    reader to RE-DERIVE that from `scripts/prod-prober/cron-manifest.json` at the time they run this,
    not to trust the sentence — the manifest is the oracle and this paragraph is a reading of it.

    **Blast radius, BEFORE the pre-flight.** Four things, each stated plainly. (1) A new `cron.job`
    row makes the prober's `cron-drift` arm report drift until the committed manifest carries it —
    which is why the re-capture is step 2 of this session and not a follow-up task. (2) The job writes
    one `public.cron_runs` row per tick, 24 a day, unpruned; other consumers of that table are all
    narrowed by `cron_name` and are unaffected, but the row count grows. (3) When it alarms it posts
    to the analytics service carrying the Vault-held service key in a header — the same key and the
    same destination an existing job already uses, so no new secret leaves and no new destination is
    reachable. (4) If `net.http_post` is unavailable or the destination is refused, the tick RAISES
    and is red in `cron.job_run_details`; that is deliberate, and the reason is in the migration
    header.

    **Pre-flight — ALL BLOCKING.** Each check states what it proves, the exact command, and what a
    failure means. At minimum: the database marker query from CLAUDE.md, run FIRST, with the
    instruction to stop on a NULL or non-PROD answer rather than proceeding on a guess; that the
    migration has applied to PROD (the function exists, is `SECURITY DEFINER`, and `anon` and
    `authenticated` hold no EXECUTE); that `system_settings` holds an allow-listed
    `analytics_service_url` and the Vault holds exactly one `analytics_service_key`; that at least one
    `public.cron_runs` row exists under `cron_name = 'prod_prober'`, because registering the observer
    before the prober has ever written one means the first tick alarms immediately — which is CORRECT
    behaviour and should not be a surprise, so say which it is rather than leaving the operator to
    guess; that `node scripts/prod-prober/run.mjs --preflight-repoint` passes, so the committed
    manifest agrees with live PROD BEFORE anything changes; and that the working tree is clean, so the
    manifest the capture writes can be reviewed as a diff.

    **Step 1 — the schedule (LIVE op 1 of 2).** Give the exact statement, in the idiom the 164.5.1
    session used: `SELECT cron.schedule('prod_prober_cadence_check', '20 * * * *', $$SELECT
    public.prod_prober_cadence_check();$$);`. State that `cron.schedule` UPSERTs on
    `(jobname, username)` so it is safe to re-run, and that a return value is the jobid. ⛔ State, in
    bold, that this statement belongs here and in no migration file, and why — a migration that wrote
    the live job catalog would be the first in this repository to do so and would make every later
    apply an unreviewed catalog write.

    **Step 2 — the manifest, in the SAME session (LIVE op 2 of 2).** Run
    `node scripts/prod-prober/run.mjs --capture-manifest --out <a scratch path>`, review the diff
    against the committed manifest, confirm the ONLY change is the new job row, move it into
    `scripts/prod-prober/cron-manifest.json`, and commit. State that the capture REFUSES and writes
    nothing if any row fails a hygiene rule or if the database carries no comment marker, so a refusal
    is information and not an obstacle. ⛔ State that stopping after step 1 leaves the repository in a
    state where the prober's own drift arm reports a regression this phase caused — which is the arm
    working, and the remedy is to finish step 2, never to silence the arm.

    **First tick — what to expect.** At minute 20: one new `public.cron_runs` row under `cron_name =
    'prod_prober_cadence_check'`. `status = 'ok'` if the prober has contacted within the ceiling,
    `status = 'error'` and one post otherwise. Give the two narrow, `cron_name`-scoped queries that
    read exactly that, and say that a tick which RAISED leaves no row at all — so an absent row after
    the first scheduled minute means the tick failed, and `cron.job_run_details` is where to look for
    the SQLSTATE. ⛔ Add the standing caution: `cron.job_run_details.status` proves a job was invoked
    and never that an async post landed; the delivery question is answered on the `net._http_response`
    side, which is the distinction the `cron-obs` prober arm exists to make.

    **Watching it.** Name the three readings and what each does and does not prove: the observer's own
    rows (its liveness), the prober's contact rows (what it measures), and the analytics-service side
    (whether the alert was received at all). Point at `docs/runbooks/sentry-triage.md` for the
    escalation, if and only if plan 03's checkpoint recorded outcome (a) or (b); if it recorded (c),
    say instead, plainly, that the terminal hop is a service log line nobody is paged on and name the
    TODOS entry that owns it.

    **Rollback.** `SELECT cron.unschedule('prod_prober_cadence_check');`, then re-capture the manifest
    in the SAME session for the same reason as step 2, then commit. Say what unscheduling costs — the
    cadence is unobserved again, which is the state `[PROBER-CADENCE-UNDELIVERED-01]` describes — and
    that the rows already written are left in place because nothing reads them destructively.

    **What this runbook deliberately does not cover.** Two entries. It does not cover PROD `pg_cron`
    itself stopping — name the TODOS residual id task 2 creates. And it does not cover shortening the
    prober's own GitHub Actions schedule: under the same throttling more attempts buys a louder claim,
    not a bounded gap, which is the measurement this whole phase rests on.

    Finally, add the runbook to `docs/runbooks/README.md` wherever that file indexes the others,
    matching the existing entries' shape.
  </action>
  <verify>
    <automated>test -f docs/runbooks/prod-prober-cadence-go-live.md &amp;&amp; grep -q 'prod-prober-cadence-go-live' docs/runbooks/README.md &amp;&amp; ! grep -rn "db push\|--project-ref\|--db-url\|db reset" docs/runbooks/prod-prober-cadence-go-live.md</automated>
  </verify>
  <done>The runbook exists with the ledger runbook's section order, carries the exact schedule statement and the same-session manifest re-capture, states its blast radius before its pre-flight, carries a rollback, names both forbidden shortcuts, contains no forbidden CLI verb and no secret, project ref, absolute home path or untruncated UUID, and is indexed in the runbooks README.</done>
</task>

<task type="auto">
  <name>Task 2: Criterion 5 — name the turtle, dispose of it, and give it an id</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md — the `[PROBER-CADENCE-UNDELIVERED-01]` entry, for the house style of a booked entry: the dated measurement, the scope statement so nobody over-reads it, the ⛔/⚠️/⭐ register, and the owner/destination line. Also read a nearby ACCEPTED-RESIDUAL entry to copy how this file records something recorded rather than fixed.
    - .planning/ROADMAP.md, the Phase 164.1.1 section — criterion 5's exact wording, which requires the residual be NAMED, not solved.
    - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-RESEARCH.md — the evidence: no mechanism in this repository periodically checks whether `pg_cron` is still executing jobs, whether the extension is enabled, or whether PROD Postgres is reachable independently of any specific job's body.
  </read_first>
  <action>
    Add ONE new entry to `TODOS.md` recording criterion 5's residual, with an id. Use
    `[PGCRON-LIVENESS-UNWATCHED-01]` unless a grep shows that id already taken.

    Write it as a MEASUREMENT, dated today, in this file's voice. Say what was searched and what was
    found: `cron.job_run_details` is referenced in several migrations as operator-visible and one
    comment states plainly that it is rarely scraped; no SQL, Python or TypeScript in this repository
    periodically checks pg_cron's own liveness independently of a specific job's body.

    State the consequence exactly, without softening and without inflating. If PROD Postgres or the
    `pg_cron` extension stops, `prod_prober_cadence_check()` stops with it, and the observer this
    phase ships is silent for the same reason the thing it observes would be. That is the turtle one
    level down, and it is not closed.

    State the DISPOSITION, and state it as a decision: ACCEPTED as a named residual, on two grounds.
    First, narrowness — a total Postgres outage is a whole-application incident with many louder
    symptoms than a missing alert, so what is genuinely uncovered is the narrow case of pg_cron
    degrading silently while the rest of Postgres works. Second, the alternative considered and
    rejected: adding a fifth arm to the GitHub-hosted prober would let it read the observer's own
    recency, but the prober shares GitHub's unreliability, which is this phase's entire premise, and
    Phase 164.1.1's own CONTEXT puts the prober's arms out of scope. Record that it was considered and
    why it was not taken — an unexamined option and a rejected one are different things.

    State the MANUAL path that does exist, and mark it as manual in the same sentence rather than in a
    later caveat: the observer writes one `public.cron_runs` row per tick under its own `cron_name`,
    so a human running `latest_cron_success('prod_prober_cadence_check')` — the admin point query the
    pre-flight checklist already uses — sees its silence. ⛔ Do not call that coverage anywhere. It is
    a query somebody has to decide to run, which is precisely the property this phase's subject
    lacked.

    Leave the entry UNROUTED unless the founder routes it, and say so explicitly with the reason, in
    the same way this file records its other deliberately unrouted entries. Criterion 5 asks for the
    choice to be STATED, not for the gap to be closed; inventing a destination phase would record a
    commitment nobody made.

    Then, under `[PROBER-CADENCE-UNDELIVERED-01]`, add a dated progress note — NOT a closure. Say what
    is shipped (the contact write, the observer, the gate, the route, the runbook) and what remains
    (the live registration and the manifest re-capture, plan 06). ⛔ Do not tick the entry: its own
    text says the observer must live where a scheduler that actually fires does, and until the job is
    registered nothing observes anything.
  </action>
  <verify>
    <automated>grep -c 'PGCRON-LIVENESS-UNWATCHED-01' TODOS.md &amp;&amp; grep -A2 'PROBER-CADENCE-UNDELIVERED-01' TODOS.md | grep -q '\[ \]'</automated>
  </verify>
  <done>TODOS.md carries a dated, named, disposed residual for the unwatched `pg_cron` liveness, recording the rejected alternative and marking the manual path as manual; `[PROBER-CADENCE-UNDELIVERED-01]` carries a dated progress note and is still open.</done>
</task>

<task type="auto">
  <name>Task 3: Make the criterion-5 disposition readable from the phase itself, not only from TODOS</name>
  <files>docs/runbooks/prod-prober-cadence-go-live.md</files>
  <read_first>
    - docs/runbooks/prod-prober-cadence-go-live.md — the "what this runbook deliberately does not cover" section task 1 wrote.
    - TODOS.md — the residual entry task 2 created, for its exact id and wording.
  </read_first>
  <action>
    Close the loop so the residual is reachable from every place a reader lands. Three small edits,
    and the reason for each is that this repository has a measured incident in which a scope amendment
    touched one file and the refused claim went on standing in the others.

    In the runbook's "deliberately does not cover" section, replace the placeholder reference with the
    residual's real id and one sentence of what it covers, so a founder reading the runbook at 3am
    does not have to grep `TODOS.md` to find out whether the thing that just went quiet is a known
    gap.

    In the same section, add the one-line statement of the observer's own liveness reading — the
    query name and what its silence means — marked explicitly as a MANUAL check a human must decide to
    run, never as monitoring.

    Then confirm, by reading rather than by assuming, that the phase's criterion-5 obligation is now
    satisfied in BOTH places it can be read: `TODOS.md` (the ledger) and the runbook (the operational
    surface). Record in the plan SUMMARY which two places you verified and what each says, so the next
    reader can check the pair rather than re-deriving it.
  </action>
  <verify>
    <automated>grep -q 'PGCRON-LIVENESS-UNWATCHED-01' docs/runbooks/prod-prober-cadence-go-live.md &amp;&amp; grep -q 'PGCRON-LIVENESS-UNWATCHED-01' TODOS.md</automated>
  </verify>
  <done>The residual id appears in both the runbook and TODOS.md; the runbook states the manual liveness reading and marks it manual; the SUMMARY records the two places verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runbook text → a PROD session | every statement in it will be pasted into a session against the production database |
| `docs/` and `TODOS.md` → the world | this repository is PUBLIC |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1.1-20 | Tampering | a runbook statement run against the wrong database | critical | mitigate | The database marker query from CLAUDE.md is the FIRST blocking pre-flight check, with an explicit instruction to stop on a NULL or non-PROD answer; the runbook contains no `db push`, `db reset`, `--project-ref` or `--db-url` |
| T-164.1.1-21 | Information Disclosure | secrets or identifiers in a public runbook | high | mitigate | No connection string, project ref, key, account number, broker server name or absolute home path; UUIDs truncated to eight characters; the verify command greps for the forbidden CLI verbs |
| T-164.1.1-22 | Repudiation | a live registration without the manifest moving | high | mitigate | The re-capture is step 2 of the SAME session, its omission is named as a regression this phase would have caused, and the runbook states that the remedy is to finish step 2 rather than silence the arm |
| T-164.1.1-23 | Repudiation | a residual recorded in one place and contradicted in another | medium | mitigate | Task 3 closes the loop deliberately, and the SUMMARY records which two surfaces were verified — this repository has a measured incident of a one-file amendment leaving the refused claim standing elsewhere |
| T-164.1.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package |
</threat_model>

<verification>
- `docs/runbooks/prod-prober-cadence-go-live.md` exists, is indexed in `docs/runbooks/README.md`, and contains none of `db push`, `db reset`, `--project-ref`, `--db-url`.
- The residual id resolves in BOTH `TODOS.md` and the runbook.
- `[PROBER-CADENCE-UNDELIVERED-01]` is still an open checkbox and carries a dated progress note.
- Nothing in this plan touched PROD, a migration, or `scripts/prod-prober/cron-manifest.json`.
</verification>

<success_criteria>
- The live registration is written down as a runbook in this repository's established shape, with the manifest re-capture as a step of the same session.
- Criterion 5's turtle is named, measured, disposed as an accepted residual with an id, and reachable from both the ledger and the operational surface.
- Nothing claims coverage the phase does not have.
</success_criteria>

<output>
Create `.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-05-SUMMARY.md` when done.
</output>
