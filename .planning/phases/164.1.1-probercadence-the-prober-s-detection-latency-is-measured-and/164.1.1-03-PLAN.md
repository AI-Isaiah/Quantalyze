---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/prod-prober-wiring.test.ts
autonomous: false
requirements: [PROBER-CADENCE-UNDELIVERED-01]
tags: [prod-prober, checkpoint, ci-gate, anti-vacuity, alerting]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

user_setup:
  - service: railway-analytics-service
    why: "The alarm's LAST hop. Nothing in the repository can establish whether an alert reaching analytics-service reaches a human, because that depends on a Railway environment variable no committed file records. Shipping the alarm without answering it would recreate this phase's own defect one layer down."
    env_vars:
      - name: SENTRY_DSN
        source: "Railway → the analytics-service project → the production environment → Variables. Only its PRESENCE is needed, never its value — `analytics-service/sentry_init.py` makes every capture a silent no-op when it is unset."

must_haves:
  truths:
    - "D-11: criterion 4 is VERIFY-ONLY and stays that way. `.github/workflows/prod-prober.yml`'s header was already corrected in commit `126517a8`; this plan pins it with a test and does NOT re-derive or re-edit the language."
    - "The pin is not vacuous: a calibration twin mutates a scratch copy of the workflow header and observes the predicate FLIP, so the assertion is measured rather than asserted."
    - "D-03 (CTX-03) is discharged on the DELIVERY side, not only the write side: before any task commits to a terminal escalation, a human states whether `SENTRY_DSN` is actually set on the Railway analytics-service. An alarm on a channel nobody reads is exactly the vacuity class this repo ranks above ordinary correctness."
    - "The checkpoint's outcome is written down where plan 04 reads it, with the date and who measured it — not carried in a conversation."
  artifacts:
    - "src/__tests__/prod-prober-wiring.test.ts — a new describe pinning the measured-delivery figures in the workflow header, with its calibration twin"
    - "a recorded checkpoint answer naming the terminal channel plan 04 must implement, dated"
  key_links:
    - "checkpoint answer → plan 04's escalation implementation in the new analytics-service route"
    - "the workflow header at .github/workflows/prod-prober.yml → the new pin in src/__tests__/prod-prober-wiring.test.ts → the `frontend` aggregator"
  prohibitions:
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT edit `.github/workflows/prod-prober.yml`. Criterion 4 is already met; re-deriving the figures risks stomping correct language with a restatement that drifts, which is the named citation-drift class this repo already carries." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT guess the answer to the checkpoint, infer it from the existence of a Sentry triage runbook, or proceed to plan 04 on an assumption. The runbook proves the ORG uses Sentry; it proves nothing about this service's variable." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: privacy, status: resolved, verification: judgment, statement: "MUST NOT record the VALUE of `SENTRY_DSN`, `RESEND_API_KEY` or any other credential in the checkpoint answer, in a SUMMARY, or anywhere under `.planning/` — this repository is PUBLIC and `.planning/` is tracked. Presence or absence only." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: test, statement: "MUST NOT add a negative grep over the superseded header wording. The pin asserts what the header DOES say; a negative grep for a phrase would put that phrase into this plan and into the test, where the next reader cannot tell a gate from an example." }
---

<objective>
Answer the one question the repository cannot answer about itself, and pin the one criterion that
is already met.

Purpose: this phase exists because a control claimed a guarantee it did not deliver. Its own alarm
must not repeat that shape one layer down. Research established the HTTP hop from PROD to Railway
is live and reviewed; what it could NOT establish is whether anything at the far end reaches a
human. That is a measurement a person takes in a dashboard, so it is a checkpoint, and it blocks
plan 04's escalation rather than being decided by it.

Output: a dated, recorded answer naming the terminal channel plan 04 implements, and a calibrated
test pinning the workflow header's measured-delivery figures.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-CONTEXT.md
@.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-RESEARCH.md
@docs/runbooks/sentry-triage.md
@.github/workflows/prod-prober.yml

## Decision register — see plan 01's `<context>` for the full mapping

- D-03 = CTX-03 — measure the channel before choosing it. Its write-side half is discharged by RESEARCH.md; its DELIVERY-side half is this plan's checkpoint.
- D-08 (planner) — the destination is the existing allow-listed `analytics_service_url` and a new `/api` route in analytics-service. The checkpoint does NOT reopen the destination; it decides only what that route does at its far end.
- D-11 (planner) — criterion 4 is verify-only.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: CHECKPOINT — does the alarm's last hop reach a human? Decide the terminal channel before anything commits to one</name>
  <precondition>The founder can open the Railway dashboard for the analytics-service project's production environment.</precondition>
  <reversibility rating="reversible">Changing the escalation later is a small edit in one route file; what is NOT reversible is shipping a phase that CLAIMS an alarm reaches a human when it does not.</reversibility>
  <decision>Which terminal channel does the prober-cadence alarm escalate through, given whether `SENTRY_DSN` is actually set on the Railway analytics-service?</decision>
  <context>
    The PROD-side observer (plan 01) fires `net.http_post` at the already-allow-listed Railway
    analytics-service host when the prober's contact gap crosses the ceiling. That hop is live,
    reviewed and currently used by another scheduled job. What the repository cannot say is what
    happens at the far end. Analytics-service initialises Sentry only when `SENTRY_DSN` is set and
    makes every capture a silent no-op otherwise, and whether that variable is set in the Railway
    production environment is recorded in no committed file. Project memory separately records
    `RESEND_API_KEY` as an unset gap in Vercel production with no recorded resolution, and research
    found no processor that drains `notification_dispatches` rows — so neither is a viable fallback
    without work of its own.

    ⛔ Do not guess, and do not infer this from the existence of `docs/runbooks/sentry-triage.md`:
    that runbook proves the ORG uses Sentry, not that this service's variable is set. Shipping an
    alarm whose last hop is unverified would recreate this phase's own defect one layer down.

    ONE fact settles it, and only its PRESENCE is needed, never its value: in Railway → the
    analytics-service project → the production environment → Variables, is `SENTRY_DSN` set?

    ⭐ Recommendation: take (a) or (b). The Sentry path costs a few lines against a pattern that
    already exists in the target file, needs no new credential, and lands in a triage practice that
    already exists. (c) is the honest fallback, not the goal.
  </context>
  <options>
    <option id="a-sentry-already-set">
      <name>(a) SENTRY_DSN IS set — escalate through Sentry</name>
      <pros>Plan 04 implements a rate-limited `sentry_sdk` capture plus a structured log on every occurrence, modelled on `_alert_kek_unavailable` in `analytics-service/routers/internal.py` — this service's existing, reviewed shape for exactly this. No new credential, no new dependency, and `docs/runbooks/sentry-triage.md` already documents the human triage practice that receives it.</pros>
      <cons>Delivery still depends on a variable outside this repository, so a later unset would silently re-open the gap; the phase should say so rather than imply permanence.</cons>
    </option>
    <option id="b-sentry-set-now">
      <name>(b) It is NOT set, and the founder sets it now</name>
      <pros>Same as (a) once set. It also surfaces a finding worth keeping on its own: if it was absent until today, every existing Sentry capture in that service has been a no-op.</pros>
      <cons>Requires a founder action in the Railway dashboard before plan 04 can be verified end to end; record the date it was set.</cons>
    </option>
    <option id="c-log-only">
      <name>(c) It is NOT set and will not be set in this phase — log-only, recorded as a gap</name>
      <pros>Honest and shippable: plan 04 ships the route with a structured-log escalation only, and the gap is booked in TODOS.md with an id rather than papered over.</pros>
      <cons>⛔ Under (c) the phase must NOT claim the alarm reaches a human — not in a SUMMARY, not in the ROADMAP, not in the CHANGELOG, not in a code comment. The terminal hop is a Railway log line nobody is paged on, and that sentence is the one that must appear.</cons>
    </option>
  </options>
  <resume-signal>Reply with `a-sentry-already-set`, `b-sentry-set-now` or `c-log-only`. The chosen id, the date and who measured it are written into this plan's SUMMARY — ⛔ never the VALUE of any variable, because `.planning/` is tracked and this repository is public.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin criterion 4 where it already holds — the workflow header states the measured delivery, and the pin is calibrated</name>
  <files>src/__tests__/prod-prober-wiring.test.ts</files>
  <behavior>
    - The test reads `.github/workflows/prod-prober.yml` from disk and asserts its schedule-region header states the measured delivery rate, the measured median gap, the measured maximum gap, a date, and a pointer to this phase.
    - CALIBRATION: the same predicate, applied to a scratch copy of the header with the measured figures stripped out, REPORTS THE ABSENCE BY NAME. Without this leg the assertion is a claim about a file nobody mutated.
    - The predicate is anchored to the schedule region by a named slice anchor in the same style the file's existing workflow-text pins use, so a renamed anchor fails BY NAME instead of degenerating into a whole-file search that passes on a coincidence elsewhere in the workflow.
  </behavior>
  <read_first>
    - .github/workflows/prod-prober.yml — the `on.schedule` block and the header comment above it. Read it FIRST and confirm the figures are still there; if they are not, STOP and report it, because criterion 4 would then be a regression and not a verification.
    - src/__tests__/prod-prober-wiring.test.ts — the existing workflow-text pins, how they slice the file by named anchors, and the calibration twins beside them (`CALIBRATION: …` cases that mutate a scratch copy and assert the predicate flips). Follow that convention exactly rather than inventing one.
    - git show 126517a8 --stat — the commit that corrected the header, so the pin is written against what actually landed rather than against a paraphrase.
  </read_first>
  <action>
    Add ONE describe block to `src/__tests__/prod-prober-wiring.test.ts` pinning criterion 4.

    Slice the workflow by a named anchor around the schedule region, using the same helper and the
    same failure wording the file's existing slice-based pins use — the file already carries a
    calibration proving that a RENAMED anchor fails by name rather than degenerating, and the new pin
    must inherit that property rather than reimplement it.

    Assert, positively, that the sliced region states: the measured delivery rate, the measured median
    gap, the measured maximum gap, a date, and a reference to Phase 164.1.1. ⛔ Take the three figures
    from the file itself when you write the test, not from any planning prose — this repository
    carries a named recurring citation-drift defect class and a test that restates a number from a
    plan is the next instance of it. Write the assertion so its failure message prints what the region
    DOES say before its verdict, per this repo's diagnostic-first rule.

    ⛔ Assert only what the header DOES say. Do not add a negative grep for the superseded wording: a
    negative grep requires the superseded phrase to be written into the test and into this plan, where
    the next reader cannot tell a gate from an example, and the positive assertions plus the
    calibration twin already bound the property.

    Add the calibration twin immediately below, in the file's own `CALIBRATION:` style: build a
    scratch copy of the sliced region with the measured figures removed, run the SAME predicate, and
    assert it reports the absence naming each missing figure. A pin without this leg is an assertion
    about a file nobody mutated.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/prod-prober-wiring.test.ts</automated>
  </verify>
  <done>The new describe passes against the workflow as it stands, its calibration twin observes the predicate flip on a mutated scratch copy, and `.github/workflows/prod-prober.yml` is byte-unchanged in this plan's diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Railway environment → this repository | the alarm's terminal reachability is a fact held outside every committed file |
| `.planning/` → the world | the repo is PUBLIC and `.planning/` is tracked, so a recorded checkpoint answer is published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1.1-12 | Repudiation | an alarm whose terminal hop is a silent no-op | critical | mitigate | The checkpoint establishes presence before plan 04 commits to a channel, and outcome (c) forbids the phase from claiming human reach anywhere |
| T-164.1.1-13 | Information Disclosure | the checkpoint answer recorded under `.planning/` | high | mitigate | Presence or absence only; the task states explicitly that no credential value is written into a tracked file |
| T-164.1.1-14 | Tampering | a criterion-4 pin that cannot fail | medium | mitigate | A calibration twin mutates a scratch copy and observes the predicate flip; a pin over an unmutated file is an assertion, not a measurement |
| T-164.1.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package |
</threat_model>

<verification>
- `npx vitest run src/__tests__/prod-prober-wiring.test.ts` is green, including the new calibration twin.
- `git diff -- .github/workflows/prod-prober.yml` is EMPTY.
- The SUMMARY records the checkpoint outcome letter, its date and its measurer, and contains no credential value.
</verification>

<success_criteria>
- Criterion 4 is verified, pinned and calibrated — not re-done.
- The alarm's terminal channel is a recorded, dated fact before any code commits to it.
- If the answer is (c), the phase's obligation not to claim human reach is written down beside it.
</success_criteria>

<output>
Create `.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-03-SUMMARY.md` when done.
</output>
