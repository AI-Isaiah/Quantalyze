---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 04
subsystem: alerting
tags: [analytics-service, alerting, fastapi, anti-vacuity, sentry, rate-limiting]

# Dependency graph
requires:
  - phase: 164.1.1-plan-01
    provides: "public.prod_prober_cadence_check() — the PROD-side SECURITY DEFINER function that posts to /api/prober-cadence-alert with a Vault SERVICE_KEY, proved on a disposable pg-lane"
  - phase: 164.1.1-plan-03
    provides: "The recorded checkpoint outcome (a-sentry-already-set — SENTRY_DSN confirmed set on Railway analytics-service production, founder-measured 2026-09-18) this plan implements against"
provides:
  - "POST /api/prober-cadence-alert — the far end of the alarm, guarded by the existing SERVICE_KEY middleware, no new credential"
  - "A rate-limited sentry_sdk escalation (one per hour) plus an unconditional structured log on every occurrence, cloned from _alert_kek_unavailable"
  - "A calibrated test suite proving each of the four load-bearing behaviours by neutering it, observing RED by name, and restoring"
affects: [164.1.1-plan-05, 164.1.1-plan-06]

# Actuals (#2632)
actuals:
  tokens: 4996
  tasks: 2
  commits: 2
  plan_head_before: a3026e74

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Rate-limited operator-escalation shape (window constant + module-level last-fired timestamp + test-only reset helper + unconditional log before the window check + try/except-wrapped capture), now used twice in this service: _alert_kek_unavailable (routers/internal.py) and _escalate_prober_cadence_gap (routers/cron.py)."
    - "Required-but-nullable pydantic field (gap_minutes: int | None, no default) for a producer contract where NULL is a meaningful value (never-contacted) but ABSENCE is a contract violation — distinct from the common Optional-with-default-None shape."

key-files:
  created: []
  modified:
    - analytics-service/routers/cron.py
    - analytics-service/tests/test_prober_cadence_alert.py
    - analytics-service/tests/test_limiter_route_coverage.py

key-decisions:
  - "Route placement: /api/prober-cadence-alert lives under the existing /api prefix (SERVICE_KEY middleware), not /internal (INTERNAL_API_TOKEN) — the PROD function holds no credential for the latter and minting one would need a new Vault secret and a founder act. Zero new credentials, no carve-out."
  - "Escalation window set to 3600s (1 hour) — Claude's discretion (CONTEXT.md leaves this unspecified). Bounds Sentry noise to at most one capture per hour of sustained staleness regardless of how frequently a future cron.schedule() re-checks (owned by plans 05/06, not yet wired); the structured log fires on every occurrence regardless, so no signal is lost between escalations."
  - "gap_minutes is a REQUIRED (no default) but NULLABLE pydantic field — accepts null (the never-contacted case, the loudest possible stale signal) while refusing an omitted key with the same 4xx as any other missing field. Confirmed by calibration (NEUTER-4 below)."
  - "Response body is always 200 with {\"acknowledged\": true} — net.http_post on the database side is fire-and-forget, so a non-2xx here would add a second silent failure on top of whatever the escalation could not deliver, per the plan's own reasoning and the migration's T-164.1.1-19 accepted-residual disposition."

patterns-established:
  - "A cron-surface, SERVICE_KEY-gated route called only by trusted infrastructure (never a user session) is quarantined from the repo's slowapi rate-limit-coverage gate with a named-reason roster entry (test_limiter_route_coverage.py), not decorated with @limiter.limit — same posture as the pre-existing routers.cron.cron_sync and routers.match.cron_recompute entries."

requirements-completed: [PROBER-CADENCE-UNDELIVERED-01]

coverage:
  - id: D1
    description: "POST /api/prober-cadence-alert exists on the guarded /api prefix, accepts the body prod_prober_cadence_check() sends, and is refused for an unkeyed caller with no carve-out."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_prober_cadence_alert.py#test_unauthenticated_request_is_refused_by_service_key_middleware"
        status: pass
      - kind: integration
        ref: "analytics-service/tests/test_limiter_route_coverage.py::TestRouteLimitCoverage (partition/quarantine gate, green with the new routers.cron.prober_cadence_alert entry)"
        status: pass
    human_judgment: false
  - id: D2
    description: "Every accepted alert emits one structured log line carrying the cron name, the gap in minutes (or 'never'), and the ceiling as sent; a malformed body (missing/non-integer gap_minutes, missing cron_name) is refused 4xx with zero log and zero escalation."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_prober_cadence_alert.py#test_accepted_body_returns_200_and_logs_the_measurement, #test_null_gap_minutes_is_accepted_and_logged_as_never, #test_missing_gap_minutes_is_4xx_and_no_escalation, #test_non_integer_gap_minutes_is_4xx, #test_missing_cron_name_is_4xx"
        status: pass
    human_judgment: false
  - id: D3
    description: "Escalation fires at most once per window (two alerts inside the window produce one Sentry capture, two alerts either side of an expired window produce two), and an escalation-transport failure never changes the 200 response or propagates."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_prober_cadence_alert.py#test_two_alerts_inside_window_produce_two_logs_one_escalation, #test_two_alerts_either_side_of_window_produce_two_escalations, #test_escalation_that_raises_does_not_change_response_or_propagate"
        status: pass
    human_judgment: false
  - id: D4
    description: "No credential, header value, or the request's own X-Service-Key ever appears in a log line or an escalation argument."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification:
      - kind: unit
        ref: "analytics-service/tests/test_prober_cadence_alert.py#test_no_log_or_escalation_carries_the_request_service_key"
        status: pass
    human_judgment: false
  - id: D5
    description: "Each of the four load-bearing behaviours (unconditional log, escalation window, escalation-failure containment, body validation) was neutered in routers/cron.py, run, and the NAMED test went RED for the expected reason before being restored byte-identical."
    requirement: "PROBER-CADENCE-UNDELIVERED-01"
    verification: []
    human_judgment: true
    rationale: "The calibration RED observations are a manual, one-time act performed during this execution and recorded below in prose (exact test name + failure mode per neuter) — there is no automated re-run of a deliberately-broken production file to point a machine verification at, by the nature of the calibration itself."

# Metrics
duration: ~55min
completed: 2026-09-18
status: complete
---

# Phase 164.1.1 Plan 04: The guarded route, calibrated — Summary

**POST /api/prober-cadence-alert now exists, guarded by the existing SERVICE_KEY middleware, logging every occurrence and escalating to Sentry (rate-limited, per the 164.1.1-03 checkpoint's a-sentry-already-set outcome) — with all four load-bearing behaviours proven by neutering each one and observing the named test go RED before restoring.**

## Performance

- **Duration:** ~55 min
- **Completed:** 2026-09-18
- **Tasks:** 2/2
- **Files modified:** 3 (2 plan-listed, 1 deviation)

## Accomplishments
- Added `POST /prober-cadence-alert` to the existing `/api` router in `analytics-service/routers/cron.py`: a pydantic-validated request model (`cron_name`, `ceiling`, `detected_at` required; `gap_minutes` required-but-nullable for the never-contacted case), an unconditional structured log on every accepted alert, and a Sentry escalation rate-limited to once per hour — cloned from `_alert_kek_unavailable` in `routers/internal.py`, including the `set_tag`-before-`capture_message` idiom and the swallowing `try/except`.
- Implements the outcome the 164.1.1-03 checkpoint recorded: `SENTRY_DSN` is set on Railway analytics-service production (presence confirmed by the founder 2026-09-18, never the value) — the escalation is a real `sentry_sdk.capture_message`, not a log-only stub. The outside-this-repo delivery caveat is carried forward verbatim in the route's own comment block and in this SUMMARY's `key-decisions`.
- Wrote `analytics-service/tests/test_prober_cadence_alert.py` (10 tests): accepted body + log content, null `gap_minutes` logged as "never", four malformed-body 4xx cases with zero log/escalation, the window (one-per-window and two-either-side), escalation-failure containment, secret-leak absence, and — driven through the real `main.app` via `httpx.ASGITransport` rather than a bare-router harness — the SERVICE_KEY-middleware refusal of an unkeyed caller.
- **Calibration (Task 2's explicit requirement): each of the four load-bearing behaviours was manually neutered in `routers/cron.py`, run, confirmed RED by the exact expected test name, then restored** — see the per-behaviour log below. Restoration was verified byte-identical via `cmp` after every neuter, never `git checkout --`.

## Task Commits

Each task was committed atomically:

1. **Task 1: The guarded route — accept the alert, log the measurement, escalate once per window** - `524b300f` (feat)
2. **Task 2: Calibrate the tests — each one observed RED with the behaviour neutered, then restored** - `06ccd703` (test)

**Plan metadata:** pending (this commit)

## Calibration Log (Task 2 requirement)

| # | Behaviour neutered | Change applied | Test that went RED | Observed failure |
|---|---|---|---|---|
| 1 | Unconditional log | Moved `logger.error(...)` from before the window check to after it (log only fires when escalating) | `test_two_alerts_inside_window_produce_two_logs_one_escalation` | `AssertionError: every accepted alert must log, window or not` — `assert 1 == 2` (only 1 log line for 2 accepted alerts) |
| 2 | Escalation window | Replaced the window guard's condition with `if False: return` (always escalates) | `test_two_alerts_inside_window_produce_two_logs_one_escalation` | `capture_message.call_count == 2` where 1 was expected — a second alert inside the window re-escalated |
| 3 | Escalation-failure containment | Removed the `try/except Exception: pass` wrapping the `sentry_sdk` calls | `test_escalation_that_raises_does_not_change_response_or_propagate` | Raw `RuntimeError: sentry transport down` propagated out of the route (no longer swallowed) |
| 4 | Body validation | Added `= None` default to `gap_minutes: int \| None` (making the field optional instead of required-but-nullable) | `test_missing_gap_minutes_is_4xx_and_no_escalation` | A body omitting `gap_minutes` entirely was accepted (200) instead of refused (4xx) |

Every neuter's restoration was confirmed via `cmp routers/cron.py <pre-neuter backup>` reporting no difference before proceeding to the next neuter or to the commit.

## Files Created/Modified
- `analytics-service/routers/cron.py` - `POST /prober-cadence-alert`, `ProberCadenceAlert` request model, `_escalate_prober_cadence_gap`, `_reset_prober_cadence_alert` test hook
- `analytics-service/tests/test_prober_cadence_alert.py` - the calibrated test suite (10 tests)
- `analytics-service/tests/test_limiter_route_coverage.py` - quarantine-roster entry for the new route (deviation, see below)

## Decisions Made
See `key-decisions` in frontmatter: route placement under `/api` not `/internal`; the 3600s escalation window (Claude's discretion, CONTEXT.md left this unspecified); `gap_minutes` required-but-nullable; always-200 response.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical] Quarantine-roster entry for the new route in `test_limiter_route_coverage.py`**
- **Found during:** Task 1 (full-suite verification run)
- **Issue:** A pre-existing repo-wide gate (`test_the_unlimited_partition_equals_the_quarantine`, `test_the_partition_is_total`) requires every registered FastAPI route to be either rate-limited or explicitly quarantined with a named reason. The new `routers.cron.prober_cadence_alert` route was neither, and both tests failed by name: `unexpected=['routers.cron.prober_cadence_alert']`.
- **Fix:** Added a roster entry in `NO_LIMITER_QUARANTINE`, matching the existing `routers.cron.cron_sync` entry's posture (cron surface, SERVICE_KEY-gated, called by trusted infrastructure) — and narrower: this route is invoked only by `public.prod_prober_cadence_check()`'s own `net.http_post`, never a browser or user session.
- **Files modified:** analytics-service/tests/test_limiter_route_coverage.py
- **Verification:** `pytest tests/test_limiter_route_coverage.py tests/test_limiter_identity.py -q` — 80 passed
- **Committed in:** 06ccd703 (Task 2 commit — bundled with the test suite since it was discovered while verifying the full suite for Task 2's own `<verify>`)

---

**Total deviations:** 1 auto-fixed (1 missing critical — repo-wide invariant compliance)
**Impact on plan:** Necessary for the plan's own verification requirement ("the whole service suite is green") to hold. No scope creep — no new rate-limit behavior was added, only an explicit, reasoned exemption matching an established repo pattern.

## Issues Encountered
None beyond the deviation above.

## User Setup Required
None - no external service configuration required. `SENTRY_DSN` presence was already confirmed by the founder in plan 03; this plan reads no environment variable directly (the existing `sentry_init.py` no-op-when-unset contract governs whether `sentry_sdk.capture_message` actually transmits, and this route's `try/except` makes that irrelevant to the response either way).

## Next Phase Readiness
- The alarm's far end now exists, is guarded, is proven to run (10 tests, 4 calibrated), and answers the checkpoint's recorded outcome. Plans 05/06 (the live `cron.schedule(...)` registration on PROD) can build on a fixed contract: `cron_name`, `gap_minutes` (nullable), `ceiling`, `detected_at`, path `/api/prober-cadence-alert`, always-200.
- ⚠️ Standing caveat, carried forward and NOT resolved by this plan: delivery to a human still depends on `SENTRY_DSN` staying set on Railway, outside this repository's control. Nothing here, or in any downstream plan, may claim the alarm permanently reaches a human without that caveat attached.
- Criterion 5's residual (nothing watches PROD `pg_cron` itself) remains owned by plan 05, unchanged by this plan.

## Self-Check: PASSED

`analytics-service/routers/cron.py` confirmed on disk carrying the route (`grep -n 'prober-cadence-alert' routers/cron.py`); `analytics-service/tests/test_prober_cadence_alert.py` confirmed on disk (10 tests, all passing); both commit hashes (`524b300f`, `06ccd703`) confirmed in `git log --oneline`; `cd analytics-service && .venv/bin/python -m pytest tests/test_prober_cadence_alert.py -q` re-run clean at 10/10 passing; `cd analytics-service && .venv/bin/python -m pytest -q` (full suite) clean at 5877 passed, 89 skipped, 0 failed; `cd analytics-service && .venv/bin/python -m mypy --strict --follow-imports=silent services/ routers/ models/` (the CI-scoped strict gate) reports `Success: no issues found in 95 source files`.

---
*Phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and*
*Completed: 2026-09-18*
