---
phase: 164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and
plan: 06
type: execute
wave: 4
depends_on: ["164.1.1-05"]
files_modified:
  - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-PROD-SESSION.md
  - scripts/prod-prober/cron-manifest.json
  - TODOS.md
autonomous: false
requirements: [PROBER-CADENCE-UNDELIVERED-01]
tags: [live-op, pg-cron, prod-session, cron-manifest, checkpoint]

estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The observer is LIVE: a PROD `cron.job` row named `prod_prober_cadence_check` runs `SELECT public.prod_prober_cadence_check();` hourly, registered by a founder-approved session against the linked-to-PROD CLI, never by a migration."
    - "⚠️ `scripts/prod-prober/cron-manifest.json` carries the new job in the SAME session as the registration. Without that, the prober's `cron-drift` arm correctly reports a fresh regression this phase caused, and the only honest remedy is to finish the session — never to silence the arm."
    - "The session is AUDITABLE: every statement run against PROD, its output, and the database-marker answer that preceded it are recorded in a committed `164.1.1-PROD-SESSION.md`, the same artifact Phase 164.5.1 produced for the only comparable act in this repository. A destructive or catalog-mutating act on a shared database that lives only in a transcript is not auditable."
    - "The first live tick is OBSERVED, not assumed: the session records the observer's own `cron_runs` row, its status, and — if it alarmed — that a post was made. An absent row after the scheduled minute is recorded as a failed tick with its SQLSTATE, never as silence."
    - "`node scripts/prod-prober/run.mjs --preflight-repoint` passes AFTER the manifest is committed, proving the committed oracle and live PROD agree again."
    - "`[PROBER-CADENCE-UNDELIVERED-01]` closes here and only here, with the date, the jobid and a pointer to the session record."
  artifacts:
    - "`.planning/phases/164.1.1-…/164.1.1-PROD-SESSION.md` — the auditable record: marker answer, every statement, every output, the first-tick reading"
    - "scripts/prod-prober/cron-manifest.json — the new job row, captured from PROD by the tool, never hand-edited"
    - "TODOS.md — `[PROBER-CADENCE-UNDELIVERED-01]` closed, dated, pointing at the session record"
  key_links:
    - "checkpoint approval → cron.schedule → live PROD cron.job → --capture-manifest → cron-manifest.json → --preflight-repoint green → cron-drift arm green"
  prohibitions:
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT run `supabase db push`, `db reset --linked`, `--project-ref` or `--db-url` from this checkout at any point. This checkout's CLI is linked to PRODUCTION and the migration reaches PROD only by merge → apply-test → the Production human-reviewer gate." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT run a single statement that writes before the database-marker query has been run and has come back naming PRODUCTION. `current_database()` is `postgres` on both projects and proves nothing; a NULL marker means stop, not proceed." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT hand-edit `scripts/prod-prober/cron-manifest.json`. It is captured from PROD by `--capture-manifest`, whose refusal conditions are a feature; a hand-edited row whose command does not hash to its own sha is `manifest-invalid` by the arm's own rule." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: privacy, status: resolved, verification: judgment, statement: "MUST NOT put a connection string, a password, a project ref, a key, an MT5 account number, a broker server name or an absolute home path into the session record. UUIDs are truncated to eight characters. `.planning/` is TRACKED and this repository is PUBLIC." }
    - { requirement_id: PROBER-CADENCE-UNDELIVERED-01, category: safety, status: resolved, verification: judgment, statement: "MUST NOT shorten the prod-prober's GitHub Actions schedule as part of this session, or at all. Under the same throttling more attempts buys a louder claim, not a bounded gap — that is the measurement the whole phase rests on." }
---

<objective>
Run the live operation the runbook describes, record it so it is auditable, and close the entry.

Purpose: everything before this plan is a mechanism nobody has switched on. Until a PROD `cron.job`
row exists, the prober's cadence is exactly as unobserved as `[PROBER-CADENCE-UNDELIVERED-01]`
describes — the code is shipped and the control is not running. And the act itself has two
properties this repository has paid for: it writes a live catalog on a database other systems use,
and it invalidates a committed oracle in the same instant, so the manifest re-capture belongs to the
same session rather than to a follow-up somebody remembers.

Output: a live hourly observer, a committed manifest that agrees with PROD, an auditable session
record, and a closed entry.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/164.5.1-cronrepoint-the-live-match-engine-cron-row-is-repointed-at-t/164.5.1-PROD-SESSION.md
@TODOS.md
@CLAUDE.md
@scripts/prod-prober/cron-manifest.json

⭐ Read `docs/runbooks/prod-prober-cadence-go-live.md` (plan 05) and plan 05's SUMMARY first. This
plan EXECUTES that runbook; it does not restate it, and where the two disagree the runbook is the
instruction and this plan is the wrapper that gates and records it.

⛔ **This plan cannot start before the migration is LIVE on PROD.** The function reaches production
only by merge → `apply-test` → the `Production` environment's human-reviewer gate. Registering a job
whose function does not exist gives an error on every tick. The checkpoint below asserts that first.

⛔ **Three reviewers run before the migration is asked to apply**: `migration-reviewer`,
`rls-policy-auditor`, `silent-failure-hunter`. Their findings are fixed before the apply, not after.

## Decision register — see plan 01's `<context>` for the full mapping

- D-09 (planner) — `cron.schedule(...)` is a live op, and the manifest re-capture is a step of the same session.
- D-06 (planner) — the observer writes its own `cron_runs` row every tick; that row is what the first-tick reading looks for.
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: CHECKPOINT — approve writing a new row into the live PROD cron.job catalog</name>
  <precondition>Migration `20260918120000_prod_prober_cadence.sql` has applied to PROD through merge → `apply-test` → the `Production` reviewer gate, and `migration-reviewer`, `rls-policy-auditor` and `silent-failure-hunter` have all run against it with their findings fixed.</precondition>
  <reversibility rating="one-way">A write to the live PROD `cron.job` catalog on a database other systems use. It is reversible by `cron.unschedule`, but not before the job has ticked and written rows, and not without a second manifest re-capture — so it is treated as the irreversible act it operationally is.</reversibility>
  <decision>Do we register `prod_prober_cadence_check` in the live PROD `pg_cron` catalog NOW, and re-capture the committed cron manifest in the same session?</decision>
  <context>
    ⛔ State the four preconditions as ANSWERED FACTS with their evidence, not as a list to tick
    later. (1) The migration has applied to PROD — say HOW that was verified, naming the run or the
    query, never "it merged". (2) The three reviewers ran and their findings are fixed — name them.
    (3) `node scripts/prod-prober/run.mjs --preflight-repoint` passes right now, so the committed
    manifest and live PROD agree BEFORE the change, which is what makes the after-diff readable.
    (4) The working tree is clean, so the captured manifest can be reviewed as a diff rather than as
    a file. If any of the four is not a fact you can point at, say WHICH and stop — this checkpoint
    exists to catch exactly that.

    Blast radius, taken from the runbook rather than re-derived: one new catalog row on a shared
    production database; 24 `public.cron_runs` rows a day thereafter; an outbound post carrying an
    EXISTING Vault key to an EXISTING allow-listed destination when the ceiling is crossed; and a red
    `cron.job_run_details` entry rather than a silent tick if that post cannot be delivered.

    The cost of NOT doing it, stated because a checkpoint that lists only risks biases toward
    inaction: everything this phase built stays dormant, the prober's cadence stays exactly as
    unobserved as `[PROBER-CADENCE-UNDELIVERED-01]` describes, and the phase would close having
    shipped a control nobody switched on — which is the shape of the defect it exists to fix.
  </context>
  <options>
    <option id="proceed-now">
      <name>Proceed with the live session now</name>
      <pros>The observer starts running, the manifest and PROD agree again within the same session, and `[PROBER-CADENCE-UNDELIVERED-01]` can close with evidence rather than with an intention.</pros>
      <cons>Writes the live PROD catalog; requires the operator to stay through the first tick at minute 20 so the go-live is observed rather than assumed.</cons>
    </option>
    <option id="defer-to-a-chosen-time">
      <name>Defer the session to a chosen time</name>
      <pros>Nothing is written until an operator can see it through, including the first-tick reading; the pre-flight is simply re-run then.</pros>
      <cons>The phase stays shipped-but-dormant until it happens, and the branch carries an open entry whose closure condition is a human act somebody has to remember.</cons>
    </option>
    <option id="stop-precondition-unmet">
      <name>Stop — a precondition is not a fact</name>
      <pros>The honest answer whenever the migration's PROD apply, a reviewer pass, a green `--preflight-repoint` or a clean tree cannot be pointed at. Recording which one is missing is itself the useful output.</pros>
      <cons>Nothing proceeds until it is resolved; name the missing precondition and what would establish it.</cons>
    </option>
  </options>
  <resume-signal>Reply with `proceed-now`, `defer-to-a-chosen-time` or `stop-precondition-unmet`. The chosen id, its date, its author and the four preconditions' evidence are written into `164.1.1-PROD-SESSION.md`.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: The PROD session — marker first, register, re-capture, observe the first tick, record everything</name>
  <files>.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-PROD-SESSION.md, scripts/prod-prober/cron-manifest.json</files>
  <precondition>Task 1 returned `proceed-now`, and the operator has a PROD session available that is NOT `supabase db push`, `db reset --linked`, `--project-ref` or `--db-url` from this checkout.</precondition>
  <action>Execute `docs/runbooks/prod-prober-cadence-go-live.md` against PROD and record it as you go, in `164.1.1-PROD-SESSION.md`, modelled on the 164.5.1 session record: every statement verbatim, every output verbatim, each with what it proved.</action>
  <instructions>
    Order, and it is not negotiable:

    1. **The database marker query FIRST**, before any statement that writes — the one in CLAUDE.md
       that reads `shobj_description` for the current database. Paste its answer verbatim into the
       record. ⛔ A NULL answer means the marker was lost: stop and re-set it rather than proceeding
       on a guess. `current_database()` reads `postgres` on BOTH projects and proves nothing.
    2. Run the runbook's pre-flight checks, ALL of them, recording each answer. A failed check stops
       the session; record which one and what it said.
    3. Register the job with the runbook's exact `cron.schedule` statement. Record the returned jobid.
    4. **In the same session**, run `node scripts/prod-prober/run.mjs --capture-manifest --out <a
       scratch path>`. Diff the result against the committed manifest. ⛔ Confirm the ONLY change is
       the new job row — a capture that also moves another row means something else changed on PROD
       since the last capture, which is a finding to record and investigate, not a diff to accept.
       Move the captured file into `scripts/prod-prober/cron-manifest.json` and commit it together
       with the session record.
    5. Run `node scripts/prod-prober/run.mjs --preflight-repoint` again. It must now pass against the
       new committed manifest. Record its output.
    6. **Observe the first tick.** Wait for minute 20 and read the observer's own row with the
       `cron_name`-narrowed query the runbook gives. Record the row, its status, and — if it was
       `error` — that a post was made. ⛔ If there is NO row after the scheduled minute the tick
       RAISED: record the SQLSTATE from `cron.job_run_details` and treat it as a FAILED go-live, not
       as a quiet success. Say explicitly which of the two you observed.

    ⚠️ Nothing in this session runs against shared TEST, and nothing asserts globally about it. Every
    reading is narrowed to this phase's own `cron_name` values and its own jobname.

    ⛔ Keep the record publishable: no connection string, no password, no project ref, no key, no
    absolute home path; UUIDs truncated to eight characters. `.planning/` is tracked and this
    repository is PUBLIC.
  </instructions>
  <verification>`164.1.1-PROD-SESSION.md` records the marker answer, every pre-flight answer, the schedule statement and its jobid, the manifest diff showing only the new row, a passing `--preflight-repoint`, and an explicit first-tick reading naming which of "row observed" or "tick raised with SQLSTATE" happened. `scripts/prod-prober/cron-manifest.json` carries the new job and was produced by `--capture-manifest`, never by hand. `node scripts/prod-prober/run.mjs --self-test` still exits 0.</verification>
  <resume-signal>Type "session recorded" once the record is committed alongside the manifest, or describe where the session stopped and what the last statement returned.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Close the entry — dated, with the jobid, pointing at the session record</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md — the `[PROBER-CADENCE-UNDELIVERED-01]` entry and the dated progress note plan 05 added, plus a nearby CLOSED entry for the house closure style.
    - .planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-PROD-SESSION.md — the jobid and the first-tick reading, to cite rather than paraphrase.
  </read_first>
  <action>
    Close `[PROBER-CADENCE-UNDELIVERED-01]` in `TODOS.md`. Tick it, and append a dated closure note
    that states what now exists and points at the evidence: the observer's job name and jobid, the
    date it was registered, the session record's path, and the first-tick reading.

    Say what the entry's own text demanded and what now answers it, in one sentence each: it demanded
    the observer live where a scheduler that actually fires does, and it now runs in PROD `pg_cron`
    rather than in GitHub Actions; it demanded a measured ceiling rather than a hopeful interval, and
    the ceiling is a single constant in the migration carrying its sample and its date; it demanded
    the alarm be proven to fire, and the gate observes it firing on a stale contact and staying silent
    on a fresh one, with every arm proven red by the mutation runner.

    ⛔ State what is NOT closed, in the same note, with its id: the unwatched `pg_cron` liveness
    residual plan 05 booked. A closure that omits the residual it deliberately accepted reads as
    coverage nobody has.

    ⛔ Do not restate the ceiling's numeric value in this note, or anywhere in planning prose. Refer
    to it by its symbol and name the migration that declares it — this repository carries a named
    recurring citation-drift defect class, and a number restated in a ledger is the next instance of
    it.
  </action>
  <verify>
    <automated>grep -A3 'PROBER-CADENCE-UNDELIVERED-01' TODOS.md | grep -q '\[x\]' &amp;&amp; grep -q 'PGCRON-LIVENESS-UNWATCHED-01' TODOS.md</automated>
  </verify>
  <done>The entry is ticked and dated, cites the jobid and the session record, names what answered each of its own demands, and names the residual it did not close. No numeric ceiling appears in the note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator session → the live PROD catalog | an irreversible-in-practice write to a database other systems use |
| the committed manifest → the prober's drift arm | the oracle is invalidated by the registration in the same instant |
| `.planning/` session record → the world | the record is tracked and published |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-164.1.1-24 | Tampering | the registration landing on the wrong database | critical | mitigate | The database marker query runs FIRST and its verbatim answer is in the record; a NULL or non-PROD answer stops the session; no `db push`, `db reset --linked`, `--project-ref` or `--db-url` is used at any point |
| T-164.1.1-25 | Tampering | a hand-edited cron manifest | high | mitigate | The manifest is produced by `--capture-manifest`, which refuses a configuration failing hygiene and writes nothing; a hand-written row whose command does not hash to its own sha is `manifest-invalid` by the arm's own rule |
| T-164.1.1-26 | Repudiation | a live catalog change recorded only in a transcript | high | mitigate | Every statement and output is committed in `164.1.1-PROD-SESSION.md`; this repository has a measured incident in which a destructive act on a shared database lived in one phase log and nowhere else |
| T-164.1.1-27 | Repudiation | a go-live declared without observing a tick | high | mitigate | The session must record the first-tick reading and say explicitly which of "row observed" or "tick raised with SQLSTATE" occurred; an absent row is a failed go-live, not silence |
| T-164.1.1-28 | Information Disclosure | credentials or identifiers in a public session record | high | mitigate | Named prohibition plus the standing repository rule; UUIDs truncated to eight characters |
| T-164.1.1-29 | Tampering | a drifting manifest silencing the prober's own arm | high | mitigate | `--preflight-repoint` is re-run after the commit and must pass, proving the oracle and PROD agree again |
| T-164.1.1-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable — this plan installs no package |
</threat_model>

<verification>
- `node scripts/prod-prober/run.mjs --preflight-repoint` exits 0 against the newly committed manifest.
- `node scripts/prod-prober/run.mjs --self-test` still exits 0 (the manifest is a self-test fixture input in one scenario; a capture that broke it would show here).
- `git diff --stat` for this plan shows only the session record, the manifest and `TODOS.md`.
- The session record contains no connection string, password, project ref, key, absolute home path, or untruncated UUID.
- `[PROBER-CADENCE-UNDELIVERED-01]` is ticked, dated, cites the jobid and the session record, and names `[PGCRON-LIVENESS-UNWATCHED-01]` as the residual it did not close.

⚠️ **Ship note, not a task here.** Per the repository's CHANGELOG discipline, the PR that carries
this phase writes ONE unified `CHANGELOG.md` entry in the same commit as the `VERSION` bump, with
`VERSION` and `package.json` byte-equal four-digit strings and never `npm version`. Enumerate every
commit on the branch, group by theme, and cross-check that each maps to at least one bullet.
⚠️ `covered_files` in this phase's VERIFICATION must NOT include `TODOS.md`, `CHANGELOG.md`,
`VERSION` or `package.json`.
</verification>

<success_criteria>
- A PROD `cron.job` row named `prod_prober_cadence_check` runs hourly, registered by an approved, recorded session and by no migration.
- The committed cron manifest agrees with live PROD, captured by the tool in the same session, and `--preflight-repoint` proves it.
- The first tick was observed and recorded, including the failure reading if it failed.
- `[PROBER-CADENCE-UNDELIVERED-01]` is closed with evidence, and the residual it accepted is named in the same note.
</success_criteria>

<output>
Create `.planning/phases/164.1.1-probercadence-the-prober-s-detection-latency-is-measured-and/164.1.1-06-SUMMARY.md` when done.
</output>
