---
phase: "164.1.1"
slug: "probercadence-the-prober-s-detection-latency-is-measured-and"
status: verified
# threats_open = count of OPEN threats at or above block_on severity (the blocking gate).
# The 7 threats registered in Plan 06's <threat_model> are NOT counted here: Plan 06 (the live
# PROD cron.schedule registration) has not executed on this branch. They are DEFERRED, not open.
threats_open: 0
asvs_level: 1
block_on: high
register_authored_at_plan_time: true
threats_total: 35
threats_closed: 28
threats_deferred: 7
verdict: SECURED
audited: "2026-09-18"
head_sha: "71667b75"
branch: "feat/164.1.1-probercadence"
auditor: gsd-security-auditor
---

# Phase 164.1.1 (PROBERCADENCE) — Security

**Mode:** verification of a plan-time register — all six `164.1.1-0*-PLAN.md` files carry a
parseable `<threat_model>` block (`register_authored_at_plan_time: true`). The auditor verified
each registered mitigation against the implementation; this was not a blind scan for new
vulnerabilities.

⚠️ **Plans 01–05 are executed (5 SUMMARYs exist). Plan 06 is NOT executed** — it is the live PROD
`cron.schedule(...)` registration session, gated behind a separate founder-run op against the
linked-to-PROD Supabase CLI, documented in `docs/runbooks/prod-prober-cadence-go-live.md`.
Confirmed on disk: no `164.1.1-06-SUMMARY.md`, no `164.1.1-PROD-SESSION.md`;
`scripts/prod-prober/cron-manifest.json` carries zero rows named `prod_prober_cadence_check`.
The seven threats Plan 06 registers (six numbered + its own supply-chain check) are **deferred**,
not open defects — see the dedicated section below. Re-run this audit after that session lands.

---

## Verdict

**SECURED.** 28 of 35 registered threats closed with direct evidence (grep/read against the
cited implementation file, or an automated test I ran myself). Zero threats open. Seven threats
deferred to the not-yet-executed Plan 06 live session — tracked, not silently folded into either
the closed or the open count.

---

## Trust Boundaries (as declared across the six plans)

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → PROD Postgres | the prober's `psql` session writes one row with a live PROD session-pooler credential |
| PROD Postgres → Railway analytics-service | `net.http_post` carries the Vault-held `analytics_service_key` out of the database |
| gate transaction → the lane cluster | the SQL gate drops a CHECK constraint and deletes a Vault stand-in row to produce states production must refuse |
| PROD Postgres → Railway analytics-service (route side) | an unauthenticated-by-default HTTP surface carrying a service key in a header |
| Railway environment → this repository | the alarm's terminal reachability (SENTRY_DSN) is a fact held outside every committed file |
| operator session → the live PROD catalog | Plan 06 only — an irreversible-in-practice write to a database other systems use |
| repo (PUBLIC) → the world | migration text, `RAISE` strings, runbook prose and the Actions log are all world-readable |

---

## Threat Register — Closed (28/35)

Verified at HEAD `71667b75` on `feat/164.1.1-probercadence`, ASVS L1 (grep/read-level pattern
presence in the cited file, plus live test execution where a suite existed — not full end-to-end
trace, per the L1 depth this config specifies).

| Threat ID | Category | Severity | Disposition | Evidence |
|-----------|----------|----------|-------------|----------|
| T-164.1.1-01 | Info Disclosure | high | mitigate | `supabase/migrations/20260918120000_prod_prober_cadence.sql:157,160,167,177` — every `RAISE EXCEPTION` names the setting that failed, never its value |
| T-164.1.1-02 | Info Disclosure | medium | mitigate | same file:187-198 — the outbound alert body carries only `cron_name`/`gap_minutes`/`ceiling`/`detected_at`, no url and no key |
| T-164.1.1-03 | Tampering | high | mitigate | Layer (a): CHECK constraint `system_settings_analytics_service_url_allowed`, `supabase/migrations/20260907120000_analytics_service_settings_and_vault_tick.sql:214-223`. Layer (b): in-body regex re-test, `20260918120000_prod_prober_cadence.sql:176-178`. Both layers independently exercised by gate arm U1 (see T-10 below) |
| T-164.1.1-04 | Elevation of Privilege | high | mitigate | Fix confirmed at commit `b409adbf` (re-verified myself, not taken on trust): `20260918120000_prod_prober_cadence.sql:295` — `REVOKE ALL ... FROM PUBLIC, anon, authenticated, service_role;` with **no** `GRANT ... TO service_role` anywhere in the file (grepped, 0 hits); STEP 4 self-verifying DO block (`:298-376`) asserts via `aclexplode` that the EXECUTE grantee set equals the function owner exactly. Closes the HIGH independently raised by migration-reviewer and rls-policy-auditor pre-fix |
| T-164.1.1-05 | Spoofing | high | mitigate | `supabase/tests/test_prod_prober_cadence.sql:300-311` — gate arm S1 asserts `X-Service-Key` header is non-NULL and non-blank on the recorded `net.http_post` call |
| T-164.1.1-06 | Denial of Service | low | accept | `20260918120000_prod_prober_cadence.sql:120-124` states growth rate (24 rows/day) and unpruned status beside the write, matching the accept rationale in the plan's threat model |
| T-164.1.1-07 | Repudiation | medium | accept | `20260918120000_prod_prober_cadence.sql:180-186` — an undelivered post rolls back the whole tick including the observer's own row; the absence is the next tick's evidence, matching the accept rationale |
| T-164.1.1-08 | Tampering | high | mitigate | `supabase/tests/test_prod_prober_cadence.sql` — all 7 arms (G1,S1,O1,A1,U1,V1,N1) carry an adjacent `RED-UNDER-M` machine-executable twin. Plan 02's SUMMARY records a full-corpus mutation run (`arms: 402/402/0`, `biting: 402`, `0 defects`); I independently ran the four JS/TS meta-test files that pin this mechanism (`mutation-annotation-parser.test.ts`, `mutation-runner-floors.test.ts`, `gate-family-meta.test.ts`, `prod-prober-wiring.test.ts`) via `npx vitest run` — **312/312 passing** |
| T-164.1.1-09 | Tampering | medium | mitigate | `scripts/mutation-runner/run.mjs:1979` `ARMS_FLOOR = 402` (stale-high layer, gates on `bitingArms < armsFloor`); `src/__tests__/mutation-runner-floors.test.ts:575` re-derives the corpus from disk and fires `RATCHET STALE` on stale-low. Both present and passing in the vitest run above |
| T-164.1.1-10 | Info Disclosure | medium | mitigate | `supabase/tests/test_prod_prober_cadence.sql:506-508` — gate arm U1 asserts the destination-refusal message does NOT contain the foreign host it refused (`collector.attacker.example`) |
| T-164.1.1-11 | Tampering | critical | mitigate | `.github/workflows/ci.yml:1370-1375` (sql-mutation job header comment): "IT MUST NOT TOUCH THE SHARED TEST DATABASE" — every arm runs against its own throwaway PostgreSQL cluster via `scripts/pg-lane/run.sh`, no secret and no shared resource on the job |
| T-164.1.1-12 | Repudiation | critical | mitigate | Checkpoint recorded (`164.1.1-03-SUMMARY.md`) before Plan 04 committed to a channel; `analytics-service/routers/cron.py:1125-1129` carries the standing caveat verbatim: "Never round this up into a permanent guarantee that the alarm reaches a human" |
| T-164.1.1-13 | Info Disclosure | high | mitigate | `164.1.1-03-SUMMARY.md:74` — "No credential value is recorded anywhere in this repository — presence only" |
| T-164.1.1-14 | Tampering | medium | mitigate | `src/__tests__/prod-prober-wiring.test.ts:1907-1919` — `[164.1.1-03] criterion 4` block with a calibration twin (neutered, observed RED, restored per the SUMMARY); part of the 312-passing vitest run |
| T-164.1.1-15 | Spoofing | high | mitigate | `analytics-service/tests/test_prober_cadence_alert.py::test_unauthenticated_request_is_refused_by_service_key_middleware` — drives the real `main.app` stack via `httpx.ASGITransport`, asserts 401. I ran this test myself: **passed** |
| T-164.1.1-16 | Denial of Service | medium | mitigate | `analytics-service/routers/cron.py:1155,1198-1202` — one Sentry escalation per hour window (keyed by `cron_name`), unconditional structured log on every occurrence. Window/no-window behavior tests passed in my pytest run |
| T-164.1.1-17 | Info Disclosure | high | mitigate | `test_no_log_or_escalation_carries_the_request_service_key` (`test_prober_cadence_alert.py:294-314`) — I ran it, passed: a canary service-key value appears in neither the log nor the Sentry call args |
| T-164.1.1-18 | Repudiation | high | mitigate | `cron.py:1189-1202` — `logger.error` fires unconditionally before the window check; the escalation-failure-containment test (log still emitted when Sentry capture raises) passed in my run |
| T-164.1.1-19 | Repudiation | medium | accept | `cron.py:1226-1241` docstring states the fire-and-forget reasoning verbatim and names `net._http_response` as the real observability point plus `[PROBER-ALERT-DELIVERY-UNVERIFIED-01]` — matches the accept disposition |
| T-164.1.1-20 | Tampering | critical | mitigate | `docs/runbooks/prod-prober-cadence-go-live.md:64-82` — the database marker query (P0) is the first blocking pre-flight item, explicit abort-on-NULL-or-non-PROD instruction. Grepped the full runbook for `db push`/`db reset`/`--project-ref`/`--db-url` — 0 hits |
| T-164.1.1-21 | Info Disclosure | high | mitigate | Grepped the runbook for absolute home paths, `password`, `secret_value`, `service_role_key`, JWT-shaped strings — 0 hits; no UUIDs appear in the file at all |
| T-164.1.1-22 | Repudiation | high | mitigate | `docs/runbooks/prod-prober-cadence-go-live.md:42-48,189-228` — manifest re-capture is explicitly Step 2 of the same session; "the remedy is to finish Step 2, never to wait it out or to silence the arm" |
| T-164.1.1-23 | Repudiation | medium | mitigate | `TODOS.md:3634-3715` — `[PGCRON-LIVENESS-UNWATCHED-01]` and `[PROBER-ALERT-DELIVERY-UNVERIFIED-01]` both present, each cross-references the runbook's own "does not cover" section, consistent in both places |
| T-164.1.1-SC (plan 01) | Tampering (supply chain) | high | mitigate | `git diff --stat $(git merge-base main HEAD) HEAD` — 38 files changed on this branch, zero touch `package.json`/`package-lock.json`/`requirements.txt`/`pyproject.toml`/`Cargo.toml` |
| T-164.1.1-SC (plan 02) | Tampering (supply chain) | high | mitigate | same branch-wide diff — plan 02 (SQL gate + mutation runner floors) installs no package |
| T-164.1.1-SC (plan 03) | Tampering (supply chain) | high | mitigate | same branch-wide diff — plan 03 (checkpoint + criterion-4 pin) installs no package |
| T-164.1.1-SC (plan 04) | Tampering (supply chain) | high | mitigate | same branch-wide diff — plan 04 (route) reuses existing `sentry_sdk`/`fastapi` deps, installs nothing new |
| T-164.1.1-SC (plan 05) | Tampering (supply chain) | high | mitigate | same branch-wide diff — plan 05 (runbook + tooling) installs no package |

---

## Threat Register — Deferred to Plan 06 (7/35) — NOT open defects

Plan 06 is the live PROD `cron.schedule(...)` registration session. It has not executed on this
branch (confirmed: no `164.1.1-06-SUMMARY.md`, no `164.1.1-PROD-SESSION.md` on disk;
`scripts/prod-prober/cron-manifest.json` carries 0 rows named `prod_prober_cadence_check`,
matching `164.1.1-REVIEW.md` finding IN-03's own confirmation that the function ships
unscheduled). These threats cannot be marked CLOSED because the artifact their mitigation lives
in — a completed live session, recorded in `164.1.1-PROD-SESSION.md` — does not exist yet. None
of them reflect an absent mitigation in shipped code; the tooling behind each is present and
self-tested today.

| Threat ID | Category | Severity | What exists now | What's missing |
|-----------|----------|----------|------------------|-----------------|
| T-164.1.1-24 | Tampering | critical | Runbook P0 marker-query mechanism (same query as T-20) is written and ready | The actual live-session record of which database it ran against does not exist yet |
| T-164.1.1-25 | Tampering | high | `--capture-manifest` and its hash-validation (`manifest-invalid` kind) exist in `scripts/prod-prober/run.mjs:5168+`, self-tested | The live capture against real PROD `cron.job` hasn't run |
| T-164.1.1-26 | Repudiation | high | `164.1.1-PROD-SESSION.md` is specified by the runbook as the required transcript artifact | The file doesn't exist because the session hasn't happened |
| T-164.1.1-27 | Repudiation | high | Runbook "First tick — what to expect" section specifies exactly what must be recorded (row observed vs. SQLSTATE raised) | No first tick has occurred |
| T-164.1.1-28 | Info Disclosure | high | Same UUID-truncation / no-credential-value rule already proven compliant in the runbook (T-21) | Applies to a session record that doesn't exist yet |
| T-164.1.1-29 | Tampering | high | `--preflight-repoint` exists and is self-tested in `run.mjs:5421+` | Re-running it post-commit is a live-session step that hasn't happened |
| T-164.1.1-SC (plan 06) | Tampering (supply chain) | high | Plan 06 as written adds no dependency | Vacuously true only because nothing has executed yet; re-verify when the session runs |

**Action:** re-run `/gsd-secure-phase 164.1.1` after the founder runs the live registration
session per `docs/runbooks/prod-prober-cadence-go-live.md`, using `164.1.1-PROD-SESSION.md` as
the new evidence source for this table.

---

## Unregistered Flags

None. All five `164.1.1-0*-SUMMARY.md` files were grepped for `## Threat Flags` and any
`threat`-adjacent heading — zero hits in any of them. No executor flagged new attack surface
beyond the plan-time register.

---

## Accepted Risks Log

Three residuals, all named, dated and founder-confirmed UNROUTED on 2026-09-18. All are
consistent between `TODOS.md` and `docs/runbooks/prod-prober-cadence-go-live.md` (each names the
other, closing the "recorded in one place, contradicted in another" failure mode this repo has a
measured prior incident of). None is re-opened as a finding here.

| ID | What it is | Disposition | Where recorded |
|----|------------|-------------|-----------------|
| `[PROBER-ALERT-DELIVERY-UNVERIFIED-01]` | Nothing automated watches whether the new alarm's own `net.http_post` was delivered — `cron-obs`'s `net._http_response` reader is hardcoded to `jobname = 'match_engine_cron'` and will never fire for this function | Accepted; extending `cron-obs` was considered and rejected (`net._http_response` carries no `jobid`, join is time-only). Founder offered a routing phase at the review gate and chose UNROUTED | `TODOS.md:3680-3715`; runbook "What this runbook deliberately does not cover" |
| `[PGCRON-LIVENESS-UNWATCHED-01]` | Nothing periodically checks PROD `pg_cron`'s own liveness independently of any specific job's body | Accepted on two grounds: narrowness (a total Postgres outage has many louder symptoms) and a considered-and-rejected alternative (a GitHub-hosted watchdog would share the prober's own 27% delivery problem) | `TODOS.md:3634-3677`; runbook "What this runbook deliberately does not cover" |
| `[VAULTTICK-MANIFEST-COUNT-STALE-01]` | An applied migration's comment (`20260911120000_vault_tick_hardening.sql:352`) cites a manifest row count (14) that has since moved (15); the applied file is byte-frozen so the drift can't be corrected in place | Accepted; the conclusion the comment draws (every row runs as `postgres`) is unaffected by the count and re-confirmed at 15/15 | `TODOS.md:3718-3729` |

---

## Review Findings Independently Re-Verified as Fixed

Per the "verify the fix yourself" instruction, each of the following was checked against the
current diff and git history, not taken on the reviewer's or a prior pass's word:

| Finding | Original issue | Fix commit | Re-verification |
|---------|-----------------|------------|-------------------|
| Migration-reviewer / rls-policy-auditor HIGH | `GRANT EXECUTE ... TO service_role` on `prod_prober_cadence_check()` reproduced `[164.7-WR02-SERVICE-ROLE-EXECUTE]` | `b409adbf` | Grepped current migration for `GRANT.*service_role` — 0 hits; confirmed `REVOKE ALL ... FROM PUBLIC, anon, authenticated, service_role` at `:295` and the `aclexplode`-based owner-equality assertion at `:334-372` |
| WR-01 (code review) | `cron.py` docstring implied `cron-obs` already covers this function's own delivery | `b97b044c` | Current docstring (`cron.py:1226-1241`) attributes the gap correctly to `[PROBER-ALERT-DELIVERY-UNVERIFIED-01]` |
| WR-02 (code review) | Sentry rate-limit window was a single global, not scoped by `cron_name` | `17046cca` | Current code uses `dict[str, float]` keyed by `alert.cron_name` (`cron.py:1161,1199-1202`) |
| WR-03 (code review) | Limiter-quarantine comment overstated an unenforced "invoked ONLY by" claim | (same fix wave) | `analytics-service/tests/test_limiter_route_coverage.py:341-348` now reads "gated by the same shared service key ... no additional caller restriction is enforced" |
| IN-01 (code review) | `detected_at` validated but never surfaced in the log line or Sentry message | `385145a2` | Both now included at `cron.py:1188-1196,1206-1210` |

---

## Dependency / Supply-Chain Check

`git diff --stat $(git merge-base main HEAD) HEAD` — 38 files changed across the whole
`feat/164.1.1-probercadence` branch; zero touch any dependency manifest
(`package.json`, `package-lock.json`, `analytics-service/requirements.txt`,
`analytics-service/pyproject.toml`, `Cargo.toml`). Closes all six plans' `T-164.1.1-SC` entries
for the code that has actually shipped (plans 01–05); plan 06's own SC entry is listed under
"Deferred" above since it belongs to the unexecuted plan.

---

*Audited: 2026-09-18. Auditor: gsd-security-auditor. HEAD: `71667b75` on `feat/164.1.1-probercadence`.*
