---
audit: v1.17 MT5 — ingested, wizardable, surfaced (Phases 147–154, 156)
audited: 2026-08-14
auditor: gsd-integration-checker
tree: docs/v1.17-close-v1.18-open @ e7871403 (docs-only over main c4555fd0 = v0.62.0.0)
scope_amended: 2026-08-14 — Phase 155 + MT5-06..10, MT5-15, MT5-GOAL-01 carried to v1.18
status: gaps_found
score: 82/100
engineering_complete: true
ledger_complete: false
blockers: 4
warnings: 7
---

# v1.17 Milestone Integration Audit

**Verdict: `gaps_found`.** The *engineering* is done and the seams genuinely connect — every
in-scope requirement was traced end-to-end through source at HEAD and resolves to WIRED. The gaps
are **ledger-honesty gaps, not code gaps**, and three of the four blockers are the exact failure
mode the 2026-08-14 amendment was written to prevent: `ROADMAP.md` was amended, and **nothing else
was**.

⭐ **The amendment itself is honestly recorded — in ONE file.** `ROADMAP.md:3-23` states the
scope change explicitly, dates it, names the founder decision, names the declined alternative,
carries MT5-06..10 / MT5-15 / MT5-GOAL-01 to v1.18 (`:59`), marks Phase 155 `[→] CARRIED OUT OF
v1.17 TO v1.18` rather than `[x]` or deleted (`:125`), and re-states ⛔ *"Do not advertise MT5
until Phase 155 passes"* (`:61`). The commit `e7871403` even replaced the nine-day-stale Phase 155
PRECONDITION note. That is a model amendment.

⛔ **But `git show --stat e7871403` is one file: `.planning/ROADMAP.md`.** `STATE.md`,
`PROJECT.md`, `REQUIREMENTS.md` and `MILESTONES.md` were not touched, and two of them still carry
the refused title verbatim.

---

## The hunt the brief asked for: does anything claim MT5 numbers are verified?

**Searched:** `ROADMAP.md`, `STATE.md`, `PROJECT.md`, `MILESTONES.md`, `REQUIREMENTS.md`,
`TODOS.md`, `README.md`, `DESIGN.md`, plus every user-facing surface for a "verified"/"parity"
claim scoped to MT5.

**Result: no surface, route or component claims MT5 figures match the terminal.** The public
`api_verified` trust tier is data-*provenance* vocabulary applied uniformly across venues (sFOX,
ccxt, MT5) and does not assert numeric parity. Nothing user-facing over-claims.

**Two ledger strings do carry the refused reading**, both found and both listed as blockers below
(B1, B2). One historical string is borderline (W4).

---

## BLOCKERS

### B1 — `STATE.md` still titles v1.17 with the sentence the founder declined
`/Users/helios-mammut/claude-projects/quantalyze/.planning/STATE.md`

Frontmatter `:4` — `milestone_name: MT5 — usable end-to-end, not merely ingested`
Body `:25` — `**Milestone v1.17 — MT5: usable end-to-end, not merely ingested, Phases 147–156.**`

`ROADMAP.md:11-13` records that a bounded alternative was **declined** precisely because it
*"would leave every ledger reading 'MT5 usable end-to-end' while our numbers had never once been
compared to the broker's."* One ledger still reads exactly that.

Compounding, in the same file:
- `status: executing` (`:5`) — the milestone is closed.
- `:52-54` lists **MT5-VERIFY** as in-scope: *"live trading-day external-oracle verification,
  MT5-10 UNCAPPED, `complete_with_warnings` explained; MT5-GOAL-01 umbrella acceptance gate."*
- `:56-59` — *"29 in-scope IDs — … MT5-06..10, MT5-14, MT5-15, … + MT5-GOAL-01 umbrella — 29/29
  mapped to Phases 147–155"* and *"Roadmap: `.planning/ROADMAP.md` (v1.17 section, Phases
  147–155)."*
- `progress: total_phases: 17 / completed_phases: 15 / percent: 88` — encodes an unfinished v1.17
  that includes 155.
- Zero occurrences of `v1.18`, `Phase 157`, or the carry anywhere in 1,400 lines.

⛔ Per the stated constraint this file must be **hand-edited**, never via
`gsd-sdk query state.advance-plan` / `state.update-progress`, and the hand-written Migration-B
warning must survive.

### B2 — `PROJECT.md` carries the same title and still lists MT5-VERIFY as a v1.17 target
`/Users/helios-mammut/claude-projects/quantalyze/.planning/PROJECT.md`

`:23` — `## Current Milestone: v1.17 MT5 — usable end-to-end, not merely ingested (started 2026-08-04)`
`:47-49` — target feature bullet: *"**MT5-VERIFY — the numbers are true.** Server-UTC offset
measured live, rendered performance checked against an external oracle, on a live funded account
on a real trading day."*

This is the milestone-scope document. As written it says v1.17 targets the very thing v1.17
explicitly does not earn.

### B3 — Four delivered, code-verified, in-scope requirements are still UNTICKED with no delivery note
`/Users/helios-mammut/claude-projects/quantalyze/.planning/REQUIREMENTS.md`

| REQ | Line | Box | Phase | Phase VERIFICATION | Traced in code |
|-----|------|-----|-------|--------------------|----------------|
| SCEN-01 | `:1090` | `[ ]` | 147 | `human_needed`, **9/9** must-haves verified | **WIRED** |
| OWN-02  | `:528`  | `[ ]` | 148 | `human_needed`, **4/4** must-haves verified | **WIRED** |
| OWN-04  | `:596`  | `[ ]` | 148 | (same report) | **WIRED** |
| NAV-01  | `:1285` | `[ ]` | 149 | `human_needed`, **5/5** must-haves verified | **WIRED** |

Each requirement body is still the **original problem statement**, with no "✅ DELIVERED" note —
unlike OWN-03 (`:544`), WIZCONT-01 (`:631`) and every other requirement the later phases closed.
`ROADMAP.md:116-118` ticks all three phases `[x] … (completed 2026-08-05)` and the Progress table
(`:727-729`) records 6/6, 5/5, 5/5 Complete.

**A reader consulting REQUIREMENTS.md alone would conclude that the silent money-path correctness
bug (SCEN-01), the owner factsheet (OWN-02/04) and the entire NAV-01 surface never shipped.** That
is a worse-shaped error than an over-tick: it invites someone to re-plan work that exists.

Traces performed (all confirmed at HEAD, not inferred from SUMMARYs) — see the Integration Map.

### B4 — WIZFORM-03 has NO resolved disposition, and its recorded reason is FALSE at HEAD
`/Users/helios-mammut/claude-projects/quantalyze/.planning/REQUIREMENTS.md:778` (checkbox `[ ]`) and `:1400` (traceability row)

The traceability row states:

> ⛔ `ConnectKeyStep` / `MultiKeyConnectStep` are **Phase 153.4's** and still pass neither, so an
> MT5 user can still read "switch to a different exchange" on the CONNECT step

**Measured at HEAD — that is false.** Both call sites pass `surface: "connect"` **and** `venue`:
- `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx:1082-1089` — `surface: "connect"`, `venue: attemptExchange ?? exchange`
- `src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx:1847-1854` — `surface: "connect"`, `venue: attemptVenue` (per panel)
- `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx:~681` — `venue: snapshot.exchange ?? undefined`

The venue-conditional class is exactly three codes — `KEY_PROBE_FAILED` (`wizardErrors.ts:936`),
`KEY_RATE_LIMIT` (`:1163`), `KEY_NETWORK_TIMEOUT` (`:1184`), each carrying
`fixRequires: [null, REQUIRES_SUBSTITUTABLE_VENUE, REQUIRES_NON_SUBSTITUTABLE_VENUE]`. All three
render **only** on Connect / MultiKeyConnect / Submit, and all three of those surfaces now pass a
venue. `153-VERIFICATION.md` warning **W-153-4** already caught this contradiction on 2026-08-13
and it has not been actioned; `ROADMAP.md:372` states outright that WIZFORM-03 *"is closed
further than `REQUIREMENTS.md` admits — that row contradicts"* its own neighbour.

**This is a blocker not because the code is wrong but because the requirement has no disposition.**
An in-scope v1.17 requirement is neither met-and-ticked nor carried-with-an-owner. It must become
one or the other before close. On the evidence above the honest resolution is **tick it** and
delete the refuted sentence.

---

## WARNINGS

**W1 — `ROADMAP.md:733` Progress row reads `153. WIZFORM + MT5-14 | 0/? | Not started`.** Six
sub-phases (153.1–153.6) shipped through PR #675. Line `:739` in the same file says v1.17 closes
at 10 phases delivered *including 153*. The table contradicts the sentence six lines below it.

**W2 — `ROADMAP.md:752-768` "Requirement Coverage (v1.17)" is stale and its totals no longer
hold.** Row 153 lists `WIZFORM-01, WIZFORM-02, WIZFORM-03, WIZFORM-04, MT5-14` — **WIZFORM-05 is
absent**; row 150 lists only `OWN-03` — **OWN-05 is absent**. Both were added 2026-08-05, after
the table was written, and `REQUIREMENTS.md:1392/:1402` correctly homes them at 150/153. The
table's closing claim *"29/29 in-scope requirement IDs mapped … No orphans, no duplicates"* is
therefore wrong in both directions (two orphans; and row 156 adds five CONNECT IDs that were never
in the 29). `153-VERIFICATION.md` W-153-3 flagged the same undercount.

**W3 — `REQUIREMENTS.md` traceability matrix (`:1418-1428`) still reads "Pending" for requirements
that are ticked `[x]` in the same file** — SCEN-02..05, AUM-01..05, NAV-01. Broad staleness of one
table, not a claim about MT5, but it makes the matrix unusable as a status source.

**W4 — `MILESTONES.md:20` is the nearest thing in the repo to "MT5 numbers are verified."** The
v1.15 Phase 136 entry reads *"ground-truth parity vs `account_info().equity`"*. That parity is the
reconstruction's own offline anchor under `analytics-service/tests/` — **not** a comparison of
rendered performance against the terminal on a trading day. Someone auditing "have MT5 numbers
ever been checked?" lands here. Recommend a one-clause qualifier, e.g. *"(offline reconstruction
anchor — live trading-day parity is v1.18 Phase 155)"*. Historical record; do not rewrite the
milestone.

**W5 — `MILESTONES.md` has no v1.17 entry and `.planning/milestones/` holds no v1.17 archive.**
Consistent with "CLOSED but NOT ARCHIVED" — but the close is currently invisible in the milestone
history, so v1.17 exists only as a ROADMAP header.

**W6 — The two headline new surfaces of this milestone have ZERO e2e coverage.** `grep -rn
"my-strategies" e2e/` returns **nothing** — NAV-01's entire ranking surface is unexercised by any
spec. No spec covers the owner-factsheet lane (OWN-02), whose acceptance is *adversarial*
(anon-404-after-owner-render) and whose vitest layer models `unstable_cache` with a spy that
cannot exercise the real Next data cache (148-VERIFICATION human item 2). Separately —
**pre-existing, not a v1.17 regression** — `e2e/api-key-flow.spec.ts`,
`e2e/sync-analytics-flow.spec.ts`, `e2e/full-flow.spec.ts` and `e2e/csv-upload-flow.spec.ts` exist
in the repo and are wired into **no** CI batch (0 matches in `.github/workflows/ci.yml`), so the
connect→sync→factsheet→scenario chain has no executing E2E gate at any layer. `wizard-resume.spec.ts`
**is** wired (`ci.yml:1785`) and ran green — that is the milestone's one real browser proof.

**W7 — Phases 150/151 minted three new client surfaces that render `code: UNKNOWN` directly**, and
they sit outside the WIZFORM-02 coverage law's population (which is wizard-scoped):
`AllocateDialog.tsx:159,220`, `RenameStrategyDialog.tsx:152,161`,
`MarkOwnershipDialog.tsx:139,151`. Not a WIZFORM-02 violation as that requirement is worded
("no *wizard* failure"), and each is a genuine unclassifiable catch-all — but it is the same class
on surfaces this milestone created, and the derived coverage law will not police them. Worth a
TODOS entry alongside the two 153.7 residues already recorded there.

---

## ⭐ The Phase 153 SPAN verdict — settled

**The brief asked for an explicit answer. Here it is: YES — the Phase 153 parent checkbox
(`ROADMAP.md:122`) can now be ticked, conditional on B4.**

**What the `failed` verdict was.** `153-VERIFICATION.md` (2026-08-13, `status: failed`,
`span_verification: true`, scope 153.1→153.6) scored **5/6**. WIZFORM-01, WIZFORM-03, WIZFORM-04,
WIZFORM-05 and MT5-14 all verified. WIZFORM-02 failed with a live PROD instance
(`MT5_GATEWAY_UNCONFIGURED` rendering `code: UNKNOWN` on 2026-08-12/13) and **four** `missing`
items.

**Each missing item, re-derived from source at HEAD — not from the 153.7 summaries:**

| # | `missing` item | State at HEAD | Evidence |
|---|----------------|---------------|----------|
| 1 | Disposition for the analytics-service ROUTER envelope vocabulary — `MT5_GATEWAY_UNCONFIGURED` + `MT5_GATEWAY_UNREACHABLE` | **CLOSED** | `src/lib/wizardErrors.ts:3081-3082` — `MT5_GATEWAY_UNCONFIGURED → {SEAM_INTERNAL_FAULT, 500}`, `MT5_GATEWAY_UNREACHABLE → {SERVICE_UNREACHABLE, 503}`, both with the roster-cost reasoning written out inline |
| 2 | A DERIVED population covering `routers/**` `service_error(...)` codes | **CLOSED** | `src/lib/seam-venue-vocabulary.invariant.test.ts:89` — root is `ANALYTICS_ROOT = analytics-service` (was `services/` only); `:318-320` — callee family `service_error`/`service_error_body`/`service_error_response` with per-callee positional slots, alongside the pre-existing `ASSIGNMENT_RE` |
| 3 | A boundary decision, written down | **CLOSED** | `:134` `SCAN_EXCLUSIONS` — each exclusion carries its own reason and count pin (`:149-182`) |
| 4 | The three code-less `finalize-wizard` rejections (D-153.2-D) | **CLOSED** | `src/lib/wizardErrors.invariant.test.ts:571` — `KNOWN_CODELESS_FINALIZE_REJECTIONS = 0` |

**Independently executed at this tree** (not taken on report):

```
npx vitest run src/lib/seam-venue-vocabulary.invariant.test.ts \
               src/lib/wizardErrors.invariant.test.ts --no-file-parallelism
→ Test Files 2 passed (2) · Tests 60 passed (60)
```

Both halves of the coverage law are armed and green at HEAD. The mechanism 153.7-01 landed
**RED BY DESIGN** is now satisfied rather than removed.

**The ruling, stated precisely so it cannot be over-read:**

- The **historical** `153-VERIFICATION.md` verdict stands as written. On 2026-08-13, across
  153.1→153.6, WIZFORM-02 *had* failed. Do not rewrite that file's `status: failed`.
- The **parent goal** — "the wizard stops costing submits; errors land inline; transient infra
  never becomes a user decision; copy never advises the impossible; MT5 can declare its venue" —
  **is met at HEAD**, via inserted Phase 153.7 (verified 9/9, reviewed 0-blocking, shipped
  `c4555fd0`).
- Therefore the honest form of the tick is **`[x]` with the mechanism named**, e.g.
  *"goal met 2026-08-14 via inserted Phase 153.7; the 153.1→153.6 span verdict remains `failed`
  as the historical record."* A bare `[x]` would erase the fact that the span shipped short.
- ⚠️ **Conditional on B4.** A "6/6 requirements" claim is not supportable from the ledger while
  WIZFORM-03's checkbox reads `[ ]` and its traceability row asserts a refuted fact. Fix B4 in the
  same pass, or the tick creates a fresh contradiction.
- The residue is **named, not hidden**: five analytics-service codes still render `UNKNOWN` on
  admin/simulator terminal surfaces, and `KEY_UNDECRYPTABLE`'s remedy sentence on
  `keys/[id]/permissions` is inaccurate. Both are separate TODOS.md items (`TODOS.md:398-439`)
  with the measurement that `KeyPermissionBadge` renders no `UNKNOWN` card on that path.

---

## Cross-Phase Integration Map

Every seam below was traced through source at HEAD. No link is marked WIRED on the strength of a
SUMMARY.

| Seam | Phases | Trace | Status |
|------|--------|-------|--------|
| Composer → returns API → differenced series | 147 ↔ 152 | `ScenarioComposer.tsx:1474` `fetch('/api/strategies/${id}/returns')` → `returns/route.ts:254` selects `daily_returns, returns_series, …` → `:299` `resolveDailyReturnSeries(daily_returns, returns_series)` | **WIRED** |
| One resolver, not a third mechanism (Rule 7) | 147 | `src/lib/factsheet/resolve-series.ts:50` is the sole definition; 6 non-test consumers — factsheet v2 `:121`, discovery detail `:65`, returns route `:299`, OG card `:90`, scenario-share `:208`, `queries.ts:1892/:4387` | **WIRED** |
| Owner factsheet gate + cache isolation | 148 | `factsheet/[id]/v2/page.tsx:6` imports **both** predicates; `:295-296` public `unstable_cache` path stays `withPublishedOnly`; `:469` owner probe `withPublishedOrOwner`; `:537` owner payload built **outside** the cache wrapper | **WIRED** |
| Wizard preview → owner factsheet (OWN-04 after OWN-02) | 148 | `SyncPreviewStep.tsx:488` `href={/factsheet/${strategyId}/v2}`, `:494` "View full factsheet →" — resolves through the owner lane above, so no dead end | **WIRED** |
| Sidebar → /my-strategies → shared ranking | 149 | `Sidebar.tsx:142` `href:"/my-strategies"` → `my-strategies/page.tsx` → `MyStrategiesSection.tsx:87` → shared `StrategyTable` (`:207` `"owner-all-statuses"` variant) + `percentile-core.ts` `getOwnRowPercentiles`; `DashboardChrome.tsx:132` chrome regex includes the route | **WIRED** |
| Capital-ownership mark: wizard → DB → holdings gate → surfaces | 150 ↔ 151 ↔ 149/148 | migration `20260806120000_strategies_capital_ownership.sql` → written `finalize-wizard/route.ts` (20 refs) + `SubmitStep.tsx` (5) → flipped `strategies/[id]/ownership/route.ts` → **gated** `portfolio-strategies/allocation/route.ts` → read `queries.ts` (8), `capital-ownership.ts`, `strategies-row-adapter.ts` → rendered `StrategyTable.tsx`, `MyStrategiesSection.tsx`, `factsheet/[id]/v2/page.tsx` | **WIRED** (full chain, incl. the hard invariant at the allocation route) |
| Non-ccxt holdings sync as a CLASS | 151 | `analytics-service/services/closed_sets.py:223-231` pins the ccxt `fetch_balance`/`fetch_positions` surface; `services/allocator_positions.py:129` handles the class by name; regression `tests/test_allocator_positions_non_ccxt.py` | **WIRED** |
| Wizard error vocabulary: Python emit → verdict → route → envelope → field | 153.1–153.7 | `analytics-service/routers/exchange.py` `service_error(...)` → derived population (`ANALYTICS_ROOT`) → `VENUE_WIRE_CODE_TO_VERDICT` (`wizardErrors.ts:3081-3082`) → `classifyKeyValidationError` (`create-with-key/route.ts:1148`) → `buildEnvelope` → `FIELD_BY_CODE`. Both invariants executed green. | **WIRED** |
| Venue-conditional copy suppression | 153.1 ↔ 153.4 | 3 venue-conditional codes; all 3 render only on Connect / MultiKeyConnect / Submit; all 3 surfaces pass `venue` | **WIRED** (ledger disagrees → B4) |
| Draft-aware overlay → resume banner → step restore | 154 | `ContributionWizardOverlay` defers mount until the draft query answers → `WizardClient` resume banner → `useState` step init; `e2e/wizard-resume.spec.ts` wired at `ci.yml:1785`, observed green in main run 31749233994 | **WIRED** |
| Stale-screen poller terminal state | 154 | `useStrategySyncPoller.ts` — the `?? "pending"` coercion removed, `sync-progress` widened, `isComposite` gates removed as a class | **WIRED** |
| Service-role writer + revoked `authenticated` EXECUTE | 156 ↔ 153.6 | `create-with-key/route.ts:28,186,805-807` `createAdminClient()` (fail-closed on missing key); `composite/add-key/route.ts:477` `rpcAdmin.rpc("add_wizard_composite_key")`; migrations `20260813150106` (A) + `20260814120000` (B); durable guard = assertion 5h from a comment-stripped `pg_get_functiondef` | **WIRED** — ⚠️ 5d/5f/5g/5h are state-adaptive and SKIP until Migration B reaches TEST; recorded on CONNECT-01, correctly, as *"Nothing here has been observed armed-and-green in CI"* |

**Orphaned exports found: none.** **Orphaned API routes found: none** among the routes these
phases created.

---

## Requirements Integration Map

| REQ | Phase | Integration path | Status | Ledger issue |
|-----|-------|------------------|--------|--------------|
| SCEN-01 | 147 | composer → returns route → `resolveDailyReturnSeries` | **WIRED** | ⛔ B3 — unticked |
| SCEN-02 | 152 | `browse/route.ts` `isOwnRow` → wire → "Yours" chip | WIRED | matrix says "Pending" (W3) |
| SCEN-03 | 152 | row `onClick` → detail panel → factsheet link (via OWN-02) | WIRED | matrix "Pending" (W3) |
| SCEN-04 | 152 | WEIGHT/USD/MODE/LEV/NOTIONAL label strip | WIRED | matrix "Pending" (W3) |
| SCEN-05 | 152 | created-at + status disambiguation on duplicate browse rows | WIRED | matrix "Pending" (W3) |
| AUM-01 | 151 | AUM input → weight math → `commit_scenario_batch` | WIRED | matrix "Pending" (W3) |
| AUM-02 | 151 | non-ccxt holdings class (`closed_sets.py` + `allocator_positions.py`) | WIRED | matrix "Pending" (W3) |
| AUM-03 | 151 | refusal copy no longer names a non-existent control | WIRED | matrix "Pending" (W3) |
| AUM-04 | 151 | `perKeyDailiesGateSatisfied` no longer all-or-nothing; cross-role contamination fixed | WIRED | matrix "Pending" (W3) |
| AUM-05 | 151 | same class fix pre-arms sFOX | WIRED | matrix "Pending" (W3) |
| NAV-01 | 149 | Sidebar → /my-strategies → shared ranking → factsheet | **WIRED** | ⛔ B3 — unticked; ⚠️ W6 no e2e |
| OWN-02 | 148 | owner probe + cache-isolated payload | **WIRED** | ⛔ B3 — unticked; ⚠️ W6 adversarial case unproven against a real cache |
| OWN-03 | 150 | wizard question → mark → Holdings allocate; team_review structurally non-allocatable | WIRED | — |
| OWN-04 | 148 | `SyncPreviewStep.tsx:488` → owner lane | **WIRED** | ⛔ B3 — unticked |
| OWN-05 | 150 | rename affordance, owner-authz, codename redaction untouched | WIRED | ⚠️ W2 absent from ROADMAP coverage table |
| MT5-14 | 153.2 | `WIZARD_EXCHANGE_CODES` + preselect from `detectedExchange` | WIRED | — |
| WIZFORM-01 | 153.2 | inline field refusal + `FIELD_BY_CODE` routing of server field errors | WIRED | — |
| WIZFORM-02 | 153.1/.2/**.7** | derived population → verdict map → route → envelope; both invariants green | WIRED | — (5 named residues on non-wizard surfaces) |
| WIZFORM-03 | 153.1/.2/.4 | `fixRequires` filter live at all 3 emitting surfaces | **WIRED in code** | ⛔ B4 — unticked + refuted reason |
| WIZFORM-04 | 153.2 | probe skipped by venue capability at both call sites | WIRED | — |
| WIZFORM-05 | 153.3/.4 | server deadline + client `budgetKeyFor(exchange)` + `ValidateWaitCard` | WIRED | ⚠️ W2 absent from ROADMAP coverage table; 120 000 ms remains PROVISIONAL (D-27, owner = v1.18 P155) |
| WIZCONT-01 | 154 | draft-aware overlay → resume banner | WIRED | — |
| WIZCONT-02 | 154 | venue-identity partial UNIQUE + app fence | WIRED | — |
| STALE-01 | 154 | poller terminal-state fix, amber mid-re-derive | WIRED | — |
| CONNECT-01..05 | 156 | service-role writer + Migration A/B + SQL gates | WIRED | ⚠️ 4 assertions SKIP until Migration B reaches TEST (honestly recorded) |

**Carried to v1.18 — correctly recorded, NOT gaps of v1.17:** MT5-06, MT5-07, MT5-08, MT5-09,
MT5-10, MT5-15, MT5-GOAL-01 (Phase 155) and the four founder-observation items (Phase 157).
`ROADMAP.md:125-126` marks both `[→]` with the carry dated and the blockers named.

---

## E2E flow verdict

**"Allocator connects a key → sync → factsheet → project in a scenario"** — every link is
**present and connected in source**; the chain has no structural break. What it does not have is
an executing end-to-end proof:

| Link | Code | Automated proof |
|------|------|-----------------|
| Connect key | ConnectKeyStep → `create-with-key` (service-role writer) | unit + `wizard-axe`, `mt5-badge` |
| Resume mid-wizard | overlay → banner → step | ✅ `e2e/wizard-resume.spec.ts`, ran green in CI |
| Sync → terminal state | poller, `?? "pending"` removed | unit only |
| Own factsheet | owner lane + cache isolation | unit + structural gate; **adversarial anon-404 unproven against a real cache** |
| Reach it from nav | Sidebar → /my-strategies | **none** |
| Project in a scenario | composer → returns route → differenced series | unit only |

Not a blocker for this milestone — every phase declared these as `human_needed` up front and named
them honestly. It is the reason `status` here is `gaps_found` on the ledger and **not** on the
engineering.

---

## Fix list to reach `passed`

1. **B1** — hand-edit `STATE.md`: `milestone_name` → the amended title, `status` → closed, scope
   prose → drop MT5-VERIFY / MT5-06..10 / MT5-15 / MT5-GOAL-01 and point them at v1.18, `progress`
   → the delivered set, and add the carry + Phase 157. ⛔ Hand-edit only; preserve the Migration-B
   warning.
2. **B2** — `PROJECT.md:23` title; `:47-49` move the MT5-VERIFY bullet to a v1.18 heading.
3. **B3** — tick SCEN-01, OWN-02, OWN-04, NAV-01 and append a delivery note to each, in the shape
   OWN-03/WIZCONT-01 already use.
4. **B4** — resolve WIZFORM-03: tick it and **delete the refuted sentence** at `:1400` (W-153-4 has
   been open since 2026-08-13), or record a carry owner.
5. **Then** tick `ROADMAP.md:122` for Phase 153 in the qualified form above, and fix the `0/? Not
   started` Progress row (W1) and the coverage table's two orphans (W2).
6. Optional but cheap: `MILESTONES.md` v1.17 entry (W5) + the W4 qualifier on the v1.15 line.

**None of these touch source code.** The engineering half of v1.17 is complete and integrated.
