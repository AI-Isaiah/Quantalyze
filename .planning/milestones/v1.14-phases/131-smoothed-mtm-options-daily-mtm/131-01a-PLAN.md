---
phase: 131-smoothed-mtm-options-daily-mtm
plan: 01a
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/docs/evidence/drb-option-daily-marks-2026-07.json
  - analytics-service/services/deribit_ingest.py
  - analytics-service/services/deribit_txn.py
  - analytics-service/tests/test_deribit_ingest.py
  - analytics-service/tests/test_deribit_txn.py
autonomous: true
requirements: [SMTM-01, SMTM-02]
must_haves:
  truths:
    - "fetch_deribit_option_daily_marks maps stubbed 1D chart bars to dict[utc_day, close] with the sibling perp fetcher's tick→UTC-day dedupe; transient exhaustion raises DeribitTransientReadError; structural no-data returns {}"
    - "replay_option_positions + option_mtm_daily telescope EXACTLY: ΔMTM sums to Book(T) − 0 on a float-exact fixture; shorts invert signs; positions carry forward across no-trade days, marks are NEVER filled"
    - "A missing daily mark inside a listed instrument's life raises LedgerValuationError naming instrument+day — no interpolation, no session-lump fallback (locked D-07)"
    - "An instrument whose life STRADDLES the ~2.5yr chart-retention horizon (partial marks, head holes) raises LedgerValuationError via the SAME hole guard — the D-07 consequence is PINNED, not a surprise"
    - "The full analytics suite is green with the new functions DORMANT — nothing invokes smoothed yet, so every existing output is trivially byte-identical (SC-4 holds by construction in this plan)"
  artifacts:
    - path: "analytics-service/docs/evidence/drb-option-daily-marks-2026-07.json"
      provides: "Encoded feasibility probe (mark source, rejected alternatives, position semantics, telescoping check)"
    - path: "analytics-service/services/deribit_ingest.py"
      provides: "fetch_deribit_option_daily_marks (greenfield clone of fetch_deribit_perp_daily_index — NOT yet called by any production path)"
      contains: "fetch_deribit_option_daily_marks"
    - path: "analytics-service/services/deribit_txn.py"
      provides: "replay_option_positions + option_mtm_daily pure functions (pandas/async-free, NOT yet called by any production path)"
      contains: "option_mtm_daily"
  key_links:
    - from: "analytics-service/tests/test_deribit_ingest.py"
      to: "analytics-service/services/deribit_ingest.py"
      via: "stubbed-exchange tests over fetch_deribit_option_daily_marks"
      pattern: "option_daily_marks"
    - from: "analytics-service/tests/test_deribit_txn.py"
      to: "analytics-service/services/deribit_txn.py"
      via: "failing-first tests over replay_option_positions / option_mtm_daily incl. the hole + retention-straddle guards"
      pattern: "option_mtm_daily"
---

<objective>
Plan 01a of the split 131-01 (seam at the original Task-2/Task-3 boundary, per plan-check
MEDIUM-1): the evidence record + the option daily-mark fetcher + the pure replay/ΔMTM core of
the `smoothed_mtm` third factsheet basis. This plan lands and passes pytest + the AST purity
guard with NOTHING invoking smoothed yet — independently CI-green, all new code dormant.
131-01b (depends on this plan) wires the basis enums, the `use_smoothed` branch, the adapter
ΔMTM merge, the smoothed identity channels, and the SC-4/total-preservation acceptance pins.

Purpose: options books currently lump per-SESSION deltas (weeks of accrual) onto single
settlement days (live evidence: 94%/day, +3305%/month, total correct). `smoothed_mtm` spreads
each lump across the days it accrued via `Book[d]−Book[d−1]`, total-preserving (telescoping:
Σ native_pnl = Σchange + Book(last settlement)).

Output: mark fetcher + pure replay/ΔMTM functions — proven by failing-first tests, including
the D-07 hole guard and the retention-straddle case. No enum members, no branch, no adapter
merge, no worker/frontend wiring (131-01b / 131-02 / 131-03). ADDITIVE by construction: no
production path calls the new functions, so `cash_settlement` and `mark_to_market` stay
byte-identical without any gating needed in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-CONTEXT.md
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-RESEARCH.md
@.planning/phases/131-smoothed-mtm-options-daily-mtm/131-PATTERNS.md
@.planning/milestones/post-v1.8-standalone-phases/83-daily-option-mtm/83-PLAN.md

Read-first source anchors (all under analytics-service/, line numbers per 131-PATTERNS.md,
re-verify with grep before editing):
- services/deribit_ingest.py:597-691 (`fetch_deribit_perp_daily_index` — clone target),
  :1759-1836 (`build_deribit_native_ledger`), :723 (`CompletenessReport`), :787/:1820
  (`deribit_raw_rows_have_option_activity`), :1611 (`_build_dense_native_marks`)
- services/deribit_txn.py:144 (`classify_instrument` option arm), :816
  (`_NATIVE_OPTIONS_SUMMARY_TYPES`), :848-853 (basis constants + `_PNL_BASES` +
  `DEFAULT_PNL_BASIS`), :1384 (`_summary_coverage_windows`), :1506/:1652
  (`assert_balance_identity` + branch), :1691 (`_summary_contribution`), :1709
  (`_option_commission`), :1728 (`txn_rows_to_native_daily`), :1798 (`use_mtm`),
  :1847-1870 (summary handling), :1926-1932 (option trade/delivery `-commission` arm)
- services/allocated_capital.py:56 (`_VALID_PNL_BASES`), :158/:230 (validation sites)
- tests/test_deribit_txn.py:980 (AST purity guard), tests/test_cash_basis_series_sc4.py,
  tests/test_native_nav_sc4_identity.py (`_real_adapter_ledger` stubbed-exchange pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 0: Evidence record — encode the verified mark-source probe (no live run)</name>
  <files>analytics-service/docs/evidence/drb-option-daily-marks-2026-07.json</files>
  <action>
    Commit the 2026-07-22 probe facts as a JSON evidence record (same doc pattern as the
    existing drb-options-semantics-2026-07.json in the same directory — read one sibling for
    shape). Encode, verbatim from 131-RESEARCH.md and 131-CONTEXT.md "Verified feasibility":
    (1) `public/get_tradingview_chart_data resolution=1D` returns daily marks for EXPIRED
    option instruments — 4 expired BTC options (Dec-24…Sep-25 expiries), each status=ok,
    401 daily bars, real closes, bars stamped 08:00 UTC (Deribit settlement boundary);
    (2) rejected alternatives — `get_mark_price_history` returns []/HTTP-400 for options
    (DVOL-constituents only); `get_last_settlements_by_instrument` and public trades refuse
    expired instruments; (3) signed post-trade `position` field semantics — shorts negative,
    deliveries zero the position (verified live); (4) the telescoping total-preservation
    identity Σ_d native_pnl = Σchange + Book(last settlement); (5) retention horizon ~2.5yr;
    (6) ⚠️Apr-2026 delivery change: ITM option → physical settle into future → future
    delivers to cash (two log rows near expiry, final P&L unchanged). Cross-reference
    drb-options-semantics-2026-07.json (its E1–E5 facts stay valid; attribution consequences
    superseded by this phase). NO live API call gates this task — the executor ENCODES the
    settled evidence, never re-derives it (83-PLAN doctrine).
  </action>
  <verify>
    <automated>python3 -c "import json,sys; json.load(open('analytics-service/docs/evidence/drb-option-daily-marks-2026-07.json'))"</automated>
  </verify>
  <done>Evidence file exists, is valid JSON, and names the mark source, both rejected alternatives, position semantics, the telescoping identity, and the retention horizon.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: fetch_deribit_option_daily_marks — greenfield clone of the perp-index fetcher</name>
  <files>analytics-service/services/deribit_ingest.py, analytics-service/tests/test_deribit_ingest.py</files>
  <behavior>
    - Happy path: stubbed exchange returning {status:"ok", ticks:[...ms...], close:[...]} →
      dict[utc_day_str, close] with the tick→strftime("%Y-%m-%d") UTC-day mapping and dedupe,
      exactly the mapping fetch_deribit_perp_daily_index pins (same one-day-basis class)
    - Transient errors exhaust the retry budget → DeribitTransientReadError
    - BadSymbol / status!="ok" (structural no-data) → {} (honest empty, scrubbed message)
    - Non-positive closes skipped
    - Request params carry the EXACT instrument name verbatim (no suffix synthesis) and
      ms bounds derived from oldest_day / newest_day
  </behavior>
  <action>
    Write the failing tests FIRST in tests/test_deribit_ingest.py (new test class, stubbed
    exchange per the existing perp-index tests — grep the file for the perp fetcher's tests
    and mirror them). Then implement
    `fetch_deribit_option_daily_marks(exchange, instrument, *, oldest_day, newest_day, sleep, max_retries)`
    in deribit_ingest.py as a ~60-line clone of `fetch_deribit_perp_daily_index` (:597-691 —
    READ IT FIRST; same `public_get_get_tradingview_chart_data` endpoint, same transient-retry
    → DeribitTransientReadError, same {}-on-structural-no-data, same tick→UTC-day dedupe).
    Differences from the sibling: takes the instrument name verbatim, and takes an explicit
    `newest_day` (callers pass the expiry-capped span — never fetch past expiry). Returns
    dict[utc_day, close]. The structural-gap / fail-loud decision belongs to the CALLER
    (Task 2 here / 131-01b Task 4) — this function stays an honest fetch, exactly like its
    sibling.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_deribit_ingest.py -k "option_daily_marks" -x -q</automated>
  </verify>
  <done>All new tests green; tests demonstrably failed before the implementation (RED commit precedes GREEN commit); no change to fetch_deribit_perp_daily_index.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: replay_option_positions + option_mtm_daily — the pure core (deribit_txn.py)</name>
  <files>analytics-service/services/deribit_txn.py, analytics-service/tests/test_deribit_txn.py</files>
  <behavior>
    replay_option_positions(rows):
    - Long build/reduce/close; shorts negative; delivery zeroes; multi-instrument and
      multi-currency separation; rows sorted by (timestamp, id) within each instrument
      before replay (crawl concat order NOT trusted); out-of-order input handled
    - Option trade/delivery row with absent/None/""/non-numeric `position` →
      LedgerValuationError (id/type-only message; parametrized over the four shapes)
    - Perp/future/spot rows IGNORED (classification-gated via classify_instrument:144)
    - Delivery row with nonzero post-trade position ACCEPTED as data (partial delivery is
      Deribit's call) — not asserted zero
    option_mtm_daily(positions, marks):
    - Single long option held N days → ΔMTM equals hand-computed mark deltas and telescopes
      to Book(T) − 0 EXACTLY (float-exact fixture); short signs invert; positions
      carry-forward across no-trade days (marks NEVER filled)
    - test_day_grid_convention_bar_tick_day: mark[instr][D] = close of the 1D bar whose tick
      falls on UTC day D (bar stamped D 08:00) — the pinned 83-PLAN §2-Q1 convention
    - HOLE: a day inside [first_held, min(last_held, expiry)] with nonzero position and no
      bar → LedgerValuationError naming instrument+day (D-07; NO interpolation, NO
      session-lump fallback). Mutation-honesty: dropping one bar from a green fixture reddens
    - RETENTION-STRADDLE (plan-check MEDIUM-3, D-07 consequence PINNED): an instrument whose
      life STRADDLES the ~2.5yr chart-retention horizon yields PARTIAL marks — head days of
      the held window have no bar — and MUST raise LedgerValuationError via the SAME hole
      guard (no special case, no head-trim, no interpolation). Explicit fixture: held window
      opening before the horizon, marks present only from the horizon onward → fails naming
      instrument + the earliest missing day. ACCEPTED CONSEQUENCE, encoded in the test's
      docstring so it is pinned rather than a surprise: as the retention window moves forward
      in real time, a currently-GREEN options key can begin hard-failing ALL persisted
      analytics on future recomputes once a held instrument's life comes to straddle the
      horizon. This conforms to locked D-07 (fail loud over fabricate); only WHOLLY
      pre-retention instruments are excluded upstream (131-01b Task 4 partition) — straddlers
      are never excluded. The founder-visible operational trade-off is surfaced in 131-02.
    - AST purity guard (test_deribit_txn.py:980) stays green — both functions pandas/async-free
  </behavior>
  <action>
    Failing tests FIRST. Implement two pure functions in deribit_txn.py (plain dicts, sorted,
    no pandas — the AST guard enforces this):
    `replay_option_positions(rows)` → per-instrument
    {currency, first_day, last_day, positions: {day: signed_size}} reconstructed from the
    signed post-trade `position` field on option trade/delivery rows (option classification
    via the EXISTING classify_instrument option arm — do not fork it).
    `option_mtm_daily(positions, marks)` → per-(day, ccy)
    Book[c][d] = Σ_instr pos[instr][d] × mark[instr][d] over the union day grid (positions
    carry forward between events; marks never filled), ΔMTM[c][d] = Book[c][d] − Book[c][d−1].
    Returns (delta_mtm: dict[ccy, dict[day, float]], terminal_book: dict[ccy, float]) — the
    terminal book feeds 131-01b Task 5's guard channel. Instruments flagged pre-retention
    (131-01b Task 4) are excluded BEFORE this function sees them — straddlers are NOT
    (partial marks fall through to the hole guard; write the straddle fixture as its own test
    exercising that exact path). Document the bar-tick-day grid convention and the
    ≤8h-skew-cancels-day-over-day/total-exact argument in the option_mtm_daily docstring
    (from 131-RESEARCH "Day grid"). Fail-loud messages: LedgerValuationError, instrument+day
    (hole) or id/type-only (position drift) — never row payloads (leak discipline).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_deribit_txn.py -k "replay_option or option_mtm or day_grid" -x -q && python -m pytest tests/test_deribit_txn.py -k "purity" -x -q</automated>
  </verify>
  <done>All behavior tests green from failing-first; AST purity guard green; hole guard proven load-bearing by the dropped-bar mutation test; retention-straddle fixture fails loud via the same guard with the accepted consequence documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Deribit public API | untrusted chart-data responses cross into money math (dormant until 131-01b wires the caller) |
| transaction-log rows → replay | venue-supplied `position`/`change` fields drive P&L |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-131-01 | Tampering | fetch_deribit_option_daily_marks | mitigate | reuse sibling's scrubbed-error + typed-response handling; non-positive closes skipped; the book-channel anchor cross-check that independently polices mark integrity lands in 131-01b Task 5 |
| T-131-02 | Info disclosure | LedgerValuationError messages | mitigate | instrument+day / id/type-only — never row payloads or balances (existing leak discipline) |
| T-131-SC | Tampering | package installs | accept | no new packages installed in this phase |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/ -q` fully green — the new functions are
  dormant, so every pre-existing test passes UNMODIFIED (trivial SC-4)
- `python -m mypy --strict --follow-imports=silent services/deribit_txn.py services/deribit_ingest.py` → 0 errors
- AST purity guard (test_deribit_txn.py:980) green — deribit_txn.py stays pandas/async-free
- `git diff` audit: every production hunk is a NEW function (or its docstring) — NO hunk
  alters any existing execution path; no enum, branch, or adapter change in this plan
- No pre-existing test modified or deleted
</verification>

<success_criteria>
- Mark fetcher behaviors (happy path, transient exhaustion, structural no-data, verbatim
  instrument, expiry-capped bounds) green from failing-first
- replay/ΔMTM telescoping exact on the float-exact fixture; hole guard AND retention-straddle
  guard fail loud naming instrument+day; the straddle's accepted consequence is documented in
  the test itself (D-07 pinned, not a surprise)
- Full analytics suite + AST purity green with all new code dormant — independently CI-green
  before 131-01b wires anything
</success_criteria>

<output>
Create `.planning/phases/131-smoothed-mtm-options-daily-mtm/131-01a-SUMMARY.md` when done.
</output>
