---
phase: 26-stress-testing-var
plan: 02
type: execute
wave: 2
depends_on: ["26-01"]
files_modified:
  - src/app/(dashboard)/allocations/components/StressVarSection.tsx
  - src/app/(dashboard)/allocations/components/StressVarSection.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [STRESS-01, STRESS-02]

must_haves:
  truths:
    - "The own-book ScenarioComposer renders a 'Stress & VaR' section after ScenarioBenchmarkSection that lets an allocator pick a BTC shock preset (−10/−20/−30%, −30% default) and see the projected portfolio impact (STRESS-01)"
    - "The section shows historical VaR(95%) + CVaR/Expected Shortfall with an inline disclosure line (method · N overlapping days · 95% · not a forecast) — never a bare VaR (STRESS-02)"
    - "Below the Phase-22 sample floor (evaluateSampleFloor with the imported SAMPLE_FLOOR_OVERLAPPING_DAYS, never a literal 60) the section renders SampleFloorEmptyState (STRESS-02 / honesty)"
    - "Guard order is fixed (#509): scenario-side absence → BTC unavailable → floor fail → ok; each empty-state heading matches its body"
    - "Every value flows through formatPercent/formatNumber → '—' on null; losses are MONOCHROME (text-text-secondary, Geist Mono), never red/#DC2626/text-negative"
    - "The VaR/CVaR disclosure names the scenario N; the β-shock disclosure names the BTC inner-join N; two captions render when they differ (each number names its true N)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/StressVarSection.tsx"
      provides: "Props-only presentational Stress & VaR section over computeScenarioStress; SegmentedControl shock affordance; 4-state guard-order routing; em-dash discipline"
      exports: ["StressVarSection"]
      min_lines: 90
    - path: "src/app/(dashboard)/allocations/components/StressVarSection.test.tsx"
      provides: "State matrix (4 states), em-dash discipline, monochrome-not-red losses, disclosure-line presence, floor-SoT import assertion"
      contains: "StressVarSection"
      min_lines: 110
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Mounts <StressVarSection> as a sibling <Card className=\"mt-6\"> after the ScenarioBenchmarkSection Card"
      contains: "StressVarSection"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/StressVarSection.tsx"
      to: "src/app/(dashboard)/allocations/lib/scenario-stress.ts (computeScenarioStress)"
      via: "the section's only math source"
      pattern: "computeScenarioStress"
    - from: "src/app/(dashboard)/allocations/components/StressVarSection.tsx"
      to: "src/lib/sample-floor.ts (evaluateSampleFloor, SAMPLE_FLOOR_OVERLAPPING_DAYS)"
      via: "floor gate — import the SoT constant, never a literal 60"
      pattern: "evaluateSampleFloor|SAMPLE_FLOOR_OVERLAPPING_DAYS"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/app/(dashboard)/allocations/components/StressVarSection.tsx"
      via: "mount as sibling Card after ScenarioBenchmarkSection at the :1574-1580 seam"
      pattern: "<StressVarSection"
---

<objective>
Build the presentational `StressVarSection` (props-only, over `computeScenarioStress` from Plan 26-01), pin its state-matrix/honesty test suite, and mount it in the own-book `ScenarioComposer` after `ScenarioBenchmarkSection`.

This plan is the surfacing + disclosure + floor-gating half of Phase 26. The math is already proven null-safe in 26-01; this section translates the result into the DESIGN.md-conformant UI contract (26-UI-SPEC.md): a `SegmentedControl` shock affordance, a headline projected impact, VaR(95%) + CVaR rows, mandatory inline disclosure, and the fixed #509 guard-order empty states. Losses are honest monochrome data, never red — the explicit divergence from the anti-pattern `VarExpectedShortfall.tsx` widget.

Purpose: deliver the allocator-facing capability for STRESS-01 + STRESS-02 with every honesty invariant (em-dash-not-0, floor-gate, heading-matches-body, two-N disclosure, no bare VaR) verified by component tests, and wire it into the composer with every prop already in scope (no new state/fetch/memo).
Output: `StressVarSection.tsx` (presentational), `StressVarSection.test.tsx` (state matrix + honesty), and a surgical mount edit to `ScenarioComposer.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-stress-testing-var/26-UI-SPEC.md
@.planning/phases/26-stress-testing-var/26-PATTERNS.md
@.planning/phases/26-stress-testing-var/26-01-SUMMARY.md
@DESIGN.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase this session — use directly, no exploration. -->

From src/app/(dashboard)/allocations/lib/scenario-stress.ts (built in Plan 26-01 — this section's only math source):
```typescript
export interface ScenarioStress {
  varN: number;                  // VaR window overlap (scenario n)
  betaN: number;                 // β-shock window overlap (BTC inner-join n) — can be smaller
  beta: number | null;
  projectedImpact: number | null;
  var: number | null;
  cvar: number | null;
}
export function computeScenarioStress(
  portfolioDaily: DailyPoint[], btcDaily: DailyPoint[],
  opts?: { confidence?: number; shock?: number },
): ScenarioStress;   // defaults confidence=0.95, shock=-0.30
```

From src/lib/sample-floor.ts (the Phase-22 floor SoT — import the constant, NEVER a literal 60):
```typescript
export const SAMPLE_FLOOR_OVERLAPPING_DAYS = 60;
export function evaluateSampleFloor(n: number | null | undefined, floor?: number): SampleFloorVerdict;
// verdict.reason: "ok" | "below-floor" | "no-usable-n"; guards no-usable-n (null/NaN/Inf/negative) FIRST.
```

From src/components/scenarios/SampleFloorEmptyState.tsx:
```typescript
export function SampleFloorEmptyState(props: {
  verdict: SampleFloorVerdict;   // renders null when verdict.reason === "ok" (fail loud)
  feature?: string;              // e.g. "VaR" / "stress"
  strategyCount?: number;        // 0/1 routes to fewStrategiesBody
}): JSX.Element | null;
```

From src/components/ui/EmptyStateCard.tsx:
```typescript
export function EmptyStateCard(props: { heading: string; body: string }): JSX.Element;
```

From src/components/strategy-v2/SegmentedControl.tsx (the shock affordance — UI-SPEC locked):
```typescript
export function SegmentedControl(props: {
  options: { id: string; label: string; disabled?: boolean }[];
  activeId: string;
  onChange: (id: string) => void;
  ariaLabel: string;             // active segment = border-accent text-accent (the ONE accent use); aria-pressed
}): JSX.Element;
```

From src/lib/scenario-history.ts:
```typescript
export function methodologyLine(n: number): string;
// `Historical realized · ${n} overlapping days · not a forecast.`  — extend with " · 95%" per UI-SPEC.
```

From src/lib/utils.ts (both return "—" for null/non-finite; formatPercent prints the sign):
```typescript
export function formatPercent(value: number | null | undefined, decimals?: number): string;
export function formatNumber(value: number | null | undefined, decimals?: number): string;
```

From src/lib/scenario.ts:
```typescript
export type DailyPoint = { date: string; value: number };
```

ScenarioComposer mount seam (src/app/(dashboard)/allocations/components/ScenarioComposer.tsx) — VERIFIED line numbers:
- `:93`  existing `import { ScenarioBenchmarkSection } from "./ScenarioBenchmarkSection";`  (add the StressVarSection import next to it; `Card` already imported)
- `:1574-1580`  the existing `<Card className="mt-6"><ScenarioBenchmarkSection .../></Card>` block
- `:1580`  its closing `</Card>`  — INSERT the new sibling `<Card className="mt-6">` IMMEDIATELY AFTER, before the Pairwise correlation Card at `:1582+`
- Props already in composer scope: `scenarioMetrics.portfolio_daily_returns ?? []` (`:1576`), `btcDaily` (state `:439`), `btcAvailable` (state `:440`), `scenarioMetrics.n` (`:937`), `deAliased.strategies.length` (`:923/:937`)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the props-only StressVarSection (4-state guard order + em-dash + monochrome)</name>
  <files>src/app/(dashboard)/allocations/components/StressVarSection.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx (the VERBATIM template: imports + "use client" at :1-11, props shape at :62-72, MetricRow at :74-98, guard-order routing at :119-131, methodology caption at :160-162)
    - src/app/(dashboard)/allocations/widgets/risk/VarExpectedShortfall.tsx (the ANTI-PATTERN — do NOT copy: red losses #DC2626, fabricated 0 zero-state, <10 floor; reuse NOTHING from its presentation)
    - src/components/scenarios/SampleFloorEmptyState.tsx (below-floor render; feature + strategyCount props; renders null on ok)
    - src/components/strategy-v2/SegmentedControl.tsx (:31-73 — the shock affordance props + accent-active visual)
    - src/components/ui/EmptyStateCard.tsx (heading/body)
    - .planning/phases/26-stress-testing-var/26-UI-SPEC.md (the FULL approved contract: §States 4-state order, §Copywriting verbatim copy, §Color monochrome-loss rule, §Typography 4-size/2-weight)
    - .planning/phases/26-stress-testing-var/26-PATTERNS.md (§StressVarSection.tsx pattern assignments — the two-N gating note)
    - src/app/(dashboard)/allocations/lib/scenario-stress.ts (computeScenarioStress + ScenarioStress, from 26-01)
  </read_first>
  <behavior>
    - State 1: portfolioDaily.length === 0 ⇒ EmptyStateCard heading "Stress & VaR unavailable" + scenario-side body (names the scenario, NOT BTC).
    - State 2: !btcAvailable ⇒ EmptyStateCard heading "Stress testing unavailable" + BTC-unavailable body (names BTC).
    - State 3: !evaluateSampleFloor(n, SAMPLE_FLOOR_OVERLAPPING_DAYS).ok ⇒ SampleFloorEmptyState(verdict, feature="VaR", strategyCount).
    - State 4 (ok): SegmentedControl (−10/−20/−30%, −30% default active) + headline "Projected portfolio impact" row + "Value at Risk (95%)" row + "Expected Shortfall (CVaR, 95%)" row + disclosure caption(s).
    - A null estimate cell renders "—" (via formatPercent), NEVER "0.00" and NEVER red.
    - Changing the active shock preset recomputes the projected impact (the segment selection IS the interaction; no submit CTA).
    - When the VaR window N and the BTC-overlap N differ, render two methodology captions (each names its true N).
  </behavior>
  <action>
    Create `src/app/(dashboard)/allocations/components/StressVarSection.tsx` as a `"use client"` props-only presentational component, mirroring `ScenarioBenchmarkSection.tsx` verbatim in structure (copy the import block, the `MetricRow` helper, the guard-order routing skeleton, the caption pattern). It is the SIBLING template — copy structure, copy guard order, copy the formatter-on-every-value discipline. Do NOT copy anything from the anti-pattern `VarExpectedShortfall.tsx`.

    Props interface `StressVarSectionProps`: `portfolioDaily: DailyPoint[]`, `btcDaily: DailyPoint[]`, `btcAvailable: boolean`, `n: number` (the VaR-window scenario N), `strategyCount: number`.

    The ONLY state is `const [shockId, setShockId] = useState("-0.30")` for the active `SegmentedControl` preset (UI-SPEC: −10/−20/−30%, −30% default). All math comes from `computeScenarioStress(portfolioDaily, btcDaily, { shock: Number(shockId) })` — recompute on the active shock (a `useMemo` keyed on `portfolioDaily, btcDaily, shockId` is fine).

    **Guard-order routing (NON-NEGOTIABLE #509, copy from ScenarioBenchmarkSection.tsx:119-131, adapt to UI-SPEC §States):**
    1. `if (portfolioDaily.length === 0)` ⇒ `<EmptyStateCard heading="Stress & VaR unavailable" body={NO_SCENARIO_RETURNS_BODY} />` (scenario-side; never blame BTC).
    2. `if (!btcAvailable)` ⇒ `<EmptyStateCard heading="Stress testing unavailable" body={BTC_UNAVAILABLE_BODY} />`.
    3. `const verdict = evaluateSampleFloor(n, SAMPLE_FLOOR_OVERLAPPING_DAYS); if (!verdict.ok) return <SampleFloorEmptyState verdict={verdict} feature="VaR" strategyCount={strategyCount} />;` — IMPORT `SAMPLE_FLOOR_OVERLAPPING_DAYS` from `@/lib/sample-floor`; NEVER a literal `60`.
    4. ok ⇒ the heading + SegmentedControl + the three MetricRows + disclosure caption(s).

    All empty-state copy is VERBATIM from UI-SPEC §Copywriting (the heading MUST match its body, #509): `NO_SCENARIO_RETURNS_BODY = "This scenario has no projected return history yet, so there's nothing to stress or measure. Add strategies with enough history to the scenario first."`; `BTC_UNAVAILABLE_BODY = "The BTC factor series isn't available right now, so we can't project a market shock. Try again shortly."`. Below-floor copy comes from `@/lib/sample-floor` via `SampleFloorEmptyState` — NEVER re-authored.

    **MetricRow + values:** copy the `MetricRow` shape verbatim from ScenarioBenchmarkSection.tsx:74-98 (`flex justify-between border-b border-border/50 py-2`, label `text-xs text-text-muted`, value `text-xs font-metric text-text-secondary`). Wrap EVERY value through `formatPercent` so null ⇒ "—". Numbers are MONOCHROME (`text-text-secondary`, Geist Mono) — NEVER `text-negative` / `#DC2626` / red, even for losses (UI-SPEC §Color, the explicit divergence from the widget). Heading H3 = `text-base font-semibold text-text-primary`, copy = `BTC shock & downside risk over {N} overlapping days` (UI-SPEC §Copywriting). Rows: `Projected portfolio impact` / `Value at Risk (95%)` / `Expected Shortfall (CVaR, 95%)`.

    **Shock control (per D-Area1 / UI-SPEC §Affordance, the SegmentedControl shock affordance):** `<SegmentedControl options={[{id:"-0.10",label:"−10%"},{id:"-0.20",label:"−20%"},{id:"-0.30",label:"−30%"}]} activeId={shockId} onChange={setShockId} ariaLabel="BTC shock" />`. The active segment is the ONE accent use in the section. Do NOT add a custom-magnitude input (deferred per CONTEXT §Area1, planner discretion declined for minimal control) and do NOT add the 99% level (CONTEXT default 95% only) — keep the section minimal.

    **Two-N disclosure (RESEARCH Pitfall 2 + the two-N trap):** the VaR/CVaR caption uses `methodologyLine(result.varN)` extended with " · 95%" (UI-SPEC target: "Historical · {N} overlapping days · 95% · not a forecast."). The shock caption names `result.betaN` and the shock assumptions (UI-SPEC §Copywriting shock disclosure line: single-factor BTC, linear β propagation, historical β over {betaN} overlapping days, point-in-time — not a forecast). When `result.varN !== result.betaN`, render BOTH captions (each names its own true N); when equal, a single caption is fine. Build on `methodologyLine` exactly as the sibling appends its annualization note at ScenarioBenchmarkSection.tsx:161 — NEVER hand-author the disclosure copy. Captions are `text-[11px] text-text-muted`.

    Add `data-testid` attributes on the metric rows/values (e.g. `stress-row-{metric}` / `stress-value-{metric}`) mirroring the sibling so the component tests can assert per-cell em-dash discipline. No fenced code beyond the implementation; no icons (DESIGN.md "no decorative elements").
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/StressVarSection.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/allocations/components/StressVarSection.tsx` exists, is `"use client"`, exports `StressVarSection`, and imports `computeScenarioStress` from `../lib/scenario-stress`.
    - Floor SoT: `grep -E "SAMPLE_FLOOR_OVERLAPPING_DAYS|evaluateSampleFloor" StressVarSection.tsx` returns matches AND `grep -E "(= |\(|, )60[^0-9]" StressVarSection.tsx` (after filtering comments) returns 0 matches — the literal 60 is never re-declared.
    - Monochrome losses: `grep -E "#DC2626|text-negative|text-red|text-destructive" StressVarSection.tsx` returns 0 matches (the explicit divergence from VarExpectedShortfall.tsx).
    - Guard order present in source order: `portfolioDaily.length === 0` check appears BEFORE the `!btcAvailable` check, which appears BEFORE the `evaluateSampleFloor` check (verifiable by line order).
    - Disclosure single-sourced: `grep "methodologyLine" StressVarSection.tsx` returns a match; the section does NOT hand-author "overlapping days · not a forecast" as a string literal (grep that literal returns 0 in the .tsx outside the methodologyLine call).
    - Shock affordance: `grep "SegmentedControl" StressVarSection.tsx` matches, with options `-0.10`/`-0.20`/`-0.30` and `activeId={shockId}` default `"-0.30"`.
    - `npx tsc --noEmit` reports no type errors referencing StressVarSection.tsx.
  </acceptance_criteria>
  <done>StressVarSection is a props-only presentational section over computeScenarioStress with the fixed 4-state #509 guard order, SegmentedControl shock affordance, em-dash-on-null and monochrome (never red) values, the imported floor SoT (no literal 60), and single-sourced two-N disclosure captions.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the StressVarSection state-matrix + honesty test suite</name>
  <files>src/app/(dashboard)/allocations/components/StressVarSection.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/StressVarSection.tsx (the component under test, from Task 1)
    - src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.test.tsx (the VERBATIM test template: buildDates + series helpers at :25-50, the full-string methodology assertion at :90-92, the scenario-side-vs-BTC empty-state attribution at :155-200, the beta/alpha within-row em-dash assertions at :202-244, the no-role=alert / no-red assertions at :99/:126-127)
    - src/components/scenarios/SampleFloorEmptyState.test.tsx (:73-83 — below-floor render + honest-absence, no red)
    - .planning/phases/26-stress-testing-var/26-UI-SPEC.md (§States + §Copywriting — the exact strings to assert)
  </read_first>
  <action>
    Create `src/app/(dashboard)/allocations/components/StressVarSection.test.tsx` (Vitest + @testing-library/react). Copy the `buildDates` + non-degenerate `series` helpers verbatim from ScenarioBenchmarkSection.test.tsx:25-50 (business-day ISO dates, alternating non-degenerate values; build a >=60-overlap series for the ok-state tests so the floor passes).

    Mirror the state-matrix tests (ScenarioBenchmarkSection.test.tsx:60-244), naming them so they double as the 26-VALIDATION §States checks:

    1. **ok state** renders the `SegmentedControl` (assert the `−30%` segment is active by default), the headline "Projected portfolio impact" row, the "Value at Risk (95%)" row, the "Expected Shortfall (CVaR, 95%)" row, AND the FULL methodology line including the literal N — assert the complete string `Historical realized · {N} overlapping days · not a forecast.` plus the " · 95%" extension (assert the full string, not a substring, mirroring :90-92).
    2. **shock interaction** (STRESS-01): fire a click on the `−10%` segment and assert the projected-impact cell changes (the segment selection recomputes the projection; no submit CTA).
    3. **scenario-side empty** (`portfolioDaily=[]`): heading + body name the SCENARIO cause ("Stress & VaR unavailable" / no-return body), NOT the BTC cause (mirror :173-200; assert the BTC copy is ABSENT).
    4. **BTC-unavailable empty** (`btcAvailable=false`, non-empty portfolioDaily): heading + body name the BTC cause ("Stress testing unavailable") (mirror :155-171).
    5. **below-floor** (`n < SAMPLE_FLOOR_OVERLAPPING_DAYS`, real overlap): renders `SampleFloorEmptyState` with heading "Not enough history for this estimate"; assert the VaR/CVaR rows are ABSENT.
    6. **em-dash discipline** (the honesty core): a degenerate ok-path input that yields a null VaR/impact renders `"—"` in the value cell, NEVER `"0.00"` — use `within(row).getByTestId("stress-value-...")` per the beta/alpha pattern at :202-244.
    7. **monochrome losses** (the explicit divergence from VarExpectedShortfall.tsx): with a loss-producing series, assert the VaR/CVaR value cells carry NO `text-negative`/`text-red`/`#DC2626`/`text-destructive` class (query the cell's className).
    8. **honest absence**: every empty state has NO `role="alert"` and NO red/`.text-negative` class (copy :99, :126-127, SampleFloorEmptyState.test.tsx:73-83).
    9. **floor SoT import assertion** (named "uses floor SoT"): assert the below-floor render uses the shared gate — e.g. render at `n = SAMPLE_FLOOR_OVERLAPPING_DAYS - 1` (import the constant into the TEST too) and assert the floor empty state shows; render at `n = SAMPLE_FLOOR_OVERLAPPING_DAYS` and assert the ok state shows (proves the section gates on the SoT value, not a hard-coded 60).

    All fixtures inline; suite fast (<2s).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/StressVarSection.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/app/(dashboard)/allocations/components/StressVarSection.test.tsx"` passes with all named tests: ok state, shock interaction, scenario-side empty, BTC-unavailable empty, below-floor, em-dash discipline, monochrome losses, honest absence, uses floor SoT.
    - The ok-state test asserts the FULL methodology string (incl. the literal N and " · 95%"), not a substring — `grep -E "overlapping days.*95%|95%.*not a forecast" StressVarSection.test.tsx` confirms the assertion exists.
    - The scenario-side-empty test asserts the BTC copy is ABSENT and vice-versa (heading-matches-body / #509 attribution).
    - The em-dash test asserts a value cell renders `"—"` and explicitly asserts it is NOT `"0.00"`.
    - The monochrome test asserts NO `text-negative`/`text-red`/`#DC2626` on the VaR/CVaR cells.
    - The "uses floor SoT" test imports `SAMPLE_FLOOR_OVERLAPPING_DAYS` and asserts the gate flips at that exact value (no hard-coded 60 in the test either).
  </acceptance_criteria>
  <done>The full state matrix, em-dash-not-0 discipline, monochrome-not-red losses, two-N disclosure presence, heading-matches-body attribution, and floor-SoT gating are all pinned and green. A red loss, a fabricated 0, a bare VaR, a misattributed empty state, or a hard-coded floor would each fail loud.</done>
</task>

<task type="auto">
  <name>Task 3: Mount StressVarSection in the own-book ScenarioComposer</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the import block around :93; the mount seam at :1574-1580 — the existing ScenarioBenchmarkSection Card; the Pairwise correlation Card that follows at :1582+)
    - src/app/(dashboard)/allocations/components/StressVarSection.tsx (the component to mount, from Task 1 — its props)
    - .planning/phases/26-stress-testing-var/26-PATTERNS.md (§ScenarioComposer.tsx MOUNT EDIT ONLY — the verified seam + the prop sources table)
  </read_first>
  <action>
    Make a SURGICAL mount edit ONLY to `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` — own-book composer ONLY (NOT the example-universe Strategy Sandbox; sandbox stress/VaR is deferred per CONTEXT §Area4).

    1. Add `import { StressVarSection } from "./StressVarSection";` immediately next to the existing `import { ScenarioBenchmarkSection } from "./ScenarioBenchmarkSection";` at `:93`. (`Card` is already imported at `:71` — do not re-import.)

    2. Insert a NEW sibling `<Card className="mt-6">` block IMMEDIATELY AFTER the existing `ScenarioBenchmarkSection` Card's closing `</Card>` at `:1580` (before the "Pairwise correlation" Card at `:1582+`):
       ```
       <Card className="mt-6">
         <StressVarSection
           portfolioDaily={scenarioMetrics.portfolio_daily_returns ?? []}
           btcDaily={btcDaily}
           btcAvailable={btcAvailable}
           n={scenarioMetrics.n}
           strategyCount={deAliased.strategies.length}
         />
       </Card>
       ```
       Every prop is ALREADY in composer scope (verified: `scenarioMetrics.portfolio_daily_returns` :1576, `btcDaily` state :439, `btcAvailable` state :440, `scenarioMetrics.n` :937, `deAliased.strategies.length` :923/:937) — add NO new state, NO new fetch, NO new memo. This is a mount-only edit; touch nothing else (Rule 3 surgical changes).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -i "ScenarioComposer" || echo "ScenarioComposer type-clean"; npm run test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "import { StressVarSection }" ScenarioComposer.tsx` returns a match adjacent to the ScenarioBenchmarkSection import.
    - `grep -n "<StressVarSection" ScenarioComposer.tsx` returns exactly ONE match, and it appears AFTER the `<ScenarioBenchmarkSection` mount and BEFORE the "Pairwise correlation" Card (verifiable by line order).
    - The new mount passes all five props (`portfolioDaily`, `btcDaily`, `btcAvailable`, `n`, `strategyCount`) sourced from existing composer scope — no new `useState`/`useEffect`/`fetch`/`useMemo` was added to the composer (the diff adds only the import + the Card block).
    - No edit to the Strategy Sandbox / ScenarioBuilder (own-book composer ONLY): `git diff --name-only` shows only ScenarioComposer.tsx + the two new files, not ScenarioBuilder.tsx.
    - `npx tsc --noEmit` reports no type errors referencing ScenarioComposer.tsx.
    - `npm run test` is fully green (the full suite, including the new lib + section tests and the coverage-impacting files).
  </acceptance_criteria>
  <done>StressVarSection is mounted as a sibling Card after ScenarioBenchmarkSection in the own-book composer, fed entirely from existing scope (no new state/fetch), the sandbox is untouched, and the full test suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

As in Plan 26-01, Phase 26 adds **NO server surface, NO migration, NO Python, NO new auth, NO new route, NO new dependency, NO persistence** (RESEARCH §Security Domain). This plan is one presentational React section + a mount edit. The load-bearing risk class is **HONESTY / CORRECTNESS** — the section must never render a fabricated number, a bare VaR, a red loss, or a misattributed empty state. The one light input-validation surface is the shock control, which is a CLOSED preset domain (`SegmentedControl`, three fixed magnitudes) — no free-text input is added (custom-magnitude deferred), so there is no untrusted-numeric path to validate.

| Boundary | Description |
|----------|-------------|
| computeScenarioStress result → rendered cell | a null estimate must render "—", never "0.00" and never red |
| degenerate/unavailable props → empty state | the rendered heading+body must name the TRUE cause (#509) |
| shock preset selection → projection | closed domain (3 presets); no untrusted free-text shock magnitude |

## Honesty / Correctness Threat Register (the real STRIDE-analog for this plan)

| Threat ID | Category (analog) | Component | Disposition | Mitigation Plan (the falsifiable test) |
|-----------|-------------------|-----------|-------------|----------------------------------------|
| T-26-07 | Misrepresentation (fabricated number) | a null VaR/impact rendered as "0.00" instead of "—" | mitigate | Task 2.6 "em-dash discipline": a null cell renders "—", explicitly asserted NOT "0.00". formatPercent/formatNumber are the single render discipline. |
| T-26-08 | Misrepresentation (loss painted as error) | losses rendered red (the VarExpectedShortfall.tsx anti-pattern) | mitigate | Task 2.7 "monochrome losses": VaR/CVaR cells carry NO text-negative/#DC2626; source grep (Task 1) returns 0 red classes. |
| T-26-09 | Misrepresentation (false precision) | a tail estimate shown on <60 overlapping days | mitigate | Task 2.5 below-floor + Task 2.9 "uses floor SoT": gate flips at SAMPLE_FLOOR_OVERLAPPING_DAYS (imported, not a literal 60); source grep (Task 1) confirms no re-declared 60. |
| T-26-10 | Misrepresentation (bare VaR) | a VaR shown without method/N/confidence disclosure | mitigate | Task 2.1: the ok state asserts the FULL methodology line (method · N · 95% · not a forecast) renders whenever a VaR renders; single-sourced via methodologyLine. |
| T-26-11 | Misrepresentation (wrong attribution / wrong N) | a scenario-side absence blamed on BTC, or the wrong N disclosed | mitigate | Task 1 fixed guard order (#509) + Task 2.3/2.4 attribution tests; two captions when varN ≠ betaN, each naming its true N. |
| T-26-12 | Information disclosure (sandbox leak) | stress/VaR leaking into the example-universe Sandbox (out of scope v1) | mitigate | Task 3 mounts in the own-book composer ONLY; acceptance criterion asserts ScenarioBuilder.tsx is NOT in the diff. |
| T-26-13 | Tampering (input validation) | an untrusted/non-finite shock magnitude | accept | Closed preset domain (SegmentedControl, 3 fixed magnitudes); no free-text input added (custom-magnitude deferred). If a custom input is ever added, it must clamp + reject non-finite per RESEARCH §V5 (mirror handleLeverageChange) — out of scope this plan. |
| T-26-SC | Tampering (supply chain) | npm/pip/cargo installs | accept | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit not applicable; CONTEXT locks "no new dependency"). Any install attempt is a scope violation to escalate. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/StressVarSection.test.tsx"` is fully green (state matrix + em-dash + monochrome + disclosure + floor SoT).
- `npm run test` (full suite) is green, including the 26-01 lib tests, this section's tests, and the coverage gate (lines 82 / statements 80 / functions 74 / branches 72 per CLAUDE.md).
- `npx tsc --noEmit` reports no type errors referencing StressVarSection.tsx or ScenarioComposer.tsx.
- The composer edit is mount-only (import + one sibling Card), own-book composer ONLY; the Strategy Sandbox / ScenarioBuilder is untouched.
- No new dependency, no server code, no migration, no Python in this plan.
</verification>

<success_criteria>
- An allocator on the own-book Scenario composer sees a "Stress & VaR" section after the benchmark section, picks a BTC shock preset, and sees a β-propagated projected impact (STRESS-01) plus historical VaR(95%) + CVaR with full inline disclosure (STRESS-02).
- Below the Phase-22 floor, on a degenerate scenario, or on a BTC-unavailable fetch, the section renders the correct honest empty state (heading matches body) — never a fabricated number.
- Losses are monochrome honest data, every value em-dashes on null, the VaR is never bare, and the disclosure names the true N(s).
- Every honesty/correctness threat (T-26-07..12) has a passing falsifiable test.
</success_criteria>

<output>
Create `.planning/phases/26-stress-testing-var/26-02-SUMMARY.md` when done.
</output>
