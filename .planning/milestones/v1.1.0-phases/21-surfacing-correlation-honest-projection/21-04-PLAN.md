---
phase: 21-surfacing-correlation-honest-projection
plan: 04
type: execute
wave: 3
depends_on: ["21-02", "21-03"]
files_modified:
  - src/components/scenarios/ScenarioBuilder.tsx
  - src/components/scenarios/ScenarioBuilder.honesty.test.tsx
autonomous: true
requirements: [SURF-03, CORR-03, IMPACT-01, IMPACT-02]
must_haves:
  truths:
    - "The Strategy Sandbox surface shows an 'Example universe' badge so it is never confused with the own-book Scenario tab"
    - "The Sandbox projection shows the persistent 'PROJECTED — hypothetical, not your live book' badge"
    - "The Sandbox shows a coverage caveat naming N overlapping days AND the shortest-history strategy name"
    - "The Sandbox's correlation label reads 'Avg |ρ|' (reconciled with the composer/KPI strip)"
    - "No PercentileRankBadge renders on the Sandbox blend — the guard queries by data-testid and FAILS if one is wired in; a positive-control render proves the testid query is non-vacuous"
  artifacts:
    - path: "src/components/scenarios/ScenarioBuilder.tsx"
      provides: "Example universe + PROJECTED badges, coverage caveat, Avg |ρ| relabel"
      contains: "Example universe"
    - path: "src/components/scenarios/ScenarioBuilder.honesty.test.tsx"
      provides: "IMPACT-01 framing test + IMPACT-02 non-vacuous neuter guard for the Sandbox"
      contains: "percentile-rank-badge"
  key_links:
    - from: "src/components/scenarios/ScenarioBuilder.tsx coverage caveat"
      to: "shortestHistoryName + metrics.n"
      via: "shortestHistoryName(deAliased.strategies) for the caveat name"
      pattern: "shortestHistoryName"
    - from: "ScenarioBuilder.honesty.test.tsx neuter guard"
      to: "PercentileRankBadge ABSENT (by data-testid)"
      via: "queryByTestId('percentile-rank-badge') null on the Sandbox; a positive-control render proves the query matches a real badge"
      pattern: "percentile-rank-badge"
---

<objective>
Apply the honesty surfaces to the example-universe Strategy Sandbox (ScenarioBuilder): the "Example universe" badge (SURF-03 labeling), the persistent PROJECTED badge + coverage caveat (IMPACT-01), the "Avg |ρ|" relabel (CORR-03 reconciliation), and a NEW neuter-check test file replicating the composer's strengthened R3 guard for this surface (IMPACT-02).

Purpose: ScenarioBuilder already mounts the heatmap and computes its metrics, but it has no honesty framing, no "Example universe" badge, says "Avg |corr|" instead of "Avg |ρ|", and has NO test file at all. This plan brings the Sandbox to parity with the own-book composer's honesty contract and adds its first test file.
Output: two badges + a caveat + a relabel on the builder; a new `ScenarioBuilder.honesty.test.tsx` covering the framing AND the no-peer-ranking guard (by the data-testid Plan 03 adds to PercentileRankBadge).

THE ENGINE IS FROZEN — do NOT edit scenario.ts / scenario-dealias.ts. ScenarioBuilder already computes `metrics`/`strategyNames` (:201-210); consume them read-only. The `CorrelationHeatmap` mount at :435 already exists and was updated in Plan 02 — do NOT change its mount here beyond what the new prop shape requires for the caption (it is already passed correlationMatrix + strategyNames). The `data-testid="percentile-rank-badge"` on PercentileRankBadge is added by Plan 03 (this plan depends on 21-03); do NOT add it again — consume it read-only in the guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-RESEARCH.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-PATTERNS.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-UI-SPEC.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-02-SUMMARY.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-03-SUMMARY.md

<interfaces>
<!-- ScenarioBuilder.tsx (verified file:line) — inputs already present, read-only -->
- :201-204 — `metrics = useMemo(() => computeScenario(...), [...])` (has correlation_matrix, avg_pairwise_correlation, n).
- :206-210 — `strategyNames = useMemo(() => { for (const s of deAliased.strategies) out[s.id] = s.name; ... })`.
- :266-269 — the component's `return (` is at :266; :267 opens `<div className="mt-6 space-y-6">`; the FIRST inner block is the KPI strip `<div className="grid grid-cols-2 md:grid-cols-6 gap-3">` at :269. There is NO title block / heading at :266 — the page title lives in the PARENT `scenarios/page.tsx` `<PageHeader>` (do NOT touch it). The first `<h2>` in this file is the equity-curve heading at ~:293. The honesty badges go in a NEW header row inserted at the TOP of the `mt-6 space-y-6` container, BEFORE the KPI strip block at :269.
- :283-286 — `<MetricCard label="Avg |corr|" value={formatNumber(metrics.avg_pairwise_correlation)} />` → RELABEL to "Avg |ρ|".
- :425-439 — the "Pairwise correlation" <Card> with `<CorrelationHeatmap correlationMatrix={...} strategyNames={...} />` (already mounted; Plan 02 updated the component). Pass overlappingDays={metrics.n} + avgAbsCorrelation={metrics.avg_pairwise_correlation} to match the composer's single-source contract.

<!-- shortestHistoryName (created in Plan 02) -->
- `import { shortestHistoryName } from "@/lib/scenario-history"` — pass the de-aliased strategy set the builder holds.

<!-- PercentileRankBadge (CONFIRMED EXISTS — verified shape) -->
- src/components/strategy/PercentileRankBadge.tsx — the concrete peer panel to assert ABSENT.
- GROUND TRUTH: it renders a `<span>` (root) whose ONLY visible text is the metric label ("Sharpe", "Max DD", "Return", etc.) + ordinal suffix; "percentile" lives ONLY in a `title=` attribute (so `queryByText(/percentile/i)` matches nothing — vacuous), and the visible labels COLLIDE with the Sandbox's own MetricCards (so an ABSENT assertion keyed on them would false-fail). Plan 03 adds a render-only `data-testid="percentile-rank-badge"` to the root `<span>`; this plan's guard queries THAT testid (it exists by Wave 3 because this plan depends on 21-03).

<!-- ScenarioComposer.test.tsx strengthened R3 guard — the MODEL to replicate (read-only reference; in Plan 03's scope) -->
- The R3 guard asserts `queryByTestId("percentile-rank-badge")` is null on the projection AND renders `<PercentileRankBadge metric="sharpe" percentile={95} />` in isolation as a positive control (`getByTestId(...)` IS found). Replicate this STRUCTURE for ScenarioBuilder, keeping a positive control so an all-null tree can't pass vacuously.

<!-- UI-SPEC tokens -->
- "Example universe" badge: neutral-outline pill (same token family as PROJECTED), copy `Example universe` (uppercase). NOT accent (accent = verified/action — misleads on an illustrative surface), NOT <Badge>.
- PROJECTED badge: `inline-flex items-center rounded-sm border border-text-muted px-2 py-0.5 text-[10px] uppercase tracking-wide font-semibold text-text-muted`, copy `PROJECTED — hypothetical, not your live book`, plain text, no role="alert".
- Coverage caveat: `mt-2 text-[11px] text-text-muted`, copy `Projected from {N} overlapping days. Shortest history: {strategyName}. Not a forecast.`

<!-- NOTE: ScenarioBuilder has NO test file today (verified). The .honesty.test.tsx is net-new. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Example universe + PROJECTED badges, coverage caveat, Avg |ρ| relabel on the Sandbox (SURF-03, CORR-03, IMPACT-01)</name>
  <files>src/components/scenarios/ScenarioBuilder.tsx</files>
  <read_first>
    - src/components/scenarios/ScenarioBuilder.tsx (the file being modified — return/container :266-269, KPI strip :269, MetricCard "Avg |corr|" :283-286, equity-curve h2 :293, heatmap card :425-439, metrics/strategyNames :201-210)
    - src/lib/scenario-history.ts (shortestHistoryName from Plan 02)
    - .planning/phases/21-surfacing-correlation-honest-projection/21-02-SUMMARY.md (confirm CorrelationHeatmap prop names: overlappingDays / avgAbsCorrelation)
    - 21-UI-SPEC.md § "Component Contracts > 2, 4, 5" + § "Copywriting Contract" (Example universe / PROJECTED / caveat / Avg |ρ| strings)
    - 21-PATTERNS.md § "ScenarioBuilder.tsx" + § "Shared Patterns"
  </read_first>
  <action>
    Insert a NEW header row at the TOP of the `mt-6 space-y-6` container (i.e. directly inside the `<div className="mt-6 space-y-6">` at :267, BEFORE the KPI strip block at :269) holding (a) an "Example universe" neutral-outline pill (same token as PROJECTED: `inline-flex items-center rounded-sm border border-text-muted px-2 py-0.5 text-[10px] uppercase tracking-wide font-semibold text-text-muted`, copy `Example universe`) so the Sandbox is unmistakable vs the own-book Scenario tab (SURF-03 labeling), and (b) the persistent PROJECTED pill with copy `PROJECTED — hypothetical, not your live book` (always rendered, plain text, no role="alert", NOT bg-accent/warning/<Badge>). Do NOT touch the parent `scenarios/page.tsx` `<PageHeader>` (the page title) — these badges are a new row inside ScenarioBuilder's own container. Add a coverage caveat (`mt-2 text-[11px] text-text-muted`) with copy `Projected from {N} overlapping days. Shortest history: {strategyName}. Not a forecast.` using `N = metrics.n` and `strategyName = shortestHistoryName(<the builder's de-aliased strategies>)` (import from @/lib/scenario-history). Relabel the MetricCard at :283-286 from `label="Avg |corr|"` to `label="Avg |ρ|"` (reconcile with the composer/KPI strip — one definition across the surface; value unchanged). On the existing heatmap mount at :435, add `overlappingDays={metrics.n}` and `avgAbsCorrelation={metrics.avg_pairwise_correlation}` to match the single-source caption contract from Plan 02. Give the badges/caveat stable test ids (e.g. `sandbox-example-universe-badge`, `scenario-projected-badge`, `scenario-coverage-caveat`). Do NOT touch scenario.ts/scenario-dealias.ts or refactor the builder's data path.
  </action>
  <verify>
    <automated>npx vitest run "src/components/scenarios/ScenarioBuilder.honesty.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - ScenarioBuilder.tsx renders the literal text "Example universe" and "PROJECTED — hypothetical, not your live book" in a new header row at the top of the `mt-6 space-y-6` container (source assertion: both literals present; the parent PageHeader is untouched)
    - The MetricCard label is "Avg |ρ|" (source assertion: `grep "Avg |ρ|"` present; `"Avg |corr|"` absent)
    - The coverage caveat uses `metrics.n` and `shortestHistoryName` (imported from @/lib/scenario-history)
    - The heatmap mount passes overlappingDays={metrics.n} and avgAbsCorrelation={metrics.avg_pairwise_correlation}
    - No bg-accent/warning/<Badge>/role="alert" on the honesty pills; no diff to scenario.ts/scenario-dealias.ts or scenarios/page.tsx
  </acceptance_criteria>
  <done>SURF-03 labeling + IMPACT-01 framing + CORR-03 relabel are live on the Strategy Sandbox, at parity with the composer.</done>
</task>

<task type="auto">
  <name>Task 2: New ScenarioBuilder honesty + non-vacuous neuter-guard test file (IMPACT-01, IMPACT-02)</name>
  <files>src/components/scenarios/ScenarioBuilder.honesty.test.tsx</files>
  <read_first>
    - src/components/scenarios/ScenarioBuilder.tsx (read the component props/usage so the new test renders it correctly — there is NO existing builder test to copy setup from)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (the strengthened R3 guard MODEL — `queryByTestId("percentile-rank-badge")` null + isolated positive-control render; replicate this structure)
    - src/components/strategy/PercentileRankBadge.tsx (read-only — it carries `data-testid="percentile-rank-badge"` on its root span as of Plan 03; the guard targets THAT testid, NOT a colliding visible label and NOT /percentile/i)
    - 21-RESEARCH.md § "Validation Architecture > Wave 0 Gaps" + 21-PATTERNS.md § "Neuter-check / ABSENT guard (IMPACT-02)"
  </read_first>
  <action>
    Create `src/components/scenarios/ScenarioBuilder.honesty.test.tsx` — the FIRST test file for this component. Build minimal render fixtures (a ≥2-strategy scenario with ≥10 overlapping days; mock or stub the same dependencies the composer test stubs, e.g. heavy children, so the render is deterministic). Add: (1) an IMPACT-01 framing test asserting the "Example universe" badge, the "PROJECTED — hypothetical, not your live book" badge (always present), and the coverage caveat (`Projected from {N} overlapping days. Shortest history: {name}.`) all render; (2) a CORR-03 test asserting the correlation label reads "Avg |ρ|" (not "Avg |corr|"); (3) an IMPACT-02 neuter guard REPLICATING the composer's strengthened structure. The ABSENT assertion MUST be `expect(screen.queryByTestId("percentile-rank-badge")).toBeNull()` on the rendered Sandbox — query by the render-only data-testid Plan 03 added (NOT `queryByText(/percentile/i)`, which matches nothing because "percentile" is only in a `title` attr, and NOT a visible label like "Sharpe", which collides with the Sandbox's own MetricCards). Keep the `/ranked against peers/i` ABSENT assertion. ADD a REQUIRED positive-control sub-step in the SAME spec: render `<PercentileRankBadge metric="sharpe" percentile={95} />` in isolation and assert `screen.getByTestId("percentile-rank-badge")` IS found — proving the testid query is non-vacuous (it would catch the badge if it were ever wired into the Sandbox projection). Also keep a surface-level positive control (a known rendered marker present) so an all-null tree can't pass vacuously. The neuter test must FAIL if a peer panel is ever wired into the Sandbox projection.
  </action>
  <verify>
    <automated>npx vitest run "src/components/scenarios/ScenarioBuilder.honesty.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The new test file exists and renders ScenarioBuilder
    - Framing test asserts "Example universe", the PROJECTED badge, and the coverage caveat are present
    - CORR-03 test asserts "Avg |ρ|" label (and that "Avg |corr|" is gone)
    - The neuter guard asserts `screen.queryByTestId("percentile-rank-badge")` is null on the Sandbox (NOT queryByText on /percentile/i or a colliding label) plus `/ranked against peers/i` absent
    - The SAME spec renders `<PercentileRankBadge metric="sharpe" percentile={95} />` in isolation and asserts `getByTestId("percentile-rank-badge")` IS found (positive control proving the query matches a real badge), and a surface-level positive control is retained
  </acceptance_criteria>
  <done>IMPACT-02 is locked on the Sandbox too: the guard fails if a PercentileRankBadge is rendered on the Sandbox blend, and the positive-control proves the testid query matches a real badge (non-vacuous). The Sandbox now has its first regression test covering the honesty contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example-universe blend → Sandbox projection render | Same honesty boundary as the composer: an illustrative what-if blend must never be peer-ranked / percentile-scored, and must be visibly labeled as an example universe (not the allocator's live book). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | Tampering (data-integrity / honesty) | Peer/percentile panel on the Sandbox blend | mitigate | NEW neuter guard in ScenarioBuilder.honesty.test.tsx replicating the composer's strengthened structure (`queryByTestId("percentile-rank-badge")` null + isolated positive-control render). Fails if a peer panel is wired in; positive control proves non-vacuity. |
| T-21-11 | Spoofing (surface confusion) | Sandbox mistaken for own-book scenario | mitigate | Persistent "Example universe" + PROJECTED neutral-outline badges make the illustrative, hypothetical nature unmistakable (SURF-03 labeling + IMPACT-01). |
| T-21-12 | Tampering (false precision) | Coverage caveat / Avg |ρ| numbers | mitigate | N from metrics.n; shortest name from unit-tested shortestHistoryName; Avg |ρ| single-sourced from metrics.avg_pairwise_correlation. No invented numbers. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (RESEARCH Package Legitimacy Audit). |

> The Sandbox route's V4 access-control boundary (allocator-only) lives in the unchanged server gate at scenarios/page.tsx:50-52 (test-pinned by page.role-gate.test.ts) and the Plan-01 sidebar gate. This plan does NOT touch the route and must not weaken it.
</threat_model>

<verification>
- `npx vitest run "src/components/scenarios/ScenarioBuilder.honesty.test.tsx"` green.
- After Wave 3: `npm run test` across `src/components/scenarios/**` + `src/components/portfolio/**` + `src/app/(dashboard)/allocations/**` green; then `npm run test:coverage` (blocking gate) before /gsd:verify-work.
- No diff to scenario.ts / scenario-dealias.ts or scenarios/page.tsx.
</verification>

<success_criteria>
- SURF-03 labeling: "Example universe" badge on the Sandbox (new header row, parent PageHeader untouched).
- IMPACT-01: persistent PROJECTED badge + coverage caveat (N + shortest-history name) on the Sandbox.
- CORR-03: Sandbox label reconciled to "Avg |ρ|".
- IMPACT-02: new non-vacuous neuter guard for the Sandbox (PercentileRankBadge ABSENT by data-testid, isolated positive control present).
- Engine + route untouched.
</success_criteria>

<output>
Create `.planning/phases/21-surfacing-correlation-honest-projection/21-04-SUMMARY.md` when done.
</output>
</content>
</invoke>
