---
phase: 21-surfacing-correlation-honest-projection
plan: 03
type: execute
wave: 2
depends_on: ["21-02"]
files_modified:
  - src/components/strategy/PercentileRankBadge.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [CORR-01, CORR-03, IMPACT-01, IMPACT-02]
must_haves:
  truths:
    - "The own-book Scenario tab renders a pairwise correlation heatmap labeled by de-aliased strategy name when ≥2 strategies share ≥10 overlapping days"
    - "The composer projection shows a persistent 'PROJECTED — hypothetical, not your live book' badge (always visible, not a tooltip)"
    - "The composer shows a coverage caveat naming N overlapping days AND the shortest-history strategy name"
    - "The heatmap caption and the KPI strip read the SAME Avg |ρ| value (single source)"
    - "No PercentileRankBadge renders on the hypothetical blend — the guard queries by data-testid and FAILS if a badge were wired in; a positive-control render proves the testid query is non-vacuous"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "CorrelationHeatmap mount + PROJECTED badge + coverage caveat + single-sourced Avg |ρ|"
      contains: "CorrelationHeatmap"
    - path: "src/components/strategy/PercentileRankBadge.tsx"
      provides: "render-only data-testid='percentile-rank-badge' on the root span (signal for the ABSENT guard)"
      contains: "percentile-rank-badge"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "CorrelationHeatmap"
      via: "import + mount with correlationMatrix={scenarioMetrics.correlation_matrix}, strategyNames, overlappingDays={scenarioMetrics.n}, avgAbsCorrelation={scenarioMetrics.avg_pairwise_correlation}"
      pattern: "CorrelationHeatmap"
    - from: "ScenarioComposer coverage caveat"
      to: "shortestHistoryName + scenarioMetrics.n"
      via: "shortestHistoryName(deAliased.strategies) for the caveat name"
      pattern: "shortestHistoryName"
    - from: "ScenarioComposer.test.tsx R3 guard"
      to: "PercentileRankBadge ABSENT (by data-testid)"
      via: "queryByTestId('percentile-rank-badge') is null on the projection; a positive-control render proves the query matches a real badge"
      pattern: "percentile-rank-badge"
---

<objective>
Wire the own-book ScenarioComposer to (a) mount the (Plan-02-updated) `CorrelationHeatmap` with de-aliased labels and the single-sourced Avg |ρ| value (CORR-01, CORR-03 value half), (b) render the persistent PROJECTED badge + coverage caveat (IMPACT-01), and (c) strengthen the existing R3 neuter guard to assert `PercentileRankBadge` is absent BY A NON-VACUOUS TESTID QUERY (IMPACT-02).

Purpose: The composer is the own-book scenario surface. It already computes `deAliased` + `scenarioMetrics` but renders NO heatmap today (verified: zero `CorrelationHeatmap` import). This plan mounts the heatmap, adds the two honesty surfaces, and locks the no-peer-ranking invariant with a falsifiable guard. This guard is the phase's headline honesty lock — it MUST be falsifiable.
Output: composer heatmap mount; PROJECTED badge + caveat; single-sourced caption/strip value; a render-only `data-testid` on PercentileRankBadge; a strengthened, non-vacuous neuter guard.

THE ENGINE IS FROZEN — do NOT edit scenario.ts / scenario-dealias.ts. `scenarioMetrics` (correlation_matrix, avg_pairwise_correlation, n) and `deAliased.strategies` are already computed in this file (:575-591); consume them read-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-RESEARCH.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-PATTERNS.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-UI-SPEC.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-02-SUMMARY.md

<interfaces>
<!-- ScenarioComposer.tsx (verified file:line) — inputs already present, read-only -->
- :575-591 — `deAliased = useMemo(() => collapseAliasedHoldingStrategies(...), [...])` and `scenarioMetrics = useMemo(() => computeScenario(deAliased.strategies, deAliased.state, dateMapCache), [...])`. `scenarioMetrics.correlation_matrix | .avg_pairwise_correlation | .n` are ready to pass.
- :995 — `<h2 className="text-2xl font-semibold text-text-primary">Scenario</h2>` (header anchor for the PROJECTED badge).
- :1036-1047 — `<KpiStrip mode="scenario" ... />` (already wired; the Avg |ρ| value flows here).
- :1050-1061 — existing caveat slot: `{leverageApplied && (<p data-testid="scenario-leverage-caveat" className="mt-2 text-[11px] text-text-muted">...)}` — reuse this typography/region for the coverage caveat.
- :1063 — `lg:grid-cols-2` charts region (EquityChart/DrawdownChart) — mount the heatmap here as a full-width row or a new <Card>.

<!-- CorrelationHeatmap (UPDATED by Plan 02) — consume its new prop shape -->
- Props: `correlationMatrix`, `strategyNames`, optional `overlappingDays?: number`, optional `avgAbsCorrelation?: number` (confirm exact names against 21-02-SUMMARY.md).

<!-- ScenarioBuilder.tsx (verified) — the mount analog to COPY (do NOT edit it here; Plan 04 owns it) -->
- :206-210 strategyNames map: `for (const s of deAliased.strategies) out[s.id] = s.name`.
- :435-438 consumer: `<CorrelationHeatmap correlationMatrix={...} strategyNames={...} />` wrapped in a "Pairwise correlation" <Card>.

<!-- ScenarioComposer.test.tsx (verified) — the R3 guard to EXTEND -->
- :87 — `KpiStrip: vi.fn(() => <div data-testid="kpi-strip-mock" />)` (positive control).
- :2163 — `it("R3 guard — the projection renders NO peer/allocator/comparator factsheet panels ...")`; :2173 asserts kpi-strip-mock present; :2182 factsheet-allocator null; :2184 queryByText(/percentile/i) null.

<!-- shortestHistoryName (created in Plan 02) -->
- `import { shortestHistoryName } from "@/lib/scenario-history"` — pass `deAliased.strategies`.

<!-- PercentileRankBadge (CONFIRMED EXISTS — verified shape) -->
- src/components/strategy/PercentileRankBadge.tsx — the concrete peer panel to assert ABSENT.
- GROUND TRUTH: it renders a `<span>` (root) whose ONLY visible text is the metric label ("Sharpe", "Max DD", "Return", "CAGR", "Sortino", "Calmar", "Volatility") + an ordinal suffix. The word "percentile" appears ONLY in a `title=` attribute — Testing Library's `queryByText`/`queryByTitle`-on-text does NOT match a `title` attribute, so `queryByText(/percentile/i)` would pass VACUOUSLY even if a real badge were wired in. The visible labels ("Sharpe", "Max DD", etc.) COLLIDE with strings already on the scenario surface (KPI strip / MetricCards), so an ABSENT assertion keyed on those would FALSE-FAIL on the honest tree. The component has NO data-testid today. This plan adds a render-only `data-testid="percentile-rank-badge"` to the root `<span>` so the guard has a unique, non-colliding signal.

<!-- UI-SPEC tokens -->
- PROJECTED badge: `inline-flex items-center rounded-sm border border-text-muted px-2 py-0.5 text-[10px] uppercase tracking-wide font-semibold text-text-muted` — NEUTRAL outline (NOT bg-accent, NOT warning-amber, NOT <Badge>). Plain text, no role="alert".
- PROJECTED copy: `PROJECTED — hypothetical, not your live book`.
- Coverage caveat copy: `Projected from {N} overlapping days. Shortest history: {strategyName}. Not a forecast.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount CorrelationHeatmap with de-aliased labels + single-sourced Avg |ρ| (CORR-01, CORR-03)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the file being modified — read deAliased/scenarioMetrics :575-591, KpiStrip mount :1036-1047, charts region :1063)
    - src/components/scenarios/ScenarioBuilder.tsx (the mount ANALOG — strategyNames :206-210, heatmap consumer :425-439; copy this pattern, do NOT edit this file)
    - .planning/phases/21-surfacing-correlation-honest-projection/21-02-SUMMARY.md (confirm the CorrelationHeatmap prop names: overlappingDays / avgAbsCorrelation)
    - 21-PATTERNS.md § "ScenarioComposer.tsx" (NET-NEW mount; Pitfall 2) + 21-UI-SPEC.md § "Component Contracts > 3"
  </read_first>
  <behavior>
    - With ≥2 active de-aliased strategies sharing ≥10 overlapping days, the composer renders a CorrelationHeatmap whose axis labels are the de-aliased strategy names.
    - With <2 strategies OR <10 overlapping days, the heatmap region renders the reason-named empty state (delegated to the heatmap via overlappingDays), never a 1×1 grid.
    - The Avg |ρ| value shown in the heatmap caption equals the value the KpiStrip shows (single source: scenarioMetrics.avg_pairwise_correlation).
  </behavior>
  <action>
    Import `CorrelationHeatmap` and build a `strategyNames` map exactly like ScenarioBuilder.tsx:206-210 (`for (const s of deAliased.strategies) out[s.id] = s.name`) in a useMemo keyed on `deAliased.strategies`. Mount `<CorrelationHeatmap correlationMatrix={scenarioMetrics.correlation_matrix} strategyNames={strategyNames} overlappingDays={scenarioMetrics.n} avgAbsCorrelation={scenarioMetrics.avg_pairwise_correlation} />` (use the exact prop names from 21-02-SUMMARY.md) in the charts region near :1063, wrapped in a <Card> mirroring the builder's "Pairwise correlation" card. Pass `scenarioMetrics.avg_pairwise_correlation` as the single source so the caption and the already-wired KpiStrip (:1036) read the same number — do NOT compute a second average. Do NOT touch scenario.ts/scenario-dealias.ts. Extend ScenarioComposer.test.tsx with: a ≥2-strategy case asserting the heatmap renders with de-aliased labels; a <2-strategy (or <10-day) case asserting the empty state (no 1×1 grid); and a single-source assertion that the caption value and the strip value agree.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - ScenarioComposer.tsx imports and mounts CorrelationHeatmap (source assertion: `grep -v '^\s*//' src/.../ScenarioComposer.tsx | grep -c CorrelationHeatmap` >= 2 — import + JSX)
    - The heatmap is passed strategyNames built from deAliased.strategies[].name and overlappingDays={scenarioMetrics.n}
    - A test asserts ≥2 de-aliased labels render in the composer heatmap
    - A test asserts the caption Avg |ρ| value === the value passed to KpiStrip (single source)
    - No diff to scenario.ts / scenario-dealias.ts
  </acceptance_criteria>
  <done>CORR-01 (composer mount) + CORR-03 (single-sourced value) satisfied on the own-book surface.</done>
</task>

<task type="auto">
  <name>Task 2: PROJECTED badge + coverage caveat on the composer projection (IMPACT-01)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (header :995, leverage-caveat slot :1050-1061)
    - src/lib/scenario-history.ts (the shortestHistoryName helper from Plan 02)
    - 21-UI-SPEC.md § "Component Contracts > 4" (neutral-outline pill, NOT bg-accent/warning/<Badge>; ARIA: informative, no role="alert") + § "Copywriting Contract" (exact PROJECTED + caveat strings)
    - 21-PATTERNS.md § "Shared Patterns > PROJECTED honesty badge / Coverage caveat slot"
  </read_first>
  <action>
    Next to/under the "Scenario" `<h2>` at :995, render a persistent PROJECTED pill using the EXACT neutral-outline token from UI-SPEC §4 (`inline-flex items-center rounded-sm border border-text-muted px-2 py-0.5 text-[10px] uppercase tracking-wide font-semibold text-text-muted`) with the literal copy `PROJECTED — hypothetical, not your live book`. It must be always-rendered (not a tooltip/hover), plain text, NO role="alert", and must NOT use `bg-accent`, warning-amber, or the filled `<Badge>` primitive. Add a coverage caveat reusing the :1053 caveat typography (`mt-2 text-[11px] text-text-muted`) with copy `Projected from {N} overlapping days. Shortest history: {strategyName}. Not a forecast.` — substitute `N = scenarioMetrics.n` and `strategyName = shortestHistoryName(deAliased.strategies)` (import from @/lib/scenario-history). Give the badge and caveat stable test ids (e.g. `data-testid="scenario-projected-badge"` / `scenario-coverage-caveat`). Extend ScenarioComposer.test.tsx to assert the PROJECTED badge text is present (always, including when no leverage is applied) and the caveat renders the live N + a strategy name.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The composer renders text "PROJECTED — hypothetical, not your live book" unconditionally (test asserts presence even with no leverage)
    - The PROJECTED element uses `border-text-muted text-text-muted` and does NOT use `bg-accent`, `role="alert"`, warning tokens, or `<Badge>`
    - The coverage caveat renders `Projected from {N} overlapping days. Shortest history: {name}. Not a forecast.` with live N from scenarioMetrics.n and name from shortestHistoryName
    - shortestHistoryName is imported from @/lib/scenario-history (no re-implemented helper)
  </acceptance_criteria>
  <done>IMPACT-01 satisfied on the own-book composer: persistent honest framing + coverage caveat.</done>
</task>

<task type="auto">
  <name>Task 3: Add a render-only data-testid to PercentileRankBadge + strengthen the R3 neuter guard with a non-vacuous ABSENT assertion (IMPACT-02)</name>
  <files>src/components/strategy/PercentileRankBadge.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/components/strategy/PercentileRankBadge.tsx (the component to tag — its root is a `<span>`; "percentile" lives ONLY in `title=`; visible text is the metric label + ordinal suffix; NO data-testid today)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (the EXISTING R3 guard :2163-2186 — positive control kpi-strip-mock :2173, factsheet-allocator null :2182, /percentile/i null :2184)
    - 21-RESEARCH.md § "Code Examples > Neuter-check model" + 21-PATTERNS.md § "Neuter-check / ABSENT guard (IMPACT-02)"
  </read_first>
  <action>
    PREREQUISITE (one-line, non-behavioral source change): add `data-testid="percentile-rank-badge"` to the ROOT `<span>` of `PercentileRankBadge` in src/components/strategy/PercentileRankBadge.tsx. This is render-only — it adds NO behavior, changes NO existing className/title/text, and gives the guard a unique signal that does NOT collide with the scenario surface's visible strings. Do NOT change anything else in that component.

    Then extend the existing R3 guard test (ScenarioComposer.test.tsx:2163-2186). The ABSENT assertion for the peer badge MUST be `expect(screen.queryByTestId("percentile-rank-badge")).toBeNull()` — query by data-testid, NOT `queryByText(/percentile/i)` (which matches nothing because "percentile" is only in a `title` attribute) and NOT a visible label like "Sharpe" (which collides with the honest KPI strip / MetricCards on the surface). Keep the other existing ABSENT assertions (factsheet-allocator, factsheet-signatures, /ranked against peers/i) and the positive control (kpi-strip-mock present). ADD a REQUIRED positive-control sub-step in the SAME spec: render `<PercentileRankBadge metric="sharpe" percentile={95} />` in isolation (its own `render(...)`) and assert `screen.getByTestId("percentile-rank-badge")` IS found — this proves the testid query is non-vacuous (it would catch the badge if it were ever wired into the projection). Do NOT weaken any existing assertion. The composer change here is the testid; everything else is test-only.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - PercentileRankBadge.tsx root `<span>` carries `data-testid="percentile-rank-badge"` (render-only; no other change)
    - The R3 guard asserts `screen.queryByTestId("percentile-rank-badge")` is null on the composer projection (NOT queryByText on /percentile/i or a colliding label)
    - The SAME spec renders `<PercentileRankBadge metric="sharpe" percentile={95} />` in isolation and asserts `getByTestId("percentile-rank-badge")` IS found (positive control proving the query matches a real badge)
    - The kpi-strip-mock positive control and all prior R3 ABSENT assertions remain (no weakening)
  </acceptance_criteria>
  <done>IMPACT-02 hardened on the composer: the no-peer-ranking guard fails if a PercentileRankBadge is rendered on the hypothetical blend, and the positive-control proves the testid query matches a real badge (non-vacuous).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hypothetical blend → projection render | The honesty concern: a what-if blend must never be presented as peer-ranked / percentile-scored (false precision). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-07 | Tampering (data-integrity / honesty) | Peer/percentile panel on a hypothetical blend | mitigate | Strengthen the R3 neuter guard to assert PercentileRankBadge ABSENT by a unique render-only data-testid (Task 3), with a positive-control render proving the query is non-vacuous. The composer builds from scenarioMetrics + KpiStrip, never FactsheetBody (which hardcodes ingestSource:"api"). The test fails if a peer panel is wired in. |
| T-21-08 | Tampering (false precision) | Coverage caveat / Avg |ρ| double-compute | mitigate | Caveat N comes from scenarioMetrics.n; shortest name from the unit-tested shortestHistoryName; Avg |ρ| is single-sourced from scenarioMetrics.avg_pairwise_correlation (no second average). No invented numbers. |
| T-21-09 | Tampering (data-integrity) | Fabricated ρ=1.0 (venue aliases) | accept (mitigated upstream) | deAliased already collapses aliases before computeScenario (frozen). Heatmap inherits the honest matrix. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (RESEARCH Package Legitimacy Audit). |

> V4 Access Control: N/A for this plan — no role surface here. The own-book composer is gated by the dashboard layout; this plan adds no auth-relevant code (the data-testid is render-only).
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` green (mount + badge + caveat + strengthened R3 guard with positive control).
- No diff to scenario.ts / scenario-dealias.ts.
- After Wave 2: `npm run test` across `src/app/(dashboard)/allocations/**` + `src/components/portfolio/**` green.
</verification>

<success_criteria>
- CORR-01: own-book composer mounts the heatmap with de-aliased labels; empty state delegated honestly.
- CORR-03 (value): caption and KpiStrip read one single-sourced Avg |ρ|.
- IMPACT-01: persistent PROJECTED badge + coverage caveat (N + shortest-history name) on the composer.
- IMPACT-02: R3 guard strengthened — PercentileRankBadge ABSENT by data-testid, with a positive control proving non-vacuity.
- Engine untouched.
</success_criteria>

<output>
Create `.planning/phases/21-surfacing-correlation-honest-projection/21-03-SUMMARY.md` when done.
</output>
</content>
</invoke>
