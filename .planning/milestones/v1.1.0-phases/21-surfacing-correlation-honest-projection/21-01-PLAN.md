---
phase: 21-surfacing-correlation-honest-projection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Sidebar.test.tsx
autonomous: true
requirements: [SURF-01, SURF-02, SURF-03]
must_haves:
  truths:
    - "An allocator sees a visible 'Scenario' tab in the dashboard tablist and can reach it by click AND by keyboard arrow-nav"
    - "The ?tab=scenario deep-link still resolves to the Scenario tab (no regression)"
    - "An allocator sees a 'Strategy Sandbox' link in the sidebar MY WORKSPACE group, below 'My Allocation'"
    - "A manager-only OR admin-only user sees NO 'Strategy Sandbox' link"
  artifacts:
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      provides: "scenario added to VISIBLE_TAB_KEYS"
      contains: "VISIBLE_TAB_KEYS"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "allocator-only Strategy Sandbox nav item + BeakerIcon"
      contains: "Strategy Sandbox"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "isAllocator boolean (NOT showsAllocatorWorkspace)"
      via: "if (isAllocator) workspaceItems.push Sandbox"
      pattern: "if \\(isAllocator\\)"
    - from: "src/app/(dashboard)/allocations/AllocationsTabs.tsx keyboard nav"
      to: "VISIBLE_TAB_KEYS"
      via: "keyboardKeys derives from VISIBLE_TAB_KEYS"
      pattern: "keyboardKeys"
---

<objective>
Make the scenario surfaces reachable: add the visible "Scenario" tab to the own-book dashboard tablist (SURF-01) and add an allocator-only "Strategy Sandbox" link to the sidebar (SURF-02/03).

Purpose: The scenario engine is shipped but the own-book Scenario tab is reachable today only via `?tab=scenario` or the "+ Allocation" button, and the example-universe Strategy Sandbox has no sidebar entry. This plan surfaces both, with the Sandbox gated to allocators only (managers and admins see nothing).
Output: One visible tab wired into `VISIBLE_TAB_KEYS`; one role-gated sidebar nav item + a new inline `BeakerIcon`; extended tests in two existing spec files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-RESEARCH.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-PATTERNS.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-UI-SPEC.md

<interfaces>
<!-- AllocationsTabs.tsx (verified file:line in 21-RESEARCH.md / 21-PATTERNS.md) -->
- `VISIBLE_TAB_KEYS: readonly TabKey[]` at :246 currently = ["overview","holdings","outcomes","mandate","risk"]. `"scenario"` is ALREADY a TabKey (:233-239), TAB_LABELS.scenario="Scenario" (:304), parseTab returns "scenario" (:288), KNOWN_TAB_RAW spreads VISIBLE_TAB_KEYS + "scenario" (:270-274), tabpanel aria-controls="panel-scenario" already wired.
- `keyboardKeys` (:440-446) derives from VISIBLE_TAB_KEYS (filters out "outcomes" only when outcomesHidden). The :427-429 comment claims keyboard nav excludes scenario — that comment becomes stale.
- TAB_BUTTON_ACTIVE / TAB_BUTTON_INACTIVE (:312-315) and the render loop over VISIBLE_TAB_KEYS (:554) auto-apply styling/button — no new style.
- "+ Allocation" header button (:632) routes via changeTab("scenario") — must NOT regress.

<!-- Sidebar.tsx (verified) -->
- `buildNavSections(..., isAdmin?, isAllocator?, ..., isManager?)` at :19; `Sidebar({ isAdmin, isAllocator, isManager, populatedSlugs })` at :108.
- `showsAllocatorWorkspace = isAllocator || isAdmin` (:34) — DO NOT use this for the Sandbox (admins must NOT see it).
- workspaceItems push pattern (:58-72): "My Allocation" pushed under showsAllocatorWorkspace; "Strategies"/"Portfolios" under showsManagerWorkspace.
- Icon factory (:247-316): all icons are `({ className }: { className?: string }) => <svg viewBox="0 0 16 16" fill="none" stroke="currentColor" strokeWidth="1.5" strokeLinecap="round" ...>`.

<!-- Sidebar.test.tsx ALREADY EXISTS (32 it/describe blocks) — research's "Wave 0 no Sidebar test" claim is WRONG. -->
- Renders `<Sidebar populatedSlugs={[]} isAllocator={true} />`, `... isAllocator={false} isManager={true} />`, admin view. Asserts allocator sees "My Allocation" but NOT "Strategies"/"Portfolios"; managers see Strategies+Portfolios.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add visible Scenario tab to the dashboard tablist (SURF-01)</name>
  <files>src/app/(dashboard)/allocations/AllocationsTabs.tsx, src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx (the file being modified — read VISIBLE_TAB_KEYS:246, the :427-429 keyboard-nav comment, keyboardKeys:440-446, render loop:554, "+ Allocation":632)
    - 21-PATTERNS.md § "AllocationsTabs.tsx (SURF-01)" (the analog + the "single edit suffices" confirmations)
  </read_first>
  <action>
    Add the string literal `"scenario"` to the `VISIBLE_TAB_KEYS` array at AllocationsTabs.tsx:246 (after "risk"). This is the only wiring edit needed — `scenario` is already a full TabKey with label, parse-case, ARIA panel, and the render loop + keyboardKeys both derive from VISIBLE_TAB_KEYS, so the visible button AND arrow-nav reach come for free. Do NOT add new style constants (reuse TAB_BUTTON_ACTIVE/INACTIVE) and do NOT touch parseTab, TAB_LABELS, KNOWN_TAB_RAW, or the tabpanel wiring. Update the now-stale comment at :427-429 (which states keyboard nav excludes scenario) so it no longer contradicts the new behavior — per the project's "clean up your own mess" rule; do not rewrite unrelated comments. Do NOT regress the "+ Allocation" changeTab("scenario") deep-link at :632 or the `?tab=scenario` parseTab case.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - VISIBLE_TAB_KEYS contains "scenario" (source assertion: grep `"scenario"` in the VISIBLE_TAB_KEYS literal)
    - A new/extended test in AllocationsTabs.scenario-composer.test.tsx asserts a button/tab with the accessible name "Scenario" is rendered in the tablist, and that keyboard arrow-nav reaches it (keyboardKeys includes "scenario")
    - A test asserts `?tab=scenario` (or activeTab="scenario") still resolves to the Scenario panel — deep-link not regressed
    - The :427-429 comment no longer claims scenario is excluded from keyboard nav
  </acceptance_criteria>
  <done>SURF-01 satisfied: the Scenario tab is visible in the strip, click- and keyboard-reachable, and the deep-link still works.</done>
</task>

<task type="auto">
  <name>Task 2: Add allocator-only Strategy Sandbox sidebar link + BeakerIcon (SURF-02/03)</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/Sidebar.test.tsx</files>
  <read_first>
    - src/components/layout/Sidebar.tsx (the file being modified — read buildNavSections:19, role booleans:34, workspaceItems push:58-72, icon factory:247-316)
    - src/components/layout/Sidebar.test.tsx (ALREADY EXISTS — the spec to EXTEND; read the allocator/manager/admin render cases)
    - 21-PATTERNS.md § "Sidebar.tsx (SURF-02 / SURF-03)" (the push-on-boolean analog + icon factory shape + the isAllocator-ONLY gate rationale)
    - 21-UI-SPEC.md § "Component Contracts > 2. Strategy Sandbox sidebar link" (label, position, neutral icon convention)
  </read_first>
  <action>
    In Sidebar.tsx, define a new `BeakerIcon({ className })` following the inline-SVG icon factory pattern at :247-316 (16px viewBox, 1.5px stroke, currentColor, strokeLinecap/Linejoin round) — a lab-flask/beaker outline (no icon dependency). Then push a NavItem `{ label: "Strategy Sandbox", href: "/scenarios", icon: BeakerIcon }` onto `workspaceItems`, directly UNDER the "My Allocation" push, gated on `if (isAllocator)` ONLY — NOT `showsAllocatorWorkspace` (which is `isAllocator || isAdmin`); per CONTEXT D-locked decision managers AND admins must see no Sandbox entry. Use the literal label "Strategy Sandbox" and href "/scenarios" per the Copywriting Contract. Reuse the existing NavItemLink render (no new active/hover styling). Document inline (one short comment) that the sidebar hide is defense-in-depth and the real security boundary is the server gate at scenarios/page.tsx:50-52 (do NOT touch that route — it already redirects non-allocators including admin-only). Then EXTEND src/components/layout/Sidebar.test.tsx with three cases: allocator (`isAllocator={true}`) SEES "Strategy Sandbox"; manager-only (`isAllocator={false} isManager={true}`) does NOT; admin-only (`isAdmin={true}` with isAllocator falsy) does NOT.
  </action>
  <verify>
    <automated>npx vitest run "src/components/layout/Sidebar.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Sidebar.tsx contains a "Strategy Sandbox" NavItem pushed under `if (isAllocator)` (source assertion: the push is inside an `if (isAllocator)` block, NOT `if (showsAllocatorWorkspace)`)
    - A new `BeakerIcon` matches the icon factory shape (16px viewBox, stroke="currentColor", strokeWidth="1.5")
    - Sidebar.test.tsx asserts: allocator SEES "Strategy Sandbox"; manager-only does NOT (`queryByText("Strategy Sandbox")` is null); admin-only does NOT
    - No change to src/app/(dashboard)/scenarios/page.tsx (the server gate is the boundary, unchanged)
  </acceptance_criteria>
  <done>SURF-02/03 satisfied: allocators (and only allocators) see a labeled Strategy Sandbox link; the server route gate is untouched and remains the security boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → `/scenarios` RSC route | A non-allocator (manager / admin-only) could direct-navigate to `/scenarios`, which uses `createAdminClient()` to read the RLS-bypassed institutional strategy universe. The sidebar link is NOT this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Information Disclosure | Sidebar Sandbox link visibility | mitigate | Gate the link on `isAllocator` ONLY (not `showsAllocatorWorkspace`); Sidebar.test.tsx asserts manager-only AND admin-only do not see it. This is defense-in-depth, NOT the control. |
| T-21-02 | Elevation of Privilege (apparent) | `/scenarios` route access | accept (already mitigated) | The real boundary is the server gate at scenarios/page.tsx:50-52 (`isAllocator = role IN ('allocator','both')` else `redirect("/")`), already test-pinned by page.role-gate.test.ts. This plan does NOT touch that route; it must not weaken it. No new code needed — existing control. |
| T-21-03 | Spoofing/Tampering | URL tab value (`?tab=scenario`) | accept | Already validated by `parseTab` (silent fallback to overview on unknown values). No new user input introduced. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | This plan installs zero packages (confirmed by RESEARCH Package Legitimacy Audit — no installs in Phase 21). |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx" "src/components/layout/Sidebar.test.tsx"` green.
- Manual (advisory): load the allocator dashboard — the Scenario tab is in the strip; load as manager/admin — no Strategy Sandbox link.
</verification>

<success_criteria>
- SURF-01: Scenario tab visible + click/keyboard reachable; `?tab=scenario` deep-link unbroken.
- SURF-02: Strategy Sandbox link present in the sidebar MY WORKSPACE group for allocators.
- SURF-03: Link gated on `isAllocator` only (manager/admin do not see it); server route gate untouched.
- Both touched spec files green; no source change to scenarios/page.tsx.
</success_criteria>

<output>
Create `.planning/phases/21-surfacing-correlation-honest-projection/21-01-SUMMARY.md` when done.
</output>
