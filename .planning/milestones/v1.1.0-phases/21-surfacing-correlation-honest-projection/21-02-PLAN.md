---
phase: 21-surfacing-correlation-honest-projection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/portfolio/CorrelationHeatmap.tsx
  - src/components/portfolio/CorrelationHeatmap.test.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.test.tsx
  - src/lib/scenario-history.ts
  - src/lib/scenario-history.test.ts
autonomous: true
requirements: [CORR-02, CORR-03, CORR-04]
must_haves:
  truths:
    - "A heatmap with >10 strategies renders ALL strategy rows/labels in a scroll container — never truncated, never a 'top 10' disclosure"
    - "A heatmap with <2 active strategies renders a reason-naming empty state, never a 1×1 grid and never an Avg |ρ| number"
    - "A heatmap with <10 overlapping days renders a reason-naming empty state distinct from the <2-strategies copy"
    - "The KPI strip cell label reads 'Avg |ρ|' (not 'Avg ρ')"
    - "shortestHistoryName(strategies) returns the name of the strategy with the shortest common return history"
  artifacts:
    - path: "src/components/portfolio/CorrelationHeatmap.tsx"
      provides: "show-all heatmap + reason-routed empty state (overlappingDays prop) + Avg |ρ| caption"
      contains: "overflow-x-auto"
    - path: "src/app/(dashboard)/allocations/components/KpiStrip.tsx"
      provides: "Avg |ρ| relabel"
      contains: "Avg |ρ|"
    - path: "src/lib/scenario-history.ts"
      provides: "shortestHistoryName pure helper"
      exports: ["shortestHistoryName"]
  key_links:
    - from: "src/components/portfolio/CorrelationHeatmap.tsx empty state"
      to: "host-provided overlappingDays / reason"
      via: "optional prop drives the <10-days vs <2-strategies copy"
      pattern: "overlappingDays|emptyReason"
    - from: "src/app/(dashboard)/allocations/components/KpiStrip.tsx Avg |ρ| cell"
      to: "metrics.avg_pairwise_correlation"
      via: "value unchanged; only the label changes"
      pattern: "avg_pairwise_correlation"
---

<objective>
Promote the existing `CorrelationHeatmap` to its show-all, honest-empty-state, single-sourced form; relabel the KPI strip's correlation cell to "Avg |ρ|"; and create the one net-new pure helper the honesty caveat needs (`shortestHistoryName`).

Purpose: This is the presentational + pure-logic foundation that the two host plans (composer + builder) consume in Wave 2. It removes the top-10 truncation (CORR-04 superseded by show-all), extends the empty-state gate to the two degenerate cases (CORR-02), single-sources the "Avg |ρ|" label (CORR-03), and provides the unit-tested helper for the coverage caveat (IMPACT-01 support).
Output: a show-all + reason-routed heatmap; an "Avg |ρ|" KPI cell; a `shortestHistoryName` helper with its own unit test; updated component tests.

THE ENGINE IS FROZEN. Do NOT touch `src/lib/scenario.ts` or `src/lib/scenario-dealias.ts`. The empty-state thresholds (<10 overlapping days → null matrix; 0 active → null) and `avg_pairwise_correlation` (off-diagonal absolute mean) are ALREADY correct and SCENARIO-05-pinned. Any edit there is a regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-RESEARCH.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-PATTERNS.md
@.planning/phases/21-surfacing-correlation-honest-projection/21-UI-SPEC.md

<interfaces>
<!-- CorrelationHeatmap.tsx (verified file:line) -->
- Props today: `{ correlationMatrix: Record<string,Record<string,number>> | null; strategyNames: Record<string,string> }`.
- PALETTE/correlationBg (:51-72) + pickTextColor (:112) — correlation-SIGN palette (teal-diversifying / orange-concentrated). KEEP. Do NOT import the factsheet return-magnitude palette (real path `src/app/factsheet/[id]/v2/palette.ts`) — its return-magnitude scale is semantically inverted here; UI-SPEC §Color ratified divergence.
- aria-label (:167) is built from the live id count `n` (`${n} strategies. Teal indicates ...`). With show-all, `n = ids.length` = the TRUE strategy count, so the aria-label automatically names the full count (e.g. "12 strategies") — no capped "10". Do NOT hardcode a count.
- `pickTopTenByAvgCorr` (:128-143) + `const ids = useMemo(() => correlationMatrix ? pickTopTenByAvgCorr(...) : [], ...)` (:146-149) — REMOVE; replace with `ids = correlationMatrix ? Object.keys(correlationMatrix) : []`.
- Empty-state gate (:151-157): `if (!correlationMatrix || ids.length === 0)` → render `rounded-lg border border-border bg-surface px-4 py-8 text-center text-text-muted text-sm` "No correlation data available." EXTEND to also trigger on `ids.length < 2` and route reason copy.
- Render contract (:164-227): role="figure"+aria-label, grid gap-px bg-border, gridTemplateColumns "80px repeat(N, minmax(48px,1fr))", axis labels text-[10px] uppercase tracking-wider truncate, cells font-metric text-xs v.toFixed(2), per-cell role="img"+aria-label, diagonal #F1F5F9, missing #F8F9FA "—". KEEP.

<!-- CorrelationHeatmap.test.tsx (verified) -->
- Empty-state tests at :11-22 assert /No correlation data/i — these COPY assertions change.
- Truncation tests at :100-165 assert "10 strategies" survives + a low-corr strategy truncated out — REPLACE with show-all.
- WCAG contrast sweep :179-248 — do NOT touch (palette unchanged).

<!-- KpiStrip.tsx (verified) -->
- "Avg ρ" cell at :415-421: `{ label: "Avg ρ", raw: allKeysStale ? null : avgRhoValue, formatted: formatNumber(... ,2), sub: resolveAvgRhoSub(avgRhoValue), metricKey: "avg_pairwise_correlation" }`. avgRhoValue (:331) already = avg_pairwise_correlation. CHANGE THE LABEL ONLY to "Avg |ρ|". Keep AVG_RHO_PENDING_SUB / stale→null semantics.

<!-- KpiStrip.test.tsx (verified — 13 occurrences of "Avg ρ"; NOT KpiStrip.scenario.test.tsx) -->
- :52 `const labels = ["AUM","YTD TWR","Sharpe","Max DD 12m","Avg ρ"]`; :12/:14/:109 doc+assert the "Avg ρ" literal + R4 honest null-path copy. These literal expectations must become "Avg |ρ|".

<!-- shortestHistoryName analog -->
- The call sites (composer/builder) pass `deAliased.strategies` — the OUTPUT of `collapseAliasedHoldingStrategies` in `src/lib/scenario-dealias.ts`, whose element type is `StrategyForBuilder` (the collapse returns `{ strategies: StrategyForBuilder[]; state }`). StrategyForBuilder.daily_returns[] present client-side (scenario.ts:57). The helper reduces over each strategy's return-window length / earliest date to return the shortest-history NAME. Pure, no side effects, no new server field (Assumption A1). New file src/lib/scenario-history.ts (a scenario-dealias.ts sibling).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shortestHistoryName pure helper (IMPACT-01 support)</name>
  <files>src/lib/scenario-history.ts, src/lib/scenario-history.test.ts</files>
  <read_first>
    - src/lib/scenario.ts (READ-ONLY — confirm StrategyForBuilder shape at :49-62, especially daily_returns[] at :57; do NOT edit this file)
    - src/lib/scenario-dealias.ts (READ-ONLY — the export/location convention to mirror for the new sibling file; confirm `collapseAliasedHoldingStrategies` returns `{ strategies: StrategyForBuilder[]; state }` so the test fixture matches the real call-site element type)
    - 21-RESEARCH.md § "Open Questions > 2. Coverage-caveat 'shortest history' derivation" + Assumption A1
    - 21-PATTERNS.md § "shortestHistoryName helper" (the pure-reduce shape)
  </read_first>
  <behavior>
    - Given strategies each with a name + daily_returns (or a date window), returns the NAME of the strategy with the shortest history (fewest overlapping/available trading days, or latest start date — pick the definition that matches scenarioMetrics.n's overlap semantics and document it).
    - Two strategies, one with a shorter return window → returns the shorter one's name.
    - Tie → deterministic (returns the first by input order); document the tiebreak.
    - Empty / single-strategy input → returns null or the lone name (define and test the degenerate case; never throws).
  </behavior>
  <action>
    Create `src/lib/scenario-history.ts` exporting a pure `shortestHistoryName(strategies)` that reduces over the de-aliased strategy set and returns the shortest-history strategy NAME (string) — no side effects, no new server field, reading only client-side `daily_returns` window metadata already present on each strategy (per Assumption A1). Mirror the export/location convention of `scenario-dealias.ts`. Accept the same de-aliased strategy element type the composer/builder already hold — `StrategyForBuilder` (the element type of `deAliased.strategies`, the output of `collapseAliasedHoldingStrategies` the call sites actually pass); import the type from scenario.ts, do NOT redefine it. Document the exact "shortest" definition (window length vs earliest start) in a one-line comment so it matches the caveat's intent. Write `src/lib/scenario-history.test.ts` covering the behavior cases above; the test fixture MUST match the element type of `deAliased.strategies` (the collapse output the call sites actually pass — `StrategyForBuilder`), so the unit test exercises the real call-site shape, not a raw/hand-rolled struct. Do NOT touch scenario.ts or scenario-dealias.ts.
  </action>
  <verify>
    <automated>npx vitest run "src/lib/scenario-history.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `shortestHistoryName` is exported from src/lib/scenario-history.ts (source assertion: `export function shortestHistoryName` or `export const shortestHistoryName`)
    - The test fixture uses the `StrategyForBuilder` element type of `deAliased.strategies` (the call-site shape), not a hand-rolled struct
    - The test asserts the multi-strategy shortest-pick, the tie tiebreak, and the degenerate (empty/single) case
    - No diff to src/lib/scenario.ts or src/lib/scenario-dealias.ts
  </acceptance_criteria>
  <done>The caveat-name helper exists, is pure, is unit-tested against the real call-site shape, and reads only existing client-side data.</done>
</task>

<task type="auto">
  <name>Task 2: Show-all heatmap + reason-routed empty state + Avg |ρ| caption (CORR-02, CORR-03, CORR-04)</name>
  <files>src/components/portfolio/CorrelationHeatmap.tsx, src/components/portfolio/CorrelationHeatmap.test.tsx</files>
  <read_first>
    - src/components/portfolio/CorrelationHeatmap.tsx (the file being modified — read PALETTE:51-72, pickTopTenByAvgCorr:128-149, empty-state gate:151-157, aria-label:167, render:164-227)
    - src/components/portfolio/CorrelationHeatmap.test.tsx (the spec to update — read empty-state tests :11-22, truncation tests :100-165, WCAG sweep :179-248 which must stay green)
    - 21-UI-SPEC.md § "Component Contracts > 3" (show-all scroll container, empty-state triggers, Avg |ρ| caption typography) + § "Copywriting Contract" (the exact empty-state heading/body strings)
    - 21-PATTERNS.md § "CorrelationHeatmap.tsx" (REMOVE/EXTEND/KEEP blocks + the prop-shape decision)
  </read_first>
  <action>
    (a) SHOW-ALL: delete `pickTopTenByAvgCorr` (:128-143) and its useMemo (:146-149); set `ids = correlationMatrix ? Object.keys(correlationMatrix) : []`. Keep `overflow-x-auto` on the figure wrapper and wrap the grid in a vertically-scrollable container bounded ~70vh (per UI-SPEC §3) so large-N grids scroll both axes; cells stay ≥48px wide. Do NOT render any "showing top 10" disclosure (CORR-04 is satisfied by removal, not a caption). The aria-label at :167 already interpolates the live `n = ids.length`; with show-all that is the TRUE count — verify it now names the real strategy count (e.g. "12 strategies"), not a capped 10. Note: the engine does NOT null a 1-strategy matrix — it returns a 1×1 `{id:{id:1}}` with `avg_pairwise_correlation=null` (scenario.ts builds the matrix for any non-empty ids). The component-level `ids.length < 2` gate added below is the ONLY thing preventing a 1×1 grid for CORR-02 — it is load-bearing, not redundant. (b) EMPTY STATE (CORR-02): extend the gate so `ids.length < 2` ALSO renders the empty state (a 1×1 grid is forbidden), and add an optional `overlappingDays?: number` prop so the host (which has scenarioMetrics.n) can distinguish the `<10 overlapping days` reason from the `<2 strategies` reason. Route the empty-state copy per the Copywriting Contract: heading "Not enough overlap to correlate"; body = the <2-strategies string when fewer than 2 ids, the <10-days string when `overlappingDays` is provided and <10, the combined-fallback string otherwise. Keep the existing empty-state shell shape (`rounded-lg border border-border bg-surface px-4 py-8 text-center text-text-muted text-sm`). NEVER show an Avg |ρ| number in the empty state. (c) Avg |ρ| CAPTION (CORR-03): render a single caption "Avg |ρ| {value}" where the value comes from a host-passed single-sourced number (add an optional prop, e.g. `avgAbsCorrelation?: number`); label `text-[11px] text-text-muted`, number `font-metric`. Do NOT let the heatmap compute its own average. KEEP the correlation-SIGN palette and the full render/ARIA contract; do NOT import the factsheet return-magnitude palette (`src/app/factsheet/[id]/v2/palette.ts`). Update CorrelationHeatmap.test.tsx: REPLACE the two truncation tests (:100-165) with show-all assertions (a 12-strategy matrix renders all 12 labels; no "top 10"/truncation string), update the empty-state copy assertions (:11-22) to the new reason-named copy, and add a `<2 strategies` empty-state test and a `<10 overlapping days` (overlappingDays prop) empty-state test. Leave the WCAG contrast sweep untouched.
  </action>
  <verify>
    <automated>npx vitest run "src/components/portfolio/CorrelationHeatmap.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `pickTopTenByAvgCorr` is gone (source assertion: `grep -v '^\s*\*' src/components/portfolio/CorrelationHeatmap.tsx | grep -c pickTopTenByAvgCorr` == 0)
    - A 12-strategy matrix test asserts all 12 labels render and NO "top 10"/"truncat" string appears
    - The figure's `aria-label` names the TRUE strategy count (e.g. "12 strategies" for a 12-strategy matrix), not a capped 10 (CorrelationHeatmap.tsx:~167 builds the aria-label from `n`)
    - An empty-state test for `<2 strategies` and one for `overlappingDays < 10` each assert the distinct reason copy, and assert no `toFixed`/Avg-|ρ| number is shown
    - The figure wrapper keeps `overflow-x-auto` and gains a max-height scroll container
    - The factsheet return-magnitude palette (`src/app/factsheet/[id]/v2/palette.ts`) is NOT imported (source assertion: no `palette` import from factsheet)
    - WCAG contrast sweep test still green
  </acceptance_criteria>
  <done>CORR-02/03/04 presentational behavior is complete: show-all (aria-label names the true count), reason-named empty states (the `ids.length < 2` gate prevents a 1×1 grid), single-sourced caption; truncation tests replaced; palette and a11y intact.</done>
</task>

<task type="auto">
  <name>Task 3: Relabel KPI strip correlation cell to "Avg |ρ|" (CORR-03)</name>
  <files>src/app/(dashboard)/allocations/components/KpiStrip.tsx, src/app/(dashboard)/allocations/components/KpiStrip.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/KpiStrip.tsx (the file being modified — read the "Avg ρ" cell :415-421, avgRhoValue source :331, AVG_RHO_PENDING_SUB / resolveAvgRhoSub)
    - src/app/(dashboard)/allocations/components/KpiStrip.test.tsx (the spec to update — 13 occurrences of "Avg ρ" incl. labels array :52, R4 null-path :109)
    - 21-UI-SPEC.md § "Component Contracts > 5" + § "Runtime State Inventory" (the relabel is display-string-only; tests asserting the literal must update in the SAME change)
  </read_first>
  <action>
    In KpiStrip.tsx, change the cell `label: "Avg ρ"` (:416) to `label: "Avg |ρ|"`. CHANGE THE LABEL ONLY — `avgRhoValue` (:331) already flows from `avg_pairwise_correlation` (off-diagonal absolute mean), and the honest-pending semantics (`AVG_RHO_PENDING_SUB` / stale → null) stay exactly as-is. Then update KpiStrip.test.tsx: replace every literal "Avg ρ" expectation with "Avg |ρ|" (the labels array at :52, the doc comments at :12/:14, the R4 null-path test at :109, and the remaining occurrences). The failing-then-passing test is the correct signal, not a regression. Do NOT alter the value, the pending copy, or the cell order.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx" "src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - KpiStrip.tsx cell label is "Avg |ρ|" (source assertion: grep `"Avg |ρ|"` present, `"Avg ρ"` absent in KpiStrip.tsx)
    - KpiStrip.test.tsx asserts the cell label "Avg |ρ|" (no remaining "Avg ρ" literal expectation)
    - The R4 honest null-path test still passes (value/pending semantics unchanged)
    - KpiStrip.scenario.test.tsx still green
  </acceptance_criteria>
  <done>CORR-03 label half is done: exactly one correlation label ("Avg |ρ|") across the surface; value untouched. (Single-source value wiring lands in the host plans 03/04.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine output → presentational heatmap | The heatmap renders whatever matrix it is given. The integrity concern is honesty (no fabricated number), not an attacker. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04 | Tampering (data-integrity / honesty) | Correlation empty state | mitigate | Extend the gate so `<2` strategies and `<10` overlapping days render reason-named empty states — never a 1×1 grid (the engine returns a 1×1 `{id:{id:1}}` for 1 strategy; the component gate is the only thing blocking it), never a fabricated Avg |ρ|. Tests assert no number is shown in the empty state. |
| T-21-05 | Tampering (data-integrity) | Fabricated ρ=1.0 between venue aliases | accept (already mitigated upstream) | `collapseAliasedHoldingStrategies` (frozen, wired in the hosts) de-aliases before the engine; the heatmap inherits the honest matrix. Not touched here. |
| T-21-06 | Tampering (false precision) | Heatmap computing its own average | mitigate | The heatmap does NOT compute Avg |ρ|; it receives one host-passed single-sourced value (CORR-03). Prevents caption/strip divergence. |
| T-21-SC | Tampering | npm/pip/cargo installs | accept | Zero installs (RESEARCH Package Legitimacy Audit — no Phase 21 installs). |

> Access-control (V4) is N/A for this plan — these are presentational/pure-logic files with no role surface. The access-control boundary lives in Plan 01 (sidebar) and the unchanged server route.
</threat_model>

<verification>
- `npx vitest run "src/lib/scenario-history.test.ts" "src/components/portfolio/CorrelationHeatmap.test.tsx" "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx" "src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx"` green.
- No diff to src/lib/scenario.ts or src/lib/scenario-dealias.ts (frozen engine).
- Coverage gate: new files (scenario-history.ts, the heatmap changes) carry tests so the blocking `npm run test:coverage` gate is not regressed.
</verification>

<success_criteria>
- CORR-04 (superseded): truncation removed, show-all scroll container, aria-label names the true count, no disclosure caption.
- CORR-02: `<2` strategies and `<10` overlapping days each render a distinct reason-named empty state, never a 1×1 grid or fabricated number.
- CORR-03 (label + value-prep): KPI cell reads "Avg |ρ|"; heatmap caption takes one host-passed single-sourced value.
- IMPACT-01 support: `shortestHistoryName` helper exists and is unit-tested against the real call-site shape.
- Engine untouched; palette + WCAG sweep intact.
</success_criteria>

<output>
Create `.planning/phases/21-surfacing-correlation-honest-projection/21-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
