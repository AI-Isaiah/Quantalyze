---
phase: 24-benchmark-comparison
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/benchmark/btc/route.ts
  - src/app/api/benchmark/btc/route.test.ts
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "GET /api/benchmark/btc returns the BTC daily-returns series as [{date, value}], sorted ascending, pct-changed from close_price (first row dropped)"
    - "The response is cacheable (public Cache-Control), NOT private/no-store — it is shared market data"
    - "The route reads benchmark_prices server-side and exposes NO tenant/user/allocator/strategy data (3-column table only)"
    - "A read error degrades to HTTP 200 with an empty array (so the composer renders the honest empty state, never a red alert)"
  artifacts:
    - path: "src/app/api/benchmark/btc/route.ts"
      provides: "GET handler exposing BTC daily returns"
      exports: ["GET"]
    - path: "src/app/api/benchmark/btc/route.test.ts"
      provides: "Series-shape + sort + pct_change + cache-header + error-degrade tests"
  key_links:
    - from: "src/app/api/benchmark/btc/route.ts"
      to: "benchmark_prices"
      via: "supabase server client .from('benchmark_prices').eq('symbol','BTC')"
      pattern: "benchmark_prices"
    - from: "src/app/api/benchmark/btc/route.ts"
      to: "Cache-Control: public"
      via: "response header (NOT NO_STORE_HEADERS)"
      pattern: "Cache-Control"
---

<objective>
Create the single net-new GET route that exposes the BTC benchmark daily-returns series to the frontend: read `benchmark_prices` (symbol='BTC') server-side, sort ascending, convert close_price to daily returns via pct-change, and return `[{date, value}]` with a PUBLIC cacheable `Cache-Control` response header (shared market data — the deliberate contrast with the allocator no-store routes).

Purpose: BENCH-01's data path. The benchmark daily-returns series is not currently exposed anywhere (only a per-strategy `btc_benchmark_returns` aligned to each strategy's own window, and an aggregated scalar). The composer needs a standalone BTC series spanning an arbitrary scenario blend window.
Output: `src/app/api/benchmark/btc/route.ts` + route test. No migration, no Python change → Railway is a no-op.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-benchmark-comparison/24-RESEARCH.md
@.planning/phases/24-benchmark-comparison/24-CONTEXT.md

<must_read_before_route_code>
AGENTS.md: this is NOT the trained Next.js. BEFORE writing the route, read
`node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md`
(esp. caching: Route Handlers are NOT cached by default in Next 16; a DB read via
the SSR cookie client is DYNAMIC, so `force-static`/`use cache` do NOT apply —
cache via a response `Cache-Control` header). RESEARCH §"State of the Art" cites
this. Set `export const runtime = "nodejs"` (the SSR cookie client needs Node).
</must_read_before_route_code>

<interfaces>
From src/lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>; // request-scoped SSR client, anon key, cookies()
```

From src/lib/database.types.ts (benchmark_prices already typed, line 457):
```
benchmark_prices: { date: string; symbol: string; close_price: number }
```

Table facts (from migrations, VERIFIED): `benchmark_prices` has exactly 3 columns
(date, symbol, close_price), PK (date, symbol); RLS enabled with `SELECT USING(true)`
and writes restricted to service_role (20260406065011_security_hardening.sql:3-19).
The anon SSR client can read but CANNOT write. NO allocator_id / user_id / tenant column.

Python parity reference (NOT called — just the convention): benchmark.py
`prices_to_returns` = `pct_change().dropna()` (drops the first row that has no prior close).

Analog read route (server client + runtime="nodejs"): src/app/api/strategies/browse/route.ts
— note it uses NO_STORE_HEADERS because strategies are visibility-scoped; the benchmark
route deliberately does the OPPOSITE (public cache) because the data is identical for
every caller and leaks nothing.

The no-store constant to NOT use here: src/lib/api/headers.ts → NO_STORE_HEADERS = { "Cache-Control": "private, no-store" }.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — route test stub (RED)</name>
  <files>src/app/api/benchmark/btc/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/browse/route.test.ts if present (sibling route-test style; otherwise find any `src/app/api/**/route.test.ts` for the supabase-mock pattern)
    - .planning/phases/24-benchmark-comparison/24-RESEARCH.md §Validation "Wave 0 Gaps" (route test bullets) + Pattern 3 (route shape)
  </read_first>
  <action>
    Create `src/app/api/benchmark/btc/route.test.ts` importing `{ GET }` from `./route` (does not exist — RED). Mock `@/lib/supabase/server`'s `createClient` to return a stub whose `.from('benchmark_prices').select(...).eq('symbol','BTC').order('date',{ascending:true})` resolves to a known ordered close-price fixture. Write failing tests:
    - "shape + pct_change": given closes [{date:'2024-01-01',close_price:100},{...:'01-02',110},{...:'01-03',99}] the GET body is `[{date:'2024-01-02', value:0.10}, {date:'2024-01-03', value:-0.10}]` — first row dropped (no prior close), each value = close/prevClose − 1. Assert dates ascending and the first input date is absent.
    - "public cache header": the Response has a `Cache-Control` header whose value contains `public` and does NOT contain `no-store` / `private`.
    - "error → 200 empty": when the supabase query resolves `{ data: null, error: {...} }`, GET returns status 200 with body `[]` (degrades to the honest empty state, never 500).
    - "no tenant data": every object in the body has exactly the keys `date` and `value` — no `symbol`, no `close_price`, nothing else.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/benchmark/btc/route.test.ts" 2>&1 | grep -q "fail\|Cannot find" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and FAILS because `./route` does not export GET yet.
    - The pct_change test fixture has a known hand-computed expected output (10% up then ~−10% down) so the transform is pinned, not echoed.
    - The error-degrade test asserts status 200 + `[]`, not a thrown error / 500.
  </acceptance_criteria>
  <done>route.test.ts present, asserts shape/sort/pct_change/cache/error-degrade/no-tenant-keys, fails (route absent).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GET /api/benchmark/btc (GREEN)</name>
  <files>src/app/api/benchmark/btc/route.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md (caching model — REQUIRED per AGENTS.md before route code)
    - src/app/api/strategies/browse/route.ts:1-40 (server-client + runtime="nodejs" wiring; the no-store contrast)
    - src/lib/supabase/server.ts:6-29 (createClient signature)
  </read_first>
  <behavior>
    - GET reads `benchmark_prices` where symbol='BTC', ordered by date ascending, selecting only `date, close_price`.
    - Converts to daily returns: for i>=1, `value = rows[i].close_price / rows[i-1].close_price - 1`, stamped at `rows[i].date`; the first row is dropped (no prior close), matching `prices_to_returns` pct_change().dropna().
    - Returns `NextResponse.json(series, { status: 200, headers: { "Cache-Control": "public, s-maxage=3600, stale-while-revalidate=86400" } })`. (s-maxage value is Claude's Discretion per CONTEXT — 1h is safely fresh vs benchmark.py's ~daily upsert + 48h reject; pick this or justify another. Load-bearing: `public`, cacheable, NOT no-store.)
    - On query error OR empty/missing data: return `NextResponse.json([], { status: 200, headers: {...same cache header...} })` (degrade to honest empty state). Capture the error to Sentry if the project's capture util is the convention in sibling routes, but never surface a 500/red envelope.
    - Accepts NO query params; symbol is hard-coded 'BTC' (V5 input-validation: no user input reaches SQL; CONTEXT locks BTC-only).
  </behavior>
  <action>
    Create `src/app/api/benchmark/btc/route.ts`. `export const runtime = "nodejs"`. Implement `export async function GET()` per the behavior block, using `createClient()` from `@/lib/supabase/server`. Guard a null/zero/division-by-zero prevClose (skip the point rather than emit Infinity/NaN). Do NOT import `NO_STORE_HEADERS`. Do NOT add a `?symbol=` param. Hard-code `.eq("symbol", "BTC")`.
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/benchmark/btc/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 tests pass (shape, sort, pct_change goldens, public cache header, error→200-empty, date/value-only keys).
    - The route does NOT import or use `NO_STORE_HEADERS`: `grep -c NO_STORE_HEADERS src/app/api/benchmark/btc/route.ts` returns 0.
    - The route hard-codes the symbol: `grep -c "\"BTC\"\|'BTC'" src/app/api/benchmark/btc/route.ts` >= 1, and there is no `searchParams`/`?symbol` handling.
    - `export const runtime = "nodejs"` present.
  </acceptance_criteria>
  <done>GET route live; returns BTC daily-returns series with public cache header; error degrades to 200 []; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/benchmark/btc | Authenticated caller requests shared market data; no user input crosses (no params) |
| route → benchmark_prices | Server-side read via anon SSR client; RLS `SELECT USING(true)`; writes blocked to service_role |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Information Disclosure | GET /api/benchmark/btc response | accept | Table is 3 columns (date, symbol, close_price), zero tenant/user/allocator data; the series is identical for every caller, so a shared/public CDN cache leaks nothing (this is WHY it may be `public`-cached, contrast: tenant routes use private/no-store). Verified against migration 20260405093623 (table shape) + 20260406065011 (RLS). |
| T-24-02 | Tampering (SQL injection) | symbol parameter | mitigate | NO `?symbol=` param accepted; symbol hard-coded `'BTC'`; the Supabase `.eq("symbol","BTC")` is parameterized regardless. Do not add a free-text symbol param (CONTEXT locks BTC-only; if ever added, validate against an allowlist). |
| T-24-03 | Tampering (write via read route) | benchmark_prices | mitigate | Route exposes GET only; RLS restricts writes to service_role; the anon SSR client cannot INSERT/UPDATE/DELETE (20260406065011:12-19). |
| T-24-04 | Denial of Service | unbounded read | accept | `benchmark_prices` BTC rows are capped (~1000, benchmark.py days=1000); a bounded ascending select is small. Optionally `.limit(1100)`. Low risk. |
| T-24-SC | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH Package Legitimacy Audit = no-op). No install gate needed. |
</threat_model>

<verification>
- `npx vitest run "src/app/api/benchmark/btc/route.test.ts"` green.
- `npm run typecheck` + `npm run lint` pass.
- Route exposes only `{date, value}` objects; no tenant data; public-cache header present; error path returns 200 `[]`.
</verification>

<success_criteria>
- BENCH-01 read path exists: BTC daily returns reachable at a cacheable GET route, honest-empty on failure, no tenant exposure.
</success_criteria>

<output>
Create `.planning/phases/24-benchmark-comparison/24-02-SUMMARY.md` when done.
</output>
