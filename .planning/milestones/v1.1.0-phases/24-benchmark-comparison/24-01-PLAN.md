---
phase: 24-benchmark-comparison
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scenario.ts
  - src/lib/scenario.test.ts
  - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts
  - src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts
autonomous: true
requirements: [BENCH-01]

must_haves:
  truths:
    - "computeScenario exposes a full-resolution daily portfolio-return series (every date, unrounded) for the active scenario"
    - "The benchmark lib aligns scenario daily returns to BTC daily returns by INTERSECTION (inner-join, no zero-fill) and reports the aligned count n"
    - "Tracking error, information ratio, alpha, and beta are computed over the aligned window with 252-day annualization and match hand-computed goldens"
    - "A degenerate metric (te=0, var(b)=0, n<2) yields null (not a fabricated 0) so the UI can render an em-dash"
  artifacts:
    - path: "src/lib/scenario.ts"
      provides: "Additive OPTIONAL portfolio_daily_returns field on ComputedMetrics"
      contains: "portfolio_daily_returns"
    - path: "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts"
      provides: "innerJoinByDate + computeScenarioBenchmark pure helpers"
      exports: ["computeScenarioBenchmark"]
    - path: "src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts"
      provides: "Golden TE/IR/alpha/beta + intersection + null-safety tests"
  key_links:
    - from: "src/app/(dashboard)/allocations/lib/scenario-benchmark.ts"
      to: "src/lib/portfolio-stats.ts"
      via: "import computeAlphaBeta + computeTrackingError"
      pattern: "computeAlphaBeta|computeTrackingError"
    - from: "src/lib/scenario.ts"
      to: "ComputedMetrics.portfolio_daily_returns"
      via: "additive optional engine output built from commonDates + portDaily"
      pattern: "portfolio_daily_returns"
---

<objective>
Produce the two pure-TS data sources the benchmark comparison reads from: (1) an ADDITIVE, OPTIONAL `portfolio_daily_returns` field on `computeScenario`'s `ComputedMetrics` output exposing the full-resolution daily portfolio-return series (the engine already builds it internally as `portDaily` but only returns a downsampled `equity_curve`), and (2) a new `scenario-benchmark.ts` lib that inner-joins that series to the BTC daily-returns series by date and assembles tracking error / information ratio / alpha / beta over the aligned window, 252-annualized, reusing the existing golden-tested `computeAlphaBeta` + `computeTrackingError`.

Purpose: These are the load-bearing honesty primitives for BENCH-01. The intersection-not-union alignment (RESEARCH Pitfall 2) and the null-not-zero degenerate handling are the milestone honesty invariants; getting either wrong silently fabricates active return.
Output: `scenario.ts` additive OPTIONAL field + `scenario-benchmark.ts` lib + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-benchmark-comparison/24-RESEARCH.md
@.planning/phases/24-benchmark-comparison/24-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from live source — no exploration required. -->

From src/lib/scenario.ts (ComputedMetrics — ADD the new field additively AND OPTIONALLY):
```typescript
export interface ComputedMetrics {
  n: number;
  twr: number | null;
  // ... existing fields unchanged ...
  equity_curve: Array<{ date: string; value: number }>; // downsampled (every 5 days), rounded
  effective_start: string | null;
  effective_end: string | null;
  // NEW (additive, OPTIONAL — non-breaking): full-resolution daily portfolio returns, exact dates, UNROUNDED.
  // portfolio_daily_returns?: Array<{ date: string; value: number }>;
}
```
The field MUST be declared OPTIONAL (`portfolio_daily_returns?: ...`). It is consumed in this codebase at multiple `ComputedMetrics` CONSTRUCTION sites that this plan does not touch — `liveBaselineToComputedMetrics` in ScenarioComposer.tsx and `NULL_METRICS` in ScenarioComparePanel.tsx (phase 23), plus any others. Declaring it optional is fully additive/non-breaking: every existing construction site compiles UNCHANGED, no edit required. `computeScenario` itself ALWAYS sets it (to the real series or `[]`); only the unmodified external construction sites rely on it being optional. Consumers read it with a `?? []` default.

Engine internals to source the field from (DO NOT recompute): `commonDates` (scenario.ts:190, the union axis, sorted) + `portDaily` (scenario.ts:221-236, weighted/renormalized/leveraged daily returns). The field is `commonDates.map((date, i) => ({ date, value: portDaily[i] }))`. NOTE: `commonDates` is the engine's zero-filled UNION axis — that is correct as the LEFT side of the later inner-join; the inner-join (in the lib below) drops any date BTC doesn't also have, so union-axis dates a late strategy zero-filled are kept only if BTC covers them.

From src/lib/portfolio-stats.ts (REUSE as-is, do NOT reimplement):
```typescript
// beta = cov(r,b)/var(b); alpha = (mean(r) - beta*mean(b)) * 252. Returns {alpha:0,beta:0} when n<2 OR var(b)=0 — the two are INDISTINGUISHABLE from the return value.
export function computeAlphaBeta(returns: number[], benchmark: number[]): { alpha: number; beta: number };
// TE = stdDev(r-b, sample) * sqrt(252). Returns 0 when n<2. (positional zip by index — NOT date-aligned)
export function computeTrackingError(returns: number[], benchmark: number[]): number;
```

From src/lib/portfolio-math-utils.ts:
```typescript
export function mean(values: number[]): number;
export interface DailyPoint { date: string; value: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 — golden test stub for scenario-benchmark.ts (RED)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts</files>
  <read_first>
    - src/lib/portfolio-stats.test.ts:286-343 (the golden-test pattern to mirror: cov/var beta golden, hand-computed expected values)
    - src/app/(dashboard)/allocations/lib/scenario-compare.test.ts (sibling test style in this dir)
    - .planning/phases/24-benchmark-comparison/24-RESEARCH.md §"Assembling the four metrics" + §Validation Architecture "Wave 0 Gaps"
  </read_first>
  <action>
    Create `src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts` importing `computeScenarioBenchmark` (does not exist yet — RED). Write failing tests that pin BENCH-01:
    - "golden": a small hand-computed pair `p[] = [...]`, `b[] = [...]` (≈6-10 daily returns, fully overlapping dates) with hand-computed expected beta (cov/var), alpha ((mean_p − β·mean_b)×252), tracking error (std(p−b, sample)×√252), and information ratio (mean(p−b)×252/te). Compute the four expected numbers BY HAND in a comment and assert each `toBeCloseTo` with explicit precision. Mirror the cov/var-beta golden style at portfolio-stats.test.ts:299-314.
    - "intersection": a portfolioDaily with dates {d1..d8} and a btcDaily covering only {d3..d6} (plus an extra date d9 not in portfolio). Assert the returned `n === 4`, and that injecting a wildly divergent value on a NON-overlapping date (d1 or d9) does NOT change any metric — proving inner-join, not union. A test that would PASS under a union/positional zip must FAIL here.
    - "null-safety / em-dash source": n<2 aligned → every metric field is `null` (not 0). A constant benchmark (var(b)=0) → beta/alpha degenerate path returns `null` for the affected fields, never a fabricated 0. te=0 (p≡b) → informationRatio is `null` (guard `te>0` before dividing).
    Stub the import target so the file type-checks against the planned export shape `{ n, trackingError, informationRatio, alpha, beta, correlation }` with all numeric fields `number | null`.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts" 2>&1 | grep -q "fail\|Cannot find\|is not a function" && echo "RED confirmed"</automated>
  </verify>
  <acceptance_criteria>
    - Test file exists and FAILS (module/symbol missing) — RED state proven.
    - The "intersection" test is constructed so it would fail under a positional-zip/union implementation (a non-overlapping date carries a divergent value that must be excluded).
    - Golden expected values are hand-computed in comments, not derived from the implementation.
  </acceptance_criteria>
  <done>scenario-benchmark.test.ts present, asserts golden + intersection + null-safety, fails because the lib does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement scenario-benchmark.ts (inner-join + 4 metrics) (GREEN)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-benchmark.ts</files>
  <read_first>
    - src/lib/portfolio-stats.ts:412-455 (computeAlphaBeta + computeTrackingError — the exact helpers to call; note they zip POSITIONALLY and return 0 not null for degenerate input, and {alpha:0,beta:0} for BOTH n<2 AND var(b)=0 — indistinguishable)
    - src/lib/scenario.ts:348-400 (the Pearson sample-cov/std·std correlation shape to mirror for the optional correlation field)
    - src/lib/portfolio-math-utils.ts:64 (`mean`) + :11 (`DailyPoint`)
    - analytics-service/routers/portfolio.py:915-916 (the `reindex(...).dropna()` inner-join the TS mirrors) and metrics.py:811-814 (IR reference)
  </read_first>
  <behavior>
    - innerJoinByDate(port, bench): builds a `Map<date,value>` from bench, iterates `port` in order, keeps only dates present in both → returns `{ dates, p, b }`; NO zero-fill, NO interpolation.
    - computeScenarioBenchmark(portfolioDaily, btcDaily): inner-joins, sets `n = p.length`; if `n < 2` returns all-null metrics with that `n`. Otherwise compute `varB` (population variance of the aligned benchmark returns `b`) FIRST. If `varB === 0` (constant benchmark) short-circuit beta/alpha to `null` WITHOUT calling/trusting computeAlphaBeta's silent-0 (the helper returns {alpha:0,beta:0} for both n<2 and var(b)=0 — those are indistinguishable, so the degenerate case MUST be detected here, not inferred from the helper). Otherwise `{alpha,beta} = computeAlphaBeta(p,b)`. Always `te = computeTrackingError(p,b)`; `ir = te > 0 ? (mean(p.map((v,i)=>v-b[i])) * 252) / te : null`; correlation via the Pearson sample shape.
    - All annualization is ×252 / ×√252 via the reused helpers — NO √365 anywhere.
  </behavior>
  <action>
    Create `src/app/(dashboard)/allocations/lib/scenario-benchmark.ts`. Implement and export `computeScenarioBenchmark(portfolioDaily: DailyPoint[], btcDaily: DailyPoint[]): ScenarioBenchmark` where `ScenarioBenchmark = { n: number; trackingError: number | null; informationRatio: number | null; alpha: number | null; beta: number | null; correlation: number | null }`. Also export `innerJoinByDate` (used by the test and the composer for the {N} count) and the `ScenarioBenchmark` type. Reuse `computeAlphaBeta`/`computeTrackingError` from `@/lib/portfolio-stats` and `mean` from `@/lib/portfolio-math-utils` — do NOT hand-roll OLS or std. Compute `varB` (population variance of the aligned benchmark returns: `mean(b.map(x=>(x-mean(b))**2))`) BEFORE calling computeAlphaBeta; if `varB === 0` short-circuit to `{beta:null, alpha:null}` WITHOUT trusting the helper's silent-0. Do NOT infer the degenerate case from computeAlphaBeta's return value (which is {alpha:0,beta:0} for BOTH n<2 and var(b)=0). A constant benchmark must surface "—", not a fabricated 0 (per CONTEXT honesty invariant). Per D (BENCH-01): the math is internally correct and 252-annualized; it is NOT parity-bound to a live Python path.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 tests pass (golden numbers, intersection n===4 with non-overlap exclusion, null degenerate paths).
    - `computeScenarioBenchmark` imports and calls `computeAlphaBeta` + `computeTrackingError` (grep: `computeAlphaBeta` and `computeTrackingError` both present in the new file).
    - `varB === 0` is detected explicitly BEFORE the beta/alpha decision (grep: `varB` present; the constant-benchmark branch returns null without depending on computeAlphaBeta's return).
    - No `Math.sqrt(365)` / `365` annualization token anywhere in the file: `grep -v '^\s*//' src/app/(dashboard)/allocations/lib/scenario-benchmark.ts | grep -c '365'` returns 0.
    - A constant-benchmark input returns `beta:null` (var(b)=0 honest "—"), verified by a test assertion.
  </acceptance_criteria>
  <done>scenario-benchmark.ts exports computeScenarioBenchmark + innerJoinByDate + ScenarioBenchmark; all golden/intersection/null tests green; varB=0 short-circuit pins beta/alpha to null.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add additive OPTIONAL portfolio_daily_returns to computeScenario</name>
  <files>src/lib/scenario.ts, src/lib/scenario.test.ts</files>
  <read_first>
    - src/lib/scenario.ts:85-112 (ComputedMetrics shape) + :190-236 (commonDates + portDaily) + :192-208 and :281-297 (the two early-return blocks that null the metrics)
    - src/lib/scenario.test.ts (existing pins — verify the additive field does not regress them; find the assertion style)
  </read_first>
  <behavior>
    - ComputedMetrics gains `portfolio_daily_returns?: Array<{ date: string; value: number }>` — declared OPTIONAL so the external construction sites (liveBaselineToComputedMetrics, NULL_METRICS, any others) compile UNCHANGED. This plan does NOT edit any of those sites; the optional declaration makes the field fully additive/non-breaking.
    - On the main success path `computeScenario` ALWAYS sets it to `commonDates.map((date,i) => ({ date, value: portDaily[i] }))` — same length as `n`, exact dates, UNROUNDED (do not `.toFixed`), built BEFORE the downsample loop.
    - On BOTH degenerate early-returns (n<10 at scenario.ts:192-208, and the non-finite / minCumulative<=0 block at :281-297) `computeScenario` sets the field to `[]` — a degenerate scenario yields no overlap rather than a false window.
    - Additive only: every existing `scenario.test.ts` pin holds (no field renamed/removed; downsampled `equity_curve` untouched).
  </behavior>
  <action>
    Edit `src/lib/scenario.ts`: add `portfolio_daily_returns?: Array<{ date: string; value: number }>` (OPTIONAL — the `?` is required; do NOT make it required) to the `ComputedMetrics` interface (documented as full-resolution daily, unrounded, the source for benchmark alignment; consumers needing wealth-form convert separately — mirror the `equity_curve` doc-comment convention). The optional declaration is fully additive: external construction sites (`liveBaselineToComputedMetrics` in ScenarioComposer.tsx, `NULL_METRICS` in ScenarioComparePanel.tsx, any others) compile unchanged and need NO edit. In `computeScenario`, build `const portfolio_daily_returns = commonDates.map((date, i) => ({ date, value: portDaily[i] }));` right after `portDaily` is filled (~:236), and include it in the main `return` object (~:417). Add `portfolio_daily_returns: []` to BOTH early-return objects (n<10 block and the anyNonFinite/minCumulative<=0 block). Do NOT round and do NOT downsample this field.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The `ComputedMetrics` field is declared OPTIONAL: `grep -E 'portfolio_daily_returns\?:' src/lib/scenario.ts` matches ≥1 (the `?` is present in the interface declaration).
    - Extend `scenario.test.ts`: a "daily_returns" test asserts the new field's length === `result.n` and its dates equal the engine's internal daily axis (first/last date match `effective_start`/`effective_end`), and equals `[]` on a degenerate input (single strategy / <10 days). This test PASSES.
    - All pre-existing `scenario.test.ts` assertions still pass (additive — no regression).
    - The field is unrounded: a value in `portfolio_daily_returns` retains more precision than the corresponding rounded `equity_curve` point would (assert it is not pre-rounded to 5 decimals).
    - `npm run typecheck` passes with NO edits to liveBaselineToComputedMetrics or NULL_METRICS or any other construction site — the optional field requires none.
  </acceptance_criteria>
  <done>ComputedMetrics.portfolio_daily_returns lands additively and OPTIONALLY; computeScenario always sets it; new daily_returns test green; full scenario.test.ts green; no construction site edited.</done>
</task>

</tasks>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-benchmark.test.ts" src/lib/scenario.test.ts` green.
- `npm run typecheck` passes. Because `portfolio_daily_returns` is OPTIONAL, the additive field compiles at all `ComputedMetrics` construction sites WITHOUT any edit — `liveBaselineToComputedMetrics` (ScenarioComposer.tsx), `NULL_METRICS` (ScenarioComparePanel.tsx), and any others are left untouched and compile unchanged.
- Honesty invariants proven by test: intersection-not-union (non-overlap date excluded), null-not-zero (constant benchmark → beta null via explicit varB=0 short-circuit; n<2 → all null), 252-annualization (golden values), `[]` on degenerate scenario.
</verification>

<success_criteria>
- BENCH-01 math + alignment primitives exist, golden-tested, 252-annualized, intersection-aligned.
- `computeScenario` exposes the full daily series additively and OPTIONALLY with zero regression and zero construction-site edits.
- Degenerate inputs surface null (em-dash-ready), never a fabricated number.
</success_criteria>

<output>
Create `.planning/phases/24-benchmark-comparison/24-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
