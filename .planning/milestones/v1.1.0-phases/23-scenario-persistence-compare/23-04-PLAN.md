---
phase: 23-scenario-persistence-compare
plan: 04
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - src/app/(dashboard)/allocations/hooks/useScenarioState.ts
  - src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx
autonomous: true
requirements: [PERSIST-02]
must_haves:
  truths:
    - "Opening a saved scenario runs its draft through scenarioDraftCodec (ok/readonly/reset) BEFORE hydrating — never a bare cast"
    - "An ok draft hydrates the composer; a drifted draft surfaces the EXISTING fingerprint-mismatch banner automatically (no special-casing)"
    - "A reset (older incompatible) draft renders the honest 'older format, can't be reopened' notice — NEVER a silent empty composer"
    - "A readonly (newer-version) draft hydrates but blocks edits with the honest read-only notice"
    - "The composer toolbar shows Save scenario (no scenario open) or Update scenario + Save as new (a saved scenario open), tracking loadedScenarioId"
  artifacts:
    - path: "src/app/(dashboard)/allocations/hooks/useScenarioState.ts"
      provides: "one-shot hydrateFromSaved(draft) seam routed through setValue (not removeStored)"
      contains: "hydrateFromSaved"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Save/Update/Save-as-new toolbar + loadedScenarioId state + codec-trichotomy Open handling"
      contains: "loadedScenarioId"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "scenarioDraftCodec"
      via: "decode the row's draft (ok→hydrate, readonly→hydrate+block, reset→honest notice) before hydrateFromSaved"
      pattern: "scenarioDraftCodec"
    - from: "src/app/(dashboard)/allocations/hooks/useScenarioState.ts"
      to: "fingerprintMismatch banner"
      via: "hydrateFromSaved writes saved draft into value; storedMismatch derives automatically"
      pattern: "setValue"
---

<objective>
Add the reopen/hydration seam and the Save/Update toolbar. A one-shot `hydrateFromSaved(draft)` on `useScenarioState` routes through `setValue` (so the existing fingerprint-mismatch banner fires automatically), and the composer gains the Save/Update/Save-as-new controls + `loadedScenarioId` tracking, applying the `scenarioDraftCodec` trichotomy on Open.

Purpose: PERSIST-02. The reopen path MUST reuse the existing codec trichotomy + fingerprint banner verbatim — a drifted draft surfaces the banner, a `reset` draft shows an honest notice (never a silent empty composer), a `readonly` draft blocks edits. This is the same no-invented-data invariant as Phase 21/22 and the #509 fix.
Output: `hydrateFromSaved` seam + test; composer toolbar + Open wiring + test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-scenario-persistence-compare/23-PATTERNS.md
@.planning/phases/23-scenario-persistence-compare/23-RESEARCH.md
@.planning/phases/23-scenario-persistence-compare/23-UI-SPEC.md
@.planning/phases/23-scenario-persistence-compare/23-02-SUMMARY.md

**AGENTS.md mandate:** NOT training-data Next.js — read `node_modules/next/dist/docs/` before any client/server-boundary or fetch code if needed.

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

useScenarioState (src/app/(dashboard)/allocations/hooks/useScenarioState.ts):
  - UseScenarioStateReturn interface at :65-83 (extend it with hydrateFromSaved)
  - setValue((prev) => …) is the mutator path (:202-231); reset() calls removeStored (:232-237) — DO NOT use removeStored for hydrate
  - storedMismatch = value.init_holdings_fingerprint !== fingerprint (:157-160); fingerprintMismatch = storedMismatch && !mismatchDismissed (:173)
  - setMismatchDismissed exists (banner dismiss state)

Recommended seam (RESEARCH Q2):
  hydrateFromSaved = useCallback((saved: ScenarioDraft) => {
    setValue(() => saved);        // in-memory working draft; autosave persists on next edit (NOT removeStored — Pitfall 6)
    setMismatchDismissed(false);  // freshly-opened scenario gets a fresh banner
  }, [setValue]);

scenarioDraftCodec (scenario-state.ts:521): decode(raw: string | null) → { kind: "ok"|"readonly"|"reset", value } trichotomy.
  Reuse via decode(JSON.stringify(row.draft)) OR extract a decodeDraftObject(obj) helper — do NOT bypass (Pitfall 3 / M-0153).
  ok → hydrateFromSaved(value). readonly (schema_version ahead) → hydrate but block edits + read-only notice.
  reset (older/invalid) → do NOT hydrate; render honest "older format" notice.

Composer toolbar header self-analog: ScenarioComposer.tsx:992-993 (flex flex-wrap items-center gap-3, the "Scenario" H2).
  Do NOT re-style the PROJECTED badge / fingerprint-mismatch banner (:992-1044) — reopen reuses them verbatim.
loadedScenarioId: string | null is composer useState — Open sets it; reset() clears it; Save(id null)→POST;
  Update(id set)→PUT that row; Save as new→POST + set new id.

UI-SPEC copy (§Copywriting): "Save scenario" / "Update scenario" / "Save as new scenario" / "Name this scenario" /
  "Enter a name to save this scenario." / "Scenario names are limited to 120 characters." /
  reset: "This saved scenario uses an older format and can't be reopened." /
  readonly: "This scenario was saved by a newer version and is read-only here." /
  failure: "Couldn't save this scenario. …" / "Couldn't open this scenario. Try again."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the hydrateFromSaved one-shot seam to useScenarioState</name>
  <files>src/app/(dashboard)/allocations/hooks/useScenarioState.ts, src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/hooks/useScenarioState.ts:65-83 (UseScenarioStateReturn — extend), :157-173 (storedMismatch/fingerprintMismatch derivation), :202-237 (setValue mutators + reset/removeStored — the seam routes through setValue, NOT removeStored)
    - src/app/(dashboard)/allocations/lib/scenario-state.ts:75-96 (ScenarioDraft shape — carries init_holdings_fingerprint)
  </read_first>
  <behavior>
    - hydrateFromSaved(savedDraft) sets the in-memory working draft to savedDraft (the fingerprint banner then derives automatically from value.init_holdings_fingerprint vs current fingerprint).
    - A savedDraft whose init_holdings_fingerprint != current fingerprint makes fingerprintMismatch true WITHOUT any loadedFromDb special-case branch (Pitfall 2).
    - hydrateFromSaved does NOT call removeStored (does not destructively wipe the localStorage key — Pitfall 6); it uses setValue like the mutators.
    - A freshly-opened scenario resets mismatchDismissed to false (fresh banner).
  </behavior>
  <action>
    RED first: write `src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx` (renderHook style; mock holdingsSummary so fingerprint is known) encoding the behavior block — hydrate a draft with a MATCHING fingerprint (no banner), then a MISMATCHED fingerprint (banner fires), and assert the localStorage key is not wiped by hydrate. Run it — MUST fail (no hydrateFromSaved yet). GREEN: add `hydrateFromSaved: (draft: ScenarioDraft) => void` to UseScenarioStateReturn (:65-83) and the return object (:271-282); implement it via the RESEARCH Q2 seam — `setValue(() => saved); setMismatchDismissed(false);` — routed through the SAME setValue the mutators use. Do NOT add a loadedFromDb branch that bypasses fingerprintMismatch; do NOT use removeStored. REFACTOR; keep green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - RED commit: test fails before hydrateFromSaved exists.
    - hydrateFromSaved routes through setValue (not removeStored); test proves the localStorage key is NOT destructively wiped on hydrate.
    - A mismatched-fingerprint hydrate fires fingerprintMismatch with no special-case branch.
    - mismatchDismissed reset to false on hydrate (fresh banner).
    - No new loadedFromDb branch bypassing the existing derivation.
  </acceptance_criteria>
  <done>hydrateFromSaved seam green; banner derives automatically; localStorage not clobbered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Save/Update toolbar + loadedScenarioId + codec-trichotomy Open handling</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:992-1044 (toolbar header self-analog + PROJECTED badge + fingerprint banner — reuse verbatim, do NOT re-style)
    - src/app/(dashboard)/allocations/lib/scenario-state.ts:521-583 (scenarioDraftCodec trichotomy — decode + ok/readonly/reset)
    - src/components/ui/Button.tsx (variants primary/secondary/ghost/danger per UI-SPEC)
    - .planning/phases/23-scenario-persistence-compare/23-UI-SPEC.md §Component Inventory 1 + §Copywriting (exact tokens + copy)
    - src/app/api/allocator/scenario/saved/route.ts + [id]/route.ts (Plan 02 — POST/PUT endpoints to call)
  </read_first>
  <behavior>
    - No scenario open (loadedScenarioId null): toolbar shows a single primary "Save scenario" → click reveals an inline "Name this scenario" input + confirm (NO modal). Submit validates trim + 1..120; empty → "Enter a name to save this scenario."; >120 → "Scenario names are limited to 120 characters."; valid → POST → on success set loadedScenarioId to the returned id.
    - A saved scenario open (loadedScenarioId set): primary becomes "Update scenario" (PUT that row), with secondary "Save as new scenario" (POST a new row → set the new id).
    - Open(savedRow): decode row.draft through scenarioDraftCodec — ok → hydrateFromSaved + set loadedScenarioId; readonly → hydrate + set loadedScenarioId + render "This scenario was saved by a newer version and is read-only here." + block edit controls; reset → do NOT hydrate, render "This saved scenario uses an older format and can't be reopened." (NEVER a silent empty composer).
    - reset() (clear composer) clears loadedScenarioId.
    - A drifted (fingerprint-mismatch) reopened scenario surfaces the EXISTING banner verbatim (not re-styled).
    - A hard save/open failure routes the canonical error copy ("Couldn't save this scenario. …" / "Couldn't open this scenario. Try again.").
  </behavior>
  <action>
    RED first: write `src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx` encoding the behavior block (mock fetch for POST/PUT; mock a saved row's draft for ok/readonly/reset cases; assert the reset case renders the honest notice and does NOT hydrate an empty composer). Run it — MUST fail. GREEN: add the Save/Update control INTO the existing `flex flex-wrap items-center gap-3` header row (ScenarioComposer.tsx:993) using Button variants per UI-SPEC; add `loadedScenarioId: string | null` useState (Open sets it, reset() clears it); wire Save→POST, Update→PUT, Save-as-new→POST. Implement Open via scenarioDraftCodec(defaultDraft).decode(JSON.stringify(row.draft)) (or a decodeDraftObject helper) — branch ok/readonly/reset per the behavior block; call the Plan-01 hook's hydrateFromSaved only on ok/readonly; on reset render the honest notice and do NOT hydrate. Reuse the existing PROJECTED badge + fingerprint-mismatch banner verbatim (do not re-style :992-1044). Use ONLY UI-SPEC tokens/copy. REFACTOR; keep green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - RED commit: the save/open test fails before the toolbar/Open wiring exists.
    - Toolbar: "Save scenario" (id null) vs "Update scenario" + "Save as new scenario" (id set); inline name input (no modal); name validation copy exact.
    - Open decodes through scenarioDraftCodec (never a bare cast): ok→hydrate, readonly→hydrate+block+read-only notice, reset→honest notice + NO hydrate (test asserts no empty-composer silent default).
    - loadedScenarioId set on Open, cleared on reset().
    - Existing PROJECTED badge + fingerprint-mismatch banner reused verbatim (not re-styled); drifted reopen fires the banner.
    - Hard failure uses the canonical error copy.
    - Existing ScenarioComposer.test.tsx still green (no regression).
    - Only UI-SPEC tokens/copy; no new icons.
  </acceptance_criteria>
  <done>Save/Update/Save-as-new toolbar + codec-trichotomy Open wiring green; reset honesty + reused banner proven.</done>
</task>

</tasks>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` green.
- `npm run typecheck` + `npm run lint` clean.
- The reopen `reset` path renders the honest notice and never silently defaults to an empty composer (test-proven).
</verification>

<success_criteria>
- Reopen runs the saved draft through the codec trichotomy before hydrating (no bare cast).
- ok→hydrate; drift→existing banner; reset→honest notice (no empty composer); readonly→block edits.
- Save/Update/Save-as-new toolbar tracks loadedScenarioId; no reintroduced leverage UI.
</success_criteria>

<output>
Create `.planning/phases/23-scenario-persistence-compare/23-04-SUMMARY.md` when done.
</output>
