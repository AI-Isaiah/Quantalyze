---
phase: 23-scenario-persistence-compare
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260621120000_scenarios_table_and_rls.sql
  - supabase/migrations/down/20260621120000-rollback.sql
  - supabase/tests/test_scenarios_rls.sql
  - src/lib/database.types.ts
  - src/lib/database.types.test.ts
autonomous: true
requirements: [PERSIST-01]
must_haves:
  truths:
    - "A scenarios table exists with allocator-owned RLS (only the owning allocator sees/writes their rows)"
    - "Cross-tenant content is provably isolated: tenant A cannot read/update/delete tenant B's scenario by row id"
    - "src/lib/database.types.ts exposes scenarios Row/Insert/Update with correct nullability, hand-patched (not regenerated)"
  artifacts:
    - path: "supabase/migrations/20260621120000_scenarios_table_and_rls.sql"
      provides: "forward-timestamped scenarios table + owner RLS + name CHECK + (allocator_id, updated_at desc) index"
      contains: "CREATE POLICY scenarios_owner"
    - path: "supabase/migrations/down/20260621120000-rollback.sql"
      provides: "matching rollback (DROP TABLE scenarios CASCADE)"
      contains: "DROP TABLE"
    - path: "supabase/tests/test_scenarios_rls.sql"
      provides: "two-tenant cross-content RLS assertions by row id (read + negative write path)"
      contains: "CROSS-TENANT LEAK"
    - path: "src/lib/database.types.ts"
      provides: "hand-patched scenarios Row/Insert/Update/Relationships block, tripwire comments preserved"
      contains: "scenarios"
  key_links:
    - from: "supabase/migrations/20260621120000_scenarios_table_and_rls.sql"
      to: "src/lib/database.types.ts"
      via: "hand-patched Row matching the migration column set"
      pattern: "schema_version"
    - from: "supabase/tests/test_scenarios_rls.sql"
      to: "scenarios_owner policy"
      via: "forge request.jwt.claims sub + SET LOCAL ROLE authenticated, assert by row id"
      pattern: "request\\.jwt\\.claims"
---

<objective>
Create the `scenarios` persistence layer: a forward-timestamped migration (table + owner RLS + name CHECK + list index), its rollback, the two-tenant content RLS test, and the hand-patched `database.types.ts` with `expectTypeOf` pins. This is the foundation every other Phase 23 plan builds on.

Purpose: PERSIST-01 requires an RLS-scoped durable store for the `ScenarioDraft` JSONB (never the raw series). RLS fails silently, so the SQL test asserts on cross-tenant *content* by row id — the highest-value correctness guarantee in this phase.
Output: One forward migration + rollback, one `supabase/tests/test_*.sql`, a hand-patched `database.types.ts`, and extended `database.types.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-scenario-persistence-compare/23-RESEARCH.md
@.planning/phases/23-scenario-persistence-compare/23-PATTERNS.md

<interfaces>
<!-- Extracted from codebase — executor uses these directly, no exploration needed. -->

scenarios table column contract (CONTEXT line 24, authoritative):
  id            uuid pk default gen_random_uuid()
  allocator_id  uuid not null references profiles on delete cascade
  name          text not null  CHECK (length(btrim(name)) between 1 and 120)
  draft         jsonb not null
  schema_version int not null
  created_at    timestamptz not null default now()
  updated_at    timestamptz not null default now()
  -- NO UNIQUE on name (CONTEXT line 29 — 23505 timebomb)
  -- index scenarios(allocator_id, updated_at DESC) for list ordering

api_keys_owner RLS pattern (supabase/migrations/20260405061912_rls_policies.sql:22):
  CREATE POLICY api_keys_owner ON api_keys FOR ALL USING (user_id = auth.uid()) WITH CHECK (user_id = auth.uid());
  -- profiles.id IS the auth user id (profiles_own keyed on id = auth.uid()), so allocator_id = auth.uid() is correct.

database.types.ts hand-patch analog: the for_quants_leads HAND-PATCHED block at database.types.ts:1021-1097
  (Row / Insert / Update / Relationships quad; Insert makes defaulted cols optional; Update makes all optional).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the scenarios migration + rollback</name>
  <files>supabase/migrations/20260621120000_scenarios_table_and_rls.sql, supabase/migrations/down/20260621120000-rollback.sql</files>
  <read_first>
    - supabase/migrations/20260405061911_initial_schema.sql:19-32 (api_keys CREATE TABLE shape — id/references/timestamptz)
    - supabase/migrations/20260405061912_rls_policies.sql:22 (api_keys_owner owner policy to mirror)
    - supabase/migrations/down/20260620120000-rollback.sql (rollback naming convention: hyphen + -rollback, same timestamp)
  </read_first>
  <action>
    Create `supabase/migrations/20260621120000_scenarios_table_and_rls.sql` (forward timestamp — later than the remote tip `20260620120000` so migration-policy.yml does not reject it as backdated). Body: CREATE TABLE scenarios with the exact column contract in the interfaces block (id uuid pk default gen_random_uuid(); allocator_id uuid not null references profiles on delete cascade; name text not null with CHECK (length(btrim(name)) between 1 and 120); draft jsonb not null; schema_version int not null; created_at/updated_at timestamptz not null default now()). ALTER TABLE scenarios ENABLE ROW LEVEL SECURITY. CREATE POLICY scenarios_owner ON scenarios FOR ALL USING (allocator_id = auth.uid()) WITH CHECK (allocator_id = auth.uid()). CREATE INDEX on scenarios (allocator_id, updated_at DESC) for the list ordering. Do NOT add a UNIQUE constraint on name (per D — 23505 timebomb). Do NOT add a set_updated_at() trigger function — touching updated_at = now() in the UPDATE route (Plan 02) avoids tripping the dump-sql-functions.ts --check snapshot gate; a plain table + policy defines no tracked function. Create the matching `supabase/migrations/down/20260621120000-rollback.sql` with `DROP TABLE scenarios CASCADE;`.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260621120000_scenarios_table_and_rls.sql && test -f supabase/migrations/down/20260621120000-rollback.sql && grep -q "scenarios_owner" supabase/migrations/20260621120000_scenarios_table_and_rls.sql && grep -q "WITH CHECK (allocator_id = auth.uid())" supabase/migrations/20260621120000_scenarios_table_and_rls.sql && ! grep -qi "UNIQUE.*name\|create function set_updated_at" supabase/migrations/20260621120000_scenarios_table_and_rls.sql && grep -q "DROP TABLE scenarios" supabase/migrations/down/20260621120000-rollback.sql</automated>
  </verify>
  <acceptance_criteria>
    - Forward migration file present with a timestamp strictly greater than 20260620120000.
    - scenarios_owner policy is FOR ALL with BOTH USING and WITH CHECK keyed on allocator_id = auth.uid().
    - name CHECK is length(btrim(name)) between 1 and 120; NO UNIQUE constraint on name.
    - Index on (allocator_id, updated_at DESC) present.
    - No set_updated_at trigger function created (avoids the snapshot gate).
    - Rollback file exists with matching timestamp and DROP TABLE scenarios CASCADE.
    - DO NOT push to prod — migration applies at /land-and-deploy (anon NO-EXEC verified). No `supabase db push` in this plan.
  </acceptance_criteria>
  <done>Migration + rollback exist; policy + CHECK + index correct; no trigger fn; no name UNIQUE.</done>
</task>

<task type="auto">
  <name>Task 2: Write the two-tenant content RLS test</name>
  <files>supabase/tests/test_scenarios_rls.sql</files>
  <read_first>
    - supabase/tests/test_funding_fees_rls.sql (full — exact structure: pre-clean by email; seed two tenants auth.users→profiles; forge request.jwt.claims sub + SET LOCAL ROLE authenticated; cross-content assertions by row id; negative write path; teardown via cascade)
    - supabase/migrations/20260621120000_scenarios_table_and_rls.sql (the policy under test, from Task 1)
  </read_first>
  <action>
    Create `supabase/tests/test_scenarios_rls.sql` mirroring `test_funding_fees_rls.sql`. The chain is shorter than funding_fees: seed only `auth.users → profiles → scenarios` per tenant (drop the api_keys/strategies/funding_fees inserts; one scenarios INSERT per tenant capturing its id via `RETURNING id INTO scen_a_id`/`scen_b_id`). Use plain PL/pgSQL `DO $$ … $$` with `RAISE EXCEPTION` on failure and `RAISE NOTICE` on pass; NO pgTAP; no `\!`/`\copy`/`\o` meta-commands (the sql-tests preflight rejects them). Defensive pre-clean by email at the top: `DELETE FROM auth.users WHERE email IN ('test-scen-rls-tenant-a@quantalyze.test','test-scen-rls-tenant-b@quantalyze.test');`. As tenant A (forge sub=uid_a, SET LOCAL ROLE authenticated): assert A sees scen_a_id (else RAISE 'read policy regressed'); assert A does NOT see scen_b_id (else RAISE 'CROSS-TENANT LEAK'). Negative write path: as A, `UPDATE scenarios SET name='hijacked' WHERE id = scen_b_id` must affect 0 rows; RESET ROLE and verify B's name unchanged by id via a service-role read. Positive path: assert A CAN update and delete its OWN row (so the policy isn't over-tight). Teardown: `DELETE FROM auth.users WHERE id IN (uid_a, uid_b);` (cascades) plus the repeated email-based DELETE outside the DO block.
  </action>
  <verify>
    <automated>test -f supabase/tests/test_scenarios_rls.sql && grep -q "CROSS-TENANT LEAK" supabase/tests/test_scenarios_rls.sql && grep -q "request.jwt.claims" supabase/tests/test_scenarios_rls.sql && grep -q "SET LOCAL ROLE authenticated" supabase/tests/test_scenarios_rls.sql && ! grep -qE '\\\\!|\\\\copy|\\\\o' supabase/tests/test_scenarios_rls.sql</automated>
  </verify>
  <acceptance_criteria>
    - Test seeds TWO tenants end-to-end (auth.users → profiles → scenarios) and captures each scenarios row id.
    - Asserts on cross-tenant CONTENT by specific row id (A sees scen_a_id, A canNOT see scen_b_id) — NOT a count or pg_policies presence check.
    - Negative write path: A's UPDATE/DELETE of scen_b_id affects 0 rows AND B's row is verified unchanged by id.
    - Positive path: A can update + delete its OWN row.
    - Plain PL/pgSQL DO blocks with RAISE EXCEPTION; no pgTAP; no \!/\copy/\o meta-commands.
    - Teardown cascades via auth.users delete + repeated email-based DELETE.
  </acceptance_criteria>
  <done>RLS test asserts cross-tenant content by row id (read + write) and the positive own-row path; runs under psql ON_ERROR_STOP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Hand-patch database.types.ts + pin with expectTypeOf</name>
  <files>src/lib/database.types.ts, src/lib/database.types.test.ts</files>
  <read_first>
    - src/lib/database.types.ts:1-48 (GENERATED-FILE header — preserve untouched) and :1021-1097 (for_quants_leads HAND-PATCHED Row/Insert/Update/Relationships block to mirror)
    - src/lib/database.types.test.ts:12-25 (expectTypeOf drift-pin pattern)
    - src/__tests__/critical-regressions.test.ts:54-101 (the [#14] block — greps for HAND-PATCHED + migration 115 strings; do NOT strip them)
  </read_first>
  <behavior>
    - database.types.test.ts: scenarios Row exposes schema_version: number, draft: Json (generated alias), name/allocator_id/id/created_at/updated_at: string.
    - database.types.test.ts: scenarios Insert makes defaulted cols optional (NonNullable<Insert["id"]> is string), name+draft+schema_version required.
    - critical-regressions [#14] still passes — HAND-PATCHED + migration 115 tripwire strings survive the edit.
  </behavior>
  <action>
    Hand-ADD a `scenarios` block inside `Database["public"]["Tables"]` in `src/lib/database.types.ts` (do NOT run `supabase gen types` — it cannot be regenerated without prod DB access and would strip the [#14] tripwire comments). Mirror the for_quants_leads quad exactly: Row { id: string; allocator_id: string; name: string; draft: Json; schema_version: number; created_at: string; updated_at: string }; Insert makes id?/created_at?/updated_at? optional, name+draft+schema_version required; Update makes all optional; Relationships = one FK entry to profiles (and public_profiles) mirroring the for_quants_leads_processed_by_fkey shape at lines 1081-1095 (referencedColumns ["id"]). Add a tripwire comment in the same style: `// HAND-PATCHED — scenarios added by migration 20260621120000; a regen linked to a project missing this migration silently reverts the block`. Preserve the GENERATED-FILE header and the existing HAND-PATCHED / migration 115 comments verbatim. Then extend `src/lib/database.types.test.ts` with a describe/it block pinning the new columns' presence + nullability per the behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/lib/database.types.test.ts src/__tests__/critical-regressions.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - scenarios Row/Insert/Update/Relationships added by hand (no gen types run); GENERATED-FILE header + migration-115 tripwire comments intact.
    - A new `HAND-PATCHED — scenarios added by migration 20260621120000` comment present.
    - database.types.test.ts has expectTypeOf pins for schema_version (number), draft (Json), and Insert optionality of defaulted cols.
    - critical-regressions [#14] passes (tripwire strings survive).
    - npm run typecheck clean.
  </acceptance_criteria>
  <done>scenarios types hand-patched, expectTypeOf-pinned, [#14] guard green, typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → Postgres (RLS) | Each allocator's queries cross into the shared scenarios table; RLS is the sole tenant gate |
| migration → live prod DB | DDL applied at /land-and-deploy, anon NO-EXEC verified (executor never pushes) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | scenarios_owner SELECT | mitigate | RLS USING (allocator_id = auth.uid()); proven by test_scenarios_rls.sql asserting A cannot see B's row by id (RLS fails silently → content assertion, not policy presence) |
| T-23-02 | Tampering | scenarios_owner UPDATE/DELETE | mitigate | RLS WITH CHECK + USING; test asserts A's UPDATE/DELETE of B's row affects 0 rows and B's row is unchanged by id |
| T-23-03 | Tampering | database.types.ts regen | mitigate | hand-patch only; [#14] critical-regressions guard asserts HAND-PATCHED tripwire strings survive |
| T-23-04 | Tampering | backdated migration | mitigate | forward timestamp (> 20260620120000) clears migration-policy.yml backdating gate |
</threat_model>

<verification>
- `npx vitest run src/lib/database.types.test.ts src/__tests__/critical-regressions.test.ts` green.
- `npm run typecheck` clean.
- Migration + rollback + RLS test files present with correct content (grep gates in Task 1/2).
- At /land-and-deploy: migration applies, anon query of scenarios denied/0-rows (manual land-time verify, NOT an autonomous task).
</verification>

<success_criteria>
- scenarios table + owner RLS + name CHECK + list index defined in a forward migration with rollback.
- Cross-tenant content isolation proven by row-id assertions in test_scenarios_rls.sql.
- database.types.ts hand-patched with scenarios Row/Insert/Update, tripwire intact, expectTypeOf-pinned.
</success_criteria>

<output>
Create `.planning/phases/23-scenario-persistence-compare/23-01-SUMMARY.md` when done.
</output>
