---
phase: 25-read-only-sharing
plan: 04
type: execute
wave: 2
depends_on: ["25-01", "25-02"]
files_modified:
  - src/app/scenario-share/[token]/share-resolve.ts
  - src/app/scenario-share/[token]/share-resolve.test.ts
  - src/app/scenario-share/[token]/page.tsx
  - src/app/scenario-share/[token]/page.test.tsx
autonomous: true
requirements: [SHARE-02, SHARE-03]
must_haves:
  truths:
    - "A recipient opens /scenario-share/<token> and sees the scenario name + PROJECTED framing + KPI strip + EquityChart (+ benchmark overlay if available) + CorrelationHeatmap — in return/percentage form only, with no allocator identity, no USD AUM, no holdings/api_keys, no dashboard nav"
    - "An invalid / revoked / unknown token → notFound() (global not-found.tsx), never a partial render or a different scenario"
    - "A version-ahead / undecodable / dangling-ref draft → honest-absence EmptyStateCard ('This shared scenario can't be displayed'), NEVER a live-book substitution (the DI-23-01 landmine)"
    - "An empty-addedStrategies share → honest correlation/projection empty states (series=[] is expected, not a bug)"
    - "The page is force-dynamic; after a revoke the next load 404s (no edge cache outlives the revoked_at write)"
    - "The public route is rate-limited limit-first with publicIpLimiter before any DB/crypto work"
  artifacts:
    - path: "src/app/scenario-share/[token]/page.tsx"
      provides: "public force-dynamic RSC: limit→hashShareToken→RPC→codec-outcome branch→compute→reused components"
      contains: "force-dynamic"
    - path: "src/app/scenario-share/[token]/share-resolve.ts"
      provides: "pure helper: decode draft via codec, map outcome→ok|honest-absence (DI-23-01), shape series for computeScenario"
    - path: "src/app/scenario-share/[token]/share-resolve.test.ts"
      provides: "version-ahead/garbage draft → honest-absence (NOT a rendered curve) unit test"
  key_links:
    - from: "page.tsx"
      to: "get_shared_scenario RPC (Plan 25-01) via createAdminClient + hashShareToken (Plan 25-02)"
      via: "admin.rpc('get_shared_scenario', { p_token_hash: hashShareToken(token) })"
      pattern: "get_shared_scenario|hashShareToken"
    - from: "share-resolve.ts"
      to: "scenarioDraftCodec outcome branch"
      via: "only outcome==='ok' renders; readonly/reset → honest-absence; never read .value on non-ok"
      pattern: "outcome|honest"
---

<objective>
Build the recipient side: the public `src/app/scenario-share/[token]/page.tsx` (a `force-dynamic` RSC that rate-limits, hashes the URL token, calls the `get_shared_scenario` RPC via `createAdminClient`, branches on the codec outcome, server-computes the projection + correlation + benchmark, and renders the reused props-only components in return/percentage form) and a pure `share-resolve.ts` helper isolating the DI-23-01 honest-absence decision so it is unit-testable without mounting the page.

Purpose: SHARE-02 (recipient view with zero leak) + the route-layer arm of SHARE-03 (force-dynamic + no-store → revoke is immediate, no cache outlives it). The single highest-risk decision is already locked upstream (the RPC returns only addedStrategies[].id published series); this plan's risk is the DI-23-01 landmine — the dashboard codec's `version_ahead`/`reset` outcomes carry a `value = defaultDraft` that, on a PUBLIC page, would surface a live-book-shaped object. The page MUST branch on `outcome` and render honest-absence for `readonly`/`reset`, NEVER read `.value` on a non-`"ok"` outcome.
Output: the public page + the pure resolve helper + their tests (incl. the resolve→revoke→404 page test).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/25-read-only-sharing/25-CONTEXT.md
@.planning/phases/25-read-only-sharing/25-RESEARCH.md
@.planning/phases/25-read-only-sharing/25-PATTERNS.md
@.planning/phases/25-read-only-sharing/25-UI-SPEC.md

<interfaces>
<!-- Contracts this plan consumes. Extracted from the codebase + Plans 25-01/25-02. -->

From Plan 25-02 (src/lib/scenario-share-token.ts): hashShareToken(raw: string): string   // page hashes the URL token, passes p_token_hash
From Plan 25-01 RPC: get_shared_scenario(p_token_hash TEXT) → rows { name TEXT, draft JSONB, schema_version INT, series JSONB }
  series = [{ strategy_id: uuid, daily_returns: [...] }]  (addedStrategies[].id published only; [] when none)

Codec (the DI-23-01 landmine) — src/app/(dashboard)/allocations/lib/scenario-state.ts:554-608, 57:
  scenarioDraftCodec(defaultDraft: ScenarioDraft, raw): { value: ScenarioDraft; outcome: "ok"|"readonly"|"reset"; reason: string|null }
  outcome "readonly" = version_ahead (schema_version > SCENARIO_SCHEMA_VERSION=2); "reset" = parse_failed/schema_invalid/version_mismatch
  value = defaultDraft on readonly/reset  ← on a PUBLIC page rendering this is the live-book substitution leak. NEVER read .value on non-"ok".
  SCENARIO_SCHEMA_VERSION = 2 (line 57) — pin version-ahead against the constant, NOT "1".

Compute engines (pure, run server-side):
  computeScenario(strategies, state, dateMapCache) → ComputedMetrics { portfolio_daily_returns, correlation_matrix, avg_pairwise_correlation, equity_curve, … }  — src/lib/scenario.ts:149 (degenerate all-null shape when activeIds.length===0, scenario.ts:157-173)
  computeScenarioBenchmark(portfolioDaily: DailyPoint[], btcDaily: DailyPoint[]) → ScenarioBenchmark (TE/IR/α/β, 252-annualized)  — scenario-benchmark.ts:97

Public-page precedents:
  src/app/demo/page.tsx:1-6,38 — `export const dynamic="force-dynamic"` + createAdminClient + SECURITY BOUNDARY comment
  src/app/factsheet/[id]/v2/page.tsx:219-277 — `const { id } = await params` (Next 16) + gate → notFound()
  src/app/api/demo/portfolio-pdf/[id]/route.ts:39-49,113-115 — publicIpLimiter limit-first + the no-store cache-control reasoning comment
  src/lib/supabase/admin.ts:10 createAdminClient(); src/lib/ratelimit.ts publicIpLimiter(10,"60 s"),checkLimit,getClientIp,rateLimitDenyJson

Reused presentational (props-only, NO new visuals): EquityChart; CorrelationHeatmap (src/components/portfolio/CorrelationHeatmap.tsx); ScenarioBenchmarkSection (src/app/(dashboard)/allocations/components/ScenarioBenchmarkSection.tsx); EmptyStateCard ({heading, body}, src/components/ui/EmptyStateCard.tsx:23)
Benchmark series: GET /api/benchmark/btc (public, cacheable — do NOT add no-store to it)
Global 404: src/app/not-found.tsx (notFound() resolves to it)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: share-resolve.ts pure helper + DI-23-01 honest-absence unit test</name>
  <files>src/app/scenario-share/[token]/share-resolve.ts, src/app/scenario-share/[token]/share-resolve.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.ts:554-608,57 (scenarioDraftCodec outcomes + SCENARIO_SCHEMA_VERSION=2 + the defaultDraft landmine)
    - src/lib/scenario.ts:149,157-173 (computeScenario signature + the degenerate all-null shape for activeIds.length===0)
    - .planning/phases/25-read-only-sharing/25-RESEARCH.md (§Pitfall 3 DI-23-01 + §Architecture Pattern 2 snapshot/empty-addedStrategies expected state)
    - .planning/phases/25-read-only-sharing/25-PATTERNS.md (Pattern Assignment 6 DI-23-01 + Assignment 9 share-resolve test)
    - src/app/(dashboard)/allocations/lib/scenario-benchmark.ts:97 (computeScenarioBenchmark signature)
  </read_first>
  <behavior>
    - resolveSharedScenario(rpcRow) returns a discriminated result: `{ kind: "ok"; name; metrics; portfolioDaily; … }` ONLY when the codec outcome is `"ok"`; `{ kind: "honest-absence" }` when the codec outcome is `"readonly"` (version_ahead, schema_version > 2) or `"reset"` (parse_failed/schema_invalid/version_mismatch). It NEVER reads the codec `.value` on a non-`"ok"` outcome (no live-book substitution).
    - It does NOT pass a live-book-shaped defaultDraft to the codec — it passes an empty/neutral default so a non-ok outcome carries no holdings (and is discarded anyway).
    - For an `"ok"` draft, it maps the RPC `series` ([{strategy_id, daily_returns}]) into the `StrategyForBuilder`-shaped input computeScenario expects, runs computeScenario, and (given a btc series) computeScenarioBenchmark — server-side, returning return/percentage-form metrics (no USD).
    - Empty-addedStrategies (series=[]) → computeScenario's degenerate all-null shape → the result still `kind:"ok"` but the components render their honest correlation/projection empty states (expected, not a bug).
  </behavior>
  <action>
    Create `src/app/scenario-share/[token]/share-resolve.ts` as a PURE module (no Next/RSC imports, no fetch) exporting a `resolveSharedScenario(...)` that takes the RPC row (`{ name, draft, schema_version, series }`) and an optional btc series, and returns a discriminated union `{ kind: "ok", … } | { kind: "honest-absence" }`. Decode the draft via `scenarioDraftCodec` and BRANCH ON `outcome`: only `"ok"` proceeds to compute; `"readonly"` and `"reset"` → `{ kind: "honest-absence" }` (per SHARE-02 / DI-23-01). NEVER read `.value` on a non-`"ok"` outcome. Pass a neutral/empty defaultDraft to the codec (NOT a live-book-shaped one — on a public page there is no viewer book; this guarantees no live-book object can leak even if outcome handling regressed). Map RPC `series` → the `StrategyForBuilder[]` input `computeScenario` needs (using the decoded `draft` state for weights/leverage), run `computeScenario`, and expose `portfolio_daily_returns` for the page to feed `computeScenarioBenchmark` with the BTC series. Keep it return/percentage form only — no USD. Then author `share-resolve.test.ts`: a draft blob with `schema_version` = 3 (> SCENARIO_SCHEMA_VERSION=2) → `kind:"honest-absence"` (NOT a computed result); a garbage/unparseable draft → `kind:"honest-absence"`; a valid ok draft with a published series → `kind:"ok"` with non-null metrics; an ok draft with empty series → `kind:"ok"` mapping to the degenerate all-null shape. Assert the helper NEVER returns a rendered/curve result for a non-ok outcome.
  </action>
  <verify>
    <automated>npx vitest run "src/app/scenario-share/[token]/share-resolve.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `resolveSharedScenario` returns `kind:"honest-absence"` for schema_version=3 (version-ahead) and for an unparseable draft, and NEVER reads the codec `.value` on a non-`"ok"` outcome (the test mutation-proves a version-ahead draft does NOT yield a computed/curve result).
    - For an ok draft it returns `kind:"ok"` with non-null computeScenario metrics; empty-series → the degenerate all-null shape (still `kind:"ok"`).
    - The module is pure (no `next/*` import, no fetch); it passes a neutral (non-live-book) defaultDraft to the codec. `! grep -q "next/navigation\|createAdminClient\|fetch(" "src/app/scenario-share/[token]/share-resolve.ts"`.
    - Test green via the verify command.
  </acceptance_criteria>
  <done>The pure resolve helper isolates the DI-23-01 honest-absence decision (version-ahead/garbage → honest-absence, never a live-book substitution) and the compute path, unit-tested without mounting the page.</done>
</task>

<task type="auto">
  <name>Task 2: public /scenario-share/[token] RSC page + resolve→revoke→404 page test</name>
  <files>src/app/scenario-share/[token]/page.tsx, src/app/scenario-share/[token]/page.test.tsx</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md (Next 16: `params` is a Promise — `await params`; PageProps)
    - node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md (Next 16 caching defaults — for the no-store reasoning)
    - src/app/demo/page.tsx:1-6,38 (force-dynamic + createAdminClient + SECURITY BOUNDARY comment to reproduce)
    - src/app/factsheet/[id]/v2/page.tsx:219-277 (await params + gate→notFound())
    - src/app/api/demo/portfolio-pdf/[id]/route.ts:39-49,113-115 (limit-first publicIpLimiter + the no-store cache-control reasoning comment to copy verbatim)
    - src/app/scenario-share/[token]/share-resolve.ts (Task 1 — resolveSharedScenario)
    - .planning/phases/25-read-only-sharing/25-UI-SPEC.md (§Copywriting Contract recipient side + §Interaction State Matrix recipient side + §Honesty & Safety Invariants + §Accessibility)
    - .planning/phases/25-read-only-sharing/25-PATTERNS.md (Pattern Assignment 6 — the full page composite incl. limit-first ordering, the headers() vs route-handler note, the cache-control comment, the engine signatures)
    - src/components/ui/EmptyStateCard.tsx:23; src/app/not-found.tsx
  </read_first>
  <action>
    Create `src/app/scenario-share/[token]/page.tsx` — a public RSC OUTSIDE `(dashboard)` (inherits no auth layout). `export const dynamic = "force-dynamic"` (reproduce the demo-page SECURITY BOUNDARY comment + the "revoke must be instant; no ISR/edge cache" rationale). Signature `{ params }: { params: Promise<{ token: string }> }`; `const { token } = await params` (Next 16). Steps in order (per SHARE-02 + SHARE-03):
    1. LIMIT FIRST: read the client IP via `next/headers` `headers()` (Next 16 async) and `getClientIp`, `checkLimit(publicIpLimiter, `scenario-share:${ip}`)` BEFORE any DB/crypto work; on deny render an honest rate-limited state (or notFound() — keep it indistinguishable per Pitfall 5; a minimal "try again shortly" is acceptable but must not leak existence). Mirror the demo-pdf limit-first ordering.
    2. `const admin = createAdminClient();` (service_role = transport ONLY; the RPC is the gate). `admin.rpc("get_shared_scenario", { p_token_hash: hashShareToken(token) })` — hash the URL token in Node (Plan 25-02), pass the hash (Plan 25-01 RPC takes `p_token_hash`).
    3. No row (empty result) → `notFound()` (resolves to the global `src/app/not-found.tsx`). Identical 404 for invalid / revoked / unknown — no oracle.
    4. Fetch `GET /api/benchmark/btc` (public, cacheable — do NOT add no-store to it); a 200 `[]` degrades the benchmark section to its honest "unavailable" empty state.
    5. `resolveSharedScenario(row, btc)` (Task 1). `kind:"honest-absence"` → render `EmptyStateCard` heading "This shared scenario can't be displayed" / body "The link is valid, but this scenario was saved in a newer format we can't show here." (NEVER a curve, NEVER a live-book substitution — DI-23-01). `kind:"ok"` → render the recipient view: name header (Instrument Serif, scenario NAME only — never the allocator identity/email), the framing line "Shared scenario · PROJECTED — hypothetical, not a live book", the PROJECTED pill, the methodology line, the KPI strip, `EquityChart` (+ benchmark overlay if available), `CorrelationHeatmap` (its own <2 / <10-day honest empty states), `ScenarioBenchmarkSection` (its 3 honest empty states + em-dash on degenerate). Return/percentage form ONLY — no USD, no holdings, no api_keys, no peer/percentile panels, no Save/Update/Open/edit controls, no dashboard nav/tabs.
    Copy the no-store cache-control reasoning comment verbatim from the demo-pdf route into the page (a cached response replays after the token is revoked). The page is `force-dynamic`; if a data route handler is split out it ALSO needs `Cache-Control: no-store` + `force-dynamic` (per RESEARCH Open-Question-2; recommend keeping it in the RSC for one fewer surface). Reproduce the demo SECURITY BOUNDARY comment naming that every Supabase read here is gated by the token-scoped RPC and MUST never call `getMyAllocationDashboard` / read an arbitrary id.

    Author `src/app/scenario-share/[token]/page.test.tsx` (mock `createAdminClient().rpc` + the benchmark fetch + `notFound`): (a) a resolving token renders the name + PROJECTED framing and renders NO USD/identity/api_keys/nav (assert absence of those strings + assert no `getMyAllocationDashboard` call); (b) an unknown token (RPC 0 rows) → `notFound()` called; (c) the resolve→revoke→404 arm: first call resolves (RPC returns a row) → 200 render; then mock the RPC to return 0 rows (revoked) and assert the second render calls `notFound()` — proving revoke immediacy at the route layer with no cached survivor (SHARE-03); (d) a version-ahead RPC row → the honest-absence EmptyStateCard, never a curve.
  </action>
  <verify>
    <automated>npx vitest run "src/app/scenario-share/[token]/page.test.tsx" && grep -q 'dynamic = "force-dynamic"' "src/app/scenario-share/[token]/page.tsx" && grep -q "get_shared_scenario" "src/app/scenario-share/[token]/page.tsx" && grep -q "publicIpLimiter" "src/app/scenario-share/[token]/page.tsx" && grep -q "notFound" "src/app/scenario-share/[token]/page.tsx" && ! grep -q "getMyAllocationDashboard" "src/app/scenario-share/[token]/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Page is `force-dynamic`, OUTSIDE `(dashboard)`, awaits `params`, rate-limits limit-first with `publicIpLimiter` before any DB/crypto, calls `get_shared_scenario` via `createAdminClient` with `p_token_hash: hashShareToken(token)`, and `notFound()`s on 0 rows. `! grep -q getMyAllocationDashboard` and no holdings/AUM/api_keys read.
    - Version-ahead/garbage row → the honest-absence EmptyStateCard (heading matches body), NEVER a rendered curve / live-book substitution.
    - Recipient render shows the scenario NAME only (no allocator identity), PROJECTED framing persistent, return/percentage form only (no USD), reused presentational components, no dashboard nav/edit controls.
    - The page test proves: resolve→200; unknown→notFound; resolve→revoke(RPC 0 rows)→notFound (route-layer revoke immediacy, SHARE-03); version-ahead→honest-absence.
    - The reused `GET /api/benchmark/btc` is NOT given a no-store header.
  </acceptance_criteria>
  <done>The public recipient page resolves a share token through the leak-scoped RPC under a force-dynamic + limit-first + no-store discipline, renders the reused components in return/percentage form with PROJECTED framing and no leak, honest-absences a version-ahead draft, and 404s an unknown/revoked token; the page test pins the revoke→404 immediacy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon recipient → /scenario-share/[token] page | Untrusted, sessionless; the only inputs are the URL token + IP; every Supabase read is gated by the token-scoped RPC |
| page → get_shared_scenario RPC | createAdminClient (service_role) is transport ONLY; the RPC self-scopes — the page must never query an arbitrary id or call getMyAllocationDashboard |
| revoke write (Plan 25-03) → next page load | force-dynamic + no-store guarantees no cache outlives the revoked_at write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-13 | Information Disclosure | DI-23-01 codec landmine (version_ahead/reset → defaultDraft → live-book substitution on a PUBLIC page) | mitigate | `share-resolve.ts` branches on codec `outcome`; only `"ok"` renders; `"readonly"`/`"reset"` → honest-absence; never reads `.value` on non-ok; passes a neutral (non-live-book) defaultDraft. Proven: version-ahead/garbage draft → honest-absence unit test + page test, never a curve. |
| T-25-14 | Tampering / Bypass | Revoked link still resolving from an edge cache | mitigate | `export const dynamic = "force-dynamic"` on the page + the no-store cache-control reasoning; RPC `revoked_at IS NULL` (Plan 25-01). Proven: page test resolve→revoke(RPC 0 rows)→notFound. |
| T-25-15 | Information Disclosure | page over-reads (getMyAllocationDashboard / holdings / AUM / api_keys / peer panels) | mitigate | The ONLY Supabase read is the token-scoped RPC; the SECURITY BOUNDARY comment forbids arbitrary-id reads; render is return/percentage form only. Proven: `! grep getMyAllocationDashboard`; page test asserts no USD/identity/api_keys strings + no dashboard call. |
| T-25-16 | DoS | scraping / brute-force on the public route | mitigate | `publicIpLimiter` (10/60s/IP) limit-first before any DB/crypto; `force-dynamic` page is cheap (one RPC + one cacheable benchmark fetch). Misconfig → 503 (rateLimitDenyJson). |
| T-25-17 | Spoofing (oracle) | token enumeration / timing / status oracle | mitigate | Identical `notFound()` 404 for invalid AND revoked AND unknown (no oracle distinguishing revoked from never-existed); the RPC lookup is a single indexed hash equality (no timing delta); 256-bit token (Plan 25-02). |
| T-25-07 | Information Disclosure | recipient sees absolute AUM / USD book size | mitigate | Render return/percentage form only — no USD-scaled value, no AUM-derived thresholds (UI-SPEC Honesty Invariant 1). Proven: page test asserts no USD string. |
| T-25-SC | Tampering | npm/pip/cargo installs | accept | No package installs — all in-tree (RESEARCH Package Legitimacy Audit = N/A). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run "src/app/scenario-share/[token]/share-resolve.test.ts"` — DI-23-01 honest-absence (version-ahead/garbage → honest-absence, never a curve) + ok/empty-series compute.
- `npx vitest run "src/app/scenario-share/[token]/page.test.tsx"` — resolve→200, unknown→404, resolve→revoke→404 (route-layer immediacy), version-ahead→honest-absence, no-leak assertions.
- grep gates on the page — force-dynamic, RPC call, publicIpLimiter, notFound, and absence of getMyAllocationDashboard.
- Per-wave: `npm run test:coverage` (the new page + helper must carry tests to hold the coverage ratchet).
- Next 16 page conventions verified against `node_modules/next/dist/docs/` (await params, force-dynamic) per AGENTS.md.
</verification>

<success_criteria>
- Public `/scenario-share/[token]` page is force-dynamic, limit-first, calls the leak-scoped RPC via service_role transport, and 404s unknown/revoked tokens.
- Version-ahead/undecodable draft → honest-absence EmptyStateCard, never a live-book substitution (DI-23-01 closed).
- Recipient view: scenario name only, PROJECTED framing persistent, return/percentage form only, reused components, no leak (no getMyAllocationDashboard / holdings / AUM / api_keys / peer panels / dashboard nav).
- Revoke is immediate at the route layer (resolve→revoke→404 page test); the reused BTC route keeps its cacheable header.
- share-resolve + page tests green; coverage ratchet held.
</success_criteria>

<output>
Create `.planning/phases/25-read-only-sharing/25-04-SUMMARY.md` when done.
</output>
