---
phase: 22-methodology-honesty-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
  - "src/components/scenarios/ScenarioBuilder.tsx"
  - "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
  - "src/components/scenarios/ScenarioBuilder.honesty.test.tsx"
autonomous: true
requirements: [HONEST-01]
must_haves:
  truths:
    - "The own-book composer projection caveat reads the canonical methodology line 'Historical realized · {N} overlapping days · not a forecast'"
    - "The /scenarios Sandbox projection caveat reads the identical methodology line"
    - "Both caveats keep the conditional 'Shortest history: {name}.' clause when the de-aliased set is non-empty, and omit it otherwise (never a phantom strategy)"
    - "Both caveats still carry data-testid='scenario-coverage-caveat' and the mt-2 text-[11px] text-text-muted token (Phase-21 tests stay green)"
    - "The N value is read live from the frozen engine (scenarioMetrics.n / metrics.n), never hardcoded or fabricated"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Composer methodology line (HONEST-01) — in-place copy upgrade of the Phase-21 caveat"
      contains: "Historical realized"
    - path: "src/components/scenarios/ScenarioBuilder.tsx"
      provides: "Sandbox methodology line (HONEST-01) — identical in-place copy upgrade"
      contains: "Historical realized"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "Composer caveat assertions extended for the methodology line (Phase-21 assertions retained)"
      contains: "Historical realized"
    - path: "src/components/scenarios/ScenarioBuilder.honesty.test.tsx"
      provides: "Sandbox caveat assertions; the anchored ^Projected regex at :114 updated to match the new line front"
      contains: "Historical realized"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/lib/scenario.ts ComputedMetrics.n"
      via: "scenarioMetrics.n rendered inline in the caveat <p>"
      pattern: "scenarioMetrics\\.n"
    - from: "src/components/scenarios/ScenarioBuilder.tsx"
      to: "src/lib/scenario.ts ComputedMetrics.n"
      via: "metrics.n rendered inline in the caveat <p>"
      pattern: "metrics\\.n"
---

<objective>
Upgrade the already-shipped Phase-21 coverage caveat into the canonical HONEST-01 methodology line on BOTH projection surfaces — the own-book `ScenarioComposer` and the `/scenarios` Sandbox `ScenarioBuilder` — by folding the method label and horizon into the single existing caveat `<p>`. This is a surgical in-place copy upgrade, not a feature build: edit the contents of the existing `data-testid="scenario-coverage-caveat"` paragraph, keep the testid + token, and retain the conditional shortest-history clause.

Purpose: Closes the "how was this number computed?" gap. Phase 21 shipped "Projected from N overlapping days. … Not a forecast." HONEST-01 names the ACTUAL method ("Historical realized" — the engine computes realized statistics over the overlap window; bootstrap is Phase 27, do NOT claim it here) so the disclosure states method + N + horizon in one coherent line.

Output: Two upgraded caveat lines (identical copy on both surfaces) + extended caveat tests on both, with all Phase-21 assertions still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-methodology-honesty-scaffolding/22-CONTEXT.md
@.planning/phases/22-methodology-honesty-scaffolding/22-UI-SPEC.md
@.planning/phases/22-methodology-honesty-scaffolding/22-PATTERNS.md

<read_first>
The two caveat render sites (edit the CONTENTS of the existing <p>, do NOT delete-and-recreate):
@src/app/(dashboard)/allocations/components/ScenarioComposer.tsx  (caveat at lines 1059-1068)
@src/components/scenarios/ScenarioBuilder.tsx  (caveat at lines 296-303)

The two existing caveat tests to EXTEND (keep every existing assertion green):
@src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx  (IMPACT-01 caveat block at lines 2419-2440 — uses order-agnostic .toContain)
@src/components/scenarios/ScenarioBuilder.honesty.test.tsx  (IMPACT-01 caveat block at lines 110-116 — line 114 has an ANCHORED ^Projected regex)
</read_first>

<interfaces>
<!-- Exact current render — extracted from the codebase. Edit CONTENTS only. -->

Composer caveat (ScenarioComposer.tsx:1059-1068) — current:
  <p data-testid="scenario-coverage-caveat" className="mt-2 text-[11px] text-text-muted">
    Projected from {scenarioMetrics.n} overlapping days.
    {coverageShortestName !== null ? ` Shortest history: ${coverageShortestName}.` : ""}{" "}
    Not a forecast.
  </p>

Sandbox caveat (ScenarioBuilder.tsx:296-303) — current:
  <p data-testid="scenario-coverage-caveat" className="mt-2 text-[11px] text-text-muted">
    Projected from {metrics.n} overlapping days.
    {shortestName ? ` Shortest history: ${shortestName}.` : ""} Not a forecast.
  </p>

UI-SPEC Copywriting Contract target form (middot `·` separators):
  "Historical realized · {N} overlapping days · not a forecast"
  …with the shortest-history clause retained within the SAME line when present:
  "Historical realized · {N} overlapping days · not a forecast. Shortest history: {strategyName}."

Method label VERBATIM: "Historical realized". Horizon VERBATIM: "not a forecast".
{N} stays scenarioMetrics.n / metrics.n rendered inline in DM Sans — do NOT switch to .font-metric / Geist Mono.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fold the methodology line into both caveat sites + update the anchored sandbox regex</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/components/scenarios/ScenarioBuilder.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/components/scenarios/ScenarioBuilder.honesty.test.tsx</files>
  <behavior>
    - Composer caveat (ScenarioComposer.test.tsx, IMPACT-01 block ~2419-2440): text still .toContain(`Projected from ${n} overlapping days.`) OR is restructured to assert the live N inline — but MUST additionally assert text.toContain("Historical realized") and text.toContain("not a forecast") and the `·` middot form; existing `Shortest history: ${REF_BTC}.` assertion stays.
    - Sandbox caveat (ScenarioBuilder.honesty.test.tsx, IMPACT-01 block ~110-116): the anchored `/^Projected from \d+ overlapping days\./` at line 114 is updated to match the new front, e.g. `/^Historical realized · \d+ overlapping days · not a forecast/`; existing `Shortest history: Short Leg.` (line 115) assertion stays; add `expect(text).toContain("Historical realized")`.
    - Both surfaces: when the de-aliased set is empty, the "Shortest history: {name}." clause is OMITTED (conditional retained); when present it appears within the same single line. N is the live engine value, never hardcoded.
  </behavior>
  <action>
Edit ONLY the inner text of the existing `<p data-testid="scenario-coverage-caveat" className="mt-2 text-[11px] text-text-muted">` at ScenarioComposer.tsx:1059-1068 and ScenarioBuilder.tsx:296-303 — do NOT delete and re-create the element (Pitfall 1; preserves the testid + token + Phase-21 tests). Fold method + N + horizon into ONE line per the UI-SPEC Copywriting Contract: lead with the method label "Historical realized", middot-separate the overlapping-day count, then the horizon "not a forecast" — rendered form "Historical realized · {N} overlapping days · not a forecast". Keep `{N}` sourced inline from the engine (`scenarioMetrics.n` in the composer, `metrics.n` in the sandbox) in DM Sans — do NOT switch to `.font-metric`/Geist Mono (UI-SPEC Typography note). Retain the existing conditional shortest-history clause VERBATIM in behavior — composer keys on `coverageShortestName !== null`, sandbox on `shortestName` — appending " Shortest history: {name}." within the same line only when non-null (never name a phantom strategy). Do NOT add a second `<p>` (CONTEXT: fold, do not stack). Both surfaces must read identically (CONTEXT: BOTH composer and sandbox). Per HONEST-01.

Then update the two tests in the SAME task (so the copy change and its assertions land together): in ScenarioComposer.test.tsx (order-agnostic `.toContain`) ADD assertions for "Historical realized" and the new "not a forecast" / middot form without removing the Phase-21 assertions; in ScenarioBuilder.honesty.test.tsx the **PLANNER ALERT applies** — the anchored `/^Projected from \d+ overlapping days\./` at line 114 BREAKS when "Historical realized ·" moves to the front, so update that regex to anchor on the new line front (e.g. `/^Historical realized · \d+ overlapping days · not a forecast/`) and ADD a "Historical realized" `.toContain`, keeping the `Shortest history: Short Leg.` and `Not a forecast.` assertions consistent with the final rendered casing.

Do NOT touch `src/lib/scenario.ts` / `scenario-dealias.ts` (engine FROZEN). Do NOT alter the PROJECTED badge, the parent PageHeader, or any KPI-strip rendering. Do NOT add a new token, color, or DOM element (UI-SPEC: no new visual primitive).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/components/scenarios/ScenarioBuilder.honesty.test.tsx"</automated>
    <automated>test "$(grep -c 'Historical realized' 'src/app/(dashboard)/allocations/components/ScenarioComposer.tsx' 'src/components/scenarios/ScenarioBuilder.tsx' | grep -v ':0$' | wc -l | tr -d ' ')" = "2"</automated>
    <automated>grep -c 'data-testid="scenario-coverage-caveat"' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" "src/components/scenarios/ScenarioBuilder.tsx"</automated>
  </verify>
  <done>Both caveat sites render "Historical realized · {N} overlapping days · not a forecast" with the conditional shortest-history clause retained; both keep the testid + token; ScenarioComposer.test.tsx asserts the new method label alongside the retained Phase-21 assertions; ScenarioBuilder.honesty.test.tsx's anchored regex is updated and green; engine files untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen engine → client render | `ComputedMetrics.n` flows read-only into the caveat string. No untrusted external input crosses here; `n` is a server-computed number already nulled for degenerate inputs by the frozen engine. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Information Disclosure (trust) | Methodology line copy | mitigate | Label the ACTUAL method "Historical realized" (not "bootstrap"/"forecast"); horizon "not a forecast" — the no-invented-data / no-false-precision honesty control this requirement exists to enforce. N is read live from the engine, never fabricated. |
| T-22-02 | Tampering | Inline `{N}` value | accept | `{N}` is the frozen engine's `ComputedMetrics.n`, already finite-guarded and nulled-on-degenerate upstream (scenario.ts). No new input path; no parsing of untrusted data in this plan. Low risk. |
| T-22-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs zero packages (no `npm install`); no supply-chain surface. |

No auth, session, access-control, crypto, PII, network, or persistence surface is touched (ASVS V2/V3/V4/V6 not applicable — both files are existing authed client components; copy-only edit). The dominant "threat" is the honesty/false-precision threat, mitigated by T-22-01.
</threat_model>

<verification>
- `npx vitest run` on both touched component tests is green (Phase-21 assertions + new HONEST-01 assertions).
- Both rendered caveats read "Historical realized · {N} overlapping days · not a forecast" identically.
- `data-testid="scenario-coverage-caveat"` present on both surfaces (Phase-21 testid preserved).
- `src/lib/scenario.ts` / `scenario-dealias.ts` show no diff (engine frozen).
- Per wave merge: `npm test` full suite green; phase gate `npm run test:coverage` green (lines 82 / stmts 80 / fns 74 / branches 72).
</verification>

<success_criteria>
- HONEST-01 satisfied on BOTH the own-book composer and the /scenarios Sandbox: one folded methodology line stating method + N + horizon.
- No second caveat paragraph; no token/color/DOM change; inline N in DM Sans.
- The sandbox `^Projected` anchored regex is updated in the same task as the copy change (no red test).
- Phase-21 caveat semantics ("Projected from N…" → live N; "Shortest history: {name}."; "Not a forecast.") are not lost — folded, not deleted.
</success_criteria>

<output>
Create `.planning/phases/22-methodology-honesty-scaffolding/22-01-SUMMARY.md` when done.
</output>
