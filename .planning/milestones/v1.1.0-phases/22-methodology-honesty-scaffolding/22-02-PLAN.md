---
phase: 22-methodology-honesty-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/lib/sample-floor.ts"
  - "src/lib/sample-floor.test.ts"
  - "src/components/scenarios/SampleFloorEmptyState.tsx"
  - "src/components/scenarios/SampleFloorEmptyState.test.tsx"
  - "src/__tests__/contracts/contracts-registry.test.ts"
  - "src/__tests__/contracts/REGISTRY.md"
autonomous: true
requirements: [HONEST-02]
must_haves:
  truths:
    - "A single shared minimum-sample floor (default 60 overlapping days) is defined exactly once, as an exported named constant"
    - "A gate evaluateSampleFloor(n, floor?) returns { ok, n, floor, reason } and treats null/NaN/non-finite/negative n and n < floor as below-floor, never as passing"
    - "A per-call floor override is honored (Stress/MC can pass their own bar)"
    - "A below-floor input renders the shared honest empty state, naming the actual N and the floor — never 'No data', never a fabricated number, never red / role='alert'"
    - "A regression test pins the floor value + every gate branch, and is registered in CONTRACT_GUARDS so it fails loud if dropped"
  artifacts:
    - path: "src/lib/sample-floor.ts"
      provides: "HONEST-02 single-source primitive: SAMPLE_FLOOR_OVERLAPPING_DAYS constant + evaluateSampleFloor gate + reason/empty-state copy"
      contains: "SAMPLE_FLOOR_OVERLAPPING_DAYS"
      min_lines: 30
    - path: "src/lib/sample-floor.test.ts"
      provides: "Value pin (toBe(60)) + exhaustive gate-branch coverage (Pitfall 4 defense)"
      contains: "SAMPLE_FLOOR_OVERLAPPING_DAYS"
    - path: "src/components/scenarios/SampleFloorEmptyState.tsx"
      provides: "Below-floor honest empty state — verbatim CorrelationHeatmap shell, reason-routed body naming N + floor"
      contains: "rounded-lg border border-border bg-surface"
    - path: "src/components/scenarios/SampleFloorEmptyState.test.tsx"
      provides: "Render test: body names N + floor; NOT role='alert' / not red"
    - path: "src/__tests__/contracts/contracts-registry.test.ts"
      provides: "CONTRACT_GUARDS entry for sample-floor.test.ts (fail-loud if dropped)"
      contains: "sample-floor.test.ts"
  key_links:
    - from: "src/components/scenarios/SampleFloorEmptyState.tsx"
      to: "src/lib/sample-floor.ts"
      via: "imports SAMPLE_FLOOR_OVERLAPPING_DAYS + evaluateSampleFloor + reason copy"
      pattern: "from \"@/lib/sample-floor\""
    - from: "src/__tests__/contracts/contracts-registry.test.ts"
      to: "src/lib/sample-floor.test.ts"
      via: "CONTRACT_GUARDS array entry (existence check)"
      pattern: "sample-floor\\.test\\.ts"
---

<objective>
Build the HONEST-02 single-source minimum-sample floor primitive: a new PURE module `src/lib/sample-floor.ts` (mirroring `scenario-history.ts` conventions — no fetch, no side effects, no time reads) exporting the floor constant (default 60 overlapping days), the gate function, and the below-floor reason/empty-state copy. Pin it with an exhaustive regression test, register that test in `CONTRACT_GUARDS`, and prove the below-floor honest empty state renders (naming the actual N and the floor) via a render test on a small consuming component that reuses the `CorrelationHeatmap` empty-state shell verbatim.

Purpose: HONEST-02 is the ONE source of truth for the distributional/tail sample floor that Phases 26 (Stress/VaR) and 27 (Monte-Carlo) will import — so no future feature defines its own floor. This phase BUILDS + PINS + EXPORTS the primitive and proves the empty state renders. It does NOT retrofit the 60-day gate onto the live composer/sandbox projection (RESEARCH Open Q3 — that would change Phase-21 ≥10-day behavior); per-call degenerate routing (0/1 strategy, metric-nullity) lives at the future call site.

Output: `sample-floor.ts` + its regression pin, a reason-routed `SampleFloorEmptyState` component + render test, and a CONTRACT_GUARDS registration (+ REGISTRY.md row).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-methodology-honesty-scaffolding/22-CONTEXT.md
@.planning/phases/22-methodology-honesty-scaffolding/22-UI-SPEC.md
@.planning/phases/22-methodology-honesty-scaffolding/22-PATTERNS.md

<read_first>
Convention models — mirror their structure (header, named-export, never-throws, value-pin):
@src/lib/scenario-history.ts        (pure-lib header + degenerate-safe single export)
@src/lib/min-history.ts             (self-documenting named-constant + message-helper)

Empty-state shell + reason-routing to copy VERBATIM (do NOT redesign; do NOT modify this file):
@src/components/portfolio/CorrelationHeatmap.tsx   (copy consts ~143-154; routing + shell ~171-193)

Value-pin convention (copy the `expect(CONST).toBe(value)` arm; do NOT copy the readFileSync Python parity arm — no Python floor this phase):
@src/app/(dashboard)/allocations/lib/holding-outcome-adapter.test.ts   (lines 101-112)

Registry to extend (+1 CONTRACT_GUARDS entry; do NOT touch EXPECTED_RULES):
@src/__tests__/contracts/contracts-registry.test.ts   (CONTRACT_GUARDS ~57-87; EXPECTED_RULES ~32-46; wiring test ~108)
@src/__tests__/contracts/REGISTRY.md
</read_first>

<interfaces>
<!-- Locked gate contract (CONTEXT leaves signature to discretion; RESEARCH Open Q1 + PATTERNS lock this richer shape). -->

src/lib/sample-floor.ts MUST export:

  /** Conservative distributional/tail bar — DISTINCT from correlation's 10-day bar
   *  (scenario.ts) and min-history.ts chart bars (250/365). Single source of truth;
   *  Phases 26/27 import this, never re-declare `60`. */
  export const SAMPLE_FLOOR_OVERLAPPING_DAYS = 60;

  export type SampleFloorReason = "ok" | "below-floor" | "no-usable-n";
  export interface SampleFloorVerdict { ok: boolean; n: number | null; floor: number; reason: SampleFloorReason; }

  /** Floor check on a finite overlapping-day count. n == null | !Number.isFinite(n) | n < 0 → "no-usable-n"
   *  (NEVER ok); n < floor → "below-floor"; else ok. The gate is NOT a full degenerate-input classifier:
   *  0/1-strategy and non-finite-METRICS-with-large-n route at the call site (the caller knows strategy
   *  count + whether the engine nulled metrics — see RESEARCH Pitfall 2). */
  export function evaluateSampleFloor(
    n: number | null | undefined,
    floor?: number,   // defaults to SAMPLE_FLOOR_OVERLAPPING_DAYS
  ): SampleFloorVerdict;

  // Reason/empty-state copy (UI-SPEC Copywriting Contract — exported so 26/27 reuse, never re-author):
  // heading: "Not enough history for this estimate"  (DISTINCT from correlation's "Not enough overlap to correlate")
  // below-floor body: names BOTH actual N and the floor + the consuming feature noun, e.g.
  //   "These strategies share {N} overlapping days — fewer than the {floor} needed for an honest {feature} estimate. Pick strategies with longer common history."
  // no-usable-n body: "Not enough usable return history to estimate this yet. Pick strategies with longer, cleaner common history."
  // 0/1-strategy body (call-site supplied case): "Add at least 2 active strategies with {floor}+ overlapping days for an honest estimate."

Empty-state shell to reuse VERBATIM (CorrelationHeatmap.tsx:187-192 — PINNED tokens, UI-SPEC §2):
  <div className="rounded-lg border border-border bg-surface px-4 py-8 text-center text-text-muted text-sm">
    <div className="font-semibold text-text-secondary">{reasonHeading}</div>
    <div className="mt-1 text-[11px]">{body}</div>
  </div>

CONTRACT_GUARDS entry to ADD (mirror existing object shape):
  { path: "src/lib/sample-floor.test.ts", batch: "Phase22",
    invariant: "SAMPLE_FLOOR_OVERLAPPING_DAYS=60 + gate branch behavior (HONEST-02 single source)" },
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the pure sample-floor primitive + its exhaustive regression pin</name>
  <files>src/lib/sample-floor.ts, src/lib/sample-floor.test.ts</files>
  <behavior>
    - Value pin: `expect(SAMPLE_FLOOR_OVERLAPPING_DAYS).toBe(60)` (fails loud if a future feature changes the floor instead of overriding per-call).
    - evaluateSampleFloor(120) → { ok: true, n: 120, floor: 60, reason: "ok" }.
    - evaluateSampleFloor(59) → { ok: false, n: 59, floor: 60, reason: "below-floor" }.
    - evaluateSampleFloor(60) → ok: true (boundary: >= floor passes).
    - evaluateSampleFloor(null) → { ok: false, reason: "no-usable-n", n: null }.
    - evaluateSampleFloor(undefined) → reason: "no-usable-n".
    - evaluateSampleFloor(NaN) → reason: "no-usable-n".
    - evaluateSampleFloor(Infinity) → reason: "no-usable-n".
    - evaluateSampleFloor(-5) → reason: "no-usable-n" (negative is not a usable day count).
    - evaluateSampleFloor(30, 20) → ok: true (per-call override floor=20 respected).
    - evaluateSampleFloor(30, 40) → reason: "below-floor" (override floor=40).
    - The below-floor body string names BOTH the actual N and the floor; the no-usable-n body names neither number (no fabrication).
  </behavior>
  <action>
Create `src/lib/sample-floor.ts` modeled on `scenario-history.ts`: open with the verbatim pure-TS invariant header line "Pure TypeScript — no fetch, no side effects, no DOM/time reads." followed by a doc block documenting each degenerate route AND the boundary note (per RESEARCH Pitfall 2) that this gate is a floor check on a finite overlapping-day count, NOT a full degenerate-input classifier — 0/1-strategy and non-finite-metrics-with-large-n route at the call site. Export the floor as the single self-documenting named const `SAMPLE_FLOOR_OVERLAPPING_DAYS = 60` with the comment explaining why 60 (distributional/tail) is DISTINCT from correlation's 10 (scenario.ts) and min-history.ts's 250/365 chart bars (Pitfall 3 — name it distinctively, do NOT use a `MIN_*` name that collides on grep). Export `evaluateSampleFloor(n, floor = SAMPLE_FLOOR_OVERLAPPING_DAYS)` returning `{ ok, n, floor, reason }` per the locked interface: guard `n == null || !Number.isFinite(n) || n < 0` → `reason: "no-usable-n", ok: false` FIRST (never passes — Pitfall 2); then `n < floor` → `reason: "below-floor", ok: false`; else `ok: true, reason: "ok"`. Never throw. Export the heading + the three reason-body copy builders (below-floor names N + floor + feature noun; no-usable-n names no number; 0/1-strategy names the floor) using the `min-history.ts insufficientHistoryMessage` "name actual + required" builder shape and the UI-SPEC Copywriting Contract wording. Per HONEST-02.

Create `src/lib/sample-floor.test.ts` mirroring the exhaustive degenerate matrix of `scenario-history.test.ts` (one `it` per branch) PLUS the `holding-outcome-adapter.test.ts:101-104` value-pin arm `expect(SAMPLE_FLOOR_OVERLAPPING_DAYS).toBe(60)`. Cover EVERY branch listed in <behavior> (this is the Pitfall 4 coverage defense for the blocking gate: functions 74 / branches 72) and assert each reason string names the right numbers. Do NOT copy the `readFileSync(...match.py...)` cross-runtime parity arm — there is no Python distributional floor this phase (Pitfall 3). The pin MUST fail when neutered: a consumer hardcoding `60` instead of importing the const must break this test (Rule 9/12 fail-loud; CONTEXT specifics).

Do NOT touch `src/lib/scenario.ts` (frozen). Do NOT add a Python parity test. Do NOT add an ESLint rule (deferred to 26/27 per RESEARCH A3 / B16-B17 precedent).
  </action>
  <verify>
    <automated>npx vitest run src/lib/sample-floor.test.ts --coverage.enabled=false</automated>
    <automated>grep -v '^\s*\*\|^\s*//' src/lib/sample-floor.ts | grep -c 'export const SAMPLE_FLOOR_OVERLAPPING_DAYS = 60'</automated>
  </verify>
  <done>`sample-floor.ts` exports the named floor const (60), the `evaluateSampleFloor` gate returning `{ok,n,floor,reason}` with the no-usable-n/below-floor/ok branches, and the reason copy; `sample-floor.test.ts` pins the value + every gate branch and is green; no engine/Python/ESLint changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the below-floor honest empty state + register the single-source pin</name>
  <files>src/components/scenarios/SampleFloorEmptyState.tsx, src/components/scenarios/SampleFloorEmptyState.test.tsx, src/__tests__/contracts/contracts-registry.test.ts, src/__tests__/contracts/REGISTRY.md</files>
  <behavior>
    - SampleFloorEmptyState given a below-floor verdict (n=30, floor=60) renders the heading "Not enough history for this estimate" and a body containing BOTH "30" and "60" (names actual N + floor).
    - Given a no-usable-n verdict (n=null), renders the no-usable-n body (no fabricated number).
    - The card is NOT role="alert" and uses no negative/warning color class (honest absence, not an error — UI-SPEC Color).
    - The card markup matches the pinned CorrelationHeatmap shell (rounded-lg border border-border bg-surface px-4 py-8 …).
    - contracts-registry.test.ts: the `guard exists: $path` it.each includes `src/lib/sample-floor.test.ts` and stays green; the `plugin exports exactly the expected rule set` test stays green (EXPECTED_RULES untouched).
  </behavior>
  <action>
Create `src/components/scenarios/SampleFloorEmptyState.tsx` — a small presentational client component that takes the `SampleFloorVerdict` (and an optional feature noun + strategy count for the 0/1-strategy body) and renders the verbatim `CorrelationHeatmap.tsx:187-192` shell: `<div className="rounded-lg border border-border bg-surface px-4 py-8 text-center text-text-muted text-sm">` with a `font-semibold text-text-secondary` heading and a `mt-1 text-[11px]` body. Reason-route the body exactly like `CorrelationHeatmap.tsx:171-186` (shared heading, body branched by the specific reason in precedence order): no-usable-n body, then 0/1-strategy body (call-site count), then the below-floor body that names actual N + floor. Import the heading + body copy + the gate from `@/lib/sample-floor` (do NOT re-author the strings; do NOT import or modify `CorrelationHeatmap` — copy the shell markup, reuse the lib copy). No `role="alert"`, no red/warning class (UI-SPEC Color: a below-floor state is honest absence, not an error). This proves the empty state RENDERS for export to 26/27 — do NOT wire it into the live composer/sandbox projection or gate the existing ≥10-day behavior (RESEARCH Open Q3; CONTEXT deferred).

Create `src/components/scenarios/SampleFloorEmptyState.test.tsx` (RTL render): assert the below-floor render names both N and the floor, the no-usable-n render names no number, and that the container is NOT `role="alert"` and carries no negative/warning color class (mirror the Phase-21 heatmap empty-state assertion style).

Extend `src/__tests__/contracts/contracts-registry.test.ts`: ADD exactly ONE entry to the `CONTRACT_GUARDS` array per the locked interface block (`{ path: "src/lib/sample-floor.test.ts", batch: "Phase22", invariant: "SAMPLE_FLOOR_OVERLAPPING_DAYS=60 + gate branch behavior (HONEST-02 single source)" }`). Do NOT modify `EXPECTED_RULES` and do NOT add an ESLint rule (the `plugin exports exactly the expected rule set` wiring test at ~line 108 would go red; lint rule deferred to 26/27). Add the matching human-readable row to `src/__tests__/contracts/REGISTRY.md` (same turn — REGISTRY.md and the array are edited together per its own instructions).

Do NOT touch the frozen engine, the composer/sandbox projection, or the existing correlation empty state.
  </action>
  <verify>
    <automated>npx vitest run src/components/scenarios/SampleFloorEmptyState.test.tsx src/__tests__/contracts/contracts-registry.test.ts --coverage.enabled=false</automated>
    <automated>grep -c 'sample-floor.test.ts' src/__tests__/contracts/contracts-registry.test.ts</automated>
    <automated>grep -c 'role="alert"' src/components/scenarios/SampleFloorEmptyState.tsx | grep -qx 0 && echo "no role=alert (correct)"</automated>
  </verify>
  <done>SampleFloorEmptyState renders the pinned shell with the reason-routed body naming N + floor (and a no-number no-usable-n body), is not an alert/red, and its render test is green; sample-floor.test.ts is registered in CONTRACT_GUARDS + REGISTRY.md and both the existence test and the plugin-wiring test stay green; EXPECTED_RULES untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → gate (pure fn) | `evaluateSampleFloor(n)` receives an overlapping-day count. The value originates from the frozen engine (server-computed, finite-guarded). The gate defensively re-validates `Number.isFinite` regardless. No external/untrusted input enters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-03 | Information Disclosure (trust) | Below-floor empty state | mitigate | The floor gate routes degenerate/below-floor inputs to an honest empty state that NAMES the actual N + floor and never fabricates a number — the no-invented-data / false-precision honesty control. Heading/body are reason-routed so the allocator knows what to fix. |
| T-22-04 | Tampering | `evaluateSampleFloor` input `n` | mitigate | Gate guards `n == null || !Number.isFinite(n) || n < 0` → "no-usable-n" FIRST (never passes), defending downstream 26/27 distributional estimates from non-finite poisoning (ASVS V5 light input-validation; mirrors engine's `Number.isFinite` defensiveness). Pinned by the regression test's NaN/Infinity/negative branches. |
| T-22-05 | Repudiation (drift) | Single-source floor definition | mitigate | The CONTRACT_GUARDS-registered value+branch pin fails loud if the floor constant or a gate branch is silently changed/dropped — prevents a future feature from forking its own floor (the whole point of HONEST-02). |
| T-22-SC | Tampering | npm/pip/cargo installs | N/A | This plan installs zero packages (no `npm install`); no supply-chain surface. |

No auth, session, access-control, crypto, PII, network, or persistence surface is touched (ASVS V2/V3/V4/V6 not applicable — pure lib + a presentational client component + a test-registry edit). The dominant threats are the honesty (false-precision) and drift threats, mitigated by T-22-03/04/05.
</threat_model>

<verification>
- `npx vitest run src/lib/sample-floor.test.ts` green — value pin `toBe(60)` + every gate branch (ok / below-floor / no-usable-n incl. null/NaN/Infinity/negative / per-call override).
- `npx vitest run src/components/scenarios/SampleFloorEmptyState.test.tsx` green — below-floor body names N + floor; no-usable-n body names no number; not role=alert / not red.
- `npx vitest run src/__tests__/contracts/contracts-registry.test.ts` green — `guard exists` includes `sample-floor.test.ts`; `plugin exports exactly the expected rule set` stays green (EXPECTED_RULES untouched).
- `grep -rn "SAMPLE_FLOOR_OVERLAPPING_DAYS" src/` shows the const declared in EXACTLY ONE file (`sample-floor.ts`); consumers import it.
- Per wave merge: `npm test` full suite green; phase gate `npm run test:coverage` green (lines 82 / stmts 80 / fns 74 / branches 72 — every gate branch pinned, Pitfall 4 defended).
</verification>

<success_criteria>
- HONEST-02 satisfied: ONE exported floor constant (60), a gate returning `{ok,n,floor,reason}` that never passes a null/NaN/non-finite/negative/below-floor n, with a honored per-call override.
- The below-floor honest empty state renders (verbatim CorrelationHeatmap shell), names the actual N + floor, and is not an error/red/alert.
- The single-source regression pin is registered in CONTRACT_GUARDS (+ REGISTRY.md) and fails loud if dropped.
- No new ESLint rule, no EXPECTED_RULES edit, no Python parity, no correlation-bar unification, no retrofit onto the live ≥10-day composer/sandbox projection.
</success_criteria>

<output>
Create `.planning/phases/22-methodology-honesty-scaffolding/22-02-SUMMARY.md` when done.
</output>
