---
phase: 64-presentation-purification
plan: 02
type: execute
wave: 2
depends_on: ["64-01"]
files_modified:
  - src/app/scenario-share/[token]/share-resolve.ts
  - src/app/scenario-share/[token]/share-resolve.test.ts
  - src/app/scenario-share/[token]/page.tsx
  - src/app/scenario-share/[token]/page.test.tsx
autonomous: true
requirements: [PRESENT-03]

must_haves:
  truths:
    - "A MIXED share (persisted memberKeyIds non-empty AND addedStrategies non-empty) renders the verbatim caption 'computed from this scenario's catalog strategies only' on the public share page"
    - "Catalog-only shares (memberKeyIds []) and pre-v4 shares (membership undefined) render NO caption — honest-absence-of-membership unchanged, never a caption invented for unknown membership"
    - "Book-only shares stay honest-absence 'book-only' (the ok branch is unreachable for them) — the :214 guard byte-unchanged"
    - "The caption reads ONLY the already-decoded draft JSONB (memberKeyIds + the by-construction non-empty addedStrategies) — no new RPC/SQL/query, zero private data, SECURITY BOUNDARY intact"
  artifacts:
    - path: "src/app/scenario-share/[token]/share-resolve.ts"
      provides: "ResolvedOk.isMixed boolean computed null-safely from decoded draft membership"
      contains: "isMixed"
    - path: "src/app/scenario-share/[token]/page.tsx"
      provides: "scenario-mixed-caption <p> in the header note register"
      contains: "scenario-mixed-caption"
  key_links:
    - from: "src/app/scenario-share/[token]/share-resolve.ts"
      to: "src/app/scenario-share/[token]/page.tsx"
      via: "isMixed on the kind:'ok' return → conditional caption render"
      pattern: "isMixed"
    - from: "src/app/scenario-share/[token]/page.tsx"
      to: "public-role tests"
      via: "data-testid"
      pattern: "scenario-mixed-caption"
---

<objective>
PRESENT-03: the public share page renders a one-line honest caption —
"computed from this scenario's catalog strategies only" (LOCKED VERBATIM) —
when the shared draft is MIXED: persisted `memberKeyIds` non-empty AND
`addedStrategies` non-empty (red-team F3: book legs blend with catalog adds,
but only the catalog legs are publicly computable, so the projection is the
renormalized added legs).

Also carries the PHASE GATE (wave 2, after 64-01): full suite + coverage
ratchet + lint + tsc + GUARD-03.

Purpose: honest presentation for mixed shares, zero private data.
Output: `isMixed` threaded through `ResolvedOk`; caption in the share page's
note register; RED-first tests for all three caption conditions; phase gate green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/64-presentation-purification/64-CONTEXT.md
@.planning/phases/64-presentation-purification/64-UI-SPEC.md (Contract B — styling is EXACT)
@.planning/phases/64-presentation-purification/64-VALIDATION.md
@DESIGN.md
@src/app/scenario-share/[token]/share-resolve.ts
@src/app/scenario-share/[token]/page.tsx

<interfaces>
<!-- Grounded anchors, verified at planning time. -->

share-resolve.ts:
- `const draft = decoded.value;` (~:138 — only the `outcome === "ok"` branch reads it; DI-23-01).
- Book-only guard at :214: `if (strategies.length === 0) return { kind: "honest-absence", reason: "book-only" };`
  → the `kind:"ok"` branch is only reachable with addedStrategies non-empty
  (the "addedStrategies non-empty" half of MIXED is guaranteed BY CONSTRUCTION).
- The ok return (:267–273):
```typescript
return {
  kind: "ok",
  name: row.name,
  metrics,
  portfolioDaily,
  strategyNames,
};
```
- `ResolvedOk` interface at ~:64–75 (kind/name/metrics/portfolioDaily/strategyNames) — gains `isMixed: boolean`.
- `ScenarioDraft.memberKeyIds: string[]` is REQUIRED at the type level (v4) but the
  tolerant codec decodes pre-v4 blobs with it undefined AT RUNTIME — hence the
  null-safe `(draft.memberKeyIds ?? []).length` idiom (the Phase-62
  `isBookOnlyDraft` precedent).

page.tsx:
- `:214` — the methodologyLine sibling the caption follows:
```tsx
<p className="mt-1 text-xs text-text-muted">{methodologyLine(metrics.n)}</p>
```
  inside `<header>`; the caption is the NEXT sibling `<p>`, same classes.
- SECURITY BOUNDARY comment block at the top (:1–11) — byte-untouched; the page's
  ONLY Supabase read stays the token-scoped RPC.

Existing test fixtures to reuse:
- share-resolve.test.ts already carries `memberKeyIds: []` fixtures (:82, :236,
  :282) and an OMITTED-membership fixture (:556–568, "memberKeyIds intentionally
  OMITTED — the null-safe path under test") — the pre-v4 idiom to copy.
- page.test.tsx is a route-layer harness: rpcMock drives the RPC row,
  renderToStaticMarkup renders the real RSC (share-resolve runs for REAL — it is
  pure), notFound/headers/ratelimit/admin mocked. The admin `from()` throws to
  prove no arbitrary table read.
- IMPORTANT render detail: React escapes apostrophes in static markup as
  `&#x27;` — assert the caption copy with an entity-tolerant regex, e.g.
  `/computed from this scenario(?:&#x27;|&apos;|')s catalog strategies only/`,
  never a raw `toContain("scenario's")`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — isMixed + caption-condition tests (mixed / catalog-only / pre-v4)</name>
  <files>src/app/scenario-share/[token]/share-resolve.test.ts, src/app/scenario-share/[token]/page.test.tsx</files>
  <read_first>
    - .planning/phases/64-presentation-purification/64-UI-SPEC.md (Contract B — render condition + exact styling)
    - src/app/scenario-share/[token]/share-resolve.test.ts (fixture helpers; :556–568 omitted-membership idiom)
    - src/app/scenario-share/[token]/page.test.tsx (rpcMock harness + existing no-USD pins)
  </read_first>
  <behavior>
    - Test 1 (RED anchor, resolve layer): mixed draft (memberKeyIds: [one api-key
      UUID] + 1 addedStrategy + its series row) → kind "ok" AND isMixed === true.
    - Test 2: catalog-only draft (memberKeyIds: []) → kind "ok" AND isMixed === false.
    - Test 3: pre-v4 draft (memberKeyIds OMITTED, decodes ok via the upgrade
      path) → kind "ok" AND isMixed === false (never invent a caption for
      unknown membership).
    - Test 4 (RED anchor, page layer): mixed-draft RPC row → static markup
      contains data-testid="scenario-mixed-caption", the verbatim copy
      (entity-tolerant regex), classes mt-1 text-xs text-text-muted, and NO
      role attribute on the caption node.
    - Test 5: catalog-only row AND omitted-membership row → markup does NOT
      contain "scenario-mixed-caption".
  </behavior>
  <action>
    share-resolve.test.ts — add a new
    `describe("Phase 64 / PRESENT-03 — isMixed (mixed-share caption condition)")`
    with Tests 1–3, reusing the existing fixture helpers. The pre-v4 fixture
    copies the :556–568 omitted-membership idiom (a lower-schema-version draft
    whose decode upgrades to ok with membership undefined at runtime). Do NOT
    touch the existing book-only honest-absence tests or the :82/:236/:282
    fixtures — Test 2 builds its OWN fixture. Note in a comment that
    book-only-with-members is unreachable on the ok branch (the :214
    strategies.length guard), so isMixed needs no book-only case.

    page.test.tsx — add Tests 4–5 inside the existing harness (rpcMock returns
    the crafted row; renderToStaticMarkup the page element as the existing tests
    do). Assert the copy with the entity-tolerant regex from <interfaces> (React
    escapes the apostrophe as &#x27; in static markup). Also assert the existing
    no-USD invariant still holds on the mixed render (the caption introduces no
    "$"). Vitest transpiles without type-checking, so accessing the not-yet-
    existing `isMixed` field runs and fails honestly (undefined !== true) — that
    plus the missing testid are the genuine RED anchors; the absence tests
    (2/3/5) are green-by-vacuity pre-implementation, which is acceptable.

    Run both suites, RECORD the red on Tests 1 and 4. Commit:
    `test(64-02): RED — PRESENT-03 isMixed + mixed-share caption conditions`
  </action>
  <verify>
    <automated>npx vitest run "src/app/scenario-share/[token]/share-resolve.test.ts" "src/app/scenario-share/[token]/page.test.tsx" --no-file-parallelism 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - Tests 1 and 4 FAIL against current code (red recorded); all pre-existing
      tests in both files still pass (no collateral).
    - `grep -c 'PRESENT-03' "src/app/scenario-share/[token]/share-resolve.test.ts"` ≥ 1.
    - `grep -c 'scenario-mixed-caption' "src/app/scenario-share/[token]/page.test.tsx"` ≥ 2
      (positive + negative pins).
    - Existing book-only + no-USD test bodies byte-unchanged (`git diff` on the two
      test files shows only ADDED blocks).
    - RED commit exists.
  </acceptance_criteria>
  <done>All three caption conditions are pinned RED-first at both the resolve and page layers.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — thread isMixed through ResolvedOk; render the caption in the note register</name>
  <files>src/app/scenario-share/[token]/share-resolve.ts, src/app/scenario-share/[token]/page.tsx</files>
  <read_first>
    - .planning/phases/64-presentation-purification/64-UI-SPEC.md (Contract B styling block — EXACT)
    - src/app/scenario-share/[token]/share-resolve.ts (:60–80 ResolvedOk, :214 guard, :267–273 ok return)
    - src/app/scenario-share/[token]/page.tsx (:195–232 header + KPI section; the resolved destructure site)
  </read_first>
  <action>
    share-resolve.ts:
    1. Add `isMixed: boolean` to the `ResolvedOk` interface with a doc comment:
       PRESENT-03 / red-team F3 — the draft blends persisted book members
       (memberKeyIds) with catalog adds; only the catalog legs are publicly
       computable, so the rendered projection is the renormalized added legs.
       Computed from the already-decoded draft JSONB ONLY — no RPC/SQL change.
    2. In the `kind:"ok"` return (:267–273) add
       `isMixed: (draft.memberKeyIds ?? []).length > 0,` — null-safe `?? []`
       (the Phase-62 isBookOnlyDraft precedent: pre-v4 decode leaves membership
       undefined at runtime despite the required v4 type → falsy → false → no
       caption). Comment that the addedStrategies-non-empty half of MIXED is
       guaranteed by the :214 book-only guard, so the ok branch needs only the
       membership check. The :214 guard itself is byte-untouched.

    page.tsx:
    3. Surface `isMixed` from the resolved-ok object at the existing consumption
       site (add it to however the page currently reads name/metrics off
       `resolved` — match the existing style, no restructure).
    4. Insert the caption as the sibling `<p>` IMMEDIATELY AFTER the
       methodologyLine paragraph (:214), inside `<header>` — exact markup from
       the UI-SPEC (locked):
       `{isMixed && (`
       `  <p data-testid="scenario-mixed-caption" className="mt-1 text-xs text-text-muted">`
       `    computed from this scenario&apos;s catalog strategies only`
       `  </p>`
       `)}`
       Copy VERBATIM — lowercase leading "computed", no trailing period,
       `&apos;` escape per the file's JSX prose convention. NO role attribute,
       NO accent/warning/positive/negative classes (honesty-color rule). Do NOT
       place it in the KPI grid, chart section, or correlation card.
    5. SECURITY BOUNDARY comment, imports, and everything else on the page
       byte-untouched — no new Supabase read, no client-module import
       (page-server-boundary.test.ts must stay green).

    Commit: `feat(64-02): PRESENT-03 — mixed-share honesty caption on the public share page`
  </action>
  <verify>
    <automated>npx vitest run "src/app/scenario-share/[token]/share-resolve.test.ts" "src/app/scenario-share/[token]/page.test.tsx" "src/app/scenario-share/[token]/page-server-boundary.test.ts" --no-file-parallelism 2>&1 | tail -5 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 3 suites green (Tasks-1 RED tests now pass); tsc 0 errors; `npm run lint`
      0 new errors on the 2 touched files.
    - `grep -c 'scenario-mixed-caption' "src/app/scenario-share/[token]/page.tsx"` = 1.
    - `grep -c "catalog strategies only" "src/app/scenario-share/[token]/page.tsx"` = 1
      (verbatim copy present exactly once).
    - `grep -n 'role=' "src/app/scenario-share/[token]/page.tsx"` shows NO role on the
      caption line; `grep -n 'text-positive\|text-negative\|accent' ...page.tsx` adds
      no new match vs pre-edit.
    - `grep -c 'isMixed' "src/app/scenario-share/[token]/share-resolve.ts"` ≥ 2
      (interface + return).
    - `git diff` on share-resolve.ts touches ONLY the ResolvedOk interface + the ok
      return (the :214 guard and everything else byte-unchanged).
  </acceptance_criteria>
  <done>Mixed shares render the locked caption in the note register; catalog-only/pre-v4 render none; resolve layer exposes isMixed from decoded JSONB only.</done>
</task>

<task type="auto">
  <name>Task 3: Phase gate — full suite + coverage ratchet + lint + tsc + GUARD-03</name>
  <files>(verification only — no file modifications expected)</files>
  <read_first>
    - .planning/phases/64-presentation-purification/64-VALIDATION.md (wave-gate contract)
  </read_first>
  <action>
    Runs AFTER 64-01 (this plan is wave 2) — gates the WHOLE phase:
    1. `npm run test:coverage` — full vitest suite, 0 failed; coverage thresholds
       (lines 82 / statements 80 / functions 74 / branches 72) enforced by
       vitest.config.ts; RECORD the actuals in the SUMMARY.
    2. `npm run lint` — 0 errors (1 pre-existing frozen-EquityChart warning is
       the known baseline).
    3. `npx tsc --noEmit` — 0 errors.
    4. GUARD-03: `git diff $(git merge-base origin/main HEAD) -- src/lib/scenario.ts src/lib/scenario-window.ts` empty.
    5. Phase-requirement greps (paired positive/negative):
       - `grep -n 'label: "AUM"' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` → empty
         AND `grep -c '@lg:grid-cols-4' ...KpiStrip.tsx` = 1 (PRESENT-01).
       - composer commit-modal blocks green within the full run + ScenarioComposer.test.tsx
         has zero diff vs its pre-phase state (PRESENT-02).
       - `grep -c 'scenario-mixed-caption' "src/app/scenario-share/[token]/page.tsx"` = 1 (PRESENT-03).
    6. If any gate is red: fix at root within this plan's scope, or FAIL LOUD in
       the SUMMARY — never mark the phase gate passed with a skip.

    No commit expected. LAND-step reminder for /ship (NOT a task): bump VERSION +
    package.json in the SAME commit; all Phase-64 plans land together on
    v1.6-membership-schema-v4.
  </action>
  <verify>
    <automated>npm run test:coverage 2>&1 | tail -15 && npm run lint 2>&1 | tail -3 && npx tsc --noEmit && git diff --quiet $(git merge-base origin/main HEAD) -- src/lib/scenario.ts src/lib/scenario-window.ts && echo PHASE-GATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - Full suite 0 failed; coverage actuals recorded and above the 82/80/74/72 ratchet.
    - lint 0 errors; tsc 0 errors; PHASE-GATE-OK printed.
    - All three PRESENT greps pass as listed.
  </acceptance_criteria>
  <done>Phase 64 gate green: full suite + ratchet + lint + tsc + GUARD-03 + requirement greps all recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous client → /scenario-share/[token] SSR | PUBLIC sessionless route; token-scoped SECURITY DEFINER RPC is the sole gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-03 | I | share-resolve isMixed | mitigate | boolean computed from the already-decoded, leak-scoped draft JSONB; no new query — page.test's admin `from()` throw-guard proves no arbitrary table read; DI-23-01 outcome-branching untouched (isMixed only on the ok return) |
| T-64-04 | I | caption copy | mitigate | static locked literal naming no member/key/identity/USD; verbatim grep gate; existing no-USD pin re-asserted on the mixed render |
| T-64-05 | S/D | public route surface | accept | no new endpoint; existing rate-limit + token gate + force-dynamic unchanged; the caption adds one static text node |
| T-64-SC | Tampering | npm/pip installs | accept | no packages installed in this phase — no install tasks exist; legitimacy gate N/A |
</threat_model>

<verification>
- share-resolve + page + page-server-boundary suites green; RED anchors flipped.
- Phase gate: full `npm run test:coverage` 0 failed + ratchet; lint; tsc; GUARD-03 zero-diff.
- axe note: the caption is a static non-interactive `<p>` in the blessed muted
  register (4.85:1 at 12px, AA) — the composed axe scans stay green by construction;
  no new interactive node.
- Live public-role spot check of the caption is Phase 65 GUARD-04 (per
  64-VALIDATION.md manual-only map); no scenario-share e2e spec exists today
  (planning-time grep) — nothing to keep green in e2e/.
</verification>

<success_criteria>
- Mixed share → verbatim caption in the note register; catalog-only + pre-v4 → none;
  book-only unreachable and untouched.
- isMixed reads decoded draft JSONB only; SECURITY BOUNDARY byte-intact.
- 2 commits (test RED + feat GREEN); phase gate recorded green.
- VERSION + package.json bump stays a LAND-step note, not a task commit.
</success_criteria>

<output>
Create `.planning/phases/64-presentation-purification/64-02-SUMMARY.md` when done
(include coverage actuals and the phase-gate results).
</output>
