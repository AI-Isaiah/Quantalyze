---
phase: 64-presentation-purification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/KpiStrip.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.test.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.warmup.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [PRESENT-01, PRESENT-02]

must_haves:
  truths:
    - "The scenario tab's KPI strip renders exactly 4 return-form cells — YTD TWR, Sharpe, Max DD 12m, Avg |ρ| — with no AUM / dollar figure (matches the share page's 'No USD, no AUM' contract)"
    - "The strip reflows 1-up mobile / 2-up @sm / 4-up @lg with the @container host and every cell's font-mono tabular-nums byte-intact (Phase-52 invariants)"
    - "Commit diff modal weight × AUM sizing behaves exactly as before — the commit-modal test blocks (T_C_P1933 :2016, NEW-C18-07 :2740, NEW-C18-05 :2775, IMP-3 :2962, H-0133 :2990) pass with ZERO diff hunks touching them; ScenarioComposer.test.tsx's ONLY diff is the reviewed :769-798 legacy-v1-draft discriminator re-point"
    - "Degenerate state unchanged: all-excluded → null KPIs → em-dash; the scenario-data-sources-empty honest card untouched"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/KpiStrip.tsx"
      provides: "4-entry cells[] (no AUM entry) + @lg:grid-cols-4 grid; aum prop fully deleted from KpiStripProps"
      contains: "@lg:grid-cols-4"
    - path: "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx"
      provides: "4-cell shape pin (labels in canonical order, AUM absent)"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "KpiStrip"
      via: "sole production mount (~:3533, mode='scenario')"
      pattern: "<KpiStrip"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "commit diff modal"
      via: "scenarioAum (computation + commit-boundary consumers byte-unchanged)"
      pattern: "scenarioAum"
---

<objective>
PRESENT-01: remove the AUM KPI from the scenario tab's KPI strip so it becomes
return-form only (the scenario-tab analog of the public share page's existing
"No USD, no AUM" contract), reflowing the strip to 4 cells at @lg. Nothing
replaces the AUM slot (no-invented-data).

PRESENT-02 (a GUARD, not a change): commit sizing keeps reading `scenarioAum`
at the COMMIT boundary (diff modal) byte-unchanged — only the KPI-strip
CONSUMER of the AUM number is removed. The `aum` prop is deleted from
KpiStripProps ENTIRELY (orchestrator decision: purification phase — dead
required API must not survive; the production sole-mount is already verified),
which schedules the ~39 `aum={...}` test-mount removals as reviewed acts. The
`scenarioAum` VALUE and its commit-modal consumers remain untouchable; the
PRESENT-02 lock is on the commit-modal test BLOCKS (byte-unchanged), not on a
whole-file zero-diff — ScenarioComposer.test.tsx needs one reviewed
discriminator re-point (:769-798) because its :797 assertion reads
`kpiProps?.aum` off the mocked strip.

Purpose: position-space facts leave the scenario tab's presentation (phase goal).
Output: 4-cell return-form KpiStrip with no aum API; re-pointed strip tests
(reviewed acts); documented golden/e2e sweep; commit-modal blocks green with
zero hunks touching them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/64-presentation-purification/64-CONTEXT.md
@.planning/phases/64-presentation-purification/64-UI-SPEC.md
@DESIGN.md
@src/app/(dashboard)/allocations/components/KpiStrip.tsx

<interfaces>
<!-- Grounded anchors, verified at planning time (line refs are pre-edit). -->

KpiStrip.tsx (the ONE file that owns the strip):
- Props interface: `aum: number | null` at :64 — DELETED ENTIRELY by this plan
  (orchestrator decision, option a); `total_aum?: number | null` on the
  analytics shape at :48.
- `const aumValue: number | null = aum ?? analytics?.total_aum ?? null;` at :324.
- `cells: Cell[]` at :372 — FIVE entries; the FIRST is the AUM cell (:373–389,
  `label: "AUM"`, `metricKey: null`, sub-copy "Projected — sum of enabled holdings"
  in scenario mode). Remaining four (in order): `YTD TWR` (:391), `Sharpe` (:402),
  `Max DD 12m` (:413), `Avg |ρ|` (:429).
- Grid at :469: `className="grid grid-cols-1 gap-3 @sm:grid-cols-2 @lg:grid-cols-5"`
  inside the `@container` host div at :467 (host and grid are SEPARATE elements —
  Phase 52-02 containment invariant; do not merge them).
- `resolveSub(raw, isAum, defaultSub)` at :344 — the `isAum` param exists ONLY for
  the AUM warm-up exemption (Phase 07 / 07-03 f9); every non-AUM caller passes false.
- Render-loop ternary at :529: `label === "AUM" ? null : primaryRaw` — dead once
  the AUM cell is gone; it ESCAPES the `label: "AUM"` and grid-token greps, so it
  carries its own acceptance gate (trim item 4f).
- Possibly-dead aum-keyed entries (AUM cell has `metricKey: null`, so these are
  expected unreachable): TREND map `aum: "up-good"` (:191), MIN_DELTA `aum: 0.01`
  (:215), `key === "aum"` formatter branches (:260, :279).

ScenarioComposer.tsx — sole production mount (~:3533, verified by the UI checker;
FactsheetView.tsx:588 + OutcomesWidget.tsx:181 are LOCAL KpiStrip functions with a
`kpis` prop — no collateral):
```tsx
<KpiStrip
  mode="scenario"
  scenarioMetrics={scenarioMetrics}
  liveMetrics={liveMetricsForKpi}
  metrics={liveMetricsForKpi}
  analytics={{}}
  aum={scenarioAum}
  snapshotCount={snapshotCount}
  ...
/>
```

Test pins on the OLD contract (planning-time survey, corrected at revision 1):
- KpiStrip.test.tsx :40 `describe("KpiStrip — designer 5-cell shape (D-09)")`;
  :41 "renders exactly 5 cells" with `labels = ["AUM", "YTD TWR", "Sharpe",
  "Max DD 12m", "Avg |ρ|"]` at :52; :290-region M-0085 AUM-$NaN follow-up test;
  :306 "AUM is exempt from warmup helper" (+ instance-count comments ~:324);
  :339 @container grid test (no `grid-cols-5` literal pinned in ANY test file —
  planning-time grep across `src --include='*.test.*'` returned zero).
  Plus 16 `aum={...}` mount props (removed in Task 2 with the prop deletion).
- KpiStrip.scenario.test.tsx :120 + :146 — the WR-02 pair pinning the AUM cell's
  "Projected — sum of enabled holdings" disclosure. Plus 16 `aum={...}` mount
  props (removed in Task 2).
- KpiStrip.warmup.test.tsx — no "AUM" LABEL pins, but 7 `aum={...}` mount props
  (removed in Task 2) and warm-up instance counts; run it.
- ScenarioComposer.test.tsx — mocks KpiStrip (:106); T_C3 (:571) asserts ONLY
  `mode` / `scenarioMetrics` / `liveMetrics` (NOT the aum prop). HOWEVER the
  legacy-v1-draft test at :769-798 asserts `kpiProps?.aum` at :797, using
  aum=100000 as its version-bump discriminator — this ONE test needs a reviewed
  re-point (Task 2); a whole-file zero-edit lock is UNSATISFIABLE. The
  PRESENT-02 verbatim set is the commit-boundary BLOCKS: T_C_P1933 :2016,
  NEW-C18-07 :2740, NEW-C18-05 :2775, IMP-3 :2962, H-0133 :2990 — zero diff
  hunks may touch them.

Test-mount removal budget (Task 2, all reviewed acts, one commit with the prop
deletion so tsc is green at the Task-2 gate): KpiStrip.test.tsx 16 +
KpiStrip.scenario.test.tsx 16 + KpiStrip.warmup.test.tsx 7 ≈ 39 `aum={...}`
removals, plus the ScenarioComposer.test.tsx :769-798 discriminator re-point.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — re-point the strip-shape tests to the 4-cell contract (reviewed acts)</name>
  <files>src/app/(dashboard)/allocations/components/KpiStrip.test.tsx, src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx</files>
  <read_first>
    - .planning/phases/64-presentation-purification/64-UI-SPEC.md (Contract A)
    - src/app/(dashboard)/allocations/components/KpiStrip.test.tsx (whole file)
    - src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx (whole file)
    - src/app/(dashboard)/allocations/components/KpiStrip.warmup.test.tsx (whole file)
  </read_first>
  <behavior>
    - Test 1 (the RED anchor): the strip renders exactly 4 cells, labels in
      canonical order ["YTD TWR", "Sharpe", "Max DD 12m", "Avg |ρ|"] — FAILS
      against the current 5-cell code.
    - Test 2: no cell labeled "AUM" and no dollar-formatted value renders in
      either mode (negative pin — also red pre-implementation, since the AUM
      cell currently renders).
  </behavior>
  <action>
    Re-point the strip-shape tests to the Phase-64 4-cell contract. Every edit is
    an individually reviewed act with an inline `Phase 64 / PRESENT-01` rationale
    comment (GUARD-02 discipline — never a blind update).

    ORDERING (honest sequencing, per revision 1): this RED task runs
    PRE-deletion — the `aum` prop still exists on KpiStripProps, so all mounts
    in these files (including any new/edited ones) KEEP passing `aum={...}` for
    now. The ~32 `aum={...}` mount removals in these two files land WITH the
    prop-deletion commit in Task 2, so tsc stays green at every gate.

    1. KpiStrip.test.tsx :40–52 — rename the describe from "designer 5-cell shape
       (D-09)" to the 4-cell shape (cite Phase 64 / PRESENT-01 in the comment);
       change the exactly-5 assertion to exactly-4 and the labels array to
       ["YTD TWR", "Sharpe", "Max DD 12m", "Avg |ρ|"] (order preserved). Add the
       Test-2 negative pin (queryByText("AUM") null; no /\$/ match on any cell
       value) in the same describe.
    2. KpiStrip.test.tsx ~:290 — RETIRE the M-0085 "AUM with NaN … formatCurrency
       leaks '$NaN'" follow-up test: its premise (an AUM cell) dies with
       PRESENT-01. Keep the :246 Sharpe/Avg|ρ| NaN test byte-unchanged. Note in
       the retirement comment that the formatCurrency non-finite guard follow-up
       moves to utils-level if ever needed.
    3. KpiStrip.test.tsx :306 — RETIRE "AUM is exempt from warmup helper": the
       exemption's only subject is the AUM cell. Recompute any warm-up
       instance-count assertions in the surrounding tests for a 4-cell strip
       (the exempt cell is gone; the four remaining cells are all non-exempt) —
       each count change is a reviewed act with the arithmetic in the comment.
    4. KpiStrip.test.tsx :339 @container test — verify it pins no column-variant
       literal (planning-time grep found no `grid-cols-5` in any test); if it
       does, re-point to `@lg:grid-cols-4`. The @container-host-on-separate-
       element and font-mono/tabular-nums assertions stay byte-unchanged.
    5. KpiStrip.scenario.test.tsx :120 + :146 — RETIRE the WR-02 pair (AUM
       projected-disclosure): the disclosed cell no longer exists. All other
       scenario-mode delta/tooltip tests stay byte-unchanged (they key on
       twr/sharpe/max_drawdown/avg_pairwise_correlation metricKeys, not aum).
    6. Do NOT touch KpiStrip.warmup.test.tsx in this task — it has no "AUM"
       label pins; its 7 `aum={...}` mount props are removed in Task 2 with the
       prop deletion, and if any of its instance-count assertions turn red from
       the cell removal, Task 2 re-points them there as reviewed acts.
    7. Do NOT touch ScenarioComposer.test.tsx in this task — its :769-798
       discriminator re-point lands in Task 2, where the mount-line removal
       makes it necessary.

    Run the two edited suites and RECORD the red (the 4-cell shape + AUM-absence
    assertions must fail against the current 5-cell component). Commit:
    `test(64-01): RED — re-point KpiStrip shape tests to the 4-cell return-form contract`
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx" "src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx" --no-file-parallelism 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - The 4-cell shape test and the AUM-absence pin FAIL (red recorded) against the
      unmodified component; no other NEW failures beyond the reviewed re-points.
    - `grep -c 'PRESENT-01' "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx"` ≥ 1
      (rationale comments present).
    - `grep -n '"AUM"' "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx"` matches only
      inside negative pins / retirement rationale — the labels array no longer lists it.
    - RED commit exists.
  </acceptance_criteria>
  <done>Strip tests pin the 4-cell contract and are red against current code; every re-point/retirement carries inline rationale.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — remove the AUM cell, delete the aum prop, reflow to @lg:grid-cols-4, clean all test mounts</name>
  <files>src/app/(dashboard)/allocations/components/KpiStrip.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/KpiStrip.test.tsx, src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx, src/app/(dashboard)/allocations/components/KpiStrip.warmup.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - DESIGN.md (spacing/typography sections — read BEFORE the layout edit, per CONTEXT)
    - .planning/phases/64-presentation-purification/64-UI-SPEC.md (Contract A — the grid token change)
    - src/app/(dashboard)/allocations/components/KpiStrip.tsx (:40–90 props, :180–260 helpers, :300–530 cells+grid+render loop)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (:3525–3545 mount; grep scenarioAum for the commit consumers)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (:769–798 legacy-v1-draft test; :2016/:2740/:2775/:2962/:2990 commit-modal blocks — read to confirm you touch NONE of them)
  </read_first>
  <action>
    KpiStrip.tsx (per UI-SPEC Contract A — straight cells[] splice, NOT a
    mode-gated branch; sole production mount is scenario-mode):

    1. Delete the AUM entry (the first cells[] object, :373–389). Remaining 4
       cells and their order/sub-copy/metricKeys byte-unchanged.
    2. Grid :469 — the ONE layout token change: `@lg:grid-cols-5` →
       `@lg:grid-cols-4`. Keep `grid grid-cols-1 gap-3 @sm:grid-cols-2`, the
       `role="group"` / aria-label, and the separate `@container` host (:467)
       byte-for-byte (Phase-52 container-query + tabular-nums invariants).
    3. Rewrite the "5-cell sources (D-09)" comment block (:307–323) to the
       4-cell truth (drop the AUM bullet; cite Phase 64 / PRESENT-01).
    4. Trim the now-dead aum chain — EACH deletion preceded by a grep proving
       zero remaining consumers (fail loud, never assume):
       a. `aumValue` (:324) — dead once the cell is gone.
       b. The `aum` prop (:64) + its JSDoc — DELETE ENTIRELY from KpiStripProps
          (orchestrator decision, option a: purification phase — dead required
          API must not survive; the production sole-mount is already verified).
          This is a full prop deletion, not an optionalization.
       c. `total_aum?` (:48) — only if the :324 fallback was its sole reader.
       d. `resolveSub`'s `isAum` param (:344–352) — all surviving callers pass
          false; simplify the signature and the :334 docstring (the AUM warm-up
          exemption note dies with the cell).
       e. TREND `aum:` (:191), MIN_DELTA `aum:` (:215), `key === "aum"`
          formatter branches (:260, :279) — the AUM cell carried
          `metricKey: null`, so these SHOULD be unreachable; delete ONLY after
          `grep -n '"aum"' KpiStrip.tsx` confirms no surviving reader keys them.
          If any live consumer exists, leave that entry byte-unchanged
          (surgical-change rule) and note it in the SUMMARY.
       f. Render-loop ternary :529 — `label === "AUM" ? null : primaryRaw`:
          drop the ternary and pass `primaryRaw` directly (no cell carries the
          "AUM" label any more). This line ESCAPES the label:/grid grep gates,
          so it has its own acceptance line below.
    5. Degenerate behavior unchanged: null metrics → em-dash via the existing
       formatters; no new empty state.

    ScenarioComposer.tsx:
    6. Remove `aum={scenarioAum}` from the ~:3533 mount — the ONLY line this
       plan touches in the composer. `scenarioAum`'s computation and its
       commit-modal consumers are UNTOUCHABLE (PRESENT-02):
       `git diff -- "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"`
       must show exactly the single mount-line hunk.

    Test-mount cleanup — lands in THIS commit so tsc is green at the Task-2
    gate (Task 1's RED ran pre-deletion with mounts still passing aum; the prop
    deletion above makes every remaining `aum={...}` a tsc error):
    7. Remove ALL `aum={...}` props from test mounts as individually reviewed
       acts — KpiStrip.test.tsx (16), KpiStrip.scenario.test.tsx (16),
       KpiStrip.warmup.test.tsx (7; the plan's earlier "0 AUM references" claim
       was FALSE — it has 7 aum mount props, no "AUM" label pins). Reviewed act
       means: confirm each removal only strips the dead prop and does not alter
       what the test observes; where a test USED the aum value as part of its
       assertion, re-point per items 8-9.
    8. KpiStrip.warmup.test.tsx — after the removals, run it; if any warm-up
       instance-count assertion turns red from the cell removal, re-point counts
       as individually reviewed acts with the 4-cell arithmetic in a Phase-64
       rationale comment.
    9. ScenarioComposer.test.tsx :769-798 (legacy-v1-draft restore test) —
       reviewed re-point of its version-bump discriminator: :797 asserts
       `kpiProps?.aum` (aum=100000) off the mocked KpiStrip, which dies with the
       mount-line removal. Re-point the discriminator onto a DIFFERENT observable
       of the SAME reset behavior — e.g., the rendered weight rows / metrics
       reflecting all 3 restored holdings — with an inline Phase 64 / PRESENT-01
       rationale comment (GUARD-02 discipline). This is the ONLY edit to this
       file; the commit-modal blocks (T_C_P1933 :2016, NEW-C18-07 :2740,
       NEW-C18-05 :2775, IMP-3 :2962, H-0133 :2990) stay byte-unchanged — Task 3
       proves it via the branch diff.

    Commit: `feat(64-01): PRESENT-01 — scenario KPI strip is return-form only (AUM cell + prop removed, 4-up @lg reflow)`
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx" "src/app/(dashboard)/allocations/components/KpiStrip.scenario.test.tsx" "src/app/(dashboard)/allocations/components/KpiStrip.warmup.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" --no-file-parallelism 2>&1 | tail -5 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All 5 suites above pass; tsc 0 errors; `npm run lint` 0 new errors on the
      touched source files.
    - `grep -n 'label: "AUM"' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` → no matches (exit 1).
    - `grep -n 'label === "AUM"' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` → no matches (exit 1)
      — the :529 render-loop ternary is gone (item 4f; this line escapes the other grep gates).
    - `grep -nw 'aum' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` → no matches (exit 1) AND
      `grep -n 'aumValue\|total_aum\|isAum' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` → no matches
      (exit 1) — the prop and its whole dead chain are fully deleted (word-match, so
      "maximum"/metric prose can't false-positive).
    - `grep -nE '(^|[^A-Za-z])aum=\{' <file>` → no matches (exit 1) for each of
      KpiStrip.test.tsx, KpiStrip.scenario.test.tsx, KpiStrip.warmup.test.tsx —
      all ~39 test-mount props removed (pattern excludes `scenarioAum={`).
    - `grep -n 'kpiProps?.aum' "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` → no matches
      (exit 1) — the :797 discriminator is re-pointed, with a Phase-64 rationale comment present
      (`grep -c 'PRESENT-01' ScenarioComposer.test.tsx` ≥ 1).
    - `grep -c '@lg:grid-cols-4' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` = 1 AND
      `grep -n '@lg:grid-cols-5' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` → no matches
      (paired positive+negative gate).
    - `grep -c '@container' "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` unchanged from
      before the edit (host preserved); `grep -c 'tabular-nums' ...KpiStrip.tsx` unchanged.
    - `git diff --stat` on ScenarioComposer.tsx shows exactly 1 changed hunk (the mount line);
      `grep -c 'scenarioAum' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` =
      (pre-edit count − 1) — computation + commit consumers intact, only the mount ref gone.
  </acceptance_criteria>
  <done>The scenario strip renders 4 return-form cells at @lg:grid-cols-4; the aum prop is deleted from KpiStripProps with all ~39 test-mount props removed as reviewed acts in the same commit (tsc green); the composer source diff is the single mount line; the composer test's only edit is the reviewed :769-798 discriminator re-point.</done>
</task>

<task type="auto">
  <name>Task 3: PRESENT-02 block-level guard + golden/e2e sweep + GUARD-03</name>
  <files>(verification only — no file modifications expected)</files>
  <read_first>
    - .planning/phases/64-presentation-purification/64-VALIDATION.md (per-task verification map)
  </read_first>
  <action>
    PRESENT-02 (the guard requirement — prove commit sizing is byte-unaffected;
    revision 1 narrowed this from a whole-file zero-diff to a BLOCK-level lock,
    since :797's `kpiProps?.aum` discriminator made a zero-edit file lock
    unsatisfiable):
    1. `git diff $(git merge-base origin/main HEAD) -- "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"`
       — inspect the hunk headers: the ONLY hunk(s) MUST fall inside the
       :769-798 legacy-v1-draft test (the reviewed Task-2 discriminator
       re-point). NO hunk may overlap the commit-modal blocks — T_C_P1933
       :2016, NEW-C18-07 :2740, NEW-C18-05 :2775, IMP-3 :2962, H-0133 :2990
       (pre-edit line numbers; map via the hunk headers' old-file ranges). If
       ANY hunk touches those blocks, STOP and surface it — the commit-modal
       blocks are verbatim-locked.
    2. Run the composer suite and confirm the commit-modal blocks pass verbatim:
       T_C_P1933 (scenarioAum=0 refuses commit), NEW-C18-05 (weight=0 + positive
       AUM → named error), NEW-C18-07, IMP-3, H-0133.

    Golden/e2e sweep (the UI-SPEC watch item — document, don't assume):
    3. `grep -rn 'AUM' e2e/ | grep -v Binary` and `grep -rln 'grid-cols-5' e2e/ src --include='*.test.*' --include='*.spec.ts'`
       — planning-time result was EMPTY for both (no e2e/golden pins the AUM KPI
       or the 5-col grid; demo-screenshot / svg-chart-parity snapshots don't
       render the scenario strip). Re-run and record the result in the SUMMARY.
       If ANY hit appears: schedule a reviewed re-bake (v1.4 golden-bake
       precedent) — never blind-update a snapshot.
    4. Run the two cross-cutting guard suites that name KpiStrip:
       src/__tests__/phase-52-container-tabular-nums.test.tsx (KpiStrip is in
       CONTAINER_MIGRATED) and src/__tests__/phase-63-series-space-guards.test.ts.

    GUARD-03 (milestone-wide frozen engine):
    5. `git diff $(git merge-base origin/main HEAD) -- src/lib/scenario.ts src/lib/scenario-window.ts`
       → MUST be empty.

    No commit expected (verification-only). If steps 1/5 fail, this plan is NOT
    done — fix at root before proceeding.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" src/__tests__/phase-52-container-tabular-nums.test.tsx src/__tests__/phase-63-series-space-guards.test.ts --no-file-parallelism 2>&1 | tail -5 && git diff --quiet $(git merge-base origin/main HEAD) -- src/lib/scenario.ts src/lib/scenario-window.ts && echo GUARD-03-OK</automated>
  </verify>
  <acceptance_criteria>
    - The branch diff on ScenarioComposer.test.tsx contains ONLY hunk(s) inside the
      :769-798 legacy-v1-draft test; zero hunks overlap :2016 / :2740 / :2775 /
      :2962 / :2990 (old-file ranges) — recorded in the SUMMARY with the hunk headers.
    - Composer + phase-52 + phase-63 guard suites all pass; the five commit-modal
      tests pass verbatim.
    - Sweep greps recorded in SUMMARY: expected empty → "no golden/e2e re-bake needed";
      any hit → reviewed re-bake task surfaced to the orchestrator.
    - GUARD-03-OK printed (frozen engine zero-diff vs merge-base).
  </acceptance_criteria>
  <done>PRESENT-02 proven byte-unaffected at the block level via verbatim tests + hunk-range audit; sweep documented; frozen engine zero-diff.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed client → scenario tab | existing surface; this plan removes a rendered value, adds no input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | I | KpiStrip (scenario strip) | mitigate | AUM (position-space dollar figure) removed from presentation; grep gates `label: "AUM"` AND `label === "AUM"` absent; parity with the share page's no-USD contract |
| T-64-02 | T | commit sizing path (scenarioAum → diff modal) | mitigate | scenarioAum computation + commit consumers byte-unchanged; verbatim test gate (T_C_P1933 / NEW-C18-05 class) + hunk-range audit proving zero diff hunks touch the commit-modal test blocks |
| T-64-SC | Tampering | npm/pip installs | accept | no packages installed in this phase — no install tasks exist; legitimacy gate N/A |
</threat_model>

<verification>
- KpiStrip 3 suites + ScenarioComposer 2 suites + phase-52 + phase-63 guards green.
- `npx tsc --noEmit` 0 errors; `npm run lint` 0 new errors.
- GUARD-03 zero-diff vs merge-base.
- PRESENT-02 hunk-range audit on ScenarioComposer.test.tsx recorded in SUMMARY.
- Sweep result documented in SUMMARY (expected: no golden/e2e hits, no re-bake).
</verification>

<success_criteria>
- Scenario strip: exactly 4 return-form cells, `@lg:grid-cols-4`, no dollar figure,
  @container host + tabular-nums invariants intact; the `aum` prop deleted from
  KpiStripProps entirely (no dead required API survives).
- Commit-modal behavior byte-identical, proven by the five verbatim tests passing
  AND a hunk-range audit showing zero diff hunks touch their blocks; the composer
  test file's only diff is the reviewed :769-798 discriminator re-point.
- Every test re-point/retirement/mount-trim is a reviewed act with inline
  Phase-64 rationale (mount-trims may share one rationale comment per file).
- 2 commits: test RED (pre-deletion, mounts still pass aum) + feat GREEN (prop
  deletion + ~39 mount removals + discriminator re-point, tsc green).
- VERSION + package.json bump is a LAND-step note (all Phase-64 plans land together) — NOT a task commit.
</success_criteria>

<output>
Create `.planning/phases/64-presentation-purification/64-01-SUMMARY.md` when done
(include the sweep grep results, the hunk-range audit, and the dead-chain
deletions actually performed).
</output>
