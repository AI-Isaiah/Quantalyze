---
phase: 62-explicit-draft-series-membership-schema-v4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-state.ts
  - src/app/api/allocator/scenario/saved/route.ts
  - src/app/api/allocator/scenario/saved/[id]/route.ts
  - src/app/(dashboard)/allocations/lib/scenario-state.test.ts
  - src/app/(dashboard)/allocations/lib/scenario-state.localStorage.test.ts
  - src/app/scenario-share/[token]/share-resolve.test.ts
  - src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx
autonomous: true
requirements: [MEMBER-01]
must_haves:
  truths:
    - "A v3 draft blob decodes outcome:ok with reason:upgraded_v3_membership (never reset)"
    - "A v2 draft blob decodes outcome:ok with a distinct v2-chain reason (never reset) — no stored v2 draft dropped"
    - "A v5 (current+1) draft decodes outcome:readonly reason:version_ahead (unchanged forward-compat)"
    - "A v4 draft lacking memberKeyIds decodes outcome:ok with membership UNDERIVED (the codec-decode schema is TOLERANT) — NEVER reset, because an upgraded v2/v3 draft that round-tripped through the localStorage codec legitimately reaches the v4 exact-version branch underived"
    - "An upgraded v3 (and v2-chain) working draft survives an encode→decode round-trip through the codec without reset (the blocker regression)"
    - "scenarioDraftSaveSchema (save-route-only refined schema) rejects a direct v4 POST body lacking memberKeyIds — fail-loud lives at the SAVE boundary, not the codec"
    - "memberKeyIds rides the whole-draft jsonb persistence with no SQL/RPC change (the save route swaps its validator to scenarioDraftSaveSchema)"
    - "deriveMembershipFromGate, isBookOnlyDraft (null-safe), setMemberKeyIds are single exported definitions consumed by later plans"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      provides: "memberKeyIds field, zod optional bound (TOLERANT — NO superRefine on the shared codec-decode schema), a SEPARATE exported scenarioDraftSaveSchema carrying the schema_version>=4 superRefine, double version bump, two non-destructive codec branches + a tolerant v4 exact-version branch, deriveMembershipFromGate, isBookOnlyDraft (null-safe), setMemberKeyIds"
      contains: "export function deriveMembershipFromGate"
    - path: "src/app/api/allocator/scenario/saved/route.ts"
      provides: "body validation swapped from scenarioDraftSchema to scenarioDraftSaveSchema (v4 membership enforced at the save boundary only)"
      contains: "scenarioDraftSaveSchema"
    - path: "src/app/api/allocator/scenario/saved/[id]/route.ts"
      provides: "PUT UpdateBodySchema draft validator swapped to scenarioDraftSaveSchema — symmetric save-boundary enforcement (checker W-A: locked decision requires absence to be impossible in a persisted v4 draft on BOTH save routes)"
      contains: "scenarioDraftSaveSchema"
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.test.ts"
      provides: "RED-first codec + helper tests incl. the upgraded-draft encode→decode round-trip regression"
      contains: "upgraded_v2_chain"
  key_links:
    - from: "scenarioDraftCodec.decode"
      to: "SCENARIO_SCHEMA_VERSION_PREV (=3) branch and literal-2 branch"
      via: "rawVersion trichotomy"
      pattern: "rawVersion === 2"
    - from: "scenarioDraftSchema (shared codec-decode)"
      to: "memberKeyIds parse-tolerance"
      via: ".optional() so v2/v3 AND an underived-v4 round-trip all safeParse clean; NO superRefine on this schema"
      pattern: "memberKeyIds"
    - from: "scenarioDraftSaveSchema (save-route-only)"
      to: "memberKeyIds v4 enforcement at saved/route.ts body-validate"
      via: ".superRefine requires memberKeyIds when schema_version>=4 — kept OFF the shared codec-decode schema"
      pattern: "scenarioDraftSaveSchema"
---

<objective>
Establish the persisted-membership foundation for Phase 62: add `memberKeyIds: string[]`
to `ScenarioDraft`, bump `SCENARIO_SCHEMA_VERSION` 3→4 and `SCENARIO_SCHEMA_VERSION_PREV`
2→3, and add the SECOND non-destructive codec branch so BOTH v2 and v3 drafts decode
`ok` (never reset). Export the three shared functions the other three plans consume:
`deriveMembershipFromGate` (upgrade-read derivation), `isBookOnlyDraft` (null-safe
book-only predicate), and `setMemberKeyIds` (new-save stamping transform). This is MEMBER-01.

Purpose: The whole phase stands on this codec + these helpers. The single highest risk
is the double version bump silently deleting every stored v2 draft (Pitfall 1) — this
plan closes it by construction and proves it RED-first. The SECOND, subtler risk
(the checker blocker) is that a v2/v3 draft upgrades in-memory to `{schema_version:4,
memberKeyIds:undefined}`, is persisted by `useCrossTabStorage({codec})` at
`allocations.scenario_v0_15.{allocatorId}` (useScenarioState.ts:141-144), and on the NEXT
decode reaches the v4 EXACT-version branch underived. So the codec-decode schema MUST be
TOLERANT (a v4 blob without membership decodes `ok`, membership underived — never reset),
otherwise every upgraded draft is silently dropped on the round-trip. The v4 fail-loud
enforcement moves to a SEPARATE, exported `scenarioDraftSaveSchema` (Open Question 3's
literal recommendation) that ONLY the save route uses — a direct v4 POST without membership
is rejected there, while the codec-decode path stays tolerant. Plan 04 makes upgraded
drafts self-describing on reopen (derive + `setMemberKeyIds` into working state) so the
persisted localStorage blob converges to a proper v4-with-membership draft.

Output: `scenario-state.ts` with the field, TOLERANT shared schema (optional, no
superRefine) + a separate `scenarioDraftSaveSchema` (v4 superRefine), double bump, two
non-destructive codec branches + tolerant v4 branch, three exported helpers; `saved/route.ts`
swapped to the refined validator; RED-first tests incl. the encode→decode round-trip
regression; all version-relative fixtures across the suite rebased.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/62-explicit-draft-series-membership-schema-v4/62-RESEARCH.md
@.planning/phases/62-explicit-draft-series-membership-schema-v4/62-PATTERNS.md

<interfaces>
<!-- Current source of truth the executor extends. Read the file; do not assume. -->

ScenarioDraft (scenario-state.ts:91) — add `memberKeyIds: string[];` REQUIRED at v4,
placed after `window?: CoverageWindow` and before `lastEditedAt`.

Version constants (scenario-state.ts:66,73):
  SCENARIO_SCHEMA_VERSION = 3        → 4
  SCENARIO_SCHEMA_VERSION_PREV = 2   → 3

Bound constants already present (scenario-state.ts:613-614):
  MAX_DRAFT_KEY_LENGTH = 512

scenarioDraftSchema window field precedent (scenario-state.ts:651-660) — `.optional()` so
v2 blobs safeParse clean. `schema_version: z.number()` (:637). This SHARED schema is REUSED
by the codec safeParse in EVERY decode branch — it MUST stay tolerant (no superRefine), or
an underived-v4 round-trip blob would reset (the blocker). The save route stops using it
directly and switches to the new `scenarioDraftSaveSchema` (below).

Save route body-validate (saved/route.ts:37 import, :59 body-validate) — currently
`scenarioDraftSchema`. SWAP to `scenarioDraftSaveSchema` so v4 membership is enforced at
the save boundary only. `MAX_DRAFT_BODY_BYTES` (256KB, :52) already caps total payload.

localStorage round-trip surface (useScenarioState.ts:134-158) — `scenarioDraftCodec(defaultDraft)`
is handed to `useCrossTabStorage<ScenarioDraft>({ codec })`; it ENCODES the in-memory draft
back to `allocations.scenario_v0_15.{allocatorId}` and DECODES it on the next read/tab. This
is the exact path that makes an underived-v4 blob re-enter the v4 branch — the tolerance is
what keeps it alive.

Codec decode current single non-destructive branch (scenario-state.ts:741-763):
  `if (rawVersion === SCENARIO_SCHEMA_VERSION_PREV) { safeParse → ok/upgraded_v2_windowless }`
  followed by the final `return { outcome: "reset", reason: "version_mismatch" }`.

Forward-compat branch (scenario-state.ts:697-706): `rawVersion > SCENARIO_SCHEMA_VERSION`
→ readonly/version_ahead — DO NOT TOUCH, it self-adjusts.

Exact-version branch (scenario-state.ts:709-719): `rawVersion === SCENARIO_SCHEMA_VERSION`
→ safeParse (shared TOLERANT schema) → ok. A v4 blob WITHOUT membership decodes ok with
membership underived (NOT reset) — this is the round-trip survival the blocker requires.

defaultDraftFromHoldings fresh literal (scenario-state.ts:226-233) — the ONE writer that
does not spread; must add `memberKeyIds: []` (only `holdings` in scope).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — failing codec + helper tests for v4 (incl. round-trip regression)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts (current version fixtures, the PERSIST-01 v2 block ~:504-559, forward-version test ~:472-478)
    - src/app/(dashboard)/allocations/lib/scenario-state.ts (codec decode :690-763, version constants :66,73)
    - src/app/(dashboard)/allocations/hooks/useScenarioState.ts (:134-158 — the codec round-trip via useCrossTabStorage)
    - .planning/phases/62-explicit-draft-series-membership-schema-v4/62-RESEARCH.md (Test Map + Pitfall 1 + Open Question 3)
  </read_first>
  <behavior>
    - A blob with schema_version=3 (valid v3 shape, no memberKeyIds) decodes { outcome:"ok", reason:"upgraded_v3_membership" }, schema_version upgraded in-memory to 4, memberKeyIds LEFT UNDERIVED (absent on the decoded value).
    - A blob with schema_version=2 (valid v2 shape, no window, no memberKeyIds) decodes { outcome:"ok", reason:"upgraded_v2_chain" } (distinct reason), never reset.
    - A blob with schema_version=5 decodes { outcome:"readonly", reason:"version_ahead" } (forward-compat unchanged).
    - A blob with schema_version=4 WITH memberKeyIds=["k"] decodes { outcome:"ok", reason:null } (exact-version adopt).
    - A blob with schema_version=4 that LACKS memberKeyIds decodes { outcome:"ok" } with membership UNDERIVED (absent) — NOT reset. The codec-decode schema is tolerant; this is the underived-v4 round-trip case (blocker). It must never take the reset path.
    - ROUND-TRIP REGRESSION (blocker): take a valid v3 blob → decode (yields the underived-v4 upgrade) → encode the decoded value via the same codec → decode the encoded blob AGAIN → the second decode is outcome:"ok" (NOT reset). Repeat the same round-trip for a v2 blob (v2 → decode → encode → decode → ok). This proves an upgraded working draft persisted to localStorage and re-read is never silently dropped.
    - scenarioDraftSaveSchema.safeParse on a v4 draft WITHOUT memberKeyIds FAILS (fail-loud at the save boundary); scenarioDraftSaveSchema.safeParse on a v4 draft WITH memberKeyIds passes; scenarioDraftSaveSchema on a v2/v3 blob (schema_version < 4) passes (refine skips) — proving the refine lives on the SAVE schema, not the codec schema.
    - deriveMembershipFromGate(true, ["a","b"]) === ["a","b"]; deriveMembershipFromGate(false, ["a","b"]) === []; returns a fresh array (not the same reference).
    - isBookOnlyDraft table (NULL-SAFE): memberKeyIds=["k"] + addedStrategies=[] ⇒ true; memberKeyIds=[] + addedStrategies=[] ⇒ false; memberKeyIds=["k"] + addedStrategies=[{...}] ⇒ false; memberKeyIds=[] + addedStrategies=[{...}] ⇒ false; memberKeyIds=undefined (underived) + addedStrategies=[] ⇒ false (does NOT throw).
    - setMemberKeyIds(draft, ids) returns { ...draft, memberKeyIds: ids } and does not mutate the input.
    - defaultDraftFromHoldings(...) result carries memberKeyIds: [].
    - Spread-preservation: after toggleHolding / addStrategyBrowse / removeAddedStrategy / setWeightOverride / setWindow, the returned draft still carries the pre-existing memberKeyIds value.
  </behavior>
  <action>Add a new describe block `MEMBER-01 v4 codec + membership helpers` to scenario-state.test.ts covering every case in <behavior>. Reference the version constants relatively where a fixture is "current" (SCENARIO_SCHEMA_VERSION) or "current+1" (SCENARIO_SCHEMA_VERSION + 1), but pin the v3 and v2 upgrade fixtures to the LITERAL integers 3 and 2 respectively (they test absolute two-version-span survival, not "prev"). Import `deriveMembershipFromGate`, `isBookOnlyDraft`, `setMemberKeyIds`, and `scenarioDraftSaveSchema` from scenario-state.ts — these imports and the assertions MUST fail to compile/run now (the exports and branches do not exist yet). The encode→decode round-trip assertions MUST fail now (there is no v4 handling and no tolerant v4 branch). Run the test file and CAPTURE the RED failure (at minimum: unresolved imports for the four new exports, the v2-blob-decodes-ok assertion failing against the current reset path, and the round-trip regression red). Do NOT implement anything in this task.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism 2>&1 | grep -Eiq "fail|error|cannot find|is not exported|is not a function" && echo RED-CONFIRMED</automated>
  </verify>
  <acceptance_criteria>
    - The new describe block exists and references upgraded_v3_membership AND upgraded_v2_chain string literals.
    - The block includes the v4-with-membership (ok/null), the v4-WITHOUT-membership (ok, underived — NOT reset), and the encode→decode round-trip regression (v3 and v2) cases.
    - The block asserts scenarioDraftSaveSchema REJECTS a v4 draft without membership.
    - Running the file emits a failure (unresolved new exports and/or v2-decodes-ok and/or round-trip assertions red) — RED-CONFIRMED printed.
    - No edits to scenario-state.ts in this task (git diff shows only the test file changed).
  </acceptance_criteria>
  <done>The v4 codec (tolerant decode + round-trip survival), the save-only refined schema, and the helper behaviors are encoded as tests that fail against the current v3 code (RED proven).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — field, double bump, tolerant codec + save-only refined schema, three helpers</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.ts, src/app/api/allocator/scenario/saved/route.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.ts (ScenarioDraft :91, version constants + doc comments :60-73, defaultDraftFromHoldings :226-233, boundedRecord + MAX_DRAFT_KEY_LENGTH :613-614, scenarioDraftSchema window field :651-660, codec decode :690-763)
    - src/app/api/allocator/scenario/saved/route.ts (:37 schema import, :59 body-validate)
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts (the RED tests from Task 1 — the contract to satisfy)
    - .planning/phases/62-explicit-draft-series-membership-schema-v4/62-PATTERNS.md (scenario-state.ts pattern section)
  </read_first>
  <action>
1. Interface: add `memberKeyIds: string[];` to ScenarioDraft after `window?: CoverageWindow` and before `lastEditedAt`, with a doc comment noting it is REQUIRED at v4, empty array = blank-authored, left underived by the non-destructive upgrade branches (and by an underived-v4 round-trip).
2. zod (SHARED schema): add `memberKeyIds: z.array(z.string().max(MAX_DRAFT_KEY_LENGTH)).max(64).optional()` to scenarioDraftSchema after the `window` field. It MUST be `.optional()` (Pitfall 3) AND scenarioDraftSchema MUST carry NO superRefine — the v2/v3 upgrade safeParse AND the underived-v4 round-trip safeParse (blocker) both run this schema on blobs lacking membership; a required field or a v4-membership superRefine here would fail safeParse and route to reset (silent delete on the localStorage round-trip).
3. v4 enforcement OFF the codec schema (Open Question 3 / the blocker fix): create and EXPORT a separate `scenarioDraftSaveSchema = scenarioDraftSchema.superRefine((d, ctx) => { if (d.schema_version >= 4 && d.memberKeyIds === undefined) ctx.addIssue({ code: z.ZodIssueCode.custom, path: ["memberKeyIds"], message: "memberKeyIds required at schema_version >= 4" }); })`. This refined schema is used ONLY by the save route (step 4). The codec-decode branches keep using the tolerant scenarioDraftSchema. A v2/v3 blob (schema_version < 4) still passes scenarioDraftSaveSchema (refine skips). Add a doc comment explaining WHY the refine is NOT on the shared schema (the underived-v4 localStorage round-trip must decode ok, never reset).
4. saved/route.ts: swap the body-validate from `scenarioDraftSchema` to `scenarioDraftSaveSchema` (update the import at :37 and the parse call at :59). No SQL/RPC change. A direct v4 POST lacking membership is now rejected at the save boundary (fail-loud), while the codec stays tolerant.
4b. saved/[id]/route.ts (checker W-A — symmetric PUT boundary): in `UpdateBodySchema` (:78), swap the `draft:` validator from `scenarioDraftSchema` to `scenarioDraftSaveSchema` (update the import). Without this, a v4 body persisted via PUT bypasses the v4 membership contract server-side. Safe: existing route tests use schema_version:2 fixtures and the >=4 refine skips pre-v4, so `saved/[id]/route.test.ts` stays green unchanged.
5. Version constants: SCENARIO_SCHEMA_VERSION 3→4; SCENARIO_SCHEMA_VERSION_PREV 2→3. Update the PREV doc comment to name the v3→v4 membership upgrade AND explicitly document the second literal-2 chain branch below.
6. Codec: the existing `rawVersion === SCENARIO_SCHEMA_VERSION_PREV` branch now keys on v3 — change its reason string to "upgraded_v3_membership" (membership left underived: the `...safe.data` spread carries no memberKeyIds). Then ADD a SECOND non-destructive branch keyed on the LITERAL `rawVersion === 2`, same safeParse-then-spread shape, distinct reason "upgraded_v2_chain", placed BEFORE the final `version_mismatch` reset. It must decode ok despite BOTH window-absent AND memberKeyIds-absent. The EXACT-version branch (`rawVersion === SCENARIO_SCHEMA_VERSION`) already safeParses the tolerant schema, so a v4 blob without membership decodes ok (underived) — verify it does NOT reset (this is the round-trip survival). A genuinely-corrupt v2/v3/v4 blob (schema-invalid on the tolerant schema) still falls to reset(schema_invalid). Do NOT touch the forward-compat readonly branch.
7. defaultDraftFromHoldings: add `memberKeyIds: []` to the returned literal (only holdings in scope; real membership is stamped by the composer in plan 04).
8. Export `deriveMembershipFromGate(gate: boolean, eligibleApiKeyIds: string[]): string[]` — `gate ? [...eligibleApiKeyIds] : []`. This is the ONE upgrade-read derivation rule; it must NOT read entryMode (old drafts predate it).
9. Export `isBookOnlyDraft(draft: ScenarioDraft): boolean` — `(draft.memberKeyIds ?? []).length >= 1 && (draft.addedStrategies ?? []).length === 0`. NULL-SAFE: an underived (undefined-membership) decoded draft must NOT throw at `.length` (plan 03 calls this predicate on codec-decoded raw drafts).
10. Export `setMemberKeyIds(draft: ScenarioDraft, ids: string[]): ScenarioDraft` — `{ ...draft, memberKeyIds: ids }` (pure, no mutation). This is the new-save STAMP transform the composer invokes in plan 04; it is DISTINCT from deriveMembershipFromGate.
Do NOT touch src/lib/scenario.ts or src/lib/scenario-window.ts (GUARD-03 frozen zero-diff).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "SCENARIO_SCHEMA_VERSION = 4" src/app/(dashboard)/allocations/lib/scenario-state.ts` matches; `SCENARIO_SCHEMA_VERSION_PREV = 3` matches.
    - `grep -c "upgraded_v3_membership\|upgraded_v2_chain" src/app/(dashboard)/allocations/lib/scenario-state.ts` returns 2 (both reasons present).
    - `grep -n "rawVersion === 2" src/app/(dashboard)/allocations/lib/scenario-state.ts` matches (the literal second branch).
    - `grep -n "export const scenarioDraftSaveSchema\|export function scenarioDraftSaveSchema" src/app/(dashboard)/allocations/lib/scenario-state.ts` matches; the `superRefine` gating on `schema_version >= 4` is on scenarioDraftSaveSchema, and `grep -c "superRefine" src/app/(dashboard)/allocations/lib/scenario-state.ts` returns exactly 1 (the shared schema has none).
    - `grep -n "scenarioDraftSaveSchema" src/app/api/allocator/scenario/saved/route.ts` matches (the save route uses the refined validator); `grep -c "scenarioDraftSchema" src/app/api/allocator/scenario/saved/route.ts` shows the body-validate no longer references the tolerant schema for parse.
    - `grep -n "scenarioDraftSaveSchema" "src/app/api/allocator/scenario/saved/[id]/route.ts"` matches (W-A: the PUT UpdateBodySchema draft validator uses the refined schema too); `npx vitest run "src/app/api/allocator/scenario/saved/[id]/route.test.ts" --no-file-parallelism` exits 0 unchanged.
    - `grep -n "export function deriveMembershipFromGate\|export function isBookOnlyDraft\|export function setMemberKeyIds" ...scenario-state.ts` returns all three; isBookOnlyDraft uses `?? []`.
    - `git diff --stat src/lib/scenario.ts src/lib/scenario-window.ts` is empty (GUARD-03).
    - The Task 1 describe block passes (all v4 codec + helper assertions green, incl. v4-WITHOUT-membership → ok/underived and the encode→decode round-trip regression).
  </acceptance_criteria>
  <done>v4 field + TOLERANT shared schema + separate save-only refined schema + double bump + two non-destructive codec branches + tolerant v4 round-trip + three exported helpers land; save route uses the refined validator; the RED tests are now GREEN; frozen engine untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Pitfall-2 — rebase every version-relative fixture, full suite green</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.test.ts, src/app/(dashboard)/allocations/lib/scenario-state.localStorage.test.ts, src/app/scenario-share/[token]/share-resolve.test.ts, src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx</files>
  <read_first>
    - src/app/scenario-share/[token]/share-resolve.test.ts (line 102 `.toBe(3)` pin; the ~11 `schema_version: 2` fixtures at 124,127,136,168,179,243,288,319,426,432,528,543)
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts (the PERSIST-01 v2 block :504-559, Test C `SCENARIO_SCHEMA_VERSION + 1` :543, forward-version comment :472-478)
    - src/app/(dashboard)/allocations/lib/scenario-state.localStorage.test.ts (:89-152 legacy-helper relative fixtures)
    - src/app/(dashboard)/allocations/hooks/useScenarioState.hydrate.test.tsx (hand-built draft fixtures)
    - .planning/phases/62-explicit-draft-series-membership-schema-v4/62-PATTERNS.md (Version-relative test-fixture rebase section)
  </read_first>
  <action>
Grep `schema_version:` and `SCENARIO_SCHEMA_VERSION` across `src/**/*.test.ts*` and classify EVERY hit as relative (self-adjusts, leave alone) or absolute (rebase), preserving each test's INTENT (Rule 9 — reviewed rebases, never blind bumps):
1. share-resolve.test.ts:102 — `expect(SCENARIO_SCHEMA_VERSION).toBe(3)` → `.toBe(4)` (verify the surrounding intent: if it asserts the CURRENT constant equals 4, use `.toBe(4)`; if it builds a version-AHEAD envelope it should be `SCENARIO_SCHEMA_VERSION + 1`).
2. share-resolve.test.ts ~11 `schema_version: 2` fixtures — decide PER fixture: a fixture proving "two-versions-back still resolves ok" KEEPS `schema_version: 2` (now exercises the new v2-chain branch); a fixture that meant "the previous version upgrades" becomes `schema_version: 3` (the v3 branch). Preserve the computed-metric-vs-honest-absence expectation each was asserting — a v2 fixture that previously computed a metric MUST still compute one (proves the v2-chain branch works), never flip to honest-absence. Any hand-built CURRENT-version draft fixture must now include `memberKeyIds` (a v4 draft is expected to carry it, though the tolerant codec would not reset if it lacked it).
3. scenario-state.test.ts:504-559 PERSIST-01 v2 hardcodes + Test C `SCENARIO_SCHEMA_VERSION + 1` (was 4, now 5) — re-base to the new constants keeping the "v2 survives / genuinely-legacy resets" intent. Confirm no existing test still asserts "v4 without membership → reset" — the tolerant-codec contract changed that to ok/underived; rebase any such assertion to expect ok.
4. useScenarioState.hydrate.test.tsx — any draft built by hand at the CURRENT version should include `memberKeyIds: []`.
5. scenario-state.localStorage.test.ts:89-152 — relative fixtures; verify they still pass, adjust only if a hand-built current-version draft needs memberKeyIds.
Run the FULL frontend suite + tsc after rebasing.
  </action>
  <verify>
    <!-- Full `npm test` is the per-task verify here (a latency outlier, ~minutes) BECAUSE this
         is a cross-file fixture rebase spanning four test files across two subsystems — a
         scoped run cannot prove the whole version-relative population is green. nyquist_compliant. -->
    <automated>npm test 2>&1 | tail -20 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "toBe(3)" src/app/scenario-share/[token]/share-resolve.test.ts` no longer pins the schema version to 3 (rebased to 4 or +1 relative).
    - No `share-resolve.test.ts` `schema_version: 2` fixture flipped from a computed metric to honest-absence (grep the diff — a v2 fixture's expected `kind` must be unchanged).
    - No remaining test asserts "v4 without membership → reset" (the tolerant-codec contract).
    - Full `npm test` reports 0 failed.
    - `npx tsc --noEmit` exits 0.
    - `git diff --stat src/lib/scenario.ts src/lib/scenario-window.ts` empty (GUARD-03).
  </acceptance_criteria>
  <done>Every version-relative fixture is reviewed-rebased with intent preserved; the full suite and tsc are green on the v4 bump.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted jsonb → codec decode | untrusted stored draft blob crosses into the app on read (server rows AND the localStorage round-trip) |
| client POST body → scenarioDraftSaveSchema | untrusted save payload validated by the refined zod schema at the save route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02 | Denial of Service | memberKeyIds array in scenarioDraftSchema | mitigate | `.max(64)` element-count bound + per-element `.string().max(512)` (MAX_DRAFT_KEY_LENGTH); the route-level MAX_DRAFT_BODY_BYTES 256KB cap already bounds total payload |
| T-62-03 | Tampering | codec decode of a malformed/downgraded/underspecified version blob | mitigate | Integer-version guard on the readonly branch (unchanged); corrupt v2/v3/v4 blobs fall to reset(schema_invalid) inside each branch; a v4 blob lacking membership decodes ok with membership UNDERIVED (tolerant — never reset, so an upgraded localStorage round-trip is not silently dropped); the v4 membership CONTRACT is enforced at the save boundary via scenarioDraftSaveSchema (fail-loud there, not the codec) |
| T-62-SC | Tampering | npm installs | accept | No package installs this phase (pure in-repo TS) — Package Legitimacy Audit N/A |
</threat_model>

<verification>
- `npm test` full suite: 0 failed.
- `npx tsc --noEmit`: clean.
- `npm run lint`: no new react-hooks errors.
- Coverage above ratchet (82/80/74/72): `npm run test:coverage` (phase-gate only, not per task).
- Frozen zero-diff: `git diff src/lib/scenario.ts src/lib/scenario-window.ts` empty.
</verification>

<success_criteria>
- MEMBER-01 satisfied: a saved draft persists `memberKeyIds` at SCENARIO_SCHEMA_VERSION 4; v3 AND v2 blobs decode ok (non-destructive); a v4 blob without membership decodes ok/underived and survives an encode→decode round-trip (blocker closed); v5 readonly; the save route rejects a v4 body without membership via scenarioDraftSaveSchema (fail-loud at the save boundary); three shared helpers exported and unit-tested; no SQL/RPC change; frozen engine untouched.
</success_criteria>

<output>
Create `.planning/phases/62-explicit-draft-series-membership-schema-v4/62-01-SUMMARY.md` when done.
</output>
</output>
