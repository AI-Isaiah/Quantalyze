---
phase: 63-holdings-snapshot-fallback-engine-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-adapter.ts
  - src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "scenario-adapter exports ONE added-only engine-set construction (buildAddedOnlySet) provably equivalent to mergeAddedIntoPerKeySet with an empty per-key set"
    - "No engine-set builder output ever contains an id starting with 'holding:' — per-key ids are api_keys UUIDs, added ids are strategies UUIDs, disjoint by construction (explicit no-alias assertion)"
    - "A gate=false book holder gets a BLANK-mode composer (added-only) with the DSRC-02 note rendered — never a broken or empty book UI (D1)"
    - "Book entry is unavailable-with-note when gate=false; selecting book cannot enter a mode with no engine"
    - "gate=true book behavior is byte-identical to before (init, segment control, note suppression)"
    - "Frozen engine zero-diff: git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts is empty"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-adapter.ts"
      provides: "exported buildAddedOnlySet wrapper delegating to buildAddedUnits"
      contains: "buildAddedOnlySet"
    - path: "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts"
      provides: "no-alias assertion (ENGINE-04 precondition b) + buildAddedOnlySet equivalence pins"
      contains: "buildAddedOnlySet"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "gate-aware blank-mode init + repointed showDataSourcesFallback condition"
  key_links:
    - from: "ScenarioComposer entryMode init"
      to: "payload.perKeyDailiesGateSatisfied"
      via: "hasLiveBook && gate ? book : blank"
      pattern: "perKeyDailiesGateSatisfied"
    - from: "showDataSourcesFallback"
      to: "hasLiveBook (NOT entryMode === \"book\")"
      via: "repointed condition — Pitfall 2 (dead-note prevention)"
      pattern: "showDataSourcesFallback"
---

<objective>
Lay the two foundations every later deletion stage stands on: (1) the ONE shared
added-only engine-set construction (`buildAddedOnlySet`) plus the explicit no-alias
assertion (ENGINE-04 precondition b, RESEARCH Wave-0 item); (2) ENGINE-03 — gate=false
books initialize to BLANK mode with the existing calm DSRC-02 note repointed so it
still renders (D1 locked; Pitfall 2). Landing ENGINE-03 BEFORE the ENGINE-01 deletion
means no intermediate state ever shows a gate=false book mode with no engine behind it.

Purpose: the staged deletions (Plans 02–04) replace holdings paths with `buildAddedOnlySet`
and rely on blank-mode being the honest gate=false landing spot.
Output: exported wrapper + adapter test pins; gate-aware composer init + entry-mode
refusal + segment affordance + note repoint, all render-test pinned.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-RESEARCH.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md

<interfaces>
<!-- Surviving adapter path (scenario-adapter.ts, verified on this branch). Consume, do not re-derive. -->
buildAddedUnits (PRIVATE, scenario-adapter.ts:94-124): the ONE warm-up-gated,
  weight-0-default added-strategy unit construction. Signature:
  (addedStrategies, addedStrategyReturnsLookup, addedStrategyMetadataLookup) → StrategyForBuilder[]
mergeAddedIntoPerKeySet (scenario-adapter.ts:313-354): the book+gate survivor.
  Empty-per-key equivalence (RESEARCH-proven): mergeAddedIntoPerKeySet(
    { strategies: [], state: { selected: {}, weights: {}, startDates: {} } }, added, returns, meta)
  produces EXACTLY today's blank-mode output: selected[id]=true, weights[id]=0,
  startDates[id] = start ?? "2022-01-01". Note :322 early-return: addedStrategies.length===0 → returns perKey unchanged.
buildPerKeyStrategyForBuilderSet (scenario-adapter.ts:247-284): per-key unit id = api_keys.id UUID (:261).
  Mirror its signature/JSDoc convention for the new wrapper.

Composer touch points (ScenarioComposer.tsx, line numbers current on branch v1.6-membership-schema-v4 — re-grep if head advanced):
  :691-694  hasLiveBook = rawHoldingsSummary.length > 0; entryMode init useState(hasLiveBook ? "book" : "blank")
  :1126-1137 handleEntryModeSelect
  :2985-3029 entry-mode segment control ("From my book" renders when hasLiveBook at :2997; radiogroup arrow-key handler :2990-2995)
  :1805-1814 showDataSourcesFallback = entryMode === "book" && !payload.perKeyDailiesGateSatisfied && (payload.eligibleApiKeyIds ?? []).length > 0
  :3269-3280 note render, data-testid="scenario-data-sources-fallback", locked copy "Per-source modeling needs per-key history."
  :1327-1331 MEMBER-04 ineligible-member disclosure (UNTOUCHED)
  :1441-1444 membership save STAMP entryMode === "book" && payload.perKeyDailiesGateSatisfied (UNTOUCHED — already consistent: forced blank stamps [])
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: buildAddedOnlySet wrapper + explicit no-alias assertion (ENGINE-04 precondition b, Wave-0)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-adapter.ts, src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-adapter.ts (buildAddedUnits :94-124, buildPerKeyStrategyForBuilderSet :247-284, mergeAddedIntoPerKeySet :313-354)
    - src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts (existing T8 "no id is BOTH UUID and scope_ref" + PK2 "id === api_key_id" pins — extend, do not duplicate)
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md (§1 — the surviving path is the replacement; Don't-Hand-Roll)
  </read_first>
  <behavior>
    - buildAddedOnlySet(added, returnsLookup, metaLookup) output deep-equals mergeAddedIntoPerKeySet({ strategies: [], state: { selected: {}, weights: {}, startDates: {} } }, added, returnsLookup, metaLookup) for a non-empty added set (2+ strategies, one with returns, one without).
    - buildAddedOnlySet([]) returns the empty set shape ({ strategies: [], state: { selected: {}, weights: {}, startDates: {} } }) — matching today's empty blank output.
    - Added units in the output: selected[id] === true, weights[id] === 0, startDates[id] === returns[0]?.date ?? "2022-01-01" (the F9 H-0133 test-pinned invariants, inherited via buildAddedUnits).
    - No-alias (ENGINE-04 precondition b): merging a per-key set (unit id = api-key UUID) with added strategies (ids = distinct strategy UUIDs) preserves the strategy COUNT (per-key count + added count) and every output id is unique — the set never silently merges two units.
    - Id-format pin (ENGINE-05 runtime precursor): every strategy id in the outputs of buildAddedOnlySet, mergeAddedIntoPerKeySet, and buildPerKeyStrategyForBuilderSet satisfies id.startsWith("holding:") === false.
  </behavior>
  <action>Export `buildAddedOnlySet(addedStrategies, addedStrategyReturnsLookup, addedStrategyMetadataLookup)` from scenario-adapter.ts as a thin delegation to the existing private `buildAddedUnits` plus the selected/weights/startDates trio (selected=true, weight=0, startDate = returns[0]?.date ?? "2022-01-01") — i.e. the empty-per-key `mergeAddedIntoPerKeySet` reduction, made greppable. Mirror the JSDoc convention of `buildPerKeyStrategyForBuilderSet`. Do NOT write a new inline StrategyForBuilder literal loop (Don't-Hand-Roll — the defaults are F9 H-0133 test-pinned invariants); do NOT modify buildAddedUnits, mergeAddedIntoPerKeySet, or buildStrategyForBuilderSet (the builder deletes in Plan 04, not here). In scenario-adapter.test.ts add a new describe block covering every case in behavior, including the deep-equality equivalence oracle against mergeAddedIntoPerKeySet-with-empty-per-key. Write assertions RED-first where possible (the wrapper doesn't exist yet — the import itself is the RED anchor). Commit as one atomic act.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function buildAddedOnlySet" "src/app/(dashboard)/allocations/lib/scenario-adapter.ts" exits 0.
    - scenario-adapter.test.ts contains a deep-equality assertion between buildAddedOnlySet output and mergeAddedIntoPerKeySet with an empty per-key set (grep for both identifiers inside the new describe block).
    - scenario-adapter.test.ts contains an assertion matching /startsWith\(["']holding:["']\)/ applied to builder outputs (the no-alias/id-format pin).
    - npx vitest run on scenario-adapter.test.ts exits 0; npx tsc --noEmit exits 0.
    - git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts outputs nothing (GUARD-03).
    - buildStrategyForBuilderSet is UNTOUCHED in this task (git diff for this commit shows no hunk inside scenario-adapter.ts:126-212's function body other than possible import-region additions).
  </acceptance_criteria>
  <done>buildAddedOnlySet exported and equivalence-pinned; no-alias + id-format assertions green; frozen engine zero-diff.</done>
</task>

<!-- RESEQUENCED (orchestrator decision, 2026-07-03 — executor checkpoint Option A):
Task 2 (ENGINE-03) MOVED to plan 63-02 Task 0. The blank-init flip breaks 24
default-payload composer tests; 9 of them (H-0487, WINDOW-06 Deselect, 7× Phase-59
PERSIST-01 window-reopen) require the (gate=false AND book) state ENGINE-03 removes
and are the GUARD-02 break-by-construction set owned by the ENGINE-01 deletion.
Landing ENGINE-03 alone forces either out-of-scope machinery work or it.skip on a
shipped feature's suite during composer surgery. ENGINE-03 therefore lands
atomically WITH the ENGINE-01 deletion in 63-02. Wave-1 artifacts: RED tests were
committed as 6a0960a1 then reset off the branch tip (still reachable by hash for
cherry-pick); the working GREEN impl is preserved as
/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/bcad0af2-5069-474b-8e0b-1997c063b848/scratchpad/engine03-impl.patch
The task text below is retained for reference; 63-02 Task 0 is authoritative. -->
<task type="auto" tdd="true">
  <name>Task 2 [MOVED → 63-02 Task 0]: ENGINE-03 — gate=false blank-mode init + note repoint (init and note in the SAME commit — Pitfall 2)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (init :691-694, handleEntryModeSelect :1126-1137, segment control :2985-3029, showDataSourcesFallback :1805-1814, note render :3269-3280, MEMBER-04 disclosure :1327-1331, save stamp :1441-1444)
    - DESIGN.md (MANDATORY before the affordance styling decision — the only visual change this phase)
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-RESEARCH.md (§ENGINE-03 Concrete Touch Points, Pitfall 2)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (existing DSRC-02 note tests + entry-mode blocks — find the testids and harness in use)
  </read_first>
  <behavior>
    - RED first: render the composer with hasLiveBook=true + perKeyDailiesGateSatisfied=false + eligibleApiKeyIds non-empty → initial entryMode is BLANK (added-only composer renders) AND data-testid="scenario-data-sources-fallback" is in the document with the locked copy "Per-source modeling needs per-key history." (both assertions fail against current code: init is book, and once init is blank the OLD note condition entryMode==="book" would kill the note — this pair is the Pitfall-2 pin).
    - gate=false + book-entry affordance: attempting to select "book" (via the chosen affordance) does NOT enter book mode — entryMode stays "blank".
    - gate=true + hasLiveBook: initial entryMode is "book", the note does NOT render, segment control behaves exactly as before (existing tests must stay green unmodified).
    - No live book (hasLiveBook=false): init blank, note does NOT render when eligibleApiKeyIds is empty (existing behavior preserved).
    - Reopen edge (pin per RESEARCH §ENGINE-03 item 5): opening a saved BOOK draft (memberKeyIds non-empty) under gate=false renders the forced-blank composer without throwing; the MEMBER-04 disclosure machinery is untouched.
  </behavior>
  <action>In ONE commit (Pitfall 2 — the init change and the note repoint must never separate): (1) repoint the entryMode init (:692) to `hasLiveBook && payload.perKeyDailiesGateSatisfied ? "book" : "blank"`; (2) make `handleEntryModeSelect` (:1126) refuse "book" when `!payload.perKeyDailiesGateSatisfied`; (3) repoint `showDataSourcesFallback` (:1811) from `entryMode === "book"` to `hasLiveBook` (keeping the `!gate && eligible>0` conjuncts and the locked copy verbatim); (4) segment-control affordance (:2997): book entry becomes unavailable-with-note when gate=false — disabled-with-explanation vs hidden is Claude's discretion WITHIN DESIGN.md (read it first); keep the radiogroup arrow-key handler (:2990-2995) consistent with the choice (an arrow-key must not land focus in an unreachable mode). Read-only-empty book mode was explicitly rejected at kickoff — do not build it. Do NOT touch: the save stamp (:1441-1444 — already gate-aware, forced blank stamps []), the MEMBER-04 disclosure (:1327-1331), scenarioAum, handleCommit, or anything in src/lib/scenario.ts / scenario-window.ts (GUARD-03). Blank-mode commit already refuses adds with the existing AUM error (scenarioAum=0) — existing behavior, do not "fix". Author the behavior tests RED-first in ScenarioComposer.test.tsx, capture RED, then implement GREEN.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'perKeyDailiesGateSatisfied' output shows the entryMode init line now references the gate (grep the useState init region), and grep on the showDataSourcesFallback assignment no longer contains `entryMode === "book"`.
    - The locked note copy "Per-source modeling needs per-key history." is byte-identical (grep -F exits 0) and data-testid="scenario-data-sources-fallback" is still the hook.
    - New gate=false describe block green; the RED state was captured before implementation (RED noted in the commit sequence: test commit fails, feat commit passes).
    - P61-BUG-1 block (ScenarioComposer.test.tsx:7262 "added strategies join the per-key book projection") passes VERBATIM — git diff for this task shows zero hunks inside that describe block (GUARD-02).
    - "Pitfall 3 … NOT collapsed (avg-ρ honest)" block (~:4838) passes verbatim.
    - Full ScenarioComposer.test.tsx run exits 0; npx tsc --noEmit exits 0.
    - git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts outputs nothing (GUARD-03).
  </acceptance_criteria>
  <done>gate=false initializes blank with the note rendered (never a silent blank composer); book entry unreachable at gate=false; gate=true behavior byte-identical; P61 pins green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR payload → client composer | `perKeyDailiesGateSatisfied` / `eligibleApiKeyIds` are server-computed; the client only branches on them (no new untrusted input) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-01 | Denial of Service (broken UI for gate=false holders) | composer entryMode init + DSRC-02 note | mitigate | init + note repoint land in ONE commit (Pitfall 2), pinned by the paired RED-first render test on `scenario-data-sources-fallback` |
| T-63-SC | Tampering | npm/pip/cargo installs | accept (N/A) | zero package installs this phase (RESEARCH Package Legitimacy Audit: not applicable); any install task appearing is a plan violation |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism` exits 0
- `npx tsc --noEmit` and `npm run lint` clean on touched files
- GUARD-03: `git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts` prints nothing
- P61 verbatim survivors green: composer P61 block (:7262), Pitfall-3 avg-ρ block (~:4838)
- Wave gate (RESEARCH Sampling Rate): full `npm test` exits 0 before the wave merges
</verification>

<success_criteria>
- buildAddedOnlySet exported, equivalence- and no-alias-pinned (ENGINE-04 precondition b banked early)
- ENGINE-03 satisfied: gate=false → blank init + rendered note + unreachable book entry, D1-honest
- Zero behavior change for gate=true books; frozen engine zero-diff
</success_criteria>

<output>
Create `.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-01-SUMMARY.md` when done
</output>
