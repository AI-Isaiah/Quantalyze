---
phase: 63-holdings-snapshot-fallback-engine-removal
plan: 04
type: execute
wave: 4
depends_on: [63-03]
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-adapter.ts
  - src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts
  - src/app/(dashboard)/allocations/lib/drawdown.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx
  - src/app/(dashboard)/allocations/components/__tests__/AllocationsTabs.scenario-state-preservation.test.tsx
  - src/app/scenario-share/[token]/share-resolve.ts
  - src/lib/queries.ts
  - src/lib/queries.my-allocation.test.ts
  - src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts
  - src/lib/scenario-dealias.ts
  - src/lib/scenario-dealias.test.ts
  - src/lib/scenario-history.ts
  - src/lib/scenario-history.test.ts
autonomous: true
requirements: [ENGINE-04]
must_haves:
  truths:
    - "buildStrategyForBuilderSet no longer exists; buildAddedUnits, buildPerKeyStrategyForBuilderSet, mergeAddedIntoPerKeySet, and buildAddedOnlySet survive verbatim"
    - "The gate=false SSR live-baseline is the emptyDefault shape (AUM preserved from holdings, all metrics null → honest em-dash KPI) — liveBaselineMetricsFromHoldings is retired"
    - "src/lib/scenario-dealias.ts and its test file are deleted in a single reviewed re-baseline commit whose message carries the rationale, ONLY after all three preconditions pass: (a) avg-ρ honesty green, (b) no-alias assertion green, (c) grep proves 0 production importers — import-specifier scoped AND broad-token, unblocked because Task 2 rewords scenario-history.ts:25 BEFORE the gate"
    - "holdingsSummary and reconstructHoldingReturnsByScopeRef STAY in queries.ts (Holdings tab + commit sizing + payload contract untouched — locked)"
    - "Frozen engine zero-diff vs origin/main"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "gate=false liveBaselineMetrics ternary repointed to the emptyDefault shape; no scenario-dealias import"
    - path: "src/lib/scenario-dealias.ts"
      provides: "ABSENT — module retired (verify with test -f returning false)"
  key_links:
    - from: "queries.ts liveBaselineMetrics ternary (gate=false branch)"
      to: "emptyDefault shape (AUM from holdings, null metrics)"
      via: "direct construction replacing liveBaselineMetricsFromHoldings"
      pattern: "liveBaselineMetrics"
---

<objective>
ENGINE-04: retire the alias machinery. Three staged acts in the locked order:
(stage 3) delete `buildStrategyForBuilderSet` from the adapter now that both call
sites are gone; (inserted stage — the RESEARCH discovery) repoint the queries.ts
gate=false SSR baseline to the in-function `emptyDefault` and retire
`liveBaselineMetricsFromHoldings` — the THIRD dealias importer and the hard
prerequisite for ENGINE-04's "0 production importers" precondition (Pitfall 1);
(final stage) delete `src/lib/scenario-dealias.ts` as a single reviewed re-baseline
commit after all three preconditions pass.

Purpose: completes the deletion chain; after this plan no production module builds
holdings engine units or collapses aliases anywhere.
Output: adapter shrunk, SSR baseline honest for gate=false (0 real users), dealias
module gone, every test retirement a reviewed act.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-RESEARCH.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-03-SUMMARY.md

<interfaces>
<!-- Verified on branch v1.6-membership-schema-v4 — RE-GREP anchors before editing. -->
scenario-adapter.ts:
  :126-212  export function buildStrategyForBuilderSet — DELETE (both call sites gone after Plans 02/03)
  :34-42    buildHoldingRef import + HoldingRefInput type (only used by the deleted builder) — DELETE with it
  :68-79    ScenarioAdapterInputs interface — DELETE or shrink (grep first: no production consumer of the interface itself)
  KEEP verbatim: buildAddedUnits (:88-124), buildPerKeyStrategyForBuilderSet (:247-284), mergeAddedIntoPerKeySet (:313-354), buildAddedOnlySet (Plan 01)

⚠ TASK-1 SEQUENCING HAZARD (locked): src/lib/scenario-dealias.test.ts:13 imports and :297 CALLS
buildStrategyForBuilderSet ("Drive the REAL adapter"). That file deletes ONLY in Task 3, behind the
3-precondition reviewed re-baseline gate. So between Task 1 and Task 3, tsc is red on that ONE file
BY DESIGN, and the buildStrategyForBuilderSet token remains in the tree at exactly three
non-Task-1-owned sites: scenario-dealias.test.ts:13/:297 (Task 3 deletion) and queries.ts:2085
(Task 2 comment reword). Task 1's gates are scoped accordingly; the tree-wide grep gate lives in
Task 3's acceptance, where it is genuinely satisfiable.

queries.ts:
  :37         collapseAliasedHoldingStrategies import — DELETE (with the function below)
  :2131-2241  liveBaselineMetricsFromHoldings — RETIRE (builds holdings units :2154-2173, collapses :2202, computeScenario)
  :2140-2148  emptyDefault (IN the function today) — the replacement shape:
              { aum: totalAum, ytdTwr: null, sharpe: null, maxDd: null, avgRho: null, equity: [], drawdown: [] }
              (AUM preserved from holdings; null metrics → KpiStrip honest "—" convention). Hoist/preserve it.
  :3151-3159  the ternary: perKeyDailiesGateSatisfied ? liveBaselineMetricsFromPerKeyDailies(...) : liveBaselineMetricsFromHoldings(...)
              → repoint the FALSE branch to the emptyDefault shape (still fed totalAum from holdings)
  :2085       stale comment citing scenario-adapter.ts::buildStrategyForBuilderSet — REWORD dropping the
              literal identifier (this reword is what unblocks Task 3's tree-wide grep gate)
  :2256, :2260, :2267, :2278, :2285, :2340, :2362, :2932 — comment-ONLY mentions of
              liveBaselineMetricsFromHoldings inside the KEPT liveBaselineMetricsFromPerKeyDailies
              (:2256-2362) and the holdings read (:2932) — REWRITE as doc-only edits (the enclosing
              branch logic stays byte-untouched). The D3 fallback doc block around :2256 is now FALSE:
              its SEMANTICS must be rewritten to the new truth — gate=false → emptyDefault (AUM
              preserved from holdings, all metrics null), NO holdings-snapshot reconstruction — not
              merely de-identified. These rewrites are what make Task 2's grep-exit-1 gate on the
              identifier satisfiable (grep counts comments — gate hygiene).
  STAYS (locked): holdingsSummary production, reconstructHoldingReturnsByScopeRef + the holdingReturnsByScopeRef
  payload field (payload contract untouched this phase — Open Question 1 resolution: LEAVE IT, note as future cleanup),
  isPerKeyDailiesEligibleKey (:2429-2437), the holdings read (:2703)

drawdown.ts (src/app/(dashboard)/allocations/lib/drawdown.ts):
  :4 (header doc, :2-:5 region) — production doc comment citing liveBaselineMetricsFromHoldings as the
  SSR caller of deriveSnapshotDrawdowns — Task 2 doc-only reword: repoint the citation to the surviving
  SSR caller (liveBaselineMetricsFromPerKeyDailies); zero code hunks in this file.

Dealias retirement preconditions (ALL THREE before the delete commit):
  (a) avg-|ρ| honesty green — the Pitfall-3 "NOT collapsed (count preserved)" block (ScenarioComposer.test.tsx ~:4838)
  (b) explicit no-alias assertion green — landed in Plan 01 (scenario-adapter.test.ts)
  (c) grep proves 0 production importers — BOTH scopes, for robustness:
      import-specifier: grep -rnE "from ['\"].*scenario-dealias" src --include="*.ts*" | grep -v "\.test\." → empty
      broad token:      grep -rln "scenario-dealias" src --include="*.ts*" | grep -v "\.test\." → empty
      (the broad gate would DEADLOCK on scenario-history.ts:25's "sibling scenario-dealias.ts"
      comment unless Task 2 lands its reword first — which it does, by design)
Re-baseline commit class: v1.5 ADR-001 precedent (phase-52-frozen-spine-guards.test.ts:14-19, 158-165) —
one commit, rationale in the message, retiring together: the module, scenario-dealias.test.ts,
and the scenario-history doc-comment repoints.

Test fallout owned by this plan:
  scenario-adapter.test.ts :45-345 (T1-T15 + B4-signature — retire with the builder) and :416-591
  (H-0132 commit-oracle round-trip — retire; note handleCommit itself is draft-based and unchanged,
  the oracle's SUBJECT vanishes, not the commit gate)
  ScenarioComposer.save.test.tsx :100 (dead mock key), :111 (REAL import — tsc breaks the moment the
  export deletes), :703-:713 and :735-:738 (vi.mocked blocks) — Task 1, SAME commit as the builder
  deletion; plumbing-only, oracles untouched
  AllocationsTabs.scenario-state-preservation.test.tsx :141 (dead mock key) — Task 1, same commit,
  plumbing-only
  getMyAllocationDashboard.scenario.test.ts :23 (import), :595/:614 (direct liveBaselineMetricsFromHoldings
  fallback calls) — Task 2, explicit reviewed disposition: retire with the function / repoint to the
  new gate=false → emptyDefault expectation
  queries.my-allocation.test.ts — FULL liveBaselineMetricsFromHoldings fallout map (Task 2; re-grep — SIX sites,
  each with an explicit retire-vs-repoint disposition, stated per block in Task 2's action):
    :492         comment only — doc-only reword
    :1492-:1560  H-0487/H-0493 SSR collapse call-site guard describe (direct calls :1524/:1527/:1541) —
                 RETIRE (its subject retires); the NEW gate=false → emptyDefault expectation replaces its coverage
    :1672-:1682  difference-oracle INSIDE the KEPT "per-key branch" blend test — REPOINT, never retire
    :1685-:1710  "fallback branch → snapshot reconstruction" test — pins OLD gate=false behavior, breaks
                 by construction — retire/repoint into the new emptyDefault pin
    :1966-:1992  partial-coverage whole-allocator fallback test — REPOINT to the new expected result
    :2062-:2090  C1-regression difference-oracle INSIDE a KEPT gate test — REPOINT the oracle only
  scenario-history.ts :25 ("sibling scenario-dealias.ts" convention comment — MUST land before the
  Task 3 gate or precondition (c) deadlocks) + :50 ("output of collapseAliasedHoldingStrategies") —
  Task 2 doc-only rewords; scenario-history.test.ts :10 stays with the Task 3 re-baseline commit
  share-resolve.ts :180 (comment cites the builder's weight-0 invariant — repoint citation to
  buildAddedUnits) and :244 (negative-doc "There is NO collapseAliasedHoldingStrategies here" —
  reword WITHOUT the literal identifier, e.g. "There is NO alias-collapse step here", or the Plan-05
  ENGINE-05 source scan goes permanently red)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stage 3 — delete buildStrategyForBuilderSet + adapter test retirements</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-adapter.ts, src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx, src/app/(dashboard)/allocations/components/__tests__/AllocationsTabs.scenario-state-preservation.test.tsx, src/app/scenario-share/[token]/share-resolve.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-adapter.ts (:34-42, :68-79, :126-212; the KEEP list)
    - src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts (:45-345 builder blocks, :416-591 H-0132 block; Plan-01 no-alias block and T8/PK2 — KEEP)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx (:100 mock key, :111 REAL import, :703-:713/:735-:738 vi.mocked — plumbing only; the entryMode-stamping oracles stay verbatim)
    - src/app/(dashboard)/allocations/components/__tests__/AllocationsTabs.scenario-state-preservation.test.tsx (:141 mock key — plumbing only)
    - src/app/scenario-share/[token]/share-resolve.ts (:180 + :244 comments; :147-191 already-series-space unit build — read, verify no code change needed)
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md (§1, §8 dispositions)
  </read_first>
  <action>One reviewed commit: delete buildStrategyForBuilderSet (:126-212), its now-orphan buildHoldingRef/HoldingRefInput imports (:34-42), and delete-or-shrink ScenarioAdapterInputs (:68-79) after a grep confirms no production consumer. Retire the adapter test blocks whose subject vanishes — T1-T15 + B4-signature (:45-345) and the H-0132 commit-oracle block (:416-591) — with the commit message stating each retirement's rationale (builder deleted; H-0132's subject vanishes while handleCommit itself is draft-based and unchanged). KEEP the Plan-01 buildAddedOnlySet/no-alias block and the T8/PK2 pins verbatim. In the SAME commit (the :111 REAL import otherwise breaks tsc the moment the export deletes), drop the dead buildStrategyForBuilderSet mock plumbing as a mapped reviewed act: ScenarioComposer.save.test.tsx :100 mock key, :111 import, :703-:713/:735-:738 vi.mocked blocks; AllocationsTabs.scenario-state-preservation.test.tsx :141 mock key. Plumbing-only (rationale for the commit message: the composer stopped calling the builder at Wave 2 — Plan 02 — so these mocks are dead wiring; the suites' oracles — entryMode stamping, scenario-state preservation — must show ZERO assertion hunks). Doc-only repoints in share-resolve.ts: :180 comment citation from buildStrategyForBuilderSet to buildAddedUnits, and :244 negative-doc reworded WITHOUT the literal collapseAliasedHoldingStrategies identifier (e.g. "There is NO alias-collapse step here") so the Plan-05 ENGINE-05 comment-inclusive scan can ban the token in this file (share-resolve code is already series-space, verified; no code hunk allowed in that file). ⚠ Do NOT delete or edit src/lib/scenario-dealias.test.ts in this task, under ANY circumstance — it imports (:13) and CALLS (:297) buildStrategyForBuilderSet ("Drive the REAL adapter"), so it goes tsc-red the moment the export deletes and STAYS red until Task 3. That is the designed intermediate state: the file's deletion is LOCKED behind Task 3's 3-precondition reviewed re-baseline gate (ENGINE-04), and deleting or patching it early to green this task's gates would bypass that gate. This task's tsc and grep gates are scoped below precisely so no workaround is needed.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" "src/app/(dashboard)/allocations/components/__tests__/AllocationsTabs.scenario-state-preservation.test.tsx" --no-file-parallelism && npx tsc --noEmit 2>&1 | grep -v "scenario-dealias\.test" | grep -c "error TS" | grep -qx "0" && echo "sources compile clean — tsc errors, if any, confined to scenario-dealias.test.ts (deletes in Task 3)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "buildStrategyForBuilderSet" exits 1 against EACH of the five files this task owns (comments included by design): src/app/(dashboard)/allocations/lib/scenario-adapter.ts, src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx, src/app/(dashboard)/allocations/components/__tests__/AllocationsTabs.scenario-state-preservation.test.tsx, "src/app/scenario-share/[token]/share-resolve.ts". The token's ONLY remaining tree occurrences are owned elsewhere by design: queries.ts:2085 (Task 2 reword) and scenario-dealias.test.ts:13/:297 (Task 3 gated deletion) — the tree-wide grep-exit-1 gate lives in Task 3's acceptance.
    - git diff for the two mock-plumbing files is plumbing-only: zero hunks inside any it()/expect body of ScenarioComposer.save.test.tsx or AllocationsTabs.scenario-state-preservation.test.tsx; both suites pass otherwise unmodified.
    - grep -q "collapseAliasedHoldingStrategies" "src/app/scenario-share/[token]/share-resolve.ts" exits 1 (the :244 negative-doc reword landed — ENGINE-05 scan precondition).
    - buildAddedUnits, buildPerKeyStrategyForBuilderSet, mergeAddedIntoPerKeySet, buildAddedOnlySet all still present in scenario-adapter.ts (four grep -q exit 0) with zero diff hunks in their bodies.
    - share-resolve.ts diff for this commit is comment-only (git diff -- the file shows no non-comment line changes).
    - src/lib/scenario-dealias.test.ts shows ZERO diff hunks in this task (git diff for the commit does not touch it — its deletion is Task 3's gated act).
    - Adapter suite green; tsc errors, if any, confined to src/lib/scenario-dealias.test.ts (pending Task 3 — the scoped pipeline in verify proves all other sources compile). git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts empty (GUARD-03).
  </acceptance_criteria>
  <done>The holdings→units builder no longer exists in production or any Task-1-owned file; surviving adapter path verbatim; scenario-dealias.test.ts untouched, red on tsc by design pending Task 3.</done>
</task>

<task type="auto">
  <name>Task 2: Inserted stage — queries.ts gate=false baseline repoint to emptyDefault (the 3rd importer, ENGINE-04 prerequisite)</name>
  <files>src/lib/queries.ts, src/lib/queries.my-allocation.test.ts, src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts, src/lib/scenario-history.ts, src/app/(dashboard)/allocations/lib/drawdown.ts</files>
  <read_first>
    - src/lib/queries.ts (:37 import; liveBaselineMetricsFromHoldings :2131-2241 incl. emptyDefault :2140-2148; the ternary :3151-3159; :2085 comment; the STAYS list)
    - src/lib/queries.my-allocation.test.ts (the FULL six-site fallout map from the interfaces block: :492, :1492-:1560, :1672-:1682, :1685-:1710, :1966-:1992, :2062-:2090 — re-grep each anchor)
    - src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts (:23 import; :595/:614 direct fallback-call blocks — the retiring subject's last test consumers)
    - src/lib/scenario-history.ts (:25 "sibling scenario-dealias.ts" comment; :50 "output of collapseAliasedHoldingStrategies" comment — doc-only rewords in THIS commit, before the Task 3 gate)
    - src/app/(dashboard)/allocations/lib/drawdown.ts (:2-:5 header doc — the :4 liveBaselineMetricsFromHoldings citation; doc-only reword, zero code hunks)
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md (§4 — the load-bearing finding + exact BEFORE excerpts)
  </read_first>
  <action>One reviewed commit (rationale in message: the gate=false population is 0 real users — 2 prod residue rows GUARD-01 deletes; the honest null-metric emptyDefault replaces a holdings-snapshot computation that is itself the machinery this phase removes; keeping it would re-poison avgRho with fabricated ρ=1.0, the H-0487/H-0493 class): repoint the ternary's FALSE branch (:3151-3159) to the emptyDefault shape (AUM from holdings preserved — totalAum still computed from phase07.holdingsSummary; all metrics null; equity/drawdown empty), then RETIRE liveBaselineMetricsFromHoldings (:2131-2241) and the collapseAliasedHoldingStrategies import (:37) in the same commit (never leave an unused importer between commits). Preserve/hoist the emptyDefault construction — do NOT hand-roll a local mini-collapse (Don't-Hand-Roll). Reword the :2085 stale comment DROPPING the literal buildStrategyForBuilderSet identifier (this reword unblocks Task 3's tree-wide grep gate), and rewrite the comment-ONLY liveBaselineMetricsFromHoldings mentions inside the KEPT code (:2256, :2260, :2267, :2278, :2285, :2340, :2362, :2932) as doc-only edits — the enclosing liveBaselineMetricsFromPerKeyDailies branch logic and the :2932 holdings read stay byte-untouched; the D3 fallback doc block around :2256 MUST be rewritten to the new truth (gate=false → emptyDefault: AUM preserved, all metrics null — NO holdings-snapshot reconstruction), never merely de-identified. Also reword the drawdown.ts:4 header-doc citation (production doc comment naming liveBaselineMetricsFromHoldings as the SSR caller of deriveSnapshotDrawdowns) to the surviving SSR caller, liveBaselineMetricsFromPerKeyDailies — doc-only, zero code hunks in drawdown.ts. In queries.my-allocation.test.ts, disposition ALL SIX fallout sites explicitly (each an individually reviewed act with inline rationale — no mechanical sweep): (i) :492 comment — doc-only reword dropping the identifier; (ii) :1492-:1560 SSR collapse call-site guard describe (H-0487/H-0493, direct calls :1524/:1527/:1541) — RETIRE (rationale: its subject retires) and ADD the replacement expectation pinning gate=false → emptyDefault (aum preserved AND ytdTwr/sharpe/maxDd/avgRho all null); (iii) :1672-:1682 — the falsifiable difference-oracle INSIDE the KEPT "per-key branch: every active key has dailies" blend test: REPOINT, never retire — the kept per-key coverage must keep a falsifiable axis; replace the snapshot-reconstruction contrast with an emptyDefault contrast (assert the per-key result's metrics are non-null, i.e. NOT the new gate=false shape — proves the per-key branch, not the fallback, produced them); (iv) :1685-:1710 "fallback branch: no per-key rows → stats derive from the snapshot reconstruction" — pins the OLD gate=false behavior and breaks by construction: retire/repoint this block INTO the new gate=false → emptyDefault pin (natural home for the added expectation in (ii)); (v) :1966-:1992 partial-coverage whole-allocator fallback test — REPOINT: the honesty truth it pins survives (never a blended half-per-key/half-snapshot curve) but the expected result becomes emptyDefault (null metrics, AUM preserved) and must still NOT equal the per-key-only blend; (vi) :2062-:2090 C1 regression — KEPT test: REPOINT only the :2079 snapMetrics difference-oracle (assert the result's metrics are non-null / NOT the emptyDefault shape → proves the per-key branch activated); the per-key equality assertions and the D2 AUM pin stay byte-unchanged. In getMyAllocationDashboard.scenario.test.ts — explicit reviewed disposition: retire the :595/:614 direct-fallback-call blocks and the :23 import WITH the function (rationale for the commit message: the subject vanishes; the SSR gate=false behavior is re-pinned by the NEW emptyDefault expectation above); if any block's oracle axis is the SSR ternary rather than the function itself, REPOINT it to the gate=false → emptyDefault expectation instead of retiring. In scenario-history.ts, reword the :25 "sibling scenario-dealias.ts" convention comment and the :50 "output of collapseAliasedHoldingStrategies" comment (doc-only, zero code hunks) — these MUST land in this commit or Task 3's precondition-(c) broad grep deadlocks on :25. DO NOT touch: holdingsSummary production, reconstructHoldingReturnsByScopeRef, the holdingReturnsByScopeRef payload field (LEAVE IT — Open Question 1; note as future cleanup in the SUMMARY), isPerKeyDailiesEligibleKey, the gate=true per-key branch.</action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-allocation.test.ts "src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts" --no-file-parallelism && npx tsc --noEmit 2>&1 | grep -v "scenario-dealias\.test" | grep -c "error TS" | grep -qx "0" && echo "sources compile clean — tsc errors, if any, confined to scenario-dealias.test.ts (deletes in Task 3)"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "liveBaselineMetricsFromHoldings" src/lib/queries.ts exits 1 (comments included by design — satisfiable via this task's doc-only rewrites of :2256-:2932); grep -q "scenario-dealias" src/lib/queries.ts exits 1; grep -q "buildStrategyForBuilderSet" src/lib/queries.ts exits 1 (the :2085 reword landed — Task 3's tree-wide gate unblocked).
    - grep -q "liveBaselineMetricsFromHoldings" src/lib/queries.my-allocation.test.ts exits 1 (all six fallout sites dispositioned, comments included).
    - grep -q "liveBaselineMetricsFromHoldings" "src/app/(dashboard)/allocations/lib/drawdown.ts" exits 1 (the :4 header-doc reword landed); git diff for drawdown.ts is comment-only.
    - grep -q "liveBaselineMetricsFromHoldings" "src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts" exits 1 (import + both direct-call blocks dispositioned).
    - grep -q "scenario-dealias" src/lib/scenario-history.ts exits 1 AND grep -q "collapseAliasedHoldingStrategies" src/lib/scenario-history.ts exits 1 (the :25/:50 rewords landed — Task 3 gate unblocked).
    - The rewritten doc block near :2256 states the new gate=false → emptyDefault semantics (null metrics, AUM preserved) with no snapshot-reconstruction claim remaining.
    - git diff for this commit shows zero non-comment hunks inside liveBaselineMetricsFromPerKeyDailies and zero code hunks of any kind in scenario-history.ts.
    - grep -q "reconstructHoldingReturnsByScopeRef" src/lib/queries.ts exits 0 and grep -q "holdingsSummary" src/lib/queries.ts exits 0 (locked stays).
    - queries.my-allocation.test.ts contains a gate=false expectation asserting aum is preserved and the four metric fields are null (grep the new block for toBeNull/null assertions); the KEPT per-key blend test (:1672 region) and the C1 regression (:2062 region) each retain a falsifiable non-null/emptyDefault-contrast oracle with an inline Phase-63 rationale comment.
    - Both suites green; tsc errors, if any, confined to src/lib/scenario-dealias.test.ts (pending Task 3 — the scoped pipeline in verify proves all other sources compile). git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts empty (GUARD-03).
  </acceptance_criteria>
  <done>queries.ts no longer imports scenario-dealias (identifier-clean, comments included); gate=false SSR baseline honest and test-pinned with kept-coverage oracles repointed, never retired; stale docs (queries.ts :2085/:2256-:2932, drawdown.ts :4) reworded; scenario-history rewords landed ahead of the Task 3 gate.</done>
</task>

<task type="auto">
  <name>Task 3: Final stage — dealias module retirement as a reviewed re-baseline commit (3-precondition gate)</name>
  <files>src/lib/scenario-dealias.ts, src/lib/scenario-dealias.test.ts, src/lib/scenario-history.test.ts</files>
  <read_first>
    - src/lib/scenario-dealias.ts (read the header docstring so the re-baseline commit message can cite what is being retired and why it existed)
    - src/lib/scenario-history.test.ts (:10 — the last doc citation; scenario-history.ts :25/:50 were already reworded in Task 2)
    - src/__tests__/phase-52-frozen-spine-guards.test.ts (:14-19, :158-165 — the ADR-001 reviewed re-baseline precedent whose commit-message class this mirrors)
  </read_first>
  <action>GATE FIRST — run and record all three preconditions, in order, and STOP (fail loud, do not delete) if any is red: (a) `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism` green, confirming the Pitfall-3 avg-ρ honesty block passes; (b) `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts" --no-file-parallelism` green, confirming the Plan-01 no-alias assertion; (c) BOTH importer greps print nothing — import-specifier scoped: `grep -rnE "from ['\"].*scenario-dealias" src --include="*.ts*" | grep -v "\.test\."`, AND broad token: `grep -rln "scenario-dealias" src --include="*.ts*" | grep -v "\.test\."` (0 production importers; the broad gate is satisfiable only because Task 2 already reworded scenario-history.ts:25 — if it trips on a stray comment mention, land that reword as its own doc commit BEFORE deleting; never widen the grep filter instead). THEN one reviewed re-baseline commit deleting src/lib/scenario-dealias.ts and src/lib/scenario-dealias.test.ts whole, plus the doc-only repoint in scenario-history.test.ts:10 (it cites the collapse; the scenario-history.ts source comments were handled in Task 2). Commit message must carry the full rationale (ADR-001 class): what the collapse fixed (symbol-keyed holdings aliasing fabricating ρ=1.0), why it is now structurally impossible (no holdings units reach any engine; per-key ids = api_keys UUIDs, added ids = strategies UUIDs, disjoint — pinned by the no-alias assertion), and the three precondition results. Never a silent deletion.</action>
  <verify>
    <automated>test ! -f src/lib/scenario-dealias.ts && test ! -f src/lib/scenario-dealias.test.ts && [ -z "$(grep -rnE "from ['\"].*scenario-dealias" src --include='*.ts*')" ] && [ -z "$(grep -rln 'scenario-dealias' src --include='*.ts*' | grep -v '\.test\.')" ] && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/scenario-dealias.ts and src/lib/scenario-dealias.test.ts do not exist (test -f exits 1 for both).
    - grep -rq "buildStrategyForBuilderSet" src --include="*.ts*" exits 1 — the TREE-WIDE gate, comments and tests included (MOVED here from Task 1: genuinely satisfiable only now, after Task 2's queries.ts:2085 reword and this task's scenario-dealias.test.ts deletion cleared the last two carriers).
    - Both precondition-(c) greps print nothing: the import-specifier grep (grep -rnE "from ['\"].*scenario-dealias" src --include="*.ts*") and the broad-token grep (grep -rln "scenario-dealias" src --include="*.ts*" | grep -v "\.test\.").
    - The deletion commit message contains the rationale (grep the message for "re-baseline" and "no-alias").
    - FULL `npm test` exits 0 (wave gate), `npx tsc --noEmit` exits 0 (the scenario-dealias.test.ts redness designed in Tasks 1-2 resolves with the deletion), and `npm run lint` has 0 errors.
    - GUARD-03: git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts outputs nothing.
    - P61 verbatim survivors explicitly re-run green: composer P61 block, scenario-compare P61-BUG-2 + Atlas golden, T_CP8, share route T_SH13/14 (run the 4 files listed in RESEARCH's verbatim-survivor table).
  </acceptance_criteria>
  <done>Dealias machinery fully retired via a reviewed re-baseline act; tree-wide builder grep clean; full suite green; P61 net intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR → client payload | liveBaselineMetrics shape for gate=false changes (holdings-computed → emptyDefault); no new untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-06 | Tampering (silent baseline change for real users) | queries.ts gate=false ternary | mitigate | population verified 0 real users (prod-grounded 2026-07-03; GUARD-01 deletes the 2 residue rows); new test pins gate=false → emptyDefault; gate=true branch byte-untouched |
| T-63-07 | Repudiation (silent module deletion losing the aliasing safety rationale) | scenario-dealias retirement | mitigate | 3-precondition gate (honesty green, no-alias green, 0 importers) + ADR-001-class reviewed re-baseline commit carrying the rationale |
| T-63-08 | Tampering (gate bypass under red-tsc pressure) | scenario-dealias.test.ts between Tasks 1-3 | mitigate | Task 1 explicitly forbids deleting/editing the file; Task 1/2 tsc gates are scoped to exclude it; Task 1 acceptance requires zero diff hunks on it |
| T-63-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- Full `npm test` + lint + tsc green at the wave gate
- ENGINE-04 grep gate: no non-test reference to scenario-dealias anywhere in src/
- Tree-wide buildStrategyForBuilderSet grep exit 1 (Task 3 acceptance — comments and tests included)
- GUARD-03 diff empty; P61 verbatim-survivor files explicitly re-run green
- queries.ts locked stays confirmed by grep (holdingsSummary, reconstructHoldingReturnsByScopeRef present)
</verification>

<success_criteria>
- ENGINE-04 satisfied: builder deleted, third importer retired via the emptyDefault repoint, dealias module gone via a reviewed re-baseline commit with all three preconditions recorded
- No behavior change for gate=true users; gate=false SSR baseline honest (null metrics, AUM preserved)
</success_criteria>

<output>
Create `.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-04-SUMMARY.md` when done
</output>
