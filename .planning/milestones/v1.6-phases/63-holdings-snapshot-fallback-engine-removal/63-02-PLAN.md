---
phase: 63-holdings-snapshot-fallback-engine-removal
plan: 02
type: execute
wave: 2
depends_on: [63-01]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [ENGINE-01, ENGINE-03]
# ENGINE-03 RESEQUENCED IN from 63-01 (orchestrator decision, 2026-07-03): the
# blank-init flip and the holdings-path deletion are inseparable for the composer
# test suite (9 GUARD-02 break-by-construction tests need both to land together).
must_haves:
  truths:
    - "A gate=false book holder gets a BLANK-mode composer (added-only) with the DSRC-02 note rendered — never a broken or empty book UI (D1/ENGINE-03)"
    - "Book entry is unavailable-with-note when gate=false; selecting book cannot enter a mode with no engine"
    - "gate=true book behavior is unchanged (init, segment control, note suppression)"
    - "The composer builds its engine set exclusively from per-key + added-strategy units: book+gate = mergeAddedIntoPerKeySet(perKeyAdapterOutput, added…); blank/gate=false = buildAddedOnlySet(added…)"
    - "ScenarioComposer.tsx contains NO reference (code or comment) to buildStrategyForBuilderSet, collapseAliasedHoldingStrategies, mapDeAliasedWeightsToRawBasis, symbolByHoldingId, or the scenario-dealias import specifier"
    - "The optimizer apply-back calls scenario.applyWeightOverrides(weights) directly and the #528 apply-back drift behavior is unchanged"
    - "scenarioAum commit-boundary sizing, handleCommit, draft seeding, bridge add, save stamp, and liveMetricsForKpi are byte-untouched"
    - "P61-BUG-1 and the Pitfall-3 avg-ρ honesty blocks pass VERBATIM"
    - "Frozen engine zero-diff vs origin/main"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "series-space-only engine-set selection (activeAdapterOutput → mergeAddedIntoPerKeySet | buildAddedOnlySet; deAliased memo replaced by the identity pair)"
      contains: "buildAddedOnlySet"
  key_links:
    - from: "activeAdapterOutput else-branch (blank/gate=false)"
      to: "buildAddedOnlySet (scenario-adapter.ts, Plan 01)"
      via: "direct call"
      pattern: "buildAddedOnlySet\\("
    - from: "engineState window injection"
      to: "projectionState (direct — no reconstructed collapse state)"
      via: "spread of window onto the identity-pair state, in composer only (GUARD-03)"
      pattern: "projectionState"
---

<objective>
ENGINE-01: delete the holdings-snapshot path from the composer — the
`buildStrategyForBuilderSet` call site, the alias-collapse consumption
(`deAliased` memo, `symbolByHoldingId`), and the optimizer's dealias weight mapping.
The engine set becomes: book+gate → merged per-key set (unchanged); blank/gate=false →
`buildAddedOnlySet` (Plan 01's shared construction). Stage 1 of the locked deletion
order; each act an atomic revertable commit.

Purpose: the composer is the largest holdings-engine consumer; removing it first
unblocks compare (Plan 03) and the adapter/dealias retirements (Plan 04).
Output: ScenarioComposer.tsx free of holdings-engine machinery + composer test
plumbing rebased, H-0487 retired as a reviewed act.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-RESEARCH.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (landed). Consume, do not re-derive. -->
scenario-adapter.ts exports:
  buildAddedOnlySet(addedStrategies, addedStrategyReturnsLookup, addedStrategyMetadataLookup)
    → { strategies, state: { selected, weights, startDates } }  // added-only engine set, blank-mode equivalent
  mergeAddedIntoPerKeySet(perKey, added, returnsLookup, metaLookup)  // book+gate survivor, KEEP verbatim

Composer deletion map (line numbers current on branch v1.6-membership-schema-v4 — RE-GREP each anchor before editing, Plan 01 shifted this file):
  :130        buildStrategyForBuilderSet import — DELETE
  :74-76      collapseAliasedHoldingStrategies, mapDeAliasedWeightsToRawBasis imports from @/lib/scenario-dealias — DELETE
  :1681-1710  adapterOutput memo (holdings-path buildStrategyForBuilderSet call) — DELETE
  :1777-1798  activeAdapterOutput — REPLACE: if (usePerKeySources) mergeAddedIntoPerKeySet(...) else buildAddedOnlySet(...)
  :1847-1857  symbolByHoldingId memo — DELETE (no aliasing source remains)
  :1947-1955  deAliased memo (collapseAliasedHoldingStrategies call) — REPLACE with the identity pair
              { strategies: activeAdapterOutput.strategies, state: projectionState }
              (projectionState already covers selected/weights/leverage/startDates for every unit id)
  :2158-2173  engineState post-collapse window-injection idiom — SIMPLIFY: window spreads directly onto the
              identity-pair state; the "collapse drops window" HAZARD-FIX comment must be REWRITTEN.
              ⚠ ALL edits stay in the composer — never in scenario.ts / scenario-window.ts (GUARD-03).
  :3703-3711  mapDeAliasedWeightsToRawBasis in WeightOptimizerSection.onApply — REPLACE with direct
              scenario.applyWeightOverrides(weights). Safe because: the dealias docstring
              (scenario-dealias.ts:193-201) documents the mapping is the IDENTITY on all-passthrough sets
              (every set, once holdings units are gone), and the optimizer universe (:3692-3694) is filtered
              to selected ids so the weight vector covers the full basis — matching zero-then-overwrite semantics.
  :30, :53, :1845, :2691  header/inline comments citing the B4 adapter pin / holdingReturnsByScopeRef / collapse hazards — REWRITE in the same commits

Downstream consumers of the deleted deAliased memo (repoint to the identity pair):
  dateMapCache (:1956), selectedSpanById (:1976), engineState (:2167), scenarioMetrics (:2175),
  diversification memo, strategyCount props (:3661, :3678), optimizer universe (:3692)

STAYS BYTE-UNTOUCHED (verify zero-diff intent per site):
  scenarioAum (:2708-2731 — legit `holding:` DRAFT-toggle read, PRESENT-02/Phase 64), handleCommit (:2801-2860),
  useScenarioState/defaultDraftFromHoldings (:1151), bridge add (:4241-4243), save stamp (:1441-1444),
  liveMetricsForKpi (:2664-2667) + KpiStrip (:3520-3531)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 [RESEQUENCED from 63-01 Task 2]: ENGINE-03 — gate=false blank-mode init + note repoint</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-01-PLAN.md (the MOVED Task 2 — its behavior/action/acceptance text is the full spec for this task; the RESEQUENCED comment above it explains why it lives here)
    - DESIGN.md (MANDATORY before the affordance decision)
  </read_first>
  <action>Land ENGINE-03 using the Wave-1 artifacts: (1) `git cherry-pick 6a0960a1` — the already-authored RED tests (5 assertions: blank init at gate=false, note renders via repointed condition, book-entry refusal, gate=true unchanged, reopen edge). If the hash is unreachable, re-author from 63-01 Task 2's behavior block. (2) Apply the preserved GREEN impl: `git apply /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/bcad0af2-5069-474b-8e0b-1997c063b848/scratchpad/engine03-impl.patch` (86-line patch: canEnterBook = hasLiveBook && gate; init flip; handleEntryModeSelect refusal; showDataSourcesFallback → hasLiveBook; segment/arrow-key/tabIndex gating). Review the patch against 63-01 Task 2's action spec before committing — you own it now. Commit as feat with the Pitfall-2 note (init + note repoint in ONE commit). EXPECTED mid-plan red: ~24 default-payload book tests break on the init flip — do NOT fix them here; Task 2 owns every disposition. The 5 new ENGINE-03 tests MUST be green.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism -t "ENGINE-03"; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The 5 ENGINE-03 tests pass; tsc exits 0.
    - The locked note copy "Per-source modeling needs per-key history." is byte-identical; data-testid="scenario-data-sources-fallback" unchanged.
    - The save stamp (:1441-1444), MEMBER-04 disclosure (:1327-1331), scenarioAum, and handleCommit regions show zero hunks.
    - git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts outputs nothing (GUARD-03).
  </acceptance_criteria>
  <done>gate=false initializes blank with the note; book unreachable at gate=false; gate=true byte-identical; known 24-test red pending Tasks 1-2.</done>
</task>

<task type="auto">
  <name>Task 1: Delete the composer holdings path; wire the identity pair</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (every anchor in the interfaces block — re-grep line numbers first; the stays-untouched list)
    - src/lib/scenario-dealias.ts (:193-201 mapDeAliasedWeightsToRawBasis identity docstring — the justification for the direct applyWeightOverrides call; read, do not edit)
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md (§2 — target shape for activeAdapterOutput + engineSet)
  </read_first>
  <action>Apply the deletion map from the interfaces block, per D-locked staged mechanics (atomic revertable commit): delete the two import groups, the adapterOutput holdings memo, the symbolByHoldingId memo, and the mapDeAliasedWeightsToRawBasis apply-back (→ direct scenario.applyWeightOverrides(weights)); replace activeAdapterOutput with the two-branch shape (mergeAddedIntoPerKeySet | buildAddedOnlySet — RESEARCH Code Examples target shape, deps array shrinks by adapterOutput); replace the deAliased memo with the identity pair and repoint ALL its downstream consumers (dateMapCache, selectedSpanById, engineState, scenarioMetrics, diversification, strategyCount props, optimizer universe) to it; simplify the engineState window-injection idiom (window spreads directly onto the identity-pair state) and REWRITE the now-false HAZARD-FIX comment; rewrite the stale header/inline comments (:30, :53, :1845, :2691). Every edit stays inside ScenarioComposer.tsx — src/lib/scenario.ts and scenario-window.ts are frozen (GUARD-03, Pitfall 4: the temptation is to simplify window handling "one level deeper" — do not). The stays-untouched list must show zero diff hunks. tsc is the first gate (deletions surface as compile errors first).</action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism; echo "exit=$? (test failures expected ONLY in holdings-premise blocks — Task 2 resolves them)"</automated>
  </verify>
  <acceptance_criteria>
    - All four greps exit 1 (identifier absent, comments included by design) against "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx": `grep -q "buildStrategyForBuilderSet"`, `grep -q "collapseAliasedHoldingStrategies"`, `grep -q "mapDeAliasedWeightsToRawBasis"`, `grep -q "symbolByHoldingId"`; and `grep -q "scenario-dealias"` exits 1.
    - grep -q "buildAddedOnlySet" on the composer exits 0 (the else-branch replacement is wired).
    - npx tsc --noEmit exits 0.
    - git diff for this commit touches ONLY ScenarioComposer.tsx; zero hunks intersect scenarioAum (:2708-2731), handleCommit (:2801-2860), the save stamp (:1441-1444), bridge add (:4241-4243), or liveMetricsForKpi (:2664-2667) regions.
    - git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts outputs nothing (GUARD-03).
  </acceptance_criteria>
  <done>Composer engine-set selection is series-space only; compiles clean; only holdings-premise tests are red pending Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Composer test plumbing rebase + H-0487 reviewed retirement (GUARD-02 acts)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx — the FULL buildStrategyForBuilderSet map (~53 occurrences across ~25 book-mode tests spanning :31-:7671, NOT just :255-290 — re-grep every anchor first): header comment :31; vi.mock factory key :145-:156; import/realCollapse plumbing :255-:290 (:264, :274); arg/call-count oracles :979-:994 and :2251-:2253; T_C_ADAPT1-3 arg-contract blocks :2259/:2306/:2388 (call inspections :2288/:2372/:2447); 12 EMPTY defensive beforeEach mocks (:488, :4343, :4920, :5226, :5762, :5929, :6142, :6322, :6614, :6773, :7350-:7356, :7671); 26 NON-EMPTY engine-injecting mockReturnValue sites (pre-mapped in action 1b); the H-0487 block ~:644; the P61-BUG-1 block :7262; the Pitfall-3 block ~:4838
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md (§8 — commit 4b852f13 reviewed-repoint class: inline rationale comment + dedicated commit rationale)
  </read_first>
  <action>Two reviewed acts, template commit 4b852f13 (inline rationale at each changed line + commit message carrying why-it-moved / non-vacuity-preserved): (1) REBASE the adapter/dealias mock plumbing across the WHOLE file — blast radius is ~53 buildStrategyForBuilderSet occurrences across ~25 book-mode tests (:31-:7671), in three disposition classes, each site an individually reviewed act (Pitfall 5: NO mechanical file-wide sweep — this file hosts the P61-BUG-1 and Pitfall-3 verbatim survivors). (1a) DEAD WIRING — pure removal, behavior-neutral (these mocks return EMPTY sets the composer no longer reads at all): the :31 header-comment citation, the vi.mock factory key (:145-:156), the :264 import and :274 realCollapse wiring, and the 12 describe-level EMPTY beforeEach resets (:488, :4343, :4920, :5226, :5762, :5929, :6142, :6322, :6614, :6773, :7350-:7356, :7671). Two of these sit INSIDE survivor-hosting describes — Phase 37 (:4343, the describe hosting Pitfall-3) and P61-BUG-1 (:7350-:7356): removing that describe-level dead beforeEach plumbing is the ONLY hunk class permitted inside those two describes; every it() body in them stays byte-verbatim. (1b) ENGINE-INJECTION REPOINTS — 26 non-empty mockReturnValue sites whose tests' engine sets previously arrived through the mocked holdings path: Phase 10 block :669/:2027/:2091/:3115/:3154/:3312/:3704/:3827/:3980/:4025; Phase 43 :5060; WINDOW-01 + its shared helper :5198/:5253/:5474/:5551/:5620/:5721; WINDOW-04/05 presets :5854; POLISH-01 :5948/:6070; WINDOW-02/03 auto-toggle :6219; POLISH-02 :6380/:6463/:6519; WINDOW-06 :6641; Phase 59 PERSIST-01 :7074. Per-test repoint expectation, stated explicitly: post-deletion each of these tests' engine set comes from the REAL path — book+gate: per-key payload fixtures → mergeAddedIntoPerKeySet; blank/gate=false: browse-added strategies → buildAddedOnlySet — with unit ids/series/spans preserved so every WINDOW-01..06 / POLISH / PERSIST assertion body stays byte-unchanged (the verbatim-survivor rule protects ORACLES, never dead plumbing). (1c) SUBJECT-VANISHES oracles — reviewed retire-or-repoint: the :994 returns-lookup arg oracle, the :2253 single-call oracle (its truth — the composer must not re-derive the live baseline — repoints to the per-key path if preservable), and the T_C_ADAPT1-3 composer→builder arg-contract blocks (:2259/:2306/:2388): the call contract they pin no longer exists; retire with rationale or repoint to the mergeAddedIntoPerKeySet/buildAddedOnlySet contract. The DSRC recompute oracles that use REAL @/lib/scenario stay untouched. (1d) ENGINE-03 GENERIC-BOOK REPOINTS — 14 default-payload book tests that broke on Task 0's init flip (they merely need book mode, not gate=false): T_C2, T_C3b, T_C6, the read-only-rows block, commit-modal T_C18/T_C21/B11×2/Read-only-tokens/NEW-C18-13/NEW-C18-05/F-01, T_C16, T_C_M5, and the legacy-v1 block — repoint each test's payload to `perKeyDailiesGateSatisfied: true` (verified vehicle-only: holdings still render from holdingsSummary, assertions unchanged; T_C6 confirmed green with the one-line change during the 63-01 checkpoint investigation). One-line fixture repoints with a shared inline rationale comment at each describe. (2) RETIRE the H-0487 block (~:644, "multi-venue BTC collapses before computeScenario — avgRho not fabricated 1.0"). Rationale to state verbatim in the commit: RESEARCH nominally allocated this retirement to the ENGINE-04 re-baseline, but its premise (holdings units reaching the engine via the composer) dies at THIS stage — a green wave gate requires retiring it here, as its own reviewed act; the aliasing source (symbol-keyed holdings series) no longer reaches any engine, so the fabricated-ρ hazard it guarded is structurally impossible (the Pitfall-3 count-preserved block remains the living avg-ρ honesty pin). Do NOT touch any it() assertion body of the P61-BUG-1 block (:7262), the Pitfall-3 block (~:4838), the save.test entryMode stamping suites, or the WINDOW-01..06 blocks (verbatim survivors at the ORACLE level — Pitfall 5: no mechanical suite-wide sweep). Run the full composer file; then run the verbatim-survivor blocks explicitly.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Full ScenarioComposer.test.tsx run exits 0; tsc exits 0; lint 0 errors.
    - git diff for the repoint commits shows ZERO hunks inside any it() body of the P61-BUG-1 describe block (:7262) and the Pitfall-3 it() block (~:4839); the SOLE permitted hunk inside those two describes is the removal of the describe-level dead beforeEach mock plumbing (:7350-:7356 / :4343-:4346) (GUARD-02 verbatim survival — verify with git diff -U0 + block line ranges).
    - Every WINDOW-01..06 / POLISH-01/02 / PERSIST-01 assertion body is byte-unchanged: each hunk inside those describes is confined to mock/fixture plumbing (engine-set delivery repointed to the real per-key/added path) and carries an inline rationale comment.
    - The H-0487 retirement is its own commit whose message contains the stated rationale (grep the commit message for "H-0487").
    - grep -q "buildStrategyForBuilderSet" on ScenarioComposer.test.tsx exits 1 (plumbing fully rebased).
    - git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts outputs nothing (GUARD-03).
  </acceptance_criteria>
  <done>Composer suite fully green with series-space-only plumbing; H-0487 retired as a reviewed act; P61 pins verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | pure client-side deletion; no new inputs, endpoints, or auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-02 | Tampering (frozen-engine drift under deletion pressure) | src/lib/scenario.ts, scenario-window.ts | mitigate | window-idiom simplification confined to the composer; GUARD-03 zero-diff gate in every task's acceptance criteria (Pitfall 4) |
| T-63-03 | Repudiation (silent loss of the avg-ρ honesty net) | H-0487 retirement | mitigate | retirement is an individually reviewed commit with rationale; the Pitfall-3 count-preserved block stays verbatim as the living honesty pin |
| T-63-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- Full composer suite + tsc + lint green
- GUARD-03 diff empty
- P61-BUG-1 (:7262) and Pitfall-3 (~:4838) blocks pass with zero diff hunks inside their it() bodies (sole permitted describe-level hunk: dead beforeEach mock removal)
- The composer contains none of the five banned identifiers (grep gates in Task 1)
- Wave gate (RESEARCH Sampling Rate): full `npm test` exits 0 before the wave merges
</verification>

<success_criteria>
- ENGINE-01 satisfied: composer engine set = per-key + added units only, blank = buildAddedOnlySet
- Optimizer apply-back behavior (#528) unchanged via direct applyWeightOverrides
- Every test change an individually reviewed act; verbatim survivors untouched
</success_criteria>

<output>
Create `.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-02-SUMMARY.md` when done
</output>
