---
phase: 63-holdings-snapshot-fallback-engine-removal
plan: 05
type: execute
wave: 5
depends_on: [63-04]
files_modified:
  - src/__tests__/phase-63-series-space-guards.test.ts
autonomous: false
requirements: [ENGINE-05, GUARD-01, GUARD-02, GUARD-03]
user_setup: []
must_haves:
  truths:
    - "A vitest guard fails loud if buildStrategyForBuilderSet, collapseAliasedHoldingStrategies, mapDeAliasedWeightsToRawBasis, symbolByHoldingId, or a scenario-dealias import reappears in any scenario-surface production source — or if scenario-dealias / collapseAliasedHoldingStrategies / liveBaselineMetricsFromHoldings reappears in src/lib/queries.ts"
    - "A runtime guard asserts no surviving engine-set builder output ever contains an id starting with 'holding:' — and the guard was falsified during authoring (Rule 9)"
    - "The two phase10-rpc-*@test.local residue holders' allocator_holdings rows are deleted from prod (khslejtfbuezsmvmtsdn); their auth.users rows are KEPT; the grounding query returns 0 gate=false holders"
    - "The P61 regression suites pass verbatim at the phase gate (GUARD-02) and the frozen engine is zero-diff vs origin/main (GUARD-03)"
    - "Full suite + coverage ratchet green (deletion phases can drop functions/branches — verify, don't assume)"
  artifacts:
    - path: "src/__tests__/phase-63-series-space-guards.test.ts"
      provides: "ENGINE-05 source-scan + runtime guard (readFileSync class)"
      contains: "holding:"
  key_links:
    - from: "phase-63 guard test"
      to: "scenario-adapter.ts / scenario-compare.ts / ScenarioComposer.tsx / ScenarioComparePanel.tsx / share-resolve.ts / queries.ts (3-token subset)"
      via: "readFileSync source scan asserting banned-identifier ABSENCE"
      pattern: "readFileSync"
---

<objective>
Close the phase: (1) ENGINE-05 — the grep-guard test that makes the deletion durable
(fails on any reintroduction of `holding:` scopeRefs as engine unit ids); (2) GUARD-01 —
the prod residue deletion (ORCHESTRATOR-executed via Supabase MCP; the executor has NO
Supabase MCP); (3) the phase-wide GUARD-02/GUARD-03 gate: P61 verbatim survivors, frozen
engine zero-diff, full suite + coverage.

Purpose: guards convert a one-time deletion into a standing invariant; the prod cleanup
makes "0 gate=false holders" literally true.
Output: guard test file (falsified during authoring), prod verified clean, phase gate
recorded. VERSION + package.json bump is a phase-wide LAND step (at /ship), NOT a task commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-RESEARCH.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md
@.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-04-SUMMARY.md

<interfaces>
<!-- Guard classes to mirror (both verified in-repo): -->
Source-scan class: src/__tests__/admin-csrf-ratelimit-grep.test.ts — readFileSync over named
  production sources, per-file assertion with actionable failure message. THIS is the ENGINE-05 mechanism.
Git-delta class: src/__tests__/phase-52-frozen-spine-guards.test.ts — resolveBaselineRef() (:108-128,
  fail-loud per Rule 12), changedFiles(base) (:136-146), per-path assertion loop (:194-208).
  OPTIONAL for GUARD-03 (the wave-gate command is what CONTEXT demands).

ENGINE-05 scan set (production sources):
  src/app/(dashboard)/allocations/lib/scenario-adapter.ts
  src/app/(dashboard)/allocations/lib/scenario-compare.ts
  src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx
  src/app/scenario-share/[token]/share-resolve.ts
  src/lib/queries.ts  (SSR-baseline surface — 3-token subset, see below)
Banned identifiers (assert ABSENCE per file): buildStrategyForBuilderSet,
  collapseAliasedHoldingStrategies, mapDeAliasedWeightsToRawBasis, symbolByHoldingId,
  and the "scenario-dealias" import specifier — on the 5 scenario-surface files.
  queries.ts token subset: scenario-dealias, collapseAliasedHoldingStrategies,
  liveBaselineMetricsFromHoldings (the builder/optimizer tokens don't apply there).
  Scan is COMMENT-INCLUSIVE by design; it stays green because Plan 04 reworded every
  negative-doc mention (share-resolve.ts:244, queries.ts :2256-:2932, scenario-history.ts:25/:50).
⚠ Precision hazard (Pitfall 3): do NOT ban the literal "holding:" wholesale — scenarioAum
  legitimately reads scopeRef.startsWith("holding:") on DRAFT toggle refs (ScenarioComposer.tsx ~:2726,
  PRESENT-02 keeps it through Phase 64) and buildHoldingRef stays in Holdings-tab/bridge surfaces.
  The invariant is "no holding: scopeRef as an ENGINE UNIT ID", enforced by (a) banned-identifier
  absence + (b) the runtime id-format layer — never a blanket string ban.

Runtime layer (mirrors adapter T8/PK2): for every surviving builder output
  (buildPerKeyStrategyForBuilderSet, mergeAddedIntoPerKeySet, buildAddedOnlySet):
  expect(s.id.startsWith("holding:")).toBe(false)

GUARD-01 (ORCHESTRATOR-ONLY — executor has NO Supabase MCP; prod project khslejtfbuezsmvmtsdn):
  Table: allocator_holdings (owner col allocator_id, FK auth.users ON DELETE CASCADE;
  migration 20260420073003_allocator_holdings.sql). Keep auth.users rows (locked — may pin other FK residue).
  allocator_equity_snapshots rows NOT in scope.
  A1 (execution-time confirmation): api_keys owner column is user_id — confirm via
    select column_name from information_schema.columns where table_name='api_keys' and column_name in ('user_id','allocator_id');
  A2 (execution-time confirmation): re-run the grounding BEFORE deleting — if holders ≠ exactly the
    2 phase10-rpc rows, STOP and surface (never delete on a stale grounding).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ENGINE-05 guard test (source-scan + runtime layers, Rule-9 falsified)</name>
  <files>src/__tests__/phase-63-series-space-guards.test.ts</files>
  <read_first>
    - src/__tests__/admin-csrf-ratelimit-grep.test.ts (the readFileSync class — per-file loop + actionable message shape)
    - src/app/(dashboard)/allocations/lib/scenario-adapter.ts (exports for the runtime layer: buildPerKeyStrategyForBuilderSet, mergeAddedIntoPerKeySet, buildAddedOnlySet)
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-PATTERNS.md (§6 — scan set, banned identifiers, precision hazard)
  </read_first>
  <behavior>
    - Source-scan: for each of the 5 scenario-surface scan-set files, the source contains NONE of the 5 banned tokens (buildStrategyForBuilderSet, collapseAliasedHoldingStrategies, mapDeAliasedWeightsToRawBasis, symbolByHoldingId, scenario-dealias); src/lib/queries.ts contains NONE of its 3-token subset (scenario-dealias, collapseAliasedHoldingStrategies, liveBaselineMetricsFromHoldings) — one it() per file with an actionable failure message naming the token and the series-space invariant.
    - Runtime: outputs of buildPerKeyStrategyForBuilderSet, mergeAddedIntoPerKeySet, and buildAddedOnlySet (driven with representative fixtures incl. added strategies with returns) contain no strategy id with id.startsWith("holding:") === true.
    - Non-vacuity (Rule 9): during authoring, falsify BOTH layers — temporarily add a banned identifier string to one scan-set file (watch the scan go red, revert), and feed a fixture with a "holding:"-prefixed id through an assertion helper (watch the runtime layer go red, revert). Record both falsifications in the commit message.
  </behavior>
  <action>Create src/__tests__/phase-63-series-space-guards.test.ts mirroring the admin-csrf-ratelimit-grep.test.ts class: readFileSync each scan-set file (resolve paths from the repo root fail-loud — a missing file is a test FAILURE, not a skip, Rule 12), loop banned tokens per file. Add the runtime id-format layer against the three surviving builders. Do NOT ban the "holding:" literal in ScenarioComposer.tsx or anywhere outside builder OUTPUTS (Pitfall 3 — scenarioAum's DRAFT-ref read is legitimate; a blanket ban would be permanently red). File placement/naming follows the existing phase-NN guard class. This task is deliberately AFTER all removals (locked: written in-phase, end-stage).</action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-63-series-space-guards.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The guard file exists, imports readFileSync, references all 6 scan-set paths (5 scenario-surface + src/lib/queries.ts), all 5 banned tokens, and the queries.ts liveBaselineMetricsFromHoldings subset token (grep each).
    - The runtime layer asserts startsWith("holding:") === false on builder outputs (grep the pattern).
    - Guard suite green; commit message records both Rule-9 falsifications (grep the message for "falsif").
    - The guard does NOT assert on scenarioAum / draft toggle refs (no blanket "holding:" source ban — verify the scan tokens match exactly the per-file mapping: the 5 identifiers on the scenario surfaces, the 3-token subset on queries.ts).
  </acceptance_criteria>
  <done>Reintroduction of any holdings-engine identifier or holding:-id engine unit fails CI loud.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: GUARD-01 — prod residue deletion (ORCHESTRATOR executes via Supabase MCP)</name>
  <what-built>All code stages are complete; the fallback engine is deleted. The last gate=false holders on prod are the 2 phase10-rpc-*@test.local residue users' allocator_holdings rows. The executor has NO Supabase MCP — this step is executed by the ORCHESTRATOR session against prod project khslejtfbuezsmvmtsdn, autonomously per standing user policy (destructive op confirmed in CONTEXT).</what-built>
  <how-to-verify>
    Execute in ONE Supabase MCP session against khslejtfbuezsmvmtsdn, in this exact order:

    1. A1 confirmation — api_keys owner column:
       select column_name from information_schema.columns where table_name='api_keys' and column_name in ('user_id','allocator_id');
       Expect user_id. If different, adapt the grounding query's join column accordingly.

    2. A2 grounding re-run — resolve the residue ids AND confirm the holder set is unchanged:
       select id, email from auth.users where email like 'phase10-rpc-%@test.local';
       → MUST return exactly 2 rows. Then the gate=false-holders grounding (eligibility predicate from isPerKeyDailiesEligibleKey, queries.ts:2429-2437):
       select h.allocator_id, count(*) as holdings_rows
       from allocator_holdings h
       where not exists (
         select 1 from api_keys k
         where k.user_id = h.allocator_id
           and k.is_active
           and (k.sync_status is null or k.sync_status <> 'revoked')
           and k.disconnected_at is null
       )
       group by 1;
       → MUST return exactly the same 2 allocator_ids and nothing else.
       ⛔ STOP AND SURFACE if either result differs (new gate=false holders appeared — do not delete on a stale grounding).

    3. DELETE — keyed to the two EXECUTION-TIME-CONFIRMED UUIDs from step 2, NEVER a broad predicate:
       delete from allocator_holdings where allocator_id in ('<uuid-1-from-step-2>', '<uuid-2-from-step-2>');
       Scope locked: holdings rows ONLY. Do NOT delete the auth.users rows (they may pin other FK residue). allocator_equity_snapshots NOT in scope.

    4. VERIFY — re-run the step-2 grounding query: it MUST return 0 rows (0 gate=false holders remain).
  </how-to-verify>
  <files>(no repo files — prod DB khslejtfbuezsmvmtsdn, table allocator_holdings)</files>
  <action>PAUSE the executor here. The ORCHESTRATOR session (which has Supabase MCP) executes the four-step sequence in how-to-verify against prod khslejtfbuezsmvmtsdn, autonomously per standing user policy: A1 column confirmation → A2 grounding re-run with hard STOP on drift → DELETE keyed to the two execution-time-confirmed UUIDs (never a broad predicate; holdings rows only, auth.users kept) → post-delete verification query returns 0 rows.</action>
  <verify>
    <automated>MISSING — prod destructive op via Supabase MCP (executor has no MCP); verification IS the step-4 grounding query returning 0 rows, recorded verbatim in the SUMMARY</automated>
  </verify>
  <done>Deletion row count recorded; grounding query returns 0 gate=false holders; the 2 auth.users rows still exist (select count returns 2).</done>
  <resume-signal>Orchestrator confirms: deletion row count reported, grounding query returns 0 rows, auth.users rows intact. Then proceed to Task 3.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Phase gate — GUARD-02 verbatim net + GUARD-03 zero-diff + full suite/coverage</name>
  <files></files>
  <read_first>
    - .planning/phases/63-holdings-snapshot-fallback-engine-removal/63-RESEARCH.md (§Test-Fallout Map verbatim-survivor table — the exact suites/blocks to run)
  </read_first>
  <action>Run and RECORD the phase gate (no source edits expected; any red here is a blocking finding to fix at root, never skip): (1) GUARD-02 verbatim survivors explicitly — the composer P61 block (ScenarioComposer.test.tsx:7262), compare P61-BUG-2 block + Atlas golden + F5 + the :793 MEMBER-02 union-lock survivor (scenario-compare.test.ts — :793 is protected BY NAME because its enclosing MEMBER-02 block contains the legitimately-repointed :810), T_CP8 (ScenarioComparePanel.test.tsx:332), share T_SH13/14 (share/route.test.ts:368,383), share-resolve book-only (:524), Pitfall-3 avg-ρ (~:4838) — and confirm via git log that every repoint across Plans 02-04 was an individually rationaled act (each repoint commit message names its test and why). (2) GUARD-03 — git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts prints nothing. (3) Full npm test green, npm run lint 0 errors, npx tsc --noEmit clean, npm run test:coverage above the ratchet (82/80/74/72 — deletion phases can DROP functions/branches; verify with the real numbers, do not assume improvement). (4) ENGINE-04 grep gate: grep -rln "scenario-dealias" src --include="*.ts*" | grep -v "\.test\." empty. Record all outputs in the SUMMARY. NOTE (not a commit): VERSION + package.json bump happens at the phase-wide /ship LAND step, both files in the SAME commit — flag it in the SUMMARY as the land-step reminder.</action>
  <verify>
    <automated>npm test && npm run lint && npx tsc --noEmit && [ -z "$(git diff origin/main..HEAD -- src/lib/scenario.ts src/lib/scenario-window.ts)" ] && [ -z "$(grep -rln 'scenario-dealias' src --include='*.ts*' | grep -v '\.test\.')" ]</automated>
  </verify>
  <acceptance_criteria>
    - Full suite exits 0; lint 0 errors; tsc clean.
    - npm run test:coverage passes the ratchet thresholds (record actual lines/statements/functions/branches numbers in the SUMMARY).
    - GUARD-03 diff command output empty (recorded verbatim).
    - Each verbatim-survivor file run exits 0 and git diff origin/main..HEAD over Plans 01-05 shows zero hunks inside the P61-BUG-1, P61-BUG-2, Atlas golden, F5, the scenario-compare.test.ts:793 union-lock survivor, T_CP8, T_SH13/14, share-resolve :524, and Pitfall-3 blocks (GUARD-02).
    - SUMMARY carries the VERSION + package.json land-step reminder.
  </acceptance_criteria>
  <done>Phase 63 verifiably complete: guards standing, prod clean, P61 net verbatim, frozen engine zero-diff, coverage above ratchet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Orchestrator → prod DB (Supabase MCP) | the ONLY destructive operation of the phase crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-63-08 | Tampering (wrong-row prod deletion) | GUARD-01 allocator_holdings delete | mitigate | delete keyed to the 2 execution-time-confirmed UUIDs (never a broad/email-pattern DELETE predicate); A2 grounding re-run BEFORE with a hard STOP on any drift; post-delete verification query must return 0 rows; auth.users rows kept |
| T-63-09 | Tampering (position-space reintroduction) | scenario surfaces | mitigate | ENGINE-05 dual-layer guard (source-scan + runtime id-format), Rule-9 falsified during authoring, precision-scoped to avoid permanent-red (Pitfall 3) |
| T-63-SC | Tampering | package installs | accept (N/A) | zero installs this phase |
</threat_model>

<verification>
- Guard test green and falsification-proven
- Prod: 0 gate=false holders (grounding query output recorded)
- Full suite + coverage + lint + tsc green; GUARD-03 diff empty; ENGINE-04 grep gate empty
</verification>

<success_criteria>
- ENGINE-05, GUARD-01, GUARD-02, GUARD-03 all satisfied with recorded evidence
- Phase ready for /gsd:verify-work 63; VERSION bump flagged for the /ship land step
</success_criteria>

<output>
Create `.planning/phases/63-holdings-snapshot-fallback-engine-removal/63-05-SUMMARY.md` when done
</output>
