---
phase: 65-authed-prod-canary
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []          # verification-only; code changes ONLY if a prod bug is found (feature branch)
autonomous: false           # live authed-prod session; runs in the ORCHESTRATOR/main session (needs Playwright MCP + Supabase service-role — gsd-executor has neither, Phase-63 GUARD-01 precedent)
requirements: [GUARD-04]
must_haves:
  truths:
    - "Book blend, blank mode, and mixed keys+added render live on authed prod with 0 console errors"
    - "Save→reopen round-trips the persisted membership; compare columns and share mint/resolve behave correctly on the purified surfaces"
    - "The golden P61 numbers reproduce for a book-only Atlas draft — v3→v4 upgrade computes identically, no series-space regression"
  artifacts:
    - path: ".planning/phases/65-authed-prod-canary/65-VERIFICATION.md"
      provides: "Per-item evidence, P61 format"
    - path: ".planning/phases/65-authed-prod-canary/65-01-SUMMARY.md"
      provides: "Plan summary"
---

# Phase 65 — Authed Prod Canary (GUARD-04) — PLAN 01

Goal: the purified scenario surfaces verified live on authed prod — the whole
series-space chain (book blend, blank mode, mixed keys+added, membership
round-trip, compare, share) works end-to-end with 0 console errors, and old
drafts compute identically (v3→v4).

Prod = quantalyze.xyz @ **v0.36.0.0** (PR #572, squash `f78f036b`; Vercel
Production success at exact SHA + Railway analytics SUCCESS — deploy already
verified). LIVE VERIFICATION ONLY — no code changes unless the canary finds a
prod bug (then bug-queue discipline: queue mid-item reports, finish the current
item first; every bug gets a regression test that fails without the fix, fixed
on a feature branch).

## Login recipe (the P61 recipe verbatim — no password typed)

1. Mint a magic link with the SERVICE-ROLE key against prod project
   `khslejtfbuezsmvmtsdn`:
   ```
   curl -s -X POST "https://khslejtfbuezsmvmtsdn.supabase.co/auth/v1/admin/generate_link" \
     -H "apikey: $SUPABASE_SERVICE_KEY" -H "Authorization: Bearer $SUPABASE_SERVICE_KEY" \
     -H "Content-Type: application/json" \
     -d '{"type":"magiclink","email":"qa-demo@quantalyze.app"}'
   ```
   Take `action_link` from the response (never echo the service key).
2. Playwright MCP browser → navigate to the `action_link` → the verify
   redirect establishes the session (qa-demo: role=both, is_admin).
   Landing on quantalyze.xyz OR quantalyze-rho.vercel.app is fine (same deploy).
3. Navigate to `/allocations` → the surface resolves to the **Atlas demo book**
   (a11ca111…): 4 API keys + 5 portfolio strategies, no CSV sources.
4. Fallback if the MCP browser can't carry the session: the passwordless
   setSession-cookie → curl route → grep-RSC-flight recipe (memory ⭐).

**Console discipline:** after EVERY checklist item, read the browser console
(Playwright MCP `read_console_messages`) and record the error count for that
item — per-item capture, never one sweep at the end. Expected: 0 errors on
every item.

## Golden P61 reference numbers (61-VERIFICATION.md, observed live 2026-07-03)

- Saved book-draft compare column: **Cum +0.06% / Sharpe 0.11** over its
  **40-day persisted window** (pinned to the persisted window → must match
  exactly at displayed precision).
- Live-book compare column: **155 overlapping days** (observed 2026-07-03;
  the live column recomputes at today's union, so ≥155 with growth only from
  days elapsed since — flag anything else).
- Drawer-add Helios Funding Carry + preset "Common period (all in)" → blend
  header **"Mean of 5 strategies"**.

## Checklist

### A. SC1 — purified surfaces render live (console check per item)

- [ ] A1 **book blend** — /allocations scenario tab in book mode on the Atlas
      book: blend built from the PER-KEY engine set (rows labeled `key {uuid}`,
      header "Mean of 4 strategies" at the common-period default), real
      numbers, no broken/empty book UI. Record the 4 per-key UUIDs from the
      row labels (needed for B3's leak grep). 0 console errors.
- [ ] A2 **KPI strip return-form only (new in 64)** — the scenario KPI strip
      shows exactly 4 cells @lg viewport (≥1024px: YTD TWR · Sharpe ·
      Max DD 12m · Avg |ρ|) and **NO AUM cell** anywhere in the strip; nothing
      replaces the AUM slot. 0 console errors.
- [ ] A3 **blank-slate mode** — switch the entry-mode segment to blank: honest
      blank composer — NO holdings resurrection (zero book rows appear), only
      drawer-add available, metrics honest-empty until a strategy is added.
      Switch back to book → the per-key blend returns intact. 0 console errors.
- [ ] A4 **mixed keys+added** — in book mode, drawer-add a catalog strategy
      (P61 precedent: Helios Funding Carry): it enters the coverage accounting
      (auto-excluded region if short vs the sticky window is fine); preset
      "Common period (all in)" makes it a member → header "Mean of 5
      strategies"; moving its weight input MOVES the blend numbers (P61-BUG-1
      stays fixed). 0 console errors.

### B. SC2 — persistence + share on the purified surfaces

- [ ] B1 **save mixed → reopen: membership round-trip** — save the A4 mixed
      (book+added) scenario; reopen it: per-key members intact (engine basis
      BOOK), added leg + weight round-trip. Red-team F-1 fix check: switch the
      segment to BLANK first, then reopen the saved book draft from that
      blank-mode session — it must ADOPT book basis (per-key rows render);
      then "Update portfolio" (save-over) and reopen once more — membership
      NOT wiped (per-key rows still present, never silently reduced to
      added-only). 0 console errors at each step.
- [ ] B2 **compare** — the saved mixed draft's compare column computes real
      numbers on its PERSISTED membership next to the Live book column (both
      columns compute; per-column effective windows in the `<tfoot>` Window
      row). Live-book column shows **≥155 overlapping days** (growth only from
      days elapsed since 2026-07-03 — flag anything else; this binds the
      series-space-regression golden even when C2a is skipped). 0 console
      errors.
- [ ] B3 **share mint + public resolve (mixed)** — mint a share on the mixed
      draft (must succeed); open the public link LOGGED-OUT (fresh incognito
      context or curl): the page renders the added legs computing, and carries
      the new caption VERBATIM: "computed from this scenario's catalog
      strategies only" (`data-testid="scenario-mixed-caption"`, sibling of the
      methodology line). Leak check: fetch the page source (curl, logged-out)
      and grep for each of the 4 per-key UUIDs recorded in A1 — **zero hits**.
      0 console errors on the public page.
- [ ] B4 **book-only mint 409s** — save a book-only draft (no added
      strategies); attempt to mint a share: POST returns **409
      `book_only_draft`** and the saved-list surfaces the honest message
      verbatim: "This scenario is built only on your private book sources,
      which are never shown on a public link. Add catalog strategies to share
      a computable projection." 0 console errors.

### C. SC3 — v3→v4 identity (HIGHEST-VALUE item)

- [ ] C1 **check for the pre-existing v1.5-saved Atlas book draft** — open the
      saved-scenarios list and look for any draft saved BEFORE v1.6 (saved
      under schema v3). Note: 61-VERIFICATION's addendum records the "P61
      fix-verify book-only" residue as DELETED from prod, so absence is
      expected — but check first.
- [ ] C2a **if a v3 draft survives** — open it: upgrades NON-destructively (no
      reset, no dropped scenario; a provenance/membership note is acceptable),
      and its compare column reproduces the P61 goldens: **Cum +0.06% /
      Sharpe 0.11 over its 40-day persisted window**, next to the Live-book
      column at **155 overlapping days** (≥155 per the growth note above).
      Do NOT save-over or delete this draft — it is not canary residue and
      stays in place. 0 console errors.
- [ ] C2b **fallback if the v3 draft is gone (expected)** — create a book-only
      draft fresh on the Atlas book and save it; verify its compare column
      computes REAL numbers over its persisted window (non-empty Cum/Sharpe —
      never the P61-BUG-2 honest-empty "0 overlapping days"); and verify the
      "Mean of 5 strategies" golden reproduces via drawer-add Helios Funding
      Carry + the "Common period (all in)" preset. 0 console errors.

### D. Cleanup (P61 pattern)

- [ ] D1 delete EVERY scenario the canary created (B1 mixed, B4 book-only,
      C2b fallback) from prod; spot-check that deleting the shared draft
      cascades — the B3 public link 404s after deletion (P61 precedent).
      Confirm the saved list is back to its pre-canary state (any surviving
      pre-v1.6 draft from C2a untouched). Session self-expires.

## Output contract

- `.planning/phases/65-authed-prod-canary/65-VERIFICATION.md` — per-item
  evidence in the P61 format: dateline + prod version/SHA header, per-item
  observed behavior with the literal on-screen strings/numbers, per-item
  console-error count, screenshots where they carry evidence (gantt/header/
  caption class), bugs section (root-caused, file:line where possible),
  verdict against the 3 roadmap success criteria.
- `.planning/phases/65-authed-prod-canary/65-01-SUMMARY.md` — plan summary
  (standard GSD summary shape).
- Any bug found: queued, then fixed on a feature branch with a regression
  test that fails without the fix — never inline in this phase's checklist run.
