---
phase: 50-primitive-refresh-missing-primitives
plan: 06
type: execute
wave: 3
depends_on: ["50-03", "50-04"]
files_modified:
  - src/components/strategy/StrategyTable.tsx
  - src/components/strategy/StrategyTable.test.tsx
autonomous: true
requirements: [STATE-03, STATE-04]
user_setup: []

must_haves:
  truths:
    - "The Discovery StrategyTable reshapes best-in-class: sticky header + sticky first column, low-priority columns collapse to a reachable per-row detail, a visible scroll cue appears only on overflow, and a working density control toggles row height"
    - "Collapsed columns relocate their REAL values into the reachable detail — never fabricated zeros, demo numbers, or em-dash placeholders (no-invented-data preserved)"
    - "The tab-panel/density micro-interactions cross-fade via the native View-Transition helper and fall back to instant under prefers-reduced-motion"
    - "discovery-axe stays green: zero axe violations; sort/pagination/sparklines/the role=tabpanel WatchlistTabs wiring all still work"
  artifacts:
    - path: "src/components/strategy/StrategyTable.tsx"
      provides: "Reshaped dense table: sticky thead/first-col, @container priority-collapse to <details>, scroll cue, table-scoped density control, font-metric tabular-nums preserved"
      contains: "sticky"
    - path: "src/components/strategy/StrategyTable.test.tsx"
      provides: "Extended: sticky-class presence, collapsed-detail shows the SAME real value (no 0/—), density toggles --row-h, panel wiring intact"
      contains: "density"
  key_links:
    - from: "src/components/strategy/StrategyTable.tsx (density control)"
      to: "src/app/globals.css (--row-h / --density-pad via table-scoped data-density)"
      via: "data-density attribute on the table root (NOT body) reads the scoped rule from Plan 04"
      pattern: "data-density"
    - from: "src/components/strategy/StrategyTable.tsx (collapsed columns)"
      to: "the same real cell value relocated into <details>"
      via: "formatPercent/formatCurrency honest-null rendering — never fabricated"
      pattern: "details"
---

<objective>
Wave 2 of Phase 50: reshape the Discovery `StrategyTable` (table view mode) best-in-class —
the template the surface phases 52/53 will replicate. Four behaviors: (1) sticky header +
sticky first column with a strict z-index stack and opaque backgrounds; (2) priority collapse
of low-priority columns via Tailwind v4 `@container`, relocating their REAL values into a
reachable per-row `<details>` (honest degradation — no fabricated zeros); (3) a visible scroll
cue shown only on horizontal overflow; (4) a working, table-scoped density control. Wire the
tab-panel + density micro-interactions through the native View-Transition helper from Plan 04
(reduced-motion-safe). Builds ON the existing `ResponsiveTable` wrapper (keeps its unique-
aria-label landmark) and the new `Table` base where it fits.

Purpose: deliver STATE-03 (dense reshape) + STATE-04 (motion) on the lowest-blast-radius public
surface, axe-gated by the existing `e2e/discovery-axe.spec.ts`. Output: reshaped StrategyTable +
extended tests; discovery-axe stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md

<interfaces>
<!-- The reshape target + the scaffolding it consumes (Plans 03 & 04). Greenfield CSS comes from RESEARCH, not a codebase analog. -->

StrategyTable — src/components/strategy/StrategyTable.tsx ("use client", already)
  COLUMNS def line 35; viewMode "table" branch from line 398; wrapper line 399
  <div className="overflow-x-auto rounded-xl border border-border bg-surface">;
  showStarColumn = userId !== undefined (line 337); leading star col + Strategy name = first col;
  numeric cells px-4 py-3 text-right font-metric (lines 470-488); row hover hover:bg-page/50 (line 432);
  sort-on-header-click; pagination (lines 535-557); honest-null via formatPercent/formatCurrency (lines 479-490);
  external panel id={panelId} role="tabpanel" (lines 385-394) — wiring with WatchlistTabs MUST stay intact.

Greenfield CSS (NO in-repo analog — use RESEARCH patterns 3 & 4):
  Pattern 3 sticky: thead th -> sticky top-0 z-20 bg-surface; first col cells -> sticky left-0 z-10 bg-surface border-r border-border; corner th -> z-30. Backgrounds OPAQUE.
  Pattern 4 collapse: wrap in @container overflow-x-auto; low-priority <td className="@max-3xl:hidden">; relocate collapsed REAL values into per-row <details>.
  Pitfall 5: the sticky first column must NOT inherit the translucent hover:bg-page/50 (keep it solid bg-surface) or scrolled cells bleed through.

Table-scoped density (Plan 04): a data-density="tight" rule scoped to the table root sets --row-h/--density-pad WITHOUT the global font-size; do NOT use body[data-density].
View-Transition helper (Plan 04): withViewTransition(update) — wrap the density/panel setState; instant fallback under reduced-motion + no-support.
Priority order (UI-SPEC): Strategy > Return% > CAGR > Sharpe > Max DD always visible; Volatility, 6 Month, AUM, Return spark, Underwater spark collapse first. Actions column reachable at every width.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sticky header + first column + visible scroll cue (greenfield CSS)</name>
  <read_first>
    - src/components/strategy/StrategyTable.tsx (the viewMode="table" branch from line 398; wrapper line 399; first-col star+name; hover line 432)
    - src/components/ResponsiveTable.tsx (the role=region aria-label landmark to keep — do NOT regress its unique name)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (Pattern 3 + Pitfall 5)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md (§Dense Reshape behavior 1 + 3)
  </read_first>
  <files>src/components/strategy/StrategyTable.tsx</files>
  <action>
    Apply Pattern 3 sticky CSS: thead cells -> sticky top-0 z-20 bg-surface; the first column
    cells (the leading star col when showStarColumn + the Strategy-name col) -> sticky left-0 z-10
    bg-surface border-r border-border; the corner header cell -> z-30. Backgrounds MUST be opaque
    bg-surface. Pitfall 5: do NOT let the row hover hover:bg-page/50 apply to the sticky first
    column — keep it solid bg-surface (override on that cell) so scrolled cells do not bleed
    through. Add a visible scroll cue: a right-edge gradient fade + "Scroll for more columns ->"
    in text-caption text-text-muted, shown ONLY when scrollWidth > clientWidth (measure on the
    scroll container; component is already "use client"); mark the cue aria-hidden (it pairs with,
    never replaces, the ResponsiveTable aria-label — avoid double-announcing). Keep the
    ResponsiveTable wrapper + its unique aria-label. Refresh cell type text-sm -> text-body;
    numeric cells KEEP font-metric tabular-nums (load-bearing for alignment).
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - thead cells carry `sticky top-0` + an explicit z-index + opaque `bg-surface`; the first column carries `sticky left-0` + opaque bg + `border-r`; the corner cell out-ranks both (highest z).
    - The sticky first column does NOT receive the translucent `hover:bg-page/50` (Pitfall 5).
    - A scroll cue with text "Scroll for more columns ->" renders aria-hidden and is gated on `scrollWidth > clientWidth`.
    - The ResponsiveTable region aria-label is unchanged; numeric cells keep `font-metric tabular-nums`.
    - StrategyTable.test.tsx passes.
  </acceptance_criteria>
  <done>The table has a sticky header + sticky first column (opaque, correct z-stack, no hover bleed) and an overflow-gated scroll cue.</done>
</task>

<task type="auto">
  <name>Task 2: Priority collapse to reachable detail (honest values) + table-scoped density control</name>
  <read_first>
    - src/components/strategy/StrategyTable.tsx (COLUMNS line 35; the 3 trailing cols Return spark/Underwater/Actions; honest-null formatPercent/formatCurrency lines 479-490)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (Pattern 4 @container + the honest-degradation note; Open Question 2 table-scoped density)
    - src/app/globals.css (the Plan-04 table-scoped data-density rule + the existing --row-h/--density-pad tokens)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md (§Dense Reshape behavior 2 + 4; STATE-02 honest-absence)
  </read_first>
  <files>src/components/strategy/StrategyTable.tsx</files>
  <action>
    Wrap the table region in @container (Tailwind v4 core — no plugin). Keep Strategy > Return% >
    CAGR > Sharpe > Max DD always visible; mark Volatility, 6 Month, AUM, and the two sparkline
    columns to collapse first (e.g. @max-3xl:hidden at narrow container widths). Relocate the
    collapsed columns' REAL values into a reachable per-row <details> (keyboard- and SR-reachable
    "More"/"Details" affordance). HONEST DEGRADATION (no-invented-data, STATE-02): the <details>
    renders the SAME value the visible cell would (via the existing formatPercent/formatCurrency
    honest-null path) — NEVER a fabricated 0, demo number, or em-dash placeholder; a genuinely
    null value renders the existing honest-null treatment. The Actions column (Simulate Impact)
    stays reachable at every width. Add a density control: a segmented "Comfortable"/"Compact"
    control with accessible name "Table density" that sets a data-density attribute on the TABLE
    ROOT (NOT body — reads the Plan-04 table-scoped rule -> --row-h 44/36, --density-pad 16/12);
    the active option uses accent text. Wrap the density setState in withViewTransition (Plan 04)
    so the row-height change cross-fades and falls back to instant under reduced-motion. Compact
    must NOT clip text (density changes row height, not legibility).
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The table region is an @container; low-priority columns carry an @container max-width hidden variant; the always-visible priority set (Strategy/Return%/CAGR/Sharpe/Max DD) is never collapsed; Actions stays reachable.
    - The per-row <details> renders the SAME real value as the visible cell for each collapsed column; a test asserts NO `0`/`—`/demo value is injected where the source value is null (honest-null path used).
    - The density control has accessible name "Table density", sets `data-density` on the TABLE ROOT (not body), and toggling it changes the effective --row-h; the global allocator-dashboard density is unaffected.
    - The density toggle is wrapped in `withViewTransition` (Plan 04 helper).
    - StrategyTable.test.tsx passes (extended assertions GREEN).
  </acceptance_criteria>
  <done>Low-priority columns collapse to an honest reachable detail and a table-scoped density control toggles row height with a reduced-motion-safe transition.</done>
</task>

<task type="auto">
  <name>Task 3: Extend StrategyTable.test.tsx + confirm discovery-axe stays green</name>
  <read_first>
    - src/components/strategy/StrategyTable.test.tsx (existing sort/pagination/tabpanel assertions to PRESERVE)
    - e2e/discovery-axe.spec.ts (the axe gate — already in the seeded MA-8 CI list; SLUG crypto-sma)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (§Validation — STATE-03 test map)
  </read_first>
  <files>src/components/strategy/StrategyTable.test.tsx</files>
  <action>
    Extend StrategyTable.test.tsx with: (a) sticky-class presence on thead + first-col cells;
    (b) the honest-value assertion — for a row whose collapsed column source value is null, the
    <details> shows the existing honest-null rendering and NOT a fabricated 0/—/demo number;
    (c) density toggle changes the table-root data-density (and the effective --row-h path);
    (d) the existing role=tabpanel / aria-labelledby wiring with WatchlistTabs still resolves
    (do not regress the Plan-05 contract). PRESERVE all existing sort-on-header-click, pagination,
    sparkline, and EmptyWatchlist assertions. Do NOT modify e2e/discovery-axe.spec.ts (it already
    runs seeded in CI and covers /discovery); just keep it green — run it locally if seed env is
    available, otherwise rely on CI.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/StrategyTable.test.tsx && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - StrategyTable.test.tsx asserts: sticky classes present; collapsed-detail honest value (no fabricated 0/—); density toggle flips the table-root data-density; tabpanel wiring intact.
    - All pre-existing StrategyTable.test.tsx assertions still pass (sort/pagination/sparkline/empty).
    - `npm run lint` is clean (no new design-lint violations — no raw hex, no rem-less clamp).
    - e2e/discovery-axe.spec.ts is unmodified and (in CI's seeded run) reports zero violations.
  </acceptance_criteria>
  <done>The reshape is test-pinned (sticky/honest-collapse/density/panel-wiring) and the discovery axe gate stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy metric data -> table cells / collapsed detail | Per-strategy metric values render into visible cells AND the relocated <details>; a fabrication here would violate the locked no-invented-data invariant (a data-integrity threat, not just aesthetic). |
| inline density var -> CSP | The density control sets inline `style` using `var(--row-h)`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-09 | Tampering / Information (data integrity) | collapsed-column <details> values | mitigate | The collapsed detail renders the SAME value via the existing formatPercent/formatCurrency honest-null path; the plan forbids fabricated 0/—/demo values and a unit test asserts a null source stays honest-null (no-invented-data / STATE-02). Acceptance-criteria gated. |
| T-50-04 | Tampering (XSS) | table cell + details content | mitigate | All values render as React children (escaped); no `dangerouslySetInnerHTML`. |
| T-50-05 | DoS (self) / CSP | inline `style={{height:"var(--row-h)"}}` | accept | `style-src 'unsafe-inline'` already present (RESEARCH V5); no CSP change needed. |
| V2/V3/V4/V6 ASVS | — | n/a | n/a (not applicable) | Public discovery surface; presentation/data-rendering only, no auth/session/access-control/crypto change. Marked non-applicable explicitly. |
</threat_model>

<verification>
- `npx vitest run src/components/strategy/StrategyTable.test.tsx` GREEN (sticky/honest-collapse/density/panel-wiring).
- `npm run lint` clean.
- e2e/discovery-axe.spec.ts unmodified; CI seeded run reports zero violations.
- Sort/pagination/sparkline/EmptyWatchlist/tabpanel behaviors preserved.
</verification>

<success_criteria>
- StrategyTable reshapes best-in-class: sticky header + first column, @container priority-collapse to an honest reachable detail, overflow-gated scroll cue, table-scoped density control (STATE-03).
- Tab-panel + density micro-interactions cross-fade via the native helper, reduced-motion-safe (STATE-04).
- discovery-axe green; no-invented-data preserved; WatchlistTabs panel wiring intact.
</success_criteria>

<output>
Create `.planning/phases/50-primitive-refresh-missing-primitives/50-06-SUMMARY.md` when done.
</output>
