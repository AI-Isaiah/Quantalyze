---
phase: 50-primitive-refresh-missing-primitives
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/ui/Tabs.test.tsx
  - src/components/ui/Table.test.tsx
  - src/components/ui/Field.test.tsx
  - src/components/ui/Button.test.tsx
  - src/components/ui/Modal.test.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-04]
user_setup: []

must_haves:
  truths:
    - "@radix-ui/react-tabs@1.1.15 is installed, pinned exact, with no postinstall script, after an explicit human legitimacy approval"
    - "RED test contracts exist for the three new primitives (Tabs/Table/Field) describing the a11y/role/aria behavior they must satisfy"
    - "Lock tests exist for Button + Modal pinning the focus: -> focus-visible: edit so the refresh cannot silently regress the keyboard ring"
  artifacts:
    - path: "package.json"
      provides: "@radix-ui/react-tabs exact-pinned dependency"
      contains: "\"@radix-ui/react-tabs\": \"1.1.15\""
    - path: "src/components/ui/Tabs.test.tsx"
      provides: "RED contract: role=tab/tabpanel, single aria-selected, arrow/Home/End keyboard"
      min_lines: 30
    - path: "src/components/ui/Table.test.tsx"
      provides: "RED contract: th scope=col, region landmark name, sticky-class presence"
      min_lines: 20
    - path: "src/components/ui/Field.test.tsx"
      provides: "RED contract: htmlFor/id, aria-describedby joins hint+error, aria-invalid"
      min_lines: 20
    - path: "src/components/ui/Button.test.tsx"
      provides: "Lock test: text-body/text-caption tier classes + focus-visible ring, no bare focus:ring"
      min_lines: 15
    - path: "src/components/ui/Modal.test.tsx"
      provides: "Lock test: title text-h3, close button focus-visible ring"
      min_lines: 15
  key_links:
    - from: "package.json"
      to: "node_modules/@radix-ui/react-tabs"
      via: "npm install (gated by human legitimacy checkpoint)"
      pattern: "@radix-ui/react-tabs"
---

<objective>
Wave 0 of Phase 50: stand up the dependency + the executable test contracts that every
later wave is gated against. Install the one external package (`@radix-ui/react-tabs`)
behind a blocking human legitimacy checkpoint (slopcheck was unavailable in research, so
it is `[ASSUMED]`), and author the failing (RED) test contracts for the three genuinely-new
primitives (Tabs/Table/Field) plus minimal lock tests for the two core primitives whose
refresh has behavior-adjacent edits (Button + Modal focus-visible).

Purpose: The contracts precede the implementation (GREEN is Wave 1's job for the new
primitives; the core-primitive lock tests stay green through the CSS refresh). This protects
the BP-03 coverage ratchet (82/80/74/72, a blocking CI gate) — the six core primitives have
NO existing tests today and the three new primitives add substantial new lines/branches.
Output: a pinned dependency + 5 colocated test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md

<interfaces>
<!-- Test-file authoring conventions the executor needs. Extracted from the codebase. -->

cn() helper — src/lib/utils.ts:72
  export function cn(...classes: (string | false | undefined | null)[]): string

Existing colocated test analog — src/components/ui/CardShell.test.tsx
  RTL render + screen.getByRole/getByText, jsdom env, no extra setup.

jsdom dialog stub (Modal.test.tsx needs it) — src/components/admin/AdminTabs.test.tsx:33-46
  HTMLDialogElement.prototype.showModal / close are not implemented in jsdom;
  stub them before rendering a component that calls showModal().

Phase-49 fluid tier utilities (already live) — src/app/globals.css:139-143
  text-h3 / text-body / text-small / text-caption / text-micro
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy-verify @radix-ui/react-tabs before install (T-50-SC)</name>
  <read_first>
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (§Package Legitimacy Audit — slopcheck UNAVAILABLE this session, package tagged [ASSUMED])
  </read_first>
  <files>(none — human verification gate; no files are written in this task)</files>
  <action>
    PAUSE for a blocking human legitimacy verification of @radix-ui/react-tabs BEFORE the Task 2
    install. This is the T-50-SC supply-chain mitigation: slopcheck was unavailable in research,
    so the package is [ASSUMED] and this gate is NEVER auto-approvable (workflow.auto_advance is
    ignored for legitimacy checkpoints). Present the npm-registry signals for human sign-off and
    do NOT run `npm install` until the human approves.
  </action>
  <what-built>
    Nothing automated yet — this is the supply-chain gate that MUST clear before the
    `npm install` in Task 2. Research could not run slopcheck, so `@radix-ui/react-tabs`
    is `[ASSUMED]` and a blocking human verification is mandatory.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@radix-ui/react-tabs in a browser.
    2. Confirm: version 1.1.15 exists and is recent; weekly downloads are in the tens of
       millions; the repository link is github.com/radix-ui/primitives (the official Radix
       monorepo); the package has no install/postinstall script.
    3. Confirm the package is NOT on the project banned-packages list in
       ~/.claude/CLAUDE.md (it is not — only react-native-international-phone-number and
       react-native-country-select are banned; @radix-ui is permitted).
  </how-to-verify>
  <verify>Human confirms the registry signals and types the resume signal; until then Task 2 (the install) is blocked.</verify>
  <acceptance_criteria>
    - Human has visited npmjs.com/package/@radix-ui/react-tabs and confirmed: official
      radix-ui/primitives repo, 1.1.15 published, multi-million weekly downloads, no
      postinstall script.
    - Human types "approved" (or the equivalent resume signal). Without this the install
      task does NOT run.
  </acceptance_criteria>
  <done>Human has approved the pinned install of @radix-ui/react-tabs@1.1.15, or aborted with a concern.</done>
  <resume-signal>Type "approved" to authorize the pinned install, or describe a concern to abort.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install @radix-ui/react-tabs@1.1.15 exact-pinned</name>
  <read_first>
    - package.json (the dependencies block — confirm no @radix-ui entry exists yet)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (§Installation — exact version 1.1.15)
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>
    After the Task 1 checkpoint clears, run `npm install --save-exact @radix-ui/react-tabs@1.1.15`.
    The version MUST be pinned exact ("1.1.15", no caret) per the Package Legitimacy Audit —
    edit package.json to "1.1.15" if npm writes a caret. Do NOT enable any next.config flag
    here (this plan adds no config change; View Transitions in this milestone use the native
    document.startViewTransition path per the phase decision, so experimental.viewTransition
    is intentionally NOT added). Run `npm view @radix-ui/react-tabs@1.1.15 scripts.postinstall`
    and confirm it is empty (no postinstall). Do not run `npm audit fix` (out of scope).
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.dependencies['@radix-ui/react-tabs']!=='1.1.15'){console.error('not pinned exact:',p.dependencies['@radix-ui/react-tabs']);process.exit(1)} console.log('pinned ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies has `"@radix-ui/react-tabs": "1.1.15"` (exact, no caret).
    - `package-lock.json` resolves @radix-ui/react-tabs to 1.1.15.
    - `npm view @radix-ui/react-tabs@1.1.15 scripts.postinstall` returns empty.
    - `node_modules/@radix-ui/react-tabs/package.json` exists.
  </acceptance_criteria>
  <done>The exact-pinned Radix Tabs dependency is installed with no postinstall script; Wave 1 can import it.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Author RED contracts (Tabs/Table/Field) + lock tests (Button/Modal)</name>
  <read_first>
    - src/components/ui/CardShell.test.tsx (RTL render + getByRole pattern — the analog for every new test)
    - src/components/strategy/WatchlistTabs.test.tsx (role/aria/tabIndex/keyDown assertions — borrow for Tabs.test.tsx)
    - src/components/admin/AdminTabs.test.tsx (lines 33-46: HTMLDialogElement showModal/close jsdom stub — borrow for Modal.test.tsx)
    - src/components/ui/Button.tsx (current sizeStyles: sm=text-xs, md=text-sm, lg=text-base; base has focus:ring-2 focus:ring-accent/50)
    - src/components/ui/Modal.tsx (title h2 text-lg font-semibold; close button has NO focus ring today)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (§Validation Architecture — Phase Requirements -> Test Map; §New-Primitive Test Patterns)
  </read_first>
  <files>src/components/ui/Tabs.test.tsx, src/components/ui/Table.test.tsx, src/components/ui/Field.test.tsx, src/components/ui/Button.test.tsx, src/components/ui/Modal.test.tsx</files>
  <behavior>
    Tabs.test.tsx (RED — Tabs.tsx does not exist yet, so this fails on import until Wave 1):
      - Import { Tabs, TabsList, TabsTrigger, TabsContent } from "@/components/ui/Tabs".
      - Render two triggers + two panels; assert triggers expose role="tab", panels role="tabpanel".
      - Assert exactly one trigger has aria-selected="true" at a time; clicking the second flips it.
      - Assert roving tabindex: active trigger tabIndex=0, inactive tabIndex=-1.
      - Assert keyboard: fireEvent.keyDown(activeTrigger, { key: "ArrowRight" }) moves selection
        to the next trigger (activationMode automatic); Home/End jump to first/last.
    Table.test.tsx (RED — Table.tsx does not exist yet):
      - Import { Table, ... parts } from "@/components/ui/Table".
      - Assert header cells render with scope="col" (getByRole("columnheader")).
      - Assert the table is reachable by an accessible name (caption or aria-label) — getByRole("table", { name }).
    Field.test.tsx (RED — Field.tsx does not exist yet):
      - Import { Field } from "@/components/ui/Field".
      - Render Field with label + hint + error wrapping an <input>.
      - Assert getByLabelText(label) resolves the control (htmlFor/id wired).
      - Assert control aria-describedby contains BOTH the hint id and the error id (space-joined).
      - Assert control aria-invalid="true" when error is set; absent/undefined when no error.
    Button.test.tsx (LOCK — Button.tsx exists; these go GREEN only after the Wave-1 refresh):
      - Render <Button size="md">; assert className matches /text-body/ and does NOT match /\btext-sm\b/.
      - Render <Button size="sm">; assert className matches /text-caption/.
      - Assert base className matches /focus-visible:ring/ and does NOT match /(?<!-visible)\bfocus:ring/ (no bare focus:ring).
      - Render every variant x size; assert no throw (public prop API unchanged).
    Modal.test.tsx (LOCK — Modal.tsx exists; goes GREEN after the Wave-1 refresh):
      - Stub HTMLDialogElement.prototype.showModal/close (jsdom gap) as in AdminTabs.test.tsx:33-46.
      - Render <Modal open title="X">; assert the title element className matches /text-h3/ and not /text-lg/.
      - Assert the close button (aria-label="Close") className matches /focus-visible:ring/.
  </behavior>
  <action>
    Write the 5 colocated test files per the behavior block, mirroring CardShell.test.tsx's
    RTL+jsdom style. The three new-primitive tests (Tabs/Table/Field) are EXPECTED to fail RED
    now (module-not-found on import) — that is the contract; Wave-1 Plan 03 makes them GREEN.
    The two core lock tests (Button/Modal) currently fail on the tier/focus-visible asserts
    (the source still has text-sm / focus:ring / text-lg) — Wave-1 Plan 02's refresh flips them
    GREEN. Do NOT modify any source primitive in this plan; tests only. Use specific RTL queries
    (getByRole/getByLabelText), never snapshot tests. Keep each test focused so coverage maps
    cleanly to the new lines the primitives will add.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/Tabs.test.tsx src/components/ui/Table.test.tsx src/components/ui/Field.test.tsx 2>&1 | grep -qE "Cannot find module|Failed to resolve import|No test|fail" && echo "RED-as-expected" || (echo "expected RED contracts to fail pre-implementation"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist and are valid TS/TSX (no syntax errors — `npx tsc --noEmit` does not error on these files for reasons other than the not-yet-existing Tabs/Table/Field modules).
    - Tabs/Table/Field tests fail RED with a module-not-found / unresolved-import error (the contract precedes the implementation).
    - Button.test.tsx and Modal.test.tsx assert the post-refresh tier classes (text-body/text-caption/text-h3) and focus-visible ring; they are RED now and become the GREEN gate for Wave-1 Plan 02.
    - No source file under src/components/ui/*.tsx (non-test) is modified by this plan.
  </acceptance_criteria>
  <done>The executable contracts exist: 3 new-primitive RED specs + 2 core lock specs, ready to gate Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> developer machine / CI | A new third-party package (`@radix-ui/react-tabs`) crosses into the build via `npm install`; its install scripts would run with the developer's privileges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-SC | Tampering | `npm install @radix-ui/react-tabs` (supply chain) | mitigate | slopcheck was unavailable in research -> package is `[ASSUMED]`. Task 1 is a BLOCKING human legitimacy checkpoint (never auto-approvable) verifying official radix-ui/primitives repo + 52M weekly downloads + no postinstall, BEFORE the Task 2 install. Version pinned EXACT (1.1.15) so a future malicious release cannot float in via caret. |
| T-50-01 | Tampering | `package-lock.json` integrity | accept | npm records a subresource-integrity hash per resolved package; exact-pin + committed lockfile means CI installs the verified artifact. No additional control needed beyond the pin. |
| V2 Authentication / V3 Session / V4 Access Control / V6 Cryptography | — | n/a | n/a (not applicable) | This plan adds a dev dependency + test files only; no auth/session/access-control/crypto surface is touched. Explicitly marked non-applicable rather than omitted. |
</threat_model>

<verification>
- `npm view @radix-ui/react-tabs@1.1.15 scripts.postinstall` is empty (no postinstall).
- package.json pins `"@radix-ui/react-tabs": "1.1.15"` exact.
- `npx vitest run src/components/ui/{Tabs,Table,Field}.test.tsx` fails RED (module-not-found) — contract precedes implementation.
- `npx vitest run src/components/ui/{Button,Modal}.test.tsx` fails RED on the tier/focus-visible asserts (source not yet refreshed) — these become Wave-1 Plan 02's GREEN gate.
</verification>

<success_criteria>
- Radix Tabs installed, exact-pinned, no postinstall, after a blocking human legitimacy approval.
- 5 colocated test files exist: Tabs/Table/Field (RED contracts) + Button/Modal (lock tests).
- No production primitive source modified in this plan.
</success_criteria>

<output>
Create `.planning/phases/50-primitive-refresh-missing-primitives/50-01-SUMMARY.md` when done.
</output>
