---
phase: 50-primitive-refresh-missing-primitives
plan: 04
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/app/globals.css
  - src/lib/view-transition.ts
  - src/lib/view-transition.test.ts
autonomous: true
requirements: [STATE-04]
user_setup: []

must_haves:
  truths:
    - "A reusable client helper performs a View Transition via the native document.startViewTransition API and falls back to an instant state update when the API is unavailable or the user prefers reduced motion"
    - "The globals.css reduced-motion blocks are EXTENDED (not bypassed) so every ::view-transition animation collapses to an instant swap under prefers-reduced-motion: reduce"
    - "A table-scoped density rule reads the existing --row-h / --density-pad tokens so a table can flip density without changing the global allocator-dashboard density"
    - "No motion library is introduced and no experimental.viewTransition next.config flag is added (native startViewTransition path per the phase decision)"
  artifacts:
    - path: "src/lib/view-transition.ts"
      provides: "withViewTransition(update) helper: native startViewTransition + reduced-motion/no-support instant fallback"
      min_lines: 20
    - path: "src/app/globals.css"
      provides: "Extended reduced-motion block zeroing ::view-transition-old/new + a table-scoped data-density rule"
      contains: "view-transition"
  key_links:
    - from: "src/lib/view-transition.ts"
      to: "document.startViewTransition"
      via: "feature-detect + reduced-motion check, else run update() directly"
      pattern: "startViewTransition"
    - from: "src/app/globals.css (reduced-motion block)"
      to: "::view-transition-old / ::view-transition-new"
      via: "animation-duration: 0s !important under prefers-reduced-motion: reduce"
      pattern: "view-transition-(old|new)"
---

<objective>
Wave 1 of Phase 50: lay the motion + density CSS scaffolding the StrategyTable reshape and
the Tabs consolidation (both Wave 2) consume. Provide a small reusable `withViewTransition`
client helper that wraps the NATIVE `document.startViewTransition` API (the phase decision:
native API in a client island, NOT React `<ViewTransition>` + the experimental next.config
flag — lower blast radius for two opt-in micro-interactions), degrading to an instant state
update when the API is unsupported or the user prefers reduced motion. Extend the existing
`globals.css` `@media (prefers-reduced-motion: reduce)` blocks so every `::view-transition`
animation collapses to an instant swap. Add a TABLE-SCOPED `data-density` rule that reuses the
existing `--row-h` / `--density-pad` tokens (so a public table's density control does not flip
the global allocator-dashboard density — RESEARCH Q2).

Purpose: restrained, reduced-motion-safe micro-interactions via native CSS + the View
Transitions API only (STATE-04), with no motion library. Output: 1 helper + 1 test + globals.css
extensions. This plan is intentionally independent (only globals.css + a new lib file) so it
runs parallel to the core refresh (Plan 02) and the new primitives (Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md

<interfaces>
<!-- Existing globals.css blocks this plan EXTENDS (never bypasses). Verified line numbers. -->

Reduced-motion blocks (extend these) — src/app/globals.css:152-157, :353, :370
  @media (prefers-reduced-motion: reduce) { .animate-pulse { animation: none; opacity: 1; } }
  (+ two more reduced-motion blocks at L353 and L370)

Density tokens (reuse, do NOT redefine) — src/app/globals.css:171-196
  :root { --row-h: 44px; --density-pad: 16px; }
  body[data-density="tight"] { --row-h: 36px; --density-pad: 12px; font-size: 13px; }   <- NOTE the global font-size: 13px must NOT leak into a public table
  body[data-density="loose"] { --row-h: 52px; --density-pad: 20px; }
  body[data-density="tight"] [data-allocator-dashboard] { ... }   <- the existing global consumer

Phase decision (recorded by the planner): View Transitions use the NATIVE
  document.startViewTransition() in a client island. next.config.ts is NOT modified
  (no experimental.viewTransition). Reduced-motion + no-API-support both fall back to instant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: withViewTransition helper + reduced-motion / no-support fallback</name>
  <read_first>
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (Pattern 5 + Pitfall 3 — native vs React API; the phase chose native)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md (§Motion — durations/easings; VT scoped to tab-panel + density toggle ONLY)
    - src/lib/utils.ts (existing lib conventions — how helpers are exported)
  </read_first>
  <files>src/lib/view-transition.ts, src/lib/view-transition.test.ts</files>
  <behavior>
    - withViewTransition(update): when document.startViewTransition exists AND the user does NOT prefer reduced motion, call document.startViewTransition(update); otherwise call update() synchronously (instant swap). update is a () => void that performs the state change.
    - Returns gracefully (no throw) when document is undefined (SSR) — run update() directly.
    - Test: mock document.startViewTransition present + matchMedia(reduced-motion)=false -> startViewTransition called with the update.
    - Test: matchMedia(reduced-motion)=true -> startViewTransition NOT called, update() called directly.
    - Test: startViewTransition undefined -> update() called directly, no throw.
  </behavior>
  <action>
    Write a tiny client-safe helper `withViewTransition(update: () => void): void`. Feature-detect
    `typeof document !== "undefined" && typeof document.startViewTransition === "function"` AND
    check `!window.matchMedia("(prefers-reduced-motion: reduce)").matches`; if both, call
    document.startViewTransition(update); else call update() directly. Do NOT add any next.config
    flag and do NOT import React's ViewTransition (native path per the phase decision). Do NOT add
    framer-motion / motion / @headlessui. Keep it framework-agnostic so both the Tabs panel swap
    and the StrategyTable density toggle (Wave 2) can wrap their setState in it.
  </action>
  <verify>
    <automated>npx vitest run src/lib/view-transition.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - view-transition.test.ts passes all three branches (VT-on, reduced-motion-fallback, no-support-fallback).
    - `grep -c 'startViewTransition' src/lib/view-transition.ts` >= 1.
    - No import of `react`'s ViewTransition, `framer-motion`, `motion`, or `@headlessui` in the file.
    - `grep -c 'experimental' next.config.ts` is unchanged (this plan does NOT touch next.config.ts).
  </acceptance_criteria>
  <done>A reusable native-View-Transition helper exists with reduced-motion + no-support instant fallback, no motion library, no config flag.</done>
</task>

<task type="auto">
  <name>Task 2: Extend globals.css — reduced-motion VT kill + table-scoped density rule</name>
  <read_first>
    - src/app/globals.css (lines 152-157 reduced-motion; lines 171-196 density tokens + allocator-dashboard consumer)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md (globals.css section — EXTEND not bypass; reuse --row-h/--density-pad; RESEARCH Q2 table-scoped)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (Pattern 5 reduced-motion CSS block from the Next docs)
  </read_first>
  <files>src/app/globals.css</files>
  <action>
    EXTEND the existing reduced-motion handling (do NOT add a competing standalone block that
    bypasses it): inside a prefers-reduced-motion: reduce block, add a rule
    `::view-transition-old(*), ::view-transition-new(*) { animation-duration: 0s !important; }`
    so every View Transition collapses to an instant swap under reduced motion (pairs with the
    helper's matchMedia guard — belt and suspenders). Add a TABLE-SCOPED density rule: a selector
    like `[data-density="tight"]` scoped to a table root (e.g. a data attribute the StrategyTable
    root will carry in Wave 2) that sets --row-h: 36px; --density-pad: 12px WITHOUT the global
    `font-size: 13px` and WITHOUT touching body[data-density] (so flipping a public table's
    density does NOT change the allocator-dashboard density — RESEARCH Q2). Reuse the EXISTING
    --row-h / --density-pad token names; do not invent new tokens. Keep the diff minimal and
    additive; do not modify the existing body[data-density] or [data-allocator-dashboard] rules.
  </action>
  <verify>
    <automated>grep -qE 'view-transition-(old|new)' src/app/globals.css && grep -q 'prefers-reduced-motion' src/app/globals.css && npx vitest run src/components/ui/ && echo "globals extended + ui suite green"</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains a `::view-transition-old`/`::view-transition-new` rule with `animation-duration: 0s !important` INSIDE a `@media (prefers-reduced-motion: reduce)` block.
    - A table-scoped density rule sets `--row-h` and `--density-pad` to the tight steps WITHOUT a `font-size` declaration and WITHOUT modifying `body[data-density]`.
    - The pre-existing `body[data-density="tight"]`, `[data-allocator-dashboard]`, and `.animate-pulse` reduced-motion rules are unchanged (`git diff` shows only additions in these regions).
    - The src/components/ui/ test suite still passes (no CSS-driven regression).
  </acceptance_criteria>
  <done>globals.css extends reduced-motion to View Transitions and provides a table-scoped density rule reusing the existing tokens; the global allocator density behavior is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inline style vars -> CSP | The density control sets inline `style={{ height: "var(--row-h)" }}` in Wave 2; the helper + CSS here enable that path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-05 | DoS (self) / CSP breakage | inline density vars enabled by this CSS | accept | `style-src 'self' 'unsafe-inline'` is ALREADY in next.config.ts headers (RESEARCH Security V5); inline `style={{height:"var(--row-h)"}}` is covered. No CSP change is made or needed. |
| T-50-06 | Tampering (motion supply chain) | motion library | mitigate | The plan forbids framer-motion / motion / @headlessui (acceptance-criteria gated) and adds no next.config experimental flag; motion is native browser API + CSS only, so no new third-party motion runtime enters the bundle. |
| V2/V3/V4/V5/V6 ASVS | — | n/a | n/a (not applicable) | CSS + a tiny client helper; no auth/session/access-control/input/crypto surface. Marked non-applicable explicitly. |
</threat_model>

<verification>
- `npx vitest run src/lib/view-transition.test.ts` GREEN (all three fallback branches).
- globals.css has the reduced-motion `::view-transition-*` zeroing rule and a table-scoped density rule reusing `--row-h`/`--density-pad`.
- next.config.ts is NOT modified (no experimental.viewTransition); no motion library in package.json.
- The existing body[data-density] / allocator-dashboard / animate-pulse rules are unchanged.
</verification>

<success_criteria>
- Native View-Transition helper with reduced-motion + no-support instant fallback exists; no motion library, no config flag.
- globals.css reduced-motion blocks extended to View Transitions; table-scoped density rule reuses existing tokens.
- The allocator-dashboard global density behavior is untouched.
</success_criteria>

<output>
Create `.planning/phases/50-primitive-refresh-missing-primitives/50-04-SUMMARY.md` when done.
</output>
