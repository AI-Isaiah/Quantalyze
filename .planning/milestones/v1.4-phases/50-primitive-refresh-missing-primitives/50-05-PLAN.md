---
phase: 50-primitive-refresh-missing-primitives
plan: 05
type: execute
wave: 3
depends_on: ["50-03"]
files_modified:
  - src/components/admin/AdminTabs.tsx
  - src/components/admin/AdminTabs.test.tsx
  - src/components/auth/ProfileTabs.tsx
  - src/components/auth/ProfileTabs.test.tsx
  - src/components/strategy/WatchlistTabs.tsx
  - src/components/strategy/WatchlistTabs.test.tsx
autonomous: true
requirements: [UI-02, UI-03]
user_setup: []

must_haves:
  truths:
    - "The 3 hand-rolled tab implementations (AdminTabs / ProfileTabs / WatchlistTabs) render through the ONE canonical Tabs primitive — 3 impls collapse to 1, the highest-value strangler dedup"
    - "Each consumer's existing behavior is byte-faithful: AdminTabs keeps its local-state + count pill, ProfileTabs keeps its ?tab= derive-each-render URL control (IN-06), WatchlistTabs keeps its idBase/panelId id contract that the external StrategyTable role=tabpanel resolves against"
    - "Ported tests are GREEN with getByRole(\"button\") updated to getByRole(\"tab\") (Radix triggers are role=tab) and all idBase/aria-controls/roving-tabindex/automatic-activation assertions preserved"
  artifacts:
    - path: "src/components/admin/AdminTabs.tsx"
      provides: "AdminTabs rendered via Tabs underline variant, local useState controlled, count pill preserved"
      contains: "@/components/ui/Tabs"
    - path: "src/components/auth/ProfileTabs.tsx"
      provides: "ProfileTabs via Tabs underline, value=activeTab derived from ?tab= each render (IN-06 preserved)"
      contains: "@/components/ui/Tabs"
    - path: "src/components/strategy/WatchlistTabs.tsx"
      provides: "WatchlistTabs via Tabs segmented, explicit idBase-derived trigger ids + aria-controls=panelId preserved"
      contains: "@/components/ui/Tabs"
  key_links:
    - from: "src/components/strategy/WatchlistTabs.tsx"
      to: "src/components/strategy/StrategyTable.tsx (external role=tabpanel)"
      via: "explicit trigger id `${idBase}-tab-all` + aria-controls=panelId resolves the panel aria-labelledby"
      pattern: "idBase|panelId|aria-controls"
    - from: "src/components/auth/ProfileTabs.tsx"
      to: "URL ?tab= query param"
      via: "value={parseTabParam(searchParams.get('tab'))} derive-each-render (IN-06)"
      pattern: "parseTabParam|searchParams"
---

<objective>
Wave 2 of Phase 50: consolidate the 3 hand-rolled Tabs implementations (AdminTabs, ProfileTabs,
WatchlistTabs) onto the ONE canonical `Tabs` primitive built in Wave 1 — the highest-value
strangler dedup in the phase (3 inconsistent roving-tabindex impls collapse to 1 audited Radix
impl). Each consumer's existing behavior must be byte-faithful, and each `.test.tsx` is ported in
the SAME plan (BP-03 coverage ratchet held). This is the single biggest risk in the phase
(RESEARCH Pitfall 1) because WatchlistTabs feeds an external StrategyTable panel via an imperative
id contract that Radix would otherwise overwrite.

Purpose: prove the strangler on a clean dedup (UI-03) while delivering the a11y-correct canonical
Tabs across the app (UI-02). Output: 3 migrated consumers + 3 ported test files, all GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md

<interfaces>
<!-- The Tabs primitive (Wave 1, Plan 03) the consumers port onto, + each consumer's preserve-list. -->

Tabs primitive — src/components/ui/Tabs.tsx (Plan 03)
  exports Tabs (Root) / TabsList / TabsTrigger / TabsContent; variant "underline" | "segmented";
  value/defaultValue/onValueChange/activationMode passthrough; TabsTrigger honors an explicit `id`.

AdminTabs — src/components/admin/AdminTabs.tsx
  local useState<Tab>; underline strip lines 123-143; active "border-accent text-accent";
  count pill bg-accent text-white rounded-full; 5 tab bodies (IntroRequestsTab...). Test stubs
  HTMLDialogElement.showModal at lines 33-46; clicks via getByText("Strategy Review").

ProfileTabs — src/components/auth/ProfileTabs.tsx
  "use client"; activeTab = parseTabParam(searchParams.get("tab"), isAllocator) derived EACH
  render (IN-06 fix, line 69); setActiveTab does router.replace ?tab= (lines 74-81); allocatorOnly
  tab gating (line 83 filter). Active "border-accent text-text-primary". Test queries triggers via
  getByRole("button", ...) at 3 sites (MUST become getByRole("tab")).

WatchlistTabs — src/components/strategy/WatchlistTabs.tsx
  props { scope, onScopeChange, count, idBase, panelId }; segmented container; builds
  id={`${idBase}-tab-all`} + aria-controls={panelId}; data-testid="watchlist-count-badge".
  External panel — StrategyTable.tsx:385-394 renders <div id={panelId} role="tabpanel"
  aria-labelledby={`${tabIdBase}-tab-${scope}`}>. Test pins: allTab.id === "abc123-tab-all",
  aria-controls === panelId, roving tabindex 0/-1, automatic activation on Arrow/Home/End,
  data-testid badge.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port AdminTabs onto the Tabs underline variant (+ test)</name>
  <read_first>
    - src/components/admin/AdminTabs.tsx (the underline strip lines 123-143; local useState<Tab>; count pill line 137; the 5 tab bodies)
    - src/components/admin/AdminTabs.test.tsx (getByText("Strategy Review") clicks survive; showModal stub lines 33-46)
    - src/components/ui/Tabs.tsx (the primitive to consume — underline variant)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md (AdminTabs section)
  </read_first>
  <files>src/components/admin/AdminTabs.tsx, src/components/admin/AdminTabs.test.tsx</files>
  <action>
    Replace the hand-rolled tab strip (lines ~123-143) with Tabs/TabsList/TabsTrigger from
    @/components/ui/Tabs (underline variant). Keep the local useState<Tab> by wiring Radix
    value={tab} + onValueChange={setTab}. Preserve the active treatment "border-accent text-accent"
    and the count pill (now text-micro per the Wave-1 refresh) via the Trigger className hook. The
    5 tab bodies (IntroRequestsTab/StrategyReviewTab/etc.) render as TabsContent panels OR stay
    conditionally rendered if that keeps the ported test simpler — verify against AdminTabs.test.tsx.
    AdminTabs.test.tsx clicks via getByText (survives Radix) — keep those; reuse the existing
    showModal stub. Do NOT change which tabs exist or the modal flows.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/AdminTabs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - AdminTabs.tsx imports from @/components/ui/Tabs and no longer hand-rolls a tab keydown handler.
    - AdminTabs.test.tsx passes; the 5 tabs + the active count pill render; modal flows unchanged.
    - Local useState controls the active tab via Radix value/onValueChange (no behavior change).
  </acceptance_criteria>
  <done>AdminTabs renders through the canonical Tabs primitive (underline) with its count pill + local-state control intact.</done>
</task>

<task type="auto">
  <name>Task 2: Port ProfileTabs onto Tabs underline (URL-controlled, IN-06 preserved) + test port</name>
  <read_first>
    - src/components/auth/ProfileTabs.tsx (parseTabParam line 37; activeTab derive-each-render line 69; setActiveTab router.replace lines 74-81; allocatorOnly filter line 83; active "border-accent text-text-primary")
    - src/components/auth/ProfileTabs.test.tsx (getByRole("button") at 3 sites — Pitfall 2; back/forward parity lines 140-169)
    - src/components/ui/Tabs.tsx
    - .planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md (ProfileTabs section — IN-06 + Pitfall 2 mandatory test port)
  </read_first>
  <files>src/components/auth/ProfileTabs.tsx, src/components/auth/ProfileTabs.test.tsx</files>
  <action>
    Replace the hand-rolled strip with Tabs underline. Wire Radix value={activeTab} (still derived
    EACH render from parseTabParam(searchParams.get("tab"), isAllocator) — DO NOT reintroduce a
    useState snapshot; that is the IN-06 fix) + onValueChange={setActiveTab} (the existing
    router.replace ?tab= handler). Preserve the allocatorOnly tab filter and the active treatment
    "border-accent text-text-primary" via the Trigger className hook. TEST PORT (Pitfall 2,
    MANDATORY, mechanical NOT a regression): change every getByRole("button", { name }) ->
    getByRole("tab", { name }) and getAllByRole("button") (the tab-order assertion) ->
    getAllByRole("tab") in ProfileTabs.test.tsx. The back/forward parity test (lines 140-169) must
    still pass (URL is the source of truth).
  </action>
  <verify>
    <automated>npx vitest run src/components/auth/ProfileTabs.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ProfileTabs.tsx imports @/components/ui/Tabs; activeTab is still derived from searchParams each render (grep shows parseTabParam(searchParams.get("tab")...) feeding Radix value); no new useState for the active tab.
    - ProfileTabs.test.tsx: zero remaining `getByRole("button"` / `getAllByRole("button"` for the tab triggers (all became `"tab"`); the suite passes including the back/forward parity test.
    - The allocatorOnly tab gating is unchanged (non-allocator users do not see allocator-only tabs).
  </acceptance_criteria>
  <done>ProfileTabs renders through the canonical Tabs primitive (underline) with its ?tab= URL control (IN-06) intact; tests ported to role=tab.</done>
</task>

<task type="auto">
  <name>Task 3: Port WatchlistTabs onto Tabs segmented preserving the StrategyTable id contract (+ test) — HIGHEST RISK</name>
  <read_first>
    - src/components/strategy/WatchlistTabs.tsx (props idBase/panelId; id={`${idBase}-tab-all`} + aria-controls={panelId} lines 53-57; aria-label="Strategy list scope" line 48; data-testid badge)
    - src/components/strategy/StrategyTable.tsx (lines 384-394: the EXTERNAL <div id={panelId} role="tabpanel" aria-labelledby={`${tabIdBase}-tab-${scope}`}> the trigger ids must resolve)
    - src/components/strategy/WatchlistTabs.test.tsx (pins: allTab.id === "abc123-tab-all" ~L195, aria-controls === panelId ~L203, roving tabindex 0/-1, automatic activation Arrow/Home/End, watchlist-count-badge)
    - src/components/ui/Tabs.tsx (segmented variant + explicit-id passthrough — the whole reason Plan 03 exposed it)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md (Pitfall 1 + Open Question 1 — recommend option (a): preserve imperative ids via the segmented variant; lift-panel is fallback only)
  </read_first>
  <files>src/components/strategy/WatchlistTabs.tsx, src/components/strategy/WatchlistTabs.test.tsx</files>
  <action>
    Port WatchlistTabs to the Tabs segmented variant. CRITICAL (Pitfall 1, option (a) — lowest
    blast radius): pass EXPLICIT trigger ids `${idBase}-tab-all` / `${idBase}-tab-watchlist` and
    aria-controls={panelId} through to the TabsTrigger (the Wave-1 primitive honors an explicit
    id). The StrategyTable panel is rendered OUTSIDE this component and depends on those ids for
    its aria-labelledby — do NOT let Radix auto-generate them away. Keep the segmented container
    styling (active "bg-accent/10 text-accent"), the aria-label="Strategy list scope", the
    data-testid="watchlist-count-badge", controlled via scope/onScopeChange (Radix value=scope +
    onValueChange=onScopeChange), and automatic activation (arrow changes scope). Port
    WatchlistTabs.test.tsx KEEPING every pinned assertion (idBase-derived ids, aria-controls,
    roving tabindex 0/-1, automatic activation, count badge) — these are the acceptance gate; if
    option (a) cannot keep them green, fall back to lifting the panel into a Tabs.Content
    (RESEARCH option (b)) and update StrategyTable's tabpanel block accordingly (note: that would
    also require editing StrategyTable.tsx which Plan 06 owns — coordinate; option (a) avoids it).
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/WatchlistTabs.test.tsx src/components/strategy/StrategyTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - WatchlistTabs.tsx imports @/components/ui/Tabs (segmented) and the triggers carry explicit ids `${idBase}-tab-all` / `${idBase}-tab-watchlist` and aria-controls={panelId}.
    - WatchlistTabs.test.tsx passes ALL pinned assertions: allTab.id === "abc123-tab-all", aria-controls === panelId, roving tabindex 0/-1, automatic activation on Arrow/Home/End, watchlist-count-badge present.
    - StrategyTable.test.tsx tabpanel/aria-labelledby assertions still pass (the external panel's aria-labelledby resolves to a real trigger id).
    - Option (a) was used (no StrategyTable.tsx edit from THIS plan); if option (b) was unavoidable, it is documented in the SUMMARY and coordinated with Plan 06.
  </acceptance_criteria>
  <done>WatchlistTabs renders through the canonical Tabs primitive (segmented) WITHOUT breaking the external StrategyTable role=tabpanel wiring; the full id/aria/keyboard test gate is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL query param -> ProfileTabs render | `?tab=` is untrusted user-controlled input that selects the active tab. |
| role gating -> ProfileTabs tab visibility | allocatorOnly tabs are an access-adjacent UI gate (must not widen). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-07 | Tampering (untrusted URL param) | ProfileTabs `?tab=` | accept | `parseTabParam` already coerces any unknown/forbidden value to the default `personal` (and respects isAllocator) — the port preserves this validation verbatim; no new parsing surface. |
| T-50-08 | Elevation of Privilege (tab gating) | ProfileTabs allocatorOnly filter | mitigate | The port preserves the `ALL_TABS.filter(allocatorOnly || isAllocator)` gate byte-faithfully so a non-allocator cannot reach allocator-only tabs; this is a UI affordance gate (the underlying data routes have their own server access control, unchanged). Acceptance-criteria gated. |
| T-50-04 | Tampering (XSS) | tab labels / panel content | mitigate | All tab labels + panel content render as React children (escaped); no `dangerouslySetInnerHTML` introduced by the port. |
| V2/V3/V6 ASVS | — | n/a | n/a (not applicable) | No session/auth/crypto change; the tab gating is V4-adjacent UI only (covered by T-50-08). Marked non-applicable explicitly. |
</threat_model>

<verification>
- `npx vitest run src/components/admin/AdminTabs.test.tsx src/components/auth/ProfileTabs.test.tsx src/components/strategy/WatchlistTabs.test.tsx src/components/strategy/StrategyTable.test.tsx` all GREEN.
- The 3 consumers import @/components/ui/Tabs; no hand-rolled tab keydown handlers remain.
- ProfileTabs test has zero `getByRole("button"` for tab triggers; WatchlistTabs id/aria-controls assertions intact.
- `npm run test:coverage` holds 82/80/74/72 (run at wave merge).
</verification>

<success_criteria>
- 3 hand-rolled tab impls collapse to the one canonical Tabs primitive (strangler dedup proven — UI-03).
- AdminTabs (local-state), ProfileTabs (?tab= IN-06), WatchlistTabs (idBase/panelId contract) all byte-faithful.
- All 3 ported test files green; the StrategyTable external panel wiring is intact.
</success_criteria>

<output>
Create `.planning/phases/50-primitive-refresh-missing-primitives/50-05-SUMMARY.md` when done.
</output>
