---
phase: 50-primitive-refresh-missing-primitives
plan: 02
type: execute
wave: 2
depends_on: ["50-01"]
files_modified:
  - src/components/ui/Button.tsx
  - src/components/ui/Input.tsx
  - src/components/ui/Textarea.tsx
  - src/components/ui/Select.tsx
  - src/components/ui/Badge.tsx
  - src/components/ui/Modal.tsx
  - src/components/ui/Skeleton.tsx
  - src/components/ui/Card.tsx
autonomous: true
requirements: [UI-01, UI-04]
user_setup: []

must_haves:
  truths:
    - "Refreshing the core primitives visibly re-points their type onto the Phase-49 fluid --text-* tiers without any per-call-site edit (the 68 Button importers and all other consumers inherit for free)"
    - "Button and Modal-close show their focus ring on keyboard/programmatic focus (focus-visible:) and not on mouse click"
    - "Modal stays a native <dialog>; Select stays a native <select> — no Radix import is added to either"
  artifacts:
    - path: "src/components/ui/Button.tsx"
      provides: "Refreshed Button: md/lg=text-body, sm=text-caption, focus-visible ring"
      contains: "focus-visible:ring"
    - path: "src/components/ui/Modal.tsx"
      provides: "Refreshed Modal: title text-h3, close button focus-visible ring, still native dialog"
      contains: "text-h3"
    - path: "src/components/ui/Input.tsx"
      provides: "Refreshed Input: control text-body, label text-small, error text-caption; keeps focus: soft ring"
      contains: "text-body"
  key_links:
    - from: "src/components/ui/Button.tsx"
      to: "src/app/globals.css (--text-body / --text-caption tiers)"
      via: "Tailwind text-body / text-caption utility classes"
      pattern: "text-(body|caption)"
    - from: "src/components/ui/Button.test.tsx"
      to: "src/components/ui/Button.tsx"
      via: "lock test goes GREEN on the refreshed tier classes + focus-visible"
      pattern: "text-body"
---

<objective>
Wave 1 of Phase 50: refresh the 8 core primitives (Button, Input, Textarea, Select, Badge,
Modal, Skeleton, Card) by re-pointing their internal Tailwind type classes onto the Phase-49
fluid `--text-*` tier utilities — additive/CSS-only, with every primitive's PUBLIC PROP API
byte-identical so all existing consumers inherit the refresh with zero call-site edits (UI-01).
The only behavior-adjacent edit is migrating Button + Modal-close from `focus:` to
`focus-visible:` for the keyboard ring. Modal stays native `<dialog>`; Select stays native
`<select>` (UI-04).

Purpose: Existing consumers get the evolved look "for free." This is the lowest-risk, highest-
leverage work in the phase. Output: 8 refreshed primitive source files; the Button + Modal lock
tests from Wave 0 (Plan 01) flip GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md
@DESIGN.md

<interfaces>
<!-- The exact migration map (verified against the live sources). The refresh is a class swap only. -->

Fluid tier utilities (live, Phase 49) — src/app/globals.css:139-143
  text-h3   (16->18px)   text-body  (14->16px)   text-small (13->14px)
  text-caption (12->13px)   text-micro (10->11px)

Per-primitive class -> tier map (UI-SPEC §Typography migration mapping):
  Button md/lg  text-sm / text-base  -> text-body
  Button sm     text-xs              -> text-caption
  Input/Select/Textarea control  text-sm        -> text-body
  Input/Select/Textarea <label>  text-sm font-medium -> text-small font-medium
  Input/Select/Textarea error    text-xs        -> text-caption
  Badge         text-xs             -> text-caption (micro variant: text-micro uppercase tracking-wider)
  Modal title   text-lg font-semibold -> text-h3 font-semibold
  Skeleton      (no token change; confirm bg-border/60; reduced-motion inherited)
  Card          (NO change this phase — rounded-xl radius deferred to 52/53)

cn() helper — src/lib/utils.ts:72
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refresh Button + Modal (the focus-visible pair)</name>
  <read_first>
    - src/components/ui/Button.tsx (sizeStyles sm/md/lg at lines 15-19; base className with focus:ring at line 35; variantStyles FROZEN)
    - src/components/ui/Modal.tsx (title h2 at line 30; close button lines 31-44; native <dialog> lines 23-27 — KEEP native)
    - src/components/ui/Button.test.tsx + Modal.test.tsx (the Wave-0 lock tests this task turns GREEN)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md (Button + Modal/Card/Badge sections; RESEARCH Pitfall 4 on focus-visible)
  </read_first>
  <files>src/components/ui/Button.tsx, src/components/ui/Modal.tsx</files>
  <behavior>
    - Button sizeStyles: sm text-xs -> text-caption; md text-sm -> text-body; lg text-base -> text-body. KEEP px/py, min-h-[44px], rounded-lg, transition-colors, variantStyles map, the ButtonProps interface (variant/size + native attrs — unchanged).
    - Button base: focus:outline-none focus:ring-2 focus:ring-accent/50 -> focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/50.
    - Modal title: h2 className text-lg font-semibold -> text-h3 font-semibold (keep text-text-primary).
    - Modal close button: ADD focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent/50 (it has none today); keep aria-label="Close", the hover, and the native <dialog> + showModal()/close() lifecycle exactly. Do NOT swap to Radix Dialog (UI-04).
  </behavior>
  <action>
    Apply the exact class swaps from the behavior block. Button + Modal are the ONLY two
    primitives that migrate focus: -> focus-visible: this phase (Pitfall 4) — do NOT migrate
    field focus rings. No prop interface change on either. After editing, the Wave-0
    Button.test.tsx / Modal.test.tsx lock tests must pass.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/Button.test.tsx src/components/ui/Modal.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'text-body|text-caption' src/components/ui/Button.tsx` shows md/lg=text-body, sm=text-caption; `grep -c 'text-sm\|text-base\|text-xs' src/components/ui/Button.tsx` (sizeStyles region) returns 0.
    - `grep -c 'focus-visible:ring' src/components/ui/Button.tsx` >= 1 AND `grep -E '(^|[^-])focus:ring' src/components/ui/Button.tsx` returns nothing (no bare focus:ring).
    - `grep -c 'text-h3' src/components/ui/Modal.tsx` >= 1 AND `grep -c 'text-lg' src/components/ui/Modal.tsx` == 0; close button has focus-visible:ring; `grep -c '<dialog' src/components/ui/Modal.tsx` >= 1 (still native).
    - The Wave-0 Button.test.tsx and Modal.test.tsx pass.
    - ButtonProps interface and ModalProps interface are byte-identical to pre-refresh (no added/removed/renamed prop).
  </acceptance_criteria>
  <done>Button + Modal are token-refreshed with focus-visible rings; Modal stays native; lock tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Refresh Input + Textarea + Select (field controls, keep focus: soft ring)</name>
  <read_first>
    - src/components/ui/Input.tsx (label line 19; control line 27; error line 30 — keep the focus:border-border-focus focus:ring-2 focus:ring-accent/20 soft glow)
    - src/components/ui/Textarea.tsx + src/components/ui/Select.tsx (near-identical mirrors of Input; Select stays native <select>)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md (Input/Textarea/Select section + the "keep focus: here" note)
  </read_first>
  <files>src/components/ui/Input.tsx, src/components/ui/Textarea.tsx, src/components/ui/Select.tsx</files>
  <behavior>
    - All three controls: <label> text-sm font-medium -> text-small font-medium; control text -> text-body; error <p> text-xs -> text-caption.
    - KEEP the soft field focus glow focus:border-border-focus focus:ring-2 focus:ring-accent/20 EXACTLY (fields legitimately show focus on click — Pitfall 4: do NOT migrate these to focus-visible:).
    - KEEP min-h-[44px], rounded-lg, forwardRef + displayName + useId, the inline label/error markup, and every public prop (label?/error?/native attrs; Select also options). Select stays a native <select> (UI-04) — no Radix.
  </behavior>
  <action>
    Apply the type-tier swaps to all three controls; leave the focus rings as focus: (soft
    accent glow). The three files share the same label/control/error shape — apply the same
    map consistently. No prop interface change.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/ && grep -L 'text-body' src/components/ui/Input.tsx src/components/ui/Textarea.tsx src/components/ui/Select.tsx | head -1 | grep -q . && (echo "a control file is missing text-body"; exit 1) || echo "controls refreshed"</automated>
  </verify>
  <acceptance_criteria>
    - Each of Input/Textarea/Select: control text is text-body; label is text-small font-medium; error is text-caption.
    - `grep -c 'focus:ring-accent/20' src/components/ui/Input.tsx` >= 1 (soft field ring preserved, NOT migrated to focus-visible).
    - `grep -c '<select' src/components/ui/Select.tsx` >= 1 (still native) AND no `@radix-ui` import in Select.tsx.
    - The existing src/components/ui/ test suite passes (no public-prop regression).
  </acceptance_criteria>
  <done>The three field controls are token-refreshed, keep their click-visible focus glow, and Select stays native.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh Badge + Skeleton; confirm Card unchanged</name>
  <read_first>
    - src/components/ui/Badge.tsx (base className line 53; colorMap/statusMap/statusLabelMap FROZEN verbatim)
    - src/components/ui/Skeleton.tsx (line 11: animate-pulse rounded-md bg-border/60 + aria-hidden)
    - src/components/ui/Card.tsx (line 22: rounded-xl — DEFERRED, do NOT change)
    - .planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md (Badge/Modal/Card/Skeleton section; RESEARCH anti-pattern + A3 on Card radius deferral)
  </read_first>
  <files>src/components/ui/Badge.tsx, src/components/ui/Skeleton.tsx, src/components/ui/Card.tsx</files>
  <behavior>
    - Badge: base text-xs -> text-caption; the uppercase micro variant (where a consumer renders it) uses text-micro uppercase tracking-wider. colorMap/statusMap/statusLabelMap and public props (label/type) FROZEN verbatim.
    - Skeleton: confirm animate-pulse rounded-md bg-border/60 + aria-hidden; do NOT add a prefers-reduced-motion block (the .animate-pulse freeze at globals.css:152 is inherited — duplicating is a divergence risk). No token change unless bg-border/60 is not the --color-border token (it is — confirm only).
    - Card: NO change. rounded-xl (12px) vs DESIGN.md 8px is a visible restyle DEFERRED to 52/53. Touching it here violates the additive/CSS-only-no-visual-delta contract. Leave Card.tsx byte-identical (it is included in files_modified only to assert "no change" and prevent a parallel plan from racing it).
  </behavior>
  <action>
    Swap Badge text-xs -> text-caption (+ micro variant note). Confirm Skeleton needs no edit
    beyond verifying the token. Leave Card.tsx unmodified. Do not add new branches or props.
  </action>
  <verify>
    <automated>npx vitest run src/components/ui/ && grep -c 'text-caption' src/components/ui/Badge.tsx | grep -qv '^0$' && grep -c 'rounded-xl' src/components/ui/Card.tsx | grep -qv '^0$' && echo "badge refreshed, card radius preserved"</automated>
  </verify>
  <acceptance_criteria>
    - Badge base type is text-caption; `grep -c 'text-xs' src/components/ui/Badge.tsx` == 0; colorMap/statusMap entries unchanged.
    - Skeleton.tsx has no new `@media (prefers-reduced-motion` block; bg-border/60 + aria-hidden intact.
    - Card.tsx still contains `rounded-xl` (radius NOT changed this phase) and is otherwise unmodified.
    - The src/components/ui/ test suite passes.
  </acceptance_criteria>
  <done>Badge token-refreshed; Skeleton verified inheriting the reduced-motion freeze; Card radius intentionally preserved for 52/53.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| consumer code -> primitive render | 68 Button importers + all Input/Select/Modal consumers render user/admin-supplied content through these primitives; the refresh must not introduce an injection sink or weaken a control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-02 | Tampering (XSS) | Refreshed primitives rendering user content | mitigate | Refresh is a Tailwind class swap only. No `dangerouslySetInnerHTML` is introduced; React escaping is preserved on every text node. The change touches className strings, not value rendering. |
| T-50-03 | Spoofing (focus indicator) | Button/Modal focus-visible migration | accept (verify) | focus-visible fires on keyboard/programmatic focus, so the AA focus indicator (WCAG 2.4.7/1.4.11, >=3:1) is preserved for keyboard users; axe cannot catch a missing mouse ring (intended removal). Verified via the manual keyboard sweep recorded in 50-VALIDATION.md. |
| V2/V3/V4/V6 ASVS | — | n/a | n/a (not applicable) | Presentation-only class swaps; no auth/session/access-control/crypto surface. Marked non-applicable explicitly. |
</threat_model>

<verification>
- `npx vitest run src/components/ui/` passes (incl. the Wave-0 Button/Modal lock tests now GREEN).
- No primitive's public prop interface changed (consumers compile unchanged — `npx tsc --noEmit` clean).
- Modal still renders a native `<dialog>`; Select still renders a native `<select>`; neither imports `@radix-ui/*`.
- Card.tsx unchanged (rounded-xl preserved).
</verification>

<success_criteria>
- 8 core primitives consume the fluid `--text-*` tiers (Card intentionally untouched); public props frozen.
- Button + Modal-close use focus-visible rings; field controls keep their focus: soft glow.
- UI-04 preserved: Modal native dialog, Select native select.
</success_criteria>

<output>
Create `.planning/phases/50-primitive-refresh-missing-primitives/50-02-SUMMARY.md` when done.
</output>
