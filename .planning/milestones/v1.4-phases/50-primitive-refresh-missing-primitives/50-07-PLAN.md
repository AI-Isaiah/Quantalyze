---
phase: 50-primitive-refresh-missing-primitives
plan: 07
type: execute
wave: 3
depends_on: ["50-02", "50-03"]
files_modified:
  - src/components/admin/ComputeJobsTable.tsx
  - src/components/admin/ComputeJobsTable.test.tsx
  - e2e/admin-compute-jobs-axe.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [UI-03, UI-02]
user_setup: []

must_haves:
  truths:
    - "The /admin/compute-jobs surface (the ComputeJobsTable client component) renders through the Button / Table / Field primitives instead of raw <button>/<table>/<input>, with behavior byte-identical (filters, auto-refresh persistence, load-more pagination all unchanged)"
    - "The migration is demonstrably incremental (strangler): ONE surface, no big-bang rewrite, no other surface touched, tests ported in the same plan"
    - "The /admin/compute-jobs admin gate (isAdminUser redirect) is untouched and the compute_jobs claim-token select gate is not regressed"
    - "An axe spec proves zero WCAG-AA violations on the migrated surface and is wired into CI"
  artifacts:
    - path: "src/components/admin/ComputeJobsTable.tsx"
      provides: "Pilot migrated onto Button (Load more) + Table base (jobs table) + Field/Select primitives (filters) + Field checkbox (auto-refresh); inline hex/style props replaced by --color-*/--text-* tokens"
      contains: "@/components/ui/"
    - path: "e2e/admin-compute-jobs-axe.spec.ts"
      provides: "axe gate on /admin/compute-jobs, mirrors admin-csv-status-axe (seed-gated, URL-pinned false-green guard)"
      min_lines: 30
  key_links:
    - from: "src/components/admin/ComputeJobsTable.tsx"
      to: "src/components/ui/{Button,Table,Field}"
      via: "raw <button>/<table>/<input> replaced by primitive imports"
      pattern: "@/components/ui/(Button|Table|Field)"
    - from: "e2e/admin-compute-jobs-axe.spec.ts"
      to: ".github/workflows/ci.yml (seeded MA-8 playwright list)"
      via: "spec added to BOTH the CI npx-playwright list AND its own HAS_SEED_ENV const"
      pattern: "admin-compute-jobs-axe"
---

<objective>
Wave 2 of Phase 50: the strangler pilot (UI-03). Migrate ONE non-engine surface off raw
`<button>`/`<table>`/`<input>` onto the primitives, behavior-identical, to prove the strangler is
incremental (broad per-surface migration is deferred to 52/53). The pilot is
**`/admin/compute-jobs`** via its `ComputeJobsTable` client component — chosen (overriding
RESEARCH's `/admin/usage`, which is table-only) because it genuinely exercises raw `<button>`
(Load more, line 284), raw `<table>` (line 197), raw `<input type="checkbox">` (auto-refresh,
line 173) AND raw `<select>` filters (lines 146/159) TOGETHER on one cohesive non-engine,
admin-gated surface — satisfying the CONTEXT.md "button + table + input together" constraint that
`/admin/usage` cannot. Bonus: it carries 23 inline hardcoded-hex/style sites the migration cleans
onto `--color-*`/`--text-*` tokens.

Purpose: demonstrate the primitive toolkit replaces raw-element sprawl with no behavior
regression and no access-control change. Output: migrated component + ported test + a new axe
spec wired into CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-RESEARCH.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md
@.planning/phases/50-primitive-refresh-missing-primitives/50-UI-SPEC.md

<interfaces>
<!-- The pilot surface + the primitives it adopts + the CI wiring trap. -->

ComputeJobsTable — src/components/admin/ComputeJobsTable.tsx ("use client", already)
  raw <select> filters lines 146-170; raw <input type="checkbox"> auto-refresh line 173;
  raw <table> in ResponsiveTable line 197; raw <button> "Load more" line 284.
  23 inline #hex/style={{}} sites (e.g. style={{borderColor:"#E2E8F0",color:"#1A1A2E"}}).
  Persistence: useCrossTabStorage auto-refresh pref (byte-compat String(bool)) — DO NOT change.
  Fetch /api/admin/compute-jobs with status/kind/offset params — DO NOT change.

Primitives to adopt (Plans 02/03):
  Button — src/components/ui/Button.tsx (Load more -> <Button variant="secondary" size="sm">)
  Table base — src/components/ui/Table.tsx (the jobs table -> semantic <th scope=col> parts)
  Field + native Select — src/components/ui/{Field,Select}.tsx (the status/kind filters; auto-refresh checkbox)

Page gate (PRESERVE byte-faithfully) — src/app/(dashboard)/admin/compute-jobs/page.tsx
  force-dynamic RSC; if (!isAdminUser(...)) redirect("/discovery/crypto-sma"); the select list
  intentionally OMITS claim_token (compute_jobs claim-token leak gate — see below).

Security grep gate (must stay green) — src/__tests__/compute-jobs-claim-token-not-leaked.test.ts
  fails the build if any src/ file does select("*") against compute_jobs. The pilot edits the
  CLIENT component (renders a fetched row shape), not the page select — keep it that way.

Existing test — src/components/admin/ComputeJobsTable.test.tsx
  mocks fetch (empty page); asserts auto-refresh defaults OFF, reads persisted "true" as ON,
  toggling persists String(bool); queries getByRole("checkbox", { name: /auto-refresh/i }).

axe spec analog — e2e/admin-csv-status-axe.spec.ts (seed-gated, admin-login, buildAxe().analyze() -> violations.toEqual([]), URL-pin false-green guard).
CI list — .github/workflows/ci.yml: the seeded MA-8 `npx playwright test \` block (~line 1266) — add the new spec here AND set its own HAS_SEED_ENV const (FLOW-01 trap: a seed-gated spec must be in BOTH places or it never runs in CI).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ComputeJobsTable onto Button + Table + Field/Select primitives + tokens (behavior-identical)</name>
  <read_first>
    - src/components/admin/ComputeJobsTable.tsx (the whole file — raw select/checkbox/table/button + the 23 inline hex/style sites; the useCrossTabStorage persistence + fetch logic to PRESERVE)
    - src/components/ui/Button.tsx (post-refresh; Load more target)
    - src/components/ui/Table.tsx (Plan 03 semantic parts; the jobs table target)
    - src/components/ui/Field.tsx + src/components/ui/Select.tsx (the filter + checkbox targets)
    - src/components/ResponsiveTable.tsx (the wrapper already used at line 196 — keep label="Compute jobs")
    - .planning/phases/50-primitive-refresh-missing-primitives/50-PATTERNS.md (admin/usage pilot section + the button+input flag the planner resolved to compute-jobs)
  </read_first>
  <files>src/components/admin/ComputeJobsTable.tsx</files>
  <action>
    Replace the raw elements with primitives, preserving behavior EXACTLY:
    - "Load more" raw <button> (line 284) -> <Button variant="secondary" size="sm"> keeping
      onClick={loadMore} + disabled={loading} + the "Load more" label.
    - The jobs <table> (line 197) -> the Table base parts (semantic <th scope="col"> headers,
      named table) inside the existing ResponsiveTable wrapper (keep label="Compute jobs" — the
      unique-aria-label landmark). Keep the 6 columns, the status badge, font-metric tabular-nums
      on numeric cells, the truncate+title on Target/Last Error, the empty "No compute jobs found." row.
    - The status/kind <select> filters -> the native Select primitive (or Field-wrapped Select);
      the auto-refresh <input type="checkbox"> -> a Field-wrapped checkbox keeping the accessible
      name "Auto-refresh" and the toggleAutoRefresh handler. Preserve useCrossTabStorage exactly
      (byte-compat String(bool) persistence) and the /api/admin/compute-jobs fetch params.
    - Replace ALL inline #hex / style={{}} color/border values with the --color-* / --text-*
      token utilities (text-caption/text-micro tiers; border-border; text-text-muted/primary/
      secondary; the status colors map onto the existing semantic tokens). The design-lint
      no-raw-hex rule must pass on this file after.
    Do NOT change the fetch contract, the persistence, the status-color semantics, or the page's
    admin gate. This is the ONLY surface migrated — touch no other component.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/ComputeJobsTable.test.tsx && npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ComputeJobsTable.tsx imports from @/components/ui/Button, @/components/ui/Table, and @/components/ui/{Field,Select}; raw `<button>`, raw `<table>`, and the raw filter/checkbox controls are gone (replaced by primitives).
    - `grep -cE '#[0-9A-Fa-f]{6}' src/components/admin/ComputeJobsTable.tsx` == 0 (inline hex removed); `npm run lint` reports no new design-lint violations on the file.
    - useCrossTabStorage auto-refresh persistence and the /api/admin/compute-jobs fetch params are unchanged (behavior-identical).
    - The ResponsiveTable wrapper keeps label="Compute jobs"; numeric cells keep font-metric tabular-nums.
    - No `select("*")` against compute_jobs is introduced (claim-token gate stays green).
  </acceptance_criteria>
  <done>The pilot surface renders entirely through primitives + tokens with byte-identical behavior; raw elements and inline hex are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Port ComputeJobsTable.test.tsx to the primitives (behavior assertions preserved)</name>
  <read_first>
    - src/components/admin/ComputeJobsTable.test.tsx (the fetch mock + auto-refresh persistence asserts; getByRole("checkbox", { name: /auto-refresh/i }))
    - src/components/admin/ComputeJobsTable.tsx (the migrated component)
  </read_first>
  <files>src/components/admin/ComputeJobsTable.test.tsx</files>
  <action>
    Update the test queries to match the primitive DOM where they shifted, KEEPING every behavior
    assertion: auto-refresh defaults OFF, reads persisted "true" as ON after deferred hydration,
    toggling persists String(bool). The auto-refresh control is still a checkbox with accessible
    name "Auto-refresh" (Field-wrapped) so getByRole("checkbox", { name: /auto-refresh/i }) should
    still resolve — adjust only if the accessible name path changed. Add a render assertion that
    the "Load more" Button renders when hasMore (mock a full page) and that the jobs table renders
    via the Table base (getByRole("table") + a columnheader scope check). Keep the fetch mock.
    This is a mechanical port (BP-03) — flag any query change as a DOM-shape adjustment, not a
    behavior regression.
  </action>
  <verify>
    <automated>npx vitest run src/components/admin/ComputeJobsTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - ComputeJobsTable.test.tsx passes; the 3 auto-refresh persistence assertions are preserved and green.
    - getByRole("checkbox", { name: /auto-refresh/i }) still resolves the auto-refresh control.
    - A new assertion confirms the jobs render through a real <table> with scope="col" headers (Table base) and the Load-more Button appears when hasMore.
  </acceptance_criteria>
  <done>The pilot test is ported to the primitive DOM with all behavior assertions intact.</done>
</task>

<task type="auto">
  <name>Task 3: Add the /admin/compute-jobs axe spec + wire it into CI (FLOW-01 both-places)</name>
  <read_first>
    - e2e/admin-csv-status-axe.spec.ts (the 1:1 analog — seed gate, admin login, buildAxe().analyze() -> violations.toEqual([]), URL-pin false-green guard)
    - e2e/helpers/axe.ts (buildAxe factory — do NOT redeclare the rule set locally)
    - e2e/helpers/seed-test-project.ts (seedTestAllocator + the admin-user seed helper used by the analog)
    - .github/workflows/ci.yml (the seeded MA-8 `npx playwright test \` block ~line 1266 — add the spec here)
  </read_first>
  <files>e2e/admin-compute-jobs-axe.spec.ts, .github/workflows/ci.yml</files>
  <action>
    Create e2e/admin-compute-jobs-axe.spec.ts mirroring admin-csv-status-axe.spec.ts 1:1: same
    HAS_SEED_ENV const (TEST_SUPABASE_URL + TEST_SUPABASE_SERVICE_ROLE_KEY), same admin login +
    seed, navigate to /admin/compute-jobs, assert the heading/URL (false-green guard), run
    buildAxe(page).analyze() and expect violations.toEqual([]). Use the buildAxe factory — do NOT
    redeclare the rule set. Then wire it into CI: add the line `e2e/admin-compute-jobs-axe.spec.ts`
    (with the trailing backslash continuation) to the seeded MA-8 `npx playwright test` list in
    .github/workflows/ci.yml (~line 1266). FLOW-01 TRAP (mandatory): a seed-gated spec must be in
    BOTH the ci.yml list AND carry its own HAS_SEED_ENV self-skip const, or it never runs in CI.
    Do NOT add it to the public/unseeded list (the surface is admin-gated). Keep the spec
    admin-gated; do NOT weaken the page's isAdminUser redirect.
  </action>
  <verify>
    <automated>test -f e2e/admin-compute-jobs-axe.spec.ts && grep -q 'admin-compute-jobs-axe.spec.ts' .github/workflows/ci.yml && grep -q 'HAS_SEED_ENV' e2e/admin-compute-jobs-axe.spec.ts && grep -q 'buildAxe' e2e/admin-compute-jobs-axe.spec.ts && echo "axe spec present + wired"</automated>
  </verify>
  <acceptance_criteria>
    - e2e/admin-compute-jobs-axe.spec.ts exists, uses the buildAxe factory (no local rule-set redeclaration), targets /admin/compute-jobs with a URL/heading false-green guard, and self-skips via its own HAS_SEED_ENV const.
    - .github/workflows/ci.yml seeded MA-8 list includes `e2e/admin-compute-jobs-axe.spec.ts` (FLOW-01: both places).
    - The spec is NOT added to the public/unseeded list (admin-gated surface).
    - The page's isAdminUser redirect is unchanged.
  </acceptance_criteria>
  <done>A seeded, admin-gated axe spec proves zero violations on the migrated pilot and is wired into CI in both required places.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous/non-admin request -> /admin/compute-jobs | Only admins may view compute-job operational data; the page's isAdminUser redirect is the access-control boundary. |
| compute_jobs row -> client component | The fetched row shape must never include claim_token (a capability token); leaking it lets a viewer race the worker. |
| fetched job data -> table cells | Last-error / target strings render into cells. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-10 | Elevation of Privilege (access-control regression) | /admin/compute-jobs admin gate | mitigate | The migration is presentation-only on the CLIENT component; the page's `if (!isAdminUser(...)) redirect(...)` gate is NOT touched (acceptance-criteria gated), and the axe spec runs admin-authed. ASVS V4 preserved byte-faithfully. |
| T-50-11 | Information Disclosure (claim-token leak) | compute_jobs select | mitigate | The pilot edits the client renderer, not the page's select list (which intentionally omits claim_token); the `compute-jobs-claim-token-not-leaked` grep gate stays green (no `select("*")` introduced). Acceptance-criteria gated. |
| T-50-04 | Tampering (XSS) | job last_error/target cells | mitigate | All fetched strings render as React children (escaped); the migration introduces no `dangerouslySetInnerHTML`. |
| T-50-SC | Tampering (supply chain) | new e2e spec deps | accept | The axe spec reuses the existing buildAxe + seed helpers; no new package is added. |
| V3 Session / V6 Cryptography | — | n/a | n/a (not applicable) | No session/cookie or crypto change; auth is the existing admin redirect (covered by T-50-10). Marked non-applicable explicitly. |
</threat_model>

<verification>
- `npx vitest run src/components/admin/ComputeJobsTable.test.tsx` GREEN (behavior preserved).
- `npm run lint` clean; `grep -cE '#[0-9A-Fa-f]{6}' src/components/admin/ComputeJobsTable.tsx` == 0.
- The compute-jobs claim-token grep gate test still passes (no `select("*")`).
- e2e/admin-compute-jobs-axe.spec.ts exists, is seed-gated, and is in the CI seeded MA-8 list (both places).
- The page's isAdminUser redirect is unchanged.
</verification>

<success_criteria>
- /admin/compute-jobs renders through Button + Table + Field/Select primitives with byte-identical behavior (strangler proven — UI-03).
- One surface only; no big-bang rewrite; tests ported in the same plan (BP-03).
- Admin gate + claim-token gate preserved; a seeded axe spec gates zero violations and is wired into CI correctly.
</success_criteria>

<output>
Create `.planning/phases/50-primitive-refresh-missing-primitives/50-07-SUMMARY.md` when done.
</output>
