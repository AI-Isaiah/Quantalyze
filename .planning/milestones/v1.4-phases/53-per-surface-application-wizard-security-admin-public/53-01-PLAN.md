---
phase: 53-per-surface-application-wizard-security-admin-public
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/loading.tsx
  - src/app/(dashboard)/strategies/new/wizard/loading.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/error.tsx
  - src/app/(dashboard)/strategies/new/wizard/error.test.tsx
  - src/app/(dashboard)/strategies/error.tsx
  - src/app/(dashboard)/strategies/error.test.tsx
autonomous: true
requirements: [STATE-05, BP-02]
must_haves:
  truths:
    - "The wizard route shows a WizardChrome-shaped skeleton (stepper rail + first-step field block) instead of a blank flash before WizardClient hydrates"
    - "A server-prep error in the wizard route renders a digest-only error boundary with a working retry, never the raw error message"
    - "The strategies list route has an error boundary (it had loading.tsx but no error.tsx)"
    - "The existing wizard behavioral guards (transitions / autosave / finalize POST / step-enum validation) are green and pinned as the frozen-behavior baseline for Plan 02"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/loading.tsx"
      provides: "WizardChrome-shaped Suspense fallback skeleton with sr-only role=status liveness"
      contains: "role=\"status\""
    - path: "src/app/(dashboard)/strategies/new/wizard/error.tsx"
      provides: "wizard server-prep error boundary (unstable_retry, digest-only)"
      contains: "unstable_retry"
    - path: "src/app/(dashboard)/strategies/error.tsx"
      provides: "strategies-list error boundary"
      contains: "unstable_retry"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/error.tsx"
      to: "error.digest"
      via: "render digest only, never error.message"
      pattern: "error\\.digest"
    - from: "src/app/(dashboard)/strategies/new/wizard/loading.tsx"
      to: "@/components/ui/Skeleton"
      via: "assemble skeleton from primitives"
      pattern: "Skeleton"
---

<objective>
Add the missing route-state files for the wizard surface (STATE-05) and PIN the wizard behavioral
guard as the frozen-behavior baseline that Plan 02's UX upgrade must keep green. This is the
state-coverage half of the wizard wave; Plan 02 (Wave 2) does the additive UX upgrade on top.

Purpose: Eliminate the blank-flash gap during server-prep before WizardClient hydrates, and give the
wizard subtree a digest-only error boundary — without touching the WizardClient state machine. Pinning
the existing guards first makes Plan 02's "did I break a transition / autosave / the POST contract?"
question falsifiable on every run.
Output: 3 new route-state files (`wizard/loading.tsx`, `wizard/error.tsx`, `strategies/error.tsx`) +
their render tests; verified-green baseline run of the 3 existing wizard behavioral guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-PATTERNS.md

<interfaces>
<!-- Exact analogs the executor copies from. Read these FIRST; do not explore further. -->

Skeleton primitives — src/components/ui/Skeleton.tsx:
  export function Skeleton({ className }: { className?: string })   // animate-pulse rounded bg-border/60, aria-hidden
  export function SkeletonText({ lines }: { lines: number })
  export function SkeletonCard()

Error-boundary signature (Next 16.2.0) — src/app/strategy/[id]/error.tsx:
  "use client"; default export ({ error, unstable_retry }:
    { error: Error & { digest?: string }; unstable_retry: () => void })
  // renders digest ONLY (never error.message); console.error in useEffect; <Button onClick={() => unstable_retry()}>

Loading-fallback liveness — src/app/factsheet/[id]/v2/loading.tsx:63-66:
  <p className="sr-only" role="status" aria-live="polite">Loading factsheet — computing analytics.</p>

WizardChrome measure — src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx:105:
  mx-auto max-w-3xl px-6 py-10   // the wizard loading skeleton must match this measure

Error-test precedent — src/app/strategy/[id]/v2/error.test.tsx (render heading+body+CTA, retry invokes unstable_retry, digest shown)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the wizard behavioral guards as the frozen-behavior baseline</name>
  <files>(no files created — runs WizardClient.test.tsx, localStorage.test.ts, finalize-wizard/route.test.ts)</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx (transitions + autosave assertions)
    - src/lib/wizard/localStorage.test.ts (step-enum validation / validSteps)
    - src/app/api/strategies/finalize-wizard/route.test.ts (POST body shape)
    - .planning/phases/53-per-surface-application-wizard-security-admin-public/53-VALIDATION.md (Wave 0: pin guards FIRST)
  </read_first>
  <action>
    Run the three existing wizard behavioral guards and confirm green, establishing the FROZEN-behavior
    baseline that Plan 02 (review step + inline validation + primitive migration) must keep green per
    RESEARCH Pitfall 3. Do NOT add a new git-delta guard — Phase 53 deliberately edits WizardClient, so
    the invariant is BEHAVIOR (transitions / autosave / POST body / step-enum validation), proven by these
    suites. Record the exact pass output in the SUMMARY so Plan 02 has a known-good reference. If any guard
    is red on current main, STOP and report — the baseline must be green before the wizard is touched.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx" "src/lib/wizard/localStorage.test.ts" "src/app/api/strategies/finalize-wizard/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - All three suites pass (exit 0) on current main with no skipped tests.
    - SUMMARY records the pass and names them as the Plan-02 frozen-behavior baseline.
  </acceptance_criteria>
  <done>The three wizard behavioral guards are confirmed green and recorded as Plan 02's baseline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add wizard loading.tsx + error.tsx (RSC fallback + digest-only boundary)</name>
  <files>
    src/app/(dashboard)/strategies/new/wizard/loading.tsx
    src/app/(dashboard)/strategies/new/wizard/loading.test.tsx
    src/app/(dashboard)/strategies/new/wizard/error.tsx
    src/app/(dashboard)/strategies/new/wizard/error.test.tsx
  </files>
  <read_first>
    - src/app/factsheet/[id]/v2/loading.tsx (skeleton fidelity bar + sr-only role=status)
    - src/app/(dashboard)/strategies/loading.tsx (cards-grid primitive-assembled idiom)
    - src/app/strategy/[id]/error.tsx (exact error.tsx shape + doc-comment rationale to mirror)
    - src/app/strategy/[id]/v2/error.test.tsx (the render-test precedent)
    - src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx (line 105 measure; stepper-rail structure to skeleton-mirror)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (unstable_retry Props; RSC message-stripping)
  </read_first>
  <behavior>
    - loading.tsx renders a WizardChrome-shaped skeleton (a 4-cell stepper-rail placeholder + a first-step
      field-block placeholder) at the `mx-auto max-w-3xl px-6 py-10` measure, assembled from Skeleton
      primitives, with a closing `sr-only role="status" aria-live="polite"` "Loading strategy setup." node.
    - loading.test.tsx asserts the role=status liveness node exists and at least the stepper-rail anchor
      structure renders (smoke render — skeleton has no logic).
    - error.tsx is `"use client"`, accepts `{ error, unstable_retry }`, renders the "Something went wrong"
      heading + body copy + a "Try again" Button wired to `unstable_retry()`, and renders `Error ID: {digest}`
      ONLY when `error.digest` exists — never `error.message`. `console.error("[wizard-error]", error)` in useEffect.
      The fallback Link target is `/strategies` (the surface's own list).
    - error.test.tsx asserts: heading+body render; clicking "Try again" calls `unstable_retry` once;
      digest renders when present; `error.message` text never appears in the DOM.
  </behavior>
  <action>
    Create `wizard/loading.tsx` as an RSC (no "use client") modeled on the factsheet bar but shaped to the
    WizardChrome stepper rail + first-step panel (53-UI-SPEC §Dominant visual anchor → wizard). A single
    `animate-pulse` on the shell wrapper is the sanctioned idiom (Skeleton already pulses; the anti-pattern
    is bespoke per-element pulse divs). Create `wizard/error.tsx` mirroring `strategy/[id]/error.tsx`
    verbatim in shape, changing only the console tag to `[wizard-error]` and the fallback Link to
    `/strategies`; copy the UI-SPEC Copywriting strings ("Something went wrong" + body + "Error ID:
    {digest}"). Co-locate both render tests in the SAME change (coverage ratchet is a blocking gate). Keep
    server-fetch in `page.tsx` — do NOT move auth/data into `(dashboard)/layout.tsx` (RESEARCH Pitfall 5).
    Do NOT touch WizardClient, WizardChrome behavior, or any step file.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/loading.test.tsx" "src/app/(dashboard)/strategies/new/wizard/error.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="status"' "src/app/(dashboard)/strategies/new/wizard/loading.tsx"` ≥ 1.
    - `grep -c 'unstable_retry' "src/app/(dashboard)/strategies/new/wizard/error.tsx"` ≥ 1.
    - `grep -c 'error\.message' "src/app/(dashboard)/strategies/new/wizard/error.tsx"` == 0.
    - error.tsx contains `error.digest` gate; console tag is `[wizard-error]`.
    - Both render tests pass; the message-never-rendered assertion is present in error.test.tsx.
    - WizardClient.tsx / WizardChrome.tsx / steps/* are byte-unchanged (git diff shows only the 4 new files).
  </acceptance_criteria>
  <done>Wizard route shows a WizardChrome-shaped skeleton on slow nav and a digest-only retry boundary on server-prep error; both tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add strategies-list error.tsx</name>
  <files>
    src/app/(dashboard)/strategies/error.tsx
    src/app/(dashboard)/strategies/error.test.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/error.tsx (canonical shape)
    - src/app/strategy/[id]/error.tsx (subtree precedent)
    - src/app/strategy/[id]/v2/error.test.tsx (render-test precedent)
    - .planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md (Route-state matrix: strategies list has loading.tsx but ❌ error.tsx)
  </read_first>
  <behavior>
    - error.tsx is `"use client"` with `{ error, unstable_retry }`, renders the standard "Something went
      wrong" heading + body + "Try again" Button → `unstable_retry()`, digest-only, console tag
      `[strategies-error]`.
    - error.test.tsx asserts heading+body, retry-invokes-once, digest-when-present, message-never-rendered.
  </behavior>
  <action>
    Create `strategies/error.tsx` mirroring the same digest-only error-boundary shape as Task 2 (console
    tag `[strategies-error]`, fallback Link to `/discovery/crypto-sma` or `/strategies/new`). Co-locate the
    render test. The strategies list already has `strategies/loading.tsx` — do NOT modify it.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/error.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'unstable_retry' "src/app/(dashboard)/strategies/error.tsx"` ≥ 1.
    - `grep -c 'error\.message' "src/app/(dashboard)/strategies/error.tsx"` == 0.
    - Render test passes incl. the message-never-rendered assertion.
  </acceptance_criteria>
  <done>The strategies list route has a tested digest-only error boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC server-prep → client error boundary | A thrown server-prep error (digest) crosses into the new `error.tsx` client component |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Information Disclosure | new `error.tsx` files (wizard, strategies) | mitigate | Render `error.digest` ONLY; never `error.message`. Next strips RSC error messages in prod; mirror `strategy/[id]/error.tsx`. Enforced by the `grep error.message == 0` acceptance gate + the message-never-rendered test assertion. (ASVS V7) |
| T-53-02 | Tampering | WizardClient state machine / finalize POST | mitigate | This plan adds route-state files ONLY and touches no WizardClient/step code; Task 1 pins the behavioral guards green so any accidental disturbance is caught. The finalize-wizard POST validation stays authoritative. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | Phase installs zero packages (RESEARCH Package Legitimacy Audit: N/A). No new dependency surface. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/loading.test.tsx" "src/app/(dashboard)/strategies/new/wizard/error.test.tsx" "src/app/(dashboard)/strategies/error.test.tsx"` — all green.
- `npm run lint` — route-contract guard stays green (new loading.tsx/error.tsx are not page.tsx → no manifest entry needed; RESEARCH Pitfall 8).
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx" "src/lib/wizard/localStorage.test.ts" "src/app/api/strategies/finalize-wizard/route.test.ts"` — still green (frozen-behavior baseline intact).
</verification>

<success_criteria>
- Wizard route has a WizardChrome-shaped `loading.tsx` (sr-only role=status) and a digest-only `error.tsx` (unstable_retry).
- Strategies list route has a digest-only `error.tsx`.
- No `error.message` rendered in any new error boundary (grep == 0 + test assertion).
- The three wizard behavioral guards are green and recorded as Plan 02's baseline.
- Route-contract guard + PUBLIC_ROUTES unchanged and green.
</success_criteria>

<output>
Create `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-01-SUMMARY.md` when done
</output>
