---
phase: 53-per-surface-application-wizard-security-admin-public
plan: 06
type: execute
wave: 3
depends_on: [53-04, 53-05, 53-02]
files_modified:
  - src/components/layout/DashboardChrome.tsx
  - src/components/layout/DashboardChrome.test.tsx
  - eslint.config.mjs
autonomous: true
requirements: [APPLY-04, BP-02]
must_haves:
  truths:
    - "Admin and /portfolios fluid-fill to max-w-[1920px] then center (added to DashboardChrome.isWide), while wizard/auth/marketing stay narrow and the admin match-detail full-bleed carve-out is unaffected"
    - "DashboardChrome.test.tsx asserts admin + /portfolios get max-w-[1920px]; a still-narrow route (e.g. /strategies) holds the not-widened assertion"
    - "The admin + portfolios surface globs' no-raw-font-px lint is error and greps clean (the EmptyStateCard shared-primitive boundary is resolved explicitly)"
  artifacts:
    - path: "src/components/layout/DashboardChrome.tsx"
      provides: "isWide allow-list extended with admin|portfolios"
      contains: "admin|portfolios"
  key_links:
    - from: "src/components/layout/DashboardChrome.tsx isWide"
      to: "max-w-[1920px] vs max-w-7xl ternary"
      via: "regex allow-list selects the content-container measure"
      pattern: "isWide.*1920|max-w-\\[1920px\\]"
    - from: "eslint.config.mjs error block"
      to: "admin + portfolios globs"
      via: "no-raw-font-px error after both surfaces grep clean"
      pattern: "admin|portfolios"
---

<objective>
The consolidated wiring change for the two data surfaces (APPLY-04): raise admin + /portfolios to fluid-fill
~1920 via the ONE `DashboardChrome.isWide` allow-list edit (+ flip the paired test assertions), and ratchet
the admin + portfolios no-raw-font-px globs to `error` now that Plans 04/05 have migrated their type.

Purpose: This is the single wiring point Pattern 1 identifies — a page-level `max-w-[1920px]` wrapper is
clamped by the shell's `max-w-7xl`, so the allow-list is the only mechanism that actually fluid-fills past
1280. Consolidating both surfaces' width raise + both eslint flips into one plan keeps the shared
`DashboardChrome` and `eslint.config.mjs` files owned by exactly one plan (no same-wave conflict).
Output: `DashboardChrome.isWide` extended; its test flipped; admin + portfolios eslint globs at error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-PATTERNS.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-04-SUMMARY.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-05-SUMMARY.md

<interfaces>
<!-- The exact edit sites verified on disk. -->

DashboardChrome.tsx:72 (CURRENT):
  const isWide = /^\/(allocations|compare|discovery)(\/|$)/.test(pathname);
→ PHASE 53: add admin|portfolios:
  const isWide = /^\/(allocations|compare|discovery|admin|portfolios)(\/|$)/.test(pathname);
  // ternary at :166-170 → isWide ? "max-w-[1920px]" : "max-w-7xl"
  // comment at :64-71 currently says the Phase-53 surfaces "keep max-w-7xl" — update it.
  // LEAVE the isFullBleed carve-out /^\/admin\/match\/[^/]+\/?$/ (:61-62) — different branch, unaffected.

DashboardChrome.test.tsx:232-236 (CURRENT — asserts /portfolios stays max-w-7xl):
  expect(container).toHaveClass("max-w-7xl");          → FLIP to "max-w-[1920px]"
  expect(container).not.toHaveClass("max-w-[1920px]"); → FLIP to .not.toHaveClass("max-w-7xl")
  // add a parallel admin assertion; retarget the not-widened negative case to /strategies (stays narrow).
  // keep the /discoveryx regex-boundary test (:238-243) green.

eslint.config.mjs: repo-wide warn (:82); the error ratchet block files: array (:108-150).
  EmptyStateCard.tsx:27 carries text-[11px] (shared primitive) — decide: migrate it OR scope the admin/
  portfolios globs to exclude src/components/ui/EmptyStateCard.tsx. State the decision explicitly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin + portfolios to DashboardChrome.isWide and flip the test</name>
  <files>
    src/components/layout/DashboardChrome.tsx
    src/components/layout/DashboardChrome.test.tsx
  </files>
  <read_first>
    - src/components/layout/DashboardChrome.tsx (:61-72 isFullBleed + isWide; :166-170 the measure ternary; :64-71 comment)
    - src/components/layout/DashboardChrome.test.tsx (:232-236 the /portfolios not-widened assertion; :238-243 the regex-boundary test)
    - .planning/phases/53-...-public/53-PATTERNS.md (§DashboardChrome.isWide — exact edit + caution on the match-detail carve-out)
  </read_first>
  <behavior>
    - isWide matches `/admin` and `/portfolios` (and their sub-paths) → the content container gets
      `max-w-[1920px]`; wizard/auth/marketing/strategies stay `max-w-7xl`.
    - The `isFullBleed` admin match-detail carve-out is unaffected (different branch).
    - DashboardChrome.test.tsx asserts: `/admin` container → `max-w-[1920px]`; `/portfolios` container →
      `max-w-[1920px]` (flipped); a still-narrow route (`/strategies`) holds the not-widened assertion
      (`max-w-7xl`, not `max-w-[1920px]`); the `/discoveryx` regex-boundary test stays green.
  </behavior>
  <action>
    Edit `DashboardChrome.tsx:72` to add `admin|portfolios` to the `isWide` regex exactly per PATTERNS;
    update the comment at :64-71 (it currently states the Phase-53 surfaces keep `max-w-7xl`). LEAVE the
    `isFullBleed` `/^\/admin\/match\/[^/]+\/?$/` carve-out untouched. Flip `DashboardChrome.test.tsx:232-236`
    for /portfolios, add a parallel /admin assertion, and retarget the negative not-widened case to
    `/strategies` (stays narrow). Tune admin/portfolios column behavior was handled in Plans 04/05; this
    plan is the width-raise wiring only.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/DashboardChrome.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "isWide = /\^.*admin.*portfolios" src/components/layout/DashboardChrome.tsx` matches (both added).
    - DashboardChrome.test.tsx: /admin AND /portfolios assert `max-w-[1920px]`; the not-widened assertion targets a still-narrow route; the regex-boundary test is green.
    - The `isFullBleed` match-detail carve-out regex is byte-unchanged.
  </acceptance_criteria>
  <done>Admin + /portfolios fluid-fill to 1920; the paired test asserts the widened + a still-narrow route; the full-bleed carve-out is intact.</done>
</task>

<task type="auto">
  <name>Task 2: Ratchet the admin + portfolios no-raw-font-px globs to error</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (:82 repo-wide warn; :108-150 the error ratchet block)
    - .planning/phases/53-...-public/53-PATTERNS.md (§eslint ratchet edit + Shared Patterns note: EmptyStateCard.tsx:27 text-[11px])
    - .planning/phases/53-...-public/53-04-SUMMARY.md + 53-05-SUMMARY.md (confirm admin + components/portfolio greps clean)
  </read_first>
  <action>
    Confirm `src/app/(dashboard)/admin/**`, `src/components/admin/**`, `src/app/(dashboard)/portfolios/**`,
    and `src/components/portfolio/**` grep clean of raw `text-[Npx]` (Plans 04/05 migrated them). Resolve the
    `EmptyStateCard.tsx:27` `text-[11px]` shared-primitive boundary EXPLICITLY: either (a) migrate that one
    site in `src/components/ui/EmptyStateCard.tsx` to `--text-micro`/`--text-caption` and include it, OR
    (b) keep the admin/portfolios globs scoped to NOT cover `src/components/ui/**` (since EmptyStateCard is a
    shared primitive, not an admin/portfolios surface file). Prefer (a) if it is a single clean site and
    no other shared-primitive raw-px is pulled in; state the chosen option and rationale in the SUMMARY.
    Then ADD the four (or three, if EmptyStateCard excluded) clean globs to the `error` ratchet block's
    `files:` array. Repo-wide stays `warn`. Do NOT attempt the repo-wide flip (Phase 54 BP-03).
  </action>
  <verify>
    <automated>grep -RnE "text-\[[0-9]+px\]" "src/app/(dashboard)/admin/" "src/components/admin/" "src/app/(dashboard)/portfolios/" "src/components/portfolio/" | grep -v '^\s*//' | wc -l | grep -qx '0' && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The admin + portfolios (+ their component) globs grep clean of raw `text-[Npx]` (excluding comments).
    - eslint.config.mjs `error` block includes the admin + portfolios globs; the EmptyStateCard boundary decision is recorded in the SUMMARY.
    - `npm run lint` passes (no-raw-font-px error on the new globs + route-contract guard green).
  </acceptance_criteria>
  <done>Admin + portfolios no-raw-font-px globs are at error and clean; the shared-primitive boundary is resolved explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | layout-measure + lint-config edits only; no data flow, input, or auth change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-15 | Tampering | isFullBleed match-detail carve-out | mitigate | The width-raise edits `isWide` only; the `isFullBleed` regex is left byte-unchanged (acceptance gate), preserving the match-detail page's distinct branch. |
| T-53-16 | (config integrity) | eslint ratchet scope | mitigate | Globs flipped to `error` only after grep-clean; repo-wide stays `warn` (no premature repo-wide flip that would red-CI Phase-54 orphans). |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run src/components/layout/DashboardChrome.test.tsx` — admin + portfolios widened; narrow route holds; regex-boundary green.
- `npm run lint` — no-raw-font-px error on admin + portfolios globs + route-contract guard green.
- `grep -RnE "text-\[[0-9]+px\]" admin/portfolios globs` (excluding comments) == 0.
</verification>

<success_criteria>
- `DashboardChrome.isWide` includes admin + portfolios; test asserts both at `max-w-[1920px]` + a still-narrow route; full-bleed carve-out intact.
- Admin + portfolios no-raw-font-px globs at error and clean; EmptyStateCard shared-primitive boundary resolved explicitly.
- `npm run lint` green.
</success_criteria>

<output>
Create `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-06-SUMMARY.md` when done
</output>
