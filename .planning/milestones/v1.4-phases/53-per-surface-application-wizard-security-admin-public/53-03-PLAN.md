---
phase: 53-per-surface-application-wizard-security-admin-public
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/security/page.tsx
  - src/app/(marketing)/page.tsx
  - src/app/(marketing)/for-quants/page.tsx
  - src/app/(marketing)/demo/page.tsx
  - src/app/(marketing)/demo/founder-view/page.tsx
  - src/app/(marketing)/legal/disclaimer/page.tsx
  - src/app/(marketing)/legal/privacy/page.tsx
  - src/app/(marketing)/legal/terms/page.tsx
  - eslint.config.mjs
autonomous: true
requirements: [APPLY-03, BP-02]
must_haves:
  truths:
    - "/security renders the evolved system end-to-end: named fluid --text-* tiers (no raw text-[Npx]), no-clip body, DESIGN.md-conformant"
    - "Marketing page bodies (home, for-quants, demo, demo/founder-view, legal/*) conform to the fluid tiers and no-clip; the P51 (marketing) shell/masthead/footer is untouched"
    - "The marketing + /security surface globs' no-raw-font-px lint is error and greps clean"
    - "/security, /demo, /for-quants, /legal stay in PUBLIC_ROUTES and the route-contract guard stays green (no route moved)"
  artifacts:
    - path: "src/app/(marketing)/security/page.tsx"
      provides: "/security body migrated to fluid tiers, no-clip, primitive-conformant"
      contains: "--text-"
  key_links:
    - from: "src/app/(marketing)/security/page.tsx"
      to: "globals.css --text-* tiers"
      via: "named tier classes replace raw text-[Npx]"
      pattern: "--text-(page-title|h2|body|caption)"
    - from: "eslint.config.mjs error block"
      to: "src/app/(marketing)/security/** + body globs"
      via: "no-raw-font-px ratcheted to error after grep-clean"
      pattern: "marketing.*security|no-raw-font-px"
---

<objective>
Conform the prose/marketing surfaces (APPLY-03 /security + the remaining public page BODIES) to the
evolved system — fluid `--text-*` tiers, no-clip, primitive adoption — without touching the Phase-51
`(marketing)` shell, masthead, footer, or marketing copy/layout.

Purpose: /security and the public bodies are the lowest-risk surfaces (already in PUBLIC_ROUTES, static
RSC prose, no async data gap → no route-state files needed). They are a clean Wave-1 surface that ratchets
the marketing/security no-raw-font-px glob to error.
Output: migrated /security + marketing body type; no-clip fixes on in-scope marketing tile/description
sites; marketing+security eslint glob flipped to error.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-PATTERNS.md
@.planning/audits/truncation-audit.md

<interfaces>
<!-- Fluid tiers + no-clip analogs. The marketing category permits the ONE --text-h2 use. -->

Fluid type tiers (globals.css @theme, Phase 49) — prose/marketing category ≤ 4 tiers:
  --text-page-title (24→32 Instrument Serif) · --text-h2 (20→24 DM Sans 600 — MARKETING-ONLY) ·
  --text-body (14→16) · --text-caption (12→13).  --text-hero = landing home hero only.

No-clip treatments — src/components/ui/ScopedBanner.tsx:29-35:
  WRAP: className="break-words min-w-0"   (entity names in cards/headings)
  single-line + title=: <h3 className="truncate" title={fullText}>{fullText}</h3>  (only where alignment needs it)

PUBLIC_ROUTES — src/proxy.ts:17 ALREADY includes /security, /demo, /for-quants, /legal (do not move any route).
Route-contract guard scans only page.tsx — these pages stay page.tsx in place → no manifest change.

/demo resolves to src/app/(marketing)/demo/page.tsx (verified). The Meeting-hero "Verdict / Evidence /
Action" editorial layout is LOCKED — conform its type/no-clip, do NOT restructure it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate /security + marketing body type to fluid tiers and fix in-scope clips</name>
  <files>
    src/app/(marketing)/security/page.tsx
    src/app/(marketing)/page.tsx
    src/app/(marketing)/for-quants/page.tsx
    src/app/(marketing)/demo/page.tsx
    src/app/(marketing)/demo/founder-view/page.tsx
    src/app/(marketing)/legal/disclaimer/page.tsx
    src/app/(marketing)/legal/privacy/page.tsx
    src/app/(marketing)/legal/terms/page.tsx
  </files>
  <read_first>
    - src/app/(marketing)/security/page.tsx (27 raw text-[Npx] sites — the densest marketing migration)
    - src/components/ui/ScopedBanner.tsx (:29-35 break-words min-w-0 reference)
    - .planning/audits/truncation-audit.md (the classification SoT for any in-scope marketing tile/description clips)
    - .planning/phases/53-...-public/53-UI-SPEC.md (Typography per-surface subset: prose category; Truncation contract; "marketing copy FROZEN")
    - .planning/phases/53-...-public/53-PATTERNS.md (§type migration: marketing bodies 39 raw-px across 7 files; the six /security persistent-underline links are LOCKED accent)
  </read_first>
  <action>
    Migrate every raw `text-[Npx]`/`text-sm` on /security (27 sites) and the marketing page BODIES
    (home/for-quants/demo/founder-view/legal-*, ~39 sites across 7 files) to the named `--text-*` tiers,
    prose category: `--text-page-title` (page H1) / `--text-h2` (the ONE place section H2 is permitted) /
    `--text-body` / `--text-caption`; the home hero keeps `--text-hero` (landing-only). Fix the in-scope
    accidental clips per the truncation audit (marketing/demo description `line-clamp-2` sites, tile names)
    with WRAP (`break-words min-w-0`) or single-line + `title=` per the audit's per-site recommendation;
    NEVER relocate a clip (no new truncate/line-clamp without recovery). Do NOT touch the P51 `(marketing)`
    shell/masthead/`LegalFooter`, do NOT redesign marketing copy or the /demo Meeting-hero editorial layout,
    and PRESERVE the six /security persistent-underline body links (LOCKED accent, WCAG 1.4.1). No new font,
    no hardcoded hex in JSX.
  </action>
  <verify>
    <automated>grep -RnE "text-\[[0-9]+px\]|text-(xs|sm|base|lg|xl)\b" "src/app/(marketing)/security/" "src/app/(marketing)/page.tsx" "src/app/(marketing)/for-quants/" "src/app/(marketing)/demo/" "src/app/(marketing)/legal/" | grep -v '^\s*//' | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - 0 raw `text-[Npx]` (excluding comments) across /security + the named marketing body files.
    - In-scope audit clip sites carry `title=`/wrap; no new bare `truncate`/`line-clamp` without recovery.
    - The six /security persistent-underline links are intact (accent underline preserved).
    - The P51 `(marketing)` shell/masthead/`LegalFooter` files are byte-unchanged (git diff shows only page bodies + eslint).
    - No hardcoded hex in the touched JSX; ≤ 4 prose tiers + 2 weights.
  </acceptance_criteria>
  <done>/security and the marketing bodies render the fluid tiers with no-clip recovery; shell and copy untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Flip the marketing + /security no-raw-font-px glob to error</name>
  <files>eslint.config.mjs</files>
  <read_first>
    - eslint.config.mjs (:82 repo-wide warn; :104-150 the strangler error block)
    - .planning/phases/53-...-public/53-PATTERNS.md (§eslint ratchet edit; A4 (auth) is 0 raw-px)
  </read_first>
  <action>
    After Task 1 greps clean, ADD the migrated marketing/security globs
    (`src/app/(marketing)/security/**` and the marketing body globs — confirm `(auth)/**` is already 0
    raw-px and include it too since it needs no migration per PATTERNS A4) to the existing `error` ratchet
    block's `files:` array in eslint.config.mjs. The repo-wide level stays `warn`. Do NOT attempt the
    repo-wide flip (Phase 54 BP-03). Do NOT include the marketing masthead/footer shared shell files if
    they would re-introduce debt — scope the glob to the page bodies. Verify the marketing/security globs
    grep clean BEFORE flipping (a leftover raw-px on a migrated glob is a contract violation).
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - eslint.config.mjs `error` block includes the migrated marketing/security (+ auth) globs.
    - `npm run lint` passes (no-raw-font-px error on the new globs + route-contract guard green).
    - /security, /demo, /for-quants, /legal still present in `src/proxy.ts` PUBLIC_ROUTES (unchanged).
  </acceptance_criteria>
  <done>The marketing/security/auth surface globs are ratcheted to no-raw-font-px error and lint is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → marketing/security pages | unauthenticated routes; already in PUBLIC_ROUTES |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-06 | Elevation of Privilege | route move / PUBLIC_ROUTES drift | mitigate | Phase 53 moves NO route; /security,/demo,/for-quants,/legal stay in PUBLIC_ROUTES and are page.tsx in place → the route-contract guard (scans only page.tsx) stays green. `npm run lint` gate (#512 regression class). |
| T-53-07 | Information Disclosure | no-clip `title=` on marketing | accept | Marketing bodies render only already-public content; `title=` exposes nothing gated. Low-value public surface. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `grep -RnE "text-\[[0-9]+px\]" "src/app/(marketing)/security/" "src/app/(marketing)/"` (excluding comments) == 0 on the migrated globs.
- `npm run lint` — no-raw-font-px error on the new globs + route-contract guard + PUBLIC_ROUTES check green.
- `npx playwright test e2e/reflow-sweep-authed.spec.ts -g "security"` — /security row (320) green (no horizontal overflow).
</verification>

<success_criteria>
- /security + marketing bodies migrated to the fluid tiers (0 raw text-[Npx]); in-scope clips recovered.
- Marketing/security/auth no-raw-font-px glob is error and clean.
- P51 shell, masthead, footer, marketing copy, and /demo Meeting-hero layout untouched.
- No route moved; PUBLIC_ROUTES + route-contract guard green.
</success_criteria>

<output>
Create `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-03-SUMMARY.md` when done
</output>
