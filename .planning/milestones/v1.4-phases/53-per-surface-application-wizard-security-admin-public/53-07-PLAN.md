---
phase: 53-per-surface-application-wizard-security-admin-public
plan: 07
type: execute
wave: 4
depends_on: [53-02, 53-03, 53-04, 53-05, 53-06]
files_modified:
  - .planning/phases/53-per-surface-application-wizard-security-admin-public/53-CONFORMANCE.md
autonomous: false
requirements: [BP-02]
must_haves:
  truths:
    - "Each Phase-53 surface (wizard, /security + marketing, admin, /portfolios, auth) passes the 7-point DESIGN.md-conformance check (type/no-clip/color/spacing/state/responsiveness/boundaries)"
    - "The full unit suite, e2e specs, and the coverage ratchet are green; the frozen-spine guard confirms the math islands are untouched"
    - "proxy.ts PUBLIC_ROUTES + the route-contract guard are green for every touched route"
    - "The admin ultra-wide e2e gap is documented (proven via component Vitest + conformance, not the allocator-seeded sweep; admin-seeded e2e row deferred to Phase 54)"
  artifacts:
    - path: ".planning/phases/53-per-surface-application-wizard-security-admin-public/53-CONFORMANCE.md"
      provides: "per-surface 7-point conformance results + the documented admin-e2e gap"
      min_lines: 30
  key_links:
    - from: "53-CONFORMANCE.md"
      to: "each in-scope surface"
      via: "7-point DESIGN.md-conformance pass per surface"
      pattern: "PASS|FAIL"
---

<objective>
The phase exit gate (BP-02): run the per-surface DESIGN.md-conformance check across all five surface
groups, run the full test/coverage/e2e suite, confirm the frozen islands and routing guards are intact,
and document the admin ultra-wide e2e gap. This is the "no two apps" guard — it certifies every surface
conforms to the same evolved system before `/gsd:verify-work`.

Purpose: A human-verifiable conformance record that prevents legacy-vs-evolved drift and confirms the
milestone-wide invariants (math frozen, WCAG-AA floor, no-invented-data, PUBLIC_ROUTES) held through the
per-surface work.
Output: `53-CONFORMANCE.md` with a 7-point pass per surface + the documented admin-e2e gap; green full
suite + coverage; a human-verify checkpoint on the conformance verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-VALIDATION.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-02-SUMMARY.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-03-SUMMARY.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-04-SUMMARY.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-05-SUMMARY.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-06-SUMMARY.md

<interfaces>
<!-- The 7-point conformance exit (53-UI-SPEC §Per-Surface Conformance Exit) + the gate commands. -->

7-point per-surface check (BP-02): 1 Type (named tiers, glob error, ≤4 tiers/2 weights) · 2 No-clip
(audit clips recovered, none relocated, legit clips intact) · 3 Color (no hex in JSX, accent reserved-for,
empty neutral) · 4 Spacing (4px ladder + documented exceptions, 44px targets) · 5 State (loading/error
where required, honest degenerate, no blank flash, no fabricated data) · 6 Responsiveness (320→2560 no
clip/overlap/h-scroll, @container parent/child, tabular-nums) · 7 Boundaries (no frozen island RSC-ified,
PUBLIC_ROUTES + route-contract guard green, coverage green).

Gate commands:
  npm run test            (full unit)
  npm run test:e2e        (wizard-axe + reflow-sweep-authed; admin EXCLUDED — Pitfall 7)
  npm run test:coverage   (the BLOCKING frontend-coverage gate: lines 82/stmts 80/fns 74/branches 72)
  npm run lint            (no-raw-font-px error on all migrated globs + route-contract guard + PUBLIC_ROUTES)
  npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts   (math islands zero-diff)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the full gate suite + per-surface DESIGN.md-conformance and write 53-CONFORMANCE.md</name>
  <files>.planning/phases/53-per-surface-application-wizard-security-admin-public/53-CONFORMANCE.md</files>
  <read_first>
    - .planning/phases/53-...-public/53-UI-SPEC.md (§Per-Surface Conformance Exit — the 7 points; §Checker Sign-Off dimensions)
    - DESIGN.md (the conformance authority: tiers, accent reserved-for, 4px ladder + exceptions, 2 weights)
    - .planning/phases/53-...-public/53-VALIDATION.md (Manual-Only: admin ultra-wide gap; Sampling Rate)
    - all five 53-0{2..6}-SUMMARY.md (per-surface results to roll up)
  </read_first>
  <action>
    Run the full gate suite (`npm run test`, `npm run test:e2e`, `npm run test:coverage`, `npm run lint`,
    and `phase-52-frozen-spine-guards.test.ts`). For EACH surface group (wizard, /security + marketing,
    admin, /portfolios, auth), evaluate the 7-point DESIGN.md-conformance check (53-UI-SPEC §Per-Surface
    Conformance Exit) and record PASS/FAIL per point with the evidence (test name / grep result / spec row).
    Apply react-best-practices + frontend-design judgment to the touched files and note any AI-slop patterns
    found+removed. Explicitly DOCUMENT the admin ultra-wide e2e gap: admin responsiveness is proven via the
    component Vitest structural tests (Plan 05) + this conformance check, NOT the allocator-seeded
    reflow-sweep (Pitfall 7 — the seed stamps role='allocator' and admin redirects non-admins → false-green);
    an admin-seeded e2e reflow row is a Phase-54 hermetic-seed concern. Write all results to
    `53-CONFORMANCE.md`. If ANY point fails, STOP and report which surface/point — do not paper over a gap.
  </action>
  <verify>
    <automated>npm run test && npm run test:coverage && npm run lint && npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test`, `npm run test:coverage`, `npm run lint`, and the frozen-spine guard all pass (exit 0).
    - `npm run test:e2e` (wizard-axe both branches incl. review step + reflow-sweep-authed 320/2560 + /security) green; admin row absent by design (documented).
    - 53-CONFORMANCE.md records a 7-point PASS for every surface group with evidence; any FAIL is surfaced, not hidden.
    - The admin ultra-wide e2e gap is documented (component Vitest + conformance proof; Phase-54 deferral).
    - No frozen island RSC-ified; PUBLIC_ROUTES + route-contract guard green; coverage ratchet green.
  </acceptance_criteria>
  <done>The full gate suite is green and 53-CONFORMANCE.md certifies every surface conforms, with the admin-e2e gap documented.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the per-surface conformance verdict</name>
  <what-built>
    All five Phase-53 surfaces conformed to the evolved system (fluid tiers, no-clip, @container, route-state
    files), the wizard UX upgrade (review step + inline validation), admin/portfolios fluid-fill to 1920, and
    a 7-point per-surface DESIGN.md-conformance record in 53-CONFORMANCE.md. Full unit/e2e/coverage/lint +
    frozen-spine guard are green.
  </what-built>
  <how-to-verify>
    1. Read `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-CONFORMANCE.md` —
       confirm a 7-point PASS per surface and that the admin ultra-wide e2e gap is documented (not silently absent).
    2. Run the dev server and visit, resizing the browser 320px → 2560px (use responsive mode):
       `/strategies/new/wizard` (and `?source=csv`) — confirm the review step recaps only entered values,
       inline errors appear at the field on blur, the envelope stays the summary; no clip/overlap at any width.
    3. Visit `/admin` and `/portfolios` — confirm they fluid-fill toward 1920 then center (not a 2-col table
       marooned in whitespace); tables reshape without breaking number alignment; no clip on names/emails.
    4. Visit `/security` and a marketing body page — confirm fluid type, no clip, the six /security underline
       links intact, the P51 shell/masthead/footer unchanged.
    5. Trigger a slow nav / forced error on a route to see the skeleton + the digest-only error boundary
       (no raw error message).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any surface/point that fails conformance.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whole app → conformance gate | the phase exit verifies invariants held across all touched surfaces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-17 | Repudiation | silent conformance gap | mitigate | 53-CONFORMANCE.md records a per-point PASS/FAIL with evidence; the human-verify checkpoint blocks on a missing/false PASS. A FAIL stops the plan (no paper-over). |
| T-53-18 | Tampering | math islands / WCAG-AA floor | mitigate | `phase-52-frozen-spine-guards.test.ts` (scenario.ts/compute.ts/EquityChart zero-diff) + the axe specs (WCAG-AA) are part of the gate suite. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npm run test` + `npm run test:e2e` + `npm run test:coverage` + `npm run lint` all green.
- `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` — math islands untouched.
- 53-CONFORMANCE.md exists with a 7-point PASS per surface + the documented admin-e2e gap.
- Human-verify checkpoint approved.
</verification>

<success_criteria>
- Every surface group passes the 7-point DESIGN.md-conformance check (recorded with evidence).
- Full unit + e2e + coverage + lint + frozen-spine guard green.
- Admin ultra-wide e2e gap documented (component-proven; Phase-54 deferral).
- Human-verify checkpoint approved.
</success_criteria>

<output>
Create `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-07-SUMMARY.md` when done
</output>
