---
phase: 53-per-surface-application-wizard-security-admin-public
plan: 02
type: execute
wave: 2
depends_on: [53-01]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx
  - src/lib/wizard/localStorage.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ReviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ReviewStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx
  - eslint.config.mjs
  - e2e/wizard-axe.spec.ts
autonomous: true
requirements: [APPLY-02, BP-02]
must_haves:
  truths:
    - "The manager wizard shows a read-only Review & confirm step before finalize on BOTH branches (API + CSV), recapping only entered values, with an Edit affordance per section that returns to the owning step"
    - "Wizard field errors surface inline at the field on blur and on submit (via the Field primitive a11y), while the WizardErrorEnvelope stays the single role=alert summary"
    - "Wizard fields render the Phase-50 Field/Input/Select/Button primitives; the WizardChrome step structure is preserved"
    - "The wizard state machine transitions, autosave semantics, and finalize-wizard POST body are unchanged (the pinned behavioral guards stay green)"
    - "The wizard surface glob's no-raw-font-px lint is error and greps clean"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ReviewStep.tsx"
      provides: "read-only recap step (entered values only, per-section Edit, no role=alert)"
      min_lines: 40
    - path: "src/lib/wizard/localStorage.ts"
      provides: "WizardStepKey + validSteps extended with review / csv_review"
      contains: "review"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      to: "ReviewStep"
      via: "step === review / csv_review render branch + setStep transition from metadata"
      pattern: "review"
    - from: "wizard fields"
      to: "src/lib/wizardErrors.ts"
      via: "inline error surfaced through Field error prop (existing copy, never invented)"
      pattern: "wizardErrors|WizardErrorCopy"
    - from: "src/lib/wizard/localStorage.ts validSteps"
      to: "WizardStepKey union"
      via: "review/csv_review accepted by validation, safe-degrades unknown to SSR default"
      pattern: "validSteps"
---

<objective>
The genuinely-new wizard UX upgrade (APPLY-02): surface the existing wizard validation inline per-field,
add a read-only Review & confirm recap step before finalize on both branches, and migrate wizard fields
onto the Phase-50 Field/Input/Select/Button primitives — ADDITIVELY, with the WizardClient state machine,
autosave, and finalize POST contract FROZEN.

Purpose: A manager sees errors at the field (not only in the top banner) and confirms entered values
before creating a strategy — the one new UX area in this phase. Everything is additive surfacing + one
new render branch; no transition, autosave, or POST body changes.
Output: review step (`ReviewStep.tsx` + test) wired into both branches; inline validation via `Field`;
primitive-migrated step fields; `WizardStepKey`/`validSteps` extended; wizard eslint glob ratcheted to
`error`; wizard-axe extended to the review step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-PATTERNS.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-01-SUMMARY.md

<interfaces>
<!-- Real on-disk field names (PATTERNS Rule-7 correction wins over RESEARCH/UI-SPEC prose). -->

Field a11y wrapper — src/components/ui/Field.tsx (already wires aria for you):
  <Field label={string} hint?={string} error?={string}> {control} </Field>
  // wires control.id, aria-invalid (error ? "true" : undefined), aria-describedby=[hintId,errorId]
  // renders {error && <p id={errorId} className="text-caption text-negative">{error}</p>}
  // Field does NOT validate (ASVS V5) — consumer supplies the error string.

Wizard error copy — src/lib/wizardErrors.ts:
  type WizardErrorCopy = { title; cause; fix[]; docsHref?; actions? }   // :79-89 — NO human_message field
  // keyed by WizardErrorCode; inline message uses cause/title — NEVER a new string.
  // The role=alert summary (WizardErrorEnvelope → ErrorEnvelope) renders envelope.cause + envelope.fix.

STEP_INDEX — src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:57:
  const STEP_INDEX: Record<WizardStepKey, 1|2|3|4> = {
    connect_key:1, sync_preview:2, metadata:3, submit:4,
    csv_upload:1, csv_preview:2, csv_metadata:3, csv_submit:4 };
  // existing step render branches at :614/:621/:647/:662(submit)/:689/:722/:757/:797
  // metadata→submit transition currently at handleMetadataComplete (:415 setStep("submit"))

WizardChrome step arrays — WizardChrome.tsx:13 DEFAULT_STEPS, :28 CSV_STEPS;
  gridColsClass ternary :87-88 only handles 3-vs-4 (must gain a 5-col arm).

localStorage — src/lib/wizard/localStorage.ts:59 WizardStepKey union; :302 validSteps array; :312 includes-guard safe-degrade.

Recap data already in WizardClient state:
  API:  metadataDraft (name, description, categoryId, strategyTypes, subtypes, markets,
        supportedExchanges, leverageRange, aum, maxCapacity) + syncSnapshot
  CSV:  csvPreview (fmt, row_count, date_range — REAL parsed numbers)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the read-only Review & confirm step on both branches</name>
  <files>
    src/app/(dashboard)/strategies/new/wizard/steps/ReviewStep.tsx
    src/app/(dashboard)/strategies/new/wizard/steps/ReviewStep.test.tsx
    src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
    src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx
    src/lib/wizard/localStorage.ts
  </files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx (STEP_INDEX :57, render branches :614-831, handleMetadataComplete :412-423, submit branch :662-674)
    - src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx (:13 DEFAULT_STEPS, :28 CSV_STEPS, :87-88 gridColsClass)
    - src/lib/wizard/localStorage.ts (:59 WizardStepKey, :302 validSteps, :312 includes-guard)
    - src/lib/wizard/localStorage.test.ts (validSteps test to extend)
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx (the pinned transition/autosave guard — must stay green)
    - .planning/phases/53-...-public/53-UI-SPEC.md (Wizard UX Upgrade §3 + Copywriting: "Review & confirm" / body / "Edit")
  </read_first>
  <behavior>
    - ReviewStep renders a read-only recap of ENTERED values only (API: name/description/category/types/
      subtypes/markets/supportedExchanges/leverageRange/aum/maxCapacity from metadataDraft+syncSnapshot;
      CSV: fmt/row_count/date_range from csvPreview — real parsed numbers). Each row: --text-caption label
      + --text-body value (numbers Geist Mono tabular-nums). Hairline `border-t border-border` dividers,
      no card-on-card. NO role="alert".
    - Each recap section has an "Edit" affordance calling a passed `onEdit(step)` that maps to the owning step.
    - Final CTA is the existing finalize verb: "Create strategy" (API) / "Submit strategy" (CSV).
    - ReviewStep.test.tsx asserts: recap shows ONLY provided values (no fabricated value / em-dash / zero
      appears for an absent field); clicking a section "Edit" calls onEdit with the owning step key; the
      rendered subtree has NO role="alert"; the final CTA label matches the branch verb.
    - WizardClient adds `step === "review"` and `step === "csv_review"` render branches (mirror the existing
      submit branches); metadata now advances to review (review "Continue" advances to submit); STEP_INDEX
      value union widens to 1|2|3|4|5 with review:4/submit:5 (API) and csv_review:4/csv_submit:5 (CSV).
    - localStorage validSteps accepts "review"/"csv_review"; an unknown stored step still safe-degrades to
      the SSR default (existing includes-guard) — proven by the extended localStorage.test.ts.
  </behavior>
  <action>
    Create `steps/ReviewStep.tsx` as a "use client" read-only recap component (data passed as props from
    WizardClient state — do NOT re-fetch or fabricate). Assemble from the WizardChrome narrow measure +
    hairline-divider editorial idiom (no new primitive). Wire it into WizardClient as a NEW render branch
    plus ONE transition: change `handleMetadataComplete` to `setStep("review")` (and the CSV metadata→submit
    seam to `setStep("csv_review")`); review "Continue" → `setStep("submit")`/`setStep("csv_submit")`;
    review "Back"/Edit → `setStep(owningStep)` via the existing seam. Extend `STEP_INDEX` (PATTERNS edit
    site 1), `DEFAULT_STEPS`/`CSV_STEPS` + the `gridColsClass` 5-col arm (edit site 2), the `WizardStepKey`
    union (edit site 3), and the `validSteps` array (edit site 4) — per PATTERNS A3. SubmitStep/CsvSubmitStep
    still do the POST; do NOT touch finalize-wizard/csv-finalize. Do NOT change the transition functions'
    bodies beyond the metadata→review and review→submit re-pointing, autosave (`saveWizardState`/
    `loadWizardState`), or the POST body shape (RESEARCH Pitfall 3). NO data migration for stored drafts.
    The OKX passphrase field stays required; per-source schema unchanged.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ReviewStep.test.tsx" "src/lib/wizard/localStorage.test.ts" "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx" "src/app/api/strategies/finalize-wizard/route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - ReviewStep.test.tsx asserts no-fabricated-value, per-section Edit→onEdit(owningStep), no role="alert", correct CTA verb.
    - `grep -c '"review"\|"csv_review"' src/lib/wizard/localStorage.ts` covers BOTH the WizardStepKey union and validSteps.
    - WizardClient.test.tsx + finalize-wizard/route.test.ts + localStorage.test.ts all still GREEN (frozen behavior; the Plan-01 baseline holds).
    - `git diff src/app/api/strategies/finalize-wizard/` is EMPTY (POST contract untouched).
    - No fabricated value / count-up / demo number in ReviewStep (no-invented-data).
  </acceptance_criteria>
  <done>Both wizard branches show a read-only review step that recaps only entered values with per-section Edit; transitions/autosave/POST are provably unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface existing validation inline per-field + migrate fields to primitives</name>
  <files>
    src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
    src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx
    src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx
    src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx
  </files>
  <read_first>
    - src/components/ui/Field.tsx (:60-92 a11y wiring; consumer supplies the error string)
    - src/lib/wizardErrors.ts (:79-89 WizardErrorCopy { title, cause, fix[] }; per-code copy e.g. :99-110)
    - src/components/error/ErrorEnvelope.tsx (:113,122-125 — the role=alert summary renders cause+fix)
    - src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.tsx (already uses Input/Select — bring onto Field)
    - src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx (wires aria-invalid but NOT aria-describedby — the exact gap Field closes)
    - .planning/phases/53-...-public/53-UI-SPEC.md (Wizard UX Upgrade §1 + §2; Pitfall 6 double-announce)
  </read_first>
  <behavior>
    - Each migrated wizard field is wrapped in `Field` and passes the EXISTING wizardErrors copy
      (WizardErrorCopy.cause/title for that field's code) as `error` only when `blurred && fieldError`
      (on blur) and on submit. Field renders the message as `--text-caption text-negative` with
      aria-invalid + aria-describedby (announced when focus lands).
    - Per-field messages are NOT role="alert"; the WizardErrorEnvelope stays the single role="alert" summary
      rendering envelope.cause + envelope.fix.
    - On submit-with-errors, focus moves to the FIRST invalid field.
    - MetadataStep.test.tsx asserts: blur surfaces the wizardErrors string through Field (aria-invalid="true"
      + aria-describedby pointing at the message id); the per-field message node is NOT role="alert"; the
      message text equals the existing wizardErrors copy (not a new string).
  </behavior>
  <action>
    Migrate the wizard step fields to `Field`/`Input`/`Select`/`Button`/`Textarea` (strangler), surfacing
    the EXISTING `wizardErrors.ts` copy inline per PATTERNS §"Wizard inline per-field validation surfacing".
    Reuse `WizardErrorCopy.cause`/`title` for the field's error code — NEVER invent a new inline string
    (no-invented-data; copy-drift guard). Keep `WizardErrorEnvelope` as the role=alert summary (additive
    path; Pitfall 6 — do not mark per-field messages role=alert, do not remove the envelope). The migration
    swaps `<input>`→`<Input>`/`<Field>` ONLY — it MUST NOT change any `onChange`/`onBlur`/`onComplete`
    wiring or what a step's transition does (RESEARCH Pitfall 3). Keep the broker-selector grid + CSV
    escape-hatch card visual contracts (3-col broker grid; `border-accent bg-accent/5` active card;
    secondary "Upload CSV →" CTA). CsvUploadStep's missing `aria-describedby` is closed by `Field`.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - MetadataStep.test.tsx: blur sets aria-invalid="true" + aria-describedby on the control; the message id matches; the per-field message is NOT role="alert"; message text == the existing wizardErrors copy.
    - `grep -RnE "role=.alert" src/app/(dashboard)/strategies/new/wizard/steps/` returns no per-field alert (only WizardErrorEnvelope/CsvValidationEnvelope keep alert).
    - No new error string introduced (inline copy sourced from wizardErrors.ts WizardErrorCopy).
    - WizardClient.test.tsx still green (no transition re-wire).
  </acceptance_criteria>
  <done>Wizard fields surface existing validation inline on blur+submit through Field a11y, with the envelope still the lone role=alert summary, and render the Phase-50 primitives.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate wizard raw font-px, flip the wizard eslint glob to error, extend wizard-axe to review</name>
  <files>
    eslint.config.mjs
    e2e/wizard-axe.spec.ts
  </files>
  <read_first>
    - eslint.config.mjs (:82 repo-wide warn; :104-150 the Phase-52 strangler `error` block naming the Phase-53 surfaces)
    - e2e/wizard-axe.spec.ts (existing api + csv branch axe coverage to extend to the review step)
    - .planning/phases/53-...-public/53-PATTERNS.md (§Per-surface type migration: wizard = 22 raw text-[Npx] across 9 files; tier budget form: page-title/h3/body/caption)
  </read_first>
  <action>
    Migrate every raw `text-[Npx]` / `text-sm` on the wizard surface
    (`src/app/(dashboard)/strategies/new/**`, ~22 sites across the 9 step/chrome files incl. the new
    ReviewStep) to the named `--text-*` tiers (form category: `--text-page-title`/`--text-h3`/`--text-body`/
    `--text-caption`; `--text-micro` only for badge/broker-chip text). After grep proves the glob clean,
    ADD `"src/app/(dashboard)/strategies/new/**"` to the existing `error` ratchet block's `files:` array in
    eslint.config.mjs (repo-wide stays `warn`). Do NOT attempt the repo-wide flip (that is Phase 54 BP-03).
    Extend `e2e/wizard-axe.spec.ts` to walk to and scan the new review step on both branches (zero
    violations; WCAG-AA floor). Do NOT change font families (Instrument Serif titles / DM Sans UI / Geist
    Mono numbers).
  </action>
  <verify>
    <automated>grep -RnE "text-\[[0-9]+px\]|text-(xs|sm|base|lg|xl)\b" "src/app/(dashboard)/strategies/new/" | grep -v '^\s*//' | wc -l | grep -qx '0' && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnE "text-\[[0-9]+px\]" "src/app/(dashboard)/strategies/new/"` (excluding comments) == 0.
    - eslint.config.mjs `error` block includes `src/app/(dashboard)/strategies/new/**`.
    - `npm run lint` passes (no-raw-font-px error on the wizard glob + route-contract guard green).
    - `npx playwright test e2e/wizard-axe.spec.ts` covers the review step on api + csv branches, zero violations.
    - ≤ 4 active text tiers + 2 weights on the wizard surface.
  </acceptance_criteria>
  <done>Wizard surface is migrated onto the fluid tiers, its no-raw-font-px glob is error and clean, and wizard-axe covers the new review step at zero violations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client wizard form → finalize-wizard POST | manager-entered values validated server-side at finalize (authoritative) |
| stale browser tab (old WizardStepKey) → localStorage draft | a future-tab step value could be read by older code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Tampering | wizard inline validation vs finalize POST | mitigate | Inline validation is presentation-only (surfaces existing wizardErrors copy); the finalize-wizard POST validation stays authoritative and unchanged. Never gate the POST on client-only inline state. `git diff src/app/api/strategies/finalize-wizard/` empty + the pinned route.test.ts green. (ASVS V5) |
| T-53-04 | Tampering | WizardStepKey / validSteps / autosave | mitigate | Additive enum extension only; `validSteps.includes` safe-degrades an unknown stored step to the SSR default (no data migration). Proven by the extended localStorage.test.ts; WizardClient.test.tsx (autosave) stays green. |
| T-53-05 | Spoofing/Info | review recap (no-invented-data) | mitigate | Recap renders ONLY entered values from existing state; ReviewStep.test.tsx asserts no fabricated value/em-dash/zero for absent fields. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx" "src/lib/wizard/localStorage.test.ts" "src/app/api/strategies/finalize-wizard/route.test.ts"` — frozen-behavior baseline still green.
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ReviewStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/MetadataStep.test.tsx"` — new behavior green.
- `npm run lint` — wizard no-raw-font-px error + route-contract guard green.
- `npx playwright test e2e/wizard-axe.spec.ts` — zero axe violations incl. the review step.
- `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` — math islands untouched.
</verification>

<success_criteria>
- Read-only Review & confirm step on both branches; recap shows only entered values; per-section Edit returns to the owning step.
- Inline per-field validation on blur+submit via Field; envelope remains the single role=alert summary; submit focuses the first invalid field.
- Fields render Phase-50 primitives; WizardChrome step structure preserved.
- Transitions/autosave/finalize POST provably unchanged (pinned guards green; finalize diff empty).
- Wizard no-raw-font-px glob is error and grep-clean; wizard-axe covers the review step.
</success_criteria>

<output>
Create `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-02-SUMMARY.md` when done
</output>
