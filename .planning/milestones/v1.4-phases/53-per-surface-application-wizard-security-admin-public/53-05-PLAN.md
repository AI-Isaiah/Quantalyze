---
phase: 53-per-surface-application-wizard-security-admin-public
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/admin/loading.tsx
  - src/app/(dashboard)/admin/loading.test.tsx
  - src/app/(dashboard)/admin/error.tsx
  - src/app/(dashboard)/admin/error.test.tsx
  - src/components/admin/ComputeJobsTable.tsx
  - src/components/admin/ComputeJobsTable.test.tsx
  - src/components/admin/MatchQueueIndex.tsx
  - src/components/admin/MatchQueueIndex.test.tsx
  - src/components/admin/AllocatorMatchQueue.tsx
  - src/components/admin/AllocatorMatchQueue.test.tsx
  - src/app/(dashboard)/admin/partner-pilot/[partner_tag]/page.tsx
  # Task 3 type/raw-px migration also touches (scope honesty, grep-gated):
  # + ~13 src/components/admin/** files (66 raw-px sites)
  # + ~8 src/app/(dashboard)/admin/** page files (15 raw-px sites)
autonomous: true
requirements: [APPLY-04, STATE-05, BP-02]
must_haves:
  truths:
    - "Admin routes show a data-table-anchored skeleton instead of a blank flash, and a digest-only retry boundary on fetch error (shared admin/loading.tsx + admin/error.tsx)"
    - "Admin data tables (ComputeJobsTable, MatchQueueIndex, AllocatorMatchQueue) reshape via @container with the host on a parent and @-variants on child th/td (never same-element), preserving tabular-nums across breakpoints"
    - "Admin accidental clips are recovered: the partner-pilot email mid-clip recovers via title=, dense table name cells single-line+title= or wrap per the audit"
    - "Admin access gates are intact: !user redirect(/login) then !isAdminUser redirect(/discovery/crypto-sma)"
    - "Admin type is migrated onto the fluid tiers (raw text-[Npx] removed across admin pages + components/admin)"
  artifacts:
    - path: "src/app/(dashboard)/admin/loading.tsx"
      provides: "shared admin data-table-anchored skeleton with sr-only role=status"
      contains: "role=\"status\""
    - path: "src/app/(dashboard)/admin/error.tsx"
      provides: "shared admin digest-only error boundary"
      contains: "unstable_retry"
  key_links:
    - from: "src/components/admin/ComputeJobsTable.tsx"
      to: "ResponsiveTable @container host"
      via: "className=@container on host, @max-*/@min-* on child th/td"
      pattern: "@container"
    - from: "src/app/(dashboard)/admin/error.tsx"
      to: "error.digest"
      via: "render digest only, never error.message"
      pattern: "error\\.digest"
---

<objective>
Bring the admin surface (APPLY-04 + STATE-05) to the v1.4 bar: add the missing route-state files, reshape
the admin data tables via `@container` (parent/child host), recover the admin accidental clips, and migrate
admin type to the fluid tiers — WITHOUT removing any access gate. The fluid-fill width raise
(`DashboardChrome.isWide`) and the admin eslint ratchet flip are deferred to Plan 06 (Wave 2) to keep the
shared `DashboardChrome`/`eslint.config.mjs` files out of this wave.

Purpose: Admin is the highest-sensitivity in-scope surface (staff-facing, access-gated). Its responsiveness
is NOT e2e-provable via the allocator seed (Pitfall 7) — prove via component Vitest + the conformance check.
Output: shared `admin/loading.tsx`+`admin/error.tsx` (+ per-sub-page only where layout differs); `@container`
on the three admin tables with structural tests; admin clip fixes; admin type migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-PATTERNS.md
@.planning/audits/truncation-audit.md

<interfaces>
<!-- The @container idiom (parent/child host) + skeleton/error analogs + access-gate shape. -->

@container host idiom — src/components/strategy/StrategyTable.tsx:516-520 + src/components/ResponsiveTable.tsx:55-63:
  <ResponsiveTable label="…" className="@container" scrollRef={ref}>   // PARENT host (overflow-x-auto div carries @container)
    <table>
      <th className={`… ${col.collapse ? "@max-3xl:hidden" : ""}`}>   // CHILD variants — NEVER same element as host
      <td className="… font-metric tabular-nums @max-3xl:hidden">{value}</td>
  // ⚠️ #551: same-element host never matches, freezes grid 1-wide. jsdom class-string FALSE-PASSES.
  // Verify with a parent/child STRUCTURAL assertion, not a class-string check.

Skeleton bar — src/app/factsheet/[id]/v2/loading.tsx (header rule + N rows + sr-only role=status).
Error shape — src/app/(dashboard)/error.tsx / src/app/strategy/[id]/error.tsx (unstable_retry, digest-only).
Error-test precedent — src/app/strategy/[id]/v2/error.test.tsx.

Access gate (PRESERVE) — src/app/(dashboard)/admin/page.tsx:16-18:
  const { data:{user} } = await supabase.auth.getUser();
  if (!user) redirect("/login");
  if (!(await isAdminUser(supabase, user))) redirect("/discovery/crypto-sma");
  // fetch is in the PAGE body → loading.tsx WILL render (Pitfall 5). Do NOT move fetch into layout.

Admin tables: ComputeJobsTable.tsx ALREADY imports ResponsiveTable; MatchQueueIndex.tsx (raw <table> :244)
and AllocatorMatchQueue.tsx (raw <table> :521/:623/:672) need a containment host added.

Empty state — src/components/ui/EmptyStateCard.tsx ({ heading, body }, neutral, NO role=alert).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared admin loading.tsx + error.tsx (+ per-sub-page only where layout differs)</name>
  <files>
    src/app/(dashboard)/admin/loading.tsx
    src/app/(dashboard)/admin/loading.test.tsx
    src/app/(dashboard)/admin/error.tsx
    src/app/(dashboard)/admin/error.test.tsx
  </files>
  <read_first>
    - src/app/factsheet/[id]/v2/loading.tsx (skeleton bar fidelity + sr-only role=status)
    - src/app/(dashboard)/error.tsx (canonical error shape)
    - src/app/strategy/[id]/v2/error.test.tsx (render-test precedent)
    - src/app/(dashboard)/admin/page.tsx (access gate + page-body fetch — do NOT move to layout)
    - .planning/phases/53-...-public/53-UI-SPEC.md (Route-state matrix admin rows; "shared covers most; per-page only where layout differs"; Dominant anchor = data table)
  </read_first>
  <behavior>
    - admin/loading.tsx (RSC, no "use client") renders a data-table-anchored skeleton: a page-title Skeleton
      + a `border border-border bg-surface` block with a header rule + N (~8) Skeleton rows, closed by an
      `sr-only role="status" aria-live="polite"` "Loading admin." node. The shell does NOT re-impose
      `max-w-7xl` (DashboardChrome owns the measure once Plan 06 widens it).
    - admin/error.tsx ("use client", `{ error, unstable_retry }`) renders "Something went wrong" + body +
      "Try again"→`unstable_retry()`, `Error ID: {digest}` ONLY when digest exists, console `[admin-error]`.
    - admin/loading.test.tsx asserts role=status node + the table-anchor structure render.
    - admin/error.test.tsx asserts heading+body, retry-invokes-once, digest-when-present, message-NEVER-rendered.
  </behavior>
  <action>
    Create the SHARED `admin/loading.tsx` + `admin/error.tsx` modeled on the factsheet bar + canonical
    error shape (53-UI-SPEC: a shared pair covers most sub-pages; generic Skeleton acceptable for admin
    internal pages). Add a per-sub-page `loading.tsx` ONLY where a sub-page's layout differs enough to
    warrant a distinct match-layout skeleton (Claude's discretion; the match-detail full-bleed page is a
    candidate). Co-locate render tests in the SAME change (coverage gate). Keep server-fetch in each
    `page.tsx`; do NOT move auth/fetch into `(dashboard)/layout.tsx` (Pitfall 5). PRESERVE every admin
    access gate exactly (`redirect("/login")` then `isAdminUser` redirect) — a restyle MUST NOT remove a gate.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/admin/loading.test.tsx" "src/app/(dashboard)/admin/error.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="status"' "src/app/(dashboard)/admin/loading.tsx"` ≥ 1.
    - `grep -c 'unstable_retry' "src/app/(dashboard)/admin/error.tsx"` ≥ 1; `grep -c 'error\.message' "src/app/(dashboard)/admin/error.tsx"` == 0.
    - Both render tests pass incl. the message-never-rendered assertion.
    - `git diff src/app/(dashboard)/admin/page.tsx` shows the access gate intact (no redirect removed).
  </acceptance_criteria>
  <done>Admin routes have a data-table-anchored skeleton and a digest-only retry boundary; access gates intact; tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: @container-reshape the admin data tables (parent/child host) with structural tests for ALL THREE</name>
  <files>
    src/components/admin/ComputeJobsTable.tsx
    src/components/admin/ComputeJobsTable.test.tsx
    src/components/admin/MatchQueueIndex.tsx
    src/components/admin/MatchQueueIndex.test.tsx
    src/components/admin/AllocatorMatchQueue.tsx
    src/components/admin/AllocatorMatchQueue.test.tsx
  </files>
  <read_first>
    - src/components/strategy/StrategyTable.tsx (:516-520 host idiom; :559,577-583 priority-collapse + tabular-nums)
    - src/components/ResponsiveTable.tsx (:55-63 — @container lands on the overflow-x-auto host div)
    - src/components/admin/ComputeJobsTable.tsx (already uses ResponsiveTable — add className=@container)
    - src/components/admin/MatchQueueIndex.tsx (raw <table> :244 — needs a containment host)
    - src/components/admin/AllocatorMatchQueue.tsx (raw <table> :521/:623/:672 — needs a containment host; ALREADY imports ResponsiveTable per 53-PATTERNS:189)
    - .planning/phases/53-...-public/53-PATTERNS.md (§@container migrations; Pitfall 1 @container-size; Pitfall 2 tabular-nums; Pitfall 4 ultra-wide tuning; :220 parent/child structural-assertion rule)
  </read_first>
  <behavior>
    - Each admin table's containment region carries plain `className="@container"` (inline-size); column
      visibility/priority-collapse uses `@max-*:`/`@min-*:` variants on CHILD th/td — never the same element
      as the host. Every columnar number stays Geist Mono `tabular-nums` across every `@max-*` breakpoint.
    - For ComputeJobsTable: add `@container` to the existing ResponsiveTable host. For MatchQueueIndex and
      AllocatorMatchQueue: wrap the raw `<table>` in a `ResponsiveTable className="@container"` host (parent)
      and move the `@`-variants onto the child cells.
    - STRUCTURAL test per migrated table — REQUIRED for ALL THREE (ComputeJobsTable, MatchQueueIndex,
      AllocatorMatchQueue), NOT class-string jsdom: assert the element carrying `@container` is an ANCESTOR
      of the elements carrying `@max-*`/`@min-*` (parent/child, never same-element) AND that numeric columns
      retain `tabular-nums`. This is the #551-falsifiable guard — a class-string check false-passes and is
      forbidden.
  </behavior>
  <action>
    Migrate the three admin data tables to the `@container` idiom per PATTERNS §"@container table migrations"
    and the StrategyTable precedent. Use plain `@container` (NEVER `@container-size` — Pitfall 1 collapses
    height to 0). Collapse columns from the RIGHT in priority order; relocate a hidden value into a
    `<details>` if needed — never a fabricated em-dash/zero (no-invented-data). Tune for the wider canvas
    (un-collapse at `@min-*`) so the table is not stranded at ultra-wide (Pitfall 4). Add the parent/child
    STRUCTURAL test for EACH of the three tables — ComputeJobsTable, MatchQueueIndex, AND AllocatorMatchQueue
    (model AllocatorMatchQueue's assertion on the MatchQueueIndex/ComputeJobsTable structural test; it shares
    the ResponsiveTable host structure, so the same ancestor-of-variant + tabular-nums assertion applies).
    A class-string jsdom check is NOT acceptable for any of the three (#551 false-pass). Do NOT change any
    table data/sort/fetch behavior — this is layout containment only.
  </action>
  <verify>
    <automated>npx vitest run "src/components/admin/ComputeJobsTable.test.tsx" "src/components/admin/MatchQueueIndex.test.tsx" "src/components/admin/AllocatorMatchQueue.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - STRUCTURAL parent/child @container test present for ALL THREE admin tables (ComputeJobsTable, MatchQueueIndex, AllocatorMatchQueue) — asserting the host is a parent ancestor of the `@max-*`/`@min-*` child variants (falsifiable, NOT a class-string check).
    - Each migrated table: the `@container` host is on a DIFFERENT element than its `@max-*`/`@min-*` variants (asserted structurally, parent ancestor of child).
    - `tabular-nums` preserved on numeric columns (asserted for each of the three).
    - No `@container-size` anywhere in the touched files.
    - No fabricated em-dash/zero introduced for a collapsed column.
    - Table data/sort behavior unchanged (existing table tests still green).
  </acceptance_criteria>
  <done>The three admin tables reshape via a parent/child @container host with falsifiable structural tests (one per table, all three) and preserved tabular-nums.</done>
</task>

<task type="auto">
  <name>Task 3: Recover admin accidental clips + migrate admin type to fluid tiers</name>
  <files>
    src/app/(dashboard)/admin/partner-pilot/[partner_tag]/page.tsx
    src/components/admin/MatchQueueIndex.tsx
    src/components/admin/AllocatorMatchQueue.tsx
    # Type/raw-px migration spans the whole admin glob (grep-gated by <verify>):
    # + ~13 src/components/admin/** files (66 raw-px sites: e.g. ComputeJobsTable, MatchEvalDashboard,
    #   AdminTabs, UsageTable, DeletionRequestsTable, IntrosTable, PartnerImportPanel, UsersTable, etc.)
    # + ~8 src/app/(dashboard)/admin/** page files (15 raw-px sites: e.g. page.tsx, csv-status,
    #   compute-jobs, deletion-requests, intros, usage, partner-import, for-quants-leads, users)
  </files>
  <read_first>
    - .planning/audits/truncation-audit.md (admin clip sites SoT)
    - .planning/phases/53-...-public/53-PATTERNS.md (Truncation table: partner-pilot :166/170/209/212 email mid-clip unrecoverable; MatchQueueIndex :289 mandate_archetype; AllocatorMatchQueue :560/:701; legitimate clips ComputeJobsTable :240/261 + compute-jobs/page :125/135 — DO NOT strip)
    - src/components/ui/ScopedBanner.tsx (:29-35 wrap reference)
    - .planning/phases/53-...-public/53-PATTERNS.md (§type migration: admin pages 15 raw-px / 8 files; components/admin 66 raw-px / 13 files; data category ≤ 4 tiers h3/body/small/caption)
  </read_first>
  <action>
    Fix the admin accidental clips per the audit: partner-pilot `[partner_tag]/page.tsx:166/170/209/212`
    (the email at :170 is a mid-clip and MUST recover via `title=`; allocator/staged names + status·manager
    via `title=` or wrap), MatchQueueIndex `:289` mandate_archetype (`max-w-[260px]` dense cell → single-line
    + `title=`), AllocatorMatchQueue `:560/:701` reason/founder-note (`title=`). Apply `title=` ONLY to data
    already rendered to the admin — never pull an un-gated field into a `title` (ASVS V7). DO NOT strip the
    legitimate clips' recovery affordances (ComputeJobsTable :240/261, compute-jobs/page :125/135 — already
    carry `title=`/intentional ID slice). Migrate admin raw `text-[Npx]` across BOTH the admin page tree
    (~8 files, 15 sites) AND `src/components/admin/**` (~13 files, 66 sites) to the named `--text-*` tiers
    (data category: `--text-h3`/`--text-body`/`--text-small`/`--text-caption`; `--text-micro` only for
    badge/status-chip text). This is a glob-wide migration — the listed files in <files> are the named
    clip-fix targets; the type migration additionally touches the ~21 other admin files noted there. Do NOT
    flip the admin eslint glob here — Plan 06 owns the eslint.config.mjs edit (this plan must not touch that
    shared file).
  </action>
  <verify>
    <automated>grep -RnE "text-\[[0-9]+px\]|text-(xs|sm|base|lg|xl)\b" "src/app/(dashboard)/admin/" "src/components/admin/" | grep -v '^\s*//' | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - partner-pilot `:170` email renders with `title=` (recoverable); the other named admin clip sites carry `title=`/wrap per the audit.
    - Legitimate clips (ComputeJobsTable :240/261, compute-jobs/page :125/135) still carry their recovery affordance (not stripped).
    - The grep `<verify>` returns 0: `grep -RnE "text-\[[0-9]+px\]|text-(xs|sm|base|lg|xl)\b" "src/app/(dashboard)/admin/" "src/components/admin/"` (excluding comments) == 0 — gating the WHOLE migrated admin glob (page tree + components/admin), not just the named files.
    - No `title=` added to a control that already has accessible text; no un-gated field pulled into a title.
    - `eslint.config.mjs` is NOT modified by this plan.
  </acceptance_criteria>
  <done>Admin clips recover (incl. the partner-pilot email), legitimate clips preserved, admin type migrated onto the fluid tiers across the full admin glob (page tree + components/admin), grep-gated to 0 raw-px.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-admin user → admin route | access-gated: redirect(/login) then isAdminUser redirect |
| RSC fetch error → admin error.tsx | thrown digest crosses into the client boundary |
| rendered admin data → no-clip title= | title= must expose only already-rendered data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-08 | Elevation of Privilege | admin access gates | mitigate | Restyle MUST NOT remove `redirect("/login")` or the `isAdminUser` redirect; `git diff admin/page.tsx` gate-intact check. The route-contract `admin` class + PUBLIC_ROUTES lockstep enforce at lint. (ASVS V4) |
| T-53-09 | Information Disclosure | admin error.tsx | mitigate | digest ONLY, never error.message; grep == 0 + message-never-rendered test. (ASVS V7) |
| T-53-10 | Information Disclosure | no-clip title= on admin email/notes | mitigate | `title=` applied ONLY to data already rendered to the admin (the clipped string); never pull an un-gated field into a title. The partner-pilot email is already rendered to the admin → title= safe. |
| T-53-11 | DoS/Tampering | @container regression (#551) | mitigate | Parent/child host enforced by a falsifiable STRUCTURAL test (ancestor relationship) on ALL THREE admin tables, not a false-passing jsdom class-string check. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/admin/loading.test.tsx" "src/app/(dashboard)/admin/error.test.tsx" "src/components/admin/ComputeJobsTable.test.tsx" "src/components/admin/MatchQueueIndex.test.tsx" "src/components/admin/AllocatorMatchQueue.test.tsx"` — all green.
- `grep -RnE "text-\[[0-9]+px\]" "src/app/(dashboard)/admin/" "src/components/admin/"` (excluding comments) == 0.
- `npm run lint` — route-contract guard green (admin eslint flip is Plan 06; admin raw-px stays warn until then, so lint is green now).
- Admin ultra-wide responsiveness is proven via the component structural tests + the conformance check (Plan 07), NOT e2e (Pitfall 7 — allocator seed redirects non-admins; documented gap, admin-seeded e2e is Phase 54).
</verification>

<success_criteria>
- Shared admin `loading.tsx`+`error.tsx` (+ per-sub-page only where layout differs); digest-only; sr-only liveness.
- Three admin tables reshape via parent/child `@container` host with falsifiable structural tests (one per table, all three); tabular-nums preserved.
- Admin accidental clips recover (incl. partner-pilot email); legitimate clips preserved.
- Admin type migrated onto fluid tiers (0 raw text-[Npx] across admin pages + components/admin).
- Admin access gates intact; eslint.config.mjs untouched (Plan 06 owns the flip).
</success_criteria>

<output>
Create `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-05-SUMMARY.md` when done
</output>
