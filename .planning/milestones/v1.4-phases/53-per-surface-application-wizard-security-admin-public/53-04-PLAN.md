---
phase: 53-per-surface-application-wizard-security-admin-public
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/portfolios/loading.tsx
  - src/app/(dashboard)/portfolios/loading.test.tsx
  - src/app/(dashboard)/portfolios/error.tsx
  - src/app/(dashboard)/portfolios/error.test.tsx
  - src/app/(dashboard)/portfolios/[id]/loading.tsx
  - src/app/(dashboard)/portfolios/[id]/error.tsx
  - src/app/(dashboard)/portfolios/[id]/manage/loading.tsx
  - src/app/(dashboard)/portfolios/[id]/manage/error.tsx
  - src/app/(dashboard)/portfolios/[id]/documents/loading.tsx
  - src/app/(dashboard)/portfolios/[id]/documents/error.tsx
  - src/app/(dashboard)/portfolios/page.tsx
  - src/app/(dashboard)/portfolios/[id]/manage/page.tsx
  - src/components/portfolio/DocumentList.tsx
  - src/components/portfolio/PortfolioOptimizer.tsx
  - src/components/portfolio/MorningBriefing.tsx
autonomous: true
requirements: [APPLY-04, STATE-05, BP-02]
must_haves:
  truths:
    - "Every portfolios route (list, [id], [id]/manage, [id]/documents) has a loading.tsx (match-layout skeleton) and a digest-only error.tsx"
    - "Portfolios accidental clips recover: card name/description wrap, manage strategy name wraps/title=, document title recovers, optimizer suggestion name wraps (the legitimate strategy_id clip is preserved)"
    - "Portfolio access gate intact: !user redirect(/login)"
    - "components/portfolio raw text-[Npx] migrated onto the fluid tiers"
  artifacts:
    - path: "src/app/(dashboard)/portfolios/loading.tsx"
      provides: "portfolios cards-grid skeleton with sr-only role=status"
      contains: "role=\"status\""
    - path: "src/app/(dashboard)/portfolios/error.tsx"
      provides: "portfolios digest-only error boundary"
      contains: "unstable_retry"
  key_links:
    - from: "src/app/(dashboard)/portfolios/error.tsx"
      to: "error.digest"
      via: "render digest only, never error.message"
      pattern: "error\\.digest"
    - from: "src/app/(dashboard)/portfolios/page.tsx"
      to: "portfolio name/description"
      via: "wrap break-words min-w-0 (no bare truncate/line-clamp)"
      pattern: "break-words"
---

<objective>
Bring the /portfolios surface (APPLY-04 + STATE-05) to the v1.4 bar: add the missing route-state files
across the list + detail + manage + documents routes, recover the portfolios accidental clips, and migrate
`components/portfolio/**` type to the fluid tiers — WITHOUT removing the access gate. The fluid-fill width
raise and the portfolios eslint ratchet flip are deferred to Plan 06 (Wave 2) to keep the shared
`DashboardChrome`/`eslint.config.mjs` files out of this wave.

Purpose: /portfolios is a data surface lacking ALL route-state files; this fills them and recovers the
card/manage/document clips. The portfolios page TREE is already 0 raw-px, but `components/portfolio/**`
carries 28 raw-px sites that must migrate so Plan 06 can flip the portfolios glob clean.
Output: 5 route-state pairs (list + 3 detail routes + their tests); portfolios clip fixes;
components/portfolio type migration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-UI-SPEC.md
@.planning/phases/53-per-surface-application-wizard-security-admin-public/53-PATTERNS.md
@.planning/audits/truncation-audit.md

<interfaces>
<!-- Skeleton/error analogs + no-clip + the portfolio access gate + dominant anchors. -->

Cards-grid skeleton — src/app/(dashboard)/strategies/loading.tsx (SkeletonCard grid idiom):
  {Array.from({length:5}).map((_,i) => <SkeletonCard key={i} />)}
Detail skeleton anchor — src/app/factsheet/[id]/v2/loading.tsx (name header + headline-metric block).
Error shape — src/app/(dashboard)/error.tsx (unstable_retry, digest-only).
Error-test precedent — src/app/strategy/[id]/v2/error.test.tsx.
Liveness — sr-only role="status" aria-live="polite" "Loading portfolios." (factsheet :63-66).

Access gate (PRESERVE) — src/app/(dashboard)/portfolios/page.tsx:21-24:
  const { data:{user} } = await supabase.auth.getUser();
  if (!user) redirect("/login");
  const portfolios = await getUserPortfolios();   // fetch in PAGE body → loading.tsx WILL render (Pitfall 5)

No-clip — ScopedBanner.tsx:29-35 wrap (break-words min-w-0); title= for tabular alignment only.
Empty state — EmptyStateCard.tsx ({ heading, body }, neutral, NO role=alert) — extend existing empty branches.

Dominant anchors (53-UI-SPEC): list → SkeletonCard grid; [id]/[id]/manage → name header + headline-metric block.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add portfolios route-state files (list + [id] + manage + documents)</name>
  <files>
    src/app/(dashboard)/portfolios/loading.tsx
    src/app/(dashboard)/portfolios/loading.test.tsx
    src/app/(dashboard)/portfolios/error.tsx
    src/app/(dashboard)/portfolios/error.test.tsx
    src/app/(dashboard)/portfolios/[id]/loading.tsx
    src/app/(dashboard)/portfolios/[id]/error.tsx
    src/app/(dashboard)/portfolios/[id]/manage/loading.tsx
    src/app/(dashboard)/portfolios/[id]/manage/error.tsx
    src/app/(dashboard)/portfolios/[id]/documents/loading.tsx
    src/app/(dashboard)/portfolios/[id]/documents/error.tsx
  </files>
  <read_first>
    - src/app/(dashboard)/strategies/loading.tsx (SkeletonCard grid idiom for the list)
    - src/app/factsheet/[id]/v2/loading.tsx (detail name-header + headline-metric anchor + sr-only role=status)
    - src/app/(dashboard)/error.tsx (canonical error shape)
    - src/app/strategy/[id]/v2/error.test.tsx (render-test precedent)
    - src/app/(dashboard)/portfolios/page.tsx (access gate + page-body fetch)
    - .planning/phases/53-...-public/53-UI-SPEC.md (Route-state matrix portfolios rows; Dominant anchor per route)
  </read_first>
  <behavior>
    - portfolios/loading.tsx → a responsive grid of SkeletonCard (name line + description + metric row)
      matching the live card grid; closing sr-only role="status" "Loading portfolios.".
    - portfolios/[id] + [id]/manage loading.tsx → portfolio-name header Skeleton + headline-metric block +
      secondary holdings/strategy rows; documents loading.tsx → a leaner document-list skeleton.
    - Each error.tsx ("use client", `{ error, unstable_retry }`) → "Something went wrong" + body + "Try
      again"→unstable_retry, digest-only, console tag `[portfolios-error]` / `[portfolio-detail-error]` etc.
    - portfolios/loading.test.tsx + portfolios/error.test.tsx assert role=status, retry-invokes-once,
      digest-when-present, message-NEVER-rendered. (The deeper-route files reuse the shared error shape;
      a smoke render test for the list pair covers the gate; add deeper tests if the layout differs.)
  </behavior>
  <action>
    Create the 5 route-state pairs (list + [id] + [id]/manage + [id]/documents). The list uses the
    SkeletonCard grid; detail routes use the factsheet name-header + headline-metric anchor (match-layout
    where cheap, per Claude's discretion). Mirror the canonical error.tsx shape (digest-only, unstable_retry,
    per-surface console tag). Co-locate at least the list pair's render tests in the SAME change (coverage
    gate); add deeper-route tests where the layout warrants. Keep server-fetch in each page.tsx; do NOT
    move auth/fetch into `(dashboard)/layout.tsx` (Pitfall 5). PRESERVE the `redirect("/login")` gate.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/portfolios/loading.test.tsx" "src/app/(dashboard)/portfolios/error.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'role="status"' "src/app/(dashboard)/portfolios/loading.tsx"` ≥ 1.
    - Every portfolios route in scope (list, [id], [id]/manage, [id]/documents) has both loading.tsx and error.tsx.
    - `grep -RIl 'error\.message' src/app/(dashboard)/portfolios/**/error.tsx` returns nothing (digest-only).
    - List pair render tests pass incl. message-never-rendered.
    - `git diff src/app/(dashboard)/portfolios/page.tsx` shows the `redirect("/login")` gate intact.
  </acceptance_criteria>
  <done>All four portfolios routes have match-layout skeletons and digest-only error boundaries; access gate intact; tested.</done>
</task>

<task type="auto">
  <name>Task 2: Recover portfolios accidental clips</name>
  <files>
    src/app/(dashboard)/portfolios/page.tsx
    src/app/(dashboard)/portfolios/[id]/manage/page.tsx
    src/components/portfolio/DocumentList.tsx
    src/components/portfolio/PortfolioOptimizer.tsx
    src/components/portfolio/MorningBriefing.tsx
  </files>
  <read_first>
    - .planning/audits/truncation-audit.md (portfolios clip sites SoT)
    - .planning/phases/53-...-public/53-PATTERNS.md (Truncation table: portfolios/page :44/:48; [id]/manage :69; DocumentList :68; PortfolioOptimizer :78 (—:81 strategy_id LEGITIMATE, leave); MorningBriefing :33 line-clamp-3)
    - src/components/ui/ScopedBanner.tsx (:29-35 wrap reference)
  </read_first>
  <action>
    Fix the portfolios accidental clips per the audit: `portfolios/page.tsx:44` portfolio name `<h3 …
    truncate>` → WRAP (`break-words min-w-0`), `:48` description `line-clamp-2` → wrap or keep clamp only if
    a full-detail surface is one documented click away; `[id]/manage/page.tsx:69` strategy name → wrap or
    `title=`; `DocumentList.tsx:68` document title → wrap or `title=`; `PortfolioOptimizer.tsx:78` suggestion
    name → WRAP (LEAVE `:81` raw strategy_id — it is a legitimate clip); `MorningBriefing.tsx:33` narrative
    `line-clamp-3` → wrap or add an expand affordance. NEVER relocate a clip (no new bare truncate/line-clamp
    without recovery). Apply `title=` only to already-rendered data. Do NOT change any data-fetch/behavior.
  </action>
  <verify>
    <automated>npx vitest run src/components/portfolio/ src/app/\(dashboard\)/portfolios/</automated>
  </verify>
  <acceptance_criteria>
    - portfolios/page.tsx:44 name uses `break-words min-w-0` (not bare `truncate`).
    - The named portfolios clip sites carry wrap/`title=`/expand per the audit; no new bare truncate/line-clamp without recovery.
    - PortfolioOptimizer.tsx:81 raw strategy_id clip is preserved (legitimate — not stripped).
    - Existing portfolio component tests still green.
  </acceptance_criteria>
  <done>Portfolios card/manage/document/optimizer/narrative clips recover; the legitimate strategy_id clip preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate components/portfolio type to the fluid tiers</name>
  <files>
    src/components/portfolio/DocumentList.tsx
    src/components/portfolio/PortfolioOptimizer.tsx
    src/components/portfolio/MorningBriefing.tsx
  </files>
  <read_first>
    - .planning/phases/53-...-public/53-PATTERNS.md (§type migration: components/portfolio 28 raw-px across 18 files; portfolios page tree already 0; A2 the portfolios glob needs components/portfolio clean to flip)
    - globals.css (the --text-* spine; data category ≤ 4 tiers: h3/body/small/caption)
  </read_first>
  <action>
    Migrate every raw `text-[Npx]`/`text-sm` across `src/components/portfolio/**` (~28 sites / 18 files) to
    the named `--text-*` tiers (data category: `--text-h3`/`--text-body`/`--text-small`/`--text-caption`;
    `--text-micro` only for badge/status-chip text). NOTE the shared `src/components/ui/EmptyStateCard.tsx:27`
    carries a `text-[11px]` — if a portfolios degenerate state renders EmptyStateCard, that shared primitive
    is covered by the EXCLUDE-or-migrate decision recorded in Plan 06 (do NOT migrate the shared primitive
    here; Plan 06 owns the eslint glob boundary). Do NOT flip the portfolios eslint glob here (Plan 06 owns
    eslint.config.mjs). Do NOT change font families.
  </action>
  <verify>
    <automated>grep -RnE "text-\[[0-9]+px\]|text-(xs|sm|base|lg|xl)\b" src/components/portfolio/ | grep -v '^\s*//' | wc -l | grep -qx '0'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -RnE "text-\[[0-9]+px\]" src/components/portfolio/` (excluding comments) == 0.
    - `eslint.config.mjs` is NOT modified by this plan.
    - ≤ 4 data tiers + 2 weights; no font-family change.
  </acceptance_criteria>
  <done>components/portfolio is migrated onto the fluid tiers (0 raw text-[Npx]), unblocking the Plan-06 portfolios glob flip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated user → /portfolios | access-gated: redirect(/login) |
| RSC fetch error → portfolios error.tsx | thrown digest crosses into the client boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-12 | Elevation of Privilege | portfolios access gate | mitigate | Restyle MUST NOT remove `redirect("/login")`; `git diff portfolios/page.tsx` gate-intact check. (ASVS V4) |
| T-53-13 | Information Disclosure | portfolios error.tsx (×5 routes) | mitigate | digest ONLY, never error.message; grep over `portfolios/**/error.tsx` finds no `error.message` + message-never-rendered test. (ASVS V7) |
| T-53-14 | Information Disclosure | no-clip title= on portfolios | mitigate | `title=` applied only to already-rendered data; never pull an un-gated field into a title. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/portfolios/loading.test.tsx" "src/app/(dashboard)/portfolios/error.test.tsx" src/components/portfolio/` — green.
- `grep -RnE "text-\[[0-9]+px\]" src/components/portfolio/` (excluding comments) == 0.
- `npm run lint` — route-contract guard green (portfolios eslint flip is Plan 06; portfolios raw-px page tree already 0).
</verification>

<success_criteria>
- All four portfolios routes have match-layout `loading.tsx` + digest-only `error.tsx` (+ list tests).
- Portfolios accidental clips recover; the legitimate strategy_id clip preserved.
- components/portfolio migrated onto fluid tiers (0 raw text-[Npx]).
- Portfolio access gate intact; eslint.config.mjs untouched (Plan 06 owns the flip).
</success_criteria>

<output>
Create `.planning/phases/53-per-surface-application-wizard-security-admin-public/53-04-SUMMARY.md` when done
</output>
