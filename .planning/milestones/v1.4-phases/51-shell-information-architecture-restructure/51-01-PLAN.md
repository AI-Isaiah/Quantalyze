---
phase: 51-shell-information-architecture-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/routing/route-contract-manifest.ts
  - src/__tests__/check-route-contract.test.ts
  - scripts/check-route-contract.ts
  - src/components/layout/Breadcrumb.test.tsx
  - src/components/layout/Sidebar.test.tsx
  - src/components/layout/PageHeader.test.tsx
autonomous: true
requirements: [NAV-03, NAV-02]
must_haves:
  truths:
    - "A failing guard-unit test exists that drives runCheck(tmpTree, manifest) and asserts the 4 violation classes (unclassified, missing-from-PUBLIC_ROUTES, missing-redirect, stale)"
    - "A failing breadcrumb a11y test asserts the leaf crumb has aria-current=page and linked crumbs carry a focus-visible ring class"
    - "A failing sidebar a11y test asserts the active NavItemLink has aria-current=page + a focus-visible ring class, AND pins the role OR-logic so a completeness edit cannot regress T-45-01"
    - "A failing PageHeader test asserts that, given a breadcrumb prop, PageHeader renders a <Breadcrumb> above the <h1>, and that omitting the prop renders identically to today (RED — the breadcrumb prop does not exist until 51-03)"
  artifacts:
    - path: "src/lib/routing/route-contract-manifest.ts"
      provides: "RouteClass union + RouteEntry type + an (initially partial) ROUTE_CONTRACT_MANIFEST data-only module"
      contains: "ROUTE_CONTRACT_MANIFEST"
    - path: "src/__tests__/check-route-contract.test.ts"
      provides: "Guard-unit RED test driving runCheck against a tmp fixture tree (one fixture per violation class + the stripComments comment-bypass carve-out)"
      contains: "runCheck"
    - path: "scripts/check-route-contract.ts"
      provides: "Guard skeleton exporting runCheck/findRouteFiles/stripComments so the RED test imports resolve (logic stubbed to fail the contract, not the import)"
      exports: ["runCheck", "findRouteFiles", "stripComments"]
    - path: "src/components/layout/PageHeader.test.tsx"
      provides: "RED test pinning NAV-02's PageHeader breadcrumb prop (renders <Breadcrumb> above <h1> when passed; identical to today when omitted) — turns GREEN in 51-03"
      contains: "breadcrumb"
  key_links:
    - from: "src/__tests__/check-route-contract.test.ts"
      to: "scripts/check-route-contract.ts"
      via: "import { runCheck, stripComments }"
      pattern: "from .*scripts/check-route-contract"
    - from: "scripts/check-route-contract.ts"
      to: "src/lib/routing/route-contract-manifest.ts"
      via: "import { ROUTE_CONTRACT_MANIFEST }"
      pattern: "route-contract-manifest"
---

<objective>
Ship the Wave-0 executable contract for Phase 51's durable win — the route-contract guard — and for the breadcrumb/sidebar/PageHeader a11y + back-path additions, as FAILING tests that pin the behavior before any implementation. This mirrors the project's proven "RED contract precedes GREEN" precedent (49-01, 50-01): the guard's runCheck rules, the 3 a11y gaps, and the NAV-02 PageHeader breadcrumb prop are encoded as assertions first, so Wave-1 plans 02/03 implement against a fixed target instead of inventing one.

Purpose: Make the #512 (307→login) lockstep, the 3 UI-SPEC a11y gaps, and the NAV-02 PageHeader breadcrumb back-path machine-checkable from the first commit, so completeness/move work in later plans cannot silently regress them.
Output: A guard skeleton + data-only manifest type module + 4 RED test files (guard-unit, Breadcrumb a11y, Sidebar a11y, PageHeader breadcrumb-prop).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md
@.planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md
@.planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md

<interfaces>
<!-- The analog this plan clones. Executor uses these directly — no exploration needed. -->

scripts/check-admin-route-manifest.ts (the TEMPLATE, 444 lines) exports:
  export function stripComments(contents: string): string            // L107 — single-pass char tokenizer, erases comment+string+template-literal contents (REUSE VERBATIM)
  export function findRouteFiles(root: string): string[]             // L328 — recursive walk, currently matches "route.ts"
  export function runCheck(rootDir: string): string[]                // L363 — pure, returns violations[]; empty = pass
  function main(): void                                              // L414 — exit(1) on violations; dormant-under-test guard at L439-442

src/lib/auth/rbac-manifest.ts (the data-only-module analog, 213 lines):
  a pure data module — imports nothing, runs no logic, loadable from tests/CI/docs without server-only/Supabase.

src/__tests__/check-admin-route-manifest.test.ts (the guard-unit-test analog):
  drives runCheck(tmpTree) against a hand-built tmp fixture tree via mkdtempSync/writeFileSync; asserts the returned violations array; one fixture per violation class; pins the stripComments comment-bypass carve-out.

src/components/layout/MobileNav.tsx (the a11y pattern that already closes BOTH gaps):
  L70  aria-current={active ? "page" : undefined}
  L76  "focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent"
  active rule: pathname === href || pathname.startsWith(href + "/")   (SSR-safe, no useSearchParams)

src/components/layout/Sidebar.tsx NavItemLink (L301-334) — current active rule L308:
  const active = pathname === item.href || pathname.startsWith(item.href + "/");
  // <Link> currently has NO aria-current and NO focus-visible class.
  buildNavSections role OR-logic (DO NOT REGRESS — T-45-01): showsAllocatorWorkspace = isAllocator || isAdmin (L34); showsManagerWorkspace = isManager || isAdmin (L35); showsDiscovery = isAllocator || isAdmin (L36).

src/components/layout/Breadcrumb.tsx (47 lines) — current structure:
  <nav aria-label="Breadcrumb"> (L15); linked crumb <Link className="hover:text-text-primary transition-colors"> (L33, no focus-visible); leaf <span className="text-text-primary font-medium"> (L40, no aria-current).
  takes a curated `items: BreadcrumbItem[]` prop (KEEP — RESEARCH Pattern 3a; do NOT auto-derive from segments).

src/components/layout/PageHeader.tsx — current structure:
  PageHeaderProps { title; description?; actions?; meta? } (L3-9); single <h1> text-[32px] (L15-17). Has NO `breadcrumb` prop today — that prop is added in 51-03, which is why the PageHeader test ships RED here.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Manifest type module + guard skeleton (imports resolve, contract fails)</name>
  <files>src/lib/routing/route-contract-manifest.ts, scripts/check-route-contract.ts</files>
  <read_first>
    - src/lib/auth/rbac-manifest.ts (the data-only-module analog — type union + entry shape + doc-comment naming the enforcing guard, L22-32, L48-70)
    - scripts/check-admin-route-manifest.ts (the guard template — shebang+imports L1/L54-65, stripComments L107-220 REUSE VERBATIM, findRouteFiles L328-351, runCheck L363-412, main()+dormant-guard L414-444)
    - .planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md (§check-route-contract.ts and §route-contract-manifest.ts — the exact clone instructions + the page-path→URL derivation step)
    - .planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md (§Architecture Pattern 4 L318-333 — the 4 runCheck rules; §Code Examples L429-439 — parsing PUBLIC_ROUTES)
  </read_first>
  <action>
    Create src/lib/routing/route-contract-manifest.ts as a pure data-only module (imports nothing, runs no logic — same property as rbac-manifest.ts so CI/tests/docs load it without pulling server-only). Export `type RouteClass = "public" | "private" | "admin" | "exception"` and `type RouteEntry = { route: string; class: RouteClass; redirectFrom?: string; notes: string }`. Export `const ROUTE_CONTRACT_MANIFEST: RouteEntry[]` containing ONLY a small seed (a handful of representative entries — e.g. `/legal` public, `/allocations` private, `/admin` admin, `/api/health` exception); the FULL 57-route population is plan 51-02's job. Add a doc-comment naming `scripts/check-route-contract.ts` as the enforcing guard (mirror rbac-manifest.ts L22-32).

    Create scripts/check-route-contract.ts by cloning scripts/check-admin-route-manifest.ts: copy the shebang `#!/usr/bin/env -S npx tsx`, the node:fs/node:url/node:path imports, the `__dirname`/`REPO_ROOT` lines, and the `stripComments` tokenizer VERBATIM (do not re-hand-roll it). Adapt `findRouteFiles` to match `name === "page.tsx"` (not "route.ts"). Export `runCheck(rootDir: string, manifest = ROUTE_CONTRACT_MANIFEST): string[]` with the 4 rules from RESEARCH Pattern 4 STUBBED (each rule present and reachable, but the implementation deliberately incomplete so the RED test in Task 2 fails on assertions, NOT on a missing export). Include the page-path→URL derivation helper (strip `src/app`, drop `(group)` parens segments, `[seg]`→`:seg`). Copy `main()` and the `import.meta.url === file://${process.argv[1]}` dormant-under-test guard at the bottom VERBATIM so the script never runs on import inside vitest. Do NOT wire it into package.json lint yet (that is plan 51-02).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E 'route-contract' || echo "no type errors in route-contract files"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/routing/route-contract-manifest.ts` exists, exports `RouteClass`, `RouteEntry`, and `ROUTE_CONTRACT_MANIFEST`, and imports nothing (verify: `grep -c '^import' src/lib/routing/route-contract-manifest.ts` returns 0)
    - `scripts/check-route-contract.ts` exists and exports `runCheck`, `findRouteFiles`, `stripComments` (verify: `grep -Ec 'export function (runCheck|findRouteFiles|stripComments)' scripts/check-route-contract.ts` returns 3)
    - `findRouteFiles` matches page.tsx, not route.ts (verify: `grep 'page.tsx' scripts/check-route-contract.ts` matches AND `grep -v '^#' scripts/check-route-contract.ts | grep -c '"route.ts"'` returns 0)
    - the dormant-under-test guard is present (verify: `grep 'import.meta.url' scripts/check-route-contract.ts` matches)
    - package.json `"lint"` is UNCHANGED this plan (verify: `grep -c 'check-route-contract' package.json` returns 0)
  </acceptance_criteria>
  <done>The manifest module and guard skeleton compile, exports resolve for the Task-2 test, the script is dormant under vitest, and lint wiring is deferred to 51-02.</done>
</task>

<task type="auto">
  <name>Task 2: RED guard-unit test (4 violation classes + comment-bypass carve-out)</name>
  <files>src/__tests__/check-route-contract.test.ts</files>
  <read_first>
    - src/__tests__/check-admin-route-manifest.test.ts (the test analog — imports+tmp-tree harness L1-41, one-fixture-per-class design L43-90, the stripComments comment-bypass pin L81-90)
    - scripts/check-route-contract.ts (the runCheck/stripComments signatures created in Task 1)
    - src/proxy.ts (PUBLIC_ROUTES literal L7 — the array the guard parses; the matcher L52-55 the guard's Rule 2 mirrors)
    - .planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md (§check-route-contract.test.ts — the per-violation-class fixture list)
  </read_first>
  <action>
    Create src/__tests__/check-route-contract.test.ts by cloning src/__tests__/check-admin-route-manifest.test.ts. Reuse its tmp-tree harness (mkdtempSync into tmpdir, a `writeRoute(relativePath, contents)` helper that mkdirs the parent and writes the file, beforeEach/afterEach create+rmSync). Import `runCheck` and `stripComments` from ../../scripts/check-route-contract and `RouteEntry` from ../lib/routing/route-contract-manifest.

    Write one `it()` per violation class, each building a tiny fixture tree (a `src/app/.../page.tsx` plus a `src/proxy.ts` fixture carrying a `const PUBLIC_ROUTES = [...]` literal) and asserting the returned violations array:
      - PASS: a public route present in both the fixture manifest AND PUBLIC_ROUTES → `runCheck` returns `[]`.
      - Rule 2 / #512: a manifest-"public" route ABSENT from PUBLIC_ROUTES → violations include a MISSING-FROM-PUBLIC entry naming the route.
      - Rule 1: a page.tsx with NO manifest entry → violations include an UNCLASSIFIED entry.
      - Rule 3: a manifest entry with `redirectFrom` but no matching next.config.ts `redirects()` source → violations include a MISSING-REDIRECT entry.
      - Rule 4: a manifest entry whose route maps to no real page file → violations include a STALE entry.
      - Comment-bypass carve-out (the stripComments regression): a fixture proxy.ts where `"/legal"` appears ONLY inside a comment must NOT satisfy the Rule-2 lockstep — assert that a manifest-"public" `/legal` whose only PUBLIC_ROUTES "match" is commented-out still produces a MISSING-FROM-PUBLIC violation. Drive this through `stripComments` exactly as the admin test pins it at L81-90.

    These assertions MUST FAIL against the stubbed runCheck from Task 1 (that is the RED contract). Do not soften them to pass the stub.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/check-route-contract.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - the test file imports `runCheck` and `stripComments` from the guard and `RouteEntry` from the manifest (verify: `grep -c 'check-route-contract\|route-contract-manifest' src/__tests__/check-route-contract.test.ts` >= 2)
    - it covers all 4 rules + PASS + the comment-bypass carve-out (verify: `grep -Ec "it\(" src/__tests__/check-route-contract.test.ts` >= 6)
    - the suite is RED now: `npx vitest run src/__tests__/check-route-contract.test.ts` exits non-zero with failing assertions (NOT module-not-found, NOT a process.exit from the script)
    - the comment-bypass test references stripComments (verify: `grep -c 'stripComments' src/__tests__/check-route-contract.test.ts` >= 1)
  </acceptance_criteria>
  <done>A RED guard-unit test pins the 4 lockstep rules + the stripComments comment-bypass; it fails on assertions against the Task-1 stub, ready for 51-02 to turn GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: RED breadcrumb + sidebar + PageHeader a11y/back-path tests (the 3 UI-SPEC gaps + NAV-02 breadcrumb prop + role-OR-logic pin)</name>
  <files>src/components/layout/Breadcrumb.test.tsx, src/components/layout/Sidebar.test.tsx, src/components/layout/PageHeader.test.tsx</files>
  <read_first>
    - src/components/layout/Breadcrumb.tsx (current: leaf span L40 has no aria-current; linked Link L33 has no focus-visible)
    - src/components/layout/Sidebar.tsx (NavItemLink L301-334: active rule L308, no aria-current, no focus-visible; buildNavSections role OR-logic L34-36)
    - src/components/layout/PageHeader.tsx (PageHeaderProps L3-9 — NO breadcrumb prop today; single <h1> L15-17)
    - src/components/layout/MobileNav.tsx (the closed-gaps reference: aria-current L70, focus-visible:outline-2 outline-accent L76)
    - src/components/layout/Sidebar.test.tsx and src/components/layout/MobileNav.test.tsx (the existing colocated tests — match their render/query/vi.mock("next/navigation") style; these are the STYLE ANCHORS because Breadcrumb.test.tsx / PageHeader.test.tsx do not exist yet)
    - .planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md (§Item state contract L162-176, §Breadcrumb Contract L210-221, §Unified Active/Hover/Focus L270-289)
  </read_first>
  <action>
    **Create** src/components/layout/Breadcrumb.test.tsx (it does NOT exist yet) following the existing colocated style in Sidebar.test.tsx / MobileNav.test.tsx (the same `render`/`screen`/`vi.mock("next/navigation")` harness). Add two RED assertions: (a) render a multi-crumb `items` array and assert the LEAF crumb element carries `aria-current="page"` (query the last crumb; it is currently a plain styled span with no aria-current); (b) assert a LINKED (non-leaf) crumb's className includes a focus-visible ring token (`focus-visible:ring-2` and `ring-accent`, or the outline equivalent the implementation will use — assert the focus-visible keyword + accent token, never bare `focus:`). These fail against today's component.

    Extend src/components/layout/Sidebar.test.tsx (it DOES exist) with: (a) render the sidebar so a known route is active and assert the active NavItemLink carries `aria-current="page"` (currently absent); (b) assert the NavItemLink className includes a focus-visible ring/outline with the accent token (currently absent); (c) a role-OR-logic PIN that locks T-45-01: assert that a `manager`-only (isManager=true, isAllocator=false, isAdmin=false) render does NOT show an allocator-workspace-only entry, and an `allocator` render DOES — so any 51-03 completeness edit that leaks an allocator surface to a manager turns this test red. Mirror the active rule `pathname === href || pathname.startsWith(href + "/")` exactly; do not introduce useSearchParams.

    **Create** src/components/layout/PageHeader.test.tsx (it does NOT exist yet — same colocated style anchor) with a RED assertion for the NAV-02 breadcrumb back-path: (a) render `<PageHeader title=... breadcrumb={[...multi-crumb...]} />` and assert a `<Breadcrumb>` (its `<nav aria-label="Breadcrumb">` landmark) renders ABOVE the `<h1>` in DOM order; (b) render `<PageHeader title=... />` with NO breadcrumb prop and assert NO breadcrumb landmark is rendered (identical-to-today). This file is RED because PageHeaderProps has no `breadcrumb` prop until 51-03 — the prop will not typecheck/render today; mark the file clearly so 51-03 knows this flips to GREEN when it adds the prop.

    All new assertions MUST FAIL now (the components have not yet gained aria-current/focus-visible, and PageHeader has no breadcrumb prop) EXCEPT the Sidebar role-OR-logic pin, which should PASS today (it locks existing-correct behavior). Mark each file clearly so 51-03 knows which assertions flip to GREEN.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Breadcrumb.test.tsx src/components/layout/Sidebar.test.tsx src/components/layout/PageHeader.test.tsx 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - Breadcrumb test asserts leaf `aria-current="page"` and a linked-crumb focus-visible+accent class (verify: `grep -Ec "aria-current|focus-visible" src/components/layout/Breadcrumb.test.tsx` >= 2)
    - Sidebar test asserts active-item `aria-current="page"` + focus-visible+accent class AND a manager-vs-allocator role-leak pin (verify: `grep -Ec "aria-current|focus-visible|isManager|isAllocator" src/components/layout/Sidebar.test.tsx` >= 3)
    - PageHeader test asserts the breadcrumb prop renders a Breadcrumb landmark above the h1 AND that omitting it renders no breadcrumb (verify: `grep -Ec "breadcrumb|Breadcrumb" src/components/layout/PageHeader.test.tsx` >= 2)
    - the a11y-gap + PageHeader-breadcrumb assertions are RED now (run shows the new aria-current/focus-visible + breadcrumb-prop assertions failing) while the Sidebar role-OR-logic pin passes
    - no `useSearchParams` introduced in any of the three tests (verify: `grep -c 'useSearchParams' src/components/layout/Breadcrumb.test.tsx src/components/layout/Sidebar.test.tsx src/components/layout/PageHeader.test.tsx` returns 0)
  </acceptance_criteria>
  <done>RED a11y + back-path tests pin the 3 UI-SPEC gaps and the NAV-02 PageHeader breadcrumb prop; a GREEN role-OR-logic pin locks T-45-01; 51-03 implements against these.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → proxy.ts auth gate | Untrusted requests classified public/private by PUBLIC_ROUTES; a mis-classification either over-exposes a private route or 307→login-breaks a public/share link (#512) |
| nav render → role props | Role booleans computed server-side in (dashboard)/layout.tsx; client nav must not leak a surface's existence to the wrong role (T-45-01) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Denial of Service (broken share link) | proxy.ts PUBLIC_ROUTES vs route tree | mitigate | This plan ships the RED guard-unit test pinning Rule 2 (manifest-public ⊆ PUBLIC_ROUTES) — the #512 lockstep becomes machine-checkable before any move |
| T-51-02 | Information Disclosure | Sidebar buildNavSections role OR-logic | mitigate | Task 3 ships a GREEN role-leak pin (manager must not see allocator-only entries) that turns red if 51-03's completeness edit regresses T-45-01 |
| T-51-03 | Tampering | stripComments bypass on PUBLIC_ROUTES parse | mitigate | Task 2's comment-bypass carve-out asserts a commented-out `"/legal"` cannot satisfy the lockstep — clones the admin-guard's hardened tokenizer regression |
| T-51-SC | Tampering | npm installs | n/a | RESEARCH §Package Legitimacy Audit: zero package installs this phase — no slopcheck checkpoint required |

ASVS non-applicable this plan: V3 Session Management (no session/cookie logic), V6 Cryptography (none).
</threat_model>

<verification>
- `npx vitest run src/__tests__/check-route-contract.test.ts` is RED on assertions (not import errors).
- `npx vitest run src/components/layout/Breadcrumb.test.tsx src/components/layout/Sidebar.test.tsx src/components/layout/PageHeader.test.tsx` shows the new a11y-gap + PageHeader-breadcrumb assertions RED, the role-OR-logic pin GREEN.
- `npx tsc --noEmit` clean for the new route-contract files.
- `npm run lint` UNCHANGED (no new gate wired yet — that is 51-02).
</verification>

<success_criteria>
- The route-contract guard skeleton + data-only manifest module exist and compile; runCheck/findRouteFiles/stripComments are exported and dormant under vitest.
- A RED guard-unit test pins the 4 lockstep rules + the stripComments comment-bypass carve-out.
- RED breadcrumb + sidebar a11y tests pin the 3 UI-SPEC gaps; a RED PageHeader test pins the NAV-02 breadcrumb prop; a GREEN role-OR-logic pin locks T-45-01.
- No package installs; no lint wiring yet; no production component/route behavior changed.
</success_criteria>

<output>
Create `.planning/phases/51-shell-information-architecture-restructure/51-01-SUMMARY.md` when done
</output>
</content>
</invoke>
