---
phase: 51-shell-information-architecture-restructure
plan: 02
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - src/lib/routing/route-contract-manifest.ts
  - scripts/check-route-contract.ts
  - package.json
  - src/__tests__/contracts/contracts-registry.test.ts
  - src/__tests__/contracts/REGISTRY.md
autonomous: true
requirements: [NAV-03]
must_haves:
  truths:
    - "The route-contract guard runs in `npm run lint` and exits 1 when a page route is unclassified, a manifest-public route is missing from PUBLIC_ROUTES, a redirectFrom has no redirects() entry, or a manifest entry is stale"
    - "The full route-contract inventory (all 57 page routes + the /api/health and /auth/callback exceptions) is classified in ROUTE_CONTRACT_MANIFEST and the guard is GREEN against the live tree"
    - "The guard is registered in the contracts registry (CONTRACT_GUARDS + REGISTRY.md) so deleting/renaming it fails CI"
  artifacts:
    - path: "scripts/check-route-contract.ts"
      provides: "Complete runCheck implementing the 4 lockstep rules against the live src/app tree + parsed proxy.ts PUBLIC_ROUTES + next.config.ts redirects()"
      exports: ["runCheck", "findRouteFiles", "stripComments"]
    - path: "src/lib/routing/route-contract-manifest.ts"
      provides: "ROUTE_CONTRACT_MANIFEST classifying every page route (public/private/admin/exception) — the NAV-03 inventory SoT"
      contains: "ROUTE_CONTRACT_MANIFEST"
    - path: "package.json"
      provides: "lint chain invoking the route-contract guard + a check:route-contract script"
      contains: "check-route-contract"
  key_links:
    - from: "package.json"
      to: "scripts/check-route-contract.ts"
      via: "lint chain && tsx scripts/check-route-contract.ts"
      pattern: "tsx scripts/check-route-contract"
    - from: "src/__tests__/contracts/contracts-registry.test.ts"
      to: "scripts/check-route-contract.ts"
      via: "CONTRACT_GUARDS existence pin"
      pattern: "check-route-contract"
    - from: "scripts/check-route-contract.ts"
      to: "src/proxy.ts"
      via: "parse PUBLIC_ROUTES literal (Rule 2 lockstep)"
      pattern: "PUBLIC_ROUTES"
---

<objective>
Turn the Wave-0 guard contract GREEN: finalize runCheck to implement the 4 lockstep rules, populate ROUTE_CONTRACT_MANIFEST with the full 57-route inventory (the NAV-03 hard predecessor), wire the guard into `npm run lint`, and register it in the contracts registry. This permanently closes the #512 (307→login) drift class by construction — a route added/moved without a PUBLIC_ROUTES (or explicit private) classification + a redirect entry where applicable fails CI.

Purpose: The durable win of Phase 51. After this, the lockstep between the route tree, proxy.ts PUBLIC_ROUTES, and the redirect map is enforced at lint time, not hand-maintained.
Output: A GREEN guard wired into lint + registered in the registry, plus the complete route-contract inventory.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md
@.planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md
@.planning/phases/51-shell-information-architecture-restructure/51-01-SUMMARY.md

<interfaces>
<!-- From 51-01 (Wave 0) — extend, don't recreate. -->
scripts/check-route-contract.ts: runCheck(rootDir, manifest=ROUTE_CONTRACT_MANIFEST): string[]  (4 rules STUBBED — implement them here)
src/lib/routing/route-contract-manifest.ts: RouteClass, RouteEntry, ROUTE_CONTRACT_MANIFEST (seeded — populate fully here)
src/__tests__/check-route-contract.test.ts: the RED suite — must go GREEN once runCheck is complete.

<!-- The lockstep targets -->
src/proxy.ts L7 — the literal the guard parses:
  const PUBLIC_ROUTES = ["/login","/signup","/strategy","/factsheet","/api/factsheet","/browse","/api/keys","/api/trades","/api/verify-strategy","/api/alert-digest","/portfolio-pdf","/scenario-share","/api/benchmark/btc","/legal","/demo","/api/demo","/for-quants","/api/for-quants-lead","/security"];

next.config.ts — currently has rewrites()+headers(), NO redirects() yet (51-05 adds one). The guard's Rule 3 parses redirects() sources; an empty/absent redirects() means zero redirectFrom entries are satisfiable — which is correct for this plan (no moves yet).

package.json L11 (the lint chain to extend):
  "lint": "eslint --cache --cache-location node_modules/.cache/.eslintcache src/ && tsx scripts/check-admin-route-manifest.ts"
L19: "check:admin-route-manifest": "tsx scripts/check-admin-route-manifest.ts"

src/__tests__/contracts/contracts-registry.test.ts — CONTRACT_GUARDS array (L76-107), existence pin (L117-124), `>= 20` floor (L112). The check-admin-route-manifest.ts row (the analog) is the last entry.

<!-- The full inventory to encode — from RESEARCH §Route-Contract Inventory -->
PUBLIC page routes: / , /login /signup /forgot-password /reset-password (auth), /legal/* , /for-quants, /security, /demo /demo/founder-view, /browse/* , /factsheet/* , /strategy/* , /scenario-share/[token], /portfolio-pdf/[id]
PRIVATE dashboard: /allocations, /discovery/[slug]/* , /strategies/* , /portfolios/* , /profile, /compare, /decks, /referral, /recommendations, /scenarios (redirect-stub), /preferences (redirect-stub), /onboarding /pending-approval
ADMIN: /admin and all /admin/* (page routes)
EXCEPTIONS: /api/health (unauthenticated probe, NOT in PUBLIC_ROUTES — RESEARCH L139), /auth/callback (OAuth flow route.ts — RESEARCH L140)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete runCheck (4 lockstep rules) + populate the full inventory manifest</name>
  <files>scripts/check-route-contract.ts, src/lib/routing/route-contract-manifest.ts</files>
  <read_first>
    - scripts/check-route-contract.ts (the Task-1/51-01 skeleton — finish the 4 stubbed rules)
    - scripts/check-admin-route-manifest.ts (runCheck reference L363-412 — the violation-string + STALE-entry shape to mirror)
    - src/lib/routing/route-contract-manifest.ts (the seed to expand to all 57 routes)
    - src/proxy.ts (PUBLIC_ROUTES L7 to parse; the matcher semantics path===route || startsWith(route+"/"))
    - next.config.ts (rewrites()/headers() — Rule 3 parses a redirects() block; none exists yet, so redirectFrom set is empty this plan)
    - .planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md (§Route-Contract Inventory L72-141 — the full classification table incl. the two exception carve-outs)
  </read_first>
  <action>
    In scripts/check-route-contract.ts implement the 4 runCheck rules against the live tree:
      Rule 1 (classify): for every page.tsx found by findRouteFiles, derive its URL (strip src/app, drop (group) parens segments, [seg]→:seg) and require a ROUTE_CONTRACT_MANIFEST entry; emit an UNCLASSIFIED violation naming the file+derived-route otherwise.
      Rule 2 (#512 lockstep): for every manifest entry with class "public", parse PUBLIC_ROUTES out of proxy.ts (read the file, run stripComments, regex the `const PUBLIC_ROUTES = [...]` literal, extract the quoted prefixes) and require the route be covered by the prefix matcher (route === prefix || route.startsWith(prefix + "/")); emit MISSING-FROM-PUBLIC otherwise. Use the SAME matcher semantics proxy.ts uses so the guard and runtime agree.
      Rule 3 (redirect lockstep): for every manifest entry with `redirectFrom`, parse next.config.ts redirects() sources (stripComments + match the redirects() array of `{ source, destination }`) and require a matching `source === redirectFrom`; emit MISSING-REDIRECT otherwise. (No redirects() exists yet → no redirectFrom entries this plan → this rule is satisfied vacuously and stays ready for 51-05.)
      Rule 4 (no stale): for every manifest entry whose `class` is not `exception`, require a real page file resolves to its route; emit STALE otherwise. Exceptions (/api/health, /auth/callback) are route.ts/non-page paths and are explicitly skipped from the page-file existence check (mirror RESEARCH L139-140).
    Keep runCheck pure (returns string[]). Keep main()'s exit(1)-on-violations and the dormant-under-test guard intact.

    In src/lib/routing/route-contract-manifest.ts replace the seed with the FULL inventory: one RouteEntry per page route from RESEARCH §Route-Contract Inventory, alphabetical-by-route. Classify per the table: marketing/share/auth-form routes "public"; dashboard surfaces "private"; /admin/* "admin"; /api/health and /auth/callback "exception" with a notes field explaining why (unauthenticated probe / OAuth flow — not in PUBLIC_ROUTES by design). The redirect-stubs /scenarios and /preferences are "private" this plan (no redirectFrom yet — 51-05 converts /scenarios to a redirectFrom). Each entry's notes states why its class is correct.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/check-route-contract.test.ts && tsx scripts/check-route-contract.ts; echo "guard exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - the RED guard-unit test from 51-01 is now GREEN (verify: `npx vitest run src/__tests__/check-route-contract.test.ts` exits 0)
    - the guard runs clean against the live tree (verify: `tsx scripts/check-route-contract.ts` exits 0 with no violations printed)
    - the manifest classifies every page route — running the guard prints zero UNCLASSIFIED violations; deliberately deleting a manifest entry and re-running prints exactly one UNCLASSIFIED (smoke-test the rule, then restore)
    - /api/health and /auth/callback are class "exception" (verify: `grep -E '"/api/health"|"/auth/callback"' src/lib/routing/route-contract-manifest.ts` matches and both are within `class: "exception"` entries)
    - Rule 2 uses the proxy matcher semantics (verify: `grep -c "startsWith" scripts/check-route-contract.ts` >= 1)
  </acceptance_criteria>
  <done>runCheck implements all 4 rules GREEN against the live tree, the full 57-route inventory is classified, and the 51-01 RED suite passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guard into npm run lint + register in the contracts registry</name>
  <files>package.json, src/__tests__/contracts/contracts-registry.test.ts, src/__tests__/contracts/REGISTRY.md</files>
  <read_first>
    - package.json (lint chain L11 + check:admin-route-manifest L19 — the pattern to mirror)
    - src/__tests__/contracts/contracts-registry.test.ts (CONTRACT_GUARDS L76-107, the check-admin-route-manifest row at the end of the array, the `>= 20` floor L112, existence pin L117-124)
    - src/__tests__/contracts/REGISTRY.md (the human-readable table to add a row to)
    - .planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md (§contracts-registry.test.ts — the exact row to add)
  </read_first>
  <action>
    In package.json, append the guard to the lint chain so it runs after the admin-route guard: `... && tsx scripts/check-admin-route-manifest.ts && tsx scripts/check-route-contract.ts`. Add a sibling script `"check:route-contract": "tsx scripts/check-route-contract.ts"` mirroring check:admin-route-manifest.

    In src/__tests__/contracts/contracts-registry.test.ts add a CONTRACT_GUARDS row (after the check-admin-route-manifest.ts row): `{ path: "scripts/check-route-contract.ts", batch: "NAV-03", invariant: "ROUTE_CONTRACT_MANIFEST ↔ PUBLIC_ROUTES + redirects() lockstep (the #512 class, lint gate)" }`. The existence pin (L117-124) then locks it. The `>= 20` floor still holds (count grows by one). The contracts.yml wiring test needs no change (lint now invokes the new gate).

    In src/__tests__/contracts/REGISTRY.md add the matching human-readable row for scripts/check-route-contract.ts naming the NAV-03 invariant.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/contracts-registry.test.ts && npm run lint 2>&1 | tail -5; echo "lint exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` invokes the route-contract guard and exits 0 on the clean tree (verify: `grep -c 'tsx scripts/check-route-contract.ts' package.json` >= 1 AND `npm run lint` exits 0)
    - a `check:route-contract` script exists (verify: `grep -c '"check:route-contract"' package.json` returns 1)
    - the guard is registered (verify: `grep -c 'scripts/check-route-contract.ts' src/__tests__/contracts/contracts-registry.test.ts src/__tests__/contracts/REGISTRY.md` >= 2)
    - the contracts-registry suite is GREEN incl. the existence pin (verify: `npx vitest run src/__tests__/contracts/contracts-registry.test.ts` exits 0)
    - lockstep proof: temporarily add a fake public manifest entry not in PUBLIC_ROUTES, run `npm run lint` → it exits 1 with a MISSING-FROM-PUBLIC violation; restore and confirm exit 0 (the #512 gate works by construction)
  </acceptance_criteria>
  <done>The route-contract guard is wired into lint + registered in the contracts registry; CI fails by construction on a PUBLIC_ROUTES/redirect lockstep break.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon client → proxy.ts auth gate | PUBLIC_ROUTES classification gates anon access; a public route absent from PUBLIC_ROUTES → 307→login (#512); a private route wrongly listed public → over-exposure |
| route tree → manifest classification | A new/moved route must be classified; an unclassified route is a silent gap the guard now refuses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Denial of Service (broken share link) | proxy.ts PUBLIC_ROUTES ↔ route tree | mitigate | Rule 2 fails lint when a manifest-public route is not covered by PUBLIC_ROUTES — the #512 lockstep is now enforced at lint time (proven by the lockstep smoke-test in Task 2) |
| T-51-04 | Information Disclosure / Elevation | a private route mis-classified public | mitigate | The manifest is the explicit classification SoT; a route is "public" only if intentionally listed AND covered by PUBLIC_ROUTES; page-level getUser() remains the defense-in-depth backstop (unchanged) |
| T-51-05 | Tampering | redirects() open-redirect / stale lockstep | mitigate | Rule 3 requires every redirectFrom map to a static redirects() source (no user input); the guard rejects a redirectFrom with no matching config entry |
| T-51-06 | Tampering | stripComments bypass parsing proxy.ts | mitigate | runCheck parses PUBLIC_ROUTES through the hardened stripComments tokenizer (cloned verbatim from the admin guard); the 51-01 comment-bypass test pins it |
| T-51-SC | Tampering | npm installs | n/a | Zero package installs this phase (RESEARCH §Package Legitimacy Audit) — no checkpoint required |

ASVS: V1 Architecture (the guard documents+enforces the public/private trust boundary) — satisfied. V4 Access Control — satisfied (PUBLIC_ROUTES allowlist lockstep enforced). V3/V6 non-applicable (no session/crypto change).
</threat_model>

<verification>
- `npx vitest run src/__tests__/check-route-contract.test.ts` GREEN (the 51-01 RED contract now passes).
- `tsx scripts/check-route-contract.ts` exits 0 on the live tree.
- `npm run lint` exits 0 and invokes the guard; the lockstep smoke-test (fake public entry) makes it exit 1.
- `npx vitest run src/__tests__/contracts/contracts-registry.test.ts` GREEN incl. the existence pin.
</verification>

<success_criteria>
- The route-contract guard is GREEN against the live tree and enforces the 4 lockstep rules.
- The full 57-route inventory is classified in ROUTE_CONTRACT_MANIFEST (the NAV-03 SoT), with /api/health + /auth/callback as documented exceptions.
- The guard runs in `npm run lint` and is registered in CONTRACT_GUARDS + REGISTRY.md.
- A deliberate PUBLIC_ROUTES/redirect lockstep break fails CI by construction (the #512 class is closed).
</success_criteria>

<output>
Create `.planning/phases/51-shell-information-architecture-restructure/51-02-SUMMARY.md` when done
</output>
