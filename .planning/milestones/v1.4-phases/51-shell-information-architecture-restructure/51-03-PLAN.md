---
phase: 51-shell-information-architecture-restructure
plan: 03
type: execute
wave: 2
depends_on: ["51-01"]
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Breadcrumb.tsx
  - src/components/layout/PageHeader.tsx
autonomous: true
requirements: [NAV-01, NAV-02]
must_haves:
  truths:
    - "Every genuine orphan allocator surface (/compare, /decks, /referral, /recommendations) is reachable from its role's nav OR via a parent + a breadcrumb back-path — no dead end"
    - "The desktop sidebar active item exposes aria-current=page and a focus-visible accent ring; the role OR-logic (T-45-01) is unchanged"
    - "The breadcrumb leaf exposes aria-current=page and linked crumbs carry a focus-visible accent ring; PageHeader can render a curated breadcrumb above the H1"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Nav completeness (orphan entries inside the correct role-gated section) + NavItemLink aria-current + focus-visible"
      contains: "aria-current"
    - path: "src/components/layout/Breadcrumb.tsx"
      provides: "aria-current on the leaf + focus-visible ring on linked crumbs"
      contains: "aria-current"
    - path: "src/components/layout/PageHeader.tsx"
      provides: "optional breadcrumb prop rendering <Breadcrumb> above the H1"
      contains: "breadcrumb"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "usePathname active rule"
      via: "pathname === href || pathname.startsWith(href + '/')"
      pattern: "startsWith\\(item.href"
    - from: "src/components/layout/PageHeader.tsx"
      to: "src/components/layout/Breadcrumb.tsx"
      via: "renders <Breadcrumb items={breadcrumb}/>"
      pattern: "Breadcrumb"
---

<objective>
Make "where am I / how do I get back" answerable on every surface: complete the role-scoped nav so the genuine orphan allocator surfaces are reachable, close the 3 UI-SPEC a11y gaps (sidebar + breadcrumb aria-current + focus-visible accent ring), and single-source breadcrumbs through PageHeader so dashboard surfaces get a consistent back-path. All additive onto the live shell — the role OR-logic (T-45-01) and the v1.3 mobile shell stay untouched.

Purpose: NAV-01 (no orphan/dead-end surfaces, role-scoped) + NAV-02 (breadcrumbs + consistent active/hover/focus + back-paths app-wide), turning the 51-01 RED a11y tests + the 51-01 RED PageHeader breadcrumb-prop test GREEN.
Output: Updated Sidebar/Breadcrumb/PageHeader + GREEN a11y + PageHeader-breadcrumb tests + the role-OR-logic pin still GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md
@.planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md
@.planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md
@.planning/phases/51-shell-information-architecture-restructure/51-01-SUMMARY.md

<interfaces>
<!-- The shell components to evolve additively — extracted so no exploration is needed. -->

src/components/layout/Sidebar.tsx:
  buildNavSections(populatedSlugs, isAdmin, isAllocator, flaggedCount, isManager) — role OR-logic (DO NOT REGRESS):
    showsAllocatorWorkspace = isAllocator || isAdmin   (L34)
    showsManagerWorkspace   = isManager || isAdmin     (L35)
    showsDiscovery          = isAllocator || isAdmin   (L36)
    workspaceItems.push({...}) inside the showsAllocatorWorkspace branch  (L59-66)
  NavItem shape (L9-15); inline 16×16 stroke-1.5 SVG icons house style (L336-425) — reuse an existing glyph, no icon dependency.
  NavItemLink (L301-334): active rule L308 `pathname === item.href || pathname.startsWith(item.href + "/")` (KEEP — SSR-safe, no useSearchParams); the <Link> currently has NO aria-current and NO focus-visible.
  buildPrimaryMobileNav (L145-195) — mobile twin, LOCKED v1.3; mirrors the SAME OR-logic; ≤5 cells. Do NOT restructure.

src/components/layout/MobileNav.tsx (the pattern to mirror — already closes both gaps):
  aria-current={active ? "page" : undefined}                                  (L70)
  "focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent" (L76)

src/components/layout/Breadcrumb.tsx (47 lines):
  <nav aria-label="Breadcrumb"> (L15); linked crumb <Link className="hover:text-text-primary transition-colors"> (L33); leaf <span className="text-text-primary font-medium"> (L40). Takes curated `items: BreadcrumbItem[]` (KEEP).

src/components/layout/PageHeader.tsx:
  PageHeaderProps { title; description?; actions?; meta? }  (L3-9); single <h1> (L15-17), text-[32px] (KEEP — no type churn this phase). Has NO breadcrumb prop yet — this plan ADDS it, turning the 51-01 RED PageHeader.test.tsx GREEN.

<!-- Orphan ownership (verified) -->
/compare — allocator (uses Breadcrumb already), /decks — allocator, /referral — allocator/manager ("Earn rewards by referring…"), /recommendations — allocator (accredited-gated, mandate-CTA-reachable today).
/discovery has NO bare page.tsx → nav must target a concrete slug /discovery/crypto-sma (bare /discovery 404s).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sidebar — nav completeness + aria-current + focus-visible (role OR-logic preserved)</name>
  <files>src/components/layout/Sidebar.tsx</files>
  <behavior>
    - The active NavItemLink renders aria-current="page"; inactive items render no aria-current (turns 51-01 RED → GREEN).
    - Every NavItemLink className includes the focus-visible accent ring/outline (keyboard-only), never bare focus:.
    - /compare, /decks, /referral, /recommendations are each reachable: either a labeled nav entry inside the allocator/account role-gated section OR (for a child-of-workspace surface) reachable via its parent + a breadcrumb back-path — never a dead end.
    - A manager-only render (isManager=true, isAllocator=false, isAdmin=false) does NOT show any allocator-workspace-only entry; an allocator render does (the 51-01 role-OR-logic pin stays GREEN).
  </behavior>
  <read_first>
    - src/components/layout/Sidebar.tsx (buildNavSections L19-114 incl. role OR-logic L34-36 and workspaceItems.push L59-66; NavItemLink L301-334; icon glyphs L336-425)
    - src/components/layout/MobileNav.tsx (the aria-current L70 + focus-visible L76 pattern to mirror)
    - src/components/layout/Sidebar.test.tsx (the 51-01 RED a11y assertions + the role-OR-logic pin to satisfy)
    - .planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md (§Item state contract L162-176, §Role-scoped nav COMPLETENESS L182-192, §Mobile parity L194-201)
    - .planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md (Pitfall 5 L399-403 role-OR-logic; Pitfall 6 redirect-stubs; NAV-01 orphan set)
  </read_first>
  <action>
    In NavItemLink (L301-334) add additively to the existing <Link>: `aria-current={active ? "page" : undefined}` and append `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent` to the className. Keep the active rule at L308 EXACTLY (no useSearchParams). Keep the active background `bg-sidebar-active` slate (accent ring on the dark rail; accent fill on navy fails contrast per UI-SPEC §Color).

    In buildNavSections complete the genuine orphans WITHOUT touching the `||` derivations: add labeled nav entries for /compare, /decks (and /referral if placed in nav) INSIDE the existing `showsAllocatorWorkspace` MY WORKSPACE section (alongside workspaceItems.push L59-66), and /referral inside ACCOUNT if that reads better as an account affordance — each with an existing reused 16×16 glyph (no icon dep). For /recommendations and any surface that is genuinely a child reached by an in-page control (mandate-CTA), it is acceptable to leave it OUT of top-level nav and instead rely on its parent + a breadcrumb back-path (Task 2/Task 3 deliver the breadcrumb) — document that choice in the SUMMARY per UI-SPEC §completeness. Do NOT add nav entries for /scenarios or /preferences (redirect-stubs) or /security (public marketing, reachable via the marketing shell). Never nav-link bare /discovery (404) — discovery entries already target concrete slugs; keep that.

    If any new orphan would surface as a mobile primary cell, mirror buildPrimaryMobileNav's SAME OR-logic and keep distinct hrefs + the ≤5 cap; otherwise leave the LOCKED v1.3 mobile nav structurally unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Sidebar.test.tsx src/components/layout/MobileNav.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - the 51-01 Sidebar a11y assertions are GREEN (verify: `npx vitest run src/components/layout/Sidebar.test.tsx` exits 0)
    - NavItemLink carries aria-current + focus-visible accent (verify: `grep -Ec 'aria-current|focus-visible:.*accent' src/components/layout/Sidebar.tsx` >= 2)
    - the role OR-logic is byte-unchanged (verify: `grep -E 'showsAllocatorWorkspace = isAllocator \|\| isAdmin|showsManagerWorkspace = isManager \|\| isAdmin|showsDiscovery = isAllocator \|\| isAdmin' src/components/layout/Sidebar.tsx` matches all three)
    - no useSearchParams introduced (verify: `grep -c useSearchParams src/components/layout/Sidebar.tsx` returns 0)
    - the orphan entries point at concrete hrefs, never bare /discovery (verify: `grep -c '"/discovery"' src/components/layout/Sidebar.tsx` returns 0; any /compare /decks /referral entries use exact paths)
  </acceptance_criteria>
  <done>The genuine orphans are reachable, the sidebar active/focus a11y gaps are closed, and T-45-01 role OR-logic is provably preserved. (The route-contract guard-green proof belongs to 51-02 — these nav entries are pre-classified private routes that cannot break the guard.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Breadcrumb a11y + PageHeader breadcrumb prop (single-sourced back-path)</name>
  <files>src/components/layout/Breadcrumb.tsx, src/components/layout/PageHeader.tsx</files>
  <behavior>
    - The breadcrumb leaf (current page) renders aria-current="page" (turns 51-01 RED → GREEN); intermediate linked crumbs do not.
    - Linked crumbs carry the focus-visible accent ring (keyboard-only), never bare focus:.
    - PageHeader accepts an optional `breadcrumb?: BreadcrumbItem[]`; when passed it renders <Breadcrumb items={breadcrumb}/> above the <h1> (turns the 51-01 RED PageHeader.test.tsx GREEN); when omitted PageHeader renders exactly as today (additive, no regression).
  </behavior>
  <read_first>
    - src/components/layout/Breadcrumb.tsx (whole file — leaf span L40, linked Link L33, the curated items prop L3-10)
    - src/components/layout/PageHeader.tsx (PageHeaderProps L3-9, single <h1> L15-17 — no breadcrumb prop today)
    - src/components/layout/MobileNav.tsx (aria-current L70 pattern)
    - src/components/layout/Breadcrumb.test.tsx (the 51-01 RED leaf-aria-current + linked-focus-visible assertions)
    - src/components/layout/PageHeader.test.tsx (the 51-01 RED breadcrumb-prop assertions — Breadcrumb landmark above h1 when passed; none when omitted)
    - .planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md (§Breadcrumb Contract L210-238, §Back-Path Convention L242-261, §Hierarchy derivation L223-229)
    - .planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md (Pattern 3a — keep curated items, NOT segment auto-derivation; Pitfall 4 — no raw-UUID crumbs)
  </read_first>
  <action>
    In Breadcrumb.tsx (additive): add `aria-current="page"` to the leaf <span> (L40); append `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent` to the linked-crumb <Link> className (L33). Keep the <nav aria-label="Breadcrumb"> wrapper, the curated `items` prop (do NOT auto-derive from segments — avoids raw-UUID crumbs on [id]/[token]), the chevron separator, and the muted-text color (AA-passing #64748B). For long leaf labels, keep legitimate truncation only (`truncate max-w-[…]` + `title={fullLabel}`); never clip the leaf so the page identity is unreadable.

    In PageHeader.tsx add an optional `breadcrumb?: BreadcrumbItem[]` to PageHeaderProps; when present, render `<Breadcrumb items={breadcrumb} />` directly above the <h1> (import BreadcrumbItem/Breadcrumb from ./Breadcrumb). Keep the single <h1> and `text-[32px]` unchanged (no type churn this phase). Surfaces that do not pass `breadcrumb` render identically to today. This turns the 51-01 RED PageHeader.test.tsx GREEN.

    Do NOT retrofit every page's breadcrumb invocation in this plan — the prop + a11y are the mechanism; the back-path convention is satisfied app-wide because the breadcrumb is now consistently available through PageHeader and the active nav item highlights the parent. Surfaces already passing a curated Breadcrumb (compare, discovery/[slug], strategies/[id]/edit, portfolios/*) continue to work unchanged.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Breadcrumb.test.tsx src/components/layout/PageHeader.test.tsx 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - the 51-01 Breadcrumb a11y assertions are GREEN (verify: `npx vitest run src/components/layout/Breadcrumb.test.tsx` exits 0)
    - the leaf has aria-current and linked crumbs have focus-visible accent (verify: `grep -Ec 'aria-current="page"|focus-visible:.*accent' src/components/layout/Breadcrumb.tsx` >= 2)
    - the 51-01 PageHeader breadcrumb-prop assertions are GREEN — the prop renders a <Breadcrumb> above the h1 when passed and nothing when omitted (verify: `npx vitest run src/components/layout/PageHeader.test.tsx` exits 0)
    - PageHeader has the optional breadcrumb prop wired to <Breadcrumb> (verify: `grep -c 'breadcrumb' src/components/layout/PageHeader.tsx` >= 1 AND `grep -c 'Breadcrumb' src/components/layout/PageHeader.tsx` >= 1)
    - the breadcrumb still uses curated items, not segment auto-derivation (verify: `grep -c 'useSelectedLayoutSegments\|usePathname' src/components/layout/Breadcrumb.tsx` returns 0)
  </acceptance_criteria>
  <done>The breadcrumb a11y gaps are closed and PageHeader single-sources a curated breadcrumb back-path additively (the 51-01 RED PageHeader.test.tsx is now GREEN), with no type churn and no segment-derivation regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| nav render → role props | Role booleans come from the server (dashboard layout); the client nav must not surface a manager/admin-only destination to an allocator or vice versa (T-45-01) |
| nav/breadcrumb link → route | Every link must resolve to real content (no 307→login, no bare /discovery 404) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-02 | Information Disclosure | Sidebar buildNavSections | mitigate | Orphan entries are added INSIDE existing role-gated sections; the `||` derivations are byte-unchanged; the 51-01 role-leak pin (manager must not see allocator-only) stays GREEN — asserted in Task 1 acceptance |
| T-51-07 | Denial of Service (dead link) | nav links to bare /discovery (404) | mitigate | UI-SPEC §Route-Contract Notes: any [slug]-only nav target uses a concrete landing slug; acceptance asserts no bare "/discovery" href. (The route-contract guard-green proof is 51-02's deliverable; these nav entries are pre-classified private routes that cannot break the guard.) |
| T-51-08 | Information Disclosure | breadcrumb on dynamic [id]/[token] segments | mitigate | Curated `items` (not segment auto-derivation) — no raw-UUID/token crumb is ever rendered (Pitfall 4); leaf truncation keeps page identity legible |
| T-51-SC | Tampering | npm installs | n/a | Zero package installs (RESEARCH §Package Legitimacy Audit) |

ASVS V4 Access Control — satisfied (role OR-logic preserved). V3/V6 non-applicable (no session/crypto change). V5 Input Validation — n/a (no user input; breadcrumb items are server-curated labels).
</threat_model>

<verification>
- `npx vitest run src/components/layout/*.test.tsx` GREEN — the 51-01 a11y RED assertions + the 51-01 PageHeader breadcrumb-prop assertions now pass, the role-OR-logic pin still passes, the LOCKED MobileNav test unchanged.
- `npm run lint` exits 0.
- Manual (post-merge): walk allocator/manager/admin nav — every owned surface reachable, current location legible, a back-path present, no dead end.
</verification>

<success_criteria>
- Genuine orphan allocator surfaces are reachable (nav entry or parent + breadcrumb back-path) — no dead end.
- The 3 UI-SPEC a11y gaps are closed (sidebar + breadcrumb aria-current + focus-visible accent), turning the 51-01 RED tests GREEN.
- PageHeader single-sources a curated breadcrumb back-path additively (the 51-01 RED PageHeader.test.tsx is now GREEN), no type churn.
- T-45-01 role OR-logic preserved (the role-leak pin stays GREEN); the LOCKED v1.3 mobile shell unchanged.
</success_criteria>

<output>
Create `.planning/phases/51-shell-information-architecture-restructure/51-03-SUMMARY.md` when done
</output>
</content>
</invoke>
