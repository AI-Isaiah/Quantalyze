---
phase: 51-shell-information-architecture-restructure
plan: 04
type: execute
wave: 3
depends_on: ["51-02"]
files_modified:
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/page.tsx
  - src/app/(marketing)/for-quants/page.tsx
  - src/app/(marketing)/security/page.tsx
  - src/app/(marketing)/legal/layout.tsx
  - src/app/(marketing)/legal/privacy/page.tsx
  - src/app/(marketing)/legal/terms/page.tsx
  - src/app/(marketing)/legal/disclaimer/page.tsx
  - src/app/(marketing)/demo/layout.tsx
  - src/app/(marketing)/demo/page.tsx
  - src/app/(marketing)/demo/founder-view/page.tsx
  - src/lib/routing/route-contract-manifest.ts
  - e2e/marketing-shell.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [NAV-04]
must_haves:
  truths:
    - "Landing (/), /legal/*, /for-quants, /security, /demo, /demo/founder-view all resolve at their CURRENT URLs after moving into the (marketing) route group (zero URL change)"
    - "Each wrapped page keeps its own metadata export, exactly one <main> and exactly one <h1> (no duplicate landmark — the JOURNEY-03 axe class), and stays anon-reachable (in PUBLIC_ROUTES, no 307→login)"
    - "The (marketing) layout owns shared header/footer chrome only (server-rendered); the demo banner and legal tab-nav survive as nested layouts; the landing authed redirect stays in the page"
  artifacts:
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Shared server-rendered marketing header (Sign in/Sign up) + LegalFooter mounted once"
      contains: "LegalFooter"
    - path: "src/app/(marketing)/demo/layout.tsx"
      provides: "Nested layout preserving the demo banner (distinct chrome)"
      contains: "main"
    - path: "e2e/marketing-shell.spec.ts"
      provides: "Anon canary asserting each marketing route resolves at its URL (<400, single main h1, public)"
      contains: "toBeLessThan(400)"
  key_links:
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/legal/LegalFooter.tsx"
      via: "shared footer mounted once"
      pattern: "LegalFooter"
    - from: "e2e/marketing-shell.spec.ts"
      to: ".github/workflows/ci.yml"
      via: "wired into the UNSEEDED playwright list (FLOW-01: else it never runs)"
      pattern: "marketing-shell"
    - from: "src/lib/routing/route-contract-manifest.ts"
      to: "src/app/(marketing)/**"
      via: "guard derives (marketing) routes to their URLs (drops the (group) segment)"
      pattern: "marketing"
---

<objective>
Introduce the `(marketing)` Next route group (parens → ZERO URL change) wrapping the public marketing/info routes — landing, /legal/*, /for-quants, /security, /demo — in ONE shared server-rendered shell, replacing today's duplicated per-page headers, WITHOUT changing any public URL or regressing SEO. The route-contract guard (51-02) proves the move's lockstep: each route's derived URL is unchanged, so it stays classified public and in PUBLIC_ROUTES by construction.

Purpose: NAV-04 — a shared shell with the proxy allowlist + the route-contract manifest in lockstep, SEO and per-page metadata preserved.
Output: The `(marketing)` group + nested layouts (demo banner, legal tab-nav) + an anon canary wired into CI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md
@.planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md
@.planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md
@.planning/phases/51-shell-information-architecture-restructure/51-02-SUMMARY.md

<interfaces>
<!-- The chrome to consolidate + the analogs. Extracted so no exploration is needed. -->

src/app/(auth)/layout.tsx — the group-layout mechanism analog: a plain server component wrapping {children}; the (parens) segment is folder-only, ZERO URL change.

src/app/page.tsx (landing) — the masthead to HOIST into (marketing)/layout.tsx (current header, L49-72):
  <header className="border-b border-border bg-white"> · inner h-16 max-w-6xl px-6 flex items-center justify-between
  logo <Link href="/" className="...font-display text-lg tracking-tight..."> Quantalyze
  Log in <Link href="/login" ...min-h-[44px]...>  +  Get Started <Link href="/signup" ...bg-accent...>
  UI-SPEC §Marketing Shell renames CTAs to "Sign in"/"Sign up" and adds focus-visible:ring-2 ring-accent.
  CRITICAL: the authed redirect `if (user) redirect("/discovery/crypto-sma")` (page.tsx L40-42) STAYS in the page, NOT the layout.

src/app/legal/layout.tsx — distinct chrome to PRESERVE as a nested layout: TABS [privacy/terms/disclaimer] tab-nav; its own `export const metadata` (title "Legal — …"); <main className="mx-auto max-w-3xl px-6 py-12">. When nested under (marketing), DROP the legal <header> masthead (the shared header now provides it) and KEEP ONLY the tab-nav + <main> + metadata.

src/app/demo/layout.tsx — distinct chrome to PRESERVE as a nested layout: the sticky DemoBanner ("Live demo — simulated allocator data" + "Sign up →"), with its A11Y-01 single-<main> discipline (the banner is <header>, the page body owns the single <main>). KEEP as (marketing)/demo/layout.tsx.

src/app/for-quants/layout.tsx — has its own header (Sign in/Sign up). Under (marketing) the shared header replaces it; the page's "Request a Call" modal stays in the page tree.

src/app/security/page.tsx — has its OWN hand-rolled <header> in the PAGE + a rich `export const metadata` (title, description, alternates.canonical "/security", robots index/follow, openGraph). PRESERVE the metadata VERBATIM; drop the page's hand-rolled masthead in favor of the shared header; keep the single <main>/<h1>.

src/components/legal/LegalFooter.tsx — the shared footer (4 legal links + copyright); mount ONCE in (marketing)/layout.tsx and remove the per-page/per-layout mounts under the group.

next.config.ts — the `/demo/:path*` Cache-Control + Vary:Cookie headers (L71-82) are keyed on the /demo URL; since the route group is folder-only the /demo URL is UNCHANGED, so these still match. Do NOT touch next.config.ts.

proxy.ts — PUBLIC_ROUTES (L7) + isAuthBounceExempt (isDemoRoute/isForQuantsRoute/isSecurityRoute/isLegalRoute) match on URL; URLs unchanged → NO proxy edit needed. Verify, do not rewrite.

e2e/axe-app-wide.spec.ts PUBLIC_ROUTES matrix (L82-88) — already scans / , /security, /for-quants, /browse, /demo at desktop+mobile with a status<400 + visible-anchor gate (main h1). The marketing-shell spec adds the same-URL + still-public assertions.
ci.yml UNSEEDED playwright list (the line running axe-app-wide.spec.ts + security-page etc., ~L1073) — a NEW public e2e MUST be appended here or it never runs (FLOW-01).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the (marketing) group + move pages + nested layouts (zero URL change, per-page metadata preserved)</name>
  <files>src/app/(marketing)/layout.tsx, src/app/(marketing)/page.tsx, src/app/(marketing)/for-quants/page.tsx, src/app/(marketing)/security/page.tsx, src/app/(marketing)/legal/layout.tsx, src/app/(marketing)/demo/layout.tsx, src/lib/routing/route-contract-manifest.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route-groups.md (folder-only / zero-URL-change semantics + the multiple-root-layout caveat — NOT applicable here, (marketing) shares the existing root app/layout.tsx)
    - src/app/(auth)/layout.tsx (the group-layout mechanism analog)
    - src/app/page.tsx (the masthead to hoist L49-72 + the authed redirect L40-42 that STAYS in the page)
    - src/app/legal/layout.tsx + src/app/demo/layout.tsx (the distinct chrome to preserve as nested layouts — RESEARCH Pitfall 3)
    - src/app/security/page.tsx (the metadata to preserve verbatim + the hand-rolled header to drop)
    - src/app/for-quants/layout.tsx (the per-page header the shared layout replaces)
    - src/components/legal/LegalFooter.tsx (the shared footer to mount once)
    - .planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md (§Marketing Shell Contract L293-337 — header/footer/SEO rules)
    - .planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md (Pitfall 2 metadata/SEO loss; Pitfall 3 per-route-layout collision; Runtime State L359-371 /demo headers)
  </read_first>
  <action>
    Create the `src/app/(marketing)/` route group and MOVE the marketing pages into it (folder move — zero URL change; use `git mv` so history is preserved where possible):
      - landing: src/app/page.tsx → src/app/(marketing)/page.tsx (KEEP the authed `redirect("/discovery/crypto-sma")` in the page; REMOVE the now-duplicated hand-rolled <header> from the page since the group layout provides it; keep the page's single <main>/<h1> and its content).
      - /for-quants: src/app/for-quants/page.tsx → src/app/(marketing)/for-quants/page.tsx; DELETE src/app/for-quants/layout.tsx (its header is replaced by the shared layout; the page keeps its modal/CTA).
      - /security: src/app/security/page.tsx → src/app/(marketing)/security/page.tsx; PRESERVE its `export const metadata` VERBATIM (title/description/alternates.canonical/robots/openGraph); drop only the page's hand-rolled <header> masthead; keep single <main>/<h1>.
      - /legal: move src/app/legal/* → src/app/(marketing)/legal/*; convert legal/layout.tsx → src/app/(marketing)/legal/layout.tsx keeping ONLY the TABS tab-nav + <main className="mx-auto max-w-3xl …"> + `export const metadata`; DROP the legal <header> masthead (shared header now provides it) and DROP its own LegalFooter mount (the group layout mounts it once).
      - /demo: move src/app/demo/* → src/app/(marketing)/demo/*; keep demo/layout.tsx → src/app/(marketing)/demo/layout.tsx with the DemoBanner intact and its single-<main> A11Y-01 discipline; founder-view page moves with it.

    Create src/app/(marketing)/layout.tsx as a SERVER component (no "use client"): hoist the landing masthead (logo → "/", "Sign in" → /login, "Sign up" → /signup Button-primary accent), add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent` to each link, keep `min-h-[44px]` tap targets, wrap `{children}`, and mount `<LegalFooter />` ONCE below children. The layout owns header/footer chrome ONLY — do NOT add a `<main>`, a `<h1>`, or any `metadata`/`generateMetadata` (each page keeps its own).

    Leave src/app/browse/* UNGROUPED (CONTEXT excludes it — it is the separate un-gated SEO mirror of discovery). Do NOT touch next.config.ts or proxy.ts (URLs unchanged → /demo cache headers, PUBLIC_ROUTES, and isAuthBounceExempt all still match).

    Update src/lib/routing/route-contract-manifest.ts ONLY if a route entry's derived path changed (it should NOT — the group is folder-only; the guard drops the (group) segment so /security etc. derive identically). Re-run the guard; if it flags anything, the move broke a URL — fix the move, not the manifest.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "Route|error|✓|Compiled" | head -40; echo "---guard---"; tsx scripts/check-route-contract.ts; echo "guard exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - the (marketing) group exists with all pages moved (verify: `ls "src/app/(marketing)/page.tsx" "src/app/(marketing)/security/page.tsx" "src/app/(marketing)/for-quants/page.tsx" "src/app/(marketing)/legal/layout.tsx" "src/app/(marketing)/demo/layout.tsx"` all exist) and the old paths are gone (verify: `ls src/app/page.tsx src/app/security 2>&1 | grep -c 'No such file\|cannot access'` >= 1)
    - `npm run build` succeeds and the route manifest shows /, /security, /for-quants, /legal/privacy, /demo at their UNCHANGED URLs (verify: build "Route (app)" table lists `/security`, `/for-quants`, `/legal/privacy`, `/demo` — NOT `/(marketing)/security`)
    - the route-contract guard exits 0 (verify: `tsx scripts/check-route-contract.ts` exits 0 — proves no URL drift / no PUBLIC_ROUTES lockstep break)
    - the (marketing) layout is server-rendered and owns no h1/main/metadata (verify: `grep -c '"use client"\|<h1\|export const metadata\|<main' "src/app/(marketing)/layout.tsx"` returns 0; `grep -c 'LegalFooter' "src/app/(marketing)/layout.tsx"` >= 1)
    - security metadata preserved verbatim (verify: `grep -c 'alternates\|canonical\|openGraph' "src/app/(marketing)/security/page.tsx"` >= 2)
    - the landing authed redirect stays in the page (verify: `grep -c 'redirect("/discovery/crypto-sma")' "src/app/(marketing)/page.tsx"` >= 1)
    - next.config.ts and proxy.ts UNCHANGED (verify: `git diff --name-only | grep -Ec 'next.config.ts|src/proxy.ts'` returns 0)
  </acceptance_criteria>
  <done>The (marketing) group wraps every marketing route at its unchanged URL with one shared server-rendered shell, distinct chrome preserved via nested layouts, per-page metadata intact, and the guard proves zero URL/allowlist drift.</done>
</task>

<task type="auto">
  <name>Task 2: marketing-shell anon canary + axe single-landmark assertions, wired into CI</name>
  <files>e2e/marketing-shell.spec.ts, .github/workflows/ci.yml</files>
  <read_first>
    - e2e/security-page.spec.ts (the anon-reachable status<400 + visible-h1 canary shape, L29-40)
    - e2e/axe-app-wide.spec.ts (the PUBLIC_ROUTES matrix L82-88 + the status<400 + visible-anchor gate + the HAS_SEED_ENV header comment + the FLOW-01 CI-wiring note L29-47)
    - .github/workflows/ci.yml (the UNSEEDED playwright list ~L1073 that runs axe-app-wide.spec.ts + security-page — append the new spec here)
    - .planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md (§e2e/marketing-shell.spec.ts — the exact assertions + the BOTH-places CI wiring requirement)
    - .planning/phases/51-shell-information-architecture-restructure/51-UI-SPEC.md (§SEO/chrome safety L321-337 — single <main>/<h1>, no client boundary)
  </read_first>
  <action>
    Create e2e/marketing-shell.spec.ts (PUBLIC / unseeded — no HAS_SEED_ENV gate). For each marketing route { /, /security, /for-quants, /legal/privacy, /legal/terms, /legal/disclaimer, /demo, /demo/founder-view } as an ANONYMOUS visitor (no auth): assert `res.status()` < 400 (NOT a 307→login — the #512 assertion), assert exactly one `<main>` landmark and exactly one level-1 heading are present and visible (the JOURNEY-03 single-landmark class), and assert the page still resolves at its requested URL (no redirect away). Mirror the security-page.spec shape; keep it deterministic (no seed dependency).

    Wire the new spec into BOTH required places (FLOW-01): the spec needs no HAS_SEED_ENV const (it is public/unseeded), so the single wiring point is .github/workflows/ci.yml — append `e2e/marketing-shell.spec.ts` to the UNSEEDED `npx playwright test …` list that already runs axe-app-wide.spec.ts (~L1073). Verify the spec is discovered by playwright locally before relying on CI.

    Do NOT add this spec to any seeded/authed list (it is public). Do NOT introduce a client boundary anywhere in the marketing shell that would defer the <h1>/metadata.
  </action>
  <verify>
    <automated>npx playwright test e2e/marketing-shell.spec.ts --list 2>&1 | tail -20; echo "---ci-wired---"; grep -c 'marketing-shell.spec.ts' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - the spec exists and asserts status<400 + single main + single h1 + same-URL for each marketing route (verify: `grep -Ec 'toBeLessThan\(400\)|getByRole\("main"\)|level: 1|heading' e2e/marketing-shell.spec.ts` >= 3)
    - the spec is wired into the UNSEEDED ci.yml playwright list (verify: `grep -c 'marketing-shell.spec.ts' .github/workflows/ci.yml` >= 1 AND it is NOT inside a HAS_SEED_ENV-gated block)
    - playwright discovers the spec (verify: `npx playwright test e2e/marketing-shell.spec.ts --list` lists the tests, exit 0)
    - the spec has NO seed/auth dependency (verify: `grep -c 'HAS_SEED_ENV\|qa-demo\|setSession' e2e/marketing-shell.spec.ts` returns 0)
  </acceptance_criteria>
  <done>An anon canary proves every (marketing) route resolves at its unchanged URL as a single-landmark public page, and it runs in CI by being wired into the unseeded playwright list (FLOW-01 satisfied).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon crawler/visitor → (marketing) routes | Marketing routes must stay anon-reachable (public) at their exact URLs and crawlable (metadata/canonical intact) — a folder move must not change a URL or move a page out of PUBLIC_ROUTES |
| shared layout → per-page SEO | The shared layout must not hoist/suppress any page's metadata/<h1> (SEO + landmark integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Denial of Service (broken public/share link) | (marketing) URL stability ↔ PUBLIC_ROUTES | mitigate | Route group is folder-only (verified vs route-groups.md); the route-contract guard (51-02) exits 0 only if every derived URL is unchanged + still public; the anon canary asserts <400 (not 307→login) for /security /legal/* /demo (share-token-class public links) |
| T-51-03 | Information Disclosure (SEO/metadata loss) | (marketing)/layout.tsx | mitigate | The layout owns chrome only (asserted: no h1/main/metadata in the layout); each page keeps its own metadata verbatim (security's canonical/robots/openGraph asserted); single <main>/<h1> asserted by the axe canary (the JOURNEY-03 class) |
| T-51-09 | Tampering (cache poisoning) | /demo Vary:Cookie cache headers | mitigate | The /demo URL is unchanged (folder-only), so the next.config.ts `/demo/:path*` Cache-Control + Vary:Cookie still matches; next.config.ts is asserted UNCHANGED so the per-session demo cache keying is preserved |
| T-51-SC | Tampering | npm installs | n/a | Zero package installs (RESEARCH §Package Legitimacy Audit) |

ASVS V4 Access Control — satisfied (PUBLIC_ROUTES + isAuthBounceExempt unchanged; guard proves it). V1 Architecture — the move preserves the documented public boundary. V3/V5/V6 non-applicable (no session/input/crypto change; redirect destinations are not introduced this plan).
</threat_model>

<verification>
- `npm run build` succeeds; the route table shows every marketing URL UNCHANGED (no `(marketing)` segment in any URL).
- `tsx scripts/check-route-contract.ts` exits 0 (no URL/allowlist drift from the move).
- `npx playwright test e2e/marketing-shell.spec.ts` green locally; the spec is in the unseeded ci.yml list.
- `git diff --name-only` shows next.config.ts and proxy.ts UNTOUCHED.
- Manual (post-deploy): authed + anon prod canary per marketing route — 200 + real content + canonical/metadata intact (the manual SSR-prop recipe).
</verification>

<success_criteria>
- The `(marketing)` group wraps landing + /legal/* + /for-quants + /security + /demo in one shared server-rendered shell with ZERO URL change.
- Each wrapped page keeps its own metadata + single <main>/<h1>; the demo banner + legal tab-nav survive as nested layouts; the landing authed redirect stays in the page.
- The route-contract guard + an anon canary prove every route still resolves at its URL and stays public (no 307→login, no duplicate landmark); the canary runs in CI.
- next.config.ts (/demo cache headers) and proxy.ts (PUBLIC_ROUTES) are untouched and still match.
</success_criteria>

<output>
Create `.planning/phases/51-shell-information-architecture-restructure/51-04-SUMMARY.md` when done
</output>
