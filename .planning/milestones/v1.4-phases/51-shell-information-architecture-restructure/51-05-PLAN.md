---
phase: 51-shell-information-architecture-restructure
plan: 05
type: execute
wave: 4
depends_on: ["51-02", "51-04"]
files_modified:
  - next.config.ts
  - src/lib/routing/route-contract-manifest.ts
  - e2e/route-redirects.spec.ts
  - .github/workflows/ci.yml
files_deleted:
  - src/app/(dashboard)/scenarios/page.tsx
autonomous: true
requirements: [NAV-01, NAV-03]
must_haves:
  truths:
    - "The old /scenarios path 308-redirects to /allocations?tab=scenario via next.config.ts redirects() and resolves to real content (no 307→login, no 404)"
    - "The route-contract manifest carries the move (redirectFrom: /scenarios) and the guard's Rule 3 proves the redirects() entry exists — lockstep enforced"
    - "An anon redirect-resolves canary asserts /scenarios redirects (308) toward the composer, wired into CI"
  artifacts:
    - path: "next.config.ts"
      provides: "async redirects() with the /scenarios → /allocations?tab=scenario permanent (308) move"
      contains: "redirects"
    - path: "e2e/route-redirects.spec.ts"
      provides: "Anon canary: /scenarios redirects toward the composer (no 307→login)"
      contains: "scenarios"
  key_links:
    - from: "next.config.ts"
      to: "/allocations?tab=scenario"
      via: "redirects() { source: '/scenarios', destination: '/allocations?tab=scenario', permanent: true }"
      pattern: "/allocations\\?tab=scenario"
    - from: "src/lib/routing/route-contract-manifest.ts"
      to: "next.config.ts redirects()"
      via: "redirectFrom: '/scenarios' satisfied by Rule 3"
      pattern: "redirectFrom"
    - from: "e2e/route-redirects.spec.ts"
      to: ".github/workflows/ci.yml"
      via: "wired into the unseeded playwright list (FLOW-01)"
      pattern: "route-redirects"
---

<objective>
Execute the ONE selective route move this phase takes — formalize the legacy `/scenarios` in-page redirect stub into a `next.config.ts` `redirects()` 308 (so the guard covers it and the old `page.tsx` stub is retired cleanly) — under the full HARD-RULE triple: redirect (308) + classification in lockstep + an anon canary proving the old link resolves (no #512 307→login). This is the smallest, safest, fully-reversible move; `/preferences` is deliberately NOT moved (it has custom query-merging logic a static redirect would regress).

Purpose: NAV-01 (a clean consolidation with a guarded redirect) + NAV-03 (the redirect map ↔ guard ↔ proxy lockstep, proven by Rule 3 + a canary).
Output: a redirects() entry, the retired stub, the manifest move, and a CI-wired redirect canary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md
@.planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md
@.planning/phases/51-shell-information-architecture-restructure/51-02-SUMMARY.md
@.planning/phases/51-shell-information-architecture-restructure/51-04-SUMMARY.md

<interfaces>
<!-- The move targets + analogs. Extracted so no exploration is needed. -->

src/app/(dashboard)/scenarios/page.tsx (the stub to RETIRE) — currently a clean 1:1:
  redirect("/allocations?tab=scenario");   // FLOW-02 (Phase 32) — no query-merge logic, no role gate. SAFE to formalize.
  (Contrast: /preferences/page.tsx has custom param-merging — utm preserved, tab forced to "mandate" — which a static redirect CANNOT replicate, so /preferences is NOT moved.)

next.config.ts — add `async redirects()` as a sibling of the existing rewrites()/headers():
  redirects() runs BEFORE the proxy + BEFORE the filesystem; permanent:true → 308 (method-preserving); query string auto-preserved. (RESEARCH Pattern 2 + Code Examples L417-426.)
  The /allocations?tab=scenario destination is an authed dashboard route — its auth is enforced by the (dashboard) layout + page guards at the DESTINATION; the redirect itself is unconditional and public-safe (no secret in the path).

src/lib/routing/route-contract-manifest.ts — the /scenarios entry (classified "private" in 51-02) becomes the move: set `redirectFrom: "/scenarios"` so Rule 3 requires a matching redirects() source. After deleting the page file, the entry must NOT trip Rule 4 STALE — model it exactly as the 51-02 guard design intends for a redirected-away route (a redirectFrom entry whose page file is intentionally gone).

e2e/route-redirects.spec.ts (NEW) — analog e2e/security-page.spec.ts L29-40 (anon status<400 + visible content). The canary asserts /scenarios redirects toward the composer, NOT a 307→login.
ci.yml unseeded playwright list (~L1073) — append the new spec (FLOW-01: else it never runs).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add redirects() (308), retire the stub, carry the move in the manifest (guard Rule 3 green)</name>
  <files>next.config.ts, src/app/(dashboard)/scenarios/page.tsx, src/lib/routing/route-contract-manifest.ts</files>
  <read_first>
    - next.config.ts (the existing rewrites() L4-11 + headers() L12-84 siblings — add redirects() alongside; do NOT touch the /demo cache headers)
    - src/app/(dashboard)/scenarios/page.tsx (the clean 1:1 stub to retire)
    - src/app/(dashboard)/preferences/page.tsx (the custom-param-merge stub that is NOT moved — confirm the contrast)
    - scripts/check-route-contract.ts (Rule 3 — parses redirects() sources; Rule 4 — STALE; understand how a redirectFrom entry with no page file is modeled)
    - src/lib/routing/route-contract-manifest.ts (the /scenarios entry to convert into the move)
    - .planning/phases/51-shell-information-architecture-restructure/51-RESEARCH.md (Pattern 2 + Code Examples L417-426 + Open Question 1 — fewest moves; the /scenarios formalization is the recommended single move)
    - .planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md (§next.config.ts — add redirects() as a sibling; §proxy.ts — lockstep)
  </read_first>
  <action>
    In next.config.ts add an `async redirects()` method as a sibling of rewrites()/headers(), returning `[{ source: "/scenarios", destination: "/allocations?tab=scenario", permanent: true }]` (→ 308, method-preserving, query auto-preserved). Do NOT modify rewrites(), headers(), or the /demo cache-header block.

    Delete src/app/(dashboard)/scenarios/page.tsx (the stub is now superseded by the config redirect — redirects() runs before the filesystem, so the old page never renders). Grep for in-app `href="/scenarios"` / `redirect("/scenarios")` / `push("/scenarios")` callers and repoint each at the new canonical `/allocations?tab=scenario` (the redirect catches external links; in-app links should target the canonical path directly).

    Update src/lib/routing/route-contract-manifest.ts: convert the `/scenarios` entry into the move per the 51-02 guard contract — set `redirectFrom: "/scenarios"` so Rule 3 requires the redirects() source (which now exists), and ensure the entry does not trip Rule 4 STALE now that the page file is gone (use the moved-route shape the 51-02 manifest established). Do NOT touch proxy.ts PUBLIC_ROUTES: /scenarios was never public (it is a private dashboard redirect to an authed destination), and /allocations stays private — the destination's auth is enforced at the (dashboard) layout, so no PUBLIC_ROUTES delta is needed. Confirm /preferences is left exactly as-is (its custom param-merge logic is not redirects()-expressible).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "Redirect|/scenarios|error|Compiled" | head -20; echo "---guard---"; tsx scripts/check-route-contract.ts; echo "guard exit: $?"; echo "---lint---"; npm run lint 2>&1 | tail -3; echo "lint exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - next.config.ts has a redirects() with the exact tuple (verify: `grep -c 'redirects' next.config.ts` >= 1 AND `grep -c '/allocations?tab=scenario' next.config.ts` >= 1 AND `grep -c 'permanent: true' next.config.ts` >= 1)
    - the stub is deleted (verify: `ls "src/app/(dashboard)/scenarios/page.tsx" 2>&1 | grep -c 'No such file\|cannot access'` >= 1)
    - no in-app caller still links bare /scenarios (verify: `grep -rEc 'href="/scenarios"|redirect\("/scenarios"\)|push\("/scenarios"\)' src/ | grep -v ':0' | grep -vc test` returns 0 outside tests)
    - the manifest carries the move and the guard's Rule 3 is satisfied (verify: `grep -c 'redirectFrom' src/lib/routing/route-contract-manifest.ts` >= 1 AND `tsx scripts/check-route-contract.ts` exits 0 — no MISSING-REDIRECT, no STALE)
    - `npm run lint` exits 0 (the guard runs in the chain and passes); `npm run build` succeeds and emits the /scenarios → /allocations redirect
    - rewrites()/headers()/the /demo cache block + proxy.ts PUBLIC_ROUTES are unchanged (verify: `git diff next.config.ts | grep -Ec '^\-.*(security.txt|s-maxage|Vary|X-Frame)' ` returns 0 AND `git diff --name-only | grep -c 'src/proxy.ts'` returns 0)
    - /preferences is untouched (verify: `git diff --name-only | grep -c 'preferences'` returns 0)
  </acceptance_criteria>
  <done>The /scenarios move is formalized as a guarded 308 redirect with the stub retired, the manifest's redirectFrom satisfies Rule 3, and no PUBLIC_ROUTES/headers/preferences collateral changes.</done>
</task>

<task type="auto">
  <name>Task 2: Anon redirect-resolves canary, wired into CI (the #512 assertion for the move)</name>
  <files>e2e/route-redirects.spec.ts, .github/workflows/ci.yml</files>
  <read_first>
    - e2e/security-page.spec.ts (the anon status<400 + visible-content canary shape, L29-40)
    - e2e/axe-app-wide.spec.ts (the status<400 + visible-anchor gate + the FLOW-01 CI-wiring header comment)
    - .github/workflows/ci.yml (the unseeded playwright list ~L1073 that runs axe-app-wide.spec.ts + security-page — append the new spec here)
    - .planning/phases/51-shell-information-architecture-restructure/51-PATTERNS.md (§e2e/route-redirects.spec.ts — the per-move canary assertion + the FLOW-01 both-places wiring)
    - .planning/phases/51-shell-information-architecture-restructure/51-CONTEXT.md (the HARD CONSTRAINT (c): every move needs a canary proving the old link resolves, NOT a 307→login)
  </read_first>
  <action>
    Create e2e/route-redirects.spec.ts (PUBLIC / unseeded). As an ANONYMOUS visitor, navigate to /scenarios and assert: the response chain is a redirect toward /allocations?tab=scenario (assert the final URL is /allocations with the scenario tab, NOT /login — the #512 assertion), and the resulting status is < 400 (the destination renders or, for an anon user, the destination's OWN auth governs — assert specifically that the redirect target is the composer path and NOT /login). Because /allocations is authed, an anon visitor following the 308 will ultimately reach the destination's auth gate — the canary's job is to prove the MOVE redirect lands on /allocations (the composer), not that /scenarios itself 307→login. Assert the redirect Location/target resolves to /allocations?tab=scenario and never to /login (the #512 regression class). Keep the spec deterministic and seed-free.

    Wire the spec into the unseeded ci.yml playwright list (append `e2e/route-redirects.spec.ts` to the same `npx playwright test …` line that runs axe-app-wide.spec.ts, ~L1073) — FLOW-01: a new seed-free e2e must be added here or it never runs in CI. Verify playwright discovers it locally.
  </action>
  <verify>
    <automated>npx playwright test e2e/route-redirects.spec.ts --list 2>&1 | tail -15; echo "---ci-wired---"; grep -c 'route-redirects.spec.ts' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - the canary asserts /scenarios redirects toward /allocations?tab=scenario and NEVER to /login (verify: `grep -Ec 'scenarios|allocations|login' e2e/route-redirects.spec.ts` >= 2 AND the spec asserts the target is /allocations not /login)
    - the spec is wired into the unseeded ci.yml playwright list (verify: `grep -c 'route-redirects.spec.ts' .github/workflows/ci.yml` >= 1, not inside a HAS_SEED_ENV-gated block)
    - playwright discovers the spec (verify: `npx playwright test e2e/route-redirects.spec.ts --list` exits 0 and lists the test)
    - the spec has no seed/auth dependency (verify: `grep -c 'HAS_SEED_ENV\|qa-demo\|setSession' e2e/route-redirects.spec.ts` returns 0)
  </acceptance_criteria>
  <done>An anon canary proves the /scenarios move lands on the composer (never 307→login), satisfying the CONTEXT hard-rule triple, and it runs in CI via the unseeded playwright list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon visitor → /scenarios redirect | A move must not 307→login the old link (#512); the redirect must land on the canonical destination |
| redirects() destination | The destination is a static internal path (no user input) — must not become an open redirect |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-01 | Denial of Service (broken link) | /scenarios move ↔ redirects()/PUBLIC_ROUTES | mitigate | The move ships the full triple: a 308 redirects() entry + the guard Rule 3 proving the entry exists + an anon canary asserting the target is /allocations, never /login — the #512 class cannot recur for this move by construction |
| T-51-05 | Tampering (open redirect) | next.config.ts redirects() destination | mitigate | The destination is a static internal literal `/allocations?tab=scenario` (no `:param`, no request-derived host); the guard Rule 3 only matches static sources — no user input ever reaches the redirect |
| T-51-10 | Information Disclosure (over-exposure) | classification of the moved route | mitigate | /scenarios was never public and the destination /allocations stays private (authed at the (dashboard) layout); no PUBLIC_ROUTES delta — the move does not widen the public surface (asserted: proxy.ts untouched) |
| T-51-SC | Tampering | npm installs | n/a | Zero package installs (RESEARCH §Package Legitimacy Audit) |

ASVS V4 Access Control — satisfied (no public-surface widening; destination auth preserved). V5 Input Validation — the redirect source/destination are static config literals (no user input). V1 Architecture — the redirect map ↔ guard ↔ proxy lockstep is enforced. V3/V6 non-applicable.
</threat_model>

<verification>
- `npm run build` emits the /scenarios → /allocations?tab=scenario 308 redirect; build succeeds.
- `tsx scripts/check-route-contract.ts` exits 0 (Rule 3 satisfied, no STALE); `npm run lint` exits 0.
- `npx playwright test e2e/route-redirects.spec.ts` green locally; the spec is in the unseeded ci.yml list.
- `git diff` shows proxy.ts + /preferences + the /demo cache headers UNTOUCHED.
- Manual (post-deploy): authed-prod canary — /scenarios reaches the live composer with the scenario tab (the passwordless SSR-prop recipe), no 307→login.
</verification>

<success_criteria>
- /scenarios is formalized into a next.config.ts redirects() 308 to /allocations?tab=scenario; the in-page stub is retired; in-app callers repointed.
- The route-contract manifest carries the move (redirectFrom) and the guard's Rule 3 proves the redirects() entry exists — lockstep enforced, guard + lint green.
- An anon canary asserts the move lands on the composer (never 307→login), wired into CI.
- No PUBLIC_ROUTES / headers / /preferences collateral changes; /preferences deliberately not moved.
</success_criteria>

<output>
Create `.planning/phases/51-shell-information-architecture-restructure/51-05-SUMMARY.md` when done
</output>
