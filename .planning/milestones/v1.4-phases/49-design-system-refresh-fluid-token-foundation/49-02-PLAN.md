---
phase: 49-design-system-refresh-fluid-token-foundation
plan: 02
type: execute
wave: 1
depends_on: ["49-01"]
files_modified:
  - src/lib/design-tokens/typography.ts
  - src/app/globals.css
  - DESIGN.md
autonomous: true
requirements: [DS-01, DS-02, DS-03]
must_haves:
  truths:
    - "Eight named fluid --text-* clamp tokens exist in a PLAIN @theme block (not @theme inline) in globals.css, each with a rem middle term and max <= 2.5x min"
    - "TYPE_SCALE in typography.ts holds the same eight tiers, each clamp string byte-identical to globals.css"
    - "DESIGN.md has a fluid-type-spine section + a Decisions Log row, and each tier's px endpoints appear verbatim"
    - "The Wave-0 drift test and clamp guard both go GREEN"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Plain @theme block with --text-{hero,page-title,h2,h3,body,small,caption,micro} clamp tokens; @theme inline colors + --space-grid-gap byte-unchanged"
      contains: "@theme {"
    - path: "src/lib/design-tokens/typography.ts"
      provides: "TYPE_SCALE with all 8 tiers populated (minPx/maxPx/clamp), clamp strings verbatim-matching globals.css"
      contains: "hero"
    - path: "DESIGN.md"
      provides: "Fluid Type Spine section + Decisions Log row documenting the clamp tokens, rem-term + 2.5x invariant, the TS mirror, and that existing text-sm/text-[14px] usages are untouched"
      contains: "Fluid Type"
  key_links:
    - from: "src/app/globals.css"
      to: "src/lib/design-tokens/typography.ts"
      via: "verbatim clamp-string agreement asserted by the drift test"
      pattern: "--text-hero:\\s*clamp"
    - from: "DESIGN.md"
      to: "src/lib/design-tokens/typography.ts"
      via: "px endpoints appear verbatim (drift test)"
      pattern: "Fluid Type"
---

<objective>
Build the token spine: populate `TYPE_SCALE` with all eight fluid tiers, add the PLAIN `@theme {--text-*}` block to `globals.css` (sibling to the existing `@theme inline` color block — NOT appended into it), and refresh DESIGN.md with a Fluid Type Spine section + a Decisions Log row. This turns the Wave-0 drift and clamp guard tests GREEN.

Purpose: This is the locked spine phases 50-53 build on. The load-bearing subtlety is plain `@theme` vs `@theme inline` — `inline` bakes the clamp literal and flattens the var chain, defeating 400%-zoom safety (WCAG 1.4.4). The clamp tokens MUST stay live `var(--text-*)` references.
Output: populated `typography.ts`, new plain `@theme` block in `globals.css`, refreshed DESIGN.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-design-system-refresh-fluid-token-foundation/49-RESEARCH.md
@.planning/phases/49-design-system-refresh-fluid-token-foundation/49-PATTERNS.md

<interfaces>
<!-- The exact tier→px mapping from DESIGN.md §Typography (lines 25-31). Anchors are Claude's-discretion within the hard invariants; the illustrative anchors below already satisfy them — verify each tier maxPx <= 2.5*minPx. -->

Tier → px (DESIGN.md §Typography):
  hero        48px landing / 32px page-title   → token --text-hero uses 32→48
  page-title  32px                             → --text-page-title 24→32  (note: hero "page-title 32" maps here as the page-title tier)
  h2          24px                             → --text-h2 20→24
  h3          16px (semibold)                  → --text-h3 16→18
  body        14px                             → --text-body 14→16
  small       13px                             → --text-small 13→14
  caption     12px                             → --text-caption 12→13
  micro       10-11px (labels/badges)          → --text-micro 10→11

Illustrative anchors (RESEARCH Pattern 2; min-viewport 320px, max-viewport 1280px) — each MUST satisfy rem-term + maxPx <= 2.5*minPx:
  --text-hero:       clamp(2rem,    1.5rem  + 2.5vw,  3rem)      /* 32→48, ratio 1.5 */
  --text-page-title: clamp(1.5rem,  1.2rem  + 1.5vw,  2rem)      /* 24→32, ratio 1.33 */
  --text-h2:         clamp(1.25rem, 1.1rem  + 0.75vw, 1.5rem)    /* 20→24 */
  --text-h3:         clamp(1rem,    0.95rem + 0.25vw, 1.125rem)  /* 16→18 */
  --text-body:       clamp(0.875rem,0.85rem + 0.125vw,1rem)      /* 14→16 */
  --text-small:      clamp(0.8125rem,0.8rem + 0.0625vw,0.875rem) /* 13→14 */
  --text-caption:    clamp(0.75rem, 0.73rem + 0.0625vw,0.8125rem)/* 12→13 */
  --text-micro:      clamp(0.625rem,0.61rem + 0.0625vw,0.6875rem)/* 10→11 */

Pitfall 2 (Tailwind built-ins): tier names hero/page-title/h2/h3/body/small/caption/micro do NOT collide with Tailwind's default --text-sm/base/lg/xl/2xl. Safe.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate TYPE_SCALE with all eight tiers</name>
  <files>src/lib/design-tokens/typography.ts</files>
  <behavior>
    - After this task, `TYPE_SCALE` has 8 keys: hero, page-title, h2, h3, body, small, caption, micro.
    - Each tier's `clamp` string contains a `rem` term and the implied max rem <= 2.5x min rem.
    - Each `clamp` string will match `globals.css` verbatim (Task 2) — the drift test enforces this.
  </behavior>
  <read_first>
    - src/lib/design-tokens/typography.ts (the Wave-0 skeleton from 49-01 — interface + buildClamp + empty TYPE_SCALE)
    - src/lib/design-tokens/trust-tier.ts (the `as const satisfies Record<…>` precedent)
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-RESEARCH.md (Pattern 2 derivation + Code Examples TS-mirror shape)
  </read_first>
  <action>
    Fill `TYPE_SCALE` in `src/lib/design-tokens/typography.ts` with the eight tier rows from the `<interfaces>` table above: `hero` `{ minPx: 32, maxPx: 48, clamp: "clamp(2rem, 1.5rem + 2.5vw, 3rem)" }`, `"page-title"` `{ minPx: 24, maxPx: 32, clamp: "clamp(1.5rem, 1.2rem + 1.5vw, 2rem)" }`, and likewise `h2`, `h3`, `body`, `small`, `caption`, `micro` per the illustrative anchors (or values you derive with the `buildClamp` helper — but then check in the resulting STATIC clamp strings, never call `buildClamp` at module-eval time, so the drift test reads a literal). Keep `as const satisfies Record<string, TypeTier>`. The whitespace inside each `clamp(...)` string MUST be reproduced byte-identically in `globals.css` Task 2 (the drift test does an exact `toContain` on `--text-${tier}: ${clamp}`). Validate each tier: `maxPx <= 2.5 * minPx` (hero 48<=80 ✓ … all pass). Do NOT touch trust-tier.ts or any color token.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -q "typography" && (echo "TYPE ERR" && exit 1) || node -e "import('./src/lib/design-tokens/typography.ts').catch(()=>{});console.log('ok')" 2>/dev/null; npx vitest run tests/a11y/design-token-drift.test.ts 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `TYPE_SCALE` has exactly the 8 named keys (hero, page-title, h2, h3, body, small, caption, micro).
    - Every tier `clamp` string matches `/clamp\(.*rem.*\)/` (has a rem term).
    - For every tier, `maxPx <= 2.5 * minPx` (assert via a quick node check or the Wave-0 clamp guard once globals.css lands).
    - `npx tsc --noEmit` is clean for `typography.ts`.
  </acceptance_criteria>
  <done>TYPE_SCALE holds all eight zoom-safe tiers and type-checks; clamp strings are the canonical literals globals.css must mirror.</done>
</task>

<task type="auto">
  <name>Task 2: Add the plain @theme {--text-*} block to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (lines 1-128 — the existing `@theme inline` color block; confirm it opens with `@theme inline` at line 3 and ends at line 128; the new block goes AFTER the closing brace, NOT inside it)
    - src/lib/design-tokens/typography.ts (the canonical clamp strings to copy verbatim — from Task 1)
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-PATTERNS.md (the globals.css Pattern Assignment — plain-vs-inline distinction)
  </read_first>
  <action>
    Add a NEW sibling `@theme { … }` block (PLAIN — no `inline` keyword) to `src/app/globals.css`, placed immediately after the closing `}` of the existing `@theme inline {…}` color block (after line 128). Lead it with a comment: `/* Phase 49 / DS-02 — fluid type spine. PLAIN @theme (no \`inline\`) so text-* utilities keep var(--text-*) and the clamp() re-evaluates on zoom (WCAG 1.4.4). Tier→px maps DESIGN.md §Typography 1:1. Strings MUST match TYPE_SCALE[tier].clamp in src/lib/design-tokens/typography.ts verbatim (tests/a11y/design-token-drift.test.ts pins it). */`. Then declare the eight tokens `--text-hero`, `--text-page-title`, `--text-h2`, `--text-h3`, `--text-body`, `--text-small`, `--text-caption`, `--text-micro` each set to its `TYPE_SCALE[tier].clamp` string COPIED VERBATIM (same internal whitespace). Each declaration ends in `;`. Do NOT move any `--color-*` token out of `@theme inline`. Do NOT touch `--space-grid-gap: 10px`, `--spacing-sidebar`, the radius ladder, or the factsheet-dark tail. After writing, build once and inspect emitted CSS that `text-hero` resolves to `font-size: var(--text-hero)` (a live var), NOT a baked clamp literal (RESEARCH A3 / Pitfall 1).
  </action>
  <verify>
    <automated>npx vitest run tests/visual/fluid-type-tokens.test.ts tests/a11y/design-token-drift.test.ts 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^@theme inline' src/app/globals.css` returns 1 and a plain `@theme {` (no `inline`) block also exists (`grep -nE '^@theme \{' src/app/globals.css` matches).
    - All eight `--text-*` tokens are inside the plain block; `grep -c -- '--text-' src/app/globals.css` >= 8.
    - The drift test no-inline assertion passes (no `--text-` inside the `@theme inline` block).
    - `git diff src/app/globals.css` shows NO change to any `--color-*` line or to `--space-grid-gap`.
    - `npx vitest run tests/visual/fluid-type-tokens.test.ts` is GREEN (rem-term + 2.5x for all tiers).
  </acceptance_criteria>
  <done>The plain `@theme {--text-*}` block exists, colors/space stay byte-stable, and the Wave-0 clamp guard goes GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh DESIGN.md — Fluid Type Spine section + Decisions Log row</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (lines 17-31 §Typography for the existing scale prose; the Decisions Log table starting ~line 221, esp. the 2026-05-06 `--color-*` row and the 2026-04-30 AA-shift rows — the rationale-density + append-row convention)
    - src/lib/design-tokens/typography.ts (the tier px endpoints to cite verbatim — the drift test asserts each minPx appears in DESIGN.md)
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-PATTERNS.md (the DESIGN.md Pattern Assignment — evolve-in-place, do NOT touch color rows or --space-grid-gap)
  </read_first>
  <action>
    Edit DESIGN.md (DS-01) in two surgical places. (a) Add a "Fluid Type Spine" subsection near §Typography that documents: the eight named fluid tiers (hero/page-title/h2/h3/body/small/caption/micro) with their px endpoints (32-48, 24-32, 20-24, 16-18, 14-16, 13-14, 12-13, 10-11), that they are clamp()-based in a PLAIN `@theme` block, the two hard invariants (every clamp carries a `rem` middle term; max <= 2.5x min for WCAG 1.4.4 / W3C F94 200%-zoom safety), that the TS mirror is `src/lib/design-tokens/typography.ts` and the drift test is `tests/a11y/design-token-drift.test.ts`, and that existing `text-sm`/`text-[14px]` usages are UNTOUCHED in this phase (migrated per-surface in phases 52/53 — additive, no big-bang). Ensure every tier's minPx number (32, 24, 20, 16, 14, 13, 12, 10) appears as literal text so the drift test's `designMd.includes(String(minPx))` passes. Keep the existing §Typography scale lines intact (evolve in place, do not delete). (b) Append ONE pipe-delimited Decisions Log row dated 2026-06-29 titled e.g. "Fluid `--text-*` type spine in a plain `@theme` block (v1.4 Phase 49 / DS-01·DS-02·DS-03)" with rationale covering: plain-vs-inline reasoning, the rem-term + 2.5x invariant, the TS mirror + 3-way drift test + scoped design-lint, fonts/accent/space ladder unchanged (evolve in place), and the per-surface migration deferral. Do NOT edit any color Decisions Log row or the `--space-grid-gap` row; never delete historical rows.
  </action>
  <verify>
    <automated>npx vitest run tests/a11y/design-token-drift.test.ts tests/a11y/trust-tier-tokens.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DESIGN.md contains a "Fluid Type" section and each tier minPx (32, 24, 20, 16, 14, 13, 12, 10) appears as literal text.
    - Exactly one new Decisions Log row was appended (dated 2026-06-29); no existing row text changed (`git diff DESIGN.md` shows only additions + the one new section).
    - `npx vitest run tests/a11y/design-token-drift.test.ts` is GREEN (all 3 sources agree, no-inline guard holds, px endpoints present).
    - `tests/a11y/trust-tier-tokens.test.ts` still GREEN (no color drift introduced).
  </acceptance_criteria>
  <done>DESIGN.md documents the fluid spine, the three-way drift test is fully GREEN, and no color/space token or historical row was altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Static CSS tokens + a TS const + a markdown doc. No auth/session/network/input/crypto/data surface. Per 49-RESEARCH.md §Security Domain every ASVS L1 category is non-applicable; CSS token values are author-controlled literals, not user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-03 | Tampering | CSS token value injection | accept | `--text-*` values are author-controlled static `clamp()` literals in globals.css, never user input. No injection surface. |
| T-49-04 | Tampering | accidental edit of color/space tokens (byte-stability regression) | mitigate | Acceptance criteria assert `git diff` shows NO change to `--color-*` or `--space-grid-gap`; the existing `trust-tier-tokens.test.ts` + `chart-contrast.test.ts` pins catch color drift. |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs in this plan; the no-new-dep posture is asserted. Kept per template. |
</threat_model>

<verification>
- `npx vitest run tests/visual/fluid-type-tokens.test.ts tests/a11y/design-token-drift.test.ts` — both GREEN.
- `npx vitest run tests/a11y/trust-tier-tokens.test.ts tests/a11y/chart-contrast.test.ts` — still GREEN (no color drift).
- `grep -nE '^@theme \{' src/app/globals.css` matches a plain block; `grep -c '^@theme inline' src/app/globals.css` == 1.
- `git diff package.json` empty (no new dependency).
- One build inspected: `text-hero` emits `font-size: var(--text-hero)` (live var, not a baked literal).
</verification>

<success_criteria>
- Eight zoom-safe fluid `--text-*` clamp tokens live in a plain `@theme` block; colors + `--space-grid-gap` byte-unchanged.
- TYPE_SCALE mirrors them verbatim; DESIGN.md documents them + a Decisions Log row.
- The Wave-0 DS-02 clamp guard and DS-03 three-way drift test are both GREEN.
- Zero new dependencies.
</success_criteria>

<output>
Create `.planning/phases/49-design-system-refresh-fluid-token-foundation/49-02-SUMMARY.md` when done.
</output>
