---
phase: 49-design-system-refresh-fluid-token-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/audits/truncation-audit.md
autonomous: true
requirements: [TYPE-01]
must_haves:
  truths:
    - "A checked-in truncation-audit doc exists that inventories every truncate / line-clamp / text-ellipsis / overflow-hidden+whitespace-nowrap / manual-ellipsis site in src/"
    - "Every inventoried site is tagged legitimate (bounded label with title/tooltip recovery or intentional single-line affordance) vs accidental-clip (clips meaningful content, no recovery)"
    - "The audit is structured so phases 52/53 can consume it (file:line, pattern, classification, recovery affordance) — informing, not gating, this phase"
  artifacts:
    - path: ".planning/audits/truncation-audit.md"
      provides: "TYPE-01 classification: full census table + per-site legitimate/accidental-clip tag + recovery affordance + note, plus a summary count and a 'feeds phases 52/53' framing"
      contains: "accidental-clip"
  key_links:
    - from: ".planning/audits/truncation-audit.md"
      to: "src/ (truncation sites)"
      via: "ripgrep census over truncate/line-clamp/text-ellipsis/overflow-hidden+whitespace-nowrap"
      pattern: "truncate"
---

<objective>
Produce the TYPE-01 truncation classification: a checked-in `.planning/audits/truncation-audit.md` that inventories every clip/ellipsis site in `src/` and tags each as legitimate vs accidental-clip, so the per-surface phases (52/53) introduce fluid type without merely relocating a clip.

Purpose: This is the inventory that makes "fluid type never just relocates a clip" enforceable downstream. It is NOT a hard gate inside Phase 49 — it informs phases 52/53. It is independent of the token/lint work (no code dependency), so it runs in parallel with the token spine.
Output: `.planning/audits/truncation-audit.md` (and the `.planning/audits/` directory, which does not yet exist).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-design-system-refresh-fluid-token-foundation/49-RESEARCH.md
@.planning/phases/49-design-system-refresh-fluid-token-foundation/49-PATTERNS.md

<interfaces>
<!-- Census patterns + the recon baseline from RESEARCH. The executor regenerates the census fresh (the tree may have drifted) and classifies. -->

Census recon baseline (RESEARCH §phase_requirements TYPE-01 + Sources): ~34 files with `truncate`, 9 `line-clamp`, 2 `text-ellipsis`, 22 `overflow-hidden`, 24 `whitespace-nowrap`, 2 manual-ellipsis idiom files. Regenerate fresh — numbers are a sanity check, not a target.

Census ripgrep patterns (run over src/):
  rg -n "truncate" src/
  rg -n "line-clamp" src/
  rg -n "text-ellipsis" src/
  rg -n "overflow-hidden" src/ | rg "whitespace-nowrap"   # the combined single-line clip idiom (cross-reference per file)
  rg -n "text-overflow|\\u2026|'\\.\\.\\.'" src/            # manual-ellipsis idiom

Classification criteria (RESEARCH Pattern 6 / PATTERNS.md):
  legitimate      = bounded label with a title/tooltip recovery, OR an intentional single-line affordance (e.g. a one-line table cell with a hover/title showing full text)
  accidental-clip = clips meaningful content with NO recovery affordance (no title, no tooltip, no expand)

Suggested table columns:
  | file:line | pattern | element / context | classification | recovery affordance | note |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the truncation census and classify every site</name>
  <files>.planning/audits/truncation-audit.md</files>
  <read_first>
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-RESEARCH.md (Pattern 6 / the TYPE-01 row — census strategy, classification criteria, doc structure)
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-PATTERNS.md (the `.planning/audits/truncation-audit.md` "no analog" section — table shape + legitimate-vs-accidental criteria)
    - For EACH non-obvious site: read enough of the owning component to determine whether a title/tooltip recovery exists (do NOT classify from the grep line alone — a `truncate` with a sibling `title={…}` or a TouchTooltip wrapper is legitimate).
  </read_first>
  <action>
    First create the `.planning/audits/` directory (it does not yet exist). Run the census ripgreps from `<interfaces>` over `src/` to enumerate every `truncate`, `line-clamp`, `text-ellipsis`, the combined `overflow-hidden` + `whitespace-nowrap` single-line-clip idiom (cross-reference per file — only a co-located pair is the clip idiom), and the manual-ellipsis idiom. For EACH site, open the owning component far enough to determine the recovery affordance — a `truncate`/`line-clamp` that has a sibling `title={…}` attribute, an `aria-label`, or a tooltip wrapper (e.g. `TouchTooltip`) is LEGITIMATE (bounded label with recovery); a single-line affordance that is intentional (e.g. a fixed-width metric chip) is LEGITIMATE; a clip on meaningful prose/labels with NO title/tooltip/expand is ACCIDENTAL-CLIP. Write `.planning/audits/truncation-audit.md` with: (a) a one-paragraph header stating this is the TYPE-01 artifact, that it INFORMS phases 52/53 (not a Phase-49 gate), and the date; (b) a census summary line with the counts per pattern; (c) the full classification table `| file:line | pattern | element / context | classification | recovery affordance | note |`, one row per site; (d) a short "Accidental-clip shortlist" section listing only the accidental-clip rows so phases 52/53 can triage them first. Do NOT modify any component — this is a read-only classification artifact. Do NOT add a hard CI gate (the optional census-count guard is deferred per RESEARCH).
  </action>
  <verify>
    <automated>test -f .planning/audits/truncation-audit.md && grep -qi "accidental-clip" .planning/audits/truncation-audit.md && grep -qiE "legitimate" .planning/audits/truncation-audit.md && echo "AUDIT-OK" || (echo "MISSING audit or classifications" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/audits/truncation-audit.md` exists with a header framing it as TYPE-01 informing phases 52/53.
    - It contains a census summary with per-pattern counts and a full `file:line | pattern | … | classification | recovery affordance | note` table.
    - Every row is classified `legitimate` or `accidental-clip` (no blank classifications — `grep -c "| legitimate \\|| accidental-clip" ` count equals the table row count).
    - An "Accidental-clip shortlist" section lists the accidental-clip rows for downstream triage.
    - No `src/` component file was modified (`git status --porcelain src/` empty for this plan).
  </acceptance_criteria>
  <done>Every clip/ellipsis site in src/ is inventoried and classified; the doc is ready to feed phases 52/53 so fluid type never just relocates a clip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan produces a single read-only markdown classification doc and modifies no code. No auth/session/network/input/crypto/data surface (49-RESEARCH.md §Security Domain — all ASVS L1 N-A). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-07 | Information Disclosure | audit doc enumerating component file:line refs | accept | The audit lists in-repo file paths + class-name patterns only — no secrets, no user data. It is a planning artifact under `.planning/`. No disclosure surface beyond what the repo already contains. |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs (ripgrep + Read only). The no-new-dep posture holds. Kept per template. |
</threat_model>

<verification>
- `.planning/audits/truncation-audit.md` exists and classifies every census site.
- `git status --porcelain src/` shows no `src/` modification from this plan (read-only classification).
- No new dependency.
</verification>

<success_criteria>
- A complete, per-site-classified truncation audit exists at `.planning/audits/truncation-audit.md`.
- Each site is tagged legitimate vs accidental-clip with its recovery affordance recorded.
- The doc is structured to feed phases 52/53; no code changed; no gate added in Phase 49.
</success_criteria>

<output>
Create `.planning/phases/49-design-system-refresh-fluid-token-foundation/49-04-SUMMARY.md` when done.
</output>
