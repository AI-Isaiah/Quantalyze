---
phase: 49-design-system-refresh-fluid-token-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/design-tokens/typography.ts
  - tests/visual/fluid-type-tokens.test.ts
  - tests/a11y/design-token-drift.test.ts
autonomous: true
requirements: [DS-02, DS-03]
must_haves:
  truths:
    - "A clamp-grep guard test exists that asserts every --text-* token in globals.css has a rem term and max <= 2.5x min"
    - "A three-way drift test exists that asserts DESIGN.md, the plain @theme block, and the TS mirror agree, and fails if any --text-* lands in @theme inline"
    - "Both tests are RED against the current tree (tokens absent) — the executable contract before implementation"
  artifacts:
    - path: "src/lib/design-tokens/typography.ts"
      provides: "Empty TYPE_SCALE skeleton + TypeTier interface + buildClamp helper (no tiers yet) so the drift test imports cleanly and fails on assertions, not collection"
      contains: "TYPE_SCALE"
    - path: "tests/visual/fluid-type-tokens.test.ts"
      provides: "DS-02 clamp guard: rem-term + 2.5x ratio over every --text-* in globals.css"
      contains: "globals.css"
    - path: "tests/a11y/design-token-drift.test.ts"
      provides: "DS-03 three-way drift + no-inline guard"
      contains: "TYPE_SCALE"
  key_links:
    - from: "tests/a11y/design-token-drift.test.ts"
      to: "src/lib/design-tokens/typography.ts"
      via: "import { TYPE_SCALE }"
      pattern: "from .@/lib/design-tokens/typography."
    - from: "tests/visual/fluid-type-tokens.test.ts"
      to: "src/app/globals.css"
      via: "readFileSync of globals.css"
      pattern: "globals\\.css"
---

<objective>
Establish the Wave-0 executable test contract for the fluid type spine BEFORE any token is written. Create the two guard tests (clamp-rem/2.5x grep and three-way drift) plus an empty `typography.ts` skeleton so the tests collect and fail RED against the current tree (no `--text-*` tokens exist yet).

Purpose: Per `.planning/phases/49-design-system-refresh-fluid-token-foundation/49-VALIDATION.md`, a failing-test wave precedes implementation so DS-02 and DS-03 have automated assertions the moment the tokens land in Wave 1. This is the Nyquist contract — the tests are the spec.
Output: `src/lib/design-tokens/typography.ts` (empty scaffold), `tests/visual/fluid-type-tokens.test.ts`, `tests/a11y/design-token-drift.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-design-system-refresh-fluid-token-foundation/49-RESEARCH.md
@.planning/phases/49-design-system-refresh-fluid-token-foundation/49-PATTERNS.md
@.planning/phases/49-design-system-refresh-fluid-token-foundation/49-VALIDATION.md

<interfaces>
<!-- The TS mirror this plan scaffolds and the drift test pins. Extracted from RESEARCH Pattern 2 + PATTERNS.md. The Wave-1 plan (49-02) fills the tiers. -->

Target shape (Wave 1 fills the tier rows; Wave 0 ships the interface + empty/partial map):
```
export interface TypeTier { readonly minPx: number; readonly maxPx: number; readonly clamp: string; }
export const TYPE_SCALE = { /* tiers added in 49-02 */ } as const satisfies Record<string, TypeTier>;
```

The eight named tiers (from DESIGN.md §Typography lines 25-31, filled in 49-02):
hero (48/32) · page-title (32) · h2 (24) · h3 (16) · body (14) · small (13) · caption (12) · micro (10-11).

Hard invariants every tier MUST satisfy (RESEARCH Pattern 2): the clamp middle term contains a `rem` component; `maxPx <= 2.5 * minPx`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the TS type-mirror skeleton (empty TYPE_SCALE)</name>
  <files>src/lib/design-tokens/typography.ts</files>
  <read_first>
    - src/lib/design-tokens/trust-tier.ts (the exact `as const satisfies Record<…>` + header-comment pattern to mirror — analog per PATTERNS.md)
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-PATTERNS.md (the `typography.ts` Pattern Assignment section)
  </read_first>
  <action>
    Create `src/lib/design-tokens/typography.ts` as a framework-neutral (NO React import) module mirroring `trust-tier.ts`'s structure: a header doc-comment that states the SoT role, declares framework-neutral so it loads in the Vitest node context, and names the drift test `tests/a11y/design-token-drift.test.ts` that pins it. Export an interface `TypeTier` with readonly `minPx: number`, `maxPx: number`, `clamp: string`. Export `TYPE_SCALE` as an EMPTY object literal `as const satisfies Record<string, TypeTier>` — the eight tier rows are deliberately deferred to plan 49-02 (Wave 1); leave a comment `// Tiers populated in 49-02 (Wave 1). Wave 0 ships this skeleton so the drift test imports cleanly and fails on assertions, not on a missing module.`. Also export a pure helper `buildClamp({ minPx, maxPx, minVw, maxVw }: { minPx: number; maxPx: number; minVw: number; maxVw: number }): string` that derives a `clamp(<minRem>, <interceptRem> + <slopeVw>, <maxRem>)` string per the RESEARCH Pattern 2 derivation (slopeVw = (maxPx-minPx)/(maxVw-minVw)*100; interceptRem = (minPx - slopeVw/100*minVw)/16; min/max in rem = px/16). The helper exists so 49-02 can derive-then-check-in static strings. Do NOT add any external dependency.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -q "typography" && echo "TYPE ERR" || npx vitest run tests/a11y/design-token-drift.test.ts 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/design-tokens/typography.ts` exists, exports `TypeTier`, `TYPE_SCALE`, and `buildClamp`.
    - The file has zero React/Next imports (loads in node/Vitest context — `grep -L "react" src/lib/design-tokens/typography.ts` returns the path).
    - `TYPE_SCALE` is an empty `as const satisfies Record<string, TypeTier>` (no tier rows yet — those are 49-02).
    - `npx tsc --noEmit` reports no error originating in `typography.ts`.
  </acceptance_criteria>
  <done>The TS mirror skeleton exists and type-checks; the drift test can import `TYPE_SCALE` without a module-resolution error.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the DS-02 clamp guard test (RED)</name>
  <files>tests/visual/fluid-type-tokens.test.ts</files>
  <behavior>
    - Test: collects every `--text-*: clamp(...)` declaration in `src/app/globals.css` via `matchAll(/--text-[\w-]+:\s*clamp\(([^;]+)\);/g)`.
    - Test: asserts at least 8 named tiers are declared (FAILS now — zero exist).
    - Test (per-token): every clamp args string contains a `rem` term (`/\brem\b/`) — WCAG 1.4.4 zoom-safety.
    - Test (per-token): max rem <= 2.5 * min rem (guarantees 200% zoom reach).
  </behavior>
  <read_first>
    - tests/visual/strategy-v2-type-scale.test.ts (the grep-over-source violation-collector precedent)
    - tests/a11y/chart-contrast.test.ts (lines ~110-121 — the `globals.css` regex-pin idiom)
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-RESEARCH.md (Code Examples — the exact `it.each` over `TEXT_TOKENS` shape)
  </read_first>
  <action>
    Create `tests/visual/fluid-type-tokens.test.ts` mirroring the RESEARCH Code-Examples shape: `readFileSync(resolve(process.cwd(), "src/app/globals.css"), "utf8")`, build `TEXT_TOKENS = [...css.matchAll(/--text-[\w-]+:\s*clamp\(([^;]+)\);/g)]`. Three `describe`-scoped assertions under "fluid type clamp guard (DS-02)": (1) `it("declares at least the 8 named tiers")` → `expect(TEXT_TOKENS.length).toBeGreaterThanOrEqual(8)`; (2) `it.each(TEXT_TOKENS.map(m => [m[0], m[1]]))("%s has a rem term (zoom-safe, WCAG 1.4.4)")` → `expect(/\brem\b/.test(args)).toBe(true)`; (3) `it.each(...)("%s max <= 2.5x min (guarantees 200% zoom)")` → parse all `([\d.]+)rem` floats, `expect(Math.max(...rems)).toBeLessThanOrEqual(2.5 * Math.min(...rems))`. This test MUST be RED now (zero `--text-*` clamp tokens in globals.css → the "at least 8 tiers" assertion fails). No new dependency; no watch flag.
  </action>
  <verify>
    <automated>npx vitest run tests/visual/fluid-type-tokens.test.ts 2>&1 | grep -qE "(fail|FAIL|✗|×|at least the 8)" && echo "RED-as-expected" || (echo "ERROR: test passed or did not run — expected RED" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/visual/fluid-type-tokens.test.ts` exists and is collected by Vitest (no import/collection error).
    - Running it now produces a FAILING "declares at least the 8 named tiers" assertion (RED) because no `--text-*` clamp tokens exist yet.
    - The file references `src/app/globals.css` via `readFileSync` and contains the literal regex `--text-` and `clamp(`.
    - No external package import is added.
  </acceptance_criteria>
  <done>The DS-02 clamp guard exists and is RED against the current tree; it will go GREEN when 49-02 adds the plain `@theme {--text-*}` block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the DS-03 three-way drift test with no-inline guard (RED)</name>
  <files>tests/a11y/design-token-drift.test.ts</files>
  <behavior>
    - Test: for each tier in TYPE_SCALE, the plain `@theme` block of globals.css contains `--text-${tier}: ${TYPE_SCALE[tier].clamp}` verbatim.
    - Test: the `@theme inline` block contains NO `--text-` token (no regression into inline — the CONTEXT-called-out guard).
    - Test: the plain `@theme` block contains every tier and >= 8 tiers exist.
    - Test: each tier's px endpoints (minPx) appear verbatim in DESIGN.md §Typography.
    - All RED now (TYPE_SCALE empty + no plain @theme block).
  </behavior>
  <read_first>
    - tests/a11y/trust-tier-tokens.test.ts (the DESIGN.md-parse + `designMd.includes(x)` verbatim-assert pattern AND the section-slice/brace idiom to adapt for the inline-vs-plain `@theme` boundary)
    - src/lib/design-tokens/typography.ts (the TYPE_SCALE shape this test iterates — created in Task 1)
    - .planning/phases/49-design-system-refresh-fluid-token-foundation/49-RESEARCH.md (Pattern 4 — the four assertions + the `extractBlock` brace-balancer sketch)
  </read_first>
  <action>
    Create `tests/a11y/design-token-drift.test.ts` extending the `trust-tier-tokens.test.ts` conventions: `import { TYPE_SCALE } from "@/lib/design-tokens/typography"`, `readFileSync` DESIGN.md and `src/app/globals.css`. Implement a small in-test `extractBlock(css, openRegex)` brace-balancer (no parser dep) that returns the body between a matched `@theme` opener and its balanced closing brace. Derive `inlineBlock = extractBlock(css, /@theme\s+inline\s*\{/)` and `plainBlock = extractBlock(css, /@theme\s*\{(?!\s*inline)/)` (the no-`inline` instance). Then assert, per RESEARCH Pattern 4: (1) for every `[tier, t]` of `Object.entries(TYPE_SCALE)`: `expect(plainBlock).toContain(\`--text-${tier}: ${t.clamp}\`)` (CSS ↔ TS verbatim); (2) `expect(inlineBlock).not.toContain(\`--text-${tier}\`)` (no regression into `@theme inline`); (3) `expect(Object.keys(TYPE_SCALE).length).toBeGreaterThanOrEqual(8)`; (4) `expect(designMd).toContain(String(t.minPx))` (DESIGN.md ↔ TS per §Typography). Guard against an empty TYPE_SCALE making the `it.each` empty: add an explicit `it("declares >= 8 tiers")` top-level assertion so the suite is RED (not vacuously green) while TYPE_SCALE is empty. This test MUST be RED now. No new dependency; no watch flag.
  </action>
  <verify>
    <automated>npx vitest run tests/a11y/design-token-drift.test.ts 2>&1 | grep -qE "(fail|FAIL|✗|×|8 tiers)" && echo "RED-as-expected" || (echo "ERROR: expected RED" && exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `tests/a11y/design-token-drift.test.ts` exists, imports `TYPE_SCALE` from `@/lib/design-tokens/typography`, and reads both DESIGN.md and `src/app/globals.css`.
    - It contains an `extractBlock` brace-balancer and distinguishes `@theme inline` from the plain `@theme` instance (no parser dependency).
    - Running it now is RED via an explicit `>= 8 tiers` assertion (TYPE_SCALE is empty).
    - The no-inline assertion `expect(inlineBlock).not.toContain("--text-…")` is present in source.
  </acceptance_criteria>
  <done>The DS-03 three-way drift + no-inline guard exists and is RED; it goes GREEN when 49-02 fills TYPE_SCALE, adds the plain `@theme` block, and adds the px endpoints to DESIGN.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan creates only build/CI-tier test files + a static TS const skeleton. No auth, session, network, input-handling, crypto, or data-access surface is touched. Per 49-RESEARCH.md §Security Domain, every ASVS L1 category is non-applicable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | Tampering | new npm/pip/cargo install | accept | This plan installs ZERO packages (RESEARCH Package Legitimacy Audit: N/A). The only mitigation needed is the no-new-dep posture, which the acceptance criteria assert (`no external package import added`). |
| T-49-02 | Information Disclosure | drift/clamp test reading repo files | accept | Tests `readFileSync` only author-controlled in-repo files (DESIGN.md, globals.css, typography.ts) at build/CI time; they never ship and parse no user input. No disclosure surface. |
| T-49-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan; if any are introduced, gate behind blocking-human checkpoint per RESEARCH. Kept here per template requirement. |
</threat_model>

<verification>
- `npx vitest run tests/visual/fluid-type-tokens.test.ts tests/a11y/design-token-drift.test.ts` collects both files (no import error) and both are RED (tokens absent) — the expected Wave-0 state.
- `npx tsc --noEmit` shows no error from `typography.ts`.
- `grep -rL "react" src/lib/design-tokens/typography.ts` confirms framework-neutral.
- No new entry in `package.json` dependencies/devDependencies (`git diff --stat package.json` empty).
</verification>

<success_criteria>
- The two guard test files exist, collect cleanly, and fail RED against the current tree.
- `typography.ts` exists as an importable, type-checking, framework-neutral skeleton with an empty `TYPE_SCALE` and a `buildClamp` helper.
- Zero new dependencies introduced.
</success_criteria>

<output>
Create `.planning/phases/49-design-system-refresh-fluid-token-foundation/49-01-SUMMARY.md` when done.
</output>
