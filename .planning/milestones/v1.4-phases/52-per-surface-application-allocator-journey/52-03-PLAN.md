---
phase: 52-per-surface-application-allocator-journey
plan: 03
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/app/(dashboard)/compare/page.tsx
  - src/app/(dashboard)/compare/loading.tsx
  - src/app/(dashboard)/compare/loading.test.tsx
  - src/app/(dashboard)/compare/error.tsx
  - src/app/(dashboard)/compare/error.test.tsx
  - src/components/strategy/CompareTable.tsx
  - src/components/strategy/CompareTable.test.tsx
autonomous: true
requirements: [APPLY-01, TYPE-02, TYPE-03, TYPE-04, STATE-01, STATE-02, BP-01]
user_setup: []
must_haves:
  truths:
    - "The compare page fluid-fills toward ~1920px then centers; no horizontal scroll/overlap at 320px or 2560px"
    - "The CompareTable responds to its own container width via @container with tabular-nums alignment preserved across the breakpoint"
    - "Compare table name cells recover full strategy names via title= (tabular context)"
    - "Navigating to /compare shows a multi-column comparison-table skeleton while awaiting; a thrown error renders the digest-only boundary; the empty 'nothing selected' state is honest (no fabricated zeros)"
    - "The compare surface has zero raw text-[Npx] (ready for the Wave-3 no-raw-font-px=error ratchet)"
  artifacts:
    - path: "src/app/(dashboard)/compare/loading.tsx"
      provides: "Comparison-table-anchor RSC skeleton"
      contains: "role=\"status\""
    - path: "src/app/(dashboard)/compare/error.tsx"
      provides: "Client error boundary, unstable_retry, digest-only"
      contains: "unstable_retry"
    - path: "src/components/strategy/CompareTable.test.tsx"
      provides: "CompareTable @container / tabular-nums / name-cell title= behavior tests"
      contains: "@container"
  key_links:
    - from: "src/components/strategy/CompareTable.tsx"
      to: "@container / ResponsiveTable"
      via: "container-query host"
      pattern: "@container"
    - from: "src/app/(dashboard)/compare/page.tsx"
      to: "withPublishedOnly"
      via: "visibility gate preserved"
      pattern: "withPublishedOnly"
---

<objective>
Bring the **compare** surface (the `/compare` side-by-side comparison route; Bridge is NOT here — it's the cream-family BridgeWidget set inside allocations, RESEARCH-resolved) fully to the v1.4 bar:
- raise the page from no cap to fluid-fill `max-w-[1920px] mx-auto` (data surface), tuning column behavior so the comparison table reads deliberate at the wider measure, not stranded in whitespace (Pitfall 4);
- migrate `CompareTable` to `@container` (TYPE-04) preserving `tabular-nums`;
- fix the compare table name cells (single-line + `title=`, tabular context — audit treatment);
- add route-level `loading.tsx` (multi-column comparison-table dominant anchor) + `error.tsx` (STATE-01) with render tests;
- preserve the existing honest "nothing selected" / degenerate empty states (STATE-02);
- migrate the surface's raw `text-[Npx]` onto `--text-*` tiers so it is CLEAN. The `no-raw-font-px=error` ratchet for this glob lands in the consolidated Wave-3 plan 52-07 (single owner of `eslint.config.mjs`) to avoid a shared-file collision across the parallel surface plans.

Purpose: compare is a data surface; the `withPublishedOnly` visibility gate and `?ids=` slice-to-4 parsing are preserved exactly.
Output: 1 page MOD, 2 new route files + 2 tests, 1 component MOD + 1 new component test (`CompareTable.test.tsx`). This plan does NOT edit eslint.config.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md
@.planning/phases/52-per-surface-application-allocator-journey/52-PATTERNS.md
@.planning/audits/truncation-audit.md
@DESIGN.md

<interfaces>
<!-- Compare page wrapper (src/app/(dashboard)/compare/page.tsx:127) — the success return: -->
  `return (<><PageHeader title={title} breadcrumb={[…]} /><div className="space-y-8"><CompareTable items={items} /><CompareEquityOverlay … /><CompareCorrelationMatrix … /></div></>)`
  ADD the fluid-fill cap: wrap the `<div className="space-y-8">` in `<div className="mx-auto max-w-[1920px]">` (data surface). KEEP the auth gate above (`if (!user) redirect("/login")` ~line 30) and `withPublishedOnly` (~line 2/56) EXACTLY.
  The "nothing selected" / fewer-than-2 branches return early (~lines 36-42, 99-105) with honest copy — extend, don't replace; UI-SPEC copy: heading "Select strategies to compare", body "Pick two or more strategies from discovery to see them side by side."

<!-- CompareTable (src/components/strategy/CompareTable.tsx) — migrate to @container. It is the comparison-table component compare renders; one column per selected strategy. Mark the table's containment region @container (host on ResponsiveTable if it wraps one, else on the table's scroll wrapper) and use @-prefixed variants for column behavior at the wider measure (un-collapse columns at @min-* rather than spreading 2 columns across 1920px — Pitfall 4). KEEP tabular-nums on every numeric cell. Name cells: single-line + title={strategy.name} (tabular context, audit treatment (b)). -->

<!-- CompareCorrelationMatrix.tsx:90/113 axis labels already carry title= (LEGITIMATE clips) — DO NOT remove their recovery affordance. This plan does NOT touch CompareCorrelationMatrix except to preserve those. -->

CompareTable.test.tsx is NEW in this plan (it does not exist yet). It is owned solely by 52-03, sits beside CompareTable.tsx, and is covered by the coverage gate like every other `*.test.tsx`. It holds the three CompareTable behavior assertions (written RED in Task 1 before the migration), distinct from the phase-52-container-tabular-nums.test.tsx registry which 52-01 owns — no cross-plan ownership conflict.

loading.tsx analog: src/app/factsheet/[id]/v2/loading.tsx (structure) + src/app/(dashboard)/discovery/[slug]/loading.tsx (the Skeleton row-loop idiom for the table). Dominant anchor = a multi-column comparison-table skeleton (one column per selected strategy) above a correlation-matrix placeholder. Closed by sr-only role="status".

error.tsx analog: src/app/(dashboard)/error.tsx (verbatim shape — unstable_retry, digest-only, "use client").

Skeleton primitives: src/components/ui/Skeleton.tsx — Skeleton/SkeletonText/SkeletonCard.

Test analog: src/app/strategy/[id]/v2/error.test.tsx (route-file render test); src/components/strategy/StrategyTable.test.tsx (component @container/tabular-nums assertion idiom for the new CompareTable.test.tsx).

Frozen-island note: none of these files are frozen islands. Run the 52-frozen-spine guard after to prove scenario.ts/compute.ts untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Page-shell fluid-fill + CompareTable @container + name-cell title= (APPLY-01 / TYPE-03 / TYPE-04 / TYPE-02)</name>
  <files>src/app/(dashboard)/compare/page.tsx, src/components/strategy/CompareTable.tsx, src/components/strategy/CompareTable.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/compare/page.tsx (the success return ~127 + the auth gate ~30 + withPublishedOnly ~2/56 + the early-return empty branches ~36/99)
    - src/components/strategy/CompareTable.tsx (the comparison table to migrate; find its scroll/containment wrapper + numeric cells + name cells)
    - src/components/strategy/CompareCorrelationMatrix.tsx (lines 90/113 — confirm the title= recovery you must NOT remove)
    - src/components/strategy/StrategyTable.tsx (the @container host idiom)
    - src/components/strategy/StrategyTable.test.tsx (the component-level @container/tabular-nums assertion idiom — model CompareTable.test.tsx on it)
    - src/components/ResponsiveTable.tsx (the @container host that merges className onto the scroll region)
    - DESIGN.md (data density > card density governs the ultra-wide fill)
  </read_first>
  <behavior>
    - Test 1: CompareTable numeric cells carry `tabular-nums` (+ font-mono/font-metric) — alignment preserved after the @container migration.
    - Test 2: the table containment region carries `@container`; column behavior at the wider measure uses @-prefixed variants, not viewport `lg:`.
    - Test 3: name cells render `title={strategy.name}` so the full name recovers on hover (TYPE-02 tabular treatment) and no name cell drops the name for a fabricated placeholder.
  </behavior>
  <action>
    RED FIRST: create `src/components/strategy/CompareTable.test.tsx` and write the three behavior assertions above into it (model the render + className-query idiom on `StrategyTable.test.tsx`): render `CompareTable` with a 2-strategy fixture, then assert (1) numeric cells carry `tabular-nums`, (2) the containment region carries `@container` (and no viewport `lg:` drives the column behavior), (3) each name cell has `title={strategy.name}` and renders the real name (never a fabricated placeholder). Run it and confirm it FAILS (the migration is not done yet). THEN implement: in `compare/page.tsx` wrap the success-return content (`<div className="space-y-8">…`) in a `<div className="mx-auto max-w-[1920px]">` shell (data surface fluid-fill). Do NOT remove the `redirect("/login")` auth gate or `withPublishedOnly`. In `CompareTable.tsx`, mark the table's scroll/containment wrapper `className="@container"` (use `ResponsiveTable` as the host if the table already wraps one; else add the class to the existing overflow wrapper) and tune column behavior with `@min-[Npx]:`/`@max-*:` variants so the comparison columns fill the wider canvas deliberately (un-collapse at `@min-*`, never strand 2 columns in whitespace — Pitfall 4). Use plain `@container`, never `@container-size` (Pitfall 1). Keep `tabular-nums` on every numeric cell. On the strategy-name cells add `title={strategy.name}` single-line (tabular context). Re-run `CompareTable.test.tsx` until GREEN. Do NOT touch CompareCorrelationMatrix except to leave its existing title= intact.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/compare/page.test.tsx" "src/components/strategy/CompareTable.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/strategy/CompareTable.test.tsx` exists and exits 0 (the three behavior tests pass after implementation).
    - `grep -c "max-w-\[1920px\]" "src/app/(dashboard)/compare/page.tsx"` returns >= 1.
    - `grep -c "withPublishedOnly" "src/app/(dashboard)/compare/page.tsx"` is unchanged (gate preserved) AND `redirect("/login")` still present.
    - `grep -c "@container" "src/components/strategy/CompareTable.tsx"` returns >= 1; `@container-size` returns 0.
    - `grep -c "title=" "src/components/strategy/CompareTable.tsx"` returns >= 1 on the name cells.
    - `grep -c "title=" "src/components/strategy/CompareCorrelationMatrix.tsx"` is unchanged (>= 2 — legitimate clips preserved).
    - Both vitest files exit 0.
  </acceptance_criteria>
  <done>Compare fluid-fills to 1920 with deliberate column behavior; CompareTable is @container with tabular-nums preserved and name cells recovered via title=, all proven by the new CompareTable.test.tsx (RED→GREEN); correlation-matrix legitimate clips intact; visibility gate preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Route loading.tsx + error.tsx with tests (STATE-01 / STATE-02 / ASVS V7)</name>
  <files>src/app/(dashboard)/compare/loading.tsx, src/app/(dashboard)/compare/loading.test.tsx, src/app/(dashboard)/compare/error.tsx, src/app/(dashboard)/compare/error.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/loading.tsx (the fidelity bar)
    - src/app/(dashboard)/discovery/[slug]/loading.tsx (the Skeleton row-loop idiom for the table skeleton)
    - src/app/(dashboard)/error.tsx (the error.tsx shape to copy)
    - src/components/ui/Skeleton.tsx (assemble from these)
    - src/app/strategy/[id]/v2/error.test.tsx (the render-test precedent)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md
    - .planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md (Copywriting Contract + compare dominant anchor = side-by-side comparison table)
  </read_first>
  <action>
    Create `src/app/(dashboard)/compare/loading.tsx` — RSC (no "use client") at `mx-auto max-w-[1920px] px-6 py-6`, dominant anchor = a multi-column comparison-table skeleton (assemble columns with `Skeleton`, mirror the discovery row-loop), above a correlation-matrix placeholder, closed by `<p className="sr-only" role="status" aria-live="polite">Loading comparison — computing analytics.</p>`. Per RESEARCH Pattern 2: `animate-pulse` may sit on the RSC shell wrapper (the sanctioned route-shell idiom, matching the factsheet `<article className="… animate-pulse">`) with the `Skeleton` primitives inside it — do NOT hand-roll bespoke per-element pulse divs. Create `src/app/(dashboard)/compare/error.tsx` copying the `(dashboard)/error.tsx` shape (`"use client"`, `unstable_retry`, `console.error("[compare-error]", error)`, the UI-SPEC error copy, `{error.digest && Error ID}` digest-only, `<Button onClick={() => unstable_retry()}>`). Create `loading.test.tsx` (smoke-render + `getByRole("status")`) and `error.test.tsx` (heading + retry invokes unstable_retry + console.error fires).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/compare/loading.test.tsx" "src/app/(dashboard)/compare/error.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `compare/loading.tsx` + `error.tsx` + both `.test.tsx` exist; the vitest command exits 0.
    - `loading.tsx` has NO `"use client"` and contains `role="status"`.
    - `error.tsx` contains `unstable_retry` and renders no `error.message` (grep returns 0).
    - `error.test.tsx` asserts the retry prop is invoked.
  </acceptance_criteria>
  <done>Compare has a comparison-table-anchored loading skeleton + a digest-only error boundary, both tested green.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate raw text-[Npx] on compare + verify honest empty states (DS-04 prep / STATE-02)</name>
  <files>src/app/(dashboard)/compare/page.tsx, src/components/strategy/CompareTable.tsx</files>
  <read_first>
    - src/app/(dashboard)/compare/page.tsx (the early-return empty branches ~36/99 — confirm honest, no fabricated zeros)
    - src/components/strategy/CompareTable.tsx (audit for raw text-[Npx])
    - DESIGN.md (the --text-* tier mapping)
  </read_first>
  <action>
    Audit the compare surface (`src/app/(dashboard)/compare/**` and `src/components/strategy/CompareTable.tsx`) for raw `text-[Npx]` and migrate each onto a `--text-*` tier (data-surface subset: --text-h3/body/small/caption). Verify the existing "nothing selected" and "<2 selected" empty branches render honest copy (UI-SPEC "Select strategies to compare") via the neutral empty pattern — never a fabricated zero/count-up — and extend their copy to the UI-SPEC strings if they diverge. Do NOT add an eslint override here (52-07 owns eslint.config.mjs and ratchets this glob to error once it is clean). Do not introduce any new truncate without a title.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/compare/page.test.tsx" src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "text-\[[0-9][0-9]*px\]" "src/app/(dashboard)/compare/" "src/components/strategy/CompareTable.tsx" | grep -v "\.test\."` returns ZERO lines (surface clean for the Wave-3 ratchet).
    - The empty-state branch in compare/page.tsx renders no numeric/count-up placeholder and `npx vitest run "src/app/(dashboard)/compare/page.test.tsx"` exits 0.
    - `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` exits 0.
  </acceptance_criteria>
  <done>Compare surface has zero raw font-px, empty states stay honest, frozen islands zero-diff. (The eslint error-ratchet for this glob lands in 52-07.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/unauthed → /compare | redirect("/login") gate must survive the restyle |
| unpublished strategy → compare view | `withPublishedOnly` filters un-published items — must not be stripped |
| RSC error → error.tsx | server error message must not leak to the client |
| clipped name → title= | recovery exposes only the already-rendered name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-08 | Elevation of Privilege | compare/page.tsx withPublishedOnly | mitigate | Keep `withPublishedOnly` + `redirect("/login")` exactly; acceptance asserts both present and unchanged |
| T-52-09 | Information Disclosure | compare/error.tsx | mitigate | digest-only, never error.message (mirror dashboard error.tsx) |
| T-52-10 | Information Disclosure | CompareTable title={name} | mitigate | title exposes only the rendered, already-visibility-filtered strategy.name; never an un-gated field |
| T-52-11 | Tampering | frozen island via restyle | mitigate | scenario.ts/compute.ts untouched; 52-frozen-spine guard in Task 3 verify |
| T-52-SC | Tampering | npm installs | accept | Zero installs (CSS/route-file/chrome only) |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/compare/page.test.tsx" "src/app/(dashboard)/compare/loading.test.tsx" "src/app/(dashboard)/compare/error.test.tsx" "src/components/strategy/CompareTable.test.tsx" src/__tests__/phase-52-container-tabular-nums.test.tsx src/__tests__/phase-52-frozen-spine-guards.test.ts` — green.
- Zero raw `text-[Npx]` remain under the compare surface (the surface is clean; 52-07 then flips its glob to error).
- The 52-01 authed 2560 reflow row covers /compare — verified at the phase gate.
</verification>

<success_criteria>
- Compare fluid-fills to 1920 with deliberate columns and no overflow at 320/2560; CompareTable is @container with tabular-nums preserved and names recovered (proven by CompareTable.test.tsx); route loading/error present and tested; visibility gate intact; surface has zero raw px (ready for the 52-07 ratchet); frozen islands zero-diff.
</success_criteria>

<output>
Create `.planning/phases/52-per-surface-application-allocator-journey/52-03-SUMMARY.md` when done
</output>
</content>
