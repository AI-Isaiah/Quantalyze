---
phase: 52-per-surface-application-allocator-journey
plan: 06
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx
  - src/app/factsheet/[id]/v2/DistributionPanels.tsx
  - src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx
  - src/app/factsheet/[id]/v2/SignaturePanels.tsx
  - src/app/factsheet/[id]/v2/HeatmapPanels.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.tsx
  - src/app/factsheet/[id]/v2/ComparatorPicker.tsx
  - src/app/factsheet/[id]/v2/not-found.tsx
  - src/app/factsheet/[id]/v2/error.tsx
autonomous: true
requirements: [APPLY-01, TYPE-02, TYPE-03, TYPE-04, STATE-02, BP-01]
user_setup: []
must_haves:
  truths:
    - "The factsheet keeps its ~1440 measure and reads well filling toward 1920 (KPI strip + chart panels do not look sparse); no horizontal scroll/overlap at 320px or 2560px"
    - "Factsheet KPI strip + analytical panels respond to their own container width via @container with tabular-nums preserved"
    - "The legitimate factsheet KPI-label clips (FactsheetView:647/653 fixed-label text-ellipsis) keep their bounded-label affordance; the chart-legend recovery (TimeSeriesChart:1109) is preserved"
    - "The composer (?tab=scenario) and risk (?tab=risk) chrome stays conformant; scenario.ts / compute.ts / FactsheetProvider / TimeSeriesChart math are byte-identical (SCENARIO-05 / BODY-02 green, no island RSC-ified)"
    - "The factsheet v2 chrome has zero raw text-[Npx] (ready for the Wave-3 ratchet) EXCEPT the chart-internal files left under the existing chart lint exemption"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "Factsheet KPI strip + panels on @container + fluid tiers"
      contains: "@container"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "@container"
      via: "KPI-strip / panel container host"
      pattern: "@container"
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "factsheet-context / compute.ts"
      via: "FROZEN — chrome-only, no math import change"
      pattern: "FactsheetProvider|useFactsheet"
---

<objective>
Bring the **factsheet** surface (`/factsheet/[id]/v2`) plus the **composer (?tab=scenario)** and **risk (?tab=risk)** chrome to the v1.4 bar — CHROME/LAYOUT ONLY, because the factsheet/composer are frozen client islands (math byte-identical):
- KEEP the factsheet `max-w-[1440px]` measure (do NOT raise to 1920 — UI-SPEC: verify it reads well filling toward 1920; the KPI strip + chart panels must not look sparse);
- migrate the factsheet KPI strip (`FactsheetView.tsx:639` `grid-cols-3 … lg:grid-cols-9`) and the analytical/distribution/cross-signature panels from viewport breakpoints to `@container` (TYPE-04), preserving `tabular-nums` and the legitimate fixed-KPI-label clips at :647/653;
- migrate the heavy factsheet-chrome raw `text-[Npx]` baseline onto `--text-*` tiers — EXCEPT the chart-internal files (`TimeSeriesChart.tsx`, `HistogramChart.tsx` internals), which sit under the existing chart lint exemption and are math/SVG-coordinate-adjacent;
- confirm the composer/risk chrome is conformant WITHOUT touching `scenario.ts`/`compute.ts`/`FactsheetProvider`/`TimeSeriesChart` math.

The factsheet ALREADY has `loading.tsx`/`error.tsx` (the fidelity bar) — no STATE-01 route-file work here; `not-found.tsx`/`error.tsx` get a type-migration pass only.
The panel-migration work is split across two tasks (Task 2a = the two real `@container` candidate panels; Task 2b = the remaining lighter type-pass files) so no single task migrates 9 files.
Output: factsheet-chrome MODs (type + container) on the non-frozen panel/view files. This plan does NOT edit eslint.config.mjs (52-07 owns it) and does NOT touch any of the 8 frozen islands.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md
@.planning/phases/52-per-surface-application-allocator-journey/52-PATTERNS.md
@.planning/audits/truncation-audit.md
@DESIGN.md

<interfaces>
<!-- FROZEN ISLANDS — DO NOT EDIT (the 52-frozen-spine guard asserts zero-diff on all 8):
       src/lib/scenario.ts, src/lib/factsheet/compute.ts,
       src/app/factsheet/[id]/v2/factsheet-context.tsx (FactsheetProvider),
       src/hooks/useBreakpoint.ts, src/app/(dashboard)/allocations/lib/montecarlo.worker.ts,
       src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx,
       src/components/charts/TouchTooltip.tsx, src/hooks/useTapPin.ts.
     FactsheetView.tsx IMPORTS the FactsheetProvider/compute output — keep those imports; do NOT change the math, move compute to the server, or remove a "use client" the island depends on. -->

<!-- FactsheetView KPI strip (src/app/factsheet/[id]/v2/FactsheetView.tsx):
       ~line 192: shell `className="factsheet-v2-shell mx-auto max-w-[1440px] px-4 sm:px-6 lg:px-10 …"` — KEEP max-w-[1440px].
       ~line 630: `const lgCols = items.length === 9 ? "lg:grid-cols-9" : "lg:grid-cols-7";`
       ~line 639: `<div className={`grid grid-cols-3 sm:grid-cols-3 md:grid-cols-3 ${lgCols} md:divide-y-0`}>`
     MIGRATE to @container: mark the strip region @container and switch lg:grid-cols-N → @-prefixed (@5xl:/@min-[Npx]:) so the 9-cell strip responds to ITS width. KEEP grid-cols-3 mobile fallback as a container variant.
     LEGITIMATE clips at :647 (KPI tile LABEL, text-ellipsis, fixed label set) and :653 (KPI metric VALUE, tabular-nums) — AUDIT-classified legitimate. PRESERVE the text-ellipsis + recovery context; DO migrate their raw text-[Npx] sizes onto tiers keeping tabular-nums + whitespace-nowrap. -->

<!-- Panel components — split across Task 2a and Task 2b:
       Task 2a (PRIMARY @container candidates — the real container-query work):
         AnalyticalPanels.tsx (21 raw px, grid-cols viewport variants — the heaviest), DistributionPanels.tsx (8 raw px, grid-cols viewport variants).
       Task 2b (remaining lighter type-pass / minimal-container files):
         CrossSignaturePanels.tsx (3 px), SignaturePanels.tsx (4 px), HeatmapPanels.tsx, BatchDPanels.tsx, ComparatorPicker.tsx (2 px), not-found.tsx (2 px), error.tsx (2 px — type-pass only).
     Migrate grid-cols viewport variants to @container where a panel renders at varying width, and migrate raw text-[Npx] onto --text-* tiers. These are presentation components reading already-computed payload props — no math touch. -->

<!-- CHART-INTERNAL FILES under the existing chart lint exemption (eslint.config.mjs ~89-94/126 sets no-raw-font-px: off for chart globs) — math/SVG-coordinate-adjacent: TimeSeriesChart.tsx (5 px, the legit … legend clip at 1109 + truncate() helper at 1129), HistogramChart.tsx (4 px internals). DO NOT migrate raw px inside the SVG-coordinate chart internals; leave them under the chart exemption. The TimeSeriesChart … legend clip (1109) is AUDIT-legitimate (the tooltip IS the recovery) — preserve it. These files are NOT in this plan's files_modified. -->

<!-- not-found.tsx (2 px) + error.tsx (2 px): factsheet already has these; give them a type-migration pass only (text-[Npx] → tiers). error.tsx is already on unstable_retry; do NOT change its signature or leak error.message. -->

<!-- Composer (?tab=scenario) + Risk (?tab=risk) live in src/app/(dashboard)/allocations/AllocationsTabs.tsx — that file's tab shell is owned by the allocations surface flow (52-02). THIS plan does NOT edit AllocationsTabs.tsx; the composer's ScenarioComposer clip + the allocations measure raise are 52-02's. This plan's composer/risk obligation is the VERIFICATION gate (frozen-spine + svg-parity), confirming no island was RSC-ified — not a code edit. -->

Container test contract: src/__tests__/phase-52-container-tabular-nums.test.tsx — append the factsheet KPI strip / a migrated panel to its registry.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: FactsheetView KPI strip @container + fluid-type migration (TYPE-04 / TYPE-02 / APPLY-01)</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (the KPI strip ~630-653, the legit clips at 647/653, ~27 raw text-[Npx] sites, the max-w-[1440px] shell at ~192)
    - src/components/strategy/StrategyTable.tsx (the @container host idiom)
    - .planning/audits/truncation-audit.md (FactsheetView:647/653 rows — LEGITIMATE; preserve recovery)
    - src/__tests__/phase-52-container-tabular-nums.test.tsx (the alignment contract; append the factsheet KPI strip)
    - .planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md (factsheet keeps ~1440; prose category tiers; KPI strip reads --text-small/--text-caption)
    - DESIGN.md (the --text-* tier mapping; 2-weight rule; uppercase tracking for eyebrows not a 3rd weight)
  </read_first>
  <behavior>
    - Test 1: the FactsheetView KPI strip region carries `@container`; its column count responds to container width via @-prefixed variants (not `lg:grid-cols-9`), with the `grid-cols-3` mobile fallback as a container variant.
    - Test 2: every KPI metric VALUE cell keeps `font-mono tabular-nums` (alignment preserved); the KPI LABEL keeps its `text-ellipsis whitespace-nowrap` bounded-label affordance (legitimate clip — not removed).
    - Test 3: the factsheet shell stays `max-w-[1440px]` (not raised) and no fabricated zero appears for a degenerate metric (the existing FactsheetBody.degenerate test stays green).
  </behavior>
  <action>
    In `FactsheetView.tsx`: mark the KPI-strip container `@container` and replace the `lg:grid-cols-9`/`lg:grid-cols-7` viewport variants with `@`-prefixed container variants (`@5xl:grid-cols-9` / `@min-[Npx]:` tuned so the strip responds to its own width), keeping `grid-cols-3` as the container-narrow fallback. Use plain `@container` (Pitfall 1). Migrate the file's raw `text-[Npx]` onto `--text-*` tiers (text-[9px]/[10px]/[11px]/[12px] → --text-micro/caption; text-[13px]/[14px] → --text-small/body; text-[28px]/[36px]/[44px] H1 → --text-page-title with the existing Instrument Serif), preserving `tabular-nums`, `whitespace-nowrap`, and the `text-ellipsis` on the legit KPI label (:647). KEEP `max-w-[1440px]` (do NOT raise). Do NOT change any FactsheetProvider/compute import or move math to the server. Append the factsheet KPI strip to the container test registry.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx" src/__tests__/phase-52-container-tabular-nums.test.tsx src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@container" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` returns >= 1; `@container-size` returns 0; `lg:grid-cols-9` no longer present.
    - `grep -c "tabular-nums" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` is unchanged or higher; `text-ellipsis` still present at the KPI label.
    - `grep -c "max-w-\[1440px\]" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` returns >= 1 (measure kept).
    - `grep -rn "text-\[[0-9][0-9]*px\]" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` returns ZERO lines.
    - The three vitest files exit 0 (degenerate honest, container aligned, frozen islands zero-diff).
  </acceptance_criteria>
  <done>Factsheet KPI strip is @container with tabular-nums + legit-label clip preserved; FactsheetView raw px migrated; ~1440 measure kept; degenerate states honest; frozen islands zero-diff.</done>
</task>

<task type="auto">
  <name>Task 2a: Primary @container panel migration — AnalyticalPanels + DistributionPanels (TYPE-04 / APPLY-01)</name>
  <files>src/app/factsheet/[id]/v2/AnalyticalPanels.tsx, src/app/factsheet/[id]/v2/DistributionPanels.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx (21 raw px + grid-cols viewport variants — the heaviest, the real container candidate)
    - src/app/factsheet/[id]/v2/DistributionPanels.tsx (8 raw px + grid-cols viewport variants — the second real container candidate)
    - src/components/strategy/StrategyTable.tsx (the @container idiom)
    - DESIGN.md (the --text-* tier mapping)
  </read_first>
  <action>
    For AnalyticalPanels and DistributionPanels: where a panel renders at varying width inside the factsheet body/rail, migrate its `grid-cols` / `md:`/`lg:` viewport variants to `@container` variants (mark the panel containment region `@container`, `@`-prefixed children); use plain `@container` (Pitfall 1). Migrate every raw `text-[Npx]` in those two files onto a `--text-*` tier, preserving `tabular-nums` on numeric cells. Do NOT touch any compute.ts / factsheet-analytics.ts import or the math — these read already-computed payload props. Do NOT migrate the SVG-internal chart files (TimeSeriesChart/HistogramChart stay under the chart lint exemption and are not in files_modified).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/AnalyticalPanels.test.tsx" "src/app/factsheet/[id]/v2/DistributionPanels.test.tsx" src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "text-\[[0-9][0-9]*px\]" "src/app/factsheet/[id]/v2/AnalyticalPanels.tsx" "src/app/factsheet/[id]/v2/DistributionPanels.tsx" | grep -v "\.test\."` returns ZERO lines.
    - `grep -rc "@container" "src/app/factsheet/[id]/v2/AnalyticalPanels.tsx" "src/app/factsheet/[id]/v2/DistributionPanels.tsx"` sums >= 1; `@container-size` returns 0 across them.
    - The vitest files exit 0 (frozen islands zero-diff). NOTE: if AnalyticalPanels.test.tsx / DistributionPanels.test.tsx do not yet exist, run the nearest existing panel render test that imports them, or the factsheet body test that mounts them, and record the substituted command in the SUMMARY.
  </acceptance_criteria>
  <done>AnalyticalPanels + DistributionPanels are on @container where width-varying, raw px migrated onto tiers, frozen islands zero-diff.</done>
</task>

<task type="auto">
  <name>Task 2b: Remaining panel type-pass — CrossSignature/Signature/Heatmap/BatchD panels + ComparatorPicker + not-found/error (TYPE-04 / APPLY-01 / ASVS V7)</name>
  <files>src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx, src/app/factsheet/[id]/v2/SignaturePanels.tsx, src/app/factsheet/[id]/v2/HeatmapPanels.tsx, src/app/factsheet/[id]/v2/BatchDPanels.tsx, src/app/factsheet/[id]/v2/ComparatorPicker.tsx, src/app/factsheet/[id]/v2/not-found.tsx, src/app/factsheet/[id]/v2/error.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx (3 px), SignaturePanels.tsx (4 px), HeatmapPanels.tsx, BatchDPanels.tsx
    - src/app/factsheet/[id]/v2/ComparatorPicker.tsx (2 px)
    - src/app/factsheet/[id]/v2/not-found.tsx (2 px), error.tsx (2 px — type-pass only, keep unstable_retry, no error.message)
    - src/components/strategy/StrategyTable.tsx (the @container idiom — apply only where a panel genuinely renders at varying width)
    - DESIGN.md (the --text-* tier mapping)
  </read_first>
  <action>
    For CrossSignaturePanels, SignaturePanels, HeatmapPanels, BatchDPanels: migrate every raw `text-[Npx]` onto a `--text-*` tier, preserving `tabular-nums` on numeric cells; where one of these genuinely renders at varying width, migrate its `grid-cols`/`md:`/`lg:` viewport variants to plain `@container` variants (Pitfall 1) — but the dominant work here is the type-pass, not new containers. Migrate the raw `text-[Npx]` in ComparatorPicker, not-found, and error onto tiers. Give error.tsx a type-pass ONLY (do NOT change its unstable_retry signature; never render error.message). Do NOT touch any compute.ts / factsheet-analytics.ts import or the math — these read already-computed payload props. Do NOT migrate the SVG-internal chart files (TimeSeriesChart/HistogramChart stay under the chart lint exemption and are not in files_modified).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/BatchDPanels.ownbook.test.tsx" "src/app/factsheet/[id]/v2/BatchDPanels.peer-scenario.test.tsx" "src/app/factsheet/[id]/v2/ComparatorPicker.test.tsx" src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "text-\[[0-9][0-9]*px\]" "src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx" "src/app/factsheet/[id]/v2/SignaturePanels.tsx" "src/app/factsheet/[id]/v2/HeatmapPanels.tsx" "src/app/factsheet/[id]/v2/BatchDPanels.tsx" "src/app/factsheet/[id]/v2/ComparatorPicker.tsx" "src/app/factsheet/[id]/v2/not-found.tsx" "src/app/factsheet/[id]/v2/error.tsx" | grep -v "\.test\."` returns ZERO lines.
    - `grep -c "error.message" "src/app/factsheet/[id]/v2/error.tsx"` returns 0 (still digest-only); `grep -c "unstable_retry" "src/app/factsheet/[id]/v2/error.tsx"` is unchanged (signature intact).
    - Across any panel file that gained one, `@container-size` returns 0 (plain `@container` only).
    - The vitest files exit 0.
  </acceptance_criteria>
  <done>The remaining factsheet panels + ComparatorPicker/not-found/error have raw px migrated onto tiers (containers added only where width-varying), error.tsx stays digest-only with its unstable_retry signature intact, frozen islands zero-diff.</done>
</task>

<task type="auto">
  <name>Task 3: Composer/risk chrome conformance verify + frozen-island byte-identity gate (BP-01 / SCENARIO-05 / BODY-02)</name>
  <files>(verification-only — no source edits)</files>
  <read_first>
    - src/__tests__/phase-52-frozen-spine-guards.test.ts (the 52-scoped 8-island zero-diff guard)
    - src/__tests__/phase-29-frozen-spine-guards.test.ts and src/__tests__/phase-30-frozen-spine-guards.test.ts (the existing SCENARIO-05 git-delta gates)
    - e2e/svg-chart-parity.spec.ts (the byte-golden chart-parity gate — read the header to know what it covers)
    - src/lib/scenario.test.ts (the 252-day annualization convention pins — must stay green and unchanged)
  </read_first>
  <action>
    This task is a VERIFICATION gate, not a code edit — it proves the chrome work in 52-02/06 did not RSC-ify or perturb any frozen island. Confirm none of the 8 frozen islands appears in the phase git delta (run the 52-frozen-spine guard). Confirm the existing SCENARIO-05/BODY-02 gates and scenario.test.ts are byte-unchanged and green. Run the svg-chart-parity goldens to confirm no chart byte-diff (the no-recompute boundary was not crossed). Confirm the composer (?tab=scenario) and risk (?tab=risk) panels are reached via the unchanged RSC-page + "use client" tab-tree + next/dynamic({ssr:false}) split (read AllocationsTabs.tsx WITHOUT editing it). If any guard goes red, STOP and revert the offending chrome edit — never `--update-snapshots`. Do NOT edit AllocationsTabs.tsx or any island here.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts src/__tests__/phase-29-frozen-spine-guards.test.ts src/__tests__/phase-30-frozen-spine-guards.test.ts src/lib/scenario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts src/__tests__/phase-29-frozen-spine-guards.test.ts src/__tests__/phase-30-frozen-spine-guards.test.ts src/lib/scenario.test.ts` exits 0 (all islands zero-diff, convention pins intact).
    - `git diff --name-only HEAD | grep -cE "scenario\.ts|factsheet/compute\.ts|factsheet-context\.tsx|useBreakpoint\.ts|montecarlo\.worker\.ts|EquityChart\.tsx|TouchTooltip\.tsx|useTapPin\.ts"` returns 0.
    - `git diff --name-only HEAD | grep -c "AllocationsTabs.tsx"` returns 0 (tab shell untouched by this plan — owned by 52-02).
    - The svg-chart-parity goldens are confirmed unchanged (no `--update-snapshots` was run) — documented in the SUMMARY.
  </acceptance_criteria>
  <done>All 8 frozen islands are byte-identical, SCENARIO-05/BODY-02 + scenario convention pins green, the composer/risk RSC-boundary split is intact, and no chart golden was rebaselined.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chrome restyle → frozen island | A keys/memo/boundary cleanup could accidentally RSC-ify scenario.ts/compute.ts/the chart islands (Pitfall 3) |
| RSC error → factsheet error.tsx | the factsheet error boundary must not leak the server message |
| factsheet visibility/signature gate | the factsheet has a signature/visibility gate; the restyle must not strip it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-18 | Tampering | frozen island RSC-ification via boundary cleanup | mitigate | Treat all 8 islands as read-only-structure; Task 3 runs the 52 + 29 + 30 frozen-spine guards + scenario.test.ts + svg-goldens; never --update-snapshots on a diff |
| T-52-19 | Information Disclosure | factsheet error.tsx | mitigate | Type-pass only (Task 2b); keep digest-only, never render error.message (acceptance asserts error.message absent + unstable_retry signature intact) |
| T-52-20 | Information Disclosure | factsheet visibility/signature gate | mitigate | The chrome migration touches only className/type on presentation components reading already-gated payload props; no signature/visibility check is in a className |
| T-52-SC | Tampering | npm installs | accept | Zero installs (CSS/chrome only) |
</threat_model>

<verification>
- `npx vitest run "src/app/factsheet/[id]/v2/*.test.tsx" src/__tests__/phase-52-frozen-spine-guards.test.ts src/__tests__/phase-29-frozen-spine-guards.test.ts src/__tests__/phase-30-frozen-spine-guards.test.ts src/lib/scenario.test.ts src/__tests__/phase-52-container-tabular-nums.test.tsx` — green.
- `npx playwright test e2e/svg-chart-parity.spec.ts` — goldens unchanged (run at the phase gate).
- Zero raw `text-[Npx]` under factsheet v2 chrome (excluding the chart-exempt SVG internals).
</verification>

<success_criteria>
- Factsheet KPI strip + panels are @container with tabular-nums + legit-label clips preserved; ~1440 measure kept and reads well filling toward 1920; chrome raw px migrated; composer/risk RSC-boundary intact; all 8 frozen islands byte-identical (SCENARIO-05/BODY-02 + svg-goldens green).
</success_criteria>

<output>
Create `.planning/phases/52-per-surface-application-allocator-journey/52-06-SUMMARY.md` when done
</output>
</content>
