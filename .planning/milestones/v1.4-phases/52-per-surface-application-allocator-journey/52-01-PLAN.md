---
phase: 52-per-surface-application-allocator-journey
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/__tests__/phase-52-frozen-spine-guards.test.ts
  - e2e/reflow-sweep-authed.spec.ts
  - src/__tests__/phase-52-container-tabular-nums.test.tsx
autonomous: true
requirements: [BP-01, TYPE-03, TYPE-04]
must_haves:
  truths:
    - "A 52-scoped frozen-spine git-delta guard fails RED if any of the seven frozen island files changes vs the phase baseline"
    - "The authed reflow sweep asserts no horizontal overflow at 2560px on the in-scope allocator surfaces"
    - "A container-query alignment test asserts tabular-nums columns stay aligned across the @container breakpoint (RED contract until a surface migrates)"
  artifacts:
    - path: "src/__tests__/phase-52-frozen-spine-guards.test.ts"
      provides: "Git-delta zero-diff guard over the 7 frozen islands"
      contains: "FROZEN_ISLANDS"
    - path: "e2e/reflow-sweep-authed.spec.ts"
      provides: "2560px ultra-wide reflow row across allocator surfaces"
      contains: "2560"
    - path: "src/__tests__/phase-52-container-tabular-nums.test.tsx"
      provides: "Per-container tabular-nums alignment contract"
      contains: "tabular-nums"
  key_links:
    - from: "src/__tests__/phase-52-frozen-spine-guards.test.ts"
      to: "git diff <baseline> HEAD"
      via: "execFileSync git delta"
      pattern: "changedFiles|git diff"
    - from: "e2e/reflow-sweep-authed.spec.ts"
      to: "assertNoReflow"
      via: "2560 viewport row"
      pattern: "setViewportSize.*2560"
---

<objective>
Wave 0: lay the executable contracts the per-surface plans (52-02…52-06) verify against — BEFORE any surface is touched. Three Wave-0 artifacts:
1. A 52-scoped frozen-spine git-delta guard asserting the seven frozen island files are zero-diff vs the phase baseline (BP-01 belt-and-suspenders over the existing svg-golden + scenario.test.ts gates — RESEARCH Open-Q2).
2. A 2560px ultra-wide reflow row folded into the existing seeded authed sweep (APPLY-01 / TYPE-03 — the sweep is 320px-only today).
3. A container-query tabular-nums alignment test contract (TYPE-04) that the surface plans satisfy as they migrate KpiStrip/CompareTable/factsheet panels to `@container`.

Purpose: the contract precedes the implementation, so a frozen-island RSC-ification, an ultra-wide overflow, or a broken tabular-nums column fails LOUD in CI rather than silently shipping green.
Output: three test files (one new guard, one MOD to the authed sweep, one new container contract).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-per-surface-application-allocator-journey/52-RESEARCH.md
@.planning/phases/52-per-surface-application-allocator-journey/52-VALIDATION.md

<interfaces>
<!-- The phase-30 guard is the near-verbatim analog. Copy its baseline-resolve + git-delta machinery; change only the FROZEN list + the FALLBACK_BASE_SHA + the message text. -->

From src/__tests__/phase-30-frozen-spine-guards.test.ts (the analog to copy):
- `FALLBACK_BASE_SHA` constant = the planning-time HEAD sha (set to `cd2fcb4c` for phase 52).
- `git(args: string[]): string` — execFileSync wrapper (no shell).
- `refExists(ref)`, `resolveBaselineRef()` — fail-loud baseline resolve (Rule 12).
- `changedFiles(base): string[]` — committed `git diff --name-only base HEAD` ∪ untracked `git ls-files --others --exclude-standard`.
- `CHANGED = changedFiles(resolveBaselineRef())` at module load.
- Assertion shape: `expect(CHANGED, "<message>").not.toContain(FROZEN_FILE)`.

The SEVEN frozen island file paths (verified to exist on disk):
- `src/lib/scenario.ts`
- `src/lib/factsheet/compute.ts`
- `src/app/factsheet/[id]/v2/factsheet-context.tsx`
- `src/hooks/useBreakpoint.ts`
- `src/app/(dashboard)/allocations/lib/montecarlo.worker.ts`
- `src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx`
- `src/components/charts/TouchTooltip.tsx`
- `src/hooks/useTapPin.ts`
(Note: that is 8 paths — the "seven islands" in CONTEXT collapses EquityChart+TouchTooltip+useTapPin into one "chart interactivity" island. Guard ALL eight paths.)

From e2e/reflow-sweep-authed.spec.ts (the seeded authed sweep — already FLOW-01 dual-wired in ci.yml's seeded MA-8 list at line ~1278, so an ADDITIVE describe block needs NO new CI wiring — the rotate-stability fold is the precedent):
- `HAS_SEED_ENV` const + `test.skip(!HAS_SEED_ENV, …)` self-skip guard.
- `loginViaForm(page, email, password)` helper.
- `seedTestAllocator()` from `./helpers/seed-test-project`.
- `assertNoReflow(page, anchorSelector)` from `./helpers/reflow` — viewport-agnostic; measures `doc.scrollWidth - doc.clientWidth <= 1`.
- `AUTHED_ROUTES` array shape: `{ path, anchor, label }`. Allocations anchor = `'h1:has-text("My Allocation")'`; the same anchor works at any viewport.

From e2e/helpers/reflow.ts:
- `assertNoReflow(page: Page, anchorSelector: string): Promise<void>` — already 2560-ready; only the `setViewportSize({ width: 2560, height: 1440 })` value is new.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 52-scoped frozen-spine git-delta guard (BP-01)</name>
  <read_first>
    - src/__tests__/phase-30-frozen-spine-guards.test.ts (copy its machinery near-verbatim)
    - src/__tests__/phase-29-frozen-spine-guards.test.ts (the migration-delta sibling, for context on why the baseline resolves the way it does)
    - .planning/phases/52-per-surface-application-allocator-journey/52-RESEARCH.md (Open-Q2; Pitfall 3)
  </read_first>
  <action>
    Create `src/__tests__/phase-52-frozen-spine-guards.test.ts` by copying `phase-30-frozen-spine-guards.test.ts`'s baseline-resolve + git-delta machinery (`git`, `refExists`, `resolveBaselineRef`, `changedFiles`, the module-load `BASE`/`CHANGED` constants, and the fail-loud Rule-12 throw). Change THREE things: (1) set `FALLBACK_BASE_SHA = "cd2fcb4c"` (the phase-52 planning-time HEAD); (2) replace the single `FROZEN_ENGINE`/`FROZEN_ENGINE_TEST` constants with a `FROZEN_ISLANDS` string[] of all EIGHT island paths listed in the `<interfaces>` block; (3) rewrite the describe block to one `it(...)` per island file asserting `expect(CHANGED, "Phase 52 exit gate VIOLATED — <path> changed; it is a FROZEN client island (BP-01 / SCENARIO-05 / BODY-02). Revert it.").not.toContain(path)`, plus the "resolves a real baseline ref" non-vacuity test. Keep the comment header explaining WHY (frozen islands must not be RSC-ified during a restyle; this catches a structural edit even when svg-goldens happen not to diff). Do NOT touch any island file.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/phase-52-frozen-spine-guards.test.ts` exists and `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` exits 0 against a clean tree (no island touched yet, so CHANGED contains none of them).
    - The file references all eight island paths verbatim (grep `grep -c "scenario.ts\|compute.ts\|factsheet-context\|useBreakpoint\|montecarlo.worker\|EquityChart\|TouchTooltip\|useTapPin" src/__tests__/phase-52-frozen-spine-guards.test.ts` returns >= 8).
    - `FALLBACK_BASE_SHA` equals `cd2fcb4c`.
    - The guard fails loud (throws) if the baseline ref cannot resolve (the Rule-12 `resolveBaselineRef` throw is preserved verbatim).
  </acceptance_criteria>
  <done>Guard file exists, runs green on the untouched tree, asserts zero-diff on all eight frozen-island paths via real git delta, and fails loud with no resolvable baseline.</done>
</task>

<task type="auto">
  <name>Task 2: 2560px ultra-wide reflow row folded into the authed sweep (APPLY-01 / TYPE-03)</name>
  <read_first>
    - e2e/reflow-sweep-authed.spec.ts (the seeded authed sweep — add an ADDITIVE describe; the rotate-stability fold at the bottom of this file is the additive-fold precedent that needs no new CI wiring)
    - e2e/helpers/reflow.ts (assertNoReflow is viewport-agnostic — confirm it measures clientWidth, not innerWidth)
    - .github/workflows/ci.yml lines 1266-1283 (confirm reflow-sweep-authed.spec.ts is already in the seeded MA-8 playwright list — so an additive describe inside it needs NO new FLOW-01 wiring)
  </read_first>
  <action>
    Add an ADDITIVE `test.describe("reflow sweep @ 2560px ultra-wide — authed", …)` block at the END of `e2e/reflow-sweep-authed.spec.ts` (mirror the rotate-stability fold pattern already there: it reuses `HAS_SEED_ENV` + `test.skip` + `seedTestAllocator` + `loginViaForm`, so NO new CI wiring). Inside it, seed one allocator in `beforeAll`, login in `beforeEach`, then loop a NEW const `ULTRAWIDE_ROUTES` of the in-scope authed surfaces: `/allocations` (anchor `'h1:has-text("My Allocation")'`), `/allocations?tab=scenario` (same anchor), `/allocations?tab=risk` (same anchor), and `/compare` (anchor a visible content node the seeded allocator renders — confirm against the compare page's `PageHeader title`). For each: `await page.setViewportSize({ width: 2560, height: 1440 })`, `page.goto(r.path)`, throw on HTTP >= 400, then `assertNoReflow(page, r.anchor)`. Each anchor MUST be a route-specific VISIBLE content element (Pitfall 5 / W-02 false-green guard) so a 404/unhydrated page fails loud. Add a header comment noting this is the cheap in-scope subset of the app-wide 2560 row that formally lands Phase 54.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "reflow-sweep-authed" | grep -qx 0 && echo "typecheck-clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "2560" e2e/reflow-sweep-authed.spec.ts` returns >= 1.
    - The new describe block reuses `HAS_SEED_ENV` and `test.skip` (grep both inside the new block region) — it does NOT add a second seed-env const.
    - `npx tsc --noEmit` reports no errors originating in `e2e/reflow-sweep-authed.spec.ts`.
    - Each ULTRAWIDE_ROUTES anchor is a `:has-text` or id selector on a real content node (no bare `body`/`main` chrome anchor).
    - `.github/workflows/ci.yml` is NOT modified (the spec is already in the seeded list — verified in read_first).
  </acceptance_criteria>
  <done>An additive 2560px describe exists in the authed sweep, reuses the existing seed/skip/login scaffolding (no new CI wiring), anchors every route on a visible content node, and typechecks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: container-query tabular-nums alignment contract (TYPE-04)</name>
  <read_first>
    - src/components/strategy/StrategyTable.tsx (the only working @container in the repo, Phase 50-06 — lines ~516-583 show the @container host + @max-3xl:hidden collapse + tabular-nums cells)
    - src/app/(dashboard)/allocations/components/KpiStrip.test.tsx (the existing KpiStrip test shape to mirror for jsdom rendering)
    - src/app/(dashboard)/allocations/components/KpiStrip.tsx (line ~454 `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-5` — the viewport-breakpoint that 52-02 migrates to @container; line ~514/524 the `font-mono … tabular-nums` value cells)
  </read_first>
  <behavior>
    - Test 1 (RED→GREEN as surfaces migrate): for each `@container`-migrated component this phase touches, every columnar numeric cell carries BOTH `font-mono` (or `font-metric`) AND `tabular-nums` so glyph width is fixed under the fluid `--text-*` tier — assert by querying rendered numeric cells and checking their className includes `tabular-nums`.
    - Test 2: a collapsed column (`@max-3xl:hidden`) is the RIGHTMOST in priority order (no ragged middle gap) and its real value relocates into a `<details>`/expandable, never a fabricated em-dash or zero (no-invented-data / STATE-02).
    - Test 3 (contract marker): the test imports a `CONTAINER_MIGRATED` registry array (declared in this file, seeded empty or with StrategyTable) that each surface plan appends its migrated component to — so the contract is non-vacuous and tracks coverage.
  </behavior>
  <action>
    Create `src/__tests__/phase-52-container-tabular-nums.test.tsx`. Render the EXISTING `StrategyTable` `@container` precedent (it already ships migrated in Phase 50-06) and assert its numeric `<td>` cells carry `tabular-nums` and `font-metric`/`font-mono`, that `@max-3xl:hidden` collapse columns are right-priority, and that no collapsed cell renders a fabricated `—`/`0` in place of the real value (the real value relocates into the per-row details). Declare a `CONTAINER_MIGRATED` list of component identifiers the phase migrates (`["StrategyTable", "KpiStrip", "CompareTable"]`) and assert it is non-empty so the contract is tracked. Use the KpiStrip test's jsdom render idiom; mock only what those tests already mock. This is the alignment-preservation gate the 52-02/03/06 container migrations must keep green.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-52-container-tabular-nums.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/phase-52-container-tabular-nums.test.tsx` exists; `npx vitest run` on it exits 0.
    - `grep -c "tabular-nums" src/__tests__/phase-52-container-tabular-nums.test.tsx` returns >= 1.
    - The test asserts at least one collapsed-column cell does NOT contain a literal `—` or fabricated `0` in place of the real value (the no-invented-data assertion).
    - `CONTAINER_MIGRATED` is declared and non-empty (grep returns a match).
  </acceptance_criteria>
  <done>A container-query alignment contract exists, runs green against the StrategyTable precedent, asserts tabular-nums + right-priority collapse + no fabricated em-dash, and tracks the migrated-component registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git working tree → CI guard | The frozen-spine guard reads the real git delta; a tampered baseline sha could let an island edit pass |
| seeded authed session → reflow sweep | The 2560 sweep runs against a real seeded allocator session in CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | phase-52-frozen-spine guard baseline | mitigate | `resolveBaselineRef()` fails LOUD (throws, Rule 12) if no baseline resolves — never silently passes; FALLBACK_BASE_SHA pinned to the planning HEAD |
| T-52-02 | Repudiation | 2560 reflow false-green | mitigate | Every route anchored on a route-specific VISIBLE content node (Pitfall 5) so a 404/login/unhydrated page fails loud, not silently green |
| T-52-03 | Spoofing | unseeded reflow run | accept | `test.skip(!HAS_SEED_ENV)` skips when no seed env — documented W-02 false-green prevention; the seeded CI run is the real gate |
| T-52-SC | Tampering | npm/pip/cargo installs | mitigate | No installs in this plan (CSS/test-only phase); RESEARCH Package Legitimacy Audit records zero packages — no [ASSUMED]/[SUS] checkpoint needed |
</threat_model>

<verification>
- `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts src/__tests__/phase-52-container-tabular-nums.test.tsx` — both green.
- `npx tsc --noEmit` — no errors in the modified e2e spec.
- No frozen-island file appears in `git diff --name-only` (the guard self-proves on the clean tree).
</verification>

<success_criteria>
- The 52-frozen-spine guard exists, runs green on the untouched tree, and would fail RED if any of the eight island paths were edited.
- The authed reflow sweep has a 2560px row over the allocator surfaces, reusing existing seed scaffolding with no new CI wiring.
- The container tabular-nums contract exists and tracks the components 52-02/03/06 will migrate.
</success_criteria>

<output>
Create `.planning/phases/52-per-surface-application-allocator-journey/52-01-SUMMARY.md` when done
</output>
