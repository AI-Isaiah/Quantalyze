---
phase: 52-per-surface-application-allocator-journey
plan: 07
type: execute
wave: 3
depends_on: ["52-02", "52-03", "52-04", "52-05", "52-06"]
files_modified:
  - eslint.config.mjs
autonomous: true
requirements: [APPLY-01, TYPE-02]
user_setup: []
must_haves:
  truths:
    - "no-raw-font-px is error for every allocator-journey surface migrated in 52-02..06 (allocations, compare, discovery, single-strategy, factsheet chrome)"
    - "npm run lint exits 0 — the migrated globs have zero raw text-[Npx], so flipping them to error does not introduce a single new lint failure"
    - "The repo-wide no-raw-font-px stays warn (the remaining dirty 53-surface globs are untouched — the strangler ratchet is per-surface, not big-bang)"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Per-surface no-raw-font-px=error overrides for the 52 surfaces"
      contains: "no-raw-font-px"
  key_links:
    - from: "eslint.config.mjs"
      to: "the 52-migrated surface globs"
      via: "no-raw-font-px: error override blocks"
      pattern: "quantalyze/no-raw-font-px"
---

<objective>
Consolidated Wave-3 strangler ratchet: flip `quantalyze/no-raw-font-px` to **error** for every allocator-journey surface that 52-02..06 migrated to the fluid `--text-*` spine, in ONE edit to `eslint.config.mjs`.

This plan is the SINGLE owner of `eslint.config.mjs` for phase 52 — it exists so the five parallel Wave-2 surface plans never collide on that shared file. Each surface plan left its glob CLEAN (zero raw `text-[Npx]`); this plan adds the `error` overrides for all of them at once and proves `npm run lint` stays green.

Output: one `eslint.config.mjs` MOD adding the per-surface `error` overrides. No surface code is touched here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-per-surface-application-allocator-journey/52-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- eslint.config.mjs (the existing precedent, ~line 82): -->
  `{ files: ["src/lib/design-tokens/**"], rules: { "quantalyze/no-raw-font-px": "error" } }`
  The repo-wide level is `warn` (~line 75, dirty baseline). The comment at ~68-73 states: "The 52/53 strangler ratchets the remaining ~53/54 dirty surfaces to error one at a time." Chart globs are `off` (~89-94/126) — DO NOT flip those.

<!-- The 52-migrated surface globs to flip to error (each was cleaned by its Wave-2 plan): -->
  - 52-02 allocations: `src/app/(dashboard)/allocations/**`
  - 52-03 compare:     `src/app/(dashboard)/compare/**`, `src/components/strategy/CompareTable.tsx`
  - 52-04 discovery:   `src/app/(dashboard)/discovery/**`, `src/components/strategy/StrategyGrid.tsx`
  - 52-05 single-strategy: `src/app/strategy/[id]/**`
  - 52-06 factsheet chrome: `src/app/factsheet/[id]/v2/**` BUT with the chart-internal SVG files EXEMPT — the existing chart `off` glob already exempts TimeSeriesChart/HistogramChart; ensure the new factsheet `error` override does NOT re-enable the rule on those chart-internal files (order the override BEFORE / scope it so the chart `off` glob still wins, or list the factsheet glob excluding the chart files). Verify by running lint — if a chart-internal raw px trips error, the exemption ordering is wrong.

<!-- ESLint flat-config precedence: later objects override earlier ones for matching files. Place the factsheet error override so the chart `off` glob (TimeSeriesChart/HistogramChart) is NOT clobbered — i.e. keep the chart `off` block AFTER the factsheet error block, OR exclude the chart files from the factsheet error glob via ignores. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-surface no-raw-font-px=error overrides for the 52 surfaces + prove lint green</name>
  <read_first>
    - eslint.config.mjs (the full file — the repo-wide warn ~75, the design-tokens error precedent ~82, the chart off globs ~89-94/126, the strangler comment ~68-73, and the flat-config object ordering)
    - .planning/phases/52-per-surface-application-allocator-journey/52-RESEARCH.md (the Code Example for the per-glob override + the "chart globs stay off" note)
    - .planning/phases/52-per-surface-application-allocator-journey/52-02-SUMMARY.md (confirm allocations was left clean)
    - .planning/phases/52-per-surface-application-allocator-journey/52-03-SUMMARY.md (confirm compare clean)
    - .planning/phases/52-per-surface-application-allocator-journey/52-04-SUMMARY.md (confirm discovery clean)
    - .planning/phases/52-per-surface-application-allocator-journey/52-05-SUMMARY.md (confirm single-strategy clean)
    - .planning/phases/52-per-surface-application-allocator-journey/52-06-SUMMARY.md (confirm factsheet chrome clean + the chart-exempt files)
  </read_first>
  <action>
    Add to `eslint.config.mjs`, after the `src/lib/design-tokens/**` error block, the per-surface `error` overrides for the five migrated surfaces: `{ files: ["src/app/(dashboard)/allocations/**", "src/app/(dashboard)/compare/**", "src/components/strategy/CompareTable.tsx", "src/app/(dashboard)/discovery/**", "src/components/strategy/StrategyGrid.tsx", "src/app/strategy/[id]/**", "src/app/factsheet/[id]/v2/**"], rules: { "quantalyze/no-raw-font-px": "error" } }`. CRITICAL: the chart-internal `off` glob (TimeSeriesChart/HistogramChart and any chart-coordinate files) MUST still win for those files — keep the chart `off` block AFTER this error block (flat-config: later wins) OR exclude the chart files from the factsheet glob via `ignores`. Update the strangler comment (~68-73) to note the phase-52 surfaces are now ratcheted (53 surfaces remain for Phase 53). Run `npm run lint`; because every migrated surface was cleaned in Wave 2, it must exit 0. If lint goes red on a surface, the corresponding Wave-2 plan missed a raw px — STOP and report which file/glob (do NOT migrate code here; that is the surface plan's job).
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -8; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run lint` exits 0.
    - `grep -c "no-raw-font-px.*error\|\"error\"" eslint.config.mjs` is >= 2 (design-tokens + the new 52 block) and the 52 block lists all five surface globs.
    - Flipping a single migrated surface's glob to error introduces ZERO new lint errors (proven by the exit-0 run) — confirming Wave 2 left every surface clean.
    - The repo-wide `no-raw-font-px` is still `warn` (grep the repo-wide rule line is unchanged) and the chart `off` globs are intact (`grep -c "no-raw-font-px.*off\|\"off\"" eslint.config.mjs` unchanged).
    - A deliberate scratch check (optional, documented in SUMMARY): temporarily adding `text-[13px]` to a migrated file makes `npm run lint` FAIL, then removing it restores green — proving the error override is non-vacuous.
  </acceptance_criteria>
  <done>All five 52 surface globs are at no-raw-font-px=error, lint is green (Wave 2 left them clean), the repo-wide warn + chart off exemptions are intact, and the error ratchet is proven non-vacuous.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eslint config edit → chart exemption | A mis-ordered error override could re-enable no-raw-font-px on the math-adjacent chart SVG internals, forcing a token migration into chart-coordinate code that should stay exempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-21 | Tampering | factsheet error override clobbering the chart off glob | mitigate | Order the chart `off` block AFTER the factsheet error block (flat-config later-wins) or exclude chart files via ignores; acceptance asserts the chart off globs are intact and lint is green (a clobber would red on TimeSeriesChart raw px) |
| T-52-22 | Denial of Service | a missed Wave-2 px making lint red | mitigate | The plan STOPS and reports the offending glob rather than migrating code here — keeps the ratchet honest and surfaces an incomplete surface migration loudly (Rule 12) |
| T-52-SC | Tampering | npm installs | accept | Zero installs (config-only edit) |
</threat_model>

<verification>
- `npm run lint` — exit 0 with all five 52 surface globs at error.
- The chart `off` exemption still applies to TimeSeriesChart/HistogramChart (no error forced into chart-coordinate code).
- Repo-wide no-raw-font-px stays warn (Phase-53 surfaces untouched).
</verification>

<success_criteria>
- Every allocator-journey surface migrated in 52-02..06 is at no-raw-font-px=error; lint is green because Wave 2 cleaned them; the strangler stays per-surface (repo-wide warn + chart off intact); the ratchet is proven non-vacuous.
</success_criteria>

<output>
Create `.planning/phases/52-per-surface-application-allocator-journey/52-07-SUMMARY.md` when done
</output>
