---
phase: 52-per-surface-application-allocator-journey
plan: 05
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/app/strategy/[id]/page.tsx
  - src/app/strategy/[id]/loading.tsx
  - src/app/strategy/[id]/loading.test.tsx
  - src/app/strategy/[id]/error.tsx
  - src/app/strategy/[id]/error.test.tsx
autonomous: true
requirements: [APPLY-01, TYPE-02, TYPE-03, STATE-01, STATE-02, BP-01]
user_setup: []
must_haves:
  truths:
    - "The single-strategy page keeps its narrow readable prose measure (max-w-3xl) — it does NOT fluid-fill — with no horizontal scroll/overlap at 320px or 2560px"
    - "Navigating to /strategy/[id] shows a page-title + headline-metric anchored skeleton at the narrow measure while awaiting"
    - "A thrown error in the strategy subtree renders the digest-only route error boundary with a working retry"
    - "The strategy/[id] surface has zero raw text-[Npx] (ready for the Wave-3 no-raw-font-px=error ratchet)"
  artifacts:
    - path: "src/app/strategy/[id]/loading.tsx"
      provides: "Narrow-measure page-title + headline-metric skeleton"
      contains: "role=\"status\""
    - path: "src/app/strategy/[id]/error.tsx"
      provides: "Route-level error boundary, unstable_retry, digest-only"
      contains: "unstable_retry"
  key_links:
    - from: "src/app/strategy/[id]/loading.tsx"
      to: "@/components/ui/Skeleton"
      via: "narrow-measure skeleton at max-w-3xl"
      pattern: "max-w-3xl"
    - from: "src/app/strategy/[id]/page.tsx"
      to: "getPublicStrategyDetail"
      via: "server-fetch in the page (not a layout)"
      pattern: "getPublicStrategyDetail"
---

<objective>
Bring the **single-strategy** surface (`/strategy/[id]` → v2 subtree) to the v1.4 bar:
- KEEP the narrow readable prose measure (`max-w-3xl`) — single-strategy is a prose/detail page and does NOT fluid-fill (UI-SPEC layout table), but verify it holds with no overflow/overlap at 320px and 2560px;
- add route-level `loading.tsx` (dominant anchor = page-title + headline-metric block at the narrow measure) — the `strategy/[id]` route has none today;
- add a route-level `error.tsx` for the strategy subtree (today only the `/v2` child has one) with a working `unstable_retry` and digest-only info;
- with render tests for both (coverage gate);
- migrate any raw `text-[Npx]` on the surface onto `--text-*` tiers (prose subset: --text-page-title/h3/body/caption) so it is CLEAN (the `no-raw-font-px=error` ratchet for this glob lands in the consolidated Wave-3 plan 52-07, single owner of `eslint.config.mjs`).

The frozen islands `EquityChart`/`TouchTooltip`/`useTapPin` may appear inside the v2 subtree — adding a sibling `loading.tsx`/`error.tsx` does NOT RSC-ify them (they wrap the page in Suspense/error boundaries).
Output: 1 page MOD (verify-only if no raw px), 2 new route files + 2 tests. This plan does NOT edit eslint.config.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md
@.planning/phases/52-per-surface-application-allocator-journey/52-PATTERNS.md
@.planning/audits/truncation-audit.md
@DESIGN.md

<interfaces>
<!-- strategy/[id]/page.tsx: server-fetches via getPublicStrategyDetail(id) IN THE PAGE (lines ~23/81 — stays in the page so loading.tsx renders; Pitfall 5). The success return wraps in `<div className="mx-auto max-w-3xl px-4 py-12 sm:px-6">` (~line 117) — KEEP max-w-3xl (prose page, do NOT fluid-fill). The page also handles a not-found/error result branch. -->

loading.tsx analog: src/app/factsheet/[id]/v2/loading.tsx (structure) but at the NARROW measure `mx-auto max-w-3xl px-4 py-12 sm:px-6`. Dominant anchor (UI-SPEC) = a `--text-page-title` H1 line (Instrument Serif) + the primary headline-metric block at the TOP of the narrow measure, then an equity-chart placeholder below at the same narrow width. Assemble from Skeleton primitives; closed by `<p className="sr-only" role="status" aria-live="polite">Loading strategy — computing analytics.</p>`.

error.tsx analog: src/app/strategy/[id]/v2/error.tsx (the secondary analog — it uses usePathname() for a fallback link) + src/app/(dashboard)/error.tsx (the canonical unstable_retry shape). This route-level error covers the WHOLE strategy subtree (not v2-specific). Use `"use client"`, `unstable_retry` (NOT reset), `console.error("[strategy-error]", error)`, the UI-SPEC error copy, `{error.digest && Error ID: {digest}}` digest-only, `<Button onClick={() => unstable_retry()}>Try again</Button>`. Keep the v2/error.tsx as-is (do not edit it — that is the child boundary).

Skeleton primitives: src/components/ui/Skeleton.tsx.
Test analog: src/app/strategy/[id]/v2/error.test.tsx.

Typography subset (UI-SPEC prose category): --text-page-title (H1) · --text-h3 (section) · --text-body · --text-caption. Page title stays Instrument Serif.

Frozen-island note: EquityChart/TouchTooltip/useTapPin may render inside the v2 subtree — do NOT touch them; the new route files only wrap the page. Run the 52-frozen-spine guard after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Route loading.tsx (narrow-measure, page-title anchor) + tests (STATE-01 / TYPE-03)</name>
  <read_first>
    - src/app/strategy/[id]/page.tsx (the max-w-3xl prose return ~117 + getPublicStrategyDetail fetch ~23/81 — confirm fetch is in the page)
    - src/app/factsheet/[id]/v2/loading.tsx (the fidelity structure to mirror at the narrow measure)
    - src/components/ui/Skeleton.tsx (assemble from these)
    - src/app/strategy/[id]/v2/error.test.tsx (the render-test precedent for the loading smoke test)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md (the layout-data caveat — fetch must stay in the page)
    - .planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md (single-strategy dominant anchor = page-title + headline-metric block; prose category ≤4 tiers)
  </read_first>
  <action>
    Create `src/app/strategy/[id]/loading.tsx` — RSC (no "use client") at `mx-auto max-w-3xl px-4 py-12 sm:px-6` (the NARROW prose measure — do NOT use 1920). Dominant anchor = a `--text-page-title`-sized H1 skeleton line (a `Skeleton className="h-8 w-2/3"` evoking the Instrument Serif title) + a headline-metric block (one or two `Skeleton` metric rows), then an equity-chart placeholder (`Skeleton className="h-[280px] w-full"`) below at the same narrow width. Close with `<p className="sr-only" role="status" aria-live="polite">Loading strategy — computing analytics.</p>`. Create `loading.test.tsx` (smoke-render + `getByRole("status")` + assert the narrow `max-w-3xl` wrapper exists). Do NOT move getPublicStrategyDetail out of the page.
  </action>
  <verify>
    <automated>npx vitest run "src/app/strategy/[id]/loading.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/strategy/[id]/loading.tsx` + `loading.test.tsx` exist; the vitest command exits 0.
    - `loading.tsx` has NO `"use client"`, contains `max-w-3xl` (narrow measure, NOT 1920), and contains `role="status"`.
    - `grep -c "max-w-\[1920px\]" "src/app/strategy/[id]/loading.tsx"` returns 0 (prose page does not fluid-fill).
    - `getPublicStrategyDetail` remains in page.tsx (grep present).
  </acceptance_criteria>
  <done>Single-strategy has a narrow-measure, page-title-anchored loading skeleton with an sr-only liveness hint, tested green; the prose measure is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Route-level error.tsx for the strategy subtree + test (STATE-01 / ASVS V7)</name>
  <read_first>
    - src/app/strategy/[id]/v2/error.tsx (the secondary analog — usePathname fallback link; DO NOT edit this child boundary)
    - src/app/(dashboard)/error.tsx (the canonical unstable_retry + digest-only shape to copy)
    - src/components/ui/Button.tsx (the Button used in the error CTA)
    - src/app/strategy/[id]/v2/error.test.tsx (the render-test precedent)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (unstable_retry + RSC error-message stripping)
    - .planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md (Copywriting Contract error strings)
  </read_first>
  <action>
    Create `src/app/strategy/[id]/error.tsx` covering the whole strategy subtree: `"use client"`, props `{ error: Error & { digest?: string }; unstable_retry: () => void }` (Next 16.2.0 — NOT reset), `useEffect(() => console.error("[strategy-error]", error), [error])`, body "Something went wrong" + "This section encountered an error. You can retry or navigate to another page.", `{error.digest && <p>Error ID: {error.digest}</p>}` (digest ONLY — never error.message), `<Button onClick={() => unstable_retry()}>Try again</Button>` + a fallback Link (mirror the v2 usePathname idiom if a contextual back-link is wanted, else a static "Go to Discovery"). Do NOT edit the v2/error.tsx child. Create `error.test.tsx` (heading present, retry invokes unstable_retry, console.error fires on mount).
  </action>
  <verify>
    <automated>npx vitest run "src/app/strategy/[id]/error.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/strategy/[id]/error.tsx` + `error.test.tsx` exist; the vitest command exits 0.
    - `error.tsx` contains `"use client"` and `unstable_retry`; `grep -c "error.message" "src/app/strategy/[id]/error.tsx"` returns 0 (digest-only).
    - `error.test.tsx` asserts the retry prop is invoked (toHaveBeenCalled on a vi.fn).
    - `git diff --name-only HEAD | grep -c "strategy/\[id\]/v2/error.tsx"` returns 0 (child boundary untouched).
  </acceptance_criteria>
  <done>The strategy subtree has a digest-only route error boundary with working retry, tested green; the v2 child boundary is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate raw text-[Npx] on strategy/[id] + verify narrow-measure preserved (DS-04 prep / TYPE-03 / STATE-02)</name>
  <read_first>
    - src/app/strategy/[id]/page.tsx (audit for raw text-[Npx]; confirm max-w-3xl preserved; confirm honest not-found/degenerate branch)
    - DESIGN.md (prose --text-* tier mapping)
  </read_first>
  <action>
    Audit `src/app/strategy/[id]/**` for raw `text-[Npx]` and migrate each onto a prose `--text-*` tier (--text-page-title/h3/body/caption; H1 stays Instrument Serif). Confirm the page-shell stays `max-w-3xl` (do NOT raise it — prose page). Confirm the existing not-found/degenerate branch renders honest copy (no fabricated zeros). Do NOT edit eslint.config.mjs (52-07 owns it and ratchets this glob to error once clean). Do not introduce any new truncate without a title.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "text-\[[0-9][0-9]*px\]" "src/app/strategy/[id]/" | grep -v "\.test\."` returns ZERO lines (clean for the Wave-3 ratchet).
    - `grep -c "max-w-3xl" "src/app/strategy/[id]/page.tsx"` returns >= 1 (narrow measure preserved); `max-w-[1920px]` absent.
    - `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` exits 0 (EquityChart/TouchTooltip/useTapPin untouched).
  </acceptance_criteria>
  <done>Single-strategy has zero raw font-px, the narrow prose measure is preserved, frozen islands zero-diff. (The eslint error-ratchet for this glob lands in 52-07.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC error → strategy error.tsx | server error message must not leak to the client boundary |
| public strategy fetch | getPublicStrategyDetail returns public-listing data; the page must not over-expose on the new error/loading path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-15 | Information Disclosure | strategy/[id]/error.tsx | mitigate | digest-only, never error.message (mirror dashboard error.tsx); acceptance asserts error.message absent |
| T-52-16 | Tampering | frozen island (EquityChart/TouchTooltip/useTapPin in v2 subtree) | mitigate | Adding sibling loading/error wraps the page in Suspense/error boundaries — it does NOT RSC-ify the islands; 52-frozen-spine guard in Task 3 verify proves them zero-diff |
| T-52-17 | Tampering | v2/error.tsx child boundary | accept | The new route-level error.tsx is additive; the v2 child boundary is left untouched (acceptance asserts it is not in the diff) |
| T-52-SC | Tampering | npm installs | accept | Zero installs (CSS/route-file/chrome only) |
</threat_model>

<verification>
- `npx vitest run "src/app/strategy/[id]/loading.test.tsx" "src/app/strategy/[id]/error.test.tsx" src/__tests__/phase-52-frozen-spine-guards.test.ts` — green.
- Zero raw `text-[Npx]` under strategy/[id] (clean for 52-07).
- Narrow-measure reflow at 320/2560 holds (prose measure stays max-w-3xl).
</verification>

<success_criteria>
- Single-strategy keeps its narrow prose measure with no overflow at 320/2560; route loading (page-title anchor) + error present and tested; surface has zero raw px (ready for the 52-07 ratchet); the frozen islands and the v2 child boundary are untouched.
</success_criteria>

<output>
Create `.planning/phases/52-per-surface-application-allocator-journey/52-05-SUMMARY.md` when done
</output>
