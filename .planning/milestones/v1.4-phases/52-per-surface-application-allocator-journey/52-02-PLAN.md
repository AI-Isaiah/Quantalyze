---
phase: 52-per-surface-application-allocator-journey
plan: 02
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/app/(dashboard)/allocations/page.tsx
  - src/app/(dashboard)/allocations/loading.tsx
  - src/app/(dashboard)/allocations/loading.test.tsx
  - src/app/(dashboard)/allocations/error.tsx
  - src/app/(dashboard)/allocations/error.test.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.tsx
  - src/app/(dashboard)/allocations/components/AlertBanner.tsx
  - src/app/(dashboard)/allocations/components/SavedScenariosList.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/HoldingsTable.tsx
  - src/app/(dashboard)/allocations/components/MonteCarloSection.tsx
  - src/app/(dashboard)/allocations/components/StressVarSection.tsx
  - src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx
autonomous: true
requirements: [APPLY-01, TYPE-02, TYPE-03, TYPE-04, STATE-01, STATE-02, BP-01]
user_setup: []
must_haves:
  truths:
    - "The allocations page-shell fluid-fills toward ~1920px then centers; no horizontal scroll/overlap at 320px or 2560px"
    - "The KpiStrip responds to its own container width via @container, with tabular-nums columns staying aligned"
    - "The three named allocations clip sites (AlertBanner message, SavedScenariosList name, ScenarioComposer constituent name) recover their full text (wrap or title=)"
    - "Navigating to /allocations shows a match-layout skeleton (KPI strip dominant anchor) while the page awaits; a thrown error renders the digest-only error boundary with a working retry"
    - "The allocations surface has zero raw text-[Npx] / inline-hex sites (ready for the Wave-3 no-raw-font-px=error ratchet)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/loading.tsx"
      provides: "Match-layout RSC skeleton, KPI strip anchor"
      contains: "role=\"status\""
    - path: "src/app/(dashboard)/allocations/error.tsx"
      provides: "Client error boundary, unstable_retry, digest-only"
      contains: "unstable_retry"
  key_links:
    - from: "src/app/(dashboard)/allocations/loading.tsx"
      to: "@/components/ui/Skeleton"
      via: "import Skeleton primitives"
      pattern: "Skeleton"
    - from: "src/app/(dashboard)/allocations/components/KpiStrip.tsx"
      to: "@container"
      via: "container-query host class"
      pattern: "@container"
---

<objective>
Bring the **allocations dashboard** (the highest-traffic allocator surface, plus the composer/risk tabs and Bridge widgets it hosts) fully to the v1.4 bar:
- raise the page shell from `max-w-[1280px]` to fluid-fill `max-w-[1920px] mx-auto` (data surface);
- migrate `KpiStrip` from viewport breakpoints to `@container` (TYPE-04, the prime "viewport misleads a 380px-rail card" case), preserving `tabular-nums`;
- fix the three named allocations accidental clips (`AlertBanner.tsx:127`, `SavedScenariosList.tsx:529`, `ScenarioComposer.tsx:2779`) per the truncation audit — wrap or `title=`, never relocate;
- add route-level `loading.tsx` + `error.tsx` (STATE-01) modeled on the factsheet fidelity bar / dashboard error shape, with render tests (coverage gate);
- migrate the surface's raw `text-[Npx]` / inline-hex onto `--text-*` tiers + tokens so the surface is CLEAN. The `no-raw-font-px=error` ratchet for this glob lands in the consolidated Wave-3 plan 52-07 (single owner of `eslint.config.mjs`), to avoid a shared-file write-collision across the parallel surface plans.

Purpose: this is the journey's anchor surface; the composer (`scenario.ts`) and Bridge are chrome-only here.
Output: 1 page MOD, 2 new route files + 2 tests, several component MODs (clip fixes + px migration). This plan does NOT edit eslint.config.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md
@.planning/phases/52-per-surface-application-allocator-journey/52-PATTERNS.md
@.planning/audits/truncation-audit.md
@DESIGN.md

<interfaces>
<!-- Analogs and contracts the executor uses directly — no codebase scavenger hunt. -->

Page shell (src/app/(dashboard)/allocations/page.tsx:63) — RAISE this:
  current: `<div className="max-w-[1280px] mx-auto p-6 pb-20">`
  target:  `<div className="max-w-[1920px] mx-auto p-6 pb-20">`
  KEEP the auth gate above it: `const { data: { user } } = await supabase.auth.getUser(); if (!user) redirect("/login");` and `getMyAllocationDashboard(user.id)` stays in the PAGE, not a layout (Pitfall 5 — else loading.tsx never shows).

loading.tsx analog (src/app/factsheet/[id]/v2/loading.tsx): page-shell `<div>`/`<article className="mx-auto max-w-[…] … animate-pulse">`, header → KPI-strip grid → chart placeholder, closed by `<p className="sr-only" role="status" aria-live="polite">Loading … — computing analytics.</p>`.

Skeleton primitives (src/components/ui/Skeleton.tsx): `Skeleton({className})`, `SkeletonText({lines})`, `SkeletonCard()` — assemble from these; do NOT hand-roll animate-pulse divs.

error.tsx analog (src/app/(dashboard)/error.tsx) — copy verbatim shape:
  `"use client"`; props `{ error: Error & { digest?: string }; unstable_retry: () => void }` (Next 16.2.0 — NOT `reset`); `useEffect(() => console.error("[allocations-error]", error), [error])`; body "Something went wrong" + the UI-SPEC copy; `{error.digest && <p>Error ID: {error.digest}</p>}` (digest ONLY — never error.message, ASVS V7); `<Button onClick={() => unstable_retry()}>Try again</Button>`.

KpiStrip (src/app/(dashboard)/allocations/components/KpiStrip.tsx:454) — MIGRATE the grid:
  current: `className="grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-5"`
  target:  put `@container` on the strip's containment region, switch `sm:`/`lg:` to `@sm:`/`@lg:` (or `@min-[Npx]:`) so the strip responds to ITS width not the viewport. The value cells at ~line 514/524 already carry `font-mono … tabular-nums` — KEEP that on every numeric cell across the container breakpoint (Pitfall 2).

Clip sites (truncation-audit SoT):
  - AlertBanner.tsx:126-128 `<p className="truncate text-[14px]" style={{color:"#1A1A2E"}}>{head.message}</p>` → wrap (`break-words min-w-0`) OR `title={head.message}`; ALSO migrate `text-[14px]`→`--text-body`/`--text-small` tier and the inline `#1A1A2E` hex → a text token (raw hex violates DS-04).
  - SavedScenariosList.tsx:528-530 `<span className="truncate text-sm text-text-primary">{row.name}</span>` (list row, not tabular) → wrap `break-words min-w-0`.
  - ScenarioComposer.tsx:2779 `<span className="text-[12px] … truncate max-w-[160px]">{strategyNames[id] ?? id.slice(0,8)}</span>` (table-aligned <li>) → single-line + `title={strategyNames[id] ?? id}`; migrate `text-[12px]`→`--text-caption` tier. NOTE: ScenarioComposer is the composer UI shell — editing className/title is chrome; do NOT touch any scenario.ts import or blend math.

Raw-px migration targets (the allocations dirty baseline — migrate text-[Npx] onto --text-* tiers): HoldingsTable.tsx (7), StressVarSection.tsx (4), MonteCarloSection.tsx (3), OpenPositionsTable.tsx (2), KpiStrip.tsx (1), plus AlertBanner/ScenarioComposer above. Use the data-surface subset (--text-h3/body/small/caption). Keeping these clean lets 52-07 flip the glob to error with no red.

Test analog (src/app/strategy/[id]/v2/error.test.tsx): render + assert heading "something went wrong", retry button calls unstable_retry, console.error fires on mount. For loading.test.tsx: smoke-render + `getByRole("status")`.

Frozen-island caution: KpiStrip / AlertBanner / SavedScenariosList / HoldingsTable etc. are NOT frozen islands (chrome). ScenarioComposer hosts the frozen `scenario.ts` math but the file itself is the UI shell — only touch its className/title at :2779, nothing math-adjacent. Run the 52-frozen-spine guard after.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Page-shell fluid-fill + KpiStrip @container migration (APPLY-01 / TYPE-03 / TYPE-04)</name>
  <read_first>
    - src/app/(dashboard)/allocations/page.tsx (the max-w-[1280px] shell + auth gate — preserve the gate)
    - src/app/(dashboard)/allocations/components/KpiStrip.tsx (the grid at ~454 + tabular-nums value cells ~514/524)
    - src/app/(dashboard)/allocations/components/KpiStrip.test.tsx (the test to keep green / extend)
    - src/components/strategy/StrategyTable.tsx (the @container host idiom: className="@container" on the region, @max-3xl:/@min-* variants on children)
    - src/__tests__/phase-52-container-tabular-nums.test.tsx (the alignment contract this migration must satisfy — append KpiStrip if the registry tracks it)
    - DESIGN.md (KpiStrip density tokens --row-h/--density-pad; do not hardcode row heights)
  </read_first>
  <behavior>
    - Test 1: KpiStrip renders its numeric value cells with `font-mono` and `tabular-nums` (alignment preserved post-migration).
    - Test 2: The strip's container region carries `@container`; column count varies by container width via `@`-prefixed variants, not `sm:`/`lg:` viewport variants.
    - Test 3: No KPI cell renders a fabricated zero/em-dash for a warming-up/degenerate metric — the existing warmup/degenerate branch (KpiStrip.warmup.test.tsx) stays green (STATE-02).
  </behavior>
  <action>
    In `src/app/(dashboard)/allocations/page.tsx:63` change `max-w-[1280px]` to `max-w-[1920px]` (keep `mx-auto p-6 pb-20`). Do NOT move `getMyAllocationDashboard`/`getUser`/`redirect` out of the page. In `KpiStrip.tsx` mark the strip's grid container `@container` and replace `sm:grid-cols-2 lg:grid-cols-5` with container variants (`@sm:grid-cols-2 @lg:grid-cols-5` or `@min-[Npx]:` thresholds tuned to the ~380px-and-up rail width) so a strip in the narrow metrics rail stops thinking it is at desktop width. Keep `font-mono … tabular-nums` on every numeric cell (Pitfall 2). Use plain `@container` (inline-size), NEVER `@container-size` (Pitfall 1). Extend KpiStrip.test.tsx to assert the `@container` class + tabular-nums on a value cell; keep warmup/scenario tests green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/KpiStrip.test.tsx" "src/app/(dashboard)/allocations/components/KpiStrip.warmup.test.tsx" src/__tests__/phase-52-container-tabular-nums.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max-w-\[1920px\]" "src/app/(dashboard)/allocations/page.tsx"` returns >= 1 and `max-w-[1280px]` no longer appears in that file.
    - `grep -c "@container" "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` returns >= 1; `@container-size` appears 0 times.
    - `grep -c "tabular-nums" "src/app/(dashboard)/allocations/components/KpiStrip.tsx"` is unchanged or higher vs baseline (no tabular-nums removed).
    - All three vitest files in the verify command exit 0.
    - The auth gate (`redirect("/login")`) and `getMyAllocationDashboard` remain in page.tsx (grep both present).
  </acceptance_criteria>
  <done>Allocations shell fluid-fills to 1920; KpiStrip uses @container (inline-size) with tabular-nums preserved; KPI/warmup/container tests green; auth gate intact.</done>
</task>

<task type="auto">
  <name>Task 2: Route loading.tsx + error.tsx with tests (STATE-01 / STATE-02 / ASVS V7)</name>
  <read_first>
    - src/app/factsheet/[id]/v2/loading.tsx (the match-layout fidelity bar to model)
    - src/app/(dashboard)/error.tsx (the error.tsx shape to copy — unstable_retry, digest-only)
    - src/components/ui/Skeleton.tsx (assemble the skeleton from these primitives)
    - src/app/strategy/[id]/v2/error.test.tsx (the render-test precedent)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/loading.md (Next 16 loading convention + the layout-data caveat)
    - node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/error.md (Next 16 unstable_retry + RSC error-message stripping)
    - .planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md (Copywriting Contract + dominant-anchor: allocations KPI strip first/largest)
  </read_first>
  <action>
    Create `src/app/(dashboard)/allocations/loading.tsx` — an RSC (no `"use client"`) at `mx-auto max-w-[1920px] px-6 py-6`, KPI strip as the DOMINANT anchor (full-width 4-cell grid first and largest, assembled from `Skeleton`), then an equity-chart placeholder (`Skeleton className="h-[320px] w-full"`), then holdings rows, closed by `<p className="sr-only" role="status" aria-live="polite">Loading allocations — computing analytics.</p>`. Create `src/app/(dashboard)/allocations/error.tsx` by copying `(dashboard)/error.tsx` verbatim shape: `"use client"`, `unstable_retry` prop (NOT `reset`), `console.error("[allocations-error]", error)` in useEffect, the UI-SPEC copy ("Something went wrong" + "This section encountered an error. You can retry or navigate to another page."), `{error.digest && Error ID: {digest}}` (digest ONLY — never error.message), `<Button onClick={() => unstable_retry()}>Try again</Button>`. Create `loading.test.tsx` (smoke-render + `getByRole("status")` + assert KPI-anchor structure exists) and `error.test.tsx` (heading present, retry calls unstable_retry, console.error fires on mount) modeled on the v2 error test.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/loading.test.tsx" "src/app/(dashboard)/allocations/error.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/(dashboard)/allocations/loading.tsx` and `error.tsx` exist; both `.test.tsx` files exist and the vitest command exits 0.
    - `loading.tsx` has NO `"use client"` directive and contains `role="status"` (grep).
    - `error.tsx` contains `"use client"`, `unstable_retry`, and does NOT render `error.message` (grep `error.message` returns 0 in error.tsx).
    - `error.test.tsx` asserts the retry button invokes `unstable_retry` (a `toHaveBeenCalled` on a vi.fn passed as the prop).
  </acceptance_criteria>
  <done>Allocations has a route-level match-layout loading skeleton (KPI anchor, sr-only liveness) and a digest-only error boundary with working retry; both carry render tests that pass.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the three named clips + migrate all raw text-[Npx]/inline-hex on the surface (TYPE-02 / DS-04 prep)</name>
  <read_first>
    - .planning/audits/truncation-audit.md (rows for AlertBanner:127, SavedScenariosList:529, ScenarioComposer:2779 — the classification SoT)
    - src/app/(dashboard)/allocations/components/AlertBanner.tsx (line ~126-128, the truncate + raw text-[14px] + inline #1A1A2E hex)
    - src/app/(dashboard)/allocations/components/SavedScenariosList.tsx (line ~528-530, the scenario-name span)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (line ~2779 ONLY — the constituent-name span; do not touch math/scenario.ts imports)
    - src/app/(dashboard)/allocations/components/HoldingsTable.tsx (the largest dirty file — 7 raw px sites)
    - src/components/ui/ScopedBanner.tsx (the wrap reference: break-words + min-w-0)
    - DESIGN.md (the --text-* tier mapping + token names for the hex/px migration)
  </read_first>
  <action>
    Fix the three clips: (a) `AlertBanner.tsx:127` — wrap the message (`break-words min-w-0` per ScopedBanner) so the meaningful alert text is never silently cut; ALSO replace `text-[14px]` with the `--text-body`/`--text-small` tier and replace the inline `style={{color:"#1A1A2E"}}` hex with the appropriate text token (raw hex violates DS-04). (b) `SavedScenariosList.tsx:529` — replace `truncate` with `break-words` (+ keep `min-w-0` on the parent) so the user-named scenario wraps. (c) `ScenarioComposer.tsx:2779` — keep single-line (table-aligned <li>) but add `title={strategyNames[id] ?? id}` for recovery and migrate `text-[12px]`→`--text-caption`. Then migrate EVERY remaining raw `text-[Npx]` under the allocations components (HoldingsTable, MonteCarloSection, StressVarSection, OpenPositionsTable, KpiStrip) onto a `--text-*` tier so the whole surface is clean — this is the prerequisite for 52-07's `error` ratchet. Do NOT add the eslint override here (52-07 owns eslint.config.mjs). Do NOT introduce any NEW truncate/line-clamp without a title (never relocate a clip).
  </action>
  <verify>
    <automated>test $(grep -rn "text-\[[0-9][0-9]*px\]" "src/app/(dashboard)/allocations/" 2>/dev/null | grep -v "\.test\." | wc -l) -eq 0 && npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^#' "src/app/(dashboard)/allocations/components/AlertBanner.tsx" | grep -c "break-words\|title="` returns >= 1 AND `grep -c "text-\[14px\]\|#1A1A2E" "src/app/(dashboard)/allocations/components/AlertBanner.tsx"` returns 0.
    - The SavedScenariosList name span uses `break-words` instead of a bare `truncate` (grep the :529 element region).
    - `grep -c "title=" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns >= 1 at the :2779 region; `text-[12px]` no longer at that span.
    - `grep -rn "text-\[[0-9][0-9]*px\]" "src/app/(dashboard)/allocations/" | grep -v "\.test\."` returns ZERO lines (surface clean for the Wave-3 ratchet).
    - `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` exits 0 (scenario.ts/compute.ts untouched).
  </acceptance_criteria>
  <done>The three named allocations clips recover full text (wrap or title=), the surface has zero raw text-[Npx]/inline-hex, and no frozen island changed. (The eslint error-ratchet for this glob lands in 52-07.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/unauthed → /allocations | The page redirects to /login on no user — an access-control gate that must survive the restyle |
| RSC error → error.tsx render | A thrown server error must not leak its message to the client boundary |
| clipped name → title= attribute | A recovery title must expose only data already rendered, never an un-gated field |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Elevation of Privilege | allocations/page.tsx auth gate | mitigate | Keep `if (!user) redirect("/login")` exactly; the max-w raise is chrome — acceptance criteria asserts the gate is still present |
| T-52-05 | Information Disclosure | allocations/error.tsx | mitigate | Render `error.digest` only, never `error.message` (Next strips RSC messages in prod; mirror dashboard error.tsx) — acceptance asserts error.message absent |
| T-52-06 | Information Disclosure | ScenarioComposer title={name} | mitigate | `title` exposes only the already-rendered constituent name (strategyNames[id]); never pull an un-gated field into the title attribute |
| T-52-07 | Tampering | frozen island via boundary cleanup | mitigate | scenario.ts/compute.ts untouched; the 52-frozen-spine guard runs in Task 3 verify |
| T-52-SC | Tampering | npm installs | accept | Zero installs in this plan (CSS/route-file/chrome only); RESEARCH audit records zero packages |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/**/*.test.tsx" src/__tests__/phase-52-container-tabular-nums.test.tsx src/__tests__/phase-52-frozen-spine-guards.test.ts` — green.
- Zero raw `text-[Npx]` remain under `src/app/(dashboard)/allocations/` (the surface is clean; 52-07 then flips its glob to error).
- Authed 2560 reflow row (from 52-01) covers /allocations — verified at the phase gate.
</verification>

<success_criteria>
- Allocations fluid-fills to 1920 with no overflow at 320/2560; KpiStrip is @container with tabular-nums preserved; route loading/error present and tested; three clips recovered; surface has zero raw px (ready for the 52-07 ratchet); frozen islands zero-diff.
</success_criteria>

<output>
Create `.planning/phases/52-per-surface-application-allocator-journey/52-02-SUMMARY.md` when done
</output>
