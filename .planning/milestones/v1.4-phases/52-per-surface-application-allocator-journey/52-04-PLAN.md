---
phase: 52-per-surface-application-allocator-journey
plan: 04
type: execute
wave: 2
depends_on: ["52-01"]
files_modified:
  - src/app/(dashboard)/discovery/[slug]/page.tsx
  - src/components/strategy/StrategyGrid.tsx
autonomous: true
requirements: [APPLY-01, TYPE-02, TYPE-03, TYPE-04, STATE-02, BP-01]
user_setup: []
must_haves:
  truths:
    - "The discovery page fluid-fills toward ~1920px then centers; no horizontal scroll/overlap at 320px or 2560px"
    - "The StrategyGrid marketplace/discovery tile strategy name recovers full text (title= or 2-line clamp)"
    - "Discovery StrategyGrid tiles render honest presence/absence — a real strategy name or an honest empty/absent state, never a fabricated placeholder name (no-invented-data / STATE-02)"
    - "Discovery's StrategyTable @container behavior (Phase 50-06) reads deliberate at the wider measure"
    - "The accredited-investor attestation gate in discovery/layout.tsx (force-dynamic) is untouched"
    - "The discovery surface (+ StrategyGrid) has zero raw text-[Npx] (ready for the Wave-3 no-raw-font-px=error ratchet)"
  artifacts:
    - path: "src/components/strategy/StrategyGrid.tsx"
      provides: "Discovery tile with recovered name"
      contains: "title="
  key_links:
    - from: "src/app/(dashboard)/discovery/[slug]/page.tsx"
      to: "max-w-[1920px]"
      via: "page-shell fluid-fill"
      pattern: "max-w-\\[1920px\\]"
    - from: "src/app/(dashboard)/discovery/layout.tsx"
      to: "attestation gate"
      via: "UNCHANGED force-dynamic gate"
      pattern: "force-dynamic"
---

<objective>
Bring the **discovery** surface (`/discovery/[slug]` — the strategy marketplace) to the v1.4 bar:
- add a page-shell fluid-fill `max-w-[1920px] mx-auto` (data surface, no cap today);
- fix the `StrategyGrid.tsx:63` accidental clip (marketplace/discovery tile strategy `<h3>` name — `wizardErrors.ts:481` even warns managers names truncate): add `title={s.name}` or a 2-line clamp WITH recovery;
- verify discovery's StrategyTable (already `@container` from Phase 50-06) reads deliberate at the wider measure (un-collapse columns rather than stranding);
- migrate the surface's raw `text-[Npx]` onto `--text-*` tiers so it is CLEAN (the `no-raw-font-px=error` ratchet for this glob lands in the consolidated Wave-3 plan 52-07, single owner of `eslint.config.mjs`).

Discovery already has a `loading.tsx` (Phase 46) — this plan does NOT add route-state files. The accredited-investor attestation gate lives in `discovery/layout.tsx` (`force-dynamic`) and is OFF-LIMITS — restyling the page must not touch the gate.
Output: 1 page MOD, 1 component MOD (StrategyGrid). This plan does NOT edit eslint.config.mjs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md
@.planning/phases/52-per-surface-application-allocator-journey/52-PATTERNS.md
@.planning/audits/truncation-audit.md
@DESIGN.md

<interfaces>
<!-- Discovery page (src/app/(dashboard)/discovery/[slug]/page.tsx:41-78) success return is a fragment:
       <><Breadcrumb …/><PageHeader title=… /><InfoBanner>…</InfoBanner>{watchlistFailed && <div role=status…>}<StrategyTable key=… strategies=… …/></>
     There is NO page-level max-w today. ADD the fluid-fill: wrap the fragment's content in <div className="mx-auto max-w-[1920px]"> (the data-surface cap). The (dashboard)/layout.tsx does NOT cap width, so this is where the cap goes. -->

<!-- StrategyGrid clip (src/components/strategy/StrategyGrid.tsx:63):
       <h3 className="text-sm font-semibold text-text-primary group-hover:text-accent transition-colors leading-tight truncate min-w-0">{s.name}</h3>
     This is the marketplace/discovery TILE name (grid/card view of StrategyTable). Treatment: add title={s.name} (cheapest recovery, tile context) OR a 2-line clamp WITH title= recovery. Audit row: accidental-clip, no recovery. NOTE: `text-sm` is a NAMED utility, not raw px — leave it; the no-raw-font-px rule targets text-[Npx], not text-sm.
     HONEST-STATE (STATE-02): the tile renders {s.name} — the REAL strategy name — and the grid's no-strategies branch shows the existing honest empty/absent state. The clip fix recovers the real name on hover; it MUST NOT substitute a fabricated placeholder name when a name is long/missing. Never invent data. -->

<!-- StrategyTable (src/components/strategy/StrategyTable.tsx) is discovery's main content and ALREADY ships @container + @max-3xl:hidden collapse + tabular-nums from Phase 50-06. This plan does NOT re-migrate it — it VERIFIES it reads deliberate at the wider measure. If a column-tuning @min-* tweak is genuinely needed, keep it minimal and re-run its existing tests. StrategyTable.tsx is NOT in any other Wave-2 plan's files_modified, so a minimal tweak here is collision-free; but prefer to leave it untouched. -->

<!-- discovery/layout.tsx = the accredited-investor attestation gate: `export const dynamic = "force-dynamic"`, `redirect("/login?redirect=/discovery/crypto-sma")` on no user, an attestation query that defaults to BLOCKED when uncertain, returns <>{children}</> only after the gate. DO NOT EDIT THIS FILE. The page restyle must not move the gate into the page or weaken force-dynamic. -->

Frozen-island note: none of these files are frozen islands. Run the 52-frozen-spine guard after.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Page-shell fluid-fill + StrategyGrid tile clip fix (APPLY-01 / TYPE-03 / TYPE-02 / TYPE-04 / STATE-02)</name>
  <files>src/app/(dashboard)/discovery/[slug]/page.tsx, src/components/strategy/StrategyGrid.tsx</files>
  <read_first>
    - src/app/(dashboard)/discovery/[slug]/page.tsx (the fragment success return ~41-78 — no max-w today; the auth/user usage)
    - src/app/(dashboard)/discovery/layout.tsx (the attestation gate — READ to confirm you must NOT touch it; force-dynamic + redirect + blocked-default)
    - src/components/strategy/StrategyGrid.tsx (line ~63 — the <h3> tile name truncate, no recovery; confirm it renders the REAL {s.name} and an honest no-strategies branch)
    - src/components/ui/ScopedBanner.tsx (the wrap reference, if 2-line clamp is preferred over title=)
    - src/components/strategy/StrategyTable.tsx (confirm it already ships @container — read the host region only; prefer not to edit it)
    - .planning/audits/truncation-audit.md (StrategyGrid:63 row — accidental-clip)
    - .planning/phases/52-per-surface-application-allocator-journey/52-UI-SPEC.md (layout table: discovery is a data surface → fluid-fill ~1920)
  </read_first>
  <action>
    In `discovery/[slug]/page.tsx` wrap the fragment's rendered content (Breadcrumb/PageHeader/InfoBanner/StrategyTable) in `<div className="mx-auto max-w-[1920px]">` so the data surface fluid-fills toward 1920 then centers. Do NOT move or alter the attestation gate (it stays in layout.tsx; do not edit layout.tsx). In `StrategyGrid.tsx:63` fix the tile-name clip: add `title={s.name}` to the `<h3>` (cheapest recovery for a tile) and KEEP `min-w-0`; if a 2-line clamp is preferred for the tile layout, use `line-clamp-2` WITH `title={s.name}` recovery (never a clamp without recovery — no clip relocation). The tile MUST keep rendering the real `{s.name}` and the grid's honest no-strategies/empty branch — never substitute a fabricated placeholder name (STATE-02 / no-invented-data). Verify the StrategyTable (already @container from Phase 50-06) un-collapses or reads deliberate at the wider measure; only if a column-tuning `@min-*` tweak is genuinely needed, keep it minimal and re-run StrategyTable's tests.
  </action>
  <verify>
    <automated>npx vitest run "src/components/strategy/StrategyTable.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "max-w-\[1920px\]" "src/app/(dashboard)/discovery/[slug]/page.tsx"` returns >= 1.
    - `git diff --name-only HEAD | grep -c "discovery/layout.tsx"` returns 0 (attestation gate untouched).
    - `grep -c "title=" "src/components/strategy/StrategyGrid.tsx"` returns >= 1 at the :63 tile name; if line-clamp-2 is used it co-occurs with title=.
    - `grep -c "{s.name}" "src/components/strategy/StrategyGrid.tsx"` is unchanged or higher (the tile still renders the real name; no fabricated placeholder introduced).
    - `npx vitest run "src/components/strategy/StrategyTable.test.tsx"` exits 0 (StrategyTable tests still green).
  </acceptance_criteria>
  <done>Discovery fluid-fills to 1920; the StrategyGrid tile name recovers via title= (or clamp+title) while still rendering the real {s.name} (honest, no fabricated placeholder); the attestation gate is untouched; StrategyTable tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate raw text-[Npx] on discovery (+ StrategyGrid) (DS-04 prep)</name>
  <files>src/app/(dashboard)/discovery/[slug]/page.tsx, src/components/strategy/StrategyGrid.tsx</files>
  <read_first>
    - src/components/strategy/StrategyGrid.tsx (audit for raw text-[Npx])
    - src/app/(dashboard)/discovery/[slug]/page.tsx (audit for raw text-[Npx])
    - DESIGN.md (the --text-* tier mapping; text-sm/text-xs named utilities are fine — only text-[Npx] migrates)
  </read_first>
  <action>
    Audit `src/app/(dashboard)/discovery/**` and `src/components/strategy/StrategyGrid.tsx` for raw `text-[Npx]` font sizes and migrate each onto a `--text-*` tier (data-surface subset). Leave named utilities (text-sm/text-xs) as-is. Do NOT add StrategyTable.tsx to a migration glob (it is shared and chart-adjacent — leave its lint level as-is; 52-07 scopes the discovery glob + StrategyGrid only). Do NOT edit eslint.config.mjs (52-07 owns it). Do not introduce any new truncate/line-clamp without a title.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "text-\[[0-9][0-9]*px\]" "src/app/(dashboard)/discovery/" "src/components/strategy/StrategyGrid.tsx" | grep -v "\.test\."` returns ZERO lines (clean for the Wave-3 ratchet).
    - `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` exits 0.
  </acceptance_criteria>
  <done>Discovery + StrategyGrid have zero raw font-px, frozen islands zero-diff. (The eslint error-ratchet for this glob lands in 52-07.)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| non-attested user → /discovery | The accredited-investor attestation gate (discovery/layout.tsx, force-dynamic) blocks non-attested access — the highest-value gate in this plan's scope |
| clipped tile name → title= | recovery exposes only the already-rendered strategy name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-12 | Elevation of Privilege | discovery/layout.tsx attestation gate | mitigate | The page restyle is OFF-LIMITS to layout.tsx; acceptance asserts layout.tsx is NOT in the git diff so force-dynamic + the blocked-default attestation gate cannot be weakened by the restyle |
| T-52-13 | Information Disclosure | StrategyGrid title={s.name} | mitigate | title exposes only the rendered marketplace tile name (already a public-listing field); never an un-gated field |
| T-52-14 | Tampering | frozen island | mitigate | scenario.ts/compute.ts untouched; 52-frozen-spine guard in Task 2 verify |
| T-52-SC | Tampering | npm installs | accept | Zero installs (CSS/chrome only) |
</threat_model>

<verification>
- `npx vitest run "src/components/strategy/StrategyTable.test.tsx" src/__tests__/phase-52-frozen-spine-guards.test.ts` — green.
- Zero raw `text-[Npx]` under discovery + StrategyGrid (clean for 52-07).
- The 52-01 authed 2560 reflow row covers /discovery via the allocator surfaces; discovery is also covered by the existing 320px public/authed sweep.
- `git diff --name-only HEAD | grep discovery/layout.tsx` — empty (gate untouched).
</verification>

<success_criteria>
- Discovery fluid-fills to 1920 with no overflow at 320/2560; the tile-name clip is recovered while the tile still renders the real {s.name} (honest, no fabricated placeholder — STATE-02); the attestation gate is provably untouched; surface has zero raw px (ready for the 52-07 ratchet); frozen islands zero-diff.
</success_criteria>

<output>
Create `.planning/phases/52-per-surface-application-allocator-journey/52-04-SUMMARY.md` when done
</output>
</content>
