---
phase: 54-verification-v1-3-debt-cleanup-visual-regression-replacement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/admin/partner-import/page.tsx
  - src/app/(dashboard)/admin/users/page.tsx
  - src/app/(dashboard)/admin/users/[id]/page.tsx
  - src/app/(dashboard)/admin/for-quants-leads/page.tsx
  - src/__tests__/admin-width.test.tsx
autonomous: true
requirements: [VERIFY-05]
must_haves:
  truths:
    - "The 4 prose/form admin pages no longer over-stretch at >=1920px (inner max-w cap applied)"
    - "Admin DATA pages (e.g. partner-roi) keep the wide 1920px measure"
    - "DashboardChrome.isWide regex is unchanged"
    - "A static source-scan test pins both the in-scope cap and the out-of-scope absence"
  artifacts:
    - path: "src/__tests__/admin-width.test.tsx"
      provides: "Static source-scan asserting the 4 prose/form pages have the inner cap and a data page does not"
      contains: "OUT OF SCOPE"
  key_links:
    - from: "src/__tests__/admin-width.test.tsx"
      to: "the 4 admin prose/form page.tsx files"
      via: "readFileSync + className substring assertion"
      pattern: "max-w-"
---

<objective>
Apply an inner `max-w` cap to the four prose/form admin pages (`partner-import`, `users`, `users/[id]`, `for-quants-leads`) so they no longer fluid-fill to ~1856px at ultra-wide, while admin DATA tables keep the wide 1920px measure. Prove it with a STATIC source-scan unit test (NOT a jsdom render — jsdom has no layout engine and can't tell 1100px from 1440px).

Purpose: RT-W2 (carried from P53, under VERIFY-05). The `DashboardChrome.isWide` regex fluid-fills the whole `/admin` tree — correct for data tables, wrong for prose/form pages where inputs stretch to ~1807px. CONTEXT REJECTED narrowing the regex (fragile; admin mixes prose + data). The decided fix is a per-page inner cap.
Output: 4 modified admin pages + 1 new static source-scan test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-PATTERNS.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-CONTEXT.md

<interfaces>
Analog test: src/app/(dashboard)/allocations/widgets/performance/composer-width.test.tsx (Phase-38 idiom — readFileSync + className substring, bidirectional in-scope/out-of-scope assertions).
Page wrapper shape (all 4 identical): `return ( <> <PageHeader … ` — line anchors: partner-import :89, users :59, users/[id] :99, for-quants-leads :28.
Mechanism: DashboardChrome.tsx:64-77 documents the allocator pages set their OWN page-level cap; admin prose pages mirror that by wrapping content in a constraining `<div className="mx-auto max-w-…">`.
Contrast (must NOT change): src/app/(dashboard)/admin/partner-roi/page.tsx:42 (DATA page, same shape, stays wide).
DESIGN.md governs the exact prose width value — read it before picking the cap.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply inner max-w cap to the 4 admin prose/form pages</name>
  <read_first>
    - DESIGN.md (the prose/measure width direction — governs the exact max-w value)
    - src/components/layout/DashboardChrome.tsx (:64-77 isWide rationale, :171-175 cap mechanism — read but DO NOT edit)
    - src/app/(dashboard)/admin/partner-import/page.tsx (:89 return shape)
    - src/app/(dashboard)/admin/users/page.tsx (:59)
    - src/app/(dashboard)/admin/users/[id]/page.tsx (:99 — note a SECOND return at :179, cap only the primary content return)
    - src/app/(dashboard)/admin/for-quants-leads/page.tsx (:28)
    - src/app/(dashboard)/admin/partner-roi/page.tsx (:42 — the contrast DATA page, do NOT edit)
  </read_first>
  <action>
    For each of the 4 prose/form pages, constrain the page content with an inner `max-w` cap. Pick ONE consistent prose-appropriate Tailwind measure governed by DESIGN.md (a prose/form width, e.g. `max-w-3xl` or `max-w-5xl` per DESIGN.md's measure scale — read DESIGN.md and choose; document the choice in the SUMMARY). Apply it by wrapping the fragment's content in a `<div className="mx-auto max-w-…">` (the PageHeader may stay full-bleed or be wrapped too — match the allocator-page precedent in DashboardChrome's comment). Keep the SAME cap literal across all 4 files so the static test can assert one token. Do NOT edit DashboardChrome.tsx (the isWide regex stays as-is — CONTEXT-locked). Do NOT touch partner-roi or any other admin DATA page. For users/[id]/page.tsx, cap only the primary content return (:99), not the loading/error return (:179). This is a className-wrapper edit, not a font-px edit — no overlap with the BP-03 plans.
  </action>
  <verify>
    <automated>grep -l "max-w-" "src/app/(dashboard)/admin/partner-import/page.tsx" "src/app/(dashboard)/admin/users/page.tsx" "src/app/(dashboard)/admin/users/[id]/page.tsx" "src/app/(dashboard)/admin/for-quants-leads/page.tsx" ; npx eslint "src/app/(dashboard)/admin/partner-import/page.tsx" "src/app/(dashboard)/admin/users/page.tsx" "src/app/(dashboard)/admin/users/[id]/page.tsx" "src/app/(dashboard)/admin/for-quants-leads/page.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files contain the chosen inner `max-w-…` cap literal
    - `git diff` shows NO change to DashboardChrome.tsx or partner-roi/page.tsx
    - `npx eslint` on the 4 files is clean (admin/** is already at `no-raw-font-px: error` — must stay clean)
  </acceptance_criteria>
  <done>The 4 prose/form admin pages carry a consistent inner max-w cap; the isWide regex and the data pages are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Static source-scan test admin-width.test.tsx</name>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/composer-width.test.tsx (the exact idiom to copy — readFileSync + .toContain / .not.toContain, in-scope + out-of-scope)
    - .planning/phases/54-.../54-RESEARCH.md (Pitfall 7 — jsdom can't tell 1100 from 1440; NEVER a render test)
    - the 4 admin pages from Task 1 (to know the exact cap literal)
  </read_first>
  <action>
    Create `src/__tests__/admin-width.test.tsx` copying the composer-width.test.tsx structure VERBATIM (readFileSync of each page.tsx via `join(process.cwd(), …)`, substring asserts — NOT a render/jsdom test; Pitfall 7). IN-SCOPE block: assert each of the 4 prose/form pages `.toContain(<the cap literal>)`. OUT-OF-SCOPE block (CRITICAL — copy composer-width.test.tsx:72-81): assert a DATA admin page (`admin/partner-roi/page.tsx`) `.not.toContain(<the cap literal>)`, so an over-broad future edit that caps a data page fails CI. Add a comment explaining the jsdom-layout pitfall (why this is a source scan, not a render). The test must not drop coverage below the ratchet (lines 82 / stmts 80 / fns 74 / branches 72).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/admin-width.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - admin-width.test.tsx passes
    - The file contains both an IN-SCOPE assertion (4 pages have the cap) and an "OUT OF SCOPE" assertion (partner-roi does NOT)
    - It uses readFileSync (static scan), NOT render() / jsdom
  </acceptance_criteria>
  <done>A static source-scan test pins the RT-W2 fix bidirectionally; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | className-wrapper + test edits; no runtime input, network, or DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-03-01 | Tampering | Over-broad max-w edit capping a data page | mitigate | The OUT-OF-SCOPE assertion in admin-width.test.tsx fails CI if a data page gains the cap |

No high/medium threats — layout/test-only, no new attack surface.
</threat_model>

<verification>
- `npx vitest run src/__tests__/admin-width.test.tsx` — green (in-scope + out-of-scope both asserted).
- `git diff` — DashboardChrome.tsx and partner-roi/page.tsx untouched.
- The authed reflow sweep does NOT cover /admin (allocator-role seed can't reach admin routes) — this static test is the sole automated RT-W2 gate; that is by design (RESEARCH admin-exclusion caveat).
</verification>

<success_criteria>
The 4 prose/form admin pages have a documented inner max-w cap; admin data tables keep the wide measure; the isWide regex is unchanged; a static source-scan test pins both directions and is green.
</success_criteria>

<output>
Create `.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-03-SUMMARY.md` when done.
</output>
