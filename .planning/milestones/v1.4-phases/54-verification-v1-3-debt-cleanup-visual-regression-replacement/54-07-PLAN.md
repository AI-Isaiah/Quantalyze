---
phase: 54-verification-v1-3-debt-cleanup-visual-regression-replacement
plan: 07
type: execute
wave: 2
depends_on: [54-04]
files_modified:
  - e2e/no-clip-sweep.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [VERIFY-03]
must_haves:
  truths:
    - "A runtime Playwright no-clip sweep exists that fails on reintroduced truncation/ellipsis-clip (scrollWidth>clientWidth + text-overflow detection)"
    - "It runs over routes x viewports including 2560px"
    - "Deliberate clamps (avatars, line-clamp) are allowlisted, not flagged"
    - "It is FLOW-01 dual-wired: public half in the unseeded ci.yml list; seeded half in the MA-8 list AND carrying its own HAS_SEED_ENV const"
    - "Every probe anchors on a VISIBLE element + HTTP<400 throw (no false-green)"
  artifacts:
    - path: "e2e/no-clip-sweep.spec.ts"
      provides: "Runtime clip-detection sweep, public + seeded halves"
      contains: "scrollWidth"
    - path: ".github/workflows/ci.yml"
      provides: "no-clip-sweep wired into BOTH playwright lists"
      contains: "no-clip-sweep"
  key_links:
    - from: "e2e/no-clip-sweep.spec.ts seeded half"
      to: ".github/workflows/ci.yml MA-8 seeded list"
      via: "FLOW-01 dual-wire (filename in list + HAS_SEED_ENV const in spec)"
      pattern: "no-clip-sweep"
---

<objective>
Add a runtime Playwright no-clip guard (VERIFY-03) — a new `e2e/no-clip-sweep.spec.ts` that detects content cut-off (`scrollWidth > clientWidth` + CSS `text-overflow:ellipsis` on non-empty text) across routes × viewports (incl. 2560), failing the build on any reintroduced truncation/clip. Allowlist genuinely-intended clamps (avatars, deliberate line-clamp). Dual-wire it (FLOW-01) so it actually runs in CI.

Purpose: ROADMAP success criterion 3 — "a no-clip CI guard fails the build on any reintroduced truncation." This is a RUNTIME check (more truthful than a static class ban — catches actual cut-off, not just utility usage), reusing the reflow-sweep scaffolding + the assertTargetSizes per-element-walk idiom.
Output: 1 new dual-wired e2e spec.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-RESEARCH.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-PATTERNS.md

<interfaces>
Scaffolding analogs: e2e/reflow-sweep.spec.ts (public, NO seed gate) + e2e/reflow-sweep-authed.spec.ts (seeded; HAS_SEED_ENV :32-37; loginViaForm; beforeAll seed; beforeEach login). Per-element walk: e2e/helpers/reflow.ts:119-165 (assertTargetSizes loops querySelectorAll in page.evaluate, accumulates offenders, asserts measured>0 so an empty page can't false-green).
Clip predicate (RESEARCH §VERIFY-03 / PATTERNS lines 57-73): in a page.evaluate, for each `body *`: skip if it matches/closes an ALLOW selector (`[data-clamp-ok]`, `.avatar`, `[class*="line-clamp"]`); flag when `getComputedStyle.textOverflow === "ellipsis" && overflow !== "visible"` AND `scrollWidth > clientWidth + 1` AND non-empty text. Assert the offender array is empty.
Anti-false-green (helpers/reflow.ts:52-55): each probe `await expect(page.locator(anchor)).toBeVisible()` + HTTP status>=400 early throw BEFORE the probe.
Public routes: reflow-sweep.spec.ts:41-57 PUBLIC_ROUTES. Authed anchor: `'h1:has-text("My Allocation")'`.
FLOW-01 dual-wire (both places required):
  place 1 unseeded: ci.yml ~:1073 (the npx playwright test … line) — add e2e/no-clip-sweep.spec.ts for the public describe.
  place 1 seeded: ci.yml ~:1266-1280 (the MA-8 list) — add e2e/no-clip-sweep.spec.ts for the seeded describe.
  place 2 spec: `const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;` + `test.skip(!HAS_SEED_ENV, "no-clip-sweep: seed env not wired (W-02)")` on the seeded describe.
Viewports: include desktop, mobile (375), and 2560 ultra-wide.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author e2e/no-clip-sweep.spec.ts (public + seeded halves)</name>
  <read_first>
    - e2e/reflow-sweep.spec.ts (public scaffold; :30-32 no-seed; :41-57 PUBLIC_ROUTES)
    - e2e/reflow-sweep-authed.spec.ts (:32-37 HAS_SEED_ENV; loginViaForm; :99-118 beforeAll/beforeEach)
    - e2e/helpers/reflow.ts (:52-55 visible-anchor guard; :119-165 assertTargetSizes per-element walk)
    - e2e/helpers/seed-test-project.ts (seedTestAllocator :76 — for the seeded half)
    - .planning/phases/54-.../54-PATTERNS.md (no-clip-sweep section — the clip predicate verbatim)
  </read_first>
  <action>
    Create e2e/no-clip-sweep.spec.ts with TWO describes: a PUBLIC describe (no seed gate — loops PUBLIC_ROUTES) and a SEEDED describe (HAS_SEED_ENV-gated, seeds one allocator in beforeAll, logs in beforeEach, loops the allocator-reachable authed routes anchored on `'h1:has-text("My Allocation")'`). For each route × viewport (desktop 1280, mobile 375, ultrawide 2560): set the viewport, goto the route, throw on HTTP>=400, `await expect(page.locator(anchor).first()).toBeVisible()` (false-green guard), then run the clip-detection `page.evaluate` predicate (the ALLOW-list + ellipsis + scrollWidth>clientWidth+1 + non-empty-text walk from the interfaces) and `expect(clipped).toEqual([])` with the offender list in the message. Mirror the assertTargetSizes discipline: accumulate offenders, do not bail on the first. Allowlist: `[data-clamp-ok]`, `.avatar`, `[class*="line-clamp"]`. Do NOT include /admin in the seeded authed routes (the allocator seed can't reach admin — false-green); admin clip coverage rides the public routes + the static width test.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('e2e/no-clip-sweep.spec.ts','utf8'); for(const t of ['scrollWidth','clientWidth','textOverflow','HAS_SEED_ENV','2560','toBeVisible']){ if(!s.includes(t)){console.error('missing',t);process.exit(1)} } console.log('no-clip-sweep has clip predicate + seed gate + 2560 + false-green guard')"</automated>
  </verify>
  <acceptance_criteria>
    - The spec has a public describe (no seed gate) and a seeded describe (HAS_SEED_ENV-gated with seedTestAllocator)
    - The clip predicate flags scrollWidth>clientWidth+1 + ellipsis on non-empty text, with the avatar/line-clamp/data-clamp-ok allowlist
    - Every probe has the visible-anchor + HTTP<400 guards; viewports include 2560
    - It self-skips locally without seed env (no false-green)
  </acceptance_criteria>
  <done>A runtime no-clip sweep exists with public + seeded halves, the allowlist, and the false-green guards.</done>
</task>

<task type="auto">
  <name>Task 2: FLOW-01 dual-wire no-clip-sweep into ci.yml (both lists)</name>
  <read_first>
    - .github/workflows/ci.yml (:1066-1073 unseeded list + comment; :1258-1282 seeded MA-8 list + the :1264 dual-wire reminder comment)
    - e2e/no-clip-sweep.spec.ts (confirm the HAS_SEED_ENV const is present — place 2)
    - .planning/phases/54-.../54-PATTERNS.md (Shared Patterns -> FLOW-01)
  </read_first>
  <action>
    Add `e2e/no-clip-sweep.spec.ts` to BOTH ci.yml playwright lists: (1) the UNSEEDED list (~:1073) so the public describe runs unseeded; (2) the SEEDED MA-8 list (~:1266-1280, before `--timeout 60000`) so the seeded describe runs against the MA-8 DB. The spec's HAS_SEED_ENV const (place 2) was added in Task 1. This is the exact dual-wire the FLOW-01 comment at :1264 demands ("Update both this list and the e2e/<spec>.spec.ts HAS_SEED_ENV constant"). Do NOT touch the lighthouse-mobile job (that is Plan 54-04). Verify the filename appears in BOTH lists.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const n=(y.match(/no-clip-sweep\.spec\.ts/g)||[]).length; if(n<2){console.error('no-clip-sweep wired in only',n,'list(s) — need 2 (unseeded+seeded)');process.exit(1)} console.log('no-clip-sweep dual-wired in',n,'lists')"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/no-clip-sweep.spec.ts` appears in BOTH the unseeded list (~:1073) and the seeded MA-8 list (~:1266-1280)
    - The spec carries its HAS_SEED_ENV const (place 2 satisfied)
    - The lighthouse-mobile job is untouched
  </acceptance_criteria>
  <done>no-clip-sweep is FLOW-01 dual-wired — it will actually run in CI (public unseeded + seeded MA-8).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded test -> shared MA-8 test DB | the seeded no-clip describe seeds one allocator (unique timestamped email, inert to other specs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-07-01 | Tampering | Gate silently never runs (FLOW-01) | mitigate | Dual-wire verified by counting `no-clip-sweep` in BOTH ci.yml lists (>=2) + the HAS_SEED_ENV const present |
| T-54-07-02 | Tampering | False-green against blank/login page | mitigate | Visible-anchor + HTTP<400 guards before every probe; assertTargetSizes measured>0 discipline |
| T-54-07-03 | Tampering | Seeded allocator polluting other specs | mitigate | seedTestAllocator emits a unique timestamped email (inert); rows are user-scoped and torn down by the MA-8 teardown infra (no crypto-sma discovery seed here) |

No high threats — e2e authoring + CI wiring; the seed is a single user-scoped allocator with no cross-spec assertion surface.
</threat_model>

<verification>
- `no-clip-sweep` appears in both ci.yml lists (count >= 2).
- The spec has the clip predicate, the allowlist, the HAS_SEED_ENV gate, and the false-green guards.
- The seeded half seeds only a unique allocator (no crypto-sma discovery seed).
</verification>

<success_criteria>
A runtime no-clip CI guard exists, fails on reintroduced truncation/ellipsis-clip across routes × viewports (incl. 2560), allowlists deliberate clamps, and is FLOW-01 dual-wired so it runs in CI. ROADMAP success criterion 3 (no-clip portion) is met.
</success_criteria>

<output>
Create `.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-07-SUMMARY.md` when done.
</output>
