---
phase: 54-verification-v1-3-debt-cleanup-visual-regression-replacement
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/axe-app-wide.spec.ts
  - e2e/reflow-sweep.spec.ts
  - e2e/reflow-sweep-authed.spec.ts
  - e2e/svg-chart-parity.spec.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-04]
must_haves:
  truths:
    - "A 2560px ultra-wide row is in the axe VIEWPORTS matrix so every public/authed/embedded scan fans out to 2560"
    - "An app-wide 2560px reflow sweep exists (public half unseeded + authed half additive-folded into the already-wired authed spec)"
    - "The svg-chart-parity tolerance goldens land green-by-skip (WR-02 guard) — never blind --update-snapshots"
    - "Every 2560 probe anchors on a VISIBLE content element + HTTP<400 early throw (no false-green)"
  artifacts:
    - path: "e2e/axe-app-wide.spec.ts"
      provides: "2560 row in VIEWPORTS"
      contains: "2560"
    - path: "e2e/reflow-sweep.spec.ts"
      provides: "Public 2560 ultra-wide reflow describe"
      contains: "2560"
  key_links:
    - from: "the 2560 reflow/axe rows"
      to: "assertNoReflow / buildAxe (e2e/helpers)"
      via: "page.setViewportSize({width:2560}) + visible-anchor guard"
      pattern: "setViewportSize"
---

<objective>
Add the ultra-wide (2560px) viewport row to the axe matrix and the reflow sweep app-wide (VERIFY-01), and land the tolerance-golden specs green-by-skip via the WR-02 golden-pending guard (VERIFY-04). This wires the three deferred Phase-52 canaries (seeded 2560 reflow + svg goldens + authed ultra-wide) into the running matrix without baking PNGs (the bake is a deliberate, deferred per-chart CI commit — never blind `--update-snapshots`).

Purpose: ROADMAP success criteria 1 (2560 row green app-wide; deferred P52 canaries run) and 4 (byte-identity replaced by tolerance goldens). The 2560 row in the axe VIEWPORTS const fans out to public + authed + embedded scans automatically (three for-loops iterate it). The reflow public half is unseeded; the authed 2560 block already partly exists and is additive-folded (no new FLOW-01 wiring).
Output: 2560 rows in the axe + reflow specs; the golden specs guarded green-by-skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-RESEARCH.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-PATTERNS.md

<interfaces>
axe-app-wide.spec.ts:92-95 — `VIEWPORTS` const; three for-loops at :100,:132,:245 iterate it. Add `{ w: 2560, h: 1440, name: "ultrawide" }`.
reflow-sweep.spec.ts:41-57 — `PUBLIC_ROUTES` (unseeded, NO seed gate — provable by grep :30-32). Add a parallel 2560 describe over PUBLIC_ROUTES.
reflow-sweep-authed.spec.ts:289-354 — a 2560 ultra-wide authed block ALREADY EXISTS (allocator subset: /allocations, ?tab=scenario, ?tab=risk, /compare). It is additive-folded into the already-dual-wired host spec (Pattern 2 — NO new FLOW-01 wiring). Extend ULTRAWIDE_ROUTES with any app-wide authed routes that the allocator-role seed CAN reach.
  - ADMIN routes are EXCLUDED from this authed sweep (reflow-sweep-authed.spec.ts:26-31 — seedTestAllocator stamps role='allocator'; /admin redirects a non-admin -> false-green). RT-W2 admin width is covered by the static admin-width.test.tsx (Plan 54-03), not here.
svg-chart-parity.spec.ts:79-102,:159 — the WR-02 golden-pending guard (SNAPSHOT_DIR dir-scan for *.png; skips LOUDLY when only README.md). Tolerances :215-228 — 0.02 per-panel, 0.05 full-page, threshold 0.2. The bake is DEFERRED (locked) — leave it green-by-skip.
Anti-false-green (helpers/reflow.ts:52-55): every probe `await expect(page.locator(anchor)).toBeVisible()` + HTTP status>=400 early throw.
@container 2560 trap (Pitfall 3): a Tailwind v4 same-element host+variant freezes grids 1-wide at 2560; assertNoReflow (runtime) catches the overflow jsdom misses. Any 2560 finding traces to a same-element host/variant — fix by splitting host(parent)/variant(child).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 2560 row to the axe VIEWPORTS matrix</name>
  <read_first>
    - e2e/axe-app-wide.spec.ts (:92-95 VIEWPORTS; :100,:132,:245 the three for-loops that iterate it)
    - .planning/phases/54-.../54-PATTERNS.md (axe-app-wide section — the exact 2560 row)
  </read_first>
  <action>
    Add `{ w: 2560, h: 1440, name: "ultrawide" }` as a third entry in the `VIEWPORTS` const at axe-app-wide.spec.ts:92. The three for-loops (public :100, authed :132, embedded :245) iterate VIEWPORTS, so this one row fans every scan out to 2560 automatically. Do NOT un-skip the dormant authed/embedded describes here (that is Plan 54-08's VERIFY-02 work) — this task only widens the matrix the public rows run at; the authed/embedded rows stay HAS_SEED_ENV-gated. Confirm the public 2560 rows have a visible-anchor guard (they inherit it from the existing public describe pattern).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('e2e/axe-app-wide.spec.ts','utf8'); if(!/2560/.test(s)){console.error('no 2560 row');process.exit(1)} console.log('2560 in VIEWPORTS')" && npx tsc --noEmit -p tsconfig.json 2>/dev/null | grep -i "axe-app-wide" || echo "typecheck clean for spec"</automated>
  </verify>
  <acceptance_criteria>
    - VIEWPORTS contains a 2560 entry
    - The spec still typechecks; the public describe's 2560 rows inherit the visible-anchor + HTTP<400 guards
    - The dormant authed/embedded describes remain HAS_SEED_ENV-gated (NOT un-skipped here)
  </acceptance_criteria>
  <done>The axe matrix scans every public route at 2560px (authed/embedded 2560 will activate with VERIFY-02 in 54-08).</done>
</task>

<task type="auto">
  <name>Task 2: App-wide 2560 reflow sweep (public unseeded + authed additive-fold)</name>
  <read_first>
    - e2e/reflow-sweep.spec.ts (:30-32 NO seed gate; :41-57 PUBLIC_ROUTES; the existing describe shape)
    - e2e/reflow-sweep-authed.spec.ts (:26-31 admin-exclusion rationale; :289-354 the existing 2560 ultra-wide authed block; :32-37 HAS_SEED_ENV)
    - e2e/helpers/reflow.ts (:47 assertNoReflow; :52-55 visible-anchor guard)
    - .planning/phases/54-.../54-PATTERNS.md (reflow section — copy the :311-353 shape)
  </read_first>
  <action>
    PUBLIC half (reflow-sweep.spec.ts): add a `test.describe("reflow sweep @ 2560px ultra-wide — public")` that loops PUBLIC_ROUTES, sets viewport 2560×1440, throws on HTTP>=400, and calls `assertNoReflow(page, r.anchor)`. UNSEEDED — no env gate (matching the file's deliberate no-seed property). AUTHED half (reflow-sweep-authed.spec.ts): the 2560 block at :289-354 already covers the allocator subset; EXTEND its ULTRAWIDE_ROUTES with any additional app-wide routes the allocator-role seed CAN reach (e.g. /discovery, /portfolios if seeded, the strategy-v2 factsheet) — do NOT add /admin routes (admin-exclusion :26-31; covered by 54-03's static test). This is an additive fold into the already-dual-wired host spec (Pattern 2) — NO new ci.yml wiring needed for the authed half. The public half is already in the unseeded ci.yml list (reflow-sweep.spec.ts is at :1073). If `assertNoReflow` reds at 2560, root-cause it to a same-element @container host/variant and fix by splitting host(parent)/variant(child) — do NOT widen the slop (CLAUDE.md Rule 6).
  </action>
  <verify>
    <automated>node -e "const a=require('fs').readFileSync('e2e/reflow-sweep.spec.ts','utf8'); const b=require('fs').readFileSync('e2e/reflow-sweep-authed.spec.ts','utf8'); if(!/2560/.test(a)){console.error('public 2560 missing');process.exit(1)} if(!/2560/.test(b)){console.error('authed 2560 missing');process.exit(1)} console.log('2560 reflow public+authed present')"</automated>
  </verify>
  <acceptance_criteria>
    - reflow-sweep.spec.ts has a public 2560 describe over PUBLIC_ROUTES with the visible-anchor + HTTP<400 guards
    - reflow-sweep-authed.spec.ts's ULTRAWIDE_ROUTES covers the app-wide allocator-reachable routes (no /admin)
    - The authed 2560 block stays inside the already-HAS_SEED_ENV-gated host spec (no new FLOW-01 wiring)
  </acceptance_criteria>
  <done>The reflow sweep runs app-wide at 2560px — public unseeded, authed additive-folded; admin width is covered by the static test.</done>
</task>

<task type="auto">
  <name>Task 3: Land the tolerance-golden specs green-by-skip (VERIFY-04)</name>
  <read_first>
    - e2e/svg-chart-parity.spec.ts (:79-102 the WR-02 dir-scan guard; :144,:159 the test.skip; :215-228 tolerances)
    - e2e/strategy-v2-chart-parity.spec.ts (active per-panel goldens — the tolerance precedent)
    - .planning/phases/54-.../54-PATTERNS.md (WR-02 golden-pending guard section)
    - .planning/phases/54-.../54-CONTEXT.md (the locked: NEVER blind --update-snapshots; bake deferred)
  </read_first>
  <action>
    Ensure the svg-chart-parity tolerance goldens cover the desktop AND the 2560 ultra-wide viewport so the byte-identity replacement (VERIFY-04) extends to ultra-wide. Add a 2560 toHaveScreenshot row/describe to svg-chart-parity.spec.ts mirroring the existing desktop+320px rows, with the SAME tolerances (0.02 per-panel, 0.05 full-page, threshold 0.2) and the SAME WR-02 golden-pending guard (`test.skip(HAS_SEED_ENV && !HAS_GOLDENS, GOLDEN_PENDING_REASON)`) so it lands green-by-skip until a deliberate bake. Mask dynamic regions per the existing masking pattern. Do NOT bake PNGs and do NOT run `--update-snapshots` (locked Out-of-Scope). The spec is already in the seeded MA-8 list (ci.yml:1280) and already HAS_SEED_ENV-gated — additive 2560 rows need no new wiring.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('e2e/svg-chart-parity.spec.ts','utf8'); if(!/GOLDEN_PENDING|HAS_GOLDENS/.test(s)){console.error('WR-02 guard missing');process.exit(1)} if(!/2560/.test(s)){console.error('2560 golden row missing');process.exit(1)} console.log('2560 golden row present + WR-02 guard intact')" && ls e2e/__snapshots__/svg-chart-parity.spec.ts/ | grep -i "\.png$" && echo "WARN: PNGs present — bake should be deferred" || echo "no PNGs — correctly green-by-skip"</automated>
  </verify>
  <acceptance_criteria>
    - svg-chart-parity.spec.ts has a 2560 golden row with 0.02/0.05 tolerances and the WR-02 guard
    - The snapshot dir holds NO *.png (bake deferred — green-by-skip)
    - No `--update-snapshots` was run; no blind bake
  </acceptance_criteria>
  <done>The tolerance goldens cover desktop + 2560 and land green-by-skip; the bake remains a deliberate deferred CI commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded test -> shared MA-8 test DB | the authed 2560 reflow rows seed an allocator |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-06-01 | Tampering | False-green at 2560 (blank/unhydrated page) | mitigate | Every probe anchors on a visible content element + HTTP<400 early throw (helpers/reflow.ts:52-55) |
| T-54-06-02 | Tampering | Blind --update-snapshots masking a regression | mitigate | WR-02 guard keeps specs green-by-skip; bake is deferred + deliberate (locked Out-of-Scope ban) |
| T-54-06-SC | Tampering | npm installs | accept | No installs — all tooling present (RESEARCH Package Legitimacy Audit: none) |

No high threats — e2e spec authoring only; the seeded authed rows are additive folds into already-wired specs (teardown hermeticity is handled in 54-08).
</threat_model>

<verification>
- 2560 present in axe + both reflow specs + the golden spec.
- The golden snapshot dir holds no PNGs (green-by-skip).
- No new FLOW-01 wiring needed (all edits are additive folds into already-wired specs / the existing unseeded list).
</verification>

<success_criteria>
The 2560 ultra-wide row scans app-wide in axe + reflow (public unseeded + authed additive-fold, admin via the static test); the tolerance goldens cover desktop + 2560 and land green-by-skip with the bake deferred. ROADMAP success criteria 1 + 4 (golden portion) are met.
</success_criteria>

<output>
Create `.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-06-SUMMARY.md` when done.
</output>
