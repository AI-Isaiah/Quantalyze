---
phase: 54-verification-v1-3-debt-cleanup-visual-regression-replacement
plan: 09
type: execute
wave: 4
depends_on: [54-01a, 54-01b, 54-02a, 54-02b, 54-03, 54-05, 54-08]
files_modified:
  - .planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-DESIGN-AUDIT.md
  - .planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-BP-03-EXEMPTION-NOTE.md
autonomous: false
requirements: [VERIFY-05]
user_setup: []
must_haves:
  truths:
    - "An app-wide design-review audit against the locked DESIGN.md runs NOW, with findings folded or logged"
    - "The RT-W2 admin max-w fix (Plan 54-03) is confirmed in the audit"
    - "The BP-03 EquityChart/SVG off-glob exemption is documented for the milestone audit so BP-03 is not read as an unmet gap"
    - "The real-device authed sign-off is encoded as a documented human_needed checkpoint (no device/network in this sandbox)"
    - "WCAG-AA floor + all LOCKED invariants are confirmed intact and proven"
  artifacts:
    - path: ".planning/phases/54-.../54-DESIGN-AUDIT.md"
      provides: "App-wide design-review audit findings + dispositions"
    - path: ".planning/phases/54-.../54-BP-03-EXEMPTION-NOTE.md"
      provides: "Documented EquityChart/SVG off-glob exemption rationale for the milestone audit"
  key_links:
    - from: "54-DESIGN-AUDIT.md"
      to: "DESIGN.md locked contract + the RT-W2 fix + the BP-03 token spine"
      via: "design-review / gsd-ui-review pass"
      pattern: "DESIGN.md"
---

<objective>
Run the app-wide design-review audit (VERIFY-05) NOW against the locked DESIGN.md — this is an AUDIT of the existing v1.4 UI, not a new UI build. Confirm the RT-W2 admin width fix, the BP-03 token spine, and the WCAG-AA floor; fold actionable findings; document the BP-03 EquityChart/SVG exemption for the milestone audit; and encode the real-device authed sign-off as a documented human_needed checkpoint (this sandbox has no real device and no Bash network).

Purpose: ROADMAP success criterion 4 (app-wide design-review audit passes; WCAG-AA floor + LOCKED invariants intact) + criterion 5 (RT-W2 confirmed). The design-review portion runs now; the real-device sign-off lands as a deferred human checkpoint, consistent with prior milestones' deferred authed UATs.
Output: a design-audit findings doc + a BP-03 exemption note + a human-verify checkpoint for the real-device sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-CONTEXT.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-wide design-review audit against DESIGN.md</name>
  <read_first>
    - DESIGN.md (the locked design contract — fonts, colors, spacing, anti-decoration rules)
    - .planning/phases/54-.../54-CONTEXT.md (VERIFY-05 scope: this is an audit of existing UI, not a UI-SPEC; RT-W2 is the one concrete change, already done in 54-03)
    - the RT-W2 fix (the 4 admin pages from 54-03) + the BP-03 token spine (globals.css + the migrated files)
    - the 54-08 outcome (authed + mobile axe rows re-enabled against the seeded MA-8 DB) — this plan is serialized AFTER 54-08 so the WCAG-AA floor claim below is BACKED by green authed/mobile axe runs, not asserted blind
    - CLAUDE.md skill routing (design-review / design-consultation are skills — invoke the design-review pass)
  </read_first>
  <action>
    Run an app-wide design-review pass against the locked DESIGN.md (use the project's design-review / gsd-ui-review skill per CLAUDE.md routing; do NOT use Grok or Codex — fresh-Claude is the sole review mechanism per the project directives). Audit the v1.4 surfaces app-wide: the allocator journey, wizard, /security, admin (incl. the new RT-W2 caps), public/marketing, portfolios. Check against DESIGN.md: type spine (the fixed-token migration must not have visibly altered any size — it is byte-identical), color/spacing tokens, anti-decoration (no gradients/blobs/parallax/motion-libraries), and the WCAG-AA floor (now PROVEN by the authed + mobile axe rows re-enabled in 54-08, which this plan depends on — cite their green CI run in the audit rather than claiming the floor without evidence). Write findings to `.planning/phases/54-.../54-DESIGN-AUDIT.md` with each finding's severity + disposition (fix-now / log-as-debt / accept). For any fix-now finding that is a small surgical className/token correction, fold it (surgical, CLAUDE.md Rule 3) and note it; for anything larger, log it as debt with a clear pointer — do NOT silently expand scope. Confirm in the doc that the RT-W2 admin caps render correctly (cross-check with the static admin-width.test.tsx). Flag any code that does not match DESIGN.md (QA-mode rule).
  </action>
  <verify>
    <automated>test -f ".planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-DESIGN-AUDIT.md" && grep -qi "DESIGN.md" ".planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-DESIGN-AUDIT.md" && grep -qi "RT-W2\|admin.*max-w\|width" ".planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-DESIGN-AUDIT.md" && echo "audit doc present with DESIGN.md + RT-W2 coverage"</automated>
  </verify>
  <acceptance_criteria>
    - 54-DESIGN-AUDIT.md exists with per-finding severity + disposition
    - It confirms the RT-W2 admin caps and the byte-identical token spine
    - It confirms (or flags) the WCAG-AA floor and the anti-decoration rules
    - Any fix-now folds are surgical; larger findings are logged as debt, not silently dropped
    - If lint/tests were touched by a fold, `npx eslint "src/**/*.{ts,tsx}"` stays at 0 errors
  </acceptance_criteria>
  <done>The app-wide design-review audit ran against DESIGN.md; findings are dispositioned; RT-W2 + token spine + WCAG-AA confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Document the BP-03 EquityChart/SVG exemption for the milestone audit</name>
  <read_first>
    - eslint.config.mjs (the frozen off-glob from 54-01a + the error flip from 54-05)
    - src/__tests__/phase-52-frozen-spine-guards.test.ts (:152-161 FROZEN_ISLANDS; :158 EquityChart)
    - .planning/phases/54-.../54-CONTEXT.md (the BP-03/FROZEN conflict resolution)
    - REQUIREMENTS.md (BP-03 body — "incl. the frozen EquityChart + chart-internal SVG")
  </read_first>
  <action>
    Write `.planning/phases/54-.../54-BP-03-EXEMPTION-NOTE.md` recording why BP-03's literal "incl. the frozen EquityChart + chart-internal SVG" is satisfied by an ESLint off-glob exemption rather than a px→token edit: editing those files reds the FROZEN_ISLANDS git-diff guard (phase-52-frozen-spine-guards.test.ts:158), a LOCKED invariant. The CONTEXT-locked resolution is "error repo-wide EXCEPT documented frozen-chart islands" — every non-frozen site is cleaned + the frozen sites are explicitly, documentably exempt. Cite the off-glob location in eslint.config.mjs, the 4 exempt files (EquityChart + TimeSeriesChart/HistogramChart/MasterBrush), and the proof that no-raw-font-px is error everywhere else (the 54-05 `npx eslint` clean run). This note exists so the milestone audit does NOT read BP-03 as an unmet gap.
  </action>
  <verify>
    <automated>test -f ".planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-BP-03-EXEMPTION-NOTE.md" && grep -qi "EquityChart" ".planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-BP-03-EXEMPTION-NOTE.md" && grep -qi "FROZEN\|frozen-spine\|git-diff" ".planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-BP-03-EXEMPTION-NOTE.md" && echo "exemption note present"</automated>
  </verify>
  <acceptance_criteria>
    - 54-BP-03-EXEMPTION-NOTE.md exists, names the 4 exempt files, cites the FROZEN_ISLANDS guard, and the off-glob location
    - It documents that no-raw-font-px is error everywhere else (BP-03 satisfied honestly)
  </acceptance_criteria>
  <done>The BP-03 frozen-chart exemption is documented so the milestone audit treats BP-03 as met.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The full v1.4 verification suite (49–54): app-wide ultra-wide/axe/reflow/no-clip matrix, the px→token migration (error repo-wide), the RT-W2 admin width caps, the lhci ratchet, and the tolerance-golden harness (bake deferred). The app-wide design-review audit (Task 1) ran against DESIGN.md.
    What CANNOT be automated here: a real-device authed sign-off — this sandbox has no physical device and the Bash sandbox has no external network. This is the VERIFY-05 human_needed item, consistent with prior milestones' deferred authed UATs.
  </what-built>
  <how-to-verify>
    On a physical phone and/or tablet, with a real authed session (qa-demo@quantalyze.app on prod, or a seeded test user):
    1. Load the authed surfaces (/allocations and its ?tab=scenario / ?tab=risk, /compare, /discovery, /portfolios, /admin prose+data pages, the wizard).
    2. At each, confirm: no horizontal overflow/reflow; no clipped/ellipsis-truncated content that should be visible; the prose/form admin pages no longer over-stretch (RT-W2); type/spacing match DESIGN.md; no a11y regressions (focus order, contrast, touch targets).
    3. Confirm the deferred golden PNG bake is understood as a SEPARATE deliberate per-chart CI commit (never blind --update-snapshots) — not part of this sign-off.
    4. Record the result (device, OS, surfaces checked, any findings) so VERIFICATION.md can carry it as the human_needed item.
  </how-to-verify>
  <resume-signal>Type "signed off" with the device/findings, or "defer" to carry it as the human_needed VERIFICATION.md item.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Audit + documentation; the human checkpoint involves a real authed session on a physical device (user-operated) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-09-01 | Information Disclosure | real authed session on a physical device during sign-off | accept | User-operated on their own device against prod/test; no credential is recorded in the planning docs |

No high/medium automated threats — audit + docs + a human checkpoint; no new code surface.
</threat_model>

<verification>
- 54-DESIGN-AUDIT.md + 54-BP-03-EXEMPTION-NOTE.md exist with the required content.
- Any folded design fixes keep `npx eslint "src/**/*.{ts,tsx}"` at 0 errors and the unit suite green.
- The real-device sign-off is encoded as a blocking human-verify checkpoint (human_needed if deferred).
</verification>

<success_criteria>
The app-wide design-review audit ran against DESIGN.md (RT-W2 + token spine + WCAG-AA confirmed); the BP-03 frozen-chart exemption is documented; the real-device authed sign-off is a documented human checkpoint. ROADMAP success criteria 4 (audit) + 5 (RT-W2 confirmed) are met; the deferred real-device item is encoded, not silently dropped.
</success_criteria>

<output>
Create `.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-09-SUMMARY.md` when done.
</output>
