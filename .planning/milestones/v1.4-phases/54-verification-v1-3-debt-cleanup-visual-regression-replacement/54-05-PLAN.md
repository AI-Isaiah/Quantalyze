---
phase: 54-verification-v1-3-debt-cleanup-visual-regression-replacement
plan: 05
type: execute
wave: 3
depends_on: [54-01a, 54-01b, 54-02a, 54-02b]
files_modified:
  - eslint.config.mjs
autonomous: true
requirements: [BP-03]
must_haves:
  truths:
    - "no-raw-font-px is `error` repo-wide (the :82 rule flips from warn to error)"
    - "npx eslint over the whole src tree reports 0 no-raw-font-px errors"
    - "The frozen-chart off-glob (54-01a) and test-exempt blocks remain"
    - "scenario.ts / FactsheetBody byte-equivalence guards stay GREEN"
    - "EquityChart + 3 chart-internal SVG files have zero git-diff (frozen-spine guard green)"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Repo-wide no-raw-font-px: error"
      contains: "no-raw-font-px"
  key_links:
    - from: "eslint.config.mjs :82 repo-wide rule"
      to: "the cleaned src tree (from 54-01b/54-02a/54-02b) + the off-globbed frozen islands (from 54-01a)"
      via: "ESLint flat-config severity"
      pattern: "no-raw-font-px.*error"
---

<objective>
Flip `no-raw-font-px` from `warn` to `error` repo-wide (eslint.config.mjs:82) — the LAST BP-03 step. After Plans 54-01b, 54-02a and 54-02b cleaned every non-frozen, non-test orphan and 54-01a added the frozen-chart off-glob, this flip makes any future raw `text-[Npx]` a build failure while the documented frozen-chart islands stay explicitly exempt.

Purpose: BP-03 / ROADMAP success criterion 2 — `no-raw-font-px` is `error` repo-wide (no `warn` orphans remain), with `scenario.ts`/`FactsheetBody` still byte-equivalent on the locked surfaces. This satisfies "error repo-wide" honestly: every non-frozen site is cleaned, and the frozen sites are explicitly, documentably exempt (the CONTEXT-locked BP-03/FROZEN conflict resolution — never edit EquityChart).
Output: one severity flip + a documentation comment, with all guards green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-PATTERNS.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-CONTEXT.md

<interfaces>
eslint.config.mjs:82 — `"quantalyze/no-raw-font-px": "warn"` -> flip to `"error"`.
MUST remain after the flip: the charts/** off-glob (:193-199), the NEW frozen-chart off-glob (added in 54-01a), the test-exempt block (:220-233), the design-tokens error block (:90-92).
The per-surface error ratchet blocks (:89-92, :107-189) become redundant once repo-wide is error — leave them (harmless; future cleanup, not required).
Guards that MUST stay green: src/__tests__/phase-52-frozen-spine-guards.test.ts (EquityChart + 3 SVG zero-diff), the FactsheetBody GUARD-02 innerHTML test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip no-raw-font-px to error repo-wide + document the frozen exemption</name>
  <read_first>
    - eslint.config.mjs (:64-82 the repo-wide rule + the warn rationale comment; :193-199 charts off-glob; the frozen off-glob added in 54-01a; :220-233 test-exempt)
    - .planning/phases/54-.../54-PATTERNS.md (eslint.config.mjs BP-03 ratchet section — the THREE coordinated edits, flip is LAST)
    - .planning/phases/54-.../54-CONTEXT.md (the BP-03/FROZEN conflict resolution)
  </read_first>
  <action>
    First confirm the precondition: `npx eslint "src/**/*.{ts,tsx}" --format json` shows ZERO `no-raw-font-px` WARNINGS outside the off-globbed frozen + charts + test files (i.e. 54-01b/54-02a/54-02b fully cleaned the tree and 54-01a added the frozen off-glob). If any non-exempt warning remains, STOP and report it (do NOT flip — the flip would red CI; CLAUDE.md Rule 12 fail-loud). Then change eslint.config.mjs:82 `"quantalyze/no-raw-font-px"` from `"warn"` to `"error"`. Update the adjacent rationale comment (the :64-82 block that explains "repo-wide WARN here") to record that BP-03 has flipped it to error repo-wide, with the frozen EquityChart + chart-internal SVG explicitly exempted via the off-glob (cite phase-52-frozen-spine-guards.test.ts:158 and the CONTEXT decision) so BP-03 is not read as an unmet gap at milestone audit. Do NOT remove the charts off-glob, the frozen off-glob, the test-exempt block, or the design-tokens error block. Do NOT edit any chart file.
  </action>
  <verify>
    <automated>npx eslint "src/**/*.{ts,tsx}" && node -e "const f=require('fs').readFileSync('eslint.config.mjs','utf8'); if(!/no-raw-font-px\W+:\W*.error./.test(f.replace(/\s+/g,' '))){console.error('rule not error');process.exit(1)} console.log('no-raw-font-px is error repo-wide')"</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint "src/**/*.{ts,tsx}"` exits 0 (zero errors repo-wide)
    - eslint.config.mjs :82 reads `"error"` (not `"warn"`)
    - The frozen off-glob, charts off-glob, and test-exempt blocks are all still present
    - The rationale comment documents the EquityChart/SVG exemption for the milestone audit
  </acceptance_criteria>
  <done>no-raw-font-px is error repo-wide; eslint is clean; the frozen exemption is documented.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the locked invariants stayed green</name>
  <read_first>
    - src/__tests__/phase-52-frozen-spine-guards.test.ts (:152-189 FROZEN_ISLANDS git-diff; :158 EquityChart)
    - the FactsheetBody GUARD-02 innerHTML test (locate via grep "GUARD-02" src)
  </read_first>
  <action>
    Run the frozen-spine guard and the FactsheetBody byte-equivalence test to confirm the entire BP-03 migration (54-01a/54-01b/54-02a/54-02b/54-05) left the locked surfaces byte-equivalent. Confirm `git diff --stat` shows zero changes to EquityChart.tsx, TimeSeriesChart.tsx, HistogramChart.tsx, MasterBrush.tsx, scenario.ts, and FactsheetBody's byte-locked output. If any guard reds, trace the offending edit (a chart file was wrongly touched, or a fixed token changed a rendered size) and report — do NOT suppress (CLAUDE.md Rule 6 root-cause, Rule 12 fail-loud).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts $(grep -rl "GUARD-02" src --include="*.test.tsx" | head -1)</automated>
  </verify>
  <acceptance_criteria>
    - phase-52-frozen-spine-guards.test.ts passes (non-vacuously — it fails loud if it can't resolve a baseline)
    - The FactsheetBody GUARD-02 innerHTML test passes
    - `git diff --stat` shows zero changes to the 4 frozen chart files + scenario.ts
  </acceptance_criteria>
  <done>All LOCKED byte-equivalence invariants are proven intact after the BP-03 flip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | ESLint config severity flip; build-time gate only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-05-01 | Tampering | Flipping to error with residual orphans -> reds CI | mitigate | Task 1 hard-gates on a zero-warning precondition before flipping; fail-loud if any non-exempt orphan remains |
| T-54-05-02 | Tampering | Frozen chart / scenario.ts byte-equivalence | mitigate | Task 2 re-runs the frozen-spine + GUARD-02 guards as tripwires |

No high/medium threats — build-time lint config, no new attack surface.
</threat_model>

<verification>
- `npx eslint "src/**/*.{ts,tsx}"` exits 0.
- `npx vitest run src/__tests__/phase-52-frozen-spine-guards.test.ts` + GUARD-02 — green.
- `git diff --stat` — zero changes to the 4 frozen chart files and scenario.ts.
</verification>

<success_criteria>
no-raw-font-px is `error` repo-wide; eslint is clean; the frozen-chart islands are documented exemptions; scenario.ts / FactsheetBody byte-equivalence and the frozen-spine guard are all green. ROADMAP success criterion 2 is met.
</success_criteria>

<output>
Create `.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-05-SUMMARY.md` when done.
</output>
