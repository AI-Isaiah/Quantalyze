---
phase: 54-verification-v1-3-debt-cleanup-visual-regression-replacement
plan: 08
type: execute
wave: 3
depends_on: [54-06, 54-07]
files_modified:
  - e2e/axe-app-wide.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [VERIFY-02]
must_haves:
  truths:
    - "The dormant authed + embedded axe describes run hermetically against the seeded MA-8 DB"
    - "The cross-spec-dangerous crypto-sma discovery seed is torn down (delete-by-id in finally/afterAll)"
    - "axe-app-wide is FLOW-01 dual-wired into the seeded MA-8 list (it was public-only before)"
    - "discovery-hide-examples-default and /demo public rows stay green (no v1.3 pollution)"
  artifacts:
    - path: "e2e/axe-app-wide.spec.ts"
      provides: "Un-skipped authed/embedded describes + crypto-sma seed teardown"
      contains: "delete"
    - path: ".github/workflows/ci.yml"
      provides: "axe-app-wide in the seeded MA-8 list"
      contains: "axe-app-wide"
  key_links:
    - from: "e2e/axe-app-wide.spec.ts seedBridgeCandidate(crypto-sma)"
      to: "strategies table delete-by-id"
      via: "finally { admin.from('strategies').delete().eq('id', seeded.strategyId) }"
      pattern: "delete\\(\\)\\.eq"
---

<objective>
Re-enable the authed + mobile axe rows (deferred in v1.3 for lack of a hermetic seeded DB) against the existing seeded MA-8 harness, with explicit teardown of the one cross-spec-dangerous seed. This was disabled at ci.yml:1293-1309 because broad authed seeding into the SHARED MA-8 DB left a `crypto-sma` discovery strategy around that `discovery-hide-examples-default.spec.ts` asserts is EMPTY (plus a `/demo` 500 and spurious wizard findings — the v1.3 pollution trap).

Purpose: ROADMAP success criterion 3 — "authed/mobile axe rows are re-enabled against a hermetic seeded DB." The hermeticity is achieved by un-skipping the dormant describes, wiring axe-app-wide into the MA-8 list (FLOW-01), and tearing down the dangerous crypto-sma seed by id in a finally/afterAll.
Output: un-skipped authed/embedded axe describes + teardown + the MA-8 wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-RESEARCH.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-PATTERNS.md

<interfaces>
axe-app-wide.spec.ts: HAS_SEED_ENV :61-63 (stays); authed describe :123 with `test.skip(!HAS_SEED_ENV, …)` :124 (stays — self-skips locally, runs seeded in CI); embedded describe :236 with :237 skip (stays); the dangerous seed `seedBridgeCandidate({categorySlug:"crypto-sma"})` at :187 — discovery-hide-examples-default.spec.ts asserts that category EMPTY.
seedBridgeCandidate RETURNS `{ strategyId, ownerUserId }` (seed-test-project.ts:72-74, :274). Seed helpers use a service-role admin client + assertNotProductionSupabaseUrl / assertSupabaseServiceRoleKey prod-misconfig probes (:59-60) — the admin client only ever points at the TEST project (qmnijlgmdhviwzwfyzlc), never prod (khslejtfbuezsmvmtsdn).
Teardown idiom (PATTERNS axe section / RESEARCH VERIFY-02 example): `const seeded = await seedBridgeCandidate({categorySlug:"crypto-sma"}); try { login+goto+axe } finally { await admin.from("strategies").delete().eq("id", seeded.strategyId); }`.
FLOW-01 dual-wire: add `e2e/axe-app-wide.spec.ts` to the seeded MA-8 list (ci.yml ~:1266-1280) — it is ALREADY in the unseeded list (:1073, public rows) and ALREADY HAS_SEED_ENV-gated (place 2 satisfied). Update the :1293-1309 comment that says it is "intentionally NOT in this seeded MA-8 list" to record the hermetic teardown that makes it safe now.
The 2560 row added in 54-06 will now ALSO run for the authed/embedded describes (the three for-loops iterate VIEWPORTS) — that is the VERIFY-01 authed ultra-wide canary activating.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tear down the crypto-sma discovery seed (hermeticity)</name>
  <read_first>
    - e2e/axe-app-wide.spec.ts (:36-46 the pollution-rationale comment; :184-193 the seedBridgeCandidate(crypto-sma) call site)
    - e2e/helpers/seed-test-project.ts (:202 seedBridgeCandidate; :72-74,:274 the {strategyId, ownerUserId} return; :59-60 the prod-URL safety probes; the admin client accessor)
    - e2e/discovery-hide-examples-default.spec.ts (what it asserts EMPTY — confirm the category)
    - .planning/phases/54-.../54-RESEARCH.md (Pitfall 2 — the exact pollution; VERIFY-02 code example)
  </read_first>
  <action>
    Capture the return of `seedBridgeCandidate({categorySlug:"crypto-sma"})` at :187 into `const seeded` and wrap the dependent login+goto+axe body in `try { … } finally { await admin.from("strategies").delete().eq("id", seeded.strategyId); }` (obtain the service-role admin client the same way the seed helper does — import/construct it via the existing helper, NOT a new client). This deletes the published non-example strategy after the test so discovery-hide-examples-default.spec.ts sees the crypto-sma category EMPTY again. CRITICAL safety: the admin client must target ONLY the TEST Supabase project — rely on the helper's existing `assertNotProductionSupabaseUrl` probe; do NOT hardcode a project ref, and never point at prod (khslejtfbuezsmvmtsdn). The other authed/wizard/allocator seeds are user-scoped (unique timestamped emails) and inert to other specs, but for true hermeticity also tear down any strategy/owner this describe creates in an afterAll. Do NOT delete rows you did not seed.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('e2e/axe-app-wide.spec.ts','utf8'); if(!/seedBridgeCandidate[\s\S]{0,400}finally[\s\S]{0,200}delete\(\)\.eq\(.id./.test(s)){console.error('crypto-sma seed not torn down in a finally');process.exit(1)} console.log('crypto-sma seed teardown present')"</automated>
  </verify>
  <acceptance_criteria>
    - The crypto-sma `seedBridgeCandidate` result is captured and deleted by id in a `finally` (and/or afterAll)
    - The teardown uses the helper's existing admin client (with its prod-URL safety probe), never a hardcoded prod ref
    - Only seeded-by-this-spec rows are deleted
  </acceptance_criteria>
  <done>The dangerous crypto-sma discovery seed is torn down by id — discovery-hide-examples-default sees the category empty; no v1.3 pollution.</done>
</task>

<task type="auto">
  <name>Task 2: Wire axe-app-wide into the seeded MA-8 list + update the rationale comment</name>
  <read_first>
    - .github/workflows/ci.yml (:1266-1282 the MA-8 list; :1293-1309 the "intentionally NOT in this seeded MA-8 list" comment block)
    - e2e/axe-app-wide.spec.ts (:61-63 HAS_SEED_ENV — confirm place 2 already satisfied)
    - .planning/phases/54-.../54-PATTERNS.md (axe section integration point + FLOW-01)
  </read_first>
  <action>
    Add `e2e/axe-app-wide.spec.ts` to the seeded MA-8 list in ci.yml (the `npx playwright test \` block ending `--timeout 60000`, ~:1266-1280). It stays in the unseeded list too (its public rows). Then REWRITE the :1293-1309 comment that currently says axe-app-wide is "intentionally NOT in this seeded MA-8 list … running axe-app-wide here regressed three other behaviours" — replace it with a comment recording that VERIFY-02 (Phase 54) re-enabled it hermetically: the authed/embedded describes self-skip via HAS_SEED_ENV (place 2), and the dangerous crypto-sma discovery seed is now torn down by id in a finally (Task 1), so the three v1.3 regressions (discovery-hide-examples pollution, /demo 500, spurious wizard findings) no longer occur. Do NOT remove the FLOW-01 dual-wire reminder at :1264.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const mblock=y.slice(y.indexOf('npx playwright test \\\\', y.indexOf('MA-8')), y.indexOf('--timeout 60000', y.indexOf('MA-8'))); if(!/axe-app-wide\.spec\.ts/.test(mblock)){console.error('axe-app-wide not in MA-8 seeded list');process.exit(1)} console.log('axe-app-wide wired into seeded MA-8 list')"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/axe-app-wide.spec.ts` appears in the seeded MA-8 list (and remains in the unseeded list)
    - The :1293-1309 comment is updated to document the hermetic re-enable (no longer says "intentionally NOT")
    - The FLOW-01 reminder at :1264 is preserved
  </acceptance_criteria>
  <done>axe-app-wide runs its authed/embedded/mobile/2560 rows in the seeded MA-8 job; the rationale comment reflects the hermetic re-enable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded test -> SHARED MA-8 test DB | broad authed seeding (the exact surface that caused v1.3 pollution) |
| teardown admin client -> Supabase project | a service-role delete — MUST target the TEST project only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-08-01 | Tampering / Data-loss | teardown deleting prod data | mitigate | The admin client carries assertNotProductionSupabaseUrl (seed-test-project.ts:59-60); it only ever targets the TEST project (qmnijlgmdhviwzwfyzlc), never prod (khslejtfbuezsmvmtsdn); delete is scoped to the seeded strategyId only |
| T-54-08-02 | Tampering | v1.3 cross-spec pollution recurrence | mitigate | crypto-sma seed torn down by id in a finally (Task 1) so discovery-hide-examples-default sees the category empty; user-scoped seeds are inert |
| T-54-08-03 | Information Disclosure | real session token leaking into CI logs/artifacts | mitigate | The authed rows log in via loginViaForm against the test project's seeded user; no token is echoed to the job log or uploaded as an artifact (axe results are pass/fail counts, not credentials) |

No residual high threats — the data-loss and pollution risks are mitigated by the helper's prod-URL probe + scoped delete-by-id.
</threat_model>

<verification>
- axe-app-wide.spec.ts has the crypto-sma teardown in a finally; the admin client uses the helper's prod-safety probe.
- axe-app-wide appears in BOTH ci.yml lists (unseeded + seeded MA-8).
- In a configured CI, the seeded MA-8 job runs axe-app-wide authed/embedded/mobile/2560 rows AND discovery-hide-examples-default + the /demo public rows stay green (no pollution).
</verification>

<success_criteria>
The authed + mobile (+ 2560) axe rows run hermetically against the seeded MA-8 DB; the dangerous crypto-sma seed is torn down by id; discovery-hide-examples-default and /demo stay green; no real session token leaks to CI logs/artifacts. ROADMAP success criterion 3 (authed/mobile axe portion) is met.
</success_criteria>

<output>
Create `.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-08-SUMMARY.md` when done.
</output>
