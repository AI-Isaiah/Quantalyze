---
phase: 54-verification-v1-3-debt-cleanup-visual-regression-replacement
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - lighthouserc.json
autonomous: false
requirements: [VERIFY-03]
user_setup: []
must_haves:
  truths:
    - "The lhci report artifact is available unconditionally (or via a deterministic measure step) so the measured floor can be READ, not guessed"
    - "lhci minScore is raised from 0.60 to (lowest measured 3-run median across the 5 public routes) minus 0.02"
    - "The mobile formFactor + screenEmulation + npm-run-start prod build are unchanged"
    - "The 5 PUBLIC-only URLs are unchanged (no authed URL leaks to temporary-public-storage)"
  artifacts:
    - path: "lighthouserc.json"
      provides: "Ratcheted minScore on categories:performance"
      contains: "minScore"
    - path: ".github/workflows/ci.yml"
      provides: "Unconditional lhci artifact upload (or one-shot measure step) for the floor read"
  key_links:
    - from: ".github/workflows/ci.yml lighthouse-mobile job"
      to: "lighthouserc.json minScore"
      via: "lhci autorun assert.assertions"
      pattern: "lhci autorun"
---

<objective>
Ratchet the lighthouse-mobile budget up from 0.60. First make the measured floor READABLE (the artifact currently uploads `if: failure()` only at ci.yml:1477), then set `minScore = (lowest measured 3-run median across the 5 public routes) - 0.02` — the data-driven "under-actual" philosophy that fails a real regression without flaking on noise.

Purpose: VERIFY-03. The flat 0.60 budget is a guess; the 2026-06-28 single-run baseline showed `/demo` at 0.67 lowest. The ratchet must be derived from a clean CI measurement, not picked.
Output: an unconditional (or one-shot) lhci measure path + a ratcheted minScore.

NOTE: This plan contains a CHECKPOINT — reading the measured floor requires a CI run's lhci output, which a human/CI must surface. The plan lands the artifact-read mechanism, then a decision checkpoint to enter the measured value, then the ratchet edit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-RESEARCH.md
@.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-PATTERNS.md

<interfaces>
lighthouserc.json:34 — `"categories:performance": ["error", { "minScore": 0.6 }]`.
ci.yml:1464 — `npx lhci autorun --config=lighthouserc.json` (3-run median, mobile formFactor, npm run start prod build).
ci.yml:1475-1482 — the artifact upload is `if: failure()` ONLY (the re-measure subtlety — RESEARCH Pitfall 5 / Assumption A2).
DO NOT touch (RESEARCH Pitfalls 5+6): `formFactor:"mobile"` + `screenEmulation` (no `preset:"mobile"` — invalid in Lighthouse 12.x); `startServerCommand:"npm run start"` (never next dev); the 5 PUBLIC-only URLs (T-48-05-INFO — public-only avoids authed-URL info-disclosure to temporary-public-storage).
Per-URL scores live in `.lighthouseci/manifest.json` + `*.report.json` -> `categories.performance.score`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the lhci measured floor readable</name>
  <read_first>
    - .github/workflows/ci.yml (:1455-1482 — the lhci autorun step + the `if: failure()` upload)
    - lighthouserc.json (the full config — upload.target, assertions, urls)
    - .planning/phases/54-.../54-RESEARCH.md (Pitfall 5 — read the artifact JSON; Don't-Hand-Roll table — parse manifest.json)
  </read_first>
  <action>
    Make the measured per-URL 3-run-median scores readable from CI WITHOUT relying on a failure. Choose ONE: (a) change the `Upload Lighthouse reports` step's condition from `if: failure()` to `if: always()` (simplest — the report uploads every run so the floor can be read from a green run), OR (b) add a short `Print measured scores` step after `lhci autorun` that node-parses `.lighthouseci/manifest.json` for each URL's `summary.performance` and echoes them to the job log. Keep the existing PUBLIC-only URL set, the placeholder env (no TEST_SUPABASE_*), and `npm run start`. This is a CI-yaml-only edit; do NOT change lighthouserc.json yet.
  </action>
  <verify>
    <automated>node -e "const y=require('fs').readFileSync('.github/workflows/ci.yml','utf8'); const seg=(y.split('Upload Lighthouse')[1]||''); const ok=/if:\s*always\(\)/.test(seg) || /manifest\.json/.test(y); if(!ok){console.error('no readable-floor mechanism'); process.exit(1)} console.log('readable-floor mechanism present')"</automated>
  </verify>
  <acceptance_criteria>
    - The lhci report is uploaded on success (or per-URL scores are echoed to the job log) so the measured floor is observable from a green CI run
    - The 5 public URLs, mobile formFactor/screenEmulation, and `npm run start` are unchanged
    - No TEST_SUPABASE_* env added to the lighthouse-mobile job
  </acceptance_criteria>
  <done>The measured per-URL lhci scores are readable from CI without depending on a failed run.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>What is the lowest measured 3-run-median performance score across the 5 public routes (from the CI lhci artifact / job log)?</decision>
  <context>The ratchet must be DATA-driven: minScore = lowest measured median - 0.02. Without a fresh CI measurement we'd be guessing (the banned flat-0.65 approach). The 2026-06-28 single-run reference put /demo lowest at 0.67 — but that was a single run, not the 3-run median this gate uses. The executor needs the human/CI to report the actual measured floor before setting the constant.</context>
  <options>
    <option id="use-measured">
      <name>Use the freshly measured floor</name>
      <pros>Data-driven, matches the under-actual philosophy, won't flake</pros>
      <cons>Requires a CI run to produce the artifact first</cons>
    </option>
    <option id="conservative-067">
      <name>Fall back to the 2026-06-28 reference floor (~0.65 = 0.67 - 0.02) if no fresh measure is available</name>
      <pros>Still a real ratchet up from 0.60; unblocks the plan</pros>
      <cons>Based on a single-run not a 3-run median; revisit when a clean median lands</cons>
    </option>
  </options>
  <resume-signal>Reply with the lowest measured 3-run-median score (e.g. "0.71"), or "use 0.65 fallback".</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Ratchet minScore to (measured floor - 0.02)</name>
  <read_first>
    - lighthouserc.json (:34 minScore + the :3 "under-actual" philosophy comment)
    - The decision-checkpoint result (the measured floor value)
  </read_first>
  <action>
    Set `categories:performance` `minScore` in lighthouserc.json to (the floor from the checkpoint) minus 0.02, rounded to 2 decimals. Update the adjacent comment to record the measured floor and the date (mirroring the coverage-ratchet comment style — "set a few points under measured actual so a real regression fails but noise doesn't"). Change ONLY the minScore constant; leave assertions structure, URLs, formFactor, screenEmulation, and startServerCommand untouched. The new value MUST be strictly greater than 0.60 (a real ratchet up).
  </action>
  <verify>
    <automated>node -e "const c=require('./lighthouserc.json'); const a=c.ci.assert.assertions['categories:performance']; const m=a[1].minScore; if(!(m>0.6)){console.error('minScore not ratcheted up:',m);process.exit(1)} console.log('minScore =',m)"</automated>
  </verify>
  <acceptance_criteria>
    - `minScore > 0.60` (real ratchet) and equals (measured floor - 0.02)
    - The adjacent comment records the measured floor + date
    - URLs / formFactor / screenEmulation / startServerCommand unchanged (git diff is minScore + comment only)
  </acceptance_criteria>
  <done>lhci minScore is ratcheted up from 0.60 to the data-driven floor; a regression below it fails the lighthouse-mobile job.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job -> temporary-public-storage | lhci uploads reports to a public URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-54-04-01 | Information Disclosure | lhci report upload | mitigate | Keep the URL set PUBLIC-only (T-48-05-INFO); never add an authed route or TEST_SUPABASE_* env to the lighthouse-mobile job — public placeholders only |
| T-54-04-02 | Tampering | minScore guessed not measured | mitigate | Decision checkpoint forces a data-driven floor; verify asserts minScore > 0.60 |

No high threats — CI-config only; the one medium (info-disclosure) is mitigated by the existing public-only URL set, which this plan must not change.
</threat_model>

<verification>
- `node -e` assertion confirms minScore > 0.60.
- `git diff lighthouserc.json` is minScore + comment only.
- The lighthouse-mobile CI job remains public-only with placeholder env.
</verification>

<success_criteria>
The lhci floor is readable from a green CI run; minScore is ratcheted up from 0.60 to (measured 3-run-median floor - 0.02); the mobile/prod-build/public-URL configuration is otherwise unchanged.
</success_criteria>

<output>
Create `.planning/phases/54-verification-v1-3-debt-cleanup-visual-regression-replacement/54-04-SUMMARY.md` when done.
</output>
