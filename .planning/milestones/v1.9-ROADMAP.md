# Roadmap: Quantalyze

## Milestones

- ✅ **v1.0–v1.6** — shipped; see `MILESTONES.md` + `milestones/` archives.
- ✅ **v1.7 — Deribit Exchange Coverage & Carry-Forward Burn-Down** — Phases 66–72 (tag `v1.7` @ `9a1e7b8e`, shipped 2026-07-05). Archive: `milestones/v1.7-phases/`.
- ✅ **v1.8 — Flow-Aware TWR + Native-Unit NAV** — Phases 73–80 (tag `v1.8` @ `eb8e357e`, shipped 2026-07-09). Native-unit (79–80) shipped inside this tag; the premature "v1.9 Native-Unit" label was retired. Archive: `milestones/v1.8-phases/`, `milestones/v1.8-native-unit-ROADMAP.md`.
- 🚧 **v1.9 — Multi-Key Composite Strategy (frontend-led, #596)** — Phases 85–91 (below). Numbering continues from 85 (82–84 were post-v1.8 standalone/archived).

## Notes

- **Deferred to Backlog** (from v1.8): Phase 81 (ccxt venues native + USD-space legacy retirement); the daily-option mark-to-market exploration (was Phase 83 dir, uncommitted). GitHub **Backlog** milestone #1 holds #596/#337/#338.
- **Post-v1.8 standalone phases** (archived, `milestones/post-v1.8-standalone-phases/`): Phase 82 (deribit-derivatives-pnl), Phase 83 (daily-option-mtm), Phase 84 (#597 blend/allocation asset-class annualization — shipped v0.40.0.0, PR #600).

---

# v1.9 — Multi-Key Composite Strategy (Phases 85–91)

**Milestone goal:** A user onboards ONE strategy composed of N keys (each with an active window) through the wizard, sees them stitched into one track record, and views the factsheet with missing-data segments marked and an honestly-gated `cash_settlement ↔ mark_to_market` basis toggle — end to end in the GUI.

**Milestone acceptance gate:** the GUI-onboarded 3-key Zavara composite reproduces cum ~62.66% / maxDD ~−4.13% on `cash_settlement` through the PRODUCTION path.

**Frozen invariants (LOCKED, apply to every phase):**
- **No-invented-data** — gaps are first-class marked segments excluded from compounding, never zero-filled flat lines; MTM is honestly disabled where it would lie.
- **Single-key back-compat** — every existing single-key strategy behaves byte-identically; no backfill, `strategies.api_key_id` untouched.
- **MTM = honest gating (Path 1)** — the toggle ships, but MTM is enabled only on perp-only/USD-native or Phase-83-smoothed books; honest-disabled-with-reason on un-smoothed options books (incl. Zavara). NEVER the ungated naive toggle (live 94%/day spike evidence). Default + headline + acceptance + peer/ranking stay `cash_settlement`.
- **Zero new npm deps** — installed deps + in-repo components only (zod `superRefine`, native `<input type="date">`, `SegmentedControl`, bespoke `TimeSeriesChart`, `dateday`).
- **Deferred, do NOT scope in:** Phase 83 MTM-smoothing, Phase 81 ccxt-native, #337, mig-038 root-fix.

## Phases

- [x] **Phase 85: Thin Data Model (`strategy_keys`)** - Additive join table + `metrics_json_by_basis` stub; single-key byte-identity proven. COMPLETE + verified on TEST (2026-07-10).
- [x] **Phase 86: Production Stitch Endpoint** - Harness `stitch_key_outputs` promoted to a fail-loud production worker job. All 4 plans executed; migration applied+reconciled on TEST; BLOCKING Zavara 3-key acceptance passed LIVE (cum 62.6646% / maxDD −4.1328% cash_settlement). 2 specialist+2 red-team rounds converged (opus convergence pass CLEAN); verifier ACHIEVED 6/6. Carry-forward: F-3 mig-038 resubmit-poison → Phase 87 PUB-02 (shared status-bridge, documented).
- [x] **Phase 87: #338 All-N-Keys-Complete Publish Gate** - No composite publishes with a silently-failed member key. COMPLETE + verified ACHIEVED 4/4 (2026-07-10). F-3 mig-038 poison CLOSED (per-(strategy,kind) created_at supersession); M-3 published-composite delete-hole CLOSED (BEFORE DELETE guard + GDPR sanitize exemption). Both migrations applied+reconciled on TEST, all SQL tests GREEN incl. cross-kind SAFETY + sanitize-exempt; 3 specialists + opus red team CLEAN; a real GDPR-account-deletion regression caught+fixed mid-review.
- [x] **Phase 88: Wizard Multi-Key Onboarding UX** - Repeatable add-key + window/seq editor + shared overlap validation + idempotency-fence re-scope (completed 2026-07-10)
- [x] **Phase 89: Composite Preview** - Per-key boundaries, stitched track, attribution, mini-gantt; submit blocked on non-success keys (completed 2026-07-10)
- [x] **Phase 90: Factsheet Markers + Basis Toggle** - Stitched track through the real `FactsheetBody` + gap markers + honest-gated MTM toggle. COMPLETE + verified 7/7 (2026-07-11). Zero DDL, read+render. ⭐Load-bearing work = a read-path fix (composites misclassified as "api" → invented panels + flat-zero gap lines) applied to BOTH the factsheet route AND the discovery detail page via a shared `composite-read-path.ts`. Config-driven cumulative-method (arithmetic only for "simple"/Zavara, geometric mainline — both gap-invariant so chart endpoint == persisted headline). 13 findings across 3 review rounds (⭐fable red team caught a CRITICAL chart/headline mixed-basis + a regression the first fix introduced, both missed by 4 specialists). Carry-forward: LOW-2 config-drift (worker-side → Phase 91/backlog, see 90-deferred-items.md).
- [x] **Phase 90.5: Dynamic Per-Strategy Leverage** - Factsheet live per-strategy leverage (client `compute.ts` recompute, ephemeral) [LEV-01]; scenario-planner Save persists leverage into the saved-scenario draft [LEV-02, German user request]. COMPLETE + verified 7/7 (2026-07-11). Zero DDL, frontend-only, `scenario.ts` engine byte-untouched, no `SCENARIO_SCHEMA_VERSION` bump. ⭐LEV-01 SINGLE-KEY only (composites fail-closed — recompute would re-open Phase-90 divergence); L=1 byte-identical (same object ref). ⭐The fable red team caught the crux 4 specialists missed: LEV-02 persisted `leverageOverrides` but only 1 of the draft's 3 readers applied it → fixed the share page + compare panel (all 3 now consistent, deep-equality pinned). 12 findings across 3 review rounds (incl. a half-done bleed-fix, cross-surface HIGHs, a public-share Sentry quota-burn). Coverage 85.85/83.74/80.44/76.86.
- [x] **Phase 91: Frontend Test & QA** - Vitest + seed-gated e2e + #338 fault-injection + Zavara end-to-end acceptance + axe + `/qa`. COMPLETE + verified 7/8 (2026-07-11) — QA-01 ✅, QA-03 ✅, QA-02 ⚠️ SC-3 piece 3 (live-GUI Zavara onboarding) OPEN as an explicit human_verification USER GATE (pieces 1+2 shipped/credited). Test/e2e/ci/seed only, zero shipping `src/**`. ⭐Fable red team POLICY-VERIFIED the CRITICAL fix (seed composite under the logged-in allocator so the wizard's RLS browser reads of strategy_analytics/strategy_keys/csv_daily_returns resolve) — a QA-gate false-red the test-analyzer caught statically that would have failed both onboarding e2e the instant CI env landed. Seed-gated e2e green is CI-gated (specs self-skip locally). See `91-EVIDENCE.md` + `91-VERIFICATION.md`.

## Phase Details

### Phase 85: Thin Data Model (`strategy_keys`)
**Goal**: A composite strategy's member keys, windows, and sequence persist durably — with zero disturbance to any existing single-key strategy.
**Depends on**: Nothing (first v1.9 phase)
**Requirements**: COMP-01 (+ the COMP-04 `metrics_json_by_basis` JSONB schema stub — COMP-04 itself is satisfied in Phase 86)
**Success Criteria** (what must be TRUE):
  1. A strategy row links to N api keys, each with `window_start`/`window_end`/`seq`, via an additive `strategy_keys` join table with owner-coherence trigger + RLS — a user cannot attach another tenant's key.
  2. `strategy_analytics` carries an additive nullable `metrics_json_by_basis` JSONB stub (no backfill, no NOT-NULL existing rows can't satisfy).
  3. Every existing single-key strategy behaves byte-identically — no backfill, `strategies.api_key_id` untouched; proven by the regression suite green with zero snapshot diffs.
  4. The migration is pure-additive `CREATE TABLE`/`ALTER ADD` with a self-verifying `DO` block AND a down migration; post-merge prod object verification passes (migrations auto-apply on merge).
**Plans**: 1 plan

Plans:
- [x] 85-01-PLAN.md — Additive strategy_keys migration (table + owner-coherence trigger + RLS + metrics_json_by_basis stub + down mirror) + SQL regression — code executed (commits daac3057, 0bf902ba); live test-DB apply pending orchestrator
**Migration-first / prod-auto-apply**: YES — pure-additive DDL + down migration + post-merge object verification mandatory.

### Phase 86: Production Stitch Endpoint
**Goal**: The production path reconstructs each member key's dailies, clips to declared windows, and stitches them into ONE honest combined track with both P&L bases persisted.
**Depends on**: Phase 85 (`strategy_keys` + JSONB stub)
**Requirements**: COMP-02, COMP-03, COMP-04
**Success Criteria** (what must be TRUE):
  1. A `stitch_composite` Railway worker job (dispatched from a Next.js route — only the worker decrypts keys) runs `build_composite_dailies`: per-key ledger fan-out → clip to half-open `[start, end)` windows (matching `capital_on_date`) → ONE combined series → allocated-capital metrics over the full window.
  2. Overlapping windows FAIL LOUD (never last-write-wins); the overlap guard's semantics live in ONE shared helper convention consumed by both this backend and the Phase 88 wizard validator (v1.5 lesson: same inputs / different derivations = silent divergence).
  3. Gap days carry an explicit coverage mask — surfaced with per-key first/last-day boundaries, gap days, and overlap days as `data_quality_flags` — never zero-filled as flat performance.
  4. The stitched cumulative return is an arithmetic sum end-to-end with an inception-seeded max-drawdown (no per-key geometric chain-link seam spikes).
  5. Both `cash_settlement` and `mark_to_market` bases are computed and persisted at derive time into `metrics_json_by_basis`.
  6. **BLOCKING failure criterion**: the 3-key Zavara fixture reproduces cum ~62.66% / maxDD ~−4.13% on `cash_settlement` through this production path, provable headless (no GUI dependency).
**Plans**: 4 plans

Plans:
- [x] 86-01-PLAN.md — stitch_composite job-kind migration + metrics_json_by_basis SQL shape guard — code executed (commits 93f2b972 feat, ba815b52 test); [BLOCKING] live test-DB apply pending orchestrator
- [x] 86-02-PLAN.md — TDD pure stitch core (half-open clip, fail-loud overlap guard + shared Phase-88 convention fixture, coverage mask, MTM honesty gate) — EXECUTED (commits 10253660 test RED, 89f71e39 feat GREEN); services/stitch_composite.py pure, 26 tests + shared fixture, full suite 3524 pass CI-3.12, additive-only
- [x] 86-03-PLAN.md — run_stitch_composite_job worker fan-out + both-bases persist + dispatch + Next.js composite enqueue — EXECUTED (commits 27aa36f7/80acdef8 has_option_activity signal, 0531d656/751d671b worker, ec97e0e5 dispatch+route W-4 fail-closed, a63759ae watchdog); Deribit path shipped (ccxt composite members fail-loud follow-up), 14 tests GREEN, parity byte-identical, full suite 3538 pass CI-3.12 coverage 88.50%, vitest 41 pass
- [x] 86-04-PLAN.md — headless 3-key Zavara acceptance (BLOCKING phase gate, A1 window confirmation) + full-suite/parity gates
**Research**: `--research-phase` REQUIRED at plan time (stitch seam semantics — DEEP RESEARCH flag per research/SUMMARY.md). Highest-risk phase of the milestone; deliberately front-loaded.
**Migration note**: if the Phase 85 JSONB stub needs extension, any DDL here stays pure-additive + down migration + post-merge verification (prod auto-apply).

### Phase 87: #338 All-N-Keys-Complete Publish Gate
**Goal**: A composite strategy can never reach `published` while any member key has silently failed.
**Depends on**: Phase 86 (composite derive path exists to gate)
**Requirements**: PUB-01, PUB-02
**Success Criteria** (what must be TRUE):
  1. `finalize_wizard_strategy` (and any status-advancing path) advances a composite's verification to `published` ONLY when every member key reaches terminal-success.
  2. A permanent per-key failure observably degrades the composite to `complete_with_warnings`/`failed` — never a silently-holed `published` track.
  3. A failed member key is re-onboarded on a fresh ledger path, never retried in place (mig-038 "any failed_final→failed" retry-poisoning hazard); the re-onboard path is documented.
  4. The existing single-key verification flow is unchanged (regression green).
**Plans**: 3 plans

Plans:
- [x] 87-01-PLAN.md — [TDD] PUB-02/F-3: four-part supersession SQL test → surgical mig-038 re-base (branch (b) per-(strategy,kind) created_at supersession; branches (a)/(c) byte-identical) + byte-identical-restore down (mig 20260710150000). Applied+reconciled+GREEN on TEST (Parts 1-4 incl. cross-kind SAFETY). Commits 03fb00d1/dbdd69d0.
- [x] 87-02-PLAN.md — [TDD] M-3: publish-integrity SQL test → fail-loud BEFORE DELETE SECDEF guard on api_keys scoped to published-composite membership + GDPR sanitize exemption + drop-style down (mig 20260710160000). Applied+reconciled+GREEN on TEST (Parts 1-5 incl. sanitize-exempt). Commits 44ed1d1f/e1f71ffc/54d9bfb4.
- [x] 87-03-PLAN.md — PUB-01 explicit pinning: worker member-failure fault-injection (pytest 21) + route composite gate pins (vitest 25: failed→400 / complete→published / not-done→409) + OQ-1 defense-in-depth pure read + W-1 sole-writer source-scan guard (5/5, scans src+functions+migrations). Commits 38af9b8a/a3fe40a5/849fd9b9/df3db7f4/827a2907.
**Research**: `--research-phase` REQUIRED at plan time (verification state machine + mig-038 hazard — DEEP RESEARCH flag per research/SUMMARY.md). Safety-critical; MUST precede the wizard so the GUI can never create ungated composites.
**Planner note (2026-07-10)**: Research verdict — PUB-01's dangerous direction is ALREADY fail-closed (worker fails whole stitch job loud-permanent → failed_final → bridge → gate blocks; sole `published` writer is admin/strategy-review); the phase deliverables are (a) make it explicit+test-pinned, (b) close the F-3 bridge poison so the fail-closed signal is accurate, (c) close M-3. All 4 VALIDATION.md open questions resolved in the plans (OQ-1 include, OQ-2 created_at, OQ-3 include, OQ-4 SC-4=success-path). ⚠️ Both migrations AUTO-APPLY TO PROD on merge — request migration-reviewer at review time.

### Phase 88: Wizard Multi-Key Onboarding UX
**Goal**: A user onboards N keys — each with an active window and sequence — to one strategy through the wizard, with loud client-side validation.
**Depends on**: Phase 87 (publish gate live before the GUI can create composites)
**Requirements**: ONB-01, ONB-02, ONB-03
**Success Criteria** (what must be TRUE):
  1. In `ConnectKeyStep`, a user can repeatably add keys (`keys: KeyWindow[]`), each with an assigned window + sequence, entering their own credentials — which are never logged or echoed.
  2. Client-side zod `superRefine` validation fails loud on overlaps, non-monotone sequence, `end < start`, and future windows — sharing the ONE helper convention with the Phase 86 backend overlap guard (no divergent derivations).
  3. Adding a second/third key does NOT return the existing draft: the `wizard_session_id` idempotency fence is re-scoped per `(session, api_key)` (read `create-with-key/route.ts:130` before planning).
  4. Single-key onboarding through the same wizard is unchanged.
**Plans**: 5 plans

Plans:
- [x] 88-01-PLAN.md — [Wave 1] Shared windowsOverlap predicate + cross-package drift-guard + keyWindowsSchema 4-rule superRefine (ONB-02, TDD)
- [x] 88-02-PLAN.md — [Wave 1] Migration 20260710180000_wizard_composite: add_wizard_composite_key + set_wizard_composite_members SECDEF RPCs + down + 2 LIVE SQL tests (ONB-03; L-4 dissolved via wholesale write, no DEFERRABLE)
- [x] 88-03-PLAN.md — [Wave 1] finalize-wizard composite-first hoist ahead of isUnifiedBackboneActive (LOCKED Option A) + O-1 per-member scope-broadening probe loop + W-4 fail-closed (TDD)
- [x] 88-04-PLAN.md — [Wave 2] composite/add-key + composite/set-members routes mirroring create-with-key (validate→encrypt→RPC, uniform {code}; ONB-01/03, TDD)
- [x] 88-05-PLAN.md — [Wave 3] MultiKeyConnectStep per approved UI-SPEC (State A/B, A1 snapshot pin, A4 buildEnvelope summary) + WizardClient wiring + neutrality regression gates (ONB-01)
**UI hint**: yes
**Migration note**: 88-02 DDL AUTO-APPLIES TO PROD on merge — orchestrator must apply+reconcile file timestamp on TEST + run both SQL tests LIVE. Deploy note: USE_COMPUTE_JOBS_QUEUE=true required in prod at ship time (non-blocking flag).

### Phase 89: Composite Preview
**Goal**: Before submit, the user sees exactly what the composite will look like — and cannot submit a broken one.
**Depends on**: Phase 88 (multi-key wizard state exists to preview)
**Requirements**: PREV-01
**Success Criteria** (what must be TRUE):
  1. `SyncPreviewStep` shows per-key first/last day, the stitched combined track, per-key attribution, and a coverage mini-gantt of the key windows (v1.5 gantt pattern reused, never rebuilt).
  2. Pre-submit warnings surface (gaps, `data_quality_flags`); submit is blocked while any member key is non-success.
**Plans**: 4 plans

Plans:
- [x] 89-01-PLAN.md — [Wave 1, TDD] Pure compositeAttribution helper (partitionAttribution Σ-invariant + attributionBasisFromConfig mirroring the server cumulative_method branch — FLAG-1 resolved: arithmetic only on literal 'simple', else geometric)
- [x] 89-02-PLAN.md — [Wave 1, TDD] /api/keys/sync composite-first kickoff (strategy_keys probe → stitch_composite enqueue BEFORE isUnifiedBackboneActive, fail-closed W-4 mirror) — plan-time reachability discovery: preview mounts before finalize, so the existing route would enqueue sync_trades/unified-resync for a composite and the stitched row PREV-01 previews would never exist
- [x] 89-03-PLAN.md — [Wave 1, TDD] SyncPreviewStep composite branch: membership-probe discriminator (Pitfall 1), composite heavy read (mask + members + full series + denominator config), additive snapshot, waiting/gate_failed/passed-shell renders (combined track, member-naming failed gate, CTA relabels, FLAG-3 badge omission) + sibling render pins; frozen single-key pins pass UNMODIFIED (SC-4)
- [x] 89-04-PLAN.md — [Wave 2, TDD] Passed-render completion: per-key attribution table (signed contribution via 89-01, basis-honest caption), reused CoverageTimeline mini-gantt (rows by seq, declared bars + actual-span band), neutral gap warnings from server gap_spans + amber DQ caveats; full-suite + coverage phase gate
**UI hint**: yes

### Phase 90: Factsheet Markers + Basis Toggle
**Goal**: The composite factsheet renders the honest stitched track — gaps marked, boundaries visible, and an MTM toggle that is honestly gated rather than naively enabled.
**Depends on**: Phase 89 (composites onboardable end-to-end)
**Requirements**: FS-01, FS-02, FS-03
**Success Criteria** (what must be TRUE):
  1. The composite factsheet renders the stitched track through the real `FactsheetBody` (parity by construction, never a fork), with per-key boundary markers (`segmentBoundaries` SVG overlay on the bespoke `TimeSeriesChart`).
  2. Missing-data / gap / pre-rollout segments render as explicit markers (`missingSegments: {start,end,kind}[]` — never a flat zero-fill line) and are excluded from risk-metric compounding.
  3. The `cash_settlement ↔ mark_to_market` `SegmentedControl` toggle re-labels every dependent KPI on switch, reading `metrics_json_by_basis`; MTM is enabled ONLY where the gating predicate passes (perp-only/USD-native or Phase-83-smoothed; predicate: stitched ledger contains `options_settlement_summary` rows and lacks Phase-83 daily marks → disabled — read `deribit_txn.py:629-695`), rendered disabled-with-reason on un-smoothed options books incl. Zavara.
  4. Default + headline + peer/ranking stats stay `cash_settlement` in every state of the toggle.
**Plans**: 5 plans

Plans:
- [x] 90-01-PLAN.md — Wave-0 RED scaffolds: arithmetic curve (D3) + sparse-invariance + csv-arm pins (W1)
- [x] 90-02-PLAN.md — Wave-0 RED scaffolds: markers + basis-gate tests + D7 FROZEN_ISLANDS carve-out (W1)
- [x] 90-03-PLAN.md — Read-path fix: composite branch, sparse csv_daily_returns, arithmetic swap, cash overlay, cache v4 (W2)
- [ ] 90-04-PLAN.md — FS-01/FS-02 segment-marker SVG overlay in TimeSeriesChart + segmentMarkers config flag (W3)
- [ ] 90-05-PLAN.md — FS-03 BasisContext + SegmentedControl disabledReason + ControlBar toggle + KpiStrip-only relabel (W3)
**UI hint**: yes
**Migration note**: any additive JSONB/flag DDL here follows the same pure-additive + down migration + post-merge verification discipline (prod auto-apply).

### Phase 90.5: Dynamic Per-Strategy Leverage (Factsheet + Scenario-Planner)
**Goal**: A user can dynamically change per-strategy leverage on the factsheet and see every KPI re-derive live — matching the leverage control the scenario-planner already ships.
**Depends on**: Phase 90 (builds on the finalized composite `FactsheetBody`/`compute.ts` render path)
**Requirements**: LEV-01, LEV-02
**Scope note (inserted 2026-07-10 via /gsd-phase --insert; frontend-only)**: The scenario-planner ALREADY implements per-strategy leverage (internal "R4"): numeric input + live re-derive + engine math `r → L·r` at `src/lib/scenario.ts:429`, leverage-invariant Sharpe/Sortino/correlation, caveat UI at `ScenarioComposer.tsx:3808`. Its live overlay is DONE and out of scope except reuse-as-pattern. TWO pieces of NEW work: (a) **factsheet** [LEV-01] — today only a display-only `leverage_range` text chip, no scaling → ephemeral live control (NO schema/recompute/persistence); (b) **scenario-planner Save persistence** [LEV-02, user decision 2026-07-10, carve-out from the ephemeral lock] — persist the per-strategy leverage into a saved scenario's `draft` on Save/Update. The factsheet stays ephemeral; ONLY the scenario-planner explicit-Save path persists.
**Success Criteria** (what must be TRUE):
  1. The factsheet exposes a per-strategy leverage input (reusing the `ScenarioComposer` controlled-number + clamp pattern: `useState<Record<string,number>>`, `handleLeverageChange` clamp to `[0, MAX_LEVERAGE]`, fail-loud on non-finite) that rescales the served `strategy_analytics.daily_returns` by L **client-side** and re-runs the SAME `src/lib/factsheet/compute.ts` KPI compute (never a forked metrics path).
  2. Leverage is modeled as cost-free daily-return scaling `r → L·r` (borrow/funding/liquidation explicitly EXCLUDED), with the same caveat copy the scenario-planner shows; annualization (√365 crypto / √252 traditional) is unchanged by L and risk-adjusted KPIs (Sharpe/Sortino) stay leverage-invariant, while cumulative/CAGR/maxDD scale correctly.
  3. **Factsheet** leverage is EPHEMERAL: no persistence to DB or draft, no `metrics_json`/schema write — resetting/reloading returns to L=1. The default state (L=1 for every strategy) renders byte-identical to today's factsheet (neutrality pin).
  4. `returns_denominator_config` (allocated-capital denominator) is NOT entangled with the leverage scalar — they remain orthogonal.
  5. **[LEV-02]** Saving a scenario persists per-strategy leverage into the saved-scenario `draft`, and loading that scenario restores it: `leverageByRef` (`ScenarioComposer.tsx:849`) ⇄ a NEW **optional** `leverageOverrides: Record<string,number>` on `scenarioDraftSchema` (`scenario-state.ts`), folded in at POST/PUT (`:1657/:1697`, twin of `setMemberKeyIds`) and rehydrated on load. Optional field ⇒ **no `schema_version` bump / no upgrade-codec branch**; a pre-existing draft without it reads `{}`. **No DB migration** (`scenarios.draft` is jsonb). Commit/mandate-diff semantics untouched — save/load only. Round-trip + optional-field-absent backward-compat tests pass.
**Plans**: 5 plans
**UI hint**: yes
**Migration note**: none — frontend-only, no DDL (LEV-02 persists via the existing `scenarios.draft` jsonb, optional schema field, no `schema_version` bump).

Plans:
- [ ] 90.5-01-PLAN.md — Shared leverage contract `src/lib/leverage.ts` (MAX_LEVERAGE + sanitizeLeverage + sanitizeLeverageMap, TDD; wave 1)
- [ ] 90.5-02-PLAN.md — LEV-02 schema seam: optional `leverageOverrides` + `setLeverageOverrides` on scenario-state (TDD, no refine/no version bump; wave 1)
- [ ] 90.5-03-PLAN.md — LEV-01 payload `periodsPerYear` + cache bump v5 + `leverage-context.tsx` (L=1 identity / L≠1 recompute; wave 2)
- [ ] 90.5-04-PLAN.md — LEV-02 composer wiring: POST/PUT fold + rehydrate-replace (session-bleed fix) + route pins (wave 2)
- [ ] 90.5-05-PLAN.md — LEV-01 FactsheetView control + MODELED eyebrow + caveat + GUARD-04 extension (wave 3)

### Phase 91: Frontend Test & QA
**Goal**: The multi-key composite flow is proven end-to-end — including the milestone acceptance fixture and the #338 fault path — and locked in as blocking CI.
**Depends on**: Phase 90 + Phase 90.5 (full surface incl. factsheet leverage exists to test)
**Requirements**: QA-01, QA-02, QA-03
**Success Criteria** (what must be TRUE):
  1. Vitest unit/component coverage lands on the new wizard step + factsheet components; the blocking CI coverage gate (lines 82 / stmts 80 / fns 74 / branches 72) stays green.
  2. A seed-gated Playwright e2e proves multi-key onboarding end-to-end — wired in BOTH `HAS_SEED_ENV` AND the `ci.yml` list (v1.2 lesson).
  3. **Milestone acceptance**: the GUI-onboarded 3-key Zavara composite reproduces cum ~62.66% / maxDD ~−4.13% on `cash_settlement` through the production service.
  4. A #338 fault-injection test forces one of N keys to permanent-fail and proves the composite is NOT published.
  5. axe scans on wizard + factsheet pass (serious+critical filter), DESIGN.md conformance holds, and a `/qa` pass is clean. Any credential-redaction fixtures get the gitleaks allowlist in the same batch.
**Plans**: 6 plans
**UI hint**: yes

Plans:
- [x] 91-01-PLAN.md — QA-01 coverage gap-fill (windowOverlap.test.ts + basis-context.test.tsx)
- [x] 91-02-PLAN.md — seedCompositeStrategy() helper (Zavara-shaped + failed variant, one fixture source)
- [x] 91-03-PLAN.md — Multi-key onboarding e2e + #338 failed-member GUI e2e (seed-gated)
- [x] 91-04-PLAN.md — Zavara factsheet-render acceptance e2e + composite-factsheet/leverage axe scans
- [x] 91-05-PLAN.md — wizard-axe multi-key cases + ci.yml MA-8 two-place wiring + full regression gate
- [x] 91-06-PLAN.md — Credit ledger + SC-3 piece-3 human_verification OPEN record + orchestrator /qa pass

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 85. Thin Data Model | 1/1 | Complete   | 2026-07-10 |
| 86. Production Stitch Endpoint | 4/4 | Complete   | 2026-07-10 |
| 87. #338 Publish Gate | 3/3 | Complete   | 2026-07-10 |
| 88. Wizard Multi-Key UX | 5/5 | Complete   | 2026-07-10 |
| 89. Composite Preview | 4/4 | Complete   | 2026-07-10 |
| 90. Factsheet Markers + Toggle | 5/5 | Complete   | 2026-07-11 |
| 90.5. Dynamic Per-Strategy Leverage | 5/5 | Complete   | 2026-07-11 |
| 91. Test & QA | 6/6 | ✅ Complete (7/8; SC-3 pc3 OPEN user gate) | 2026-07-11 |

## Traceability (16/16 v1 requirements mapped + LEV-01 inserted 2026-07-10 — no orphans, no duplicates)

| Requirement | Phase | Status |
|-------------|-------|--------|
| COMP-01 | Phase 85 | ✅ Satisfied |
| COMP-02 | Phase 86 | ✅ Satisfied |
| COMP-03 | Phase 86 | ✅ Satisfied |
| COMP-04 | Phase 86 (schema stub delivered in Phase 85) | ✅ Satisfied |
| PUB-01 | Phase 87 | ✅ Satisfied |
| PUB-02 | Phase 87 | ✅ Satisfied |
| ONB-01 | Phase 88 | ✅ Satisfied |
| ONB-02 | Phase 88 | ✅ Satisfied |
| ONB-03 | Phase 88 | ✅ Satisfied |
| PREV-01 | Phase 89 | ✅ Satisfied |
| FS-01 | Phase 90 | ✅ Satisfied |
| FS-02 | Phase 90 | ✅ Satisfied |
| FS-03 | Phase 90 | ✅ Satisfied |
| LEV-01 | Phase 90.5 | ✅ Satisfied |
| LEV-02 | Phase 90.5 | ✅ Satisfied |
| QA-01 | Phase 91 | ✅ Satisfied |
| QA-02 | Phase 91 | ⚠️ Partial — SC-3 piece 3 (live-GUI Zavara) OPEN, user gate |
| QA-03 | Phase 91 | ✅ Satisfied |

---

*v1.9 phases populated by `/gsd-new-milestone` (roadmapper, 2026-07-10). Ledger reconciled 2026-07-10.*
