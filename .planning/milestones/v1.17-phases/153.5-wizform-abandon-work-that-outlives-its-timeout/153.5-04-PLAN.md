---
phase: 153.5-wizform-abandon
plan: 04
type: execute
wave: 3
depends_on: [153.5-02, 153.5-03]
files_modified:
  - analytics-service/routers/exchange.py
  - analytics-service/services/ingestion/mt5.py
  - analytics-service/services/job_worker.py
  - analytics-service/services/allocator_positions.py
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_mt5_validate.py
  - analytics-service/tests/test_ingestion_mt5.py
  - analytics-service/tests/test_mt5_derive_branch.py
  - analytics-service/tests/test_allocator_positions_non_ccxt.py
  - analytics-service/tests/test_process_key.py
autonomous: true
requirements: [ABANDON-05, ABANDON-06, ABANDON-07]
must_haves:
  truths:
    - "An Mt5SessionAbandoned escaping the router probe answers the EXISTING transient disposition (424 NETWORK_UNAVAILABLE, recoverable, trace.outcome=transient) — never a bodyless 500 whose R-1 meaning is 'service-permanent, do not retry' (D-40)"
    - "The derive job routes it to a transient DispatchResult with NO strategy-analytics failure stamp; the holdings job to AllocatorHoldingsSyncTransientError — both retried, never user-blamed"
    - "The two process_key adapter.validate call sites (:774, :1586) can no longer 500 the wizard on an escaping Mt5SessionAbandoned — each answers its site's existing transient envelope, minting NO new user-facing code (153.1 owns the code table)"
    - "No arm anywhere classifies the refusal as a user fault — no auth/wrong_server stamp, no venue-blame copy"
  artifacts:
    - path: "analytics-service/routers/exchange.py"
      provides: "except Mt5SessionAbandoned arm beside the probe timeout arm; re-cut Pitfall-6 docblock"
      contains: "Mt5SessionAbandoned"
    - path: "analytics-service/routers/process_key.py"
      provides: "enclosing try at both adapter.validate sites"
      contains: "Mt5SessionAbandoned"
  key_links:
    - from: "analytics-service/routers/exchange.py"
      to: "services.mt5_client.Mt5SessionAbandoned"
      via: "import added to the existing from services.mt5_client import block (:24)"
      pattern: "Mt5SessionAbandoned"
---

<objective>
D-40: "zero call-site edits" describes the FENCE, not the CLASSIFICATION. A plain
`Exception` falls through every existing arm; on the router that is an unhandled
bodyless 500 (= R-1 "service-permanent, do not retry" — the opposite of the truth), and
at `routers/process_key.py:774`/`:1586` there is no enclosing try at all. Add the small
`except Mt5SessionAbandoned` arms routing each site to its EXISTING transient
disposition, plus the process_key wrapping. Re-cut the exchange.py Pitfall-6 docblock
(#6a's "releases nothing" note) that plan 02's construction fence partially supersedes.

⚠️ Perspective for the executor: on the genuinely-abandoned path nobody is awaiting, so
these arms never run — asyncio discards the zombie's raise (D-39). The arms exist for
the FALSE-POSITIVE path: a legitimate caller that trips the fence must be told
"transient, retry", never "your key/server is wrong" and never "permanent". Say this in
each arm's comment.

Purpose: an operator fault must never be dressed as a user fault or a permanent one.
Output: five call-surface arms, per-site tests, one docblock re-cut.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-CONTEXT.md

Targeted reads (one pass each):
- `analytics-service/routers/exchange.py` — imports :24-40; the probe `wait_for` + its arms :707-770 (the `except asyncio.TimeoutError` transient arm at :711-727 is the template to mirror byte-for-byte); the Pitfall-6 `finally` block :899-948 (re-cut target); ⛔ do NOT add an arm to the connect-stage try :513-560 — plan 02 proved the live path cannot raise there, and the zombie path is unreachable by any arm.
- `analytics-service/services/ingestion/mt5.py` — the probe `wait_for` arms around :266-300 and the adapter's documented propagate-transient disposition (~:269-275).
- `analytics-service/services/job_worker.py` — the derive read arms :3578-3707 (TimeoutError → transient DispatchResult is the template; note which arms stamp `_stamp_strategy_analytics_failed` — the new arm must NOT).
- `analytics-service/services/allocator_positions.py` — the read arms :657-695 (every existing arm raises `AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE)` — mirror exactly).
- `analytics-service/routers/process_key.py` — `:740-800` (`_run_validate_only`, the `val = await adapter.validate(submission)` at :774) and `:1560-1620` (the second site at :1586); read enough surrounding code to identify each site's existing transient refusal shape (how a `Mt5TerminalBusyError` or adapter-raised transient surfaces today — if none is handled, mirror the nearest service-unavailable envelope this router already emits; ⛔ mint no new code).
- Test analogs: `tests/test_mt5_validate.py` (router probe fault-injection cases), `tests/test_ingestion_mt5.py`, `tests/test_mt5_derive_branch.py`, `tests/test_allocator_positions_non_ccxt.py`, `tests/test_process_key.py` (find its validate-only cases via grep for `validate-and-encrypt` / `_run_validate_only`).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The four service-layer arms + exchange Pitfall-6 re-cut</name>
  <files>analytics-service/routers/exchange.py, analytics-service/services/ingestion/mt5.py, analytics-service/services/job_worker.py, analytics-service/services/allocator_positions.py, analytics-service/tests/test_mt5_validate.py, analytics-service/tests/test_ingestion_mt5.py, analytics-service/tests/test_mt5_derive_branch.py, analytics-service/tests/test_allocator_positions_non_ccxt.py</files>
  <behavior>
    - Router: monkeypatch the installed client so a probe method raises Mt5SessionAbandoned → response is 424 with code NETWORK_UNAVAILABLE and recoverable true, and the trace records outcome "transient" — asserted against the response body, not the arm's source.
    - Adapter: Mt5SessionAbandoned raised inside Mt5Adapter.validate's probe propagates OUT of validate unchanged (type-identity assert) — the adapter's documented transient disposition.
    - Derive: the read raising Mt5SessionAbandoned yields a FAILED DispatchResult with transient error_kind, and the strategy-analytics failure stamp helper was NOT invoked (spy on it).
    - Holdings: the read raising Mt5SessionAbandoned surfaces as AllocatorHoldingsSyncTransientError with MT5_UNREACHABLE_NOTE as its str (the user-copy contract), chained from the original.
    - Negative control (one site suffices, router): with the new arm temporarily absent the same injection produces the fall-through (unhandled) — observe once during development, do not keep.
  </behavior>
  <action>
1. `routers/exchange.py`: import `Mt5SessionAbandoned` in the :24 block. Add
   `except Mt5SessionAbandoned:` immediately after the probe's
   `except asyncio.TimeoutError` arm (:711-727), byte-mirroring its disposition:
   WARNING log (not exception), `trace.outcome = "transient"`,
   `raise VenueTransientHTTPException(status_code=424, code="NETWORK_UNAVAILABLE",
   detail=NETWORK_ERROR_DETAIL, recoverable=True)`. Comment: false-positive path only;
   nobody awaits a true zombie (D-39/D-40). ⛔ Arm placement must precede any broader
   `except Exception` in the same try. ⛔ Do not touch the connect-stage try.
2. `services/ingestion/mt5.py`: add an explicit `except Mt5SessionAbandoned: raise` arm
   ahead of the probe's other arms, with the comment that propagation IS this adapter's
   documented transient disposition and the arm exists so a future broad except cannot
   silently absorb it. (Zero behavior change today — the test pins the propagation.)
3. `services/job_worker.py` derive arms (:3578-3707): add the arm returning a
   `DispatchResult` shaped like the TimeoutError arm's (FAILED, transient error kind,
   message naming the abandonment WITHOUT any `_WRONG_SERVER_TOKENS`/`_AUTH_TOKENS`
   member — this text lands in `compute_jobs.error_message`), and ⛔ NO
   `_stamp_strategy_analytics_failed`. ⛔ Do NOT call `_mt5_bounded_restart` from this
   arm — a fence refusal means the terminal belongs to someone else; restarting it
   would be doing to them exactly what this phase stops.
4. `services/allocator_positions.py` (:657-695): add the arm raising
   `AllocatorHoldingsSyncTransientError(MT5_UNREACHABLE_NOTE) from exc`, mirroring its
   siblings.
5. Re-cut `routers/exchange.py:899-948` (Pitfall-6 finally docblock): the #6a note that
   a connect-stage timeout leaves `client is None` so "the finally releases nothing"
   stays TRUE — append that the orphaned construction now self-disposes at the sink
   (plan 02's fence), so "releases nothing" is no longer a leak, and cite
   WIZFORM-ABANDON.
6. Add the behavior tests to the four test files listed. Import the exception from
   `services.mt5_client` (owner), never a re-export.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_mt5_validate.py tests/test_ingestion_mt5.py tests/test_mt5_derive_branch.py tests/test_allocator_positions_non_ccxt.py -q && make typecheck</automated>
  </verify>
  <done>All four dispositions proven at the response/result level; no user-blame stamp anywhere; no restart from the refusal arm; Pitfall-6 record matches post-fence reality; mypy clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: process_key's two uncovered adapter.validate sites</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_process_key.py</files>
  <behavior>
    - Site :774 (`_run_validate_only`, the wizard's validate-and-encrypt leg): with the adapter monkeypatched to raise Mt5SessionAbandoned, the route answers this router's existing transient/service-unavailable envelope (status + code asserted from the response), NOT a bodyless 500 — and the envelope carries no auth/wrong-server framing.
    - Site :1586: same injection, same contract, driven through that site's route.
    - Both tests assert the response BODY shape (code present, retry semantics transient), not merely the status integer.
  </behavior>
  <action>
Wrap each `val = await adapter.validate(submission)` (:774 and :1586 at HEAD — re-locate
by the symbol if the hints have rotted) in a try/except with a single
`except Mt5SessionAbandoned:` arm. Disposition: FIRST look for how this router already
answers an adapter-raised transient (grep the file for `Mt5TerminalBusyError`,
`SERVICE_UNREACHABLE`, or the nearest transient `service_error`/envelope in the same
function); route to that existing shape with retryable/transient semantics. ⛔ Mint no
new user-facing code (153.1 owns the table); ⛔ 500-retryable:false and any 4xx blaming
credentials/server are both forbidden dispositions for this arm. If the two sites'
existing shapes differ, mirror each site's own — do not unify them in this phase.
Comment each arm: operator-side refusal, transient by construction, false-positive path
only (D-40).

Add the two route-level tests to `tests/test_process_key.py`, following that module's
existing fixture/DI idioms for stubbing the adapter registry (read its existing
validate-only cases first and mirror their setup — do not invent a new harness).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/pytest tests/test_process_key.py -q && .venv/bin/pytest tests/test_process_key_200_discriminator.py tests/test_process_key_onboard_contract.py -q && make typecheck</automated>
  </verify>
  <done>Both sites answer a coded transient envelope under injection; sibling process_key contract suites stay green; no new code minted; mypy clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fence refusal → HTTP contract | an operator fault crossing into a user-facing verdict |
| fence refusal → job error columns | raw text landing in operator-visible `error_message` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.5-12 | Repudiation | router/process_key 500 fall-through | mitigate | explicit transient arms at all uncovered sites (this plan) |
| T-153.5-13 | Info. disclosure | derive error_message text | mitigate | fixed token-free message; class name only in telemetry |
| T-153.5-14 | DoS | refusal arm restarting someone else's terminal | mitigate | explicit ⛔ no `_mt5_bounded_restart` in the new derive arm + test spies |
</threat_model>

<verification>
cd analytics-service && .venv/bin/pytest tests/test_mt5_validate.py tests/test_ingestion_mt5.py tests/test_mt5_derive_branch.py tests/test_allocator_positions_non_ccxt.py tests/test_process_key.py tests/test_long_fetch.py -q && make typecheck
</verification>

<success_criteria>
- Every surface that can see the refusal routes it to its existing transient disposition; zero new user-facing codes; zero user-blame paths.
- process_key's wizard leg can no longer 500 on it.
- All touched suites green; mypy clean.
</success_criteria>

<output>
Create `.planning/phases/153.5-wizform-abandon-work-that-outlives-its-timeout/153.5-04-SUMMARY.md` when done.
</output>
