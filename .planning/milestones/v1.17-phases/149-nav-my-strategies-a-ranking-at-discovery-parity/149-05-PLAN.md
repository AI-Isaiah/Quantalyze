---
phase: 149-nav-my-strategies-a-ranking-at-discovery-parity
plan: 05
type: execute
wave: 4
depends_on: ["149-01", "149-02", "149-03", "149-04"]
files_modified:
  - src/__tests__/phase-149-my-strategies-parity.test.ts
  - TODOS.md
  - .planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-VALIDATION.md
autonomous: true
requirements: [NAV-01]
tags: [structural-gate, mutation-testing, ci-invariant]

must_haves:
  truths:
    - "A structural CI gate pins: the published-only default, prop-absence on both public pages, exactly one widening consumer, the server predicates (incl. the TWO-branch withPublishedOnly COUNT in getPercentiles and the archived exclusion), one StrategyTable export, the unchanged getPercentiles signature, and the SINGLE percentile scoring core used by both callers"
    - "At least 4 mutations at independent production sites were observed RED and reverted by re-editing (never git checkout --)"
    - "The falsifiability ledger in 149-VALIDATION.md carries Observed ✅ rows with pasted failure lines"
    - "The full phase gate (test:coverage + typecheck + lint + 148 regression pair) is green"
  artifacts:
    - path: "src/__tests__/phase-149-my-strategies-parity.test.ts"
      provides: "two-layer source-scan gate cloned from phase-148, with Rule-9 NON-VACUITY ledger header"
      min_lines: 150
    - path: "TODOS.md"
      provides: "three deferred-item entries (wizard preselect, duplicate h1, grid rowLinkMode)"
  key_links:
    - from: "src/__tests__/phase-149-my-strategies-parity.test.ts"
      to: "src/components/strategy/StrategyTable.tsx"
      via: "literal pin on the destructuring default"
      pattern: "visibility = \\\\\"published-only\\\\\""
---

<objective>
Clone the 148 gate architecture (binding ruling) to make the phase's structural claims ASSERTED,
not observed — pinning (a) no second ranking implementation, (b) StrategyTable's published
default + discovery/browse call sites unchanged, (c) getPercentiles signature unchanged with BOTH
withPublishedOnly branches intact (an occurrence COUNT, not a presence check — checker B-3),
(d) ONE percentile scoring core called by both getPercentiles and getOwnRowPercentiles (the
founder scorer ruling — no second formula) — then run the Rule-9 mutation campaign, file the
deferred items in TODOS.md (the ONE backlog ground truth), complete the 149-VALIDATION.md ledger,
and pay the full phase gate.

Checker W-3 ruling (binding — record in SUMMARY): there is NO checkpoint task for the founder
proof case. The SUMMARY must state explicitly that SC-1's founder proof case (8 keys →
4 ranked+scored rows → 2 placeholders, per the archived ruling) is discharged by post-merge PROD
UAT — it is listed in 149-VALIDATION.md's Manual-Only table, not verified in-phase.

Purpose: SC-3 verbatim ("asserted not merely observed") + the roadmap trap ("provably unchanged
public predicate") + Nyquist falsifiability.
Output: the CI gate, observed-RED evidence, TODOS entries, completed VALIDATION ledger.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-PATTERNS.md (§9 — the clone map: header shape, fail-loud reader, stripComments, productionSources walk, extractors, assertion voice, anti-vacuity, self-pin idiom)
@.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-VALIDATION.md
@src/__tests__/phase-148-owner-lane-cache-isolation.test.ts
@src/__tests__/phase-147-series-resolution-guards.test.ts (:207-227 anti-vacuity form)
@TODOS.md

<interfaces>
Pins the gate asserts (comment-stripped source; one it() per pin so a failure names the offender;
a missing pinned file is a FAILURE, not a skip):

Layer B (per-file literals):
1. `StrategyTable.tsx` contains `visibility = "published-only"` (the destructuring default IS the public invariant) AND contains `strategies.filter((s) => s.status === "published")` (the published arm survived — parameterized, never deleted).
2. `discovery/[slug]/page.tsx` and `browse/[slug]/page.tsx` each contain NO `visibility=`, NO `placeholderKeys`, NO `onFinishSetup` token (absence-as-assertion — what "provably unchanged" means).
3. `queries.ts` `getStrategiesByCategory` body (functionBody extractor) still contains `withPublishedOnly(` (presence check — count 1, sound as-is; checker B-3 verified).
4. `getMyStrategies` body contains `.eq("user_id"` AND `.neq("status", "archived")` (W-4: archived rows excluded from the ranked list) and NEITHER `withPublishedOrOwner` NOR `discovery_categories!inner`.
5. `getPercentiles` declarationHead contains `categorySlug?: string` (signature unchanged — CONTEXT lock, as re-read by the 2026-08-05 scorer ruling) and its COMMENT-STRIPPED body contains EXACTLY TWO `withPublishedOnly(` occurrences (checker B-3: both branches — the scoped AND the un-scoped; an occurrence COUNT, not a presence check, so deleting the un-scoped wrapper can no longer hide behind the scoped one) and NOT `user_id` (predicate unchanged: own drafts structurally cannot enter the map).
6. `my-strategies/page.tsx` calls `getOwnRowPercentiles(` (the own-scorer wire — drafts get Pnn) AND its COMMENT-STRIPPED source contains NO `getPercentiles(` token (I-2 single-fetch ruling: the published-universe query runs ONCE, inside the helper; getPercentiles remains the discovery-surface caller of the same core — pin 9's "both callers" property is unchanged).
7. `StrategyTable.tsx` contains the `effectiveViewMode` guard expression `visibility === "owner-all-statuses" ? "table" : viewMode` (grid unreachable on the owner surface) and the `s.status === "published"` gate adjacent to `SimulateImpactButton`.
8. `getStrategylessActiveKeys` body contains BOTH `.eq("owner_id", userId)` (the strategy_keys read) and `isPerKeyDailiesEligibleKey` (census-correct anti-join, pinned so an api_key_id-only regression reddens); `deriveStrategylessKeys` body contains the literal `"archived"` (archived ≠ coverage — W-4).
9. Single scoring core (founder scorer ruling): `getPercentiles` body AND `getOwnRowPercentiles` body EACH contain `scoreAgainstPopulation(`; the COMMENT-STRIPPED `queries.ts` contains NO `100 - ` (the inversion arm lives ONLY in percentile-core.ts — no second formula).

Layer A (repo walk over production sources, __tests__/node_modules/test-files excluded):
10. Exactly ONE production file contains `visibility="owner-all-statuses"` and its path contains `my-strategies` (no second widening consumer).
11. Exactly ONE production file matches `export function StrategyTable` (no second ranking implementation — SC-3 literal) AND exactly ONE production file matches `export function scoreAgainstPopulation` (the one core).
12. Anti-vacuity: the STRIPPED StrategyTable.tsx still contains `owner-all-statuses`, and the walk's offender-extractor DOES find the my-strategies file (empty offender list elsewhere means clean, not blind — 147:207-227 form).

Rule-9 NON-VACUITY mutation campaign (INDEPENDENT production sites, second-member preference
per the ledger rule — mutate the site the author did NOT have in mind):
- M1: delete the `= "published-only"` default in StrategyTable's destructuring → pin 1 RED; record whether StrategyTable.visibility.test.tsx also reddens (the measured behavioral/structural asymmetry note, 148 precedent).
- M2 (second member): add `visibility="owner-all-statuses"` to the **browse** call site (not discovery — browse is the member nobody talks about) → pins 2+10 RED.
- M3: swap getMyStrategies' `.eq("user_id", userId)` for `withPublishedOrOwner(query, userId)` → pin 4 RED; record whether page.test.tsx SC-1b also reddens.
- M4: in getPercentiles' un-scoped branch, remove the `withPublishedOnly(` wrapper → pin 5 RED (count 2 → 1 — GENUINELY caught now; checker B-3: the old presence-check form was blind to this exact mutation because the scoped branch's occurrence kept it green).
- M5 (optional fifth, placeholder class): in deriveStrategylessKeys, drop the linked-keys union from the covered Set → queries.my-strategies.test.ts RED (the Alpha Centauri fixture) — already observed in plan 02 if run there; otherwise observe here.
- M6 (SC-5 second member — checker W-6): change `showViewToggle={visibility !== "owner-all-statuses"}` at the StrategyFilters mount to `showViewToggle={true}` → `StrategyTable.visibility.test.tsx` toggle-hide case RED (pin 7 does NOT see the toggle — this is the mutation that proves the behavioral spec carries that invariant).
- M7 (scorer core — checker B-4 ledger row): in percentile-core.ts, flip the inversion arm `100 - percentile` → `percentile` → BOTH callers' tests RED: `queries.percentiles.test.ts` (the :74-80 best=80/worst=0 pin) AND `percentile-core.test.ts` (inversion case).
- M8 (B-1 coercion — the re-derived SC-4 row): in StrategyTable's chip derivation, drop the `analyticsPresent` coercion (pass `s.analytics.computation_status ?? null` unconditionally) → `StrategyTable.pending-chip.test.tsx` never-enqueued-≥16h "No data" case RED (EMPTY_ANALYTICS's hardcoded "pending" resurrects the forever-Syncing defect).
- M9 (SC-5 member A — the precise site, checker I-1): in StrategyTable.tsx, replace the `effectiveViewMode` DERIVATION expression `const effectiveViewMode = visibility === "owner-all-statuses" ? "table" : viewMode;` with `const effectiveViewMode = viewMode;` → gate pin 7 RED; record that the behavioral toggle-hide case does NOT redden (the measured structural/behavioral asymmetry — M6 is the mutation the behavioral spec carries).
Every mutation: run → paste the failing assertion line into the gate header ledger + VALIDATION.md → revert by RE-EDITING the mutated line (never `git checkout --`) → `git diff --quiet` on the file exits 0 → suite green.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the phase-149 structural gate (clone of 148)</name>
  <files>src/__tests__/phase-149-my-strategies-parity.test.ts</files>
  <read_first>src/__tests__/phase-148-owner-lane-cache-isolation.test.ts (whole file — clone header shape, readSource fail-loud reader :151-163, stripComments :173-179, productionSources :182-196, extractors :202-295, assertion voice :307-315), src/__tests__/phase-147-series-resolution-guards.test.ts (:207-227), src/__tests__/phase-32-frozen-spine-guards.test.ts (:294-311 regex self-pin idiom)</read_first>
  <action>
    Create the gate implementing pins 1–12 from the interfaces block, one `it()` per pin, each
    body carrying the "what this forbids is not hypothetical" note in the 148 voice (e.g. pin 2:
    "the failure this forbids: one prop on a public page serves every user's drafts to anon
    through the shared component"; pin 5: "the failure this forbids: a category-scoped page keeps
    the scoped branch published-only while the global branch silently widens — a presence check
    cannot see it, only the count can"; pin 9: "the failure this forbids: a second inline
    percentile formula that drifts from the core — two rankings that disagree about the same
    number"). Copy the comment-stripping (load-bearing — StrategyTable's own prop docs name
    `owner-all-statuses`, and getPercentiles' doc comment contains `100 - raw_percentile`, which
    would self-invalidate an unstripped pin-9 scan), the fail-loud missing-file reader (Rule 12),
    and the anti-vacuity assertions. Header: WHY THIS EXISTS → WHY A STRUCTURAL LAYER (the
    behavioral specs pin render outcomes; only a source scan pins that public CALL SITES pass
    nothing) → WHAT IS PINNED (numbered list matching the it()s) → comment-hygiene note → an
    EMPTY `Rule-9 NON-VACUITY` ledger section to be filled by Task 2. Grep-gate hygiene: every
    count-based assertion strips comments first and self-pins its regex (phase-32 idiom) — no
    bare `grep -c`-equivalent over unstripped source.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-149-my-strategies-parity.test.ts --no-file-parallelism</automated>
  </verify>
  <done>Gate green on the fixed tree, 12 pins + anti-vacuity; header complete except the mutation ledger.</done>
</task>

<task type="auto">
  <name>Task 2: Mutation campaign + TODOS entries + VALIDATION ledger + phase gate</name>
  <files>src/__tests__/phase-149-my-strategies-parity.test.ts, TODOS.md, .planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-VALIDATION.md</files>
  <read_first>TODOS.md (append-only convention — the ONE backlog ground truth), 149-VALIDATION.md (Falsifiability Ledger + Sign-Off sections)</read_first>
  <action>
    1. Run mutations M1–M4 and M6–M9 (M5 if not observed in plan 02) per the interfaces block —
       every Falsifiability Ledger row still ⬜ gets its mutation run here: mutate →
       observe RED (paste the exact failing assertion) → revert by re-editing → verify
       `git diff --quiet -- <file>` exits 0 → re-run green. Record each in the gate header's
       Rule-9 ledger (mutation, observed output, revert method) and in 149-VALIDATION.md's
       Falsifiability Ledger (flip rows to `✅ Observed` with pasted evidence; a skipped mutation
       is recorded skipped, NEVER caught).
    2. Append to TODOS.md (three entries, Phase-149 provenance noted):
       (a) "Finish setup →" wizard key-preselect: ContributionWizardOverlay has no preselect
       seam; the link opens the wizard fresh — preselect = one optional prop threaded into
       WizardClient, founder-deferred 2026-08-05.
       (b) Duplicate `h1` "My Strategies": manager `/strategies` and allocator `/my-strategies`
       share the title — every future test/e2e selector must scope by route/href/data-testid,
       never bare h1 text (research Pitfall 10; benign at runtime).
       (c) StrategyGrid href dead-ends (`${basePath}/${categorySlug}/${id}` → withPublishedOnly →
       notFound) for any FUTURE owner-scoped grid consumer — 149 hid the toggle instead
       (founder ruling); a `rowLinkMode` prop is the fix if grid ever ships on an owner surface.
    3. Complete 149-VALIDATION.md: set frontmatter `nyquist_compliant: true`,
       `wave_0_complete: true`, status `executed`; fill the Sign-Off checklist honestly (any
       unmet box stays unchecked with a reason).
    4. Pay the phase gate and paste the tail outputs into the SUMMARY:
       `npm run test:coverage && npm run typecheck && npm run lint`, plus the 148 regression pair
       `npx vitest run "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" src/__tests__/phase-148-owner-lane-cache-isolation.test.ts --no-file-parallelism`, plus
       `npx playwright test e2e/discovery.spec.ts e2e/discovery-prefs-isolation.spec.ts` (the two
       public surfaces whose invariance this phase claims). ⚠️ Full gate is `test:coverage`, NOT
       bare `npm test` (blocking 82/80/74/72 thresholds). ⚠️ If a CI-only red appears later:
       Node-22-vs-25 skew, reproduce with `PATH=/opt/homebrew/opt/node@22/bin:$PATH`.
    5. In the SUMMARY (checker W-3 ruling — verbatim requirement): state that SC-1's founder
       proof case (8 active keys → 4 ranked+scored rows → 2 placeholders; archived rows excluded
       from the ranked list, archived-only-covered keys placeholder-eligible) is NOT discharged
       in-phase — it is discharged by post-merge PROD UAT per the 149-VALIDATION.md Manual-Only
       table. No checkpoint task exists for it, by ruling.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-149-my-strategies-parity.test.ts --no-file-parallelism && git diff --quiet -- src/components/strategy/StrategyTable.tsx src/lib/queries.ts src/lib/percentile-core.ts "src/app/browse/[slug]/page.tsx" && npm run typecheck</automated>
  </verify>
  <done>≥4 mutations observed RED with pasted evidence (incl. M4's count-2→1, M6's toggle case, M7's both-callers red, M8's forever-spinner resurrection, M9's pin-7-only asymmetry), all reverted (git diff clean on mutated files); gate ledger + VALIDATION ledger complete; TODOS carries the three entries; SUMMARY carries the W-3 discharge statement; full coverage gate + lint + 148 pair + public e2e green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → CI | the gate is the edit-time enforcement layer for every runtime mitigation in plans 01–04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-149-16 | Information Disclosure (regression class) | future edits widening public surfaces | mitigate | pins 1, 2, 10 make the widening a named CI failure (security_requirement b, asserted) |
| T-149-17 | Information Disclosure (regression class) | future predicate drift in queries | mitigate | pins 3, 4, 5 (occurrence COUNT), 8, 9 (security_requirements a + d, asserted; the un-scoped-branch mutation is now genuinely caught) |
| T-149-18 | Repudiation (vacuous verification) | a gate that cannot fail | mitigate | Rule-9 mutation campaign with observed RED + anti-vacuity assertions; second-member mutations on the browse call site and the showViewToggle wire |
| T-149-SC | Tampering | npm installs | accept | Zero packages installed this phase |
</threat_model>

<verification>
- Gate green; mutation evidence pasted in two artifacts; `git status` clean of mutation residue (`grep -rn MUTANT src/` → 0).
- `npm run test:coverage && npm run typecheck && npm run lint` green; 148 regression pair green; the two public e2e specs green.
</verification>

<success_criteria>
SC-3 holds in its own words: structural reuse ASSERTED — including the two-branch
withPublishedOnly count and the single scoring core. Every security invariant from plans 01–04
has an edit-time pin that was watched failing. VALIDATION ledger complete; deferred items filed
in the single backlog; the founder-proof discharge path stated in the SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/149-nav-my-strategies-a-ranking-at-discovery-parity/149-05-SUMMARY.md` when done. Record: mutation table (site → observed line → revert), phase-gate tails, TODOS entry texts, and the W-3 founder-proof discharge statement.
</output>
