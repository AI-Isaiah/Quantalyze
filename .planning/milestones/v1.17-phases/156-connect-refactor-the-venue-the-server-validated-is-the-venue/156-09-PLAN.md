---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 09
type: execute
wave: 7
landing: PR-B
depends_on: ["156-07"]
files_modified:
  - supabase/tests/test_wizard_session_idempotency.sql
  - supabase/tests/test_wizard_composite_fence.sql
  - supabase/tests/test_csv_finalize_double_submit.sql
  - supabase/tests/test_api_keys_venue_identity_uniq.sql
autonomous: true
requirements: [CONNECT-01, CONNECT-03]

must_haves:
  truths:
    - "Every pin asserting `authenticated` HAS EXECUTE is inverted, and each gains a service_role positive"
    - "Seven call sites drive an `authenticated` claim into the new gate; FIVE are re-shaped to service_role via THREE set_config edits (two in the fence file, one in the CSV file); Parts 3b and 3c deliberately retain `authenticated` because they are refusal assertions"
    - "test_wizard_composite_fence Part 3b asserts the NEW boundary instead of silently pinning a deleted control"
    - "Part 3c (minted in PR A by plan 03) is re-cut with Part 3b — a second vacuous green test is not left behind"
    - "add_wizard_composite_key gains the stale-re-base canary its twin already has, and both canaries assert auth.uid() is ABSENT"
    - "A connection role that cannot EXECUTE the RPCs fails loudly by name, instead of reddening every direct RPC call in the file for an unrelated reason"
  artifacts:
    - path: "supabase/tests/test_wizard_composite_fence.sql"
      provides: "inverted G2 pin, five re-shaped call sites (two claim edits), an honest Part 3b and Part 3c"
    - path: "supabase/tests/test_api_keys_venue_identity_uniq.sql"
      provides: "the composite twin of the stale-re-base canary, plus the auth.uid()-absent line on both"
  key_links:
    - from: "supabase/tests/test_wizard_composite_fence.sql"
      to: "add_wizard_composite_key"
      via: "service-role-shaped request.jwt.claims before each direct call"
      pattern: "service_role"
---

<objective>
Flip every remaining gate that Migration B falsifies, re-shape the **five** direct RPC call sites
that would otherwise go red for a reason unrelated to this change, and replace the **two**
assertions that are about to become **vacuous green tests pinning a deleted control**.

Purpose: this is the phase's largest mechanical breakage surface, and one part of it is not
mechanical at all. `156-PATTERNS.md` FINDING A shows RESEARCH's G3/G4 clearance is **wrong** — it
reasoned about the grant layer only, while **seven** call sites (after PR A mints Part 3c) drive
`auth.uid()` by setting the JWT role claim to `authenticated`, which the new in-body gate refuses
regardless of ACL. **Five** of those seven are re-shaped here; the other two — Part 3b and Part 3c —
keep `authenticated` because a refusal is exactly what they assert. FINDING B shows
`test_wizard_composite_fence.sql` Part 3b keeps **passing** after the change while the guarantee it
names (cross-user elevation, T-88-03) no longer exists in the database at all.

Output: four SQL gate files that assert the post-156 boundary and say where the old one went.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-MEASUREMENTS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-VALIDATION.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-07-SUMMARY.md

⛔ **ANCHOR BY PART NAME, NEVER BY LINE — for `test_wizard_composite_fence.sql`.** PR A
(`156-03-PLAN.md` Task 4) inserts **Part 3c** between Part 3b and Part 4 **and** adds a variable to
that file's `DECLARE` block, so **every line number below the `DECLARE` block shifts** before this
plan runs. Any positional anchor into that file written at plan time is stale by construction.
`test_csv_finalize_double_submit.sql` is **not** touched by PR A, so its line numbers stand.

⛔ **FINDING A — seven call sites.** `auth.role()` reads `request.jwt.claims ->> 'role'`. These
sites set that claim to `authenticated` and then call an RPC whose new body gate requires
`service_role`. ⚠️ One `set_config` serves Parts 1+2, and another serves Parts 4a+4b — **claim
sites and call sites are not the same count**:

| File / Part | Call | Ambient claim | PR-B treatment |
|---|---|---|---|
| fence — Part 1 | `add_wizard_composite_key` | the claim opening the *"Part 1 — draft fence + Part 2"* block | **re-shape** |
| fence — Part 2 | `add_wizard_composite_key` | same claim as Part 1 | **re-shape** (no separate edit) |
| fence — Part 3b | `add_wizard_composite_key` | Part 3b's own claim | ⛔ **keeps `authenticated`** — refusal assertion, see FINDING B |
| fence — Part 3c *(minted by PR A)* | `create_wizard_strategy` | Part 3c's own claim | ⛔ **keeps `authenticated`** — refusal assertion, see FINDING B |
| fence — Part 4a | `create_wizard_strategy` | the claim opening the *"Part 4 — single-key regression"* block | **re-shape** |
| fence — Part 4b | `create_wizard_strategy` | same claim as Part 4a | **re-shape** (no separate edit) |
| `test_csv_finalize_double_submit.sql:218` | `create_wizard_strategy` (cross-source control) | `:212-213` | **re-shape** |

⭐ **The accounting, stated once so no criterion can drift from it:** **seven** call sites drive an
`authenticated` claim into the new gate; **five** are re-shaped; **two** (Parts 3b and 3c)
deliberately are not. Those five call sites are reached by exactly **three** `set_config` edits —
**two** in `test_wizard_composite_fence.sql` and **one** in `test_csv_finalize_double_submit.sql`.
⛔ A criterion that says *"four claim sites"* is counting call sites as claim sites; after PR A the
fence file holds **four** claim sites in total and **two** of them must not be touched, so
"four claim sites now carry `service_role`" is unsatisfiable without breaking Parts 3b/3c.

⚠️ **Part 3c does not exist until PR A lands.** `156-03-PLAN.md` Task 4 mints it — the
`create_wizard_strategy` cross-user twin of Part 3b, which the repo has never had (the only
single-key cross-user assertion is `wizard-rpcs-live-db.test.ts:211`, `it.skipIf(!HAS_LIVE_DB)`,
which never runs in CI). ⛔ Part 3c must **NOT** be re-shaped to `service_role`: like Part 3b it is
a *refusal* assertion and keeps its `authenticated` claim. It must, however, be **re-cut alongside
Part 3b in Task 2** — it inherits FINDING B exactly, and leaving it would hand PR B a **second**
vacuous green test pinning a deleted control, which is the precise defect class this phase exists
to end.

⚠️ **The correct re-cut is `json_build_object('sub', <uid>::text, 'role', 'service_role')` — flip the
role, keep the `sub`.** Deleting the `set_config` leaves `auth.role()` NULL and the call is still
refused. After 156 the RPCs no longer read `auth.uid()`, so `sub` becomes decorative for these sites
— keeping it keeps the fixture honest and makes the diff a one-token change.

⛔ **FINDING B — Part 3b goes vacuous** (anchor by name; its line numbers shifted in PR A). It presents a *different* identity in the JWT
than `p_user_id` and asserts `insufficient_privilege`, naming *"cross-user elevation (T-88-03)"*.
Phase 156 **deletes** `auth.uid()` from the body. The assertion keeps passing — via the **role**
gate — while the control it names no longer exists in the DB. A green test pinning a dead guarantee
is worse than a deleted one, because it reads as coverage. The ownership binding now lives **at the
route** (`p_user_id === user.id` from `withAuth`), and Part 3b must say so in its own prose or the
next reader will believe the DB still enforces it.

⛔ **Anti-vacuity:** `has_function_privilege('authenticated', …) = FALSE` **also passes on a database
where the function was dropped.** Every inverted pin gets a `service_role`-TRUE companion.

⚠️ **GSD worktree agents get NO `node_modules`.** `npx vitest` exits 1 with `MODULE_NOT_FOUND` and
`npx tsc` resolves an unrelated joke package. **Symlink the main checkout's `node_modules` into the
worktree before trusting ANY result.** A broken environment exits 1 exactly like a failing test —
grep for the per-test **NAME** among the failures, never read a bare exit code.
⛔ **This plan is the one that self-certifies if that rule is skipped.** Task 2's falsifiability
mutation is *"neuter the route-side assertion referenced in Part 3b's prose → the named vitest case
reds"*. In a `node_modules`-less worktree that mutation "passes" while vitest ran **zero** tests —
the mutation proof would certify a pointer that was never checked. The neutered case must therefore
be identified **by its full test name** in the failure list, and that line pasted.

⚠️ **CI runs Node 22; local is Node 25.** A CI-only vitest failure here is **not** a flake —
reproduce with `PATH=/opt/homebrew/opt/node@22/bin`.

⚠️ **Pitfall 9** — a grey `sql-tests` is not a pass; re-run rather than interpret.
**Local run:** `/opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f <file>` against a
throwaway PG16 fixture (loopback TCP, not a unix socket).

<interfaces>
**The remedy idiom, already in-repo** — `test_api_key_delete_atomicity.sql:148-149`, `:169-171`,
`:261`: three role shapes in one file, all through the same GUC, with
`set_config('request.jwt.claims', NULL, true)` as the closing reset. `:261` carries the comment
*"Service-role-shaped call."*

**The pins to invert:**
- **G1** `test_wizard_session_idempotency.sql:129-135` — `IF NOT has_function_privilege('authenticated',
  'create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)','EXECUTE')
  THEN RAISE`. ⛔ **Keep the `anon` assertion at `:133-135` byte-intact.**
- **G2** `test_wizard_composite_fence.sql:54-70` — the same three-assertion shape (`anon` ×2 +
  `authenticated` ×1) for `add_wizard_composite_key(uuid,…,uuid)`. ⛔ Keep the `anon` half at `:55-58`.

**The canary to twin** — `test_api_keys_venue_identity_uniq.sql:174-208`, asserted from the OUTSIDE
so it survives the migration being edited: each `NOT ILIKE` line names a guarantee that a body older
than `20260811210000` would silently revert (`pg_advisory_xact_lock`, `wizdraft:`, `attested_venue`),
plus the single-overload assertion at `:174-186` that is the argument for `CREATE OR REPLACE` over
`DROP`+`CREATE`. ⭐ **The `add_wizard_composite_key` twin of this entire block does not exist.**

Signature literals (⚠️ asymmetric arity):
- `create_wizard_strategy(uuid,text,text,text,text,text,text,text,integer,text,uuid,text)` — 12
- `add_wizard_composite_key(uuid,text,text,text,text,text,text,text,integer,text,uuid)` — 11
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Invert G1 and G2, add service_role positives, and make an unprivileged connection role fail by name</name>
  <files>supabase/tests/test_wizard_session_idempotency.sql, supabase/tests/test_wizard_composite_fence.sql</files>
  <read_first>
    - `supabase/tests/test_wizard_session_idempotency.sql:90-139` — the body-substring assertion at
      `:96` (the `wizdraft:` fence pin) and the G1 privilege block at `:129-135`
    - `supabase/tests/test_wizard_composite_fence.sql:40-75` — the G2 block, including the two `anon`
      assertions that stay
    - `.planning/phases/156-.../156-MEASUREMENTS.md` § A3 — the measured `sql-tests` connection role
      and whether it is superuser/owner
    - `supabase/tests/test_api_key_delete_atomicity.sql:140-190` — the role-shaping idiom
  </read_first>
  <action>
**Invert G1 and G2.** Turn each `IF NOT has_function_privilege('authenticated', <sig>, 'EXECUTE')
THEN RAISE` into `IF has_function_privilege('authenticated', <sig>, 'EXECUTE') THEN RAISE`, with a
message naming Phase 156 / CONNECT-01 and stating that a re-grant re-opens the PostgREST door. ⛔
Leave every `anon` assertion byte-intact.

**Add a `service_role` positive next to each inverted pin** — `IF NOT
has_function_privilege('service_role', <sig>, 'EXECUTE') THEN RAISE 'connect-a-key is broken'`. Its
message must name the *outage*, not the privilege: this is the assertion that catches a REVOKE that
went one role too far. Write the anti-vacuity reasoning into the comment — the negative alone passes
on a database where the function was dropped.

**Add the A3 self-diagnosing connection-role assertion** at the top of *each* of the two files: if
`has_function_privilege(current_user, <sig>, 'EXECUTE')` is FALSE for either RPC, `RAISE` a message
that names `current_user` and says plainly that this gate's direct RPC calls cannot run under this
connection role and that the failure is **environmental, not a regression**. ⭐ This converts
RESEARCH's assumption A3 from something that would surface as six confusing red assertions into one
loud, self-explaining line — and it keeps working if the CI connection ever changes.

⛔ **THE A3-FAIL BRANCH — do not improvise this at the point of failure.** `156-01-PLAN.md:180-183`
assigns this plan the contingency for a FAIL-shaped A3. A loud `RAISE` **fails the gate**; it does
not make it pass. If `156-MEASUREMENTS.md` § A3 records the `sql-tests` connection role as **neither
superuser nor the function owner (`postgres`)**, the diagnosing `RAISE` alone leaves `sql-tests`
**permanently red on PR B** with no remedy — so apply the remedy **in the same task**, in this order:

1. **Preferred — fixture-level grant.** At the top of each affected file, inside the gate's own
   setup, `GRANT EXECUTE ON FUNCTION public.<sig> TO <the measured current_user>;` for both
   signatures. ⚠️ This is a **fixture** grant made inside the file's transaction and rolled back
   with it — it must never appear in `supabase/migrations/**`, and it must be scoped to the
   measured role, never to `authenticated` (which would silently re-open the door PR B just shut
   and green G1/G2's inverted pins for the wrong reason). If the connection role lacks `WITH GRANT
   OPTION` on the function, this arm errors and you fall to (2).
2. **Fallback — `SET LOCAL ROLE postgres`** bracketing the direct RPC calls, with a matching
   `RESET ROLE`, mirroring this file's existing `SET LOCAL ROLE authenticated` / `RESET ROLE`
   bracket idiom. Requires the connection role to be a member of `postgres`.
3. **If neither arm is available**, the diagnosing `RAISE` stands as-is, and the executor **STOPS
   and reports to the orchestrator** — the six direct call sites cannot run in CI under this
   connection at all, which is a phase-level finding (the gates would have to move to a different
   lane), not something to be worked around inside this task.

⛔ Whichever arm is taken, write which one and **why** into the file's comment, naming the measured
role from `156-MEASUREMENTS.md` § A3 — a future reader must not have to re-derive it.
⛔ If A3 measured superuser-or-owner, take **no** remedy arm: the diagnosing `RAISE` alone is the
whole deliverable, and adding an unnecessary fixture grant weakens the gate.

⛔ Do not touch the body-substring assertions (`test_wizard_session_idempotency.sql:96`) — plan 07's
migration must keep them true, and they are an independent check on it.
⛔ `RAISE` format strings: single literal or dollar-quoted.
  </action>
  <verify>
    <automated>for f in supabase/tests/test_wizard_session_idempotency.sql supabase/tests/test_wizard_composite_fence.sql; do /opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f $f "$PG16_FIXTURE_URL" || exit 1; done; grep -c "has_function_privilege('service_role'" supabase/tests/test_wizard_session_idempotency.sql supabase/tests/test_wizard_composite_fence.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files run clean against a fully-migrated PG16 fixture
    - Each file contains at least one `has_function_privilege('service_role', …)` positive assertion
    - Each file's `anon` assertions are byte-unchanged (`git diff` proves it)
    - Each file carries the `current_user` EXECUTE pre-check whose message names `current_user` and
      the word *environmental*
    - **A3 branch taken explicitly:** if `156-MEASUREMENTS.md` § A3 records a non-superuser,
      non-owner role, exactly one remedy arm (fixture `GRANT` / `SET LOCAL ROLE postgres`) is
      present and its file comment names the measured role and the arm chosen; if A3 records
      superuser-or-owner, **no** remedy arm is present. ⛔ In no case does a fixture `GRANT` name
      `authenticated`, and in no case does any grant reach `supabase/migrations/**`
    - `test_wizard_session_idempotency.sql:96`'s body-substring assertion is unchanged
    - **Falsifiability, observed and pasted:** re-GRANT `authenticated` on the fixture → both
      inverted pins red; REVOKE `service_role` → both positives red with the outage-worded message
  </acceptance_criteria>
  <done>Both privilege pins assert the new polarity in both directions, and a connection role that cannot run these gates says so in one line instead of six.</done>
</task>

<task type="auto">
  <name>Task 2: Re-shape the five direct RPC calls, and replace Part 3b's and Part 3c's dead guarantee</name>
  <files>supabase/tests/test_wizard_composite_fence.sql, supabase/tests/test_csv_finalize_double_submit.sql</files>
  <read_first>
    - `supabase/tests/test_wizard_composite_fence.sql` **in full, as it stands after PR A** — the
      six direct call sites, the four claim sites (the one opening Parts 1+2, Part 3b's, Part 3c's,
      and the one opening Part 4), and the closing `set_config(…, NULL, true)` reset at the end of
      the `DO $$` block. ⛔ **Anchor by Part name, never by line** — PR A inserted Part 3c and added
      a `DECLARE` variable, so every line number below the `DECLARE` block moved
    - **Part 3c as it stands after PR A** (`156-03-SUMMARY.md` records where it landed): the
      `create_wizard_strategy` cross-user twin of Part 3b, sitting between Part 3b and Part 4, with
      its own `session_spoof` and its zero-row-delta assertion
    - `supabase/tests/test_csv_finalize_double_submit.sql:110-125, :205-272` — the claim at
      `:212-213`, the call at `:218`, and the closing reset at `:267`
    - `supabase/tests/test_api_key_delete_atomicity.sql:255-265` — the `"Service-role-shaped call."`
      comment idiom
    - `src/app/api/strategies/create-with-key/route.test.ts` — the CONNECT-03b route-level ownership
      case, which Part 3b's new prose must point at by name
  </read_first>
  <action>
**Re-shape all five sites — with three edits.** At each ambient `set_config('request.jwt.claims',
…)` that precedes a direct wizard-RPC call **and is not a refusal assertion's own claim**, change
the `'role'` value from `'authenticated'` to `'service_role'` and keep the `'sub'`. That is **two**
edits in `test_wizard_composite_fence.sql` (the claim opening Parts 1+2, and the claim opening Part
4 — one claim serves 4a and 4b) and **one** in `test_csv_finalize_double_submit.sql` (the claim
preceding the `create_wizard_strategy` cross-source control at `:218`), covering **five** call
sites in total. ⛔ Do **not** flip the CSV file's first claim — it precedes `finalize_csv_strategy`,
which is not a wizard RPC and is untouched by this phase. Add the `-- Service-role-shaped call.` comment at each, plus one sentence explaining
**why** it changed: after Phase 156 the RPC bodies gate on `auth.role() = 'service_role'` and no
longer read `auth.uid()`, so `sub` is retained for fixture honesty and no longer affects the outcome.
⛔ Keep every existing closing `set_config('request.jwt.claims', NULL, true)` reset.
⚠️ ⛔ Change **only** the claims that immediately precede a wizard-RPC call. Other claims in these
files drive RLS and non-RPC assertions; flipping one of those silently changes what a different test
means.

**Replace Part 3b — and Part 3c with it.** (By Part name: both blocks' line numbers moved in PR A.) Their current assertions — a different
`sub` than `p_user_id` must raise `insufficient_privilege` — are about to pass for a reason that has
nothing to do with the guarantee they name. ⛔ **Both, in the same task, with the same treatment.**
Part 3c is the `create_wizard_strategy` twin minted in PR A by `156-03-PLAN.md` Task 4; re-cutting
3b while leaving 3c green would hand PR B a second vacuous test pinning a deleted control — the
exact instance-not-class defect this phase exists to end, committed by the plan that names it.
Re-cut each to assert the **new** boundary and say what happened to the old one:

1. **The new negative:** a caller presenting `role: 'authenticated'` is refused with `42501`
   **whatever `sub` it carries** — assert it for two `sub` values, the matching uid and a different
   one, so the message cannot be read as "cross-user elevation is still checked here".
2. **Nothing minted:** row delta 0, matching plan 08's discipline.
3. **The prose:** state in the block's comment that the `auth.uid() <> p_user_id` guard is **gone
   from the database**, that the ownership binding now lives entirely at the route, and **name the
   route-level unit assertion** (`p_user_id === user.id` from `withAuth`, in
   `create-with-key/route.test.ts` and its composite twin) as the surviving control. ⛔ Do not leave
   the T-88-03 reference implying a DB-level control that no longer exists — restate it as
   *"formerly enforced here; now enforced at the route"*. Part 3c's prose names
   `create-with-key/route.test.ts`; Part 3b's names the composite twin.

⛔ **Part 3b and Part 3c keep their `authenticated` claims.** They are refusal assertions — flipping
either to `service_role` would assert that the *server's own writer* is refused, which is the
opposite of true and would red on a correct database. They are therefore **not** among the sites
re-shaped above.

⛔ Do not soften or delete any other assertion in either file.
  </action>
  <verify>
    <automated>FENCE=supabase/tests/test_wizard_composite_fence.sql; CSV=supabase/tests/test_csv_finalize_double_submit.sql; for f in "$FENCE" "$CSV"; do /opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f "$f" "$PG16_FIXTURE_URL" || exit 1; done; grep -q 'Part 3b' "$FENCE" && grep -q 'Part 3c' "$FENCE" && grep -q 'Part 4 — single-key regression' "$FENCE" && test "$(grep -vE '^[[:space:]]*--' "$FENCE" | grep -c "'role', 'service_role'")" -eq 2 && test "$(grep -vE '^[[:space:]]*--' "$CSV" | grep -c "'role', 'service_role'")" -eq 1 && awk "/Part 3b/{i=1} /Part 4 — single-key regression/{i=0} /^[[:space:]]*--/{next} /'role', 'authenticated'/{if(!i){print \"STRAY authenticated claim outside Part 3b/3c at line \" FNR; bad=1}} END{exit bad?1:0}" "$FENCE"</automated>
  </verify>
  <acceptance_criteria>
    - Both files run clean against a fully-migrated PG16 fixture
    - Exactly **two** claim sites in `test_wizard_composite_fence.sql` now carry `service_role` —
      the claim opening the *"Part 1 — draft fence + Part 2"* block and the claim opening the
      *"Part 4 — single-key regression"* block — **covering four call sites** (Parts 1, 2, 4a, 4b);
      and exactly **one** in `test_csv_finalize_double_submit.sql` (the claim at `:212-213`
      preceding the call at `:218`), covering one. **Five call sites, three edits.** The `sub` value
      at each is unchanged. ⛔ Anchor by Part name — PR A shifted the fence file's line numbers.
      ⛔ Part 3b's and Part 3c's claim sites deliberately **retain** `authenticated` — they assert a
      refusal, and flipping them would assert the server's own writer is refused, so a criterion
      demanding *four* `service_role` claim sites in this file would be unsatisfiable
    - ⛔ **Zero stray `authenticated` claims:** every remaining `'role', 'authenticated'` on a
      non-comment line of `test_wizard_composite_fence.sql` lies **between the Part 3b marker and
      the `Part 4 — single-key regression` marker** — i.e. belongs to Part 3b or Part 3c, each of
      which may set the claim once per `sub` value it tests. The verify command asserts this by
      range and prints the line number of any stray
    - `test_csv_finalize_double_submit.sql`'s remaining `authenticated` claim is the Part 1
      `finalize_csv_strategy` one — **not** a wizard RPC, and unchanged
    - No other `set_config('request.jwt.claims', …)` in either file changed role
    - Every closing `set_config(…, NULL, true)` reset is retained
    - Part 3b **and Part 3c** each assert `42501` for **two** distinct `sub` values and a zero row
      delta — 3b against `add_wizard_composite_key`, 3c against `create_wizard_strategy`
    - Both blocks' comments state the guard is gone from the DB and name the route-level unit
      assertion that replaces it (3b → the composite twin, 3c → `create-with-key/route.test.ts`)
    - ⛔ `grep -c 'T-88-03'` in `test_wizard_composite_fence.sql` returns **zero** occurrences that
      are not qualified by *"formerly enforced here; now enforced at the route"*
    - **Falsifiability, observed and pasted:** revert one claim site to `'authenticated'` → that
      site's assertion reds naming the RPC; neuter the route-side assertion referenced in Part 3b's
      prose → the named vitest case reds (proving the pointer is live, not decorative); re-GRANT
      `authenticated` EXECUTE on the fixture → **neither** Part 3b nor Part 3c reds, which is the
      measured demonstration that the pre-re-cut assertions were vacuous and the re-cut was required
    - ⛔ **The vitest mutation is identified BY TEST NAME, not by exit code.** Paste the failure
      line carrying the case's full name. In a `node_modules`-less GSD worktree `npx vitest` exits 1
      with `MODULE_NOT_FOUND` exactly as a reddened test does — a bare exit code would certify a
      mutation against a suite that never ran. Symlink the main checkout's `node_modules` first
  </acceptance_criteria>
  <done>Five call sites that would have gone red for the wrong reason now call the way the server calls, and BOTH gates that would have stayed green for the wrong reason — Part 3b and its single-key twin Part 3c — assert something true instead.</done>
</task>

<task type="auto">
  <name>Task 3: Give the composite twin the stale-re-base canary, and assert auth.uid() is gone on both</name>
  <files>supabase/tests/test_api_keys_venue_identity_uniq.sql</files>
  <read_first>
    - `supabase/tests/test_api_keys_venue_identity_uniq.sql:35-50` (the 12-arg signature pin at `:41`)
      and `:170-215` (the single-overload assertion and the three `NOT ILIKE` canary lines with the
      reasoning comment at `:174-180`)
    - `supabase/schema/functions/add_wizard_composite_key.sql` as regenerated by plan 07 — the
      guarantees its canary must name (`wizcomposite:`, `attested_venue`, the lazy draft creation)
    - `.planning/phases/156-.../156-RESEARCH.md` § Pitfall 8 — what a stale re-base costs
      (`attested_venue` dropped ⇒ every MT5 finalize answers a permanent `KEY_SCOPE_CHECK_UNAVAILABLE`)
  </read_first>
  <action>
Add the twin of the `:174-208` block for `add_wizard_composite_key`: read
`pg_get_functiondef` for the 11-arg signature and assert its single-overload count, then
`NOT ILIKE '%pg_advisory_xact_lock%'`, `NOT ILIKE '%wizcomposite:%'` and
`NOT ILIKE '%attested_venue%'`, each with a message naming the guarantee a stale re-base would
silently revert and the migration whose body would be reverted to.

Add to **both** canaries — the existing `create_wizard_strategy` one and the new one — a line
asserting the body **does contain** `auth.role()` and **does not contain** `auth.uid()`. ⭐ This is
the Trap-B structural guard asserted from the **outside**: the migration's own post-verify checks it
too, but this copy survives the migration being edited, re-based, or replayed from a stale snapshot.
Its message must name the failure precisely — a body carrying `auth.uid()` under a `service_role`
caller means the ownership comparison is a permanent silent no-op, not a stricter check.

⛔ Do not weaken or renumber any existing assertion in this file; the 12-arg signature pin at `:41`
and the single-overload assertion are load-bearing arguments for `CREATE OR REPLACE` over
`DROP`+`CREATE`.
  </action>
  <verify>
    <automated>/opt/homebrew/opt/postgresql@16/bin/psql -v ON_ERROR_STOP=1 -f supabase/tests/test_api_keys_venue_identity_uniq.sql "$PG16_FIXTURE_URL" && grep -c 'wizcomposite:' supabase/tests/test_api_keys_venue_identity_uniq.sql && grep -c "auth.uid()" supabase/tests/test_api_keys_venue_identity_uniq.sql</automated>
  </verify>
  <acceptance_criteria>
    - The file runs clean against a fully-migrated PG16 fixture
    - A canary block for `add_wizard_composite_key` exists asserting single-overload,
      `pg_advisory_xact_lock`, `wizcomposite:` and `attested_venue`
    - **Both** canaries assert `auth.role()` present and `auth.uid()` **absent**, with a message
      naming the silent-no-op consequence
    - The existing `create_wizard_strategy` assertions and the `:41` signature pin are byte-unchanged
      apart from the added lines
    - **Falsifiability, observed and pasted:** replace one function body on the fixture with a
      pre-`20260811210000` version → the `attested_venue` canary reds naming PR #675; replace it with
      a body carrying the relaxed `auth.uid()` comparison → the new line reds naming the no-op
  </acceptance_criteria>
  <done>Both RPC bodies are pinned from outside the migration chain, so a stale re-base cannot revert PR #675 or reintroduce the relaxed guard without a named, red assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL gate fixture → RPC body | The gate's JWT role claim is what selects the body's arm; a mis-shaped claim silently changes what is tested |
| migration chain → declared canonical body | A re-base can revert a guarantee with no error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-45 | *(control failure)* | Part 3b passing via the role gate while pinning a deleted cross-user control | mitigate | Re-cut to assert the new boundary for two `sub` values, with prose naming the route-level replacement |
| T-156-45c | *(control failure)* | **Part 3c** — the twin minted in PR A — left green while pinning the same deleted control | mitigate | Task 2 re-cuts 3b and 3c together; acceptance requires both to assert `42501` for two `sub` values, and the re-GRANT mutation proves both were vacuous before the re-cut |
| T-156-47b | Denial of Service | A FAIL-shaped A3 leaving `sql-tests` permanently red on PR B with the remedy improvised at the point of failure | mitigate | Task 1 names three ordered arms (fixture `GRANT` to the measured role / `SET LOCAL ROLE postgres` / STOP and report) and forbids granting to `authenticated` or writing any grant into `supabase/migrations/**` |
| T-156-46 | Repudiation | Inverted pins that pass on a database where the function was dropped | mitigate | Every inverted pin gains a `service_role`-TRUE positive worded as an outage assertion |
| T-156-47 | Denial of Service | Every direct RPC call red for an environmental reason (A3) read as a code regression | mitigate | A `current_user` EXECUTE pre-check raises one message naming the role and the word *environmental* |
| T-156-48 | Tampering | A stale re-base reverting PR #675's `attested_venue` stamp | mitigate | Canary twinned for the composite; both canaries assert from outside the migration chain |
| T-156-49 | Elevation of Privilege | The relaxed `auth.uid()` guard reintroduced by a later re-base | mitigate | Both canaries assert `auth.uid()` absent, with the silent-no-op consequence in the message |
| T-156-50 | Tampering | Flipping a JWT claim that drives a non-RPC assertion, or flipping a refusal assertion's own claim | mitigate | The action restricts the change to claims immediately preceding a wizard-RPC call and excludes Parts 3b/3c; acceptance counts exactly **two** `service_role` claim sites in the fence file and **one** in the CSV file, and the verify command range-checks that every surviving `authenticated` claim lies inside Part 3b/3c |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- All four files run clean under `psql -v ON_ERROR_STOP=1` against a fully-migrated PG16 fixture
- Seven falsifiability mutations observed RED and pasted
- `git diff` proves every `anon` assertion and the `:41` signature pin are byte-unchanged
- `sql-tests` observed **green** (not grey) in CI against TEST
</verification>

<success_criteria>
Every gate Migration B falsifies now asserts the new boundary; the **five** call sites that would
have broken environmentally call the way the server calls (via **three** claim edits); and **both**
assertions that would have stayed green while their subject was deleted — Part 3b and its single-key
twin Part 3c — say what is actually true and where the control moved.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-09-SUMMARY.md` when done
</output>
