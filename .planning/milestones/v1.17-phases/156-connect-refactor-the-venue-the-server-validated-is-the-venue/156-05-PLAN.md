---
phase: 156-connect-refactor-the-venue-the-server-validated-is-the-venue
plan: 05
type: execute
wave: 4
landing: PR-A
depends_on: ["156-04"]
files_modified:
  - src/__tests__/phase-156-wizard-rpc-writer-guard.test.ts
  - src/__tests__/wizard-rpcs-live-db.test.ts
  - TODOS.md
  - CHANGELOG.md
  - VERSION
  - package.json
autonomous: true
requirements: [CONNECT-02]

must_haves:
  truths:
    - "A new user-scoped .rpc call to either wizard RPC reddens a normal `npm run test` run before it can land"
    - "The guard's falsifiability is RECORDED — it was observed to red under a deliberate reintroduction, and to green again after removal"
    - "The two deliberately-out-of-scope items are logged to TODOS.md rather than fixed here"
    - "PR A carries a VERSION and package.json bump in ONE commit, with a CHANGELOG entry"
  artifacts:
    - path: "src/__tests__/phase-156-wizard-rpc-writer-guard.test.ts"
      provides: "CONNECT-02b — the structural invariant that makes 'the route was rewired' falsifiable"
      min_lines: 80
    - path: "TODOS.md"
      provides: "the two logged-not-fixed items"
  key_links:
    - from: "src/__tests__/phase-156-wizard-rpc-writer-guard.test.ts"
      to: "src/app/api/strategies/{create-with-key,composite/add-key}/route.ts"
      via: "source scan for .rpc call sites and their client binding"
      pattern: "create_wizard_strategy|add_wizard_composite_key"
---

<objective>
Make plan 04's rewiring **structurally irreversible**: a source-scan guard that reds on any new
`.rpc("create_wizard_strategy"|"add_wizard_composite_key")` reached from a user-scoped client — and
close out PR A (version, changelog, and the two items this phase deliberately does **not** fix).

Purpose: the route unit tests prove the route calls the admin client *today*. They do not stop a
future contributor adding a second call site in a `.tsx` server action or a `src/lib` helper. The
census-boundary lesson from Phase 142.1 finding 4 is exactly this — a gate that globs too narrowly
lets a writer added one directory over pass silently.

Output: one new guard test with a recorded falsifiability proof, two `TODOS.md` entries, and a
version bump.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-PATTERNS.md
@.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-VALIDATION.md

⚠️ **GSD worktree agents get NO `node_modules`** — symlink the main checkout's before trusting any
result; grep for per-test names among failures, never a bare exit code.
⚠️ **CI runs Node 22; local is Node 25** — a CI-only failure here is not a flake.

<interfaces>
**Analog to copy wholesale:** `src/__tests__/strategies-published-sole-writer-guard.test.ts` — a
sole-writer source scan with an allowlist, carve-outs, and a recorded falsifiability proof. Its
shape:
- `:5-21` the docblock stating the INVARIANT, why a source scan (runs in the normal unit suite,
  needs no new CI step, the failure message points at the offending file)
- `:63-71` ⭐ the FALSIFIABILITY RECORD — the part most often skipped
- `:73-89` the walker + `REPO_ROOT`/`SRC_DIR` constants + the allowlist + the anchored regex idiom

**Anchored call-site regex idiom** — `src/__tests__/audit-coverage.test.ts:220-224`:
`new RegExp("\\\\.rpc\\\\(\\\\s*['\"](?:" + NAMES.join("|") + ")['\"]")`.

⚠️ **A name-only scan cannot see WHICH client `.rpc` hangs off.** State the heuristic explicitly
rather than implying a stronger guarantee. The tractable invariant is a **pairing** one, evaluated
per file: a file containing a wizard-RPC `.rpc(` call site must (a) import `createAdminClient` from
`@/lib/supabase/admin`, and (b) contain no binding of `createClient()` that could be the receiver —
i.e. if the file binds both, the `.rpc` receiver identifier must be the admin one.
⚠️ Budget for false positives: the analog's carve-outs exist because a naive regex produced them.
Test files and `.planning/` are excluded; `supabase/migrations/**` legitimately contains the names
as SQL and must not be scanned as TS.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the CONNECT-02b structural guard and record its falsifiability</name>
  <files>src/__tests__/phase-156-wizard-rpc-writer-guard.test.ts</files>
  <read_first>
    - `src/__tests__/strategies-published-sole-writer-guard.test.ts` in full — docblock, walker,
      allowlist, carve-outs and the falsifiability record
    - `src/__tests__/audit-coverage.test.ts:200-230` — `MUTATING_RPC_NAMES` and the anchored
      `MUTATING_RPC_RE` idiom
    - `src/app/api/strategies/create-with-key/route.ts` and
      `src/app/api/strategies/composite/add-key/route.ts` as they stand after plan 04 — the two
      files that must PASS, and the identifiers the pairing heuristic keys on
  </read_first>
  <action>
Create `src/__tests__/phase-156-wizard-rpc-writer-guard.test.ts` on the analog's structure.

**Scan A — sanctioned writers.** Walk `src/**` (all extensions the analog walks, explicitly
including `.tsx`, so a server action one directory over cannot slip past — the Phase 142.1 finding-4
lesson), excluding test files and this guard itself. For every file whose source matches the
anchored wizard-RPC `.rpc(` regex, assert the file is one of the two allowlisted route files. A
third file appearing anywhere is a failure naming the path.

**Scan B — the pairing heuristic.** For each of the two allowlisted files, assert it imports
`createAdminClient` from `@/lib/supabase/admin`, and that the identifier the `.rpc(` call hangs off
is bound from `createAdminClient(...)` and **not** from `createClient(...)`. Implement it as: capture
the receiver identifier immediately preceding `.rpc(` and require a `const <that identifier> =
createAdminClient(` binding in the same file. ⚠️ `create-with-key` legitimately binds **both**
clients — the heuristic must therefore key on the receiver identifier, never on "does this file
mention `createClient`".

**Scan C — anti-vacuity.** Assert both allowlisted files are actually found and actually matched
(a non-zero count per file, hand-typed as 1 each). Without this, a regex that silently stops
matching turns the whole guard green — the same failure mode as the SQL gate's SKIP trap.

Write the docblock in the analog's voice: the invariant in one sentence, why a source scan, what the
heuristic can and **cannot** see (it cannot see runtime; it sees lexical binding), and the named
successor obligation if either route is ever restructured.

⭐ **Record the falsifiability proof in the docblock, having actually run it:** add a throwaway
non-test file under `src/` containing a user-scoped `.rpc("create_wizard_strategy"` call, observe
Scan A red naming that path, remove it, observe green. Then temporarily re-point one route's `.rpc`
receiver to a `createClient()` binding, observe Scan B red, revert, observe green. Paste both
failing messages into the docblock. ⛔ Verify the tree is clean afterwards (`git status`) — a
throwaway probe file left behind is itself the defect.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-156-wizard-rpc-writer-guard.test.ts --no-file-parallelism && npx tsc --noEmit && test -z "$(git status --porcelain | grep -v 'phase-156-wizard-rpc-writer-guard')"</automated>
  </verify>
  <acceptance_criteria>
    - The guard passes against the post-plan-04 tree
    - The docblock contains **two** pasted failing messages — one for Scan A (a third writer) and
      one for Scan B (a user-scoped receiver) — each produced by an actually-run mutation
    - Scan C's hand-typed per-file expected count is present, so a regex that stops matching reds
    - The walker includes `.tsx` and `src/lib/**`, not just `src/app/api/**`
    - `git status --porcelain` shows no leftover throwaway probe file
    - `npx tsc --noEmit` clean; `npm run lint` no new errors
  </acceptance_criteria>
  <done>A second writer, or a rewiring back to the user-scoped client, reds a normal test run before it can land — and the guard has been proven to red, not merely asserted to.</done>
</task>

<task type="auto">
  <name>Task 2: Log what this phase deliberately does not fix, and close out PR A</name>
  <files>src/__tests__/wizard-rpcs-live-db.test.ts, TODOS.md, CHANGELOG.md, VERSION, package.json</files>
  <read_first>
    - `TODOS.md` — the single ground-truth backlog; match its existing entry format exactly
    - `src/__tests__/wizard-rpcs-live-db.test.ts:150-230` — the `it.skipIf(!HAS_LIVE_DB)` cases that
      call the RPCs with a **user-scoped** client and expect success
    - `CHANGELOG.md` — the most recent entry, for format
    - `VERSION` and `package.json` — currently `0.59.0.0`; they must move together in ONE commit
      (`critical-regressions.test.ts` fails CI otherwise)
    - `.planning/phases/156-.../156-RESEARCH.md` § "Open Questions" 3 and 4
  </read_first>
  <action>
**TODOS.md — two entries, logged not fixed (Rule 3):**
1. `add_wizard_composite_key` is absent from `MUTATING_RPC_NAMES`
   (`src/__tests__/audit-coverage.test.ts:204-216`) while its single-key twin is present — a real
   audit-coverage gap that pre-dates this phase. ⛔ Not fixed here: adding it creates an
   audit-emission obligation on a route Phase 156 is already rewiring. Reference RESEARCH Open
   Question 4.
2. The `asset_class` annualization stamp still reads the forgeable `apiKeyExchange`
   (`finalize-wizard/route.ts:1275-1285`) rather than `attestedVenue`. ⛔ Out of scope: the swap
   needs its own oracle over √365 vs √252 that this phase does not have. Reference RESEARCH Open
   Question 3.

**`src/__tests__/wizard-rpcs-live-db.test.ts`:** this file is `it.skipIf(!HAS_LIVE_DB)` and
**NEVER runs in CI** — ⛔ it is not a gate and must not be counted toward any success criterion.
Update its docblock and the affected cases so a stale live test is not a trap for whoever next runs
it locally: state that the RPCs now accept a `service_role` caller, that `authenticated` still works
**until the follow-up migration lands**, and name that migration. Do not delete cases.

**Close out PR A:** bump `VERSION` and `package.json` in lockstep in ONE commit, and add a
`CHANGELOG.md` entry for the service-role writer that says plainly that this is **landing 1 of 2**
and that `authenticated` EXECUTE is withdrawn in the follow-up PR — so the changelog does not claim
CONNECT-01 is closed.

⛔ Never bundle a commit with edits: edit → verify (grep/tsc) → commit, each its own step.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/critical-regressions.test.ts --no-file-parallelism && npx tsc --noEmit && test "$(cat VERSION)" = "$(node -p "require('./package.json').version")" && grep -c 'add_wizard_composite_key' TODOS.md</automated>
  </verify>
  <acceptance_criteria>
    - `TODOS.md` gains exactly two entries, each naming a file:line and the reason it is NOT fixed here
    - `src/__tests__/audit-coverage.test.ts` is **unmodified** (`git diff --name-only` proves it)
    - `finalize-wizard/route.ts:1275-1285` is **unmodified**
    - `wizard-rpcs-live-db.test.ts` docblock states it never runs in CI and names the follow-up migration
    - `cat VERSION` equals `package.json`'s `version`, both changed in ONE commit
    - The `CHANGELOG.md` entry states "landing 1 of 2" and does **not** claim `authenticated` EXECUTE
      has been withdrawn
    - `critical-regressions.test.ts` passes
  </acceptance_criteria>
  <done>PR A ships an honest changelog, and the two things this phase chose not to do are written down where the backlog actually lives instead of being rediscovered by the next reviewer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future contributor → `src/**` | A new `.rpc` call site added anywhere under `src/` can re-open the door the routes just closed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-156-22 | Elevation of Privilege | A second, user-scoped wizard-RPC call site added later | mitigate | Scan A walks all of `src/**` including `.tsx` and `src/lib/**`, with a recorded red-under-mutation proof |
| T-156-23 | Repudiation | The guard silently stops matching (regex drift) and reports green | mitigate | Scan C's hand-typed per-file expected count reds on a zero match — the SKIP-trap lesson applied to TS |
| T-156-24 | Tampering | A throwaway falsifiability probe file left in the tree | mitigate | Task 1's automated verify includes a `git status --porcelain` cleanliness assertion |
| T-156-25 | Repudiation | A changelog claiming CONNECT-01 closed at PR A | mitigate | Acceptance requires the "landing 1 of 2" wording and forbids the withdrawal claim |
| T-156-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed by this phase; no install task exists |
</threat_model>

<verification>
- The guard passes and has two pasted red-under-mutation proofs
- `git status` clean of probe artifacts
- `VERSION` and `package.json` agree and moved in one commit
- `src/__tests__/audit-coverage.test.ts` and `finalize-wizard/route.ts` untouched by this plan
</verification>

<success_criteria>
Plan 04's rewiring cannot be silently undone, and PR A's paperwork states exactly what it did and
did not close.
</success_criteria>

<output>
Create `.planning/phases/156-connect-refactor-the-venue-the-server-validated-is-the-venue/156-05-SUMMARY.md` when done
</output>
