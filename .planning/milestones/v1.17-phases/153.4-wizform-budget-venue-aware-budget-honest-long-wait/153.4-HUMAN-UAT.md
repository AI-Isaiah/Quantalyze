---
status: partial
phase: 153.4-wizform-budget-venue-aware-budget-honest-long-wait
source: [153.4-VERIFICATION.md]
started: 2026-08-11T08:10:00Z
updated: 2026-08-11T08:10:00Z
---

## Current Test

[awaiting human testing]

All five items below are hardware- or calendar-gated: they need a founder at the MT5
terminal, on a trading day, with the live funded account's read-only investor password
— or a live analytics gateway, or human eyes on motion behaviour. None is dischargeable
from the repo. Item 1 is deferred to Phase 155 (MT5-VERIFY) **by design**, not by
omission: D-27 records that no uncensored successful MT5 validation exists anywhere, so
the 120 000 ms budget is the founder's staleness tolerance rather than a measurement.

## Tests

### 1. Live MT5 single-key validation, full wait ladder
expected: On a trading day with `MT5_ENABLED`, connect a live MT5 key through the single-key wizard and watch the whole wait — card mounts, the 40% rung discloses the queue and reveals `Stop waiting`, the 75% rung turns amber, the verdict arrives. An honest verdict (success or a classified failure) lands inside 120 s. No `UNKNOWN`, no silent spinner, no client abort.
result: [pending]

### 2. Cancel-then-resubmit reconcile
expected: Press `Stop waiting` at ~48 s of a live MT5 validate, then press `Validate key and continue` again with the same fields. A neutral grey line (never red, never an error envelope); every field preserved; focus lands on the submit button; the re-submit resolves to the SAME strategy/key rather than a 409 dead end or a second stored credential.
result: [pending]

### 3. Composite MT5 draft carry-over against a live gateway
expected: In the composite wizard with `NEXT_PUBLIC_MT5_ENABLED=true`, pick MT5 in State A, fill all three fields, click "+ Add another key window", then validate panel 0 against a live gateway. The panel shows the MT5 card pressed, `MT5 login` / `Investor password` / `Broker server` labels, and the add-key POST succeeds with the broker server carried.
result: [pending]

### 4. ValidateWaitCard visual review against DESIGN.md
expected: The wait-ladder colours conform to DESIGN.md §Semantic-color gates; the dot's `animate-pulse` is suppressed under `prefers-reduced-motion`; the plain-`<button>` focus ring renders at full opacity.
result: [pending]

### 5. Remove a composite member panel mid-validate
expected: Removing a panel while its validate is still in flight (the Remove control is NOT disabled during `validating`) produces no console error, no wedged panel, no visible artifact.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
