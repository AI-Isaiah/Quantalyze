---
phase: 153.4-wizform-budget-venue-aware-budget-honest-long-wait
plan: 02
type: execute
wave: 2
depends_on: ["153.4-01"]
files_modified:
  - src/lib/resilient-fetch.ts
  - src/lib/analytics-client.ts
  - src/lib/analytics-client.test.ts
  - src/lib/seam-budgets.invariant.test.ts
autonomous: true
requirements: [WIZFORM-05]
must_haves:
  truths:
    - "An MT5 validate-key call spends the 120 000 ms row; every other venue spends the 30 000 ms row"
    - "An unrecognised, empty or absent venue string falls back to the default row and NEVER throws"
    - "No venue value ever reaches a breaker key — the selector returns one of two module constants"
    - "The two venue arms are declared as mutually exclusive branches, so the route's worst case is a MAX and not a SUM"
    - "budgetKeyFor carries no venue name literal — a second serialized venue is covered without editing it"
  artifacts:
    - path: "src/lib/analytics-client.ts"
      provides: "exported budgetKeyFor(exchange) + the wired validateKey call site"
      contains: "budgetKeyFor"
    - path: "src/lib/resilient-fetch.ts"
      provides: "branch-labelled validate-key legs on the three validate routes"
      contains: "serialized-venue"
    - path: "src/lib/seam-budgets.invariant.test.ts"
      provides: "EXPECTED_ROUTE_BUDGETS mirror, the four-row multiBranch roster, a hand-typed serialized-branch worst case"
      contains: "serialized-venue"
  key_links:
    - from: "src/lib/analytics-client.ts validateKey"
      to: "src/lib/resilient-fetch.ts SEAM_BUDGETS"
      via: "budgetKeyFor(exchange) replacing the hardcoded budgetKey literal"
      pattern: "budgetKeyFor\\(exchange\\)"
    - from: "src/lib/analytics-client.ts budgetKeyFor"
      to: "src/lib/closed-sets.ts VENUE_CAPABILITIES"
      via: "venueIsSerialized — the capability record, never a venue name"
      pattern: "venueIsSerialized"
---

<objective>
Select the new budget by CAPABILITY, and declare it on the three routes that spend it as a
mutually exclusive `branch` so SC-4b takes a MAX rather than a SUM.

Purpose: the budget row from plan 01 is inert until a call site names it. `validateKey` already
receives the venue as its first parameter and today hardcodes `{ budgetKey: "validate-key" }` —
that one literal is the whole client-side fix (D-01). The `branch` labelling is what stops the
seam-budget invariant from charging a request for both venue arms, which describes a path no
request takes (D-04, RESEARCH Pattern 2).

Output: `budgetKeyFor(exchange)`, three re-branched route rows, and an invariant test that has
re-derived every number it narrates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153.4-wizform-budget-venue-aware-budget-honest-long-wait/153.4-CONTEXT.md
@.planning/phases/153.4-wizform-budget-venue-aware-budget-honest-long-wait/153.4-01-SUMMARY.md

<interfaces>
<!-- Extracted from HEAD 2026-08-09 plus plan 153.1-02. Use directly; do not re-explore. -->

src/lib/closed-sets.ts  (minted by plan 153.1-02 Task 1 — CONSUME, do not re-create)
- `export function venueIsSerialized(v: string | null | undefined): boolean;`
   null / unknown / absent-row => **false** ("never claim queueing we cannot observe")
- `export const VENUE_CAPABILITIES` — `as const satisfies Record<SupportedExchange,
   VenueCapabilities>`; only `mt5` carries `serialized: true`
- ⛔ This plan does NOT edit `closed-sets.ts`. Phase 153.1 owns it.

src/lib/analytics-client.ts
- `async function analyticsRequest(path, body, options: { budgetKey: SeamBudgetKey;
   tenantId: string; timeoutMs?: number; method?: string; correlationId?: string })`   (:323-)
- `const timeoutMs = options.timeoutMs ?? SEAM_BUDGETS[options.budgetKey].timeoutMs;` (:349)
  — the `timeoutMs` override is **TESTS ONLY** since 140-05; ⛔ it is invisible to SC-4b, so it is
  NOT an alternative to a table row
- `export async function validateKey(exchange: string, apiKey: string, apiSecret: string,
   passphrase: string | undefined, tenant: TenantIdentity)`   (:669-687)
  with `{ budgetKey: "validate-key", tenantId: tenant.userId }` at `:684`  <- THE ONE LITERAL

src/lib/process-key-client.ts
- `function budgetKeyFor(flowType: FlowType): SeamBudgetKey` (:111-136) — the many-to-one
  precedent, whose `default:` assigns to a `never` and THROWS. ⚠️ This plan's variant must
  deliberately NOT copy that.

src/lib/resilient-fetch.ts  (after plan 153.4-01)
- `SEAM_ROUTE_BUDGETS` at `:738-`; the three routes to re-branch:
  - `"src/app/api/keys/validate-and-encrypt/route.ts"` (:745) — legs `validate-key`,
    `encrypt-key`, `process-key-unified-dormant`, all `calls: 1`
  - `"src/app/api/strategies/create-with-key/route.ts"` (:753) — `validate-key`, `encrypt-key`
  - `"src/app/api/strategies/composite/add-key/route.ts"` (:760) — `validate-key`, `encrypt-key`
- the ONLY labelled row today, and the comment style a new labelled row owes:
  `"src/app/api/strategies/finalize-wizard/route.ts"` (:795-817) with
  `branch: "composite"` / `branch: "single-key"`

src/lib/seam-budgets.invariant.test.ts
- `function branchesOf<T extends { calls: number; branch?: string }>(budgets)` (:566-584) —
  unlabelled legs are SHARED and charged to EVERY branch
- `const EXPECTED_ROUTE_BUDGETS` (:386-469) — deep-equality mirror, asserted at `:705-720`
- `it("exercises the branch MAX on at least one row ...")` (:919-932) — asserts
  `multiBranch.map(([path]) => path)` equals exactly `[FINALIZE_WIZARD_ROUTE]`, and
  `branchesOf(finalize-wizard).length === 2`
- the header headroom table (:85-131), incl. the "validate-and-encrypt (3 calls)" column and the
  prose naming finalize-wizard's composite at 277 500 ms / 22 500 ms headroom as the tightest row
- two hand-typed literal oracles that must NOT move: keys/sync failing `56_000` (:801),
  finalize-wizard composite failing `277_500` (:836)
- SC-4b formula (:596-664): per branch,
  `Σ(timeoutMs × calls × (1+retries)) + Σ(retries × calls × 500) + Σ(storeCmds(state) × (1+retries) × 4250 × calls)`
  with `STORE_COMMAND_WORST_CASE_MS = 4_250` and `storeCmds = {closed:1, open:1, failing:3}`;
  the `open` state spends NO request budget
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two mutually exclusive venue branches on the three validate routes</name>
  <files>src/lib/resilient-fetch.ts</files>
  <read_first>
    - `src/lib/resilient-fetch.ts` `:698-737` (the `SEAM_ROUTE_BUDGETS` docblock, especially the
      `branch` paragraph "names a MUTUALLY EXCLUSIVE path through the route ... an unlabelled row
      is one implicit branch"), `:745-766` (the three rows to change), `:795-817` (the
      finalize-wizard row — the comment style to mirror).
    - `.planning/phases/153-…/153-PATTERNS.md` §⭐4, the "`branch` legs" block and the ⚠️ note that
      `encrypt-key` and `process-key-unified-dormant` stay UNLABELLED (shared).
    - `.planning/phases/153-…/153-RESEARCH.md` §Pattern 2 and §Table C row 3.
  </read_first>
  <action>
    On each of `keys/validate-and-encrypt`, `strategies/create-with-key` and
    `strategies/composite/add-key`, replace the single `{ key: "validate-key", calls: 1 }` leg with
    TWO labelled legs:
    - `{ key: "validate-key", calls: 1, branch: "default-venue" }`
    - `{ key: "validate-key-serialized", calls: 1, branch: "serialized-venue" }`

    Leave `encrypt-key` and (on `validate-and-encrypt`) `process-key-unified-dormant` UNLABELLED.
    An unlabelled leg is shared and charged to every branch — which is correct: the encrypt call
    runs whichever venue arm was taken.

    Label by CAPABILITY, not by venue family. `"ccxt"` would be wrong twice over — sFOX is not
    ccxt, and a second serialized venue would have no home. `default-venue` / `serialized-venue`
    mirrors the `VENUE_CAPABILITIES.serialized` flag the selector reads.

    Above each changed row write a comment in the voice of the finalize-wizard block: name the two
    branches, state WHY they are exclusive (one request carries one `exchange`, and
    `budgetKeyFor(exchange)` in `analytics-client.ts` returns exactly one key for it — a row that
    summed both would charge a path no request takes), and state that the shared legs are
    deliberately unlabelled.

    ⛔ Do not change `expectedMaxDurationS` on any row. ⛔ Do not touch the finalize-wizard row.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` clean.
    - Source assertion: `grep -c 'branch: "serialized-venue"' src/lib/resilient-fetch.ts` returns 3.
    - Source assertion: `grep -c 'branch: "default-venue"' src/lib/resilient-fetch.ts` returns 3.
    - Source assertion: within the three changed rows, `encrypt-key` and
      `process-key-unified-dormant` legs carry NO `branch:` key (read the rows).
    - `npx vitest run src/lib/seam-budgets.invariant.test.ts` is EXPECTED to be RED at this point
      (SC-4d deep equality and the multiBranch roster). Paste the two failing assertion names —
      they are the guards working, and Task 3 clears them.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Three routes declare two exclusive venue branches; the shared legs stay unlabelled; the invariant test reds in exactly the two places that model the change.</done>
</task>

<task type="auto">
  <name>Task 2: budgetKeyFor(exchange) — the deliberate divergence, and the one literal it replaces</name>
  <files>src/lib/analytics-client.ts, src/lib/analytics-client.test.ts</files>
  <read_first>
    - `src/lib/analytics-client.ts` `:300-352` (`analyticsRequest`'s docblock, including the
      TESTS-ONLY status of `timeoutMs`) and `:660-687` (`validateKey`, the tenant-ordering comment
      above it, and the `budgetKey: "validate-key"` literal at `:684`).
    - `src/lib/process-key-client.ts:111-136` — the analog, verbatim, including its `never`
      assignment and its "Fail loud per Rule 12 ... There is no correct silent answer here"
      comment. You are diverging from this; read what you are diverging from.
    - `src/lib/resilient-fetch.ts:95-110` — the T-140-01 note: "MODULE CONSTANT — never interpolate
      user input into it ... a user-influenced breaker key is a trivial cross-tenant denial of
      service".
    - `.planning/phases/153-…/153-PATTERNS.md` §⭐4 "⚠️ Invert the `default` arm for the venue
      variant"; `.planning/phases/153-…/153-RESEARCH.md` §Pattern 1 and §Security Domain rows V13
      and the "User-influenced breaker key" threat.
    - `src/lib/analytics-client.test.ts` `:24-45` (the `server-only` / `next/headers` mocks every
      describe in this file relies on) and `:661-730` (the SEAM-01 delegation describe — the
      resilientFetch-mocking style to mirror).
  </read_first>
  <action>
    **Export `budgetKeyFor(exchange: string | null | undefined): SeamBudgetKey`** from
    `analytics-client.ts`, declared immediately above `validateKey`. Implementation:
    return the serialized budget key when `venueIsSerialized(exchange)` is true, otherwise the
    default key. Two module-constant string literals, no template literal, no interpolation.

    **The docblock must record BOTH deliberate divergences, or a reviewer will "fix" them back:**
    1. **It never throws.** `process-key-client.ts`'s analog closes with
       `const _exhaustive: never = flowType; throw ...` because `FlowType` is a closed union owned
       by this codebase. `exchange` here is a caller-supplied `string` that arrives from a wizard
       form body — an unrecognised value is a normal input, not a programming error. `default:`
       returns the DEFAULT budget key, which is byte-identical to today's behaviour for every
       venue that is not serialized. Throwing would convert a typo into a 500.
    2. **It reads the capability, not a venue name.** No `switch` over venue strings, no
       `if (exchange === "mt5")`. `VENUE_CAPABILITIES.serialized` is the single fact, and a second
       serialized venue is covered by editing that record — never this function. This is the
       instance-not-class defect the P140 campaign paid 37 scrapped commits for.
    Also state T-140-01 explicitly: the return value is one of two module constants and reaches
    `breakerKeysFor(budgetKey)`; ⛔ never build it as `` `validate-key-${exchange}` ``, which would
    let one caller shard the breaker so it never trips, or mint a key that trips for a cohort.

    **Wire the one literal:** `validateKey`'s `{ budgetKey: "validate-key", ... }` at `:684`
    becomes `{ budgetKey: budgetKeyFor(exchange), ... }`. Nothing else in `validateKey` changes.
    ⛔ Do NOT add a `timeoutMs` option at this or any call site — the override is TESTS ONLY since
    140-05 and is invisible to SC-4b's arithmetic, so it would grant a budget no invariant checks.

    **Tests in `analytics-client.test.ts`** (a new `describe`, using the file's existing mocks):
    - A HAND-TYPED expectation table over all six supported venues — `binance`, `okx`, `bybit`,
      `deribit`, `sfox` -> `"validate-key"`; `mt5` -> `"validate-key-serialized"`. Type the
      expected key as a literal in the test; ⛔ do not derive it from `VENUE_CAPABILITIES`.
    - The fallback cases, each its own assertion with its own message: an unknown venue string
      (`"kraken"`), the empty string, `null`, `undefined`, and a mixed-case value (`"MT5"` — the
      predicate lowercases, so this must select the SERIALIZED key; if it does not, the wizard's
      uppercase display value would silently spend the short budget).
    - A no-throw assertion: `expect(() => budgetKeyFor("../../etc/passwd")).not.toThrow()`, and the
      returned value is `"validate-key"`.
    - A CLASS assertion that can actually fail: read this file's own source from disk
      (`readFileSync`) and assert that the `budgetKeyFor` function body contains no venue-name
      string literal — scan for `mt5`, `binance`, `okx`, `bybit`, `deribit`, `sfox`, case
      insensitively, and expect zero matches inside the function body. Comment-strip is not needed
      if you bound the scan to the function body between its signature and its closing brace; state
      the bound in a comment. This is the falsifier for "an instance fix wearing a class fix's
      name" (VALIDATION ledger's "prefer the second member of a class" rule).
    - A wiring assertion: mock `resilientFetch`, call `validateKey("mt5", ...)`, and assert the
      `budgetKey` the core received is `"validate-key-serialized"`; repeat with `"binance"` and
      assert `"validate-key"`. Testing the helper is not testing that the call site invokes it.
    - A cross-module agreement assertion: `SEAM_BUDGETS[budgetKeyFor("mt5")].timeoutMs` is
      `120_000` (hand-typed) and `SEAM_BUDGETS[budgetKeyFor("binance")].timeoutMs` is `30_000`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/analytics-client.test.ts` GREEN. Paste the summary line.
    - Behaviour assertions, run and pasted: the six-venue table, the five fallback cases, and the
      two wiring assertions.
    - Falsifiability, RUN and PASTED: change `budgetKeyFor` to return `"validate-key"`
      unconditionally. The `mt5` table row AND the `mt5` wiring assertion must both red — the
      wiring one is the point (a green helper with a dead call site is the failure this catches).
      Paste both, then revert.
    - Falsifiability, RUN and PASTED: rewrite `budgetKeyFor` as `exchange === "mt5" ? ... : ...`.
      The source-scan class assertion must RED. Paste it, then revert.
    - Source assertion: `grep -n 'budgetKey: "validate-key"' src/lib/analytics-client.ts` returns
      nothing.
    - Source assertion: `grep -c 'validate-key-\\${' src/lib/analytics-client.ts` returns 0 — no
      interpolated budget key anywhere (T-140-01).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/analytics-client.test.ts</automated>
  </verify>
  <done>`budgetKeyFor` selects by capability, never throws, carries no venue literal, and `validateKey` spends the key it returns — proven at the call site, not only in the helper.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive every number seam-budgets.invariant.test.ts narrates</name>
  <files>src/lib/seam-budgets.invariant.test.ts</files>
  <read_first>
    - `src/lib/seam-budgets.invariant.test.ts` `:80-131` (the header headroom table and the prose
      around it — including the "create-with-key remains the two-call shape the old table described
      (38 500 / 8 500 / 55 500 ms)" sentence), `:386-469` (`EXPECTED_ROUTE_BUDGETS`), `:566-584`
      (`branchesOf`), `:596-664` (the SC-4b arithmetic), `:700-720` (SC-4d deep equality),
      `:790-845` (the two hand-typed literal oracles and the "THE ORACLE IS A LITERAL, NOT THE
      FORMULA" doctrine), `:900-932` (the two-branch assertion and the multiBranch roster).
    - `.planning/phases/153-…/153-RESEARCH.md` §Table B and §Table C rows 13, 14, 15.
      ⚠️ **Table B is arithmetically inconsistent and must NOT be copied.** Its
      `validate-and-encrypt` mt5 rows (222 750 / 12 750 / 248 250) are computed at **120 000 ms**
      while its `create-with-key` / `add-key` mt5 rows (128 500 / 8 500 / 145 500) are computed at
      **90 000 ms**. Its own footnote says the figures were hand-computed and never executed.
      Derive from the test run.
  </read_first>
  <action>
    **(a) Mirror the new legs into `EXPECTED_ROUTE_BUDGETS`** byte-for-byte, in the same order they
    appear in `SEAM_ROUTE_BUDGETS`. This map is a hand-typed roster, not a re-read of the table —
    it is the only assertion that can see a leg being DROPPED.

    **(b) Move the multiBranch roster** at `:919-932`. It asserts
    `multiBranch.map(([path]) => path)` equals exactly `[FINALIZE_WIZARD_ROUTE]` and its comment
    says "Hand-typed 1: exactly one row is multi-branch today". Four rows are multi-branch now.
    The expected array is compared with `toEqual`, so ORDER matters and it follows declaration
    order in `SEAM_ROUTE_BUDGETS`: validate-and-encrypt, create-with-key, composite/add-key,
    finalize-wizard. Rewrite the comment to "Hand-typed 4". ⛔ Do not replace the roster with a
    `.length` check — a length is green under a swap.

    **(c) Add a hand-typed literal oracle for the new worst branch**, beside the existing
    `keys/sync` (56 000) and `finalize-wizard` composite (277 500) oracles and in the same voice.
    Assert that `validate-and-encrypt`'s `serialized-venue` branch in the FAILING state is
    `248_250` ms, with the arithmetic spelled out in the comment: 120 000 + 30 000 + 60 000 =
    210 000 request, plus 3 legs x 3 commands x (1+0) rounds x 4 250 ms x 1 call = 38 250 store.
    ⭐ THE ORACLE IS A LITERAL, NOT THE FORMULA — do not compute the expected value from
    `SEAM_BUDGETS` inside the test. State what a movement means: a LOWER number means a leg stopped
    being charged; a HIGHER one means a budget or a store constant moved, and the 300 000 ms
    ceiling leaves 51 750 ms of headroom on this branch.

    **(d) Update the header headroom table (`:85-131`) from the RUN, not from Table B.** The
    `validate-and-encrypt` column is now the MAX across its two branches, i.e. the serialized one.
    Cross-check figures (verify each against the test run before writing them down):

    | route | branch | closed | open | failing |
    |---|---|---|---|---|
    | validate-and-encrypt | serialized-venue | 222 750 | 12 750 | 248 250 |
    | validate-and-encrypt | default-venue | 132 750 | 12 750 | 158 250 |
    | create-with-key / composite-add-key | serialized-venue | 158 500 | 8 500 | 175 500 |
    | create-with-key / composite-add-key | default-venue | 68 500 | 8 500 | 85 500 |
    | finalize-wizard | composite | 192 500 | 42 500 | 277 500 (UNCHANGED) |

    The prose claim that finalize-wizard's composite is the tightest row in the table at 22 500 ms
    of headroom REMAINS TRUE (the new worst is 248 250 with 51 750 of headroom) — say so
    explicitly rather than leaving the reader to recompute.

    ⚠️ **A pre-existing false sentence sits in this table's prose**: "create-with-key remains the
    two-call shape the old table described (38 500 / 8 500 / 55 500 ms)". Those are
    finalize-wizard's SINGLE-KEY branch numbers, not create-with-key's. This plan re-branches
    create-with-key, so leaving the sentence would be shipping a wrong statement about a row this
    commit changed. Correct it to the figures the run produces and note in the comment that the
    incumbent numbers were wrong before this change, so the correction is not mistaken for a
    consequence of it.

    ⛔ Do NOT touch the `56_000` or `277_500` oracles — every leg on those branches is `retries: 0`
    and this change cannot move them. If either moves, stop: something else broke.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/seam-budgets.invariant.test.ts` GREEN. Paste the summary line.
    - Paste the per-route worst-case figures the run reports (the SC-4b failure messages embed
      them; run once with a deliberately lowered ceiling if needed to surface them, then revert) and
      show they match what was written into the header table.
    - Behaviour assertion, run and pasted: the new `248_250` serialized-branch oracle.
    - Behaviour assertion, run and pasted: `56_000` and `277_500` are both still green.
    - Falsifiability, RUN and PASTED: delete the `branch` labels from `create-with-key`'s two
      validate legs. The multiBranch roster must RED (the route drops out of the four). Paste it,
      then revert. ⚠️ Note in the summary whether SC-4b alone would have noticed — it would not,
      because summing over-states, and that is exactly why the roster is hand-typed.
    - Source assertion: `grep -c '38 500 / 8 500 / 55 500' src/lib/seam-budgets.invariant.test.ts`
      returns 0.
    - Source assertion: `grep -c 'Hand-typed 1: exactly one row' src/lib/seam-budgets.invariant.test.ts`
      returns 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/seam-budgets.invariant.test.ts src/lib/seam-constants.pin.test.ts</automated>
  </verify>
  <done>SC-4a/b/d/e/f are green with the new legs, the multiBranch roster names four rows, a new hand-typed oracle pins the serialized branch at 248 250 ms, and the header table states numbers that were re-derived from a run.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next route body `exchange` | the caller-supplied venue string that now selects a budget |
| `budgetKeyFor` -> `breakerKeysFor(budgetKey)` -> Upstash | the only place a wire value could become a breaker key |
| Next route -> Railway seam | the wall-clock the route's Vercel ceiling is checked against |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.4-05 | Denial of Service | `budgetKeyFor(exchange)` — user-influenced breaker key (T-140-01) | mitigate | Returns one of TWO module-constant literals; the venue never reaches the returned string. Asserted by the `validate-key-\${` grep and by the no-throw / path-traversal-string case. |
| T-153.4-06 | Denial of Service | an unknown venue selecting the LONG budget | mitigate | `venueIsSerialized` answers `false` for null / unknown / absent-row (153.1-02), so an unrecognised string falls to the 30 000 ms row. A caller cannot buy a 120 s lambda hold by inventing a venue name. Asserted by the fallback cases. |
| T-153.4-07 | Denial of Service | a route breaching its 300 s Vercel ceiling on the new branch | mitigate | SC-4b recomputes per branch; the serialized branch's failing worst case is pinned at a hand-typed 248 250 ms against a 300 000 ms ceiling. |
| T-153.4-08 | Tampering | `branch` labels deleted, silently restoring the SUM | mitigate | The hand-typed four-row multiBranch roster reds; the falsifiability run proves it. SC-4b alone would not notice, because a SUM over-states. |
| T-153.4-09 | Denial of Service | a longer serialized budget holding a Vercel invocation ~4x longer | **accept (recorded)** | Carried from plan 01 / RESEARCH §Security Domain: the `100/hour` per-tenant limiter on `/api/validate-key` is the only bound, and a per-IP cap is no defence against a distributed caller. Recorded, NOT mitigated. |
| T-153.4-SC | Tampering | npm/pip/cargo installs | mitigate | Not engaged — this plan installs nothing (RESEARCH §Package Legitimacy Audit: zero-package phase). |
</threat_model>

<verification>
- `npx vitest run src/lib/analytics-client.test.ts src/lib/seam-budgets.invariant.test.ts src/lib/seam-constants.pin.test.ts src/lib/seam-retry-registry.test.ts`
- `npx tsc --noEmit`
- `npm run lint`
- ⚠️ CI is Node 22, local is Node 25 — reproduce a CI-only failure with
  `PATH=/opt/homebrew/opt/node@22/bin:$PATH`.
</verification>

<success_criteria>
- `validateKey("mt5", ...)` spends `validate-key-serialized`; every other venue, and every unknown string, spends `validate-key`.
- `budgetKeyFor` never throws and contains no venue-name literal — both asserted, both falsified once.
- The three validate routes declare `default-venue` / `serialized-venue` branches; `encrypt-key` and the dormant leg stay shared.
- SC-4a/b/d/e/f green; the multiBranch roster names four rows; a hand-typed 248 250 ms oracle exists.
- The header headroom table states run-derived figures, and the pre-existing false create-with-key sentence is corrected.
</success_criteria>

<output>
Create `.planning/phases/153.4-wizform-budget-venue-aware-budget-honest-long-wait/153.4-02-SUMMARY.md` when done
</output>
