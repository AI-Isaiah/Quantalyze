---
phase: 153.4-wizform-budget-venue-aware-budget-honest-long-wait
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resilient-fetch.ts
  - src/lib/seam-constants.pin.test.ts
  - src/lib/seam-retry-registry.ts
  - src/lib/seam-retry-registry.test.ts
autonomous: true
requirements: [WIZFORM-05]
must_haves:
  truths:
    - "A serialized-venue validate-key call is granted 120 000 ms, and that number is recorded as PROVISIONAL with the phase that tightens it"
    - "The breaker lock tombstone outlives the new longest budget exactly — (30 + 90) x 1000 >= 120 000 — and both the literal pin and the derived pin say so"
    - "Landing the budget row WITHOUT the tombstone raise reddens an assertion that does not restate the budget as a literal"
    - "The new row carries retries 0 and a written NO-retry audit verdict; it is absent from RETRY_SAFE_ANALYTICS"
    - "The client ceiling exceeds the server worst case (105 s) by a margin, asserted rather than narrated"
  artifacts:
    - path: "src/lib/resilient-fetch.ts"
      provides: "SeamBudgetKey 'validate-key-serialized' + its 120 000 ms row + BREAKER_LOCK_TOMBSTONE_S 90"
      contains: "validate-key-serialized"
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "the four hand-typed oracle maps extended, the tombstone literal pin at 90, both A-25 assertions re-cut, the client>server pin"
      contains: "105_000"
    - path: "src/lib/seam-retry-registry.ts"
      provides: "a NO-retry audit verdict for the serialized row"
      contains: "validate-key-serialized"
  key_links:
    - from: "src/lib/resilient-fetch.ts SEAM_BUDGETS"
      to: "BREAKER_LOCK_TOMBSTONE_S"
      via: "A-25 coupling: (COOLDOWN + TOMBSTONE) * 1000 >= max(timeoutMs)"
      pattern: "BREAKER_LOCK_TOMBSTONE_S"
    - from: "src/lib/seam-retry-registry.ts RETRY_AUDIT_NO_ANALYTICS"
      to: "SeamBudgetKey"
      via: "compile-time exhaustiveness in seam-retry-registry.test.ts"
      pattern: "EXPECTED_ALL_ANALYTICS_KEYS"
---

<objective>
Mint the `validate-key-serialized` seam budget at **120 000 ms** and pay for it, in the same
commit, by moving `BREAKER_LOCK_TOMBSTONE_S` 60 -> 90 (D-26). Then move every hand-typed pin and
prose restatement that the new row falsifies.

Purpose: WIZFORM-05's client leg is a single number, but that number is coupled to a breaker
invariant (A-25) whose incumbent pin restates both sides as literals and therefore cannot see the
coupling break (RESEARCH Finding 1, Pitfall 1). Landing the row alone would be green and wrong.

Output: a fourth budget magnitude in `SEAM_BUDGETS`, a tombstone that spans it exactly, and a pin
surface that reddens if either half moves without the other.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153.4-wizform-budget-venue-aware-budget-honest-long-wait/153.4-CONTEXT.md

<interfaces>
<!-- Extracted from HEAD 2026-08-09. Use these directly; do not re-explore. -->

src/lib/resilient-fetch.ts
- `export const BREAKER_COOLDOWN_S = 30;`                      (:252)
- `export const BREAKER_LOCK_TOMBSTONE_S = 60;`                (:274, docblock :262-273)
- `export type SeamBudgetKey = "validate-key" | "encrypt-key" | "bridge" | "simulator"
   | "portfolio-optimizer" | "optimize-weights" | "match-eval" | "match-recompute"
   | "portfolio-analytics" | "process-key-enqueue" | "process-key-sync" | "keys-permissions"
   | "process-key-unified-dormant";`                           (:415-428)
- `export const SEAM_BUDGETS: Record<SeamBudgetKey, { timeoutMs: number;
   dependencies: readonly SeamServiceDependency[]; retries: number; notes: string }>`  (:530-)
- the `"validate-key"` row at `:537-548`: `timeoutMs: 30_000`, `dependencies: ["mt5-gateway"]`,
  `retries: SEAM_RETRIES`, `notes: "Live exchange auth probe ..."`

src/lib/seam-retry-registry.ts
- `export const RETRY_SAFE_ANALYTICS` (:421-)  — four YES verdicts, objects with `.evidence`
- `export const RETRY_AUDIT_NO_ANALYTICS: Readonly<Partial<Record<SeamBudgetKey, string>>>`
  (:518-543) — five NO verdicts, the VALUE is the prose; docblock at `:508-517` says
  "Exactly FIVE entries; together with the four YES entries they cover all nine analytics
  wrappers (the exhaustiveness pin)."

src/lib/seam-retry-registry.test.ts
- `type RouteBudgetKey = "keys-permissions" | "process-key-enqueue" | "process-key-sync"
   | "process-key-unified-dormant";`                            (:113-117)
- `const EXPECTED_ALL_ANALYTICS_KEYS = [...] as const satisfies readonly SeamBudgetKey[];`
  (:119-130) — nine keys, SORTED
- `type _MissingAnalyticsVerdict = Exclude<SeamBudgetKey,
   (typeof EXPECTED_ALL_ANALYTICS_KEYS)[number] | RouteBudgetKey>;` + a
  `extends never ? true : never` binding  (:132-139)  <- a 14th SeamBudgetKey is a COMPILE ERROR
  until it is classified
- `it("YES∪NO analytics keys cover ALL nine wrappers", ...)`     (:186-192)
- `const EXPECTED_ANALYTICS_NO_EVIDENCE = 5;`                    (:441)

src/lib/seam-constants.pin.test.ts   (the ONE RULE: every expectation is a hand-typed literal)
- `EXPECTED_TIMEOUT_MS`   (:97-112)   13 rows
- `EXPECTED_BUDGET_KEYS`  (:119-133)  13 keys, asserted as SORTED SET equality at `:263-272`
- `EXPECTED_DEPENDENCIES` (:157-171)
- `EXPECTED_RETRIES`      (:185-199)
- registry size literals  (:411-444)  incl. `RETRY_AUDIT_NO_ANALYTICS ... .toBe(5)` and the
  prose "Together with the 4 above this is the whole 9-wrapper analytics surface"
- `it("uses exactly three magnitudes — 15s, 30s and 60s — spelled out", ...)`  (:558-565)
- `it("BREAKER_LOCK_TOMBSTONE_S is the literal 60", ...)`                       (:695-703)
- `it("A-25: the tombstone outlives the longest seam budget", ...)`             (:705-717)
  asserts `BREAKER_LOCK_TOMBSTONE_S * 1_000 >= 60_000 - 30_000`  <- both sides hand-typed
- ⭐ the DERIVED A-25 assertion + its SELF-TEST, landed by **plan 153.1-01 Task 1**, sitting
  immediately after `:717`. It computes `Math.max(...Object.values(SEAM_BUDGETS).map(b =>
  b.timeoutMs))` and compares it to a ceiling built from the hand-typed literals `(60 + 30) *
  1_000`. Its SELF-TEST uses a synthetic table containing `120_000` to prove the ceiling can be
  exceeded.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The 120 000 ms row and the tombstone raise — ONE commit (D-26)</name>
  <files>src/lib/resilient-fetch.ts</files>
  <read_first>
    - `src/lib/resilient-fetch.ts` `:95-110` (the T-140-01 "MODULE CONSTANT — never interpolate
      user input" note on `BREAKER_KEY`), `:240-274` (`BREAKER_COOLDOWN_S`, then
      `BREAKER_LOCK_TOMBSTONE_S` and the docblock sentence "60 s, because the guard must span the
      longest budget in `SEAM_BUDGETS` (`process-key-sync`, 60 000 ms) ... = 90 s >= 60 s"),
      `:415-428` (the `SeamBudgetKey` union), `:530-560` (the `validate-key` and `encrypt-key`
      rows — the row shape and the `notes` voice to clone).
    - `.planning/phases/153-…/153-PATTERNS.md` §⭐4, the "`SEAM_BUDGETS` row to clone" block.
    - `.planning/phases/153-…/153-RESEARCH.md` §Finding 1 (the A-25 table: at 120 000 ms the
      tombstone MUST rise to >= 90) and §Pitfall 5 (retrying into an open breaker).
    - `.planning/phases/153.3-…/153.3-03-PLAN.md` `:119-128` and `153.3-04-PLAN.md` `:114-120` —
      the server chain 45 000 ms IPC < 55 s rpyc < 60 s stage < 75 s deadline, plus a 20 s bounded
      lease wait and a 10 s release outside the deadline: **server worst case 105 s**.
  </read_first>
  <action>
    Three edits, ONE task, ONE commit. D-26 makes the budget row and the tombstone raise
    inseparable: land the row alone and A-25 is false while every existing assertion is green.

    **(a) Widen the `SeamBudgetKey` union** with `"validate-key-serialized"`, placed immediately
    after `"validate-key"`.

    Name it after the CAPABILITY, not the venue. `serialized` is the `VENUE_CAPABILITIES` flag
    Phase 153.1 mints for venues whose calls funnel through one terminal lease. A key named
    `validate-key-mt5` would be the instance-not-class defect this repo has paid for once, and it
    would need renaming the day a second serialized venue arrives.

    **(b) Add the `SEAM_BUDGETS` row**, immediately after `"validate-key"`:
    - `timeoutMs: 120_000`
    - `dependencies: ["mt5-gateway"]` — the same declaration the incumbent row carries and for the
      same reason (`exchange.py`'s `_validate_mt5_key` raises `MT5_GATEWAY_UNREACHABLE` as a 503
      naming that dependency). Copy the incumbent row's dependency comment and adapt it; do not
      invent a new dependency.
    - `retries: SEAM_RETRIES` — the module constant, which is `0`. ⛔ Never a literal `1` here, and
      ⛔ never an entry in `RETRY_SAFE_ANALYTICS` (Task 3 writes the NO verdict). RESEARCH Pitfall
      5: in the OPEN state `CircuitOpenError` is thrown BEFORE `fetch`, so a retry re-runs
      `isBreakerOpen`, charges a second store round and throws again — retry here is not merely
      discouraged, it is structurally wasteful, and it is how one slow venue takes down every other
      user's submits (D-07).
    - `notes:` must state, in the voice of the neighbouring rows: (i) that this row is spent by
      venues whose `VENUE_CAPABILITIES.serialized` is true, selected by `budgetKeyFor(exchange)` in
      `analytics-client.ts` (plan 153.4-02); (ii) that **120 000 ms is PROVISIONAL (D-27)** — it is
      the founder's stated staleness tolerance ("if it is 2 minutes old, that is fine"), not a
      measurement, because no uncensored successful MT5 validation exists anywhere; (iii) that
      Phase 153.3's per-stage `stage`/`duration_ms` instrumentation is what will produce a real
      p50/p95 and **Phase 155 (MT5-VERIFY)** is the owner of tightening it; (iv) that the server
      worst case it must contain is **105 s** = 20 s bounded lease wait + 75 s end-to-end deadline
      + 10 s release outside the deadline, leaving 15 s of margin.
    ⛔ Do not write "35–70 s" anywhere. That figure is derived from the old timeout constants and
    says so itself (`153-UI-SPEC.md:226`); citing it as evidence is the fabrication D-27 forbids.

    **(c) Raise `BREAKER_LOCK_TOMBSTONE_S` from 60 to 90 and rewrite its docblock.** The existing
    sentence — "60 s, because the guard must span the longest budget in `SEAM_BUDGETS`
    (`process-key-sync`, 60 000 ms) ... `BREAKER_COOLDOWN_S + BREAKER_LOCK_TOMBSTONE_S = 90 s >= 60
    s`" — becomes false the instant (b) lands. Replace it with the new arithmetic, naming
    `validate-key-serialized` at 120 000 ms as the new longest budget and stating the inequality
    holds **exactly**: `(30 + 90) x 1000 = 120 000 >= 120 000`. Say that the equality is tight on
    purpose, so any further budget raise must move this constant again, and that the derived pin in
    `seam-constants.pin.test.ts` is what enforces it.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` reports the EXPECTED compile error in `src/lib/seam-retry-registry.test.ts`
      (`_MissingAnalyticsVerdict`) and no others in `src/lib/resilient-fetch.ts`. Paste it. That
      error is the exhaustiveness fence working; Task 3 clears it.
    - Source assertion: `grep -n 'validate-key-serialized' src/lib/resilient-fetch.ts` returns
      exactly 2 lines at this point (the union member and the row key).
    - Source assertion: `grep -n 'BREAKER_LOCK_TOMBSTONE_S = ' src/lib/resilient-fetch.ts` shows
      `= 90`.
    - Source assertion: `sed -n '240,290p' src/lib/resilient-fetch.ts | grep -c 'process-key-sync'`
      returns `0` — the stale "the longest budget is process-key-sync" sentence is gone.
    - Source assertion: the new row's body contains `retries: SEAM_RETRIES` and contains neither
      `retries: 1` nor the substring `retriesOverride`.
    - `git diff --stat` shows exactly one file changed. The row and the tombstone are in the same
      commit — this is D-26 and it is not negotiable.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/seam-budgets.invariant.test.ts</automated>
  </verify>
  <done>`SEAM_BUDGETS` has a fourth magnitude at 120 000 ms declared on `mt5-gateway` with `retries: SEAM_RETRIES`, `BREAKER_LOCK_TOMBSTONE_S` is 90, and its docblock states the new coupling exactly.</done>
</task>

<task type="auto">
  <name>Task 2: Move all eleven pin sites in seam-constants.pin.test.ts, including the three that stay green while their premise breaks</name>
  <files>src/lib/seam-constants.pin.test.ts</files>
  <read_first>
    - `src/lib/seam-constants.pin.test.ts` — the file docblock's ONE RULE (`:36-60`); the four
      oracle maps and their docblocks (`:90-199`); the sorted-set-equality assertion and its `it`
      title (`:262-272`); the registry-size block (`:411-444`); the magnitudes assertion
      (`:558-565`); the breaker block (`:690-731`), which is the tombstone literal pin, the
      literal-vs-literal A-25 assertion, and A-14.
    - ⭐ the DERIVED A-25 assertion and its SELF-TEST landed by plan **153.1-01 Task 1**, sitting
      immediately below `:717`. Read them in full before editing — the SELF-TEST's synthetic value
      is a landmine (see the action).
    - `.planning/phases/153-…/153-RESEARCH.md` §Table C rows 4, 5, 7, 8, 9, 12 and §Pitfall 1.
    - `.planning/phases/153-…/153-PATTERNS.md` §⭐4 "Pin-test assertion styles to mirror" and
      §Shared Pattern **C**.
    - `.planning/phases/153-…/153-VALIDATION.md` §Oracle Independence (the two ⭐ bullets) and the
      Falsifiability Ledger row **SC-4**.
  </read_first>
  <action>
    Every expectation added here is a HAND-TYPED literal. ⛔ Never `Object.keys(...).length`,
    never a value read out of `SEAM_BUDGETS`.

    **The four oracle maps** — add a `"validate-key-serialized"` entry to each, and EXTEND the
    docblock prose rather than only adding a line (PATTERNS §⭐4):
    - `EXPECTED_TIMEOUT_MS` (`:97-112`): `120_000`.
    - `EXPECTED_BUDGET_KEYS` (`:119-133`): the key. Its `it` title at `:263` reads "declares
      exactly the **13** pinned budget keys" — that prose becomes false; move it to 14.
    - `EXPECTED_DEPENDENCIES` (`:157-171`): `["mt5-gateway"]`.
    - `EXPECTED_RETRIES` (`:185-199`): `0`. Add one clause to that map's docblock recording WHY
      this row is 0 and must stay 0 (D-07 / RESEARCH Pitfall 5), so a future reader does not read
      the 0 as an oversight.

    **The registry-size literals** (`:411-444`): `RETRY_AUDIT_NO_ANALYTICS` moves from `.toBe(5)`
    to `.toBe(6)`, its failure message gains `validate-key-serialized` in the named list, and the
    prose "Together with the 4 above this is the whole **9-wrapper** analytics surface" becomes
    **10-wrapper**. ⚠️ The message is what a future reader trusts; a count that moves while its own
    message still names five keys is the 141.2/D-03 failure this file already records.

    **The magnitudes assertion** (`:558-565`): its title says "uses exactly **three** magnitudes —
    15s, 30s and 60s". Rewrite the title and the comment to **four magnitudes — 15s, 30s, 60s and
    120s** and add the fourth representative, `expect(BUDGET_TABLE["validate-key-serialized"]
    ?.timeoutMs).toBe(120_000)`. RESEARCH Table C row 8: this is one of the three sites whose
    assertions keep passing while their own prose goes false.

    **The tombstone literal pin** (`:695-703`): `.toBe(60)` becomes `.toBe(90)`, and its comment
    gains the new arithmetic. ⚠️ This site is NOT in Table C — it was found by re-derivation. Do
    not skip it because the table did not list it.

    **The literal-vs-literal A-25 assertion** (`:705-717`): KEEP IT (D-19; VALIDATION §Oracle
    Independence's last two bullets — it catches the constants moving, the derived one catches the
    coupling breaking, and neither implies the other). Move its restatement of the longest budget
    from `60_000` to `120_000`, so the expression reads `>= 120_000 - 30_000`, and rewrite the
    comment: the longest budget is no longer `process-key-sync` at 60 000 ms but
    `validate-key-serialized` at 120 000 ms.

    **⭐ The DERIVED A-25 assertion (153.1-01) and its SELF-TEST — the landmine.** The derived
    assertion compares `Math.max` over `SEAM_BUDGETS` to a ceiling built from the hand-typed
    literals `(60 + 30) * 1_000`. With Task 1 landed it is RED, and the ONLY correct fix is to move
    the hand-typed `60` to `90` — never to weaken, relax or delete the assertion, and never to
    substitute `BREAKER_LOCK_TOMBSTONE_S` for the literal (that is the ONE RULE, and it is exactly
    what made the incumbent pin blind). Then:
    - Its **SELF-TEST asserts that a synthetic `120_000` budget EXCEEDS the `(60 + 30) * 1_000`
      ceiling.** At `(90 + 30) * 1_000` that is no longer true and the SELF-TEST becomes false.
      Move the synthetic value ABOVE the new ceiling (use `150_000`) and move the ceiling literal
      in the SELF-TEST too, keeping the shared reducer so both paths still exercise the same code.
    - Rewrite the derived assertion's failure message. It currently names "Phase 153.4 / D-26 —
      120 000 ms plus `BREAKER_LOCK_TOMBSTONE_S` 60 -> 90 in ONE commit" as future work and
      instructs the reader to move the hand-typed 60 to 90 when it lands. That has now landed:
      restate it as the standing rule ("any budget above `(TOMBSTONE + COOLDOWN) x 1000` must move
      the tombstone in the SAME commit; the equality is tight at 120 000 today") and keep the
      record that **D-18's 90 000 ms is SUPERSEDED by D-26** — D-18 let a test invariant pick a
      production timeout, and that reasoning was rejected.

    **The NEW client-vs-server assertion** — nothing asserts this today and it is the whole point
    of WIZFORM-05 (D-04). Add an `it` beside A-25:
    `expect(BUDGET_TABLE["validate-key-serialized"]?.timeoutMs).toBeGreaterThan(105_000)`.
    `105_000` is HAND-TYPED here with its derivation written in the comment: Phase 153.3 ships a
    20 s bounded lease acquisition + a 75 s end-to-end probe deadline + a 10 s release that runs
    OUTSIDE the deadline (`analytics-service/routers/exchange.py`,
    `services/mt5_concurrency.py`). ⛔ Do not import or parse any Python constant into a TS test.
    The message must say what a failure means: the client would abandon the request before the
    server's honest verdict can land, which is the exact inversion WIZFORM-05 exists to close, and
    that the fix is to raise the budget (and the tombstone with it) or to lower the server chain —
    never to lower this literal. Note that the Python side pins the same relationship independently
    from its own end (plans 153.3-03 and 153.3-04 assert `(lease + deadline + release) * 1000 <
    120_000`), so the two statements are independent oracles, not one restated twice.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/seam-constants.pin.test.ts` GREEN. Paste the summary line.
    - Falsifiability, RUN and PASTED (VALIDATION ledger SC-4): revert
      `BREAKER_LOCK_TOMBSTONE_S` to `60` in `resilient-fetch.ts` and re-run this file. The
      **derived** A-25 assertion must RED and the **literal-vs-literal** one must ALSO red (its
      restatement now reads `120_000`). Paste both failing assertions, then restore `90`.
      ⚠️ If only one reds, stop — the pair is not doing the two different jobs D-19 requires.
    - Falsifiability, RUN and PASTED: temporarily set the new row's `timeoutMs` to `100_000` —
      BELOW the 105 000 ms server worst case, i.e. the inversion re-created. The client>server
      assertion must RED. Paste it, then restore `120_000`.
    - Source assertion: `grep -c 'three magnitudes' src/lib/seam-constants.pin.test.ts` returns 0.
    - Source assertion: `grep -c '9-wrapper' src/lib/seam-constants.pin.test.ts` returns 0.
    - Source assertion: `grep -n '60_000 - 30_000' src/lib/seam-constants.pin.test.ts` returns
      nothing.
    - Source assertion: inside the DERIVED A-25 `it` block body, `BREAKER_LOCK_TOMBSTONE_S` and
      `BREAKER_COOLDOWN_S` appear ZERO times (read the block; the file uses both legitimately
      elsewhere).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/seam-constants.pin.test.ts</automated>
  </verify>
  <done>Eleven sites in this file moved together: four oracle maps, the "13 keys" title, the registry counts and their message, the magnitudes prose plus a fourth representative, the tombstone literal, both A-25 assertions, the derived SELF-TEST's synthetic value, and a new hand-typed client>server pin.</done>
</task>

<task type="auto">
  <name>Task 3: The NO-retry audit verdict, the exhaustiveness fence, and the counts that follow it</name>
  <files>src/lib/seam-retry-registry.ts, src/lib/seam-retry-registry.test.ts</files>
  <read_first>
    - `src/lib/seam-retry-registry.ts` `:40-60` (the registry's own contract, incl. the sentence
      about `EXPECTED_ALL_ANALYTICS_KEYS`), `:505-543` (`RETRY_AUDIT_NO_ANALYTICS`, its docblock
      "Exactly FIVE entries ... all nine analytics wrappers", and the `validate-key` verdict — the
      voice to mirror).
    - `src/lib/seam-retry-registry.test.ts` `:105-140` (`RouteBudgetKey`,
      `EXPECTED_ALL_ANALYTICS_KEYS`, the two `Exclude`-based compile-time exhaustiveness
      bindings), `:186-192` ("ALL nine wrappers"), `:400-445` (the four per-map evidence-count
      literals and the docblock explaining why the breakdown is four literals and not one sum).
    - `.planning/phases/153-…/153-RESEARCH.md` §Table C row 11 and §Pitfall 5.
  </read_first>
  <action>
    **`seam-retry-registry.ts`** — add a `"validate-key-serialized"` verdict to
    `RETRY_AUDIT_NO_ANALYTICS`, immediately after `"validate-key"`. The prose is the audit, so
    write the audit, not a cross-reference: the same live-credential exchange probe as
    `validate-key` and non-idempotent for the same reason, PLUS the serialized-venue amplification
    — the row's `mt5-gateway` dependency plus the global `breaker:railway` mean a retry re-runs
    `isBreakerOpen` before any fetch in the OPEN state, charges a second store round and throws
    again, and a retried 120 000 ms leg would double the wall-clock this route's Vercel ceiling is
    checked against in `seam-budgets.invariant.test.ts`. Name D-07 as the standing prohibition.
    ⛔ Do not add the key to `RETRY_SAFE_ANALYTICS`. ⛔ Do not cite a file:line coordinate inside
    the evidence string — a guard in the test file scans these strings for coordinates.

    Rewrite the `RETRY_AUDIT_NO_ANALYTICS` docblock: "Exactly FIVE entries; together with the four
    YES entries they cover all nine analytics wrappers" becomes SIX / ten.

    **`seam-retry-registry.test.ts`** — three moves:
    - `EXPECTED_ALL_ANALYTICS_KEYS` gains `"validate-key-serialized"`. ⚠️ The array is SORTED and
      the union assertion compares sorted arrays: insert it in sort order (immediately after
      `"validate-key"`). This is also what clears the `_MissingAnalyticsVerdict` compile error
      Task 1 deliberately produced — the key is an analytics-wrapper budget, not a route budget, so
      it does NOT join `RouteBudgetKey`.
    - The `it("YES∪NO analytics keys cover ALL nine wrappers")` title becomes **ten**.
    - `EXPECTED_ANALYTICS_NO_EVIDENCE` moves 5 -> 6, and its trailing comment gains the new key.
      ⚠️ Its docblock explains at length why the breakdown is four separate literals rather than one
      sum — a compensating pair of moves once kept a total green while its own message went false.
      Move the one literal that changed and leave the other three.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` CLEAN across the repo — the `_MissingAnalyticsVerdict` error from Task 1
      is gone. Paste the clean run.
    - `npx vitest run src/lib/seam-retry-registry.test.ts` GREEN. Paste the summary line.
    - Behaviour assertion, run and pasted: `RETRY_SAFE_ANALYTICS["validate-key-serialized"]` is
      `undefined` and `RETRY_AUDIT_NO_ANALYTICS["validate-key-serialized"]` is a non-empty string.
    - Falsifiability, RUN and PASTED: move the key from `RETRY_AUDIT_NO_ANALYTICS` into
      `RETRY_SAFE_ANALYTICS` with `retries: 1`. The disjointness/agreement assertions in
      `seam-retry-registry.test.ts` AND the row-vs-registry agreement pin in
      `seam-constants.pin.test.ts` must both red. Paste them, then revert.
    - Source assertion: `grep -c 'Exactly FIVE entries' src/lib/seam-retry-registry.ts` returns 0.
    - Source assertion: `grep -c 'ALL nine wrappers' src/lib/seam-retry-registry.test.ts` returns 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/seam-retry-registry.test.ts src/lib/seam-constants.pin.test.ts</automated>
  </verify>
  <done>The new row carries a written NO-retry verdict, the ten-wrapper exhaustiveness holds at compile time and at runtime, and every count literal that follows from it moved with its own message.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Next route (`create-with-key`, `composite/add-key`, `validate-and-encrypt`) | the caller-supplied `exchange` string crosses here |
| Next route -> Railway analytics seam | the budget, the breaker keys and the deadline live here |
| seam -> Upstash breaker store | the lock/tombstone lifetime this plan changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.4-01 | Denial of Service | the new `SEAM_BUDGETS` key | mitigate | The key is a MODULE CONSTANT in a closed union. T-140-01: a user-influenced breaker key is a trivial cross-tenant DoS. `breakerKeysFor` derives from the row's declared `dependencies` plus `breaker:railway`; nothing on this row is built from a wire value. Task 1's acceptance greps that only two occurrences of the literal exist in `resilient-fetch.ts`. |
| T-153.4-02 | Denial of Service | retry amplification into an open breaker | mitigate | `retries: SEAM_RETRIES` (0) on the row, a written NO verdict in `RETRY_AUDIT_NO_ANALYTICS`, absence from `RETRY_SAFE_ANALYTICS`, and the row-vs-registry agreement pin. Falsifiability run proves the pair reds when the verdict is flipped. |
| T-153.4-03 | Denial of Service | a 120 s budget holds a Vercel lambda 4x longer on the serialized arm | **accept (recorded)** | RESEARCH §Security Domain records the only bound: the `100/hour` per-tenant limiter on `/api/validate-key` (`exchange.py`). A per-IP cap is no defence against a distributed caller. This is the same accepted exposure `process-key-sync`'s ME-04 note carries. ⛔ Recorded, NOT mitigated — do not write "mitigated" anywhere in the row's notes. |
| T-153.4-04 | Tampering | the A-25 breaker guard silently losing its coupling | mitigate | The tombstone raise lands in the same commit as the budget (D-26), and the derived assertion (153.1-01) reds on any future budget above `(TOMBSTONE + COOLDOWN) x 1000`. The falsifiability run in Task 2 is the proof it can fail. |
| T-153.4-SC | Tampering | npm/pip/cargo installs | mitigate | **Not engaged — this plan installs nothing.** RESEARCH §Package Legitimacy Audit records the phase as zero-package. If that ever changes, the Package Legitimacy Gate runs first. |
</threat_model>

<verification>
- `npx vitest run src/lib/seam-constants.pin.test.ts src/lib/seam-retry-registry.test.ts src/lib/seam-budgets.invariant.test.ts`
- `npx tsc --noEmit`
- `npm run lint`
- ⚠️ CI is Node 22, local is Node 25. A CI-only failure is not a flake — reproduce with
  `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run <file>`.
</verification>

<success_criteria>
- `SEAM_BUDGETS["validate-key-serialized"].timeoutMs === 120_000`, `dependencies === ["mt5-gateway"]`, `retries === 0`.
- `BREAKER_LOCK_TOMBSTONE_S === 90` and its docblock states `(30 + 90) x 1000 = 120 000 >= 120 000`.
- Both A-25 assertions exist and both red when the tombstone is reverted to 60 (observed, pasted).
- A hand-typed assertion pins the client budget above the 105 s server worst case.
- The key carries a NO-retry verdict and is absent from `RETRY_SAFE_ANALYTICS`.
- Eleven pin sites in `seam-constants.pin.test.ts` and four in the retry registry moved, including every prose restatement.
</success_criteria>

<output>
Create `.planning/phases/153.4-wizform-budget-venue-aware-budget-honest-long-wait/153.4-01-SUMMARY.md` when done
</output>
