---
phase: 153.1-wizform-codes-honest-codes-venue-capability-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/wizardErrors.invariant.test.ts
  - src/lib/seam-constants.pin.test.ts
autonomous: true
requirements: [WIZFORM-02]
must_haves:
  truths:
    - "The roster scanner can read a roster declared with a line break between `(` and `[` — the shape SubmitStep.tsx uses"
    - "The emitter scanner can read a coded rejection whose status is not 400"
    - "The emitter scanner can read a coded rejection whose `error` value is a template literal containing `${...}`"
    - "The emitter scanner still refuses `{ error, code }` key order — proven by a negative SELF-TEST, not by absence of evidence"
    - "A seam budget longer than (tombstone + cooldown) reddens an assertion that does not restate the budget as a literal"
  artifacts:
    - path: "src/lib/wizardErrors.invariant.test.ts"
      provides: "whitespace-tolerant deriveRoster, per-route status predicate, four new SELF-TESTs"
      contains: "statusRe"
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "A-25 derived-longest-budget assertion beside the kept literal one"
      contains: "A-25"
  key_links:
    - from: "src/lib/wizardErrors.invariant.test.ts"
      to: "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx"
      via: "deriveRoster reading KNOWN_FINALIZE_CODES (proved by SELF-TEST here, wired in 153.1-06)"
      pattern: "deriveRoster"
    - from: "src/lib/seam-constants.pin.test.ts"
      to: "SEAM_BUDGETS"
      via: "Math.max over every row's timeoutMs"
      pattern: "SEAM_BUDGETS"
---

<objective>
Make the two guards that this sub-phase's later plans depend on **able to fail**, and land them
GREEN AT HEAD before any production code moves.

Purpose: three of the four scanner mechanisms WIZFORM-02's class fix rides on are broken or too
narrow at HEAD, and every one of them fails **silently** (a scanner that matches nothing
satisfies every "missing is empty" assertion). Separately, D-19 requires the A-25 breaker
coupling to be checkable before Phase 153.4 raises the budget against it.

Output: `wizardErrors.invariant.test.ts` with a hardened `deriveRoster`, a per-route status
predicate and four SELF-TESTs; `seam-constants.pin.test.ts` with a derived A-25 assertion beside
the kept literal one.

⚠️ This plan adds NO `ROUTES` entry and moves NO production constant. Everything here must be
green at HEAD on the unmodified `src/` tree.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-CONTEXT.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-PATTERNS.md
@.planning/phases/153-wizform-form-errors-belong-on-the-form-mt5-declarable/153-VALIDATION.md

<measured_at_head>
All of the following were MEASURED at `0c4f01d8` by the planner, not inferred. They are the
reason this plan exists. Re-derive before editing (D-10) and record any drift.

1. **`deriveRoster` returns `[]` for `KNOWN_FINALIZE_CODES` today.** `SubmitStep.tsx:230`
   declares `const KNOWN_FINALIZE_CODES: ReadonlySet<WizardErrorCode> = new Set<WizardErrorCode>(`
   and the `[` is on the NEXT line (`:231`). `deriveRoster` (`:155-163`) does
   `source.indexOf("([", start)` — which returns **-1** for that file — then returns `[]`.
   Reproduce: `node -e 'const s=require("fs").readFileSync("src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx","utf-8");const st=s.indexOf("const KNOWN_FINALIZE_CODES");console.log(s.indexOf("([",st))'`
   → prints `-1`. The two incumbent rosters wrap after `=` (`ConnectKeyStep.tsx:265-266`,
   `MultiKeyConnectStep.tsx:214-215`) so their `([` IS adjacent — which is why nobody has hit
   this. ⛔ Do NOT "fix" this by reformatting `SubmitStep.tsx`: the joined line is ~93 chars and
   Prettier will re-break it, silently re-blinding the scanner.
2. **`EMITTER_RE` (`:100-101`) gates on `status:\s*400`.** `finalize-wizard/route.ts` answers its
   coded arms at 400/403/404/409/502/503, so a 400-only predicate sees almost none of them.
3. **`EMITTER_RE`'s `error:[^}]*\}` cannot cross a template interpolation.** Four
   `finalize-wizard` bodies interpolate — `:428` (`${MAX_DOLLAR_VALUE}`), `:439` (same),
   `:1091` (`${MAX_COMPOSITE_MEMBERS}`), `:503` (`${OWN_CAPITAL}` / `${TEAM_REVIEW}`). The `}`
   that closes `${…}` terminates `[^}]*` early and the match dies. These stay invisible even
   after the D-34 reorder and even after the status predicate widens.
4. **The A-25 pin (`:705-717`) is literal-vs-literal:** `BREAKER_LOCK_TOMBSTONE_S * 1_000 >=
   60_000 - 30_000`. `SEAM_BUDGETS` is ALREADY imported at `:3`, so the derived form needs no new
   import. Longest `timeoutMs` in the table today is `60_000`.
</measured_at_head>

<interfaces>
<!-- Extracted from src/lib/wizardErrors.invariant.test.ts at HEAD. Use these directly. -->

interface RouteUnderTest {
  readonly label: string;      // human name in failure messages
  readonly route: string;      // absolute path to the route handler
  readonly rosterFile: string; // absolute path to the file holding the roster
  readonly rosterName: string; // the roster's declared name
}

const EMITTER_RE =
  /NextResponse\.json\(\s*\{\s*code:\s*"([A-Z][A-Z0-9_]*)"\s*,\s*error:[^}]*\}\s*,\s*\{[^}]*status:\s*400/g;

function deriveEmittedCodes(source: string): string[];   // :133-139
function deriveUnionMembers(source: string): string[];   // :151-153
function deriveRoster(source: string, name: string): string[];  // :155-163

const EXPECTED_SITES_PER_ROUTE = 12;          // :183
const EXPECTED_FORMAT_EMITTERS_PER_ROUTE = 1; // :191
const DERIVED_FLOOR = 14;                     // :203

// src/lib/seam-constants.pin.test.ts — already imported at :2-19
import { SEAM_BUDGETS, BREAKER_LOCK_TOMBSTONE_S, BREAKER_COOLDOWN_S } from "./resilient-fetch";
// SEAM_BUDGETS: Record<SeamBudgetKey, { timeoutMs: number; dependencies: …; retries: number; notes: string }>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: The A-25 derived assertion — a breaker coupling that CAN fail (D-19)</name>
  <files>src/lib/seam-constants.pin.test.ts</files>
  <read_first>
    - `src/lib/seam-constants.pin.test.ts` — the file docblock's ONE RULE (`:36-60`), the
      `BREAKER_LOCK_TOMBSTONE_S` value pin (`:695-703`), the A-25 assertion (`:705-717`), and the
      A-14 assertion below it (`:719-731`) as the assertion-prose style to match.
    - `.planning/phases/153-…/153-PATTERNS.md` §Shared Pattern **C** ("a hand-typed pin is a
      MANUAL checklist item when it restates a table fact") and §⭐4's closing note ("belongs
      BESIDE this one, not instead of it").
    - `.planning/phases/153-…/153-VALIDATION.md` §Oracle Independence, the two ⭐ bullets.
  </read_first>
  <action>
    Add a NEW `it(...)` immediately after the existing `it("A-25: the tombstone outlives the
    longest seam budget")` at `:705`. **Keep the existing assertion byte-unchanged** — it catches
    the constants moving; this one catches the coupling breaking (D-19; VALIDATION §Oracle
    Independence's last two bullets say neither implies the other).

    The new assertion is DERIVATION-vs-HAND-TYPED:
    - Derive `longestBudgetMs` as the `Math.max` over `Object.values(SEAM_BUDGETS)` mapped to
      `timeoutMs`. That is the only derived side.
    - Compare it against a ceiling built from **hand-typed literals** `60` (the tombstone,
      seconds) and `30` (the cooldown, seconds): `expect(longestBudgetMs).toBeLessThanOrEqual((60
      + 30) * 1_000)`. ⛔ Do NOT write `BREAKER_LOCK_TOMBSTONE_S` or `BREAKER_COOLDOWN_S` into
      this expression — reading the expectation out of the module under test is the exact failure
      this file's ONE RULE forbids, and it is what makes the incumbent A-25 pin unable to fail.
    - Guard the derivation against vacuity in the same test: assert `Object.keys(SEAM_BUDGETS)`
      has at least 13 rows and `longestBudgetMs >= 60_000`. A `Math.max` over an empty or
      mis-shaped table returns `-Infinity`, which would satisfy the ceiling forever.
    - Failure message must name: the property ("the longest seam budget must fit inside
      tombstone + cooldown, or a request admitted just before a lock was armed can no longer see
      it"), the owner of the next move (**Phase 153.4 / D-26** — `120_000` ms plus
      `BREAKER_LOCK_TOMBSTONE_S` 60 → 90 in ONE commit), and the maintenance instruction (when
      those land, the hand-typed `60` here moves to `90` **in the same commit**). Record that
      **D-18's `90_000` is SUPERSEDED by D-26** so nobody re-derives the old number from this
      test's arithmetic — D-18 chose 90 000 because it was the largest value needing only ONE
      constant changed, which let a test invariant pick a production timeout, and that reasoning
      was rejected. **D-28 (bounding the MT5 `close()` separately) is likewise SUPERSEDED by
      D-30**, which takes `shutdown()` off the request path entirely; it is named here only
      because the A-25 sizing conversation cites it. ⛔ Neither superseded decision is implemented
      by this sub-phase, and neither number belongs in this assertion.

    Add a second `it(...)` SELF-TEST proving the assertion CAN fail without touching 153.4's
    files: run the same `Math.max` reducer over a **synthetic** table literal
    (`{ a: { timeoutMs: 15_000 }, b: { timeoutMs: 150_000 } }`), assert it returns `150_000`, and
    assert explicitly that `150_000` **exceeds** the `(BREAKER_COOLDOWN_S + BREAKER_LOCK_TOMBSTONE_S)
    × 1_000` ceiling the real assertion applies. Extract the reducer into a small local helper so
    the real assertion and the SELF-TEST use the SAME code path; a SELF-TEST over a re-typed
    reducer proves nothing.

    ⛔ **The synthetic MUST be `150_000`, not `120_000` — amended by the orchestrator 2026-08-09
    after 153.4 planning surfaced the collision.** A `120_000` synthetic passes today (the ceiling
    is `(30 + 60) × 1_000 = 90_000`) but goes FALSE the moment 153.4 lands D-26's tombstone raise,
    because the ceiling becomes exactly `(30 + 90) × 1_000 = 120_000` and `120_000 > 120_000` is
    false. `150_000` clears BOTH ceilings, so this SELF-TEST never needs churning and there is no
    window in which a sub-phase boundary leaves it red.
    ⚠️ Keep the synthetic **hand-typed** — do NOT derive it as `ceiling + 1`. Deriving it from the
    module under test is precisely the self-referential oracle this file exists to forbid; a
    hand-typed literal that outlives the planned ceiling change is the correct trade. Add a
    comment naming D-26 so the coupling is visible if the ceiling is ever raised past 150 s.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/seam-constants.pin.test.ts` is GREEN at HEAD with no production
      constant modified. Paste the summary line.
    - Source assertion: `grep -c 'BREAKER_LOCK_TOMBSTONE_S\|BREAKER_COOLDOWN_S'` inside the new
      `it` block's body is **0** (verify by reading the block, not by a whole-file grep — the
      file legitimately uses both constants elsewhere).
    - The SELF-TEST demonstrates a `120_000` budget failing the ceiling. Paste that assertion.
    - The existing `it("A-25: the tombstone outlives the longest seam budget")` is unchanged —
      `git diff` on that hunk shows only added lines below it.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/seam-constants.pin.test.ts</automated>
  </verify>
  <done>Two assertions exist for A-25: the kept literal-vs-literal one, and a derived-vs-hand-typed one that reds when a budget longer than 90 000 ms lands without the tombstone moving. Both green at HEAD.</done>
</task>

<task type="auto">
  <name>Task 2: Harden deriveRoster so a line-broken `new Set&lt;T&gt;(\n [` is readable (D-09a)</name>
  <files>src/lib/wizardErrors.invariant.test.ts</files>
  <read_first>
    - `src/lib/wizardErrors.invariant.test.ts` `:155-163` (`deriveRoster`), `:404-421` (the
      existing roster SELF-TEST), and the file header `:40-54` (the comment-strip doctrine).
    - `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx:229-283` — the EXACT
      declaration shape the scanner must read (indented inside a function body, `(` and `[` on
      different lines, close is `],\n        );`).
    - `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx:265-267` and
      `MultiKeyConnectStep.tsx:214-216` — the two incumbent shapes whose derivation must not move.
  </read_first>
  <action>
    Replace `deriveRoster`'s two anchor searches with whitespace-tolerant ones. Today it uses
    `source.indexOf("([", start)` and `source.indexOf("])", open)`; both require the two
    characters to be ADJACENT. Use regexes anchored from `start` instead — an opening match on
    `(` followed by any whitespace then `[`, and a closing match on `]` followed by any
    whitespace then `)`. Keep the function's contract identical: bounded to the named
    declaration, returns `[]` when either anchor is missing, extracts `"[A-Z][A-Z0-9_]*"`
    literals only.

    ⚠️ Preserve the boundary guarantee the existing SELF-TEST at `:404-421` pins: the scan must
    still stop at the roster's own close and must not run into the NEXT `const KNOWN_*` set. The
    lazy/first-match semantics of the new anchors must be verified, not assumed.

    ⛔ Do NOT reformat `SubmitStep.tsx` to make the old anchors work. The joined declaration is
    ~93 characters, Prettier re-breaks it, and the scanner would silently re-blind on the next
    `npm run lint -- --fix`. Fixing the scanner is the class fix; reformatting the subject is the
    instance fix.

    Add a SELF-TEST named for what it protects, using a fake source that reproduces
    `SubmitStep.tsx`'s shape **verbatim in structure**: eight leading spaces, the type
    annotation, `new Set<WizardErrorCode>(` at end of line, `[` on the next line, two code
    literals, then `],` and `);` on separate lines. Assert the two codes come back. Add a second
    fake immediately after it with a DIFFERENT roster name and assert the first scan did not
    swallow it.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/wizardErrors.invariant.test.ts` is GREEN at HEAD. Paste the summary
      line — ⛔ never "assume the scanner reaches it" (this is the plan-level trap).
    - Behaviour proof, run and pasted: a node one-liner that requires nothing from vitest and
      prints `deriveRoster(SubmitStep.tsx, "KNOWN_FINALIZE_CODES").length`. Before this task it is
      `0`; after, it must be the real member count (≥ 10). Paste both numbers.
    - Non-regression proof: the same one-liner for `KNOWN_CREATE_WITH_KEY_CODES` and
      `KNOWN_ADD_KEY_CODES` returns the SAME count before and after (the planner measured 24 each
      — reconcile if you measure otherwise and record the number you measured).
    - The existing SELF-TEST "the roster scan reads the named Set and stops at its close" is
      unchanged and still green.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.invariant.test.ts</automated>
  </verify>
  <done>`deriveRoster` reads a roster whose `(` and `[` are on different lines; the two incumbent rosters derive identically to before; a SELF-TEST pins the line-broken shape.</done>
</task>

<task type="auto">
  <name>Task 3: Per-route status predicate + an error body that survives `${…}` (D-34 foundation)</name>
  <files>src/lib/wizardErrors.invariant.test.ts</files>
  <read_first>
    - `src/lib/wizardErrors.invariant.test.ts` `:71-101` (the predicate docblock and
      `EMITTER_RE`), `:129-139` (`deriveEmittedCodes`), `:333-388` (the three existing
      SELF-TESTs).
    - `src/app/api/strategies/finalize-wizard/route.ts` — `:342-350` (a bare 400 arm), `:565-580`
      (a 503 coded arm), `:420-445` (the two `${MAX_DOLLAR_VALUE}` bodies), `:1086-1096` (the
      `${MAX_COMPOSITE_MEMBERS}` body), `:496-507` (the `${OWN_CAPITAL}` body).
    - `.planning/phases/153-…/153-PATTERNS.md` §⭐2 ("THE EMITTER REGEX CONSTRAINS THE CODE YOU
      WRITE") and §153-RESEARCH Pitfall 3.
  </read_first>
  <action>
    Two independent widenings of the emitter predicate, plus the SELF-TESTs the file's own
    discipline owes for each.

    **(a) Status becomes a per-route property.** Add a `statusRe: string` field to
    `RouteUnderTest` holding a regex FRAGMENT, and give both existing entries the value `"400"`
    — byte-identical behaviour for `create-with-key` and `composite/add-key`, so their pinned
    literal counts (`EXPECTED_SITES_PER_ROUTE = 12`, the `EXPECTED_SPLIT_CODES` vocabulary) stay
    true. Turn `EMITTER_RE` into a factory that builds the regex for a given fragment, and give
    `deriveEmittedCodes(source, statusRe = "400")` a defaulted second parameter so the three
    existing SELF-TESTs call it unchanged. `finalize-wizard`'s entry (added in 153.1-06) will
    pass `"[45]\\d\\d"` — every 4xx/5xx coded rejection, because that route answers
    400/403/404/409/502/503 and the D-34 blindness is precisely that a 400-only predicate cannot
    see the pre-existing arms.
    Rewrite the predicate docblock at `:71-99` so its prose states the PER-ROUTE population
    ("input-validation rejections" is true of the two incumbents and false of finalize-wizard).
    Leaving the old prose green while its premise changes is Pitfall 1 in this file's own voice.

    **(b) The error body must survive a template interpolation.** Replace `error:[^}]*\}` with a
    bounded lazy scan that reaches the object literal's real closing brace — `error:` then a
    lazy any-character run capped at a few hundred characters, then `\}` followed by the
    existing `\s*,\s*\{[^}]*status:` context. The cap is what stops a lazy match running across
    two adjacent emitters; pick it, and write the number's reason in the docblock.
    ⛔ Do NOT relax the `code:`-FIRST requirement or the `[A-Z][A-Z0-9_]*` literal class. Those
    two are the levers that make the D-34 reorder mandatory and the lowercase `draft_state_invalid`
    visible as a defect; relaxing them would "fix" the assertion by making the defect legal.

    **The four SELF-TESTs this widening owes** (the file's rule: a relaxed predicate must prove
    the relaxed form matches):
    1. POSITIVE, widened status — a `{ code: "X", error: "…" }` emitter at `status: 502` matches
       under `"[45]\\d\\d"`.
    2. NEGATIVE, narrow status — the SAME 502 source returns `[]` under the default `"400"`.
       This is what proves the per-route scoping is real and not decorative.
    3. POSITIVE, interpolated body — an emitter whose `error` is a template literal containing
       `${SOME_CONST}` (mirror `:503`'s two-interpolation shape) matches and yields its code.
    4. NEGATIVE, wrong key order — a `{ error: "…", code: "X" }` emitter returns `[]` under BOTH
       status fragments. Name it for what it protects: this is Pitfall 3 / D-34, and without it
       "the scanner found nothing" is indistinguishable from "there was nothing to find".
  </action>
  <acceptance_criteria>
    - `npx vitest run src/lib/wizardErrors.invariant.test.ts` GREEN at HEAD with `ROUTES` still
      holding exactly TWO entries. Paste the summary line and the test-name list.
    - Non-regression proof, run and pasted: `deriveEmittedCodes` over each incumbent route still
      returns **12** sites (the pinned literal). If either number moves, STOP — the per-route
      scoping leaked.
    - Blindness proof, run and pasted: `deriveEmittedCodes(stripped(finalize-wizard route.ts),
      "[45]\\d\\d")` at HEAD returns a count you record. It will be LOW (the planner measured
      that all 14 pre-existing coded emitters are `error`-first and therefore invisible). Record
      the number — 153.1-06 reconciles against it after the reorder, and a number that does not
      move is how a blind scanner is caught.
    - All four new SELF-TESTs present and named for the defect they protect against.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.invariant.test.ts</automated>
  </verify>
  <done>The scanner can see a non-400 coded rejection and an interpolated error body; it still cannot see `{ error, code }` order; both incumbent routes derive exactly 12 sites as before.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | This plan edits **test files only**. No request path, no response body, no persisted value changes. The trust boundaries this sub-phase touches are crossed in 153.1-03/-05. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.1-01 | Tampering (of the guard, not the system) | `wizardErrors.invariant.test.ts` predicate widening | mitigate | A widened predicate that matches MORE could admit non-rejection shapes and inflate counts, hiding a lost guard. Mitigated by SELF-TEST 2 (narrow config must still reject the widened shape) and by the unchanged 12-site literal on both incumbent routes. |
| T-153.1-02 | Repudiation (a guard that cannot fail) | `seam-constants.pin.test.ts` A-25 | mitigate | A derived assertion over an empty/mis-shaped table returns `-Infinity` and passes forever. Mitigated by the in-test vacuity guard (≥13 rows, `longestBudgetMs >= 60_000`) and the synthetic-table SELF-TEST. |
| T-153.1-03 | Denial of Service (downstream) | A-25 breaker coupling | accept-then-mitigate | The tombstone/budget coupling protects against a re-armed breaker on stale evidence. This plan does not change it; it makes the break OBSERVABLE before 153.4 raises the budget (D-26). Recorded, not exploited here. |
| T-153.1-SC | Tampering | npm/pip/cargo installs | n/a | **Zero package installs in this phase** — RESEARCH §Package Legitimacy Audit: "Not applicable — this phase installs zero external packages" [VERIFIED against every file this plan touches]. No legitimacy checkpoint is owed. If any task in this sub-phase later proposes an install, the Package Legitimacy Gate runs first. |
</threat_model>

<verification>
- `npx vitest run src/lib/wizardErrors.invariant.test.ts src/lib/seam-constants.pin.test.ts` green.
- `npx tsc --noEmit` clean.
- `git status` shows exactly two modified files, both under `src/lib/`, both `*.test.ts`.
- No file under `src/app/` or any non-test module is modified by this plan.
</verification>

<success_criteria>
1. `deriveRoster("SubmitStep.tsx", "KNOWN_FINALIZE_CODES")` returns ≥ 10 members (was `0`),
   measured and pasted.
2. Both incumbent routes still derive exactly 12 emitting sites and their rosters derive the same
   counts as before.
3. Four new emitter SELF-TESTs exist: widened-status positive, narrow-status negative,
   interpolated-body positive, `{ error, code }`-order negative.
4. A second A-25 assertion exists that derives the longest budget from `SEAM_BUDGETS` and compares
   it to hand-typed `60`/`30`, with a SELF-TEST proving a `120_000` budget fails it.
5. Everything green at HEAD with zero production-source edits.
</success_criteria>

<output>
Create `.planning/phases/153.1-wizform-codes-honest-codes-venue-capability-foundation/153.1-01-SUMMARY.md` when done.
Record in it: the measured `deriveRoster` counts (before/after), the measured
`deriveEmittedCodes` count for `finalize-wizard` at HEAD under the widened predicate, and the
lazy-match character cap you chose with its reason. 153.1-06 reconciles against all three.
</output>
