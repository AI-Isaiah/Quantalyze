---
phase: 153.6-parity-the-fixes-that-only-landed-on-one-path
plan: 06
type: execute
wave: 2
depends_on: [153.6-04]
files_modified:
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/lib/wizardErrors.invariant.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
autonomous: true
requirements: [PARITY-05]
must_haves:
  truths:
    - "A probe parse miss (2xx body the schema cannot read — what a rolling analytics deploy produces) renders honest transient copy WITH a Retry control (D-12)"
    - "The genuinely permanent probe-status arm keeps KEY_SCOPE_CHECK_UNAVAILABLE with NO Retry — the two conditions no longer share a code"
    - "Both EXPECTED_TABLE_SIZE pins read 75 and agree; EXPECTED_FINALIZE_REJECTION_SITES reads 32 UNMOVED — the proof this is a fix, not an addition (D-18)"
    - "The new code renders its OWN copy, not UNKNOWN's — asserted on the rendered TITLE, because UNKNOWN's copy is recoverable and would fake the Retry for the wrong reason (P5, opposite direction)"
  artifacts:
    - path: "src/lib/wizardErrors.ts"
      provides: "KEY_SCOPE_CHECK_UNREADABLE union member + recoverable copy entry (actions include clear_and_retry)"
      contains: "KEY_SCOPE_CHECK_UNREADABLE"
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "the parse-miss arm emits the new code; the permanent arm untouched"
      contains: "KEY_SCOPE_CHECK_UNREADABLE"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx"
      provides: "KNOWN_FINALIZE_CODES member added in the SAME commit the route emits it"
      contains: "KEY_SCOPE_CHECK_UNREADABLE"
  key_links:
    - from: "finalize-wizard/route.ts parse-miss arm (:895-906)"
      to: "wizardErrors.ts copy table"
      via: "the new code string; PROBE_PARSE_MISS identity singleton untouched"
      pattern: "KEY_SCOPE_CHECK_UNREADABLE"
    - from: "SubmitStep.tsx KNOWN_FINALIZE_CODES"
      to: "the route's emitted vocabulary"
      via: "same-commit membership (an omitted member falls to UNKNOWN)"
      pattern: "KEY_SCOPE_CHECK_UNREADABLE"
    - from: "wizardErrors.test.ts meta-test (:3010-3048)"
      to: "the two EXPECTED_TABLE_SIZE declarations"
      via: "regex count .toBe(2) + agreement — unchanged"
      pattern: "EXPECTED_TABLE_SIZE"
---

<objective>
Cluster E: restore the Retry affordance for a probe parse miss without weakening the permanent arm.
153.2-04 correctly moved the parse miss off `KEY_NETWORK_TIMEOUT` (an honest deletion of a lie) but
onto `KEY_SCOPE_CHECK_UNAVAILABLE`, whose copy structurally suppresses Retry — yet a 2xx body the
schema cannot read is NOT always permanent: it is also what a rolling analytics deploy produces.

Remedy shape (RESEARCH §Q15, locked): mint a NEW code `KEY_SCOPE_CHECK_UNREADABLE` with honest
RECOVERABLE copy for the parse miss; leave `KEY_SCOPE_CHECK_UNAVAILABLE` untouched for the
permanent probe-status arm. ⛔ Never revert to KEY_NETWORK_TIMEOUT ("we could not reach the
exchange" is the removed lie). ⛔ Never add a recoverable action to the permanent code.

D-13/D-18 bind hard: pins are RE-CUT, never deleted. `EXPECTED_TABLE_SIZE` 74 → 75 at BOTH sites;
`EXPECTED_FINALIZE_REJECTION_SITES` STAYS 32 — if this plan finds itself changing 32, it has
mis-scoped E: STOP.

Output: the new code wired end to end (route → copy table → SubmitStep), every pin re-cut, both
falsifying mutations observed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-CONTEXT.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-RESEARCH.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-PATTERNS.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-VALIDATION.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-04-SUMMARY.md

Key sections: RESEARCH §Q15 (classification site, the recoverability mechanism, the COMPLETE pin
inventory table); PATTERNS §E (the exact re-cut idiom, the meta-test's positive control, the
KNOWN_FINALIZE_CODES trap template, recoverable actions shapes, A5-resolved: "nineteen codes" is
PROSE at :545 and :938, not a pin).

⚠️ MEMORY lesson (recurred 2026-08-08): e2e grep-gates scan src/ only — grep the WHOLE repo
INCLUDING e2e/ for any code string touched here before changing it (assumption A6).
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mint KEY_SCOPE_CHECK_UNREADABLE and wire it end to end</name>
  <files>src/lib/wizardErrors.ts, src/app/api/strategies/finalize-wizard/route.ts, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/lib/wizardErrors.ts:164-170 (union comment convention), :1013-1015 (KEY_NETWORK_TIMEOUT's recoverable actions shape), :1017-1035 (KEY_SCOPE_CHECK_UNAVAILABLE — the entry whose mechanism comment the new entry mirrors in the opposite direction)
    - src/app/api/strategies/finalize-wizard/route.ts:181-194 (PROBE_PARSE_MISS singleton + its identity-check rationale — keep it), :817-831 (the permanent arm — DO NOT TOUCH), :895-906 (the parse-miss arm — the code string changes, the 502 and the identity check stay)
    - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx:316-350 (KNOWN_FINALIZE_CODES + the two same-commit trap comments; :335-350 is the template)
    - src/lib/envelope.ts (RECOVERABLE_ACTIONS — the membership that derives recoverable)
  </read_first>
  <behavior>
    - Parse miss ⇒ 502 + code KEY_SCOPE_CHECK_UNREADABLE + recoverable:true ⇒ ErrorEnvelope renders Retry
    - Permanent probe status ⇒ KEY_SCOPE_CHECK_UNAVAILABLE, recoverable:false, no Retry (unchanged — asserted, not assumed)
    - The rendered TITLE for the parse miss is the NEW entry's title, not UNKNOWN's (P5: UNKNOWN's copy is recoverable, so an omitted KNOWN_FINALIZE_CODES member would fake the right control for the wrong reason)
  </behavior>
  <action>
    FIRST: grep the WHOLE repo (including e2e/) for `KEY_SCOPE_CHECK_UNAVAILABLE` and audit every
    hit — any e2e/spec asserting the parse-miss path's code/copy must be re-cut in this commit
    (A6). Then: (1) add `KEY_SCOPE_CHECK_UNREADABLE` to the WizardErrorCode union with the 5-line
    comment convention (what it means: a 2xx probe body the schema cannot read — transient, e.g. a
    rolling analytics deploy; why split from KEY_SCOPE_CHECK_UNAVAILABLE: that code is the
    PERMANENT arm's and must keep suppressing Retry). (2) Copy entry with honest transient copy
    ("we could not read the scope check's answer — usually a deploy in progress; try again" tone —
    never "could not reach the exchange") and `actions: ["clear_and_retry", "request_call"]`; the
    entry's first paragraph mirrors KEY_SCOPE_CHECK_UNAVAILABLE's mechanism comment in the
    OPPOSITE direction: clear_and_retry ∈ RECOVERABLE_ACTIONS ⇒ buildEnvelope derives
    recoverable:true ⇒ ErrorEnvelope renders Retry — that is the entire fix. (3) Route: the
    :895-906 arm's `code` becomes the new string; the 502 status, the error text and the
    PROBE_PARSE_MISS identity check stay (⛔ keep the module-scope singleton — the :181-193
    rationale). The :817-831 permanent arm is untouched. (4) SubmitStep: add the member to
    KNOWN_FINALIZE_CODES IN THIS COMMIT, with a trap comment copied from the :335-350 template
    (naming that omission ⇒ UNKNOWN ⇒ an accidental Retry for the wrong reason). (5) route.test.ts:
    the parse-miss case asserts 502 + the NEW code; the permanent-status case asserts the OLD code
    still has no recoverable action. UI-copy note: text-only inside the existing envelope
    component — no visual decisions (DESIGN.md not implicated); if any visual question arises, read
    DESIGN.md first and stop.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts src/lib/wizardErrors.test.ts 2>&1 | tail -5 | grep -qi "passed"</automated>
  </verify>
  <acceptance_criteria>
    - Whole-repo grep (incl. e2e/) audited; every dependent string re-cut in the same commit
    - New code wired at all four sites in ONE commit (union, copy, route arm, KNOWN_FINALIZE_CODES)
    - Permanent arm diff-empty; PROBE_PARSE_MISS singleton untouched
    - Parse-miss test asserts the rendered TITLE (not just Retry presence)
  </acceptance_criteria>
  <done>The parse miss has its own honest recoverable code, end to end, permanent arm untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Re-cut every pin; prove the unmoved ones did not move; falsify</name>
  <files>src/lib/wizardErrors.test.ts, src/lib/wizardErrors.invariant.test.ts</files>
  <read_first>
    - src/lib/wizardErrors.test.ts:1476-1490 and :1740-1760 (the two EXPECTED_TABLE_SIZE sites + their DIFFERENT docblock reasonings — each must be re-run over the new entry separately), :3010-3048 (the meta-test + its positive control)
    - src/lib/wizardErrors.invariant.test.ts:516 (KNOWN_CODELESS=3), :521-534 (EXPECTED_FINALIZE_REJECTION_SITES=32 + its arithmetic docblock), :270 (expectedSites 29), :545 + :938 (the "nineteen codes" PROSE)
    - 153.6-RESEARCH.md §Q15 pin-inventory table (the complete, verified list)
    - 153.6-VALIDATION.md rows E and E-pin
  </read_first>
  <action>
    Re-cut, in one commit with Task 1's landing: (1) BOTH `EXPECTED_TABLE_SIZE` literals 74 → 75
    (wizardErrors.test.ts:1486 and :1750) — re-run EACH docblock's reasoning over the new entry
    separately (the two guards scan different populations); do NOT change the meta-test's
    `.toBe(2)` count or its regex shape. (2) The "nineteen codes" PROSE → twenty at
    invariant.test.ts:545 and :938 (A5-resolved: prose re-cut, not a pin). (3) VERIFY UNMOVED and
    state so in the SUMMARY: `EXPECTED_FINALIZE_REJECTION_SITES = 32` (the scanner counts
    NextResponse.json rejection LITERALS — the arm already existed; only its code string changed),
    `KNOWN_CODELESS_FINALIZE_REJECTIONS = 3`, `expectedSites: 29`. ⛔ If any diff moves 32, the
    change added or removed a rejection site — E is mis-scoped; STOP and re-examine. (4) Add test
    coverage for the new copy entry itself (the branches-gate: new copy adds branches; coverage
    thresholds are blocking — lines 82 / statements 80 / functions 74 / branches 72).
    FALSIFY (ledger): E — temporarily reclassify the parse miss back onto the permanent code →
    the route.test parse-miss case AND the wizardErrors Retry assertion red; E-pin — set ONE
    EXPECTED_TABLE_SIZE site to 75 with the other at 74 → the `-t "cannot silently diverge"`
    agreement assertion reds loudly (this proves the two pins cannot drift apart — the ledger's
    second-member-of-the-class row). Restore both; paste evidence.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizardErrors.test.ts src/lib/wizardErrors.invariant.test.ts -t "" 2>&1 | tail -5 | grep -qi "passed" && grep -c "const EXPECTED_TABLE_SIZE = 75;" src/lib/wizardErrors.test.ts | grep -q "^2$" && grep -c "EXPECTED_FINALIZE_REJECTION_SITES = 32" src/lib/wizardErrors.invariant.test.ts | grep -q "^1$"</automated>
  </verify>
  <acceptance_criteria>
    - Both EXPECTED_TABLE_SIZE sites read 75; meta-test count/regex untouched; each docblock's reasoning re-run and noted
    - 32 / 3 / 29 unmoved (grep-proven); nineteen→twenty at both prose sites
    - E and E-pin mutations observed red and recorded in the ledger
    - `npm test` full suite green with coverage thresholds
  </acceptance_criteria>
  <done>Every pin re-cut, none deleted; the unmoved pins proven unmoved; both falsifications recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analytics-service probe body → finalize-wizard schema | an unreadable 2xx is a deploy-skew artifact, not a user or venue fact |
| error code → rendered controls | recoverability is derived from actions membership — a code choice IS an access decision on the Retry control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.6-E1 | Denial of service (user dead-end) | parse miss rendered permanent | mitigate | new recoverable code for the transient condition only |
| T-153.6-E2 | Elevation (of an affordance) | Retry leaking to the genuinely permanent arm | mitigate | separate codes; permanent arm diff-empty; both directions asserted |
| T-153.6-E3 | (meta) invisible ship | omitted KNOWN_FINALIZE_CODES member falls to UNKNOWN whose copy IS recoverable | mitigate | same-commit membership + rendered-TITLE assertion (never Retry-presence alone) |
| T-153.6-SC | Tampering | package installs | accept | phase installs nothing |
</threat_model>

<verification>
- `npm test` green; branches coverage holds with the new copy entry tested
- Whole-repo grep (incl. e2e/) shows no stale assertion on the old code for the parse-miss path
- VALIDATION ledger rows E and E-pin reconciled with pasted evidence
</verification>

<success_criteria>
- A rolling deploy no longer strands the founder on a dead end — the parse miss retries
- The permanent arm's Retry suppression is intact and asserted
- D-18's exact pin inventory executed: 74→75 twice, 32/3/29 unmoved, prose re-cut
</success_criteria>

<output>
Create `.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-06-SUMMARY.md` when done.
Record: the pin diff table (moved vs proven-unmoved), the whole-repo grep audit, both mutation observations.
</output>
