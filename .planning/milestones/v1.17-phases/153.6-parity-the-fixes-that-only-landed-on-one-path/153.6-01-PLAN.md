---
phase: 153.6-parity-the-fixes-that-only-landed-on-one-path
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/mt5_probe.py
  - analytics-service/routers/exchange.py
  - analytics-service/services/ingestion/mt5.py
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_mt5_validate_parity.py
  - analytics-service/tests/test_mt5_validate.py
  - analytics-service/tests/test_job_worker.py
autonomous: true
requirements: [PARITY-01, PARITY-02]
must_haves:
  truths:
    - "An order_check-refusing terminal (automated trading disabled server-side) classifies as an OPERATOR fault on BOTH paths — never MT5_WRONG_SERVER, never a 400 blaming the user's broker server (A1)"
    - "A netref materialization failure yields None from the terminal read on BOTH paths, logging the exception CLASS only — never the message (A2)"
    - "The worker stamps the operator fault ('permanent', <curated>) — it is never retried against the shared terminal, and the curated copy names no credential and no _WRONG_SERVER/_AUTH token (A3)"
    - "Mt5SessionAbandoned escapes the shared terminal read unabsorbed, on both paths (B1)"
  artifacts:
    - path: "analytics-service/services/mt5_probe.py"
      provides: "The ONE probe body: read_terminal (abandon arm first), run_probe (terminal short-circuit), assert_expected_login, Mt5GatewayMisconfigured"
      min_lines: 80
      exports: ["read_terminal", "run_probe", "assert_expected_login", "Mt5GatewayMisconfigured"]
    - path: "analytics-service/tests/test_mt5_validate_parity.py"
      provides: "Behavioural parity cases: short_circuit, materialize, operator-fault permanent — each driven through BOTH paths"
      contains: "short_circuit"
    - path: "analytics-service/services/job_worker.py"
      provides: "classify_exception arm mapping Mt5GatewayMisconfigured → ('permanent', curated constant)"
      contains: "Mt5GatewayMisconfigured"
  key_links:
    - from: "analytics-service/routers/exchange.py"
      to: "services/mt5_probe.py"
      via: "import + delegation (local _read_terminal/_probe/_assert_expected_login bodies deleted)"
      pattern: "from services\\.mt5_probe import|from services import mt5_probe"
    - from: "analytics-service/services/ingestion/mt5.py"
      to: "services/mt5_probe.py"
      via: "import + delegation (worker's divergent copies deleted)"
      pattern: "mt5_probe"
    - from: "analytics-service/services/job_worker.py"
      to: "services/mt5_probe.py"
      via: "isinstance arm in classify_exception"
      pattern: "Mt5GatewayMisconfigured"
---

<objective>
Close cluster A (the three fixes 153.3 landed on `routers/exchange.py` whose twins never reached
`services/ingestion/mt5.py`) and B1 (the router's `_read_terminal` broad `except` absorbing
`Mt5SessionAbandoned`) — by EXTRACTION, not by copying. The probe body moves into ONE new module,
`services/mt5_probe.py`, that both paths call; the divergent copies are DELETED. Per D-14, A2 and B1
are ONE edit: the shared `read_terminal` carries the abandon arm ahead of both sibling arms, so
porting the router's broad arm cannot import B1 onto the worker path.

Purpose: this phase's charter — "what makes the two paths unable to diverge again?" Extraction removes
today's three divergences (D-06 first half); plan 05 adds the ast roster over what remains.
Output: `services/mt5_probe.py`, both callers rewired, the A3 permanent-classification arm, and
behavioural parity tests that drive BOTH paths.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-CONTEXT.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-RESEARCH.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-PATTERNS.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-VALIDATION.md

Key sections: RESEARCH §Q6 (side-by-side diff of the three twins), §Q7 (what extracts vs what
resists), §Q8 (classify_exception + the InvalidToken analog), PATTERNS §A1 (module shape + the
byte-for-byte lift table), §A3 (parity-test harness), §A4 (classify arm), §B1 (abandon-arm analogs),
§S1/S2 (comment beats + class-only logging).
</context>

<interfaces>
<!-- Contracts for the new module. Callers keep their OWN dispositions (RESEARCH §Q7). -->

New module `analytics-service/services/mt5_probe.py` (inside the mypy --strict floor):

```python
class Mt5GatewayMisconfigured(Exception):
    """Operator fault: the gateway terminal refuses automated trading (the
    'Disable automatic trading through the external Python API' option), so an
    investor password cannot be distinguished from a master password.
    PERMANENT — a retry can never clear it. Message is CURATED operator copy:
    it must contain no credential vocabulary and no _WRONG_SERVER/_AUTH token."""

def assert_expected_login(info: dict[str, Any], *, login: int) -> None: ...
def read_terminal(client: Mt5Client, *, log_prefix: str) -> dict[str, Any] | None: ...
    # arm order: Mt5SessionAbandoned -> raise (FIRST); Mt5ClientError -> log code, None;
    # Exception -> log type(...).__name__ ONLY, None
def run_probe(client: Mt5Client, *, login: int, log_prefix: str)
    -> tuple[dict[str, Any], dict[str, Any], dict[str, Any] | None]: ...
    # login/account_info PRE bracket -> read_terminal -> IF classify_trade_capability(info, {},
    # terminal) == "undetermined": POST bracket, return (info, {}, terminal) WITHOUT order_check
    # -> else order_check -> POST bracket -> return
```

In-tree imports of `mt5_probe.py`: `services.mt5_client` (Mt5Client, Mt5ClientError,
Mt5SessionAbandoned) and `services.mt5_validation` (classify_trade_capability,
mt5_probe_request/terminal helpers as needed) ONLY. ⛔ NEVER `routers.*`, never `job_worker`.
The live client is a PARAMETER, never a module-level handle (PATTERNS No-Analog #2).
Exact signatures are the executor's call from the two existing bodies — the router's is the correct
twin (`exchange.py:588-706`); keep parameters minimal and framework-free.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create services/mt5_probe.py — the one probe body, lifted from the router</name>
  <files>analytics-service/services/mt5_probe.py</files>
  <read_first>
    - analytics-service/routers/exchange.py:580-710 (the three correct bodies + the :671-697 incident rationale)
    - analytics-service/services/ingestion/mt5.py:210-260 (the divergent twins being replaced)
    - analytics-service/services/mt5_concurrency.py:1-35 (direction-rule docstring to mirror)
    - analytics-service/services/mt5_validation.py:1-101 (two-caller docstring framing + Mt5ValidationError exception shape)
    - 153.6-RESEARCH.md "Code Examples" (read_terminal reference implementation)
  </read_first>
  <behavior>
    - read_terminal re-raises Mt5SessionAbandoned before any other arm (D-09/D-14)
    - read_terminal logs `error_class=%s` with type name ONLY on an unconverted transport raise (T-134-01 — mt5linux f-string-interpolates the password into remotely-eval'd source; only the CLASS is safe)
    - run_probe returns without calling order_check when classify_trade_capability(info, {}, terminal) == "undetermined" (A1 — the POST bracket still runs)
    - Mt5GatewayMisconfigured carries a curated, credential-free message
  </behavior>
  <action>
    Create `analytics-service/services/mt5_probe.py`. Lift the three bodies byte-for-byte from
    `routers/exchange.py` per the PATTERNS §A1 table: `assert_expected_login` (`:588-609`),
    `read_terminal` (`:611-661`) and `run_probe` (`:663-706`), converting nested closures to
    module-level functions taking the client (and login) as parameters. The 27-line incident
    rationale at `:671-697` MOVES WITH the code — it is the incident record. While extracting,
    add the `except Mt5SessionAbandoned: raise` arm to `read_terminal` ABOVE both the
    `Mt5ClientError` arm and the broad arm (this is A2+B1 as ONE edit, D-14 — never port `:650`
    as-is). The arm's comment carries the three S1 beats: why the type matches no sibling arm
    (plain Exception by design, D-42), the false-positive-path note (D-39), and what must NOT be
    done in the arm. Add `Mt5GatewayMisconfigured(Exception)` with a curated operator-facing
    message constant (model: `mt5_validation.Mt5ValidationError`'s docstring naming both call
    sites). Module docstring composes `mt5_concurrency.py:22-35`'s direction rule ("only in-tree
    imports are services.mt5_client and services.mt5_validation; MUST NEVER import routers.*;
    neither routers/exchange.py nor services/ingestion/mt5.py may import the other" — D-07) with
    `mt5_validation.py:1-8`'s two-caller framing, and states the new fact: the client is a
    PARAMETER, this module owns no session and cannot fence one (PATTERNS No-Analog #2).
    ⛔ Do NOT move the lease bounds or timeout chains — they are deliberately divergent and
    documented at the call sites (`exchange.py:846`, `ingestion/mt5.py:73-90`, `:180-190`).
  </action>
  <verify>
    <automated>cd analytics-service && python -c "from services.mt5_probe import read_terminal, run_probe, assert_expected_login, Mt5GatewayMisconfigured" && mypy --strict --follow-imports=silent services/mt5_probe.py && grep -c "from routers\|import routers" services/mt5_probe.py | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Module imports cleanly; mypy --strict clean (cast(), never `# type: ignore`)
    - Zero import edges to `routers.*` or `services.job_worker`
    - read_terminal arm order: Mt5SessionAbandoned → Mt5ClientError → Exception (class-only log)
    - run_probe short-circuits order_check on "undetermined", still runs the POST login bracket
    - The :671-697 incident rationale text is present in the module
  </acceptance_criteria>
  <done>services/mt5_probe.py exists with the four exports, correct arm ordering, direction-rule docstring, and a clean strict-mypy run.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire both callers onto the shared module; delete the divergent copies</name>
  <files>analytics-service/routers/exchange.py, analytics-service/services/ingestion/mt5.py, analytics-service/tests/test_mt5_validate.py</files>
  <read_first>
    - analytics-service/routers/exchange.py:580-720 (bodies to delete, call sites to rewire)
    - analytics-service/services/ingestion/mt5.py:180-370 (worker copies + the D-31 arm at :319-365 + the abandon propagate arm at :270-290)
    - 153.6-RESEARCH.md §Q6 (exact per-path consequences), §Q7 (dispositions STAY at call sites)
    - analytics-service/tests/test_mt5_validate.py (router-side harness for the new abandoned_read_terminal case)
  </read_first>
  <action>
    Router: delete the local `_assert_expected_login`, `_read_terminal`, `_probe` closures from
    `_validate_mt5_key_probe` and delegate to `mt5_probe.assert_expected_login/read_terminal/
    run_probe`. All HTTP dispositions (service_error, VenueTransientHTTPException, HTTPException,
    trace.outcome) stay in the router untouched — only the probe MECHANICS move. The bounded lease
    (`_MT5_LEASE_WAIT_S`) stays.
    Worker: delete `services/ingestion/mt5.py`'s `_assert_expected_login` (:216-228),
    `_read_terminal` (:230-245) and `_probe` (:247-259) and delegate to the shared module — this
    lands A1 (short-circuit) and A2 (class-only broad arm) on the worker path structurally. Replace
    the D-31 operator-fault arm's bare `RuntimeError` (:338-345) with
    `raise Mt5GatewayMisconfigured(...)` using the module's curated constant (A3's condition half;
    the disposition half is Task 3). The non-operator `Mt5ClientError(0, ...)` transient arm and the
    existing `except Mt5SessionAbandoned: raise` propagate arm (:270-290) stay. The unbounded lease
    (`wait_s=None`) and the diverged timeout chain stay, with their rationale comments.
    Add the B1 behavioural case to `tests/test_mt5_validate.py`: a `terminal_info` raise of
    `Mt5SessionAbandoned` inside the probe escapes `read_terminal` unabsorbed and the router answers
    the D-40 transient disposition — name it to match `-k abandoned_read_terminal`.
    ⚠️ If any existing roster/pin test reds because the outermost enclosing `def` of a probe body
    moved (PATTERNS §A2 brittleness note), RE-CUT the pin to the new truth — never delete a pin,
    never lower a floor; list every re-cut in the SUMMARY.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_validate_parity.py tests/test_mt5_validate.py -x -q && grep -c "def _read_terminal\|def _probe\b" routers/exchange.py services/ingestion/mt5.py | grep -v ":0$" | wc -l | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - No local `_read_terminal`/`_probe` definition remains in either caller (grep count 0 in both)
    - Both callers import from services.mt5_probe; worker still has zero `routers.` imports
    - Lease bounds and timeout chains untouched (their rationale comments still present)
    - `-k abandoned_read_terminal` passes; full pytest run from analytics-service/ green
    - mypy --strict clean over services/ routers/ models/
  </acceptance_criteria>
  <done>Both paths delegate to mt5_probe; divergent copies deleted; B1 behavioural case green; suite green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: A3 disposition — the classify_exception permanent arm + behavioural parity cases + falsification</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_job_worker.py, analytics-service/tests/test_mt5_validate_parity.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:610-725 (classify_exception ordering contract + the InvalidToken analog at :627-635 + the H-1113 ordering-comment style at :636-641)
    - analytics-service/tests/test_job_worker.py:46-53 (TestClassifyException docstring convention)
    - analytics-service/tests/test_mt5_validate_parity.py:1-92 (two-path harness: exchange_module fixture, _adapter_req, _router_detail)
    - analytics-service/services/mt5_validation.py:66-84 (_WRONG_SERVER_TOKENS/_AUTH_TOKENS for the negative-vocabulary assertion)
    - 153.6-VALIDATION.md Falsifiability Ledger rows A1/A2/A3
  </read_first>
  <behavior>
    - classify_exception(Mt5GatewayMisconfigured(...)) == ("permanent", CURATED) — a fixed constant, never str(exc)
    - The curated message contains no member of _WRONG_SERVER_TOKENS or _AUTH_TOKENS and no credential word (password/investor/master/secret)
    - Parity: an order_check-refusing terminal produces ZERO order_check calls on either path (A1)
    - Parity: a materialization failure yields None + class-only log on both paths (A2)
    - Parity: the operator-fault condition classifies permanent on both paths (A3)
  </behavior>
  <action>
    Add ONE new arm to `classify_exception`: `isinstance(exc, Mt5GatewayMisconfigured)` →
    `("permanent", <curated fixed message>)`, placed BEFORE the ccxt hierarchy and before the `:721`
    fall-through per the file's most-specific-first contract, mirroring the InvalidToken analog
    (curated constant + a comment stating why str(exc) is unsafe here: mt5linux interpolates the
    password into remotely-eval'd source, so raw text is a disclosure surface). Per D-17/OQ-4 this
    is deliberately the classify_exception arm, NOT a DispatchResult in long_fetch and NOT an
    HTTPException(422) — state that choice in the arm's comment so a reviewer does not "add" the
    other convention as well (the sink arm also covers process_key.py's other adapter.validate
    sites). Import the exception type from services.mt5_probe (worker → services leaf: correct
    direction).
    Tests: (1) `tests/test_job_worker.py` TestClassifyException gains `-k
    mt5_gateway_misconfigured`: permanent + curated + the negative-vocabulary assertion importing
    the live token tables. (2) `tests/test_mt5_validate_parity.py` gains the three parity cases
    (names matching `-k short_circuit` / `-k materialize` and the A3 case), each driving BOTH the
    router (via the exchange_module fixture) and the adapter (via _adapter_req) through a fake
    client, asserting the DISPOSITIONS agree — never comparing implementations. Extend the file's
    docstring with the new divergence record in its established shape.
    FALSIFY (VALIDATION ledger rows A1/A2/A3 — run each mutation, observe the red, restore, paste
    the failing assertion into the ledger's Observed column):
    A1: delete the shared short-circuit → `-k short_circuit` reds.
    A2: widen the narrow arm so Mt5SessionAbandoned is caught by the broad except →
    `-k materialize` AND `-k abandoned_read_terminal` red.
    A3: revert the classify arm to the ("unknown", str(exc)) fall-through → `-k
    mt5_gateway_misconfigured` reds.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_validate_parity.py tests/test_job_worker.py tests/test_mt5_validate.py -q && pytest --cov=services --cov=routers --cov=main_worker --cov-fail-under=80 -q && mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - New arm sits above the ccxt hierarchy with an ordering comment; returns the curated constant
    - All three parity cases green; each drives BOTH paths
    - All three falsifying mutations observed RED and restored; ledger rows A1/A2/A3 updated with pasted failing assertions
    - Coverage gate (--cov-fail-under=80) passes with the new module counted
  </acceptance_criteria>
  <done>Operator fault classifies permanent with curated credential-free copy; parity cases prove both paths agree; mutations observed red and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rpyc gateway → analytics-service | remote exception TEXT may embed the interpolated MT5 password (mt5linux evals f-string source) |
| worker → user-visible job state | classify_exception's sanitized_message is persisted and rendered |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.6-01 | Information disclosure | mt5_probe.read_terminal broad arm; classify_exception fall-through | mitigate | class-only logging (type().__name__); curated constant in the new arm; negative-vocabulary test against live _WRONG_SERVER/_AUTH tables (was: raw rpyc text → sanitized_message, T-134-01/T-153.3-23) |
| T-153.6-02 | Denial of service (self) | job_worker retry loop | mitigate | Mt5GatewayMisconfigured → permanent; the fault can no longer serialize retries against the ONE shared terminal ahead of every other user's validate |
| T-153.6-03 | Repudiation / false telemetry | operator triage reading absorbed abandon as a gateway fault | mitigate | abandon arm FIRST in the shared read_terminal (B1); WARNING at the fence remains the signal |
| T-153.6-SC | Tampering | package installs | accept | phase installs nothing (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `cd analytics-service && pytest` full run green (from analytics-service/ ONLY — repo-root fires live broker calls)
- `mypy --strict --follow-imports=silent services/ routers/ models/` clean
- grep: zero `def _read_terminal` / `def _probe` in the two callers; zero `routers` imports in services/mt5_probe.py
- VALIDATION.md ledger rows A1/A2/A3 carry pasted failing assertions (never "the test covers it")
</verification>

<success_criteria>
- The three 153.3 fixes exist on both paths by construction (one module, both callers delegate)
- B1 closed as the same edit (abandon arm first in the shared read_terminal)
- The A3 fault is permanent + curated at the sink; no credential vocabulary reachable
- All falsifying mutations observed RED, restored, and recorded in the ledger
</success_criteria>

<output>
Create `.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-01-SUMMARY.md` when done.
Record: every roster/pin re-cut performed, the exact curated message text, and the ledger evidence.
</output>
