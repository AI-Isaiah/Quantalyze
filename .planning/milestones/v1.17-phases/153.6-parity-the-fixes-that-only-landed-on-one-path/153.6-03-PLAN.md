---
phase: 153.6-parity-the-fixes-that-only-landed-on-one-path
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260811210000_api_keys_attested_venue.sql
  - supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql
  - supabase/tests/test_api_keys_exchange_not_user_writable.sql
  - supabase/schema/functions/create_wizard_strategy.sql
  - supabase/schema/functions/add_wizard_composite_key.sql
autonomous: true
requirements: [PARITY-04]
must_haves:
  truths:
    - "A client INSERT on api_keys (any role outside postgres/service_role/supabase_admin) cannot persist a non-NULL attested_venue — the BEFORE INSERT trigger scrubs it"
    - "Both wizard RPCs stamp attested_venue = p_exchange and RETAIN their advisory-lock idempotency fences (F6 / composite twin)"
    - "All pre-phase PROD rows carry attested_venue = their census-time exchange; the 2 mt5 rows are probe-exempt via ATTESTATION, not label"
    - "The migration ABORTS loudly on PROD-census drift (D-01d) yet applies cleanly on TEST — and a PROD apply can never silently skip the pin"
    - "test_api_keys_exchange_not_user_writable.sql's assertions 2/3 still gate on the 20260810120000 comment substring (no silent SKIP), and assertion 4 (DELETE survives) still passes"
  artifacts:
    - path: "supabase/migrations/20260811210000_api_keys_attested_venue.sql"
      provides: "column + INVOKER trigger + two RPC re-bases + count-pinned backfill + aborting self-verify blocks"
      contains: "pg_advisory_xact_lock"
    - path: "supabase/tests/test_api_keys_exchange_not_user_writable.sql"
      provides: "assertion 5: DELETE + re-INSERT with forged attested_venue succeeds AND stores NULL, behind its own new-migration-id gate"
      contains: "attested_venue"
    - path: "supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql"
      provides: "corrected header (D-04): the 'can never be corrected back' falsehood removed; residual argument rewritten"
      contains: "attested_venue"
  key_links:
    - from: "create_wizard_strategy / add_wizard_composite_key"
      to: "api_keys.attested_venue"
      via: "INSERT column widened inside both SECURITY DEFINER bodies"
      pattern: "attested_venue"
    - from: "BEFORE INSERT trigger"
      to: "api_keys"
      via: "SECURITY INVOKER function scrubbing non-privileged NEW.attested_venue"
      pattern: "SECURITY INVOKER"
    - from: "COMMENT ON COLUMN public.api_keys.exchange"
      to: "test_api_keys_exchange_not_user_writable.sql gate"
      via: "the preserved 20260810120000 substring"
      pattern: "20260810120000"
---

<objective>
Cluster D, DB half: create the server-attested venue record the probe gate will read (plan 04).
One migration: `api_keys.attested_venue` (nullable text), written ONLY by the two SECURITY DEFINER
wizard RPCs; a SECURITY INVOKER `BEFORE INSERT` trigger that NULLs any client-supplied value; both
RPCs re-based on their LATEST bodies; a count-pinned backfill of the 29 existing PROD rows that
ABORTS on census drift; and the D-04 header correction of `20260810120000`.

⚠️ **This migration AUTO-APPLIES to PROD the moment the PR merges. It is the highest-risk artifact
of the phase.** D-02 is absolute: NO new GRANT or REVOKE on `api_keys` (migration 027's column
allowlist means the new column is simply never granted — state this, do not act on it).

Calibration (bind all prose to this): a SELF-targeted control bypass — an owner mislabelling their
own key to dodge a probe on their own key. Not a tenant leak, not a data breach.

Output: the migration, the corrected 20260810120000 header, SQL test assertion 5, and regenerated
canonical function dumps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-CONTEXT.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-RESEARCH.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-PATTERNS.md
@.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-VALIDATION.md

Key sections: RESEARCH §Q3(a) (the recommended shape, property table), §Q4 (backfill reasoning),
§Q16 (the migration's six ordered parts + conventions), PATTERNS §D1 (every analog with lines),
§D3 (SQL test conventions), No-Analog #1 (the count-pin composes two conventions), §S5/S6.

Planning-time verifications (2026-08-11, this session):
- Assumption A2 CONFIRMED: no `CREATE OR REPLACE` of either RPC after `20260602190000` /
  `20260710180000` (later files only MENTION the names in comments).
- `supabase/schema/functions/create_wizard_strategy.sql` and `add_wizard_composite_key.sql` are
  @generated canonical dumps (header: "DO NOT EDIT BY HAND … Regenerate with `npm run
  schema:functions`") — use them as the re-base SOURCE cross-check, and REGENERATE them after the
  migration lands.
- D-01d census (read-only, 2026-08-11, khslejtfbuezsmvmtsdn): **29 rows total — deribit 13, okx 9,
  bybit 5, mt5 2** (both mt5 rows one owner, created 2026-08-04).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify the census, then write the migration</name>
  <files>supabase/migrations/20260811210000_api_keys_attested_venue.sql</files>
  <read_first>
    - supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql (FULL — header template, trigger analog :110-138, comment marker :153-158, self-verify blocks :161-201, the :181 REVOKE warning D-02 rests on)
    - supabase/migrations/20260602190000_f6_wizard_session_idempotency.sql:62-157 (create_wizard_strategy LATEST body; the advisory-lock fence at :120-128 MUST survive)
    - supabase/migrations/20260710180000_wizard_composite.sql:53-141 (add_wizard_composite_key LATEST body)
    - supabase/schema/functions/create_wizard_strategy.sql + add_wizard_composite_key.sql (canonical dumps — cross-check the re-base)
    - supabase/migrations/20260726000225_strategy_verifications_tenant_scope_uniq.sql:93-119 (BEGIN/lock_timeout frame + pre-flight abort with MEASURED note)
    - supabase/migrations/20260501055202 (:321, :343 — the `IF v_x <> N THEN RAISE EXCEPTION` hand-typed-literal pin shape)
    - 153.6-RESEARCH.md "Code Examples" (the trigger function reference)
  </read_first>
  <action>
    FIRST: re-run the read-only census against PROD `khslejtfbuezsmvmtsdn` via the Supabase MCP
    (`SELECT exchange, count(*) FROM public.api_keys GROUP BY exchange` + total). The census is
    valid for HOURS, not weeks. If it still reads 29 total / 2 mt5, hand-type those literals; if it
    has moved, RE-CUT the literals to the fresh measurement — but if an UNEXPECTED mt5-labelled row
    appeared (a row not created 2026-08-04 by the known owner), STOP: that is the forgery signal
    this phase exists to close; report it, do not backfill over it. If the MCP is unavailable, STOP
    and surface — never guess the counts.

    Write `supabase/migrations/20260811210000_api_keys_attested_venue.sql` (timestamp: actual
    authoring time; MUST sort after 20260810120000). Framed `BEGIN; SET lock_timeout = '3s'; …
    COMMIT;` (the 20260726000225 convention — this migration mixes DDL + data). Header copies
    20260810120000's section order (Background / What changes, numbered / Residual, deliberately
    NOT closed here / Idempotency), calibrated as a SELF-targeted control bypass. Contents, in
    order:

    1. `ALTER TABLE public.api_keys ADD COLUMN IF NOT EXISTS attested_venue text;`
       ⛔ NO GRANT, NO REVOKE anywhere in this file (D-02). State in the header WHY none is needed:
       migration 20260410225608 column-allowlists SELECT, so the new column is simply never granted
       to authenticated; table-level INSERT stays untouched — the trigger is what makes a client-
       supplied value irrelevant.
    2. `CREATE OR REPLACE FUNCTION public.create_wizard_strategy(...)` — re-based VERBATIM on
       20260602190000:62-157 (grep ALL migrations for the function name first and confirm it is
       still the latest; cross-check against the canonical schema/functions dump), with EXACTLY two
       additions: `attested_venue` in the INSERT column list and `p_exchange` in VALUES. Carry the
       re-base sentence naming the source migration (the B5b-lesson idiom at 20260602190000:26-31).
       ⚠️ P3: the body MUST still contain `pg_advisory_xact_lock` — if it does not, the re-base
       took a stale definition.
    3. Same for `public.add_wizard_composite_key(...)` re-based on 20260710180000:53-141.
    4. `CREATE OR REPLACE FUNCTION public.scrub_client_supplied_attested_venue()` — SECURITY
       INVOKER (stated explicitly, with the S5 trap comment: under DEFINER current_user is the
       owner and the check ALWAYS passes — the prevent_profile_role_change no-op bug),
       `SET search_path = public, pg_catalog`; body: privileged callers
       ('postgres','service_role','supabase_admin') RETURN NEW; everyone else
       `NEW.attested_venue := NULL; RETURN NEW;`. DROP TRIGGER IF EXISTS + CREATE TRIGGER
       `api_keys_scrub_attested_venue BEFORE INSERT ON public.api_keys FOR EACH ROW …`.
    5. Backfill: `UPDATE public.api_keys SET attested_venue = exchange WHERE attested_venue IS
       NULL;` with the D-01d rationale comment: a bounded, DATED snapshot — after this migration
       the column stops being an authority for every row created afterwards; the residual is
       exactly "rows that existed on <census date> and had already been forged", and UPDATE on
       exchange has been revoked since 20260810120000, so a pre-phase forgery required DELETE +
       re-INSERT with a detectable created_at footprint.
    6. Self-verify DO blocks — ⚠️ PATTERNS No-Analog #1: no existing migration aborts on a DATA
       count equality; COMPOSE the `IF v_x <> <hand-typed literal> THEN RAISE EXCEPTION … USING
       ERRCODE` shape from the schema-fact pins (20260501055202:321) with the pre-flight/post-verify
       pairing of 20260419140917:11-56, and SAY SO in the block comment. Every literal carries
       `-- MEASURED <date> against khslejtfbuezsmvmtsdn`.

       ⚠️ TEST-APPLICABILITY (planner-resolved design — the census pin is PROD-shaped, but this
       file also applies to TEST `qmnijlgmdhviwzwfyzlc` via MCP and the SQL test gate needs it
       there; a pin that aborts every TEST apply would leave assertion 5 skipping forever, a guard
       that never guards). Use the two-sided discriminator so a PROD apply can NEVER silently skip
       the strict pin:
         v_total := count(*); v_mt5 := count(*) WHERE exchange='mt5';
         v_sig   := count(*) WHERE exchange='mt5' AND created_at::date = DATE '2026-08-04';
         -- (the census signature: PROD's 2 mt5 rows, one owner, both created 2026-08-04 — a
         --  stable, public-safe discriminator; re-cut alongside the counts if the census moved)
         IF v_total = 29 AND v_mt5 = 2 THEN NULL;            -- PROD exactly as censused: pass
         ELSIF v_sig >= 1 THEN RAISE EXCEPTION '… PROD census drift (total=%, mt5=%): re-measure
           and re-cut the pinned literals before applying. Rolling back.', v_total, v_mt5;
         ELSE RAISE NOTICE '… census signature absent — non-PROD apply (TEST/local/CI); strict
           census pin not applicable; structural verifies below still enforce.';
         END IF;
       Residual (state it in the comment, honestly): a PROD state where BOTH census mt5 rows were
       deleted within hours of the census is indistinguishable from TEST and would skip the pin —
       accepted, because deleting both rows also breaks their strategies' key links loudly.
       THEN the environment-independent post-verifies, each ABORTING (never NOTICE-only —
       20260429063138's NOTICE shape is the shape that cannot fail; do not copy it):
       (a) zero rows with `attested_venue IS NULL`; (b) both RPC bodies contain
       `pg_advisory_xact_lock` (read pg_proc/prosrc); (c) the trigger exists on api_keys; (d) the
       exchange column comment still contains '20260810120000' (P4 canary, enforced here too).
    7. `COMMENT ON COLUMN public.api_keys.attested_venue` naming THIS migration id (the gate marker
       for the SQL test's assertion 5) and stating: SERVER-ATTESTED venue, written only by the two
       wizard RPCs, scrubbed to NULL on client INSERT, read by finalize-wizard's scope-broadening
       probe gate; NULL ⇒ probed.
    8. Re-stamp `COMMENT ON COLUMN public.api_keys.exchange`: remove the now-false "Read by
       finalize-wizard as the input to the scope-broadening probe gate" claim (it keeps the
       asset_class annualization sentence — that reader stays on exchange, OQ-2 deliberately out of
       scope), ⛔ PRESERVING the literal substring `20260810120000` (D-19b/P4 — dropping it turns
       the SQL test's assertions 2/3 into silent SKIPs).

    After writing: apply to TEST (`qmnijlgmdhviwzwfyzlc`) via Supabase MCP `apply_migration` and
    confirm the NOTICE branch fires there and all structural verifies pass. Then run
    `npm run schema:functions` to regenerate the canonical dumps for both RPCs (they are @generated
    and now stale).
  </action>
  <verify>
    <automated>grep -v '^\s*--' supabase/migrations/20260811210000_api_keys_attested_venue.sql | grep -ciE "GRANT|REVOKE" | grep -q "^0$" && grep -c "pg_advisory_xact_lock" supabase/migrations/20260811210000_api_keys_attested_venue.sql | grep -qv "^0$" && grep -c "SECURITY INVOKER" supabase/migrations/20260811210000_api_keys_attested_venue.sql | grep -qv "^0$" && grep -c "RAISE EXCEPTION" supabase/migrations/20260811210000_api_keys_attested_venue.sql | grep -qv "^0$"</automated>
  </verify>
  <acceptance_criteria>
    - Census re-measured via MCP immediately before the literals were typed; values + date recorded in the migration comments and the SUMMARY
    - Zero GRANT/REVOKE statements (comment-stripped grep = 0) — D-02
    - Both RPC re-bases contain pg_advisory_xact_lock and the re-base sentence naming their source migration; `git diff` of each body vs its canonical dump shows ONLY the attested_venue additions
    - Trigger function is SECURITY INVOKER with the S5 trap comment
    - Self-verify: strict pin two-sided (pass-exact / abort-on-signature-drift / notice-on-non-PROD), plus four ABORTING structural verifies
    - Applied cleanly to TEST via MCP (NOTICE branch observed); `npm run schema:functions` re-run, dumps updated
    - FALSIFY D-backfill (ledger): in a BEGIN…ROLLBACK on TEST via MCP, fabricate the census signature (insert 2 mt5 rows with created_at 2026-08-04 under a test user) plus a drift row, run the self-verify block body, observe the RAISE EXCEPTION, ROLLBACK; paste the error into the ledger
  </acceptance_criteria>
  <done>Migration authored, TEST-applied, dumps regenerated, D-backfill mutation observed aborting.</done>
</task>

<task type="auto">
  <name>Task 2: D-04 header correction + SQL test assertion 5 (the round trip)</name>
  <files>supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql, supabase/tests/test_api_keys_exchange_not_user_writable.sql</files>
  <read_first>
    - supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql (header :1-100 — the "can never be corrected back" claim ~:88-91 and the residual argument resting on it)
    - supabase/tests/test_api_keys_exchange_not_user_writable.sql (FULL — the gate :110-127, positive control :100-109, refused-AND-unchanged double :130-156, DELETE canary :181-192, ROLE fixture :83-98)
    - 153.6-RESEARCH.md §Q16 item 6 + "D-05's round-trip case, and its honest limit"
  </read_first>
  <action>
    (a) Correct 20260810120000's header prose (D-04): the file is ALREADY APPLIED to PROD — state
    explicitly in the edit that this is a DOCUMENTARY correction, comments only, zero DB effect.
    Remove/rewrite the "the mislabelled row can never be corrected back" claim and the
    "self-defeating forgery" residual argument that rests on it; the corrected text records the
    truth: table-level INSERT and DELETE survive (deliberately — line 181's own warning), so
    DELETE + re-INSERT under a forged exchange was live until 20260811210000 made the probe gate
    read the server-attested venue instead. Calibrate as self-targeted control bypass.
    (b) Extend `test_api_keys_exchange_not_user_writable.sql` with assertion 5 behind its OWN gate
    keyed on the NEW migration id via the attested_venue column comment (the test DB lags main;
    skip LOUDLY with the file's existing skip idiom when absent). Assertion 5, using the existing
    ROLE/JWT fixture: seed a row as postgres with `attested_venue = 'binance'` and assert it
    STORED (positive control — the privileged path survives the trigger; without it the scrub
    assertion could pass vacuously); then `SET LOCAL ROLE authenticated`, DELETE the row (keep
    assertion 4, the over-broad-REVOKE canary, untouched and passing), re-INSERT it with
    `exchange = 'mt5'` AND an explicit `attested_venue = 'mt5'`; assert BOTH halves of the
    succeeded-AND-scrubbed double (the file's :130-156 idiom inverted): the INSERT SUCCEEDS
    (D-02/D-03 — client INSERT stays live) AND the stored attested_venue reads back NULL.
    Also add the marker-present positive assertion for the exchange comment's 20260810120000
    substring (P4: prove the gate evaluated TRUE, so a future re-stamp that drops it fails instead
    of skipping).
    State in a comment what this file CANNOT prove (D-05's honest limit): SQL asserts the probe's
    INPUT is unforgeable; the probe's OUTCOME (a forged row is PROBED) is the TS half in
    finalize-wizard/route.test.ts (plan 04). Neither half alone discharges D-05.
  </action>
  <verify>
    <automated>grep -c "attested_venue" supabase/tests/test_api_keys_exchange_not_user_writable.sql | grep -qv "^0$" && grep -c "20260810120000" supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql | grep -qv "^0$" && bash -c 'grep -q "never be corrected back" supabase/migrations/20260810120000_lock_api_keys_exchange_column.sql && exit 1 || exit 0'</automated>
  </verify>
  <acceptance_criteria>
    - Header falsehood removed; correction marked documentary; residual argument rewritten
    - Assertion 5 present: own gate on the new migration id, positive control, succeeded-AND-scrubbed double, DELETE canary (assertion 4) untouched
    - Marker-present positive assertion added for the 20260810120000 substring
    - CI SQL job green (supabase/tests auto-discovery — the ONLY place SQL tests run; verify on the PR)
    - FALSIFY D-trigger (ledger): in a BEGIN…ROLLBACK on TEST via MCP, disable/drop the trigger, run assertion 5's client re-INSERT, observe the forged value PERSIST (i.e. the assertion body would red), ROLLBACK; record in the ledger (CI-only row — this MCP transaction is the local stand-in)
  </acceptance_criteria>
  <done>Header truthful; the round trip is tested end-to-end at the SQL layer with its honest limit stated; D-trigger falsification recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → api_keys writes | INSERT/DELETE remain granted (D-02); everything the client writes is untrusted |
| SECURITY DEFINER RPCs → api_keys | the ONLY privileged wizard writers; their p_exchange is server-validated at mint |
| migrations merge → PROD | supabase/migrations/** auto-applies on merge; no staging step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.6-D1 | Elevation of privilege (self-scoped) | DELETE + re-INSERT with forged exchange to dodge the ASVS V4 scope-broadening probe | mitigate | attested_venue written only by the DEFINER RPCs; BEFORE INSERT trigger NULLs client values; probe gate reads attestation only (plan 04) |
| T-153.6-D2 | Tampering | the trigger itself | mitigate | SECURITY INVOKER (DEFINER would make the privilege check a no-op — the prevent_profile_role_change bug, stated at the function) |
| T-153.6-D3 | Tampering (regression) | RPC re-base | mitigate | re-based on grep-verified LATEST bodies; self-verify aborts if pg_advisory_xact_lock absent (F6 fence) |
| T-153.6-D4 | Denial of service (self) | MT5 finalize after a backfill-less deploy | mitigate | count-pinned backfill; NULL ⇒ probed would otherwise hand every MT5 finalize a permanent KEY_SCOPE_CHECK_UNAVAILABLE |
| T-153.6-D5 | (meta) guard silently disarmed | SQL test gate on the comment substring | mitigate | substring preserved + marker-present positive assertion + migration self-verify (d) |
| T-153.6-D6 | Information disclosure | census literals in a PUBLIC repo | accept | counts and a date only; no key material, no emails; ⛔ gitleaks allowlist is PATH-based over .planning/ — keep secrets out of plan prose |
| T-153.6-SC | Tampering | package installs | accept | phase installs nothing |
</threat_model>

<verification>
- Migration applies on TEST via MCP (NOTICE branch); CI SQL job green; PROD apply on merge expected to take the strict-pass branch
- Comment-stripped grep: zero GRANT/REVOKE in the new migration
- Both RPC diffs vs canonical dumps show only the attested_venue additions; dumps regenerated
- Ledger rows D-backfill and D-trigger reconciled with observed evidence
</verification>

<success_criteria>
- attested_venue exists, unforgeable by construction on every client path, stamped by both RPCs
- 29 pre-phase rows backfilled; the pin aborts on PROD drift and passes on TEST
- 20260810120000's header tells the truth; the SQL test proves the round trip at the input layer
</success_criteria>

<output>
Create `.planning/phases/153.6-parity-the-fixes-that-only-landed-on-one-path/153.6-03-SUMMARY.md` when done.
Record: the fresh census values + timestamp, the TEST apply output, both falsification observations.
</output>
