---
phase: 153.6
reviewer: migration-reviewer (third pass, fresh agent)
date: 2026-08-12
status: clear
findings: 0 CRITICAL, 0 HIGH, 0 MEDIUM, 0 LOW
verdict: CLEAR — nothing clears the bar
audits: supabase/migrations/20260811210000_api_keys_attested_venue.sql @ 950 lines
---

# 153.6 — Migration Audit, Round 3 (CLEAR)

> Transcribed by the orchestrator; the agent's constraints block it from writing report files.

Round 3 ran under an explicit impact bar: report only confidence >=7 findings that cause
production breakage, security bypass, data-integrity loss, a guard that cannot fail, or a
durable false guarantee. Prose, placement and hygiene were explicitly out of scope, and a
clean result was named as the expected outcome.

**Empty findings list. Nothing clears the bar.**

## Method — measured, not inferred

Built a PG16 fixture on loopback TCP (`initdb -U postgres`, port 55432) reproducing
`auth.users`/`auth.uid()`, `profiles`, `api_keys` (with `api_keys_exchange_check` and the
migration-027 column-grant posture), `strategies` + `strategies_user_wizard_session_uniq`,
the four Supabase roles, and — importantly — **pre-existing RPC stubs with
`REVOKE ALL … FROM PUBLIC` + `GRANT EXECUTE TO authenticated`**, so `CREATE OR REPLACE`'s
ACL preservation is exercised the way it will be on PROD. Fixture torn down; no source file
modified.

### Priority 1 — the reordering (round 2's biggest new risk)

Walked the transaction in execution order: `ALTER` 227 → `$constraint$` 285 → RPCs 306/418
→ trigger 515-565 → `$census$` 590 → `SET LOCAL` 695 → backfill 697 → both
`COMMENT ON COLUMN` 713/755 → `$verify$` 771 → `COMMIT`.

Only two cross-statement reads exist: the GUC (written 695, read 700 and 801) and
`api_keys.exchange`'s comment (written 755, read 853). **Both are now written-before-read;
nothing is written twice.** Applied against a PROD-shaped seed (13 deribit / 9 okx / 5 bybit
/ 2 mt5 @ 2026-08-04 11:37 + 14:20 UTC): clean commit, `UPDATE 29`, both NOTICEs correct.
M2-02's fix verified by mutation — stripping the `20260810120000` substring from the
re-stamp now **ABORTS** at (d) instead of committing.

### Priority 2 — guards that cannot fail ⭐

Ran a mutation battery. **Five mutations survive the migration's own `$verify$` block:**
trigger `BEFORE INSERT`→`AFTER INSERT`; →`BEFORE UPDATE`; scrub body neutered; scrub
allowlist widened with `authenticated`; and both RPCs stripped of the `attested_venue` write
(check (b) arm 3 greps `pg_get_functiondef` for a token that also appears in the body's own
comments, so it survives that edit).

**Then ran `supabase/tests/test_api_keys_exchange_not_user_writable.sql` against each of
those five applied states: it fails loudly and by the correct name on every one** — 5c for
the four trigger/scrub mutations, 5d for the RPC mutation — while passing on the unmutated
control.

Conclusion: the guard *surface* fails under each mutation. The `$verify$` block is a
**redundant layer over the enforcing test, not a vacuous one.** Controls confirmed:
backfill deleted → (a) aborts; `NOT VALID` → (g) aborts.

### Priority 3 — the DATE[] rework (M2-01)

Both directions hold. Aborts on all three PROD drift shapes: 3rd mt5 on a new date
(`mt5=3, sig=2`), 3rd mt5 on the *pinned* date (`mt5=3, sig=3`, caught by the
`v_mt5 <> c_pin_mt5` arm), and one censused mt5 deleted (`mt5=1, sig=1`). Total drift
(+4 okx) commits with the reported delta, as designed. Signature-free TEST-shaped and empty
databases both take the lenient NOTICE branch and commit, leaving assertion 5 armed.

### Priority 4 — live PROD apply

Clean. **Idempotent, proven:** applied twice with a post-apply `attested_venue IS NULL` row
created after the cutoff inserted between runs — the second apply committed and **left that
row NULL** (`nulls=1, total=30`), so the dated cutoff really does prevent retro-attestation.

The 2 live mt5 rows (2026-08-04 < 2026-08-11T00:00Z) are inside the backfill, and the census
guarantees no unmeasured probe-exempt row can exist at apply time, so no live MT5 key can
regress to `KEY_SCOPE_CHECK_UNAVAILABLE`.

Check (e) will not spuriously abort PROD: `20260411103316` / `20260513084844` /
`20260515114310` / `20260602190000` and `20260710180000` all `REVOKE ALL … FROM PUBLIC, anon`
+ `GRANT EXECUTE … TO authenticated`. No other production writer INSERTs `api_keys` (three
browser sites only: `ApiKeyManager.tsx:254`, `StrategyForm.tsx:140`,
`AllocatorExchangeManager.tsx:591`); other migration hits are in-transaction self-verify
probes or superseded RPC bodies. `api_keys_stamp_first_added` is `AFTER INSERT`, so no
BEFORE-trigger ordering interaction.

### Priority 5 — mechanical

D-02 `GRANT|REVOKE` over non-comment lines = **0**. Template artifacts = 0. Exactly one
`BEGIN;`/`COMMIT;`, no `ROLLBACK`. Dollar tags balanced and distinct (`$$`×6,
`$census$`/`$constraint$`/`$verify$` ×2 each). No `||` in any `RAISE` format string.
**Every argument-bearing `RAISE` was force-fired** — census abort, both pre-flight NOTICEs,
(a), (d), (g)-NOT-VALID, (f) owner-mismatch, (f) role-loop, (e) authenticated-lost, (e)
anon-acquired — all substituted correctly, so no placeholder/argument mismatch hides in an
untaken branch. `search_path = public, pg_catalog` on all three functions. Filename
conformant, no timestamp collision, no APFS case collision, `20260811210000` > remote tip so
the backdate workflow passes with no allowlist entry.
`npx tsx scripts/dump-sql-functions.ts --check` → "SQL function snapshot is current (110
functions)". `20260810120000` byte-identical to `origin/main` (invariant 11 satisfied).

---

## One sub-bar observation — logged, explicitly NOT a finding

Assertion 5's gate keys on the `20260811210000` substring in `api_keys.attested_venue`'s
comment. Measured: dropping that substring commits the migration and makes the whole 5a–5e
family print `SKIP (5)`. The migration has a post-verify for the *exchange* marker (check
(d)) but no symmetric one for this marker, which gates strictly more.

The file carries the marker today and all of 5a–5e were proven to run. The realistic path to
losing it is a *future* migration re-stamping that comment, at which point this file's
`$verify$` no longer runs — so a symmetric check would buy little.

Guard hygiene, not user-facing or data-integrity. Logged to `TODOS.md` per the repo's
stopping rule. **Must not spawn a round 4.**
