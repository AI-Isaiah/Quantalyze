---
phase: 153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered
verified: 2026-08-14T06:54:47Z
status: human_needed
score: 9/9 must-haves verified
overrides_applied: 0
re_verification: null
gaps: []
warnings:
  - id: W-153.7-1
    concern: >-
      THE VERDICT-TABLE → CLIENT-ROSTER HOP IS UNGUARDED, and it is the one hop
      that decides whether a disposition reaches the user. MEASURED, not
      inferred: deleting the line `"SEAM_INTERNAL_FAULT",` from
      `KNOWN_CREATE_WITH_KEY_CODES` (ConnectKeyStep.tsx) leaves
      ConnectKeyStep.test.tsx + wizardErrors.test.ts +
      wizardErrors.invariant.test.ts + seam-venue-vocabulary.invariant.test.ts
      ALL GREEN (312 passed / 0 failed) — while the wizard would then render
      `code: UNKNOWN` with a Retry control for MT5_GATEWAY_UNCONFIGURED,
      ADAPTER_INIT_FAILED and INTERNAL, three faults the service marks
      `retryable=False`. That is the exact shape of the 2026-08-05
      SERVICE_UNREACHABLE incident this roster's own docblock records. The
      asymmetry is structural: `wizardErrors.invariant.test.ts`'s ROUTES guard
      cross-checks each route's LITERAL-minted codes against its roster (which
      is why the three new finalize codes ARE covered), but codes returned by
      `classifyKeyValidationError` are never literals in the route, so they fall
      outside `expectedSites` entirely.
    severity: warning
    kind: fail-open-one-hop-down
    blocking: false
  - id: W-153.7-2
    concern: >-
      OWNERSHIP DANGLES. `KNOWN_CREATE_WITH_KEY_CODES`' docblock says "do not
      grow this list further, derive it there", assigning the derived-roster
      class fix to "Phase 153 / WIZFORM-02". Phase 153.7 grew the list at BOTH
      key steps (SEAM_INTERNAL_FAULT) without deriving it, and WIZFORM-02 is now
      ticked COMPLETE — so that pointer now names a closed requirement and the
      class fix has no owner. It is not recorded in TODOS.md (grepped).
    severity: warning
    kind: process
    blocking: false
  - id: W-153.7-3
    concern: >-
      COVERAGE-LAW ROW 1 REACH IS UNCHANGED AND NARROWER THAN THE PHASE GOAL'S
      WORDING. `ROUTES` in wizardErrors.invariant.test.ts still enumerates
      exactly THREE Next route files and matches only literal
      `NextResponse.json({ code: "X" }, { status })` sites. Six of our own Next
      routes mint `code: "UNAUTHENTICATED"` in TypeScript (named in the
      UNAUTHENTICATED exemption row) and are outside any derived population. The
      phase's declared scope was the PYTHON half and the four `missing` items —
      all discharged — so this is pre-existing and out of scope, but it bounds
      the literal claim "every code that can reach a user-facing surface".
    severity: warning
    kind: scope-boundary
    blocking: false
  - id: W-153.7-4
    concern: >-
      DOCUMENTATION — REQUIREMENTS.md's WIZFORM-02 rollup writes the residue as
      "six analytics-service codes still render `UNKNOWN` … plus
      `KEY_UNDECRYPTABLE` on `keys/[id]/permissions`". Measured: that route's
      terminal answers PROBE_FAILED / PROBE_BACKEND_UNAVAILABLE / PROBE_TIMEOUT
      / PROBE_RATE_LIMITED / CIRCUIT_OPEN and NEVER `UNKNOWN`. The TODOS.md
      entry states it correctly ("what is wrong is the accuracy of a remedy
      sentence"); the REQUIREMENTS.md sentence overstates it by one code.
    severity: warning
    kind: documentation
    blocking: false
human_verification:
  - test: >-
      Render the wizard connect step against a create-with-key response of
      `{ code: "SEAM_INTERNAL_FAULT" }` (503/500 path) — e.g. by forcing the
      MT5 gateway env unset locally, or by stubbing the route response — and
      read the card.
    expected: >-
      Title "Something failed on our side while we checked this key.", the
      cause paragraph, both fix bullets, a correlation id, `request_call` and
      `expand_log` controls, and NO Retry control. NOT "We could not classify
      this failure".
    why_human: >-
      No test renders SEAM_INTERNAL_FAULT in a component. The unit tests assert
      `buildEnvelope(...).recoverable === false` and the classifier verdict, but
      nothing exercises ConnectKeyStep's membership check for this code — which
      is precisely the hop W-153.7-1 measured as unguarded. This is the check
      that would catch a roster regression today.
  - test: >-
      Render the wizard submit step against each of the three newly-coded
      finalize rejections (`DRAFT_LOOKUP_FAILED` 500, `DRAFT_FINALIZE_FAILED`
      500, `SEAM_RESPONSE_UNREADABLE` 502).
    expected: >-
      Three DISTINCT cards. `DRAFT_LOOKUP_FAILED` may say nothing was changed;
      `DRAFT_FINALIZE_FAILED` must NOT claim nothing was saved;
      `SEAM_RESPONSE_UNREADABLE` must claim neither outcome and must offer
      `leave_and_return` (open your strategies list) with no Retry.
    why_human: >-
      Route-level tests assert the wire `code`, and the copy table is
      size-pinned, but nothing renders these three on the SubmitStep surface.
      The distinction between the three sentences is the whole reason three
      members were minted rather than one, and it is a copy/visual judgement.
  - test: >-
      DESIGN.md conformance pass on the four new WIZARD_ERROR_COPY members
      (SEAM_INTERNAL_FAULT, SEAM_RESPONSE_UNREADABLE, DRAFT_LOOKUP_FAILED,
      DRAFT_FINALIZE_FAILED) as rendered in the error envelope.
    expected: >-
      Voice, sentence length and control affordances match the incumbent
      entries; no new visual treatment introduced.
    why_human: >-
      Copy voice and visual conformance are founder-owned per DESIGN.md and
      cannot be asserted programmatically.
---

# Phase 153.7: WIZFORM-02-CLASS — every code that can reach a user is covered — Verification Report

**Phase Goal:** WIZFORM-02's CLASS closes — the coverage law's population is derived from every code that can reach a user-facing surface, so a server-classified failure can never again render `code: UNKNOWN`; and a newly-minted `service_error(...)` code with no disposition reds CI by name instead of arriving on a user's screen.
**Verified:** 2026-08-14T06:54:47Z
**Status:** human_needed
**Re-verification:** No — initial verification of 153.7 (it is itself the gap closure for `153-VERIFICATION.md`, status `failed`, 5/6)
**Verified against:** branch `feat/phase-153.7-wizform-02-class`, working tree CLEAN before and after every mutation below.

---

## How this report was produced

⛔ **No number, table or receipt in any SUMMARY.md was accepted as evidence.** The two load-bearing
oracles were re-derived from scratch by this verifier:

1. **A brand-new Python `ast` census**, written here, over `analytics-service/**` with the same
   three exclusions the guard declares, computing the union of the assignment shape and the
   four-callee call shape. It returned **exactly 37 codes, member for member identical** to the
   hand-typed `EXPECTED_EMITTED_CODES`. It also independently reproduced both exclusion count pins
   (lifting the `tests` exclusion adds exactly `CSV_PARSE_FAILED`, `SOME_FUTURE_CODE`,
   `SOME_FUTURE_UNLISTABLE_CODE`; lifting `services/error_contract.py` adds **zero** codes).
2. **Every Falsifiability-Ledger mutation was actually applied to the working tree, run, and
   reverted** — five mutations, all restored, `git status --short` empty afterwards.

`git diff --stat 6ae4352d..HEAD -- analytics-service/` is EMPTY, so the census could not have gone
stale — and it was regenerated anyway rather than read from the frozen table.

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | The derived population is every user-reachable analytics-service code — both roots and both shapes, 17 → 37 | ✓ VERIFIED | Independent AST census written by this verifier returns the same 37 codes, member for member. Root `analytics-service/**`; `CALLEE_ARG_SLOT` covers `service_error`(1) / `service_error_response`(1) / `service_error_body`(**0**) / `VenueTransientHTTPException`(`code=`). 42 call sites, 2 literal-less, 22 distinct call-shape codes — my census agrees on all four figures |
| 2 | `EXPECTED_EMITTED_CODES` (37) and `DERIVED_FLOOR` (22) are HAND-TYPED, not `[...derived]` | ✓ VERIFIED | Read member-for-member at `seam-venue-vocabulary.invariant.test.ts` lines 588–626: 37 string literals, one per line. `const DERIVED_FLOOR = 22;` is a bare integer literal with the 0.6 × 37 arithmetic stated. No `[...derived]`, no `Array.from(derived)`, no `derived.size` anywhere in either pin |
| 3 | A code with neither a verdict row nor a reasoned exemption reds CI **BY NAME** | ✓ VERIFIED | FL-1 mutation run by this verifier: deleted the `MT5_GATEWAY_UNREACHABLE` verdict row → assertion failed with `expected [ 'MT5_GATEWAY_UNREACHABLE' ] to deeply equal []`. Restored |
| 4 | A NEW undisposed `service_error(...)` under `routers/` reds the composed gate | ✓ VERIFIED | The falsifier (`mkdtempSync` → `<tmp>/routers/zzz.py` → **the REAL** `deriveEmitterSites(tmp)` → **the REAL** `VENUE_WIRE_CODE_TO_VERDICT` / `VENUE_WIRE_CODES_WITHOUT_VERDICT` Maps → `toEqual(["ZZZ_FALSIFIER_ROUTER_CODE"])`). Not a second copy of the scanner. Observed to RED under the FL-2 matcher-narrowing mutation |
| 5 | All 37 codes are dispositioned — 8 verdict rows, 12 individually-measured exemptions | ✓ VERIFIED | 8 rows land in `VENUE_WIRE_CODE_TO_VERDICT` with per-code reasoning; each has a replay test through the REAL `classifyKeyValidationError` using the byte-copied Python `detail=` string **plus a NO-CODE CONTROL** proving the table (not the cascade) moved the verdict. The 12 `WITHOUT_VERDICT` rows each name their OWN consuming route and OWN arm — read all twelve; none is a copy of another |
| 6 | The mt5-gateway family no longer renders `UNKNOWN`, on **BOTH** key routes, falsifiably | ✓ VERIFIED | FL-4 mutation run here: one deleted shared row → 4 failures across 4 files, including `create-with-key/route.test.ts` **and** `composite/add-key/route.test.ts` together. Twin-path proven; the fix lives in shared `wizardErrors.ts`, zero route-local edits |
| 7 | Every `finalize-wizard` rejection carries a code; ledger 0; `EXPECTED_FINALIZE_REJECTION_SITES` stays 32 | ✓ VERIFIED | `KNOWN_CODELESS_FINALIZE_REJECTIONS = 0`, `EXPECTED_FINALIZE_REJECTION_SITES = 32` (never edited), `expectedSites` 29 → 32. FL-5 mutation run here: bumping the pin to 33 reds the reconciliation. Three arms coded in the route diff and all three in `KNOWN_FINALIZE_CODES` |
| 8 | Under-reach cannot pass silently — the fail-open hazard is closed | ✓ VERIFIED | Three narrowing mutations run here. Drop the call scan → **12** reds incl. roster, floor, both-shapes, reach pin, falsifier. Narrow root back to `services/` → **7** reds incl. REACH PIN. Drop `service_error_body` from `CALLEE_ARG_SLOT` → **3** reds incl. REACH PIN by name. All restored |
| 9 | Prose records match the widened predicate and the measured enclosing symbols | ✓ VERIFIED | REGISTRY.md + contracts-registry.test.ts re-cut in lockstep (both green). `resilient-fetch.ts` census 13 → 15 with the anchors corrected — AST-confirmed here: `MT5_GATEWAY_UNREACHABLE` ×2 in `_connect_and_probe`; `MT5_GATEWAY_UNCONFIGURED` ×3 in `_validate_mt5_key_probe` + ×1 in `_connect_and_probe`. The **counting (503) set is unchanged** — no breaker key moved |

**Score:** 9/9 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/lib/seam-venue-vocabulary.invariant.test.ts` | Parameterised root, call-shape matcher with per-callee slots, pinned exclusions, 37-member roster, floor 22, reach pin, both-shapes, tmpdir falsifier | ✓ VERIFIED | 1219 lines (was ~313). `CALLEE_ARG_SLOT` present. 20 tests, all green. `deriveEmitterSites(root, exclusions)` genuinely parameterised — the falsifier and the exclusion self-test both call it on synthetic roots |
| `src/lib/wizardErrors.ts` | 8 verdict rows + 12 exemption rows + 4 new copy members | ✓ VERIFIED | Rows at 3057–3064 and 3155–3300+. Four new `WIZARD_ERROR_COPY` members (`DRAFT_LOOKUP_FAILED`, `DRAFT_FINALIZE_FAILED`, `SEAM_INTERNAL_FAULT`, `SEAM_RESPONSE_UNREADABLE`) — 76 → 80 |
| `src/lib/wizardErrors.test.ts` | Group-A replays + neuter-proof + table-size pins | ✓ VERIFIED | 8 replay tests each with a NO-CODE CONTROL; a behavioural `recoverable` derivation test; `EXPECTED_TABLE_SIZE = 80` at both declarations with a guard asserting the two cannot diverge |
| `src/app/api/strategies/finalize-wizard/route.ts` | Three formerly code-less arms coded | ✓ VERIFIED | Diff shows all three, each with the claim-scope reasoning; route tests assert each `body.code` |
| `src/app/api/strategies/composite/add-key/route.test.ts` | Twin-path regression | ✓ VERIFIED | Redded together with `create-with-key` under FL-4 |
| `TODOS.md` | Two recorded deferrals | ✓ VERIFIED | Both present under *Phase 153.7 — recorded deferrals*, specific and separate, each stating why a verdict row could not fix it |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `deriveEmitterSites` (widened) | the EXISTING disposition assertion | the `derived` set — assertion body untouched | ✓ WIRED | No duplicate both-halves assertion was written; the incumbent `it(...)` block runs over the widened set (confirmed at lines 711–730) |
| tmpdir falsifier | `deriveEmitterSites(root)` | the REAL function over a synthetic root | ✓ WIRED | `mkdtempSync` + real Maps; no second scanner copy |
| `classifyKeyValidationError` seamCode lookup | the 8 new verdict rows | `VENUE_WIRE_CODE_TO_VERDICT.get(seamCode)` | ✓ WIRED | Both key routes call the shared classifier; FL-4 proves the edge is live at both |
| each new verdict's `WizardErrorCode` | `KNOWN_CREATE_WITH_KEY_CODES` / `KNOWN_ADD_KEY_CODES` | roster membership | ⚠️ WIRED, UNGUARDED | Membership IS present at both steps (ConnectKeyStep:350, MultiKeyConnectStep:330) and `SEAM_MISCONFIGURED` correctly resolves through the translate-first hop. But **nothing reds if the membership is removed** — see W-153.7-1 |
| the three finalize arms | `KNOWN_FINALIZE_CODES` in SubmitStep.tsx | route scanner + roster cross-check | ✓ WIRED **and guarded** | `wizardErrors.invariant.test.ts`'s ROUTES entry cross-checks literal-minted route codes against the roster; these three are literals, so they ARE covered. (The asymmetry with the row above is exactly W-153.7-1) |

---

### Data-Flow Trace (Level 4)

| Artifact | Data variable | Source | Produces real data | Status |
|----------|---------------|--------|--------------------|--------|
| `seam-venue-vocabulary.invariant.test.ts` | `derived` | `deriveEmitterSites(ANALYTICS_ROOT)` over real `.py` files on disk | Yes — 37 codes, independently reproduced | ✓ FLOWING |
| `VENUE_WIRE_CODE_TO_VERDICT` | `verdict` | consumed by `classifyKeyValidationError` at both key routes | Yes — FL-4 proves the consumer changes behaviour | ✓ FLOWING |
| `WIZARD_ERROR_COPY.SEAM_INTERNAL_FAULT` | `actions` | `buildEnvelope` → `RECOVERABLE_ACTIONS` | Yes — `recoverable:false` genuinely DERIVED from the absence of `clear_and_retry` / `try_another_key`, asserted in test | ✓ FLOWING |
| ConnectKeyStep error card | `code` | route `{ code }` → translate-first → roster membership | Yes today; no render test | ⚠️ FLOWING, unproven at the DOM (human item 1) |

---

### Behavioural Spot-Checks

| Behaviour | Command | Result | Status |
|-----------|---------|--------|--------|
| Coverage-law file is green over the widened population | `vitest run src/lib/seam-venue-vocabulary.invariant.test.ts src/lib/wizardErrors.invariant.test.ts` | 2 files, 50 tests passed | ✓ PASS |
| Independent AST census agrees with the hand-typed roster | custom `python3 ast` script (written by verifier) | 37 codes, exact match | ✓ PASS |
| Python census guard still green from the right cwd | `cd analytics-service && python3 -m pytest -q tests/test_raw_5xx_census.py` | 19 passed | ✓ PASS |
| Typecheck | `npx tsc --noEmit` | exit 0 | ✓ PASS |
| Seam budgets / wiring / contracts unaffected | `vitest run seam-budgets.invariant + resilient-fetch.wiring + contracts-registry + seam-wire-vocabulary + seam-transport-attribution` | 5 files, 197 tests passed | ✓ PASS |

---

### Falsifiability Ledger — every mutation applied, run and reverted by this verifier

| FL | Mutation applied | Observed | Status |
|----|------------------|----------|--------|
| FL-1 | Deleted `["MT5_GATEWAY_UNREACHABLE", …]` from `VENUE_WIRE_CODE_TO_VERDICT` | Disposition assertion RED **naming the code**; Group-A replay RED | ✓ VALIDATED |
| FL-2a | Disabled the call-shape half of the scanner | 12 RED: floor, roster, exclusion pins, fossil, blind-spot, **reach pin**, **both-shapes**, **falsifier**, 4 SELF-TESTs | ✓ VALIDATED |
| FL-2b | Narrowed the root back to `analytics-service/services` | 7 RED incl. **reach pin** and **both-shapes** | ✓ VALIDATED |
| FL-2c | Dropped `service_error_body` from `CALLEE_ARG_SLOT` | 3 RED incl. **REACH PIN by name** and the per-callee slot SELF-TEST | ✓ VALIDATED |
| FL-3 | (covered by FL-2a) | `FALSIFIER — a NEW undisposed service_error(...) code in routers/ reds the gate, by name` was among the 12 | ✓ VALIDATED |
| FL-4 | (same edit as FL-1) | **create-with-key** AND **composite/add-key** regression tests RED **together** | ✓ VALIDATED |
| FL-5 | Bumped `EXPECTED_FINALIZE_REJECTION_SITES` 32 → 33 | `finalize-wizard: NOT ONE NEW rejection may answer without a code` RED | ✓ VALIDATED |
| **extra** | Removed `"SEAM_INTERNAL_FAULT"` from `KNOWN_CREATE_WITH_KEY_CODES` | **312 passed / 0 failed — NOTHING RED** | ✗ NOT GUARDED → W-153.7-1 |

Tree restored after every mutation; `git status --short` empty.

---

### Oracle Independence checklist (VALIDATION.md O-1..O-7), re-checked

| # | Oracle | Verdict | How this verifier checked it |
|---|--------|---------|------------------------------|
| O-1 | `EXPECTED_EMITTED_CODES` hand-typed | ✅ independent | Read all 37 literals; grepped the file for `[...derived]` / `Array.from(` / `derived.size` in a pin position — none |
| O-2 | `DERIVED_FLOOR` hand-typed | ✅ independent | `const DERIVED_FLOOR = 22;` bare literal |
| O-3 | AST census by a DIFFERENT mechanism, REGENERATED | ✅ independent | Wrote and ran a fresh `python3 ast` census; exact agreement. `analytics-service/` has ZERO drift since `6ae4352d`, so staleness was impossible either way |
| O-4 | `dynamicishByFile` hand-typed per-file counts | ✅ independent | Present and redded under three separate narrowing mutations |
| O-5 | 8 Group-A replays against the REAL classifier | ✅ independent | Each carries a NO-CODE CONTROL asserting the same sentence still lands at `UNKNOWN` without the code — this is what makes "the table moved the verdict" checkable |
| O-6 | tmpdir falsifier calls the REAL `deriveEmitterSites` | ✅ independent | Read the body; real function, real Maps, `rmSync` in `finally` |
| O-7 | `EXPECTED_FINALIZE_REJECTION_SITES` invariance | ✅ independent | Still 32, never edited in the diff; bumping it reds |

⛔ **None of the three forbidden constructions is present.** No roster written as `[...derived]`. The
census was regenerated, not read frozen. The twelve `WITHOUT_VERDICT` reasons are twelve distinct
measurements — each names its own consuming route and its own arm (`_gate_process_key`'s two arms
distinguished by which runs first; `get_key_permissions`' two codes distinguished by REMEDY;
`recompute`'s three distinguished by which lookup failed and by permanence; `eval_metrics`' two
distinguished by which path out of the route they take; portfolio's two distinguished by
before/after the computation started; `portfolio_simulator` by its surface). Several rows state
plainly that the code STILL renders `UNKNOWN` on its own non-wizard surface rather than hiding it.

---

## Audit of the executors' judgement calls

**1. Minting `SEAM_INTERNAL_FAULT` (`EXPECTED_TABLE_SIZE` 76 → 77, then → 80 with plan 03's three).**
**UPHELD.** `SEAM_MISCONFIGURED`'s copy asserts *"we stopped before sending the request"*. Measured at
the emitters: `INTERNAL` (`exchange.py`, inside `validate_key`) fires in the generic escape from
`validate_key_permissions` — **after** the venue probe was issued; `ADAPTER_INIT_FAILED` is a code
fault; and `MT5_GATEWAY_UNCONFIGURED`'s D-31 arm (AST-confirmed enclosing function
`_validate_mt5_key_probe`) fires **after** the gateway terminal ran and refused to classify. The
claim is false at all three, so reusing that member would have shipped a false sentence.
`SEAM_INTERNAL_FAULT`'s own copy — *"We never store a key we could not check, so no key was stored"* —
is true at every emitter that routes to it: all three live inside `validate_key`, which both key
routes call before `encryptKey` and before the create RPC. `recoverable:false` is **genuinely
derived**: `actions: ["request_call", "expand_log"]` contains neither member of
`RECOVERABLE_ACTIONS`, and a test asserts `buildEnvelope(...).recoverable === false` for it while
asserting `true` for the two transient siblings. The three `SEAM_MISCONFIGURED` assignees were
checked at source too — `EGRESS_PROXY_MISCONFIGURED` raises at `make_sfox_client` construction
ABOVE the `get_balances()` try, and `KEK_UNAVAILABLE` is `encrypt_key`'s literal first statement.

**2. `ANALYTICS_ROW_NOT_CREATED` / `PORTFOLIO_ANALYTICS_FAILED` dispositioned as reaching no user.**
**UPHELD, re-derived independently.** There is no `src/app/api/portfolio-analytics` route
(`find`/`ls` over `src/app/api` — the only portfolio routes are optimizer, pdf, alerts, strategies,
documents). `computePortfolioAnalytics` appears in `src` **only** in its own definition
(`analytics-client.ts:813`), in test files, and in three independent prose pins
(`seam-retry-registry.ts`, `resilient-fetch.ts`, `tenant-claim.ts`). Zero production callers.
The two exemption rows also differ on the one thing a disposition turns on (before vs after the
computation ran) and both record the day-a-caller-appears condition.

**3. The gating question — `keys/[id]/permissions`. RE-DERIVED INDEPENDENTLY. UPHELD.**
Reachability: `SyncPreviewStep.tsx:2476` renders `<KeyPermissionBadge apiKeyId={apiKeyId} />`, so the
route IS reachable from a wizard surface. Rendering: `KeyPermissionBadge.tsx:119-122` builds
`err.code ? \`${err.code}: ${message}\` : message` and throws it as plain text — it never calls
`buildEnvelope`, never touches `wizardErrors`, and no `UNKNOWN` card can render there. Emission: the
route's terminal answers `PROBE_BACKEND_UNAVAILABLE` / `PROBE_TIMEOUT` / `PROBE_FAILED` (`:562-565`),
plus `PROBE_RATE_LIMITED` and `CIRCUIT_OPEN` — **`UNKNOWN` is not in its vocabulary at all**. So
WIZFORM-02's criterion is not violated on that path. The real defect there is a remedy sentence
("Try again." for a ciphertext that will never decrypt) and it is recorded as its own TODOS.md item
rather than folded into a closed requirement. ⚠️ One overstatement in the REQUIREMENTS.md rollup —
see W-153.7-4.

**4. `resilient-fetch` completeness pin 13 → 15; `_validate_mt5_key` a wrong-but-existing symbol.**
**UPHELD. No breaker key moved.** The counting (503) table is byte-identical apart from the symbol
correction; the two arrivals are both 500s and therefore inert by the R-1 rule. AST-confirmed
enclosures at HEAD: `MT5_GATEWAY_UNREACHABLE` ×2 → `_connect_and_probe`;
`MT5_GATEWAY_UNCONFIGURED` ×3 → `_validate_mt5_key_probe`, ×1 → `_connect_and_probe`. Exactly what
the re-cut docblock now says. `seam-budgets.invariant.test.ts` and `resilient-fetch.wiring.test.ts`
are green.

---

### Requirements Coverage

| Requirement | Source plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| WIZFORM-02 | 153.7-01, -02, -03 | No wizard failure renders `code: UNKNOWN` / "We could not classify this failure" when the server DID classify it — and the closing sweep is driven from the EMITTING SITES rather than a hand-listed set | ✓ SATISFIED (with named residue) | All four `missing` items from `153-VERIFICATION.md` discharged: (1) both mt5-gateway codes have verdict rows; (2) the derived population now covers `routers/**` `service_error(...)` and reds by name — falsified three ways here; (3) the boundary decision is written IN FULL in `EXPECTED_EMITTED_CODES`' re-cut docblock, with every exclusion carrying its own reason and count pin; (4) the three code-less finalize rejections are coded, ledger 3 → 0 with the 32 total untouched |

**Orphaned requirements:** none. WIZFORM-02 is the only ID declared by any 153.7 plan, and
`REQUIREMENTS.md` maps no other ID to Phase 153.7.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | `TBD` / `FIXME` / `XXX` scan over all 15 non-planning files touched by the phase | — | **ZERO hits** |
| — | — | `TODO` / `HACK` / `PLACEHOLDER` scan over the same files | — | **ZERO hits** |

No debt markers. No stub returns. No hardcoded-empty props. The one behavioural "absence" pattern in
the phase (`actions` lacking `RECOVERABLE_ACTIONS` members) is deliberate, documented and asserted.

---

### Deferred Items

Both items below are recorded in TODOS.md by the phase itself and are correctly OUT of this phase's
scope; they are listed here so the verdict is not read as "nothing is left".

| # | Item | Recorded where | Note |
|---|------|----------------|------|
| 1 | `keys/[id]/permissions` runs a FOURTH private `PROBE_*` classifier with no coverage law over it; `KEY_UNDECRYPTABLE`'s remedy sentence is wrong | TODOS.md § *Phase 153.7 — recorded deferrals* | Same class, different criterion. Does not render `UNKNOWN` |
| 2 | Five routes answer every upstream 5xx as `code: "UNKNOWN"` through a terminal arm that forwards 4xx faithfully | TODOS.md, same section | Four of five are admin-only; the fifth is the portfolio impact panel |

⚠️ Neither W-153.7-1 nor W-153.7-2 is recorded anywhere. They are new findings from this
verification.

---

### Human Verification Required

Three items, all of them about **rendered copy that no test renders**. See frontmatter for the full
form.

1. **SEAM_INTERNAL_FAULT renders on the wizard connect step, with no Retry control.**
   This is the single highest-value check, because it is also the check that would catch a
   regression at the one unguarded hop (W-153.7-1).
2. **The three new finalize codes render three DISTINCT cards** with the correct claim scope
   (`DRAFT_FINALIZE_FAILED` must not claim nothing was saved; `SEAM_RESPONSE_UNREADABLE` must claim
   neither outcome).
3. **DESIGN.md conformance** on the four new copy members.

---

## Gaps Summary

**There are no gaps.** All nine observable truths are verified against the codebase, every one of
the five Falsifiability-Ledger mutations was applied and observed to red the named tests, and the
phase's two strongest oracles were re-derived from scratch rather than read from a SUMMARY. WIZFORM-02
is genuinely met: the mechanism's REACH is now the boundary the phase locked, and the reach itself is
falsifiable in three independent ways (matcher narrowing, root narrowing, callee dropping) plus a
permanent synthetic-tree falsifier that runs the real scanner through the real disposition Maps.

**What stops this being `passed`:** four new copy members render to users and nothing renders them in
a test, so three human checks are required. That is a ceremony gate, not a defect.

**The one finding worth reading before the next phase** is W-153.7-1. The coverage law now reaches
every emitted Python code and every disposition — but the last hop, from a verdict's
`WizardErrorCode` to the client roster that decides whether it renders, is guarded for
literal-minted route codes and **unguarded for classifier-returned codes**. I measured it: deleting
`SEAM_INTERNAL_FAULT` from `KNOWN_CREATE_WITH_KEY_CODES` leaves 312 tests green while re-creating
the exact 2026-08-05 incident. This is not a failure of the phase's stated goal — no must-have
required a derived roster, and the membership IS present — but the roster's own docblock assigned
that class fix to "Phase 153 / WIZFORM-02", which this phase just ticked COMPLETE, leaving the fix
without an owner and unrecorded in TODOS.md. Recommend adding it as a third TODOS.md item in the
same section, so the pointer is not left dangling.

---

*Verified: 2026-08-14T06:54:47Z*
*Verifier: Claude (gsd-verifier) — goal-backward, FORCE stance*
*⛔ Not committed. Orchestrator bundles this with the phase artifacts.*
*⛔ `gsd-sdk query state.advance-plan` / `state.update-progress` deliberately NOT run (pointed at Phase 156).*
