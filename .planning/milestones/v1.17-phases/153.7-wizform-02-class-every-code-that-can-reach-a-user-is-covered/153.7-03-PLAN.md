---
phase: 153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered
plan: 03
type: execute
wave: 3
depends_on: [153.7-01, 153.7-02]
files_modified:
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/lib/wizardErrors.ts
  - src/lib/wizardErrors.test.ts
  - src/lib/wizardErrors.invariant.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
  - src/__tests__/contracts/REGISTRY.md
  - src/__tests__/contracts/contracts-registry.test.ts
  - src/lib/resilient-fetch.ts
  - TODOS.md
autonomous: true
requirements: [WIZFORM-02]

must_haves:
  truths:
    - "Every finalize-wizard rejection carries a code — the code-less ledger reads 0, and EXPECTED_FINALIZE_REJECTION_SITES stays 32 (the invariance IS the proof nothing was invented: 32 − 32 = 0)"
    - "BOTH key routes — create-with-key AND its twin composite/add-key — answer the mt5-gateway family with the same non-UNKNOWN verdict, proven by regression tests that fail when the verdict row is neutered"
    - "The prose records match the widened predicate and the measured enclosing functions — no reader is pointed at the pre-153.7 boundary or at _validate_mt5_key"
  artifacts:
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "Three formerly code-less rejection arms (500 draft-load, 500 finalize-RPC, 502 upstream-shape) each answering with a code"
    - path: "src/lib/wizardErrors.ts"
      provides: "Three new WIZARD_ERROR_COPY members in the established wizard error voice"
    - path: "src/app/api/strategies/composite/add-key/route.test.ts"
      provides: "Twin-path regression: the shared-table fix reaches the add-key catch identically"
    - path: "TODOS.md"
      provides: "Deferred boundary question recorded: the keys/[id]/permissions private PROBE_* cascade is the next instance of the class"
  key_links:
    - from: "the three finalize arms"
      to: "KNOWN_FINALIZE_CODES in SubmitStep.tsx"
      via: "NextResponse.json({ code, error }, { status }) sites the route scanner derives, roster-checked client-side"
      pattern: "KNOWN_FINALIZE_CODES"
    - from: "create-with-key catch AND composite/add-key catch"
      to: "the Plan-02 MT5_GATEWAY_UNREACHABLE verdict row"
      via: "the SHARED classifyKeyValidationError — one table, two routes"
      pattern: "classifyKeyValidationError"
---

<objective>
Close the two remaining `missing` items from the 153 span verification, plus the prose re-cuts:
(1) the three code-less `finalize-wizard` rejections each get a copy member and a code
(D-153.2-D: 500 draft-load, 500 finalize-RPC, 502 upstream-shape), driving
`KNOWN_CODELESS_FINALIZE_REJECTIONS` 3 → 0; (2) the `create-with-key` terminal arm — already
FIXED by Plan 02's shared-table verdict rows (the loss was inside shared `wizardErrors.ts`, so a
verdict row fixes both key routes at once) — gets regression tests on BOTH routes, because a
route-local fix at `create-with-key` alone would be the twin-path defect in its purest form, and
⭐ the twin is `composite/add-key`, NOT `finalize-wizard` (which never calls
`classifyKeyValidationError` — measured, CONTEXT correction 4); (3) the stale prose records are
re-cut to the widened predicate and the measured enclosing functions.

Output: three coded finalize arms + copy members, pin moves with the arithmetic proof, twin
regression tests, prose re-cuts, one TODOS.md deferral entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

⚠️ Worktrees have no `node_modules` — run verifies from a checkout that has them, or `npm ci`.
⚠️ CI is Node 22 / local may be Node 25 — re-run quick verify under
`PATH=/opt/homebrew/opt/node@22/bin:$PATH`; `vi.spyOn` + `restoreAllMocks`, never `vi.stubGlobal`.
⚠️ Backlog is single-sourced: the deferral entry goes in root TODOS.md, nowhere else.
</execution_context>

<context>
@.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-CONTEXT.md
@.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-RESEARCH.md
@.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-PATTERNS.md
@src/lib/wizardErrors.invariant.test.ts
@src/app/api/strategies/finalize-wizard/route.ts
@DESIGN.md

Key sections: PATTERNS §1.2 (the ROUTES population, the codeless-rejection pins and their
docblocks — including the exact three arms and the collapse sentence — and the dated-paragraph
re-cut voice for `expectedSites`), PATTERNS §1.4 (the terminal arm + the twin, with the
"DIVERGENCE-FREE … byte-identical by design" in-file declaration), PATTERNS §3 (re-cut Forms
1–3), PATTERNS §4 (citation surface — finalize-wizard, create-with-key, composite/add-key,
resilient-fetch.ts and wizardErrors.ts are ALL members), RESEARCH Q4 (pin inventory + the prose
records that go stale), RESEARCH Q7 (the four-step loss trace and the third-consumer flag),
RESEARCH Open Q1 (the permissions-route deferral recommendation).
</context>

<constraints>
1. **`EXPECTED_FINALIZE_REJECTION_SITES` MUST STAY 32.** Its invariance is the proof the change
   was a fix, not an addition (re-cut Form 3: three arms cross OUT of the code-less ledger and
   INTO the coded population; 32 − 32 = 0). If the derived rejection-site count moves, an arm was
   invented — stop and re-measure rather than bump the pin.
2. **Both key route PRODUCTION files are read-only.** The fix is Plan 02's shared table. Any
   proposed edit inside either catch block must state, measured, why the shared table cannot
   carry it (PATTERNS §1.4 verdict). This plan touches only their `.test.ts` files.
3. **Pins re-cut, never deleted** — with one in-file-authorized exception shape: the
   `KNOWN_CODELESS_FINALIZE_REJECTIONS` docblock pre-authorizes its own collapse at zero. Honor
   the file's own instruction: lower the literal to 0, delete the three bullets from the ledger
   docblock (the assertion message says exactly this: "lower the literal and delete its row from
   the ledger's docblock"), KEEP the constant and the assertion — at 0 the assertion IS "every
   rejection carries a code", which is where WIZFORM-02's criterion lands. Record the collapse
   condition being reached in a dated re-cut comment.
4. **Citation surface discipline**: finalize-wizard/route.ts, wizardErrors.ts and
   resilient-fetch.ts are on SEAM_CITATION_SURFACE — no bare `path:line` in any new comment, and
   no `path:line` inside strings either (the measured evasion). Symbols and machine codes only.
5. **`statusRe: "400"` on the two key-route ROUTES entries is narrow ON PURPOSE** — do not widen
   it (PATTERNS §1.2a).
6. ⛔ WIZFORM-05 / the 120s validate budget stays closed. Nothing in this plan touches
   `_MT5_VALIDATE_INITIALIZE_TIMEOUT_MS` or any budget.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Code the three finalize-wizard rejections — ledger 3 → 0, sites 29 → 32, total pinned at 32</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/lib/wizardErrors.ts, src/lib/wizardErrors.test.ts, src/lib/wizardErrors.invariant.test.ts, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx</files>
  <action>
    Locate the three code-less arms in finalize-wizard/route.ts — the ledger docblock in
    wizardErrors.invariant.test.ts already names them by status and message: 500 "Could not load
    draft" (the strategy lookup's failure), 500 "Could not finalize wizard draft" (the RPC's
    generic failure), 502 "Upstream service returned unexpected response". Give each arm a code
    in the route's established `NextResponse.json({ code, error }, { status })` shape so the
    route scanner derives the three new sites.

    Mint three WIZARD_ERROR_COPY members in wizardErrors.ts (names at executor's judgment within
    the established member-naming voice — e.g. a draft-load member, a finalize-RPC member, an
    upstream-shape member). Copy per DESIGN.md and the wizard error voice: title states what
    happened, cause states what is and is not known (whether the draft/strategy changed — the
    honesty the UNKNOWN copy exists for, now stated specifically per arm), fix lines name a
    remedy not a subsystem; H-0305 holds (never the raw upstream message).

    Pin moves, each in its established re-cut form (PATTERNS §3):
    - `ROUTES` finalize entry `expectedSites` 29 → 32: dated arrow paragraph reusing the
      arithmetic sentence structure literally (PATTERNS §1.2a instruction): three arms crossed
      OUT of the code-less ledger and INTO this population; `EXPECTED_FINALIZE_REJECTION_SITES`
      unchanged at 32; ledger 3 → 0; 32 − 32 = 0 proves nothing was invented.
    - `KNOWN_CODELESS_FINALIZE_REJECTIONS` 3 → 0 per constraint 3 (delete the three docblock
      bullets, keep constant + assertion, dated collapse note).
    - `EXPECTED_FINALIZE_REJECTION_SITES` stays 32 — verify the derived count agrees; do not touch
      the literal.
    - `EXPECTED_TABLE_SIZE` at BOTH declarations in wizardErrors.test.ts moves +3 in lockstep
      (from whatever value Plan 02 left — read the current value at execution time, do not assume
      76; the divergence guard asserts exactly 2 declarations with one shared value). Form-1
      dated comment at both.
    - 3 rows into `KNOWN_FINALIZE_CODES` in SubmitStep.tsx.
    - The combined-route `DERIVED_FLOOR` in wizardErrors.invariant.test.ts: leave it (its
      docblock explains the >= semantics; RESEARCH calls leaving it defensible) — note the
      decision in the SUMMARY.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/lib/wizardErrors.invariant.test.ts src/lib/wizardErrors.test.ts</automated>
    Green: codeless count 0, finalize sites 32, rejection total 32 unchanged, table-size
    divergence guard green, roster reconciliation green. Then seam-citations.invariant.test.ts
    green (new route/copy comments carry no bare citations).
  </verify>
  <done>
    Three arms coded, three members minted, all pins moved in their established forms, ledger at
    0 with the collapse recorded, KNOWN_FINALIZE_CODES +3.
  </done>
</task>

<task type="auto">
  <name>Task 2: Twin regression — both key routes answer the mt5-gateway family identically, and fail when neutered</name>
  <files>src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/composite/add-key/route.test.ts</files>
  <action>
    Add a regression test to EACH route's existing test file (both exist — follow each file's
    established mocking pattern for the analytics client rather than inventing one): mock the
    key-validation call to throw an error shaped like AnalyticsUpstreamError with
    seamCode = "MT5_GATEWAY_UNREACHABLE" and message = the real Python detail string (verbatim
    from `_connect_and_probe` in routers/exchange.py), and assert the route answers the Plan-02
    verdict — the same non-UNKNOWN code and status in BOTH routes — and does NOT fire the
    unclassified-key-error Sentry capture (the `code === "UNKNOWN"` arm).

    WHY this test exists (encode it in the test's comment): the fix is a row in the SHARED
    wizardErrors.ts table, which reaches both routes at once — but a future route-local change
    (e.g. pre-stringifying the caught error before classification, which the add-key catch
    comment explicitly warns sends a breaker trip to UNKNOWN/500) would silently re-open the
    twin path. Route-local regression tests are the alarm; this milestone's single most repeated
    mistake is fixing one path of a byte-identical pair.

    Prove-fails-when-neutered: temporarily remove the MT5_GATEWAY_UNREACHABLE verdict row from
    wizardErrors.ts → BOTH new tests must fail with UNKNOWN/500 → restore → green. Record both
    outputs for the SUMMARY; leave no neutering in the tree.

    Also verify (measured, not assumed — CONTEXT flags it): the alias-table assertion in
    wizardErrors.invariant.test.ts asserting `SEAM_CODE_TO_WIZARD_CODE` does NOT contain
    MT5_GATEWAY_UNREACHABLE stays true — Plan 02's verdict row is in a DIFFERENT table, so it
    should — and record the check.

    Node 22 note applies; use vi.spyOn + restoreAllMocks for any new mock.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/app/api/strategies/create-with-key/route.test.ts src/app/api/strategies/composite/add-key/route.test.ts</automated>
    Both files green including the two new regression tests; neuter-proof outputs recorded (both
    red with the row removed, both green restored); alias-table assertion confirmed green.
  </verify>
  <done>
    Both routes regression-tested against the shared-table fix; neuter-proof recorded; zero
    production-route edits (git diff shows only .test.ts changes for these routes).
  </done>
</task>

<task type="auto">
  <name>Task 3: Prose re-cuts + the TODOS.md deferral</name>
  <files>src/__tests__/contracts/REGISTRY.md, src/__tests__/contracts/contracts-registry.test.ts, src/lib/resilient-fetch.ts, src/lib/wizardErrors.ts, TODOS.md</files>
  <action>
    Re-cut the prose records RESEARCH Q4 measured as stale (no test asserts these strings —
    Assumption A3 — but they are the discoverable index; update them anyway):

    1. REGISTRY.md's row and contracts-registry.test.ts's `invariant:` string for the seam-venue
       vocabulary invariant, which both still say "DERIVED from
       `analytics-service/services/exchange.py`" (already wrong at HEAD): restate the widened
       predicate — derived from `analytics-service/**` minus the pinned exclusions, via both the
       `error_code =` assignment shape and the `service_error` call family (incl.
       `VenueTransientHTTPException`'s keyword code). Same sentence in both files, per the
       one-row-per-invariant convention.
    2. resilient-fetch.ts's hand-derived `dependency=` census: the mt5-gateway row still names
       `_validate_mt5_key` — a function that still exists but encloses NONE of the six gateway
       emitters (PATTERNS §5 refinement: it is a WRONG symbol, not a dangling one). Fix to the
       measured enclosures: MT5_GATEWAY_UNCONFIGURED → `_validate_mt5_key_probe` (×3) +
       `_connect_and_probe` (×1); MT5_GATEWAY_UNREACHABLE → `_connect_and_probe` (×2). Keep the
       table's symbol-anchored row format exactly (it is on the citation surface and its docblock
       records WHY symbols, not lines).
    3. wizardErrors.ts's `SEAM_CODE_TO_WIZARD_CODE` docblock still says MT5_GATEWAY_UNREACHABLE
       "correctly answer[s] UNKNOWN" — a sentence whose premise Plan 02 deleted. Rewrite that
       passage to reflect the verdict row (symbol-anchored; the alias table itself is unchanged).
    4. TODOS.md: add ONE entry recording the deferred boundary question (RESEARCH Open Q1
       recommendation): the keys/[id]/permissions route runs its own private PROBE_* substring
       cascade — a fourth classifier with no coverage law over it, where KEY_MISSING_EXCHANGE,
       KEY_UNDECRYPTABLE and internal.py's KEK_UNAVAILABLE land; it is the next instance of the
       WIZFORM-02 class. Deferred deliberately from 153.7; the Group-B disposition reasons
       already name it.

    Then the phase-gate verification: full phase surface + Python census + Node 22 re-run.
  </action>
  <verify>
    <automated>node_modules/.bin/vitest run src/lib/wizardErrors.test.ts src/lib/seam-venue-vocabulary.invariant.test.ts src/lib/wizardErrors.invariant.test.ts src/lib/seam-citations.invariant.test.ts "src/app/(dashboard)/strategies/new/wizard" src/__tests__/contracts/contracts-registry.test.ts</automated>
    Exit 0. Then `cd analytics-service && python3 -m pytest -q tests/test_raw_5xx_census.py` →
    19 passed (run FROM analytics-service/, python3, never repo root). Then the 2-file quick
    check under `PATH=/opt/homebrew/opt/node@22/bin:$PATH`.
  </verify>
  <done>
    Prose records match the widened predicate and measured symbols; TODOS.md carries the
    deferral; full phase surface green on both Node versions; Python census untouched and green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → finalize-wizard route | untrusted request; the three new arms answer classified failures |
| upstream analytics-service → finalize route | the 502 upstream-shape arm handles a malformed upstream body |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-153.7-09 | Information disclosure | new finalize copy leaking draft/RPC/upstream internals | mitigate | H-0305 preserved — wire body carries the code, never the raw upstream message; copy names a remedy, not a subsystem |
| T-153.7-10 | Repudiation | Sentry capture with key material in the regression path | mitigate | preserve captureToSentry secrets scrubbing at both terminal arms; the new tests assert the UNKNOWN capture does NOT fire on classified codes |
| T-153.7-11 | Tampering | twin-path drift (one route fixed, one silently regressing) | mitigate | per-route regression tests + the neuter-proof; production catch blocks untouched |
| T-153.7-SC | Tampering | npm/pip/cargo installs | accept | zero packages installed this phase |
</threat_model>

<verification>
Phase gate: full phase-surface vitest command exit 0 on local Node AND under Node 22 PATH;
`cd analytics-service && python3 -m pytest -q tests/test_raw_5xx_census.py` → 19 passed;
`EXPECTED_FINALIZE_REJECTION_SITES` literal untouched at 32; git diff shows no production edits
to create-with-key/route.ts or composite/add-key/route.ts.
</verification>

<success_criteria>
- KNOWN_CODELESS_FINALIZE_REJECTIONS = 0, expectedSites = 32, EXPECTED_FINALIZE_REJECTION_SITES
  unchanged at 32, EXPECTED_TABLE_SIZE +3 at both declarations in lockstep — all in the
  established re-cut voices, with the 32 − 32 = 0 arithmetic stated
- Both key routes regression-tested for the mt5-gateway family; tests fail when the shared
  verdict row is neutered; alias-table assertion verified, not assumed
- Prose records (REGISTRY.md, contracts-registry.test.ts, resilient-fetch.ts census,
  SEAM_CODE_TO_WIZARD_CODE docblock) match measured reality; TODOS.md records the
  permissions-route deferral
- WIZFORM-02 now fully closed: after this plan, Phase 153's parent checkbox is tickable
  (PRD success criterion 6) — note it in the SUMMARY for the verifier
</success_criteria>

<output>
Create `.planning/phases/153.7-wizform-02-class-every-code-that-can-reach-a-user-is-covered/153.7-03-SUMMARY.md`
when done. Must include: the pin arithmetic (before/after per pin), the neuter-proof outputs, the
alias-table check result, and the deployment-adjacent note from RESEARCH (MT5_GATEWAY_UNCONFIGURED
is now less likely on PROD since MT5 is configured; MT5_GATEWAY_UNREACHABLE is exactly what the
documented gateway wedges produce — the user-facing impact is live and recurring, carried by the
retryable sibling).
</output>
