---
phase: 148-own-owner-factsheet-without-cache-disclosure
plan: 04
type: execute
wave: 3
depends_on: [148-03]
files_modified:
  - src/__tests__/phase-148-owner-lane-cache-isolation.test.ts
  - src/app/factsheet/[id]/v2/page.tsx   # transient Rule-9 mutations only — reverted, net zero diff
  - .planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-VALIDATION.md
autonomous: true
requirements: [OWN-02]

must_haves:
  truths:
    - "The cached builder is provably unreachable from the owner lane — asserted as a CI invariant, not observed once (SC2 structural layer, CONTEXT lock)"
    - "unstable_cache appears exactly once in page.tsx; its callback contains the withPublishedOnly literal and never withPublishedOrOwner"
    - "No production file outside page.tsx reaches buildFactsheetPayloadCached or fetchAndBuildPayload — no third factsheet-resolution mechanism minted"
    - "generateMetadata never contains withPublishedOrOwner (draft metadata leakage pinned shut)"
  artifacts:
    - path: "src/__tests__/phase-148-owner-lane-cache-isolation.test.ts"
      provides: "SC2-B structural source-scan CI invariant, phase-147-guards architecture"
      min_lines: 120
  key_links:
    - from: "src/__tests__/phase-148-owner-lane-cache-isolation.test.ts"
      to: "src/app/factsheet/[id]/v2/page.tsx"
      via: "fail-loud readSource of a pinned path + repo-wide productionSources walk"
      pattern: "factsheet/\\[id\\]/v2/page.tsx"
---

<objective>
Ship SC2's structural layer: a repo-wide source-scan CI invariant (clone of the phase-147-guards
architecture — orchestrator ruling) proving the shared cache can ONLY ever be populated by the
published-only builder, and run the two Rule-9 mutations that prove the gate can fail.

Purpose: CONTEXT locks SC2 acceptance as BOTH layers; the behavior layer landed in 148-03. The
structural layer is the load-bearing one — it holds on every future commit, not just today.
Output: the new guard file; observed mutation evidence in its header + 148-VALIDATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-PATTERNS.md

Key sections: PATTERNS §6 (clone architecture wholesale: fail-loud readSource — a missing pinned
source is a FAILURE not a skip; stripComments — load-bearing here TWICE: page.tsx prose names
BOTH predicates, and src/lib/factsheet/types.ts:545 carries a comment naming fetchAndBuildPayload
that would falsely trip the repo-walk; productionSources walk excluding tests/.d.ts;
paren-balanced argument extraction :245-259; anti-vacuity test; Rule-9 mutation ledger
convention). RESEARCH §6b (recommended assertions + the type-level half already carried by the
148-02 signature).
</context>

<interfaces>
Shipped state this gate pins (from 148-02/148-03):
- page.tsx: exactly one `unstable_cache(` call, inside buildFactsheetPayloadCached; callback text
  `async () => fetchAndBuildPayload(id, withPublishedOnly)`; buildFactsheetPayloadCached takes a
  single `cacheKey: string` parameter and no visibility parameter (⚠️ the declaration is
  formatted across three lines — no single-line `(cacheKey: string)` substring exists in the
  shipped source); Lane B calls `fetchAndBuildPayload(id, (q` … with withPublishedOrOwner inside
  the lambda; `export const dynamic = "force-dynamic"` present; generateMetadata contains
  withPublishedOnly only.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write phase-148-owner-lane-cache-isolation.test.ts (147-guards clone)</name>
  <files>src/__tests__/phase-148-owner-lane-cache-isolation.test.ts</files>
  <read_first>
    - src/__tests__/phase-147-series-resolution-guards.test.ts (FULL — header intent statement :20-23, mutation ledger :60-92, helpers :99-179, Layer A/B structure :185-234 and :261+, anti-vacuity :208-226)
    - src/app/factsheet/[id]/v2/page.tsx (post-148-03 state — the shapes being pinned; do not modify in this task)
    - 148-PATTERNS.md §6 (⚠️ stripComments BEFORE every match — the page header prose names both predicates; a bare grep would self-invalidate)
  </read_first>
  <action>
    Create the guard file cloning the 147 architecture (readSource fail-loud, stripComments,
    productionSources, paren-balanced extractor adapted to locate the unstable_cache( call and
    slice its callback body + the buildFactsheetPayloadCached declaration head). Header: state the
    intent in the 147 idiom — "ROADMAP 148 SC2 has a structural clause: the shared factsheet cache
    is only ever populated by the published-only builder, with the cached builder unreachable from
    the owner lane. This file enforces it as a CI invariant, not as an observation made once
    during the phase" — plus the Rule-9 mutation ledger (filled in Task 2).

    Assertions (all on stripComments output; expected tokens are literals in this file):
    1. In page.tsx the token `unstable_cache(` appears EXACTLY once.
    2. The extracted unstable_cache callback body contains `withPublishedOnly` and does NOT
       contain `withPublishedOrOwner`, and contains `fetchAndBuildPayload(id, withPublishedOnly)`
       (literal identifier, not a variable).
    3. The extracted buildFactsheetPayloadCached declaration head (the extractor's slice from the
       `function buildFactsheetPayloadCached` token to its opening brace) does NOT contain
       `visibility` and does NOT contain `StrategyVisibility` — the type-level unrepresentability
       claim as a NEGATIVE, formatting-independent check. ⛔ Do NOT add a positive
       `(cacheKey: string)` substring assertion: the shipped declaration is formatted across
       three lines, so that literal does not exist as a substring — it would pin formatting, not
       the seam, and redden on any reflow. The negative clause alone still carries mutation
       SC-2B-b (threading a visibility param into the wrapper reddens it).
    4. Repo-wide (productionSources over src/, tests excluded): no file other than
       "src/app/factsheet/[id]/v2/page.tsx" contains `buildFactsheetPayloadCached` or
       `fetchAndBuildPayload` — no third factsheet-resolution mechanism minted (the 147
       "no fifth reader" clause restated). stripComments is load-bearing for this walk for a
       SECOND reason beyond page.tsx's own prose: src/lib/factsheet/types.ts:545 is a COMMENT
       naming `fetchAndBuildPayload` ("branch of the read path (page.tsx `fetchAndBuildPayload`)")
       — an un-stripped walk would falsely flag types.ts as an offender.
    5. The extracted generateMetadata function body does NOT contain `withPublishedOrOwner`
       (threat T-148-03: draft name/description can never enter page meta/OG).
    6. page.tsx contains `export const dynamic = "force-dynamic"` (response-cache pin survives
       future refactors — the tearsheet hazard class).
    7. Anti-vacuity: the extractor DID find one unstable_cache call with a non-empty callback
       body, AND stripComments(page.tsx) still contains `withPublishedOrOwner` somewhere (the
       Lane B code) — so an empty offender list means clean, not blind.
    Missing page.tsx (rename/move) → explicit failure via readSource, never a skip.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-148-owner-lane-cache-isolation.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - All 7 assertion groups green against shipped code; exit 0
    - grep -c "stripComments" in the new file ≥ 3 (helper defined + applied before matching)
    - Assertion 3 contains NO positive `(cacheKey: string)` match — negative visibility/StrategyVisibility clauses only
    - Assertion 4's walk excludes __tests__ and *.test.ts* (clone of productionSources) — verified by reading the helper — and passes with types.ts:545's comment present (stripComments proves its worth)
    - No allowlist-only design: assertion 4 is a repo-wide walk, so a brand-new offender file is caught
  </acceptance_criteria>
  <done>SC2-B is a standing CI invariant with anti-vacuity teeth.</done>
</task>

<task type="auto">
  <name>Task 2: Rule-9 mutations — prove the gate reddens; record in header + VALIDATION</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/__tests__/phase-148-owner-lane-cache-isolation.test.ts, .planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-VALIDATION.md</files>
  <read_first>
    - 148-VALIDATION.md (Falsifiability Ledger rows SC-2B-a / SC-2B-b — exact mutations)
    - src/__tests__/phase-147-series-resolution-guards.test.ts (:60-92 — the ledger format: mutation, observed output, revert by re-edit)
  </read_first>
  <action>
    Two semantic production mutations to page.tsx, each observed RED then reverted by re-editing
    the mutated line (never git checkout — live-ledger rule):
    a. Payload-build site: change the unstable_cache callback to
       `fetchAndBuildPayload(id, (q) => q)` (identity predicate — the scariest real regression:
       the cache would serve UNFILTERED rows to anon). Gate assertion 2 must redden. Also run the
       owner-lane behavior file to record whether it catches this one (expected: it does NOT —
       that asymmetry is WHY the structural layer exists; state this in the SUMMARY).
    b. Signature-gate site (the orchestrator-preferred second member of the class): change
       buildFactsheetPayloadCached's signature to `(cacheKey: string, visibility: StrategyVisibility)`
       and thread `visibility` into the callback in place of the literal. typecheck stays green —
       only gate assertions 2 and 3 redden (assertion 3's NEGATIVE clause fires on the
       `visibility` token entering the declaration head). This proves the gate guards the seam
       itself, not just today's call shape.
    Record both observed failure outputs in the gate file's header ledger AND flip 148-VALIDATION.md
    rows SC-2B-a/SC-2B-b to ✅ Observed with pasted assertion lines. Update the Per-Task map for
    148-04. After reverts: full targeted battery green and page.tsx byte-identical (git diff --quiet).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-148-owner-lane-cache-isolation.test.ts "src/app/factsheet/[id]/v2/page.owner-lane.test.tsx" --no-file-parallelism && git diff --quiet -- "src/app/factsheet/[id]/v2/page.tsx" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Both mutations OBSERVED red (pasted output in the header ledger and VALIDATION.md — no "asserted only" rows)
    - Mutation (b) chosen at the SIGNATURE site, not a repeat of the payload-build site
    - page.tsx net-zero diff after reverts; typecheck 0; both test files green
  </acceptance_criteria>
  <done>The structural gate is proven falsifiable at two independent sites; evidence on disk.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future commits ↔ the cache-isolation property | the gate converts a one-time review conclusion into a standing CI invariant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-148-01 | Information Disclosure | owner-populated cache entry served to anon (SC2) | mitigate | Structural layer: exactly-one unstable_cache + published-only-literal callback + visibility-free wrapper signature, enforced repo-wide on every CI run |
| T-148-02 | Elevation of Privilege | predicate drop inside the cached callback (identity predicate = unfiltered rows to anon) | mitigate | Mutation (a) proves the gate reddens on exactly this edit; behavior tests provably do NOT catch it — the gate is the sole control and is now pinned |
| T-148-03 | Information Disclosure | draft metadata via generateMetadata/OG | mitigate | Assertion 5 pins generateMetadata free of the owner predicate permanently |
| T-148-07 | Information Disclosure | response-cache regression (loss of force-dynamic) | mitigate | Assertion 6 pins the export |
| T-148-SC | Tampering | package installs | accept (vacuous) | Zero packages installed this phase |
</threat_model>

<verification>
- Gate green on shipped code; both mutations observed RED and reverted; VALIDATION.md updated;
  tree net-zero on production files.
</verification>

<success_criteria>
- SC2's structural clause holds "asserted, not observed" (CONTEXT lock) — a CI invariant with
  recorded falsifiability at the payload-build site AND the signature-gate site.
</success_criteria>

<output>
Create `.planning/phases/148-own-owner-factsheet-without-cache-disclosure/148-04-SUMMARY.md` when done.
</output>
