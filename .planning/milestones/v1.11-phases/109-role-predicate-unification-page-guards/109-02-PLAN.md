---
phase: 109-role-predicate-unification-page-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260716120000_backfill_staff_role_both.sql
  - supabase/tests/test_staff_role_both_backfill.sql
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Sidebar.test.tsx
  - src/components/layout/MobileNav.test.tsx
autonomous: false
requirements: [ROLE-01, ROLE-02, ROLE-03, ROLE-05]

must_haves:
  truths:
    - "A manager-role account sees no allocator workspace (My Allocation, Scenario, Recommendations, Compare, Decks) and no Discovery in nav"
    - "An allocator-role account sees zero sell-side surfaces (Strategies, Portfolios) in nav"
    - "An is_admin+manager account sees the manager workspace + Admin section — NOT the allocator workspace"
    - "No staff row has is_admin=true with role other than 'both' (empty-set invariant, CI-enforced)"
    - "The backfill migration and the || isAdmin drop land in the SAME plan/PR — atomic (the hard GATE)"
  artifacts:
    - path: "supabase/migrations/20260716120000_backfill_staff_role_both.sql"
      provides: "Idempotent staff role='both' backfill (UPDATE profiles WHERE is_admin AND role<>'both')"
      contains: "SET role = 'both'"
    - path: "supabase/tests/test_staff_role_both_backfill.sql"
      provides: "RED-guarded empty-set assertion (bare DO-block, RAISE EXCEPTION on violation)"
      contains: "is_admin = true AND role NOT IN ('both')"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Pure-role nav derivation — no || isAdmin in buildNavSections or buildPrimaryMobileNav"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "(dashboard)/layout.tsx role flags"
      via: "buildNavSections(populatedSlugs, isAdmin, isAllocator, flaggedCount, isManager) — flags now used pure"
      pattern: "buildNavSections"
    - from: "supabase/migrations/20260716120000_backfill_staff_role_both.sql"
      to: "profiles.role"
      via: "UPDATE guarded by is_admin = true AND role <> 'both'"
      pattern: "UPDATE profiles"
---

<objective>
The atomic GATE unit (ROLE-05 + the OR-in drop): backfill every `is_admin` staff account to `role='both'` AND drop `|| isAdmin` from the nav derivations, in one plan/PR, proven by a pre-merge empty-set SQL assertion. These two changes are inseparable — landing the drop without the backfill locks every staff account out of the allocator workspace (per D: "The role='both' backfill and the || isAdmin drop are ONE atomic unit — never split across PRs").

Purpose: ROLE-01/02/03 — `profiles.role` becomes the sole nav predicate, `is_admin` gates only the Admin section; ROLE-05 — no staff member loses access.
Output: backfill migration + RED-guarded SQL assertion + pure-role Sidebar.tsx + flipped nav test assertions + the migration MCP-applied to the test project (human checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/109-role-predicate-unification-page-guards/109-RESEARCH.md
@.planning/phases/109-role-predicate-unification-page-guards/109-PATTERNS.md

Verified anchors (2026-07-16): the `|| isAdmin` OR-in lives in exactly TWO functions in ONE file — `Sidebar.tsx` lines 49-51 (`buildNavSections`) and 198-199 (`buildPrimaryMobileNav`). `MobileNav.tsx` delegates to `buildPrimaryMobileNav` and needs NO source edit (only its test file changes). Latest migration timestamp is `20260716090000` — the new migration MUST sort after it.

Project conventions in force: merging `supabase/migrations/**` to main AUTO-applies to PROD; the test project (qmnijlgmdhviwzwfyzlc) must be MCP-caught-up BEFORE merge or RED-guarded SQL tests fail in CI. This migration is a data UPDATE only (no function/schema change) — no `npm run schema:functions` snapshot refresh needed.

<interfaces>
From src/components/layout/Sidebar.tsx (verified line anchors):
- L49-51 (buildNavSections): `const showsAllocatorWorkspace = isAllocator || isAdmin;` / `const showsManagerWorkspace = isManager || isAdmin;` / `const showsDiscovery = isAllocator || isAdmin;`
- L198-199 (buildPrimaryMobileNav): `const showsAllocatorWorkspace = p.isAllocator || p.isAdmin;` / `const showsManagerWorkspace = p.isManager || p.isAdmin;`
- L131: Admin section spread gated on `isAdmin` alone — UNCHANGED
- L41-48: stale comment block ("Admins see BOTH allocator AND manager surfaces (triage/demo)") — update to role-only derivation
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backfill migration + RED-guarded empty-set SQL assertion</name>
  <files>supabase/migrations/20260716120000_backfill_staff_role_both.sql, supabase/tests/test_staff_role_both_backfill.sql</files>
  <read_first>
    - supabase/migrations/20260521150000_universal_signup_approval_gate.sql (the canonical idempotent backfill-UPDATE-with-WHY-header analog)
    - supabase/tests/test_handle_new_user_role_allowlist.sql (the bare BEGIN; DO $$...$$; ROLLBACK; assertion style — NO pgTAP plan()/finish())
    - supabase/migrations/20260520222848_lock_profile_role_at_signup.sql (the prevent_profile_role_change trigger the backfill must not be blocked by — SECURITY INVOKER; a migration runs as table owner/postgres so it should not fire; the Task 3 checkpoint is the definitive verification)
  </read_first>
  <action>
    Create migration `supabase/migrations/20260716120000_backfill_staff_role_both.sql` (timestamp sorts after 20260716090000, the current latest). Multi-line WHY header per the analog: this is the Phase 109 ROLE-05 atomic staff backfill; it ships in the SAME PR as the Sidebar || isAdmin drop because dropping the OR-in without it locks every is_admin account out of the allocator workspace; role='both' is the durable replacement predicate. Body: single UPDATE on profiles setting role to 'both' where is_admin = true AND role <> 'both'. Idempotent by the WHERE guard (re-run is a no-op). Note in the header that prevent_profile_role_change (20260520222848) is SECURITY INVOKER and does not fire for the owner/postgres migration runner — verified at test-project apply (Task 3).

    Create `supabase/tests/test_staff_role_both_backfill.sql` in the sibling bare-DO-block style: BEGIN; DO $$ block that SELECTs count(*) FROM profiles WHERE is_admin = true AND role NOT IN ('both') into a local, and RAISE EXCEPTION 'GATE FAILED: % staff rows have is_admin=true but role NOT IN (both)' when nonzero; ROLLBACK. This is RED-guarded: it passes only once Task 3's MCP apply has run against the test project.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260716120000_backfill_staff_role_both.sql && test -f supabase/tests/test_staff_role_both_backfill.sql && grep -c "role <> 'both'" supabase/migrations/20260716120000_backfill_staff_role_both.sql</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; migration filename timestamp lexically sorts after 20260716090000
    - Migration UPDATE is guarded by `is_admin = true AND role <> 'both'` (idempotent) — source assertion via grep
    - Assertion file uses the bare `BEGIN; DO $$ ... $$; ROLLBACK;` shape with RAISE EXCEPTION (grep confirms `RAISE EXCEPTION` and absence of `SELECT plan(`)
    - Migration header explains the atomicity WHY and the trigger note (A2)
  </acceptance_criteria>
  <done>Migration + assertion committed together; assertion is the CI-enforced proof of the GATE invariant.</done>
</task>

<task type="auto">
  <name>Task 2: Drop || isAdmin from both nav derivations + flip the admin-sees-all test assertions</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/Sidebar.test.tsx, src/components/layout/MobileNav.test.tsx</files>
  <read_first>
    - src/components/layout/Sidebar.tsx (both OR-in sites: buildNavSections L49-51, buildPrimaryMobileNav L198-199; Admin gate L131; stale comment L41-48)
    - src/components/layout/Sidebar.test.tsx (admin-sees-all fixtures at L115, L121, L173, L192-203, L214-216, L445-446 — KNOWN SITES, NOT EXHAUSTIVE; do NOT partial-edit off these line numbers. The authoritative gate is: the whole-repo grep below + a fully green Sidebar.test.tsx suite after the flip. Read the WHOLE file and flip every admin-sees-all expectation.)
    - src/components/layout/MobileNav.test.tsx (L12 — same)
  </read_first>
  <action>
    In Sidebar.tsx, drop `|| isAdmin` from the three derivations in buildNavSections (L49-51: showsAllocatorWorkspace, showsManagerWorkspace, showsDiscovery become pure isAllocator/isManager/isAllocator) and `|| p.isAdmin` from the two in buildPrimaryMobileNav (L198-199). Leave the Admin section gate (L131, isAdmin alone) UNCHANGED. Update the stale L41-48 comment block ("Admins see BOTH … triage/demo") to state the role-only derivation: profiles.role is the sole persona predicate; is_admin gates only the Admin section; staff hold role='both' (Phase 109). Do NOT touch MobileNav.tsx source — it delegates to buildPrimaryMobileNav.

    Flip the admin-sees-all test assertions to role-only across the WHOLE of Sidebar.test.tsx (known fixtures at L115, L121, L173, L192-203, L214-216, L445-446 — treat as a starting map, NOT the complete list; read the whole file and flip every admin-sees-all expectation) and MobileNav.test.tsx (L12): an isAdmin-only (or isAdmin+manager) fixture must now assert the allocator workspace items are ABSENT and the Admin section PRESENT (ROLE-03); pure-allocator fixtures assert Strategies/Portfolios ABSENT (ROLE-02); pure-manager fixtures assert My Allocation/Scenario/Recommendations/Compare/Decks/Discovery ABSENT (ROLE-01). Add a role='both' case asserting BOTH workspaces render (ROLE-05 nav truth). Each flipped test must fail if the OR-in were reintroduced (Rule 9).

    Then grep the WHOLE repo — not just src/ (v1.10 SC-3 lesson: a deleted UI string lingered in e2e/) — for any other spec asserting admin-sees-all nav: search `e2e/` and all `**/*.test.tsx?` for isAdmin-based expectations of allocator/manager workspace items (search terms: isAdmin near workspace/allocator/manager nav labels). KNOWN e2e sites that reference `isAdmin` and MUST be reconciled (Playwright is advisory in CI — it will NOT turn the `frontend` check red, so a stale admin-sees-all assertion here would silently rot): `e2e/full-flow.spec.ts` and `e2e/match-queue.spec.ts`. Read both, and for any assertion premised on an admin seeing the allocator/manager workspace, update it to the role-only reality (admin sees only Admin; role drives the workspace). This reconciliation is LOAD-BEARING, not optional. Update any hits the same way; record the grep command + the full hit list (incl. the two named specs and their resolution) in the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/Sidebar.test.tsx src/components/layout/MobileNav.test.tsx --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isAllocator || isAdmin\|isManager || isAdmin\|p.isAllocator || p.isAdmin\|p.isManager || p.isAdmin" src/components/layout/Sidebar.tsx` returns empty (0 hits)
    - `grep -n "isAdmin" src/components/layout/Sidebar.tsx` still shows the L131 Admin-section gate (Admin gating intact)
    - Sidebar + MobileNav vitest green with the flipped assertions (admin-only fixture: allocator items absent, Admin present; both fixture: both workspaces present)
    - Whole-repo grep for admin-sees-all nav assertions executed over e2e/ + **/*.test.tsx; `e2e/full-flow.spec.ts` and `e2e/match-queue.spec.ts` explicitly reconciled to role-only (or documented as not admin-sees-all nav assertions after reading); full hit list + resolution recorded in SUMMARY
  </acceptance_criteria>
  <done>Nav derivations are pure role; Admin section still is_admin-gated; every admin-sees-all assertion repo-wide flipped; tests green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] MCP-apply the backfill migration to the TEST project before merge</name>
  <files>none — external apply via Supabase MCP against test project qmnijlgmdhviwzwfyzlc (no repo file changes)</files>
  <read_first>
    - supabase/migrations/20260716120000_backfill_staff_role_both.sql (the SQL body to apply)
    - .planning/phases/109-role-predicate-unification-page-guards/109-RESEARCH.md (Assumptions Log A2 — trigger-interference check)
  </read_first>
  <action>
    Apply the Task-1 backfill migration to the live TEST project (qmnijlgmdhviwzwfyzlc) via Supabase MCP `apply_migration` BEFORE merge, so the RED-guarded SQL assertion passes in CI (test-project catch-up rule). The gsd-executor has NO Supabase MCP — this step MUST be performed by the orchestrator session or the human. After apply, correct the `supabase_migrations.schema_migrations` version row to `20260716120000` (MCP stamps now() — known drift workaround), then run the empty-set count query to verify the UPDATE actually took effect (this doubles as the A2 prevent_profile_role_change trigger check).
  </action>
  <what-built>Task 1 created `supabase/migrations/20260716120000_backfill_staff_role_both.sql` and the RED-guarded empty-set assertion `supabase/tests/test_staff_role_both_backfill.sql`. The assertion fails in CI until the migration is applied to the live test DB (qmnijlgmdhviwzwfyzlc) — types/tests come from the live test DB, not the migration file. The executor has NO Supabase MCP; the orchestrator session (or human) must apply it.</what-built>
  <how-to-verify>
    1. From the main session (which has Supabase MCP), against project qmnijlgmdhviwzwfyzlc: apply the migration via `apply_migration` with name `backfill_staff_role_both` and the file's SQL body.
    2. NOTE the known MCP drift: `apply_migration` stamps now() instead of the file timestamp — after apply, rename/insert the `supabase_migrations.schema_migrations` row to version `20260716120000` so CI migration ordering matches the file (established workaround).
    3. Verify the empty-set invariant on the test project: run `SELECT count(*) FROM profiles WHERE is_admin = true AND role NOT IN ('both');` — must return 0. This is ALSO the A2 verification: if the prevent_profile_role_change trigger had blocked the UPDATE, staff rows would remain and this count would be nonzero.
    4. Confirm no error mentioning prevent_profile_role_change was raised during apply.
    5. Reminder for merge time: merging to main AUTO-applies this migration to PROD — after deploy, re-run the same count query against prod (khslejtfbuezsmvmtsdn) and confirm 0.
  </how-to-verify>
  <resume-signal>Type "applied" once the test-project apply + empty-set count 0 are confirmed, or describe the failure (e.g. trigger blocked the UPDATE — that falsifies assumption A2 and needs a trigger-exemption revision)</resume-signal>
  <verify>
    <automated>MISSING — executor lacks Supabase MCP; manual verification query on the test project: SELECT count(*) FROM profiles WHERE is_admin = true AND role NOT IN ('both'); must return 0 (CI supabase test job re-proves it via supabase/tests/test_staff_role_both_backfill.sql)</automated>
  </verify>
  <acceptance_criteria>
    - apply_migration succeeded on qmnijlgmdhviwzwfyzlc with no prevent_profile_role_change error
    - schema_migrations row corrected to version 20260716120000
    - Empty-set count query returns 0 on the test project (A2 verified)
  </acceptance_criteria>
  <done>Test project caught up; RED-guarded assertion able to pass in CI; A2 confirmed; prod post-deploy recheck noted in SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration runner → profiles table | The backfill UPDATE runs as table owner/postgres; the prevent_profile_role_change trigger (SECURITY INVOKER) must not silently block it |
| nav render → role flags | Nav visibility is a UX hint, not the security boundary (RLS + page guards + withAllocatorAuth remain authoritative) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-06 | Denial of Service | Staff lockout — OR-in drop ships without the backfill | mitigate | migration + Sidebar edit in THIS one plan/PR (atomic GATE); empty-set SQL assertion is the CI-enforced proof; both fixture nav test asserts both workspaces render |
| T-109-07 | Denial of Service | prevent_profile_role_change trigger silently no-ops the backfill UPDATE (assumption A2) | mitigate | definitive check at the Task 3 test-project MCP apply: post-apply empty-set count must be 0; nonzero falsifies A2 and blocks merge |
| T-109-08 | Elevation of Privilege | is_admin still grants a marketplace surface via a missed OR-in site | mitigate | RESEARCH verified the OR-in exists at exactly 2 sites in 1 file (MobileNav delegates); grep acceptance gate returns 0 hits post-edit; flipped tests fail if reintroduced |
| T-109-09 | Information Disclosure | Nav-hide treated as the security boundary | accept | nav is a UX hint by design; the authoritative boundary is Plan 03's server-side page guard + existing RLS/withAllocatorAuth (defense-in-depth per RESEARCH Architectural Responsibility Map) |
| T-109-15 | Repudiation | Stale admin-sees-all assertion in `e2e/full-flow.spec.ts` / `e2e/match-queue.spec.ts` survives (Playwright advisory in CI, does not gate `frontend`) — a future reader trusts a spec that encodes the retired behavior | mitigate | load-bearing e2e reconciliation in Task 2; both specs named and their resolution recorded in the SUMMARY |
| T-109-SC | Tampering | npm/pip installs | accept | no package installs in this plan (zero new deps) |
</threat_model>

<verification>
- `npx vitest run src/components/layout/Sidebar.test.tsx src/components/layout/MobileNav.test.tsx --no-file-parallelism` green
- OR-in grep gate: 0 hits for the four dropped expressions in Sidebar.tsx; Admin gate at L131 intact
- Test-project empty-set query returns 0 (checkpoint confirmed)
- supabase SQL test job green in CI (the RED-guarded assertion passes only after the checkpoint)
- `npm run lint` green
</verification>

<success_criteria>
- Backfill + OR-in drop atomically in one plan/PR; empty-set invariant CI-enforced
- Manager fixture: zero allocator/Discovery nav items; allocator fixture: zero Strategies/Portfolios; is_admin+manager fixture: manager workspace + Admin only
- role='both' fixture renders both workspaces (staff preserved)
- Migration applied to test project with schema_migrations version corrected to 20260716120000
</success_criteria>

<output>
Create `.planning/phases/109-role-predicate-unification-page-guards/109-02-SUMMARY.md` when done. Record: the whole-repo admin-sees-all grep command + hit list, the A2 verification outcome, and the prod post-deploy count-query reminder.
</output>
