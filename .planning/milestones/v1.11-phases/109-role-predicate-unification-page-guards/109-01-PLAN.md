---
phase: 109-role-predicate-unification-page-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/auth/requireRolePage.ts
  - src/lib/auth/requireRolePage.test.ts
  - src/lib/api/withAllocatorAuth.test.ts
autonomous: true
requirements: [ROLE-04, ROLE-06]

must_haves:
  truths:
    - "A wrong-role user is redirected to their own role's home surface — never shown a 403 or a half-rendered page"
    - "A DB error during the role lookup throws (to error.tsx) and NEVER redirects — a transient Postgres hiccup cannot masquerade as wrong-role"
    - "A missing profile row throws with a Sentry soft signal and NEVER redirects"
    - "An unknown/malformed role is denied (owns=false) and redirected to /pending-approval — a terminal, unguarded route, structurally loop-free"
    - "role='both' owns BOTH surfaces and is never redirected off either"
    - "The 403 denied-action copy in withAllocatorAuth names the required role (ROLE-06)"
  artifacts:
    - path: "src/lib/auth/requireRolePage.ts"
      provides: "Shared three-branch server-side page guard on profiles.role"
      exports: ["requireRolePage"]
      min_lines: 40
    - path: "src/lib/auth/requireRolePage.test.ts"
      provides: "Branch-by-branch + full role×is_admin matrix unit coverage"
      min_lines: 80
  key_links:
    - from: "src/lib/auth/requireRolePage.ts"
      to: "profiles table"
      via: "supabase.from('profiles').select('role').eq('id', user.id).maybeSingle()"
      pattern: "from\\(.profiles.\\)"
    - from: "src/lib/auth/requireRolePage.ts"
      to: "next/navigation"
      via: "redirect(homeHref) OUTSIDE any try/catch"
      pattern: "redirect\\("
    - from: "src/lib/auth/requireRolePage.ts"
      to: "@/lib/sentry-capture"
      via: "captureToSentry with role_gate_failure tags"
      pattern: "captureToSentry"
---

<objective>
Create the ONE shared server-side page guard `requireRolePage` that mirrors `withAllocatorAuth.ts`'s three-branch failure discipline on `profiles.role`, with the wrong-role branch substituting a server `redirect()` for the 403 response. This is the interface Plan 03 attaches to all 7 owned route entry points (per D: "Add ONE shared server helper… mirroring withAllocatorAuth.ts's three-branch discipline").

Purpose: ROLE-04 — direct URL navigation to an unowned route must server-side redirect to the visitor's role home, never 403/half-render — with the full role×is_admin matrix proven redirect-loop-free in unit tests. Also closes ROLE-06 by asserting the existing denied-action 403 copy names the role requirement.
Output: `src/lib/auth/requireRolePage.ts` + `requireRolePage.test.ts` (new), one copy assertion in `withAllocatorAuth.test.ts` (if missing).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/109-role-predicate-unification-page-guards/109-RESEARCH.md
@.planning/phases/109-role-predicate-unification-page-guards/109-PATTERNS.md

TRAP — two role systems exist. This guard targets `profiles.role` (`allocator`|`manager`|`both`) — the system used by `withAllocatorAuth.ts` and `(dashboard)/layout.tsx:45-46`. Do NOT import or mirror `requireRole`/`withRole` from `src/lib/auth.ts` (those read the unrelated `user_app_roles` RBAC join table).

MODIFIED Next.js (16.2.10): read `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/redirect.md` before writing any redirect code. Key fact (redirect.md:50-52): `redirect()` throws `NEXT_REDIRECT` and MUST be called OUTSIDE any try/catch — a wrapping catch swallows it and the wrong-role user renders the page anyway (fail-open).

<interfaces>
Contracts the guard consumes (extracted from source — no exploration needed):

From src/lib/api/withAllocatorAuth.ts (the three-branch analog):
- Profile read shape: `supabase.from("profiles").select("role").eq("id", user.id).maybeSingle()` returning `{ data, error }`
- DB-error branch: console.error first, then `captureToSentry(error, { tags: { ...gate_failure: "true", ...kind: "lookup_error", code }, extra: { user_id }, level: "error" })`
- Missing-profile branch: `captureToSentry(new Error(...), { tags: { ...kind: "missing_profile" }, extra: { user_id }, level: "warning" })`
- Wrong-role 403 copy pattern: names the role ("allocator role required")

From src/lib/sentry-capture.ts:
- `captureToSentry(err, { tags, extra, level })` — best-effort; caller owns the console.error line

From src/app/(dashboard)/layout.tsx:45-46 (the role idiom — SSR source of truth):
- `isAllocator = role === "allocator" || role === "both"`
- `isManager = role === "manager" || role === "both"`

From src/lib/approval.ts:33-55 (deny-by-default discipline):
- exhaustive switch with `default: return false` — any role outside the known set is denied
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: requireRolePage — three-branch guard + full matrix test (RED→GREEN)</name>
  <files>src/lib/auth/requireRolePage.ts, src/lib/auth/requireRolePage.test.ts</files>
  <read_first>
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/redirect.md (redirect throws NEXT_REDIRECT; outside try/catch)
    - src/lib/api/withAllocatorAuth.ts (the three-branch analog — branch semantics, Sentry tags, console.error pairing)
    - src/lib/api/withAllocatorAuth.test.ts (mocking shape for the supabase client + branch assertions)
    - src/lib/sentry-capture.ts (captureToSentry signature)
    - src/app/(dashboard)/layout.tsx (lines 44-46 — the role idiom)
    - src/lib/approval.ts (lines 33-55 — deny-by-default default branch)
  </read_first>
  <behavior>
    - DB-error branch: profile read returns `error` → console.error then captureToSentry(level "error", tags role_gate_failure="true"/role_gate_kind="lookup_error"/role_gate_code) → THROWS; `redirect` is NEVER called (assert the redirect mock has zero calls — the test must fail if a redirect sneaks into this branch)
    - Missing-profile branch: `data` is null → captureToSentry(level "warning", role_gate_kind="missing_profile") → THROWS; redirect NEVER called
    - Wrong-role: role="manager", need="allocator" → redirect("/strategies") (the MANAGER's OWN home, not the surface being visited)
    - Wrong-role: role="allocator", need="manager" → redirect("/allocations")
    - Owner passes: role="allocator"+need="allocator", role="manager"+need="manager" → returns without redirect or throw
    - both owns everything: role="both" × need∈{allocator,manager} → returns; NEVER redirected (loop-freedom anchor)
    - Deny-by-default: role="analyst" / "" / null → redirect("/pending-approval") for BOTH need values (terminal unguarded route — cannot loop between the two surface homes)
    - is_admin irrelevance (ROLE-03 support): the guard selects ONLY "role" — assert the select argument is exactly "role" (structural proof is_admin cannot influence the decision)
    - Matrix: cover all 6 role×is_admin rows of RESEARCH.md's redirect-loop matrix (is_admin has no code path, so the rows collapse to the role cases above — enumerate them anyway in a describe block named after the matrix so the loop-freedom proof is explicit)
  </behavior>
  <action>
    Write the failing test first (mirror withAllocatorAuth.test.ts's supabase-client mock shape; mock next/navigation's redirect to throw a sentinel so call/no-call is assertable), commit RED, then implement.

    Implementation — create src/lib/auth/requireRolePage.ts with signature `requireRolePage(supabase, user, need: "allocator" | "manager"): Promise<void>` (supabase = the page's already-created server client, same type withAllocatorAuth receives; user = the page's getUser() result — caller has already handled !user). Start the file with the `import "server-only"` directive like withAllocatorAuth. Body, in order:
    1. Read `supabase.from("profiles").select("role").eq("id", user.id).maybeSingle()`.
    2. DB-error branch: console.error with user_id/code/message, captureToSentry per the interfaces block, then `throw error`. NEVER redirect here.
    3. Missing-profile branch: console.error, captureToSentry (level "warning", role_gate_kind "missing_profile"), `throw new Error("Profile row missing for auth user")`. NEVER redirect.
    4. Compute `owns` with the role idiom: need==="allocator" → role==="allocator"||role==="both"; need==="manager" → role==="manager"||role==="both". Unknown role → owns=false (deny-by-default per approval.ts).
    5. Compute homeHref from the user's ACTUAL role (not the need): "manager"→"/strategies", "allocator"→"/allocations", anything else→"/pending-approval". Deriving it internally (rather than a caller-passed homeHref) is deliberate — a caller-supplied wrong target could create a redirect loop; internal derivation makes loop-freedom provable in this one file (per D: home targets are Claude's Discretion; confirmed against routing: allocator home /allocations, manager home /strategies per RESEARCH matrix).
    6. `if (!owns) redirect(homeHref)` — this line sits at top level of the function body, OUTSIDE any try/catch (Pitfall 2). Do not wrap steps 1-5 in a try/catch that also encloses step 6.

    Do NOT key any branch on is_admin (ROLE-03). Do NOT import from src/lib/auth.ts.
  </action>
  <verify>
    <automated>npx vitest run src/lib/auth/requireRolePage.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/auth/requireRolePage.test.ts --no-file-parallelism` green, covering: DB-error→throw+no-redirect, missing-profile→throw+no-redirect, manager→/strategies, allocator→/allocations, both→never redirected (both needs), owner-passes, unknown-role→/pending-approval (both needs)
    - Source assertion: `grep -n "redirect(" src/lib/auth/requireRolePage.ts` shows the redirect call NOT inside a try block (no `try {` between the profile read's error handling and the redirect line)
    - Source assertion: `grep -c "is_admin" src/lib/auth/requireRolePage.ts` returns 0
    - Source assertion: `grep -c "user_app_roles\|requireRole\b.*lib/auth" src/lib/auth/requireRolePage.ts` returns 0
    - RED commit exists before GREEN commit (test(109-01) then feat(109-01))
  </acceptance_criteria>
  <done>Guard exists, exports requireRolePage, all matrix/branch tests green, redirect provably outside try/catch, no is_admin key.</done>
</task>

<task type="auto">
  <name>Task 2: ROLE-06 — assert the denied-action 403 copy names the role</name>
  <files>src/lib/api/withAllocatorAuth.test.ts</files>
  <read_first>
    - src/lib/api/withAllocatorAuth.ts (the wrong-role 403 branch and its response body copy)
    - src/lib/api/withAllocatorAuth.test.ts (existing assertions — check whether the copy is already asserted)
  </read_first>
  <action>
    ROLE-06 requires a role-denied action to show honest copy naming the role requirement, never an impossible retry. Pages are covered by the redirect (a denied user never sees a dead-end page); the residual denied-action surface is the API 403 from withAllocatorAuth. Verify withAllocatorAuth.test.ts contains an assertion that the wrong-role 403 response body copy names the allocator role (matches /allocator role required/i or the exact in-source string). If the assertion already exists, record its test name + line in the SUMMARY and make no edit (Rule 3 — surgical). If missing, add one focused assertion to the existing wrong-role test case (do not restructure the file). Also confirm by reading the 403 body that it contains no retry affordance/instruction — record the finding.
  </action>
  <verify>
    <automated>npx vitest run src/lib/api/withAllocatorAuth.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/api/withAllocatorAuth.test.ts --no-file-parallelism` green
    - A test assertion exists (pre-existing or added) matching the wrong-role 403 copy against a pattern that includes the word naming the role (e.g. /allocator role required/i)
    - SUMMARY records: assertion location (file:line) + confirmation the 403 body offers no retry that cannot succeed
  </acceptance_criteria>
  <done>ROLE-06 copy is test-pinned: the denied-action 403 names the required role, proven by a green assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → RSC page render | Any authenticated user can request any route URL; the guard is the server-side authorization decision |
| guard → profiles table | Role fact comes from the DB, not the session cookie |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Elevation of Privilege | requireRolePage — redirect swallowed by try/catch (fail-open: wrong-role user renders the page) | mitigate | redirect() at top level outside try/catch; test asserts wrong-role fires the redirect sentinel; grep acceptance criterion proves no wrapping try |
| T-109-02 | Denial of Service | requireRolePage — transient DB error misread as wrong-role, bouncing a valid owner off their own surface | mitigate | DB-error branch throws (503-equivalent via error.tsx), never redirects; test asserts zero redirect calls on the error branch |
| T-109-03 | Elevation of Privilege | requireRolePage — unknown/malformed role string bypasses the owns check | mitigate | deny-by-default (owns=false for any role outside {allocator,manager,both}), mirroring approval.ts; explicit unknown-role test cases |
| T-109-04 | Denial of Service | requireRolePage — redirect loop for a denied role (bouncing between the two surface homes) | mitigate | homeHref derived internally from the ACTUAL role; unknown role targets terminal unguarded /pending-approval; both never redirected; matrix test enumerates all rows |
| T-109-05 | Elevation of Privilege | is_admin treated as a marketplace grant inside the guard | mitigate | guard selects and branches on role ONLY; grep gate is_admin count 0 in the guard file |
| T-109-SC | Tampering | npm/pip installs | accept | no package installs in this plan (zero new deps — RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/lib/auth/requireRolePage.test.ts src/lib/api/withAllocatorAuth.test.ts --no-file-parallelism` — all green
- `npm run lint` — no new errors (react-hooks/lint gate per project convention)
- Redirect-loop matrix (RESEARCH.md lines 300-308) fully enumerated in the test file's describe block
</verification>

<success_criteria>
- requireRolePage.ts exists with the exact three-branch discipline; redirect provably outside try/catch
- All 6 role×is_admin matrix rows + missing-profile/DB-error rows covered green
- Unknown role terminally redirected to /pending-approval (no loop possible)
- ROLE-06 copy assertion green
</success_criteria>

<output>
Create `.planning/phases/109-role-predicate-unification-page-guards/109-01-SUMMARY.md` when done. Record: the chosen signature, the internal homeHref map (incl. the /pending-approval terminal rationale), and the ROLE-06 assertion location.
</output>
