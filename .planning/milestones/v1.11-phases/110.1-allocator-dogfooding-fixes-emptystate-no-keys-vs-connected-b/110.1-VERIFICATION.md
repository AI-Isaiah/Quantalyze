---
phase: 110.1-allocator-dogfooding-fixes-emptystate-no-keys-vs-connected-b
verified: 2026-07-16T20:30:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
---

# Phase 110.1: Allocator dogfooding fixes — Verification Report

**Phase Goal:** Three live-dogfooding honesty findings (EmptyState no-keys vs connected-but-empty · balance↔holdings label · validate-error mislabel) fixed honestly, each with a regression test that fails without the fix. No engine/reconstruction changes; no invented data.
**Verified:** 2026-07-16T20:30:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | ------- | ---------- | -------------- |
| 1 | DOGFOOD-1: EmptyState distinguishes no-keys from connected-but-empty; connected-but-empty does NOT tell the user to "Connect a … key" and links to Manage exchanges | ✓ VERIFIED | `EmptyState.tsx:45-90` — required `hasConnectedKeys` prop; third branch (`!hasSyncing && hasConnectedKeys`) renders "Your exchange is connected — no open positions have synced yet…" with a single `Manage exchanges →` link to `/profile?tab=exchanges` (no "Connect" text). Wired at `AllocationDashboardV2.tsx:101` `hasConnectedKeys={activeVenues.length > 0}`. Regression Test A (`EmptyState.test.tsx:116-144`) asserts the connect copy is ABSENT and only a Manage-exchanges anchor exists at `hasConnectedKeys=true` — fails without the fix. |
| 2 | DOGFOOD-2: Exchanges-tab "Active Allocation auto-synced" affirmative suppressed when allocator_holdings empty; real balance still shown; NO fabricated holdings | ✓ VERIFIED | `AllocatorExchangeManager.tsx:677-681` — affirmative renders only in the `hasHoldings` branch; otherwise `first sync in progress` / `no open positions yet`. `hasHoldings` sourced from a server head-count (`profile/page.tsx:57-60`, `count:"exact", head:true`, `.eq("allocator_id", user.id)`) threaded ProfileTabs→ExchangesTabContent→manager. No writes to `allocator_holdings` in the diff (label-only fix). Regression Test A (`.test.tsx:1384-1404`) asserts affirmative absent + honest copy + balance row still renders at `hasHoldings=false`. |
| 3 | DOGFOOD-3: fail-closed probe path returns an honest PROBE_FAILED "could not verify" message (not "trading or withdrawal permissions") WHILE still rejecting the key (read_only stays false) | ✓ VERIFIED | `exchange.py:1116-1130` — `if probe_error:` branch runs BEFORE `has_withdraw/has_trade`, sets `error_code="PROBE_FAILED"` + honest copy; `read_only` derived at :1111 (`has_read and not has_trade and not has_withdraw` → False on fail-closed default) is unchanged. TS backstop `route.ts:167-179` returns honest "could not be verified as read-only" at status 400 (grep: `trading or withdrawal permissions` = 0). Rejection preserved: `process_key.py:920` `val.read_only is False` gate and `long_fetch.py` transient bail intact. Pytest `TestValidateKeyPermissionsProbeError` (3 tests) pins PROBE_FAILED + read_only False + valid True + no scope copy; vitest route Test A pins negative-match. |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | ----------- | ------ | ------- |
| `src/app/(dashboard)/allocations/EmptyState.tsx` | 3-way branch incl. `hasConnectedKeys` | ✓ VERIFIED | prop declared + branched (grep `hasConnectedKeys`=6); Manage-exchanges link, no Connect text in the connected branch |
| `src/app/(dashboard)/allocations/AllocationDashboardV2.tsx` | passes `hasConnectedKeys` | ✓ VERIFIED | `:101` `hasConnectedKeys={activeVenues.length > 0}`, no payload/query change |
| `src/components/exchanges/AllocatorExchangeManager.tsx` | subtitle gated on `hasHoldings` | ✓ VERIFIED | `:677-681`; affirmative only in hasHoldings branch (grep `Active Allocation auto-synced`=1) |
| `src/app/(dashboard)/profile/page.tsx` | holdings head-count → hasHoldings | ✓ VERIFIED | `:57-60` head:true count scoped to user.id; no row payload |
| `src/components/auth/ProfileTabs.tsx` + `ExchangesTabContent.tsx` | thread hasHoldings | ✓ VERIFIED | required-prop threaded end-to-end; tsc clean (per SUMMARY) |
| `analytics-service/services/exchange.py` | PROBE_FAILED derivation | ✓ VERIFIED | `:1116-1130`; deribit early-return + scope branches untouched |
| `src/app/api/keys/validate-and-encrypt/route.ts` | honest backstop copy | ✓ VERIFIED | `:167-179`; F5b 4xx-forward byte-identical |
| test files (4) | regression-first, real assertions | ✓ VERIFIED | see truth evidence; all assert absence of the buggy behavior |

### Key Link Verification

| From | To | Via | Status |
| ---- | --- | --- | ------ |
| AllocationDashboardV2 | EmptyState | `hasConnectedKeys={activeVenues.length > 0}` | ✓ WIRED |
| profile/page.tsx | AllocatorExchangeManager | `hasHoldings` through ProfileTabs → ExchangesTabContent | ✓ WIRED |
| exchange.py | route.ts | `result["error"]` → 400 detail → F5b verbatim forward | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| All 3 frontend regression suites | `vitest run EmptyState + AllocatorExchangeManager + validate-and-encrypt route --no-file-parallelism` | 3 files / 60 tests passed | ✓ PASS |
| DOGFOOD-3 Python derivation | `pytest tests/test_exchange.py::TestValidateKeyPermissionsProbeError` | 3 passed (within 51 passed) | ✓ PASS |

Note: local `pytest tests/test_process_key.py` shows 1 failure — `ModuleNotFoundError: No module named 'pandera'` when `main` imports `routers.csv`. This is an environment gap in the local Python 3.14 (missing dependency), NOT a regression from Phase 110.1 — it is unrelated to the DOGFOOD-3 change and the 3 probe-error tests all pass. SUMMARY records `test_process_key.py — 49 passed` in the CI env with the dependency installed.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | No debt markers (TBD/FIXME/XXX) or stubs introduced in the touched files | ℹ️ Info | None |

### Scope Fence

`git diff --name-only b23be9d3~1..07faaf62` = 12 files, all within the two plans' declared sets. NO `equity_reconstruction.py`, NO deribit reconstruction, NO Simulate-Impact / "+ Allocation" / scenario dispatch files. Fence respected.

### Deferred-item note

Plan-01 SUMMARY deferred 5 pre-existing `ApiKeyForm` add-key test failures (`getByLabelText(/API Secret/i)` matched multiple inputs). These were fixed by follow-up commit `07faaf62` ("disambiguate API Secret label matcher from reveal toggle") — `AllocatorExchangeManager.test.tsx` now runs fully green (no skips) in this verification. No open deferral remains.

### Human Verification Required

None. All three findings are copy/data-honesty fixes whose exact rendered strings and rejection semantics are pinned by passing unit/component tests; no external-service, real-time, or visual-only behavior remains unverifiable programmatically.

### Gaps Summary

No gaps. All three dogfooding findings are honestly fixed in shipped code, each backed by a regression test that asserts the ABSENCE of the buggy behavior (not a tautology). The DOGFOOD-3 fix changes only the user-visible reason (PROBE_FAILED) — `read_only` stays False and every downstream rejection gate (`process_key.py:920`, `long_fetch` transient bail, legacy 400) still refuses the unverifiable key, so the fail-closed security posture is preserved. Scope fences honored.

---

_Verified: 2026-07-16T20:30:00Z_
_Verifier: Claude (gsd-verifier)_
