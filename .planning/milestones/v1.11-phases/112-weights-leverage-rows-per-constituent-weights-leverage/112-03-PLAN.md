---
phase: 112-weights-leverage-rows-per-constituent-weights-leverage
plan: 03
type: execute
wave: 3
depends_on: [112-01, 112-02]
files_modified: []
autonomous: true
requirements: [WEIGHTS-01, WEIGHTS-02]

must_haves:
  truths:
    - "src/lib/scenario.ts is byte-frozen (SC-3): git diff --exit-code clean vs the working tree baseline"
    - "The CONSTIT-04 / backbone grep gates are green (no symbol-keyed engine path, no Data-Sources identifier reintroduced)"
    - "The full allocations suite + engine behavior pins + leverage contract tests are green with no coverage regression"
  artifacts:
    - path: "src/lib/scenario.ts"
      provides: "the frozen engine — UNTOUCHED is the deliverable"
  key_links:
    - from: "phase close"
      to: "SC-3 freeze"
      via: "git diff --exit-code src/lib/scenario.ts + scenario-backbone-gates.test.ts"
      pattern: "scenario-backbone-gates"
---

<objective>
Phase-close gate sweep (the 112-GATE row in 112-VALIDATION.md): prove the SC-3 engine freeze, the anti-symbol-key grep gates, the engine behavior pins, and the full-surface test suite are ALL green after Plans 00-02. A dirty scenario.ts diff or a red backbone gate is a PHASE FAILURE (locked decision 2) — this plan modifies nothing; it verifies and documents.

Purpose: locked decision 2 makes the freeze gate an explicit exit criterion; the coverage CI gate (lines 82/stmts 80/funcs 74/branches 72) is blocking on merge.
Output: green sweep evidence in the SUMMARY; phase ready for /gsd:verify-work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-VALIDATION.md
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-01-SUMMARY.md
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-3 freeze + gate battery</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-VALIDATION.md (the 112-GATE row + sampling contract)
    - src/lib/scenario-backbone-gates.test.ts (what the grep gates enforce — read to interpret any failure, NEVER edit to make green)
  </read_first>
  <action>
    Run the gate battery in order and capture output: (1) git diff --exit-code src/lib/scenario.ts — must exit 0 (byte-frozen; also diff against the branch base to prove no intermediate commit touched it: git diff --exit-code $(git merge-base HEAD origin/main) -- src/lib/scenario.ts). (2) npx vitest run src/lib/scenario-backbone-gates.test.ts --no-file-parallelism — the CONSTIT-04 whole-repo orphan grep gate + SC-3 pins. (3) npx vitest run src/lib/scenario.test.ts --no-file-parallelism — the 50 engine behavior pins (leverage application unchanged). (4) npx vitest run src/lib/leverage.test.ts --no-file-parallelism — the sanitize-on-read contract. If ANY is red: STOP, treat as phase failure, root-cause in Plans 01/02 output (never patch the gate or the engine).
  </action>
  <verify>
    <automated>git diff --exit-code src/lib/scenario.ts && npx vitest run src/lib/scenario-backbone-gates.test.ts src/lib/scenario.test.ts src/lib/leverage.test.ts --no-file-parallelism 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - git diff --exit-code src/lib/scenario.ts exits 0 (working tree AND vs merge-base).
    - scenario-backbone-gates.test.ts, scenario.test.ts, leverage.test.ts all pass with zero skips.
  </acceptance_criteria>
  <done>Freeze + gate evidence captured verbatim in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Full-surface sweep — allocations suite, tsc, lint, coverage sanity</name>
  <files>(none — verification only)</files>
  <read_first>
    - vitest.config.ts (coverage thresholds: lines 82 / statements 80 / functions 74 / branches 72 — the blocking CI ratchet)
  </read_first>
  <action>
    Run: (1) npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism — the whole surface including all Wave-0-now-green regressions (local flakes under parallelism are a known pattern; a flake re-runs isolated before being called red). (2) npx tsc --noEmit. (3) npm run lint (react-hooks ERRORS are missed by tsc+vitest — pre-push rule). (4) Coverage spot-check on the touched area: npx vitest run "src/app/(dashboard)/allocations" --coverage --no-file-parallelism 2>&1 | tail -20 — confirm no threshold breach signal from the new UI branches (full-suite coverage is CI's job; this is the early warning). Record the six Wave-0 regression tests by name in the SUMMARY as the phase's guarded behaviors.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Allocations suite fully green (0 failures; flakes proven green isolated).
    - tsc 0 errors; lint 0 errors.
    - No coverage-threshold warning attributable to Phase-112 files.
  </acceptance_criteria>
  <done>Phase 112 exit evidence complete; ready for the post-execute chain (code review + fix → gates → verifier) and /qa for the manual honesty-copy check (112-VALIDATION Manual-Only table).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | verification-only plan; gates ARE the mitigation evidence for T-112-01/02/04 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-05 | Tampering | gate self-neutering | mitigate | gates are read-only in this plan; any red gate is a phase failure routed back to Plans 01/02 — the gate file is never edited to pass |
| T-112-SC | Tampering | package installs | n/a | no packages installed this phase |
</threat_model>

<verification>
- Entire 112-VALIDATION Per-Task Verification Map shows green, including the 112-GATE row.
- Manual-only item (honesty copy in the browser) explicitly handed to /qa — not silently skipped.
</verification>

<success_criteria>
- All three ROADMAP Phase-112 success criteria demonstrably TRUE with test evidence.
- scenario.ts byte-frozen; no symbol-keyed path; suite + tsc + lint green.
</success_criteria>

<output>
Create `.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-03-SUMMARY.md` when done (include the verbatim gate outputs + the named guarded-behavior test list).
</output>
