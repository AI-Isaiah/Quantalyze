---
phase: 112-weights-leverage-rows-per-constituent-weights-leverage
plan: 01
type: execute
wave: 1
depends_on: [112-00]
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-state.ts
  - src/app/(dashboard)/allocations/hooks/useScenarioState.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [WEIGHTS-01]

must_haves:
  truths:
    - "An allocator types a weight on a per-key constituent row and the blend share equals the typed value exactly"
    - "Weights sum to 1 across the mixed per-key + added engine-unit set after any single-row edit (never the enabledIdsOf basis)"
    - "Excluding a weighted per-key row shrinks the denominator honestly; re-including restores the typed weight"
    - "A single-row weight edit stamps exactly ONE ref into userWeightOverrides (diffCount honesty preserved)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      provides: "applyWeightOverrides with optional userExplicitRefs param (additive, backward compatible)"
      contains: "userExplicitRefs"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "per-key weight input on scenario-constituent-perkey rows + engine-unit-basis weight writer + blendShareByRef derivation"
      contains: "blendShareByRef"
  key_links:
    - from: "per-key row weight input onChange"
      to: "scenario.applyWeightOverrides(vector, basisIds, [editedRef])"
      via: "the engine-unit-basis writer (WR-01 pattern) — NEVER plain setWeightOverride for per-key/mixed edits"
      pattern: "applyWeightOverrides"
    - from: "ScenarioComposer projectionState"
      to: "CompositionList weight input value"
      via: "blendShareByRef prop (normalized over selected engine units)"
      pattern: "blendShareByRef"
---

<objective>
WEIGHTS-01: make the per-key (strategy-level) constituent rows weight-editable, routing every weight edit on the mixed per-key + added surface through the SELECTED-ENGINE-UNIT sum-to-1 basis (the WR-01 `applyWeightOverrides(weights, basisIds)` pattern) — never `setWeightOverride`'s `enabledIdsOf` basis, which excludes per-key units (Pitfall 2, locked decision 3). Weight = equity-capital share per the LOCKED WEIGHTS-00 Route-1/Option-A model; leverage stays a separate multiplier (Plan 02). Engine untouched (SC-3).

Purpose: closes Pitfall 2 with a RED-proven regression; turns the Wave-0 weight tests green.
Output: editable weight on every unified constituent row; sum-to-1 invariant holding across the mixed set; diffCount-honest single-ref stamping.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-CONTEXT.md
@DESIGN.md
@src/app/(dashboard)/allocations/lib/scenario-state.ts
@src/app/(dashboard)/allocations/hooks/useScenarioState.ts

<interfaces>
Verified working-tree contracts (2026-07-17):

- `applyWeightOverrides(draft, weights, basisIds?)` (scenario-state.ts:643-683): atomic full-vector write; renormalizes ONCE over basisIds; on a vector that already sums to 1 the renormalization is identity-scale, so typed values stick exactly. Stamps every provided ref into `userWeightOverrides` (:677-680) — the H-0126 diffCount driver this plan parameterizes.
- Hook wrapper: `useScenarioState.ts` — interface member `applyWeightOverrides: (weights, basisIds?) => void` (:114), `useCallback` wrapper (:324) delegating to the pure `applyWeightsPure` import (:50).
- `projectionState` (ScenarioComposer.tsx:2229-2274): per engine unit, weight = `draft.weightOverrides[s.id]` if finite else `activeAdapterOutput.state.weights[s.id] ?? 0`. In merged mode (`mergeAddedIntoPerKeySet`, scenario-adapter.ts:203) per-key defaults are equity SHARES normalized over the per-key sleeve (Σ=1 over keys) and added defaults are 0 — so the engine mass Σ selected weights can exceed 1 (e.g. keys 1.0 + added override 0.5 = 1.5) until this plan's writer materializes an explicit sum-1 vector.
- Engine-unit basis expression (WR-01, ScenarioComposer.tsx:4048-4051): `engineSet.strategies.filter((s) => engineSet.state.selected[s.id]).map((s) => s.id)`.
- `handleWeightChange` (ScenarioComposer.tsx:1037-1070): non-finite → console.warn + commitError + keep prior value; >1 → clamp message; clamps [0,1] locally then dispatches. KEEP this fail-loud contract verbatim.
- Per-key row render site: ScenarioComposer.tsx:4851-4905 (the "Phase 112 fence" comment at :4842-4850 says NO weight/leverage inputs — this plan removes the WEIGHT half of that fence). Added-row weight input reference: :4969-4979 (id `weight-${a.id}`, step 0.001, min 0, max 1, value `weight.toFixed(3)`, disabled when !enabled).
- `CompositionListProps` (:4785-4820) + invocation (:4446-4457).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: applyWeightOverrides — optional userExplicitRefs param (diffCount honesty)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.ts, src/app/(dashboard)/allocations/hooks/useScenarioState.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.ts:625-683 (applyWeightOverrides JSDoc + body, userWeightOverrides stamping), :106-130 (userWeightOverrides field doc, H-0126)
    - src/app/(dashboard)/allocations/hooks/useScenarioState.ts:106-130 (mutator interface), :318-330 (setWeightOverride/applyWeightOverrides useCallback wrappers)
    - src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts (the Wave-0 RED test (a) this task turns green)
  </read_first>
  <behavior>
    - Wave-0 test (a): applyWeightOverrides(draft, vector, basisIds, [K1]) stamps ONLY K1 into userWeightOverrides (K2/A absent) — currently RED.
    - Backward compat: omitting the 4th arg stamps ALL provided refs (existing optimizer Apply behavior unchanged — pinned by the pre-existing apply-weights tests).
  </behavior>
  <action>
    Add an optional fourth parameter `userExplicitRefs?: ReadonlyArray<string>` to the pure `applyWeightOverrides` (scenario-state.ts:643). When provided, the userWeightOverrides spread (:677-680) records only those refs (each with its post-normalization weight from nextWeights); when omitted, keep the current stamp-all-refs behavior byte-for-byte (the optimizer's "allocator clicked Apply for the whole vector" semantics). Extend the JSDoc: single-row composer edits pass the one edited ref so diffCount counts one user gesture as one edit (H-0126 honesty), full-vector optimizer applies omit it.

    Thread the param through useScenarioState.ts: the interface member (:114) gains the optional third arg, the useCallback wrapper (:324) forwards it to applyWeightsPure. No other callers change (param optional).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts" "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 RED test (a) now GREEN; Wave-0 pins (b)/(c)/(d) still green; all pre-existing scenario-state tests green.
    - `npx tsc --noEmit` clean (interface + wrapper + pure fn signatures agree).
    - grep shows exactly ONE writer of userWeightOverrides semantics changed (applyWeightOverrides); setWeightOverride (:617-620) untouched.
  </acceptance_criteria>
  <done>Param shipped additively; omitting it reproduces today's behavior; single-ref stamping proven by the Wave-0 test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Engine-unit-basis weight writer + blendShareByRef derivation in ScenarioComposer</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1037-1070 (handleWeightChange — the fail-loud clamp/message contract to preserve), :2229-2274 (projectionState), :2283-2286 (engineSet), :2094-2095 (usePerKeySources), :4035-4052 (the WR-01 optimizer apply-back — the basis expression to reuse)
    - .planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md § "The weight-sum recipe" points 1-5 + § "Design tension"
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (Wave-0 tests (b) and (d) this task enables)
  </read_first>
  <behavior>
    - Typing w on any row in mixed mode lands EXACTLY w as that row's blend share; the other selected engine units scale proportionally into (1−w); sum over selected engine units = 1 within 1e-9.
    - A single edit stamps only the edited ref into userWeightOverrides (via Task 1's param).
    - Added-only mode (usePerKeySources false) keeps the legacy setWeightOverride path byte-for-byte — zero behavior change for non-book composers.
  </behavior>
  <action>
    (1) Derive `blendShareByRef` (useMemo after projectionState): for ids with projectionState.selected true, share = projectionState.weights[id] / Σ(selected projectionState.weights); Σ ≤ 0 → empty map (no fabricated shares). This is the single display-and-edit basis for effective shares.

    (2) Extend handleWeightChange (keep its non-finite reject + clamp messages verbatim): after computing clampedWeight, branch — when usePerKeySources is FALSE, dispatch the legacy scenario.setWeightOverride(scopeRef, clampedWeight) exactly as today. When TRUE (the mixed per-key + added surface), build basisIds via the WR-01 expression (engineSet.strategies filtered by engineSet.state.selected, mapped to id), read current effective shares from blendShareByRef, and construct the full vector: edited ref = clampedWeight; every other basis ref = its share × (1−clampedWeight)/Σ(other shares); edge Σ(other shares)=0 → equal split of (1−clampedWeight) over the others (mirror setWeightOverride's :599-602 fallback); no others → edited ref gets clampWeight alone. Dispatch scenario.applyWeightOverrides(vector, basisIds, [scopeRef]) — atomic, per D-locked decision 3 (never a setWeightOverride loop; never the enabledIdsOf basis). Since the vector sums to 1, the typed value sticks exactly.

    (3) Add a code comment at the writer reconciling the CONSTIT-03 design tension per the research recommendation: togglePerKeySource stays the include/exclude channel (untouched); ALL weight renormalization on this surface now flows through the explicit engine-unit basis, so the toggle's delete-on-re-include semantics no longer govern weight math. Exclusion honestly shrinks the sum denominator via the engine's ephemeral renorm (scenario.ts:314-319 — read-only reference); typed weights persist in weightOverrides across exclude/re-include (Wave-0 pin (d)).

    Do NOT touch src/lib/scenario.ts. Do NOT modify togglePerKeySource.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts" --no-file-parallelism 2>&1 | tail -5 && git diff --exit-code src/lib/scenario.ts</automated>
  </verify>
  <acceptance_criteria>
    - Writer exists and routes mixed-mode edits through applyWeightOverrides with the engine-unit basisIds and [scopeRef] as userExplicitRefs; added-only mode diff shows NO change to the setWeightOverride dispatch.
    - `git diff --exit-code src/lib/scenario.ts` clean.
    - Composer weight tests remain RED only on the missing INPUT (Task 3), not on writer math — verified by the Task-3 run.
    - npx tsc --noEmit clean.
  </acceptance_criteria>
  <done>Engine-unit-basis writer live behind the existing fail-loud handler; legacy path untouched; SC-3 clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the weight input on per-key rows (remove the weight half of the Phase-112 fence)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:4785-4905 (CompositionListProps + per-key row + fence comment), :4965-4979 (added-row weight input — the exact markup/classes to mirror), :4446-4457 (CompositionList invocation)
    - DESIGN.md:148-166 (Numbers Contract — mono/tabular digits; no fabricated values)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (Wave-0 tests (a) weight half, (b), (d))
  </read_first>
  <behavior>
    - Wave-0 test (a) weight-input half: per-key row renders number input id `weight-<api_key_id>`, disabled when excluded.
    - Wave-0 test (b): typed 0.3 → displayed 0.300, K2 → ~0.3111, A → ~0.3889, sum 1.
    - Wave-0 test (d): exclude → re-include restores 0.300.
  </behavior>
  <action>
    Thread `blendShareByRef: Record<string, number>` through CompositionListProps and the :4446 invocation. In the per-key row (:4851-4905): add a sr-only label + number input mirroring the added-row weight input exactly (id `weight-${k.id}`, type number, step 0.001, min 0, max 1, same className, disabled={!included}), value = (blendShareByRef[k.id] ?? draft.weightOverrides[k.id] ?? 0).toFixed(3) — an excluded row falls back to its preserved stored weight (blendShareByRef only carries selected ids), wired to the existing onSetWeight prop (which is handleWeightChange). Rewrite the fence comment at :4842-4850: weight input now live (Phase 112 WEIGHTS-01); leverage input still fenced to Plan 02; a per-key source still has no remove button (toggled, not removed).

    Update the added-row weight input's value derivation ONLY if the Wave-0 mixed-share test proves the stored `draft.weightOverrides[a.id] ?? 0` display diverges from the row's true blend share after materialization — after the first engine-basis edit the stored weights ARE the sum-1 shares, so no change is expected; if the pre-first-edit added display must stay byte-identical, leave it (surgical rule).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism 2>&1 | tail -12 && git diff --exit-code src/lib/scenario.ts</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 composer tests (b) and (d) GREEN; test (a) green on its weight-input assertions (its leverage-input assertions stay RED for Plan 02 — split the test if the Wave-0 author combined them, so this plan exits with zero unexpected reds).
    - All pre-existing ScenarioComposer tests green; `npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism` has no new failures beyond the declared Plan-02 reds (leverage input + save prune).
    - git diff --exit-code src/lib/scenario.ts clean; npx vitest run src/lib/scenario-backbone-gates.test.ts green.
  </acceptance_criteria>
  <done>Per-key rows are weight-editable end-to-end: input → handleWeightChange → engine-unit-basis applyWeightOverrides → projectionState → blend. Typed 30% renders as 30% of the blend.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| weight input → client draft state | untrusted numeric input (NaN/Infinity/negative/huge via paste) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | handleWeightChange | mitigate | preserved fail-loud contract: non-finite rejected with visible commitError + prior value kept; [0,1] clamp at the handler boundary; clampAllWeights defense-in-depth exit gate in every mutator |
| T-112-03 | DoS (draft corruption) | weightOverrides persistence | mitigate | no SCENARIO_SCHEMA_VERSION bump (locked decision 5) — per-key entries are new VALUES in the existing map; older drafts load unchanged |
| T-112-SC | Tampering | package installs | n/a | no packages installed this phase |
</threat_model>

<verification>
- Wave-0 weight tests (state (a)(b)(c)(d) + composer (b)(d) + composer (a) weight half) all green.
- `git diff --exit-code src/lib/scenario.ts` clean after every task (SC-3).
- Added-only mode behavior byte-identical (legacy setWeightOverride path untouched — existing test suite is the guard).
</verification>

<success_criteria>
- ROADMAP 112 SC-1 TRUE: allocator sets per-strategy weights on strategy-level rows (e.g. A→30%, B→10%) with sum-to-1 preserved through the collapse over the SELECTED ENGINE UNIT basis.
- Pitfall 2 closed with a RED-proven regression (Wave-0 state test (c) documents the failure mode; composer test (b) proves the fix).
- Open Question 2 resolved as tested behavior: typed per-key weight preserved on exclude, restored on re-include.
</success_criteria>

<output>
Create `.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-01-SUMMARY.md` when done.
</output>
