---
phase: 112-weights-leverage-rows-per-constituent-weights-leverage
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx
autonomous: true
requirements: [WEIGHTS-01, WEIGHTS-02]

must_haves:
  truths:
    - "A RED-proof test exists for every Phase-112 guarded behavior BEFORE any implementation lands (regression-first)"
    - "The two research pitfalls (per-key leverage pruned at Save; wrong weight-sum basis) each have a test that FAILS on the current tree"
    - "Pre-existing tests in all three touched files stay green"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts"
      provides: "engine-unit-basis sum-to-1 pins + userExplicitRefs RED test + togglePerKeySource weight-preservation pin"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "per-key weight/leverage input RED tests + typed-share/blend RED tests + exclude→re-include preservation RED test"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx"
      provides: "Pitfall-1 RED test: leverage-only per-key edit survives Save (pruned today)"
  key_links:
    - from: "ScenarioComposer.test.tsx new cases"
      to: "scenario-constituent-perkey rows"
      via: "data-testid + input id selectors weight-<api_key_id> / leverage-<api_key_id>"
      pattern: "scenario-constituent-perkey"
    - from: "ScenarioComposer.save.test.tsx new case"
      to: "draft.leverageOverrides in the PUT/POST body"
      via: "save-route payload assertion"
      pattern: "leverageOverrides"
---

<objective>
Wave 0 test scaffold (Nyquist Dimension 8): write the RED regression tests for every Phase-112 guarded behavior BEFORE implementation, proving each test fails on the current tree for the RIGHT reason (assertion mismatch / missing element — never an import error or crash). These tests are the automated `<verify>` targets for Plans 01 and 02.

Purpose: regression-first discipline (project rule) — every guarded behavior gets a test that fails without the fix. Closes the two ❌ W0 gaps in 112-VALIDATION.md.
Output: extended test files committed RED (with captured failure output in the commit body), pre-existing tests untouched and green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-CONTEXT.md
@src/app/(dashboard)/allocations/lib/scenario-state.ts
@src/app/(dashboard)/allocations/lib/scenario-adapter.ts

<interfaces>
Key contracts the tests assert against (verified in working tree 2026-07-17):

From src/app/(dashboard)/allocations/lib/scenario-state.ts:
- `setWeightOverride(draft, scopeRef, newWeight)` (:581) — renormalizes others over `enabledIdsOf(draft)` (:306, filters `toggleByScopeRef === true`). Per-key `api_key_id` refs are NEVER in that basis (togglePerKeySource deletes the ref on re-include, :439).
- `applyWeightOverrides(draft, weights, basisIds?)` (:643) — atomic full-vector write, renormalizes ONCE over `basisIds` (WR-01) or `enabledIdsOf` when omitted. Stamps EVERY provided ref into `userWeightOverrides` (:677-680, H-0126 — drives diffCount).
- `togglePerKeySource(draft, ref)` (:430) — exclude writes `toggleByScopeRef[ref]=false`; re-include DELETES the ref; NEVER touches `weightOverrides`.

From src/app/(dashboard)/allocations/lib/scenario-adapter.ts:
- `buildPerKeyStrategyForBuilderSet` (:131) — per-key unit id === api_key_id; weights = RAW clamped equity USD.
- `mergeAddedIntoPerKeySet` (:203) — normalizes per-key USD to equity SHARES (÷Σ) at the merge point; added units default weight 0 (overlaid by draft.weightOverrides downstream in projectionState).

From src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:
- Per-key row: `data-testid="scenario-constituent-perkey"` (:4864); added row: `data-testid="scenario-constituent-added"` (:4931). Added-row inputs use ids `weight-${a.id}` (:4970) and `leverage-${a.id}` (:4984) — the per-key inputs MUST mirror these ids keyed by api_key_id.
- `pruneLeverageToDraftRefs(leverageByRef, draft)` (:720-734) — keeps only refs in `addedStrategies ∪ toggleByScopeRef-keys ∪ weightOverrides-keys`. Called at Save (:1734-1737 PUT, :1778-1781 POST) inside `setLeverageOverrides(...)`.
- `projectionState` (:2229-2274) — overlays `draft.weightOverrides[s.id]` and `leverageByRef[s.id]` per engine unit.

From src/lib/leverage.ts: `MAX_LEVERAGE = 10` (:36), `sanitizeLeverage` (:79), `sanitizeLeverageMap` (:105).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: State-layer weight-basis tests — engine-unit sum-to-1 pins + userExplicitRefs RED</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts (existing fixture/draft-builder patterns — extend, do not fork conventions)
    - src/app/(dashboard)/allocations/lib/scenario-state.ts:306-310 (enabledIdsOf), :430-446 (togglePerKeySource), :581-623 (setWeightOverride), :643-683 (applyWeightOverrides + userWeightOverrides stamping)
    - .planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md § "The weight-sum recipe" + Pitfall 2
  </read_first>
  <action>
    Add four tests to scenario-state-apply-weights.test.ts using a mixed fixture: two per-key refs K1/K2 (api_key_id-style ids ABSENT from toggleByScopeRef — the included-by-default per-key state) and one added strategy A in addedStrategies with toggleByScopeRef[A]=true and weightOverrides[A]=0.5.

    (a) RED — userExplicitRefs: call applyWeightOverrides(draft, {K1:0.3, K2:0.311111, A:0.388889}, [K1,K2,A], [K1]) with a FOURTH argument naming the single user-edited ref. Assert userWeightOverrides afterwards contains K1 but NOT K2 and NOT A. This fails today (the parameter does not exist; all provided refs are stamped per :677-680). This RED-proofs the diffCount-honesty extension Plan 01 adds — a single-row edit must not inflate diffCount by the whole basis.

    (b) GREEN pin — engine-unit basis sum-to-1: applyWeightOverrides(draft, fullVector, [K1,K2,A]) where fullVector sums to 1 → assert weightOverrides[K1]+weightOverrides[K2]+weightOverrides[A] === 1 within 1e-9 AND the typed value for each ref is exact (renormalizeWeights is identity-scale on a sum-1 vector). Pins WR-01 behavior over the mixed per-key + added basis.

    (c) GREEN pin (wrong-tool characterization, per 112-VALIDATION Wave-0 item 2): setWeightOverride(draft, K1, 0.3) → assert its renormalization did NOT touch K2 (per-key refs are outside enabledIdsOf) and therefore the sum over the mixed engine set {K1,K2,A} does NOT equal 1. This documents WHY the composer writer must never route per-key edits through plain setWeightOverride (Pitfall 2, the #528 drift class).

    (d) GREEN pin — weight preservation across toggle: seed weightOverrides[K1]=0.3, call togglePerKeySource(draft,K1) (exclude) then togglePerKeySource again (re-include) → assert weightOverrides[K1] === 0.3 both after exclude and after re-include (togglePerKeySource never touches weightOverrides — the preserve-and-restore contract Open Question 2 relies on).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state-apply-weights.test.ts" --no-file-parallelism 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE new test fails (the userExplicitRefs test, a) and the failure is an expect() assertion on userWeightOverrides membership — not a TypeError/import error.
    - Tests (b), (c), (d) pass on the current tree (they pin existing behavior).
    - All pre-existing tests in the file still pass.
    - Test (b) asserts the 1e-9 tolerance explicitly (the scenario-state.ts:10 invariant).
  </acceptance_criteria>
  <done>4 new tests committed as `test(112-00): RED userExplicitRefs + engine-unit basis pins` with the RED output quoted in the commit body; 1 RED / 3 GREEN confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Composer RED tests — per-key weight/leverage inputs, typed share, exclude→re-include restore</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (existing render harness: payload fixtures with perKeyReturnsByApiKeyId / eligibleApiKeyIds / apiKeys, book-mode gate setup, existing added-row weight/leverage interaction tests — reuse the established fixture builders verbatim)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:4841-4905 (per-key row render + the Phase-112 fence comment), :4969-4995 (added-row input reference: ids weight-${id} / leverage-${id})
    - .planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md § Existing infra → gap table + § Code Examples
  </read_first>
  <action>
    Add a `describe("Phase 112 — per-key weights + leverage")` block using the existing book-mode (perKeyDailiesGateSatisfied) harness with two eligible per-key sources (equity 60000 / 40000 → merged equity shares 0.6/0.4) and one added strategy with weightOverride 0.5 (mixed engine mass 1.5; effective shares K1 0.4, K2 0.2667, A 0.3333).

    RED tests to write (all fail on the current tree because the per-key fence at ScenarioComposer.tsx:4847-4850 renders no inputs):
    (a) Each scenario-constituent-perkey row renders a number input with id `weight-<api_key_id>` and a number input with id `leverage-<api_key_id>` (min 0, max MAX_LEVERAGE on the leverage input), both disabled when the row is excluded.
    (b) Typing 0.3 into K1's weight input → K1's displayed weight value becomes 0.300 AND the sum of all three rows' effective weights is 1 within tolerance AND the other rows scale proportionally (K2 → ~0.3111, A → ~0.3889 given the fixture above — assert with a sensible toFixed(3)/closeTo tolerance).
    (c) Setting K1's leverage input to 2 → an existing projection-derived rendered output changes vs the 1× baseline (pick the most stable observable already asserted elsewhere in this file — e.g. a KPI/summary testid the harness reads; the blend re-derives per engine wᵢ·Lᵢ·rᵢ).
    (d) Type 0.3 into K1's weight → toggle K1 excluded (role=switch on the per-key row) → toggle back included → K1's weight input again reads 0.300 (preserve-and-restore, Open Question 2).

    Do NOT modify ScenarioComposer.tsx in this task. Use the added-row tests in this same file as the interaction-pattern reference (fireEvent/userEvent conventions).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 4 new tests FAIL on the current tree, each with a query/assertion failure (element `weight-<api_key_id>` not found, or share assertion mismatch) — never a harness/import crash.
    - Every pre-existing test in ScenarioComposer.test.tsx still passes.
    - The expected shares in test (b) are the concrete fixture-derived numbers (0.300 / ~0.3111 / ~0.3889), not symbolic placeholders.
  </acceptance_criteria>
  <done>New describe block committed as `test(112-00): RED per-key weight/leverage input + share tests` with RED output quoted; 4 RED / 0 collateral failures.</done>
</task>

<task type="auto">
  <name>Task 3: Save-path RED test — leverage-only per-key edit survives Save (Pitfall 1)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx (existing Save harness: how PUT/POST bodies are captured and how leverageByRef is driven — reuse)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:720-734 (pruneLeverageToDraftRefs current-set), :1734-1737 and :1778-1781 (the two setLeverageOverrides + prune call sites)
    - src/lib/leverage.ts:79-118 (sanitizeLeverage / sanitizeLeverageMap — the sanitize-on-read contract)
    - .planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md § Pitfall 1 + Pitfall 4
  </read_first>
  <action>
    Two additions:

    (a) RED — prune drop: in the book-mode Save harness, drive a leverage-only edit on an INCLUDED per-key ref (set leverageByRef[api_key_id]=2 via the leverage channel the harness exposes; write NO weight override and NO toggle entry for that ref), trigger Save, and assert the captured request body's draft.leverageOverrides[api_key_id] === 2. FAILS today: the ref is absent from addedStrategies ∪ toggleByScopeRef ∪ weightOverrides, so pruneLeverageToDraftRefs drops it (ScenarioComposer.tsx:724-728). NOTE: until Plan 02 lands the per-key leverage INPUT, drive leverageByRef through whatever channel the existing harness uses for leverage state (e.g. the added-row leverage handler pattern or direct state seeding the harness already supports) — the assertion is on the SAVE PAYLOAD, not the input widget.

    (b) GREEN pin — sanitize-on-read for a per-key ref: hydrate a saved draft whose leverageOverrides carries a hostile per-key value (999 → clamps to MAX_LEVERAGE=10; -3 → 1; a non-finite → 1 per sanitizeLeverage :79-97) and assert (i) the draft LOADS (never reset/deleted — the no-zod-refine contract, Pitfall 4 / locked decision 4) and (ii) the effective leverage is the sanitized value. If an equivalent pin already exists for added refs, extend it with the api_key_id-ref case rather than duplicating.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" --no-file-parallelism 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Test (a) FAILS on the current tree with an assertion on the save body's leverageOverrides (expected 2, received undefined) — the exact Pitfall-1 symptom.
    - Test (b) passes (sanitize-on-read already covers arbitrary refs) and proves: hostile value in, clamped value out, draft alive.
    - All pre-existing save tests still pass.
  </acceptance_criteria>
  <done>Committed as `test(112-00): RED per-key leverage prune-drop at Save + sanitize pin`; 1 RED / 1 GREEN confirmed with output quoted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| number input → client draft state | untrusted numeric input (paste: NaN, Infinity, negative, huge) |
| persisted jsonb draft → hydration codec | hostile/stale persisted leverage or weight values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | weight/leverage inputs → draft | mitigate | Task 3(b) pins sanitize-on-read (sanitizeLeverage clamp, draft never reset); handler clamps pinned by existing handleWeightChange/handleLeverageChange tests |
| T-112-02 | Tampering | saved jsonb draft hydration | mitigate | no zod .min/.max refine ever added (a refine failure routes the codec to RESET → deletes the draft); Task 3(b) is the regression guard |
| T-112-SC | Tampering | package installs | n/a | Phase installs NO packages (Vitest already present — 112-RESEARCH Validation Architecture); no legitimacy audit needed |
</threat_model>

<verification>
- All three test files run green EXCEPT exactly the 6 intended RED tests (1 state-layer, 4 composer, 1 save).
- Each RED failure is an assertion/query mismatch, never a crash — captured in commit bodies as the RED proof.
- No production file modified (git diff limited to the three test files).
</verification>

<success_criteria>
- 112-VALIDATION.md Wave-0 checklist satisfiable: every Plan 01/02 task now has an automated verify target.
- RED baseline documented; Plans 01/02 turn these exact tests green.
</success_criteria>

<output>
Create `.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-00-SUMMARY.md` when done (list the 6 RED tests by name + their failure messages — Plan 01/02 executors flip them green).
</output>
