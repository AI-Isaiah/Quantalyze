---
phase: 112-weights-leverage-rows-per-constituent-weights-leverage
plan: 02
type: execute
wave: 2
depends_on: [112-01]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [WEIGHTS-02]

must_haves:
  truths:
    - "An allocator adjusts leverage on a per-key constituent row and the blend re-derives from the levered daily series (engine wᵢ·Lᵢ·rᵢ, zero engine edits)"
    - "A leverage-only per-key edit survives Save → reopen (Pitfall 1 closed)"
    - "A hostile leverage value can never delete or corrupt the saved draft (sanitize-on-read, never a zod refine)"
    - "Each constituent row shows a derived READ-ONLY notional (equity × L) — informative only, never a weight input; non-derivable → em-dash, never 0"
    - "When any selected row is levered, an honest caveat states Sharpe/Sortino/Calmar do not shift with leverage"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "per-key leverage input + pruneLeverageToDraftRefs per-key extension + notional column + leverage-invariance caveat"
      contains: "eligiblePerKeyIds"
  key_links:
    - from: "per-key leverage input"
      to: "leverageByRef via handleLeverageChange"
      via: "the EXISTING clamp handler (ScenarioComposer.tsx:1077-1099) — no new leverage map, no new handler"
      pattern: "handleLeverageChange"
    - from: "pruneLeverageToDraftRefs"
      to: "eligible per-key api_key_ids"
      via: "third parameter folded into the `current` keep-set at both Save call sites (:1734-1737, :1778-1781)"
      pattern: "pruneLeverageToDraftRefs"
    - from: "notional cell"
      to: "blendShareByRef × totalBookEquity × leverageByRef"
      via: "derived display-only computation; formatCurrency from @/lib/utils; em-dash on null/non-finite"
      pattern: "formatCurrency"
---

<objective>
WEIGHTS-02 + the locked WEIGHTS-00 notional column: render the per-row LEVERAGE input on per-key constituent rows (mirroring the added-row reference, reusing `leverageByRef` + `handleLeverageChange` verbatim — no new leverage machinery), fix the Pitfall-1 prune-drop so a leverage-only per-key edit survives Save, and add the DERIVED read-only notional (equity × L) column with the honest Sharpe/Sortino/Calmar leverage-invariance caveat. The blend re-derivation already exists in the frozen engine (`wᵢ·Lᵢ·rᵢ`, scenario.ts:424-430) and `projectionState` already wires `leverageByRef[api_key_id]` — this plan is input surface + save-prune + honest display only.

Purpose: closes Pitfall 1 with the Wave-0 RED regression; delivers locked decision 1's `{weight editable} × {leverage editable} → {notional derived read-only}` row columns.
Output: fully levered per-key rows; Save-safe leverage; honest notional + invariance caveat per DESIGN.md Numbers Contract.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-CONTEXT.md
@DESIGN.md
@src/lib/leverage.ts

<interfaces>
Verified working-tree contracts (2026-07-17):

- `handleLeverageChange(scopeRef, leverage)` (ScenarioComposer.tsx:1077-1099): non-finite → warn + commitError + keep prior; clamps [0, MAX_LEVERAGE] with visible message; writes `setLeverageByRef((prev) => ({...prev, [scopeRef]: clamped}))`. REUSE VERBATIM for per-key refs (locked decision 4).
- `pruneLeverageToDraftRefs(leverageByRef, draft)` (:720-734): keep-set = addedStrategies ∪ toggleByScopeRef-keys ∪ weightOverrides-keys. An INCLUDED per-key ref with a leverage-only edit is in NONE of the three → dropped (Pitfall 1). Call sites: PUT :1734-1737, POST :1778-1781, both as `setLeverageOverrides(scenario.draft..., pruneLeverageToDraftRefs(leverageByRef, scenario.draft))`.
- Added-row leverage input reference (:4983-4995): id `leverage-${a.id}`, type number, step 0.1, min 0, max MAX_LEVERAGE, value `(leverageByRef[a.id] ?? 1).toString()`, title "Leverage multiplier (1× = unlevered; excludes borrow cost)", disabled when !enabled.
- `equityByApiKeyId` (:2054-2062): api_key_id → Σ holdingEquityContributionLocal — the canonical D2 equity source (NEVER re-derive from value_usd).
- `dataSourceKeys` (:2177-2181): payload.apiKeys filtered to payload.eligibleApiKeyIds — the eligible per-key id source.
- `blendShareByRef` (added by Plan 01): selected engine unit id → effective blend share (sum 1).
- `formatCurrency` from `@/lib/utils` — allocations-surface USD formatter; null handled at call site with em-dash (pattern: mandate-gates.ts:196).
- Frozen-engine facts for the caveat copy (read-only): leverage scales return/vol/maxDD (scenario.ts:109-114) but Sharpe/Sortino/Calmar and the correlation matrix are leverage-INVARIANT (:110-117, :403-407 — costless daily-return scaling, no borrow modeled). Existing precedent subtitle: "Correlation does not shift with per-strategy leverage" (:4074).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the leverage input on per-key rows (remove the rest of the Phase-112 fence)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:4851-4905 (per-key row post-Plan-01), :4983-4995 (added-row leverage input to mirror exactly), :1077-1099 (handleLeverageChange)
    - src/lib/leverage.ts:36 (MAX_LEVERAGE=10), :79-118 (sanitize contract — read-only, do not modify)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (Wave-0 tests (a) leverage half + (c))
  </read_first>
  <behavior>
    - Wave-0 test (a) leverage half: per-key row renders number input id `leverage-<api_key_id>`, min 0, max MAX_LEVERAGE, disabled when excluded.
    - Wave-0 test (c): setting K1 leverage to 2 changes the projection-derived output vs the 1× baseline (blend re-derives via the frozen engine's wᵢ·Lᵢ·rᵢ — projectionState :2260-2261 already wires it).
  </behavior>
  <action>
    In the per-key row (:4851-4905), after the Plan-01 weight input: add a sr-only label + leverage number input mirroring :4983-4995 exactly — id `leverage-${k.id}`, step 0.1, min 0, max MAX_LEVERAGE, value (leverageByRef[k.id] ?? 1).toString(), title "Leverage multiplier (1× = unlevered; excludes borrow cost)", aria-label naming the source, disabled={!included}, onChange dispatching the existing onSetLeverage prop (already threaded to handleLeverageChange at :4454). Delete the remaining leverage half of the fence comment; state the row's final column anatomy in the comment: {equity-share weight editable} × {leverage editable} → {notional derived read-only, Task 3} per WEIGHTS-00 Route 1/Option A (A1 locked 2026-07-17). No new state, no new handler, no zod refine anywhere (locked decision 4). Key strictly by api_key_id — never a symbol/coin (Pitfall 3, CONSTIT-04 grep gate).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism 2>&1 | tail -10 && git diff --exit-code src/lib/scenario.ts</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 composer tests (a) (both input halves) and (c) GREEN; all pre-existing composer tests green.
    - src/lib/scenario.ts diff clean; npx vitest run src/lib/scenario-backbone-gates.test.ts green (no symbol-keyed identifier reintroduced).
    - The input clamps via handleLeverageChange only — grep confirms zero new `.min(`/`.max(` zod refines on leverage anywhere in the diff.
  </acceptance_criteria>
  <done>Per-key rows are leverage-editable end-to-end: input → handleLeverageChange → leverageByRef → projectionState → frozen-engine wᵢ·Lᵢ·rᵢ re-derivation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pitfall-1 fix — pruneLeverageToDraftRefs keeps eligible per-key refs</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:706-734 (pruneLeverageToDraftRefs JSDoc + body), :1720-1790 (both Save call sites in context), :2177-2181 (dataSourceKeys), :2094-2095 (usePerKeySources)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx (Wave-0 RED test (a) this task turns green + sanitize pin (b))
  </read_first>
  <behavior>
    - Wave-0 save test (a): leverage-only edit on an INCLUDED per-key ref (no weight, no toggle) → Save → request body draft.leverageOverrides[api_key_id] === 2 — currently RED (pruned).
    - Stale-ref hygiene preserved: a leverage entry for a ref that is neither an added strategy, a toggled ref, a weighted ref, NOR an eligible per-key id is still dropped at Save.
  </behavior>
  <action>
    Add a third parameter `eligiblePerKeyIds: ReadonlyArray<string>` to pruneLeverageToDraftRefs and fold it into the `current` keep-set (:724-728). Update the JSDoc: an included per-key source legitimately carries leverage with NO weight override and NO toggle entry (absent = included), so eligibility itself is the keep signal — Pitfall 1, 112-RESEARCH. At BOTH call sites (:1734-1737 PUT and :1778-1781 POST) pass `usePerKeySources ? dataSourceKeys.map((k) => k.id) : []` — when the per-key path is inactive no per-key leverage input renders, so an empty keep-extension preserves today's stale-pruning behavior exactly.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" --no-file-parallelism 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 save test (a) GREEN: set per-key leverage 2× only → Save → payload leverageOverrides[api_key_id] === 2 → (reopen covered by the sanitize pin reading it back).
    - Sanitize pin (b) still green (hostile 999/-3/non-finite → clamped, draft alive).
    - All pre-existing save tests green (added-ref pruning + removed-leg dropping unchanged).
  </acceptance_criteria>
  <done>Reopened saved scenario shows per-key leverage intact — the Pitfall-1 warning sign ("reset to 1×") is regression-guarded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Derived read-only notional column + leverage-invariance honesty caveat</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - DESIGN.md:148-166 (Numbers Contract: mono/tabular via .font-metric; null/non-finite → em-dash `—`, NEVER 0, never fabricated; a — cell never carries semantic color)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:2054-2062 (equityByApiKeyId), :4074 (the existing correlation-invariance subtitle — copy pattern precedent), :4841-5011 (CompositionList rows)
    - src/app/(dashboard)/allocations/lib/mandate-gates.ts:196-197 (formatCurrency + em-dash call-site pattern)
    - .planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-RESEARCH.md § "Honest levered-KPI treatment" (the invariance table)
  </read_first>
  <behavior>
    - Test (new, this task, write FIRST): with book equity 100000 (K1 60000/K2 40000), K1 weight 0.3 and K1 leverage 2 → K1's notional cell renders formatCurrency(0.3 × 100000 × 2) = the $60,000-formatted string; the unlevered K2 cell shows share × 100000 × 1.
    - Test: added-only mode (no book equity) → every notional cell renders `—` (em-dash), never $0.
    - Test: when any selected row's leverage ≠ 1 the invariance caveat renders (data-testid `scenario-leverage-invariance-note`); with all rows at 1× it does not.
  </behavior>
  <action>
    (1) Derive `totalBookEquity` in the composer: Σ equityByApiKeyId over the eligible per-key ids (dataSourceKeys ids) when usePerKeySources, else null — the allocator's real book equity, the base the WEIGHTS memory's "Notional = equity × L" reads against. Thread totalBookEquity (number|null) into CompositionList alongside blendShareByRef.

    (2) Notional cell on BOTH row types (per-key and added), rendered after the leverage input as read-only text (NOT an input — locked decision 1: notional is NEVER a weight input): notional = blendShareByRef[id] × totalBookEquity × (leverageByRef[id] ?? 1). Any factor null/absent/non-finite (no book equity; excluded row absent from blendShareByRef; Σ ≤ 0) → em-dash `—` per the Numbers Contract — never 0, never a fabricated value. Format via formatCurrency from @/lib/utils with the mandate-gates.ts:196 null-handling pattern; className carries the mono metric treatment consistent with the row's inputs; title tooltip: "Notional = equity × leverage — derived, informative only (minimum-investment check); never a weight input". data-testid `scenario-constituent-notional`.

    (3) Honesty caveat: inside the CompositionList card, render a muted caption (data-testid `scenario-leverage-invariance-note`) when any INCLUDED row has (leverageByRef[id] ?? 1) !== 1, mirroring the :4074 subtitle pattern: leverage scales return, volatility and max drawdown; risk-adjusted ratios (Sharpe, Sortino, Calmar) and correlation do not shift with leverage — borrow cost is not modeled. This is the DESIGN.md honesty point from 112-CONTEXT (never imply leverage improves Sharpe).

    (4) Write the three behavior tests FIRST (in ScenarioComposer.test.tsx, extending the Plan-00 describe block), watch them fail, then implement.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism 2>&1 | tail -8 && git diff --exit-code src/lib/scenario.ts</automated>
  </verify>
  <acceptance_criteria>
    - The three new tests GREEN, with the concrete fixture-derived dollar assertion (0.3 × 100000 × 2) and the em-dash absence case asserted literally ("—").
    - Notional is text, not an input element (assert tagName/role in the test — the never-a-weight-input lock made structural).
    - Caveat renders exactly when a selected row is levered; absent at all-1×.
    - src/lib/scenario.ts diff clean.
  </acceptance_criteria>
  <done>Row anatomy complete per locked decision 1: {weight editable} × {leverage editable} → {notional derived read-only}; honesty caveat live; nulls dash, never zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| leverage input → client state → jsonb draft | untrusted numeric input persisting to scenarios.draft |
| persisted jsonb draft → hydration | hostile/stale leverage values on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-01 | Tampering | handleLeverageChange | mitigate | existing clamp [0, MAX_LEVERAGE] with visible message; non-finite rejected, prior value kept (reused verbatim — no new handler) |
| T-112-02 | Tampering | draft hydration (leverageOverrides) | mitigate | sanitizeLeverageMap on read; NEVER a zod .min/.max refine (refine failure → codec RESET → deletes the whole draft — locked decision 4); Wave-0 pin (b) is the guard |
| T-112-04 | Information integrity | notional display | mitigate | derived-only, read-only, em-dash on non-derivable (never a fabricated dollar figure an LP could act on) |
| T-112-SC | Tampering | package installs | n/a | no packages installed this phase |
</threat_model>

<verification>
- All Wave-0 RED tests now GREEN (composer (a)(c) + save (a)); plus this plan's three notional/caveat tests.
- `npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism` fully green (wave-merge sampling per 112-VALIDATION).
- `git diff --exit-code src/lib/scenario.ts` clean; scenario-backbone-gates green.
- <human-check>Honesty copy renders correctly in the browser (notional reads as derived/informative; invariance caveat legible) — deferred to /qa on the dev server per 112-VALIDATION Manual-Only table.</human-check>
</verification>

<success_criteria>
- ROADMAP 112 SC-2 TRUE: leverage per constituent row, blend re-derives from levered dailies, no symbol-keyed path.
- ROADMAP 112 SC-3 TRUE: leverage sanitized on read; a bad value can never delete the saved draft.
- Pitfall 1 closed with the Wave-0 RED-proven regression.
- Locked decision 1 columns shipped: weight × leverage → notional (read-only).
</success_criteria>

<output>
Create `.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-02-SUMMARY.md` when done.
</output>
