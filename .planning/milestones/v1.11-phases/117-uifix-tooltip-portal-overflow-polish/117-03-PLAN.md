---
phase: 117-uifix-tooltip-portal-overflow-polish
plan: 03
type: execute
wave: 2
depends_on: ["117-02"]
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx
autonomous: true
requirements: [UIFIX-03]
must_haves:
  truths:
    - "The factsheet CUM RETURN KPI value renders IN FULL for extreme/high-leverage magnitudes — no truncation, no ellipsis"
    - "The KPI value keeps font-mono tabular-nums text-h2 leading-none and the signTone color gate — type is never shrunk below the DESIGN.md text-h2 minimum"
    - "Sibling KPI cards (CAGR / Sharpe / Sortino / …) and the eyebrow labels stay visually identical — the label <p> KEEPS its pinned whitespace-nowrap/overflow-hidden/text-ellipsis bounded-label affordance"
    - "The axe route x viewport matrix re-runs green after the whole Phase-117 batch"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "KPI value <p> without the truncation trio (layout fix, not type shrink)"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx"
      provides: "RED-first extreme-value no-ellipsis assertion + label-clip guard"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "KPI items.map value cell"
      via: "single shared value <p> className (all cells uniform by construction)"
      pattern: "tabular-nums text-h2"
---

<objective>
UIFIX-03: the factsheet CUM RETURN KPI card must display extreme values (e.g. under high leverage) without truncation/ellipsis. The culprit is the KPI VALUE `<p>` at `FactsheetView.tsx:883-895` carrying `whitespace-nowrap overflow-hidden text-ellipsis`. Fix by layout (allow the value to wrap/fit), never by shrinking below the DESIGN.md `text-h2` minimum; keep sibling KPI cards and the eyebrow labels visually identical. Close the phase by re-running the axe route×viewport matrix green over the whole Phase-117 batch.

Purpose: high-leverage what-if projections produce long formatted percentages that currently ellipsize — a Numbers-Contract integrity defect (a truncated number reads as a different number).
Output: one surgical className change + RED-first regression test + green axe matrix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-uifix-tooltip-portal-overflow-polish/117-CONTEXT.md
@.planning/phases/117-uifix-tooltip-portal-overflow-polish/117-UI-SPEC.md (§UIFIX-03 contract)
@src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx
@src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx

<interfaces>
Verified current state (planning survey 2026-07-18):

- KPI strip cells render from ONE `items.map` (`FactsheetView.tsx:871`); `items[0]` is `{ label: "Cum. Return", value: pctSigned(m.cum_ret, 1), tone: signTone(m.cum_ret) }` (line 786). A className change on the value `<p>` therefore applies to ALL cells uniformly — sibling-uniformity holds by construction, but CAGR/Sharpe/Sortino must be verified visually unchanged (short values render identically with or without the trio).
- VALUE `<p>` (line 883-895, the fix target): `mt-1.5 sm:mt-2 font-mono tabular-nums text-h2 leading-none whitespace-nowrap overflow-hidden text-ellipsis` + inline signTone color. ONLY the truncation trio `whitespace-nowrap overflow-hidden text-ellipsis` changes.
- LABEL `<p>` (line 877-882): carries the same trio and MUST KEEP IT — `FactsheetView.kpistrip.test.tsx` behavior 2 PINS "the KPI LABEL keeps its text-ellipsis whitespace-nowrap bounded-label affordance (the AUDIT-classified legitimate clip on the KPI label <p> — not removed)". Touching the label breaks a pinned behavior. This is the sibling-guard trap.
- Panel wrapper `<section className="mt-6 overflow-hidden @container">` (line 863-869) — untouched; data-panel treatment (square, flat, 1px hairlines) preserved.
- Existing suites to keep green: `FactsheetView.kpistrip.test.tsx` (container-query columns + tabular-nums + label-clip pins), `FactsheetView.leverage.test.tsx` + `FactsheetView.leverage-honesty.test.tsx` (extreme-leverage path).
- axe harness: `e2e/axe-app-wide.spec.ts` — the Phase-48 route×viewport WCAG-AA matrix (three viewports incl. Desktop 1280×800 + mobile 375×812), covers the strategy-v2/factsheet route; run via `npx playwright test e2e/axe-app-wide.spec.ts` (`npm run test:e2e` scoped).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-first extreme-value no-ellipsis regression + label-clip and sibling guards</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx</files>
  <read_first>src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx (render harness + existing behavior pins — especially the label-clip pin), src/app/factsheet/[id]/v2/FactsheetView.tsx lines 780-900 (items construction + cell markup), 117-UI-SPEC.md §UIFIX-03</read_first>
  <behavior>
    New `describe("UIFIX-03: CUM RETURN extreme value renders untruncated")` in the kpistrip suite, reusing its FactsheetBody harness:
    - Test 1 (no-ellipsis, RED): build a payload whose `cum_ret` formats to a LONG extreme string (e.g. a metrics value yielding `+1,234,567.8%` through `pctSigned(m.cum_ret, 1)` — derive the input from the real formatter, don't hand-fabricate the rendered string). Locate the CUM RETURN VALUE `<p>` (the element whose textContent equals the full formatted value, in the cell labeled "Cum. Return"). Assert: (a) textContent equals the FULL formatted string (jsdom renders full text regardless of CSS — the load-bearing check is (b)); (b) the value `<p>` className contains NONE of `whitespace-nowrap`, `overflow-hidden`, `text-ellipsis` (the class trio IS the truncation mechanism; its absence is the checkable no-ellipsis contract in jsdom); (c) className still contains `font-mono`, `tabular-nums`, `text-h2`, `leading-none` (type never shrunk below the DESIGN.md minimum — no smaller `text-*` token introduced).
    - Test 2 (label guard, GREEN before and after): the "Cum. Return" LABEL `<p>` STILL contains all three of `whitespace-nowrap`, `overflow-hidden`, `text-ellipsis` — the pinned legitimate bounded-label clip is NOT removed (protects the existing kpistrip behavior-2 pin from collateral).
    - Test 3 (sibling uniformity + signTone): every cell value `<p>` in the strip shares the same className (uniform by items.map construction), and the signTone gate still colors a positive cum_ret `var(--color-positive)` / leaves a `—` value colorless (Numbers Contract unchanged).
  </behavior>
  <action>Write the three tests; run the file and confirm Test 1(b) fails by ASSERTION on the current tree (trio present) while Tests 2-3 pass (they are guards, valid before and after). Do not modify FactsheetView.tsx in this task.</action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx" --no-file-parallelism  # Test 1 RED by className assertion; guards + all pre-existing tests green</automated>
  </verify>
  <done>Test 1 RED by assertion; Tests 2-3 + every pre-existing kpistrip test green; extreme value derived through the real `pctSigned` formatter path.</done>
  <acceptance_criteria>
    - Test 1 asserts absence of ALL THREE truncation tokens on the VALUE `<p>` and presence of `font-mono tabular-nums text-h2 leading-none`.
    - Test 2 asserts presence of all three tokens on the LABEL `<p>` (pinned affordance preserved).
    - Test 3 asserts sign-tone behavior unchanged (positive → `var(--color-positive)`, `—` colorless).
    - No pre-existing kpistrip assertion is edited or weakened.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Layout fix — remove the truncation trio from the KPI VALUE cell only</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>src/app/factsheet/[id]/v2/FactsheetView.tsx lines 863-900 (panel wrapper + items.map + label/value <p>s), 117-UI-SPEC.md §UIFIX-03, .planning/phases/117-uifix-tooltip-portal-overflow-polish/117-PATTERNS.md §FactsheetView</read_first>
  <action>
    On the VALUE `<p>` (line 884) ONLY: remove `whitespace-nowrap overflow-hidden text-ellipsis` and add the minimal wrap/fit allowance an extreme value needs inside the grid cell — `break-words` (long unbroken numeric strings must be allowed to wrap within the cell; add `min-w-0` on the cell wrapper ONLY if the grid track otherwise refuses to shrink — smallest correct diff, implementer's call per CONTEXT discretion). Keep `mt-1.5 sm:mt-2 font-mono tabular-nums text-h2 leading-none` and the inline signTone style byte-identical. LABEL `<p>` (line 878) byte-untouched (pinned bounded-label affordance). Panel wrapper, grid classes, hairline borders, and the short-track caveat block byte-untouched. Do not shrink the type: no `text-*` token below `text-h2`, no `clamp` override, no font-size inline style.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx" "src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx" "src/app/factsheet/[id]/v2/FactsheetView.leverage-honesty.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>Task-1 Test 1 GREEN; label guard + sibling guard + all pre-existing kpistrip/leverage suites green; tsc clean.</done>
  <acceptance_criteria>
    - `git diff` on FactsheetView.tsx touches ONLY the value `<p>` className (and at most a `min-w-0` on its cell wrapper) — label `<p>`, panel wrapper, and grid markup byte-identical.
    - The value `<p>` retains `text-h2` (grep-checkable) and gains no smaller type token.
    - Full factsheet v2 directory suite green (run `npx vitest run "src/app/factsheet/[id]/v2/" --no-file-parallelism` as the closing sweep).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Phase-close acceptance — axe route×viewport matrix green over the whole 117 batch</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>e2e/axe-app-wide.spec.ts (header comments — matrix shape, seeding expectations), e2e/strategy-v2-axe.spec.ts</read_first>
  <action>
    Re-run the existing axe WCAG-AA matrix (UIFIX-03 acceptance; also sweeps the plan-01 tooltip and plan-02 ring changes since this is wave 2): `npx playwright test e2e/axe-app-wide.spec.ts e2e/strategy-v2-axe.spec.ts`. The harness manages its own server per playwright config — do not hand-roll a server. If a spec requires seeded fixtures/auth unavailable locally (per the spec's own header comments about seeded vs unseeded floors), run the unseeded/public floor that IS hermetic and record exactly which matrix cells ran — fail loud on any skip (Rule 12: "green" must name what ran). Any axe violation introduced by Phase-117 diffs is fixed at root in the owning file (className-level), never by adding an axe disable/exclusion.
  </action>
  <verify>
    <automated>npx playwright test e2e/axe-app-wide.spec.ts e2e/strategy-v2-axe.spec.ts</automated>
  </verify>
  <done>Axe matrix specs exit 0 (or the hermetic floor exits 0 with an explicit SUMMARY note naming any environment-gated cells that could not run locally and why); zero new axe exclusions in any spec file.</done>
  <acceptance_criteria>
    - Playwright exit code 0 on the run; no `git diff` in `e2e/` (no exclusions/disables added).
    - SUMMARY records the exact matrix cells executed (route × viewport) and any environment-gated skips by name — no silent skips.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | className-only change to a rendered formatted number; no data flow or input surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-03 | Repudiation (data integrity display) | KPI value truncation | mitigate | this IS the fix — an ellipsized number misrepresents the metric; full-value render restores Numbers-Contract honesty |
| T-117-SC | Tampering | npm installs | accept | zero installs this phase |
</threat_model>

<verification>
- Task-1 RED → Task-2 GREEN on the no-ellipsis assertion; label-clip pin + sibling uniformity + signTone guards green throughout.
- Full `src/app/factsheet/[id]/v2/` suite green under `--no-file-parallelism`; tsc + lint 0 errors.
- Axe route×viewport matrix green (Task 3), covering the full Phase-117 batch.
</verification>

<success_criteria>
- UIFIX-03 truths hold: extreme CUM RETURN value renders in full at `text-h2`, siblings + labels visually identical, axe matrix green.
- Zero new tokens; type never shrunk below the DESIGN.md minimum; label bounded-clip affordance preserved.
</success_criteria>

<output>
Create `.planning/phases/117-uifix-tooltip-portal-overflow-polish/117-03-SUMMARY.md` when done.
</output>
