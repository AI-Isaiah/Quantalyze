---
phase: 117-uifix-tooltip-portal-overflow-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/HeatmapPanels.tsx
  - src/app/factsheet/[id]/v2/DistributionPanels.tsx
  - src/app/factsheet/[id]/v2/focus-ring-clipproof.test.tsx
  - src/components/ResponsiveTable.tsx
  - src/components/ResponsiveTable.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.test.tsx
  - src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.tsx
  - src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.test.tsx
  - src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx
autonomous: true
requirements: [UIFIX-02]
must_haves:
  truths:
    - "Every focusable control inside an overflow-x-auto (or overflow-hidden-ancestor) container at the enumerated sites carries a CLIP-PROOF focus-visible indicator (drawn inside the element bounds), >=2px, accent-colored at >=3:1 contrast"
    - "No enumerated site relies on a positive-offset outline (draws OUTSIDE the element -> still clipped by ancestor overflow) or on the browser default outline alone"
    - "The shared ResponsiveTable focusable scroll region shows an unclipped focus ring, covering MetricsColumn worst-drawdowns + StressWindowsPanel + all other consumers centrally"
  artifacts:
    - path: "src/components/ResponsiveTable.tsx"
      provides: "clip-proof focus-visible ring on the shared tabIndex=0 scroll region"
      contains: "ring-inset"
    - path: "src/app/factsheet/[id]/v2/focus-ring-clipproof.test.tsx"
      provides: "RED-first ring assertions for the factsheet section nav + heatmap/distribution scroll regions"
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      provides: "clip-proof focus idiom on TAB_BUTTON_ACTIVE/INACTIVE"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "section-nav anchors"
      via: "clip-proof focus-visible tokens replacing the positive-offset outline"
      pattern: "focus-visible:ring-inset"
    - from: "src/components/ResponsiveTable.tsx"
      to: "MetricsColumn/StressWindowsPanel reflow suites"
      via: "shared region className"
      pattern: "overflow-x-auto"
---

<objective>
UIFIX-02: make focus-visible indicators render FULLY (WCAG 2.4.7) for every focusable control living inside an `overflow-x-auto` container (or a focusable scroll region inside an `overflow-hidden` panel) at the enumerated Phase-117 sites — by switching to the in-repo CLIP-PROOF idiom (inset ring, drawn inside the element bounds) and never a positive-offset outline. Zero new tokens; reuse `--color-accent`.

Purpose: today the affected controls use `focus-visible:outline-*` with 0/positive offset (outline draws OUTSIDE the element → clipped at the scroll-container edge) or rely on the browser default outline (same clip), failing WCAG 2.4.7 focus-visible.
Output: uniform clip-proof focus idiom across the enumerated files + RED-first rendered-className regression assertions per fixed site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/117-uifix-tooltip-portal-overflow-polish/117-CONTEXT.md
@.planning/phases/117-uifix-tooltip-portal-overflow-polish/117-PATTERNS.md (§Shared Patterns — the LOAD-BEARING clip-safety note)
@.planning/phases/117-uifix-tooltip-portal-overflow-polish/117-UI-SPEC.md (§UIFIX-02 contract)
@src/components/ResponsiveTable.tsx
@src/app/(dashboard)/allocations/MandateSegmentedRadio.tsx (line 56 — the ring-inset precedent)

<interfaces>
Verified current state of every enumerated site (surveyed 2026-07-18 during planning):

1. `FactsheetView.tsx:1025` — section nav `<nav aria-label="Factsheet sections" className="… overflow-x-auto">`; anchors at ~:1037 carry `focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-accent` → POSITIVE offset = clipped (the trap). Query in tests via `aria-label="Factsheet sections"` → `within(...).getAllByRole("link")`.
2. `AllocationsTabs.tsx:803` — tablist `overflow-x-auto sm:overflow-x-visible`; tab buttons use consts `TAB_BUTTON_ACTIVE`/`TAB_BUTTON_INACTIVE` (lines 329-332) ending in `focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent` (offset 0 — outline still draws OUTSIDE the border edge → clipped at the strip edge on mobile; buttons also hang `-mb-[10px]` below the container). NOTE: the consts carry a "dashboard-parity / byte-identical" comment, but NO test pins the literal strings (verified in planning: no `focus-visible` or `-mb-[10px]` assertions in AllocationsTabs.test.tsx / MandateTabPanel.test.tsx / InsightStrip.test.tsx, and no non-test sibling shares the string). Edit the consts in place; update the stale byte-identical comment honestly.
3. `ResponsiveTable.tsx:56-64` — shared `role="region" tabIndex={0}` scroll wrapper (`overflow-x-auto`), NO explicit focus-visible classes → relies on the browser default outline, which ancestor `overflow-hidden` panels clip. Central fix point for MetricsColumn worst-drawdowns + StressWindowsPanel (their reflow tests assert this component's region) + every other ResponsiveTable consumer.
4. `HeatmapPanels.tsx:65-69` (monthly table region) and `:198-203` (daily calendar region) — inline copies of the ResponsiveTable idiom (`overflow-x-auto` + `role="region"` + `tabIndex={0}`, distinct aria-labels), no explicit focus ring; both sit inside `overflow-hidden` factsheet panels.
5. `DistributionPanels.tsx:497-501` — correlation-matrix scroll region, same inline idiom, no explicit ring.
6. `ScenarioFlaggedHoldingsList.tsx:200` container `overflow-x-auto`; expand/collapse `<button>` at :224-234 (aria-label "Expand review"/"Collapse review") has NO focus-visible class (browser default outline → clipped at scroll edges).
7. `ExposureByClass.tsx:142` — `overflow-x-auto` (+ conditional `overflow-y-auto`) but planning grep found NO focusable child (no button/a/tabIndex inside) → AUDIT-ONLY site.

The clip-proof idiom (Rule 7 — pick one, surface the other): Convention B `ring-inset` (precedent `MandateSegmentedRadio.tsx:56` — `focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-accent/20`) draws the indicator INSIDE the element = immune to ancestor overflow clipping. DEVIATION FROM THE PRECEDENT, on purpose: use FULL-OPACITY `focus-visible:ring-accent` (already used at 42 sites), NOT `ring-accent/20` — a 20%-opacity accent fails the ≥3:1 non-text-contrast requirement (WCAG 1.4.11) the UI-SPEC binds this fix to. Technique locked for test determinism: `focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-accent` at every fixed site. Convention A (positive-offset outline) remains valid OUTSIDE overflow contexts and is NOT touched elsewhere.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED-first ring assertions per site (rendered-className, clip-proof token present / clipped idiom absent)</name>
  <files>src/components/ResponsiveTable.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.test.tsx, src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.test.tsx, src/app/factsheet/[id]/v2/focus-ring-clipproof.test.tsx</files>
  <read_first>src/components/ResponsiveTable.test.tsx, src/app/factsheet/[id]/v2/MetricsColumn.worst-drawdowns-reflow.test.tsx + src/app/factsheet/[id]/v2/StressWindowsPanel.reflow.test.tsx (the reflow-assertion style to extend), src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx (the FactsheetBody render harness + localStorage/sentry stub idiom to reuse), src/app/(dashboard)/allocations/AllocationsTabs.test.tsx, src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.test.tsx, 117-PATTERNS.md §Shared Patterns</read_first>
  <behavior>
    Each fixed site gets a rendered-DOM className assertion that FAILS on the current tree (RED) and encodes WHY (test name/comment cites WCAG 2.4.7 + "positive-offset/default outline draws OUTSIDE the element → clipped by ancestor overflow; inset ring draws inside → clip-proof"):
    - ResponsiveTable.test.tsx (extend): the `role="region"` `tabIndex={0}` wrapper className contains `focus-visible:ring-2`, `focus-visible:ring-inset`, `focus-visible:ring-accent` and does NOT contain `ring-accent/20`.
    - AllocationsTabs.test.tsx (extend): a rendered tab button's className contains the three ring tokens and does NOT contain `focus-visible:outline` (the clipped idiom being replaced on these buttons).
    - ScenarioFlaggedHoldingsList.test.tsx (extend): the expand/collapse button (query via aria-label "Expand review") className contains the three ring tokens.
    - NEW colocated `src/app/factsheet/[id]/v2/focus-ring-clipproof.test.tsx` with three describes: (a) factsheet section-nav — render via the kpistrip-test FactsheetBody harness idiom, query `aria-label="Factsheet sections"` → each link's className contains the three ring tokens and does NOT contain `outline-offset-1`; (b) HeatmapPanels — render standalone with fixture props, both `role="region"` elements (query by their aria-labels) carry the three ring tokens; (c) DistributionPanels — same for the correlation-matrix region.
  </behavior>
  <action>Write all assertions; run each touched suite and confirm every new assertion fails by ASSERTION (className mismatch), never by crash/import error. Do not modify any component file in this task. FactsheetView.kpistrip.test.tsx itself is NOT edited (its harness is copied, keeping that file free for plan 117-03).</action>
  <verify>
    <automated>npx vitest run src/components/ResponsiveTable.test.tsx "src/app/(dashboard)/allocations/AllocationsTabs.test.tsx" "src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.test.tsx" "src/app/factsheet/[id]/v2/focus-ring-clipproof.test.tsx" --no-file-parallelism  # new assertions RED by className mismatch; pre-existing tests green</automated>
  </verify>
  <done>Every fix site (1-6 in interfaces) has a failing-without-fix rendered-className assertion; ExposureByClass (site 7) intentionally has none pending the Task-2 audit.</done>
  <acceptance_criteria>
    - Assertions check the LITERAL tokens `focus-visible:ring-2`, `focus-visible:ring-inset`, `focus-visible:ring-accent` on the rendered element's className.
    - Negative assertions pin the trap: `outline-offset-1` absent from the factsheet nav anchors; `focus-visible:outline` absent from tab buttons; `ring-accent/20` absent from every newly-ringed element (WCAG 1.4.11 ≥3:1).
    - Test names/comments cite WCAG 2.4.7 and the outside-vs-inside paint mechanics (Rule 9: intent, not just behavior).
    - All new tests RED by assertion on the unfixed tree; every pre-existing test in the three extended files stays green.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply the clip-proof idiom at all six fix sites + honest audit of ExposureByClass</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/HeatmapPanels.tsx, src/app/factsheet/[id]/v2/DistributionPanels.tsx, src/components/ResponsiveTable.tsx, src/app/(dashboard)/allocations/AllocationsTabs.tsx, src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.tsx, src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx</files>
  <read_first>Each file's target lines per the interfaces block (FactsheetView.tsx:~1025-1050, AllocationsTabs.tsx:320-335 + 790-835, ResponsiveTable.tsx:50-67, HeatmapPanels.tsx:60-75 + 195-210, DistributionPanels.tsx:495-505, ScenarioFlaggedHoldingsList.tsx:200 + 218-236, ExposureByClass.tsx:130-160), src/app/(dashboard)/allocations/MandateSegmentedRadio.tsx:56 (precedent)</read_first>
  <action>
    Surgical className edits only (Rule 3 — no adjacent refactors, no markup changes):
    1. FactsheetView.tsx section-nav anchors (~:1037): replace `focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-accent` with `focus-visible:ring-2 focus-visible:ring-inset focus-visible:ring-accent` (the positive-offset outline is the trap — it draws OUTSIDE and stays clipped). Keep `rounded-sm`, pointer-coarse tokens, active/inactive branches untouched. Do NOT touch the file's other outline sites (they are not inside overflow containers).
    2. AllocationsTabs.tsx TAB_BUTTON_ACTIVE + TAB_BUTTON_INACTIVE (329-332): replace the trailing `focus-visible:outline focus-visible:outline-2 focus-visible:outline-accent` with the same three ring tokens in BOTH consts; update the now-stale "byte-identical to the previous inlined versions" comment (lines ~324-328) to record the Phase-117 focus-idiom change honestly (no test pins the literal strings — verified in planning).
    3. ResponsiveTable.tsx region div (~:59): append the three ring tokens to the base class list (currently `["overflow-x-auto", className]`) — central fix for MetricsColumn worst-drawdowns, StressWindowsPanel, and all other consumers; the region previously relied on the browser default outline (clipped by ancestor overflow-hidden panels).
    4. HeatmapPanels.tsx both regions (:65, :198) and DistributionPanels.tsx region (:497): append the same three ring tokens to each `role="region" tabIndex={0}` element's className.
    5. ScenarioFlaggedHoldingsList.tsx expand button (:231 className): append the three ring tokens (button currently has NO focus-visible indicator).
    6. ExposureByClass.tsx (:142 container): AUDIT — confirm by reading the full rendered subtree that no focusable element (button, a[href], [tabIndex], input, select) exists inside the overflow container. If confirmed: make NO change and record the finding + evidence in the SUMMARY (WCAG 2.4.7 applies to focusable UI; adding speculative classes violates Rule 2). If a focusable element IS found: apply the same three ring tokens to it and add the matching RED-first assertion to its suite.
    Zero new tokens anywhere: `ring-accent` (42 existing sites) + Tailwind's stock `ring-2`/`ring-inset` utilities only. Do NOT use `ring-accent/20` (fails ≥3:1).
  </action>
  <verify>
    <automated>npx vitest run src/components/ResponsiveTable.test.tsx "src/app/(dashboard)/allocations/AllocationsTabs.test.tsx" "src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.test.tsx" "src/app/factsheet/[id]/v2/focus-ring-clipproof.test.tsx" "src/app/factsheet/[id]/v2/MetricsColumn.worst-drawdowns-reflow.test.tsx" "src/app/factsheet/[id]/v2/StressWindowsPanel.reflow.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>All Task-1 assertions GREEN; both reflow suites still green (region keeps `overflow-x-auto` + role/tabIndex contract); tsc clean; ExposureByClass audit outcome recorded.</done>
  <acceptance_criteria>
    - `focus-visible:ring-inset` appears at every fixed site: FactsheetView nav anchors (1), TAB_BUTTON consts (2), ResponsiveTable region (1), HeatmapPanels regions (2), DistributionPanels region (1), ScenarioFlaggedHoldingsList expand button (1) — minus any recorded audit-exempt finding.
    - The section-nav anchor line in FactsheetView.tsx no longer contains `outline-offset-1`; unrelated outline sites in the file are byte-untouched (surgical scope, verify via diff hunks).
    - No occurrence of `ring-accent/20` in any Phase-117 diff hunk.
    - `git diff` for this task touches ONLY className strings + the one stale comment — no JSX structure, handler, aria, or role changes.
    - Factsheet nav + tabs keep their existing hover/active treatments byte-identical (only the focus-visible tokens change).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Cross-suite regression sweep for the touched surfaces</name>
  <files>src/components/ResponsiveTable.test.tsx</files>
  <read_first>117-CONTEXT.md §Reusable Assets (test inventory)</read_first>
  <action>Run the full colocated suites of every touched surface (whole factsheet v2 directory, AllocationsTabs + ScenarioFlaggedHoldingsList + MandateTabPanel suites, ResponsiveTable) with `--no-file-parallelism`, plus `npm run lint` (react-hooks ERRORS surface here, not in tsc — the lone PRE-EXISTING `react-hooks` warning in EquityChart.tsx is out of scope and must remain the only warning). Fix any collateral test regression at root (e.g. a class-order-sensitive assertion) — never by loosening the new ring assertions.</action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/" "src/app/(dashboard)/allocations/AllocationsTabs.test.tsx" "src/app/(dashboard)/allocations/MandateTabPanel.test.tsx" "src/app/(dashboard)/allocations/ScenarioFlaggedHoldingsList.test.tsx" src/components/ResponsiveTable.test.tsx --no-file-parallelism && npm run lint</automated>
  </verify>
  <done>All listed suites green; lint 0 errors with only the pre-existing EquityChart.tsx warning.</done>
  <acceptance_criteria>
    - Factsheet v2 directory suite green (includes kpistrip/leverage/reflow files — proves no visual-contract collateral).
    - Allocations touched suites + MandateTabPanel (dashboard-parity neighbor) green.
    - `npm run lint` exit 0 with no NEW warnings.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | className-only presentational changes; no data flow, input handling, or privilege surface touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-117-02 | Information Disclosure | focus indicator visibility | mitigate | this IS the fix — an invisible/clipped focus indicator is an a11y integrity defect (WCAG 2.4.7); inset accent ring ≥2px ≥3:1 restores it |
| T-117-SC | Tampering | npm installs | accept | zero installs this phase; Tailwind stock utilities only |
</threat_model>

<verification>
- Every fixed site's ring assertion RED-before/GREEN-after (Task 1 → Task 2).
- Reflow suites (MetricsColumn worst-drawdowns, StressWindowsPanel) green — the region contract (`overflow-x-auto`, role, tabIndex) unchanged.
- `npx tsc --noEmit` 0 errors; `npm run lint` 0 errors.
- No `ring-accent/20`, no positive-offset outline inside any overflow container at the enumerated sites.
</verification>

<success_criteria>
- UIFIX-02 truth holds at all seven enumerated sites: six fixed with the inset accent ring, ExposureByClass honestly audited (fix-or-record).
- Zero new design tokens; accent reserved-use preserved (focus indication only).
- Both PATTERNS traps respected: no positive-offset outline used as the "fix"; full-opacity accent (not /20) for WCAG 1.4.11 contrast.
</success_criteria>

<output>
Create `.planning/phases/117-uifix-tooltip-portal-overflow-polish/117-02-SUMMARY.md` when done.
</output>
