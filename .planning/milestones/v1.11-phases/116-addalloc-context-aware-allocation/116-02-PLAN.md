---
phase: 116-addalloc-context-aware-allocation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/OptimizerPanel.tsx
  - src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx
autonomous: true
requirements: [ADDALLOC-04]

must_haves:
  truths:
    - "An allocator with zero portfolios sees the Simulate-Impact/Optimizer card explain WHY it is inert and offer one clickable remedy: Connect Exchange → /profile?tab=exchanges"
    - "No allocator-facing link to the manager-only /portfolios route remains in OptimizerPanel (the ROLE-02 redirect()-bounce dead-end is gone)"
    - "The remedy keeps the secondary (non-accent) button treatment so it does not compete with the primary header CTA"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/OptimizerPanel.tsx"
      provides: "Honest zero-portfolio remedy deep-linking the canonical allocator connect-exchange route"
      contains: "/profile?tab=exchanges"
    - path: "src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx"
      provides: "Rule-9 intent test pinning the new href + honest copy (rewritten from the stale /portfolios pin)"
      contains: "Connect Exchange"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/OptimizerPanel.tsx"
      to: "/profile?tab=exchanges"
      via: "next/link href in the 0-portfolio gate"
      pattern: "profile\\?tab=exchanges"
---

<objective>
Fix the zero-portfolio Simulate-Impact dead-end (ADDALLOC-04): the 0-portfolio gate in OptimizerPanel currently links `href="/portfolios"` ("Create portfolio →") — a manager-only route an allocator gets server-side redirect()-bounced from (ROLE-02). Replace it with the canonical allocator connect-exchange deep-link + honest copy naming why the affordance is inert, and rewrite the test that pins the stale href in lockstep.

Purpose: no disabled dead-ends — a blocked affordance must offer a clickable remedy (CONTEXT.md §Simulate-Impact remedy; generalizes ROLE-06).
Output: rewired 0-portfolio card + intent-preserving regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-addalloc-context-aware-allocation/116-CONTEXT.md
@.planning/phases/116-addalloc-context-aware-allocation/116-UI-SPEC.md

<interfaces>
From src/app/(dashboard)/allocations/components/OptimizerPanel.tsx (:94-111) — the 0-portfolio gate: a Card with the shared "Diversification Optimizer" heading const (:83-92), a body paragraph, and the Link at :103-108 with the secondary treatment classes `mt-4 inline-flex items-center justify-center rounded-lg border border-border bg-white px-3 py-1.5 text-caption font-medium text-text-primary transition-colors hover:bg-page`.

Canonical route precedent (do not invent): `/profile?tab=exchanges` is already used at ScenarioComposer.tsx:3604, OnboardingBanner.tsx:77, EmptyState.tsx:71,89.

From src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx (:89-111) — the 0-portfolio gate test: pins the old body copy (:102-106) and `href="/portfolios"` (:107-108), plus the "no optimizer mount" assertion (:110) which must be KEPT.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap the /portfolios dead-end for the connect-exchange remedy + lockstep intent test</name>
  <files>src/app/(dashboard)/allocations/components/OptimizerPanel.tsx, src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/OptimizerPanel.tsx — :80-120 (heading const, 0-portfolio gate, non-empty branch untouched)
    - src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx — whole file (183 lines; the 0-portfolio test to rewrite + the other tests that must stay green)
    - .planning/phases/116-addalloc-context-aware-allocation/116-UI-SPEC.md — §Copywriting Contract (locked verbatim copy) and §Color (remedy is intentionally NON-accent)
  </read_first>
  <behavior>
    - Test 1 (rewrite of the existing 0-portfolio test, Rule-9 intent-preserving): with portfolios: [] the card renders the heading text "Simulate Impact needs a live portfolio" and the body text "Connect a read-only exchange API key to build your allocation, then Simulate Impact models new strategies against it." verbatim; getByRole link with accessible name matching /Connect Exchange/ has href "/profile?tab=exchanges"; queryByRole link /Create portfolio/ is null; the link's className contains "border-border" and does NOT contain "bg-accent" (secondary treatment pinned — the remedy must not compete with the primary CTA); the existing "no optimizer mount" assertion (queryByText /Run Optimizer/ null) is kept.
  </behavior>
  <action>
    Rewrite the 0-portfolio test first (RED against the current tree — it fails by assertion because the old link/copy still render). Then edit ONLY the 0-portfolio gate branch of OptimizerPanel.tsx (:95-111), per UI-SPEC §Copywriting Contract verbatim:
    - Keep the shared "Diversification Optimizer" heading const untouched (it also serves the non-empty branch).
    - Replace the current body paragraph with two lines using only existing tokens: the remedy heading "Simulate Impact needs a live portfolio" (text-small font-medium text-text-primary, mt-3) and the body "Connect a read-only exchange API key to build your allocation, then Simulate Impact models new strategies against it." (text-small text-text-secondary, mt-1). Neutral fact voice — no semantic red/amber, no cheerful empty state (DESIGN.md semantic-color gate: a zero is not bad).
    - Change the Link to href="/profile?tab=exchanges" with label "Connect Exchange →", keeping the existing secondary className string byte-identical (border border-border bg-white … — deliberately NOT bg-accent, per UI-SPEC §Color).
    - Do not touch the non-empty branch, the footer disclaimer, the glossary, or any other test.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - The rewritten test was RED (assertion mismatch) before the OptimizerPanel.tsx edit and is GREEN after; all other OptimizerPanel tests pass unmodified.
    - `grep -c 'href="/portfolios"' "src/app/(dashboard)/allocations/components/OptimizerPanel.tsx"` returns 0; `grep -c "profile?tab=exchanges" "src/app/(dashboard)/allocations/components/OptimizerPanel.tsx"` returns 1.
    - The three locked strings render verbatim: "Simulate Impact needs a live portfolio", "Connect a read-only exchange API key to build your allocation, then Simulate Impact models new strategies against it.", "Connect Exchange →".
    - The remedy link className is the pre-existing secondary string (contains "border-border" and "bg-white", not "bg-accent").
    - npx tsc --noEmit exits 0.
  </acceptance_criteria>
  <done>The zero-portfolio Simulate-Impact card names why it is inert and deep-links the real allocator onboarding path; no /portfolios link remains in OptimizerPanel; the lockstep intent test pins href + copy + secondary treatment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /profile?tab=exchanges | Existing authenticated allocator surface; the link swap grants nothing new. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | Tampering | 0-portfolio remedy link | accept | Static in-app href to a pre-existing guarded route (same target as EmptyState/OnboardingBanner); no user input flows into the URL. |
| T-116-SC | Tampering | package installs | accept | Zero package installs in this phase. |
</threat_model>

<verification>
- npx vitest run "src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx" --no-file-parallelism → all green.
- grep gate: no `href="/portfolios"` in OptimizerPanel.tsx; exactly one `/profile?tab=exchanges`.
- tsc 0 errors; npm run lint 0 errors.
</verification>

<success_criteria>
- ADDALLOC-04: the zero-portfolio Simulate-Impact disabled affordance deep-links the real allocator onboarding path (connect exchange) with honest copy — the manager-only /portfolios dead-end is gone, proven by a regression test that fails without the fix.
</success_criteria>

<output>
Create `.planning/phases/116-addalloc-context-aware-allocation/116-02-SUMMARY.md` when done.
</output>
