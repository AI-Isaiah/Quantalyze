---
phase: 116-addalloc-context-aware-allocation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [ADDALLOC-01, ADDALLOC-02, ADDALLOC-03]

must_haves:
  truths:
    - "On Holdings or Overview, the header button reads '+ Allocation' and clicking it opens the ContributionWizardOverlay (API-key or CSV onboarding) inline — no navigation, no tab change"
    - "On Scenario, the header button reads '+ Strategy' and clicking it opens the StrategyBrowseDrawer (others' published + own private contributions, with the 'Can't find it? Add your own' upload escape hatch)"
    - "The button's aria-label states the real per-tab action — the stale 'Add allocation — open Scenario tab' mislabel is gone repo-wide"
    - "Closing a header-triggered overlay returns focus to the header button (keyboard users are not dropped at document.body)"
    - "The button never silently no-ops on any tab and never opens a dropdown/menu"
  artifacts:
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      provides: "Per-tab dispatch on activeTab + tab-level ContributionWizardOverlay host + focus-return-to-trigger"
      contains: "ContributionWizardOverlay"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "onRegisterOpenBrowse / onBrowseClosed optional props (additive, mirrors onRegisterOpen seam)"
      contains: "onRegisterOpenBrowse"
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx"
      provides: "Wiring tests for the per-tab dispatch, overlay open, and focus return"
      min_lines: 60
  key_links:
    - from: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      to: "ContributionWizardOverlay"
      via: "tab-level contributeOpen state + isOpen prop"
      pattern: "ContributionWizardOverlay"
    - from: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      to: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      via: "onRegisterOpenBrowse registration callback threaded through ScenarioTabContent"
      pattern: "onRegisterOpenBrowse"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "StrategyBrowseDrawer"
      via: "registered open function calls setBrowseOpen(true)"
      pattern: "setBrowseOpen\\(true\\)"
---

<objective>
Make the header "+ Allocation" button context-aware per the founder's locked decisions (CONTEXT.md §Implementation Decisions): on Scenario it becomes "+ Strategy" and opens the strategy picker (ADDALLOC-01); on Holdings/Overview it stays "+ Allocation" and opens the real-data onboarding wizard inline (ADDALLOC-02); on every tab the action is deterministic — never a silent no-op, never a dropdown (ADDALLOC-03). The corrected aria-label matches the real action.

Purpose: today the button is hard-wired `onClick={() => changeTab("scenario")}` — a wrong action on Holdings/Overview and a silent no-op ON the Scenario tab (the exact bug the founder hit in live dogfood 2026-07-18).
Output: rewired AllocationsTabs.tsx + additive registration props on ScenarioComposer.tsx + wiring tests that fail without each seam.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/116-addalloc-context-aware-allocation/116-CONTEXT.md
@.planning/phases/116-addalloc-context-aware-allocation/116-PATTERNS.md
@.planning/phases/116-addalloc-context-aware-allocation/116-UI-SPEC.md

This is a REWIRE — no new component, no new tokens, no new overlay primitive (UI-SPEC lock). Both overlay targets already exist and are trigger-agnostic:
- `ContributionWizardOverlay` (props: `isOpen`, `onClose`, `onSuccess?(strategyId)`) — portal + role="dialog", pulls focus IN on open (panelRef.focus at :72), Esc + backdrop dismiss built in. It does NOT restore trigger focus — that is the host's job (PATTERNS §Focus-return).
- `StrategyBrowseDrawer` (props: `isOpen`, `onClose`, `onAdd`, `onAddOwn?`, `allocatorMandate`, `fetchStrategies?`) — mounted TWICE inside ScenarioComposer (empty-state return :3618-3644 and main return :4872-4898), both driven by the composer-local `browseOpen` state (:847).

<interfaces>
From src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx (:33-38):
- `ContributionWizardOverlayProps = { isOpen: boolean; onClose: () => void; onSuccess?: (strategyId: string) => void }`

From src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:
- `ScenarioComposerProps` (:327-344) — existing optional registration seam to mirror: `onRegisterOpen?: (open: (row: SavedScenarioRow) => void) => void` (:338), ref-stabilized registration effect at :1771-1775.
- `browseOpen`/`setBrowseOpen` useState at :847; `contributeOpen` at :853.
- Both StrategyBrowseDrawer mounts use `onClose={() => setBrowseOpen(false)}` (:3620, :4874) and `onAddOwn={() => { setBrowseOpen(false); setContributeOpen(true); }}` (:3629-3632, :4883-4886).

From src/app/(dashboard)/allocations/AllocationsTabs.tsx:
- `activeTab: TabKey = parseTab(searchParams.get("tab"))` (:382) — URL-derived each render, no local state.
- `changeTab(key)` (:462-468) — router.replace, scroll:false.
- Header button (:759-766) with the D-20 comment (:756-758) that must be rewritten (it documents the old routes-to-scenario behavior).
- `ScenarioTabContent(props)` (:878-1002) — in-file sub-component; renders `<ScenarioComposer payload={props} … onRegisterOpen={handleRegisterOpen} onScenarioSaved={handleMutated}/>` (:977-983). The registration-callback threading for the new prop goes through here.
- `isUiV2` gate (:838-849): `{activeTab === "scenario" && (isUiV2 ? <ScenarioTabContent/> : <ScenarioStub/>)}` — ScenarioComposer is dynamic-imported (:120-146) and scenario-tab-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Context-aware header button + tab-level ContributionWizardOverlay host (Holdings/Overview branch)</name>
  <files>src/app/(dashboard)/allocations/AllocationsTabs.tsx, src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx — :756-766 (the button + stale D-20 comment), :362-382 (props + activeTab derivation), :462-468 (changeTab), :838-864 (scenario gate + tab-agnostic mount zone near TweaksToggle), :878-1002 (ScenarioTabContent)
    - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx — whole file (183 lines; the props contract + focus/Esc posture you must NOT duplicate or modify)
    - src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx — :1-130 (harness: next/navigation mocks, lsStore, panel stubs) and :478-492 (T_AT9, pins the OLD behavior — rewrite it)
    - .planning/phases/116-addalloc-context-aware-allocation/116-UI-SPEC.md — §Context-Aware Label (dispatch table), §Focus/Keyboard/A11y Contract
  </read_first>
  <behavior>
    - Test 1 (overview, default tab): the header button's visible label is "+ Allocation" and its accessible name is "Add allocation — connect an exchange or upload a CSV"; clicking it renders the (mocked) ContributionWizardOverlay stub with isOpen=true, and router.replace is NEVER called with a "tab=scenario" URL (no navigation).
    - Test 2 (?tab=holdings): same branch — label "+ Allocation", click opens the overlay stub.
    - Test 3 (?tab=scenario): visible label is "+ Strategy", accessible name is "Add strategy — open the strategy picker"; the wizard overlay stub does NOT open on click (the browse dispatch is Task 2's seam — here assert only label/aria + no wizard).
    - Test 4 (focus return): with the overlay open on overview, invoking the stub's onClose returns document.activeElement to the header button.
    - Test 5 (onSuccess): invoking the stub's onSuccess closes the overlay, returns focus to the header button, and calls router.refresh exactly once.
    - Test 6 (T_AT9 rewrite, in AllocationsTabs.scenario-composer.test.tsx): the old query getByRole button name /Add allocation — open Scenario tab/ finds NOTHING; the new accessible name "Add allocation — connect an exchange or upload a CSV" is present on the default (overview) tab. Do NOT click in this file (its harness does not mock ContributionWizardOverlay; clicking would mount the real WizardClient).
  </behavior>
  <action>
    Create src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx by copying the harness idiom from AllocationsTabs.scenario-composer.test.tsx (:22-130): the next/navigation vi.mock trio (useSearchParams/useRouter/usePathname), the lsStore localStorage mock, the panel marker stubs, and STUB_PROPS. Additionally vi.mock "./components/ContributionWizardOverlay" to a lightweight marker stub: renders a div with data-testid "contribution-overlay-stub" only when isOpen, plus two buttons that invoke the received onClose and onSuccess("test-id") props — this tests the WIRING without pulling WizardClient into jsdom. Also vi.mock "./components/ScenarioComposer" to a minimal stub (Task 2 extends it). Write the behavior tests above RED first. Local runs of touched allocations tests use --no-file-parallelism (known flake under parallelism).

    Then rewire AllocationsTabs.tsx:
    1. Add a `const addButtonRef = useRef<HTMLButtonElement>(null)` and a `const [contributeOpen, setContributeOpen] = useState(false)` at the AllocationsTabs component level (NOT inside ScenarioTabContent). This is an ADDITIVE tab-level host per PATTERNS §Wiring Seam — the composer's own internal contributeOpen/Browse→"Add your own" handoff stays byte-untouched (deliberate deviation from the literal "lift OUT of" wording, for Rule-3 surgical scope; closed overlays render null so the UI-SPEC single-modal contract holds).
    2. Replace the button (:759-766): attach ref={addButtonRef}; derive label, aria-label, and onClick from activeTab per the UI-SPEC dispatch table — activeTab === "scenario" → label "+ Strategy", aria-label "Add strategy — open the strategy picker", action = the Task-2 browse dispatch (for THIS task, stub it as a named handler `handleHeaderAdd` whose scenario branch is a TODO wired in Task 2); every other tab → label "+ Allocation", aria-label "Add allocation — connect an exchange or upload a CSV", action = setContributeOpen(true). Note the binary dispatch intentionally generalizes the UI-SPEC 3-row table to outcomes/mandate/risk (they take the Holdings/Overview branch) so ADDALLOC-03's never-a-silent-no-op holds on EVERY tab — document this in the replacement comment. Keep the existing className verbatim (bg-accent primary treatment, focus-visible outline — UI-SPEC: zero token changes). Rewrite the stale D-20 comment (:756-758) — it must no longer claim the button routes to the Scenario tab, and the literal string "open Scenario tab" must not survive anywhere in the file.
    3. Mount `<ContributionWizardOverlay isOpen={contributeOpen} onClose={handleContributeClose} onSuccess={handleContributeSuccess}/>` unconditionally in the tab-agnostic zone next to TweaksToggle/Tweaks (:851-856) so it is reachable on every tab. handleContributeClose = set contributeOpen false + addButtonRef.current?.focus() (UI-SPEC §Close → focus return; the overlay pulls focus IN itself but never restores the trigger). handleContributeSuccess = same close + focus, plus one router.refresh() so a freshly-connected key/CSV reflects in the SSR payload without waiting for the 30s overview poll (discretion; wrap in the same try/catch-free direct call the codebase uses for user-initiated refresh — the guarded interval pattern at :445-458 is for background polling only). Import ContributionWizardOverlay statically (it renders null when closed; its heavy WizardClient content only mounts when open).
    4. In AllocationsTabs.scenario-composer.test.tsx, rewrite T_AT9 (:478-492) per behavior Test 6 — intent-preserving: the sentinel now pins "the header chip's accessible name states the real action and the mislabel is gone", not the retired routes-to-scenario behavior.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - All 6 behavior tests green; each was RED (assertion failure, not crash) before the AllocationsTabs.tsx change.
    - `grep -rn "open Scenario tab" src/ e2e/` returns 0 hits (whole-repo, per the v1.10 e2e grep lesson).
    - `grep -c "ContributionWizardOverlay" "src/app/(dashboard)/allocations/AllocationsTabs.tsx"` ≥ 2 (import + mount).
    - The exact strings "+ Strategy", "+ Allocation", "Add strategy — open the strategy picker", "Add allocation — connect an exchange or upload a CSV" appear in AllocationsTabs.tsx.
    - No new dropdown/menu primitive in the diff (`git diff` for AllocationsTabs.tsx contains no `role="menu"` and no new component file).
    - ContributionWizardOverlay.tsx is byte-unmodified (`git diff --exit-code -- "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx"`).
    - Full pre-existing AllocationsTabs suites stay green: npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.onboarding.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx" --no-file-parallelism
  </acceptance_criteria>
  <done>On Holdings/Overview (and outcomes/mandate/risk) the header "+ Allocation" opens the wizard overlay inline with correct aria-label and focus return; the scenario-tab label/aria read "+ Strategy"; the old mislabel is gone repo-wide; all suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scenario "+ Strategy" → composer Browse signal (onRegisterOpenBrowse) + browse-close focus return</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/AllocationsTabs.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx — :327-344 (ScenarioComposerProps + the onRegisterOpen precedent :338), :757-765 (destructure), :847-853 (browseOpen/contributeOpen), :1771-1775 (ref-stabilized registration effect to mirror), :3618-3634 and :4872-4888 (BOTH StrategyBrowseDrawer mounts — onClose and onAddOwn call sites)
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx — :878-1002 (ScenarioTabContent: composerOpenRef/handleRegisterOpen precedent :890, :932-937; the ScenarioComposer render :977-983), the Task-1 handleHeaderAdd
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx — head of file for its existing mock harness (check how StrategyBrowseDrawer is mocked there before writing assertions against it)
  </read_first>
  <behavior>
    - Test 1 (composer side, ScenarioComposer.test.tsx): rendering the REAL composer with an onRegisterOpenBrowse capture prop yields a registered function; invoking it inside act() makes the StrategyBrowseDrawer receive isOpen=true (assert via that file's existing StrategyBrowseDrawer mock/marker). This test fails if the registration effect or the setBrowseOpen(true) body is neutered.
    - Test 2 (composer side): with an onBrowseClosed spy prop, opening browse then invoking the drawer's onClose calls the spy exactly once; invoking the drawer's onAddOwn (the wizard handoff) does NOT call the spy.
    - Test 3 (host side, AllocationsTabs.addalloc.test.tsx): on ?tab=scenario, extend the ScenarioComposer stub to capture onRegisterOpenBrowse (registering a vi.fn on render, mirroring the file's onRegisterOpen capture idiom); clicking "+ Strategy" invokes the registered function exactly once and never calls router.replace.
    - Test 4 (host side, pending drain): the stub delays registration (capture the callback but register only after a user event / rerender); clicking "+ Strategy" before registration then registering afterwards invokes the newly-registered open function once (the dynamic-import loading window is not a silent no-op).
    - Test 5 (host side, focus return): after a header-initiated browse open, the stub invoking the received onBrowseClosed returns document.activeElement to the header button; a NON-header browse close (onBrowseClosed invoked without a prior header click) does not steal focus.
  </behavior>
  <action>
    Composer side (additive only — do not touch browse/add/draft logic):
    1. Add two optional props to ScenarioComposerProps with TSDoc naming Phase 116/ADDALLOC-01: `onRegisterOpenBrowse?: (open: () => void) => void` (host registers to receive an imperative open-Browse handler — the header "+ Strategy" dispatch) and `onBrowseClosed?: () => void` (fired when the Browse drawer closes to the composer surface, so the host can restore trigger focus; NOT fired on the onAddOwn wizard handoff, which is a modal-to-modal transition, not a close).
    2. Register via the exact ref-stabilization pattern at :1771-1775: mirror onRegisterOpenRef with an onRegisterOpenBrowseRef, and a mount effect that calls it with a stable function that does setBrowseOpen(true).
    3. At BOTH StrategyBrowseDrawer mounts (:3620 and :4874) change onClose to also invoke onBrowseClosed?.() after setBrowseOpen(false). Leave both onAddOwn handlers byte-unchanged. src/lib/scenario.ts (the frozen engine) is untouched by construction — this task edits only the composer component.

    Host side (AllocationsTabs.tsx):
    4. In ScenarioTabContent, accept and thread a new optional prop pair down to ScenarioComposer (onRegisterOpenBrowse, onBrowseClosed) — passed in from AllocationsTabs alongside the existing {...props} spread (extend ScenarioTabContent's props type in-file; keep the pre-existing test call sites compiling since the additions are optional).
    5. In AllocationsTabs: add composerBrowseOpenRef (stores the registered open fn), pendingBrowseOpenRef (boolean), and headerBrowseTriggeredRef (boolean). handleRegisterOpenBrowse stores the fn and, if pendingBrowseOpenRef is set, invokes it immediately and clears the flag (this drains a click that landed during the next/dynamic composer chunk load — ADDALLOC-03's no-silent-no-op during the loading window). handleBrowseClosed: if headerBrowseTriggeredRef is set, focus addButtonRef and clear the flag; otherwise no-op (in-composer Browse triggers keep their current focus behavior).
    6. Complete Task 1's handleHeaderAdd scenario branch: if isUiV2 is false (the explicit localStorage opt-out ScenarioStub rollback surface) keep the legacy changeTab("scenario") with a comment naming why (the stub predates ADDALLOC and is out of its scope); otherwise set headerBrowseTriggeredRef, then invoke composerBrowseOpenRef.current if registered, else set pendingBrowseOpenRef. Clear pendingBrowseOpenRef and headerBrowseTriggeredRef whenever activeTab leaves "scenario" (guard against a stale pend re-opening Browse on a later remount).
    7. Update the ScenarioComposer stub in AllocationsTabs.addalloc.test.tsx per behavior Tests 3-5 and, if ScenarioComposer.test.tsx's harness needs a StrategyBrowseDrawer prop capture for Tests 1-2, extend its existing mock minimally (match its idiom — do not restructure the harness).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - All 5 behavior tests green; composer-side tests were RED before the ScenarioComposer.tsx change; host-side tests were RED before the AllocationsTabs.tsx wiring.
    - `grep -c "onRegisterOpenBrowse" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 3 (prop TSDoc/decl, destructure, registration) and appears in AllocationsTabs.tsx.
    - Both drawer onClose sites invoke onBrowseClosed; both onAddOwn handlers are byte-identical to before (git diff shows no onAddOwn hunk).
    - `git diff --exit-code origin/main -- src/lib/scenario.ts` exits 0 (SC-3 engine freeze — this phase must not touch it).
    - Composer regression suites green: npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" --no-file-parallelism
    - npx tsc --noEmit exits 0 and npm run lint reports 0 errors.
  </acceptance_criteria>
  <done>Clicking "+ Strategy" on the Scenario tab opens the composer's StrategyBrowseDrawer (including during the dynamic-import loading window via the pending drain); closing it returns focus to the header trigger when header-initiated; the composer change is additive-only and the frozen engine is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → existing authed routes | Unchanged. The rewire dispatches to overlays whose backing routes (strategy browse via withPublishedOrOwner RLS, wizard finalize routes) keep their existing server-side authorization. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-01 | Elevation | header dispatch → ContributionWizardOverlay/StrategyBrowseDrawer | accept | Client-only rewire of WHICH existing overlay opens; grants no new capability — all data access is enforced server-side by the untouched Phase-109/110 guards (RLS + withPublishedOrOwner + wizard route guards). |
| T-116-02 | Information Disclosure | StrategyBrowseDrawer opened from a new trigger | accept | Same drawer, same fetch ("/api/strategies/browse"), same owner-inclusive scoping — the trigger location does not change the query's session-derived userId. |
| T-116-SC | Tampering | package installs | accept | Zero package installs in this phase (rewire of in-repo primitives only); no legitimacy gate required. |
</threat_model>

<verification>
- Plan suites (local, flake-safe): npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.addalloc.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" --no-file-parallelism
- Whole-repo mislabel gate: grep -rn "open Scenario tab" src/ e2e/ → 0 hits.
- SC-3: git diff --exit-code origin/main -- src/lib/scenario.ts → exit 0.
- tsc 0 errors; npm run lint 0 errors (the lone pre-existing EquityChart.tsx react-hooks warning is out of scope).
</verification>

<success_criteria>
- ADDALLOC-01: on ?tab=scenario, "+ Strategy" opens the StrategyBrowseDrawer (published + own private, with the onAddOwn upload escape hatch) — proven by composer-side and host-side wiring tests.
- ADDALLOC-02: on Holdings/Overview, "+ Allocation" opens ContributionWizardOverlay (API key or CSV) inline with no navigation — proven by the overlay-stub wiring tests.
- ADDALLOC-03: dispatch is deterministic on every tab (binary on activeTab), covers the dynamic-import loading window via the pending drain, and introduces no dropdown; aria-labels state the real action.
- A11y contract held: focus returns to the header trigger on close; the overlay's own focus-in/Esc/backdrop posture is byte-unmodified.
</success_criteria>

<output>
Create `.planning/phases/116-addalloc-context-aware-allocation/116-01-SUMMARY.md` when done.
</output>
