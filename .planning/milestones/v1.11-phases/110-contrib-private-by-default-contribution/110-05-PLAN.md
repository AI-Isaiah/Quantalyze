---
phase: 110-contrib-private-by-default-contribution
plan: 05
type: execute
wave: 2
depends_on: ["110-03"]
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/DashboardChrome.tsx
  - src/components/layout/MobileNav.tsx
  - src/components/layout/MobileSidebarDrawer.tsx
  - src/components/layout/Sidebar.test.tsx
  - src/components/layout/DashboardChrome.test.tsx
  - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx
  - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [CONTRIB-01, CONTRIB-05]
must_haves:
  truths:
    - "An allocator (role allocator or both) sees an 'Add a Strategy' nav entry in the allocator workspace block that opens the contribution overlay via a client action — NO navigation, NO href"
    - "A manager-only or admin-without-allocator account never sees the entry (desktop, mobile drawer, mobile bar)"
    - "The Browse drawer surfaces a 'Can't find it? Add your own' CTA that opens the same overlay; after a successful contribution the Browse drawer refetches and shows the new private row"
  artifacts:
    - path: "src/components/layout/DashboardChrome.tsx"
      provides: "overlay host state + onAddStrategy threading"
      contains: "ContributionWizardOverlay"
    - path: "src/components/layout/Sidebar.tsx"
      provides: "allocator-scoped 'Add a Strategy' action entry in BOTH nav builders"
      contains: "Add a Strategy"
    - path: "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"
      provides: "CONTRIB-05 CTA"
      contains: "Add your own"
  key_links:
    - from: "src/components/layout/DashboardChrome.tsx"
      to: "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx"
      via: "state-driven mount (import precedent: DashboardChrome already imports @/app/(dashboard)/allocations/AllocationContext)"
      pattern: "ContributionWizardOverlay"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx"
      via: "sibling contributeOpen state next to browseOpen"
      pattern: "ContributionWizardOverlay"
---

<objective>
Wire the two launch surfaces for the contribution overlay: the allocator-scoped "Add a Strategy" nav action (CONTRIB-01, the ROLE-02 scoped-contribution exception) and the Browse drawer "Can't find it? Add your own" CTA (CONTRIB-05).

Purpose: the locked CONTEXT decision is a CLIENT ACTION, not a `?add=1` query-param route — the user's UAT direction is "no navigation at all". `NavItem` is href-based today, so this adds the minimal client-action affordance to the nav system without breaking the href items, and never routes an allocator toward the 109 manager-guarded `/strategies` subtree.

Output: nav action entry (desktop + both mobile navs), DashboardChrome overlay host, drawer CTA + ScenarioComposer wiring, role-scoped tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-contrib-private-by-default-contribution/110-CONTEXT.md
@src/components/layout/Sidebar.tsx
@src/components/layout/DashboardChrome.tsx
@src/components/layout/MobileNav.tsx
@src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx

MODIFIED Next.js 16.2.x — read node_modules/next/dist/docs/ guides before App Router/client-component work. DESIGN.md governs icon/spacing/copy treatment.

<interfaces>
Verified seams (source, 2026-07-16):
- `NavItem` (`Sidebar.tsx:9-15`): `{ label: string; href: string; icon: IconComponent; badge?: number }` — href-based.
- Allocator desktop block: `buildNavSections`, `if (showsAllocatorWorkspace) { workspaceItems.push({ label: "My Allocation", href: "/allocations", ... }) }` (`:78-104`); `showsAllocatorWorkspace = isAllocator` (`:53`); role='both' lights it.
- Mobile builder: `buildPrimaryMobileNav(p)` (`:198+`) — SC#1 trio leads (`My Allocation`, `Risk`, `Bridge`), Discovery is a discretionary `fillers` item trimmed when the ≤5 cap binds; comment `:174` region: "the two navs share one source of truth / never drift".
- `MobileNav.tsx:52` calls `buildPrimaryMobileNav({ isAllocator, isManager, isAdmin, flaggedCount })` itself — the action callback must be threaded to MobileNav.
- `DashboardChrome.tsx` — "use client" shell: renders `<Sidebar ...>` (`:148`), `<MobileSidebarDrawer>` (`:122,:194`), children at `:110,:176`; ALREADY imports from `@/app/(dashboard)/allocations/AllocationContext` (`:11`) — the components→app import for the overlay has precedent.
- `ScenarioComposer.tsx` drawer pattern: `const [browseOpen, setBrowseOpen] = useState(false)` (`:844`), opener `:3275`, mount `:3282-3284` `<StrategyBrowseDrawer isOpen={browseOpen} onClose={() => setBrowseOpen(false)} ...>`.
- `StrategyBrowseDrawer.tsx`: props `:71-77` (`isOpen`, `onClose`, optional `fetchStrategies` test override); lazy-fetch contract (`:18`): fetches `/api/strategies/browse` ONCE per open — reopening refetches; existing button precedents `:471-477,:523-531`.

From plan 110-03 (dependency): `ContributionWizardOverlay` at `src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx` with contract `{ isOpen: boolean; onClose: () => void; onSuccess?: (strategyId: string) => void }`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Allocator "Add a Strategy" nav action + DashboardChrome overlay host</name>
  <files>src/components/layout/Sidebar.tsx, src/components/layout/DashboardChrome.tsx, src/components/layout/MobileNav.tsx, src/components/layout/MobileSidebarDrawer.tsx, src/components/layout/Sidebar.test.tsx, src/components/layout/DashboardChrome.test.tsx</files>
  <read_first>src/components/layout/Sidebar.tsx (NavItem type :9-15; buildNavSections allocator block :78-104; buildPrimaryMobileNav :198-235; how render sites map items to Links); src/components/layout/DashboardChrome.tsx (Sidebar/MobileSidebarDrawer/MobileNav mounts + the AllocationContext app-import precedent :11); src/components/layout/MobileNav.tsx (:52 builder call + item rendering); src/components/layout/MobileSidebarDrawer.tsx (whether it renders NavItems); existing Sidebar/DashboardChrome tests</read_first>
  <behavior>
    - Test 1: Sidebar with isAllocator=true renders "Add a Strategy" inside the allocator workspace section as a `<button type="button">` (no href, no Link); clicking fires the action callback.
    - Test 2: isAllocator=false (manager-only) → entry absent from buildNavSections AND buildPrimaryMobileNav output; admin-without-allocator-role → absent (T-45-01 class).
    - Test 3: role='both' (isAllocator && isManager) → entry present.
    - Test 4: DashboardChrome — triggering the nav action mounts ContributionWizardOverlay with isOpen=true; onClose flips it back; onSuccess closes it.
    - Test 5: existing nav items still render as Links (no regression on href items in any of the three render surfaces).
  </behavior>
  <action>
Extend the nav item model with the MINIMAL client-action affordance (per D — locked: client action, NOT a `/…?add=1` query-param route): make `NavItem` a union or add optional fields so an item is EITHER `{ href }` (existing, untouched) OR `{ action: "add-strategy" }` (new, no href). Add the entry `{ label: "Add a Strategy", icon: <a plus-style icon consistent with DESIGN.md/existing icon set>, action: "add-strategy" }` INSIDE the `showsAllocatorWorkspace` branch of BOTH `buildNavSections` (after the existing allocator items) AND `buildPrimaryMobileNav` (as a discretionary FILLER so the SC#1 trio keeps priority under the ≤5 cap) — never in the manager branch, honoring the ":174 two navs never drift" single-sourcing.

Render sites: Sidebar desktop, MobileNav, and MobileSidebarDrawer (if it renders NavItems) map action items to `<button type="button" onClick={() => onNavAction?.(item.action)}>` styled to match sibling nav items; href items unchanged. Thread a new optional `onNavAction?: (action: "add-strategy") => void` prop (or a specific `onAddStrategy`) from DashboardChrome into Sidebar, MobileNav, and MobileSidebarDrawer.

DashboardChrome: add `const [contributeOpen, setContributeOpen] = useState(false)`; pass the action handler (`() => setContributeOpen(true)`) to all three nav surfaces; mount `<ContributionWizardOverlay isOpen={contributeOpen} onClose={() => setContributeOpen(false)} onSuccess={() => { setContributeOpen(false); router.refresh(); }} />` once at the chrome level (import from `@/app/(dashboard)/allocations/components/ContributionWizardOverlay` — precedent: the AllocationContext import at `:11`). Copy is allocator-framed ("Add a Strategy" — adding a strategy to track/compose), never manager "publish to investors" (per D / CONTRIB-01).
  </action>
  <acceptance_criteria>
- All 5 behavior tests pass; the entry appears in the output of BOTH builders only when isAllocator is true.
- The entry is rendered as a button on every surface — `grep` shows no `href` associated with "Add a Strategy" and no route string `/strategies` introduced anywhere in this diff.
- Existing Sidebar/MobileNav tests green (href items unbroken).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/layout --no-file-parallelism && grep -rn "Add a Strategy" src/components/layout/Sidebar.tsx | grep -c "href" # expect 0</automated>
  </verify>
  <done>CONTRIB-01's entry point is live: allocator-scoped, action-driven, zero navigation, present on desktop + both mobile navs, hosting the overlay at chrome level.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Browse drawer "Can't find it? Add your own" CTA + ScenarioComposer wiring</name>
  <files>src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx, src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx (props :71-77, button precedents :471-477/:523-531, lazy-fetch contract :18, null gate :322); src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx; src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (:844 browseOpen state, :3275 opener, :3282-3284 drawer mount — mirror this exact pattern)</read_first>
  <behavior>
    - Test 1 (drawer): with `onAddOwn` provided, the open drawer renders a "Can't find it? Add your own" CTA (`type="button"`); clicking calls `onAddOwn` exactly once.
    - Test 2 (drawer): without `onAddOwn`, the CTA is absent (optional-prop safety for other mounts).
    - Test 3 (composer, if a ScenarioComposer test harness exists — else cover via the drawer test + a targeted mount test): the CTA click closes the browse drawer and opens ContributionWizardOverlay; overlay onSuccess closes the overlay and REOPENS the browse drawer (which refetches per its once-per-open contract, so the owner's new private row appears).
  </behavior>
  <action>
StrategyBrowseDrawer: add optional prop `onAddOwn?: () => void` (mirror the existing optional-prop style at `:71-77`); render the CTA — copy exactly "Can't find it? Add your own" — as a `type="button"` element styled per the existing drawer button precedents, placed where it reads as the escape hatch of the browse list (e.g. beneath the results/empty state — DESIGN.md governs; also render it in the empty/no-results state where it matters most). Clicking calls `onAddOwn`.

ScenarioComposer: add `const [contributeOpen, setContributeOpen] = useState(false)` as a sibling to `browseOpen` (`:844`); pass `onAddOwn={() => { setBrowseOpen(false); setContributeOpen(true); }}` to the StrategyBrowseDrawer mount (`:3282-3284`); mount `<ContributionWizardOverlay isOpen={contributeOpen} onClose={() => setContributeOpen(false)} onSuccess={() => { setContributeOpen(false); setBrowseOpen(true); }} />` next to the drawer. The onSuccess → reopen-browse chain is the CONTRIB-03 payoff: the drawer refetches on open and the freshly-contributed private row is selectable. (ScenarioComposer is a god file — surgical additions only: one state hook, one prop, one mount; no refactor.)
  </action>
  <acceptance_criteria>
- Drawer tests pass (CTA present-with-prop / absent-without / fires once).
- ScenarioComposer diff is limited to the state hook + prop + overlay mount (no unrelated churn — reviewable in isolation).
- The CTA is a button (no href/Link); grep confirms no navigation to /strategies introduced.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx" --no-file-parallelism && grep -c "onAddOwn" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" # expect >=1</automated>
  </verify>
  <done>CONTRIB-05 delivered: the drawer's dead-end becomes a contribution entry, and a successful contribution lands the user back in a refreshed Browse showing their private strategy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| role-derived nav rendering | nav visibility is a UX hint — server/RLS enforce; but a leaked entry to managers is still an info-disclosure smell (T-45-01 class) |
| overlay triggers → manager-guarded routes | any href fallback would bounce allocators off /strategies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-16 | Information Disclosure | "Add a Strategy" leaking outside the allocator block | mitigate | entry lives ONLY inside showsAllocatorWorkspace in both builders; Test 2 pins manager-only + admin-without-role absence |
| T-110-17 | Denial of Service (UX-loss) | trigger navigating to a 109-guarded route | mitigate | action-item affordance has no href by construction; grep gate: no /strategies string in the diff |
| T-110-18 | Spoofing | client opening the overlay without allocator role (e.g. via devtools) | accept | overlay is cosmetic; finalize routes + RLS + RPC guards are the real boundary (plans 01/02/04) — a non-allocator finalizing their own private strategy gains nothing |
| T-110-SC | Tampering | package installs | accept | zero new dependencies |
</threat_model>

<verification>
- Layout + drawer test suites green; full `npm run test` + `npm run lint` green at plan close.
- Manual (deferred to phase UAT, per 110-VALIDATION.md): as allocator, "Add a Strategy" opens the overlay with no URL change; complete a CSV contribution; it appears in Browse for the owner and never for a second account.
</verification>

<success_criteria>
- CONTRIB-01 + CONTRIB-05 fully wired: two launch surfaces, one reusable overlay, role-scoped visibility proven by tests, zero navigation anywhere in the trigger paths.
</success_criteria>

<output>
Create `.planning/phases/110-contrib-private-by-default-contribution/110-05-SUMMARY.md` when done.
</output>
