---
phase: 110-contrib-private-by-default-contribution
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/visibility.ts
  - src/app/api/strategies/browse/route.ts
  - src/app/api/strategies/browse/route.test.ts
  - tools/eslint-plugin-quantalyze/rules/no-owner-or-on-admin-client.mjs
  - tools/eslint-plugin-quantalyze/tests/no-owner-or-on-admin-client.test.ts
  - tools/eslint-plugin-quantalyze/index.mjs
  - eslint.config.mjs
autonomous: true
requirements: [CONTRIB-03, CONTRIB-04]
must_haves:
  truths:
    - "The browse route returns the owner's own private rows PLUS all published rows; a non-owner gets only published rows"
    - "The owner id in the browse predicate comes ONLY from the withAllocatorAuth session — no client-supplied param can alter it"
    - "A raw .or('...user_id.eq...') outside visibility.ts is an ESLint ERROR"
  artifacts:
    - path: "src/lib/visibility.ts"
      provides: "withPublishedOrOwner helper (the pre-documented 3-line extension)"
      contains: "withPublishedOrOwner"
    - path: "src/app/api/strategies/browse/route.ts"
      provides: "owner-inclusive Browse discovery"
      contains: "withPublishedOrOwner"
    - path: "tools/eslint-plugin-quantalyze/rules/no-owner-or-on-admin-client.mjs"
      provides: "CONTRIB-04 build-time backstop"
      contains: "user_id"
  key_links:
    - from: "src/app/api/strategies/browse/route.ts"
      to: "src/lib/visibility.ts"
      via: "import + call with session user.id"
      pattern: "withPublishedOrOwner"
    - from: "eslint.config.mjs"
      to: "tools/eslint-plugin-quantalyze/rules/no-owner-or-on-admin-client.mjs"
      via: "rule registration at error severity"
      pattern: "no-owner-or-on-admin-client"
---

<objective>
Realize owner-inclusive Browse discovery (CONTRIB-03) via the pre-staged `withPublishedOrOwner` seam, and land the CONTRIB-04 build-time + query-builder layers: an ESLint rule banning raw owner-OR predicates, plus route tests proving non-owners never receive another user's unpublished rows and that the userId is session-only.

Purpose: `src/lib/visibility.ts:32-40` literally documents `withPublishedOrOwner` as a deliberately-omitted 3-line extension "to add when a genuine owner-inclusive discovery surface is first written" — this phase is that surface. The lint rule stops a future admin-client swap from silently removing the RLS backstop (Pitfall 4).

Output: helper + one-line browse swap + extended route tests + a registered, tested lint rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-contrib-private-by-default-contribution/110-CONTEXT.md
@src/lib/visibility.ts
@src/app/api/strategies/browse/route.ts
@tools/eslint-plugin-quantalyze/rules/no-raw-published-predicate.mjs

<interfaces>
From 110-PATTERNS.md (verified against source):
- `visibility.ts:74-86` — `withPublishedOnly` is the structural template (generic `<Q>` + structural cast to dodge TS2589; client-safe, NO server-only imports). Docstring `:32-40` prescribes exactly: `q.or(`status.eq.published,user_id.eq.${authUserId}`)`. File carries the `B10 visibility:` marker.
- `browse/route.ts` — swap site `:119` (`withPublishedOnly(supabase.from("strategies").select("id, name, codename, disclosure_tier, markets, strategy_types, is_example"))`); import at `:4`; session user from `withAllocatorAuth` (`AllocatorUser`, `:91-92`); user-scoped `createClient()` at `:108`; RLS comment to update at `:109-113`.
- `no-raw-published-predicate.mjs` — clone template: `fileHasMarker(sourceCode, [...])` exemption from `./_shared.mjs` (`:21,47`), `meta.messages`, `create(context)` returning a `CallExpression[callee.property.name='...']` visitor (`:45-62`).
- Registration: `tools/eslint-plugin-quantalyze/index.mjs` (import near `:20`, rules-map entry near `:31`); `eslint.config.mjs` (enable at error, mirror `:46`; check the `:262-271` off-override block — if sibling quantalyze rules are disabled there for test fixtures, add this rule there too).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add withPublishedOrOwner and swap it into the browse route (session-only userId)</name>
  <files>src/lib/visibility.ts, src/app/api/strategies/browse/route.ts, src/app/api/strategies/browse/route.test.ts</files>
  <read_first>src/lib/visibility.ts (whole file — docstring :32-40 + withPublishedOnly :74-86 template + B10 marker); src/app/api/strategies/browse/route.ts (:1-135 — imports, withAllocatorAuth user, createClient, swap site :119, comment :109-113); src/app/api/strategies/browse/route.test.ts (existing published-only assertions to extend)</read_first>
  <behavior>
    - Test 1: browse called as user A (session id A) → query chain receives `.or("status.eq.published,user_id.eq.A")`; response includes A's private row + published rows (mock builder returning both).
    - Test 2: browse called as user B → the `.or()` filter embeds B's session id, NOT any id from query/body — assert a request carrying a `?user_id=A` (or similar) param does NOT alter the filter string (session-only contract, CONTRIB-03/04).
    - Test 3: existing published-only expectations updated — non-owner receives only published rows (the mock RLS/builder never returns A's private row for B's id).
    - Test 4 (visibility unit, colocate where withPublishedOnly is tested if such a test exists, else in the route test): `withPublishedOrOwner(q, "uid")` calls `q.or("status.eq.published,user_id.eq.uid")` and returns the builder.
  </behavior>
  <action>
In `src/lib/visibility.ts`: add exported `withPublishedOrOwner<Q>(query: Q, authUserId: string): Q` implemented as the docstring's exact 3-line extension — `(query as { or(filter: string): Q }).or(`status.eq.published,user_id.eq.${authUserId}`)` — mirroring `withPublishedOnly`'s structural-cast style. Update the module docstring: `withPublishedOrOwner` is no longer OMITTED; its first consumer is the browse route (Phase 110 / CONTRIB-03); note the predicate deliberately includes ALL owner-visible statuses (`published OR user_id=eq`) matching the `strategies_read` RLS shape per the locked decision D. Keep the `B10 visibility:` file marker intact (the new lint rule in Task 2 exempts this file by that marker). No server-only imports.

In `browse/route.ts`: swap the import at `:4` and the call at `:119` from `withPublishedOnly(...)` to `withPublishedOrOwner(..., user.id)` where `user` is the `AllocatorUser` from `withAllocatorAuth` — session-derived only, never a request param (per D). Keep the route on the user-scoped `createClient()` (`:108`) — NEVER `createAdminClient()` (Pitfall 4). Update the `:109-113` comment (currently "RLS ... enforces ... status='published'") to describe the two-layer owner-inclusive contract: query-builder predicate mirrors RLS; RLS remains the backstop.

Extend `route.test.ts` per the behavior block. Note: the private rows only EXIST after plan 110-01's migration, but these are mocked-builder unit tests — no DB dependency, no ordering constraint on plan 01.
  </action>
  <acceptance_criteria>
- `withPublishedOrOwner` exported from visibility.ts with the exact filter string `status.eq.published,user_id.eq.${authUserId}`; no server-only import added.
- browse/route.ts contains zero references to `withPublishedOnly` and zero `createAdminClient` references; the only id fed to the predicate is `user.id` from `withAllocatorAuth`.
- All 4 behavior tests pass; the session-only test (Test 2) FAILS if the route is mutated to read a userId from the request (verify by temporarily neutering — wiring test, not helper test).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/strategies/browse --no-file-parallelism && grep -c "withPublishedOnly" src/app/api/strategies/browse/route.ts # expect 0</automated>
  </verify>
  <done>Browse is owner-inclusive with a session-only userId; helper lives in visibility.ts as pre-documented; tests prove both the inclusion (owner) and exclusion (non-owner) directions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clone the lint backstop — no-owner-or-on-admin-client rule + test + registration</name>
  <files>tools/eslint-plugin-quantalyze/rules/no-owner-or-on-admin-client.mjs, tools/eslint-plugin-quantalyze/tests/no-owner-or-on-admin-client.test.ts, tools/eslint-plugin-quantalyze/index.mjs, eslint.config.mjs</files>
  <read_first>tools/eslint-plugin-quantalyze/rules/no-raw-published-predicate.mjs (the clone template — fileHasMarker exemption, meta.messages, CallExpression visitor); tools/eslint-plugin-quantalyze/tests/no-raw-published-predicate.test.ts (RuleTester harness); tools/eslint-plugin-quantalyze/index.mjs (:20,:31 registration pattern); eslint.config.mjs (:46 enable pattern + :262-271 off-override block)</read_first>
  <behavior>
    - Invalid: `.or('status.eq.published,user_id.eq.' + id)` / `.or(\`...user_id.eq.${id}\`)` in an arbitrary file → 1 report with the messageId directing to withPublishedOrOwner.
    - Invalid: `createAdminClient().from("strategies").select().or("user_id.eq.x")` → reports (the admin-client leak scenario, Pitfall 4).
    - Valid: the same `.or(...user_id...)` in a fixture carrying the `B10 visibility:` marker (visibility.ts itself) → no report.
    - Valid: `.or("status.eq.published,is_example.eq.true")` (no user_id) → no report.
  </behavior>
  <action>
Clone `no-raw-published-predicate.mjs` into `rules/no-owner-or-on-admin-client.mjs`: visitor `"CallExpression[callee.property.name='or']"`; report when the first argument is a string-bearing node (Literal, or TemplateLiteral whose quasis join matches) containing `user_id.eq.` (regex `/user_id\.eq\./`); exempt files via `fileHasMarker(sourceCode, ["B10 visibility:", "B10 sanctioned-exception:"])` from `./_shared.mjs` exactly like the analog. Message: route owner-inclusive queries through `withPublishedOrOwner` (src/lib/visibility.ts) — a raw owner-OR, especially on a service-role client, can leak every user's private rows. Handle TemplateLiteral as well as Literal — the real-world pattern is a template string embedding the id.

Clone the RuleTester test with the valid/invalid cases from the behavior block. Register: `index.mjs` import + rules-map entry; `eslint.config.mjs` `"quantalyze/no-owner-or-on-admin-client": "error"` mirroring `:46`; if the `:262-271` override block disables sibling quantalyze rules for a fixture/test scope, add this rule there identically.

Finally run `npm run lint` on the repo — `visibility.ts` must pass via its marker; the browse route must pass because it calls the helper (no raw `.or` literal).
  </action>
  <acceptance_criteria>
- Rule reports on both Literal and TemplateLiteral `.or()` args containing `user_id.eq.`; marker-exempt file produces no report.
- Rule registered in index.mjs AND enabled at "error" in eslint.config.mjs (both greps below).
- `npm run lint` exits 0 on the full repo with the rule active.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tools/eslint-plugin-quantalyze/tests/no-owner-or-on-admin-client.test.ts --no-file-parallelism && grep -c "no-owner-or-on-admin-client" tools/eslint-plugin-quantalyze/index.mjs # >=2; grep -c "no-owner-or-on-admin-client" eslint.config.mjs # >=1; npm run lint</automated>
  </verify>
  <done>The build-time layer of CONTRIB-04 is live: a raw owner-OR predicate anywhere outside visibility.ts fails lint, with a test pinning both directions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client request → browse route | query/body params are untrusted; only the session may name the owner |
| query builder → Postgres | the `.or()` predicate is the query-builder isolation layer; RLS backstops it |
| future refactor → admin client | a service-role swap would bypass RLS entirely |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Information Disclosure | browse route owner predicate | mitigate | userId exclusively from withAllocatorAuth session; Test 2 pins that a client-supplied user_id param cannot alter the filter |
| T-110-06 | Information Disclosure | admin-client `.or()` bypass of RLS | mitigate | ESLint rule at error severity + rule test (Task 2); browse stays on user-scoped createClient (grep gate) |
| T-110-07 | Spoofing | client-supplied userId | mitigate | no request field is read for identity; session-only contract asserted in route test |
| T-110-08 | Information Disclosure | over-broad owner predicate (own drafts visible to owner) | accept | owner seeing their OWN non-published rows is by design (locked decision D — predicate mirrors strategies_read RLS exactly) |
| T-110-SC | Tampering | package installs | accept | zero new dependencies |
</threat_model>

<verification>
- Browse route tests green including the session-only and non-owner-exclusion assertions.
- Lint rule test green; `npm run lint` green repo-wide.
- No `createAdminClient` and no `withPublishedOnly` remain in browse/route.ts.
</verification>

<success_criteria>
- CONTRIB-03 delivered (owner-inclusive discovery, session-only id) and CONTRIB-04's query-builder + build-time layers delivered (the RLS layer lands in plan 110-01).
</success_criteria>

<output>
Create `.planning/phases/110-contrib-private-by-default-contribution/110-02-SUMMARY.md` when done.
</output>
