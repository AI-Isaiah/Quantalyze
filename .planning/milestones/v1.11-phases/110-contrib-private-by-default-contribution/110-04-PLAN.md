---
phase: 110-contrib-private-by-default-contribution
plan: 04
type: execute
wave: 2
depends_on: ["110-01"]
files_modified:
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/strategies/csv-finalize/route.test.ts
autonomous: true
requirements: [CONTRIB-02]
must_haves:
  truths:
    - "A finalize request with entry_context='contribution' terminates the strategy at status='private' on BOTH the API-key path (legacy + unified arms) and the CSV path — never 'pending_review'"
    - "A finalize request without entry_context (or ='manager') behaves byte-identically to today ('pending_review')"
    - "A contribution finalize never triggers the admin publish-review notification, but STILL enqueues analytics/dailies derivation (allocator needs KPIs)"
    - "No consumer anywhere in the repo (src/, e2e/, tests, analytics-service/) mishandles the new 'private' status — public surfaces exclude it, owner surfaces include it"
  artifacts:
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "entry_context branch → p_terminal_status='private'"
      contains: "entry_context"
    - path: "src/app/api/strategies/csv-finalize/route.ts"
      provides: "same contribution branch on the CSV path"
      contains: "entry_context"
  key_links:
    - from: "src/app/api/strategies/finalize-wizard/route.ts"
      to: "finalize_wizard_strategy RPC"
      via: "p_terminal_status rpc arg"
      pattern: "p_terminal_status"
    - from: "src/app/api/strategies/csv-finalize/route.ts"
      to: "finalize_csv_strategy RPC"
      via: "p_terminal_status rpc arg"
      pattern: "p_terminal_status"
---

<objective>
Thread the contribution branch through both finalize routes: `entry_context` request field → `p_terminal_status='private'` RPC arg → `status: "private"` response, on the legacy AND unified finalize arms and the CSV path — plus the whole-repo `'private'` status-consumer audit.

Purpose: this is the server half of CONTRIB-02. Plan 110-01 gave the RPCs the guarded terminal-status parameter; this plan makes the routes select it. The manager flow stays byte-identical (default `'pending_review'`). Do NOT fork per-source — CSV / single API-key / composite all flow the SAME branch (locked decision).

Output: two branched routes with extended tests + a repo-wide status audit table in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-contrib-private-by-default-contribution/110-CONTEXT.md
@.planning/phases/110-contrib-private-by-default-contribution/110-RESEARCH.md
@src/app/api/strategies/finalize-wizard/route.ts
@src/app/api/strategies/csv-finalize/route.ts
@supabase/migrations/20260716130500_finalize_terminal_status_param.sql

<interfaces>
Cross-plan contract (FIXED — plan 110-03's WizardClient sends these exact names):
- Request body optional field `entry_context: "manager" | "contribution"` on BOTH finalize endpoints; absent → `"manager"` (back-compat: today's callers send nothing).
- RPC arg (from plan 110-01): `p_terminal_status` accepting only `'pending_review' | 'private'`; the RPC RAISEs on anything else.
- Response `status` field returns the ACTUAL terminal status: `"private"` on the contribution branch.

Verified route seams (source, 2026-07-16):
- finalize-wizard/route.ts: `runLegacyFinalize` calls `supabase.rpc("finalize_wizard_strategy", {...})` at `:713-729`; legacy response `status: "pending_review"` at `:973-978`; unified arm `unifiedFinalizeWizardHandler` responses at `:1150-1158` and `:1165` (note `:1132`: the unified arm intentionally returns the legacy `{strategy_id, status:'pending_review'}` shape wizard chrome expects); composite dispatch is wired ONLY through the legacy path (`:1064-1066` — unified arm must not silently orphan stitch_composite); ownership belt-and-braces `.eq("user_id", user.id)` at `:427-432`; admin-notify escalation comment at `:947` ("strategy lands in pending_review with nobody told — escalate"); notify/last_sync block `:779-838`; stitch_composite enqueue `:870-908`.
- csv-finalize/route.ts: `finalize_csv_strategy` RPC invoked server-side (~`:1039-1126`, SECDEF called as the user, `:1078-1083`); header comment `:18-20` documents pending_review/csv_uploaded; orphan comment `:945` references pending_review.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: entry_context branch in finalize-wizard (legacy + unified arms)</name>
  <files>src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>src/app/api/strategies/finalize-wizard/route.ts (body validation/schema; :713-729 rpc call; :973-978 + :1150-1165 responses; :779-838 notify block; :534-620 + :870-908 composite hoist/enqueue; :1064-1066 unified/composite note); src/app/api/strategies/finalize-wizard/route.test.ts (existing pending_review assertions — extend, don't weaken)</read_first>
  <behavior>
    - Test 1: body without entry_context → rpc called WITHOUT p_terminal_status (or with 'pending_review') and response `status: "pending_review"` — existing tests stay green.
    - Test 2: `entry_context: "contribution"` → rpc receives `p_terminal_status: "private"`; response `status: "private"` (assert on whichever arm the test fixture exercises; cover BOTH arms).
    - Test 3: `entry_context: "contribution"` → the admin publish-review notification path is NOT invoked (spy/mock on the notify helper), while the analytics/sync enqueue IS still invoked (locked: contribution computes analytics).
    - Test 4: `entry_context: "garbage"` → 400 from body validation (zod enum), never reaches the rpc.
  </behavior>
  <action>
Add `entry_context` to the route's body validation as a zod enum `["manager", "contribution"]` defaulting to `"manager"`. Derive `const terminalStatus = entryContext === "contribution" ? "private" : "pending_review"` ONCE near the top of the handler and thread it to: (a) the legacy `finalize_wizard_strategy` rpc call (`:713-729`) as `p_terminal_status: terminalStatus`; (b) the legacy response (`:973-978`) returning `status: terminalStatus`; (c) the unified arm — trace where `unifiedFinalizeWizardHandler` performs/records the status transition and thread the same terminal status through it, and return `status: terminalStatus` at `:1150-1158`/`:1165`. If the unified arm's status write happens via the SAME RPC, this is just arg threading; if it happens via a different writer, thread terminalStatus to that writer — do NOT leave the unified arm hardcoded (prod runs unified: process_key_unified_backbone='on').

Add a code comment where entry_context is parsed: a client-supplied entry_context is safe by construction — BOTH reachable terminal statuses are non-published, publication requires the admin queue which only lists 'pending_review', and the RPC RAISEs on any other value (server-side enforcement per V5; T-110-10).

Contribution-mode side effects: skip the admin "new strategy pending review" notification/escalation (`:779-838`, `:947` area) — a private contribution is not a review candidate; KEEP the analytics/dailies enqueue and last_sync/sync_trades behavior (locked decision: contribution DOES compute analytics). Composite path: per the locked "no artificial source restriction / do not fork per-source" decision, the composite hoist + stitch_composite enqueue (`:534-620`, `:870-908`) stays as-is with the terminal status applied the same way — no composite-specific fork.
  </action>
  <acceptance_criteria>
- All 4 behavior tests pass; pre-existing manager-path tests pass unmodified.
- Both response sites and both arms return the actual terminal status (grep: no remaining hardcoded `status: "pending_review"` response literal outside the terminalStatus derivation).
- Contribution branch: notify skipped, analytics enqueue retained (Test 3 pins the wiring, proven to fail when the branch is neutered).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard --no-file-parallelism</automated>
  </verify>
  <done>The API-key finalize (both arms, composite included) terminates contributions at 'private' with analytics intact and no review-queue signaling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: same contribution branch on csv-finalize</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/app/api/strategies/csv-finalize/route.test.ts</files>
  <read_first>src/app/api/strategies/csv-finalize/route.ts (body validation; ~:1039-1126 finalize_csv_strategy rpc invocation; :18-20 + :945 pending_review comments; the persist_csv_daily_returns + analytics enqueue flow :522-696); src/app/api/strategies/csv-finalize/route.test.ts</read_first>
  <behavior>
    - Test 1: default/manager body → `finalize_csv_strategy` rpc without p_terminal_status (or 'pending_review'); existing behavior byte-identical.
    - Test 2: `entry_context: "contribution"` → rpc receives `p_terminal_status: "private"`; response status "private"; `persist_csv_daily_returns` + analytics enqueue still invoked.
    - Test 3: invalid entry_context → 400 before the rpc.
  </behavior>
  <action>
Mirror Task 1 exactly on the CSV path: zod enum `entry_context` defaulting `"manager"`; `terminalStatus` derivation; `p_terminal_status: terminalStatus` on the `finalize_csv_strategy` rpc call; response status reflects the actual terminal status; skip any publish-review notification on the contribution branch; KEEP `persist_csv_daily_returns` and the analytics/compute enqueue (dailies are canonical — the contribution needs its daily series + KPIs). Update the stale header comment (`:18-20`) and the `:945` orphan comment to document both terminal statuses. Do NOT fork the flow per-source — one branch variable, applied where the literal was.
  </action>
  <acceptance_criteria>
- All 3 behavior tests pass; pre-existing CSV tests unmodified and green.
- `grep -n "pending_review" src/app/api/strategies/csv-finalize/route.ts` shows only comment/derivation references — no hardcoded response or rpc literal bypassing terminalStatus.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/app/api/strategies/csv-finalize --no-file-parallelism</automated>
  </verify>
  <done>CSV contributions finalize private with their daily series persisted and analytics enqueued.</done>
</task>

<task type="auto">
  <name>Task 3: whole-repo 'private' status-consumer audit (incl. e2e/ + analytics-service/)</name>
  <files>e2e/ and test fixtures as found (audit-driven; no src/ production edits expected — flag any needed to the SUMMARY)</files>
  <read_first>The grep output itself is the read target; for each hit, read enough surrounding context to classify. Key known consumers to explicitly clear: src/app/(dashboard)/admin/page.tsx:40 (pending_review queue filter), src/app/api/admin/strategy-review/route.ts:175 (promotion), src/lib/queries.ts:255 area (public detail published gate), factsheet/PDF + match/intro surfaces (A5)</read_first>
  <action>
Run the audit the v1.10 lesson demands (grep-gates that scan src/ only MISS e2e/): `grep -rn "pending_review\|status IN\|\.eq(\"status\"\|status\.eq\." src/ e2e/ supabase/ analytics-service/ tools/ --include="*.ts" --include="*.tsx" --include="*.py" --include="*.sql"` (plus a pass for `'published'` status-set literals in fixtures). Classify EVERY hit into: (a) public/verify surface — must EXCLUDE 'private' (surfaces filtering `status='published'` or `pending_review` are safe by construction — VERIFY each, don't assume); (b) owner surface — must INCLUDE it (browse route: plan 110-02; anything else found → fix or flag); (c) status-set enumeration (fixtures, types, zod enums, test seeds) — widen to include 'private' where the enumeration claims completeness; (d) irrelevant. Explicitly confirm: admin queue auto-excludes 'private'; strategy-review promotion path cannot touch a 'private' row; public detail `/strategy/[id]`, factsheet PDF, and match/intro queries exclude it. Produce the classification table in the SUMMARY (file:line → class → action taken). Fix category-(c) fixtures/e2e specs in this task; if any category-(a)/(b) production gap is found, fix it here and flag it prominently in the SUMMARY.
  </action>
  <acceptance_criteria>
- SUMMARY contains the full audit table; every grep hit classified — zero unclassified hits.
- Admin queue, strategy-review, public detail, factsheet, match surfaces each have an explicit exclusion confirmation line (file:line evidence).
- Full TS suite + lint green after any fixture edits.
  </acceptance_criteria>
  <verify>
    <automated>npm run test -- --run 2>/dev/null || npm run test; npm run lint</automated>
  </verify>
  <done>No status consumer anywhere in the repo mishandles 'private'; evidence table recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → finalize routes | entry_context and all payload fields are untrusted input |
| routes → SECDEF RPCs | routes select the terminal status; RPC enforces the allowed set |
| 'private' rows → public surfaces | every published-gated surface must exclude the new status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-12 | Elevation of Privilege | contribution reaching the publish queue | mitigate | terminalStatus branch + Test 2 per route; admin queue filters pending_review only (Task 3 confirmation) |
| T-110-13 | Tampering | client forcing publication via entry_context | mitigate | zod enum (400 on garbage) + RPC RAISE guard on terminal status (plan 110-01); code comment documents the by-construction safety |
| T-110-14 | Information Disclosure | 'private' leaking onto public detail/factsheet/match | mitigate | Task 3 explicit per-surface exclusion confirmations with file:line evidence |
| T-110-15 | Repudiation | contribution silently skipping analytics (broken KPIs blamed on pipeline) | mitigate | Test 3/Task 1 + Test 2/Task 2 pin the analytics enqueue survives the branch |
| T-110-SC | Tampering | package installs | accept | zero new dependencies |
</threat_model>

<verification>
- Both route test files green including the new contribution-branch assertions; full suite + lint green.
- Audit table complete in SUMMARY with per-surface exclusion evidence.
</verification>

<success_criteria>
- CONTRIB-02 is end-to-end true at the API layer: contribution → 'private', manager → 'pending_review', analytics preserved, review signaling suppressed, zero repo-wide status-consumer regressions.
</success_criteria>

<output>
Create `.planning/phases/110-contrib-private-by-default-contribution/110-04-SUMMARY.md` when done (MUST include the Task 3 audit table).
</output>
