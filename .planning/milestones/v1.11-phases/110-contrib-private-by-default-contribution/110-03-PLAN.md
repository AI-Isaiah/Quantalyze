---
phase: 110-contrib-private-by-default-contribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx
  - src/app/(dashboard)/allocations/components/ContributionWizardOverlay.test.tsx
autonomous: true
requirements: [CONTRIB-01, CONTRIB-02]
must_haves:
  truths:
    - "WizardClient in contribution mode NEVER calls router.push('/strategies') on any terminal path (success, failure, 404, delete) — it invokes injected onSuccess/onClose callbacks instead"
    - "The manager page mounts WizardClient byte-compatible (no new required props; default behavior identical)"
    - "ContributionWizardOverlay mounts the wizard inline via createPortal with NO URL change and NO useSearchParams dependency"
    - "In contribution mode the wizard sends entry_context='contribution' to both finalize endpoints and shows allocator-framed copy (no sell-side 'your investors' / 'Submit for review' language)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx"
      provides: "the reusable overlay unit Phase 116 mounts on '+ Allocation'"
      min_lines: 60
      exports: ["ContributionWizardOverlay"]
    - path: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      provides: "entryContext/onSuccess/onClose/sourceOverride parameterization"
      contains: "entryContext"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx"
      to: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      via: "keyed mount with entryContext='contribution' + callbacks"
      pattern: "entryContext=\\{?\"contribution\""
    - from: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      to: "/api/strategies/finalize-wizard and /api/strategies/csv-finalize"
      via: "entry_context field in the finalize POST bodies"
      pattern: "entry_context"
---

<objective>
Extract the existing onboarding wizard behind an `entryContext` + injected-callback seam (single source of wizard truth — NO fork), and build `ContributionWizardOverlay`: the state-driven `createPortal` panel that mounts the wizard inline with zero page navigation. This overlay is the REUSABLE unit Phase 116 mounts on "+ Allocation".

Purpose: the user's live-UAT direction (2026-07-16) is an inline overlay — never a navigation into the 109 manager-guarded `/strategies/new/wizard` route. The wizard's terminal paths currently hardcode `router.push("/strategies")`, which would server-redirect-bounce an allocator off a manager route and destroy the overlay context (Pitfall 2).

Output: parameterized WizardClient (manager page untouched in behavior) + the overlay component + tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-contrib-private-by-default-contribution/110-CONTEXT.md
@.planning/phases/110-contrib-private-by-default-contribution/110-PATTERNS.md
@src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
@src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx
@src/app/(dashboard)/allocations/components/ScenarioCommitDrawer.tsx

MODIFIED Next.js 16.2.x — read the relevant guides in node_modules/next/dist/docs/ before writing any portal/client-component code. DESIGN.md governs the overlay's visual treatment.

<interfaces>
Verified WizardClient seams (source, 2026-07-16):
- Props: `interface WizardClientProps { initialDraft: InitialDraft | null }`, `export function WizardClient({ initialDraft })` (`:56-58,:112`).
- Source derivation: `const source: "api" | "csv" = searchParams.get("source") === "csv" ? "csv" : "api"` (`:119-120`) — fixed per mount; the manager page remounts via `key={source}` (`page.tsx:120-121`). There is NO in-wizard source toggle — the switch is URL-driven on the manager page, so the overlay owns the toggle.
- `initialDraft=null` is a supported fresh start: `if (!initialDraft) return "connect_key"` (`:144`).
- Terminal paths to parameterize (THE trap — all push the 109 manager-guarded `/strategies`): fail-safe redirects `:357,:362` (`router.push("/strategies")` in the ~`:341-370` draft-gone/error branch); `handleSubmitSuccess` `:550` (`router.push("/strategies?wizard_submitted=1")` + `router.refresh()`, comment at `:542` says "Wizard finalize sets status='pending_review'"); 404 non-csv branch `:594`; CSV submit success `:1053-1054`.
- Overlay analogs: `StrategyBrowseDrawer.tsx:71-77` (isOpen/onClose props), `:210-238,:322` (Esc handler + hooks-above-null-gate + `if (!isOpen) return null`); `ScenarioCommitDrawer.tsx:26-27,:1133-1139` (`createPortal(<div className="fixed inset-0 z-[200] ...">, document.body)`).

Cross-plan contract (FIXED — plan 110-04 implements the server side with these exact names):
- Finalize POST bodies (`/api/strategies/finalize-wizard`, `/api/strategies/csv-finalize`) gain an optional field `entry_context: "manager" | "contribution"`; the server defaults absent → `"manager"` (back-compat). Contribution mode MUST send `entry_context: "contribution"`.
- The finalize responses return `status: "private"` on the contribution branch (vs `"pending_review"`); do not hard-assert `"pending_review"` in any contribution-mode client code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parameterize WizardClient — entryContext, sourceOverride, onSuccess/onClose, copy branch</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx</files>
  <read_first>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx (props :56-120, terminal paths :341-370/:535-554/:589-600/:1050-1055, finalize fetch call sites); src/app/(dashboard)/strategies/new/wizard/page.tsx (:76-121 — the manager mount that must stay behavior-identical); any existing WizardClient/WizardChrome test files (extend, do not fork)</read_first>
  <behavior>
    - Test 1 (default/manager mode): mounting with only `initialDraft` behaves exactly as today — existing tests pass UNMODIFIED except where they assert the new optional props' defaults.
    - Test 2 (contribution mode success): with `entryContext="contribution"` and mocked finalize fetch, submit success calls `onSuccess(strategyId)` and NEVER calls router.push (spy on useRouter mock).
    - Test 3 (contribution mode fail-safe): the draft-gone/404 branches call `onClose()` — no router.push.
    - Test 4 (payload): in contribution mode the finalize POST body contains `entry_context: "contribution"`; in manager mode it contains `entry_context: "manager"` (or omits it — server defaults to manager; pick ONE and assert it).
  </behavior>
  <action>
Extend `WizardClientProps` with optional `entryContext?: "manager" | "contribution"` (default `"manager"`), `sourceOverride?: "api" | "csv"`, `onSuccess?: (strategyId: string) => void`, `onClose?: () => void`. Parameterize in place — do NOT fork the component (locked: single source of wizard truth for Phase 116).

Source derivation: `const source = sourceOverride ?? (searchParams.get("source") === "csv" ? "csv" : "api")` — the `useSearchParams()` hook keeps running unconditionally (hooks rules) but contribution mode never depends on route searchParams (Pitfall 3).

Terminal paths: at each of `:357`, `:362`, `:594` replace the unconditional `router.push("/strategies")` with: contribution mode → `onClose?.()`; manager mode → existing push (unchanged). At `:550` and `:1053` replace the success `router.push("/strategies?wizard_submitted=1"); router.refresh()` with: contribution mode → `onSuccess?.(strategyId)` (thread the finalized strategy id from the finalize response); manager mode → unchanged. Update the `:542` and `:1050` comments: finalize terminal status is `'pending_review'` in manager mode and `'private'` in contribution mode (per D — CONTRIB-02).

Finalize payloads: add `entry_context: entryContext` to BOTH the finalize-wizard and csv-finalize POST bodies (exact snake_case field name per the cross-plan contract). Do not assert `status === "pending_review"` from the response on the contribution branch.

Copy branch (locked: no sell-side copy for allocators): grep WizardClient.tsx AND WizardChrome.tsx for sell-side strings ("Submit for review", "your investors", "publish", "review"); where they render in contribution mode, branch on `entryContext` to allocator-framed copy (e.g. submit CTA "Add to my strategies"; explanatory line noting the strategy stays private to the owner). Keep manager copy byte-identical. If WizardChrome needs the flag, thread `entryContext` down as a prop.
  </action>
  <acceptance_criteria>
- Zero unconditional `router.push("/strategies"...)` remain: every one is behind the manager-mode branch (grep gate below counts pushes NOT guarded — verify by test, not grep alone: Tests 2-3).
- `wizard/page.tsx` requires NO edits (all new props optional with manager defaults).
- Contribution-mode finalize bodies carry `entry_context: "contribution"` (Test 4).
- Manager-mode snapshot/behavior tests green without weakening.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard" --no-file-parallelism && grep -c "entry_context" "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx" # expect >=2 (both finalize payloads)</automated>
  </verify>
  <done>WizardClient is dual-mountable: manager page unchanged, contribution mode fully callback-driven with allocator copy and the entry_context payload field.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build ContributionWizardOverlay — the reusable createPortal unit</name>
  <files>src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx, src/app/(dashboard)/allocations/components/ContributionWizardOverlay.test.tsx</files>
  <read_first>src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx (:71-77 props contract, :210-238 Esc effect, :322 null gate — copy this hook discipline); src/app/(dashboard)/allocations/components/ScenarioCommitDrawer.tsx (:26-27, :1133-1139 createPortal shell); src/components/ui/Modal.tsx (dialog shell alternative); DESIGN.md (overlay visual treatment)</read_first>
  <behavior>
    - Test 1: `isOpen=false` renders nothing (null gate).
    - Test 2: `isOpen=true` renders the portal panel containing the (mocked) WizardClient with `entryContext="contribution"`, `initialDraft=null`, and the callbacks wired.
    - Test 3: Escape keydown calls `onClose`.
    - Test 4: toggling the internal source selector (API key ↔ CSV) remounts WizardClient (key change observable via the mock) with the matching `sourceOverride`.
    - Test 5: the mocked WizardClient invoking its `onSuccess("id-1")` propagates to the overlay's `onSuccess` prop with "id-1".
  </behavior>
  <action>
Create `"use client"` component `ContributionWizardOverlay` with props `{ isOpen: boolean; onClose: () => void; onSuccess?: (strategyId: string) => void }`. Structure (per PATTERNS, layered analogs):
- All hooks ABOVE the `if (!isOpen) return null` gate (StrategyBrowseDrawer discipline); Esc-key `useEffect` keyed on `[isOpen, onClose]`.
- `createPortal` to `document.body` with the ScenarioCommitDrawer fixed-inset backdrop shell (`z-[200]`, DESIGN.md-conformant panel).
- Internal `const [source, setSource] = useState<"api" | "csv">("api")` + a small two-option selector (API key / CSV upload) rendered in the overlay header — needed because the manager page's CSV switch is URL-driven and the overlay has no route searchParams (Pitfall 3). Reset source to "api" when the overlay closes.
- Mount `<WizardClient key={source} entryContext="contribution" sourceOverride={source} initialDraft={null} onSuccess={(id) => onSuccess?.(id)} onClose={onClose} />` — the `key` drives the CSV↔API remount exactly like `wizard/page.tsx:120-121` does with URL keying.
- `initialDraft={null}` = fresh wizard per open (discretion: overlay does not resume server drafts in 110; abandoned overlay drafts are reaped by the cleanup cron from 20260713120000 — document in a comment).
- FORBIDDEN in this file: `useSearchParams`, `router.push`, any import of `next/navigation` for navigation (the whole point is zero URL change).
This component is the Phase 116 reuse unit — keep it presentation-agnostic of its trigger (nav item, Browse CTA, future "+ Allocation" all just control `isOpen`).
  </action>
  <acceptance_criteria>
- All 5 behavior tests pass with WizardClient mocked.
- `grep -c "useSearchParams" ContributionWizardOverlay.tsx` = 0 and `grep -c "router.push" ContributionWizardOverlay.tsx` = 0.
- Component exports exactly the `{ isOpen, onClose, onSuccess? }` contract (plan 110-05 and Phase 116 mount against it).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.test.tsx" --no-file-parallelism && grep -c "useSearchParams" "src/app/(dashboard)/allocations/components/ContributionWizardOverlay.tsx" # expect 0</automated>
  </verify>
  <done>The reusable inline-overlay wizard exists: portal-mounted, Esc-dismissable, source-toggling via keyed remount, zero navigation, contribution-mode wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| overlay client → finalize APIs | entry_context is client-supplied; must never be able to cause publication (server guard in plans 01/04) |
| allocator session → manager-guarded routes | any navigation to /strategies bounces the allocator (Phase 109 guard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-09 | Denial of Service (UX-loss) | wizard terminal redirects bouncing allocators | mitigate | every terminal path callback-parameterized in contribution mode; Tests 2-3 pin no router.push |
| T-110-10 | Tampering | client-supplied entry_context | transfer | neither value can publish — the RPC terminal-status guard (plan 110-01 T-110-02) is the enforcement; client field is a routing hint only (documented in code comment) |
| T-110-11 | Information Disclosure | sell-side copy leaking manager framing to allocators | mitigate | entryContext copy branch; manager copy byte-identical |
| T-110-SC | Tampering | package installs | accept | zero new dependencies |
</threat_model>

<verification>
- Wizard + overlay test files green; manager-mode behavior unchanged (existing wizard tests pass).
- No useSearchParams / router.push in the overlay; no unconditional /strategies push in WizardClient.
</verification>

<success_criteria>
- A single WizardClient serves both mounts; the overlay is a drop-in reusable unit with the `{isOpen, onClose, onSuccess}` contract that plan 110-05 (nav + CTA) and Phase 116 ("+ Allocation") consume.
</success_criteria>

<output>
Create `.planning/phases/110-contrib-private-by-default-contribution/110-03-SUMMARY.md` when done.
</output>
