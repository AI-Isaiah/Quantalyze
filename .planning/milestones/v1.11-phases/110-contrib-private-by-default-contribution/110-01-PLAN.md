---
phase: 110-contrib-private-by-default-contribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260716130000_strategies_status_private.sql
  - supabase/migrations/20260716130500_finalize_terminal_status_param.sql
  - supabase/tests/test_strategies_private_owner_isolation.sql
  - supabase/schema/functions/finalize_wizard_strategy.sql
  - supabase/schema/functions/finalize_csv_strategy.sql
autonomous: false
requirements: [CONTRIB-02, CONTRIB-04]
must_haves:
  truths:
    - "A strategies row can hold status='private' (CHECK constraint admits it)"
    - "The finalize RPCs can terminate a draft at status='private' when and only when asked; 'published' can never be a finalize terminal status"
    - "As user B, a SELECT against user A's status='private' strategy returns ZERO rows (RLS layer); user A's own SELECT returns it"
  artifacts:
    - path: "supabase/migrations/20260716130000_strategies_status_private.sql"
      provides: "status CHECK widened to include 'private'"
      contains: "strategies_status_check"
    - path: "supabase/migrations/20260716130500_finalize_terminal_status_param.sql"
      provides: "finalize_wizard_strategy + finalize_csv_strategy re-created with p_terminal_status param"
      contains: "p_terminal_status"
    - path: "supabase/tests/test_strategies_private_owner_isolation.sql"
      provides: "RLS cross-owner isolation assertion (CONTRIB-04 DB layer)"
      contains: "private"
  key_links:
    - from: "supabase/migrations/20260716130500_finalize_terminal_status_param.sql"
      to: "strategies.status"
      via: "UPDATE/INSERT ... status = p_terminal_status"
      pattern: "p_terminal_status"
    - from: "supabase/tests/test_strategies_private_owner_isolation.sql"
      to: "strategies_read RLS policy"
      via: "request.jwt.claims session switch + content-assert"
      pattern: "request\\.jwt\\.claims"
---

<objective>
Create the DB unit for private-by-default contribution: widen the strategies `status` CHECK to admit `'private'`, re-base and re-create the two SECURITY DEFINER finalize RPCs with a guarded `p_terminal_status` parameter, prove cross-owner isolation at the RLS layer with a new SQL test, and MCP-apply everything to the test project before merge.

Purpose: CONTRIB-02's core trap — the existing wizard finalizes at `status='pending_review'`, which is a PUBLISH CANDIDATE (`admin/page.tsx:40` lists it; `admin/strategy-review/route.ts:175` promotes it to `published`). A contribution must terminate at an owner-only status that is never a publish candidate. RLS `strategies_read` (`status='published' OR user_id=auth.uid()`) already makes `'private'` owner-visible and never public — NO RLS policy migration (REQUIREMENTS non-goal).

Output: two migrations (sorted after `20260716120000`), regenerated SQL-fn snapshots, one RLS isolation test, all applied to test project qmnijlgmdhviwzwfyzlc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/110-contrib-private-by-default-contribution/110-CONTEXT.md
@.planning/phases/110-contrib-private-by-default-contribution/110-RESEARCH.md
@supabase/migrations/20260602180000_funding_fees_exchange_check.sql
@supabase/migrations/20260521185008_wizard_finalize_inserts_verification.sql
@supabase/schema/functions/finalize_wizard_strategy.sql
@supabase/schema/functions/finalize_csv_strategy.sql
@supabase/tests/test_strategy_keys_rls.sql

<interfaces>
Verified function lineage (grep of all migrations, 2026-07-16):
- `finalize_wizard_strategy` — LATEST def: `20260521185008_wizard_finalize_inserts_verification.sql` (also the snapshot source per `supabase/schema/functions/finalize_wizard_strategy.sql` header). Guard gauntlet at :57-106 (auth.uid() present, auth.uid()=p_user_id, SELECT...FOR UPDATE, owner-match, source='wizard', status='draft'); status write at :108-125 (`status = 'pending_review'`); strategy_verifications insert at :135-158; footer `SET search_path = public, pg_catalog` + REVOKE FROM PUBLIC, anon + GRANT EXECUTE TO authenticated at :167-168.
- `finalize_csv_strategy` — LATEST def: `20260501055202_strategy_verifications.sql:185` (snapshot confirms). Atomically INSERTs the strategies row with `status='pending_review'` (snapshot ~:88) + a strategy_verifications row (`status='validated'`, `trust_tier='csv_uploaded'`).
- Current status CHECK: `initial_schema.sql:63` — `CHECK (status IN ('draft','pending_review','published','archived'))`, constraint name `strategies_status_check`.
- Latest migration on disk: `20260716120000_backfill_staff_role_both.sql` — new files MUST sort after it.

Cross-plan contract (fixed — plans 03/04 depend on these exact names):
- New RPC parameter on BOTH functions: `p_terminal_status TEXT DEFAULT 'pending_review'`, guarded to `('pending_review','private')`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Widen the status CHECK and re-create both finalize RPCs with a guarded p_terminal_status param</name>
  <files>supabase/migrations/20260716130000_strategies_status_private.sql, supabase/migrations/20260716130500_finalize_terminal_status_param.sql, supabase/schema/functions/finalize_wizard_strategy.sql, supabase/schema/functions/finalize_csv_strategy.sql</files>
  <read_first>supabase/migrations/20260602180000_funding_fees_exchange_check.sql (the CHECK DROP-then-ADD idiom analog, :36-81); supabase/schema/functions/finalize_wizard_strategy.sql and finalize_csv_strategy.sql (the CANONICAL latest bodies to re-base on — do NOT re-base on an older migration); supabase/migrations/20260521185008_wizard_finalize_inserts_verification.sql (footer conventions)</read_first>
  <action>
Migration A — `20260716130000_strategies_status_private.sql` (clone the funding_fees_exchange_check structure: BEGIN / pre-flight fail-loud / DROP CONSTRAINT IF EXISTS / ADD CONSTRAINT / self-verifying DO block / COMMIT): drop `strategies_status_check` and re-add as `CHECK (status IN ('draft','pending_review','published','archived','private'))`. This is a CHECK widening, NOT an RLS-policy migration (the REQUIREMENTS non-goal rejects only a policy migration; `strategies_read` at `rls_policies.sql:28-30` already makes `'private'` owner-visible + never-public per D — CONTEXT open-question resolution [KEY]).

Migration B — `20260716130500_finalize_terminal_status_param.sql`: re-base BOTH function bodies byte-for-byte on the snapshot files in `supabase/schema/functions/` (the memory rule: grep ALL migrations + re-base on the LATEST def — the snapshots ARE the replayed latest defs), then for each function: (1) `DROP FUNCTION IF EXISTS` with the EXACT existing signature — required because adding a parameter under CREATE OR REPLACE would create a second overload and break PostgREST rpc dispatch; (2) `CREATE FUNCTION` identical to the latest def PLUS an appended trailing parameter `p_terminal_status TEXT DEFAULT 'pending_review'`; (3) as the FIRST body statement add a guard: `IF p_terminal_status NOT IN ('pending_review','private') THEN RAISE EXCEPTION` — this is the server-side enforcement that no caller (including a direct PostgREST call by an authenticated user) can ever finalize to `'published'`; (4) replace the hardcoded `'pending_review'` terminal write with `p_terminal_status` (`finalize_wizard_strategy`: the `UPDATE strategies SET ... status = ...`; `finalize_csv_strategy`: the strategies-row INSERT status column); (5) KEEP the `strategy_verifications` insert in both functions on BOTH terminal statuses — trust-tier provenance (`api_verified`/`csv_uploaded`) is a data-quality label the owner's surfaces read, NOT a publish signal (Claude's-discretion resolution of RESEARCH Open Q3; document this in a SQL comment); (6) keep everything else byte-identical: SECURITY DEFINER, `SET search_path = public, pg_catalog`, all guard gauntlet assertions, `REVOKE ALL ... FROM PUBLIC, anon` + `GRANT EXECUTE ... TO authenticated` footer (re-issue REVOKE/GRANT after the DROP+CREATE since the drop discards grants).

Then run `npm run schema:functions` to regenerate the snapshot files (the SQL-fn-snapshot CI guard requires it).
  </action>
  <acceptance_criteria>
- Both migration filenames sort strictly after `20260716120000_backfill_staff_role_both.sql`.
- Migration A contains `DROP CONSTRAINT IF EXISTS strategies_status_check` and the 5-value CHECK including `'private'`; contains a self-verify DO block; contains NO `CREATE POLICY`/`ALTER POLICY`.
- Migration B contains `DROP FUNCTION IF EXISTS finalize_wizard_strategy` and `DROP FUNCTION IF EXISTS finalize_csv_strategy` (exact old signatures), both re-created with `p_terminal_status TEXT DEFAULT 'pending_review'`, a `NOT IN ('pending_review','private')` RAISE guard, SECURITY DEFINER, search_path pin, and REVOKE/GRANT footer.
- `git diff supabase/schema/functions/` shows both snapshots now carry `p_terminal_status` (regenerated, not hand-edited).
  </acceptance_criteria>
  <verify>
    <automated>ls supabase/migrations/ | sort | tail -3 | head -2 | grep -c "2026071613" # expect 2; grep -c "p_terminal_status" supabase/migrations/20260716130500_finalize_terminal_status_param.sql # expect >=6; grep -v '^--' supabase/migrations/20260716130000_strategies_status_private.sql | grep -c "CREATE POLICY" # expect 0; npm run schema:functions && git diff --exit-code supabase/schema/functions/ # expect clean after regen</automated>
  </verify>
  <done>Two migrations exist, sorted after 20260716120000; both RPCs re-based on the latest defs with the guarded param; snapshots regenerated and in sync.</done>
</task>

<task type="auto">
  <name>Task 2: RLS cross-owner isolation SQL test for status='private'</name>
  <files>supabase/tests/test_strategies_private_owner_isolation.sql</files>
  <read_first>supabase/tests/test_strategy_keys_rls.sql (the canonical tenant-isolation analog — copy its structure: header doc block :1-37, defensive pre-clean :44-48, BEGIN/fixtures/ROLLBACK :50-55, plain PL/pgSQL DO with RAISE EXCEPTION — NO pgTAP); supabase/migrations/20260405061912_rls_policies.sql:28-33 (strategies_read policy under test)</read_first>
  <action>
Create `supabase/tests/test_strategies_private_owner_isolation.sql` cloning the `test_strategy_keys_rls.sql` structure verbatim (header documenting "RLS fails silently — assert by CONTENT/count, not by error"; pre-clean `DELETE FROM auth.users WHERE email IN (...)` for the two fixture users; `BEGIN;` ... `ROLLBACK;`; `DO $$` blocks with `RAISE EXCEPTION` on mismatch; runs under `psql -v ON_ERROR_STOP=1` in the sql-tests CI job).

Fixtures: two users uid_a / uid_b (gen_random_uuid(), inserted into auth.users); one strategies row owned by uid_a with `status='private'` (this INSERT is itself the CHECK-widen probe — it fails loudly if migration A is missing); one strategies row owned by uid_a with `status='published'` (control).

Assertions (all content-asserted, both directions):
1. As uid_b (set `request.jwt.claims` + `SET LOCAL ROLE authenticated` exactly like the analog): SELECT count of uid_a's private row → MUST be 0.
2. As uid_b: SELECT count of the published control row → MUST be 1 (proves the session switch works and the 0 above is isolation, not a broken harness).
3. As uid_a: SELECT count of own private row → MUST be 1.
4. As anon (analog's anon-role switch): private-row count → MUST be 0.
5. Guard pin: as uid_a, call `finalize_wizard_strategy` with `p_terminal_status => 'published'` against a fixture draft and assert it RAISES (EXCEPTION block catching the RAISE and re-raising if it did NOT fail) — pins the never-published invariant at the DB layer.
  </action>
  <acceptance_criteria>
- File matches the `test_*.sql` glob (runs in the sql-tests CI job) and contains BEGIN + ROLLBACK, `request.jwt.claims`, and zero pgTAP calls.
- Contains all 5 assertions above; every assertion is a count/content check inside a DO block with RAISE EXCEPTION on failure — no assertion passes vacuously on empty-set.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ROLLBACK" supabase/tests/test_strategies_private_owner_isolation.sql # >=1; grep -c "request.jwt.claims" supabase/tests/test_strategies_private_owner_isolation.sql # >=2; grep -ci "pgtap" supabase/tests/test_strategies_private_owner_isolation.sql # expect 0. Full execution happens in Task 3 (test project) + CI sql-tests job.</automated>
  </verify>
  <done>Isolation test authored, content-asserted in both directions, RED-guarded on the migration (fails loudly until migration A is applied).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: MCP-apply migrations to the test project and run the isolation test</name>
  <what-built>Two migrations (status CHECK widen + finalize RPC p_terminal_status) and a RED-guarded RLS isolation test. The executor has NO Supabase MCP access — the orchestrator/user must apply them to the test project qmnijlgmdhviwzwfyzlc BEFORE merge (test-DB catch-up rule: RED-guarded SQL tests fail CI otherwise; merging supabase/migrations/** to main auto-applies to PROD afterwards).</what-built>
  <how-to-verify>
1. Via Supabase MCP against project qmnijlgmdhviwzwfyzlc: `apply_migration` for `20260716130000_strategies_status_private.sql`, then `20260716130500_finalize_terminal_status_param.sql` (in order).
2. ⚠️ apply_migration stamps `now()` into schema_migrations, not the file timestamp — after applying, UPDATE the two schema_migrations rows' version to `20260716130000` / `20260716130500` (or manual INSERT) so prod auto-apply on merge does not double-apply.
3. Via MCP `execute_sql`, run the full contents of `supabase/tests/test_strategies_private_owner_isolation.sql` (it is self-contained BEGIN...ROLLBACK) → expect completion with no exception.
4. Sanity: `SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='strategies_status_check'` → contains `'private'`; `SELECT count(*) FROM pg_proc WHERE proname='finalize_wizard_strategy'` → exactly 1 (no overload left behind).
  </how-to-verify>
  <files>supabase/migrations/20260716130000_strategies_status_private.sql, supabase/migrations/20260716130500_finalize_terminal_status_param.sql (applied to test project — no file edits in this task)</files>
  <action>PAUSE for the orchestrator/user (executor has NO Supabase MCP): apply both migrations to test project qmnijlgmdhviwzwfyzlc via MCP apply_migration in filename order, fix the schema_migrations version rows to the file timestamps (apply_migration stamps now() — rename/UPDATE to 20260716130000 / 20260716130500 so prod auto-apply on merge does not double-apply), then run the isolation test via MCP execute_sql, following the numbered steps in how-to-verify.</action>
  <verify>
    <automated>MISSING — human/orchestrator MCP checkpoint by design: executor has no Supabase MCP; verification is the how-to-verify steps (isolation test completes with no exception; constraint def contains 'private'; exactly one finalize_wizard_strategy overload). CI's sql-tests job re-proves it post-merge.</automated>
  </verify>
  <done>Both migrations live on qmnijlgmdhviwzwfyzlc with correct schema_migrations versions; test_strategies_private_owner_isolation.sql ran green via MCP; no function overloads left behind.</done>
  <resume-signal>Type "applied" once both migrations are on the test project and the isolation test ran green, or describe the failure</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PostgREST → SECDEF RPC | any authenticated user can invoke the finalize RPCs directly with arbitrary args |
| authenticated reader → strategies table | RLS is the only barrier between user B and user A's private rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Information Disclosure | strategies_read RLS vs status='private' | mitigate | new SQL test content-asserts non-owner sees 0 rows (Task 2 assertions 1+4); no policy change needed |
| T-110-02 | Elevation of Privilege | finalize RPC p_terminal_status | mitigate | RAISE guard restricting terminal status to ('pending_review','private') — 'published' unreachable from any caller (Task 1 step 3, pinned by Task 2 assertion 5) |
| T-110-03 | Tampering | RPC ownership/from-draft gauntlet | mitigate | re-base keeps the existing auth.uid()=p_user_id + FOR UPDATE + status='draft' guards byte-identical |
| T-110-04 | Elevation of Privilege | dropped-and-recreated fn grants | mitigate | re-issue REVOKE FROM PUBLIC, anon + GRANT TO authenticated after DROP+CREATE (SECDEF in-policy anon-EXECUTE lesson does not apply — fn not called in a policy) |
| T-110-SC | Tampering | package installs | accept | zero new dependencies this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- Both migrations sorted after 20260716120000; snapshots regenerated in the same commit.
- Isolation test green on the test project (checkpoint) and in the CI sql-tests job.
- No RLS policy created/altered anywhere in this plan.
</verification>

<success_criteria>
- `'private'` is a valid strategies.status; finalize RPCs accept p_terminal_status limited to ('pending_review','private'); cross-owner isolation content-asserted at the RLS layer; migrations live on the test project before merge.
</success_criteria>

<output>
Create `.planning/phases/110-contrib-private-by-default-contribution/110-01-SUMMARY.md` when done (include the schema_migrations timestamp-fix confirmation from the checkpoint).
</output>
