---
phase: 115.1-e2-display-repoint-worker-side-flow-aware-equity
plan: 04
type: execute
wave: 3
depends_on: ["115.1-02", "115.1-03"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_derive_allocator_equity_job.py
autonomous: true
requirements: [BACKBONE-02, STITCH-01]
must_haves:
  truths:
    - "The key-mode derive epilogue persists per-key real flows + LIVE anchor to allocator_equity_derived (kind='key_inputs:<api_key_id>') — Option B, no second crawl/decryption ever needed"
    - "The epilogue enqueues derive_allocator_equity for ctx.key_row['user_id'] (authoritative owner — NEVER the job payload)"
    - "run_derive_allocator_equity_job is crawl-free and decryption-free: DB reads → compose_allocator_equity → single-row atomic upsert of (allocator_id,'equity_curve')"
    - "pd.Series indices are built DIRECTLY from the csv_daily_returns 'date' string column — no to_datetime anywhere on the path"
    - "Nothing on the new path writes allocator_equity_snapshots (fake-PostgREST raise pin green)"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "key-mode epilogue persist+enqueue; run_derive_allocator_equity_job; TIMEOUT_PER_KIND + dispatch registration"
      contains: "run_derive_allocator_equity_job"
    - path: "analytics-service/tests/test_derive_allocator_equity_job.py"
      provides: "All plan-01 job pins GREEN (persistence, no-legacy-write, enqueue-gap, owner)"
  key_links:
    - from: "run_derive_broker_dailies_job key-mode return site (job_worker.py ~:2935-2946)"
      to: "allocator_equity_derived kind='key_inputs:*' upsert + derive_allocator_equity enqueue"
      via: "epilogue inserted BEFORE the key-mode DispatchResult(DONE) return"
      pattern: "key_inputs:"
    - from: "run_derive_allocator_equity_job"
      to: "services.allocator_equity_compose.compose_allocator_equity"
      via: "the ONLY derivation call"
      pattern: "compose_allocator_equity"
    - from: "dispatch() elif chain (job_worker.py:5796+)"
      to: "run_derive_allocator_equity_job"
      via: "kind='derive_allocator_equity' branch"
      pattern: "derive_allocator_equity"
---

<objective>
Wire the worker: (1) an Option-B epilogue in the EXISTING key-mode
`run_derive_broker_dailies_job` that persists the per-key real flows + live equity anchor it
ALREADY fetched (crawl ~:2582-2624, anchor ~:2529-2532, decryption via
`_allocator_key_preflight` :736) and enqueues the compose job; (2) the new crawl-free
`derive_allocator_equity` handler that assembles DB inputs, calls the plan-03 compose
module, and atomically upserts the display row. Turns plan-01's job pins green.

Purpose: production derivation path, self-healing (any key refresh re-composes the
allocator), zero added exchange I/O. Output: worker code + green job tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-RESEARCH.md
@analytics-service/services/job_worker.py
@analytics-service/services/allocator_equity_compose.py
@analytics-service/tests/test_derive_allocator_equity_job.py
@analytics-service/tests/test_derive_broker_dailies_dualmode.py
</context>

<interfaces>
From plan 02 (DB): `allocator_equity_derived (allocator_id, kind, payload, computed_at)`,
PK `(allocator_id, kind)`; kinds `'equity_curve'` and `'key_inputs:<api_key_id>'`.
key_inputs payload: `{flows: [{utc_day_iso, usd_signed}], anchor_usd, anchor_asof, venue}`.

From plan 03 (Python):
`compose_allocator_equity(returns_by_key, flows_by_key, anchors_by_key) -> payload dict`.

Existing worker symbols (verified): `is_key_mode` branch + `allocator_id = ctx.key_row["user_id"]`
(:1914), ccxt anchor `equity` from `fetch_account_equity_and_upnl_usd` (:2532), ccxt flows
`external_flows = ccxt_rows_to_dated_flows(...)` (:2614-2617), deribit flows
`_completeness.dated_external_flows` (:2518), key-mode DONE return (:2935-2946),
`TIMEOUT_PER_KIND` (:246-261), `dispatch()` if/elif (:5796+, unknown kinds permanent-FAILED
:5870-5875), `db_execute` wrapper pattern (used throughout the persist section :2907-2934).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Option-B epilogue — persist per-key flows + live anchor, enqueue compose</name>
  <files>analytics-service/services/job_worker.py</files>
  <action>
    Insert an epilogue in `run_derive_broker_dailies_job` INSIDE the `if is_key_mode:` block
    that currently logs and returns `DispatchResult(DONE)` (~:2935-2946), BEFORE the return.
    Steps:

    1. Assemble the key_inputs payload: `flows` = the job's `external_flows` list serialized
       as `[{"utc_day_iso": f.utc_day_iso, "usd_signed": f.usd_signed}]` (both venue
       branches already produce `list[ExternalFlow]` — deribit via
       `_completeness.dated_external_flows`, ccxt via `ccxt_rows_to_dated_flows`).
       `anchor_usd`: ccxt branch → the live `equity` value already fetched via
       `fetch_account_equity_and_upnl_usd` (the venue's own total-equity read — deliberately
       NOT an allocator_holdings sum, avoiding the derivative-notional trap where value_usd
       is notional and equity is unrealized_pnl_usd). Deribit branch → locate the native
       core's terminal/current equity in the reconstruction result (read the deribit branch;
       the native core reconstructs to current account state). If the deribit terminal
       equity is genuinely not reachable from the epilogue scope, persist
       `anchor_usd: null` for deribit keys and record in the SUMMARY that the compose job's
       fallback (documented, honest) is an `allocator_holdings` sum that MUST mirror
       `holdingEquityContribution` (spot → value_usd; derivative → unrealized_pnl_usd,
       queries.ts:2046-2056) — do NOT silently sum value_usd. `anchor_asof`: now-UTC ISO
       timestamp; `venue`: the branch venue. If the anchor read was flagged untrustworthy in
       the ccxt branch (`balance_error` truthy or `equity is None`), persist
       `anchor_usd: null` — never a heuristic anchor.
    2. Upsert to `allocator_equity_derived` with `kind = "key_inputs:" + api_key_id`,
       on_conflict="allocator_id,kind", via the existing `db_execute` wrapper pattern.
       Transient DB errors bubble (job retries; the csv_daily_returns upsert above is
       idempotent) — no silent swallow, no outcome downgrade.
    3. Enqueue the compose job: mirror an existing worker-side enqueue (the sync_trades
       epilogue :1504-1534 pattern) for kind `'derive_allocator_equity'` with allocator
       target `ctx.key_row["user_id"]` — the T-115-16 authoritative-owner rule; NEVER a
       job-payload allocator_id. A unique-violation from the one-inflight index is benign
       (log + continue — mirror how existing enqueues treat dedup collisions).
    4. Log line (no USD magnitudes): key id, allocator id, venue, n_flows, anchor-present
       bool.

    Surgical-change discipline: touch ONLY the key-mode epilogue region; do not reorder or
    reformat the surrounding persist/reconcile code; strategy-mode is untouched.
  </action>
  <verify>
    <automated>cd analytics-service && python -c "import services.job_worker" && grep -n "key_inputs:" services/job_worker.py | head -1 && grep -n 'key_row\["user_id"\]' services/job_worker.py | grep -c . | awk '$1>=2'</automated>
  </verify>
  <done>Epilogue persists flows+anchor and enqueues compose before the key-mode DONE return; owner sourced from ctx.key_row["user_id"]; module imports; strategy-mode diff-free.</done>
</task>

<task type="auto">
  <name>Task 2: run_derive_allocator_equity_job — crawl-free compose handler + registration</name>
  <files>analytics-service/services/job_worker.py</files>
  <action>
    New async handler `run_derive_allocator_equity_job(job)` (place near the other allocator
    handlers; docstring: crawl-free, decryption-free DB-only compose — 115.1 RD-3 Option B):

    1. `allocator_id = job["allocator_id"]` (the queue target, written by the SECDEF enqueue
       RPC — trusted; the spoofing surface was the epilogue enqueue, already pinned).
    2. Load the allocator's api_keys and filter with `eligible_key_predicate` (import from
       services.allocator_equity_derive — the ONE definition; never inline a predicate).
    3. Load per-key `csv_daily_returns` rows for eligible key ids
       (`.eq("allocator_id", allocator_id)` + api_key_id scoping consistent with how the
       per-key axis is written — read the key-mode upsert payload builder to match columns).
       Build `returns_by_key[k] = pd.Series(values, index=[row["date"] for row in rows])` —
       the `date` column is already a 'YYYY-MM-DD' string from PostgREST; NEVER
       `pd.to_datetime`/`parse_dates`/`DatetimeIndex` (carry-in #3; the core hard-asserts
       and a DatetimeIndex would stringify to 'YYYY-MM-DD 00:00:00' and silently misalign
       flows vs returns).
    4. Load `allocator_equity_derived` rows with `kind LIKE 'key_inputs:%'` for the
       allocator; parse into `flows_by_key` (reconstruct `ExternalFlow(utc_day_iso, usd_signed)`)
       and `anchors_by_key` (anchor_usd, may be None → that key is honestly dropped by
       compose). Keys with returns but no key_inputs row → anchor None (same honest drop).
       Housekeeping: delete key_inputs rows whose api_key_id is no longer in the eligible
       set (cheap orphan cleanup; bounded `.like("kind", "key_inputs:%")` delete restricted
       to this allocator_id).
    5. `payload = compose_allocator_equity(returns_by_key, flows_by_key, anchors_by_key)` —
       the ONLY derivation call. Zero eligible keys or zero returns → still upsert the
       honest untrustworthy payload (empty curve, reasons) — the frontend gate reads
       `is_trustworthy`, and an honest row beats a stale one.
    6. Single-row atomic upsert `(allocator_id, 'equity_curve', payload, computed_at=now)`
       on_conflict="allocator_id,kind" via db_execute. NEVER touch
       `allocator_equity_snapshots` (the fake-PostgREST pin enforces this forever).
    7. Structural compose errors (the compose module's loud asserts) → permanent FAILED with
       a `scrub_freeform_string`-scrubbed message (the 115-04 disposition pattern); DB/
       network errors → bubble transient.

    Registration: add `"derive_allocator_equity": 5 * 60` to TIMEOUT_PER_KIND (:246-261,
    with a "pure DB + math, no exchange I/O" comment); add the `elif` in dispatch()
    (:5796+, keep the if/elif style — it is deliberate for test monkeypatching). 5 min < 10
    min → no WATCHDOG_PER_KIND_OVERRIDES entry needed, but run
    `python -m pytest tests -k watchdog_headroom -q` to prove the invariant test agrees.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests -k "watchdog_headroom" -q && grep -q '"derive_allocator_equity": 5 \* 60' services/job_worker.py && python -c "import services.job_worker"</automated>
  </verify>
  <done>Handler registered in TIMEOUT_PER_KIND + dispatch; watchdog invariant test green; no to_datetime on the path; handler contains zero exchange/decryption calls.</done>
</task>

<task type="auto">
  <name>Task 3: Green the job pins + full worker regression</name>
  <files>analytics-service/tests/test_derive_allocator_equity_job.py</files>
  <action>
    Run the plan-01 job pins (persistence, no-legacy-write raise-pin, enqueue-gap,
    owner-from-key_row) against the real handler + epilogue using the fake-PostgREST
    harness. Fix harness/signature mismatches on the test side only; pinned behaviors are
    non-negotiable. Add one more pin now implementable: the epilogue with
    `balance_error`-flagged anchor persists `anchor_usd: null` (never a heuristic anchor).

    Then run the worker regression battery: the dual-mode derive suite (proves the epilogue
    did not disturb key-mode or strategy-mode), the deribit allocator suite, and the
    goldens.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_derive_allocator_equity_job.py tests/test_derive_broker_dailies_dualmode.py tests/test_e2_deribit_allocator_dailies.py tests/test_e2_match_score_golden.py tests/test_e1_delete_gate.py -q</automated>
  </verify>
  <done>All plan-01 job pins GREEN; null-anchor honesty pin added and green; dual-mode + deribit suites + goldens green (epilogue is regression-free).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job payload → handler | allocator/api_key targets cross from queue into worker |
| exchange responses → persisted flows/anchor | untrusted external data persisted as money-data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115.1-12 | Spoofing | compose-enqueue owner | mitigate | ctx.key_row["user_id"] only (T-115-16); pinned in job test |
| T-115.1-13 | Tampering | allocator_equity_snapshots | mitigate | fake-PostgREST raise pin + plan-06 whole-repo grep gate |
| T-115.1-14 | Info disclosure | error messages / logs | mitigate | scrub_freeform_string on dispositions; no USD in logs (T-115-05) |
| T-115.1-15 | DoS | retry loops on structural errors | mitigate | typed NavReconstructionError/compose asserts → permanent FAILED (the T-74-02 class) |
| T-115.1-16 | Tampering | fabricated anchor | mitigate | balance_error/None → anchor null → key honestly dropped; never heuristic |
</threat_model>

<verification>
- All plan-01 job pins green; dual-mode + deribit worker suites green; goldens byte-stable.
- Watchdog-headroom invariant test green with the new kind.
- `git diff services/allocator_equity_derive.py` still empty.
</verification>

<success_criteria>
- A key-mode derive run leaves behind: fresh csv_daily_returns (existing), a key_inputs row (new), and a pending compose job for the key's owner (new).
- The compose job produces exactly one atomic (allocator_id,'equity_curve') row from frozen-core math, with honest degradation on missing inputs.
</success_criteria>

<output>
Create `.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-04-SUMMARY.md` when done (record the deribit anchor resolution — direct vs holdings-fallback).
</output>
