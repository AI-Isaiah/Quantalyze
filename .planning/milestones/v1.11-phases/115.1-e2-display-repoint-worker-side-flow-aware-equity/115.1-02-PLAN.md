---
phase: 115.1-e2-display-repoint-worker-side-flow-aware-equity
plan: 02
type: execute
wave: 2
depends_on: ["115.1-01"]
files_modified:
  - supabase/migrations/<UTC-timestamp>_allocator_equity_derived_surface.sql
  - supabase/tests/test_allocator_equity_derived_rls.sql
  - supabase/tests/test_derive_allocator_keys_fanout.sql
  - src/lib/gdpr-export-manifest.ts
autonomous: true
requirements: [BACKBONE-02, BACKBONE-03]
user_setup:
  - service: supabase (prod)
    why: "Migrations AUTO-APPLY to PROD on merge to main — the merge itself is the prod DDL deploy"
    dashboard_config:
      - task: "Watch the prod migration run on merge; verify allocator_equity_derived + new kind + pg_cron job exist"
        location: "Supabase prod project khslejtfbuezsmvmtsdn — Database → Migrations / cron.job"
must_haves:
  truths:
    - "allocator_equity_derived exists with PK (allocator_id, kind), owner-SELECT RLS, service-role write, anon fully denied"
    - "compute_job_kinds admits 'derive_allocator_equity'; BOTH kind CHECKs are re-based on 20260710130000 (the api_key coherence arm SURVIVES — regression-pinned in SQL)"
    - "A recurring key-mode fan-out enqueues derive_broker_dailies per eligible allocator key daily (closes the P115 enqueue gap)"
    - "The new table is GDPR-manifested and the coverage script passes"
    - "The migration is applied to the TEST project via MCP BEFORE any SQL test runs (and never touches allocator_equity_snapshots or its jobs/cron)"
  artifacts:
    - path: "supabase/migrations/<UTC-timestamp>_allocator_equity_derived_surface.sql"
      provides: "Table + RLS + kind registration + re-based CHECKs + fan-out fn + pg_cron schedule + DO-block self-checks"
      contains: "allocator_equity_derived"
    - path: "supabase/tests/test_allocator_equity_derived_rls.sql"
      provides: "RLS gate: owner SELECT / cross-user deny / anon deny / service write"
    - path: "supabase/tests/test_derive_allocator_keys_fanout.sql"
      provides: "Fan-out eligibility + kind-CHECK admission + api_key-coherence-arm survival regression"
    - path: "src/lib/gdpr-export-manifest.ts"
      provides: "direct-ownership manifest entry for allocator_equity_derived"
      contains: "allocator_equity_derived"
  key_links:
    - from: "enqueue_derive_broker_dailies_for_allocator_keys()"
      to: "_enqueue_compute_job_internal (10-param overload, 20260420073003:330)"
      via: "p_api_key_id arg"
      pattern: "p_api_key_id"
    - from: "compute_jobs_kind_target_coherence"
      to: "20260710130000 definition (15-kind list + api_key arm)"
      via: "re-base copy + new arms"
      pattern: "derive_allocator_equity"
---

<objective>
Create the NEW keyed persistence surface `allocator_equity_derived` (one JSONB row per
`(allocator_id, kind)` — the `strategy_analytics_series` precedent, atomic
replace-on-upsert), register the new compute-job kind `derive_allocator_equity`, and close
the P115-flagged recurring key-mode enqueue gap with a daily pg_cron fan-out. This is the
PRODUCTION-TOUCHING migration of the phase: merging to main auto-applies to prod. Plan it
additive-only — the legacy `allocator_equity_snapshots` table, its jobs, its cron and its
constraints are NEVER touched.

Purpose: give plans 04/05 their surface and the derive pipeline its recurring trigger.
Output: one migration + two SQL test gates + GDPR manifest coverage, MCP-applied to TEST.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-RESEARCH.md
@supabase/migrations/20260710130000_stitch_composite_kind.sql
@supabase/migrations/20260428120919_strategy_analytics_series.sql
@supabase/migrations/20260420213754_allocator_equity_refresh.sql
@analytics-service/scripts/phase35_backfill_enqueue.py
@src/lib/gdpr-export-manifest.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — table + RLS + kind registration (re-based) + recurring fan-out + cron</name>
  <files>supabase/migrations/<UTC-timestamp>_allocator_equity_derived_surface.sql</files>
  <action>
    Name the file with the CURRENT UTC timestamp (`date -u +%Y%m%d%H%M%S`) — it must sort
    after 20260716131000. Follow the repo migration house style (numbered STEP sections,
    WHY-comments, terminal DO-block self-checks — mirror 20260710130000's structure).

    STEP 1 — table (per `strategy_analytics_series` precedent 20260428120919:71-88):
    `CREATE TABLE allocator_equity_derived (allocator_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE, kind text NOT NULL, payload jsonb NOT NULL, computed_at timestamptz NOT NULL DEFAULT now(), PRIMARY KEY (allocator_id, kind))`.
    Comment the two kind families: `'equity_curve'` (display row) and
    `'key_inputs:<api_key_id>'` (Option-B per-key flows+anchor rows). ON DELETE CASCADE from
    auth.users is the GDPR sanitize path — do NOT add any delete-guard trigger (if one is
    ever added later it must exempt `quantalyze.sanitize_in_progress` — memory
    reference_sanitize_user_delete_guard_exemption; state this in a comment).

    STEP 2 — RLS: ENABLE + FORCE row level security. Policies: service_role ALL (the worker
    writes with service key), authenticated owner SELECT `USING (allocator_id = auth.uid())`
    (the SSR read in plan 05 uses the USER client — the phase36 per-key pattern,
    20260624120000 step-4 comment). NO anon policy, NO owner INSERT/UPDATE/DELETE. If any
    SECURITY DEFINER helper is referenced in a `{public}` policy (should not be needed here),
    remember the anon-EXECUTE trap (memory reference_secdef_public_policy_needs_anon_execute).

    STEP 3 — kind registration, re-based on migration 20260710130000 (VERIFIED still the
    latest CHECK definition — 20260716090000 is an RPC guard only and redefines NEITHER
    constraint; re-verify with `grep -l "ADD CONSTRAINT compute_jobs_kind_check" supabase/migrations/*.sql`
    before writing):
      a. `INSERT INTO compute_job_kinds (name) VALUES ('derive_allocator_equity') ON CONFLICT (name) DO NOTHING;`
      b. DROP+ADD `compute_jobs_kind_check`: copy 20260710130000's full 16-entry array
         VERBATIM and append `'derive_allocator_equity'`.
      c. DROP+ADD `compute_jobs_kind_target_coherence`: copy 20260710130000's full
         definition VERBATIM — it carries the `derive_broker_dailies` api_key arm that older
         defs drop (dropping it silently kills key-mode derives) — and add the arm
         `kind = 'derive_allocator_equity' → allocator_id IS NOT NULL AND strategy_id IS NULL AND portfolio_id IS NULL AND api_key_id IS NULL`.
      d. In-flight dedup: NO new index needed — `compute_jobs_one_inflight_per_kind_allocator`
         (20260418194206:151, generic on `(allocator_id, kind)` for in-flight statuses)
         already covers the new kind, and the api_key-scoped index from 20260420073003
         covers the fan-out. Add a comment stating this reuse; do not create duplicates.

    STEP 4 — recurring key-mode fan-out fn `enqueue_derive_broker_dailies_for_allocator_keys()`
    SECURITY DEFINER, mirroring `enqueue_refresh_allocator_equity_for_all`
    (20260420213754:314-361: advisory-lock skip, per-key loop, exception-swallow per key with
    NOTICE) but enqueueing kind `'derive_broker_dailies'` via the 10-param
    `_enqueue_compute_job_internal` with `p_api_key_id := <key id>` and ALL other targets
    NULL (coherence: the api_key arm). Eligibility predicate — byte-match
    `eligible_key_predicate` (allocator_equity_derive.py:187-203) and the phase35 SQL:
    `is_active = true AND sync_status IS DISTINCT FROM 'revoked' AND disconnected_at IS NULL`,
    restricted to allocator-owned keys exactly as `scripts/phase35_backfill_enqueue.py`
    scopes them (read the script's query :82-121 and mirror its key-set definition — do not
    invent a different ownership filter). A unique-violation on the in-flight index for a
    key is skipped as benign (mirror the precedent's per-key exception handling).

    STEP 5 — pg_cron: `cron.schedule('derive-allocator-key-dailies', '30 5 * * *', ...)`
    (05:30 UTC — after the 04:00 holdings poll and the 05:00 legacy refresh; hour within the
    safe 1-22 band the repo's checks demand). Guard with the repo's idempotent
    schedule-if-absent pattern from 20260420213754.

    STEP 6 — DO-block self-checks: table exists with PK; RLS enabled+forced; kind admitted
    by the CHECK; coherence constraint still admits a `derive_broker_dailies` row shape with
    api_key_id (the re-base regression); cron job registered at a safe hour; and a NEGATIVE
    check: `allocator_equity_snapshots` DDL is byte-untouched by this migration (assert the
    migration file itself contains no reference — this is enforced by the plan-06 grep gate;
    in-migration, simply never reference it).

    PROHIBITED in this file: any statement touching `allocator_equity_snapshots`, its
    policies, `refresh-allocator-equity` cron, or `enqueue_refresh_allocator_equity_for_all`.
  </action>
  <verify>
    <automated>ls supabase/migrations/ | tail -1 | grep allocator_equity_derived_surface && ! grep -q "allocator_equity_snapshots" supabase/migrations/*allocator_equity_derived_surface.sql</automated>
  </verify>
  <done>Migration file exists, timestamp-sorted last, contains table+RLS+kind+coherence+fan-out+cron+DO-checks, and contains ZERO references to allocator_equity_snapshots.</done>
</task>

<task type="auto">
  <name>Task 2: MCP-apply to TEST project, then SQL test gates (RLS + fan-out + re-base regression)</name>
  <files>supabase/tests/test_allocator_equity_derived_rls.sql, supabase/tests/test_derive_allocator_keys_fanout.sql</files>
  <action>
    ORDER MATTERS: apply the migration to the TEST Supabase project (qmnijlgmdhviwzwfyzlc)
    via the Supabase MCP `apply_migration` FIRST (memory
    project_test_project_catchup_unmasks_stale_tests — unapplied migrations make RED-guarded
    SQL tests fail), then FIX the `schema_migrations` row version to the FILE timestamp
    (MCP stamps now() — memory feedback_supabase_apply_migration_drift). NEVER apply to the
    prod project (khslejtfbuezsmvmtsdn) — prod applies itself on merge.

    Then write the two CI-run SQL gates (supabase/tests/test_*.sql is the ONLY DB gate
    location CI runs — memory reference_db_test_ci_wiring; follow the existing files'
    begin/rollback + role-switch conventions):

    `test_allocator_equity_derived_rls.sql`:
    - seed two users + one row each (service role);
    - owner (authenticated, request.jwt set to user A) SELECTs own row → 1;
    - owner A SELECTs user B's row → 0 rows (cross-user deny);
    - anon SELECT → 0 rows or 42501 (either is a pass — assert NOT-visible);
    - authenticated INSERT/UPDATE/DELETE as owner → rejected (write is service-role only);
    - service role upsert on (allocator_id, kind) conflict → single-row replace (atomicity
      precedent).

    `test_derive_allocator_keys_fanout.sql`:
    - seed api_keys fixtures: eligible (is_active, sync_status NULL, disconnected_at NULL),
      revoked (sync_status='revoked'), disconnected (disconnected_at set), inactive
      (is_active=false) — mirroring the phase35 predicate;
    - call `enqueue_derive_broker_dailies_for_allocator_keys` fan-out fn → exactly the eligible key(s) get a
      pending `derive_broker_dailies` compute_jobs row with api_key_id set and NULL
      strategy/portfolio/allocator targets;
    - second call → no duplicate (in-flight index dedup);
    - kind-CHECK admission: INSERT a `derive_allocator_equity` row with allocator_id target
      succeeds; with strategy_id target → violates coherence (exception caught = pass);
    - RE-BASE REGRESSION: INSERT a `derive_broker_dailies` row with api_key_id target
      succeeds (proves the api_key coherence arm SURVIVED the re-base — the exact silent
      failure mode migration 20260710130000's header warns about);
    - cron.job row 'derive-allocator-key-dailies' exists with schedule hour in 1-22.
    All inside BEGIN/ROLLBACK so the shared test DB is left clean (concurrent-CI-flake
    memory: run serially, don't parallelize).
  </action>
  <verify>
    <automated>ls supabase/tests/test_allocator_equity_derived_rls.sql supabase/tests/test_derive_allocator_keys_fanout.sql && grep -q "42501\|0 rows\|anon" supabase/tests/test_allocator_equity_derived_rls.sql && grep -q "api_key_id" supabase/tests/test_derive_allocator_keys_fanout.sql</automated>
    <human-check>Run both files against the TEST project (psql or MCP execute_sql) and paste the pass output into the SUMMARY — CI will run them on the PR, but prove them green locally first.</human-check>
  </verify>
  <done>Migration applied to TEST with corrected schema_migrations timestamp; both SQL gates exist and pass against TEST; the re-base regression (api_key arm survival) is pinned.</done>
</task>

<task type="auto">
  <name>Task 3: GDPR manifest + coverage gate + specialist review routing</name>
  <files>src/lib/gdpr-export-manifest.ts</files>
  <action>
    Add `{ kind: "direct", table: "allocator_equity_derived", user_column: "allocator_id" }`
    to the manifest, placed and commented per the module's house style (cite: new 115.1
    derived-equity surface; contains per-key flow USD magnitudes + derived curve — same
    sensitivity class as allocator_equity_snapshots.value_usd; ON DELETE CASCADE from
    auth.users handles sanitize). Then run the coverage gate script.

    REVIEW ROUTING (execute-phase requirement, not optional): after Tasks 1-3 are complete,
    dispatch BOTH `migration-reviewer` and `rls-policy-auditor` specialist reviews over the
    migration + SQL tests diff, and fix any HIGH/CRITICAL findings before this plan's
    SUMMARY is written. Record their verdicts in the SUMMARY.
  </action>
  <verify>
    <automated>npx tsx scripts/check-gdpr-export-coverage.ts && grep -q "allocator_equity_derived" src/lib/gdpr-export-manifest.ts && npx vitest run src/lib/gdpr-export-manifest.test.ts --no-file-parallelism 2>/dev/null || npx tsx scripts/check-gdpr-export-coverage.ts</automated>
  </verify>
  <done>Coverage script green with the new table manifested; migration-reviewer + rls-policy-auditor both dispatched and their HIGH/CRITICAL findings fixed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anon/authed client → allocator_equity_derived | RLS is the only gate on a money-data table |
| merge to main → PROD DDL | migrations auto-apply; a bad CHECK re-base bricks the queue mid-deploy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115.1-04 | Info disclosure | allocator_equity_derived (per-key flow USD) | mitigate | owner-SELECT RLS + anon deny, pinned in test_allocator_equity_derived_rls.sql |
| T-115.1-05 | Tampering | compute_jobs coherence re-base | mitigate | copy 20260710130000 verbatim + SQL regression pinning api_key-arm survival |
| T-115.1-06 | DoS | fan-out queue saturation | mitigate | existing in-flight partial-index dedup + advisory-lock skip (precedent pattern) |
| T-115.1-07 | Elevation | SECDEF fan-out fn | mitigate | rls-policy-auditor review; fn takes no user args, cron-only caller |
| T-115.1-08 | Tampering | GDPR deletion path | mitigate | ON DELETE CASCADE; no delete-guard trigger added |
</threat_model>

<verification>
- Migration applied to TEST via MCP before SQL tests; schema_migrations version corrected.
- Both SQL gates pass; re-base regression pinned.
- GDPR coverage script green.
- migration-reviewer + rls-policy-auditor verdicts recorded.
</verification>

<success_criteria>
- New surface + kind + recurring fan-out exist on TEST, additive-only (zero legacy-table references).
- All five must_have truths hold; prod application is deferred to merge (watched — user_setup).
</success_criteria>

<output>
Create `.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-02-SUMMARY.md` when done.
</output>
