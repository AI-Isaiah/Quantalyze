---
phase: 115.1-e2-display-repoint-worker-side-flow-aware-equity
plan: 05
type: execute
wave: 3
depends_on: ["115.1-02"]
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.my-allocation.test.ts
  - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx
  - src/app/(dashboard)/allocations/widgets/performance/equity-curve.equitydailypoints.test.tsx
autonomous: true
requirements: [BACKBONE-02, BACKBONE-03]
must_haves:
  truths:
    - "No derived row OR is_trustworthy=false → equityDailyPoints is BYTE-IDENTICAL to today's legacy rendering (the plan-01 SAFETY pin stays green) — prod cutover is safe pre-backfill"
    - "Derived row with is_trustworthy=true → equityDailyPoints comes from payload.curve at the ONE producer site (queries.ts:2409), so chart + V2 factsheet + composer baseline all reflect the same basis (RD-1)"
    - "The payload carries equityCurveSource: 'derived'|'legacy' and the UI shows an honest provenance indicator — legacy-fallback is NEVER presented as api_verified-grade (RD-2)"
    - "snapshotCount / minHistoryDepthMonths / equityBaselineUnknown / equitySnapshots stay snapshot-sourced and untouched"
    - "match.py / holding-compare-adapter / liveBaselineMetricsFromPerKeyDailies untouched — display-only w.r.t. score semantics"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "allocator_equity_derived read in the fan-out + derived-or-legacy producer + equityCurveSource payload field"
      contains: "allocator_equity_derived"
    - path: "src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx"
      provides: "honest provenance indicator (derived vs legacy snapshot history)"
  key_links:
    - from: "getMyAllocationDashboard Promise.all fan-out"
      to: "allocator_equity_derived (kind='equity_curve')"
      via: "USER supabase client + owner RLS + explicit .eq('allocator_id', userId) gate (phase36 pattern, T-36-03-01)"
      pattern: "allocator_equity_derived"
    - from: "queries.ts:2409 producer"
      to: "payload.curve → DailyPoint[] (derived) OR equitySnapshotsToDailyPoints (legacy)"
      via: "is_trustworthy gate"
      pattern: "is_trustworthy"
---

<objective>
Repoint the allocator $-equity display at the ONE producer site (`queries.ts:2409`
`equitySnapshotsToDailyPoints`) onto the derived surface with a MANDATORY
derived-if-trustworthy-ELSE-legacy fallback (the safety non-negotiable: all 517 prod
allocator keys currently have zero per-key rows — a hard repoint blanks every dashboard),
plus an honest provenance indicator per DESIGN.md.

Purpose: BACKBONE-02 display half — one consistent basis for chart, V2 factsheet and
composer baseline (RD-1), safe before the founder-gated backfill runs.
Output: repointed producer + provenance UI + all plan-01 vitest pins green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-RESEARCH.md
@DESIGN.md
@src/lib/queries.ts
@src/lib/allocation-helpers.ts
@src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx
@src/lib/queries.my-allocation.test.ts
</context>

<interfaces>
DB row (plan 02): `allocator_equity_derived` — read `payload, computed_at` where
`allocator_id = userId AND kind = 'equity_curve'` (maybeSingle). Payload:
`{curve: [{date: 'YYYY-MM-DD', equity_usd: number}], flags, degrade_reasons, is_trustworthy: boolean, scalars, inputs}`.

Payload type additions (`MyAllocationDashboardPayload`, queries.ts:1616+):
- `equityCurveSource: "derived" | "legacy"` — provenance of equityDailyPoints;
- `derivedCurveComputedAt: string | null` — computed_at of the derived row when used, else null.

Producer rule (the safety invariant, verbatim):
`derived row exists && payload.is_trustworthy === true` →
`equityDailyPoints = payload.curve.map(p => ({date: p.date, value: p.equity_usd}))`
(already dense — NO forward-fill adapter, NO new Date() re-parsing); ELSE the existing
`equitySnapshotsToDailyPoints(...)` call byte-unchanged.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: SSR read + derived-or-legacy producer + payload provenance fields</name>
  <files>src/lib/queries.ts</files>
  <action>
    In `getMyAllocationDashboard` (the read) + `derivePhase07Fields` (the producer):
    1. Add one read to the EXISTING data fan-out at ~:2580 (the `phase36PerKeyDailiesRes`
       site, AFTER the sequential auth gate at :2505-2510 — NOT the :1686-1710 payload TYPE
       def): USER supabase
       client (NOT admin — mirror the `phase36PerKeyDailiesRes` pattern and its T-36-03-01
       comment: owner RLS is the tenant boundary, `.eq("allocator_id", userId)` is
       defence-in-depth) → `.from("allocator_equity_derived").select("payload, computed_at").eq("allocator_id", userId).eq("kind", "equity_curve").maybeSingle()`.
       Swallow PGRST205 (table missing on pre-migration envs) into null like the `mandate`
       read does — the fallback then renders legacy, which is exactly the safety semantics.
    2. Thread the derived row (curve + is_trustworthy + source) INTO the exported helper
       `derivePhase07Fields` (starts :2345; called from getMyAllocationDashboard at :2857) as a
       NEW parameter, and apply the gated rule at its producer line :2409 — note :2409 is INSIDE
       `derivePhase07Fields`, NOT getMyAllocationDashboard's body. Malformed
       payload defence: if `payload.curve` is not an array of `{date, equity_usd}` with
       finite numbers, treat as absent (legacy fallback) — a corrupt derived row must
       degrade to legacy, never crash SSR or render NaN.
    3. Add `equityCurveSource` + `derivedCurveComputedAt` to the payload type with
       doc-comments in the file's house style (cite 115.1/RD-1/RD-2: one producer, all
       consumers — chart + buildAllocatorPortfolioFactsheetPayload + composer baseline —
       deliberately share the basis; legacy fallback is load-bearing until the founder
       backfill runs).
    4. UNTOUCHED (verify by diff review): `snapshotCount`, `minHistoryDepthMonths`,
       `equityBaselineUnknown`, `equitySnapshots`, `partitionTrustworthyEquitySnapshots`,
       `liveBaselineMetricsFromPerKeyDailies`, the snapshots/holdings reads, and everything
       outside this function. Widget zod schema (widget-data.ts:116) needs no change —
       DailyPoint shape is identical.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-allocation.test.ts --no-file-parallelism && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>All three plan-01 pins GREEN (safety + derived + untrusted-fallback); tsc clean; diff confined to the fan-out read, the producer site, and the payload type.</done>
</task>

<task type="auto">
  <name>Task 2: Honest provenance indicator (RD-2) per DESIGN.md</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx</files>
  <action>
    Read DESIGN.md FIRST (Numbers Contract + provenance/badge conventions) — all visual
    decisions come from there, no deviation. Thread `equityCurveSource` (and
    `derivedCurveComputedAt`) to the EquityChart widget through its existing prop path
    (find how `equityDailyPoints` reaches it from AllocationDashboardV2 and ride the same
    channel — smallest possible prop surface).

    Render an honest, low-key provenance line/badge in the chart header area per DESIGN.md:
    derived → e.g. "Derived from per-key returns + flows" (+ computed_at, muted); legacy →
    e.g. "Broker snapshot history" in the plainer/legacy style. HARD RULE: the
    legacy-fallback state must NEVER reuse the `api_verified`-grade visual treatment
    (Numbers Contract honesty). Existing warm-up/baseline banners (snapshot-sourced) stay
    exactly as they are on the legacy path. On the derived-trustworthy path, keep the
    RD-2-minimal disposition: do NOT rewire the snapshot warm-up gating in this phase —
    indicator only; note the RD-2 chart-gate refinement as a follow-up for design-review at
    phase QA. DrawdownChart needs no indicator of its own (it sits beside the equity chart);
    do not touch it.
  </action>
  <verify>
    <automated>npx vitest run src/app/\(dashboard\)/allocations/widgets/performance/ --no-file-parallelism && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <done>Indicator renders per DESIGN.md for both sources; legacy never api_verified-styled; existing widget tests green; no DrawdownChart/banner rewiring.</done>
</task>

<task type="auto">
  <name>Task 3: Consumer-test extension + Node-22 CI-parity run</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/equity-curve.equitydailypoints.test.tsx, src/lib/queries.my-allocation.test.ts</files>
  <action>
    Extend `equity-curve.equitydailypoints.test.tsx` with the two provenance render states
    (derived-trustworthy → derived indicator + curve from payload; legacy → legacy indicator
    + snapshot-derived curve unchanged). Add a queries-level pin that a MALFORMED derived
    payload (curve not an array / non-finite equity_usd) falls back to legacy (the corrupt-
    row defence from Task 1). Test hygiene: `vi.unstubAllGlobals()` in afterEach wherever
    fetch/localStorage are stubbed (the Node-22 leak class).

    Then the CI-parity gate: run the touched test files AND the full frontend suite under
    Node 22 exactly as CI does — deterministic Node-gap failures are NOT flakes (memory
    reference_ci_node22_vs_local_node25).
  </action>
  <verify>
    <automated>PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run src/lib/queries.my-allocation.test.ts src/app/\(dashboard\)/allocations/widgets/performance/ --no-file-parallelism && PATH=/opt/homebrew/opt/node@22/bin:$PATH npm run lint</automated>
  </verify>
  <done>All repoint + provenance + malformed-payload tests green under Node 22; lint clean (react-hooks errors surface here, not in CI).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authed user → allocator_equity_derived read | RLS + explicit .eq ownership gate |
| DB payload → SSR render | JSONB payload is worker-written but treated as untrusted shape at read |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115.1-17 | Info disclosure | cross-tenant curve read | mitigate | USER client + owner RLS + .eq("allocator_id", userId) after the sequential auth assert (queries.ts:2505-2510) |
| T-115.1-18 | DoS | malformed payload crashes SSR | mitigate | shape-validate; malformed → legacy fallback, pinned in test |
| T-115.1-19 | Repudiation | legacy curve presented as verified | mitigate | equityCurveSource + honest indicator; never api_verified styling on fallback (RD-2) |
| T-115.1-20 | Tampering | score-semantics drift | mitigate | match.py/compare-adapter/KPI paths untouched; plan-06 match-golden byte-parity gate |
</threat_model>

<verification>
- Plan-01 SAFETY pin green post-repoint (the invariant that makes prod cutover safe).
- Derived + untrusted + malformed pins green; provenance render states pinned.
- Node-22 full-parity run + lint green.
</verification>

<success_criteria>
- Every allocator with no derived data sees a pixel-identical dashboard (fallback).
- A derived-trustworthy allocator sees the derived curve with honest provenance, at one producer site feeding all consumers consistently.
</success_criteria>

<output>
Create `.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-05-SUMMARY.md` when done.
</output>
