---
phase: 115.1-e2-display-repoint-worker-side-flow-aware-equity
plan: 01
subsystem: testing
tags: [pytest, vitest, regression-first, golden, allocator-equity, backbone-02, backbone-03]

# Dependency graph
requires:
  - phase: 115
    provides: "Frozen P115 derivation core (allocator_equity_derive.py) + e2_fixtures.py shared STITCH fixtures"
provides:
  - "GREEN safety pin proving no-derived-row → legacy equityDailyPoints render byte-unchanged (prod-cutover safety net)"
  - "4 RED Python carry-in wiring pins against services.allocator_equity_compose (plan 03 turns green)"
  - "3 RED Python compose-job / no-legacy-write / owner-scoped-enqueue pins (plan 04 turns green)"
  - "2 RED vitest derived-repoint pins (plan 05 turns green)"
  - "Recorded byte-stable match-score golden baseline for the phase-wide re-assert gate"
affects: [115.1-03, 115.1-04, 115.1-05, allocator_equity_compose, run_derive_allocator_equity_job, getMyAllocationDashboard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Regression-first RED scaffold: import-inside-test yields per-pin ModuleNotFound/ImportError RED naming the exact missing symbol"
    - "Raise-on-legacy-write fake PostgREST table enforces BACKBONE-03 (any allocator_equity_snapshots write = test failure)"

key-files:
  created:
    - analytics-service/tests/test_e2_segment_blend_wiring.py
    - analytics-service/tests/test_derive_allocator_equity_job.py
  modified:
    - src/lib/queries.my-allocation.test.ts

key-decisions:
  - "Pins import compose/handler INSIDE each test body so RED is a clean per-pin missing-symbol error, not a file-level collection error"
  - "Compose-payload assertions target the stable contract fields (degrade_reasons tokens, is_trustworthy, scalars.computable, curve) so they turn green when plan 03 lands regardless of flag-dict-vs-list wrapper shape"
  - "SAFETY vitest pin reproduces the reference legacy render via equitySnapshotsToDailyPoints on a trustworthy fixture — deep-equal is unambiguous and green on CURRENT code"

patterns-established:
  - "Owner-scoped enqueue pin (T-115.1-02): job payload carries a spoofed allocator_id; the assertion demands the enqueue target == ctx.key_row['user_id']"

metrics:
  duration: ~45m
  completed: 2026-07-18
  tasks: 2
  files: 3
---

# Phase 115.1 Plan 01: Regression-first RED scaffold Summary

Wave-1 regression-first scaffold for the E2 display-repoint. Establishes one GREEN safety pin (the prod-cutover fallback contract) and nine RED pins (four worker-side carry-in wirings, three compose-job/enqueue behaviors, two frontend derived-repoint behaviors) that Waves 2–5 turn green, plus the byte-stable match-score golden baseline the later gate re-asserts. Zero production files touched.

## What was built

### Task 1 — Python RED pins + golden baseline (commit `8b3155a1`)

`analytics-service/tests/test_e2_segment_blend_wiring.py` — four carry-in pins against the not-yet-existing `services.allocator_equity_compose.compose_allocator_equity`:

1. **Carry-in #1 + #4 (segment-wise blend / exclusive-only EXCLUSIVE_FILL):** two offset dense windows (A d0..d9, B d3..d9) + anchors → `exclusive_fill` absent from `degrade_reasons` and `is_trustworthy=True`. Mutation-falsifiable: raw-union feeding 0-fills A's exclusive lead → EXCLUSIVE_FILL blocking → reddens.
2. **Carry-in #2 (shared seam list):** gap-seam fixture (A d0..d4, B d10..d14) → seam gap days absent from `curve` (no invented data) and `scalars.computable=True` (proves `returns_by_key` threaded into `build_allocator_ledger`).
3. **MEDIUM-6 stray-key trap (drop-before-segment):** three keys, two anchors (third None) → compose does NOT raise (the stray-key guard at allocator_equity_derive.py:857-878), `dropped_key` in `degrade_reasons`, curve covers only anchored keys.
4. **Carry-in #3 (ISO-string index boundary):** a `pd.DatetimeIndex` series → `NavReconstructionError` propagates; the ISO-string sibling composes cleanly.

`analytics-service/tests/test_derive_allocator_equity_job.py` — three job/enqueue pins:

5. **Compose-job persistence:** `run_derive_allocator_equity_job({allocator_id})` upserts exactly one `(allocator_id,'equity_curve')` row matching the display-row payload contract.
6. **No-legacy-write (BACKBONE-03):** a raise-on-write fake table makes ANY `allocator_equity_snapshots` operation a hard failure.
7. **Owner-scoped enqueue gap (T-115.1-02):** the key-mode `run_derive_broker_dailies_job` epilogue must enqueue one `derive_allocator_equity` targeted at `ctx.key_row['user_id']`, never a spoofed job-payload `allocator_id`.

### Task 2 — Vitest safety pin + derived-repoint pins (commit `a8c3e28e`)

`src/lib/queries.my-allocation.test.ts` — extended the existing supabase-mock harness (added an in-memory `allocator_equity_derived` table, seeded-empty by default) with a `describe("115.1 equity display-repoint")` block:

1. **SAFETY pin (GREEN, never redden):** no derived row → `equityDailyPoints` deep-equals `equitySnapshotsToDailyPoints(snapshots.map({asof,value_usd}))`; `snapshotCount`/`minHistoryDepthMonths`/`equityBaselineUnknown` stay snapshot-sourced.
2. **RED derived-trustworthy:** `is_trustworthy=true` → `equityDailyPoints` maps `payload.curve` directly (no forward-fill) + `equityCurveSource==="derived"`.
3. **RED untrusted-fallback:** `is_trustworthy=false` → legacy render + `equityCurveSource==="legacy"`.

## Pin status (as required by the plan)

| Pin | File | Status | RED reason |
|-----|------|--------|-----------|
| Carry-in #1/#4 blend | test_e2_segment_blend_wiring.py | RED | ModuleNotFoundError: services.allocator_equity_compose |
| Carry-in #2 seam | test_e2_segment_blend_wiring.py | RED | ModuleNotFoundError: services.allocator_equity_compose |
| Drop-before-segment | test_e2_segment_blend_wiring.py | RED | ModuleNotFoundError: services.allocator_equity_compose |
| Carry-in #3 ISO index | test_e2_segment_blend_wiring.py | RED | ModuleNotFoundError: services.allocator_equity_compose |
| Compose-job persist | test_derive_allocator_equity_job.py | RED | ImportError: run_derive_allocator_equity_job |
| No-legacy-write | test_derive_allocator_equity_job.py | RED | ImportError: run_derive_allocator_equity_job |
| Owner-scoped enqueue | test_derive_allocator_equity_job.py | RED | AssertionError (no epilogue enqueue today) |
| **SAFETY fallback** | queries.my-allocation.test.ts | **GREEN (current code)** | — invariant holds pre-repoint |
| Derived-trustworthy | queries.my-allocation.test.ts | RED | expected equityCurveSource 'derived', got undefined |
| Untrusted-fallback | queries.my-allocation.test.ts | RED | expected equityCurveSource 'legacy', got undefined |

Every RED pin fails for the RIGHT reason (missing module/handler/read or a named-behavior assertion), not a syntax error. The SAFETY pin is GREEN against the CURRENT tree — the fallback contract is correct.

## Golden baseline recorded

Standing goldens run GREEN pre-phase (`test_e2_match_score_golden.py` + `test_e1_delete_gate.py` → 7 passed). Byte-stable match-score golden the later gate re-asserts:

- `analytics-service/tests/fixtures/e2_match_score_golden.json`
- sha256: `284bed421ff99b7ed85b5c968c99f5715528541226f6e3690102cbbe6b3c363a`

## Deviations from Plan

None — plan executed exactly as written. No production files touched (verified: only the three test files + this SUMMARY changed).

## Verification

- Task 1 gate: goldens green AND `test_e2_segment_blend_wiring.py` RED AND `test_derive_allocator_equity_job.py` RED — all confirmed.
- Task 2 gate: `-t "115.1"` → 1 passed / 2 failed; `-t "SAFETY"` → 1 passed — confirmed. Full file: 66 passed / 2 intended-RED, no pre-existing regressions.
- `tsc --noEmit` clean (0 errors) — the vitest additions are type-safe.

## Self-Check: PASSED

All three test files + SUMMARY.md present on disk; both per-task commits (`8b3155a1`, `a8c3e28e`) exist in git history.
