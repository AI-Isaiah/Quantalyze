---
phase: 115.1-e2-display-repoint-worker-side-flow-aware-equity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_e2_segment_blend_wiring.py
  - analytics-service/tests/test_derive_allocator_equity_job.py
  - src/lib/queries.my-allocation.test.ts
autonomous: true
requirements: [BACKBONE-02, BACKBONE-03]
must_haves:
  truths:
    - "A GREEN safety pin exists NOW proving: no derived row → legacy equityDailyPoints render byte-unchanged (the pin that makes prod cutover safe pre-backfill)"
    - "RED pins exist for: derived-trustworthy repoint, untrusted-fallback, segment-wise blend wiring, shared seam list, unanchored-key drop order, compose-job persistence, epilogue compose-enqueue"
    - "Every RED pin fails for the RIGHT reason (missing module/handler/read, or assertion), not a syntax error"
    - "Baseline goldens (match-score golden + E1 delete gate) run green BEFORE any 115.1 code lands"
  artifacts:
    - path: "analytics-service/tests/test_e2_segment_blend_wiring.py"
      provides: "Carry-in #1/#2/#3/#4 wiring pins against services.allocator_equity_compose (RED until plan 03)"
    - path: "analytics-service/tests/test_derive_allocator_equity_job.py"
      provides: "Compose-job persistence + no-legacy-write + enqueue-gap pins (RED until plan 04)"
    - path: "src/lib/queries.my-allocation.test.ts"
      provides: "Safety fallback GREEN pin + derived-repoint RED pins"
  key_links:
    - from: "analytics-service/tests/test_e2_segment_blend_wiring.py"
      to: "services.allocator_equity_compose.compose_allocator_equity"
      via: "import (RED: module absent until plan 03)"
      pattern: "allocator_equity_compose"
    - from: "src/lib/queries.my-allocation.test.ts"
      to: "allocator_equity_derived read in getMyAllocationDashboard"
      via: "fake PostgREST table fixture keyed allocator_equity_derived"
      pattern: "allocator_equity_derived"
---

<objective>
Wave-0 regression-first RED scaffold for the 115.1 display-repoint. Before ANY production
code changes, pin: (1) the SAFETY invariant — no derived row → legacy `value_usd` rendering
byte-unchanged (this pin must be GREEN immediately and may NEVER redden; it is what makes
the prod cutover safe while all 517 prod allocator keys have zero per-key
`csv_daily_returns`); (2) the derived-repoint contract (RED); (3) the four P115 carry-ins
as falsifiable worker-side wiring pins (RED); (4) the compose-job persistence +
epilogue-enqueue gap (RED). Also run the two standing goldens as a pre-phase baseline.

Purpose: every subsequent wave has a failing test to turn green and a green test it must not
break. Output: three test files + a recorded green baseline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-CONTEXT.md
@.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-RESEARCH.md
@analytics-service/services/allocator_equity_derive.py
@analytics-service/tests/e2_fixtures.py
@src/lib/queries.my-allocation.test.ts
</context>

<interfaces>
<!-- THE PHASE-WIDE PAYLOAD CONTRACT. Plans 03/04 produce it, plan 05 consumes it.
     All three plans and BOTH pin files must use exactly this shape. -->

Table `allocator_equity_derived` (created in plan 02), PK `(allocator_id, kind)`:

- Display row — `kind = 'equity_curve'`, `payload` JSONB:
  ```json
  {
    "curve": [{"date": "YYYY-MM-DD", "equity_usd": 123.45}],
    "flags": ["..."],
    "degrade_reasons": ["..."],
    "is_trustworthy": true,
    "scalars": {"mwr": 0.12, "dietz": 0.11, "computable": true},
    "inputs": {"n_keys": 2, "anchor_asof": "YYYY-MM-DD", "composed_at": "ISO-8601"}
  }
  ```
- Per-key Option-B input row — `kind = 'key_inputs:<api_key_id>'`, `payload` JSONB:
  ```json
  {
    "flows": [{"utc_day_iso": "YYYY-MM-DD", "usd_signed": -100.0}],
    "anchor_usd": 5000.0,
    "anchor_asof": "ISO-8601",
    "venue": "okx"
  }
  ```

Compose module contract (plan 03): `services/allocator_equity_compose.py` exports
`compose_allocator_equity(returns_by_key: dict[str, pd.Series], flows_by_key: dict[str, list[ExternalFlow]], anchors_by_key: dict[str, float | None]) -> dict`
returning exactly the display-row payload dict above. All series indices are ISO
`YYYY-MM-DD` strings (the frozen core hard-asserts this).

Frontend contract (plan 05): `getMyAllocationDashboard` payload gains
`equityCurveSource: "derived" | "legacy"`; `equityDailyPoints` becomes
`payload.curve.map(p => ({date: p.date, value: p.equity_usd}))` when a derived row exists
AND `payload.is_trustworthy === true`, else the EXISTING legacy snapshot path unchanged.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Python RED pins — carry-in wiring + compose-job + enqueue gap (plus green baseline)</name>
  <files>analytics-service/tests/test_e2_segment_blend_wiring.py, analytics-service/tests/test_derive_allocator_equity_job.py</files>
  <action>
    FIRST record the pre-phase baseline: run `python -m pytest tests/test_e2_match_score_golden.py tests/test_e1_delete_gate.py -x` from analytics-service/ and confirm green — these are the standing goldens the whole phase must keep byte-stable.

    Create `tests/test_e2_segment_blend_wiring.py` pinning the four P115 carry-ins as they
    will be wired by `services.allocator_equity_compose.compose_allocator_equity` (module
    does not exist yet — RED via ImportError is acceptable and expected). Reuse fixture
    styles from `tests/e2_fixtures.py`. Pins (all against the frozen core's documented
    contract — see RESEARCH §Q5 and §Carry-in #2/#3; the core file
    `allocator_equity_derive.py` is READ-ONLY reference, never edited):

    1. **Segment-wise blend / exclusive-only EXCLUSIVE_FILL (carry-ins #1+#4):** two keys
       with OFFSET dense windows (e.g. key A days d1..d10, key B days d4..d10, both dense
       calendar-daily like broker_dailies.py:124-137 produces) + anchors for both → the
       composed payload has `exclusive_fill_days == 0` semantics: `"EXCLUSIVE_FILL"` never
       appears in `flags`/`degrade_reasons`, AND `is_trustworthy` is True for this clean
       fixture. Assert the late key's absent lead days (d1..d3) blend over the covering set
       only (single-key passthrough for A's exclusive segment — the composed blended
       behavior is observable via the curve/inputs; at minimum assert no EXCLUSIVE_FILL and
       trustworthy=True, which the CURRENT raw-union feeding of blend_concurrent_returns
       cannot satisfy — that is the mutation-falsifiability: feeding the raw union sets
       exclusive_fill_days>0 → blocking flag → this pin reddens).
    2. **Shared seam list (carry-in #2):** a rotation fixture (key A ends, key B starts
       later — a Seam with gap days) → the compose result is internally consistent: the
       curve's seam gap-days are ABSENT from `curve` (no invented data) and `scalars.computable`
       is True (proving `returns_by_key` was passed to `build_allocator_ledger` — omitting
       it forces computable=False per allocator_equity_derive.py:1026-1029).
    3. **Unanchored-key drop ORDER (the MEDIUM-6 stray-key trap):** three keys with returns
       but only two anchors (third anchor None) → compose does NOT raise, the dropped key
       surfaces via degrade_reasons (REASON_NO_ANCHOR/DROPPED_KEY semantics), and the curve
       covers only anchored keys. This pin reddens if segmentation runs over the returns
       key-set before dropping unanchored keys (the curve's MEDIUM-6 guard raises on a seam
       referencing a non-anchored key, allocator_equity_derive.py:857-878).
    4. **ISO-string index boundary (carry-in #3):** passing a series with a
       pd.DatetimeIndex → compose raises loudly (the core's `_assert_iso_day_index`
       propagates); passing 'YYYY-MM-DD' string indices succeeds.

    Create `tests/test_derive_allocator_equity_job.py` (RED — handler absent) using the
    115-04 in-memory-filtering fake-PostgREST pattern (copy the fake from
    `tests/test_e2_deribit_allocator_dailies.py` / `test_derive_broker_dailies_dualmode.py`):

    5. **Compose-job persistence pin:** `run_derive_allocator_equity_job({allocator_id})`
       reads per-key `csv_daily_returns` + `kind='key_inputs:*'` rows from
       `allocator_equity_derived`, and upserts exactly ONE `(allocator_id,'equity_curve')`
       row whose payload matches the interfaces contract (curve non-empty, is_trustworthy
       present, scalars present).
    6. **No-legacy-write pin (BACKBONE-03):** the fake PostgREST RAISES if ANY operation
       (insert/upsert/update/delete) targets `allocator_equity_snapshots`; run both the
       compose job AND (once plan 04 lands) the key-mode epilogue under it.
    7. **Enqueue-gap pin:** the key-mode `run_derive_broker_dailies_job` epilogue enqueues
       one `derive_allocator_equity` job targeted at `ctx.key_row["user_id"]` (assert the
       enqueue call's allocator target equals key_row.user_id and NOT a spoofed
       job-payload allocator_id — the T-115-16 pin). RED until plan 04.

    Do NOT use pytest.importorskip or xfail markers — these are honest RED pins; the wave
    gates below prove RED explicitly.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_match_score_golden.py tests/test_e1_delete_gate.py -x && ! python -m pytest tests/test_e2_segment_blend_wiring.py -x -q && ! python -m pytest tests/test_derive_allocator_equity_job.py -x -q</automated>
  </verify>
  <done>Goldens green (baseline recorded); both new pytest files exist and FAIL (RED) — segment-wiring fails on missing `services.allocator_equity_compose`, job test fails on missing `run_derive_allocator_equity_job`; failure output shows the right missing symbol, not a syntax error.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest safety pin (GREEN now) + derived-repoint pins (RED)</name>
  <files>src/lib/queries.my-allocation.test.ts</files>
  <action>
    Read the existing test file's supabase-mock pattern first and extend it (do not fork a
    new harness). Add a `describe("115.1 equity display-repoint")` block with three pins:

    1. **SAFETY GREEN pin (never redden):** with NO `allocator_equity_derived` row in the
       mock (table read returns null/empty — for the current code, which does not read the
       table at all, arrange the mock so an unexpected read of `allocator_equity_derived`
       returns empty rather than throwing), `getMyAllocationDashboard(...).equityDailyPoints`
       is DEEP-EQUAL to `equitySnapshotsToDailyPoints(snapshots.map(s=>({asof, value_usd})))`
       over the same trustworthy-snapshot fixture — i.e., legacy rendering byte-unchanged.
       This pin must pass against CURRENT code (pre-repoint) and against the plan-05 code
       (post-repoint, fallback branch). Also assert `snapshotCount`,
       `minHistoryDepthMonths`, and `equityBaselineUnknown` are unchanged by fixture — they
       stay snapshot-sourced forever.
    2. **RED derived-trustworthy pin:** mock an `allocator_equity_derived` row
       `(kind='equity_curve', payload.is_trustworthy=true, payload.curve=[...])` per the
       interfaces contract → expect `equityDailyPoints` equals
       `payload.curve.map(p=>({date:p.date, value:p.equity_usd}))` (NO forward-fill adapter
       on this path) and `equityCurveSource === "derived"`. Fails now (no read, no field).
    3. **RED untrusted-fallback pin:** derived row present but
       `payload.is_trustworthy=false` → `equityDailyPoints` equals the legacy
       snapshot-derived points and `equityCurveSource === "legacy"`. Fails now.

    Test hygiene (CI Node-gap class, memory reference_ci_node22_vs_local_node25): any
    `vi.stubGlobal` gets `vi.unstubAllGlobals()` in afterEach; re-stub localStorage in
    beforeEach if the harness uses it. Structure pins 2–3 so their failure message names the
    missing behavior ("expected equityCurveSource").
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-allocation.test.ts --no-file-parallelism -t "115.1" 2>&1 | grep -E "1 passed|2 failed" && npx vitest run src/lib/queries.my-allocation.test.ts --no-file-parallelism -t "SAFETY" 2>&1 | grep -q "1 passed"</automated>
  </verify>
  <done>Safety pin GREEN against current code; the two derived-repoint pins RED with assertion failures naming the missing read/field; all pre-existing tests in the file still green; no leaked global stubs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → assertions | no real credentials/URLs; fixtures synthetic only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115.1-01 | Tampering | allocator_equity_snapshots (two-writers race) | mitigate | pin 6 makes ANY new write to the legacy table a test failure, forever |
| T-115.1-02 | Spoofing | compose-enqueue owner | mitigate | pin 7 asserts owner = ctx.key_row["user_id"], never job payload (T-115-16) |
| T-115.1-03 | Info disclosure | test output | accept | fixtures synthetic; no real USD/keys in fixtures |
</threat_model>

<verification>
- Goldens green baseline recorded in SUMMARY (match-score golden + E1 delete gate).
- RED pins fail for the right reason (missing symbol/behavior), proven in verify commands.
- SAFETY vitest pin green against CURRENT code.
</verification>

<success_criteria>
- 7 Python pins written (4 wiring + 3 job/enqueue), all RED with correct failure modes.
- 3 vitest pins written: 1 GREEN (safety), 2 RED (repoint contract).
- Zero production files touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-01-SUMMARY.md` when done (record the baseline golden output hashes).
</output>
