---
phase: 115.1-e2-display-repoint-worker-side-flow-aware-equity
plan: 06
type: execute
wave: 4
depends_on: ["115.1-03", "115.1-04", "115.1-05"]
files_modified:
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: false
requirements: [BACKBONE-02, BACKBONE-03, STITCH-01]
user_setup:
  - service: railway (analytics worker) — FOUNDER PREREQUISITES FOR LIVE ACCEPTANCE (not code blockers)
    why: "The derived surface stays empty (safe legacy fallback) until these founder-gated ops run"
    dashboard_config:
      - task: "Approval-gated prod backfill: railway ssh \"cd /app && python -m scripts.phase35_backfill_enqueue\" (never executor-run)"
        location: "Railway analytics service"
      - task: "Set E2_GROUND_TRUTH_* env and run scripts/e2_allocator_ground_truth.py on a real allocator account (exit 3 = pending; pass = reconciles to anchor within same-day tolerance AND is_trustworthy)"
        location: "Railway analytics service / founder shell"
must_haves:
  truths:
    - "Full pytest (serial), full vitest with coverage thresholds, and lint are green under CI-parity (Node 22)"
    - "match-score golden is BYTE-STABLE and the E1 delete gate green (score semantics untouched end-to-end)"
    - "WHOLE-REPO grep gate (src/ + e2e/ + analytics-service/ + supabase/): zero new writes to allocator_equity_snapshots; frozen core byte-unchanged; no to_datetime on the compose path"
    - "Founder prerequisites (backfill run, E2_GROUND_TRUTH env) are recorded as LIVE-acceptance gates, explicitly NOT blocking the code merge"
    - "A human has verified the dashboard renders unchanged (legacy path) in the browser"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "115.1 marked complete with plan checkboxes"
    - path: ".planning/REQUIREMENTS.md"
      provides: "BACKBONE-02 display-half closure note; BACKBONE-03 else-branch shipped note"
  key_links:
    - from: "phase diff"
      to: "allocator_equity_snapshots"
      via: "grep gate proving ONLY pre-existing references remain (additive-only)"
      pattern: "allocator_equity_snapshots"
---

<objective>
Phase gate: prove the whole 115.1 delivery against its goal-backward truths, run the parity
and additive-only gates over the WHOLE repo, close the ledgers, and hand the founder the
two approval-gated LIVE-acceptance ops. Ends with a human browser verification (the /qa
rule: GUI changes need browser proof).

Purpose: nothing merges on green unit tests alone — the gates that made P114/P115 safe run
here too. Output: green gate battery + updated ledgers + founder handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full gate battery — suites, goldens, whole-repo grep gates</name>
  <files>(none — verification only)</files>
  <action>
    Run, in order, failing loud on the first red:
    1. `cd analytics-service && python -m pytest` — SERIAL (shared test-DB fence flake;
       never -n auto). Includes the match-score golden (byte-parity anchor: 115.1 touched
       neither routers/match.py nor the legacy table, so the golden MUST be byte-stable —
       any diff is a phase-stopping finding, not a re-bless) and the E1 delete gate.
    2. `npm run test:coverage` — full sharded-equivalent vitest with the blocking coverage
       thresholds (82/80/74/72).
    3. `PATH=/opt/homebrew/opt/node@22/bin:$PATH npm run lint` — CI-parity Node 22.
    4. WHOLE-REPO grep gates (memory: e2e grep-gates that scan src/ only have burned us —
       grep everything):
       a. Two-writers gate: `git diff <phase-base>..HEAD` introduces NO
          insert/upsert/update/delete targeting `allocator_equity_snapshots` and NO changes
          to its policies/cron/jobs — list every diff hunk mentioning the table (filtering
          comment-only lines, not bare `grep -c` on unfiltered files) and assert each is a
          comment/doc reference only.
       b. Frozen core: `git diff <phase-base>..HEAD -- analytics-service/services/allocator_equity_derive.py`
          is EMPTY. ALSO assert `git diff <phase-base>..HEAD -- analytics-service/services/equity_reconstruction.py`
          is EMPTY (legacy store producer untouched — the additive-only invariant, belt-and-suspenders to the two-writers grep).
       c. ISO boundary: no `to_datetime|parse_dates|DatetimeIndex` in
          `services/allocator_equity_compose.py` nor in the new handler/epilogue regions of
          job_worker.py (grep the diff, comment-filtered).
       d. Scenario engine frozen: `git diff <phase-base>..HEAD -- src/lib/scenario.ts` (and
          the engine's frozen module set) is EMPTY.
    5. Confirm the pg_cron fan-out + kind exist on the TEST project (Supabase MCP query) —
       the prod counterpart is verified post-merge by the founder (user_setup).
    Record every command + result in the SUMMARY (fail-loud: no skipped gate may be
    reported as passed).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest -q 2>&1 | tail -3 && cd .. && npm run test:coverage 2>&1 | tail -5 && PATH=/opt/homebrew/opt/node@22/bin:$PATH npm run lint</automated>
  </verify>
  <done>All suites + coverage + lint green; match golden byte-stable; all four grep gates pass with evidence recorded; TEST project objects confirmed.</done>
</task>

<task type="auto">
  <name>Task 2: Ledger closure + founder LIVE-acceptance handoff</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>
    Use Edit (NEVER Write — the Write-truncation hazard on .planning ledgers; spot-check
    `wc -l` after each edit; never `git add .planning`):
    - ROADMAP 115.1 block: check off the six plans, set the phase complete note (display
      repoint shipped with derived-or-legacy fallback; live cutover pending founder ops).
    - REQUIREMENTS: BACKBONE-02 → note the display half is CODE-COMPLETE with the fallback,
      LIVE acceptance pending the founder backfill + ground-truth run; BACKBONE-03 → note
      the else-branch shipped (new surface, legacy store untouched); STITCH-01 → note the
      allocator display path now consumes the per-key dailies pipeline.
    - STATE: position/decisions update per house style.
    - In the SUMMARY, write the explicit FOUNDER HANDOFF section: (1) approval-gated prod
      backfill command (`railway ssh "cd /app && python -m scripts.phase35_backfill_enqueue"`);
      (2) `E2_GROUND_TRUTH_*` env + `scripts/e2_allocator_ground_truth.py` live run =
      the LIVE acceptance (derived curve reconciles to the current-holdings anchor within
      same-day tolerance AND reads is_trustworthy); (3) post-merge prod checks: migration
      run green, `allocator_equity_derived` + kind + `derive-allocator-key-dailies` cron
      exist in prod, dogfood dashboard flips to derived after the first compose. State
      plainly: these gate LIVE acceptance, NOT the merge — the fallback makes the merge
      safe.
  </action>
  <verify>
    <automated>grep -q "115.1" .planning/ROADMAP.md && wc -l .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/STATE.md | awk '$1>50 || /total/'</automated>
  </verify>
  <done>Ledgers updated via Edit with sane line counts; founder handoff section written with exact commands; nothing in .planning staged for git.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human browser verification — unchanged legacy rendering + honest provenance</name>
  <action>Pause and present the how-to-verify steps below to the user; do not proceed past this gate on any signal other than the resume-signal. This checkpoint verifies work already automated in plans 02-05 — no new automation happens here.</action>
  <what-built>The 115.1 display-repoint: derived $-equity surface + worker derivation + frontend repoint with derived-or-legacy fallback and provenance indicator. Pre-backfill, every account should render EXACTLY as before (legacy path).</what-built>
  <how-to-verify>
    1. Start the dev server (or use the preview deploy) and log in as the allocator test
       account (Keychain service: quantalyze-test, alloc@quantalyze.test).
    2. Open the allocations dashboard: the $-equity curve + drawdown chart render exactly
       as before this phase (legacy path), with the "Broker snapshot history" provenance
       indicator visible and NOT styled as verified-grade.
    3. Confirm the V2 Overview factsheet numbers are unchanged (fallback = legacy basis).
    4. Optional (recommended): run /qa on the dashboard for the browser-level proof the
       memory rules require for GUI changes.
    5. LIVE acceptance (later, founder-gated, NOT part of this checkpoint): after the prod
       backfill + first compose, a dogfood account should flip to the derived indicator and
       the ground-truth harness must pass.
  </how-to-verify>
  <resume-signal>Type "approved" or describe rendering issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase diff → prod | merge auto-applies the migration and deploys the worker/frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115.1-21 | Tampering | legacy store / score path | mitigate | whole-repo grep gates + byte-stable match golden as the phase exit criterion |
| T-115.1-22 | Repudiation | silent gate-skipping | mitigate | fail-loud: every gate command + output recorded; skipped ≠ passed |
| T-115.1-23 | DoS | prod migration mid-deploy failure | mitigate | additive-only DDL + TEST-project pre-application + founder watch on the prod run |
</threat_model>

<verification>
Full suites + coverage + lint (Node 22) + match golden byte-stable + delete gate + four
whole-repo grep gates + human browser verification.
</verification>

<success_criteria>
- Every plan-01 pin green; every gate green with recorded evidence.
- Founder handoff written: backfill + ground-truth env flagged as LIVE-acceptance
  prerequisites, explicitly not merge blockers.
- Human approved the unchanged legacy rendering.
</success_criteria>

<output>
Create `.planning/phases/115.1-e2-display-repoint-worker-side-flow-aware-equity/115.1-06-SUMMARY.md` when done.
</output>
