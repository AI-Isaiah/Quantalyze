---
phase: 111-constit-unified-constituent-presentation-parity-pre-check-re
plan: 03
type: execute
wave: 3
depends_on: ["111-01", "111-02"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/(dashboard)/allocations/lib/scenario-state.ts
  - src/app/(dashboard)/allocations/lib/scenario-state.test.ts
autonomous: true
requirements: [CONSTIT-01, CONSTIT-02, CONSTIT-03]
must_haves:
  truths:
    - "The composer renders every source (per-key api-key / CSV / catalog / composite) as one uniform constituent row in a single list — no separate 'Data Sources' section exists"
    - "Each constituent row shows a provenance badge (api-verified / csv / self-reported / composite); rows with underivable provenance show no badge (honest absence)"
    - "Toggling a per-key source off uses the SAME include/exclude channel (draft.toggleByScopeRef) as toggling an added strategy off — includeByApiKeyId is deleted"
    - "A book seed shows strategy/key-level constituent rows only; per-coin holdings render nowhere in the constituent list (Holdings tab unchanged)"
    - "The all-sources-excluded honest-empty state and the per-source history-missing fallback survive, re-homed onto the unified list"
    - "The blend numbers rendered for an unchanged selection are identical before and after the reshape (presentation-only — the parity-gated engine inputs are untouched)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Unified CompositionList rows for per-key + added constituents with provenance badges; Data-Sources section deleted; single toggle channel"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "Unified-list + badge + shared-toggle + no-Data-Sources-section assertions; zero scenario-data-sources* testids remain"
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      provides: "Per-key scope refs riding the existing toggleByScopeRef mechanism without corrupting added-strategy weight normalization"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      via: "per-key include/exclude read from draft.toggleByScopeRef (absent → included)"
      pattern: "toggleByScopeRef"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/components/strategy/TrustTierLabel.tsx"
      via: "provenance badge per constituent row"
      pattern: "TrustTierLabel"
---

<objective>
The CONSTIT UI reshape (gated behind the green 111-01 parity gate): delete the separate "Data Sources" section, render every source as ONE uniform constituent row in CompositionList with a provenance badge, collapse per-coin holdings out of the list, and unify the two divergent toggle channels into one.

Purpose: make the UI match the engine — `scenario.ts` already consumes one unified StrategyForBuilder list (Phase 63); this is presentation catch-up, not new math.
Output: reshaped composer + fully rewritten test coverage. Blend numbers unchanged for identical selections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-RESEARCH.md   (Data-Sources removal map = the authoritative file:line delete list; Pitfalls 3/4/5)
@DESIGN.md   (Numbers Contract: mono numerics, sign-only color; radius ladder; AI-Slop Ban)
@src/app/(dashboard)/allocations/lib/scenario-state.ts

GATE PRECONDITION: 111-01's SUMMARY must state "parity verified". If it states BLOCKED-divergence, STOP immediately — this plan must not execute until the founder re-baseline is recorded in .planning/PROJECT.md Key Decisions and re-verified (CONTEXT protocol).
</context>

<interfaces>
From ScenarioComposer.tsx (line anchors verified 2026-07-16 — re-grep if drifted):
- Data-Sources render block :3581-3649 (Card + per-key switch rows + fallback InfoBanner); all-excluded empty card ~:3908-3910 (testid scenario-data-sources-empty).
- State to remove: showDataSources :2101; showDataSourcesFallback :2114-2117; dataSourceKeys memo :2122-2125; allDataSourcesExcluded :2138-2142; includeByApiKeyId useState :905 + handleDataSourceToggle :913-915 + resets :1251/:1467/:1518.
- Selection feed to the engine: `selected[s.id] = includeByApiKeyId[s.id] ?? true` at ~:2186 (absent → included default).
- Keep + reuse: dataSourceLabel helper :645-669 (row label); CompositionList inline component :4804-5030 (per-coin holdingsSummary rows :4879-4931 to REPLACE; added-strategy rows :4937-5026 with toggle + CoverageStateChip as the row template).
- Per-key unit ids are `key <uuid>` strings (scenario-adapter mint; comment at :2582) — collision-free scope refs.

From scenario-state.ts:
- `draft.toggleByScopeRef: Record<string, boolean>` (:111); the toggle mutator (~:329-385) flips a ref AND rescales weightOverrides among enabled refs (sum-to-1 invariant, header :10).

From 111-02 (Wave 2 outputs, available now):
- `deriveProvenance(...) → ProvenanceTier | null`; addedStrategyMetadataLookup carries trust_tier/is_composite; TrustTierLabel accepts "composite"; per-key rows are api_verified by construction.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Unify the toggle channels — per-key include/exclude rides draft.toggleByScopeRef</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.ts, src/app/(dashboard)/allocations/lib/scenario-state.test.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>scenario-state.ts in full (the toggle mutator's weight-rescale invariant is the landmine), ScenarioComposer.tsx:895-920 (includeByApiKeyId + the CF-05 transient-by-design comment), :2136-2225 (selected-map + all-excluded derivations), resets at :1251/:1467/:1518</read_first>
  <behavior>
    - Toggling a per-key constituent off/on flips draft.toggleByScopeRef for its `key <uuid>` ref; the engine selected-map reads `draft.toggleByScopeRef[ref] ?? true` (absent → included, preserving back-compat with saved drafts that predate per-key refs).
    - Toggling a per-key ref does NOT mutate any added-strategy weightOverrides entry (per-key weights stay on the equity-share path; weight editing is Phase 112 — regression test proves added weights are byte-identical across a per-key toggle).
    - The three reset sites clear per-key toggle state through the one channel (no orphaned includeByApiKeyId resets remain).
    - includeByApiKeyId, handleDataSourceToggle, and their useState are deleted; a whole-repo grep for includeByApiKeyId returns zero hits.
  </behavior>
  <action>
Route per-key include/exclude through scenario-state's existing toggleByScopeRef channel using the per-key unit id (`key <uuid>`) as the scope ref. If the existing toggle mutator's weight-rescale path cannot handle refs with no weightOverrides entry without perturbing added-strategy weights, add a narrowly-scoped state helper for weightless refs INSIDE scenario-state (same draft field, same mechanism — one channel is the CONSTIT-03 requirement; a second useState in the composer is prohibited). Repoint the engine selected-map (:2186) and the all-excluded derivation (:2142) to the draft channel with the absent→included default. Delete includeByApiKeyId/handleDataSourceToggle/resets. Surface-and-supersede note (Rule 7): Phase-66 CF-05 deliberately made per-key toggles transient; CONSTIT-03 (locked, 2026-07-16) supersedes it — per-key exclusions now persist with the draft like every other constituent toggle. Update the :900-904 comment block to record the supersession instead of silently contradicting it.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && test "$(grep -rn "includeByApiKeyId" src/ e2e/ tests/ | grep -cv "Binary")" = "0"</automated>
  </verify>
  <done>One toggle channel; selected-map behavior identical for the default (all-included) case; weight-preservation regression green; zero includeByApiKeyId references repo-wide.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the Data-Sources section; render every source as a uniform badged row in CompositionList</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>ScenarioComposer.tsx:3581-3649 + :3908-3910 (delete targets), :4804-5030 (CompositionList — the added-strategy row :4937-5026 is the row template), :645-669 (dataSourceLabel), DESIGN.md Numbers Contract + coverage-chip palette entry (2026-07-02)</read_first>
  <behavior>
    - CompositionList renders per-key sources as constituent rows (label via dataSourceLabel, toggle via the Task-1 channel, CoverageStateChip where coverage state exists) interleaved in the SAME single list as added-strategy rows — one uniform row anatomy, no sub-sections.
    - Every row carries a TrustTierLabel provenance badge from deriveProvenance: per-key rows api_verified by construction; added rows from the 111-02 lookup; null provenance renders no badge.
    - Per-key rows show NO weight or leverage inputs in this phase (Phase 112 fence) — include/exclude toggle only.
    - The per-coin holdingsSummary rows (:4879-4931) are gone from the list; the Holdings tab is untouched.
    - The "Data sources" Card, the per-source fallback InfoBanner, and the scenario-data-sources-empty card are deleted; the all-excluded honest-empty state and the per-source-history-missing fallback re-render ON the unified list (Pitfall 5 — no silent loss of honest-empty affordances).
  </behavior>
  <action>
Execute the RESEARCH removal map exactly: delete the render block :3581-3649, the empty-card :3908-3910, and the now-dead state (showDataSources, showDataSourcesFallback, dataSourceKeys memo folded into the unified list source, allDataSourcesExcluded replaced by a unified all-off derivation). Build the per-key rows inside CompositionList reusing the added-strategy row structure (same spacing, mono numerics per the Numbers Contract, sign-only color, radius ladder — reuse, do not fork styling per CONTEXT). Re-home the two honest states: all-constituents-off shows the honest-empty treatment on the list itself; a per-key source whose history is missing keeps its fail-loud InfoBanner adjacent to its row or as a list-level banner (executor's judgment within fail-loud + no-invented-data conventions). New testids follow a `scenario-constituent-*` naming scheme. RED FLAG rule (Pitfall 3 / CONSTIT-04): if any step appears to require editing src/lib/scenario.ts or the StrategyForBuilder engine shape, STOP and flag — the reshape has leaked into the frozen engine. Engine inputs (per-key units, weights, series) must be byte-identical for an unchanged selection; only presentation moved.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && git diff --exit-code src/lib/scenario.ts && npx vitest run src/lib/scenario-backbone-gates.test.ts src/lib/scenario.test.ts --no-file-parallelism</automated>
  </verify>
  <done>Single unified list rendering all sources with badges; Data-Sources section gone; honest states re-homed; SC-3 keep-gate + scenario.test.ts behavior pins green; scenario.ts byte-untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the ~15 scenario-data-sources* tests to unified-list assertions + pre-delete whole-repo grep</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>ScenarioComposer.test.tsx occurrences of scenario-data-sources (RESEARCH cites ~lines 1934, 1986, 2005, 4608, 4625, 4638-4661, 4804-4906, 7530-7999 — re-grep for the live set)</read_first>
  <action>
Rewrite every scenario-data-sources* assertion to the unified-list model, preserving each test's INTENT (Rule 9): source visibility → row presence in the unified list; source toggle → shared-channel toggle on the row; all-excluded card → unified honest-empty state; fallback banner → re-homed fallback. Add the three named describe/it groups the RESEARCH test map commands target: "unified constituent list" (single list, no Data-Sources section — assert the section testid/heading is ABSENT), "provenance badge" (one badge per row across all four variants, none when provenance is null), "shared toggle" (per-key + added rows toggle through the same mechanism; per-key toggle leaves added weights untouched). Then run the CONSTIT-04 pre-delete-commit sweep: whole-repo grep (src/ AND e2e/ AND tests/ AND scripts/ — the SC-3 lesson is that gates scanning src/ only miss e2e/) for "Data sources", "Data Sources", "dataSources", and "scenario-data-sources"; every surviving hit must be either the retained dataSourceLabel helper family or a deliberate historical comment — resolve or justify each hit in the SUMMARY before committing.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && test "$(grep -rn "scenario-data-sources" src/ e2e/ tests/ scripts/ 2>/dev/null | grep -cv "Binary")" = "0"</automated>
  </verify>
  <done>Zero scenario-data-sources* testids anywhere in the repo; the three named test groups pass; whole-repo grep results dispositioned in the SUMMARY; full composer suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client React state → frozen engine inputs | Toggle-channel rewiring feeds computeScenario's selected map |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-05 | Tampering | scenario.ts / StrategyForBuilder | mitigate | Hard fence: SC-3 keep-gate + scenario.test.ts behavior pins + `git diff --exit-code src/lib/scenario.ts` in every task verify; any needed engine edit = STOP-and-flag |
| T-111-06 | Denial of service (UX) | Honest-empty / fallback states | mitigate | Pitfall-5 re-home is an explicit behavior assertion, not best-effort — tests pin the all-off and history-missing affordances |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" src/lib/scenario-backbone-gates.test.ts src/lib/scenario.test.ts --no-file-parallelism` green.
- `git diff --exit-code src/lib/scenario.ts` clean; `npx tsc --noEmit` + `npm run lint` clean.
- Whole-repo grep sweep dispositioned (Task 3).
</verification>

<success_criteria>
ROADMAP 111 SC-2/3/4 satisfied: one uniform badged constituent list, no Data-Sources section, book seed collapsed to key/strategy level with per-coin detail only on Holdings, one shared include/exclude mechanism — with the frozen engine byte-untouched and blend outputs unchanged for identical selections.
</success_criteria>

<output>
Create `.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-03-SUMMARY.md` when done.
</output>
