---
phase: 111-constit-unified-constituent-presentation-parity-pre-check-re
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/capture-constit-parity-golden.ts
  - analytics-service/tests/fixtures/constit_parity_fixture.json
  - analytics-service/tests/fixtures/constit_parity_golden.json
  - analytics-service/tests/test_constit_blend_parity.py
autonomous: true
requirements: [CONSTIT-05]
must_haves:
  truths:
    - "A committed pytest re-derives the composer blend in pandas WITHOUT importing scenario.ts and passes against a committed engine golden within stated tolerance"
    - "The test computes BOTH candidate blends (fixed-weight-per-key AND time-varying-per-position) and reports which matches the engine, and the A-vs-B max divergence, as data"
    - "Re-running the capture script reproduces both committed JSONs byte-identically (deterministic, no RNG, no live DB)"
    - "If the fixed-weight blend fails parity, execution STOPS before any Wave-2 UI work (no tolerance loosening, no oracle edits to force a match)"
  artifacts:
    - path: "scripts/capture-constit-parity-golden.ts"
      provides: "Deterministic fixture generator + one-shot frozen-engine golden capture (the ONLY place scenario.ts is invoked)"
    - path: "analytics-service/tests/fixtures/constit_parity_fixture.json"
      provides: "Committed inputs: per-key daily series (ragged starts, weight drift), per-key equity paths, fixed engine weights, cashflow variant"
    - path: "analytics-service/tests/fixtures/constit_parity_golden.json"
      provides: "Committed computeScenario output for the fixture: blended daily series, cumulative curve, TWR, vol, Sharpe, Sortino, maxDD, CAGR"
    - path: "analytics-service/tests/test_constit_blend_parity.py"
      provides: "The CONSTIT-05 gate: pandas re-derivation A (fixed-weight) asserted vs golden; B (time-varying) reported vs A and vs golden"
  key_links:
    - from: "analytics-service/tests/test_constit_blend_parity.py"
      to: "analytics-service/tests/fixtures/constit_parity_fixture.json"
      via: "json.load — the oracle consumes ONLY raw fixture inputs, never scenario.ts"
      pattern: "constit_parity_fixture"
    - from: "scripts/capture-constit-parity-golden.ts"
      to: "src/lib/scenario.ts"
      via: "import computeScenario (read-only golden capture; engine stays byte-frozen)"
      pattern: "computeScenario"
---

<objective>
CONSTIT-05 — THE GATE, first deliverable, alone in Wave 1. Prove (or refute) with an independent numpy/pandas re-derivation that the per-key daily-series blend the composer renders today is reproduced from raw inputs — and determine which interpretation of "per-position weighted blend" the engine implements: (A) fixed-weight-per-key or (B) time-varying-per-position.

Purpose: no CONSTIT UI merges before this gate is green (ROADMAP 111 SC-1). The oracle is deliberately independent — it must never import or call `scenario.ts`.
Output: committed deterministic fixture + engine golden + a re-runnable pytest gate that reports both interpretations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-RESEARCH.md
@.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-CONTEXT.md
@.planning/ROADMAP.md

Key source (read before implementing — exact arithmetic to reproduce):
@src/lib/scenario.ts        (READ-ONLY — byte-frozen; lines 226-667 = computeScenario; :314-319 normWeight; :359-376 union date axis; :412-431 per-day blend with pre-start member drop; :447-452 compound; :528-574 KPIs; :645-666 output rounding)
@analytics-service/tests/test_composite_headline_parity.py   (committed-oracle test pattern to mirror)
@e2e/helpers/seed-test-project.ts   (lines 931-935: the sin-based deterministic series pattern)
</context>

<interfaces>
From src/lib/scenario.ts (frozen engine — the golden capture calls this, the pandas oracle must NOT):
- `computeScenario(strategies: StrategyForBuilder[], selected: Record<string, boolean>, weights?, leverage?, window?)` — the live composer baseline passes NO window (union date-axis path).
- `StrategyForBuilder` fields the fixture must populate per key: `id`, `daily_returns: {date, daily_return}[]`, `weight` (raw USD equity, engine renormalizes), `leverage` (1), `asset_class: "crypto"` (→ ppy 365), `disclosure_tier`.
- Blend semantics: per day i, r_i = Σ_s w_s·L_s·ret_{s,i} / Σ_s w_s over members whose start ≤ day i (a not-yet-started member drops from BOTH numerator and denominator); w_s FIXED across the window; c_i = Π(1+r_j); vol = sampleStd(r)·√365; sharpe = mean(r)·365/vol; CAGR on the calendar clock (calendarYears), NOT n/ppy.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Deterministic fixture generator + one-shot frozen-engine golden capture</name>
  <files>scripts/capture-constit-parity-golden.ts, analytics-service/tests/fixtures/constit_parity_fixture.json, analytics-service/tests/fixtures/constit_parity_golden.json</files>
  <read_first>src/lib/scenario.ts (computeScenario signature + output shape, incl. what precision the returned curve/KPIs carry — rounding at :645-666 decides Task 2 tolerances), e2e/helpers/seed-test-project.ts:931-935, scripts/ directory for existing tsx-run conventions</read_first>
  <action>
Create scripts/capture-constit-parity-golden.ts. It does three things in one deterministic run (no RNG, no Date.now, no live DB — per RESEARCH A4, live-DB tests never run in CI):

1. GENERATE the fixture inputs from closed-form formulas (sin/cos per the seedCompositeStrategy pattern): 3 per-key daily-return series over ~120 consecutive dates with (a) ragged starts — key_b starts ~20 days after key_a/key_c, exercising the engine's pre-start member drop; (b) deliberate weight drift — give key_a large EARLY returns and key_b large LATE returns so fixed-weight and time-varying blends measurably diverge (Pitfall 1: a symmetric fixture is tautological); (c) per-key equity paths E_k(i) compounding from a chosen E_k(0), plus a cashflow variant where key_b receives a synthetic deposit mid-window that steps its equity path but does NOT alter its TWR daily series (pre-empts the TWR-vs-$-equity trap, divergence-watch #7); (d) fixed engine weights = each key's FINAL-day equity (the "current equity snapshot" semantics of queries.ts:2190). Include at least one negative-equity clamp case only if it can be expressed without contrivance; otherwise document clamp semantics in the fixture README string instead.

2. CAPTURE the engine truth: build one StrategyForBuilder per key (weight = raw USD final equity, leverage 1, asset_class "crypto", exploratory tier, all selected), call computeScenario with NO window, and record the blended daily return series, cumulative curve, and KPIs (twr, volatility, sharpe, sortino, max_drawdown, cagr) plus the date axis. Record values at whatever precision the engine returns them (inspect :645-666); note in the golden JSON metadata which fields are engine-rounded and to how many decimals, so the pytest sets tolerance ≥ rounding granularity.

3. WRITE both JSONs to analytics-service/tests/fixtures/ with sorted keys and fixed number formatting so re-runs are byte-identical.

Run command must be documented in the script header (npx tsx scripts/capture-constit-parity-golden.ts). If scenario.ts's import graph blocks plain tsx (@/ path aliases), fall back to running the capture through vitest's node environment or tsx --tsconfig — the requirement is a single committed re-runnable command, not a specific runner. This script is the ONLY artifact allowed to import scenario.ts; it must not modify it (CONSTIT-04).
  </action>
  <verify>
    <automated>npx tsx scripts/capture-constit-parity-golden.ts && git diff --exit-code analytics-service/tests/fixtures/constit_parity_fixture.json analytics-service/tests/fixtures/constit_parity_golden.json</automated>
  </verify>
  <done>Both JSONs committed; re-running the capture is a no-op diff (determinism proven); fixture demonstrably has ragged starts + weight drift + a cashflow variant (assert in the script: fixed-weight terminal wealth ≠ time-varying terminal wealth on the raw inputs, else fail loudly "fixture is tautological").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pandas parity oracle — compute BOTH interpretations, assert A, report B</name>
  <files>analytics-service/tests/test_constit_blend_parity.py</files>
  <read_first>analytics-service/tests/test_composite_headline_parity.py (fixture-shape + committed-oracle conventions), analytics-service/scripts/golden_parity.py (stdlib+pandas+numpy-only discipline; never print raw USD NAV magnitudes — T-73-02), the two fixture JSONs from Task 1</read_first>
  <behavior>
    - Test A-vs-engine curve: fixed-weight-per-key blend (align on union date axis; per day r_i = Σ w_k·ret_{k,i}/Σ w_k over started keys with FIXED w_k; cumprod(1+r)) matches the golden cumulative curve via numpy.testing.assert_allclose at atol/rtol 1e-9 if the golden is unrounded, else at the golden's recorded rounding granularity.
    - Test A-vs-engine KPIs: vol (sample std ÷ n−1, ×√365), sharpe (mean·365/vol), sortino (downside-RMS ÷ n), maxDD (running peak of cumulative), TWR, CAGR (calendar-year clock from real date span, NOT n/365) each match the golden within atol 5e-6 or the golden's rounding granularity, whichever is looser.
    - Test gap-fill semantics: before key_b's start, key_b contributes to NEITHER numerator NOR denominator (replicates scenario.ts:422-430); a day where a started key has a 0.0 gap-filled return keeps that key in the denominator.
    - Report (no assert): interpretation B — time-varying weights w_k(i) = E_k(i)/Σ E(i) from the fixture equity paths, blended and compounded — vs A and vs the golden: max abs curve divergence + terminal-wealth delta, emitted via a plain assert-message/print so the run output carries the founder-facing signal. B's divergence is DATA, not failure.
    - Report (no assert): the cashflow variant — B computed with the deposit-stepped equity path vs B without — demonstrating the blend is TWR-based and cashflow-neutral (divergence-watch #7).
  </behavior>
  <action>
Write the pytest module using ONLY stdlib + pandas + numpy (mirror golden_parity.py discipline). It must NOT import, shell out to, or otherwise invoke scenario.ts or any TS runtime — inputs come exclusively from constit_parity_fixture.json, expectations from constit_parity_golden.json. Module docstring states: what CONSTIT-05 gates, the fixture identity, the tolerances, and the two interpretations with their meaning (A = composer semantics per RESEARCH trace; B = time-varying "true book return" hypothesis, RESEARCH A3). Implement blend-then-compound only (divergence-watch #2). No printing of raw USD magnitudes — returns/ratios only (T-111-01).
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_constit_blend_parity.py -x</automated>
  </verify>
  <done>Test green with A matching the golden within stated tolerance; run output reports the A-vs-B divergence numbers; test is offline-deterministic (no skipif, no network, no DB) so it actually gates CI.</done>
</task>

<task type="auto">
  <name>Task 3: Evaluate the gate + record the outcome (divergence = HARD STOP)</name>
  <files>analytics-service/tests/test_constit_blend_parity.py</files>
  <action>
Run the full gate and record the outcome. Two branches:

PARITY HOLDS (interpretation A matches the golden within tolerance — the expected branch per RESEARCH, since A reproduces the engine's own arithmetic): record "parity verified" in this plan's SUMMARY.md with (a) the fixture identity, (b) the tolerances asserted, (c) the A-vs-B divergence numbers the test reported and what they mean (the composer's rendered blend is the FIXED-weight interpretation; a time-varying book return differs by the reported magnitude — this is the founder-facing datum for any future re-baseline conversation, per RESEARCH A3). Also ensure the same facts live durably in the test module docstring (the .planning dir is local-only). Wave 2+ may proceed.

PARITY DIVERGES (interpretation A fails the assert): STOP. Do NOT loosen tolerances, do NOT edit the oracle or the golden to force green, do NOT begin any Wave-2 UI work (plans 111-02/03/04 are blocked by depends_on and MUST stay blocked). Surface the divergence to the founder as a re-baseline decision: what the engine computes, what the re-derivation computes, and the candidate new canonical blend definition. Per CONTEXT protocol the decision is recorded in .planning/PROJECT.md Key Decisions, the oracle is re-verified against the NEW definition, and only then does the phase proceed. Mark this plan blocked in the SUMMARY with the full numeric evidence.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_constit_blend_parity.py -x && cd .. && npx vitest run src/lib/scenario-backbone-gates.test.ts --no-file-parallelism</automated>
  </verify>
  <done>Gate outcome recorded (parity-verified facts in SUMMARY + test docstring, OR a blocked SUMMARY with divergence evidence and no Wave-2 work started); SC-3 keep-gate still green; `git diff --exit-code src/lib/scenario.ts` clean (engine untouched by the capture work).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test output → CI logs | Oracle output is printed in CI; must not leak account-scale data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-01 | Information disclosure | test_constit_blend_parity.py output | mitigate | Synthetic sin-based fixture only (no real allocator data committed); print returns/ratios, never raw USD NAV magnitudes (golden_parity.py discipline, T-73-02 precedent) |
| T-111-02 | Tampering | scripts/capture-constit-parity-golden.ts vs frozen engine | mitigate | Capture script imports computeScenario read-only; Task 3 verify includes `git diff --exit-code src/lib/scenario.ts` + SC-3 gate |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_constit_blend_parity.py -x` green.
- `npx tsx scripts/capture-constit-parity-golden.ts && git diff --exit-code analytics-service/tests/fixtures/` — deterministic no-op.
- `git diff --exit-code src/lib/scenario.ts` — engine byte-untouched.
- `cd analytics-service && pytest` — full python suite still green (coverage gate 80 unaffected: new file adds only tests).
</verification>

<success_criteria>
ROADMAP 111 SC-1 satisfied: the independent re-derivation proves parity (recorded with fixture + tolerance), OR execution is halted with the divergence surfaced as a founder re-baseline decision — never papered over. The gate is committed, offline, deterministic, and re-runnable in CI.
</success_criteria>

<output>
Create `.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-01-SUMMARY.md` when done, explicitly stating the gate outcome (parity verified / BLOCKED-divergence) and the A-vs-B divergence numbers.
</output>
