---
phase: 111-constit-unified-constituent-presentation-parity-pre-check-re
plan: 04
type: execute
wave: 4
depends_on: ["111-03"]
files_modified:
  - src/lib/scenario-backbone-gates.test.ts
autonomous: true
requirements: [CONSTIT-04]
must_haves:
  truths:
    - "A permanent CI grep gate fails if any removed Data-Sources identifier reappears anywhere in src/, e2e/, tests/, or scripts/ (not just src/ — the SC-3 lesson)"
    - "scenario.ts is byte-identical to main (SC-3 keep-gate green + git diff proof)"
    - "The full TS suite and the analytics-service pytest suite are green at phase close"
  artifacts:
    - path: "src/lib/scenario-backbone-gates.test.ts"
      provides: "New CONSTIT-04 describe block: repo-wide orphaned-Data-Sources token scan alongside the existing SC-2/SC-3 gates"
      contains: "CONSTIT-04"
  key_links:
    - from: "src/lib/scenario-backbone-gates.test.ts"
      to: "e2e/"
      via: "the new gate's walk roots include e2e/, tests/, scripts/ (whole-repo, comment-stripped)"
      pattern: "e2e"
---

<objective>
CONSTIT-04 permanent gates: a repo-wide grep gate that keeps the Data-Sources delete clean forever, plus the phase-close verification sweep proving the frozen engine is byte-untouched and both test suites are green.

Purpose: the v1.10 SC-3 lesson — grep gates that scan src/ only let deleted-feature strings linger in e2e/; this gate scans the whole repo and runs in CI on every push.
Output: extended scenario-backbone-gates.test.ts + a clean full-suite verification record.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-RESEARCH.md
@src/lib/scenario-backbone-gates.test.ts   (walkSource + stripComments template; SC-3 keep-gate at :101-114)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repo-wide orphaned-Data-Sources grep gate</name>
  <files>src/lib/scenario-backbone-gates.test.ts</files>
  <read_first>src/lib/scenario-backbone-gates.test.ts in full (walkSource roots, stripComments mechanics, existing gate naming/comment conventions)</read_first>
  <behavior>
    - A new describe block ("CONSTIT-04 — Data-Sources orphan scan") walks src/, e2e/, tests/, AND scripts/ (comment-stripped source only) and asserts ZERO live occurrences of the removed identifiers: the scenario-data-sources testid prefix, showDataSources, includeByApiKeyId, handleDataSourceToggle, allDataSourcesExcluded.
    - The gate cannot self-invalidate: the banned tokens are constructed by concatenation in the test source (or the gate file excludes itself from the walk) so the token list itself never trips the scan — and a companion assertion proves the gate WOULD fire by scanning a synthetic in-memory string containing a banned token (test-the-wiring rule: prove it fails when neutered).
    - Existing SC-2/SC-3 gates in the file are byte-unchanged.
  </behavior>
  <action>
Extend scenario-backbone-gates.test.ts additively with the new describe block reusing its walkSource/stripComments helpers, widening the walk roots to the four directories for THIS gate only (the existing gates keep their current roots). Ban the five removed identifiers listed in the behavior block — deliberately NOT a generic "dataSource" substring: the dataSourceLabel helper family is retained by design (RESEARCH removal map), and over-broad tokens create false positives. Follow grep-gate hygiene: match against comment-stripped content, never assert `== 0` on raw files where header prose could self-match.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-backbone-gates.test.ts --no-file-parallelism</automated>
  </verify>
  <done>Gate green on the post-reshape tree; the neutered-gate companion assertion proves detection works; SC-2/SC-3 blocks untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Phase-close verification sweep — freeze proof + both suites green</name>
  <files>(none — verification only)</files>
  <action>
Run the phase-close sweep and record results in the SUMMARY: (1) `git diff --exit-code origin/main -- src/lib/scenario.ts` proves the engine is byte-identical to main across the whole phase (CONSTIT-04's byte-freeze, stronger than the SC-3 existence gate); (2) the SC-3 keep-gate and scenario.test.ts behavior pins green; (3) the CONSTIT-05 parity gate re-run green (`cd analytics-service && pytest tests/test_constit_blend_parity.py -x`); (4) full TS suite via `npm run test:coverage` (coverage thresholds are a blocking gate — lines 82 / statements 80 / functions 74 / branches 72); (5) `cd analytics-service && pytest` full python suite; (6) `npm run lint` (catches react-hooks ERRORS that tsc+vitest miss). Any red = fix within this phase's scope or fail loud in the SUMMARY — never a silent skip (Rule 12).
  </action>
  <verify>
    <automated>git diff --exit-code origin/main -- src/lib/scenario.ts && npm run test:coverage && cd analytics-service && pytest</automated>
  </verify>
  <done>All six checks green and recorded; phase ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate → future contributors | The gate is the durable enforcement surface for the delete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-07 | Tampering | Grep gate self-invalidation | mitigate | Token-concatenation construction + neutered-gate detection assertion (the gate provably fires on a banned token) |
| T-111-08 | Repudiation | Engine freeze claim | mitigate | `git diff --exit-code origin/main -- src/lib/scenario.ts` recorded in SUMMARY, not asserted from memory |
</threat_model>

<verification>
- `npx vitest run src/lib/scenario-backbone-gates.test.ts --no-file-parallelism` green (old + new gates).
- Full sweep of Task 2 recorded in SUMMARY.
</verification>

<success_criteria>
ROADMAP 111 SC-5 satisfied: scenario.ts byte-frozen with proof, whole-repo orphan scan green and permanently enforced in CI, both suites green at phase close.
</success_criteria>

<output>
Create `.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-04-SUMMARY.md` when done.
</output>
