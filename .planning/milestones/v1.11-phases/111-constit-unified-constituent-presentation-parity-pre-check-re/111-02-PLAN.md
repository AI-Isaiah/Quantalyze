---
phase: 111-constit-unified-constituent-presentation-parity-pre-check-re
plan: 02
type: execute
wave: 2
depends_on: ["111-01"]
files_modified:
  - src/lib/design-tokens/trust-tier.ts
  - src/components/strategy/TrustTierLabel.tsx
  - DESIGN.md
  - tests/a11y/trust-tier-tokens.test.ts
  - src/lib/queries.ts
  - src/app/api/strategies/[id]/returns/route.ts
  - src/app/api/strategies/[id]/returns/route.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [CONSTIT-02]
must_haves:
  truths:
    - "A `composite` provenance badge variant exists, DESIGN.md-allowlisted, using only hexes already in DESIGN.md (AI-Slop Ban respected)"
    - "An added strategy's trust_tier reaches the composer for BOTH book strategies (SSR payload) and drawer-added strategies (lazy returns fetch)"
    - "A composite strategy is identified in the composer via the server-truth data_quality_flags.composite boolean, projected as a boolean only (full flags blob never shipped to the allocator payload)"
    - "Per-key constituents derive api-verified provenance by construction (a per-key unit IS a connected exchange key)"
  artifacts:
    - path: "src/lib/design-tokens/trust-tier.ts"
      provides: "4th token slot `composite`; ProvenanceTier type = TrustTier | 'composite'; DB TrustTier union stays 3-valued"
      contains: "composite"
    - path: "src/lib/queries.ts"
      provides: "strategy_verifications + data_quality_flags projected into MyAllocationDashboardPayload.strategies[].strategy as trust_tier + is_composite"
    - path: "src/app/api/strategies/[id]/returns/route.ts"
      provides: "Widened response: trust_tier + is_composite alongside daily_returns/asset_class (BLEND-01 pattern)"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "addedStrategyMetadataLookup extended with provenance; lazy settle records trust_tier/is_composite beside asset_class"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/lib/queries.ts"
      via: "payload.strategies[].strategy.trust_tier consumed by addedStrategyMetadataLookup"
      pattern: "trust_tier"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/app/api/strategies/[id]/returns/route.ts"
      via: "fetchAddedReturns settle() records the widened response fields"
      pattern: "is_composite|trust_tier"
---

<objective>
CONSTIT-02 wiring half: create the 4th provenance badge variant (`composite`, RESEARCH gap A1) and thread `trust_tier` + the composite discriminator to added-strategy constituents (RESEARCH gap A2 — this is real wiring, not pure surfacing). No visual reshape yet — plan 111-03 renders the badges on the unified rows; this plan makes the data reachable and the badge component capable.

Purpose: without this, the unified rows in Wave 3 would render blank/wrong badges for added strategies and have no composite variant at all.
Output: badge token + widened payloads + composer-side provenance lookup, all test-covered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-RESEARCH.md
@DESIGN.md   (Color section :171-192 = the hex allowlist; Trust-Tier Badges section :285-300; AI-Slop Ban / gradient allowlist :430)
@src/lib/design-tokens/trust-tier.ts
@src/components/strategy/TrustTierLabel.tsx
@tests/a11y/trust-tier-tokens.test.ts

AGENTS.md: this repo runs a modified Next.js 16.2 — read node_modules/next/dist/docs for any route-handler conventions before editing the returns route.
</context>

<interfaces>
From src/lib/design-tokens/trust-tier.ts:
- `type TrustTier = "api_verified" | "csv_uploaded" | "self_reported"` — this union mirrors DB values on strategy_verifications.trust_tier and MUST stay 3-valued (WatchlistPanel.tsx:30 TIER_ORDER and Strategy["trust_tier"] depend on it).
- `TRUST_TIER_TOKENS as const satisfies Record<TrustTier, TrustTierTokenSlot>` with slots {fill, text, border, label}.

From src/components/strategy/TrustTierLabel.tsx:
- `TrustTierLabel({ trustTier: TrustTier | null | undefined })` — null/undefined renders nothing (contract to preserve); emits data-trust-tier attribute for tests.

From src/lib/queries.ts:
- `MyAllocationDashboardPayload.strategies[].strategy` inline type at :1522-1561 (no trust_tier today); the embedded PostgREST select building it is at :3117-3147; the row normalizer/caster region is ~:3229-3320.
- The latest-verification pick pattern (D-04: trust_tier lives ONLY on strategy_verifications): queries.ts:465-478 and watchlist-read.ts:100-120 — copy this, do not invent a new one.

From src/app/api/strategies/[id]/returns/route.ts:
- Response contract: `{ daily_returns: DailyPoint[], asset_class: string | null }` (BLEND-01 widening precedent at :75-86, probe select at :150).

From ScenarioComposer.tsx:
- `addedStrategyMetadataLookup` memo at :1942-1979 (currently Pick<StrategyForBuilder, "disclosure_tier"|"cagr"|"sharpe"|"asset_class">), consumed by mergeAddedIntoPerKeySet/buildAddedOnlySet at :2055-2090.
- Lazy fetch settle at :1125-1175: `settle(series, assetClass)` writes addedReturnsById + addedAssetClassById.
- Composite discriminator precedent: `dqf?.composite === true` (src/app/factsheet/[id]/v2/page.tsx:90).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: `composite` badge variant — token + component + DESIGN.md allowlist row</name>
  <files>src/lib/design-tokens/trust-tier.ts, src/components/strategy/TrustTierLabel.tsx, DESIGN.md, tests/a11y/trust-tier-tokens.test.ts</files>
  <read_first>tests/a11y/trust-tier-tokens.test.ts (drift-gate mechanics: every hex AND label must appear verbatim in DESIGN.md), src/app/(dashboard)/allocations/components/WatchlistPanel.tsx:30-33 (TIER_ORDER hardcodes the 3 DB tiers — must NOT be affected)</read_first>
  <behavior>
    - TrustTierLabel renders a pill with data-trust-tier="composite" and label "Composite" when passed "composite".
    - TrustTierLabel with null/undefined still renders nothing (existing contract preserved byte-for-byte).
    - tests/a11y drift gate passes: the composite slot's hexes and label appear verbatim in DESIGN.md.
    - Type-level: `TrustTier` stays 3-valued; a new exported `ProvenanceTier = TrustTier | "composite"` is the widened badge-layer union.
  </behavior>
  <action>
Add a `composite` slot to TRUST_TIER_TOKENS as an outline pill using ONLY hexes already in DESIGN.md's Color section — recommended: fill #FFFFFF, text #1A1A2E, border #1A1A2E (text-primary dark navy: a distinct 4th identity in the restrained palette; api_verified keeps the sole accent fill), label "Composite". Do NOT widen the `TrustTier` type (it mirrors DB values — WatchlistPanel TIER_ORDER, Strategy["trust_tier"], and watchlist grouping depend on 3 values); instead export `type ProvenanceTier = TrustTier | "composite"` from trust-tier.ts and retype TRUST_TIER_TOKENS as Record<ProvenanceTier, TrustTierTokenSlot>. Widen TrustTierLabel's prop to ProvenanceTier | null | undefined; null contract unchanged; keep the exact locked visual classes (rounded-sm border px-2 py-0.5 text-xs font-medium — no icons, no gradients, per the badge ladder and AI-Slop Ban). Add the `composite` row to DESIGN.md's Trust-Tier Badges table plus a dated Decisions Log entry (CONSTIT-02, per D-CONSTIT locked taxonomy) so the a11y drift gate's verbatim hex+label assertions pass. Audit every TRUST_TIER_TOKENS consumer (grep imports) for Object.keys/values iteration that assumes 3 variants; WatchlistPanel getGroupLabel indexes by a real TrustTier so it is safe — verify, don't assume.
  </action>
  <verify>
    <automated>npx vitest run tests/a11y/trust-tier-tokens.test.ts --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>4-variant token record compiles; drift gate green; WatchlistPanel untouched and its test green; TrustTierLabel renders the composite pill under test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Server-side threading — SSR payload + returns route carry trust_tier and is_composite</name>
  <files>src/lib/queries.ts, src/app/api/strategies/[id]/returns/route.ts, src/app/api/strategies/[id]/returns/route.test.ts</files>
  <read_first>src/lib/queries.ts:3117-3147 (embedded select) and ~:3229-3320 (normalizer), queries.ts:465-478 (latest-verification pick), src/app/api/strategies/[id]/returns/route.ts in full (BLEND-01 widening + probe pattern + its stale-deploy tolerance notes), src/app/api/strategies/[id]/returns/route.test.ts</read_first>
  <behavior>
    - getMyAllocationDashboard payload: strategies[].strategy.trust_tier equals the most-recent strategy_verifications row's trust_tier (null when no verification rows), and strategies[].strategy.is_composite is a strict boolean derived from data_quality_flags.composite === true (malformed/absent flags → false).
    - Returns route: a 200 body carries trust_tier (string | null) and is_composite (boolean) alongside daily_returns/asset_class; the existing WR-01 failure semantics (non-ok / malformed body never settles) are unchanged.
    - The full data_quality_flags blob is NOT present anywhere in the payload or route response (boolean projection only).
  </behavior>
  <action>
In queries.ts: extend the portfolio_strategies embedded select (:3126-3143) with `strategy_verifications (trust_tier, status, created_at)` and add `data_quality_flags` to the nested strategy_analytics projection; in the normalizer, project `trust_tier` via the established D-04 latest-verification pick (copy queries.ts:465-478 exactly — most-recent created_at wins) and `is_composite` via strict `=== true` coercion mirroring factsheet v2 page.tsx:90 (T-92-05 lesson: malformed jsonb must never read as composite). Widen the MyAllocationDashboardPayload.strategies[].strategy inline type (:1522-1561) with `trust_tier: string | null` and `is_composite: boolean`, each with a JSDoc naming CONSTIT-02 and the D-04 source-of-truth. Do NOT ship the raw data_quality_flags object to the client — project the boolean server-side (T-111-03).

In the returns route: widen the response interface and the DB probe (mirror the BLEND-01 asset_class widening at :75-86/:150) to also select strategy_verifications + strategy_analytics.data_quality_flags and emit `trust_tier` + `is_composite` with the same strict coercions. Route stays under the existing RLS-scoped client; no visibility widening — a strategy the caller can already fetch returns for merely gains two non-sensitive metadata fields (trust_tier is already public on factsheets/watchlist). Extend route.test.ts: new-field assertions plus a regression that a missing verification row yields trust_tier null (not a 500).
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>Both payload paths carry the two fields; route tests green including the null-verification regression; no raw flags blob in any response; existing queries.ts consumers compile untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Composer-side provenance lookup — settle path + metadata memo + derivation precedence</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>ScenarioComposer.tsx:937-945 (addedAssetClassById state + purge discipline), :1125-1175 (fetchAddedReturns/settle incl. WR-01 + stale-deploy tolerance), :1942-1979 (addedStrategyMetadataLookup incl. the BLEND-01 always-emit-an-entry rule)</read_first>
  <behavior>
    - A pure exported helper (e.g. deriveProvenance) maps constituent metadata to ProvenanceTier | null with precedence: is_composite === true → "composite"; else trust_tier when it is a valid TrustTier value; else null (badge hidden — honest absence, no invented data).
    - Book-strategy path: the lookup entry for a book strategy carries provenance derived from payload.strategies[].strategy.trust_tier/is_composite.
    - Drawer-added path: after a lazy fetch settles, the lookup entry carries the route-provided values; a stale-deploy response missing the new fields degrades to null provenance (mirrors the asset_class tolerance), never throws.
    - Purge discipline: removing an added strategy purges its provenance entry exactly like addedAssetClassById (mirror the existing removal effect).
  </behavior>
  <action>
Add an addedProvenanceById-style state beside addedAssetClassById (:941), recorded in settle() from the widened route body and purged on remove identically to the asset-class map. Extend the addedStrategyMetadataLookup memo (:1942-1979) — preserving the BLEND-01 "emit an entry for EVERY added strategy" rule — with `trust_tier` and `is_composite` (book payload wins; else the lazily-fetched value; else null/false). Export the deriveProvenance helper (place it in a small pure module or alongside the composer's other pure helpers — NOT in scenario.ts, NOT in scenario-adapter's engine-facing StrategyForBuilder shape: provenance is presentation metadata and must not ride into the frozen engine, Pitfall 3). Per-key constituents need no threading: their provenance is "api_verified" by construction (scenario-adapter mints one unit per connected api_key, RESEARCH map) — encode that rule in deriveProvenance's doc and in a unit test. No rendering changes in this plan (rows reshape in 111-03); the lookup extension must not alter the engine set construction inputs at :2055-2090 beyond the widened Pick type.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>deriveProvenance unit-tested for all four variants + null; settle/purge symmetry tested; composer suite green; `git diff --exit-code src/lib/scenario.ts src/app/(dashboard)/allocations/lib/scenario-adapter.ts -- ':!*.test.*'` shows scenario.ts untouched (adapter only if strictly needed for types, engine never).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → RSC/route payload | New metadata fields cross into client-visible payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-03 | Information disclosure | MyAllocationDashboardPayload + returns route | mitigate | Project only trust_tier (already public on factsheets/watchlist) + a server-coerced is_composite boolean; the raw data_quality_flags blob (degraded_members venue detail) never ships to the client |
| T-111-04 | Tampering | is_composite coercion | mitigate | Strict `=== true` jsonb coercion (T-92-05 precedent) so malformed flags can never assert composite provenance |
</threat_model>

<verification>
- `npx vitest run tests/a11y/trust-tier-tokens.test.ts "src/app/api/strategies/[id]/returns/route.test.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism` green.
- `npx tsc --noEmit` + `npm run lint` clean.
- `git diff --exit-code src/lib/scenario.ts` — frozen engine untouched.
</verification>

<success_criteria>
All four provenance variants are derivable in the composer with real data paths (book SSR, lazy drawer fetch, per-key by construction, composite via server-truth boolean); the composite badge variant exists inside the DESIGN.md allowlist; zero engine/type leakage into scenario.ts.
</success_criteria>

<output>
Create `.planning/phases/111-constit-unified-constituent-presentation-parity-pre-check-re/111-02-SUMMARY.md` when done.
</output>
