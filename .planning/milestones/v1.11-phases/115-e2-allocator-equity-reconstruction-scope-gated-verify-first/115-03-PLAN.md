---
phase: 115-e2-allocator-equity-reconstruction-scope-gated-verify-first
plan: 03
type: execute
wave: 3
depends_on: [115-02]
files_modified:
  - analytics-service/services/allocator_equity_derive.py
  - analytics-service/tests/test_e2_equity_curve_layer.py
  - analytics-service/tests/test_e2_seam_ledger.py
autonomous: true
requirements: [STITCH-03, STITCH-04, STITCH-05, STITCH-06]
must_haves:
  truths:
    - "Perf-curve (TWR, flow-neutral) and $-equity-curve are DIFFERENT outputs: identical only on a zero-flow fixture; divergent by exactly the flow step on a one-deposit fixture (STITCH-03)"
    - "Unknown start derives backward from the terminal anchor through the return path; anchor=None -> NO $-curve, flagged, never invented (STITCH-04)"
    - "ALL flows — real external AND synthetic seam entries — live in ONE dated ledger (ExternalFlow shape) that both the $-replay and the Dietz/MWR scalars consume (STITCH-05/06)"
    - "TWR is clean across a rotation seam (product of segment TWRs, no seam return) while the $-curve steps by exactly the seam jump (STITCH-06)"
  artifacts:
    - path: "analytics-service/services/allocator_equity_derive.py"
      provides: "$-equity backward replay + unified ledger + seam synthetic flows + Dietz/MWR threading"
      contains: "ExternalFlow"
    - path: "analytics-service/tests/test_e2_equity_curve_layer.py"
      provides: "STITCH-03/04 pins (zero-flow equivalence, deposit divergence, backward replay, honest degradation)"
    - path: "analytics-service/tests/test_e2_seam_ledger.py"
      provides: "STITCH-05/06 pins (one ledger, seam TWR-clean, $ step, Dietz/MWR consume the same ledger)"
  key_links:
    - from: "analytics-service/services/allocator_equity_derive.py"
      to: "services/external_flows.py ExternalFlow"
      via: "the ONE ledger entry shape"
      pattern: "from services.external_flows import ExternalFlow"
    - from: "analytics-service/services/allocator_equity_derive.py"
      to: "services/portfolio_metrics.py compute_mwr / compute_modified_dietz"
      via: "scalar consumers of the unified ledger (the KEPT path gets its first production caller)"
      pattern: "portfolio_metrics import"
---

<objective>
Layer the $-equity curve on top of the wave-2 blend: backward replay from the terminal anchor through the flow-neutral return path plus a dated cashflow ledger (STITCH-03/04), with real external flows and synthetic seam flows entering ONE ledger that the Dietz/MWR scalars consume (STITCH-05/06). Pure functions; fixture flows exercise the real-flow arm fully. This pure core is the tested foundation the Phase 115.1 worker-side display derivation consumes (real-flow crawl -> new keyed surface -> frontend repoint) — a definite consumer one phase away, not speculative.

Purpose: this is the heart of the phase — the founder-locked STITCH contract as executable math.
Output: completed allocator_equity_derive.py + two contract test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-RESEARCH.md

BINDING corrections (RESEARCH supersedes CONTEXT):
- The real cashflow machinery is the nav_twr DATED-FLOW convention (`NAV_{t-1} = NAV_t - pnl_t - F_t`), NOT a live Dietz/MWR ledger — compute_mwr has ZERO production call sites today (dormant scalar). STITCH-05 means: flows enter the ONE dated-flow ledger; Dietz/MWR are scalar CONSUMERS of it (this plan wires that consumption, giving the KEPT path its first real caller).
- csv_daily_returns has NO NAV column — the $ curve is not persisted anywhere on the dailies path; this layer computes it from (returns, flows, anchor).
- MWR/Dietz output is thread-only this phase: computed + tested, no UI display (RESEARCH Open Question 3 recommendation).
- Delete-gate Part B: never place `portfolio_metrics` and the legacy twr token on the same source line.
</context>

<interfaces>
From analytics-service/services/external_flows.py:
class ExternalFlow(NamedTuple): utc_day_iso: str; usd_signed: float  — deposit positive, withdrawal negative; validate_flow_shape(flow) optional shape check.

From analytics-service/services/nav_twr.py (CONVENTIONS to follow — the pnl-based function itself takes daily_pnl which the dailies path does not persist; this layer replays on RETURNS):
- backward identity on returns: equity_{t-1} = (equity_t - F_t) / (1 + r_t); forward check: equity_t = equity_{t-1} * (1 + r_t) + F_t.
- flow day placement: flows land on their UTC day (services/dateday.epoch_ms_to_iso_day upstream); a flow on a no-return day is a valid zero-return equity day, never an orphan (nav_twr HIGH-1 precedent).
- guards: dust/negative equity and flow-dominated days fail loud or flag — mirror the spirit (NavReconstructionError for structural refusals); never spread unknowns across history.

From analytics-service/services/portfolio_metrics.py:
compute_mwr(cash_flows: list[dict{date, amount}], final_value: float, end_date=None) -> float | None
compute_modified_dietz(begin_value: float, end_value: float, cash_flows: list[dict{amount, day}], period_days: int) -> float | None
(Adapt the ledger's ExternalFlow entries into these plain dict shapes at the call boundary.)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: $-equity backward replay + STITCH-03/04 pins</name>
  <files>analytics-service/services/allocator_equity_derive.py, analytics-service/tests/test_e2_equity_curve_layer.py</files>
  <read_first>analytics-service/services/allocator_equity_derive.py (wave-2 state — the file being modified), analytics-service/services/nav_twr.py L27-45 and L776-860 (sign/day conventions, HIGH-1 union-flow-days, the construction-sanity self-check pattern), analytics-service/services/external_flows.py, analytics-service/tests/e2_fixtures.py</read_first>
  <behavior>
    - Test 1 (STITCH-04): per-key backward replay from a known terminal anchor over the fixture return path with zero flows reproduces the hand-computed NAV levels exactly (round fixture numbers); the forward identity (equity_t - F_t)/equity_{t-1} - 1 == r_t holds for every day (float tolerance 1e-9).
    - Test 2 (STITCH-03 equivalence pin): zero-flow fixture -> perf-curve (cumprod of blended returns, normalized) and $-curve (normalized by its start) are IDENTICAL day-by-day.
    - Test 3 (STITCH-03 divergence pin): one-deposit fixture -> the two curves diverge by exactly the flow step on the deposit day and stay parallel in return-space after it; the deposit never appears as a return.
    - Test 4 (honest degradation): anchor=None for a key -> that key contributes NO $-series; the allocator $-curve is emitted only over the window where ALL contributing keys are anchored, with a degradation flag; the perf-curve is UNAFFECTED. anchor=None for every key -> $-curve None + flag, perf-curve intact.
    - Test 5 (flow on a no-return day): a deposit on a day absent from the return index becomes a valid zero-return equity day (HIGH-1 mirror), never dropped and never an exception.
    - Test 6 (allocator aggregation): allocator $-curve == sum of per-key $-curves over the common anchored window (exact on fixtures).
  </behavior>
  <action>
  Extend allocator_equity_derive.py with the equity layer: replay_key_equity(returns: pd.Series, flows: list[ExternalFlow], anchor: float | None) -> per-key $-series (or None + reason), walking BACKWARD from the anchor with the (equity_t - F_t)/(1+r_t) identity and unioning flow days into the index first (HIGH-1 mirror); then allocator_equity_curve(per_key_equity: dict) summing across keys over the common anchored window. Include a construction-sanity self-check mirroring nav_twr's reconcile pattern: after the backward roll, replay FORWARD and assert byte-agreement, raising NavReconstructionError on divergence (import the exception from services.nav_twr). Refuse structurally on non-positive intermediate equity with a scrubbed message (no raw USD in the exception text — pass counts/days only). Tests RED first, then implement, then GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_equity_curve_layer.py tests/test_e1_delete_gate.py -q</automated>
  </verify>
  <acceptance_criteria>Tests 1-6 green; forward/backward self-check present; no raw USD in any raise/log string; delete-gate green.</acceptance_criteria>
  <done>STITCH-03/04 hold as executable, pinned math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ONE unified ledger — real + synthetic seam flows, Dietz/MWR threading</name>
  <files>analytics-service/services/allocator_equity_derive.py, analytics-service/tests/test_e2_seam_ledger.py</files>
  <read_first>analytics-service/services/allocator_equity_derive.py (Task-1 state), analytics-service/services/portfolio_metrics.py (compute_mwr + compute_modified_dietz full docstrings and dict shapes), analytics-service/services/ccxt_flows.py L164-230 (the dated-flow production conventions the ledger mirrors)</read_first>
  <behavior>
    - Test 1 (one ledger): build_allocator_ledger(real_flows_by_key, seams, per_key_equity) returns a single ordered list of tagged entries (ExternalFlow shape + provenance tag real|seam); the SAME list object feeds both the $-replay and the Dietz/MWR adapters (identity assertion — no second ledger is constructed anywhere in the module: grep-style test asserting one construction site).
    - Test 2 (STITCH-06 seam magnitude): rotation fixture with both segments anchored -> synthetic entry usd_signed == (next segment first-day equity) - (prev segment last-day equity), dated on the next segment's first day.
    - Test 3 (STITCH-06 TWR clean): cumulative TWR across the seam == product of the two segments' cumulative TWRs (no seam return injected); the $-curve steps by exactly the seam entry.
    - Test 4 (unknown seam): prev segment unanchored -> seam entry flagged magnitude-unknown; Dietz/MWR return None (fail-loud, never a fabricated scalar); $-curve truncated to the anchored side with a flag.
    - Test 5 (STITCH-05 threading): compute_mwr and compute_modified_dietz called on the unified ledger (real deposit + seam entry fixture) return finite scalars matching an inline hand-derivation on the same flow list; the adapter converts ExternalFlow -> the {date, amount}/{amount, day} dict shapes the KEPT functions expect.
  </behavior>
  <action>
  Extend allocator_equity_derive.py: build_allocator_ledger(...) producing the single provenance-tagged ledger; seam synthetic entries computed from the wave-2 Seam records + per-key $-series boundary values (STITCH-06: windowed stitch and cashflow accounting are ONE code path — the seam entry is appended to the SAME list real flows live in, before any consumer runs); a thin adapter mwr_and_dietz_from_ledger(ledger, begin_value, end_value, period_days) delegating to services.portfolio_metrics (import portfolio_metrics on its own line with no legacy twr token anywhere near it — delete-gate Part B). Scalars are computed and returned in the module's result object; NO display wiring anywhere (thread-only per RESEARCH recommendation — record this in the docstring as the deliberate STITCH-05 scope). Tests RED -> implement -> GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_seam_ledger.py tests/test_e2_equity_curve_layer.py tests/test_e2_allocator_blend.py tests/test_e1_delete_gate.py tests/test_e2_match_score_golden.py -q</automated>
  </verify>
  <acceptance_criteria>Tests 1-5 green; exactly one ledger construction site; compute_mwr gains its first production call path via the adapter; whole E2 suite so far + delete-gate + match golden green.</acceptance_criteria>
  <done>STITCH-05/06 satisfied by construction: one ledger, seam-clean TWR, stepping $-curve, fail-loud unknowns.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | pure functions; fixture data only |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-05 | Information disclosure | NavReconstructionError / flag payloads | mitigate | exception + flag text carries days/counts/bools only, never raw USD (T-73-02) |
| T-115-06 | Tampering (self) | unknown seam magnitude | mitigate | flagged unknown -> scalars None + truncated $-curve; never estimated |
| T-115-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_e2_allocator_blend.py tests/test_e2_equity_curve_layer.py tests/test_e2_seam_ledger.py tests/test_e2_match_score_golden.py tests/test_e1_delete_gate.py -q
</verification>

<success_criteria>
- All four STITCH pins (zero-flow equivalence, deposit divergence, seam TWR-clean + $ step, one-ledger Dietz/MWR consumption) green.
- Honest degradation on missing anchors/unknown seams; no invented data anywhere.
- Match golden byte-stable; delete-gate green.
</success_criteria>

<output>
Create `.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-03-SUMMARY.md` when done.
</output>
