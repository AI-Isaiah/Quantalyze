---
phase: 115-e2-allocator-equity-reconstruction-scope-gated-verify-first
plan: 02
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - analytics-service/services/allocator_equity_derive.py
  - analytics-service/tests/test_e2_allocator_blend.py
autonomous: true
requirements: [STITCH-01]
must_haves:
  truths:
    - "Concurrent allocator keys compose via the capital-weighted BLEND (Phase-36 D1/D3 semantics, ported canonically to Python) — never via the overlap-guarded disjoint-window stitch (Landmine L1)"
    - "The blend is all-or-nothing per allocator (D3): if any eligible key has an empty series the result is an honest empty, never a mixed-basis curve"
    - "Coverage segmentation distinguishes concurrent blocks from genuine sequential rotation boundaries, using the shared half-open window convention"
  artifacts:
    - path: "analytics-service/services/allocator_equity_derive.py"
      provides: "pure blend + segmentation core (STITCH-01); module docstring carries the additive rationale + STITCH-02 deferral pointer + the never-write-allocator_equity_snapshots invariant"
      min_lines: 120
    - path: "analytics-service/tests/test_e2_allocator_blend.py"
      provides: "blend semantics + D3 gate + L1 no-overlap-stitch regression tests"
  key_links:
    - from: "analytics-service/services/allocator_equity_derive.py"
      to: "services/stitch_composite.py"
      via: "window vocabulary reuse for SEQUENTIAL segments only"
      pattern: "stitch_composite"
    - from: "analytics-service/tests/test_e2_allocator_blend.py"
      to: "tests/e2_fixtures.py"
      via: "import of the wave-1 scenario builders"
      pattern: "e2_fixtures"
---

<objective>
Build the canonical Python allocator blend (STITCH-01): per-key daily-return series -> capital-weighted perf-curve for concurrent coverage, plus coverage segmentation that identifies genuine sequential rotation boundaries (where the plan-03 seam machinery applies). Pure functions, no I/O — callers inject data. Consumers: the plan-03 $-ledger core (this phase) and the Phase 115.1 worker-side display derivation (next phase); the pure core is the tested foundation both build on.

Purpose: Python must own the canonical blend so match/factsheet/UI converge (the Phase-36 TS blend in queries.ts is the display-era semantic precedent, not the owner).
Output: services/allocator_equity_derive.py (blend + segmentation half) with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-RESEARCH.md

BINDING constraints:
- ADDITIVE only. This module NEVER reads or writes `allocator_equity_snapshots` and never imports the writer arm of equity_reconstruction (Pitfall 5). The legacy store keeps sole ownership of its table.
- Landmine L1: `stitch_clipped_series` RAISES CompositeOverlapError on overlap BY DESIGN. Concurrent keys go through the weighted blend; do not call `assert_windows_disjoint` on live sibling keys.
- Delete-gate hygiene: never place `portfolio_metrics` and the legacy twr token on the same source line (test_e1_delete_gate.py Part B scans lines).
</context>

<interfaces>
From src/lib/queries.ts L2095-2130 (the semantic precedent to mirror — READ it, do not port blindly):
- D1: one "strategy" per api_key_id from its csv_daily_returns (date, daily_return); weight = that key's share of CURRENT equity (static weights from holdings, not time-varying).
- D2: AUM stays from holdings; only curve SHAPE + KPIs come from the blend.
- D3: blend is used IFF EVERY eligible key has a non-empty per-key series; otherwise the WHOLE allocator degrades to the honest empty baseline — never a half-blend.

From analytics-service/services/stitch_composite.py:
- MemberWindow NamedTuple, windows_overlap(a, b), clip_to_window(...) — half-open [start, end); coverage_mask(clipped) -> dict. Reuse the VOCABULARY for sequential segments; the overlap-raising stitch core is NOT for concurrent keys.

From analytics-service/scripts/phase35_backfill_enqueue.py (eligibility, role-agnostic):
is_active = true AND sync_status IS DISTINCT FROM 'revoked' AND disconnected_at IS NULL
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: capital-weighted concurrent blend (D1/D2/D3 canonical port)</name>
  <files>analytics-service/services/allocator_equity_derive.py, analytics-service/tests/test_e2_allocator_blend.py</files>
  <read_first>src/lib/queries.ts L2095-2200 (liveBaselineMetricsFromPerKeyDailies — exact D1/D3 semantics incl. how partially-missing days inside the overlap are treated), analytics-service/tests/e2_fixtures.py (wave-1 builders), analytics-service/services/metrics.py L398-430 (compute_all_metrics signature — the consumer of the blended series), analytics-service/services/stitch_composite.py L69-160 (window vocabulary)</read_first>
  <behavior>
    - Test 1: two concurrent keys with known weights (0.6/0.4 of current equity) blend day-by-day: blended r_t == 0.6*rA_t + 0.4*rB_t exactly for every overlapping day (hand-computed expected values from e2_fixtures).
    - Test 2: D3 all-or-nothing — one eligible key with an EMPTY series makes the whole blend return the honest-empty result (None/empty series + reason flag), never a single-key curve; the sole-eligible-key case (one key, non-empty) passes through as that key's series with weight 1.0.
    - Test 3: L1 regression — the blend path on fully-overlapping keys never raises CompositeOverlapError and never calls assert_windows_disjoint (assert by monkeypatching stitch_composite.assert_windows_disjoint to raise if called from blend code).
    - Test 4: weights are STATIC current-equity shares (D1): a mid-window change in relative performance does not change the weights applied to earlier days.
    - Test 5: all-zero weights degrade to equal-weight (mirroring match.py's documented fallback) with a flag, never a ZeroDivision.
  </behavior>
  <action>
  Create services/allocator_equity_derive.py. Module docstring MUST state: (1) this is the ADDITIVE STITCH-01..06 derivation shipped while STITCH-02 store retirement is DEFERRED (pointer to .planning/phases/115-*/115-STITCH-02-DEFERRAL.md and the residual readers R1/R2/R3-partial/R5/R6); (2) the module never touches allocator_equity_snapshots (two-writers race, Pitfall 5); (3) the Phase-36 TS blend (queries.ts liveBaselineMetricsFromPerKeyDailies) is the semantic precedent — D1 static current-equity weights, D2 AUM-from-holdings, D3 all-or-nothing.
  Implement pure functions (exact boundaries at Claude's discretion per CONTEXT, suggested): eligible_key_predicate(key_row) -> bool encoding the phase35 predicate for reuse by the Phase 115.1 display derivation; blend_concurrent_returns(series_by_key: dict[str, pd.Series], weights_by_key: dict[str, float]) -> BlendResult (blended pd.Series + flags dict). Mirror the TS handling of days where a subset of keys lacks a row inside the common window — read the TS code and replicate its choice exactly; document the choice in the docstring with the queries.ts line cite. Write the tests RED first (module absent/functions unimplemented), prove RED with a pytest run, implement, prove GREEN. Keep the module I/O-free (no supabase import) so plan-03/05 tests stay hermetic. Never log raw USD values (weights are ratios — fine; equity inputs stay out of log/exception strings, T-73-02).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_allocator_blend.py tests/test_e1_delete_gate.py -q</automated>
  </verify>
  <acceptance_criteria>All 5 behavior tests green; module has no supabase/db import; docstring carries deferral pointer + Pitfall-5 invariant; delete-gate green.</acceptance_criteria>
  <done>Canonical Python blend exists with D1/D2/D3 parity to the TS precedent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: coverage segmentation — concurrent blocks vs sequential rotation seams</name>
  <files>analytics-service/services/allocator_equity_derive.py, analytics-service/tests/test_e2_allocator_blend.py</files>
  <read_first>analytics-service/services/allocator_equity_derive.py (as left by Task 1), analytics-service/tests/fixtures/window_overlap_convention.json (the ONE shared half-open overlap spec), analytics-service/services/stitch_composite.py L153-320 (windows_overlap, clip_to_window, coverage_mask)</read_first>
  <behavior>
    - Test 6: keys A+B concurrent for the whole window -> segmentation returns ONE concurrent segment, zero seams.
    - Test 7: key C ends day X, key D begins day X+1 (half-open convention) -> two sequential segments with exactly one seam boundary at (C_last_day, D_first_day); the seam carries both key ids and both boundary days.
    - Test 8: rotation with partial overlap (C ends day X, D began day X-5) -> the overlap days are CONCURRENT (blended), the pre-overlap C-only and post-overlap D-only runs are their own segments; no seam is emitted inside a blended transition (a seam exists only where the covering-key set changes with ZERO shared coverage day).
    - Test 9: a genuine gap (C ends day X, D begins day X+4) -> seam recorded WITH a gap span (the gap days are absent, never zero-filled — no-invented-data invariant).
  </behavior>
  <action>
  Extend allocator_equity_derive.py with segment_coverage(series_by_key) -> list of segments, each tagged concurrent (multi-key, blend applies) or single-key, plus an ordered list of Seam records (prev_key, prev_last_day, next_key, next_first_day, gap_days). Derive windows from each key's actual series index (first/last day), using the half-open convention from window_overlap_convention.json; reuse stitch_composite.windows_overlap for the pairwise predicate rather than inline interval math (Don't-Hand-Roll). The Seam record is the STITCH-06 handoff consumed by wave 3 — it identifies WHERE synthetic flows apply; no equity math in this task. Tests RED -> implement -> GREEN in the same task.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_allocator_blend.py -q && cd .. && true</automated>
  </verify>
  <acceptance_criteria>Tests 6-9 green; segmentation reuses windows_overlap; gaps flagged, never filled; full blend test file green alongside Task 1 tests.</acceptance_criteria>
  <done>Wave 3 has a deterministic Seam contract to attach synthetic flows to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | pure in-process functions; data injected by callers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-03 | Information disclosure | log/exception strings in allocator_equity_derive | mitigate | no raw NAV/flow USD in any log or raise message (T-73-02 discipline; flags carry bools/counts) |
| T-115-04 | Tampering (self) | gap days in segmentation | mitigate | gaps marked absent, never zero-filled (no-invented-data) |
| T-115-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_e2_allocator_blend.py tests/test_e2_match_score_golden.py tests/test_e1_delete_gate.py -q
</verification>

<success_criteria>
- Blend + segmentation pure core green; D1/D2/D3 mirrored with cited TS precedent.
- No CompositeOverlapError path reachable from concurrent keys; no snapshots table contact.
- Match golden still byte-stable; delete-gate green.
</success_criteria>

<output>
Create `.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-02-SUMMARY.md` when done.
</output>
