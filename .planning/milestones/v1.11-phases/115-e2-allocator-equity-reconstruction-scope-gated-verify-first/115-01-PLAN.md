---
phase: 115-e2-allocator-equity-reconstruction-scope-gated-verify-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-STITCH-02-DEFERRAL.md
  - .planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-VALIDATION.md
  - .planning/REQUIREMENTS.md
  - analytics-service/tests/test_e2_match_score_golden.py
  - analytics-service/tests/fixtures/e2_match_score_golden.json
  - analytics-service/tests/e2_fixtures.py
autonomous: true
requirements: [STITCH-02]
must_haves:
  truths:
    - "STITCH-02 is recorded DEFERRED with the full residual-blocker ledger; the DISPLAY-REPOINT is recorded as SCHEDULED in Phase 115.1 (NOT deferred to 116/117) — BACKBONE-02 = derivation core (115), BACKBONE-03 else-branch = display-repoint (115.1)"
    - "A byte-stable golden of match.py score_candidates output exists and is GREEN — framed as cheap insurance (match.py is UNCHANGED this phase, cannot go red from these edits), NOT derivation-correctness evidence (the plan-05 oracle is the real proof)"
    - "A1 is answered with recorded numbers: per-key csv_daily_returns coverage for eligible allocator keys on prod + test, split BY VENUE so the all-deribit gap (plan 04) is quantified"
    - "115-VALIDATION.md exists in the phase 109-113 conventional form (Requirements->Test Map, Sampling Rate, Wave-0 Gaps)"
  artifacts:
    - path: ".planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-STITCH-02-DEFERRAL.md"
      provides: "STITCH-02 deferral + residual-blocker ledger + BACKBONE-02/03 -> 115/115.1 mapping + A1 by-venue table"
    - path: ".planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-VALIDATION.md"
      provides: "extracted Validation Architecture matching phases 109-113"
    - path: "analytics-service/tests/test_e2_match_score_golden.py"
      provides: "byte-stable score_candidates golden (insurance pin, not correctness proof)"
    - path: "analytics-service/tests/fixtures/e2_match_score_golden.json"
      provides: "captured golden (exact-compare)"
    - path: "analytics-service/tests/e2_fixtures.py"
      provides: "shared E2 fixture builders consumed by plans 02/03/05"
  key_links:
    - from: "analytics-service/tests/test_e2_match_score_golden.py"
      to: "routers/match.py _load_allocator_context + services/match_engine.py score_candidates"
      via: "direct import + mocked supabase"
      pattern: "score_candidates|_load_allocator_context"
---

<objective>
Wave 0: pin the world and record the scope resolution BEFORE any derivation code exists. Capture the match.py score golden (insurance only — match input path is UNCHANGED), record the STITCH-02 deferral WITH the display-repoint scheduled in Phase 115.1, verify A1 (csv_daily_returns coverage, split by venue to size the deribit gap), extract 115-VALIDATION.md, and land the shared fixtures.

Purpose: census did NOT clear — make the deferral durable and map BACKBONE-02 (derivation core = 115) / BACKBONE-03 else-branch (display-repoint = 115.1) so nothing is silently dropped.
Output: deferral record, VALIDATION.md, golden + assertion, A1 numbers, shared fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-RESEARCH.md
@.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md

BINDING (RESEARCH + coordinator phase split, supersedes CONTEXT where they differ):
- Census did NOT clear -> STITCH-02 store retirement DEFERRED. Nothing here deletes/stops equity_reconstruction.py, its writer jobs, allocator_equity_snapshots, pg_cron, or the deribit carve-out.
- compute_twr METHOD deletion OUT (live dep process_key.py:983; delete-gate imports the module at test_e1_delete_gate.py:139).
- DISPLAY-REPOINT (BACKBONE-03 else-branch) ships in the NEW Phase 115.1 (roadmap line 199): worker-side real-flow crawl -> new keyed surface -> repoint queries.ts equityDailyPoints. Record it SCHEDULED there, not "deferred to 116/117".
</context>

<interfaces>
From analytics-service/services/match_engine.py (L802):
score_candidates(allocator_id, preferences, portfolio_strategies, portfolio_returns: dict[str, pd.Series], portfolio_weights: dict[str, float], candidate_strategies, candidate_returns, excluded_strategy_ids=None, thumbs_down_ids=None, portfolio_aum=None) -> ScoreCandidatesResult
Returns dict: {mode, filter_relaxed, engine_version, weights_version, effective_preferences, relaxed_overrides, effective_thresholds, candidates: [{strategy_id, score, rank, score_breakdown, reasons, score_error}, ...]}

From analytics-service/routers/match.py:
- _load_allocator_context(allocator_id) -> dict (L651, SYNC plain def — call WITHOUT await; finding f1 in test_match_integration_phase09.py header)
- _load_holding_portfolio_context(allocator_id) -> dict (L533).

Eligible-key predicate (phase35_backfill_enqueue.py — role-agnostic):
is_active = true AND sync_status IS DISTINCT FROM 'revoked' AND disconnected_at IS NULL
PostgREST: `.eq("is_active", True).or_("sync_status.is.null,sync_status.neq.revoked").is_("disconnected_at", "null")` — a bare .neq drops NULL sync_status rows.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: STITCH-02 deferral (with 115.1 mapping) + 115-VALIDATION.md + A1 by venue</name>
  <files>.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-STITCH-02-DEFERRAL.md, .planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-VALIDATION.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/phases/115-*/115-RESEARCH.md (section 1 "THE SCOPE GATE", Assumptions Log A1, Validation Architecture ~L282-312), .planning/REQUIREMENTS.md (STITCH block L45-51, BACKBONE block L41-43), .planning/ROADMAP.md (Phase 115 L179 + Phase 115.1 L199), analytics-service/scripts/phase35_backfill_enqueue.py (eligibility predicate), a phase 109-113 VALIDATION.md (mirror its section layout — ls .planning/phases/11*/ for the file)</read_first>
  <action>
  Create 115-STITCH-02-DEFERRAL.md (Write; new file) with FOUR sections. (a) VERDICT: census did not clear; store retirement DEFERRED; ship the additive derivation core only. (b) RESIDUAL-BLOCKER LEDGER — transcribe the RESEARCH census enough to stand alone: R1 match.py:586 per-symbol breakdown (HARD), R2 holding-compare-adapter.ts:153 (HARD), R3-partial queries.ts:2583 breakdown/provenance, R5 GDPR export manifest, R6 SQL enqueue surface (migration 076 first-connect, 20260422101911 reconnect, 070:382 pg_cron) + compute_job_kinds constraints (>=4 migrations), writer-side monopoly (_compute_daily_equity L807 sole per-symbol producer), same-FILE consumers F1 (process_key.py:983/long_fetch.py:391 -> to_metrics_snapshot -> compute_twr LIVE), F2 (job_worker.py:1792 price chain), F3 (match.py:54 reconstruct_symbol_returns), F4 (test_e1_delete_gate.py:139). State: compute_twr METHOD deletion also deferred. (c) BACKBONE MAPPING table: BACKBONE-02 = SPLIT — derivation CORE ships in Phase 115 (this phase, plans 02-05); frontend $-curve DISPLAY-REPOINT + worker-side real-flow crawl ship in Phase 115.1 (roadmap L199). BACKBONE-03 (store retirement) stays DEFERRED pending the residual census. Note: the all-deribit dogfooding gap is CLOSED in THIS phase (plan 04) as the concrete existing-consumer deliverable (Phase-36 blend picks up deribit per-key dailies). (d) A1 COVERAGE TABLE: via Supabase MCP, READ-ONLY SELECTs on BOTH projects (prod khslejtfbuezsmvmtsdn, test qmnijlgmdhviwzwfyzlc): eligible api_keys (predicate above) LEFT JOIN a per-api_key_id count of csv_daily_returns rows, annotated BY venue (exchange column) so the deribit subset is explicit. Record eligible key count total + per venue, keys-with-0-rows per venue (the deribit-with-0-rows count sizes plan 04), min/max counts. COUNTS ONLY — never balances/NAV (T-73-02). If eligible keys have 0 rows, note remediation `railway ssh "cd /app && python -m scripts.phase35_backfill_enqueue"` as an execute-time decision; do NOT run it here.
  Then create 115-VALIDATION.md extracting the Validation Architecture from 115-RESEARCH.md ~L282-312 (Test Framework, Requirements->Test Map, Sampling Rate, Wave-0 Gaps) into the phase-109-113 layout. Update the Test Map for the REVISED scope: STITCH-01 blend (plan 02) + deribit dailies gap-closure (plan 04 — test_derive_broker_dailies_dualmode.py extension), STITCH-03/04/05/06 pure core (plan 03), oracle (plan 05); DROP any read-endpoint row.
  Then EDIT (never Write — truncates .planning ledgers) .planning/REQUIREMENTS.md: STITCH-02 line append "— DEFERRED 2026-07-17 (115 census did NOT clear; residual blockers in phases/115-*/115-STITCH-02-DEFERRAL.md)"; BACKBONE-02 line append "— SPLIT: derivation core = Phase 115; frontend display-repoint = Phase 115.1"; confirm BACKBONE-03 stays gate/deferred with the 115.1 else-branch pointer. Spot-check `wc -l .planning/REQUIREMENTS.md` before/after — no shrink beyond the edited-line delta.
  </action>
  <verify>
    <automated>grep -q "115.1" .planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-STITCH-02-DEFERRAL.md && grep -q "DEFERRED" .planning/REQUIREMENTS.md && grep -q "Phase 115.1" .planning/REQUIREMENTS.md && test -f .planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>Deferral file has verdict + all residual blockers + BACKBONE 115/115.1 mapping + A1 by-venue numbers from both projects; VALIDATION.md exists in convention; REQUIREMENTS.md STITCH-02 + BACKBONE-02 lines carry the notes; line count sane.</acceptance_criteria>
  <done>Deferral + scope split durable; display-repoint recorded as Phase 115.1; A1 answered by venue.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: capture the match.py score golden (insurance pin, GREEN immediately)</name>
  <files>analytics-service/tests/test_e2_match_score_golden.py, analytics-service/tests/fixtures/e2_match_score_golden.json</files>
  <read_first>analytics-service/tests/test_match_integration_phase09.py (WHOLE file, 260 lines — mock-supabase builder + snapshot fixture patterns; f1 finding: _load_allocator_context is SYNC, plain def, no await), analytics-service/routers/match.py L533-680 (_load_holding_portfolio_context consumption), analytics-service/services/match_engine.py L802-840 (score_candidates signature + result shape)</read_first>
  <behavior>
    - Test 1: for a fixed fixture allocator (holdings + >=35 days of breakdown snapshots across 2 symbols + >=2 candidate strategies with deterministic series), _load_allocator_context per-holding series/weights/warm_up_dropped exactly equal the golden (assert_series_equal check_exact=True; dict eq; int eq).
    - Test 2: score_candidates full output (mode, engine_version, weights_version, every candidate score/rank/score_breakdown/reasons/score_error) exactly equals the committed JSON golden (json.dumps sort_keys + fixed float repr, string-equal). No tolerance.
    - Test 3: regeneration guard — env UPDATE_GOLDEN=1 rewrites the JSON and FAILS with "golden regenerated, rerun" so a regen never silently passes in CI.
  </behavior>
  <action>
  Create tests/test_e2_match_score_golden.py mirroring the phase09 mock-supabase pattern (copy the builder helpers into this file — test files stay self-contained). Fixture: one allocator, 2 holdings on distinct symbols with value_usd weights, >=35 daily breakdown snapshots (deterministic compounding like phase09 _make_snapshot_series), candidate strategies with fixed return series so score_candidates runs the personalized path deterministically (ENGINE_VERSION/WEIGHTS_VERSION read from the module + embedded in the golden — a legit version bump regenerates via UPDATE_GOLDEN). Run once with UPDATE_GOLDEN=1 to capture the JSON, then clean to prove GREEN.
  Module docstring MUST state the honest framing: match.py's input path is UNCHANGED in Phase 115, so this golden is cheap INSURANCE against accidental shared-code perturbation (e.g. reconstruct_symbol_returns moving) — it is NOT evidence the new derivation is correct; correctness is proven by the independent oracle in plan 05.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_match_score_golden.py tests/test_e1_delete_gate.py -q</automated>
  </verify>
  <acceptance_criteria>Golden JSON committed; exact-compare GREEN; UPDATE_GOLDEN fails loud; docstring carries the insurance-not-correctness framing; delete-gate green.</acceptance_criteria>
  <done>Match insurance pin captured pre-phase, byte-stable, honestly framed.</done>
</task>

<task type="auto">
  <name>Task 3: shared E2 derivation fixtures (2 concurrent keys + 1 rotated key + flow days)</name>
  <files>analytics-service/tests/e2_fixtures.py</files>
  <read_first>analytics-service/services/external_flows.py (ExternalFlow NamedTuple: utc_day_iso str, usd_signed float; deposit positive / withdrawal negative), analytics-service/tests/fixtures/window_overlap_convention.json (the ONE shared half-open overlap spec), analytics-service/services/nav_twr.py L27-45 (flow sign + day-bucket conventions)</read_first>
  <action>
  Create tests/e2_fixtures.py: pure deterministic builders (no I/O, no supabase) that plans 02/03/05 import read-only: (a) make_per_key_returns(key_id, start_day, n_days, daily) -> pd.Series indexed by ISO day; (b) a canonical THREE-KEY scenario: keys A and B fully CONCURRENT over 60 days (different paths), key C ROTATED (window ends the day before a replacement key D begins — a genuine sequential seam, half-open per the shared convention); (c) flow fixtures: list[ExternalFlow] with one mid-window deposit (positive) and one withdrawal (negative, incl. the fee-debit convention) on days present in the return index, plus one flow on a no-trade day (the union-day shape nav_twr HIGH-1 handles); (d) per-key anchor fixtures: known terminal equities chosen so backward replay produces round, hand-checkable NAV levels, plus an anchor=None variant for honest-degradation tests; (e) a deribit-flavored per-key series variant (venue tag) so plan 04's tests share the shape. Document in the docstring that these encode the STITCH scenarios and that later test files add LOCAL fixtures rather than editing this module (this file is created once in wave 1, imported afterwards).
  </action>
  <verify>
    <automated>cd analytics-service && python -c "import tests.e2_fixtures as f; f" && python -m pytest tests/test_e2_match_score_golden.py -q</automated>
  </verify>
  <acceptance_criteria>Module imports clean; builders deterministic; scenario covers concurrent pair + rotated seam + real-flow days + anchor/None + a deribit-flavored variant.</acceptance_criteria>
  <done>Shared fixtures in place for plans 02/03/04/05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| MCP -> Supabase prod/test | read-only SQL for A1 (counts only) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Information disclosure | A1 coverage table | mitigate | record row COUNTS only; never balances/NAV/flow USD (T-73-02 class) |
| T-115-02 | Tampering | .planning/REQUIREMENTS.md | mitigate | Edit-only (never Write), wc -l spot-check before/after |
| T-115-SC | Tampering | package installs | accept | no packages installed this phase (RESEARCH Package Legitimacy Audit: none) |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_e2_match_score_golden.py tests/test_e1_delete_gate.py -q
grep -q "DEFERRED" .planning/REQUIREMENTS.md && grep -q "Phase 115.1" .planning/REQUIREMENTS.md
</verification>

<success_criteria>
- Deferral + BACKBONE 115/115.1 mapping + A1 by-venue numbers durable; VALIDATION.md in convention; REQUIREMENTS.md edited (not truncated).
- Match golden captured GREEN and honestly framed before any derivation code exists.
- Shared fixtures importable; delete-gate green.
</success_criteria>

<output>
Create `.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-01-SUMMARY.md` when done. Surface the A1 by-venue verdict prominently (esp. deribit-with-0-rows) — plans 02 and 04 read it.
</output>
