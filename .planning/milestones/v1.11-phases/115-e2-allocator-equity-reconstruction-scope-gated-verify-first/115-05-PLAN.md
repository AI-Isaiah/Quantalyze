---
phase: 115-e2-allocator-equity-reconstruction-scope-gated-verify-first
plan: 05
type: execute
wave: 4
depends_on: [115-03, 115-04]
files_modified:
  - analytics-service/tests/test_e2_parity_oracle.py
  - analytics-service/scripts/e2_allocator_ground_truth.py
autonomous: true
requirements: [STITCH-03, STITCH-04, STITCH-05, STITCH-06, BACKBONE-02]
user_setup:
  - service: railway (analytics worker)
    why: "live anchor-consistency acceptance run against a real allocator account (fixture gates ship regardless)"
    env_vars:
      - name: E2_GROUND_TRUTH_API_KEY / E2_GROUND_TRUTH_API_SECRET (read-only allocator exchange key)
        source: "founder-provisioned read-only exchange key, set via Railway service variables (deribit_ground_truth.py runbook pattern)"
must_haves:
  truths:
    - "An INDEPENDENT pandas re-derivation (not the module's own code) confirms internal consistency: (equity_t - F_t)/equity_{t-1} - 1 == r_t day-by-day, blend TWR == compute_all_metrics output, zero-flow equivalence, seam pins (114-01 oracle pattern — no tautology)"
    - "The golden gate is anchored on anchor-consistency + internal-consistency + seam pins + match-score byte-parity — NOT on curve parity vs the old store (mark-basis vs cash-basis makes that impossible; L4)"
    - "The committed ground-truth script proves read-only scope before fetching, emits one sanitized JSON, exits non-zero on any skip, and records old-store-vs-new divergence for a ccxt allocator as EVIDENCE, not an assertion"
    - "Phase gate: full analytics suite green, delete-gate green, match golden byte-stable after ALL phase edits"
  artifacts:
    - path: "analytics-service/tests/test_e2_parity_oracle.py"
      provides: "the independent oracle over the full fixture scenario"
    - path: "analytics-service/scripts/e2_allocator_ground_truth.py"
      provides: "committed read-only acceptance script (deribit_ground_truth pattern) + runbook docstring"
  key_links:
    - from: "analytics-service/tests/test_e2_parity_oracle.py"
      to: "services/allocator_equity_derive.py"
      via: "oracle re-derives what the module claims, from raw fixtures"
      pattern: "allocator_equity_derive"
    - from: "analytics-service/scripts/e2_allocator_ground_truth.py"
      to: "the live exchange (env creds) + the allocator_equity_derive core computation"
      via: "railway ssh one-off, env-only credentials"
      pattern: "read.only|readonly"
---

<objective>
Close the phase with the independent verification layer: a parity oracle that re-derives every STITCH claim from raw fixtures with inline pandas (the 114-01/111-01 pattern — the test must be able to catch the module lying), plus the committed ground-truth script for the real-account anchor-consistency acceptance run, plus the final phase gates (full suite, delete-gate, match golden).

Purpose: the golden gate for E2 is deliberately NOT byte-parity vs the old store (impossible: mark-basis vs cash-basis). This plan builds the achievable, meaningful gate set from RESEARCH §6.
Output: oracle test file + ground-truth script + green phase gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-RESEARCH.md

BINDING (RESEARCH §6 + Landmine L4):
- NEVER gate on old-store curve parity for ccxt (mark-basis vs cash-basis — a shape gate would fail honestly and permanently). Old-vs-new divergence for one ccxt allocator is captured as documented EVIDENCE only.
- For deribit-only allocators there is NO store curve at all (the carve-out wrote nothing) — the new path is the first-ever curve; the gate there is anchor-consistency + internal-consistency only.
- The live acceptance run requires founder-set env creds; if absent, the script exits non-zero with an explicit SKIP message and the fixture gates still carry the phase (RESEARCH Environment Availability).
</context>

<interfaces>
From analytics-service/scripts/deribit_ground_truth.py (the pattern to copy — read it fully): committed read-only one-off, `railway ssh "cd /app && python -m scripts.e2_allocator_ground_truth"`, credentials via env only, proves read-only scope BEFORE fetching, whitelisted output fields, single sanitized JSON to stdout, never writes prod tables, non-zero exit on any skip.

From analytics-service/services/metrics.py: compute_all_metrics(returns, periods_per_year=365, ...) — the scalar the oracle re-derives against with inline pandas (cumprod, std, etc.), never by calling a second in-tree helper (that would be a tautology).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: independent parity oracle over the full fixture scenario</name>
  <files>analytics-service/tests/test_e2_parity_oracle.py</files>
  <read_first>analytics-service/services/allocator_equity_derive.py (final wave-3 state — result shapes), analytics-service/tests/e2_fixtures.py, the 114-01 oracle precedent: analytics-service/tests/ (grep for the phase-114 oracle test file, pattern "oracle" — mirror its inline re-derivation discipline), analytics-service/tests/test_e2_equity_curve_layer.py (what is already pinned, to avoid duplicating rather than independently re-deriving)</read_first>
  <behavior>
    - Oracle 1 (internal consistency): from RAW fixture inputs (per-key returns + flows + anchors — not module outputs), an inline pandas walk re-derives the $-curve forward and asserts the module's curve matches day-by-day (1e-9); then asserts (equity_t - F_t)/equity_{t-1} - 1 reproduces r_t for every day.
    - Oracle 2 (blend TWR vs backbone): inline cumprod over the hand-weighted blended returns equals compute_all_metrics' cumulative return on the module's blended series (the inline side NEVER calls metrics helpers).
    - Oracle 3 (zero-flow equivalence): on the zero-flow fixture, normalized perf-curve == normalized $-curve exactly (independent normalization inline).
    - Oracle 4 (seam): inline product of segment TWRs == cross-seam TWR; inline seam-jump arithmetic == the ledger's synthetic entry; the Dietz/MWR scalar recomputed inline (simple Dietz formula in the test body) agrees with the module's scalar on the fixture ledger within 1e-9.
    - Oracle 5 (fabrication canary): mutate a copy of the fixture (drop one flow) and assert the oracle FAILS — proving the oracle can actually catch a divergence (test-the-test, injection-proven RED discipline from the delete-gate lineage).
  </behavior>
  <action>
  Create tests/test_e2_parity_oracle.py. Discipline: every assertion's expected side is derived INLINE in the test from raw fixture data with plain pandas/numpy — no imports from allocator_equity_derive on the expected side, no metrics.py helpers on the expected side. Oracle 5 uses pytest.raises around the oracle helper on a corrupted fixture to prove sensitivity. Keep runtime under a few seconds (fixture-sized frames).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_parity_oracle.py -q</automated>
  </verify>
  <acceptance_criteria>Oracles 1-5 green; expected-side code provably independent (no module/metrics imports in expected-side derivations); corruption canary red-capable.</acceptance_criteria>
  <done>The module cannot silently drift: an independent observer pins every STITCH claim.</done>
</task>

<task type="auto">
  <name>Task 2: ground-truth script + final phase gates</name>
  <files>analytics-service/scripts/e2_allocator_ground_truth.py</files>
  <read_first>analytics-service/scripts/deribit_ground_truth.py (FULL file — the committed pattern: env creds, read-only proof, whitelisted sanitized JSON, exit codes), analytics-service/services/allocator_equity_derive.py</read_first>
  <action>
  Create scripts/e2_allocator_ground_truth.py copying the deribit_ground_truth pattern: (1) read env creds (E2_GROUND_TRUTH_* names documented in the docstring runbook; missing -> print explicit SKIP reason, exit 2); (2) prove read-only key scope BEFORE any data fetch; (3) fetch live current equity for the account (per-venue: ccxt total-equity path or deribit account state — reuse the existing exchange helpers, never new fetchers); (4) compute the derived terminal equity via the allocator_equity_derive core over the account's persisted csv_daily_returns + persisted anchors (service-role read, read-only); (5) emit ONE sanitized JSON to stdout: anchor_consistency {derived_terminal, live_equity, drift_pct, within_same_day_tolerance: bool}, plus for a ccxt allocator an old-store-vs-new EVIDENCE block (divergence summary + the mark-basis-vs-cash-basis reason cite) explicitly labeled non-gating; whitelist output fields, never write any table, non-zero exit on any skip/partial (fail loud). Docstring runbook: `railway ssh "cd /app && python -m scripts.e2_allocator_ground_truth"`, founder env setup steps, and the note that fixture gates carry the phase when creds are absent. Attempt the live run via railway ssh IF the env vars are already set; if not set, record "pending founder env" in the SUMMARY — do not block the phase.
  Then run the FINAL PHASE GATES and record results in the SUMMARY: full analytics suite (`python -m pytest -q`, ~3687+ tests, coverage gate >=80 must hold with the new modules), delete-gate file, match golden byte-stable after all phase edits.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest -q 2>&1 | tail -5 && python -m pytest tests/test_e1_delete_gate.py tests/test_e2_match_score_golden.py -q</automated>
  </verify>
  <acceptance_criteria>Script committed + compiles (python -m py_compile) + pattern-faithful (env-only creds, read-only proof, sanitized single-JSON, non-zero on skip); full suite green with coverage gate intact; delete-gate green; match golden byte-stable; live-run status (done or pending-founder-env) recorded.</acceptance_criteria>
  <done>Phase 115 acceptance surface complete: fixture gates green, real-account runbook committed, nothing regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| script -> live exchange | read-only creds from env, one-off via railway ssh |
| script -> prod DB | service-role READ-ONLY (csv_daily_returns, holdings, api_keys) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-11 | Information disclosure | ground-truth stdout | mitigate | whitelisted fields only; drift as pct + bool; raw account USD appears only in the founder-run sanitized JSON, never in logs/tests |
| T-115-12 | Elevation | script write access | mitigate | zero table writes by construction (pattern-copied); read-only key scope proven before fetch |
| T-115-13 | Repudiation | silent skip of the live run | mitigate | non-zero exit + explicit SKIP reason; SUMMARY records pending-founder-env |
| T-115-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
cd analytics-service && python -m pytest -q
cd analytics-service && python -m pytest tests/test_e1_delete_gate.py tests/test_e2_match_score_golden.py tests/test_e2_parity_oracle.py -q
</verification>

<success_criteria>
- Oracle independent and corruption-sensitive; all STITCH pins re-proven by a second observer.
- Ground-truth script committed, runbook documented, live run done or honestly pending founder env.
- Full suite + coverage gate + delete-gate + match golden all green post-phase.
</success_criteria>

<output>
Create `.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-05-SUMMARY.md` when done.
</output>
