---
phase: 115-e2-allocator-equity-reconstruction-scope-gated-verify-first
plan: 04
type: execute
wave: 2
depends_on: [115-01]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_derive_broker_dailies_dualmode.py
  - analytics-service/tests/test_e2_deribit_allocator_dailies.py
autonomous: true
requirements: [STITCH-01, BACKBONE-02]
must_haves:
  truths:
    - "An eligible all-deribit allocator key produces per-key csv_daily_returns rows via key-mode derive_broker_dailies (keyed (api_key_id, date) with denormalized allocator_id = api_keys.user_id) — closing the dogfooding gap where all-deribit allocators got NOTHING"
    - "The rows satisfy the Phase-36 blend D3 eligibility (isPerKeyDailiesEligibleKey / liveBaselineMetricsFromPerKeyDailies), so the EXISTING frontend blend renders shape + KPIs for an all-deribit allocator — a real existing consumer, not future scaffolding"
    - "The root cause of the gap is identified (enqueue coverage vs handler path vs never-backfilled) and fixed at source; a mutation-falsifiable regression test fails without the fix"
    - "The legacy store, its jobs, allocator_equity_snapshots, the deribit carve-out, and the compute_twr method are NOT touched"
  artifacts:
    - path: "analytics-service/tests/test_e2_deribit_allocator_dailies.py"
      provides: "reproduction + regression: eligible deribit allocator key -> csv_daily_returns rows -> D3-eligible for the blend"
    - path: "analytics-service/services/job_worker.py"
      provides: "root-cause fix in the key-mode derive_broker_dailies path/enqueue for deribit allocator keys (only if the root cause lands here)"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "csv_daily_returns (api_key_id, date, allocator_id)"
      via: "key-mode deribit native derive upsert"
      pattern: "csv_daily_returns"
    - from: "analytics-service/tests/test_e2_deribit_allocator_dailies.py"
      to: "run_derive_broker_dailies_job key-mode + deribit native branch"
      via: "mocked deribit account state + native ledger"
      pattern: "run_derive_broker_dailies_job"
---

<objective>
Close the all-deribit dogfooding gap: make deribit allocator keys produce per-key `csv_daily_returns` through key-mode `derive_broker_dailies`, so the EXISTING Phase-36 frontend blend (queries.ts `liveBaselineMetricsFromPerKeyDailies` — shape + KPIs) picks them up. Today all-deribit allocators get NOTHING (the legacy store's carve-out hard-fails deribit, and the dailies path either isn't reaching them or isn't producing rows). This gives Phase 115 a real, shipping consumer — the derivation core (plans 02/03) is no longer unconsumed; deribit allocators see a curve immediately.

Purpose: verify-first — reproduce the gap, root-cause it, fix at source, prove the blend now renders for an all-deribit allocator.
Output: root-cause fix + a mutation-falsifiable regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-RESEARCH.md
@.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-01-SUMMARY.md

BINDING constraints:
- DO NOT touch the legacy store: no edits to equity_reconstruction.py, no writes/upserts/reads-for-merge on allocator_equity_snapshots, do not remove the legacy deribit carve-out (equity_reconstruction.py:2084/2430 keeps failing loud for the store's own jobs), do not touch the compute_twr method (P114 delete-gate imports the module).
- Worker-only key decryption LOCKED: only _allocator_key_preflight decrypts (never trust a job-payload allocator_id; the authoritative owner is api_keys.user_id — job_worker.py:1910).
- No fabricated data: if the deribit native derive structurally refuses (UnmarkableCurrency / InceptionReconciliation / incomplete ledger), it must keep failing loud with a terminal analytics stamp — NEVER synthesize spot-gap rows.
- Migration caution: supabase/migrations/** AUTO-APPLIES to PROD on merge. Only add a migration if the root cause is genuinely an enqueue-registration gap; if so, gate it behind a supabase/tests/test_*.sql check and call it out for the merge watcher. Prefer a code-level fix.
- Account-size leak: never log raw NAV/flow USD (T-73-02); scrub error strings (scrub_freeform_string).
</context>

<interfaces>
From analytics-service/services/job_worker.py:
- run_derive_broker_dailies_job(job) (L1869) — DUAL-MODE; is_key_mode = bool(job.get("api_key_id")). Key-mode: _allocator_key_preflight -> api_key_id = ctx.key_row["id"], allocator_id = ctx.key_row["user_id"]; upsert csv_daily_returns on_conflict="api_key_id,date"; NO compute_analytics_from_csv enqueue; NO strategy_analytics stamp.
- The deribit native branch: `if venue == "deribit":` (L2111) -> fetch_deribit_native_account_state, build_deribit_native_ledger, combine_native_ledger (services.deribit_ingest / services.broker_dailies). This branch runs for BOTH modes (it reads venue, not role).
- Key-mode dispatch: L5829 `elif kind == "derive_broker_dailies":`.

Eligible-key predicate (phase35 / D3): is_active AND sync_status IS DISTINCT FROM 'revoked' AND disconnected_at IS NULL.

Existing enqueue sites for derive_broker_dailies:
- strategy-mode: sync_trades epilogue (job_worker.py:1504), cron re-sync (cron.py:448) — both strategy_id-scoped.
- key-mode (api_key_id-scoped): scripts/phase35_backfill_enqueue.py (one-off backfill, role/venue-agnostic predicate) + connect/reconnect enqueue (RESEARCH R6: migration 076 / 20260422101911).

Phase-36 blend consumer (frontend, DO NOT edit here — plan 04 only makes the rows exist):
- src/lib/queries.ts liveBaselineMetricsFromPerKeyDailies (D1/D2/D3); isPerKeyDailiesEligibleKey. The blend is used IFF EVERY eligible key has a non-empty per-key series.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: reproduce + root-cause the all-deribit gap (RED)</name>
  <files>analytics-service/tests/test_e2_deribit_allocator_dailies.py</files>
  <read_first>analytics-service/services/job_worker.py L1869-2200 (the whole key-mode derive + deribit native branch — the code being characterized), analytics-service/tests/test_derive_broker_dailies_dualmode.py (WHOLE file — the mock-exchange + capture-upsert harness to reuse; note the deribit_flow_fixtures import), analytics-service/tests/fixtures/deribit_flow_fixtures.py (the deribit native-ledger fixtures), analytics-service/scripts/phase35_backfill_enqueue.py (the key-mode enqueue predicate — does it reach deribit?), the 115-01 A1 by-venue numbers (deribit-with-0-rows count)</read_first>
  <behavior>
    - Test 1 (handler path): run_derive_broker_dailies_job in KEY-MODE with venue=deribit + a mocked native account state + native ledger fixture upserts csv_daily_returns keyed (api_key_id, date) with allocator_id = key.user_id, strategy_id None, and does NOT enqueue compute_analytics_from_csv. This either passes (handler is fine → gap is enqueue/backfill) or fails (handler bug → the RED to fix). Record which in the SUMMARY.
    - Test 2 (structural refusal still fails loud): a deribit key-mode derive whose native core raises NavReconstructionError/LedgerCompletenessError stamps a terminal analytics failure and writes ZERO csv_daily_returns rows (no fabricated spot-gap data).
    - Test 3 (enqueue coverage characterization): assert the key-mode enqueue predicate (phase35 + connect-time path) INCLUDES an eligible deribit allocator key — i.e. deribit is not filtered out at enqueue. If the current code filters deribit, this test is the RED that pins the enqueue-side root cause.
  </behavior>
  <action>
  Create tests/test_e2_deribit_allocator_dailies.py reusing the dualmode harness (mock exchange, capture upserts, deribit fixtures). Write the three tests to CHARACTERIZE the exact failure point. Run them and record in the SUMMARY which layer is broken: (H) handler key-mode+deribit path, (E) enqueue never reaches deribit allocator keys, or (B) enqueue is fine but the backfill never ran for existing deribit keys (a data state, remediated by the 115-01 backfill command, not a code fix). This task's deliverable is the failing/passing characterization that scopes Task 2 — at least one test must be RED-capable so Task 2 has a concrete target (if ALL three pass, the root cause is (B) data-only: record that, run the backfill enqueue as the fix, and Task 2 becomes the durable regression pin only).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_deribit_allocator_dailies.py -q --no-header -rN; cd .. && true</automated>
  </verify>
  <acceptance_criteria>Three characterization tests exist and run; the SUMMARY records the root-cause layer (H/E/B) with evidence; delete-gate untouched.</acceptance_criteria>
  <done>The gap is reproduced and root-caused, not guessed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: fix at source + prove the Phase-36 blend renders for an all-deribit allocator (GREEN)</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_e2_deribit_allocator_dailies.py, analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>analytics-service/services/job_worker.py (the exact lines Task 1 identified), analytics-service/tests/test_e2_deribit_allocator_dailies.py (Task-1 RED), analytics-service/tests/test_derive_broker_dailies_dualmode.py (to extend with a deribit key-mode case alongside the ccxt ones)</read_first>
  <behavior>
    - Test 4 (root-cause fixed): the Task-1 RED test(s) now GREEN — an eligible deribit allocator key produces >=2 dense csv_daily_returns rows keyed (api_key_id, date, allocator_id).
    - Test 5 (mutation-falsifiable wiring): neutering the fix (e.g. reverting the deribit-inclusion in the enqueue predicate, or breaking the key-mode deribit upsert key) makes Test 4 fail — prove the wiring, not just the helper (test each call site invokes the fix).
    - Test 6 (D3 blend eligibility): given the produced rows for an all-deribit allocator, assert they satisfy the Phase-36 eligibility contract — a non-empty per-key series exists for every eligible key so liveBaselineMetricsFromPerKeyDailies would use the blend (assert on the Python-side row shape the TS predicate requires: is_active/sync_status/disconnected_at eligible key WITH >=1 csv_daily_returns row; if a lightweight TS unit test is cheaper, add it under src/lib/__tests__ referencing the fixture shape — planner's discretion, but the Python assertion is mandatory).
    - Test 7 (dualmode parity): the ccxt key-mode + strategy-mode assertions in test_derive_broker_dailies_dualmode.py remain byte-unchanged (no regression from the deribit fix).
  </behavior>
  <action>
  Fix at the root cause identified in Task 1:
  - If (H) handler bug: correct the key-mode + deribit native branch so it upserts csv_daily_returns with the (api_key_id, date) key + denormalized allocator_id, mirroring the ccxt key-mode upsert exactly (never the strategy path). Do NOT touch the deribit strategy-mode behavior.
  - If (E) enqueue gap: extend the key-mode enqueue to include eligible deribit allocator keys. Prefer a code-level fix (job_worker / a script predicate). ONLY add a migration if the enqueue registration genuinely lives in SQL; if so, gate it with a supabase/tests/test_*.sql check and flag the prod auto-apply in the SUMMARY for the merge watcher.
  - If (B) data-only: run `railway ssh "cd /app && python -m scripts.phase35_backfill_enqueue"` (idempotency-guarded) to backfill the existing deribit keys; keep Test 4-6 as the durable regression pins so the handler/enqueue can never silently regress.
  In all branches: no legacy-store contact, no carve-out edit, no compute_twr touch, fail-loud on structural refusal (Test 2 stays green). Extend test_derive_broker_dailies_dualmode.py with a deribit key-mode case. Prove RED->GREEN and the mutation-falsifiable neutering.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_e2_deribit_allocator_dailies.py tests/test_derive_broker_dailies_dualmode.py tests/test_e1_delete_gate.py tests/test_e2_match_score_golden.py -q</automated>
  </verify>
  <acceptance_criteria>Tests 4-7 green; Task-1 RED now passes; neutering the fix reddens Test 4 (wiring proven); ccxt dualmode unchanged; delete-gate + match golden green; no store/carve-out/compute_twr contact (grep-verify the diff touches none of equity_reconstruction.py / allocator_equity_snapshots / compute_twr).</acceptance_criteria>
  <done>All-deribit allocators produce per-key dailies and the existing Phase-36 blend renders for them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker -> deribit exchange | read-only key decrypted ONLY in _allocator_key_preflight; owner from api_keys.user_id, never job payload |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-14 | Tampering (self) | deribit native derive gaps | mitigate | structural refusal -> terminal fail + zero rows; never fabricate spot-gap data (Test 2) |
| T-115-15 | Information disclosure | derive error/log strings | mitigate | scrub_freeform_string; counts/codes only, never raw NAV/flow USD (T-73-02) |
| T-115-16 | Elevation | job-payload allocator_id | mitigate | allocator_id sourced from _allocator_key_preflight (api_keys.user_id), never the payload |
| T-115-17 | Tampering | accidental legacy-store edit | mitigate | diff grep-gate: touches none of equity_reconstruction.py / allocator_equity_snapshots / compute_twr |
| T-115-SC | Tampering | package installs | accept | none this phase |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_e2_deribit_allocator_dailies.py tests/test_derive_broker_dailies_dualmode.py tests/test_e1_delete_gate.py -q
git diff --name-only | grep -Ev 'equity_reconstruction|allocator_equity_snapshots' || true   # legacy store untouched
</verification>

<success_criteria>
- Eligible all-deribit allocator keys produce per-key csv_daily_returns; the existing Phase-36 blend renders shape + KPIs for them.
- Root cause identified + fixed at source; mutation-falsifiable regression pins in place.
- Legacy store / carve-out / compute_twr untouched; structural refusals still fail loud.
- Delete-gate + match golden green.
</success_criteria>

<output>
Create `.planning/phases/115-e2-allocator-equity-reconstruction-scope-gated-verify-first/115-04-SUMMARY.md` when done. Record the root-cause layer (H/E/B) and, if a migration was added, flag the prod auto-apply for the merge watcher.
</output>
