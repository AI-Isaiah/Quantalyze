---
phase: 114-e1-backbone-absorption-sharpe-twr-deletion
plan: 02
type: execute
wave: 2
depends_on: [114-01]
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/routers/portfolio.py
  - analytics-service/tests/test_e1_sharpe_twr_parity.py
autonomous: true
requirements: [BACKBONE-01]

must_haves:
  truths:
    - "The weighted portfolio series routes through compute_all_metrics: portfolio Sharpe/vol (and verify_strategy Sharpe) are read from backbone output, TWR scalars from a backbone-module equity-endpoint helper"
    - "Zero remaining call sites of compute_twr or _compute_sharpe_and_vol in routers/portfolio.py (the defs still exist until plan 114-03; only calls are gone)"
    - "The vol_status/sharpe_status data-quality channel (portfolio.py L1089-1090) is byte-preserved for every REACHABLE status (ok, insufficient_history, zero_volatility, nan_vol); nan_mean/nan_sharpe are proven-unreachable dead branches under pandas skipna — documented in the helper docstring, never synthesized"
    - "sharpe_vol_status_from_backbone returns the graceful legacy tuple on a degenerate all-NaN (len>=2) series WITHOUT raising — a pre-backbone short-circuit keeps degenerate input out of compute_all_metrics, so a data-quality status can never regress to a production 500"
    - "The golden oracle from 114-01 stays green, now ALSO asserting the new helpers ≡ oracle"
  artifacts:
    - path: "analytics-service/services/metrics.py"
      provides: "Backbone-derived replacement helpers"
      exports: ["total_return_from_equity", "sharpe_vol_status_from_backbone"]
    - path: "analytics-service/routers/portfolio.py"
      provides: "All six legacy call sites re-routed"
  key_links:
    - from: "analytics-service/routers/portfolio.py"
      to: "services.metrics helpers"
      via: "import + six re-routed call sites"
      pattern: "total_return_from_equity|sharpe_vol_status_from_backbone"
    - from: "analytics-service/services/metrics.py::sharpe_vol_status_from_backbone"
      to: "compute_all_metrics"
      via: "internal single call with periods_per_year"
      pattern: "compute_all_metrics\\("
---

<objective>
Re-route all six legacy call sites in `routers/portfolio.py` onto backbone-derived
helpers in `services/metrics.py`, byte-preserving displayed numbers (proven by
the 114-01 oracle) and the vol_status/sharpe_status data-quality channel. The
legacy definitions stay in place until plan 114-03 — this plan only removes
their USES, so a parity failure here aborts with nothing deleted.

Purpose: BACKBONE-01 clause 1 — "the weighted portfolio series routes through
compute_all_metrics" — landed as a reversible step strictly between the golden
gate (114-01) and the delete (114-03).
Output: two new helpers in services/metrics.py; routers/portfolio.py with zero
legacy calls; oracle extended to cover the helpers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-e1-backbone-absorption-sharpe-twr-deletion/114-CONTEXT.md
@.planning/phases/114-e1-backbone-absorption-sharpe-twr-deletion/114-01-SUMMARY.md
</context>

<interfaces>
Contracts the executor implements against (extracted by the planner; do not
re-derive from scratch):

From services/metrics.py (existing):
- compute_all_metrics(returns: pd.Series, benchmark_returns=None, periods_per_year: int = DEFAULT_PERIODS_PER_YEAR (=252), cumulative_method="geometric", day_basis="calendar") -> MetricsResult
- MetricsResult subscript proxies metrics_json: m["volatility"], m["sharpe"] (both already _safe_float'd -> None on NaN/Inf); requires a DatetimeIndex on returns (monthly resample)
- _safe_float(value) -> float | None

From routers/portfolio.py (legacy contract being replaced):
- _compute_sharpe_and_vol(returns) -> (vol, mean_ret, sharpe, status); status in {"ok","insufficient_history","zero_volatility","nan_vol","nan_mean","nan_sharpe"}; both call sites DISCARD mean_ret, so the replacement drops it from the tuple. REACHABILITY (planner-verified vs pandas skipna): ok / insufficient_history / zero_volatility / nan_vol are reachable; nan_mean / nan_sharpe are unreachable dead branches (once vol is finite+nonzero the same >=2 non-NaN observations give a finite mean and finite mean/vol). nan_vol == "vol is NaN" == std over all-NaN or a single non-NaN observation.
- compute_twr(equity, []) === equity.iloc[-1]/equity.iloc[0] - 1 with guards: len<2 -> None, iloc[0]==0 -> None (single-segment collapse, proven by the 114-01 oracle)

New helper signatures to create in services/metrics.py:
- total_return_from_equity(equity: pd.Series) -> float | None
- sharpe_vol_status_from_backbone(returns: pd.Series, periods_per_year: int = 252) -> tuple[float | None, float | None, str]  # (vol, sharpe, status)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the two backbone-derived helpers to services/metrics.py + extend the oracle</name>
  <files>analytics-service/services/metrics.py, analytics-service/tests/test_e1_sharpe_twr_parity.py</files>
  <read_first>
    - analytics-service/tests/test_e1_sharpe_twr_parity.py (whole file — the 114-01 oracle you extend)
    - analytics-service/services/metrics.py L326-400 (_safe_float + module layout), L398-410 (compute_all_metrics signature)
    - analytics-service/routers/portfolio.py L596-626 (the legacy status-code contract being reproduced)
  </read_first>
  <action>
    In services/metrics.py, below compute_all_metrics (keep module layout;
    match existing docstring conventions), add:

    total_return_from_equity(equity): return None when equity is None or
    len < 2; take first = float(equity.iloc[0]); when first == 0.0 log a
    warning mirroring the legacy M-0698 begin_val=0 message shape and return
    None; otherwise return _safe_float(float(equity.iloc[-1]) / first - 1.0).
    WORDING (WARNING 3): the docstring/comment must say "the deleted TWR
    scalar" / "the forward TWR scalar" and MUST NOT contain the bare compute_twr
    token, or the plan-114-03 tree-walk delete-gate goes RED on metrics.py.
    Docstring MUST state: this is the backbone-blessed replacement for the
    deleted portfolio_metrics TWR scalar at its four events=[] call sites;
    on a cumprod series whose first value is (1+r_0) it intentionally
    preserves the legacy day-0-exclusion semantics (byte-identical mandate,
    Phase 114) — do NOT swap in metrics_json cumulative_return, which
    includes day 0 (the 114-01 oracle asserts the divergence).

    sharpe_vol_status_from_backbone(returns, periods_per_year=252): reproduce
    the legacy REACHABLE status contract from backbone output, per D-clause in
    114-CONTEXT (statuses feed the data_quality channel at portfolio.py
    L1089-1090). CRITICAL (BLOCKER fix): TWO pre-backbone guards mirror the
    legacy short-circuits AND keep a degenerate series OUT of the full pipeline
    (monthly resample / mtd-ytd slices / cumprod / qs.stats.*). compute_all_metrics
    guards ONLY len<2 (metrics.py:457, planner-verified) and raises ValueError
    there — it is UNPROVEN to degrade gracefully on an all-NaN len>=2 series, so
    feeding one in risks a production 500 where legacy returned a graceful
    data-quality status. The guards prevent that:
      - len(returns) <= 1 -> return (None, None, "insufficient_history") WITHOUT
        calling the backbone (matches legacy L612-613; also dodges the :457 raise);
      - pd.isna(returns.std()) -> return (None, None, "nan_vol") WITHOUT calling
        the backbone. Legacy nan_vol IS exactly "vol is NaN" (L616,620): a
        std(ddof=1) over an all-NaN series OR a single non-NaN observation is
        NaN, and _safe_float(NaN)->None. Detecting it via pandas std (skipna —
        the SAME call legacy used at L614) faithfully reproduces the legacy
        nan_vol OUTPUT and structurally never lets an all-NaN series reach the
        pipeline. This guard changes nothing on the normal path (a real series
        has finite std) and does NOT intercept the flat-series case (std==0.0
        is finite, not NaN -> falls through to the zero_volatility branch below);
      - else call compute_all_metrics(returns, periods_per_year=periods_per_year)
        ONCE; read vol = m["volatility"], sharpe = m["sharpe"];
      - vol is None -> (None, None, "nan_vol") (defensive belt-and-braces: the
        std guard above should already have caught every NaN-vol case);
      - vol == 0.0 -> (0.0, None, "zero_volatility");
      - else -> (vol, sharpe, "ok") (sharpe is finite whenever vol is finite and
        nonzero on real data).
    DEAD BRANCHES (documented, NOT reproduced): legacy "nan_mean"/"nan_sharpe"
    (L622-625) are UNREACHABLE under pandas skipna — once vol is finite+nonzero
    the same >=2 non-NaN observations yield a finite mean and a finite mean/vol,
    so neither status can occur. Do NOT synthesize a nan_mean/nan_sharpe branch;
    state their unreachability in the docstring. Also note mean_ret was dropped
    from the legacy 4-tuple because both production call sites discard it
    (surgical-change rule), and status != "ok" always implies sharpe None.
    WORDING (WARNING 3 — the plan-114-03 delete-gate bans the bare compute_twr
    symbol tree-wide): in this helper's docstring refer to "the deleted TWR
    scalar" / "the forward TWR scalar", NEVER the bare compute_twr token, or the
    gate goes RED.

    Extend tests/test_e1_sharpe_twr_parity.py TestBackboneDerivationParity with
    helper-level assertions (these are the permanent wiring pins):
      - total_return_from_equity on the cumprod of fixtures (a)/(d) and on the
        equity fixture (e) equals the oracle TWR values at rel 1e-12; the
        1-point and first==0 guards return None;
      - sharpe_vol_status_from_backbone on fixture (a) returns (vol, sharpe,
        "ok") matching the oracle at rel 1e-12; on flat (b) returns
        (0.0, None, "zero_volatility"); on 1-sample (c) returns
        (None, None, "insufficient_history");
      - BLOCKER fix b — on the DEGENERATE all-NaN fixture (f) returns EXACTLY
        (None, None, "nan_vol") and DOES NOT RAISE, matching the legacy
        graceful baseline pinned in 114-01 (this is the anti-500 proof; assert
        inside a not.pytest.raises / try-no-except style so a pipeline raise
        fails the test loudly rather than erroring out);
      - a monkeypatch guard proving the short-circuit is STRUCTURAL, not
        incidental: patch services.metrics.compute_all_metrics to raise on call,
        then assert sharpe_vol_status_from_backbone on fixtures (c) and (f)
        STILL returns the graceful tuples (i.e. the degenerate paths provably
        never invoke the pipeline). Restore the patch after.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_e1_sharpe_twr_parity.py -q && .venv/bin/python -m pytest tests/test_metrics.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Both helpers exist in services/metrics.py with the documented contracts
    - sharpe_vol_status_from_backbone has the len<=1 AND pd.isna(std) pre-backbone guards; docstring documents nan_mean/nan_sharpe as unreachable dead branches and uses "deleted TWR scalar" wording (never the bare compute_twr token)
    - Degenerate all-NaN fixture (f) proven to return (None,None,"nan_vol") without raising; monkeypatch proves the degenerate paths never call compute_all_metrics
    - Oracle green including the new helper-level assertions (legacy baseline class still green too)
    - Existing test_metrics.py suite untouched-green (no regression to compute_all_metrics)
  </acceptance_criteria>
  <done>Backbone-derived replacements exist, are oracle-proven ≡ legacy, and nothing in production consumes them yet — a pure additive step.</done>
</task>

<task type="auto">
  <name>Task 2: Re-route the six call sites in routers/portfolio.py</name>
  <files>analytics-service/routers/portfolio.py</files>
  <read_first>
    - analytics-service/routers/portfolio.py L28-40 (import block), L805-845 (per-strategy TWR loop + portfolio TWR), L938-990 (benchmark TWR + sharpe/vol + statuses), L1085-1095 (status persistence), L2294-2310 (verify_strategy sites)
  </read_first>
  <action>
    Surgical re-route — six sites, nothing else (Rule 3; do not touch the
    legacy _compute_sharpe_and_vol def at L596, it dies in plan 114-03):

    1. L32 import: change to import compute_mwr and compute_period_returns only
       (drop compute_twr); add total_return_from_equity and
       sharpe_vol_status_from_backbone to the existing services.metrics import.
    2. L811: twr = total_return_from_equity(eq).
    3. L835-837: portfolio_twr = total_return_from_equity of the cumprod of
       portfolio_returns_series (keep the expression shape; do not reorder the
       surrounding logic).
    4. L948: btc_twr = total_return_from_equity of the cumprod of (1 + b_aligned).
    5. L985: vol, sharpe, sharpe_status = sharpe_vol_status_from_backbone(
       portfolio_returns_series) — note the tuple is now 3-wide (mean_ret slot
       removed); L986 vol_status line stays byte-identical.
    6. L2302: twr = total_return_from_equity(cumulative). L2306:
       _vol, sharpe, _sharpe_status = sharpe_vol_status_from_backbone(returns).
       Update the L2304-2305 comment ("extracted into helper so the three
       router callsites ... don't drift apart") to state Sharpe/vol now derive
       from the unified backbone compute_all_metrics (Phase 114 / BACKBONE-01).
       WORDING (WARNING 3): any new/edited comment here must refer to "the
       deleted TWR scalar" and MUST NOT contain the bare compute_twr token
       (the plan-114-03 gate scans portfolio.py); use total_return_from_equity
       by name when referring to the new helper.

    Confirm with grep that routers/portfolio.py retains ZERO call sites of the
    two legacy symbols outside the L596 def block, and that compute_twr no
    longer appears in the L32 import. Then run the full analytics test suite —
    byte-parity means every existing router/behavior test must stay green with
    no assertion edits (if one fails, that IS a parity break: stop, do not
    adjust the test, re-check the re-route against the oracle).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - grep -n over routers/portfolio.py shows compute_twr only absent (import + calls gone) and _compute_sharpe_and_vol only at its def block (L596 region) and nowhere as a call
    - Full pytest suite green with ZERO test-file edits in this task
    - vol_status/sharpe_status persistence lines (L1089-1090) byte-unchanged
    - Oracle file green (run as part of the full suite)
  </acceptance_criteria>
  <done>BACKBONE-01 clause 1 satisfied: Sharpe/vol/TWR at all production call sites derive from the unified backbone, displayed numbers provably unchanged, legacy defs now dead code awaiting the gated delete in 114-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB series -> metrics compute | unchanged; same series inputs, new derivation path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-03 | Tampering | portfolio analytics payload (Sharpe/vol/TWR + status codes) | mitigate | oracle-pinned helpers; full suite must pass with zero test edits (any edit = parity break) |
| T-114-04 | DoS | compute_all_metrics is heavier than the deleted 6-line helper at 2 call sites | accept | once-per-compute-job / once-per-verification frequency; no hot loop (per-strategy loop uses the cheap endpoint helper, not the backbone call) |
| T-114-SC | Tampering | supply chain | accept | zero new packages installed |
</threat_model>

<verification>
- Full `pytest -q` green from analytics-service with no test-file changes in Task 2
- Oracle + census green
- git diff limited to services/metrics.py, routers/portfolio.py, tests/test_e1_sharpe_twr_parity.py
</verification>

<success_criteria>
- Six call sites re-routed; helpers oracle-proven; data-quality statuses preserved
- Legacy defs still present (delete is 114-03's gated job)
</success_criteria>

<output>
Create `.planning/phases/114-e1-backbone-absorption-sharpe-twr-deletion/114-02-SUMMARY.md` when done.

SUMMARY MUST include this verifier-guidance note (WARNING 2): the ROADMAP goal
"the allocator/scenario Sharpe and TWR DERIVE from the one backbone
(compute_all_metrics)" is satisfied in the intended "single home in the backbone
MODULE" sense. Sharpe/vol are read directly from compute_all_metrics output;
TWR is computed by total_return_from_equity — an endpoint-ratio
(eq.iloc[-1]/eq.iloc[0]-1) helper CO-LOCATED in services/metrics.py (the backbone
module) rather than a value read out of the MetricsResult dict. This is the
deliberate, byte-identical choice (the 114-01 oracle asserts that
cumulative_return differs from legacy TWR by the day-0 factor, so reading
cumulative_return would CHANGE displayed numbers). Downstream verifiers must NOT
flag "TWR does not literally come out of compute_all_metrics" as a goal-miss —
the goal is single-source-in-the-backbone-module, and that is met.
</output>
