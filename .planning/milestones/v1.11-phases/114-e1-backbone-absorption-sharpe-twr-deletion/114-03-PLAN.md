---
phase: 114-e1-backbone-absorption-sharpe-twr-deletion
plan: 03
type: execute
wave: 3
depends_on: [114-02]
files_modified:
  - analytics-service/services/portfolio_metrics.py
  - analytics-service/routers/portfolio.py
  - analytics-service/tests/test_portfolio_metrics.py
  - analytics-service/tests/test_portfolio_router_audit_2026_05_07.py
  - analytics-service/tests/test_nav_twr.py
  - analytics-service/tests/test_coverage_extras.py
  - analytics-service/tests/test_e1_sharpe_twr_parity.py
  - analytics-service/tests/test_e1_delete_gate.py
autonomous: true
requirements: [BACKBONE-01]

must_haves:
  truths:
    - "compute_twr and _compute_sharpe_and_vol no longer exist as live symbols anywhere in analytics-service (hasattr-proven, not just grep)"
    - "compute_mwr, compute_modified_dietz, compute_period_returns still import AND function post-delete (the cashflow/IRR path the backbone cannot reproduce)"
    - "A permanent Python delete-gate fails CI if either deleted symbol re-enters as a live symbol or as a tree reference — with the equity_reconstruction METHOD exemption pinned until Phase 115"
    - "Legacy-symbol tests are migrated, preserving their INTENT (status codes, None-guards, nav_twr agreement) against the new helpers, never silently dropped"
  artifacts:
    - path: "analytics-service/services/portfolio_metrics.py"
      provides: "Cashflow/IRR module only: compute_mwr, compute_modified_dietz, compute_period_returns, _parse_date"
    - path: "analytics-service/tests/test_e1_delete_gate.py"
      provides: "Permanent re-entry gate + KEEP-path import/function test"
      min_lines: 80
      contains: "hasattr"
  key_links:
    - from: "analytics-service/tests/test_e1_delete_gate.py"
      to: "services.portfolio_metrics + routers.portfolio"
      via: "live-symbol hasattr assertions (comment/string-proof, stronger than grep for Python)"
      pattern: "not hasattr"
    - from: "analytics-service/tests/test_e1_delete_gate.py"
      to: "whole analytics-service tree"
      via: "pathlib walk with concatenated tokens, self-excluded, >=100-file neuter guard"
      pattern: "parents\\[1\\]"
---

<objective>
Execute the gated delete: remove `compute_twr` from services/portfolio_metrics.py
and `_compute_sharpe_and_vol` from routers/portfolio.py (both dead code since
114-02), migrate every legacy-symbol test to the backbone-derived helpers
WITHOUT losing test intent, prove the KEPT cashflow/IRR path still imports and
functions, and install the permanent Python delete-gate that prevents re-entry.

Purpose: BACKBONE-01 clauses 2-4. The 114-01 golden is green and the 114-02
re-route shipped with zero test edits — the delete is now provably safe.
Output: trimmed portfolio_metrics.py; portfolio.py without the legacy def;
migrated test files; permanent gate test_e1_delete_gate.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/114-e1-backbone-absorption-sharpe-twr-deletion/114-CONTEXT.md
@.planning/phases/114-e1-backbone-absorption-sharpe-twr-deletion/114-01-SUMMARY.md
@.planning/phases/114-e1-backbone-absorption-sharpe-twr-deletion/114-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the two symbols + migrate legacy-symbol tests (intent-preserving)</name>
  <files>analytics-service/services/portfolio_metrics.py, analytics-service/routers/portfolio.py, analytics-service/tests/test_portfolio_metrics.py, analytics-service/tests/test_portfolio_router_audit_2026_05_07.py, analytics-service/tests/test_nav_twr.py, analytics-service/tests/test_coverage_extras.py, analytics-service/tests/test_e1_sharpe_twr_parity.py</files>
  <read_first>
    - analytics-service/services/portfolio_metrics.py L1-70 (module docstring + compute_twr block L60-151), L154-165 (compute_mwr uses _parse_date — KEEP _parse_date)
    - analytics-service/routers/portfolio.py L590-630 (_compute_sharpe_and_vol block to delete)
    - analytics-service/tests/test_portfolio_metrics.py L1-130 and L480-520 (compute_twr tests incl. M-0698 warn tests)
    - analytics-service/tests/test_portfolio_router_audit_2026_05_07.py L1-50 and L260-310 (_compute_sharpe_and_vol status tests M-0626/M-0615)
    - analytics-service/tests/test_nav_twr.py L1-60 (header + imports) and L290-325 (test_twr_agrees_with_compute_twr — the only with-events use in the tree)
    - analytics-service/tests/test_coverage_extras.py L15-25 and L185-220, L288-310 (compute_twr edge tests)
  </read_first>
  <action>
    Production deletes:
    - services/portfolio_metrics.py: delete the whole compute_twr block
      (function + its section banner, L60-151) and remove "TWR" from the module
      docstring (now: MWR, Modified Dietz, period returns — the cashflow/IRR
      path the backbone cannot reproduce, per BACKBONE-01). KEEP _parse_date
      (compute_mwr depends on it), the logger, and all imports still used
      (numpy/scipy/pandas remain used by compute_mwr).
    - routers/portfolio.py: delete the _compute_sharpe_and_vol function block
      (def + docstring, L596-625 region). 114-02 already removed all calls.

    Test migrations — preserve INTENT (Rule 9), never silently drop a behavior
    pin; the new home for each intent is the backbone helper:
    - test_portfolio_metrics.py: remove compute_twr from the import and delete
      the compute_twr test functions (basic chaining, day-0 skip, M-0698
      dropped-sub-period warning tests). Their surviving intents: the None-guards
      and endpoint semantics are already pinned on total_return_from_equity by
      the 114-01/02 oracle; the M-0698 begin-value-0 WARNING intent must be
      re-pinned — add a caplog test (in test_e1_sharpe_twr_parity.py, permanent
      class) asserting total_return_from_equity on a first-value-0.0 equity
      series returns None AND emits a warning. The with-events chaining tests
      have no successor by design: the cashflow-TWR machinery is exactly the
      dead generality being deleted (all production callers passed events=[]);
      state this in the task commit message.
    - test_portfolio_router_audit_2026_05_07.py: the M-0626/M-0615 status-code
      tests are live intent — PORT them to sharpe_vol_status_from_backbone
      (import from services.metrics; adjust tuple width 4 -> 3, drop mean_ret
      expectations, keep every REACHABLE status expectation identical: ok,
      insufficient_history, zero_volatility). ADD the nan_vol case the legacy
      suite never covered (BLOCKER follow-through): assert the helper on an
      all-NaN len>=2 series returns (None,None,"nan_vol") without raising — so
      the ported suite covers every reachable code, not just the three the
      legacy tests exercised. Do NOT add nan_mean/nan_sharpe assertions (proven
      unreachable dead branches — 114-02). Update the file header list (L12) to
      name the new helper and note the dead-branch exclusion.
    - test_nav_twr.py: rewrite test_twr_agrees_with_compute_twr to keep its
      agreement INTENT without the deleted comparator: replace the
      compute_twr(equity, events) scalar with the hand-derived closed-form
      sub-period chain written out literally (same style as the exp_cum
      expression already used at L286-292 of this file): for equity
      [1000, 1100, 1050, 1200] with a +100 deposit on 2026-01-03 the expected
      chained value is (1100/1000) * ((1050-100)/1100) * (1200/1050) - 1 — the
      end-of-day flow-numerator convention; derive it in-test from the fixture
      numbers, assert nav_twr_cum against it at rel 1e-12. Also RENAME the test
      away from "..._compute_twr" (the function name itself embeds the banned
      token). WORDING (WARNING 3): reword the module docstring (L13) and the
      test docstring (L295-297) to say "the deleted forward TWR scalar" /
      "the retired portfolio_metrics TWR scalar" — the rewritten file must
      contain the bare compute_twr token in ZERO places (imports, call sites,
      function NAMES, docstrings, and comments all included), because the
      Task-2 tree-walk gate scans test_nav_twr.py and this file is NOT on the
      METHOD-exemption list. Grep the file for the bare token before finishing
      the task; expect zero hits.
    - test_coverage_extras.py: remove compute_twr from the import and delete
      its test methods (too-short None, day-0 skip, mid-period withdrawal,
      all-subperiods-invalid, single-obs skip). Too-short/None intents already
      live on total_return_from_equity oracle pins; cashflow variants retire
      with the machinery.
    - test_e1_sharpe_twr_parity.py: delete class TestLegacyParityBaseline
      (it imports the deleted symbols) and remove the legacy imports; update
      the census test's pinned inventory to the post-delete expectation
      (deletion-target tokens now allowed ONLY in services/equity_reconstruction.py
      and tests/test_equity_curve_builder.py — the Phase-115 METHOD exemption);
      add the M-0698 caplog warning re-pin here (permanent class).

    Run the FULL suite plus the exact CI coverage command — deleting covered
    lines shifts the denominator, and the 80% gate must stay green.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest -q -p no:cacheprovider && .venv/bin/python -m pytest --cov=services --cov=routers --cov=main_worker --cov-report=term-missing --cov-fail-under=80 -q</automated>
  </verify>
  <acceptance_criteria>
    - Both symbols deleted; full suite green; CI coverage command exits 0 at >=80%
    - Status-code tests live on against sharpe_vol_status_from_backbone with identical REACHABLE status expectations PLUS the newly-added nan_vol case; no nan_mean/nan_sharpe assertions (dead branches)
    - test_nav_twr.py contains the bare compute_twr token in ZERO places (grep-proven: imports, call sites, function names, docstrings, comments) before the task closes
    - nav_twr agreement test green against the hand-derived closed-form (no deleted-symbol reference anywhere in the file, docstrings included)
    - M-0698 begin-value-0 warning intent re-pinned via caplog on total_return_from_equity
    - Census test updated and green on the post-delete tree
  </acceptance_criteria>
  <done>The duplicate Sharpe/TWR stack is gone; every surviving behavior intent has a named new home; the cashflow-TWR dead generality is retired on record, not silently.</done>
</task>

<task type="auto">
  <name>Task 2: Permanent delete-gate + KEEP-path import/function test</name>
  <files>analytics-service/tests/test_e1_delete_gate.py</files>
  <read_first>
    - analytics-service/tests/test_e1_sharpe_twr_parity.py (census walk pattern from 114-01 — reuse its walk discipline)
    - analytics-service/services/portfolio_metrics.py (post-delete state — the KEEP surface)
  </read_first>
  <action>
    Create tests/test_e1_delete_gate.py — the permanent Python mirror of the
    JS whole-repo grep-gate pattern (v1.10 CONSTIT-04 lesson), with 111-04
    self-invalidation-proofing. Three parts:

    Part A — live-symbol gate (comment/string-proof, the primary gate):
    import services.portfolio_metrics and routers.portfolio as modules; assert
    not hasattr for compute_twr and _compute_sharpe_and_vol on
    services.portfolio_metrics, and not hasattr for _compute_sharpe_and_vol
    and compute_twr on routers.portfolio (the latter also proves the L32
    import stays trimmed). Build the attribute-name strings by concatenation
    so this file never trips its own Part B.

    Part B — whole-tree token walk: pathlib walk of every .py under
    analytics-service (Path(__file__).parents[1]), skipping .venv,
    __pycache__, and this gate file plus the 114-01 parity file by name
    (both legitimately carry concatenated tokens). Rules:
      - the sharpe token (built by concatenation) must appear in ZERO scanned
        files;
      - the twr token may appear ONLY in the exemption set
        {services/equity_reconstruction.py, tests/test_equity_curve_builder.py}
        — the same-named EquityCurveBuilder METHOD, pinned until Phase 115 /
        STITCH-02 retires it (exemption is allowed-but-not-required, so the
        Phase-115 deletion will NOT break this gate);
      - additionally, no scanned line may contain BOTH "portfolio_metrics" and
        the twr token (blocks a re-import/attribute-access re-entry even
        inside the exempted files).
    Neuter-guards (self-invalidation-proofing per 111-04): assert >=100 files
    scanned; assert the walk actually visited services/portfolio_metrics.py
    and routers/portfolio.py (anti-over-narrowing); never use a bare ==0 count
    on unfiltered text (the token-concatenation + explicit skip-list IS the
    filter, per the grep-gate hygiene rule).

    Part C — KEEP-path proof (BACKBONE-01 clause 3, the post-delete import
    test): from services.portfolio_metrics import compute_mwr,
    compute_modified_dietz, compute_period_returns — then functional smoke,
    not import-only: compute_modified_dietz(100.0, 110.0, [], 30) approx 0.10;
    compute_period_returns on a 3-day DatetimeIndex series returns the
    return_24h/return_mtd/return_ytd keys with finite values; compute_mwr on a
    simple two-flow case (one -1000 investment a year before end_date, final
    value 1100) returns a rate approx 0.10 at rel 1e-2. Also import
    routers.process_key (its L1018 lazy import of compute_period_returns must
    survive — module import suffices as the wiring check).

    Prove the gate can fail (test-the-wiring memory): temporarily inject a
    live twr token into a scratch .py under analytics-service/services/, run
    the gate, confirm RED, revert — record the RED/GREEN pair in the SUMMARY
    (do NOT commit the injection).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_e1_delete_gate.py tests/test_e1_sharpe_twr_parity.py -q && .venv/bin/python -m pytest -q -p no:cacheprovider</automated>
  </verify>
  <acceptance_criteria>
    - Gate green on the clean tree; proven RED under a live-token injection (recorded, reverted)
    - hasattr assertions cover both modules and both symbols
    - equity_reconstruction METHOD exemption documented in-file with a Phase-115/STITCH-02 pointer, allowed-but-not-required
    - KEEP-path functional smoke green (Dietz approx 0.10, MWR approx 0.10, period-returns keys) + routers.process_key imports
    - >=100 files scanned assertion + must-visit assertions present
  </acceptance_criteria>
  <done>BACKBONE-01 fully closed: re-entry of either deleted symbol fails CI permanently via live-symbol AND tree-token gates; the kept cashflow/IRR surface is proven working, not just importable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI gate -> future PRs | the delete-gate is the trust anchor preventing dark-path re-entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-05 | Tampering | re-entry of deleted duplicate stack (zombie code) | mitigate | permanent hasattr + tree-walk gate, injection-proven RED, neuter-guarded |
| T-114-06 | Elevation/Repudiation | silent loss of test intent during migration | mitigate | every deleted test's intent gets a named successor or an on-record retirement rationale in the commit |
| T-114-07 | DoS | coverage gate regression from deleting covered lines | mitigate | exact CI coverage command run in-plan (--cov-fail-under=80) |
| T-114-SC | Tampering | supply chain | accept | zero new packages installed |
</threat_model>

<verification>
- Full pytest + CI coverage command green from analytics-service
- Gate RED/GREEN injection proof recorded in SUMMARY
- git diff touches ONLY the eight files in files_modified
</verification>

<success_criteria>
- Both symbols deleted and gate-locked; KEEP path proven; all migrated tests green
- ROADMAP Phase-114 success criteria 1-3 all TRUE
</success_criteria>

<output>
Create `.planning/phases/114-e1-backbone-absorption-sharpe-twr-deletion/114-03-SUMMARY.md` when done — MUST include the injection RED/GREEN proof and the retired-intent register (which legacy tests died, where each intent now lives).
</output>
