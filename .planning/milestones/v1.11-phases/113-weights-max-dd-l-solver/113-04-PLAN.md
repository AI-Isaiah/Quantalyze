---
phase: 113-weights-max-dd-l-solver
plan: 04
type: execute
wave: 4
depends_on: [113-03]
files_modified: []
autonomous: true
requirements: [WEIGHTS-03, WEIGHTS-04]
must_haves:
  truths:
    - "src/lib/scenario.ts is BYTE-FROZEN vs origin/main AND across every Phase-113 commit (SC-3) — a dirty diff is phase failure"
    - "The full phase gate battery is green: backbone gates, engine pins, leverage pins, allocations sweep, tsc, lint, blocking coverage ratchet"
    - "Negative constraints hold: no SCENARIO_SCHEMA_VERSION bump, no zod .min/.max refine on target/leverage, no duplicated engine math in the solver, production diff confined to the declared Phase-113 files"
  artifacts: []
  key_links: []
---

<objective>
Phase-close gate battery (verification-only, no production code) — mirrors the 112-03 pattern. Prove SC-3, run every suite the phase touches plus the blocking coverage ratchet, and enforce the phase's negative constraints before the post-execute chain (code review → verifier → red team).

Purpose: the quality-gate's final SC-3 freeze gate plan; catches drift the per-plan verifies can miss (cross-commit engine edits, coverage regression from the new module, constraint leaks).
Output: gate evidence recorded in the SUMMARY; zero code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-weights-max-dd-l-solver/113-VALIDATION.md (Phase gate + 113-GATE row)
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-VERIFICATION.md (the 8-gate battery precedent + negative-constraint list)
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-3 freeze + backbone gates + negative constraints</name>
  <files>(none — verification only)</files>
  <read_first>.planning/phases/113-weights-max-dd-l-solver/113-VALIDATION.md (113-GATE row), git log of the phase's commits on the current branch (identify the first Phase-113 commit hash for the cross-commit freeze check)</read_first>
  <action>
    Run and record (fail the plan on any non-zero):
    1. `git diff --exit-code origin/main -- src/lib/scenario.ts` AND `git diff --exit-code <first-113-commit>^..HEAD -- src/lib/scenario.ts` — byte-frozen both against main and across every phase commit (the 112-03 double check).
    2. `npx vitest run src/lib/scenario-backbone-gates.test.ts src/lib/scenario.test.ts src/lib/leverage.test.ts --no-file-parallelism` — backbone gates + 50 engine behavior pins + leverage sanitize pins.
    3. Negative constraints on the PHASE-113 production diff (`git diff origin/main -- 'src/**' ':(exclude)**/*.test.*'`): (i) `SCENARIO_SCHEMA_VERSION` constant value unchanged (grep the diff for the constant — any hit must be test-fixture-only); (ii) no new zod `.min(`/`.max(` refine on leverage or target (the 90.5/112 draft-deletion guard); (iii) production files touched are EXACTLY solve-leverage.ts + ScenarioComposer.tsx (list the diff's file names and compare); (iv) no engine-math duplication in solve-leverage.ts — its only drawdown source is `computeScenario` (assert `grep -c "computeScenario(" on the module ≥ 1` AND no local cumulative-product/peak-trough loop: grep the module for `peak` — any hit must be a comment; record the evidence).
    Record each command + exit code in the SUMMARY (the verifier re-runs them).
  </action>
  <verify>
    <automated>All commands in the action exit 0 / match; evidence table in SUMMARY</automated>
  </verify>
  <acceptance_criteria>
    - Both freeze diffs exit 0 (vs origin/main AND `<first-113-commit>^..HEAD`), commands + hashes recorded.
    - The gate vitest run is fully green (backbone gates + engine pins + leverage pins), counts recorded.
    - All four negative constraints hold with pasted evidence: (i) schema version unchanged, (ii) zero new zod refines, (iii) production diff file list === {solve-leverage.ts, ScenarioComposer.tsx}, (iv) solver's only drawdown source is computeScenario (grep evidence, `peak` hits are comments only).
  </acceptance_criteria>
  <done>SC-3 proven twice; gates green; all four negative constraints hold with recorded evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite sweep + blocking coverage ratchet + manual-QA handoff</name>
  <files>(none — verification only)</files>
  <read_first>.planning/phases/113-weights-max-dd-l-solver/113-VALIDATION.md (Manual-Only Verifications), CLAUDE.md Test Coverage section (ratchet thresholds lines 82 / stmts 80 / funcs 74 / branches 72)</read_first>
  <action>
    1. `npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism` — full allocations sweep.
    2. `npx tsc --noEmit` and `npm run lint` — 0 errors (the lone pre-existing react-hooks warning in EquityChart.tsx is out of scope; anything NEW fails).
    3. `npm run test:coverage` — the authoritative full-suite run must exit 0 against the blocking ratchet; record the four coverage numbers and compare flat-to-up vs the 112-03 close (86.89 / 84.76 / 81.96 / 78.33) — the new solver module ships with its own unit file so coverage must not regress.
    4. Hand the manual-only check to `/qa` explicitly in the SUMMARY (do NOT mark it done): on a dev server, flip a row to Target max-DD, set a sleeve target, confirm the derived L renders read-only, the resulting portfolio max-DD note reads as computed (not solved), and an unreachable target shows the honest em-dash state per DESIGN.md — the 113-VALIDATION Manual-Only row.
    Nothing to commit (verification-only; .planning is gitignored).
  </action>
  <verify>
    <automated>Sweep + tsc + lint + coverage all exit 0; coverage numbers recorded and ≥ ratchet</automated>
  </verify>
  <acceptance_criteria>
    - Allocations sweep, `npx tsc --noEmit`, and `npm run lint` all exit 0 with zero NEW warnings/errors.
    - `npm run test:coverage` exits 0; the four numbers are recorded, each ≥ its ratchet (82/80/74/72) and compared against the 112-03 baseline (86.89/84.76/81.96/78.33) with no regression attributable to Phase 113.
    - The SUMMARY contains an explicit `/qa` handoff paragraph for the DESIGN.md copy check (derived-L read-only, portfolio note "computed", em-dash on unreachable) — marked handed-off, NOT done.
  </acceptance_criteria>
  <done>Full battery green; coverage ratchet holds; manual /qa handoff documented; phase ready for /gsd:verify-work + post-execute review chain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | verification-only plan; enforces the phase's existing mitigations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-08 | Tampering | frozen engine | mitigate | double freeze-diff (vs main + cross-commit) — this plan IS the mitigation gate |
</threat_model>

<verification>
Every gate command exit 0; evidence recorded; no production diff produced by this plan.
</verification>

<success_criteria>
- SC-3 byte-freeze proven vs origin/main and across all phase commits.
- Backbone/engine/leverage pins + allocations sweep + tsc + lint + blocking coverage all green.
- Four negative constraints evidenced; manual /qa item handed off, not skipped.
</success_criteria>

<output>
Create `.planning/phases/113-weights-max-dd-l-solver/113-04-SUMMARY.md` when done.
</output>
