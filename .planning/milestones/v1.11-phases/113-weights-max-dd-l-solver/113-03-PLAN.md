---
phase: 113-weights-max-dd-l-solver
plan: 03
type: execute
wave: 3
depends_on: [113-00, 113-02]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx
autonomous: true
requirements: [WEIGHTS-03, WEIGHTS-04]
must_haves:
  truths:
    - "Every constituent row (per-key AND added) carries a mode toggle Leverage | Target max-DD DEFAULTING to Leverage; Target mode is opt-in per row"
    - "Committing a reachable sleeve target writes the solved L through the EXISTING handleLeverageChange → leverageByRef path (derived L visible read-only in Target mode); mode + target are TRANSIENT — only the solved L persists, no SCENARIO_SCHEMA_VERSION bump"
    - "The resulting PORTFOLIO-level max-DD for the solved L is displayed as a computed read-only value (full-book computeScenario output), labelled computed — never solved"
    - "Infeasible/degenerate solves render honest copy + em-dash and write NOTHING to leverageByRef — never a fabricated L"
    - "A solve writes LEVERAGE only: weights, mixed-book renorm, sole-unit refuse, and diffCount are byte-untouched (Phase-112 landmines not reintroduced)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "handleTargetCommit + transient targetModeByRef/solveResultByRef state in the composer; toggle + target input + readOnly derived-L + portfolio note + honest-state line in CompositionList rows"
      contains: "solveLeverageForMaxDD"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/app/(dashboard)/allocations/lib/solve-leverage.ts"
      via: "handleTargetCommit calls solveLeverageForMaxDD with the memoized engineSet/dateMapCache/blendBasis"
      pattern: "solveLeverageForMaxDD\\("
    - from: "handleTargetCommit"
      to: "leverageByRef"
      via: "success path routes through handleLeverageChange (same clamp, same message channel)"
      pattern: "handleLeverageChange\\("
    - from: "CompositionList portfolio note"
      to: "scenarioMetrics.max_drawdown"
      via: "portfolioMaxDrawdown prop threaded from the existing full-book computeScenario memo"
      pattern: "portfolioMaxDrawdown"
---

<objective>
Wire the solver into the Scenario Composer: per-row mode toggle (default Leverage), target input with solve-on-commit, derived L visible read-only via the existing leverage input, resulting portfolio-level max-DD readout, honest infeasible states per DESIGN.md Numbers Contract. Flips the Wave-0 component tests (h)-(l) and the save-survival test green.

Purpose: WEIGHTS-03 UI + the founder's one-shot-calculator + portfolio-readout locks; WEIGHTS-04's honest-state rendering.
Output: ScenarioComposer.tsx extended (parent wiring + CompositionList rows); all Phase-113 component/save tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-weights-max-dd-l-solver/113-CONTEXT.md (⭐ FOUNDER DECISION LOCK — one-shot calculator, transient mode/target, portfolio readout, default Leverage)
@.planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md (§Existing infra → gap map, §Landmine check, Pitfall 5 no live re-solve)
@.planning/phases/112-weights-leverage-rows-per-constituent-weights-leverage/112-VERIFICATION.md (the leverage surface being extended + landmines)
@DESIGN.md (Numbers Contract: tail-risk 2dp, signed %, null → em-dash `—`; formatter-module rule)

<interfaces>
From src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (extracted 2026-07-17 — anchors, verify on read):
```typescript
// :885   const [leverageByRef, setLeverageByRef] = useState<Record<string, number>>({});
// :1172  function handleLeverageChange(scopeRef: string, leverage: number)
//          — Number.isFinite guard → setCommitError + keep prior; clamp [0, MAX_LEVERAGE]
//            with visible message; writes setLeverageByRef({...prev, [scopeRef]: clamped})
// :2359  projectionState memo (leverage merged per s.id at :2390)
// :2413  engineSet = { strategies: activeAdapterOutput.strategies, state: projectionState }
// :2460  dateMapCache = buildDateMapCache(engineSet.strategies)   // built ONCE — reuse, never rebuild per trial
// :2678  engineState = coverageWindow ? { ...engineSet.state, window: coverageWindow } : engineSet.state
// :2703  blendBasis = blendPeriodsPerYear(selected engineSet.strategies)
// :2712  scenarioMetrics = computeScenario(engineSet.strategies, engineState, dateMapCache, blendBasis)
// :4619  <CompositionList ... onSetWeight={handleWeightChange} onSetLeverage={handleLeverageChange} ... />
// :5022  function CompositionList({ ... }) — props include leverageByRef, onSetLeverage, blendShareByRef,
//          totalBookEquity, mixedPerKeyBook; per-key rows map at :5090 (testid scenario-constituent-perkey,
//          weight-${k.id} / leverage-${k.id} inputs at :5157/:5175, notional span :5190);
//          added rows at :5240 (scenario-constituent-added, leverage-${a.id} :5291, notional :5306)
```
From src/app/(dashboard)/allocations/lib/solve-leverage.ts (Plans 01/02 — final):
```typescript
export type SolveLeverageResult =
  | { ok: true; leverage: number; sleeveMaxDD: number }
  | { ok: false; reason: "unreachable" | "no-drawdown" | "insufficient-history" | "degenerate"; ceiling?: number };
export function solveLeverageForMaxDD(args: {
  strategies: StrategyForBuilder[]; engineState: ScenarioState;
  dateMapCache: Map<string, Map<string, number>>; periodsPerYear: number;
  ref: string; targetMaxDD: number; maxLeverage?: number;
}): SolveLeverageResult;
```
Pinned testids from Wave 0 (implement EXACTLY these): `scenario-leverage-mode-toggle` (+`data-mode`), `target-dd-${ref}` input, `scenario-target-dd-state`, `scenario-target-dd-portfolio-note`; `leverage-${ref}` gains readOnly in Target mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Composer parent wiring — transient mode/solve state + handleTargetCommit</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx lines 880-900 (leverage state), 1138-1200 (handleLeverageChange — the exact fail-loud pattern to mirror), 2350-2420 + 2660-2720 (projectionState/engineSet/dateMapCache/engineState/blendBasis/scenarioMetrics memos), 4600-4640 (CompositionList call site); src/app/(dashboard)/allocations/lib/solve-leverage.ts (final contract + TSDoc reason copy)</read_first>
  <action>
    In the ScenarioComposer parent (per D-locks in 113-CONTEXT):
    1. Add transient UI state (NEVER persisted, NEVER in the draft codec): `targetModeByRef: Record<string, boolean>` (true = Target mode; absent/false = Leverage — the default) and `solveResultByRef: Record<string, SolveLeverageResult>` (last solve outcome per row, cleared when the row's mode flips back to Leverage or the row is excluded). Both plain `useState`, reset alongside the existing leverageByRef session-reset on open (mirror the :1566/:1617 session-bleed resets — grep `leverageByRef was never reset` for the two sites).
    2. Add `handleTargetCommit(scopeRef: string, targetPct: number)`: mirror handleLeverageChange's fail-loud shape — `!Number.isFinite(targetPct)` or `targetPct <= 0` or `targetPct >= 100` → `setCommitError` with honest copy naming the valid range, keep prior state, return (never clamp a target — a clamped target would solve for a value the allocator didn't ask). Otherwise call `solveLeverageForMaxDD({ strategies: engineSet.strategies, engineState, dateMapCache, periodsPerYear: blendBasis, ref: scopeRef, targetMaxDD: targetPct / 100 })` — the MEMOIZED inputs, rebuild nothing (research: dateMapCache reuse is the perf win). On `ok`: round `leverage` to 3 decimals and route it through `handleLeverageChange(scopeRef, rounded)` (same clamp, same message channel — a solved L IS a leverage; the 3dp rounding shifts dd by ≪ DD_TOL) and store the result in solveResultByRef. On `!ok`: store the result, write NOTHING to leverageByRef (Pitfall 3). One-shot calculator (founder lock): no effect/subscription re-solves on other edits — the solve fires ONLY inside this commit handler (Pitfall 5).
    3. Thread new props to CompositionList: `targetModeByRef`, `onSetTargetMode(ref, on)` (flipping OFF clears that row's solveResultByRef entry), `onCommitTarget: handleTargetCommit`, `solveResultByRef`, and `portfolioMaxDrawdown: scenarioMetrics.max_drawdown` (the existing full-book memo already reflects a committed solved L — the founder's portfolio readout costs ZERO extra computeScenario calls).
    Do not touch handleWeightChange, the save fold, pruneLeverageToDraftRefs, or the draft codec — a solved L persists through the existing path untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism 2>&1 | tail -5 — pre-existing 112 tests stay green (the new (h)-(l) stay RED until Task 2 renders the rows)</automated>
  </verify>
  <acceptance_criteria>
    - `handleTargetCommit` exists and calls `solveLeverageForMaxDD` with EXACTLY the memoized `engineSet.strategies`, `engineState`, `dateMapCache`, `periodsPerYear: blendBasis` — zero rebuilt caches, zero new computeScenario call sites in the parent.
    - Success path: `handleLeverageChange(scopeRef, <3dp-rounded L>)` is the ONLY leverage writer; failure path writes nothing to `leverageByRef`; invalid targets (non-finite/≤0/≥100) → `setCommitError` with range-naming copy, no clamp, no solve.
    - `targetModeByRef` + `solveResultByRef` are plain `useState`, reset at both leverageByRef session-reset sites, absent from the draft codec and Save payload.
    - The solver is invoked ONLY inside handleTargetCommit (grep: exactly one `solveLeverageForMaxDD(` call site — no useEffect/useMemo re-solve).
    - `npx tsc --noEmit` exit 0; all pre-existing Phase-112 composer tests green.
  </acceptance_criteria>
  <done>Parent state + handleTargetCommit + prop threading compile; solver called with memoized inputs only; success routes through handleLeverageChange; failure writes nothing; zero 112 regressions.</done>
</task>

<task type="auto">
  <name>Task 2: CompositionList row UI — toggle, target input, readOnly derived L, portfolio note, honest states</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx lines 5022-5065 (CompositionList props), 5090-5200 (per-key row: switch, weight/leverage inputs, notional span — the cell block the toggle joins), 5240-5315 (added row equivalents); DESIGN.md (Numbers Contract + radius ladder + font-metric for numerals); the Wave-0 tests (h)-(l) in ScenarioComposer.test.tsx (the exact queries to satisfy)</read_first>
  <action>
    Extend CompositionList's props interface (per Task 1's threading) and BOTH row types (per-key `k.id` and added `a.id` — WEIGHTS-03 is per-row, and Phase 112 gave both a leverage input):
    1. Mode toggle: a compact segmented control before the leverage input — `data-testid="scenario-leverage-mode-toggle"`, `data-mode={target ? "target" : "leverage"}`, accessible name naming the row (mirror the row switch's aria-label discipline), disabled when the row is excluded (mirrors the existing inputs' disabled={!included}). Default Leverage (absent from targetModeByRef).
    2. Target mode rendering: reveal `<input id={'target-dd-' + ref} type="number" min 0 max 100 step 0.1>` (percent; sr-only label + title stating it targets THIS CONSTITUENT'S OWN standalone max drawdown — the sleeve, not the portfolio; the honest-semantics copy is load-bearing). Commit on blur AND Enter → `onCommitTarget(ref, Number(value))` — solve-on-commit, never per keystroke. Set the existing `leverage-${ref}` input `readOnly` (NOT disabled — the derived L stays visible in the a11y tree and keeps its value; founder: "derived leverage stays visible, read-only"). Flipping back to Leverage mode restores editability — the solved L is then a normal Phase-112 leverage the allocator hand-tweaks.
    3. Solve-state line under the row controls, driven by `solveResultByRef[ref]`: on `ok` render `data-testid="scenario-target-dd-portfolio-note"` — "Portfolio max-DD at {L}×: {portfolioMaxDrawdown formatted}" with the value formatted as a signed 2dp percentage (tail-risk rule) in `font-metric`, em-dash `—` when `portfolioMaxDrawdown` is null; copy must read as COMPUTED for the whole book (the founder's honest "what does 4× on this sleeve do to my whole book" readout — computed, never "solved"). On `!ok` render `data-testid="scenario-target-dd-state"` with the reason's TSDoc copy (unreachable includes the ceiling: "Unreachable at {ceiling}×") plus an em-dash where a derived value would sit; no semantic color on the dash (DESIGN.md color gate).
    4. Formatting: follow the surface's existing pattern for the notional cell (`notionalText`) — if the allocations family already owns a formatter module use it; otherwise format beside notionalText's helper with a comment citing the Numbers Contract (never a bare inline toFixed scattered per cell — one helper, both row types).
    Keep the row's weight input, notional span, provenance badge, and coverage chip byte-untouched. No new store, no draft-codec field, no schema bump.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism — Wave-0 (h) default-Leverage, (i) target-mode readOnly, (j) solve commit + portfolio note, (k) infeasible honesty, (l) landmine non-regression ALL GREEN; full file green</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 tests (h)-(l) ALL GREEN unmodified (the implementation satisfies the pinned queries — any test edit requires a documented Rule-1 deviation).
    - BOTH row types (`scenario-constituent-perkey` and `scenario-constituent-added`) render the toggle (default `data-mode="leverage"`, disabled when excluded), the Target-mode `target-dd-<ref>` input (min 0 / max 100, blur+Enter commit), and the readOnly (never disabled) `leverage-<ref>` in Target mode.
    - Honest rendering: `scenario-target-dd-state` shows the exact TSDoc reason copy with ceiling interpolation and an em-dash `—` (no semantic color); `scenario-target-dd-portfolio-note` shows a signed 2dp percentage in `font-metric` via ONE shared helper (no per-cell inline toFixed) and reads "computed", em-dash when null.
    - The row's weight input, notional span, provenance badge, and coverage chip are byte-unchanged in the diff; no draft-codec/schema change.
  </acceptance_criteria>
  <done>Both row types carry the full Target-mode surface with the pinned testids; honest states render em-dash + reason copy; portfolio note reads the live full-book value; (h)-(l) green.</done>
</task>

<task type="auto">
  <name>Task 3: Save-survival green + landmine sweep + wave-merge regression battery</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx (the Wave-0 solved-L persistence test + the 112 prune tests), .planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md §Landmine check (solved L is OUT of diffCount like a typed leverage — the locked decision)</read_first>
  <action>
    1. Run the Wave-0 save test — it should flip green from Tasks 1-2 with ZERO production edits (a solved L persists through the untouched 112 fold). If it is RED, the fix belongs in Tasks 1-2's wiring (e.g. the solve wrote outside leverageByRef) — never in the save path or the test.
    2. Landmine confirmation sweep (planning-context mandate): assert in the existing test file if not already covered by (l) — a solve does not change `draft.weightOverrides` (weight inputs byte-stable), does not fire the sole-unit refuse message, and does not alter diffCount semantics (leverage stays out of diffCount exactly like a typed L — if an easy diffCount probe exists in the 112 tests, extend it one assertion; otherwise the weight-stability assertion plus the save payload's absence of weight changes suffices — document which).
    3. Wave-merge battery per 113-VALIDATION Sampling Rate: run the allocations sweep + engine/leverage pins and confirm zero collateral.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" --no-file-parallelism && npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism && npx vitest run src/lib/scenario.test.ts src/lib/leverage.test.ts src/lib/scenario-backbone-gates.test.ts --no-file-parallelism && git diff --exit-code origin/main -- src/lib/scenario.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The Wave-0 save test flips GREEN with zero edits to the save fold, pruneLeverageToDraftRefs, or the test itself (the payload carries `draft.leverageOverrides[<ref>]` = solved L, current SCENARIO_SCHEMA_VERSION, and no "targetMaxDD"/"rowMode" keys).
    - Landmine evidence recorded in the SUMMARY: weightOverrides untouched by a solve, no sole-unit refuse fired, solved L out of diffCount like a typed L (whichever probe was used, documented).
    - Full battery green: save file + allocations sweep + scenario/leverage/backbone pins; `git diff --exit-code origin/main -- src/lib/scenario.ts` exit 0; `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Save-survival green; landmines confirmed untouched with test evidence; full allocations sweep + engine pins green; SC-3 clean; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allocator keyboard → target input | NaN/paste garbage/out-of-range percentages cross here |
| persisted draft → rehydrated leverage | a tampered stored solved L crosses on load |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Tampering | target-dd input | mitigate | handleTargetCommit rejects non-finite/≤0/≥100 with visible copy, never clamps, never solves a value the allocator didn't type (mirrors handleLeverageChange) |
| T-113-06 | Tampering | rehydrated leverageOverrides | transfer | inherited: `sanitizeLeverageMap` on read coerces out-of-range with a Sentry signal (leverage.ts:79-118) — solved L is indistinguishable from typed L |
| T-113-07 | Information Disclosure | portfolio note | accept | renders the allocator's OWN already-authorized book metric; no new data crosses any boundary |
</threat_model>

<verification>
All Phase-113 component + save tests green; full allocations sweep green; engine/backbone/leverage pins green; SC-3 freeze clean; tsc clean.
</verification>

<success_criteria>
- Mode toggle default-Leverage on every constituent row; Target mode = input + readOnly derived L + honest states + portfolio readout, all per pinned testids.
- Solved L flows through handleLeverageChange → leverageByRef → engine → notional → Save with zero new stores/schema changes; mode+target transient.
- Landmines untouched with test evidence; no fabricated value on any path.
</success_criteria>

<output>
Create `.planning/phases/113-weights-max-dd-l-solver/113-03-SUMMARY.md` when done.
</output>
