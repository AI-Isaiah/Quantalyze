---
phase: 113-weights-max-dd-l-solver
plan: 01
type: execute
wave: 1
depends_on: [113-00]
files_modified:
  - src/app/(dashboard)/allocations/lib/solve-leverage.ts
  - src/app/(dashboard)/allocations/lib/solve-leverage.test.ts
autonomous: true
requirements: [WEIGHTS-03]
must_haves:
  truths:
    - "A reachable sleeve max-DD target back-solves the UNIQUE implied leverage via a ruin-clamped MONOTONE bisect on the frozen engine's real r→L·r transform (founder lock: sleeve-level, exact-match, smallest-L)"
    - "The founder example holds: sleeve unlevered max-DD 5%, target 20% → L = 4.000 ± L_TOL"
    - "A target below the unlevered sleeve max-DD resolves by DELEVERAGE (L < 1) on the domain [0, min(MAX_LEVERAGE, L_ruin)]"
    - "The solver never samples inside the ruin/null region and never edits scenario.ts (SC-3)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/solve-leverage.ts"
      provides: "Real implementation: sleeve-state builder, monotone ruin-predicate bisect for L_ruin, reachability pre-check, monotone smallest-L bisect, per-solve memoization"
      min_lines: 80
      exports: ["solveLeverageForMaxDD", "SolveLeverageResult", "DD_TOL", "L_TOL"]
  key_links:
    - from: "src/app/(dashboard)/allocations/lib/solve-leverage.ts"
      to: "src/lib/scenario.ts"
      via: "computeScenario called per trial L (never duplicated)"
      pattern: "computeScenario\\("
    - from: "src/app/(dashboard)/allocations/lib/solve-leverage.ts"
      to: "src/lib/leverage.ts"
      via: "MAX_LEVERAGE default ceiling"
      pattern: "MAX_LEVERAGE"
---

<objective>
Implement the core solver: `solveLeverageForMaxDD` back-solves the leverage L whose SLEEVE standalone max-DD (that one constituent, weight 1, leverage L, through the frozen `computeScenario`) matches the target, over the ruin-clamped domain `[0, min(MAX_LEVERAGE, L_ruin)]`. Flips the Wave-0 core-solve REDs (a)-(d) green.

Purpose: WEIGHTS-03 — the numerical solve on the REAL transform (the ROADMAP's grid-scan-then-bisect degenerates to a monotone bisect under the founder's sleeve-level lock; the non-monotone PORTFOLIO max-DD from research does NOT affect this solve).
Output: working solver module, Wave-0 tests (a)-(d) green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-weights-max-dd-l-solver/113-CONTEXT.md (⭐ FOUNDER DECISION LOCK — sleeve-level target, monotone, smallest-L, domain)
@.planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md (§Solver architecture, §Ruin boundary IS monotone, §Don't Hand-Roll, Pitfalls 2/4)

<interfaces>
From src/lib/scenario.ts (BYTE-FROZEN — call, never edit, never copy its loops):
```typescript
export function computeScenario(
  strategies: StrategyForBuilder[], state: ScenarioState,
  dateMapCache: Map<string, Map<string, number>>, periodsPerYear = 252,
): ComputedMetrics;
// .max_drawdown: NEGATIVE fraction rounded toFixed(5); null on n<10 OR ruin (cumulative ≤ 0 / non-finite)
// Single weight-1 constituent ⇒ portDaily = L·r (renorm by UNlevered mass) ⇒ its max_drawdown IS the sleeve's
```
From src/lib/leverage.ts: `export const MAX_LEVERAGE = 10`.
From Wave 0 (113-00) — the contract this plan fills in:
```typescript
export type SolveLeverageResult =
  | { ok: true; leverage: number; sleeveMaxDD: number }
  | { ok: false; reason: "unreachable" | "no-drawdown" | "insufficient-history" | "degenerate" | "unimplemented"; ceiling?: number };
export function solveLeverageForMaxDD(args: {
  strategies: StrategyForBuilder[]; engineState: ScenarioState;
  dateMapCache: Map<string, Map<string, number>>; periodsPerYear: number;
  ref: string; targetMaxDD: number; maxLeverage?: number;
}): SolveLeverageResult;
export const DD_TOL = 1e-3; export const L_TOL = 1e-2;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sleeve-state builder + degenerate short-circuits + monotone smallest-L bisect</name>
  <files>src/app/(dashboard)/allocations/lib/solve-leverage.ts</files>
  <read_first>src/app/(dashboard)/allocations/lib/solve-leverage.ts (the Wave-0 skeleton + TSDoc contract), src/app/(dashboard)/allocations/lib/solve-leverage.test.ts (fixtures (a)-(d) and (f) define exact expected values), src/lib/scenario.ts lines 226-260 + 417-431 + 478-510 + 559-574 (reference ONLY — signature, single-unit reduction, ruin guard, maxDD loop), .planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md §Solver architecture</read_first>
  <action>
    Replace the `"unimplemented"` body of `solveLeverageForMaxDD` (keep the union variant itself — Plan 113-02 deletes it) with the real solve, structured as small pure internal functions in the same module:
    1. `sleeveStateAt(L)` (internal): builds `{ selected: { [ref]: true }, weights: { [ref]: 1 }, startDates: engineState.startDates, window: engineState.window, leverage: { [ref]: L } }` — restricting `selected` to the one ref makes the frozen engine reduce to `portDaily = L·r` (founder lock; do NOT spread `engineState.selected`). Carry `window` verbatim: a selected in-blend row spans it; a non-spanning row degrades honestly through the engine's own null path.
    2. `ddAt(L)` (internal): `computeScenario(strategies, sleeveStateAt(L), dateMapCache, periodsPerYear).max_drawdown`, memoized in a per-solve `Map` keyed by `L.toFixed(4)` (research: skip repeat evals; max_drawdown is annualization-invariant so periodsPerYear only rides along for signature fidelity).
    3. Validate `targetMaxDD`: `Number.isFinite && targetMaxDD > 0 && targetMaxDD < 1` — else return `{ ok: false, reason: "degenerate" }` (the UI validates before calling; this is the fail-loud backstop, never a clamp).
    4. Degenerate short-circuits in order: `ddAt(0)` null ⇒ n<10 ⇒ `{ ok: false, reason: "insufficient-history" }` (at L=0 all returns are 0 so null can ONLY mean the n<10 guard — the clean discriminator). Then `ddAt(1)` null (with ddAt(0) non-null) ⇒ ruin at ≤1× (catastrophic |r|≥1 day) ⇒ `{ ok: false, reason: "degenerate" }` (founder table: data-quality refusal, do NOT solve below the 1× ruin point).
    5. Monotone bisect for the smallest L with `Math.abs(ddAt(L)) ≥ targetMaxDD` on `[0, L_max]` (L_max from Task 2; until Task 2 lands use `maxLeverage ?? MAX_LEVERAGE` — Task 2 tightens it): maintain the invariant `lo` has |dd| < target, `hi` has |dd| ≥ target. Reachability pre-check: `Math.abs(ddAt(L_max)) < targetMaxDD` ⇒ if `ddAt(L_max) === 0` return `{ ok: false, reason: "no-drawdown" }` else `{ ok: false, reason: "unreachable", ceiling: L_max }`. Iterate to interval width ≤ 1e-4 (≤ ~17 evals over a width-10 domain); return `{ ok: true, leverage: hi, sleeveMaxDD: ddAt(hi) }`. Returning `hi` under that invariant IS the smallest-L root — on any flat interval (5dp engine rounding plateaus) it converges to the lower end, exactly the founder's smallest-L semantics. |dd| is monotone non-decreasing on the sleeve (single series, until ruin) so the root is unique — no grid pre-scan needed (the ROADMAP grid degenerates; note this in a comment referencing the founder lock).
    Never loop on `Math.abs(null)` — every `ddAt` in the bisect is inside the pre-checked non-null domain; add a defensive null check that returns `{ ok: false, reason: "degenerate" }` rather than NaN-propagating (fail loud, Pitfall 2).
    Budget sanity: total evals ≈ 2 short-circuits + 1 reachability + ~17 bisect + Task 2's ~12 ruin evals ≈ 30-35 `computeScenario` calls — inside the research's 50-70 envelope.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/solve-leverage.test.ts" --no-file-parallelism — Wave-0 tests (a) L=4.000, (b) L≈2.6015 non-closed-form, (c) deleverage L=0.500, (f) insufficient-history + degenerate now GREEN; (d)/(e)/(g) may stay RED until Task 2 / Plan 02</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 tests GREEN: (a) `leverage` within `L_TOL` of 4.000; (b) within `L_TOL` of 2.6015 AND more than `L_TOL` away from the retired closed-form 2.538; (c) within `L_TOL` of 0.500; (f) exact reasons `insufficient-history` and `degenerate` on the pinned fixtures.
    - The module's ONLY drawdown source is `computeScenario` (no local cumulative/peak loop — grep for `peak` in the module yields comments only); `ddAt` results are memoized per solve keyed by `L.toFixed(4)`.
    - `sleeveStateAt` sets `selected` to exactly `{ [ref]: true }` and `weights` to `{ [ref]: 1 }`, carrying `startDates` and `window` from `engineState`.
    - `git diff origin/main -- src/lib/scenario.ts` empty; `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>Founder fixture solves to 4.000 ± L_TOL; compounding fixture proves numerical-not-closed-form; deleverage works; both degenerate short-circuits return exact reasons; no scenario.ts diff.</done>
</task>

<task type="auto">
  <name>Task 2: Ruin-clamped domain — monotone L_ruin bisect + explicit maxLeverage ceiling</name>
  <files>src/app/(dashboard)/allocations/lib/solve-leverage.ts, src/app/(dashboard)/allocations/lib/solve-leverage.test.ts</files>
  <read_first>src/app/(dashboard)/allocations/lib/solve-leverage.ts (Task 1 state), .planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md (§Ruin boundary IS monotone — the up-set argument; Pitfall 2 scanning past the ruin ceiling)</read_first>
  <action>
    Add the ruin clamp: internal `ruinedAt(L)` predicate = `ddAt(L) === null` (valid ONLY after Task 1's short-circuits guaranteed n≥10 and non-ruin at 1×, so null ⇒ ruin). Compute `L_max`: let `ceil = maxLeverage ?? MAX_LEVERAGE`; if `!ruinedAt(ceil)` then `L_max = ceil`; else bisect the monotone up-set on `[1, ceil]` maintaining lo = non-ruined / hi = ruined to width ≤ 1e-2 (~10-12 evals) and set `L_max = lo` (the last PROVEN non-ruined trial — using lo as the ceiling is the ε-margin; never sample above it afterward). Thread `L_max` into Task 1's reachability pre-check and bisect domain `[0, L_max]`; when `unreachable`, report `ceiling: L_max` (so a ruin-clamped ceiling like 3.32 surfaces honestly instead of a fabricated MAX_LEVERAGE).
    If Wave-0 test (d) (ruin fixture, −0.30 day, target 0.50 → L ≈ 1.6667 with termination) or (e) (`maxLeverage: 2.5` → `{ reason: "unreachable", ceiling: 2.5 }`) needs a fixture-value correction discovered during implementation, fix the FIXTURE only with an in-test comment deriving the arithmetic — never loosen a tolerance to force green.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/solve-leverage.test.ts" --no-file-parallelism — (a)-(e) + (f) GREEN; only (g) round-trip and any Plan-02 honest-state refinements may remain RED. Then: git diff --exit-code origin/main -- src/lib/scenario.ts (clean) && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 test (d) GREEN: ruin fixture (−0.30 day, L_ruin ≈ 3.333) solves target 0.50 to within `L_TOL` of 1.6667 and TERMINATES (no null-region sampling — every bisect eval lies in `[0, L_max]` with `L_max = lo` of the ruin bisect).
    - Wave-0 test (e) GREEN: explicit `maxLeverage: 2.5` yields exactly `{ ok: false, reason: "unreachable", ceiling: 2.5 }` with no `leverage` property.
    - `L_max = min(maxLeverage ?? MAX_LEVERAGE, last-proven-non-ruined L)`; the unreachable branch reports the ACTUAL ceiling (ruin-clamped when ruin bites first).
    - `git diff --exit-code origin/main -- src/lib/scenario.ts` exit 0; `npx tsc --noEmit` exit 0; any fixture correction carries an in-test arithmetic derivation comment (tolerances untouched at DD_TOL=1e-3 / L_TOL=1e-2).
  </acceptance_criteria>
  <done>Ruin fixture solves without touching the null region; explicit maxLeverage ceiling honored and reported; SC-3 clean; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI target input → solver | solver re-validates targetMaxDD (finite, (0,1)) as a fail-loud backstop |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02 | Tampering | solveLeverageForMaxDD input | mitigate | Number.isFinite + (0,1) range gate returns honest degenerate, never clamps/NaN-propagates |
| T-113-03 | DoS | per-solve engine-eval loop | mitigate | hard domain bounds + fixed-iteration bisects (~30-35 evals, no unbounded loop); memoized ddAt |
</threat_model>

<verification>
Wave-0 solver tests (a)-(f) green; (g) allowed RED (Plan 02). `git diff --exit-code origin/main -- src/lib/scenario.ts` clean. `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- solveLeverageForMaxDD returns the unique smallest-L root on the ruin-clamped domain `[0, min(maxLeverage ?? MAX_LEVERAGE, L_ruin)]`.
- Founder example 5%→20%→4.000 green; deleverage green; ruin clamp + honest ceiling green; degenerate short-circuits green.
- Zero engine edits; zero duplicated engine math (only computeScenario calls).
</success_criteria>

<output>
Create `.planning/phases/113-weights-max-dd-l-solver/113-01-SUMMARY.md` when done.
</output>
