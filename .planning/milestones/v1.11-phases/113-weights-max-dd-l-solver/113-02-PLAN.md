---
phase: 113-weights-max-dd-l-solver
plan: 02
type: execute
wave: 2
depends_on: [113-01]
files_modified:
  - src/app/(dashboard)/allocations/lib/solve-leverage.ts
  - src/app/(dashboard)/allocations/lib/solve-leverage.test.ts
autonomous: true
requirements: [WEIGHTS-04]
must_haves:
  truths:
    - "The solved L, re-fed through the FROZEN engine (not the solver), reproduces the sleeve target max-DD within DD_TOL — and the check has teeth (a perturbed L fails it)"
    - "Every infeasible/degenerate input yields an honest discriminated reason with NO leverage value — never a fabricated L, DD, or silent clamp"
    - "The Wave-0-only 'unimplemented' scaffolding variant is deleted from the union — it cannot ship"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/solve-leverage.ts"
      provides: "Final solver: complete honest-state coverage, no unimplemented variant, TSDoc documenting each reason + the UI copy contract"
      exports: ["solveLeverageForMaxDD", "SolveLeverageResult", "DD_TOL", "L_TOL"]
  key_links:
    - from: "src/app/(dashboard)/allocations/lib/solve-leverage.test.ts"
      to: "src/lib/scenario.ts"
      via: "round-trip re-feed through computeScenario (non-tautological)"
      pattern: "computeScenario\\("
---

<objective>
Close WEIGHTS-04 at the solver layer: flip the Wave-0 round-trip test (g) green, harden/complete the honest failure states, and delete the Wave-0 `"unimplemented"` scaffolding variant. All solver unit tests green at plan close.

Purpose: WEIGHTS-04 — "a round-trip test confirms the solved L, re-applied, reproduces the target drawdown within tolerance" + "an infeasible target or degenerate series shows an honest state — never a fabricated leverage or drawdown value".
Output: final solver module, full solve-leverage.test.ts suite green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/113-weights-max-dd-l-solver/113-CONTEXT.md (WEIGHTS-04 honest states — locked)
@.planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md (§Honest failure states table, §Round-trip test design, Pitfall 3 clamp-and-lie)

<interfaces>
From Plan 113-01 — the module under refinement:
```typescript
export type SolveLeverageResult =
  | { ok: true; leverage: number; sleeveMaxDD: number }
  | { ok: false; reason: "unreachable" | "no-drawdown" | "insufficient-history" | "degenerate" | "unimplemented"; ceiling?: number };
// "unimplemented" is Wave-0 scaffolding — THIS plan deletes it.
export const DD_TOL = 1e-3; export const L_TOL = 1e-2;
```
Engine rounding fact: `max_drawdown` is `toFixed(5)` (scenario.ts:652) — DD_TOL=1e-3 sits safely above 1e-5 rounding granularity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Round-trip green + perturbation teeth + convergence tightening</name>
  <files>src/app/(dashboard)/allocations/lib/solve-leverage.ts, src/app/(dashboard)/allocations/lib/solve-leverage.test.ts</files>
  <read_first>src/app/(dashboard)/allocations/lib/solve-leverage.test.ts (Wave-0 test (g): forward-then-back design — target derived from computeScenario at L*=2.5, solve, re-feed the ENGINE at L_solved, plus the +0.15 perturbation assertion), src/app/(dashboard)/allocations/lib/solve-leverage.ts (Plan-01 bisect + memoization), .planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md §Round-Trip Test</read_first>
  <action>
    Run test (g). If RED, tighten the solver — never the test: the bisect must terminate with `Math.abs(Math.abs(ddAt(L_solved)) − targetMaxDD) ≤ DD_TOL` on reachable targets (interval width 1e-4 on a slope-O(0.06/L) curve yields dd error ≈ 6e-6 + 1e-5 rounding — comfortably inside 1e-3; if not, the bug is an off-by-one on the lo/hi invariant, not the tolerance). Confirm the perturbation assertion (L_solved + 0.15 breaks the DD_TOL match) passes — if it does NOT, the fixture's dd-slope is too shallow and the FIXTURE must steepen (in-test arithmetic comment), because a round-trip check that survives a perturbed L is tautological (Nyquist clause, planning-context lock).
    Add one additional round-trip case at a deleverage root (L* = 0.6 on the −0.10-day fixture) so the tolerance contract is proven on both sides of 1×.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/solve-leverage.test.ts" --no-file-parallelism 2>&1 | tail -3 — round-trip tests green including perturbation teeth</automated>
  </verify>
  <acceptance_criteria>
    - Wave-0 test (g) GREEN with BOTH assertions: `|L_solved − 2.5| ≤ L_TOL` and the ENGINE re-feed `|Math.abs(computeScenario(..., sleeveStateAt(L_solved), ...).max_drawdown) − target| ≤ DD_TOL`.
    - The perturbation assertion GREEN: at `L_solved + 0.15` the engine-re-fed dd differs from target by > DD_TOL (non-tautology proven; any fixture steepening documented with in-test arithmetic).
    - NEW deleverage round-trip GREEN at `L* = 0.6` on the −0.10-day fixture (same two-sided assertion pattern).
    - `DD_TOL` remains 1e-3 and `L_TOL` remains 1e-2 (no loosening — grep the module for the constants).
  </acceptance_criteria>
  <done>Both round-trips (lever-up + deleverage) green via ENGINE re-feed; perturbation proves non-tautology; no test tolerance was loosened.</done>
</task>

<task type="auto">
  <name>Task 2: Honest-state completion + delete the "unimplemented" scaffolding</name>
  <files>src/app/(dashboard)/allocations/lib/solve-leverage.ts, src/app/(dashboard)/allocations/lib/solve-leverage.test.ts</files>
  <read_first>src/app/(dashboard)/allocations/lib/solve-leverage.ts (full module), .planning/phases/113-weights-max-dd-l-solver/113-RESEARCH.md §Honest failure states table (the 7-row case matrix), DESIGN.md Numbers Contract (em-dash rule the TSDoc must reference for consumers)</read_first>
  <action>
    1. Sweep the research §Honest-failure table against the implementation and tests; add any missing case as a NEW test first (RED), then cover it: flat series with target 0 or negative → rejected by the input gate as `degenerate` (a zero target is not a solve); all-negative series solves normally (assert a finite ok result — honesty means a LOW L, not a refusal); non-finite target (NaN/Infinity — the STRIDE T-113-02 probe) → `degenerate`, no throw.
    2. Every `{ ok: false }` branch must carry NO `leverage`/`sleeveMaxDD` property (assert via `"leverage" in result === false` on at least one case) — the type-level guarantee the UI's em-dash rendering relies on (Pitfall 3: an infeasible target must never surface a number).
    3. Delete `"unimplemented"` from the `SolveLeverageResult` union and any dead branch referencing it. TSDoc each remaining reason with the honest UI copy contract Plan 113-03 renders: unreachable → "Unreachable at {ceiling}×"; no-drawdown → "No drawdown in this series"; insufficient-history → "Insufficient history to model drawdown"; degenerate → "Series can't be modeled (data quality)".
    4. Confirm eval budget stays bounded: add a lightweight test that counts computeScenario invocations for one full solve via a wrapping spy (vi.spyOn on the imported module namespace or dependency-injected evaluator if the module already takes one — prefer the least-invasive that works under the project's vitest setup) and asserts ≤ 70 calls (the research envelope; guards against an accidental unbounded loop regression).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/solve-leverage.test.ts" --no-file-parallelism (ALL green) && ! grep -q "unimplemented" "src/app/(dashboard)/allocations/lib/solve-leverage.ts" && npx tsc --noEmit && git diff --exit-code origin/main -- src/lib/scenario.ts</automated>
  </verify>
  <acceptance_criteria>
    - Every row of the research §Honest-failure table has a test asserting the EXACT reason string; the NaN/Infinity/≤0/≥1 target gate returns `degenerate` without throwing; the all-negative series returns a finite `{ ok: true }`.
    - At least one infeasible case asserts `"leverage" in result === false` (no value-shaped field on any failure branch).
    - `! grep -q "unimplemented"` on solve-leverage.ts passes (variant + dead branches deleted); each remaining reason carries the pinned TSDoc copy Plan 03 renders.
    - The eval-count test asserts ≤ 70 computeScenario calls per solve; full solve-leverage suite GREEN; `npx tsc --noEmit` exit 0; SC-3 diff clean.
  </acceptance_criteria>
  <done>Full solver suite green; unimplemented variant gone (grep-proven); every infeasible branch value-free; eval budget pinned ≤ 70; SC-3 + tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI target input → solver | NaN/Infinity/≤0/≥1 targets cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-02 | Tampering | targetMaxDD input | mitigate | explicit non-finite/out-of-range test → honest degenerate, no throw, no clamp (this plan adds the test) |
| T-113-04 | Repudiation | infeasible results | mitigate | discriminated union carries the machine-readable reason + ceiling — the UI can never render a value-shaped lie |
</threat_model>

<verification>
Full solve-leverage.test.ts green; `! grep -q "unimplemented"` on the module; SC-3 freeze clean; tsc clean.
</verification>

<success_criteria>
- Round-trip (WEIGHTS-04) green in both directions with perturbation-proven teeth.
- All 7 research honest-state rows covered by exact-reason tests; no fabricated value on any infeasible path.
- Wave-0 scaffolding variant deleted; module final for UI consumption.
</success_criteria>

<output>
Create `.planning/phases/113-weights-max-dd-l-solver/113-02-SUMMARY.md` when done.
</output>
