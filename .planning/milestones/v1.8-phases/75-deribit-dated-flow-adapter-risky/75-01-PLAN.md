---
phase: 75-deribit-dated-flow-adapter-risky
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/external_flows.py
  - analytics-service/tests/test_external_flows.py
  - analytics-service/tests/fixtures/deribit_flow_fixtures.py
  - analytics-service/tests/test_deribit_flow_fixtures.py
autonomous: true
requirements: [FLOW-01]
must_haves:
  truths:
    - "A venue-agnostic ExternalFlow(utc_day_iso, usd_signed) contract exists and unpacks positionally as (day, usd) so the core's `day_raw, usd_raw = flow` consumes it drop-in"
    - "Five LTP068-shaped synthetic Deribit txn-log fixture scenarios exist as importable builders, consumed by every downstream test wave"
  artifacts:
    - path: "analytics-service/services/external_flows.py"
      provides: "ExternalFlow NamedTuple contract (pure, no I/O) — Phase 76 ccxt adapters import verbatim"
      contains: "class ExternalFlow"
      min_lines: 12
    - path: "analytics-service/tests/fixtures/deribit_flow_fixtures.py"
      provides: "LTP068-shaped synthetic txn-log row builders for the 5 flow scenarios"
      exports: ["linear_flow_day_rows", "inverse_flow_day_with_index_rows", "inverse_flow_day_without_index_rows", "dominating_withdrawal_rows", "pure_flow_no_trade_rows"]
  key_links:
    - from: "analytics-service/services/nav_twr.py::_flows_to_daily_usd"
      to: "analytics-service/services/external_flows.py::ExternalFlow"
      via: "positional unpack `day_raw, usd_raw = flow`"
      pattern: "day_raw, usd_raw = flow"
---

<objective>
Establish the ONE venue-agnostic dated-flow contract `ExternalFlow = (utc_day_iso, usd_signed)`
in a new pure `services/external_flows.py`, and synthesize the shared LTP068-shaped Deribit
txn-log fixtures that every downstream Phase-75 test wave consumes. This is the Wave-0 foundation:
the contract the core already unpacks (Phase 73/74 threaded `external_flows`), and the fixture
scenarios that make the RISKY valuation waves mutation-honest.

Purpose: FLOW-01 — a single flow shape consumed by the core regardless of venue (Deribit here,
ccxt in Phase 76). Building it first is interface-first ordering: downstream tasks receive the
contract and fixtures rather than exploring the codebase for them.

Output: `services/external_flows.py` (contract), `tests/fixtures/deribit_flow_fixtures.py`
(5 scenario builders), and their self-tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-CONTEXT.md
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-RESEARCH.md
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-VALIDATION.md

<interfaces>
<!-- The core already consumes exactly (day, usd). Contract is drop-in. -->
From analytics-service/services/nav_twr.py (Phase 73, UNCHANGED this phase):
  # _flows_to_daily_usd (nav_twr.py:111-133) does:
  #   day_raw, usd_raw = flow          # positional unpack
  #   day = _row_utc_day(day_raw)      # re-dates through the shared helper
  #   usd = _coerce_float(usd_raw)     # fail-loud coerce
  # so ExternalFlow MUST unpack positionally as (utc_day_iso, usd_signed).

From analytics-service/services/deribit_txn.py (UNCHANGED this plan; fixtures mimic its row schema):
  # _EXTERNAL_FLOW_TYPES = {"transfer","deposit","withdrawal","usdc_reward"}
  # _INVERSE_CURRENCIES = {"BTC","ETH"}; _LINEAR_CURRENCIES include USDC/USDT/USD/EURR
  # A txn-log row is a Mapping with keys: type, currency, change, timestamp (epoch-ms),
  #   and optionally index_price / instrument_name. Deposit/withdrawal rows carry NO
  #   instrument_name and NO own index_price (structural — see 75-RESEARCH.md Q3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the venue-agnostic ExternalFlow contract</name>
  <files>analytics-service/services/external_flows.py, analytics-service/tests/test_external_flows.py</files>
  <read_first>
    - 75-RESEARCH.md Q5 (the locked NamedTuple shape) and "Don't Hand-Roll" (contract shape row)
    - analytics-service/services/nav_twr.py lines 111-133 (how the core unpacks a flow)
    - analytics-service/services/deribit_txn.py lines 1-46 (the pure-module import discipline to mirror)
  </read_first>
  <behavior>
    - Test: `ExternalFlow("2026-03-14", -42000.0)` unpacks positionally to `("2026-03-14", -42000.0)` (drop-in for `day_raw, usd_raw = flow`).
    - Test: field access `.utc_day_iso` / `.usd_signed` returns the two members.
    - Test (optional light shape validation, if a validator is added): a non-finite `usd_signed` (NaN/inf) and an empty `utc_day_iso` are rejected by the validator; a valid pair passes. Do NOT duplicate the core's business coercion — shape-only.
    - Test: the module imports NOTHING beyond stdlib/typing (source-scan: no `import ccxt`, no `import pandas`, no I/O) — mirrors deribit_txn.py discipline.
  </behavior>
  <action>
    Create `services/external_flows.py` defining `class ExternalFlow(NamedTuple)` with exactly two
    fields — `utc_day_iso: str` and `usd_signed: float` — per 75-RESEARCH.md Q5 (deposit +,
    withdrawal −, event-time USD). Keep it PURE and I/O-free: stdlib/typing imports ONLY, no ccxt,
    no pandas, no network, no file access. Docstring must state it is the SINGLE venue-agnostic
    contract that Phase 76 ccxt adapters import verbatim and that the core (`nav_twr._flows_to_daily_usd`)
    consumes it by positional unpack. An OPTIONAL lightweight shape validator (`utc_day_iso` non-empty,
    `usd_signed` finite) MAY live here per FLOW-01, but do NOT re-implement the core's fail-loud
    `_coerce_float` / `_row_utc_day` business logic — the core already owns valuation/dating; this
    module owns SHAPE only. Add a module-level source-scan-friendly import block (no forbidden imports).
  </action>
  <verify>
    <automated>PY312=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python; cd analytics-service && $PY312 -m pytest tests/test_external_flows.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `ExternalFlow` unpacks positionally as (day, usd); the drop-in test is GREEN.
    - Source-scan: `grep -nE '^(import|from) ' analytics-service/services/external_flows.py | grep -v '^#' | grep -Ev '(typing|__future__)'` returns NO ccxt/pandas/os/requests/httpx imports.
    - No I/O in the module (pure).
  </acceptance_criteria>
  <done>ExternalFlow contract exists, is pure, unpacks positionally, and its self-test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Synthesize the 5 LTP068-shaped flow-scenario fixtures</name>
  <files>analytics-service/tests/fixtures/deribit_flow_fixtures.py, analytics-service/tests/test_deribit_flow_fixtures.py</files>
  <read_first>
    - 75-VALIDATION.md "Wave 0 (pre-flight)" (the exact 5 scenarios required)
    - 75-RESEARCH.md Q6 (VCR-vs-stub: use in-process synthetic rows, NOT vcrpy; mirror the existing stub row schema) and the "Code Examples" row schema
    - analytics-service/tests/test_deribit_txn.py lines 50-205 (existing txn_change_to_usd row shapes: type/currency/change/timestamp/index_price)
    - analytics-service/tests/test_deribit_ingest.py around test_producer_values_quiet_inverse_day_via_settlement_index (the stub row pattern to mirror)
  </read_first>
  <action>
    Create `tests/fixtures/deribit_flow_fixtures.py` exporting FIVE builder functions returning
    lists of synthetic Deribit txn-log rows (Mappings with type/currency/change/timestamp[epoch-ms],
    plus index_price/instrument_name where a real row would carry them). Rows are LTP068-shaped
    (an inverse BTC subaccount with material withdrawals) but SYNTHETIC — no real LTP068 rows exist
    in-repo (75-RESEARCH.md A2/A3). The five scenarios (75-VALIDATION.md Wave 0):
      1. `linear_flow_day_rows()` — a USDC deposit (`type="deposit", currency="USDC", change=+50000`) on a
         trading day; passes through as USD (no index needed).
      2. `inverse_flow_day_with_index_rows()` — a BTC withdrawal (`type="withdrawal", currency="BTC",
         change=-0.5`) on a day that ALSO carries a same-day settlement/index-bearing row for BTC, so an
         own same-day index exists. Include the paired index source (an index-bearing settlement row OR a
         supplemental_index entry the test supplies).
      3. `inverse_flow_day_without_index_rows()` — a BTC withdrawal on a QUIET day (no trading, no
         index-bearing row) → the fail-loud / Finding-C1 scenario (no own same-day index).
      4. `dominating_withdrawal_rows()` — a withdrawal whose valued USD is >= prior-day NAV (the SC4
         `flow_dominated_guard` case; the acceptance wave asserts NaN + warning, NOT r_t==0).
      5. `pure_flow_no_trade_rows()` — a material-but-sub-NAV flow on a day with zero return-bearing
         rows (the SC4 `r_t==0` case).
    Provide each builder with parametrizable amounts/timestamps and a known same-day settlement index
    constant (e.g. `BTC_INDEX_2026_03_14 = 42000.0`) that the valuation waves assert against for
    event-time proof. Keep builders pure (no I/O); place under `tests/fixtures/` with an `__init__.py`
    if the package layout requires it. This is shared Wave-0 scaffold — Waves 2/3/4 import it.
  </action>
  <verify>
    <automated>PY312=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python; cd analytics-service && $PY312 -m pytest tests/test_deribit_flow_fixtures.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5 builders import and return non-empty row lists whose rows carry `type` in the expected class, `currency`, numeric `change`, and an epoch-ms `timestamp`.
    - The self-test asserts: scenario 1 currency is linear (USDC); scenarios 2/3/4/5 flow rows are inverse (BTC); scenario 3 carries NO own same-day index; the sign convention holds (deposit change>0, withdrawal change<0).
    - The known same-day index constant is exported for the valuation-wave event-time proof.
  </acceptance_criteria>
  <done>Five importable LTP068-shaped scenario builders exist with a GREEN shape self-test; downstream waves can import them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| txn-log rows → contract | Untrusted exchange rows are shaped into `ExternalFlow`; this plan only defines the shape + synthetic fixtures (no live rows). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-01 | Tampering | external_flows.py shape validation | mitigate | Optional validator rejects non-finite `usd_signed` / empty day; the core still fail-loud-coerces downstream (no silent zeroing). |
| T-75-01-P | Tampering | pure-module import discipline | mitigate | Source-scan forbids ccxt/pandas/I/O imports so the contract stays inert and reusable (Phase 76 imports it verbatim). |
</threat_model>

<verification>
- `pytest tests/test_external_flows.py tests/test_deribit_flow_fixtures.py -q` GREEN in the CI-3.12 venv.
- Source-scan confirms `external_flows.py` is pure (no ccxt/pandas/I/O).
- The core's positional-unpack contract (`day_raw, usd_raw = flow`) is satisfied by `ExternalFlow`.
</verification>

<success_criteria>
- FLOW-01 contract module exists, pure, positionally-unpackable, self-test GREEN.
- Five shared LTP068-shaped fixture scenarios exist and are importable by Waves 2/3/4.
</success_criteria>

<output>
Create `.planning/phases/75-deribit-dated-flow-adapter-risky/75-01-SUMMARY.md` when done.
</output>
