---
phase: 75-deribit-dated-flow-adapter-risky
plan: 04
type: tdd
wave: 4
depends_on: [75-03]
files_modified:
  - analytics-service/tests/test_derive_broker_dailies_dualmode.py
autonomous: true
requirements: [FLOW-02]
must_haves:
  truths:
    - "A real BTC withdrawal becomes a correctly-signed, event-time-valued F_t on its actual UTC day when run through the full deribit dated-flow path into combine_realized_and_funding -> reconstruct_nav_and_twr"
    - "A non-dominating pure-flow day (|F| < NAV_{t-1}, no trading) yields r_t == 0 (the flow-neutral TWR property)"
    - "A DOMINATING withdrawal (|F| >= NAV_{t-1}) trips flow_dominated_guard -> r_t NaN + complete_with_warnings, NOT a fabricated +/-100% day"
    - "Removing the dated flow from the F_t list changes the reconstructed NAV / cumulative return (the flow is load-bearing, not dropped)"
  artifacts:
    - path: "analytics-service/tests/test_derive_broker_dailies_dualmode.py"
      provides: "LTP068 acceptance: sub-NAV pure-flow -> r_t==0; dominating withdrawal -> flow_dominated_guard; both SC4 cases pinned"
      contains: "ltp068"
  key_links:
    - from: "dated ExternalFlow list"
      to: "analytics-service/services/nav_twr.py::reconstruct_nav_and_twr"
      via: "combine_realized_and_funding(external_flows=...) -> core F_t"
      pattern: "external_flows="
---

<objective>
Prove the whole RISKY path is honest end-to-end. Author the LTP068 acceptance (SC4, reconciled)
through the real integration seam `combine_realized_and_funding(external_flows=...) ->
reconstruct_nav_and_twr`: a real BTC withdrawal becomes a correctly-signed, event-time-valued `F_t`
on its actual day; a non-dominating pure-flow day yields `r_t == 0` (the flow-neutral property proven
in Phase 73); and a DOMINATING withdrawal (LTP068 withdrew ~$2.5M) correctly trips
`flow_dominated_guard` (NaN + `complete_with_warnings`) rather than fabricating a ±100% day. BOTH
outcomes are correct honest behavior and BOTH are pinned.

Purpose: FLOW-02 acceptance — this is the milestone's silent-corruption gate at the integration level.
The dominating-vs-sub-NAV distinction (75-CONTEXT.md SC4 nuance, 75-RESEARCH.md Pitfall 5 + Open Q2)
is the subtle correctness point: "pure-flow day → r_t==0" holds ONLY while `|F| < NAV_{t-1}`.

Output: the LTP068 dual-case acceptance in `test_derive_broker_dailies_dualmode.py`. Tests only —
no production code changes (the production path is complete after 75-03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-CONTEXT.md
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-RESEARCH.md
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-VALIDATION.md
@.planning/phases/75-03-SUMMARY.md

<interfaces>
From analytics-service/services/broker_dailies.py:
  # combine_realized_and_funding(realized, funding, *, account_balance, balance_error=False,
  #   external_flows=None, open_unrealized_usd=0.0) -> (returns: pd.Series, meta: dict)
  #   meta carries computation_status_hint + the NAV-guard keys (dust_nav_guard / negative_nav_guard /
  #   flow_dominated_guard) when they fired (74-02 NavTWRMeta contract).

From analytics-service/services/nav_twr.py (Phase 73, the guard semantics to assert):
  # FLOW_DOM_RATIO = 1.0  -> |F_t| >= NAV_{t-1} breaks the chain-link: r_t = NaN + flow_dominated_guard
  #   + complete_with_warnings (nav_twr.py:233-256). A sub-NAV pure flow -> r_t == 0 (flow cancels in
  #   the numerator: NAV_t - NAV_{t-1} == F_t by backward reconstruction, nav_twr.py:185-230).

From analytics-service/services/deribit_txn.py (75-02):
  # deribit_dated_external_flows_usd(rows, *, supplemental_index=None) -> list[ExternalFlow]

From analytics-service/tests/fixtures/deribit_flow_fixtures.py (75-01):
  # pure_flow_no_trade_rows (sub-NAV) ; dominating_withdrawal_rows ; inverse_flow_day_with_index_rows
  # + the known same-day BTC index constant for event-time assertions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LTP068 acceptance — sub-NAV pure-flow day yields r_t == 0</name>
  <files>analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>
    - 75-RESEARCH.md Q6 (LTP068 acceptance, the flow-neutral property, the sub-NAV caveat), "RISKY fail-loud + no-fabricated-day proofs" items 1-3
    - 75-CONTEXT.md decision 4 + "SC4 nuance (reconciled)"
    - analytics-service/services/nav_twr.py lines 185-256 (the flow-neutral algebra + guard threshold)
    - existing structure of analytics-service/tests/test_derive_broker_dailies_dualmode.py (mirror its combine_realized_and_funding call style)
  </read_first>
  <behavior>
    - Event-time + sign proof: build dated flows from `inverse_flow_day_with_index_rows()` via
      `deribit_dated_external_flows_usd(..., supplemental_index=<same-day BTC index>)`; the resulting
      `ExternalFlow` for the withdrawal day is negative and equals `change * BTC_INDEX` on the row's
      actual UTC day. Substituting a different-day index / 1.0 / current price → RED.
    - Flow-neutral proof: feed a realized daily_pnl series + a sub-NAV withdrawal dated flow into
      `combine_realized_and_funding(realized, [], account_balance=<anchor>, external_flows=<dated>)`;
      the withdrawal day's `r_t == 0` (approx, rtol tight) and `computation_status_hint == "complete"`
      (no guard fired). Anchor + flow sized so `|F| < NAV_{t-1}`.
    - Dropped-flow proof: removing the dated flow from the `external_flows` list changes the
      reconstructed NAV / cumulative return away from the flow-neutral result → RED (the flow is
      load-bearing; the flow-neutral property only holds when the flow is PRESENT).
  </behavior>
  <action>
    Add an `ltp068`-named acceptance test to `test_derive_broker_dailies_dualmode.py` for the
    NON-dominating case. Compose the real seam: fixture rows → `deribit_dated_external_flows_usd`
    (supplying the same-day BTC settlement index the fixture defines) → `combine_realized_and_funding`
    with `external_flows=` the dated list and a realistic `account_balance` anchor such that the
    withdrawal is material but STRICTLY under prior-day NAV. Assert (a) the withdrawal `ExternalFlow` is
    correctly signed and event-time valued, (b) the withdrawal day's `r_t == 0` (flow-neutral) with
    `complete` status, and (c) a mutation dropping the flow from `external_flows` changes the result
    (dropped-flow proof). Keep it mutation-honest — each assertion must go RED under wrong-time
    valuation / a dropped flow.
  </action>
  <verify>
    <automated>PY312=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python; cd analytics-service && $PY312 -m pytest tests/test_derive_broker_dailies_dualmode.py -k ltp068 -q</automated>
  </verify>
  <acceptance_criteria>
    - The sub-NAV withdrawal day yields `r_t == 0` with `complete` status; the flow is correctly signed and event-time valued.
    - Dropping the flow from `external_flows` changes the reconstructed result (dropped-flow proof RED without the flow).
  </acceptance_criteria>
  <done>The flow-neutral property is proven end-to-end for a real-shaped BTC withdrawal on a sub-NAV pure-flow day.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: LTP068 acceptance — dominating withdrawal trips flow_dominated_guard</name>
  <files>analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>
    - 75-RESEARCH.md Pitfall 5 ("Flow-dominated day != r_t==0") + Open Q2
    - 75-VALIDATION.md item 6 (TWR flow-neutrality, reconciled — BOTH pinned)
    - analytics-service/services/nav_twr.py lines 233-256 (flow_dominated_guard, FLOW_DOM_RATIO)
  </read_first>
  <behavior>
    - Dominating-guard proof: feed a dated withdrawal from `dominating_withdrawal_rows()` sized so its
      valued `|F| >= NAV_{t-1}` into `combine_realized_and_funding(...)`; the withdrawal day's `r_t` is
      NaN and `meta` carries `flow_dominated_guard` with `computation_status_hint ==
      "complete_with_warnings"` — NOT `r_t == 0` and NOT a fabricated ±100% day.
    - Honest-not-bug proof: assert this is a WARNING (complete_with_warnings), not an error/permanent
      fail — the day is surfaced honestly, not fabricated.
    - Boundary distinction: the SAME machinery yields `r_t==0` for the sub-NAV case (Task 1) and the
      guard for the dominating case (this task) — pin BOTH so a future change cannot collapse them.
  </behavior>
  <action>
    Add the DOMINATING-case `ltp068` acceptance test. Build the dated flow from
    `dominating_withdrawal_rows()` (valued via the fixture's same-day index) and choose an
    `account_balance` anchor so the reconstructed prior-day NAV is SMALLER than `|F|`, tripping
    `FLOW_DOM_RATIO`. Assert `r_t` is NaN for that day, `flow_dominated_guard` is set in `meta`, and the
    status hint is `complete_with_warnings`. Add a comment referencing 75-CONTEXT SC4-nuance: LTP068's
    real ~$2.5M withdrawal is the motivating case, and tripping the guard is the CORRECT honest behavior
    (never a fabricated ±100% day). This mutation-honest test goes RED if the guard is removed or if the
    day fabricates a magnitude instead of NaN.
  </action>
  <verify>
    <automated>PY312=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python; cd analytics-service && $PY312 -m pytest tests/test_derive_broker_dailies_dualmode.py -q && $PY312 -m pytest tests/test_deribit_txn.py tests/test_deribit_ingest.py tests/test_nav_twr.py tests/test_external_flows.py -q</automated>
  </verify>
  <acceptance_criteria>
    - The dominating withdrawal day yields NaN + `flow_dominated_guard` + `complete_with_warnings` (not r_t==0, not a fabricated day).
    - Both SC4 cases (sub-NAV → 0, dominating → guard) are pinned in the same file; the broader phase-75 test set is GREEN.
  </acceptance_criteria>
  <done>The dominating-withdrawal guard is proven; both reconciled SC4 outcomes are pinned; the phase's RISKY acceptance is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dated flows → core (integration) | The full valued-flow list crosses into reconstruct_nav_and_twr; a mis-sized/mis-timed flow would fabricate a day at the integration level. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-04-NEU | Tampering | flow-neutral property | mitigate | Sub-NAV pure-flow → r_t==0 pinned; dropped-flow mutation proves the flow is load-bearing. |
| T-75-04-DOM | Tampering | dominating-withdrawal guard | mitigate | |F|>=NAV_{t-1} → NaN + flow_dominated_guard + complete_with_warnings pinned (honest, never fabricated ±100%). |
| T-75-04-EVT | Tampering | event-time valuation at integration | mitigate | Withdrawal ExternalFlow asserted equal to change*same-day index on its actual day; wrong-time/1.0 → RED. |
</threat_model>

<verification>
- `tests/test_derive_broker_dailies_dualmode.py -k ltp068` GREEN in the CI-3.12 venv (both cases).
- Phase-75 test set (`test_deribit_txn.py test_deribit_ingest.py test_nav_twr.py test_external_flows.py test_derive_broker_dailies_dualmode.py`) GREEN.
- Full analytics suite green + coverage >=80% before `/gsd:verify-work` (phase gate, per 75-VALIDATION.md).
</verification>

<success_criteria>
- FLOW-02 acceptance: a real BTC withdrawal → correctly-signed event-time F_t on its actual day; sub-NAV pure-flow → r_t==0; dominating withdrawal → flow_dominated_guard (both SC4 cases pinned, mutation-honest).
</success_criteria>

<output>
Create `.planning/phases/75-deribit-dated-flow-adapter-risky/75-04-SUMMARY.md` when done.
</output>
