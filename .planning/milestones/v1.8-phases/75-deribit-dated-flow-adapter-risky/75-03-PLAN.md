---
phase: 75-deribit-dated-flow-adapter-risky
plan: 03
type: tdd
wave: 3
depends_on: [75-02]
files_modified:
  - analytics-service/services/deribit_ingest.py
  - analytics-service/tests/test_deribit_ingest.py
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_job_worker_deribit.py
autonomous: true
requirements: [FLOW-02]
must_haves:
  truths:
    - "The Deribit crawl accumulates a dated list[ExternalFlow] on CompletenessReport (replacing the net-scalar + saw_unvalued_inverse_flow fields), sourced from deribit_dated_external_flows_usd"
    - "A quiet-day inverse withdrawal gets its same-day settlement index fetched (Finding C1 end-to-end) so it values instead of failing the whole job"
    - "The F1 scalar anchor correction (equity -= net_external_flow_usd) at job_worker.py:1968-1979 is DELETED; equity is no longer adjusted by a net flow"
    - "Deribit dated flows are passed as external_flows= into combine_realized_and_funding, feeding ONLY the core F_t term"
  artifacts:
    - path: "analytics-service/services/deribit_ingest.py"
      provides: "CompletenessReport.dated_external_flows: list[ExternalFlow]; crawl accumulation + flow-day settlement-index fetch"
      contains: "dated_external_flows"
    - path: "analytics-service/services/job_worker.py"
      provides: "F1 scalar deleted; external_flows threaded into combine_realized_and_funding"
      contains: "external_flows="
  key_links:
    - from: "analytics-service/services/deribit_ingest.py crawl loop"
      to: "analytics-service/services/deribit_txn.py::deribit_dated_external_flows_usd"
      via: "per (scope, currency) accumulation into CompletenessReport.dated_external_flows"
      pattern: "deribit_dated_external_flows_usd"
    - from: "analytics-service/services/job_worker.py combine call"
      to: "analytics-service/services/broker_dailies.py::combine_realized_and_funding"
      via: "external_flows=_completeness.dated_external_flows"
      pattern: "external_flows=_completeness"
---

<objective>
Retire the net-scalar flow contract end-to-end and route dated flows through the honest core. In the
Deribit crawl (`deribit_ingest.py`), replace `CompletenessReport.net_external_flow_usd` +
`saw_unvalued_inverse_flow` with a dated `list[ExternalFlow]` accumulated via
`deribit_dated_external_flows_usd`, and — because Finding C1 (Plan 75-02) now makes
`inverse_days_needing_index` emit inverse flow days — feed the fetched `supplemental_index` into the
dated-flow producer so quiet-day inverse withdrawals value instead of failing the job. Then delete
the F1 scalar anchor correction (`equity -= net_external_flow_usd`, `job_worker.py:1968-1979`, its
SOLE consumer) and pass `external_flows=_completeness.dated_external_flows` into
`combine_realized_and_funding`.

Purpose: FLOW-02 — the scalar under-corrected (inverse flows punted to `balance_error`); the dated
list feeds ONLY the NAV `F_t` term while staying excluded from the realized sum (count-once via
INFORMATIONAL_TYPES). The dataclass field swap and the F1 deletion are ATOMIC — the scalar's producer
and its sole consumer are edited together so no intermediate state references removed fields.

Output: dated-flow accumulation + C1 flow-day fetch in the crawl; F1 deleted; dated flows threaded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-CONTEXT.md
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-RESEARCH.md
@.planning/phases/75-02-SUMMARY.md

<interfaces>
From analytics-service/services/deribit_ingest.py:
  # CompletenessReport (dataclass, :510-533): fields expected, entries, net_external_flow_usd,
  #   saw_unvalued_inverse_flow, total_return_rows. REPLACE the two flow fields with
  #   dated_external_flows: list[ExternalFlow]. total_return_rows STAYS (C2 floor).
  # crawl loop (:588-693): per (scope, currency) it already builds `supplemental` from
  #   inverse_days_needing_index(rows) + fetch_deribit_settlement_index (:630-664), then calls
  #   txn_rows_to_daily_records(rows, supplemental_index=supplemental) (:665). The F1 accumulation
  #   at :669-673 calls deribit_linear_external_flow_usd(rows) — REPLACE with
  #   deribit_dated_external_flows_usd(rows, supplemental_index=supplemental) and extend the dated list.
  # Because inverse_days_needing_index now (75-02) returns inverse FLOW days too, `supplemental`
  #   already covers them — pass the SAME supplemental map into the dated-flow producer.

From analytics-service/services/job_worker.py:
  # deribit branch: realized, _completeness = await fetch_deribit_ledger_daily_records(...) (:1888)
  # C2 floor uses _completeness.total_return_rows (:1948-1953) — UNCHANGED, must stay.
  # F1 scalar to DELETE (:1968-1979): the `if equity is not None and not balance_error:` block that
  #   reads _completeness.saw_unvalued_inverse_flow and subtracts _completeness.net_external_flow_usd.
  # combine call (:2011-2014): combine_realized_and_funding(realized, funding, account_balance=equity,
  #   balance_error=balance_error) — ADD external_flows=_completeness.dated_external_flows.
  # NavReconstructionError is already caught permanent at :2015+ (74-04). LedgerValuationError from an
  #   unvaluable inverse flow is already caught permanent at :1917-1942 (inherited; no new handler).

From analytics-service/services/broker_dailies.py (UNCHANGED — 74-02 already threads it):
  # combine_realized_and_funding(realized, funding, *, account_balance, balance_error,
  #   external_flows=None, open_unrealized_usd=0.0)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CompletenessReport dated flows + crawl accumulation + C1 flow-day fetch</name>
  <files>analytics-service/services/deribit_ingest.py, analytics-service/tests/test_deribit_ingest.py</files>
  <read_first>
    - analytics-service/services/deribit_ingest.py lines 510-533 (CompletenessReport), 588-693 (crawl loop, supplemental fetch, F1 accumulation)
    - 75-RESEARCH.md "Finding C1", "Runtime State Inventory" (only consumer of the old fields is job_worker), Q3 (same-day index sourcing)
    - analytics-service/tests/test_deribit_ingest.py around test_producer_values_quiet_inverse_day_via_settlement_index (:682) — the stub/monkeypatch producer pattern to mirror
  </read_first>
  <behavior>
    - Dated accumulation proof: crawling `inverse_flow_day_with_index_rows()` yields a `CompletenessReport.dated_external_flows` containing one correctly-signed, event-time-valued `ExternalFlow` on the withdrawal's actual UTC day.
    - C1 end-to-end proof: crawling a quiet-day inverse withdrawal (`inverse_flow_day_without_index_rows()`) with a stubbed `fetch_deribit_settlement_index` returning the same-day price → the flow VALUES (appears in dated_external_flows), NOT a `LedgerValuationError`. Reverting the 75-02 C1 extension (so the day is never fetched) makes this RED (fail loud).
    - Linear proof: a USDC deposit day accumulates a +USD dated flow with no index fetch.
    - Field-swap proof: `CompletenessReport` no longer exposes `net_external_flow_usd` / `saw_unvalued_inverse_flow`; `total_return_rows` still present and correct.
    - Count-once proof (crawl level): the same flow row is absent from the returned realized `daily_records` (txn_rows_to_daily_records skips it) and present once in `dated_external_flows`.
  </behavior>
  <action>
    In `deribit_ingest.py`: (1) Replace `CompletenessReport.net_external_flow_usd: float` and
    `saw_unvalued_inverse_flow: bool` with `dated_external_flows: list[ExternalFlow] = field(default_factory=list)`
    (import `ExternalFlow` from `services.external_flows`); keep `total_return_rows`. Update the dataclass
    docstring. (2) In the crawl loop, replace the F1 accumulation (`flow, saw_inverse =
    deribit_linear_external_flow_usd(rows)` at :669-673) with
    `dated = deribit_dated_external_flows_usd(rows, supplemental_index=supplemental)` and extend a
    per-crawl `dated_external_flows` accumulator; the SAME `supplemental` map already built for
    `txn_rows_to_daily_records` (which now covers inverse flow days via the 75-02 C1 extension) is passed
    in. (3) Return `CompletenessReport(..., dated_external_flows=dated_external_flows,
    total_return_rows=total_return_rows)`. Remove the now-unused `deribit_linear_external_flow_usd` import
    if nothing else uses it. Do NOT change the supplemental-fetch machinery (`inverse_days_needing_index`
    +`fetch_deribit_settlement_index` cache) beyond confirming it feeds the dated producer — the C1
    extension in 75-02 already widened which days it fetches. Update any test asserting the removed fields.
  </action>
  <verify>
    <automated>PY312=/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python; cd analytics-service && $PY312 -m pytest tests/test_deribit_ingest.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `CompletenessReport.dated_external_flows` carries correctly-signed, event-time-valued flows; the old scalar fields are gone.
    - The C1 end-to-end test proves a quiet-day inverse withdrawal VALUES via the fetched settlement index (RED if the C1 extension is reverted).
    - `total_return_rows` and the C2 floor inputs are unchanged; full `tests/test_deribit_ingest.py` GREEN.
  </acceptance_criteria>
  <done>The crawl produces a dated ExternalFlow list on CompletenessReport, quiet-day inverse flows value via the fetched index, and the net-scalar fields are retired.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the F1 scalar + thread dated flows into combine_realized_and_funding</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_job_worker_deribit.py</files>
  <read_first>
    - analytics-service/services/job_worker.py lines 1838-1892 (deribit branch, realized/_completeness), 1943-1979 (C2 floor to KEEP + F1 scalar to DELETE), 2011-2035 (combine call + NavReconstructionError catch)
    - 75-RESEARCH.md Q2 (F1 deletion plan, sole consumer confirmed), "Anti-Patterns to Avoid" (never silently degrade an unvaluable inverse flow to balance_error)
  </read_first>
  <behavior>
    - F1-deletion proof: `equity` passed to `combine_realized_and_funding` equals the raw
      `fetch_deribit_account_equity_usd` anchor — it is NOT reduced by any net flow. A test/source-scan
      proves the `equity = equity - _completeness.net_external_flow_usd` line is gone and not reintroduced.
    - No-double-correction proof: with dated flows present, the anchor is corrected exactly once — inside
      the core's F_t roll — and never a second time by a scalar (the scalar path is absent).
    - Threading proof: `combine_realized_and_funding` is called with
      `external_flows=_completeness.dated_external_flows`; the dated list reaches the core.
    - C2-floor-preserved proof: the material-equity + zero-return-rows floor (`total_return_rows == 0`)
      still fires (unchanged).
    - Fail-loud inheritance proof: an unvaluable inverse flow surfaces as `LedgerValuationError`
      (permanent, already caught at :1917-1942) — NOT silently degraded to `balance_error` (the deleted
      scalar's old behavior).
  </behavior>
  <action>
    In `job_worker.py`: DELETE the entire F1 block at :1968-1979 (`if equity is not None and not
    balance_error: if _completeness.saw_unvalued_inverse_flow: balance_error = True else: equity = equity
    - _completeness.net_external_flow_usd`). KEEP the C2 equity-vs-activity floor (:1948-1953) and the
    `funding: list[Any] = []` line (:1980-1981) — funding stays empty (settlement-bundled). Change the
    `combine_realized_and_funding(realized, funding, account_balance=equity, balance_error=balance_error)`
    call (:2012-2014) to also pass `external_flows=_completeness.dated_external_flows`. Do NOT touch the
    NavReconstructionError catch (74-04) or the LedgerValuationError catch (:1917-1942) — the dated path
    inherits both. The `equity` anchor now flows into the core UNADJUSTED; the core's backward NAV roll
    (`NAV_{t-1} = NAV_t - pnl_t - F_t`) performs the one honest flow correction. Add a mutation-honest
    source-scan test asserting `net_external_flow_usd`/`saw_unvalued_inverse_flow` no longer appear in the
    F1 region of job_worker.py (the double-correction cannot be reintroduced).
  </action>
  <verify>
    <automated>PY312=/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python; cd analytics-service && $PY312 -m pytest tests/test_job_worker_deribit.py -q; grep -nE 'equity = equity - .*external_flow|saw_unvalued_inverse_flow' analytics-service/services/job_worker.py | grep -v '^#'</automated>
  </verify>
  <acceptance_criteria>
    - The F1 scalar subtraction is deleted; the grep returns NO active `equity -= net_external_flow` / `saw_unvalued_inverse_flow` line in job_worker.py.
    - `combine_realized_and_funding` is called with `external_flows=_completeness.dated_external_flows`.
    - The C2 floor and both permanent-fail catches (Ledger/Nav) are unchanged; the job_worker deribit tests are GREEN.
  </acceptance_criteria>
  <done>The net-scalar anchor correction is gone, equity flows unadjusted into the core, and dated flows feed the core's F_t exactly once.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawl → CompletenessReport | Dated flows accumulated from untrusted rows; a mis-fetched index day = fail-loud, not silent. |
| _completeness → combine → core | The equity anchor + dated flows cross into the honest NAV/TWR core; double-correction (scalar + F_t) would fabricate a day. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-75-03-DBL | Tampering | F1 scalar vs core F_t | mitigate | Delete the sole scalar consumer; source-scan proves no double-correction can be reintroduced. |
| T-75-03-C1E | Denial of Service | quiet-day inverse flow fetch | mitigate | The same supplemental map (widened by 75-02 C1) feeds the dated producer; C1 end-to-end test proves the flow values instead of failing the job. |
| T-75-03-DEG | Information Disclosure / Integrity | unvaluable inverse flow | mitigate | No silent degrade to balance_error; LedgerValuationError (permanent, scrubbed) inherited from :1917-1942. |
| T-75-03-C2 | Spoofing (empty-green ledger) | C2 floor | accept/preserve | total_return_rows floor unchanged — a material-equity zero-activity ledger still fails loud. |
</threat_model>

<verification>
- `tests/test_deribit_ingest.py` and `tests/test_job_worker_deribit.py` GREEN in the CI-3.12 venv.
- Source-scan: no active F1 scalar subtraction / saw_unvalued_inverse_flow reference in job_worker.py.
- Field swap complete: no code references CompletenessReport.net_external_flow_usd / saw_unvalued_inverse_flow.
- `mypy --strict` clean on the two changed service files.
</verification>

<success_criteria>
- FLOW-02 (wiring half): dated flows accumulated on CompletenessReport, quiet-day inverse flows value via the fetched index (C1 end-to-end), F1 scalar deleted, dated flows threaded to the core.
</success_criteria>

<output>
Create `.planning/phases/75-deribit-dated-flow-adapter-risky/75-03-SUMMARY.md` when done.
</output>
