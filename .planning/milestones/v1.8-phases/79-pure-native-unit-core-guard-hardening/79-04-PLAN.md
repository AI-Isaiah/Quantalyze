---
phase: 79-pure-native-unit-core-guard-hardening
plan: 04
type: tdd
wave: 4
depends_on: [79-01, 79-02, 79-03]
files_modified:
  - analytics-service/services/deribit_txn.py
  - analytics-service/services/deribit_ingest.py
  - analytics-service/tests/test_deribit_txn.py
  - analytics-service/tests/test_deribit_ingest.py
  - analytics-service/tests/test_native_nav_sc4_identity.py
autonomous: true
requirements: [IDX-01, NAT-01]

must_haves:
  truths:
    - "All FIVE _INVERSE_CURRENCIES census consumers (§7.1) take keyword-only indexable_currencies with default = the current frozenset — every existing caller/test byte-identical"
    - "With SOL injected into the set, txn_change_to_usd values a SOL row (change × sol index), classify_instrument_settlement classifies SOL-PERPETUAL, _day_ccy_own_index seeds SOL, inverse_days_needing_index plans SOL fetches — the key-1 crash class dies at the function level"
    - "build_deribit_indexable_currencies produces static_floor ∪ {probed ccy} from stubbed public/get_index_price responses, is cached-per-job shaped, AND is threaded live into fetch_deribit_ledger_daily_records (built once per job right after enumerate_currencies) so SOL heals on the existing USD-space path in Phase 79 (§7.2; 79-CONTEXT G6)"
    - "The SC-4 dual-run suite proves legacy and native paths bit-exact (check_exact=True series AND byte-equal meta) over the §4.2 all-USD-family fixture matrix, plus the IEEE x*1.0 bit-identity micro-pin"
  artifacts:
    - path: "analytics-service/services/deribit_txn.py"
      provides: "indexable_currencies keyword on txn_change_to_usd / classify_instrument_settlement / _day_ccy_own_index / inverse_days_needing_index (default _INVERSE_CURRENCIES)"
      contains: "indexable_currencies"
    - path: "analytics-service/services/deribit_ingest.py"
      provides: "build_deribit_indexable_currencies probe builder, built once per job and threaded live into fetch_deribit_ledger_daily_records (:636 gate + inverse_days_needing_index)"
      contains: "build_deribit_indexable_currencies"
    - path: "analytics-service/tests/test_native_nav_sc4_identity.py"
      provides: "dual-run legacy-vs-native bit-exact suite + IEEE micro-pin (§4.2)"
      contains: "check_exact"
  key_links:
    - from: "txn_change_to_usd (deribit_txn.py:210)"
      to: "indexable_currencies parameter"
      via: "membership test against the injected set instead of the module constant"
      pattern: "indexable_currencies"
    - from: "fetch_deribit_ledger_daily_records (deribit_ingest.py:585)"
      to: "build_deribit_indexable_currencies(exchange, currencies, static_floor=_INVERSE_CURRENCIES)"
      via: "built ONCE per job right after enumerate_currencies; the built set feeds the :636 gate AND inverse_days_needing_index(rows, indexable_currencies=indexable)"
      pattern: "build_deribit_indexable_currencies"
    - from: "tests/test_native_nav_sc4_identity.py"
      to: "reconstruct_nav_and_twr AND reconstruct_native_nav_and_twr"
      via: "dual-run over identical rows; assert_series_equal(check_exact=True) + meta dict equality"
      pattern: "check_exact"
---

<objective>
Generalize `_INVERSE_CURRENCIES` from "the two currencies we bothered to hardcode"
to "any currency with a resolvable `{ccy}_usd` index" WITHOUT changing the
fail-loud shape (contract §7.2): every census consumer (§7.1) gains
`indexable_currencies: AbstractSet[str] = _INVERSE_CURRENCIES` keyword-only —
default byte-identical; the constant is retained as the STATIC FLOOR (degraded-
mode default, never the ceiling). Build the per-job probe-set builder from the
verbatim-existing probe code (`deribit_ingest.py:816-827`) AND thread it live
(79-CONTEXT G6, revised): `fetch_deribit_ledger_daily_records` builds the real
set ONCE per job right after `enumerate_currencies` (:585) and threads it into
the :636 supplemental-index gate and the internal `inverse_days_needing_index`
call — the USD-space SOL heal (key-1's crash) lands in Phase 79 on the existing
path (§7.2). Only `build_deribit_native_ledger` / `combine_native_ledger` /
native-core routing remain in Phase 80.

Then close the phase with the SC-4 dual-run identity suite (§4.2): USD-native
accounts must be BIT-EXACT old-vs-new — identity holds by construction (branch-1
coalescing + IEEE-754 ×1.0 no-op + verbatim arithmetic reuse, §4.1 ops A–J), and
this suite is what proves it and what Phase 80 gates on (SHIP GATE i).

Purpose: SC-5 (including the live SOL-heal threading) + SC-6 (identity-suite half) of Phase 79.
Output: 5-consumer kwarg injection; probe builder threaded live; SC-4 suite + IEEE micro-pin.

IDX-01; the suite exercises NAT-01 (gates in Phase 80 as SC-4/INCEPT-01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v1.9-ROADMAP.md
@.planning/STATE.md
@.planning/phase-78/P1_native_core_contract.md
@.planning/phases/79-pure-native-unit-core-guard-hardening/79-CONTEXT.md

<interfaces>
The §7.1 census (ALL anchors VERIFIED current):

| Site | Today on SOL |
|---|---|
| `txn_change_to_usd` deribit_txn.py:210-217 | raises LedgerValuationError ("refusing to blind-multiply an unknown currency") — the key-1 crash |
| `classify_instrument_settlement` deribit_txn.py:156-161 | raises ValueError for SOL-PERPETUAL |
| `_day_ccy_own_index` deribit_txn.py:494 | skips SOL rows — no same-day own index seeds |
| `inverse_days_needing_index` deribit_txn.py:559-560 | skips SOL — settlement-index fetch never planned |
| `fetch_deribit_ledger_daily_records` deribit_ingest.py:636 | skips the supplemental-index fetch for SOL |

Probe code (reused verbatim per §3.3(a)/§7.2): deribit_ingest.py:816-827 —
`await exchange.public_get_get_index_price({"index_name": f"{ccy.lower()}_usd"})`,
success → member. `enumerate_currencies` (:269) is the authoritative universe;
`settlement_index_cache` (:601) is the per-job cache discipline to mirror;
`USD_FAMILY` (79-01) is excluded from probing.

For the SC-4 suite: legacy path = `trades_to_daily_returns_with_status`
(transforms.py:83-90, core call at :198+) → `reconstruct_nav_and_twr`
(nav_twr.py:627). Native path = `reconstruct_native_nav_and_twr` (79-02). §4.1
identity mechanism: branch-1 coalescing (same floats, same row order — ops A/B/C),
×1.0 IEEE no-op, verbatim reuse (ops D–J incl. wedge `terminal = anchor − upnl`
nav_twr.py:673 and guard comparisons :362-368).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: indexable_currencies kwarg across the 4 deribit_txn consumers — RED then GREEN</name>
  <files>analytics-service/services/deribit_txn.py, analytics-service/tests/test_deribit_txn.py</files>
  <read_first>
    - services/deribit_txn.py:95-110 (constants + assert), :133-162 (classify_instrument_settlement), :184-237 (txn_change_to_usd), :462-513 (_day_ccy_own_index), :516-578 (inverse_days_needing_index)
    - tests/test_deribit_txn.py — the existing SOL/BUIDL refusal pins (they must STAY green under the default)
  </read_first>
  <behavior>
    - default_byte_identical: calling each of the four functions WITHOUT the new
      kwarg over the existing fixture corpus produces today's outputs exactly —
      SOL still refuses/skips by default (every existing caller/test byte-identical,
      §7.2). The existing "refusing to blind-multiply" pins stay green untouched.
    - sol_injected_per_function: with `indexable_currencies=frozenset({"BTC",
      "ETH", "SOL"})`: (a) txn_change_to_usd values a SOL row `change ×
      index_price` (own or fallback) — same multiply, same same-day rule, no new
      conversion path; (b) classify_instrument_settlement("SOL-PERPETUAL")
      returns (True, "SOL"); (c) _day_ccy_own_index seeds a (day, "SOL") entry
      from a SOL index-bearing row; (d) inverse_days_needing_index includes a
      quiet SOL day. (e) BUIDL with the same injected set STILL refuses in
      txn_change_to_usd — the refusal text/semantics unchanged, now firing only
      for genuinely un-indexable currencies (§7.2, the §3.4 branch-3 behavior by
      construction).
    - constant_retained_as_floor: `_INVERSE_CURRENCIES == frozenset({"BTC","ETH"})`
      still (the static floor — comment retargeted, value unchanged).
  </behavior>
  <action>
    RED: add the tests (kwarg absent → TypeError → red; defaults corpus already
    green — keep those as the byte-identity pins). GREEN: add
    `*, indexable_currencies: AbstractSet[str] = _INVERSE_CURRENCIES` to all four
    functions and replace each `in _INVERSE_CURRENCIES` membership test with the
    parameter (:156, :210, :494, :560; also the `sorted(_INVERSE_CURRENCIES)` in
    the two raise messages → `sorted(indexable_currencies)` so the message names
    the ACTUAL set consulted). `inverse_days_needing_index` passes its set through
    to its internal `_day_ccy_own_index` call (:547) so the two can never diverge
    (the :541-545 "all three never disagree" pin). Update the :97-102 comment:
    the constant is the STATIC FLOOR — currencies known-resolvable without a
    probe; the runtime set is injected (§7.2); it is the degraded-mode default,
    never the ceiling. Keep the :108-110 static assert.
    Mutation-honesty: changing the default set flips default_byte_identical red;
    testing against the module constant instead of the parameter flips
    sol_injected_per_function red; letting BUIDL through with an injected set
    flips (e) red (the silent-mis-scale class the module exists to fight).
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_deribit_txn.py -x -q && cd analytics-service && $PY312 -m mypy --strict services/deribit_txn.py</automated>
  </verify>
  <acceptance_criteria>
    Four consumers parameterized; ALL pre-existing deribit_txn pins green
    untouched; SOL works per-function under injection; BUIDL still refuses;
    mypy --strict clean.
  </acceptance_criteria>
  <done>Membership is injected with a byte-identical static-floor default across the deribit_txn census.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build_deribit_indexable_currencies probe builder + LIVE threading into fetch_deribit_ledger_daily_records (SOL heals on the existing path) — RED then GREEN</name>
  <files>analytics-service/services/deribit_ingest.py, analytics-service/tests/test_deribit_ingest.py</files>
  <read_first>
    - services/deribit_ingest.py:545-680 (fetch_deribit_ledger_daily_records — the :636 membership test + :601 settlement_index_cache discipline), :816-827 (the verbatim probe), :269-300 (enumerate_currencies)
    - tests/test_deribit_ingest.py — existing stubbed-exchange style
  </read_first>
  <behavior>
    - probe_builder: `build_deribit_indexable_currencies(exchange, currencies,
      *, static_floor=_INVERSE_CURRENCIES)` returns
      `static_floor ∪ {ccy for ccy in currencies if ccy ∉ USD_FAMILY and
      ccy ∉ static_floor and the stubbed
      public_get_get_index_price(f"{ccy.lower()}_usd") succeeds with a finite
      positive index_price}` — per §7.2/§3.3(a), the :816-827 probe shape
      verbatim (exception → not a member, non-finite/≤0 → not a member; USD-family
      AND static-floor members never probed — BTC/ETH are members by the floor,
      so existing BTC/ETH-only stubs that never stub the index endpoint stay
      green). Stub matrix: SOL resolves → in; BUIDL raises → out; USDC → never
      probed (assert the mock was NOT called for it); BTC → in without a probe.
    - probe_called_once_per_currency: the builder probes each currency at most
      once (the per-job cache discipline of settlement_index_cache :601 — the
      result is computed once and REUSED across scopes; the builder is called
      ONCE per job by fetch).
    - built_and_threaded_live (G6, revised): inside
      `fetch_deribit_ledger_daily_records`, IMMEDIATELY after
      `currencies = await enumerate_currencies(exchange, scopes[0], {})` (:585)
      and before the Pass-1 scope loop, `indexable = await
      build_deribit_indexable_currencies(exchange, currencies,
      static_floor=_INVERSE_CURRENCIES)` is built ONCE per job; the :636 gate
      tests `ccy_upper in indexable` (NOT the module constant) and the internal
      call becomes `inverse_days_needing_index(rows,
      indexable_currencies=indexable)`. A source-scan asserts the built set (not
      `_INVERSE_CURRENCIES`) drives the :636 gate.
    - sol_heals_on_existing_path: a stubbed exchange whose enumerated universe
      includes SOL, whose `public_get_get_index_price("sol_usd")` resolves
      finite-positive, and whose txn log carries a SOL `change` row →
      `fetch_deribit_ledger_daily_records` returns daily records for SOL and
      raises NO `LedgerValuationError` (the key-1 crash healed on the existing
      USD-space path, in Phase 79). Mirror case: the SAME account when the
      `sol_usd` probe RAISES / resolves non-finite → SOL ∉ `indexable` → the SOL
      row still refuses loudly (LedgerValuationError) — the fail-loud shape
      survives (§7.2 branch-3 by construction).
  </behavior>
  <action>
    RED: add the tests. GREEN: (1) implement `async def
    build_deribit_indexable_currencies(exchange, currencies, *,
    static_floor=_INVERSE_CURRENCIES) -> frozenset[str]` next to the probe it
    reuses (:816-827), with a §7.2 docstring (the real set per job =
    floor ∪ probed; the floor is degraded-mode, never the ceiling; SOL is
    indexable because sol_usd resolves, BUIDL is not). Reject a non-finite/≤0
    index_price; USD_FAMILY AND static-floor members are never probed; scrub any
    logged ccxt error (scrub_freeform_string precedent :291). (2) THREAD IT LIVE:
    inside `fetch_deribit_ledger_daily_records`, immediately after the
    `enumerate_currencies` call (:585) and before the Pass-1 scope loop, add
    `indexable = await build_deribit_indexable_currencies(exchange, currencies,
    static_floor=_INVERSE_CURRENCIES)`; replace the :636
    `ccy_upper in _INVERSE_CURRENCIES` gate with `ccy_upper in indexable`, and
    replace the inner `inverse_days_needing_index(rows)` (:638-640 region) with
    `inverse_days_needing_index(rows, indexable_currencies=indexable)`. Build
    ONCE per job (mirror the settlement_index_cache :601 once-per-job
    discipline). Cite 79-CONTEXT G6 (revised): the USD-space SOL heal lands in
    79; only the native-core adapters remain in Phase 80.
    Mutation-honesty: probing USD-family flips probe_builder red (wasted calls +
    the disjointness design); returning probed-only (dropping the floor) flips
    probe_builder red on BTC/ETH; reverting the :636 gate to the module constant
    `_INVERSE_CURRENCIES` (un-threading the built set) flips
    sol_heals_on_existing_path red — SOL would refuse again; inverting the probe
    success test (admitting SOL only when the probe RAISES) flips both
    probe_builder and the sol_heals mirror case.
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_deribit_ingest.py -x -q && cd analytics-service && $PY312 -m mypy --strict services/deribit_ingest.py</automated>
  </verify>
  <acceptance_criteria>
    Probe builder green on the stub matrix; the built set is threaded live into
    the :636 gate + inverse_days_needing_index; sol_heals_on_existing_path green
    (SOL returns records, no LedgerValuationError) AND the mirror refuses;
    pre-existing stub tests green under a SOL-free universe; mypy --strict clean.
  </acceptance_criteria>
  <done>The probe builder is threaded live; SOL heals on the existing USD-space path (stub-proven, no LedgerValuationError) and un-indexable currencies still refuse loudly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC-4 dual-run identity suite + IEEE micro-pin (tests/test_native_nav_sc4_identity.py)</name>
  <files>analytics-service/tests/test_native_nav_sc4_identity.py</files>
  <read_first>
    - contract §4.1 (the three identity mechanisms + operations table A–J), §4.2 (the suite spec verbatim)
    - services/transforms.py:83-90 + the reconstruct_nav_and_twr call (:198+) — the legacy side
    - services/native_nav.py (79-02) — NativeLedger + reconstruct_native_nav_and_twr — the native side
    - services/deribit_txn.py:713/:768 (by_day row-order accumulation — op A the shim must reproduce), :271-283 (deribit_equity_to_usd summaries-order anchor — op C)
  </read_first>
  <behavior>
    - fixture_matrix (§4.2 item 1, all synthetic all-USDC/USDT): {no flows,
      quiet-day flow, flow-dominated day, negative-reconstructed-NAV day (an
      INTERIOR break — retained returns on BOTH sides — so twr_chain_broken
      fires in the legacy AND native meta and a forked/one-sided detector is
      caught), dust day, uPnL wedge below 5%, wedge above 5%, multi-USD-family
      currencies on the same day (float64 order-sensitive change values chosen so
      `(a+b)+c != a+(b+c)` in IEEE-754 — e.g. a large magnitude plus two small
      ones — so a currency-sum-before-roll neuter reddens on the NUMBER rather
      than passing silently on "nice" values)}. Each fixture defines per-currency
      rows (day, ccy, change) + per-currency terminal equities + optional
      per-currency wedge + 4-field flows.
    - dual_run_bit_exact (§4.2 items 2–3): legacy = the per-day summed records
      through trades_to_daily_returns_with_status(...) (anchor = Σ equities in
      summaries order − Σ wedge, flows as today); native = the SAME rows through
      a TEST-LOCAL adapter shim → NativeLedger (per-currency native_pnl summed
      per (day, ccy) in row order; branch-1 coalescing happens inside the core;
      marks EMPTY — branch-1 must not appear in marks; 4-field flows;
      full_history=True with residual-clean fixtures) →
      reconstruct_native_nav_and_twr(indexable_currencies=frozenset()).
      Assert `pd.testing.assert_series_equal(legacy, native, check_exact=True)`
      (BIT-exact) AND `dict(legacy_meta) == dict(native_meta)` (guard flags and
      computation_status_hint byte-equal — incl. the warned fixtures:
      flow-dominated, dust, negative-NAV → twr_chain_broken parity via 79-03,
      wedge > 5% → unrealized_pnl_in_anchor parity).
    - ieee_micro_pin (§4.2 item 4): `struct.pack("<d", x * 1.0) ==
      struct.pack("<d", x)` over a sample including -0.0, denormals
      (5e-324, 2.2e-308), large magnitudes (1e308), and typical NAV floats — the
      premise pinned AT the premise, so a platform/numpy change is caught here,
      not downstream.
    - inception_gate_green_on_matrix: every matrix fixture passes the §5 gate
      (fixtures constructed residual-clean), pinning that the gate does not
      perturb the identity path.
  </behavior>
  <action>
    Create the suite per §4.2 verbatim. The shim is TEST-LOCAL (the real adapter
    `build_deribit_native_ledger` is Phase 80 — this suite re-runs against the
    real adapter there, SHIP GATE i) and must reproduce the exact legacy
    accumulation floats: sum per (day, ccy) in producer row order (op A), Σ
    terminal equities in the same iteration order (op C), wedge subtraction as
    `terminal = anchor − upnl` (op D). Parametrize over the matrix. Document each
    fixture with the §4.1 operation(s) it stresses (A–J table in the module
    docstring).
    Mutation-honesty (§4.2 item "prove it fails when neutered" — encode as
    comments naming the neuter per assertion): (1) mutating the native path to
    sum currencies BEFORE rolling (order change) flips the multi-USD-family
    fixture's check_exact red — GUARANTEED because that fixture's change values
    are float64 order-sensitive (`(a+b)+c != a+(b+c)`), so the re-association
    perturbs the last bit (a "nice"-valued fixture would let this neuter pass
    silently); (2) multiplying by a recomputed 1.0-ish mark (e.g. float("1.0")
    arithmetic detour or a mark Series lookup for branch-1) flips check_exact red
    on any fixture via the IEEE premise; (3) deriving day-0 prev differently
    (prev0 ≠ the literal cur − pnl0 − flow_t reduction) flips the no-flows
    fixture red on day 0; (4) a forked/one-sided break detector or a meta key
    divergence flips the negative-reconstructed-NAV (interior-break) fixture's
    meta dict equality red — the break is INTERIOR (retained both sides) so
    twr_chain_broken MUST appear in BOTH legacy and native meta; a detector that
    fires on only one path (or native missing unrealized_pnl_in_anchor on the
    >5% wedge fixture) reddens the dict equality.
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_native_nav_sc4_identity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    Full matrix bit-exact (series) + byte-equal (meta); IEEE micro-pin green;
    inception gate green across the matrix; every mutation-honesty neuter is
    named in-suite next to the assertion it reddens.
  </acceptance_criteria>
  <done>SC-4 identity is proven on synthetic fixtures — the suite Phase 80 re-points at the real adapter and gates merge on (roadmap SHIP GATE i). Live tuning/real keys NOT claimed here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exchange REST (public index endpoints) → probe builder | untrusted probe responses decide index-multiply eligibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-12 | Tampering (integrity) | injected indexable set | mitigate | default = static floor (byte-identical); BUIDL-still-refuses pin; USD_FAMILY ∩ indexable disjointness validated at classification time (79-01 G1) |
| T-79-13 | Spoofing | probe response admitting a bogus currency | mitigate | membership only enables the SAME same-day index multiply (no new conversion path, §7.2); a bad mark still refuses at the ≤0/non-finite gate; density contract refuses missing days |
| T-79-14 | Tampering (integrity) | SC-4 identity drift | mitigate | bit-exact dual-run suite (check_exact + meta equality) + IEEE premise pin; §4.1 ops A–J each stressed by a named fixture |
| T-79-15 | Tampering (integrity) | live probe threading admits a bogus currency | mitigate | sol_heals mirror case: an unresolvable/raising probe leaves the ccy OUT → the row still refuses loudly (LedgerValuationError); USD_FAMILY ∩ indexable disjointness (79-01 G1); source-scan asserts the built set (not the constant) drives the :636 gate |
| T-79-SC | Tampering | pip installs | accept | no packages installed — no supply-chain surface |
</threat_model>

<verification>
- Setup: `$PY312` per 79-01 (python3.12 venv pinned to analytics-service/requirements.txt).
- `$PY312 -m pytest analytics-service/tests/ -x -q` fully green (whole analytics suite — the phase-exit bar).
- `$PY312 -m mypy --strict analytics-service/services/deribit_txn.py analytics-service/services/deribit_ingest.py` clean.
- `grep -rn "build_deribit_indexable_currencies" analytics-service/services/deribit_ingest.py` shows BOTH the definition AND the once-per-job call inside `fetch_deribit_ledger_daily_records` (built right after enumerate_currencies, :585).
</verification>

<success_criteria>
- SC-5 (full): all five census consumers injected with default-identical membership; probe builder stub-proven AND threaded live into fetch_deribit_ledger_daily_records (built once per job after enumerate_currencies); SOL heals on the existing USD-space path (records returned, no LedgerValuationError); BUIDL / an unresolvable probe still refuses loudly.
- SC-6 (suite half): the §4.2 dual-run suite + IEEE micro-pin + gate-on-matrix are green on synthetic fixtures, mutation-honest, ready for Phase 80's real-adapter re-run.
</success_criteria>

<output>
Create `.planning/phases/79-pure-native-unit-core-guard-hardening/79-04-SUMMARY.md` when done.
</output>
