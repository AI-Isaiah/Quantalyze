---
phase: 79-pure-native-unit-core-guard-hardening
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/external_flows.py
  - analytics-service/services/nav_twr.py
  - analytics-service/services/deribit_txn.py
  - analytics-service/services/native_nav.py
  - analytics-service/tests/test_external_flows.py
  - analytics-service/tests/test_nav_twr.py
  - analytics-service/tests/test_deribit_txn.py
  - analytics-service/tests/test_native_nav.py
  - src/lib/complete-status-scan.test.ts
autonomous: true
requirements: [NAT-02, NAT-03, SI-01]

must_haves:
  truths:
    - "A 4-field ExternalFlow passes through _flows_to_daily_usd AND validate_flow_shape unchanged; every existing 2-arg producer/consumer is byte-identical via the defaults (currency='USD', quantity=None)"
    - "ONE USD_FAMILY frozenset {USD, USDC, USDT, EURR, DAI} lives in external_flows.py; deribit_txn._LINEAR_CURRENCIES is an alias of it (identity, not a copy)"
    - "classify_currency returns exactly three branches with USD_FAMILY winning first; it is pure and never raises; the USD_FAMILY ∩ indexable disjointness check is a separate validator that raises on overlap"
    - "UnmarkableCurrencyError / InceptionReconciliationError subclass NavReconstructionError and carry ONLY leak-safe attributes (no raw balances/quantities/USD)"
    - "A vitest source-scan fails on any NEW `=== 'complete'` exact-match status consumer in src/ outside a frozen allowlist (SI-01)"
  artifacts:
    - path: "analytics-service/services/external_flows.py"
      provides: "USD_FAMILY frozenset; ExternalFlow(utc_day_iso, usd_signed, currency='USD', quantity=None); indexed-access validate_flow_shape with currency/quantity checks"
      contains: "USD_FAMILY"
    - path: "analytics-service/services/native_nav.py"
      provides: "MarkBranch enum; classify_currency; _assert_families_disjoint; UnmarkableCurrencyError; InceptionReconciliationError"
      contains: "classify_currency"
    - path: "src/lib/complete-status-scan.test.ts"
      provides: "SI-01 frozen-allowlist source scan over src/ for exact-match complete comparisons"
      contains: "complete"
  key_links:
    - from: "nav_twr._flows_to_daily_usd (nav_twr.py:201)"
      to: "ExternalFlow 4-field shape"
      via: "indexed access flow[0], flow[1] (no positional exhaustive unpack)"
      pattern: "flow\\[0\\]"
    - from: "deribit_txn._LINEAR_CURRENCIES (deribit_txn.py:95)"
      to: "external_flows.USD_FAMILY"
      via: "alias assignment (single source of truth)"
      pattern: "USD_FAMILY"
---

<objective>
Extend the shared types the native core (79-02) will consume, WITHOUT changing any
existing behavior: `ExternalFlow` grows `(currency, quantity)` with BOTH positional
2-unpack sites fixed in the SAME commit (contract §2 — the one place the extension
is not purely additive, App A #1); `USD_FAMILY` becomes the single source of truth
in `external_flows.py` with `_LINEAR_CURRENCIES` aliasing it (§3.2); the
three-branch `classify_currency`/`MarkBranch` and the two refuse-error types land
in the new `services/native_nav.py` (§3.1, §3.4). Plus the SI-01 CI grep-guard
(P0 carry-forward from PR #591).

Purpose: SC-2, SC-3 (classifier half), SC-7 of Phase 79. Foundation wave — every
other plan depends on these types.
Output: extended `external_flows.py`; 2 indexed-unpack fixes; `native_nav.py`
(classifier + errors only — the core is 79-02); SI-01 vitest scan.

NAT-02, NAT-03, SI-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v1.9-ROADMAP.md
@.planning/STATE.md
@.planning/phase-78/P1_native_core_contract.md
@.planning/phases/79-pure-native-unit-core-guard-hardening/79-CONTEXT.md

<interfaces>
<!-- All anchors RE-VERIFIED against current main during planning. -->

From services/external_flows.py (VERIFIED):
- `class ExternalFlow(NamedTuple)` (:34-45): `utc_day_iso: str`, `usd_signed: float`.
- `validate_flow_shape(flow)` (:48-76): `day, usd = flow` 2-unpack at :63 —
  BREAKS on a 3+-field NamedTuple (`ValueError: too many values to unpack`).
- Module is stdlib + typing ONLY (purity contract, :25-28) — USD_FAMILY here keeps
  deribit_txn's purity scan green (same rationale as deribit_txn.py:48-52).

From services/nav_twr.py (VERIFIED):
- `_flows_to_daily_usd` (:188-210): `day_raw, usd_raw = flow` 2-unpack at :201 —
  the second break site. Docstring (:27-30) allows bare `(day, usd)` 2-tuples, so
  the indexed fix must tolerate plain 2-tuples too.
- `flow_coverage_gap_evidence` (:585-624): `f.utc_day_iso` attribute access at
  :623 — UNAFFECTED by added fields (do not touch).
- `NavReconstructionError` (:155-160) — base class for the new refuse errors.

From services/deribit_txn.py (VERIFIED):
- `_LINEAR_CURRENCIES = frozenset({"USDC","USDT","USD","EURR"})` (:95).
- `_INVERSE_CURRENCIES = frozenset({"BTC","ETH"})` (:102).
- import-time disjointness assert (:108-110) — RETAIN (still covers the static
  sets); the dynamic-set check is the new `_assert_families_disjoint` (G1).
- `ExternalFlow(day, usd)` producer at :670 — 2-arg construction; defaults keep it
  byte-identical (the (day, ccy)-keyed 4-field producer is Phase 80, §2.3).
- `ccxt_flows.py:295` producer — same: untouched, defaults fill (Phase 81/P3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExternalFlow (currency, quantity) + both 2-unpack fixes + USD_FAMILY promotion — RED then GREEN</name>
  <files>analytics-service/services/external_flows.py, analytics-service/services/nav_twr.py, analytics-service/services/deribit_txn.py, analytics-service/tests/test_external_flows.py, analytics-service/tests/test_nav_twr.py, analytics-service/tests/test_deribit_txn.py</files>
  <read_first>
    - services/external_flows.py (whole file — 77 lines)
    - services/nav_twr.py:188-210 (_flows_to_daily_usd), :27-30 (bare-2-tuple tolerance)
    - services/deribit_txn.py:88-110 (_LINEAR_MARGIN_MARKERS/_LINEAR_CURRENCIES/_INVERSE_CURRENCIES + assert), :180-181 (_row_is_linear consumer of _LINEAR_CURRENCIES)
    - tests/test_external_flows.py, tests/test_deribit_txn.py — existing pin style
  </read_first>
  <behavior>
    - four_field_through_both_consumers (the §2.3 pinned test): a
      `ExternalFlow("2026-01-02", -500.0, "BTC", -0.012)` passes through
      `_flows_to_daily_usd([flow])` (sums usd_signed on its day) AND
      `validate_flow_shape(flow)` (returns it unchanged) — RED today with
      `ValueError: too many values to unpack` at both sites.
    - legacy_defaults_byte_identical: `ExternalFlow(day, usd)` (2-arg, positional —
      the deribit_txn.py:670 / ccxt_flows.py:295 construction) yields
      `currency == "USD"` and `quantity is None`; `_flows_to_daily_usd` over a
      mixed list of 2-field flows and bare `(day, usd)` tuples produces the exact
      Series it produces today (assert_series_equal, check_exact).
    - validate_flow_shape_extended: rejects empty/non-upper `currency` and a
      non-finite `quantity`; accepts `quantity=None` (legacy) and finite floats.
    - usd_family_membership: `USD_FAMILY == frozenset({"USD","USDC","USDT","EURR","DAI"})`
      and `deribit_txn._LINEAR_CURRENCIES is USD_FAMILY` (identity — an alias, not
      a copy that can drift). `_row_is_linear` on a DAI-currency row returns True
      (behavior-neutral for Deribit: no DAI wallet exists there, §3.2).
    - static_disjointness_retained: importing deribit_txn still asserts
      `USD_FAMILY ∩ _INVERSE_CURRENCIES == ∅` at import time.
  </behavior>
  <action>
    RED: add the tests above (the 4-field passthrough tests fail on the 2-unpacks).
    GREEN, all in ONE commit (§2.3 — the extension and the unpack fixes are
    inseparable):
    1. `external_flows.py`: add `USD_FAMILY: frozenset[str] = frozenset({"USD",
       "USDC", "USDT", "EURR", "DAI"})` with an evidence-citation comment per §3.2
       (additions are code edits with evidence, never config/DB/venue metadata; DAI
       addition is behavior-neutral for Deribit). Extend the NamedTuple:
       `currency: str = "USD"`, `quantity: float | None = None`, with the §2.2
       field-semantics docstring: `usd_signed` keeps its exact current meaning and
       stays authoritative for the legacy path; `currency`/`quantity` are the
       native channel; invariant `usd_signed == quantity × same-day mark`
       (branch-1: `quantity == usd_signed`, mark ≡ 1.0); the native core reads
       ONLY `(utc_day_iso, currency, quantity)` and never trusts a producer-side
       `usd_signed`.
    2. `validate_flow_shape` (:63): replace `day, usd = flow` with indexed access
       `day, usd = flow[0], flow[1]`; additionally validate `flow.currency` is a
       non-empty, already-UPPERCASE str (getattr with "USD" default so bare tuples
       still validate) and `flow.quantity` is None or finite (math.isfinite) —
       shape-only, still returns the flow unchanged.
    3. `nav_twr.py:201`: replace `day_raw, usd_raw = flow` with
       `day_raw, usd_raw = flow[0], flow[1]` — tolerates 4-field flows AND the
       bare 2-tuples the core docstring allows (:27-30). NO other nav_twr change
       in this plan.
    4. `deribit_txn.py:95`: `_LINEAR_CURRENCIES = USD_FAMILY` (alias; import from
       services.external_flows — the import at :52 already exists for
       ExternalFlow). Keep the :108-110 static assert as-is (it now covers
       USD_FAMILY ∩ _INVERSE_CURRENCIES).
    Producers at deribit_txn.py:670 / ccxt_flows.py:295 are NOT touched — defaults
    keep them byte-identical (§2.3 table).
    Mutation-honesty: reverting EITHER indexed fix flips four_field_through_both_consumers
    red (too-many-values); changing a default flips legacy_defaults_byte_identical
    red; making _LINEAR_CURRENCIES a copy (== but not is) flips the alias-identity
    pin red.
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_external_flows.py analytics-service/tests/test_nav_twr.py analytics-service/tests/test_deribit_txn.py -x -q && cd analytics-service && $PY312 -m mypy --strict services/external_flows.py services/nav_twr.py services/deribit_txn.py</automated>
  </verify>
  <acceptance_criteria>
    4-field flow passes both consumption sites; ALL pre-existing tests in the three
    test files stay green (byte-identity via defaults); alias is identity; mypy
    --strict clean on all three modules.
  </acceptance_criteria>
  <done>ExternalFlow carries (currency, quantity); both 2-unpack sites indexed; one USD_FAMILY source; everything existing byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: classify_currency / MarkBranch + refuse-error types in new services/native_nav.py — RED then GREEN</name>
  <files>analytics-service/services/native_nav.py, analytics-service/tests/test_native_nav.py</files>
  <read_first>
    - contract §3.1 (branch rules), §3.4 (UnmarkableCurrencyError), §5.3 (InceptionReconciliationError), §1.1 (module purity discipline)
    - services/nav_twr.py:1-36 (module-docstring purity pattern to mirror), :155-160 (NavReconstructionError)
    - services/deribit_txn.py:104-110 (the "linear checked FIRST" disjointness rationale classify_currency mirrors)
  </read_first>
  <behavior>
    - three_branches: `classify_currency("USDC", indexable=frozenset({"BTC"}))` →
      USD_FAMILY; `("BTC", ...)` → INDEXED; `("BUIDL", ...)` → UNMARKABLE;
      lowercase input is uppercased first ("btc" → INDEXED).
    - usd_family_wins_first (the §3.1 priority pin): with a deliberately
      overlapping `indexable=frozenset({"USDC"})`, `classify_currency("USDC", ...)`
      returns USD_FAMILY (mirrors "both converters check linear FIRST",
      deribit_txn.py:104-110). Mutation: swapping branch order returns INDEXED → red.
    - classify_never_raises: junk inputs ("", weird unicode) classify (UNMARKABLE),
      never raise — pure per §3.1.
    - disjointness_validator (G1 in 79-CONTEXT): `_assert_families_disjoint(
      usd_family, indexable)` raises NavReconstructionError naming the overlapping
      currencies (currency CODES only — leak-safe) when the sets overlap; passes on
      disjoint sets. classify_currency itself never performs this check.
    - error_types: `UnmarkableCurrencyError` carries `currency`, `venue`, `reason`
      (one of "no_usd_index" | "missing_daily_marks" | "flow_quantity_missing"),
      `missing_day_count: int`; `InceptionReconciliationError` carries `currencies`
      (list[str]), `venue`, and the RELATIVE breach ratio. BOTH subclass
      NavReconstructionError (isinstance pin — the permanent worker disposition,
      deribit_txn.py:58-61). A leak-scan assertion: str(exc) and all attributes
      contain NO float that is a raw balance/quantity/USD amount (construct with
      known values; assert absent from the message).
  </behavior>
  <action>
    RED: create tests/test_native_nav.py with the tests above (module import fails —
    RED). GREEN: create `services/native_nav.py` with a module docstring mirroring
    nav_twr.py:1-36 (pure, I/O-free, stdlib + pandas + numpy only; NO logging of
    raw NAV/balance/flow values — T-73-02/T-76-03-LEAK class, §1.1). Implement:
    - `class MarkBranch(str, Enum)`: USD_FAMILY="usd_family", INDEXED="indexed",
      UNMARKABLE="unmarkable" (§3.1 verbatim).
    - `def classify_currency(ccy: str, *, indexable: AbstractSet[str],
      usd_family: AbstractSet[str] = USD_FAMILY) -> MarkBranch` — uppercase, then
      usd_family first, then indexable, else UNMARKABLE. Pure; never raises.
      Import USD_FAMILY from services.external_flows (single source).
    - `def _assert_families_disjoint(usd_family, indexable) -> None` — raises
      NavReconstructionError on overlap (called by the 79-02 core at
      classification time; G1). Docstring: replaces the static assert's coverage
      for the DYNAMIC indexable set (§3.2); the static deribit_txn assert is
      retained for the static floor.
    - `class UnmarkableCurrencyError(NavReconstructionError)` per §3.4 verbatim
      (docstring: value-gated — zero-everywhere UNMARKABLE currencies are skipped
      silently per the deribit_txn.py:282-283 precedent; attributes leak-safe,
      missing_day_count is a COUNT, never the values held).
    - `class InceptionReconciliationError(NavReconstructionError)` per §5.3
      verbatim (relative ratio only, NEVER raw residual quantities/USD).
    Do NOT implement NativeLedger / the core / the tolerances here — 79-02 owns
    those (single-concern wave; the file grows in place).
    Mutation-honesty: dropping the value-gate wording is pinned in 79-02 (the
    behavior lives in the core); here, swapping classify branch order flips
    usd_family_wins_first red; removing a leak-safe attribute or embedding a raw
    quantity in the message flips the leak-scan red; un-subclassing
    NavReconstructionError flips the isinstance pin red.
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_native_nav.py -x -q && cd analytics-service && $PY312 -m mypy --strict services/native_nav.py</automated>
  </verify>
  <acceptance_criteria>
    Three branches with USD_FAMILY-first priority; classify pure/never-raises;
    disjointness validator raises on overlap; both error types leak-safe and
    NavReconstructionError subclasses; mypy --strict clean.
  </acceptance_criteria>
  <done>native_nav.py exists with classifier + validator + refuse errors; ready for the 79-02 core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SI-01 — CI source-scan guard against fresh exact-match 'complete' status consumers</name>
  <files>src/lib/complete-status-scan.test.ts</files>
  <read_first>
    - src/lib/closed-sets.ts:193 (isComputedAnalytics — the PR #591 shared helper this guard protects)
    - Current census (grep at plan time, RE-VERIFY at execute time with `grep -rn "=== ['\\\"]complete['\\\"]" --include="*.ts" --include="*.tsx" src/`): closed-sets.ts (the helper itself), queries.ts:1279, types.ts:1291, app/strategy/[id]/page.tsx:140, app/api/verify-strategy/[id]/status/route.ts:107, app/(dashboard)/portfolios/[id]/page.tsx (3 sites), components/landing/VerificationProgress.tsx (2), components/landing/VerificationSection.tsx, components/strategy/ApiKeyManager.tsx, components/strategy/SyncProgress.tsx (2)
  </read_first>
  <behavior>
    - scan_matches_frozen_allowlist: recursively read `src/**/*.{ts,tsx}`
      (excluding `*.test.*`/`*.spec.*`), count occurrences per file of the regex
      `===\s*["']complete["']`, and assert the resulting `{file: count}` map
      EQUALS a frozen allowlist constant (regenerated from the execute-time
      census). A NEW site (new file, or a new occurrence in an allowlisted file)
      fails with a message directing the author to `isComputedAnalytics()`
      (closed-sets.ts:193) instead of a fresh exact-match — the laundering surface
      PR #591 closed. A REMOVED site also fails (stale allowlist must shrink) so
      the list can never rot into an over-allow.
    - scanner_unit_pin (mutation-honest without touching real files): the scan is
      factored as a pure function over `(path, source)` pairs; a unit test feeds a
      synthetic source containing `status === "complete"` and asserts it is
      counted — neutering the regex (e.g. requiring `computation_status`) flips
      this red.
  </behavior>
  <action>
    Create `src/lib/complete-status-scan.test.ts` (vitest — it runs inside the
    existing sharded `frontend` CI gate, making it a CI guard by construction; no
    workflow YAML change). Use node `fs`/`path` recursion from the repo's `src/`
    root (resolve via `__dirname`); exclude test/spec files (they legitimately pin
    exact strings). Freeze the allowlist as `{ "src/lib/closed-sets.ts": 1, ... }`
    built from the execute-time grep census — verify each entry is a status
    comparison before freezing (they are allowlisted AS-IS, including
    non-computation statuses like sync/verification state machines; the guard's
    job is only to stop NEW sites landing unnoticed, roadmap SC-7). Keep the
    file-read synchronous and the test single — no parallel-suite contention.
    Mutation-honesty: adding `x === 'complete'` to any non-test src/ file flips
    the scan red (assert via the unit-pin, not by writing repo files).
  </action>
  <verify>
    <automated>npx vitest run src/lib/complete-status-scan.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    Scan green against the frozen census; unit-pin proves a synthetic new site is
    detected; test participates in the standard vitest CI shards (no config edit
    needed — it matches the existing include glob).
  </acceptance_criteria>
  <done>SI-01 guard live: no fresh exact-match 'complete' consumer can land without reddening CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| producer flows → pure core | untrusted (currency, quantity) fields now cross into the flow contract |
| repo source → CI gate | SI-01 defends the status-string surface against silent re-widening |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering | ExternalFlow.quantity | mitigate | validate_flow_shape rejects non-finite quantity; the core re-checks via _coerce_float (79-02); branch-2 quantity=None refused, never back-solved (§2.2) |
| T-79-02 | Information Disclosure | UnmarkableCurrencyError / InceptionReconciliationError | mitigate | leak-safe attributes only (currency codes, reason, counts, relative ratio); leak-scan test pins message contents |
| T-79-03 | Tampering (integrity) | USD_FAMILY drift | mitigate | alias-identity pin (`is USD_FAMILY`) + retained import-time disjointness assert + membership test; additions require evidence-cited code edits (§3.2) |
| T-79-04 | Repudiation | fresh `=== 'complete'` consumers | mitigate | SI-01 frozen-allowlist scan in the blocking `frontend` CI gate |
| T-79-SC | Tampering | pip/npm installs | accept | this phase installs NO packages (stdlib+pandas+numpy; vitest already present) — no supply-chain surface |
</threat_model>

<verification>
- Setup: `$PY312` = a python3.12 venv pinned to `analytics-service/requirements.txt`
  (e.g. `uv venv --python 3.12 && uv pip install -r analytics-service/requirements.txt pytest mypy`)
  — NEVER the shared local `.venv` (B-mypy CI-pin discipline).
- `$PY312 -m pytest analytics-service/tests/ -x -q` fully green (no pre-existing pin regresses).
- `$PY312 -m mypy --strict` clean on external_flows.py, nav_twr.py, deribit_txn.py, native_nav.py.
- `npx vitest run src/lib/complete-status-scan.test.ts --no-file-parallelism` green.
</verification>

<success_criteria>
- SC-2: ExternalFlow carries (currency, quantity); both 2-unpack sites fixed same-commit; everything existing byte-identical via defaults.
- SC-3 (classifier half): three-branch classify_currency + one USD_FAMILY source + leak-safe refuse-error types exist.
- SC-7: SI-01 scan blocks fresh exact-match 'complete' consumers in CI.
</success_criteria>

<output>
Create `.planning/phases/79-pure-native-unit-core-guard-hardening/79-01-SUMMARY.md` when done.
</output>
