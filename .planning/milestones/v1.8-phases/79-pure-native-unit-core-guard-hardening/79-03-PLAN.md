---
phase: 79-pure-native-unit-core-guard-hardening
plan: 03
type: tdd
wave: 2
depends_on: [79-01]
files_modified:
  - analytics-service/services/nav_twr.py
  - analytics-service/services/metrics.py
  - analytics-service/tests/test_nav_twr.py
  - analytics-service/tests/test_broker_dailies.py
  - analytics-service/tests/test_metrics.py
autonomous: true
requirements: [DQ-03]

must_haves:
  truths:
    - "An interior NaN break (retained returns on BOTH sides) compounds ONLY the maximal contiguous suffix after the LAST break AND raises twr_chain_broken; the bridged product is provably DIFFERENT in the fixture"
    - "The CAGR annualization window and the compounded total_return window are the SAME days — both derive from ONE `_last_interior_break_suffix(returns)` helper in nav_twr.py (no metrics-local break-detector); a multi-interior-break fixture reddens any divergence"
    - "The no-NaN path is bit-identical to the old Π(1+r)−1 product (SC-4 clean path); leading-NaN-only compounds the suffix with NO new flag"
    - "cumulative_twr is DELETED — no two coexisting cumulative semantics; no services/ caller computes Π(1+returns.dropna()) inline (source-scanned)"
    - "twr_chain_broken rides complete_with_warnings by the NAV_TWR_GUARD_KEYS registry's own construction (nav_twr.py:139-143)"
    - "gap_fill_daily_returns never converts guard-NaN'd EXISTING days to 0.0 (frozen by a pinned test)"
  artifacts:
    - path: "analytics-service/services/nav_twr.py"
      provides: "_last_interior_break_suffix(returns) -> pd.Series (the single suffix-boundary source); cumulative_twr_segmented(returns) -> tuple[float, dict[str, bool]] consuming it; twr_chain_broken on NavTWRMeta + NAV_TWR_GUARD_KEYS + _build_nav_meta; flag merged in reconstruct_nav_and_twr"
      contains: "twr_chain_broken"
    - path: "analytics-service/services/metrics.py"
      provides: "total_return via cumulative_twr_segmented and _cagr_index via the shared _last_interior_break_suffix — one suffix-boundary source, no inline dropna-bridge / no forked break-detector"
      contains: "cumulative_twr_segmented"
  key_links:
    - from: "reconstruct_nav_and_twr (nav_twr.py:627)"
      to: "_build_nav_meta flags mapping"
      via: "cumulative_twr_segmented(returns)[1] merged into flags before meta build (ONE break-detection semantics)"
      pattern: "twr_chain_broken"
    - from: "metrics.py total_return (:468)"
      to: "nav_twr.cumulative_twr_segmented"
      via: "import + call replacing the inline (1 + returns.dropna()).prod()"
      pattern: "cumulative_twr_segmented"
    - from: "metrics.py _cagr_index (:486)"
      to: "nav_twr._last_interior_break_suffix"
      via: "the CAGR elapsed-days basis reads the SAME suffix Series the number compounds — one boundary source, no metrics-local break reimpl"
      pattern: "_last_interior_break_suffix"
---

<objective>
Kill the silent chain-bridging (contract §6): `cumulative_twr` (nav_twr.py:371-377)
does `returns.dropna()` and compounds ACROSS a genuine capital discontinuity —
the break is flagged but the NUMBER silently bridges it (§6.1). Replace it with
`cumulative_twr_segmented` (interior break ⇒ suffix-only + `twr_chain_broken`),
delete the old function in the SAME change (App A #4 — two coexisting cumulative
semantics is the "surface conflicts, don't average them" violation), append the
flag to the guard-key registry so it rides `complete_with_warnings` by
construction, and migrate the ONE production inline-bridging caller
(`metrics.py:468` — grep-verified: `cumulative_twr` itself has ZERO production
callers; the §6.2 "no inline Π(1+returns.dropna())" scan is only satisfiable by
migrating `total_return`; 79-CONTEXT G5).

This wave PRECEDES the native core (79-02) because contract §1.3 step 6 requires
the core's meta assembly to include "the §6 chain-break key" — building the
hardening first means the core is written once, complete (79-CONTEXT wave note).

Purpose: SC-4 of Phase 79 (DQ-03).
Output: segmented cumulative + guard key + legacy-core wiring + metrics migration
+ the three pins (§6.3 fixture, source-scan, gap-fill freeze).

DQ-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/v1.9-ROADMAP.md
@.planning/STATE.md
@.planning/phase-78/P1_native_core_contract.md
@.planning/phases/79-pure-native-unit-core-guard-hardening/79-CONTEXT.md

<interfaces>
From services/nav_twr.py (VERIFIED):
- `cumulative_twr(returns) -> float` (:371-377): `retained = returns.dropna()`;
  empty → NaN; else `Π(1+retained) − 1`. The bridge.
- `NavTWRMeta` (:106-132) + `NAV_TWR_GUARD_KEYS` (:145-152) — the one-line-edit
  registry ("adding a guard ... propagates by construction", :139-143): the tuple
  is iterated by transforms._merge_status_meta, analytics_runner's lift
  (analytics_runner.py:~1765 `for _guard_key in NAV_TWR_GUARD_KEYS`), and
  job_worker's broker pre-stamp. A pin already exists:
  `set(NAV_TWR_GUARD_KEYS) <= set(NavTWRMeta.__annotations__)`.
- `_build_nav_meta` (:380-406) — explicit per-key assignment for mypy.
- `reconstruct_nav_and_twr` (:627-700): `returns, flags = chain_linked_twr(...)`
  at :691, flags merged (:698-699), `_build_nav_meta(flags)` at :700.
- `apply_flow_coverage_terminus` (:508-538) — NaNs LEADING days AFTER the core
  returns (76-04 wiring), so leading NaN at core scope = day-0 guard only.
- forbidden-substitution source-scan precedent: tests/test_nav_twr.py:427
  `test_no_forbidden_denominator_guards` (the pattern §6.2 cites via :359-361).

From services/broker_dailies.py (VERIFIED):
- `gap_fill_daily_returns` (:118-127): `reindex(full_idx, fill_value=0.0)` —
  fill_value only fills NEWLY-CREATED labels; existing guard-NaN days stay NaN
  (already compliant, §6.2 — the pin FREEZES this against a `.fillna(0)` refactor).

From services/metrics.py (VERIFIED):
- `total_return = _safe_float((1 + returns.dropna()).prod() - 1)` (:468) — THE
  production inline bridge. `_cagr_index = returns.dropna().index` (:486) spans
  the whole window; CAGR reuses total_return (:484-489).
- `returns_for_chart = returns.fillna(0)...` (:460) — CHART-only bridging
  (equity-curve continuity), explicitly out of §6 scope (it is presentation, not
  the headline compounded figure). Do NOT touch.
- test files: tests/test_metrics.py (unit), test_metrics_minigolden.py /
  test_metrics_parity.py (goldens — clean fixtures, must stay green untouched).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — §6.3 interior-break fixture + segmented semantics; GREEN — cumulative_twr_segmented, delete cumulative_twr</name>
  <files>analytics-service/services/nav_twr.py, analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - services/nav_twr.py:371-377 (cumulative_twr), :345-368 (_guard_denominator — what makes a day NaN)
    - tests/test_nav_twr.py:262, :278, :503, :661-681, :1166-1172 (every existing cumulative_twr call site — the migration census; all are TESTS)
  </read_first>
  <behavior>
    - interior_break_suffix_only (§6.3 verbatim): a returns fixture with a
      mid-history `negative_nav_guard` NaN flanked by valid returns on BOTH sides:
      (a) flags contain `twr_chain_broken: True`; (b) the float equals the product
      over the maximal contiguous suffix AFTER the LAST break exactly, and the
      fixture asserts `suffix_product != bridged_product` (so a regression to
      bridging fails on the NUMBER, not just the flag); (c) when threaded through
      reconstruct_nav_and_twr (Task 2) the status is complete_with_warnings.
    - clean_path_bit_identical: no-NaN series → float bit-equal to
      `float((1.0 + returns).prod() - 1.0)` (the old expression), NO flag —
      the SC-4 clean path.
    - leading_nan_only: NaNs only at the head (a DQ-02 terminus segment /
      day-0 guard shape) → compound the post-NaN suffix, flags == {} (the
      terminus/DQ-01 machinery already flagged the cause — no double-flag).
    - multiple_interior_breaks: suffix after the LAST break is compounded (the
      anchored, trustworthy segment — it chains back from the real venue
      terminal), single flag.
    - all_nan_terminal: no day survived → (NaN, {}) — same terminal case as today
      (:375-376). NEVER returns NaN when a valid suffix exists.
    - deletion: `from services.nav_twr import cumulative_twr` raises ImportError
      (pytest.raises around importlib import or a getattr assert) — no two
      coexisting semantics.
  </behavior>
  <action>
    RED first: add the tests; they fail (function absent). GREEN: implement
    `cumulative_twr_segmented(returns: pd.Series) -> tuple[float, dict[str, bool]]`
    in nav_twr.py beside the old site, per the §6.2 docstring contract verbatim
    (no-NaN → old product bit-identical; leading-only → suffix, no flag; interior
    → last-break suffix + {"twr_chain_broken": True}; never stitches, never NaN
    when a suffix exists, NaN only when nothing survived). DELETE `cumulative_twr`
    in the SAME commit and migrate the test-only call sites (census above): the
    semantics tests keep their exact expected VALUES (clean fixtures compound
    identically; the :661-681 terminus tests compare suffix products — re-express
    with `cumulative_twr_segmented(...)[0]`); the :1166-1172 flow-dominated test
    has ONE interior NaN — its expected value is already the retained product of a
    series whose only NaN is interior... verify per-fixture whether the NaN is
    interior or boundary and assert the NEW honest semantics (suffix-only) with an
    explicit comment where the number changes — a changed expectation here is the
    DQ-03 behavior change landing, not a regression; document each in the diff.
    Mutation-honesty (contract §6.3): re-adding a `.dropna()` bridge flips
    interior_break_suffix_only red on BOTH the flag and the number
    (suffix ≠ bridged is asserted in-fixture); compounding the PREFIX instead of
    the suffix flips multiple_interior_breaks red.
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_nav_twr.py -x -q && cd analytics-service && $PY312 -m mypy --strict services/nav_twr.py</automated>
  </verify>
  <acceptance_criteria>
    Segmented semantics green on all five behaviors; cumulative_twr gone; every
    remaining test_nav_twr.py pin green (clean-fixture values unchanged;
    interior-NaN expectations consciously updated with in-diff justification).
  </acceptance_criteria>
  <done>One cumulative semantics exists: suffix-honest, flagged, bit-identical on clean series.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: twr_chain_broken → NavTWRMeta + NAV_TWR_GUARD_KEYS + legacy-core wiring</name>
  <files>analytics-service/services/nav_twr.py, analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - services/nav_twr.py:106-152 (NavTWRMeta + registry + its :139-143 by-construction note), :380-406 (_build_nav_meta), :691-700 (flag merge point in reconstruct_nav_and_twr)
    - services/analytics_runner.py:~1750-1768 (the NAV_TWR_GUARD_KEYS iteration that lifts guard keys — read-only, NOT modified: the registry propagates by construction)
  </read_first>
  <behavior>
    - meta_key_rides_registry: `"twr_chain_broken" in NAV_TWR_GUARD_KEYS` and the
      existing `set(NAV_TWR_GUARD_KEYS) <= set(NavTWRMeta.__annotations__)` pin
      still holds — by :139-143's design this alone propagates the key through
      transforms._merge_status_meta, the analytics_runner lift, and the job_worker
      pre-stamp (NO edits in those files; assert by the registry pin, not by
      touching consumers).
    - core_sets_flag: a reconstruct_nav_and_twr fixture engineering a mid-history
      negative reconstructed NAV (guard-NaN flanked by valid days) returns meta
      with `twr_chain_broken: True` AND `computation_status_hint ==
      "complete_with_warnings"` (the hint was ALREADY warnings via
      negative_nav_guard — the key is additive, the status does not change class).
    - clean_and_leading_unchanged: a clean fixture and a day-0-guard-only fixture
      carry NO twr_chain_broken key — byte/status-identical meta to today (SC-4
      clean path; leading NaN is not an interior break).
  </behavior>
  <action>
    1. Add `twr_chain_broken: bool` to NavTWRMeta (total=False) with a §6.2
       comment (interior chain break — the cumulative figure compounds only the
       post-break suffix; rides the same complete_with_warnings channel, NO
       parallel status). Append `"twr_chain_broken"` to NAV_TWR_GUARD_KEYS (:152)
       — the one-line registry edit. Add the explicit
       `if flags.get("twr_chain_broken"): ...` assignment in _build_nav_meta
       (mirror the existing keys).
    2. In `reconstruct_nav_and_twr`, after `returns, flags = chain_linked_twr(...)`
       (:691) merge `cumulative_twr_segmented(returns)[1]` into flags (ONE
       break-detection semantics — the same function that computes the honest
       number decides brokenness; no forked detector). The float result is
       discarded here (the core returns the daily series; the cumulative consumer
       is metrics, Task 3).
    Note the sanctioned behavior delta: interior-break accounts gain an additive
    meta key (their status hint is ALREADY complete_with_warnings because a DQ-01
    guard fired for any interior NaN) — this is DQ-03's whole point; clean
    accounts are byte-identical.
    Mutation-honesty: removing the registry append flips meta_key_rides_registry
    red; detecting breaks with a second inline implementation instead of
    cumulative_twr_segmented is caught at review by the key_links pattern; a
    leading-NaN fixture gaining the flag flips clean_and_leading_unchanged red.
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_nav_twr.py -x -q && cd analytics-service && $PY312 -m mypy --strict services/nav_twr.py</automated>
  </verify>
  <acceptance_criteria>
    twr_chain_broken present in registry + meta on interior-break fixtures only;
    all pre-existing byte-identity pins (74-01, 76-x, 77-x) stay green; mypy clean.
  </acceptance_criteria>
  <done>The chain break is loud end-to-end via the registry, with zero consumer-file edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: metrics total_return migration + source-scan + gap-fill freeze pin</name>
  <files>analytics-service/services/metrics.py, analytics-service/tests/test_metrics.py, analytics-service/tests/test_broker_dailies.py, analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - services/metrics.py:460-490 (returns_for_chart / total_return / _cagr_index / cagr block, incl. the NEW-C02-05 and TWR-05 comments)
    - services/broker_dailies.py:118-127 (gap_fill_daily_returns reindex semantics)
    - tests/test_metrics.py — fixture style; tests/test_metrics_minigolden.py + test_metrics_parity.py exist as clean-fixture goldens (must stay green UNTOUCHED — they prove the clean path is bit-identical)
  </read_first>
  <behavior>
    - total_return_suffix_honest: compute_all_metrics over a series with an
      interior NaN → `total_return` equals the post-break suffix product, NOT the
      dropna-bridged product (both computed in-test; asserted different — the
      §6.3 mutation shape at the metrics surface).
    - cagr_window_consistent: with total_return now suffix-based, the CAGR
      elapsed-days basis derives from the SINGLE shared suffix source —
      `_last_interior_break_suffix(returns) -> pd.Series` in nav_twr.py, the SAME
      helper `cumulative_twr_segmented` compounds (no metrics-local reimpl, no
      second break-detector — the §6.2 anti-pattern). `_cagr_index` becomes that
      Series' index (first retained day AFTER the last interior break → last day),
      NOT the full-window `returns.dropna().index`. Proven on a MULTI-interior-
      break fixture (≥2 interior breaks): the CAGR window must span only the
      post-last-break suffix, matching the compounded window exactly, or the
      annualization is a mixed-basis fabrication. Clean series: identical to today
      (bit-equal — the suffix IS the whole series).
    - shared_suffix_single_source: `_last_interior_break_suffix(returns)` is the
      ONE function that decides the retained suffix; `cumulative_twr_segmented`
      compounds it and metrics' `_cagr_index` reads its `.index` — a source-scan
      (the §6.2 forbidden-substitution pattern) asserts metrics.py contains NO
      second `returns.dropna()`-based suffix/break derivation feeding total_return
      or _cagr_index (only the allowlisted window KPIs survive).
    - clean_bit_identical: a no-NaN fixture produces byte-identical total_return
      + cagr to the pre-change values (golden suites untouched and green).
    - no_inline_bridge_scan (§6.2 source-scan, the tests/test_nav_twr.py:427
      forbidden-substitution pattern): read the SOURCE of services/metrics.py and
      services/nav_twr.py (importlib.util.find_spec / __file__ read; do NOT
      importlib.reload) and assert no `dropna()).prod(` / `dropna()).cumprod(`
      token computes a headline cumulative — allowlist the intentional survivors
      by exact token: metrics.py's rolling-window `.apply(lambda x: (1 + x).prod()`
      (~:528, per-window stats), mtd/ytd/tail `.add(1).prod()` slices (mtd ~:616,
      ytd ~:617, three_month ~:620 — window KPIs, not THE cumulative chain), and
      the chart-only cumprod on fillna(0) (~:467). Line numbers RE-DERIVE at
      execute-time via the prescribed grep (`grep -n "prod(" services/metrics.py`)
      — do NOT trust these literals. The forbidden class is exactly
      `(1 + returns.dropna()).prod()` as the headline total_return.
    - gap_fill_freeze (§6.2 pinned test, in test_broker_dailies.py): a returns
      Series with an interior NaN day AND a missing calendar day →
      gap_fill_daily_returns fills the MISSING label with 0.0 but the existing
      NaN day STAYS NaN (a `.fillna(0)` refactor would silently un-break the
      chain — this pin makes that refactor red).
  </behavior>
  <action>
    1. nav_twr.py: extract the retained-suffix selection Task 1's
       `cumulative_twr_segmented` computes inline into a SINGLE private
       `_last_interior_break_suffix(returns: pd.Series) -> pd.Series` (the
       maximal contiguous non-NaN suffix after the LAST interior break; the whole
       series when there is no interior break; empty when nothing survives) and
       re-point `cumulative_twr_segmented` to consume it — ONE boundary source,
       no forked detector. metrics.py: import BOTH `cumulative_twr_segmented` and
       `_last_interior_break_suffix` from services.nav_twr. Replace :468 with
       `total_return = _safe_float(cumulative_twr_segmented(returns)[0])` and
       derive `_cagr_index` (:486) as `_last_interior_break_suffix(returns).index`
       — the SAME days the number compounds (no metrics-local dropna/break
       reimpl). Do NOT wire the returned flags into metrics' output — status
       flags ride NavTWRMeta from the core (Task 2); metrics only needs the
       honest number (keep the :463-466 NEW-C02-05 comment updated: the one NaN
       policy is now "segmented suffix", chart stays fillna(0)).
    2. Add the source-scan test + the gap-fill freeze pin per the behaviors.
    3. Update the :469-489 TWR-05 comment minimally (surgical: only the sentences
       describing the dropna basis) — do not restructure the block.
    Mutation-honesty: reverting :468 to the inline dropna-prod flips BOTH
    total_return_suffix_honest AND no_inline_bridge_scan red; a
    `gap_fill_daily_returns` refactor to `.fillna(0)` flips gap_fill_freeze red;
    deriving CAGR days from the full `returns.dropna().index` while compounding
    the suffix flips cagr_window_consistent red on the MULTI-interior-break
    fixture (a single-break fixture could mask a boundary off-by-one, so the pin
    uses ≥2 interior breaks); giving metrics its OWN suffix/break derivation
    instead of consuming `_last_interior_break_suffix` flips
    shared_suffix_single_source (the source-scan reddens on the second detector).
  </action>
  <verify>
    <automated>$PY312 -m pytest analytics-service/tests/test_metrics.py analytics-service/tests/test_metrics_minigolden.py analytics-service/tests/test_metrics_parity.py analytics-service/tests/test_broker_dailies.py analytics-service/tests/test_nav_twr.py -x -q && cd analytics-service && $PY312 -m mypy --strict services/metrics.py</automated>
  </verify>
  <acceptance_criteria>
    Interior-break total_return is suffix-honest; CAGR basis matches the
    compounded window; BOTH metrics golden suites green UNTOUCHED (clean path
    bit-identical); source-scan + gap-fill pins green and mutation-honest.
  </acceptance_criteria>
  <done>No production caller bridges a chain break; the pins make regressions red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard-NaN'd daily series → headline KPIs | a broken chain crossing into presented cumulative figures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-05 | Tampering (integrity) | cumulative bridging | mitigate | segmented suffix-only compounding; in-fixture suffix≠bridged assertion; source-scan bans the inline dropna-prod class |
| T-79-06 | Repudiation | silent break in stored status | mitigate | twr_chain_broken rides NAV_TWR_GUARD_KEYS → complete_with_warnings by registry construction (:139-143) |
| T-79-07 | Tampering | gap_fill un-breaking NaN days | mitigate | frozen pin: reindex fills only NEW labels; a fillna(0) refactor reddens |
| T-79-SC | Tampering | pip installs | accept | no packages installed — no supply-chain surface |
</threat_model>

<verification>
- Setup: `$PY312` per 79-01 (python3.12 venv pinned to analytics-service/requirements.txt; never the shared .venv).
- `$PY312 -m pytest analytics-service/tests/ -x -q` fully green.
- `$PY312 -m mypy --strict analytics-service/services/nav_twr.py analytics-service/services/metrics.py` clean.
- Metrics golden suites (minigolden + parity) pass with ZERO fixture edits — the clean-path bit-identity proof.
</verification>

<success_criteria>
- SC-4 (phase 79): cumulative_twr_segmented replaces cumulative_twr (deleted, one semantics); interior break ⇒ suffix + twr_chain_broken riding complete_with_warnings; no-NaN path bit-identical; leading-NaN unflagged; source-scan + gap-fill pins in place and mutation-honest.
</success_criteria>

<output>
Create `.planning/phases/79-pure-native-unit-core-guard-hardening/79-03-SUMMARY.md` when done.
</output>
