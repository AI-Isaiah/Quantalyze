---
phase: 80-deribit-native-adapter-production-switch
plan: 04
type: execute
wave: 2
depends_on: [80-01]
files_modified:
  - analytics-service/services/native_nav.py
  - analytics-service/scripts/deribit_native_parity.py
  - analytics-service/tests/test_native_nav.py
autonomous: false
requirements: [INCEPT-01, ACC-03]

must_haves:
  truths:
    - "Every inception-gated real Deribit key re-derives through the native path and the §5 gate reconciles it to pre-history ≈0 within the live-tuned tolerance (gate ii); any key carrying returns_denominator_config (Zavara) is CARVED OUT of that assertion — it bypasses the gate by construction — and its Bug-B coupling invariant (exclude_spot_extraction == (denominator_config is not None)) is verified instead"
    - "ALL THREE tolerance knobs — INCEPTION_ABS_TOL_USD, INCEPTION_REL_TOL, INCEPTION_NATIVE_DUST_REL — are calibrated against the real keys with recorded evidence; tightening is expected, any loosening carries a written per-key/per-currency justification (FLOW_DOM_RATIO precedent)"
    - "An old-vs-new golden parity panel (classify_delta reuse, frozen oracle OLD side) shows USD-native accounts UNCHANGED, coin accounts MOVED with zero UNEXPLAINED, and the founder signs the coin movers including LTP068"
    - "LTP068's magnitude is category-valid: the inflated ~109,000% CAGR (driven by the −$480,782 phantom start capital / omitted translation integral) is gone; the ACC-02 final sign-off + v1.7 close-out trigger rides this gate"
    - "The parity panel audits the COMPLETE enumerated set of existing Deribit-venue accounts (not a hardcoded 3): audited set == live-DB-enumerated set with zero un-audited accounts, and every genuinely all-USD-family account is byte-identical old-vs-new — this completeness assertion, NOT the §5 inception gate, is the D8 closure that no existing account is silently rescaled"
    - "The live Deribit strategies rows carry asset_class='crypto' (√365), and /qa browser-verifies the re-derived Deribit factsheet on authed prod shows the category-valid returns the founder signed"
  artifacts:
    - path: "analytics-service/scripts/deribit_native_parity.py"
      provides: "read-only old-vs-new parity panel over the FULL enumerated Deribit key set (classify_delta buckets, frozen oracle OLD, live native NEW)"
      contains: "classify_delta"
    - path: "analytics-service/tests/test_native_nav.py"
      provides: "boundary regression pinning the calibrated tolerance constants (widening past the boundary reddens)"
      contains: "INCEPTION"
  key_links:
    - from: "deribit_native_parity.py"
      to: "services.parity_diff.classify_delta + scripts.golden_parity frozen oracle"
      via: "OLD = frozen anchor-to-today oracle; NEW = live native combine_native_ledger; each account bucketed"
      pattern: "classify_delta"
    - from: "gate ii tuning"
      to: "native_nav.py three tolerance constants (:178/:180/:199)"
      via: "the ONLY sanctioned native_nav.py edit (widened D2 carve-out: THREE knobs)"
      pattern: "INCEPTION_NATIVE_DUST_REL"
---

<objective>
The LIVE, human-gated wave — the ONLY genuinely pending Phase-80 work. **Reconciled
reality (80-RESEARCH, 2026-07-10): the production switch is ALREADY MERGED (PR #598,
`eb8e357e`, v0.38.2.0) and — because Railway auto-deploys the analytics worker on green
main — presumptively ALREADY LIVE.** Every Deribit account is routing through
`combine_native_ledger` with the inception gate active at its UNTUNED defaults. Gates
(ii) and (iii) are therefore no longer merge blockers — they are the LIVE ACCEPTANCE
gates that close the phase: calibrate the tolerances against the real keys, prove the
switch correct over the COMPLETE enumerated account set, and obtain the founder sign-off
(incl. LTP068) that absorbs ACC-02 and triggers the v1.7 close-out.

This plan is a **runbook plus exact gate assertions**. It touches live credentials and
production evidence; no checkpoint auto-advances. Its only code outputs are (a) the
read-only parity-panel script and (b) the sanctioned three-constant tolerance calibration
+ boundary regression — both shipped via feature branch + /ship, never direct to main.

Purpose: INCEPT-01 (gate ii) + ACC-03 (gate iii, absorbing ACC-02 + the v1.7 close-out
trigger) + the real-KEY tier of ship gate (i).
Output: tuned tolerances (with evidence), the parity panel script + panel record, the
founder sign-off record, and the v1.7/ACC-02 close-out trigger.

INCEPT-01, ACC-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-deribit-native-adapter-production-switch/80-CONTEXT.md
@.planning/phases/80-deribit-native-adapter-production-switch/80-RESEARCH.md
@.planning/phase-78/P1_native_core_contract.md
@.planning/phases/80-deribit-native-adapter-production-switch/80-01-SUMMARY.md

<interfaces>
<!-- Corrected anchors (80-RESEARCH drift table, verified 2026-07-10). Re-derive via
     grep -n at execution. -->

services/native_nav.py (LANDED core — untouched except the sanctioned carve-out):
- THE THREE TOLERANCE KNOBS (widened D2 carve-out — this wave's ONLY native_nav.py edit):
  `INCEPTION_ABS_TOL_USD = 1.00` (:178), `INCEPTION_REL_TOL = 1e-4` (:180), and the NEW
  third knob `INCEPTION_NATIVE_DUST_REL = 1e-4` (:199, per-currency native dust floor,
  throughput-relative; its in-code comment says calibration "is NOT claimed here" —
  observed production dust ratio ≈2e-6, the constant sits ~50x above). All three are
  constant tuning per the FLOW_DOM_RATIO precedent — no arithmetic/logic edits.
- `_assert_inception_reconciled` (:661); `InceptionReconciliationError(currencies, venue,
  breach_ratio)` (:137) — carries the relative breach ratio only (leak discipline).

services/broker_dailies.py:168 `combine_native_ledger(ledger, indexable, *,
denominator_config=None)`:
- `denominator_config is not None` (Zavara allocated-capital, v1.8) BYPASSES
  `reconstruct_native_nav_and_twr` AND the §5 inception gate entirely (:211-216).
- Bug-B COUPLING INVARIANT: `ledger.exclude_spot_extraction == (denominator_config is
  not None)` — the job worker derives both from the same config; verify per Zavara key.

services/parity_diff.py:158 `classify_delta(old_returns, new_returns, *, old_metrics,
new_metrics, has_flows, rtol, ...)` → UNCHANGED / REANNUALIZATION / FLOW_MOVED /
UNEXPLAINED.
scripts/golden_parity.py:43 `old_anchor_to_today_returns(...)` (the FROZEN OLD oracle),
:141 `gate_account(...)`; fixtures `tests/fixtures/golden_parity/oracle_input.json` +
`oracle_pre73_expected.json`.
scripts/deribit_acceptance.py — the read-only live re-crawl harness this wave's panel
script extends (argparse --account STRATEGY_ID:KEY_INDEX:LABEL:WINDOW_START:WINDOW_END;
SUPABASE_URL + SUPABASE_SERVICE_KEY; _OK_STATUSES = {complete, complete_with_warnings}).

asset_class (research A1, handed over by 80-01 Task 3): code + migration verified;
the LIVE prod-row check (`strategies.asset_class='crypto'` for every Deribit key) is
THIS wave's obligation (gate iii step 6).

Live ops (memory): Railway analytics one-off — `railway ssh "cd /app && python -m
scripts.X"`; env key SUPABASE_SERVICE_KEY. Prod Supabase project khslejtfbuezsmvmtsdn.
The 3 real Deribit keys include the coin account LTP068 (v1.7 P72 canary; today's
category-INVALID headline: CAGR inflated to ~109,000% via the −$480,782 phantom start
capital / omitted translation integral Σ_c ∫ B_c·dP_c).

WATCH (mig-038 poison, D7 defect (1), research A4): "any failed_final → failed" in
sync_strategy_analytics_status poisons retry-after-failure. SI-02 (mig 20260708120000,
runner-owned `computation_warned`) is LANDED and deployed with #598 — but during repeated
live re-runs still verify the warned status survives each run, and watch railway logs
for a mig-038 permanent-fail of an otherwise-recoverable key.

NEVER print secrets (Deribit keys, SUPABASE_SERVICE_KEY). Panel output is relative
ratios + bucket labels only — no raw NAV/balance/residual figures (D6).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the read-only parity-panel driver (scripts/deribit_native_parity.py)</name>
  <files>analytics-service/scripts/deribit_native_parity.py</files>
  <action>
    Build the panel driver the checkpoints below run. It does NOT exist yet (verified —
    `scripts/` has deribit_acceptance.py / golden_parity.py only). Model it on
    `scripts/deribit_acceptance.py` (same argparse --account spec, same env contract,
    same _OK_STATUSES) and REUSE the landed primitives — never re-implement:
    OLD side = the frozen anchor-to-today oracle (`scripts/golden_parity.py:
    old_anchor_to_today_returns` + `gate_account`); NEW side = a live read-only native
    re-derivation via `build_deribit_native_ledger` → `combine_native_ledger` (threading
    the account's `returns_denominator_config` exactly as job_worker does, so a Zavara
    key exercises its REAL branch); bucket each account via `parity_diff.classify_delta`.
    Two hard properties: (1) READ-ONLY — zero production-row writes (no upserts to
    strategy_analytics/dailies; assert by construction: the script imports no persistence
    helpers); (2) LEAK DISCIPLINE — stdout carries account label, bucket, relative
    ratios, and completion/refusal codes ONLY, never raw NAV/balance/flow/residual values.
    Also emit, per account: whether the inception gate applied (denominator_config is
    None) or was bypassed (Zavara), and on InceptionReconciliationError the breach_ratio
    — these feed Task 3's calibration. Include a `--enumerate` mode that lists ALL
    Deribit-venue accounts from the DB (strategies joined to api_keys where
    exchange='deribit') so gate iii's D8 completeness assertion is mechanical.
    Add a unit test only if a pure helper warrants it; the script's live behavior is
    validated at the checkpoints (this is a runbook tool, not a shipped feature path).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -c "import ast,sys; tree=ast.parse(open('scripts/deribit_native_parity.py').read()); names=[n.name for node in ast.walk(tree) if isinstance(node,(ast.Import,)) for n in node.names]+[node.module for node in ast.walk(tree) if isinstance(node,ast.ImportFrom) and node.module]; assert not any('persist' in (m or '') for m in names), 'panel must be read-only'; print('imports OK')" && grep -n "classify_delta" scripts/deribit_native_parity.py && grep -n "combine_native_ledger" scripts/deribit_native_parity.py && grep -n "denominator_config" scripts/deribit_native_parity.py && grep -n "enumerate" scripts/deribit_native_parity.py</automated>
  </verify>
  <done>Script exists, imports only read/derive primitives (no persistence), reuses classify_delta + combine_native_ledger + the frozen oracle, threads denominator_config, and offers --enumerate. Committed on a feature branch (never main) per workflow.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2: LIVE observe + re-derive the full enumerated Deribit key set</name>
  <what-built>The production switch is ALREADY LIVE (merged #598; Railway auto-deploys on
  green main) — so this checkpoint first OBSERVES what production has already done under
  the switch at default tolerances, then runs the read-only panel re-derivation that
  produces the gate-ii/iii evidence. SI-02 landed IN #598, so it is deployed with the
  switch — verify, don't assume.</what-built>
  <how-to-verify>
    1. PRECONDITION: 80-01 SUMMARY green (suites 23+90 pinned; anchors verified).
    2. OBSERVE FIRST (the switch may already have re-derived keys): check
       `railway deployment list` confirms the worker runs a post-eb8e357e build, then
       inspect `strategy_analytics.computation_status` for every Deribit key. Record any
       InceptionReconciliationError / UnmarkableCurrencyError PERMANENT failures already
       stamped by the live switch at default tolerances — these are gate-ii calibration
       inputs, and (WATCH, mig-038 / research A4) a `failed_final` here poisons retry:
       note any key needing a manual status reset AFTER tolerances are tuned.
    3. VERIFY SI-02 DEPLOYED: migration `20260708120000_sync_status_failed_final_bounce`
       present in prod `supabase_migrations.schema_migrations`; spot-check a warned key's
       `computation_warned` marker exists. If absent, STOP — a status bounce during these
       re-runs could launder the complete_with_warnings evidence gate iii must observe.
    4. ENUMERATE (D8 input + Zavara carve-out input): run the panel's `--enumerate` via
       `railway ssh "cd /app && python -m scripts.deribit_native_parity --enumerate"` —
       record EVERY Deribit-venue account AND which carry a non-null
       `returns_denominator_config` (research A2 expects at most one — the Zavara key;
       if more, record all: each is inception-gate-EXEMPT by construction).
    5. RE-DERIVE: run the panel (read-only) over the FULL enumerated set. Record per key:
       completed / InceptionReconciliationError (+ breach_ratio) /
       UnmarkableCurrencyError (investigate — an unindexable nonzero currency) / Zavara
       bypass (+ Bug-B coupling invariant verified: exclude_spot_extraction ==
       (denominator_config is not None)).
    6. Verify the warned status of any complete_with_warnings key SURVIVED the re-run
       (no mig-038 launder): computation_status vs data_quality_flags spot-check.
  </how-to-verify>
  <resume-signal>Type "keys re-derived" with the per-key outcome (complete / warned /
  which refusal + breach_ratio / Zavara-bypass), the enumeration count, and the
  denominator_config carve-out list — or describe a blocker.</resume-signal>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: SHIP GATE (ii) — calibrate the THREE inception tolerances against the real keys</name>
  <decision>Set INCEPTION_ABS_TOL_USD, INCEPTION_REL_TOL, AND INCEPTION_NATIVE_DUST_REL
  to values under which every INCEPTION-GATED real key (i.e., the enumerated set MINUS
  the denominator_config/Zavara carve-outs, which bypass the gate by construction)
  reconciles to a ~zero pre-history balance per currency, valued at inception-day
  marks.</decision>
  <context>
    The calibration surface is THREE knobs, not two — the CONTEXT's D2 carve-out is
    hereby WIDENED (research risk #4) to include the per-currency native dust floor
    `INCEPTION_NATIVE_DUST_REL` (native_nav.py:199), whose in-code comment explicitly
    disclaims live calibration ("green NOT claimed here"; observed production dust ratio
    ≈2e-6 vs the 1e-4 default). All three currently sit at contract defaults
    ($1.00 / 1e-4 / 1e-4), tuned only on synthetic fixtures. This is the sanctioned
    INCEPT-01 constant-tuning job (FLOW_DOM_RATIO precedent, nav_twr.py:64-65) — the
    ONLY native_nav.py edit permitted this phase; zero arithmetic/logic changes.
    Expectation from the §5.1 empirical observation: BTC reconciles to ~0.048-coin-dust
    ratio territory; USDC/ETH exact. Tightening after calibration is expected; LOOSENING
    any of the three requires written evidence (fee-rounding dust across two independent
    measurements — reported equity vs summed ledger — or, for the dust floor, a measured
    throughput-relative rounding ratio). A key that will not reconcile within a
    defensible tolerance is a REAL signal (missing rows / mis-classified type / wrong
    scope) — investigate, do NOT loosen to pass it. GATE-II SCOPE RULE: a Zavara
    denominator_config key CANNOT be cited as gate-ii evidence in either direction — it
    never executes `_assert_inception_reconciled`; its correctness claim is the Bug-B
    coupling invariant (Task 2 step 5) + the gate-iii parity bucket.
  </context>
  <options>
    <option id="tighten">
      <name>Tighten (expected)</name>
      <pros>All inception-gated keys reconcile with margin; the gate stays a real check; matches the §5.1 empirical dust and the ≈2e-6 observed dust ratio</pros>
      <cons>None if the keys genuinely reconcile — this is the anticipated outcome</cons>
    </option>
    <option id="hold-defaults">
      <name>Hold defaults ($1.00 / 1e-4 / 1e-4)</name>
      <pros>Contract-specified; no change to justify</pros>
      <cons>Only defensible if the real-key residuals sit comfortably inside the defaults with margin; the dust floor's own comment says its live green is unproven</cons>
    </option>
    <option id="loosen-with-evidence">
      <name>Loosen (requires evidence)</name>
      <pros>Admits a key whose residual is provably fee-rounding dust across two independent measurements (or a measured throughput-relative rounding ratio for the dust floor)</pros>
      <cons>Must carry a written justification per key + currency + knob; loosening to mask missing rows is FORBIDDEN — investigate instead</cons>
    </option>
  </options>
  <gate_assertion>
    After tuning: every INCEPTION-GATED key (Zavara carve-outs excluded and listed) passes
    `_assert_inception_reconciled` with the chosen constants; the residual-vs-tolerance
    margin per key is recorded (relative ratios only — no raw residuals, leak discipline
    D6). Update tests/test_native_nav.py with a boundary regression pinning the chosen
    THREE constants (mutation-honesty neuter: widening any knob past its boundary must
    redden). Ship the constants + test via feature branch → /ship → PR (never direct
    main); after merge+deploy, reset any mig-038-poisoned key (Task 2 step 2) and confirm
    it re-derives clean. GATE (ii) is green ONLY when all inception-gated keys pass live.
  </gate_assertion>
  <resume-signal>Select: tighten, hold-defaults, or loosen-with-evidence — with the
  per-key residual margins, the Zavara carve-out list, and (if loosening) the written
  justification per knob.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SHIP GATE (iii) — golden parity panel + founder sign-off (incl. LTP068)</name>
  <what-built>A read-only old-vs-new golden parity panel (Task 1's script) over the
  COMPLETE enumerated set of existing Deribit accounts (NOT a hardcoded 3):
  OLD = the frozen anchor-to-today oracle (scripts/golden_parity.py, flow-blind),
  NEW = the live native combine_native_ledger. Each account bucketed via
  parity_diff.classify_delta into UNCHANGED / REANNUALIZATION / FLOW_MOVED / UNEXPLAINED.</what-built>
  <how-to-verify>
    0. COMPLETENESS ENUMERATION (the D8 hard gate step — run FIRST). The switch routes
       EVERY Deribit account through the native core with NO per-account flag, so any
       account outside the audited set would be silently rescaled unseen. ASSERT: the
       panel's audited account set == Task 2's `--enumerate` live-DB set, ZERO un-audited.
       If enumeration returned more accounts than the panel ran, EXPAND --account to
       cover every one before proceeding; each is classify_delta-bucketed exactly like
       the original 3.
    1. Run the panel (read-only) over the FULL enumerated set with the Task-3-tuned
       constants deployed; inspect the bucket per account.
    2. ASSERT: every USD-native account is UNCHANGED (the live corollary of ship gate i).
       If a nominally-USD-native account MOVED, confirm it holds coin DUST (D8 — a
       legitimate parity-panel MOVED case); otherwise it is a regression that BLOCKS the
       phase close.
    3. ASSERT: every coin-margined account MOVED with the delta EXPLAINED
       account-by-account (FLOW_MOVED or a founder-accepted category-correction) and
       ZERO accounts bucket UNEXPLAINED — including every account added by step 0.
    4. REAL-KEY SC-4 SHADOW (the remaining tier of ship gate i, D8 defense-in-depth):
       for EACH enumerated account confirmed genuinely all-USD-family (zero non-USD-family
       value), assert byte-identical stored dailies old-vs-new (check_exact=True series +
       byte-equal meta). A silent rescale of a supposedly-USD-native account reddens HERE
       — note the §5 inception gate is NOT this backstop (a silently-rescaled account can
       still roll to ≈0); the completeness enumeration + this byte-identity shadow are the
       D8 closure.
    5. LTP068 specifically: confirm the magnitude is category-valid — the inflated
       ~109,000% CAGR is GONE (no −$480,782 phantom start capital; the omitted translation
       integral Σ_c ∫ B_c·dP_c is now carried). Present OLD (category-invalid) vs NEW
       side by side for the founder.
    6. ASSET_CLASS LIVE SPOT-CHECK (handed over from 80-01 Task 3 / research A1): for
       every enumerated Deribit strategy row, `asset_class = 'crypto'` — so the live
       headline Sharpe/vol annualize on √365, not a silent √252. A 'traditional' row here
       is a real metric bug: fix the row (data fix, migration-consistent) before the
       founder reads the headline numbers.
    7. /qa BROWSER VERIFICATION (the verification half of the founder sign-off): run /qa
       against authed prod (qa-demo or founder account) on the re-derived Deribit
       factsheet(s) — confirm the rendered returns/CAGR are the category-valid NEW numbers
       (LTP068 no longer ~109,000%), no stale cached OLD figures, no console/render errors
       on the factsheet panels.
    8. FOUNDER SIGN-OFF: founder reviews the coin movers (incl. LTP068 OLD→NEW) and signs
       off on the accounts that SHOULD move. Record who/when/per-account verdict in the
       SUMMARY.
    9. On sign-off: this ABSORBS the ACC-02 final founder sign-off and TRIGGERS the v1.7
       close-out (audit → complete → cleanup). Note the trigger in the SUMMARY; do not
       run the v1.7 cleanup here.
  </how-to-verify>
  <gate_assertion>
    GATE (iii) green iff: audited set == enumerated set (zero un-audited); zero
    UNEXPLAINED buckets; every USD-native account UNCHANGED with byte-identical stored
    dailies old-vs-new (or a confirmed-dust MOVED); every Deribit strategy row
    asset_class='crypto'; /qa confirms the rendered factsheets show the NEW numbers;
    every coin mover founder-signed incl. LTP068 category-valid. The completeness
    enumeration + byte-identity shadow — NOT the inception gate — is the D8 closure.
    Phase 80 closes ONLY with gates (i: 80-01 pinned + step-4 real-key shadow) + (ii) +
    (iii) all green.
  </gate_assertion>
  <resume-signal>Type "founder signed: <per-account verdicts, incl. LTP068 OLD→NEW>" to
  approve, or describe the UNEXPLAINED / regressed account that blocks the close.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live credentials → read-only re-derivation | the panel must NOT write production rows; read-only like deribit_acceptance |
| tolerance tuning → the gate's meaning | loosening any of the THREE knobs to mask a real reconciliation failure defeats the gate |
| Zavara bypass → gate-ii evidence | a denominator_config key never runs the inception gate — citing it as gate-ii evidence is false assurance |
| repeated re-runs → warned status | a status bounce (mig-038) could launder gate-iii evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-13 | Tampering (integrity) | three-knob inception tolerance tuning | mitigate | tighten-or-justify policy per knob; loosening to pass a non-reconciling key forbidden — investigate; per-key margin recorded; boundary regression reddens on widening |
| T-80-14 | Repudiation | gate-iii evidence launder | mitigate | SI-02 deployed with #598 (verified Task 2 step 3); warned status re-checked after each re-run; WATCH mig-038 "any failed_final → failed" poison (A4) |
| T-80-15 | Information Disclosure | parity panel / tuning output | mitigate | relative ratios + bucket labels + codes only; no raw residuals/balances; scrub any live error before display; never print keys/SERVICE_KEY |
| T-80-16 | Elevation of Privilege | live re-derivation writes | mitigate | read-only panel script (no persistence imports — AST-checked in Task 1); Railway one-off with SUPABASE_SERVICE_KEY, verified read-only |
| T-80-17 | Spoofing (false evidence) | Zavara carve-out mis-scoping gate ii | mitigate | enumerate denominator_config keys FIRST (Task 2 step 4); gate-ii assertion runs over the inception-gated set only; Zavara keys verified via the Bug-B coupling invariant + gate-iii bucket instead |
| T-80-18 | Tampering (silent metric bug) | asset_class='traditional' Deribit row → silent √252 | mitigate | live prod-row spot-check (gate iii step 6) before the founder reads headline numbers; 80-01 verified the code+migration tier |
| T-80-SC | Tampering | pip installs | accept | no packages installed (stdlib+pandas+numpy present) |
</threat_model>

<verification>
- SHIP GATE (i): 80-01 pinned the synthetic + real-adapter tiers; the real-KEY stored-dailies
  tier closes at Task 4 step 4.
- SHIP GATE (ii): every inception-gated real key passes `_assert_inception_reconciled` with
  the tuned THREE constants; Zavara keys carved out + coupling-invariant-verified; the
  boundary regression in tests/test_native_nav.py is green and reddens when any knob widens
  past its boundary.
- SHIP GATE (iii): panel audited set == enumerated set, zero UNEXPLAINED, USD-native
  UNCHANGED + byte-identity shadow, asset_class='crypto' live-confirmed, /qa
  browser-confirmed, coin movers founder-signed incl. LTP068 category-valid.
- SI-02 verified deployed; warned status survives the re-runs (no mig-038 launder).
- Constants + panel script + boundary test land via feature branch → /ship → PR; never
  direct to main.
- This wave is autonomous:false — no checkpoint auto-advances; each requires explicit human sign-off.
</verification>

<success_criteria>
- Every inception-gated real Deribit key re-derives through the LIVE native path; inception
  gate green within the live-calibrated three-knob tolerances (gate ii, evidence recorded);
  Zavara denominator_config keys carved out and coupling-invariant-verified.
- Golden parity panel over the COMPLETE enumerated set: USD-native UNCHANGED (+ byte-identity
  shadow), coin accounts MOVED + explained, zero UNEXPLAINED, founder-signed incl. LTP068
  (gate iii).
- LTP068 magnitude category-valid (the ~109,000% CAGR / −$480,782 phantom is gone); ACC-02
  final sign-off absorbed; v1.7 close-out triggered.
- asset_class='crypto' confirmed on the live rows; /qa confirms the authed-prod factsheet
  renders the NEW numbers.
- Phase 80 closes ONLY with gates (i)+(ii)+(iii) all green.
</success_criteria>

<output>
Create `.planning/phases/80-deribit-native-adapter-production-switch/80-04-SUMMARY.md` when done —
record the tuned three-knob tolerances + per-key margins, the Zavara carve-out list + coupling
verdicts, the parity buckets over the full enumerated set, the asset_class spot-check, the /qa
result, the founder sign-off (who/when/per-account incl. LTP068 OLD→NEW), and the v1.7/ACC-02
close-out trigger.
</output>
