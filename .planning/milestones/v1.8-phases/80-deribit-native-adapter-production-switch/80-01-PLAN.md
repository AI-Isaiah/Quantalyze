---
phase: 80-deribit-native-adapter-production-switch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [NAT-04, NAT-05, SC-4, SI-02]

must_haves:
  truths:
    - "The SC-4 dual-run identity suite is green against the REAL build_deribit_native_ledger adapter under $PY312 (23 tests, incl. test_dual_run_bit_exact_real_adapter, test_dust_account_excluded_from_identity, test_same_family_swap_is_noop_in_coalesced_usd_bucket)"
    - "The native-core / broker-dailies / golden-parity suites are green under $PY312 (90 tests, incl. test_ltp068_shape_flow_moved and test_deribit_one_path_shape)"
    - "The Deribit job branch (job_worker.py: if venue == \"deribit\":) reaches combine_native_ledger with NO per-account dispatch flag and never calls combine_realized_and_funding inside the branch"
    - "Every Phase-80 deliverable function exists at the corrected anchor from the 80-RESEARCH drift table: txn_rows_to_native_daily, build_deribit_native_ledger, fetch_deribit_native_account_state, combine_native_ledger, the SI-02 migration 20260708120000 + Part-4 regression"
    - "All THREE inception-tolerance knobs sit at their untuned defaults (INCEPTION_ABS_TOL_USD=1.00, INCEPTION_REL_TOL=1e-4, INCEPTION_NATIVE_DUST_REL=1e-4) — recorded as the 80-04 gate-ii calibration input, NOT asserted as calibrated"
    - "The asset_class='crypto' annualization path for API-key (Deribit) strategies is wired at the code level: migration 20260709130000 backfills crypto WHERE api_key_id IS NOT NULL, analytics_runner selects asset_class and threads periods_per_year_for_asset_class, and crypto → 365 / traditional|None → 252"
  artifacts:
    - path: "analytics-service/tests/test_native_nav_sc4_identity.py"
      provides: "SHIP GATE (i) synthetic + real-adapter tier evidence (landed in PR #598)"
      contains: "check_exact=True"
    - path: "supabase/tests/test_sync_status_preserves_warnings.sql"
      provides: "SI-02 failed_final-bounce regression (Part 4, mig 20260708120000)"
      contains: "failed_final"
  key_links:
    - from: "services/job_worker.py deribit branch"
      to: "broker_dailies.combine_native_ledger"
      via: "the production switch (no per-account flag) — grep-asserted, not rebuilt"
      pattern: "combine_native_ledger"
    - from: "analytics_runner.py"
      to: "metrics.periods_per_year_for_asset_class"
      via: "strategy_row asset_class → √365 for crypto (research risk #5)"
      pattern: "periods_per_year_for_asset_class"
---

<objective>
VERIFY-LANDED wave. The ENTIRE autonomous code scope originally planned as 80-01…80-03
(plus SI-02) shipped out-of-band in **PR #598 (`eb8e357e`, v0.38.2.0, merged 2026-07-09)**
— see 80-RESEARCH.md (headline finding) and 80-PATTERNS.md (dominant finding). This plan
writes ZERO code. It mechanically pins the landed reality as the phase's merge evidence
for ship gate (i, synthetic + real-adapter tier) and SC-6/SI-02, so the only genuinely
pending work (80-04 live gates ii/iii) starts from a verified baseline.

Any executor emitting a build/edit task against `deribit_txn.py`, `deribit_ingest.py`,
`broker_dailies.py`, `native_nav.py`, or `job_worker.py` in this wave is IN ERROR — the
landed code has evolved BEYOND the original plan spec (pnl_basis, exclude_spot_extraction,
denominator_config, assert_balance_identity) and rebuilding would fork shipped, tested
invariants (violates D2 zero-core-edits and Rule 3 surgical changes).

Purpose: convert the stale 80-01…80-03 build framing into verification evidence
(NAT-04, NAT-05, SC-4 gate-i non-live tiers, SI-02).
Output: 80-01-SUMMARY.md recording every green suite + grep anchor; no file edits.

NAT-04, NAT-05, SC-4, SI-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/80-deribit-native-adapter-production-switch/80-CONTEXT.md
@.planning/phases/80-deribit-native-adapter-production-switch/80-RESEARCH.md

<interfaces>
<!-- Corrected anchors (80-RESEARCH drift table, verified 2026-07-10). HINTS — re-derive
     via grep -n at execution; files grew ~2× since the CONTEXT was written. -->

services/deribit_txn.py:
- `txn_rows_to_native_daily(rows, *, pnl_basis=..., exclude_spot_extraction=...)` :1501 —
  returns plain `dict[str, dict[str, float]]` (D9 honored: pandas-pure module, AST guard).
- `deribit_dated_external_flows_usd` :895 — already 4-field `(day,ccy)` ExternalFlow.
- `_NATIVE_CASH_BEARING_TYPES` :583+ — the SANCTIONED `swap` reclassification (a
  documented divergence from D#5 "verbatim"; USD-bucket no-op pinned by
  `test_same_family_swap_is_noop_in_coalesced_usd_bucket`).

services/deribit_ingest.py:
- `build_deribit_native_ledger` :1725; `fetch_deribit_native_account_state` :1346 (D5
  one-read); `assert_ledger_complete` :1903.

services/broker_dailies.py:
- `combine_native_ledger(ledger, indexable, *, denominator_config=None)` :168 — the
  Zavara `denominator_config` branch (:211) BYPASSES `reconstruct_native_nav_and_twr`
  and the §5 inception gate by design (gate-ii carve-out, handled in 80-04).

services/job_worker.py:
- `if venue == "deribit":` :2008 → native build :2145 → `combine_native_ledger` :2188 →
  `NavReconstructionError` PERMANENT disposition :2273. Legacy
  `combine_realized_and_funding` survives only for OTHER venues (:2451).
- SI-02 runner-owned `computation_warned` clear on terminal fail :2052.

services/native_nav.py (UNTOUCHED core, D1/D2):
- `reconstruct_native_nav_and_twr(ledger, *, indexable_currencies, venue="")` :579.
- THREE tolerance knobs (80-04 gate-ii surface): `INCEPTION_ABS_TOL_USD = 1.00` :178,
  `INCEPTION_REL_TOL = 1e-4` :180, `INCEPTION_NATIVE_DUST_REL = 1e-4` :199 (NEW third
  knob; in-code comment disclaims "green NOT claimed here"). `_assert_inception_reconciled` :661.

asset_class (#597, research risk #5):
- migration `supabase/migrations/20260709130000_strategies_asset_class.sql` — column +
  CHECK + backfill `SET asset_class='crypto' WHERE api_key_id IS NOT NULL`.
- `services/metrics.py:43` `periods_per_year_for_asset_class` (crypto→365, else 252).
- `services/analytics_runner.py` selects `asset_class` (:1193, :2128) and threads it
  (:1656, :2256).

$PY312 = `analytics-service/.venv/bin/python` (Python 3.12.13, CI-pinned). System
python3 is 3.14 — NEVER use it. pytest flag: `-p no:cacheprovider`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the landed test suites green under $PY312 (gate-i non-live tiers)</name>
  <files>(none — verification only)</files>
  <action>
    From `analytics-service/`, run the landed suites with the CI-pinned venv and record
    exact pass counts in the SUMMARY. Expected (research-verified 2026-07-10): SC-4
    identity suite 23 passed; native/broker/parity suites 90 passed. Named tests that
    MUST appear in the passing set: `test_dual_run_bit_exact_real_adapter` (ship gate i,
    real-adapter tier), `test_dust_account_excluded_from_identity` (D8),
    `test_same_family_swap_is_noop_in_coalesced_usd_bucket` (sanctioned swap-reclass
    divergence from D#5), `test_ltp068_shape_flow_moved` (gate-iii primitive),
    `test_deribit_one_path_shape` (D3 no-flag route). Also run
    `tests/test_job_worker_deribit.py` and `tests/test_deribit_ingest.py` (disposition +
    adapter coverage; exit 0 required, counts recorded). If ANY test fails: STOP and
    escalate — do NOT patch code in this wave (a failure means main drifted since the
    2026-07-10 research pass and the phase needs re-research, per D2).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_native_nav_sc4_identity.py -q -p no:cacheprovider && .venv/bin/python -m pytest tests/test_native_nav.py tests/test_broker_dailies.py tests/test_golden_parity.py -q -p no:cacheprovider && .venv/bin/python -m pytest tests/test_job_worker_deribit.py tests/test_deribit_ingest.py -q -p no:cacheprovider</automated>
  </verify>
  <done>All commands exit 0; SC-4 suite reports 23 passed and the native/broker/parity run reports 90 passed (or higher — never lower; a lower count is a regression to escalate); the five named tests are all in the passing set.</done>
</task>

<task type="auto">
  <name>Task 2: Grep-assert the production switch + landed anchors + SI-02</name>
  <files>(none — verification only)</files>
  <action>
    Mechanically confirm the landed reality at the corrected anchors (re-derive line
    numbers via `grep -n`; treat research numbers as hints):
    1. Deliverable functions exist: `def txn_rows_to_native_daily` (deribit_txn.py),
       `def build_deribit_native_ledger` and `def fetch_deribit_native_account_state`
       (deribit_ingest.py), `def combine_native_ledger` (broker_dailies.py).
    2. Production switch wired one-path (D3): the `if venue == "deribit":` branch in
       job_worker.py reaches `combine_native_ledger(` BEFORE any
       `combine_realized_and_funding(` occurrence — assert via the python slice check in
       <verify> (the legacy combine survives only for other venues). Additionally assert
       NO per-account dispatch flag exists (comment-filtered absence grep).
    3. SI-02 landed BEFORE any live re-run (D#8/SC-6): migration
       `20260708120000_sync_status_failed_final_bounce.sql` exists; the Part-4
       failed_final-bounce regression is present in
       `supabase/tests/test_sync_status_preserves_warnings.sql`; the runner-owned
       `computation_warned` clear exists in job_worker.py. (The SQL regression executes
       in the CI sql-tests gate — record its presence + the CI gate name, do not run it
       against a live DB here.)
    4. Record the THREE gate-ii tolerance knobs and their current DEFAULT values from
       native_nav.py (`INCEPTION_ABS_TOL_USD` 1.00, `INCEPTION_REL_TOL` 1e-4,
       `INCEPTION_NATIVE_DUST_REL` 1e-4) in the SUMMARY as the 80-04 calibration input.
       Defaults are EXPECTED here — this task must NOT fail on them and must NOT edit them
       (the D2 carve-out edit belongs exclusively to 80-04 gate ii).
  </action>
  <verify>
    <automated>cd analytics-service && grep -n "def txn_rows_to_native_daily" services/deribit_txn.py && grep -n "def build_deribit_native_ledger" services/deribit_ingest.py && grep -n "def fetch_deribit_native_account_state" services/deribit_ingest.py && grep -n "def combine_native_ledger" services/broker_dailies.py && .venv/bin/python -c "src=open('services/job_worker.py').read(); i=src.index('if venue == \"deribit\":'); j=src.index('combine_realized_and_funding(', i); assert 'combine_native_ledger(' in src[i:j], 'deribit branch does not route native before legacy combine'; print('one-path OK')" && test "$(grep -v '^[[:space:]]*#' services/job_worker.py | grep -ciE 'per[_-]?account.*(flag|dispatch)|native_(enabled|flag)')" -eq 0 && grep -n "INCEPTION_ABS_TOL_USD\|INCEPTION_REL_TOL\|INCEPTION_NATIVE_DUST_REL" services/native_nav.py && cd .. && ls supabase/migrations/20260708120000_sync_status_failed_final_bounce.sql && grep -n "failed_final" supabase/tests/test_sync_status_preserves_warnings.sql | head -3 && grep -n "computation_warned" analytics-service/services/job_worker.py | head -1</automated>
  </verify>
  <done>Every grep returns a hit at (approximately) the drift-table anchor; the python one-path slice check prints "one-path OK"; the comment-filtered dispatch-flag absence grep counts 0; the SI-02 migration + Part-4 regression + runner-owned marker are all present; the three tolerance defaults are recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the asset_class='crypto' annualization wiring for Deribit keys (code level)</name>
  <files>(none — verification only)</files>
  <action>
    Research risk #5 / assumption A1: a missing or 'traditional' asset_class would
    silently annualize Deribit on √252 — orthogonal to native math but it would corrupt
    the LIVE headline Sharpe/vol the 80-04 founder review inspects. Verify the CODE-LEVEL
    wiring that makes every API-key (hence Deribit) strategy crypto/√365:
    1. Migration `20260709130000_strategies_asset_class.sql` adds the column with CHECK
       ('crypto','traditional') and backfills `SET asset_class = 'crypto' WHERE
       api_key_id IS NOT NULL` — so every existing broker-key strategy (all supported
       exchanges are crypto venues, incl. deribit) is crypto by migration.
    2. `analytics_runner.py` SELECTs `asset_class` on both the CSV path (:1193) and the
       broker path (:2128) and threads `periods_per_year_for_asset_class(...)` into
       `compute_all_metrics` (:1656, :2256).
    3. `periods_per_year_for_asset_class`: crypto → 365; 'traditional' and None → 252
       (the fail-safe default is √252 — which is exactly WHY the live-row spot-check
       matters; see below).
    LIMITATION (record in SUMMARY, do not attempt here): this proves the code + migration,
    NOT the actual prod rows (a row inserted between the #597 code deploy and the
    migration apply, or manually flipped, could still be 'traditional'). The LIVE prod-row
    spot-check (`SELECT id, asset_class FROM strategies WHERE api_key_id IS NOT NULL AND
    <deribit venue>`) is DEFERRED to 80-04 gate iii step 6 — this wave is autonomous and
    the executor has no prod-DB access.
  </action>
  <verify>
    <automated>grep -n "asset_class = 'crypto'" supabase/migrations/20260709130000_strategies_asset_class.sql && grep -n "api_key_id IS NOT NULL" supabase/migrations/20260709130000_strategies_asset_class.sql && grep -n "periods_per_year_for_asset_class" analytics-service/services/analytics_runner.py && grep -n "asset_class" analytics-service/services/analytics_runner.py | grep -i "select" && cd analytics-service && .venv/bin/python -c "from services.metrics import periods_per_year_for_asset_class as f; assert f('crypto')==365 and f('traditional')==252 and f(None)==252; print('annualization wiring OK: crypto=365, traditional/None=252')"</automated>
  </verify>
  <done>Migration backfill + CHECK present; analytics_runner selects and threads asset_class on both paths; the python assertion prints OK; the SUMMARY records the deferred live-row spot-check as an explicit 80-04 gate-iii step-6 obligation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification output → planning ledger | test/grep output is the phase's merge evidence; it must not leak raw financial values |
| local .venv → CI truth | local greens must reflect the CI-pinned environment, not a drifted venv (A3) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01 | Repudiation | verify-only wave claiming "landed" falsely | mitigate | every claim backed by a pytest exit-0 or grep -n hit recorded verbatim in the SUMMARY; a failure STOPS the wave (escalate, never patch) |
| T-80-02 | Information Disclosure | pytest failure output on financial fixtures | mitigate | suites run with -q; on failure record test IDs + codes only, never raw NAV/balance values (leak discipline D6) |
| T-80-03 | Tampering | executor "fixing" landed code to make a check pass | mitigate | files_modified is EMPTY; any code edit in this wave violates D2/Rule 3 — hard STOP instruction in every task |
| T-80-SC | Tampering | pip installs | accept | no packages installed this phase (research: "No new packages") |
</threat_model>

<verification>
- All three tasks' automated commands exit 0 under $PY312 (`analytics-service/.venv/bin/python`, 3.12.13).
- Zero file modifications (`git status --porcelain` clean apart from the SUMMARY under .planning/, which is gitignored/local).
- SUMMARY records: suite counts (23 / 90 / job-worker+ingest counts), all grep anchors as re-derived, the three tolerance defaults, and the two obligations handed to 80-04 (live-row asset_class spot-check; gate-ii calibration of THREE knobs).
</verification>

<success_criteria>
- Ship gate (i) synthetic + real-adapter tiers pinned green (the real-KEY stored-dailies tier is 80-04's).
- NAT-04 / NAT-05 / SI-02 verified LANDED at the corrected anchors; production switch confirmed one-path with no per-account flag.
- asset_class code-level wiring confirmed; live-row check explicitly deferred to 80-04.
- No code was written or edited.
</success_criteria>

<output>
Create `.planning/phases/80-deribit-native-adapter-production-switch/80-01-SUMMARY.md` when done —
record pass counts, re-derived anchors, tolerance defaults, and the deferred 80-04 obligations.
</output>
