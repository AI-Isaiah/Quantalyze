---
phase: 80-deribit-native-adapter-production-switch
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
superseded: true
superseded_by: "PR #598 (eb8e357e, v0.38.2.0, merged 2026-07-09) — verified by 80-01"
requirements: [NAT-05, SC-4]

must_haves:
  truths: []
  artifacts: []
  key_links: []
---

# SUPERSEDED — DO NOT EXECUTE

The entire scope of this plan (`combine_native_ledger`, the Deribit job-path production
switch with NO per-account flag, the typed `NavReconstructionError` disposition, and the
SC-4 dual-run identity suite against the REAL adapter) **landed out-of-band in PR #598
(`eb8e357e`, v0.38.2.0, merged 2026-07-09)** — see `80-RESEARCH.md` and `80-PATTERNS.md`.

Current locations (verified 2026-07-10):
- `combine_native_ledger` — `analytics-service/services/broker_dailies.py:168`
  (note: gained a `denominator_config` kwarg — the Zavara allocated-capital branch that
  BYPASSES the §5 inception gate; 80-04 gate ii carves those keys out)
- Job route — `services/job_worker.py:2008` (`if venue == "deribit":`) → native build
  :2145 → combine :2188 → PERMANENT disposition :2273
- SC-4 suite — `analytics-service/tests/test_native_nav_sc4_identity.py` (23 passed,
  incl. `test_dual_run_bit_exact_real_adapter`, D8 dust exclusion, swap-noop pin)

Executing this plan's original build tasks would collide with shipped, tested code —
forbidden by locked decision D2 and Rule 3 (surgical changes).

**Verification evidence for this scope lives in the 80-01 VERIFY-LANDED wave**
(`80-01-PLAN.md` Tasks 1–2). The remaining SC-4 tier — the real USD-native KEY's stored
dailies byte-identical old-vs-new — is a LIVE assertion owned by **80-04 gate iii step 4**.

<tasks>
<!-- intentionally empty — superseded; see 80-01 (verification) and 80-04 (live real-key tier) -->
</tasks>
