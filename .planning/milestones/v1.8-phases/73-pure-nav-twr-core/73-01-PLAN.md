---
phase: 73-pure-nav-twr-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/nav_twr.py
  - analytics-service/tests/test_nav_twr.py
autonomous: true
requirements: [TWR-01, TWR-02, DQ-01]

must_haves:
  truths:
    - "Given (daily_pnl, anchor_nav), the core reconstructs a per-day NAV series backward (NAV_{t-1}=NAV_t-pnl_t-F_t) that a numpy-pinned oracle reproduces to fp precision"
    - "Chain-linked daily TWR r_t=(NAV_t-NAV_{t-1}-F_t)/NAV_{t-1} and cumulative Pi(1+r)-1 are computed with flow in the numerator, not the denominator"
    - "same-day multi-flow, day-0 flow, zero-NAV interior day, and partial-window inputs each produce the correct series (zero-NAV day breaks the chain-link, never divides)"
    - "dust (<$1000), negative, and flow-dominated (|F| >= NAV_{t-1}) denominators each break the chain-link for that day and raise complete_with_warnings via ReturnsComputationMeta - never a fabricated number"
    - "with external_flows=[] and open_unrealized_usd=0.0 the returns Series is byte-identical to today's trades_to_daily_returns_with_status daily_pnl branch for an estimated_start>0 account"
    - "nav_twr.py contains no clamp/floor/max(...,floor)/replace(0,...)/clip/nonzero-fillna on a NAV denominator (source-scan enforced)"
  artifacts:
    - path: "analytics-service/services/nav_twr.py"
      provides: "Pure I/O-free backward NAV reconstruction + chain-linked TWR + fail-loud denominator guards"
      contains: "def reconstruct_nav_and_twr"
      min_lines: 120
    - path: "analytics-service/tests/test_nav_twr.py"
      provides: "numpy-oracle parity, mutation/revert-proof, edge-case, DQ-guard, source-scan, and F=0 byte-identity tests"
      contains: "def test_zero_flow_byte_identical"
      min_lines: 150
  key_links:
    - from: "analytics-service/services/nav_twr.py::reconstruct_nav_and_twr"
      to: "NavTWRMeta (nav_twr.py subclass of ReturnsComputationMeta, total=False)"
      via: "returns (pd.Series, meta) with computation_status_hint"
      pattern: "computation_status_hint"
    - from: "analytics-service/services/nav_twr.py"
      to: "deribit_txn.py::_row_utc_day"
      via: "shared UTC-day bucketing for flows and pnl"
      pattern: "_row_utc_day|_utc_day"
---

<objective>
Build the new pure, I/O-free `analytics-service/services/nav_twr.py`: backward daily-NAV
reconstruction from the exchange anchor, true chain-linked time-weighted returns with the
end-of-day flow convention, and fail-loud guards on every NAV denominator. Prove it byte-identical
to today's honest return path on flow-less input so Phase 74 can wire callers safely.

Purpose: Kill the silent NAV/base-substitution bug class at its root by replacing anchor-to-today
with a reconstruction that can never fabricate a base. This phase is additive and revert-proof:
nothing here touches production data paths (callers are wired in Phase 74).

Output: `services/nav_twr.py` (new module) + `tests/test_nav_twr.py` (new test file), both green
in the CI-3.12 venv, covering TWR-01, TWR-02, DQ-01, and the SC-4 F=0 byte-identity pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Interpreter (MANDATORY — from 73-VALIDATION.md):** the local `.venv` is Python 3.14 and
SIGSEGVs at pytest collection in native pandas tslibs. Run every test against the CI-matching
3.12 venv:
`PY312=/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python`
If that path no longer exists, recreate it: `uv venv --python 3.12 <path> && <path>/bin/python -m pip ...`
or `uv pip sync analytics-service/requirements.txt analytics-service/requirements-dev.txt` into a
3.12/3.13 venv. NEVER run pandas-importing tests on the local 3.14 interpreter. Coverage gate runs in CI.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/73-pure-nav-twr-core/73-RESEARCH.md
@.planning/phases/73-pure-nav-twr-core/73-VALIDATION.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the live codebase 2026-07-05. Use directly. -->

The current honest return contract to preserve (analytics-service/services/transforms.py):
  class ReturnsComputationMeta(TypedDict):
      used_heuristic_capital: bool
      balance_error: bool
      computation_status_hint: str   # "complete" | "complete_with_warnings"
  # daily_pnl branch (L120-176): parse ISO8601 utc -> groupby date sum ->
  #   _DUST_BALANCE_THRESHOLD = 1000.0
  #   estimated_start = account_balance - daily_pnl.sum()
  #   initial_capital = estimated_start if estimated_start > 0 else account_balance   # L154-159 THE BUG
  #   equity = initial_capital + daily_pnl.cumsum()
  #   prev_equity = equity.shift(1).fillna(initial_capital).replace(0, initial_capital)  # L175 FORBIDDEN GUARD
  #   returns_values = daily_pnl / prev_equity
  # returns = pd.Series(returns_values.values, index=pd.DatetimeIndex(returns_values.index), name="returns")

Discipline template (analytics-service/services/deribit_txn.py):
  class LedgerValuationError(ValueError): ...                 # L52 permanent/structural, not transient
  def _coerce_float(value, *, field, row) -> float: ...       # L57 contextual fail-loud coercion
  assert not (CASH_BEARING_TYPES & INFORMATIONAL_TYPES), ...  # L330 import-time disjoint-set invariant
  def _row_utc_day(ts) -> str: ...                            # L382 tolerant epoch-ms AND ISO -> 'YYYY-MM-DD'

Cross-check oracle (analytics-service/services/portfolio_metrics.py) — do NOT duplicate, do NOT modify:
  def compute_twr(equity: pd.Series, events: list[dict]) -> float | None:   # L64
      end_before_cf = end_val - cf_adjustment       # L126  subtract flow from END value
      sub_r = (end_before_cf / begin_val) - 1.0     # L140  == (NAV_t - NAV_{t-1} - F_t)/NAV_{t-1}
  # day-0 skip (L72-91) and begin_val==0 segment-skip (L128-138) are the DQ patterns to mirror.
  # NOTE: this is FORWARD + SCALAR (equity series -> one float). nav_twr.py is BACKWARD + SERIES.

The F=0 identity (the load-bearing byte-identity proof, from 73-RESEARCH.md):
  With F_t=0 and anchor_nav=account_balance:
    NAV_{t-1} = NAV_t - pnl_t  =>  NAV_{t-1} = initial_capital + cumsum_{<t}(pnl) = prev_equity_t
    r_t = (NAV_t - NAV_{t-1} - 0)/NAV_{t-1} = pnl_t / prev_equity_t   -> identical by algebra.
  So SC-4 byte-identity holds by construction ONLY for estimated_start > 0. The byte-identity
  fixture MUST use an account with account_balance - Σpnl > $1000.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Module skeleton + backward NAV reconstruction (TWR-01)</name>
  <files>analytics-service/services/nav_twr.py, analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - analytics-service/services/deribit_txn.py L52-68 (LedgerValuationError, _coerce_float), L330 (disjoint-set assert), L382 (_row_utc_day) — the discipline to mirror verbatim
    - analytics-service/services/transforms.py L120-176 — the current daily_pnl branch shape the backward roll must reproduce
    - analytics-service/tests/test_deribit_txn.py — revert-proof test structure to model the new test file on
  </read_first>
  <behavior>
    - Backward roll: for a daily_pnl Series (DatetimeIndex) with terminal_nav = anchor_nav - open_unrealized_usd and a flows_by_day Series, NAV_{t-1} = NAV_t - pnl_t - F_t; NAV at the last index == terminal_nav.
    - A numpy re-derivation (independent cumulative-subtraction of pnl+flow from the anchor) reproduces the reconstructed NAV Series to fp precision (np.testing.assert_allclose rtol<=1e-12).
    - Mutation/revert-proof: flipping the roll sign (NAV_t + pnl_t) or dropping the flow term makes the oracle test RED.
    - _flows_to_daily_usd sums signed USD per UTC day using the SAME day-key helper as pnl bucketing; a same-day multi-flow list collapses to one summed row on the correct day.
  </behavior>
  <action>
    Create `services/nav_twr.py` importing ONLY stdlib + pandas + numpy. Define `class
    NavReconstructionError(ValueError)` mirroring `LedgerValuationError`; module constant
    `DUST_NAV_FLOOR = 1000.0` (matches transforms.py `_DUST_BALANCE_THRESHOLD`); a docstring note
    defining the `ExternalFlow = (utc_day_iso: str, usd_signed: float)` shape (deposit +,
    withdrawal −) — shape only; sourcing/valuation is Phase 75. Reuse `_row_utc_day` discipline for
    a single shared UTC-day helper used by BOTH flow and pnl bucketing (import from deribit_txn or
    replicate with an import-time note; do NOT fork a second date-boundary helper — Pitfall #11).
    Implement `_flows_to_daily_usd(external_flows) -> pd.Series` (signed USD summed per UTC day) and
    `reconstruct_nav(daily_pnl: pd.Series, terminal_nav: float, flows_by_day: pd.Series) -> pd.Series`
    (the backward roll). Use `_coerce_float(value, field=..., row=...)`-style fail-loud coercion on
    malformed pnl/flow values (no bare `float()`). Terminal-NAV basis: accept `open_unrealized_usd`
    upstream so terminal_nav = anchor_nav - open_unrealized_usd (default 0.0 => no-op this phase, per
    the locked realized-basis decision). Create `tests/test_nav_twr.py` with the numpy-oracle parity
    test and the mutation test, modeled on test_deribit_txn.py revert-proof discipline.
  </action>
  <acceptance_criteria>
    - `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py::test_backward_nav_matches_numpy_oracle -x -q` passes
    - `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py::test_nav_roll_mutation_detected -x -q` passes (asserts a sign/term mutation would fail)
    - `grep -nE '(^import|^from)' analytics-service/services/nav_twr.py | grep -vE 'pandas|numpy|typing|datetime|collections|dataclasses|deribit_txn|^from __future__' | grep -v '^#'` returns no rows (stdlib/pandas/numpy + in-repo discipline only)
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py -k "oracle or mutation" -x -q</automated>
  </verify>
  <done>nav_twr.py exists with NavReconstructionError, DUST_NAV_FLOOR, _flows_to_daily_usd, reconstruct_nav; numpy-oracle and mutation tests green in the 3.12 venv; imports are stdlib/pandas/numpy only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chain-linked TWR + cumulative + edge cases (TWR-02)</name>
  <files>analytics-service/services/nav_twr.py, analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - analytics-service/services/portfolio_metrics.py L64-151 — compute_twr end-of-day numerator (end_before_cf = end_val - cf_adjustment, L126), day-0 skip, begin_val==0 segment-skip — the cross-check oracle and the segmentation discipline to mirror
    - analytics-service/services/nav_twr.py (Task 1 output) — reconstruct_nav + _flows_to_daily_usd
  </read_first>
  <behavior>
    - Chain-linked r_t = (NAV_t - NAV_{t-1} - F_t)/NAV_{t-1} with the flow in the NUMERATOR; cumulative = Π(1+r)-1 over the retained days.
    - day-0 flow: a flow on the first index is handled per the end-of-day convention (no divide by a pre-history NAV).
    - same-day multi-flow: two flows on one UTC day sum before entering r_t on that day.
    - zero-NAV interior day: NAV_{t-1}==0 breaks the chain-link at that day (r_t omitted, cumulative product restarts) — it must NEVER divide by zero or fabricate a value.
    - partial window: a short series (e.g. 3 days) produces the correct chain-linked cumulative.
    - Cross-check: on a shared synthetic fixture, the nav_twr per-day compounding agrees with portfolio_metrics.compute_twr's scalar to fp tolerance.
  </behavior>
  <action>
    Add `chain_linked_twr(nav: pd.Series, daily_pnl: pd.Series, flows_by_day: pd.Series) -> tuple[pd.Series, dict]`
    computing r_t with the end-of-day numerator convention (subtract F_t from the numerator, divide by
    NAV_{t-1}) and returning a `returns` Series named "returns" on a DatetimeIndex plus a flags dict.
    Handle the zero-NAV interior day by breaking the chain-link at that day (mirror compute_twr's
    begin_val==0 segment-skip at L128-138) — reserve the threshold-based dust/negative/flow-dominated
    guards for Task 3; here handle only the intrinsic zero-NAV break so no division-by-zero is possible.
    Add the public entry `reconstruct_nav_and_twr(daily_pnl, anchor_nav, *, external_flows=None,
    open_unrealized_usd=0.0) -> tuple[pd.Series, NavTWRMeta]` wiring
    _flows_to_daily_usd -> reconstruct_nav -> chain_linked_twr. Extend tests/test_nav_twr.py with
    test_twr_edge_cases (day-0 flow, same-day multi-flow, zero-NAV interior, partial window) and
    test_twr_agrees_with_compute_twr (cross-check vs portfolio_metrics.compute_twr on a shared fixture).
  </action>
  <acceptance_criteria>
    - `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py::test_twr_edge_cases -x -q` passes
    - `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py::test_twr_agrees_with_compute_twr -x -q` passes
    - `grep -c "def reconstruct_nav_and_twr" analytics-service/services/nav_twr.py` returns 1
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py -k "edge_cases or compute_twr" -x -q</automated>
  </verify>
  <done>reconstruct_nav_and_twr and chain_linked_twr exist; all four edge cases and the compute_twr cross-check pass in the 3.12 venv; zero-NAV interior day breaks the link with no division-by-zero.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fail-loud NAV-denominator guards + source-scan + F=0 byte-identity pin (DQ-01, SC-4)</name>
  <files>analytics-service/services/nav_twr.py, analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - analytics-service/services/transforms.py L1-38 (ReturnsComputationMeta), L145-176 (daily_pnl branch, the estimated_start>0 honest path), L226-259 (_build_meta + the 8-consumer complete-vs-complete_with_warnings note)
    - analytics-service/services/nav_twr.py (Task 2 output) — chain_linked_twr guard site
    - 73-RESEARCH.md "Pattern 1: Fail-loud denominator guard" + "Anti-Patterns to Avoid" (forbidden replace(0,...)/clip/max(...,floor))
  </read_first>
  <behavior>
    - Three guards, checked BEFORE the denominator divides, each breaking the chain-link for that day and raising computation_status_hint="complete_with_warnings" (never substituting a base): dust-floor NAV_{t-1} < DUST_NAV_FLOOR ($1000); negative reconstructed NAV_{t-1} <= 0; flow-dominated |F_t| >= FLOW_DOM_RATIO * NAV_{t-1} with FLOW_DOM_RATIO = 1.0 (conservative default per the locked DQ-01 decision).
    - Each guard sets a distinct additive flag key in the returned meta (recommended: dust_nav_guard, negative_nav_guard, flow_dominated_guard); the two existing meta keys (used_heuristic_capital, balance_error) and their complete/complete_with_warnings semantics are unchanged.
    - source-scan: nav_twr.py source contains NO `.replace(0`, `.clip(`, `max(` on a NAV/denominator, or nonzero `.fillna(` on a denominator (the forbidden substitution class).
    - F=0 byte-identity: with external_flows=[] and open_unrealized_usd=0.0, for an account with account_balance - Σpnl > $1000, the returns Series equals today's trades_to_daily_returns_with_status daily_pnl-branch output (assert_series_equal rtol<=1e-12).
    - The estimated_start<=0 case FLAGS (dust/negative guard) rather than silently substituting account_balance — the intended divergence from today.
  </behavior>
  <action>
    In `chain_linked_twr`, add the three threshold guards at the denominator site per 73-RESEARCH.md
    Pattern 1, each calling `break_chain(t)` and recording its flag key. Define a new TypedDict IN
    nav_twr.py — `class NavTWRMeta(ReturnsComputationMeta, total=False): dust_nav_guard: bool;
    negative_nav_guard: bool; flow_dominated_guard: bool` — and use it as the core's return type; do
    NOT modify transforms.py's `ReturnsComputationMeta` (it is imported read-only, and adding undeclared
    keys to it is a mypy error). Reuse transforms.py's `_build_meta` complete_with_warnings convention
    for the status hint — new keys are ADDITIVE; do NOT alter the existing two keys' semantics, do NOT
    drive strategy_analytics.computation_status here — that wiring plus the 8-consumer migration is Phase 74.
    Define FLOW_DOM_RATIO = 1.0 as a documented module constant. Add tests:
    test_dq_guards_flag_not_substitute (each guard breaks the link + sets complete_with_warnings, and
    the estimated_start<=0 account flags instead of substituting), test_no_forbidden_denominator_guards
    (static source-scan asserting the forbidden patterns are absent), and
    test_zero_flow_byte_identical (import trades_to_daily_returns_with_status, build an
    account_balance - Σpnl > $1000 fixture, assert the two returns Series are equal to rtol<=1e-12).
  </action>
  <acceptance_criteria>
    - `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py::test_dq_guards_flag_not_substitute -x -q` passes
    - `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py::test_no_forbidden_denominator_guards -x -q` passes
    - `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py::test_zero_flow_byte_identical -x -q` passes
    - `grep -nE '\.replace\(0|\.clip\(|np\.clip\(|max\([^)]*floor|np\.maximum\(|\.fillna\([1-9]' analytics-service/services/nav_twr.py | grep -v '^#'` returns no rows
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py -x -q</automated>
  </verify>
  <done>Three fail-loud NAV-denominator guards flag (never substitute); the source-scan test forbids the substitution class; the F=0 byte-identity pin is green for an estimated_start>0 account and the estimated_start<=0 account flags; the full test_nav_twr.py suite passes in the 3.12 venv.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal caller -> nav_twr | in-process Python call; data arrives from already-validated internal callers. No auth/network/SQL/user-input surface in this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | reconstruct_nav_and_twr NAV denominator | mitigate | A silent wrong number is the harm class. DQ-01 fail-loud guards (dust/negative/flow-dominated) break the chain-link and flag complete_with_warnings; the source-scan test (test_no_forbidden_denominator_guards) forbids clamp/floor/replace(0,...)/clip on any NAV denominator. |
| T-73-02 | Information Disclosure | logging inside nav_twr | mitigate | Do NOT log raw NAV/flow USD values (account-size leak); log symbol+date+status only (scrub_freeform_string precedent). The core is I/O-free and should not log payloads. |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: none). All deps pinned + in production use. |

No new external attack surface; pure in-process computation. Block-on: high (none expected).
</threat_model>

<verification>
- Full new-file suite green in the CI-3.12 venv: `cd analytics-service && "$PY312" -m pytest tests/test_nav_twr.py -x -q`
- nav_twr.py stays inside the coverage denominator (under services/, not scripts/); ships with >=80% line coverage (CI `--cov-fail-under=80` is a BLOCKING gate).
- Source-scan guard green: no forbidden denominator substitution patterns in nav_twr.py.
</verification>

<success_criteria>
- TWR-01: backward NAV reconstruction matches the numpy-pinned oracle to fp precision; tests are revert-proof.
- TWR-02: chain-linked r_t + cumulative computed with flow in the numerator; all four edge cases pass; cross-checked against portfolio_metrics.compute_twr.
- DQ-01: every NAV denominator guarded (dust/negative/flow-dominated) -> break-link + complete_with_warnings, never a fabricated number; source-scan forbids the substitution class.
- SC-4: with external_flows=[] and open_unrealized_usd=0.0 the returns Series is byte-identical to today's daily_pnl branch for an estimated_start>0 account; the estimated_start<=0 account flags instead of substituting.
</success_criteria>

<output>
Create `.planning/phases/73-pure-nav-twr-core/73-01-SUMMARY.md` when done.
</output>
