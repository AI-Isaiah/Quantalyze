---
phase: 73-pure-nav-twr-core
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/parity_diff.py
  - analytics-service/tests/test_parity_diff.py
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "A reusable primitive diffs an old-vs-new return Series and classifies each account's delta into exactly one bucket: unchanged / reannualization / flow_moved / unexplained"
    - "An identical old/new series classifies as 'unchanged'"
    - "A series differing only by the known 365/252 CAGR/Calmar factor classifies as 'reannualization', not a regression"
    - "A materially-moved series (flow-heavy) classifies as 'flow_moved'; anything not matching a known bucket classifies as 'unexplained'"
  artifacts:
    - path: "analytics-service/services/parity_diff.py"
      provides: "old-vs-new series-diff + delta-bucket classifier primitive (ACC-01 infrastructure; the panel + live run are Phase 78)"
      contains: "def classify_delta"
      min_lines: 40
    - path: "analytics-service/tests/test_parity_diff.py"
      provides: "unit tests on synthetic series for each classification bucket"
      contains: "def test_"
      min_lines: 40
  key_links:
    - from: "analytics-service/services/parity_diff.py::classify_delta"
      to: "delta bucket taxonomy"
      via: "returns one of the fixed bucket labels"
      pattern: "unchanged|reannualiz|flow_moved|unexplained"
---

<objective>
Stand up ONLY the reusable series-diff / delta-bucket classifier primitive that the ACC-01 golden
old-vs-new parity harness (Phase 78) will consume. This is the small, isolated infrastructure piece
that RESEARCH Open Question 2 recommends building early; the multi-venue panel and the live parity
run are explicitly Phase 78, not this phase.

Purpose: The Phase 78 hard gate must classify EVERY delta into exactly one expected bucket and accept
no unexplained movement. Building the classification primitive now (with unit tests on synthetic
series) de-risks that gate and gives Phase 74's wiring a ready comparison tool. Keep it small.

Output: `services/parity_diff.py` (new, pure, I/O-free) + `tests/test_parity_diff.py`.

Note: ACC-01 GATES/COMPLETES in Phase 78 (per REQUIREMENTS.md traceability). Phase 73 delivers only
the reusable primitive — do NOT build a multi-venue panel, real-account fixtures, or any live run here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

**Interpreter (MANDATORY — from 73-VALIDATION.md):** the local `.venv` is Python 3.14 and
SIGSEGVs at pytest collection in native pandas tslibs. Run every test against the CI-matching
3.12 venv:
`PY312=/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python`
If that path no longer exists, recreate a 3.12/3.13 venv via `uv venv --python 3.12` +
`uv pip sync analytics-service/requirements.txt analytics-service/requirements-dev.txt`. NEVER run
pandas-importing tests on the local 3.14 interpreter. Coverage gate runs in CI.
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/73-pure-nav-twr-core/73-RESEARCH.md

<interfaces>
<!-- The Phase 78 bucket taxonomy this primitive must express (ROADMAP Phase 78 SC-1, REQUIREMENTS ACC-01): -->
  # (a) flow-less account       -> daily return SERIES + cumulative + Sharpe/vol UNCHANGED  => "unchanged"
  # (b) TWR-05 reannualization   -> CAGR/Calmar shift by the known 365/252 calendar factor   => "reannualization" (known, NOT a regression)
  # (c) flow-heavy account       -> series MUST move and is now honest                        => "flow_moved"
  # anything outside (a)/(b)/(c)                                                              => "unexplained" (fails the Phase 78 gate)
  # The v1.5 frozen-engine re-baseline ceremony is the precedent (shadow/dual-compute until clean).
  # pandas.testing.assert_series_equal(check_exact=False, rtol=...) is the natural "unchanged" primitive.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Series-diff + delta-bucket classifier primitive (ACC-01 infrastructure)</name>
  <files>analytics-service/services/parity_diff.py, analytics-service/tests/test_parity_diff.py</files>
  <read_first>
    - 73-RESEARCH.md Open Question 2 (build only the reusable comparison primitive in Phase 73; defer the panel to Phase 78) + "State of the Art" (v1.5 frozen-engine re-baseline precedent)
    - .planning/ROADMAP.md Phase 78 SC-1 (the (a)/(b)/(c) bucket taxonomy the classifier must express)
    - analytics-service/tests/test_metrics.py — existing pandas-fixture test style to match
  </read_first>
  <behavior>
    - classify_delta compares an old and a new return Series (aligned DatetimeIndex) plus their scalar metric deltas and returns exactly one bucket label: "unchanged", "reannualization", "flow_moved", or "unexplained".
    - Identical old/new series (within rtol tolerance) -> "unchanged".
    - Series identical but CAGR/Calmar differ by only the known 365/252 factor (within tolerance) -> "reannualization".
    - Series materially moved beyond tolerance in a way consistent with a flow injection -> "flow_moved".
    - Any movement not matching a known bucket -> "unexplained" (the Phase 78 gate-failing bucket).
    - Pure / I/O-free (stdlib + pandas + numpy only); no panel, no fixtures of real accounts, no live run.
  </behavior>
  <action>
    Create `services/parity_diff.py` importing only stdlib + pandas + numpy. Implement a small
    `classify_delta(old_returns, new_returns, *, old_metrics=None, new_metrics=None, rtol=...) -> str`
    that returns one of the four fixed bucket labels expressing the Phase 78 (a)/(b)/(c)/unexplained
    taxonomy, using `pandas.testing.assert_series_equal`-style tolerance comparison for the series-
    unchanged check and the known 365/252 factor for the reannualization check. Expose the bucket
    labels as module constants so Phase 78 imports them rather than restating string literals. Add
    `tests/test_parity_diff.py` with one synthetic-series test per bucket (unchanged,
    reannualization, flow_moved, unexplained). Keep the module small — this is ONLY the primitive; the
    multi-venue panel and live run are Phase 78.
  </action>
  <acceptance_criteria>
    - `cd analytics-service && "$PY312" -m pytest tests/test_parity_diff.py -x -q` passes (one test per bucket green)
    - `grep -c "def classify_delta" analytics-service/services/parity_diff.py` returns 1
    - `grep -nE '(^import|^from)' analytics-service/services/parity_diff.py | grep -vE 'pandas|numpy|typing|^from __future__' | grep -v '^#'` returns no rows (stdlib/pandas/numpy only)
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && "$PY312" -m pytest tests/test_parity_diff.py -x -q</automated>
  </verify>
  <done>parity_diff.py exposes classify_delta + the four bucket-label constants; every bucket has a green synthetic-series test in the 3.12 venv; imports are stdlib/pandas/numpy only; no panel or live run built (deferred to Phase 78).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| internal caller -> parity_diff | in-process Python call on two return Series. No auth/network/SQL/user-input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-04 | Tampering | classify_delta bucket boundaries | mitigate | A misclassified "unexplained" delta silently passing as "unchanged" is the harm class (it would let a real regression through the Phase 78 gate). The per-bucket synthetic-series tests pin each boundary; the default disposition for a non-matching delta is "unexplained" (fail-closed). |
| T-73-SC | Tampering | npm/pip/cargo installs | accept | No package installs (all deps pinned + in production use). |

No new external attack surface; pure in-process computation. Block-on: high (none expected).
</threat_model>

<verification>
- `cd analytics-service && "$PY312" -m pytest tests/test_parity_diff.py -x -q` green in the CI-3.12 venv.
- parity_diff.py stays inside the coverage denominator (under services/); ships with >=80% line coverage.
- Scope discipline: only the classifier primitive exists — no multi-venue panel, no real-account fixtures, no live run (those are Phase 78).
</verification>

<success_criteria>
- ACC-01 (infrastructure only): the reusable series-diff / delta-bucket classifier exists and correctly classifies unchanged / reannualization / flow_moved / unexplained on synthetic series; the ACC-01 requirement itself gates and completes in Phase 78.
</success_criteria>

<output>
Create `.planning/phases/73-pure-nav-twr-core/73-03-SUMMARY.md` when done.
</output>
