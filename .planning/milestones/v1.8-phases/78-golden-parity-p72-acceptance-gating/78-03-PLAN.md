---
phase: 78-golden-parity-p72-acceptance-gating
plan: 03
type: execute
wave: 3
depends_on: [78-01, 78-02]
files_modified: []
autonomous: false
requirements: [ACC-02]

user_setup:
  - service: railway + prod Deribit/Supabase env
    why: "The ACC-02 canary re-crawls the live Deribit ledger and reads persisted prod factsheets — executor subagents have no railway/Supabase/Deribit access."
    env_vars:
      - name: DERIBIT_CLIENT_ID_{N} / DERIBIT_CLIENT_SECRET_{N}
        source: "Railway prod env (read-only Deribit keys) — already set; never printed"
      - name: SUPABASE_URL / SUPABASE_SERVICE_KEY
        source: "Railway prod env — already set; never printed"
    dashboard_config:
      - task: "Founder supplies the real LTP056/068/016 strategy UUIDs, key indices, and onboarding windows at run time (prod secrets, NOT in repo)"
        location: "Founder-held; passed via the --account <uuid>:<idx>:<label>:<start>:<end> tuples"

must_haves:
  truths:
    - "A read-only pre-73 snapshot (csv_daily_returns + CAGR) of LTP056/068/016 is captured BEFORE the re-derive overwrites them"
    - "LTP056/068/016 re-derived under the corrected flow-aware returns BEFORE the canary reads them"
    - "The P72 acceptance canary (deribit_acceptance.py) exits 0 under corrected returns"
    - "LTP068 is no longer +458% / 229,214% CAGR — magnitude matches the exchange statement"
    - "Trade counts match, funding reconciles, inverse-P&L signs verified vs exchange statements (LTP056/068/016)"
    - "Deribit session_upl field name confirmed against a live key (A1); OKX/Bybit/Binance wallet-scope confirmed vs statements"
  artifacts:
    - path: "analytics-service/scripts/deribit_acceptance.py"
      provides: "The P72 canary (5 check_* gates) re-run as-is under corrected returns — EXISTING, no code change"
      contains: "def check_daily_reconcile"
  key_links:
    - from: "read-only pre-73 snapshot of LTP056/068/016"
      to: "post-re-derive UNEXPLAINED-delta root-causing (SC-1)"
      via: "capture current csv_daily_returns + CAGR before the overwrite"
      pattern: "csv_daily_returns"
    - from: "corrected re-derive of LTP056/068/016 in prod (derive_broker_dailies job)"
      to: "scripts/deribit_acceptance.py canary read"
      via: "sequence — re-derive persists corrected rows, THEN the canary reads them (Pitfall 5)"
      pattern: "deribit_acceptance"
---

<objective>
Close ACC-02 — the live, founder-gated half of the gate. Snapshot the current
(pre-73, inflated) LTP056/068/016 rows read-only, re-derive the three strategies
under the corrected flow-aware returns, then re-run the carried v1.7 P72
acceptance canary (`scripts/deribit_acceptance.py`, 5 `check_*` gates) against the
freshly-persisted factsheets. Green closes ACC-02 and triggers the v1.7 audit ->
complete -> cleanup. The four deferred founder-validation gates all fold into this
one live run.

Purpose: There is NO automated net for real-account correctness — the
reconciliation residual is self-consistent by construction (P76 wallet-scope
class), so only golden parity vs real exchange statements + founder confirmation
catches a wrong anchor. This is the ONLY mechanism.
Output: A captured pre-73 snapshot, an exit-0 canary run under corrected returns,
and founder confirmation of LTP056/068/016 magnitudes, the Deribit `session_upl`
field name, and the OKX/Bybit/Binance wallet-scope. NO code is written in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-CONTEXT.md
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-RESEARCH.md
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-VALIDATION.md
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-02-SUMMARY.md

<interfaces>
<!-- EXISTING script, re-run as-is. No code change. Verified at HEAD. -->
scripts/deribit_acceptance.py:
  CLI: --account <uuid>:<key_index>:<label>:<start>:<end>   (repeatable; ISO dates)
  gates: check_factsheet_status, check_date_coverage, check_daily_reconcile, check_inverse_signs
  check_daily_reconcile gates on NONZERO-P&L days (zero-days advisory) as of 9a1e7b8e — inherit that fix.
  exit 0 = every gate passed. Reads persisted strategy_analytics / csv_daily_returns (SELECT-only).
  Reads DERIBIT_CLIENT_ID_{N}/SECRET_{N} + SUPABASE_URL/SUPABASE_SERVICE_KEY from env; exit 2 on missing env.
  Prints neither creds nor raw USD magnitudes on any path.

<!-- Concrete re-derive mechanism (verified in-repo) -->
The LTP strategies are Deribit-derivative api-key accounts; the corrected factsheet is
produced by an api_key-scoped `derive_broker_dailies` compute job → services/analytics_runner.py::
run_strategy_analytics(strategy_id) persists corrected csv_daily_returns + strategy_analytics.
Enqueue pattern to mirror: scripts/phase35_backfill_enqueue.py (enqueues ONE derive_broker_dailies
compute job per active key). mig-038 caveat: services/analytics_status.py::sync_strategy_analytics_status
maps "any failed_final → failed" — a prior failed run POISONS retry, silently blocking the re-derive.
Existing status-reset one-off: scripts/reset_stuck_computing_rows.py.

Real UUIDs/key-indices/windows are prod secrets (NOT in repo). Founder supplies at run time.
Docstring example (placeholders only): <uuid>:1:LTP056:2025-08-01:2025-09-30 (windows A2, indices A3 — founder-confirmed).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Snapshot pre-73, clear poisoned status, then re-derive LTP056/068/016 (prod, orchestrator/founder)</name>
  <read_first>
    - .planning/phases/78-golden-parity-p72-acceptance-gating/78-RESEARCH.md (Pitfall 5 — reconcile ordering; Runtime State Inventory — prod rows hold PRE-73 inflated values until re-derived).
    - analytics-service/scripts/phase35_backfill_enqueue.py — the api_key-scoped `derive_broker_dailies` enqueue pattern (the concrete re-derive mechanism).
    - analytics-service/services/analytics_status.py — `sync_strategy_analytics_status` (the mig-038 "any failed_final → failed" retry-poison to check/clear first).
    - analytics-service/scripts/reset_stuck_computing_rows.py — the existing status-reset one-off.
    - analytics-service/scripts/deribit_acceptance.py — reads PERSISTED rows, so the re-derive MUST land first.
  </read_first>
  <what-built>
    The corrected flow-aware chain (P73-77) is complete on `v1.8-flow-aware-twr`.
    Prod `csv_daily_returns` / `strategy_analytics` for LTP056/068/016 still hold
    the PRE-73 inflated values (LTP068 = +458% cum / 229,214% CAGR); the re-derive
    OVERWRITES them, so the +458% baseline must be captured first to root-cause any
    post-hoc UNEXPLAINED delta (SC-1 "every delta explained account-by-account").
  </what-built>
  <action>
    Orchestrator/founder-run (executor subagents have no railway/Supabase access,
    so this task is `autonomous: false`), in strict order:
    (1) SNAPSHOT: read-only dump of LTP056/068/016 current `csv_daily_returns`
        series + `strategy_analytics` CAGR/cumulative to a local file (Supabase
        read or `railway ssh` read-only SELECT) — this is the pre-73 witness for
        account-by-account delta root-causing, since the re-derive overwrites it.
    (2) STATUS CLEAR (mig-038): check each strategy's analytics status; if a prior
        run left it `failed`/`failed_final`, clear it (mirror
        `scripts/reset_stuck_computing_rows.py`) so `sync_strategy_analytics_status`
        does not poison the retry and silently block the re-derive.
    (3) RE-DERIVE: enqueue an api_key-scoped `derive_broker_dailies` compute job
        for each of the three LTP keys (mirror `scripts/phase35_backfill_enqueue.py`)
        — the queue worker runs `derive_broker_dailies` → `run_strategy_analytics`
        and re-persists corrected `csv_daily_returns` + `strategy_analytics`.
    Sequence FIRST — before the Task 2 canary read (Pitfall 5). No code is written;
    this is a live prod snapshot + re-derive. Do NOT surface raw USD magnitudes to
    shared logs (the local snapshot file stays local).
  </action>
  <how-to-verify>
    1. Snapshot file exists locally with LTP056/068/016 pre-73 series + CAGR
       (including LTP068's +458% / 229,214% baseline) for later delta root-causing.
    2. Each strategy's analytics status is not stuck `failed`/`failed_final` before
       enqueue (cleared if it was — mig-038 poison).
    3. The three `derive_broker_dailies` jobs run to completion; each strategy
       re-persists with a corrected `computation_status` (`complete` or
       `complete_with_warnings`) — NOT the stale pre-73 rows.
    4. Do this BEFORE Task 2 — running the canary against stale inflated rows
       reconciles the OLD factsheet and gives a misleading pass/fail (Pitfall 5).
  </how-to-verify>
  <verify>Snapshot captured; poisoned status cleared; LTP056/068/016 re-derived post-P77 (corrected computation_status) before Task 2 runs.</verify>
  <acceptance_criteria>
    - A local pre-73 snapshot of LTP056/068/016 (`csv_daily_returns` + CAGR) exists before the overwrite.
    - No strategy is left in a poisoned `failed_final` state blocking the re-derive (mig-038 checked/cleared).
    - LTP056/068/016 `strategy_analytics` / `csv_daily_returns` rows reflect the corrected flow-aware pipeline (re-derived timestamp is post-P77).
    - LTP068's persisted cumulative return is no longer +458% (sanity glance — founder confirms exact magnitude in Task 2).
    - No raw USD magnitudes surfaced to shared logs.
  </acceptance_criteria>
  <done>The pre-73 snapshot is captured, poisoned status cleared, and the three LTP factsheets hold corrected flow-aware values in prod; stale pre-73 rows are gone.</done>
  <resume-signal>Type "re-derived" once the snapshot is captured and the three LTP factsheets are corrected in prod, or describe what blocked.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Re-run the P72 canary + founder-confirm magnitudes and field/scope (ACC-02 close)</name>
  <read_first>
    - analytics-service/scripts/deribit_acceptance.py — the 5 `check_*` gates + `--account` CLI (re-run as-is; no code change).
    - .planning/phases/78-golden-parity-p72-acceptance-gating/78-CONTEXT.md (the four converging founder-validation gates).
    - .planning/phases/78-golden-parity-p72-acceptance-gating/78-RESEARCH.md (Assumptions A1/A2/A3 — session_upl field, windows, key indices — all founder-confirmable here).
  </read_first>
  <what-built>
    The existing P72 canary re-run under corrected returns. This is the HARD
    live gate — its green closes ACC-02 and triggers the v1.7 audit -> complete
    -> cleanup.
  </what-built>
  <action>
    Founder/orchestrator-run (`autonomous: false`): with Task 1 landed, run
    `scripts/deribit_acceptance.py` via `railway ssh` for the three LTP accounts
    (founder supplies the real UUIDs / key indices / windows — prod secrets, never
    committed), confirm exit 0 across the 5 check_* gates, and record founder
    confirmation of the magnitudes vs statements, the Deribit `session_upl` field
    name (A1), and the OKX/Bybit/Binance wallet-scope (P76). If the canary flags a
    delta, use the Task-1 pre-73 snapshot to root-cause it account-by-account. No
    code is written; the canary is re-run as-is.
  </action>
  <how-to-verify>
    1. With the three LTP strategies re-derived (Task 1), run via railway ssh
       (founder supplies the real UUIDs / key indices / windows — prod secrets):
       `railway ssh "cd /app &amp;&amp; python -m scripts.deribit_acceptance \
         --account <uuid>:<idx>:LTP056:<start>:<end> \
         --account <uuid>:<idx>:LTP068:<start>:<end> \
         --account <uuid>:<idx>:LTP016:<start>:<end>"`
    2. Confirm exit 0 — all of check_factsheet_status, check_date_coverage,
       check_daily_reconcile (nonzero-P&L days exact; zero-days advisory),
       check_inverse_signs pass.
    3. Founder confirms against exchange statements: LTP056/068/016 trade counts
       match, funding reconciles, inverse-P&L signs correct, and LTP068 is no
       longer +458% (magnitude now honest; compare to the Task-1 snapshot).
    4. Founder confirms the Deribit `session_upl` field name against a live key
       (A1 — wrong name degrades the uPnL wedge to 0.0 safely, no correctness
       risk, but confirm here).
    5. Founder confirms the OKX/Bybit/Binance wallet-scope anchor vs statements
       (P76 wrong-anchor class — invisible to the reconciliation residual; golden
       parity vs real statements is the only catch).
  </how-to-verify>
  <verify>Canary exits 0 for all three LTP accounts under corrected rows; founder confirmation of magnitudes + A1 field + wallet-scope recorded.</verify>
  <acceptance_criteria>
    - `deribit_acceptance.py` exits 0 for all three LTP accounts under the re-derived corrected rows.
    - Founder confirms: trade counts match, funding reconciles, inverse-P&L signs verified, LTP068 magnitude honest (no longer +458%).
    - Deribit `session_upl` field name confirmed (A1); OKX/Bybit/Binance wallet-scope confirmed vs statements.
    - No creds or raw USD magnitudes leaked to shared logs.
  </acceptance_criteria>
  <done>ACC-02 green: the P72 canary passes under corrected returns and the founder-validation gates are confirmed — triggering v1.7 audit -> complete -> cleanup.</done>
  <resume-signal>Type "acc-02 green" with founder confirmation, or describe the failing gate / magnitude discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Deribit ledger -> canary | Read-only re-crawl via prod creds (env only). |
| Supabase persisted rows -> canary | SELECT-only read of strategy_analytics / csv_daily_returns. |
| pre-73 snapshot file | Stays local; carries raw magnitudes — never pushed to shared logs/repo. |
| shared logs <- live run | No creds, no raw USD magnitudes on any path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02 | Information Disclosure | deribit_acceptance.py live run (creds + magnitudes) + local snapshot file | mitigate | Creds passed to ccxt/Supabase clients only, never printed (exit 2 on missing env); canary emits gate booleans, never dollar magnitudes; the snapshot file stays local, never committed or logged. Founder-run, `autonomous: false`. |
| T-78-06 | Tampering | canary reading STALE pre-73 rows | mitigate | Sequenced: snapshot + corrected re-derive (Task 1) MUST land before the canary read (Task 2) — Pitfall 5. |
| T-78-07 | Denial of Service | mig-038 status-poison silently blocking the re-derive | mitigate | Check/clear `failed_final` status before enqueue (reset_stuck_computing_rows pattern) so the re-derive is not silently skipped. |
| T-78-SC | Tampering | package installs | accept | No package installs this phase (no new deps). Nothing to audit. |
</threat_model>

<verification>
- Pre-73 snapshot of LTP056/068/016 captured before the re-derive overwrite.
- mig-038 poisoned status checked/cleared before enqueue.
- `deribit_acceptance.py` exits 0 for LTP056/068/016 under corrected re-derived rows.
- Founder confirmation recorded: magnitudes vs statements (vs the snapshot), session_upl (A1), wallet-scope.
- The snapshot + re-derive (Task 1) proven to have landed before the canary run (Task 2).
</verification>

<success_criteria>
LTP056/068/016 are verified as honest track records against exchange statements
under the corrected returns (LTP068 no longer +458%), with a pre-73 snapshot
retained for delta root-causing, and the P72 acceptance canary re-runs green —
closing ACC-02 and triggering the v1.7 audit -> complete -> cleanup.
</success_criteria>

<output>
Create `.planning/phases/78-golden-parity-p72-acceptance-gating/78-03-SUMMARY.md` when done.
</output>
