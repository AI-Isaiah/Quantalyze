---
phase: 78-golden-parity-p72-acceptance-gating
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/scripts/golden_parity.py
  - analytics-service/tests/test_golden_parity.py
  - analytics-service/tests/fixtures/golden_parity/oracle_input.json
  - analytics-service/tests/fixtures/golden_parity/oracle_pre73_expected.json
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "The frozen anchor-to-today oracle reproduces the committed pre-73 golden series within rtol 1e-9"
    - "Perturbing the oracle (dropping the estimated_start<=0 -> account_balance fallback) turns the oracle golden test RED"
    - "The oracle transcribes BOTH branches (daily_pnl and individual-trades) verbatim from 9a1e7b8e"
    - "The golden witness is captured from the REAL old module at 9a1e7b8e (worktree import), independent of the Task-1 transcription"
  artifacts:
    - path: "analytics-service/scripts/golden_parity.py"
      provides: "Frozen anchor-to-today oracle (old_anchor_to_today_returns), provenance-commented to 9a1e7b8e:services/transforms.py L148-215"
      contains: "def old_anchor_to_today_returns"
    - path: "analytics-service/tests/fixtures/golden_parity/oracle_pre73_expected.json"
      provides: "Independent pre-73 golden series captured from the real old code at ref 9a1e7b8e"
    - path: "analytics-service/tests/test_golden_parity.py"
      provides: "test_oracle_matches_pre73_golden (mutation-honest oracle pin)"
      contains: "def test_oracle_matches_pre73_golden"
  key_links:
    - from: "analytics-service/scripts/golden_parity.py::old_anchor_to_today_returns"
      to: "9a1e7b8e:analytics-service/services/transforms.py L148-215"
      via: "verbatim transcription with provenance comment"
      pattern: "9a1e7b8e"
---

<objective>
Stand up the LOAD-BEARING half of the ACC-01 golden-parity gate: a frozen
"anchor-to-today" oracle that re-materializes the OLD (silently-inflated,
pre-73) daily-returns behaviour so the harness has something honest to diff the
NEW flow-aware core against. The OLD formula was DELETED from `transforms.py` in
P73/P74; it survives verbatim at the v1.8-branch merge-base `9a1e7b8e`. This plan
transcribes it (both branches) into the harness as a pure, dependency-free
function and pins it against an independently-captured pre-73 golden series so it
can never silently drift.

Purpose: A subtly-wrong OLD oracle produces spurious FLOW_MOVED/UNEXPLAINED
deltas and destroys the entire gate's authority (RESEARCH Pitfall 1). Isolating
and golden-pinning the oracle FIRST de-risks everything Plan 02 builds on top.
Output: `scripts/golden_parity.py` (oracle only), a committed golden JSON, and a
mutation-honest oracle self-test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-CONTEXT.md
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-RESEARCH.md

<interfaces>
<!-- The OLD formula to transcribe — VERIFIED present at the merge-base ref. -->
<!-- Retrieve verbatim with: git show 9a1e7b8e:analytics-service/services/transforms.py | sed -n '148,215p' -->
<!-- 9a1e7b8e == git merge-base v1.8-flow-aware-twr main (confirmed). -->
<!-- At that ref the buggy fallback lives inside trades_to_daily_returns_with_status -->
<!-- (def @ L70; the `estimated_start if estimated_start > 0 else account_balance` line @ L199), -->
<!-- with a parallel branch in trades_to_daily_returns (def @ L41; fallback @ L153-155). -->

daily_pnl branch (the LTP/broker path — RESEARCH Pattern 1):
```text
_DUST = 1000.0
if account_balance and account_balance > _DUST:
    total_pnl = daily_pnl.sum()
    estimated_start = account_balance - total_pnl
    initial_capital = estimated_start if estimated_start > 0 else account_balance   # <-- THE BUG
else:
    initial_capital = max(daily_pnl.abs().mean() * 100, abs(daily_pnl.sum()), 10000) # heuristic
equity = initial_capital + daily_pnl.cumsum()
prev_equity = equity.shift(1).fillna(initial_capital).replace(0, initial_capital)   # <-- divide-guard
returns = daily_pnl / prev_equity
```
There is a PARALLEL individual-trades branch at the same ref (Open Question 2).
Transcribe BOTH branches; fixture-test the daily_pnl branch (the LTP path).

Existing golden-ceremony analog to mirror (v1.5): tests/fixtures/golden_252d_input.json + golden_252d_expected.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe the frozen anchor-to-today oracle (both branches)</name>
  <files>analytics-service/scripts/golden_parity.py</files>
  <read_first>
    - `git show 9a1e7b8e:analytics-service/services/transforms.py` (lines 148-215) — the source of truth for BOTH branches. Transcribe verbatim, do not paraphrase.
    - analytics-service/services/parity_diff.py — the docstring bucket taxonomy this oracle feeds (context only; do not modify).
  </read_first>
  <action>
    Create `analytics-service/scripts/golden_parity.py` with a single pure
    function `old_anchor_to_today_returns(daily_pnl: pd.Series, account_balance:
    float | None) -> pd.Series` that reproduces the pre-73 daily_pnl branch
    verbatim (dust-floor $1000, `estimated_start = account_balance - total_pnl`,
    the `estimated_start > 0 else account_balance` fallback that IS the +458%
    LTP068 bug, the heuristic-capital else-branch, `equity = initial_capital +
    cumsum`, `prev_equity.shift(1).fillna(initial_capital).replace(0,
    initial_capital)`, `returns = daily_pnl / prev_equity`). Add a second
    function `old_anchor_to_today_returns_from_trades(...)` transcribing the
    parallel individual-trades branch at the same ref (Open Question 2). Head
    both with a provenance comment naming the exact ref and lines
    (`frozen from 9a1e7b8e:services/transforms.py L148-215 — DO NOT "fix", this
    IS the OLD behaviour we diff against`). stdlib + pandas + numpy ONLY — no
    imports from `services.transforms` / `nav_twr` (RESEARCH rejected re-importing
    the old module for the RUNTIME oracle: pulls the whole service graph and
    drifts). Do NOT emit raw USD magnitudes anywhere (T-78-01 leak class).
  </action>
  <verify>
    <automated>cd analytics-service &amp;&amp; python -c "import ast; ast.parse(open('scripts/golden_parity.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/golden_parity.py` defines `old_anchor_to_today_returns` and `old_anchor_to_today_returns_from_trades`.
    - Both functions carry a provenance comment containing the literal ref `9a1e7b8e` and line range `L148-215`.
    - The daily_pnl branch preserves the `estimated_start > 0 else account_balance` fallback AND the `.replace(0, initial_capital)` divide-guard (grep-verifiable, filtering comments).
    - No import of `services.transforms` or `services.nav_twr`.
  </acceptance_criteria>
  <done>The frozen oracle exists as pure source, both branches transcribed verbatim with provenance comments.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Capture the pre-73 golden series and pin the oracle against it</name>
  <files>analytics-service/tests/fixtures/golden_parity/oracle_input.json, analytics-service/tests/fixtures/golden_parity/oracle_pre73_expected.json, analytics-service/tests/test_golden_parity.py</files>
  <read_first>
    - analytics-service/tests/fixtures/golden_252d_input.json + golden_252d_expected.json — the v1.5 golden-ceremony format to mirror.
    - analytics-service/scripts/golden_parity.py — the oracle written in Task 1 (the code under pin).
    - `git show 9a1e7b8e:analytics-service/services/transforms.py` (L70-215) — the REAL old `trades_to_daily_returns_with_status` (the independent witness source; the fallback is at L199).
  </read_first>
  <behavior>
    - test_oracle_matches_pre73_golden: `old_anchor_to_today_returns(input)` equals the committed expected series within rtol 1e-9 (per-point, `assert_series_equal`).
    - WITNESS INDEPENDENCE (advisory): the golden must be captured from the REAL old module, NOT from the Task-1 transcription. Generate `oracle_pre73_expected.json` ONE-TIME by checking out `9a1e7b8e` in a throwaway `git worktree`, importing the real `services.transforms.trades_to_daily_returns_with_status`, and calling it on `oracle_input.json` inputs adapted to drive the daily_pnl branch — then dumping the returned series. Do NOT `exec`/run the extracted snippet or the Task-1 oracle to produce the golden (that would silently reproduce the transcription and defeat independence). NOTE: this one-time worktree import is NOT the rejected runtime-oracle checkout — the RUNTIME oracle (Task 1) stays a pure transcription with zero service-graph imports; only the WITNESS that pins it is sourced from real old code, once, offline.
    - The input covers BOTH regimes: an `estimated_start > 0` account (byte-identity control shape) AND an `estimated_start <= 0` account (the LTP068 profits-withdrawn bug that yields the inflated series).
    - Mutation: dropping the `estimated_start <= 0 -> account_balance` fallback from the Task-1 oracle turns this test RED (the transcription diverges from the real-code witness).
  </behavior>
  <action>
    Commit `tests/fixtures/golden_parity/oracle_input.json` (a small deterministic
    trades/daily_pnl + account_balance pair covering both regimes above).
    Generate `oracle_pre73_expected.json` ONCE from the real pre-73 module via a
    throwaway `git worktree add <tmp> 9a1e7b8e` + import of
    `trades_to_daily_returns_with_status` (document the exact generation command
    in a comment at the top of the test) so the golden is an INDEPENDENT witness
    of the true old behaviour — never derived from the transcription. Add
    `tests/test_golden_parity.py::test_oracle_matches_pre73_golden` asserting the
    live (transcribed) oracle reproduces that witness within rtol 1e-9 via
    `pd.testing.assert_series_equal`. Run in the CI-3.12 venv (local 3.14 SIGSEGVs
    on pandas). Do NOT print USD magnitudes in any assertion message.
  </action>
  <verify>
    <automated>cd analytics-service &amp;&amp; PYTHONPATH=. python -m pytest tests/test_golden_parity.py::test_oracle_matches_pre73_golden -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_golden_parity.py::test_oracle_matches_pre73_golden` exits 0.
    - The golden JSON's provenance (captured from the REAL old module at ref 9a1e7b8e via worktree import, not from the transcription) is documented in a comment.
    - Neutering the oracle (removing the `estimated_start<=0 -> account_balance` fallback) makes the test FAIL — verify by a scratch mutation, then revert.
    - The full analytics suite stays at/above baseline (3147 passed) in the CI-3.12 venv.
  </acceptance_criteria>
  <done>The oracle is golden-pinned against an independent pre-73 real-code witness; the pin is proven mutation-honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture JSON -> oracle | Deterministic committed inputs; no live/untrusted data. |
| CI logs <- harness | Gate/test output must never carry raw USD magnitudes (account-size leak class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Information Disclosure | golden_parity oracle + test assertion strings | mitigate | Oracle/tests emit series/booleans; never print raw USD NAV/flow magnitudes (T-73-02/T-77-02 discipline). Assertion messages omit dollar values. |
| T-78-03 | Tampering | frozen oracle drifting from pre-73 truth | mitigate | Independent golden captured from real code at 9a1e7b8e (worktree import, not the transcription); mutation-honest pin fails on any drift. |
| T-78-SC | Tampering | package installs | accept | No package installs this phase (RESEARCH: no new deps). Nothing to audit. |
</threat_model>

<verification>
- `pytest tests/test_golden_parity.py::test_oracle_matches_pre73_golden -x` exits 0 in the CI-3.12 venv.
- The oracle carries a `9a1e7b8e` provenance comment on both branches.
- The golden witness is generated from the real old module via worktree import (documented), independent of the transcription.
- A scratch mutation (drop the `<=0` fallback) proves the pin goes RED, then reverted.
- Full analytics suite >= 3147 passed / 92 skipped.
</verification>

<success_criteria>
The frozen anchor-to-today oracle exists as pure source (both branches,
provenance-commented to 9a1e7b8e), reproduces an independently-captured pre-73
golden series (from the real old module) within rtol 1e-9, and its pin is proven
mutation-honest.
</success_criteria>

<output>
Create `.planning/phases/78-golden-parity-p72-acceptance-gating/78-01-SUMMARY.md` when done.
</output>
