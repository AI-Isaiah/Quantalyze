---
phase: 78-golden-parity-p72-acceptance-gating
plan: 02
type: execute
wave: 2
depends_on: [78-01]
files_modified:
  - analytics-service/scripts/golden_parity.py
  - analytics-service/tests/test_golden_parity.py
  - analytics-service/tests/fixtures/golden_parity/panel_fixtures.py
autonomous: true
requirements: [ACC-01]

must_haves:
  truths:
    - "A flow-less control per venue (Deribit/OKX/Bybit/Binance) classifies UNCHANGED on the returns series"
    - "An LTP068-shaped flow-heavy fixture classifies FLOW_MOVED"
    - "The panel gate exits nonzero on ANY UNEXPLAINED delta"
    - "A flow-less control's CAGR shift buckets as REANNUALIZATION (metric-only), not UNEXPLAINED — the no-move invariant is keyed on the SERIES"
    - "Neutering classify_delta or the panel gate, or flipping has_flows=True for a control, makes the self-test RED"
  artifacts:
    - path: "analytics-service/scripts/golden_parity.py"
      provides: "gate_account + main() panel driver over classify_delta (frozen oracle OLD, live core NEW)"
      contains: "def gate_account"
    - path: "analytics-service/tests/test_golden_parity.py"
      provides: "Mutation-honest self-test (flowless->UNCHANGED, LTP068->FLOW_MOVED, any-UNEXPLAINED-fails, series-vs-CAGR)"
    - path: "analytics-service/tests/fixtures/golden_parity/panel_fixtures.py"
      provides: "Per-venue flow-less control fixtures + LTP068-shaped flow-heavy fixture"
  key_links:
    - from: "analytics-service/scripts/golden_parity.py::gate_account"
      to: "analytics-service/services/parity_diff.py::classify_delta"
      via: "import + call with caller-supplied has_flows"
      pattern: "classify_delta"
    - from: "analytics-service/scripts/golden_parity.py::gate_account"
      to: "analytics-service/services/nav_twr.py::reconstruct_nav_and_twr"
      via: "live NEW-core call (via combine_realized_and_funding / trades_to_daily_returns_with_status)"
      pattern: "reconstruct_nav_and_twr|combine_realized_and_funding|trades_to_daily_returns_with_status"
---

<objective>
Build the ACC-01 panel gate: a thin driver over the already-shipped
`services/parity_diff.py::classify_delta` primitive that, per panel account,
computes the OLD series (Plan-01 frozen oracle) and the NEW series (live
flow-aware core), classifies the delta, asserts the expected bucket, and exits
nonzero on any mismatch or any UNEXPLAINED. Back it with the CI-enforced,
mutation-honest self-test that IS the real gate.

Purpose: This is the HARD BLOCKER that authorizes the production flip. Flow-less
controls per venue prove byte-identity survived the shared-path refactor (P74
pins hold end-to-end); an LTP068-shaped fixture proves the honest move happens;
zero UNEXPLAINED proves every delta is accounted for.
Output: `gate_account` + `main()` in `scripts/golden_parity.py`, per-venue
fixtures, and the mutation-honest `tests/test_golden_parity.py` gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-CONTEXT.md
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-RESEARCH.md
@.planning/phases/78-golden-parity-p72-acceptance-gating/78-01-SUMMARY.md

<interfaces>
<!-- REUSE these; do NOT reinvent. All verified at HEAD. -->

services/parity_diff.py:
  classify_delta(old_returns: pd.Series, new_returns: pd.Series, *,
                 old_metrics: Mapping|None = None, new_metrics: Mapping|None = None,
                 has_flows: bool = False, rtol: float = 1e-9,
                 reannualization_factor: float = 365/252,
                 reannualization_rtol: float = 1e-6) -> str
  UNCHANGED, REANNUALIZATION, FLOW_MOVED, UNEXPLAINED, BUCKET_LABELS, REANNUALIZATION_FACTOR
  # series-unchanged + reannualization live entirely in classify_delta — do NOT re-key on CAGR.

NEW flow-aware core (call, do NOT reimplement the chain-link):
  services/transforms.py:
    trades_to_daily_returns_with_status(trades, account_balance=None, balance_error=False, *,
                                        external_flows=None, open_unrealized_usd=0.0) -> (pd.Series, NavTWRMeta)
  services/broker_dailies.py:
    combine_realized_and_funding(...)   # the daily_pnl/LTP path
  services/nav_twr.py:
    reconstruct_nav_and_twr(...)        # the terminus the two above delegate to

metrics (read-only; do NOT modify metrics.py):
  services/metrics.py::compute_all_metrics(returns, periods_per_year=...).metrics_json -> {"cagr": ..., "calmar": ..., ...}
  # HEAD compute_all_metrics puts CAGR/Calmar on the 365-calendar clock. To EXERCISE the
  # REANNUALIZATION bucket you must feed classify_delta an OLD metrics dict on the 252 basis
  # (see Task 3 LOW-3 note) — computing both sides at HEAD yields identical scalars (UNCHANGED, not REANNUALIZATION).

OLD series (from Plan 01):
  scripts/golden_parity.py::old_anchor_to_today_returns(daily_pnl, account_balance) -> pd.Series

Existing fixture bedrock to reuse/extend:
  tests/fixtures/deribit_flow_fixtures.py: linear_flow_day_rows, inverse_flow_day_with_index_rows,
    dominating_withdrawal_rows, pure_flow_no_trade_rows
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Per-venue panel fixtures (flow-less controls + LTP068-shape)</name>
  <files>analytics-service/tests/fixtures/golden_parity/panel_fixtures.py</files>
  <read_first>
    - analytics-service/tests/fixtures/deribit_flow_fixtures.py — the fixture-row shape and helpers to reuse/extend.
    - analytics-service/scripts/golden_parity.py — the oracle signature the fixtures feed (Plan 01).
    - .planning/phases/78-golden-parity-p72-acceptance-gating/78-RESEARCH.md (Sampling Rate + Validation Architecture) — "≥1 flow-less byte-identity control per venue + the 3 LTP movement cases".
  </read_first>
  <action>
    Create `tests/fixtures/golden_parity/panel_fixtures.py` exposing: (a) one
    flow-less control per live venue (Deribit, OKX, Bybit, Binance) as
    `(daily_pnl, account_balance)` pairs with `estimated_start > 0` (the
    byte-identity regime where NEW `external_flows=None` is algebraically
    identical to the OLD oracle per the P74 pins), and (b) an LTP068-shaped
    flow-heavy fixture (profits-withdrawn `estimated_start <= 0` and/or a real
    dated external flow) that MUST move. Reuse `deribit_flow_fixtures.py` helpers
    where the shapes overlap. Each fixture is tagged with its expected bucket and
    its `has_flows` truth. Use synthetic/recorded rows only — NO live keys (this
    is the CI-gated portion). Do NOT bake raw USD magnitudes into log/print paths.
  </action>
  <verify>
    <automated>cd analytics-service &amp;&amp; PYTHONPATH=. python -c "from tests.fixtures.golden_parity import panel_fixtures as p; print(len(p.flowless_controls()))"</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one flow-less control per venue (Deribit, OKX, Bybit, Binance) — 4 controls minimum, each `has_flows=False`.
    - At least one LTP068-shaped flow-heavy fixture tagged `has_flows=True`, expected bucket FLOW_MOVED.
    - Flow-less controls use the `estimated_start > 0` regime (byte-identity precondition).
    - No live-key/network access; fixtures import cleanly under PYTHONPATH=.
  </acceptance_criteria>
  <done>Per-venue flow-less controls + an LTP068-shaped mover exist as importable fixtures with expected-bucket + has_flows tags.</done>
</task>

<task type="auto">
  <name>Task 2: Panel-gate driver (gate_account + main) over classify_delta</name>
  <files>analytics-service/scripts/golden_parity.py</files>
  <read_first>
    - analytics-service/services/parity_diff.py — `classify_delta` signature + bucket constants (the primitive to drive; do NOT reinvent).
    - analytics-service/services/transforms.py + broker_dailies.py + nav_twr.py — the NEW-core entry points to call for the NEW series.
    - analytics-service/services/metrics.py (compute_all_metrics -> metrics_json cagr/calmar) — read-only; do NOT modify.
    - analytics-service/scripts/golden_parity.py — the oracle from Plan 01 (the OLD side).
  </read_first>
  <action>
    Extend `scripts/golden_parity.py` with `gate_account(daily_pnl,
    account_balance, *, external_flows, open_unrealized_usd, has_flows,
    expected_bucket) -> bool`: compute OLD via `old_anchor_to_today_returns`,
    NEW via the live core (`combine_realized_and_funding` -> `reconstruct_nav_and_twr`
    for the daily_pnl/LTP path; `trades_to_daily_returns_with_status` for the
    individual-trades path — pick by fixture shape, do NOT reimplement the
    chain-link), derive `old_metrics`/`new_metrics` as `{"cagr","calmar"}` from
    `compute_all_metrics(...).metrics_json`, then call `classify_delta(old, new,
    old_metrics=..., new_metrics=..., has_flows=has_flows)`. Assert `bucket !=
    UNEXPLAINED` (fail-closed) and return `bucket == expected_bucket`. Add
    `main()` that iterates the panel, accumulates buckets, and exits NONZERO on
    any mismatch or any UNEXPLAINED. `has_flows` is ALWAYS caller-supplied from
    the panel (never inferred) — flow-less controls pass `has_flows=False` so any
    movement fails closed as UNEXPLAINED. Emit ONLY buckets + counts + booleans;
    NEVER raw USD NAV/flow/series magnitudes (T-78-01). Keep the driver read-only
    — never mutate production factsheets (shadow/dual-compute, CONTEXT lock). Do
    NOT wire this script into `.github/workflows/ci.yml` (RESEARCH Pitfall 4 — the
    self-test in Task 3 is the CI gate; the driver is fixture/live-run only).
  </action>
  <verify>
    <automated>cd analytics-service &amp;&amp; PYTHONPATH=. python -c "from scripts.golden_parity import gate_account, main"</automated>
  </verify>
  <acceptance_criteria>
    - `gate_account` imports and calls `classify_delta` from `services.parity_diff` (grep-verifiable).
    - The NEW series comes from the live core (`reconstruct_nav_and_twr` / `combine_realized_and_funding` / `trades_to_daily_returns_with_status`) — no reimplemented chain-link.
    - `gate_account` asserts `bucket != UNEXPLAINED`; `main()` exits nonzero on any UNEXPLAINED or bucket mismatch.
    - `has_flows` is a required caller-supplied param (no inference).
    - No write path to production factsheet tables; no `.github/workflows/ci.yml` edit.
  </acceptance_criteria>
  <done>The panel-gate driver classifies each account via classify_delta, fails closed on UNEXPLAINED, and exits nonzero on any panel breach.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mutation-honest self-test (the CI-enforced gate)</name>
  <files>analytics-service/tests/test_golden_parity.py</files>
  <read_first>
    - .planning/phases/78-golden-parity-p72-acceptance-gating/78-RESEARCH.md (Mutation-Honest Self-Test — the 5 mutations that MUST go RED).
    - analytics-service/services/parity_diff.py — bucket semantics + the series-vs-CAGR distinction (LOW-3).
    - analytics-service/tests/fixtures/golden_parity/panel_fixtures.py — the fixtures under test (Task 1).
    - analytics-service/scripts/golden_parity.py — gate_account/main (Task 2).
  </read_first>
  <behavior>
    - test_flowless_controls_unchanged: each per-venue flow-less control -> classify_delta returns UNCHANGED on the series (has_flows=False).
    - test_flowless_control_cagr_is_reannualization: a control whose CAGR shifted by 365/252 with a byte-identical series -> REANNUALIZATION (NOT UNEXPLAINED) — proves the no-move invariant is keyed on the SERIES, not CAGR (LOW-3). REACHABILITY NOTE (advisory): because the Task-2 driver computes BOTH old_metrics and new_metrics via HEAD `compute_all_metrics` (365-clock), on a byte-identical series they come out identical and the REANNUALIZATION branch is UNREACHABLE. This test MUST therefore construct the OLD `old_metrics` on the 252-day basis explicitly — either a synthetic `{"cagr","calmar"}` dict where cagr is the pre-TWR-05 252-clock value, OR `compute_all_metrics(series, periods_per_year=252)` on a frozen 252 clock — and feed it to `classify_delta` alongside the 365-basis `new_metrics`, so the genuine 365/252 shift is exercised and buckets as REANNUALIZATION. Do NOT drive this case through the Task-2 gate_account default (both-at-HEAD) — call classify_delta directly with the asymmetric metrics.
    - test_ltp068_shape_flow_moved: the LTP068-shaped fixture (has_flows=True) -> FLOW_MOVED.
    - test_any_unexplained_fails_gate: an injected unexplained-move fixture makes main()/gate_account exit nonzero / raise.
    - Mutation honesty (each MUST turn a test RED): (1) force classify_delta -> UNCHANGED unconditionally; (2) drop the `assert bucket != UNEXPLAINED` from the driver; (3) flip a control's has_flows to True.
  </behavior>
  <action>
    Add `tests/test_golden_parity.py` cases covering all four behaviours above,
    driven by the Task-1 fixtures. Each case pins the expected bucket per account.
    For the LOW-3 case, exercise the REANNUALIZATION bucket with an ASYMMETRIC
    metrics pair (252-basis old vs 365-basis new — see the behavior note) so the
    365/252 shift is real and the branch is reachable; assert the flow-less
    control's series stays UNCHANGED while its scalars bucket REANNUALIZATION,
    never UNEXPLAINED. Run in the CI-3.12 venv (local 3.14 SIGSEGVs). The file is
    auto-collected by the existing `pytest --cov=services` CI job — do NOT edit
    ci.yml. Keep assertion messages free of USD magnitudes.
  </action>
  <verify>
    <automated>cd analytics-service &amp;&amp; PYTHONPATH=. python -m pytest tests/test_golden_parity.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `tests/test_golden_parity.py` exits 0 with the four named cases present.
    - The LOW-3 case feeds classify_delta a 252-basis old_metrics vs 365-basis new_metrics (asymmetric) so REANNUALIZATION is actually reached — NOT both-at-HEAD (which yields UNCHANGED).
    - Flow-less control classified UNCHANGED on series; its (asymmetric) CAGR shift buckets REANNUALIZATION, never UNEXPLAINED.
    - LTP068-shaped fixture classified FLOW_MOVED; an injected UNEXPLAINED move makes the gate exit nonzero.
    - Neutering classify_delta (force UNCHANGED), dropping the `assert bucket != UNEXPLAINED`, OR flipping a control's has_flows=True each makes at least one test FAIL — verify each by scratch mutation, then revert.
    - Full analytics suite stays at/above baseline (3147 passed) in the CI-3.12 venv.
  </acceptance_criteria>
  <done>The mutation-honest gate is green in CI and proven to go RED under classifier-neuter, gate-neuter, and has_flows-flip mutations; the REANNUALIZATION bucket is genuinely exercised.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture panel -> driver | Synthetic committed inputs; CI-gated, no live keys. |
| driver -> production factsheet tables | Read-only/shadow; the driver must NEVER write (CONTEXT dual-compute lock). |
| CI logs <- gate | Buckets/counts/booleans only; no raw USD magnitudes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Information Disclosure | gate_account / main / test messages | mitigate | Emit classification buckets + counts + booleans; never print USD NAV/flow/series magnitudes (T-73-02/T-76-03-LEAK discipline). |
| T-78-04 | Tampering | has_flows=True passed for a flow-less control | mitigate | has_flows is caller-supplied and pinned False for controls; a dedicated mutation test goes RED if flipped (defeat-the-net guard). |
| T-78-05 | Tampering | driver mutating production factsheets during the run | mitigate | Driver is read-only/shadow by construction; acceptance criteria forbid any write path to strategy_analytics/csv_daily_returns. |
| T-78-SC | Tampering | package installs | accept | No package installs this phase (no new deps). Nothing to audit. |
</threat_model>

<verification>
- `pytest tests/test_golden_parity.py -x` exits 0 in the CI-3.12 venv.
- Three scratch mutations (classifier-neuter, gate-neuter, has_flows-flip) each proven to go RED, then reverted.
- The LOW-3 case is proven to reach REANNUALIZATION (asymmetric 252-vs-365 metrics), not UNCHANGED.
- No `.github/workflows/ci.yml` change; the self-test is auto-collected by the existing analytics pytest job.
- Full analytics suite >= 3147 passed / 92 skipped.
</verification>

<success_criteria>
The ACC-01 panel gate is complete and CI-enforced: flow-less controls per venue
classify UNCHANGED (series) with a genuinely-exercised REANNUALIZATION CAGR shift,
an LTP068-shaped fixture classifies FLOW_MOVED, any UNEXPLAINED exits the gate
nonzero, and the self-test is proven mutation-honest under classifier-neuter,
gate-neuter, and has_flows-flip.
</success_criteria>

<output>
Create `.planning/phases/78-golden-parity-p72-acceptance-gating/78-02-SUMMARY.md` when done.
</output>
