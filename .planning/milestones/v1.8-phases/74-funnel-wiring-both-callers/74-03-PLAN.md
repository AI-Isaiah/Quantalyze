---
phase: 74-funnel-wiring-both-callers
plan: 03
type: tdd
wave: 3
depends_on: ["74-02"]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_analytics_runner.py
autonomous: true
requirements: [TWR-04]

must_haves:
  truths:
    - "The three NAV-guard keys (dust_nav_guard, negative_nav_guard, flow_dominated_guard) from returns_meta lift into top_level_flags alongside used_heuristic_capital (analytics_runner.py ~:1704-1713)"
    - "The status-promotion predicate (analytics_runner.py ~:1775-1778) promotes to complete_with_warnings when any guard key OR used_heuristic_capital OR balance_error fires"
    - "A no-guard, flow-less, estimated_start>0 account stays computation_status=complete (status-identical to today) — the 8 exact-string consumers are unaffected"
    - "A NavReconstructionError at the analytics_runner:1309 callsite yields a terminal permanent failed (not a retried unknown) — a typed catch stamps failed and raises HTTPException 4xx so classify_exception buckets it permanent"
    - "sync_strategy_analytics_status (mig-038) is NOT modified; a guard-flagged SUCCESS keeps its compute_job done"
  artifacts:
    - path: "analytics-service/services/analytics_runner.py"
      provides: "guard-key DQF lift + promotion predicate extension + NavReconstructionError typed catch"
      contains: "negative_nav_guard"
  key_links:
    - from: "analytics_runner.py returns_meta"
      to: "top_level_flags / computation_status"
      via: "guard-key lift + promotion predicate"
      pattern: "negative_nav_guard"
    - from: "analytics_runner.py:1309 callsite"
      to: "permanent failed stamp"
      via: "except NavReconstructionError -> HTTPException 4xx"
      pattern: "NavReconstructionError"
---

<objective>
Wire the honest core's DQ signal and fail-loud error type into the analytics_runner
call site (analytics_runner.py:1309 — the CSV/stored-trades path and the status
chokepoint for all 8 frontend consumers). Two behaviors:

1. Status promotion (CONTEXT grey-area #4, Pitfall #5): lift the three NAV-guard keys
   from the returns meta into top-level DQF and extend the promotion predicate so a
   broken-NAV-denominator run promotes to complete_with_warnings — while a no-guard,
   flow-less account stays exactly `complete` (byte- AND status-identical; the 8
   exact-string `=== "complete"` consumers keep working). Do NOT swap to the raw
   computation_status_hint (RESEARCH anti-pattern) — extend the DQF predicate.

2. Fail-loud propagation (CONTEXT grey-area #5, Pitfall #4): a NavReconstructionError
   (non-finite/non-numeric anchor or pnl) must land PERMANENT, not the retried-forever
   `unknown` it becomes as a bare ValueError wrapped to HTTPException(500). Add a typed
   catch mirroring the LedgerValuationError pattern.

Explicitly out of scope (verify, do not change): the 8 frontend consumers (deferred
courtesy migration) and sync_strategy_analytics_status / mig-038 (read-only this phase).

Purpose: route the caller honestly end-to-end — the guard signal reaches the factsheet
status and a structural fault reaches a terminal gate, not an infinite spinner.
Output: DQF lift + promotion extension + typed permanent catch, integration-tested.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Python for ALL analytics pytest (MANDATORY — local 3.14 SIGSEGVs on pandas):
`/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python`
Run from `analytics-service/`. TDD: test-RED then feat-GREEN. `.planning/` is gitignored — commit CODE/tests only.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/74-funnel-wiring-both-callers/74-CONTEXT.md
@.planning/phases/74-funnel-wiring-both-callers/74-RESEARCH.md
@.planning/phases/74-funnel-wiring-both-callers/74-01-SUMMARY.md
@.planning/phases/74-funnel-wiring-both-callers/74-02-SUMMARY.md

@analytics-service/services/analytics_runner.py
@analytics-service/services/nav_twr.py

<interfaces>
analytics_runner.py:
  :1309  returns, returns_meta = trades_to_daily_returns_with_status(trades, account_balance=..., balance_error=False)
  :1313  if len(returns) < 2: stamp failed + raise HTTPException(400)   # NaN entries still counted
  :1704-1713  DQF lift: `if returns_meta["used_heuristic_capital"] and not (...): top_level_flags["used_heuristic_capital"]=True`
              `if returns_meta["balance_error"]: top_level_flags["balance_error"]=True`
  :1775-1788  consumer_specific_flags = used_heuristic_capital OR balance_error;
              computation_status_value = "complete_with_warnings" if consumer_specific_flags else "complete"
  :1896  except Exception as e: stamp failed + raise HTTPException(status_code=500)   # generic -> classify_exception -> unknown -> retried

services/nav_twr.py:
  NavReconstructionError(ValueError)  # import from services.nav_twr
  NavTWRMeta guard keys carried on returns_meta by 74-02: dust_nav_guard / negative_nav_guard / flow_dominated_guard (present only when fired).

job_worker.classify_exception (:311-380): HTTPException 400/422 (400<=s<500, not 408/429/403/404) -> ("permanent", ...).
LedgerValuationError typed-catch template: job_worker.py:1916-1941 (scrub + stamp failed + return permanent).
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Lift NAV-guard keys into DQF + extend the status-promotion predicate</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/tests/test_analytics_runner.py</files>
  <read_first>
    - analytics_runner.py:1704-1713 (used_heuristic_capital / balance_error DQF lift) and :1751-1788 (the long comment block + consumer_specific_flags predicate + the 8-consumer list).
    - services/nav_twr.py:64-76,268-290 (guard key names + hint semantics).
    - 74-02-SUMMARY.md — confirm which guard keys the delegated meta now carries.
  </read_first>
  <behavior>
    - RED: an estimated_start<=0 fixture through the runner path sets computation_status='complete_with_warnings' and top_level_flags contains negative_nav_guard=True (fails today — guard keys are dropped, status stays complete).
    - RED: top_level_flags surfaces dust_nav_guard / flow_dominated_guard when those fire.
    - GREEN-invariant: a no-guard, flow-less, estimated_start>0 fixture keeps computation_status=='complete' and does NOT add any guard key (status-identical; the 8 consumers unaffected).
  </behavior>
  <action>
    In the DQF lift block (~:1704-1713), after the existing used_heuristic_capital/balance_error
    handling, read the three guard keys from returns_meta (dust_nav_guard, negative_nav_guard,
    flow_dominated_guard) and set each into top_level_flags (guarding for the None-vs-empty
    dict distinction the upsert relies on) ONLY when present-and-True — same additive shape as
    used_heuristic_capital. Extend the consumer_specific_flags predicate (~:1775-1778) to OR in
    the three guard keys so a guarded run promotes computation_status to complete_with_warnings.
    Do NOT touch the section-level flags or the `complete` path for no-guard runs — a flow-less
    estimated_start>0 account has zero guard keys and MUST stay `complete` (the 8 exact-string
    consumers listed at :1760-1767). Do NOT read computation_status_hint as the source (keep the
    deliberate DQF-predicate design). Update the explanatory comment to note the guard keys are
    now consumer-promoting (SC-4 semantics preserved).
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_analytics_runner.py -k "status_guard_promotion or complete_unchanged" -x -q</automated>
  </verify>
  <acceptance_criteria>
    Guard keys lift into top_level_flags and promote status to complete_with_warnings; a no-guard
    flow-less account stays complete with no guard key added (status-identical, consumers safe).
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Typed NavReconstructionError permanent catch at the analytics_runner callsite</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/tests/test_analytics_runner.py</files>
  <read_first>
    - analytics_runner.py:1896-1912 (the generic `except Exception as e:` that stamps failed + raises HTTPException(500)).
    - job_worker.py:311-380 (classify_exception: 4xx 400/422 -> permanent; 5xx -> unknown/retried) and :1916-1941 (LedgerValuationError typed-catch template).
    - services/nav_twr.py:78-108 (NavReconstructionError + _coerce_float triggers).
  </read_first>
  <behavior>
    - RED: a malformed-input compute (a non-finite/non-numeric pnl or account_balance reaching the core) stamps strategy_analytics computation_status='failed' AND surfaces as permanent (HTTPException status in 400..499, not 500) so classify_exception buckets it permanent — asserts NOT retried. Fails today (bare ValueError -> generic 500 -> unknown -> retried).
    - GREEN-invariant: a genuinely transient error still reaches the generic handler (stays retryable) — the typed catch is narrow to NavReconstructionError only.
  </behavior>
  <action>
    Import NavReconstructionError from services.nav_twr. Add `except NavReconstructionError as exc:`
    BEFORE the generic `except Exception as e:` (~:1896). In it: scrub the message
    (scrub_freeform_string, same helper the codebase uses — do not log raw NAV/USD), upsert
    strategy_analytics computation_status='failed' with the scrubbed computation_error, then
    `raise HTTPException(status_code=422, detail=<scrubbed>)` so classify_exception maps it
    permanent (422 is in the 400<=s<500 permanent bucket, not 408/429/403/404). Mirror the
    LedgerValuationError structure at job_worker.py:1916-1941. Keep it narrow (only the typed
    subclass) so a transient ValueError escaping elsewhere still falls through to the generic
    handler and stays transient-retryable. Do NOT modify sync_strategy_analytics_status / any
    migration — a legitimate permanent failed here is exactly what mig-038 should reflect.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_analytics_runner.py -k "nav_error_permanent" -x -q</automated>
  </verify>
  <acceptance_criteria>
    NavReconstructionError at :1309 stamps failed and raises a 4xx that classify_exception buckets
    permanent (not retried); the catch is narrow to the typed subclass; no migration touched.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core meta → strategy_analytics.computation_status → 8 frontend consumers | DQ signal crosses into the public factsheet status |
| malformed input → analytics_runner → compute_jobs retry classifier | a structural fault must classify permanent, not transient |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02 | Denial of Service | NavReconstructionError mis-classified transient -> infinite retry on a permanent fault | mitigate | Task 2 typed catch -> HTTPException 422 -> classify_exception permanent |
| T-74-05 | Tampering | guarded (NaN) account rendered as canonical-complete on the public factsheet | mitigate | Task 1 promotes guard runs to complete_with_warnings; no-guard runs stay complete (consumers coherent) |
| T-74-03 | Information disclosure | account-size USD in stamped computation_error | mitigate | scrub_freeform_string before stamping (Task 2) |

No package installs → no supply-chain (SC) row.
</threat_model>

<verification>
- `pytest tests/test_analytics_runner.py -q` green.
- Integration: guard fixture -> complete_with_warnings + guard key in DQF; no-guard fixture -> complete unchanged.
- Confirm no migration file touched and sync_strategy_analytics_status unchanged (guard-flagged SUCCESS stays a `done` compute_job).
- Per-wave merge suite: `pytest tests/test_transforms.py tests/test_analytics_runner.py tests/test_csv_analytics_runner.py -q`.
</verification>

<success_criteria>
- Guard signal promotes status honestly; flow-less `complete` accounts unchanged (SC-4 status invariant; 8 consumers safe).
- NavReconstructionError lands permanent at the analytics_runner callsite (no retry-forever).
- mig-038 untouched; no consumer migration in this phase.
</success_criteria>

<output>
Create `.planning/phases/74-funnel-wiring-both-callers/74-03-SUMMARY.md` when done.
</output>
