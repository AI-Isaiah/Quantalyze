---
phase: 74-funnel-wiring-both-callers
plan: 04
type: tdd
wave: 3
depends_on: ["74-02"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_derive_broker_dailies_dualmode.py
  - analytics-service/tests/test_csv_analytics_runner.py
autonomous: true
requirements: [TWR-04]

must_haves:
  truths:
    - "A NavReconstructionError raised by combine_realized_and_funding on the broker path (job_worker.py ~:2010) is caught typed and returns DispatchResult FAILED with error_kind=permanent (not a retried unknown), stamping a scrubbed terminal failed"
    - "The csv_daily_returns upsert (job_worker.py:2062-2082) applies the 74-01 NaN policy: guarded-day NaN rows are handled per the finding (skip-NaN-row or persist-null) so no crash and no fabricated magnitude is written, on BOTH is_key_mode and strategy-mode branches"
    - "An estimated_start<=0 account renders honest end-to-end (NaN/flagged, never a fabricated magnitude, never a crash) through the broker + CSV analytics path"
    - "sync_strategy_analytics_status (mig-038) is NOT modified; a guard-flagged SUCCESS keeps its compute_job done (guards are warnings, not failures)"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "broker-path NavReconstructionError permanent catch + NaN-safe csv_daily_returns upsert"
      contains: "NavReconstructionError"
  key_links:
    - from: "job_worker.py:2010 combine_realized_and_funding"
      to: "DispatchResult permanent failed"
      via: "except NavReconstructionError"
      pattern: "NavReconstructionError"
    - from: "job_worker.py:2062-2082 csv_daily_returns upsert"
      to: "NaN-safe daily_return rows"
      via: "74-01 NaN policy (skip/persist)"
      pattern: "daily_return"
---

<objective>
Wire the broker call site (job_worker.py -> broker_dailies.combine_realized_and_funding
:2010, reached by every Bybit/Binance/OKX/Deribit key + the reconcile harness) honestly:

1. Fail-loud propagation (CONTEXT grey-area #5, Pitfall #4): the combine call at :2010
   sits OUTSIDE the deribit LedgerValuationError try, so a NavReconstructionError there
   escapes to the generic classifier and is retried forever as `unknown`. Add a typed
   catch that returns a permanent FAILED, mirroring the LedgerValuationError disposition.

2. NaN serialization (Pitfall 3, resolving 74-01's finding): the returns Series can now
   carry guarded-day NaN for an estimated_start<=0 account. The csv_daily_returns upsert
   does `float(val)`; apply the 74-01 policy so NaN is handled honestly on both is_key_mode
   and strategy-mode branches — never a crash, never a fabricated magnitude.

3. End-to-end honesty: prove an estimated_start<=0 account renders NaN/flagged all the way
   through, never a magnitude.

Explicitly out of scope (verify, do not change): sync_strategy_analytics_status / mig-038
(read-only). A guard-flagged SUCCESS must keep its compute_job `done` so the bridge maps it
`complete` and the runner's complete_with_warnings write stays authoritative — do NOT turn a
guard (a warning) into a failed_final compute_job (that would poison mig-038 retry, the STATE blocker).

Purpose: the second caller goes honest — structural faults terminate, guarded days persist
truthfully. Output: broker permanent catch + NaN-safe upsert + end-to-end honesty test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Python for ALL analytics pytest (MANDATORY — local 3.14 SIGSEGVs on pandas):
`/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python`
Run from `analytics-service/`. TDD: test-RED then feat-GREEN. `.planning/` is gitignored — commit CODE/tests only.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/74-funnel-wiring-both-callers/74-CONTEXT.md
@.planning/phases/74-funnel-wiring-both-callers/74-RESEARCH.md
@.planning/phases/74-funnel-wiring-both-callers/74-01-SUMMARY.md
@.planning/phases/74-funnel-wiring-both-callers/74-02-SUMMARY.md

@analytics-service/services/job_worker.py
@analytics-service/services/broker_dailies.py
@analytics-service/services/nav_twr.py

<interfaces>
job_worker.py (derive_broker_dailies):
  :2010  returns, meta = combine_realized_and_funding(realized, funding, account_balance=equity, balance_error=...)
         # OUTSIDE the deribit try/except (that try catches LedgerValuationError at :1916-1941)
  :2013  if len(returns) < 2: ... insufficient-history -> DONE (NaN entries counted in len)
  :2038-2053  _mark_insufficient(): strategy-mode upsert failed + data_quality_flags {"csv_source": True}  <-- stamp template for strategy-mode
  :1862-1878  _stamp_deribit_analytics_failed(): defined INSIDE the deribit branch only -> NOT in scope at :2010
  :2061-2095  csv_daily_returns upsert: is_key_mode rows (api_key_id,date,daily_return=float(val)) / strategy-mode rows (strategy_id,date,daily_return=float(val)); chunked _UPSERT_CHUNK=1000
  is_key_mode / strategy_id / api_key_id / allocator_id in scope; DispatchResult(outcome=..., error_message=..., error_kind=...)
  classify_exception (:311-380): return DispatchResult error_kind="permanent" is the terminal disposition.

services/nav_twr.py: NavReconstructionError (import from services.nav_twr). scrub_freeform_string is the message scrubber used across job_worker.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Typed NavReconstructionError permanent catch on the broker path</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>
    - job_worker.py:2001-2053 (the combine call at :2010, the RateLimitExceeded/finally around it, the <2 branch + _mark_insufficient template).
    - job_worker.py:1916-1941 (LedgerValuationError typed-catch disposition to mirror: scrub + stamp + return permanent).
    - job_worker.py:1862-1878 (_stamp_deribit_analytics_failed — note it is NOT in scope at :2010; reuse the _mark_insufficient inline-upsert shape instead).
    - services/nav_twr.py:78-108 (NavReconstructionError triggers).
  </read_first>
  <behavior>
    - RED: a broker compute whose combined records force a non-finite/non-numeric value into the core (NavReconstructionError) returns DispatchResult(outcome=FAILED, error_kind="permanent") and, in strategy-mode, stamps strategy_analytics computation_status='failed' with a scrubbed message. Fails today (error escapes to generic classifier -> unknown -> retried).
    - GREEN-invariant: a healthy broker compute still returns DONE and upserts dailies unchanged.
  </behavior>
  <action>
    Import NavReconstructionError from services.nav_twr. Wrap the combine_realized_and_funding
    call at :2010 in `try/except NavReconstructionError as exc:`. In the handler: scrub the
    message via scrub_freeform_string (never log raw NAV/USD — T-73-02); for strategy-mode
    (not is_key_mode) upsert strategy_analytics computation_status='failed' with the scrubbed
    computation_error and data_quality_flags {"csv_source": True} using the same inline-upsert
    shape as _mark_insufficient (:2038-2053); for is_key_mode skip the stamp (no per-key
    analytics row, mirroring the existing <2 branch). Then `return DispatchResult(outcome=
    DispatchOutcome.FAILED, error_message=<scrubbed>, error_kind="permanent")`. Keep the catch
    narrow to the typed subclass so a transient ValueError still falls through to the generic
    handler. Do NOT touch any migration or sync_strategy_analytics_status; a guard-flagged
    SUCCESS (guards present but no exception) must still return DONE (guards are warnings) so it
    does not become a failed_final compute_job.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_derive_broker_dailies_dualmode.py -k "nav_error_permanent" -x -q</automated>
  </verify>
  <acceptance_criteria>
    NavReconstructionError on the broker path returns FAILED/permanent and stamps a scrubbed
    terminal failed in strategy-mode (no stamp in key-mode); healthy computes still DONE; catch
    narrow to the typed subclass; no migration touched.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: NaN-safe csv_daily_returns upsert + end-to-end estimated_start<=0 honesty</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_csv_analytics_runner.py, analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>
    - 74-01-SUMMARY.md — the per-sink NaN finding (TOLERATES vs NEEDS-A-GUARD + guard location). This is the authoritative policy input.
    - job_worker.py:2055-2095 (the csv_daily_returns upsert, both is_key_mode and strategy-mode rows_payload builders, `daily_return: float(val)`).
    - services/broker_dailies.py:107-116 (gap_fill fills MISSING days 0.0 but leaves guarded-day NaN as NaN).
  </read_first>
  <behavior>
    - RED (only if 74-01 finding = NEEDS-A-GUARD): a returns Series with guarded-day NaN upserts without crashing and writes NO fabricated magnitude for the NaN days (skip-NaN-row or persist-null per the finding) — on BOTH is_key_mode and strategy-mode branches. Fails today if float(NaN) is rejected by the column type.
    - RED: end-to-end — an estimated_start<=0 broker account produces a factsheet/analytics row that is flagged (complete_with_warnings) with NaN/absent guarded days, never a fabricated CAGR magnitude, never an exception.
    - GREEN-invariant: a healthy (all-finite) account upserts every day exactly as today.
  </behavior>
  <action>
    Apply the 74-01 NaN policy to BOTH rows_payload builders (:2062-2071 key-mode, :2074-2082
    strategy-mode). If the finding is skip-NaN-row: filter `for ts, val in returns.items() if
    pd.notna(val)` before building each payload (a guarded day has no honest return to persist).
    If the finding is persist-null: write daily_return=None for NaN days instead of float(val)
    (only if the column is nullable per the finding). Whichever the finding dictates, apply it
    identically to both branches and document the choice. Add an end-to-end honesty test asserting
    an estimated_start<=0 account flows through without exception, persists no fabricated magnitude
    on guarded days, and the strategy_analytics status is complete_with_warnings (not complete).
    If 74-01 found both sinks TOLERATE NaN, no upsert code change is needed — still add the
    end-to-end honesty test to lock the behavior. Do NOT alter the len(returns)<2 guards or the
    gap_fill semantics.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_csv_analytics_runner.py tests/test_derive_broker_dailies_dualmode.py -k "nan_account_honest or nan_upsert" -x -q</automated>
  </verify>
  <acceptance_criteria>
    Guarded-day NaN is handled per the 74-01 finding on both upsert branches (no crash, no
    fabricated magnitude); an estimated_start<=0 account renders honest end-to-end
    (complete_with_warnings, NaN/absent guarded days); healthy accounts upsert unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| broker exchange input → combine → core | untrusted realized/funding/equity amounts cross into NAV reconstruction |
| guarded-day NaN → csv_daily_returns → metrics/factsheet | a NaN must persist honestly, never as a magnitude, never crash |
| structural fault → compute_jobs retry classifier | permanent vs transient disposition |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-02 | Denial of Service | broker-path NavReconstructionError retried forever as unknown | mitigate | Task 1 typed catch -> DispatchResult permanent |
| T-74-01 | Tampering / Information disclosure | fabricated magnitude written for a guarded day, or a crash hiding the account | mitigate | Task 2 NaN-safe upsert per 74-01 finding + end-to-end honesty test |
| T-74-03 | Information disclosure | account-size USD in stamped computation_error | mitigate | scrub_freeform_string before stamping (Task 1) |
| T-74-06 | Tampering | guard warning mis-mapped to failed_final -> mig-038 retry-poisoning (STATE blocker) | mitigate | guard-flagged SUCCESS returns DONE (warning, not failure); no migration touched |

No package installs → no supply-chain (SC) row.
</threat_model>

<verification>
- `pytest tests/test_derive_broker_dailies_dualmode.py tests/test_csv_analytics_runner.py -q` green.
- Phase gate before /gsd:verify-work: FULL suite `pytest tests -q` green in the CI-3.12 venv (blast-radius phase — every venue flows through the changed path).
- Confirm mig-038 / sync_strategy_analytics_status untouched and guard-flagged SUCCESS keeps compute_job DONE.
</verification>

<success_criteria>
- Broker-path structural faults land permanent (no retry-forever); guarded days persist honestly (no crash, no magnitude).
- estimated_start<=0 accounts render honest end-to-end (complete_with_warnings + NaN/flagged).
- mig-038 untouched; guard warnings never become failed_final.
</success_criteria>

<output>
Create `.planning/phases/74-funnel-wiring-both-callers/74-04-SUMMARY.md` when done — record the
applied NaN policy (skip-row / persist-null / tolerated) and confirm the full suite is green.
</output>
