---
phase: 74-funnel-wiring-both-callers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_transforms.py
  - analytics-service/tests/test_broker_dailies.py
  - analytics-service/tests/test_csv_analytics_runner.py
autonomous: true
requirements: [TWR-03, TWR-04]

must_haves:
  truths:
    - "A written NaN-tolerance finding states, for EACH downstream sink (compute_all_metrics/compute_period_returns AND the csv_daily_returns float(val) upsert at job_worker.py:2068/2078), whether it TOLERATES or NEEDS-A-GUARD for a guarded-day NaN in the returns Series"
    - "Byte-identity snapshot pins capture today's EXACT returns Series for the daily_pnl branch and the individual-trades branch of trades_to_daily_returns_with_status on flow-less / estimated_start>0 fixtures"
    - "A byte-identity snapshot pin captures today's EXACT returns for broker_dailies.combine_realized_and_funding on a realized+funding flow-less fixture"
    - "All snapshot pins PASS against the current (pre-refactor) code so they form a revert-proof safety net before Wave 2 changes transforms.py"
  artifacts:
    - path: "analytics-service/tests/test_transforms.py"
      provides: "daily_pnl + individual-trades byte-identity snapshot pins (estimated_start>0)"
      contains: "def test_"
    - path: "analytics-service/tests/test_broker_dailies.py"
      provides: "combine_realized_and_funding byte-identity snapshot pin"
      contains: "combine_realized_and_funding"
    - path: "analytics-service/tests/test_csv_analytics_runner.py"
      provides: "NaN-tolerance characterization test + documented finding"
      contains: "def test_"
  key_links:
    - from: "test_transforms.py"
      to: "services.transforms.trades_to_daily_returns_with_status"
      via: "direct call on fixtures"
      pattern: "trades_to_daily_returns_with_status"
    - from: "test_broker_dailies.py"
      to: "services.broker_dailies.combine_realized_and_funding"
      via: "direct call on realized+funding fixture"
      pattern: "combine_realized_and_funding"
---

<objective>
Wave 0 pre-flight for the high-blast-radius transforms.py refactor. Two jobs, both
BEFORE any production code changes:

1. Resolve the one LOW-confidence dependency (RESEARCH A1 / Pitfall 3): does the
   downstream tolerate a guarded-day NaN in the returns Series? Verify BOTH sinks
   (compute_all_metrics/compute_period_returns AND the csv_daily_returns
   `float(val)` upsert) and write a definitive finding. This finding is the
   authoritative input to plans 74-03 and 74-04.
2. Author the byte-identity SNAPSHOT pins (the revert-proof safety net) that
   capture today's exact numbers for all three flow-less input shapes
   (daily_pnl branch, individual-trades branch, broker combine path) so Wave 2's
   delegation diff can be proven behavior-preserving.

This plan changes NO production code. It only adds tests and a documented finding.
The mutation-honest divergence / fallback-deletion pins (which assert the NEW
estimated_start<=0 behavior) are authored RED→GREEN inside plan 74-02 alongside
the diff — this plan's pins must stay GREEN across the whole phase.

Purpose: prove the shared path is safe to change and remove the last unknown before
touching the ONE function every venue flows through (Pitfall #12).
Output: green snapshot pins in three test files + a NaN-tolerance finding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Python for ALL analytics pytest (local 3.14 SIGSEGVs on pandas — MANDATORY):
`/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python`
Run pytest from the `analytics-service/` directory. `.planning/` is gitignored — commit CODE/tests only.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-funnel-wiring-both-callers/74-CONTEXT.md
@.planning/phases/74-funnel-wiring-both-callers/74-RESEARCH.md
@.planning/phases/74-funnel-wiring-both-callers/74-VALIDATION.md
@.planning/phases/73-pure-nav-twr-core/73-01-SUMMARY.md

@analytics-service/services/transforms.py
@analytics-service/services/broker_dailies.py
@analytics-service/services/nav_twr.py

<interfaces>
<!-- Contracts the pins call. Extracted from the working tree — no exploration needed. -->

services/transforms.py:
  trades_to_daily_returns_with_status(trades: list[dict], account_balance: float|None=None,
      balance_error: bool=False) -> tuple[pd.Series, ReturnsComputationMeta]
    - daily_pnl branch  (:120-176): rows carry order_type='daily_pnl'; price=abs $PnL, side buy=+/sell=-.
      estimated_start = account_balance - total_pnl; estimated_start>0 -> initial_capital=estimated_start.
    - individual-trades branch (:178-212): raw fills (price*quantity notional, fee); NO order_type='daily_pnl'.
    - both build equity = initial_capital + pnl.cumsum(); prev_equity = equity.shift(1).fillna(initial_capital);
      returns = pnl / prev_equity.
  ReturnsComputationMeta keys: used_heuristic_capital, balance_error, computation_status_hint.

services/broker_dailies.py:
  combine_realized_and_funding(realized_pnl_records: list[dict], funding_rows: Sequence[Mapping],
      account_balance: float|None, balance_error: bool=False) -> tuple[pd.Series, dict]
    - concatenates realized + funding_rows_to_daily_pnl_records(...), calls _with_status,
      then gap_fill_daily_returns (reindex every calendar day, fill 0.0).

Existing fixtures to model on: tests/test_accuracy.py:167-315 (trades_to_daily_returns known balance),
tests/test_transforms.py (heuristic + _with_status behavior), tests/test_nav_twr.py (2000/1500 divergence fixture).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: NaN-tolerance verification of both downstream sinks + finding</name>
  <files>analytics-service/tests/test_csv_analytics_runner.py</files>
  <read_first>
    - services/nav_twr.py:_guard_denominator (:233-256), chain_linked_twr (:185-230), cumulative_twr (:259-265) — guarded days emit np.nan; cumulative_twr drops NaN.
    - services/job_worker.py:2055-2095 — csv_daily_returns upsert, `daily_return: float(val)` for both is_key_mode and strategy-mode branches.
    - services/analytics_runner.py:1309-1324 — trades_to_daily_returns_with_status result feeds compute_all_metrics downstream (len(returns)<2 guard counts NaN entries).
    - compute_all_metrics / compute_period_returns definitions (grep `def compute_all_metrics`, `def compute_period_returns` under services/) — inspect whether they dropna/fillna or propagate.
  </read_first>
  <action>
    Determine empirically whether a returns Series containing leading and interior np.nan
    (the shape the core emits for an estimated_start<=0 account on guarded days) survives
    each downstream sink WITHOUT crashing and WITHOUT silently producing a fabricated
    magnitude. Two sinks to probe: (a) compute_all_metrics + compute_period_returns
    (analytics_runner path), (b) the `float(val)` conversion + Postgres double-precision
    daily_return column used by the csv_daily_returns upsert (job_worker.py:2068/2078).
    Probe (a) directly by constructing a Series with NaN entries in the venv and calling the
    two functions. Probe (b) by evaluating `float(float("nan"))` behavior and confirming
    against the daily_return column type (numeric vs double precision — grep the migration
    that creates csv_daily_returns for the column type; `float8`/double tolerates NaN,
    `numeric` rejects it). Record a definitive finding for EACH sink: TOLERATES (NaN dropped
    or ignored honestly) or NEEDS-A-GUARD (would crash or reject), and the exact guard
    location if needed. Encode the finding as a passing characterization test
    `test_nan_returns_downstream_tolerance` in test_csv_analytics_runner.py that asserts the
    CURRENT behavior of sink (a) on a NaN-bearing Series (e.g. metrics computed on the
    non-NaN days, or the documented exception raised). Write the human-readable finding
    (both sinks, TOLERATES/NEEDS-A-GUARD, guard locations) into the plan SUMMARY so 74-03
    and 74-04 consume it. If a sink NEEDS-A-GUARD and the fix is not localizable to
    job_worker.py:2062-2082 (upsert skip-NaN) or a single analytics_runner dropna, HALT and
    surface a blocker rather than guessing.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_csv_analytics_runner.py -k nan_returns_downstream_tolerance -x -q</automated>
  </verify>
  <acceptance_criteria>
    Characterization test passes and pins the current NaN behavior of compute_all_metrics /
    compute_period_returns. SUMMARY records a per-sink finding (TOLERATES or NEEDS-A-GUARD +
    guard location) for both compute_all_metrics AND the csv_daily_returns float(val) upsert.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Byte-identity snapshot pins — daily_pnl + individual-trades branches (GREEN now)</name>
  <files>analytics-service/tests/test_transforms.py</files>
  <read_first>
    - services/transforms.py:70-223 (both branches) — confirm the exact input shapes.
    - tests/test_accuracy.py:167-315 — existing fixture shapes for trades_to_daily_returns.
    - tests/test_nav_twr.py — the SC-4 daily_pnl byte-identity pin (rtol 1e-12, check_names=False) as the assertion pattern to mirror.
  </read_first>
  <action>
    Add two snapshot pins to test_transforms.py that call trades_to_daily_returns_with_status
    directly on flow-less, estimated_start>0 fixtures and freeze today's exact output.
    Pin 1 `test_byte_identical_daily_pnl_snapshot`: a daily_pnl-format fixture (rows with
    order_type='daily_pnl', a real account_balance well above the $1000 dust floor and large
    enough that estimated_start = account_balance - total_pnl > 0). Capture the returns Series
    values as literal expected values and assert equality to rtol 1e-12 (index-name excluded,
    per the SC-4 convention). Pin 2 `test_byte_identical_individual_snapshot`: an
    individual-trades fixture (raw buy/sell rows with price/quantity/fee, NO order_type='daily_pnl',
    real account_balance, estimated_start>0) — this is the branch Phase 73 SC-4 never covered
    (portfolio.py:2260 feeds it real fills). Capture and pin its exact returns values to rtol
    1e-12. Both pins MUST pass against the current code — they are the revert-proof safety net
    that Wave 2's delegation must keep green. Do NOT assert any NaN/guard behavior here (that
    is 74-02's divergence pin). Keep both fixtures estimated_start>0 so no guard fires.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_transforms.py -k "byte_identical_daily_pnl_snapshot or byte_identical_individual_snapshot" -x -q</automated>
  </verify>
  <acceptance_criteria>
    Both snapshot pins pass against current code; each freezes the exact returns Series
    (rtol 1e-12) for its branch on an estimated_start>0 flow-less fixture; the individual-trades
    fixture uses raw buy/sell rows with a real account_balance (net-new coverage vs SC-4).
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Byte-identity snapshot pin — broker combine_realized_and_funding (GREEN now)</name>
  <files>analytics-service/tests/test_broker_dailies.py</files>
  <read_first>
    - services/broker_dailies.py:119-134 (combine_realized_and_funding), :73-104 (funding_rows_to_daily_pnl_records), :107-116 (gap_fill_daily_returns).
    - tests/test_broker_dailies.py — existing fixtures for realized_pnl_records / funding_rows shapes.
  </read_first>
  <action>
    Add `test_byte_identical_combine_snapshot` to test_broker_dailies.py: build a flow-less
    realized+funding fixture (a handful of realized daily_pnl records + funding rows across a
    few UTC days, account_balance above the dust floor and giving estimated_start>0), call
    combine_realized_and_funding, and freeze the exact gap-filled returns Series values as
    literal expected values to rtol 1e-12. This pins the broker call site (broker_dailies.py:130
    -> job_worker.py:2010) so Wave 2's param threading is proven byte-identical. Preserve the
    gap-fill invariant in the assertion (every calendar day in [first,last] present, no-activity
    days == 0.0). MUST pass against current code.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_broker_dailies.py -k byte_identical_combine_snapshot -x -q</automated>
  </verify>
  <acceptance_criteria>
    Pin passes against current code, freezes the exact gap-filled returns Series (rtol 1e-12)
    for a flow-less realized+funding fixture, and asserts the gap-fill invariant (dense calendar,
    0.0 no-activity days).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exchange numeric input → analytics | untrusted pnl/balance/funding amounts enter the returns pipeline |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering / Information disclosure | guarded-day NaN reaching a downstream sink presented as a valid metric | mitigate | Task 1 empirically verifies both sinks tolerate/guard NaN honestly (never a fabricated magnitude) BEFORE the path flips |
| T-74-03 | Information disclosure | account-size / USD in snapshot fixtures or logs | accept | test fixtures use synthetic amounts; no raw prod NAV persisted; pins log nothing |

No package installs in this plan → no supply-chain (SC) threat row.
</threat_model>

<verification>
- All three snapshot pins + the NaN characterization test pass in the CI-3.12 venv.
- Quick regression: `pytest tests/test_transforms.py tests/test_broker_dailies.py tests/test_nav_twr.py -q` stays green (no regression from adding tests).
- SUMMARY contains the per-sink NaN finding consumed by 74-03/74-04.
</verification>

<success_criteria>
- NaN dependency (RESEARCH A1) is resolved with a definitive per-sink finding.
- Revert-proof byte-identity safety net exists for all three flow-less input shapes and is GREEN.
- No production code changed.
</success_criteria>

<output>
Create `.planning/phases/74-funnel-wiring-both-callers/74-01-SUMMARY.md` when done.
The SUMMARY MUST state, for each downstream sink, TOLERATES or NEEDS-A-GUARD (+ exact guard
location) — this is the required input to plans 74-03 and 74-04.
</output>
