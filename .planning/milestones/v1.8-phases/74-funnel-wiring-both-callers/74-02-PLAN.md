---
phase: 74-funnel-wiring-both-callers
plan: 02
type: tdd
wave: 2
depends_on: ["74-01"]
files_modified:
  - analytics-service/services/transforms.py
  - analytics-service/services/broker_dailies.py
  - analytics-service/tests/test_transforms.py
  - analytics-service/tests/test_broker_dailies.py
autonomous: true
requirements: [TWR-03, TWR-04]

must_haves:
  truths:
    - "trades_to_daily_returns_with_status delegates BOTH branches (daily_pnl and individual-trades) to nav_twr.reconstruct_nav_and_twr; the equity-curve/fallback blocks are deleted"
    - "The silent estimated_start<=0 -> account_balance substitution AND prev_equity.replace(0, initial_capital) are GONE from BOTH branches (transforms.py:154-159/:175 and :196-199/:211)"
    - "An estimated_start<=0 account now emits np.nan + negative_nav_guard + computation_status_hint=complete_with_warnings instead of a fabricated magnitude, on BOTH branches"
    - "The 74-01 byte-identity snapshots (daily_pnl, individual-trades, broker combine) stay GREEN — flow-less estimated_start>0 accounts are unchanged to rtol 1e-12"
    - "trades_to_daily_returns_with_status AND combine_realized_and_funding accept external_flows + open_unrealized_usd kwargs (defaults None/0.0) threaded to the core"
    - "A source-scan test FAILS if the estimated_start->account_balance substitution or .replace(0 base swap is reintroduced anywhere in transforms.py source (outside # comments)"
  artifacts:
    - path: "analytics-service/services/transforms.py"
      provides: "delegated-to-core body, both branches, fallbacks deleted, new flow params"
      contains: "reconstruct_nav_and_twr"
    - path: "analytics-service/services/broker_dailies.py"
      provides: "external_flows + open_unrealized_usd param passthrough"
      contains: "external_flows"
    - path: "analytics-service/tests/test_transforms.py"
      provides: "divergence + fallback-deletion + source-scan mutation-honest pins (both branches)"
      contains: "negative_nav_guard"
  key_links:
    - from: "services/transforms.py"
      to: "services.nav_twr.reconstruct_nav_and_twr"
      via: "aggregate daily_pnl Series -> core"
      pattern: "reconstruct_nav_and_twr"
    - from: "services/broker_dailies.py"
      to: "services.transforms.trades_to_daily_returns_with_status"
      via: "threaded external_flows/open_unrealized_usd kwargs"
      pattern: "external_flows"
---

<objective>
The core wiring diff. Route the ONE shared function every venue flows through
(transforms.trades_to_daily_returns_with_status) through the Phase-73 honest core
`nav_twr.reconstruct_nav_and_twr`, on BOTH internal branches, deleting the silent
fallback (estimated_start<=0 -> account_balance) and the forbidden replace(0) base
swap on each. Because all four production call sites (analytics_runner.py:1309,
broker_dailies.py:130, and the two wrapper callers process_key.py:896 /
portfolio.py:2260) reach this one function body, delegating the body makes ALL FOUR
honest with a single diff (close the whole class, not point-fixes — CONTEXT grey-area #2,
Pitfall #1).

Anchor algebra (verified byte-identity, RESEARCH Pattern 1):
- real-anchor sub-branch (account_balance > dust, estimated_start>0):
  anchor_nav = account_balance. The core's terminal_nav = anchor_nav (F=0, uPnL=0),
  backward roll NAV_{t-1}=NAV_t-pnl_t, day-0 prev = NAV_0-pnl_0 = initial_capital —
  algebraically identical to transforms' equity curve. This is exactly the SC-4 pin.
- heuristic sub-branch (account_balance None / <= dust): preserve the existing
  used_heuristic_capital derivation of initial_capital, then pass
  anchor_nav = initial_capital + total_pnl (synthetic terminal) so the core reconstructs
  the same synthetic base and reproduces today's heuristic returns byte-for-byte — while
  deleting replace(0) on that path too (TWR-03 requires it gone EVERYWHERE).

TDD: RED (divergence + fallback-deletion + source-scan pins) → GREEN (the diff). The
74-01 snapshots are the concurrent revert-proof safety net and MUST stay green.

Purpose: deliver the honest divergence the milestone exists for, provably behavior-
preserving on every flow-less account before any adapter (Phase 75+) touches prod data.
Output: delegated transforms body + param-threaded broker combine + mutation-honest pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

Python for ALL analytics pytest (MANDATORY — local 3.14 SIGSEGVs on pandas):
`/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python`
Run from `analytics-service/`. TDD: commit test-RED then feat-GREEN per task (mirror Phase 73).
`.planning/` is gitignored — commit CODE/tests only.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-funnel-wiring-both-callers/74-CONTEXT.md
@.planning/phases/74-funnel-wiring-both-callers/74-RESEARCH.md
@.planning/phases/73-pure-nav-twr-core/73-01-SUMMARY.md

@analytics-service/services/transforms.py
@analytics-service/services/nav_twr.py
@analytics-service/services/broker_dailies.py

<interfaces>
services/nav_twr.py (the core to wire to):
  reconstruct_nav_and_twr(daily_pnl: pd.Series, anchor_nav: float, *,
      external_flows: Sequence[Any]|None=None, open_unrealized_usd: float=0.0
  ) -> tuple[pd.Series, NavTWRMeta]
    - terminal_nav = anchor_nav - open_unrealized_usd; backward roll; chain-linked TWR;
      guarded days -> np.nan + flag. Empty daily_pnl -> empty Series + clean meta.
  NavReconstructionError(ValueError) — raised by _coerce_float on non-finite/non-numeric input.
  NavTWRMeta(ReturnsComputationMeta, total=False) adds dust_nav_guard / negative_nav_guard /
      flow_dominated_guard (present only when fired); inherited used_heuristic_capital &
      balance_error are False from the core. computation_status_hint = complete_with_warnings iff any guard.

services/transforms.py (what to refactor):
  ReturnsComputationMeta keys: used_heuristic_capital, balance_error, computation_status_hint.
  _build_meta(*, used_heuristic_capital, balance_error) -> ReturnsComputationMeta.
  daily_pnl aggregation: daily_pnl = df.groupby("date")["daily_pnl"].sum()  (:127)
  individual-trades aggregation: daily_agg["pnl"] = net_notional - total_fees  (:184-188)
  DUST floor = 1000.0 (:146/:195) — matches nav_twr.DUST_NAV_FLOOR.

Phase-73 divergence fixture (test_nav_twr.py): account_balance=1500, total_pnl=2000
=> estimated_start=-500; OLD transforms fabricates returns.iloc[0]==2000/1500; core emits NaN.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Delegate the daily_pnl branch to the core + delete its fallback</name>
  <files>analytics-service/services/transforms.py, analytics-service/tests/test_transforms.py</files>
  <read_first>
    - services/transforms.py:120-176 (daily_pnl branch: aggregation, dust check, estimated_start<=0 substitution :154-159, replace(0) :175).
    - services/nav_twr.py:293-319 (reconstruct_nav_and_twr) + :268-290 (_build_nav_meta).
    - tests/test_transforms.py — heuristic contract tests (do not break them) + the 74-01 daily_pnl snapshot.
  </read_first>
  <behavior>
    - RED: estimated_start<=0 daily_pnl fixture (balance=1500, total_pnl=2000) -> returns.iloc[0] is np.nan, meta has negative_nav_guard=True, computation_status_hint=="complete_with_warnings" (fails against current code, which fabricates 2000/1500).
    - RED: mutation-honest fallback-deletion pin -> asserts the same account is NaN, not account_balance-substituted (fails if the else:account_balance branch returns).
    - RED: source-scan -> transforms.py source (excluding lines whose first non-space char is #) contains neither `initial_capital = account_balance` nor `.replace(0`.
    - GREEN-invariant: the 74-01 daily_pnl byte-identity snapshot (estimated_start>0) stays rtol-1e-12 identical.
    - GREEN-invariant: existing heuristic-branch (account_balance=None) tests unchanged.
  </behavior>
  <action>
    Keep the aggregation (`daily_pnl = df.groupby("date")["daily_pnl"].sum()`) and the dust
    check that sets used_heuristic_capital. Replace the equity-curve block (:171-176) with a
    delegation to reconstruct_nav_and_twr. For the real-anchor sub-branch pass
    anchor_nav = account_balance; for the heuristic sub-branch keep the existing initial_capital
    derivation (max(mean_abs_pnl*100, abs(total_pnl), 10000)) and pass
    anchor_nav = initial_capital + daily_pnl.sum() (synthetic terminal) so the core reconstructs
    the same base. Thread the new function params external_flows/open_unrealized_usd (see Task 3
    for the signature add) into the core call. DELETE the estimated_start<=0 -> account_balance
    substitution (:154-159 else) and the prev_equity.replace(0,...) swap (:175) — the core's
    fail-loud guards replace them. Map the returned NavTWRMeta into the function's meta: the core
    already yields used_heuristic_capital=False/balance_error=False, so OR the core's guard-derived
    hint with the transforms-side used_heuristic_capital/balance_error to build the final meta
    (heuristic OR guard OR balance_error -> complete_with_warnings), and carry the guard keys
    (dust_nav_guard/negative_nav_guard/flow_dominated_guard) through onto the returned meta so
    74-03 can lift them. Do NOT delete the heuristic branch (process_key.py:896 always passes
    account_balance=None; deleting it breaks the landing-page card — RESEARCH anti-pattern).
    When rewording any docstring, do NOT write the literal tokens `.replace(0` or
    `initial_capital = account_balance` (they trip the source-scan — see 73-01 SUMMARY deviation 2).
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_transforms.py -k "daily_pnl or fallback or forbidden or heuristic or byte_identical" -x -q</automated>
  </verify>
  <acceptance_criteria>
    daily_pnl branch delegates to the core; estimated_start<=0 emits NaN+negative_nav_guard+
    complete_with_warnings; both fallback tokens gone (source-scan green); 74-01 daily_pnl
    snapshot + existing heuristic tests still green.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Extract-aggregate helper + delegate the individual-trades branch + delete its fallback</name>
  <files>analytics-service/services/transforms.py, analytics-service/tests/test_transforms.py</files>
  <read_first>
    - services/transforms.py:178-212 (individual-trades branch: notional/fee groupby :180-188, estimated_start<=0 substitution :196-199, replace(0) :211).
    - The daily_pnl delegation just written in Task 1 (reuse its core-call + meta-mapping shape).
    - tests/test_transforms.py — the 74-01 individual-trades snapshot; tests/test_accuracy.py:167-315.
  </read_first>
  <behavior>
    - RED: individual-trades estimated_start<=0 fixture (raw fills, balance chosen so estimated_start<0) -> returns first valid day is np.nan, meta negative_nav_guard=True, complete_with_warnings (fails against current code fabricating a magnitude).
    - RED: fallback-deletion pin for the individual-trades branch (:199/:211) — fails if the zero-to-initial swap or estimated_start else-substitution returns.
    - GREEN-invariant: the 74-01 individual-trades byte-identity snapshot (estimated_start>0, real fills) stays rtol-1e-12 identical.
  </behavior>
  <action>
    Factor the individual-trades daily-PnL aggregation (notional = price*quantity, sign sells
    negative, fee_usd; daily_agg["pnl"] = net_notional - total_fees) into a small helper that
    returns a `pnl` Series named to match the core's daily_pnl input. Then delegate exactly like
    the daily_pnl branch: real-anchor sub-branch anchor_nav = account_balance; heuristic
    sub-branch keep initial_capital = abs(net_notional.iloc[0]) or 10000 and pass
    anchor_nav = initial_capital + daily_agg["pnl"].sum(). Feed the aggregated Series to
    reconstruct_nav_and_twr with the threaded flow params, map NavTWRMeta -> meta identically to
    Task 1. DELETE the estimated_start<=0 -> account_balance substitution (:196-199) and
    prev_equity.replace(0,...) (:211). This is the ONLY way portfolio.py:2260 (real fills) reaches
    the honest path and satisfies TWR-03's :199 requirement (Pitfall #2). Preserve the
    used_heuristic_capital semantics for the abs(...).iloc[0] sub-branch.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_transforms.py tests/test_accuracy.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    individual-trades branch delegates via the extract-aggregate helper; estimated_start<=0 emits
    NaN+negative_nav_guard+complete_with_warnings; both :199/:211 fallback tokens gone; 74-01
    individual-trades snapshot + test_accuracy fixtures still green.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 3: Add external_flows/open_unrealized_usd params + thread through broker combine</name>
  <files>analytics-service/services/transforms.py, analytics-service/services/broker_dailies.py, analytics-service/tests/test_broker_dailies.py</files>
  <read_first>
    - services/transforms.py:70-95 (the _with_status signature/docstring) and :41-67 (the thin wrapper — must keep working with defaults).
    - services/broker_dailies.py:119-134 (combine_realized_and_funding).
    - tests/test_broker_dailies.py — the 74-01 combine snapshot.
  </read_first>
  <behavior>
    - RED: passing external_flows=[(day_iso, usd)] through combine_realized_and_funding reaches the core (assert it raises NavReconstructionError for an orphan flow day, proving the param is threaded — flows dated in-window would be Phase 75; here just prove the wire, not valuation).
    - GREEN-invariant: the 74-01 combine byte-identity snapshot (default params) stays rtol-1e-12 identical; the thin trades_to_daily_returns wrapper (no new kwargs) still works.
  </behavior>
  <action>
    Add keyword-only params `external_flows: Sequence[Any]|None = None` and
    `open_unrealized_usd: float = 0.0` to trades_to_daily_returns_with_status (defaults preserve
    every existing caller byte-identical) and pass them into the reconstruct_nav_and_twr calls in
    both branches. Add the same two params to broker_dailies.combine_realized_and_funding and
    forward them to trades_to_daily_returns_with_status. Leave the thin trades_to_daily_returns
    wrapper (transforms.py:41-67) signature unchanged (it keeps calling _with_status with defaults).
    Do NOT change any of the four call sites' arguments in this phase — they rely on the defaults
    (CONTEXT lock: external_flows=None, open_unrealized_usd=0.0). Add the RED param-threading
    assertion to test_broker_dailies.py.
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_broker_dailies.py tests/test_transforms.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    Both functions accept external_flows/open_unrealized_usd (defaults None/0.0) threaded to the
    core; the param-threading RED test proves the wire; the 74-01 combine snapshot + thin-wrapper
    behavior stay byte-identical.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exchange numeric input → transforms → nav_twr core | untrusted pnl/balance amounts cross into NAV reconstruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-74-01 | Tampering / Information disclosure | delegated body silently re-introducing a fabricated base (substitution/replace(0)) | mitigate | source-scan test (Task 1) bans the token class; divergence pins prove NaN-not-magnitude on both branches |
| T-74-04 | Tampering | malformed pnl/balance producing a silent NaN stamped complete | mitigate | core `_coerce_float` fail-loud (nav_twr) reached by the delegation; non-finite input raises NavReconstructionError (caught in 74-03/74-04) |
| T-74-03 | Information disclosure | account-size USD in fixtures/logs | accept | synthetic fixtures; no raw prod NAV logged |

No package installs → no supply-chain (SC) row.
</threat_model>

<verification>
- Full blast-radius suite green (per RESEARCH sampling): `pytest tests/test_transforms.py tests/test_broker_dailies.py tests/test_accuracy.py tests/test_nav_twr.py -q`.
- The 74-01 byte-identity snapshots (all three) remain green — behavior-preserving proof.
- Source-scan confirms both fallback token classes are gone from transforms.py.
- Confirm no call-site arguments changed (external_flows/open_unrealized_usd stay at defaults this phase).
</verification>

<success_criteria>
- TWR-03: silent fallback deleted unconditionally on BOTH branches; regression-tested each branch (revert-proof).
- TWR-04: both direct callers route through the core (via the delegated body); zero-flow byte-identity holds on all three input shapes; external_flows/open_unrealized_usd params present.
- estimated_start<=0 accounts flag+NaN honestly on both branches; flow-less accounts byte-identical.
</success_criteria>

<output>
Create `.planning/phases/74-funnel-wiring-both-callers/74-02-SUMMARY.md` when done — note the guard
keys now carried on the meta (input to 74-03's DQF lift) and confirm all three 74-01 snapshots stayed green.
</output>
