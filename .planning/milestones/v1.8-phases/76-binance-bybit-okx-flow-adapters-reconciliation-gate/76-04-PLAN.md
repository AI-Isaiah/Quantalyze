---
phase: 76-binance-bybit-okx-flow-adapters-reconciliation-gate
plan: 04
type: execute
wave: 3
depends_on: ["76-02", "76-03"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/services/broker_dailies.py
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_job_worker.py
  - analytics-service/tests/test_broker_dailies.py
autonomous: true
requirements: [FLOW-03, DQ-02]

must_haves:
  truths:
    - "For binance/okx/bybit the else-branch sources dated event-time flows (promoted fetch → I/O price resolver → pure ccxt_rows_to_dated_flows) and threads them into combine_realized_and_funding at the SAME seam the Deribit branch uses"
    - "End-to-end per venue: a real deposit + an internal own-transfer → ONLY the deposit becomes an F_t in the reconstructed TWR"
    - "A deposit outside the venue's deposit-history retention → the DQ-02 gate segments at the terminus, refuses pre-terminus TWR, and flags flow_coverage_incomplete → complete_with_warnings (never a fabricated return over the gap)"
    - "A transient flow-fetch error bubbles as retryable (NOT a permanent truncation); a NavReconstructionError from the residual/valuation is permanent-failed and scrubbed"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "ccxt else-branch wiring: fetch transfers + resolve event-time prices + ccxt_rows_to_dated_flows → external_flows → combine; coverage-terminus signal derivation"
      contains: "ccxt_rows_to_dated_flows"
    - path: "analytics-service/services/analytics_runner.py"
      provides: "flow_coverage_incomplete lifted into data_quality_flags + status-promotion predicate (extends the 74-03 lift)"
      contains: "flow_coverage_incomplete"
  key_links:
    - from: "services/job_worker.py else branch (:1982)"
      to: "services/ccxt_flows.py::ccxt_rows_to_dated_flows"
      via: "fetch_ccxt_transfers + price resolver + pure valuation → external_flows"
      pattern: "ccxt_rows_to_dated_flows|fetch_ccxt_transfers"
    - from: "services/job_worker.py"
      to: "combine_realized_and_funding(external_flows=...)"
      via: "same seam as the deribit branch (:2012)"
      pattern: "external_flows="
---

<objective>
Wire the ccxt flow adapter end-to-end and apply the DQ-02 gate. In the
`derive_broker_dailies` else branch (binance/okx/bybit), fetch deposits +
withdrawals via the promoted `fetch_ccxt_transfers`, resolve the event-time daily
closes for non-stable currencies (I/O — reuse the existing OHLCV/CoinGecko/
token_price_history source), run the pure `ccxt_rows_to_dated_flows`, and thread the
result into `combine_realized_and_funding` at the SAME seam the Deribit branch uses.
Derive the coverage-terminus signal (retention window vs pnl window) and inject it so
DQ-02 segments a retention gap. Prove per-venue own-transfer exclusion end-to-end and
the retention-gap segmentation, and lift `flow_coverage_incomplete` into the status
pipeline (extending the 74-03 DQF lift).

Purpose: FLOW-03 (real flows correct the TWR numerator on all three ccxt venues) +
DQ-02 (a coverage gap fails loud, never mis-attributed).

Output: the ccxt else-branch wiring + I/O price resolver + DQ-02 application + the
end-to-end per-venue + terminus acceptance suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-CONTEXT.md
@.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-RESEARCH.md
@.planning/phases/75-deribit-dated-flow-adapter-risky/75-03-SUMMARY.md
@analytics-service/services/broker_dailies.py

<interfaces>
<!-- The wiring seam (VERIFIED job_worker.py:1838-2015). -->
```python
external_flows: list[ExternalFlow] | None = None       # :1840, initialized before branches
# deribit branch sets external_flows = _completeness.dated_external_flows  (:1979)
else:  # binance/okx/bybit  (:1982)
    equity, balance_error = await fetch_account_equity_usd(ctx.exchange, venue)   # ANCHOR
    realized = await fetch_all_trades(ctx.exchange, since_ms=None)
    funding  = await fetch_funding_{binance|okx|bybit}(ctx.exchange, funding_label, None)
    # NEW: rows = fetch_ccxt_transfers(deposits)+fetch_ccxt_transfers(withdrawals)
    # NEW: price_index = resolve same-UTC-day closes for non-stable ccys (I/O)
    # NEW: external_flows = ccxt_rows_to_dated_flows(rows, venue=venue, price_index=price_index)
    # NEW: flow_coverage_start_day = derived terminus (OKX ~90d retention vs pnl window)
returns, meta = combine_realized_and_funding(                                     # :2012
    realized, funding, account_balance=equity, balance_error=balance_error,
    external_flows=external_flows,          # threaded to reconstruct_nav_and_twr
)
# NavReconstructionError already caught permanent at :2016 (scrubbed, no from-exc leak).
```

<!-- Promoted fetch (76-01) + pure valuer (76-02) + gate (76-03) to compose. -->
services/ccxt_flow_fetch.py::fetch_ccxt_transfers(exchange, kind, since_ms, now_ms)  # I/O
services/ccxt_flows.py::ccxt_rows_to_dated_flows(rows, *, venue, price_index)        # PURE
services/nav_twr.py::apply_flow_coverage_terminus(returns, flow_coverage_start_day)   # standalone gate helper (76-03)

<!-- Existing price source to REUSE for the resolver (do NOT build a new fetcher). -->
equity_reconstruction.py:695 _fetch_ohlcv_daily · :752 _fetch_coingecko_daily_closes · token_price_history cache (:846). RESEARCH Don't-Hand-Roll: reuse these.

<!-- combine seam unchanged — DQ-02 terminus is applied POST-combine on the returns Series (no transforms.py threading; the 74 byte-identity pins stay GREEN). -->
broker_dailies.py:119 combine_realized_and_funding(..., *, external_flows, open_unrealized_usd)  # docstring-only change here

<!-- Status lift to EXTEND (74-03). -->
analytics_runner.py ~:1704-1713 (DQF lift loop) + ~:1775-1788 (complete_with_warnings promotion predicate) — OR the guard keys already ride via the meta; add flow_coverage_incomplete to the predicate.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the ccxt else-branch — fetch → event-time price resolver → pure valuation → external_flows</name>
  <read_first>
    - analytics-service/services/job_worker.py:1982-2015 (the else branch + combine seam)
    - analytics-service/services/broker_dailies.py:34-37,119-149 (docstring premise + combine signature)
    - analytics-service/services/equity_reconstruction.py:695-860 (OHLCV/CoinGecko/token_price_history price source to reuse)
    - analytics-service/services/ccxt_flows.py (the pure valuer from 76-02)
    - analytics-service/services/ccxt_flow_fetch.py (the promoted fetch from 76-01)
  </read_first>
  <files>analytics-service/services/job_worker.py, analytics-service/services/broker_dailies.py, analytics-service/tests/test_job_worker.py, analytics-service/tests/test_broker_dailies.py</files>
  <action>
    In the else branch (:1982), after realized+funding, fetch deposits and withdrawals
    via `fetch_ccxt_transfers`, collect the non-stable currencies + their UTC days, and
    resolve a `price_index: dict[(day,ccy), close]` using the EXISTING
    `_fetch_ohlcv_daily` + CoinGecko + `token_price_history` source (reuse — do NOT build
    a new price fetcher; RESEARCH Don't-Hand-Roll). Extract a small I/O resolver helper in
    job_worker (or a thin I/O function) that returns the price_index; keep the math in the
    pure `ccxt_rows_to_dated_flows`. Set `external_flows = ccxt_rows_to_dated_flows(rows,
    venue=venue, price_index=price_index)` before the combine call at :2012 (external_flows
    is already initialized None at :1840 and already threaded into combine — reuse that
    exact seam, mirroring the deribit branch at :1979). Do NOT add a kwarg through
    combine → transforms → core: the DQ-02 terminus is applied POST-combine (Task 2) via
    the standalone `apply_flow_coverage_terminus` helper, so transforms.py is untouched and
    the Phase 74 byte-identity pins stay GREEN. Update the `broker_dailies.py:34-36` docstring: ccxt venues DO enumerate deposits/
    withdrawals now (State-of-the-Art §) — mid-window flows are no longer an accepted
    flagged limitation for ccxt venues. Inherit the `BROKER_DAILIES_VIA_FUNDING` kill-switch
    (flows ride the same path). Do NOT touch the deribit branch.
  </action>
  <acceptance_criteria>
    - Each ccxt venue sets external_flows from real fetched+valued rows before combine.
    - The price resolver reuses the existing OHLCV/CoinGecko/token_price_history source (no new fetcher).
    - A stablecoin-only account's flows value at 1.0; a BTC-deposit account values at same-UTC-day close.
    - broker_dailies docstring updated; kill-switch behavior preserved.
    - Existing job_worker/broker_dailies suites GREEN; deribit branch untouched.
  </acceptance_criteria>
  <verify>
    <automated>/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_job_worker.py analytics-service/tests/test_broker_dailies.py -x</automated>
  </verify>
  <done>The ccxt else-branch sources event-time dated flows through the promoted fetch + pure valuer and threads them into the core at the deribit seam.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the DQ-02 gate + end-to-end per-venue + retention-terminus acceptance + status lift</name>
  <read_first>
    - analytics-service/services/nav_twr.py (reconcile_flow_residual + flow_coverage_start_day from 76-03)
    - analytics-service/services/equity_reconstruction.py:73,576-601,1947-1962 (OKX_TRADE_TERMINUS_DAYS + terminus signal + pre_terminus flag pattern)
    - analytics-service/services/analytics_runner.py (~:1704-1788 the 74-03 DQF lift + promotion predicate)
    - analytics-service/services/job_worker.py:2016-2056 (the existing NavReconstructionError permanent catch)
  </read_first>
  <files>analytics-service/services/job_worker.py, analytics-service/services/analytics_runner.py, analytics-service/tests/test_job_worker.py</files>
  <action>
    Derive `flow_coverage_start_day` in the else branch: when the venue's deposit-history
    retention window starts AFTER the pnl-window start (mirror the OKX terminus signal at
    :576-601, `OKX_TRADE_TERMINUS_DAYS=90`), the earliest capital is unfetchable → set the
    coverage start-day so DQ-02 segments; a genuine clean-empty at the boundary segments,
    a fetch ERROR bubbles retryable (WR-04 — do NOT convert a transient error into a
    segment; `fetch_ccxt_transfers` already bubbles all but ccxt.NotSupported). AFTER
    `returns, meta = combine_realized_and_funding(...)`, call the standalone pure
    `apply_flow_coverage_terminus(returns, flow_coverage_start_day)` (76-03), replace
    `returns` with the segmented series, and merge the `flow_coverage_incomplete` flag into
    `meta` — no transforms.py threading (the 74 byte-identity pins stay GREEN). Extend the 74-03 status lift in
    analytics_runner: add `flow_coverage_incomplete` to the DQF lift + the
    complete_with_warnings promotion predicate (mirroring negative_nav_guard/dust/flow_dom).
    Author end-to-end acceptance: (a) per venue (binance/okx/bybit) a real deposit + an
    internal own-transfer through the real seam → ONLY the deposit becomes an F_t in the
    reconstructed returns (mutation: neuter the filter → own-transfer inflates → RED);
    (b) a deposit outside OKX retention → the reconstructed series segments, pre-terminus
    days are NaN/dropped, `flow_coverage_incomplete` + complete_with_warnings, and NO
    fabricated return over the gap (mutation: remove the segment → a number appears → RED);
    (c) a transient fetch error → the job is retryable, NOT a permanent truncation, and no
    segmentation. Reuse in-process synthetic rows / stub exchanges (P75 discipline) for the
    per-venue + transient proofs; a vcr cassette is acceptable where a real ccxt call shape
    is needed.
  </action>
  <acceptance_criteria>
    - flow_coverage_start_day is derived from the retention-vs-pnl window and threaded to the core.
    - Per-venue end-to-end: only the deposit (not the own-transfer) reaches F_t (mutation-honest RED under a neutered filter).
    - Retention-gap: series segments at the terminus, pre-terminus days NaN/dropped, flow_coverage_incomplete → complete_with_warnings, no fabricated return.
    - Transient fetch error → retryable, not a permanent truncation, no segment.
    - analytics_runner lifts flow_coverage_incomplete into DQF + promotion; SC-4 (flow-less/full-coverage account stays `complete`) preserved.
    - mypy --strict clean on the three changed service files.
  </acceptance_criteria>
  <verify>
    <automated>/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_job_worker.py analytics-service/tests/test_analytics_runner.py analytics-service/tests/test_nav_twr.py -x</automated>
  </verify>
  <done>The DQ-02 gate segments a retention gap and flags it, per-venue own-transfer exclusion holds end-to-end, transient errors stay retryable, and flow_coverage_incomplete surfaces through the status pipeline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ccxt exchange → analytics worker | untrusted transfer rows + signed-request errors + price data cross into the reconstruction |
| coverage window → factsheet status | a retention gap must surface as complete_with_warnings, never a silent complete |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-04-GAP | Tampering (mis-attributed gap) | retention-terminus wiring | mitigate | Derive flow_coverage_start_day from retention-vs-pnl window → DQ-02 segments; end-to-end mutation proof REDs if the segment is removed |
| T-76-04-TRANS | Denial of Service (retry-forever / over-truncation) | flow-fetch error handling | mitigate | Transient fetch error bubbles retryable (WR-04); NavReconstructionError stays permanent-failed at :2016 (scrubbed, no from-exc leak) — the transient-vs-terminal proof pins both |
| T-76-04-SIG | Information Disclosure | ccxt error / price-fetch logs | mitigate | scrub_freeform_string before logging exchange exceptions (exchange.py:2670 / job_worker.py:1857 precedent); no raw NAV/flow USD in logs |
| T-76-04-SCOPE | Tampering (wrong wallet pool) | anchor vs flow wallet scope | transfer | A mis-scoped anchor makes Σflows fail the DQ-02 residual/segmentation (fail loud); founder confirmation of Binance SPOT-vs-USDⓈ-M / Bybit FUND-UNIFIED deferred to 76-05 / Phase 78 |
| T-76-04-SC | Tampering | pip installs | accept | No installs — ccxt/pandas/numpy pinned |
</threat_model>

<verification>
- Full analytics suite GREEN in the CI-3.12 venv (baseline 3036 / 92 skip); all 74/75 byte-identity + acceptance pins stay GREEN.
- `test_job_worker.py`, `test_broker_dailies.py`, `test_analytics_runner.py`, `test_nav_twr.py` GREEN.
- `mypy --strict` clean on job_worker.py, broker_dailies.py, analytics_runner.py.
</verification>

<success_criteria>
- All three ccxt venues source event-time dated flows into the honest core at the deribit seam.
- Per-venue own-transfer exclusion proven end-to-end (only the deposit reaches F_t).
- A retention gap segments + flags complete_with_warnings, never fabricating a return; transient errors stay retryable.
</success_criteria>

<output>
Create `.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-04-SUMMARY.md` when done.
</output>
