---
phase: 76-binance-bybit-okx-flow-adapters-reconciliation-gate
plan: 05
type: execute
wave: 4
depends_on: ["76-04"]
files_modified: []
autonomous: false
requirements: [FLOW-03]

must_haves:
  truths:
    - "The Binance SPOT-vs-USDⓈ-M and Bybit FUND/UNIFIED/CONTRACT wallet-scope question is explicitly recorded for FOUNDER confirmation at the Phase 78 acceptance gate — anchor, PnL, and flows must read the SAME pool of capital"
    - "Until confirmed, the DQ-02 reconciliation gate (76-03/76-04) is the interim safety net — a wrong wallet scope makes Σflows fail to reconcile and fails loud (segment/flag), never a silent mis-attribution"
  artifacts: []
  key_links: []
---

<objective>
Record the live wallet-scope confirmation as a deferred FOUNDER gate for Phase 78 —
NOT a Phase 76 blocker. FLOW-03 SC-4 requires that Binance SPOT-vs-USDⓈ-M and Bybit
FUND/UNIFIED/CONTRACT wallet scope be verified against the live account roster so the
anchor, PnL, and flows all read the same pool of capital. This cannot be proven from
code alone (RESEARCH Q4/Q5, MEDIUM confidence; the live roster is not in git and not
in this environment). Per the locked decision, the adapter is built to the best-known
scope and the DQ-02 gate makes a WRONG scope fail loud — so this is a confirmation
checkpoint, not a build task.

Purpose: surface the one thing Claude literally cannot verify (the live wallet the
production keys point at) as an explicit human gate at Phase 78, with the DQ-02 gate
as the interim safety net.

Output: an acknowledged, deferred founder-confirmation gate (no code — verification only).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-CONTEXT.md
@.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Founder wallet-scope confirmation (deferred to Phase 78)</name>
  <action>Pause and surface the wallet-scope question below for founder confirmation; this is a verification gate, not a code change. Record the acknowledgement and carry it to the Phase 78 acceptance ceremony.</action>
  <what-built>
    The ccxt flow adapters (76-01..76-04) source dated event-time external flows for
    Binance/Bybit/OKX and thread them into the honest NAV/TWR core, with a DQ-02
    reconciliation gate that fails loud (segments + flags) on a coverage or scope
    mismatch. The adapters were built to the best-known wallet scope:
      - Binance: `fetch_balance()` sets no `defaultType` → the anchor reads the SPOT
        wallet `total.USDT` while realized PnL/funding come from USDⓈ-M futures
        (`fapiPrivate_get_income`). If the live keys trade USDⓈ-M with margin in the
        futures wallet, the anchor may undercount (Pitfall 5 / A1).
      - Bybit: the anchor reads a single UNIFIED `fetch_balance()`; principal held in
        FUND (or CONTRACT) may be uncovered (Q5 / A2).
  </what-built>
  <how-to-verify>
    This is a DEFERRED gate for the Phase 78 acceptance ceremony — acknowledge here so
    it is recorded and carried, then confirm at Phase 78 against the live roster:
    1. Confirm which Binance wallet holds the traded capital on the production keys
       (SPOT vs USDⓈ-M), and that the anchor + PnL + flows all read that same pool. If
       futures-scoped, the fix is `options['defaultType']='future'` (or reading the
       futures wallet) — a Phase 78 follow-on, NOT a Phase 76 change.
    2. Confirm the Bybit anchor covers the pool the flows land in (net/combine
       FUND+UNIFIED+CONTRACT if principal spans wallets).
    3. Confirm (A2) that Bybit off-chain withdrawals excluded by the `withdrawType=='0'`
       filter are genuinely own-transfers, not third-party cash-out.
    Interim safety net (already shipped in 76-03/76-04): a wrong scope makes the DQ-02
    residual fail to reconcile → the gate segments/flags rather than silently
    mis-attributing — so a wrong scope cannot silently ship an inflated track record.
  </how-to-verify>
  <resume-signal>Type "acknowledged — deferred to Phase 78" to record the gate and complete Phase 76, or provide the live-roster answers now if available.</resume-signal>
</task>

</tasks>

<verification>
- The wallet-scope confirmation is recorded as a Phase 78 acceptance gate (no code change in Phase 76).
- The DQ-02 gate (76-03/76-04) is confirmed as the interim safety net.
</verification>

<success_criteria>
- The Binance/Bybit wallet-scope question is explicitly carried to Phase 78 for founder confirmation.
- Phase 76 is not blocked on live-roster access; the DQ-02 gate guarantees a wrong scope fails loud.
</success_criteria>

<output>
Create `.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-05-SUMMARY.md` when done.
</output>
