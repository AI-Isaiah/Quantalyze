---
phase: 76-binance-bybit-okx-flow-adapters-reconciliation-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/equity_reconstruction.py
  - analytics-service/services/ccxt_flow_fetch.py
  - analytics-service/tests/test_equity_reconstruction.py
  - analytics-service/tests/test_exchange_pagination.py
autonomous: true
requirements: [FLOW-03]

must_haves:
  truths:
    - "The allocator-dashboard equity-reconstruction output is byte-identical before and after `_fetch_transfers` is promoted to a shared module"
    - "`_fetch_transfers` is importable from a shared module the ccxt flow adapter can consume"
    - "OKX (100/page) and Bybit (50/page) multi-page transfer history is fully fetched — the `len(page)<500` break no longer truncates rows beyond the first capped page"
  artifacts:
    - path: "analytics-service/services/ccxt_flow_fetch.py"
      provides: "The promoted, shared, importable ccxt transfer-fetch I/O helper (fetch_ccxt_transfers)"
      contains: "def fetch_ccxt_transfers"
    - path: "analytics-service/tests/test_equity_reconstruction.py"
      provides: "Characterization pin: allocator-dashboard equity reconstruction byte-identical across the promotion"
    - path: "analytics-service/tests/test_exchange_pagination.py"
      provides: "OKX/Bybit >page-cap transfer pagination proof"
  key_links:
    - from: "analytics-service/services/equity_reconstruction.py:1739"
      to: "services/ccxt_flow_fetch.py::fetch_ccxt_transfers"
      via: "import + call at the allocator-dashboard deposits/withdrawals fetch"
      pattern: "fetch_ccxt_transfers|_fetch_transfers"
---

<objective>
Pre-flight for the ccxt flow adapters: promote `equity_reconstruction._fetch_transfers`
(the paginated ccxt deposit/withdrawal fetch) into a shared importable I/O module
with ZERO behavior change to its current allocator-dashboard consumer, pinned by a
before/after characterization test, and fix the latent OKX/Bybit under-pagination
(the `len(page)<500` break that drops rows past the first venue-capped page).

Purpose: FLOW-03 requires ONE flow-fetch path (not three copies). The ccxt adapter
(76-04) will import this promoted fetch. The promotion must not shift a single
existing factsheet, and the pagination bug — latent today but load-bearing on the
full-history factsheet path — must be fixed before flows ride this helper.

Output: `services/ccxt_flow_fetch.py` (shared fetch), a byte-identical allocator
consumer, a characterization pin, and a >page-cap pagination proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-CONTEXT.md
@.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-RESEARCH.md

<interfaces>
<!-- The helper being promoted (VERBATIM current signature — preserve it). -->
From analytics-service/services/equity_reconstruction.py:642 :
```python
async def _fetch_transfers(
    exchange: Any, kind: str, since_ms: int, now_ms: int
) -> list[dict[str, Any]]:
    # paginates fetch_deposits/fetch_withdrawals in 90-day windows;
    # page_limit=500; breaks on len(page)<page_limit (THE BUG for OKX/Bybit);
    # catches ONLY ccxt.NotSupported (feature detection), lets every other
    # exception bubble (WR-04 transient-vs-terminal discipline — do NOT change).
```

Current sole consumer (must stay byte-identical) — equity_reconstruction.py:1739-1744:
```python
deposits = await _fetch_transfers(exchange, "deposits", transfers_since_ms, now_ms)
withdrawals = await _fetch_transfers(exchange, "withdrawals", transfers_since_ms, now_ms)
```

Per-venue page caps (RESEARCH Pitfall 3, introspected ccxt 4.5.59):
Binance ~1000/page · OKX 100/page · Bybit 50/page. The current fixed
`page_limit=500` + `break on len(page)<500` drops OKX/Bybit rows past page 1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Characterization pin of the allocator-dashboard equity reconstruction (before/after safety net)</name>
  <read_first>
    - analytics-service/services/equity_reconstruction.py:1700-1970 (the reconstruct entry that consumes `_fetch_transfers` + stamps `pre_terminus_balance_unknown`)
    - analytics-service/tests/test_equity_reconstruction.py (existing harness/fixtures + stub-exchange pattern)
    - analytics-service/tests/test_equity_reconstruction_integration.py (integration fixture shapes)
  </read_first>
  <files>analytics-service/tests/test_equity_reconstruction.py</files>
  <action>
    Add a characterization test `test_equity_reconstruction_byte_identical_across_transfers_promotion` that drives the allocator-dashboard equity-reconstruction entry against a deterministic stub exchange (deposits + withdrawals + a couple of trades + OHLCV closes) and snapshots the full reconstructed row output (equity curve + `pre_terminus_balance_unknown` + telemetry) to a fixed expected structure (rtol 1e-12, check_names=False — mirror the 74-01 byte-identity snapshot discipline). This pins the allocator behavior BEFORE Task 2 moves `_fetch_transfers`, so the move is proven behavior-preserving. Do NOT modify production code in this task — capture current behavior only. If the existing suite already has an equivalent end-to-end reconstruction snapshot, extend/assert against it rather than duplicating.
  </action>
  <acceptance_criteria>
    - Test asserts the reconstructed output (rows + pre_terminus flag + telemetry keys) exactly against a committed expected snapshot.
    - Test is GREEN against current (pre-promotion) code.
    - Test is revert-proof: it would RED if the reconstruction math or the transfer-driven equity changed.
  </acceptance_criteria>
  <verify>
    <automated>/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_equity_reconstruction.py -k byte_identical_across_transfers_promotion -x</automated>
  </verify>
  <done>The characterization pin is GREEN against pre-promotion code and snapshots the allocator equity-reconstruction output.</done>
</task>

<task type="auto">
  <name>Task 2: Promote `_fetch_transfers` to shared `services/ccxt_flow_fetch.py` with ZERO behavior change</name>
  <read_first>
    - analytics-service/services/equity_reconstruction.py:642-692 (`_fetch_transfers` body — move verbatim)
    - analytics-service/services/equity_reconstruction.py:1739-1744 (the consumer to re-point)
    - analytics-service/services/deribit_ingest.py:37-44 (the pure/I-O split import discipline precedent)
  </read_first>
  <files>analytics-service/services/ccxt_flow_fetch.py, analytics-service/services/equity_reconstruction.py</files>
  <action>
    Create `services/ccxt_flow_fetch.py` (I/O module — this is the venue-agnostic ccxt transfer FETCH home; it is I/O, NOT the pure valuation module 76-02 builds). Move the `_fetch_transfers` body into a public `fetch_ccxt_transfers(exchange, kind, since_ms, now_ms)` with the IDENTICAL signature, docstring intent, 90-day windowing, and the WR-04 exception discipline (catch ONLY `ccxt.NotSupported`, let all else bubble — do NOT alter). In `equity_reconstruction.py`, delete the local `_fetch_transfers` and import `fetch_ccxt_transfers` from the new module, re-pointing the two consumer calls at :1739-1744. Preserve the `_rate_limit_sleep` behavior (import it or move it too if it is private to equity_reconstruction — keep one definition, no duplicate). This is a pure relocation: the Task 1 pin MUST stay byte-identical GREEN. Do NOT fix pagination here (Task 3 owns it).
  </action>
  <acceptance_criteria>
    - `fetch_ccxt_transfers` exists in `services/ccxt_flow_fetch.py`, importable, same signature/behavior as the old `_fetch_transfers`.
    - `equity_reconstruction.py` no longer defines `_fetch_transfers`; its consumer imports and calls `fetch_ccxt_transfers`.
    - The Task 1 characterization pin stays GREEN (byte-identical) — no behavior drift.
    - `mypy --strict` clean on both changed service files.
  </acceptance_criteria>
  <verify>
    <automated>/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_equity_reconstruction.py -x && grep -q "def fetch_ccxt_transfers" analytics-service/services/ccxt_flow_fetch.py && ! grep -q "async def _fetch_transfers" analytics-service/services/equity_reconstruction.py</automated>
  </verify>
  <done>`_fetch_transfers` is promoted to `fetch_ccxt_transfers` in the shared module, the allocator consumer imports it, and the characterization pin is byte-identical GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix OKX/Bybit under-pagination (len<500 break) — full multi-page fetch</name>
  <read_first>
    - analytics-service/services/ccxt_flow_fetch.py (the just-promoted `fetch_ccxt_transfers`)
    - analytics-service/services/equity_reconstruction.py:695-745 (`_fetch_ohlcv_daily` — the same-class break-bug fix precedent; cursor-advance + empty-page termination, NOT len<cap)
    - analytics-service/tests/test_exchange_pagination.py (existing paginated-fetch test patterns / synthetic paged fetcher)
    - analytics-service/tests/conftest_vcr.py (cassette harness + PII/signature redaction if a vcr cassette is used)
  </read_first>
  <behavior>
    - A Bybit-style fetcher returning full 50-row pages across 3 pages then a short page → ALL rows returned (not truncated at 50).
    - An OKX-style fetcher returning full 100-row pages across 2 pages → ALL rows returned (not truncated at 100).
    - Termination is by cursor-non-advance / empty page / window-end, NOT by `len(page) < page_limit`.
    - Binance ~1000/page path unchanged; a genuinely short final page still terminates.
    - The Task 1 allocator characterization pin stays byte-identical GREEN.
  </behavior>
  <files>analytics-service/services/ccxt_flow_fetch.py, analytics-service/tests/test_exchange_pagination.py</files>
  <action>
    RED→GREEN. Fix `fetch_ccxt_transfers` so a full-but-venue-capped page does not trip the end-of-data break. Either (a) drive pagination purely by cursor-advance + empty-page + window-end (drop the `len(page) < page_limit` break, mirroring the `_fetch_ohlcv_daily` fix at :700-738 which removed exactly this premature break), or (b) hand pagination to ccxt `params={'paginate': True}` where the venue supports it. Choose the option that keeps the allocator characterization pin byte-identical (Decision-1 no-behavior-change constraint) — prefer (a) since it is the in-repo precedent and does not depend on per-venue ccxt cursor support. Keep the 90-day outer windowing and the WR-04 `ccxt.NotSupported`-only catch. Author a synthetic paged-fetcher fixture (or a vcr cassette using the redacting conftest) proving OKX 100/page and Bybit 50/page multi-page histories return every row. This is the RESEARCH Pitfall 3 fix.
  </action>
  <verify>
    <automated>/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_exchange_pagination.py -k transfers analytics-service/tests/test_equity_reconstruction.py -x</automated>
  </verify>
  <done>OKX/Bybit >page-cap transfer histories are fully fetched (multi-page fixture GREEN), the len<500 break is gone, and the allocator characterization pin stays byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ccxt exchange → analytics I/O | fetch_deposits/withdrawals return untrusted rows + signed-request errors cross into the service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01-TRUNC | Tampering (data integrity) | `fetch_ccxt_transfers` pagination | mitigate | Remove the `len<page_limit` break so a full venue-capped page is not mistaken for end-of-history — a truncated transfer list would silently drop real capital flows; proven by the >page-cap fixture |
| T-76-01-SIG | Information Disclosure | ccxt errors bubbling from the fetch | accept | WR-04 discipline preserved: non-NotSupported errors bubble to the existing `classify_exception`/`scrub_freeform_string` handler (job_worker) — no raw signed-request string logged in this module |
| T-76-01-SC | Tampering | npm/pip installs | accept | No package installs — ccxt/pandas/numpy already pinned in requirements.txt (RESEARCH: audit N/A) |
</threat_model>

<verification>
- Full analytics suite GREEN in the CI-3.12 venv (baseline 3036 passed / 92 skipped).
- `test_equity_reconstruction*.py` all GREEN (the promotion is behavior-preserving).
- `mypy --strict` clean on `equity_reconstruction.py` + `ccxt_flow_fetch.py`.
</verification>

<success_criteria>
- `fetch_ccxt_transfers` is the ONE shared importable ccxt transfer-fetch path.
- The allocator-dashboard equity reconstruction is byte-identical (characterization pin GREEN).
- OKX/Bybit multi-page transfer histories are fully fetched (no len<500 truncation).
</success_criteria>

<output>
Create `.planning/phases/76-binance-bybit-okx-flow-adapters-reconciliation-gate/76-01-SUMMARY.md` when done.
</output>
