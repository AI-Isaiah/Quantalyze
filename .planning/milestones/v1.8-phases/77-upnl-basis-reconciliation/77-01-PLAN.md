---
phase: 77-upnl-basis-reconciliation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/nav_twr.py
  - analytics-service/tests/test_nav_twr.py
autonomous: true
requirements: [FLOW-04]

must_haves:
  truths:
    - "A material terminal uPnL wedge (|open_unrealized_usd|/anchor > 5%) surfaces unrealized_pnl_in_anchor on the returned NavTWRMeta"
    - "The reconstructed return Series is byte-identical (rtol 1e-12) whether uPnL is subtracted at the terminal (open_unrealized_usd=X) or the anchor is pre-reduced (anchor-X, open_unrealized_usd=0) — proving the wedge is a pure terminal shift with NO intra-window step"
    - "No per-day historical uPnL Series/array is constructed anywhere in the roll — the realized-basis-intraday / MTM-at-endpoint invariant is documented in reconstruct_nav_and_twr's docstring"
    - "A dust/near-zero anchor (<= DUST_NAV_FLOOR) never raises the materiality flag on noise"
  artifacts:
    - path: "analytics-service/services/nav_twr.py"
      provides: "UNREALIZED_MATERIALITY_RATIO const; unrealized_pnl_in_anchor on NavTWRMeta + _build_nav_meta; core materiality computation in reconstruct_nav_and_twr; invariant docstring"
      contains: "unrealized_pnl_in_anchor"
    - path: "analytics-service/tests/test_nav_twr.py"
      provides: "wedge-equivalence byte-identity, no-step-discontinuity, materiality-boundary, no-fabricated-marks source-scan tests"
      contains: "unrealized_pnl_in_anchor"
  key_links:
    - from: "reconstruct_nav_and_twr (nav_twr.py:481-528)"
      to: "_build_nav_meta flags mapping"
      via: "materiality ratio computed from open_unrealized_usd + anchor_nav, merged into flags before _build_nav_meta"
      pattern: "unrealized_pnl_in_anchor"
---

<objective>
Add the `unrealized_pnl_in_anchor` materiality flag to the pure NAV/TWR core and
document the realized-basis-intraday / mark-to-market-at-endpoint invariant. The
wedge SUBTRACTION already exists (`terminal_nav = anchor_nav − open_unrealized_usd`
at `nav_twr.py:507-509`); this plan supplies (a) the materiality flag when the
wedge is material relative to the anchor, and (b) the docstring that locks the
Q3 verdict (per-day true-up is NOT implemented — historical open-position marks
are not retrievable on read-only keys) as an executable, source-scanned invariant.

Purpose: SC-3 (materiality flag) + SC-2/SC-4 (no step discontinuity; documented
invariant) of Phase 77. This is the load-bearing core change; the venue reads
(77-02) and wiring (77-03) supply and thread the value.
Output: `nav_twr.py` materiality flag + docstring; four mutation-honest tests.

FLOW-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-upnl-basis-reconciliation/77-RESEARCH.md
@.planning/phases/77-upnl-basis-reconciliation/77-VALIDATION.md

<interfaces>
<!-- The wedge seam ALREADY EXISTS. This plan adds the flag + docstring only. -->

From services/nav_twr.py (VERIFIED):
- `DUST_NAV_FLOOR = 1000.0` (:54), `FLOW_DOM_RATIO = 1.0` (:61)
- `class NavTWRMeta(ReturnsComputationMeta, total=False)` (:90) — currently carries
  `dust_nav_guard`, `negative_nav_guard`, `flow_dominated_guard`,
  `flow_coverage_incomplete` (all `total=False`, present only when fired).
- `_build_nav_meta(flags: Mapping[str, bool]) -> NavTWRMeta` (:334-358) — sets
  `computation_status_hint = "complete_with_warnings" if flags else "complete"`,
  then assigns each known flag key explicitly (for mypy) when `flags.get(key)`.
- `reconstruct_nav_and_twr(daily_pnl, anchor_nav, *, external_flows=None,
  open_unrealized_usd=0.0)` (:481). At :507-509:
  `terminal_nav = _coerce_float(anchor_nav,...) - _coerce_float(open_unrealized_usd,...)`.
  At :527-528: `returns, flags = chain_linked_twr(...)` then
  `return returns, _build_nav_meta(flags)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — materiality-flag, wedge-equivalence, no-step, no-fabricated-marks tests</name>
  <files>analytics-service/tests/test_nav_twr.py</files>
  <read_first>
    - services/nav_twr.py:481-528 (reconstruct_nav_and_twr, the wedge seam + return)
    - services/nav_twr.py:334-358 (_build_nav_meta flag assignment pattern)
    - services/nav_twr.py:90-106 (NavTWRMeta TypedDict keys)
    - tests/test_nav_twr.py — existing test style (fixtures, rtol pins, mutation pattern from 74-01/76-03)
  </read_first>
  <behavior>
    - wedge_equivalence: `reconstruct_nav_and_twr(pnl, anchor=A, open_unrealized_usd=X)`
      returns a Series byte-identical (assert_series_equal rtol=1e-12, check_names=False)
      to `reconstruct_nav_and_twr(pnl, anchor=A-X, open_unrealized_usd=0.0)`. Proves the
      wedge is a pure terminal shift.
    - no_step_discontinuity: with a large open position across the window end
      (anchor A material, wedge X), the reconstructed nav terminal equals A-X and the
      day n-1→n return equals pnl_n / NAV_{n-1} with NO uPnL term (uPnL never enters an
      intra-window day). Mutation note in the test docstring: injecting X into the last
      intra-window NAV would break the equivalence assertion.
    - unrealized_pnl_in_anchor boundary: |X|/A just ABOVE 0.05 → meta has
      `unrealized_pnl_in_anchor is True` AND `computation_status_hint ==
      "complete_with_warnings"`; just BELOW 0.05 → key ABSENT AND hint == "complete".
      Both signs of X exercised. Mutation: flipping `>` to `>=`/`<` reddens the boundary.
    - dust/zero-anchor noise guard: anchor <= DUST_NAV_FLOOR (e.g. 500.0) with any wedge
      → flag ABSENT (ratio not evaluated on a dust base).
    - no_historical_mark source-scan (mirror the 73/76 forbidden-substitution scans):
      read services/nav_twr.py source; assert the wedge appears ONLY as a scalar
      subtraction on terminal_nav and NO per-day uPnL Series/array is constructed
      (e.g. assert no `unrealized.*Series`/`upnl.*iloc`/per-day-uPnL loop token). Encodes
      the Q3 "no fabricated marks" verdict.
  </behavior>
  <action>
    Add the four tests to tests/test_nav_twr.py. Reuse the existing daily_pnl fixture
    style (small DatetimeIndex Series). Import UNREALIZED_MATERIALITY_RATIO from
    services.nav_twr once Task 2 adds it — in RED, reference the literal 0.05 boundary
    and the `unrealized_pnl_in_anchor` key (tests fail because the key is never set yet).
    Use assert_series_equal(rtol=1e-12, check_names=False) for the equivalence pin. The
    source-scan test opens the module file via importlib.util.find_spec / __file__ and
    greps the source string (do NOT importlib.reload). Run under $PY312.
  </action>
  <verify>
    <automated>/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_nav_twr.py -k "wedge_equivalence or unrealized_pnl_in_anchor or no_step or no_historical_mark" -x</automated>
  </verify>
  <acceptance_criteria>
    All four new tests are collected and FAIL (RED) against the current core (the flag
    key is never set; the materiality const does not exist). No pre-existing test in
    test_nav_twr.py regresses to error at collection.
  </acceptance_criteria>
  <done>Four tests present and RED for the right reason (missing flag/const, not import error in unrelated code).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — materiality flag + realized-basis invariant docstring in the core</name>
  <files>analytics-service/services/nav_twr.py</files>
  <read_first>
    - services/nav_twr.py:54-61 (DUST_NAV_FLOOR / FLOW_DOM_RATIO const style)
    - services/nav_twr.py:90-106 (NavTWRMeta), :334-358 (_build_nav_meta), :481-528 (reconstruct_nav_and_twr)
  </read_first>
  <action>
    1. Add module const `UNREALIZED_MATERIALITY_RATIO = 0.05` beside FLOW_DOM_RATIO
       (:61) with a comment citing Q5 (5% of anchor; warning-only; tuned at Phase 78
       like FLOW_DOM_RATIO).
    2. Add `unrealized_pnl_in_anchor: bool` to NavTWRMeta (:106), total=False, with a
       comment: "Phase 77 FLOW-04 — the MTM anchor's open uPnL wedge is material
       (|open_unrealized_usd|/anchor > UNREALIZED_MATERIALITY_RATIO). Rides the same
       complete_with_warnings channel as the DQ-01/DQ-02 guards; NO parallel status."
    3. In `_build_nav_meta` (:350-357), add the explicit assignment
       `if flags.get("unrealized_pnl_in_anchor"): meta["unrealized_pnl_in_anchor"] = True`
       (mirror the existing keys — the `warn = bool(flags)` line already flips the hint).
    4. In `reconstruct_nav_and_twr`, AFTER computing terminal_nav (:507-509) and BEFORE
       `return`, compute the materiality flag from the ALREADY-AVAILABLE `anchor_nav`
       and `open_unrealized_usd`: coerce both via `_coerce_float`; evaluate the ratio
       ONLY when the coerced anchor > DUST_NAV_FLOOR (noise/divide-by-zero guard — a
       dust or non-positive anchor is meaningless); set a local
       `unrealized_pnl_in_anchor = abs(upnl) / anchor > UNREALIZED_MATERIALITY_RATIO`.
       Merge it into the `flags` mapping returned by chain_linked_twr BEFORE
       `_build_nav_meta(flags)` (build a new dict: `flags = {**flags,
       "unrealized_pnl_in_anchor": True}` only when the boolean is True, so the SC-4
       zero-wedge default stays byte/status-identical — no key when uPnL==0).
    5. Extend the `reconstruct_nav_and_twr` docstring (:488-496) with the Q3 invariant
       (per RESEARCH.md verbatim intent): "Intra-window NAV is realized-basis; uPnL is
       reconciled only at the terminal (endpoint), because historical open-position
       marks are not retrievable on read-only keys. A material terminal wedge is surfaced
       via `unrealized_pnl_in_anchor`, never spread across history (that would fabricate
       marks)." Do NOT construct any per-day uPnL array. Trust the wedge's sign verbatim
       (a positive uPnL lowers the realized-basis terminal); non-finite uPnL already
       fails loud via _coerce_float at :509.
    Keep the raise/log discipline: the flag is a BOOL — never log/emit the raw USD wedge
    (account-size leak T-73-02 / T-76-03-LEAK).
  </action>
  <verify>
    <automated>/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_nav_twr.py -x -q && cd analytics-service && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m mypy --strict services/nav_twr.py</automated>
  </verify>
  <acceptance_criteria>
    All four Task-1 tests PASS. Every pre-existing test_nav_twr.py test (74-01 byte-identity,
    76-03 reconcile, DQ guards) stays GREEN — proving the zero-wedge default is unchanged.
    mypy --strict clean on nav_twr.py. Boundary mutation (flip `>` to `<`) reddens the
    materiality test; equivalence mutation (subtract wedge into nav.iloc[-1] post-roll)
    reddens the no-step test.
  </acceptance_criteria>
  <done>Materiality flag fires >5% / clear <=5% / suppressed on dust; wedge-equivalence byte-identical; invariant documented; mypy clean; full test_nav_twr.py green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pure core ← caller-supplied floats | `open_unrealized_usd` / `anchor_nav` cross into the I/O-free core; must be finite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-01 | Tampering | reconstruct_nav_and_twr terminal | mitigate | `_coerce_float` on anchor_nav + open_unrealized_usd rejects non-finite → NavReconstructionError (never a silent NaN terminal) — existing at :507-509 |
| T-77-02 | Information Disclosure | materiality flag / logs | mitigate | flag is a BOOL; no raw USD wedge/NAV in any log or raise (account-size leak class) |
| T-77-03 | Tampering (integrity) | Q3 no-fabricated-marks | mitigate | source-scan test asserts NO per-day uPnL array is constructed; wedge applied as a single terminal scalar |
| T-77-SC | Tampering | pip installs | accept | This phase installs NO packages (RESEARCH Package Legitimacy Audit: none) — no supply-chain surface |
</threat_model>

<verification>
- `$PY312 -m pytest analytics-service/tests/test_nav_twr.py -q` fully green (new + all prior pins).
- `$PY312 -m mypy --strict analytics-service/services/nav_twr.py` clean.
- SC-4 preserved: a zero-wedge call carries no `unrealized_pnl_in_anchor` key and stays `complete`.
</verification>

<success_criteria>
- unrealized_pnl_in_anchor fires when |open_unrealized_usd|/anchor > 5%, clear at/below, suppressed on dust anchors.
- Wedge is a pure terminal shift (byte-identity to a pre-reduced anchor; no intra-window step).
- The realized-basis-intraday / MTM-at-endpoint invariant is documented; no per-day mark array exists (source-scanned).
</success_criteria>

<output>
Create `.planning/phases/77-upnl-basis-reconciliation/77-01-SUMMARY.md` when done.
</output>
