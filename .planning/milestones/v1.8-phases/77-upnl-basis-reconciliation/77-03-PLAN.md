---
phase: 77-upnl-basis-reconciliation
plan: 03
type: tdd
wave: 2
depends_on: ["77-01", "77-02"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_job_worker.py
  - analytics-service/tests/test_analytics_runner.py
autonomous: true
requirements: [FLOW-04]

must_haves:
  truths:
    - "OKX/Deribit derive the wedge from the companion read and pass open_unrealized_usd into combine_realized_and_funding so the roll terminal is realized-basis"
    - "Bybit/Binance pass open_unrealized_usd=0.0 — a flow-less Bybit/Binance account's return is UNCHANGED (no double-count)"
    - "A dust/heuristic anchor (balance_error True or equity <= DUST_NAV_FLOOR) forces open_unrealized_usd=0.0 — the wedge is never subtracted onto a heuristic base"
    - "A material wedge raises unrealized_pnl_in_anchor which lifts the factsheet to complete_with_warnings via the P73-76 DQ bridge (pre-stamp + promotion predicate); a healed account clears it"
    - "No derive-path code mutates a stored equity scalar with the wedge — the wedge only reaches the roll terminal (csv_daily_returns is the sole derive write)"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "venue-gated open_unrealized_usd derivation (companion reads) + threaded into combine_realized_and_funding + unrealized_pnl_in_anchor added to _BROKER_WARN_FLAGS pre-stamp"
      contains: "open_unrealized_usd"
    - path: "analytics-service/services/analytics_runner.py"
      provides: "unrealized_pnl_in_anchor added to DataQualityFlags, the NavTWRMeta lift loop, the consumer_specific_flags promotion predicate, and the broker warn-flag lift"
      contains: "unrealized_pnl_in_anchor"
  key_links:
    - from: "derive_broker_dailies (job_worker.py:2101 ccxt / :2003 deribit)"
      to: "combine_realized_and_funding(open_unrealized_usd=...)"
      via: "venue-gated wedge from the 77-02 companion reads, forced 0 on balance_error/dust"
      pattern: "open_unrealized_usd"
    - from: "meta.unrealized_pnl_in_anchor"
      to: "strategy_analytics.data_quality_flags → complete_with_warnings"
      via: "_BROKER_WARN_FLAGS pre-stamp (job_worker) + lift loop + promotion predicate (analytics_runner)"
      pattern: "unrealized_pnl_in_anchor"
---

<objective>
Wire the venue-gated uPnL wedge into the broker derive path and lift its materiality
flag through the existing P73-76 DQ bridge. This closes FLOW-04: OKX/Deribit feed the
companion wedge into `combine_realized_and_funding(open_unrealized_usd=...)` (the roll
terminal becomes realized-basis); Bybit/Binance pass 0.0 (no double-count); a
balance_error/dust anchor forces 0.0; and `unrealized_pnl_in_anchor` promotes the
factsheet to `complete_with_warnings`. The reported CURRENT NAV re-add is DEFINITIONAL —
the derive path writes only `csv_daily_returns`, never a stored equity scalar, so the
displayed MTM AUM keeps its existing source untouched (Q4 tail confirmed).

Purpose: SC-2 (realized-basis terminal wired) + SC-3 (flag lifted to the factsheet).
Output: job_worker + analytics_runner wiring; venue-gated + DQ-bridge tests.

FLOW-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-upnl-basis-reconciliation/77-RESEARCH.md
@.planning/phases/77-upnl-basis-reconciliation/77-VALIDATION.md
@.planning/phases/77-upnl-basis-reconciliation/77-01-SUMMARY.md
@.planning/phases/77-upnl-basis-reconciliation/77-02-SUMMARY.md

<interfaces>
From 77-01 (nav_twr.py): `NavTWRMeta.unrealized_pnl_in_anchor: bool` set by the core when
`|open_unrealized_usd|/anchor > UNREALIZED_MATERIALITY_RATIO` (5%), on the
complete_with_warnings channel.

From 77-02: `exchange.fetch_account_equity_and_upnl_usd(exchange, venue) -> (equity,
balance_error, open_unrealized_usd)` (OKX upl / Bybit-Binance 0.0);
`deribit_ingest.fetch_deribit_account_equity_and_upnl_usd(exchange) -> (equity,
balance_error, open_unrealized_usd)` (session_upl / 0.0 fallback).

From services/broker_dailies.py (VERIFIED — NO CHANGE NEEDED):
- `combine_realized_and_funding(realized, funding, account_balance, balance_error=False,
  *, external_flows=None, open_unrealized_usd=0.0)` (:130-160) already threads
  `open_unrealized_usd` straight to the core.

From services/job_worker.py (VERIFIED):
- deribit branch anchor read at :2003 (`fetch_deribit_account_equity_usd`).
- ccxt else-branch anchor read at :2101 (`fetch_account_equity_usd(ctx.exchange, venue)`).
- the combine call at :2172-2175 (currently passes external_flows, NOT open_unrealized_usd).
- `_BROKER_WARN_FLAGS` pre-stamp tuple at :2378-2387 (flow_coverage_incomplete,
  negative_nav_guard, dust_nav_guard, flow_dominated_guard) → data_quality_flags upsert.

From services/analytics_runner.py (VERIFIED):
- `DataQualityFlags` TypedDict (:117-181) — add the key near :179.
- NavTWRMeta guard-key lift loop at :1747-1755.
- `consumer_specific_flags` promotion predicate at :1826-1835.
- broker `_BROKER_WARN_FLAGS` lift at :2156-2165.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — venue-gated wedge + DQ-bridge tests</name>
  <files>analytics-service/tests/test_job_worker.py, analytics-service/tests/test_analytics_runner.py</files>
  <read_first>
    - services/job_worker.py:2097-2175 (ccxt else-branch anchor read + combine call), :2000-2005 (deribit anchor), :2360-2398 (pre-stamp)
    - services/analytics_runner.py:1747-1755, :1826-1835, :2156-2165 (the three lift/promotion sites)
    - tests/test_job_worker.py + tests/test_analytics_runner.py — existing derive_broker_dailies / promotion test style (stubbed ctx.exchange + supabase spy)
  </read_first>
  <behavior>
    - upnl_wedge_venue_okx: derive_broker_dailies for OKX with a companion read returning
      a material upl → combine_realized_and_funding is called with open_unrealized_usd == the
      wedge (spy the kwarg), and the resulting meta carries unrealized_pnl_in_anchor.
    - upnl_wedge_venue_bybit / _binance: companion returns wedge 0.0 → combine called with
      open_unrealized_usd == 0.0, and a flow-less Bybit/Binance return Series is
      BYTE-IDENTICAL (rtol 1e-12) to the pre-77 baseline (no double-count). Mutation note:
      forcing a non-zero wedge here reddens this (Pitfall 2).
    - upnl_wedge_heuristic_anchor: balance_error True (or equity <= DUST_NAV_FLOOR) → the
      wedge is forced to 0.0 even for OKX (never subtract onto a heuristic base); no flag.
    - prestamp_lifts_flag: when meta.unrealized_pnl_in_anchor is True, the strategy_analytics
      pre-stamp upsert includes {"unrealized_pnl_in_anchor": True}; a clean re-derive omits it
      (healed → cleared).
    - promotion_predicate (analytics_runner): a strategy_analytics row whose
      data_quality_flags carries unrealized_pnl_in_anchor → run_csv_strategy_analytics
      promotes computation_status to "complete_with_warnings"; absent → stays "complete"
      (SC-4 exact-string). Boundary mutation: dropping the predicate OR-term reddens.
  </behavior>
  <action>
    Add tests to test_job_worker.py (venue gating + heuristic guard + pre-stamp) and
    test_analytics_runner.py (lift loop + promotion predicate). Reuse existing stubbed
    ctx.exchange / supabase upsert-spy fixtures. Patch the 77-02 companion reads to return
    controlled (equity, balance_error, wedge). For the byte-identity Bybit/Binance pin,
    snapshot the returns Series with the wedge path present but wedge==0 and assert
    equality to a wedge-absent baseline. Run under $PY312. In RED these fail because
    job_worker still calls the 2-tuple reads and never threads open_unrealized_usd, and
    analytics_runner has no unrealized_pnl_in_anchor term.
  </action>
  <verify>
    <automated>/private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_job_worker.py analytics-service/tests/test_analytics_runner.py -k "upnl or unrealized_pnl_in_anchor or wedge" -x</automated>
  </verify>
  <acceptance_criteria>
    New tests collected and RED for the right reason (wedge not threaded; predicate term
    missing). No unrelated test errors at collection.
  </acceptance_criteria>
  <done>Venue-gating, heuristic-guard, pre-stamp, and promotion tests present and RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — thread the venue-gated wedge + lift the flag through the DQ bridge</name>
  <files>analytics-service/services/job_worker.py, analytics-service/services/analytics_runner.py</files>
  <read_first>
    - services/job_worker.py:2000-2175 (both anchor reads + combine call), :2378-2398 (pre-stamp)
    - services/analytics_runner.py:117-181, :1747-1755, :1826-1835, :2156-2165
    - 77-01-SUMMARY.md + 77-02-SUMMARY.md (final function names/return shapes)
  </read_first>
  <action>
    job_worker.py (derive_broker_dailies):
    1. deribit branch (:2003): switch the anchor read to
       `fetch_deribit_account_equity_and_upnl_usd` → `equity, balance_error,
       open_unrealized_usd`. (Import from services.deribit_ingest.)
    2. ccxt else-branch (:2101): switch to
       `fetch_account_equity_and_upnl_usd(ctx.exchange, venue)` →
       `equity, balance_error, open_unrealized_usd` (OKX upl; bybit/binance 0.0).
    3. Add ONE shared noise-guard right after each read (or a single point before the
       combine call): force `open_unrealized_usd = 0.0` when `balance_error` is True OR
       `equity is None` OR `abs(equity) <= DUST_NAV_FLOOR` (import DUST_NAV_FLOOR from
       services.nav_twr) — never subtract a wedge onto a heuristic/dust base (Pitfall 5).
       A guarded/adjusted equity keeps its existing balance_error handling.
    4. combine call (:2172-2175): add `open_unrealized_usd=open_unrealized_usd`. Do NOT
       mutate `equity` (the stored/displayed MTM anchor stays full — the re-add is
       definitional; the derive path writes only csv_daily_returns, confirming Q4 tail).
    5. Pre-stamp (:2378): add `"unrealized_pnl_in_anchor"` to the `_BROKER_WARN_FLAGS`
       tuple so a material wedge rides the same MED-2/MED-3 broker→CSV bridge and a clean
       re-derive clears it.
    analytics_runner.py:
    6. Add `unrealized_pnl_in_anchor: bool` to `DataQualityFlags` (~:179) with a Phase-77
       comment.
    7. Add `"unrealized_pnl_in_anchor"` to the NavTWRMeta guard-key lift loop (:1747-1752).
    8. Add `or (top_level_flags or {}).get("unrealized_pnl_in_anchor")` to the
       `consumer_specific_flags` predicate (:1826-1835).
    9. Add `"unrealized_pnl_in_anchor"` to the broker `_BROKER_WARN_FLAGS` lift tuple
       (:2156-2161).
    No raw USD anywhere (flag is a bool). broker_dailies.py needs NO change (already
    threads open_unrealized_usd).
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest tests/test_job_worker.py tests/test_analytics_runner.py -x -q && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m mypy --strict services/job_worker.py services/analytics_runner.py</automated>
  </verify>
  <acceptance_criteria>
    All Task-1 tests PASS. OKX/Deribit thread the real wedge; Bybit/Binance stay 0.0 with
    byte-identical flow-less returns; balance_error/dust forces 0.0; unrealized_pnl_in_anchor
    lifts to complete_with_warnings and clears on a healed account. SC-4 exact-string
    "complete" preserved for a no-wedge flow-less account. mypy --strict clean on both files.
  </acceptance_criteria>
  <done>Wedge threaded venue-gated + noise-guarded; flag bridged to the factsheet; stored MTM equity untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Full-suite + no-stored-scalar-mutation guard (Q4 tail)</name>
  <files>analytics-service/tests/test_job_worker.py</files>
  <read_first>
    - services/job_worker.py: the derive_broker_dailies write path (csv_daily_returns upsert; confirm no account_balance_usdt write on this path — RESEARCH Q4 says derive writes only csv_daily_returns)
  </read_first>
  <action>
    Add a source-scan/behavioral test asserting derive_broker_dailies does NOT subtract
    `open_unrealized_usd` from any stored equity scalar (account_balance_usdt is not
    written on the derive path; the wedge reaches only combine_realized_and_funding's
    open_unrealized_usd kwarg). Then run the FULL analytics suite in $PY312 to confirm all
    P73-76 byte-identity / delegation / DQ pins stay green (no cross-file regression from
    the new flag).
  </action>
  <verify>
    <automated>cd analytics-service && /private/tmp/claude-501/<project-scratch>/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    Full analytics suite green in the CI-3.12 venv (>= 3092 pass baseline + new tests, 92
    skip). The no-stored-scalar test passes, encoding the Q4-tail invariant.
  </acceptance_criteria>
  <done>Full suite green; the definitional re-add (no stored-scalar mutation) is pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| companion read → derive path | venue-supplied wedge crosses into the roll; must be gated + noise-guarded |
| meta flag → factsheet status | DQ flag lifts the user-visible computation_status |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-07 | Tampering (integrity) | Bybit/Binance double-count | mitigate | wedge forced 0.0 for walletBalance venues; flow-less byte-identity test pins them unmoved; Phase 78 parity gate |
| T-77-08 | Tampering | wedge onto heuristic/dust anchor | mitigate | force open_unrealized_usd=0.0 when balance_error / equity<=DUST_NAV_FLOOR (Pitfall 5) |
| T-77-09 | Information Disclosure | account-size leak | mitigate | flag is a bool; no raw USD wedge/NAV in logs, raises, or the DQ flags JSON |
| T-77-10 | Tampering | stored equity scalar mutation (Q4) | mitigate | derive writes only csv_daily_returns; no-stored-scalar test; equity never reduced by the wedge |
| T-77-SC | Tampering | pip installs | accept | no packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- Full analytics suite green in $PY312; mypy --strict clean on job_worker.py + analytics_runner.py.
- OKX/Deribit realized-basis terminal wired; Bybit/Binance flow-less returns byte-identical.
- unrealized_pnl_in_anchor lifts to complete_with_warnings and clears on heal; SC-4 "complete" preserved.
</verification>

<success_criteria>
- SC-2: the roll terminal is realized-basis (anchor − wedge) for the MTM venues; no step discontinuity; stored MTM equity untouched.
- SC-3: a material wedge surfaces on the factsheet as complete_with_warnings via the DQ bridge.
- No double-count on Bybit/Binance; no wedge on heuristic/dust anchors; per-day true-up NOT implemented.
</success_criteria>

<output>
Create `.planning/phases/77-upnl-basis-reconciliation/77-03-SUMMARY.md` when done.
</output>
