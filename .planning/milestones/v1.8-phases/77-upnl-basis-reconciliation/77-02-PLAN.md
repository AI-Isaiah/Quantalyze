---
phase: 77-upnl-basis-reconciliation
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/exchange.py
  - analytics-service/services/deribit_ingest.py
  - analytics-service/tests/test_exchange.py
  - analytics-service/tests/test_job_worker_deribit.py
autonomous: true
requirements: [FLOW-04]

must_haves:
  truths:
    - "OKX open uPnL is read from the SAME private_get_account_balance response that already reads totalEq — no new HTTP round-trip"
    - "Bybit and Binance return a structurally-zero wedge (their anchor is realized-basis walletBalance) so the caller never subtracts uPnL there"
    - "Deribit session uPnL is read from the SAME get_account_summaries response as the equity anchor and USD-converted with the same index_prices; an absent/uncertain field falls back to wedge 0.0 (NEVER fabricated)"
  artifacts:
    - path: "analytics-service/services/exchange.py"
      provides: "OKX companion returning (totalEq, upl); venue-dispatched equity+uPnL read returning (equity, balance_error, open_unrealized_usd)"
      contains: "upl"
    - path: "analytics-service/services/deribit_ingest.py"
      provides: "Deribit account equity + session-uPnL companion returning (equity, balance_error, open_unrealized_usd)"
      contains: "session_upl"
  key_links:
    - from: "fetch_okx_total_equity_usd path (exchange.py:2690-2724)"
      to: "data[0].upl"
      via: "same raw private_get_account_balance response object"
      pattern: "upl"
    - from: "fetch_deribit_account_equity_usd (deribit_ingest.py:706-761)"
      to: "summaries[].session_upl"
      via: "same get_account_summaries response + index_prices USD conversion"
      pattern: "session_upl"
---

<objective>
Add the venue-matched companion open-uPnL reads (SC-1) WITHOUT any new fetch. OKX's
`upl` rides the same `private_get_account_balance` response as `totalEq`; Deribit's
session uPnL rides the same `get_account_summaries` response as the equity anchor;
Bybit and Binance are structurally realized-basis (walletBalance) so their wedge is
0.0. The single most important property (Q2 / Pitfall 2): the companion returns the
uPnL *embedded in the specific anchor field*, which is 0 for the walletBalance venues —
so blindly-subtracting on Bybit/Binance can never happen.

Purpose: SC-1 — the one genuinely new upstream data dependency, gated to the MTM venues.
Output: exchange.py OKX+dispatch companion; deribit_ingest.py session-uPnL companion;
stubbed-response tests proving the venue gating and the safe Deribit fallback.

FLOW-04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/77-upnl-basis-reconciliation/77-RESEARCH.md
@.planning/phases/77-upnl-basis-reconciliation/77-VALIDATION.md

<interfaces>
From services/exchange.py (VERIFIED):
- `fetch_okx_total_equity_usd(exchange) -> float | None` (:2690-2724): calls
  `raw = await exchange.private_get_account_balance()`; `data = (raw or {}).get("data")`;
  reads `data[0].get("totalEq")`. `data[0].get("upl")` is the SIBLING wedge in the SAME
  response (OKX total account unrealized PnL in USD).
- `fetch_account_equity_usd(exchange, venue) -> tuple[float | None, bool]` (:2727-2745):
  dispatches OKX→raw totalEq, else `fetch_usdt_balance_with_status` (Bybit/Binance).
  Returns `(equity, balance_error)`.

From services/deribit_ingest.py (VERIFIED):
- `fetch_deribit_account_equity_usd(exchange) -> tuple[float | None, bool]` (:706-761):
  `resp = await exchange.private_get_get_account_summaries({})`;
  `summaries = resp["result"]["summaries"]`; resolves a `{ccy}_usd` index per non-linear
  ccy into `index_prices`; `equity = deribit_equity_to_usd(summaries, index_prices)`.
  Each `summ` carries `currency`, `equity`, and (per Deribit schema) `session_upl`.
- `deribit_txn._LINEAR_CURRENCIES` (USD-family pass-through as USD).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OKX + venue-dispatch companion uPnL read (exchange.py) — RED then GREEN</name>
  <files>analytics-service/services/exchange.py, analytics-service/tests/test_exchange.py</files>
  <read_first>
    - services/exchange.py:2690-2745 (fetch_okx_total_equity_usd + fetch_account_equity_usd)
    - services/exchange.py: fetch_usdt_balance_with_status (Bybit/Binance path) — signature + return shape
    - tests/test_exchange.py — existing OKX stub style (raw private_get_account_balance mock)
  </read_first>
  <behavior>
    - okx_upl: a stubbed OKX exchange whose private_get_account_balance returns
      `{"data": [{"totalEq": "100000", "upl": "8000"}]}` → the new dispatch returns
      `(equity≈100000.0, balance_error=False, open_unrealized_usd≈8000.0)` from a SINGLE
      call to private_get_account_balance (assert the mock was awaited exactly once —
      no new fetch).
    - okx_upl sign: `upl` negative (e.g. "-4000") → open_unrealized_usd ≈ -4000.0
      (sign trusted verbatim).
    - okx_missing_upl: response with totalEq but no `upl` key / null upl →
      open_unrealized_usd == 0.0, balance_error stays driven by totalEq only (never
      fabricate a wedge).
    - bybit_binance_zero: venue in {"bybit","binance"} → open_unrealized_usd == 0.0
      regardless of balance (the anchor is realized-basis walletBalance). Mutation note:
      returning a non-zero wedge here is the Pitfall-2 double-count.
    - okx_balance_error: failed/empty response → (None, True, 0.0).
  </behavior>
  <action>
    1. Refactor the OKX raw read so both fields come from ONE response. Add
       `fetch_okx_total_equity_and_upl_usd(exchange) -> tuple[float | None, float]`
       returning `(totalEq_or_None, upl_float)`: reuse the existing try/scrub/parse of
       fetch_okx_total_equity_usd; additionally read `data[0].get("upl")`, coerce via a
       local float-or-0.0 (null/absent/non-numeric → 0.0 — never fabricate). Keep
       fetch_okx_total_equity_usd working (delegate it to the new function's first
       element) so existing callers/tests are unaffected.
    2. Add `fetch_account_equity_and_upnl_usd(exchange, venue) -> tuple[float | None,
       bool, float]` beside fetch_account_equity_usd: for OKX use the combined read
       (`eq, upl` → `(eq, eq is None, upl if eq is not None else 0.0)`); for every other
       ccxt venue (bybit/binance) call fetch_usdt_balance_with_status and return
       `(equity, balance_error, 0.0)` — wedge structurally 0 (Q2/Pitfall 2). Docstring:
       cite that Bybit/Binance map to realized-basis walletBalance (ccxt 4.5.59
       bybit.parse_balance:3376 / binance spot), so the wedge is 0 and subtracting it
       would double-count. Do NOT change fetch_account_equity_usd (allocator/other
       callers keep the 2-tuple).
    Scrub-before-log discipline unchanged; no raw USD in logs.
  </action>
  <verify>
    <automated>/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_exchange.py -k "okx_upl or upnl or wedge or zero" -x && cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m mypy --strict services/exchange.py</automated>
  </verify>
  <acceptance_criteria>
    OKX single-call companion returns (equity, balance_error, upl); Bybit/Binance return
    wedge 0.0; missing upl → 0.0; sign preserved. mypy --strict clean on exchange.py.
    fetch_account_equity_usd + its existing tests unchanged/green.
  </acceptance_criteria>
  <done>Venue-dispatched (equity, balance_error, open_unrealized_usd) read; OKX no new fetch; Bybit/Binance structurally 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deribit session-uPnL companion (deribit_ingest.py) — RED then GREEN</name>
  <files>analytics-service/services/deribit_ingest.py, analytics-service/tests/test_job_worker_deribit.py</files>
  <read_first>
    - services/deribit_ingest.py:706-761 (fetch_deribit_account_equity_usd — response shape + index_prices resolution)
    - services/deribit_txn.py:245-299 (deribit_equity_to_usd — per-ccy USD conversion + _LINEAR_CURRENCIES)
    - tests/test_job_worker_deribit.py — existing get_account_summaries stub style
  </read_first>
  <behavior>
    - deribit_session_upl: summaries `[{"currency":"BTC","equity":2.0,"session_upl":0.3}]`
      with a BTC_usd index of 40000 → equity ≈ 80000.0 AND open_unrealized_usd ≈ 12000.0
      (0.3 BTC × 40000), from the SAME summaries response + same index_prices. USD-family
      currency session_upl passes through as USD (no index multiply).
    - deribit_missing_field (A1 fallback): summaries with equity but NO session_upl key /
      null → open_unrealized_usd == 0.0 (safe conservative fallback; realized-basis
      terminal on equity), NEVER a fabricated value.
    - deribit_balance_error: failed read → (None, True, 0.0).
    - deribit_no_index: a non-linear ccy holding uPnL but no resolvable USD index →
      mirrors the existing equity behavior (balance_error path), wedge not fabricated.
  </behavior>
  <action>
    Add `fetch_deribit_account_equity_and_upnl_usd(exchange) -> tuple[float | None, bool,
    float]`: reuse the EXISTING body of fetch_deribit_account_equity_usd (same
    get_account_summaries call + index_prices resolution + deribit_equity_to_usd for the
    anchor), and additionally sum a session-uPnL wedge from the SAME summaries: for each
    summ read `session_upl` (per RESEARCH A1 — the Deribit account-summary open-unrealized
    component; confirm the field name against a live LTP read at execute time), coerce
    absent/null/non-numeric to 0.0 (fallback, never fabricate), and USD-convert with the
    SAME index_prices used for equity (USD-family pass-through; non-linear ccy × its
    {ccy}_usd index — reuse the exact conversion rule of deribit_equity_to_usd; if the
    equity anchor already failed loud for a missing index, the wedge path inherits the
    same (None, True, 0.0) disposition). Keep fetch_deribit_account_equity_usd intact
    (delegate its 2-tuple to the new function's first two elements) so existing callers
    are unaffected. Add a docstring note tagging session_upl as [ASSUMED A1] with the
    wedge-0 fallback contract. No raw USD in logs.
  </action>
  <verify>
    <automated>/private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m pytest analytics-service/tests/test_job_worker_deribit.py -k "session_upl or upnl or wedge or fallback" -x && cd analytics-service && /private/tmp/claude-501/-Users-helios-mammut-claude-projects-quantalyze/fcce1bd5-15ef-4e42-adb9-85cfc9ad484c/scratchpad/venv312/bin/python -m mypy --strict services/deribit_ingest.py</automated>
  </verify>
  <acceptance_criteria>
    Deribit companion returns (equity, balance_error, session-uPnL-USD); missing field →
    wedge 0.0 (fallback, no fabrication); index/USD conversion matches the equity anchor's
    rule. mypy --strict clean. fetch_deribit_account_equity_usd + its tests unchanged/green.
  </acceptance_criteria>
  <done>Deribit session uPnL read from the same summaries response with a safe 0.0 fallback; equity anchor path intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exchange REST (read-only key) → worker | untrusted exchange JSON crosses here (totalEq/upl, summaries/session_upl) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-77-04 | Tampering (integrity) | Bybit/Binance wedge | mitigate | venue dispatch returns structural 0.0 for walletBalance venues — subtracting there would double-count (Pitfall 2); 77-03 parity test pins flow-less accounts unmoved |
| T-77-05 | Tampering | malformed upl/session_upl field | mitigate | null/absent/non-numeric coerces to 0.0 (fallback, never fabricate); finite coercion happens in the core (_coerce_float) when subtracted |
| T-77-06 | Information Disclosure | balance/upl in logs | mitigate | scrub-before-log preserved; no raw USD emitted |
| T-77-SC | Tampering | pip installs | accept | no packages installed (RESEARCH audit: none) |
</threat_model>

<verification>
- `$PY312 -m pytest analytics-service/tests/test_exchange.py analytics-service/tests/test_job_worker_deribit.py -q` green.
- `$PY312 -m mypy --strict analytics-service/services/exchange.py analytics-service/services/deribit_ingest.py` clean.
- OKX asserts single response object (no new fetch); Bybit/Binance wedge == 0.0; Deribit missing-field → 0.0.
</verification>

<success_criteria>
- SC-1: companion open-uPnL read exists per venue, gated to the MTM venues (OKX upl, Deribit session_upl); Bybit/Binance structurally 0.
- No new HTTP round-trip for the MTM venues; safe non-fabricating fallback on absent fields.
</success_criteria>

<output>
Create `.planning/phases/77-upnl-basis-reconciliation/77-02-SUMMARY.md` when done.
</output>
