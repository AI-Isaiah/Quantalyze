---
milestone: v1.2.1
audited: 2026-06-25
status: passed
scores:
  requirements: 18/18
  phases: 5/5
  integration: 18/18
  flows: 2/2
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: 37-honest-per-data-source-toggle
    items:
      - "Deferred UAT (37-HUMAN-UAT.md): authed live-prod per-source toggle on real per-key data — needs post-deploy + post-backfill + an authed Chromium session (headless can't hydrate authed pages). Code paths proven by the vitest RTL suite incl. the mutation-falsifiable DSRC-03 honesty oracle."
  - phase: 38-composer-factsheet-parity-blank-mode-fix
    items:
      - "Deferred UAT (38-HUMAN-UAT.md): factsheet-grade interaction feel (wheel-zoom/drag-pan/brush/keyboard + shared window) and blank-slate overlay end-to-end — only verifiable in a deployed authed Chromium session (JSDOM has no layout/D3-zoom). Code paths proven incl. the shared-window test + mutation-falsifiable PARITY-03 blank-slate cases."
      - "Pre-existing unused import trackUsageEventClient in AllocationsTabs.tsx:33 (logged in 38-04 deferred-items) — out of scope, no functional impact."
nyquist:
  compliant_phases: [37, 38]
  partial_phases: [34, 35]
  missing_phases: [36]
  overall: partial
---

# Milestone v1.2.1 — scenario-tab-hardening — Audit

**Audited:** 2026-06-25
**Status:** ✅ passed
**Definition of done:** Unify every exchange API key's stats through the ONE CSV →
`compute_all_metrics` path (per-key dailies, 252-unified annualization) so Overview,
Scenario, and factsheets read the same honest source, and finish the scenario composer's
convergence onto the factsheet (honest per-data-source toggle + factsheet-grade charts).

## Requirements Coverage — 18/18 satisfied

| Req | Phase | VERIFICATION | SUMMARY | Final |
|-----|-------|--------------|---------|-------|
| ANNUAL-01..05 | 34 | passed | listed | satisfied |
| DAILIES-01..04 | 35 | passed | listed | satisfied |
| UNIFY-01..03 | 36 | passed | listed | satisfied (traceability checkboxes reconciled this audit) |
| DSRC-01..03 | 37 | human_needed (auto-checks passed) | listed | satisfied |
| PARITY-01..03 | 38 | human_needed (auto-checks passed) | listed | satisfied (traceability checkboxes reconciled this audit) |

3-source cross-reference (VERIFICATION status + SUMMARY `requirements_completed` + REQUIREMENTS
traceability) confirms all 18 satisfied. Stale `[ ]/Pending` checkboxes for UNIFY-02/03 and
PARITY-01/02 (their owning phases were complete + verified) were reconciled to `[x]/Complete`
during this audit. No orphaned requirements. No unsatisfied requirements.

## Phase Verification — 5/5

| Phase | Status | Note |
|-------|--------|------|
| 34 Asset-class-aware annualization | passed | — |
| 35 Per-key dailies foundation | passed | — |
| 36 Repoint stats reads | passed | — |
| 37 Honest per-data-source toggle | human_needed | 3/3 must-haves verified; UAT deferred to post-deploy (37-HUMAN-UAT.md) |
| 38 Composer factsheet parity + blank-mode fix | human_needed | 3/3 must-haves verified; UAT deferred to post-deploy (38-HUMAN-UAT.md) |

`human_needed` = all automated checks pass; only authed live-prod verification is pending
(deferred-by-construction — not a code gap). No phase has `gaps_found`.

## Cross-Phase Integration — 18/18 wired, 2/2 flows complete

The integration checker traced both chains end-to-end (grep + suite runs, all green):

- **Chain 1 — per-key unification pipeline (34→35→36→37):** the explicit-252 basis (34) →
  the `csv_daily_returns` per-`api_key_id` store + derive job (35) → `queries.ts` reading it
  into Overview stats via the compute path (36) → the composer reading the SAME per-key series
  on the payload for its toggle (37). **Critical consistency check PASSED:** the eligibility
  predicate (`isPerKeyDailiesEligibleKey` / `allActiveKeysHavePerKeyDailies`) is a single SoT
  computed SSR-side; the composer mirrors `perKeyDailiesGateSatisfied` / `eligibleApiKeyIds`
  from the payload and never re-derives it — no divergence between Overview (36) and composer (37).
- **Chain 2 — composer factsheet convergence (37→38):** the toggle's exclusion mutates
  `projectionState.selected[api_key_id]` → `computeScenario` re-blends → `scenarioWealthSeries`
  → `ScenarioFactsheetChart` (real factsheet `TimeSeriesChart` + `MasterBrush` under one
  `persist={false}` provider, shared brush-zoom window). Blank-slate (PARITY-03) renders the
  scenario overlay (degeneracy keys on scenario length, not baseline). All wired.

0 orphaned exports · 0 missing connections · 0 broken flows. RLS/owner-gate on the per-key read
confirmed + cross-tenant-leak test pinned.

## Nyquist Coverage (discovery only — non-blocking)

| Phase | VALIDATION.md | Compliant |
|-------|---------------|-----------|
| 34 | exists | partial (nyquist_compliant: false — older phase) |
| 35 | exists | partial (nyquist_compliant: false — older phase) |
| 36 | missing | — (older phase; verified passed without a VALIDATION.md) |
| 37 | exists | ✅ compliant |
| 38 | exists | ✅ compliant |

Phases 34–36 (built in prior sessions) predate this run's Nyquist discipline; they verified
`passed` regardless. Optional retroactive `/gsd:validate-phase 34|35|36` could close the
discovery gap but is NOT required — non-blocking, no requirement is unverified.

## Verdict

All 18 requirements satisfied and cross-phase-wired; 5/5 phases verified; 2/2 E2E flows complete;
no critical gaps; no broken flows. Outstanding items are deferred-by-construction post-deploy UATs
(2) + an optional Nyquist-discovery gap on the three older phases. **Milestone achieved its
definition of done → proceed to complete-milestone.**
