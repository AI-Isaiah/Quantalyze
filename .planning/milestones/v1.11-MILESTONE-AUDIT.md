---
milestone: v1.11
audited: 2026-07-18
status: tech_debt
scores:
  requirements: 29/29 satisfied-or-wired (0 unsatisfied, 0 orphaned)
  phases: 10/10 have VERIFICATION.md (7 passed, 3 human_needed — QA pending, not code gaps)
  integration: 8/8 cross-phase seams WIRED (0 orphaned, 0 missing)
  flows: E2E complete (allocator connect-key → dailies/backbone → derived-or-legacy equity → Overview/Scenario render; contribution overlay → private → Browse re-surface; + Strategy → picker)
gaps:
  requirements: []   # no unsatisfied / orphaned requirements — every REQ group WIRED end-to-end
  integration: []    # no blocking wiring gaps
  flows: []          # no broken flows
tech_debt:
  - phase: 109-role / 110-contrib
    items:
      - "VERIFICATION status human_needed — pre-existing manual-QA items from earlier waves (never blockers; code verified)."
  - phase: 117-uifix
    items:
      - "VERIFICATION human_needed: pixel tooltip placement (overflow/edge/Dialog/drawer), focus-ring visual non-clipping (6 sites, keyboard tab), axe route×viewport matrix on the authed/embedded surfaces that fold the changed components (seed-gated; dormant in the public-floor axe run) + an extreme high-leverage CUM RETURN value. Recommend a seeded /qa pass or seeded-env axe CI run."
  - phase: 115.1-e2-display-repoint
    items:
      - "BACKBONE-E2 derived equity path is code-wired end-to-end but DORMANT in prod: renders legacy basis via the is_trustworthy-else-legacy fallback until the two founder-gated ops run — (1) key-mode backfill `railway ssh \"cd /app && python -m scripts.phase35_backfill_enqueue\"`; (2) `E2_GROUND_TRUTH_*` env + live `scripts/e2_allocator_ground_truth.py`. Intentional safety; the derived seam + LIVE anchor reconciliation are unexercised against a real prod account until activated."
  - phase: cross-cutting
    items:
      - "Pre-existing react-hooks/exhaustive-deps lint WARNING at EquityChart.tsx:1119 (lint exits 0; owned by earlier waves; sits on the derived-curve render seam)."
      - "Durable constraint (not a break): /strategies/new/wizard stays manager-guarded (ROLE-09); allocators reach the connect wizard ONLY via the inline ContributionWizardOverlay. Any future ADDALLOC-adjacent entry point handing an allocator an href to that route would dead-end at the 109 redirect — 116 correctly uses the overlay."
  - phase: follow-ups (founder findings 2026-07-18, out of milestone scope)
    items:
      - "Per-key stitch progress in the multi-key connect wizard (founder finding) — NOT an ADDALLOC/UIFIX criterion; track as a follow-up (next milestone / backlog)."
      - "Duplicate-key React console warning on the warming-up allocator Overview — PARKED: non-functional (chart renders fine), not reproducible in jsdom (charts size via ResponsiveContainer=0×0) or the current live state; every FactsheetBody tick generator is collapse-safe (#621 already fixed the one collapsing case). Needs the exact key string captured from a live warm-up-state console to pinpoint (likely EquityChart inner-SVG transient)."
merge_state: "Tail branch gsd/v1.11-scenario-composer-v2-tail (phases 114/115/115.1/116/117 + migration 20260717233529_allocator_equity_derived_surface.sql) NOT merged to main. Migration applied to TEST only; prod applies on merge. Expected — ship is a separate founder-gated /ship + /land-and-deploy decision."
---

# Milestone v1.11 — Scenario Composer v2: Definition-of-Done Audit

**Verdict: tech_debt** — the milestone's CODE definition-of-done is met (all 29 requirements across ROLE · CONTRIB · CONSTIT · WEIGHTS · BACKBONE E1/E2 · ADDALLOC · UIFIX are WIRED end-to-end with zero unsatisfied/orphaned requirements and zero blocking integration or flow gaps). The `tech_debt` status reflects accumulated deferred/QA items — manual QA on 3 human_needed phases, the founder-gated E2 activation, a pre-existing lint warning, and two out-of-scope founder follow-ups — none of which are code blockers.

## Requirements Coverage (3-source cross-reference)

Every requirement group cross-references clean across REQUIREMENTS.md, phase VERIFICATION.md, and SUMMARY frontmatter. Integration check confirmed all seams in source (not SUMMARY self-reports). See the integration map: ROLE-01..05 (nav derivation + 7 guard sites), CONTRIB-01..05 (overlay finalize → private → owner-inclusive browse), CONSTIT-01..05 (frozen scenario.ts blend), WEIGHTS (112/113 handlers intact, additive-seam-only), BACKBONE-E1 (golden-gated deletion), BACKBONE-E2/STITCH (producer→store→consumer→display chain), ADDALLOC-01..04 (per-tab dispatch + remedy), UIFIX-01..03 (portal/ring-inset/break-words on shared surfaces).

## Phase Verification Roll-up

| Phase | Status | Note |
|-------|--------|------|
| 109 ROLE | human_needed | pre-existing manual QA |
| 110 CONTRIB | human_needed | pre-existing manual QA |
| 110.1, 111, 112, 113, 114, 115, 116 | passed | 116 also browser-verified (founder bug fixed) |
| 115.1 E2 display-repoint | (phase-close gate battery green) | derived path dormant pending founder ops |
| 117 UIFIX | human_needed | pixel/axe QA pending; code verified + review-fixed + browser smoke-clean |

## Integration & E2E

No blockers. 8/8 cross-phase seams WIRED, 0 orphaned, 0 missing, E2E flows complete. 3 advisory warnings captured under tech_debt (E2 dormant-by-design, EquityChart lint warning, manager-route durable constraint).

## Recommendation

No gap-closure phases required — there are no code gaps. The tech_debt items are QA + founder-deployment ops + tracked follow-ups. The milestone is code-complete and ready for the founder's ship decision (review → PR → land-and-deploy), after which complete-milestone (archive/tag) and the E2 activation ops apply.
