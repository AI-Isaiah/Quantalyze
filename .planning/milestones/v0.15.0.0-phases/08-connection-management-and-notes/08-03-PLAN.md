---
phase: 08-connection-management-and-notes
plan: 03
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - src/components/notes/sanitize-schema.ts
  - src/components/notes/NoteRender.tsx
  - src/components/notes/NoteRender.test.tsx
  - src/components/notes/useNoteAutoSave.ts
  - src/components/notes/useNoteAutoSave.test.ts
  - src/components/notes/NoteSaveStatus.tsx
  - src/components/notes/NoteSaveStatus.test.tsx
  - src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx
  - src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx
  - src/app/(dashboard)/allocations/lib/dashboard-defaults.ts
  - src/app/globals.css
autonomous: true
requirements:
  - MANAGE-04
  - MANAGE-05
  - MANAGE-06
tags:
  - notes
  - markdown
  - autosave
  - widget
must_haves:
  truths:
    - "NoteRender renders markdown with rehype-sanitize; script/iframe/img/on* attrs are stripped; <a> tags get rel=noopener target=_blank"
    - "useNoteAutoSave fires the PATCH save on textarea blur (not on keystroke) and guards against rapid-blur races via a generation counter"
    - "useNoteAutoSave does NOT flush on unmount — consumers must trigger save before unmount (blur covers the dominant path)"
    - "NoteSaveStatus renders Saving… / Note saved / Last saved: N — / Save failed — retry per UI-SPEC §7 copy"
    - "NotesWidget upgrades in place: fetches with scope_kind=portfolio + scope_ref=portfolio.id; renders markdown when not editing; saves on blur"
    - "LAYOUT_VERSION bumps 2 → 3 and the default dashboard layout includes notes-widget at x:0 y:27 w:4 h:4"
    - "The institutional prose-note CSS rule lives in globals.css (no @tailwindcss/typography dep added)"
  artifacts:
    - path: "src/components/notes/NoteRender.tsx"
      provides: "Shared markdown render component for all 4 scopes"
      exports: ["NoteRender"]
    - path: "src/components/notes/sanitize-schema.ts"
      provides: "hast-util-sanitize schema with img removed + href restricted to http/https"
      exports: ["noteSanitizeSchema"]
    - path: "src/components/notes/useNoteAutoSave.ts"
      provides: "On-blur PATCH /api/notes hook with generation-guard + 2s retry-once on 5xx; NO unmount flush"
      exports: ["useNoteAutoSave", "SaveState", "UseNoteAutoSaveReturn"]
    - path: "src/components/notes/NoteSaveStatus.tsx"
      provides: "Aria-live status component mirroring MandateSaveStatus layout/animation"
      exports: ["NoteSaveStatus"]
    - path: "src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx"
      provides: "Portfolio-scope notes widget (upgrade in place)"
      contains: "scope_kind=portfolio"
  key_links:
    - from: "src/components/notes/NoteRender.tsx"
      to: "src/components/notes/sanitize-schema.ts"
      via: "rehypeSanitize plugin argument"
      pattern: "noteSanitizeSchema"
    - from: "src/components/notes/useNoteAutoSave.ts"
      to: "/api/notes PATCH (new shape)"
      via: "fetch with {scope_kind, scope_ref, content}"
      pattern: "scope_kind,.*scope_ref,.*content"
    - from: "src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx"
      to: "useNoteAutoSave"
      via: "hook invocation with scope_kind='portfolio'"
      pattern: "useNoteAutoSave\\(\"portfolio\""
    - from: "src/app/(dashboard)/allocations/lib/dashboard-defaults.ts"
      to: "DEFAULT_LAYOUT notes-widget entry"
      via: "tile entry with i=notes-1 w=4 h=4"
      pattern: "notes-widget"
---

<objective>
Create the three shared notes primitives — `NoteRender` (react-markdown + rehype-sanitize + remark-gfm), `useNoteAutoSave` (on-blur PATCH with generation-guard + single 5xx retry; NO unmount flush), `NoteSaveStatus` (aria-live status mirroring MandateSaveStatus) — and upgrade the existing portfolio-scope `NotesWidget` in place to consume them. Ship the `.prose-note` CSS rule + `LAYOUT_VERSION` bump + `notes-widget` entry in `DEFAULT_LAYOUT` so new allocators see the Notes card out of the box.

Purpose: Plan 01 shipped the route contract + new deps in `package.json`. This plan ships the client-side primitives every note surface consumes. Plan 04 (per-scope UIs) imports from `src/components/notes/*` directly — this plan is the sole producer of those primitives. Upgrading NotesWidget in place proves the contract works end-to-end against the new route shape without any multi-scope UI complexity.

Output: three shared files + schema module + CSS rule + NotesWidget upgraded; 2s "Note saved" flash reuses the existing `.mandate-saved-flash` keyframe; dashboard default layout includes the widget; LAYOUT_VERSION bumped to 3 per Finding #5.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-connection-management-and-notes/08-CONTEXT.md
@.planning/phases/08-connection-management-and-notes/08-RESEARCH.md
@.planning/phases/08-connection-management-and-notes/08-PATTERNS.md
@.planning/phases/08-connection-management-and-notes/08-UI-SPEC.md
@.planning/phases/08-connection-management-and-notes/08-VALIDATION.md
@.planning/phases/08-connection-management-and-notes/08-01-PLAN.md
@CLAUDE.md
@DESIGN.md

<interfaces>
<!-- Contracts consumed by this plan. -->

Plan 01 shipped:
```typescript
// /api/notes PATCH body (zod-validated):
{
  scope_kind: "portfolio" | "holding" | "bridge_outcome" | "strategy",
  scope_ref: string,
  content: string
}
// Response on 200: { updated_at: string | null }
// 403 on ownership failure; 400 on invalid body / byte cap; 500 on DB error.

// /api/notes GET query: ?scope_kind=...&scope_ref=...
// 200: { content, updated_at }
// 404 if no note exists; 400 on missing/invalid params.
```

Plan 01 installed (package.json exact pins):
```
react-markdown@10.1.0
rehype-sanitize@6.0.0
remark-gfm@4.0.1
```

Existing primitives to clone from:
```typescript
// src/components/mandate/useMandateAutoSave.ts — lines 30-170 (strip field-map + 429 + backoff)
// src/components/mandate/MandateSaveStatus.tsx — lines 28-75 (clone with 1 copy swap)
// src/components/mandate/formatRelativeTime.ts — re-imported by NoteSaveStatus
```

Widget slug + registry facts (verified in codebase):
```
src/app/(dashboard)/allocations/widgets/index.ts:95 — lazy("notes-widget") already wired to NotesWidget
src/app/(dashboard)/allocations/lib/widget-registry.ts:396 — "notes-widget" entry exists (re-use as-is)
src/app/(dashboard)/allocations/lib/dashboard-defaults.ts:15 — export const LAYOUT_VERSION = 2
DEFAULT_LAYOUT currently does NOT include a notes-widget tile entry — Plan 03 adds one + bumps to 3.
```

Existing NotesWidget contract:
```typescript
// src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx
// Receives WidgetProps, reads data.portfolio.id
// Current: fetch(`/api/notes?portfolio_id=${id}`) + 1s debounce save
// Target: fetch(`/api/notes?scope_kind=portfolio&scope_ref=${encodeURIComponent(id)}`) + on-blur save
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 tests (RED) — NoteRender sanitizer + useNoteAutoSave hook + NoteSaveStatus + NotesWidget upgrade</name>
  <read_first>
    - src/components/mandate/useMandateAutoSave.ts (clone source — lines 1-170)
    - src/components/mandate/MandateSaveStatus.tsx (clone source — lines 1-90)
    - src/components/mandate/formatRelativeTime.ts (exported helper — used by NoteSaveStatus)
    - src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx (existing widget to upgrade)
    - src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx (existing test file to extend)
    - .planning/phases/08-connection-management-and-notes/08-RESEARCH.md §Pattern 3 (sanitize schema code)
    - .planning/phases/08-connection-management-and-notes/08-RESEARCH.md §Pattern 4 (useNoteAutoSave contract)
    - .planning/phases/08-connection-management-and-notes/08-UI-SPEC.md §4 / §6 / §7 (state machine + copy)
    - .planning/phases/08-connection-management-and-notes/08-PATTERNS.md §4 / §5 / §6 / §7 (clone-and-modify instructions)
  </read_first>
  <behavior>
    NoteRender.test.tsx — XSS fuzz + GFM passthrough (jsdom + @testing-library/react):
    - Renders `# Heading\n**bold**` and the output DOM contains `<h1>Heading</h1>` + `<strong>bold</strong>`.
    - Renders `<script>alert(1)</script>hi` and the output DOM has NO `<script>` element; the string "alert(1)" does NOT appear in textContent.
    - Renders `<img src=x onerror=alert(1)>` and the output DOM has NO `<img>` element.
    - Renders `<iframe src="https://evil"></iframe>` and the output DOM has NO `<iframe>`.
    - Renders `[link](javascript:alert(1))` and the output `<a>` element has NO `javascript:` href (either the `<a>` is absent or its href is empty / stripped).
    - Renders `[ok](https://example.com)` and the output `<a>` has `href="https://example.com"`, `rel="noopener noreferrer"`, `target="_blank"`.
    - Renders a GFM table (pipe syntax) and the output contains `<table>`, `<thead>`, `<tbody>`, `<th>`, `<td>`.
    - Renders `~~strike~~` and the output contains `<del>strike</del>`.
    - Renders task list `- [x] done` and the output contains a checked checkbox (remark-gfm behaviour).

    useNoteAutoSave.test.ts — 6 assertions (S2: unmount-flush contract explicitly tested):
    - Happy path: `save("hello")` fetches PATCH /api/notes with body `{scope_kind:"portfolio",scope_ref:"abc",content:"hello"}`; on 200 the hook transitions saveState idle → saving → saved and sets lastSavedAt. Flash auto-fades to idle after ~2s (use `vi.useFakeTimers()` + `vi.advanceTimersByTime(2000)`).
    - 4xx no-retry: fetch resolves with status 400; saveState ends at "error"; fetch called exactly once.
    - 5xx retry-once: fetch resolves with 500 on first call, 200 on second; total calls = 2 after a 2s delay; saveState ends at "saved".
    - 5xx retry exhausted: fetch resolves with 500 twice; saveState ends at "error"; fetch called exactly 2 times.
    - Rapid-blur race: call save("a") then immediately call save("b") before the first resolves. Only the second save's response updates state (first response's stale generation is dropped). Assert saveState after both resolutions shows the content from the second save (via lastSavedAt/generation behaviour).
    - **does NOT flush on unmount — verifies fire-and-forget exit contract (S2):** Mount the hook via `renderHook`, call the exposed API to mark dirty content (e.g. set an internal dirty-tracking state if the hook exposes one, OR simply do NOT call `save()`), then call `unmount()` WITHOUT triggering blur. Assert `fetch` was NOT called. This pins the invariant that consumers MUST rely on blur (or explicit save before navigation) to persist; the hook does not flush on unmount. Rationale: unmount-flush would race with StrictMode double-mount in dev and add noise for a rare case; blur covers the dominant path.

    NoteSaveStatus.test.tsx — copy + animation:
    - saveState="saving", lastSavedAt=null → rendered text === "Saving…"? Actually per UI-SPEC §7 the saving state renders "Saving…" (ellipsis char). Assert exact text.
    - saveState="saved", lastSavedAt=new Date() → rendered contains "Note saved" (the 2s flash label).
    - saveState="idle", lastSavedAt=null → rendered text empty (no noise per UI-SPEC §7).
    - saveState="idle", lastSavedAt=now-30s (via `now={now+30000}` test-seam prop) → rendered contains "Last saved:" + a relative-time string.
    - saveState="error", lastSavedAt=null → rendered contains "Save failed — retry".
    - role="status", aria-live="polite", data-testid="note-save-status" attributes present on the wrapper element.

    meta.test.tsx extension — NotesWidget upgrade:
    - Mount with `data={{portfolio:{id:"p1"}}}`. Expect the fetch URL to be `/api/notes?scope_kind=portfolio&scope_ref=p1` (new shape).
    - Initial fetch returns `{content:"**hi**",updated_at:"2026-04-21T00:00:00Z"}`. After mount, NoteRender rendered markdown: `<strong>hi</strong>` present. Textarea NOT rendered yet (read mode).
    - Click "Edit" button → textarea appears with value "**hi**".
    - Type into textarea, trigger `blur`. Expect PATCH /api/notes called exactly once with body `{scope_kind:"portfolio",scope_ref:"p1",content:"<typed>"}`. NOT on keystroke.
    - After successful save, NoteSaveStatus shows "Note saved" (2s) then switches to "Last saved: …".
  </behavior>
  <action>
    Create or extend the following test files (they will be RED — no implementation yet):

    1. `src/components/notes/NoteRender.test.tsx` — 9 assertions from <behavior>. Use `render(<NoteRender content={...} />)` from @testing-library/react; query via `container.querySelector(...)` for tag-level assertions; use `screen.queryByText` for XSS string leakage.
    2. `src/components/notes/useNoteAutoSave.test.ts` — 6 assertions. Use `renderHook` from @testing-library/react + `vi.useFakeTimers()`. Mock `global.fetch` via `vi.spyOn(global, "fetch")`. The 6th test ("does NOT flush on unmount") calls `unmount()` without prior blur and asserts zero fetch calls — this pins the S2 contract.
    3. `src/components/notes/NoteSaveStatus.test.tsx` — 6 assertions (5 copy states + aria attrs).
    4. Extend `src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx` with NotesWidget upgrade assertions from <behavior>. Keep the existing 2 tests; add 5 new cases. Replace the "legacy portfolio_id fetch URL" expectation if the existing tests encode it.

    Run `npx vitest run src/components/notes src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx` — expect failures on missing modules + wrong-URL assertions. Commit as `test(08-03): add notes primitives + NotesWidget upgrade tests (RED)`.
  </action>
  <verify>
    <automated>npx vitest run src/components/notes src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/components/notes/NoteRender.test.tsx
    - File exists: src/components/notes/useNoteAutoSave.test.ts
    - File exists: src/components/notes/NoteSaveStatus.test.tsx
    - `grep -q 'scope_kind=portfolio' src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx` returns 0
    - `grep -q 'Note saved' src/components/notes/NoteSaveStatus.test.tsx` returns 0
    - `grep -q 'javascript:alert' src/components/notes/NoteRender.test.tsx` returns 0 (XSS fuzz string present)
    - `grep -q 'noopener noreferrer' src/components/notes/NoteRender.test.tsx` returns 0 (<a> attr assertion)
    - `grep -q 'Rapid-blur' src/components/notes/useNoteAutoSave.test.ts` returns 0 OR grep 'race' — the race-guard test is present
    - `grep -Eq 'does NOT flush on unmount|not flush on unmount|fire-and-forget exit' src/components/notes/useNoteAutoSave.test.ts` returns 0 (S2 — unmount-flush contract test present)
    - `npx vitest run src/components/notes` returns non-zero (RED — modules do not exist)
    - Git commit prefix `test(08-03):`
  </acceptance_criteria>
  <done>Four test files exist + extended meta.test.tsx; all RED; committed under `test(08-03):`. Includes the S2 "does NOT flush on unmount" test case pinning the fire-and-forget exit contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement NoteRender + sanitize-schema + prose-note CSS (GREEN — markdown render primitive)</name>
  <read_first>
    - .planning/phases/08-connection-management-and-notes/08-RESEARCH.md §Pattern 3 (sanitize-schema.ts + NoteRender.tsx — copy verbatim)
    - .planning/phases/08-connection-management-and-notes/08-UI-SPEC.md §5 (prose-note CSS block — copy verbatim)
    - src/app/globals.css (locate an insertion point below existing mandate-saved-flash keyframe — add .prose-note rules there)
    - node_modules/react-markdown/package.json (confirm 10.1.0 present; if not, re-run Plan 01 Task 3)
    - node_modules/rehype-sanitize/package.json (confirm 6.0.0)
    - node_modules/remark-gfm/package.json (confirm 4.0.1)
  </read_first>
  <action>
    1. Create `src/components/notes/sanitize-schema.ts` verbatim from 08-RESEARCH.md §Pattern 3:

    ```typescript
    import { defaultSchema } from "hast-util-sanitize";
    import type { Schema } from "hast-util-sanitize";

    const ALLOWED_TAGS = (defaultSchema.tagNames ?? []).filter(
      (t) => !["img", "input", "details", "summary", "picture", "source"].includes(t),
    );

    export const noteSanitizeSchema: Schema = {
      ...defaultSchema,
      tagNames: ALLOWED_TAGS,
      protocols: {
        ...(defaultSchema.protocols ?? {}),
        href: ["http", "https"],
      },
    };
    ```

    2. Create `src/components/notes/NoteRender.tsx` verbatim from 08-RESEARCH.md §Pattern 3 (lines 411-449). The component wraps ReactMarkdown with `remarkPlugins={[remarkGfm]}` + `rehypePlugins={[[rehypeSanitize, noteSanitizeSchema]]}` + a `components.a` override that renders `<a href rel="noopener noreferrer" target="_blank">`. The schema is imported at module scope (NOT re-created inline — Pitfall 1).

    3. Append the `.prose-note` CSS block from 08-UI-SPEC.md §5 to `src/app/globals.css`. Rules target `.prose-note p`, `.prose-note h1..h3`, `.prose-note ul/ol/li`, `.prose-note code`, `.prose-note pre`, `.prose-note blockquote`, `.prose-note table/th/td`, `.prose-note del`, `.prose-note hr`. Use the token refs `var(--font-mono)`, `var(--color-border)`, `var(--color-page)`, `var(--color-text-muted)` as in 08-UI-SPEC §5 verbatim. Place the block after the existing `.mandate-saved-flash` keyframe so it's discoverable.

    4. Run `npx vitest run src/components/notes/NoteRender.test.tsx`. All 9 assertions GREEN. If the GFM task-list assertion is flaky in jsdom (missing `<input>` in ALLOWED_TAGS means tasks render as plain list items — they SHOULD; remark-gfm's taskListItem uses `<input type="checkbox" disabled>` which sanitize strips). Adjust the test to assert the checkbox rendering behaviour consistent with the `input`-stripped schema: the task list renders as a `<li>` containing the word "done" without a literal `[x]`. If the test was written to require the actual checkbox, adjust the schema to include `"input"` under `tagNames` OR adjust the test to match the shipped policy (no `<input>`). Planner recommendation: keep `input` stripped (UI-SPEC §5 intent is "no forms in notes") and test the rendered `<li>` text + strikethrough behaviour only.

    Commit as `feat(08-03): NoteRender + sanitize-schema + prose-note CSS`.
  </action>
  <verify>
    <automated>npx vitest run src/components/notes/NoteRender.test.tsx 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/components/notes/NoteRender.tsx
    - File exists: src/components/notes/sanitize-schema.ts
    - `grep -q 'noteSanitizeSchema' src/components/notes/sanitize-schema.ts` returns 0
    - `grep -q 'defaultSchema' src/components/notes/sanitize-schema.ts` returns 0
    - `grep -q '"img"' src/components/notes/sanitize-schema.ts` returns 0 (referenced in filter)
    - `grep -q 'href: \["http", "https"\]' src/components/notes/sanitize-schema.ts` returns 0
    - `grep -q 'noteSanitizeSchema' src/components/notes/NoteRender.tsx` returns 0
    - `grep -q 'remarkGfm' src/components/notes/NoteRender.tsx` returns 0
    - `grep -q 'noopener noreferrer' src/components/notes/NoteRender.tsx` returns 0
    - `grep -q 'target="_blank"' src/components/notes/NoteRender.tsx` returns 0
    - `grep -q '.prose-note' src/app/globals.css` returns 0
    - `npx vitest run src/components/notes/NoteRender.test.tsx` returns 0
    - `npm run typecheck` returns 0
  </acceptance_criteria>
  <done>NoteRender + sanitize-schema landed; prose-note CSS added to globals.css; 9 NoteRender tests green; module-scope schema constant (Pitfall 1 avoided); no rehype-raw anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement useNoteAutoSave + NoteSaveStatus (GREEN — hook + status component, NO unmount flush)</name>
  <read_first>
    - src/components/mandate/useMandateAutoSave.ts (entire file — this is the clone source; strip field map + 429 + backoff chain)
    - src/components/mandate/MandateSaveStatus.tsx (entire file — clone with copy swap only)
    - src/components/mandate/formatRelativeTime.ts (re-exported by NoteSaveStatus — do NOT duplicate)
    - src/app/globals.css (find `.mandate-saved-flash` keyframe — NoteSaveStatus reuses the class name directly)
    - .planning/phases/08-connection-management-and-notes/08-RESEARCH.md §Pattern 4 (useNoteAutoSave source)
    - .planning/phases/08-connection-management-and-notes/08-PATTERNS.md §5 / §6 (what to keep, what to drop)
    - .planning/phases/08-connection-management-and-notes/08-UI-SPEC.md §7 (copy table — 5 states)
  </read_first>
  <action>
    1. Create `src/components/notes/useNoteAutoSave.ts` verbatim from 08-RESEARCH.md §Pattern 4 (lines 461-530). Key differences from `useMandateAutoSave`:
       - Signature: `useNoteAutoSave(scope_kind, scope_ref, initialLastSavedAt?)` — two mandatory scope args + optional initial timestamp.
       - Strip the `fieldErrors` map, `savingFields` set, 429 Retry-After handling, and the 4-attempt exponential chain.
       - Keep the generation counter (`generationRef`) + 2s "saved → idle" fade timer.
       - Retry exactly ONCE on network error OR 5xx after a 2-second wait. 4xx does NOT retry.
       - `fetch("/api/notes", {method:"PATCH", headers, body: JSON.stringify({scope_kind, scope_ref, content}), credentials:"same-origin"})`.
       - Guard against stale responses: after each fetch resolution, check `generationRef.current === gen` before mutating state.
       - Return `{ saveState, lastSavedAt, save }` — no clearError / savingFields / fieldErrors surface.

       **Unmount-flush contract (S2 — pinned):** The hook MUST NOT flush on unmount. There is no `useEffect(() => () => flush(), [])` cleanup that triggers a PATCH. Rationale: blur-triggered save already covers the dominant user path; adding unmount-flush would race with StrictMode double-mount in dev and add noise for a rare case. Consumers (NotesWidget, HoldingNoteRow, BridgeOutcomeNoteSection, StrategyNoteCard) rely on blur or an explicit `save()` call before navigation. If `save()` is in flight when the hook unmounts, the generation guard ensures any state updates after unmount are no-ops — the in-flight fetch is not aborted (fire-and-forget exit), but its response will not mutate unmounted state. Document this invariant in a one-line JSDoc comment above the exported `useNoteAutoSave` function: `// Contract: NO unmount flush. Consumers rely on blur or explicit save() to persist.`

    2. Create `src/components/notes/NoteSaveStatus.tsx` per 08-PATTERNS.md §6. Clone MandateSaveStatus shape verbatim and change:
       - Import `SaveState` from `./useNoteAutoSave`.
       - Import `formatRelativeTime` from `../mandate/formatRelativeTime` — do NOT duplicate.
       - Wrapper `data-testid="note-save-status"`.
       - Copy mapping per UI-SPEC §7:
         - `saving` → "Saving…" (ellipsis character U+2026)
         - `saved` → "Note saved" (2s flash using `.mandate-saved-flash` CSS class, same checkmark SVG + `bg-accent/10 text-accent` circle wrapper)
         - `idle` + `lastSavedAt === null` → render nothing (empty — UI-SPEC §7 explicitly)
         - `idle` + `lastSavedAt !== null` → "Last saved: {formatRelativeTime(lastSavedAt.getTime(), effectiveNow)}"
         - `error` → "Save failed — retry"
       - Test seams: `now?: number` (injected fixed time) + `tickIntervalMs?: number` (default 15000) — same contract as MandateSaveStatus.
       - Keep `role="status" aria-live="polite"`.

    3. Run:
    ```
    npx vitest run src/components/notes/useNoteAutoSave.test.ts src/components/notes/NoteSaveStatus.test.tsx
    ```
    All 12 assertions GREEN (6 hook + 6 status, including the S2 "does NOT flush on unmount" test).

    Commit as `feat(08-03): useNoteAutoSave + NoteSaveStatus (on-blur PATCH + aria-live status; no unmount flush)`.
  </action>
  <verify>
    <automated>npx vitest run src/components/notes/useNoteAutoSave.test.ts src/components/notes/NoteSaveStatus.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - File exists: src/components/notes/useNoteAutoSave.ts
    - File exists: src/components/notes/NoteSaveStatus.tsx
    - `grep -q 'useNoteAutoSave' src/components/notes/useNoteAutoSave.ts` returns 0
    - `grep -q 'generationRef' src/components/notes/useNoteAutoSave.ts` returns 0
    - `grep -q 'scope_kind' src/components/notes/useNoteAutoSave.ts` returns 0
    - `grep -q 'retry' src/components/notes/useNoteAutoSave.ts` returns 0 (retry logic present)
    - `grep -c 'fieldErrors' src/components/notes/useNoteAutoSave.ts` returns 0 (simplification applied)
    - `grep -c '429' src/components/notes/useNoteAutoSave.ts` returns 0 (simplification applied)
    - `grep -Eq 'NO unmount flush|no unmount flush|Contract: NO unmount' src/components/notes/useNoteAutoSave.ts` returns 0 (S2 — invariant documented in JSDoc)
    - `grep -q 'Note saved' src/components/notes/NoteSaveStatus.tsx` returns 0
    - `grep -q 'Save failed — retry' src/components/notes/NoteSaveStatus.tsx` returns 0
    - `grep -q 'Saving…' src/components/notes/NoteSaveStatus.tsx` returns 0
    - `grep -q 'data-testid="note-save-status"' src/components/notes/NoteSaveStatus.tsx` returns 0
    - `grep -q 'formatRelativeTime' src/components/notes/NoteSaveStatus.tsx` returns 0
    - `grep -q 'mandate-saved-flash' src/components/notes/NoteSaveStatus.tsx` returns 0 (CSS class reuse)
    - `npx vitest run src/components/notes/useNoteAutoSave.test.ts src/components/notes/NoteSaveStatus.test.tsx` returns 0 (includes the S2 unmount-flush assertion)
    - `npm run typecheck` returns 0
  </acceptance_criteria>
  <done>useNoteAutoSave + NoteSaveStatus landed per RESEARCH §Pattern 4 + PATTERNS §6; 12 tests green; the hook simplifications (no fieldErrors / no 429 / single retry) are reflected in grep; unmount-flush contract (S2) pinned via JSDoc + test; formatRelativeTime reused from mandate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: NotesWidget upgrade in place + LAYOUT_VERSION bump + default layout entry (GREEN — portfolio-scope surface)</name>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx (entire current file — 137 lines)
    - src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx (extend with NotesWidget upgrade test cases from Task 1)
    - src/app/(dashboard)/allocations/lib/dashboard-defaults.ts (locate LAYOUT_VERSION const + DEFAULT_LAYOUT array)
    - src/app/(dashboard)/allocations/lib/widget-registry.ts (line 396 — existing "notes-widget" entry, do NOT duplicate)
    - src/app/(dashboard)/allocations/widgets/index.ts (line 95 — existing lazy import, do NOT duplicate)
    - src/components/notes/NoteRender.tsx / useNoteAutoSave.ts / NoteSaveStatus.tsx (just shipped — contracts consumed here)
    - .planning/phases/08-connection-management-and-notes/08-UI-SPEC.md §4a (widget layout x:0 y:27 w:4 h:4)
    - .planning/phases/08-connection-management-and-notes/08-UI-SPEC.md §8 (LAYOUT_VERSION bump 2 → 3 rationale)
    - .planning/phases/08-connection-management-and-notes/08-RESEARCH.md §Finding #5 (bump is required)
    - .planning/phases/08-connection-management-and-notes/08-PATTERNS.md §7 (upgrade-in-place diff)
  </read_first>
  <action>
    1. Rewrite `src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` per 08-PATTERNS.md §7 diff:

    - Swap the legacy fetch URL from `/api/notes?portfolio_id=${portfolioId}` to:
      ```typescript
      const res = await fetch(
        `/api/notes?scope_kind=portfolio&scope_ref=${encodeURIComponent(portfolioId)}`,
        { credentials: "same-origin" }
      );
      ```
    - Replace the inline `save` function + `setTimeout(save, 1000)` debounce + hand-rolled saveState with:
      ```typescript
      const { saveState, lastSavedAt, save } = useNoteAutoSave("portfolio", portfolioId ?? "", null);
      ```
      Guard against `portfolioId === undefined`: if not present, render the loading placeholder without mounting the hook's save path. (Pattern: conditional hook invocation is disallowed; fall back to a no-op scope_ref `""` and short-circuit the textarea. OR, move the hook above the early-return and make `save` a no-op when portfolioId is falsy — do the latter by checking inside the textarea's onBlur handler.)
    - Replace the always-on textarea with a read/edit toggle:
      ```tsx
      const [editing, setEditing] = useState(false);
      const [draft, setDraft] = useState(notes);
      useEffect(() => { if (!editing) setDraft(notes); }, [notes, editing]);
      // ...
      {editing ? (
        <textarea
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          onBlur={() => { void save(draft); setEditing(false); setNotes(draft); }}
          className="flex-1 w-full resize-none rounded border border-border p-2 font-mono text-[13px] leading-[1.6] focus:border-accent focus:outline-none"
          placeholder="Portfolio notes — markdown supported."
          autoFocus
        />
      ) : (
        <div className="flex-1 overflow-auto">
          {notes ? (
            <NoteRender content={notes} />
          ) : (
            <p className="text-sm text-text-muted">Portfolio notes — markdown supported.</p>
          )}
          <button
            type="button"
            onClick={() => setEditing(true)}
            className="mt-2 text-xs text-accent underline hover:text-accent-hover"
          >
            Edit
          </button>
        </div>
      )}
      <NoteSaveStatus saveState={saveState} lastSavedAt={lastSavedAt} />
      ```
    - Delete the legacy unmount-flush effect (the current widget's fire-and-forget save on unmount at lines 82-95). **Per S2, useNoteAutoSave does NOT flush on unmount — consumers rely on blur.** The on-blur handler above is the sole persistence trigger for this widget. Do NOT add an unmount-effect wrapper around `save()` in NotesWidget; the blur path is sufficient and matches the S2-pinned contract.
    - Delete the old `stateLabels` / `stateColors` maps + the hand-rolled aria-live `<span>` at lines 127-133 — NoteSaveStatus replaces them.

    2. Edit `src/app/(dashboard)/allocations/lib/dashboard-defaults.ts`:
    - Bump `export const LAYOUT_VERSION = 2;` → `export const LAYOUT_VERSION = 3;`.
    - Add a new tile entry to `DEFAULT_LAYOUT` (position per UI-SPEC §4a):
      ```typescript
      { i: "notes-1", widgetId: "notes-widget", x: 0, y: 27, w: 4, h: 4 }
      ```
      (Match the exact tile shape already in DEFAULT_LAYOUT — read the file first; if the tile shape uses `type` instead of `widgetId` or an alternate field name, match it verbatim.)
    - Add a brief code comment above the new entry: `// Phase 08: portfolio-scope NotesWidget (MANAGE-05). LAYOUT_VERSION bumped 2→3 per UI-SPEC §8.`

    3. Extend `src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx` so Task 1's NotesWidget upgrade assertions go GREEN. Mock `global.fetch` for initial GET + subsequent PATCH. Use `userEvent` to click Edit, type into the textarea, and `userEvent.tab()` to blur.

    4. Run:
    ```
    npx vitest run src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx
    ```
    All NotesWidget upgrade tests GREEN. Then run the broader allocations suite to catch any LAYOUT_VERSION-related regressions:
    ```
    npx vitest run src/app/(dashboard)/allocations
    ```
    All tests GREEN. If `useDashboardConfig.test.ts` or similar fails because of the LAYOUT_VERSION bump (expected — the test asserts config reset behaviour on version mismatch), UPDATE the expectation to reflect the bump (not the test's guard behaviour — the guard is correct; only the expected version number changes).

    Commit as `feat(08-03): NotesWidget upgrade (multi-scope fetch + on-blur + markdown) + LAYOUT_VERSION bump`.
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx src/app/(dashboard)/allocations 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'scope_kind=portfolio' src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` returns 0
    - `grep -q 'useNoteAutoSave' src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` returns 0
    - `grep -q 'NoteRender' src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` returns 0
    - `grep -q 'NoteSaveStatus' src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` returns 0
    - `grep -q 'portfolio_id=' src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` returns 1 (legacy URL absent)
    - `grep -q 'setTimeout' src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` returns 1 (1s debounce removed)
    - `grep -q 'onBlur' src/app/(dashboard)/allocations/widgets/meta/NotesWidget.tsx` returns 0
    - `grep -q 'LAYOUT_VERSION = 3' src/app/(dashboard)/allocations/lib/dashboard-defaults.ts` returns 0
    - `grep -q '"notes-1"' src/app/(dashboard)/allocations/lib/dashboard-defaults.ts` returns 0
    - `grep -q 'notes-widget' src/app/(dashboard)/allocations/lib/dashboard-defaults.ts` returns 0
    - `npx vitest run src/app/(dashboard)/allocations/widgets/meta/meta.test.tsx` returns 0
    - `npx vitest run src/app/(dashboard)/allocations` returns 0
    - `npm run typecheck` returns 0
    - `npm run lint` returns 0
  </acceptance_criteria>
  <done>NotesWidget upgraded in place consuming the three new shared primitives; fetch URL uses scope_kind/scope_ref; save triggers on blur not keystroke (no unmount flush per S2); markdown renders in read mode; LAYOUT_VERSION bumped 2→3 and DEFAULT_LAYOUT includes notes-1 entry; all allocations-scope tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user-supplied markdown → NoteRender | Stored plain text rendered to DOM; sanitizer schema is the XSS backstop |
| NoteRender → external `<a>` clicks | Rewritten with rel=noopener + target=_blank to prevent tabnabbing |
| useNoteAutoSave → /api/notes PATCH | User-scoped cookie session; scope_kind/scope_ref validated by route (Plan 01) |
| localStorage (none new here) | Plan 03 introduces no new localStorage keys (LAYOUT_VERSION bump resets existing persisted layouts but stores no new data) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Tampering / Info Disclosure | Stored XSS via markdown | mitigate | react-markdown escapes raw HTML by default (no rehype-raw); rehype-sanitize with noteSanitizeSchema strips `<img>`, `<input>`, `<details>`, `<summary>`, `<picture>`, `<source>`; href restricted to http/https; `<a>` rewrite forces `rel="noopener noreferrer" target="_blank"`. NoteRender.test.tsx covers `<script>`, `<img onerror>`, `<iframe>`, `javascript:` href, unicode edge cases |
| T-08-12 | Spoofing | Tabnabbing via `target="_blank"` | mitigate | `components.a` override in NoteRender adds `rel="noopener noreferrer"` verbatim for every `<a>` that renders. Test asserts the attribute presence |
| T-08-13 | Tampering | Rapid-blur race drops newer content | mitigate | useNoteAutoSave's `generationRef` bumps on each call, and the stale response is dropped before `setLastSavedAt` runs. Test: rapid `save("a")` then `save("b")` asserts final state reflects "b" |
| T-08-14 | DoS | Markdown bundle bloats all routes | mitigate | RESEARCH §Standard Stack — lazy-import NoteRender via React.lazy; existing `widgets/index.ts:95` already lazy-imports NotesWidget; the markdown bundle loads only when the widget mounts |
| T-08-15 | Tampering | Schema re-creation causes ReactMarkdown remount flicker | mitigate | `noteSanitizeSchema` declared at module scope in sanitize-schema.ts (Pitfall 1); NoteRender imports the constant, never re-creates inline |
| T-08-21 | Integrity (accepted trade-off) | Unflushed draft on sudden unmount | accept | S2 pins NO unmount flush. Rationale: blur already covers the dominant user path; adding unmount-flush would race with StrictMode double-mount in dev and create more noise than it resolves for a rare case. Unit test: `does NOT flush on unmount` asserts zero fetch calls when the hook unmounts without prior blur. Consumers are documented to rely on blur or explicit save() before navigation |
</threat_model>

<verification>
1. **XSS manual probe** — mount NotesWidget against a test account, paste the following content into the textarea, blur, reload:
   ```
   # Hello
   <script>alert("x")</script>
   <img src=x onerror=alert(1)>
   [click](javascript:alert(1))
   [ok](https://example.com)
   ```
   Expected: heading renders; script/img/iframe absent; javascript-href stripped; https link rendered with rel=noopener + target=_blank.

2. **On-blur save cadence** — type several keystrokes into the textarea WITHOUT blurring. Confirm NO PATCH requests fire. Blur → exactly one PATCH fires. Network tab shows body `{scope_kind:"portfolio",scope_ref:"<uuid>",content:"<typed>"}`.

3. **No unmount flush (S2 manual probe)** — type several keystrokes into the textarea WITHOUT blurring, then navigate away (unmount the widget, e.g. by changing dashboard tabs or routing elsewhere). Confirm NO PATCH request fires on unmount. This is the shipped contract; allocators must blur or use an explicit save affordance to persist.

4. **Status cadence** — after a successful save, "Note saved" flashes for 2s then switches to "Last saved: just now" → after 15s ticks to "Last saved: X sec ago" (or equivalent relative time).

5. **LAYOUT_VERSION reset** — in a browser session with a custom layout saved to localStorage (quantalyze-dashboard-config key), reload `/allocations`. Confirm the layout resets to DEFAULT_LAYOUT (incl. the new notes-1 tile). No user-facing banner (Voice-D8 accepted tech debt).

6. **Full test surface:**
   ```
   npx vitest run src/components/notes src/app/(dashboard)/allocations
   npm run typecheck && npm run lint
   ```
   All green. No regressions in upstream Plan 01 suite.
</verification>

<success_criteria>
- `src/components/notes/{NoteRender, sanitize-schema, useNoteAutoSave, NoteSaveStatus}` all exist with the behaviour encoded in Task 1's tests.
- NotesWidget fetches using the new route shape (`scope_kind=portfolio&scope_ref=...`), renders markdown in read mode, saves on blur (no keystroke debounce, no unmount flush), and displays the shared NoteSaveStatus.
- `.prose-note` CSS rule in globals.css provides headings/paragraphs/lists/tables/code/blockquote/del/hr styling using design tokens.
- `LAYOUT_VERSION = 3` in dashboard-defaults.ts; `DEFAULT_LAYOUT` includes a `notes-1` tile at x:0 y:27 w:4 h:4 bound to the existing `notes-widget` registry slug.
- Targeted test surface green: NoteRender (9), useNoteAutoSave (6 — includes S2 unmount-flush assertion), NoteSaveStatus (6), meta (existing + 5 new) — plus the broader allocations suite with no regressions.
- `npm run typecheck` + `npm run lint` green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-connection-management-and-notes/08-03-SUMMARY.md` covering:
- Three shared primitives summary — exports, test count, lazy-import strategy confirmed.
- Sanitize schema decisions — tag removals (img/input/details/summary/picture/source), href protocol allowlist.
- useNoteAutoSave simplifications versus useMandateAutoSave (fieldErrors dropped, 429 dropped, single 5xx retry, generation guard kept, NO unmount flush per S2).
- LAYOUT_VERSION bump side effect on existing users (localStorage reset once).
- Hooks for Plan 04 — the four per-scope surfaces consume these three shared primitives directly; no further shared-component work expected. Plan 04 consumers also rely on blur (no unmount flush).
- Any deviations from UI-SPEC / RESEARCH code samples (expected: none — copy is locked).
</output>
</content>
</invoke>
