---
plan: 09.1-02
status: complete
tasks_completed: 3
tasks_total: 3
phase: 09.1
---

# Plan 09.1-02 — Extend AllocationsTabs from 2 to 6 tabs (D-05/D-06)

## Outcome

`AllocationsTabs.tsx` now exposes the six designer surfaces — Overview / Holdings / Outcomes / Mandate / Risk / Scenario — preserving Plan 01's `+ Allocation` chrome button, the `allocations.ui_v2` flag block, and the V2/legacy conditional render verbatim. The four new tab bodies (`HoldingsTabPanel`, `OutcomesTabPanel`, `MandateTabPanel`, `RiskTabPanel`) ship as thin shells; Plans 08 and 10 fill them. Routing semantics are pinned by a 9-case test suite.

## Tasks

1. **Task 1 — extend AllocationsTabs.tsx 2→6 tabs.** Added Overview as the new default; mapped legacy `?tab=performance` to Overview with URL cleanup; full WAI-ARIA tab pattern (`aria-selected`, `aria-controls`, ArrowLeft/Right/Home/End focus management). Live-refresh polling now only fires on Overview (not on the four new tabs).
2. **Task 2 — add 4 tab-body panel stubs.** Created `HoldingsTabPanel.tsx`, `OutcomesTabPanel.tsx`, `MandateTabPanel.tsx`, `RiskTabPanel.tsx` as thin shells accepting `MyAllocationDashboardPayload` so Plans 08 and 10 can fill them without re-touching the tab shell.
3. **Task 3 — extend AllocationsTabs.test.tsx to 6-tab routing.** 9 test cases pinning: default → Overview, each `?tab={key}` → matching panel active, legacy `?tab=performance` → Overview + URL strip, unknown `?tab=xyz` → Overview silent fallback (no URL cleanup), and ArrowRight wrap across all 6 tabs in D-05 order. Each panel body is mocked to a marker div so the test depends on tab-shell routing only, not on any downstream Plan 04/05/08/10 implementation.

## Key files

- `src/app/(dashboard)/allocations/AllocationsTabs.tsx` (rewritten — 6-tab shell + WAI-ARIA + URL routing)
- `src/app/(dashboard)/allocations/HoldingsTabPanel.tsx` (new)
- `src/app/(dashboard)/allocations/OutcomesTabPanel.tsx` (new)
- `src/app/(dashboard)/allocations/MandateTabPanel.tsx` (new)
- `src/app/(dashboard)/allocations/RiskTabPanel.tsx` (new)
- `src/app/(dashboard)/allocations/AllocationsTabs.test.tsx` (rewritten — 9-case routing suite)

## Verification

- 258/258 allocations tests pass (was 237 after Wave 1; +21 net from this plan + Plan 04)
- `tsc --noEmit` exit 0
- `+ Allocation` button, `uiV2` flag block, and V2/legacy conditional render all preserved
- Phase 07 D-04 silent-fallback contract preserved for unknown `?tab=` values

## Notes

- Tasks 1 and 2 were committed by the original parallel executor agent (commits `f0cbbfb`, `2333ff2`); Task 3 + this SUMMARY were completed inline by the orchestrator after the original agent hit an API overload mid-execution. No work was lost.
- Existing Phase 07 `AllocationsTabs.test.tsx` was rewritten (not extended) because the 2-tab structure no longer reflects reality. The new 9-case suite is a strict superset of the routing assertions it replaced.
