# Phase 09.1: Allocator Dashboard UI refresh — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in `09.1-CONTEXT.md` — this log preserves the alternatives considered.

**Date:** 2026-04-24
**Phase:** 09.1 — allocator-dashboard-ui-refresh-implement-designer-provided-a
**Areas discussed:** Grid architecture, Tabs + widget scope, Row-expand + Bridge UX, Rollout + data adapter

---

## Pre-step: Gray area selection (multi-select)

| Option | Description | Selected |
|--------|-------------|----------|
| Grid architecture | Replace react-grid-layout with designer's 4-col snap-grid, or keep + restyle, or dual-track | ✓ |
| Tabs + widget scope | 5 design tabs vs current 2; 7-widget default vs current 50+; Phase 10 Scenario fit | ✓ |
| Row-expand + Bridge UX | Design's Metrics/Outcome/Notes tabbed sub-row + right drawer vs current Phase 08 inline patterns | ✓ |
| Rollout + data adapter | Big-bang vs flag; how to bridge design's richer holdings columns to current `holdingsSummary` | ✓ |

**User's choice:** All four.
**Notes:** None; all four areas deep-dived.

---

## Area 1 — Grid architecture

### Q1: Grid system

| Option | Description | Selected |
|--------|-------------|----------|
| Adopt 4-col snap-grid (Recommended) | Replace RGL with design's custom CSS grid + ¼-step resize; one-time mapper from RGL to `{k, w}` | ✓ |
| Keep RGL, restyle only | Keep RGL 12-col pixel grid + LAYOUT_VERSION plumbing; re-skin tiles | |
| Dual-track: snap-grid default, RGL advanced | Overview in snap-grid; RGL unlocked via Tweaks/settings | |

**User's choice:** Adopt 4-col snap-grid.
**Notes:** —

### Q2: LAYOUT_VERSION / localStorage transition

| Option | Description | Selected |
|--------|-------------|----------|
| Bump 3→4, reset all custom layouts (Recommended) | Phase 05 / Phase 08 precedent — one-time loss, no migration code | ✓ |
| Bump 3→4 + best-effort migrate | Map RGL tiles → snap-grid `{k, w}` by widget ID; unsupported dropped to picker | |
| Brand-new storage key | `quantalyze-dashboard-v2` key; legacy stays dormant for rollback | |

**User's choice:** Bump 3→4, reset all custom layouts.
**Notes:** —

### Q3: Widget vertical sizing

| Option | Description | Selected |
|--------|-------------|----------|
| Auto-height per widget (Recommended) | Content-driven height; matches designer source; `{k, w}` only | ✓ |
| Fixed row-height with row-span | Introduce `h` field; same-problem as RGL | |
| Three height classes | `short` / `tall` / `extra-tall` declared in catalog; fixed per class | |

**User's choice:** Auto-height per widget.
**Notes:** —

### Q4: Hover chrome placement

| Option | Description | Selected |
|--------|-------------|----------|
| Design's floating hover chrome (Recommended) | SizeStepper + drag + close float top-right, `opacity 0→1` on hover; add `:focus-within` and keyboard alts | ✓ |
| Always-visible compact header | 22px header strip with chrome buttons; safer a11y, loses "clean chart" aesthetic | |
| Dots menu only | Single `⋯` opens popover with resize/remove/drag; fully keyboard-accessible, diverges from design | |

**User's choice:** Design's floating hover chrome.
**Notes:** —

### Check-in: Area 1 done?

| Option | Description | Selected |
|--------|-------------|----------|
| Next area | Grid decisions locked; move to Tabs + widget scope | ✓ |
| More Grid questions | Drop-indicator UX, responsive breakpoints, resize-handle a11y, narrow-widget overflow | |

**User's choice:** Next area.
**Notes:** —

---

## Area 2 — Tabs + widget scope

### Q1: Tab set

| Option | Description | Selected |
|--------|-------------|----------|
| 5 design tabs + Scenario stub (Recommended) | Overview / Holdings / Outcomes / Mandate / Risk + existing Scenario as 6th | ✓ |
| 4 tabs: Overview, Holdings, Outcomes, Scenario | Drop Mandate and Risk placeholders | |
| 3 tabs: Overview, Holdings, Scenario | Collapse Outcomes into Overview widget | |
| Keep current 2 tabs + design-refresh inside Performance | Don't restructure; Performance becomes Overview | |

**User's choice:** 5 design tabs + Scenario stub.
**Notes:** —

### Q2: Legacy widgets

| Option | Description | Selected |
|--------|-------------|----------|
| Preserve all, surface via Widget picker (Recommended) | Extend picker to full WIDGET_REGISTRY; Overview defaults to 7 | ✓ |
| Keep design's 10-item catalog, drop the rest | Clean sweep — remove 40+ widgets from registry | |
| Preserve all, group under "Advanced" in picker | 10 default + collapsible Advanced section with legacy 40+ | |

**User's choice:** Preserve all, surface via Widget picker.
**Notes:** —

### Q3: Scenario tab in 09.1

| Option | Description | Selected |
|--------|-------------|----------|
| Keep existing ScenarioStub.tsx, restyle (Recommended) | Preserve Phase 09 flagged-holdings wiring + Phase 10 contract; restyle only | ✓ |
| Placeholder "Coming in Phase 10" card | Drop ScenarioStub; use design's RiskPlaceholder pattern | |
| Hide Scenario tab until Phase 10 | Feature-flag tab visibility | |

**User's choice:** Keep existing ScenarioStub.tsx, restyle.
**Notes:** —

### Q4: KPI + chart rewrite scope

| Option | Description | Selected |
|--------|-------------|----------|
| Port design's shape, preserve logic (Recommended) | 5-cell KPI + SVG equity chart, route sub-copy through Phase 07 warmup | ✓ |
| Keep existing components, restyle only | KpiStrip + Recharts EquityCurve untouched structurally | |
| Port design verbatim, stub warmup states | Drop Phase 07 warmup/stale logic | |

**User's choice:** Port design's shape, preserve logic.
**Notes:** —

---

## Area 3 — Row-expand + Bridge UX

### Q1: Holdings row click behavior

| Option | Description | Selected |
|--------|-------------|----------|
| Adopt design's 3-tab sub-row (Recommended) | Single row-expand opens Metrics / Record outcome / Notes tabs | ✓ |
| Metrics + Notes tabs only; outcome stays inline via banner | Sub-row has Metrics + Notes; outcome via BridgeOutcomeBanner | |
| Keep Phase 08 row patterns, drop design's Metrics/Outcome tabs | HoldingNoteRow unchanged; Notes-only | |

**User's choice:** Adopt design's 3-tab sub-row.
**Notes:** —

### Q2: Notes tab integration

| Option | Description | Selected |
|--------|-------------|----------|
| Wrap primitives inside new Notes tab (Recommended) | Reuse NoteRender + useNoteAutoSave + NoteSaveStatus + buildHoldingScopeRef | ✓ |
| New standalone note component | Build new NoteInlineEditor; duplicates Phase 08 logic | |
| Defer per-holding notes to future phase | Drop HoldingNoteRow entirely | |

**User's choice:** Wrap primitives inside new Notes tab.
**Notes:** —

### Q3: Bridge banner + drawer

| Option | Description | Selected |
|--------|-------------|----------|
| Widget + drawer, keep inline banner for per-row (Recommended) | Overview widget opens drawer; Holdings rows keep inline banner | ✓ |
| Drawer only, drop inline banner | All bridge interactions via Overview widget → drawer | |
| Inline banner only, skip the drawer | Keep current; widget routes to /bridge page | |

**User's choice:** Widget + drawer, keep inline banner for per-row.
**Notes:** —

### Q4: Bridge banner default variant

| Option | Description | Selected |
|--------|-------------|----------|
| Full (hero) (Recommended) | 14×18px gradient card, title, breach count, Review CTA | ✓ |
| Card | Icon + warning tone card, `Card padding=md` | |
| Subtle | One-line strip with Dismiss/Review | |
| Variant is user-configurable via Tweaks | Ship all 3; default Full; tied to Tweaks fate | |

**User's choice:** Full (hero).
**Notes:** Non-selected variants (`subtle`, `card`) stay in code for dev/admin QA via the Tweaks panel (see Area 4 Q3).

---

## Area 4 — Rollout + data adapter

### Q1: Rollout strategy

| Option | Description | Selected |
|--------|-------------|----------|
| Feature-flag, default OFF for 1 week (Recommended) | localStorage flag, bake, then flip + delete legacy | ✓ |
| Big-bang, single PR | All-new code, legacy deleted; git-revert only rollback | |
| Per-tab incremental | Ship Overview first, then Holdings, etc.; "mixed aesthetic" intermediate states | |
| Feature-flag, default ON from day 1 | Flag is rollback knob only, not gradual rollout | |

**User's choice:** Feature-flag, default OFF for 1 week.
**Notes:** —

### Q2: Data shape adapter

| Option | Description | Selected |
|--------|-------------|----------|
| Thin adapter: join existing sources (Recommended) | holdings-adapter.ts joins holdingsSummary + flaggedHoldings + matchDecisionsByHoldingRef + strategies[] + analytics; rows without strategy match show `—` | ✓ |
| Widen the server payload | Extend MyAllocationDashboardPayload + getMyAllocationDashboard() with per-holding Sharpe/DD/Age/status/manager/tag | |
| Ship design columns with "—" stubs, defer | Columns render em-dash until Phase 11+ wires real data | |

**User's choice:** Thin adapter: join existing sources.
**Notes:** —

### Q3: Tweaks panel production fate

| Option | Description | Selected |
|--------|-------------|----------|
| Drop entirely (Recommended) | Bake design defaults as constants; no floating panel | |
| Ship as user preferences | Persist to localStorage.allocations.prefs | |
| Admin / dev-only (feature-flag) | Gate visibility on `NEXT_PUBLIC_QA_MODE=true` | ✓ |

**User's choice:** Admin / dev-only (feature-flag).
**Notes:** Preserves the design-iteration loop without exposing config surface to real allocators. `NEXT_PUBLIC_QA_MODE` doubles as the ui_v2 override knob during bake.

### Q4: "+ Allocation" button destination

| Option | Description | Selected |
|--------|-------------|----------|
| Scenario tab (pre-Phase 10 stub) (Recommended) | Routes to /allocations?tab=scenario | ✓ |
| /strategies (discover page) | Routes to verified strategy marketplace | |
| Opens Bridge drawer in "browse" mode | Drawer with mandate-fit ranking, no underperformer | |
| Disabled placeholder, Phase 10 wires it | Visible but disabled with tooltip | |

**User's choice:** Scenario tab (pre-Phase 10 stub).
**Notes:** —

---

## Check-in: Explore more or ready for CONTEXT.md?

| Option | Description | Selected |
|--------|-------------|----------|
| Ready for CONTEXT.md (Recommended) | Remaining smaller items become Claude's discretion | ✓ |
| Explore more gray areas | Widget-picker IA, custom range picker + intraday, sidebar badge, test strategy, a11y contract | |

**User's choice:** Ready for CONTEXT.md.
**Notes:** —

---

## Claude's Discretion

The following were left open for the researcher / planner to resolve using the 4 locked primary decisions as guardrails:

- Widget-picker IA for the full 50+ widgets (categories vs search)
- Sidebar "1" badge on My Allocation driven by `flaggedHoldings.length`
- Drop-indicator animation / snap-line styling during drag
- Responsive breakpoints (match design's 980/640; desktop-only remains the commitment)
- Test strategy for the grid rewrite + adapter + feature-flag flows
- Accessibility contract for drag/resize/hover-chrome
- Legacy deletion PR (follow-up to 09.1, out of scope here)

## Deferred Ideas

Captured in `09.1-CONTEXT.md §deferred`:
- Intraday hourly chart (1D/1W timeframes)
- Top movers / Activity feed / Flows ledger widgets
- Mobile responsive polish
- Dark mode
- Server-side payload widening for richer holdings columns
- Legacy dashboard code deletion + flag removal (follow-up PR)
