---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/docs/STATUS_CONTRACT.md
  - analytics-service/services/error_contract.py
  - analytics-service/routers/exchange.py
  - analytics-service/routers/internal.py
  - analytics-service/tests/test_status_contract_exchange_internal.py
  - analytics-service/tests/test_mt5_validate.py
autonomous: true
requirements: [PYAPI-05, PYAPI-06]
must_haves:
  truths:
    - "An exchange-side fault (maintenance, revoked key, IP-allowlist change) answers 424; a bad-credential fault answers 4xx; only a genuine service fault answers 5xx — on every validate-key and permissions arm"
    - "A permanent misconfiguration (KEK, egress proxy, MT5 env) answers 500 retryable:false, never 503 — so it cannot self-sustain a breaker flap"
    - "The four-class contract exists as a committed in-repo artifact 140.2 can read, including the 424=recoverable/non-counting and dependency-keyed obligations"
  artifacts:
    - path: "analytics-service/docs/STATUS_CONTRACT.md"
      provides: "The emit-side contract SEAMCORE-01 consumes (four classes, R-1, R-2, 424 rationale, 140.2 obligations)"
    - path: "analytics-service/services/error_contract.py"
      provides: "R-2 envelope helper {code, dependency, retryable} + per-dependency Retry-After literal table"
  key_links:
    - from: "routers/exchange.py + routers/internal.py 5xx arms"
      to: "services/error_contract.py"
      via: "service_error helper on every remapped site"
      pattern: "error_contract"
---

<objective>
Establish the PYAPI-05 status-attributability contract as a committed artifact + shared helper, and
remap the first 12 of the 24 enumerated 5xx sites: exchange.py S-01..S-07 and internal.py S-08..S-12.

Purpose: today a single user's broken key or a Binance outage emits 5xx and trips the platform-wide
breaker (A-01/C-12). The contract makes the status line alone decidable: CALLER 4xx · CALLER'S
EXCHANGE 424 · SERVICE-TRANSIENT 503+Retry-After+dependency · SERVICE-PERMANENT 500 retryable:false.
Output: STATUS_CONTRACT.md, error_contract.py, 12 remapped sites, literal-oracle tests per site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  (Q2 in full — the S-01…S-24 table is the authoritative site list; Q2.2 the 424 rationale; Q2.3 C-11)
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-PATTERNS.md  (Class 1 table with verified lines; exchange.py error_code analog; audit.py Sentry analog)
</context>

<critical_warnings>
1. **The S-01…S-24 table in RESEARCH is the authoritative 5xx list** — NOT the findings doc's prose
   and NOT CONTEXT.md's earlier (corrected) file mapping. `/process-key` contains ZERO explicit 5xx
   sites; the 502 arms at internal.py:218/:326/:339 belong to the PERMISSIONS route.
2. **Line numbers were verified 2026-07-26** (PATTERNS Class 1). If drifted, re-locate by grepping
   the quoted detail string, never by guessing.
3. **Do not touch the NOT-seam-reachable sites** (exchange.py:453,491,553; csv.py:95;
   portfolio.py:2242,2446; cron.py:613,631; debug_key_flow.py:56,100; analytics_runner.py:1725) —
   enumerated so completeness is provable, excluded so budget isn't spent. Exception: record
   analytics_runner.py:1725 (HTTPException raised in worker context — category error) as a hygiene
   flag in the SUMMARY, no code change.
4. **500 means "do not retry" (R-1).** An unhandled exception is 500 text/plain with no body
   (TRAP-2), so 500 MUST be classifiable with no body at all. Never emit a deliberate transient
   fault as 500; never emit a permanent fault as 503.
5. **Retry-After values are per-dependency LITERALS declared in ONE table** in error_contract.py
   (OPEN-2 / Cluster-D lesson) — never inlined at raise sites, and test oracles pin them as literals
   in the test file, not imports from the table.
6. **Oracles must not be self-referential** (non-negotiable #3): every test asserts the literal
   status int and literal code string, never values imported from error_contract.py.
7. **Existing tests this plan INVALIDATES (TRAP-9 — enumerate, decide rewrite-vs-extend, never
   merely relax):**
   - `analytics-service/tests/test_mt5_validate.py:452-453` — asserts 503 + `detail ==
     NETWORK_ERROR_DETAIL` for the MT5-gateway-unset arm. Broken by Task 2 (S-02/S-03 → 500
     MT5_GATEWAY_UNCONFIGURED). Decision: REWRITE — pin literal 500 + literal
     "MT5_GATEWAY_UNCONFIGURED" + retryable false (keep the existing no-credential-in-logs
     assertions — they are orthogonal and still correct). A relaxed assertion (e.g.
     `status_code >= 500`) silently deletes coverage; the rewritten test must pin the NEW contract
     with literals, not merely stop failing.
   After the remap, run the touched test FILES in full (not only `-k` filters) and apply the same
   rewrite-vs-extend discipline to any additional red — name every rewritten test in the SUMMARY.
</critical_warnings>

<tasks>

<task type="auto">
  <name>Task 1: Contract artifact + R-2 envelope helper</name>
  <files>analytics-service/docs/STATUS_CONTRACT.md, analytics-service/services/error_contract.py</files>
  <read_first>
    - .planning/phases/140.1-*/140.1-RESEARCH.md Q2.0 (four classes + R-1/R-2), Q2.2 (424 rationale
      — reproduce its 4-step reasoning), Q2.3 (C-11 obligations)
    - analytics-service/services/exchange.py:978-1021 (the error_code discriminator precedent — the
      shape R-2 generalizes)
    - analytics-service/routers/simulator.py:445-460 (the {error, correlation_id} body — closest
      existing envelope; the helper must be a superset)
  </read_first>
  <action>
    STATUS_CONTRACT.md: the four-class table (CALLER 4xx / CALLER'S EXCHANGE 424 / SERVICE-TRANSIENT
    503+Retry-After+dependency / SERVICE-PERMANENT 500 retryable:false), rules R-1 and R-2, the Q2.2
    424 reasoning, the dependency vocabulary (mt5-gateway, kek, supabase, egress-proxy), and an
    explicit "Obligations for 140.2" section: 424 is recoverable AND non-counting; breaker keys on
    the named dependency, not globally; retryable:false NEVER counts; unhandled 500 has no body and
    must be safe to classify bodyless. Include the full S-01…S-24 site→target mapping table so 140.2
    can diff against it.
    error_contract.py: a `service_error(status, code, *, dependency=None, retryable, retry_after=None,
    detail=None, correlation_id=None)` helper returning an HTTPException (or JSONResponse where the
    caller needs one) whose body carries {code, dependency, retryable, detail} with detail always a
    scalar string; plus RETRY_AFTER_SECONDS: a single dict of per-dependency integer literals.
    mypy --strict clean; no new packages.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m mypy --strict --follow-imports=silent services/error_contract.py && python3 -c "import services.error_contract" && test -f docs/STATUS_CONTRACT.md</automated>
  </verify>
  <acceptance_criteria>
    - STATUS_CONTRACT.md contains: 4-class table, R-1, R-2, 424 rationale, dependency vocabulary,
      140.2 obligations section, and all 24 S-rows with today→target statuses.
    - error_contract.py exports service_error and RETRY_AFTER_SECONDS; detail is typed str; body key
      set is exactly {code, dependency, retryable, detail} plus correlation_id when supplied.
    - No Retry-After integer appears anywhere outside RETRY_AFTER_SECONDS.
  </acceptance_criteria>
  <done>Contract is a committed artifact; helper importable and strict-clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remap exchange.py S-01..S-07 (7 sites)</name>
  <files>analytics-service/routers/exchange.py, analytics-service/tests/test_status_contract_exchange_internal.py, analytics-service/tests/test_mt5_validate.py</files>
  <read_first>
    - analytics-service/routers/exchange.py:90-450 (all seven sites in context)
    - analytics-service/services/exchange.py:982-1021 (validate_key_permissions' error_code values —
      S-06's split leans on "an escape here is by definition unclassified, but ccxt.* is venue-attributable")
    - analytics-service/tests/test_exchange_router_c0202.py (router-test idiom for this file)
  </read_first>
  <behavior>
    One test per site, status + code pinned as LITERALS. The complete list (count: 7):
    - S-01 exchange.py:108 — malformed WORKER_EGRESS_PROXY_URL: 503 → 500, code EGRESS_PROXY_MISCONFIGURED, retryable false
    - S-02 exchange.py:215 — MT5_GATEWAY_HOST/PORT unset: 503 → 500, code MT5_GATEWAY_UNCONFIGURED
    - S-03 exchange.py:220 — MT5_GATEWAY_PORT not int: 503 → 500, code MT5_GATEWAY_UNCONFIGURED
    - S-04 exchange.py:235 — MT5 gateway connect TIMEOUT: 503 stays, dependency "mt5-gateway", Retry-After present
    - S-05 exchange.py:238 — MT5 gateway connect FAILED: 503 stays, dependency "mt5-gateway", Retry-After present
    - S-06 exchange.py:404 — bare except around validate_key_permissions: SPLIT — ccxt.BaseError → 424 code EXCHANGE_PROBE_FAILED; any other exception → 500 code INTERNAL with copy that does NOT mention credentials
    - S-07 exchange.py:424 — get_kek() raises: 503 → 500, code KEK_UNAVAILABLE, retryable false
  </behavior>
  <action>
    Replace each raise with service_error(...) per the list above. S-06 needs an explicit
    `except ccxt.BaseError` arm ahead of the generic arm; the generic arm's detail must not contain
    the word "credentials" (the current copy blames the user for our bug — C-16). Transient sites
    (S-04/S-05) take Retry-After from RETRY_AFTER_SECONDS["mt5-gateway"].
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_exchange_internal.py -q -k exchange && python3 -m mypy --strict --follow-imports=silent routers/exchange.py</automated>
  </verify>
  <acceptance_criteria>
    - All 7 sites remapped; `grep -n "status_code=503" analytics-service/routers/exchange.py` shows
      503 remaining ONLY at the two transient sites (S-04/S-05 region) — paste output into SUMMARY.
    - 7 tests, each pinning literal status + literal code string (grep the test file: zero imports
      from services.error_contract for expected values).
    - S-06 has two arms; the 500 arm's detail contains no substring "credential".
    - Every deliberate 5xx body carries code, dependency, retryable keys (asserted per test).
  </acceptance_criteria>
  <done>exchange.py emits per the contract; 7/7 sites covered by literal-oracle tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remap internal.py S-08..S-12 (5 sites) + KEK operator capture</name>
  <files>analytics-service/routers/internal.py, analytics-service/tests/test_status_contract_exchange_internal.py</files>
  <read_first>
    - analytics-service/routers/internal.py:200-345 (all five sites)
    - analytics-service/services/audit.py:428-467 (Sentry pattern: set_tag → capture, whole capture
      wrapped in try/except pass, structured log alongside)
  </read_first>
  <behavior>
    The complete list (count: 5):
    - S-08 internal.py:208 — get_kek() raises: 503 → 500, code KEK_UNAVAILABLE, retryable false, PLUS
      a rate-limited sentry capture_message tagged for KEK misconfig (PYAPI-06 site 4; tag value must
      contain no substring of any secret)
    - S-09 internal.py:214 — decrypt_credentials raises: stays 500, gains code KEY_UNDECRYPTABLE,
      retryable false
    - S-10 internal.py:218 — api_keys.exchange NULL/empty: 502 → 422, code KEY_MISSING_EXCHANGE
      (caller's own data — the clearest mis-attribution in the file)
    - S-11 internal.py:326 — create_exchange raised non-ValueError: 502 → 424, code EXCHANGE_INIT_FAILED
    - S-12 internal.py:339 — any exception from detect_permissions: 502 → 424, code EXCHANGE_PROBE_FAILED
  </behavior>
  <action>
    Remap all five via service_error. For S-08 copy audit.py's capture shape (set_tag before
    capture_message, try/except pass around the Sentry call, structured log alongside) and bound the
    capture (module-level once-per-process or time-window guard) so a KEK outage does not flood
    Sentry per-request. Do NOT touch internal.py's hand-rolled per-key_id limiter (:67-91) — it is
    correctly keyed and not part of any class here.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_exchange_internal.py -q && python3 -m mypy --strict --follow-imports=silent routers/internal.py && python3 -m pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - All 5 sites remapped; `grep -cn "status_code=502" analytics-service/routers/internal.py`
      returns 0 (no 502 remains in the file) — paste into SUMMARY.
    - 5 literal-oracle tests (status + code literals in the test file).
    - S-08's Sentry spy test: exactly one capture with the KEK tag under repeated failures; tag/value
      contain no secret substring.
    - Combined with Task 2: 12/12 sites of this plan's slice covered; SUMMARY states the count
      against the S-table (S-01..S-12).
    - Full analytics-service suite green at end of plan (`python3 -m pytest -q`) — the wave-1
      full-suite closure VALIDATION requires, so a wave-1 status remap cannot surface as a red
      three waves later. (Local pandera-missing reds are a known env gap —
      `pip install 'pandera==0.32.1' --break-system-packages` — not a defect.)
  </acceptance_criteria>
  <done>internal.py emits per the contract; exchange-fault arms are breaker-inert 424s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| caller's exchange→our status line | venue faults must not read as our degradation |
| response body→TS breaker | 140.2's discriminator consumes these statuses |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-07 | Denial of Service | breaker tripped by exchange/caller faults | mitigate | 424/4xx remap S-06, S-10..S-12 |
| T-140.1-08 | Denial of Service | self-sustaining flap on permanent misconfig | mitigate | 500 retryable:false on S-01..S-03, S-07..S-09 |
| T-140.1-09 | Information Disclosure | error copy blaming credentials for our bug | mitigate | S-06 generic arm copy constraint |
| T-140.1-SC | Tampering | package installs | accept | no packages installed in this plan |
</threat_model>

<verification>
- 12 sites remapped with per-site literal-oracle tests; mypy --strict clean.
- STATUS_CONTRACT.md committed with the full S-table and 140.2 obligations.
</verification>

<success_criteria>
Phase success criterion 2 for the validate-key/permissions surface: exchange-side and bad-credential
faults answer 4xx/424; only genuine service faults answer 5xx. Contract artifact exists for 140.2.
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-03-SUMMARY.md` when done.
Record: grep evidence for the 503/502 counts, the analytics_runner.py:1725 hygiene flag.
</output>
