---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 04
type: execute
wave: 2
depends_on: [140.1-03]
files_modified:
  - analytics-service/routers/match.py
  - analytics-service/routers/simulator.py
  - analytics-service/routers/portfolio.py
  - analytics-service/main.py
  - analytics-service/tests/test_status_contract_match_sim_portfolio.py
  - analytics-service/tests/test_verify_service_key_middleware.py
  - analytics-service/tests/test_match_router.py
autonomous: true
requirements: [PYAPI-05]
must_haves:
  truths:
    - "No user-reachable detail interpolates a raw exception (the {err} leaks at match.py S-15/S-17 are gone)"
    - "A caller-supplied eval window that is too large answers 400, not 503"
    - "SERVICE_KEY-unset answers 500 retryable:false (bodyless-safe), and /health S-24 is untouched"
  artifacts:
    - path: "analytics-service/routers/match.py"
      provides: "S-13..S-17 remapped, err-interpolation stripped"
    - path: "analytics-service/main.py"
      provides: "S-23 JSONResponse literal 503→500 with code, still returned (never raised)"
  key_links:
    - from: "match/simulator/portfolio 5xx arms"
      to: "services/error_contract.py"
      via: "service_error helper"
      pattern: "error_contract"
---

<objective>
Complete the PYAPI-05 remap: match.py S-13..S-17, simulator.py S-18, portfolio.py S-19/S-20, and
main.py S-23 — the remaining 9 explicit sites of the 24-site class (S-21/S-22 are the implicit
unhandled-500s, which R-1 makes safe by construction; S-24 stays untouched).

Purpose: finish the emit-side contract so every seam-reachable status line is attributable at the
source, and close the two raw-exception-into-detail leaks the findings doc never noticed.
Output: 9 remapped sites + literal-oracle tests; the whole 24-row S-table accounted for.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  (S-13..S-24 rows; Q2.3)
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-PATTERNS.md  (Class 1 rows 13-24 with verified lines; main.py never-raise analog)
@analytics-service/docs/STATUS_CONTRACT.md  (created by 140.1-03 — the contract these sites implement)
</context>

<critical_warnings>
1. **Scope fence: Phase 146 owns rate limits on `routers/match.py`** (/recompute and /eval have NO
   limiter today — record the gap in the SUMMARY, do NOT add slowapi here).
2. **S-23 (`main.py:230`) is a `JSONResponse` literal inside `verify_service_key`
   (BaseHTTPMiddleware), NOT an `HTTPException` raise.** It will not appear in an HTTPException
   grep. The fix edits the literal's status_code and body; it MUST remain a returned JSONResponse —
   the comment at main.py:221-228 documents why raising there produces a 500 + double Sentry
   capture (QUANTALYZE-4). Do not register a handler for it.
3. **S-24 (`main.py:279`, /health stale 503) is CORRECT — leave it alone.** /health is excluded
   from the seam. Do NOT route the health warmer through the seam core under any reading (A-12:
   the breaker would block its own recovery probe).
4. **Stripping `{err}` from detail (S-15/S-17) must not destroy diagnostics** — log the exception
   server-side (structured log with correlation_id) at the same site; only the response body loses it.
5. **S-19/S-20 sit on the zero-caller route** (/api/portfolio-analytics — budget row exists, no TS
   caller). Fix for contract uniformity; do not spend budget on caller-side reasoning.
6. **The in-file comment at match.py:1666-1670** is the reasoning the whole contract copies —
   preserve it; extend, don't delete.
7. **Existing tests this plan INVALIDATES (TRAP-9 — enumerate, decide rewrite-vs-extend, never
   merely relax):**
   - `analytics-service/tests/test_verify_service_key_middleware.py:83-95`
     (`test_unconfigured_service_key_returns_clean_503`) — asserts 503 + exact body
     `{"detail": "Service not configured"}`. Broken by Task 3 (S-23 → 500 + code/retryable).
     Decision: REWRITE, and RENAME (the test name pins the old status): pin literal 500, literal
     "SERVICE_KEY_UNCONFIGURED", retryable false, and that the body is well-formed JSON (the
     returned-not-raised proof stays).
   - `analytics-service/tests/test_match_router.py:2618-2624` — asserts the S-14 503 body's
     `detail` is a scalar string containing "retry"/"temporarily". Broken by Task 1 (S-14
     rebuilt through service_error — the body gains the envelope keys and a `.lower()` on a
     non-string detail errors). Decision: REWRITE the assertions — pin literal 503, literal
     dependency "supabase", Retry-After header present, and `detail` still a scalar string
     (the human copy survives).
   A relaxed or deleted assertion silently deletes coverage; every rewritten test must pin the NEW
   contract with literals, not merely stop failing. Name every rewritten test in the SUMMARY.
</critical_warnings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remap match.py S-13..S-17 (5 sites) and strip the err leaks</name>
  <files>analytics-service/routers/match.py, analytics-service/tests/test_status_contract_match_sim_portfolio.py, analytics-service/tests/test_match_router.py</files>
  <read_first>
    - analytics-service/routers/match.py:1640-1830 (all five sites; the :1666-1670 comment)
    - analytics-service/services/error_contract.py (the helper + RETRY_AFTER_SECONDS)
  </read_first>
  <behavior>
    The complete list (count: 5; lines verified 2026-07-26 — relocate by detail string if drifted):
    - S-13 match.py:1648 ("actor admin check temporarily unavailable"): 503 stays, gains dependency
      "supabase" + Retry-After
    - S-14 match.py:1674 ("profile role check temporarily unavailable"): 503 stays, dependency
      "supabase" + Retry-After
    - S-15 match.py:1765 (f"Scoring failed: {err}"): 500, code SCORING_FAILED, retryable false, and
      the response detail contains NO exception text — a canary test raises an exception whose
      message contains "CANARY_ERR" and asserts zero occurrences in the serialized response
    - S-16 match.py:1818 (PaginatedSelectTruncated): 503 → 400, code EVAL_WINDOW_TOO_LARGE — the
      truthful statement is "narrow your window", not "we are down"
    - S-17 match.py:1826 (f"Eval failed: {err}"): 500, code EVAL_FAILED, same canary discipline as S-15
  </behavior>
  <action>
    Remap via service_error. At S-15/S-17, move the interpolated exception into a structured
    log.error with correlation_id at the same site so operators keep the diagnostic. Add no limiter
    anywhere in this file (Phase 146's scope).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_match_sim_portfolio.py -q -k match && python3 -m mypy --strict --follow-imports=silent routers/match.py</automated>
  </verify>
  <acceptance_criteria>
    - 5/5 sites remapped; `grep -nE 'f"(Scoring|Eval) failed: \{' analytics-service/routers/match.py`
      returns 0 lines (paste into SUMMARY).
    - Canary tests for S-15 and S-17 pass (zero CANARY_ERR occurrences in body) and turn RED if the
      interpolation is restored.
    - S-16 test pins literal 400 + literal "EVAL_WINDOW_TOO_LARGE".
    - S-13/S-14 tests assert Retry-After header present and dependency == "supabase" (literals).
    - No slowapi import/decorator added to match.py (grep confirms; Phase 146 note in SUMMARY).
    - `test_match_router.py:2618-2624` rewritten per warning 7 — pins literal 503 + dependency
      "supabase" + Retry-After present + scalar-string detail; the whole file runs green.
  </acceptance_criteria>
  <done>match.py emits per the contract; both err-leaks closed with canary oracles.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remap simulator.py S-18 and portfolio.py S-19/S-20 (3 sites)</name>
  <files>analytics-service/routers/simulator.py, analytics-service/routers/portfolio.py, analytics-service/tests/test_status_contract_match_sim_portfolio.py</files>
  <read_first>
    - analytics-service/routers/simulator.py:440-460 (S-18 — already the closest body to R-2; ALIGN
      it to the helper, do not rewrite the route)
    - analytics-service/routers/portfolio.py:640-660, :1150-1170 (S-19, S-20)
  </read_first>
  <behavior>
    The complete list (count: 3):
    - S-18 simulator.py:453: stays 500, gains code SIMULATION_FAILED, retryable false; keeps its
      correlation_id
    - S-19 portfolio.py:653 ("Failed to create analytics row" — insert returned no row): 500 → 503,
      dependency "supabase", Retry-After present (transient Supabase blip)
    - S-20 portfolio.py:1163 ("Portfolio analytics computation failed"): stays 500, gains code
      PORTFOLIO_ANALYTICS_FAILED, retryable false
  </behavior>
  <action>
    Remap the three sites via service_error. Touch ONLY the raise sites in portfolio.py — the
    private Limiter at portfolio.py:146 and the L-5..L-8 decorators belong to Plan 140.1-07 (wave 4);
    modifying them here creates a same-file collision with that plan's diff. Same for exchange.py —
    not in this plan at all.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_match_sim_portfolio.py -q && python3 -m mypy --strict --follow-imports=silent routers/simulator.py routers/portfolio.py</automated>
  </verify>
  <acceptance_criteria>
    - 3/3 sites remapped with literal-oracle tests (status + code literals).
    - portfolio.py diff contains NO change to line-146 region or any @limiter/@key_func decorator
      (executor confirms via git diff and states it in SUMMARY).
    - S-19 test asserts 503 + dependency "supabase" + Retry-After present.
  </acceptance_criteria>
  <done>All three sites contract-conformant; limiter surface untouched for Plan 07.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: S-23 — the middleware 503 literal becomes a classified 500</name>
  <files>analytics-service/main.py, analytics-service/tests/test_status_contract_match_sim_portfolio.py, analytics-service/tests/test_verify_service_key_middleware.py</files>
  <read_first>
    - analytics-service/main.py:198-240 (verify_service_key: skip-list, the :221-228 never-raise
      comment, the :230 JSONResponse literal, the :232-234 compare_digest arm)
    - .planning/phases/140.1-*/140.1-RESEARCH.md Q2.3 (why 503-here flaps the breaker forever)
  </read_first>
  <behavior>
    - SERVICE_KEY unset ⇒ status 500 (literal), body {code:"SERVICE_KEY_UNCONFIGURED",
      retryable:false, ...}, still a RETURNED JSONResponse.
    - Stale-caller-key arm (:232-234) stays 401 — correct, do not change its status.
    - /health (S-24) behaviour byte-identical (test: stale-heartbeat still answers 503 {status:"stale"}).
  </behavior>
  <action>
    Edit the JSONResponse literal at main.py:230 only: status 503→500, body gains code and
    retryable:false (align the body keys with error_contract's envelope, but construct the
    JSONResponse inline — importing FastAPI HTTPException machinery into middleware is the trap the
    :221-228 comment forbids). Do not add Sentry captures or startup assertions here — that is Plan
    140.1-08's PYAPI-06 work on this same function; keep this diff minimal to avoid churn.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_match_sim_portfolio.py -q -k "service_key or health" && python3 -m mypy --strict --follow-imports=silent main.py</automated>
  </verify>
  <acceptance_criteria>
    - Test with SERVICE_KEY unset: any non-skipped route answers literal 500 with literal code
      "SERVICE_KEY_UNCONFIGURED"; a raise-based implementation would double-capture — assert the
      response is well-formed JSON (proves the return path).
    - /health test: unchanged 503 stale shape (S-24 pinned so this plan provably did not touch it).
    - `grep -n "Service not configured" analytics-service/main.py` shows the literal now inside a
      status_code=500 JSONResponse (paste into SUMMARY).
    - `test_verify_service_key_middleware.py`'s unconfigured-key test rewritten (and renamed) per
      warning 7 — pins literal 500 + "SERVICE_KEY_UNCONFIGURED" + retryable false; the whole
      file runs green.
    - SUMMARY tallies the full class: 24 S-rows — **21 explicit sites edited** across plans 03+04
      (S-01..S-20 `HTTPException` raises + S-23, the `JSONResponse` literal). S-23 does NOT appear
      in a `grep "status_code=5"` HTTPException sweep — which is exactly how a "20 edited"
      off-by-one arises; count it explicitly. Plus S-21/S-22 implicit (no edit possible or needed,
      R-1 makes them safe) and S-24 deliberately unchanged: 21 + 2 + 1 = 24.
  </acceptance_criteria>
  <done>S-23 classified; the permanent-503 breaker flap shape is gone; class tally reconciled.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| exception text→response body | raw err interpolation leaks internals to callers |
| middleware→ServerErrorMiddleware | raising in BaseHTTPMiddleware double-captures |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-10 | Information Disclosure | match.py S-15/S-17 detail | mitigate | strip {err}, canary oracle, server-side structured log |
| T-140.1-11 | Denial of Service | S-23 permanent 503 flap | mitigate | 500 retryable:false, bodyless-safe per R-1 |
| T-140.1-12 | Denial of Service | S-16 mislabels caller fault as outage | mitigate | 400 EVAL_WINDOW_TOO_LARGE |
| T-140.1-SC | Tampering | package installs | accept | no packages installed in this plan |
</threat_model>

<verification>
- 9 sites remapped, per-site literal oracles green, mypy --strict clean.
- Full 24-row tally reconciled in the SUMMARY.
</verification>

<success_criteria>
Phase success criterion 2 completed across the whole seam surface; success criterion 6's
"no response body echoes" partially advanced (the S-15/S-17 leak class closed at these sites).
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-04-SUMMARY.md` when done.
Record: the 24-row reconciliation, the match.py rate-limit gap note for Phase 146.
</output>
