# Phase 140.1 — RESEARCH A: limiter identity (Q1) + status attributability contract (Q2)

**Researched:** 2026-07-26
**Scope:** the two BLOCKING design questions. Not a re-derivation of Cluster C.
**Confidence:** HIGH on everything read from source in this session; every claim below carries a
`file:line` or a version-pinned citation.

## Versions this research was verified against

| Package | requirements.txt pin (DEPLOYED) | Locally installed (where source was read) | Divergence risk |
|---|---|---|---|
| fastapi | `0.139.0` (`requirements.txt:64`) | 0.135.1 | none for the behaviours cited |
| starlette | `0.46.2` (`requirements.txt:230`) | 0.52.1 | ⚠️ see CONTRADICTION-1 |
| slowapi | `0.1.10` (`requirements.txt:226`) | 0.1.9 (`/opt/homebrew/lib/python3.14/site-packages/slowapi`) | ⚠️ see ASSUMPTION-1 |
| limits | `5.8.0` (`requirements.txt:104`) | 5.8.0 | none |
| uvicorn | `uvicorn[standard]==0.51.0` (`requirements.txt:278`) | 0.42.0 | none — `proxy_headers` default identical |
| PyJWT | `pyjwt[crypto]==2.13.0` (`requirements.txt:183`) | 2.12.1 | none — **already a declared dep** |

Local python import failures in `csv_validator` / `middleware` / `mt5` are the known missing-`pandera`
environmental gap, not a defect. Not chased.

---

## Q1 — Limiter identity for `/process-key` (PYAPI-02) and the IP-keyed class (PYAPI-03)

### Q1.0 Ground truth established this session

| # | Fact | Evidence |
|---|---|---|
| G-1 | The `/process-key` bucket key is `process_key:<sha256(bearer)[:16]>` — token only. | `routers/process_key.py:99-105` |
| G-2 | Bucket identity = **(key_func result, request URL path)**. `_key_style` defaults to `"url"` (empirically confirmed: a freshly constructed `Limiter` reports `_key_style == 'url'`), so the scope is `request["path"]`, **not** the function name. *Which* limits apply is still looked up by function name. ⇒ `/process-key` owns exactly **one** 100/hour bucket per distinct bearer, and two different paths never share a counter. | `slowapi/extension.py:565`, `:488` (`limit_scope = lim.scope or endpoint`), `:578-582`; empirical probe of `Limiter(key_func=get_remote_address)` |
| G-3 | slowapi storage is **`memory://`** — in-process, per-replica, wiped on every deploy. Empirically confirmed on a freshly constructed `Limiter`: `MemoryStorage`, `_storage_uri is None`, `_headers_enabled == False` (⇒ **no `Retry-After` on any slowapi 429** — confirms C-15), `_swallow_errors == False` (⇒ G-8). | `slowapi/extension.py:238-244`; empirical probe |
| G-4 | The limit check runs **inside the endpoint wrapper**, i.e. AFTER FastAPI body validation and BEFORE the first line of the handler. | `slowapi/extension.py:717-731` (`async_wrapper` calls `_check_request_limit(...)` then `await func(...)`) |
| G-5 | `_verify_internal_token(request)` is the **first statement of the handler body** — therefore third gate. | `routers/process_key.py:536` |
| G-6 | ⇒ Effective order on `/process-key` is **pydantic 422 → slowapi 429 → auth 403**. This confirms C-18's ordering claim from source. | G-4 + G-5 |
| G-7 | `key_func` receives only the `Request`. It cannot read the parsed body (the body is un-awaited at that point). Any forgery-resistant identity must therefore live in a **header**. | `slowapi/extension.py:496-498` |
| G-8 | If `key_func` **raises**, the exception escapes `_check_request_limit` (`extension.py:632-648`: `RateLimitExceeded` re-raised; anything else re-raised unless `swallow_errors`) → unhandled → 500 `text/plain`. **A key function must never raise.** | `slowapi/extension.py:630-648` |
| G-9 | Stacking two `@limiter.limit(...)` decorators on one endpoint evaluates **both** limits in one pass: `functools.wraps` keeps `__module__.__name__` identical, both limits land in the same `_route_limits[name]` list, the outermost wrapper evaluates the whole list and then sets `request.state._rate_limiting_complete = True` so the inner wrapper skips. | `slowapi/extension.py:699-702` + `:726-731` + `:486-528` |
| G-10 | `get_remote_address` is **`request.client.host`** — the immediate TCP peer. It does NOT read `X-Forwarded-For`. (`get_ipaddr` does; it is not used anywhere in this repo.) | `slowapi/util.py:19-26`; repo grep: zero `get_ipaddr` hits |
| G-11 | uvicorn is launched as `uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}` with **no `--proxy-headers` / `--forwarded-allow-ips`**. uvicorn defaults `proxy_headers=True` but `forwarded_allow_ips="127.0.0.1"`, so `ProxyHeadersMiddleware` refuses to rewrite `client.host` for a non-loopback peer (Railway's edge). ⇒ `get_remote_address` collapses to the edge-proxy IP. | `analytics-service/Dockerfile:39`; `uvicorn/config.py` `Config.__init__` defaults |
| G-12 | `X-User-Id` reaches Python on every `/process-key` call but is used **only** for a WARN log. Confirms C-23. | `src/lib/process-key-client.ts:159`; `routers/process_key.py:565-574` |
| G-13 | `X-User-Access-Token` is forwarded by **exactly one** caller: `csv-finalize`. Teaser / onboard / resync / csv-validate send nothing. | `src/lib/process-key-client.ts:163-165`; only `src/app/api/strategies/csv-finalize/route.ts:1197` passes `userAccessToken` |
| G-14 | There is **no** `SUPABASE_JWT_SECRET`, no JWKS URL, and no JWT-verification code anywhere in `analytics-service/`. The service's env surface is `SUPABASE_URL` / `SUPABASE_ANON_KEY` / `SUPABASE_SERVICE_KEY` only. `services/db.py:105-109` merely *attaches* a token to PostgREST — it verifies nothing locally. | repo-wide grep for `JWT_SECRET|jwks` → **0 hits** |
| G-15 | `INTERNAL_API_TOKEN` is a **shared secret already present on both sides**: Vercel sends it (`process-key-client.ts:124,155`), Python reads it (`routers/process_key.py:266`, `routers/internal.py:110`). No new secret is required to mint/verify an HMAC. | as cited |
| G-16 | The public teaser is capped Vercel-side at `publicIpLimiter` = **10 req / 60 s per IP** (`src/lib/ratelimit.ts:115`, `makeLimiter` → `Ratelimit.slidingWindow`). 10/min = 600/hour ⇒ **one anonymous IP can drain the platform's 100/hour `/process-key` bucket in ≈10 minutes.** C-09 is not just true, it is cheap. | `src/lib/ratelimit.ts:88,115`; `src/app/api/verify-strategy/route.ts:7,31-34` |

### Q1.1 The four candidate identities

| | Mechanism | Trust boundary it assumes | What breaks if the assumption is wrong | Cost |
|---|---|---|---|---|
| **(a)** | **HMAC'd tenant claim minted Vercel-side.** Vercel sends `X-Tenant-Claim: <user_id>.<exp>.<hmac-sha256(INTERNAL_API_TOKEN, "<user_id>.<exp>")>`; Python's `key_func` recomputes with `hmac.compare_digest` and buckets on `user_id` iff valid. | *Only* that `INTERNAL_API_TOKEN` stays confidential — **the exact assumption the endpoint's auth already rests on** (G-15). Adds **no new trust surface and no new secret**. | If the token leaks the attacker already holds full `/process-key` auth, so the limiter is not the marginal loss. No *new* failure mode. | ~25 lines Python (stdlib `hmac`/`hashlib`, no dep), ~10 lines TS. `mypy --strict` clean (all `str`). |
| **(b)** | **Verify the forwarded Supabase JWT offline**, bucket on `sub`. | That every flow forwards a JWT **and** that Python can verify it with no network hop. | **Both false today.** G-13: only `csv-finalize` forwards one — 4 of 5 flows send nothing, and the **teaser is anonymous so no JWT exists at all**. G-14: no `SUPABASE_JWT_SECRET`, no JWKS config, zero verification code in the service. Making it work needs (i) a TS change on 4 routes (outside this phase's boundary), (ii) a **new production secret** or a JWKS fetch — i.e. network I/O inside `key_func`, where a raise is a 500 (G-8). | HIGH. `pyjwt[crypto]==2.13.0` already being a declared dep (`requirements.txt:183`) is the only point in its favour. **Rejected.** |
| **(c)** | **Split the anonymous teaser onto its own bucket.** | That `flow_type` is trustworthy at key-func time. | `flow_type` lives in the **body** and G-7 says `key_func` cannot see it. The only header proxy is `X-User-Id: "public"` (`verify-strategy/route.ts:192`) — client-settable, so an attacker sets `X-User-Id: <victim-uuid>` and lands in the paying tenants' bucket. Closes C-09's anonymous vector, leaves cross-tenant starvation open. | LOW cost, PARTIAL fix. Valuable only **on top of** (a). |
| **(d)** | **Layered: per-tenant bucket AND a global ceiling** (two stacked `@limiter.limit` decorators). | Inner: same as (a). Outer: nothing. | The ceiling is the backstop for the case where the claim is absent — a future caller that forgets the header falls into a shared `unverified` bucket and can still not exceed the platform cap. | +1 decorator line. G-9 proves stacking evaluates both limits in one pass with no double-count. |

### Q1.2 RECOMMENDATION — (a) + (c) + (d) as ONE mechanism

**Bucket on an HMAC-verified tenant claim, with a dedicated anonymous bucket and a stacked global ceiling.**

The replacement for `routers/process_key.py:75-105` returns, in order:

| Condition | Bucket | Rationale |
|---|---|---|
| `X-Tenant-Claim` present, HMAC valid (`hmac.compare_digest`), unexpired, payload != `public` | `process_key:t:<user_id>` | forgery-resistant per-tenant window |
| HMAC valid, payload == `public` | `process_key:anon` | the whole public teaser shares ONE allowance — this is what closes C-09 |
| header absent / malformed / bad HMAC / expired | `process_key:unverified:<sha256(bearer)[:16]>` | **never raise** (G-8); degrade to today's behaviour and WARN, so a caller that stops sending the header is visible before it starves anyone |

Plus a second stacked decorator carrying a platform ceiling keyed on the bearer hash.

**Why (a) not (b):** (a) reuses the secret the endpoint *already* authenticates with (G-15) — zero new
trust assumptions, zero new production secrets. (b) needs a new secret AND a TS change on four routes
AND still cannot serve the teaser, which has no JWT by construction (G-13).

**Why a header, not a body field:** G-7. Not a style choice.

**Why `key_func` must never raise:** G-8 — a raising key function converts a throttling concern into an
unhandled 500 `text/plain`, which then feeds the TS breaker. That is the exact A-02/C-12 class this
programme exists to close.

**Ordering interaction with PYAPI-04:** G-6 shows auth is gate 3 today. Once PYAPI-04 moves
`_verify_internal_token` ahead of the limiter, the `unverified` bucket becomes reachable only by holders
of `INTERNAL_API_TOKEN` — i.e. "a trusted caller forgot the header", exactly the WARN case.
**Plan PYAPI-02 and PYAPI-04 in the same wave.** Shipping PYAPI-02 alone leaves an unauthenticated
caller able to allocate `unverified` buckets.

**Rollout order (no flag day):** ship the Python key function FIRST (it tolerates a missing header by
falling back), then the TS minting. This keeps the TS half genuinely mechanical and additive, satisfying
the CONTEXT boundary rule rather than straining it.

### Q1.3 What a test proving it looks like

`analytics-service/tests/` (pytest — the form that runs in CI):

| Test | Asserts | Fails without the fix? |
|---|---|---|
| `test_key_func_two_tenants_get_distinct_buckets` | valid claims for A and B produce different strings | yes |
| `test_key_func_forged_claim_falls_to_unverified` | same `user_id`, one HMAC byte flipped -> `unverified:` bucket, **not** `t:<user_id>` | yes — the C-23 regression |
| `test_key_func_ignores_unsigned_x_user_id` | `X-User-Id: <victim>` with no claim header never yields `t:<victim>` | yes — pins the PR#241 lesson permanently |
| `test_key_func_never_raises` | parametrised over `None`, `""`, `"a.b"`, non-UTF8 bytes, 64 KB header, non-numeric `exp` -> always returns `str` | yes (G-8) |
| `test_teaser_claim_buckets_to_anon` | payload `public` -> `process_key:anon`, distinct from every tenant bucket | yes — the C-09 gate |
| `test_two_stacked_limits_both_evaluated` | live `TestClient`: exhaust A's per-tenant limit -> A 429s while B still passes; then exhaust the ceiling -> B 429s too | yes (G-9) |
| `test_process_key_limit_key_func_is_not_get_remote_address` | identity assertion, mirroring `tests/test_simulator_router.py:992-1004` | yes |

**Oracle discipline (Cluster D lesson):** expected bucket strings must be **literals** in the test, never
re-derived from the production helper. `tests/test_process_key.py:2542` is the existing shape — but it
currently pins *token-only* keying, so it must be **rewritten, not extended**. Flag that explicitly to the
planner so the change is deliberate rather than a silently deleted assertion (TRAP-9).

### Q1.4 PYAPI-03 — the IP-keyed limiter class, FULLY ENUMERATED

⚠️ **The finding's framing ("three routers re-declare private `Limiter(key_func=get_remote_address)`")
under-scopes the class.** The defect is *an IP-keyed bucket behind a proxy* (G-10 + G-11), and one route
has that defect while correctly importing the **shared** limiter. Enumerating by "private `Limiter()`"
finds 3 modules / 8 routes and **misses `optimizer.py`** — precisely the instances-vs-classes failure
mode TRAP-5 exists to prevent.

**Private `Limiter()` declarations (3):** `routers/exchange.py:40`, `routers/csv.py:24`,
`routers/portfolio.py:146`. Each gets its **own isolated `memory://` storage** (G-3), so they do not
even share counters with `app.state.limiter`.

**Every IP-keyed decorated route (9) — this is the class:**

| # | Route | Decorator | Limit | Limiter instance | Seam-reachable? | Effective scope today |
|---|---|---|---|---|---|---|
| L-1 | `POST /api/validate-key` | `exchange.py:336` | 100/hour | private `exchange.py:40` | ✅ `validate-key` budget | PLATFORM-WIDE |
| L-2 | `POST /api/encrypt-key` | `exchange.py:418` | 100/hour | private `exchange.py:40` | ✅ `encrypt-key` budget | PLATFORM-WIDE |
| L-3 | `POST /api/fetch-trades` | `exchange.py:447` | 10/hour | private `exchange.py:40` | ❌ no TS caller (only a string in `debug-key-flow/route.ts:160`) | PLATFORM-WIDE |
| L-4 | `POST /api/csv/validate` | `csv.py:57` | 30/hour | private `csv.py:24` | ❌ TS `csv-validate` goes via `/process-key` (`csv-validate/route.ts:206`); the Python route is referenced only in a comment | PLATFORM-WIDE |
| L-5 | `POST /api/portfolio-analytics` | `portfolio.py:1532` | 10/hour | private `portfolio.py:146` | ⚠️ budget row exists, **zero callers** (`resilient-fetch.ts:203`) | PLATFORM-WIDE |
| L-6 | `POST /api/portfolio-optimizer` | `portfolio.py:1592` | 10/hour | private `portfolio.py:146` | ✅ `portfolio-optimizer` budget | PLATFORM-WIDE |
| L-7 | `POST /api/portfolio-bridge` | `portfolio.py:1852` | 10/hour | private `portfolio.py:146` | ✅ `bridge` budget | PLATFORM-WIDE |
| L-8 | `POST /api/verify-strategy` (Python) | `portfolio.py:2147` | 5/hour | private `portfolio.py:146` | ❌ the TS `/api/verify-strategy` teaser calls `/process-key`, not this | PLATFORM-WIDE |
| L-9 | `POST /api/optimize-weights` | `optimizer.py:31` | 20/minute | **SHARED** `services/rate_limit.py:29` — inherits its default `key_func=get_remote_address` | ✅ `optimize-weights` budget | PLATFORM-WIDE ⭐ **missed by the "private Limiter" framing** |

**Correctly keyed today (do not touch):** `routers/process_key.py:518` (token-hash key — the subject of
Q1.2), `routers/simulator.py:232` (`_simulator_rate_limit_key`, user-keyed, `simulator.py:57-90`).

**No limiter at all (out of scope — RATE-03 / Phase 146 per CONTEXT `<deferred>`):**
`routers/match.py:1619` `POST /api/match/recompute` and `routers/match.py:1793` `GET /api/match/eval` —
both **are** seam-reachable (`analytics-client.ts:399,418`). Record the gap, do not fix it here.

**Not slowapi at all:** `routers/internal.py:67-91` `_consume_rate_limit` is a hand-rolled in-memory
bucket keyed on **`key_id`** — the correct dimension, and bounded at 200 keys. It is *not* an instance of
this defect class. It is per-process and per-replica though (same caveat as G-3).

#### Token cost per user-visible flow

| Flow | TS entry | Python buckets spent | Cost |
|---|---|---|---|
| Key connect (wizard) | `keys/validate-and-encrypt/route.ts:243,256` | L-1 **and** L-2 | **2 tokens, from two SEPARATE buckets** — G-2: bucket = (key, endpoint name), so validate-key and encrypt-key do *not* share a counter. The finding's "spends two tokens" is right; the implication that they draw on one 100/hour pool is not. |
| Scenario optimize | `analytics-client.ts:280` | L-9 | 1 token from a **20/minute platform-wide** pool — the tightest ceiling on the seam |
| Bridge | `analytics-client.ts:338` | L-7 | 1 of 10/hour platform-wide |
| Portfolio optimizer | `analytics-client.ts:320` | L-6 | 1 of 10/hour platform-wide |
| Simulator | `analytics-client.ts:363` | none IP-keyed (correctly user-keyed) | — |
| `/process-key` (teaser, onboard, resync, csv) | `process-key-client.ts:151` | the single 100/hour token bucket | 1 — see Q1.2 |
| Key-permission badge | `keys/[id]/permissions/route.ts:131` | `internal.py` per-`key_id` bucket, 10/min | 1 per key |

**The operationally worst number on this table is L-9: 20/minute for the whole platform.** Two users
running Scenario Composer concurrently can 429 each other, and G-9/C-15 means that 429 renders as
`UNKNOWN` ("Analytics service error") in the wizard.

**Prescription for the class:** replace `key_func=get_remote_address` at all nine sites with the same
verified-tenant key function from Q1.2 (or, where no tenant claim is available, an explicit
`platform:<route>` key that is *documented* as a ceiling rather than accidentally global), and delete the
three private `Limiter()` instances in favour of `services/rate_limit.py:29`. **Do not add a new
library** — CONTEXT locks this, and nothing here needs one.

---

## Q2 — The status attributability contract (PYAPI-05)

### Q2.0 The contract (this is what 140.2 builds its discriminator on)

**The status code answers exactly one question: *should this response count against the analytics
service's own health?*** Everything else — user copy, remedy, retry affordance — is carried in the body
and is 140.3's concern.

Four classes. Every 5xx-capable site must be assignable to exactly one, **at the site**, without
downstream inference:

| Class | Decision question | Status | `Retry-After` | 140.2 obligation |
|---|---|---|---|---|
| **CALLER** | Is the caller's request, credentials, or authorization at fault? | `400` / `401` / `403` / `404` / `422` / `429` | on 429 only | never counts |
| **CALLER'S EXCHANGE** | Is the third party *the caller named* at fault (venue down, throttling us, key revoked at the venue, IP-allowlist change)? | **`424 Failed Dependency`** | optional | **never counts** |
| **SERVICE-TRANSIENT** | Is one of *our* dependencies temporarily unavailable such that an identical retry could succeed? | `503` | **required** | counts — but keyed on the named `dependency`, not globally |
| **SERVICE-PERMANENT** | Is this a misconfiguration or a bug that an identical retry cannot fix? | `500` | **never** | **never counts** — a deterministic fault can only be cleared by an operator, so counting it guarantees a self-sustaining outage (A-12 / C-17 class) |

Two rules follow, and they are the load-bearing ones:

- **R-1 — `500` means "do not retry".** Today `500` is used for both bugs and transient failures, which is
  why A-02's undecryptable key can re-trip the breaker forever. Splitting `500` (permanent) from `503`
  (transient) makes the breaker's input set decidable without body parsing — which matters because
  **TRAP-2**: an unhandled exception is `500` `text/plain` (verified: `starlette.middleware.errors`
  `ServerErrorMiddleware.__call__` → `PlainTextResponse("Internal Server Error", status_code=500)`), so
  `500` *must* be safe to classify with no body at all.
- **R-2 — every deliberate 5xx body carries `{code, dependency, retryable}`.** `code` is the stable
  machine discriminator PYAPI-10 asks for; `dependency` names *which* of our dependencies failed
  (`mt5-gateway`, `kek`, `supabase`, `egress-proxy`) so 140.2 can key a breaker per dependency instead of
  the single global `breaker:railway` that A-01 shows is false. An unhandled `500` has no body — that is
  the signal that it is unclassified, and R-1 makes it safe.

### Q2.1 Every 5xx-emitting site reachable from the seam

**Seam-reachable Python endpoints (11), established from the TS side:** `/process-key`
(`process-key-client.ts:151`); `/internal/keys/{id}/permissions` (`keys/[id]/permissions/route.ts:131`);
`/api/validate-key`, `/api/encrypt-key`, `/api/optimize-weights`, `/api/portfolio-analytics`,
`/api/portfolio-optimizer`, `/api/portfolio-bridge`, `/api/simulator`, `/api/match/recompute`
(`analytics-client.ts:241,255,280,308,320,338,363,399`); `/api/match/eval` (`analytics-client.ts:418`).

| # | Site | Endpoint | Today | Trigger | Class | **Target** | Note |
|---|---|---|---|---|---|---|---|
| S-01 | `exchange.py:108` | `/api/validate-key` | 503 | sFOX client ctor `ValueError` — malformed `WORKER_EGRESS_PROXY_URL` | SERVICE-PERMANENT | **500** `code=EGRESS_PROXY_MISCONFIGURED`, `retryable:false` | deterministic until an operator edits env; 503 today makes it flap forever |
| S-02 | `exchange.py:215` | `/api/validate-key` | 503 | `MT5_GATEWAY_HOST`/`PORT` unset | SERVICE-PERMANENT | **500** `code=MT5_GATEWAY_UNCONFIGURED` | same shape as S-01. This is half of A-01 |
| S-03 | `exchange.py:220` | `/api/validate-key` | 503 | `MT5_GATEWAY_PORT` not an int | SERVICE-PERMANENT | **500** `code=MT5_GATEWAY_UNCONFIGURED` | |
| S-04 | `exchange.py:235` | `/api/validate-key` | 503 | MT5 gateway connect **timed out** | SERVICE-TRANSIENT | **503** `dependency=mt5-gateway`, `Retry-After` | genuinely transient (gateway restarts on deploy). 140.2 must key the breaker on `dependency` — this is the other half of A-01 |
| S-05 | `exchange.py:238` | `/api/validate-key` | 503 | MT5 gateway connect **failed** (any exc) | SERVICE-TRANSIENT | **503** `dependency=mt5-gateway` | |
| S-06 | `exchange.py:404` | `/api/validate-key` | 500 "Key validation failed. Please check your credentials." | bare `except Exception` around `validate_key_permissions` | **SPLIT** | `ccxt.BaseError` → **424** `code=EXCHANGE_PROBE_FAILED`; anything else → **500** `code=INTERNAL`, copy that does **not** mention credentials | C-16. `services/exchange.py:982-1021` already classifies known ccxt errors and returns `error_code`, so an escape here is by definition unclassified — but a `ccxt.*` escape is still attributable to the venue |
| S-07 | `exchange.py:424` | `/api/encrypt-key` | 503 "Encryption not configured" | `get_kek()` raises — KEK missing/rotated | SERVICE-PERMANENT | **500** `code=KEK_UNAVAILABLE`, `retryable:false` | C-17 verbatim. A permanent 503 on the busiest seam endpoint is the self-sustaining-outage shape |
| S-08 | `internal.py:208` | `/internal/keys/{id}/permissions` | 503 | `get_kek()` raises | SERVICE-PERMANENT | **500** `code=KEK_UNAVAILABLE` | same class as S-07; the finding lists only `/api/encrypt-key`. **Missing from the findings doc** |
| S-09 | `internal.py:214` | `/internal/keys/{id}/permissions` | 500 "Failed to decrypt credentials" | `decrypt_credentials` raises for one row | SERVICE-PERMANENT | **500** `code=KEY_UNDECRYPTABLE`, `retryable:false` | A-02. Status is already right; the fix is that 140.2 must not count `retryable:false`, and the badge must cache the negative |
| S-10 | `internal.py:218` | `/internal/keys/{id}/permissions` | **502** "API key has no exchange set" | `api_keys.exchange` is NULL/empty | **CALLER / data** | **422** `code=KEY_MISSING_EXCHANGE` | a NULL column on the caller's own row is not a service fault under any reading. Clearest mis-attribution in the file |
| S-11 | `internal.py:326` | `/internal/keys/{id}/permissions` | 502 | `create_exchange` raised non-`ValueError` | CALLER'S EXCHANGE | **424** `code=EXCHANGE_INIT_FAILED` | C-12 |
| S-12 | `internal.py:339` | `/internal/keys/{id}/permissions` | 502 "Exchange permission probe failed" | **any** exception from `detect_permissions` | CALLER'S EXCHANGE | **424** `code=EXCHANGE_PROBE_FAILED` | C-12's headline. Binance maintenance / revoked key / IP-allowlist change all land here, uncached, 5 badges per page render |
| S-13 | `match.py:1647` | `/api/match/recompute` | 503 "actor admin check temporarily unavailable" | `_is_admin_profile` returned `None` (Supabase blip) | SERVICE-TRANSIENT | **503** `dependency=supabase` | already correct; add `dependency` + `Retry-After` |
| S-14 | `match.py:1673` | `/api/match/recompute` | 503 "profile role check temporarily unavailable" | `_is_allocator_profile` returned `None` | SERVICE-TRANSIENT | **503** `dependency=supabase` | already correct; the in-file comment at `:1666-1670` is the reasoning the whole contract should copy |
| S-15 | `match.py:1765` | `/api/match/recompute` | 500 `f"Scoring failed: {err}"` | `_score_one_allocator` raised | SERVICE-PERMANENT | **500** `code=SCORING_FAILED` — **and strip `{err}` from `detail`** | ⚠️ interpolating a raw exception into a user-reachable `detail` is TRAP-1's class on a response body (C-13). Not in the findings doc |
| S-16 | `match.py:1817` | `/api/match/eval` | 503 "Eval truncated at N pages" | `PaginatedSelectTruncated` | **CALLER** (the caller's `lookback_days` exceeded a hard cap) | **400** `code=EVAL_WINDOW_TOO_LARGE` | a 503 here says "we are down" when the truthful statement is "narrow your window". **Not in the findings doc** |
| S-17 | `match.py:1826` | `/api/match/eval` | 500 `f"Eval failed: {err}"` | any exception | SERVICE-PERMANENT | **500** `code=EVAL_FAILED`, **strip `{err}`** | same leak class as S-15 |
| S-18 | `simulator.py:452` | `/api/simulator` | 500 `{error, correlation_id}` | any exception in the sim body | SERVICE-PERMANENT | **500** `code=SIMULATION_FAILED` | body shape is already the closest thing in the service to the R-2 envelope — use it as the template |
| S-19 | `portfolio.py:653` | `/api/portfolio-analytics` | 500 "Failed to create analytics row" | insert returned no row | SERVICE-TRANSIENT | **503** `dependency=supabase` | reachable only via `_compute_portfolio_analytics`, whose sole caller is `portfolio.py:1568` — the **zero-caller** route (`resilient-fetch.ts:203`). Fix for contract uniformity, not for user impact |
| S-20 | `portfolio.py:1163` | `/api/portfolio-analytics` | 500 "Portfolio analytics computation failed" | compute raised | SERVICE-PERMANENT | **500** `code=PORTFOLIO_ANALYTICS_FAILED` | same reachability caveat as S-19 |
| S-21 | *(implicit)* every seam endpoint | all 11 | **500 `text/plain`** | any unhandled exception | UNCLASSIFIED | **500**, no body | A-03 / TRAP-2. `/api/portfolio-optimizer` (`portfolio.py:1591`), `/api/portfolio-bridge` (`portfolio.py:1851`) and `/api/optimize-weights` (`optimizer.py:30`) have **no explicit 5xx at all** — S-21 is their *only* 5xx path |
| S-22 | *(implicit)* `/process-key` | `/process-key` | **500 `text/plain`** | any unhandled exception | UNCLASSIFIED | **500**, no body | ⭐ **`routers/process_key.py` contains ZERO explicit 5xx sites.** Its only status codes are `403,403,401,422,422` (`:270,278,805,832,855`). Every 5xx it has ever emitted is S-22 |
| S-23 | `main.py:230` | all except `/health`, `/internal/*`, `/process-key` | 503 "Service not configured" | `SERVICE_KEY` env unset | SERVICE-PERMANENT | **500** `code=SERVICE_KEY_UNCONFIGURED` | the 503 half of C-11 — see Q2.3 |
| S-24 | `main.py:279` | `/health` | 503 `{status:"stale"}` | worker heartbeat stale | SERVICE-TRANSIENT | **unchanged** | ✅ correct as-is. `/health` is excluded from the seam (`SEAM_EXCLUSIONS`), so leave it alone — and note A-12: routing the warmer through the core would make this 503 trip the breaker that then blocks recovery |

**5xx sites in `analytics-service/` that are NOT seam-reachable** (do not plan work against them; listed
so the enumeration is provably complete): `exchange.py:453,491,553` (`/api/fetch-trades` — no TS caller;
only a string literal in `debug-key-flow/route.ts:160`); `csv.py:95` (`/api/csv/validate` — the TS
`csv-validate` route goes through `/process-key`, `csv-validate/route.ts:206`); `portfolio.py:2242,2446`
(Python `/api/verify-strategy` — the TS teaser calls `/process-key`, not this);
`cron.py:613,630` (`/api/cron-sync`); `debug_key_flow.py:56,100` (founder-only SSE);
`services/analytics_runner.py:1725` — ⚠️ this raises `HTTPException(500)` from
`run_csv_strategy_analytics`, whose **only** caller is `services/job_worker.py:1947`, i.e. the worker.
An `HTTPException` raised outside an HTTP request is a category error that can never render; flag it to
the planner as a hygiene item, not a contract item.

### Q2.2 The hard case, stated explicitly

> *A 502 for "the user's exchange is down" must not trip our breaker, but it is genuinely not the user's
> fault either. What status does it get, and why?*

**`424 Failed Dependency`, with `code: EXCHANGE_PROBE_FAILED` and the venue name in the body.**

Reasoning, in the order it must survive review:

1. **It cannot be 5xx.** Status is the breaker's input (R-1) and the analytics service is provably
   healthy in this scenario — it successfully reached the venue and received a refusal. Emitting 5xx
   makes C-12 true: one dashboard render with five keys during a Binance outage = five 502s = a global
   trip that denies Deribit users, the optimizer, admin match and CSV finalize.
2. **It should not be `400`.** `400` already means "your request was malformed" across
   `exchange.py:96,114,123,130,143,203,204,385,393,412`. Overloading it destroys the distinction between
   "fix your input" and "wait for your venue", which is the single most important thing to tell this user.
3. **`424` is exact and free.** It is a registered IANA status (RFC 4918 §11.4), it is a 4xx so it is
   breaker-inert by construction, and it is distinguishable **from the status line alone** — which matters
   because TRAP-2 means the body may be absent.
4. **"Not the user's fault" is a copy problem, not a status problem.** The 4xx/5xx axis answers
   *"is our service degraded?"*, not *"who is to blame?"*. Conflating those two questions is the root
   cause of both A-01 and C-12. 140.3 renders `424` as *"Binance is not responding right now — your key
   is fine, try again shortly"*, which is neither an accusation nor a false claim about our uptime.

**Inter-phase obligation this creates for 140.2:** the discriminator must treat `424` as *recoverable,
non-counting*. If 140.2 collapses all 4xx into "caller error, not recoverable", `424` renders as an
un-retryable dead end — the B-01/B-22 failure shape. Record this in the phase contract explicitly.

### Q2.3 C-11's asymmetry, and what this contract does to it

C-11 verified against source:

| Direction | Mechanism | Result |
|---|---|---|
| Vercel's `ANALYTICS_SERVICE_KEY` stale | `main.py:232-234` `secrets.compare_digest` fails → **401** | 4xx ⇒ breaker never trips, `/health` stays green (`main.py:200-201` skips the middleware), `warm-analytics` stays green. **Total seam outage, zero operator signal.** |
| Python's `SERVICE_KEY` unset | `main.py:229-230` → **503** | 5xx on every call ⇒ breaker trips, expires, re-trips, forever (A-08/A-25) |
| Either direction | `/process-key` (`main.py:216-219`) and `/internal/*` (`main.py:208-209`) are **skipped by the middleware entirely** | both keep working — so the outage is *partial*, which is exactly why it does not look like an outage |

**The same misconfiguration, opposite behaviour, and neither is observable.** Under the Q2.0 contract:

- The `SERVICE_KEY`-unset arm (S-23) becomes **`500` `code=SERVICE_KEY_UNCONFIGURED`, `retryable:false`**
  — R-1 makes it non-counting, so the permanent flap stops.
- The stale-key arm stays **`401`** — that is correct, the caller genuinely presented a bad credential.

**Neither change produces an operator signal, and this phase cannot produce one.** The status contract is
necessary but not sufficient here. Two obligations must be recorded rather than quietly fixed:

- **140.1 (in scope, cheap):** emit a **structured ERROR log with a stable event name** on every
  `X-Service-Key` rejection at `main.py:234` and on the unset branch at `:230`. A 401 storm from a single
  caller is the only in-service evidence this failure mode leaves. Today `main.py:229-234` logs nothing at
  all on either branch — verified.
- **Out of scope, must be recorded for 140.2/ops:** `/health` cannot detect this because it is the one
  path the middleware skips (`main.py:200-201`). A credential-carrying probe is the only real detector,
  and it belongs with the warmer, not here.

⚠️ **Do not "fix" C-11 by routing the `/health` warmer through the seam core.** That is A-12: a cold
`/health` probe failing is normal, the warmer would feed `recordSeamFailure`, and the breaker would then
block its own recovery probe. Self-sustaining outage, zero tests red (D-14).

---

## Corrections to `140-FINDINGS-CONSOLIDATED.md` and to `140.1-CONTEXT.md`

Verified against source this session. The findings doc has been through five review rounds; these are
the places it is still wrong or imprecise in a way that would mislead a planner.

| # | Claim | Verdict | Evidence |
|---|---|---|---|
| **X-1** | CONTEXT `<canonical_refs>`: *"`analytics-service/routers/internal.py` — `/process-key`; the 502 arms (`:218`, `:326`, `:339`)"* | ⛔ **WRONG FILE MAPPING.** `/process-key` lives in `routers/process_key.py` (`:60` `APIRouter(prefix="/process-key")`). `internal.py:218/326/339` belong to `POST /internal/keys/{key_id}/permissions` (`internal.py:126`). **`process_key.py` contains ZERO 5xx sites** — its only statuses are `403,403,401,422,422` at `:270,278,805,832,855`. A planner following CONTEXT would hunt for 502 arms in the wrong handler. | `routers/process_key.py:60`, `routers/internal.py:48,126`; exhaustive `status_code=` grep of `process_key.py` |
| **X-2** | C-18: an unauthenticated caller can *"burn tokens from C-09's single global bucket without holding the token"* | ⛔ **FALSE, and the truth is worse.** The key function hashes whatever bearer was supplied, falling back to the literal `"unauthenticated"` (`process_key.py:99-105`) — so a tokenless caller lands in a **different** bucket and cannot drain the platform one. Meanwhile G-4/G-6 show pydantic validation runs **before** slowapi, so the `source:"sfox"` → 422 flag-enumeration path **consumes no token at all and is therefore completely unthrottled**. The DoS half of C-18 is wrong; the enumeration half is unbounded. | `process_key.py:99-105`; `slowapi/extension.py:717-731` |
| **X-3** | C-09: the anonymous DoS is real but unquantified | ✅ **CONFIRMED and cheap.** `publicIpLimiter` is 10/60 s per IP (`src/lib/ratelimit.ts:115`) = 600/hour, against a 100/hour Python bucket. **One IP drains the platform's whole allowance in ≈10 minutes.** The real vector is *through Vercel* (which attaches the real `INTERNAL_API_TOKEN`), not a direct Railway hit — see X-2. | `src/lib/ratelimit.ts:88,115`; `verify-strategy/route.ts:31-34,180` |
| **X-4** | C-10: *"**Three** routers re-declare private `Limiter(key_func=get_remote_address)`"* | ⚠️ **UNDER-SCOPED.** Three private `Limiter()` instances, yes (`exchange.py:40`, `csv.py:24`, `portfolio.py:146`) — but the defect *class* is IP keying, and **`routers/optimizer.py:31` has it while correctly importing the shared limiter** (it inherits `services/rate_limit.py:29`'s default `key_func`). Nine routes, not eight. Enumerating by "private Limiter" misses `/api/optimize-weights` — a live seam endpoint on the tightest ceiling on the platform (20/**minute**). **This is TRAP-5 latent inside the findings doc itself.** | `routers/optimizer.py:24,31`; `services/rate_limit.py:29`; see Q1.4 |
| **X-5** | C-10: *"validate/encrypt 100/hour (and key-connect spends **two** tokens)"* | ⚠️ **IMPRECISE.** Buckets are keyed on (key, **path**) — G-2 — so `/api/validate-key` and `/api/encrypt-key` have **separate** 100/hour counters. Key-connect spends one token from each. It cannot exhaust a shared 100/hour pool in 50 connects, as the phrasing implies. | G-2; `keys/validate-and-encrypt/route.ts:243,256` |
| **X-6** | C-15: *"slowapi's 429 has **no `detail`** and no `Retry-After`"* | ⚠️ **HALF RIGHT.** No `Retry-After` — ✅ confirmed empirically (`_headers_enabled == False`; `extension.py:380`). But the body is **not** empty: `{"error": "Rate limit exceeded: 100 per 1 hour"}` (`extension.py:80-82`). The reason it renders as `UNKNOWN` is that `analytics-client.ts:179` reads `error.detail ?? "Analytics service error"` and slowapi keys it `error`, not `detail`. That makes the TS half a **one-line** fix in 140.2 — materially cheaper than "the 429 carries nothing". | `slowapi/extension.py:75-86`; `src/lib/analytics-client.ts:179` |
| **X-7** | A-03: *"verified: starlette 0.52.1"* | ⚠️ **VERIFIED AGAINST THE WRONG VERSION.** `requirements.txt:230` pins **starlette 0.46.2**; 0.52.1 is what happens to be installed locally. The `PlainTextResponse("Internal Server Error", status_code=500)` behaviour is identical in both, so the *conclusion* stands — but the same slip in a version-sensitive claim would not. Same pattern applies to fastapi (pin 0.139.0 vs local 0.135.1) and slowapi (pin 0.1.10 vs local 0.1.9). | `requirements.txt:64,226,230`; `starlette.middleware.errors.ServerErrorMiddleware` |
| **X-8** | The findings doc's evidence list for PYAPI-05 (`exchange.py:108,215,220,235,238`; `internal.py:214,218,326,339`; `/api/validate-key`; `/api/encrypt-key`) | ⚠️ **INCOMPLETE — 5 seam-reachable sites missing.** `internal.py:208` (KEK 503, same class as the cited `/api/encrypt-key` one), `match.py:1647`, `match.py:1673`, `match.py:1765`, `match.py:1817`, `match.py:1826`, `simulator.py:452`, `portfolio.py:653`, `portfolio.py:1163`, plus the implicit unhandled-500 on all 11 endpoints and `main.py:230`. **Two of the omissions are genuine mis-attributions the doc never noticed: `match.py:1817` answers 503 for a caller-supplied window that is too large, and `match.py:1765`/`:1826` interpolate a raw exception into a user-reachable `detail`** (TRAP-1's class on a response body — C-13's surface, unremarked). See Q2.1 S-08, S-13..S-20. | exhaustive AST walk of every `HTTPException(status_code>=500)` in `routers/` + `services/` |
| **X-9** | PART 6: *"no SSR exposure … a breaker trip can never 500 a page"* — and the general assumption that `/api/csv/validate` and Python `/api/verify-strategy` are live seam endpoints | ✅ **CONFIRMED, with an addition.** Neither `/api/csv/validate` (`csv.py:57`) nor Python `/api/verify-strategy` (`portfolio.py:2147`) has any TS caller — both TS routes of those names go through `/process-key`. `/api/fetch-trades` (`exchange.py:447`) likewise has no caller. Their 5xx sites are **not** seam-reachable and must not consume plan budget. | `analytics-client.ts` path literals; repo grep for each path |

## Assumptions and open questions

| # | Item | Risk if wrong | How to close |
|---|---|---|---|
| **ASSUMPTION-1** | slowapi source was read at **0.1.9**; production pins **0.1.10** (`requirements.txt:226`). The behaviours G-2/G-4/G-8/G-9 are core dispatch logic and are very unlikely to have changed in a patch release. | If `0.1.10` changed the decorator-stacking or `_key_style` semantics, G-9 (layered limits) breaks silently — one of the two limits would simply never evaluate. | ONE pytest against a live `TestClient` that exhausts the inner limit and then the outer, asserting both 429. This is `test_two_stacked_limits_both_evaluated` in Q1.3 — it is a *required* test, not optional, precisely because of this gap. |
| **ASSUMPTION-2** | `FORWARDED_ALLOW_IPS` is not set in Railway's environment (it appears nowhere in the repo, but the repo cannot see Railway's dashboard-set vars). | If it *is* set to `*`, `get_remote_address` would return the leftmost `X-Forwarded-For` entry instead of the edge IP. | Does **not** change the recommendation: the seam's client is a Vercel lambda behind egress NAT, so per-IP is the wrong dimension either way. Worth one `railway variables` check during execution for the record. |
| **ASSUMPTION-3** | The analytics service runs a **single** Railway replica, so `memory://` limiter storage behaves as one global bucket per key. | With N replicas every limit becomes effectively N× looser and buckets reset unevenly on rolling deploys. | Confirm replica count at execution. If >1, the per-tenant limiter is *directionally* still correct but the numbers in Q1.4 are wrong by a factor of N — say so in the plan rather than silently shipping. |
| **OPEN-1** | `424` is a new status on this seam. `AnalyticsUpstreamError` fail-loud `RangeError`s on a malformed status (A-15), and `424` is well-formed, so nothing breaks — but `admin/match/*` currently flattens all upstream 4xx to a generic 500 (B-12). | A `424` from `/api/match/*` would render as "please try again" with no venue named. | Not this phase's to fix; **record as a 140.2 obligation** alongside "424 = recoverable, non-counting". |
| **OPEN-2** | Where should the `Retry-After` on a `503` come from? slowapi does not emit one and the service has no notion of dependency recovery time. | A fixed constant is a guess; omitting it means 140.3 cannot "name the real wait" (B-11). | Recommend a per-`dependency` constant declared in one table in `services/` rather than inlined at each raise site — the Cluster D lesson about constants pinned to literals applies here too. |
| **OPEN-3** | Whether the anonymous teaser's own bucket (`process_key:anon`) should be sized differently from a tenant bucket. | Sizing it at 100/hour still lets anonymous traffic consume as much as a paying tenant, which is a business call, not a technical one. | Founder decision. Technical recommendation: size `anon` **below** a tenant bucket, since the teaser's Vercel-side cap (10/min/IP) is per-IP and therefore trivially horizontal. |

---
*Written 2026-07-26 for `/gsd:plan-phase` 140.1. Every table row above was read from source in this
session; nothing was carried over from the findings doc unverified. READ-ONLY on source — no file
outside this document was modified.*
