---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 09
subsystem: phase-gate
tags: [gate, mutation-ledger, obligations, ci-mirror, falsifiability]
requires: [140.1-01, 140.1-02, 140.1-03, 140.1-04, 140.1-05, 140.1-06, 140.1-07, 140.1-08]
provides:
  - "CI-mirrored green gate evidence for the fully-composed phase tree"
  - "M1-M8 canonical mutation ledger with per-row first-hand / cited / asserted labelling"
  - "140.1-TS-OBLIGATIONS.md (31 obligations) for 140.2 / 140.3 / 145 / 146"
  - "PROD wedge blast-radius = 0 rows; Railway N=1"
affects: []
tech-stack:
  added: []
  patterns:
    - "Anti-vacuity harness proof: before trusting an empty-result SQL gate, deliberately falsify one assertion and confirm the transport surfaces it"
key-files:
  created:
    - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-09-SUMMARY.md
  modified:
    - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
decisions:
  - "PYAPI-03 reconciliation is 9/9, NOT 10/10 — a tenth IP-keyed route (simulator.py:92) is quarantined, not repaired"
  - "M2/M6/M8 re-run first-hand in this session rather than accepting the prior plans' cited evidence"
  - "SQL gates executed against TEST via the Supabase Management API, with a deliberate falsification to prove the transport is not silently green"
metrics:
  duration: ~95 min
  completed: 2026-07-26
---

# Phase 140.1 Plan 09: Phase Gate Summary

Every CI-mirrored gate is green on the fully-composed tree (pytest 4620 passed / 90.28% cov,
mypy --strict 0, eslint 0 errors, tsc 0, vitest 8867 passed, both new SQL gates green on TEST with
a falsification proof); the canonical M1–M8 mutation ledger is 8/8 with **M2, M6 and M8 re-observed
RED first-hand in this session**; and the four known partial-deliveries are recorded honestly rather
than reported as closed.

**Plan status: COMPLETE.** Tasks 1, 2 and 3 all done. Task 3's artifact was produced in a prior
session that died before writing this SUMMARY; it was verified against its acceptance criteria and
corrected in three places (below), not rewritten.

---

## Headline: what is NOT true

Reported first, because the whole point of this gate plan is that the programme previously shipped
"verified" work with 166 latent defects.

1. **PYAPI-03 is 9/9, not 10/10.** Nine routes are off `get_remote_address` — but they key on
   `platform:<path>`, not tenant, because `src/lib/analytics-client.ts` mints no `X-Tenant-Claim`
   and that file is Phase 140.2's. Only `/process-key` is genuinely per-tenant today.
2. **A tenth IP-keyed route is still live** at `analytics-service/routers/simulator.py:92`.
3. **Plan 05 over-broadened a 403** — ordinary validation failures now answer 403 where 422 would be
   sharper.
4. **Three of the 96 pytest skips are unconditional `@pytest.mark.skip`s that dodge a flake**, and a
   further 40 skip in CI too. "96 skipped, pre-existing environmental" — the phrase prior waves used
   — is **not accurate as stated**.

Details in §6 and §2.

---

## 1. Task 1 — every gate CI will run, run locally

Working tree at `56fb7167` on `feat/v1.16-production-resilience`. Baseline for all deltas is the
pre-phase commit `43449cc6`.

| Gate | Command (CI-mirrored) | Result |
|---|---|---|
| pytest + coverage | `python3 -m pytest --cov=services --cov=routers --cov=main_worker --cov-fail-under=80 -q -rs` | **4620 passed, 96 skipped, 0 failed** in 69.83s · exit 0 |
| coverage floor | same run | `TOTAL 15944 1549 90%` → **`Required test coverage of 80% reached. Total coverage: 90.28%`** |
| mypy strict | `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` | **`Success: no issues found in 89 source files`** — 0 errors |
| eslint | `npm run lint` | **0 errors**, 1 pre-existing warning (below) |
| tsc | `npx tsc --noEmit` | **exit 0, zero output lines** |
| vitest (FULL suite) | `npx vitest run --no-file-parallelism` | **Test Files 696 passed \| 19 skipped (715)** · **Tests 8867 passed \| 287 skipped (9154)** · 367.63s · exit 0 |
| SQL gate 1 | `test_strategy_verifications_wizard_session_tenant_scope.sql` on TEST `qmnijlgmdhviwzwfyzlc` | **GREEN** (`[]`, HTTP 201) |
| SQL gate 2 | `test_enqueue_compute_job_dedupe_non_terminal.sql` on TEST | **GREEN** (`[]`, HTTP 201) |

**Zero source changes were needed** — no gate was red, so no deviation-rule fix was applied. The
only files this plan touched are the two `.planning/` artifacts (which are gitignored).

### eslint's one warning is not ours

```
src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx
  1119:6  warning  React Hook useMemo has a missing dependency: 'period'
✖ 1 problem (0 errors, 1 warning)
[check-admin-route-manifest] OK — 20 admin routes, all declared in manifest.
[check-route-contract] OK — 56 page routes, all declared in the manifest.
```

`EquityChart.tsx` appears in **zero** commits of this phase (`git diff --stat 43449cc6..HEAD`
lists 38 files; that is not one of them). Pre-existing, out of scope per the executor's scope
boundary, not fixed.

### The full vitest suite was run, not just the one file

The plan's `<verify>` names only the `process-key-client` test. The repo lesson (shared fixtures
diff → BOTH suites) applies here: plan 06 added 54 lines to `src/lib/process-key-client.ts`, which
is the shared choke point every seam consumer routes through. The full suite was therefore run
serially. 0 failures across 696 files.

Vitest was also **not** re-run under Node 22 — there was no failure to attribute, so the
CI-vs-local Node-version check (CI=Node 22, local=Node 25) had nothing to disambiguate. Recorded so
the next reader knows it was considered rather than forgotten.

### No new `# type: ignore`

```
git diff 43449cc6..HEAD -- analytics-service | grep -c '^+.*type: ignore'   → 0
git diff 43449cc6..HEAD -- analytics-service | grep -c '^-.*type: ignore'   → 0
git diff 43449cc6..HEAD                      | grep -c '^+.*type: ignore'   → 0   (whole repo)
```

Zero added, zero removed, repo-wide. The phase's narrowing was done with `cast()` throughout, as
the critical warning required.

---

## 2. The 96 skips — enumerated and adjudicated, not inherited

Prior waves wrote "96 skipped, pre-existing environmental" without verifying it. **That claim is
wrong in three places.** Every skip is accounted for below; the multipliers in pytest's
`SKIPPED [N]` lines are summed, so the table totals exactly 96.

| # | Reason bucket | Where | Verdict |
|---:|---|---|---|
| **46** | `test Supabase project not configured` (`SUPABASE_TEST_URL` / `SUPABASE_TEST_SERVICE_KEY`) | `test_compute_jobs_fencing.py` (25), `test_compute_similarity_sql.py` (10), `test_drain_semantics.py` (6), `test_transition_rpc.py` (5) | ✅ **ENVIRONMENTAL and genuinely covered** — CI's `python` job sets both from `secrets.TEST_SUPABASE_*` gated on `vars.E2E_TEST_DB_CONFIGURED`, which is **`true`** (verified live: `gh variable list` → `E2E_TEST_DB_CONFIGURED  true  2026-04-30`). These run in CI. |
| **31** | `Live test Supabase project not configured (TEST_SUPABASE_DB_URL unset)` | `test_persist_csv_daily_returns_live.py` (12), `test_csv_daily_returns_dualaxis_live.py` (7), `test_trigger_rls_audit.py` (4), `test_csv_daily_returns_perkey_rls_live.py` (3), `test_sync_trades_preserves_fills.py` (2), `test_resend_correlation_rls.py` (2), `test_job_worker.py` (1) | ⚠️ **ENVIRONMENTAL LOCALLY, BUT DEAD IN CI TOO — FINDING-14.** `TEST_SUPABASE_DB_URL` is wired into the `sql-tests` job **only** (`ci.yml:810`). The `python` job's `env:` block sets `SUPABASE_TEST_URL`, `SUPABASE_TEST_SERVICE_KEY` and `CI` — nothing else. **These 31 run nowhere.** |
| **5** | `Python parity test gated on HAS_PY_ENV=1` | `test_outcomes_kpi_parity.py` | 🚩 **NOT ENVIRONMENTAL — FINDING-15.** A repo-wide grep (excluding `node_modules`/`.git`) for `HAS_PY_ENV` returns **three lines, all inside the test file itself.** No workflow, Makefile, script or `.env` sets it. Permanently dormant Phase-4-vs-Phase-5 KPI parity coverage. |
| **4** | `cassette not yet recorded: binance/{happy,auth-fail,rate-limit,schema-drift}.yaml` | `test_repro_key_flow.py:38` | 🚩 **NOT ENVIRONMENTAL — FINDING-16.** `analytics-service/tests/cassettes/` contains `okx/` and `bybit/` only. Missing fixtures are missing in CI too. Founder gate 16-08 Task 3, still open. |
| **3** | `P1 TODO — flaky httpx.ReadTimeout` | `test_compute_jobs_fencing.py` | 🚩 **NOT ENVIRONMENTAL — unconditional `@pytest.mark.skip`, a test skipped to dodge a failure.** Named in full below. |
| **3** | `FOUNDER-GATED` sFOX live smoke (`SFOX_SANDBOX_KEY` / `SFOX_GROUND_TRUTH_KEY` unset) | `test_sfox_client_live.py` (2), `test_sfox_ground_truth_live.py` (1) | ⚠️ **Credential-gated, and the skip messages say so themselves: _"Skipping keeps CI green, but phase 118 SC-3 stays human_needed until this runs green — a skip is NOT a pass."_** Counted as environmental, flagged because the code disagrees with calling it covered. |
| **1** | `dynamic apply/re-apply roundtrip requires SUPABASE_TEST_URL` | `test_migration_108_idempotent.py:115` | ✅ ENVIRONMENTAL (runs in CI — same var as bucket 1). |
| **1** | `live-ccxt tests require QUANTALYZE_LIVE_CCXT=1` | `test_equity_reconstruction_live.py` | ✅ ENVIRONMENTAL (live network, deliberately opt-in). |
| **1** | `integration tests require QUANTALYZE_INTEGRATION_DB=1` | `test_equity_reconstruction_integration.py` | ✅ ENVIRONMENTAL (opt-in). |
| **1** | `H-0762 live probe needs HAS_LIVE_DB + SUPABASE_URL + SUPABASE_ANON_KEY` | `test_upsert_strategy_analytics_series_batch_privilege.py:125` | ✅ ENVIRONMENTAL. |
| **96** | | | |

### The three that are NOT environmental by any reading

Unconditional `@pytest.mark.skip` — no `skipif`, no env var, no platform gate. They are skipped
because they were flaky, and by name:

| File:line | Test |
|---|---|
| `tests/test_compute_jobs_fencing.py:1063` | `test_late_mark_done_with_stale_token_raises_serialization_failure` |
| `tests/test_compute_jobs_fencing.py:1183` | `test_late_mark_failed_with_stale_token_raises_serialization_failure` |
| `tests/test_compute_jobs_fencing.py:1612` | `test_late_mark_done_after_w2_completed_raises_serialization_failure` |

**In fairness to whoever wrote them, these are the best-documented skips in the repo.** Each carries
a four-part reason: root cause (httpx.ReadTimeout at ~120s from contention on the shared test
project when `python` and `e2e` run concurrently), a 2026-07-12 re-justification explaining why
Phase-97's CI-01 fix does *not* address it, an argument that the contract is independently pinned
(mig-117's server-side self-verify `DO` block plus 9/12 sibling fence tests plus mocked
equivalents), and a sanctioned re-enable recipe. They are a **deliberate, argued deferral, not a
silent dodge.**

They are still skips. **Nothing in this phase touched them and nothing in this phase depends on
them.** Recorded so that "4620 passed" is never read as "4620 + 96 verified".

**Plain statement, per the fail-loud rule: this phase's own tests all ran. None of the 96 skips is
in a file this phase created or modified.** Verified by set intersection, not by eyeballing — the
19 files in `git diff --name-only 43449cc6..HEAD -- analytics-service/tests` intersected against the
19 files carrying a `^SKIPPED` line gives the **empty set**:

```
comm -12 <phase-touched tests/*.py>  <files with SKIPPED lines>   →   (empty)
```

---

## 3. `analytics-service/main.py` is outside the mypy gate — verdict: **NOT deliberate**

The orchestrator asked whether the exclusion is intentional. It is not, and the CI comment says so
against itself.

**Evidence, in the order it settles the question:**

1. **There is no mypy config that excludes it.** `analytics-service/mypy.ini` and `setup.cfg` do not
   exist. `pyproject.toml` `[tool.mypy]` carries flags plus three `[[tool.mypy.overrides]]` blocks —
   **no `exclude`, no `files`, no per-module ignore naming `main`**. The gate surface is set purely
   by the paths on the command line: `mypy --strict --follow-imports=silent services/ routers/ models/`.
2. **The CI comment claims a coverage main.py contradicts.** `ci.yml:985-993`: *"the strict floor
   started at `services/ingestion/` … and now covers **ALL running-service code** — `services/`,
   `routers/` and `models/`"*, then explains only two exclusions: `scripts/` (*"one-off operational
   tooling … not the running service"*) and `tests/` (*"untyped by design"*). **`main.py` is the
   FastAPI application entrypoint — it is running-service code by the comment's own definition, and
   the comment does not mention it.** It was left out because the widening was done directory by
   directory and a top-level module is not a directory.
3. **Nothing else re-adds it.** `Makefile` `typecheck` runs the same three paths.

**Current standalone count: 5 errors.**

```
$ python3 -m mypy --strict --follow-imports=silent main.py
main.py:231: error: Function is missing a return type annotation  [no-untyped-def]
main.py:278: error: Missing type arguments for generic type "Task"  [type-arg]
main.py:631: error: Function is missing a return type annotation  [no-untyped-def]
main.py:631: error: Function is missing a type annotation for one or more parameters  [no-untyped-def]
main.py:734: error: Function is missing a return type annotation  [no-untyped-def]
Found 5 errors in 1 file (checked 1 source file)
```

**The 6 → 5 the prior wave observed is real, and it is an improvement, not a regression.** Measured
against the pre-phase baseline without any `git checkout` (the `.planning/` ledgers are untracked;
checking out would delete them) — `git show 43449cc6:analytics-service/main.py` piped to a scratch
copy and type-checked from the `analytics-service` cwd:

```
Found 6 errors in 1 file (checked 1 source file)
```

The error the phase **removed** is baseline `:174`:

```
error: Argument 2 to "add_exception_handler" of "Starlette" has incompatible type
"Callable[[Request[State], RateLimitExceeded], Response]"; expected ...  [arg-type]
```

i.e. plan 08's `cast()`-narrowed handler registration fixed a real strict error at the site it
touched. The surviving 5 are all pre-existing `no-untyped-def` / `type-arg` on functions the phase
did not touch.

**No change was made to the gate paths, as instructed.** Recorded as an observation for whoever owns
the B-mypy programme: adding `main.py` to the gate costs 5 annotations and closes the entrypoint.

---

## 4. Task 1 — the two new SQL gates, run against TEST and proven non-vacuous

`psql` is not installed locally and `TEST_SUPABASE_DB_URL` is a GitHub secret, so — exactly as plans
01 and 02 did — the files were executed through the Supabase **Management API**
(`POST /v1/projects/{ref}/database/query`) against **TEST `qmnijlgmdhviwzwfyzlc`**, using the
Supabase CLI's stored token. **PROD `khslejtfbuezsmvmtsdn` was never contacted.** Both files are
`BEGIN; … ROLLBACK;`-wrapped, so nothing was persisted even transiently.

**Precondition confirmed first** — the migration under test really is applied to TEST, so a pass
means something:

```sql
select indexname, indexdef from pg_indexes
where tablename='strategy_verifications' and indexname like '%wizard_session%';
```
```json
[{"indexname":"strategy_verifications_strategy_wizard_session_uniq",
  "indexdef":"CREATE UNIQUE INDEX strategy_verifications_strategy_wizard_session_uniq
              ON public.strategy_verifications USING btree (strategy_id, wizard_session_id)"}]
```

Exactly one index, composite, old single-column one gone.

| Gate | Result |
|---|---|
| `supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql` | `[]` · `HTTP_STATUS=201` → **GREEN** |
| `supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql` | `[]` · `HTTP_STATUS=201` → **GREEN** |

### Anti-vacuity: an empty result was not taken on trust

The Management API discards `RAISE NOTICE`, so a **passing** gate and a **silently ignored** gate
both return `[]`. That is precisely the silent-green shape this programme exists to kill. So one
assertion was deliberately falsified — A4's expected unique-index count flipped `1` → `99` in a
scratch copy (never in the repo) — and re-run:

```
{"message":"Failed to run sql query: ERROR:  P0001: TEST FAILED (A4): expected exactly 1 unique
index covering wizard_session_id on strategy_verifications, found 1. More than one means the old
single-column index was left in place alongside the new composite one — the old one still enforces
GLOBAL uniqueness, so the tenant-scope fix is inert.
CONTEXT:  PL/pgSQL function inline_code_block line 147 at RAISE"}
HTTP_STATUS=400
```

**The transport surfaces `RAISE EXCEPTION` as HTTP 400 with the verbatim message.** `[]` + HTTP 201
is therefore a real pass, not an unexecuted file. Status: **VERIFIED GREEN**, not
UNVERIFIED-LOCALLY.

CI itself runs these differently (`psql -v ON_ERROR_STOP=1 -f`, `sql-tests` job, gated on
`vars.E2E_TEST_DB_CONFIGURED` = **`true`** and on a non-fork PR head). Both files match the job's
`supabase/tests/test_*.sql` glob with no workflow edit, and both pass the job's meta-command
preflight — **re-run locally in this session with the workflow's four exact ERE patterns**
(`^\\\!|^\\!| \\\!| \\!`, `\\copy\b`, `\\COPY\b`, `^\\o |^\\o$| \\o `) against both files:
`Preflight CLEAN`.

---

## 5. Task 2 — the canonical M1–M8 mutation ledger

Canonical definitions are `140.1-RESEARCH.md` §"Mutation checks the plan must run before declaring
done" (lines 1027-1043). Each row is labelled with **who observed the RED**, not merely that a RED
is claimed somewhere.

| M# | Mutation | Named RED oracle | RED evidence | Provenance | Source |
|---|---|---|---|---|---|
| **M1** | Revert the composite index to single-column | PYAPI-01a + PYAPI-01c, in `supabase/tests/test_strategy_verifications_wizard_session_tenant_scope.sql` | `{"a1_red": true, "a1_sqlstate": "23505", "a4_red": true, "mutant_cols": "{wizard_session_id}"}` — both A1 and A4 fired; run **inside a rolled-back DB transaction**, so no file and no schema was left mutated | **CITED** — first-hand by plan 01's own executor | `140.1-01-SUMMARY.md:139-151` |
| **M2** ⭐ | Drop `.eq("strategy_id", …)` from the **race-winner site only** (`routers/process_key.py:1226`), leaving the pre-check intact | `test_pyapi_01d_race_winner_read_is_tenant_scoped` | **`1 failed, 3 passed`** — see the verbatim run below | ✅ **FIRST-HAND, THIS SESSION** (also first-hand in plan 02) | this SUMMARY §5.1 |
| **M3** | Move `_verify_internal_token` back below validation (restore the bare `return await call_next(request)` skip) | PYAPI-04a (`test_process_key_auth_order.py`) | **8 tests RED**, including 04a; reverted, `grep -rn MUTANT analytics-service/ src/` empty | **CITED** — first-hand by plan 06's executor | `140.1-06-SUMMARY.md:79-84` |
| **M4** | Re-add `input` to the app-global 422 handler's error dicts (`... MUTANT_M4={err.get('input')!r}`) | PYAPI-07a — **both canary oracles** | **5 tests RED including both canaries**; reverted → `11 passed`; `grep -c MUTANT main.py` → 0 | **CITED** — first-hand by plan 08's executor | `140.1-08-SUMMARY.md:176-178` |
| **M5** | Change the 429 `code` literal (→ `"MUTANT_M5_THROTTLED"`) | PYAPI-08a and 08c | **08a and 08c RED**; reverted → `7 passed`; `grep -c MUTANT` → 0 | **CITED** — first-hand by plan 08's executor | `140.1-08-SUMMARY.md:224-226` |
| **M6** ⭐ | Make the enqueue-aware pre-check return the literal `queued: True` **without enqueuing** | `test_pyapi_09a_wedged_session_replay_enqueues_and_reports_queued` — the **job-existence** assertion, not the field | **`4 failed, 3 passed`** — see §5.2 | ✅ **FIRST-HAND, THIS SESSION** (also first-hand in plan 02) | this SUMMARY §5.2 |
| **M7** | Restore the scope rejection to a bare-dict 200 | PYAPI-10b — `test_pyapi_10b_write_capable_key_rejection_is_403` (×4 params) **plus** the 4 rewritten legacy tests | **`10 failed, 75 passed`**, listed by name; reverted → `85 passed`, no residue | **CITED** — first-hand by plan 05's executor | `140.1-05-SUMMARY.md:236-253` |
| **M8** ⭐ | Delete `ok` from **shape 5 only** | `test_pyapi_10a_every_200_carries_ok_discriminator[5-synchronous-success]` | **`1 failed, 15 passed`** — exactly one parametrised case | ✅ **FIRST-HAND, THIS SESSION** (also first-hand in plan 05) | this SUMMARY §5.3 |

**Count: 8/8. Zero rows are "asserted only".** Five are cited to a named oracle and a pasted failure
count in the owning plan's SUMMARY; three (the ⭐ second-member-of-class detectors) were re-run and
re-observed in this session even though their cited evidence was already explicit, because "the
prior wave said so" is the exact failure mode this plan exists to close.

### 5.1 M2 re-run — first-hand

Applied by deleting **line 1226 only** (`.eq("strategy_id", strategy_id)` inside the `race_winner =
one(...)` block), leaving the pre-check's identical line at `:1137` untouched. Occurrence count went
`2 → 1`, confirming a single-site mutation.

```
FAILED tests/test_process_key.py::test_pyapi_01d_race_winner_read_is_tenant_scoped
1 failed, 3 passed, 87 deselected
```

**Exactly one oracle red; the pre-check oracle stayed green.** That is the whole point — a fix
applied to only the first member of the class would still pass the pre-check test. The failure log
even shows the leak path executing: `process_key.idempotent_race_resolved verification_id=ver-raced`
with the assertion diffing on `{'strategy_id': '22222222-bbbb-...'}`.

Reverted from an explicit scratch copy; occurrence count back to `2`.

### 5.2 M6 re-run — first-hand

Applied at the **pre-check emitter only** (`routers/process_key.py:1167-1173`), replacing the
`_resume_duplicate_job(...)` call with `_queued, _job_state = True, "enqueued"  # MUTANT_M6`. The
race-winner emitter's own `_resume_duplicate_job(...)` call was left intact.

```
E  AssertionError: the duplicate path must actually enqueue — a `queued:true` that no job
   backs is the C-19 wedge with nicer wording
E  assert 0 == 1
E   +  where 0 = len([])
E  AssertionError: assert 'enqueued' == 'running'
FAILED tests/test_process_key.py::test_pyapi_09a_wedged_session_replay_enqueues_and_reports_queued
FAILED tests/test_process_key.py::test_pyapi_09_duplicate_with_live_job_reports_running
FAILED tests/test_process_key.py::test_pyapi_09_duplicate_on_finished_session_does_not_resync
FAILED tests/test_process_key.py::test_pyapi_09_synchronous_duplicate_is_not_applicable
4 failed, 3 passed, 84 deselected
```

**The oracle asserts the SIDE EFFECT — `len(recorded_enqueue_calls) == 1` — not the self-reported
`queued` field.** `assert 0 == 1 where 0 = len([])` is the proof: the mutant reports `queued:true`
happily, and the test fails anyway because no job was recorded. A field-reading oracle would have
passed. Louder than plan 02 recorded (4 red, not 1) because plan 05's later work added three more
`pyapi_09*` cases that also read the real job state.

Reverted from the scratch copy; `grep -n MUTANT_M6` → no match.

### 5.3 M8 re-run — first-hand

Applied by deleting **`"ok": True,` at `routers/process_key.py:1564` only** — the shape-5
synchronous-success emitter. Guarded by asserting the two following lines (`"code": None,` and
`"verification_id": verification_id,`) before deleting, so the wrong `"ok": True` could not be hit;
the other four remained.

```
E  AssertionError: shape 5-synchronous-success has no `ok` discriminator — a consumer must sniff
   field presence to classify it; got keys ['code', 'correlation_id', 'encrypted_credentials',
   'errors', 'fingerprint', 'matched_strategy_id', 'metrics_snapshot', 'status', 'trust_tier',
   'verification_id']
FAILED tests/test_process_key_200_discriminator.py::
       test_pyapi_10a_every_200_carries_ok_discriminator[5-synchronous-success]
1 failed, 15 passed
```

**Exactly one of the six parametrised shape cases went red.** The oracle discriminates *per shape*
rather than passing on any single site — which is the property that would have caught the
five discarded fix batches. (16 cases in the file today vs the 10 plan 05 saw; plan 08 added to it.)

Reverted from the scratch copy.

### 5.4 Post-mutation clean-tree proof

Run after the third revert, and again at the end of the plan:

```
$ git status --porcelain
?? analytics-service/scripts/nautilus_factsheet.py

$ git diff --stat
(no output)

$ grep -rn "MUTANT" analytics-service/routers analytics-service/services src/lib/process-key-client.ts
(no output, exit 1)

$ python3 -m pytest tests/test_process_key.py tests/test_process_key_200_discriminator.py -q
107 passed
```

**On the one untracked file:** `analytics-service/scripts/nautilus_factsheet.py` was already
untracked when this plan started and is **not** this plan's work. It was not added, committed, moved
or deleted. `git status --porcelain` is therefore **not** literally empty, and the plan's
`test -z "$(git status --porcelain)"` verify command would return non-zero on it. Stating that
plainly rather than claiming an empty tree: **the working tree is clean modulo that one
pre-existing untracked file, and `git diff` against HEAD is genuinely empty.**

No `git clean`, no `git stash`, no `git reset --hard`, no blanket `git checkout` was used at any
point. Each mutation was reverted by copying back an explicit pre-mutation scratch copy of
`routers/process_key.py` taken before the first mutation.

### 5.5 Numbering collisions reconciled — the supplementary mutations

Three plans ran mutations under **local** numbering that collides with the canonical M1–M8.
`140.1-TS-OBLIGATIONS.md` §8 records the 140.1-08 collision and delegates the reconciliation here.
**Two further collisions (plans 03 and 04) were not previously recorded and are reconciled here for
the first time.** None of these is a canonical mutation; none substitutes for one; all are logged as
supplementary because each is a genuine proven-RED oracle check that should not be lost.

| Supp. ID | Plan called it | Actual mutation | Named RED oracle | Result |
|---|---|---|---|---|
| **S-01** | 140.1-03 "M1" | `internal.py` S-12: `424` → `503` (re-classify a venue fault as ours) | `test_s12_permission_probe_failure_is_venue_attributable_424` + canary | 2 failed, 16 passed |
| **S-02** | 140.1-03 "M2" | `exchange.py` S-06: delete the `except ccxt.BaseError` arm | `test_s06_ccxt_escape_is_exchange_attributable_424` | 1 failed, 17 passed |
| **S-03** | 140.1-04 "M1" | S-15: restore the exception interpolation (`detail=f"MUTANT Scoring failed: {err}"`) | `test_s15_scoring_failure_leaks_no_exception_text` | 1 failed, 11 passed |
| **S-04** | 140.1-04 "M2" | S-16: re-classify the caller's oversized window as a transient outage (400 → 503) | `test_s16_eval_window_too_large_is_caller_400` | 1 failed, 11 passed |
| **S-05** | 140.1-04 "M3" | S-23: back to a transient 503 | `test_s23_unconfigured_service_key_is_permanent_500` **and** `test_unconfigured_service_key_returns_classified_500` | 2 failed, 14 passed — **fires in two files**, the intended redundancy |
| **S-06** | 140.1-08 "M6" | `_SECRET_CAPTURE_MIN_INTERVAL_S = 300.0` → `0.0` | the 25-request storm test | RED (25 captures, not 1) |
| **S-07** | 140.1-08 "M7" | capture message → `f"Platform secret {secret_name}={SERVICE_KEY} is {fault}"` | the no-substring oracle | RED |

**Canonical M6 and M8 are NOT the same thing as S-06 and S-07**, and canonical M6 and M7 both have
their own first-hand evidence (plans 02 and 05, plus this session's re-run of M6). The collision was
purely a naming accident in three plans that each restarted local counters at "M1". Recorded so a
future reader counting "M1…M7 in plan 04's SUMMARY" does not conclude the canonical ledger is
duplicated or double-counted.

**Total mutation checks proven RED across the phase: 8 canonical + 7 supplementary = 15.**

---

## 6. TRAP-9 audit — do the rewritten tests pin the NEW contract, or just stop failing?

**The standard applied:** a rewritten test must assert a **literal** value of the new contract.
"It stops failing" is not coverage. A test whose only change is a status literal is fine *provided*
the new literal is exact (`== 403`) and not a widened range (`>= 400`, `in (403, 422)`).

### 6.1 The whole-phase falsifier, run first

Before sampling, the strongest single check — **was any assertion deleted without replacement?**

```
git diff 43449cc6..HEAD -- analytics-service/tests | grep -cE '^\+[^+].*assert '   → 466 added
git diff 43449cc6..HEAD -- analytics-service/tests | grep -cE '^-[^-].*assert '    →  11 removed
```

**All 11 removals, enumerated, with where each went:**

| × | Removed line | Where it went |
|---|---|---|
| 4 | `assert r.status_code == 200[, r.text]` (one carrying the comment `# envelope returns 200 with ok=False per DESIGN-05.`) | → `assert r.status_code == 403, r.text` (plan 05, PYAPI-10b). Status literal moved; every orthogonal assertion in those tests survives verbatim |
| 1 | `assert resp.status_code == 503` | → `assert resp.status_code == 500` (plan 04, S-23) |
| 1 | `assert resp.json() == {"detail": "Service not configured"}` | → the **full four-key envelope** dict literal (strictly more assertion, not less) |
| 1 | `assert ei.value.status_code == 503` | → `== 500` (plan 03, MT5_GATEWAY_UNCONFIGURED) |
| 1 | `assert ei.value.detail == NETWORK_ERROR_DETAIL` | → `detail["code"] == "MT5_GATEWAY_UNCONFIGURED"` + `dependency == "mt5-gateway"` + `retryable is False` + no `Retry-After` + `detail != AUTH_FAILED_DETAIL` (1 line → 5) |
| 1 | `assert key_func(req_a) == ka` | → survives in `test_key_func_unverified_fallback_is_token_keyed` as `key_func(_req({...})) == ka` |
| 1 | `assert "retry" in body.get("detail","").lower() or "temporarily" ...` | → preserved verbatim in the rewrite, reading from `body["detail"]["detail"]` where the string now lives (**confirmed present in current source**) |
| 1 | a docstring line beginning "assert" (`lifespan). We assert that the on-disk source explicitly contains`) | prose, not an assertion |

**Nothing was relaxed to a range and nothing was deleted without a stronger replacement.**
466 added vs 11 removed is a 42:1 ratio.

### 6.2 Sample: 12 rewritten test functions, read from current source

Every rewritten test named in plans 03, 04, 05, 06 and 08 was sampled — that is the **complete
population** of genuinely-rewritten tests, not a subset.

| # | Test | Pins a literal? | The literal(s) |
|---|---|---|---|
| 1 | `test_mt5_validate.py::test_mt5_missing_gateway_env_is_permanent_500_and_secret_free` (03) | ✅ | `500`, `"MT5_GATEWAY_UNCONFIGURED"`, `"mt5-gateway"`, `retryable is False`, no `Retry-After`, `!= AUTH_FAILED_DETAIL`, + 3 credential-absence literals (`"123456"`, `"s3cr3t-pw"`, `"MyBroker-Live"`) — **9 asserts** |
| 2 | `test_match_router.py::test_recompute_returns_503_on_profile_check_transient_error` (04) | ✅ | `503`, `dependency == "supabase"`, `retryable is True`, `Retry-After` present and `int(...) > 0`, `isinstance(human, str)`, + the preserved transient-copy assertion — **7 asserts** |
| 3 | `test_verify_service_key_middleware.py::test_unconfigured_service_key_returns_classified_500` (04) | ✅✅ | `500` **and the entire body as one exact dict literal** `{"detail": {"code": "SERVICE_KEY_UNCONFIGURED", "dependency": None, "retryable": False, "detail": "Service not configured"}}`, plus `"Retry-After" not in resp.headers` |
| 4 | `test_process_key.py::test_process_key_validate_failure_returns_envelope` (05) | ✅ | `403`, `ok is False`, `code == "AUTH_FAILED"`, `correlation_id == _TEST_CID`, `debug_context.verification_id == "ver-bad"` |
| 5 | `test_process_key.py::test_process_key_sync_pipeline_rejects_write_capable_key` ×3 params (05) | ✅ | `403`, `ok is False`, `code == error_code`, **plus two side-effect assertions** (`"transition_strategy_verification" in rpc_names`, `errors[0]["code"] == error_code`) |
| 6 | `..._sync_scope_rejection_uses_validation_unexpected_fallback` (05) | ✅ | `403`, `ok is False`, `code == "VALIDATION_UNEXPECTED"` |
| 7 | `..._sync_scope_rejection_survives_rpc_failure` (05) | ✅ | `403`, `ok is False`, `code == "TRADE_SCOPE"` |
| 8 | `test_process_key.py::test_process_key_auth_missing_token` (06) | ✅✅ | `500` **and `r.json() == {…}` whole-dict literal** (`INTERNAL_TOKEN_UNCONFIGURED`) |
| 9 | `test_process_key.py::test_process_key_auth_wrong_token` (06) | ✅✅ | `401` **and `r.json() == {…}` whole-dict literal** (`UNAUTHENTICATED`) |
| 10 | `test_process_key.py::test_key_func_unverified_fallback_is_token_keyed` (06) | ✅ | all 3 original relational assertions kept verbatim **+ the new literal prefix** `ka.startswith("process_key:unverified:")` + `"aaa" not in ka` |
| 11 | `test_process_key.py::test_process_key_exempt_from_x_service_key_in_middleware` (06) | ⚠️ **weakest of the sample** | asserts **source substrings**, not behaviour: `"/process-key" in src`, `"/internal/" in src`, `"_gate_process_key" in src`. Literals, yes — but grep-level. See note below |
| 12 | `test_process_key_auth_order.py::test_authenticated_invalid_body_reaches_validation_and_422s` (08) | ✅ | `422`, `resp.json()["detail"] == "body.source: value_error"`, `code == "VALIDATION_FAILED"`, + all four `_FLAG_SUBSTRINGS` asserted **absent** |

**Sampled: 12 functions / 15 parametrised cases. Pin a literal: 12 of 12. Merely stop failing:
0 of 12.**

**The one caveat (#11).** `test_process_key_exempt_from_x_service_key_in_middleware` reads
`main.py`'s source text and asserts three substrings are present. It pins literals and it is a real
strengthening over the test it replaced (which asserted a contract that had become false while still
passing) — but a source-grep assertion can hold while the behaviour is broken: the string
`_gate_process_key` can be present in a branch that is never reached. It should eventually be driven
through the app instead. Named here rather than buried; **not** a phase-caused defect.

**One honest trap while auditing:** a naive `grep '^\s*assert'` on test #12 appears to show
`assert "SFOX_ENABLED" in resp.text` still live — which would mean plan 08's contract inversion never
landed. **It is inside the docstring**, quoted deliberately to record the BEFORE state. Read the
function body before believing a grep. (Same shape in #3, where a docstring sentence begins
"asserted:".)

### 6.3 Plan 07's 60 "casualties" are NOT rewrites — verified by diff, not by trust

Plan 07 reported 58 + 2 tests broken by consolidating onto one `Limiter`, claiming *"every assertion
in all 58 is verbatim unchanged — only the stub target moved."* Checked directly:

| File | Changed assertion lines in the whole-phase diff |
|---|---|
| `test_sfox_validate.py` | **0** |
| `test_routers_audit_2026_05_17.py` | **0** |
| `test_exchange_router_c0202.py` | **0** |
| `test_c19_portfolio_fixes.py` | **0** |
| `test_verify_strategy_redaction.py` | **0** |
| `test_status_contract_exchange_internal.py` | 70 — **all `+`, zero `-`**: net-new S-01..S-07 oracles from plans 03/04, not a rewrite |
| `test_mt5_validate.py` | 8 — **6 `+` / 2 `-`**: the documented plan-03 `503 → 500` rewrite (audit row #1), net strengthening |

**Plan 07's claim holds.** The stub-target relocation changed fixtures, not assertions.

### 6.4 The known vacuous test — verified against source, and a SECOND one found

**Confirmed as reported.** `analytics-service/tests/test_simulator_router.py:965`
`TestG15_004_LimiterIsCanonicalSingleton::test_main_app_state_limiter_is_same_singleton`:

- **Docstring claims:** *"The Limiter object on `app.state.limiter` (registered in main.py) is the
  SAME object the route decorator references."*
- **Body, in full:** `from services import rate_limit as rate_limit_module` /
  `from routers import simulator as simulator_router` /
  `assert simulator_router.limiter is rate_limit_module.limiter`
- It **never imports `main`** and **never touches `app.state`**. Its sibling three lines above,
  `test_simulator_limiter_is_singleton` (`:958`), has the **identical** assertion.

Pre-existing; the real invariant IS covered by plan 07's own
`test_limiter_identity.py::test_app_state_limiter_is_the_same_object_the_decorators_use`
(`import main; assert main.app.state.limiter is rl.limiter`). The misleading test still exists and
still has no owner.

**NEW — FINDING-13, a second misleading test at the same site.** Immediately below it,
`TestG15_005_RateLimitIsUserKeyed::test_route_uses_user_keyed_key_func_not_ip` (`:992`) asserts
only:

```python
assert hasattr(simulator_router, "_simulator_rate_limit_key")
key_func = simulator_router._simulator_rate_limit_key
assert key_func is not get_remote_address
```

But `routers/simulator.py:92` is:

```python
return f"simulator:ip:{get_remote_address(request)}"
```

The key func **wraps** `get_remote_address` rather than **being** it, so `is not` passes while the
route is fully IP-keyed. **The class name, the class docstring ("must be rate-limited per-user") and
the test name all assert a property that is false against source.** This is the test that would have
caught FINDING-10 and instead conceals it. Added to `140.1-TS-OBLIGATIONS.md` §8 as **FINDING-13**,
folded into TS-30 (repairing `simulator.py:92` must repair this test's name and claim in the same
commit).

---

## 7. The four known findings, stated plainly

The obligations artifact records these as TS-items. This section is their **plain-language
accounting**, so nobody has to read a 31-row table to learn what did not get delivered.

### 7.1 PYAPI-03 is PARTIALLY delivered. **The number is 9/9, not 10/10.**

**What landed, verified first-hand in this session:**

```
$ grep -rn "get_remote_address" analytics-service/routers/
optimizer.py:34   # <comment>
portfolio.py:55   # <comment>
portfolio.py:176  # <comment>
csv.py:21         # <comment>
exchange.py:41    # <comment>
simulator.py:23   # <docstring>
simulator.py:36   from slowapi.util import get_remote_address
simulator.py:52   # <comment>
simulator.py:92       return f"simulator:ip:{get_remote_address(request)}"
```

**Exactly one live call site remains, and it is `simulator.py:92`.** Every other hit is a comment or
docstring. Nine routes are off `get_remote_address`; zero private `Limiter()` instances remain
(AST-gated by `test_limiter_identity.py`).

**What did NOT land:** those nine routes key on **`platform:<route-path>`, not on tenant.**
`src/lib/analytics-client.ts` mints no `X-Tenant-Claim`, and that file belongs to Phase 140.2
(obligation **TS-04**). Only `/process-key` is genuinely per-tenant today, because
`src/lib/process-key-client.ts` mints the claim for it.

**Consequence, stated so it is not discovered in production:** the sharpest bucket is
**L-9 `/api/optimize-weights` at 20/minute, platform-wide.** Two allocators running Scenario
Composer concurrently can still 429 each other. The rename from "IP-keyed" to "platform-keyed" is
honest and it removes the Vercel-egress-NAT collapse, but it is **not** isolation. TS-04 flips all
nine to per-tenant with **zero Python changes**.

**Do not report PYAPI-03 as 10/10 closed.**

### 7.2 A TENTH IP-keyed route exists — `analytics-service/routers/simulator.py:92`

Plan 07's do-not-touch list describes `_simulator_rate_limit_key` as *"correctly user-keyed"*.
**That premise is FALSE against source** (verbatim source above). A PR#241 follow-up removed the
`X-User-Id` read (unsigned header ⇒ a caller could mint a fresh 20/hour bucket per request) and
moved the per-tenant quota **in-handler** to `_check_simulator_user_rate`, leaving the slowapi
decorator as an IP-keyed ceiling — which behind Railway's edge is **platform-wide 20/hour**.
The module docstring is stale in the same direction, still describing a header read that no longer
happens.

**Severity, honestly:** meaningfully **less** severe than L-9's was, because `/api/simulator` has a
real per-user tier underneath. The exposure is a ceiling one tenant can exhaust for everyone — not
first-mover starvation of the whole quota.

**Status: quarantined, not repaired.** `test_limiter_identity.py:91`
`IP_KEYED_QUARANTINE: frozenset[str] = frozenset({"simulator.py"})` with an **equality** assertion at
`:464` — so the exemption cannot silently grow **and** goes red the day someone fixes the route,
forcing the allow-list to shrink rather than rot.

**OPEN ITEM.** Owner: **needs its own plan** (`/gsd-phase --insert` → `/gsd-plan-phase`); recorded as
**TS-30**. It is not assignable to 140.2/140.3/145/146 as those are currently scoped. Its repair
must also fix FINDING-13's misleading test (§6.4).

### 7.3 KNOWN REGRESSION — plan 05 over-broadened a 403

`_scope_rejected` at **`analytics-service/routers/process_key.py:1295-1299`** (⚠️ the obligations
file recorded `:1245-1249`; plan 06 inserted ~50 lines above it — **corrected in the artifact by this
plan**) is a **three-arm OR behind a single return**:

```python
_scope_rejected = (
    not val.valid
    or val.read_only is False
    or val.error_code in {"TRADE_SCOPE", "WITHDRAW_SCOPE"}
)
if _scope_rejected:
    ...
```

PYAPI-10b needed the two **scope** arms to answer **403** instead of 200. Because all three arms
share one return, moving it also moved `not val.valid` — so **`AUTH_FAILED`, `PERMISSION_DENIED` and
a malformed CSV now answer 403 where 422 would be sharper.**

**Why it is a regression and not a bug:** 403 is not *wrong*. All three arms are CALLER-class and
breaker-inert, so nothing retries incorrectly and no circuit trips. It is **coarse**: a user whose
CSV is malformed is told *"you're not allowed"* when the truth is *"that file is broken"*.

**Sharper target state:** split into `403` for scope/authorization (`read_only is False`,
`TRADE_SCOPE`, `WITHDRAW_SCOPE`) and `422` for malformed input (`not val.valid`).

**Who should own it:** **NOBODY DOES.** TS-14 hands 140.2's consumers the *fact* (handle the envelope
by `ok`, not by status), which is enough to keep the UI correct — but the **split itself is
HOMELESS**. It is declared homeless in `140.1-TS-OBLIGATIONS.md` §6 rather than quietly assigned to a
phase that would not have it in scope.

### 7.4 The rest — FINDING-11, FINDING-12, the vacuous test, the mypy gate

| Item | Status |
|---|---|
| **FINDING-11** — test modules mounting a bare `FastAPI()`, so they never see the app-global 422/429 handlers | ⚠️ **The finding under-counted its own class.** It named **six**; the real count is **nine** (`test_correlation_middleware.py` also mounts a bare app but `include_router`s nothing — legitimately a middleware test, excluded). The three the finding missed: `test_status_contract_match_sim_portfolio.py:71`/`:333`, `test_maybe_single_none_guards.py:107`, `test_debug_key_flow_router.py:30`. **Corrected in the artifact by this plan.** Under-enumerating a class is the exact defect shape this programme exists to catch, so it is called out rather than silently patched. **PYAPI-07 is gated by exactly ONE file** (`test_validation_error_contract.py`, which drives the real `main.app`). **NEGATIVE half stands: none of the nine is vacuous today** — do NOT schedule a "fix the broken tests" sweep. **TS-31**, needs its own plan. |
| **FINDING-12** — two 429 shapes coexist | ✅ Confirmed against source: `routers/match.py:1742` and `routers/simulator.py:249` both raise a plain `HTTPException(status_code=429, detail="<string>")`, answering `{"detail": "…"}` while every slowapi 429 answers the `RATE_LIMITED` envelope. A 140.2 discriminator keying on `body.code` finds none on those two. Both pre-date 140.1. Registration order is irrelevant **by construction** — a plain `HTTPException` has no `RateLimitExceeded` in its MRO, so Starlette never routes it to the app-global handler. **TS-23**, shared 140.2 / 146. |
| **Vacuous `test_main_app_state_limiter_is_same_singleton`** | ✅ Verified against source in §6.4 — body is a verbatim copy of its sibling, never touches `app.state`. Real invariant IS covered elsewhere. Still exists, still unowned. Plus **FINDING-13**, a second misleading test 27 lines below it. |
| **`main.py` outside the mypy gate** | ✅ Verdict in §3: **NOT deliberate** — no config excludes it, and the CI comment claims to cover "ALL running-service code" while omitting the app entrypoint. Current standalone count **5**, down from **6** at baseline; the phase removed one and added none. Gate paths **not changed**, as instructed. |

---

## 8. Task 3 — the obligations artifact (produced in a prior session, verified and corrected here)

`.planning/phases/140.1-…/140.1-TS-OBLIGATIONS.md` — **243 lines, 31 obligations**
(140.2 → 16 · 140.3 → 4 · 146 → 3 · 145 → 2 · ops → 4 · needs-its-own-plan → 2).

**Acceptance criteria re-verified after the corrections below:**

```
test -f …/140.1-TS-OBLIGATIONS.md                     → PRESENT
grep -c '140\.2' …/140.1-TS-OBLIGATIONS.md            → 11   (required ≥ 7)
```

It was **not rewritten** (a wholesale `Write` of a 32 KB file would silently truncate at the output
cap). Three targeted `Edit`s were made, each correcting a **factual error** found by checking the
claim against source in this session:

| # | What was wrong | Correction |
|---|---|---|
| 1 | **TS-14** cited `routers/process_key.py:1245-1249` for `_scope_rejected` | → **`:1295-1299`**, with the drift cause noted inline (recorded from plan 05's tree; plan 06 inserted ~50 lines above it). The artifact's own header claims line numbers were re-verified on 2026-07-26; this one had been missed |
| 2 | **TS-31 / FINDING-11** said **six** modules mount a bare `FastAPI()` | → **nine**, with the three missed modules listed at file:line, and `test_correlation_middleware.py` explicitly excluded with the reason (mounts no router) |
| 3 | §8 findings table had no home for the three new findings | → added **FINDING-13** (second misleading simulator test → folded into TS-30), **FINDING-14** (`TEST_SUPABASE_DB_URL` never wired into the `python` job → 31 tests run nowhere), **FINDING-15** (`HAS_PY_ENV` set nowhere → 5 tests dormant), **FINDING-16** (binance cassettes never recorded → 4 tests dormant). 14–16 are declared **HOMELESS** — CI wiring / founder gate, no owning phase |

The measurements the plan required were taken in the prior session and are recorded in the artifact
§7; both are reproduced here so this SUMMARY stands alone:

- **PROD wedge blast radius: 0 rows.** Read-only against PROD `khslejtfbuezsmvmtsdn`;
  `sv_total = 20`, `sv_draft = 0`, `wedged_draft_no_job = 0`. Two anti-vacuity checks were done
  (that `'draft'` is a legal CHECK-constraint value, and that both sides of the `not exists` join
  have real rows: 12 `published` + 8 `validated`; `compute_jobs` 1222 `done` + 106 `failed_final`).
  **No backfill needed; the founder decision RESEARCH Q4 asked for is moot.**
- **Railway, ASSUMPTION-2 and -3 both CLOSED.** `FORWARDED_ALLOW_IPS` **NOT SET** (51 vars, none
  matching) → `get_remote_address` returns the edge IP, not a caller-controlled `X-Forwarded-For`
  entry. `quantalyze-analytics` `numReplicas = 1` → **N = 1, so NO N× limiter multiplier applies
  today** and the documented limits are the real limits. ⚠️ The moment anyone scales past one
  replica, every `memory://` limit becomes N× looser and TS-27's secret-capture throttle allows up to
  N captures per window. Nothing in the codebase detects that. Re-check before scaling and before
  quoting any limit as an SLO.

Also recorded while in PROD: the composite-index migration `20260726000225` has **not** reached PROD
(expected — unmerged branch). It auto-applies on merge to `main` and **is a production event**; plan
140.1-01's PROD-apply watch obligation stands.

---

## 9. Deviations from plan

### Auto-fixed issues

**None.** No gate was red, so no deviation rule 1/2/3 fix was triggered. **Zero source files were
changed by this plan.**

### Tooling substitutions (not code deviations)

**1. Supabase Management API instead of `psql` / MCP — Task 1(3)**
- **Found during:** Task 1.
- **Issue:** the plan's `<verify>` names `psql` or MCP. `psql` is not installed locally,
  `TEST_SUPABASE_DB_URL` is a GitHub secret, and no `mcp__supabase__*` tool is exposed to this agent
  (the known upstream bug that strips MCP tools from agents with a restricted tool set).
- **Resolution:** `POST https://api.supabase.com/v1/projects/qmnijlgmdhviwzwfyzlc/database/query`
  with the Supabase CLI's stored token — the same transport the MCP `execute_sql` tool uses, the same
  route plans 01 and 02 took. The Supabase CLI is linked to **PROD**, so `--linked` was deliberately
  **not** used and PROD was never contacted by these runs.
- **Added rigour:** because the API discards `RAISE NOTICE`, a pass and a no-op are
  indistinguishable — so the harness itself was falsified (§4) before either `[]` was believed.

**2. Full vitest suite instead of the single named file — Task 1(4)**
- **Found during:** Task 1.
- **Rationale:** plans 01–08 touched `src/lib/process-key-client.ts` (the shared seam choke point),
  and the repo lesson is that a shared-fixture diff requires BOTH suites in full. Ran
  `npx vitest run --no-file-parallelism`: 696 files / 8867 tests, 0 failures, 367s.

**3. M2, M6 and M8 re-run first-hand rather than cited — Task 2**
- **Found during:** Task 2.
- **Rationale:** all three already had explicit first-hand evidence in plans 02 and 05, so citing
  them would have satisfied the letter of the plan. They were re-run anyway, because the three are
  the second-member-of-class detectors and "the previous wave verified it" is the precise mechanism
  that produced this repair programme. Cost ~4 minutes; the results confirm the prior evidence and
  in M6's case are **louder** than recorded (4 red, not 1).

**4. Three factual corrections to the Task 3 artifact — Task 3**
- See §8. Each was a claim that failed when checked against source in this session. Corrective
  `Edit`s only; the artifact was never rewritten wholesale.

### Authentication gates

**None.** No auth gate was hit. The Supabase Management API token was already present in the login
keychain; no credential was minted, rotated or printed.

### Deferred issues

**None deferred by this plan.** Everything found is recorded in `140.1-TS-OBLIGATIONS.md` with an
owner or an explicit HOMELESS declaration. Per the plan's critical warning 4, **`TODOS.md` and all
other backlog/ledger files were left untouched** — routing obligations into the backlog is the
orchestrator's call, not this plan's.

## Known Stubs

**None.** This plan created no code and no UI surface.

## Threat Flags

**None.** No file created or modified by this plan introduces network endpoints, auth paths, file
access patterns or schema changes. The only two files touched are `.planning/` markdown artifacts.

The plan's own threat register is satisfied:

| Threat ID | Disposition | Evidence |
|---|---|---|
| T-140.1-25 — Repudiation: unproven oracles (self-referential pass) | **mitigated** | §5 ledger: 8/8 canonical + 7 supplementary, every row labelled first-hand / cited with a named oracle and a pasted failure count; 3 re-observed in this session |
| T-140.1-26 — Tampering: mutation residue in tree | **mitigated** | §5.4: `git diff` empty, `grep -rn MUTANT` empty (exit 1), 107 passed after revert |
| T-140.1-SC — Tampering: package installs | **accepted / honoured** | **Zero packages installed.** The documented `pandera==0.32.1` rescue was **not needed**: `python3 -c "import pandera"` → **`pandera 0.32.1`** already present, and the pytest run had **0 collection errors** — so `csv_validator`/`middleware`/`mt5` were fully exercised, not silently skipped |

---

## 10. Commits

**One commit belongs to this plan, and it landed in the prior session before it died:**

| Hash | Message | What it fixed |
|---|---|---|
| `56fb7167` | `chore(140.1-09): allowlist the two PYAPI no-echo canary fixtures in gitleaks` | A **red CI secret-scan gate** — the credential-safety canaries added by plans 07/08 contain synthetic key-shaped fixtures that gitleaks flagged. Narrow allowlist in `.gitleaks.toml`, not a scanner-wide suppression |

**No further commit was produced, and that is correct, not an omission:**

- This session changed **zero source files** (§9). `git diff` against `HEAD` is empty.
- The only two files written are `140.1-TS-OBLIGATIONS.md` and this SUMMARY, both under
  `.planning/`, which **is gitignored in this repo**. They are correctly uncommitted local files and
  were never staged. No `git add` of `.planning/` was attempted.
- `analytics-service/scripts/nautilus_factsheet.py` remains untracked and untouched — it is not this
  plan's work and was deliberately not added, committed, moved or deleted.
- No merge, no push, no PR. Branch unchanged: `feat/v1.16-production-resilience` @ `56fb7167`.

---

## 11. Self-Check

**Files claimed created/modified:**

| Path | Result |
|---|---|
| `.planning/phases/140.1-…/140.1-09-SUMMARY.md` | **FOUND** |
| `.planning/phases/140.1-…/140.1-TS-OBLIGATIONS.md` | **FOUND** (243 lines; `grep -c '140\.2'` = 11 ≥ 7) |

**Commit claimed:**

| Hash | Result |
|---|---|
| `56fb7167` | **FOUND** — `git log --oneline` HEAD |

**Gate claims, each backed by pasted output above:**

| Claim | Verified |
|---|---|
| pytest 4620 passed / 96 skipped / 0 failed, exit 0 | ✅ §1 |
| coverage 90.28% ≥ 80 floor | ✅ §1 |
| mypy --strict 0 errors on `services/ routers/ models/` | ✅ §1 |
| 0 new `# type: ignore` vs `43449cc6` | ✅ §1 |
| eslint 0 errors; tsc exit 0 | ✅ §1 |
| vitest 8867 passed / 0 failed | ✅ §1 |
| both new SQL gates green on TEST, + falsification proof | ✅ §4 |
| 96 skips enumerated to exactly 96, each adjudicated | ✅ §2 |
| M1–M8 ledger 8/8; M2/M6/M8 first-hand this session | ✅ §5 |
| tree clean (modulo the pre-existing untracked file); no MUTANT residue | ✅ §5.4 |
| TRAP-9 audit: 12 sampled, 12 pin a literal, 0 merely stop failing | ✅ §6 |
| the four known findings recorded honestly | ✅ §7 |

## Self-Check: PASSED

