---
phase: 140.1-pyapi-python-service-contract-status-attributability-limiter
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_process_key.py
  - supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql
autonomous: true
requirements: [PYAPI-01, PYAPI-09]
must_haves:
  truths:
    - "Neither strategy_verifications read in /process-key can return another tenant's row: both sites filter on strategy_id AND wizard_session_id, and ownership of strategy_id is asserted first"
    - "A replay of a wedged session (SV row exists, no compute job) enqueues the job and answers with a truthful queued value — no state exists from which the client is told to retry forever"
    - "No flow that never sends wizard_session_id (resync via keys/sync) can ever receive WIZARD_DUPLICATE"
  artifacts:
    - path: "analytics-service/routers/process_key.py"
      provides: "Pre-check moved below the strategy_id branch, scoped reads, ownership assert, enqueue-aware duplicate path, job_state field"
    - path: "supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql"
      provides: "Regression fence pinning the shared RPC dedupe the fix depends on"
  key_links:
    - from: "process_key.py duplicate path"
      to: "enqueue_compute_job RPC"
      via: "supabase.rpc on the idempotent-hit arm"
      pattern: "enqueue_compute_job"
---

<objective>
Close the Python half of PYAPI-01 (both unscoped `strategy_verifications` reads) and all of PYAPI-09
(idempotency completion): move the duplicate pre-check below the `strategy_id` branch, scope BOTH
read sites, assert ownership, make the duplicate path enqueue-aware so `queued` is truthful, and pin
the shared RPC dedupe this fix now depends on.

Purpose: today a foreign collision returns another tenant's verification_id/status/trust_tier, and a
plain double-submit wedges a session into `WIZARD_DUPLICATE, queued:false` forever (C-19/C-20) —
surfaced to the user as success.
Output: restructured `/process-key` handler + pytest oracles PYAPI-01d/01e/09a/09c/09d + SQL fence 09b.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-CONTEXT.md
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-RESEARCH.md  (Q3 "The correct shape" + Q4 in full)
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-PATTERNS.md  (Class 3, Class 4, "Enqueue-aware pre-check" no-analog note)
</context>

<critical_warnings>
1. **TRAP-5 — the class has TWO read sites and TWO return sites, not one.** Read sites:
   `routers/process_key.py:722-728` (pre-check) AND `:895-901` (23505 race-winner re-fetch). Return
   sites: `:747-755` AND `:910-918`. A fix that closes only the pre-check leaves the MORE reachable
   site open — once the constraint is tenant-scoped, the 23505 arm is the one that fires on a
   foreign collision. Count: 2 read sites, 2 return sites. All four change.
2. **Ordering: the pre-check must MOVE below the `strategy_id is None` branch before it can be
   scoped at all.** Today it runs at `:721`, before `strategy_id` is read at `:768` — there is
   nothing to scope by at that point. Skip the pre-check entirely when `strategy_id` is None (those
   arms — validate-only `:771`, csv-finalize delegate `:815` — write no SV row). This ONE move also
   un-shadows the csv-finalize branch (closes C-20).
3. **Scope fence:** Phase 145 owns csv-finalize's transaction shape (`finalize_csv_strategy` /
   persist / after() atomicity). This plan changes ONLY control flow and the reply contract inside
   `process_key.py`. Do not touch the RPCs or `csv-finalize/route.ts`.
4. **`scoped read` is necessary, not sufficient — `strategy_id` is caller-supplied.** The ownership
   assert (strategies lookup filtered on id AND user_id, 403 on miss, for non-teaser flows) is the
   second layer. A user-scoped Supabase client is NOT available on onboard/resync today (only csv
   forwards X-User-Access-Token) — use the explicit Python filter; the header-forwarding fix is a
   recorded 140.2 obligation, not this plan's work.
5. **No new SQL is required for PYAPI-09.** `_enqueue_compute_job_internal`
   (`migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql:181+`) already dedupes on
   (target, kind) over non-terminal statuses. Re-calling it on the duplicate path is safe — but PIN
   that behaviour with the SQL fence (Task 3) because it is a shared RPC with many callers.
6. **Gate the re-enqueue on the SV row's status being non-terminal (`draft`)** — a
   legitimately-completed session must not silently re-sync. Do NOT pass p_idempotency_key (cheap
   tightening, out of scope — flag only).
7. **TS readers of `queued`/`WIZARD_DUPLICATE` will see a changed contract**:
   `keys/sync/route.ts:438` (branches on queued===false && code==='WIZARD_DUPLICATE') and
   `finalize-wizard/route.ts:1419-1420` (documents {queued:true, code:WIZARD_DUPLICATE} as illegal).
   These are 140.2's files. Record both file:line obligations in the SUMMARY; do NOT edit them.
   This is safe pre-merge: nothing ships to main until 140.2/140.3 land on the same branch.
</critical_warnings>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move + scope the pre-check, add the ownership assert (PYAPI-01d/01e)</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_process_key.py</files>
  <read_first>
    - analytics-service/routers/process_key.py:700-950 (the handler region: pre-check :721-755,
      strategy_id resolution :768, validate-only :771, csv branch :783-850, SV INSERT :868-882,
      23505 handler :889-918, enqueue :923-941)
    - analytics-service/services/db.py:71-76 (the service-role client both reads use)
    - analytics-service/tests/test_process_key.py:477-560 (wiz-race fixtures — the mocking idiom to
      extend for recording filter sets)
  </read_first>
  <behavior>
    - PYAPI-01d: with the supabase client mocked to record filter chains, BOTH read sites (pre-check
      and 23505 re-fetch) issue .eq("strategy_id", …) alongside .eq("wizard_session_id", …). Assert
      on the RECORDED filter set — never on a helper the handler exports. Two separate cases, one per
      site (this is the M2 mutation detector).
    - PYAPI-01e: context.user_id not owning strategy_id ⇒ 403, and NO strategy_verifications read is
      issued (assert the mock recorded zero SV selects).
    - Positive control: owner + matching wizard_session_id still gets the idempotent reply.
  </behavior>
  <action>
    Relocate the duplicate pre-check block from :721 to after strategy_id resolution and after the
    strategy_id-is-None arms (validate-only, csv-finalize delegation), so it runs only when a
    long-fetch flow has a concrete strategy_id. Add .eq("strategy_id", strategy_id) to both read
    sites. Before the pre-check, for non-teaser flows, assert ownership: select from strategies
    filtered on id AND context.user_id via the service-role client; on miss return the existing
    envelope shape as a 403 JSONResponse (match the :804-812 wrapping idiom). Teaser flow
    (TEASER_ANCHOR_STRATEGY_ID, fresh uuid4 per call at :635) bypasses the ownership assert — it has
    no tenant. Keep both return sites' field lists unchanged in this task (shape work is Task 2 and
    Plan 140.1-05).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key.py -q -k "scope or ownership or wiz_race" && python3 -m mypy --strict --follow-imports=silent routers/process_key.py</automated>
  </verify>
  <acceptance_criteria>
    - grep of process_key.py shows exactly 2 `.eq("wizard_session_id"` read sites and BOTH are
      immediately chained with `.eq("strategy_id"` — verified by `grep -n 'eq("wizard_session_id"'
      analytics-service/routers/process_key.py` returning 2 lines, each whose statement also contains
      strategy_id (executor pastes both statements into the SUMMARY).
    - The pre-check no longer executes before the strategy_id-is-None arms. The DURABLE proof is
      PYAPI-09c in Task 2 (the csv double-submit fixture reaches the csv-finalize branch); within
      this task the recorded mock call order is sufficient interim evidence. Do NOT write a
      temporary control-flow test — Task 2 would delete it and it proves nothing PYAPI-09c doesn't.
    - PYAPI-01d passes and is RED if either site's .eq("strategy_id") is removed (run mutation M2:
      drop it from the race-winner site ONLY, confirm RED, revert — record in SUMMARY).
    - PYAPI-01e passes: 403 + zero SV reads on non-owner.
    - mypy --strict clean on the touched files; no `# type: ignore` added (use cast()).
  </acceptance_criteria>
  <done>Both read sites scoped, ownership asserted, pre-check relocated; oracles PYAPI-01d/01e green and mutation-checked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enqueue-aware duplicate path + truthful queued/job_state (PYAPI-09a/09c/09d)</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_process_key.py</files>
  <read_first>
    - analytics-service/routers/process_key.py:747-755, :895-941 (both WIZARD_DUPLICATE emitters +
      the enqueue call at :923-935)
    - .planning/phases/140.1-*/140.1-RESEARCH.md Q4 "The three candidate mechanisms" (#1 is the
      chosen shape) and "The reply contract itself" (the three-row queued/job_state table)
  </read_first>
  <behavior>
    - PYAPI-09a (economic): SV row exists in `draft`, NO compute_jobs row; re-POST the same session
      ⇒ reply carries queued:true AND a compute_jobs row now exists (assert the job's existence via
      the recorded RPC call, not the reply field — this is what defeats mutation M6).
    - PYAPI-09c (control flow): plain double-submit on a csv/finalize session with strategy_id
      absent ⇒ the second call reaches the csv-finalize branch, NOT the duplicate short-circuit.
    - PYAPI-09d: resync (keys/sync sends no wizard_session_id; a fresh uuid4 is minted at :720) ⇒
      WIZARD_DUPLICATE is never emitted. Assert the branch's absence for that flow, not its behaviour.
    - Duplicate with a LIVE job ⇒ queued:true, job_state "running" (no new enqueue side effect
      beyond the idempotent RPC call).
  </behavior>
  <action>
    On an idempotent hit for a long-fetch flow whose SV row status is non-terminal (draft): call
    enqueue_compute_job (idempotent per the shared RPC's dedupe) and derive the reply from the RPC
    outcome — queued reflects "a non-terminal compute_job exists for this verification at the moment
    of reply". Add job_state to BOTH duplicate emitters (:747-755 and :910-918) with the three
    literal values not_applicable / running / enqueued per RESEARCH Q4's table; synchronous flows'
    replies gain job_state:"not_applicable" only where the duplicate path is concerned (full 200-shape
    unification is Plan 140.1-05 — do not add `ok` here). If the prior job reached terminal status
    (done/failed_final), do NOT re-enqueue — return the idempotent reply with the terminal state
    visible. Delete or make reachable the dead WIZARD_DUPLICATE consumer path for resync per
    PYAPI-09d (the Python side: guarantee no wizard_session_id-less flow can emit it).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key.py -q && python3 -m mypy --strict --follow-imports=silent routers/ services/ models/</automated>
  </verify>
  <acceptance_criteria>
    - PYAPI-09a green, and mutation M6 (return literal queued:True without enqueuing) turns it RED —
      run, confirm, revert, record.
    - PYAPI-09c green — proves the C-20 dead end is closed by the Task 1 move.
    - PYAPI-09d green — no WIZARD_DUPLICATE reachable for resync.
    - BOTH emitters (:747-755 region and :910-918 region) carry job_state — proven by TESTS, not
      grep: one fixture reaches the pre-check emitter and one reaches the 23505 race-winner emitter,
      EACH asserting job_state present in the reply body with a literal expected value. (`grep -c`
      counts lines and "return-dict contexts" is prose — not mechanizable; the two duplicate-path
      fixtures ARE the gate. Paste both emitters' final code into the SUMMARY.)
    - job_state values in tests are string LITERALS ("running"/"enqueued"/"not_applicable"), not
      constants imported from the module under test.
    - SUMMARY records the two TS reader obligations (keys/sync/route.ts:438,
      finalize-wizard/route.ts:1419-1420) for 140.2, plus the p_idempotency_key flag (not done).
  </acceptance_criteria>
  <done>Duplicate path is enqueue-aware and truthful; the wedge state is unreachable going forward; TS obligations recorded.</done>
</task>

<task type="auto">
  <name>Task 3: SQL regression fence on the shared RPC dedupe (PYAPI-09b)</name>
  <files>supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql</files>
  <read_first>
    - supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql:181+ (the
      _enqueue_compute_job_internal dedupe: select-existing over non-terminal statuses, ON CONFLICT
      DO NOTHING + re-select)
    - supabase/tests/test_strategies_private_owner_isolation.sql (structure analog: pre-clean,
      BEGIN…ROLLBACK, fixture-scoped counts)
  </read_first>
  <action>
    Write a PL/pgSQL gate: two consecutive enqueue_compute_job calls for the same (strategy, kind)
    while the first row is non-terminal produce exactly ONE compute_jobs row (fixture-scoped count
    = 1, literal); a third call AFTER transitioning the row to a terminal status produces a second
    row (pins the "misses on terminal" behaviour the Task 2 gate relies on). This passes today —
    it is a regression fence, not a RED-first test: the Task 2 mechanism depends on this shared
    RPC's semantics staying fixed, and the RPC has many other callers.
  </action>
  <verify>
    <automated>psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_enqueue_compute_job_dedupe_non_terminal.sql  # (fallback: MCP execute_sql of the same assertions in a rolled-back txn)</automated>
  </verify>
  <acceptance_criteria>
    - File auto-discoverable under supabase/tests/test_*.sql; runs green on TEST.
    - Both behaviours pinned: dedupe-while-non-terminal (count=1) and new-row-after-terminal (count=2).
    - Counts are fixture-scoped literals; BEGIN…ROLLBACK hygiene per the analog.
  </acceptance_criteria>
  <done>Shared-RPC dedupe fenced in CI; PYAPI-09's mechanism cannot be silently invalidated by a future RPC change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| caller→/process-key body | strategy_id and wizard_session_id are caller-supplied |
| service-role client→DB | reads bypass RLS entirely |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1-04 | Information Disclosure | both SV read sites | mitigate | .eq(strategy_id) on both + ownership 403 gate (PYAPI-01d/01e) |
| T-140.1-05 | Elevation of Privilege | caller-supplied strategy_id | mitigate | strategies id+user_id ownership assert before any SV read |
| T-140.1-06 | Denial of Service | permanent WIZARD_DUPLICATE wedge | mitigate | enqueue-aware duplicate path (PYAPI-09a) |
| T-140.1-SC | Tampering | package installs | accept | no packages installed in this plan |
</threat_model>

<verification>
- Full analytics-service pytest green; mypy --strict clean.
- Mutations M2 and M6 each turn their named oracle RED (run + reverted, recorded).
</verification>

<success_criteria>
Phase success criteria 1 (query half) and 5: no duplicate pre-check can return a foreign row; no
replay returns "duplicate" for work never enqueued; no retry-forever state exists.
</success_criteria>

<output>
Create `.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-02-SUMMARY.md` when done.
Record: mutation M2/M6 evidence, the two TS reader obligations, the p_idempotency_key flag.
</output>
