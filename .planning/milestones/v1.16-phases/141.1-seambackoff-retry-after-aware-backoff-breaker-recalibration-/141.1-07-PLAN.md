---
phase: 141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration
plan: 07
type: execute
wave: 3
depends_on: ["141.1-04"]
files_modified:
  - src/lib/resilient-fetch.wiring.test.ts
  - src/lib/seam-constants.pin.test.ts
  - src/lib/seam-budgets.invariant.test.ts
autonomous: true
requirements: [SEAM-06]
tags: [census, wiring, anti-vacuity, sc4b, accounting, instance-not-class]

must_haves:
  truths:
    - "D-09: the call-site census covers the RETRY axis — a discovered site with no retriesOverride ('<ABSENT>') or a drifted retry expression reddens the existing set-equality; the axis 141 left uncensused is closed by EXTENDING EXPECTED_BINDINGS, not by a parallel file"
    - "D-14 (D-14a): every `it.each(Object.keys(...))` over a map under test on the seam surface is fenced with a hand-typed length pin — ALL class-γ members (seam-constants.pin.test.ts:339 + the two SEAM_EXCLUSIONS sites in seam-budgets.invariant.test.ts:817/:856), not just the named instance"
    - "D-14 (D-14b): SEAM_RETRY_BACKOFF_MS (250), SEAM_RETRY_JITTER_MAX_MS (250) and their sum (500) carry literal pins like every sibling constant"
    - "D-15: SC-4b charges the store term PER LEG with (1 + retries) — keys/sync failing === 56,000ms (hand-typed) and finalize-wizard composite UNCHANGED at 277,500ms (hand-typed); STORE_COMMANDS_PER_SEAM_CALL.failing stays 3, documented as per-attempt"
  artifacts:
    - path: "src/lib/resilient-fetch.wiring.test.ts"
      provides: "DiscoveredBinding.retry field + encoder + roster retry expressions + no-'<ABSENT>' assertion"
    - path: "src/lib/seam-constants.pin.test.ts"
      provides: "map-length fences + retry-constant literal pins"
    - path: "src/lib/seam-budgets.invariant.test.ts"
      provides: "per-leg storeMs with (1+retries), hand-typed 56,000 / 277,500 assertions, corrected docblocks + header preamble, SEAM_EXCLUSIONS length fences"
  key_links:
    - from: "src/lib/resilient-fetch.wiring.test.ts encodeBinding"
      to: "DiscoveredBinding.retry"
      via: "encoder concatenation — existing set-equality reddens on drift with no new assertion"
      pattern: "retry"
    - from: "src/lib/seam-budgets.invariant.test.ts storeMs"
      to: "SEAM_BUDGETS[b.key].retries"
      via: "per-leg (1 + retries) factor gated on STATE_SPENDS_REQUEST_BUDGET"
      pattern: "1 \\+ .*retries"
---

<objective>
Close the instance-not-class findings on the guard surface. D-09 is THE structural finding of the
campaign: 141 added a second classification axis (retry) with no census, even though the census
pattern existed in-repo for exactly this purpose. Extend it — a field, not a file. Then fence the
vacuous `it.each(Object.keys(...))` class (ALL three members, enumerated by grep), pin the two
unpinned retry constants, and recharge SC-4b's store term per retried leg.

Purpose: discharges D-09 (C2), D-14a (D4), D-14b (D3), D-15 (E1). SC-F, SC-J, SC-K, SC-L.
Output: the extended census, the anti-vacuity fences, the corrected accounting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-CONTEXT.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-RESEARCH.md
@.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-PATTERNS.md

⚠️ Runs AFTER 141.1-04 (D-08): every production call site now passes `retriesOverride`
explicitly, so the census's no-'<ABSENT>' assertion holds at the final tree.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: D-09 — retry axis on the existing census (SC-F)</name>
  <files>src/lib/resilient-fetch.wiring.test.ts</files>
  <read_first>
    - src/lib/resilient-fetch.wiring.test.ts — `DiscoveredBinding` + `encodeBinding` (~:545-561), the three discovery families (~:568-665, esp. `discoverLiteralCallSites` and `firstLiteralArg`'s sentinel idiom), `EXPECTED_BINDINGS` (~:678-713), `EXPECTED_SEAM_CALL_FILES` docblock (~:715-723 — it NAMES the 'discovered by nothing' property the retry axis has), the four assertions incl. the `>= 14` vacuity fence (~:757-811) and the 'Never widen this assertion' message register (~:779-787)
    - src/lib/analytics-client.ts (~:434) and src/lib/process-key-client.ts (~:453) — the two chokepoint retry expressions the roster pins as literals
    - 141.1-RESEARCH.md § "D-09 — extending EXPECTED_BINDINGS, not replacing it" — the 5-step extension
  </read_first>
  <behavior>
    - Every discovered binding carries a `retry` string: the normalised source expression passed as `retriesOverride` at that site, or the sentinel `"<ABSENT>"`.
    - The existing set-equality reddens on any retry-expression drift (encoder carries the field).
    - An explicit loud assertion: no discovered binding is `"<ABSENT>"`.
    - The vacuity floor still holds (raise the `>= 14` literal if discovery row count grew).
  </behavior>
  <action>
    Add `retry: string` to `DiscoveredBinding` with a docblock naming Phase 141.1 / D-09 (the
    second classification axis, censused so a new call site cannot be discovered by nothing on
    the retry dimension). Append it in `encodeBinding`. In `discoverLiteralCallSites`, after the
    `resilientFetch("key",` match, scan the init text for `retriesOverride:` and capture the
    expression (comments already stripped by `readCode`, so a commented-out override cannot fool
    it); use `"<ABSENT>"` when missing, matching the existing `"<NON-LITERAL PATH>"` sentinel
    idiom. For families (i)/(ii) — the two client chokepoints — pin the two registry-lookup
    expressions as literals: `RETRY_SAFE_ANALYTICS[options.budgetKey]?.retries ?? 0` and
    `RETRY_SAFE_FLOW_TYPES[args.flow_type]?.retries ?? 0`. Extend every `EXPECTED_BINDINGS` entry
    with its per-site retry expression (route sites: `0`).

    Add ONE new assertion — no `"<ABSENT>"` — whose message quotes the EXPECTED_SEAM_CALL_FILES
    docblock's 'discovered by nothing' phrase and notes tsc also enforces this post-D-08; the
    census is the belt that survives an `as any` or a JS caller (mirroring the runtime validator
    vs the compile-time type). Match the fail-loud register: name the production meaning, name
    the remedy, end with never-widen. Do NOT create a new census file (17 unrewired stripComments
    copies exist; a parallel census is the exact defect this phase repairs).
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.wiring.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-F a): delete `retriesOverride: ...` from the `analyticsRequest` init in analytics-client.ts → the census set-equality REDs showing `<ABSENT>` (run ONLY the wiring test — vitest's esbuild ignores the tsc error, proving the census works below the type system); revert → GREEN. Paste the failing diff line.
    - MUTATION (SC-F b): change the process-key chokepoint's expression (e.g. swap in `?? 1`) → set-equality REDs on the drifted expression; revert → GREEN.
    - The `>= N` vacuity floor reflects the post-extension discovery count (raise it if discovery grew; never lower).
    - Zero new files created (`git status` shows only the wiring test modified).
  </acceptance_criteria>
  <done>The retry axis is censused inside the existing mechanism; absence and drift both redden below the type system.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-14a class-γ fences (ALL three members) + D-14b literal pins (SC-J, SC-K)</name>
  <files>src/lib/seam-constants.pin.test.ts, src/lib/seam-budgets.invariant.test.ts</files>
  <read_first>
    - src/lib/seam-constants.pin.test.ts — the defect at ~:339 (`it.each(Object.keys(RETRY_SAFE_ANALYTICS))`), the file's OWN correct rule at ~:83-89 (`EXPECTED_TIMEOUT_MS`: iterate a hand-typed map, never the table's keys), sibling literal pins (~:384-506) and the derived-sum precedent (~:509-519)
    - src/lib/seam-budgets.invariant.test.ts — the other two class-γ members: `it.each(Object.keys(SEAM_EXCLUSIONS))` at ~:817 and ~:856
    - Class-γ enumeration (pre-verified at plan time): `grep -rn "it.each(Object.keys(" src/ --include="*.ts"` → exactly 3 hits: seam-constants.pin.test.ts:339, seam-budgets.invariant.test.ts:817, :856. Re-run the grep to confirm before declaring the class closed.
  </read_first>
  <behavior>
    - SC-J: emptying `RETRY_SAFE_ANALYTICS` reddens (`.toBe(4)` hand-typed); same fences for `RETRY_SAFE_FLOW_TYPES` (2), `RETRY_AUDIT_NO_FLOW_TYPES` (2), `RETRY_AUDIT_NO_ANALYTICS` (5).
    - Class-γ closure: emptying `SEAM_EXCLUSIONS` reddens a hand-typed length fence beside each of its two `it.each` sites (one shared fence in the file is sufficient if both iterate the same map).
    - SC-K: `SEAM_RETRY_BACKOFF_MS` === 250, `SEAM_RETRY_JITTER_MAX_MS` === 250, and the sum === 500 (SC-4b charges the max) — all hand-typed literals with sibling-style messages.
  </behavior>
  <action>
    In `seam-constants.pin.test.ts`: beside the ~:339 `it.each`, add hand-typed length fences for
    all four registry maps (SC-J's literal form; alternatively restructure to a hand-typed key
    list per the file's own :83-89 discipline if the diff stays comparable — prefer whichever is
    smaller). Add the three SC-K literal pins in the sibling style (both-directions message:
    what raising/lowering means for SC-4b's charged max).

    In `seam-budgets.invariant.test.ts`: add a hand-typed `Object.keys(SEAM_EXCLUSIONS).length`
    fence (read the map to hand-count its current size; type the literal) with the "BLIND, not
    satisfied" message register, closing the remaining class-γ members. Re-run the class grep and
    record the closure (3/3) in the SUMMARY — fixing only the named instance would repeat the
    exact miss this phase exists to repair.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-constants.pin.test.ts src/lib/seam-budgets.invariant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-J): comment out one `RETRY_SAFE_ANALYTICS` entry → the `.toBe(4)` fence is RED (while the `it.each` alone would have shrunk silently); revert → GREEN. Paste the failing assertion.
    - MUTATION (class-γ member 2/3): comment out one `SEAM_EXCLUSIONS` entry → the new fence is RED; revert → GREEN.
    - MUTATION (SC-K): change `SEAM_RETRY_BACKOFF_MS` to 300 → the literal pin AND the sum pin are RED; revert → GREEN.
    - `grep -rn "it.each(Object.keys(" src/ --include="*.ts"` output in the SUMMARY with each hit either fenced or justified — the class is closed by enumeration, not by the named instance.
  </acceptance_criteria>
  <done>All three class-γ members fenced; both retry constants and their sum pinned; class closure evidenced by grep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: D-15 — per-leg store recharge + hand-typed worst cases (SC-L)</name>
  <files>src/lib/seam-budgets.invariant.test.ts</files>
  <read_first>
    - src/lib/seam-budgets.invariant.test.ts — the SC-4b arithmetic (~:631-693): the `requestMs` leg-reduce with its existing per-leg `(1 + retries)` (the analog three lines up), the flat `storeMs` (~:650-657, the defect), `STORE_COMMANDS_PER_SEAM_CALL` + its "THREE IS A CEILING" docblock (~:282-312), `STORE_COMMAND_WORST_CASE_MS` (4250), `STATE_SPENDS_REQUEST_BUDGET`, the header preamble at ~:81 ("with every row at retries: 0" — stale) and its table, the anti-vacuity fence (~:697-717)
    - 141.1-RESEARCH.md § "D-15 — SC-4b, the corrected arithmetic (numbers verified)" — the exact replacement reduce, the per-attempt command table, and the verified route figures
  </read_first>
  <behavior>
    - storeMs is computed INSIDE the leg reduce: per leg, `STORE_COMMANDS_PER_SEAM_CALL[state] * (1 + (STATE_SPENDS_REQUEST_BUDGET[state] ? SEAM_BUDGETS[b.key].retries : 0)) * STORE_COMMAND_WORST_CASE_MS * b.calls`.
    - New hand-typed assertions: keys/sync total in the `failing` state === 56_000 (literal, NOT derived from the formula — self-referential oracles are this repo's documented money-math failure mode); finalize-wizard composite in `failing` === 277_500 (literal — catches both the flat-6 and the per-route shortcut, which would show 405_000 / a phantom breach).
    - `STORE_COMMANDS_PER_SEAM_CALL.failing` stays 3; the ceiling assertion for every route still passes (no ceiling breached — accounting correction, not a limit change).
  </behavior>
  <action>
    Move the store term into the same reduce as `requestMs`, gated on
    `STATE_SPENDS_REQUEST_BUDGET[state]` for the retry factor (in the `open` state the call
    throws before fetch — no second round). Copy the "Zero when retries=0, so every non-flipped
    leg's term vanishes exactly as before" comment register. Do NOT apply a route-level
    multiplier (retries is a leg property; multi-leg branches have no single value) and do NOT
    change `failing` to 6 (double-charges non-retried legs; false-REDs finalize-wizard composite
    at 405,000ms).

    Update the `STORE_COMMANDS_PER_SEAM_CALL` docblock: document the 3 as PER-ATTEMPT, and add
    the sentence the file owes itself — its own warning said "a future edit that adds a store
    round trip has to come back here"; 141 was that edit, and this (1+retries) factor is the
    return visit. Fix the ~:81 preamble ("with every row at retries: 0" → five rows at 1) and add
    the retried worst case (keys/sync, 56,000) to the header table so a reader sees a retried
    number. Add the two hand-typed assertions beside the existing anti-vacuity fence, message in
    the ~:683-692 register (name the branch, both terms, the ceiling, the legal remedies, never
    widen).
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-budgets.invariant.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION (SC-L): delete the `(1 + ...retries...)` factor from the new storeMs term → the 56_000 assertion is RED (computes 43,250); revert → GREEN. Paste the failing assertion.
    - MUTATION (anti-shortcut): change `failing` from 3 to 6 → the 277_500 assertion is RED (405,000); revert → GREEN — proving the false-RED trap is fenced.
    - All existing route-ceiling assertions stay GREEN (no ceiling breached at the corrected numbers).
    - `grep -c "56_000\|56000" src/lib/seam-budgets.invariant.test.ts` ≥ 1 as a hand-typed literal in an expect (not derived).
    - The ~:81 preamble no longer claims every row is 0 (`grep -c "every row at .retries: 0." src/lib/seam-budgets.invariant.test.ts` → 0).
  </acceptance_criteria>
  <done>SC-4b charges the true retried worst case per leg; both wrong-fix traps (flat 6, per-route) are fenced by hand-typed literals; prose matches the arithmetic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build/test tier → production wiring | the census and SC-4b certify properties of production source from disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.1-16 | Elevation (unaudited retry site) | census retry axis | mitigate | `<ABSENT>` sentinel + set-equality — a new site without a verdict expression reddens even under `as any` |
| T-141.1-17 | DoS (headroom under-charge) | SC-4b store arithmetic | mitigate | per-leg (1+retries) recharge; hand-typed 56,000 / 277,500 oracles fence both wrong-fix directions |
| T-141.1-18 | Tampering (vacuous guard) | it.each(Object.keys) class | mitigate | hand-typed length fences on all 3 enumerated members; grep evidence in SUMMARY |
| T-141.1-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/lib/resilient-fetch.wiring.test.ts src/lib/seam-constants.pin.test.ts src/lib/seam-budgets.invariant.test.ts` — green
- `npm run typecheck` — 0 errors
- All named mutations observed RED then reverted GREEN (evidence pasted)
- Class-γ grep output recorded with 3/3 members fenced
</verification>

<success_criteria>
- SC-F, SC-J, SC-K, SC-L all Observed
- The census extension reddens on absence AND drift with no parallel mechanism created
- SC-4b's corrected numbers match the review's cited figures (56,000 / unchanged 277,500)
</success_criteria>

<output>
Create `.planning/phases/141.1-seambackoff-retry-after-aware-backoff-breaker-recalibration-/141.1-07-SUMMARY.md` when done. Include mutation evidence and the class-γ closure grep.
</output>
