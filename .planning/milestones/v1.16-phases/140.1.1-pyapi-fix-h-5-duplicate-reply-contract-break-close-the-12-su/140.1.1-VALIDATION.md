---
phase: 140.1.1
slug: pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su
status: planned
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-26
planned: 2026-07-26
---

# Phase 140.1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> The falsifiability doctrine from RESEARCH `## Validation Architecture` governs:
> **every oracle names its source; literals, never values read back from the code under test.**
> 140.1's 19 test files had ZERO self-referential oracles and two document refusing them — do not regress.
> M-15 (`tests/test_process_key_200_discriminator.py:416-424`, `len(_SHAPES) == 6` against a list
> literal in the same file) is the counter-example this phase must delete (plan 06 Task 3 deletes it).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (`analytics-service/pytest.ini`, testpaths=tests) · vitest (the ONE cross-boundary contract test) · mypy --strict (CI gate) |
| **Config file** | `analytics-service/pytest.ini`, `vitest.config.ts` |
| **Quick run command** | `cd analytics-service && python3 -m pytest tests/<plan's named file> -q` |
| **Full suite command** | `cd analytics-service && python3 -m pytest -q && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` |
| **Cross-boundary command** | `npx vitest run tests/lib/process-key-onboard-contract-parity.test.ts` (runs in the EXISTING frontend-test CI job via the `tests/lib/**` glob — no new `needs:` edge) |
| **Estimated runtime** | quick <60s; full ~5 min |

⚠️ **`mypy --strict` is a PR-CI gate that the pytest run does NOT execute** — run it per plan.
`cast()` only; **`# type: ignore` is banned**. 140.1 ended with 0 new ignores — keep it.
⚠️ **pandera 0.32.1 IS installed in this checkout** (research C-5, verified: collection clean,
4714 tests, 2.97s). CONTEXT.md's "local reds are a missing pandera dep" constraint is **stale** —
do not plan skips around it. If reds appear, treat them as real until proven environmental.
⚠️ **slowapi skew:** local **0.1.9** vs `analytics-service/requirements.txt:226` pin **0.1.10**.
Plan 07 Task 1 syncs the env to 0.1.10 BEFORE any #3/#4/#5 mutation cycle and records the version.

---

## Sampling Rate

- **After every task commit:** quick run (the plan's named test file) + `mypy --strict` on touched packages
- **After every wave:** full analytics-service suite + (wave 2+) the vitest contract test — encoded in plan 02 Task 3 (wave 1), plan 04 Task 3 (wave 2), plan 06 Task 3 (wave 3), plan 07 Task 3 (wave 4 — the phase-wide gate)
- **Before `/gsd:verify-work`:** full suite + mypy + vitest green, **and** the mutation re-run protocol below executed with RED observed first-hand
- **Max feedback latency:** < 5 min

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 01-T1 | 01 | 1 | PYAPIFIX-06 | T-140.1.1-02/03 | 429 arm + 500-dep membership + 503 retry_after source guards | unit (RED-first) | `pytest tests/test_error_contract_429.py tests/test_error_contract_500_dependency.py tests/test_error_contract_retry_after_source.py -q` | new | ⬜ pending |
| 01-T2 | 01 | 1 | PYAPIFIX-04 | T-140.1.1-01 | scalar-detail ValueError at construction | unit (RED-first) | `pytest tests/test_error_contract_scalar_detail.py -q` + mypy | new | ⬜ pending |
| 01-T3 | 01 | 1 | PYAPIFIX-06 | — | doc/code reconciliation (C-21) | full suite | `pytest -q` + mypy | existing | ⬜ pending |
| 02-T1 | 02 | 1 | PYAPIFIX-02 | T-140.1.1-05 | ONE permanent-code allow-list, long_fetch reads it | unit | `pytest tests/ -k long_fetch -q` + grep | existing | ⬜ pending |
| 02-T2 | 02 | 1 | PYAPIFIX-02 | T-140.1.1-04/06 | 5 transient → 424/recoverable:true; 5 permanent → 403; fence tests UNEDITED | parametrised (RED-first) | `pytest tests/test_process_key.py tests/test_envelope_recoverable.py -q` | new+existing | ⬜ pending |
| 02-T3 | 02 | 1 | PYAPIFIX-02 | — | M-4 carve-out recorded, REQUIREMENTS amended | grep gate + full suite | `grep "BLOCKED-BY: TS-05" .planning/REQUIREMENTS.md` + `pytest -q` + mypy | ledger | ⬜ pending |
| 03-T1 | 03 | 1 | PYAPIFIX-01 | T-140.1.1-08 | predicate extracted to pure leaf, zero behavior change | existing TS suite | `npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts` + tsc + lint | new | ✅ green (59 passed UNEDITED; tsc 0; lint 0 errors) |
| 03-T2 | 03 | 1 | PYAPIFIX-01 | T-140.1.1-07 | committed fixture == real TestClient body (both duplicate arms) | contract (falsifiability probe) | `pytest tests/test_process_key_onboard_contract.py -q` | new | ✅ green (6 passed; probe RED observed on key-set assertion, reverted) |
| 04-T1 | 04 | 2 | PYAPIFIX-03 | T-140.1.1-10/11 | B1: S-11 424→500, dependency dropped, logger.error | contract (RED-first) | `pytest tests/test_status_contract_exchange_internal.py -q` | existing | ✅ green (19 passed; RED `assert 424 == 500` observed) |
| 04-T2 | 04 | 2 | PYAPIFIX-03 | T-140.1.1-10 | B2+B3: 400→500 at both sites, first-ever tests | contract (RED-first) | `pytest tests/test_create_exchange_attribution.py -q` | new | ✅ green (7 passed; RED 3 failed observed — `assert 400 == 500` ×2 + `string indices must be integers`) |
| 04-T3 | 04 | 2 | PYAPIFIX-03 | T-140.1.1-12 | M-14: 2 log companions; absent header logs NOTHING | structlog capture + full suite | `pytest tests/test_service_key_log_companion.py -q` + `pytest -q` + mypy | new | ✅ green (4 passed; RED 3 failed observed; 0-event fence proved falsifiable by neuter probe → 1 failed / 3 passed, reverted) |
| 05-T1 | 05 | 2 | PYAPIFIX-01 | T-140.1.1-13 | widened predicate accepts P1/P2, rejects N1/N2/N3 | parity (RED-first) | `npx vitest run tests/lib/process-key-onboard-contract-parity.test.ts` + tsc | new | ✅ green (9 passed; RED 3 failed observed — P1 rejected AND N2 wrongly accepted; tsc 0; `vi.mock` count 0) |
| 05-T2 | 05 | 2 | PYAPIFIX-01 | T-140.1.1-14 | route.test rewritten not deleted; retained negative; coverage gate | vitest + coverage | `npx vitest run …route.test.ts` + `npm run test:coverage` | existing | ✅ green (61 passed, was 59, 0 deleted; coverage 84.36/78.37/81.43/86.49 vs 80/72/74/82) |
| 05-T3 | 05 | 2 | PYAPIFIX-01 | T-140.1.1-15 | BOTH-direction neuter proof; ledger reconciled; M-11 recorded | mutation + grep gates | greps on 140.1-TS-OBLIGATIONS.md + git clean | ledger | ✅ green (both REDs observed first-hand: A `only in reply: ['codes']` 2 failed; B `() => true` 4 failed; both restored, tree clean, 0 MUTANT) |
| 06-T1 | 06 | 3 | PYAPIFIX-05 | T-140.1.1-16 | #1/#2 ccxt-family parametrisation; 2 mutation cycles RED | mutation re-run | `pytest tests/test_status_contract_exchange_internal.py -q` + protocol | existing | ✅ green (26 passed, was 19; A `NetworkError` ⇒ 3 failed, B `ExchangeNotAvailable` ⇒ 6 failed, both `assert 500 == 424`, both OBSERVED + reverted) |
| 06-T2 | 06 | 3 | PYAPIFIX-05 | T-140.1.1-17 | #6 "15" literal x4 sites, #7 two-phase clock, #12 copy equality; 3 cycles RED | mutation re-run | targeted node ids per cycle | existing | ✅ green (152 passed; #6 15→900 ⇒ 4 failed `assert '900' == '15'`; #7 300→1e18 ⇒ 1 failed `assert 1 == 2`; #12 junk copy ⇒ 1 failed; all OBSERVED + reverted) |
| 06-T3 | 06 | 3 | PYAPIFIX-05 | T-140.1.1-17 | M-15 AST fence; probes M-15 (RED on 7th shape) + M-15b (green on line shift) + W-4 (green on non-200) | mutation re-run | `pytest tests/test_process_key_200_discriminator.py -q` | existing | ✅ green (17 passed; (a) 7th shape ⇒ 1 failed naming `dict:code,mutant,ok`; (b) line shift ⇒ 17 passed; (c) `status_code=418` ⇒ 17 passed; all OBSERVED + reverted) |
| 07-T1 | 07 | 4 | PYAPIFIX-05 | T-140.1.1-19 | slowapi synced to 0.1.10 + #3/#4 behavioural oracles; 2 cycles RED | mutation re-run (versioned) | `pip show slowapi` + `pytest tests/test_limiter_identity.py -q` | existing | ✅ green (68 passed, was 63; slowapi **0.1.10**; #3 `return ""` ⇒ 3 failed incl. `never answered 429 within 4 calls`; #4 per-request uuid ⇒ 3 failed incl. the stability assertion; both OBSERVED + reverted) |
| 07-T2 | 07 | 4 | PYAPIFIX-05 | T-140.1.1-21 | #5 tight-band Retry-After; 1 cycle RED | mutation re-run (versioned) | `pytest tests/test_rate_limit_contract.py -q` | existing | ✅ green (7 passed; `_retry_after_seconds` → `return 1` ⇒ **2 failed** — band fired at BOTH route A and route B; OBSERVED + reverted; slowapi **0.1.10**) |
| 07-T3 | 07 | 4 | PYAPIFIX-05 | T-140.1.1-20 | #8–#11 claim-parser oracles (#11 at BOTH sites); 5 runs RED; phase gate | mutation re-run + full gates | `pytest tests/test_tenant_claim_parsing.py -q` + full suite + mypy + `vitest run tests/lib/` | new | ✅ green (14 passed; #8 ⇒ 2 failed, #9 ⇒ 3 failed, #10 ⇒ 3 failed, #11-A ⇒ 1 failed, #11-B ⇒ 1 failed **on the other test**; all 5 OBSERVED + reverted; phase gate below) |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky. No 3 consecutive tasks lack an automated verify (none do).*

---

## Mutation Re-Run Protocol (PYAPIFIX-05 — BLOCKING)

"The test now covers it" is **not evidence**. For each of the 13 (12 review survivors + M-15):

1. Apply the exact mutation named in `140.1-REVIEW.md` to the production file.
2. Run the pinning test. **Observe RED first-hand** — capture the failing assertion text.
3. Revert the mutation (scratch-copy restore; the working tree must be clean after — NEVER `git stash`/`git checkout` while .planning ledgers are live).
4. Re-run. **Observe GREEN.**
5. Record mutation / command / RED assertion text in the plan's SUMMARY.

A survivor is closed only when steps 2 and 4 were both **observed**, not inferred.
Flag every slowapi-dependent survivor (#3/#4/#5) as re-confirmed against the **installed** version
(plan 07 syncs to the CI pin 0.1.10 first), and note which version that was.
Survivor→task map: #1/#2→06-T1 · #6/#7/#12→06-T2 · M-15→06-T3 · #3/#4→07-T1 · #5→07-T2 · #8-#11→07-T3.

---

## Cross-Process Contract Gate (PYAPIFIX-01 — BLOCKING)

The defect is that **both suites are green because each mocks the other.** Therefore:

- [ ] The oracle exercises the **real** Python reply against the **real** TS guard predicate (plan 03: full-stack TestClient equality; plan 05: real extracted predicate over the same committed fixture).
- [ ] **Two new mocks agreeing with each other does not satisfy this gate** (parity test has ZERO vi.mock blocks — asserted by grep in plan 05).
- [ ] Neutering **either** side alone turns a test RED — plan 05 Task 3 proves both directions, each observed first-hand.
- [ ] Both sides run in the **existing** CI jobs on a normal PR (python job + frontend-test `tests/lib/**` glob), with no new infrastructure. The fixture is COMMITTED (sibling jobs have no `needs:` edge — a generated file is impossible).

---

## Wave 0 Requirements

Wave 1 carries the Wave-0 prerequisites (plan 03): the extraction of
`isProcessKeyOnboardResponse` + `ProcessKeyOnboardResponse` to `src/lib/process-key-onboard-contract.ts`
(the predicate is NOT exported today and route.ts needs 10 vi.mock blocks to import — PATTERNS §6),
and the committed shared fixture `analytics-service/tests/fixtures/process_key_onboard_contract.json`.
Both block plan 05 (depends_on: 140.1.1-03). Set `wave_0_complete: true` when plan 03 lands.

**LANDED 2026-07-26** (commits `82f84f28` extraction, `0c45da2c` fixture + pytest):
`src/lib/process-key-onboard-contract.ts` exists with **0 import statements** and both
symbols exported; `route.ts` consumes it (one implementation repo-wide).
`analytics-service/tests/fixtures/process_key_onboard_contract.json` is **tracked in
git** — 5 cases, 2 positive / 3 negative — and its 2 positive bodies are proven equal to
REAL full-stack `main.app` TestClient replies on both duplicate arms. Falsifiability
probe executed (P1 `job_state`→`job_states` ⇒ RED, reverted ⇒ GREEN).
`wave_0_complete: true` set above.

---

## Wave 2 Gate — plan 04 Task 3 (executed 2026-07-26)

| Gate | Result |
|---|---|
| Full `python3 -m pytest -q` | **4716 passed / 96 skipped / 0 failed** (68s) |
| `mypy --strict --follow-imports=silent services/ routers/ models/` | **Success: no issues found in 89 source files** |
| New `# type: ignore` | **0** |

Arithmetic: wave-1 closed at 4704/96; plan 04 adds 12 tests (+1 S-11 rewrite split,
+7 attribution, +4 log companion) ⇒ 4716, skips unchanged. No silent skips.

Plan 05 (wave 2, TypeScript) still owes the vitest half of this wave's gate.

**Vitest half PAID 2026-07-26 (plan 05):**

| Gate | Result |
|---|---|
| `npm run test:coverage` (full frontend suite) | **8878 passed / 287 skipped / 0 failed** (697 files) |
| Coverage vs `vitest.config.ts` thresholds | stmts **84.36**/80 · branches **78.37**/72 · fns **81.43**/74 · lines **86.49**/82 — all clear |
| `npx tsc --noEmit` | exit **0** |
| `npm run lint` | **0 errors** (1 pre-existing unrelated warning) |
| Full `python3 -m pytest -q` re-run after the neuter cycles | **4716 passed / 96 skipped / 0 failed** — identical to the plan-04 baseline, so the Direction-A restore is complete |
| `mypy --strict` | Success, 89 files; **0** new `# type: ignore` |

---

## Cross-Process Contract Gate — CLOSED 2026-07-26 (plan 05 Task 3)

All four boxes below are now satisfied; the checklist further down is answered here.

| Requirement | Evidence |
|---|---|
| Real-vs-real, both halves | Python: plan 03's full-stack `main.app` TestClient equality. TypeScript: the REAL extracted predicate over the SAME committed fixture. |
| Zero mocks | `grep -c "vi.mock" tests/lib/process-key-onboard-contract-parity.test.ts` → **0** |
| **Direction A** — neuter Python ⇒ pytest RED | `_wizard_duplicate_reply` `"code"`→`"codes"` ⇒ **2 failed**: *"disagree on their KEY SET … only in reply: ['codes']; only in fixture: ['code']"*. Restored from `/tmp/pk.orig.py` ⇒ **6 passed**. **OBSERVED.** |
| **Direction B** — neuter TypeScript ⇒ vitest RED | `return true;` as the predicate's first statement ⇒ **4 failed** (all three negatives wrongly accepted + the anti-degradation case). Restored from `/tmp/pkoc.orig.ts` ⇒ **9 passed**. **OBSERVED.** |
| Existing CI jobs, committed fixture | pytest under `analytics-service/tests/`; vitest under the `tests/lib/**` glob. No new `needs:` edge, no config change. Fixture tracked by `git ls-files`. |
| Tree clean after both cycles | `git status --porcelain` = `M TODOS.md` + the pre-existing untracked script only; `grep -rn MUTANT` → **0** |

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| (none — every criterion in this phase is automated) | | | |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (plan 03 creates the fixture + extraction)
- [x] No watch-mode flags
- [x] Feedback latency < 5 min
- [x] Mutation re-run protocol executed for all 13, RED observed first-hand — **CLOSED 2026-07-26**: batch 1 (plan 06, 6 survivors / 8 runs) + batch 2 (plan 07, 7 survivors / 8 runs, #11 at two sites) = **13 of 13**, every RED/GREEN OBSERVED, zero "asserted only", zero non-reddening findings
- [x] Cross-process contract gate satisfied (real-vs-real, both directions) — CLOSED 2026-07-26 by plan 05 Task 3; both REDs observed first-hand
- [x] `mypy --strict` clean, 0 new `# type: ignore` — **Success, 89 files**; `git diff 56fb7167..HEAD | grep -c "^+.*type: ignore"` → **0**
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** planned 2026-07-26 — 7 plans / 4 waves; map populated by planner.

---

## Wave 3 Gate — plan 06 Task 3 (executed 2026-07-26)

| Gate | Result |
|---|---|
| Full `python3 -m pytest -q` | **4724 passed / 96 skipped / 0 failed** (68.7 s) |
| `mypy --strict --follow-imports=silent services/ routers/ models/` | **Success: no issues found in 89 source files** |
| New `# type: ignore` | **0** |
| `grep -rn MUTANT analytics-service/ src/` | **0** |
| Production files modified | **0** — this plan changes oracles only |

Arithmetic: wave-2 closed at 4716/96; plan 06 replaces one S-06 424 test with an
8-row parametrisation (+7) and one M-15 fence test with two (+1) ⇒ **4724**,
skips unchanged. No silent skips.

## Mutation Re-Run Protocol — batch 1 CLOSED 2026-07-26 (plan 06)

**6 of the 13 closed, every RED/GREEN OBSERVED first-hand, zero "asserted only".**
Full verbatim ledger in `140.1.1-06-SUMMARY.md`.

| Survivor | Mutation | Observed |
|---|---|---|
| **#1** | `routers/exchange.py` `except ccxt.BaseError` → `ccxt.NetworkError` | **3 failed / 5 passed** — `PermissionDenied must answer 424, got 500` / `assert 500 == 424` |
| **#2** | same → `ccxt.ExchangeNotAvailable` | **6 failed / 2 passed** — `RateLimitExceeded must answer 424, got 500` |
| **#6** | `services/error_contract.py` `"supabase": 15` → `900` | **4 failed / 0 passed** — `assert '900' == '15'`, ≥1 red per file, targeted node ids only |
| **#7** | `routers/internal.py` `_KEK_ALERT_WINDOW_S = 300.0` → `1e18` | **1 failed** — `the operator signal must RETURN once the 300 s window expires` / `assert 1 == 2` |
| **#12** | `routers/exchange.py` MT5-unconfigured copy → `"Gateway unavailable."` | **1 failed** — `assert 'Gateway unavailable.' == 'The MetaTrad... not a retry.'` |
| **M-15** | (a) 7th 200 return | **1 failed / 16 passed** — `only in the router: ['dict:code,mutant,ok']` |
| **M-15b** | (b) 3 blank lines above the handler | **17 passed** (GREEN required — the fingerprint is not position-keyed) |
| **W-4** | (c) `return JSONResponse(status_code=418, content={})` | **17 passed** (GREEN required — the 200-capable filter excludes it) |

All restores from `cp` scratch copies under `/tmp`; **zero** `git stash` /
`git checkout` / `git clean`. Tree clean after every cycle.

**Still open for plan 07 (batch 2):** #3 / #4 / #5 (slowapi — sync 0.1.9 → the CI
pin 0.1.10 first and record the version) and #8–#11 (claim parser, #11 at BOTH sites).
→ **ALL CLOSED 2026-07-26 by plan 07. See below.**

---

## Wave 4 Gate — plan 07 Task 3 (executed 2026-07-26) — THE PHASE-WIDE GATE

| Gate | Result |
|---|---|
| Full `python3 -m pytest -q` | **4743 passed / 96 skipped / 0 failed** (99.9 s) |
| `python3 -m pytest --collect-only -q` | **4837 collected**, 0 errors |
| `mypy --strict --follow-imports=silent services/ routers/ models/` | **Success: no issues found in 89 source files** |
| `npx tsc --noEmit` | exit **0** |
| `npm run test:coverage` (FULL frontend suite — stronger than the plan's `vitest run tests/lib/`) | **8878 passed / 287 skipped / 0 failed**, 697 files, exit **0** |
| Coverage vs `vitest.config.ts` thresholds | stmts **84.36**/80 · branches **78.38**/72 · fns **81.45**/74 · lines **86.49**/82 — all clear |
| `npm run lint` | **0 errors** (1 pre-existing unrelated warning) |
| New `# type: ignore` phase-wide (`git diff 56fb7167..HEAD`) | **0** |
| `grep -rn MUTANT analytics-service/ src/` | **0** |
| Production files modified by plan 07 | **0** — oracles only |

Arithmetic: wave 3 closed at 4724/96; plan 07 adds 5 (Task 1) + 0 (Task 2 adds
assertions to existing tests) + 14 (Task 3's new file) = **+19** ⇒ **4743**,
skips unchanged at **96**. No silent skips.

⚠️ **The 4837-collected vs 4839-accounted delta was traced, not assumed.** Two
**module-level** collection skips — `test_equity_reconstruction_integration.py:38`
and `test_equity_reconstruction_live.py:34`, both
`pytest.skip(..., allow_module_level=True)` — are counted as skips by the runner
but are not test ITEMS, so `--collect-only` does not list them. 4837 items =
4743 passed + 94 item-skips; +2 module-skips = 96. Pre-existing (proved by
`--collect-only --ignore=<the new file>` → 4823 = 4837 − this plan's 14) and in
no PYAPIFIX evidence chain.

## Mutation Re-Run Protocol — batch 2 CLOSED 2026-07-26 (plan 07) → 13/13 PHASE-WIDE

**7 more survivors closed at 8 sites; every RED OBSERVED first-hand.**
Full verbatim ledger in `140.1.1-07-SUMMARY.md`.

**slowapi synced 0.1.9 → the CI pin `0.1.10` BEFORE any of #3/#4/#5**, and both
internals the mutations depend on re-confirmed present on 0.1.10: the
`if all(args)` empty-key skip (`extension.py:506-527`) and
`request.state.view_rate_limit`'s `(limit, [key, scope])` shape (`:530`).

| Survivor | Mutation | Observed | slowapi |
|---|---|---|---|
| **#3** | `rate_limit.py:208` → `return ""` | **3 failed / 65 passed** — `default_platform_key returned the empty string…` · `assert '' == 'platform:/py…'` · **`never answered 429 within 4 calls`** | **0.1.10** |
| **#4** | same → per-request `uuid4().hex` | **3 failed / 65 passed** — `assert 'platform:d7f…' == 'platform:/py…'` · `not stable across requests` · **`never answered 429 within 4 calls`** | **0.1.10** |
| **#5** | `main.py:_retry_after_seconds` → `return 1` | **2 failed / 5 passed** — `Retry-After is 1s … anything below 3240s is understating it` (route A) + the same at route B | **0.1.10** |
| **#8** | `rate_limit.py:274` drop the length bound | **2 failed / 12 passed** — `a 513-char claim was ACCEPTED` · `ran hmac.new 1 time(s) on a 513-char header` | n/a |
| **#9** | `:285` `rsplit` → `split` | **3 failed / 11 passed** — `assert 'claimtest:un…' == 'claimtest:t:a.b.c'` · `assert None == 'a.b.c'` · same via `tenant_or_platform_key` | n/a |
| **#10** | `:286` drop the empty-payload guard | **3 failed / 11 passed** — `assert 'claimtest:t:' == 'claimtest:un…'` · `assert '' is None` | n/a |
| **#11 site A** | `:333` `==` → `startswith` | **1 failed / 13 passed** — `got 'claimtest:anon'…` (the `:417` test stayed GREEN) | n/a |
| **#11 site B** | `:417` `==` → `startswith` | **1 failed / 13 passed** — `got 'claimtest:anon' from tenant_or_platform_key — the key function for all nine PYAPI-03 routes` (the `:333` test stayed GREEN) | n/a |

**#11 is 2 of 2, and the asymmetry is the proof**: the same mutation at two
lines reddened a *different single test* each time, with the other green — which
a single "covers the anon arm" test could never have demonstrated.

⚠️ **#8's fixture is a correctly-minted, VALID, merely oversized 513-char claim,
not 513 chars of junk.** Junk would NOT have reddened: without the guard,
`rsplit` raises on an unparseable string and the never-raise `except` swallows
it, so `is None` holds either way. Recorded because the obvious fixture would
have produced a false "closed".

All restores from `cp` scratch copies under `/tmp`; **zero** `git stash` /
`git checkout` / `git clean` / `git reset`. Tree clean and `grep -rn MUTANT` → 0
after every cycle.
