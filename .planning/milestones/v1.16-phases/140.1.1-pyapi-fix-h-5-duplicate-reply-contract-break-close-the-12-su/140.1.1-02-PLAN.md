---
phase: 140.1.1-pyapi-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/exchange.py
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_process_key.py
  - analytics-service/tests/test_envelope_recoverable.py
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PYAPIFIX-02]
must_haves:
  truths:
    - "Each of the five venue-transient codes (PROBE_FAILED, DDOS_PROTECTION, RATE_LIMITED, EXCHANGE_UNAVAILABLE, NETWORK_UNAVAILABLE) through /process-key answers 424 with recoverable:true (ROADMAP criterion 2)"
    - "Each genuine caller fault (AUTH_FAILED, PERMISSION_DENIED, TRADE_SCOPE, WITHDRAW_SCOPE, MISSING_SCOPE) still answers 403 with recoverable:false"
    - "No response body says recoverable:false beside 'Try again in a moment' — the recoverable field and the status agree (criterion 2 second clause; requires the A2 edit, which no status change can fix)"
    - "The four TRAP-9 fence tests (test_process_key.py:991, :2500, :2568, :2631) pass UNEDITED — proof the fix did not over-reach into the deferred 422-vs-403 item"
    - "ONE canonical permanent-code allow-list exists (incl. MISSING_SCOPE); process_key's pre-gate and recoverable derivation both read it; long_fetch's local permanent_codes stays BYTE-UNCHANGED with the MISSING_SCOPE divergence recorded as a deferred follow-up (checker W-3 ruling — recorded, not silently unified; no new divergent enumeration created)"
  artifacts:
    - path: analytics-service/services/exchange.py
      provides: "canonical PERMANENT_VALIDATION_ERROR_CODES allow-list next to the codes themselves"
      contains: "PERMANENT_VALIDATION_ERROR_CODES"
    - path: analytics-service/tests/test_envelope_recoverable.py
      provides: "direct unit oracle: _envelope_error recoverable for all ten codes"
  key_links:
    - from: analytics-service/routers/process_key.py
      to: analytics-service/services/exchange.py
      via: "pre-gate + recoverable both derive from the shared allow-list"
      pattern: "PERMANENT_VALIDATION_ERROR_CODES"
---

<objective>
PYAPIFIX-02: a fault at the caller's venue answers 424/recoverable through `/process-key`, not
403/terminal. Class A per PATTERNS §1/§2 — **2 in-fence sites** (A1: the `_scope_rejected` gate
`process_key.py:1295-1299` + its SINGLE return `:1360-1365` — ONE code path; the review's "two
sites" over-count is corrected by PATTERNS §9-M1. A2: the `recoverable` set at
`process_key.py:358-359` — a separate edit no status-code change fixes) — **+ 3 decision-pending
sites** (A3 `routers/exchange.py:143-145`, A4 `:146-152`, A5 `:504-505`) which this plan
ENUMERATES and DEFERS with a recorded carve-out `BLOCKED-BY: TS-05`.

Fix shape (LOCKED, orchestrator decision 2): an **ALLOW-LIST of PERMANENT codes, defaulting to
transient**. Reference implementation: `services/ingestion/long_fetch.py:386-397`
(`permanent_codes = {"AUTH_FAILED","PERMISSION_DENIED","TRADE_SCOPE","WITHDRAW_SCOPE"}` plus the
VALIDATION_UNEXPECTED-only-when-our-own-fallback nuance). A denylist of transient codes fails
UNSAFE and is literally the existing bug at `:357-359`. Do not reintroduce one under any name
(including the `VENUE_TRANSIENT_ERROR_CODES` shape RESEARCH round-2 suggested — PATTERNS §9-M3
corrected it).

STANDING CORRECTIONS (do not inherit): `/exchange/validate-key` does NOT "already handle this
correctly" — it never evaluates `read_only is False` (PATTERNS §9-M2; the CONTEXT.md:113 claim is
FALSE). And long_fetch is correct for ALL FIVE codes (§9-M3), not "incomplete for four".

STRUCTURE RULING (checker W-1, orchestrator-ACCEPTED — do not re-raise): A2 stays folded into
Task 2 rather than becoming a task of its own. It is separately enumerated, has its own action
paragraph, its own dedicated oracle (tests/test_envelope_recoverable.py over all ten codes),
and its own must_haves truth; splitting would push this plan to 4 tasks and breach the scope
target. The failure mode the criterion guards against — A2 silently absorbed into a
status-code fix — does not occur.

Purpose: when OKX blips, the caller retries and succeeds instead of being told to fix credentials.
Output: shared constant, 424 pre-gate, recoverable fix, additive tests, recorded M-4 carve-out.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md  (§2 Class A — the authoritative inventory; §8 analog rows; §9-M1/M2/M3/M4)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md  (§PYAPIFIX-02; corrections C-2, C-15, C-17)
@analytics-service/services/ingestion/long_fetch.py
@analytics-service/services/exchange.py
@analytics-service/routers/process_key.py
</context>

<interfaces>
Verified at 56fb7167:
- Transient error_code assignment sites: DDOS_PROTECTION services/exchange.py:1112 · RATE_LIMITED
  :1123 · EXCHANGE_UNAVAILABLE :1135 · NETWORK_UNAVAILABLE :1145 · PROBE_FAILED :1207.
- `read_only is False` appears exactly TWICE in analytics-service/: process_key.py:1297 and
  long_fetch.py:331 (orchestrator-verified grep). The syntactic search finds 1 of 5 real members —
  membership is behavioural, not textual.
- `_envelope_error` (process_key.py:352-360): `"recoverable": code in {"RATE_LIMITED",
  "EXCHANGE_UNAVAILABLE","NETWORK_UNAVAILABLE"}` — omits PROBE_FAILED and DDOS_PROTECTION. The
  human copy "Try again in a moment." comes from services/exchange.py:1203-1206 — a DIFFERENT
  module; criterion 2's second clause cannot be met by any status-only change (RESEARCH C-2).
- /process-key is S-22: it uses its OWN top-level DESIGN-05 envelope (`_envelope_error`), NOT
  error_contract's body.detail envelope (140.1-05 locked decision — a parallel envelope on this
  route is forbidden). The new 424 therefore uses the route's own envelope shape, mirroring the
  existing 403 return at :1360-1365. `error_contract._validate` is NOT involved on this route.
- The VALIDATION_UNEXPECTED fallback: process_key.py:1305 `val.error_code or "VALIDATION_UNEXPECTED"`
  — OUR OWN fallback (error_code is None) is permanent and stays 403 (which is exactly what fence
  test :2568 pins); an adapter-SET "VALIDATION_UNEXPECTED" may be transient (long_fetch's
  `_is_unexpected_fallback` nuance — carry it across).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Canonical permanent-code allow-list in services/exchange.py (behavior-preserving; long_fetch deliberately untouched)</name>
  <files>analytics-service/services/exchange.py</files>
  <read_first>
    - analytics-service/services/ingestion/long_fetch.py:280-400 (pre-gate, _scope_rejected, permanent_codes, _is_permanent, _is_unexpected_fallback, and the comment at :296-305 stating the reasoning)
    - analytics-service/services/exchange.py:1095-1215 (code-assignment sites + the fail-closed default at :1189-1207)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §8 rows "Class A fix" and "Canonical transient/permanent code sets" (its "all three consumers read it or none is added" constraint is OVERRIDDEN by the checker W-3 ruling: long_fetch is deliberately excluded this phase — divergence recorded, not silently unified)
  </read_first>
  <action>
    In `services/exchange.py`, next to the error-code assignment sites, add ONE module-level
    frozenset `PERMANENT_VALIDATION_ERROR_CODES = frozenset({"AUTH_FAILED", "PERMISSION_DENIED",
    "TRADE_SCOPE", "WITHDRAW_SCOPE", "MISSING_SCOPE"})` with a docstring stating the
    allow-list-of-permanent rule: any probe `error_code` NOT in this set is venue-transient and
    must be treated as retryable — an unknown future code fails SAFE (transient). Docstring must
    also state: "VALIDATION_UNEXPECTED" is permanent ONLY when it is our own fallback (i.e. the
    adapter set no error_code at all); an adapter-set VALIDATION_UNEXPECTED stays transient — the
    consumer carries that check (long_fetch's `_is_unexpected_fallback` is the model).
    MISSING_SCOPE — RESOLVED AT SOURCE (checker W-3 ruling; the hedge is closed, implement
    exactly this): MISSING_SCOPE IS reachable — services/exchange.py:1047-1058 (the deribit
    scope_detail arm) sets read_only=False and error_code="MISSING_SCOPE", then returns early
    WITHOUT ever setting valid=True, so `_scope_rejected` fires with `_reject_code ==
    "MISSING_SCOPE"`. It MUST be in the shared set: today it reaches process_key's 403 arm
    (permanent); omitting it from the allow-list would drop it through the new transient
    default to 424-retryable — an infinitely-retried permanently-missing read scope, a NEW bug
    of exactly the class this phase closes. (Task 2's negative-control parametrisation pins
    MISSING_SCOPE → 403/recoverable:false — that case is the load-bearing regression pin.)
    long_fetch is NOT re-pointed at the shared constant in this phase. Its local
    `permanent_codes` (long_fetch.py:391-394) omits MISSING_SCOPE, so long_fetch treats a
    permanently-missing read scope as TRANSIENT/retryable on the worker retry path today;
    re-pointing would silently flip that transient→permanent — a production behaviour change
    named in no requirement and no success criterion. Leave long_fetch.py BYTE-UNCHANGED and
    record the divergence in the SUMMARY as a deferred follow-up, verbatim: "long_fetch's
    local `permanent_codes` omits MISSING_SCOPE and therefore retries a permanently-missing
    read scope; unifying it with the shared constant is a correct fix but changes worker retry
    semantics — deferred, not overlooked." An undocumented omission would falsify the
    "class closed" claim. The root-cause site services/exchange.py:1189-1207
    is deliberately NOT changed (PATTERNS A8 — high blast radius; gate at the consumers).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/ -k "long_fetch" -q 2>&1 | tail -3 && grep -c "PERMANENT_VALIDATION_ERROR_CODES" services/exchange.py && git diff --quiet -- services/ingestion/long_fetch.py</automated>
  </verify>
  <acceptance_criteria>
    - `PERMANENT_VALIDATION_ERROR_CODES` defined exactly once, in services/exchange.py (grep count 1 there).
    - long_fetch.py is BYTE-UNCHANGED (checker W-3): `git diff analytics-service/services/ingestion/long_fetch.py` is EMPTY — the local permanent_codes set stays; the divergence is recorded in the SUMMARY, not silently unified.
    - Every existing long_fetch test green, unedited.
    - No transient-code denylist constant was created anywhere: `grep -rn "VENUE_TRANSIENT" analytics-service/services/ analytics-service/routers/` → 0 hits.
  </acceptance_criteria>
  <done>One canonical allow-list of permanent codes (incl. MISSING_SCOPE) exists at the source of the codes; long_fetch deliberately untouched, divergence recorded as a deferred follow-up.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A1 + A2 — the 424 pre-gate and the recoverable fix in process_key.py</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_process_key.py, analytics-service/tests/test_envelope_recoverable.py</files>
  <read_first>
    - analytics-service/routers/process_key.py:340-365 (_envelope_error) and :1285-1372 (the three-arm _scope_rejected gate, the :1305 fallback, and the single 403 return at :1360-1365)
    - analytics-service/services/ingestion/long_fetch.py:296-333 (the pre-gate shape + the :296-305 comment to adapt)
    - analytics-service/tests/test_process_key.py:985-996, :2434-2447, :2494-2507, :2562-2575, :2625-2636 (the four fence tests — READ so you know exactly what must NOT change)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §2 (A1/A2 rows + "Tests that must move")
  </read_first>
  <behavior>
    RED first — write these tests, observe them fail against HEAD, then implement:
    - In test_process_key.py (APPEND at end of file; do not insert above existing tests):
      a parametrisation over ALL FIVE transient codes asserting status == 424 (literal) and
      body recoverable is True; a negative-control parametrisation over AUTH_FAILED /
      PERMISSION_DENIED / TRADE_SCOPE / WITHDRAW_SCOPE / MISSING_SCOPE asserting 403 (literal)
      and recoverable is False. Use the :2434-2446 parametrize+ids idiom. Grep-verified today:
      the five transient codes have ZERO hits in this file — the work is ADDITIVE.
    - New tests/test_envelope_recoverable.py: direct unit test of _envelope_error's recoverable
      for all ten codes (5 transient → True, 5 permanent → False), each expected value a literal
      in the test, never derived from the shared constant (no self-referential oracle).
  </behavior>
  <action>
    A1: in the `_scope_rejected` block of the `process_key` handler, insert a venue-transient
    pre-gate BEFORE the 403 return at :1360-1365 (mirror long_fetch.py:306-318's position:
    transient escape FIRST, scope verdict second). Condition: `val.error_code` is present
    (non-None — our own VALIDATION_UNEXPECTED fallback stays on the 403 path, matching fence test
    :2568) AND `val.error_code not in PERMANENT_VALIDATION_ERROR_CODES` (import the Task-1
    constant). On that condition return `JSONResponse(status_code=424, content=_envelope_error(...))`
    with the SAME envelope key set as the existing 403 return (ok/code/human_message/
    debug_context/correlation_id/recoverable) — the route's own DESIGN-05 envelope, NOT
    error_contract's (S-22; a parallel envelope is forbidden per the 140.1-05 decision, restated
    in <interfaces>). Adapt long_fetch's :296-305 comment to state the reasoning at the new gate.
    A2: replace `_envelope_error`'s recoverable computation at :358-359 (:357 is correlation_id). The current shape
    (allow-list of RECOVERABLE codes) fails unsafe. New shape derived from the shared constant:
    recoverable is True iff the code is a probe-produced code NOT in
    PERMANENT_VALIDATION_ERROR_CODES. CONSTRAINT: `_envelope_error` serves every refusal arm of
    this route, not only the probe path — enumerate its call sites in this file (all in
    process_key.py) and every code value they pass; codes outside the ten-probe-code universe
    (e.g. STRATEGY_NOT_OWNED, WIZARD_* codes, INTERNAL_TOKEN codes) must keep their CURRENT
    recoverable value. Pin each such arm's recoverable in test_envelope_recoverable.py so the new
    derivation provably does not flip a non-probe code. If that requires scoping the new
    derivation to probe codes explicitly (e.g. recoverable computed by the caller for probe
    verdicts and defaulted False otherwise), do that rather than a blanket not-in-permanent
    default — a blanket default-True for unknown codes would mark STRATEGY_NOT_OWNED recoverable,
    which is wrong.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key.py tests/test_envelope_recoverable.py -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - The 5-transient parametrisation: each answers 424 + recoverable true (observed RED before the pre-gate, GREEN after — paste counts).
    - The 5-permanent negative control: each answers 403 + recoverable false.
    - FENCE (ROADMAP TRAP-9 row): tests at :991, :2500, :2568, :2631 pass and are BYTE-UNEDITED — `git diff analytics-service/tests/test_process_key.py` shows ONLY appended hunks at end of file; zero hunks touching lines 1-3100. NO marker comments added to the four (orchestrator decision 3 overrides PATTERNS' marker suggestion: zero edits).
    - No body can contradict itself: on the 424 path, recoverable is true beside the "Try again in a moment." copy; assert both in one test case.
    - `_run_validate_only`'s failure arm (process_key.py:686) still answers 200 + ok:false (its own pinned tests stay green — scope fence from 140.1-05).
    - mypy --strict: "Success: no issues found in 89 source files".
    - Class A landing is executable (checker I-2): `grep -c "PERMANENT_VALIDATION_ERROR_CODES" analytics-service/routers/process_key.py` → exactly 2 (the A1 pre-gate + the A2 recoverable derivation) — proof BOTH A1 and A2 landed.
  </acceptance_criteria>
  <done>All five venue-transient codes answer 424/recoverable:true; all five caller faults stay 403/recoverable:false; the four fence tests unedited and green.</done>
</task>

<task type="auto">
  <name>Task 3: M-4 carve-out recorded + REQUIREMENTS wording amended + full-suite gate</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md:89 (the PYAPIFIX-02 requirement line)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md §PYAPIFIX-02 "The M-4 fence" (the four resolutions and why enumerate-and-defer wins) and C-18 (the [object Object] chain)
  </read_first>
  <action>
    Record the M-4 carve-out as an explicit decision (orchestrator decision 6 — an undocumented
    omission silently falsifies "class closed, not point-fixed"):
    (1) In this plan's SUMMARY, a section "M-4 carve-out — BLOCKED-BY: TS-05" listing A3
    `routers/exchange.py:143-145`, A4 `:146-152`, A5 `:504-505` as enumerated Class-A members,
    DEFERRED with the verified reason: migrating them to `service_error(424)` turns body.detail
    from scalar to object; `src/lib/analytics-client.ts:177-180` then feeds "[object Object]" into
    `classifyKeyValidationError`'s substring cascade (src/lib/wizardErrors.ts:936-1034) → terminal
    UNKNOWN/500 dead-end render, until TS-05 (owned by 140.2/140.3) lands. Both 400 and 424 are
    4xx ⇒ breaker-inert ⇒ no attributability change today.
    (2) Amend .planning/REQUIREMENTS.md's PYAPIFIX-02 line: change "at every site" to "at every
    site a Python-side change can correct without depending on an unlanded TypeScript obligation,
    with the remainder (routers/exchange.py:145, :152, :505) enumerated and blocker-named
    (BLOCKED-BY: TS-05)". Make the edit surgically (this ledger is gitignored and has no backup —
    never rewrite it wholesale; edit the one line).
    (3) Do NOT edit 140.1-TS-OBLIGATIONS.md here — plan 140.1.1-05 owns that file and adds the
    TS-05↔M-4 pairing row (single-owner rule; note the handoff in the SUMMARY).
    Then run the wave gate: full pytest + mypy.
  </action>
  <verify>
    <automated>grep -n "BLOCKED-BY: TS-05" .planning/REQUIREMENTS.md && cd analytics-service && python3 -m pytest -q 2>&1 | tail -3 && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "BLOCKED-BY: TS-05" .planning/REQUIREMENTS.md` ≥ 1 hit; the three file:line members named in the amended requirement.
    - REQUIREMENTS.md line count changed by ≤ 3 lines (surgical edit, `wc -l` before/after recorded in SUMMARY).
    - routers/exchange.py:145/:152/:505 are NOT in this plan's diff (the carve-out is a record, not an edit).
    - Full analytics-service pytest green; mypy "Success: no issues found in 89 source files".
  </acceptance_criteria>
  <done>The class is closed by enumeration: 2 fixed, 3 recorded with a named blocker; the requirement wording is no longer falsified on merge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → /process-key | key-validation verdicts cross to untrusted callers |
| /process-key → wizard render | status + recoverable drive user retry behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.1-04 | Elevation of privilege | 424 pre-gate ordering | mitigate | pre-gate keys ONLY on probe error_code classification; scope verdicts (TRADE_SCOPE/WITHDRAW_SCOPE) and AUTH_FAILED stay 403 — pinned by the unedited fence tests + the 5-permanent negative control |
| T-140.1.1-05 | Repudiation | fail-safe default direction | mitigate | allow-list of PERMANENT codes: an unknown future code is treated transient (retry), never silently terminal; a caller-fault code must be NAMED to become permanent |
| T-140.1.1-06 | Information disclosure | 424 envelope | mitigate | new 424 reuses the existing _envelope_error key set — no new fields, no exception text, human copy from services/exchange.py names no credential |
| T-140.1.1-SC | Tampering | package installs | accept | zero new packages in this plan |
</threat_model>

<verification>
- 5 transient → 424/recoverable:true; 5 permanent → 403/recoverable:false (parametrised, literals).
- Four fence tests byte-unedited and green (git diff proof).
- One shared allow-list (incl. MISSING_SCOPE); process_key's two readers consume it; long_fetch
  byte-unchanged with the divergence recorded; zero transient-denylist constants.
- Full pytest + mypy green; 0 new `# type: ignore`.
</verification>

<success_criteria>
ROADMAP criterion 2 observably true (RESEARCH Validation row 2 command). PYAPIFIX-02 closed:
2 in-fence sites fixed, 3 members enumerated + blocker-named, no self-contradicting body.
</success_criteria>

<output>
Create `.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-02-SUMMARY.md` when done.
Record: RED-first evidence, the M-4 carve-out section, the MISSING_SCOPE/long_fetch divergence
record (the verbatim deferred-follow-up sentence from Task 1), the W-1 structure ruling (A2
folded into Task 2 — orchestrator-accepted), and the handoff note that plan 05 writes the
TS-OBLIGATIONS row.
</output>
