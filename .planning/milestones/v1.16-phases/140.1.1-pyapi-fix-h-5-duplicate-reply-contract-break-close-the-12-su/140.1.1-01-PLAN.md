---
phase: 140.1.1-pyapi-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/error_contract.py
  - analytics-service/docs/STATUS_CONTRACT.md
  - analytics-service/tests/test_error_contract_429.py
  - analytics-service/tests/test_error_contract_500_dependency.py
  - analytics-service/tests/test_error_contract_scalar_detail.py
  - analytics-service/tests/test_error_contract_retry_after_source.py
autonomous: true
requirements: [PYAPIFIX-04, PYAPIFIX-06]
must_haves:
  truths:
    - "service_error_body with a list/dict/int/bytes detail raises ValueError at construction — nothing non-scalar can reach a wire body (ROADMAP criterion 4)"
    - "service_error(429, 'RATE_LIMITED', retryable=True, retry_after=30) constructs and its headers carry Retry-After == '30' (criterion 6a)"
    - "service_error(500, 'X', dependency='binance', retryable=False) raises ValueError; 'kek'/'egress-proxy'/'mt5-gateway'/None all still validate (criterion 6b — MEMBERSHIP reading, never prohibition)"
    - "Every new guard fires identically through BOTH entry points: service_error AND service_error_response (criterion 6c)"
    - "Zero existing production call sites regress — the 6 legit 500-with-dependency sites, the 7 500-without, and every 503 site stay green (criterion 6d)"
  artifacts:
    - path: analytics-service/tests/test_error_contract_429.py
      provides: "PYAPIFIX-06(a) oracle: 1 positive + 4 rejections, x both entry points"
    - path: analytics-service/tests/test_error_contract_500_dependency.py
      provides: "PYAPIFIX-06(b) oracle: 1 rejection + 4 permitted, x both entry points"
    - path: analytics-service/tests/test_error_contract_scalar_detail.py
      provides: "PYAPIFIX-04 oracle: list/dict/int/bytes each raise, x both entry points"
    - path: analytics-service/tests/test_error_contract_retry_after_source.py
      provides: "C4(a) oracle: a 503 retry_after that drifts from RETRY_AFTER_SECONDS[dependency] raises ValueError; a 503 naming a no-transient-arm dependency (kek) raises ValueError, NOT KeyError"
  key_links:
    - from: analytics-service/services/error_contract.py
      to: analytics-service/docs/STATUS_CONTRACT.md
      via: "the 429 CALLER-row split lands in the SAME commit as the 429 arm (C-21: the arm ships already-falsified otherwise)"
      pattern: "429"
---

<objective>
Close all FOUR Class-C guard gaps in `services/error_contract.py` (PATTERNS §4: C1 scalar-detail,
C2 429+Retry-After unconstructable, C3 500-accepts-venue-dependency, C4 RETRY_AFTER_SECONDS
sole-source claim) — PYAPIFIX-04 + PYAPIFIX-06. This breaks the circular deferral: no downstream
phase can reach these (140.2/140.3 are TypeScript-only; 146 is rate-limit-only).

Purpose: Phase 140.2 renders from `body.detail.detail` and keys the breaker on `dependency`.
Unenforced rules here poison both. "Enforce, don't document" is this phase's stated principle.
Output: 4 new guards in error_contract.py, 4 new test files, 2 doc reconciliations.

Scope fence (explicit, per orchestrator ruling): BUILD the 429 arm; do NOT migrate the two
in-handler 429 sites (`analytics-service/routers/simulator.py:249`, `analytics-service/routers/match.py:1742`)
— deferred to 140.2/146 per TS-23. Do NOT touch `main.py:513-527`'s live 429 handler (TS-07 is a
NEGATIVE obligation: its scalar top-level detail is deliberate).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md  (sections: PYAPIFIX-04, PYAPIFIX-06, Validation Architecture rows 4/6a-6d)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md  (section 4 — Class C sweep; §9-M6)
@analytics-service/services/error_contract.py  (252 lines — read IN FULL)
@analytics-service/docs/STATUS_CONTRACT.md
</context>

<interfaces>
Verified at HEAD 56fb7167 (from PATTERNS §4):
- `_validate` at error_contract.py:82-155 — has NO `detail` parameter. 424 arm :132-142 already
  requires retryable=True on a 4xx (the structural precedent the 429 arm follows). 503 arm
  :108-113 checks dependency membership. >=500 arm :117-127 checks only status/retryable.
  Generic CALLER arm :146-155 raises on retryable=True and (via :99-101) on retry_after.
- `service_error_body` at :158-181 — has the `detail` parameter; ZERO callers outside this module
  (grep-proven): only `service_error:208-209` and `service_error_response:242-243`, and BOTH of
  those also call `_validate` first (:206, :239). Either location covers both entry points.
- `SERVICE_DEPENDENCIES` docstring :52-56; `RETRY_AFTER_SECONDS` :63-75 ({"mt5-gateway": 30, "supabase": 15}).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PYAPIFIX-06 — the 429 arm, the 500-dependency membership guard, and the C4(a) 503 retry_after source guard, in _validate</name>
  <files>analytics-service/services/error_contract.py, analytics-service/tests/test_error_contract_429.py, analytics-service/tests/test_error_contract_500_dependency.py, analytics-service/tests/test_error_contract_retry_after_source.py</files>
  <read_first>
    - analytics-service/services/error_contract.py (ALL 252 lines — every existing guard, both entry points)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md §PYAPIFIX-06 (the exact arm shapes, the C-14 caller enumeration)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §4 (C2/C3/C4 rows; "Who breaks" enumeration; analog rows)
    - analytics-service/tests/test_status_contract_exchange_internal.py:240-260 (the "# Literal, NOT imported" oracle idiom to copy)
  </read_first>
  <behavior>
    RED first (write these tests, watch them fail against HEAD):
    - test_error_contract_429.py: positive — service_error(429, "RATE_LIMITED", retryable=True,
      retry_after=30) constructs; assert its headers Retry-After == "30" as a QUOTED LITERAL in the
      test (never read back from the argument or from RETRY_AFTER_SECONDS). Four rejections, each
      pytest.raises(ValueError): retryable=False · retry_after=None · retry_after=0 · dependency="binance".
      EVERY case run through BOTH service_error(...) and service_error_response(...) (criterion 6c).
    - test_error_contract_500_dependency.py: pytest.raises(ValueError) on
      service_error(500, "X", dependency="binance", retryable=False); NO raise for each of
      dependency="kek", "egress-proxy", "mt5-gateway", and dependency=None. Both entry points.
      (The 4 permitted cases are the guard against the PROHIBITION misreading, which would redden
      7 production sites — PATTERNS §4 enumerates them.)
    - test_error_contract_retry_after_source.py: a 503 with dependency="supabase" and
      retry_after=16 raises; retry_after=15 passes (literals 15/16 in the test file, comment
      "# Literal, NOT imported from RETRY_AFTER_SECONDS"). Same for mt5-gateway 30/31.
      THIRD case (checker W-2): a 503 with dependency="kek" raises ValueError — kek is in
      SERVICE_DEPENDENCIES but has no RETRY_AFTER_SECONDS entry; assert with
      pytest.raises(ValueError), so a bare-index KeyError implementation FAILS this test.
  </behavior>
  <action>
    Three edits inside `_validate` (error_contract.py:82-155), matching the file's existing style
    (raise ValueError with a prose message stating rule + why):
    (a) NEW 429 arm inserted after the 424 block (:132-142) and BEFORE the generic CALLER checks
    (:146-155): if status_code == 429 — raise unless dependency is None; raise unless retryable is
    True (the alternative emits retryable:false beside a Retry-After header, the self-contradicting
    body R-1 forbids — this is why retryable=True was chosen over exempting 429 from :100-101, per
    RESEARCH OQ-4b, decided here, doc follows in Task 3); raise unless retry_after is a positive
    int; then return so the generic CALLER arm never sees a 429. The 424 arm at :139-142 is the
    in-file precedent for a retryable 4xx — cite it in the arm's comment.
    (b) In the existing `if status_code >= 500:` block (:117-127), after the retryable check:
    raise when `dependency is not None and dependency not in SERVICE_DEPENDENCIES`. MEMBERSHIP
    reading is mandatory — do NOT copy the 503 arm's bare membership line verbatim: the 503 arm
    REQUIRES a dependency, the 500 arm PERMITS None (7 production sites pass none:
    main.py:597, :691; exchange.py:492; simulator.py:459; portfolio.py:1197; match.py:1797, :1881).
    (c) C4(a), in the 503 arm (checker W-2 form — NEVER a bare index):
    `_expected = RETRY_AFTER_SECONDS.get(dependency)`; if `_expected is None` raise ValueError
    (message: this dependency has no declared transient arm — a kek/egress-proxy fault is a
    permanent 500 per RETRY_AFTER_SECONDS' docstring); else if `retry_after != _expected` raise
    ValueError (helper-site drift). Rationale: `kek`/`egress-proxy` pass the arm's
    SERVICE_DEPENDENCIES membership check but are deliberately ABSENT from RETRY_AFTER_SECONDS
    (:73-78), so `RETRY_AFTER_SECONDS[dependency]` bare would turn that contract violation into
    an opaque KeyError — contradicting the module's documented posture (docstring :32-36:
    "The guards raise ValueError"). Zero live callers hit it today (all five helper 503 sites
    read RETRY_AFTER_SECONDS[dep]), but the wrong-exception path must not ship.
    PATTERNS §4-M6 verified zero existing callers violate the drift guard.
    Leave the :100-101 and :146-155 rules otherwise untouched. No new type: ignore anywhere
    (cast() if mypy needs narrowing).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_error_contract_429.py tests/test_error_contract_500_dependency.py tests/test_error_contract_retry_after_source.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All three new test files green; each was observed RED before the arms were added (paste the RED counts into the SUMMARY).
    - The positive 429 case asserts headers Retry-After == "30" as a quoted literal.
    - Rejection count: exactly 4 rejection cases in test_error_contract_429.py, each raising through BOTH entry points (8 raise assertions minimum).
    - `grep -n "dependency is not None and dependency not in SERVICE_DEPENDENCIES" analytics-service/services/error_contract.py` → exactly 1 hit (the membership form, not the bare form).
    - Regression fence (criterion 6d): `python3 -m pytest tests/test_status_contract_exchange_internal.py tests/test_status_contract_match_sim_portfolio.py tests/test_match_router.py -q` — all green, zero edits to those files in this plan.
    - `simulator.py:249` and `match.py:1742` appear NOWHERE in this plan's diff (git diff --stat confirms only the files_modified list).
  </acceptance_criteria>
  <done>429-with-Retry-After is constructable with 4 enforced rejections; a 500 cannot name a venue; a 503's retry_after cannot drift from RETRY_AFTER_SECONDS; both entry points enforce all three; zero existing sites regressed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PYAPIFIX-04 — the scalar-detail hard guard in service_error_body</name>
  <files>analytics-service/services/error_contract.py, analytics-service/tests/test_error_contract_scalar_detail.py</files>
  <read_first>
    - analytics-service/services/error_contract.py:158-181 (service_error_body) and :190-252 (both entry points)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §4 ("Where each guard goes" + mypy note) and §8 analog rows (positive-property guard, error_contract.py:96)
  </read_first>
  <behavior>
    RED first: test_error_contract_scalar_detail.py — pytest.raises(ValueError) for detail as each
    of: list, dict, int, bytes. Each non-scalar built IN the test file (never import _FALLBACK_DETAIL
    or any error_contract constant). Each case driven through BOTH service_error and
    service_error_response. Plus two pass-cases: detail=None (falls back) and detail="a string".
  </behavior>
  <action>
    Placement DECISION (recorded): the guard goes in `service_error_body` (:158-181), before the
    dict literal is built — NOT in `_validate`. Grounds: both entry points call both functions
    (RESEARCH C-16 / PATTERNS §9-M6 agree coverage is complete either way), and service_error_body
    already has the `detail` parameter, so this is zero signature churn; putting it in `_validate`
    means widening a private signature and updating both call sites (:206, :239). Consequence:
    Task 1 (in _validate) and this task (in service_error_body) land in DIFFERENT functions and
    cannot collide in one hunk.
    Guard form: positive-property test — raise ValueError when `detail is not None and not
    isinstance(detail, str)`, naming the received type in the message. NEVER enumerate bad types
    (isinstance(detail, (list, dict)) misses int/bytes/pydantic models — the Class-A lesson).
    NEVER coerce with str() (emitting "['a', 'b']" to a user is the same defect in a string
    costume). mypy: the narrowing is statically dead under `detail: str | None` — if mypy --strict
    complains, widen the parameter at the boundary and cast() after the guard; `# type: ignore`
    is BANNED.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_error_contract_scalar_detail.py -q && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - All 4 non-scalar cases raise through both entry points (8 raise assertions); observed RED before the guard, GREEN after.
    - mypy --strict output is exactly "Success: no issues found in 89 source files" (baseline preserved; 0 new type: ignore — `grep -rn "type: ignore" analytics-service/services/error_contract.py` → 0 hits).
    - The guard tests the positive property (`isinstance(detail, str)`); `grep -n "isinstance(detail, (list" analytics-service/services/error_contract.py` → 0 hits.
  </acceptance_criteria>
  <done>The body.detail.detail scalar guarantee is a construction-time ValueError, matching every other class rule in the module (ROADMAP criterion 4).</done>
</task>

<task type="auto">
  <name>Task 3: Doc-and-code reconciliation (C-21) + full-suite fence</name>
  <files>analytics-service/services/error_contract.py, analytics-service/docs/STATUS_CONTRACT.md</files>
  <read_first>
    - analytics-service/docs/STATUS_CONTRACT.md:32-40 (the class table CALLER row) and :270-280 (the live-handler note)
    - analytics-service/services/error_contract.py:63-69 (the RETRY_AFTER_SECONDS docstring)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md C-21 (why both doc lines are already false at HEAD)
  </read_first>
  <action>
    Two doc edits, in the SAME commit as (or a commit after, but same plan as) Task 1's arm —
    the arm ships already-falsified otherwise:
    (1) STATUS_CONTRACT.md:34 — split `429` out of the CALLER row into its own row:
    retryable: true, Retry-After: required, dependency: forbidden, breaker: never counts.
    (2) error_contract.py:63-69 docstring — the claim "These are the ONLY Retry-After integers in
    the service" is FALSE at HEAD (internal.py:227 inlines str(int(_RATE_LIMIT_WINDOW_S));
    main.py:526 mints the limiter-window value at runtime). Narrow it to "the only PER-DEPENDENCY
    Retry-After integers", and name the two helper-bypassing sites in the docstring so C4's
    unguardable half is documented, not silently half-closed (PATTERNS C4 row: closing C4(a) alone
    and declaring C4 shut is forbidden).
    Then run the plan-wide fence.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -q 2>&1 | tail -3 && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ONLY Retry-After" analytics-service/services/error_contract.py` → 0 hits of the unqualified claim; the narrowed per-dependency wording present.
    - STATUS_CONTRACT.md has a dedicated 429 row (grep "429" shows it outside the 400/401/403/404/422 row).
    - Full analytics-service pytest run green (do NOT exclude csv_validator/middleware/mt5 modules — pandera 0.32.1 IS installed, collection is clean at 4714 tests; the CONTEXT constraint saying otherwise is STALE per C-5. If reds appear there, treat as real).
    - mypy: "Success: no issues found in 89 source files".
  </acceptance_criteria>
  <done>The new 429 arm, its doc row, and the corrected sole-source claim are consistent; full suite + mypy green (wave-1 gate encoded, not assumed).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → analytics API | error envelopes cross to untrusted callers |
| analytics API → 140.2 breaker | `dependency` on 5xx becomes a breaker key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.1-01 | Information disclosure | service_error_body detail passthrough | mitigate | C1 guard: non-scalar detail (lists/dicts carrying internal structures, exc.args) raises at construction instead of reaching the wire |
| T-140.1.1-02 | Denial of service | breaker keyed on dependency (SEAMCORE-01) | mitigate | C3 membership guard: a venue name on a 500 can no longer mint a per-dependency breaker key for something that is not ours |
| T-140.1.1-03 | Tampering (retry storms) | Retry-After values | mitigate | C4(a) guard ties helper-site 503 Retry-After to RETRY_AFTER_SECONDS; 429 arm requires a positive value |
| T-140.1.1-SC | Tampering | package installs | accept | zero new packages in this plan |
</threat_model>

<verification>
- All 4 new test files green; each observed RED first against HEAD (paste counts in SUMMARY).
- Regression fence: test_status_contract_exchange_internal.py + test_status_contract_match_sim_portfolio.py + test_match_router.py green, unedited.
- Full `python3 -m pytest -q` green; mypy --strict "Success: no issues found in 89 source files"; 0 new `# type: ignore`.
- Scope fence honored: no diff in routers/simulator.py, routers/match.py, main.py.
- C5–C10 untouched (checker I-3): `git diff analytics-service/services/error_contract.py`
  touches ONLY the 429 arm, the >=500 arm, the 503 arm, `service_error_body`, and the
  RETRY_AFTER_SECONDS docstring — no other hunks.
</verification>

<success_criteria>
ROADMAP criteria 4, 6a, 6b (+6c/6d observation rows from RESEARCH Validation Architecture) are
observably true via the commands above. PYAPIFIX-04 and PYAPIFIX-06 closed.
</success_criteria>

<output>
Create `.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-01-SUMMARY.md` when done.
Record in it: the RED-first evidence per test file, the placement decision (service_error_body, grounds), and the retryable:true-on-429 decision (OQ-4b, taken here, doc moved with it).
</output>
