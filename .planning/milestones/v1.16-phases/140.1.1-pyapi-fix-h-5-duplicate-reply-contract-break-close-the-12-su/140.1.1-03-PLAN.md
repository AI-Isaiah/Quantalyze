---
phase: 140.1.1-pyapi-fix
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/process-key-onboard-contract.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - analytics-service/tests/fixtures/process_key_onboard_contract.json
  - analytics-service/tests/test_process_key_onboard_contract.py
autonomous: true
requirements: [PYAPIFIX-01]
must_haves:
  truths:
    - "isProcessKeyOnboardResponse and ProcessKeyOnboardResponse exist as EXPORTED symbols in a pure src/lib leaf module, and route.ts imports them back — exactly ONE predicate, not a copy"
    - "A committed fixture's positive cases are proven equal (key-for-key, type-for-type) to REAL TestClient responses from the REAL /process-key duplicate arm — the Python side of the contract is bound to the artifact"
    - "A deliberate one-key edit to the fixture reddens the pytest (the equality is live, not decorative)"
  artifacts:
    - path: src/lib/process-key-onboard-contract.ts
      provides: "the extracted predicate + type, verbatim from route.ts:1424-1460, exported, zero imports"
    - path: analytics-service/tests/fixtures/process_key_onboard_contract.json
      provides: "5 cases: 2 positive (real emitted shapes) + 3 negative (shapes the guard must reject)"
    - path: analytics-service/tests/test_process_key_onboard_contract.py
      provides: "real full-stack TestClient response == fixture positive cases; silent-shrink guard"
  key_links:
    - from: src/app/api/strategies/finalize-wizard/route.ts
      to: src/lib/process-key-onboard-contract.ts
      via: "import — route behavior unchanged, single source of truth"
      pattern: "process-key-onboard-contract"
    - from: analytics-service/tests/test_process_key_onboard_contract.py
      to: analytics-service/tests/fixtures/process_key_onboard_contract.json
      via: "equality against a real TestClient body"
      pattern: "process_key_onboard_contract"
---

<objective>
PYAPIFIX-01, Python-side + Wave-1 prerequisites (Class E). Two artifacts every later assertion
depends on: (1) the TS guard predicate extracted to a pure `src/lib/` leaf (it is NOT exported
today — declared `function` at route.ts:1433, file ends at 1460; importing route.ts from a test
needs 10 `vi.mock` blocks including next/server and server-only, which is exactly the
"second pair of mocks" defect PYAPIFIX-01 forbids — PATTERNS §6, feasibility RESOLVED);
(2) the committed shared JSON fixture + the pytest that proves it equals what the REAL route
emits. The fixture MUST be committed, not generated: ci.yml:206 (frontend-test) and :933 (python)
are sibling jobs with NO needs: edge — a generated golden file is impossible (PATTERNS §6 CI table).

Precedent to copy wholesale: `tests/lib/composite/window-overlap-convention-parity.test.ts:29` ↔
`analytics-service/tests/test_stitch_composite.py:38` sharing
`analytics-service/tests/fixtures/window_overlap_convention.json`; `vitest.config.ts:30-33`
documents the TS↔Python parity glob as intentional.

This plan does NOT change the guard's logic (plan 05 does, on the extracted module). Python
production code is NOT touched — the TypeScript side is the side that changes (orchestrator
decision 1), and it changes in plan 05.

Output: extracted leaf module, rewired route.ts, committed fixture, fixture-equality pytest.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md  (§6 Class E — precedents E-P1..E-P4, artifact locations, feasibility resolution, verified facts)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md  (§PYAPIFIX-01 — mechanism table, recommended shape, OQ-1)
@src/app/api/strategies/finalize-wizard/route.ts
@analytics-service/routers/process_key.py
@tests/lib/composite/window-overlap-convention-parity.test.ts
@analytics-service/tests/test_stitch_composite.py
</context>

<interfaces>
Verified at 56fb7167 (PATTERNS §6):
- `type ProcessKeyOnboardResponse` route.ts:1424-1431 (NOT exported); `function
  isProcessKeyOnboardResponse(body: unknown): body is ProcessKeyOnboardResponse` :1433-1460, EOF
  (NOT exported; the queued:true arm incl. the mixed-envelope rejection is :1437-1445). The three
  previously-cited ranges (:1433-1444/-1450/-1462) are ALL wrong — true extent :1433-1460 (§9-M10).
- The predicate is PURE: reads only its body argument — no imports, no closures, no module state.
  Extraction drags nothing.
- Producer: `_wizard_duplicate_reply` process_key.py:661-690; returned dict :680-690 carries
  ok, code, idempotent, verification_id, status, trust_tier, correlation_id, queued, job_state.
  Two emitters :1173 and :1250 (one shared builder). `_resume_duplicate_job` :587-660 returns
  (False, "not_applicable") for non-long-fetch or non-draft rows; (True, "enqueued"|"running")
  after a real enqueue.
- Duplicate path gate: process_key.py:976-979 — requires non-teaser flow_type AND a caller-supplied
  context.wizard_session_id. The pytest must construct this explicitly (H-5 is latent; no live
  caller reaches it — RESEARCH C-3).
- Full-stack pytest fixture precedent: analytics-service/tests/test_process_key.py:267-278
  (main.app + verify_service_key). Use the FULL stack (RESEARCH OQ-1 resolved: a contract test
  that bypasses the auth middleware is not proving the wire shape), NOT the bare FastAPI() app
  at :88-106.
- Fixture naming convention: flat JSON, snake_case, no subdirectory, in
  analytics-service/tests/fixtures/ (siblings enumerated in PATTERNS §6).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract the predicate + type to src/lib/process-key-onboard-contract.ts (zero behavior change)</name>
  <files>src/lib/process-key-onboard-contract.ts, src/app/api/strategies/finalize-wizard/route.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.ts:1400-1460 (the doc-comment block :1406-1424, the type :1424-1431, the predicate :1433-1460 — move the COMMENT with the code; plan 05 will invert its claim)
    - src/lib/wizardErrors.ts:9-25 (the leaf-module convention: zero imports, zero env reads, zero module-load side effects — copy the LOAD-BEARING-IMPORT-PATH comment idiom)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §6 (feasibility resolution — why export-in-place is unusable)
    - AGENTS.md discharge (checker I-4): PATTERNS §6's route-export finding — the repo already
      exports non-route symbols from route files on Next 16.2.11, and this task only deletes
      lines from route.ts and adds one import (no new Next.js API surface). That verified
      precedent is the explicit discharge of the AGENTS.md read-node_modules/next/dist/docs
      directive for this edit.
  </read_first>
  <action>
    Create `src/lib/process-key-onboard-contract.ts`: move route.ts:1424-1460 VERBATIM (the type,
    the predicate, and the :1406-1424 doc-comment block above them), add `export` to both symbols,
    add a header comment following the wizardErrors.ts:9-25 leaf idiom naming both consumers (the
    route and the parity test plan 05 adds) and stating the zero-imports invariant. Do NOT alter
    any predicate logic — the mixed-envelope rejection line stays for now (plan 05 owns the
    semantic change; this plan is pure motion, so a diff of the moved bytes is empty).
    In route.ts: delete :1406-1460, add one import of both symbols from
    `@/lib/process-key-onboard-contract`. No other route.ts change. Scope-fence statement for the
    record (PATTERNS Feasibility Risk 1): CONTEXT's "out of scope: everything TypeScript beyond
    the one contract test" fences the 140.2/140.3 TS surface, NOT this one file PYAPIFIX-01
    names; CONTEXT's own "decide deliberately WHICH side changes" clause sanctions it.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts 2>&1 | tail -5 && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/process-key-onboard-contract.ts has ZERO import statements (`grep -c "^import" src/lib/process-key-onboard-contract.ts` → 0).
    - Both symbols exported: `grep -n "export function isProcessKeyOnboardResponse\|export type ProcessKeyOnboardResponse" src/lib/process-key-onboard-contract.ts` → 2 hits.
    - route.ts imports them: `grep -n "process-key-onboard-contract" src/app/api/strategies/finalize-wizard/route.ts` → 1 hit; the file no longer declares the predicate (`grep -c "function isProcessKeyOnboardResponse" route.ts` → 0).
    - Existing route.test.ts fully green UNEDITED (zero behavior change proven by the strictest available oracle: the suite that pins the current guard).
    - tsc --noEmit clean; npm run lint clean.
  </acceptance_criteria>
  <done>Exactly one predicate exists, in a pure exported leaf; route.ts consumes it; all existing TS tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Committed fixture + fixture-equality pytest against the REAL duplicate arm</name>
  <files>analytics-service/tests/fixtures/process_key_onboard_contract.json, analytics-service/tests/test_process_key_onboard_contract.py</files>
  <read_first>
    - analytics-service/tests/test_process_key.py:88-111 and :267-278 (both app fixtures — use the FULL-stack one; copy its service-key/middleware setup) plus the file's supabase/RPC stubbing idiom for the duplicate pre-check and enqueue paths
    - analytics-service/routers/process_key.py:587-690 and :955-1000, :1113-1260 (what state produces queued:true vs queued:false; what the reply carries)
    - analytics-service/tests/test_stitch_composite.py:37-94 (the fixture-loader + equality idiom) and tests/lib/composite/window-overlap-convention-parity.test.ts:44-55 (the silent-shrink guard to mirror)
  </read_first>
  <action>
    Create `analytics-service/tests/fixtures/process_key_onboard_contract.json`: a top-level
    object with a `consumers` array naming both readers (this pytest and
    `tests/lib/process-key-onboard-contract-parity.test.ts`, added by plan 05), a header note
    forbidding relocation (copy E-P1's idiom), and a `cases` array of exactly 5 entries, each
    `{name, body, expect_valid}`:
    P1 `queued_true_resumed_wedge` — body: ok true, code "WIZARD_DUPLICATE", idempotent true,
    verification_id (uuid string), status, trust_tier, correlation_id, queued true, job_state
    "enqueued"; expect_valid true.
    P2 `queued_false_finished_duplicate` — same keys with queued false, job_state
    "not_applicable"; expect_valid true.
    N1 `queued_absent` — a body with no queued key; expect_valid false.
    N2 `empty_code` — queued false arm with code ""; expect_valid false.
    N3 `idempotent_without_duplicate_code` — idempotent true with code "OTHER"; expect_valid false.
    (The three negatives are what stops the plan-05 widening from degrading to () => true —
    RESEARCH §PYAPIFIX-01 recommended shape, step 1.)
    Create `tests/test_process_key_onboard_contract.py`: drive the REAL `/process-key` route
    through the FULL main.app TestClient down BOTH duplicate arms — construct the gate explicitly:
    non-teaser flow_type + context.wizard_session_id + a stubbed existing strategy_verifications
    row (status "draft" with a resumable long-fetch job stub → P1; a non-draft status → P2),
    following test_process_key.py's existing stubbing idiom. For each positive case assert against
    the fixture: sorted key set EQUAL; per-key python type matches the fixture value's type; and
    for the stable discriminant keys (ok, code, idempotent, queued, job_state) values EQUAL.
    Dynamic keys (verification_id, correlation_id, status, trust_tier) are type-checked, not
    value-pinned — record this split in a comment. Silent-shrink guard: assert len(cases) == 5
    (literal) and that both expect_valid values appear. The test must NOT import anything from
    routers.process_key to build its expectations — the fixture is the only oracle.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_process_key_onboard_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - Pytest green driving the REAL route (grep the test file: `TestClient` present; `main` imported; zero monkeypatching of `_wizard_duplicate_reply` itself — stubs allowed only at the DB/RPC boundary).
    - Falsifiability probe, executed and recorded: edit ONE key name in the fixture's P1 body → pytest RED (paste the failure line) → revert → GREEN. Working tree clean after (`git status --porcelain` shows only the intended new files).
    - Fixture has exactly 5 cases, 2 expect_valid true / 3 false (`python3 -c` count check or jq).
    - Fixture committed content is valid JSON (`python3 -m json.tool < tests/fixtures/process_key_onboard_contract.json > /dev/null`).
    - Full targeted regression: `python3 -m pytest tests/test_process_key.py -q` still green (no interference with existing stubs).
  </acceptance_criteria>
  <done>The committed artifact provably equals what the real route emits on both duplicate arms; the negatives are in place for plan 05's TS side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python service → TS route consumer | the cross-process reply shape this plan pins |
| test fixtures → CI | a committed artifact both CI jobs read independently |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.1-07 | Tampering | fixture drift | mitigate | pytest equality against a real TestClient body — a drifted fixture or a drifted emitter reddens CI (falsifiability probe executed in-task) |
| T-140.1.1-08 | Spoofing | contract test bypassing auth middleware | mitigate | FULL main.app stack with verify_service_key (OQ-1 resolved) — the wire shape is proven through the real gate order |
| T-140.1.1-09 | Information disclosure | fixture contents | mitigate | fixture carries synthetic uuids only; no credentials, no real tenant data |
| T-140.1.1-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- Extraction: zero-behavior-change proven by the unedited existing route.test.ts suite + tsc + lint.
- Fixture equality: green against the real route; one-key mutation probe RED→revert→GREEN, recorded.
- No Python production file modified in this plan (git diff --stat confirms).
</verification>

<success_criteria>
ROADMAP criterion 1's Python half + Wave-1 prerequisites: the real-reply artifact exists,
committed, proven; the real predicate is importable without a single mock. Plan 05 completes
criterion 1.
</success_criteria>

<output>
Create `.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-03-SUMMARY.md` when done.
Record: the fixture falsifiability probe evidence, the stubbing boundary used for each duplicate arm.
</output>
