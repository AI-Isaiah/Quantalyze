---
phase: 140.1.1-pyapi-fix
plan: 05
type: execute
wave: 2
depends_on: [140.1.1-03]
files_modified:
  - src/lib/process-key-onboard-contract.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
  - tests/lib/process-key-onboard-contract-parity.test.ts
  - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
autonomous: true
requirements: [PYAPIFIX-01]
must_haves:
  truths:
    - "The real TS predicate ACCEPTS both real Python duplicate replies (queued:true and queued:false, each with code/idempotent) — the mixed-envelope rejection is gone (ROADMAP criterion 1)"
    - "The widened predicate still REJECTS all three negative fixture cases — teeth bought back, not traded away"
    - "Neutering the Python emitter reddens the pytest; neutering the TS predicate reddens the vitest — the oracle fails from BOTH directions, each observed first-hand"
    - "TS-01/TS-02/TS-03 match what landed; TS-23 annotated UNBLOCKED; two new rows added (TS-05↔M-4 pairing, M-11 wizard_session_id) — 140.2 cannot redo or undo this work"
  artifacts:
    - path: tests/lib/process-key-onboard-contract-parity.test.ts
      provides: "real predicate over the committed fixture — all 5 cases, silent-shrink guard, zero mocks"
    - path: src/lib/process-key-onboard-contract.ts
      provides: "widened union + 3 new invariants; mixed-envelope rejection deleted; comment inverted"
  key_links:
    - from: tests/lib/process-key-onboard-contract-parity.test.ts
      to: analytics-service/tests/fixtures/process_key_onboard_contract.json
      via: "readFileSync — the ONLY shared artifact; neither side imports the other's expectations"
      pattern: "process_key_onboard_contract"
---

<objective>
PYAPIFIX-01, TS side (LOCKED, orchestrator decision 1: the TypeScript consumer changes). `queued`
(a job fact) and `duplicate` (a submission fact) are ORTHOGONAL — `_resume_duplicate_job` exists
precisely to produce the state where both are true; the guard encodes a mutual exclusion the
domain does not have. The guard is NOT uniformly strict: its queued:true arm already silently
permits ok, job_state, status, trust_tier, correlation_id — it singles out exactly the falsified
premise. Deleting that one line removes an untrue assertion; the SAME change must buy the teeth
back: three new invariants plus three negative fixture cases (all six planned here).

No rollout constraint exists (nothing on this branch is deployed — RESEARCH C-12); no Python-side
option was TypeScript-free anyway (route.ts:1355-1367 drops job_state — C-4).

Also owned here: the TS-OBLIGATIONS ledger reconciliation and the M-11 recorded decision — this
plan is the SOLE owner of `140.1-TS-OBLIGATIONS.md` in this phase (plan 02 hands off its M-4 row
here).

Output: widened predicate with teeth, parity test, rewritten route.test case, reconciled ledger,
both-direction neuter proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md  (§PYAPIFIX-01 "WHICH SIDE CHANGES" + buy-the-teeth-back table; §TS-OBLIGATIONS Reconciliation; §Newly Folded Findings M-11)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md  (§6 — "the test that must be REWRITTEN, not deleted"; E-P1 silent-shrink idiom)
@src/lib/process-key-onboard-contract.ts
@tests/lib/composite/window-overlap-convention-parity.test.ts
@.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
</context>

<interfaces>
From plan 03 (wave 1, landed):
- src/lib/process-key-onboard-contract.ts exports ProcessKeyOnboardResponse +
  isProcessKeyOnboardResponse, moved verbatim — the mixed-envelope rejection line and the moved
  doc-comment block (formerly route.ts:1406-1424) still present.
- analytics-service/tests/fixtures/process_key_onboard_contract.json — 5 cases (P1 queued:true
  resumed wedge; P2 queued:false finished duplicate; N1 queued absent; N2 code ""; N3
  idempotent:true with code "OTHER"), pytest-proven equal to real TestClient bodies.
Existing TS test to rewrite (never delete): src/app/api/strategies/finalize-wizard/
route.test.ts:1474 "rejects mixed envelope (queued=true with code field) with 502", intent
comment :1470-1473.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parity test (RED) → widen the predicate with the three new invariants (GREEN)</name>
  <files>tests/lib/process-key-onboard-contract-parity.test.ts, src/lib/process-key-onboard-contract.ts</files>
  <read_first>
    - src/lib/process-key-onboard-contract.ts (the whole leaf — union, queued:true arm, rejection line, doc comment)
    - tests/lib/composite/window-overlap-convention-parity.test.ts:1-60 (header naming both consumers, resolve(process.cwd(), ...), readFileSync, silent-shrink guard :47-55, it.each)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md §PYAPIFIX-01 (the exact three-invariant table)
  </read_first>
  <behavior>
    Write the parity test FIRST: readFileSync the fixture from
    analytics-service/tests/fixtures/process_key_onboard_contract.json (resolve from
    process.cwd(), E-P1 idiom); silent-shrink guard asserting exactly 5 cases with both
    expect_valid values present (literals); it.each over cases asserting
    isProcessKeyOnboardResponse(case.body) === case.expect_valid. Run it: P1 FAILS against the
    unmodified strict predicate — the RED proving the test exercises the real guard. Then widen;
    all 5 pass.
  </behavior>
  <action>
    In src/lib/process-key-onboard-contract.ts:
    (1) DELETE the mixed-envelope rejection in the queued:true arm — the single line
    testing `"code" in r || "idempotent" in r`.
    (2) ADD three invariants (RESEARCH buy-the-teeth-back table): (a) queued:true arm keeps
    requiring verification_id to be a string — do not weaken; (b) on EITHER arm, when "code" is
    present it must be a non-empty string (kills N2); (c) when "idempotent" is present it must be
    true AND code must equal "WIZARD_DUPLICATE" — the iff pairing the emitter guarantees at
    process_key.py:680-690 (kills N3).
    (3) Widen the ProcessKeyOnboardResponse union so the queued:true variant admits optional
    code/idempotent (and job_state if the union names it) — exactly TS-01's prescription: widen
    the union, delete the rejection, prefer discriminating on ok + job_state.
    (4) INVERT the moved doc comment (TS-03): {queued:true, code:"WIZARD_DUPLICATE"} is NOT a
    backbone bug — it is the normal resumed-wedge reply: "a duplicate submission whose wedged job
    we re-enqueued". Same commit as the widening (the ledger's own warning: fix both or the guard
    grows back).
  </action>
  <verify>
    <automated>npx vitest run tests/lib/process-key-onboard-contract-parity.test.ts 2>&1 | tail -5 && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Parity test observed RED first (P1 rejected by the strict predicate — paste the failure), GREEN after widening; N1/N2/N3 all still rejected.
    - `grep -c '"code" in r' src/lib/process-key-onboard-contract.ts` → 0 (the rejection line is gone).
    - A WIZARD_DUPLICATE literal appears inside the predicate (the iff invariant).
    - `grep -c "vi.mock" tests/lib/process-key-onboard-contract-parity.test.ts` → 0 — the fixture is the only shared artifact; zero mocks.
    - tsc --noEmit clean.
  </acceptance_criteria>
  <done>The real predicate accepts both real reply shapes and rejects all three negatives; one false assertion deleted, three true ones added.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the pinned rejection test + coverage gate</name>
  <files>src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/finalize-wizard/route.test.ts:1455-1500 (the :1474 test + intent comment :1470-1473; the STATE.processKeyResult stubbing idiom)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §6 ("The test that must be REWRITTEN, not deleted")
  </read_first>
  <action>
    Rewrite route.test.ts:1474: it now asserts the OPPOSITE for the real shape — a queued:true
    body carrying code "WIZARD_DUPLICATE" + idempotent true + string verification_id flows through
    the route WITHOUT the 502/Sentry arm (the resumed-wedge success path, forwarded per
    :1344-1354). RETAIN a negative case in the same describe block proving the guard still bites
    on a genuinely malformed body — use N2's shape (code "") or N3's (idempotent true, code
    "OTHER") and assert the 502 + "process-key onboard contract violation" capture still fires.
    Update the test name and the intent comment to the inverted contract. Then run the coverage
    gate (RESEARCH OQ-6: the extraction's coverage delta is unmeasured and frontend-coverage is a
    BLOCKING gate — thresholds lines 82 / stmts 80 / fn 74 / branches 72).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts 2>&1 | tail -5 && npm run test:coverage 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - The rewritten case asserts acceptance of the real shape; the retained negative still hits the 502 arm (both green).
    - Zero deleted tests: route.test.ts's test count >= pre-edit count (record both numbers in the SUMMARY).
    - `npm run test:coverage` passes all four thresholds (paste the summary line — this settles OQ-6).
  </acceptance_criteria>
  <done>The route-level pin matches the widened contract and keeps a live negative; coverage proven, not assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Both-direction neuter proof + TS-OBLIGATIONS reconciliation + M-11 record</name>
  <files>.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md</files>
  <read_first>
    - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md (rows TS-01, TS-02, TS-03, TS-23; the header rule "correct it here — do not fork a second list"; do NOT touch TS-04..TS-22 or TS-24..TS-31)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md §TS-OBLIGATIONS Reconciliation (per-row edits under the "TS changes" column) and §Newly Folded Findings M-11
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-VALIDATION.md (Cross-Process Contract Gate + Mutation Re-Run Protocol — both BLOCKING)
  </read_first>
  <action>
    (1) Both-direction neuter proof (BLOCKING gate): Direction A — mutate
    analytics-service/routers/process_key.py `_wizard_duplicate_reply` (single line: rename the
    "code" key to "codes"); run `python3 -m pytest tests/test_process_key_onboard_contract.py -q`;
    observe RED, capture the assertion text; revert; observe GREEN. Direction B — mutate the
    extracted predicate (single line: first statement `return true`); run the parity vitest;
    observe RED (the three negative cases now pass wrongly → assertion failures); revert; GREEN.
    Protocol per VALIDATION.md: scratch copy OUTSIDE the repo first, single-line mutation
    (occurrence count before/after), targeted test only, restore, `git status --porcelain` clean,
    `grep -rn MUTANT analytics-service/ src/` empty. NEVER `git stash`/`git checkout` (live
    .planning ledgers have no git backup).
    (2) Ledger edits in 140.1-TS-OBLIGATIONS.md (surgical row edits; wc -l before/after in the
    SUMMARY): TS-01 — Owner 140.2 → 140.1.1, mark DONE-IN-140.1.1, append the three compensating
    invariants so 140.2 does not re-widen further, add pointers to the shared fixture and the
    extracted leaf module. TS-02 — owner stays 140.2; sharpen the remedy to "re-point at
    code === 'WIZARD_DUPLICATE' alone, dropping the queued conjunct". TS-03 — mark
    DONE-IN-140.1.1 (comment inverted in the same commit as TS-01; note the true comment extent
    was :1406-1424, wider than the cited :1419-1420). TS-23 — annotate "UNBLOCKED by 140.1.1
    PYAPIFIX-06(a): the helper now constructs a 429 with Retry-After"; stays open, owner
    140.2/146. ADD row (i): pair "TS-05 lands" with "remap routers/exchange.py:145/:152/:505 to
    424" (plan 02's M-4 carve-out, BLOCKED-BY: TS-05), owner 140.2/140.3. ADD row (ii): "wizard
    payload sends wizard_session_id" (M-11), owner = whichever phase touches the wizard payload,
    STRICTLY AFTER PYAPIFIX-01.
    (3) M-11 recorded decision in this plan's SUMMARY (the record IS the deliverable;
    implementation is NOT): onboard's dedupe is not broken, it is UNWIRED (no live caller sends
    wizard_session_id); the remedy is ONE TS payload field at finalize-wizard route.ts:840-851,
    not a new idempotency subsystem (REJECTED: porting scenario/commit's Idempotency-Key — needs
    a migration + the M-19 no-branch-protection hazard; REJECTED: harden as dead code — fixes
    nothing real); STRICTLY ordered after PYAPIFIX-01 (adding the field first converts H-5 from
    latent to a live 502); now a ledger row.
  </action>
  <verify>
    <automated>grep -n "DONE-IN-140.1.1" "/Users/helios-mammut/claude-projects/quantalyze/.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md" && grep -n "BLOCKED-BY: TS-05" "/Users/helios-mammut/claude-projects/quantalyze/.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md" && git -C /Users/helios-mammut/claude-projects/quantalyze status --porcelain | grep -v "^??" | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Both neuter directions observed first-hand with pasted failure text in the SUMMARY; working tree clean after both cycles.
    - TS-01 and TS-03 carry DONE-IN-140.1.1; TS-23 carries the UNBLOCKED annotation; the two new rows exist (grep hits above).
    - TS-04..TS-22 and TS-24..TS-31 byte-unchanged (diff of the ledger shows edits only in the named rows + the two additions).
    - The M-11 decision appears in the SUMMARY with the option chosen, both rejected options, and the ordering constraint.
  </acceptance_criteria>
  <done>The cross-process oracle is proven bidirectional; the ledger 140.2 reads matches reality; M-11 is decided and recorded, not implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python service → TS route consumer | the duplicate-reply contract this plan widens |
| wizard render ← route forward arms | code/idempotent drive "already submitted" UX |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.1-13 | Tampering | predicate degradation to () => true | mitigate | three negative fixture cases + Direction-B neuter proof — a vacuous predicate reddens the parity test |
| T-140.1.1-14 | Spoofing | malformed upstream bodies reaching the wizard | mitigate | teeth bought back: non-empty code, idempotent↔WIZARD_DUPLICATE iff, verification_id string retained; the 502 arm still fires on real malformation (retained negative in route.test.ts) |
| T-140.1.1-15 | Repudiation | ledger drift misleading 140.2 | mitigate | TS-01/02/03/23 corrected in place per the ledger's own no-fork rule; unowned rows untouched |
| T-140.1.1-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- Parity test green over all 5 fixture cases with zero mocks; route.test.ts rewritten, count non-decreasing.
- Both-direction neuter proof recorded with verbatim failure text; tree clean.
- Coverage thresholds pass; tsc + lint clean.
- Ledger: 2 DONE rows, 1 UNBLOCKED annotation, 2 new rows, everything else byte-identical.
</verification>

<success_criteria>
ROADMAP criterion 1 fully observable (RESEARCH Validation row 1): pytest and vitest both bound to
the same committed fixture, each to its own REAL implementation; a one-key fixture edit reddens
BOTH sides. PYAPIFIX-01 closed; M-11 decided; ledger reconciled.
</success_criteria>

<output>
Create `.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-05-SUMMARY.md` when done.
Record: RED-first parity evidence, both neuter directions with verbatim failures, the coverage table, the M-11 decision block, the ledger wc -l before/after.
</output>
