---
phase: 140.1.1-pyapi-fix
plan: 07
type: execute
wave: 4
depends_on: [140.1.1-03, 140.1.1-04, 140.1.1-05, 140.1.1-06]
files_modified:
  - analytics-service/tests/test_limiter_identity.py
  - analytics-service/tests/test_rate_limit_contract.py
  - analytics-service/tests/test_tenant_claim_parsing.py
autonomous: true
requirements: [PYAPIFIX-05]
must_haves:
  truths:
    - "Survivors #3/#4 (default_platform_key → '' / per-request-unique), #5 (Retry-After hard-coded 1), and #8–#11 (four claim-parser guards, including the :417 sibling of #11) each turn a named test RED — re-run and observed first-hand ON THE INSTALLED slowapi VERSION, which is recorded (ROADMAP criterion 5)"
    - "default_platform_key returning '' can no longer ship green — the behavioural oracle drives real requests and asserts the (N+1)th is 429 (an empty key makes slowapi SKIP limiting entirely)"
    - "The #11 mutation is run at BOTH sites — rate_limit.py:333 (tenant_rate_limit_key) AND :417 (tenant_or_platform_key, the key func for the nine ex-IP-keyed routes) — closing 2 of 2, not 1 of 2 (PATTERNS §9-M9)"
    - "Phase-wide gates green: full pytest, mypy --strict, vitest tests/lib, collection count sane"
  artifacts:
    - path: analytics-service/tests/test_tenant_claim_parsing.py
      provides: "claim-parser oracles for #8–#11 — the four guards had NO test at all"
  key_links:
    - from: analytics-service/tests/test_rate_limit_contract.py
      to: analytics-service/main.py
      via: "tight-band Retry-After oracle reading the response header the handler mints"
      pattern: "Retry-After"
---

<objective>
PYAPIFIX-05, batch 2: the three slowapi-DEPENDENT survivors (#3, #4, #5) and the four untested
claim-parser guards (#8–#11), plus the phase-wide gate run. (Wave-4 placement, checker B-1:
this plan carries the phase-wide gate and merges plan 06's mutation ledger — it MUST run
after plan 06 has landed its five re-toothed test files; depends_on encodes it.)

Version constraint (LOCKED): the review measured every slowapi-dependent mutation on 0.1.9;
`analytics-service/requirements.txt:226` pins **0.1.10** (the version CI runs). Any "observed
RED" claim for #3/#4/#5 made on 0.1.9 is NOT evidence (PATTERNS Feasibility Risk 3). Sync the
local env to the pinned 0.1.10 FIRST, record the version, and run every cycle on it. The
dependencies at risk: `__evaluate_limits`' `if all(args)` empty-key skip and
`request.state.view_rate_limit`'s shape — both slowapi internals a patch release can move.
(This is an env sync to an already-declared, already-pinned dependency — requirements.txt:226 —
not a new-package install; no legitimacy checkpoint applies.)

Subject-line corrections to honor (trust PATTERNS over the review): #8's guard is at
rate_limit.py:274 (`:150` is only the constant declaration — a "test line 150" instruction
produces a self-referential constant assertion, §9-M7); #5's mutation targets the computation
(`_retry_after_seconds`, main.py:470-487) while the oracle reads the HEADER minted at main.py:526
(§9-M8); #11 has the unnamed sibling at :417 (§9-M9).

Output: 3 behavioural limiter oracles + 1 new claim-parser test file, 8 recorded mutation cycles,
the phase gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-RESEARCH.md  (§PYAPIFIX-05 rows 3,4,5,8,9,10,11; §Validation Architecture protocol; OQ-5)
@.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md  (§5 rows 3,4,5,8-11; §9-M7/M8/M9; Feasibility Risk 3)
@analytics-service/services/rate_limit.py
@analytics-service/tests/test_limiter_identity.py
@analytics-service/tests/test_rate_limit_contract.py
</context>

<interfaces>
Verified subjects/oracles (PATTERNS §5):
- #3/#4: subject services/rate_limit.py:208 `return _platform_bucket(request)`
  (default_platform_key); weak oracle test_limiter_identity.py:483 asserts function IDENTITY —
  true for any body. rate_limit.py:152-154 documents the hazard: an empty key makes slowapi SKIP
  the limit entirely. Behavioural driver analog: test_rate_limit_contract.py's _trip_route_a
  (N+1 requests → 429).
- #5: computation main.py:470-487 (_retry_after_seconds); header minted main.py:526; weak oracle
  test_rate_limit_contract.py:227-229 `assert 0 < seconds <= _ROUTE_A_WINDOW_S` (1 passes).
- #8: guard rate_limit.py:274 `if not raw or len(raw) > _CLAIM_MAX_CHARS:` (constant at :150).
- #9: rate_limit.py:285 `raw.rsplit(".", 2)`. #10: :286 `if not payload: return None`.
- #11: :333 `if payload == ANON_PAYLOAD:` in tenant_rate_limit_key AND the identical :417 in
  tenant_or_platform_key (nine routes depend on the latter).
- Claim minting for tests: the HMAC recipe is pinned at tests/test_process_key.py:3065-3099
  (payload.exp.hmac-sha256 over "payload.exp", key INTERNAL_API_TOKEN) — re-implement from
  stdlib in the new test file; never call the verifier to build its own input.
- 140.1-08 lesson (STATE): slowapi APPENDS to limiter._route_limits on every decoration and
  sibling suites reload routers, so N duplicate limits share one bucket — trip loops must be
  BOUNDED-AND-DRIVEN (iterate until 429 with a cap), never fixed-call-count.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: slowapi 0.1.10 sync + survivors #3/#4 — behavioural default_platform_key oracles</name>
  <files>analytics-service/tests/test_limiter_identity.py</files>
  <read_first>
    - analytics-service/services/rate_limit.py:143-158, :195-225 (default_platform_key, _platform_bucket, the :152-154 hazard comment)
    - analytics-service/tests/test_limiter_identity.py:470-500 (the identity assertion at :483 and the file's fixture/app idioms)
    - analytics-service/tests/test_rate_limit_contract.py (the _trip_route_a bounded-driver idiom)
  </read_first>
  <action>
    First: `python3 -m pip install 'slowapi==0.1.10' --break-system-packages`, then
    `python3 -m pip show slowapi` — record the version in the SUMMARY; leave the env at 0.1.10
    (the CI pin; do NOT restore 0.1.9 — matching CI is the point). Confirm the two slowapi
    internals still behave as assumed (OQ-5): a decorated test route with an empty-string key is
    NOT limited (proves `if all(args)` skip exists in 0.1.10) — that probe then becomes part of
    the oracle below.
    Add to test_limiter_identity.py (keep the :483 identity assertion — augment, don't replace):
    (a) `default_platform_key(request) != ""` and stability: two DISTINCT requests yield the SAME
    key (kills #4's per-request-unique mutant); (b) a behavioural trip: drive a route decorated
    with the default key func with a small limit and assert the (N+1)th response is 429 (kills
    #3's empty-string mutant — an empty key skips limiting, so no 429 ever fires). Use a
    bounded-and-driven loop (iterate to 429 with a hard cap), never a fixed call count. Then two
    mutation cycles per protocol: mutate rate_limit.py:208's body to return "" → observe RED
    (the 429 never arrives / the != "" assertion fires) → revert → GREEN; mutate to a
    per-request-unique value (e.g. uuid) → observe RED (stability assertion) → revert → GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pip show slowapi | head -2 && python3 -m pytest tests/test_limiter_identity.py -q 2>&1 | tail -3 && git status --porcelain | grep -v "^??" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `pip show slowapi` reports 0.1.10 and that exact string is recorded in the SUMMARY next to every #3/#4/#5 cycle.
    - Both mutation cycles observed RED first-hand ON 0.1.10 (verbatim failure lines + counts); reverted, GREEN.
    - The behavioural oracle drives real requests (grep the new tests: a client call loop, a 429 literal); the old identity assertion still present.
    - Tree clean; zero MUTANT strings.
  </acceptance_criteria>
  <done>Returning "" or a per-request key from default_platform_key can no longer ship green; evidence is version-stamped 0.1.10.</done>
</task>

<task type="auto">
  <name>Task 2: Survivor #5 — the Retry-After VALUE tight-band oracle</name>
  <files>analytics-service/tests/test_rate_limit_contract.py</files>
  <read_first>
    - analytics-service/main.py:458-530 (locate _retry_after_seconds — post-plan-04 positions — and the header mint site; the review's :482 vs :526 split per §9-M8)
    - analytics-service/tests/test_rate_limit_contract.py:210-240 (the weak `0 < seconds <= window` oracle and the trip driver)
  </read_first>
  <action>
    Strengthen the oracle: immediately after tripping a FRESH 1-hour bucket, assert the response
    Retry-After (read from the header the handler mints, main.py:526 path) is WITHIN A TIGHT BAND
    of the real remaining window — `seconds > _ROUTE_A_WINDOW_S * 0.9` in addition to the
    existing upper bound. The band's arithmetic derives from the declared limit string in the
    TEST (the route's declared window), never from _retry_after_seconds' own return. Respect the
    bounded-and-driven loop rule (duplicate decorations accumulate across suite reloads). Then
    the mutation cycle: replace the computation's return with hard-coded 1 → the band assertion
    goes RED (1 fails > 3240) → revert → GREEN. Run on slowapi 0.1.10 (Task 1 synced it); note
    the `request.state.view_rate_limit` shape was re-confirmed by the run itself.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_rate_limit_contract.py -q 2>&1 | tail -3 && git status --porcelain | grep -v "^??" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The band assertion exists with the 0.9 factor against the window constant declared in/derived from the limit string (not read from the function under test).
    - Mutation (hard-coded 1) observed RED first-hand on 0.1.10 (verbatim line + counts); reverted, GREEN.
    - The pre-existing `0 < seconds <= window` upper-bound assertion retained.
  </acceptance_criteria>
  <done>A wrong-but-small Retry-After (the retry-storm shape) can no longer ship green.</done>
</task>

<task type="auto">
  <name>Task 3: Survivors #8–#11 — the claim-parser oracles (both #11 sites) + phase-wide gates</name>
  <files>analytics-service/tests/test_tenant_claim_parsing.py</files>
  <read_first>
    - analytics-service/services/rate_limit.py:143-160, :270-295, :325-340, :410-425 (the four guards: :274 length, :285 rsplit, :286 empty-payload, :333 anon equality; and the :417 sibling in tenant_or_platform_key)
    - analytics-service/tests/test_process_key.py:3065-3099 (the stdlib HMAC re-implementation idiom — copy it; never mint a claim by calling the verifier)
    - .planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-PATTERNS.md §5 rows 8-11 + §9-M7/M9
  </read_first>
  <action>
    New file tests/test_tenant_claim_parsing.py, minting claims with stdlib hmac/hashlib
    (test_process_key.py:3065-3099 idiom):
    #8: a 513-char header → verify_tenant_claim returns None, AND no HMAC work occurred (patched
    hmac.new spy asserts zero calls) — the guard is :274, not the :150 constant.
    #9: a correctly-minted claim whose payload is "a.b.c" → the tenant bucket is
    "<scope>:t:a.b.c" (the split mutant yields a different bucket).
    #10: a valid-MAC claim with payload "" → falls to the :unverified: bucket, never "<scope>:t:".
    #11: a correctly-minted claim with payload "public-enemy" → bucket "<scope>:t:public-enemy",
    NOT "<scope>:anon" — asserted through BOTH tenant_rate_limit_key AND tenant_or_platform_key
    (the :417 sibling that nine routes key on).
    Then FOUR mutation cycles (protocol as before): drop the :274 length guard → RED; :285
    rsplit→split → RED; drop the :286 empty-payload guard → RED; :333 ==→startswith → RED; and
    run the ==→startswith mutation AGAIN at :417 → RED (record as one survivor, two sites, 2/2).
    Finally the PHASE-WIDE GATE (explicit, not an afterthought):
    `python3 -m pytest -q` (full suite) · `python3 -m pytest --collect-only -q | tail -1`
    (collection sane, ≈4714+new, zero errors) · `python3 -m mypy --strict
    --follow-imports=silent services/ routers/ models/` (Success, 89 source files, 0 new type:
    ignore across the whole phase: `grep -rn "type: ignore" analytics-service/services/
    analytics-service/routers/` count unchanged from baseline) · from repo root `npx vitest run
    tests/lib/` (the parity home green).
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_tenant_claim_parsing.py -q && python3 -m pytest -q 2>&1 | tail -3 && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/ && cd <repo-root> && npx vitest run tests/lib/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 5 mutation runs recorded (4 survivors, #11 at two sites), each RED observed first-hand with verbatim failure + counts, each reverted to GREEN, tree clean, zero MUTANT strings.
    - The #8 oracle asserts BOTH the None return and the zero-HMAC-work spy.
    - Phase gate: full pytest green; collection clean; mypy "Success: no issues found in 89 source files"; vitest tests/lib green; slowapi version recorded (0.1.10).
    - No test imports ANON_PAYLOAD/_CLAIM_MAX_CHARS from rate_limit to build its own expectations (grep the new file: expectations are literals/stdlib-minted).
  </acceptance_criteria>
  <done>All four never-tested guards have behavioural oracles; both #11 sites closed; every phase gate green on the CI-pinned slowapi.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller headers → rate-limit key derivation | X-Tenant-Claim parsing decides which bucket throttles a request |
| test env → CI env | slowapi version skew decides whether mutation evidence is real |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.1-19 | Denial of service | default_platform_key returning "" | mitigate | behavioural 429 oracle — silently-disabled limiting (slowapi's all(args) skip) now reddens CI |
| T-140.1.1-20 | Spoofing | claim parsing (oversize, dotted, empty, prefix payloads) | mitigate | #8-#11 oracles: forged/degenerate claims land in unverified/anon-correct buckets, never a tenant bucket they did not authenticate for; #11 closed at both key functions |
| T-140.1.1-21 | Tampering (retry storms) | Retry-After value | mitigate | tight-band oracle — a wrong-but-small value reddens |
| T-140.1.1-SC | Tampering | pip install slowapi==0.1.10 | accept | version already pinned at requirements.txt:226 and run by CI daily; env sync to the committed pin, not a new package; version recorded in SUMMARY |
</threat_model>

<verification>
- 8 mutation cycles (2+1+5 runs) all FIRST-HAND on recorded slowapi 0.1.10; tree clean after each.
- Full pytest, collection, mypy, vitest tests/lib — all green (the phase-wide gate is encoded here, wave 4's end).
- 0 new `# type: ignore` phase-wide.
</verification>

<success_criteria>
ROADMAP criterion 5 completed: all 12 review survivors + M-15 (plan 06) now turn named tests RED,
observed first-hand — including default_platform_key's "" (skip-limiting-entirely) and the
Retry-After value — with the slowapi version recorded.
</success_criteria>

<output>
Create `.planning/phases/140.1.1-pyapi-fix-h-5-duplicate-reply-contract-break-close-the-12-su/140.1.1-07-SUMMARY.md` when done.
Record: the full 13-row mutation ledger for the PHASE (rows 1-13, merging plan 06's table — survivor · mutation · node id · verbatim RED · counts · FIRST-HAND · slowapi version where relevant), and the phase-gate outputs.
</output>
