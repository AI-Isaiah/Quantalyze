---
phase: 140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route
verified: 2026-07-26T18:35:51Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: null
  note: "Initial verification — no prior VERIFICATION.md in the phase directory."
disposition:
  PYAPIFIX2-01: >-
    PYTHON HALF CLOSED (7/7 sites carry code+recoverable); RENDER HALF → OB-1, owner 140.3
warnings:
  - id: W-01
    severity: warning
    truth: "SC1 — a venue-transient fault answers a typed, classifiable error at EVERY consumer"
    file: "analytics-service/routers/exchange.py:544"
    finding: >-
      The `except ccxt.BaseError` escape arm on the SAME live route (`/api/validate-key`)
      still raises `service_error(424, "EXCHANGE_PROBE_FAILED", dependency=req.exchange,
      retryable=True, …)` — a NESTED envelope. Its `body.detail` is an OBJECT, so
      `analytics-client.ts:177-180`'s `error.detail ?? …` yields "[object Object]",
      `classifyKeyValidationError` misses every branch, and the render is `UNKNOWN`/500
      "our team has been notified" with no retry affordance — the identical user-visible
      symptom SC1 names, for a verdict the site itself marks `retryable=True`.
      The phase's enumeration predicate (`raise HTTPException` lexically inside a
      venue-`except`) structurally cannot see `raise service_error(...)` sites, so this
      arm was never a class candidate. Python-side it IS typed and machine-readable at
      `body.detail.code`, and the render defect is covered by the pre-existing generic
      obligation TS-05 (owner 140.2/140.3), so this is NOT an unowned escape and NOT a
      blocker — but SC1's "EVERY consumer" phrasing is met only for the flat-shape subset.
  - id: W-02
    severity: warning
    truth: "SC6 — the phase's artifacts state only what they can support"
    file: "TODOS.md (commit a12b9eaa)"
    finding: >-
      The routing note for the fenced-out STATUS_CONTRACT drift says
      "`routers/portfolio.py:2242` is a comment line (the 429 raise is at `:2244-2247`)".
      `:2242` IS a comment line at HEAD ✓, but the 429 raise is at `:2254-2263` at HEAD —
      it was shifted by this phase's own commit `321e5c21`, and the parenthetical was
      carried from the base rather than re-derived. Inside the requirement whose rule is
      "every coordinate RE-DERIVED at HEAD, never old + N". Confined to a backlog routing
      note (not `docs/STATUS_CONTRACT.md`, whose four corrected coordinates ARE all
      HEAD-accurate); the finding it describes is real and correctly routed.
  - id: W-03
    severity: info
    truth: "SC1 — the committed cross-language fixture is 140.3's parity input"
    file: "analytics-service/tests/fixtures/validate_key_venue_transient_contract.json"
    finding: >-
      All 14 fixture cases are consumed (one per wire test, keyed by `trigger`), and each
      is pinned against both the test literals and the real wire body. There is no
      assertion that the fixture contains NO EXTRA case, so a fabricated 15th case could
      be added and would reach 140.3's TS parity test with no Python counterpart. One
      `set(_BY_TRIGGER) == {…14 literals…}` assertion would close it.
---

# Phase 140.1.2: PYAPI-FIX2 — Verification Report

**Phase Goal:** The venue-transient class is closed on the route real users actually hit, and every finding that survived adversarial refutation is closed or explicitly, reasonedly deferred.
**Verified:** 2026-07-26T18:35:51Z
**Status:** passed (6/6), with 2 warnings + 1 info recorded below
**Re-verification:** No — initial verification
**Scope verified:** `git diff 39688d69..HEAD` — 14 commits, 19 files, +2348/−30

---

## VERIFIER-MUST-MIRROR (B-1) — mirrored verbatim as instructed

**PYAPIFIX2-01: PYTHON HALF CLOSED (7/7 sites carry code+recoverable); RENDER HALF → OB-1, owner 140.3**

This is not a bare tick. ROADMAP SC1 also demands that a Binance-maintenance-shaped failure
*no longer renders* as `UNKNOWN`/500 with no retry affordance. That render assertion is a
TypeScript assertion (`create-with-key/route.ts` returns the classifier-computed status and
discards the upstream one) and is **not delivered by this phase**. Verified independently at
source below: the wire now carries the answer; nothing in TypeScript reads it yet.

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence gathered at source |
|---|-------|--------|-----------------------------|
| 1 | Venue-transient fault answers a typed, classifiable error at every consumer of `validate_key_permissions`, incl. `/api/validate-key` — **Python half** | ✓ VERIFIED (split, honestly recorded) | 7 `VenueTransientHTTPException` raise sites at `exchange.py:163,178,371,387,409,598` + `portfolio.py:2354`; `service_error(` appears at NONE of them; handler at `main.py:583-598` emits flat `{detail, code, recoverable}`. 19/19 tests pass. See W-01. |
| 2 | A permanent MT5 credential fault is never retried as a transient venue fault | ✓ VERIFIED | REPRODUCE gate opened, fix is provenance-based: `ValidationResult.permanent: bool \| None = None` (`adapter.py:153`), set `True` only at `mt5.py:109` and `:235`; `long_fetch.py:414` consults `val.permanent is True` FIRST, `permanent_codes` set byte-unchanged. |
| 3 | `/internal` throttling emits the service's own envelope | ✓ VERIFIED | `internal.py:245-251` = `service_error(429, "RATE_LIMITED", retryable=True, retry_after=int(_RATE_LIMIT_WINDOW_S), detail=…)`; the `error_contract.py:69-78` comment it invalidated inverted in the SAME commit `b98dee1e`. |
| 4 | Every user-facing 429 carries a `Retry-After` | ✓ VERIFIED | 4/4: `match.py:1747`, `portfolio.py:1966`, `portfolio.py:2262`, `simulator.py:254`. Each sourced from the WINDOW constant its own guard enforces, not the count. |
| 5 | The 200-discriminator corpus cannot silently shrink | ✓ VERIFIED | New injective containment oracle binds `_SHAPES` (wire bodies) to `_ROUTER_SRC` AST-declared 200 shapes; `_envelope_error` added to `_SHAPE_BEARING_FUNCTIONS`. Falsifiability re-observed first-hand (probe P3 below). |
| 6 | The phase's artifacts state only what they can support | ✓ VERIFIED | `140.1.1-VERIFICATION.md` `status: gaps_found` with both false claims corrected in place (not just the field flipped); STATUS_CONTRACT heading, S-11 refs and the not-seam-reachable list all re-derived and re-checked by me at HEAD; census independently re-derived. See W-02. |

**Score: 6/6 truths verified.**

---

### Truth 1 — the 7-site class, re-derived by behaviour (not counted from the SUMMARY)

I re-derived class membership myself rather than accepting the enumeration.

**Raise sites at HEAD** (`grep -rn VenueTransientHTTPException`, non-test):

| # | Site | Code | recoverable | `service_error` present? |
|---|------|------|-------------|--------------------------|
| C1 | `exchange.py:163` (sFOX 429) | `RATE_LIMITED` | `True` | no |
| C2 | `exchange.py:178` (sFOX 5xx/status 0) | `NETWORK_UNAVAILABLE` | `True` | no |
| C3 | `exchange.py:371` (MT5 probe timeout) | `NETWORK_UNAVAILABLE` | `True` | no |
| C4 | `exchange.py:387` (`Mt5AccountMismatchError`) | `NETWORK_UNAVAILABLE` | `True` | no |
| C5 | `exchange.py:409` (transient `Mt5ClientError`) | `NETWORK_UNAVAILABLE` | `True` | no |
| C6 | `exchange.py:598` (ccxt verdict collapse, **live key-connect**) | `result["error_code"]` verbatim | `not in PERMANENT_VALIDATION_ERROR_CODES` | no |
| C7 | `portfolio.py:2354` (verify-strategy collapse, dead route) | `validation["error_code"]` verbatim | same rule | no |

- **`detail` byte-identity:** the `detail=` expression is unchanged at all seven (`RATE_LIMITED_DETAIL`, `NETWORK_ERROR_DETAIL` ×4, `result["error"]`, `validation["error"]`). `services/exchange.py` — the module that owns every one of those strings — **is not in the phase diff at all**, so no copy moved.
- **`service_error(` at a class site:** none. It appears elsewhere in `exchange.py` (15 hits) and `portfolio.py` (4) at the `ADAPTER_INIT_FAILED` / `EXCHANGE_PROBE_FAILED` / `INTERNAL` arms — different arms, dispositioned in W-01.
- **8th site?** I applied the behavioural predicate independently over every non-test `.py`. Remaining bare-400 arms on the same route (`exchange.py:111/141/196/256/257/399/401/419` — `AUTH_FAILED_DETAIL`, `MT5_WRONG_SERVER_DETAIL`, `MT5_MASTER_PASSWORD_DETAIL`) are **permanent caller faults**, not venue-transient, and all three classify correctly in the TS cascade today (`authentication failed` / `broker server` / `master password` branches). `exchange.py:478/493` and `internal.py:356` are server feature gates. **No eighth venue-transient site under this predicate.** The one site the predicate cannot see is recorded as **W-01**.
- **Producer invariant (what makes the verbatim, fallback-free code safe):** I read `validate_key_permissions` branch-by-branch — every branch that assigns `result["error"]` assigns `result["error_code"]` in the same branch, and the dict is initialised with `"error_code": None`, so the failure mode is a loud `ValueError` from the exception class's own guard, never a fabricated code. This is mechanised at the producer by `test_error_implies_error_code_at_every_producer_branch`.

**The render half is genuinely absent, confirmed at source (not taken from the SUMMARY):**
`src/lib/analytics-client.ts:174-181` still does `error.detail ?? "Analytics service error"` → `new AnalyticsUpstreamError(message, res.status)`; `src/lib/wizardErrors.ts:936-1035` is still a pure substring cascade over `err.message` terminating at `{code:"UNKNOWN", status:500}` — **no branch reads `body.code` or `body.recoverable`**. `EXCHANGE_UNAVAILABLE`'s copy matches no branch; `NETWORK_UNAVAILABLE`'s copy contains "Network" while the branch tests `timeout`/`etimedout`. The split disposition is therefore accurate, not a hedge.

**Ledger honesty (checked in all three places, as the stance required):**

| Ledger | Records the split? | Evidence |
|---|---|---|
| `.planning/REQUIREMENTS.md:102` | ✓ | `⚠️ PYTHON HALF CLOSED (7/7 sites carry code+recoverable); RENDER HALF → OB-1, owner 140.3` + the reason the render assertion cannot be met in Python |
| `.planning/ROADMAP.md:112` (SC1) | ✓ | `⚠️ DISPOSITION (140.1.2 plan 04): … this criterion is HALF met by this phase, deliberately and on the record` |
| `.planning/REQUIREMENTS.md:282` (traceability) | ✓ | `Complete (PYAPIFIX2-01 Python half only — render half owned by 140.3 / TS-35)` — not the bare string `Complete` |
| `140.1-TS-OBLIGATIONS.md` | ✓ | TS-32 corrected (stale `:505` + 3 missing MT5 arms named, coordinates deliberately removed); **TS-35 added** in the file's own ID space with OB-1 provenance; count recounted 34→35 |

---

### Truth 2 — the REPRODUCE gate and the provenance fix

- **Fix is provenance-based, as claimed.** `ValidationResult.permanent: bool | None = None` — genuinely tri-state, default `None`, and `long_fetch` uses `val.permanent is True` (identity, not truthiness) so `None` falls through unchanged.
- **`long_fetch`'s `permanent_codes` classification of non-MT5 codes is byte-unchanged.** The diff over `long_fetch.py` adds only comments plus the one disjunct; the set literal `{"AUTH_FAILED", "PERMISSION_DENIED", "TRADE_SCOPE", "WITHDRAW_SCOPE"}` is untouched, `MISSING_SCOPE` stays deliberately omitted, and `_is_unexpected_fallback` is untouched.
- **No other adapter opts in.** `grep -rn "permanent=" services/` → exactly two assignments, both in `mt5.py`. `services/exchange.py` (`PERMANENT_VALIDATION_ERROR_CODES`) is not in the diff.
- **The tests drive production code, not a hand-built object.** `test_long_fetch_mt5_wrong_server_is_permanent` / `…_master_password_is_permanent` instantiate the REAL `Mt5Adapter` (blank server → offline rejection with `_build_client` booby-trapped to fail loud if a client is built; canonical offline terminal double for the master-password case) and patch `mt5_enabled_server → True`. `test_long_fetch_mt5_disabled_gate_is_not_why_permanent` is the load-bearing negative control proving the kill-switch produces `permanent` for a DIFFERENT reason — without it both tests would pass green while the defect stayed live.

---

### Toothless-test audit (the programme's signature defect)

I read the new/changed test files for self-referential oracles rather than accepting the claim.

| File | Imports from module under test? | Verdict |
|---|---|---|
| `test_validate_key_venue_transient.py` | Only inside function bodies, and only as **cross-artifact** pins (`RATE_LIMITED_DETAIL` etc. compared against module-level literals typed in the test) — the documented `test_sfox_validate.py:462-471` pattern. Every wire expectation (status, key set, `detail`, `code`, `recoverable`) is a literal. | ✓ clean |
| `test_status_contract_match_sim_portfolio.py` | No. `"15"`, `"3600"` are string literals with in-code notes refusing the import. | ✓ clean |
| `test_match_router.py` (new block) | No. `30` literal + header/body consistency cross-check via regex over the copy. | ✓ clean |
| `test_long_fetch.py` (new block) | No. `"permanent"` / `"MT5_WRONG_SERVER"` / the disabled-copy substring are literals. | ✓ clean |
| `test_process_key_200_discriminator.py` (new fence) | Two sides from two different artifacts (router AST vs live wire bodies). **Not** a count, **not** `len(_SHAPES)`. | ✓ clean |

**Falsifiability spot-checks — I ran five mutations myself and observed each redden. All files restored via `git checkout --`; `git status` clean afterwards.**

| # | Mutation | Result | Observed |
|---|---|--------|----------|
| P1 | C6 `recoverable=result[…] not in PERMANENT_…` → hardcoded `True` | ✓ RED | `test_c6_permanent_auth_failure_is_not_recoverable` — 1 failed, 18 passed |
| P2 | `long_fetch` drop `val.permanent is True` disjunct | ✓ RED | both MT5 permanence tests — 2 failed, 30 passed |
| P3 | delete `_SHAPES` row `5-synchronous-success` | ✓ RED | fence names the uncovered fingerprint `dict:code,correlation_id,encrypted_credentials,…` — exactly as the SUMMARY claimed |
| P4 | remove `Retry-After` from `simulator.py` | ✓ RED | `test_simulator_per_user_quota_429_advertises_its_window` — `KeyError: 'Retry-After'` |
| P5 | revert `internal.py` 429 to raw `HTTPException` | ✓ RED | `test_internal_throttle_429_carries_the_service_envelope` |

---

### Truth 6 — artifact truth, re-checked at HEAD by me

| Item | Claim | My check at HEAD |
|---|---|---|
| `140.1.1-VERIFICATION.md` | reads `gaps_found` | ✓ `status: gaps_found` + `status_correction` block; the blanket "no already-correct test was weakened or removed" sentence is **replaced in place** by two specific, supportable facts (the deleted `test_pyapi_10a_exactly_six_shapes_are_covered` function and the contested "already-correct"), and `deferred[0]`'s `grep -c` restated as marker-exists-not-list-complete |
| STATUS_CONTRACT `## 1. The four classes` | → five | ✓ reads `## 1. The five classes`; table has 5 data rows |
| S-11 `internal.py:414` | → `:442` / `:471` | ✓ `:442` = `except Exception:`, `:471` = `raise service_error(500, "ADAPTER_INIT_FAILED",` |
| not-seam-reachable list `exchange.py:453,491,553` | → the six real `fetch_trades` arms | ✓ `:660` 503 kek, `:670` 400 no key, `:685` 404, `:689` 403, `:698` 500 decrypt, `:760` 500 fetch — all six verified line-by-line |
| dependency census `six live sites / seven pass none` | → `six of sixteen / ten pass none` | ✓ **independently re-derived by my own AST sweep**: 16 `service_error(500, …)` call sites, 6 naming a `dependency`, 10 not. Matches exactly. |
| Fenced-out pre-existing drift | routed, not swept | ✓ `TODOS.md` carries the `test_mt5_validate.py` self-referential-oracle finding and the `portfolio.py` STATUS_CONTRACT drift. See **W-02** for a coordinate slip inside that note. |

---

### Behavioural Spot-Checks (run first-hand)

| Behaviour | Command | Result | Status |
|---|---|---|---|
| New venue-transient suite | `pytest tests/test_validate_key_venue_transient.py` | 19 passed | ✓ PASS |
| Other new/changed suites | `pytest tests/test_long_fetch.py tests/test_match_router.py tests/test_process_key_200_discriminator.py tests/test_status_contract_*.py` | 187 passed | ✓ PASS |
| Full analytics-service suite (regression fence) | `pytest -q -p no:randomly` | **4772 passed, 96 skipped, 0 failed** in 70s | ✓ PASS — reconciles exactly with the phase gate's claimed 4772 |
| CI-exact type gate | `mypy --strict --follow-imports=silent services/ routers/ models/` | `Success: no issues found in 89 source files` | ✓ PASS |
| `main.py` (NOT in the CI gate) | same, with `main.py` appended | 5 errors, all at pre-existing sites (`lifespan`, `_crash_handler`, `verify_service_key`, `health`) — none in the new handler | ✓ PASS (pre-existing) |

---

### Scope Fence

| Fence | Status |
|---|---|
| Python + Python tests + planning artifacts only | ✓ 19 files: 10 `.py` (src) + 6 `.py`/`.json` (tests/fixtures) + 3 `.md` |
| **ZERO TypeScript in the diff** | ✓ `git diff --name-only \| grep -E '\.tsx?$' \| wc -l` → **0** |
| Do not merge/push/ship | ✓ branch `feat/v1.16-production-resilience`, unmerged, unpushed |
| Debt markers (`TBD`/`FIXME`/`XXX`) introduced | ✓ **none** across all 19 files |
| `TODO`/`HACK`/`PLACEHOLDER` | 1 hit, `match.py:1150` — pre-existing, references a formal follow-up PR, outside every diff hunk |

---

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|---|---|---|---|
| **PYAPIFIX2-01** | Venue-transient class closed at every consumer | ✓ SATISFIED — **Python half only, split recorded in all 3 ledgers** | 7/7 flat sites verified at source; 14 wire cases + 2 guard tests + 3 cross-artifact pins; render half → TS-35/OB-1 owner 140.3. W-01 recorded. |
| **PYAPIFIX2-02** | MT5 permanent credential faults not retried | ✓ SATISFIED | Provenance tri-state; `permanent_codes` byte-unchanged; real-adapter tests + kill-switch negative control; mutation P2 |
| **PYAPIFIX2-03** | `/internal` 429 adopts `service_error` | ✓ SATISFIED | `internal.py:245`; builder arm has its first call site; invalidated comment inverted same-commit; mutation P5 |
| **PYAPIFIX2-04** | Every user-facing 429 carries `Retry-After` | ✓ SATISFIED | 4/4 sites; window-not-count sourcing; literal pins; mutation P4 |
| **PYAPIFIX2-05** | `_SHAPES` corpus cannot silently shrink | ✓ SATISFIED | Injective containment oracle over two artifacts; mutation P3 reddens naming the fingerprint |
| **PYAPIFIX2-06** | Artifact truth | ✓ SATISFIED | All 4 items + the W-1 self-correction verified independently at HEAD. W-02 recorded. |

No orphaned requirements: `REQUIREMENTS.md:282` maps `PYAPIFIX2-01..06` to Phase 140.1.2 and all six are claimed by the four plans.

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `analytics-service/routers/exchange.py` | 544 | Nested `service_error(424, …)` envelope on the live key-connect route | ⚠️ Warning (W-01) | Renders `UNKNOWN`/500 with no retry affordance for a `retryable=True` verdict. Owned by TS-05 (140.2/140.3). |
| `TODOS.md` | routing note (a12b9eaa) | Coordinate not re-derived at HEAD | ⚠️ Warning (W-02) | Backlog note points at `:2244-2247`; raise is at `:2254-2263`. Cosmetic; the routed finding itself is correct. |
| `tests/fixtures/validate_key_venue_transient_contract.json` | — | No "no extra case" fence | ℹ️ Info (W-03) | A fabricated 15th case would reach 140.3's TS parity test unpinned. |

---

### Human Verification Required

**None.** `140.1.2-VALIDATION.md` §Manual-Only Verifications declares zero manual criteria, and I confirm that: every criterion above was verifiable programmatically and was verified. The wizard-render behaviour (a retry affordance during a Binance maintenance window) is **out of scope for this phase** by its own recorded disposition — it is 140.3's, via ledger row TS-35 — and is therefore not deferred human work of 140.1.2.

---

### Gaps Summary

**No gaps.** Every success criterion is met at source, not merely claimed. The two things worth a
human decision are recorded as warnings rather than gaps because neither is an unmet must-have:

- **W-01** is a real, reachable instance of the phase's own headline symptom at an 8th site on the
  same route — but that site *is* typed (`body.detail.code`), and its render defect is the
  pre-existing, owned TS-05 obligation, not an escape this phase created or hid. Worth scheduling
  alongside TS-05/TS-35 so 140.3 closes the route rather than the subset.
- **W-02** is a one-line coordinate slip in a backlog routing note. It does not touch the four
  STATUS_CONTRACT corrections, all of which I re-derived at HEAD and found correct.

The strongest positive evidence: five mutation probes I injected myself all reddened, the full
4772-test suite is green with the CI-exact `mypy --strict` gate clean, and the class enumeration
survived an independent re-derivation by behaviour.

---

_Verified: 2026-07-26T18:35:51Z_
_Verifier: Claude (gsd-verifier) — goal-backward, FORCE stance_
