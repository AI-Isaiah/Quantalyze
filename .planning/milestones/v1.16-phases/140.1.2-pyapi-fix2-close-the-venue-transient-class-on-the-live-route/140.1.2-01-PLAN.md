---
phase: 140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  # test_long_fetch.py is modified on BOTH branches (Task 1 always runs). The three production
  # files below are REPRODUCED-BRANCH-ONLY (Task 2): on the NOT-REPRODUCIBLE exit Task 2 is
  # skipped by design and none of them appear in the diff. That is a correct, green execution —
  # not a shortfall. Read plan 01's SUMMARY decision record before judging their absence.
  - analytics-service/tests/test_long_fetch.py
  - analytics-service/services/ingestion/adapter.py      # REPRODUCED branch only
  - analytics-service/services/ingestion/mt5.py          # REPRODUCED branch only
  - analytics-service/services/ingestion/long_fetch.py   # REPRODUCED branch only
autonomous: true
requirements: [PYAPIFIX2-02]
must_haves:
  truths:
    - "A long-fetch job whose adapter validation returns MT5_WRONG_SERVER or MT5_MASTER_PASSWORD yields error_kind='permanent' (never retried) — OR the plan records first-hand observed evidence it could not be reproduced and exits green with zero production edits"
    - "The MT5 master-password REJECTION itself is byte-unchanged (still rejected, never persisted) — only its retry classification changes"
    - "long_fetch's classification of every non-MT5 code is byte-unchanged: MISSING_SCOPE stays OMITTED from the local permanent_codes (the deliberate 140.1.1 W-3 divergence), AUTH_FAILED/PERMISSION_DENIED/TRADE_SCOPE/WITHDRAW_SCOPE stay permanent"
    - "The sync-424 half (half A) is recorded UNREACHABLE with the C-2 proof (source whitelist + _is_long_fetch routing), with NO defensive code added"
  artifacts:
    # UNCONDITIONAL — Task 1 runs on both branches of the reproduce-first gate.
    - path: "analytics-service/tests/test_long_fetch.py"
      provides: "MT5 permanence cases + mt5_enabled_server negative control (names contain 'mt5' and 'permanent' so the -k gate selects them)"
    # CONDITIONAL — REPRODUCED branch only. On the NOT-REPRODUCIBLE exit Task 2 is skipped by
    # design and this artifact MUST NOT exist. A verifier that demands it unconditionally would
    # FAIL a correctly-executed plan; check the SUMMARY's recorded gate verdict first.
    - path: "analytics-service/services/ingestion/adapter.py"
      condition: "REPRODUCED branch only (Task 1 gate = REPRODUCED)"
      provides: "ValidationResult.permanent: bool | None = None (adapter-stated permanence verdict; None = no verdict, fall through to existing list logic)"
      contains: "permanent"
  key_links:
    # CONDITIONAL — REPRODUCED branch only, same rule as the adapter.py artifact above: absent by
    # design on the NOT-REPRODUCIBLE exit, and its absence there is not a gap.
    - from: "analytics-service/services/ingestion/mt5.py"
      to: "analytics-service/services/ingestion/long_fetch.py"
      condition: "REPRODUCED branch only (Task 1 gate = REPRODUCED)"
      via: "ValidationResult.permanent=True minted at the two MT5 sites, consulted by long_fetch's rejection-classification block"
      pattern: "permanent"
---

<objective>
Close PYAPIFIX2-02: a permanent MT5 credential fault (`MT5_WRONG_SERVER`, `MT5_MASTER_PASSWORD`)
must never be retried as a transient venue fault on the live worker path — 3 gateway-serialised
RPyC retries burned on a credential that can never succeed, with `MT5_ENABLED=true` in production.

REPRODUCE-FIRST, GATED: the RED test IS the reproduction. "Could not reproduce" is a valid,
budget-saving green exit. RESEARCH C-2 proves half A (the sync `/process-key` 424 gate) is
UNREACHABLE for `source="mt5"` — that half gets a recorded proof, zero code.

Purpose: stop retry-storming the single-lock MT5 gateway for unfixable credentials; align Python's
verdict with TypeScript's (wizardErrors.ts:1002/1005 already calls both 400 client faults).
Output: RED-observed then GREEN-observed tests + a provenance-based fix (or a recorded
NOT-REPRODUCIBLE disposition).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-CONTEXT.md
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-RESEARCH.md  (§PYAPIFIX2-02, C-2, P-2, P-7)
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-PATTERNS.md  (§PYAPIFIX2-02, the four-vocabulary table)

Binding programme rules: RED observed first-hand (never asserted-only) · no self-referential
oracles (every expectation a literal typed in the test or stdlib-minted) · `cast()` never
`# type: ignore` · restore probes from `/tmp` cp copies, never `git checkout`/`git stash` ·
never `git add` anything under `.planning/`.

Line numbers below verified at `39688d69` — ALWAYS re-locate by code text before editing.
</context>

<interfaces>
Verified at source this planning session (quote-level receipts in PATTERNS.md):

- `services/ingestion/long_fetch.py` — MT5 kill-switch at the block reading
  `if source == "mt5" and not mt5_enabled_server():` (≈:285-291) returns
  `error_kind="permanent"` with `MT5_DISABLED_DETAIL` — this is why the
  `mt5_enabled_server` patch is MANDATORY in every test here. The rejection classifier is the
  block declaring the local `permanent_codes = {"AUTH_FAILED", "PERMISSION_DENIED",
  "TRADE_SCOPE", "WITHDRAW_SCOPE"}` (≈:391-403) and returning
  `DispatchResult(outcome=FAILED, error_kind="permanent" if _is_permanent else "transient")`.
- `services/ingestion/mt5.py` — `_wrong_server()` helper mints
  `ValidationResult(valid=False, read_only=None, error_code="MT5_WRONG_SERVER", ...)` (≈:98-107);
  the master-password arm mints `error_code="MT5_MASTER_PASSWORD"` inside the
  `if is_trade_capable(info, probe):` block (≈:218-227). SECURITY: that rejection is an EoP gate
  (trade-capable login NEVER persisted) — do not alter the rejection, only the classification.
- `services/ingestion/adapter.py` — `ValidationResult` dataclass already carries
  `valid`, `read_only`, `error_code`, `human_message`, `debug_context`. Consumed by ≥6 modules —
  any new field MUST have a default or mypy --strict breaks widely.
- Harness analog: `tests/test_long_fetch.py:952-1015`
  (`test_long_fetch_probe_error_is_transient_not_permanent`) — job dict with
  `metadata.{unified_backbone_at_claim:"true", verification_id, source, flow_type:"onboard",
  correlation_id, context}`, `MagicMock()` adapter with `validate = AsyncMock(return_value=
  ValidationResult(...))`, `patch("services.ingestion.long_fetch.get_adapter", ...)` +
  `patch("services.ingestion.long_fetch.get_supabase",
  return_value=_build_supabase_mock(existing_status="draft"))`, then assert on
  `result.error_kind` with a message stating the business harm (Rule 9).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GATED reproduce-first — write the RED tests and run the decision rule</name>
  <files>analytics-service/tests/test_long_fetch.py</files>
  <read_first>
    - analytics-service/tests/test_long_fetch.py:940-1015 (the harness to copy exactly, incl. _build_supabase_mock)
    - analytics-service/services/ingestion/long_fetch.py — the mt5_enabled_server kill-switch block AND the permanent_codes rejection block (read both in full)
    - analytics-service/services/ingestion/mt5.py — the two mint sites (code text: error_code="MT5_WRONG_SERVER" / error_code="MT5_MASTER_PASSWORD") and MT5_DISABLED-related detail constants
    - 140.1.2-RESEARCH.md §"The reproduce-first gate — exact mechanism" (the decision-rule table)
  </read_first>
  <behavior>
    - Test A (`test_long_fetch_mt5_wrong_server_is_permanent`): job with source="mt5",
      flow_type="onboard"; stub adapter validate returns ValidationResult(valid=False,
      read_only=None, error_code="MT5_WRONG_SERVER", human_message="…", debug_context=None);
      patches: get_adapter, get_supabase(existing_status="draft"), AND
      patch("services.ingestion.long_fetch.mt5_enabled_server", return_value=True) — MANDATORY.
      Assert result.error_kind == "permanent" with a failure message stating the harm
      ("a wrong-server credential can never succeed; retrying burns 3 serialised gateway probes").
    - Test B (`test_long_fetch_mt5_master_password_is_permanent`): identical shape,
      error_code="MT5_MASTER_PASSWORD".
    - Negative control (`test_long_fetch_mt5_disabled_gate_is_not_why_permanent`): same job,
      mt5_enabled_server patched to return_value=False, adapter validate NOT reached — assert
      error_kind == "permanent" AND error_message contains the MT5-disabled detail text (pin the
      distinguishing substring as a LITERAL typed in the test, read once from the constant's
      current value at source — never imported). This proves the enabled=True patch in A/B is
      load-bearing: without it A/B would pass for the WRONG reason.
    - All test names contain "mt5" and "permanent" so the gate command selects them.
  </behavior>
  <action>
    Append the three tests to tests/test_long_fetch.py copying the :952-1015 harness shape.
    Oracle integrity: never import MT5 detail constants or the permanent_codes set into the test;
    every expectation is a literal typed in the test file.

    Then run the GATE — NO `-x` (revision I-4). Under `-x` a REPRODUCED run aborts on Test A's
    failure and the negative control never executes, so the one piece of evidence that makes the
    RED trustworthy (that permanence did NOT come from the kill-switch) would be missing exactly
    when it is needed. All three tests run on every invocation; this command is byte-identical to
    the <verify><automated> below:
    `cd analytics-service && python3 -m pytest tests/test_long_fetch.py -k "mt5 and permanent" -q`

    DECISION RULE (encode the observed output verbatim in the SUMMARY):
    - Tests A/B FAIL with observed `error_kind == "transient"` ⇒ REPRODUCED. This failure IS the
      observed RED. Commit the RED tests (`test(140.1.2-01): RED — MT5 permanent codes retried as
      transient (PYAPIFIX2-02)`), proceed to Task 2.
    - Tests A/B PASS ("permanent") AND the negative control proves the kill-switch was patched
      out (i.e. permanence did NOT come from the disabled gate) ⇒ NOT REPRODUCIBLE. STOP: write
      NO production code, keep the now-green tests as regression pins, record the exact command +
      output + control evidence in the SUMMARY, mark PYAPIFIX2-02 disposition "NOT REPRODUCIBLE —
      evidence recorded", skip Task 2, go to Task 3.
    - Tests error / cannot be constructed ⇒ INCONCLUSIVE. Escalate in the SUMMARY with the
      traceback; do NOT guess, do NOT proceed on the strength of the source read alone.

    Also record (SUMMARY only, no code): the C-2 half-A unreachability proof — quote
    routers/process_key.py's @field_validator("source") whitelist (mt5 admitted only to
    onboard/resync) and _is_long_fetch routing both to the worker before the sync path; state
    that no defensive membership was added because the wire-boundary validator is the real fence.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_long_fetch.py -k "mt5 and permanent" -q</automated>
  </verify>
  <acceptance_criteria>
    - Gate command run and its verbatim output captured in the SUMMARY (RED observed, or
      NOT-REPRODUCIBLE evidence, or escalation — one of the three, explicitly).
    - Negative control exists and passes; its distinguishing assertion pins a literal substring.
      The gate command carries NO `-x`, so the control is observed on the REPRODUCED path too
      (a `-x` run would abort at Test A and never reach it) — the SUMMARY must show all three
      test outcomes from a single run.
    - Zero imports of production constants/sets into the new tests (grep the new test block for
      `MT5_WRONG_SERVER_DETAIL|MT5_MASTER_PASSWORD_DETAIL|permanent_codes|PERMANENT_VALIDATION` → 0).
    - Half-A proof recorded in SUMMARY with quoted source.
  </acceptance_criteria>
  <done>The reproduce verdict is decided by observed execution, not inference, and is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The fix (ONLY if Task 1 = REPRODUCED) — provenance verdict, not a longer list</name>
  <files>analytics-service/services/ingestion/adapter.py, analytics-service/services/ingestion/mt5.py, analytics-service/services/ingestion/long_fetch.py</files>
  <read_first>
    - analytics-service/services/ingestion/adapter.py (ValidationResult definition, full)
    - 140.1.2-PATTERNS.md §"The FOUR sibling permanent/transient vocabularies" (V1–V4) and the csv_adapter.py:172 open-code-space warning
    - analytics-service/routers/cron.py:134-143 (CREDENTIAL_REJECTION_CODES — the polarity reference; read, do NOT modify)
    - analytics-service/services/exchange.py:1008-1023 (the DELIBERATE DIVERGENCE docstring — the reason long_fetch's set must NOT be re-pointed)
  </read_first>
  <behavior>
    - After the fix, Tasks 1A/1B pass GREEN (error_kind == "permanent" for both MT5 codes,
      with mt5_enabled_server=True).
    - A ValidationResult with permanent=None (every other adapter today) classifies exactly as
      before — pin with one control test: an unknown code (e.g. "SOME_FUTURE_CODE") with
      permanent=None stays "transient" (fail-safe), and permanent omitted defaults to None.
  </behavior>
  <action>
    Gate on PROVENANCE, not a string list (CONTEXT-locked: "any fix keyed on a string list is
    wrong by construction" — csv_adapter.py mints an open code space):
    1. adapter.py: add `permanent: bool | None = None` to ValidationResult with that exact
       default (None = "adapter states no verdict"). A default is required — the dataclass is
       consumed by ≥6 modules and mypy --strict is a CI gate. Docstring states the tri-state
       semantics and that this is the seed of the backlogged unification (OB-11/OB-12) — do NOT
       propose the unification itself.
    2. mt5.py: set `permanent=True` inside `_wrong_server()` and in the master-password
       ValidationResult. Touch NOTHING else in the file — the rejection logic, is_trade_capable,
       and all detail copy are byte-unchanged (EoP gate).
    3. long_fetch.py: in the rejection-classification block, consult the adapter verdict FIRST:
       permanence is `val.permanent is True` OR the existing local-list logic, verbatim. The
       local `permanent_codes` set is byte-unchanged (MISSING_SCOPE stays omitted — the W-3
       divergence is deliberate; re-pointing at PERMANENT_VALIDATION_ERROR_CODES is a production
       behaviour change named in no requirement). Add an in-code note that the list is
       structurally uncompletable and the adapter verdict is the correct channel.
    Run the gate command → observe GREEN. Run the file's full suite. Then
    `cd analytics-service && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/`
    — fix any new error via cast()/defaults, NEVER `# type: ignore`.
    Commit: `feat(140.1.2-01): GREEN — adapter-stated permanence for MT5 credential faults (PYAPIFIX2-02)`.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_long_fetch.py -q && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/</automated>
  </verify>
  <acceptance_criteria>
    - Tasks 1A/1B observed RED before this task, GREEN after — both runs quoted in SUMMARY.
    - `git diff` for mt5.py shows ONLY the two `permanent=True` additions (rejection copy and
      logic byte-unchanged).
    - long_fetch.py local permanent_codes set byte-unchanged (grep: MISSING_SCOPE still absent).
    - mypy --strict clean, 89 files, 0 new `# type: ignore` (`git diff | grep -c "type: ignore"` → 0).
    - The unknown-code fail-safe control passes (unknown + permanent=None ⇒ transient).
  </acceptance_criteria>
  <done>Both MT5 codes classify permanent via adapter provenance; all other classifications provably unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Plan-close gate (wave-boundary full suite)</name>
  <files></files>
  <read_first>
    - 140.1.2-RESEARCH.md §Validation Architecture (baseline 4743 passed / 96 skipped / 0 failed; collection 4837)
  </read_first>
  <action>
    Run `cd analytics-service && python3 -m pytest -q` (unscoped) and
    `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/`.
    Reconcile the pytest count ARITHMETICALLY:
    **expected passed = 4743 + (tests THIS plan added) + (tests added by any SIBLING WAVE-1 plan
    already landed on this branch).** Plans 01 and 02 are BOTH wave 1 and this command is
    UNSCOPED, so whichever of the two lands second also counts its sibling's tests — a bare
    `4743 + mine` will NOT reconcile, and that is expected, not a defect.
    Establish the sibling term FIRST-HAND before drawing any conclusion: `git log --oneline
    39688d69..HEAD` to see whether plan 02's commits are present, and read
    `140.1.2-02-SUMMARY.md`'s own count line for the number it added (0 if it has not landed).
    Then state the full arithmetic in the SUMMARY as
    `4743 + <mine> + <sibling> = <observed>`. Any RESIDUAL delta after the sibling term is
    accounted for must be TRACED (the 140.1.1 module-level-skip lesson) — never waved through,
    never assumed. Record counts in SUMMARY.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -q 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - 0 failed; skip count still 96; passed count reconciles exactly with the sibling wave-1
      term named EXPLICITLY — `4743 + <mine> + <sibling> = <observed>` shown in the SUMMARY,
      including how the sibling term was established (git log + that plan's SUMMARY, or 0).
    - mypy: Success, 89 files, 0 errors.
  </acceptance_criteria>
  <done>Full suite green with reconciled arithmetic; wave-1 boundary paid for this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker → MT5 gateway (RPyC) | venue-side credentials cross; retry policy governs load on the single-lock gateway |
| adapter → job classifier | ValidationResult carries the credential verdict |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.2-01 | Elevation of Privilege | mt5.py master-password rejection | mitigate | rejection byte-unchanged (acceptance criterion: diff shows only `permanent=True`); trade-capable login still never persisted |
| T-140.1.2-02 | Denial of Service | MT5 gateway retry amplification | mitigate | the fix itself: 3 serialised probes → 1 for permanent faults |
| T-140.1.2-03 | Information Disclosure | test fixtures / error_message | accept | offline stubs only, no live credentials; error_message carries fixed copy |
| T-140.1.2-SC | Tampering | package installs | accept | this phase installs nothing (RESEARCH Package Legitimacy Audit: N/A); any plan proposing an install is out of scope |
</threat_model>

<verification>
- Gate command output captured verbatim for the reproduce decision (one of three explicit exits).
- RED then GREEN both observed first-hand (or NOT-REPRODUCIBLE evidence recorded).
- Negative control proves mt5_enabled_server patch is load-bearing.
- Full suite + mypy at plan close, arithmetic reconciled.
</verification>

<success_criteria>
ROADMAP criterion 2 observably true: a permanent MT5 credential fault is never retried as
transient — or the phase records, with first-hand evidence, that the path is unreachable.
Zero behaviour change for any non-MT5 adapter; the master-password EoP gate untouched.
</success_criteria>

<output>
Create `.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-01-SUMMARY.md`
including: the gate's verbatim output, the decision taken, the half-A proof quotes, RED/GREEN
observations, and the count reconciliation. Never `git add` anything under `.planning/`.
</output>
