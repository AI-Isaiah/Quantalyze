---
phase: 140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/routers/internal.py
  - analytics-service/services/error_contract.py
  - analytics-service/routers/match.py
  - analytics-service/routers/portfolio.py
  - analytics-service/routers/simulator.py
  - analytics-service/tests/test_status_contract_exchange_internal.py
  - analytics-service/tests/test_match_router.py
  - analytics-service/tests/test_status_contract_match_sim_portfolio.py
  - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
autonomous: true
requirements: [PYAPIFIX2-03, PYAPIFIX2-04, PYAPIFIX2-06]
must_haves:
  truths:
    - "/internal/keys/{key_id}/permissions' 429 emits the service's own envelope (nested body.detail carrying code=RATE_LIMITED, retryable=true) with Retry-After: 60 — giving service_error's 429 arm its FIRST call site"
    - "Every user-facing 429 carries Retry-After: match.py force-recompute (dynamic wait_s), portfolio-bridge (3600), simulator (3600); plus the dead-route verify-strategy per-email 429 (3600) closed for class integrity"
    - "The error_contract.py comment that documented the internal.py bypass is corrected in the SAME commit as the migration (C-10)"
    - "The 500-dependency census comment reads the AST-re-derived figure (6 of 16 carry dependency), never 7+3 arithmetic — the planner-settled PYAPIFIX2-06 exception, fenced to comment lines in one file"
    - "The TS-OBLIGATIONS ledger gains the new obligation as TS-34 (its own ID space, no forked OB-N list) AND its count header is recounted to 34 in the same edit"
  artifacts:
    - path: "analytics-service/routers/internal.py"
      provides: "service_error(429, ...) migration at the _consume_rate_limit raise"
      contains: "service_error"
    - path: "analytics-service/tests/test_status_contract_match_sim_portfolio.py"
      provides: "Retry-After literal pins for bridge/simulator/verify-strategy 429s"
  key_links:
    - from: "analytics-service/routers/internal.py"
      to: "analytics-service/services/error_contract.py"
      via: "service_error(429, ...) construction validated by the _validate 429 arm"
      pattern: "service_error\\(\\s*429"
---

<objective>
Close PYAPIFIX2-03 (the /internal 429 adopts the service's own envelope — the 140.1.1 builder arm
gains its first call site) and PYAPIFIX2-04 (every user-facing 429 carries Retry-After — 4 sites,
header-only). Also lands the planner-settled PYAPIFIX2-06 census exception: the one code COMMENT
correction in error_contract.py, fenced and re-derived by AST.

Purpose: machine-classifiable throttles with honest waits; no fourth 429 body shape minted.
Output: envelope migration + 4 header additions + tests with literal-pinned values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
## Plan shape — why this is a separate plan, and how it is sequenced (revision B-4 / W-7)

⚠️ **CORRECTION TO AN EARLIER PLANNING CLAIM.** The split of this phase into four plans was
justified in planning-session prose as "zero file overlap" between plans 02 and 03. **That claim
is FALSE at source.** Plans 02 and 03 both modify THREE files:
`analytics-service/services/error_contract.py`, `analytics-service/routers/portfolio.py`, and
`.planning/phases/140.1-.../140.1-TS-OBLIGATIONS.md`. The correct statement is:

> **02 and 03 share three files; the wave-2 dependency (`140.1.2-03` has `depends_on:
> [140.1.2-02]`) sequences them — which is exactly what TRAP-8 requires.** Safety comes from the
> SEQUENCING, not from an absence of overlap.

**Do NOT re-parallelise 02 and 03 on the strength of the retracted claim** — running them in the
same wave would trip TRAP-8 (concurrent edits to the same file) on all three. The genuinely
parallel wave-1 pair is **01 vs 02**, whose `files_modified` sets were verified DISJOINT.

**W-7 — deliberate sizing call, confirmed in writing:** this plan carries 3 requirements
(PYAPIFIX2-03, -04, and the fenced -06 census comment) across 9 files — above the 5–8 target,
under the 10-file warning line. **This is deliberate, not drift.** The three units are
mechanically tiny (one raise migration, four `headers=` additions, one numeral in a comment),
they are strictly SEQUENCED into three tasks with independent verify commands, and they share one
file (`error_contract.py`) that would otherwise force a fourth plan and a fourth wave for two
comment lines. Splitting would raise total context cost, not lower it. Accepted.

@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-CONTEXT.md
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-PATTERNS.md  (§PYAPIFIX2-04 table R1–R6, §PYAPIFIX2-03, S-2, S-4)
@.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-RESEARCH.md  (§PYAPIFIX2-03, §PYAPIFIX2-04, C-5, C-8, C-9, C-10)

Binding rules: RED observed first-hand · literals typed in tests, never imported · locate every
site by CODE TEXT (three cited coordinates already drifted — C-4) · `cast()` never `# type: ignore`.

OQ-1 SETTLED BY THE PLANNER AT SOURCE (2026-07-26, quote-level): in
`src/app/api/keys/[id]/permissions/route.ts`, the non-OK arm does
`throw new Error(err.detail ?? ...)` and the catch only SUBSTRING-SNIFFS `Error.message`
(`INTERNAL_API_TOKEN` / `Upstream 5` / `ECONNREFUSED` / `not configured` / `aborted` / `timeout`)
then returns generic copy + 502. Today's scalar 429 detail matches none of those branches
(→ PROBE_FAILED); an object detail stringifies to "[object Object]" which ALSO matches none
(→ PROBE_FAILED). Classification outcome identical either way ⇒ the nested service_error shape
is SAFE here. Residual: server-side console.error message degrades to "[object Object]" on this
path — accepted; the laundering is fixed wholesale by 140.3 under ledger row **TS-34**
(provenance OB-9 — see Task 1's ledger step, revision W-5). Record this settlement in the
SUMMARY; the executor need not re-derive it but MUST NOT contradict it without reading the file.
</context>

<interfaces>
Verified at source at `39688d69` (re-locate by code text):

- `routers/internal.py` — the raise (after `_verify_internal_token(request)` — authn ordering is
  ASVS V2, preserve it): `if not _consume_rate_limit(key_id): raise HTTPException(status_code=429,
  detail="Too many permission probes for this key. Try again in a moment.",
  headers={"Retry-After": str(int(_RATE_LIMIT_WINDOW_S))})`. `_RATE_LIMIT_WINDOW_S = 60`
  (module constant ≈:115) ⇒ int() == 60, clears the retry_after <= 0 guard (OQ-3 closed).
- `services/error_contract.py` 429 arm requires: dependency None (omit kwarg) · retryable=True ·
  retry_after not None; `_retry_after_headers` then sets the header itself, so the explicit
  `headers=` kwarg is REPLACED by the builder, not silently deleted.
- The stale comment (C-10) in `services/error_contract.py` (≈:68-73): "two sites mint one
  directly and deliberately bypass this helper: routers/internal.py:227 … and the app-global
  RateLimitExceeded handler at main.py:526. _validate cannot reach either" — both clauses become
  false when the migration lands.
- The census comment (C-5 / M-8) in `services/error_contract.py` (≈:158-163): "six live sites
  legitimately name one of ours here … and seven pass none at all" — the true figure is 6 with
  `dependency` / 10 without, of 16 `service_error(500, …)` sites.
- PYAPIFIX2-04 sites and their IN-SCOPE wait sources (never RETRY_AFTER_SECONDS — the module's
  own docs forbid it for 429s; never a fresh literal in production code):
  | Site (by code text) | Wait source | Value |
  |---|---|---|
  | `routers/match.py` "force recompute for {allocator_id} throttled" raise | `wait_s` computed a few lines above (`int(FORCE_RECOMPUTE_MIN_INTERVAL_S - (_now - _last))`, interval = 30) | dynamic 0..30 |
  ⚠️ **I-5 — recorded coordinate discrepancy, no behavioural impact.** PATTERNS §R3 cites `wait_s`
  as "computed at `:1738`". At source (verified this revision) `:1738` is
  `_last = _force_last_run.get(allocator_id, 0.0)`; the guard is `:1739` and `wait_s = int(...)` is
  `:1740`. `FORCE_RECOMPUTE_MIN_INTERVAL_S = 30` is `:96`. This plan locates every site by CODE
  TEXT, so nothing changes — the note exists so the next reader does not "fix" `:1740` back to
  `:1738`. Do NOT edit PATTERNS.md (it is a frozen planning artifact); this line is the record.
  | `routers/portfolio.py` "Too many bridge requests for this user." raise | `_BRIDGE_USER_RATE_WINDOW_SEC` (≈:198) | 3600 |
  | `routers/simulator.py` "Simulator rate limit exceeded" raise | `_SIMULATOR_USER_RATE_WINDOW_SEC` (≈:112) — NOT `_SIMULATOR_USER_RATE_LIMIT` (=20, a COUNT) | 3600 |
  | `routers/portfolio.py` "Too many verification attempts for this email." raise | `_VERIFY_STRATEGY_EMAIL_RATE_WINDOW_SEC` (≈:165) | 3600 — DEAD route, class-integrity only |
- Existing tests to EXTEND, not duplicate (B2 sweep run by the planner): `test_match_router.py`
  `test_force_true_throttled_429_when_called_twice_quickly` (asserts 429, no header assertion —
  extend); `test_status_contract_match_sim_portfolio.py` has `"Retry-After" not in resp.headers`
  assertions ONLY on 500/400 arms (S-15/S-16/S-17) — NOT invalidated, leave byte-unchanged.
  No existing test touches the internal.py 429 detail copy (grep confirmed).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PYAPIFIX2-03 — migrate the /internal 429 onto service_error, comment fixed in the SAME commit</name>
  <files>analytics-service/routers/internal.py, analytics-service/services/error_contract.py, analytics-service/tests/test_status_contract_exchange_internal.py</files>
  <read_first>
    - analytics-service/routers/internal.py — the _consume_rate_limit raise block + _verify_internal_token ordering above it
    - analytics-service/services/error_contract.py — the full 429 arm of _validate, _retry_after_headers, and BOTH comment blocks named in <interfaces>
    - analytics-service/tests/test_status_contract_exchange_internal.py:441+ (internal-router fixture — the harness analog)
    - analytics-service/routers/exchange.py — the MT5_GATEWAY_UNREACHABLE service_error raise (the only existing retry_after-passing analog; copy its kwarg shape)
  </read_first>
  <behavior>
    - RED first: new test drives the internal-router fixture to a throttled state and asserts:
      status 429 · body["detail"]["code"] == "RATE_LIMITED" · body["detail"]["retryable"] is True ·
      body["detail"]["dependency"] is None · isinstance(body["detail"]["detail"], str) and equals
      the literal "Too many permission probes for this key. Try again in a moment." (typed in the
      test) · headers["Retry-After"] == "60" (literal — OQ-3). Observe RED (today: scalar detail,
      no code). Then GREEN after the migration.
    - Auth-ordering pin: an unauthenticated request never reaches the throttle arm (extend or
      confirm an existing _verify_internal_token test covers it; if none does, add one).
  </behavior>
  <action>
    Replace the raw raise with `raise service_error(429, "RATE_LIMITED", retryable=True,
    retry_after=int(_RATE_LIMIT_WINDOW_S), detail="Too many permission probes for this key. Try
    again in a moment.")` — code "RATE_LIMITED" is REUSED from the app-global handler's vocabulary
    (main.py flat 429), not re-minted; the human copy is byte-identical; the explicit headers=
    kwarg is dropped because _retry_after_headers now supplies it (state this in the commit body).
    Keep the raise strictly AFTER _verify_internal_token.

    IN THE SAME COMMIT (C-10 — the 140.1.1-05 precedent: the comment documents the very line
    being changed): rewrite the error_contract.py ≈:68-73 comment so it is true again — one site
    now bypasses the helper (the app-global RateLimitExceeded handler at main.py's 429
    JSONResponse), and _validate now DOES reach the internal.py 429. Re-derive the main.py line
    by code text, do not copy ":526".

    In-code note at the migrated site: this creates a THIRD coexisting 429 shape (flat main.py ·
    nested service_error · bare scalars at match/simulator/portfolio) — deliberate; the winner is
    picked by TS-23's owner (140.2/146), not here.

    Ledger (Edit tool, never git add .planning) — **W-5/W-6 corrected**:
    `140.1-TS-OBLIGATIONS.md`'s ID space is **TS-01..TS-33**, and its own header rule is *"if an
    item here is wrong, correct it here — do not fork a second list."* An `OB-9` row would fork a
    second ID space inside the one file that exists to prevent forking. Therefore:
    1. Append the row as **TS-34** (NOT OB-9), citing its provenance in the row body:
       *"(provenance: OB-9, 140.1.2 review evidence)"*. Content: `keys/[id]/permissions` launders
       the 429 into `502 / PROBE_FAILED` and discards `Retry-After`; the Python half now carries
       `code` + the header correctly; **owner 140.3**, kind `change`. Follow the file's existing
       column shape exactly (ID / Kind / Owner / Location / Finding / Remedy).
    2. Annotate the **TS-23** row in place: a third 429 shape now exists; its owner must pick the
       winning shape when migrating match/simulator. Correct in place, never duplicate the row.
    3. **W-6 — RECOUNT THE HEADER IN THIS PLAN, do not leave it to plan 03.** The header reads
       `**Obligation count: 33**` with a by-owner breakdown. Adding TS-34 makes it **34**, and
       `140.3` goes from 4 to 5. Update BOTH the total and the by-owner line, and extend the
       "Edited by Phase …" provenance note with a 140.1.2 clause naming TS-34. Rationale: if this
       plan lands and plan 03 is deferred or fails, an un-recounted header self-reports a false
       count — the exact artifact-overstatement defect PYAPIFIX2-06 exists to correct. Plan 03
       recounts again to 35 when it adds TS-35; two sequential recounts are correct, one skipped
       recount is not.

    Commits: `test(140.1.2-02): RED — /internal 429 lacks machine code (PYAPIFIX2-03)` then
    `feat(140.1.2-02): GREEN — /internal 429 adopts service_error, comment inverted same-commit`.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_status_contract_exchange_internal.py -q</automated>
  </verify>
  <acceptance_criteria>
    - RED observed and quoted (missing "code"/object-detail assertion fails today), then GREEN.
    - headers["Retry-After"] == "60" pinned as a string literal typed in the test.
    - `git show` of the GREEN commit touches internal.py AND the error_contract.py :68-73 comment
      together (same commit — C-10).
    - The OQ-1 settlement (nested shape safe for this consumer) recorded in SUMMARY.
    - service_error's 429 arm now has its FIRST call site — asserted on the WIRE, not by grep.
      ⚠️ **The gate `grep -rn "service_error(" analytics-service/routers/internal.py | grep 429`
      → 1 is RETRACTED and DELETED: it reproduces the B-3 defect with inverted polarity.**
      Every `service_error` call in this codebase is MULTI-LINE (`raise service_error(` on one
      line, the status on the NEXT), so a CORRECTLY-formatted migration scores **0**, not 1 —
      the gate would block a correct executor, or pressure a single-line contortion written
      purely to satisfy grep. Do not restore it in any form.
      The BINDING assertion is the wire contract already pinned in <behavior>:
      `body["detail"]["code"] == "RATE_LIMITED"` (with `retryable is True` and
      `dependency is None`) from the Task 1 test — that can only pass if the raise went through
      the builder's 429 arm, which is exactly the claim being made.
      If a source-shape sanity check is also wanted, use a multi-line-aware one and treat it as
      INFORMATIONAL, never blocking:
      `grep -A2 "service_error(" analytics-service/routers/internal.py | grep -c 429` → ≥1.
    - TS-OBLIGATIONS: row added as **TS-34** (never `OB-9` — `grep -n "^| \*\*OB-" 140.1-TS-OBLIGATIONS.md` prints nothing; the POSITIVE assertion is that a row whose ID cell reads `TS-34` exists, quoted in the SUMMARY), OB-9 provenance cited in the row body, TS-23 annotated in place, and the count header recounted to **34** with the by-owner line updated (140.3: 4 → 5).
  </acceptance_criteria>
  <done>The 429 builder arm has its first consumer; wire shape and header pinned by literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PYAPIFIX2-04 — Retry-After on all four bare 429s, header-only</name>
  <files>analytics-service/routers/match.py, analytics-service/routers/portfolio.py, analytics-service/routers/simulator.py, analytics-service/tests/test_match_router.py, analytics-service/tests/test_status_contract_match_sim_portfolio.py</files>
  <read_first>
    - The four raise sites by code text (table in <interfaces>) and each wait constant's definition line
    - analytics-service/tests/test_match_router.py — test_force_true_throttled_429_when_called_twice_quickly (extend this, do not duplicate)
    - analytics-service/tests/test_status_contract_match_sim_portfolio.py — its client fixtures and the S-4 literal-pin idiom
    - 140.1.2-PATTERNS.md §S-4 (tight band for dynamic values — survivor #5: `0 < x <= window` is NOT teeth)
  </read_first>
  <behavior>
    - RED first: header assertions fail today at all four sites (header absent).
    - match.py (dynamic): after seeding the throttle (two quick calls), assert
      resp.headers["Retry-After"] is present · int(header) equals the integer already interpolated
      in the body copy ("retry after {N}s" — parse N from body and assert header == str(N), a
      consistency pin, PLUS band `int(header) > 27 and int(header) <= 30` with 30 typed as a
      literal in the test (fresh-bucket band, > window*0.9). Note: wait_s CAN be 0 by int()
      truncation near expiry; the header deliberately matches the body's promised number
      (Retry-After: 0 = retry now, RFC-valid) — do NOT clamp with max(1,...), which would make
      header and body disagree.
    - bridge / simulator / verify-strategy (fixed windows): assert headers["Retry-After"] == "3600"
      — the string literal typed in the test, never imported from the module.
  </behavior>
  <action>
    Add `headers={"Retry-After": str(...)}` to each HTTPException, sourcing the value from the
    SAME constant the throttle check uses (table above): `str(wait_s)` at match.py;
    `str(_BRIDGE_USER_RATE_WINDOW_SEC)` at the bridge raise; `str(_SIMULATOR_USER_RATE_WINDOW_SEC)`
    (NOT the _LIMIT count) at the simulator raise; `str(_VERIFY_STRATEGY_EMAIL_RATE_WINDOW_SEC)`
    at the verify-strategy per-email raise. HEADER-ONLY: body shapes byte-unchanged (do not
    migrate these to any envelope — that would mint a FOURTH 429 shape in the phase that mints a
    third; TS-23's owner decides the winner).

    Dead-route confirmation BEFORE touching the verify-strategy site (one-command re-check, then
    state it): `grep -n "verify-strategy" src/lib/analytics-client.ts` → 0 hits among the nine
    seam paths; rationale recorded as CLASS INTEGRITY, not user impact.

    Commits: RED test commit first, then the four header additions + GREEN.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest tests/test_match_router.py tests/test_status_contract_match_sim_portfolio.py -q</automated>
  </verify>
  <acceptance_criteria>
    - RED observed at all four sites (quoted), then GREEN.
    - Three fixed-window pins are the string literal "3600" typed in tests; the match pin is
      body-consistency + the >27/<=30 band with 30 as a test literal.
    - `git diff` on the three routers shows ONLY headers= additions (no body/copy change).
    - Existing 429 test extended, not duplicated; the S-15/S-16/S-17 "Retry-After not in headers"
      assertions byte-unchanged (they cover 500/400 arms).
    - Dead-route check output recorded in SUMMARY.
  </acceptance_criteria>
  <done>6-site/3-shape map now has 0 user-facing 429s missing the header; shapes unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: The census comment (fenced PYAPIFIX2-06 exception) + plan-close gate</name>
  <files>analytics-service/services/error_contract.py</files>
  <read_first>
    - analytics-service/services/error_contract.py — the MEMBERSHIP-not-prohibition comment block (code text: "six live sites legitimately name one of")
    - 140.1.2-PATTERNS.md M-8 (the scope-collision settlement input; the six sites with dependency listed there)
  </read_first>
  <action>
    PLANNER SETTLEMENT (record verbatim in SUMMARY): PYAPIFIX2-06 is "artifact truth only — NO
    code", but the census lives in a CODE COMMENT, not in docs/STATUS_CONTRACT.md as REQUIREMENTS
    assumed. Settled as a NAMED, FENCED exception routed into THIS plan (which already edits this
    file's comments for C-10) so the PYAPIFIX2-06 artifact plan stays literally code-free.
    Rationale: leaving a known-false census in the module every 5xx site calls is worse than a
    numeral correction; the alternative (TODOS.md) was considered and rejected as deferring a
    2-line fix already co-located with this plan's edits.

    RE-DERIVE the figure at execution time by AST (never 7+3): walk every
    `service_error`/`service_error_response` call with status 500 across routers/, services/,
    main.py; count with/without a `dependency` kwarg. Expected from the planning-session sweep:
    16 sites, 6 with dependency, 10 without — but assert your own count and use IT. Edit only the
    numeral(s) in that one comment block. Own commit; `git show --stat` for it =
    `1 file changed` and the diff touches comment lines only.

    Then the plan-close gate: `cd analytics-service && python3 -m pytest -q` (unscoped) —
    96 skipped, 0 failed, and the passed count reconciles arithmetically as
    **4743 + (tests THIS plan added) + (tests added by any SIBLING WAVE-1 plan already landed).**
    Plans 01 and 02 are BOTH wave 1 and this command is UNSCOPED, so whichever of the two lands
    second also counts its sibling's tests — a bare `4743 + mine` will NOT reconcile, and that is
    expected, not a defect. Establish the sibling term FIRST-HAND before drawing any conclusion:
    `git log --oneline 39688d69..HEAD` to see whether plan 01's commits are present, and read
    `140.1.2-01-SUMMARY.md`'s own count line for the number it added (0 if it has not landed —
    note plan 01 may legitimately add tests on BOTH its branches). State the full arithmetic in
    the SUMMARY as `4743 + <mine> + <sibling> = <observed>`; any RESIDUAL delta after the sibling
    term is accounted for must be TRACED (the 140.1.1 module-level-skip lesson), never waved
    through, never assumed. Then
    `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` → 89 files clean.
  </action>
  <verify>
    <automated>cd analytics-service && python3 -m pytest -q 2>&1 | tail -3 && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/ 2>&1 | tail -1</automated>
  </verify>
  <acceptance_criteria>
    - The AST re-derivation output (counts + the six dependency-carrying sites) quoted in SUMMARY.
    - Census commit diff = comment-only lines in error_contract.py, nothing else.
    - Full suite 0 failed; arithmetic reconciled with the sibling wave-1 term named EXPLICITLY
      (`4743 + <mine> + <sibling> = <observed>`, and how the sibling term was established);
      mypy 89 files clean; 0 new `# type: ignore`.
  </acceptance_criteria>
  <done>The census states what the code supports; wave-1 boundary paid for this plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Next.js → /internal (X-Internal-Token) | authn boundary crossed before the throttle arm |
| anonymous/authed clients → match/simulator/portfolio 429s | throttle copy + headers reach untrusted callers |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.1.2-04 | Spoofing | internal.py auth ordering | mitigate | raise stays after _verify_internal_token; pinned by test (Task 1 acceptance) |
| T-140.1.2-05 | Information Disclosure | 429 bodies/headers | mitigate | fixed copy byte-identical; header values are window constants, no internal detail added |
| T-140.1.2-06 | Denial of Service | wrong Retry-After inviting retry storms | mitigate | values sourced from the throttle's own window constants, pinned by literals/band (survivor-#5 lesson) |
| T-140.1.2-SC | Tampering | package installs | accept | zero installs this phase |
</threat_model>

<verification>
RED observed at every site before GREEN · all pins literal or banded per S-4 · C-10 comment and
migration in one commit · census re-derived by AST · full suite + mypy at plan close.
</verification>

<success_criteria>
ROADMAP criteria 3 and 4 observably true; the census comment true; no new 429 body shape beyond
the deliberate, documented third; zero TypeScript files touched.
</success_criteria>

<output>
Create `.planning/phases/140.1.2-pyapi-fix2-close-the-venue-transient-class-on-the-live-route/140.1.2-02-SUMMARY.md`
with RED/GREEN quotes, the OQ-1 settlement record, the dead-route check, the AST census output,
and count reconciliation. Never `git add` anything under `.planning/`.
</output>
