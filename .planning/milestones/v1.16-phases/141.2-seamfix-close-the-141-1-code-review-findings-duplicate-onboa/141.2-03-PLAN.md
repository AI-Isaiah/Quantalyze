---
phase: 141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/cron/flag-monitor/route.ts
  - src/app/api/cron/flag-monitor/route.test.ts
  - tests/integration/cron-flag-monitor.test.ts
autonomous: true
requirements: [SEAM-05]
tags: [flag-monitor, denominator, attempt-grain, postgrest-max-rows, fail-loud]

must_haves:
  truths:
    - "Findings 2+3+4+7 / D-02: the denominator is the server-side COUNT (`count: 'exact', head: true`) — uncapped at any volume, impossible to influence from the wire, and explicitly ATTEMPT-grained on both sides of the ratio; the dedup and its wire-controllable key are gone"
    - "Finding 12 / D-05: a PostgREST read error produces a DISTINCT `denominator_read_failed` terminal outcome (getNumerator's exact discriminated-union shape) — it can never be reported as zero traffic, and handleZeroDenominator's diagnosis stays true for the one state it names"
    - "The docblock's false seam-crossing sentence ('correlation_id is minted once per client call … a retried call reuses it') is corrected by NAME, not silently deleted; the retained per-attempt-write sentence becomes the load-bearing half of the new rationale; the known downward bias under retry is stated as a limitation"
    - "getNumerator is untouched — the dead-path repair, SCOPE HONESTY block, environment:production term, and shape-resilient parse survive byte-for-byte"
  artifacts:
    - path: "src/app/api/cron/flag-monitor/route.ts"
      provides: "counting-form getDenominator returning { kind: 'ok'; total } | { kind: 'terminal'; res } with reason denominator_read_failed; rewritten docblock"
    - path: "src/app/api/cron/flag-monitor/route.test.ts"
      provides: "ONE shape-distinguishing audit_log double with per-test row-path payloads; dedup tests deleted; SC-F/G/I pins + the SC-H ratio pin"
    - path: "tests/integration/cron-flag-monitor.test.ts"
      provides: "SC-N double moved to the counting shape; end-to-end alert-at-threshold still proven"
  key_links:
    - from: "src/app/api/cron/flag-monitor/route.ts handle()"
      to: "getDenominator"
      via: "terminal-kind early return, same shape as the numerator's at the call site"
      pattern: "denominator.kind === \"terminal\""
    - from: "getDenominator"
      to: "audit_log"
      via: "server-side COUNT, no row materialisation"
      pattern: "count: \"exact\", head: true"
---

<objective>
Close findings 2, 3, 4, 7 and 12 as ONE rewrite of `getDenominator`. The D-16 denominator is
attacker-steerable (dedup key = inbound `X-Correlation-Id`, reachable via the UNAUTHENTICATED
`/api/verify-strategy`), silently truncating (unbounded `.select()` vs PostgREST
`max_rows=1000` — proven against PROD: 7350 rows, exactly 1000 returned, HTTP 200), and
collapses nothing on the only flows it targets (proven on the wire: 42/42 prod audit rows
carry server-re-minted bare UUIDs, zero carry `wizard:`). Its stated premise is false at the
seam. D-02: revert to the counting form and make the ratio explicitly ATTEMPT-grained on both
sides — which needs no dedup at all, removing all three defects in one move.

NOT a literal revert (RESEARCH §0.4 — this contradicted the original D-02 default and is
settled): the pre-141.1 form destructured only `count` and used `?? 0`, i.e. finding 12's
defect in silent form. The target is counting form + explicit error capture + a distinct
`denominator_read_failed` terminal outcome — finding 12 fixed INSIDE this rewrite (D-05
sequencing, mandatory not optional).

Honest-claims discipline (RESEARCH §2.7, Pitfall 6): this monitor has NEVER computed a
denominator in production (the streak flag row has never been written). These are genuine
correctness defects, not live-firing incidents — the SUMMARY says "would have caught", never
"did stop", and must not claim a live-impact fix.

Purpose: findings 2, 3, 4, 7, 12 (D-02 + D-05). SC-F, SC-G, SC-H, SC-I.
Output: an uncapped, wire-uninfluenceable, attempt-grained denominator with a fail-loud read
path, plus doubles that can actually disagree with production.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-CONTEXT.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-RESEARCH.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-PATTERNS.md
@.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite getDenominator — counting form, terminal-on-error, attempt grain (SC-F, SC-G, SC-I)</name>
  <files>src/app/api/cron/flag-monitor/route.ts, src/app/api/cron/flag-monitor/route.test.ts</files>
  <read_first>
    - 141.2-PATTERNS.md §1 (the three sanctioned answers — D-02 picks shape C) and §2 (the getNumerator discriminated-union shape to copy VERBATIM, same file: SentryFetchResult at ~:84-86, the terminal returns at ~:141-147 and ~:166-184, handle()'s consumption at ~:347-348)
    - 141.2-RESEARCH.md §2.5 (the target code shape) and §2.6 (what the revert must NOT touch: getNumerator's dead-path repair at :128-133 is D-16's GOOD half — Pitfall 1: revert by FUNCTION, never by commit; also: the unattributable-singleton branch at ~:232-240 is subsumed by a raw count — say so where it is removed)
    - src/app/api/cron/flag-monitor/route.ts — getDenominator (:213-241) and the D-16 docblock (:192-215, incl. the FALSE sentence at ~:204-207 and the TRUE per-attempt-write sentence to retain); handleZeroDenominator (~:244-300, whose email body names the two causes that must stay the only two); handle() (~:330-412)
    - src/app/api/cron/flag-monitor/route.test.ts — the audit_log fetch double and the (1d)/(1e) dedup tests (~:237-252) that die with the dedup; the mock docblock at ~:50-54
  </read_first>
  <behavior>
    THE DOUBLE (one fixture, shared by Tests 3 and 4, per-test payloads): the audit_log double
    is index-simulating and SHAPE-DISTINGUISHING — it inspects the query it receives and answers
    the `head: true` COUNT chain with the scenario's true total, while any row-MATERIALISING
    select is answered with a row payload the individual test configures. The two mutations below
    need DIFFERENT row-path payloads, so the row-path answer must be per-test configurable — a
    single hardcoded row payload can red one mutation while lying for the other (the URL-blind
    double is how the original defect survived; VALIDATION Oracle Independence flags this).
    - Test 1 (SC-I): the audit_log double answers with a PostgREST error → `handle()` returns the `{ ok: false, reason: "denominator_read_failed" }` envelope, the zero-denominator streak flag is NOT advanced, and no zero-traffic alert path executes. RED today (error currently collapses to 0 and routes to handleZeroDenominator).
    - Test 2 (SC-I positive control): a genuinely-zero count still routes to handleZeroDenominator — the two states remain distinct in BOTH directions.
    - Test 3 (SC-F): count chain answers `count: 3000`; the row path is configured to serve a 1000-row page (the PostgREST max_rows cap simulation) → the computed denominator is 3000. Under mutation SC-F (restored `.select()`), the code hits the row path, gets 1000, and this test REDs.
    - Test 4 (SC-G): count chain answers `count: 200`; the row path is configured to serve 200 rows whose `metadata.correlation_id` is ONE shared wire-supplied id → the denominator is 200, not 1. Under mutation SC-G (reintroduced dedup), the code must materialise rows to dedup, sees the 200 identical ids, collapses to 1, and this test REDs. Phrased against the count contract: the count reflects ROWS regardless of metadata shape.
  </behavior>
  <action>
    RED first: write Tests 1, 3, 4 against the current tree, run, paste the failing output.

    In `src/app/api/cron/flag-monitor/route.ts`:
    - Rewrite `getDenominator` to the counting form with explicit error capture, returning the
      SAME discriminated-union shape `getNumerator` already uses in this file:
      `{ kind: "ok"; total: number } | { kind: "terminal"; res: NextResponse }`. On error:
      `console.warn` with the error, return terminal with
      `NextResponse.json({ ok: false, reason: "denominator_read_failed" })`. NEVER return 0 on
      error — 0 is `handleZeroDenominator`'s meaning. No new email/page for a read failure
      (research OQ recommendation: distinct envelope + warn only; adding a page to a monitor
      that has never run is speculative). The query: `.select("id", { count: "exact", head:
      true })` filtered on `entity_type` and the window start — server-side COUNT, no cap
      (assumption A5: prod already returned an exact `*/7350` count well above max_rows).
    - `handle()`: consume the terminal kind exactly as it consumes the numerator's.
    - Delete the dedup, the row materialisation, and the unattributable-singleton branch (note
      in the diff/commit why the raw count subsumes the singleton handling).
    - Docblock, SAME commit, docblock-correction idiom (never delete a wrong claim silently):
      name the false sentence — "correlation_id is minted once per client postProcessKey call,
      above the retry loop, so a retried call reuses it" — say it was true in TypeScript and
      false end-to-end (the Python handler re-mints a uuid4 whenever the inbound id is not a
      bare UUID, and wizardFetch sends a prefixed id on both retry-eligible flows; proven on
      the wire: all prod audit rows carry server-minted bare UUIDs). RETAIN and re-purpose the
      per-attempt-write sentence (the audit emit fires before both dedup pre-checks, once per
      HTTP attempt — verified: OQ-2) as the load-bearing half of the new rationale: Sentry
      events are per-attempt, audit rows are per-attempt, so the ratio is internally consistent
      at ATTEMPT grain with no dedup needed. State the known limitation honestly: retries
      inflate the denominator, so the rate biases DOWNWARD (quieter) when the seam retries —
      the safe direction versus false pages and wire-controlled silence; a true per-request
      rate needs a server-minted request id the retry reuses (deferred, out of scope). Also
      note the two attempt-visibility caveats: 429/401-refused attempts write no row, and the
      write is fire-and-forget (a lost row biases the rate upward — also the safe direction).
    - Do NOT touch `getNumerator` (:88-189): its query, the SCOPE HONESTY block, the
      `environment:production` term, and `parseSentryCount` survive unchanged.

    In `route.test.ts`: rebuild the audit_log double per the DOUBLE spec in behavior — count
    chain answers `{ count, error }` from the scenario's true total; row-materialising path
    answers a PER-TEST configurable payload (Test 3: a 1000-row page; Test 4: 200 rows sharing
    one correlation id). DELETE the (1d)/(1e) dedup tests with the dedup; update the mock
    docblock to describe the counting contract and the per-test row-path configuration.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/cron/flag-monitor/route.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - RED-before-GREEN pasted for Tests 1, 3, 4.
    - MUTATION SC-F (exact, from the VALIDATION ledger): restore the unbounded row-materialising `.select()` → Test 3 REDs (its configured row path serves a 1000-row page; denominator caps). Restore → GREEN. Paste both.
    - MUTATION SC-G (exact): reintroduce a dedup on the correlation-id metadata → Test 4 REDs (its configured row path serves 200 identical ids; dedup collapses to 1). Restore → GREEN. Paste both.
    - MUTATION SC-I (exact): collapse the query error back to `return 0` (or its kind-"ok"-total-0 equivalent) → Test 1 REDs AND Test 2 still distinguishes the states. Restore → GREEN. Paste both.
    - The double's row-path payload is per-test configurable and DIFFERS between Tests 3 and 4 — reviewed in the diff (one hardcoded row payload for both would let one of the two mutations pass).
    - DEF-16-2 discipline (the four rules are BINDING for this file — 141.1-08's `grep -c "path:" → 0` criterion is the canonical failure, actual answer 2, both in the mandated docblock): NO absence greps against route.ts for `correlation_id`, `distinct`, `dedup`, or `path:` — the rewritten docblock is REQUIRED to name all of them. Presence greps only: `grep -c 'count: "exact", head: true' src/app/api/cron/flag-monitor/route.ts` ≥ 1 and `grep -c "denominator_read_failed" src/app/api/cron/flag-monitor/route.ts` ≥ 1. The absence of row materialisation is proven BY TEST (Test 3's shape-distinguishing double), not by grep.
    - `getNumerator` byte-unchanged: `git diff src/app/api/cron/flag-monitor/route.ts` shows no hunk overlapping the getNumerator function body or its docblock.
  </acceptance_criteria>
  <done>The denominator is uncapped, wire-uninfluenceable, attempt-grained, and fail-loud on read errors; the two zero-states are distinct in both directions; the docblock states the corrected seam-crossing truth and the honest bias limitation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Grain pinned by a ratio over one attempt-level scenario + integration double moved to the counting shape (SC-H)</name>
  <files>tests/integration/cron-flag-monitor.test.ts, src/app/api/cron/flag-monitor/route.test.ts</files>
  <read_first>
    - tests/integration/cron-flag-monitor.test.ts — the SC-N end-to-end alert-at-threshold case and its Supabase double (currently index-simulating over rows; must move to the counting shape)
    - 141.2-RESEARCH.md §2.4 — the verified per-ATTEMPT audit-write grain (the write fires before both dedup pre-checks; each retried HTTP attempt re-enters the endpoint) — this is the premise the grain pin encodes
  </read_first>
  <behavior>
    - Test 1 (SC-H, grain pin — ONE attempt-level scenario driving BOTH sides of the ratio):
      model N user requests of which ONE retried request produced 2 attempts. The Sentry double
      returns 2 events (that request's two failures); the audit double answers the count chain
      with N+1 (attempt rows) AND its row-materialising path is configured so the retried
      request's two rows share ONE correlation id — the dedup path, if any code takes it, sees
      the pair as one request. Assert the computed RATE — the ratio the alert compares against
      the threshold — equals 2/(N+1). This is what makes SC-H independent proof of GRAIN rather
      than arithmetic over the fixture: a request-grain dedup on the denominator collapses the
      pair (rate becomes 2/N — red), and a request-grain collapse on the numerator side would
      yield 1/(N+1) — red. SC-F/SC-G assert the denominator's magnitude and wire-independence;
      this pin asserts the two SIDES agree on the attempt unit, which neither SC-F nor SC-G
      exercises. The pin's failure message states the grain contract: numerator EVENTS per
      attempt, denominator ROWS per attempt — mismatched grain on EITHER side reds this test.
    - Test 2 (end-to-end, retained): the alert still fires at threshold through the real handle() pipeline with the counting double — the SC-N property survives the rewrite.
  </behavior>
  <action>
    Move the integration test's Supabase double for audit_log to the `{ count, error }`
    head:true shape (same shape-distinguishing, per-test row-path discipline as Task 1's unit
    double). Keep the end-to-end alert-at-threshold assertion intact — it is the proof the
    monitor can still page when the Sentry env is finally configured.

    Add the SC-H grain pin per Behavior Test 1. It lives wherever it can drive the real rate
    computation end-to-end through `handle()` (unit file acceptable if the integration double
    cannot express the per-test row payload under head:true — in that case pin the grain at the
    unit level and keep the integration file's job as threshold-fires). Wherever it lands, the
    assertion is on the RATIO from one scenario, not on the denominator alone.
  </action>
  <verify>
    <automated>npx vitest run tests/integration/cron-flag-monitor.test.ts src/app/api/cron/flag-monitor/route.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - MUTATION SC-H (exact, from the VALIDATION ledger): dedupe the denominator to request grain → the grain pin REDs via the RATIO (2/N ≠ 2/(N+1) on its scenario, with the double serving the shared-id pair on the dedup path). Restore → GREEN. Paste both runs.
    - The grain pin drives numerator AND denominator from one scenario and asserts the ratio — reviewed in the diff: it does not merely re-assert the count the double returned (that property already belongs to SC-F/SC-G).
    - The end-to-end alert-at-threshold case GREEN with the counting double (no assertion weakened to make it pass — if the threshold arithmetic changed, that is a defect, not a fixture problem).
    - Both files green under Node 22 PATH (`PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run …`).
  </acceptance_criteria>
  <done>The attempt-grain contract is pinned by a ratio test that reds on a reintroduced dedup on either side, and the end-to-end alert path is proven against the counting shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| anonymous wire → audit metadata | `X-Correlation-Id` is caller-supplied via the unauthenticated teaser route |
| audit_log → alert arithmetic | the denominator drives founder paging decisions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-141.2-04 | Tampering / Repudiation | getDenominator | mitigate | count rows server-side; no caller-influenced key anywhere in the computation (V4: do not introduce a new caller-supplied field) |
| T-141.2-08 | DoS (false paging / silenced alert) | denominator truncation | mitigate | head:true COUNT has no max_rows cap (proven against prod); shape-distinguishing doubles keep it honest |
| T-141.2-09 | Repudiation (wrong diagnosis) | error-collapse to 0 | mitigate | distinct `denominator_read_failed` terminal outcome; V7 discipline: warn logs the PostgREST error only — never request credentials |
| T-141.2-SC | Tampering | package installs | accept | zero package installs in this phase |
</threat_model>

<verification>
- `npx vitest run src/app/api/cron/flag-monitor/route.test.ts tests/integration/cron-flag-monitor.test.ts` green at final tree
- SC-F, SC-G, SC-H, SC-I mutations observed RED with pasted output, restored GREEN
- `npm run typecheck` + `npm run lint` clean; Node 22 PATH reproduction before declaring stable
- VALIDATION.md rows SC-F..SC-I flipped to Observed ✅; Per-Task Verification Map Status filled for findings 2,3,4,7,12
</verification>

<success_criteria>
- Findings 2, 3, 4, 7, 12 closed in one rewrite; getNumerator untouched
- All four ledger mutations observed; doubles can disagree with production
- SUMMARY uses "would have caught" framing — no live-impact claim (the monitor has never computed a denominator in prod)
</success_criteria>

<output>
Create `.planning/phases/141.2-seamfix-close-the-141-1-code-review-findings-duplicate-onboa/141.2-03-SUMMARY.md` when done. Include the four RED outputs and the operational-reality statement (monitor inert in prod; suspected Sentry env cause is [ASSUMED] and confirming it is an operator task, not a phase task).
</output>
