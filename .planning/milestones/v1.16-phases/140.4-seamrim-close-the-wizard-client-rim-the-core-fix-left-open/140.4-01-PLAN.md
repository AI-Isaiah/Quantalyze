---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/strategyGate.ts
  - src/lib/strategyGate.test.ts
  - src/app/api/admin/strategy-review/route.ts
  - src/app/api/admin/strategy-review/route.test.ts
autonomous: true
requirements: [SEAMRIM-01]

must_haves:
  truths:
    - "An admin cannot publish a strategy whose trade-history span was never read"
    - "A read failure on the admin approve path answers 503 about US, never a gate verdict about the strategy"
    - "The refusal holds for a third consumer that forgets the checked read, because it lives in the gate"
  artifacts:
    - path: "src/lib/strategyGate.ts"
      provides: "A gate that refuses to evaluate an unrepresentable span instead of passing"
      contains: "StrategyGateUnevaluableError"
    - path: "src/app/api/admin/strategy-review/route.ts"
      provides: "error bound on every read that participates in the publish decision"
  key_links:
    - from: "src/app/api/admin/strategy-review/route.ts"
      to: "src/lib/strategyGate.ts"
      via: "checkStrategyGate throws rather than returning passed:true on a null span"
      pattern: "StrategyGateUnevaluableError"
---

<objective>
Close the **publish-side** half of C-3 — the finding CONTEXT §4 grades **CRITICAL+ and re-graded UP**,
and which CONTEXT §5c escalated further: the fail-open is not only in the wizard, it is at the
**admin publish gate**, and *an admin publishing an under-history track record as verified outranks
everything else in this phase.*

**The demonstration, executed by the researcher against HEAD and re-derivable with `npx tsx`:**

```
timestamps READ OK (2-day span): {"passed":false,"code":"INSUFFICIENT_DAYS","reason":"Trades span only 2.0 day(s)…"}
timestamp reads FAILED         : {"passed":true,"code":null,"reason":null,"detail":null}
```

`src/lib/strategyGate.ts:164-165` is `const spanDays = computeSpanDays(…); if (spanDays !== null && spanDays < STRATEGY_GATE_MIN_DAYS)`.
When the `earliest`/`latest` reads fail, `spanDays` is `null`, `spanDays !== null` is false, **the entire
7-day gate is skipped**, and the function falls through to PASS.

`checkStrategyGate` has exactly **two** production consumers — measured at plan time:
```
$ grep -rn "checkStrategyGate" src/ | grep -v "\.test\."
src/app/api/admin/strategy-review/route.ts:11    (import)
src/app/api/admin/strategy-review/route.ts:156   (call)
src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:14   (import)
src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:1124 (call)
```
This plan owns the **gate leaf** (which covers both consumers, and any third) and the **admin route**.
Plan `140.4-02` owns the wizard's seven reads.

## Coverage-law classification (CONTEXT §2 — binding)

- **Task 1 — the gate refuses to evaluate: ROW 1 (shared artefact — a domain leaf both tiers pass
  through). Measured coverage for row 1 across this programme: 100%.** It closes the publish-side harm
  at both consumers in one file, without touching either call site, and it holds for a future third
  consumer that forgets the checked read.
- **Task 2 — binding `error` on the admin route's reads: ROW 3 (per-site edit). PARTIAL BY
  CONSTRUCTION** — measured coverage for row 3 in this programme is 1/8 · 2/56 · 0/32. It is
  acceptable here **only** because the set is bounded and fully enumerated below (10 reads, 3 already
  checked, 7 to fix), and because Task 1's row-1 remedy stands behind it.

## The predicate and scope fence for "unchecked read" in this plan (CONTEXT §5c — MANDATORY)

CONTEXT §5c: *"DO NOT PICK A NUMBER, STATE A PREDICATE."* Three honest repo-wide measurements
disagree (32 / 73 / 139) because they measured different things. **This plan does not use any of them.**

- **Predicate P:** an awaited postgrest result, in either destructure shape (object `{data}`/`{count}`
  or a whole-result binding), whose value participates in the **admin approve decision**, and whose
  `error` field is never bound.
- **Scope fence:** `src/app/api/admin/strategy-review/route.ts`, the `action === "approve"` path only.
- **Derived from P over the fence, RE-MEASURED at HEAD `a77d607e` (corrected at plan-check —
  the first derivation was short by two, in the phase whose theme is *derive, never transcribe*):**
  **12 reads total** — 6 in the first-pass `Promise.all` (`:98-115`), 1 `api_keys` lookup (`:136-141`),
  3 in the TOCTOU re-check `Promise.all` (`:199-221`), **1 `strategy_keys` member count (`:280`)** and
  **1 `compute_jobs` latest-stitch-job read (`:298`)** — the last two inside the approve block and both
  feeding 409 gates.
  **5 bind `error` today:** `csvCountError` `:104` · `keyRowError` `:137` · `recheckCsvError` `:201` ·
  `memberCountError` `:280` · `stitchJobError` `:298`. **7 do not** — and **7 is the operative figure**,
  unchanged by the correction: the remedy below is complete.

The repo-wide backlog is explicitly **NOT** closed by this plan and is not claimed to be.

## What this plan deliberately does NOT do

- It does **not** add a `WizardErrorCode` or a `WIZARD_ERROR_COPY` entry, and it does **not** touch
  `src/lib/wizardErrors.ts`. Plan `140.4-03` owns that file in this wave; two plans editing it in one
  wave is TRAP-8 verbatim (*"two fixes touching the same file in one pass reintroduced each other's
  bugs"*). It also avoids inventing user-facing copy, which is founder-owned (CONTEXT §7.1).
- It does **not** use `.throwOnError()`. That is plan `140.4-02`'s decision surface; see that plan for
  the two measured reasons it was rejected at the wizard site.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-CONTEXT.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-RESEARCH.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-PATTERNS.md

<standing_constraints>
Binding on every task. You are a fresh agent with no memory of this phase's discussion; these are not optional.

- **Verify with the FULL suite, never a scoped path.** Five separate times in this programme a
  scoped-green run would have shipped a red tree. Baselines as recorded in CONTEXT §8:
  `npx vitest run` → **9792 passed | 287 skipped** (715 files) · `cd analytics-service && python3 -m pytest -q`
  → **4778 passed / 96 skipped** · `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors (1 pre-existing
  warning) · `mypy --strict` clean. Re-establish the baseline BEFORE you edit; if it does not match,
  STOP and report rather than absorbing someone else's red.
- **`vi.spyOn` + `vi.restoreAllMocks()`. NEVER `vi.stubGlobal`** — DEF-16-1, this repo's known
  CI-only failure cause (CI Node 22 vs local Node 25).
- **Zero new packages.** Nothing in this phase installs a dependency.
- `.planning/` is **gitignored with no git backup**. Never `git add .planning`. Never `gsd-sdk query state.*`.
  Never edit `.planning/STATE.md` or `.planning/ROADMAP.md` — the orchestrator owns them.
- **Branch protection on `main` is OFF** (settled founder decision). Every claim about a gate is
  *"would have caught"*, **never** *"did stop"*.
- **grep hygiene, and this programme paid for it repeatedly:** grep is **LINE-BASED** (DEF-16-2) — it
  missed two `status_code=` continuation lines inside the very population CONTEXT §3 uses as its
  cautionary example. **`grep -h` with a downstream filename filter is a NAMED PROHIBITION** (it
  produced three wrong counts). Use `grep -F` for paths containing `[id]`. Comment-strip before
  counting (`grep -vE '^\s*(//|\*)'`) — seven executors in this programme were bitten by their own
  comments matching their own greps.
- **Every pre-measured value in this plan was taken on the UNTOUCHED tree at HEAD `a77d607e`.**
  Re-run the stated command first. If it disagrees, STOP and report — do not adjust the number.
</standing_constraints>

<read_first>
- `src/lib/strategyGate.ts` in full (213 lines). Anchors, by TEXT not line: the `GateFailureCode`
  union; `StrategyGateInput`'s docstring for `earliestTradeAt` — *"Timestamp of the earliest trade, or
  null when no trades exist"*; `function computeSpanDays`; and the trade-sourced `else` branch that
  contains `if (input.tradeCount < STRATEGY_GATE_MIN_TRADES)` followed by
  `const spanDays = computeSpanDays(…)`.
- `src/app/api/admin/strategy-review/route.ts:90-265`. Read **all three** read groups and **both**
  existing fail-loud guards. Their comments state the rule this plan extends, verbatim:
  - `:117-120` — *"Fail LOUD on a csv-count read error rather than coercing to 0…"*
  - `:145-148` — *"Fail LOUD (WR-01): a coerced `isLedgerBacked=false` on a transient read error
    would reject a legitimate Deribit onboarding…"*
  **Both defend the fail-CLOSED direction (a false rejection). Neither defends the fail-OPEN
  direction, which is the one that publishes.** That asymmetry is the finding.
- `src/app/api/admin/strategy-review/route.ts:174-175` — `{ status: "published", review_note: null }`.
  This is the write the gate is the only thing standing in front of.
- `src/lib/strategyGate.test.ts` (363 lines) and `src/app/api/admin/strategy-review/route.test.ts`
  (958 lines) — the existing cases. **TRAP-9: a relaxed assertion silently deletes coverage.** If an
  existing case pins the current pass-through, land the strictly stronger replacement in the SAME
  commit and record the swap verbatim in the SUMMARY.
- `analytics-service/services/error_contract.py:180-216` — **the in-repo analog for this task's
  shape** (per `140.4-PATTERNS.md` §11): a constructor whose per-status arms raise with the *reason*
  in the message when the input is unrepresentable. Copy the discipline, not the code.
</read_first>

<interfaces>
**The contract change, stated precisely.** `checkStrategyGate` becomes **partial**: it answers a
verdict for representable inputs and **refuses** for unrepresentable ones. The refusal is a thrown
`StrategyGateUnevaluableError` exported from the same module.

The unrepresentable input is exactly one: **the trade-sourced branch, `tradeCount >= STRATEGY_GATE_MIN_TRADES`,
and `computeSpanDays(...) === null`.** In that branch trades demonstrably exist, and the input type's
own docstring says the timestamps are null *only* when no trades exist — so `null` there is a
**contradiction, not an absence**. Every other `null` combination keeps its current meaning.

**Why a throw rather than a new `GateFailureCode`:** a new code would force a
`gateFailureToWizardError` arm and user-facing copy in `src/lib/wizardErrors.ts` — a file another
wave-1 plan owns (TRAP-8) and whose sentences are founder-owned (CONTEXT §7.1). Both consumers
already have a fail-loud path for a throw: the wizard's heavy arm catches at
`SyncPreviewStep.tsx:1191` and escalates through `heavyFetchErrorsRef`; the admin route gains an
explicit catch in Task 2. Record this reasoning in the SUMMARY — the alternative is not wrong, it is
out of scope for this wave.

**Both consumers must keep behaving identically for every representable input.** The gate's existing
verdicts, thresholds and the documented `EXACTLY 7.0 days passes` boundary are untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: strategyGate refuses to evaluate an unrepresentable span (ROW 1 — a shared domain leaf)</name>
  <files>src/lib/strategyGate.ts, src/lib/strategyGate.test.ts</files>
  <read_first>
- `src/lib/strategyGate.ts` in full — the `else` (trade-sourced) branch, `computeSpanDays`, and the
  `StrategyGateInput` docstrings for `earliestTradeAt` / `latestTradeAt`.
- `src/lib/strategyGate.test.ts` — every existing case touching `INSUFFICIENT_DAYS` and every case
  that passes `earliestTradeAt: null` / `latestTradeAt: null` with a non-zero `tradeCount`. Any such
  case pins the current fall-through and is TRAP-9 territory.
  </read_first>
  <behavior>
- `tradeCount = 5`, `earliestTradeAt = null`, `latestTradeAt = null`, analytics complete, no CSV rows,
  not ledger-backed → **throws `StrategyGateUnevaluableError`**. Today this returns
  `{passed:true, code:null}` — the case reddens on the untouched tree, which is the receipt.
- `tradeCount = 5`, a 2-day span → still `{passed:false, code:"INSUFFICIENT_DAYS"}` (unchanged).
- `tradeCount = 5`, a 7.0-day span → still passes (the documented boundary is unchanged).
- `tradeCount = 0` with null timestamps → still `INSUFFICIENT_TRADES`, **no throw** (the null is a
  genuine absence there, and this is the case that distinguishes the fix from a blanket null-ban).
- The CSV / daily-returns branch with null timestamps → unchanged, **no throw**.
- `StrategyGateUnevaluableError` is exported, is an `instanceof Error`, and its message names the
  contradiction (trade count present, span unreadable) and contains **no** user data.
  </behavior>
  <action>
Export `class StrategyGateUnevaluableError extends Error` from `src/lib/strategyGate.ts`. Give it a
`name` and a **static** message naming the contradiction; interpolate only `input.tradeCount` (an
integer we produced), never a timestamp, an id or a reason string.

In the trade-sourced `else` branch, **after** the `tradeCount < STRATEGY_GATE_MIN_TRADES` return and
**before** the `spanDays < STRATEGY_GATE_MIN_DAYS` comparison, refuse when `spanDays === null`.
Do not change the comparison itself, do not change any threshold, and do not touch the CSV branch.

Write a docblock stating (a) why `null` is a contradiction *in this branch specifically*, quoting the
input type's own docstring, and (b) that the gate is now partial and every caller must have a
fail-loud arm. **Reference the input field by NAME, never by line number** — P2 measured 19 citations
checked / 5 wrong, and `2d58fd45`, whose stated purpose was *"a pointer cannot drift"*, invalidated four.

Add the behaviour cases above. Hand-type the expected verdicts; do not derive an expectation from the
function under test.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/strategyGate.test.ts</automated>
  </verify>
  <acceptance_criteria>
- **Pre-value, re-measure before you start:** `grep -c "spanDays !== null" src/lib/strategyGate.ts` → **1**.
  After the change it is still **1** — the comparison is guarded, not deleted. (A criterion satisfiable
  by deleting production code is not a check; this one is not.)
- `grep -c "StrategyGateUnevaluableError" src/lib/strategyGate.ts` ≥ **2** (the class and the throw).
- The refusal case exists and **fails on the untouched tree**: before editing `strategyGate.ts`, add
  only the test and run it — paste the RED output into the SUMMARY. A case that passes both before and
  after proves nothing.
- `grep -cE "STRATEGY_GATE_MIN_(DAYS|TRADES|CSV_ROWS)\s*=\s*[0-9]" src/lib/strategyGate.ts` → **3**,
  and each literal is unchanged from `7`, `5`, `7`.
- `git diff --name-only` contains **zero** occurrences of `src/lib/wizardErrors.ts` (TRAP-8: another
  wave-1 plan owns that file).
- `npx vitest run src/lib/strategyGate.test.ts` green; `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>
The gate refuses to evaluate a trade-sourced strategy whose span is unreadable, every representable
input keeps its existing verdict, and the refusal case reddened on the untouched tree.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the admin publish path binds `error` on all 7 unchecked reads and catches the gate's refusal</name>
  <files>src/app/api/admin/strategy-review/route.ts, src/app/api/admin/strategy-review/route.test.ts</files>
  <read_first>
- `src/app/api/admin/strategy-review/route.ts:90-265` — all three read groups, both existing fail-loud
  guards and their comments, the `checkStrategyGate` call, and the `{ status: "published" }` write.
- `src/app/api/strategies/finalize-wizard/route.ts` — the checked-read template
  `140.4-PATTERNS.md` §9 names as **exact**: `.maybeSingle()` → `if (xErr)` → `console.error(…)` →
  **500 about US**; *then* `if (!row)` → **404 about the row**. Two facts, two responses. Locate it by
  text, not line.
- `src/app/api/admin/strategy-review/route.test.ts` — the existing approve/reject cases and the two
  existing 503 read-error cases. **Do not weaken any of them.**
  </read_first>
  <behavior>
- A `strategies` read error on the approve path → **503 with the file's existing sentence**, and **no**
  `strategies` UPDATE is issued.
- A `trades` **count** read error → 503, no UPDATE. (A count that comes back `null` with no error is
  also a refusal — `?? 0` is the fabrication this whole finding is about.)
- An `earliestTrade` **or** `latestTrade` read error → 503, no UPDATE. **This is the case that would
  have caught the publish-side fail-open**: today the route publishes.
- A `strategy_analytics` read error → 503, no UPDATE.
- A TOCTOU re-check `trades` count read error → 503, no UPDATE.
- A TOCTOU re-check `strategy_analytics` read error → 503, no UPDATE.
- `checkStrategyGate` throwing `StrategyGateUnevaluableError` → 503, no UPDATE (defence in depth: the
  reads above should make it unreachable, and the case proves the arm exists anyway).
- **Anti-regression, load-bearing:** a clean approve still publishes (200 + `status: "published"`), and
  a genuine `INSUFFICIENT_DAYS` still 400s with its existing message. Both must stay green unmodified.
  </behavior>
  <action>
Bind `error` on the **7** reads enumerated in the objective and answer the file's existing 503
sentence — *"Cannot verify strategy data source. Please try again."* — reusing it verbatim rather than
authoring new copy. Mirror the two existing guards exactly: `console.error` with a stable
`[admin/strategy-review]` prefix naming which read failed, then the 503 return.

For the two **count** reads that feed the gate (`tradeCount`, and the re-check trade count), a `null`
count with no error is equally unrepresentable — refuse rather than `?? 0`. Say so in the comment.

Wrap the `checkStrategyGate` call in a `try`/`catch` narrowed to `StrategyGateUnevaluableError`
(`instanceof`), returning the same 503. Rethrow anything else — swallowing an unknown throw here is
how a fail-open comes back.

**Do not change the reject path.** Its single `.eq('id')` UPDATE is idempotent and out of scope.

Add the behaviour cases above. For every 503 case assert **both** the status **and** that no publish
UPDATE was issued — a status-only assertion cannot see a route that answers 503 after writing.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/app/api/admin/strategy-review/ src/lib/strategyGate.test.ts</automated>
  </verify>
  <acceptance_criteria>
- **THE CRITERION IS THE DEFECT ITSELF, not a proxy count.** The first-pass `Promise.all` destructure
  at `route.ts:98-115` binds `error:` on **1** of its **6** members today (`csvCountError`). After the
  change it binds `error:` on **all 6**. Measure it over that block only:
  `awk 'NR>=98 && NR<=115' src/app/api/admin/strategy-review/route.ts | grep -c "error:"` → **1** today,
  **6** after. Re-derive the line window from the code text (`const [` … `] = await Promise.all([`)
  if the file has moved.
  ⚠️ **Do NOT use a whole-file `grep -c "error:"`.** Measured on the untouched tree it returns **24**,
  because it matches every response body (`{ error: "Unauthorized" }`, `{ error: "Cannot verify…" }`,
  …), not read bindings. A `≥ 10` post-condition over that grep is **already satisfied by the
  untouched tree** — a broken implementation returns the passing value. This was caught at plan-check
  and is recorded as hazard 9 in `140.4-VALIDATION.md`.
- **Corroborating count (secondary, not the gate):**
  `grep -c 'console.error("\[admin/strategy-review\]' src/app/api/admin/strategy-review/route.ts`
  → **5** today, **≥ 12** after (one per newly-guarded read plus the gate-refusal arm). State the exact
  post-value in the SUMMARY.
- `grep -vE '^\s*(//|\*)' src/app/api/admin/strategy-review/route.ts | grep -c '?? 0'` — record the
  pre-value and the post-value; the post-value must be **strictly lower**, and every surviving `?? 0`
  must be named in the SUMMARY with the reason it is safe. (A count of 0 is not a target — the reject
  path may legitimately keep one.)
- The earliest/latest read-error case exists and **fails on the untouched tree** (today the route
  publishes). Paste the RED output.
- The clean-approve case and the genuine-`INSUFFICIENT_DAYS` case are **byte-unchanged** in the test
  file: `git diff -U0 src/app/api/admin/strategy-review/route.test.ts | grep -c '^-.*it("'` → **0**,
  or every removal is listed in the SUMMARY with its strictly stronger replacement (TRAP-9).
- `grep -c "StrategyGateUnevaluableError" src/app/api/admin/strategy-review/route.ts` ≥ **1**.
- No new user-facing sentence was authored: every new response body string already exists in this file
  on the untouched tree.
  </acceptance_criteria>
  <done>
Every read that participates in the admin approve decision binds `error`; each failure answers 503
about us with no publish write; the gate's refusal has an explicit arm; and the clean-approve and
genuine-rejection paths are untouched.
  </done>
</task>

<task type="auto">
  <name>Task 3: falsify both halves — Falsifiability Ledger rows M90 and M91</name>
  <files>src/lib/strategyGate.ts, src/app/api/admin/strategy-review/route.ts</files>
  <read_first>
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md` — the
  `## Falsifiability Ledger` rules block and the `## Gap-closure addendum` rows M86–M89 for the row
  format and the standard of evidence. **Read it; do not edit it.** The highest existing row in the
  programme is **M89**.
  </read_first>
  <action>
Run **two** mutations, one at a time, each reverted before the next. Each is a **SEMANTIC** edit to
**PRODUCTION** source — never a syntax error, never a type error, never a test edit. Record each row in
this plan's SUMMARY under a `## Falsifiability Ledger — rows M90, M91` heading (rows live in the
SUMMARY, not in a shared file — four wave-1 plans run in parallel and a shared ledger file would race).

**M90 — the gate's refusal is LIVE, not merely present.**
Mutation: in `src/lib/strategyGate.ts`, change the refusal's condition so it fires only when
`input.tradeCount < 0` (a value the branch can never hold), leaving the class, the throw and every
other line intact. This is the distinguishing mutation CONTEXT demands: *only a positive case can
falsify a carry* — a deleted guard and an inert guard both produce "no throw", so the mutation must
make the guard **present but unreachable**, not absent.
Must redden: the Task 1 refusal case.
Also record what stayed GREEN (the representable-input cases), because a mutation that reddens
everything proves the suite is coupled, not that this property is pinned.

**M91 — the admin route's fail-open arm is LIVE, and it is the SECOND member of the class.**
The class here is "a gate-feeding read whose error is bound". CONTEXT: **mutate the SECOND member.**
The first member (`csvCountError`) was already guarded before this plan; the second is the
`earliestTrade` read this plan added. Mutation: in
`src/app/api/admin/strategy-review/route.ts`, drop the `error` binding from the **`latestTrade`** read
and restore its `?? 0`/`?: null` coercion — i.e. re-create the defect at one member of the class while
leaving the other six fixed.
Must redden: the Task 2 latest-read-error case, and **not** the earliest-read-error case. Record both
observations; if the earliest case also reddens, the two cases are not independent and you must say so.

For each row record: the exact diff, the command, the pasted RED output with the failing test name and
assertion text, the named GREEN-side observations, and confirmation that `git status` is clean after
revert. **"Asserted — not observed" is not acceptable.** Branch protection is OFF, so write
*"would have caught"*, never *"did stop"*.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- Two rows, M90 and M91, each with pasted RED output including the failing test name.
- Each row names at least one thing that stayed GREEN under its mutation.
- M91's row states explicitly that it mutated the **second** member of the class and names the first.
- `git status --porcelain` after both reverts shows only the expected working-tree entries
  (` M TODOS.md` and the untracked `analytics-service/scripts/nautilus_factsheet.py`) plus this plan's
  own committed changes — **no residual mutation**.
- **FULL suite green:** `npx vitest run` matches or exceeds the 9792/287 baseline (state the exact
  numbers), `npx tsc --noEmit` exit 0, `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>
Both halves are proven falsifiable by observed RED output, the tree is restored, and the full suite is
green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres → admin approve handler | A read failure arrives **as a value**, not as a throw (supabase-js 2.110.1), so an unchecked destructure is indistinguishable from real data |
| admin approve handler → `strategies.status` | The only thing between an admin's click and a published, publicly-trusted track record |
| `strategyGate` leaf → both consumers | A verdict returned for an input the gate could not actually evaluate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.4-01-01 | **Tampering (integrity of a trust signal)** | `strategyGate.ts` trade-sourced branch | mitigate | The gate throws `StrategyGateUnevaluableError` when trades exist but the span is unreadable, instead of falling through to PASS. Row 1 — covers both consumers and any third. Falsified by M90 |
| T-140.4-01-02 | **Tampering** | `admin/strategy-review` approve path, 7 unchecked reads | mitigate | Every read that participates in the decision binds `error` and answers 503 with no UPDATE. Row 3, **partial by construction**, acceptable only because the set is bounded and enumerated (10 reads, 3 pre-checked, 7 fixed). Falsified by M91 |
| T-140.4-01-03 | **Information disclosure** | the new 503 arms | avoid | Every arm reuses a sentence that already exists in this file; the Postgres error object goes to `console.error` only, never into a response body. No new copy is authored |
| T-140.4-01-04 | **Denial of service (self-inflicted)** | over-refusing on a legitimate approve | avoid | The refusal fires only in the trade-sourced branch with `tradeCount >= 5`; the `tradeCount = 0` and CSV branches keep their current behaviour, each pinned by a named case |
| T-140.4-01-05 | Repudiation | a silent 503 with no diagnostic trail | mitigate | Every arm logs with the existing `[admin/strategy-review]` prefix naming which read failed, mirroring the two guards already in the file |
| T-140.4-01-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed.** `140.4-RESEARCH.md` §Package Legitimacy Audit records that this phase proposes none, so no `[ASSUMED]`/`[SUS]` checkpoint is owed |
</threat_model>

<verification>
- `npx vitest run` (FULL, never scoped) at or above **9792 passed | 287 skipped**; state the exact numbers.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors (1 pre-existing warning).
- Zero Python files modified. Zero `supabase/migrations/` files added. Zero `.planning/` paths staged.
- `src/lib/wizardErrors.ts` is **not** in the diff (TRAP-8 — plan `140.4-03` owns it this wave).
- Reproduce the CI Node version before declaring green:
  `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` (CI is Node 22, local is Node 25 — this
  repo's known CI-only failure class).
</verification>

<success_criteria>
- `checkStrategyGate` refuses to evaluate a trade-sourced strategy with an unreadable span; every
  representable input keeps its existing verdict and the 7.0-day boundary is unchanged.
- The admin approve path binds `error` on all 7 previously-unchecked reads and answers 503 with no
  publish write; the gate's refusal has its own arm.
- Both properties are falsified by observed RED (M90, M91), with M91 mutating the **second** member.
- The full vitest suite, `tsc` and `lint` are green.
</success_criteria>

<output>
Create `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-01-SUMMARY.md`.

Record: the pre- and post-values of every acceptance grep; the 10 admin reads with which 3 were already
checked and which 7 were fixed; the RED output for both new behaviour cases on the untouched tree; any
TRAP-9 assertion swap verbatim (old and new); the reasoning for a throw rather than a new
`GateFailureCode`; and the `## Falsifiability Ledger — rows M90, M91` section with pasted RED output,
named GREEN-side observations and the second-member statement.
</output>
