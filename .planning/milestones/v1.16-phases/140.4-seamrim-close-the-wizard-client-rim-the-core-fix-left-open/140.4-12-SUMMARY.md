---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 12
subsystem: wizard-client-rim
tags: [seamrim-08, seamrim-11, wire-render-vocabulary, coverage-law-row-1, c-3]
requirements: [SEAMRIM-08]
branch: worktree-agent-a492ec6468414743a
base: fdff98399374417503af566b1550e1c49f08f57d

requires:
  - "140.4-02 — the seven-member `.error` binding in SyncPreviewStep's single-key terminal arm"
  - "140.4-11 — the destructive-control property (same file, inverted affordance)"
  - "140.4-14 — the `no-unchecked-supabase-read` rule whose ONE true positive this plan closes"
provides:
  - "the ONE wire→wizard table is AUTHORITATIVE at both wizard surfaces; no second roster overrules a successful translation"
  - "the nested `service_error` envelope's code is read at both surfaces (`seamErrorCode`, not a top-level `data.code`)"
  - "SyncPreviewStep.tsx reports ZERO sites under 140.4-14's rule — the glob can now widen to it"
affects:
  - "140.4-14's ratchet: `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx` is now admissible to the scoped `error` glob"

tech-stack:
  added: []
  patterns:
    - "translate through the ONE table first; a second membership check keeps only its own vocabulary"
    - "a read FAILURE and an ABSENT row are different facts — maybeSingle answers no-rows with error: null"

key-files:
  created: []
  modified:
    - "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx"
    - "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx"
    - "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
    - "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx"

decisions:
  - "DELETE the second roster's jurisdiction over wire codes (coverage-law ROW 1) rather than add two members to it (ROW 2, partial by construction)"
  - "the prescribed M104 ordering mutation is a NO-OP and was PROVEN so (22 files / 333 tests green under it); substituted a same-shape re-qualification mutation that preserves every property M104 was designed to prove"
  - "the freshness-probe read logs and falls through to a COLD START rather than throwing — the fail-open direction cannot occur there and refusing would be a self-inflicted outage"
  - "140.4-11's vacuity fence RAISED 11 → 14 rather than left at 11, so the three newly reachable codes cannot silently stop being driven"

metrics:
  tasks: 3
  commits: 7
  duration_minutes: 95
  completed: 2026-07-29
---

# Phase 140.4 Plan 12: The wire↔render vocabulary boundary — Summary

**One-liner:** A wire code the ONE translation table recognises now renders its own copy at **both**
wizard surfaces without a second roster edit — the honest `RATE_LIMITED` sentence ("the cap is ours,
not your exchange's") is reachable for the first time since `140.3-01` minted it — the nested Python
envelope's code is read at both, and the file's last unchecked Supabase read is closed.

- **Branch:** `worktree-agent-a492ec6468414743a`
- **Base:** `fdff9839` (`chore: merge executor worktree (worktree-agent-aa08afaacb85b5d38)`)

## Commits

| Commit | Type | Subject |
|---|---|---|
| `e88ff360` | `test` | RED — a translated wire code is overruled by a second roster (SEAMRIM-08) |
| `8e328b0b` | `feat` | GREEN — the ONE translation table is authoritative (SEAMRIM-08) |
| `3e824ea7` | `test` | RED — the kickoff arm has no translation hop at all (SEAMRIM-08) |
| `2965877d` | `feat` | GREEN — the kickoff arm translates before it membership-checks |
| `93ed5e5c` | `test` | re-run 140.4-11's destructive-control pin over the enlarged set |
| `90c24062` | `test` | RED — the resume freshness probe swallows a read failure (SEAMRIM-11) |
| `cf5e8f88` | `fix` | GREEN — the resume freshness probe binds error (SEAMRIM-11 / C-3) |

---

## Measured pre/post — every value published WITH its predicate

> Hazard #9 (VALIDATION.md): *a bare integer without its predicate is unowned by construction.*
> Every value below was measured on the **untouched tree at `fdff9839`** before a line was written.

### The three rosters — unchanged in SIZE and in MEMBERSHIP

**P1 — `KNOWN_FINALIZE_CODES` members.** Predicate: comment-stripped lines of `SubmitStep.tsx`
(dropping lines whose first non-space characters are `//`, `*` or `/*`) matching a bare quoted
SCREAMING_SNAKE entry `^\s*"[A-Z][A-Z0-9_]*",?$`.

| tree | value |
|---|---|
| untouched (`fdff9839`) | **9** |
| after | **9** |

Membership diffed line-for-line pre vs post: **identical** (`diff` reports no change; the nine are
`KEY_SCOPE_BROADENED`, `KEY_NETWORK_TIMEOUT`, `GATE_DRAFT_GONE`, `GUARD_BLOCKED`,
`COMPOSITE_MEMBERSHIP_UNKNOWN`, `COMPOSITE_TOO_MANY_MEMBERS`, `SERVICE_UNAVAILABLE_RETRY`,
`SERVICE_UNREACHABLE`, `SEAM_MISCONFIGURED`).

**P2 — `SEAM_CODE_TO_WIZARD_CODE` members.** Predicate: comment-stripped lines of `wizardErrors.ts`
inside the Map literal matching a `["WIRE", "WIZARD"]` pair.

| tree | value |
|---|---|
| untouched | **6** |
| after | **6** — the file is **absent from the diff entirely** |

**P3 — `KNOWN_KICKOFF_CODES` members.** Predicate: comment-stripped lines of `SyncPreviewStep.tsx`
matching `^\s*[A-Z][A-Z0-9_]*: "[A-Z_]+",$`.

| tree | value |
|---|---|
| untouched | **5** |
| after | **5**, same five keys, same five values |

⇒ **This is the criterion that distinguishes a structural fix from another roster edit, and it holds:
the two codes became reachable with every list byte-unchanged in membership.**

**P4 — `git diff --name-only fdff9839..HEAD`** → exactly four paths, all inside the plan's fence:
```
src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx
src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
```
Occurrences of `src/lib/wizardErrors.ts`, `src/lib/seam-discriminator.ts`, `src/lib/envelope.ts`,
`src/components/error/ErrorEnvelope.tsx`, `DESIGN.md` → **0**. Zero Python, zero
`supabase/migrations/`, zero `.planning/` paths staged with source, zero `eslint.config.mjs`.
`git diff --stat fdff9839..HEAD -- package.json package-lock.json` → **EMPTY** (zero packages).

**P5 — the oracle literal.** `grep -n "EXPECTED_TABLE_SIZE" src/lib/wizardErrors.test.ts` → four
lines, two of them `const EXPECTED_TABLE_SIZE = 55;` **hand-typed**, unchanged. This plan minted no
code and no copy, so neither literal had cause to move.

**P6 — `seamErrorCode` at `SubmitStep.tsx`.** Predicate: raw `grep -c "seamErrorCode"`.

| tree | value |
|---|---|
| untouched | **0** |
| after | **3** (the import, one comment mention, one call) — comment-stripped: **2** |

**P7 — import lines from `@/lib/seam-discriminator`.** Predicate:
`grep -c 'from "@/lib/seam-discriminator"'`.

| file | untouched | after |
|---|---|---|
| `SubmitStep.tsx` | **1** | **1** — `seamErrorCode` added to the EXISTING import, not a second one |
| `SyncPreviewStep.tsx` | **1** | **1** — same |

### TRAP-9 — nothing was removed or weakened

**Predicate:** `git diff -U0 fdff9839..HEAD -- <both test files> | grep -E '^-.*\bit\('` → **empty.
Zero `it(` lines removed from either file.**

The *complete* set of removed lines across both test files is six, and all six are the
`140.4-11` vacuity-fence comment and its literal, replaced by a **strictly higher** fence:

```
-    // A scanner that drove nothing would report agreement forever. Eleven
-    // DISTINCT codes reach this render today (140.4 CONTEXT §4, independently
-    // re-derived by the pattern-mapper); the literal is hand-typed so it
-    // cannot be satisfied by the loop measuring itself.
-      "Fewer than eleven distinct codes were actually driven. A loop that " +
-    ).toBeGreaterThanOrEqual(11);
```

No assertion was deleted, no oracle derived, no expected value read off `WIZARD_ERROR_COPY`.

---

## The control flow, before and after

### Surface 1 — `SubmitStep.tsx`, finalize response handler

**Before** (a successful translation had to re-qualify through a second, independently-maintained
membership check):
```ts
const translated = recogniseSeamErrorCode(data.code);
const candidate = translated === "UNKNOWN" ? data.code : translated;
const surfaced: WizardErrorCode =
  candidate && KNOWN_FINALIZE_CODES.has(candidate as WizardErrorCode)
    ? (candidate as WizardErrorCode)
    : "UNKNOWN";
```

**After:**
```ts
const wireCode = seamErrorCode(data);
const translated = recogniseSeamErrorCode(wireCode);
const surfaced: WizardErrorCode =
  translated !== "UNKNOWN"
    ? translated
    : wireCode && KNOWN_FINALIZE_CODES.has(wireCode as WizardErrorCode)
      ? (wireCode as WizardErrorCode)
      : "UNKNOWN";
```

Both lists are typed `WizardErrorCode`, so the second gate added **no type safety** — only an
obligation to edit two lists in one commit, written down twice inside the set and skipped anyway.

### Surface 2 — `SyncPreviewStep.tsx`, kickoff failure arm

**Before** (no translation hop at all; the lookup indexed the RAW WIRE STRING):
```ts
const surfaced =
  (typeof failure?.code === "string" && KNOWN_KICKOFF_CODES[failure.code]) ||
  "SYNC_FAILED";
```

**After:**
```ts
const wireCode = seamErrorCode(failure);
const translated = recogniseSeamErrorCode(wireCode);
const surfaced: WizardErrorCode =
  translated !== "UNKNOWN"
    ? translated
    : (wireCode !== null && KNOWN_KICKOFF_CODES[wireCode]) || "SYNC_FAILED";
```

`KNOWN_KICKOFF_CODES ∩ {CIRCUIT_OPEN, UPSTREAM_TIMEOUT, UPSTREAM_NETWORK_ERROR, SEAM_MISCONFIGURED}`
was **∅**, so all four rendered `SYNC_FAILED` — whose copy asks the user *which step failed* about a
computation that never started.

---

## The RED output, against the untouched tree

### `RATE_LIMITED` + `VALIDATION_FAILED` + the nested envelope at `SubmitStep` (commit `e88ff360`)

```
 ❯ src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx (33 tests | 3 failed)
     × [140.4-12] a RATE_LIMITED wire code renders the HONEST rate-limit copy — the cap is OURS
     × [140.4-12] a VALIDATION_FAILED wire code renders its own member, not UNKNOWN
     × [140.4-12] the NESTED python envelope's code is read — a top-level-only reader misses 21 codes

 FAIL … > [140.4-12] a RATE_LIMITED wire code renders the HONEST rate-limit copy — the cap is OURS
AssertionError: expected 'UNKNOWN' to be 'RATE_LIMITED' // Object.is equality

Expected: "RATE_LIMITED"
Received: "UNKNOWN"

 FAIL … > [140.4-12] a VALIDATION_FAILED wire code renders its own member, not UNKNOWN
AssertionError: expected 'UNKNOWN' to be 'VALIDATION_FAILED' // Object.is equality

 FAIL … > [140.4-12] the NESTED python envelope's code is read — a top-level-only reader misses 21 codes
AssertionError: The nested envelope carried RATE_LIMITED and the render did not see it. The flat and
nested shapes must produce the SAME state — the leaf that reads both already exists and is already
imported here.: expected 'UNKNOWN' to be 'RATE_LIMITED'

 Test Files  1 failed (1)
      Tests  3 failed | 30 passed (33)
```

**`Received: "UNKNOWN"` is the measured proof that `140.3-01` shipped with zero user-visible effect
for the two codes it added.** The negative control in the same commit
(*NEGATIVE CONTROL — a real seam wire code with no wizard member still resolves to UNKNOWN*, driving
`SEAM_DEGRADED`) was **green before and after**.

### `CIRCUIT_OPEN` and its three siblings at `SyncPreviewStep` (commit `3e824ea7`)

```
 ❯ src/app/(dashboard)/…/SyncPreviewStep.render.test.tsx (50 tests | 5 failed)
     × [140.4-12] a breaker trip (CIRCUIT_OPEN) renders the breaker's own state, not SYNC_FAILED
     × [140.4-12] a seam transport failure (UPSTREAM_TIMEOUT) renders SERVICE_UNREACHABLE, not SYNC_FAILED
     × [140.4-12] a seam transport failure (UPSTREAM_NETWORK_ERROR) renders SERVICE_UNREACHABLE, not SYNC_FAILED
     × [140.4-12] our own configuration fault (SEAM_MISCONFIGURED) reaches its own state
     × [140.4-12] the NESTED python envelope's code is read at this arm too

 FAIL … > [140.4-12] a breaker trip (CIRCUIT_OPEN) renders the breaker's own state, not SYNC_FAILED
Error: CIRCUIT_OPEN is a WIRE code with no entry in KNOWN_KICKOFF_CODES, so it fell to SYNC_FAILED —
a sentence that claims we fetched trades and the computation did not complete. The breaker declined
to send the request; nothing was fetched and nothing ran.:
expect(element).toHaveAttribute("data-error-code", "SERVICE_UNAVAILABLE_RETRY")

Expected the element to have attribute:
  data-error-code="SERVICE_UNAVAILABLE_RETRY"
Received:
  data-error-code="SYNC_FAILED"

 Test Files  1 failed (1)
      Tests  5 failed | 45 passed (50)
```

Green in the same commit, before and after the fix: the **five-member roster anti-regression**
`it.each` and the `SEAM_DEGRADED` negative control.

### The freshness-probe read failure (commit `90c24062`) — see the `:521` section below

---

## The rewritten comments, verbatim

### `SubmitStep.tsx` — replacing the block that stated the right principle and then contradicted it

The removed text argued *"The mapping lives in `wizardErrors.ts` (`recogniseSeamErrorCode`) — the ONE
wire→wizard table — never in a second copy here"* and then applied a second gate anyway. Its
replacement:

```
        // 140.4-12 / SEAMRIM-08 — EACH LIST OWNS ITS OWN VOCABULARY, AND
        // NEITHER OVERRULES THE OTHER.
        //
        //   `SEAM_CODE_TO_WIZARD_CODE` (`recogniseSeamErrorCode`) owns the WIRE
        //   vocabulary — the codes the seam and the analytics service put on
        //   the wire. It is the ONE wire→wizard table and it is authoritative:
        //   a code it translates is surfaced as translated.
        //
        //   `KNOWN_FINALIZE_CODES` above owns the ROUTE-MINTED WIZARD
        //   vocabulary — codes `finalize-wizard` mints that are already
        //   `WizardErrorCode` members (`KEY_SCOPE_BROADENED`,
        //   `GATE_DRAFT_GONE`, `GUARD_BLOCKED`, …) and therefore never appear
        //   in a wire→wizard table at all.
        //
        // WHAT THIS REPLACED, AND WHY IT IS NOT A ROSTER EDIT. 140.3-05 ran the
        // translation and then made the RESULT re-qualify through
        // `KNOWN_FINALIZE_CODES`. Both are typed `WizardErrorCode`, so the
        // second gate added NO type safety — it added only an obligation to
        // edit two lists in one commit. That obligation is written down twice
        // in the set above and was skipped anyway: 140.3-01 added
        // `VALIDATION_FAILED` and `RATE_LIMITED` to the translation table, the
        // second list never learned about them, and BOTH collapsed to UNKNOWN.
        // The honest rate-limit copy — the cap is ours, not the user's exchange
        // — existed and was unreachable for three commits. Widening the roster
        // fixes those two codes and leaves the next author the same trap; this
        // deletes the roster's jurisdiction over wire codes instead, so there
        // is no second list to forget.
        //
        // THE MEMBERSHIP CHECK IS NOT WEAKENED, and this is the part to verify
        // before "simplifying" it: an unrecognised wire code still resolves to
        // UNKNOWN by BOTH paths — the table answers "UNKNOWN" for anything it
        // does not list, and the raw code then still has to be a known finalize
        // code. No identity rule is created, so `SEAM_DEGRADED` and the venue
        // codes cannot be cast into this vocabulary (that is the reason the
        // table in `wizardErrors.ts` is explicit at all).
        //
        // THE CODE IS READ THROUGH THE LEAF, not off the top level. Every
        // `service_error()` answer nests it at `body.detail.code`;
        // `seamErrorCode` handles BOTH shapes and is the same leaf
        // `seamCorrelationId` below already comes from, so the id and the code
        // are read from the same body by the same reader.
```

### `SyncPreviewStep.tsx` — the kickoff arm

```
          // 140.4-12 / SEAMRIM-08 — TRANSLATE FIRST, THEN MEMBERSHIP-CHECK.
          //
          // Until this, the lookup below was the ONLY hop and it indexed the
          // RAW WIRE STRING. `KNOWN_KICKOFF_CODES` holds wizard codes OUR OWN
          // route mints, and its intersection with the seam's wire vocabulary
          // (`CIRCUIT_OPEN`, `UPSTREAM_TIMEOUT`, `UPSTREAM_NETWORK_ERROR`,
          // `SEAM_MISCONFIGURED`) is EMPTY — so a breaker trip or a dead
          // upstream during the kickoff rendered SYNC_FAILED, which asks the
          // user which step failed about a computation that never started.
          // `SubmitStep` has had the translation hop since 140.3-05; this
          // sibling arm never did.
          //
          // Order matters and is the whole change: `SEAM_CODE_TO_WIZARD_CODE`
          // (the ONE wire→wizard table, in `wizardErrors.ts`) answers for WIRE
          // codes, and `KNOWN_KICKOFF_CODES` answers for the route-minted ones
          // it has always owned. NEITHER LIST GAINED A MEMBER — the roster's
          // reasoning below (why the two 400 arms map to VALIDATION_FAILED and
          // why no new union member was minted) is unchanged and still governs
          // its own domain.
          //
          // The fallback is NOT weakened: an unrecognised wire code leaves the
          // translation at "UNKNOWN" and then misses the roster, so it still
          // reaches SYNC_FAILED — deliberately, because a map pretending to be
          // total turns an unfamiliar code into a silent undefined state.
          //
          // The code is read through `seamErrorCode`, the same leaf
          // `seamCorrelationId` below already comes from, so a nested
          // `service_error` body (`body.detail.code`) is seen rather than read
          // as a body carrying no code at all.
```

`KNOWN_KICKOFF_CODES`' own long docblock — why `MISSING_STRATEGY_ID` / `INVALID_STRATEGY_ID` map to
`VALIDATION_FAILED`, why no new union member was minted, and why the copy table's hand-typed size
guard is not touched — is **preserved verbatim**, byte-unchanged.

---

## 140.4-11's destructive-control pin, re-run against the enlarged reachable set

**Pin name:** `PROPERTY — no reachable terminal code renders the destructive control as its SOLE
affordance` (`SyncPreviewStep.render.test.tsx`, describe `[140.4-11] SyncPreviewStep — the
destructive control must be EARNED`).

| | before this plan | after |
|---|---|---|
| scenario rows driven | **11** | **14** |
| DISTINCT codes observed (read back off `data-error-code`) | **11** | **14** |
| hand-typed vacuity fence | `>= 11` | `>= 14` |
| result | green | **green** |

The three added rows are production INPUTS (a 503 carrying the breaker's wire code, a 504 on a
transport deadline, a 500 naming our own configuration fault) — never the code they produce. The
codes they turn out to mint are `SERVICE_UNAVAILABLE_RETRY`, `SERVICE_UNREACHABLE` and
`SEAM_MISCONFIGURED`.

**The fence was RAISED rather than left at 11**, so the new rows cannot silently stop driving
anything, and it is **proven live**: temporarily set to 15 it fails with
```
AssertionError: Fewer than fourteen distinct codes were actually driven. …:
expected 14 to be greater than or equal to 15
```
⇒ exactly **14** distinct codes are driven. Reverted to 14 immediately.

**No newly reachable code arrives with a destructive sole affordance, and the reason is structural
rather than lucky:** none of the three has `try_another_key` in its `actions`
(`SERVICE_UNAVAILABLE_RETRY` and `SERVICE_UNREACHABLE` are `clear_and_retry + request_call`;
`SEAM_MISCONFIGURED` is `request_call + expand_log`), and `140.4-11` derives the destructive control
from that field. Their control rows are `[retry, back-to-strategies]`, `[retry, back-to-strategies]`
and `[back-to-strategies]`. The derived-population pin absorbed them with no change to its property
assertions — **which is what "derived" was supposed to buy, and it did.**

---

## ⭐ The explicit addition — `SyncPreviewStep.tsx:521` is closed, with its own receipt

`140.4-14` built `quantalyze/no-unchecked-supabase-read` and found its **single true positive** here:
the resume freshness probe in the mount effect, ~530 lines above the `Promise.all` `140.4-02`
converted. `140.4-14` could not fix it (its fence was the rule, not this file) and correctly refused
to silence it, so its glob had to be narrowed to a file with zero sites.

**Before:**
```ts
const { data: existing } = await supabase
  .from("strategy_analytics")
  .select("computation_status, computed_at")
  .eq("strategy_id", strategyId)
  .maybeSingle();
```

**After** (docblock abridged here; it is in the file in full):
```ts
const { data: existing, error: existingErr } = await supabase
  .from("strategy_analytics")
  .select("computation_status, computed_at")
  .eq("strategy_id", strategyId)
  .maybeSingle();
if (existingErr) {
  console.error(
    "[wizard:SyncPreviewStep] resume freshness probe read failed — " +
      "treating this resume as a COLD START, not as an absent " +
      "analytics row:",
    existingErr.message,
  );
}
```

### A read FAILURE is distinguished from a legitimate ABSENCE — and that distinction is what is pinned

The context warned about exactly this, and it is the load-bearing decision: `.maybeSingle()` answers
a genuinely missing row with `{ data: null, error: null }`. That absence is **normal** — it is the
first-time draft this probe exists to detect — and `140.4-01` had to carve `PGRST116` out of its own
predicate for the same reason. Here no carve-out is needed, because `maybeSingle` (unlike `.single()`)
does not report no-rows as an error at all.

**Three cases, all three polarities:**

| case | assertion | pre-fix |
|---|---|---|
| `a FAILED freshness probe is REPORTED — not silently read as 'this draft has no analytics row'` | one log naming the read, **and** the kickoff POST still fires exactly once | **RED** |
| `an ABSENT analytics row stays SILENT — an absence is not a failure` | zero logs; kickoff fires | green |
| `a COMPLETE + FRESH row still takes the WIZ-05 skip — the probe's success path is untouched` | zero kickoff POSTs, zero logs | green |

Without the second case a fix that logged unconditionally would pass; without the third, a fix that
ignored `data` entirely would pass.

**Observed RED (against the untouched tree, commit `90c24062`):**
```
 FAIL  src/app/(dashboard)/…/SyncPreviewStep.render.test.tsx
   > [140.4-12 / SEAMRIM-11] SyncPreviewStep — the resume freshness probe binds error
   > a FAILED freshness probe is REPORTED — not silently read as 'this draft has no analytics row'

AssertionError: The freshness probe read FAILED and nothing recorded it. supabase-js resolves a
failed read AS A VALUE, so an unbound `error` makes a statement timeout indistinguishable from a
draft that simply has no analytics row yet — the C-3 shape, at the last unchecked read in this
file.: expected +0 to be 1

- Expected
+ Received
- 1
+ 0

 Test Files  1 failed (1)
      Tests  1 failed | 52 passed (53)
```

### Why this LOGS and falls through instead of throwing — stated, not assumed

The marker read a few lines below (`data_quality_flags`) fails **CLOSED**, because it decides
COMPOSITE vs SINGLE-KEY routing. This one decides only whether to **skip a round-trip**. On a failed
read `data` is null, so the derivations below already resolve to *not complete, not fresh* and the
effect falls through to the kickoff POST — **exactly what a cold start does**. The fail-open direction
(`isComplete === true` on unread data) is unreachable by construction. Throwing would end the wizard
on a transient blip in an optimisation: the over-refusal `140.4-01` carved PGRST116 out to avoid.

⇒ **The defect closed here is SILENCE, not a wrong branch**, and the SUMMARY says so rather than
overclaiming.

### The receipt — measured with 140.4-14's actual rule, in both directions

Run with a **temporary** probe flat-config (`nextTs` + the plugin, the rule at `error` over
`src/**/*.{ts,tsx}`), deleted immediately after; `eslint.config.mjs` is **not** in the diff.

| file | before | after |
|---|---|---|
| `src/app/(dashboard)/…/steps/SyncPreviewStep.tsx` | **1 error** at `524:15`, `quantalyze/no-unchecked-supabase-read` | **0 — exit 0** |
| `src/lib/queries.ts` (vacuity fence on my own receipt) | 1 | **1** |

The `queries.ts` control matters: a probe config that silently failed to load the rule would also
report 0 on `SyncPreviewStep.tsx`. It reports 1 there, so the 0 is a real clean.

> **`SyncPreviewStep.tsx` is now CLEAN under `140.4-14`'s rule.** A follow-up can add
> `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx` to the `files:` glob at
> `eslint.config.mjs:183` and give the ratchet real teeth on a second file. ⚠️ Read `140.4-14`'s own
> caveat with it: `140.4-02` converted the seven-member `Promise.all` using the **whole-result**
> binding form (`analyticsRes.error`, …), which is the rule's declared non-goal 1 and is invisible to
> it. So on this file the rule is a **ratchet against re-introducing the destructure form**, not a
> check on the code as it stands. The site closed here was the only rule-visible one.

**The line number moved and is reported rather than transcribed:** `140.4-14` measured it at `:521`;
on my tree the same read sits at `:524`, the shift coming from `140.4-11`/`140.4-02` landings plus
this plan's two import lines. Anchored by text, never by line, everywhere in the code.

---

## Falsifiability Ledger — row M104

**Criterion:** *a translated wire code renders without a second roster edit* (SEAMRIM-08).
**Assigned shape:** second surface, **ordering** not presence — `recogniseSeamErrorCode` still in the
file.

### ⚠️ The PRESCRIBED mutation is a NO-OP, and that was PROVEN before substituting

The plan prescribes: *"mutate the SECOND member — `SyncPreviewStep`'s kickoff arm — by restoring the
old precedence there: membership-check first, translation second."* Applied exactly:

```diff
           const surfaced: WizardErrorCode =
-            translated !== "UNKNOWN"
-              ? translated
-              : (wireCode !== null && KNOWN_KICKOFF_CODES[wireCode]) ||
-                "SYNC_FAILED";
+            (wireCode !== null && KNOWN_KICKOFF_CODES[wireCode]) ||
+            (translated !== "UNKNOWN" ? translated : "SYNC_FAILED");
```

**Result: `npx tsc --noEmit` exit 0 and `npx vitest run "src/app/(dashboard)/strategies/new/wizard/"`
→ 22 files / 333 tests, ALL GREEN — including all five cases this plan wrote to catch exactly this
defect.** It is not a weak mutation; it is **not a mutation at all**:

> `KNOWN_KICKOFF_CODES` keys = {`RATE_LIMITED`, `GATE_DRAFT_GONE`, `COMPOSITE_MEMBERSHIP_UNKNOWN`,
> `MISSING_STRATEGY_ID`, `INVALID_STRATEGY_ID`}
> `SEAM_CODE_TO_WIZARD_CODE` keys = {`VALIDATION_FAILED`, `RATE_LIMITED`, `CIRCUIT_OPEN`,
> `UPSTREAM_TIMEOUT`, `UPSTREAM_NETWORK_ERROR`, `SEAM_MISCONFIGURED`}
> **Intersection = {`RATE_LIMITED`}**, and both tables answer `RATE_LIMITED` for it.
> Two lookups that never disagree are **order-independent**: swapping them changes no output.

⇒ At this arm the live property is the **PRESENCE** of the translation hop, not its order. Reported,
not adjusted — and reported *with the run that demonstrates it*, because "this mutation would not
have reddened" is itself a falsifiable claim.

### The substitute — same shape, every designed property preserved

**Present-but-unreachable**, in the exact form of the defect this plan closes at `SubmitStep`: the
translation still runs, and its result must then re-qualify through the local roster.

- semantic ✅ · **production** source ✅ · not a syntax error ✅ · **not a type error** (`tsc --noEmit`
  exit **0** under it) ✅ · no test file touched ✅
- **second surface** ✅ — `SubmitStep` is member one (the surface C-5/the finding named);
  `SyncPreviewStep`'s kickoff arm is member two, and it is the one mutated
- **invisible to a grep receipt** ✅ — `grep -c "recogniseSeamErrorCode" SyncPreviewStep.tsx` → **2**
  under the mutation (unchanged), and `grep -c 'from "@/lib/seam-discriminator"'` → **1**
  (unchanged). A receipt built on either would have reported the guard satisfied. **That is the point
  of the row.**

```diff
--- a/src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
+++ b/src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
@@ -669,10 +669,9 @@
           const wireCode = seamErrorCode(failure);
           const translated = recogniseSeamErrorCode(wireCode);
           const surfaced: WizardErrorCode =
-            translated !== "UNKNOWN"
-              ? translated
-              : (wireCode !== null && KNOWN_KICKOFF_CODES[wireCode]) ||
-                "SYNC_FAILED";
+            (wireCode !== null && KNOWN_KICKOFF_CODES[wireCode]) ||
+            (translated !== "UNKNOWN" && KNOWN_KICKOFF_CODES[translated]) ||
+            "SYNC_FAILED";
```

**Command:**
```
npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep" \
               "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep" --no-file-parallelism
```

**OBSERVED RED (pasted):**
```
 ❯ src/app/(dashboard)/…/SyncPreviewStep.render.test.tsx (53 tests | 6 failed)
     × [140.4-12] a breaker trip (CIRCUIT_OPEN) renders the breaker's own state, not SYNC_FAILED
     × [140.4-12] a seam transport failure (UPSTREAM_TIMEOUT) renders SERVICE_UNREACHABLE, not SYNC_FAILED
     × [140.4-12] a seam transport failure (UPSTREAM_NETWORK_ERROR) renders SERVICE_UNREACHABLE, not SYNC_FAILED
     × [140.4-12] our own configuration fault (SEAM_MISCONFIGURED) reaches its own state
     × [140.4-12] the NESTED python envelope's code is read at this arm too
     × PROPERTY — no reachable terminal code renders the destructive control as its SOLE affordance

 FAIL … > [140.4-12] a breaker trip (CIRCUIT_OPEN) renders the breaker's own state, not SYNC_FAILED
Error: CIRCUIT_OPEN is a WIRE code with no entry in KNOWN_KICKOFF_CODES, so it fell to SYNC_FAILED —
a sentence that claims we fetched trades and the computation did not complete. The breaker declined
to send the request; nothing was fetched and nothing ran.:
expect(element).toHaveAttribute("data-error-code", "SERVICE_UNAVAILABLE_RETRY")

Expected the element to have attribute:
  data-error-code="SERVICE_UNAVAILABLE_RETRY"
Received:
  data-error-code="SYNC_FAILED"
 ❯ src/app/(dashboard)/…/SyncPreviewStep.render.test.tsx:1720:7

 FAIL … > PROPERTY — no reachable terminal code renders the destructive control as its SOLE affordance
AssertionError: Fewer than fourteen distinct codes were actually driven. A loop that renders nothing
satisfies every assertion inside it and reports agreement forever — which is worse than having no
guard at all.: expected 11 to be greater than or equal to 14
 ❯ src/app/(dashboard)/…/SyncPreviewStep.render.test.tsx:2384:7

      Tests  6 failed | 47 passed (53)
```

**The vacuity fence fired as a second, independent detector** — under the mutation the enlarged
population collapses back to 11, and the raised literal notices. Had the fence been left at 11 (the
easier choice), that signal would not exist.

### GREEN-side observations under the mutation — named, as the row requires

1. **`SubmitStep`'s whole file passed: 33/33.** Named specifically:
   `✓ [140.4-12] a RATE_LIMITED wire code renders the HONEST rate-limit copy — the cap is OURS`,
   `✓ [140.4-12] a VALIDATION_FAILED wire code renders its own member, not UNKNOWN`,
   `✓ [140.4-12] the NESTED python envelope's code is read …`. ⇒ **the two surfaces are pinned
   independently, not by one shared assertion.**
2. **All five pre-existing `KNOWN_KICKOFF_CODES` members passed** — the `it.each` rows for
   `RATE_LIMITED`, `GATE_DRAFT_GONE`, `COMPOSITE_MEMBERSHIP_UNKNOWN`, `MISSING_STRATEGY_ID`,
   `INVALID_STRATEGY_ID`, individually named in the verbose output. The mutation is aimed at the wire
   vocabulary and leaves the roster's own domain intact, which is what makes the RED attributable.
3. `✓ [140.4-12] NEGATIVE CONTROL — a real seam wire code with no wizard member still falls back to
   SYNC_FAILED` — passed.
4. All three `[140.4-12 / SEAMRIM-11]` freshness-probe cases — passed (orthogonal, as intended).
5. **Coupling check:** 6 of 53 cases in the render file reddened; 47 passed, and the sibling
   `SubmitStep` file was untouched at 33/33. The pin is not coupled.

**Revert receipt:** `git checkout -- "src/app/(dashboard)/…/SyncPreviewStep.tsx"`, then
`git status --porcelain` → **empty**. No residual mutation.

> Branch protection on `main` is **OFF** (settled). The correct phrasing is that the workflow **would
> have caught** this at CI — never that it *did stop* anything at merge.

---

## What this does NOT close

**1. A TOTAL wire→render mapping is IMPOSSIBLE, and no plan should promise one.**
`analytics-service/services/ingestion/csv_adapter.py:172` emits

```python
error_code=first_rule.upper() if first_rule else "CSV_VALIDATION_FAILED",
```

where `first_rule = str(errors[0].get("rule"))` — a **pandera check name**, uppercased. Traced to
source, a rule name may contain parentheses and commas, so the value is not even a valid identifier
after `.upper()`. **That is an OPEN STRING SET with no registry.** No closed union and no
exhaustiveness check can ever be total over the Python side.

**2. The census — published WITH my predicates, and it DISAGREES with the plan's figures.**
Per CONTEXT §5c (*"do not pick a number, state a predicate"*), these were re-measured rather than
transcribed, and the disagreement is reported rather than reconciled by adjusting:

| quantity | plan's figure | my predicate | my measured value |
|---|---|---|---|
| TS wire codes | 53 (27 lowercase) | distinct string literals matching `(error_)?code:\s*"X"` over `src/app/api/**/*.{ts,tsx}` | **72 distinct (31 lowercase)** |
| Python literal codes | 37 | distinct literals matching `(error_)?code="X"` ∪ `"(error_)?code":\s*"X"` over `analytics-service/**/*.py` | **30 distinct** (74 SITES: 51 + 23) |
| translations | 6 | members of `SEAM_CODE_TO_WIZARD_CODE` | **6 — agrees** |

The plan published 53/37 **without their predicates**, which is hazard #9 in its own right, so
neither figure is reproducible and neither is repeated here as fact. **The ratio's message survives
either measurement:** a two-order-of-magnitude gap against **6** translations. **This plan closed the
AUTHORITY defect — a translated code is no longer overruled — not the coverage gap.**

**3. `draft_state_invalid` — recorded, not fixed.** `src/app/api/strategies/finalize-wizard/route.ts:1190`
(verified at source) mints it on a 409 over SQLSTATE 22023. It is **lowercase**, therefore
structurally incapable of matching either SCREAMING_SNAKE table, and it renders `UNKNOWN` today. It is
already recorded in the TS-35 ledger row and pinned by `SubmitStep.test.tsx`'s
*maps a 409 unknown code (draft_state_invalid) to UNKNOWN (recoverable)*. Untouched here, deliberately.
The 31 lowercase TS codes I measured are a **two-vocabulary CONVENTION, not 31 defects** — every one
is an operator/diagnostic code on an admin or account route (`would_orphan_last_admin`,
`rpc_shape_drift`, `ratelimit_misconfigured`, `idempotency_schema_drift`, …). `draft_state_invalid` is
the sole offender that crosses into the wizard surface. **Recorded; 31 codes were not normalised.**

**4. `WireErrorCode` (WP-11) stays DEFERRED, with the reason and the argument for whoever takes it.**
A union scoped to the wizard/seam surface is bounded and cheap; a repo-wide union spans **two
deliberate conventions** plus an unbounded Python set, and would either force the operator codes into
a vocabulary they do not belong to or need a second union plus a rule about which is which. That is a
design decision with a real chance of being wrong, and the coverage law does not reward rushing a
shared artefact. **The strongest argument for whichever phase picks it up:** a union is the **only**
mechanism that makes `draft_state_invalid` a **COMPILE** error, because that code is **TS-minted by
us** — every other member of the gap is a runtime string from Python and no type can reach it.

**5. Two residuals inside my own fence, named so they are not mistaken for closed:**
- `SubmitStep`'s **2xx** `data.code === "WIZARD_DUPLICATE"` check still reads the top level. That body
  is minted by our own route and is flat by construction, and the plan's interface says *"the response
  handler. Nothing else"*, so it was left alone. If `finalize-wizard` ever forwards a nested 2xx
  envelope, that read is the one that misses it.
- The **CSV** sibling `CsvSubmitStep.tsx:39` types `code: string` with **no membership check at all**
  (CONTEXT §5). It is outside this plan's `files_modified` and is untouched.

---

## Copy for founder review (DESIGN.md §Voice, Manual-Only §7.1)

**This plan authored NO new user-facing sentence.** It minted no code and no copy; `wizardErrors.ts`
is absent from the diff. What changed is **which sentences are REACHABLE**. Quoted verbatim because
§7.1 covers every sentence the phase puts in front of a user, and several of these have never been
rendered by this product before:

**Newly reachable at `SubmitStep` (finalize):**

| code | sentence, verbatim |
|---|---|
| `RATE_LIMITED` | *"You have reached our request limit."* / *"We cap how often this action can run and this attempt went over the cap. Nothing was submitted and nothing was changed — the cap is ours, not your exchange's."* / *"Wait, then run the same action again. We do not queue it or retry it for you."* |
| `VALIDATION_FAILED` | *"We could not read that request."* / *"We sent a request that failed its shape check before any work started. Nothing was submitted and nothing was changed. The fault is in our software, not in your key or your data."* / *"Contact security@quantalyze.com with your draft ID — a request-shape fault is on our side and retrying the same action will not clear it."* |

**Newly reachable at `SyncPreviewStep` (kickoff):**

| code | sentence, verbatim |
|---|---|
| `SERVICE_UNAVAILABLE_RETRY` | *"Our service is temporarily unavailable."* / *"We paused outbound requests after repeated failures so the service can recover, so this request was never sent. Nothing was submitted — this is on our side, not your key."* |
| `SERVICE_UNREACHABLE` | *"We could not reach our own service."* / *"We sent the request and never got an answer — the connection failed or ran out of time. Because no answer came back, we cannot tell whether it was processed. This is on our side, not your key or your exchange."* |
| `SEAM_MISCONFIGURED` | *"We could not send this request — our own configuration is wrong."* / *"A setting on our side is wrong, so we stopped before sending the request. Nothing was submitted and nothing was changed. Retrying will not clear it: the setting stays wrong until we fix it and redeploy. This is not your key, your exchange or your data."* |

**Sentences that STOP rendering on these paths** (they were the fallback for the codes above):

| was | now |
|---|---|
| `UNKNOWN` — *"Something went wrong."* / *"We could not classify this failure, so we cannot tell you what happened or whether your last action took effect."* | rendered only for genuinely unrecognised codes |
| `SYNC_FAILED`'s copy at the kickoff arm | rendered only for uncoded / unrecognised kickoff failures |

**One founder judgement is requested and is not self-approved:** `SEAM_MISCONFIGURED` and
`VALIDATION_FAILED` are **non-recoverable**, so on the paths that now reach them the wizard renders
**no Retry control** where it previously offered one (via `SYNC_FAILED` / `UNKNOWN`). That is the
honest render — retrying a misconfiguration or a malformed request cannot succeed — but it is a
capability removed from those paths, and the founder owns that call. The non-destructive exit
(*"Back to strategies"*, `140.4-11`) renders on every one of them, so none is a dead end.

**One NON-user-facing sentence was authored** (console only, never rendered, no PostgREST text
reaches the DOM):
> `[wizard:SyncPreviewStep] resume freshness probe read failed — treating this resume as a COLD START, not as an absent analytics row:`

---

## Deviations from Plan

### 1. [Reported, not adjusted] The prescribed M104 mutation is a NO-OP — substituted, with proof

Fully documented under **Falsifiability Ledger — row M104**. The plan's ordering swap was applied
verbatim, typechecked and run (**22 files / 333 tests, all green**) before any substitute was
considered, so the claim *"this mutation cannot redden"* is **observed, not argued**. The substitute
is present-but-unreachable at the same (second) surface and preserves every property the row was
designed to prove, including the grep-invisibility that is its point.

### 2. [Reported, not adjusted] The census figures 53/37 did not reproduce

Under my published predicates I measure **72** distinct TS code literals (31 lowercase) and **30**
distinct Python ones. The plan's 53/37 carry no predicate, so they cannot be re-derived. Per the
standing constraint, this is reported rather than adjusted; the plan's ratio argument is unaffected
and is repeated with my numbers.

### 3. [Rule 2 — Missing critical] The vacuity fence was raised, not left at its old value

`140.4-11`'s hand-typed fence (`>= 11`) would still have passed with the enlarged population, so
three added rows could have silently stopped driving anything and nothing would have noticed. Raised
to `>= 14` and proven live at 15. Strictly stronger; no assertion removed.

### 4. [Scope] The explicit addition (`:521`) is outside plan 12's stated tasks

Directed by the spawning prompt, executed in its own RED-first commit pair (`90c24062` / `cf5e8f88`),
inside plan 12's declared `files_modified` fence. `eslint.config.mjs` was **not** edited — widening
the glob belongs to a follow-up, and the receipt above is what unblocks it.

---

## Verification

| gate | result |
|---|---|
| `npx vitest run` (FULL, Node 25) | **718 passed \| 19 skipped (737 files) · 9920 passed \| 287 skipped (10207 tests)** — 0 failures |
| branch baseline at `fdff9839`, same command | **718 \| 19 (737) · 9902 \| 287 (10189)** |
| delta | **+18 tests, +0 files** — exactly this plan's additions (4 SubmitStep + 11 kickoff + 3 probe) |
| `npx tsc --noEmit` | **exit 0** (run before every commit) |
| `npm run lint` | **0 errors, 1 warning** — `EquityChart.tsx:1119` `react-hooks/exhaustive-deps`, pre-existing, matches the documented baseline exactly |
| `npx vitest run "src/app/(dashboard)/strategies/new/wizard/"` | **22 files / 333 tests passed** (baseline at `fdff9839`: 22 / 315) |
| CI-parity `PATH=/opt/homebrew/opt/node@22/bin` (FULL) | **9917 passed \| 3 failed \| 287 skipped** — all three out of fence, all three `Test timed out in 5000ms`, **all three pass in isolation** (see below) |
| CI-parity `PATH=/opt/homebrew/opt/node@22/bin`, **my fence** | **22 files / 333 tests passed** |
| packages installed | **0** (`package.json` / `package-lock.json` diff EMPTY) |
| Python / `supabase/migrations/` / `.planning/` source paths staged | **none** |
| `src/lib/wizardErrors.ts` in the diff (TRAP-8) | **no** |
| CONTEXT §8 baseline (9792 \| 287, 715 files) | exceeded — the delta is waves 1–3 already merged at my base, plus my 18 |

### The Node 22 full-suite result, stated honestly rather than rounded to "green"

**I am NOT claiming a clean Node 22 full suite.** Three files failed:

| file | failure | in my fence? |
|---|---|---|
| `src/__tests__/contracts/contracts-registry.test.ts` | `Error: Test timed out in 5000ms.` | no |
| `src/__tests__/gdpr-export.test.ts` | `Error: Test timed out in 5000ms.` | no |
| `src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx` | `TypeError: Failed to parse URL from /api/allocator/scenario/saved` — the known pre-existing isolation dependency `140.4-14` recorded | no |

**Not one is an assertion failure**, and per the standing constraint they were **re-run in isolation
before being called anything**:

```
npx vitest run src/__tests__/contracts/contracts-registry.test.ts \
               src/__tests__/gdpr-export.test.ts \
               "src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx" \
               --no-file-parallelism        # Node 22

 Test Files  3 passed (3)
      Tests  107 passed | 1 skipped (108)
```

All three pass. The Node 22 full run took **411 s** against Node 25's **274 s** on the identical
tree — the contention signature two sibling executor agents sharing this machine and `node_modules`
produce, and the same three files `140.4-11` and `140.4-14` both recorded. **Structural fence check:**
`git diff --name-only fdff9839..HEAD` is four wizard-step files; none of the three failing files
imports anything in that diff, and the same three-file set failed for the two previous plans that
touched entirely different code.

---

## Threat register outcome

| Threat ID | Disposition | Status |
|---|---|---|
| T-140.4-12-01 our own rate limit rendered as the user's exchange throttling them | mitigate | **Closed at `SubmitStep`.** `RATE_LIMITED`'s honest copy is reachable; the exchange-blaming `KEY_RATE_LIMIT` sentence is asserted ABSENT in the same case. Falsified by M104. |
| T-140.4-12-02 a fix shipping with zero user-visible effect | mitigate | **Closed.** Both codes reachable with all three rosters unchanged in size AND membership (P1–P3). |
| T-140.4-12-03 admitting an arbitrary wire string as a `WizardErrorCode` | avoid | **Held.** No identity rule; `SEAM_DEGRADED` negative controls at BOTH surfaces, green throughout. |
| T-140.4-12-04 21 nested-envelope codes silently unread | mitigate | **Closed at both surfaces** via `seamErrorCode`, added to the existing import at each; one nested-shape case per surface, both observed RED first. |
| T-140.4-12-05 a newly reachable code arriving with a destructive sole affordance | avoid | **Held, and verified rather than assumed.** 140.4-11's derived pin re-run at 14/14 with the fence raised; none of the three new codes has `try_another_key`. |
| T-140.4-12-06 claiming the vocabulary gap closed | avoid | **Held.** `## What this does NOT close` records the open Python set, my re-measured census (with predicates, and with the plan's figures marked non-reproducible), `draft_state_invalid`, and the deferred union. |
| T-140.4-12-SC npm/pip/cargo installs | mitigate | **Zero packages installed**, verified by an empty lockfile diff. |

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change. The `:521` change
adds one `console.error` carrying a PostgREST **message** only — no row payload, no table name beyond
the fixed prefix — and nothing new reaches the DOM.

## Known Stubs

None.

---

## OPEN — not closed by this plan, and not claimed to be

1. **The wire↔render COVERAGE gap** (my measured 72 TS + 30 Python distinct codes vs 6 translations)
   — this plan closed the **authority** defect only.
2. **The open Python string set** (`csv_adapter.py:172`) — structurally unclosable by any union.
3. **`draft_state_invalid`** — recorded, not fixed; a `WireErrorCode` union is the only mechanism that
   would make it a compile error.
4. **`CsvSubmitStep.tsx:39`** — `code: string`, no membership check, outside this fence.
5. **`SubmitStep`'s 2xx `data.code` read** — flat by construction today; named above.
6. **The `no-unchecked-supabase-read` glob is still ONE file.** `SyncPreviewStep.tsx` is now clean and
   admissible; adding it is a follow-up's edit to `eslint.config.mjs`, not mine.
7. **C-4 and C-5 remain founder-owed** (`140.4-11`'s OPEN section; VALIDATION.md Manual-Only §7.2 /
   §7.3). Nothing here discharges either.

## Success Criteria

- [x] `RATE_LIMITED` and `VALIDATION_FAILED` render their own copy at `SubmitStep` with **no list edited**.
- [x] The nested Python envelope's code is read at **both** surfaces.
- [x] The kickoff arm translates before it membership-checks; the four seam wire codes render their own
      copy; all five pre-existing kickoff members are unchanged (pinned individually AND as a set).
- [x] Both surfaces keep a negative control (`SEAM_DEGRADED`) proving an unrecognised code still
      resolves to the fallback.
- [x] `140.4-11`'s destructive-control pin is green across the enlarged reachable set (11 → 14, fence
      raised and proven live).
- [x] **M104 reddens at the second surface on a mutation a grep receipt would have missed** — with the
      prescribed no-op mutation *demonstrated* to be a no-op first, and the substitute recorded.
- [x] ⭐ `SyncPreviewStep.tsx:521` closed: 1 rule site → **0**, RED-first, absence-vs-failure
      distinguished, own commit.

## Self-Check

```
FOUND: src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
FOUND: src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
FOUND: src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
FOUND: src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx
FOUND: e88ff360  test(140.4-12): RED — a translated wire code is overruled by a second roster
FOUND: 8e328b0b  feat(140.4-12): GREEN — the ONE translation table is authoritative
FOUND: 3e824ea7  test(140.4-12): RED — the kickoff arm has no translation hop at all
FOUND: 2965877d  feat(140.4-12): GREEN — the kickoff arm translates before it membership-checks
FOUND: 93ed5e5c  test(140.4-12): re-run 140.4-11's destructive-control pin over the enlarged set
FOUND: 90c24062  test(140.4-12): RED — the resume freshness probe swallows a read failure
FOUND: cf5e8f88  fix(140.4-12): GREEN — the resume freshness probe binds error
CLEAN:  git status --porcelain empty after the M104 revert (the temporary eslint probe config and
        the three temporary Node-22 runner scripts are deleted; none was ever staged)
RE-MEASURED at the final tree: KNOWN_FINALIZE_CODES 9 (membership diff empty) ·
        SEAM_CODE_TO_WIZARD_CODE 6 (file absent from the diff) · KNOWN_KICKOFF_CODES 5 ·
        seam-discriminator imports 1 per file · removed `it(` lines 0 ·
        no-unchecked-supabase-read sites on SyncPreviewStep.tsx 0 (queries.ts control still 1)
```

## Self-Check: PASSED
