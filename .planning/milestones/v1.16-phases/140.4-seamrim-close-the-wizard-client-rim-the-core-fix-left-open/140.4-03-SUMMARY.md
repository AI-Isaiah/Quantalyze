---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 03
status: COMPLETE
subsystem: wizard-csv-finalize / db-constraints
tags: [idempotency, postgres-constraint, csv-wizard, error-copy, seamrim-03]
requirements: [SEAMRIM-03]
tasks_completed: "3 of 3"
commits:
  - 75eefed3  # Task 1 — migration + both SQL receipts + regenerated snapshot
  - a0ea050c  # Task 2 RED
  - fca3f5d6  # Task 2 GREEN
  - cc5e60c0  # Task 3 — client gate + copy reconciliation
ledger_rows_recorded: [M93, M94]
metrics:
  vitest: "9828 passed | 287 skipped (baseline 9792/287)"
  pytest: "4801 passed / 96 skipped (baseline 4778/96)"
  mypy: clean
  tsc: "exit 0"
  lint: "0 errors, 1 pre-existing warning"
---

# Phase 140.4 Plan 03: CSV double-submit idempotency — COMPLETE

A CSV double-submit can no longer create a second strategy: a three-column partial unique index
`(user_id, wizard_session_id, source)` is the fence, `finalize_csv_strategy` finally writes the column
that puts CSV rows inside it, and the retry the product's own copy instructs answers 200 with the
existing strategy id instead of leaking raw Postgres text into a 422 dead end.

**Applied and verified on TEST (`qmnijlgmdhviwzwfyzlc`) by the coordinator before Task 1 was committed.**

---

## ⛔ THE HARD GATE — halted, then CLEARED

The plan's hard gate: the migration must be applied to the TEST project
`qmnijlgmdhviwzwfyzlc` **via the Supabase MCP** before it may be committed, because merging
`supabase/migrations/**` to `main` auto-applies to PRODUCTION with no approval step.

**This plan halted at that gate and reported.** The coordinator then authenticated the MCP, applied the
migration to TEST and verified it, and cleared the gate. What the coordinator's apply established, which
my local execution could not:

- **The defect reproduced live** on a real database pre-apply: the two-column index still in place,
  `finalize_csv_strategy` not writing `wizard_session_id`, `dup_groups = 0` (STEP 0 pre-flight passes),
  `schema_migrations` head at `20260726000225`.
- **STEP 4's self-verify passed on the real schema**, so the column list and order were asserted out of
  `pg_index` — the `IF NOT EXISTS` no-op trap could not have slipped through.
- **Post-state:** new index `(user_id, wizard_session_id, source) WHERE wizard_session_id IS NOT NULL`;
  old index gone; function writes `p_wizard_session_id`; exactly 1 unique index over `wizard_session_id`;
  `anon` has no EXECUTE, `authenticated` does.
- **Behavioural probe** (self-rolling-back `DO` block): A first-submit INSERTED · B double-submit RAISED
  23505 · C cross-source INSERTED. **C is the case my migration's long comment argues for at length, now
  proven rather than reasoned.** 0 leftover probe rows.
- **Drift corrected:** MCP `apply_migration` stamps `now()` and recorded `20260728203240`; the
  coordinator reset `supabase_migrations.schema_migrations` to the file's `20260728120000`, which now
  sits directly after `20260726000225`. **TEST already has this migration — do not re-apply.**

⚠️ **The halt is recorded rather than erased, because the reasoning is the deliverable.** Every route
below was probed, not assumed:

| Route | Result |
|---|---|
| Supabase MCP (`mcp__supabase__*`) | **Unavailable.** No MCP tools in this agent's toolset, and `claude mcp list` reports `plugin:supabase:supabase … ! Needs authentication` |
| Local `psql` → TEST | **No credentials.** `TEST_SUPABASE_DB_URL` exists only as a GitHub Actions secret (write-only; confirmed via `gh secret list`). Absent from every `.env*` file |
| Supabase CLI (`supabase db push`) | **Refused deliberately.** `supabase/.temp/project-ref` is `khslejtfbuezsmvmtsdn` — **PRODUCTION**. Using the CLI would have targeted prod. Link state verified untouched afterwards |
| Local Supabase stack (`supabase start`) | **Docker Desktop crashed** mid image-pull: `write …/meta.db: input/output error` → `Docker Desktop is unable to start`. Host volume has 5.9 GiB free. Not repaired — that is the founder's machine state and three executors share it |

While halted: **the migration was not committed, and Tasks 2 and 3 were not started.**

**Task 2/3 were additionally coupled and could not ship ahead of Task 1** — independent of the gate.
Task 3(b) rewrites `CSV_SUBMIT_NO_STRATEGY_ID` to claim *"a repeat submit of the same wizard session
cannot create a second strategy."* That sentence was **false until the migration landed**. Shipping it
first would have been the antipattern the existing comment at `wizardErrors.ts:881-883` names: *"a
promise made ahead of its mechanism is how this whole class of copy started."* The mechanism is now on
TEST, so the sentence is honest and Task 3(b) shipped.

---

## ✅ TASK 1 — and it executed locally too, before the TEST apply

Rather than ship an unexecuted migration, I stood up a **throwaway PostgreSQL 16.13 cluster**
(`initdb` in `/tmp`, port 55432, unix socket only, destroyed afterwards) with a hand-built fixture of
the pre-migration schema, and ran the migration and both SQL test files against it for real.

**This is NOT a substitute for the TEST apply and must not be recorded as one.** Its limits:

- **PG 16.13 local vs PG 17.6.1 on TEST/PROD.**
- The fixture is a **stub** of ~6 objects, not the real 230-migration schema. It therefore says
  nothing about interactions with real RLS policies, triggers, existing rows, or the real
  `create_wizard_strategy` (stubbed — see the vacuity note under Part 4 below).
- It does confirm: the DDL is valid, the plpgsql body compiles, the STEP 0/STEP 4 `DO` blocks
  execute and assert correctly, and the receipt's four behavioural parts do what they claim
  **against a real index**.

### Migration applied cleanly

```
BEGIN / SET / DO / CREATE INDEX / COMMENT / DROP INDEX / CREATE FUNCTION / REVOKE / GRANT / COMMENT
NOTICE:  SEAMRIM-03: strategies double-submit fence is now (user_id, wizard_session_id, source);
         two-column index dropped; finalize_csv_strategy writes wizard_session_id.
DO / COMMIT
MIGRATION_EXIT=0
```

### The receipt is RED before and GREEN after

**RED, on the untouched pre-migration state** (fixture loaded with the two-column index and
`20260716130500`'s function body verbatim):

```
psql:supabase/tests/test_csv_finalize_double_submit.sql:270: ERROR:  TEST FAILED (Part 1c):
  the finalized strategy carries wizard_session_id=<NULL> , expected 2127b705-…-78b3c450cd5c -
  finalize_csv_strategy is not writing the column, so every CSV row sits OUTSIDE the partial
  unique index (review finding C-2)
RECEIPT_EXIT=3
```

**GREEN, after the migration:**

```
NOTICE:  Part 1 OK: first CSV finalize created strategy 1d472bd1-… with source=csv and
         wizard_session_id=8f3a2cd5-….
NOTICE:  Part 2 OK: the second CSV finalize for the same (user, session) raised 23505.
NOTICE:  Part 3 OK: exactly 1 strategies row and 1 strategy_verifications row survive -
         the failed submit rolled both back.
NOTICE:  Part 4 OK: a CSV finalize succeeded against a session id already used by an abandoned
         source=wizard draft - the `source` column in the index is live.
NOTICE:  test_csv_finalize_double_submit: ALL PASS
RECEIPT_EXIT=0
```

`test_wizard_session_idempotency.sql` reached **section 3** and stopped there on
`create_wizard_strategy is missing the per-(user, session) advisory lock` — my **stub** writer has no
advisory lock. plpgsql executes sequentially, so sections 1 and 2 (the column, and my rewritten
three-column index assertions) **passed**. Section 3 is a fixture limitation, not a defect; it will
pass against the real schema.

---

## The index, verbatim, with column order

```sql
CREATE UNIQUE INDEX IF NOT EXISTS strategies_user_wizard_session_source_uniq
  ON public.strategies (user_id, wizard_session_id, source)
  WHERE wizard_session_id IS NOT NULL;
```

- **`user_id` LEADS** — column 1 of 3. `wizard_session_id` is caller-supplied; a non-tenant-leading
  unique index over it is the C-08 cross-tenant leak that `20260726000225` exists to close.
- **Create-before-drop ordering, measured:** `CREATE UNIQUE INDEX` at **line 167**, `DROP INDEX` at
  **line 181**. Create precedes drop by 14 lines.
- **No NULL-distinctness hole:** `strategies.source` is `TEXT NOT NULL DEFAULT 'legacy'`
  (`20260411103316:36`), verified at source. A nullable third column would make every such row
  mutually distinct and the index inert.
- **The API guarantee is preserved exactly:** both API writers set `source='wizard'` — verified at
  `schema/functions/create_wizard_strategy.sql:88-97` and `add_wizard_composite_key.sql:70-80`. No
  migration in the tree ever `UPDATE`s `strategies.source`, so a row cannot migrate out of its own
  uniqueness bucket after insert.

### Self-verify assertions (STEP 4)

(a) column **list and order** from `pg_index` = `{user_id, wizard_session_id, source}`, index is
`indisvalid AND indisunique` · (a2) still partial on `wizard_session_id IS NOT NULL` ·
(b) `strategies_user_wizard_session_uniq` is gone · (b2) **exactly one** unique index on `strategies`
covers `wizard_session_id`, so a differently-named copy of the two-column shape cannot hide behind (b) ·
(c) `pg_get_functiondef` sliced **between `INSERT INTO strategies (` and `RETURNING id INTO
v_strategy_id`** contains both `wizard_session_id` and `p_wizard_session_id`. The slice is deliberate:
a whole-body `ILIKE '%wizard_session_id%'` is satisfied by the parameter declaration and by the
`strategy_verifications` INSERT, **both of which predate this migration** — i.e. the loose check passes
on the UNFIXED body and proves nothing.

### The cross-source control assertion, by name

**`Part 4` — "TEST FAILED (Part 4b/4c)"**, in `supabase/tests/test_csv_finalize_double_submit.sql`.
An abandoned `source='wizard'` draft carrying session `S` is created via the **real** API writer, then
a CSV finalize with the **same `S`** must succeed and mint its own strategy (2 rows for
`(uid_b, session_b)`, exactly 1 with `source='csv'`).

It carries its own **vacuity fence** (Part 4a): it first asserts the API draft really does carry
`source='wizard'` **and** the session id. Without that fence, an API writer that silently stopped
writing `wizard_session_id` would make Part 4 pass for the wrong reason and the control would report
agreement forever.

### Re-base evidence

`finalize_csv_strategy` is defined in **exactly two** migrations —
`20260501055202:185` (4-arg) and `20260716130500:225` (5-arg with `p_terminal_status`, preceded by a
`DROP FUNCTION` of the 4-arg signature). **Re-based on `20260716130500`.** Re-basing on the older body
would have silently deleted the CONTRIB-02 terminal-status guard. Exactly one change from that body:
`wizard_session_id` in the `strategies` INSERT column list, `p_wizard_session_id` in its `VALUES`.
Grants restated verbatim from `:327-328` (REVOKE from `PUBLIC, anon`; GRANT to `authenticated` — note
`20260716130500` grants **no** `service_role`, and I did not add one).

`supabase/schema/functions/finalize_csv_strategy.sql` was **regenerated** by
`npm run schema:functions` (hermetic — no DB/Docker/secrets, per the script's own docblock), not
hand-edited. `@generated` header intact; `-- source migration:` now names
`20260728120000_csv_finalize_double_submit_idempotency.sql`. `tsx scripts/dump-sql-functions.ts
--check` → *"SQL function snapshot is current (104 functions)."* Only that one snapshot file changed.

---

## Falsifiability Ledger — rows M93, M94

> ⚠️ **Row-assignment discrepancy, surfaced not resolved.** The execution prompt said *"You own ledger
> rows M92, M93, M94."* `140.4-VALIDATION.md`'s assignment table assigns **M92 to plan `02`** (*"A read
> failure is never rendered as a measurement"*, second member `earliest`) and only **M93/M94** to plan
> `03`; `140.4-03-PLAN.md` itself says *"this plan's rows are **M93** and **M94**."* I executed **M93
> only** and did **not** touch M92 — it is plan 02's row and plan 02 is running concurrently.

### M93 — the `source` column in the index is LIVE, and the receipt is not vacuous — **OBSERVED RED**

**Class:** *"a receipt that a two-column index still passes."* **Mutation:** drop `source` from the
`CREATE UNIQUE INDEX` column list — the exact "simplification" `140.4-RESEARCH.md`'s pitfall names
(*"Warning sign: an index definition with two columns"*). Semantic edit to production source (SQL is
production source here). No syntax error, no type error, no test edit. **Both mutations reverted.**

The row is recorded in **two parts**, because the first attempt revealed a second guard:

**M93-a — the migration refuses to apply.** Mutating only the index column list:

```
psql:supabase/migrations/20260728120000_csv_finalize_double_submit_idempotency.sql:425:
ERROR:  SEAMRIM-03: strategies_user_wizard_session_source_uniq covers {user_id,wizard_session_id},
        expected {user_id,wizard_session_id,source} in that order
MIGRATION_EXIT=3
```

The STEP 4 self-verify catches it, so the mutated state never lands. This confirms the column-list
assertion is live, but it **confounds** the observation M93 actually asks for — the receipt's
cross-source case never runs.

**M93-b — isolate the receipt.** Second semantic edit to the same production file: relax STEP 4's
expected array to `ARRAY['user_id', 'wizard_session_id']` so the two-column index genuinely lands.
Migration then applies (`MIGRATION_EXIT=0`), and the receipt:

```
NOTICE:  Part 1 OK: first CSV finalize created strategy d3984621-… with source=csv and
         wizard_session_id=3d1f8152-….
NOTICE:  Part 2 OK: the second CSV finalize for the same (user, session) raised 23505.
NOTICE:  Part 3 OK: exactly 1 strategies row and 1 strategy_verifications row survive -
         the failed submit rolled both back.
psql:supabase/tests/test_csv_finalize_double_submit.sql:270:
ERROR:  duplicate key value violates unique constraint "strategies_user_wizard_session_source_uniq"
RECEIPT_EXIT=3
```

- **Failing case:** Part 4, the cross-source control — the CSV finalize against a session id already
  held by an abandoned `source='wizard'` draft. That raw Postgres error **is** the permanent
  first-submit break: the user's first legitimate CSV submit fails, and because every retry reuses
  the same id, it fails forever.
- **NAMED GREEN-SIDE OBSERVATION (this is the load-bearing half of the row):** **Parts 1, 2 and 3
  stayed GREEN under the mutation** — including *"the second CSV finalize raised 23505."* That is the
  proof the two cases test **different properties**: the double-submit fence is satisfied by a
  two-column index, so **without Part 4 the receipt would have passed on the broken shape** and the
  closure would have been vacuous.
- Reverted; `grep` confirms line 168 is back to `(user_id, wizard_session_id, source)` and line 353
  to the three-element array. Re-applied the reverted migration and re-ran the receipt →
  `RECEIPT_EXIT=0`, `ALL PASS`. `git status --porcelain` shows no residual mutation.

⚠️ Branch protection on `main` is **OFF** — the SQL lane **would have caught** this, it did not stop it.

### M94 — the 23505 arm is LIVE at the SECOND member of the class — **OBSERVED RED**

**Class:** *"an RPC failure arm that does not paint raw Postgres text into a user-facing message."*
**FIRST member:** the pre-existing race-winner arm in this same file (`process_key.py`, the
`.eq("strategy_id").eq("wizard_session_id")` re-fetch) — already guarded, proven by mutation **M10**.
**SECOND member, and the one mutated:** the CSV finalize 23505 arm this plan added.

**Mutation shape: present-but-unreachable, by design.** A removed arm and a correct absence both
produce "no dedupe", so deleting the arm could not distinguish LIVE from INERT. Instead the detection
predicate was narrowed so it can never match — semantic edit to production source, no syntax error, no
type error, no test edit:

```python
- if "23505" in msg or "duplicate key" in msg.lower():
+ if "23506" in msg or "duplicate keys" in msg.lower():
```

(Both halves had to move: narrowing only the SQLSTATE would still have matched on `"duplicate key"`.)

**RED:**

```
FAILED tests/test_process_key.py::test_seamrim03_csv_finalize_23505_answers_200_with_existing_strategy
E   AssertionError: {"ok":false,"code":"CSV_FINALIZE_FAILED","human_message":"We could not complete
    the save. Check /strategies before submitting again — we cannot tell from here whether the
    strategy was created.","debug_context":{},"correlation_id":"1111...","recoverable":false}
FAILED tests/test_process_key.py::test_seamrim03_csv_finalize_23505_refetch_is_tenant_and_source_scoped
2 failed, 3 passed
```

**NAMED GREEN-SIDE OBSERVATIONS — three tests stayed green, and the third is the point of the row:**

1. `test_pyapi_09c_csv_finalize_does_not_hit_the_precheck_shortcircuit` — a **first** submit is
   unchanged, as the anti-regression case requires.
2. `test_seamrim03_csv_finalize_residual_error_message_is_static` — the residual arm still returns a
   static message, so the no-leak property is not what the 23505 arm buys.
3. ⭐ `test_seamrim03_csv_finalize_23505_refetch_miss_does_not_fabricate_success` — **it asserts 422 +
   `CSV_FINALIZE_FAILED` + no raw Postgres text, and the RESIDUAL arm satisfies all of that.** So this
   test passes whether the idempotent arm exists or not. That is exactly why the present-but-unreachable
   shape was required, and it identifies the 200-with-existing-id sibling as the **only** test in the
   file that can tell a live arm from an absent one.

The mutation run also confirmed the operator/user split works as designed: the structured log line
carried the full raw `duplicate key value violates unique constraint
"strategies_user_wizard_session_source_uniq"` while the response body carried only the static sentence.

Reverted. `git diff --stat -- analytics-service/routers/process_key.py` → **empty** against the
committed GREEN; targeted tests back to `5 passed`; `mypy --strict` clean.

⚠️ Branch protection on `main` is **OFF** — pytest **would have caught** this, it did not stop it.

---

## Tasks 2 and 3 — what shipped

### Task 2 — the 23505 arm (`fca3f5d6`, RED at `a0ea050c`)

- 23505 → **200 with the existing strategy id**, same envelope shape as a first submit
  (`ok` / `strategy_id` / `status` / `correlation_id` / `step: "finalize"`, no `code`).
- **Re-fetch scope, the three `.eq()`s named as the criterion requires: `user_id`,
  `wizard_session_id`, `source="csv"`.** `wizard_session_id` alone would re-fetch, and echo, another
  strategy's row (the C-08 lesson, stated at the race-winner analog) — and here it is not hypothetical:
  an abandoned `source='wizard'` draft can hold the very same session id, so a two-`.eq()` re-fetch
  would hand the user a *different* strategy's id as if it were their CSV upload.
- It runs on the **user-scoped** client, so RLS fences the read a second time under the explicit
  filters rather than trusting the caller-supplied `body.user_id`. A dedicated test asserts the
  service-role client issues no `strategies` select at all.
- `status` is **echoed from the row**, not hardcoded to `pending_review` like the first-submit reply:
  the row may sit at `private` (CONTRIB-02), and reporting a status we did not read would be the same
  fabricated-measurement defect C-3 is about.
- **Deviation [Rule 2 — missing error handling]:** the re-fetch is itself a network call, so it is
  wrapped. Letting it throw from inside the handler would have converted a well-understood duplicate
  into an uncaught 500 — the "cryptic 500" the behaviour spec forbids. It logs
  `process_key.csv_finalize_idempotent_refetch_failed` and falls through to the static reply.
- New log line `process_key.csv_finalize_idempotent_hit` (mirroring
  `process_key.idempotent_race_resolved`) so an operator can tell a dedup from a create — T-140.4-03-07.
- The residual message is **static**. The old body interpolated `exc`; the RED run observed it leaking
  `db.internal`, `10.0.0.7` and `port 5432` into `human_message`. It deliberately makes **no claim about
  whether the write landed**, because this arm catches non-23505 failures too and from there the outcome
  is unobservable — asserting "nothing was created" would repeat the defect class.

**TRAP-9, discharged as specified.**
Old name: `test_pyapi_09c_csv_finalize_double_submit_reaches_finalize_branch`.
New name: `test_pyapi_09c_csv_finalize_does_not_hit_the_precheck_shortcircuit`.
Reason: its `user_sb.rpc` is a `MagicMock` returning a fresh id **unconditionally**, so it never said
anything about whether the database dedups — **it is what pinned review finding C-2 green.** What it
really proves is a control-flow property (the request reaches the finalize branch, not the moved-away
`WIZARD_DUPLICATE` pre-check), which is still true. **Its assertions are byte-unchanged**; only the name
and docstring moved to match them, and it now doubles as the first-submit anti-regression case.
It passed throughout, including during the RED run — evidence the rename preserved a true property.

### Task 3(a) — the client source gate (`cc5e60c0`)

```ts
if (loaded.wizardSessionId && (loaded.source ?? "api") === source) {
```

**Post-value for the criterion:** comment-stripped `grep -c "loaded.wizardSessionId"` → **2**, unchanged
(the guard and the assignment); the guard is now conditional on `source`.

**RED observed on the untouched gate** (production line temporarily reverted):

```
× carries the wizardSessionId forward WITHIN a source, and not across one
  AssertionError: expected 'ls-session-id' to be undefined
× does NOT leak an abandoned API draft's session id into a CSV resume
  AssertionError: expected 'api-session-id' to be undefined
× does NOT leak a CSV draft's session id into an API resume
  AssertionError: expected 'csv-session-id' to be undefined
Tests  3 failed | 32 passed (35)
```

**`?? "api"` proven load-bearing** by a second mutation — the naive `loaded.source === source`:

```
Tests  4 failed | 31 passed (35)
```
reddening **two PRE-EXISTING** cases (`does NOT restore metadata step…`, `does NOT restore submit
step…`) plus the new legacy-payload guard. `source` is optional and documented as meaning `'api'` when
absent, so a bare equality would silently stop restoring for every v1 payload — a resume regression with
no error message. Both mutations reverted.

**Declining the restore is safe:** `WizardClient` seeds `wizardSessionId` from `newWizardSessionId()` on
mount (`WizardClient.tsx:182-184`), so the CSV wizard keeps its own fresh token — which is what a
distinct submission should carry.

⚠️ **DEVIATION, surfaced not averaged (Rule 7).** The plan said *"Do not weaken any of them"* about the
existing resume-override cases. One of them —
`"always carries the wizardSessionId forward so funnel correlation survives resume"` — **directly
asserted the leak being closed**: it drove a single source-less (⇒ `'api'`) payload through both
branches and required the carry on **both**. Its api→api half was and is correct; its api→csv half
encoded the defect. Deleting the test would have deleted the correct half too, so it was **re-scoped**
using the same discipline the plan mandated for TRAP-9: renamed to
`"carries the wizardSessionId forward WITHIN a source, and not across one"`, true assertion kept,
defect-encoding assertion **inverted**, and the whole reason recorded in-file. **No other existing case
was touched** — in particular the `csv_preview`/`csv_submit` regression test the CSV branch's comment
refers to is untouched, and a dedicated new case asserts the `step` and `strategyName` rules still fire.

### Task 3(b) — the copy, quoted old → new

**⚠️ All three are founder-owed under CONTEXT §7.1 (copy vs `DESIGN.md` §Voice).** These tests prove the
false claims are gone, **not** that the replacements read well.

**1. `CSV_SUBMIT_FAILED` — fix[0]**
- OLD: *"Open /strategies in another tab first. If your strategy is listed, the save did complete and
  submitting again would create a second copy."*
- NEW: *"Open /strategies in another tab first. If your strategy is listed, the save did complete and
  you are done."*
- Reason: the trailing clause is **now false**, and it was half of the contradiction.

**2. `CSV_SUBMIT_FAILED` — fix[1]**
- OLD: *"If it is not listed, submit again."*
- NEW: *"If it is not listed, submit again. Submitting again from this same wizard cannot create a
  second copy — a repeat submit resolves to the strategy that already exists."*
- Reason: states the guarantee that now exists, at the point the user acts on it.

**3. `CSV_SUBMIT_NO_STRATEGY_ID` — fix line: UNCHANGED, deliberately.**
*"Submit again. On the CSV path a repeat submit of the same wizard session cannot create a second
strategy."* It was **FALSE on the untouched tree and is TRUE again now.** Kept because Task 1 landed in
this branch — that dependency is the reason it was safe to keep, and the reason the plan halted rather
than shipping it early.

**THE CONTRADICTION, EXPLICITLY RESOLVED.** `CSV_SUBMIT_FAILED` said *"submitting again would create a
second copy"*; `CSV_SUBMIT_NO_STRATEGY_ID` said *"a repeat submit … cannot create a second strategy."*
Both are reachable from the same CSV submit step. **Resolved in favour of the promise**, because the
mechanism now exists and is the *same one* for both codes: the three-column index plus the 23505 arm.
The uncertainty in `CSV_SUBMIT_FAILED`'s title and cause is **kept** — it is still true and is a
different claim (we cannot observe whether the save completed); only the **consequence of retrying**
changed, and it is now bounded.

**Verified at source, not assumed, before making that promise:** every `clearWizardState()` call site is
a submit **success** or an explicit **delete-draft** (`WizardClient.tsx:590, 618, 659, 666, 1155`), and
delete-draft explicitly regenerates the session id per NEW-C14-08. A **failed** submit therefore keeps
the id in React state, so the retry reuses it and is fenced. Starting a fresh wizard mints a new id and
is a genuinely new submission — which is why the non-destructive check stays **first** in the list.

**4. Comment blocks (not user-facing).** `CSV_SUBMIT_NO_STRATEGY_ID`'s block now records that its
promise was false when 140.3-12 wrote it and names the three things that make it true, so the next
reader can check rather than trust — *"the guarantee itself is real HERE and is kept"* is precisely the
reasoning that let a false claim stand. `WIZARD_DUPLICATE`'s block is **scope-corrected**: it describes
the **API path only**. On the CSV branch it was unreachable, and it is **still deliberately not emitted**
— the new arm answers with the ordinary first-submit envelope, because the honest thing to show a user
who submitted twice is their strategy, not a "duplicate" notice.

**Banned-claims check:** the four hand-typed `FORBIDDEN` fragments (`been notified`,
`we fetched your trades`, `data is unchanged`, `wizard_session_id idempotency`) were read against every
new sentence by hand before writing. None appears; the scan passes.

**Table size unchanged at 55** — copy edits only, no entry added or removed, so neither
`EXPECTED_TABLE_SIZE` literal moved.

---

## Measured criteria — every pre-value measured on the untouched tree, with its predicate

⚠️ **Every remaining pre-value was RE-MEASURED at the new HEAD `885e6023`** after plans 01/04/05 landed
and `.planning/` became tracked, because a pre-value measured at `a77d607e` is a claim about a tree that
no longer exists. **All were identical** (#9 `0`, #10 `2` both `= 55`, #11 `3`, #12 `2`, #13 `1`), so
none needed adjusting — and none would have been adjusted if it had disagreed.

| # | Predicate | Plan says | Measured PRE | POST |
|---|---|---|---|---|
| 1 | `grep -vE '^\s*--' <mig> \| grep -A3 "CREATE UNIQUE INDEX" \| grep -c "source"` | ≥ 1 | n/a (new file) | **2** ✅ |
| 2 | leading column, read from the definition | `user_id` | — | **`user_id`** (col 1 of 3) ✅ |
| 3 | `grep -c "strategies_user_wizard_session_uniq" supabase/tests/test_wizard_session_idempotency.sql` | 3 → 0 | **3** ✅ | **0** ✅ |
| 4 | `grep -vE '^\s*--' <mig> \| grep -c "DROP INDEX"` | ≥ 1 | n/a | **1** ✅ |
| 5 | `grep -nE "^CREATE UNIQUE INDEX\|^DROP INDEX" <mig>` | create before drop | — | **167 / 181** ✅ |
| 6 | `grep -cE '^\s*\\' <both SQL test files>` (psql meta-commands) | 0 | — | **0 / 0** ✅ |
| 7 | `git status --porcelain \| grep -cE "20260726000225\|create_wizard_strategy\|add_wizard_composite_key"` | 0 | — | **0** ✅ |
| 8 | `grep -c "RAISE EXCEPTION" supabase/tests/test_csv_finalize_double_submit.sql` | ≥ 1 | n/a | **13** ✅ |
| 9 | `grep -cE "EXPECTED_TABLE_SIZE\s*=\s*Object\.keys" src/lib/wizardErrors.test.ts` | 0 before & after | **0** ✅ | **0** (file untouched) |
| 10 | `grep -cE "EXPECTED_TABLE_SIZE = [0-9]+" src/lib/wizardErrors.test.ts` | 2 before & after | **2** (`:1348`, `:1491`, both `= 55`) ✅ | **2**, value unchanged |
| 11 | `grep -cE "Object\.keys\([A-Za-z_]*\)\.length" src/lib/wizardErrors.test.ts` | ⚠️ TRAP — **3**, not 0 | **3** ✅ confirmed | **3** — **both oracle guards intact, neither deleted** |
| 12 | `grep -vE '^\s*(//\|\*)' src/lib/wizard/localStorage.ts \| grep -c "loaded.wizardSessionId"` | 2 | **2** ✅ | **2** — unchanged count, guard now `&& (loaded.source ?? "api") === source` |
| 13 | `grep -c 'finalize_csv_strategy RPC failed: {exc}' analytics-service/routers/process_key.py` | 1 → 0 | **1** ✅ | **0** ✅ |
| 14 | in the CSV finalize `except`, count of f-strings interpolating `exc`/`refetch_exc` | 0 | 1 (the old body) | **0** ✅ |
| 15 | the 23505 re-fetch `.eq()` chain | `user_id` + `wizard_session_id` + `source` | — | **all three** ✅ (`process_key.py:1138-1140`) |
| 16 | `git diff --name-only` for `supabase/` paths in Task 2 | 0 | — | **0** ✅ |

**Every pre-value agreed with the plan. None was adjusted.** In particular #11: the TRAP grep returns
**3** on correct code — `:1343` is the file's own comment *"Deliberately NOT
`Object.keys(WIZARD_ERROR_COPY).length`"*, and `:1352`/`:1495` are the **correct** assertions where the
hand-typed literal is the oracle and the derived count is the value under test. Both guards are intact.

**Full-suite verification at this plan's final tree:**

| Gate | Result | Baseline |
|---|---|---|
| `npx vitest run` (FULL) | **9828 passed \| 287 skipped** (716 files) | 9792 / 287 ✅ above |
| `cd analytics-service && python3 -m pytest -q` (FULL) | **4801 passed / 96 skipped** | 4778 / 96 ✅ above |
| `python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` | **Success: no issues found in 89 source files** | clean ✅ |
| `npx tsc --noEmit` | **exit 0** | exit 0 ✅ |
| `npm run lint` | **0 errors, 1 warning** | 1 pre-existing warning ✅ |
| `tsx scripts/dump-sql-functions.ts --check` | *"SQL function snapshot is current (104 functions)"* | ✅ |
| `PATH=/opt/homebrew/opt/node@22/bin npx vitest run <my files>` | **161 passed** | CI Node 22 ✅ |

The single lint warning is `EquityChart.tsx:1119` (`react-hooks/exhaustive-deps`) — pre-existing, not in
this plan's fence, **not touched**.

---

## Commits

| Task | Commit | What |
|---|---|---|
| 1 | `75eefed3` | `feat` — migration + both SQL receipts + regenerated schema snapshot |
| 2 | `a0ea050c` | `test` — RED: the 23505 arm's five cases, TRAP-9 re-scoped |
| 2 | `fca3f5d6` | `feat` — GREEN: the arm, tenant-scoped re-fetch, static residual message |
| 3 | `cc5e60c0` | `fix` — client source gate + copy reconciliation |

Files, all 10 within the plan's fence and nothing outside it:
`supabase/migrations/20260728120000_*.sql` (new) · `supabase/tests/test_csv_finalize_double_submit.sql`
(new) · `supabase/tests/test_wizard_session_idempotency.sql` ·
`supabase/schema/functions/finalize_csv_strategy.sql` · `analytics-service/routers/process_key.py` ·
`analytics-service/tests/test_process_key.py` · `src/lib/wizard/localStorage.ts` ·
`src/lib/wizard/localStorage.test.ts` · `src/lib/wizardErrors.ts` ·
`src/lib/wizardErrors.test.ts` — **not modified** (copy edits did not move the table size, and the two
hand-typed oracles were deliberately left alone).

**Untouched, as required:** `20260726000225_strategy_verifications_tenant_scope_uniq.sql` (**not
reverted, not edited**), `create_wizard_strategy`, `add_wizard_composite_key`, `SyncPreviewStep.tsx`,
`SubmitStep.tsx`, `.planning/STATE.md`, `.planning/ROADMAP.md`. Zero packages installed
(`package.json` / `package-lock.json` untouched). `supabase/.temp/project-ref` re-verified as
`khslejtfbuezsmvmtsdn`, unchanged — the CLI was never pointed anywhere.

---

## Deviations from plan

**1. [Rule 7 — surface the conflict] An existing localStorage test asserted the leak.**
`"always carries the wizardSessionId forward so funnel correlation survives resume"` required the
session-id carry on BOTH branches from one source-less payload. The plan said not to weaken the
existing cases; this one was in direct contradiction with the required change. Re-scoped rather than
deleted or averaged, using the plan's own TRAP-9 discipline. Detailed under Task 3(a). Commit `cc5e60c0`.

**2. [Rule 2 — missing error handling] The 23505 re-fetch is wrapped.**
Not in the plan. The re-fetch is a network call inside an `except` block; an exception there would
escape as an uncaught 500 — the exact "cryptic 500" the behaviour spec forbids. Logs
`process_key.csv_finalize_idempotent_refetch_failed` and falls through to the static reply.
Commit `fca3f5d6`.

**3. [judgement] `status` is echoed, not hardcoded.** The plan specified the first-submit envelope
*shape*. Copying its literal `"pending_review"` would misreport a `private` (CONTRIB-02) row, which is
the fabricated-measurement defect this phase exists to close. The shape is identical; the value is read.

**4. [scope] M93 recorded in two parts.** The specified single mutation was caught by the migration's
own STEP 4 self-verify, which prevented the mutated state from landing and confounded the intended
observation. A second edit to the same production file was needed to isolate the receipt. Both parts
recorded; neither compressed.

---

## OPEN — carried, not closed

1. **Founder-owed §7.1 — copy vs `DESIGN.md` §Voice.** This plan authored **three** new user-facing
   sentences (two `CSV_SUBMIT_FAILED` fix lines, one static `CSV_FINALIZE_FAILED` `human_message`), all
   quoted verbatim above. Tests prove the false claims are gone, **not** that the replacements read
   well. ⬜ OWED.
2. **Ledger row M92 — NOT MINE.** The execution prompt named it; `140.4-VALIDATION.md` and the plan
   both assign it to plan `02`, which was running concurrently. Deliberately not touched.
3. **The `140.4-VALIDATION.md` row table still shows M93/M94 unchecked** (`⬜`). That file is the
   phase-level assignment table and the gate collects receipts; **this plan does not edit it**, by its
   own rule that rows are recorded in each plan's own SUMMARY.
4. **The SQL lane has not run in CI on this branch yet.** `sql-tests` needs `TEST_SUPABASE_DB_URL`,
   which is a GitHub Actions secret — the two SQL files have executed against the coordinator's TEST
   apply and against a local throwaway cluster, but not yet through the CI job that will gate them.
5. **Docker Desktop is down** on this machine (`meta.db: input/output error`, ~5.9 GiB free on the host
   volume). Unrelated to this work, not repaired, flagged for the founder.
6. ⚠️ **Branch protection on `main` is OFF** (settled founder decision). Every gate named here is
   **advisory at merge** — *"would have caught"*, never *"did stop"*.

---

## Self-Check: PASSED

Files (`test -f`):
- `supabase/migrations/20260728120000_csv_finalize_double_submit_idempotency.sql` — FOUND
- `supabase/tests/test_csv_finalize_double_submit.sql` — FOUND
- `supabase/tests/test_wizard_session_idempotency.sql` — FOUND, modified
- `supabase/schema/functions/finalize_csv_strategy.sql` — FOUND, regenerated
- `analytics-service/routers/process_key.py`, `analytics-service/tests/test_process_key.py` — FOUND
- `src/lib/wizard/localStorage.ts`, `src/lib/wizard/localStorage.test.ts`, `src/lib/wizardErrors.ts` — FOUND

Commits (`git log --oneline --all | grep`): `75eefed3`, `a0ea050c`, `fca3f5d6`, `cc5e60c0` — all FOUND.

Mutations: `git status --porcelain` clean of residue; `git diff --stat` against the committed GREEN is
empty for `process_key.py` and `localStorage.ts`.

**No stubs.** Nothing in this plan renders a hardcoded empty/placeholder value.

**No threat flags.** The migration and the route arm both operate inside the trust boundaries already
enumerated in the plan's threat model; no new network endpoint, auth path, file access pattern or
schema surface was introduced beyond the single indexed column write.
