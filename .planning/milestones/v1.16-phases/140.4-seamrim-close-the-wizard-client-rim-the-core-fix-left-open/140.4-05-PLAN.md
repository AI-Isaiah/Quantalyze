---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/LiveRegion.tsx
  - src/components/ui/LiveRegion.test.tsx
  - src/components/portfolio/PortfolioOptimizer.tsx
  - src/components/portfolio/PortfolioOptimizer.test.tsx
  - src/components/admin/AllocatorMatchQueue.tsx
  - src/components/admin/AllocatorMatchQueue.test.tsx
  - src/app/(dashboard)/allocations/components/WeightOptimizerSection.tsx
  - src/app/(dashboard)/allocations/components/WeightOptimizerSection.test.tsx
autonomous: true
requirements: [SEAMRIM-10]

must_haves:
  truths:
    - "A screen-reader user is told when one of these three surfaces fails, not left with silence"
    - "The visual rendering of all three surfaces is byte-identical to before"
    - "A deleted announcement reddens a named test — three deletions shipped green in this range"
  artifacts:
    - path: "src/components/ui/LiveRegion.tsx"
      provides: "a visually-inert shared announcement primitive extracted from the in-tree idiom"
      contains: "aria-live"
  key_links:
    - from: "the three converted surfaces"
      to: "src/components/ui/LiveRegion.tsx"
      via: "one shared component carries the announcement, so consumers inherit it"
      pattern: "LiveRegion"
---

<objective>
Close the announcement class on the three surfaces this programme measured regressing, using a
**visually inert** primitive that needs no `DESIGN.md` change.

**The measurement, re-derived at plan time** (predicate published below, per CONTEXT §5c):

```
files under src/ (non-test .tsx) that set an error state : 57
  announce somewhere in-file (role="alert" | aria-live | import ErrorEnvelope) : 30
  ZERO announcement channel, file-wide                    : 27
```

Direct counts on the three targets, on the untouched tree:
```
PortfolioOptimizer.tsx      role="alert" = 0   aria-live = 0
AllocatorMatchQueue.tsx     role="alert" = 0   aria-live = 0
WeightOptimizerSection.tsx  role="alert" = 0   aria-live = 0
```
And the in-range diff: `role="alert"` **3 deletions, 0 additions** — *"announcement was the only
channel"*, because on `PortfolioOptimizer` the trigger button **unmounts on the failing branch** (the
error render `return`s a different tree), so focus falls to `<body>`; and on `AllocatorMatchQueue` the
action is bound to the `r` keyboard shortcut and the surface went from a modal `alert()` (always
announced) to a silent inline card.

**`grep -rln 'role="alert"' tests/ src/__tests__/ e2e/` → one unrelated file. No test in this repo
asserts an announcement today**, which is exactly how three deletions shipped with green CI.

## The authority, and why no founder decision is owed

`DESIGN.md` §A11y minimums (DESIGN-05) **already mandates this**, verbatim:

> *"ARIA live regions: `role="alert"` on blocking errors; `role="status"` + `aria-live="polite"` on
> non-blocking state changes."*

This plan **implements an existing DESIGN.md rule**. It does **not** touch `DESIGN.md`, and it does
**not** route these surfaces through `ErrorEnvelope`.

⚠️ **Routing them through `ErrorEnvelope` is explicitly OUT OF SCOPE** (CONTEXT §5c and §6). It is
*type-possible* — `src/lib/envelope.ts` declares `ErrorEnvelope.code` as `string`, only
`buildEnvelope(code: WizardErrorCode)` is type-fenced — but it is a **visual redesign of three
allocator surfaces**: `ErrorEnvelope` carries a locked visual contract (`border-negative/30
bg-negative/5`, *"16px DM Sans semibold #1A1A2E … REQ DESIGN-02"*, a fix-step list, a Copy-diagnostics
block, a Retry/Cancel pair) and would force these surfaces to mint a `code` and a `correlation_id` —
the pressure that leads straight into the founder-owned §Error Envelope decision CONTEXT §6 fences
off. **Do not edit `DESIGN.md`. Do not import `ErrorEnvelope` into these three files.**

## Coverage-law classification (CONTEXT §2 — binding)

- **The `<LiveRegion>` primitive itself — ROW 1 (a shared component).** `ErrorEnvelope.tsx:134` is the
  proof of the mechanism: one node carrying `role="alert"`, and 100% of its consumers inherit the
  announcement. `140.4-PATTERNS.md` §9 calls that split *"the whole coverage law in one line."*
- **Adopting it at three of the 27 surfaces — ROW 3 (per-site), and therefore PARTIAL BY
  CONSTRUCTION.** 3 of 27 is **11%**. Measured coverage for row 3 in this programme: 1/8 · 2/56 ·
  0/32. **Say this in the SUMMARY in those words.** The remaining 24 files are not closed by this plan
  and this plan does not claim them.

## The predicate and the fence, stated deliberately (CONTEXT §5 requires this)

- **Predicate:** a non-test `.tsx` under `src/` that calls a `set\w*(Error|Err|Failure|Msg)\w*\(`
  setter; announcement channel = `role="alert"` ∨ `aria-live` ∨ an `ErrorEnvelope` import, counted
  **file-wide** so an early-`return` render cannot defeat it.
- **Why file-wide:** two narrower JSX-shape predicates returned 35 files/41 sites and 13 files and
  **neither caught `PortfolioOptimizer` or `AllocatorMatchQueue`** — the two named regressions —
  because those files use an early `return`. **Do not size this remedy from a JSX-shape grep.**
- **Fence for THIS plan:** the three surfaces named in the review as regressions
  (`PortfolioOptimizer`, `AllocatorMatchQueue`, `WeightOptimizerSection`). The class is app-wide at 27
  and is explicitly left open, with the primitive in place so each future conversion is one line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@DESIGN.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-CONTEXT.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-RESEARCH.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-PATTERNS.md

<standing_constraints>
Binding on every task. You are a fresh agent with no memory of this phase's discussion.

- **Read `DESIGN.md` before any visual decision** (CLAUDE.md). This plan is deliberately
  **visually inert**; if you find yourself changing a colour, a spacing token or a typographic class,
  you have left the plan.
- **Verify with the FULL suite, never a scoped path.** Baselines (CONTEXT §8): `npx vitest run` →
  **9792 passed | 287 skipped** (715 files) · `tsc --noEmit` exit 0 · `npm run lint` 0 errors
  (1 pre-existing warning). Re-establish before editing.
- **`vi.spyOn` + `vi.restoreAllMocks()`. NEVER `vi.stubGlobal`** (DEF-16-1).
- **Zero new packages.**
- `.planning/` is gitignored with no git backup. Never `git add .planning`. Never `gsd-sdk query state.*`.
  Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`.
- **Branch protection is OFF** ⇒ *"would have caught"*, never *"did stop"*.
- **grep is LINE-BASED** (DEF-16-2). **`grep -h` with a downstream filename filter is PROHIBITED.**
- **Every pre-measured value here was taken on the untouched tree at HEAD `a77d607e`.** Re-run first.
</standing_constraints>

<read_first>
- `src/components/ui/CardShell.tsx:77` — **the idiom already in the tree, verbatim:**
  `<p role="status" aria-live="polite" className="sr-only">`. This plan is an **extraction, not an
  invention** (CLAUDE.md Rule 11). It appears inline at roughly 20 ad-hoc sites; no reusable primitive
  exists in `src/components/ui/` (verified at plan time by listing that directory).
- `DESIGN.md` §A11y minimums (DESIGN-05) — the mandate quoted above. **READ ONLY. Do not edit.**
- `src/components/error/ErrorEnvelope.tsx:134` — `role="alert"` on the container. The mechanism proof.
  **Do not import this component into the three surfaces.**
- The three error renders, located by text:
  - `src/components/portfolio/PortfolioOptimizer.tsx` — the branch
    `if (error || (computationStatus === "failed" && !suggestions)) { return ( <Card> … "Optimizer failed" … ` .
    Note the `Retry` button lives inside this returned tree, so the previous trigger has unmounted.
  - `src/components/admin/AllocatorMatchQueue.tsx` — the branch `if (error || !data) { return ( <Card className="border-negative/40"> <p …>{error || "Failed to load"}</p>` .
  - `src/app/(dashboard)/allocations/components/WeightOptimizerSection.tsx` — the
    `{status === "error" && ( <div className="mt-3"><EmptyStateCard heading={FAILURE_COPY[failure].heading} … ` block.
- `src/components/ui/CardShell.test.tsx` — the house style for a small `src/components/ui` unit test.
- The three existing test files (`PortfolioOptimizer.test.tsx`, `AllocatorMatchQueue.test.tsx`,
  `WeightOptimizerSection.test.tsx`) — do not weaken any existing case.
- `src/lib/seam-ssr-exposure.pin.test.ts:237-246` — the two-polarity discipline to copy for the
  primitive's own unit test.
</read_first>

<interfaces>
**`src/components/ui/LiveRegion.tsx`** — one component, visually inert:

- Props: the message (`string | null`) and an assertiveness switch.
- Renders `role="alert"` + `aria-live="assertive"` for blocking errors, `role="status"` +
  `aria-live="polite"` otherwise — **exactly the DESIGN-05 split**, not a choice this plan makes.
- Always `className="sr-only"`. **Zero visual delta by construction.** No colour, no spacing, no
  typography token.
- Renders the element even when the message is null (an empty live region that is present before the
  message arrives is what makes assistive tech announce the *change*; a region mounted at the same
  time as its text is frequently missed). State this reasoning in the docblock — it is the one
  non-obvious thing about the component and the thing a future "simplification" will delete.

**Adoption shape at each surface:** add `<LiveRegion message={…} assertive />` inside the existing
error branch, carrying the **same sentence the surface already renders visually**. Do not author new
copy, do not change the visible tree, do not move the Retry button.

**Out of scope, stated so it is not smuggled in:**
- `ErrorEnvelope`, `buildEnvelope`, `WizardErrorCode`, `correlation_id` — none of them.
- `DESIGN.md` — not edited.
- The other 24 non-announcing files.
- The repo-wide nested `<a><button>` sweep (synthesis §8, a separate a11y pass).
- `MatchQueueIndex.tsx`'s unchecked `fetch()` kill-switch reads — a different carrier of a different
  class, out of this plan's fence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract the LiveRegion primitive with a two-polarity unit test</name>
  <files>src/components/ui/LiveRegion.tsx, src/components/ui/LiveRegion.test.tsx</files>
  <read_first>
- `src/components/ui/CardShell.tsx:77` (the idiom), `src/components/ui/CardShell.test.tsx` (house
  style), `DESIGN.md` §A11y minimums, `src/lib/seam-ssr-exposure.pin.test.ts:237-246` (two polarities).
  </read_first>
  <behavior>
- Assertive mode → the rendered node has `role="alert"` and `aria-live="assertive"` and is queryable
  via `getByRole("alert")`.
- Non-assertive mode → `role="status"` and `aria-live="polite"`, queryable via `getByRole("status")`.
- The message text is present in the accessible tree.
- **Visually inert (the load-bearing negative):** the rendered element's `className` is exactly
  `sr-only` — no colour, spacing or typography class. A future author adding a visible style to this
  primitive breaks a named test.
- A null message still renders the region (the region must pre-exist its content).
  </behavior>
  <action>
Create `src/components/ui/LiveRegion.tsx` per `<interfaces>`. Docblock must state: that it implements
`DESIGN.md` §A11y minimums (DESIGN-05) rather than making a new design decision; that it is an
extraction of the `CardShell` idiom, not an invention; why it is `sr-only` (the announcement is a
property of the render, independent of which component draws the visual); and why the region renders
before its message arrives.

Write the unit test with **both polarities** — assert the assertive shape is present AND assert the
polite shape is present AND assert the visual-inertness negative. A test that only checks
`role="alert"` exists would pass on a component that hard-codes it and ignores its props.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/ui/LiveRegion.test.tsx</automated>
  </verify>
  <acceptance_criteria>
- **Inertness is pinned by a NAMED TEST, not by a grep.** The test file contains a case asserting the
  rendered element's `className` is **exactly** `sr-only` — no colour, spacing or typography token.
  Name that case in the SUMMARY; it is the thing **M107** reddens.
  ⚠️ CONTEXT §3: *a grep proves a state; only a guard proves the state is held* — and the one GREEN
  mutation in this programme landed on a closure whose receipt was a one-time `grep → empty`. The two
  greps below are **corroborating pre/post measurements, not the receipt**:
  `grep -c 'className="sr-only"' src/components/ui/LiveRegion.tsx` → **1**, and comment-stripped
  `grep -vE '^\s*(//|\*|/\*)' src/components/ui/LiveRegion.tsx | grep -cE 'text-|bg-|border-|px-|py-|mt-|font-'` → **0**.
- The test file asserts **both** `getByRole("alert")` and `getByRole("status")` in separate cases.
- `grep -c "ErrorEnvelope" src/components/ui/LiveRegion.tsx` → **0**.
- `git diff --name-only` contains **zero** occurrences of `DESIGN.md`.
- `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>
A visually-inert shared primitive exists in `src/components/ui/`, implements the DESIGN-05 split, and
its own test proves both polarities and its inertness.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: adopt it at the three measured-regressing surfaces, with a render assertion each</name>
  <files>src/components/portfolio/PortfolioOptimizer.tsx, src/components/portfolio/PortfolioOptimizer.test.tsx, src/components/admin/AllocatorMatchQueue.tsx, src/components/admin/AllocatorMatchQueue.test.tsx, src/app/(dashboard)/allocations/components/WeightOptimizerSection.tsx, src/app/(dashboard)/allocations/components/WeightOptimizerSection.test.tsx</files>
  <read_first>
- The three error renders located by the text anchors in `<read_first>` above.
- The three existing test files — every case in them must stay green and unmodified.
  </read_first>
  <behavior>
- `PortfolioOptimizer`: drive the optimizer request to failure → the error card renders **and**
  `getByRole("alert")` contains the same sentence the card shows. The existing visible tree
  (`"Optimizer failed"`, the message paragraph, the `Retry` button) is unchanged.
- `AllocatorMatchQueue`: drive the load to failure → `getByRole("alert")` contains the message the
  card shows (`error || "Failed to load"`). The existing `Retry` button is unchanged.
- `WeightOptimizerSection`: drive `status === "error"` (both `failure` values — `unavailable` and
  `unreadable`) → `getByRole("alert")` contains that failure's heading or body. `EmptyStateCard`
  renders exactly as before.
- **Negative control on each surface (mandatory):** the success / idle render exposes **no**
  `role="alert"`. Without it the assertion is satisfied by a component that announces unconditionally,
  which would announce on every render and is worse than silence.
  </behavior>
  <action>
Add `<LiveRegion … assertive />` inside each of the three error branches, carrying the sentence the
surface already renders. Change nothing else: no visible markup, no copy, no control placement, no
class.

Add the render assertions above to the three existing test files. Use the assertion the repo has none
of today (`getByRole("alert")`) rather than a DOM-attribute string match, so a refactor that keeps the
semantics keeps the test green.

⚠️ These files use early `return`s for their error branches. Verify by running the test that the
region is actually inside the branch that renders — a `LiveRegion` added after an early `return` is
dead code that greps clean and announces nothing. **That failure mode is exactly why the population
predicate above is file-wide.**
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/portfolio/PortfolioOptimizer.test.tsx src/components/admin/AllocatorMatchQueue.test.tsx "src/app/(dashboard)/allocations/components/WeightOptimizerSection.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
- **Pre-values, re-measure before you start** — on the untouched tree all three are `0`:
  `grep -c 'role="alert"' src/components/portfolio/PortfolioOptimizer.tsx` → 0 ·
  same for `AllocatorMatchQueue.tsx` and `WeightOptimizerSection.tsx`.
  ⚠️ **Do NOT use these greps as the post-condition.** The surfaces now import a component; the
  literal need not appear in them. The post-condition is the **render assertion**, which is the only
  thing that distinguishes a live region inside the rendered branch from one after an early `return`.
- Each of the three test files contains at least one `getByRole("alert")` (or `findByRole`) assertion
  **and** one negative-control assertion that the success/idle render exposes none.
- **Pre-value:** `grep -rln 'role="alert"' tests/ src/__tests__/ e2e/ | wc -l` → **1** (one unrelated
  file) on the untouched tree. State the post-value in the SUMMARY.
- No existing case was removed: `git diff -U0` on the three test files shows **zero** removed `it(`
  lines, or each removal is listed with its strictly stronger replacement (TRAP-9).
- The visible tree is unchanged: `git diff` on the three components shows only added `<LiveRegion …>`
  lines and the import — **no** changes to any `className`, copy string or control. State this
  explicitly in the SUMMARY.
- `git diff --name-only` contains **zero** occurrences of `DESIGN.md` and zero of `ErrorEnvelope.tsx`.
  </acceptance_criteria>
  <done>
All three surfaces announce their failure through the shared primitive, each proven by a render
assertion with a negative control, and every visible pixel is unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 3: falsify the announcement and the inertness — Falsifiability Ledger rows M96 and M107 — and record the fence honestly</name>
  <files>src/components/admin/AllocatorMatchQueue.tsx</files>
  <read_first>
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md` — the
  `## Falsifiability Ledger` rules block and rows M86–M89. **Read; do not edit.** Highest row in the
  programme: **M89**; plans 01–04 take M90–M95b; this plan's row is **M96**.
  </read_first>
  <action>
One mutation, **semantic**, to **PRODUCTION** source; never a syntax error, never a type error, never
a test edit. Record both in this plan's SUMMARY under `## Falsifiability Ledger — rows M96, M107`.

**M96 — the announcement is LIVE at the SECOND member of the class.**
The class is "a converted surface that announces its failure". `PortfolioOptimizer` is member one (the
review's headline instance). CONTEXT: **mutate the SECOND member** — `AllocatorMatchQueue`. Delete its
`<LiveRegion …>` element from the error branch, leaving the import and the visible card intact.

This is the mutation that distinguishes LIVE from INERT: *"a deleted carry and a correct absence both
produce `null`"*. Deleting the element while keeping the import means a grep for `LiveRegion` in that
file still returns a hit — so a grep-based receipt would report the guard satisfied. Only the render
assertion sees it.

Must redden: `AllocatorMatchQueue`'s new `getByRole("alert")` case.
Must stay GREEN, and record it: `PortfolioOptimizer`'s and `WeightOptimizerSection`'s announcement
cases, plus `AllocatorMatchQueue`'s own negative control and its existing visible-render cases —
proving the three surfaces are pinned independently rather than by one shared assertion.

**M107 — the primitive's VISUAL INERTNESS is LIVE, and it is a production mutation, not a style opinion.**
`src/components/ui/LiveRegion.tsx` does not exist on the untouched tree — **this plan creates it**, so
after Task 1 it is production source and a one-token edit to it is a semantic mutation like any other.

Mutation: `className="sr-only"` → `className="sr-only text-negative"` (any visible token will do; use a
token that exists in the design system so this stays a *semantic* change and not a typo).

This re-creates exactly the defect the criterion guards: a shared primitive that was adopted precisely
**because it is visually inert** silently acquiring a visual contract, on three surfaces at once, with
no `DESIGN.md` decision behind it. That is how a stop-gap becomes an unreviewed design change.

Must redden: the Task 1 inertness case — **name it in the row**.
Must stay GREEN and record it: both `getByRole` polarity cases (the announcement still works — proving
the inertness case is testing inertness and not the component in general), and all three surfaces'
render assertions.

⚠️ **This row is why the inertness criterion is not a grep.** A `grep -c 'className="sr-only"' → 1`
receipt **passes** under this mutation: the string is still there, with a visible token appended.

**Also record the fence, in the SUMMARY, in these words:** this plan converts **3 of 27** surfaces;
the adoption is a per-site edit with no artefact and is therefore **partial by construction**; the
primitive is row 1, so each future conversion is one line plus one assertion. Name the remaining 24
files by count, and state that this plan does not claim them. Branch protection is OFF ⇒ *"would have
caught"*, never *"did stop"*.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- Rows M96 **and M107** recorded, each with pasted RED output including the failing test name and
  assertion text.
- M107's row states that the `grep -c 'className="sr-only"' → 1` receipt **passes under its mutation**,
  and names the test that does not.
- The row states that the mutation **kept the import** and therefore that a grep receipt would have
  reported the guard satisfied — the point of the row.
- The row states it mutated the **second** member of the class and names the first.
- At least two named GREEN-side observations.
- `git status --porcelain` after revert shows no residual mutation.
- The SUMMARY contains the phrase **"partial by construction"** applied to the 3-of-27 adoption.
- **FULL suite green:** `npx vitest run` at or above baseline (state exact numbers); `npx tsc --noEmit`
  exit 0; `npm run lint` 0 errors.
- CI-parity run: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
  </acceptance_criteria>
  <done>
M96 proves the announcement is live at the second member with the import left in place, and the
3-of-27 fence is recorded as partial by construction.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application state change → a non-sighted user | The only channel is an ARIA announcement; focus already falls to `<body>` when the trigger unmounts |
| a shared UI primitive → the founder-owned visual contract | Any visual delta here would silently amend a `DESIGN.md` decision |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.4-05-01 | **Repudiation (accessibility)** | three error surfaces with zero announcement channel | mitigate | A shared `<LiveRegion>` primitive (row 1) adopted at the three measured regressions (row 3, **partial by construction**, 3 of 27), each pinned by a render assertion. Falsified by M96 |
| T-140.4-05-02 | Tampering (design authority) | drifting into the founder-owned §Error Envelope decision | avoid | The primitive is `sr-only` with zero visual tokens, asserted by a comment-stripped grep criterion; `DESIGN.md` and `ErrorEnvelope.tsx` are excluded from `files_modified` and asserted absent from the diff |
| T-140.4-05-03 | Tampering (guard decay) | an announcement added after an early `return` — dead code that greps clean | mitigate | The receipt is a **render** assertion, not a grep; M96 deletes the element while keeping the import precisely to prove a grep receipt would have passed |
| T-140.4-05-04 | Denial of service (assistive tech) | announcing on every render | avoid | A negative control per surface asserts the success/idle render exposes no `role="alert"` |
| T-140.4-05-05 | Repudiation | claiming the a11y class is closed at 3 of 27 | avoid | The SUMMARY states **"partial by construction"** and names the remaining count |
| T-140.4-05-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed** (`140.4-RESEARCH.md` §Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run` (FULL) at or above **9792 passed | 287 skipped**; state exact numbers.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
- `DESIGN.md`, `src/components/error/ErrorEnvelope.tsx` and `src/lib/envelope.ts` are **not** in the diff.
- Zero Python files, zero `supabase/migrations/`, zero `.planning/` paths staged.
</verification>

<success_criteria>
- A visually-inert `<LiveRegion>` primitive exists in `src/components/ui/`, implementing DESIGN-05's
  alert/status split, with a two-polarity unit test and an inertness assertion.
- All three measured-regressing surfaces announce their failure, each proven by a `getByRole("alert")`
  render assertion plus a negative control.
- Not one visible pixel changed; `DESIGN.md` was not edited.
- M96 reddens at the second member with the import left in place.
- The 3-of-27 fence is recorded as **partial by construction**.
</success_criteria>

<output>
Create `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-05-SUMMARY.md`.

Record: the re-derived 57/30/27 population and the predicate that produced it, including why a
JSX-shape predicate misses the two named regressions; the three pre-values (all 0) and the post-state;
the DESIGN-05 quote this plan implements and the explicit statement that `DESIGN.md` was not edited
and `ErrorEnvelope` was not imported; the pre/post count of test files referencing `role="alert"`; the
`## Falsifiability Ledger — rows M96, M107` section with pasted RED output and named GREEN-side observations for each;
and the **"partial by construction"** statement for the 3-of-27 adoption.
</output>
