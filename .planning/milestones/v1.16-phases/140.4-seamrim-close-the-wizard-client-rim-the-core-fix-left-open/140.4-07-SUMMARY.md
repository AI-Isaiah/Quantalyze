---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 07
subsystem: seam / log hygiene
tags: [SEAMRIM-06, seam-redaction, scrubSeamError, information-disclosure, M99]
requires:
  - src/lib/seam-redaction.ts (scrubSeamError — pre-existing, unmodified)
  - src/lib/seam-log-coverage.test.ts (the predicate this plan satisfies — NOT modified)
provides:
  - "scrubbed console sites at all 15 offending file:line across keys/sync, csv-finalize and verify-strategy"
  - "verify-strategy's already-declared perRequestSecrets applied to the CONSOLE, not only to Sentry"
  - "a clean tree at 15 of the 27 WP-1 widening sites, BEFORE plan 140.4-10 derives the roster"
affects:
  - plan 140.4-08 (the other 12 sites — measured disjoint, see § Partition)
  - plan 140.4-10 (the derived roster whose landing this de-risks)
tech-stack:
  added: []
  patterns:
    - "scrubSeamError(value) at every console site standing over a caught or error-shaped value"
    - "scrubSeamError(value, perRequestSecrets) where the handler holds a per-request credential"
key-files:
  created: []
  modified:
    - src/app/api/keys/sync/route.ts
    - src/app/api/keys/sync/route.test.ts
    - src/app/api/strategies/csv-finalize/route.ts
    - src/app/api/verify-strategy/route.ts
decisions:
  - "`.code` kept BESIDE the scrubbed rendering where it was a literal `.code` — it is the allowlisted SQLSTATE the following branches key off"
  - "`error?.code` REPLACED rather than kept: optional chaining is not covered by the guard's `.code` allowlist, which matches a literal `.`"
  - "no per-request secret passed at any keys/sync or csv-finalize site — none holds one in scope; stated per site below"
  - "the csv-finalize `computation_error` DB reason string is NOT scrubbed here — it is a persisted/rendered surface, not a console site, and is out of this plan's declared scope. Carried as an OPEN residual below."
metrics:
  duration: ~45 min
  completed: 2026-07-28
---

# Phase 140.4 Plan 07: Scrub tail A — keys/sync, csv-finalize, verify-strategy Summary

Wrapped every caught and error-shaped value that the three highest-exposure seam
routes pass to `console.*` in `scrubSeamError`, applying `verify-strategy`'s own
already-declared `perRequestSecrets` array to its logs for the first time — 15
sites closed, measured by a port of the shipped guard's own two-half predicate,
with **zero** control-flow, status-code or response-body change.

**Branch:** `worktree-agent-a2eafaf1a59cb287b`
**Base:** `21afb828` (`feat(140.4-01): GREEN — admin approve binds error on all 7 gate-feeding reads`)

| Commit | Task |
|---|---|
| `8f335fe4` | Task 1 — `keys/sync`, 6 sites |
| `f3253a6e` | Task 2 — `csv-finalize`, 6 sites |
| `5fed5d77` | Task 3 — `verify-strategy`, 3 sites + ledger row M99 |

---

## ⚠️ Coverage-law classification — PARTIAL BY CONSTRUCTION

This remedy is **coverage-law ROW 3 — a per-site edit with no artefact**, and it is
therefore **partial by construction**, in those words, exactly as CONTEXT §2 requires.
Measured coverage for that mechanism in this programme is 1/8 · 2/56 · **0/32**.

It is acceptable here for the two reasons the plan states, and both were re-established
at execution time rather than accepted:

1. **The set is fully enumerated, and the enumeration came from the guard's own
   predicate** — not from a hand count. `/tmp/seam-scan.mjs` is a verbatim port of
   `src/lib/seam-log-coverage.test.ts`'s `stripComments` / `caughtIdentifiers` /
   `consoleCalls` / `maskStrings` / `bareReferences` functions, run over **both**
   `it.each(SEAM_FILES)` halves (the catch-bound half at `:247` and the
   `error`/`*Err`/`*Error` half at `:263`).
2. **The row-1 artefact that makes it stick lands in plan `140.4-10`** — the derived
   roster, which will fail on any site this plan missed.

**A grep for `scrubSeamError` counts additions and proves nothing about absence.**
Per CONTEXT §3 the counts below are reported as a measurement of the edit, and the
**predicate run is the receipt**.

---

## The predicate, and what it measured

**Predicate (stated, not a bare integer):** for each file, comment-strip the source;
extract every `console.error(...)` / `console.warn(...)` argument list with a
quote-aware balanced-paren scan; mask string/template TEXT while preserving `${…}`
interpolations; then report every reference to (a) an identifier bound by a `catch (x)`
anywhere in the file, and (b) an identifier matching `/^(error|[\w$]*(Err|Error))$/`,
that is neither wrapped in `scrubSeamError`/`scrubSeamString` nor an allowlisted safe
property read (`.retryAfterS`, `.deadlineExceeded`, `.code`).

**Measured on the UNTOUCHED tree at base `21afb828`, before a line was written:**

```
[catch]    src/app/api/keys/sync/route.ts:220 passes bare `err`
[catch]    src/app/api/keys/sync/route.ts:382 passes bare `sessionErr`
[catch]    src/app/api/keys/sync/route.ts:382 passes bare `sessionErr`
[catch]    src/app/api/keys/sync/route.ts:382 passes bare `sessionErr`
[catch]    src/app/api/strategies/csv-finalize/route.ts:689 passes bare `placeholderThrow`
[catch]    src/app/api/strategies/csv-finalize/route.ts:689 passes bare `placeholderThrow`
[catch]    src/app/api/strategies/csv-finalize/route.ts:689 passes bare `placeholderThrow`
[catch]    src/app/api/verify-strategy/route.ts:384 passes bare `configErr`
[catch]    src/app/api/verify-strategy/route.ts:440 passes bare `err`
[errorish] src/app/api/keys/sync/route.ts:265 passes bare `assetClassErr`
[errorish] src/app/api/keys/sync/route.ts:290 passes bare `rpcError`
[errorish] src/app/api/keys/sync/route.ts:382 passes bare `Error`
[errorish] src/app/api/keys/sync/route.ts:382 passes bare `sessionErr`
[errorish] src/app/api/keys/sync/route.ts:382 passes bare `sessionErr`
[errorish] src/app/api/keys/sync/route.ts:382 passes bare `sessionErr`
[errorish] src/app/api/keys/sync/route.ts:439 passes bare `readErr`
[errorish] src/app/api/keys/sync/route.ts:454 passes bare `stampErr`
[errorish] src/app/api/strategies/csv-finalize/route.ts:1365 passes bare `error`
[errorish] src/app/api/strategies/csv-finalize/route.ts:1365 passes bare `error`
[errorish] src/app/api/strategies/csv-finalize/route.ts:532 passes bare `persistError`
[errorish] src/app/api/strategies/csv-finalize/route.ts:632 passes bare `selectErr`
[errorish] src/app/api/strategies/csv-finalize/route.ts:677 passes bare `placeholderErr`
[errorish] src/app/api/strategies/csv-finalize/route.ts:689 passes bare `Error`
[errorish] src/app/api/strategies/csv-finalize/route.ts:820 passes bare `updateError`
[errorish] src/app/api/verify-strategy/route.ts:384 passes bare `configErr`
[errorish] src/app/api/verify-strategy/route.ts:423 passes bare `persistError`
---
files scanned:          3
offence RECORDS:        26   (a site can be reported by both halves)
distinct file:line:     15
files with >=1 offence: 3
```

**The plan's plan-time re-derivation reproduced exactly: 15 distinct sites, 3 files.**
The 26-vs-15 gap is the two halves double-reporting; the plan's warning to budget for
both halves was correct and necessary — running only the catch half finds **9 records
across 4 sites** on these three files.

**AFTER (the same command, final tree):**

```
---
files scanned:          3
offence RECORDS:        0   (a site can be reported by both halves)
distinct file:line:     0
files with >=1 offence: 0
```

---

## The 15 sites, each with identifier, before and after

**Every one still REACHES its line, wrapped. Nothing was dropped** (the A-10 defect the
guard's own `FAILURE_REASON` forbids: *"Do NOT answer this by dropping err instead"*).

### `src/app/api/keys/sync/route.ts` — 6 sites

| # | line | identifier | half | per-request secret? |
|---|---|---|---|---|
| 1 | 220 | `err` | catch | no — see below |
| 2 | 265 | `assetClassErr` | errorish | no |
| 3 | 290 | `rpcError` | errorish | no |
| 4 | 382 | `sessionErr` (×3 refs + the `Error` token) | catch + errorish | no — see below |
| 5 | 439 | `readErr` | errorish | no |
| 6 | 454 | `stampErr` | errorish | no |

```diff
@@ composite membership probe @@
       console.error(
         `[keys/sync] composite membership probe failed for ${strategy_id}:`,
-        err,
+        scrubSeamError(err),
       );
@@ composite asset_class derive @@
           `[keys/sync] composite asset_class derive failed (non-blocking) for ${strategy_id}:`,
-          assetClassErr,
+          scrubSeamError(assetClassErr),
@@ stitch_composite enqueue @@
           `[keys/sync] enqueue_compute_job (stitch_composite) RPC failed for ${strategy_id}:`,
-          rpcError,
+          scrubSeamError(rpcError),
@@ session read — the X-User-Access-Token site @@
       `[keys/sync] could not read the session to forward X-User-Access-Token (non-blocking) for ${strategy_id}:`,
-      sessionErr instanceof Error ? sessionErr.message : String(sessionErr),
+      scrubSeamError(sessionErr),
@@ pre-stamp analytics read @@
       `[keys/sync] could not read existing analytics before stamping 'failed' ` +
         `(${logLabel}) for ${strategyId}:`,
-      readErr,
+      scrubSeamError(readErr),
@@ terminal stamp @@
       `[keys/sync] failed to stamp terminal 'failed' (${logLabel}) for ${strategyId}:`,
-      stampErr,
+      scrubSeamError(stampErr),
```

**The `sessionErr` site was the worst of the six and the plan called it correctly.** It
carried **three** references to the caught value — and the predicate additionally
reported the `Error` token from `instanceof Error`, because `Error` matches the
`errorish` naming convention. It is a site whose own message names
`X-User-Access-Token`, logging `err.message` — precisely where undici puts the outgoing
headers. Replacing the whole ternary with one leaf call clears all four reports at once;
`scrubSeamError` is total and renders both arms, so the ternary bought nothing.

**Why no per-request secret at any keys/sync site:** the only per-request credential this
route holds is `userAccessToken`, and it is **assigned by the very `try` block whose
`catch` is site 4** — so at that site it is necessarily still `undefined`. Passing it
would be a no-op that reads as a mitigation. The route's non-catch reference at
`postProcessKey({ userAccessToken })` reaches `scrubSeamError(message, [args.userAccessToken])`
inside `process-key-client.ts`, which the file's own docblock states and which this plan
did not touch. Sites 1–3 and 5–6 stand over PostgREST errors from the **admin** client,
whose key (`SUPABASE_SERVICE_ROLE_KEY`) is in `SEAM_SECRET_ENV_NAMES` and therefore
covered without an explicit argument.

**Count (comment-stripped, `grep -vE '^\s*(//|\*|/\*)' … | grep -c "scrubSeamError"`):
pre 2 → post 8 = +6.** Predicate on this file: **15 → 0 offences.**

### `src/app/api/strategies/csv-finalize/route.ts` — 6 sites

| # | line | identifier | half | per-request secret? |
|---|---|---|---|---|
| 7 | 532 | `persistError` | errorish | no |
| 8 | 632 | `selectErr` | errorish | no |
| 9 | 677 | `placeholderErr` | errorish | no |
| 10 | 689 | `placeholderThrow` (×3 refs + the `Error` token) | catch + errorish | no |
| 11 | 820 | `updateError` | errorish | no |
| 12 | 1365 | `error` — **TWO references in ONE call** | errorish | no |

```diff
@@ persist_csv_daily_returns @@
       persistError.code,
-      persistError.message,
+      scrubSeamError(persistError),
@@ placeholder pre-check SELECT @@
-        `… placeholder pre-check SELECT failed (non-blocking) […]: ${selectErr.message}`,
+        `… placeholder pre-check SELECT failed (non-blocking) […]: ${scrubSeamError(selectErr)}`,
@@ placeholder upsert @@
-        `… strategy_analytics placeholder upsert failed (non-blocking) […]: ${placeholderErr.message}`,
+        `… strategy_analytics placeholder upsert failed (non-blocking) […]: ${scrubSeamError(placeholderErr)}`,
@@ placeholder upsert THREW @@
-      `… placeholder upsert threw (non-blocking) […]: ${placeholderThrow instanceof Error ? placeholderThrow.message : String(placeholderThrow)}`,
+      `… placeholder upsert threw (non-blocking) […]: ${scrubSeamError(placeholderThrow)}`,
@@ metadata update @@
       updateError.code,
-      updateError.message,
+      scrubSeamError(updateError),
@@ contribution finalize_csv_strategy @@
-      error?.code,
-      error?.message,
+      scrubSeamError(error),
```

**The double-reference site (#12), quoted before and after, with both references
accounted for.** BEFORE:

```ts
console.error(
  `[strategies/csv-finalize contribution] finalize_csv_strategy failed [correlation_id=${args.correlationId}]:`,
  error?.code,
  error?.message,
);
```

AFTER:

```ts
console.error(
  `[strategies/csv-finalize contribution] finalize_csv_strategy failed [correlation_id=${args.correlationId}]:`,
  scrubSeamError(error),
);
```

**BOTH references are wrapped, not one.** And the reason both were reported is worth
recording, because it is a real hole in a reading of the guard rather than a
transcription: **`error?.code` is NOT covered by the `.code` allowlist.** The guard's
safe-property test is `new RegExp('^\\s*\\.\\s*code\\b').test(after)` — it requires a
**literal `.`**, and optional chaining puts a `?` there first. So a site that looks
allowlisted is not. Wrapping only `error?.message` would have left `error?.code`
reported and been an instance fix of a two-reference call — the shape this phase exists
to close. `scrubSeamError` renders the SQLSTATE itself (`code=…`), so the operator loses
nothing; `error` may also legitimately be `null` on the `!isUuid(newStrategyId)` arm,
which the leaf renders totally (`"null"`).

**`.code` was KEPT beside the scrubbed rendering at #7 and #11**, where it is a literal
`.code` and is the five-character SQLSTATE the following branches key off — the guard
allowlists it for exactly that reason ("hiding it would leave an operator unable to
reproduce the decision").

**`placeholderThrow` (#10)** — the whole ternary was replaced by one call, not one arm.
Scrubbing `.message` and leaving `String(placeholderThrow)` (which renders `name: message`)
would have been the same instance-not-class defect at a site on the **CSV finalize flow**,
whose outgoing headers carry `X-User-Access-Token`.

**Why no per-request secret at any csv-finalize site:** `userAccessToken` is declared at
`:1158` inside `unifiedCsvFinalizeHandler` and is **out of scope at all six sites** —
#7 is in `persistDailyReturnsOrErrorResponse`, #8–#10 in
`writeFailedStrategyAnalyticsPlaceholder`, #11 in `applyCsvMetadataUpdate`, #12 in
`contributionCsvFinalizeHandler`. Function boundaries were checked, not assumed. All six
stand over PostgREST errors whose credential (the admin client's service-role key) is in
`SEAM_SECRET_ENV_NAMES`.

**Count: pre 0 → post 7** (the file did not import the module). That is **1 import + 6
call sites**; `grep -c 'import { scrubSeamError } from "@/lib/seam-redaction";'` → **1**,
so the import is present exactly once. Predicate on this file: **15 → 0 offences.**

### `src/app/api/verify-strategy/route.ts` — 3 sites, the PUBLIC anonymous route

| # | line | identifier | half | per-request secret? |
|---|---|---|---|---|
| 13 | 384 | `configErr` | catch + errorish (reported twice) | **YES — `perRequestSecrets`** |
| 14 | 423 | `persistError` | errorish | **YES — `perRequestSecrets`** |
| 15 | 440 | `err` | catch | **YES — `perRequestSecrets`** |

```diff
-    console.error("[verify-strategy] createAdminClient config error:", configErr);
+    console.error(
+      "[verify-strategy] createAdminClient config error:",
+      scrubSeamError(configErr, perRequestSecrets),
+    );
@@
       console.error(
         "[verify-strategy] CT-3 public_token persist failed:",
-        persistError,
+        scrubSeamError(persistError, perRequestSecrets),
       );
@@
-    console.error("[verify-strategy] CT-3 public_token persist threw:", err);
+    console.error(
+      "[verify-strategy] CT-3 public_token persist threw:",
+      scrubSeamError(err, perRequestSecrets),
+    );
```

**`perRequestSecrets` is passed at ALL THREE — there is no exclusion to justify.** The
array (`[userAccessToken, body.api_key, body.api_secret]`) is declared once at `:248` in
the same handler body, and the pre-existing `captureToSentry(…, { secrets: perRequestSecrets })`
calls at `:382`, `:421` and `:438` sit **immediately beside** each of the three console
sites — which is itself the proof that the array was in scope at every one of them the
whole time. **Applying it to the logs is the entire fix; its absence is why this route is
"the worst uncovered route and it is public/anonymous."** The route's own header docblock
already said the `secrets:` argument is *"the ONLY thing standing between undici's
header-inlining (TRAP-1) and a credential leaving our infrastructure"* — and then three
lines below it logged raw.

**Count: pre 0 → post 4** (1 import + 3 sites), post ≥ 3 as required. Predicate on this
file: **3 → 0 offences.**

---

## The plan's real acceptance check — the predicate across all three files

```
$ node /tmp/seam-scan.mjs src/app/api/keys/sync/route.ts \
    src/app/api/strategies/csv-finalize/route.ts \
    src/app/api/verify-strategy/route.ts
---
files scanned:          3
offence RECORDS:        0   (a site can be reported by both halves)
distinct file:line:     0
files with >=1 offence: 0
```

**15 sites closed, measured by the predicate that will become the guard.**

### Partition against plan 08 — verified, not assumed

The roster plan `140.4-10` will derive was reproduced here (3 seam lib modules +
every `src/app/api/**/route.ts` carrying a `SEAM_IMPORT_EDGE`, comment-stripped) and came
to **18 files = 3 lib + 15 routes**, matching the plan's derivation exactly. Running the
two-half predicate over that roster on the **final** tree:

```
[catch]    src/app/api/admin/match/eval/route.ts:185 passes bare `err`
[catch]    src/app/api/admin/match/eval/route.ts:217 passes bare `err`
[catch]    src/app/api/admin/match/eval/route.ts:217 passes bare `err`
[catch]    src/app/api/admin/match/eval/route.ts:245 passes bare `err`
[catch]    src/app/api/admin/match/recompute/route.ts:136 passes bare `err`
[catch]    src/app/api/admin/match/recompute/route.ts:172 passes bare `err`
[catch]    src/app/api/admin/match/recompute/route.ts:172 passes bare `err`
[catch]    src/app/api/admin/match/recompute/route.ts:201 passes bare `err`
[catch]    src/app/api/bridge/route.ts:194 passes bare `err`
[catch]    src/app/api/portfolio-optimizer/route.ts:126 passes bare `err`
[catch]    src/app/api/portfolio-optimizer/route.ts:126 passes bare `err`
[catch]    src/app/api/portfolio-optimizer/route.ts:126 passes bare `err`
[catch]    src/app/api/portfolio-optimizer/route.ts:236 passes bare `err`
[catch]    src/app/api/scenario/optimize/route.ts:198 passes bare `err`
[catch]    src/app/api/scenario/optimize/route.ts:245 passes bare `err`
[catch]    src/app/api/simulator/route.ts:219 passes bare `err`
[errorish] src/app/api/portfolio-optimizer/route.ts:126 passes bare `Error`
---
files scanned:          18
offence RECORDS:        17
distinct file:line:     12
files with >=1 offence: 6
```

**12 distinct sites in 6 files remain, and every one of them is plan `140.4-08`'s.**
15 (this plan) + 12 (plan 08) = **27**, the phase's stated WP-1 tail, with the two sets
disjoint and exhaustive — now measured on the tree rather than partitioned on paper.
(`csv-validate`'s one-hop alias, the 28th, is structurally invisible to this predicate and
is plan `140.4-09`'s.)

---

## Falsifiability Ledger — row M99

**Criterion:** *No caught value reaches a log unscrubbed — tail A* (SEAMRIM-06).
**Mutation shape assigned:** second member of the class.

**The class** is "a seam-route console site on `verify-strategy` that scrubs", and it has
three members. **Member one is the `configErr` site** — the review's headline instance,
the `createAdminClient` config fault. **I mutated the SECOND member: the `persistError`
site** (`[verify-strategy] CT-3 public_token persist failed:`), by unwrapping it back to a
bare reference:

```diff
       console.error(
         "[verify-strategy] CT-3 public_token persist failed:",
-        scrubSeamError(persistError, perRequestSecrets),
+        persistError,
       );
```

One mutation. **Semantic**, to **PRODUCTION** source. Not a syntax error, not a type
error (`npx tsc --noEmit` exit **0** with the mutation applied — recorded deliberately, so
"the suite was green" cannot be attributed to a compile failure), not a test edit.

### Part 1 — OBSERVED NOW: GREEN. No guard in the tree sees this today.

```
FULL suite, mutation APPLIED:
 Test Files  716 passed | 19 skipped (735)
      Tests  9823 passed | 287 skipped (10110)

FULL suite, mutation REVERTED (identical tree to the commits):
 Test Files  716 passed | 19 skipped (735)
      Tests  9823 passed | 287 skipped (10110)
```

**Byte-identical. I did not observe a RED, and I am not claiming one.** The reason is
structural and was predicted by the plan: `src/app/api/verify-strategy/route.ts` is **not
a member of `SEAM_FILES`** until plan `140.4-10` derives the roster, so the guard's
`it.each(SEAM_FILES)` never opens the file. **No guard in the tree sees this today; the
receipt for this class is the derived roster in plan `140.4-10`.**

This is the same shape as the programme's one GREEN mutation in run 1 — a property that is
true of the code but held by nothing — and recording it as such is the point of the row.

### Part 2 — OBSERVED against the WIDENED predicate: the site is reported.

The same predicate, run over the derived 18-file roster **with the mutation applied**:

```
$ node /tmp/seam-scan.mjs <derived roster, 18 files> | grep verify-strategy
[errorish] src/app/api/verify-strategy/route.ts:431 passes bare `persistError`

$ node /tmp/seam-scan.mjs src/app/api/verify-strategy/route.ts
[errorish] src/app/api/verify-strategy/route.ts:431 passes bare `persistError`
---
files scanned:          1
offence RECORDS:        1
distinct file:line:     1
files with >=1 offence: 1
```

Against the same file with the mutation reverted: **0 offence records.**

The guard renders that offence list into its assertion message as
`` `${offences.join("; ")}. ${FAILURE_REASON}` ``, where `FAILURE_REASON` is the shipped
text at `seam-log-coverage.test.ts:235-244`, which ends: *"Wrap it: `scrubSeamError(err)`
from `@/lib/seam-redaction`, plus any PER-REQUEST secret this call site holds as the
second argument. Do NOT answer this by dropping `err` instead…"*

⚠️ **Phrasing, per CONTEXT §8 — branch protection on `main` is OFF, so every gate here is
advisory at merge.** Once plan `140.4-10` lands the derived roster, this guard **would
have caught** the mutation. It **did not stop** anything, and nothing in this run stopped
anything.

### Revert receipt

```
$ git status --porcelain
(clean — all three commits made, no residual mutation)
$ node /tmp/seam-scan.mjs src/app/api/verify-strategy/route.ts
files scanned: 1 · offence RECORDS: 0 · distinct file:line: 0
```

**Ledger status: M99 — recorded in two parts. Part 1 observed GREEN with the reason
stated; Part 2 observed reported under the widened predicate. This is a DEFERRED
OBLIGATION collected at the phase gate**, which `140.4-VALIDATION.md` provides for
explicitly ("Rows M99 and M100 are recorded in BOTH parts … because neither can redden
until plan `140.4-10` lands"). It must **not** be counted as an observed-RED row.

---

## Verification

| Check | Result |
|---|---|
| Predicate, both halves, 3 owned files | **0 offences** (from 26 records / 15 sites) |
| `npx tsc --noEmit` | exit **0** |
| `npx vitest run` (FULL, Node 25) | **9823 passed \| 287 skipped** (716 files passed \| 19 skipped) |
| `npm run lint` | **0 errors**, 1 pre-existing warning (`EquityChart.tsx:1119` — untouched) |
| `npx vitest run src/app/api/keys/sync/` | 40 passed |
| `npx vitest run src/app/api/strategies/csv-finalize/` | 9 passed |
| csv-finalize regression trio (`after-failloud`, `rpc`, `c14-regression`) | 48 passed \| 6 skipped |
| `npx vitest run src/app/api/verify-strategy/` | 27 passed |
| `src/lib/seam-log-coverage.test.ts` in the diff | **absent** — `git diff --name-only 21afb828..HEAD` is 4 files, none of them the guard |
| Python / `supabase/migrations/` / `.planning/` staged | **zero** of each |
| `package.json` / `package-lock.json` in the diff | **absent** — zero packages installed |

Baseline note: CONTEXT §8's `9792 passed | 287 skipped` (715 files) was measured at the
**phase base** `a77d607e`. This branch is based on `21afb828`, which already carries plans
01–06, so the branch baseline is **9823 | 287 across 735 files** — measured on this tree,
above the phase baseline, and unchanged by this plan (which adds no tests).

### ⚠️ Node 22 (CI parity) — NOT clean, and I am reporting it rather than rounding it

`PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` (v22.22.1) **failed**:
`3 failed | 9820 passed | 287 skipped`. A second Node 22 run failed a **different and
larger** set. The union across both runs:

`gdpr-export.test.ts` · `contracts/contracts-registry.test.ts` ·
`AllocationsTabs.scenario-state-preservation.test.tsx` · `ScenarioComposer.save.test.tsx` ·
`FactsheetBody.basis.test.tsx` · `FactsheetBody.guard04-no-bleed.test.tsx`

**Evidence that this is contention flake and not a regression from this plan:**

1. **Re-run of exactly those six files under Node 22 with `--no-file-parallelism`:
   `6 passed · 179 passed | 1 skipped`.** All green in isolation.
2. **The failing set is non-deterministic across two consecutive runs of the same tree** —
   the signature of a flake, not of a semantic defect.
3. **None of the six imports any file this plan touched.** `contracts-registry.test.ts`
   matches a `grep` for `csv-finalize` only because that path is a string in its registry
   table; its failing case (`[B25] resolves every quantalyze rule to "error"`) instantiates
   ESLint and resolves config for `src/lib/visibility.ts` and the frozen-chart islands —
   nothing in this diff.
4. The Node 22 run's `environment` time was **2548 s** against **473 s** on Node 25 in the
   same worktree — the run was heavily resource-contended.
5. This plan's change is the wrapping of `console.*` arguments. There is no mechanism by
   which it reaches a GDPR export size cap, a factsheet chart, or an ESLint config resolve.

**I could not close this**, and it is carried below rather than declared discharged. It
should be re-run on an unloaded machine (or read off CI, which shards) before the phase
gate. See § OPEN.

---

## Deviations from Plan

**One, and it is a test edit forced by the fix — recorded under TRAP-9.**

**[Rule 1 — Test would have broken on correct code] `keys/sync/route.test.ts`'s
stamp-failure pin asserted the RAW object**
- **Found during:** Task 1
- **Issue:** the pin asserted `expect.objectContaining({ message: "stamp write denied" })`
  as the second console argument. Once `stampErr` is scrubbed, that argument is a
  **string**, so the assertion fails on correct code.
- **TRAP-9 — the test was not destroyed; the `it(` is unchanged and quoted verbatim:**
  `it("[Finding-M] logs a failed membership_unknown stamp on the fail-closed path", async () => {`
  — same name, same test, one assertion argument changed.
- **Old → new:**
  ```diff
  -        expect.objectContaining({ message: "stamp write denied" }),
  +        expect.stringContaining("stamp write denied"),
  ```
- **Why the replacement is STRONGER, not weaker:** the old form pinned only *that
  something error-shaped was logged*. The new form pins **two** properties: that the value
  went through the scrub (it is a string, not the raw PostgREST object), **and that the
  diagnosis SURVIVED it**. That second half is precisely the A-10 non-drop check — a
  future "fix" that answered a scrub finding by dropping the value would score clean under
  the predicate and **redden here**. The predicate cannot see dropping; this assertion can.
- **Files modified:** `src/app/api/keys/sync/route.test.ts`
- **Commit:** `8f335fe4`

No other deviation. No architectural change, no auth gate, no package installed, no
control flow / status code / response body touched on any of the three routes (confirmed
by reading each `git diff` in full — the diffs contain no `NextResponse`, status literal,
`if` or `return` change).

---

## Known Stubs

None. No placeholder, no hardcoded empty value, no unwired component.

---

## Threat Flags

None. This plan introduces no new network endpoint, auth path, file-access pattern or
schema change. It **removes** surface from the register's `T-140.4-07-01` and
`T-140.4-07-02`.

Dispositions from the plan's `<threat_model>`, each with its receipt:

| Threat ID | Disposition | Receipt |
|---|---|---|
| T-140.4-07-01 | mitigate | 15/15 sites scrubbed; predicate 0 offences. **Row 3 — partial by construction**, stated above |
| T-140.4-07-02 | mitigate | `perRequestSecrets` applied at all three `verify-strategy` console sites |
| T-140.4-07-03 (dropping the value) | avoid | every site listed above with its identifier still reaching the line, wrapped; plus the strengthened test assertion, which is the only mechanism in the tree that can catch a drop |
| T-140.4-07-04 (over-redaction) | avoid | no site hand-rolls a replacement; `SEAM_PRESERVE_TOKENS` + `MIN_REDACTABLE_SECRET_LENGTH` are untouched. `keys/sync/route.test.ts`'s existing pin `expect(logged).toContain("ECONNREFUSED")` still passes |
| T-140.4-07-05 (class-not-instance) | avoid | the `csv-finalize:1365` double-reference and the two 3-reference ternaries are each fixed as a whole; the predicate confirms zero residual |
| T-140.4-07-06 (roster widened in the same commit) | avoid | `seam-log-coverage.test.ts` absent from `git diff --name-only 21afb828..HEAD` |
| T-140.4-07-SC | mitigate | zero packages installed; `package.json`/`package-lock.json` absent from the diff |

---

## OPEN — carried, not closed

1. **⚠️ The Node 22 full-suite run is not green in this worktree.** Evidence above points
   to contention flake (all named files pass in isolation under Node 22; the failing set
   was non-deterministic; none of the files relates to this diff). **I did not obtain a
   clean Node 22 full run and am not claiming one.** Re-run on an unloaded machine or read
   it off CI before the phase gate.

2. **`csv-finalize`'s `computation_error` reason string is NOT scrubbed, by scope.**
   `persistDailyReturnsOrErrorResponse` builds
   `` `persist_csv_daily_returns failed: ${persistError.message ?? "(no message)"}` `` and
   persists it to `strategy_analytics.computation_error`, which **is rendered to users** —
   `SyncPreviewStep.tsx` (*"Details: {computation_error}"*) and
   `portfolios/[id]/page.tsx:441` (`<StaleWarning error={analytics.computation_error} />`).
   That is the **same source value** as scrubbed site #7, reaching a **persisted and
   user-visible** sink rather than a console. It is outside this plan's declared scope —
   the plan restricts this file to logging edits and fences body/persisted surfaces to
   plan `140.4-09` and TS-24 / Phase 145 — and scrubbing it changes what is written to the
   database, which is a behaviour change this plan is explicitly forbidden from making.
   **It is a real residual and should be assigned an owner.** It is invisible to the seam
   log predicate, so no guard in the phase will surface it.

3. **The row-1 artefact for this remedy does not exist yet.** Until `140.4-10` derives
   `SEAM_FILES`, nothing prevents a 16th site being written into these three files
   tomorrow. That is the definition of *partial by construction* and is why this plan
   deliberately lands **before** the derivation, not with it.

---

## Self-Check: PASSED

Files claimed modified — all present and non-empty:

```
FOUND: src/app/api/keys/sync/route.ts
FOUND: src/app/api/keys/sync/route.test.ts
FOUND: src/app/api/strategies/csv-finalize/route.ts
FOUND: src/app/api/verify-strategy/route.ts
```

Commits claimed — all present on `worktree-agent-a2eafaf1a59cb287b`:

```
FOUND: 8f335fe4  fix(140.4-07): scrub all 6 caught/error-shaped values keys/sync logs
FOUND: f3253a6e  fix(140.4-07): scrub all 6 error-shaped values csv-finalize logs
FOUND: 5fed5d77  fix(140.4-07): apply verify-strategy's own perRequestSecrets to its logs
```

`git diff --name-only 21afb828..HEAD` returns exactly those four files and nothing else.
