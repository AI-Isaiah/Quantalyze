---
phase: 140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open
plan: 02
type: execute
wave: 2
depends_on: ["140.4-01"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx
autonomous: true
requirements: [SEAMRIM-02]

must_haves:
  truths:
    - "A failed read and a genuinely empty account no longer render the same sentence"
    - "The wizard never tells a user a number about their account that it did not measure"
    - "A read failure routes into the escalation the composite arm already uses, not into a terminal gate verdict"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "error bound on all seven single-key gate reads, throwing into heavyFetchErrorsRef"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx"
      provides: "a runtime receipt that read-failed does not render as measured-zero"
  key_links:
    - from: "the seven Promise.all members"
      to: "heavyFetchErrorsRef → failPolling()"
      via: "an .error on a heavy read throws, exactly as the composite arm 200 lines above already does"
      pattern: "heavyFetchErrorsRef"
---

<objective>
Close the **fabricated-observation** half of C-3 — CONTEXT §4, **CRITICAL+**, *demonstrated by
rendering*: read-failed and genuinely-empty produce **byte-identical DOM**:

> *"We found only 0 filled trade(s) on this key."* — `src/lib/wizardErrors.ts:1200`

`SyncPreviewStep.tsx:1053-1114` array-destructures **seven** `Promise.all` members and reads `.error`
on **zero** of them. Verified first-hand at plan time (`140.4-PATTERNS.md` §1a re-derived and I
re-read the block):

| # | member | destructured as | `.error` read? |
|---|---|---|---|
| 1 | `strategy_analytics` | `{ data: analytics }` | ✗ |
| 2 | `trades` count | `{ count: tradeCount }` | ✗ |
| 3 | `trades` earliest | `{ data: earliest }` | ✗ |
| 4 | `trades` latest | `{ data: latest }` | ✗ |
| 5 | `trades` sample | `{ data: sample }` | ✗ |
| 6 | `csv_daily_returns` count | `{ count: csvRowCount }` | ✗ |
| 7 | `api_keys` (ternary) | `keyRowResult` whole, `.data` at `:1118` | ✗ |

**7 of 7.** The file's own comment at `:699-701` *acknowledges* it: *"an RLS denial on `trades`
instead drops tradeCount to 0 via `?? 0` and the gate fails INSUFFICIENT_TRADES — a terminal,
loop-stopping outcome."* `onTerminal` returns `"done"`, so the poll stops and there is no
self-recovery.

**The escalation sink already exists and this plan does not build one.** The composite arm **200 lines
above in the same function** (`:848-867`) checks `.error` on all four of its reads and throws; the
catch at `:1191` increments `heavyFetchErrorsRef` and, past `MAX_CONSECUTIVE_POLL_ERRORS = 3`
(`:184`), calls `failPolling()` → a **recoverable** `SYNC_FAILED` envelope with an exit affordance.
`140.4-PATTERNS.md` §9 names this the **exact, in-file** analog. Copy it.

## Coverage-law classification (CONTEXT §2 — binding)

- **Task 1 — ROW 3 (per-site edit), and it is therefore PARTIAL BY CONSTRUCTION.** Measured coverage
  for row 3 in this programme: 1/8 · 2/56 · **0/32**. It is acceptable here only because the set is
  **bounded and fully enumerated above (exactly 7 members in one `Promise.all`)** and because the
  row-1 backstop for the publish-side harm landed in plan `140.4-01` (the gate now refuses rather than
  passing on an unreadable span).
- **The repo-wide unchecked-read backlog is NOT closed by this plan and this plan does not claim it.**
  Plan `140.4-14` adds the only row-1 mechanism available for it (an edit-time ESLint ratchet scoped
  to the files this phase converts), which guarantees the number can only go **down**.

## The predicate and scope fence (CONTEXT §5c — MANDATORY: publish a predicate, do not quote a number)

Three honest repo-wide measurements disagree — **32** (synthesis, `src/app/api/**` only), **73**
(mapper, repo-wide object-destructure that never reads `.error`), **139** (researcher, AST over all
372 postgrest sites) — because they measured different things. **This plan uses none of them.**

- **Predicate P:** an awaited postgrest result inside `SyncPreviewStep`'s **single-key terminal arm**
  whose value feeds `checkStrategyGate` or the rendered snapshot, and whose `error` is never bound.
- **Scope fence:** the one `Promise.all` at `SyncPreviewStep.tsx:1053-1114` (single-key branch only —
  the composite branch above it is already correct and must not be touched).
- **Derived from P over the fence:** **7 of 7 unchecked.**

⚠️ **The mapper's own object-destructure predicate returns ZERO here**, because this block
**array**-destructures. That is the class/instance shape in miniature — object form closed, array form
open, in the same file. Any census you run must cover both shapes or it will tell you this plan has
nothing to do.

## Why NOT `.throwOnError()` at this site

`140.4-RESEARCH.md` Item 1b makes the general case for `.throwOnError()` (it changes the *awaited
type*, so `tsc` enforces that the `?? 0` fabrication is gone). **Rejected for this site**, on two
measured grounds stated in that same section:
1. Member 7 is a **ternary** whose else-branch is `Promise.resolve({ data: null as {exchange?: string} | null })`.
   `.throwOnError()` on the true branch alone produces a union the array-destructure cannot narrow.
2. `count` remains `number | null` even on the success type, so `tradeCount ?? 0` **still type-checks**
   after the change — a checked count is not free and needs an explicit null refusal anyway.

CLAUDE.md Rule 11 (conform to the codebase) settles it: the correct shape is 200 lines above, in the
same function, already wired to the escalation sink. Record this decision in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-CONTEXT.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-RESEARCH.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-PATTERNS.md
@.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-01-SUMMARY.md

<standing_constraints>
Binding on every task. You are a fresh agent with no memory of this phase's discussion.

- **Verify with the FULL suite, never a scoped path.** Baselines (CONTEXT §8): `npx vitest run` →
  **9792 passed | 287 skipped** (715 files) · pytest **4778 / 96** · `tsc --noEmit` exit 0 ·
  `npm run lint` 0 errors (1 pre-existing warning). Plan `140.4-01` landed before this one and may
  have moved the vitest counts **up**; re-establish the baseline on your branch BEFORE editing.
- **`vi.spyOn` + `vi.restoreAllMocks()`. NEVER `vi.stubGlobal`** — DEF-16-1, this repo's CI-only
  failure cause (CI Node 22 vs local Node 25).
- **Zero new packages.**
- `.planning/` is gitignored with no git backup. Never `git add .planning`. Never `gsd-sdk query state.*`.
  Never edit `.planning/STATE.md` or `.planning/ROADMAP.md`.
- **Branch protection is OFF** ⇒ *"would have caught"*, never *"did stop"*.
- **grep is LINE-BASED** (DEF-16-2). **`grep -h` with a downstream filename filter is PROHIBITED.**
  Use `grep -F` for `[id]` paths. Comment-strip before counting — seven executors in this programme
  were bitten by their own comments matching their own greps.
- **Every pre-measured value here was taken on the untouched tree at HEAD `a77d607e`.** Re-run the
  stated command first; if it disagrees, STOP and report.
</standing_constraints>

<read_first>
- `SyncPreviewStep.tsx:840-875` — **THE ANALOG, in this same file and this same function.** Four
  `if (xRes.error) throw new Error("composite … read failed: " + …)` arms. Note the message shape:
  it names WHICH read failed and interpolates `error.message`.
- `SyncPreviewStep.tsx:695-720` — `heavyFetchErrorsRef` and `failPolling()`, and the comment at
  `:699-701` that *acknowledges the defect this plan fixes*. Quote it in the SUMMARY.
- `SyncPreviewStep.tsx:1053-1140` — the seven-member `Promise.all`, the `keyRowResult.data` read, the
  `deriveDetectedMarkets(sample ?? [])` call, and the `checkStrategyGate({...})` input object with its
  `?? 0` / `?: null` coercions.
- `SyncPreviewStep.tsx:1190-1210` — the `catch (heavyErr)` arm, `heavyFetchErrorsRef.current += 1`,
  and the `MAX_CONSECUTIVE_POLL_ERRORS` escalation. **This is where your throws land.** Note it
  tolerates the first failure and returns `"repoll"`.
- `SyncPreviewStep.tsx:184` — `const MAX_CONSECUTIVE_POLL_ERRORS = 3;`
- `SyncPreviewStep.render.test.tsx:120-230` — `installSupabaseMock`, its `PollOutcome` union
  (`row` | `supabaseError` | `throw`) and its `heavyOutcome: "empty" | "throw"` parameter. **The heavy
  fan-out currently resolves `{ data, count: 0, error: null }`** — that is the hook you extend.
- `SyncPreviewStep.poll-disjointness.runtime.test.tsx` — the runtime-pin file `140.4-RESEARCH.md`
  credits as the strongest guard in the range. Copy its docblock discipline: state what the pin does
  **and what it does not prove**.
- `src/lib/wizardErrors.ts:1200` — the fabricated sentence template
  `` `We found only ${context.trades} filled trade(s) on this key. ` ``. **READ ONLY — do not edit
  `wizardErrors.ts` in this plan.**
- `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-01-SUMMARY.md`
  — plan `140.4-01` made `checkStrategyGate` **partial**: it now throws `StrategyGateUnevaluableError`
  when trades exist and the span is unreadable. That throw lands in the same `catch (heavyErr)` arm.
  Your checked reads should make it unreachable from this consumer; the case that proves it is still
  worth writing.
</read_first>

<interfaces>
**The shape to copy, verbatim from `:848-867`:** keep each `Promise.all` member's **whole result**,
check `.error`, throw an `Error` naming which read failed, and only then bind the value.

Concretely: replace the array destructure of seven *fields* with an array destructure of seven
*results*, then a checked-binding block, then the existing consumers (`deriveDetectedMarkets`,
`keyRow`, the `checkStrategyGate` input) unchanged.

**Counts are part of the class.** `tradeCount` and `csvRowCount` are `head: true` exact counts. A
`null` count with **no** error is as unrepresentable as an error — and `?? 0` on it is exactly the
fabrication in the finding. Refuse both.

**Member 7 is a ternary and stays one.** When `apiKeyId` is falsy the else-branch resolves a synthetic
`{ data: null }` with no `error` field — that is not a read failure and must not throw. Check
`.error` only when it is present. Say so in the comment; this is the trap that turns a fix into a
regression for every keyless draft.

**Out of scope, stated so it is not smuggled in:**
- The **composite** arm above (`:806-870`) — already correct; do not "unify" the two arms.
- `src/lib/wizardErrors.ts` — plan `140.4-03` owns it this wave (TRAP-8), and plans `140.4-11` /
  `140.4-12` own the render affordances and the code vocabulary.
- The destructive-control affordance on the resulting error state — plan `140.4-11` (C-4).
- Any change to `MAX_CONSECUTIVE_POLL_ERRORS`, the poll cadence, or the stall backstop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: all seven single-key gate reads bind `error`, copying the composite arm 200 lines above</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <read_first>
- `SyncPreviewStep.tsx:840-875` (the analog), `:695-720` (the sink), `:1053-1140` (the subject),
  `:1190-1210` (the catch).
  </read_first>
  <behavior>
- A PostgREST error on the `trades` **count** member → throws; the catch increments
  `heavyFetchErrorsRef`; **no** gate verdict is computed and no `INSUFFICIENT_TRADES` state is set on
  that tick.
- A `null` count with `error: null` on the same member → also throws (a count read that returned no
  count is unrepresentable; `?? 0` is the fabrication).
- A PostgREST error on `earliest` **or** `latest` → throws. (With plan `140.4-01` landed, letting
  these through would instead reach the gate's refusal — both are fail-closed, and the case names
  which one fires.)
- A PostgREST error on `strategy_analytics`, on the `sample` read, or on `csv_daily_returns` → throws.
- `apiKeyId === null` → member 7's synthetic `{ data: null }` does **not** throw, and the flow
  proceeds exactly as today. **Load-bearing anti-regression.**
- A genuinely empty account (all reads succeed, `count: 0`) → **unchanged**: still
  `INSUFFICIENT_TRADES`, still the existing sentence. This is the case that proves the fix
  distinguishes the two states rather than replacing one blanket answer with another.
  </behavior>
  <action>
Convert the seven-member array destructure from fields to **results** and add a checked-binding block
immediately after, in the shape of `:848-867`: one `if (xRes.error) throw new Error(...)` per member,
each message naming the read (`"single-key trades count read failed: …"`, etc.) and interpolating
`error.message`. Then bind the values the downstream code already expects, so
`deriveDetectedMarkets`, `keyRow` and the `checkStrategyGate` input object are **unchanged**.

Add the two count refusals (`count === null` with no error). Keep the ternary member's shape and check
its `error` only when the property exists.

Delete the `?? 0` / `?? []` coercions **only** where the value is now provably non-null; leave any that
still guard a genuinely optional value and name each survivor in the SUMMARY with its reason.

Update the acknowledging comment at `:699-701`: it currently describes the RLS-denial-drops-to-zero
behaviour as the status quo. Rewrite it to describe what is now true, and **do not delete the
sentence about the escalation invariant** — it explains why the ref never needs a reset.

⚠️ Anchor everything by **code text**, not line number. This file was edited by five 140.3 plans and
P2 measured 19 citations checked / 5 wrong.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/"</automated>
  </verify>
  <acceptance_criteria>
- **Pre-value, re-measure before you start.** Comment-stripped count of `.error` reads inside the
  single-key terminal arm. Establish it with:
  `awk 'NR>=1040 && NR<=1150' "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx" | grep -vE '^\s*(//|\*)' | grep -c '\.error'`
  → **0** today. After the change it is **≥ 7**. (Line window re-derive it if the file has moved:
  the window is the block from `const [` … `await Promise.all([` through the `checkStrategyGate({` call.)
- Whole-file comment-stripped `?? 0` count: record pre- and post-values; post must be **strictly
  lower**, and every survivor is named with its reason in the SUMMARY.
- `grep -c "composite analytics read failed" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"`
  → **1**, unchanged. The composite arm was not touched or "unified".
- The keyless-draft case (`apiKeyId === null`) exists and is green. **This criterion cannot be
  satisfied by a broken implementation that throws on every tick.**
- The genuinely-empty-account case still renders the existing sentence and still reaches
  `INSUFFICIENT_TRADES`.
- `npx tsc --noEmit` exit 0.
  </acceptance_criteria>
  <done>
All seven members bind `error` (and both counts refuse a null count), the throws land in the existing
`heavyFetchErrorsRef` escalation, the keyless-draft path and the genuinely-empty path are unchanged,
and the composite arm was not touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: the runtime receipt — read-failed and genuinely-empty must not render the same DOM</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx</files>
  <read_first>
- `SyncPreviewStep.render.test.tsx:120-230` — `installSupabaseMock`, `PollOutcome`, `heavyOutcome`,
  and the `thenableEmpty` helper that currently resolves `{ data, count: 0, error: null }` for the
  heavy fan-out.
- `SyncPreviewStep.poll-disjointness.runtime.test.tsx:1-70` — the docblock discipline to copy,
  including its **"WHAT THIS PIN DOES NOT PROVE"** section and its `vi.spyOn` / `restoreAllMocks`
  warning.
- `src/lib/wizardErrors.ts:1200` (READ ONLY) — the exact fabricated sentence to assert against.
  </read_first>
  <action>
CONTEXT §3 is binding: **a grep is not a receipt.** Build a *runtime* receipt.

Create `SyncPreviewStep.readfailure.runtime.test.tsx` (`/** @vitest-environment jsdom */`), modelled
on the poll-disjointness runtime pin. Extend the supabase harness with a per-member read-failure
injection — either by adding a `heavyOutcome: "readError"` mode to `installSupabaseMock` in
`SyncPreviewStep.render.test.tsx` and exporting it, **or** by building a local harness in the new file.
Prefer the local harness: `seam-poll-disjointness.pin.test.ts:196-200` states the house rule —
*"a test file must not import another test file, and two independent scanners that agree are worth
more than one shared helper whose single bug blinds both tiers."* If you instead extend the shared
mock, keep every existing caller's behaviour byte-identical and say so.

Assert, with the component actually rendered:
1. **The fabrication is gone.** With the `trades` count member resolving
   `{ data: null, count: null, error: { code: "57014", message: "canceling statement due to statement timeout" } }`
   and every other member healthy, the DOM does **not** contain the substring
   `filled trade(s) on this key`.
2. **The positive counterpart (mandatory — `140.4-PATTERNS.md` §9 names
   `critical-regressions.test.ts:131-144` as the template).** With **all** members healthy and a
   genuine `count: 0`, the DOM **does** contain that same substring. Without this half the first
   assertion is satisfied by a component that renders nothing at all, which is how M-9 measured a
   guard going vacuous.
3. **The escalation is reached, not just the absence.** After `MAX_CONSECUTIVE_POLL_ERRORS` (3)
   consecutive read-failure ticks, the recoverable `SYNC_FAILED` surface renders. Drive the fake
   timers; do not assert on internal refs.
4. **Distinguishability, stated as the property:** capture the DOM text for the read-failure run and
   for the genuinely-empty run and assert they are **not equal**. This is the assertion that names
   the finding — *byte-identical DOM* — rather than one of its instances.

Write the docblock in the analog's style, including an explicit **"what this does not prove"**
paragraph (jsdom under fake timers, one component, one injected member).

⚠️ `vi.spyOn` + `vi.restoreAllMocks()` in `afterEach`. **Never `vi.stubGlobal`.**
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
- The new file contains **both polarities**: an assertion that the sentence is ABSENT under a read
  failure and an assertion that it is PRESENT under a genuine zero. State both test names in the SUMMARY.
- **The absent-half fails on the pre-Task-1 tree.** Stash Task 1's production change, run the new
  file, paste the RED output, restore. If it passes without Task 1, the harness is not injecting the
  error and the receipt is worthless.
- `grep -c "vi.stubGlobal" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx"` → **0**.
- `grep -c "restoreAllMocks" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.readfailure.runtime.test.tsx"` → **≥ 1**.
- If `SyncPreviewStep.render.test.tsx` was edited, every pre-existing test in it is still green and
  `git diff -U0` on that file shows **no removed `it(` lines** (TRAP-9), or each removal is listed
  with its strictly stronger replacement.
- The docblock contains an explicit "does not prove" section.
  </acceptance_criteria>
  <done>
A rendered runtime test proves read-failed and genuinely-empty produce different DOM, both polarities
are asserted, and the absent-half was observed RED without the production fix.
  </done>
</task>

<task type="auto">
  <name>Task 3: falsify the checked read — Falsifiability Ledger row M92</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <read_first>
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md` — the
  `## Falsifiability Ledger` rules block and rows M86–M89 for format and standard of evidence.
  **Read it; do not edit it.** Highest existing row in the programme: **M89**. Plan `140.4-01` takes
  M90–M91; this plan's row is **M92**.
  </read_first>
  <action>
One mutation, **semantic**, to **PRODUCTION** source; never a syntax or type error; never a test edit.
Record it in this plan's SUMMARY under `## Falsifiability Ledger — row M92`.

**M92 — the checked read is LIVE at the SECOND member of the class, not just the first.**
The class is "a single-key gate read that binds `error`". CONTEXT: **mutate the SECOND member.** The
`trades` **count** member is the one the runtime receipt drives, so it is member one for this purpose.
Mutate member two — the **`earliest`** read: remove its `.error` check and restore the coercion, i.e.
re-create the defect at exactly one member while the other six stay fixed.

Must redden: a case that drives a read failure on the `earliest` member specifically. **If no such
case exists, this mutation will pass and you must add the case before recording the row** — that
absence is itself the finding (a class fixed at one member and pinned at one member is an
instance-fix wearing a class-fix's clothes, which is precisely what this programme keeps
rediscovering).

Record: the exact diff, the command, the pasted RED output with the failing test name and assertion
text, at least one named GREEN-side observation (e.g. the `trades`-count case stays red/green as
expected and the keyless-draft case stays green), and `git status` clean after revert. Branch
protection is OFF ⇒ *"would have caught"*, never *"did stop"*.
  </action>
  <verify>
    <automated>npx vitest run && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- Row M92 recorded with pasted RED output including the failing test name and assertion text.
- The row states explicitly that the mutation targeted the **second** member of the class and names
  the first.
- The row names at least one thing that stayed GREEN under the mutation.
- `git status --porcelain` after revert shows no residual mutation.
- **FULL suite green:** `npx vitest run` at or above the branch baseline (state exact numbers),
  `npx tsc --noEmit` exit 0, `npm run lint` 0 errors.
- CI-parity run performed: `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green.
  </acceptance_criteria>
  <done>
M92 is recorded with observed RED at the second member of the class, the tree is restored, and the
full suite is green under both Node 25 and Node 22.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Postgres/RLS → browser wizard | supabase-js resolves PostgREST failures **as values**; an unchecked destructure is indistinguishable from real data |
| wizard render → the user's belief about their own account | A fabricated measurement about someone's money data is the product's core trust claim, inverted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.4-02-01 | **Spoofing (of a measurement)** | the seven-member `Promise.all` | mitigate | Every member binds `error` and both counts refuse a null count; failures throw into the existing `heavyFetchErrorsRef` escalation. Row 3, **partial by construction**, bounded at exactly 7 enumerated members. Falsified by M92 |
| T-140.4-02-02 | **Denial of service (self-inflicted)** | throwing on the keyless-draft ternary member | avoid | Member 7's synthetic `{data:null}` has no `error` property; the check is conditional and a named case pins the keyless path green |
| T-140.4-02-03 | Repudiation | a terminal gate verdict that stops the poll with no self-recovery | mitigate | A read failure now routes to the recoverable `SYNC_FAILED` surface with an exit affordance, via the sink that already exists |
| T-140.4-02-04 | Tampering (guard decay) | a receipt that passes on a broken implementation | mitigate | The runtime test asserts **both** polarities plus a DOM-inequality assertion, and the absent-half was observed RED without the production fix |
| T-140.4-02-05 | Information disclosure | a PostgREST error message reaching the rendered surface | avoid | Throw messages are consumed by the catch at `:1191` and logged; the user-facing surface is the existing `SYNC_FAILED` envelope copy, unchanged. No new sentence is authored |
| T-140.4-02-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero packages installed** (`140.4-RESEARCH.md` §Package Legitimacy Audit) |
</threat_model>

<verification>
- `npx vitest run` (FULL, never scoped) at or above the branch baseline; state exact numbers.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` green (CI is Node 22; local is Node 25).
- Zero Python files, zero `supabase/migrations/`, zero `.planning/` paths staged.
- `src/lib/wizardErrors.ts` is **not** in the diff (TRAP-8 — owned by `140.4-03` this phase).
- The composite arm's four `read failed` throws are byte-unchanged.
</verification>

<success_criteria>
- All seven single-key gate reads bind `error`; both counts refuse a null count.
- A read failure and a genuinely empty account render **different** DOM, proven by a rendered runtime
  test asserting both polarities.
- The keyless-draft path and the genuinely-empty path are behaviourally unchanged.
- M92 records observed RED at the **second** member of the class.
</success_criteria>

<output>
Create `.planning/phases/140.4-seamrim-close-the-wizard-client-rim-the-core-fix-left-open/140.4-02-SUMMARY.md`.

Record: the seven members with the check added to each; the pre/post `?? 0` counts and every survivor
with its reason; the RED output of the runtime receipt against the pre-fix tree; the decision record
for rejecting `.throwOnError()` at this site with both measured reasons; the rewritten `:699-701`
comment verbatim; whether the shared `installSupabaseMock` was extended or a local harness was built,
and why; and the `## Falsifiability Ledger — row M92` section.
</output>
