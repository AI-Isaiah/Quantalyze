# Phase 140.4 — SEAMRIM · PATTERN MAP

**Mapped:** 2026-07-28 · HEAD `56fb7167` · branch `feat/v1.16-production-resilience`
**Working tree at map time:** ` M TODOS.md` + `?? analytics-service/scripts/nautilus_factsheet.py` (as expected; nothing else touched — this map is read-only)

**Sources read:** `140.4-CONTEXT.md` (binding), `.planning/reviews/140-SYNTHESIS.md` §1–§4 (requirements)

**What this document is.** Not a summary of the review. It is **eight measured population censuses** and the **in-repo analogs** the executor copies from. Every number below has a re-runnable predicate beside it. Where my measurement disagrees with the review, both numbers are given with the predicate that produced each.

> ⚠️ **Read §0 first.** Three of the review's counts came from line-based greps and are short. One is short *in the exact way DEF-16-2 predicts*.

---

## 0. HEADLINE — where the review's own counts are wrong, and why

| # | review says | measured | predicate difference | consequence for the plan |
|---|---|---|---|---|
| **A** | 9 raw `HTTPException(status_code=5xx)` | **11** | review used a **line-based** grep; 2 sites put `status_code=500/503` on a **continuation line** | a per-site remedy sized at 9 leaves 2 open — the same shape as the 3-of-5 log sites |
| **B** | 6 hand-rolled non-announcing error surfaces | **27** | review scoped to seam-adjacent components; the class is app-wide | a 6-item roster is **row 2, partial by construction**, at 22% coverage |
| **C** | 32 unchecked Supabase reads | **32 confirmed** for `src/app/api/**`, **73 repo-wide** | review's scope fence was `src/app/api/**` and is not stated as one | remedy sized at 32 covers 44% of the true class |
| **D** | "a new consumer of a seam wrapper matches **neither** needle" | **false as stated** | `SEAM_IMPORT_EDGE` (`seam-poll-disjointness.pin.test.ts:163-165`) matches all three wrapper modules and already derives the exact 15 | WP-1 is a **wiring** job, not a build job — but see §6 for the cost it exposes |

**A is the important one.** The review's §3 rule is *"no plan may close a finding with a grep as its receipt."* Finding A shows the weaker corollary is also live: **a grep cannot even be trusted to size the population.** `grep -c 'HTTPException(status_code=5' → 9`; the balanced-paren scan over the same tree → 11. A broken implementation returns 9 too.

---

## 1. POPULATION — unchecked Supabase reads

**Predicate (re-runnable, `/tmp/p140_4/scan_supabase.py`):** walk `src/**/*.{ts,tsx}`; find `const|let { … } =` whose field list contains `data` or `count` and **not** `error`; take the statement text to the next depth-0 `;`; keep it if that text matches `\.(from|rpc|select|maybeSingle|single)\(|supabase|createClient|serviceClient|admin\b`.

```
TOTAL matches 158 | production 75 | test 83
```

Minus 2 verified false positives → **73 production members**:

| bucket | count |
|---|---|
| `src/app/api/**` (route handlers) | **32** ← the review's number, **CONFIRMED exactly** |
| `src/app/**` pages / SSR | 20 |
| `src/lib/**` | 11 (−2 FP → 9) |
| `src/components/**` | 10 |

**False positives, verified by reading:**
- `src/app/api/keys/sync/route.ts:138` — the matched text is a **comment** describing the *old* code (`… DISCARDING // \`error\`. …`). The live read 13 lines below **is** checked. Not a member.
- `src/app/api/portfolio-documents/route.ts:61` — `supabase.storage.getPublicUrl()` returns `{data}` with **no `error` field**. Correct API usage. Category (c).

### 1a. The predicate's own blind spot — and it hides the CRITICAL

`SyncPreviewStep.tsx:1053-1114` **array**-destructures seven `Promise.all` members, so an object-destructure predicate returns **zero** for the phase's top finding. Read directly and confirmed:

| # | member | line | field taken | `.error` read? |
|---|---|---|---|---|
| 1 | `strategy_analytics` | 1054 | `{ data: analytics }` | ✗ |
| 2 | `trades` count | 1055 | `{ count: tradeCount }` | ✗ |
| 3 | `trades` earliest | 1056 | `{ data: earliest }` | ✗ |
| 4 | `trades` latest | 1057 | `{ data: latest }` | ✗ |
| 5 | `trades` sample | 1058 | `{ data: sample }` | ✗ |
| 6 | `csv_daily_returns` count | 1059 | `{ count: csvRowCount }` | ✗ |
| 7 | `api_keys` | 1060→1120 | `keyRowResult.data` | ✗ |

**7 of 7. Zero `.error` reads.** C-3 CONFIRMED first-hand.

⇒ **Any census the plan runs must cover BOTH destructure shapes.** This is the class/instance shape in miniature: object-form closed, array-form open, in the same file.

### 1b. Classification of the 32 `src/app/api/**` members

**(a) fabricates an observation the user sees** — read fails → `?? 0` / `?? []` → a *measurement* about the user's account:

| file:line | fabrication |
|---|---|
| `src/app/api/cron/flag-monitor/route.ts:160` | `{count}` → `?? 0` → fabricated **zero-denominator** escalation to the founder |
| `src/app/api/portfolio-alerts/route.ts:57` | "you have no alerts" at HTTP 200 |
| `src/app/api/portfolio-alerts/route.ts:101` | same, second handler |
| `src/app/api/alerts/critical/route.ts:43` | same, critical lane |
| `src/app/api/alerts/[id]/acknowledge/route.ts:42` | empty portfolio list → ack silently no-ops |
| `src/app/api/strategies/finalize-wizard/route.ts:721` | `{count}` on `strategy_keys` → member count of a composite |
| `src/app/api/verify-strategy/[id]/status/route.ts:42` | verification "not found" |
| `src/app/api/verify-strategy/[id]/status/route.ts:67` | ditto, `verification_requests` |
| **client-side, same class** `SyncPreviewStep.tsx:1053-1114` | *"We found only 0 filled trade(s) on this key."* |

**(b) skips a gate / fails open** — the dangerous half:

| file:line | gate that silently opens |
|---|---|
| `src/app/api/alerts/ack/route.ts:84` | **replay/idempotency gate** — `used = null` → a consumed ack token is honoured again. ⚠️ The very next read at `:93` **does** destructure `error` — class open beside class closed, 9 lines apart |
| `src/app/api/alerts/ack/route.ts:145` | alert body read |
| `src/app/api/alerts/ack/route.ts:281` | **ownership** read (`portfolios.user_id`) |
| `src/app/api/keys/sync/route.ts:340` | `strategies.api_key_id` → NULL |
| `src/app/api/keys/sync/route.ts:347` | `api_keys.exchange` → venue silently stays `"okx"`; a Bybit/Deribit/MT5 key syncs against the wrong venue. ⚠️ `:340` is a **redundant re-SELECT** — the hardened read at `:151-158` already selected `api_key_id` |
| `src/app/api/account/deletion-request/route.ts:81` | duplicate deletion request + duplicate founder email |
| `src/app/api/bridge/route.ts:114` | → 404 *"Portfolio not found"* to the actual owner |
| `src/app/api/simulator/route.ts:144` | ditto |
| `src/lib/queries.ts:1273` | via `portfolio-optimizer/route.ts:139-144` → **403 "Forbidden" to the portfolio's owner** |
| `src/lib/notes/ownership.ts:56,69,81,100` | **four ownership predicates**, all fail-closed-looking but read-error-blind |
| `src/lib/api/approval-gate.ts:33` | role/approval gate reads `profiles` unchecked |
| `src/app/api/admin/notify-submission/route.ts:71` | ownership |
| `src/app/api/match/decisions/holding/route.ts:80,100` | holding + published-only gate |
| `src/app/api/strategies/[id]/sync-progress/route.ts:139` | ownership |
| `src/app/api/strategies/draft/[id]/route.ts:151` | draft ownership |
| `src/app/api/trades/upload/route.ts:79` | ownership |
| `src/app/api/bridge/outcome/route.ts:122` | intro-sent precondition |
| `src/app/api/intro/route.ts:95` | role check |
| `strategyGate.ts:165` (**inverse direction**) | `spanDays !== null &&` — a failed `earliest`/`latest` read **skips the 7-day gate entirely**; a 2-day strategy publishes as verified |

**(c) harmless / low** — the remainder (14): admin display-name and email joins (`admin/usage:59`, `admin/users/[id]:74`, `admin/deletion-requests:75`, `intro/route:302`, `intro-response:229`, `admin/match/send-intro:740`, `email.ts:713`), category/venue lookups (`StrategyForm.tsx:67`, `demo-recommendations.ts:110`), profile reads for display (`onboarding/page:22`, `pending-approval/page:55`, `(dashboard)/layout:36`, `profile/page:28,45`), `auth.ts:449` (`sanitize_user` RPC — worth a second look, it is a GDPR path), plus the 2 confirmed FPs.

### 1c. ⚠️ THE CLASS IS WIDER THAN "SUPABASE"

The review's M-2 lists three "client members": `MatchQueueIndex.tsx:147, :217` and `StrategyActions.tsx:41`. **None of them is a Supabase read.** Read at source:

- `MatchQueueIndex.tsx:145-152` and `:223-227` — `await fetch("/api/admin/match/kill-switch", …)` with **no `res.ok` read**, followed by `load()`. A 403/500 makes the **match-engine kill-switch** pill snap back and the founder believes the engine is OFF when it is ON.
- `StrategyActions.tsx:41-49` — `fetch(…).catch(…)`, fire-and-forget by design (documented).

⇒ The true class is **"an async result whose failure channel is never read"**, with **two carriers**: a Supabase `{data|count}` destructure *and* an unchecked `fetch()` Response. A `readOne`/`readCount` helper (WP-2) closes **only the first carrier**.

**Coverage-law row for WP-2 as specified: row 1 (shared artefact, 100%) *for the Supabase carrier only*. For the class as a whole it is row 3 — per-site, and the plan must say "partial by construction" in those words** unless a second leaf covers the fetch carrier.

---

## 2. POPULATION — error surfaces and their announcement

**Predicate:** `.tsx` under `src/`, excluding tests, that calls a `set\w*(Error|Err|Failure|Msg)\w*\(` setter. Announcement channel = `role="alert"` ∨ `aria-live` ∨ imports `ErrorEnvelope`, counted **file-wide** (shape-independent, so it cannot be defeated by an early-return render).

```
57 files set an error state
30 announce somewhere in-file
27 have ZERO announcement channel, file-wide
```

**The 27 named:**

`admin/partner-import/page.tsx` · `allocations/ScenarioFlaggedHoldingsList.tsx` · `allocations/components/WeightOptimizerSection.tsx` · `admin/AllocatorMatchQueue.tsx` · `admin/ForQuantsLeadsTable.tsx` · `admin/MatchQueueIndex.tsx` · `admin/PreferencesPanel.tsx` · `admin/SendIntroPanel.tsx` · `auth/LoginForm.tsx` · `auth/OnboardingWizard.tsx` · `auth/ProfileForm.tsx` · `auth/ResetPasswordForm.tsx` · `auth/SignupForm.tsx` · `landing/VerificationForm.tsx` · `org/OrganizationTab.tsx` · `portfolio/AlertsList.tsx` · `portfolio/AllocationEventForm.tsx` · `portfolio/CreatePortfolioForm.tsx` · `portfolio/DocumentUpload.tsx` · `portfolio/MigrationWizard.tsx` · **`portfolio/PortfolioOptimizer.tsx`** · `portfolio/RemoveStrategyButton.tsx` · `portfolio/ReplacementPanel.tsx` · `strategy/ApiKeyManager.tsx` · `strategy/PendingIntros.tsx` · `strategy/RequestIntroButton.tsx` · `strategy/StrategyForm.tsx`

**RG-1 / RG-3 confirmed by direct count:**
```
PortfolioOptimizer.tsx     role="alert" = 0   aria-live = 0
AllocatorMatchQueue.tsx    role="alert" = 0   aria-live = 0
```

**The split the review calls "the cleanest evidence for the coverage law" is real and re-measurable:** every `ErrorEnvelope` consumer announces, because `ErrorEnvelope.tsx:134` carries `role="alert"` on the container — one node, 100%. The 27 above route through nothing.

⚠️ **Predicate honesty.** Two narrower predicates I ran first returned **35 files/41 sites** and **13 files** and **neither caught `PortfolioOptimizer` or `AllocatorMatchQueue`** — the two named regressions — because they matched only the `{error && <Tag>}` render shape and those files use an early `return`. **Do not size this remedy from a JSX-shape grep.** The file-wide channel count above is the predicate that holds.

**Test-side gap, measured:** `grep -rln 'role="alert"' tests/ src/__tests__/ e2e/` → **one file** (`e2e/onboarding-banner-smoke.spec.ts`), unrelated. **No test in the repo asserts an announcement on any seam error surface.**

---

## 3. POPULATION — terminal states whose only control is destructive

**Independently re-derived. The orchestrator's settlement is CONFIRMED: the count is 1.**

Codes reaching `setPhase("gate_failed")` in `SyncPreviewStep.tsx` (sites `:575, :628, :649, :713, :789, :1146`), resolved through `KNOWN_KICKOFF_CODES:147-176` and `gateFailureToWizardError` (`wizardErrors.ts:1256-1280`) → **exactly 11 distinct codes**, matching the review.

Control logic, read at source:
- `SyncPreviewStep.tsx:1379` — Retry renders iff `errorEnvelope.recoverable` (`ErrorEnvelope.tsx:108`: `showRetry = envelope.recoverable && Boolean(onRetry)`).
- `SyncPreviewStep.tsx:1385-1405` — `errorStateHidesDestructiveControl` ? a `<Link href="/strategies">` : the destructive `onTryAnotherKey` button.
- `recoverable` = `actions ∩ {clear_and_retry, try_another_key} ≠ ∅` (`src/lib/envelope.ts` `RECOVERABLE_ACTIONS`).
- **`request_call` and `start_fresh` render NO control.** Verified: `ErrorEnvelope` renders only Retry (`:178`), Cancel (`:188`, and `WizardErrorEnvelope` — a **7-line** re-export — passes no `onCancel`), and a "Copy diagnostics" ghost button inside `<details>` (`:211`), which is not an action control.

| # | code | `actions` | recoverable | in allow-list `:1347-1354` | controls rendered |
|---|---|---|---|---|---|
| 1 | `SYNC_FAILED` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive (2) |
| 2 | `RATE_LIMITED` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive (2) |
| 3 | `GATE_DRAFT_GONE` | start_fresh, request_call | ✗ | **✔** | Link only — safe |
| 4 | `COMPOSITE_MEMBERSHIP_UNKNOWN` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive (2) |
| 5 | `VALIDATION_FAILED` | request_call | ✗ | **✔** | Link only — safe |
| 6 | `UNKNOWN` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive (2) |
| 7 | `KEY_NETWORK_TIMEOUT` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive (2) |
| 8 | `GATE_ANALYTICS_FAILED` | clear_and_retry, request_call | ✔ | ✗ | Retry + destructive (2) |
| 9 | `GATE_INSUFFICIENT_TRADES` | try_another_key, request_call | ✔ | ✗ | Retry + destructive (2) |
| 10 | `GATE_INSUFFICIENT_DAYS` | try_another_key, request_call | ✔ | ✗ | Retry + destructive (2) |
| 11 | **`GATE_NO_DATA_SOURCE`** | **start_fresh, request_call** | **✗** | **✗** | **destructive ONLY (1)** |

**TRAP-4 violation count = 1, member = `GATE_NO_DATA_SOURCE`.** No member the verifier missed. Its `actions` are byte-identical to `GATE_DRAFT_GONE`'s — the code the allow-list was created for — which is exactly why it should have been in it.

**Secondary, still true and independently checkable:**
- Rows 9 and 10 earn `recoverable` from `try_another_key`, but the handler `onRetry` receives is `handleKickoffRetry` (`:1379`), which re-runs the kickoff and re-evaluates an identical gate. `recoverable` is **mislabelled**, not spurious.
- Row 4 (`COMPOSITE_MEMBERSHIP_UNKNOWN`) — a code **about composites** — renders the single-key destructive control, because `isComposite` is false on every non-2xx kickoff (`:1400`).

**Coverage-law row.** Widening the allow-list to 3 = **row 2, hand-typed roster, partial by construction — say so in those words.** Inverting to "render the destructive control only for codes that *earn* it" makes it a property of the code table = **row 1**.

---

## 4. POPULATION — wire codes vs render vocabulary

### 4a. Emitted on the wire

| producer | distinct codes | predicate |
|---|---|---|
| TS routes, `src/app/api/**` | **53** | `code: "X"` ∪ `"code": "X"` |
| Python literals | **37** | `service_error*(…, "X")` ∪ `code[:=]"X"` ∪ `error_code[:=]"X"` |
| Python **dynamic** | **unbounded** | `csv_adapter.py:172` `error_code=first_rule.upper()` |

**The dynamic set is genuinely open, not merely large.** Traced to source: `csv_adapter.py:165` reads `errors[0]["rule"]`, produced at `csv_validator.py:780` from `rule_name = str(row.get("check", "unknown"))` (`:766`) — **a pandera check name**. Eight literal rules exist (`empty`, `parse_error`, `too_many_rows`, `date_format_ambiguous`, `date_format_normalized`, `auto_normalized_percent_form`, `daily_sharpe_sentinel`, `daily_return_dollar_form_sentinel`); the rest are whatever pandera names the check, which **may contain parentheses and commas** and is therefore not even a valid identifier after `.upper()`. No registry, no membership check, and `CsvSubmitStep.tsx:39` types it `code: string` and ships it verbatim to `data-error-code` and PostHog.

### 4b. Recognised by the render side

| table | site | size | members |
|---|---|---|---|
| `SEAM_CODE_TO_WIZARD_CODE` | `wizardErrors.ts:1551-1568` | **6** | VALIDATION_FAILED, RATE_LIMITED, CIRCUIT_OPEN, UPSTREAM_TIMEOUT, UPSTREAM_NETWORK_ERROR, SEAM_MISCONFIGURED |
| `KNOWN_FINALIZE_CODES` | `SubmitStep.tsx:148-194` | **9** | KEY_SCOPE_BROADENED, KEY_NETWORK_TIMEOUT, GATE_DRAFT_GONE, GUARD_BLOCKED, COMPOSITE_MEMBERSHIP_UNKNOWN, COMPOSITE_TOO_MANY_MEMBERS, SERVICE_UNAVAILABLE_RETRY, SERVICE_UNREACHABLE, SEAM_MISCONFIGURED |
| `KNOWN_KICKOFF_CODES` | `SyncPreviewStep.tsx:147-176` | **5** | RATE_LIMITED, GATE_DRAFT_GONE, COMPOSITE_MEMBERSHIP_UNKNOWN, MISSING_STRATEGY_ID, INVALID_STRATEGY_ID |
| `VENUE_WIRE_CODE_TO_VERDICT` | `wizardErrors.ts:1337-1353` | **6** | RATE_LIMITED, PROBE_FAILED, AUTH_FAILED, EXCHANGE_UNAVAILABLE, NETWORK_UNAVAILABLE, DDOS_PROTECTION |

### 4c. H-2 and H-3, both CONFIRMED by set arithmetic

- **H-2:** `RATE_LIMITED` and `VALIDATION_FAILED` **are** in `SEAM_CODE_TO_WIZARD_CODE` (added by 140.3-01) and **are not** in `KNOWN_FINALIZE_CODES`. `SubmitStep.tsx:206-213` translates then membership-checks ⇒ both collapse to `UNKNOWN`. **140.3-01 ships with zero user-visible effect.** Verified by reading both sets in full.
- **H-3:** `KNOWN_KICKOFF_CODES` ∩ {CIRCUIT_OPEN, UPSTREAM_TIMEOUT, UPSTREAM_NETWORK_ERROR, SEAM_MISCONFIGURED} = **∅**, and `SyncPreviewStep.tsx:603` looks up the **raw wire string** with no `recogniseSeamErrorCode` hop. Confirmed.

### 4d. ⚠️ Structurally unmatchable codes

**27 of the 53 TS wire codes are lowercase** and can never match a SCREAMING_SNAKE table:

`account_sanitized, admin_role_mutate_failed, auth_getuser_failed, book_only_draft, custom, draft_state_invalid, export_manifest_drift, grant_did_not_take, invalid_role_mutation, mutation_succeeded_but_audit_failed, no_prior_export, ok, partner_tag_conflict, profile_read_failed, ratelimit_misconfigured, revoke_did_not_take, role_not_held, roles_read_failed, rpc_shape_drift, strategy_lookup_failed, strategy_no_manager, strategy_not_found, strategy_not_published, strategy_tier_conflict, too_many_rows, user_not_found, would_orphan_last_admin`

**`draft_state_invalid` is emitted by `finalize-wizard/route.ts` — the exact route `SubmitStep` reads.** It is not a casing accident in a distant admin route; it is on the wizard's own publish path. **0 of 37 Python literal codes are lowercase**, so the convention break is TS-side only.

**Unmapped:** 53 TS + 37 Python literal = 90 distinct wire codes against **6** wire→wizard translations. Coverage **6/90 = 6.7%**.

**Coverage-law row.** Adding 2 codes to `KNOWN_FINALIZE_CODES` = **row 2, partial by construction**. `WireErrorCode` (WP-11) = **row 1** — an untranslated wire code becomes a compile error at the table. The lowercase 27 must be **normalised or explicitly quarantined** before any union is minted, or the union codifies the break.

---

## 5. POPULATION — raw `HTTPException(status_code=5xx)` bypassing `service_error()`

**The ban is real and lives at `analytics-service/services/error_contract.py:160-163`**, inside `_validate`, reachable **only** through `service_error` (`:289`) and `service_error_response`. A direct `raise HTTPException(...)` never reaches it.

**Predicate:** balanced-paren scan of every `HTTPException(` in `analytics-service/**`, excluding `tests/`, taking `status_code=NNN` **or** the first positional integer, anywhere in the call. (A line-based grep returns **9** — this returns 12, of which 1 is a comment.)

| # | site | status | exposure | line-grep saw it? |
|---|---|---|---|---|
| 1 | `routers/portfolio.py:2326` | 500 | **ANONYMOUS — public teaser.** The §3 canary site: flipping 500→502 here left **both full suites byte-identical** | ✔ |
| 2 | `routers/portfolio.py:2550` | 500 | authenticated (verify-strategy) | ✔ |
| 3 | `routers/exchange.py:681` | 503 | authenticated | ✔ |
| 4 | `routers/exchange.py:719` | 500 | authenticated | ✔ |
| 5 | `routers/exchange.py:781` | 500 | authenticated | ✔ |
| 6 | `routers/csv.py:101` | 500 | authenticated; `detail` is a **dict** (`CSV_UPSTREAM_FAIL`) — bypasses `service_error_body`'s scalar guard at `:252-256` | ✔ |
| 7 | `routers/cron.py:613` | 500 | cron/internal | ✔ |
| 8 | `routers/debug_key_flow.py:56` | 503 | internal debug | ✔ |
| 9 | `services/analytics_runner.py:1725` | 500 | internal (wraps into the job worker) | ✔ |
| **10** | **`routers/cron.py:630`** | **500** | cron/internal; `detail` interpolates `type(exc).__name__` | ✗ **continuation line** |
| **11** | **`routers/debug_key_flow.py:99`** | **503** | internal debug | ✗ **continuation line** |

**Excluded as a false positive:** `services/job_worker.py:602` — `HTTPException(500)` appears inside a `#` comment (*"wrap-to-HTTPException(500) boundary"*). Verified by reading `:596-606`.

**Coverage-law row.** Re-homing 11 sites onto `service_error()` = **row 3, per-site, enumerated** (bounded and now fully enumerated, which is the only condition under which row 3 is acceptable). The row-1 alternative: an app-level assertion that no `HTTPException` with a 5xx status is constructed outside `error_contract.py` — a module-scope test importing every router and scanning ASTs, which would have caught sites 10 and 11 the day they were written.

---

## 6. POPULATION — the "middle tier"

### 6a. The derivation already exists and is already exact

`deriveSeamRoutePaths` (`src/lib/seam-poll-disjointness.pin.test.ts:202-221`) walks `src/app/api` for `route.ts` and tests `SEAM_IMPORT_EDGE` (`:163-165`), built from `SEAM_MODULES = ["analytics-client", "resilient-fetch", "process-key-client"]` (`:156-160`).

**Ran that exact predicate:**
```
derived                       = 15 route files
SEAM_ROUTE_BUDGETS            = 15 rows
SEAM_EXCLUSIONS               =  2 rows
derived − (budgets ∪ excl)    = ∅
(budgets ∪ excl) − derived    = {cron/warm-analytics, debug-key-flow}   ← the 2 exclusions, correctly
```
**The derived set is SET-IDENTICAL to `SEAM_ROUTE_BUDGETS` at HEAD.**

⇒ **Correction to the review.** *"A new route consuming an existing seam wrapper matches neither needle"* is true of `/resilientFetch\s*\(/` (the `SEAM_FILES` needle) and of the ESLint base-URL taint. It is **false** of `SEAM_IMPORT_EDGE`, which matches all three wrapper modules. **The middle tier is already detectable — nothing is wired to the detector.**

### 6b. Consumer census

| wrapper | consumers (production) |
|---|---|
| `analytics-client` | 9 routes: `admin/match/eval`, `admin/match/recompute`, `bridge`, `keys/validate-and-encrypt`, `portfolio-optimizer`, `scenario/optimize`, `simulator`, `strategies/composite/add-key`, `strategies/create-with-key` |
| `postProcessKey` | 5 routes: `keys/sync`, `strategies/csv-finalize`, `strategies/csv-validate`, `strategies/finalize-wizard`, `verify-strategy` (+ 2 lib files) |
| direct `resilientFetch(` | 3 routes: `keys/[id]/permissions`, `keys/validate-and-encrypt`, `strategies/finalize-wizard` (+ 3 lib files) |
| **union** | **15 — exactly the table.** ✅ "by discipline, asserted nowhere" CONFIRMED |

### 6c. Who sits outside a guard

| guard | derives population from | covers the middle tier? |
|---|---|---|
| `seam-poll-disjointness.pin.test.ts` | `SEAM_IMPORT_EDGE` (disk) | **yes** |
| `seam-ssr-exposure.pin.test.ts` | `SEAM_IMPORT_EDGE` (disk) | **yes** |
| `seam-budgets.invariant.test.ts:446` | `.length).toBe(15)` + a hand-typed twin | **no — no disk term at all** |
| `seam-log-coverage.test.ts:55-64` | hand-typed `SEAM_FILES` (8) | **no** |
| `resilient-fetch.wiring.test.ts:735-798` | 3 disk discovery passes | yes, for **budget-key bindings** only |
| ESLint SEAM-01 | `ANALYTICS_SERVICE_URL` taint | **no** |

### 6d. ⚠️ THE COST WP-1 EXPOSES — measure it before planning, not after

I ported `seam-log-coverage.test.ts`'s exact predicate (comment-strip → `caughtIdentifiers` → balanced-paren `consoleCalls` → `maskStrings` → scrubber/safe-property test) into `/tmp/p140_4/logscan.mjs` and ran it over all 15 derived routes:

```
FILES WITH >=1 UNSCRUBBED CAUGHT-VALUE CONSOLE ARG: 9 / 15   (17 sites)
```

| file | offending lines |
|---|---|
| `admin/match/eval/route.ts` | 185, 217, 245 |
| `admin/match/recompute/route.ts` | 136, 172, 201 |
| `bridge/route.ts` | 194 |
| `keys/sync/route.ts` | 220, 382 |
| `portfolio-optimizer/route.ts` | 126, 236 |
| `scenario/optimize/route.ts` | 198, 245 |
| `simulator/route.ts` | 219 |
| `strategies/csv-finalize/route.ts` | 689 |
| **`verify-strategy/route.ts`** | **384, 440** ← the two the review names: credential-bearing **and** public/anonymous |

**All 9 offending files are outside `SEAM_FILES`; all 5 rostered routes are clean.** The hole's shape is exactly the roster's complement.

⇒ **Deriving `SEAM_FILES` is ~10 lines of wiring and ~17 sites of consequent work.** (My port measures 9 files; the review's re-run of the guard's own scanner measured 10. The port simplifies two sub-regexes; treat 9 as a floor, 10 as the likely figure, and let the real guard produce the authoritative list on first red.) **A plan that budgets only the wiring will land a red tree.**

**Coverage-law row: 1 (shared artefact, 100%) — highest-leverage item in the phase**, and the source is *not* the budget table (a hand-typed twin) but `SEAM_IMPORT_EDGE` (the causal import edge). Prefer the causal predicate; the budget table then becomes a second, independently-derived set to compare against, which is the `resilient-fetch.wiring.test.ts` shape.

> **Surface this conflict in the plan (CONTEXT §5 requires it):** `seam-log-coverage.test.ts:38-41` argues *against* deriving — *"a derived list would silently widen or narrow this guard whenever the budget table moved."* That objection dissolves if the source is the import edge rather than the budget table, and the file's own next paragraph (`:47-53`) records that the hand-typed alternative **already produced the hole**.

---

## 7. POPULATION — hand-maintained counts and line-number citations

### 7a. Line-number citations

**Predicate:** regex `([\w\-/.\[\]]+\.(ts|tsx|py|sql|md))[: ]:?(\d+)(-(\d+))?` over `src/`, `analytics-service/`, `tests/`, `e2e/`, `supabase/`.

```
TOTAL citations                                881
  src/                                         391
  analytics-service/                           374
  e2e/                                         115
resolvable to a unique file                    643
citations pointing PAST end-of-file            18   ← provably wrong, mechanically
```

**The 18 mechanically-wrong citations** (a hard **lower bound** — a citation landing on the wrong line *inside* the file is invisible to this check):

| citing site | cites | file actually has |
|---|---|---|
| `src/app/factsheet/[id]/v2/basis-context.tsx:545` | `analytics_runner.py:2387-2390` | 1725 lines |
| `src/lib/closed-sets.ts:394` | `analytics_runner.py:1765` | 1725 |
| `src/lib/factsheet/composite-read-path.ts:365` | `analytics_runner.py:1839` | 1725 |
| `src/lib/factsheet/composite-read-path.ts:400` | `analytics_runner.py:1938-1940` | 1725 |
| `src/lib/factsheet/composite-read-path.test.ts:353` | `analytics_runner.py:1839` | 1725 |
| `analytics-service/routers/process_key.py:519` | `analytics_runner.py:2313-2314` | 1725 |
| `analytics-service/routers/process_key.py:562` | `analytics_runner.py:2316-2317` | 1725 |
| `analytics-service/services/metrics.py:288` | `analytics_runner.py:1925` | 1725 |
| `analytics-service/services/basis_series.py:178` | `analytics_runner.py:2399` | 1725 |
| `analytics-service/services/job_worker.py:2385` | `analytics_runner.py:2304-2316` | 1725 |
| `analytics-service/services/job_worker.py:4473` | `analytics_runner.py:2291-2316` | 1725 |
| `analytics-service/tests/test_cash_basis_series_sc4.py:594` | `analytics_runner.py:2304-2316` | 1725 |
| `src/lib/api/simulatorSchema.ts:106` | `simulator_scoring.py:438` | 312 |
| `analytics-service/routers/simulator.py:178` | `services/simulator_scoring.py:634` | 312 |
| `analytics-service/tests/test_simulator_router.py:876` | `simulator_scoring.py:634` | 312 |
| `ScenarioCompareTable.all-columns.test.tsx:29` | `Table.tsx:195` | 120 |
| `tests/lib/process-key-client.test.ts:8` | `tests/integration/process-key-thin-adapters.test.ts:854` | 824 |
| `e2e/discovery-watchlist.spec.ts:415` | `WatchlistTabs.tsx:69-91` | 82 |

**Two files caused 15 of the 18** (`analytics_runner.py` shrank from >2400 to 1725; `simulator_scoring.py` from >634 to 312). Both are **out of 140.4's editing scope**, which is the point: the drift is caused by edits *elsewhere*.

**⚠️ The prompt's own two analog citations were CORRECT** — I checked and initially mis-suspected drift: `seam-ssr-exposure.pin.test.ts:237` is the `it(`, assertions at `:242-245`; `critical-regressions.test.ts:134` is the positive assertion. No drift. Recording this because "I thought a citation had drifted and it had not" is the failure mode in the other direction.

**Proposal — derive, don't hand-type.** A ~30-line vitest guard that greps every `file:line` citation in `src/` + `analytics-service/`, resolves it, and asserts the line is in range. It closes **the detectable half of the class at a chokepoint (row 1)** and reddens on the day a cited file shrinks. It cannot verify *semantic* correctness — so pair it with WP-15's structural fix (**prefer symbol references over line numbers in files this programme is actively editing**), which is row 3 and must be labelled *partial by construction*.

### 7b. Hand-typed count assertions

```
TOTAL numeric count assertions/constants        1109
  .toHaveLength(N)                               632
  .length).toBe(N)                               233
  .toBeGreaterThanOrEqual(N)                     210
  .size).toBe(N)                                  30
  EXPECTED_*_(SIZE|COUNT|CASES) = N                4
on seam / contract-guard files                    62
```

**The load-bearing ones for 140.4:**

| site | literal | what it pins | hazard |
|---|---|---|---|
| `contracts-registry.test.ts:185` | `toBeGreaterThanOrEqual(20)` | 44 measured rows (`grep -c '{ path:'` → **44**) | **24 rows deletable with green CI** |
| `seam-budgets.invariant.test.ts:446` | `.length).toBe(15)` | table vs a **literal**, no disk term | a route added to the table + the literal bumped passes |
| `seam-log-coverage.test.ts` | **none** | `SEAM_FILES` (8 members) | ⚠️ **VERIFIED: `grep -rn 'SEAM_FILES' src/ tests/ \| grep -iE 'length\|size\|toHaveLength\|toEqual'` returns EMPTY.** 8 mentions in the file, **zero** size or set pin. GRD F-1 CONFIRMED: **a roster member is silently droppable** |
| `wizardErrors.test.ts:1348, :1491` | `EXPECTED_TABLE_SIZE = 55` (twice) | copy table | duplicated literal, deliberately hand-typed per `SyncPreviewStep.tsx:167-172` |
| `resilient-fetch.wiring.test.ts:749` | `toBeGreaterThanOrEqual(14)` | vacuity fence | correct use — a **floor beside a set equality**, not instead of one |
| `tests/redis/seam-breaker.redis.test.ts:330` | `EXPECTED_CASES = 7` | self-referential; both sides in-file | |

---

## 8. POPULATION — guards deletable with green CI

`CONTRACT_GUARDS` = **44 rows** (`grep -c '{ path:' src/__tests__/contracts/contracts-registry.test.ts` → 44). Floor at `:185` is `>= 20`. The only per-row check is `existsSync` (`:188-196`).

**Two separate deletability holes:**

1. **Registered-but-hollow (24 rows of slack).** `44 − 20 = 24` rows can be deleted and CI stays green. Additionally, `existsSync` is satisfied by a `describe.skip`, an emptied roster, or `it("todo", () => {})`.

2. **Unregistered guards.** Naming-convention census (`*.{pin,invariant,purity,parity,contract,wiring}.test.*` ∪ names containing parity/registry/manifest/coverage/contract) → **33 guard-shaped files, 16 unregistered**:

`src/__tests__/check-admin-route-manifest.test.ts` · `src/__tests__/check-route-contract.test.ts` · `src/__tests__/contracts/contracts-registry.test.ts` · `src/__tests__/format-percent-contract.test.ts` · `src/__tests__/no-store-coverage.test.ts` · `allocations/widgets/index.contract.test.ts` · `allocations/widgets/performance/scenario-factsheet-parity-guard.test.ts` · `factsheet/[id]/v2/BaseLeverageNote.wiring.test.tsx` · `components/strategy/discovery-selectors.contract.test.tsx` · `src/lib/auth/rbac-manifest.test.ts` · `src/lib/scenario-optimizer-parity.test.ts` · **`src/lib/seam-copy.purity.test.ts`** · `tests/lib/admin/pii-scrub-python-parity.test.ts` · `tests/lib/composite/window-overlap-convention-parity.test.ts` · `tests/lib/process-key-onboard-contract-parity.test.ts` · **`tests/lib/validate-key-venue-transient-parity.test.ts`**

**Direct check of the seam/redis/sentry family confirms the review's four exactly:**
```
REGISTERED   seam-budgets.invariant · seam-constants.pin · seam-copy.pin · seam-discriminator.purity
             seam-discriminator · seam-errors.purity · seam-log-coverage · seam-poll-disjointness.pin
             seam-redaction · seam-ssr-exposure.pin
UNREGISTERED src/lib/seam-copy.purity.test.ts          (F-3)
UNREGISTERED src/lib/sentry-capture.test.ts            (F-15)
UNREGISTERED tests/redis/seam-breaker.redis.test.ts    (F-10)
```
+ `tests/lib/validate-key-venue-transient-parity.test.ts` (F-10) = **the four, confirmed**.

⚠️ **`sentry-capture.test.ts` and `seam-breaker.redis.test.ts` do NOT match any naming convention** — the review found them by knowing which file was a property's sole asserter. **The true population of "deletable guards" is not enumerable by filename.** 16 is a floor; the honest statement is *"≥16 by convention, plus an unknown number discoverable only by mutation."* A plan claiming to have closed this class from a filename census is claiming more than the evidence supports.

---

## 9. ANALOGS — copy from these, do not invent

| fix shape 140.4 needs | copy from | why this one |
|---|---|---|
| **a checked Supabase read that fails loud — SERVER** | `src/app/api/strategies/finalize-wizard/route.ts:645-667` | `.maybeSingle()` → `if (strategyErr)` → `console.error(…, scrubSeamError(strategyErr))` → **500 about US**; *then* `if (!strategyRow)` → **404 about the row**. Two facts, two responses. Its comment at `:653-655` states the rule: *"Closing the class, not the instances, is the point."* |
| same, **second instance already copied from it** | `src/app/api/keys/sync/route.ts:151-192` | proves the template is transferable; `:138-149` names the template by path. The 404 arm carries the P458 enumeration-safety note — **a fix must not split not-found from not-owned** |
| **a checked Supabase read that fails loud — CLIENT** | `SyncPreviewStep.tsx:848-867` | four `if (xRes.error) throw new Error(...)` in the **same function**, 200 lines above the seven unchecked ones. Throws into `heavyFetchErrorsRef` (`:702`) → `failPolling()` (`:711-720`) → recoverable `SYNC_FAILED`. **The escalation sink already exists — WP-2 wires the other seven into it, it does not build one** |
| **a derived (not hand-typed) roster** | `src/lib/resilient-fetch.wiring.test.ts:735-798` | 3 disk discovery passes → a **vacuity fence** (`:749` `toBeGreaterThanOrEqual(14)` with *"this guard is now blind, not satisfied"*) → **three `toEqual` set equalities** (`:771`, `:782`, `:793`), each with a failure message that forbids widening. This is the exact four-part shape WP-1 must reproduce |
| the **disk walker** to reuse | `src/lib/seam-poll-disjointness.pin.test.ts:202-221` (`deriveSeamRoutePaths`) + `:163-165` (`SEAM_IMPORT_EDGE`) | measured to return **exactly** `SEAM_ROUTE_BUDGETS`'s 15 at HEAD. `:196-200` explains why it is duplicated rather than imported (*"a test file must not import another test file"*) — **respect that; do not extract it into a shared helper** |
| **a total/exhaustive mapping between two vocabularies** | `wizardErrors.ts:1551-1568` (`SEAM_CODE_TO_WIZARD_CODE`) | explicit `ReadonlyMap`, and `:1561-1566` states *why* an identity rule was rejected: *"writing the mapping as `code as WizardErrorCode` would silently admit `SEAM_DEGRADED` … which is the reason this table is explicit at all."* WP-11's `WireErrorCode` union must preserve that reasoning, not replace it |
| **a census test with a quarantine allow-list** | `analytics-service/tests/test_limiter_identity.py:462-479` | **VERIFIED as cited.** Disk-derived `offenders`, `IP_KEYED_QUARANTINE: frozenset = {"simulator.py"}` (`:103`), and the assertion is `offenders == IP_KEYED_QUARANTINE` — **equality, not containment**, with `:468-469` spelling out why: *"If it is ever repaired, SHRINK this allow-list; the equality below is deliberate so a repair cannot leave a stale exemption behind."* This is the shape for §5's raw-5xx census and §8's guard registry |
| ditto, second half | same file `:558-568` | set-equality with `unexpected=` / `missing=` **both** printed in the failure message |
| **a needle SELF-TEST** | `src/lib/seam-ssr-exposure.pin.test.ts:237-246` | **VERIFIED as cited — 4 assertions, 2 polarities**: `:242-243` `toBe(true)` on two real import forms, `:244-245` `toBe(false)` on a prose mention and a non-seam import. `:238-241` states why the negative half is load-bearing: *"a scan that flagged those would be 'fixed' by weakening it."* **Every new needle in 140.4 needs this** — §5's raw-5xx scanner and §7a's citation checker especially |
| **a positive-counterpart assertion** | `src/__tests__/critical-regressions.test.ts:131-144` | **VERIFIED as cited.** `:133-136` asserts the good thing is **present** (`/resilientFetch\s*\(/.test(src) → true`) beside `:138-142` asserting the bad thing is absent. `:139-141` even documents why `\bfetch(` was rejected for the negative. **A negative-only oracle is what M-9 proved goes vacuous** |
| **an error surface that announces correctly** | `src/components/error/ErrorEnvelope.tsx:134` | `role="alert"` on the container — **one node, and 100% of consumers inherit it.** The whole coverage law in one line. WP-4's class fix is "one more consumer of this", not "27 more `role="alert"` attributes" |
| **the copy-path guard for the announcement** | `ErrorEnvelope.tsx:111-129` | the `showWait` docblock — the model for stating *two* load-bearing conditions and why each rejects an easier version |
| **a DB-constraint chokepoint (C-2 / WP-8)** | `supabase/migrations/20260602190000_f6_wizard_session_idempotency.sql:52-54` + `:111` | the partial index `strategies_user_wizard_session_uniq ON strategies (user_id, wizard_session_id) WHERE wizard_session_id IS NOT NULL`, and `:111`'s writer-side comment *"fall through so the INSERT trips strategies_user_wizard_session_uniq"*. `20260710180000_wizard_composite.sql:100` is a second writer already doing it (*"23505 → the route maps it loud"*) |
| **the Python contract constructor** | `analytics-service/services/error_contract.py:180-216` | per-status arms each raising `ValueError` with the *reason* in the message (`:190-194` is the 424-retryability rule H-4 needs enforced). WP-9's retryability guard belongs in `VenueTransientHTTPException.__init__` **in this shape** |
| **a scalar-type guard at a carrier** | `error_contract.py:245-256` | *"Tested as the POSITIVE property: enumerating the bad types (list, dict) would miss int, bytes and a pydantic model."* The template for WP-13's type work |

### C-2 — the writer, verified first-hand (deadline item)

`supabase/migrations/20260716130500_finalize_terminal_status_param.sql:296-304`:
```
INSERT INTO strategies (
  user_id, name, status, source,
  strategy_types, subtypes, markets, supported_exchanges
)
```
**`wizard_session_id` is absent** ⇒ NULL ⇒ the partial index (`WHERE wizard_session_id IS NOT NULL`) cannot bite. `RETURNING id INTO v_strategy_id` mints a fresh surrogate key, so the new `UNIQUE (strategy_id, wizard_session_id)` from `20260726000225:123` can never collide either. **CONFIRMED structurally at source.** Fix = add the column to *this* INSERT (row 1, shared artefact). Reconcile `wizardErrors.ts:882`'s now-false copy in the same commit.

---

## 10. COVERAGE-LAW ROW PER WORK PACKAGE (§2 of CONTEXT is binding)

| WP | mechanism as specified | row | plan must say |
|---|---|---|---|
| WP-1 derive `SEAM_FILES` | disk import-edge → set equality | **1 — 100%** | prefer `SEAM_IMPORT_EDGE` over the budget table as source; **budget ~17 remediation sites (§6d), not just the wiring** |
| WP-2 one checked read | `readOne`/`readCount` leaf | **1** for the Supabase carrier; **3** for the `fetch` carrier | *"partial by construction"* for the fetch half (§1c) unless a second leaf lands |
| WP-3 the wait travels | `process-key-client.ts:643-648` chokepoint | **1** | then 5 per-site `buildEnvelope` threads = row 3 |
| WP-4 announce via one component | one more `ErrorEnvelope` consumer | **1** | the 2-line `role="alert"` stop-gap is **row 3 at 2/27** — *"partial by construction"* |
| WP-5 capture delivers | `captureToSentry` leaf | **1** | ordering load-bearing; (1) before (2) |
| WP-6 one error-body constructor | today per-site | **3 (1/8)** → promote to **1** | *"partial by construction"* if shipped as per-site |
| WP-7 destructive control earned | invert roster → property | **2 → 1** | the code-set half stays **row 2** until WP-11 — *"partial by construction"* |
| WP-8 CSV double-submit | DB constraint | **1** | must land before merge (auto-applies to prod) |
| WP-9 424 discipline | `__init__` guard = **1**; re-homing = **3** | mixed | state both rows separately |
| WP-10 honest copy | per-site, enumerated | **3** | *"partial by construction"*; enumerate before starting |
| WP-11 `WireErrorCode` | a type | **1** | **normalise or quarantine the 27 lowercase codes first (§4d)** or the union codifies the break |
| WP-12 guard integrity | registry + CI regex | **1** for the skip-regex; **2** for the 4 registry rows | the registry census is a **floor**, not a population (§8) |
| WP-13 type invariants | types | **1** | |
| WP-14 test fidelity | per-site, enumerated | **3** | |
| WP-15 truth pass | per-site; class fix structural | **3**, class fix **1** | the 18 out-of-range citations (§7a) are the derivable subset |
| **NEW — raw-5xx census** | census test w/ quarantine | **1** | §5; copy `test_limiter_identity.py:462-479`; the population is **11**, not 9 |

---

## 11. FILES 140.4 WILL TOUCH — role, data flow, analog

| file | role | data flow | analog | match |
|---|---|---|---|---|
| `SyncPreviewStep.tsx` | wizard step component | request-response + poll | `SyncPreviewStep.tsx:848-867` (own composite arm) | **exact — in-file** |
| `SubmitStep.tsx` | wizard step component | request-response | `SubmitStep.tsx:206-213` (own translate-then-check) | exact — in-file |
| `strategyGate.ts` | pure gate/predicate | transform | `error_contract.py:_validate` (fail-loud on unrepresentable) | role-match |
| `src/lib/supabase` read helper (**new**) | utility leaf | CRUD | `finalize-wizard/route.ts:645-667` | exact |
| `seam-log-coverage.test.ts` | structural guard | census | `resilient-fetch.wiring.test.ts:735-798` | exact |
| `seam-budgets.invariant.test.ts` | structural guard | census | `seam-poll-disjointness.pin.test.ts:202-221` | exact |
| `contracts-registry.test.ts` | registry guard | census | `test_limiter_identity.py:462-479` | exact |
| `error_contract.py` / `exchange.py` | service constructor | transform | `error_contract.py:180-216` (own arms) | exact — in-file |
| raw-5xx census test (**new**, Python) | structural guard | census | `test_limiter_identity.py:462-479` | exact |
| citation guard (**new**, TS) | structural guard | census | `seam-ssr-exposure.pin.test.ts:237-246` (self-test) | exact |
| `PortfolioOptimizer.tsx`, `AllocatorMatchQueue.tsx`, `WeightOptimizerSection.tsx` | components | request-response | `ErrorEnvelope.tsx:134` | exact |
| `20260716130500_*.sql` writer | migration | CRUD | `20260602190000_*.sql:52-54, :111` | exact |
| `wizardErrors.ts` (WP-11) | vocabulary leaf | transform | `wizardErrors.ts:1551-1568` | exact — in-file |

---

## 12. NO ANALOG IN THIS REPO

| need | why nothing to copy |
|---|---|
| an **unchecked-`fetch()` census** | every existing guard keys on Supabase or on the seam import edge; nothing enumerates bare `fetch()` in client components (§1c) |
| a **lowercase↔SCREAMING_SNAKE wire-code normaliser** | the 27 lowercase codes have no precedent for coercion; the only in-repo precedent (`SEAM_CODE_TO_WIZARD_CODE:1561-1566`) argues **against** any identity/derivation rule |
| an **announcement assertion** | measured: **one** file in `tests/ src/__tests__/ e2e/` references `role="alert"`, and it is unrelated. WP-4's test is new construction; use `seam-ssr-exposure.pin.test.ts:237-246`'s two-polarity discipline |
| a **live-Redis mutation receipt** | CONTEXT §6 blind spot (D-01); unrunnable in a worktree, unverified by either mutation sample. **Do not claim closure here** |

---

## 13. GREP HYGIENE APPLIED IN THIS DOCUMENT

- **No `grep -h` anywhere.** Every census used `grep -r` with paths, `grep -l`, or a Python/Node AST-ish scanner that carries filenames through.
- **grep is line-based** — this bit the review at §5 (2 sites missed on continuation lines) and would have bitten §1a (the `Promise.all` array destructure). Both censuses here use balanced-delimiter scanners, not line regexes.
- **`grep -F` for `[id]` paths** — `keys/[id]/permissions/route.ts` was passed as a literal argument to the scanners, never as a regex.
- **Every count above is re-runnable.** Scanners preserved at `/tmp/p140_4/{scan_supabase.py,logscan.mjs}`. They are **outside the repo** and were never `git add`ed; `.planning/` was not staged.
- **The falsification question, asked of each headline grep:** *would a broken implementation return this same value?*
  - `grep -c 'HTTPException(status_code=5' → 9`: **yes** — it returns 9 whether or not sites 10/11 exist. **Not evidence.** Replaced with a paren scanner.
  - `grep SEAM_FILES … length|toEqual → empty`: **yes, and that is the point** — emptiness IS the finding (no pin exists), and it is confirmed by the positive count `grep -c SEAM_FILES → 8` proving the symbol is present and merely unpinned. **Evidence, because the positive counterpart was measured.**
  - `derived set == SEAM_ROUTE_BUDGETS`: a broken deriver returning `[]` would give `derived − union = ∅` too. **Guarded by also printing `|derived| = 15 > 0` and `union − derived = {the 2 exclusions}`** — a vacuous deriver fails the second check. This is `resilient-fetch.wiring.test.ts:740-750`'s vacuity fence, applied to my own measurement.

---

## Metadata

**Search scope:** `src/**`, `analytics-service/**`, `tests/**`, `e2e/**`, `supabase/migrations/**`
**Files scanned:** ~2,400 (TS/TSX/PY/SQL/MD)
**Censuses run:** 8 populations + 4 analog verifications
**Source files modified:** **0** — read-only throughout
