---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G2
type: execute
wave: 2
depends_on: ["140.3-G1"]
gap_closure: true
autonomous: true
requirements: [GC-3, GC-4]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/components/charts/TouchTooltip.tsx
  - tests/visual/recharts-touchtooltip-usage.test.ts
  - .planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-16-SUMMARY.md
  - .planning/phases/140.3-seamux-client-wizard-seam-error-surface/deferred-items.md
  - .planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md

must_haves:
  truths:
    - "SC2's residual is recorded in a FALSIFIABLE form: the two codes that reach SubmitStep without a wizard member render as UNKNOWN by a deliberate, guarded decision — and admitting either one cannot land silently."
    - "The TRAP-4 coupling that makes the one-line fix unsafe is stated at the point where someone would make that edit."
    - "No production comment, guard docblock, or phase record in the touched files recites a count or a claim it does not own and cannot keep true."
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx"
      provides: "Negative pin: COMPOSITE_UNSUPPORTED_UNIFIED resolves to UNKNOWN, deliberately"
      contains: "COMPOSITE_UNSUPPORTED_UNIFIED"
    - path: ".planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-16-SUMMARY.md"
      provides: "An appended correction block naming the two false statements the gate SUMMARY carries"
    - path: ".planning/phases/140.3-seamux-client-wizard-seam-error-surface/deferred-items.md"
      provides: "DEF-G2-1 (the SC2 residual) and DEF-G2-2 (two partial source-scan guards routed to 140.4)"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx"
      to: "src/app/api/strategies/finalize-wizard/route.ts"
      via: "the 409 arm at :1551 that emits COMPOSITE_UNSUPPORTED_UNIFIED"
      pattern: "COMPOSITE_UNSUPPORTED_UNIFIED"
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx"
      to: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      via: "the failure message naming DESTRUCTIVE_CONTROL_IS_WRONG_FOR as the same-commit prerequisite"
      pattern: "DESTRUCTIVE_CONTROL_IS_WRONG_FOR"
---

<gap_id_mapping>
⚠️ **`GC-*`, not `GAP-*`.** `140.3-GAP-RESEARCH.md:120-122` binds `GAP-3` to **SEAMUX-03, explicitly
out of scope**. A reader cross-referencing `requirements: [GAP-3, GAP-4]` would have concluded
SEAMUX-03 was absorbed here. It is not, and Phase 140.4 owns it. Full mapping in
`140.3-G1-PLAN.md`. This plan closes:

- **GC-3** — the SC2 residual (`VERIFICATION.md` gaps[2])
- **GC-4** — the stale hand-maintained count class (`VERIFICATION.md` §5.4 + `GAP-RESEARCH.md` Q4)
</gap_id_mapping>

<objective>
Two small, independent closures the verifier surfaced and no SUMMARY records.

**GC-3 — SC2's unnamed residual.** `finalize-wizard/route.ts:1551` emits
`code: "COMPOSITE_UNSUPPORTED_UNIFIED"` on a 409. It is not a member of `KNOWN_FINALIZE_CODES`
(9 members, measured), so it renders as `UNKNOWN` and collapses the `wizard_error` funnel code —
touching SC2's recognition clause and SC6's specificity clause. The finding IS honestly recorded in
production source at `wizardErrors.ts:1543`; only the SC2 adjudication row omits it while SC4, SC6
and SC7 each name theirs.

**GC-4 — the stale hand-maintained count class.** Confirmed with a falsifiable claim: six other
prose counts in this repo were checked as controls and are sound, so this is a real class and not a
universal one. **Six live members**, all in the same shape — a claim written in prose that nothing
asserts, sitting next to a guard that asserts something else correctly. Four are in production or
test source; **two are in the phase gate's own SUMMARY**, including the sentence that asserts the
very property G1 proves false.

Purpose: convert one unrecorded residual into a guarded, deliberate decision, and remove six
copies-with-no-owner.
Output: one negative pin, four prose corrections, one appended SUMMARY correction block, two
deferred-items rows, one ledger row (M89).

⚠️ **Branch protection on `main` is OFF** by a settled founder decision. Every gate here is
**ADVISORY at merge** — it **would have caught** a regression; it cannot stop one.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VERIFICATION.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-GAP-RESEARCH.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-CONTEXT.md
@src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
@src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
</context>

<pre_measured_baseline>
**Measured on the UNTOUCHED tree at planning time (2026-07-28, HEAD `e93044c1`) and independently
re-measured by the plan-checker. Q11 below is the one row that failed to reproduce and has been
corrected — read its warning before writing any anchored grep. Re-run before asserting; do not
inherit.**

| # | Measurement | Command | **Pre-value** |
|---|---|---|---|
| Q1 | `COMPOSITE_UNSUPPORTED_UNIFIED` mentions in `SubmitStep.test.tsx` | `grep -c 'COMPOSITE_UNSUPPORTED_UNIFIED' 'src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx'` | **0** |
| Q2 | The emitting arm | `grep -n 'COMPOSITE_UNSUPPORTED_UNIFIED' src/app/api/strategies/finalize-wizard/route.ts` | **`:1551`**, inside a `NextResponse.json({error, code}, {status: 409, headers: NO_STORE_HEADERS})` |
| Q3 | `KNOWN_FINALIZE_CODES` members | read `SubmitStep.tsx:148-195` | **9**: `KEY_SCOPE_BROADENED`, `KEY_NETWORK_TIMEOUT`, `GATE_DRAFT_GONE`, `GUARD_BLOCKED`, `COMPOSITE_MEMBERSHIP_UNKNOWN`, `COMPOSITE_TOO_MANY_MEMBERS`, `SERVICE_UNAVAILABLE_RETRY`, `SERVICE_UNREACHABLE`, `SEAM_MISCONFIGURED` |
| Q4 | The sibling case that already pins the OTHER residual code | `grep -n 'draft_state_invalid' 'src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx'` | **`:513`** — `it("maps a 409 unknown code (draft_state_invalid) to UNKNOWN (recoverable)")`. ⚠️ **Half of GC-3's adjudication already exists.** Only `COMPOSITE_UNSUPPORTED_UNIFIED` is missing |
| Q5 | `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` | `grep -n 'DESTRUCTIVE_CONTROL_IS_WRONG_FOR' src/` | **`SyncPreviewStep.tsx:1341`**, exactly **2** members: `GATE_DRAFT_GONE`, `VALIDATION_FAILED` |
| Q6 | `SubmitStep.test.tsx` | `npx vitest run …` | **28 tests passed** |
| Q7 | `Object.keys(WIZARD_ERROR_COPY).length` | `npx tsx -e '…'` | **55** |
| Q8 | `EXPECTED_TABLE_SIZE` guard literals | `grep -n 'EXPECTED_TABLE_SIZE' src/lib/wizardErrors.test.ts` | **`:1348` = 55**, **`:1491` = 55** — both CORRECT, both hand-typed **on purpose**. Do NOT touch them |
| Q9 | Stale prose #1 (production source) | `grep -c 'size guard stays at 53' 'src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx'` | **1**, at `:165` — *"and the copy table's hand-typed size guard stays at 53."* False at HEAD (55) |
| Q10 | Stale prose #2 (production source) | `grep -c 'across 18 files' src/components/charts/TouchTooltip.tsx` | **1**, at `:10` |
| Q10b | Stale prose #2b — same line, second number | `grep -c '18× inline' src/components/charts/TouchTooltip.tsx` | **1**, at `:10` |
| Q11 | Stale prose #3 — ⚠️ **CORRECTED AT REVIEW. READ THIS.** | `grep -c '18 charts' src/components/charts/TouchTooltip.tsx` | **1**, at `:38`. **The earlier anchor in this plan was `'all 18 charts'`, whose pre-value is 0 — the sentence wraps across lines 37/38 and grep is line-based.** That criterion was satisfiable by deleting nothing, on an untouched tree. It is DEF-16-2 recurring inside the plan that cites DEF-16-2. `grep -c 'all 18'` on this file → **0**; `grep -c '18 charts'` → **1** |
| Q12 | Stale prose #4 (test docblock) | `grep -c 'all 18 tooltip-bearing' tests/visual/recharts-touchtooltip-usage.test.ts` | **1**, at `:11` |
| Q13 | Actual `TouchTooltip` importers | `grep -rlE 'import .*TouchTooltip.* from\|from "@/components/charts/TouchTooltip"' --include='*.tsx' src/ \| grep -v '\.test\.' \| grep -v 'TouchTooltip.tsx' \| wc -l` | **20** |
| Q14 | `recharts-touchtooltip-usage.test.ts` | `npx vitest run …` | **2 tests passed** |
| Q15 | The guard next door, and why the 18 could drift unseen | read `recharts-touchtooltip-usage.test.ts:59-65` | it asserts an allow-list of **one** (`toEqual([ALLOWED])`) and **never touches the number 18** |
| Q16 | Stale prose #5 — the gate SUMMARY's false property claim | `grep -c 'zero .fetch(. calls of any kind' …/140.3-16-SUMMARY.md` (adjust quoting; the sentence is at **`:90`**) | **1** — *"contains zero `fetch(` calls of any kind. Every read is `@/lib/supabase/client`."* **False**: 3 calls, 2 to `/api/keys/sync` (G1's P4/P5). Named verbatim in `VERIFICATION.md` gaps[1] `artifacts[1].issue` |
| Q17 | Stale prose #6 — the gate SUMMARY's stale table count | `grep -n 'all \*\*54\*\* table entries' …/140.3-16-SUMMARY.md` | **`:283`** (and the same 54 recurs at **`:325`**). Actual **55** (Q7/Q8) |
| Q18 | `deferred-items.md` size and the DEF-15-1 owner | `wc -l` and `sed -n '43p'` | **122** lines; owner line reads *"SEAMUX-01 / `140.3-16` to route"* — **and `140.3-16` completed without routing it**, so that owner is a closed plan |
| Q19 | Highest Falsifiability Ledger row | inspection of `140.3-VALIDATION.md` | **M85c** at plan time; **G1 adds M86, M86b, M87, M88**, so this plan's row is **M89** |
| Q20 | Sign-off inconsistency to reconcile (pre-existing) | `sed -n '958,966p' .../140.3-VALIDATION.md` | two boxes UNTICKED — *"Oracle Independence checklist complete"* and *"`nyquist_compliant: true` set in frontmatter"* — plus **"Approval: pending"**, while the frontmatter says `nyquist_compliant: true` and the Oracle Independence section at `:882-889` is fully `[x]` |
</pre_measured_baseline>

<hard_constraints>
1. **`vi.spyOn` + `restoreAllMocks`, NEVER `vi.stubGlobal`** (DEF-16-1). `SubmitStep.test.tsx`
   already uses `vi.spyOn(globalThis, "fetch")` — match it.
2. **Zero new packages. Zero `.py` files.** Do not run `mypy`/`pytest` as a gate.
3. **`.planning/` is gitignored, `commit_docs: false`** — never `git add .planning`. Edit
   `.planning/` files with targeted edits and appends only; **never a whole-file rewrite, never
   `head|sed` pipes.** A truncating rewrite has destroyed a ledger on this project before and
   there is no git backup.
4. **Never run `gsd-sdk query state.*`**; do not touch `STATE.md` or `ROADMAP.md`.
5. **Do NOT change `KNOWN_FINALIZE_CODES`.** See the explicit decision below. This plan makes a
   test-and-documentation change only; `SubmitStep.tsx` is not in `files_modified`.
6. **Do NOT touch the two `EXPECTED_TABLE_SIZE` literals** (Q8). They are hand-typed deliberately.
   Deriving them re-creates `expect(found.length).toBe(discovered.length)` — the shape both seam
   pins forbid by name. `140.3-15`'s docblock records re-running the reasoning *before* bumping the
   literal, which the verifier called *"exactly right"*. The defect is prose held to a lower
   standard than the guard beside it, not the hand-typed number.
7. **SEAMUX-03, `complete-status-scan.test.ts`, and `analytics-service/` are OUT OF SCOPE** →
   Phase 140.4.
8. **Anchored-grep hygiene — this plan already produced one failure of it (Q11).** Before writing
   any acceptance grep: run it on the untouched tree; confirm the pre-value is NOT already the pass
   value; and **never anchor on a phrase that may wrap across a line break**, because grep is
   line-based. If a target phrase spans lines, anchor on the fragment that lives on one line, or
   use `grep -z`, or use a two-line window. Ask of every criterion: *could an untouched or broken
   tree return this same value?*
</hard_constraints>

<decision_gc3_explicit>
## The GC-3 fork, weighed and decided

The verifier offers two routes: **(a)** admit both codes to `KNOWN_FINALIZE_CODES`, or **(b)**
record the residual against SC2 the way SC4/SC6/SC7 record theirs.

**DECISION: (b) adjudication — with a falsifiable guard, not just prose. Explicitly NOT (a).**

Reasoning, stated so it can be overturned on evidence rather than re-litigated:

1. **TRAP-4 is a locked CONTEXT decision** (`CONTEXT.md:59-62`) and it bites here. `140.3-15`
   refused the adjacent one-liner DEF-15-1 for exactly this reason, and the verifier
   **ENDORSED the refusal**: `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` holds only 2 members (Q5), so a new
   non-recoverable member can leave *"Try another key"* → `handleDeleteDraft()` as the sole
   remaining control. Admitting a code is a code-set change and would require widening that set in
   the same commit.
2. **The residual is already honest in production source** (`wizardErrors.ts:1543` names both codes
   and says they are deliberately out of scope). Rule 3 — surgical — argues against a behaviour
   change to close a documentation gap.
3. **Half the adjudication already exists** (Q4): `SubmitStep.test.tsx:513` pins
   `draft_state_invalid` → `UNKNOWN`. The gap is one missing sibling case, not a design decision.
4. **UNKNOWN is recoverable**, so the user still gets a Retry affordance today. The cost of
   deferring is specificity, not a dead end.

⚠️ **This decision is scoped to gap closure and is not a permanent verdict.** If the founder wants
the user-visible improvement (specific copy instead of `UNKNOWN`), route (a) is the right follow-up
and the pin written here is precisely what forces it to be done safely: it reddens the moment the
code is admitted, with a failure message naming the same-commit prerequisite. That is the whole
point of pinning a deliberate residual rather than writing it in a document.
</decision_gc3_explicit>

<tasks>

<task type="auto">
  <name>Task 1: GC-3 — pin the SC2 residual as a deliberate, guarded decision</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx, .planning/phases/140.3-seamux-client-wizard-seam-error-surface/deferred-items.md</files>
  <action>
**Part A — the negative pin.**

Add ONE case to `SubmitStep.test.tsx`, placed immediately next to its sibling at `:513`
(`draft_state_invalid` → UNKNOWN) so the two residuals read as one recorded pair. Copy that case's
shape exactly: `vi.spyOn(globalThis, "fetch").mockResolvedValue(jsonResponse({error, code}, 409))`,
`renderStep()`, click `wizard-submit-for-review`, `await vi.waitFor(() => expect(findWizardError())
.toBeDefined())`, then assert the surfaced code.

The response body must be the real one the route sends (Q2, read it at
`finalize-wizard/route.ts:1545-1553`): `error` = *"Composite (multi-key) strategies are not yet
supported on this path."*, `code` = `COMPOSITE_UNSUPPORTED_UNIFIED`, status 409.

Assert `findWizardError()!.code` is `"UNKNOWN"`.

⚠️ **The failure message is the deliverable here, not the assertion.** An assertion that
"UNKNOWN is what happens" reads as blessing a defect. The message must make the next reader
understand that this is a RECORDED residual with a named prerequisite. It must state, in this
order: (1) `finalize-wizard:1551` emits this code on the live unified-backbone path, so a real user
can reach it; (2) it is deliberately NOT a member of `KNOWN_FINALIZE_CODES`, so it renders as
`UNKNOWN` and the `wizard_error` funnel reports `UNKNOWN` — SC2's recognition clause and SC6's
specificity clause; (3) **if you are reading this because you just admitted the code, that is the
right fix and this test is doing its job** — check `DESTRUCTIVE_CONTROL_IS_WRONG_FOR`
(`SyncPreviewStep.tsx:1341`, 2 members) **in the same commit**, because a new non-recoverable member
can leave a destructive control as the sole affordance (TRAP-4, a locked CONTEXT decision, and the
exact coupling DEF-15-1 records for the sibling code set); (4) then update this test rather than
deleting it.

Give the case a name that says it is deliberate, not accidental — something in the shape of
*"[140.3-G2] the unified-backbone 409 is a RECORDED residual: it resolves to UNKNOWN until TRAP-4 is
cleared"*.

Add a short comment above the case cross-referencing the `:513` sibling so the pair is discoverable
from either end.

**Part B — two deferred-items rows.**

Append to `.planning/phases/…/deferred-items.md`, matching the house style of the DEF-11-1 /
DEF-15-1 / DEF-16-1 entries already there (a measured statement, why it is not fixed here, and a
named owner). **Append only. Do not rewrite the file.** Pre-value 122 lines (Q18) — the count must
go up, never down.

**`### DEF-G2-1` — the SC2 residual.** Record: the two codes; the measured 9-member
`KNOWN_FINALIZE_CODES` (Q3); that `wizardErrors.ts:1543` already documents them in production
source; the TRAP-4 coupling and the 2-member `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` (Q5); that
`draft_state_invalid` was already pinned at `:513` and `COMPOSITE_UNSUPPORTED_UNIFIED` now is too.

⚠️ **Owner: `Phase 140.4`, named explicitly.** Do NOT write "the same follow-up that owns DEF-15-1"
or any indirection. Measured (Q18): DEF-15-1's own owner line reads *"SEAMUX-01 / `140.3-16` to
route"*, and `140.3-16` **completed without routing it** — so that owner resolves to a closed plan
and the item has no live home. Naming a phase directly is what stops the same thing happening
twice. Both items are the same TRAP-4 coupling on sibling code sets and should be done together;
say so, and say that doing them separately trips TRAP-4 twice.

**`### DEF-G2-2` — two partial source-scan guards, routed to 140.4.** `GAP-PATTERNS` §7.2 ranks
these 2nd and 3rd by exposure and this plan set closes neither. **Recording them is the point:
"unmentioned" must not be allowed to read as "unfound".**

- `src/lib/seam-log-coverage.test.ts:329` — `CREDENTIAL_BEARING_CALLS` matches the literal strings
  `"validateKey("` / `"encryptKey("` via `code.includes(call)`. Correct today, but an aliased
  import or a new local helper defeats it (`GAP-PATTERNS` §2.4). **No live violation.**
- `src/lib/seam-budgets.invariant.test.ts:755` — `/\bresilientFetch\s*\(/` plus the import-edge
  check at `:749` **provably do not compose**: a path calling the core through `analyticsRequest(`
  or any of the 9 public `analytics-client` wrappers passes both (`GAP-PATTERNS` §2.5). **No live
  violation.**

State for both: no live violation at HEAD, the wrapper-aware idiom to copy is
`audit-coverage.test.ts:309` + `:431-441`, and the owner is **Phase 140.4**, alongside
`complete-status-scan.test.ts` (6 live invisible sites, §7.2 rank 1) and the ~50 unmapped Python
source-reading tests.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx'</automated>
  </verify>
  <done>
`SubmitStep.test.tsx` reports **29 passed** (pre-value 28, Q6).
`grep -c 'COMPOSITE_UNSUPPORTED_UNIFIED' 'src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx'`
is **≥ 2** (pre-value **0**, Q1 — the code literal in the mocked body plus at least one mention in
the failure message). `deferred-items.md` carries both a `DEF-G2-1` and a `DEF-G2-2` heading, names
`140.4` as the owner of each, and its line count **increased** from 122 (Q18) — a decrease means the
append truncated something.
  </done>
</task>

<task type="auto">
  <name>Task 2: GC-4 — delete the six unowned prose claims and name the guard instead</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/components/charts/TouchTooltip.tsx, tests/visual/recharts-touchtooltip-usage.test.ts, .planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-16-SUMMARY.md</files>
  <action>
Six sites recite a count or a property claim they do not own and no oracle can keep true. Two
guards sit right next to two of them asserting a *different* number correctly — which is exactly
why the prose could drift unseen.

**The rule to apply (`GAP-RESEARCH` Q4): delete the number from the prose and name the guard
instead.** A pointer cannot drift. The repo has a drift-detector idiom
(`src/__tests__/security-packet-date-drift.test.ts`) for values that genuinely must appear in two
artifacts — a date that must show on a PDF *and* a web page. A count in an explanatory aside is not
that. **Do not build a drift test for a comment.**

### Part A — the four source sites

| Site | Current (measured) | Change |
|---|---|---|
| `SyncPreviewStep.tsx:165` (Q9) | *"and the copy table's hand-typed size guard stays at 53."* — false at HEAD, actual 55 | Replace the number with a pointer to the guard by symbol and file: the copy table's hand-typed size guard (`EXPECTED_TABLE_SIZE` in `src/lib/wizardErrors.test.ts`) is not touched by this. **Do not write 55.** Writing 55 fixes today and re-arms the same trap for the next `WizardErrorCode` |
| `TouchTooltip.tsx:10` (Q10 + Q10b) | *"across 18 files). The DRY anchor that avoids 18× inline `trigger` duplication"* — **two** numbers on one line | Drop **both**. Say the swap covers every tooltip-bearing recharts chart and that the enforced contract is the one-file allow-list in `tests/visual/recharts-touchtooltip-usage.test.ts` |
| `TouchTooltip.tsx:37-38` (Q11) — ⚠️ **not in the pattern map; found at planning time. TRAP-5: fix the class, not the instance** | *"(none do today — all / 18 charts pass only `formatter` + `contentStyle`)"* — **the sentence wraps across two lines** | Drop the number; keep the substantive claim (no caller overrides `trigger` today) |
| `recharts-touchtooltip-usage.test.ts:11` (Q12) | *"across all 18 tooltip-bearing charts"* | Drop the number. ⚠️ Note what the guard beside it actually asserts (Q15): an allow-list of **one**, `toEqual([ALLOWED])`, which never reads the number 18 — say that, so the next reader knows the count was never enforced and should not be reintroduced |

**Do not change any assertion in `recharts-touchtooltip-usage.test.ts`.** This task edits its
docblock prose only. TRAP-9: an assertion edit here would delete a working fence.

### Part B — the two phase-record sites (B3)

Both are named inside the gaps this plan set is closing and neither has been covered anywhere else.
`140.3-16-SUMMARY.md` currently asserts, in the phase gate's own words, the exact property that
G1 proves false.

**Method: APPEND a correction block. Never rewrite the body.** This is `GAP-RESEARCH` Open Question
4's recommendation verbatim, it matches `140.3-16`'s own Deviation 3, and it matches the
`140.3-CONTEXT.md` precedent where the locked text is preserved for provenance and a dated
`CORRECTIONS` block governs over it. A delivered SUMMARY is history; history gets annotated, not
edited.

Append a `## ⚠️ CORRECTIONS — 2026-07-28, at gap closure (plan 140.3-G2)` section at the very end
of `140.3-16-SUMMARY.md` stating that where the body below conflicts with a correction here, **the
correction governs**, and carrying exactly two entries:

1. **`:90` — the poll-chain property claim is FALSE.** Quote it, then state the measurement:
   `SyncPreviewStep.tsx` makes three network calls, two of them to `/api/keys/sync` — a seam route
   — and one on the repeating tick at `:745`. Every one is spelled `wizardFetch(`, which is why the
   pin's `/\bfetch\s*\(/` needle could not see them. Name where it is now proven properly:
   `SyncPreviewStep.poll-disjointness.runtime.test.tsx` (tick-scoped, plan G1) and the narrowed
   source backstop in `seam-poll-disjointness.pin.test.ts`. Give the re-runnable command.
   ⚠️ **This is the sentence `VERIFICATION.md` gaps[1] `artifacts[1].issue` names verbatim** — say so.
2. **`:283` and `:325` — the table-entry count is stale by one.** Both read 54; the measured value
   is **55**, and both `EXPECTED_TABLE_SIZE` guard literals are correctly at 55 and passing (Q7/Q8).
   Per `GAP-RESEARCH` Q4's recommendation, **cite the guard, not a transcribed integer**: state that
   the scan iterates `Object.entries(WIZARD_ERROR_COPY)` and is bounded by `EXPECTED_TABLE_SIZE` in
   `src/lib/wizardErrors.test.ts`, and that the correct way to read the receipt is through that
   symbol. You may state 55 once here, as a dated correction with its command — that is a
   measurement in a dated record, not a recited count in normative prose.

⚠️ **Anchored-grep trap — read before writing the criteria (this plan already failed it once).**
`TouchTooltip.tsx:22` legitimately contains the token `18` inside the file reference
`useBreakpoint.ts:18-30`, so a criterion of `grep -c '18' TouchTooltip.tsx` → 0 is **unsatisfiable
by correct code**. And the `18 charts` sentence **wraps across lines 37/38**, so the natural anchor
`'all 18 charts'` has a pre-value of **0** and would pass on an untouched tree — that is exactly
DEF-16-2, and it was the one measurement in this plan set that failed the checker's reproduction.
Every anchor in the `<done>` block below was re-measured after that correction. Use them as written.
  </action>
  <verify>
    <automated>npx vitest run tests/visual/recharts-touchtooltip-usage.test.ts src/lib/wizardErrors.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>
All five source anchors return **0**, each from a re-measured pre-value of **1**:

- `grep -c 'size guard stays at 53' 'src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx'` → **0** (pre **1**, Q9)
- `grep -c 'across 18 files' src/components/charts/TouchTooltip.tsx` → **0** (pre **1**, Q10)
- `grep -c '18× inline' src/components/charts/TouchTooltip.tsx` → **0** (pre **1**, Q10b)
- `grep -c '18 charts' src/components/charts/TouchTooltip.tsx` → **0** (pre **1** at `:38`, Q11 — **note the anchor is `18 charts`, NOT `all 18 charts`, whose pre-value is already 0**)
- `grep -c 'all 18 tooltip-bearing' tests/visual/recharts-touchtooltip-usage.test.ts` → **0** (pre **1**, Q12)

And no new count was introduced in their place:
`grep -cE '\b(55|20) (files|charts|entries|importers)\b'` on all three source files → **0** each.

`140.3-16-SUMMARY.md` carries a `## ⚠️ CORRECTIONS` heading; its line count **increased**; and the
body sentence at `:90` is **byte-unchanged** (preserved for provenance — verify with
`grep -c 'zero' …` returning its original value, or by confirming the file's first 400 lines are
unmodified).

`recharts-touchtooltip-usage.test.ts` still reports **2 passed** (Q14) with its assertions
byte-unchanged (`git diff` on that file shows comment lines only).
`src/lib/wizardErrors.test.ts` is untouched and its two `EXPECTED_TABLE_SIZE = 55` literals are
still present and still passing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Falsify the GC-3 pin (M89) and record the addendum honestly</name>
  <files>.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md</files>
  <action>
Append row **M89** to the `## Gap-closure addendum` section that plan `140.3-G1` created in
`140.3-VALIDATION.md`. If that section is absent, G1 did not complete — stop and report rather than
creating a second addendum section.

**M89 — the mutation must prove the pin is LIVE, not merely that the codebase is clean.**

| Field | Value |
|---|---|
| Target | `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx` — **production source** |
| Mutation | Add `"COMPOSITE_UNSUPPORTED_UNIFIED"` as a member of `KNOWN_FINALIZE_CODES` (a one-token semantic change, drawn from the subject's own vocabulary — it is the exact edit route (a) would make) |
| Must redden | Task 1's new case, with its own failure message naming the TRAP-4 prerequisite |
| Must stay GREEN | The sibling `draft_state_invalid` case at `:513` (proving the two residuals are independently covered, not one assertion doing double duty), and the `[140.3-05] an unrecognised code still resolves to UNKNOWN` case at `:214` (proving the membership check itself is untouched) |
| Why this shape | An inert pin and a codebase with no such code both produce "resolves to UNKNOWN". The only way to tell them apart is to make the code recognised and watch the pin fail. This is the M77c lesson applied to a negative pin: only a positive case can falsify an absence |

Record the exact command, the pasted RED output with the failing test name and the
`N failed | M passed` counts, the two green-side observations by name, and the restoration receipt
(`git diff --stat -- 'src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx'` empty;
`grep -rn MUTANT src/` → 0).

⚠️ If the mutation reddens **more** than the one new case plus whatever legitimately depends on the
code set, note which and why — an over-broad blast radius is itself a finding about coupling.

**Also record in the same addendum, explicitly:**

1. **Why GC-4 carries no M-row.** Prose deletions have no runtime behaviour to mutate, and per
   `GAP-RESEARCH` Q4 the deliberate outcome is a pointer rather than a new guard. Their receipts are
   the five anchored phrase greps in Task 2, each with a re-measured pre-value. **Say this
   explicitly** — an unexplained absence in a ledger reads as a skipped row. Include the DEF-16-2
   recurrence: the first draft of one of those five anchors (`'all 18 charts'`) had a pre-value of
   **0** because the sentence wraps across lines 37/38, so it was satisfiable on an untouched tree.
   It was caught at plan review and corrected to `'18 charts'` (pre-value 1). Recording a
   near-miss that was caught is worth more than recording only the ones that landed.
2. **Reconcile the sign-off block (Q20, pre-existing).** `140.3-VALIDATION.md` around `:958-966`
   leaves *"Oracle Independence checklist complete"* and *"`nyquist_compliant: true` set in
   frontmatter"* UNTICKED with **"Approval: pending"**, while the frontmatter says
   `nyquist_compliant: true` and the Oracle Independence section at `:882-889` is fully `[x]`. Do
   **not** silently tick the boxes and do **not** edit the frontmatter. State the contradiction in
   the addendum, name which side the evidence supports (the `:882-889` section and the frontmatter
   both say complete; only the summary checklist lags), and leave the resolution to the phase owner.
   A checklist that contradicts its own document is the same defect class this plan set exists to
   close, one level up.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx' && git diff --stat -- 'src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx'</automated>
  </verify>
  <done>
M89 is recorded with pasted RED output, both named green-side observations, and a clean restoration
receipt. The GC-4 no-row explanation is present and includes the corrected-anchor near-miss. The
Q20 sign-off contradiction is stated without being silently resolved. `SubmitStep.tsx` shows an
empty diff.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| `finalize-wizard` route → `SubmitStep` | A wire `code` crosses here and is translated into a wizard code; an unrecognised one collapses to `UNKNOWN` |
| error copy → user action | The affordances an error state renders are the user's only exits; a destructive one as the sole exit is TRAP-4 |
| phase record → future reader | A SUMMARY is trusted as the account of what shipped; a false property claim in one propagates into the next plan's assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-140.3G2-01 | **Denial of Service (user data)** | `SubmitStep` / `SyncPreviewStep` error affordances | **mitigate by refusal** | Admitting a new non-recoverable code without widening `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` (2 members, Q5) can leave a destructive control as the sole exit. This plan therefore does NOT change the code set; it installs a pin that reddens the moment someone does, naming the same-commit prerequisite. Falsified by M89 |
| T-140.3G2-02 | **Repudiation** | funnel telemetry | accept, recorded | `wizard_error {code:"UNKNOWN"}` on a reachable 409 means the funnel cannot distinguish this arm. Accepted for now; recorded as DEF-G2-1 with **Phase 140.4** as a live owner, not an indirection to a closed plan |
| T-140.3G2-03 | Tampering (documentation decay) | production comments and the phase SUMMARY | mitigate | Delete the unowned copies; name the guard by symbol; append a dated correction block to the SUMMARY rather than rewriting history |
| T-140.3G2-04 | **Tampering** (guard decay, deferred) | `seam-log-coverage.test.ts:329`, `seam-budgets.invariant.test.ts:755` | **transfer → Phase 140.4** | Both are PARTIAL needles defeatable by a wrapper; neither has a live violation at HEAD. Recorded as DEF-G2-2 so "unmentioned" cannot read as "unfound" |
| T-140.3G2-05 | Tampering | npm/pip/cargo installs | mitigate | **Zero packages proposed.** No install task exists in this plan |
</threat_model>

<verification>
1. `BASE=$(git rev-parse HEAD)` before any edit; record it in the SUMMARY.
2. `npx vitest run 'src/app/(dashboard)/strategies/new/wizard/steps/' tests/visual/ src/lib/wizardErrors.test.ts` — green.
3. `npx tsc --noEmit` — exit 0.
4. `npm run lint` — 0 errors, 1 pre-existing warning (`EquityChart.tsx`). Do not substitute a bare
   `eslint` call.
5. `npm test` — no decrease against the 9779 / 287 baseline; +1 from Task 1.
6. `git diff --stat $BASE..HEAD -- package.json package-lock.json` — **empty**.
7. `git diff --name-only $BASE..HEAD | grep -c '\.py$'` — **0**.
8. `git diff --name-only $BASE..HEAD | grep -c '^\.planning/'` — **0** (gitignored; never
   `git add .planning`).
9. `git diff --name-only $BASE..HEAD | grep -c 'SubmitStep\.tsx$'` — **0**. This plan changes no
   production behaviour in `SubmitStep`; if that file is in the diff, the M89 mutation was not
   reverted or route (a) was taken without the decision being revisited.
10. `grep -rn MUTANT src/` — **0**. `git status --short | wc -l` — **exactly 2**, and the two lines
    are the documented pre-existing ones (` M TODOS.md`,
    `?? analytics-service/scripts/nautilus_factsheet.py`). Stating the expected count matters: an
    unqualified "clean tree" check passes on a tree carrying unrelated damage.
</verification>

<success_criteria>
- [ ] `SubmitStep.test.tsx`: **29 passed** (pre **28**, Q6), the new case named so it reads as a
      deliberate recorded residual, placed beside its `:513` sibling
- [ ] The new case's failure message names `finalize-wizard:1551`, `KNOWN_FINALIZE_CODES`,
      `DESTRUCTIVE_CONTROL_IS_WRONG_FOR` and TRAP-4, and tells a future editor to widen the
      destructive-control set **in the same commit**
- [ ] `KNOWN_FINALIZE_CODES` is **unchanged** — 9 members (Q3); `SubmitStep.tsx` absent from the diff
- [ ] `DEF-G2-1` recorded with **`Phase 140.4`** named directly as owner — not an indirection to
      DEF-15-1, whose own owner (Q18) resolves to the already-closed `140.3-16`
- [ ] `DEF-G2-2` recorded, routing `seam-log-coverage.test.ts:329` and
      `seam-budgets.invariant.test.ts:755` to Phase 140.4 with "no live violation at HEAD" stated
- [ ] `deferred-items.md` line count increased from **122** (Q18); never decreased
- [ ] All **five** anchored stale-phrase greps → **0**, each from a re-measured pre-value of **1**
      (Q9, Q10, Q10b, Q11, Q12), and no replacement number was written in their place. The
      `18 charts` anchor is used, **not** `all 18 charts` (pre-value 0 — the sentence wraps)
- [ ] `140.3-16-SUMMARY.md` carries an appended dated `## ⚠️ CORRECTIONS` block covering **both**
      `:90` (the false zero-fetch property claim, named verbatim in `VERIFICATION.md` gaps[1]) and
      `:283`/`:325` (54 → 55, cited through `EXPECTED_TABLE_SIZE`). The body is **byte-unchanged**
- [ ] The two `EXPECTED_TABLE_SIZE = 55` literals are untouched (Q8) and `wizardErrors.test.ts` is
      absent from the diff
- [ ] `recharts-touchtooltip-usage.test.ts` assertions byte-unchanged; still **2 passed** (Q14)
- [ ] M89 recorded with pasted RED output plus **both** named green-side observations
      (`:513` and `:214`), and a clean restoration receipt
- [ ] The addendum explains why GC-4 carries no M-row **and** records the corrected-anchor near-miss
      (`'all 18 charts'` pre-value 0 → `'18 charts'` pre-value 1) as a DEF-16-2 recurrence
- [ ] The addendum states the Q20 sign-off contradiction without silently ticking the boxes or
      editing the frontmatter
- [ ] SUMMARY uses **"would have caught"**, never "did stop"
- [ ] SEAMUX-03 untouched; `complete-status-scan.test.ts` untouched; `analytics-service/` untouched
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G2-SUMMARY.md` when done.

Commit source files only (`commit_docs: false`; everything under `.planning/` is gitignored and is
never added). Each commit its own step — edit, verify, then commit; never bundle a commit with edits:
- `test(140.3-G2): pin the SC2 residual — COMPOSITE_UNSUPPORTED_UNIFIED resolves to UNKNOWN by decision`
- `docs(140.3-G2): drop five unowned prose counts, name the guard instead`
</output>
