---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: G1
type: execute
wave: 1
depends_on: []
gap_closure: true
autonomous: true
requirements: [GC-1, GC-2]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx
  - src/lib/seam-poll-disjointness.pin.test.ts
  - src/__tests__/contracts/contracts-registry.test.ts
  - src/__tests__/contracts/REGISTRY.md
  - .planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md

must_haves:
  truths:
    - "The retry-storm fence can fail when the repeating poll tick is retargeted at a seam route THROUGH THE WRAPPER the poll path actually uses (wizardFetch), not only through a literal `fetch(` that the poll path never writes."
    - "The fence permits the user-initiated kickoff's real seam call (/api/keys/sync) while refusing a tick-phase one — i.e. the guarded property is tick-scoped, not file-scoped."
    - "The source pin no longer asserts a claim that is false at HEAD ('ZERO fetch calls exist on the poll path')."
    - "The source pin's needle cannot go blind silently: a needle that finds nothing in a corpus that demonstrably contains 11 call sites fails loud."
    - "The import-edge predicate is still present but no longer reads as retry-storm coverage; its zero-population nature is stated with a re-runnable command rather than a transcribed integer."
    - "None of the four src/lib/*.pin.test.ts guards, nor the new runtime guard, can be deleted or renamed with CI green."
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx"
      provides: "Runtime, tick-scoped, wrapper-agnostic poll-disjointness fence"
      contains: 'vi.spyOn(globalThis, "fetch")'
      min_lines: 150
    - path: "src/lib/seam-poll-disjointness.pin.test.ts"
      provides: "Source backstop: hand-typed URL allow-list + needle yield bound + needle self-test"
      contains: "ALLOWED_POLL_PATH_URLS"
    - path: "src/__tests__/contracts/contracts-registry.test.ts"
      provides: "Registry entries for the three unregistered pins + the new runtime guard"
    - path: "src/__tests__/contracts/REGISTRY.md"
      provides: "Human-readable rows for the same four new guards"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx"
      to: "src/app/api/**/route.ts"
      via: "seam-route set derived from disk by import edge, never hand-typed"
      pattern: "analytics-client|process-key-client|resilient-fetch"
    - from: "src/lib/seam-poll-disjointness.pin.test.ts"
      to: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      via: "comment-stripped /api/ URL-literal extraction over the hand-typed roster"
      pattern: "ALLOWED_POLL_PATH_URLS"
    - from: "src/__tests__/contracts/contracts-registry.test.ts"
      to: "src/lib/*.pin.test.ts"
      via: "CONTRACT_GUARDS existsSync entry per guard"
      pattern: 'path: "src/lib/seam-poll-disjointness.pin.test.ts"'
---

<gap_id_mapping>
⚠️ **This plan set uses `GC-*` IDs, NOT `GAP-*`.** `140.3-GAP-RESEARCH.md:120-122` already binds
`GAP-1/2/3` to a **different** mapping in which **`GAP-3` = SEAMUX-03, explicitly out of scope**. A
reader cross-referencing the two would conclude SEAMUX-03 was absorbed. It was not.

| This plan set | Work | Closes |
|---|---|---|
| **GC-1** | Poll-pin re-tiering | `VERIFICATION.md` gaps[1] — *"The poll-disjointness negative pin can fail when the retry-storm hazard it exists to prevent is introduced"* |
| **GC-2** | Contracts-registry entries | `GAP-RESEARCH.md` **Rider 4** |
| **GC-3** | SC2 residual adjudication | `VERIFICATION.md` gaps[2] (= GAP-RESEARCH's `GAP-2`) |
| **GC-4** | Stale hand-maintained counts | `VERIFICATION.md` §5.4 + `GAP-RESEARCH.md` Q4 |
| **GC-5** | DESIGN.md item 8 | `VERIFICATION.md` §5.2 + `GAP-RESEARCH.md` Q5 — **removed from the execution queue**, see `140.3-G3-DECISION-BRIEF.md` |

**SEAMUX-03 (`GAP-RESEARCH.md`'s `GAP-3`) is OUT OF SCOPE and owned by Phase 140.4.** No `GC-*` ID
covers it and none should.
</gap_id_mapping>

<objective>
Repair the phase gate's own inert fence by **re-tiering it**, and stop any of this phase's
guards from being deletable with green CI.

`src/lib/seam-poll-disjointness.pin.test.ts` claims *"ZERO fetch calls exist on the poll path."*
That claim is **FALSE at HEAD**: `SyncPreviewStep.tsx` — a file on the pin's own roster — makes
three real network calls, two of them to `/api/keys/sync`, a seam route. The pin passes anyway
because its needle is `/\bfetch\s*\(/` and every one of those calls is spelled `wizardFetch(`.

This is **not a regex that is slightly too narrow.** Broadening the needle makes the pin go RED on
a clean tree, because the property it states is false at file granularity. The property that is
actually true and worth protecting is **tick-scoped**: *the repeating poll tick must not target a
seam route.* Tick-ness is a runtime fact about a callback, not a lexical fact about a file.

Purpose: restore a fence that can fail in the hazard's real shape, and register the guard set so a
repaired guard cannot then be silently deleted.
Output: one new runtime guard file, a repaired + honestly-scoped source backstop, four registry
entries, and four Falsifiability Ledger rows (M86, M86b, M87, M88).

⚠️ **No user is at risk today.** The safety property holds at HEAD — the tick's real target
`/api/strategies/[id]/sync-progress` imports no seam module (measured: `grep -c` → **0**). Only the
guard is inert. Do not write this up as a vulnerability fix.

⚠️ **Branch protection on `main` is OFF** by a settled founder decision. Every gate below is
**ADVISORY at merge**. Each one **would have caught** a regression; none of them **can stop** one.
Use that language in the SUMMARY. Do not re-raise the decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VERIFICATION.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-GAP-RESEARCH.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-GAP-PATTERNS.md
@src/lib/seam-poll-disjointness.pin.test.ts
@src/lib/seam-ssr-exposure.pin.test.ts
@src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.progress.render.test.tsx
@src/__tests__/contracts/contracts-registry.test.ts
</context>

<prerequisites>
Verify first-hand, do not assume. All were true at planning time (2026-07-28, HEAD `e93044c1`,
branch `feat/v1.16-production-resilience`):

1. `src/lib/wizard/wizard-correlation.ts:58` exports `wizardFetch`, whose body calls the
   **unqualified global** `fetch` — this is why a `globalThis.fetch` spy sees wrapper-issued calls.
2. `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.progress.render.test.tsx`
   exists (710 lines) and uses `vi.spyOn(globalThis, "fetch")` at `:125` with
   `vi.useRealTimers(); vi.restoreAllMocks();` in `afterEach` — the idioms to copy.
3. `src/app/api/strategies/[id]/sync-progress/route.ts` exists and imports no seam module.
4. `git status --short` shows only the two documented pre-existing entries (`TODOS.md` modified,
   `analytics-service/scripts/nautilus_factsheet.py` untracked). If anything else is dirty, stop.
</prerequisites>

<pre_measured_baseline>
**Every number below was measured on the UNTOUCHED tree at planning time and independently
re-measured by the plan-checker; exactly one row in this plan set failed to reproduce and it was in
G2, not here. Re-run any you rely on — do not inherit.**

| # | Measurement | Command | **Pre-value** |
|---|---|---|---|
| P1 | Shipped needle `/\bfetch\s*\(/` yield across the 19 production files under `src/app/(dashboard)/strategies/new` (comment-stripped) | node walk + regex, §Q1 of GAP-RESEARCH | **0 sites, 0 files** |
| P2 | Wrapper-aware needle yield on the SAME corpus | same walk, needle matching an identifier suffix `fetch`/`Fetch` | **11 sites, 7 files** |
| P3 | Distinct callee names among those 11 | `grep -hoE '[A-Za-z0-9_$.]*[Ff]etch\s*\(' … \| sort \| uniq -c` | **`wizardFetch` × 11 — nothing else** |
| P4 | `SyncPreviewStep.tsx` fetch-like call sites | same, single file | **3** (`:582` kickoff, `:745` tick, `:1230` retry) |
| P5 | `/api/` string literals on the roster (comment-stripped) | node extract | SyncPreviewStep: `"/api/keys/sync"`, `` `/api/strategies/${strategyId}/sync-progress` ``, `"/api/keys/sync"`; useStrategySyncPoller: **0**; SyncProgress: **0** |
| P6 | Seam routes derived from disk | `grep -rl --include='*.ts' -E 'from\s*["'"'"']@/lib/(analytics-client\|process-key-client\|resilient-fetch)["'"'"']' src/app/api/ \| grep -v '\.test\.' \| wc -l` | **15** |
| P7 | `/api/strategies/[id]/sync-progress` seam-import count | `grep -cE '@/lib/(analytics-client\|process-key-client\|resilient-fetch)' 'src/app/api/strategies/[id]/sync-progress/route.ts'` | **0** |
| P8 | `"use client"` production modules under `src/` carrying a seam import edge | node walk over non-test `.ts`/`.tsx`, `"use client"` prologue filter + `SEAM_IMPORT_EDGE` | **0**. ⚠️ The population size is deliberately NOT transcribed here — see Task 2(d) |
| P9 | `prefetch(` occurrences under `src/` | `grep -rnoE 'prefetch\s*\(' --include='*.ts' --include='*.tsx' src/ \| wc -l`, then the same piped through `grep -v '\.test\.' \| grep -v '/test/'` | **9 total, 0 of them in production files.** ⚠️ Corrected at review: an earlier "9 production" figure came from a `grep -h` whose `.test.` filter had nothing to filter, because `-h` suppresses filenames. Do not repeat that shape |
| P10 | `refetch(` occurrences under `src/` | same two commands | **20 total, 4 in production** (`allocations/page.tsx:80`, `watchlist-read.ts:142`, `ScenarioComposer.tsx:1282` and `:1999`) |
| P11 | Runtime probe, **kickoff phase** (2 × `advanceTimersByTimeAsync(0)`) | measured with a throwaway probe test, since deleted | **1 call: `/api/keys/sync`** |
| P12 | Runtime probe, **tick phase** (5 × `advanceTimersByTimeAsync(3000)` + flush) | same probe | **4 calls, ALL `/api/strategies/composite-strat-1/sync-progress`** |
| P13 | Same probe with `SyncPreviewStep.tsx:745`'s URL mutated to `` `/api/scenario/optimize` `` (keeping `wizardFetch(`) | same probe | **4 calls, ALL `/api/scenario/optimize`** — i.e. the tick-phase slice is the discriminator |
| P14 | `src/lib/seam-poll-disjointness.pin.test.ts` | `npx vitest run …` | **6 tests passed** |
| P15 | `…/SyncPreviewStep.progress.render.test.tsx` | `npx vitest run …` | **17 tests passed** |
| P16 | `src/__tests__/contracts/contracts-registry.test.ts` | `npx vitest run …` | **44 tests passed** |
| P17 | `CONTRACT_GUARDS` entries | `grep -c 'path: "' src/__tests__/contracts/contracts-registry.test.ts` | **40** |
| P18 | Registered `*.pin.test.ts` paths | `grep -o 'path: "[^"]*pin\.test\.ts"' src/__tests__/contracts/contracts-registry.test.ts` | **1** — only `seam-constants.pin.test.ts` |
| P19 | `*.pin.test.ts` files on disk | `ls src/lib/*.pin.test.ts` | **4** (constants, copy, poll-disjointness, ssr-exposure) |
| P20 | `REGISTRY.md` guard table rows | ``grep -c '^| `' src/__tests__/contracts/REGISTRY.md`` | **19** |
| P21 | Highest Falsifiability Ledger row in `140.3-VALIDATION.md` | inspection | **M85c** — new rows start at **M86** |
| P22 | Full suite baseline (from VERIFICATION §0, re-run at HEAD by the verifier) | `npm test` | **9779 passed \| 287 skipped** (714 files) |

**⚠️ The single measurement that would have caught this defect at authoring time is P1 vs P2: the
shipped needle finds 0 sites in a corpus containing 11.** That control reddens on the untouched
tree — no mutation, no revert, no fixture. Write it first (Task 2).
</pre_measured_baseline>

<hard_constraints>
Non-negotiable. Violating any of these fails the plan regardless of test results.

1. **`vi.spyOn(globalThis, "fetch")` + `vi.restoreAllMocks()`. NEVER `vi.stubGlobal`.** DEF-16-1:
   38 test files stub `fetch`, 4 never unstub, and a leaked stub is this repo's known cause of
   CI-only failures (CI Node 22 vs local Node 25).
2. **Zero new packages.** The phase's record is zero installs across all sixteen plans. Preserve it.
   `git diff <BASE>..HEAD -- package.json package-lock.json` must be **empty**.
3. **Zero `.py` files.** `analytics-service/` is untouched by this work. Do not run `mypy`/`pytest`
   as a gate for it either.
4. **`.planning/` is gitignored and `commit_docs: false`.** No step may run `git add .planning`.
   Edit `.planning/` files with targeted edits and appends — **never a whole-file rewrite, never
   `head|sed` pipes.** A truncating rewrite has destroyed a ledger on this project before and
   there is no git backup.
5. **Never run `gsd-sdk query state.*`.** Do not touch `.planning/STATE.md` or `.planning/ROADMAP.md`
   — the orchestrator owns those.
6. **SEAMUX-03 is OUT OF SCOPE** (9 of 15 seam routes carry zero code-bearing arms; owner Phase
   140.4). Do not let an arm-by-arm pass leak in. `REQUIREMENTS.md:128` stays `- [ ]` and you do
   not edit it.
7. **`complete-status-scan.test.ts` and the ~50 Python source-reading tests are OUT OF SCOPE** →
   routed to 140.4.
8. **TRAP-9 binds.** Relaxing or deleting a test assertion can silently delete coverage. Where this
   plan replaces an assertion, you must demonstrate in the SUMMARY that the replacement is
   **strictly stronger** — it detects a mutation the old one did not (M86 does exactly this).
9. **Acceptance-criterion hygiene — the highest-probability failure in this plan.** ~33 vacuous /
   unsatisfiable / misattributed criteria surfaced in this phase, and **seven executors were bitten
   by their own comments matching their own greps**. Two further instances were caught during
   review *of this very plan set* (a multi-line phrase anchor whose pre-value was already its pass
   value, and P9's `grep -h` filter that filtered nothing). Before trusting any grep you add:
   - run it on the untouched tree and confirm the pre-value;
   - ask **"could a broken or untouched implementation return this same value?"** — if yes, it is
     not a check;
   - never anchor on a phrase that may wrap across a line break (grep is line-based);
   - never use `grep -h` and then filter the output by filename — there are no filenames to filter;
   - filter comments whenever counting tokens in a file that discusses those tokens.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: The runtime tick fence — the load-bearing, wrapper-proof assertion</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx</files>
  <action>
Create a NEW sibling test file (research Open Question 1: a sibling, not 40 lines bolted into the
710-line PROG file whose stated subject is the progress piggyback — a sibling gets its own docblock
and its own registry entry).

Header: `/** @vitest-environment jsdom */`.

**Why this tier.** State it in the docblock: `wizardFetch` (`src/lib/wizard/wizard-correlation.ts:58`)
calls the UNQUALIFIED global `fetch`, which resolves against the global object at call time, so a
spy on `globalThis.fetch` observes it. That is true of every wrapper — a future `apiFetch`, a
wrapper of a wrapper, an inline `const f = fetch`. The global is the only door. No regex can have
that property; this is why the fence moved tiers rather than getting a wider needle.

**Two pure module-scope helpers**, written so the self-test can drive them with fixtures rather
than only with the real tree:

- `deriveSeamRoutePaths(apiRoot)` — recursively walk `src/app/api`, keep files named `route.ts`,
  keep those whose source matches the seam import edge (`analytics-client` | `process-key-client` |
  `resilient-fetch` imported from `@/lib/…`), and convert each file path to a URL path by stripping
  the `src/app/` prefix and the `/route.ts` suffix. **DERIVED, never hand-typed** — hand-typing
  re-creates the roster-staleness hole. Pre-value P6: exactly 15 paths, one of which
  (`/api/keys/[id]/permissions`) carries a bracketed dynamic segment.
- `isSeamRoutePath(url, seamPaths)` — strip any query string, hash and trailing slash, split into
  segments, and match **segment-wise** against each seam path, treating a bracketed segment as a
  wildcard. Segment-wise is load-bearing: `includes()` or `startsWith()` would classify
  `/api/keys/syncing` as `/api/keys/sync`, and would miss that the live tick target
  `/api/strategies/composite-strat-1/sync-progress` is 4 segments where
  `/api/strategies/composite/add-key` is also 4 but differs at segment 3.

**Harness.** Copy the idioms from the sibling PROG file verbatim in shape: `vi.mock` for
`@/lib/supabase/client`, `@/lib/for-quants-analytics` and `@/components/connect/KeyPermissionBadge`;
a chainable pure-stub Supabase client returning `computation_status: "computing"`;
`vi.useFakeTimers()` in `beforeEach`; `vi.clearAllTimers(); vi.useRealTimers(); vi.restoreAllMocks();`
in `afterEach`. Props: `strategyId`, `apiKeyId`, `wizardSessionId`, `onComplete`, `onTryAnotherKey`
— mirror `baseProps` in the sibling.

⚠️ **MOCK DESIGN IS LOAD-BEARING AND WAS MEASURED.** The fetch mock must answer `/api/keys/sync`
with the 202 kickoff body carrying `composite: true`, and must answer **every other URL** with a
VALID `SyncProgressResponse` JSON body (`jobStatus: "running"`, `stalled: false`,
`memberProgress: []`) — not the sibling file's `"{}"` default. Measured: with a `"{}"` default, the
M86 mutant makes the component throw a TypeError inside the progress `setState` updater and the
test reddens **for the wrong reason**, which would make the M86 receipt false. With a valid default
body the mutant does not crash and the redness is attributable to the URL assertion alone. Write
that reason into the mock's comment so it survives the next edit.

**Phase separation** — this is the whole idea. Render, then `await act(...)` two
`advanceTimersByTimeAsync(0)` flushes to settle mount + kickoff. Record
`const mark = fetchSpy.mock.calls.length`. Then drive 5 iterations of
`advanceTimersByTimeAsync(3000)` each followed by an `advanceTimersByTimeAsync(0)` flush.
`kickoffCalls = calls.slice(0, mark)`, `tickCalls = calls.slice(mark)`.
Measured pre-values: kickoff = 1 call `/api/keys/sync` (P11); tick = 4 calls, all
`/api/strategies/composite-strat-1/sync-progress` (P12).

**Five cases**, in this order:

1. `the seam-route set is DERIVED from disk and non-trivial` — length `toBeGreaterThanOrEqual(12)`
   (actual 15; bound set below actual so ordinary growth never touches it, the SSR pin's stated
   idiom at `:186`), plus hand-typed positive members `/api/keys/sync` and `/api/scenario/optimize`
   must be present, and `/api/strategies/[id]/sync-progress` must be absent. Fail-loud message: a
   derivation that returned nothing would report every URL safe forever.
2. `the route matcher matches a seam route and NOT a near-miss` — the four-slot self-test idiom
   copied in shape from `seam-ssr-exposure.pin.test.ts:237-246`, including its explanatory comment
   (the comment is what stops the next editor deleting the negative half). Slots, all measured:
   `/api/keys/sync` → true (canonical positive); `/api/keys/abc-123/permissions` → true (dynamic
   segment positive — the alternate spelling); `/api/strategies/composite-strat-1/sync-progress` →
   **false** (near-miss negative, and this is the load-bearing one: it is the LIVE tick target, so
   it forces the matcher to be route-specific rather than "any /api/ URL"); `/api/keys/syncing` →
   false (superstring negative). Add `/api/strategies/composite/add-key` → true to pin depth
   handling and `/api/keys/sync?x=1` → true to pin query stripping.
3. `the poll tick issues at least one network call (POSITIVE control)` — `tickCalls.length`
   `toBeGreaterThanOrEqual(1)`; actual 4. This is the M77c lesson made structural: a dead poll and
   a safe poll both produce "no seam call", so case 4's absence proves nothing without this.
   **This case is falsified by ledger row M86b — it is not decorative.**
4. `NO tick-phase call targets a seam route` — the load-bearing assertion. Map `tickCalls` to their
   URLs, filter by `isSeamRoutePath`, `toEqual([])`. Failure message: the poll ticks every 3–10
   seconds for up to fifteen minutes in every open tab; a tick that reaches a seam route records a
   failure on that cadence, re-arms the breaker continuously and becomes the thing keeping the
   circuit open — the outage is extended by our own recovery UI, and the more users are waiting the
   longer it lasts. Name the fix: read from Supabase, which is where the worker writes the result.
5. `the KICKOFF does target a seam route — the property is tick-scoped, not file-scoped` — assert
   `kickoffCalls` contains `/api/keys/sync` AND that `isSeamRoutePath` classifies it as a seam
   route. **This is the honesty case.** It records in an executable form why the old source claim
   ("ZERO fetch calls exist on the poll path") was false, and it stops a future editor "simplifying"
   this fence back into a file-scoped scan that would have to go RED on a clean tree.

**Docblock — what this does NOT prove** (the pin family's established style; keep it longer than
the assertions): it is jsdom under fake timers, not a browser, so it proves the code as executed in
that harness (research assumption A3); it observes ONE component, not the other two roster modules
(`useStrategySyncPoller.ts`, `SyncProgress.tsx`) — which is precisely why the source backstop in
Task 2 is kept rather than deleted; it says nothing about production tick scheduling or about a
fourth poller added tomorrow in a new file.
  </action>
  <verify>
    <automated>npx vitest run 'src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx' 'src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.progress.render.test.tsx'</automated>
  </verify>
  <done>
New file passes with ≥5 cases. The sibling PROG file still reports **17 passed** (P15) — unchanged,
proving no shared-harness collateral. `npx tsc --noEmit` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Repair the source pin — narrow the false claim, add the three missing controls, re-frame the dead predicate</name>
  <files>src/lib/seam-poll-disjointness.pin.test.ts</files>
  <action>
The 140.2 ancestor this pin was modelled on carries **three** controls, and the contracts registry
names them verbatim at `contracts-registry.test.ts:164`: *"Carries its own positive control,
negative control and a fail-loud lower bound on sites scanned, because a guard asserting an ABSENCE
reads as protection while doing nothing if its scanner breaks."* This pin has the positive control
(the Supabase-source case) and neither of the other two. Add both, and fix the one false claim.

**KEEP BYTE-UNCHANGED** (Rule 3 — surgical; these four are correctly tiered and correctly needled):
the roster-size/read case at `:132`, the Supabase-source positive at `:178`, and both wall-clock
cases at `:204` and `:218`.

**(a) REPLACE the `ZERO fetch calls exist on the poll path` case at `:145-163`.** Its claim is false
at HEAD (P4/P5: `SyncPreviewStep.tsx` makes 3 calls, 2 of them to the seam route `/api/keys/sync`).
Delete `EXPECTED_FETCH_CALLS_ON_POLL_PATH` — a hand-typed 0 that is not the truth is worse than no
constant. Replace with the **inverted allow-list** (the repo's own inversion precedent:
`scripts/check-admin-route-manifest.ts:298-322` treats "no known mechanism matched" as a hard
error). Extract every `/api/…` string or template literal from each comment-stripped roster module,
normalise interpolations to `*`, and compare the resulting sorted unique set against a HAND-TYPED
`ALLOWED_POLL_PATH_URLS`. Measured membership (P5), exactly two entries: `/api/keys/sync` and
`/api/strategies/*/sync-progress`.

Make each allow-list entry a record carrying `url`, a hand-typed `seam: boolean`, and a `why`
string, then assert **both halves**: (i) the extracted set equals the allow-listed urls; (ii) for
each entry, `isSeamRoutePath(url)` equals its hand-typed `seam` flag. Hand-typed values:
`/api/keys/sync` → `seam: true`, why = *"user-initiated kickoff (SyncPreviewStep.tsx:582) and
explicit retry (:1230). NOT on the tick — the tick-scoped proof lives in the runtime pin."*;
`/api/strategies/*/sync-progress` → `seam: false`, why = *"the piggybacked tick read (:745)"*.
Half (ii) is the negative discriminator: if `/api/strategies/[id]/sync-progress` ever acquires a
seam import, this reddens even though no URL changed. Duplicate the seam-route derivation and the
segment-wise matcher from Task 1 in this file (both files scan disk independently; do not import a
test file from a test file).

Failure message for half (i): a NEW URL appeared on the poll path — check whether it is on the tick
and whether it is a seam route; if both, that is the retry storm, and the runtime pin
(`SyncPreviewStep.poll-disjointness.runtime.test.tsx`) is where that is decided.

⚠️ **TRAP-9 check you must record in the SUMMARY:** this replaces an assertion. Show it is strictly
stronger — the old case stayed GREEN under M86's mutation (measured: all three of the pin's
predicates return identical values on HEAD and on the mutant), the new one goes RED.

**(b) ADD the fail-loud yield bound — write this one FIRST, it reddens on the untouched tree.**
Define a `CALL_EDGE` needle that matches a network call regardless of wrapper spelling. Do not
hand it to yourself as a fixed regex from this plan: construct it, then prove it against the
fixtures in (c), then measure its yield. Scan the 19 production `.ts`/`.tsx` files under
`src/app/(dashboard)/strategies/new` (comment-stripped, `.test.` excluded) and assert:
corpus-wide sites `toBeGreaterThanOrEqual(6)` — measured actual **11** (P2), bound set well below
actual per the repo's stated idiom so ordinary churn never touches it; and sites in
`SyncPreviewStep.tsx` alone `toBeGreaterThanOrEqual(1)` — measured actual **3** (P4). The second
bound is the precise discriminator: it is the one that reddens if the needle is ever narrowed back
to something a wrapper defeats. Failure message: a needle that finds nothing in a corpus that
contains eleven call sites reports every file clean forever — the failure mode that makes a source
guard worse than no guard, because it reads as protection.

**Analog to cite in the comment:** `src/__tests__/critical-regressions.test.ts:134`. That guard
pairs its negative (`no raw fetch(` in the two seam clients) with a **positive on the same subject**
(`/resilientFetch\s*\(/.test(src)` must be `true`) — so a subject that stopped calling the core
reddens even if the negative needle is blind. **That pairing is exactly what this pin lacked**
(`GAP-PATTERNS` §2.6 and §4.2), and the yield bound is this file's version of it. Name the file and
line so the next reader can compare the two implementations.

Pre-value to state in the SUMMARY: with the SHIPPED needle `/\bfetch\s*\(/` this control measures
**0** and would fail `0 >= 6` on the untouched tree (P1). That is a better falsifier than a staged
mutation, because it needs no revert.

**(c) ADD the needle self-test** in the four-slot idiom of `seam-ssr-exposure.pin.test.ts:237-246`,
extended with the discriminator. Required truth table:

| Fixture | Must be | Provenance |
|---|---|---|
| `const r = await fetch("/api/keys/sync");` | true | canonical spelling |
| `const r = await wizardFetch("/api/keys/sync");` | **true — THE DISCRIMINATOR** | the ONLY network-call spelling in the 19-file wizard corpus (P3). A needle matching the identifier `fetch` passes every other row and fails only this one |
| `globalThis.fetch("/api/x")` | true | qualified global |
| `// this module does not fetch anything` | false | prose without a paren |
| `router.prefetch("/x")` | false | ⚠️ **near-miss, hazard-shaped but NOT currently a production spelling.** Measured (P9): occurrences exist under `src/` but **zero of them in production files**. It stays as a fixture because `router.prefetch` is a live Next.js API a contributor may reach for tomorrow — but do not describe it as live production usage |
| `refetch()` | false | near-miss, **live in production** (P10) |

⚠️ **Do not transcribe P9/P10's integers into the file.** Put the two commands in the comment
instead and re-measure them yourself for the SUMMARY. Both figures were wrong in an earlier draft of
this plan (a `grep -h` whose `.test.` filter had no filenames to filter) and were wrong again in the
review that caught it. A count nobody can re-derive from the line beside it is the class G2 is
deleting one file over.

Carry the explanatory comment from the SSR pin's version: the negative half is the load-bearing
one, because a guard that cries wolf on a docblock gets "fixed" by weakening it.

⚠️ **This file will now contain the token `fetch(` inside its own fixtures and docblock.** Every
scan in it already runs through `stripComments`, and the fixtures are string literals in test
bodies, not scanned source — but confirm that by running the file, not by reasoning about it. This
is the exact self-inflicted-grep trap seven executors in this phase fell into.

**(d) RE-FRAME the import-edge case at `:165-176` — do NOT delete it.** Keep the assertion (deleting
a fence is TRAP-9; it does guard a real, if different, hazard). Rename it to say what it actually
covers, e.g. *"no poll module imports a seam client — structural distance, NOT the retry-storm
fence"*.

Add a comment stating the zero-population fact **qualitatively, with the command that proves it,
and with NO transcribed population integer**: a browser module reaches the seam over HTTP and leaves
no import edge, so this predicate is dead by construction for a `"use client"` roster — **zero** of
this repo's `"use client"` production modules carry a seam import edge, and all three roster modules
are `"use client"`. Write the measuring command inline so the claim is re-derivable (a one-line node
walk over non-test `.ts`/`.tsx` under `src/`, filtering on the `"use client"` prologue and
`SEAM_IMPORT_EDGE`). **The population size — how many `"use client"` modules exist — must NOT be
written down.** It is a number with no owner and no oracle, which is precisely the class G2 is
deleting four instances of in the next wave, and writing one here while deleting four there would
be incoherent. The load-bearing half is the **zero**, and the zero is already asserted by the test
itself.

Name where the real coverage lives: repo-wide by `seam-ssr-exposure.pin.test.ts` and
`tests/lib/scripts/no-server-only-leak.test.ts`; the retry-storm fence is now the runtime pin.
The point is that it must stop *reading as* retry-storm coverage.

**(e) UPDATE the file docblock.** The `WHAT THIS PIN DOES NOT PROVE` block gains an entry: the
retry-storm property is tick-scoped and is proven at runtime in
`SyncPreviewStep.poll-disjointness.runtime.test.tsx`; this file is the source backstop that catches
an edit made without running the render suite, and it covers the two roster modules the runtime
pin does not observe. Also correct limitation 4 — it currently says the pin "proves nothing about
runtime", which is still true of this file but must now point at the file where runtime IS proven.
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-poll-disjointness.pin.test.ts src/lib/seam-ssr-exposure.pin.test.ts src/lib/seam-copy.pin.test.ts src/lib/seam-constants.pin.test.ts</automated>
  </verify>
  <done>
The pin file passes with **≥9** cases (was 6, P14): 4 kept + allow-list + yield bound + needle
self-test + the re-framed import edge, plus any split you judge clearer. The other three pins are
byte-unchanged and still pass. `npx tsc --noEmit` exits 0 and `npm run lint` reports 0 errors
(1 pre-existing warning in `EquityChart.tsx`).
  </done>
</task>

<task type="auto">
  <name>Task 3: Register the guard set, then falsify all three tiers (M86, M86b, M87, M88)</name>
  <files>src/__tests__/contracts/contracts-registry.test.ts, src/__tests__/contracts/REGISTRY.md, .planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md</files>
  <action>
**Part A — GC-2 (Rider 4): close the silent-deletion hole.**

Measured (P18/P19): four `*.pin.test.ts` files are on disk; **exactly one** —
`seam-constants.pin.test.ts` (140.2) — is in `CONTRACT_GUARDS`. **All three pins Phase 140.3 added
can be deleted today with green CI.** That is precisely the gap the registry exists to close, and
the registry says so itself at `contracts-registry.test.ts:14-17`: *"a guard could be
deleted/renamed and CI would stay green."*

Add **exactly four** entries to `CONTRACT_GUARDS` (40 → 44):

1. `src/lib/seam-copy.pin.test.ts`
2. `src/lib/seam-poll-disjointness.pin.test.ts`
3. `src/lib/seam-ssr-exposure.pin.test.ts`
4. `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx`

Four and no more. Do **not** opportunistically register other guards you notice. The registry's
other unregistered files (e.g. the eight remaining eslint-plugin fixture files, which
`contracts-registry.test.ts` already documents in-source as *"not this phase's to claim"*) belong
to whoever owns them. Scope discipline is the point of a gap-closure plan.

Each entry needs `path`, `batch` (`"140.3-G1"`), and an `invariant` string in the house style —
long, specific, naming what the guard would catch and why it exists. For the poll pin and the
runtime pin, the invariant must record that the fence was **re-tiered** at gap closure: the source
pin's original `ZERO fetch calls` claim was false at file granularity because the poll path spells
every call `wizardFetch(`, and the tick-scoped property now lives at runtime.

Add a matching human-readable row per new guard to `src/__tests__/contracts/REGISTRY.md`
(pre-value P20: 19 table rows → 23).

⚠️ **Self-inflicted-grep trap.** The acceptance criteria below count occurrences of the entry-key
token in `contracts-registry.test.ts`. That file already contains long explanatory comments.
**Do not write that entry-key token inside any comment you add**, or your own prose will satisfy
your own grep. Pre-values measured on the untouched tree: 40 entries total, 1 of them a pin path.

**Part B — Falsifiability Ledger. Append a clearly-marked gap-closure section to
`140.3-VALIDATION.md`.**

The ledger is `status: complete`, `closed_by: 140.3-16`. **Do not edit its frontmatter and do not
rewrite the file.** Append a new `## Gap-closure addendum — rows M86+ (plan 140.3-G1)` section at
the end with a targeted append. Highest existing row is **M85c** (P21).

For a guard, "no violation found" is produced equally by an inert guard and a clean codebase —
exactly as a deleted carry and a correct absence both produce `null` (the M77c lesson). So every
mutation below is chosen to **distinguish live from inert**, and each is a SEMANTIC change to
PRODUCTION source drawn from the subject's own measured vocabulary — never a syntax error, never a
type error, never a test edit.

| Row | Mutation (production source) | Must redden | Must STAY GREEN | Why this shape |
|---|---|---|---|---|
| **M86** | `SyncPreviewStep.tsx:745` — retarget the tick from `` `/api/strategies/${strategyId}/sync-progress` `` to `` `/api/scenario/optimize` ``, **keeping `wizardFetch(`** | Task 1 case 4 (tick-phase seam URL) **and** Task 2's allow-list half (i) | Task 1 case 5 (the kickoff is still legitimately a seam call) and Task 1 case 3 (a call still fires) | `wizardFetch(` is the discriminator: the spelling the subject actually uses and the shipped needle could not see. M75c used a literal `fetch(` — a spelling this poll path does not contain (P3), so it proved the needle works on a shape that cannot occur. Measured under this mutant: tick-phase URLs become 4 × `/api/scenario/optimize` (P13) |
| **M86b** | `SyncPreviewStep.tsx:737-760` — remove the piggybacked `void wizardFetch(...)` progress call from the `onStatus` callback (delete the fire-and-forget block; leave the two `setState` calls intact so the poll itself still runs) | **Task 1 case 3** — the positive control (`tickCalls.length >= 1` → 0) | Task 1 case 5, and Task 2's cases | **Without this row, case 3 is an unfalsified assertion guarding an unfalsified assertion.** A poll that stopped making any tick call passes case 4 trivially — the exact M77c shape this plan invokes by name. This row is what makes case 4 non-vacuous. ⚠️ Expect the sibling `SyncPreviewStep.progress.render.test.tsx` (PROG-02/03) to redden too; that is correct and must be reported, not suppressed |
| **M87** | `src/components/strategy/SyncProgress.tsx` — add a fire-and-forget `void wizardFetch("/api/simulator");` inside its effect | Task 2's allow-list half (i) — an unlisted URL | **Task 1's runtime cases** — `SyncProgress` is not rendered by that harness | Proves the source tier has value the runtime tier cannot supply. Without this row, keeping the source pin at all is unjustified |
| **M88** | `git mv src/lib/seam-poll-disjointness.pin.test.ts src/lib/seam-poll-disjointness.pin.test.ts.disabled` | `contracts-registry.test.ts` — the `guard exists:` case for that path | the rest of the registry suite | ⚠️ **Read the note below before writing this row.** |

**M88's status, stated precisely** (adjudicated at review — write this reasoning into the row; do
not merely assert an exception):

The registry case asserts only `existsSync`. Mutating the entry's `path` string would be a **test
edit**, which the ledger's rules forbid — so the only available falsifier is a file-existence
change. That reads at first glance like an exception to the production-source rule. **It is
arguably not an exception at all:** a rename or deletion of a guard file **IS the exact hazard this
guard exists to catch** (`contracts-registry.test.ts:14-17` — *"a guard could be deleted/renamed and
CI would stay green"*), and 140.2-04 observed precisely that happening. The mutation is therefore
the hazard in its native shape, not a substitute for one. Say both halves in the row. A ledger row
that claims an exception without arguing it is how ledger rot starts.

For each row record: the exact command, the pasted RED output including the failing test name and
the counts (`N failed | M passed`), the **green-side** observation, and confirmation that the tree
was restored (`git diff --stat` empty for the mutated file, `grep -rn MUTANT src/` → 0).

⚠️ **A note the M86 receipt depends on:** if you see the mutant redden with a `TypeError` from
inside the progress `setState` updater rather than with the URL assertion's own message, your Task 1
mock's default branch is returning `"{}"` instead of a valid `SyncProgressResponse`. That is a FALSE
receipt — fix the mock and re-run. This was measured at planning time; do not skip the check.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/ src/lib/seam-poll-disjointness.pin.test.ts 'src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.poll-disjointness.runtime.test.tsx'</automated>
  </verify>
  <done>
`grep -o 'path: "[^"]*pin\.test\.ts"' src/__tests__/contracts/contracts-registry.test.ts | wc -l`
returns **4** (pre-value 1, P18). `grep -c 'path: "' …` returns **44** (pre-value 40, P17).
`contracts-registry.test.ts` reports **48** tests (pre-value 44, P16 — one added `it.each` case per
new guard). REGISTRY.md has 4 new rows (pre-value 19, P20). All four M-rows are recorded with
pasted RED output and the tree is restored.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser → `/api/*` route handler | Every seam call from a client component crosses here over HTTP; there is no import edge to observe |
| route handler → Railway analytics seam | The breaker-protected boundary this phase's guards exist to protect |
| test guard → production source | A source-scanning guard reads production bytes; its needle is the trust it places in a spelling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-140.3G-01 | **Denial of Service** (self-inflicted) | wizard sync poll tick → seam route | mitigate | Runtime tick fence (Task 1 cases 3–4, falsified by M86 + M86b) — tick-scoped and wrapper-proof by construction, plus the source allow-list backstop (Task 2a). Property holds at HEAD (P7); the guard is what was broken |
| T-140.3G-02 | **Tampering / Repudiation** (guard decay) | `src/lib/*.pin.test.ts`, the new runtime guard | mitigate | `CONTRACT_GUARDS` entries + `REGISTRY.md` rows (Task 3a), falsified by M88. Measured: 3 of 4 pins deletable with green CI today. A deleted fence and a working fence are observationally identical without this |
| T-140.3G-03 | **Tampering** (needle decay) | the source pin's needle | mitigate | Fail-loud yield bound (Task 2b) + needle self-test with the `wizardFetch(` discriminator (Task 2c). A blind needle reports every file clean forever |
| T-140.3G-04 | Information Disclosure | seam log sites | accept | Untouched. `seam-log-coverage.test.ts` and `seam-redaction.test.ts` remain the guards — **do not weaken either**. Their own partial-needle gaps are deferred to 140.4 and recorded by G2 as DEF-G2-2 |
| T-140.3G-05 | Tampering | npm/pip/cargo installs | mitigate | **Zero packages proposed.** GAP-RESEARCH's Package Legitimacy Audit has no subjects. If any task finds itself wanting a dependency, STOP and escalate — the phase's record of zero installs across sixteen plans is a constraint, not a coincidence |
</threat_model>

<verification>
Run in this order. Every command is re-runnable and every expected value was measured on the
untouched tree.

1. `BASE=$(git rev-parse HEAD)` **before any edit** — record it in the SUMMARY.
2. `npx vitest run src/lib/ src/__tests__/contracts/ 'src/app/(dashboard)/strategies/new/'` — green.
3. `npx tsc --noEmit` — exit 0.
4. `npm run lint` — 0 errors, 1 pre-existing warning (`EquityChart.tsx`). ⚠️ Do not substitute a
   bare `eslint` call: `npm run lint` also runs `check-admin-route-manifest` and
   `check-route-contract`.
5. `npm test` — expect ≥ **9779 passed | 287 skipped** (P22) plus this plan's additions. Any
   *decrease* in passing count is a regression, not noise.
6. `git diff --stat $BASE..HEAD -- package.json package-lock.json` — **empty**.
7. `git diff --name-only $BASE..HEAD | grep -c '\.py$'` — **0**.
8. `git diff --name-only $BASE..HEAD | grep -c '^e2e/'` — **0** (this plan touches no e2e spec).
9. `git diff --name-only $BASE..HEAD | grep -c '^\.planning/'` — **0**. The ledger edit is real but
   `.planning/` is gitignored; if it shows in a diff, something was force-added. Never
   `git add .planning`.
10. `grep -rn MUTANT src/` — **0**. `git status --short | wc -l` — **exactly 2**, and the two lines
    are the documented pre-existing ones (` M TODOS.md`,
    `?? analytics-service/scripts/nautilus_factsheet.py`). Stating the expected count matters: an
    unqualified "clean tree" check passes on a tree carrying unrelated damage.
11. `python3 -m mypy` / `pytest` — **do NOT run**. No Python is touched.
</verification>

<success_criteria>
- [ ] The runtime fence exists, is tick-scoped, and reddens under M86 with its own assertion message
      (not a TypeError). Pre-value for the mutant's tick-phase URLs: 4 × `/api/scenario/optimize` (P13)
- [ ] The runtime fence's positive control (case 3) holds at HEAD (tick calls = 4, P12) **and is
      falsified by M86b** — it is not an unfalsified assertion guarding another one
- [ ] The runtime fence's case 5 pins that the kickoff's `/api/keys/sync` **is** a seam call and is
      deliberately permitted — the executable record of why the old file-scoped claim was false
- [ ] The seam-route set is derived from disk (15, P6), never hand-typed, and its matcher passes a
      six-slot self-test including the near-miss `/api/strategies/composite-strat-1/sync-progress` → false
- [ ] `src/lib/seam-poll-disjointness.pin.test.ts` no longer declares or asserts against the old
      hand-typed zero constant. Check: `grep -c 'EXPECTED_FETCH_CALLS_ON_POLL_PATH' src/lib/seam-poll-disjointness.pin.test.ts`
      → **0** (pre-value **2**). ⚠️ If you name that constant in a new comment, this criterion is
      self-invalidating — do not
- [ ] The yield bound is present and its bound is met: corpus sites ≥ 6 (actual 11, P2), and
      `SyncPreviewStep.tsx` sites ≥ 1 (actual 3, P4). SUMMARY states that the SHIPPED needle measures
      **0** here (P1) and would have failed this control on the untouched tree
- [ ] Task 2(b)'s comment cites `src/__tests__/critical-regressions.test.ts:134` as the
      positive-counterpart analog this pin previously lacked
- [ ] The needle self-test asserts `wizardFetch(` → **true** — the discriminator M75c never had —
      and its `prefetch(` fixture is labelled as hazard-shaped rather than as live production usage
      (P9: zero production sites)
- [ ] **No transcribed integer** for the `prefetch`/`refetch` counts (P9/P10) or the `"use client"`
      population (P8) appears in `seam-poll-disjointness.pin.test.ts`. Each claim carries its
      measuring command instead. The SUMMARY reports the executor's own re-measured figures
- [ ] The import-edge case is still present and still asserted, with its zero-population nature
      stated qualitatively + a re-runnable command, and the real coverage cross-referenced
- [ ] `grep -o 'path: "[^"]*pin\.test\.ts"' src/__tests__/contracts/contracts-registry.test.ts | wc -l`
      → **4** (pre-value **1**, P18)
- [ ] `grep -c 'path: "' src/__tests__/contracts/contracts-registry.test.ts` → **44**
      (pre-value **40**, P17)
- [ ] M86, M86b, M87, M88 recorded in a `## Gap-closure addendum` section appended to
      `140.3-VALIDATION.md`, each with pasted RED output, the green-side observation, and restoration
      confirmed. M88 **argues** its status rather than asserting an exception
- [ ] Zero packages, zero `.py` files, zero `e2e/` files, zero `.planning/` files in
      `git diff --name-only $BASE..HEAD`
- [ ] SUMMARY uses **"would have caught"**, never "did stop" — branch protection is OFF and every
      gate here is advisory at merge
- [ ] SUMMARY records the TRAP-9 justification: the replaced assertion is strictly stronger, shown by
      M86 (old predicate GREEN under the mutation, new one RED)
- [ ] SEAMUX-03 untouched; `complete-status-scan.test.ts` untouched; `analytics-service/` untouched
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-G1-SUMMARY.md` when done.

Commit source files only (`commit_docs: false`). Suggested commits, each its own step — edit, verify,
then commit; never bundle a commit with edits:
- `test(140.3-G1): runtime tick fence — wrapper-proof poll disjointness`
- `test(140.3-G1): repair the source pin — allow-list, yield bound, needle self-test`
- `test(140.3-G1): register the four seam pins + the runtime guard (Rider 4)`
</output>
