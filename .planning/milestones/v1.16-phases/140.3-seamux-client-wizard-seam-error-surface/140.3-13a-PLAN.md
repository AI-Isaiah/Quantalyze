---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: 13a
type: execute
wave: 13
depends_on: ["140.3-12"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx
  - src/app/api/admin/match/eval/route.ts
  - src/app/api/admin/match/eval/route.test.ts
  - src/app/api/admin/match/recompute/route.ts
  - src/app/api/admin/match/recompute/route.test.ts
  - src/app/api/keys/[id]/permissions/route.ts
  - src/app/api/keys/[id]/permissions/route.test.ts
  - src/app/api/verify-strategy/route.ts
  - src/app/api/verify-strategy/route.test.ts
autonomous: true
requirements: [SEAMUX-08]

must_haves:
  truths:
    - "Every wizard variant emits a wizard_error event — the composite variant emits nothing today"
    - "Funnel events carry the specific error code, so an outage is distinguishable from a bad file"
    - "The three genuinely unvalidated `as WizardErrorCode` casts become membership-checked"
    - "Four of the nine zero-capture routes now capture, under ONE stated policy that 13b reuses"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx"
      provides: "The composite variant's wizard_error emission, which does not exist today"
      contains: "trackForQuantsEventClient"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx"
      to: "src/lib/analytics.ts"
      via: "trackForQuantsEventClient('wizard_error', {code}) — the event union already declares code as a wizardErrors stable code"
      pattern: "trackForQuantsEventClient"
---

<objective>
Make seam failures observable to **us**, not only to the user.

Purpose: requirement `SEAMUX-08` (ROADMAP SC6). Two halves, both enumerated first-hand:

**Half A — the funnel.** `src/lib/analytics.ts` declares `"wizard_error"` in its event union and
documents `code` as *"wizardErrors.ts stable code"*, so the **contract is already correct**. The
emitters collapse before it:

| Step | `wizard_error` emissions | Carries the specific code? |
|---|---|---|
| `ConnectKeyStep.tsx` | 2 | partly — `const code = (data.code as WizardErrorCode \| undefined) ?? "UNKNOWN";` is an **unvalidated cast** |
| **`MultiKeyConnectStep.tsx`** | **0** | 🔴 **emits nothing at all** — and it has **two** unvalidated casts |
| `CsvUploadStep.tsx` | 3 | yes |
| `CsvSubmitStep.tsx` | 3 | collapses to `CSV_SUBMIT_FAILED` on a codeless body |
| `SubmitStep.tsx` | 2 | ✅ **fixed by `140.3-05`** — verify, do not redo |
| `SyncPreviewStep.tsx` | 3 | ✅ **improved by `140.3-10`** — verify, do not redo |
| `MetadataStep.tsx` | 2 | yes (client-side validation codes) |
| `WizardClient.tsx` | 1 | yes |

**Half B — Sentry.** Nine of the fifteen seam routes capture **nothing**, while the copy used to claim
a team was notified. `140.3-12` removed the claim; this plan decides where the claim should become
true instead.

| Captures | Routes |
|---|---|
| **0** | `admin/match/eval` · `admin/match/recompute` · `keys/[id]/permissions` · `portfolio-optimizer` · `scenario/optimize` · `strategies/composite/add-key` · `strategies/create-with-key` · `strategies/csv-validate` · `verify-strategy` — **9 of 15** |
| ≥1 | `bridge` (2) · `keys/sync` (2) · `keys/validate-and-encrypt` (3) · `simulator` (2) · `strategies/csv-finalize` (10) · `strategies/finalize-wizard` (11) |

**CONTEXT names two of these nine.** The class is at least five times what the locked text says, and
all nine can land a user on an unexplained failure.

## ⚠️ Three unvalidated casts, and SubmitStep's is the CORRECT shape — do not "simplify" it

`ConnectKeyStep.tsx` (×1) and `MultiKeyConnectStep.tsx` (×2) do
`const code = (data.code as WizardErrorCode | undefined) ?? "UNKNOWN";` — a raw cast of **network
data** into a union, with no membership check. `SubmitStep.tsx`'s `KNOWN_FINALIZE_CODES.has(...)` is
the **correct** shape of the same problem and is the template. Copy it; do not replace it.
</objective>

## ⚠️ THIS PLAN IS ONE HALF OF A SPLIT — `140.3-13a` + `140.3-13b`

An earlier draft carried all nine Sentry routes and both funnel surfaces in one plan: **23 files,
3 tasks, 9 routes**, with a note telling the executor to split if context ran high. That mitigation was
withdrawn at the revision gate for two reasons: **a context-pressured executor is the least reliable
judge of its own context pressure**, and no plan slot or wave existed for it to split INTO, so "split"
was not an available action at execution time.

**The split is a CONTEXT measure and never a scope reduction.** The 9-of-9 Sentry obligation is held
**jointly** by the two halves, and each states which routes it owns so the union is auditable at
`140.3-16`. Neither half may mark SEAMUX-08 closed alone.

| Half | Owns |
|---|---|
| **`140.3-13a`** | Both funnel surfaces (`MultiKeyConnectStep`, `ConnectKeyStep`) + Sentry at **4** routes: `admin/match/eval`, `admin/match/recompute`, `keys/[id]/permissions`, `verify-strategy` |
| **`140.3-13b`** | Sentry at **5** routes: `strategies/create-with-key`, `strategies/composite/add-key`, `strategies/csv-validate`, `portfolio-optimizer`, `scenario/optimize` — then the ledger and the mutations |

4 + 5 = **9**. `140.3-13b` runs after `140.3-13a` and carries the joint accounting.

**This is `140.3-13a`.** It owns **both funnel surfaces** and **4 of the 9** Sentry routes:
`admin/match/eval`, `admin/match/recompute`, `keys/[id]/permissions`, `verify-strategy`.
It also **decides and records the capture policy that `140.3-13b` must reuse verbatim** — one policy
across nine routes is the point, and two halves inventing two policies would be the drift this
programme exists to close.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-CONTEXT.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-PATTERNS.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-05-SUMMARY.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-12-SUMMARY.md

<read_first>
- `src/lib/analytics.ts` — the `"wizard_error"` event union member and the docblock declaring `code`
  as the wizardErrors stable code. **The contract is correct; do not change it.**
- `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx` **as `140.3-05` left it** — the
  `KNOWN_FINALIZE_CODES` membership check and the `trackForQuantsEventClient("wizard_error", {` call
  reading `surfaced`. **This is the template for both halves of the funnel work.**
- `src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx` — the **two** casts
  `const code = (data.code as WizardErrorCode | undefined) ?? "UNKNOWN";` and the complete absence of
  any `trackForQuantsEventClient("wizard_error"` call. Read both error paths.
- `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx` — its one cast and its two
  existing emissions.
- `src/app/api/strategies/finalize-wizard/route.ts` — **the Sentry template.** It has eleven
  `captureToSentry` calls; read one, with its context object, and read how it scrubs. Copy that shape.
- `src/lib/seam-redaction.ts` — `scrubSeamError`. ⚠️ **TRAP-1: undici embeds outgoing headers in the
  error message and, in one shape, the error NAME.** On this seam that is `INTERNAL_API_TOKEN`,
  `X-User-Access-Token` (a live user JWT) and raw exchange `api_key`/`api_secret`. **Every capture
  added by this plan must go through the leaf.** Do not hand-roll a scrub.
- `src/lib/seam-log-coverage.test.ts` — the existing mechanism that fails when a bare caught
  identifier reaches a `console.*`. Read it; the new captures must not break it, and it may already
  cover part of what this plan adds.
- `src/app/api/strategies/create-with-key/route.ts` — the best-in-audit breaker cell, and one of the
  nine zero-capture routes. Its cell is a template for everything EXCEPT observability.
  </read_first>

<interfaces>
**Half A — what "carries the specific code" means concretely.** The event must carry the code the
route emitted, not a per-step bucket. `CsvSubmitStep`'s collapse to `CSV_SUBMIT_FAILED` on a codeless
body is acceptable **only while bodies are codeless**; `140.3-10` and `140.3-11` gave several arms
codes, so re-check whether that collapse is still necessary and record the answer.

**The membership-check shape, from `SubmitStep`:** a `ReadonlySet<WizardErrorCode>` of the codes the
route can actually emit, `has()`-checked before the cast, falling back to `UNKNOWN`. Three sites need
it. **Do not replace the sets with a totality check over the whole union** — a step should admit the
codes its route emits, not every code in the vocabulary; that is why `SubmitStep`'s set is
hand-written and is the correct shape.

**Half B — capture policy. Decide it ONCE and apply it to all nine.** The nine routes differ in what
they can meaningfully report. State a single rule — for example, capture on unexpected/untyped throws
and on service-permanent outcomes, but **not** on caller faults or on breaker short-circuits (a
breaker trip is an expected infrastructure fact and capturing every one of them turns Sentry into a
firehose during the exact incident it exists for). Whatever the rule, it must be:
  - **one rule**, stated in `140.3-13a`'s SUMMARY precisely enough for `140.3-13b` to apply verbatim;
  - applied at all nine **across the two halves** (4 here, 5 in `140.3-13b`), with a per-route note
    where a route is an exception and why;
  - routed through `scrubSeamError`.

⚠️ **`create-with-key` and `composite/add-key` are the two CONTEXT names, but they are 2 of 9.** A
plan that closes those two has closed 22% of the class.

⚠️ **Sentry ingestion cannot be verified by a unit test.** `140.3-VALIDATION.md` lists this as the
phase's one manual-only verification: trip the breaker in preview and confirm the issue lands in org
`metaworld-fund-ltd`. **What IS testable is that the capture is invoked and that its payload is
scrubbed.** Assert that; record the manual step as owed rather than claiming it.

**Out of scope, stated so it is not smuggled in:**
- Copy. `140.3-12` closed the copy half. If a capture makes a removed claim true again, **note it for
  the founder; do not restore the sentence** — M71 must stay red.
- `csv-finalize`'s `debug_context` — **TS-24, jointly owned with Phase 145.** Do not touch it, and do
  not import its pattern.
- `SubmitStep` and `SyncPreviewStep`'s funnel emissions — delivered by `140.3-05` and `140.3-10`.
  **Verify and record; do not redo.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Every wizard variant emits, and the three unvalidated casts get membership checks</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx</files>
  <read_first>
- `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx` as `140.3-05` left it — the
  membership check and the emission. **The template for both.**
- `src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx` — the two casts and both
  error paths.
- `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx` — the one cast and the two
  existing emissions.
- `src/lib/analytics.ts` — the event union and the `code` docblock.
  </read_first>
  <action>
Re-run the emission inventory across **all eight** wizard surfaces before editing, and confirm the
table in `<objective>` still holds. `140.3-05` and `140.3-10` moved two rows; the receipt must be
current.

Give `MultiKeyConnectStep` a `wizard_error` emission on each of its error paths, carrying the specific
code. It emits **nothing** today, so the composite variant of the funnel is blind — an outage during a
multi-key connect is invisible to us.

Replace the three unvalidated `as WizardErrorCode` casts (one in `ConnectKeyStep`, two in
`MultiKeyConnectStep`) with `SubmitStep`'s membership-checked shape: a `ReadonlySet<WizardErrorCode>`
of the codes each route can actually emit, `has()`-checked, falling back to `UNKNOWN`. Each set is
hand-written for its own route — do not share one set across steps and do not substitute a totality
check over the whole union.

Verify (do not redo) that `SubmitStep` and `SyncPreviewStep` now carry specific codes, and record the
verification. Re-check whether `CsvSubmitStep`'s collapse to `CSV_SUBMIT_FAILED` is still necessary
now that several arms carry codes, and record the answer either way.

Add cases that fail without the change:
  - a composite connect failure emits `wizard_error` with the specific code (today it emits nothing);
  - an unrecognised code at each of the three cast sites resolves to `UNKNOWN` rather than being
    admitted into the union;
  - a recognised code is emitted unchanged.
`vi.unstubAllGlobals()` in `afterEach`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/"</automated>
  </verify>
  <acceptance_criteria>
- **`grep -vE '^\s*(//|\*)' "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx" | grep -c "trackForQuantsEventClient"` ≥ **1** (was 0).** ⚠️ Comment exclusion is load-bearing: this is the SOLE criterion for the one variant that emits nothing today, and a comment mentioning `wizard_error` would satisfy a raw grep. Match the CALL, not the event name.
- `grep -rn "as WizardErrorCode | undefined" "src/app/(dashboard)/strategies/new/wizard/steps/"` → **0 hits** across all steps.
- `grep -vE '^\s*(//|\*)' "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx" | grep -c "ReadonlySet<WizardErrorCode>"` ≥ 1, and the same on `MultiKeyConnectStep.tsx` ≥ 1 — comment-excluded, since both files already discuss the union in prose.
- `grep -c "KNOWN_FINALIZE_CODES" "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx"` is unchanged (the template was copied, not moved).
- The unrecognised-code case exists at all three sites.
  </acceptance_criteria>
  <done>
Every wizard variant emits `wizard_error` with a specific code. All three unvalidated network-data
casts are membership-checked with per-route sets. `SubmitStep`'s template is intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Decide the ONE capture policy, then apply it at this half's FOUR routes</name>
  <files>src/app/api/admin/match/eval/route.ts, src/app/api/admin/match/eval/route.test.ts, src/app/api/admin/match/recompute/route.ts, src/app/api/admin/match/recompute/route.test.ts, src/app/api/keys/[id]/permissions/route.ts, src/app/api/keys/[id]/permissions/route.test.ts, src/app/api/verify-strategy/route.ts, src/app/api/verify-strategy/route.test.ts</files>
  <read_first>
- `src/app/api/strategies/finalize-wizard/route.ts` — **the Sentry template.** It has eleven
  `captureToSentry` calls; read one, with its context object, and read how it scrubs. Copy that shape.
- `src/lib/seam-redaction.ts` — `scrubSeamError`. ⚠️ **TRAP-1: undici embeds outgoing headers in the
  error message and, in one shape, the error NAME.** On this seam that is `INTERNAL_API_TOKEN`,
  `X-User-Access-Token` (a live user JWT) and raw exchange `api_key`/`api_secret`. **Every capture
  added here must go through the leaf.** Do not hand-roll a scrub.
- `src/lib/seam-log-coverage.test.ts` — the existing mechanism that fails when a bare caught
  identifier reaches a `console.*`. The new captures must not break it.
- Each of this half's four routes, at its terminal error arm. All four were edited by earlier plans in
  this phase — read them as they are ON DISK, not as any document describes them.
  </read_first>
  <action>
Re-run the enumeration across all **fifteen** seam routes and record the current split, using the
comment-excluding form so this task's own added prose cannot satisfy it later:
`grep -vE '^\s*(//|\*)' "$f" | grep -c "captureToSentry"` per route.
Measured at planning time: the nine named in `<objective>` read **0** and the other six read ≥1.
That count is the receipt for "the class was enumerated"; if it has moved, record the new numbers.

**Decide the ONE capture policy and write it down in the SUMMARY before applying it.** For example:
capture on unexpected/untyped throws and on service-permanent outcomes, but **not** on caller faults
and **not** on breaker short-circuits. Whatever the rule, `140.3-13b` reuses it **verbatim** at its
five routes — so state it precisely enough to be applied without reinterpretation.

⚠️ **Do not capture on breaker short-circuits.** A trip is an expected infrastructure fact and
capturing each one turns Sentry into a firehose during the exact correlated incident it exists to
surface. If the chosen policy says otherwise, argue it explicitly.

Apply the policy at this half's **four** routes, routing every captured value through `scrubSeamError`.
Where a route is a genuine exception, note the route and the reason — an exception with a stated
reason is fine; an unexplained gap is the under-delivery this split exists to prevent.

At each of the four, add a case asserting an unexpected failure invokes the capture, and that the
captured payload does **not** contain a secret while it **does** still contain its syscall token
(`ECONNREFUSED` / `ENOTFOUND`) — the over-redaction half of TRAP-1 matters as much as the
under-redaction half.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/app/api/admin/match/ "src/app/api/keys/[id]/permissions/" src/app/api/verify-strategy/ src/lib/seam-log-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
- **All four of this half's routes capture, asserted per file and comment-excluded** (each reads **0** on the untouched tree, so each grep is falsifiable today):
  `grep -vE '^\s*(//|\*)' "$f" | grep -c "captureToSentry"` ≥ 1 for each of
  `src/app/api/admin/match/eval/route.ts`, `src/app/api/admin/match/recompute/route.ts`,
  `"src/app/api/keys/[id]/permissions/route.ts"`, `src/app/api/verify-strategy/route.ts`
  — or the SUMMARY names the route and the stated reason it is an exception.
- Every new capture passes a scrubbed value. Run per file, substituting each changed route path for `$f`: `grep -B 3 "captureToSentry" "$f" | grep -c "scrubSeamError"` ≥ 1 (a shared scrubbed local in scope is acceptable — verify by reading).
- `src/lib/seam-log-coverage.test.ts` is green.
- The scrub tests assert BOTH directions: no secret, and the syscall token preserved.
- **The capture policy is stated in one sentence in the SUMMARY**, precise enough for `140.3-13b` to apply without reinterpretation.
- `git diff --name-only` contains **no** `csv-finalize` path (TS-24 is Phase 145's) and **none of `140.3-13b`'s five routes**.
  </acceptance_criteria>
  <done>
Both funnel surfaces emit; four of the nine routes capture under one written-down policy; every
capture is scrubbed and proven not to over-redact. The policy is recorded for `140.3-13b`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caught seam error → Sentry payload | undici embeds outgoing headers, and in one shape the error name, into the message |
| Network `data.code` → typed union | An upstream string is cast into `WizardErrorCode` with no membership check |
| Failure volume → alerting channel | A breaker trip fires repeatedly during the incident Sentry exists to surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-13-01 | Information disclosure | A Sentry capture carrying `INTERNAL_API_TOKEN`, a live `X-User-Access-Token` JWT, or raw exchange credentials (TRAP-1) | mitigate | Every new capture routes through `scrubSeamError`; tests assert both no-secret AND syscall-token-preserved, because over-redaction destroys the most valuable part of the line. |
| T-140.3-13-02 | Repudiation | The composite wizard variant emitting no funnel event, so a multi-key outage is invisible | mitigate | Emissions added on every error path; falsified by M70, which is meaningful precisely because the baseline was zero. |
| T-140.3-13-03 | Spoofing | Network data cast into `WizardErrorCode` without a membership check, so an upstream string becomes a typed code | mitigate | Per-route `ReadonlySet` membership checks at all three sites, copying `SubmitStep`'s correct shape. |
| T-140.3-13-04 | Denial of service | Capturing every breaker short-circuit, flooding Sentry during the correlated incident | mitigate | The stated policy excludes breaker short-circuits; any deviation must be argued explicitly. |
| T-140.3-13-05 | Repudiation | Closing 2 of 9 routes because CONTEXT named two | mitigate | Per-route acceptance grep over all nine, each printing ≥ 1 or naming a stated exception. |
| T-140.3-13-06 | Information disclosure | Importing `csv-finalize`'s `debug_context` pattern, which echoes an upstream body | avoid | Explicitly out of scope (TS-24, Phase 145); asserted by `git diff --name-only`. |
| T-140.3-13-07 | Repudiation | Claiming SEAMUX-08 verified when Sentry ingestion was never observed | mitigate | The manual step is recorded as OWED in the ledger and the SUMMARY, never as done. |
| T-140.3-13-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/" src/app/api/admin/match/ "src/app/api/keys/[id]/permissions/" src/app/api/verify-strategy/ src/lib/` green.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- Zero Python files modified. `csv-finalize` NOT modified.
- **`140.3-12`'s copy-honesty test must still be green** — a capture added here must not tempt a copy
  restoration; M71 stays falsifiable.
</verification>

<success_criteria>
- Every wizard variant emits `wizard_error` with a specific code; zero unvalidated casts remain.
- This half's four routes capture under ONE stated policy, or their exceptions are named.
- Every capture is scrubbed, and proven not to over-redact.
- The policy is recorded in a form `140.3-13b` can apply verbatim.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-13a-SUMMARY.md` when done.
Record: the re-run funnel inventory (all eight surfaces, before/after); the re-run Sentry split across
all fifteen routes; **the capture policy in one sentence**; this half's four routes with any exceptions
and their reasons; and an explicit hand-off line naming the five routes `140.3-13b` still owes.
</output>
