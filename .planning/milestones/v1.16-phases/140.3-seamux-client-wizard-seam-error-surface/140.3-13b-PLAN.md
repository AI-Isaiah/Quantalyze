---
phase: 140.3-seamux-client-wizard-seam-error-surface
plan: 13b
type: execute
wave: 13
depends_on: ["140.3-13a"]
files_modified:
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/create-with-key/route.test.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/strategies/composite/add-key/route.test.ts
  - src/app/api/strategies/csv-validate/route.ts
  - src/__tests__/csv-validate-route.test.ts
  - src/app/api/portfolio-optimizer/route.ts
  - src/app/api/portfolio-optimizer/route.test.ts
  - src/app/api/scenario/optimize/route.ts
  - src/app/api/scenario/optimize/route.test.ts
  - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
autonomous: true
requirements: [SEAMUX-08]

must_haves:
  truths:
    - "The remaining five zero-capture routes capture, under the SAME policy 140.3-13a recorded"
    - "All nine routes are audited from the tree, not from a SUMMARY — 4 + 5 = 9, union shown"
    - "csv-validate's case actually executes, despite its test living outside src/app/api/"
    - "The Sentry-ingestion verification is recorded as MANUAL and OWED, never as done"
  artifacts:
    - path: ".planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md"
      provides: "The joint 9-of-9 SEAMUX-08 accounting across both halves"
      contains: "SEAMUX-08"
  key_links:
    - from: "src/app/api/strategies/csv-validate/route.ts"
      to: "src/__tests__/csv-validate-route.test.ts"
      via: "the test lives OUTSIDE src/app/api/, so the verify command must reach src/__tests__/ or this route's case silently does not run"
      pattern: "csv-validate"
---

<objective>
Make seam failures observable to **us**, not only to the user.

Purpose: requirement `SEAMUX-08` (ROADMAP SC6). Two halves, both enumerated first-hand:

**Half A — the funnel.** `src/lib/analytics.ts` declares `"wizard_error"` in its event union and
documents `code` as *"wizardErrors.ts stable code"*, so the **contract is already correct**. The
emitters collapse before it:

| Step | `wizard_error` emissions | Carries the specific code? |
|---|---|---|
| `ConnectKeyStep.tsx` | 2 | partly — `const code = (data.code as WizardErrorCode \| undefined) ?? "UNKNOWN";` is an **unvalidated cast** |
| **`MultiKeyConnectStep.tsx`** | **0** | 🔴 **emits nothing at all** — and it has **two** unvalidated casts |
| `CsvUploadStep.tsx` | 3 | yes |
| `CsvSubmitStep.tsx` | 3 | collapses to `CSV_SUBMIT_FAILED` on a codeless body |
| `SubmitStep.tsx` | 2 | ✅ **fixed by `140.3-05`** — verify, do not redo |
| `SyncPreviewStep.tsx` | 3 | ✅ **improved by `140.3-10`** — verify, do not redo |
| `MetadataStep.tsx` | 2 | yes (client-side validation codes) |
| `WizardClient.tsx` | 1 | yes |

**Half B — Sentry.** Nine of the fifteen seam routes capture **nothing**, while the copy used to claim
a team was notified. `140.3-12` removed the claim; this plan decides where the claim should become
true instead.

| Captures | Routes |
|---|---|
| **0** | `admin/match/eval` · `admin/match/recompute` · `keys/[id]/permissions` · `portfolio-optimizer` · `scenario/optimize` · `strategies/composite/add-key` · `strategies/create-with-key` · `strategies/csv-validate` · `verify-strategy` — **9 of 15** |
| ≥1 | `bridge` (2) · `keys/sync` (2) · `keys/validate-and-encrypt` (3) · `simulator` (2) · `strategies/csv-finalize` (10) · `strategies/finalize-wizard` (11) |

**CONTEXT names two of these nine.** The class is at least five times what the locked text says, and
all nine can land a user on an unexplained failure.

## ⚠️ Three unvalidated casts, and SubmitStep's is the CORRECT shape — do not "simplify" it

`ConnectKeyStep.tsx` (×1) and `MultiKeyConnectStep.tsx` (×2) do
`const code = (data.code as WizardErrorCode | undefined) ?? "UNKNOWN";` — a raw cast of **network
data** into a union, with no membership check. `SubmitStep.tsx`'s `KNOWN_FINALIZE_CODES.has(...)` is
the **correct** shape of the same problem and is the template. Copy it; do not replace it.
</objective>

## ⚠️ THIS PLAN IS THE SECOND HALF OF A SPLIT — `140.3-13a` + `140.3-13b`

`140.3-13a` landed **both funnel surfaces** and Sentry at **4** routes, and — critically — **decided
and recorded the ONE capture policy**. This plan applies that **same policy, verbatim**, at the
remaining **5** routes, then carries the joint accounting and the mutations.

**The split is a CONTEXT measure and never a scope reduction.** The 9-of-9 Sentry obligation is held
**jointly**; 4 + 5 = 9. **Neither half may mark SEAMUX-08 closed alone** — this half does the
accounting because it runs second, and it must audit `140.3-13a`'s four as well as its own five.

| Half | Owns |
|---|---|
| `140.3-13a` | Funnel ×2 + `admin/match/eval`, `admin/match/recompute`, `keys/[id]/permissions`, `verify-strategy` |
| **`140.3-13b` (this plan)** | `strategies/create-with-key`, `strategies/composite/add-key`, `strategies/csv-validate`, `portfolio-optimizer`, `scenario/optimize` |

⚠️ **Do NOT re-derive the capture policy.** Read it from `140.3-13a-SUMMARY.md` and apply it as
written. Two halves inventing two policies is the drift class this programme exists to close — if the
recorded policy is ambiguous at a site, record the ambiguity and the reading you chose rather than
substituting your own rule.

⚠️ **`csv-validate`'s test file is NOT beside its route.** It lives at
`src/__tests__/csv-validate-route.test.ts` — verified on the tree; `src/app/api/strategies/csv-validate/`
contains `route.ts` only. A verify command scoped to `src/app/api/` **does not reach it**, which is
exactly how this one route's case would silently not execute. The verify command below includes
`src/__tests__/` for that reason.

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-CONTEXT.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-PATTERNS.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-VALIDATION.md
@.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-13a-SUMMARY.md

<read_first>
- `src/lib/analytics.ts` — the `"wizard_error"` event union member and the docblock declaring `code`
  as the wizardErrors stable code. **The contract is correct; do not change it.**
- `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx` **as `140.3-05` left it** — the
  `KNOWN_FINALIZE_CODES` membership check and the `trackForQuantsEventClient("wizard_error", {` call
  reading `surfaced`. **This is the template for both halves of the funnel work.**
- `src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx` — the **two** casts
  `const code = (data.code as WizardErrorCode | undefined) ?? "UNKNOWN";` and the complete absence of
  any `trackForQuantsEventClient("wizard_error"` call. Read both error paths.
- `src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx` — its one cast and its two
  existing emissions.
- `src/app/api/strategies/finalize-wizard/route.ts` — **the Sentry template.** It has eleven
  `captureToSentry` calls; read one, with its context object, and read how it scrubs. Copy that shape.
- `src/lib/seam-redaction.ts` — `scrubSeamError`. ⚠️ **TRAP-1: undici embeds outgoing headers in the
  error message and, in one shape, the error NAME.** On this seam that is `INTERNAL_API_TOKEN`,
  `X-User-Access-Token` (a live user JWT) and raw exchange `api_key`/`api_secret`. **Every capture
  added by this plan must go through the leaf.** Do not hand-roll a scrub.
- `src/lib/seam-log-coverage.test.ts` — the existing mechanism that fails when a bare caught
  identifier reaches a `console.*`. Read it; the new captures must not break it, and it may already
  cover part of what this plan adds.
- `src/app/api/strategies/create-with-key/route.ts` — the best-in-audit breaker cell, and one of the
  nine zero-capture routes. Its cell is a template for everything EXCEPT observability.
  </read_first>

<interfaces>
**Half A — what "carries the specific code" means concretely.** The event must carry the code the
route emitted, not a per-step bucket. `CsvSubmitStep`'s collapse to `CSV_SUBMIT_FAILED` on a codeless
body is acceptable **only while bodies are codeless**; `140.3-10` and `140.3-11` gave several arms
codes, so re-check whether that collapse is still necessary and record the answer.

**The membership-check shape, from `SubmitStep`:** a `ReadonlySet<WizardErrorCode>` of the codes the
route can actually emit, `has()`-checked before the cast, falling back to `UNKNOWN`. Three sites need
it. **Do not replace the sets with a totality check over the whole union** — a step should admit the
codes its route emits, not every code in the vocabulary; that is why `SubmitStep`'s set is
hand-written and is the correct shape.

**Half B — capture policy. Decide it ONCE and apply it to all nine.** The nine routes differ in what
they can meaningfully report. State a single rule — for example, capture on unexpected/untyped throws
and on service-permanent outcomes, but **not** on caller faults or on breaker short-circuits (a
breaker trip is an expected infrastructure fact and capturing every one of them turns Sentry into a
firehose during the exact incident it exists for). Whatever the rule, it must be:
  - **one rule**, INHERITED from `140.3-13a`'s SUMMARY and applied verbatim — **not re-derived here**;
  - applied at all nine **across the two halves** (4 in `140.3-13a`, 5 here), with a per-route note
    where a route is an exception and why;
  - routed through `scrubSeamError`.

⚠️ **`create-with-key` and `composite/add-key` are the two CONTEXT names, but they are 2 of 9.** A
plan that closes those two has closed 22% of the class.

⚠️ **Sentry ingestion cannot be verified by a unit test.** `140.3-VALIDATION.md` lists this as the
phase's one manual-only verification: trip the breaker in preview and confirm the issue lands in org
`metaworld-fund-ltd`. **What IS testable is that the capture is invoked and that its payload is
scrubbed.** Assert that; record the manual step as owed rather than claiming it.

**Out of scope, stated so it is not smuggled in:**
- Copy. `140.3-12` closed the copy half. If a capture makes a removed claim true again, **note it for
  the founder; do not restore the sentence** — M71 must stay red.
- `csv-finalize`'s `debug_context` — **TS-24, jointly owned with Phase 145.** Do not touch it, and do
  not import its pattern.
- `SubmitStep` and `SyncPreviewStep`'s funnel emissions — delivered by `140.3-05` and `140.3-10`.
  **Verify and record; do not redo.**
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply 140.3-13a's recorded policy at the remaining FIVE routes</name>
  <files>src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/create-with-key/route.test.ts, src/app/api/strategies/composite/add-key/route.ts, src/app/api/strategies/composite/add-key/route.test.ts, src/app/api/strategies/csv-validate/route.ts, src/__tests__/csv-validate-route.test.ts, src/app/api/portfolio-optimizer/route.ts, src/app/api/portfolio-optimizer/route.test.ts, src/app/api/scenario/optimize/route.ts, src/app/api/scenario/optimize/route.test.ts</files>
  <read_first>
- `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-13a-SUMMARY.md` — **FIRST.**
  The capture policy, stated in one sentence. **Apply it as written; do not re-derive it.**
- `src/app/api/strategies/finalize-wizard/route.ts` — the Sentry template with its context object and
  its scrubbing, and `140.3-13a`'s four routes as it left them, so this half matches that shape.
- `src/lib/seam-redaction.ts` — `scrubSeamError` and TRAP-1's rationale (undici embeds outgoing
  headers, and in one shape the error NAME, into the message).
- `src/lib/seam-log-coverage.test.ts` — the existing coverage mechanism.
- Each of this half's five routes at its terminal error arm, ON DISK.
- ⚠️ `src/__tests__/csv-validate-route.test.ts` — **`csv-validate`'s test file, which is NOT beside its
  route.** `src/app/api/strategies/csv-validate/` contains `route.ts` only.
  </read_first>
  <action>
Read `140.3-13a-SUMMARY.md`'s capture policy and apply it **verbatim** at this half's five routes,
routing every captured value through `scrubSeamError`. If the recorded policy is ambiguous at a site,
**record the ambiguity and the reading you chose** — do not substitute a different rule, and do not
invent a second policy.

Where a route is a genuine exception to the policy, note the route and the reason. An exception with a
stated reason is fine; an unexplained gap is the under-delivery the split exists to prevent.

⚠️ **`create-with-key`'s breaker cell is "the best in the audit" and is a TEMPLATE, not a fix target.**
Adding a capture to that route must not disturb its three properties (the caught VALUE passed to the
shared classifier, the status derived from the classifier, the conditional `Retry-After`).

At each of the five, add a case asserting an unexpected failure invokes the capture, and that the
captured payload does **not** contain a secret while it **does** still contain its syscall token
(`ECONNREFUSED` / `ENOTFOUND`). ⚠️ **`csv-validate`'s case goes in `src/__tests__/csv-validate-route.test.ts`**,
and the verify command must reach `src/__tests__/` or that one case silently never runs.
  </action>
  <verify>
    <automated>npx tsc --noEmit &amp;&amp; npx vitest run src/app/api/strategies/create-with-key/ src/app/api/strategies/composite/add-key/ src/app/api/strategies/csv-validate/ src/__tests__/csv-validate-route.test.ts src/app/api/portfolio-optimizer/ src/app/api/scenario/optimize/ src/lib/seam-log-coverage.test.ts</automated>
  </verify>
  <acceptance_criteria>
- **All five of this half's routes capture, asserted per file and comment-excluded** (each reads **0** on the untouched tree, so each grep is falsifiable today): `grep -vE '^\s*(//|\*)' "$f" | grep -c "captureToSentry"` ≥ 1 for each of `src/app/api/strategies/create-with-key/route.ts`, `src/app/api/strategies/composite/add-key/route.ts`, `src/app/api/strategies/csv-validate/route.ts`, `src/app/api/portfolio-optimizer/route.ts`, `src/app/api/scenario/optimize/route.ts` — or a named exception with its reason.
- **`csv-validate`'s case provably EXECUTED**, not merely written: the verify command includes `src/__tests__/csv-validate-route.test.ts` and its run output names that file. ⚠️ A verify scoped to `src/app/api/` would skip it — that is the 8-of-9 failure this criterion exists to catch.
- Every new capture passes a scrubbed value. Run per file, substituting each changed route path for `$f`: `grep -B 3 "captureToSentry" "$f" | grep -c "scrubSeamError"` ≥ 1 (a shared scrubbed local in scope is acceptable — verify by reading).
- `create-with-key`'s three breaker-cell properties are intact — verify by reading and record it.
- `src/lib/seam-log-coverage.test.ts` is green.
- `git diff --name-only` contains **no** `csv-finalize` path (TS-24 is Phase 145's) and **none of `140.3-13a`'s four routes**.
  </acceptance_criteria>
  <done>
This half's five routes capture under `140.3-13a`'s recorded policy, every capture scrubbed and proven
not to over-redact. `csv-validate`'s case executed from its real location. `create-with-key`'s cell is
undisturbed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Ledger, then mutations M69-verify and M70</name>
  <files>.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md</files>
  <read_first>
- The obligations ledger, §2. **Gitignored, no git backup — targeted edits only.**
- `140.3-VALIDATION.md` rows M69 and M70, and the Manual-Only Verifications table.
- `140.3-05-SUMMARY.md` — M69's receipt, if it was run there.
  </read_first>
  <action>
⚠️ **This half carries the accounting for BOTH halves.** Audit `140.3-13a`'s four routes as well as
this plan's five, from the tree — not from its SUMMARY. `4 + 5 = 9`, and the union must be shown.

Add a dated note under §2 recording SEAMUX-08's closure with its counts as receipts:
  - the funnel inventory across all eight wizard surfaces, before and after;
  - the Sentry split before and after — measured with `grep -vE '^\s*(//|\*)' "$f" | grep -c "captureToSentry"` per route, never a raw count, since both halves add explanatory prose naming the symbol — (it was 9 zero-capture of 15; state what it is now), **listing
    all nine routes and which half closed each**, with any stated exceptions named. A note that shows
    only this half's five has not closed SEAMUX-08;
  - the capture POLICY in one sentence, so a future route knows what to do without re-deriving it;
  - that **CONTEXT named 2 of the 9** and the class was five times larger — the correction recorded in
    place;
  - that the Sentry-ingestion verification is **manual and OWED**, with the exact step (trip the
    breaker in preview, confirm the issue in org `metaworld-fund-ltd`). Do not mark SEAMUX-08 fully
    verified on a unit test alone.
Do not change the obligation count — no row was added or removed; say so explicitly.

Then OBSERVE these mutations, one at a time, reverting between each, with no other agent active on the
tree. Between rows: `git status` clean and `grep -rn MUTANT src/` empty.
  M70 — delete `MultiKeyConnectStep.tsx`'s `wizard_error` emission ⇒ must redden the composite-variant
        funnel case. **This is the variant that emitted nothing before this plan**, so a green M70
        means the new case is not actually asserting the emission.
  M69 — **verify, do not re-run from scratch if `140.3-05` already observed it.** Read that plan's
        receipt. If it was observed there, record the cross-reference and confirm the assertion still
        exists; if it was not, run it here: emit `code: "UNKNOWN"` to the funnel at `SubmitStep`
        instead of the specific code ⇒ must redden the funnel-specificity case while the copy case
        stays green.
Record each observed failure output verbatim. An "asserted only" row is not acceptable.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/" src/app/api/ src/lib/ && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <acceptance_criteria>
- The ledger's `wc -l` is at or above its pre-edit value; the obligation count is unchanged with a stated reason.
- The note carries before/after counts for **all nine routes across both halves**, names which half closed each, and states the capture policy in one sentence.
- **The joint audit is measured from the TREE**, per route: `grep -vE '^\s*(//|\*)' "$f" | grep -c "captureToSentry"` ≥ 1 for each of the nine (each read **0** pre-phase, so every one of the nine greps is falsifiable) — or a named exception with its reason.
- The manual Sentry verification is recorded as OWED with its exact step, not as done.
- M70's receipt pastes the failing test name and assertion text.
- M69 is either cross-referenced to `140.3-05`'s receipt with the assertion confirmed present, or freshly observed here — never silently skipped.
  </acceptance_criteria>
  <done>
SEAMUX-08's closure is recorded with before/after counts and a stated capture policy. The 2-of-9
under-count is corrected in place. The manual Sentry step is surfaced as owed. M70 observed RED and
M69 accounted for.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caught seam error → Sentry payload | undici embeds outgoing headers, and in one shape the error name, into the message |
| Network `data.code` → typed union | An upstream string is cast into `WizardErrorCode` with no membership check |
| Failure volume → alerting channel | A breaker trip fires repeatedly during the incident Sentry exists to surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.3-13-01 | Information disclosure | A Sentry capture carrying `INTERNAL_API_TOKEN`, a live `X-User-Access-Token` JWT, or raw exchange credentials (TRAP-1) | mitigate | Every new capture routes through `scrubSeamError`; tests assert both no-secret AND syscall-token-preserved, because over-redaction destroys the most valuable part of the line. |
| T-140.3-13-02 | Repudiation | The composite wizard variant emitting no funnel event, so a multi-key outage is invisible | mitigate | Emissions added on every error path; falsified by M70, which is meaningful precisely because the baseline was zero. |
| T-140.3-13-03 | Spoofing | Network data cast into `WizardErrorCode` without a membership check, so an upstream string becomes a typed code | mitigate | Per-route `ReadonlySet` membership checks at all three sites, copying `SubmitStep`'s correct shape. |
| T-140.3-13-04 | Denial of service | Capturing every breaker short-circuit, flooding Sentry during the correlated incident | mitigate | The stated policy excludes breaker short-circuits; any deviation must be argued explicitly. |
| T-140.3-13-05 | Repudiation | Closing 2 of 9 routes because CONTEXT named two | mitigate | Per-route acceptance grep over all nine, each printing ≥ 1 or naming a stated exception. |
| T-140.3-13-06 | Information disclosure | Importing `csv-finalize`'s `debug_context` pattern, which echoes an upstream body | avoid | Explicitly out of scope (TS-24, Phase 145); asserted by `git diff --name-only`. |
| T-140.3-13-07 | Repudiation | Claiming SEAMUX-08 verified when Sentry ingestion was never observed | mitigate | The manual step is recorded as OWED in the ledger and the SUMMARY, never as done. |
| T-140.3-13-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed. |
</threat_model>

<verification>
- `npx vitest run src/app/api/ src/__tests__/ src/lib/ "src/app/(dashboard)/strategies/new/wizard/"` green.
  ⚠️ **`src/__tests__/` is listed explicitly** — `csv-validate`'s test lives there, not beside its route.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- Zero Python files modified. `csv-finalize` NOT modified.
- **`140.3-12`'s copy-honesty test must still be green** — M71 stays falsifiable.
- **Full suite once at this wave boundary** (`npm test`) — nine route files changed across the two halves.
</verification>

<success_criteria>
- All nine zero-capture routes capture across the two halves under ONE policy, audited from the tree.
- `csv-validate`'s case executed from `src/__tests__/`.
- Every capture is scrubbed, and proven not to over-redact.
- The manual Sentry-ingestion step is surfaced as owed rather than claimed.
</success_criteria>

<output>
Create `.planning/phases/140.3-seamux-client-wizard-seam-error-surface/140.3-13b-SUMMARY.md` when done.
Record: this half's five routes; the joint 9-of-9 audit measured from the tree with which half closed
each; the capture policy as inherited (and any ambiguity you resolved); proof that `csv-validate`'s
case executed; the `create-with-key` non-disturbance check; the M70 receipt and the M69 accounting;
and the manual Sentry verification recorded as OWED.
</output>
