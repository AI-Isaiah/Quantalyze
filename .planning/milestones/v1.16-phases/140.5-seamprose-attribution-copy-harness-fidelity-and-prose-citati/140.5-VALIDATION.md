---
phase: 140.5
slug: seamprose-attribution-copy-harness-fidelity-and-prose-citati
status: signed
nyquist_compliant: true
wave_0_complete: true
created: 2026-07-29
---

# Phase 140.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> **Authored by the orchestrator BEFORE the planner spawns** (plan-phase step 5.5). Phases 135, 136,
> 138 have no VALIDATION.md at all and 140.3's was back-filled a day late, each time because the
> pipeline was hand-driven past this gate. It is not optional.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest 4.1.10 (TS) · pytest (Python) · eslint flat config + `tools/eslint-plugin-quantalyze` |
| **Config file** | `vitest.config.ts` · `analytics-service/pytest.ini` · `eslint.config.mjs` |
| **Quick run command** | `npx vitest run <changed files>` — **for iteration ONLY, never as a commit receipt** |
| **Full suite command** | `npx vitest run` · `cd analytics-service && pytest -q` · `npm run lint` · `npx tsc --noEmit` |
| **Node 22 CI parity** | `PATH=/opt/homebrew/opt/node@22/bin:$PATH npx vitest run` |
| **Estimated runtime** | ~100 s vitest · ~110 s under Node 22 |

⚠️ **A SCOPED RUN IS NOT A COMMIT RECEIPT.** Six times in this programme a scoped-green run would
have shipped a red tree — the sixth was **140.4's own fix round**, which committed after a scoped run
and broke two harnesses. Every commit runs the full suite first.

---

## Sampling Rate

- **After every task commit:** full `npx vitest run` + `npx tsc --noEmit`
- **After every plan wave:** full vitest + pytest + mypy + lint, **and Node 22 parity**
- **Before verification:** all of the above green
- **Max feedback latency:** ~120 s

**Measured baselines to preserve (verified at `4c60518b`):**

| gate | value |
|---|---|
| `npx vitest run` | **721 files · 10052 passed · 287 skipped · 0 failed** |
| same, **Node 22** | **identical — 721 / 10052 / 287 / 0**. ⚠️ 140.5-01 re-ran Node 22 at its own HEAD, not at the untouched base, so what is DISCHARGED is the property that matters (Node 22 and Node 25 agree exactly: 723 files / 10071 / 287 / 0 on both, v22.22.1) — **not** the 721/10052 figure at `4c60518b`, which remains unre-run on Node 22. |
| `tsc --noEmit` | exit 0 |
| `npm run lint` | 0 errors, **1 pre-existing warning** (`EquityChart.tsx:1119:6`) |
| `pytest -q` | **4803 passed / 96 skipped — ✅ RE-MEASURED 2026-07-29 by 140.5-01, reproduces EXACTLY.** `cd analytics-service && python3 -m pytest -q` → `4803 passed, 96 skipped, 1399 warnings in 71.69s`. The planning caveat is discharged. |
| `mypy --strict` | **clean, 89 files — ✅ RE-MEASURED 2026-07-29 by 140.5-01, reproduces EXACTLY.** `cd analytics-service && python3 -m mypy --strict --follow-imports=silent services/ routers/ models/` → `Success: no issues found in 89 source files`. The planning caveat is discharged. |

---

## Wave 0 Requirements

- **ZERO PACKAGE INSTALLS.** Verified at the gate by
  `git diff <phase base>..HEAD -- package.json package-lock.json` → **EMPTY**. (140.4 carried the
  same contract; DEF-140.4-B declined a Workflow DevKit install under it.)
- Existing infrastructure covers all phase requirements. No new framework.
- ⚠️ **A citation guard is a NEW artefact with no analog for its central decision.** Its Wave-0
  obligation is a **needle self-test in BOTH polarities** before it is trusted: it must be shown to
  catch a deliberately-broken citation AND to stay silent on a correct one. A scanner that matches
  nothing reports agreement forever.

---

## Manual-Only Verifications

| Behavior | Why Manual | Instructions |
|----------|------------|--------------|
| Copy vs `DESIGN.md` §Voice | Tests prove false claims are gone, not that replacements read well | Founder review. ⭐ **§4a's copy is already founder-approved — exempt.** New/changed entries owed review are enumerated in 140.5-02's and 140.5-05's SUMMARYs |
| TRAP-4 five clicks (C-4) | jsdom asserts the dialog's `open` attribute; seed-gated e2e specs skip | Real browser, live composite draft |
| Sentry ingestion (C-5) | ⚠️ **The stock instruction is WRONG** — tripping the breaker captures nothing by design at all 15 routes | Point `ANALYTICS_SERVICE_URL` at an **unroutable host** in preview to force a terminal-arm failure |
| **D-01 live-Redis lane** | `tests/redis/**` needs a live store; not in the default run | **KNOWN BLIND SPOT. No plan may claim it closed.** |

---

## Falsifiability Ledger

> **Coverage answers "is it verified?". This answers "CAN the verification FAIL?"**

**Rules — binding, and violated at least once in every prior phase of this milestone:**
- The mutation is a **semantic** edit to **PRODUCTION source** — a value, a boundary, a branch
  direction. **Never a syntax error, never a type error, never an edit to a test.** `tsc --noEmit`
  must exit 0 *under* the mutation, or the mutation is inadmissible and must be substituted.
- **Observed means run.** "The test covers it" is not evidence. Paste the failing assertion.
- A skipped mutation is recorded **skipped**, never **caught**.
- ⭐ **Prefer the SECOND member of a class.** 140.4 found nine acceptance criteria that passed on an
  untouched tree or failed on correct code; the whole phase existed because 140 shipped instance
  fixes that read as class fixes.
- **Revert every mutation before committing** and prove `git status --porcelain` is clean.
  ⚠️ **Never `git checkout --` a file holding uncommitted work** — 140.4's fix round destroyed 15
  uncommitted edits that way.
- ⚠️ **W10 — cross-fence mutations under `use_worktrees: true`.** Several mutations below edit
  production source OUTSIDE their owner plan's `files_modified` fence BY DESIGN (SC-VENUE-2,
  SC-429-1, SC-R1-1, SC-HARNESS-2). The revert-before-commit discipline is the ONLY thing
  preventing cross-worktree contamination: run each with no other uncommitted work present,
  capture evidence, revert, prove porcelain clean BEFORE any commit. Each affected plan states
  this at its mutation task.

Seeded from measurement; **the planner adds one row per success criterion it defines.**

| SC | Mutation (exact edit to production source) | Must turn RED | Observed? | Evidence |
|----|-------------------------------------------|---------------|-----------|----------|
| SC-CSV-1 | `CsvSubmitStep.tsx` — remove the new `recogniseSeamErrorCode` hop, leaving the top-level `data.code` read. ⭐ **The SECOND of the two clients; `CsvUploadStep` is the one an author has in mind.** | the class guard, naming the file | ✅ **Observed** (140.5-05) | Replaced `recogniseSeamErrorCode(seamErrorCode(data))` with the top-level read it succeeded: `const code: WizardErrorCode = (data.code as WizardErrorCode \| undefined) ?? "CSV_UPSTREAM_FAIL";`. `tsc --noEmit` exit 0 under it. **4 failed \| 22 passed.** The class guard NAMED THE FILE: *"A wizard client fetches a `rateLimitDenyJson` route but never CALLS `recogniseSeamErrorCode` … Offending files: `src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx`"*. Three behavioural cases reddened beside it, independently: the nested 424 (*"expected 'We couldn't check your file just now…' to contain 'We could not reach our own service.'"*), SC-CSV-4's nested 429, and the `CSV_RATE_LIMIT` hop. ⭐ The `CsvUploadStep` suite stayed GREEN, so the guard discriminates per-client rather than reddening on any edit anywhere. Reverted by edit; `git status --porcelain` empty. |
| SC-CSV-2 | `csv-validate` client — make the fallback arm also swallow `200 + ok:false`, so a genuine row-level failure takes the upstream sentence | a positive control: a real validation failure must still render row-level copy | ✅ **Observed** (140.5-05) | ⚠️ MECHANISM SUBSTITUTED — the first attempt was **INADMISSIBLE**. Widening the arm to `if (!res.ok \|\| data.ok === false)` gave `tsc --noEmit` exit **2**, `TS2367: This comparison appears to be unintentional because the types 'true \| undefined' and 'false' have no overlap` — the narrowing makes the later `data.ok === false` block unreachable. (Same class as 140.5-02's `TS2367`.) Substituted the semantically identical collapse that is tsc-clean: widen the first condition AND delete the now-dead `data.ok === false` block, i.e. re-merge the split this plan made. `tsc --noEmit` exit **0** under the substitute. `CsvUploadStep.upstream-arm.test.tsx` → **1 failed \| 10 passed**: *"POSITIVE CONTROL: 200 + ok:false with row errors still renders the per-row path — Unable to find an element by: [data-testid=\"wizard-csv-error\"]"*, with the DOM showing `data-error-code="CSV_UPSTREAM_FAIL"` — a genuine row-level failure wearing the §4a sentence. ⭐ Every negative control still PASSED, so the positive control is an independent oracle, not a duplicate. Reverted by edit (both halves); porcelain empty. |
| SC-CSV-3 | `WIZARD_ERROR_COPY` — drop `correlation_id` from the upstream entry's rendered fields | the §4a render assertion | ✅ **Observed** (140.5-05) | ⚠️ W10 cross-fence: the mechanism lives in `src/lib/envelope.ts`, outside this plan's `files_modified`. Run on a CLEAN tree (Task 3 committed first), evidence captured, reverted, porcelain proven empty. Mutation: `buildEnvelope`'s `correlation_id,` → `correlation_id: "",` — a value edit that leaves the field present and only empties it, so the test reds on the RENDER rather than on a deleted key. `tsc --noEmit` exit 0 under it. **4 failed \| 18 passed across BOTH clients**: *"expected 'We couldn't check your file just now…' to match /wizard:[0-9a-f-]{36}/"*, with the rendered panel showing `correlation_id: ` empty — the `correlation_id: —` shape of the original defect. The two wire-id cases (*"to contain 'wizard:99999999-…'"*) reddened separately from the two fallback-id cases, so the "prefer the wire, else the session id" rule is falsifiable on both halves. |
| SC-RA-1 | The `Retry-After` chokepoint — return `undefined` for the header on the **second** thread, not the first | per-surface arrival assertions | ✅ **Observed** (140.5-03) | `ConnectKeyStep.tsx`'s failure arm: `setRetryAfterSeconds(parseRetryAfterSeconds(res.headers))` → `setRetryAfterSeconds(null)` (a value edit, tsc-clean). `tsc --noEmit` exit 0 under the mutation. `ConnectKeyStep.test.tsx` → **2 failed \| 37 passed**: *"a 429 carrying `Retry-After: 30` renders the wait — TestingLibraryElementError: Unable to find an element by: [data-testid="error-envelope-wait"]"* and *"a SECOND failure with no header does NOT render the FIRST one's wait"* (the arrival case reds; the ABSENCE case correctly still passes, which is what makes them independent). Reverted with `git checkout HEAD --` on a file holding NO uncommitted work; `git status --porcelain` empty. |
| SC-RA-2 | `SyncPreviewStep.handleRetrySync` (`:1406`) — re-drop the header after it is wired | the sixth-thread assertion (⭐ **the path 141 builds on**) | ✅ **Observed** (140.5-03) | Deleted `setRetryAfterSeconds(parseRetryAfterSeconds(res.headers));` from the new non-2xx else-branch, leaving the classification and the envelope intact — so the mutation isolates the HEADER half from the RENDER half. `tsc --noEmit` exit 0 under it. `SyncPreviewStep.retry-sync.runtime.test.tsx` → **2 failed \| 6 passed**: *"a 429 retry carrying `Retry-After: 60` RENDERS AN ENVELOPE AND THE WAIT"* and *"classifies through the SAME wire→wizard hop as the first attempt"*, both `Unable to find an element by: [data-testid="error-envelope-wait"]`. ⭐ The envelope-rendering cases still PASSED, so the two halves of the sixth thread are falsifiable independently. Reverted; porcelain clean. |
| SC-B02-1 | Re-narrow the transport-catch needle so `"timed out"` no longer matches `"timeout"` | the B-02 replay case | ✅ **Observed** (140.5-02) | ⚠️ MECHANISM ADJUSTED TWICE, both recorded. (a) Per the planner note the fix is a TYPE-FIRST branch, so the mutation became *neutralise the type-first branch in `classifyKeyValidationError`*. (b) The first attempt — `wizardCode === "NEVER_A_MEMBER"` — was **INADMISSIBLE**: `tsc --noEmit` exit **2**, `TS2367: This comparison appears to be unintentional because the types 'WizardErrorCode' and '"NEVER_A_MEMBER"' have no overlap`. Substituted a BRANCH-DIRECTION edit, `wizardCode !== "UNKNOWN"` → `wizardCode === "UNKNOWN"`, under which `tsc --noEmit` exits **0**. `wizardErrors.test.ts` → **3 failed \| 130 passed**: *"a deadline miss reaches SERVICE_UNREACHABLE, not UNKNOWN/500 — expected { code: 'UNKNOWN', status: 500 } to deeply equal { code: 'SERVICE_UNREACHABLE', …(1) }"* and the same for the not-reachable message. ⭐ The third red is the marker-hygiene case, which is what proves the mutation hit the branch rather than the copy. Reverted by edit (never `git checkout --` — the file held uncommitted work); scoped suite back to 216/216. |
| SC-B02-2 | Neutralise `SERVICE_UNREACHABLE` at the **fourth or fifth** transport catch (`MultiKeyConnectStep.tsx:806` / `:936` — the two nobody counted) | the derived-population assertion | ✅ **Observed** (140.5-03) | The **fourth** catch (`MultiKeyConnectStep`'s add-key `validatePanel` arm): `errorCode: "SERVICE_UNREACHABLE"` → `"KEY_NETWORK_TIMEOUT"`. `tsc --noEmit` exit 0 under it. **BOTH receipts red, as the plan requires.** The derived-population guard named the site: *"A wizard catch block sets KEY_NETWORK_TIMEOUT … Offending sites: `src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx:869`"*; and the per-surface case *"the ADD-KEY catch (4th of five, never counted before) renders SERVICE_UNREACHABLE — state AND telemetry"* failed beside it. **2 failed \| 54 passed.** Reverted; porcelain clean. |
| SC-CITE-1 | Introduce one bare `file:line` citation into a seam-surface file | the citation guard | ✅ **Observed** (140.5-08) | ⚠️ W10: run on a CLEAN committed tree (Task 2 guard committed first, `git status --porcelain` empty before the plant), reverted, porcelain re-proven empty BEFORE any commit. Planted `see route.ts:999 for the shape` in a production comment at `keys/sync/route.ts:222`; `tsc --noEmit` exit **0** under it. `seam-citations.invariant.test.ts` → **1 failed \| 45 passed**: *"src/app/api/keys/sync/route.ts:222 cites \`route.ts:999\`. A bare \`file.ext:NN\` citation names a coordinate that goes stale…"* — naming the file AND the line. ⭐ The other 45 file cases stayed GREEN, so the guard discriminates per-file rather than reddening on any edit anywhere. Reverted by edit; `git status --porcelain` empty. |
| SC-CITE-2 | **Vacuity control** — run the guard on a tree with no bad citation and assert it reports **zero**, plus a population floor so a scanner matching nothing cannot pass | the guard's own self-test | ✅ **Observed** (140.5-08) | On the clean committed tree (porcelain empty): `seam-citations.invariant.test.ts` → **46 passed**, zero offences across all 34 surface files, AND the three population/vacuity fences hold — file count `.toBe(34)`, comment-line floor `>=6500` (measured 10863 via the real `extractComments`), allowlist `.toBe(1)`. Supplementary: seam-surface census `MODE=full` → `citations total: 1 / PAST-EOF: 0`, the sole hit being the out-of-repo `dist/index.mjs` allowlist entry. The fences are what stop a scanner-matching-nothing reporting agreement forever. |
| SC-HARNESS-1 | Remove `vi.unstubAllGlobals()` from `src/test-setup.ts` — **re-seated per the planner mechanism note below: the landed mechanism is the config flag, so the mutation is `unstubGlobals: true` → `false` in `vitest.config.ts`** | a leak-detection case (DEF-16-1's shape) | ✅ **Observed** (140.5-01) | `tsc --noEmit` exit 0 under the mutation. `src/test-setup.leak-canary.test.ts` → **1 failed \| 1 passed**: *"test 2 — sees neither, because the harness restored both … A global stubbed in the previous test survived into this one. `unstubGlobals: true` is missing from vitest.config.ts (or was turned off) … expected true to be false"*. Reverted; `git status --porcelain` empty. ⭐ The ENV assertion still PASSED under this mutation, which is what makes the two mechanisms independently falsifiable rather than one guard with two names. |
| SC-HARNESS-2 | Add a dynamic `import("@upstash/redis")` to a file the purity sets cover — **proven today to pass all four sets in every position** | the widened purity needle | ✅ **Observed** (140.5-01) | ⚠️ W10 cross-fence: appended `void import("@upstash/redis");` to **`src/lib/seam-copy.ts`**, outside this plan's `files_modified`. Run on a CLEAN tree (Task 3 committed first, so no other uncommitted work could be lost — the 140.4 fix-round hazard). `tsc --noEmit` exit 0 under the mutation. `seam-copy.purity.test.ts` → **1 failed \| 7 passed**: *"contains no DYNAMIC import() … src/lib/seam-copy.ts now contains a dynamic import(). A lazy import is still a dependency edge … expected true to be false"*. Reverted by removing the appended block (never `git checkout --`); `git status --porcelain` empty, `grep -c "import(" src/lib/seam-copy.ts` → 0, suite back to 8 passed. |
| SC-429-1 | Flip **one** of the seven 429 fixtures from nested back to flat | the shape assertion, naming the fixture | ✅ **Observed (140.5-06); ROW RE-WORDED by 140.5-08 per 06 OPEN(f)** | ⚠️ THE STATED RECEIPT IS UNREACHABLE AS WRITTEN: a fixture flip is a TEST edit (inadmissible), and the "new nested-at-429 contract case" is a vitest test whose upstream is a `STATE.upstreamPayload` mock — no Python edit reaches it. The admissible mutation reds the PYTHON pin instead: `internal.py:246` `raise service_error(429, "RATE_LIMITED", …)` → `raise HTTPException(status_code=429, detail=<same sentence>)` (the literal pre-PYAPIFIX2-03 shape — a real historical regression). RED: `test_internal_throttle_429_carries_the_service_envelope` — *"every deliberate error body must carry the R-2 envelope at .detail"*. Reverted by edit; porcelain clean; suite back to 28 passed. ⚠️ `mypy --strict` under the mutation was blocked by the sandbox classifier — type-admissible by construction (the `raise HTTPException(...)` idiom already appears twice in the same mypy-clean file), argued NOT executed (06 OPEN(g)). |

**Planner additions (2026-07-29, rev 2 per checker B-2/B-5; one row per new success criterion; owner plan in brackets):**

| SC | Mutation (exact edit to production source) | Must turn RED | Observed? | Evidence |
|----|-------------------------------------------|---------------|-----------|----------|
| SC-SCAN-1 [01] | `src/lib/source-scan.ts` — make `stripCommentsPreserveLines` stop blanking BLOCK comments (branch-direction edit, tsc-clean; this module is production source four guards import) | its unit tests (and, once landed, downstream guard self-tests) | ✅ **Observed** (140.5-01) | Dropped the `spans.push(…)` from the block-comment branch of `commentSpansTs` (a branch-direction edit — the span is still SKIPPED, just no longer recorded). `tsc --noEmit` exit 0 under the mutation. `source-scan.test.ts` → **5 failed \| 12 passed**, e.g. *"keeps a three-line block comment three lines long: expected '/\* line one' to be ''"* and *"treats the inside of `${ }` as CODE … expected 'const t = \`a ${ /\* gone \*/ b } c\`;' not to contain 'gone'"*. ⭐ It reddened `extractComments` cases too, which is the dual-over-one-tokenizer contract holding. Reverted; `git status --porcelain` empty; 17/17 green. |
| SC-ENV-1 [01] | Remove the `process.env` snapshot-restore from `src/test-setup.ts`'s `afterEach` (the DIRECT-assignment half `unstubEnvs` cannot cover) | the leak canary's env assertion | ✅ **Observed** (140.5-01) | Deleted `restoreEnv(testBaselineEnv);` from the `afterEach`, leaving `cleanup()`. `tsc --noEmit` exit 0 under the mutation. `src/test-setup.leak-canary.test.ts` → **1 failed \| 1 passed**: *"An env var assigned directly in the previous test survived into this one … `unstubEnvs` does NOT close this half … expected 'leaked-env' to be undefined"*. ⭐ The GLOBAL assertion still PASSED, the mirror image of SC-HARNESS-1 — the two halves red independently, so neither is carrying the other. Reverted; `git status --porcelain` empty; 2/2 green. |
| SC-COPY-1 [02] | Restore `"Validation failed. See per-row breakdown below."` as `CSV_VALIDATION_FAILED.title` | the promise-pin test (asserts the phrase's ABSENCE) | ✅ **Observed** (140.5-02) | `tsc --noEmit` exit 0 under the mutation. `wizardErrors.test.ts` → **2 failed \| 123 passed**: the promise pin — *"PROMISE PIN — no CSV copy on this surface promises a per-row breakdown: expected 'validation failed. see per-row breakd…' not to contain 'per-row breakdown'"* — plus the Phase-17 title pin re-pointed in the same commit. ⭐ Two INDEPENDENT guards reddened: the phrase-absence scan and the byte-exact title pin, so neither is carrying the other. Reverted by edit; scoped suite back to 125/125. |
| SC-VENUE-1 [02] | Delete the `WITHDRAW_SCOPE` row from `VENUE_WIRE_CODE_TO_VERDICT` — ⭐ the SECOND scope member, not `TRADE_SCOPE` | the real-detail-string replay test for that code | ✅ **Observed** (140.5-02) | `tsc --noEmit` exit 0 under the mutation. **2 failed \| 147 passed** across the two files. The replay test: *"WITHDRAW_SCOPE stops rendering copy that says TRADING — expected { code: 'KEY_HAS_TRADING_PERMS', …(1) } to deeply equal { … } − \"code\": \"KEY_HAS_WITHDRAW_PERMS\""* — i.e. it fell back to the cascade's `trading\|withdraw` branch, which is exactly the defect. ⭐ The parity guard reddened INDEPENDENTLY and by name: *"expected [ 'WITHDRAW_SCOPE' ] to deeply equal []"*, proving the disposition half is not merely decorative. Reverted by edit; 149/149. |
| SC-VENUE-2 [02] | **(re-seated per checker B-2 — the prior comment-mutation was inadmissible)** Change ONE real emitter literal in `analytics-service/services/exchange.py` (e.g. `result["error_code"] = "DDOS_PROTECTION"` → a novel code) — semantic Python production edit; ⚠️ W10 cross-fence | the emitter-derived parity guard, naming the code | ✅ **Observed** (140.5-02) | ⚠️ W10 cross-fence: `analytics-service/services/exchange.py` is in **140.5-04's** `files_modified`, not this plan's. Run on a **CLEAN tree** — Task 3 was committed first (`d6a24ab0`), so `git status --porcelain` was empty before the edit and no uncommitted work could be lost (the 140.4 fix-round hazard). Mutation: `result["error_code"] = "DDOS_PROTECTION"` → `"VENUE_EDGE_BLOCKED"`. `tsc --noEmit` exit 0 under it. `seam-venue-vocabulary.invariant.test.ts` → **2 failed \| 8 passed**: the roster pin — *`- "DDOS_PROTECTION"` / `+ "VENUE_EDGE_BLOCKED"`* — **and** the disposition check, *"expected [ 'VENUE_EDGE_BLOCKED' ] to deeply equal []"*. ⭐ The guard names BOTH the arriving code and the departed one, which is the fail-loud-arrival property stated in the objective. Reverted by edit; `git status --porcelain` **empty**; guard 10/10. **Python baseline re-proven after revert: `pytest -q` → 4803 passed / 96 skipped; `mypy --strict` → no issues in 89 source files.** |
| SC-RA-3 [03] | Remove the `Retry-After` relay at `process-key-client.ts`'s non-2xx arm (the SERVER chokepoint — distinct from SC-RA-1's client thread) | the chokepoint relay test | ✅ **Observed** (140.5-03) | `res.headers.get("Retry-After")` → `res.headers.get("X-Retry-After")` — a semantic value edit that leaves the relay mechanism intact and only stops it finding the header, so the test reds on the PROPERTY rather than on a deleted line. `tsc --noEmit` exit 0 under it. `process-key-client.test.ts` → **3 failed \| 45 passed**: *"expected null to be '17'"*, *"expected null to be 'Wed, 21 Oct 2026 07:28:00 GMT'"*, *"expected null to be '30'"* (the delta-seconds, HTTP-date and non-429 cases). ⭐ The absence case and the 403 body-verbatim positive control still PASSED, so they are controls rather than duplicates of the arrival assertion. Reverted; porcelain clean. |
| SC-SU-1 [03] | Revert `SubmitStep.tsx`'s transport catch to `KEY_NETWORK_TIMEOUT` (a THIRD member, distinct from SC-B02-2's two) | the transport-attribution class guard AND the per-surface behavioural test | ✅ **Observed** (140.5-03) | `setErrorCode("SERVICE_UNREACHABLE")` → `"KEY_NETWORK_TIMEOUT"` in the `catch (err)` arm. `tsc --noEmit` exit 0 under it. **Both receipts red:** the class guard named the site — *"Offending sites: `src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx:306`"* — and the per-surface case *"maps a thrown fetch (our own hop failing) to SERVICE_UNREACHABLE, not the venue-fault code"* failed beside it. **2 failed \| 46 passed.** ⭐ A THIRD distinct catch from SC-B02-2's, so the guard is shown to discriminate per-site rather than reddening on any edit anywhere. Reverted; porcelain clean. |
| SC-CSV-4 [05] | Drop `retryAfterSeconds` from `CsvSubmitStep`'s buildEnvelope context (the second CSV client) | the CSV wait-render test | ✅ **Observed** (140.5-05) | `retryAfterSeconds: advertisedWait ?? undefined` → `retryAfterSeconds: undefined` — the header is still read through the ONE parser, only its delivery to the builder is cut, so the test reds on the THREAD rather than on a deleted line. `tsc --noEmit` exit 0 under it. `CsvSubmitStep.upstream-arm.test.tsx` → **3 failed \| 19 passed**: SC-CSV-4's own case, the TRAP-3 case and the `CSV_RATE_LIMIT` control, all *"Unable to find an element by: [data-testid=\"error-envelope-wait\"]"*. ⭐ `CsvUploadStep.upstream-arm.test.tsx` stayed GREEN, so the two clients' wait threads are independently falsifiable and neither is carrying the other. Reverted by edit; porcelain empty. |
| SC-CSV-5 [05] | Re-introduce the duplicate render in `CsvValidationEnvelope.tsx` (heading also rendered as cause when `errors.length === 0` — the 140.4-16/CR-02 regression shape) | the double-render pin, both polarities | ✅ **Observed** (140.5-05) | `causeText = authoredCause ?? null;` → `causeText = authoredCause ?? envelope.human_message;` — the exact line CR-02 removed. `tsc --noEmit` exit 0 under it. `CsvValidationEnvelope.test.tsx` → **3 failed \| 5 passed**: the NEW pin (*"the panel printed one sentence in both of its copy slots … expected [ `<p>`, `<p>` ] to have a length of 1 but got 2"*), the pre-existing CR-02 case, and — independently — the SEAM_MISCONFIGURED contradiction guard, because the suppression path falls back to the heading under this mutation. ⭐ **Both polarities held:** the two positive counterparts (per-row heading + breakdown region, and the `CSV_FILE_TOO_LARGE` anti-control) still PASSED, so the pin is not satisfiable by deleting the second line. Reverted by edit; porcelain empty. |
| SC-AGREE-1 [05] | Edit a `KNOWN_*` roster literal so an overlapping code answers differently from `SEAM_CODE_TO_WIZARD_CODE` | the agreement assertion (property: AGREEMENT, never disjointness) | ✅ **Observed** (140.5-05) | ⚠️ W10 cross-fence: `SyncPreviewStep.tsx` is 140.5-03's file. Run on a CLEAN tree, reverted, porcelain proven empty. Mutation: `KNOWN_KICKOFF_CODES.RATE_LIMITED: "RATE_LIMITED"` → `"KEY_RATE_LIMIT"` — a real sibling code, so the roster stays type-valid and the edit is a plausible reword rather than a break. `tsc --noEmit` exit 0 under it. **2 failed \| 37 passed.** This plan's guard: *"a roster and SEAM_CODE_TO_WIZARD_CODE answer DIFFERENTLY for a code they both carry … + \"KNOWN_KICKOFF_CODES.RATE_LIMITED: roster=KEY_RATE_LIMIT wire=RATE_LIMITED\""* — naming the roster, the code and BOTH answers. ⭐ 140.4's `seam-ratelimit-posture.invariant.test.ts` reddened independently on the same edit, which is a cross-check that the new guard reproduces the old one's property rather than replacing it with a weaker one. |
| **two-way collapse** [05] | ⭐ **B-1's own regression proof, not a seeded row** — collapse BOTH CSV arms to the two-way `!res.ok → §4a` shape the checker rejected | the negative controls: `CSV_FILE_TOO_LARGE`, `CSV_RATE_LIMIT` **and** `SEAM_MISCONFIGURED` | ✅ **Observed** (140.5-05) | ⚠️ MECHANISM SUBSTITUTED — first attempt **INADMISSIBLE**. Guarding branch 1 with `if (false && …)` gave `tsc --noEmit` exit **2**, `TS2345`/`TS2322` ×4: the short-circuit destroys the `data.code !== undefined` narrowing, so `data.code` widens back to `string \| undefined`. Substituted a tsc-clean pair of semantic edits that produce the identical arm: **empty both rosters** (`new Set<string>([])`) so branch 1 is unreachable, **and** replace the hop's result with the constant fallback (`const code: WizardErrorCode = "CSV_UPSTREAM_FAIL";`), which together IS `!res.ok → §4a`. `tsc --noEmit` exit **0** under the substitute. **13 failed \| 9 passed** across the two clients — every required control red on BOTH: `CSV_FILE_TOO_LARGE` (the 11 MB case), `CSV_RATE_LIMIT` ×2, `SEAM_MISCONFIGURED` ×2, plus `CSV_INVALID_FORMAT` ×2, `CSV_PERSIST_FAIL`, `CSV_SESSION_REUSED`, the persist-fail retry fence and the two nested-body cases. ⭐⭐ **THE DECISIVE OBSERVATION: every §4a case (401, 403, non-JSON, unrecognised wire code) still PASSED under the collapse.** The §4a tests alone cannot tell the correct arm from the regression the checker caught — only the negative controls can, which is the whole argument for their existence. An intermediate run with the rosters emptied but the hop intact left `CSV_RATE_LIMIT` GREEN, confirming the exclusion really does route it through the hop. Reverted by edit; porcelain empty; full suite back to 729/10193. |
| SC-424-2 [06] | Make ONE 424-arrival route collapse the forwarded upstream status to 500 (pick `keys/sync`, the second-most-obvious) | that route's new 424 arrival case | ✅ **Observed** (140.5-06) | `keys/sync/route.ts`: `if (!result.ok) return result.response;` → re-wrap the same body with `{ status: 500 }` (the "collapse the forwarded upstream status" the row specifies); `tsc --noEmit` exit **0** under it. **1 failed \| 38 passed**: *"424 is CALLER'S EXCHANGE. Collapsing it to a 5xx would claim OUR service failed, which is false and — because a 4xx is breaker-inert while a 5xx is not — would also let a venue outage trip our own circuit.: expected 500 to be 424"*. ⭐ The `code` and `recoverable` assertions still PASSED, so the status assertion is an independent oracle. Reverted by edit; porcelain clean. |
| SC-R1-1 [06] | Weaken R-1 in `analytics-service/services/error_contract.py` (permit `retryable=True` at 500) — the oracle the three corrected 500-fixtures lean on; ⚠️ W10 cross-fence | the Python R-1 pin test | ✅ **Observed** (140.5-06) | ⚠️ W10 cross-fence, run on a CLEAN tree (Task 1 committed first). Branch-condition edit (not a deletion): `if retryable:` → `if retryable and status_code != 500:` at `error_contract.py:164` — since the arm is only reached at `status_code == 500`, this permits exactly what R-1 forbids; `mypy --strict` clean under it. **6 failed \| 8 passed**: `test_500_refuses_retryable_true[…]` ×6 — *"DID NOT RAISE ValueError"*. ⭐ The 8 positive counterparts still PASSED, so the pin discriminates the rule from a blanket refusal. Reverted by edit; porcelain empty; `pytest -q` → 4817/96, `mypy --strict` → 89 clean. |
| SC-SPEC-1 [06] | — no admissible production mutation exists: the guard's SUBJECT is test files, so any tripping edit is definitionally a test edit. Recorded via the Wave-0-sanctioned substitute: **both-polarity needle self-tests through the real pipeline**, incl. the DEF-16-2 specimen (a COMMENT containing a disabling token must NOT trip) | the guard's own self-tests | ✅ **Recorded via the Wave-0-sanctioned substitute** (140.5-06) | No admissible production mutation exists (the guard's subject IS test files, so any tripping edit is a test edit). Substitute = both-polarity self-tests through the REAL pipeline (same `offencesIn`, same comment-strip): POSITIVE — all 13 needles detected in live code, AND the WRAPPED multi-line form detected reporting the CALL's start line (not the line `true` sits on); NEGATIVE — the DEF-16-2 specimen (a COMMENT mentioning the token must NOT trip, a PARAPHRASE not a copy), `skipIf`/conditional `test.skip(<expr>, …)` preconditions, and `test.slow(`; KNOWN LIMIT asserted — a needle inside a STRING literal still matches (the stripper preserves strings by contract), pinned `.toBe(1)`. Plus the guard's green run over the real population proves all ten dispositions cleared it. |
| SC-TYPE-1 [07] | — compile-time receipts: transposed `mintTenantClaim` args and `breakerKey:null` with `counts:true` are now **unrepresentable**; a runtime mutation cannot exist while `tsc` exits 0 (the ledger's own admissibility rule). Receipt = `tsc --noEmit` + the byte-identical claim compatibility pin | `tsc` under any transposition attempt; the compatibility pin | ✅ **Observed** (140.5-07) — **COMPILE RECEIPT, recorded as such** | ⚠️ **A bare `tsc --noEmit` exit 0 is VACUOUS as a receipt** — it is compatible with the property being absent, the type-checking form of "a scanner that matches nothing reports agreement forever". Both halves were therefore made FALSIFIABLE and both were RUN. **(a) mint transposition:** two `@ts-expect-error` cases added to `tenant-claim.test.ts`. Under the fix `tsc` exits **0**; reverting the signature to `(identity: string, secret: string)` makes the guarded lines legal and `tsc` exits NON-ZERO with ***`src/lib/tenant-claim.test.ts:168:9 - error TS2578: Unused '@ts-expect-error' directive.`*** and the same at ***`:184:9`***. Reverted by edit. The runtime half rides along: the transposed shape throws `TenantClaimError` (GUARD 1 absorbs it via `identity?.userId`) and returns no claim, so the secret cannot reach the wire even from a JS caller. **(b) `breakerKey`/`counts`:** the receipt is that `recordOnce(verdict.breakerKey)` — parameter type `string` — COMPILES AT ALL; it does so only because `counts` narrows the `null` away. Flattening the union back to `{counts: boolean; breakerKey: string \| null}` reds `tsc` with ***TS2345 "Argument of type 'string \| null' is not assignable to parameter of type 'string'"*** at `resilient-fetch.ts:1617`, `:1916`, `:1949` — **"Found 3 errors in the same file"**, and in NO file outside this plan's fence. ⭐ That flattening probe is a TYPE edit and is therefore **INADMISSIBLE as a ledger mutation** — it is recorded here as the compiler-derived ENUMERATION the plan asked for, never as a caught mutation. **Byte-identical claims:** the exact-claim pin and the 4-case cross-language parity fixture keep their literals untouched and pass. |
| SC-TYPE-2 [07] | Re-introduce the `SeamBreakerVerdict` fail-open (a tsc-clean production edit tolerating the null-key path as countable, e.g. a defensive `?? ""` branch) — the RUNTIME half SC-TYPE-1's compile receipt cannot reach | the new fail-open regression test | ✅ **Observed** (140.5-07) — **RUNTIME, a real mutation** | New guard `src/lib/seam-breaker-failopen.regression.test.ts` (a NEW file — no plan's fence, so no worktree-merge collision). ⚠️ **MECHANISM NOTE, and it is the whole point of the row:** post-union, "keyless while counting" can no longer be spelled `null` — the type forbids it. Its remaining spelling is the EMPTY STRING, which is exactly the ledger's own `?? ""` shape and is a legal `string`, hence tsc-clean. Mutation: `seam-discriminator.ts`'s counting return, `dependency === null ? GLOBAL_BREAKER_KEY : …` → `dependency === null ? "" : …` — a semantic VALUE edit to production source on the 503-unrecognised-dependency fallback (⭐ the arm whose key is CHOSEN rather than constant). `tsc --noEmit` exit **0** under it. **2 failed \| 3 passed.** Positive form: *"A 503 COUNTED but nothing was recorded against a real breaker key… Expected one counter against `breaker:railway:failures`"* — `expected [ ':failures' ] to deeply equal [ 'breaker:railway:failures' ]`. Negative form, reddening INDEPENDENTLY: *"A failure was counted against \":failures\" — an identifier with no breaker key in front of it… the empty string is a legal `string`, so it type-checks, and it counts against a key belonging to no dependency."* ⭐ **The `breaker:supabase` case, the transport case and the 424 positive control ALL still PASSED**, so the five cases are independent oracles rather than one guard wearing five names, and the mutation is proven isolated to the fallback arm. Reverted by edit; `git status --porcelain` empty; 5/5 green. ⚠️ **Harness correction recorded rather than papered over:** the first draft's negative case looped over a store `beforeEach` had just cleared, so it passed VACUOUSLY — it now drives its own request and carries a population floor. A second draft used a `{ok,status,json}` literal, which `readDependencyBody` skips via its `typeof res.clone !== "function"` guard, so the body was never read and `breaker:supabase` came back as `breaker:railway` — a harness bug that read exactly like a production bug. |
| SC-PROSE-1 [04] | — **skipped-with-reason**: 0 past-EOF repo-wide. Receipt = the committed census predicate (`140.5-citation-census.mjs`, outputs pasted). No guard exists off-surface BY FOUNDER DECISION §4b, so no mutation can red a test; un-correcting prose is not an admissible semantic mutation | (census output, pasted) | ⏭️ **SKIPPED-WITH-REASON** (140.5-04) | **Reason, verbatim:** "receipt is the committed census predicate per founder decision §4b — no guard exists off-surface BY DECISION, so no mutation can red a test; un-correcting prose is not an admissible semantic mutation." **Predicate:** `node .planning/phases/140.5-*/140.5-citation-census.mjs src analytics-service e2e scripts supabase tests tools` (comment-scoped; full-suffix resolution; no basename fallback). **BEFORE** (at base `0210c0ad`): `source files: 1841 / citations total: 825 / uniquely resolved: 739 / ambiguous basename: 67 / basename not in repo: 19 / PAST-EOF: 18` — and `MODE=detail` listed all 18, reproducing RESEARCH §3.2 EXACTLY (12 → `analytics_runner.py`, 3 → `simulator_scoring.py`). **AFTER** (at `a45cfdbe`): `source files: 1841 / citations total: 805 / uniquely resolved: 719 / ambiguous basename: 67 / basename not in repo: 19 / PAST-EOF: 0`, and `MODE=detail`'s `--- PAST EOF ---` section is EMPTY. ⭐ Supplementary receipt, because a census is a grep and CONTEXT §3 forbids a grep as the receipt: all 15 files were proven **comment-only** mechanically — `git show 0210c0ad:<path>` vs worktree, both through `stripCommentsPreserveLines` (140.5-01's `source-scan.ts`) + whitespace collapse → `files compared: 15, code-changed: 0`. |
| SC-PROSE-2 [04] | — **skipped-with-reason** (same basis): population/value claims corrected (sentry-capture ×2 sites, permissions "5 minutes", seam-log-coverage "EIGHT", exchange.py self-cite). Receipt = comment-stripped absence checks via source-scan, outputs pasted | (absence-check output, pasted) | ⏭️ **SKIPPED-WITH-REASON** (140.5-04) | Same verbatim reason as SC-PROSE-1. **Predicate:** each needle matched against the file's COMMENT SPANS (`extractComments`, 140.5-01) and separately against `stripCommentsPreserveLines` output — a claim lives in prose, so a code-only check here is vacuous. Output: `captures nothing` → ABSENT from **both** `sentry-capture.ts` and `sentry-capture.test.ts` (prose=0 code=0 each); `(?<![0-9])5 minutes` → ABSENT from `permissions/route.ts` while `15 minutes` → PRESENT (prose=1), i.e. the TRUE Python-TTL claim survives; `EIGHT SINCE` → ABSENT from `seam-log-coverage.test.ts`. **Vacuity fence** (comment lines extracted, so an empty scan cannot pass): 176 / 96 / 345 / 524 / 271 / 432 across the six files. **Positive control:** `captureToSentry` in `sentry-capture.ts` prose → HIT. ⚠️ **The plan's own criterion was unsatisfiable as written**: `"5 minutes"` is a SUBSTRING of `"15 minutes"`, so "absent AND present in the same file" fails on CORRECT code — a left boundary was required. Recorded, not papered over. **Population re-measured** for the sentry claim, comment-stripped, four predicates: 45 sites/17 files (19-file roster) · 41/15 (its route members) · 112 sites/46 files/32 routes (repo-wide `src/` non-test minus the helper) · **0** for `captureException\|captureMessage` — the literally-true grep. Four predicates, four answers ⇒ the replacement text carries NO integer. |
| SC-PROSE-3 [04] | — **skipped-with-reason** (same basis): the two ORDINALS reworded (not deleted-as-counts, checker B-4). Receipt = comment-stripped absence of "sixteen" + the reworded sentences quoted | (quoted rewordings) | ⏭️ **SKIPPED-WITH-REASON** (140.5-04) | Same verbatim reason as SC-PROSE-1. **This plan owns ONE of the two ordinals** — `seam-budgets.invariant.test.ts`; the sibling at `SyncPreviewStep.poll-disjointness.runtime.test.tsx` is 140.5-03's file and was NOT touched (fence). **COUNT site** `resilient-fetch.wiring.test.ts` — was: *"…which no route test can catch because all sixteen of them mock these clients wholesale."* Now: *"…because every route test that mocks a seam client does so WHOLESALE — a `vi.mock("@/lib/analytics-client")` (or `process-key-client`, or `resilient-fetch`) replaces the module, so the transport underneath it is never exercised by any of them."* Integer deleted, predicate named. Check: `sixteen` → ABSENT (prose=0 code=0). **ORDINAL site** `seam-budgets.invariant.test.ts` — was: *"A sixteenth route was previously invisible to ALL of them at once."* Now: *"A route that consumed the seam WITHOUT being added to the table was previously invisible to ALL of them at once."* Keyed to the PROPERTY; deleting the integer as a count would have made the sentence FALSE. ⚠️ Check reports `sixteen` **PRESENT (prose=1)** in that file and that is CORRECT: the single hit is inside the paragraph the file explicitly quotes and marks *"IS NOW FALSE"* (140.4's record of refuted reasoning). Editing a marked quotation to satisfy a grep falsifies a historical record — recorded as PRESENT-with-reason rather than satisfied. A prose absence check must exclude marked quotations; nothing in this repo does that yet (named as a residual for 140.5-08). |
| SC-PROSE-4 [04] | — **skipped-with-reason** (same basis): seam-surface bare-citation remainder enumerated and handed to 140.5-08. Receipt = census MODE=full output over `140.5-seam-surface.txt`, pasted | (census MODE=full output) | ⏭️ **SKIPPED-WITH-REASON** (140.5-04) | Same verbatim reason as SC-PROSE-1. **`MODE=full` over the 34-file `140.5-seam-surface.txt`, at `a45cfdbe`:** `source files: 34 / citations total: 51 / uniquely resolved: 40 / ambiguous basename: 10 / basename not in repo: 1 / PAST-EOF: 0`. Reproduces RESEARCH §3.7 EXACTLY. ⭐ **The past-EOF guard §4b literally scopes is VACUOUS here and this run re-proves it: 0 of 51.** The 10 ambiguous are the 7 `resilient-fetch.ts → exchange.py` rows (2 candidates each) + 3 `route.ts:748` rows (**88** candidates each); the 1 unresolvable is `resilient-fetch.ts:761 → dist/index.mjs:757-761`, an out-of-repo `@upstash/ratelimit` path that needs CLASSIFYING, not fixing. **Hand-off to 140.5-08, measured not guessed:** **51 bare citations across 19 of the 34 files** (same comment-scoping + the census's own `CITE` regex). Per-file, with `resilient-fetch.ts` carrying 18 of the 51 on its own: `resilient-fetch.ts` 18 · `admin/match/eval/route.ts` 5 · `admin/match/recompute/route.ts` 4 · `wizardErrors.ts` 3 · `strategies/csv-finalize/route.ts` 3 · `keys/sync/route.ts` 2 · `strategies/composite/add-key/route.ts` 2 · `strategies/create-with-key/route.ts` 2 · `CsvSubmitStep.tsx` 2 · `ratelimit.ts` 1 · `seam-errors.ts` 1 · `keys/validate-and-encrypt/route.ts` 1 · `portfolio-optimizer/route.ts` 1 · `scenario/optimize/route.ts` 1 · `strategies/finalize-wizard/route.ts` 1 · `verify-strategy/route.ts` 1 · `SyncPreviewStep.tsx` 1 · `CsvUploadStep.tsx` 1 · `CsvValidationEnvelope.tsx` 1. The 15 files with zero are already clean and are the guard's negative controls. |

**Planner mechanism notes (seeded rows whose plan-chosen mechanism shifts the mutation — spirit kept, wording adjusted; record the note beside the evidence):**
- **SC-HARNESS-1**: the landed mechanism is `unstubGlobals: true` in `vitest.config.ts` (RESEARCH §2:
  config restore is tighter and cannot be shadowed by a file-local `afterEach`), so the mutation is
  *flip `unstubGlobals` to `false`* — same property, same canary.
- **SC-B02-1**: the fix is a TYPE-FIRST branch (own-property idiom), not a widened substring, so the
  mutation is *remove the type-first branch from `classifyKeyValidationError`* — the replay case
  ("Analytics service timed out after 15000ms…" → SERVICE_UNREACHABLE) must red.
- **SC-CSV-1**: run AFTER 140.5-05 Task 3 lands the class guard (a guard landed before the fix is
  red on arrival — PATTERNS §8); evidence must show the guard NAMING `CsvSubmitStep.tsx`.
- **SC-CSV-2**: under the THREE-WAY arm (corrected §6c) the mutation is *make the §4a fallback
  branch also swallow `200 + ok:false`*; 140.5-05 additionally runs the two-way-collapse
  falsification (the negative controls MUST red) as B-1's own regression proof.
- **SC-429-1**: a fixture flip is a TEST edit (inadmissible); the admissible production mutation is
  *make `internal.py`'s per-key throttle emit the FLAT shape* → the new nested-at-429 contract case
  reds. ⚠️ W10 cross-fence; Python baseline re-proven after revert.

---

## Oracle Independence

> The failure this catches: assertions that read their expected value out of the module under test,
> so the test passes for any implementation.

- [x] No test imports a **constant** from the module it tests — expected values are **literals**. ✅ The citation guard's `SEAM_CITATION_SURFACE`, allowlist and floors are all hand-typed literals; the registry floor is a hand-typed `57`, never `CONTRACT_GUARDS.length`.
- [x] No assertion compares a value to itself via a re-export, fixture, or table under test. ✅ The registry floor-bump is a literal (hazard #3 forbids `.length`); the discriminator/log-roster corrections reference the pinned `EXPECTED_EXPORTS` / derived `EXPECTED_SEAM_FILES`, NOT the module they describe.
- [x] Registry/roster sizes pinned to a **literal count**, never `len(THE_TABLE)`. ✅ `CONTRACT_GUARDS.length` `.toBeGreaterThanOrEqual(57)`; citation surface `.toBe(34)`; allowlist `.toBe(1)` — all hand-typed.
- [x] Any fake/double pinned against the real contract it stands in for. ✅ The citation guard runs the REAL `extractComments` pipeline end-to-end on synthetic source (no double); both-polarity needle self-tests. n/a for the prose corrections.

⚠️ **Phase-specific hazards, all measured:**
1. ⭐ **`sentry-capture.ts`'s false claim is defended by a grep that is LITERALLY TRUE** — zero
   `captureException` — while the sentence it supports is false (**107 sites** via
   `captureToSentry`, repo-wide predicate; 41/45 under the roster-scoped predicates). **A true grep
   supporting a false claim is this phase's purest specimen.** No plan may accept a grep as a
   receipt (CONTEXT §3).
2. **DEF-16-2 is live inside the mechanism this phase schedules:** widening `ci.yml:1633` to
   `src/**` + `tests/**` reddens a clean tree **today**, on one hit — and that hit is **a comment**
   (`ResponsiveChartFrame.test.tsx:6`). Comment-strip before counting.
3. ⭐ **No plan may cite "881 citations."** Three predicates produced three answers (817 / 846 / 391).
   **Every count in this phase publishes its predicate or is not a count.**
4. **Do not "correct" the registry's "six seam files" to eight** — the measured truth is **nineteen**,
   and `seam-log-coverage.test.ts:177` still says eight. Correcting to eight replaces one false
   integer with another. **The same rule covers the two ORDINAL "sixteenth route" sentences —
   reword, never delete-as-count (checker B-4).**
5. **The wire table and the client rosters are NOT disjoint** — two overlaps
   (`KNOWN_KICKOFF_CODES` ∩ = `{RATE_LIMITED}`, `KNOWN_FINALIZE_CODES` ∩ = `{SEAM_MISCONFIGURED}`).
   Assert **agreement**, not disjointness. Docblocks asserting the false version are corrected in
   140.5-03.

---

## Validation Sign-Off

- [x] Every task has an automated verify or a declared Wave 0 dependency
- [x] No 3 consecutive tasks without an automated verify
- [x] No watch-mode flags
- [x] **Every success criterion has a Falsifiability Ledger row**
- [x] **Every ledger row is `Observed ✅` with pasted evidence, or explicitly skipped-with-reason** — all seeded + planner rows verified above; the SC-PROSE-1..4 rows are the sanctioned census/absence-check recordings; SC-SPEC-1 and SC-TYPE-1 are the sanctioned needle-self-test / compile-receipt recordings.
- [x] **Oracle Independence checklist complete**
- [x] `git diff <base>..HEAD -- package.json package-lock.json` is EMPTY — verified against BOTH `4c60518b` (phase base) and `453b4291` (worktree base); both diffs empty (zero installs, T-140.5-SC).
- [x] Full suite green on Node 25 **and** Node 22 — Node 25: **733 files / 10265 passed / 287 skipped / 0 failed**; Node 22 (v22.22.1): **IDENTICAL 733 / 10265 / 287 / 0**. Plus `tsc --noEmit` exit 0; `npm run lint` 0 errors + 1 pre-existing warning (`EquityChart.tsx:1119`); `pytest -q` **4817 passed / 96 skipped / 0 failed**; `mypy --strict` **89 files, no issues**; census 0 past-EOF repo-wide + 0 bare in-repo on the surface.
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ✅ **SIGNED by 140.5-08 Task 3 (2026-07-30).** Every Falsifiability Ledger row is Observed or skipped-with-reason with pasted evidence; the Oracle Independence checklist is complete; the phase gate is green on both Nodes and both languages with zero package installs. ⚠️ Branch protection on `main` is OFF (CONTEXT §9), so every gate above WOULD HAVE CAUGHT its failure at merge — none can be said to have "stopped" anything.
