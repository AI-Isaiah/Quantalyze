---
phase: 140.5-seamprose
plan: 02
subsystem: wizard-error-vocabulary
tags: [wizardErrors, seam-vocabulary, copy, DEF-140.4-C, B-02, venue-codes, parity-guard, DEF-16-2]
requires:
  - "140.5-01 — src/lib/source-scan.ts (stripCommentsPreserveLines, py mode) is the parity guard's comment handler"
provides:
  - "WIZARD_ERROR_COPY.CSV_UPSTREAM_FAIL — the §4a founder copy, VERBATIM. The interface 140.5-05 routes its three-way arm onto."
  - "SEAM_CODE_TO_WIZARD_CODE += [\"CSV_RATE_LIMIT\",\"RATE_LIMITED\"] — the CSV rate-limit deny's route to the shared envelope, WITH its stamped Retry-After"
  - "CSV_RATE_LIMIT stays ABSENT from WizardErrorCode and from every KNOWN_* roster — pinned; the absence is what makes 140.5-05's branch (2) fire"
  - "seamTransportCode — an own data property on AnalyticsTimeoutError and the not-reachable Error, read type-first by classifyKeyValidationError"
  - "VENUE_WIRE_CODE_TO_VERDICT + VENUE_WIRE_CODES_WITHOUT_VERDICT — exported; the parity guard's subject"
  - "src/lib/seam-venue-vocabulary.invariant.test.ts — Python-EMITTER-derived ↔ TS disposition parity, owed CONTRACT_GUARDS registration by 140.5-08"
affects:
  - "140.5-05 — consumes the §4a entry and the CSV_RATE_LIMIT row; MUST NOT add CSV_RATE_LIMIT to any roster"
  - "140.5-08 — owes CONTRACT_GUARDS registration of seam-venue-vocabulary.invariant.test.ts"
tech-stack:
  added: []
  patterns:
    - "Own-property marker read with `typeof`, never `instanceof` — extended from seamCode to seamTransportCode, with the two kept as SEPARATE fields on a stated security ground"
    - "Emitter-derived population + hand-typed roster pin + vacuity floor + both-polarity scanner self-tests + a DECLARED blind spot asserted rather than narrated"
    - "Delete the bare integer, name the predicate — never correct one false integer to another"
key-files:
  created:
    - src/lib/seam-venue-vocabulary.invariant.test.ts
  modified:
    - src/lib/wizardErrors.ts
    - src/lib/wizardErrors.test.ts
    - src/lib/analytics-client.ts
    - src/lib/analytics-client.test.ts
    - src/__tests__/csv-validate-route.test.ts   # FALLOUT, outside the planned fence — see Deviations
    - .planning/phases/140.5-seamprose-.../deferred-items.md
    - .planning/phases/140.5-seamprose-.../140.5-VALIDATION.md
decisions:
  - "REUSE CSV_UPSTREAM_FAIL rather than mint a new code for §4a — it already exists, already means 'upstream failure on this surface', and a second code for one fact is the drift seam-copy.ts exists to prevent. PATTERNS §4's 'avoid the CSV_ prefix' advice loses to reuse-over-rename."
  - "seamTransportCode is a SECOND field, not a widening of seamCode: seamCode is what the UPSTREAM'S BODY declared, this is what OUR TRANSPORT observed. One field carrying both would let an upstream put UPSTREAM_TIMEOUT in its envelope and be handed our transport verdict."
  - "The not-reachable throw stays a PLAIN Error. A marker is data, not a type, so it adds machine-readability without widening the four-member taxonomy the nine wrappers branch on."
  - "The parity guard derives from the REAL EMITTERS, never from adapter.py's comment enumeration — which comment-strips to the EMPTY SET and omits csv_adapter's two static codes."
  - "The csv-validate-route user-blame needle was NARROWED from 'mentions the file' to 'the file is the subject of a failure verb', with both polarities asserted. The founder copy mentions the file while asserting the opposite of blame; the defect is ATTRIBUTION, not vocabulary."
metrics:
  duration: "~4h"
  completed: 2026-07-29
---

# Phase 140.5 Plan 02: wizardErrors vocabulary — §4a copy, B-02 by type, honest venue verdicts Summary

Landed the founder-approved §4a sentence as the ONE message for an unrecognised/codeless CSV upstream failure, closed B-02 by branching on a TYPE marker instead of a substring that never matched, gave four scope/permission wire codes honest verdicts, and put a Python-EMITTER-derived parity guard behind the roster so a newly-emitted code cannot arrive without a disposition.

---

## Coverage-law rows, stated (CONTEXT §2)

| remedy | row | in the required words |
|---|---|---|
| §4a copy at `CSV_UPSTREAM_FAIL` | **row 1** | one chokepoint code for every unrecognised/codeless upstream failure, so a future upstream status is covered without an edit |
| B-02's type-first branch | **row 1** | a shared classifier artefact, resolved through the ONE existing wire→wizard table |
| `CSV_RATE_LIMIT` wire-table row | **row 2** | one explicit row in a hand-typed table — **partial by construction** |
| The four venue rows | **row 2** | a hand-typed roster, **PARTIAL BY CONSTRUCTION** |

⚠️ **The parity guard does NOT promote the venue table to row 1, and no "effective row 1" claim is made.** The prior claim was withdrawn by checker B-2 and is not reinstated here. What the guard adds is strictly **fail-loud arrival**: its population is derived from the real Python emitters, so a newly-emitted code reddens CI by name until someone writes its disposition. **A roster that cannot silently miss a new member is still a roster.** The row-1 remedy is a shared `WireErrorCode` union across the two languages, which this phase does not schedule.

---

## Measured — every count with its predicate

### The baseline is NOT what the phase documents. Two suites are red on the untouched base.

**Predicate:** `npx vitest run` (Node 25, full suite, never scoped) at `0210c0ad`, working tree porcelain-clean, no edits present.

| gate | documented (CONTEXT §9 / prompt) | **MEASURED at this base** |
|---|---|---|
| `npx vitest run` | 723 passed / 19 skipped (742 files) · 10079 / 287 / **0 failed** | **721 passed / 19 skipped (742 files) · 10077 / 287 / 2 FAILED** |
| `tsc --noEmit` | exit 0 | exit 0 ✅ |
| `npm run lint` | 0 errors + 1 warning `EquityChart.tsx:1119:6` | identical ✅ |

The two red files, **each reproduced IN ISOLATION** so neither is worker contention alone:

1. `src/__tests__/contracts/contracts-registry.test.ts` → `[B25] eslint-plugin-quantalyze wiring integrity` → **`Error: Test timed out in 5000ms.`** (isolated: 1 failed | 55 passed)
2. `src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx` → `TestingLibraryElementError: Unable to find an element by: [data-testid="kpi-strip-mock"]` (isolated: **2 failed** — it fails *harder* alone than in the full run)

Both are timeout-shaped — the `DEF-140.4-A` class, **not** `DEF-140.5-A`'s module-registry race. Logged as **`DEF-140.5-B`** in `deferred-items.md`. Neither is in this plan's fence and neither was touched.

⭐ **`DEF-140.5-A` also reproduced**, exactly as documented: `strategy-review/route.test.ts` was green on baseline runs 1 and 2 and on the final run, and red on one intermediate run (*"expected 200 to be 503"*). It is **not** a regression and was not chased.

### Post-change, at `d6a24ab0` + the prose follow-up

**Predicate:** same command, same machine, no concurrent edits.

| gate | measured |
|---|---|
| `npx vitest run` | **722 passed / 19 skipped (743 files) · 10113 passed / 287 skipped / 2 failed** |
| the 2 failures | **exactly `DEF-140.5-B`'s two files. Nothing else.** |
| `tsc --noEmit` | exit 0 |
| `npm run lint` | 0 errors, 1 pre-existing warning (`EquityChart.tsx:1119:6`) |
| `pytest -q` | **4803 passed / 96 skipped** (re-run AFTER the cross-fence revert) |
| `mypy --strict --follow-imports=silent services/ routers/ models/` | **no issues in 89 source files** (re-run after revert) |
| **Node 22 parity**, this plan's four files | **4 files / 278 tests passed** — identical to Node 25 |

**Delta: +1 file, +36 tests, and every one of them is this plan's.** Reconciled per file so nothing hides in the total:

| file | base | now | delta |
|---|---|---|---|
| `src/lib/wizardErrors.test.ts` | 117 | 139 | **+22** (8 §4a · 8 B-02 · 6 venue) |
| `src/lib/analytics-client.test.ts` | 79 | 83 | **+4** (marker assignment + the separation negative control) |
| `src/lib/seam-venue-vocabulary.invariant.test.ts` | — | 10 | **+10** (new file) |
| `src/__tests__/csv-validate-route.test.ts` | 46 | 46 | 0 — assertions re-pointed, no case added or removed |
| **total** | | | **+36 = 10113 − 10077.** Nothing else moved. |

### The emitter derivation — predicate and full member list (owed by `## OPEN` (d))

**PREDICATE, stated in full so the count is reproducible without reading the code:** for every `*.py` under `analytics-service/services/**` (recursive), strip comments and docstrings with `stripCommentsPreserveLines(src, "py")`, then find every assignment whose LHS is `error_code` in the three forms this repo uses — `error_code = "X"` (bare), `result["error_code"] = "X"` (subscript), `error_code="X"` (kwarg) — take the RHS from just after the `=` to the first `,` or newline **at paren depth zero**, and collect every `"[A-Z][A-Z0-9_]{2,}"` literal inside it.

⭐ **The depth-aware RHS window is load-bearing, not tidiness.** `exchange.py` sets `result["error_code"]` from a **parenthesised four-line ternary** whose `else` branch is `MISSING_SCOPE` — the single worst code to miss, since it is the one rendering `UNKNOWN`/500. A line-bounded regex silently drops it and the derivation still looks healthy.

⭐ **Comment-stripping is load-bearing too, and DEF-16-2 is live in this exact tree.** A raw grep for `error_code\s*=\s*"…"` over `services/` returns **20** hits; **2 of them are comments** — `exchange.py`'s `error_code="MISSING_SCOPE"` note and `long_fetch.py`'s `error_code="PROBE_FAILED"` note. Comment-stripped: **18 literal-yielding assignment sites → 17 distinct codes.**

**The 17 members, and each one's disposition:**

| code | disposition |
|---|---|
| `AUTH_FAILED` | verdict row (pre-existing) |
| `DDOS_PROTECTION` | verdict row (pre-existing) |
| `EXCHANGE_UNAVAILABLE` | verdict row (pre-existing) |
| `NETWORK_UNAVAILABLE` | verdict row (pre-existing) |
| `PROBE_FAILED` | verdict row (pre-existing) |
| `RATE_LIMITED` | verdict row (pre-existing) |
| `MISSING_SCOPE` | **verdict row — NEW** → `KEY_MISSING_READ_SCOPE` / 400 |
| `PERMISSION_DENIED` | **verdict row — NEW** → `KEY_PERMISSION_DENIED` / 400 |
| `WITHDRAW_SCOPE` | **verdict row — NEW** → `KEY_HAS_WITHDRAW_PERMS` / 400 |
| `TRADE_SCOPE` | **verdict row — NEW** → `KEY_HAS_TRADING_PERMS` / 400 |
| `UNSUPPORTED_EXCHANGE` | exemption — replayed: reaches `UNKNOWN`/500, and that is honest (our own config gap, not the user's key) |
| `VALIDATION_UNEXPECTED` | exemption — replayed: `UNKNOWN`/500; it IS the producer's own unclassified residue |
| `MT5_MASTER_PASSWORD` | exemption — replayed: `KEY_MT5_MASTER_PASSWORD`/400 via the byte-pinned cascade branch |
| `MT5_WRONG_SERVER` | exemption — replayed: `KEY_MT5_WRONG_SERVER`/400, same |
| `CSV_TOO_LARGE` | exemption — CSV surface, rendered by the route vocabulary; never reaches this classifier |
| `CSV_FORMAT_UNSUPPORTED` | exemption — same family |
| `CSV_VALIDATION_FAILED` | exemption — same family, and the static fallback of the dynamic emitter |

**Vacuity floor: 10** (~59% of 17), with the predicate printed in the failure message, per `source-scan.ts`'s explicit obligation on callers (it BLANKS trailing comments, so a tokenizer bug now fails silent rather than loud).

**⚠️ DECLARED BLIND SPOT.** `services/ingestion/csv_adapter.py`'s validation arm sets `error_code=first_rule.upper() if first_rule else "CSV_VALIDATION_FAILED"`. `first_rule` is a pandera rule name read out of the report, so the family is **OPEN** — `COLUMN_IN_DATAFRAME`, `MONOTONIC_DATES` and every rule added later are emitted codes no static derivation can enumerate. Their disposition is **by family**: CSV-surface codes rendered through `CsvValidationEnvelope`, never reaching `classifyKeyValidationError`. The blind spot is **asserted, not narrated** — the guard pins the 8 literal-less `error_code` sites by `file:line`, so a *second* dynamic emitter appearing anywhere reds instead of silently shrinking the population.

### Citations, in the files this plan owns

**Predicate:** `MODE=full node .planning/phases/140.5-*/140.5-citation-census.mjs <paths>`.

| | before | after |
|---|---|---|
| `wizardErrors.ts` + `analytics-client.ts` | **3** citations (0 past-EOF, 0 ambiguous, 0 unresolved) | — |
| + `seam-venue-vocabulary.invariant.test.ts` | — | **0** |

All three converted to symbol anchors. One was **provably stale**: `strategyGate.ts:89` pointed at a docblock sentence; the `< 7` comparison it described is `spanDays < STRATEGY_GATE_MIN_DAYS`, ~131 lines away.

### The "sixteen" integer — deleted, not corrected

Per RESEARCH §3.10 / WP-15: the population measures **26 / 23 / 15 / 16 / 19** under five predicates, so the integer was defensible under exactly one. Every site in this plan's fence now names the predicate instead.

⭐ **RESEARCH §3.10's site list is incomplete, measured.** It lists `wizardErrors.ts:21`; there is a **second** occurrence in the same file, inside `classifyKeyValidationError`'s `seamCode` docblock, which no plan named. Found by re-grepping after the fix rather than by trusting the list — which is this phase's own subject matter. Post-change: `grep -in "sixteen"` over the three owned source files → **zero hits**, including my own replacement prose (a note *about* the removed word would itself be a grep-matching-its-own-docblock specimen, DEF-16-2).

---

## What shipped

### Task 1 — the §4a vocabulary

`CSV_UPSTREAM_FAIL` now carries the founder sentences **verbatim**, split title / cause / fix without a reword, with `clear_and_retry` (re-derived, not copied: `envelope.ts` derives `recoverable` from `actions`, so without it the copy would say "try again in a moment" beside an envelope rendering no Retry control) and `request_call` as the "send us this reference" affordance. `correlation_id` is **not** interpolated — the envelope component prints it, keeping the entry's dynamic-value count at ONE.

⚠️ **The entry's docblock states the CORRECTED §6c scope explicitly**: its population is the unrecognised-or-codeless upstream failure, **not** every `!res.ok`. The route's own caller-fault codes keep their own copy, and the docblock names the concrete regression that scoping prevents — an 11 MB upload being told the failure is "on our side, not your data".

**"Nothing was saved" is verified, not asserted**, and the verification is recorded in the entry rather than left in a plan file: zero writes in the Next route, `_run_validate_only` runs `adapter.validate()` only, and the `strategies` row is created on the CSV path only at finalize. The PostHog telemetry caveat is stated so a reviewer does not have to rediscover it.

`CSV_VALIDATION_FAILED` stops promising a per-row breakdown. The docblock records all three independent reasons the promise is false **on both arms**, and says explicitly that only the COPY half is done.

`SEAM_CODE_TO_WIZARD_CODE` gains `["CSV_RATE_LIMIT", "RATE_LIMITED"]` as an explicit row with a docblock in the table's existing style, and — the part 140.5-05 depends on — a pinned assertion that `CSV_RATE_LIMIT` is **absent** from `WIZARD_ERROR_COPY`, with the reason: 05's arm tries the already-known-code branch *before* the table hop, so admitting it there would keep the route's local copy and the stamped wait would never reach the shared envelope.

### Task 2 — B-02, closed by TYPE

`AnalyticsTimeoutError` gains `seamTransportCode = "UPSTREAM_TIMEOUT"` in its constructor; the not-reachable throw goes through a `notReachableError()` factory carrying `"UPSTREAM_NETWORK_ERROR"` — a factory rather than two `Object.assign` call sites, because an unmarked second thrower is precisely the drift that made B-02 possible. `classifyKeyValidationError` reads it before the cascade and resolves it through the **ONE shared wire→wizard table**, which already maps both to `SERVICE_UNREACHABLE`; status 502, unchanged from what the dead branch returned.

`SERVICE_UNAVAILABLE_RETRY`'s "Nothing was submitted" was **not** copied — a test asserts its absence on `SERVICE_UNREACHABLE` *and* its presence on the breaker entry, so the distinction 140.3-12 fixed cannot be re-merged.

The **collision invariant was re-derived, not copied**, and landed as an executable assertion rather than a comment: both real messages are checked against all 16 cascade needles, so the marker branch is provably *rescuing* an unclassified message rather than *shadowing* a verdict.

### Task 3 — four honest verdicts + the parity guard

Four rows, two new copy entries. `KEY_PERMISSION_DENIED` names **both** candidate causes (scope OR IP allowlist) without asserting either, and its test asserts the anti-control: `KEY_IP_ALLOWLIST` — the entry we stopped routing here — *still* states "you enabled IP pinning" as observed fact, which is exactly why it was the wrong answer.

⭐ `TRADE_SCOPE` is included **because it is already right**. A row that changes nothing today is what stops the next reword from changing it.

---

## Falsifiability Ledger — 4 rows owned, 4 observed, 0 skipped

| SC | mutation | observed RED |
|---|---|---|
| **SC-COPY-1** | restore `"Validation failed. See per-row breakdown below."` as `CSV_VALIDATION_FAILED.title` | `tsc` exit 0 under it. **2 failed \| 123 passed**: *"PROMISE PIN … expected 'validation failed. see per-row breakd…' not to contain 'per-row breakdown'"* + the byte-exact Phase-17 title pin. ⭐ Two INDEPENDENT guards reddened, so neither carries the other. |
| **SC-B02-1** | *(mechanism adjusted ×2)* neutralise the type-first branch | ⚠️ First attempt `wizardCode === "NEVER_A_MEMBER"` was **INADMISSIBLE** — `tsc` exit **2**, `TS2367 … have no overlap`. Substituted a BRANCH-DIRECTION edit (`!==` → `===`), `tsc` exit 0. **3 failed \| 130 passed**: *"expected { code: 'UNKNOWN', status: 500 } to deeply equal { code: 'SERVICE_UNREACHABLE', …(1) }"* ×2. |
| **SC-VENUE-1** | delete the `WITHDRAW_SCOPE` row — the **SECOND** scope member | `tsc` exit 0. **2 failed \| 147 passed**: the replay — *`− "code": "KEY_HAS_WITHDRAW_PERMS"`*, i.e. it fell back to the `trading\|withdraw` branch, the defect itself — **and, independently, the parity guard by name**: *"expected [ 'WITHDRAW_SCOPE' ] to deeply equal []"*. |
| **SC-VENUE-2** | ⚠️ **W10 cross-fence**: `exchange.py`'s `"DDOS_PROTECTION"` → `"VENUE_EDGE_BLOCKED"` | Run on a **CLEAN tree** (Task 3 committed first at `d6a24ab0`; porcelain empty before the edit). `tsc` exit 0. **2 failed \| 8 passed**: roster pin *`− "DDOS_PROTECTION"` / `+ "VENUE_EDGE_BLOCKED"`* **and** the disposition check *"expected [ 'VENUE_EDGE_BLOCKED' ] to deeply equal []"* — the guard names both the arriving code and the departed one. |

**Every mutation was a semantic edit to production source, `tsc --noEmit` exit 0 under it, reverted BY EDIT — never `git checkout --`, since these files held uncommitted work — and `git status --porcelain` proven empty afterwards.** The one inadmissible attempt is recorded as inadmissible and substituted, not silently dropped. Python baseline re-proven after the cross-fence revert.

⚠️ **`exchange.py` is in 140.5-04's fence, not this plan's.** The revert-before-commit discipline was the only protection; it was run on a clean tree, evidence captured, reverted, porcelain proven empty.

---

## Deviations from plan

### 1. [Rule 1 — bug] `src/__tests__/csv-validate-route.test.ts` broke and is in NO plan's fence

The csv-validate route renders `WIZARD_ERROR_COPY.CSV_UPSTREAM_FAIL.title` on its 502 arm, so replacing that title reddened three assertions in a file listed in no plan's `files_modified`. Directly caused by this plan's change and blocking, so fixed here and added to `key-files.modified`.

⚠️ **One of the three needed more than a re-pointed literal.** A class guard asserted `!/validation failed|your (csv|file|data)/i` — it bans the mere **mention** of the user's file. The founder-authored §4a sentence *is* "We couldn't check your file just now." A needle that rejects founder-approved copy is measuring the wrong property: **the defect is ATTRIBUTION, not vocabulary.** Narrowed to require the user's artefact to be the **subject of a failure verb**, and — because a narrowed needle is a weakened needle — **both polarities** are now asserted: it still fires on `"CSV validation failed."`, on `"Validation failed. See per-row breakdown below."` and on `"Your file failed our checks."`, and does not fire on the founder sentence.

### 2. [Rule 3 — blocking] Task 2's RED gate is not its own commit

Task 2's RED tests live in the same two files as Task 1's GREEN, so committing them alone would have left a red tree. **The RED was observed and is recorded** (`5 failed | 211 passed`, e.g. *"expected undefined to be 'UPSTREAM_TIMEOUT'"* and *"expected { code: 'UNKNOWN', status: 500 } to deeply equal { code: 'SERVICE_UNREACHABLE', …(1) }"*), then folded into `2ad0c80b`. The TDD gate sequence for the plan as a whole holds: `test(140.5-02)` RED at `b3f02e5a` → `feat(140.5-02)` GREEN at `2ad0c80b` → `feat(140.5-02)` at `d6a24ab0`.

### 3. [Rule 2 — correctness] A second `"sixteen"` site RESEARCH §3.10 did not list

Found by re-grepping the file **after** applying the documented fix, not by trusting the list. Fixed. Recorded because it is a live instance of the phase's own subject matter: a measurement carried forward without re-measurement.

### 4. [Rule 2 — correctness] Two exemption codes get an executed reason, not a written one

`VENUE_WIRE_CODES_WITHOUT_VERDICT`'s reasons are claims about runtime behaviour. The two claiming "reaches `UNKNOWN`/500 and that is honest" are now **replayed in a test**, so a reason that stops being true reds instead of aging into fiction.

### 5. [reporting] `VENUE_WIRE_CODE_TO_VERDICT` is now exported

Required so the guard can read its subject. The guard compares a **from-disk derivation against a hand-typed roster** (never a second derivation) and pins sizes to **literal** counts, per Oracle Independence.

---

## OPEN

**(a) `CONTRACT_GUARDS` registration of `src/lib/seam-venue-vocabulary.invariant.test.ts` — owed by 140.5-08.** Unlike 140.5-01's harness canary, this one **is** a structural contract over production source and belongs in the registry. Not registered here because 140.5-08 owns registration for the phase, and a half-registered registry is worse than an unregistered guard.

**(b) The per-row-breakdown DATA half — carried forward, PII fence intact.** Only the copy half is done. Fixing the data half means forwarding `csv_adapter.py`'s `debug_context.violations`, whose pandera messages embed **raw cell values** — the surface QA ISSUE-005 is fenced around. Three things must move together: `_envelope_error` must stop discarding `val.debug_context`; the client's `pandera_errors` key must reconcile with Python's `violations` (zero Python hits for `pandera_errors`); and the PII rule must not be weakened — `csv_validator.py:783`'s `row.get('column')` is NaN for `column_in_dataframe`, which is why the user reads `'nan'`, while the real name sits in a `failure_case` field the PII rule never echoes. **Not admitted here**, per CONTEXT §7's condition that it be closed only without echoing untrusted cell values.

**(c) Founder review owed for all Claude-drafted copy** (CONTEXT §8.1). Three entries:
- `CSV_VALIDATION_FAILED` — replacement title/cause/fix
- `KEY_MISSING_READ_SCOPE` — new
- `KEY_PERMISSION_DENIED` — new

⭐ **`CSV_UPSTREAM_FAIL`'s §4a copy is EXEMPT — it is founder-authored and pre-approved, reproduced verbatim, and is not open to a reword.** The tests prove the false claims are gone, not that the three replacements read well; that judgement is the founder's.

**(d) The emitter derivation's predicate + full member list** — published above under *Measured*, with the 17 members, each disposition, the vacuity floor and its ratio, and the dynamic-emitter blind spot.

**(e) `DEF-140.5-B` — two suites red on the untouched base**, neither in this plan's fence, both left alone per the SCOPE BOUNDARY rule. ⚠️ **Every later plan in this phase is asked to compare against a figure recorded as `0 failed` that does not reproduce on this machine.** Compare against the measured base and name the files.

**(f) `UNSUPPORTED_EXCHANGE` deserves its own wizard member.** It is exempted as `UNKNOWN`/500 on the honest ground that no `KEY_*` copy is true of it — it is our own configuration gap. A dedicated member would be a real improvement and is not this plan's.

---

## Known Stubs

None. No hardcoded empty values, no placeholder text, no unwired component. Both new copy entries carry every required `WizardErrorCopy` field with real sentences, and both new venue rows are reached by replayed tests using the real Python detail strings.

---

## Threat Flags

None. No new network endpoint, auth path, file-access pattern or schema change at a trust boundary. The three registered dispositions all hold: `VENUE_WIRE_CODE_TO_VERDICT` is still a `Map` (never a `Record`/identity cast — the new rows are explicit, and a test asserts a prototype-shaped marker cannot index it); both new copy entries are static with no interpolation of URL, status or hostname; and the parity guard's population is derived from real emitters, comment-stripped, with a floor and a declared blind spot.

⭐ One threat-shaped decision worth surfacing: `seamTransportCode` was kept **separate** from `seamCode` on a spoofing ground — folding them into one field would let an upstream body declare `UPSTREAM_TIMEOUT` and be handed our own transport verdict. A negative-control test asserts an `AnalyticsUpstreamError` carries no transport marker.

---

## Self-Check: PASSED

**Files claimed created/modified — all present on disk:**
- `src/lib/seam-venue-vocabulary.invariant.test.ts` — FOUND
- `src/lib/wizardErrors.ts` — FOUND
- `src/lib/analytics-client.ts` — FOUND
- `src/__tests__/csv-validate-route.test.ts` — FOUND
- `.planning/phases/140.5-.../140.5-02-SUMMARY.md` — FOUND

**Commits claimed — all present in `git log`:**
- `b3f02e5a` — FOUND · `test(140.5-02): RED — §4a founder copy, CSV_RATE_LIMIT row, per-row-breakdown promise pin`
- `2ad0c80b` — FOUND · `feat(140.5-02): GREEN — §4a founder copy, CSV_RATE_LIMIT row, B-02 closed by TYPE`
- `d6a24ab0` — FOUND · `feat(140.5-02): four honest venue verdicts + the EMITTER-derived parity guard`

**Final full-suite run, taken with no concurrent edits, matches the figures quoted above:**
`Test Files 2 failed | 722 passed | 19 skipped (743)` · `Tests 2 failed | 10113 passed | 287 skipped (10402)`, the two failures being exactly `DEF-140.5-B`'s pair.

## TDD Gate Compliance

`test(...)` RED at `b3f02e5a` → `feat(...)` GREEN at `2ad0c80b` → `feat(...)` at `d6a24ab0`. No `refactor` gate was needed. ⚠️ Task 2's RED was **observed but not separately committed** — see Deviation 2 for why and for the pasted RED output.
