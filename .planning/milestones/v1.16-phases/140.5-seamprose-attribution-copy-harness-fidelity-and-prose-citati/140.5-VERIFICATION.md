---
phase: 140.5-seamprose-attribution-copy-harness-fidelity-and-prose-citati
verified: 2026-07-30T13:35:00Z
status: passed
score: 16/16 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  note: initial verification
---

# Phase 140.5: SEAMPROSE — Verification Report

**Phase Goal:** What the codebase SAYS about itself is true — in user copy, in comments, in citations, and in the tests that stand in for the contract — so the next phase can trust what it reads. (Also carries the HARD PREREQUISITE for phase 141: `Retry-After` travels through the seam.)
**Verified:** 2026-07-30
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

Goal-backward method. The phase promises that descriptive claims (copy, comments, citations) and contract-standing tests are TRUE, enforced by FIVE new structural guards, plus the Retry-After hard prerequisite. Each guard was checked by **mutate-and-see** (mutate the protected artifact, confirm the guard reddens, revert), not grep-and-trust. Every mutation was reverted; `git status --porcelain` shows no source leakage (only orchestrator-managed `.planning/ROADMAP.md` + `.planning/STATE.md`).

### Observable Truths

| #  | Truth | Status | Evidence |
| -- | ----- | ------ | -------- |
| 1 | All five phase guards are registered in `CONTRACT_GUARDS` | ✓ VERIFIED | contracts-registry.test.ts:226–230 registers seam-venue-vocabulary, seam-transport-attribution, seam-wire-vocabulary, spec-disabling, seam-citations |
| 2 | The registry floor is a hand-typed literal (NOT `CONTRACT_GUARDS.length`) and equals the real count | ✓ VERIFIED | Floor `toBeGreaterThanOrEqual(57)` at :280; `grep -c '^  { path:'` = **57**; comment block :257–265 documents 52→57 with the git-show predicate |
| 3 | Guard `seam-citations` is non-vacuous | ✓ VERIFIED | Planted `bridge/route.ts:99999` in envelope.ts → **1 failed** ("cites `bridge/route.ts:99999`"); reverted |
| 4 | Guard `seam-venue-vocabulary` is non-vacuous | ✓ VERIFIED | Injected `error_code = "ZZ_VACUITY_PROBE"` in exchange.py → **2 failed** (roster mismatch + no disposition); reverted |
| 5 | Guard `seam-transport-attribution` is non-vacuous | ✓ VERIFIED | Injected `KEY_NETWORK_TIMEOUT` into a ConnectKeyStep catch → **1 failed** ("catch block sets KEY_NETWORK_TIMEOUT … ConnectKeyStep.tsx:525"); reverted |
| 6 | Guard `seam-wire-vocabulary` is non-vacuous | ✓ VERIFIED | Renamed the `recogniseSeamErrorCode(` call in CsvUploadStep → **1 failed** ("client … never CALLS `recogniseSeamErrorCode`"); reverted |
| 7 | Guard `spec-disabling` is non-vacuous | ✓ VERIFIED | Planted `describe.skip` temp spec → **1 failed** ("zzz-vacuity-probe.test.ts:2 → describe.skip("); temp file deleted |
| 8 | ⭐ `Retry-After` travels the seam (HARD PREREQ for 141) | ✓ VERIFIED | process-key-client.ts:690–692 relays+re-stamps the header on the forwarded non-2xx; SyncPreviewStep 6th thread `handleRetrySync` (:1418) reads `parseRetryAfterSeconds` (:1494) → `retryAfterSeconds` state → buildEnvelope (:1527). VALIDATION SC-RA-2 mutation observed |
| 9 | §4a founder copy exists verbatim as the ONE codeless-upstream message | ✓ VERIFIED | wizardErrors.ts:978 `CSV_UPSTREAM_FAIL.title = "We couldn't check your file just now."` |
| 10 | `CSV_RATE_LIMIT` maps to `RATE_LIMITED` in the seam table | ✓ VERIFIED | wizardErrors.ts:1968 `["CSV_RATE_LIMIT", "RATE_LIMITED"]` |
| 11 | Zero past-EOF citations in tracked source | ✓ VERIFIED | census.mjs over `src analytics-service e2e scripts supabase tests tools`: **755 citations, PAST-EOF = 0** |
| 12 | sentry-capture no longer claims the seam "captures nothing"; permissions no longer documents "5 minutes" for a 60s revalidate | ✓ VERIFIED | sentry-capture.ts:29 rewords the false claim in past tense; permissions/route.ts references the `revalidate` constant (:357 `revalidate: 60`), duration has a single home (:173–176) |
| 13 | Type hazards closed: `mintTenantClaim(identity: TenantIdentity, secret)`; `SeamBreakerVerdict` is a discriminated union | ✓ VERIFIED | tenant-claim.ts:158 signature applied; seam-discriminator.ts:153 union with `counts: true`/`counts: false` arms; VALIDATION SC-TYPE-1/2 observed |
| 14 | Post-exec fix (a89cedbf): KEK_UNAVAILABLE returns `PROBE_FAILED`, not `PROBE_BACKEND_UNAVAILABLE`; characterization test flipped to lock it | ✓ VERIFIED | route.ts `isConfigError` (:549) no longer greps `"not configured"`; regression test route.test.ts:751 asserts `PROBE_FAILED` (:795). 41/41 tests pass |
| 15 | Both CSV wizard clients consult the shared wire→wizard hop | ✓ VERIFIED | CsvUploadStep.tsx and CsvSubmitStep.tsx:376 both call `recogniseSeamErrorCode`; guard #6 proves the class is held |
| 16 | The signed Falsifiability Ledger + consolidated deferred-items exist | ✓ VERIFIED | VALIDATION.md `status: signed`, every SC row Observed with pasted mutation evidence; deferred-items.md:201 records 424 breadth as owed-forward |

**Score:** 16/16 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `src/lib/seam-citations.invariant.test.ts` | §4b bare-citation ban, 34-file surface, exports `SEAM_CITATION_SURFACE` | ✓ VERIFIED | 408 lines; exports surface roster; non-vacuous; passes |
| `src/lib/seam-venue-vocabulary.invariant.test.ts` | Python-emitter ↔ TS disposition parity | ✓ VERIFIED | 353 lines; population DERIVED from exchange.py; floor-fenced; non-vacuous |
| `src/lib/seam-transport-attribution.invariant.test.ts` | no `KEY_NETWORK_TIMEOUT` in any wizard catch | ✓ VERIFIED | 491 lines; balanced-brace catch walk; non-vacuous |
| `src/lib/seam-wire-vocabulary.invariant.test.ts` | hand-off + agreement + roster-completeness | ✓ VERIFIED | 681 lines; DENY-route population derived; non-vacuous |
| `src/__tests__/contracts/spec-disabling.invariant.test.ts` | comment-stripped whole-file skip/only ban | ✓ VERIFIED | 448 lines; disk-walked population; EXEMPT ledger pinned; non-vacuous |
| `src/lib/source-scan.ts` | shared comment module (`stripCommentsPreserveLines`, `extractComments`) | ✓ VERIFIED | 440 lines; imported by citation + venue + log guards |
| `contracts-registry.test.ts` | registration + hand-typed floor | ✓ VERIFIED | 57 rows, floor literal 57 |
| `140.5-VALIDATION.md` | signed Falsifiability Ledger | ✓ VERIFIED | signed; branch-protection-off caveat stated honestly |
| `140.5-deferred-items.md` | consolidated carry-forwards | ✓ VERIFIED | 424-arrival breadth + re-homes recorded |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| Python 429 handler | browser | process-key-client relays `Retry-After` verbatim | ✓ WIRED | :690–692 re-stamps header on forwarded response |
| wizard step responses | ErrorEnvelope `waitSeconds` | parseRetryAfterSeconds → state → buildEnvelope | ✓ WIRED | SyncPreviewStep 6th thread; SC-RA-2 mutation reds independently |
| exchange.py emitters | `VENUE_WIRE_CODE_TO_VERDICT` | parity guard parses real assignments via source-scan | ✓ WIRED | mutation added a code → guard red BY NAME |
| CsvUploadStep/CsvSubmitStep | seam-discriminator + wizardErrors | `recogniseSeamErrorCode(seamErrorCode(body))` | ✓ WIRED | both clients call it; hop guard holds the class |
| fixtures in 500-suites | error_contract.py `_validate` (R-1) | fixture literals match constructor output | ✓ WIRED | Python R-1 pin `test_error_contract_500_dependency.py` + `test_validation_error_contract.py` → **23 passed** |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| Registry + 5 guards green as-committed | `vitest run` (6 files) | 153 passed | ✓ PASS |
| Full contracts suite | `vitest run src/__tests__/contracts/` | 91 passed | ✓ PASS |
| Plan test files (03/07/02/05 owners) | `vitest run` (4 files) | 292 passed | ✓ PASS |
| permissions route (isConfigError fix) | `vitest run permissions/route.test.ts` | 41 passed | ✓ PASS |
| R-1 Python contract pin | `pytest` (2 files) | 23 passed | ✓ PASS |
| Citation census (past-EOF) | `node 140.5-citation-census.mjs …` | PAST-EOF = 0 | ✓ PASS |
| Non-vacuity (all 5 guards) | mutate → run → revert | each reddens; all reverted clean | ✓ PASS |

### Requirements Coverage

SEAMPROSE-01..08 are phase-local requirement IDs derived at planning from `140-SYNTHESIS.md` (WP-3/10/12/13/14/15). They are **not present in `.planning/REQUIREMENTS.md`** — consistent with the SEAMRIM (140.4) convention, where inserted-phase review-synthesis work is not a numbered requirement family in the v1 coverage table. All eight IDs are accounted for across the 8 plans.

| Requirement | Source Plans | Status | Evidence |
| ----------- | ------------ | ------ | -------- |
| SEAMPROSE-01 (citations/prose) | 04, 07, 08 | ✓ SATISFIED | census 0 past-EOF; citation guard non-vacuous; sentry/permissions reworded |
| SEAMPROSE-02 (Retry-After / SERVICE_UNREACHABLE) | 03, 05 | ✓ SATISFIED | header relay wired; transport-attribution guard non-vacuous |
| SEAMPROSE-03 (venue codes / B-02) | 02, 03 | ✓ SATISFIED | venue-vocabulary guard; AnalyticsTimeoutError type-first branch |
| SEAMPROSE-04 (harness fidelity / spec-disabling) | 01, 06, 08 | ✓ SATISFIED | unstubGlobals config; spec-disabling guard non-vacuous |
| SEAMPROSE-05 (type invariants) | 07 | ✓ SATISFIED | mint signature + discriminated union; SC-TYPE-1/2 observed |
| SEAMPROSE-06 (test fidelity / 429 shapes / R-1) | 06 | ✓ SATISFIED | R-1 Python pin passes; 424 breadth 1-of-5 deferred by decision |
| SEAMPROSE-07 (§4a copy / venue) | 02, 05 | ✓ SATISFIED | verbatim §4a copy; CSV_RATE_LIMIT→RATE_LIMITED |
| SEAMPROSE-08 (CSV class / hand-off hole / citation guard) | 05, 08 | ✓ SATISFIED | both CSV clients consult hop; wire-vocabulary + citation guards registered |

_Note: registry batch string for seam-venue-vocabulary labels it `SEAMPROSE-05` while plan-02 frontmatter lists `SEAMPROSE-03/07`. Cosmetic label drift in a comment string; the guard exists, is registered, and is non-vacuous. Not goal-blocking._

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| — | — | No unreferenced `TBD`/`FIXME`/`XXX` in the 5 guards or key modified source | ℹ️ Info | Clean. (`skip/only/fixme` tokens in spec-disabling are the guard's needle table, not debt markers.) |

### Human Verification Required

None. The phase is structural/prose: user-facing copy is pinned by tests (`seam-copy.pin`, §4a render assertions, VALIDATION ledger), the Retry-After path is proven by mutation, and both the TS and Python contract sides were executed here. No visual/UX/real-time behavior requires a human.

### Gaps Summary

No gaps. All 16 goal-level must-haves are VERIFIED against the codebase. The five structural guards are each registered in `CONTRACT_GUARDS`, the floor is a hand-typed literal `57` matching the real row count, and each guard was proven NON-VACUOUS by mutating its protected artifact and observing a red (all reverted, tree clean). The HARD PREREQUISITE for phase 141 — `Retry-After` traveling the seam, including the sixth `handleRetrySync` thread phase 141 builds on — is wired and mutation-falsifiable. The one deferred user-facing defect (KEK_UNAVAILABLE misattribution) is closed at a89cedbf and locked by a flipped characterization test.

**Deferred-by-decision (not gaps):** the 424-arrival breadth is 1-of-5 with the remaining four consolidated in `140.5-deferred-items.md:201`, exactly as the phase scope declared.

**Advisory-CI note (not a blocker):** branch protection on `main` is OFF, so every gate here is advisory at merge — the guards WOULD catch a regression on a future run; none can be said to have "stopped" anything. The full cross-runtime matrix (Node 22 + entire `pytest`/`mypy` suites) is recorded green in the signed VALIDATION ledger (Node25≡Node22: 733/10265/287/0; pytest 4817 passed; mypy clean); this verification independently re-ran the TS-side critical pieces, the R-1 Python pin, and the census, all green.

---

_Verified: 2026-07-30_
_Verifier: Claude (gsd-verifier)_
