---
status: partial
phase: 142.1-close-142-review-findings-chain-start-stamp-preservation-dep
source: [142.1-VERIFICATION.md]
started: 2026-08-03T06:10:00Z
updated: 2026-08-03T13:15:00Z
---

## Current Test

[/qa pass 2026-08-03 discharged item 4 (PASSED) and 2/3 of item 7 (PARTIAL).
Remaining open: 1, 2, 3, 5, 6 (prose-judgement / decision / CI-drill) and 7(c)
(reaped-row wizard render — needs a TEST strand + manager-owned session).]

⛔ ~~**Item 4 is the one that matters at merge.**~~ **Item 4 CLOSED on PROD 2026-08-03** — see its
result. The rest are prose-judgement or carried-forward items.

## Tests

### 1. JOB-03 requirement prose vs the shipped invariant
expected: The 43,920 s chain-inclusive derivation in `.planning/REQUIREMENTS.md:53` is the same derivation `TestReaperThresholdInvariant` enforces; the `batch_size x max_per_kind_timeout` formula appears only as rejected.
why_human: Ledger row SC-13. The automated verify proves string presence and co-occurrence ONLY. A grep can never establish that replacement prose is TRUE.
result: [pending]

### 2. The seven backfilled 142 ledger rows vs Pass C
expected: SC-3, SC-3b, SC-4, SC-4b, SC-5, SC-5b, SC-5c in `142-VALIDATION.md` are faithful transcriptions of Pass C's results; the corrected coordinates `job_worker.py:6704` and `keys/sync/route.ts:569` are right.
why_human: Ledger row SC-14. This phase produced NO new mutation observation for these rows — they were transcribed, not re-run. Only the file SHAPE was verified. **The 142 ledger was once reported closed on exactly this kind of shape-check** — that is why this row exists.
result: [pending]

### 3. The replacement `_mark_computing` comment
expected: The two-path story at `analytics-service/services/analytics_runner.py:1243-1271` (4-hop chain bullet, CSV-first bullet) each describes a real reachable path with the stated trigger behaviour.
why_human: D-01 prose half. Greps prove the false clause is gone and the trigger is named; nothing proves the replacement prose true. The BEHAVIOURAL claim underneath it WAS confirmed live on TEST.
result: [pending]

### 4. ⛔ D-19 merge-time PROD check (LIVE RESIDUAL)
expected: After `supabase-migrate` auto-applies on merge, `cron.job.command` for `reap_strategy_analytics_stuck_computing` carries TWO single-evaluation `WITH batch AS MATERIALIZED (...)` batches — one per arm:
`(length(command) - length(replace(upper(command),'AS MATERIALIZED','')))/length('AS MATERIALIZED') = 2` and no `IN ( SELECT ... LIMIT` shape.
why_human: **PROD's registered reaper body still carries the unbounded shape** until `feat/v1.16-142-146-job-rate` merges. `20260803130000` is applied to TEST only (stamped `20260802212852`). ⚠️ Checking for a `LIMIT` token is NOT sufficient — token-presence is exactly what every gate in phases 142 and 142.1 passed over while the bound did not exist at runtime. Tracked as TODOS item 6.
result: PASSED (2026-08-03, post-merge /qa) — read-only against PROD `khslejtfbuezsmvmtsdn` after PR #659 landed in `main`. The stated predicate holds: `AS MATERIALIZED` count = 2, no `IN ( SELECT ... LIMIT` shape. NOT closed on token presence: the full 1,765-char `cron.job.command` was read and shape-checked — each arm is one single-evaluation `WITH batch AS MATERIALIZED (SELECT … LIMIT 25 FOR UPDATE SKIP LOCKED) UPDATE … FROM batch`. Job `*/15 * * * *`, `active=true`. The deployed body also carries the clock-start companion arm (stamps `computing_started_at` on `(computing, NULL)` rows non-destructively), closing 142's Gap-3 deploy-ordering window. TODOS item 6 annotated ✅ CLOSED.

### 5. `supabase-migrate` fails loud on missing secrets
expected: On a clone with Supabase secrets removed, pushing a commit touching `supabase/migrations/**` to `main` produces four `::error::` lines and a FAILED run — not a green no-op.
why_human: The workflow SHAPE is pinned by vitest (128 green) and all three arms of the extracted shell body were driven locally, but that is evidence about the SCRIPT, not about GitHub's dispatch of it.
result: [pending]

### 6. Decision: mutant-run SC-17/01 and SC-11 RED halves on TEST?
expected: A recorded decision. If TEST-side runs are wanted, each mutation (delete the trigger's coercion arm; delete the companion cron arm) must redden its Part-6 / arm-D assertion on TEST.
why_human: Both rows are recorded as positive-half-observed-on-TEST with the RED half design-proven + local-fixture only, **NOT** mutant-run on TEST. This is honestly recorded, not hidden — the open question is whether that is accepted.
result: [pending]

### 7. Carried from Phase 142, unchanged
expected: Per `142-VERIFICATION.md`'s human items — PROD cron registration, the PROD backfill census, and the live wizard render of a reaped strategy.
why_human: Requires PROD access and a live browser session. Not touched by 142.1.
result: PARTIAL (2026-08-03, /qa) — two of three closed: (a) PROD cron registration confirmed (`reap_strategy_analytics_stuck_computing`, `*/15 * * * *`, active; also registered+active on TEST `qmnijlgmdhviwzwfyzlc`); (b) PROD backfill census re-run at check time: `computation_status='computing'` count = **0**, matching the migration header's authoring-time census. (c) The live wizard render of a reaped strategy stays OPEN: PROD has zero reaped rows to render (census 0; no row carries the reaper's message), the authed browser session is the Allocator QA account (`qa-demo@quantalyze.app`) which owns no strategies, and stranding a row on PROD is not acceptable — it needs the TEST-strand path exactly as 142-VERIFICATION prescribed. Adjacent evidence gathered: the wizard's AUTH_FAILED error arm renders the 140.x panel correctly live (named copy, Retry, Diagnostics with code+correlation_id only).

## Summary

total: 7
passed: 1
partial: 1
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps
