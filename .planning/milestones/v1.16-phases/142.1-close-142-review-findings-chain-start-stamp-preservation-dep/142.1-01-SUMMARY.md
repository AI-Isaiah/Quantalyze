---
phase: 142.1
plan: 01
subsystem: job-state-integrity / evidence-integrity
tags: [ci-gate, sql-snapshot, requirements, falsifiability-ledger, deferral]
requires:
  - "supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql (Phase 142)"
  - ".planning/phases/142-.../142-VERIFICATION.md Pass C (source data for D-14)"
provides:
  - "green sql-function-snapshot CI gate (D-12 run 1)"
  - "canonical sync_strategy_analytics_status body carrying the 142 stamp semantics"
  - "JOB-03 requirement text matching the shipped chain-inclusive derivation"
  - "142-VALIDATION.md at 11/11 Observed with honest provenance"
  - "DEF-142.1-08 hard-rule deferral in TODOS.md"
affects:
  - "plan 142.1-05 (D-12 run 2 — the snapshot must be regenerated AGAIN after the trigger migration lands)"
  - "plans 143/144/145 (JOB-03 re-derivers now read the correct formula)"
tech-stack:
  added: []
  patterns:
    - "generated-artifact commit: snapshot regenerated by tooling, committed alone, never hand-edited"
    - "evidence transcription: Observed/Evidence copied verbatim from the verifier pass; coordinates corrected in the same edit"
key-files:
  created: []
  modified:
    - "supabase/schema/functions/sync_strategy_analytics_status.sql"
    - ".planning/REQUIREMENTS.md"
    - "TODOS.md"
    - ".planning/phases/142.1-close-142-review-findings-chain-start-stamp-preservation-dep/142.1-RESEARCH.md"
    - ".planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-VALIDATION.md"
decisions:
  - "DEF-142.1-08 was STRENGTHENED IN PLACE rather than appended — a plan-time entry already existed and TODOS.md is the single-ground-truth backlog; a duplicate would have been worse than the gap."
  - "The 142-VALIDATION.md sign-off box 'SQL gate MCP-applied to TEST' was deliberately LEFT UNCHECKED. D-16 is still owed; checking it would repeat the over-claim this phase exists to correct."
  - "Wave 0 Requirements checkboxes were checked (file-presence verified on disk) so the file does not assert wave_0_complete: true over an all-unchecked list — a smaller instance of the same conflation D-14 repairs."
metrics:
  duration: "~25 min"
  tasks: 3
  files: "5 modified, 0 created"
  completed: 2026-08-02
---

# Phase 142.1 Plan 01: Clear the hard-red CI gate + close the documentation-integrity items — Summary

Regenerated the stale `sync_strategy_analytics_status` snapshot as the phase's first
commit (the branch's only hard-red gate, now exit 0), then made the paper trail honest:
JOB-03 now states the chain-inclusive 43,920 s derivation with the compute_jobs formula
labelled rejected in place, D-08 is formally deferred as `DEF-142.1-08` with its
zero-hit-grep evidence and the Oracle-Independence conflict, and Phase 142's
Falsifiability Ledger reads 11/11 Observed with provenance that names the earlier
over-claim rather than hiding it.

## What Was Built

### Task 1 — D-12 run 1: regenerate the stale SQL function snapshot (phase's FIRST commit)

`npm run schema:functions` replayed all migrations and rewrote
`supabase/schema/functions/sync_strategy_analytics_status.sql`. The committed body had
been the **pre-142** function (header cited migration `20260710150000`, zero
`computing_started_at` maintenance) — meaning any author re-basing from the canonical
snapshot would have silently reverted the entire JOB-01 stamp semantics.

The regenerated body now cites `20260802120000_strategy_analytics_stuck_computing_reaper.sql`
and carries seven `computing_started_at` occurrences: the three-arm `CASE` in branch (a)
(`:71-83`, ending `ELSE strategy_analytics.computing_started_at` — the no-advance arm) and
the branch (b)/(c) `NULL` clears (`:140`, `:163`).

Only one snapshot file changed of the 104 regenerated. Committed alone (S-9 respected).

### Task 2 — D-13 + DEF-142.1-08 + research OQ closure

**`.planning/REQUIREMENTS.md` JOB-03** rewritten. It now specifies the chain-inclusive
worst case — walk `JOB_CHAIN_FOLLOW_ON` over `TIMEOUT_PER_KIND`, sum the per-hop ceiling
`(batch_size - 1) × max_handler + handler × max_attempts + backoff` — yielding **43,920 s
(12.2 h)** under the shipped 16 h threshold. The `test_every_kind_has_watchdog_headroom`
mirror reference and the "never copied from the `compute_jobs` 4h number" clause are kept.
`batch_size × max_per_kind_timeout` survives in the text **only** as an explicitly
`⛔ REJECTED` mention on the same physical line as the word REJECTED, with C-6's 9,000 s
(≈4.9× too small) result and the reason it is wrong (it counts only the LAST chain hop).
It stays in the text deliberately, so a future re-deriver meets it labelled as the wrong
answer rather than rediscovering it as plausible.

**`TODOS.md` `DEF-142.1-08`** — the entry already existed from plan time and carried four
of the five required elements. The hard rule was phrased "Do not close this by bumping
15 → 16"; it now reads **"Hard rule: never close this by bumping the literal to 16"**, plus
an execution-time confirmation that `test_main_worker.py:1295` is untouched. No duplicate
entry was created (see Deviations).

**`142.1-RESEARCH.md`** — `## Open Questions` retitled `## Open Questions (RESOLVED)` with a
banner, and each question carries an inline `**RESOLVED:**` marker while its original text is
unaltered: OQ-1 → C-5 (D-08 CUT, tracked as DEF-142.1-08); OQ-2 → plan 142.1-07 Task 3
(psql preferred, MCP `execute_sql` block-by-block fallback, route stated in the D-16
evidence); OQ-3 → C-11 (both negative anchors scoped to the bridge's `pg_get_functiondef`;
the binding constraint is instead `intervals == {expected}` over the whole `$cron$` body).

**D-08 code untouched:** `grep -rn 'len(TIMEOUT_PER_KIND)' analytics-service/tests/test_main_worker.py`
still returns the unchanged `assert len(TIMEOUT_PER_KIND) == 15` at `:1295`.

### Task 3 — D-14: backfill the seven Phase-142 ledger rows

Seven rows (SC-3, SC-3b, SC-4, SC-4b, SC-5, SC-5b, SC-5c) filled from
`142-VERIFICATION.md`'s Behavioral Spot-Checks rows tagged `[ledger row closed]`
(`:181,183,185,187,188,190,191`), transcribed rather than re-derived or re-run. The
ledger now reads **11/11 Observed, 0 pending**.

The three bonus mutations Pass C also ran were **NOT** folded in — they are not ledger
rows: `:182` (SC-3 upper bound), `:184` (conjunct deletion), `:186` (script resurrection),
`:189` (SC-5 `None` variant).

**Coordinates corrected in the same edit (C-3):** SC-5b's Mutation cell is now
`job_worker.py:6704` (was `~:6635` / `~:6744`); SC-5c's is now
`src/app/api/keys/sync/route.ts:569` (was `:532`). The old numbers survive only inside the
provenance note that records the correction.

**Provenance note** above the ledger states that these seven were verifier-run
**post-execution**, not at execution time, and names the prior in-session
"11 Observed / 0 SKIPPED" report as **WRONG** — only the four previously-SKIPPED rows had
been closed; the other seven had never been run.

**Non-backfill note** added above the Per-Task Verification Map: its rows were deliberately
not backfilled (D-14's scope is the ledger only), its pending/❌ cells are **not** evidence
of execution, and the frontmatter's `status: executed` refers to the ledger's Pass-C closure.
Without this, declaring the file executed over an all-pending status table would be a
diluted instance of the exact conflation that created this phase.

**Frontmatter:** `status: executed`, `wave_0_complete: true`. **Sign-off:** boxes checked
where genuinely true; **Approval: CLOSED 2026-08-02** citing Pass C, with the closure
explicitly scoped to the ledger and D-16 named as still open.

## Falsifiability evidence

### SC-12 — the sql-function-snapshot gate (AUTOMATED, observed both directions)

**Pre-fix (RED), observed first-hand this session before any edit** — matches the
orchestrator's 2026-08-02 verification:

```
$ npx tsx scripts/dump-sql-functions.ts --check
SQL function snapshot is stale (1 file(s)) — a migration changed a function
body but supabase/schema/functions/ wasn't regenerated:
  stale:    supabase/schema/functions/sync_strategy_analytics_status.sql

Run `npm run schema:functions` and commit supabase/schema/functions/.
[exit 1]
```

**Post-fix (GREEN), re-confirmed after all three task commits:**

```
$ npx tsx scripts/dump-sql-functions.ts --check
SQL function snapshot is current (104 functions).
[exit 0]
```

Supporting acceptance evidence:

- `grep -n "source migration"` → `20260802120000_strategy_analytics_stuck_computing_reaper.sql`
- `grep -v '^--' … | grep -c computing_started_at` → **7** (criterion: ≥ 3)
- `git log --oneline -1 --name-only` on `fea74933` → exactly one path, under
  `supabase/schema/functions/`
- `git diff --diff-filter=D HEAD~1 HEAD` → no deletions

### SC-13 — ⚠️ HUMAN / REVIEWER-ONLY. Recorded as NOT an automated pass.

Whether JOB-03's rewritten text is a **correct and sufficient** statement of the shipped
derivation is a judgment call a grep cannot make. The automated verify only proves
co-occurrence and string presence:

- `grep -F 'batch_size × max_per_kind_timeout' .planning/REQUIREMENTS.md | grep -ci 'reject'` → **1**
- `grep -c '43,920' .planning/REQUIREMENTS.md` → **1**
- `grep -c 'chain-inclusive' .planning/REQUIREMENTS.md` → **1**
- `grep -c 'DEF-142.1-08' TODOS.md` → **1**; entry contains `STRATEGY_SCOPED`, `Oracle`,
  and `never close this by bumping`

**A passing grep here is not evidence the derivation is right.** SC-13 closes only on a
human/reviewer reading the JOB-03 text against the shipped `TestReaperThresholdInvariant`.

### SC-14 — ⚠️ HUMAN / ORCHESTRATOR-ONLY. Recorded as NOT an automated pass.

The seven ledger rows were **transcribed**, not re-run. This plan produced **no new
observation** of any mutation — the underlying evidence is Pass C's, and its trustworthiness
is Pass C's to carry. The automated verify only proves the file's shape:

- zero `⬜ pending` remain in the ledger table (rows re-counted directly: **11 Observed / 0
  pending**)
- `job_worker.py:6704` and `route.ts:569` present; SC-5b/SC-5c cells free of `6635`/`6744`/`:532`
- SC-3 evidence contains `5,760` and `headroom`; SC-5c evidence contains `TS half RED`

**SC-14 closes only on the orchestrator confirming the transcription against
`142-VERIFICATION.md`.** ⚠️ Note the standing hazard this row exists for: the same ledger was
once reported closed on exactly this kind of shape-check.

## Verification Performed

| Check | Command | Result |
|-------|---------|--------|
| Snapshot gate (the plan's hard gate) | `npx tsx scripts/dump-sql-functions.ts --check` | **exit 0** — current (104 functions) |
| Task 2 automated verify | plan's 4-part `grep` conjunction | **PASS** |
| Task 3 automated verify | plan's `awk`/`grep` conjunction | **PASS** |
| Regression guard (no code touched) | `cd analytics-service && python3 -m pytest tests/test_main_worker.py -q -x` | **55 passed** in 2.66 s |
| D-08 not implemented | `grep -rn 'len(TIMEOUT_PER_KIND)' …/test_main_worker.py` | unchanged `== 15` at `:1295` |
| Ledger completeness (direct re-count, not via the plan's awk) | `sed -n '131,142p' … \| grep -c 'Observed'` / `grep -c '⬜ pending'` | **11 / 0** |

⚠️ **The plan's Task-3 `awk` range is weaker than it reads.** `awk '/## Falsifiability
Ledger/,/DEPLOYED-RUN/'` terminates at the **SC-1 row**, because SC-1's own Evidence cell
contains the string `‡DEPLOYED-RUN` — so the "zero pending between the ledger heading and
the DEPLOYED-RUN heading" check actually inspects one data row. It passes, but it is close
to vacuous. The 11-Observed / 0-pending figure above comes from a direct line-range count
of the table instead. Recorded rather than quietly relied upon.

## Deviations from Plan

### Auto-fixed / judgment calls

**1. [Rule 3 - Blocking] `DEF-142.1-08` already existed; strengthened in place instead of appended**
- **Found during:** Task 2
- **Issue:** The plan says "append a deferred item `DEF-142.1-08` to TODOS.md". An entry
  with that exact ID already existed at `TODOS.md:670`, added at plan time by the
  `gsd-plan-checker` round. It carried four of the five required elements (the item, the
  OQ-1 zero-hit-grep evidence, the Oracle-Independence conflict, the two bad remedies) but
  phrased the hard rule as "Do not close this by bumping 15 → 16" — which does **not**
  satisfy the acceptance criterion's required `never close this by bumping` string.
- **Fix:** Edited the existing entry's hard rule to
  "⛔ **Hard rule: never close this by bumping the literal to 16.**" and appended the
  execution-time confirmation that D-08 is untouched. **No duplicate entry created** —
  TODOS.md is the project's declared single ground truth for the backlog, and two
  `DEF-142.1-08` blocks would be a worse defect than the phrasing gap.
- **Files modified:** `TODOS.md`
- **Commit:** `b2fcb6db`

**2. [Rule 2 - Missing critical] Wave 0 Requirements checkboxes checked alongside `wave_0_complete: true`**
- **Found during:** Task 3
- **Issue:** The plan directs `wave_0_complete: true` in the frontmatter, but the file's
  own **Wave 0 Requirements** list sat entirely unchecked. Flipping the frontmatter over an
  all-unchecked list is a smaller instance of the exact "declared complete over a pending
  status table" conflation D-14 exists to repair.
- **Fix:** Verified all five Wave 0 artifacts present on disk
  (`test_strategy_analytics_stuck_computing_reaper.sql`, `test_computing_started_at_stamp.py`,
  `test_job07_reaper_off_worker_loop.py`, the reaper invariants in `test_main_worker.py`,
  and `JOB_CHAIN_FOLLOW_ON` in `services/job_worker.py`), checked the boxes, and added a
  note stating that **presence is all that is claimed** — end-to-end execution of the SQL
  gate remains owed as D-16.
- **Files modified:** `142-VALIDATION.md`
- **Commit:** `f46cbef4`

**3. [Rule 1 - Bug in my own edit] Provenance prose tripped the Task-3 verify**
- **Found during:** Task 3 verification
- **Issue:** My first provenance note quoted the literal string `⬜ pending` while
  describing the prior state, which made the "zero pending remain" check fail on my own
  documentation.
- **Fix:** Reworded to "sat un-run in the pending state with an empty Evidence cell" —
  same meaning, no literal marker. Verify then passed.
- **Files modified:** `142-VALIDATION.md`
- **Commit:** `f46cbef4`

**4. [Rule 3 - Blocking] Worktree base correction at startup**
- **Issue:** The worktree spawned on the merge commit `34127316` (default-branch fork,
  the documented `#2015` class), 3 commits behind the plan's `EXPECTED_BASE`.
- **Fix:** `git reset --hard c4b5d6d8` per the `<worktree_branch_check>` block, after the
  HEAD/namespace assertions passed (`worktree-agent-afd00dbbb81b8841c`, not a protected ref).
- **Commit:** n/a (pre-execution)

### Sign-off box deliberately left FALSE

`142-VALIDATION.md`'s **"SQL gate MCP-applied to TEST before it is trusted as evidence"**
is the one sign-off box still unchecked, with an inline ⛔ explaining why: the 637-line gate
file has never been run end-to-end against TEST (Phase 142.1 **D-16**), and SC-1/1b/2/2b
were closed by an orchestrator MCP run of the **extracted** cron body and re-based bridge —
not by executing the gate file. Checking it would be the over-claim this ledger already made
once. This is not an omission; it is the finding.

## Known Stubs

None. This plan produced no code, no UI, and no data path — one tool-generated SQL
snapshot and four documentation edits.

## Threat Flags

None. T-142.1-07 (a stale snapshot letting a future author silently revert JOB-01 stamp
semantics) is the threat this plan **mitigates**; the blocking gate
`sql-function-snapshot.yml:84` stays armed. ⚠️ **T-142.1-07 re-arms in plan 142.1-05**:
adding the trigger function means `npm run schema:functions` must run a **second** time
after that migration lands, or the same drift returns under a new body. No new security
surface (no endpoints, no auth paths, no file access, no schema changes) was introduced.

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 (D-12 run 1) | `fea74933` | `fix(142.1): regenerate stale sync_strategy_analytics_status snapshot (D-12 run 1)` |
| 2 (D-13 + DEF-142.1-08 + OQs) | `b2fcb6db` | `docs(142.1): correct JOB-03 derivation text (D-13); defer D-08 as DEF-142.1-08; mark research OQs resolved` |
| 3 (D-14) | `f46cbef4` | `docs(142.1): backfill 142 falsifiability ledger from Pass C, correct SC-5b/SC-5c coordinates (D-14)` |

## For the Next Plan

- **D-12 is only HALF done.** Run `npm run schema:functions` **again** after the D-17
  trigger migration lands (plan 142.1-05) — trigger functions are tracked in the 104-file
  snapshot set, so the gate re-reddens otherwise.
- **D-16 is unchanged and still owed.** The `142-VALIDATION.md` sign-off records it as the
  single false box; do not let the file's `status: executed` be read as covering it.
- **The Per-Task Verification Map was deliberately not backfilled.** Its pending rows carry
  an explicit inline disclaimer. If a later plan wants them closed, that is new scope.

## Self-Check: PASSED

All 5 files named in `key-files` confirmed present on disk. All 4 commit hashes
(`fea74933`, `b2fcb6db`, `f46cbef4`, `b0bb6749`) confirmed resolvable via
`git log --oneline --all`. Working tree clean — nothing left uncommitted.
