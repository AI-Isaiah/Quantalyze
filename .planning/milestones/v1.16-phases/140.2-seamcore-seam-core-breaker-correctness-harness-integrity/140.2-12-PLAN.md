---
phase: 140.2-seamcore-seam-core-breaker-correctness-harness-integrity
plan: 12
type: execute
wave: 12
depends_on: ["140.2-11"]
files_modified:
  - .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md
  - .planning/ROADMAP.md
  - .planning/v1.16-REPAIR-PROGRAMME.md
autonomous: true
requirements: [SEAMCORE-01, SEAMCORE-02, SEAMCORE-03, SEAMCORE-04, SEAMCORE-05, SEAMCORE-06, SEAMCORE-07, SEAMCORE-08, SEAMCORE-09, SEAMCORE-10, SEAMCORE-11]

must_haves:
  truths:
    - "Every row of the Falsifiability Ledger has been executed and OBSERVED RED first-hand at the phase's final tree, not merely at the tree that existed when its own plan ran"
    - "Every ROADMAP success criterion for this phase is either satisfied or explicitly declared unsatisfied with a reason — including SC4, which is NOT SATISFIED if the SRH spike returned FAIL"
    - "The obligations ledger's counts match its own rows, and the rows re-homed to 140.3 are marked RE-HOMED, never SATISFIED"
    - "TS-10 is re-homed to Phase 141 rather than silently dropped or silently executed"
    - "The working tree is clean and contains no surviving mutants"
  artifacts:
    - path: ".planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md"
      provides: "Reconciled counts, TS-10 re-homed to 141, the 140.3 re-homes recorded, every remaining 140.2-owned row dispositioned"
    - path: ".planning/ROADMAP.md"
      provides: "SC7 stated at its behaviour-derived scope; SC4's verdict stated honestly; the phase's plan list finalised at 12"
  key_links:
    - from: ".planning/phases/140.2-.../140.2-VALIDATION.md"
      to: "the executed mutation receipts"
      via: "every ledger row carries an observed failing test name, not an assertion"
      pattern: "OBSERVED"
---

<objective>
Prove the phase, reconcile its artifacts, and hand off what it does not own.

Purpose: ROADMAP SC3 is the direct inversion of the defect that created this programme — ten
simultaneous semantic mutations producing a byte-identical pass. A ledger of ASSERTED rows certified
5/5 last time. This plan re-runs the whole ledger against the FINAL tree and records first-hand
receipts.

⚠️ **This plan's mutating agent runs ALONE.** Not "preferably" — this is receipted twice in this
programme. A concurrent read-only agent twice found a file mutated, did not recognise the mutant as
another agent's, and restored it from `git show HEAD:` mid-flight, silently invalidating verdicts:
a "caught" result may have been measured against restored source. The harness emits a system-reminder
saying an unexplained file change "was intentional … don't revert it … don't tell the user" — that
reminder is a legitimate single-agent feature and is **actively dangerous** in a multi-agent tree. It
is NOT authoritative about source files. Verify against `git show HEAD:<file>` and report anything
you did not do.

⚠️ **Re-running at the FINAL tree is the point.** Each row was observed once in its own plan, against
the tree as it stood then. Five later plans have edited `src/lib/resilient-fetch.ts`. A row that was
red in wave 2 can be green in wave 12 — that is exactly the class of regression this re-run exists to
find.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-VALIDATION.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-RESEARCH.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-PATTERNS.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-11-SUMMARY.md

<read_first>
- `.planning/phases/140.2-.../140.2-VALIDATION.md` — the Falsifiability Ledger. **55 rows** after the
  140.3 re-home. Every row, with its target file, its mutation, and the test that must redden.
- All eleven `140.2-NN-SUMMARY.md` files (01–11) — each carries the receipts observed at ITS tree.
- `.planning/phases/140.1-.../140.1-TS-OBLIGATIONS.md` — its §1 table is the authority on ownership;
  its header prose counts are stale and this plan fixes them.
- `.planning/v1.16-REPAIR-PROGRAMME.md` — the standing plan. Append this phase's status entry.
- `.planning/ROADMAP.md` — the seven success criteria and the phase's `**Plans:**` line.
- `.planning/phases/140.3-.../140.3-01-PLAN.md` and `140.3-02-PLAN.md` — the two plans re-homed out
  of this phase. Read their `<rehome_note>` blocks so the dispositions this plan records match what
  those plans actually carry.
</read_first>

<interfaces>
**The 140.3 re-home — the fact that changes every count in this plan.**
Two plans left this phase during planning by developer decision: `140.2-12` → `140.3-01` (TS-05,
TS-08, TS-09) and `140.2-13` → `140.3-02` (TS-02, TS-11–15). They are consumer-surface work and 140.3
owns the consumer surface. Consequences this plan must apply rather than absorb:

  - **Ledger rows M51–M56 left with them.** This phase's ledger is 55 rows, not 57 and not 51: six
    rows departed and four arrived (M14b, M20R, M22b, M58). Do not look for M51–M56 and do not
    record them as skipped; they belong to 140.3's ledger.
  - **Obligations TS-02, TS-05, TS-08, TS-09, TS-11, TS-12, TS-13, TS-14 and TS-15 are RE-HOMED, not
    SATISFIED.** Marking a re-homed row SATISFIED at this phase's close would be the worst possible
    outcome of the move: an obligation with a tick and no work behind it.
  - **TS-32's `BLOCKED-BY: TS-05` is NOT cleared by this phase.** The old plan-14 text claimed it was
    "CLEARED by plan 140.2-12"; that plan is now `140.3-01` and has not run. Re-disposition TS-32 as
    still BLOCKED, with the unblocking work named as `140.3-01`.

**Obligation-count reconciliation — the row-by-row truth, to replace two contradicting prose counts.**
CONTEXT says "31 obligations, 16 addressed to this phase". The ledger header says "35 … 140.2 → 14".
Counting rows whose Owner column names 140.2, then applying the re-home:

  Sole-owned by 140.2 and EXECUTED here — 3: TS-04, TS-06, TS-16
  Sole-owned by 140.2 but RE-HOMED to 140.3 — 8: TS-02, TS-05, TS-08, TS-09, TS-11, TS-12, TS-13, TS-14
    (plus TS-15, shared with the redaction leaf's ordering constraint — 9 rows in total leave)
  Sole-owned by 140.2, RE-HOMED to Phase 141 — 1: TS-10
  Shared — 4: TS-23 (140.2/146) · TS-26 (140.2/ops) · TS-32 (was 140.2/140.3, now 140.3) ·
              TS-33 (140.2-or-140.3, unassigned)
  Already DONE in 140.1.1 — 2: TS-01, TS-03

Use the file's own §1 table, not either prose count, and recount the header in the same edit — the
convention 140.1.1 and 140.1.2 both followed. **Re-derive these numbers from the rows yourself
before writing them.** This block is provenance, not an authority; a count copied without recounting
is how the two stale prose counts got there in the first place.

**TS-10 — re-home to Phase 141. It is a PYTHON edit owned to a TypeScript-only phase.** The row asks
for `424` to be added to `job_worker.py`'s transient set. Its stated justification is "140.2 owns the
retry semantics" — but retry is **Phase 141** (SEAM-05/06), and this phase's own CONTEXT fences it to
TypeScript and forbids enabling retry. Executing it here would smuggle a Python change into a
TypeScript-only phase; dropping it silently would lose a real obligation. Re-home it, in the ledger
itself, per that file's rule.

**Dispositions to record for the remaining shared rows:**
  TS-23 — 140.2's half is DONE (the discriminator tolerates all three 429 wire shapes, pinned by a
          case per shape, in plan 140.2-06). The Python migration and the decision of which shape
          WINS stay with 146. Note that 140.1.2 added `Retry-After` to all four bare-scalar 429s
          header-only, so the migration must PRESERVE it rather than re-derive it.
  TS-26 — ops. The credential-carrying probe for a stale `ANALYTICS_SERVICE_KEY` belongs on the
          `warm-analytics` warmer and must NEVER extend `/health` (A-12 / O-7): the breaker would
          block its own recovery probe, and `/health` deliberately stays HTTP 200 when config is
          degraded so the signal cannot become the outage via a restart loop. Hand off; do not build.
  TS-32 — **still BLOCKED.** Its `BLOCKED-BY: TS-05` clears when `140.3-01` lands, not here. The
          `400 → 424` remap is Python and stays with its owning phase.
  TS-33 — send `wizard_session_id` in the finalize-wizard payload: ONE field, not a new idempotency
          subsystem. Its ordering constraint (strictly after TS-01) is satisfied. This phase was
          required to DECIDE its owner, not necessarily to execute it. Decide, record, and say which.

**Success criteria to adjudicate, one by one, against evidence rather than intent.** Any criterion
not satisfiable as written must be declared so with a reason — a phase that reports 7/7 by softening
a criterion is the failure this programme exists to end.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run the ENTIRE Falsifiability Ledger against the final tree — mutating agent ALONE</name>
  <files>.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-VALIDATION.md</files>
  <action>
Confirm no other agent is active on this working tree before starting, and state that confirmation in
the SUMMARY.

Establish a clean baseline: `git status` clean, `grep -rn MUTANT src/ tools/ tests/` empty, and the
full suite green. Record the baseline test count — a byte-identical count after a mutation is the
signature failure this ledger inverts, so the number matters.

**First, read plan 140.2-01's SUMMARY and record the SRH verdict.** It gates a subset of this ledger:
  - **PASS (either flavour)** — the real-Redis lane exists. Rows M18, M19, M20R and the real-Redis
    halves of M14, M15 and M16 are runnable and MUST be run.
  - **FAIL** — `tests/redis/` and `vitest.redis.config.ts` do not exist. Those six rows are
    **NOT-OBSERVABLE**, and that is what you record: not "skipped", not "asserted", not "satisfied by
    the fake". A fake that harvests its threshold from production cannot substitute for real Lua;
    that substitution is the exact defect this phase exists to kill. Record NOT-OBSERVABLE with the
    SRH verdict as the reason, and carry it into Task 2's SC4 adjudication as **NOT SATISFIED**.

Then walk EVERY runnable row of the Falsifiability Ledger in `140.2-VALIDATION.md`, one at a time:
  1. apply the mutation to PRODUCTION source (never to a test file — a mutation that edits a test
     proves nothing about the source);
  2. run the row's named command;
  3. record the OBSERVED failing test name and assertion text verbatim;
  4. revert;
  5. re-confirm `git status` clean and `grep -rn MUTANT` empty BEFORE the next row.
Do not batch. Batching is how a restored mutant goes unnoticed.

**M14b deserves its own attention.** It is the row that exists because the phase's headline defect
was a test DOUBLE that harvested its threshold from production's own constructor argument. Its
mutation (`BREAKER_FAILURE_THRESHOLD` 5 → 30) must redden a BEHAVIOURAL breaker test — a trip-count
case driven through the fake — and not merely the literal pin file. If only the pin file reddens,
plan 140.2-02's fix did not take at all four `fakeRatelimitFor` sites, and that is the single most
important finding this plan can produce. Check all four sites by code text before concluding.

Any row that comes back GREEN is a defect in the oracle, not in the ledger. Fix the oracle, re-run
the row, and record both the finding and the fix. Do NOT delete or soften a row to make the ledger
close — that is the exact move that produced "5/5 mutation-tested" against a suite that could not
fail.

Where a row's mutation is now unreachable because a later plan restructured the code, say so
explicitly, name the replacement mutation that targets the same property, and run THAT. An
unreachable mutation silently skipped is an asserted row wearing a different hat. M20R is already
in the ledger as exactly such a replacement (M20's counter-identity mutation after per-dependency
keying); if a second one is needed, add it the same way.
  </action>
  <verify>
    <automated>git status --porcelain | wc -l && grep -rn MUTANT src/ tools/ tests/ | wc -l</automated>
  </verify>
  <done>
Every runnable ledger row carries an observed failing test name and assertion text from THIS tree.
Any row made NOT-OBSERVABLE by an SRH FAIL is recorded as such with the verdict as its reason. Any
green row is recorded with its oracle fix. Final state: `git status` clean apart from the intended
plan diff, `grep -rn MUTANT` empty across `src/`, `tools/` and `tests/`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run the phase gates and adjudicate the seven success criteria against evidence</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Run and record, first-hand, each gate:
  - `npm run test:coverage` — must clear lines 82 / statements 80 / functions 74 / branches 72 on the
    FULL suite. New source in `src/lib/**` without tests trips the ratchet; if it trips, add tests
    rather than lowering a threshold.
  - `npx tsc --noEmit` — exit 0.
  - `npm run lint` — 0 errors.
  - the real-Redis lane via its own config — green, with the anti-vacuity executed-case count matching
    its literal. **If plan 140.2-01 recorded FAIL, this gate is NOT-RUN and the reason is the SRH
    verdict.** Record NOT-RUN; do not substitute the main suite for it and do not report the gate as
    passing because nothing failed.
  - `git diff --name-only <phase-base>..HEAD | grep "\.py$"` — must be EMPTY. This phase is
    TypeScript-only by its own CONTEXT; a Python file in the diff is a fence breach.
  - `git diff <phase-base>..HEAD` — must show ZERO new `@ts-ignore` / `@ts-expect-error` suppressions.

Known local flakes, to be named rather than smoothed: `AllocationsTabs.scenario-state-preservation`
and `MultiKeyConnectStep` fail under local file-parallelism and pass with `--no-file-parallelism`.
Neither is in this phase's diff. If the count contradicts an executor's claimed number, LOG the
contradiction rather than smoothing it — 140.1.1's verifier did exactly that and it was the right
call.

Then adjudicate each of the seven ROADMAP success criteria against evidence. For each: state
SATISFIED with the test or receipt that proves it, or NOT SATISFIED with the reason. Three need
honest statements and one of them may be a genuine miss:

  - **SC4 is CONDITIONAL on the SRH spike.** If plan 140.2-01 returned PASS, SC4 is SATISFIED with
    the lane's executed-case receipt as evidence. **If it returned FAIL, SC4 is NOT SATISFIED, and
    the SRH verdict is its reason.** That is a legitimate, valuable outcome: the criterion demands
    verification against real Redis, and a lane that does not exist has not verified anything. Do not
    downgrade the criterion to "verified against an improved fake", do not claim partial
    satisfaction, and do not let plans 06 and 07 having proceeded without the lane read as SC4 being
    met. Record the pre-costed `ioredis` fallback and its Package Legitimacy Gate cost as the named
    route to satisfying it in a follow-up.
  - **SC7** was factually wrong as written and plan 140.2-09 corrected it. Confirm the corrected
    wording is in `ROADMAP.md` and that the delivered scope is the honest one (five live routes), not
    the zero-signature-change shortcut its literal wording would have permitted.
  - **SC6's warmer clause** had NO existing guard to extend — it was additive work, not a tightening.
    State that, so the phase is not credited with tightening something that did not exist.
Also state that **SEAMCORE-06's "every Sentry capture" clause was ADDITIVE** — the seam captured
nothing to Sentry before this phase — rather than implying a leak was plugged.

Finalise the phase's `**Plans:**` line and plan list in `ROADMAP.md` at **12 plans / 12 waves**, and
record in the same edit that two plans were re-homed to 140.3 during planning, so a reader does not
read the drop from 14 to 12 as scope that was cut.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run lint && npm run test:coverage</automated>
  </verify>
  <done>
All six gates run first-hand with their outputs recorded, including any NOT-RUN with its reason.
Seven per-criterion verdicts, each backed by a named receipt or an explicit reason, with SC4's
verdict traceable to the SRH spike. Any contradiction between a recorded claim and an observed
number is logged, not smoothed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the obligations ledger and hand off what this phase does not own</name>
  <files>.planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md, .planning/v1.16-REPAIR-PROGRAMME.md</files>
  <action>
Edit `140.1-TS-OBLIGATIONS.md` with TARGETED edits only. **Never rewrite this file wholesale and
never pipe it through `head`/`sed`** — it is a gitignored planning ledger with no git backup, and a
BSD-`sed` rewrite has already truncated one ledger in this repo to four lines. Verify the line count
is at or above its pre-edit value when you are done.

Apply:
  1. **Record the 140.3 re-home first**, because it changes every count that follows. Mark TS-02,
     TS-05, TS-08, TS-09, TS-11, TS-12, TS-13, TS-14 and TS-15 as **RE-HOMED → 140.3** with the
     owning plan named (`140.3-01` for TS-05/08/09; `140.3-02` for the rest). **Never SATISFIED.**
     If any of those rows already reads SATISFIED because a 140.3 plan ran ahead of this one, verify
     that claim against the 140.3 SUMMARY before leaving it — do not take a tick on trust.
  2. **Recount the header.** Replace the stale prose counts with the row-by-row truth you re-derived
     in Task 1's reading pass. Note that CONTEXT's "31 / 16" and the previous header's "35 / 14" were
     both stale, that the §1 table is the authority, and that the re-home moved nine rows out of
     140.2's column.
  3. **Re-home TS-10 to Phase 141**, in place, with the reason stated: it is a Python edit whose
     justification ("140.2 owns the retry semantics") is false — retry is Phase 141, and 140.2 is
     TypeScript-only and forbidden from enabling retry. Record the move rather than dropping it.
  4. **Disposition TS-23, TS-26, TS-32 and TS-33** as set out in `<interfaces>`, including TS-33's
     owner decision, which this phase was required to make, and **TS-32 as still BLOCKED** pending
     `140.3-01`.
  5. **Confirm every row still owned by 140.2 carries a terminal state** — SATISFIED, VERIFIED,
     PARTIALLY SATISFIED with a named owner for the remainder, or RE-HOMED. A row still reading
     "open" at phase close is an obligation with no owner, which is exactly how TS-04 nearly went
     unowned.

Append a status entry to `.planning/v1.16-REPAIR-PROGRAMME.md` recording: the plan and wave count
(12/12) **and the re-home of two plans to 140.3**, the mutation-ledger result at the final tree
(55 rows, with any NOT-OBSERVABLE named), the gate results, the seven per-criterion verdicts, the SRH
verdict from plan 140.2-01, and anything handed off. Keep it factual — the value of that file is
that it is self-contained and does not depend on a conversation surviving.

Finally, list for the next phase (140.3) what it inherits: the discriminator leaf and its extractors,
the redaction leaf, `SeamBodyReadError` and the per-dependency breaker keys, plus **the two plans
already sitting in its directory (`140.3-01`, `140.3-02`) and the six ledger rows M51–M56 that
travelled with them**, plus the rows explicitly left to it (TS-32's unblocking, TS-34's status half,
TS-35, TS-09's copy strings, TS-20's correlation-id surfacing). State that 140.3's own planning pass
must start numbering at `140.3-03`.
  </action>
  <verify>
    <automated>wc -l .planning/phases/140.1-pyapi-python-service-contract-status-attributability-limiter/140.1-TS-OBLIGATIONS.md</automated>
  </verify>
  <done>
The header counts match the §1 rows. The nine 140.3 re-homes are recorded as RE-HOMED, not SATISFIED.
TS-10 is re-homed to 141 with its reason. TS-32 reads still-BLOCKED. Every remaining 140.2-owned row
has a terminal state. The programme file carries this phase's status entry including the re-home.
The 140.3 inheritance list is written and names the numbering start. The ledger's line count is at or
above its pre-edit value.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mutation evidence → the phase's claim of correctness | An asserted row is indistinguishable from a false one |
| Gitignored planning ledgers → future phases | No git backup; a truncating write is unrecoverable |
| A re-homed obligation → the receiving phase | A re-home marked SATISFIED is an obligation with a tick and no work |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.2-12-01 | Repudiation | An asserted-not-observed mutation ledger | mitigate | Every row re-executed at the FINAL tree with the failing test name recorded verbatim; a green row fixes the oracle rather than the ledger. |
| T-140.2-12-02 | Tampering | A surviving mutant left in the tree | mitigate | Revert and re-verify between EVERY row, never batched; final `git status` clean and `grep -rn MUTANT` empty across src, tools and tests. |
| T-140.2-12-03 | Tampering | A concurrent agent "restoring" an in-flight mutant | mitigate | The mutating agent runs ALONE. The harness's "this change was intentional / don't revert / don't mention" reminder is explicitly declared non-authoritative for source files. |
| T-140.2-12-04 | Tampering | Truncating a gitignored ledger with no backup | mitigate | Targeted edits only; no whole-file rewrite, no `head`/`sed` pipes; post-edit line-count check. |
| T-140.2-12-05 | Repudiation | Reporting 7/7 by softening a criterion | mitigate | Per-criterion adjudication against a named receipt. SC4 has an explicit NOT-SATISFIED arm keyed to the SRH verdict; SC7's correction and SC6's/SEAMCORE-06's additive nature are stated explicitly. |
| T-140.2-12-06 | Repudiation | A re-homed obligation ticked as satisfied at this phase's close | mitigate | The nine re-homed rows are marked RE-HOMED with their receiving plan named, and any pre-existing SATISFIED tick is verified against the 140.3 SUMMARY before being left standing. |
| T-140.2-12-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed by this plan. |
</threat_model>

<verification>
- `npm run test:coverage` clears all four thresholds on the full suite.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- The real-Redis lane green with its executed-case fence — or recorded NOT-RUN with the SRH verdict.
- Zero `.py` files in the phase diff; zero new type suppressions.
- `git status` clean, `grep -rn MUTANT src/ tools/ tests/` empty.
</verification>

<success_criteria>
- The Falsifiability Ledger is closed with first-hand receipts from the final tree, or with an
  explicit NOT-OBSERVABLE and its reason.
- All seven ROADMAP criteria carry an evidence-backed verdict, including the honest statements about
  SC4, SC7 and SC6.
- Every obligation still owned by 140.2 has a terminal state; every re-homed one says RE-HOMED and
  names its receiving plan; TS-10 is re-homed to 141 rather than smuggled or dropped.
</success_criteria>

<output>
Create `.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-12-SUMMARY.md` when done.
Record: the full 55-row ledger with observed failing tests (and any NOT-OBSERVABLE with its reason),
the six gate outputs, the seven verdicts, the reconciled obligation counts, the nine re-homed rows,
and the 140.3 inheritance list.
</output>
</content>
