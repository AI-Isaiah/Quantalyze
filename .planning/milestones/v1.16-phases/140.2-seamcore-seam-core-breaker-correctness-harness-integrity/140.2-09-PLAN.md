---
phase: 140.2-seamcore-seam-core-breaker-correctness-harness-integrity
plan: 09
type: execute
wave: 9
depends_on: ["140.2-08"]
files_modified:
  - src/lib/tenant-claim.ts
  - src/lib/tenant-claim.test.ts
  - src/lib/process-key-client.ts
  - src/lib/analytics-client.ts
  - src/lib/analytics-client.test.ts
  - src/app/api/strategies/create-with-key/route.ts
  - src/app/api/strategies/composite/add-key/route.ts
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/scenario/optimize/route.ts
  - tests/fixtures/tenant-claim-parity.json
  - src/__tests__/contracts/contracts-registry.test.ts
  - src/__tests__/contracts/REGISTRY.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [TS-04]

must_haves:
  truths:
    - "analytics-client mints an X-Tenant-Claim on every request that has a server-derived user identity"
    - "Two different users produce two different claims, each verifying to its own payload under an independently computed HMAC"
    - "A missing INTERNAL_API_TOKEN fails LOUD rather than silently leaving every route on platform:<path>"
    - "There is exactly ONE HMAC mint implementation in the repo"
    - "ROADMAP SC7's 'all nine' claim is corrected to the behaviour-derived truth"
  artifacts:
    - path: "src/lib/tenant-claim.ts"
      provides: "The single mint implementation, extracted from process-key-client, with the TTL pinned"
      min_lines: 40
    - path: "src/lib/tenant-claim.test.ts"
      provides: "Behavioural proof: two user ids, two claims, each verified by an HMAC computed in the test"
      min_lines: 70
    - path: "tests/fixtures/tenant-claim-parity.json"
      provides: "Committed case table for the cross-language link, so the Python reader binds to bytes rather than a copy-pasted literal"
  key_links:
    - from: "src/lib/analytics-client.ts"
      to: "src/lib/tenant-claim.ts"
      via: "header assembly mints the claim from a threaded server-derived user id"
      pattern: "X-Tenant-Claim"
    - from: "src/lib/process-key-client.ts"
      to: "src/lib/tenant-claim.ts"
      via: "the previously private mint now imported from the shared module"
      pattern: "tenant-claim"
---

<objective>
Mint `X-Tenant-Claim` from `analytics-client.ts`, flipping the reachable rekeyed Python routes from
a platform bucket to genuine per-tenant throttling — with zero Python changes.

Purpose: ROADMAP success criterion 7, inherited obligation `TS-04`. Phase 140.1 completed the Python
half; the key function provably returns `<scope>:t:<user>` the instant a claim appears, and the
cross-language HMAC link was proven end-to-end (a claim minted in Node with the TS algorithm fed to
the Python verifier). Until this lands, PYAPI-02's per-tenant guarantee covers `/process-key` ONLY.

## ⚠️ ROADMAP SC7 IS FACTUALLY WRONG AND THIS PLAN CORRECTS IT

SC7 says minting the header flips "all nine rekeyed Python routes". The behaviour-derived answer,
from a Python `partial(tenant_or_platform_key, scope=…)` sweep cross-referenced against a TypeScript
path grep, is **6 reachable from `analytics-client`, 5 live, 1 dead, 3 unreachable by construction**:

| Python route | scope | Reached by `analytics-client`? | Effect |
|---|---|---|---|
| POST /api/validate-key | validate_key | yes — `validateKey` | **flips** |
| POST /api/encrypt-key | encrypt_key | yes — `encryptKey` | **flips** |
| POST /api/optimize-weights | optimize_weights | yes — `optimizeScenarioWeights` | **flips** (the sharpest — 20/minute) |
| POST /api/portfolio-optimizer | portfolio_optimizer | yes — `runPortfolioOptimizer` | **flips** |
| POST /api/portfolio-bridge | portfolio_bridge | yes — `findReplacementCandidates` | **flips** |
| POST /api/portfolio-analytics | portfolio_analytics | wrapper exists, **ZERO production callers** | flips a dead path |
| POST /api/fetch-trades | fetch_trades | no — only an allowlisted debug SSE raw fetch | no effect |
| POST /api/csv/validate | csv_validate | no — the TS route re-targets to `/process-key` | no effect |
| POST /api/verify-strategy | verify_strategy | no — the TS route calls `postProcessKey` | no effect |

**SC7's own test clause — "prove tenant A cannot consume tenant B's allowance on at least one of the
nine" — is satisfiable with ZERO signature changes**, via `runPortfolioOptimizer` or
`findReplacementCandidates`, both of which already receive an actor id. **Do not take that cheap
pass.** Taking it would satisfy the letter of SC7 while leaving the two busiest seam endpoints
(key-connect spends TWO tokens per attempt) and the sharpest limiter (20/minute) on a platform
bucket. This plan delivers the honest scope: all six reachable wrappers mint the claim, which means
threading a server-derived user id into the three that do not have one.

**Also correcting the ledger's understatement:** TS-04's row describes this as
"`src/lib/analytics-client.ts` (headers assembly)", which reads as a one-liner. It is not.
`analyticsRequest`'s options are budget key, timeout, method and correlation id — there is **no user
id anywhere in that module** — and the MAC key is `INTERNAL_API_TOKEN`, which
`analytics-client.ts` does not read at all today (it reads `ANALYTICS_SERVICE_KEY`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-RESEARCH.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-PATTERNS.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-VALIDATION.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-08-SUMMARY.md

<read_first>
- `src/lib/process-key-client.ts` — the existing mint. Locate by the names `TENANT_CLAIM_TTL_SECONDS`
  and `mintTenantClaim`, and the header assembly containing `"X-Tenant-Claim": mintTenantClaim(...)`.
  Read its "WHY NO NEW SECRET" comment block before moving anything.
- `analytics-service/services/rate_limit.py` — READ ONLY, no edits. `verify_tenant_claim` and
  `tenant_or_platform_key`. Note the parse is `rsplit(".", 2)`, the claim is capped at 512
  characters, an expired `exp` is rejected, and comparison is `hmac.compare_digest`. Note also that
  `X-User-Id` is unsigned client input and is deliberately NOT used for bucketing — that is why the
  signed claim exists at all.
- `src/lib/analytics-client.ts` — the header block under
  `res = await resilientFetch(options.budgetKey, path, {`. `SERVICE_KEY` is captured at MODULE
  SCOPE from `ANALYTICS_SERVICE_KEY`, which is the env-before-import ordering hazard
  `resilient-fetch.wiring.test.ts` documents in its header. Do not repeat that pattern for the new
  secret: read `INTERNAL_API_TOKEN` at call time.
- `tests/lib/process-key-onboard-contract-parity.test.ts` — the committed-shared-fixture parity
  pattern, including its hard rule of zero `vi.mock` and its hand-typed roster comment.
</read_first>

<predecessor_delta>
**Four earlier plans edited the two client files this plan touches. Read them as they are ON DISK.**

| File | Changed by | What moved that matters here |
|---|---|---|
| `src/lib/analytics-client.ts` | 140.2-05, 140.2-08 | The success arm gained a `SeamBodyReadError` catch (05); the "Contract validation failed" log site now routes through `src/lib/seam-redaction.ts` (08). **The header assembly block this plan edits was NOT touched by either** — confirm that by reading before adding the mint. The `SERVICE_KEY` module-scope capture is still there and is still the hazard the plan warns about. |
| `src/lib/process-key-client.ts` | 140.2-05, 140.2-08 | The `try` covers the body reads and the catch has a `SeamBodyReadError` arm (05); the undici log site routes through the redaction leaf and is passed per-request secrets explicitly (08). `mintTenantClaim` and `TENANT_CLAIM_TTL_SECONDS` are untouched — they are this plan's to extract. |
| `src/lib/analytics-client.test.ts` | 140.2-05 | Cases for the success-arm mapping. EXTEND; delete nothing (TRAP-9). |
| `src/app/api/keys/validate-and-encrypt/route.ts` | 140.2-05, 140.2-08 | Dormant-handler body reads instrumented; its `console.error` scrubbed with per-request exchange secrets passed explicitly. This plan threads `user.id` into two `analytics-client` calls in the same file — do not disturb the secret-passing arguments 08 added. |

⚠️ **One interaction worth stating.** Plan 140.2-08's redaction leaf reads its env-secret VALUES at
call time from a hand-typed NAME list that already includes `INTERNAL_API_TOKEN`. This plan makes
`analytics-client.ts` read that same secret. The claim itself is a derived MAC, not the secret, so it
is not automatically redacted — and it does not need to be (it is a short-lived public credential,
300 s). Do not add the claim to the secret list; do add a note in the SUMMARY that this was
considered and rejected with the reason, so a later reviewer does not read the omission as an
oversight.
</predecessor_delta>

<interfaces>
**The mint, verbatim from `process-key-client.ts` — copy the ALGORITHM, do not re-derive it:**
  TTL is 300 seconds. `exp = floor(Date.now()/1000) + TTL`. `signed = "<payload>.<exp>"`.
  `mac = createHmac("sha256", secret).update(signed).digest("hex")`. Header value is
  `"<signed>.<mac>"`. The secret is `INTERNAL_API_TOKEN` — **not** `ANALYTICS_SERVICE_KEY`.

**The six wrappers that must mint, and where their identity comes from:**
  `validateKey`               — NO user-id parameter today. Callers: `strategies/create-with-key`,
                                `strategies/composite/add-key`, `keys/validate-and-encrypt` — all
                                three are `withAuth(async (req, user) => …)` and have `user.id`.
  `encryptKey`                — same three callers, same gap.
  `optimizeScenarioWeights`   — NO parameter today. Caller: `scenario/optimize`, which has `user.id`.
  `runPortfolioOptimizer`     — already receives an actor id.
  `findReplacementCandidates` — already receives a user id.
  `computePortfolioAnalytics` — already receives an actor id. ZERO production callers; thread it
                                anyway. Leaving one member of a class is the instance-not-class
                                defect this programme exists to close.

**Seven route call sites** for the three wrappers that need threading: two in
`strategies/create-with-key`, two in `strategies/composite/add-key`, two in
`keys/validate-and-encrypt`, one in `scenario/optimize`. All seven already have `user.id` in scope.

**Two wrappers where the claim is inert but harmless and forward-compatible:** `recomputeMatch` and
`evalMatch` reach Python routes with NO limiter at all (TS-21, owned by 146). `simulateAddCandidate`
reaches the TENTH, still-IP-keyed route (TS-30, quarantined). Sending a claim there is harmless; say
so rather than special-casing.

**Hostile-input constraint:** `tenant_or_platform_key` returns `<scope>:t:<payload>` with the payload
VERBATIM. The payload must be a server-derived `user.id`, never anything a caller supplied, and it
must contain no `.` (the Python parse is dot-tolerant by design, but a UUID has no dot and should
stay that way). An empty or undefined payload must be REFUSED, not minted — a claim over an empty
payload would silently bucket every caller together, which is worse than no claim.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract the mint into one shared module</name>
  <files>src/lib/tenant-claim.ts, src/lib/tenant-claim.test.ts, src/lib/process-key-client.ts</files>
  <behavior>
    - `process-key-client` produces byte-identical claims before and after the extraction, for a
      fixed payload, secret and clock.
    - The TTL is pinned to a hand-typed literal.
    - An empty or whitespace-only payload is REFUSED with a named error, never minted.
    - A payload containing a `.` is refused with a named error, so a future non-UUID identifier
      cannot silently create an ambiguous parse on the Python side.
    - An absent or empty secret fails LOUD.
  </behavior>
  <action>
Create `src/lib/tenant-claim.ts` holding the single mint implementation, moved verbatim from
`process-key-client.ts` — algorithm unchanged, so the wire bytes cannot move. Re-point
`process-key-client.ts` at it and delete its private copy. Two copies of an HMAC construction is the
drift class this programme exists to close.

Add the three refusals above at the boundary. They are fail-loud guards, not validation theatre: an
empty-payload claim would silently collapse every caller into one bucket, and 140.1's mutation #3
receipt is the warning — `default_platform_key` returning an empty string makes slowapi skip
limiting entirely and shipped GREEN against an identity assertion.

Write the test so the expected MAC is computed IN THE TEST with `createHmac`, never imported from
the module under test. Include a fixed-clock case (stub the clock, not the crypto) producing an
exact expected claim string, and the three refusal cases. Zero `vi.mock` of the module under test.

The module imports only `node:crypto`. That is safe here: no `"use client"` file imports
`analytics-client` or `process-key-client` (the only non-route references to `analytics-client` in
`wizardErrors.ts` and `seam-errors.ts` are comments), and `process-key-client.ts` already imports
`node:crypto` at its top. Do NOT put this in one of the dependency-free leaves — it is server-only
by construction and mixing it into a bundle-safe leaf would break that leaf's purity guard.
  </action>
  <verify>
    <automated>npx vitest run src/lib/tenant-claim.test.ts src/lib/process-key-client.test.ts</automated>
  </verify>
  <done>
One mint implementation repo-wide (`grep -rn "createHmac" src/` shows it only in the new module).
`process-key-client`'s claims are byte-identical for a fixed clock. The TTL is pinned to a literal
and all four fail-loud guards are covered.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thread a server-derived user id into the three wrappers and mint in analytics-client</name>
  <files>src/lib/analytics-client.ts, src/app/api/strategies/create-with-key/route.ts, src/app/api/strategies/composite/add-key/route.ts, src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/scenario/optimize/route.ts</files>
  <behavior>
    - All six reachable wrappers cause an `X-Tenant-Claim` header to be emitted.
    - The claim's payload is the route's `user.id`, never a caller-supplied value.
    - When `INTERNAL_API_TOKEN` is absent, the call FAILS LOUD rather than silently omitting the
      header and leaving the route on a platform bucket.
    - `X-Service-Key` and every other existing header is still emitted byte-for-byte — a dropped
      `X-Service-Key` unauthenticates every analytics call and reads as a total outage.
  </behavior>
  <action>
Add a required tenant-identity parameter to `analyticsRequest`'s options and to the three wrappers
that lack one (`validateKey`, `encryptKey`, `optimizeScenarioWeights`), and pass the already-present
identity through on the three that have one. Update all seven route call sites to pass `user.id`.

In the header assembly, mint the claim from the shared module using `INTERNAL_API_TOKEN` read at
CALL time — not captured at module scope. Module-scope capture is the exact ordering hazard that
makes `SERVICE_KEY` silently empty when a test sets env after a static import, and here the failure
mode is worse: an empty secret still produces a syntactically valid claim that fails
`compare_digest`, so every route silently stays on `platform:<path>` with no error anywhere. Make
absence LOUD.

Do not remove or reorder any existing header. Do not add `X-User-Id` anywhere as a bucketing input —
it is unsigned client input and is deliberately ignored by the Python key function; the signed claim
is what selects the bucket.

Note in a comment that the claim is inert on `match-recompute` / `match-eval` (no Python limiter,
TS-21) and on `simulator` (still IP-keyed, TS-30), and that sending it there is harmless and
forward-compatible — so a future reader does not "optimise" it away.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/analytics-client.test.ts src/lib/resilient-fetch.wiring.test.ts</automated>
  </verify>
  <done>
All six reachable wrappers mint. All seven route call sites pass `user.id`. An absent
`INTERNAL_API_TOKEN` fails loud. `npx tsc --noEmit` exits 0 and every pre-existing header assertion
still passes — including the eleven budget-key pins plan 140.2-03 added to
`resilient-fetch.wiring.test.ts`, whose wrapper signatures this task changes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: The behavioural proof, the parity fixture, the ROADMAP correction, and mutations M28, M44, M45, M46</name>
  <files>src/lib/analytics-client.test.ts, tests/fixtures/tenant-claim-parity.json, src/__tests__/contracts/contracts-registry.test.ts, src/__tests__/contracts/REGISTRY.md, .planning/ROADMAP.md</files>
  <behavior>
    - Driving the same wrapper with two DIFFERENT user ids produces two DIFFERENT claims.
    - Each claim verifies to its own payload under an HMAC computed in the test with `createHmac`.
    - The committed fixture's cases round-trip through the shipped mint.
  </behavior>
  <action>
Test SC7 BEHAVIOURALLY, not structurally. `expect(headers["X-Tenant-Claim"]).toBeDefined()` is a
self-referential-adjacent oracle and would have shipped green against 140.1's mutation #3. The
honest test drives two different user ids through a wrapper, asserts the two claims DIFFER, and
independently verifies each one: split on the last two dots, recompute the MAC in the test with
`createHmac`, and compare. Nothing on the expected side may be imported from the module under test.

Write `tests/fixtures/tenant-claim-parity.json` as a committed case table — payload, exp, secret and
expected claim per case — and have this test read it. Bind to the BYTES rather than re-deriving a
literal in each suite: a copy-pasted literal proves only that someone once copied correctly.

⚠️ **The Python-side reader is NOT written here.** This phase is TypeScript-only by its own CONTEXT,
and the cross-language link was already proven end-to-end in 140.1. Record an obligation that
whoever next touches `analytics-service/tests/` — Phase 146 owns the limiter surface — binds a pytest
reader to this same fixture, and add that row to `140.1-TS-OBLIGATIONS.md` under that file's own
"correct it here, do not fork a second list" rule. Say this explicitly in the SUMMARY rather than
implying the parity gate is complete.

**Correct ROADMAP SC7 in `.planning/ROADMAP.md`.** Replace "flipping all nine rekeyed Python routes"
with the behaviour-derived truth: six are reachable from `analytics-client`, five live and one dead;
three (`fetch-trades`, `csv/validate`, `verify-strategy`) are unreachable from that client by
construction and are unaffected. Keep the criterion's test clause as written — it is satisfiable and
this plan satisfies it on five live routes rather than the minimum one. Note in the same edit that
the correction came from a behaviour-derived sweep, so the number is reproducible. **Use a targeted
edit; never rewrite `ROADMAP.md` wholesale** — it is a gitignored planning ledger with no git backup,
and a truncating rewrite has already destroyed one ledger in this repo.

Register `src/lib/tenant-claim.test.ts` in `CONTRACT_GUARDS` and `REGISTRY.md`.

Then OBSERVE these mutations, one at a time, reverting between each, with no other agent active on
the tree:
  M28 — make the claim payload constant across users ⇒ must redden the two-different-ids case.
  M44 — swap the MAC secret to `ANALYTICS_SERVICE_KEY` ⇒ must redden the independent-verification
        case (this is the mistake the ledger row's brevity invites).
  M45 — drop the header from the assembly ⇒ must redden.
  M46 — remove the empty-payload refusal and mint over an empty string ⇒ must redden.
Record each observed failure output. `git status` clean and `grep -rn MUTANT src/` empty between rows.
  </action>
  <verify>
    <automated>npx vitest run src/lib/analytics-client.test.ts src/lib/tenant-claim.test.ts src/__tests__/contracts/contracts-registry.test.ts</automated>
  </verify>
  <done>
The behavioural test passes with the MAC recomputed in-test. The fixture is committed and read.
ROADMAP SC7 states the corrected scope and the file's line count is at or above its pre-edit value.
The Python-reader obligation is recorded in `140.1-TS-OBLIGATIONS.md`. M28, M44, M45 and M46 each
observed RED, then reverted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel route → Python limiter | The bucket identity crosses here and must be unforgeable |
| Caller → tenant payload | A caller-supplied payload would let anyone mint a fresh bucket |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.2-09-01 | Spoofing | Tenant-bucket forgery via an unsigned header | mitigate | The HMAC-SHA256 claim exists precisely because `X-User-Id` is unsigned; `exp` bounds replay to 300 s. No `X-User-Id` is added as a bucketing input. |
| T-140.2-09-02 | Spoofing | A caller-controlled payload reaching `<scope>:t:<payload>` verbatim | mitigate | The payload is the route's server-derived `user.id` only. Empty, whitespace-only and dot-containing payloads are refused at the mint. |
| T-140.2-09-03 | Denial of service | A silently absent `INTERNAL_API_TOKEN` leaving every route on `platform:<path>` | mitigate | Read at call time and fail LOUD on absence. An empty secret still produces a syntactically valid claim that fails `compare_digest`, so silence is the dangerous default. |
| T-140.2-09-04 | Information disclosure | The claim value appearing in a logged transport error | accept | Plan 140.2-08's redaction leaf landed first and covers header-derived values reachable from the seam log sites; the claim itself is a derived MAC over a public user id with a 300 s life, so it is deliberately NOT added to the secret list. Recorded rather than left implicit. |
| T-140.2-09-05 | Tampering | A second copy of the HMAC construction drifting from the first | mitigate | One implementation, extracted; `grep -rn "createHmac" src/` shows a single site. |
| T-140.2-09-06 | Denial of service | Per-tenant limits sized against platform-wide observed traffic | accept | Recorded, not fixed. Limit VALUES are deliberately untouched (TS-22, owned by 146): the 20/minute optimizer becomes per-tenant here and may then be wrong in the OTHER direction. Auditing after the flip, not before, is the correct order. |
| T-140.2-09-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed by this plan. |
</threat_model>

<verification>
- `npx vitest run src/lib/ src/app/api/strategies/ src/app/api/keys/ src/app/api/scenario/` green.
- `npx tsc --noEmit` exit 0 · `npm run lint` 0 errors.
- **Full suite once at this wave boundary.**
- Zero Python files modified: `git diff --name-only | grep -c "\.py$"` returns 0.
- `grep -rn "createHmac" src/` returns exactly one implementation site.
</verification>

<success_criteria>
- ROADMAP SC7's test clause is satisfied on five LIVE routes, not the minimum one, and not by the
  zero-signature-change shortcut.
- SC7's wording in ROADMAP.md states the behaviour-derived scope.
- The mint exists once, and its absence-of-secret path is loud.
</success_criteria>

<output>
Create `.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-09-SUMMARY.md` when done.
Record: the six-wrapper coverage, the seven threaded call sites, the exact ROADMAP SC7 wording as
edited, the Python-reader obligation as recorded, the claim-vs-secret-list decision, and the
M28/M44/M45/M46 receipts.
</output>
</content>
