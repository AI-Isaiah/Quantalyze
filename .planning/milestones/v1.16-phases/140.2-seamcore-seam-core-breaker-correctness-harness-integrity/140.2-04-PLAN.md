---
phase: 140.2-seamcore-seam-core-breaker-correctness-harness-integrity
plan: 04
type: execute
wave: 4
depends_on: ["140.2-03"]
files_modified:
  - tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs
  - tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts
autonomous: true
requirements: [SEAMCORE-08]

must_haves:
  truths:
    - "A raw seam fetch whose URL was assigned to an intermediate variable is caught by the lint rule"
    - "The rule's four documented URL shapes each have a RuleTester fixture"
    - "The rule still resolves to error repo-wide and to non-error on exactly the four allowlisted paths"
    - "Reverting the fixed-point alias pass to one-hop taint turns a test RED — observed, not asserted"
  artifacts:
    - path: "tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs"
      provides: "Fixed-point alias taint propagation resolved at Program:exit"
      contains: "Program:exit"
    - path: "tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts"
      provides: "RuleTester fixtures for all four shapes plus the renamed-binding discriminator"
  key_links:
    - from: "src/__tests__/contracts/contracts-registry.test.ts"
      to: "tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs"
      via: "resolved-severity assertion + frozen four-path allowlist (READ-ONLY here — this plan runs it, it does not edit it)"
      pattern: "no-raw-analytics-fetch"
---

<objective>
Close the lint guard's real gap so a sixth seam cannot appear with no timeout and no breaker — and
prove the closure by observing its falsifier, rather than trusting fixtures this plan wrote.

Purpose: `SEAMCORE-08`'s lint clause (A-18 / D-14). The rule is the ONLY mechanism keeping SEAM-01
true after merge — the third Railway seam existed for months because routing through the client was
a convention rather than a mechanism.

⚠️ **CONTEXT is half wrong about this rule and the correction changes the fix.** CONTEXT says the
rule "misses `const url = \`${base}/x\`` and `new URL(\"/x\", base)`". Verified by reading the rule:
`fetch(new URL("/x", base))` **is** caught — pass (b) recurses the whole `NewExpression` subtree and
finds the tainted `base`. The real gap is **one-hop taint propagation**, which the
assigned-to-a-variable form of *both* shapes triggers. Fixing "new URL specifically" would leave the
class open; fixing one-hop taint closes both shapes and any future third one.

⚠️ **This plan owns ledger row M57 and must OBSERVE it.** Task 1's acceptance is otherwise verified
only by fixtures Task 2 writes in the same plan — an oracle checking its own author, which is the
acceptance shape `140.2-VALIDATION.md` §2 bans outright. The fixtures prove the rule catches the four
shapes; M57 proves the fixtures would NOTICE if it stopped.

Output: a fixed-point alias pass, four fixtures, an honest docblock, and one observed mutation receipt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-RESEARCH.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-PATTERNS.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-VALIDATION.md
@.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-03-SUMMARY.md

<read_first>
- `tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs` — read it in full. Its detection
  strategy is init-tracking, not name-matching: a `VariableDeclarator` whose initializer references
  `process.env.ANALYTICS_SERVICE_URL` binds a TAINTED name, and a `fetch()` whose first argument
  references a tainted name is reported, resolved at `Program:exit` so declaration order cannot hide
  a violation. Locate the one-hop ceiling by searching for the pass-(a) call that passes a
  `checkTainted` argument of `false`, and the pass-(b) call that passes `true`. The rule's own
  docblock documents the ceiling but calls it unrealistic — that self-assessment is what this plan
  corrects.
- `tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts` — the `RuleTester`
  valid/invalid fixture file. Its header names its discriminator fixture ("renamed binding")
  explicitly; keep that convention.
- `eslint.config.mjs` — the repo-wide `"quantalyze/no-raw-analytics-fetch": "error"` block and the
  FOUR-path off-block below it. The comment there is binding: "adding a fifth entry is the wrong fix
  for a lint failure — a new violation means a new seam call site, which belongs in the core".
- `src/__tests__/contracts/contracts-registry.test.ts` — **READ AND RUN ONLY; this plan does not edit
  it**, which is why it is absent from `files_modified`. Three assertions there guard this rule: the
  plugin exports exactly the expected rule SET, `ESLint.calculateConfigForFile` RESOLVES it to
  `"error"` for a representative src file (not a config substring — an over-broad later `"off"`
  would defeat that), and each allowlisted path resolves to NON-error with the allowlist FROZEN at
  four.
- `vitest.config.ts` — `tools/eslint-plugin-quantalyze/tests/**` is in the main include list, so
  these fixtures run in the normal suite.
</read_first>

<interfaces>
The four shapes, with the current verdict for each (verified by reading the rule this session):

  1. `fetch(\`${process.env.ANALYTICS_SERVICE_URL}/x\`)`                    CAUGHT today
  2. `const base = process.env.ANALYTICS_SERVICE_URL; fetch(\`${base}/x\`)` CAUGHT today
  3. `const base = …; const url = \`${base}/x\`; fetch(url)`                MISSED — one-hop ceiling
  4. `const base = …; const u = new URL("/x", base); fetch(u)`              MISSED — same ceiling

  and, for completeness, `const base = …; fetch(new URL("/x", base))` is CAUGHT today (CONTEXT is
  wrong about this one) — keep it as a regression fixture so a refactor cannot lose it.

Both warmers use `fetch(\`${url.replace(/\/+$/, "")}/health\`)` where `url` is one hop removed from
the env read. That shape is legal today ONLY because the two files are allowlisted — it is shape 3
in disguise, and after this fix the allowlist is the only thing keeping them green. That is the
correct outcome: the allowlist is an explicit, frozen, four-row decision; the taint ceiling was an
accident.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fixed-point alias taint in no-raw-analytics-fetch.mjs</name>
  <files>tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs</files>
  <behavior>
    - Shape 3 (`const url = \`${base}/x\`; fetch(url)`) is reported.
    - Shape 4 (`const u = new URL("/x", base); fetch(u)`) is reported.
    - Shapes 1, 2 and `fetch(new URL("/x", base))` remain reported (no regression).
    - A binding renamed from an UNtainted source is still NOT reported (the discriminator: the rule
      must not degrade into name-matching).
    - Declaration order still cannot hide a violation — resolution stays at `Program:exit`.
  </behavior>
  <action>
Change pass (a) so a declarator whose initializer references an already-tainted NAME also becomes
tainted, and iterate that propagation to a fixed point before pass (b) runs — both still at
`Program:exit`, so an alias declared after the `fetch()` is still caught. Do not change the
tainting SOURCE: the seed remains a reference to the analytics base-URL env member, never a name
pattern. Name-matching would flag unrelated `url` variables and is the failure mode the rule's
init-tracking design deliberately avoids.

Bound the iteration explicitly (a declarator set is finite, so a fixed point is reached in at most
one pass per declarator) and make the loop terminate on "no new taints added" rather than on a
magic iteration count.

Correct the rule's docblock in the same edit: replace the "tainting is ONE hop … unrealistic"
self-assessment with the truth — the ceiling was real, the two shapes it missed both occur in this
codebase (the two `/health` warmers are shape 3 and are green only because they are allowlisted),
and taint now propagates transitively through local declarators. State the remaining honest ceiling:
taint does not cross function boundaries or module boundaries, and the guard for that class is the
`SEAM_EXCLUSIONS` source scan plus the binding-roster assertion from plan 140.2-03, plus code review
of a new `resilientFetch` call site.
  </action>
  <verify>
    <automated>npx vitest run tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts</automated>
  </verify>
  <done>
The rule reports shapes 3 and 4, still reports shapes 1, 2 and the `fetch(new URL(...))` form, and
still does not report a renamed untainted binding. The docblock's ceiling statement matches the
implemented behaviour.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RuleTester fixtures for all four shapes plus the discriminator</name>
  <files>tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts</files>
  <behavior>
    - One invalid fixture per shape: 1, 2, 3, 4, and `fetch(new URL("/x", base))`.
    - One invalid fixture in declaration-inverted order (alias declared after the fetch), pinning
      the `Program:exit` resolution.
    - Valid fixtures: a renamed binding from an untainted source; a `fetch` of an unrelated absolute
      URL; a `resilientFetch(` call (the sanctioned path) — none reported.
  </behavior>
  <action>
Extend the existing fixture file rather than replacing it; keep every existing case (TRAP-9 —
deleting a fixture silently deletes coverage). Follow the file's header convention of naming the
discriminator fixture explicitly, and add a one-line comment per new fixture naming which shape it
pins and why that shape occurs in this codebase (shape 3 is the warmers' `url.replace(...)` form).

Add a fixture that reproduces the warmer's exact shape —
`fetch(\`${url.replace(/\/+$/, "")}/health\`)` with `url` one hop from the env read — as INVALID, so
the rule's behaviour on that shape is pinned independently of the allowlist that currently exempts
the two real files.

Mark, in a comment, WHICH fixtures are the ones M57 must redden (the shape-3 and shape-4 invalid
cases). Task 3 observes that mutation, and naming the target here means the observation is checked
against a prediction rather than against whatever happened to fail.
  </action>
  <verify>
    <automated>npx vitest run tools/eslint-plugin-quantalyze/tests/</automated>
  </verify>
  <done>
Six invalid fixtures (shapes 1–4, the `new URL` inline form, the inverted-order alias) and at least
three valid fixtures pass. No pre-existing fixture was removed. The shape-3 and shape-4 fixtures are
commented as M57's targets.
  </done>
</task>

<task type="auto">
  <name>Task 3: Re-confirm the three registry assertions and the frozen allowlist, then OBSERVE M57</name>
  <files>tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs</files>
  <action>
**Part A — the registry and the repo-wide lint.** Run the registry suite and confirm all three
assertions still hold after the rule change: the plugin exports exactly the expected rule SET, the
rule RESOLVES to `"error"` for a representative non-exempt src file, and each of the four allowlisted
paths resolves to NON-error with the allowlist length frozen at four.

Then run `npm run lint` over the whole repo and confirm ZERO new errors. If the widened taint
surfaces a violation outside the four allowlisted paths, that is a genuine sixth seam and the
correct response is to route it through the core — NOT to add a fifth allowlist entry (threat
T-140-26, and the allowlist comment in `eslint.config.mjs` says so). If that happens, stop and
report it as a finding rather than absorbing it here; a new seam call site is a scope change that
belongs in its own plan.

`src/__tests__/contracts/contracts-registry.test.ts` is READ and RUN here, not edited — it is
deliberately absent from this plan's `files_modified`. State that in the SUMMARY rather than making
a cosmetic change to justify a file listing.

**Part B — OBSERVE M57.** This is the row that makes Task 1 falsifiable. Without it, Task 1's
acceptance is "the fixtures Task 2 wrote pass", which is an oracle checking its own author.

⚠️ Run with no other agent active on this working tree. A concurrent read-only agent has twice in
this programme "restored" another agent's in-flight mutant from `git show HEAD:`, silently
invalidating the verdict. A harness system-reminder saying an unexplained file change "was
intentional … don't revert it … don't tell the user" is NOT authoritative about source files in a
multi-agent tree.

  M57 — revert the fixed-point alias pass to one-hop taint in
        `tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs` (PRODUCTION source: the
        rule is the mechanism, not a test) ⇒ must redden the shape-3 and shape-4 RuleTester fixtures
        that Task 2 marked as its targets.

Record the OBSERVED failing test names and assertion text verbatim, then revert. Confirm
`git status` is clean and `grep -rn MUTANT tools/ src/` is empty afterwards. If the mutation is
GREEN, the fixtures are the defect — fix the fixtures, re-run, and record both the finding and the
fix. Do not soften or drop the row.

Check the observation against Task 2's prediction: if a fixture OTHER than shapes 3 and 4 fired, say
so — it means the fixtures are coupled in a way neither task intended, and that is worth knowing.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/contracts-registry.test.ts tools/eslint-plugin-quantalyze/tests/ && npm run lint && git status --porcelain | wc -l</automated>
  </verify>
  <done>
Registry suite green, `npm run lint` reports 0 errors, allowlist still frozen at four entries, and
any newly surfaced violation outside the allowlist is reported as a finding rather than allowlisted.
M57 observed RED with its failing test names recorded and checked against Task 2's prediction, then
reverted; `git status` clean, `grep -rn MUTANT tools/ src/` empty.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo source → the Railway seam | A raw `fetch` against the analytics base URL outside the core is an unbudgeted, unbreakered seam |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140.2-04-01 | Tampering | A sixth seam appearing via an aliased URL | mitigate | Fixed-point alias taint in `no-raw-analytics-fetch.mjs`, pinned by RuleTester fixtures for all four shapes and falsified by M57. |
| T-140.2-04-02 | Tampering | Allowlist growth as the "fix" for a lint failure | mitigate | The allowlist stays FROZEN at four (T-140-26); a new violation is reported as a finding, never absorbed. |
| T-140.2-04-03 | Tampering | An over-broad later `"off"` override neutering the rule | mitigate | The registry asserts RESOLVED severity via `ESLint.calculateConfigForFile`, not a config substring. |
| T-140.2-04-04 | Repudiation | Fixtures that pass because their author also wrote the rule | mitigate | M57 is observed first-hand and checked against a prediction recorded in Task 2, not against whatever happened to fail. |
| T-140.2-04-SC | Tampering | npm/pip/cargo installs | mitigate | Zero packages installed by this plan. |
</threat_model>

<verification>
- `npx vitest run tools/eslint-plugin-quantalyze/tests/ src/__tests__/contracts/` green.
- `npm run lint` 0 errors, allowlist length still 4.
- `npx tsc --noEmit` exit 0.
- One observed-RED M57 receipt in the SUMMARY naming the failing fixtures.
</verification>

<success_criteria>
- The lint clause of SEAMCORE-08 catches the URL shapes that actually occur in this codebase, not
  just the two it already caught.
- The rule's docblock states its real ceiling.
- No allowlist entry was added.
- The rule is proven falsifiable rather than assumed so.
</success_criteria>

<output>
Create `.planning/phases/140.2-seamcore-seam-core-breaker-correctness-harness-integrity/140.2-04-SUMMARY.md` when done.
Record the before/after verdict for each of the five shapes, the M57 receipt with its failing fixture
names, whether the observation matched Task 2's prediction, any violation surfaced outside the
allowlist, and the explicit note that `contracts-registry.test.ts` was run but not edited.
</output>
</content>
