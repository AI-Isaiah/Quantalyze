---
phase: 142-job-strategy-analytics-stuck-computing-reaper-computing-star
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_job07_reaper_off_worker_loop.py
  - analytics-service/scripts/reset_stuck_computing_rows.py
autonomous: true
requirements: [JOB-07, JOB-02]
must_haves:
  truths:
    - "No reaper identifier is reachable from the worker's dispatch surface — enforced by a structural gate that demonstrably CAN fail (positive control observed)"
    - "The healthz probe stays 200 through a dispatch tick with a large synthetic stranded backlog present, AND a control proves the probe detects a loop-blocking synchronous reap (a yielding sleep passes where a blocking sleep fails)"
    - "The broken one-off script reset_stuck_computing_rows.py (42703 on non-existent updated_at — C-5) is DELETED and a stays-absent gate prevents resurrection"
  artifacts:
    - path: "analytics-service/tests/test_job07_reaper_off_worker_loop.py"
      provides: "JOB-07 structural gate + behavioral healthz probe + controls + deleted-script gate"
      min_lines: 120
  key_links:
    - from: "analytics-service/tests/test_job07_reaper_off_worker_loop.py"
      to: "analytics-service/main_worker_healthz.py"
      via: "real TCP probe against the deployed healthz server (helpers from test_worker_isolation_e2e.py)"
      pattern: "_probe_healthz|healthz"
    - from: "analytics-service/tests/test_job07_reaper_off_worker_loop.py"
      to: "analytics-service/main_worker.py + services/job_worker.py + routers/ + scripts/"
      via: "comment-stripped forbidden-token scan of the dispatch-reachable surface"
      pattern: "reap_strategy_analytics_stuck_computing"
---

<objective>
Deliver the JOB-07 proof that no reaper/sweep work runs on the worker's shared asyncio event
loop, designed so it CAN fail (non-negotiable 7: the naive behavioural test alone cannot fail,
because pg_cron means no worker-loop reap path exists — the real gate is structural plus a
positive control). Also delete the broken superseded one-off script (non-negotiable 10) and pin
its absence.

Purpose: JOB-07 is the WEDGE-01 regression class — heavy synchronous work on the shared loop
froze healthz. The reaper lands in pg_cron by construction (CONTEXT locked); this plan makes
that property enforceable and falsifiable rather than incidental.

Output: `analytics-service/tests/test_job07_reaper_off_worker_loop.py`;
`analytics-service/scripts/reset_stuck_computing_rows.py` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-CONTEXT.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md

Fixed names this plan depends on (shared across the plan set, decided at planning time):
- Cron jobname: `reap_strategy_analytics_stuck_computing` (created by plan 142-04's migration).
  It must appear in Python ONLY inside this test file's forbidden-token literal.
- Deleted script path: `analytics-service/scripts/reset_stuck_computing_rows.py`.

Honesty bound (research §JOB-07, main_worker.py:637-641): the heartbeat catches a LOOP-BLOCKING
freeze but NOT a yielding single-job hang. The new file's docstring must state this bound
verbatim-in-substance; prose anywhere must not over-claim.
</context>

<interfaces>
Reusable helpers — import or copy from `analytics-service/tests/test_worker_isolation_e2e.py`
(all stdlib):

```python
_free_ephemeral_port()      # :48  — bind port 0, read assignment, release
_wait_port_listening(port)  # :62  — poll-connect until serve_forever
_probe_healthz(port)        # :80  — raw GET /healthz HTTP/1.1 over TCP, 5s read guard
_empty_claim_supabase()     # :102 — MagicMock claim RPC returning data=[]
# Positive-case shape: :119 test_healthz_stays_200_through_long_backfill
#   (monkeypatch _HEARTBEAT_INTERVAL_S to 0.02, real start_healthz_server, mid-dispatch probe,
#    assert not dt.done() to prove the probe landed DURING dispatch)
# Negative control shape: :182 test_healthz_503_when_tick_stale
#   (force LAST_TICK_AT stale, assert b"503 Service Unavailable" and b'"status": "stale"')
```

Scan-surface + comment-strip idioms — `analytics-service/tests/test_dark_path_deleted.py`:
`_repo_root()` (:37), `_strip_comment(line, lang=...)` (:55), `_py_scan_files()` (:71),
the anti-vacuity `assert scan, "..."` (:108), and the deleted-file gate (:157).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Structural gate + deleted-script gate — and delete the broken one-off</name>
  <files>analytics-service/tests/test_job07_reaper_off_worker_loop.py, analytics-service/scripts/reset_stuck_computing_rows.py</files>
  <read_first>
    - analytics-service/tests/test_dark_path_deleted.py:23-118,157-165,236-252 (two-direction gate framing, _repo_root, _strip_comment, scan-file list, anti-vacuity assert, deleted-file gate, positive control)
    - analytics-service/tests/test_limiter_identity.py:498-521 (the AST "zero constructions" alternative shape — use if AST fits better than token scan)
    - analytics-service/scripts/reset_stuck_computing_rows.py (the file being deleted — confirm nothing imports it: `grep -rn "reset_stuck_computing_rows" . --include="*.py"` must show ONLY the file itself before deleting; checker-verified zero *.py importers. The UNSCOPED repo-wide grep ALSO hits prose in CHANGELOG.md:9797 and .planning/ (research/STACK.md:72,83,150; research/SUMMARY.md:22,240; REQUIREMENTS.md:52; ROADMAP.md:391,399) — expected historical references, NOT a STOP condition; leave them untouched — W-7)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-CONTEXT.md (Threshold derivation + JOB-07 proof section — the structural-gate mandate)
  </read_first>
  <action>
    Create `analytics-service/tests/test_job07_reaper_off_worker_loop.py`. Module docstring
    states the two-direction contract (mirror test_dark_path_deleted.py:23-29 framing): the
    reaper is pg_cron-only BY CONSTRUCTION, so the enforceable Python property is ABSENCE —
    no reaper identifier reachable from the worker dispatch surface — plus controls proving
    the gate can fail. Include the honesty bound: the heartbeat detects loop-blocking freezes,
    not yielding single-job hangs.

    Structural gate `test_no_reaper_identifier_on_worker_surface`: scan the dispatch-reachable
    surface — reuse the exact file list idiom from test_dark_path_deleted._py_scan_files
    (services/analytics_runner.py, services/job_worker.py, routers/cron.py, main_worker.py,
    main.py, plus rglob of routers/ and scripts/) — for FORBIDDEN tokens, comment-stripped via
    _strip_comment: `reap_strategy_analytics_stuck_computing` (the cron jobname; typed as a
    LOCAL literal here, never a shared constant — nothing in production Python may know this
    name) and `reset_stuck_computing_rows` (script-resurrection guard). Assert zero hits with
    a message naming file:line per hit. Anti-vacuity: `assert scan_files` non-empty AND assert
    the scanner detects a synthetic in-memory line containing the jobname (scanner self-test —
    the SC-4 structural half must be demonstrably capable of RED).

    Deleted-file gate `test_superseded_one_off_stays_deleted`: assert
    scripts/reset_stuck_computing_rows.py does not exist (mirror test_dark_path_deleted.py:157-165),
    message explaining WHY it must stay dead: it selects/filters on `updated_at`, a column
    strategy_analytics does not have → PostgREST 42703 (C-5); the pg_cron reaper of migration
    20260802120000 supersedes it.

    Then `git rm analytics-service/scripts/reset_stuck_computing_rows.py` (after confirming
    via the *.py-scoped grep above that no Python code references it — the CHANGELOG.md and
    .planning/ prose references are expected and stay untouched). Do NOT write any
    replacement script.

    SC-4 mutation observation (Falsifiability Ledger row SC-4 — the structural gate):
    temporarily add a line containing the jobname
    string to main_worker.py (e.g. an assignment in dead position), run the gate, observe RED,
    paste the failing output into the SUMMARY, revert, re-run GREEN, `grep -rn MUTANT
    analytics-service/` → 0 and `git diff --stat` shows only intended files.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_job07_reaper_off_worker_loop.py -x -q && test ! -f scripts/reset_stuck_computing_rows.py && echo deleted-ok</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_job07_reaper_off_worker_loop.py -x -q` exits 0 with ≥ 2 structural tests collected.
    - `analytics-service/scripts/reset_stuck_computing_rows.py` no longer exists; `grep -rn "reset_stuck_computing_rows" --include="*.py" .` returns only the new test file's forbidden-token literal and gate.
    - The scanner self-test exists: the test file contains an assertion that a synthetic line bearing `reap_strategy_analytics_stuck_computing` IS flagged by the scan function.
    - SC-4 structural mutation OBSERVED: SUMMARY carries the pasted RED output from the temporary jobname injection into main_worker.py, plus the revert-and-green evidence.
    - The scan list assert (`assert scan_files`) is present (anti-vacuity, S-5).
  </acceptance_criteria>
  <done>Structural gate green on clean tree, RED under injected jobname (observed), script deleted with a stays-absent gate.</done>
</task>

<task type="auto">
  <name>Task 2: Behavioral healthz probe — backlog stays 200, blocking-vs-yielding control pair</name>
  <files>analytics-service/tests/test_job07_reaper_off_worker_loop.py</files>
  <read_first>
    - analytics-service/tests/test_worker_isolation_e2e.py:1-26,48-115,119-206 (docstring oracle discipline, all four helpers, the positive case with mid-dispatch probe + assert not dt.done(), and the 503 stale control)
    - analytics-service/main_worker_healthz.py:1-80 (STALE_THRESHOLD = 90.0, LAST_TICK_AT, _handle_healthz reads STALE_THRESHOLD at request time — so a monkeypatched module attribute takes effect)
    - analytics-service/main_worker.py:127-141,560-680 (heartbeat wiring: _HEARTBEAT_INTERVAL_S, LAST_TICK_AT writes at :599/:616/:643-645, dispatch loop)
  </read_first>
  <action>
    Append `class TestReaperOffWorkerLoopBehavior` to the same file, mirroring
    test_worker_isolation_e2e.py machinery (import its helpers if importable from the tests
    package; otherwise copy them with a comment naming the source lines — do not fork their
    logic).

    Positive case `test_healthz_stays_200_with_large_stranded_backlog`: build a MagicMock
    supabase whose strategy_analytics query surface represents a large stranded backlog
    (e.g. 5,000 rows at computation_status='computing') while the claim RPC returns a normal
    small batch, run a real `dispatch_tick` against the real healthz server on an ephemeral
    port (monkeypatch `_HEARTBEAT_INTERVAL_S` to 0.02 and PORT, per the :119 analog), probe
    mid-dispatch, assert `not dt.done()` (probe landed DURING dispatch), assert `b"200 OK"`,
    and assert `LAST_TICK_AT` advanced past the start stamp. Docstring must state plainly that
    this test alone CANNOT fail for the reaper property (no worker-loop reap path exists) and
    that the teeth live in the structural gate + the control pair below (Rule 9 / P-7).

    Control pair (the falsifier — this is what makes the 200 meaningful):
    - `test_probe_starved_by_loop_blocking_sync_work`: monkeypatch
      `main_worker_healthz.STALE_THRESHOLD` to 0.2 and patch the dispatch path so the (mock)
      job's handler executes a SYNCHRONOUS `time.sleep(0.5)` on the event loop (not awaited,
      not to_thread — this simulates a reaper wrongly run on the shared loop). Issue the probe
      concurrently; assert the observable starvation: the probe's response arrives only after
      the block releases AND (response latency ≥ 0.4 s, measured with time.monotonic) AND/OR
      the response is `b"503 Service Unavailable"` with `b'"status": "stale"'` because
      LAST_TICK_AT could not advance during the block. Assert LAST_TICK_AT did NOT advance
      while blocked.
    - `test_probe_healthy_when_same_work_yields`: identical setup but the handler awaits
      `asyncio.sleep(0.5)` (yielding). Assert the mid-sleep probe returns fast
      (latency < 0.1 s) and `b"200 OK"`. This pair pins the PROPERTY (blocking vs yielding),
      so replacing the blocking sleep with a yielding one flips exactly one test — the red
      asymmetry that proves the control is live.
    Also keep the analog's forced-stale 503 control (`LAST_TICK_AT = time.time() -
    (STALE_THRESHOLD + 10)` → 503) as the cheap deployed-server staleness proof.

    Restore all monkeypatched module state in finally blocks exactly as the analog does
    (`_saved_tick` discipline, server_task cancel + await with CancelledError swallow).

    SC-4b observation (Falsifiability Ledger row SC-4b): run the control pair as-written and
    paste both outcomes (starved vs healthy) into the SUMMARY — the pair IS the
    mutation+control, no production edit needed.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_job07_reaper_off_worker_loop.py -x -q && pytest tests/test_worker_isolation_e2e.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The file contains ≥ 3 behavioral tests: backlog-200, blocking-starved, yielding-healthy (plus optionally the forced-stale 503).
    - The blocking test asserts at least one of {latency ≥ 0.4 s, 503 stale body} AND that LAST_TICK_AT did not advance during the block; the yielding twin asserts latency < 0.1 s and 200 — the pair differs ONLY in time.sleep vs asyncio.sleep.
    - The backlog test's docstring names its own non-falsifiability for the reaper property and points at the structural gate (P-7 honesty).
    - `pytest tests/test_worker_isolation_e2e.py -x -q` still green (no shared-state pollution: LAST_TICK_AT and env restored).
    - Total file runtime < 15 s (no real STALE_THRESHOLD=90 waits — thresholds shrunk via monkeypatch).
  </acceptance_criteria>
  <done>Behavioral probe green; blocking-vs-yielding control pair observed producing opposite outcomes; existing isolation e2e suite unaffected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker event loop → healthz consumers (Railway) | A loop-blocking reap would silently take the money-pipeline worker out of service while healthz lies green — the WEDGE-01 class |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-04 | DoS | worker shared asyncio loop | mitigate | Structural absence gate (jobname unreachable from dispatch surface) + blocking-vs-yielding control pair proving the probe detects starvation; reaper itself lives in pg_cron (plan 142-04), a separate failure domain |
| T-142-05 | Repudiation | deleted one-off script resurrected and run against prod (42703 or, worse, a fixed variant reaping on the wrong key) | mitigate | stays-absent gate + forbidden-token scan of scripts/ |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed in this plan (stdlib-only test file) |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_job07_reaper_off_worker_loop.py -x -q` green; runtime < 15 s.
- SC-4 structural mutation observed RED and reverted; SC-4b behavioral control pair observed producing opposite outcomes.
- Script deleted; only CHANGELOG.md and the new test reference its name.
</verification>

<success_criteria>
- JOB-07 gate exists with real teeth: structural absence + scanner self-test + blocking/yielding control pair; the naive always-green form was NOT shipped alone.
- reset_stuck_computing_rows.py deleted with a stays-absent gate.
- No over-claim: docstrings state the heartbeat's loop-blocking-only detection bound.
</success_criteria>

<output>
Create `.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-02-SUMMARY.md` when done, carrying the SC-4 observation evidence (structural RED paste + control-pair outcomes).
</output>
