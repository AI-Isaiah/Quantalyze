---
phase: 142-job-strategy-analytics-stuck-computing-reaper-computing-star
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/types.ts
  - src/lib/utils.ts
  - src/__tests__/analytics-format.test.ts
  - src/__tests__/phase-52-container-tabular-nums.test.tsx
  - src/components/charts/WorstDrawdowns.test.tsx
  - src/components/strategy/CompareTable.test.tsx
  - src/components/strategy/discovery-selectors.contract.test.tsx
  - src/components/strategy/StrategyGrid.test.tsx
  - src/components/strategy/StrategyTable.test.tsx
autonomous: true
requirements: [JOB-01]
must_haves:
  truths:
    - "StrategyAnalytics carries computing_started_at: string | null — T | null EXACTLY, NEVER optional (T?): the CONTEXT/PATTERNS interface convention, and optional would let a future consumer omit the field silently (the checker verified optional is not an escape hatch here)"
    - "EMPTY_ANALYTICS and every full StrategyAnalytics fixture literal compile against the new required field — npm run typecheck exits 0 on the completed plan"
    - "The checker-measured 8-file compile blast radius is pinned as a literal file list — a 9th erroring file is a conscious STOP-and-extend, never a silent widen"
  artifacts:
    - path: "src/lib/types.ts"
      provides: "computing_started_at: string | null on StrategyAnalytics, directly after computed_at, with the NULL-semantics + reaper-cron comment"
      contains: "computing_started_at"
    - path: "src/lib/utils.ts"
      provides: "EMPTY_ANALYTICS gains computing_started_at: null (the exhaustive literal at :178 is exactly why the field cannot land type-only)"
      contains: "computing_started_at"
  key_links:
    - from: "src/lib/types.ts"
      to: "the 7 fixture files + src/lib/utils.ts"
      via: "required-field propagation — the tsc TS2741/TS2322 error list IS the census; every base object literal gains computing_started_at: null"
      pattern: "computing_started_at"
---

<objective>
Add the `computing_started_at: string | null` row-type line to `StrategyAnalytics` and
propagate the now-required field through its full compile blast radius: `EMPTY_ANALYTICS`
in `src/lib/utils.ts` plus the seven test-fixture files the checker measured empirically
(BLOCKER-2 — the exact prescribed edit was applied and `npx tsc --noEmit` run; 8 files
error, none of which were in plan 142-03's files_modified).

Purpose: JOB-01's frontend-visible surface. The reaper writes `computing_started_at` and the
row type must tell consumers it exists and what NULL means. Split out of plan 142-03 because
the 8-file radius would push that plan past scope thresholds (checker directive); this plan
is pure mechanical propagation — one identical literal insertion per site, driven by the
compiler.

Scope guards: `T | null`, NEVER `T?` — CONTEXT and PATTERNS both mandate the interface
convention, and the revision constraint pins it (optional is NOT an escape from the blast
radius). `src/lib/database.types.ts` stays untouched (the pre-existing 2-column drift there
is a TODOS.md item, logged by plan 142-03 Task 2). Zero behavior changes anywhere — every
edit is a type line or a fixture literal key.

Task-sizing deviation (documented consciously): 9 files in one task exceeds the ≤5-file
split signal, but the interface edit and its 8 compile-forced sites are ATOMIC — any
partition leaves `npm run typecheck` RED between tasks, which would violate the per-task
green-verify rule. Every edit is the same one-line literal insertion; measured context cost
is ~10-15%, well inside the task ceiling. Atomicity wins over the file-count heuristic here
(Rule 7: pick one, say why).

Output: the type line, `EMPTY_ANALYTICS` updated, 7 fixture files updated, typecheck green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-CONTEXT.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-PATTERNS.md

Fixed names this plan consumes (shared across the plan set):
- Cron jobname `reap_strategy_analytics_stuck_computing` and migration
  `20260802120000_strategy_analytics_stuck_computing_reaper.sql` — referenced ONLY in the
  type line's comment (prose), never as code.
- The checker-measured blast radius (2026-08-02, `npx tsc --noEmit` against the prescribed
  edit): `src/lib/utils.ts` (TS2741 — `EMPTY_ANALYTICS` is an exhaustive
  `StrategyAnalytics` literal at :178) plus 7 test files erroring with TS2741 (full fixture
  literals missing the new required property) and/or TS2322 (`string | null | undefined`
  not assignable to `string | null` in `Partial`-overlay fixture builders):
  src/__tests__/analytics-format.test.ts, src/__tests__/phase-52-container-tabular-nums.test.tsx,
  src/components/charts/WorstDrawdowns.test.tsx, src/components/strategy/CompareTable.test.tsx,
  src/components/strategy/discovery-selectors.contract.test.tsx,
  src/components/strategy/StrategyGrid.test.tsx, src/components/strategy/StrategyTable.test.tsx.
- The fixture idiom (verified, e.g. WorstDrawdowns.test.tsx:22-45): a `makeAnalytics(
  overrides: Partial<StrategyAnalytics> = {}): StrategyAnalytics` helper returning a full
  base object literal — the fix at every site is adding `computing_started_at: null,` to
  the BASE literal (beside `computed_at`), never weakening the type.
</context>

<interfaces>
Verified current state (direct reads, 2026-08-02):

```typescript
// src/lib/types.ts:288-294 (interface head — the new line goes directly after computed_at):
export interface StrategyAnalytics {
  id: string;
  strategy_id: string;
  computed_at: string;
  // B9: single source of truth is STRATEGY_ANALYTICS_COMPUTATION_STATUSES in
  // closed-sets.ts, pinned against the DB CHECK by check-zod-db-check-parity.test.ts.
  computation_status: StrategyAnalyticsComputationStatus;

// src/lib/utils.ts:178-182 (the exhaustive literal — gains computing_started_at: null):
export const EMPTY_ANALYTICS: StrategyAnalytics = {
  id: "",
  strategy_id: "",
  computed_at: "",
  computation_status: "pending",
```

Target type line (goes directly after `computed_at: string;`):

```typescript
  // JOB-01: NULL = "not currently computing". Writer-stamped in the SAME statement that
  // sets computation_status='computing'; cleared on every exit. The pg_cron reaper
  // reap_strategy_analytics_stuck_computing (migration 20260802120000) keys on this,
  // never computed_at.
  computing_started_at: string | null;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add the row-type line and propagate the required field through the pinned 9-file blast radius</name>
  <files>src/lib/types.ts, src/lib/utils.ts, src/__tests__/analytics-format.test.ts, src/__tests__/phase-52-container-tabular-nums.test.tsx, src/components/charts/WorstDrawdowns.test.tsx, src/components/strategy/CompareTable.test.tsx, src/components/strategy/discovery-selectors.contract.test.tsx, src/components/strategy/StrategyGrid.test.tsx, src/components/strategy/StrategyTable.test.tsx</files>
  <read_first>
    - src/lib/types.ts:288-329 (StrategyAnalytics — T | null nullability shape; the computation_status field comment style that names its pinning test)
    - src/lib/utils.ts:170-207 (extractAnalytics + the exhaustive EMPTY_ANALYTICS literal)
    - src/components/charts/WorstDrawdowns.test.tsx:20-50 (the makeAnalytics Partial-overlay fixture idiom the other six test files share in some variant)
  </read_first>
  <action>
    1. `src/lib/types.ts` StrategyAnalytics: add `computing_started_at: string | null;`
       directly after `computed_at: string;`, with the comment from the interfaces block
       (NULL = "not currently computing"; writer-stamped in the same statement as
       computation_status='computing'; names the reaper cron and migration — JOB-01). Use
       `T | null`, NEVER `T?` (interface convention; revision-constraint-pinned — optional
       is not an escape from the blast radius, it is a silent-omission hole).
    2. `src/lib/utils.ts` EMPTY_ANALYTICS (:178): add `computing_started_at: null,`
       directly after `computed_at: "",`.
    3. Run `npx tsc --noEmit`. Fix EVERY remaining error by adding
       `computing_started_at: null,` to the full StrategyAnalytics BASE object literal in
       each of the 7 pinned test files (beside its `computed_at` key). TS2741 sites are
       full literals missing the property; TS2322 sites are Partial-overlay spreads whose
       fix is ALSO the base-literal insertion. NEVER resolve an error by making the field
       optional, widening a type, adding `undefined` to the union, casting, or touching
       src/lib/database.types.ts.
    4. Blast-radius pin (BLOCKER-2 acceptance): if `npx tsc --noEmit` surfaces a
       computing_started_at-related error in ANY file outside this task's 9-file list, STOP
       — do not silently fix it. Record the file in the SUMMARY, extend files_modified
       consciously, and note the census drift (the checker measured exactly these 8 error
       files + types.ts against the 2026-08-02 tree; a 9th means the tree moved).
  </action>
  <verify>
    <automated>npm run typecheck && npx vitest run src/__tests__/analytics-format.test.ts src/__tests__/phase-52-container-tabular-nums.test.tsx src/components/charts/WorstDrawdowns.test.tsx src/components/strategy/CompareTable.test.tsx src/components/strategy/discovery-selectors.contract.test.tsx src/components/strategy/StrategyGrid.test.tsx src/components/strategy/StrategyTable.test.tsx --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "computing_started_at: string | null" src/lib/types.ts` returns exactly 1 hit, inside StrategyAnalytics, directly after computed_at.
    - `grep -cF "computing_started_at?" src/lib/types.ts` returns 0 — the field is NEVER optional.
    - `grep -c "computing_started_at: null" src/lib/utils.ts` returns 1 (EMPTY_ANALYTICS); each of the 7 test files gains ≥ 1 `computing_started_at: null` base-literal key.
    - `git diff --name-only | sort` equals EXACTLY the 9-file frontmatter list — no database.types.ts, no production component/route files, nothing else. Any extra file is a recorded, conscious files_modified extension (STOP-and-record), never a silent widen.
    - `npm run typecheck` exits 0; the 7 fixture suites pass unmodified in behavior (the only diff in each is the literal key).
  </acceptance_criteria>
  <done>Type line landed as T | null with honest NULL semantics, EMPTY_ANALYTICS and all 7 fixture files compile, blast radius matched the checker's measurement exactly (or drift recorded consciously), typecheck green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| types.ts → every StrategyAnalytics consumer | The row type is the contract all frontend readers compile against; a dishonest shape (optional field) hides the reaper key from consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-142-19 | Tampering | Field made optional (T?) to silence TS2741 → future consumers and fixtures omit it silently, and the type stops documenting the reaper contract | mitigate | T \| null pinned by acceptance criterion (`grep -cF "computing_started_at?"` == 0) + the literal 9-file blast-radius pin forcing conscious propagation |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | Zero packages installed in this plan (type + fixture literals only) |
</threat_model>

<verification>
- `npm run typecheck` exits 0 on the completed plan.
- The 7 pinned fixture suites green via the plan's vitest selection.
- `git diff --name-only` exactly equals the 9-file list (or a consciously recorded extension).
</verification>

<success_criteria>
- StrategyAnalytics documents computing_started_at as string | null with the reaper-cron comment; nothing was made optional and database.types.ts is untouched.
- The compile blast radius the checker measured is fully propagated — 8 dependent files fixed by the identical base-literal insertion.
- Zero behavior changes: every hunk is a type line or a fixture literal key.
</success_criteria>

<output>
Create `.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-06-SUMMARY.md` when done. It MUST record: the final `git diff --name-only` list vs the pinned 9 files (exact match or the recorded drift), and confirmation that no optional-field or database.types.ts escape was used.
</output>
