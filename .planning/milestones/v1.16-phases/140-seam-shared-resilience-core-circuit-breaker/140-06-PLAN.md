---
phase: 140-seam-shared-resilience-core-circuit-breaker
plan: 06
type: execute
wave: 3
depends_on: [140-02]
files_modified:
  - src/app/api/keys/[id]/permissions/route.ts
  - src/app/api/keys/[id]/permissions/route.test.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
  - src/app/api/verify-strategy/route.ts
  - src/app/api/strategies/csv-validate/route.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/keys/sync/route.seam.test.ts
autonomous: true
requirements: [SEAM-01, SEAM-02]

must_haves:
  truths:
    - "The third seam (/internal/keys/{id}/permissions, TWO duplicated AbortSignal.timeout(15_000) copies) is routed through the core — no Railway caller can hammer a dying Railway while the breaker is open (operator decision 3)"
    - "With Railway mocked to hang, keys/sync returns the typed 504 UPSTREAM_TIMEOUT envelope through the REAL process-key-client — SC-1a"
    - "All five Mechanism-B routes export maxDuration matching SEAM_ROUTE_BUDGETS"
    - "CircuitOpenError semantics survive the unstable_cache boundary: cache-miss propagates the typed error to the outer catch; cache-hit never touches the breaker"
  artifacts:
    - path: "src/app/api/keys/[id]/permissions/route.ts"
      provides: "permissions fetch via resilientFetch('keys-permissions', ...) + maxDuration"
      contains: "resilientFetch"
    - path: "src/app/api/strategies/finalize-wizard/route.ts"
      provides: "fetchLivePermissions via core + maxDuration"
      contains: "resilientFetch"
    - path: "src/app/api/keys/sync/route.seam.test.ts"
      provides: "SC-1a route-level proof with REAL client"
  key_links:
    - from: "src/app/api/strategies/finalize-wizard/route.ts"
      to: "src/lib/resilient-fetch.ts"
      via: "budgetKey keys-permissions (probe) + process-key-enqueue (onboard, via client)"
      pattern: "keys-permissions"
---

<objective>
Close the third seam and finish Mechanism B: route both duplicated
`/internal/keys/{id}/permissions` fetches through the core (SEAM-01 says EVERY seam call
site — leaving them out means two live paths still hammer a dying Railway while the breaker
is open), pin `maxDuration` on the remaining B routes, and land the SC-1a seam integration
test on `keys/sync`. The permissions route's fetch lives INSIDE an `unstable_cache` callback —
the CircuitOpenError path across that cache boundary is designed and tested explicitly, per
this fork's own Next docs.

Output: zero raw Railway fetches outside the core (excluding the 3 documented exclusions); SC-1a green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-RESEARCH.md
@src/lib/seam-errors.ts
@src/lib/resilient-fetch.ts
@src/lib/process-key-client.ts

<interfaces>
From @/lib/resilient-fetch: `resilientFetch("keys-permissions", path, init)` — 15_000ms from the table; throws CircuitOpenError; returns non-ok Responses for the caller's own handling.
From @/lib/seam-errors (dependency-free leaf): `CircuitOpenError` (`.retryAfterS`) — ALL route and test imports use the leaf, never the client modules (B-2: mocked-module re-exports are undefined under full factories; the leaf is never mocked).
Current third-seam call sites (research §4.4):
- `keys/[id]/permissions/route.ts:87-96` — GET, inside withAuth, the fetch sits INSIDE an `unstable_cache`-wrapped callback (`route.ts:78-113`, 60s revalidate); errors → `{error, code}` 502 at :211 or generic 500 at :148
- `finalize-wizard/route.ts:76-99` (`fetchLivePermissions`) — POST `?force_refresh=true`, deliberately BYPASSES the cache layers; throws on non-OK; route FAILS CLOSED (probe failure blocks finalize → 502 at :363/:375)
From src/test/helpers/upstash-breaker.ts (140-01): `createFakeUpstashStore`, `fakeRedisFor`, `fakeRatelimitFor`, `seedBreakerOpen` — importable doubles; wire in each test file's own `vi.hoisted` + `vi.mock` calls.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route both permissions call sites through the core — cache boundary designed explicitly</name>
  <files>src/app/api/keys/[id]/permissions/route.ts, src/app/api/keys/[id]/permissions/route.test.ts, src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <read_first>The fork's `unstable_cache` documentation — locate via `grep -rl "unstable_cache" node_modules/next/dist/docs` and READ it before writing code (AGENTS.md: this heavily-modified fork's docs override training data; specifically confirm whether thrown errors from the cached callback are cached, wrapped, or propagated with prototypes intact) — then src/app/api/keys/[id]/permissions/route.ts:31-116 and :140-215 (fetch block, `makeCachedFetcher`/unstable_cache layer, error shapes), src/app/api/strategies/finalize-wizard/route.ts:50-120 (fetchLivePermissions + scrubInternalToken at :105-118) and :355-380 (fail-closed gate), both route.test.ts files</read_first>
  <behavior>
    - permissions route, CACHE MISS + breaker open: the CircuitOpenError thrown by resilientFetch inside the cached callback reaches the outer handler catch as a recognizable CircuitOpenError → 503 `{error: <static copy>, code: "CIRCUIT_OPEN"}` + Retry-After — and the error result is NOT cached (a 60s-cached breaker error would outlive the cooldown)
    - permissions route, CACHE HIT: cached permissions data returned; fetch NOT called; breaker state NEVER read (no Redis interaction on hits)
    - permissions route, upstream reachable (miss): behavior byte-identical (same URL incl. query, same headers, same cache interaction); timeout → existing 502 mapping preserved
    - finalize-wizard probe (cache-bypassing): still FAILS CLOSED — CircuitOpenError → 503 with code CIRCUIT_OPEN in the route's existing error envelope shape (static copy); any other probe failure keeps today's 502 behavior
    - Neither auth gate moves; the breaker check runs inside the handlers only (via the core)
  </behavior>
  <action>
    Replace both raw `fetch(`${ANALYTICS_URL}/internal/keys/...`, { signal: AbortSignal.timeout(15_000) })` blocks with `resilientFetch("keys-permissions", <same path + query>, <same init minus signal>)` — method, query params (`?force_refresh=true` on the wizard probe), and header sets preserved byte-for-byte (these carry the internal auth headers; keep `scrubInternalToken` on any log line that could embed them). Delete the two now-duplicated 15_000 literals and any local ANALYTICS_URL constants left unreferenced.

    **Cache-boundary design (checker W-4 — decide from doc + RED-test evidence, not assumption):** the resilientFetch call replaces the raw fetch IN PLACE, inside the `unstable_cache` callback. Per standard Next semantics, a callback that THROWS produces no cache entry and the error propagates synchronously in-process on a miss — prototype intact, so the outer `err instanceof CircuitOpenError` (leaf import) works, and nothing poisons the cache. VERIFY this holds in THIS fork: write the cache-miss-breaker-open test FIRST; if it reds because the fork's cache boundary wraps or serializes callback errors (check the doc you read), fall back to a name check (`err instanceof Error && err.name === "CircuitOpenError"`) at THIS outer catch only, with a comment citing the fork behavior — NEVER return the error as a value from the cached callback (it would be cached for 60s, outliving the 30s cooldown). Record which branch reality took in the summary.

    Add CircuitOpenError handling per the behavior block: permissions route catches it in its existing error-mapping region → 503 + Retry-After; finalize-wizard catches it around the probe call → fail-closed 503 CIRCUIT_OPEN envelope. Add `export const maxDuration = 300;` (invariant-comment convention) to BOTH route files.

    Extend both route.test.ts files: permissions gets THREE cases — cache-miss + breaker open (503/CIRCUIT_OPEN + Retry-After, error not cached: a SECOND call after clearing the breaker must re-attempt the fetch), cache-hit (fetch and redis never called), and existing success path unmodified; finalize-wizard gets the fail-closed CIRCUIT_OPEN case (finalize BLOCKED, 503). Tests import the REAL `CircuitOpenError` from `@/lib/seam-errors` and mock `@/lib/resilient-fetch` to reject with it (do not convert existing factories to importActual; the leaf import is what makes instanceof work).
  </action>
  <verify>
    <automated>npx vitest run "src/app/api/keys/[id]/permissions/route.test.ts" src/app/api/strategies/finalize-wizard/route.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "AbortSignal.timeout" src/app/api/keys/[id]/permissions/route.ts src/app/api/strategies/finalize-wizard/route.ts` = 0 each
    - `grep -c "resilientFetch"` ≥ 1 in each of the two route files, with budgetKey "keys-permissions"
    - `grep -c "export const maxDuration = 300"` = 1 in each
    - permissions test covers cache-MISS-breaker-open (503/CIRCUIT_OPEN, not cached) AND cache-HIT (no fetch, no redis) cases; finalize-wizard test asserts finalize is BLOCKED (fail-closed) on breaker open
    - Summary records whether instanceof survived the fork's cache boundary or the name-check fallback was needed
  </acceptance_criteria>
  <done>Third seam closed: both duplicated constants gone, both paths breaker-aware, cache boundary explicitly designed and tested, fail-closed semantics preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Pin maxDuration on the remaining Mechanism-B routes</name>
  <files>src/app/api/verify-strategy/route.ts, src/app/api/strategies/csv-validate/route.ts, src/app/api/strategies/csv-finalize/route.ts</files>
  <read_first>src/app/api/keys/sync/route.ts:50-60 (the existing `export const maxDuration = 300` — the ONLY seam route that already declares it; match placement/style), the three target route files (top-of-file export region)</read_first>
  <action>
    Add `export const maxDuration = 300;` with the invariant-comment convention ("pinned for clarity — asserted by seam-budgets.invariant.test against SEAM_ROUTE_BUDGETS"; value must match what 140-01 recorded/pinned per its W-2 verification) to verify-strategy, csv-validate, and csv-finalize. Tally check (checker W-6 correction): of the five Mechanism-B routes, ONLY keys/sync has the export today; finalize-wizard's is added by Task 1 of this plan; these three complete the set — after this task all five B routes export it. NO other changes to these files — their CIRCUIT_OPEN handling arrived for free via postProcessKey's envelope (140-02) and their existing `if (!result.ok) return result.response` passthrough. Do not add error-handling code here.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "export const maxDuration = 300" src/app/api/verify-strategy/route.ts src/app/api/strategies/csv-validate/route.ts src/app/api/strategies/csv-finalize/route.ts` = 3 hits total
    - `npm run build` green (malformed route-segment config is a build error)
    - git diff on the three files shows ONLY the export + comment (no behavior edits)
  </acceptance_criteria>
  <done>All 15 SEAM_ROUTE_BUDGETS routes now export maxDuration; platform assumption converted to checked in-repo fact.</done>
</task>

<task type="auto">
  <name>Task 3: SC-1a seam integration test — keys/sync through the REAL process-key-client</name>
  <files>src/app/api/keys/sync/route.seam.test.ts</files>
  <read_first>140-VALIDATION.md (SC-1a row + "must NOT vi.mock @/lib/process-key-client"), src/app/api/keys/sync/route.test.ts (copy its auth/supabase harness, DROP the client mock), src/lib/process-key-client.ts (envelope fields to assert), src/test/helpers/upstash-breaker.ts (usage contract in its header comment), src/test/helpers/fetch.ts</read_first>
  <action>
    Create `route.seam.test.ts` importing the keys/sync POST handler with the REAL `@/lib/process-key-client` and REAL `@/lib/resilient-fetch`. Fake `@upstash/redis` + `@upstash/ratelimit` by importing `createFakeUpstashStore`/`fakeRedisFor`/`fakeRatelimitFor` from `src/test/helpers/upstash-breaker.ts`, wired in THIS file's own `vi.hoisted` + `vi.mock` calls (the helper exists precisely because vi.mock factories are per-file); fake `fetch` via installFetchMock; INTERNAL_API_TOKEN env set. Two cases: (1) SC-1a — fetch rejects `new DOMException("aborted","TimeoutError")` → response 504, body `{ok:false, code:"UPSTREAM_TIMEOUT", recoverable:true}` with correlation_id present — the typed envelope, lambda not held; (2) breaker open — `seedBreakerOpen(store)` → 503, body.code "CIRCUIT_OPEN", Retry-After header, `expect(fetchMock).not.toHaveBeenCalled()`. Header comment: the ordinary route.test.ts mocks the client wholesale and proves nothing about SC-1 (research §10.4). Hygiene: `afterEach(() => { restoreFetchMock(); vi.unstubAllGlobals(); })`, env restored in afterEach.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/sync/route.seam.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'vi.mock("@/lib/process-key-client"' src/app/api/keys/sync/route.seam.test.ts` = 0
    - `grep -c "upstash-breaker" src/app/api/keys/sync/route.seam.test.ts` ≥ 1 (helper imported, not hand-rolled fakes)
    - Asserts 504/UPSTREAM_TIMEOUT on hang AND (503/CIRCUIT_OPEN + fetch-never-called) on open breaker
    - `grep -c "unstubAllGlobals" src/app/api/keys/sync/route.seam.test.ts` ≥ 1
    - `npx vitest run src/app/api/keys/sync/route.seam.test.ts` green
  </acceptance_criteria>
  <done>SC-1a proven end-to-end: the money-onboarding chokepoint fails fast with the typed envelope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authed user → keys/permissions + finalize-wizard | Internal auth headers cross to Railway; finalize gates strategy publication |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-21 | Information Disclosure | internal token in probe logs | mitigate | reuse scrubInternalToken (finalize-wizard :105-118) on any log line; core never logs headers |
| T-140-22 | Elevation of Privilege | finalize proceeding on probe failure | mitigate | fail-closed preserved: breaker open BLOCKS finalize with 503; test asserts blockage |
| T-140-23 | Spoofing | header drift through core rewiring | mitigate | init passed byte-for-byte; tests assert success paths unmodified |
| T-140-24 | Information Disclosure | breaker-state oracle above withAuth | mitigate | resilientFetch invoked inside handlers after auth; no gate lines in the diff (route-level oracle test lives in 140-03 Task 3) |
| T-140-32 | Denial of Service | breaker error cached by unstable_cache for 60s, outliving the 30s cooldown | mitigate | error thrown (never returned) from the cached callback; not-cached regression asserted in the cache-miss test |
| T-140-SC | Tampering | package installs | accept | zero installs |
</threat_model>

<verification>
- `npx vitest run "src/app/api/keys/[id]/permissions/route.test.ts" src/app/api/strategies/finalize-wizard/route.test.ts src/app/api/keys/sync/route.seam.test.ts --no-file-parallelism` green
- `npm run build` + `npm run typecheck` green
- `grep -rn "ANALYTICS_SERVICE_URL" src | grep -v resilient-fetch | grep -v warm | grep -v debug-key-flow | grep -v test | grep -v correlation-id | grep -v usage-events` returns ONLY comment/doc hits, zero fetch call sites
</verification>

<success_criteria>
- SEAM-01 complete at the call-site level: every live Railway fetch flows through the core or is a documented SEAM_EXCLUSIONS row
- SEAM-02: 15/15 routes export maxDuration matching SEAM_ROUTE_BUDGETS
- SC-1a green with the REAL client — end-to-end proof on the money path
- Cache boundary behavior recorded (instanceof vs name-check fallback) in the summary
</success_criteria>

<output>
Create `.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-06-SUMMARY.md` when done.
</output>
