---
phase: 140-seam-shared-resilience-core-circuit-breaker
plan: 07
type: execute
wave: 4
depends_on: [140-03, 140-04, 140-05, 140-06]
files_modified:
  - src/lib/seam-budgets.invariant.test.ts
  - tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs
  - tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts
  - tools/eslint-plugin-quantalyze/index.mjs
  - eslint.config.mjs
autonomous: true
requirements: [SEAM-01, SEAM-02]

must_haves:
  truths:
    - "CI fails if any seam route's maxDuration export is missing, edited, or violates Σ(timeoutMs × calls × (1 + SEAM_RETRIES)) < maxDuration × 1000 — SC-4"
    - "A raw fetch of ANALYTICS_SERVICE_URL outside the allowlist fails npm run lint — SEAM-01 holds after merge, not just at merge (operator decision 4)"
    - "The invariant test reads maxDuration from route files ON DISK — it cannot pass by re-reading the table against itself (research Pitfall 5)"
  artifacts:
    - path: "src/lib/seam-budgets.invariant.test.ts"
      provides: "SC-4a + SC-4b source-scan invariant"
      contains: "readFileSync"
    - path: "tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs"
      provides: "structural ONE-core enforcement"
  key_links:
    - from: "src/lib/seam-budgets.invariant.test.ts"
      to: "SEAM_ROUTE_BUDGETS + route files on disk"
      via: "readFileSync + maxDuration regex per routePath"
      pattern: "readFileSync"
    - from: "eslint.config.mjs"
      to: "quantalyze/no-raw-analytics-fetch"
      via: "rule registration + per-path off blocks for the 4 allowlisted files"
      pattern: "no-raw-analytics-fetch"
---

<objective>
Make Phase 140 permanent: the SC-4 budget invariant (source-scan, fails loud on missing or
mismatched maxDuration) and the `no-raw-analytics-fetch` ESLint rule (the only mechanism
that keeps SEAM-01 true after merge — the third seam's existence is direct evidence that
convention alone failed). Then run the full phase gate.

Output: CI-enforced budget headroom + structurally-enforced ONE core; whole suite + build green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-RESEARCH.md
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-VALIDATION.md
@src/lib/resilient-fetch.ts

<interfaces>
From @/lib/resilient-fetch: `SEAM_ROUTE_BUDGETS` (15 routePaths → {expectedMaxDurationS, budgets:[{key, calls}]}), `SEAM_BUDGETS` (timeoutMs per key), `SEAM_RETRIES` (0 today; Phase 141 raises it — the invariant must keep holding automatically), `SEAM_EXCLUSIONS`.
Precedents to copy:
- Source-scan test: `src/app/scenario-share/[token]/page-server-boundary.test.ts` (readFileSync + structural assertion + the honest "CEILING" comment about what it does NOT cover)
- ESLint rule + RuleTester: `tools/eslint-plugin-quantalyze/rules/no-raw-retry-after-parse.mjs` + `tools/eslint-plugin-quantalyze/tests/no-raw-retry-after-parse.test.ts`
- Per-path rule-off blocks: `eslint.config.mjs:249-257`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seam-budgets.invariant.test.ts — SC-4a + SC-4b</name>
  <files>src/lib/seam-budgets.invariant.test.ts</files>
  <read_first>src/app/scenario-share/[token]/page-server-boundary.test.ts (the source-scan + CEILING-comment convention), src/lib/resilient-fetch.ts (table shapes), 140-RESEARCH.md §6.3 + Pitfall 5 + Pitfall 8</read_first>
  <action>
    Create the invariant test in `src/lib/` (in the default vitest include, counts toward the coverage ratchet). For EVERY key of `SEAM_ROUTE_BUDGETS`:
    - SC-4a: `readFileSync(routePath)` and extract the export with a multiline regex anchored on the statement (`/^export const maxDuration = (\d+)/m` — anchoring is the grep-gate hygiene; comments cannot match an `export const` statement). FAIL LOUD with a named-route message if the export is MISSING or ≠ `expectedMaxDurationS` (Pitfall 8: a hardcoded-300-fallback for un-annotated routes is the forbidden anti-pattern — no fallback).
    - SC-4b: assert `sum(budgets.map(b => SEAM_BUDGETS[b.key].timeoutMs * b.calls)) * (1 + SEAM_RETRIES) < expectedMaxDurationS * 1000` per route. The SUM across a route's budget refs is what captures the two-sequential-call routes and finalize-wizard's probe+enqueue pair — a per-entry assertion would be wrong for a third of Mechanism A (research §6.3 callsPerRequest note).
    Also assert structural completeness: `SEAM_EXCLUSIONS` has ≥ 3 entries and every exclusion path exists on disk (an exclusion pointing at a deleted file is stale documentation). Add the CEILING honesty comment: this test reads ONLY the listed route files, not the transitive import graph; a new route calling the clients is caught by the ESLint rule (Task 2), not by this test. The `timeoutMs` side comes from the table and the `maxDuration` side from DISK — never both from the table (Pitfall 5).
  </action>
  <verify>
    <automated>npx vitest run src/lib/seam-budgets.invariant.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "readFileSync" src/lib/seam-budgets.invariant.test.ts` ≥ 1
    - Test green against the current tree; MUTATION CHECK (run, then revert): temporarily change `keys/sync/route.ts` maxDuration to 299 → test FAILS naming the route; restore → green. Record the mutation check output in the summary
    - `grep -c "SEAM_RETRIES" src/lib/seam-budgets.invariant.test.ts` ≥ 1 (retry-aware formula, Phase 141-proof)
    - No fallback default for missing maxDuration anywhere in the test
  </acceptance_criteria>
  <done>SC-4 CI-enforced: 15 routes scanned from disk, summed headroom formula holds with retries=0 and survives Phase 141 raising SEAM_RETRIES.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: no-raw-analytics-fetch ESLint rule + registration</name>
  <files>tools/eslint-plugin-quantalyze/rules/no-raw-analytics-fetch.mjs, tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts, tools/eslint-plugin-quantalyze/index.mjs, eslint.config.mjs</files>
  <read_first>tools/eslint-plugin-quantalyze/rules/no-raw-retry-after-parse.mjs (rule anatomy: meta, create, messageId), tools/eslint-plugin-quantalyze/tests/no-raw-retry-after-parse.test.ts (RuleTester harness), tools/eslint-plugin-quantalyze/index.mjs (rule registry), eslint.config.mjs:40-70 (registration) and :249-257 (per-path off-block pattern)</read_first>
  <behavior>
    - REG-2 invalid: `fetch(\`${ANALYTICS_URL}/api/x\`)` where ANALYTICS_URL derives from `process.env.ANALYTICS_SERVICE_URL`; `fetch(process.env.ANALYTICS_SERVICE_URL + "/p")`; a template literal referencing an identifier initialized from ANALYTICS_SERVICE_URL
    - Valid: `resilientFetch("bridge", "/api/x", init)`; fetch of unrelated URLs; the string "ANALYTICS_SERVICE_URL" in comments
  </behavior>
  <action>
    Write the rule following `no-raw-retry-after-parse.mjs` (meta/create/messageId anatomy). Detection spec (checker W-8 — INIT-TRACKING, not name-matching; a name-match fails behavior fixture 3): (a) track VariableDeclarators whose initializer references `process.env.ANALYTICS_SERVICE_URL` anywhere in the init expression (including `??`/`||` fallback chains), recording each declared identifier name in module scope — regardless of what the identifier is called; (b) report CallExpressions whose callee is `fetch` and whose first argument references `process.env.ANALYTICS_SERVICE_URL` directly OR one of the tracked identifiers — as a bare Identifier argument, inside a TemplateLiteral expression slot, or in a BinaryExpression concatenation. The message directs to `resilientFetch` in `@/lib/resilient-fetch`. Register in `index.mjs` and in `eslint.config.mjs` as `"quantalyze/no-raw-analytics-fetch": "error"` for src/**, with per-path OFF blocks (the :249-257 pattern) for the four allowlisted files: `src/lib/resilient-fetch.ts` (the core itself), `src/app/api/debug-key-flow/**` (documented exclusion — bespoke SSE), `src/app/api/cron/warm-analytics/**` and `src/lib/warmup-analytics.ts` (health warmers, must not consume breaker budget). Write the RuleTester test with the valid/invalid fixtures above (the tests dir is in the root vitest include). Then run `npm run lint` on the full tree — it must pass, which PROVES no raw seam fetch survived waves 1-3; if it flags a file not in the allowlist, that file is a missed seam call site: fix the call site (route through the core), never widen the allowlist.
  </action>
  <verify>
    <automated>npx vitest run tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - RuleTester test green with ≥ 3 invalid and ≥ 2 valid fixtures
    - `npm run lint` green on the whole tree
    - `grep -c "no-raw-analytics-fetch" eslint.config.mjs` ≥ 2 (error registration + off blocks) and ≥ 1 in index.mjs
    - Allowlist contains EXACTLY the four paths above — no route files
  </acceptance_criteria>
  <done>SEAM-01 structurally enforced: reviving the dormant handler raw, or adding a tenth seam, fails lint.</done>
</task>

<task type="auto">
  <name>Task 3: Full phase gate</name>
  <files>(none — verification only)</files>
  <read_first>140-VALIDATION.md (sampling-rate section: the phase-gate command set)</read_first>
  <action>
    Run the complete Phase 140 gate per 140-VALIDATION.md: `npm test` (full suite — includes all five Wave-0 files, the 18 untouched mock-based route tests, the extended route tests, and the RuleTester test), `npm run typecheck`, `npm run lint`, `npm run test:coverage` (ratchet: lines 82 / statements 80 / functions 74 / branches 72 — the new lib code must not sink the ratchet), and `npm run build` (maxDuration is route-segment config; a malformed export is a build error). If a vitest failure appears that did not reproduce earlier, reproduce with `PATH=/opt/homebrew/opt/node@22/bin/node:$PATH` before treating it as a flake — leaked fetch stubs are the known Node-22-only cause. Fix anything red; nothing is skipped silently.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` fully green; `npm run test:coverage` meets the ratchet thresholds
    - `npm run build` green
    - Every SC row of 140-VALIDATION.md's Per-Task Verification Map runs green via its listed command; mark statuses in the summary
  </acceptance_criteria>
  <done>Phase gate green: all 5 success criteria automated-proven, ready for /gsd:verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer/PR → CI | The invariants themselves are the control; their integrity is the asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-25 | Tampering | invariant self-referencing (green forever) | mitigate | maxDuration read from DISK, timeoutMs from table; mutation check in acceptance proves it can fail |
| T-140-26 | Tampering | allowlist widening to silence lint | mitigate | allowlist frozen at 4 paths in acceptance; rule message directs to the core, task text forbids widening |
| T-140-27 | Denial of Service | future dashboard maxDuration change invalidating budgets silently | mitigate | explicit exports + disk-read invariant convert the platform assumption into a CI-checked fact (Pitfall 8) |
| T-140-SC | Tampering | package installs | accept | zero installs |
</threat_model>

<verification>
- `npx vitest run src/lib/seam-budgets.invariant.test.ts tools/eslint-plugin-quantalyze/tests/no-raw-analytics-fetch.test.ts --no-file-parallelism` green
- `npm test && npm run typecheck && npm run lint && npm run test:coverage && npm run build` all green
</verification>

<success_criteria>
- SC-4 (SEAM-02) fully automated: missing/mismatched maxDuration or budget-headroom violation fails CI, retry-aware for Phase 141
- SEAM-01 holds over time: raw seam fetch outside the 4-path allowlist fails `npm run lint`
- Phase gate green — all five ROADMAP success criteria have passing automated proofs
</success_criteria>

<output>
Create `.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-07-SUMMARY.md` when done.
</output>
