---
phase: 140-seam-shared-resilience-core-circuit-breaker
plan: 05
type: execute
wave: 3
depends_on: [140-02]
files_modified:
  - src/app/api/bridge/route.ts
  - src/app/api/bridge/route.test.ts
  - src/app/api/simulator/route.ts
  - src/app/api/simulator/route.test.ts
  - src/app/api/scenario/optimize/route.ts
  - src/app/api/scenario/optimize/route.test.ts
  - src/app/api/portfolio-optimizer/route.ts
  - src/app/api/portfolio-optimizer/route.test.ts
  - src/app/api/keys/validate-and-encrypt/route.ts
  - src/app/api/keys/validate-and-encrypt/route.test.ts
autonomous: true
requirements: [SEAM-02, SEAM-04]

must_haves:
  truths:
    - "All five Class-3 routes return 503 + Retry-After on CircuitOpenError instead of falling to their generic arm — SC-5c"
    - "portfolio-optimizer refunds its rate-limit token on the CIRCUIT_OPEN arm (consistent with its documented R-0002 upstream-failure refunds)"
    - "The dormant _unifiedValidateAndEncryptHandler fetch (the only timeout-less Railway call) is routed through the core — revival inherits budget + breaker automatically"
  artifacts:
    - path: "src/app/api/portfolio-optimizer/route.ts"
      provides: "CIRCUIT_OPEN arm + refund + table-sourced timeout + maxDuration"
      contains: "export const maxDuration = 300"
    - path: "src/app/api/scenario/optimize/route.test.ts"
      provides: "NEW route test (file did not exist) — CIRCUIT_OPEN + non-leaking generic arm"
    - path: "src/app/api/keys/validate-and-encrypt/route.ts"
      provides: "CIRCUIT_OPEN arm; dormant fetch through resilientFetch; maxDuration"
      contains: "resilientFetch"
  key_links:
    - from: "each Class-3 route"
      to: "CircuitOpenError from @/lib/seam-errors"
      via: "instanceof branch BEFORE the generic arm (leaf import — never through the mocked client module)"
      pattern: "instanceof CircuitOpenError"
---

<objective>
Retrofit the five Class-3 routes (bridge, simulator, scenario/optimize, portfolio-optimizer,
keys/validate-and-encrypt): each already maps AnalyticsUpstreamError/AnalyticsTimeoutError
but has NO CIRCUIT_OPEN arm, so a breaker trip falls to the generic 500/502 arm with the
wrong status and no Retry-After. Insert the typed 503 arm, pin maxDuration on all five, wire
portfolio-optimizer's timeout constant to the table, and route the dormant timeout-less
`/process-key` fetch through the core (operator decision 5).

Output: SC-5c green across all five routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-RESEARCH.md
@src/lib/resilient-fetch.ts
@src/lib/seam-errors.ts
@src/lib/analytics-client.ts

<interfaces>
From @/lib/seam-errors (the dependency-free leaf, 140-01): `CircuitOpenError` (`.retryAfterS`).
ALL route imports and ALL test imports of CircuitOpenError use `@/lib/seam-errors` — NEVER
`@/lib/analytics-client`. Checker-verified: bridge, simulator, and validate-and-encrypt
route tests are FULL `vi.mock("@/lib/analytics-client")` factories with hand-written class
shims (zero importActual). An instanceof against a class re-exported through that mocked
module would be `undefined` → `TypeError` inside the catch block. The leaf is never mocked,
so instanceof works under every existing factory shape with NO changes to the existing shims.

From @/lib/analytics-client (after 140-02): wrappers throw `CircuitOpenError` (leaf class);
`AnalyticsUpstreamError`/`AnalyticsTimeoutError` unchanged.
From @/lib/resilient-fetch: `SEAM_BUDGETS["portfolio-optimizer"].timeoutMs` (15_000) — the
route's `OPTIMIZER_TIMEOUT_MS` local constant becomes a read of this;
`resilientFetch("process-key-unified-dormant", "/process-key", init)` for the dormant handler.
Existing arm shapes (research §5.2 Class 3): each route branches `AnalyticsUpstreamError`
(forward 4xx) → `AnalyticsTimeoutError` (504) → static generic. Insert the CircuitOpenError
branch BEFORE the generic arm; per-route body shapes differ (`{error}` vs `{code}`) — match
each route's existing shape, static copy only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: bridge + simulator + scenario/optimize routes — CIRCUIT_OPEN arm + maxDuration</name>
  <files>src/app/api/bridge/route.ts, src/app/api/bridge/route.test.ts, src/app/api/simulator/route.ts, src/app/api/simulator/route.test.ts, src/app/api/scenario/optimize/route.ts</files>
  <read_first>src/app/api/bridge/route.ts:119-153 (H-1062 non-leaking arm precedent), src/app/api/simulator/route.ts:148-177, src/app/api/scenario/optimize/route.ts:127-145, src/app/api/bridge/route.test.ts (FULL mock factory with hand-written error-class shims — leave the shims untouched), src/app/api/simulator/route.test.ts (same shape)</read_first>
  <behavior>
    - Each route: wrapper rejecting with CircuitOpenError → 503, existing body shape with static copy, header Retry-After = String(retryAfterS) — SC-5c
    - Existing arms (upstream 4xx forward, timeout 504, generic static) byte-identical in behavior — pre-existing tests pass unmodified
  </behavior>
  <action>
    In each of the three routes, insert `err instanceof CircuitOpenError` — imported from `@/lib/seam-errors`, NOT from `@/lib/analytics-client` (the client module is fully mocked in these routes' tests; the leaf is never mocked) — immediately BEFORE the generic arm → 503 + Retry-After + static copy in the route's own body shape. Add `export const maxDuration = 300;` with the invariant-comment convention to all three route files. Extend bridge and simulator route tests with the SC-5c case: import the REAL `CircuitOpenError` from `@/lib/seam-errors` at the top of each test file and make the existing mocked wrapper (`findReplacementCandidates` / `simulateAddCandidate`) reject with `new CircuitOpenError(30)`; do NOT modify the existing hand-written error-class shims or convert the factories to importActual. scenario/optimize gets its route change here; its NEW test file is Task 2.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/bridge/route.test.ts src/app/api/simulator/route.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "instanceof CircuitOpenError" src/app/api/bridge/route.ts src/app/api/simulator/route.ts src/app/api/scenario/optimize/route.ts` = 1 each
    - `grep -c "from \"@/lib/seam-errors\""` ≥ 1 in each of the three route files; `grep -cE "import.*CircuitOpenError.*analytics-client"` = 0 in all three
    - `grep -c "export const maxDuration = 300"` = 1 in each of the three route files
    - bridge + simulator tests assert 503 AND the Retry-After header for the breaker case; pre-existing cases unmodified and green
  </acceptance_criteria>
  <done>Three routes typed for CIRCUIT_OPEN via the leaf import; bridge/simulator tests extended without touching their mock factories.</done>
</task>

<task type="auto">
  <name>Task 2: Create scenario/optimize/route.test.ts (new file — Wave-0 obligation)</name>
  <files>src/app/api/scenario/optimize/route.test.ts</files>
  <read_first>src/app/api/scenario/optimize/route.ts (full — auth gate, request shape, all error arms including the new CIRCUIT_OPEN one), src/app/api/bridge/route.test.ts (harness template: auth/supabase mocks, request construction), src/test/helpers/fetch.ts</read_first>
  <action>
    CREATE `src/app/api/scenario/optimize/route.test.ts` — confirmed absent today; this route has zero test coverage. Use bridge/route.test.ts as the harness template, and copy its `@/lib/analytics-client` factory **INCLUDING its hand-written `AnalyticsTimeoutError` / `AnalyticsUpstreamError` class shims** — this route imports BOTH from analytics-client (`route.ts:5-9`) and branches on both (`:128`, `:134`), so a bare factory leaves those bindings `undefined` and the FIRST `instanceof` in the catch throws `TypeError`, reddening all four cases. Add the mocked `optimizeScenarioWeights` to that shim-carrying factory; mock auth/supabase the way bridge's test does. `CircuitOpenError` still comes from the never-mocked `@/lib/seam-errors`, never from the factory. Cases, minimum: (1) CIRCUIT_OPEN — mocked wrapper rejects with the REAL `CircuitOpenError` imported from `@/lib/seam-errors` → 503 + Retry-After header; (2) AnalyticsTimeoutError → the route's existing timeout status (read it from the route's arm — do not guess); (3) generic non-leak — wrapper rejects `new Error("boom-internal-detail")` → response body does NOT contain "boom-internal-detail"; (4) one happy-path case pinning the success shape. `afterEach(() => vi.unstubAllGlobals())`.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/scenario/optimize/route.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - File exists and is green: `npx vitest run src/app/api/scenario/optimize/route.test.ts`
    - Asserts 503 + Retry-After for CircuitOpenError, the timeout arm status, and the generic-arm non-leak (message text absent from body)
    - `grep -c "unstubAllGlobals" src/app/api/scenario/optimize/route.test.ts` ≥ 1
    - CircuitOpenError imported from `@/lib/seam-errors` (grep = 1), not from analytics-client
  </acceptance_criteria>
  <done>scenario/optimize has its first route test, covering breaker, timeout, non-leak, and happy path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: portfolio-optimizer (refund + table wiring) + validate-and-encrypt (dormant fetch through core)</name>
  <files>src/app/api/portfolio-optimizer/route.ts, src/app/api/portfolio-optimizer/route.test.ts, src/app/api/keys/validate-and-encrypt/route.ts, src/app/api/keys/validate-and-encrypt/route.test.ts</files>
  <read_first>src/app/api/portfolio-optimizer/route.ts:10-20 (OPTIMIZER_TIMEOUT_MS), :123 (pass site), :127-158 (refund arms + M-0333 static 503), src/app/api/keys/validate-and-encrypt/route.ts:120-165 (the dormant `_unifiedValidateAndEncryptHandler` + its timeout-less fetch at :158) and :221-246 (live error arms), both route.test.ts files (both are FULL mock factories — leave their shims untouched)</read_first>
  <behavior>
    - portfolio-optimizer: CircuitOpenError → 503 + Retry-After + static copy AND `refundRateLimitToken()` is invoked on that arm (spy-asserted) — consistent with its R-0002 refunds at :127/:152
    - validate-and-encrypt (live handler): CircuitOpenError from validateKey/encryptKey → 503 + Retry-After + static copy in its existing body shape
    - Existing arms unchanged; existing tests pass unmodified
  </behavior>
  <action>
    portfolio-optimizer: insert the CircuitOpenError arm (import from `@/lib/seam-errors`) before the generic arm, calling `refundRateLimitToken()` first (mirror the two existing refund arms; comment cites R-0002 consistency). Replace the local `OPTIMIZER_TIMEOUT_MS = 15_000` with a read of `SEAM_BUDGETS["portfolio-optimizer"].timeoutMs` (single source — SEAM-02's stated purpose; the wrapper still accepts it as timeoutMsOverride, values identical). Add `export const maxDuration = 300;`. Extend route.test.ts: CIRCUIT_OPEN case (mocked wrapper rejects with the real leaf-imported CircuitOpenError) asserting status, Retry-After, AND the refund spy.

    validate-and-encrypt: insert the CircuitOpenError arm (leaf import) in the LIVE handler's error mapping (503 + Retry-After, static copy). In the dormant `_unifiedValidateAndEncryptHandler`, replace the raw timeout-less `fetch(`${ANALYTICS_URL}/process-key`, ...)` at :158 with `resilientFetch("process-key-unified-dormant", "/process-key", init)` preserving its headers/body verbatim; delete the now-unused local ANALYTICS_URL constant if nothing else reads it. Do NOT delete the handler itself (operator decision 5 — deletion needs a separate decision; keep its dormant-status comment). Add `export const maxDuration = 300;`. Extend route.test.ts with the live-handler CIRCUIT_OPEN case (real leaf-imported class through the existing full-mock factory).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/portfolio-optimizer/route.test.ts src/app/api/keys/validate-and-encrypt/route.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - portfolio-optimizer test asserts refundRateLimitToken called on the CIRCUIT_OPEN arm
    - `grep -c "OPTIMIZER_TIMEOUT_MS = 15_000" src/app/api/portfolio-optimizer/route.ts` = 0 (table-sourced now)
    - `grep -cE "fetch\(.*ANALYTICS" src/app/api/keys/validate-and-encrypt/route.ts` = 0 (dormant path through core); `grep -c "_unifiedValidateAndEncryptHandler" src/app/api/keys/validate-and-encrypt/route.ts` ≥ 1 (NOT deleted)
    - `grep -c "export const maxDuration = 300"` = 1 in both route files; CircuitOpenError imported from `@/lib/seam-errors` in both
    - Both test files green; `npm run typecheck` green
  </acceptance_criteria>
  <done>All five Class-3 routes typed; the last timeout-less Railway fetch is gone; optimizer budget single-sourced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authed users → compute routes | bridge/simulator/optimizer/scenario compute + key material on validate-and-encrypt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-17 | Information Disclosure | breaker error via generic arms | mitigate | typed 503 arm with static copy inserted before every generic arm; existing H-1062/M-0333 non-leak arms untouched |
| T-140-18 | Denial of Service | optimizer token not refunded on breaker trip | mitigate | refund on CIRCUIT_OPEN arm, spy-asserted (R-0002 consistency) |
| T-140-19 | Elevation/Tampering | dormant handler revived without timeout/breaker | mitigate | dormant fetch routed through core NOW; budget key process-key-unified-dormant; Wave-4 ESLint rule backstops |
| T-140-20 | Information Disclosure | breaker check above auth | mitigate | no auth-gate lines touched; arms live inside handlers after auth (diff-scoped acceptance) |
| T-140-30 | Denial of Service | instanceof TypeError under fully-mocked client (B-2) | mitigate | CircuitOpenError imported from the never-mocked leaf in routes AND tests; existing mock shims untouched |
| T-140-SC | Tampering | package installs | accept | zero installs |
</threat_model>

<verification>
- `npx vitest run src/app/api/bridge/route.test.ts src/app/api/simulator/route.test.ts src/app/api/scenario/optimize/route.test.ts src/app/api/portfolio-optimizer/route.test.ts src/app/api/keys/validate-and-encrypt/route.test.ts --no-file-parallelism` green
- `npm run typecheck` + `npm run build` green
</verification>

<success_criteria>
- SEAM-04 Class-3 closed: 5/5 routes return typed 503 + Retry-After on CircuitOpenError (SC-5c)
- SEAM-02: five maxDuration=300 exports + optimizer budget read from the ONE table
- Zero raw ANALYTICS_URL fetches remain in Mechanism A/B route files
</success_criteria>

<output>
Create `.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-05-SUMMARY.md` when done.
</output>
