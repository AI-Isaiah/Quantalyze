---
phase: 140-seam-shared-resilience-core-circuit-breaker
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seam-errors.ts
  - src/lib/resilient-fetch.ts
  - src/lib/resilient-fetch.test.ts
  - src/test/helpers/upstash-breaker.ts
autonomous: true
requirements: [SEAM-02, SEAM-03]

must_haves:
  truths:
    - "After 5 seam failures within 30s, a caller in a FRESH module context (vi.resetModules) gets CircuitOpenError without fetch ever being called"
    - "When redis.get throws OR Upstash env vars are absent (VERCEL_ENV=production included), the seam call still attempts the real fetch — breaker fails OPEN everywhere"
    - "HTTP 4xx responses never trip the breaker; 5xx, timeout, and network throws do"
    - "SEAM_BUDGETS.bridge.timeoutMs (15000) demonstrably reaches AbortSignal.timeout"
    - "CircuitOpenError lives in a dependency-free leaf importable from client-bundle-reachable code (wizardErrors → 10 'use client' components) without dragging in @upstash/* or any module side effect"
  artifacts:
    - path: "src/lib/seam-errors.ts"
      provides: "CircuitOpenError leaf — ZERO imports, zero side effects, safe in any bundle; never vi.mocked so instanceof survives every mock shape"
      exports: ["CircuitOpenError"]
    - path: "src/lib/resilient-fetch.ts"
      provides: "SEAM_BUDGETS, SEAM_ROUTE_BUDGETS, SEAM_EXCLUSIONS, SEAM_RETRIES, resilientFetch(), breaker constants; re-exports CircuitOpenError"
      exports: ["resilientFetch", "CircuitOpenError", "SEAM_BUDGETS", "SEAM_ROUTE_BUDGETS", "SEAM_EXCLUSIONS", "SEAM_RETRIES", "BREAKER_FAILURE_THRESHOLD", "BREAKER_COOLDOWN_S"]
    - path: "src/lib/resilient-fetch.test.ts"
      provides: "SC-2, SC-2-neg, SC-3a/b/c, SC-4c coverage"
    - path: "src/test/helpers/upstash-breaker.ts"
      provides: "importable fake-Upstash doubles + seedBreakerOpen() for the 140-03/140-06 seam tests (vi.mock factories are per-file — a reference to this file's factories is not reusable; an exported helper is)"
      exports: ["createFakeUpstashStore", "fakeRedisFor", "fakeRatelimitFor", "seedBreakerOpen"]
  key_links:
    - from: "src/lib/resilient-fetch.ts"
      to: "@upstash/redis + @upstash/ratelimit"
      via: "separate Redis.fromEnv() singleton (NOT imported from ratelimit.ts)"
      pattern: "Redis\\.fromEnv|slidingWindow"
    - from: "src/lib/resilient-fetch.ts"
      to: "src/lib/seam-errors.ts"
      via: "re-export of CircuitOpenError (single definition, leaf-homed)"
      pattern: "from \"./seam-errors\""
    - from: "src/lib/resilient-fetch.test.ts"
      to: "vi.hoisted shared fake store"
      via: "store survives vi.resetModules — the mechanism that distinguishes shared-store from in-memory"
      pattern: "vi\\.hoisted"
---

<objective>
Build the ONE shared resilience core: exported per-call-site timeout budget table
(SEAM-02) and the Upstash-backed `breaker:railway` circuit breaker that fails OPEN
on every store failure mode (SEAM-03). `CircuitOpenError` is homed in a dependency-free
leaf (`seam-errors.ts`) so client-bundle-reachable code (wizardErrors → 10 "use client"
components) and fully-mocked route tests can both branch on it safely. No client or
route is adapted yet.

Purpose: Phase 140 goal — a hung/dying Railway fails fast at both seam chokepoints.
Output: leaf + core + green unit contract + the shared test helper 140-03/140-06 import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-RESEARCH.md
@.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-VALIDATION.md
@.planning/ROADMAP.md
@src/lib/ratelimit.ts
@src/lib/analytics-client.ts
@src/lib/process-key-client.ts

<interfaces>
Verified Upstash primitives (installed typings, research §7.1 — do NOT re-verify):
- `redis.set("breaker:railway", "open", { ex: COOLDOWN_S, nx: true })` — ex+nx compose; nx makes concurrent trips idempotent (first writer's TTL stands)
- `redis.get<string>(key)` → `string | null`
- `redis.ttl(key)` → seconds, `-2` if absent
- `new Ratelimit({ redis, limiter: Ratelimit.slidingWindow(N, "30 s"), prefix, analytics: false })`; `.limit(id)` → `{ success, reset, ... }` — `success === false` means "over N in window" → trip
- TTL expiry IS the half-open transition. No explicit half-open state, no probe scheduler, no state machine (LOCKED decision — say so in the module docstring so reviewers don't flag "missing half-open" as a defect).

Existing clients' current timeout mechanics the core replaces:
- `analytics-client.ts:90` — `signal: AbortSignal.timeout(timeoutMs)`, timeout resolution `options?.timeoutMs ?? 30_000`
- `process-key-client.ts:132` — `AbortSignal.timeout(60_000)` hardcoded, CT-7 rationale comment at :101-107

Bundle-boundary constraint (checker-verified, BLOCKER B-1): `src/lib/wizardErrors.ts` has ZERO
server-only imports today and is value-imported by 10 `"use client"` components
(MetadataStep, CsvPreviewStep, MultiKeyConnectStep, SyncPreviewStep, CsvSubmitStep,
ConnectKeyStep, SubmitStep, CsvUploadStep, CsvValidationEnvelope, StrategyGrid). Anything
wizardErrors imports must be browser-safe. The repo convention for the inverse guard is
`src/lib/analytics.ts:1` (`import "server-only"`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seam-errors leaf + budget table + breaker constants + platform-default verification</name>
  <files>src/lib/seam-errors.ts, src/lib/resilient-fetch.ts</files>
  <read_first>src/lib/ratelimit.ts (the named-exported-constants-with-rationale-comments convention at :94-214), src/lib/analytics.ts:1 (the server-only boundary convention the leaf inverts), src/lib/analytics-client.ts (current timeouts + error classes), src/lib/process-key-client.ts (CT-7 comment), 140-RESEARCH.md §4 (call-site inventory), §6.3 (maxDuration recommendation)</read_first>
  <action>
    FIRST create `src/lib/seam-errors.ts` — a dependency-free LEAF: `export class CircuitOpenError extends Error` with `readonly retryAfterS: number`, `name = "CircuitOpenError"`, and a STATIC message ("Analytics service circuit is open") — no URLs, headers, or upstream detail. The file has ZERO import statements, zero `process.env` references, zero side effects. Header comment must state WHY it is a leaf: (1) wizardErrors — value-imported by 10 "use client" components — must branch on this class; homing it in analytics-client/resilient-fetch would pull `@upstash/*` plus resilient-fetch's non-tree-shakeable module-load side effects into the client bundle (cf. the `src/lib/analytics.ts:1` server-only convention); (2) route tests `vi.mock("@/lib/analytics-client")` with FULL factories (7 of 8 do NOT use importActual — checker-verified) — an `instanceof` against a class re-exported through a mocked module is `undefined` and throws `TypeError` inside catch blocks; this leaf is never mocked, so `instanceof CircuitOpenError` survives every existing mock shape. ALL production and test imports of CircuitOpenError use `@/lib/seam-errors`.

    THEN create `src/lib/resilient-fetch.ts` with the data/contract layer (functions come in Tasks 2-3). It re-exports `CircuitOpenError` from `./seam-errors` (single definition). Everything below is a NAMED EXPORT with a rationale comment — the `ratelimit.ts:94-214` convention; tuning must be a one-line change (operator decision 1).

    Constants: `BREAKER_KEY = "breaker:railway"` (module CONSTANT — comment: never interpolate user input; a user-influenced key is a cross-tenant DoS), `BREAKER_FAILURE_THRESHOLD = 5`, `BREAKER_WINDOW = "30 s"` (Upstash Duration string), `BREAKER_COOLDOWN_S = 30`, `DEFAULT_RETRY_AFTER_S = 30`, `SEAM_RETRIES = 0` (comment: Phase 141 raises this ONLY for calls allowlisted by the SEAM-05 idempotency audit; the SC-4b invariant `timeoutMs × calls × (1 + SEAM_RETRIES) < maxDuration × 1000` must be assertable today with retries 0).

    `export type SeamBudgetKey` union and `export const SEAM_BUDGETS: Record<SeamBudgetKey, { timeoutMs: number; notes: string }>` with EXACTLY these 13 entries (values are operator-decided — do not re-derive, do not invent a 14th):
    - `validate-key`: 30_000 — live exchange auth probe, venue-variable
    - `encrypt-key`: 30_000 — same
    - `bridge`: 15_000 — was hardcoded in analytics-client.ts:260
    - `simulator`: 15_000 — was hardcoded in analytics-client.ts:285
    - `portfolio-optimizer`: 15_000 — was OPTIMIZER_TIMEOUT_MS in the route
    - `optimize-weights`: 30_000 — scenario optimizer
    - `match-eval`: 30_000 — admin eval sweep
    - `match-recompute`: 30_000 — admin match engine
    - `portfolio-analytics`: 30_000 — notes MUST say: zero callers today (`computePortfolioAnalytics`, research §4.2); kept per operator decision 5, Phase 141 audit records "no callers"
    - `process-key-enqueue`: 15_000 — flow_type ∈ {resync, onboard}: server merely enqueues (verified `process_key.py:_is_long_fetch`); an enqueue taking 15s means Railway is sick (tightened from 60s per operator decision 2)
    - `process-key-sync`: 60_000 — flow_type ∈ {teaser, csv}: full 5-method pipeline runs inline; notes MUST say verbatim intent: "MEASURE BEFORE TIGHTENING — the only latency evidence is a code comment (~10-25s typical, process-key-client CT-7); never guess a budget on the public lead-generating teaser path" (operator decision 2 / research Pitfall 7 / assumption A2)
    - `keys-permissions`: 15_000 — third seam, replaces the two duplicated AbortSignal.timeout(15_000) constants (operator decision 3)
    - `process-key-unified-dormant`: 60_000 — the DEAD `_unifiedValidateAndEncryptHandler` fetch at keys/validate-and-encrypt/route.ts:158; routed through the core so any revival inherits a budget (operator decision 5)

    `export const SEAM_ROUTE_BUDGETS: Record<string, { expectedMaxDurationS: number; budgets: Array<{ key: SeamBudgetKey; calls: number }> }>` keyed by route file path relative to repo root. All 15 seam routes with `expectedMaxDurationS: 300` — SUBJECT to the platform-default verification below:
    - `src/app/api/keys/validate-and-encrypt/route.ts`: [{validate-key,1},{encrypt-key,1},{process-key-unified-dormant,1}]
    - `src/app/api/strategies/create-with-key/route.ts`: [{validate-key,1},{encrypt-key,1}]
    - `src/app/api/strategies/composite/add-key/route.ts`: [{validate-key,1},{encrypt-key,1}]
    - `src/app/api/bridge/route.ts`: [{bridge,1}]
    - `src/app/api/simulator/route.ts`: [{simulator,1}]
    - `src/app/api/portfolio-optimizer/route.ts`: [{portfolio-optimizer,1}]
    - `src/app/api/scenario/optimize/route.ts`: [{optimize-weights,1}]
    - `src/app/api/admin/match/eval/route.ts`: [{match-eval,1}]
    - `src/app/api/admin/match/recompute/route.ts`: [{match-recompute,1}]
    - `src/app/api/keys/sync/route.ts`: [{process-key-enqueue,1}]
    - `src/app/api/strategies/finalize-wizard/route.ts`: [{keys-permissions,1},{process-key-enqueue,1}]
    - `src/app/api/verify-strategy/route.ts`: [{process-key-sync,1}]
    - `src/app/api/strategies/csv-validate/route.ts`: [{process-key-sync,1}]
    - `src/app/api/strategies/csv-finalize/route.ts`: [{process-key-sync,1}]
    - `src/app/api/keys/[id]/permissions/route.ts`: [{keys-permissions,1}]
    Comment on the structure: the two-sequential-call routes are captured as TWO budget refs summed by the invariant test — a per-call assertion would be wrong for a third of Mechanism A (research §6.3 callsPerRequest note).

    **Platform-default verification (checker W-2 — a blind 300 pin on 14 routes could INCREASE worst-case lambda hold if the real default is lower, inverting the phase goal):** before finalizing `expectedMaxDurationS`, READ the project's actual effective default max duration from Vercel: `vercel api "v9/projects/$(node -p "require('./.vercel/project.json').projectId")"` and inspect the function-duration field (name may vary across API versions; fall back to `vercel project inspect` or the dashboard Functions settings if the API shape differs). Record the observed value verbatim in 140-01-SUMMARY.md. If it is 300 → proceed as specified. If it is LOWER → use the observed value as `expectedMaxDurationS` for the 14 currently-unpinned routes (keys/sync stays 300 — already shipped) PROVIDED every route's summed budget still satisfies SC-4b; if any route would violate (worst case: validate-and-encrypt at 120s summed), STOP and surface the conflict to the operator — budget re-derivation is an operator call, never silent. If the CLI read hits an auth wall, raise an auth-gate checkpoint dynamically (execute-plan convention) — do not guess and do not skip the recording.

    `export const SEAM_EXCLUSIONS: Record<string, string>` — explicit, reasoned exclusions (an unexplained absence is indistinguishable from an oversight, research §4.5):
    - `src/app/api/debug-key-flow/route.ts`: bespoke SSE/heartbeat with client-abort propagation (`AbortSignal.any`) the core does not model; own maxDuration=300 pin
    - `src/app/api/cron/warm-analytics/route.ts`: /health cold-start probe — must NOT consume breaker failure budget (probe failure is the normal case) and must NOT be blocked by an open breaker (a successful /health IS the recovery signal)
    - `src/lib/warmup-analytics.ts`: same reasoning, 10s fire-and-forget warmer

    Module docstring must cite the in-repo precedent `analytics-service/services/job_worker.py:731 _check_circuit_breaker` (breaker state in a shared store is this project's established pattern) and state the locked decisions: ONE core, ONE `breaker:railway` key, TTL-expiry-is-half-open, fail-OPEN divergence from ratelimit.ts's fail-CLOSED.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^import" src/lib/seam-errors.ts` = 0 (true leaf — zero imports); `grep -c "process.env" src/lib/seam-errors.ts` = 0
    - `grep -n "from \"./seam-errors\"" src/lib/resilient-fetch.ts` ≥ 1 (re-export, single definition)
    - `grep -c "export const" src/lib/resilient-fetch.ts` ≥ 8; `grep -n "breaker:railway" src/lib/resilient-fetch.ts` shows a const, never inside an interpolated template literal
    - SEAM_BUDGETS has exactly 13 keys; SEAM_ROUTE_BUDGETS exactly 15; SEAM_EXCLUSIONS exactly 3
    - `SEAM_BUDGETS["process-key-sync"].notes` contains "MEASURE BEFORE TIGHTENING"
    - 140-01-SUMMARY.md records the OBSERVED effective default maxDuration (value + how it was read); expectedMaxDurationS values are consistent with it per the W-2 rule above
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Leaf + contract layer compile; platform default recorded; every constant/table from operator decisions 1, 2, 3, 5 exists with the exact values above.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Breaker state functions + shared fake-Upstash test helper — fail OPEN on every store failure mode</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.test.ts, src/test/helpers/upstash-breaker.ts</files>
  <read_first>src/lib/ratelimit.ts:40-90 (Redis.fromEnv null-guard SHAPE only — its logging and env-branching are explicitly NOT copied, see action), 140-RESEARCH.md §7.3-7.4 and §10 (two-module-context recipe + vi.hoisted subtlety), src/lib/analytics-client.test.ts:33-60 (the resetModules + dynamic import precedent), src/test/helpers/fetch.ts (installFetchMock/restoreFetchMock)</read_first>
  <behavior>
    - SC-2 "cross-context": drive BREAKER_FAILURE_THRESHOLD failures in module context A → `vi.resetModules()` → import context B → the seam call rejects with CircuitOpenError AND `expect(fetchMock).not.toHaveBeenCalled()` (the assertion that proves "without touching Railway")
    - SC-2-neg "negative control": identical flow but with a per-factory (NON-hoisted, recreated) fake store → context B does NOT short-circuit and fetch IS called — proves the passing SC-2 test depends on shared state, not on the test never creating a second context
    - SC-3a "fails OPEN when Redis errors": fake `redis.get` throws → resilientFetch still calls fetch exactly once and resolves
    - SC-3b "fails OPEN when Upstash is unconfigured": `delete process.env.UPSTASH_REDIS_REST_URL/_TOKEN`, `VERCEL_ENV=production`, resetModules, fresh import → fetch still attempted. NO `isProduction()` branch may exist in the breaker path
  </behavior>
  <action>
    In `resilient-fetch.ts` add the breaker layer:
    - Module-scope `redis`: `Redis.fromEnv()` ONLY when both `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` are present, else `null`. Unconfigured-notice policy (checker W-1 resolution — this DIVERGES from `ratelimit.ts:57-79` deliberately; do NOT mirror it): a single UNCONDITIONAL `console.warn` at module load — never `console.error`, no `process.emitWarning`, no environment branch of any kind. Comment the double inversion explicitly — but WORD IT WITHOUT the literal tokens `isProduction`, `VERCEL_ENV` or `process.emitWarning`, because this task's own acceptance greps count them at zero (checker W-2: comment text must not collide with the grep gate). Say it as: "the limiter defers its notice inside a production-only branch and fails CLOSED in prod; the breaker warns once unconditionally and fails OPEN in ALL environments including production" (SEAM-03; a breaker that blocks traffic because its store is misconfigured is strictly worse than no breaker). Do NOT import anything from `ratelimit.ts` (it drags in NextResponse + 15 Ratelimit constructions — research §7.4). Construct a dedicated `Ratelimit` with `Ratelimit.slidingWindow(BREAKER_FAILURE_THRESHOLD, BREAKER_WINDOW)`, `analytics: false` (sidesteps the dangling `pending` promise hazard), and `prefix: "breaker"` (distinct from the limiter's "quantalyze" so keys never collide).
    - `isBreakerOpen(): Promise<{ open: boolean; retryAfterS?: number }>` — every exit path returns a value, nothing throws, no environment branch: null client → `{open:false}`; `redis.get(BREAKER_KEY) !== "open"` → `{open:false}`; open → `{open:true, retryAfterS: ttl > 0 ? ttl : DEFAULT_RETRY_AFTER_S}` via `redis.ttl`; any throw → `console.error("[resilient-fetch] breaker check failed — failing OPEN")` and `{open:false}` (runtime-failure logging stays console.error; only the load-time unconfigured notice is the single warn).
    - `recordSeamFailure(): Promise<void>` — null client → no-op; call `breakerLimiter.limit(BREAKER_KEY + ":failures")`; when `success === false` → `redis.set(BREAKER_KEY, "open", { ex: BREAKER_COOLDOWN_S, nx: true })` (comment: nx makes concurrent trips idempotent — benign race, first writer's TTL stands); entire body in try/catch that swallows with console.error. NEVER log request bodies or headers anywhere in this module (they carry raw API secrets / INTERNAL_API_TOKEN).

    Create `src/test/helpers/upstash-breaker.ts` (checker W-3 — `vi.mock` factories are per-file; 140-03 and 140-06 seam tests need IMPORTABLE doubles, not a pointer at this plan's test file). Exports: `createFakeUpstashStore()` → the `Map<string, { value: string; expiresAt: number }>`-backed store; `fakeRedisFor(store)` → `{get, set (honouring {ex, nx}), ttl (-2 when absent)}`; `fakeRatelimitFor(store, threshold)` → `{limit}` counting per-key with `success: count <= threshold`; `seedBreakerOpen(store, ttlS?)` → seeds `breaker:railway = "open"`. The helper builds PLAIN objects — each consuming test file still wires them via its own `vi.hoisted` + `vi.mock("@upstash/redis"|"@upstash/ratelimit")` calls (document this usage contract in the helper's header comment with a short example).

    In `resilient-fetch.test.ts` implement the behaviors above using the helper: `vi.hoisted(() => ({ store: ... }))` wrapping `createFakeUpstashStore()` (hoisting is load-bearing: factories re-run on `resetModules`; a factory-local store silently passes an in-memory breaker — research Pitfall 6). Use `installFetchMock`/`restoreFetchMock` plus `afterEach(() => vi.unstubAllGlobals())` — leaked fetch stubs are this repo's known CI-only (Node 22) failure cause. Simulate timeout failures the established way: `vi.fn().mockRejectedValue(new DOMException("aborted", "TimeoutError"))` — never `vi.useFakeTimers()`.

    Note: SC-2 drives failures through `resilientFetch`, which lands in Task 3 — structure the test file now with the engine tests written against the intended `resilientFetch(budgetKey, path, init)` signature; they go RED here and GREEN in Task 3. Pure breaker-function tests (isBreakerOpen fail-open paths) must be green in this task.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.test.ts -t "fails OPEN" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/resilient-fetch.test.ts -t "fails OPEN when Redis errors"` green
    - `npx vitest run src/lib/resilient-fetch.test.ts -t "fails OPEN when Upstash is unconfigured"` green (sets VERCEL_ENV=production inside the test)
    - `grep -c "vi.hoisted" src/lib/resilient-fetch.test.ts` ≥ 1; `grep -c "unstubAllGlobals" src/lib/resilient-fetch.test.ts` ≥ 1
    - `grep -cE "isProduction|VERCEL_ENV" src/lib/resilient-fetch.ts` = 0 (no environment branch anywhere in the module)
    - `grep -c "process.emitWarning" src/lib/resilient-fetch.ts` = 0; exactly one `console.warn` (the unconditional unconfigured notice)
    - `src/test/helpers/upstash-breaker.ts` exists exporting createFakeUpstashStore, fakeRedisFor, fakeRatelimitFor, seedBreakerOpen; `resilient-fetch.test.ts` imports it
    - No `throw` statement inside `isBreakerOpen` or `recordSeamFailure`
  </acceptance_criteria>
  <done>Breaker layer complete; all fail-OPEN paths (throw, null client) proven by green tests in prod-env config; shared helper published for 140-03/140-06; SC-2 tests written (RED pending Task 3).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resilientFetch engine — check, fetch-with-budget, classify, record</name>
  <files>src/lib/resilient-fetch.ts, src/lib/resilient-fetch.test.ts</files>
  <read_first>src/lib/analytics-client.ts:79-130 (current fetch/catch/!ok flow the engine must be substitutable under), src/lib/process-key-client.ts:108-188 (ditto — note `cache: "no-store"` must pass through), 140-RESEARCH.md §7.2 (failure classification) and §8 (AbortSignal.timeout spy technique)</read_first>
  <behavior>
    - SC-2 "cross-context" + SC-2-neg from Task 2 go GREEN
    - SC-3c "failure classification": 5×HTTP 400 responses → breaker does NOT trip (a subsequent call still fetches); 5×HTTP 503 → trips; 5×DOMException TimeoutError rejections → trips; 5×TypeError network throws → trips
    - SC-4c "budget reaches AbortSignal": `vi.spyOn(AbortSignal, "timeout")` (verified writable+configurable on Node 22 AND 25 — research §8); call with budgetKey "bridge" → `expect(AbortSignal.timeout).toHaveBeenCalledWith(SEAM_BUDGETS["bridge"].timeoutMs)` — assert against the exported table, not a literal
    - Timeout/network rejections are rethrown as the ORIGINAL error (DOMException/TypeError), never wrapped — the clients' existing catch-mapping (Wave 2) depends on this
  </behavior>
  <action>
    Implement `export async function resilientFetch(budgetKey: SeamBudgetKey, path: string, init: Omit<RequestInit, "signal"> & { timeoutMsOverride?: number }): Promise<Response>`:
    1. Resolve `timeoutMs = init.timeoutMsOverride ?? SEAM_BUDGETS[budgetKey].timeoutMs`.
    2. `isBreakerOpen()` → if open, `throw new CircuitOpenError(retryAfterS)`. The check happens here, per-call, inside the Node runtime — never in middleware/proxy (cannot know the call-site budget) and always invoked from INSIDE route handlers AFTER their auth gates (callers' responsibility, documented in the docstring: hoisting a breaker check above `withAuth`/`isAdminUser`/`CRON_SECRET` creates an unauthenticated breaker-state oracle).
    3. `fetch(`${ANALYTICS_URL}${path}`, { ...init, signal: AbortSignal.timeout(timeoutMs) })` where `ANALYTICS_URL = process.env.ANALYTICS_SERVICE_URL ?? "http://localhost:8002"` moves INTO this module (the core becomes the only legal home of the base URL — feeds the Wave-4 ESLint rule). Headers pass through byte-for-byte untouched — a dropped `X-User-Id` re-opens the CT-4 cross-tenant rate-limit-bucket defect.
    4. On fetch throw: classify — `err.name === "AbortError" || err.name === "TimeoutError"` (the BROADER process-key-client check; the narrower analytics-client DOMException-only check misses plain AbortError — research §8) or network throw → `await recordSeamFailure()` → RETHROW the original error unwrapped.
    5. On `Response`: `res.status >= 500` → `recordSeamFailure()`, then RETURN the Response either way. 4xx NEVER records (A4, ACCEPTED operator decision — comment verbatim rationale: a user's bad API key returning 400 is Railway working correctly; counting 4xx would let a handful of users fat-fingering keys trip the breaker and take key-connect down for everyone — a user error must never become an outage. If Railway 4xxes during genuine degradation the breaker under-trips; accepted, documented here).
    Zod/contract errors never reach this module (clients parse after the core returns).

    Finish `resilient-fetch.test.ts`: make SC-2/SC-2-neg green, add SC-3c and SC-4c per the behavior block. SC-3c's 4xx arm must assert the NEXT call still invokes fetch (breaker closed), mirroring research Pitfall 3's regression prescription.
  </action>
  <verify>
    <automated>npx vitest run src/lib/resilient-fetch.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/resilient-fetch.test.ts --no-file-parallelism` fully green, including `-t "cross-context"`, `-t "negative control"`, `-t "failure classification"`, `-t "budget reaches AbortSignal"`
    - `grep -n "not.toHaveBeenCalled" src/lib/resilient-fetch.test.ts` ≥ 1 (the SC-2 "never touched Railway" assertion)
    - `grep -cE "console\.(log|error|warn).*(body|headers)" src/lib/resilient-fetch.ts` = 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Core engine complete and fully unit-proven: breaker check → budgeted fetch → classified recording → typed CircuitOpenError, all SC-2/SC-3/SC-4c tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vercel route handler → Railway FastAPI | Outbound seam; requests carry raw API secrets + INTERNAL_API_TOKEN |
| resilient-fetch → Upstash Redis | Breaker state store; shared across all tenants |
| Server modules → client bundle | wizardErrors is client-reachable; the seam graph must never cross via the error class |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-140-01 | Denial of Service | breaker key | mitigate | `BREAKER_KEY` is a module constant; no user input ever interpolated (Task 1 acceptance grep) |
| T-140-02 | Denial of Service | breaker store outage | mitigate | fail OPEN on throw AND null client, all environments; no `isProduction()` branch (Task 2 tests SC-3a/b) |
| T-140-03 | Denial of Service | 4xx counted as failures | mitigate | infrastructure-only classification (5xx/timeout/network); SC-3c regression pins it |
| T-140-04 | Information Disclosure | core log lines | mitigate | never log request bodies/headers (carry API secrets + INTERNAL_API_TOKEN); acceptance grep in Task 3 |
| T-140-05 | Information Disclosure | CircuitOpenError message | mitigate | static message in the dependency-free leaf, no URLs/headers/upstream detail (Task 1) |
| T-140-06 | Information Disclosure | Retry-After reveals cooldown | accept | same information `rateLimitDenyJson` already publishes; operationally necessary |
| T-140-28 | Information Disclosure | server-only code shipped to browser via error-class import chain (B-1) | mitigate | CircuitOpenError homed in zero-import leaf; acceptance asserts zero imports in seam-errors.ts |
| T-140-29 | Denial of Service | pinning maxDuration ABOVE the real platform default increases lambda hold (W-2) | mitigate | observed default recorded in SUMMARY before pins; mismatch → pin observed value or STOP for operator |
| T-140-SC | Tampering | npm/pip installs | accept | ZERO new dependencies this phase (locked); `check-banned-packages.mjs` CI gate already active |
</threat_model>

<verification>
- `npx vitest run src/lib/resilient-fetch.test.ts --no-file-parallelism` green (all SC-2/SC-3/SC-4c titles present and passing)
- `npm run typecheck` and `npm run lint` green
- No import of `src/lib/ratelimit.ts` from `resilient-fetch.ts` (`grep -c "from \"./ratelimit\"" src/lib/resilient-fetch.ts` = 0)
- `grep -cE "^import" src/lib/seam-errors.ts` = 0
</verification>

<success_criteria>
- SEAM-02 data layer: SEAM_BUDGETS (13 keys) + SEAM_ROUTE_BUDGETS (15 routes, expectedMaxDurationS verified against the RECORDED platform default) + SEAM_EXCLUSIONS (3 reasoned rows) exported
- SEAM-03 proven at unit level: cross-context short-circuit, negative control, fail-OPEN ×3 (throw / null / prod-env), 4xx-never-trips
- CircuitOpenError importable from client-reachable code and fully-mocked tests with zero server-only baggage (leaf has zero imports)
- Tuning constants are one-line changes: 5 failures / "30 s" window / 30s cooldown as named exports with rationale comments
- `src/test/helpers/upstash-breaker.ts` published for the 140-03/140-06 seam tests
</success_criteria>

<output>
Create `.planning/phases/140-seam-shared-resilience-core-circuit-breaker/140-01-SUMMARY.md` when done — MUST include the observed effective default maxDuration (W-2 record).
</output>
