---
phase: 105.1
slug: onboarding-onto-backbone
status: planned
nyquist_compliant: true
wave_0_complete: false
created: 2026-07-14
---

# Phase 105.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution. Derived from 105.1-RESEARCH.md §Validation Architecture + the LOCKED CONTEXT D1–D6 + the three PATTERNS flags (A: nested ytd; B: `ytd` field name / twr←cumulative_return; C: pre-strategy asset_class source). Every behavior below carries an `<automated>` verify inside its plan task (Nyquist).

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | pytest (analytics-service), `--cov-fail-under=80` |
| **Config file** | `analytics-service/pytest.ini` (`testpaths = tests`) |
| **Quick run command** | `cd analytics-service && python -m pytest tests/test_process_key.py tests/test_teaser_derive_golden.py -x -q` |
| **Full suite command** | `cd analytics-service && python -m pytest --cov --cov-fail-under=80` |
| **Estimated runtime** | quick ~20–40s · full ~3–5 min |
| **Harness** | `_build_supabase_mock` (test_process_key.py:110-172) — chained MagicMock; RPC capture via `fake.rpc.call_args_list` (`:772-773` idiom). Executor has NO Supabase MCP; no DB/RLS test needed (zero DDL — Option c). No frontend surface changes → vitest unaffected. |

---

## Sampling Rate

- **After every task commit:** the quick-run set above.
- **After every plan wave:** `cd analytics-service && python -m pytest --cov --cov-fail-under=80` (the wave gate).
- **Before `/gsd:verify-work`:** full suite green + the no-persist grep gate (`grep -v '^\s*#' analytics-service/routers/process_key.py | grep -c 'persist_basis_series'` == 0) + `grep -q "Phase 105.1 addendum" analytics-service/services/basis_series.py` + `test "$(wc -l < .planning/BACKBONE-BYPASS-INVENTORY.md)" -ge 57`.
- **Max feedback latency:** ~40s (quick) / ~5 min (wave gate).

---

## Requirement → Test Map (coverage contract)

| Behavior (must be proven) | Test Type | Automated Command | Covered By (plan-task) |
|---------------------------|-----------|-------------------|------------------------|
| **Wiring equality**: persisted teaser `metrics_snapshot.twr/sharpe/ytd/max_drawdown` == `derive_basis_series(same series).metrics_json` values, builder sentinel values overridden; trade-level `total_pnl/trade_count/win_rate` kept from the builder (D5) | route-level unit (mock supabase) | `pytest tests/test_process_key.py -k teaser -x` (Test W) | **105.1-01 T1** |
| **FLAG A — nested ytd**: the map reads `metrics_json["metrics_json"]["ytd"]`; `"ytd"` is asserted NOT top-level (KeyError trap pinned) | unit on `_derive_return_scalars` | `pytest tests/test_process_key.py -k ytd -x` (Test A) | **105.1-01 T1** |
| **FLAG C / D2 — crypto √365**: crypto-venue teaser Sharpe == √365 derive AND != √252 derive on the same series | route-level unit | `pytest tests/test_process_key.py -k 365 -x` (Test W2) | **105.1-01 T1** |
| **FLAG C — asset_class sourcing**: okx/binance/bybit → "crypto" with NO DB read; csv → `strategies.asset_class` lookup (the finalize-coherent #597 picker signal); lookup failure → None → 252 | unit on `_resolve_asset_class` | `pytest tests/test_process_key.py -k asset_class -x` (Test R) | **105.1-01 T1** |
| **D1(b) — no-anchor-persist guard**: a full successful teaser run fires ZERO `upsert_strategy_analytics_series_batch` RPCs (blanket — stronger than anchor-filtered); neuter-proven (injected persist call flips it red) | route-level regression (rpc-capture idiom) | `pytest tests/test_process_key.py -k anchor -x` | **105.1-01 T2** |
| **D6 — degrade-to-nulls**: derive ValueError (<2 finite rows) AND the `len<2` pre-check both null the 4 return-based keys + the 5 legacy keys; response stays 200/published (never a 500); trade-level stats intact | unit + route-level | `pytest tests/test_process_key.py -k degrade -x` (Test D a/b/c) | **105.1-01 T1** |
| **D4 — flow-agnostic**: `internal_report` (and by shared-block construction `csv`) gets the same derived scalars; no `flow_type=='teaser'` compute branch | route-level unit | `pytest tests/test_process_key.py -k internal_report -x` (Test F) | **105.1-01 T1** |
| **D1(c) — equity_curve coherence**: persisted curve == cumprod over the derive's `series_rows` (date-only keys), old raw-cumprod block deleted | route-level unit | `pytest tests/test_process_key.py -k equity_curve -x` (Test C) | **105.1-01 T1** |
| **D2 — advisory golden**: 4 teaser scalars vs a first-principles paper oracle (√365 pinned; drift-detector; regen-on-intended-change docstring) | golden/advisory (test_metrics_minigolden pattern) | `pytest tests/test_teaser_derive_golden.py -x` | **105.1-01 T2** |
| **D1(a) — documented permanent exception**: basis_series docstring addendum + inventory Tier-2 #4 / DEAD-ROUTE #6 amendments; inventory edited-not-truncated | grep-gate + import + suite | `grep -q "Phase 105.1 addendum" analytics-service/services/basis_series.py && python -m pytest tests/test_basis_series.py -x` · `test "$(wc -l < .planning/BACKBONE-BYPASS-INVENTORY.md)" -ge 57` | **105.1-02 T1 + T2** |
| **No off-backbone persist path**: no non-comment `persist_basis_series` reference in process_key.py | grep-gate (comment-filtered — never a bare `== 0` on the unfiltered file) | `grep -v '^\s*#' analytics-service/routers/process_key.py \| grep -c 'persist_basis_series'` == 0 | **105.1-01 T1 verify** |

*Task IDs assigned by the planner 2026-07-14. Named neuters (each proven to flip its named test red, then reverted): drop the 4-key update → Test W; hardcode 252 → W2 + golden; drop the `except ValueError` → Test D(b); top-level ytd read → Test A; injected persist call → anchor guard.*

---

## Wave 0 Requirements

No standalone Wave 0 — both plans are Wave 1 and each TDD task creates its own RED tests before implementation:

- [ ] `tests/test_process_key.py` — Tests W/W2/C/A/R/D/F written RED against HEAD (105.1-01 T1, `test(105.1-01)` commit precedes `feat(105.1-01)`).
- [ ] `tests/test_process_key.py` — no-anchor-persist guard; its RED proof is the injected-persist neuter run (105.1-01 T2).
- [ ] `tests/test_teaser_derive_golden.py` — new file, paper oracle (105.1-01 T2).

---

## Manual-Only Verifications

- **None gating this phase.** The public landing-card preview shift (D2: ~+20% crypto Sharpe + input-conditioning delta + date-only curve keys) is ACCEPTED by user decision — a post-deploy landing-page glance belongs to milestone UAT/QA, not this phase's gate.

---

## Validation Sign-Off

| Gate | Command | Status |
|------|---------|--------|
| Quick set green | `cd analytics-service && python -m pytest tests/test_process_key.py tests/test_teaser_derive_golden.py -x -q` | ☐ |
| Wave gate | `cd analytics-service && python -m pytest --cov --cov-fail-under=80` | ☐ |
| No-persist grep gate | `grep -v '^\s*#' analytics-service/routers/process_key.py \| grep -c 'persist_basis_series'` → 0 | ☐ |
| Exception documented | `grep -q "Phase 105.1 addendum" analytics-service/services/basis_series.py` | ☐ |
| Inventory untruncated | `test "$(wc -l < .planning/BACKBONE-BYPASS-INVENTORY.md)" -ge 57` | ☐ |
