---
phase: 105.1-onboarding-onto-backbone
verified: 2026-07-14T00:00:00Z
status: passed
score: 6/6 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
---

# Phase 105.1: Onboarding onto the backbone Verification Report

**Phase Goal:** The onboarding sync-teaser COMPUTE is unified onto the backbone — return-based scalars route through `derive_basis_series` (persist NOTHING), crypto √365 per #597, flow-agnostic, with a documented permanent preview-only exception. Zero DDL.
**Verified:** 2026-07-14
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Persist-nothing holds; derive is compute-only; no-persist guard genuinely reddens | ✓ VERIFIED | `process_key.py`: 0 non-comment `persist_basis_series` (only comment `:285`), 0 `upsert_strategy_analytics_series_batch`. `derive_basis_series` (`basis_series.py:181-291`) has no supabase/rpc/persist. Neuter proof: injecting a persist RPC into the success path (`:1050`) flipped `test_teaser_persists_no_series_row` RED; restored clean. |
| 2 | crypto √365 sourcing correct AND exhaustive; no traditional venue mis-annualized | ✓ VERIFIED | `_resolve_asset_class` (`:312-347`): crypto venues → `"crypto"` (no DB read), csv → `strategies.asset_class` lookup, failure/None → None. H-11 whitelist (`:143-147`): teaser/internal_report = {okx,binance,bybit}, csv = {csv}; onboard/resync (deribit) dispatched to long-fetch at `:890` BEFORE the swap. `periods_per_year_for_asset_class`: crypto→365, traditional/None→252. `test_teaser_derive_crypto_uses_365_not_252` green. |
| 3 | 4-key map reads nested ytd; other three top-level | ✓ VERIFIED | `_derive_return_scalars` (`:381-387`): `ytd = scalars["metrics_json"]["ytd"]` (nested), twr/sharpe/max_drawdown top-level. `test_derive_return_scalars_reads_nested_ytd` asserts `"ytd" not in result.metrics_json` (top-level read would KeyError). Green. |
| 4 | Degrade-to-nulls: both boundaries share one null-out arm; keys pre-nulled | ✓ VERIFIED | 4 return-based keys pre-nulled at `:1019-1021` before the try. `except ValueError` (`:1043-1045`) and `len<2` else (`:1064-1069`) both `update(_LEGACY_NULL_ENRICHMENT)`. Best-effort generic `except` (`:1070`) cannot strand builder values (pre-nulled). Tests D(a/b/c) green; response stays 200/published. |
| 5 | Honesty docs: basis_series addendum + BYPASS-INVENTORY exception | ✓ VERIFIED | `basis_series.py:69-92` "Phase 105.1 addendum (teaser preview-only exception, PERMANENT)". `BACKBONE-BYPASS-INVENTORY.md` = 57 lines, 3× "105.1"; item #4 (`:22`) records COMPUTE bypass CLOSED + permanent preview-only exception, #6 (`:52`) + sequencing (`:57`) amended. |
| 6 | Deferred fenced: metrics_snapshot retirement, TS twin, lead-capture NOT here | ✓ VERIFIED | Phase diff touches only 4 files (`process_key.py`, `basis_series.py`, 2 test files); no migrations/SQL/DDL; `verify-strategy/route.ts` untouched. `metrics_snapshot` still written (`enriched_metrics_snapshot`, `:1010`) — not retired. No lead-capture persistence (persist-nothing). |

**Score:** 6/6 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `analytics-service/routers/process_key.py` | `_derive_return_scalars` + `_resolve_asset_class` + call-site swap | ✓ VERIFIED | Helpers at `:312`/`:350`; swap + pre-null at `:1010-1069`; old cumprod block deleted. |
| `analytics-service/services/basis_series.py` | invariant docstring addendum | ✓ VERIFIED | Addendum `:69-92`; runtime unchanged (`test_basis_series.py` 31 passed per SUMMARY). |
| `analytics-service/tests/test_teaser_derive_golden.py` | √365 paper oracle | ✓ VERIFIED | 98 lines; `test_teaser_derive_golden_crypto_365` (rel 1e-9/1e-6) + `_365_not_252` pin. |
| `analytics-service/tests/test_process_key.py` | 10 wiring/degrade/guard tests | ✓ VERIFIED | W/W2/C/A/R/D(a-c)/F + no-persist guard present and green. |
| `.planning/BACKBONE-BYPASS-INVENTORY.md` | exception documented, untruncated | ✓ VERIFIED | 57 lines (≥57 gate), 3× "105.1"; gitignored/uncommitted by design. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `_derive_return_scalars` | `derive_basis_series` | single call, `periods_per_year_for_asset_class(asset_class)` | ✓ WIRED | `:373-379` — geometric/calendar; ValueError propagates to degrade arm. |
| swap call site | `_derive_return_scalars` + `_resolve_asset_class` | `:1039-1042` | ✓ WIRED | Success path updates 4 derived keys FIRST (`:1050`), then enrichment; equity_curve re-sourced from derived series (`:1060`). |
| dispatch | sync-only swap | `_is_long_fetch` at `:890` | ✓ WIRED | onboard/resync return early to worker; swap serves teaser/csv/internal_report only. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Teaser/derive test set green | `pytest -k "teaser or derive or ..." + golden -q` | 15 passed | ✓ PASS |
| No-persist guard reddens on injected persist | inject `upsert_...batch` RPC at `:1050`, run guard | 1 failed (then reverted) | ✓ PASS |
| asset-class → periods mapping | `periods_per_year_for_asset_class` crypto/trad/None | 365/252/252 | ✓ PASS |
| tz-aware ytd comparison raises (deviation-1 basis) | `index(tz=UTC) >= Timestamp('2024-01-01')` | TypeError | ✓ PASS |

### Executor Deviation Scrutiny

| Deviation | Verdict | Evidence |
|-----------|---------|----------|
| Golden fixture tz-NAIVE not tz-aware | ✓ LEGITIMATE (not a shortcut) | `metrics.py:846` compares `returns.index >= pd.Timestamp(f"{year}-01-01")` (tz-naive); a tz-aware index raises `TypeError: Invalid comparison ... datetime64[us, UTC]` (reproduced). Production `trades_to_daily_returns` builds its index from `.dt.date` + `groupby` → tz-naive `datetime64[us]` (`transforms.py:136,201`). Fixture mirrors real canonical series. |
| `internal_report` test gained explicit `strategy_id` | ✓ LEGITIMATE (not masking a gap) | Only teaser auto-injects the anchor (`:601-602 if body.flow_type == "teaser"`). internal_report/csv require a real strategy_id in context or hit `MISSING_STRATEGY_ID` (`:823-828`). Mirrors production. `binance → "crypto"` resolves with no DB read, so the id is inert for the annualization assertion. |

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| 105.1-SC1 (D1 reworded) | No teaser scalar computed off the backbone; teaser is a documented permanent preview-only exception | ✓ SATISFIED | Truths 1, 5 — derive-only compute + documented exception in both homes. |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | none | — | No unreferenced TBD/FIXME/XXX in phase-modified files; `return None`/empty patterns are intentional degrade-to-nulls (D6). |

### Human Verification Required

None gating this phase. Per `105.1-VALIDATION.md` the public landing-card preview shift (~+20% crypto Sharpe + input-conditioning delta + date-only curve keys) is an ACCEPTED user decision (D2) and belongs to milestone UAT/QA, not this phase's gate.

### Gaps Summary

No gaps. All six must-haves verified against the actual code (not SUMMARY claims). The compute is structurally unified onto `derive_basis_series` with zero persistence, exhaustive crypto √365 sourcing guaranteed by the H-11 whitelist + sync-only dispatch, a nested-ytd map, a shared degrade-to-nulls arm with pre-nulled keys, and the permanent preview-only exception documented in both the code invariant and the program inventory. Both executor deviations are correct root-cause fixes, not shortcuts. Zero DDL; deferred scope (metrics_snapshot retirement, TS twin delete, lead-capture persistence) correctly untouched.

**Overall phase verdict: the codebase ACHIEVES the goal.**

---

_Verified: 2026-07-14_
_Verifier: Claude (gsd-verifier)_
