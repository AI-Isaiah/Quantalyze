---
phase: 105.1-onboarding-onto-backbone
plan: 02
subsystem: analytics-backbone-docs
tags: [docs, backbone-invariant, teaser, preview-only-exception, D1]
requires:
  - 105.1-CONTEXT.md (D1 justification)
provides:
  - "Phase 105.1 addendum in basis_series.py invariant docstring (committed)"
  - "BACKBONE-BYPASS-INVENTORY.md Tier-2 #4 + DEAD-ROUTE #6 + sequencing note amended (local, uncommitted)"
affects:
  - analytics-service/services/basis_series.py
tech-stack:
  added: []
  patterns: ["documented permanent invariant exception (mirror of Phase-104 addendum)"]
key-files:
  created: []
  modified:
    - analytics-service/services/basis_series.py
    - .planning/BACKBONE-BYPASS-INVENTORY.md (local/gitignored — NOT committed)
decisions:
  - "D1 honesty condition (a): teaser is compute-unified but persist-none is a PERMANENT preview-only exception, documented in both the code invariant and the program inventory so no future phase re-discovers it as a bug"
metrics:
  duration: ~4m
  completed: 2026-07-14
requirements: [105.1-SC1]
---

# Phase 105.1 Plan 02: D1 Honesty-Condition Docs Summary

Documented the teaser's compute-unified / persist-nothing state as a PERMANENT, deliberate exception to the persisted-series half of the backbone invariant — in both homes reviewers look (the `basis_series.py` module docstring and `BACKBONE-BYPASS-INVENTORY.md`). Docs-only; zero runtime behavior change.

## What Was Done

### Task 1 — basis_series.py invariant-docstring addendum (committed `03f71a92`)
Added a "Phase 105.1 addendum (teaser preview-only exception, PERMANENT)" paragraph as a sibling to the existing Phase-104 addendum, inside the module docstring, before the closing `"""`. Verbatim text of the addendum:

```
Phase 105.1 addendum (teaser preview-only exception, PERMANENT)
---------------------------------------------------------------
The sync onboarding pipeline (`routers/process_key.py` — teaser/csv/internal_report)
DERIVES its preview scalars via `derive_basis_series` but persists NO series row. This
is a documented PERMANENT exception to the persisted-series half of the invariant — it
survives Phase 106 and is NOT an open bypass to be "re-discovered" later. The COMPUTE
half of the LOCKED principle holds fully: the preview scalars are a cache of the
just-derived series within the SAME call, so no second derivation path exists (this
kills the second-derivation divergence class for onboarding). What is deliberately
absent is a persisted series backing those scalars — because there is no series READER.
Justification (D1, user 2026-07-14): the teaser landing card reads scalars + a
precomputed cumulative curve, never a series row; and the teaser's shared sentinel
anchor (`00000000-0000-0000-0000-000000000001`, `status='archived'`, user-less) makes
any hypothetical persisted row unreadable BY CONSTRUCTION — `fetch_strategy_lazy_metrics`
requires published-OR-owner, and the fixed `(strategy_id, kind)` PK is collision-prone
across concurrent teasers. A SUBMITTER-keyed teaser series for lead capture is deferred
scope (Phase 106 / a dedicated slice — USER 2026-07-14) and MUST NOT key on the archived
sentinel; that is a separate future concern, not a gap in this invariant.
```

Verification: `grep -q "Phase 105.1 addendum"` OK; `.venv/bin/python -c "import services.basis_series"` OK; `pytest tests/test_basis_series.py -x -q` → **31 passed** (proves docstring-only, no behavior change). Staged with the explicit path `git add analytics-service/services/basis_series.py` (never `-A`).

### Task 2 — BACKBONE-BYPASS-INVENTORY.md amendments (local, EDITED not written, NOT committed)
Three targeted `Edit` operations (never the Write tool — the file is gitignored with no history and has been truncated by subagents before):

1. **Tier-2 item #4** — records the sync-arm COMPUTE bypass as ✅ CLOSED in 105.1 (teaser/csv/internal_report scalars derive via `derive_basis_series`, 4-key map, √365/√252 per #597) and the scalar-without-series state as a DOCUMENTED PERMANENT preview-only exception (derive-only/persist-none — no reader; archived `00000…0001` anchor unreadable + PK-colliding), NOT an open bypass. Queued arm already transient-by-tail-job. `metrics_snapshot` store retirement + deferred lead-capture series persistence stay Phase 106.
2. **DEAD-ROUTE #6** — records that 105.1 satisfied the "onboarding routing through the backbone" half of the blocker; retirement now waits ONLY on the 3 repoints; per D1 the teaser keeps a scalar home permanently, with the deferred SUBMITTER-keyed lead-capture series (`[[project_teaser_series_persist_for_lead_contact]]`, Phase 106, keyed on submitter not the sentinel) as the candidate home. `metrics_snapshot` legacy-delete tail + lead-capture persistence remain Phase 106.
3. **Sequencing note** — replaced the now-stale "#4+#6 share a blocker (onboarding must persist a series first)" with "#4 compute-closed in 105.1 (persist-none is the documented permanent exception); #6 waits on its 3 repoints".

**Truncation guard:** `wc -l` before = **57**, after = **57** (edits replaced single lines with longer single lines; no line count shrink → no truncation). `grep -c "105.1"` = **3** (≥ 2 required). `git check-ignore` confirms the file is gitignored; `git status --porcelain` shows nothing → it was **Edited, never rewritten, and NOT git-added**.

## Deviations from Plan
None — plan executed exactly as written.

## Ownership / Concurrency
Owned only `analytics-service/services/basis_series.py` + `.planning/BACKBONE-BYPASS-INVENTORY.md`. A sibling agent concurrently owns `routers/process_key.py` + the two teaser test files — untouched. No runtime logic in `basis_series.py` changed (docstring-only; the sibling relies on unchanged function behavior — confirmed via green `test_basis_series.py`).

## Self-Check: PASSED
- `analytics-service/services/basis_series.py` — FOUND, contains "Phase 105.1 addendum"
- Commit `03f71a92` — FOUND in git log
- `.planning/BACKBONE-BYPASS-INVENTORY.md` — 57 lines, 3× "105.1", gitignored/untracked (intentionally uncommitted)
