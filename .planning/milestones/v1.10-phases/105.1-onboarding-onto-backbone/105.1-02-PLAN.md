---
phase: 105.1-onboarding-onto-backbone
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/basis_series.py
  - .planning/BACKBONE-BYPASS-INVENTORY.md
autonomous: true
requirements: [105.1-SC1]

must_haves:
  truths:
    - "The permanent preview-only exception is documented in basis_series.py's invariant docstring as a Phase-105.1 addendum sibling to the Phase-104 addendum (D1 honesty condition a)"
    - "BACKBONE-BYPASS-INVENTORY.md Tier-2 item #4 records the sync-teaser COMPUTE bypass as CLOSED (derive-only/persist-none) and the scalar-without-series state as a documented PERMANENT exception, not an open bypass"
    - "BACKBONE-BYPASS-INVENTORY.md DEAD-ROUTE #6 records that 105.1 satisfies the 'onboarding routing through the backbone' half; store retirement + 3 repoints remain Phase 106"
    - "BACKBONE-BYPASS-INVENTORY.md was EDITED, never rewritten — line count did not shrink (the file is gitignored with no history and has been truncated by subagents before)"
  artifacts:
    - path: "analytics-service/services/basis_series.py"
      provides: "Phase 105.1 addendum paragraph in the module invariant docstring"
      contains: "Phase 105.1 addendum"
    - path: ".planning/BACKBONE-BYPASS-INVENTORY.md"
      provides: "Tier-2 #4 + DEAD-ROUTE #6 amended with the 105.1 closure/exception"
      contains: "105.1"
  key_links:
    - from: "analytics-service/services/basis_series.py"
      to: ".planning/BACKBONE-BYPASS-INVENTORY.md"
      via: "the same exception documented in both homes (code invariant + program inventory)"
      pattern: "preview-only"
---

<objective>
D1 honesty condition (a): the teaser's compute-unified / persist-NOTHING state is a PERMANENT, deliberate exception to the persisted-series half of the backbone invariant — it must be documented where reviewers actually look (the basis_series.py invariant docstring) and where the unification program tracks bypasses (BACKBONE-BYPASS-INVENTORY.md), or a future phase will "re-discover" it as an open bug and ship the rejected Option (a)/(d).

HARD BOUNDARIES:
- Documentation ONLY. Zero code/behavior changes — basis_series.py gets a docstring paragraph, nothing below line ~68 changes.
- `.planning/BACKBONE-BYPASS-INVENTORY.md` is gitignored/LOCAL with NO git history and has been TRUNCATED by GSD subagents before: use targeted Edit operations ONLY (never Write), spot-check `wc -l` before/after, and NEVER `git add` it.
- Do not re-litigate D1. The deferred lead-capture persistence (USER 2026-07-14, rides Phase 106) may be REFERENCED but not implemented.

Purpose: the exception survives Phase 106 and beyond as documented intent, not tribal knowledge.
Output: docstring addendum (committed) + inventory amendments (local file, uncommitted).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105.1-onboarding-onto-backbone/105.1-CONTEXT.md (D1 — the exception's exact justification)
@.planning/phases/105.1-onboarding-onto-backbone/105.1-PATTERNS.md (§5 — the amendment shapes)

Executor constraints: NO git branch operations. `git add analytics-service/services/basis_series.py` ONLY — never `-A`, never `.planning/` or any file under it. No Supabase MCP.

<interfaces>
From analytics-service/services/basis_series.py (re-grepped 2026-07-14): the module docstring spans `:1-68`; the LOCKED-principle framing is at `:3-14` ("the scalars are a cache of the rows, never an independent source of truth"); the precedent block to mirror is the "Phase 104 addendum (BB-01, SERIES-ONLY)" paragraph at `:60-67` — a scoped exception documented in the same prose register. Insert the new paragraph AFTER `:67`, before the closing `\"\"\"` at `:68`.

From .planning/BACKBONE-BYPASS-INVENTORY.md (57 lines at plan time — re-run `wc -l` yourself):
- Line 22 — Tier-2 item #4: "**Unify onboarding** (`process_key.py:941` sync arm + `long_fetch.py:396` queued arm) — today write a scalar `metrics_snapshot` with NO series persisted (scalar-without-series window) + defer to a tail job."
- Line 52 — DEAD-ROUTE #6: onboarding `metrics_snapshot` ZOMBIE-RISK, "Blocked behind 3 repoints + onboarding routing through the backbone."
- Line 57 — Sequencing note: "#4+#6 share a blocker (onboarding must persist a series first)" — now stale under D1.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: basis_series.py invariant-docstring addendum (Phase 105.1 preview-only exception)</name>
  <files>analytics-service/services/basis_series.py</files>
  <action>
    Append a sibling paragraph after the Phase-104 addendum (`:60-67`), inside the module docstring, titled "Phase 105.1 addendum (teaser preview-only exception, PERMANENT):". Content (match the `:60-67` prose register; cite D1):
    - The sync onboarding pipeline (`routers/process_key.py` — teaser/csv/internal_report) DERIVES its preview scalars via `derive_basis_series` but persists NO series row — a documented PERMANENT exception to the persisted-series half of the invariant (survives Phase 106).
    - The COMPUTE half of the LOCKED principle holds fully: the preview scalars are a cache of the just-derived series within the same call — no second derivation path exists.
    - Justification: the teaser has NO series reader (the landing card reads scalars + a cumulative curve), and its shared sentinel anchor (`00000000-0000-0000-0000-000000000001`, `status='archived'`, user-less) makes any hypothetical persisted row unreadable by construction (`fetch_strategy_lazy_metrics` requires published-OR-owner) and PK-collision-prone across concurrent teasers on `(strategy_id, kind)`.
    - Forward pointer: a SUBMITTER-keyed teaser series for lead capture is deferred scope (Phase 106 / dedicated slice — USER 2026-07-14); it must NOT key on the archived sentinel.
    Docstring-only change — no code below the closing quotes may differ. Commit as `docs(105.1-02): document the teaser preview-only exception in the basis_series invariant` with `git add analytics-service/services/basis_series.py` (explicit path only).
  </action>
  <verify>
    <automated>cd analytics-service && grep -q "Phase 105.1 addendum" services/basis_series.py && python -c "import services.basis_series" && python -m pytest tests/test_basis_series.py -x -q</automated>
  </verify>
  <done>Addendum present adjacent to the Phase-104 addendum; module imports; test_basis_series.py green (proves docstring-only); committed with the explicit path.</done>
</task>

<task type="auto">
  <name>Task 2: BACKBONE-BYPASS-INVENTORY.md — close the compute bypass, record the permanent exception (EDIT ONLY)</name>
  <files>.planning/BACKBONE-BYPASS-INVENTORY.md</files>
  <action>
    FIRST run `wc -l .planning/BACKBONE-BYPASS-INVENTORY.md` and record the count (57 at plan time). Then three targeted Edit operations (old_string → new_string; NEVER the Write tool on this file — it is gitignored with no history and prior subagents have truncated it):
    1. Tier-2 item #4 (line ~22): amend to record — 105.1 CLOSED the sync-arm COMPUTE bypass (teaser/csv/internal_report scalars now derive via `derive_basis_series`, 4-key map, √365/√252 per #597); the scalar-without-series state is now a DOCUMENTED PERMANENT preview-only exception (derive-only/persist-none — no series reader; the archived `00000…0001` anchor is unreadable + PK-colliding), NOT an open bypass; the queued arm (`long_fetch.py:396`) was already transient-by-tail-job; the `metrics_snapshot` store retirement stays 106.
    2. DEAD-ROUTE #6 (line ~52): append — 105.1 satisfied the "onboarding routing through the backbone" half of the blocker; retirement now waits ONLY on the 3 repoints, and (per D1) must keep a teaser scalar home since no series row exists; the deferred SUBMITTER-keyed lead-capture series ([[project_teaser_series_persist_for_lead_contact]]) is the candidate home.
    3. Sequencing note (line ~57): replace the stale "#4+#6 share a blocker (onboarding must persist a series first)" clause with "#4 compute-closed in 105.1 (persist-none is the documented permanent exception); #6 waits on its 3 repoints".
    THEN re-run `wc -l` — the count MUST be >= the recorded value (edits add text; any shrink = truncation → restore from the plan-time content quoted in this plan's interfaces block and retry). Do NOT `git add` this file — it is gitignored and must stay untracked/local.
  </action>
  <verify>
    <automated>grep -c "105.1" .planning/BACKBONE-BYPASS-INVENTORY.md | grep -qvx '0' && test "$(wc -l < .planning/BACKBONE-BYPASS-INVENTORY.md)" -ge 57</automated>
  </verify>
  <done>All three amendments landed via Edit; line count >= 57 (no truncation); file remains untracked (`git status --porcelain .planning/BACKBONE-BYPASS-INVENTORY.md` shows nothing — gitignored).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Documentation-only plan; no code path, input, or persistence surface changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105.1-04 | Repudiation | undocumented invariant exception | mitigate | This plan IS the mitigation — the exception is recorded in both the code invariant and the program inventory so future phases cannot "re-discover" it as a bug and ship the rejected persist options |
| T-105.1-SC | Tampering | package installs | accept | No packages touched |
</threat_model>

<verification>
- `grep -q "Phase 105.1 addendum" analytics-service/services/basis_series.py`
- `cd analytics-service && python -m pytest tests/test_basis_series.py -x -q` green (docstring-only proof).
- `test "$(wc -l < .planning/BACKBONE-BYPASS-INVENTORY.md)" -ge 57` and `grep -c "105.1" .planning/BACKBONE-BYPASS-INVENTORY.md` >= 2.
</verification>

<success_criteria>
D1 honesty condition (a) fully landed: the permanent preview-only exception is documented in basis_series.py (committed) and BACKBONE-BYPASS-INVENTORY.md (local, edited-not-rewritten, untruncated), with the stale "must persist a series first" sequencing corrected.
</success_criteria>

<output>
Create `.planning/phases/105.1-onboarding-onto-backbone/105.1-02-SUMMARY.md` when done. Record the before/after `wc -l` of the inventory file.
</output>
