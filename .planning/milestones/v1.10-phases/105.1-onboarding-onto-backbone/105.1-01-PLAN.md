---
phase: 105.1-onboarding-onto-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/routers/process_key.py
  - analytics-service/tests/test_process_key.py
  - analytics-service/tests/test_teaser_derive_golden.py
autonomous: true
requirements: [105.1-SC1]

must_haves:
  truths:
    - "No sync-pipeline scalar is computed off the backbone: the persisted metrics_snapshot's twr/sharpe/ytd/max_drawdown EQUAL derive_basis_series(returns).metrics_json values on the same trades_to_daily_returns series — the EquityCurveBuilder values for those 4 fields never persist (D1, SC1-reworded)"
    - "Crypto teaser annualizes √365 per #597 (D2): a teaser from a crypto venue (okx/binance/bybit) produces a Sharpe equal to the √365 derive and UNEQUAL to the √252 derive on the same series (FLAG C resolved: venue → asset_class; csv source → strategies.asset_class lookup, the same signal finalize reads at analytics_runner.py:2313-2314)"
    - "The 4-key map reads ytd from the NESTED result.metrics_json['metrics_json']['ytd'] location (FLAG A — top-level scalars['ytd'] KeyErrors), pinned by a test asserting 'ytd' is NOT a top-level metrics_json key"
    - "The sync pipeline persists NO strategy_analytics_series row: zero upsert_strategy_analytics_series_batch RPC calls across a full teaser run (D1 honesty condition b), and persist_basis_series is never invoked from process_key.py"
    - "Degrade contract (D6): <2 finite daily returns → the four return-based scalars persist as None (trade-level total_pnl/trade_count/win_rate still render); derive's ValueError maps onto the existing degrade-to-nulls arm"
    - "The swap is flow-AGNOSTIC (D4): internal_report/csv route through the identical shared block — no flow_type=='teaser' branch exists in the compute block (anti-cosplay doctrine process_key.py:470-473)"
    - "The snapshot equity_curve derives from the SAME series the scalars derive from (result.series_rows cumprod), not a separate (1+r).cumprod() (D1 honesty condition c)"
  artifacts:
    - path: "analytics-service/routers/process_key.py"
      provides: "_derive_return_scalars + _resolve_asset_class helpers + the flow-agnostic derive swap in the sync compute block"
      contains: "derive_basis_series"
    - path: "analytics-service/tests/test_process_key.py"
      provides: "wiring-equality, crypto-√365, nested-ytd, asset_class-resolution, degrade-to-nulls, no-anchor-persist tests"
      contains: "upsert_strategy_analytics_series_batch"
    - path: "analytics-service/tests/test_teaser_derive_golden.py"
      provides: "advisory hand-oracle golden on the 4 teaser scalars (D2 shift-detector)"
      contains: "_derive_return_scalars"
  key_links:
    - from: "analytics-service/routers/process_key.py"
      to: "analytics-service/services/basis_series.py"
      via: "_derive_return_scalars calls derive_basis_series and reads .metrics_json — NEVER persist_basis_series"
      pattern: "derive_basis_series"
    - from: "analytics-service/routers/process_key.py"
      to: "analytics-service/services/metrics.py"
      via: "periods_per_year_for_asset_class(_resolve_asset_class(body.source, strategy_id, supabase))"
      pattern: "periods_per_year_for_asset_class"
---

<objective>
Phase 105.1 (Tier-2 collapse #4, BB-02 part): route the sync-pipeline (teaser/csv/internal_report) return-based scalars through `derive_basis_series` — compute-unified, persist-NOTHING (D1, USER-LOCKED). The teaser's public preview scalars stop being an off-backbone `EquityCurveBuilder` second-derivation and become a cache of a just-derived daily series, annualized per asset class (#597: crypto √365 — D2, USER-LOCKED). NO series row is ever persisted (the archived `00000…0001` anchor makes any row unreadable + PK-colliding by construction).

HARD BOUNDARIES (locked — do NOT re-open):
- NO DDL, no new SECDEF, no RLS. NEVER call `persist_basis_series` from process_key.py — derive is compute-only by construction (pattern-mapper confirmed: persistence is a separate function, `basis_series.py:275`).
- D4: swap the SHARED sync compute block, not a teaser fork. NO `flow_type == 'teaser'` branches downstream of `:479` (anti-cosplay doctrine `:470-473`). Branching on `body.source` is fine — H-11 already keys on source.
- D5: 4-key map ONLY (twr/sharpe/ytd/max_drawdown). NO wholesale spread of `result.metrics_json` — a spread pushes ~50 keys through the public H-04 sanitizer to unauthenticated browsers. `EquityCurveBuilder` KEEPS running: `total_pnl/trade_count/win_rate` are trade-level (documented NON-exception) and `compute_fingerprint(trades, metrics)` at `:973` still needs the builder `MetricsSnapshot` — do NOT touch `metrics` itself.
- The async onboard arm (`long_fetch.py`) is already on-backbone via its tail job — DO NOT TOUCH. The TS teaser twin (`src/app/api/verify-strategy/route.ts`) is dormant + deferred to 106 — DO NOT TOUCH. The `metrics_snapshot` store keeps being written (retirement = 106).
- Public preview numbers WILL shift (different input conditioning + √365) — ACCEPTED (D2). No byte-identity gate exists on the teaser; do not import `scalar_returns`/`densify_policy` machinery (pass neither — defaults).

Purpose: the last onboarding compute bypass closes; "nothing bypasses the unified backbone" holds structurally for the teaser.
Output: swapped sync block + 2 helpers + wiring/√365/nested-ytd/degrade/no-persist tests + advisory golden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/105.1-onboarding-onto-backbone/105.1-CONTEXT.md (D1–D6 LOCKED)
@.planning/phases/105.1-onboarding-onto-backbone/105.1-PATTERNS.md (analogs + FLAGS A/B/C)
@.planning/phases/105.1-onboarding-onto-backbone/105.1-RESEARCH.md (§Q1 teaser map, §Security Domain)

Executor constraints: NO git branch operations (only `git add <explicit paths>` — the three files in files_modified); NEVER stage `.planning/` or anything under it. No Supabase MCP — pytest against the existing `_build_supabase_mock` harness only.

<interfaces>
All line numbers re-grepped against HEAD 2026-07-14.

From analytics-service/services/basis_series.py:
- `derive_basis_series(returns: pd.Series, benchmark_rets: pd.Series | None, *, periods_per_year: int, cumulative_method: str, day_basis: str, benchmark_symbol=None, scalar_returns=None, densify_policy=None) -> BasisSeriesResult` (def `:162-172`). COMPUTE-ONLY — never persists. Raises `ValueError` when <2 finite rows survive sanitize (`:206-209`). Teaser call shape: `derive_basis_series(returns, None, periods_per_year=..., cumulative_method="geometric", day_basis="calendar")` — omit benchmark_symbol/scalar_returns/densify_policy (Pitfall 4: no byte-identity machinery here).
- `BasisSeriesResult.metrics_json` (`:153`, `:265`) — the JSON-safe scalar dict; `.series_rows` (`:155`) — `[{"date": "YYYY-MM-DD", "return": float}, ...]` sparse persisted-truth form.

From analytics-service/services/metrics.py:
- `periods_per_year_for_asset_class(asset_class: str | None) -> int` (`:43-45`): `"crypto"` → 365, anything else (incl. None) → 252 (documented conservative fallback).
- metrics_json shape (`:1177-1195`): TOP-LEVEL keys `cumulative_return` (`:1178`), `sharpe` (`:1181`), `max_drawdown` (`:1184`). **`ytd` is NESTED**: written into the inner dict at `:846`, which becomes the `"metrics_json"` key at `:1189`. FLAG A: `scalars["ytd"]` at top level KeyErrors — read `scalars["metrics_json"]["ytd"]`.

From analytics-service/services/ingestion/adapter.py:149-164 — `MetricsSnapshot` fields: `sharpe, twr, ytd, max_drawdown` (return-based — the 4-key map targets; NOTE field is `ytd`, NOT `return_ytd` — FLAG B) + `total_pnl, trade_count, win_rate` (trade-level — KEEP from builder). Mapping (mirrors `to_metrics_snapshot` at equity_reconstruction.py:3065-3087 where `twr=self.compute_twr()`): twr ← `cumulative_return`, sharpe ← `sharpe`, ytd ← nested `metrics_json.ytd`, max_drawdown ← `max_drawdown`.

From analytics-service/routers/process_key.py (the swap site):
- H-11 source whitelist (`:137-143`): sync-eligible sources are `teaser/internal_report → {okx, binance, bybit}`, `csv → {csv}`. Deribit NEVER reaches the sync block (long-fetch only).
- Anchor forcing `:479-481`; anti-cosplay doctrine `:470-473`; sync pipeline entry `:789-790`; the compute block `:863-948`: `fetch_raw :864`, `compute_metrics :867`, `_metrics_to_jsonb :888` (= services.ingestion.serde.metrics_to_jsonb, aliased import `:49` — dataclass → asdict), enrichment try `:890`, `trades_to_daily_returns :896-898`, `len>=2 gate :899`, cumprod curve `:901-905`, `find_matched_strategy :906` (function-local import `:892` — patch at `services.strategy_matching.find_matched_strategy`), degrade-to-nulls arm `:916-928`, best-effort generic except `:929-939`, metrics_captured RPC write `:941-948`. Fingerprint `:973` uses the untouched builder `metrics`.
- Sync csv arm ALWAYS has a REAL strategy_id (the `:614-621` guard returns early otherwise; csv-finalize has its own branch `:629`) — so `strategies.asset_class` is readable for csv. The wizard's #597 picker persists it (src/app/api/strategies/csv-finalize/route.ts:245-248), and finalize compute reads the same column (analytics_runner.py:2313-2314) — preview and finalize stay on the same clock.
- `_trade_to_dict` (`:261-271`): dicts pass through unchanged — test fixtures can feed plain daily_pnl dicts (`{"timestamp": ISO, "order_type": "daily_pnl", "side": "buy"|"sell", "price": abs_pnl}`) straight through fetch_raw.

From analytics-service/tests/test_process_key.py:
- `_build_supabase_mock` (`:110-172`) — chained MagicMock; `fake.rpc.call_args_list` records every RPC name+payload.
- RPC-capture idiom (`:772-773`): `rpc_calls = [c.args for c in fake.rpc.call_args_list]` then filter by name.
- The existing teaser test (`:1205-1326`) — the harness to extend: patches `routers.process_key.is_unified_backbone_active` (AsyncMock True), `routers.process_key.get_supabase`, `routers.process_key.get_adapter`, and `patch.object(services.encryption, "get_kek"/"encrypt_credentials")`. Copy this scaffolding.
- `upsert_strategy_analytics_series_batch` is the SOLE writer of strategy_analytics_series (confirmed `test_cash_basis_series_sc4.py:66`).

Landing-card reader (src/components/landing/VerificationSection.tsx:21-30): reads `twr/sharpe/return_24h/return_mtd/return_ytd/equity_curve/trade_count/matched_strategy_id`. `return_24h/return_mtd/return_ytd` come from the `compute_period_returns` enrichment — UNTOUCHED here (landing-card-specific, not the backbone). `equity_curve` entries are `{date: string, value: number}` — the date string moving from full ISO timestamp to date-only (`series_rows` format) is an accepted shape shift (JS `new Date("YYYY-MM-DD")` parses fine; no byte-identity surface).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Flow-agnostic derive swap — 4-key map, asset_class resolution, degrade, curve re-source</name>
  <files>analytics-service/routers/process_key.py, analytics-service/tests/test_process_key.py</files>
  <behavior>
    RED first — add these tests to tests/test_process_key.py (they MUST fail against HEAD), commit `test(105.1-01): ...`, then implement, commit `feat(105.1-01): ...`:
    - Test W (wiring equality — the flagship): teaser POST (flow_type=teaser, source=okx) with fetch_raw returning ~10 deterministic daily_pnl trade dicts spanning consecutive days in ONE calendar year, compute_metrics returning a REAL `MetricsSnapshot` with sentinel values (e.g. sharpe=9.9, twr=9.9, ytd=9.9, max_drawdown=9.9, total_pnl=550.0, trade_count=10, win_rate=0.7), and `services.strategy_matching.find_matched_strategy` patched → None. Capture the `transition_strategy_verification` call with `p_new_status == "metrics_captured"` and read `p_metadata["metrics_snapshot"]`. In-test, independently run `trades_to_daily_returns(same_dicts, account_balance=None)` → `derive_basis_series(returns, None, periods_per_year=365, cumulative_method="geometric", day_basis="calendar")` and assert the persisted `twr == metrics_json["cumulative_return"]`, `sharpe == metrics_json["sharpe"]`, `ytd == metrics_json["metrics_json"]["ytd"]`, `max_drawdown == metrics_json["max_drawdown"]` — AND that none of them equals the 9.9 builder sentinel (proves override, kills the "helper exists but call site doesn't invoke it" neuter). Trade-level keys still carry the builder values (total_pnl==550.0, trade_count==10, win_rate==0.7 — D5).
    - Test W2 (crypto √365 pin, D2/FLAG C): same capture; additionally derive with periods_per_year=252 on the same series and assert persisted sharpe != the √252 sharpe (fixture must have non-zero vol so the two differ).
    - Test C (equity_curve coherence): persisted `equity_curve` == cumprod walk over the in-test derive's `series_rows` (`[{"date": row_date, "value": running_prod}]`), NOT the old raw-returns cumprod with timestamp keys.
    - Test A (FLAG A pin, unit): `_derive_return_scalars(known_series, "crypto")` returns a 4-key dict whose `ytd` equals `derive_basis_series(...).metrics_json["metrics_json"]["ytd"]`, and assert `"ytd" not in result.metrics_json` (top level) — if metrics.py ever flattens ytd, this fails loudly and the map gets revisited.
    - Test R (FLAG C resolver, unit): `_resolve_asset_class("okx"|"binance"|"bybit", ...)` == "crypto" WITHOUT touching supabase (assert `fake.table.assert_not_called()`); `_resolve_asset_class("csv", "s1", fake)` selects `asset_class` from `strategies` and returns the row value; a raising/empty lookup → None (→ 252, the DB DEFAULT 'traditional' fallback).
    - Test D (degrade, D6): (a) unit — `_derive_return_scalars` propagates ValueError on a 2-row series with 1 NaN (<2 finite); (b) route — patch `routers.process_key._derive_return_scalars` with side_effect=ValueError and assert the persisted snapshot has twr/sharpe/ytd/max_drawdown ALL None plus the existing 5 legacy null keys (return_24h/return_mtd/return_ytd/equity_curve/matched_strategy_id), response still 200/published — the ValueError lands on the degrade arm, never a 500; (c) route — fetch_raw returning ONE daily_pnl row (len<2 pre-check) also yields the 4 keys None.
    - Test F (flow-agnostic, D4): flow_type=internal_report, source=binance drives the SAME derived scalars (same wiring-equality assertion) — proves the swap serves the shared block, not a teaser fork.
    NAMED NEUTERS (run locally un-committed, verify each flips the named test RED, then revert): (1) delete the `enriched_metrics_snapshot.update(<4-key dict>)` line → Test W fails; (2) hardcode `periods_per_year=252` in `_derive_return_scalars` → Tests W2 + W fail; (3) remove the `except ValueError` mapping → Test D(b) fails; (4) read `scalars["ytd"]` top-level instead of nested → Tests A + W fail.
  </behavior>
  <action>
    Implement in analytics-service/routers/process_key.py (module-level helpers + surgical call-site restructure — match the file's comment-heavy house style, cite D1–D6/FLAG A/FLAG C in comments):

    1. Top-level imports: `from services.basis_series import derive_basis_series` and `from services.metrics import periods_per_year_for_asset_class`. NEVER import persist_basis_series (grep-gated in verify). Import pandas only if annotating (`import pandas as pd` or TYPE_CHECKING guard).

    2. `_CRYPTO_VENUES = frozenset({"okx", "binance", "bybit", "deribit"})` — module constant with a comment: venue → asset_class is a property of the VENUE, not the flow; only okx/binance/bybit are sync-eligible today (H-11 `:137-143`), deribit included defensively; cross-reference the H-11 whitelist so a future venue admission must classify here.

    3. `def _resolve_asset_class(source: str, strategy_id: str, supabase: Any) -> str | None:` — FLAG C resolution (document the decision in the docstring): crypto exchange venues ⇒ `"crypto"` (matches the #597 backfill convention — api_key-sourced rows are crypto); `source == "csv"` ⇒ read `strategies.asset_class` for the REAL strategy_id present in every sync csv run (`:614` guard) via `.table("strategies").select("asset_class").eq("id", strategy_id).maybe_single().execute()` — the SAME signal finalize reads (analytics_runner.py:2313-2314), keeping preview and finalize annualization coherent; lookup failure/None row ⇒ return None (fail-soft to `periods_per_year_for_asset_class(None)` = 252 — the DB column's own DEFAULT 'traditional'). This branches on `source` (the axis H-11 already branches on) — NOT on flow_type (anti-cosplay `:470-473` intact).

    4. `def _derive_return_scalars(returns, asset_class):` → returns `(four_key_dict, equity_curve_list)`. Body: single `derive_basis_series(returns, None, periods_per_year=periods_per_year_for_asset_class(asset_class), cumulative_method="geometric", day_basis="calendar")` call (defaults per the analytics_runner seam `:2316-2317` — the teaser has no denominator_config); `scalars = dict(result.metrics_json)`; the 4-key map `{"twr": scalars["cumulative_return"], "sharpe": scalars["sharpe"], "ytd": scalars["metrics_json"]["ytd"], "max_drawdown": scalars["max_drawdown"]}` — direct indexing, with a FLAG-A comment on the nested ytd; equity_curve from `result.series_rows` via a running cumprod (`value *= 1 + row["return"]`; entries `{"date": row["date"], "value": float(value)}`) — coherent with `cumulative_return` by construction (geometric: absent gap days are multiplicative identity). Let derive's ValueError PROPAGATE (docstring: caller maps it to the degrade arm, D6).

    5. Call-site restructure (`:888-928`): immediately after `enriched_metrics_snapshot = _metrics_to_jsonb(metrics)`, null the four return-based keys (`twr/sharpe/ytd/max_drawdown` → None) with a D1 comment: the builder's return-based values are DEAD — they must NEVER persist (off-backbone); the builder now serves ONLY trade-level stats + the `:973` fingerprint. Inside the existing enrichment try: after the `len(returns) >= 2` pre-check (KEEP it — it guards None/empty pre-sanitize; derive's ValueError guards <2 FINITE post-sanitize — two different boundaries, one shared degrade arm), wrap `_derive_return_scalars(returns, _resolve_asset_class(body.source, strategy_id, supabase))` in `try/except ValueError` routing to the SAME degrade-to-nulls arm as the `len<2` else (restructure so both paths emit one identical null-out dict — no duplicated literals). On success: `enriched_metrics_snapshot.update(four_keys)` FIRST (so a later matching/period-returns exception never strands builder scalars), then the existing `compute_period_returns` + `find_matched_strategy` enrichment with `equity_curve` now taken from the helper's return (DELETE the old `(1 + returns).cumprod()` block `:901-905`). The degrade arm keeps its existing 5 legacy null keys (the 4 snapshot keys are already pre-nulled — note this in the comment). The best-effort generic `except Exception` (`:929-939`) stays: post-pre-null it can no longer persist off-backbone scalars.

    Do NOT touch: `metrics = adapter.compute_metrics(trades)` (`:867`), `_metrics_to_jsonb`, the fingerprint (`:973`), the encrypt/positions/publish tail (`:950-998`), the H-04 TS sanitizer, long_fetch.py, verify-strategy/route.ts. No new snapshot keys — the map writes only the 4 keys already in the MetricsSnapshot shape (public-surface unchanged, ASVS V5).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_process_key.py -x -q && grep -v '^\s*#' routers/process_key.py | grep -c 'persist_basis_series' | grep -qx '0' && grep -v '^\s*#' routers/process_key.py | grep -c 'derive_basis_series' | grep -qvx '0'</automated>
  </verify>
  <done>All new tests green after RED→GREEN commits; every named neuter flips its named test red (verified locally, reverted); no non-comment `persist_basis_series` reference in process_key.py; derive_basis_series wired; existing test_process_key.py tests (incl. `:1205` anchor-injection) still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: No-anchor-persist regression guard + advisory hand-oracle golden</name>
  <files>analytics-service/tests/test_process_key.py, analytics-service/tests/test_teaser_derive_golden.py</files>
  <behavior>
    - Guard test (D1 honesty condition b, in test_process_key.py): drive a FULL successful teaser run (reuse Task 1's Test W scaffolding — multi-day trades so the derive path actually executes, not the degrade arm), then `rpc_calls = [c.args for c in fake.rpc.call_args_list]`; `series_calls = [c for c in rpc_calls if c and c[0] == "upsert_strategy_analytics_series_batch"]`; `assert series_calls == []` — the blanket assertion (STRONGER than filtering by the `00000…0001` anchor: the teaser persists NO series row at all, so any series RPC under ANY id is a regression). Import `TEASER_ANCHOR_STRATEGY_ID` from `services.teaser_anchor` and reference it in the assertion message for greppability. Docstring: cites D1/Option c + why the anchor row would be unreadable (archived, user-less) + PK-colliding.
    - Golden (new tests/test_teaser_derive_golden.py, advisory shift-detector — D2): a fixed ~8-entry daily-return `pd.Series` on CONSECUTIVE calendar days (no gaps → `gap_fill_daily_returns` is identity → paper math clean) within one calendar year (→ ytd == cumulative_return under geometric), tz-aware `[us]` DatetimeIndex. Call `_derive_return_scalars(series, "crypto")` and assert the 4 scalars against FIRST-PRINCIPLES hand-computed values (pattern: test_metrics_minigolden.py — paper, not shared-helper output): cumulative_return = prod(1+r)−1 and max_drawdown from a hand-walked running peak at `pytest.approx(rel=1e-9)`; sharpe at `pytest.approx(rel=1e-6)` against mean/std×√365 matched to the ACTUAL qs.stats.sharpe convention (verify ddof/rf conventions once against the real output, document the convention in a comment — the pin's load-bearing content is the √365 factor + drift detection). Also assert the √252 value would NOT match (the D2 pin). Module docstring MUST state: ADVISORY golden — on an INTENDED convention change (like D2 itself), recompute the paper oracle in the same PR with a citation; an UNEXPLAINED mismatch is a backbone regression (Phase-101 divergence class).
    NAMED NEUTERS: (1) locally insert a `persist_basis_series(supabase, strategy_id, result=...)` call (or a bare `supabase.rpc("upsert_strategy_analytics_series_batch", {...}).execute()`) in the sync block → guard test fails; revert. (2) hardcode 252 in `_derive_return_scalars` → golden sharpe fails; revert.
  </behavior>
  <action>
    Write the guard test RED-first only in spirit — it passes immediately against Task 1's implementation (persist-nothing is already true), so its RED proof IS the neuter run: execute neuter (1), capture the failure output, revert, then commit. Golden: compute the oracle on paper per the behavior block; if the qs sharpe convention differs from naive mean/std×√365, adjust the ORACLE (never the tolerance beyond rel=1e-6) and document. Commit both as `test(105.1-01): no-anchor-persist guard + advisory teaser golden`. git add ONLY analytics-service/tests/test_process_key.py analytics-service/tests/test_teaser_derive_golden.py.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_process_key.py tests/test_teaser_derive_golden.py -x -q</automated>
  </verify>
  <done>Guard asserts zero series-persist RPCs on a full teaser run and demonstrably fails under neuter (1); golden pins all 4 scalars incl. the √365 factor with a documented paper oracle and an advisory-regen docstring; both files committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /process-key (teaser) | Unauthenticated landing-page submissions reach the sync compute block |
| metrics_snapshot → unauthenticated browsers | Persisted preview scalars flow through the H-04 allowlist sanitizer (verify-strategy/route.ts:264-278) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105.1-01 | Tampering/Elevation | teaser write surface | mitigate | Persist-NOTHING by construction (no persist_basis_series call, grep-gated) + the no-anchor-persist regression test; the `:479-481` anchor override (V4 control) untouched |
| T-105.1-02 | Information Disclosure | `_resolve_asset_class` csv-arm SELECT on caller-supplied strategy_id | accept | Reads ONLY `asset_class` ('crypto'/'traditional'), influencing only 252-vs-365; the csv sync flow is authed-wizard and SV-insert-under-arbitrary-id is a pre-existing unchanged surface; no value is echoed to the caller |
| T-105.1-03 | Tampering | metrics_snapshot shape → H-04 sanitizer | mitigate | 4-key map into EXISTING snapshot keys only (D5 — no wholesale spread, no new public keys); pinned by the wiring test asserting the persisted shape |
| T-105.1-SC | Tampering | package installs | accept | No new packages this phase (Package Legitimacy Audit N/A per 105.1-RESEARCH §Sources) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_process_key.py tests/test_teaser_derive_golden.py -x -q` green.
- `grep -v '^\s*#' analytics-service/routers/process_key.py | grep -c 'persist_basis_series'` == 0 (comments may mention it; code may not).
- `grep -n "flow_type == .teaser." analytics-service/routers/process_key.py` shows no NEW occurrence below `:481` (anti-cosplay — only the pre-existing dispatch/validator sites).
- Wave gate: `cd analytics-service && python -m pytest --cov --cov-fail-under=80` green.
</verification>

<success_criteria>
105.1-SC1 (reworded, LOCKED): no sync-pipeline scalar is computed off the backbone — the 4 return-based snapshot fields equal `derive_basis_series` output (√365 crypto / √252 traditional per #597), zero series rows persisted (guard test), degrade-to-nulls on <2 finite rows, flow-agnostic, equity_curve from the same derived series, advisory golden in place. All existing process_key tests green.
</success_criteria>

<output>
Create `.planning/phases/105.1-onboarding-onto-backbone/105.1-01-SUMMARY.md` when done. Record: the FLAG C resolution as implemented (venue map + csv strategies lookup + None→252 fallback), the golden's oracle conventions, and any qs-convention discovery.
</output>
