---
phase: 100-optimizer-favorites-notes-kpi
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260715090000_user_notes_dashboard_scope.sql
  - supabase/tests/test_user_notes_dashboard_scope.sql
  - src/lib/notes/ownership.ts
  - src/app/api/notes/route.ts
  - src/app/(dashboard)/allocations/components/DashboardNoteCard.tsx
  - src/app/(dashboard)/allocations/components/DashboardNoteCard.test.tsx
autonomous: true
requirements: [PI-04]
must_haves:
  truths:
    - "An allocator can type a private note scoped to the /allocations book and it autosaves via the existing /api/notes PATCH"
    - "scope_kind='dashboard' rows are owner-only: user B can never read/write user A's dashboard note (RLS, CI-authoritative SQL proof)"
    - "Empty note shows the honest placeholder — no sample text, no fabricated content"
  artifacts:
    - path: "supabase/migrations/20260715090000_user_notes_dashboard_scope.sql"
      provides: "additive scope_kind CHECK extension adding 'dashboard'"
      contains: "user_notes_scope_kind_check"
    - path: "supabase/tests/test_user_notes_dashboard_scope.sql"
      provides: "CI-authoritative RLS owner-scope proof for the new scope"
    - path: "src/app/(dashboard)/allocations/components/DashboardNoteCard.tsx"
      provides: "always-editable autosave note card (scope dashboard/allocations)"
      min_lines: 40
  key_links:
    - from: "src/app/(dashboard)/allocations/components/DashboardNoteCard.tsx"
      to: "src/components/notes/useNoteAutoSave.ts"
      via: "useNoteAutoSave('dashboard','allocations',…)"
      pattern: "useNoteAutoSave\\(\\s*[\"']dashboard[\"']"
    - from: "src/lib/notes/ownership.ts"
      to: "scope_kind='dashboard'"
      via: "checkScopeOwnership dashboard arm"
      pattern: "case \"dashboard\""
---

<objective>
PI-04 storage arm + widget: ONE additive migration extends the `user_notes` scope_kind CHECK with `dashboard` (per locked decision — reuse existing table, NO new table), adds the `dashboard` arm to `checkScopeOwnership` and the route's scope allow-list, proves owner-only RLS with a CI-authoritative SQL test, and builds `DashboardNoteCard` (clone of `StrategyNoteCard`, reusing `useNoteAutoSave`/`NoteRender`/`NoteSaveStatus`/`/api/notes` verbatim). Page wiring happens in plan 100-04.

Purpose: the Notes widget is the allocator's "act on it" scratchpad on the demo-hero surface.
Output: migration + SQL RLS test + ownership/route arms + DashboardNoteCard + render/autosave test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-optimizer-favorites-notes-kpi/100-UI-SPEC.md   (W1 section = THE contract)
@.planning/phases/100-optimizer-favorites-notes-kpi/100-RESEARCH.md
@DESIGN.md

<interfaces>
From supabase/migrations/20260421060316_user_notes_multiscope.sql:65-69 (LATEST def of the constraint — re-base on this, per the grep-all-migrations rule):
```sql
ALTER TABLE user_notes DROP CONSTRAINT IF EXISTS user_notes_scope_kind_check;
ALTER TABLE user_notes ADD CONSTRAINT user_notes_scope_kind_check
  CHECK (scope_kind IN ('portfolio','holding','bridge_outcome','strategy'));
```

From src/lib/notes/ownership.ts:30:
```typescript
export type ScopeKind = "portfolio" | "holding" | "bridge_outcome" | "strategy";
export async function checkScopeOwnership(supabase, userId, scope_kind: ScopeKind, scope_ref): Promise<OwnershipCheckResult>
// switch arms at :44 — portfolio | holding | bridge_outcome | strategy
```

From src/app/api/notes/route.ts:53-56: allow-list array `["portfolio","holding","bridge_outcome","strategy"]`.
Route.ts:73-81: per-scope key helper — `scope_kind === "holding" ? userId : scope_ref` (holding has no single aggregate row → caller's user_id).

From src/components/notes/StrategyNoteCard.tsx:24-45 (the clone source):
```typescript
export interface StrategyNoteCardProps { strategyId: string; initialContent: string; initialLastSavedAt: Date | null; }
const { saveState, lastSavedAt, save } = useNoteAutoSave("strategy", props.strategyId, props.initialLastSavedAt);
// save-on-blur; NoteRender for sanitized markdown; NoteSaveStatus for "Saved Ns ago"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboard scope_kind — migration + ownership/route arms + CI-authoritative RLS SQL test</name>
  <files>supabase/migrations/20260715090000_user_notes_dashboard_scope.sql, supabase/tests/test_user_notes_dashboard_scope.sql, src/lib/notes/ownership.ts, src/app/api/notes/route.ts</files>
  <read_first>supabase/migrations/20260421060316_user_notes_multiscope.sql (latest constraint def), src/lib/notes/ownership.ts (full switch), src/app/api/notes/route.ts (allow-list + key helper), one existing supabase/tests/test_*.sql for the harness idiom, UI-SPEC W1</read_first>
  <behavior>
    - test_user_notes_dashboard_scope.sql: user A inserts a row (scope_kind='dashboard', scope_ref='allocations'); as user B: SELECT returns 0 rows, UPDATE affects 0 rows, INSERT with A's user_id is rejected by RLS; CHECK accepts 'dashboard' and rejects an arbitrary string like 'bogus'.
  </behavior>
  <action>
    Migration `20260715090000_user_notes_dashboard_scope.sql` (timestamp MUST exceed latest `20260714090000` — verify with `ls -t supabase/migrations/ | head -1` first). Content: DROP + re-ADD `user_notes_scope_kind_check` with the five-value set ('portfolio','holding','bridge_outcome','strategy','dashboard'), based on the 20260421060316 definition (grep ALL migrations for other defs of this constraint first; re-base on the latest). Update the COLUMN comments to include `dashboard` (scope_ref = literal 'allocations'). Additive only — existing rows untouched, no data migration. Header comment: rationale per UI-SPEC W1 (user-scoped book note; portfolio scope breaks for 0-portfolio users).
    ownership.ts: extend `ScopeKind` union with "dashboard"; add a `case "dashboard"` arm — valid iff `scope_ref === 'allocations'` (else the same invalid/403 result shape the other arms return); ownership is trivially the authed user (RLS `user_id = auth.uid()` carries the real gate) — mirror the existing arm return shapes exactly.
    route.ts: add "dashboard" to the scope allow-list (line ~53). PRESCRIPTIVE — `resolveEntityId(scope_kind, scope_ref, userId)` (route.ts:76-82, consumed at route.ts:258 as the UUID-typed `audit_log.entity_id`) currently returns `scope_kind === "holding" ? userId : scope_ref`. Change it to include `dashboard` in the userId branch: `return (scope_kind === "holding" || scope_kind === "dashboard") ? userId : scope_ref;`. Rationale: dashboard's scope_ref is the literal `'allocations'` — NOT a UUID (breaks the UUID-typed `audit_log.entity_id` column) AND constant across ALL users (a cross-user audit key). The per-user upsert being safe (`onConflict: user_id,scope_kind,scope_ref`) does NOT clear this second consumer. So the dashboard note's `entity_id` MUST be the caller's `userId`. Update the resolveEntityId doc-comment (route.ts:72-75) to note holding AND dashboard have no single aggregate UUID row → caller's user_id.
    Regenerate `src/lib/database.types.ts` (`npm run` script or supabase gen) and commit ONLY if it diffs (a CHECK change usually doesn't alter generated types — do not commit a no-op).
    SQL test per behavior above, modeled on an existing supabase/tests/test_*.sql harness. This is the CI-authoritative gate (vitest live-DB notes tests SKIP in CI).
    Migration discipline (per standing invariants): migrations auto-apply to prod on merge — flag in the SUMMARY that migration-reviewer + rls-policy-auditor + test-project MCP catch-up must run before merge.
  </action>
  <verify>
    <automated>ls supabase/migrations/20260715090000_user_notes_dashboard_scope.sql && grep -q "'dashboard'" supabase/migrations/20260715090000_user_notes_dashboard_scope.sql && grep -q 'case "dashboard"' src/lib/notes/ownership.ts && grep -q '"dashboard"' src/app/api/notes/route.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Migration + SQL RLS test exist; ownership.ts and route.ts accept `dashboard`/'allocations'; `resolveEntityId('dashboard', …, userId)` returns `userId` (NOT the literal 'allocations'); the RLS SQL test proves user A cannot read/write user B's dashboard note; tsc green; no CREATE TABLE anywhere (reuse-only).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardNoteCard + render/autosave test</name>
  <files>src/app/(dashboard)/allocations/components/DashboardNoteCard.tsx, src/app/(dashboard)/allocations/components/DashboardNoteCard.test.tsx</files>
  <read_first>src/components/notes/StrategyNoteCard.tsx (clone source, full file), src/components/notes/useNoteAutoSave.ts, NoteRender.tsx, NoteSaveStatus.tsx, an existing notes component test for the fetch-mock idiom, UI-SPEC W1, DESIGN.md tokens</read_first>
  <behavior>
    - Renders heading "Notes" + sub-caption "Private — visible only to you." + NoteSaveStatus.
    - Empty content → textarea with placeholder "Add a private note about your allocation book — markdown supported. Visible only to you." and NO rendered preview, no sample text.
    - Typing then blur fires exactly ONE PATCH /api/notes with body {scope_kind:'dashboard', scope_ref:'allocations', content} (assert the fetch mock's call payload).
    - Non-empty content → sanitized markdown preview (NoteRender) below the textarea.
  </behavior>
  <action>
    Create `DashboardNoteCard` cloned structurally from StrategyNoteCard but per UI-SPEC W1: props `{ initialContent: string; initialLastSavedAt: Date | null }` (no id — scope is fixed). `useNoteAutoSave("dashboard", "allocations", initialLastSavedAt)`. ALWAYS-EDITABLE textarea (drop StrategyNoteCard's `editing` toggle — autosave removes the Save/edit-mode need), min-height ~6 rows, `text-body` DM Sans, focus ring border-focus. Card chrome: white surface, 1px #E2E8F0, radius-lg, padding 24px (reuse the existing Card component if that is the codebase idiom — match StrategyNoteCard's wrapper). Heading row: "Notes" text-h3 font-semibold + NoteSaveStatus right-aligned. Below-fold NoteRender preview ONLY when content non-empty. No raw text-[Npx] (lint error). Honest-empty per must_haves.
  </action>
  <verify>
    <automated>npm run test -- "src/app/(dashboard)/allocations/components/DashboardNoteCard.test.tsx" && npm run lint</automated>
  </verify>
  <done>Component + test green; PATCH payload asserted verbatim; placeholder/preview behavior per UI-SPEC; lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/notes | untrusted scope_kind/scope_ref/content |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Elevation (IDOR) | user_notes dashboard scope | mitigate | RLS user_id=auth.uid() + checkScopeOwnership dashboard arm (scope_ref must equal 'allocations'); SQL test proves cross-user denial |
| T-100-02 | DoS | /api/notes PATCH | accept (existing) | pre-existing 200KB request cap + 100KB content cap + notesUpsertLimiter cover the new scope unchanged |
| T-100-03 | Tampering | migration auto-apply to prod | mitigate | additive-only CHECK change; migration-reviewer + rls-auditor + test-project MCP catch-up before merge |
</threat_model>

<verification>
- `npm run test -- "src/app/(dashboard)/allocations/components/DashboardNoteCard.test.tsx"` green; `npx tsc --noEmit`; `npm run lint`.
- SQL test file present and shaped like the existing supabase/tests harness (runs in CI's sql-tests job).
- `grep -rn "CREATE TABLE user_notes" supabase/migrations/2026071*` → only the historical files; the new migration contains no CREATE TABLE.
</verification>

<success_criteria>
Dashboard scope lands additively (existing 4 scopes + rows untouched); owner-only proven in CI; DashboardNoteCard reuses the whole existing notes stack with zero new endpoints; honest-empty verbatim per UI-SPEC.
</success_criteria>

<output>
Create `.planning/phases/100-optimizer-favorites-notes-kpi/100-01-SUMMARY.md` when done.
</output>
