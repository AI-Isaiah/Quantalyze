---
phase: 100-optimizer-favorites-notes-kpi
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/kpi/KpiPanel.tsx
  - src/app/(dashboard)/allocations/components/KpiStrip.tsx
  - src/components/portfolio/PortfolioKpiPanel.tsx
  - src/components/portfolio/PortfolioKpiPanel.test.tsx
  - src/components/portfolio/PortfolioKPIRow.tsx
  - src/app/(dashboard)/portfolios/[id]/page.tsx
autonomous: true
requirements: [PI-06]
must_haves:
  truths:
    - "/portfolios/[id] KPI values (AUM, MTD TWR, Avg Correlation, Portfolio Sharpe) render identically to before the fold for the same PortfolioAnalytics input"
    - "KpiStrip on /allocations is byte-identical in behavior — all its existing tests pass unmodified"
    - "PortfolioKPIRow.tsx (the 4-centered-Cards anti-pattern) no longer exists in the tree"
    - "The MTD label stays MTD — never relabeled YTD; AUM cell is kept"
  artifacts:
    - path: "src/components/kpi/KpiPanel.tsx"
      provides: "shared presentational panel primitive (one white panel, N cells, hairline dividers)"
      exports: ["KpiPanel"]
    - path: "src/components/portfolio/PortfolioKpiPanel.tsx"
      provides: "PortfolioAnalytics→KpiPanel adapter (AUM/MTD/corr/Sharpe)"
      exports: ["PortfolioKpiPanel"]
  key_links:
    - from: "src/app/(dashboard)/allocations/components/KpiStrip.tsx"
      to: "src/components/kpi/KpiPanel.tsx"
      via: "KpiStrip renders through the extracted primitive"
      pattern: "KpiPanel"
    - from: "src/app/(dashboard)/portfolios/[id]/page.tsx"
      to: "src/components/portfolio/PortfolioKpiPanel.tsx"
      via: "adapter replaces <PortfolioKPIRow/> at the sole call site (~line 291)"
      pattern: "PortfolioKpiPanel"
---

<objective>
PI-06: extract a shared `KpiPanel` primitive from `KpiStrip.tsx` (KpiStrip behavior byte-identical, its tests green unmodified), build the `PortfolioKpiPanel` adapter mapping `PortfolioAnalytics` onto it, swap it in at the sole call site `portfolios/[id]/page.tsx:291`, and DELETE `PortfolioKPIRow.tsx`. No /allocations KPI change — KpiStrip already serves that surface.

Purpose: removes the divergent bespoke 4-centered-Cards KPI surface (the DESIGN.md anti-pattern) without regressing either page.
Output: KpiPanel primitive + adapter + call-site swap + deletion + field-mapping regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-optimizer-favorites-notes-kpi/100-UI-SPEC.md   (W4 = THE contract incl. the explicit field-mapping table)
@DESIGN.md

<interfaces>
From src/app/(dashboard)/allocations/components/KpiStrip.tsx:48-55:
```typescript
export interface KpiStripAnalytics { ytd_twr?: number|null; sharpe?: number|null; max_drawdown_12m?: number|null; avg_correlation?: number|null; }
interface KpiStripProps { analytics: KpiStripAnalytics | null; metrics: ComputedMetrics; … }
// internal: Cell interface (:113), warmupCopy, valueColorClass, delta pill helpers — KpiStrip-ONLY logic, stays in KpiStrip
```

From src/components/portfolio/PortfolioKPIRow.tsx: `PortfolioKPIRowProps { analytics: PortfolioAnalytics }`; sole consumer `src/app/(dashboard)/portfolios/[id]/page.tsx:291` (`<PortfolioKPIRow analytics={analytics} />`).

PortfolioAnalytics fields (src/lib/types.ts:1260-1268): total_aum, portfolio_sharpe, avg_pairwise_correlation, return_mtd — all `number | null`.

UI-SPEC W4 mapping (MTD ≠ YTD, never conflate):
| Cell | Source | Formatter | Color |
| AUM | total_aum | formatCurrency | text-text-primary |
| MTD TWR | return_mtd | formatPercent | metricColor |
| Avg Correlation | avg_pairwise_correlation | formatNumber | correlationColor preserved VERBATIM (≥0.7-red is pre-existing risk signaling — flagged, not changed) |
| Portfolio Sharpe | portfolio_sharpe | formatNumber | metricColor |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract KpiPanel primitive; KpiStrip consumes it render-tree-neutrally</name>
  <files>src/components/kpi/KpiPanel.tsx, src/app/(dashboard)/allocations/components/KpiStrip.tsx</files>
  <read_first>src/app/(dashboard)/allocations/components/KpiStrip.tsx (FULL — identify the pure presentational shell: one white panel, N columned cells with micro uppercase muted label + Geist Mono tabular-nums value + optional sub-line, hairline dividers), KpiStrip's existing test file(s), UI-SPEC W4 step 1</read_first>
  <action>
    Extract ONLY the presentational panel/cell renderer into `src/components/kpi/KpiPanel.tsx` (props: an array of cell descriptors — label, value string, valueClassName, optional sub/aria bits — derive the exact descriptor shape from what KpiStrip's render actually needs; do NOT move warmup/stale/scenario/delta logic, formatters, or copy — those stay in KpiStrip and feed the primitive as already-resolved strings/classes). Rewire KpiStrip to render through KpiPanel such that the emitted DOM is unchanged (render-tree-neutral: same elements/classes/order). KpiStrip's existing tests MUST pass UNMODIFIED — if a test needs editing, the extraction changed behavior; fix the extraction instead. KpiStripAnalytics export and KpiStrip's public props untouched.
  </action>
  <verify>
    <automated>npm run test -- KpiStrip && npx tsc --noEmit</automated>
  </verify>
  <done>KpiPanel exists and is KpiStrip's render path; all pre-existing KpiStrip tests green with zero test-file edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PortfolioKpiPanel adapter + call-site swap + delete PortfolioKPIRow</name>
  <files>src/components/portfolio/PortfolioKpiPanel.tsx, src/components/portfolio/PortfolioKpiPanel.test.tsx, src/app/(dashboard)/portfolios/[id]/page.tsx, src/components/portfolio/PortfolioKPIRow.tsx</files>
  <read_first>src/components/portfolio/PortfolioKPIRow.tsx (formatters + metricColor/correlationColor usage — the byte-identical value contract), portfolios/[id]/page.tsx:285-295, any existing PortfolioKPIRow test (its value assertions migrate into the new regression test), UI-SPEC W4 steps 2 + mapping table</read_first>
  <behavior>
    - Regression test with a fixed PortfolioAnalytics fixture (mixed real + null values): rendered AUM/MTD/corr/Sharpe strings are IDENTICAL to PortfolioKPIRow's outputs (same formatters: formatCurrency/formatPercent/formatNumber; null → "—").
    - Test FAILS if the MTD value is labeled YTD (assert label text contains "MTD", document/assert no "YTD" label) or if the AUM cell is dropped (assert 4 cells incl. AUM).
    - correlationColor semantics preserved verbatim (≥0.7 red case asserted — pre-existing risk signaling, no-regress).
  </behavior>
  <action>
    Create `PortfolioKpiPanel({ analytics }: { analytics: PortfolioAnalytics })` mapping the four cells per the UI-SPEC W4 table onto `KpiPanel` — identical formatter calls and color helpers lifted from PortfolioKPIRow (import the same helpers; do not reimplement). Cell typography adopts the KpiPanel/KpiStrip contract (micro uppercase muted label + Geist Mono tabular-nums value), replacing the centered text-2xl cards — VALUES byte-identical, presentation intentionally changed. Swap the call site at portfolios/[id]/page.tsx:291 (`<PortfolioKpiPanel analytics={analytics} />`, keep the surrounding comment intent). DELETE `src/components/portfolio/PortfolioKPIRow.tsx`; if it has its own test file, migrate any value assertions into PortfolioKpiPanel.test.tsx then delete it (deletion is the locked intent — no AskUser gate needed; cite UI-SPEC W4). Verify no other importer: `grep -rn "PortfolioKPIRow" src/` must return nothing after.
  </action>
  <verify>
    <automated>npm run test -- src/components/portfolio/PortfolioKpiPanel.test.tsx && bash -c '! grep -rn "PortfolioKPIRow" src/' && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Adapter live at the sole call site; PortfolioKPIRow gone tree-wide; regression test proves value byte-identity + MTD label + AUM kept; lint/tsc green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| none new | pure presentational refactor; server-computed values pass through unchanged |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-08 | Tampering (silent value drift) | field mapping MTD/YTD/AUM | mitigate | fixture regression test that fails on relabel or drop |
</threat_model>

<verification>
- KpiStrip suite green with zero test edits; PortfolioKpiPanel regression green; `grep -rn "PortfolioKPIRow" src/` empty; tsc + lint green.
</verification>

<success_criteria>
The bespoke KPI surface is deleted, both pages render through one shared primitive, and no metric value/label/color changed on either page.
</success_criteria>

<output>
Create `.planning/phases/100-optimizer-favorites-notes-kpi/100-03-SUMMARY.md` when done.
</output>
