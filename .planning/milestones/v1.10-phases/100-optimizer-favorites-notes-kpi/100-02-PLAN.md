---
phase: 100-optimizer-favorites-notes-kpi
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/watchlist-read.ts
  - src/app/(dashboard)/allocations/components/WatchlistPanel.tsx
  - src/app/(dashboard)/allocations/components/WatchlistPanel.test.tsx
  - src/app/(dashboard)/allocations/components/OptimizerPanel.tsx
  - src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx
  - src/components/portfolio/PortfolioOptimizer.tsx
autonomous: true
requirements: [PI-05]
must_haves:
  truths:
    - "Watchlist renders real user_favorites rows as a dense table; bulk-select removes N favorites via the existing idempotent PUT (one call per id, per-row rollback on failure)"
    - "Optimizer suggestions render as a ranked list sorted by score desc — no pie, no weights, no % allocation framing anywhere"
    - "0 portfolios / 0 favorites / service-down all show honest states, never fabricated rows"
    - "Every metric label carries its verbatim narrative tooltip; the suggestions footer disclaimer is present on /allocations"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/watchlist-read.ts"
      provides: "server reads: getFavoritesWithStrategies + getOptimizerPrefetch (exported types = the plan-04 contract)"
      exports: ["getFavoritesWithStrategies", "getOptimizerPrefetch", "FavoriteRow", "OptimizerPrefetch"]
    - path: "src/app/(dashboard)/allocations/components/WatchlistPanel.tsx"
      provides: "favorites table: sort recency/name, group none/trust-tier, bulk remove, Suggested chip"
      min_lines: 80
    - path: "src/app/(dashboard)/allocations/components/OptimizerPanel.tsx"
      provides: "thin wrapper: portfolio selector + 0-portfolio gate + footer around reused PortfolioOptimizer"
      min_lines: 40
  key_links:
    - from: "src/app/(dashboard)/allocations/components/WatchlistPanel.tsx"
      to: "/api/watchlist/[strategyId]"
      via: "client loop of idempotent PUT {action:'remove'}"
      pattern: "api/watchlist"
    - from: "src/app/(dashboard)/allocations/components/OptimizerPanel.tsx"
      to: "src/components/portfolio/PortfolioOptimizer.tsx"
      via: "next/dynamic mount, key={portfolioId}"
      pattern: "PortfolioOptimizer"
---

<objective>
PI-05 components (no page wiring — that is plan 100-04): the Watchlist panel (favorites finally rendered on /allocations) and the Optimizer panel (REUSE `PortfolioOptimizer.tsx` wholesale behind a thin wrapper), plus the server-read module whose exported types are the contract plan 100-04 wires into `page.tsx`.

Purpose: favorites + real optimizer scores are the demo-hero "act on it" moment.
Output: watchlist-read.ts (interface-first), WatchlistPanel, OptimizerPanel (+ tooltips on PortfolioOptimizer metric labels), render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-optimizer-favorites-notes-kpi/100-UI-SPEC.md   (W2 + W3 = THE contract)
@.planning/phases/100-optimizer-favorites-notes-kpi/100-RESEARCH.md
@DESIGN.md

<interfaces>
From src/components/portfolio/PortfolioOptimizer.tsx:11-27:
```typescript
export type OptimizerSuggestion = { strategy_id: string; strategy_name: string; corr_with_portfolio: number | null; sharpe_lift: number | null; dd_improvement: number | null; score: number | null; };
type ComputationStatus = "pending" | "computing" | "complete" | "failed" | null;
export interface PortfolioOptimizerProps { portfolioId: string; initialSuggestions: OptimizerSuggestion[] | null; computedAt: string | null; computationStatus: ComputationStatus; }
```
It already solves pending/computing/failed/stale(7d)/skeleton + the refresh POST /api/portfolio-optimizer. Mounted on /portfolios/[id] via next/dynamic (page.tsx:68) with `initialSuggestions={analytics.optimizer_suggestions}`, `computedAt={analytics.computed_at}`, `computationStatus` (page.tsx:453-456 shows the persisted-read shape).

From src/app/api/watchlist/[strategyId]/route.ts: `PUT` body `{ action: "add" | "remove" }` — add = upsert onConflict (user_id,strategy_id) ignoreDuplicates; remove = naturally idempotent delete (0-row delete → 200). CSRF + rate-limited.

user_favorites (mig 20260409202757): PK (user_id, strategy_id), owner-only RLS, has created_at.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: watchlist-read.ts — server reads + exported contract types</name>
  <files>src/app/(dashboard)/allocations/lib/watchlist-read.ts, src/app/(dashboard)/allocations/lib/watchlist-read.test.ts</files>
  <read_first>src/app/(dashboard)/allocations/lib/exposure-props.ts + the Phase-98 read layer (src/lib/portfolio-exposure.ts) for the read-module idiom (RLS user client, THROW on PostgREST error — never collapse error into empty), supabase/migrations/20260409202757_user_favorites.sql, portfolios/[id]/page.tsx:195-210 + 450-456 (optimizer persisted-read shape)</read_first>
  <behavior>
    - getFavoritesWithStrategies(supabase, userId) → FavoriteRow[]: user_favorites joined to strategies for {strategy_id, name, slug, trust_tier, created_at(favorited)}. Real columns only; [] for no favorites; THROWS on PostgREST error.
    - getOptimizerPrefetch(supabase, userId) → OptimizerPrefetch: { portfolios: {id,name,updated_at}[], defaultPortfolioId (most recently updated) | null, initialSuggestions, computedAt, computationStatus } — mirrors the exact read /portfolios/[id]/page.tsx performs (portfolio_analytics.optimizer_suggestions / computed_at / computation_status for the default portfolio). 0 portfolios → portfolios: [], defaultPortfolioId: null.
  </behavior>
  <action>
    Create the module with both reads + exported `FavoriteRow` / `OptimizerPrefetch` types (interface-first: plan 100-04 imports these). RLS user client (owner-scoped — never admin client). Follow the exposure read-layer's error discipline verbatim (throw reaches allocations/error.tsx). Keep the strategies join to published/visible strategies consistent with however /discovery or the watchlist writes treat strategy visibility — read the strategies select idiom in an existing allocator read before choosing columns/filters. Add a COLOCATED `watchlist-read.test.ts` (mocked Supabase client) covering the branch-heavy paths the render tests will NOT exercise — the ratchet (`branches 72`) is enforced at wave-2 (100-04) where this module can't be cleanly fixed, so cover it here: (a) THROW on PostgREST error for BOTH reads (mock an error response → expect throw, never empty), (b) 0-portfolio path → `getOptimizerPrefetch` returns `{ portfolios: [], defaultPortfolioId: null, initialSuggestions: null, … }`, (c) the default-portfolio pick (most-recently-updated selected among ≥2 portfolios), (d) 0-favorites → `[]`.
  </action>
  <verify>
    <automated>npm run test -- "src/app/(dashboard)/allocations/lib/watchlist-read.test.ts" && npx tsc --noEmit && grep -c "export" "src/app/(dashboard)/allocations/lib/watchlist-read.ts" | awk '$1>=4'</automated>
  </verify>
  <done>Module exports both reads + both types; colocated test covers both throw paths + null-default-portfolio + default-pick + empty-favorites (branch ratchet safe); tsc green; RLS-client, throw-on-error discipline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WatchlistPanel — dense table, sort/group, bulk remove, Suggested chip</name>
  <files>src/app/(dashboard)/allocations/components/WatchlistPanel.tsx, src/app/(dashboard)/allocations/components/WatchlistPanel.test.tsx</files>
  <read_first>UI-SPEC W2 (the full layout/column/interaction contract), DESIGN.md (table + badge + button tokens), TrustTierLabel component (grep its import path), an existing allocations client panel for the Card/table idiom, src/app/api/watchlist/[strategyId]/route.ts</read_first>
  <behavior>
    - Honest-empty: heading kept; body copy exactly "No favorites yet. Star strategies in Discovery to build your watchlist." + secondary link-button "Browse strategies →" to /discovery; zero ghost/skeleton rows.
    - Sort: default recency (created_at desc); segmented control switches to Name A–Z. NO score sort.
    - Group: None (default) / Verification tier — groups by trust_tier with micro uppercase headers; no invented asset-class groups.
    - Bulk: selecting ≥1 rows reveals secondary sm button "Remove N from watchlist"; clicking issues ONE PUT {action:'remove'} PER selected id (assert fetch mock called N times with the right URLs — call-site wiring proof, not a helper test); optimistic removal; a failed id's row is ROLLED BACK while successes stay removed; role="status" announces "Removed N strategies from watchlist".
    - "Suggested" chip renders iff strategy_id ∈ suggestedIds prop (cross-link to optimizer rows; real data only).
    - Star toggle: aria-pressed, fill+shape carries state (never color alone); per-row unfavorite via the same PUT.
  </behavior>
  <action>
    Client component, props `{ favorites: FavoriteRow[]; suggestedIds: string[] }` (import types from watchlist-read.ts). One panel Card containing a borderless dense TABLE per UI-SPEC W2 column spec (checkbox 44px target / Strategy name-link + TrustTierLabel / Added date Geist Mono caption / Suggested chip / star toggle). Tokens per UI-SPEC sheet: text-small cells, micro uppercase labels, accent #1B6B5A via the existing token classes, badge-ladder chip (4px radius, micro uppercase, accent border/text). NO destructive-red on bulk remove (reversible action → plain secondary). No raw text-[Npx].
  </action>
  <verify>
    <automated>npm run test -- "src/app/(dashboard)/allocations/components/WatchlistPanel.test.tsx" && npm run lint</automated>
  </verify>
  <done>All behaviors above asserted in the test (including per-id PUT loop + per-row rollback + empty copy verbatim); lint green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OptimizerPanel — selector + 0-portfolio gate + footer; tooltips on PortfolioOptimizer labels</name>
  <files>src/app/(dashboard)/allocations/components/OptimizerPanel.tsx, src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx, src/components/portfolio/PortfolioOptimizer.tsx</files>
  <read_first>UI-SPEC W3 (verbatim tooltip + footer + gate copy), src/components/portfolio/PortfolioOptimizer.tsx (FULL — its states are reused, not rebuilt), portfolios/[id]/page.tsx:68 (dynamic-mount idiom)</read_first>
  <behavior>
    - 0 portfolios → gate copy exactly "Optimizer suggestions need a portfolio to optimize against. Create one to see which strategies would improve it." + secondary "Create portfolio →" (/portfolios); PortfolioOptimizer NOT mounted; no fake suggestions.
    - ≥2 portfolios → compact select in the heading row (input tokens); 1 portfolio → no select. Switching remounts PortfolioOptimizer (key={portfolioId}) with null initials so its own pending flow runs.
    - Suggestions render via PortfolioOptimizer's existing ranked rows sorted score desc from an OptimizerSuggestion[] fixture; assert ABSENCE of any pie/donut/weight-bar/% -allocation framing (no element text matching /weight|allocation %/i, no pie svg) in the panel output.
    - Footer caption present verbatim: "Ranked by modeled fit from historical daily returns — suggestions, not an allocation and not a forecast."
    - Four metric-label tooltips verbatim (Score / Corr w/ portfolio / Sharpe lift / DD improvement per UI-SPEC W3) — accessible: focusable trigger + aria-describedby, title fallback acceptable.
  </behavior>
  <action>
    OptimizerPanel: thin client wrapper, props `{ prefetch: OptimizerPrefetch }`. Owns selector + gate + footer caption (text-caption text-text-muted); mounts PortfolioOptimizer via next/dynamic exactly like portfolios/[id]/page.tsx:68, passing initialSuggestions/computedAt/computationStatus from prefetch for the default portfolio, nulls after a switch. NEVER calls /api/optimize-weights (scores, not weights — honesty contract). All degraded states (never-computed / 503-504 failed / stale>7d amber) are PortfolioOptimizer's existing behavior — do not reimplement.
    PortfolioOptimizer.tsx: ADD the four narrative tooltips to its metric header labels (UI-SPEC W3 verbatim copy). This is a deliberate additive label-affordance change that also renders on /portfolios/[id] (mount at portfolios/[id]/page.tsx:353). EXPLICIT SC-4 boundary for the executor — if an existing PortfolioOptimizer test snapshots the metric header row and now diffs: a diff limited to the NEW `title` / `aria-describedby` tooltip attributes on the metric labels is EXPECTED and may be UPDATED (accept the snapshot). ANY diff in metric VALUES, row ORDERING, computation-state copy, or non-tooltip DOM is a REGRESSION to FIX, not update — never weaken a value/ordering/state assertion to make it pass. Note in the SUMMARY: tooltip attributes are the ONLY /portfolios/[id] optimizer change, mandated by the UI-SPEC design contract.
  </action>
  <verify>
    <automated>npm run test -- "src/app/(dashboard)/allocations/components/OptimizerPanel.test.tsx" && npm run test -- src/components/portfolio/ && npm run lint</automated>
  </verify>
  <done>Gate/footer/tooltips verbatim-asserted; no-weights absence asserted; existing PortfolioOptimizer suite green; lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client→/api/watchlist | bulk loop of user-selected strategy ids |
| client→/api/portfolio-optimizer | refresh POST with portfolio_id |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-04 | Elevation (IDOR) | watchlist PUT loop | accept (existing) | route already CSRF + owner-keyed (user_id,strategy_id) idempotent upsert/delete |
| T-100-05 | DoS | bulk remove loop hammering | accept | existing per-route rate limit; loop is user-bounded by their own favorites count |
| T-100-06 | DoS/Info | optimizer refresh | accept (existing) | 5/min limiter + refund + opaque error envelope already on the route |
| T-100-07 | Repudiation/Integrity | rendering weights the API never returned | mitigate | ranked-list-only contract + test asserting absence of weight/pie framing |
</threat_model>

<verification>
- Both panel test files green; `npm run test -- src/components/portfolio/` green (no PortfolioOptimizer regression); tsc + lint green.
- `grep -rn "optimize-weights" "src/app/(dashboard)/allocations/"` → no matches.
</verification>

<success_criteria>
Both PI-05 panels exist, contract-typed for plan 100-04 wiring, honest in every empty/degraded state, scores-never-weights enforced by test.
</success_criteria>

<output>
Create `.planning/phases/100-optimizer-favorites-notes-kpi/100-02-SUMMARY.md` when done.
</output>
