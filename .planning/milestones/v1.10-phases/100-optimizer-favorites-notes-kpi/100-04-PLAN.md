---
phase: 100-optimizer-favorites-notes-kpi
plan: 04
type: execute
wave: 2
depends_on: [100-01, 100-02]
files_modified:
  - src/app/(dashboard)/allocations/lib/dashboard-note-read.ts
  - src/app/(dashboard)/allocations/page.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.test.tsx
autonomous: true
requirements: [PI-04, PI-05]
must_haves:
  truths:
    - "On /allocations Holdings tab, 'Watchlist & Optimizer' (2-col container-query grid) and 'Notes' (full-width) sections render directly BELOW the Phase-99 exposure trio"
    - "SC-4 additive: getMyAllocationDashboard's polled payload and the exposure prop threading are byte-untouched — new data arrives only as NEW Promise.all items and NEW props"
    - "Watchlist 'Suggested' chips cross-link real current optimizer suggestion ids"
    - "Every section is honest-empty when its data is empty; errors still throw to allocations/error.tsx"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/dashboard-note-read.ts"
      provides: "dashboard-note initial read (getDashboardNote) — NEW helper owned by this plan, not in plan-02's wave-1 export contract"
      exports: ["getDashboardNote"]
    - path: "src/app/(dashboard)/allocations/HoldingsTabPanel.tsx"
      provides: "Watchlist & Optimizer + Notes sections mounted below exposure"
      contains: "WatchlistPanel"
  key_links:
    - from: "src/app/(dashboard)/allocations/page.tsx"
      to: "src/app/(dashboard)/allocations/lib/watchlist-read.ts"
      via: "new Promise.all items (favorites, optimizer prefetch, dashboard note)"
      pattern: "getFavoritesWithStrategies"
    - from: "src/app/(dashboard)/allocations/HoldingsTabPanel.tsx"
      to: "DashboardNoteCard / WatchlistPanel / OptimizerPanel"
      via: "new sections fed by new props"
      pattern: "OptimizerPanel"
---

<objective>
Wire plans 100-01 + 100-02 into the demo-hero surface: extend the `page.tsx` `Promise.all` with three NEW server reads (favorites, optimizer prefetch, dashboard-note initial content), thread them as NEW props through `AllocationsTabs` → `HoldingsTabPanel`, and mount the two new sections below the Phase-99 exposure trio — exactly the Phase-99 additive precedent, SC-4-safe.

Purpose: the consolidated /allocations demo-hero surface goes live.
Output: wired page + integration render test + SC-4/lint/coverage gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/100-optimizer-favorites-notes-kpi/100-UI-SPEC.md   (Page composition section = THE contract)
@.planning/phases/100-optimizer-favorites-notes-kpi/100-01-SUMMARY.md
@.planning/phases/100-optimizer-favorites-notes-kpi/100-02-SUMMARY.md
@DESIGN.md

<interfaces>
From src/app/(dashboard)/allocations/page.tsx:56-97 (the Phase-99 precedent to copy EXACTLY):
```typescript
const [payload, snapshot, netSeries, allocationSeries] = await Promise.all([
  getMyAllocationDashboard(user.id), getLatestExposureSnapshot(user.id), getNetExposureSeries(user.id), getAllocationSeries(user.id),
]);
// … <AllocationsTabs {...payload} exposure={exposure} />
```
Reads THROW on PostgREST errors → allocations/error.tsx (never collapse error into empty).

From src/app/(dashboard)/allocations/AllocationsTabs.tsx:362+792:
```typescript
props: MyAllocationDashboardPayload & { exposure: ExposureSectionData }
// :792  {activeTab === "holdings" && <HoldingsTabPanel {...props} />}
```

From src/app/(dashboard)/allocations/HoldingsTabPanel.tsx:45-46 + 148-162: same prop type; exposure section = `<section aria-label="Exposure" className="grid gap-4">` with `h3 text-sm font-semibold uppercase tracking-wider text-text-primary` heading + `grid grid-cols-1 gap-4 lg:grid-cols-2` body. New sections mount AFTER it (before "Section 3 — raw exchange positions"? NO — UI-SPEC says below the exposure trio; keep exchange-positions ordering intact by inserting the new sections after the exposure section and BEFORE Section 3 only if UI-SPEC's "below the exposure trio" is read literally — it is: insert directly after the exposure `</section>`).

From plan 100-02 (import contract): `getFavoritesWithStrategies`, `getOptimizerPrefetch`, `FavoriteRow`, `OptimizerPrefetch` from `./lib/watchlist-read`; `WatchlistPanel {favorites, suggestedIds}`; `OptimizerPanel {prefetch}`.
From plan 100-01: `DashboardNoteCard {initialContent, initialLastSavedAt}`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: page.tsx server reads + prop threading through AllocationsTabs</name>
  <files>src/app/(dashboard)/allocations/lib/dashboard-note-read.ts, src/app/(dashboard)/allocations/page.tsx, src/app/(dashboard)/allocations/AllocationsTabs.tsx</files>
  <read_first>page.tsx:40-100 (full Promise.all block + comments), AllocationsTabs.tsx prop type (:362) + HoldingsTabPanel mount (:792), lib/watchlist-read.ts exports (from 100-02), 100-01-SUMMARY (note initial-read guidance)</read_first>
  <action>
    page.tsx: add three NEW items to the existing `Promise.all` (per the Phase-99 precedent comment): `getFavoritesWithStrategies(…)`, `getOptimizerPrefetch(…)`, and a dashboard-note initial read via a NEW colocated helper `getDashboardNote(supabase, userId)` in `src/app/(dashboard)/allocations/lib/dashboard-note-read.ts` (this plan owns it — do NOT edit plan-02's watchlist-read.ts) — a small owner-scoped user-client select on `user_notes` where scope_kind='dashboard' AND scope_ref='allocations' returning `{ initialContent: string; initialLastSavedAt: Date | null }` (empty string + null on 0 rows; RLS scopes it to the user; THROW on real PostgREST errors — never collapse error into empty). Do NOT touch `getMyAllocationDashboard`, the exposure reads, or the existing destructure order beyond appending. Thread as NEW props: extend the `AllocationsTabs` props intersection with `{ favorites: FavoriteRow[]; optimizer: OptimizerPrefetch; note: { initialContent: string; initialLastSavedAt: Date | null } }` and pass through to `HoldingsTabPanel {...props}` (already spread — extending the type suffices). `exposure` prop and `AllocationProvider` untouched.
  </action>
  <verify>
    <automated>npx tsc --noEmit && bash -c 'grep -n "getMyAllocationDashboard(user.id)" "src/app/(dashboard)/allocations/page.tsx" | wc -l | grep -q "^ *1$"'</automated>
  </verify>
  <done>Three new reads appended; payload/exposure threading byte-untouched (git diff shows only additions around them); tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mount sections in HoldingsTabPanel + integration test + gates</name>
  <files>src/app/(dashboard)/allocations/HoldingsTabPanel.tsx, src/app/(dashboard)/allocations/HoldingsTabPanel.test.tsx</files>
  <read_first>HoldingsTabPanel.tsx (full — Section 1/2/3 structure + existing heading idiom), UI-SPEC "Page composition" (32px gaps, container-query reflow via a SEPARATE @container ancestor per the CompareTable idiom in DESIGN.md 2026-06-29 entry), existing HoldingsTabPanel/exposure tests for the render-test idiom</read_first>
  <behavior>
    - Renders section "Watchlist & Optimizer" (WatchlistPanel + OptimizerPanel in a 2-col grid that reflows on container width) directly after the Exposure section, then full-width "Notes" section (DashboardNoteCard) — exchange-positions section still renders after them.
    - Honest-empty integration: favorites=[] + 0 portfolios + empty note → all three honest states render, zero fabricated rows.
    - suggestedIds passed to WatchlistPanel are derived from `optimizer.initialSuggestions` strategy_ids (real cross-link; [] when null).
    - SC-4: the Exposure section's DOM (aria-label, heading, widget mounts) is unchanged — snapshot or structural assertions on the exposure subtree unchanged from the existing test expectations.
  </behavior>
  <action>
    Insert the two sections after the exposure `</section>` per UI-SPEC composition: section headings match the existing exposure-heading idiom in this file (h3, text-sm font-semibold uppercase tracking-wider text-text-primary — UI-SPEC's "matching the exposure section's panel-heading tier"); 32px section gaps (space-y-8 at the section level or mt-8 — match how the file spaces its sections; if the file uses `grid gap-4` wrappers per section, wrap new sections the same way with the larger inter-section gap per UI-SPEC). Two-panel grid: host `@container` on a separate ancestor from the `@lg:grid-cols-2` variants (CompareTable idiom); stacked under 1024px container width. Derive `suggestedIds` here (memo over optimizer.initialSuggestions). Extend the props type identically to Task 1.
    Gates: run `npm run test -- "src/app/(dashboard)/allocations/"` (all allocations tests incl. Phase-99 exposure tests must stay green), `npm run lint`, then `npm run test:coverage` once to confirm the ratchet (lines 82/stmts 80/fns 74/branches 72) holds with all Phase-100 additions.
  </action>
  <verify>
    <automated>npm run test -- "src/app/(dashboard)/allocations/" && npm run lint</automated>
  </verify>
  <done>Sections mounted per UI-SPEC order; honest-empty integration asserted; existing allocations tests green unmodified; lint + coverage ratchet green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| RSC→Supabase | three new owner-scoped reads on the page |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-09 | Info disclosure | new server reads | mitigate | RLS user client only (never admin) for favorites/optimizer/note reads; enforced in watchlist-read.ts (plan 02) and the note read |
| T-100-10 | Tampering (SC-4 drift) | polled payload / exposure threading | mitigate | additive-only Promise.all + unchanged existing tests as the regression gate |
</threat_model>

<verification>
- Full allocations suite + lint green; `npm run test:coverage` ratchet holds.
- git diff of page.tsx shows getMyAllocationDashboard/exposure lines unmodified.
</verification>

<success_criteria>
/allocations shows exposure trio → Watchlist & Optimizer → Notes in order, all real-data/honest-empty, with zero behavioral change to any pre-existing element (SC-4).
</success_criteria>

<output>
Create `.planning/phases/100-optimizer-favorites-notes-kpi/100-04-SUMMARY.md` when done.
</output>
