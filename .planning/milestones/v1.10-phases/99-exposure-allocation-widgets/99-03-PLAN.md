---
phase: 99-exposure-allocation-widgets
plan: 03
type: execute
wave: 2
depends_on: [99-02]
files_modified:
  - src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.tsx
  - src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.test.tsx
autonomous: true
requirements: [PI-03]

must_haves:
  truths:
    - "Per-venue allocation weights render as a 0–100% stacked area, largest venue at the bottom, venues colored by STRATEGY_PALETTE"
    - "A zero-gross asof at the series BOUNDARY renders a marked hatched band BEFORE the first stacked point (F-2 — the x-domain includes the gap edge, never clipped)"
    - "A venue absent on a day stacks at weight 0 (the true weight), while a marked gap breaks the stack — the two are visually distinct"
    - "Empty series renders the exact two-line honest-empty copy"
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.tsx"
      provides: "PI-03 stacked-area allocation-over-time widget (NOT a streamgraph)"
      exports: ["AllocationOverTime", "buildAllocationChartData"]
      min_lines: 90
  key_links:
    - from: "src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.tsx"
      to: "src/app/(dashboard)/allocations/widgets/lib/chart-gaps.tsx"
      via: "SAME gap contract as PI-02 (toGapBands/gapXDomain/renderGapAreas) — no reimplementation"
      pattern: "from \"\\.\\./lib/chart-gaps\""
    - from: "src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.tsx"
      to: "@/lib/utils STRATEGY_PALETTE"
      via: "design-system-approved categorical palette for venue bands"
      pattern: "STRATEGY_PALETTE"
    - from: "src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.tsx"
      to: "@/lib/portfolio-exposure"
      via: "TYPE-ONLY import of AllocationPoint/AsofGap"
      pattern: "import type \\{.*AllocationPoint.*\\} from \"@/lib/portfolio-exposure\""
---

<objective>
Build the PI-03 "Allocation over time" widget: recharts stacked area (0–100%), one Area per venue with `stackId`, venues sorted largest-at-bottom, STRATEGY_PALETTE colors, white hairline seams — consuming Plan 02's shared gap contract so boundary AND interior zero-gross gaps render as marked hatched bands (F-2).

Purpose: PI-03 requirement — allocator weight history from `getAllocationSeries`, coverage-mask-consistent.
Output: `AllocationOverTime.tsx` + tests. Mounted by Plan 04.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md
@src/lib/portfolio-exposure.ts
@src/app/(dashboard)/allocations/widgets/lib/chart-gaps.tsx
@src/components/charts/chart-tokens.ts
@src/components/charts/TouchTooltip.tsx
@DESIGN.md

<interfaces>
From src/lib/portfolio-exposure.ts (frozen — TYPE-ONLY imports):
```typescript
export interface AllocationPoint {
  asof: string;  // "YYYY-MM-DD"
  venues: { venue: string; valueUsd: number; weight: number }[];  // weights sum to 1 per point (gross denominators, D-P3)
}
export interface AsofGap { start: string; end: string; kind: "gap"; days: number }
// getAllocationSeries(userId) => { points, gaps } — zero-gross asofs are SKIPPED and marked
// as gaps INCLUDING at the series boundary (computeCoverageGaps, F-2).
```

From src/app/(dashboard)/allocations/widgets/lib/chart-gaps.tsx (Plan 02 — consume, do not fork):
```typescript
export const HALF_DAY_MS: number;
export function asofToUtcMs(asof: string): number;
export interface GapBand { x1: number; x2: number; midMs: number; days: number; start: string; end: string }
export function toGapBands(gaps: AsofGap[]): GapBand[];
export function gapXDomain(pointMs: number[], bands: GapBand[]): [number, number];
export function renderGapAreas(bands: GapBand[], patternId: string): ReactElement[];  // splice into chart children
export function makeDateTickFormatter(domain: [number, number]): (ms: number) => string;
```

From src/lib/utils.ts:
```typescript
export const STRATEGY_PALETTE = ["#1B6B5A","#2563EB","#D97706","#0F766E","#DC2626","#059669","#DB2777","#4338CA"] as const;
export function formatCurrency(value: number | null | undefined): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AllocationOverTime — pivot builder + stacked-area chart</name>
  <read_first>
    - .planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md (§ Widget 3 — full contract: stacked area NOT streamgraph, venue order/color, data pivot, axes, tooltip; § Gap Rendering; § Copywriting Contract)
    - src/lib/portfolio-exposure.ts getAllocationSeries (lines 352-403 — D-P3 skip-and-mark semantics + F-2 boundary marking)
    - src/app/(dashboard)/allocations/widgets/lib/chart-gaps.tsx (the shared contract from Plan 02 — consume its exact exports)
    - src/app/(dashboard)/allocations/widgets/positions/NetExposureChart.tsx (Plan 02's chart wiring idiom — Card header, legend chips, useId patternId, axis props)
  </read_first>
  <files>src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.tsx, src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.test.tsx</files>
  <behavior>
    - Test (pivot + venue order): points day1 {binance 0.6/$600K, okx 0.4/$400K}, day2 {binance 0.7, okx 0.3} → buildAllocationChartData returns venues ["binance","okx"] (mean weight desc — binance first = bottom of stack) and rows [{asofMs, binance:0.6, okx:0.4}, …]
    - Test (absent venue = true 0): day1 {binance 1.0}, day2 {binance 0.5, okx 0.5} → day1 row has okx === 0 (not undefined/null) — that day's gross sat entirely at binance
    - Test (gap sentinel): interior gap → exactly one row at mid-gap ms where EVERY venue key is null (stack breaks; connectNulls false)
    - Test (F-2 boundary gap): fixture with gaps=[{start:"2026-01-01", end:"2026-01-03", days:3}] BEFORE first point 2026-01-04 → domain[0] === utc(01-01)−12h (< first point ms) AND rendered SVG contains <title> "No data 2026-01-01 → 2026-01-03 (3 days)" — a marked band renders before the first stacked point
    - Test (empty): points=[] → EXACT copy "No allocation history yet." / "Per-venue weights build as daily position snapshots accrue."
    - Test (single venue): one venue → exactly one legend chip + rows where that venue is 1.0 (100% band, no special casing)
    - Test (header/a11y): title "Allocation over time"; as-of stamp = last point asof; wrapper role="img" aria-label "Per-venue allocation weights over time"
  </behavior>
  <action>
    Create `"use client"` `AllocationOverTime({ points, gaps }: { points: AllocationPoint[]; gaps: AsofGap[] })` at `widgets/allocation/AllocationOverTime.tsx` (the retired Widget-18 path). TYPE-ONLY portfolio-exposure imports.

    Export pure helper `buildAllocationChartData(points, gaps): { venues: string[]; rows: Record<string, number | null>[] & { asofMs }; usdByAsofMs: Map<number, Record<string, number>>; bands: GapBand[]; domain: [number, number] }`:
    - `venues`: distinct venue names sorted by MEAN weight desc (largest first — rendered first = BOTTOM of the recharts stack, the stable reading base)
    - `rows`: one per point `{ asofMs, [venue]: weight }` — a venue absent at an asof gets weight `0` (TRUE weight, not invented data); plus ONE sentinel row per gap at band.midMs where every venue key is `null`; sorted by asofMs
    - `usdByAsofMs`: asofMs → {venue: valueUsd} for the tooltip's gross-USD line
    - `bands`/`domain` via Plan 02's toGapBands/gapXDomain — domain MUST be `[min(firstPoint, firstBand.x1), max(lastPoint, lastBand.x2)]` so a leading/trailing zero-gross gap renders as a visible edge band instead of being clipped (this is the whole point of F-2)

    Empty state: same empty-card idiom, verbatim copy "No allocation history yet." / "Per-venue weights build as daily position snapshots accrue.".

    Populated: `<Card padding="sm">`, header row (title `Allocation over time` + as-of stamp = last point's asof), legend chips above the chart (one per venue in stack order: `w-2.5 h-2.5 rounded-sm` swatch in STRATEGY_PALETTE[i] + venue name text-caption text-text-secondary, wrapping `flex flex-wrap gap-x-4 gap-y-1`). Then `<ResponsiveContainer width="100%" height={260}>` hosting `<AreaChart accessibilityLayer={false} data={rows}>` (or ComposedChart) with:
    - `{...renderGapAreas(bands, patternId)}` (useId patternId)
    - one `<Area key={venue} type="monotone" dataKey={venue} stackId="alloc" fill={STRATEGY_PALETTE[i % STRATEGY_PALETTE.length]} fillOpacity={0.85} stroke="#FFFFFF" strokeWidth={1} connectNulls={false} />` per venue in sorted order (fixed venue→color assignment by that order)
    - XAxis identical to PI-02: `dataKey="asofMs" type="number" domain={domain}` + CHART_TICK_STYLE + makeDateTickFormatter(domain) + tickLine={false} axisLine={{stroke: CHART_BORDER}} interval="preserveStartEnd"
    - YAxis: `domain={[0, 1]}` tickFormatter={(v) => `${(v * 100).toFixed(0)}%`} + CHART_TICK_STYLE, tickLine={false}, axisLine={false}
    - `<TouchTooltip contentStyle={CHART_TOOLTIP_STYLE} itemSorter={(item) => -(item.value ?? 0)} labelFormatter={ms → full ISO} formatter={(v, name, item) => [`${(Number(v) * 100).toFixed(1)}% (${formatCurrency(usdByAsofMs.get(item.payload.asofMs)?.[String(name)] ?? null)})`, String(name)]} />` — per-venue rows sorted weight-desc, "42.3% ($1.24M)" shape
    Wrapper: `role="img" aria-label="Per-venue allocation weights over time"`. Height 260. NOT a streamgraph — plain 0–100% stack (weights sum to 1 by construction, D-P3).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - buildAllocationChartData tests green: venue order mean-desc, absent-venue 0, all-null sentinel per gap, F-2 boundary domain includes band.x1 < first point
    - F-2 render test green: leading-gap fixture shows a hatched band <title> before the first stacked point
    - `grep -c "connectNulls={false}" ...AllocationOverTime.tsx` ≥ 1 (on the per-venue Area element) and `grep -c 'stackId="alloc"' ` ≥ 1
    - NOT a streamgraph: no `stackOffset` prop anywhere in the file (grep count 0 — default expand/none 0–100% stack with y domain [0,1])
    - Venue colors come from STRATEGY_PALETTE (grep) — no ad-hoc hex for venue bands
    - Empty-copy exact-match test green; no try/catch in the component (grep "catch" = 0)
    - Type-only import gate + no createAdminClient/allocator_holdings/@/lib/supabase (grep 0)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Repo-wide chart contracts + type gate over the new chart</name>
  <read_first>
    - tests/visual/chart-accessibility-layer.test.ts (repo-wide accessibilityLayer contract — the new AreaChart must pass it)
    - tests/visual/recharts-touchtooltip-usage.test.ts (TouchTooltip usage contract)
  </read_first>
  <files>src/app/(dashboard)/allocations/widgets/allocation/AllocationOverTime.tsx</files>
  <action>
    Run the two repo-wide visual-contract suites plus tsc against the new widget; fix any violation IN the widget (never by editing the contract tests). Then run the full chart-gaps + NetExposureChart suites once to prove Plan 03 did not disturb the shared gap lib (it must not have modified chart-gaps.tsx at all — `git status` shows only the two Plan-03 files changed).
  </action>
  <verify>
    <automated>npx vitest run tests/visual/chart-accessibility-layer.test.ts tests/visual/recharts-touchtooltip-usage.test.ts "src/app/(dashboard)/allocations/widgets/lib/chart-gaps.test.tsx" "src/app/(dashboard)/allocations/widgets/positions/NetExposureChart.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both repo-wide contract suites green (accessibilityLayer={false} + TouchTooltip on the new chart)
    - Plan 02's chart-gaps + NetExposureChart suites still green (shared lib untouched: `git diff --name-only` contains no chart-gaps.tsx)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client widget | server-derived allocation series JSON crosses into the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-05 | Information Disclosure | AllocationOverTime bundle | mitigate | TYPE-ONLY portfolio-exposure imports; props = venue/weight/valueUsd only (six-column projection upstream) |
| T-99-06 | Repudiation (data honesty) | boundary gaps | mitigate | F-2 domain rule + render test — a leading zero-gross span can never silently vanish |
| T-99-SC | Tampering | package installs | accept | no new packages |
</threat_model>

<verification>
- Plan test file + both repo-wide visual contracts + Plan 02 suites green
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
PI-03 widget exists as a tested unit: 0–100% stacked area with STRATEGY_PALETTE venues largest-at-bottom, absent-venue-is-zero vs marked-gap distinction proven, F-2 boundary band rendered, verbatim empty copy — all on the SHARED gap contract (zero divergence from PI-02).
</success_criteria>

<output>
Create `.planning/phases/99-exposure-allocation-widgets/99-03-SUMMARY.md` when done.
</output>
