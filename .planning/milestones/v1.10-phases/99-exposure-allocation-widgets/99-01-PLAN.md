---
phase: 99-exposure-allocation-widgets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx
  - src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.test.tsx
autonomous: true
requirements: [PI-01]

must_haves:
  truths:
    - "A hedged book (long $300K spot + short $100K derivative) renders GROSS $400K / NET $200K / LONG $300K / SHORT $100K with a 'net long' caption (D-P2 signed math surfaces visually)"
    - "A single-class (all-spot) book renders one full-width labeled segment and the absent class stays in the legend as 'Derivatives · —' — never a misleading full circle"
    - "A null snapshot renders the exact two-line honest-empty copy, never a fabricated number"
    - "Long/short direction is NEVER colored red/green — semantic colors stay reserved for gains/losses"
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx"
      provides: "PI-01 segmented composition bar + KPI strip + drilldown table (NOT a donut)"
      exports: ["ExposureByClass"]
      min_lines: 120
    - path: "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.test.tsx"
      provides: "RTL/vitest coverage of hedged-book math, single-class legend, empty copy, no-red/green gate"
  key_links:
    - from: "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx"
      to: "@/lib/portfolio-exposure"
      via: "TYPE-ONLY import of ExposureSnapshot/ExposureSlice"
      pattern: "import type \\{.*ExposureSnapshot.*\\} from \"@/lib/portfolio-exposure\""
    - from: "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx"
      to: "@/components/ui/Card + @/lib/utils formatCurrency"
      via: "Card primitive wrapper + formatCurrency for every USD value"
      pattern: "formatCurrency"
---

<objective>
Build the PI-01 "Exposure by asset class" widget exactly to the 99-UI-SPEC design contract: segmented horizontal composition bar + 4-cell KPI strip (GROSS/NET/LONG/SHORT) + per-slice drilldown table + verbatim honest-empty state. Pure CSS (no recharts). Self-contained client component consuming a plain `ExposureSnapshot | null` prop — no data fetching.

Purpose: PI-01 requirement — real position-level asset-class breakdown on the demo-hero dashboard, honest when empty.
Output: `ExposureByClass.tsx` + co-located test file. Mounted by Plan 04 (do NOT touch HoldingsTabPanel here).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md
@.planning/phases/99-exposure-allocation-widgets/99-CONTEXT.md
@src/lib/portfolio-exposure.ts
@DESIGN.md
@src/components/ui/Card.tsx
@src/components/portfolio/CompositionDonut.tsx
@src/components/portfolio/AttributionBar.tsx

<interfaces>
<!-- Frozen data contract (Phase 98) — bind exactly; import TYPE-ONLY. -->
From src/lib/portfolio-exposure.ts:
```typescript
export type HoldingClass = "spot" | "derivative";
export interface ExposureSlice {
  holdingType: HoldingClass;
  venue: string;
  symbol: string;
  side: "long" | "short" | "flat";
  valueUsd: number;        // unsigned, as stored
  signedValueUsd: number;  // D-P2: short => -valueUsd
}
export interface ExposureSnapshot {
  asof: string;            // "YYYY-MM-DD"
  slices: ExposureSlice[];
  totalGrossUsd: number;
  totalNetUsd: number;
}
```

From src/components/ui/Card.tsx:
```typescript
export function Card({ padding = "md", className, children, ...props }: 
  HTMLAttributes<HTMLDivElement> & { padding?: "sm" | "md" | "lg" }): JSX
// padding="sm" => p-4 (16px). Renders bg-surface rounded-xl border border-border shadow-card.
```

From src/lib/utils.ts:
```typescript
export function formatCurrency(value: number | null | undefined): string
// null/NaN => "—"; compact: $1.2M / $450K / $850; negatives: -$1.5K
```

From src/components/charts/chart-tokens.ts:
```typescript
export const CHART_ACCENT = "#1B6B5A";  // spot segment color
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExposureByClass — KPI strip, composition bar, legend, honest-empty</name>
  <read_first>
    - .planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md (§ Widget 1 — the design contract; § Color; § Typography; § Copywriting Contract)
    - src/lib/portfolio-exposure.ts (getLatestExposureSnapshot contract + ExposureSnapshot/ExposureSlice types + D-P1/D-P2 doc header)
    - src/components/portfolio/AttributionBar.tsx lines 10-18 (empty-card idiom to copy verbatim)
    - src/components/ui/Card.tsx (wrapper primitive)
    - src/lib/utils.ts formatCurrency (line 32)
  </read_first>
  <files>src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx, src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.test.tsx</files>
  <behavior>
    - Test (hedged book, D-P2): snapshot with slices [spot/binance/BTC/long/300000/+300000, derivative/deribit/BTC-PERP/short/100000/-100000], totalGrossUsd 400000, totalNetUsd 200000 → KPI cells render GROSS "$400K", NET "$200K" with sub-caption "net long", LONG "$300K", SHORT "$100K"
    - Test (net short): totals inverted (net -200000) → NET renders "-$200K" + caption "net short"; flat book (net 0 with gross > 0) → caption "flat"
    - Test (single-class): all-spot snapshot → exactly ONE bar segment at 100% width; legend still lists BOTH classes; absent class row reads "Derivatives · —" and carries text-text-muted
    - Test (two-class shares): spot 248000 / derivative 152000 of 400000 gross → legend "Spot · $248K (62.0%)" and "Derivatives · $152K (38.0%)" (1 decimal place)
    - Test (honest-empty): snapshot={null} → renders EXACTLY "No position snapshot yet." and "Exposure appears after your first exchange sync. Snapshots older than 24 months are not shown." — and does NOT render the Card/KPI strip
    - Test (no red/green on direction): rendered container.innerHTML contains NO "text-positive", NO "text-negative", NO "#15803D", NO "#DC2626" for the hedged fixture
    - Test (a11y): bar container has role="img" and aria-label matching /^Gross exposure split: Spot \d+\.\d%, Derivatives \d+\.\d%$/
  </behavior>
  <action>
    Create `"use client"` component `ExposureByClass({ snapshot }: { snapshot: ExposureSnapshot | null })` per UI-SPEC § Widget 1. Import `ExposureSnapshot`/`ExposureSlice` as TYPE-ONLY from `@/lib/portfolio-exposure` (value import would pull the server supabase client into the client bundle). No recharts, no data fetching, no supabase import.

    Empty state (`snapshot === null`): return the AttributionBar empty-card idiom verbatim — `<div className="rounded-lg border border-border bg-surface px-4 py-8 text-center">` containing two lines: `<p className="text-small text-text-muted">No position snapshot yet.</p>` and `<p className="text-caption text-text-muted">Exposure appears after your first exchange sync. Snapshots older than 24 months are not shown.</p>`. No Card wrapper, no title in the empty state (AttributionBar parity — the copy is self-describing).

    Populated state — wrap in `<Card padding="sm">`. Header row: title `Exposure by asset class` in `text-small font-semibold text-text-primary`, right-aligned stamp `as of {snapshot.asof}` in `text-caption font-metric text-text-muted` (flex justify-between).

    1. KPI strip: `grid grid-cols-2 sm:grid-cols-4 gap-4`. Labels GROSS/NET/LONG/SHORT in `text-micro uppercase tracking-wider text-text-muted`; values `text-h3 font-semibold font-metric text-text-primary` via formatCurrency. Derivations (per UI-SPEC, no invented data): GROSS = totalGrossUsd; NET = totalNetUsd (sign carried in the number); LONG = Σ signedValueUsd over slices with signedValueUsd > 0; SHORT = Σ |signedValueUsd| over slices with signedValueUsd < 0. NET sub-caption `text-caption text-text-muted`: "net long" if totalNetUsd > 0, "net short" if < 0, "flat" if === 0. NO color coding of direction anywhere — do not use text-positive/text-negative or CHART_POSITIVE/CHART_NEGATIVE (D-XX locked: semantic colors reserved for P&L).

    2. Composition bar: plain CSS flex div, `h-3 rounded-sm overflow-hidden bg-track` rail; one segment per PRESENT class, width = class gross share of totalGrossUsd (%), colors: spot = CHART_ACCENT (#1B6B5A, import from chart-tokens), derivative = local const "#0F172A" (neutral navy per UI-SPEC — do NOT add it to chart-tokens.ts; src/components/charts/** takes no new code). 1px white seam between segments (e.g. border-l border-white on the second segment). Container: `role="img"` + `aria-label="Gross exposure split: Spot {x}%, Derivatives {y}%"` with 1-decimal percents (0.0% for an absent class).

    3. Legend row beneath: ALWAYS both classes. Present class: swatch `w-2.5 h-2.5 rounded-sm inline-block` (backgroundColor = class color) + `text-small` "Spot" + `font-metric` "· $248K (62.0%)". Absent class: `Derivatives · —` in text-text-muted with its swatch. (Percent 1dp, values via formatCurrency.)

    Write the test file FIRST with the behavior fixtures above (RED), then implement (GREEN). Query by role/text; a data-testid on bar segments (e.g. `data-testid="exposure-segment-spot"`) is acceptable for width assertions.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - PI-01 renders a segmented horizontal bar + KPI strip — grep gate: `grep -c "recharts" "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx"` returns 0 (no donut, no chart lib)
    - Hedged fixture (long 300K + short 100K) asserts GROSS $400K / NET $200K / LONG $300K / SHORT $100K + "net long" (behavior tests pass)
    - Single-class fixture asserts one full-width segment + literal legend text "Derivatives · —"
    - Empty fixture asserts BOTH verbatim copy lines and absence of the KPI strip
    - No-red/green test passes: innerHTML free of text-positive/text-negative/#15803D/#DC2626
    - Type-only import gate: `grep -n 'from "@/lib/portfolio-exposure"' "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx" | grep -v "import type" | wc -l` returns 0
    - No secret surface: `grep -cE "createAdminClient|allocator_holdings|@/lib/supabase" "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx"` returns 0
    - DESIGN.md tokens only: `grep -cE 'text-\[[0-9]+px\]' ...ExposureByClass.tsx` returns 0; classes used are the named tiers (text-h3/text-small/text-caption/text-micro/font-metric/bg-track)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ExposureByClass — per-slice drilldown table</name>
  <read_first>
    - .planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md (§ Widget 1 anatomy item 4 — table columns + idiom cite)
    - src/components/portfolio/CompositionDonut.tsx lines 70-99 (the exact table idiom to copy: text-small table, caption uppercase header with bottom border, border-border/50 hover:bg-page/50 rows, right-aligned font-metric numerics)
    - src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx (the file Task 1 created — extend it)
  </read_first>
  <files>src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.tsx, src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.test.tsx</files>
  <behavior>
    - Test (columns + order): hedged fixture → a real <table> with header cells exactly Venue / Symbol / Type / Side / Gross / Net; rows sorted by valueUsd descending (binance row before deribit row)
    - Test (cell semantics): Type cell renders "Spot"/"Deriv" with a class-colored swatch dot; Side cell renders lowercase "long"/"short"/"flat" in text-caption text-text-secondary; Net cell renders signedValueUsd via formatCurrency (short row shows "-$100K" — minus sign carries direction, no color)
    - Test (scroll region): fixture with 14 slices → the table wrapper carries max-h-64 overflow-y-auto; fixture with 5 slices → it does not
  </behavior>
  <action>
    Extend ExposureByClass with the drilldown table beneath the legend, copying the CompositionDonut table idiom: `<table className="w-full text-small">`, thead row `border-b border-border text-left text-caption text-text-muted uppercase tracking-wider`, body rows `border-b border-border/50 hover:bg-page/50 transition-colors`, numeric cells `text-right font-metric`. Columns: Venue / Symbol / Type / Side / Gross / Net. Slices sorted by valueUsd desc. Type cell = "Spot"/"Deriv" text + `w-2.5 h-2.5 rounded-sm` swatch in the class color. Side = plain lowercase `text-caption text-text-secondary` — NEVER colored by direction. Gross = formatCurrency(valueUsd); Net = formatCurrency(signedValueUsd). When slices.length > 12, wrap in `<div className="max-h-64 overflow-y-auto">` (simple scroll; no sticky header). Real `<table>` element (WCAG — class identity carried by label + position, not color alone).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Table header renders exactly the six UI-SPEC columns in order; getAllByRole("row") ordering asserts valueUsd-desc sort
    - Short row Net cell text is "-$100K" and carries no text-negative class
    - 14-slice fixture asserts the max-h-64 overflow-y-auto wrapper; 5-slice fixture asserts its absence
    - `npx tsc --noEmit` clean; all Task 1 tests still green (no regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → client widget | server-derived JSON props cross into the client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Information Disclosure | ExposureByClass client bundle | mitigate | TYPE-ONLY import from portfolio-exposure (grep gate); no supabase/admin import; props are the six-column secretless projection only |
| T-99-02 | Spoofing | allocator id | transfer | widget never sees/sends an id — Plan 04 pins `auth.uid()`-derived id at the RSC read site |
| T-99-SC | Tampering | package installs | accept | no new packages installed in this plan (recharts not even used) |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/widgets/positions/ExposureByClass.test.tsx" --no-file-parallelism` green
- `npx tsc --noEmit` clean
- Grep gates from acceptance criteria all pass
</verification>

<success_criteria>
PI-01 widget exists as a testable unit: segmented bar (not donut), KPI strip with D-P2 signed math, drilldown table, verbatim empty copy, no red/green on direction, type-only data-contract import. All behavior tests green.
</success_criteria>

<output>
Create `.planning/phases/99-exposure-allocation-widgets/99-01-SUMMARY.md` when done.
</output>
