---
phase: 99-exposure-allocation-widgets
plan: 04
type: execute
wave: 3
depends_on: [99-01, 99-02, 99-03]
files_modified:
  - src/app/(dashboard)/allocations/page.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx
  - src/app/(dashboard)/allocations/lib/exposure-props.ts
  - src/app/(dashboard)/allocations/HoldingsTabPanel.exposure.test.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.test.tsx
  - src/app/(dashboard)/allocations/HoldingsTabPanel.spot-derivative-split.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.onboarding.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx
autonomous: false
requirements: [PI-01, PI-02, PI-03]

must_haves:
  truths:
    - "The Holdings tab shows a new 'Exposure' section between Strategies and Exchange Positions with the three widgets fed by real reads"
    - "page.tsx runs the three exposure reads in Promise.all with the auth.uid()-derived user.id — never a client-supplied id — as a distinct `exposure` prop, NOT folded into the polled getMyAllocationDashboard payload"
    - "A read error propagates to allocations/error.tsx — it is NEVER collapsed into empty-state copy"
    - "Existing single-key dashboard behavior is unchanged (additive section — every pre-existing allocations test still passes)"
    - "The Phase-98 >1000-row pagination fix is confirmed on a LIVE large-allocator render, not just mocks"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/exposure-props.ts"
      provides: "ExposureSectionData type + EMPTY_EXPOSURE test fixture"
      exports: ["ExposureSectionData", "EMPTY_EXPOSURE"]
    - path: "src/app/(dashboard)/allocations/HoldingsTabPanel.exposure.test.tsx"
      provides: "Integration coverage: section placement/order, empty trio, populated render, 520-point volume fixture"
  key_links:
    - from: "src/app/(dashboard)/allocations/page.tsx"
      to: "@/lib/portfolio-exposure"
      via: "Promise.all([getMyAllocationDashboard(user.id), getLatestExposureSnapshot(user.id), getNetExposureSeries(user.id), getAllocationSeries(user.id)])"
      pattern: "getLatestExposureSnapshot\\(user\\.id\\)"
    - from: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      to: "src/app/(dashboard)/allocations/HoldingsTabPanel.tsx"
      via: "exposure prop threaded through the existing {...props} spread"
      pattern: "exposure"
    - from: "src/app/(dashboard)/allocations/HoldingsTabPanel.tsx"
      to: "widgets/positions/ExposureByClass + widgets/positions/NetExposureChart + widgets/allocation/AllocationOverTime"
      via: "DIRECT imports (NOT the B7b-locked WIDGET_COMPONENTS barrel)"
      pattern: "from \"\\./widgets/(positions|allocation)/"
---

<objective>
Wire the three widgets into the live dashboard: page.tsx fetches the three exposure reads in `Promise.all` with the authenticated user id and passes a distinct serializable `exposure` prop through `AllocationsTabs` into `HoldingsTabPanel`, which renders the new "Exposure" section between Strategies and Exchange Positions. Prove the wiring with integration tests + grep gates, keep every existing allocations test green, and close the Phase-98 carry-forward with a live >1000-row large-allocator render check (human-verified).

Purpose: PI-01/02/03 become real on the demo-hero surface; auth + error-propagation invariants pinned.
Output: modified page/tabs/panel, exposure-props type module, integration test, human-verified live check.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md
@.planning/phases/99-exposure-allocation-widgets/99-CONTEXT.md
@src/lib/portfolio-exposure.ts
@src/app/(dashboard)/allocations/page.tsx
@src/app/(dashboard)/allocations/AllocationsTabs.tsx
@src/app/(dashboard)/allocations/HoldingsTabPanel.tsx
@src/app/(dashboard)/allocations/error.tsx
@AGENTS.md

<interfaces>
Current wiring (verified 2026-07-12):
```typescript
// page.tsx (Server Component): user from supabase.auth.getUser(); redirect("/login") if absent;
//   payload = await getMyAllocationDashboard(user.id);
//   <AllocationProvider …><AllocationsTabs {...payload} /></AllocationProvider>
//   NOTE: createAdminClient IS legitimately imported here for onboarding-funnel events —
//   the grep gate below targets `allocator_holdings` + admin use in the EXPOSURE reads, not that block.

// AllocationsTabs.tsx:360  export function AllocationsTabs(props: MyAllocationDashboardPayload)
//   line ~89: HoldingsTabPanel = dynamic(() => import("./HoldingsTabPanel"), { ssr: false, … })
//   line ~789: {activeTab === "holdings" && <HoldingsTabPanel {...props} />}

// HoldingsTabPanel.tsx:38  export function HoldingsTabPanel(props: MyAllocationDashboardPayload)
//   renders: <HoldingsTable strategyRows/> (Section 1, ~line 137) then
//   <section> "Exchange Positions" (Section 2, ~line 140)
```

Widget props (Plans 01-03 — already built):
```typescript
ExposureByClass({ snapshot }: { snapshot: ExposureSnapshot | null })
NetExposureChart({ points, gaps }: { points: NetExposurePoint[]; gaps: AsofGap[] })
AllocationOverTime({ points, gaps }: { points: AllocationPoint[]; gaps: AsofGap[] })
```

New type module (this plan creates — plain .ts, type-only re-exports, no "use client"):
```typescript
// src/app/(dashboard)/allocations/lib/exposure-props.ts
import type { ExposureSnapshot, NetExposurePoint, AllocationPoint, AsofGap } from "@/lib/portfolio-exposure";
export interface ExposureSectionData {
  snapshot: ExposureSnapshot | null;
  netSeries: { points: NetExposurePoint[]; gaps: AsofGap[] };
  allocationSeries: { points: AllocationPoint[]; gaps: AsofGap[] };
}
export const EMPTY_EXPOSURE: ExposureSectionData = {
  snapshot: null,
  netSeries: { points: [], gaps: [] },
  allocationSeries: { points: [], gaps: [] },
}; // shared fixture for the six pre-existing test files + new tests
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread exposure from page.tsx through AllocationsTabs into HoldingsTabPanel</name>
  <read_first>
    - node_modules/next/dist/docs — the RSC/data-fetching guide relevant to page.tsx (AGENTS.md: this is NOT the Next.js you know; verify Promise.all-in-RSC + serializable-props conventions before editing)
    - .planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md (§ Data Contract — data flow/auth block; § Placement & Responsive Layout — section anatomy verbatim)
    - src/lib/portfolio-exposure.ts (getLatestExposureSnapshot / getNetExposureSeries / getAllocationSeries signatures — all take userId; all THROW on PostgREST errors)
    - src/app/(dashboard)/allocations/page.tsx (the file being modified — funnel-event block must stay byte-identical)
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx lines 355-365 + 780-800 (props signature + spread sites)
    - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx (the file being modified — insertion point between line ~137 and ~140)
  </read_first>
  <files>src/app/(dashboard)/allocations/lib/exposure-props.ts, src/app/(dashboard)/allocations/page.tsx, src/app/(dashboard)/allocations/AllocationsTabs.tsx, src/app/(dashboard)/allocations/HoldingsTabPanel.tsx, src/app/(dashboard)/allocations/HoldingsTabPanel.test.tsx, src/app/(dashboard)/allocations/HoldingsTabPanel.spot-derivative-split.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.onboarding.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx</files>
  <action>
    1. Create `lib/exposure-props.ts` exactly per the interface block above (ExposureSectionData + EMPTY_EXPOSURE).

    2. page.tsx: replace the single `getMyAllocationDashboard` await with
       `const [payload, snapshot, netSeries, allocationSeries] = await Promise.all([getMyAllocationDashboard(user.id), getLatestExposureSnapshot(user.id), getNetExposureSeries(user.id), getAllocationSeries(user.id)]);`
       then `const exposure: ExposureSectionData = { snapshot, netSeries, allocationSeries };` and render `<AllocationsTabs {...payload} exposure={exposure} />`. The id is `user.id` from `supabase.auth.getUser()` (already derived above the call — the rls-auditor carry-forward: the RSC passes the AUTHENTICATED id; nothing client-supplied reaches the reads). Do NOT wrap the Promise.all in try/catch — the read layer throws on PostgREST errors and the throw must reach the existing `allocations/error.tsx` boundary (an error and an empty book are distinct states; never collapse an error into empty-state copy). Do NOT fold the reads into getMyAllocationDashboard (that payload has a client refresh/poll path; the exposure reads are a daily-grain 730-day paged scan that must run once per page load). Leave the onboarding-funnel `createAdminClient` block byte-identical.

    3. AllocationsTabs.tsx: widen the signature to `props: MyAllocationDashboardPayload & { exposure: ExposureSectionData }` (type import from ./lib/exposure-props). The existing `{...props}` spreads already forward `exposure` to HoldingsTabPanel; other panels typed as plain MyAllocationDashboardPayload tolerate the extra spread prop (TS excess-property checks don't apply to spreads). No other logic changes.

    4. HoldingsTabPanel.tsx: signature `props: MyAllocationDashboardPayload & { exposure: ExposureSectionData }`. Insert between the `<HoldingsTable strategyRows…/>` (Section 1) and the Exchange Positions `<section>` — the UI-SPEC section anatomy verbatim:
       `<section aria-label="Exposure">` → `<h3 className="text-sm font-semibold uppercase tracking-wider text-text-primary">Exposure</h3>` (byte-matches the Exchange Positions heading idiom) → `<div className="mt-3 grid grid-cols-1 gap-4 lg:grid-cols-2">` containing `<ExposureByClass snapshot={props.exposure.snapshot} />`, `<NetExposureChart {...props.exposure.netSeries} />`, and `<div className="lg:col-span-2"><AllocationOverTime {...props.exposure.allocationSeries} /></div>`. DIRECT imports from `./widgets/positions/ExposureByClass`, `./widgets/positions/NetExposureChart`, `./widgets/allocation/AllocationOverTime` — do NOT add them to the B7b-locked `widgets/index.ts` WIDGET_COMPONENTS barrel. Everything else in the panel stays byte-identical (additive section only).

    5. Update the six pre-existing test files that render HoldingsTabPanel/AllocationsTabs: add `exposure={EMPTY_EXPOSURE}` (import from ./lib/exposure-props) to their render props. NO other fixture/assertion edits — if any pre-existing assertion needs changing, STOP and surface it (that would mean the section is not additive).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Auth id gate: `grep -c "getLatestExposureSnapshot(user.id)" "src/app/(dashboard)/allocations/page.tsx"` = 1, same for getNetExposureSeries(user.id) and getAllocationSeries(user.id); all three inside ONE Promise.all (source review); no other argument shape exists
    - Distinct prop gate: `grep -c "exposure" src/lib/queries.ts` unchanged from before this plan (reads NOT folded into getMyAllocationDashboard); AllocationsTabs receives `exposure={exposure}` explicitly
    - Error propagation: `grep -cE "try|catch" "src/app/(dashboard)/allocations/page.tsx"` returns 0 (page had none before; still none — reads throw to error.tsx)
    - No new secret surface: `grep -c "allocator_holdings" "src/app/(dashboard)/allocations/page.tsx"` = 0; the pre-existing funnel createAdminClient block is byte-identical (git diff shows no changes to it)
    - Barrel untouched: `git diff --name-only` does NOT contain widgets/index.ts
    - ALL pre-existing allocations tests pass with only the added exposure={EMPTY_EXPOSURE} prop (additive-section proof: zero assertion changes)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests — placement, empty trio, populated render, volume fixture, grep gates</name>
  <read_first>
    - .planning/phases/99-exposure-allocation-widgets/99-UI-SPEC.md (§ Verification intents 3, 6, 7 + § Placement)
    - src/app/(dashboard)/allocations/HoldingsTabPanel.tsx (as modified by Task 1)
    - src/app/(dashboard)/allocations/HoldingsTabPanel.test.tsx (existing render/mocking idiom to copy)
    - src/app/(dashboard)/allocations/widgets/attribution/attribution.test.tsx lines 1-20 (ResponsiveContainer jsdom mock)
  </read_first>
  <files>src/app/(dashboard)/allocations/HoldingsTabPanel.exposure.test.tsx</files>
  <behavior>
    - Test (placement/order): populated payload + EMPTY_EXPOSURE → DOM order is Strategies table, then heading "Exposure", then heading "Exchange Positions" (compareDocumentPosition or indexOf on textContent)
    - Test (empty trio): EMPTY_EXPOSURE → all three verbatim empty first-lines render ("No position snapshot yet." / "No exposure history yet." / "No allocation history yet.") and NO chart svg inside the Exposure section
    - Test (populated): exposure with a 3-slice snapshot + 5-point net series + 5-point/2-venue allocation series → widget titles "Exposure by asset class" / "Net exposure over time" / "Allocation over time" and their as-of stamps render
    - Test (volume, Phase-98 carry-forward at render level): 520 daily points × 6 venues allocation series + 520-point net series + 40-slice snapshot → renders without throwing and the Exposure section is present (proves the widgets handle a >1000-row-backed series shape; the LIVE read confirmation is Task 3)
  </behavior>
  <action>
    Create `HoldingsTabPanel.exposure.test.tsx` with the behaviors above, reusing the existing HoldingsTabPanel test fixture helpers for the MyAllocationDashboardPayload side and the recharts ResponsiveContainer mock idiom. Build the volume fixture programmatically (dates via a UTC day loop — no timezone drift). Then run the widget-boundary grep gates across the whole new surface as a test (or plain asserts in this file) mirroring UI-SPEC intent 6: read the three widget source files + chart-gaps.tsx + the modified page.tsx via node:fs and assert none contains `createAdminClient` or `allocator_holdings`, and each widget's portfolio-exposure import is `import type` — a source-level lock in the style of tests/visual/chart-accessibility-layer.test.ts so future edits cannot regress the trust boundary silently.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/HoldingsTabPanel.exposure.test.tsx" --no-file-parallelism && npm run lint && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All four behavior tests green; DOM-order assertion proves the section sits between Strategies and Exchange Positions
    - Source-lock test green: no createAdminClient / no allocator_holdings / type-only portfolio-exposure imports across ExposureByClass.tsx, NetExposureChart.tsx, AllocationOverTime.tsx, chart-gaps.tsx, page.tsx (page.tsx checked for allocator_holdings only — its funnel admin block is pre-existing and out of scope)
    - `npm run lint` clean (react-hooks + design-lint gates — tsc+vitest don't catch these) and `npx tsc --noEmit` clean
    - Volume fixture renders without error or truncation warnings
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Live large-allocator render + demo-hero visual pass (Phase-98 carry-forward)</name>
  <what-built>
    The Exposure section (PI-01 segmented bar + KPI strip + table, PI-02 net/gross chart, PI-03 stacked allocation) is wired into the Holdings tab, fully unit/integration tested. The Phase-98 pagination fix (F-1: >1000-row windows page via .range() until a short page) is mock-proven; this checkpoint confirms it against a LIVE database, and eyeballs the demo-hero surface. Before pausing, Claude has already: run the full allocations + widget test suites, lint, tsc, and `npm run build` — all green.
  </what-built>
  <action>
    PAUSE for human verification. Before pausing, ensure the automated preconditions in <what-built> are green (full allocations + widget suites, lint, tsc, npm run build). Present the how-to-verify steps to the user verbatim and wait for the resume signal. If no >1000-row live allocator exists and the user asks Claude to seed one, generate the SQL insert for the TEST project (qmnijlgmdhviwzwfyzlc, alloc@quantalyze.test) per step 1 — never seed prod.
  </action>
  <how-to-verify>
    1. LIVE >1000-row check (the carry-forward): use an allocator whose `allocator_holdings` rows within the last 730 days exceed 1000. If none exists, seed one on the TEST Supabase project (qmnijlgmdhviwzwfyzlc) for alloc@quantalyze.test — e.g. 6 venues × 4 symbols × 60 days ≈ 1440 rows via SQL insert — then run the app locally against the test project.
    2. Log in as that allocator, open My Allocation → Holdings tab.
    3. Verify PI-02/PI-03 series span the FULL seeded date range (first and last dates visible on the x-axis — a silently truncated read would end the series early with no gap marker).
    4. Verify the Exposure section reads as demo-hero quality: segmented bar + KPI strip coherent with the Exchange Positions tables below; gaps (if any seeded) show hatched "{days}d — no data" bands; no red/green anywhere on long/short direction.
    5. Design-review flag (from the UI-SPEC): the proportional hatched gap BAND is a documented geometric adaptation of the factsheet's zero-width seam (calendar-linear axis vs index axis). Confirm you accept the adaptation or route it to /design-review.
  </how-to-verify>
  <resume-signal>Type "approved" (optionally "approved, band adaptation accepted") or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → RSC | the allocator id used for the reads must derive from the session, never from client input |
| RSC → client bundle | exposure props cross into the ssr:false Holdings bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-07 | Spoofing | page.tsx read calls | mitigate | reads called with `user.id` from `supabase.auth.getUser()` only (grep-gated); RLS `allocator_holdings_owner_select` is the backstop |
| T-99-08 | Information Disclosure | exposure prop payload | mitigate | payload is the six-column secretless projection output; source-lock test forbids createAdminClient/allocator_holdings in the new surface |
| T-99-09 | Repudiation (data honesty) | error handling | mitigate | no try/catch in page.tsx — read errors hit error.tsx; widgets structurally cannot render empty-copy on error (they never fetch) |
| T-99-SC | Tampering | package installs | accept | no new packages in this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations" --no-file-parallelism` fully green (pre-existing + new)
- `npx vitest run tests/visual/chart-accessibility-layer.test.ts tests/visual/recharts-touchtooltip-usage.test.ts --no-file-parallelism` green
- `npm run lint`, `npx tsc --noEmit`, `npm run build` clean
- Human checkpoint approved (live >1000-row + visual + band-adaptation sign-off)
</verification>

<success_criteria>
PI-01/02/03 live on the Holdings tab in the UI-SPEC placement; auth-id, distinct-prop, and error-propagation invariants grep/test-locked; every pre-existing allocations test green with only additive fixture props; Phase-98 pagination carry-forward closed with a live render.
</success_criteria>

<output>
Create `.planning/phases/99-exposure-allocation-widgets/99-04-SUMMARY.md` when done. Record the checkpoint outcome (incl. the band-adaptation design-review decision) in the SUMMARY.
</output>
