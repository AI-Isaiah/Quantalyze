---
phase: 102-options-mtm-factsheet-composite-regression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/factsheet/composite-read-path.test.ts
  - src/lib/factsheet/types.ts
  - src/lib/factsheet/basis-metrics.test.ts
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/leverage-context.tsx
  - src/app/factsheet/[id]/v2/basis-context.tsx
  - src/app/factsheet/[id]/v2/basis-context.test.tsx
  - src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx
autonomous: true
requirements: [MTM-01, MTM-03]
tags: [mtm, factsheet, basis-toggle, options, sc-4, reason-copy]

must_haves:
  truths:
    - "A single-key options strategy with a DONE analytics row and a persisted metrics_json_by_basis.mark_to_market shows an ENABLED Mark-to-market segment; selecting it relabels the seven KPI scalars from the persisted MTM object"
    - "A single-key options strategy with an honest degrade reason shows the toggle DISABLED with accurate, reason-specific human copy"
    - "A row with computation_status not in {complete, complete_with_warnings} never presents a live-looking MTM object (F-4) — the payload carries no metricsByBasis at all for such a row"
    - "Every existing non-options / cash_settlement factsheet stays byte-identical: single-key without MTM data or reason renders NO toggle; the cash overlay at build-payload.ts:243 stays a no-op because a cash_settlement key is never threaded on the single-key arm (SC-4)"
    - "Leverage modeling never fabricates an MTM line: under basis=mark_to_market the leverage recompute short-circuits to the persisted overlay and no MODELED label renders"
  artifacts:
    - path: "src/lib/factsheet/composite-read-path.ts"
      provides: "singleKeyBasisOpts — the ONE shared single-key MTM read helper (F-4 gate + SC-4-safe threading)"
      exports: ["singleKeyBasisOpts"]
    - path: "src/app/factsheet/[id]/v2/page.tsx"
      provides: "computation_status in the strategy_analytics select; single-key arm threads singleKeyBasisOpts"
      contains: "computation_status"
    - path: "src/app/factsheet/[id]/v2/basis-context.tsx"
      provides: "honest human copy for all six MTM reasons + basis-agnostic default + mtmReasonTone"
      exports: ["mtmDisabledReasonCopy", "mtmReasonTone"]
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "widened toggle render gate (composite || mtmGate present) + tone-split disabled-reason color + leverage basis-guard"
  key_links:
    - from: "src/app/factsheet/[id]/v2/page.tsx"
      to: "src/lib/factsheet/composite-read-path.ts"
      via: "singleKeyBasisOpts call on the single-key else arm"
      pattern: "singleKeyBasisOpts"
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "payload.mtmGate"
      via: "render gate admits single-key MTM"
      pattern: "mtmGate != null"
    - from: "src/app/factsheet/[id]/v2/leverage-context.tsx"
      to: "basis-context.tsx"
      via: "mark_to_market identity short-circuit in useLeveragedMetrics"
      pattern: "mark_to_market"
---

<objective>
MTM-01: enable the factsheet `cash_settlement ↔ mark_to_market` SegmentedControl for a
single-key options strategy by READING the Phase-101-persisted
`metrics_json_by_basis.mark_to_market` object — behind the F-4 `computation_status`-DONE
gate — plus the delegated reason-copy pass (honest human copy for every MTM disabled
reason, including the rewritten stale `unsmoothed_options_book` string) and the
frontend half of the MTM-03 static byte-identity regression.

NO new valuation math (LOCKED). NO client-side MTM recompute (LOCKED). The read
machinery (`overlayBasisScalars`, `useBasisMetrics`) is already basis-agnostic; this
plan is the missing wiring + copy.

Purpose: the demo-adjacent honest-MTM story for the options book (Zavara) — a real
persisted MTM curve when present, a real reason when absent, never a fabricated line.
Output: wired single-key MTM read on BOTH factsheet surfaces (factsheet route +
discovery detail), widened render gate, honest reason copy with DESIGN.md-conformant
tone split, and an additive regression suite pinning F-4, SC-4, and the
leverage/MTM no-fabrication guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-options-mtm-factsheet-composite-regression/102-CONTEXT.md
@.planning/phases/102-options-mtm-factsheet-composite-regression/102-RESEARCH.md
@.planning/phases/101-options-mtm-toggle/101-01-SUMMARY.md
@.planning/phases/101-options-mtm-toggle/101-02-SUMMARY.md
@DESIGN.md

Key source (read before editing — extract, do not re-read):
@src/app/factsheet/[id]/v2/page.tsx           (:44 select, :90-120 composite/single-key arms)
@src/lib/factsheet/composite-read-path.ts     (:143-173 composite mtmGate; :175+ singleKeyDataQuality — the "one path" template)
@src/lib/factsheet/basis-metrics.ts           (:53 overlayBasisScalars, :88 hasBasisHeadline)
@src/lib/factsheet/build-payload.ts           (:60-64 BuildFactsheetOpts, :243 SC-4 cash overlay keystone, :346-347 forward)
@src/app/factsheet/[id]/v2/FactsheetView.tsx  (:1057-1195 ControlBar: :1067 composite, :1070-1074 leverageEligible, :1170 render gate, :1186-1192 reason <p>)
@src/app/factsheet/[id]/v2/basis-context.tsx  (:74-94 useBasisMetrics, :100-109 mtmDisabledReasonCopy)
@src/app/factsheet/[id]/v2/leverage-context.tsx (:96-141 useLeveragedMetrics — the cash-series recompute)
@src/lib/factsheet/types.ts                   (:485-498 metricsByBasis + mtmGate)
@src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx (:54-115 the mirrored single-key arm)

<interfaces>
Contracts the executor needs (verified against current tree — use directly):

From src/lib/factsheet/basis-metrics.ts:
- `overlayBasisScalars(base: ComputeSummary, serverScalars: Record<string, number> | null | undefined): ComputeSummary` — returns `base` UNCHANGED when serverScalars is null/undefined (the SC-4 keystone at build-payload.ts:243).
- `hasBasisHeadline(obj: unknown): boolean` — the ONE "is this basis displayable?" predicate. Do not hand-roll.

From src/lib/factsheet/types.ts:485-498 (FactsheetPayload):
- `metricsByBasis?: { cash_settlement: Record<string, number>; mark_to_market?: Record<string, number> }` — `cash_settlement` is currently REQUIRED; Task 1 widens it to optional.
- `mtmGate?: { available: boolean; reason?: "unsmoothed_options_book" | "mtm_basis_unavailable_for_venue" | string }` — open union with string tail; no union edit required to render new reasons.

From src/lib/factsheet/build-payload.ts:60-64 (BuildFactsheetOpts):
- `metricsByBasis?: FactsheetCommon["metricsByBasis"]; mtmGate?: FactsheetCommon["mtmGate"]` — already forwarded verbatim at :346-347. NO build-payload edit needed.

From src/app/api/factsheet/[id]/pdf/route.ts:231-232 — the computation_status DONE literals:
- `"complete"` and `"complete_with_warnings"` (exact strings; A2 in RESEARCH — spot-verify no third terminal-success literal exists by grepping computation_status writes in analytics-service/services/analytics_runner.py before hardcoding).

From src/app/factsheet/[id]/v2/leverage-context.tsx:96-141:
- `useLeveragedMetrics(payload)` composes `useBasisMetrics` then, when `appliedLeverage !== 1 && periodsPerYear != null`, DISCARDS the basis-overlaid `m` and recomputes from `payload.strategyReturns` (the CASH series). This is the fabrication hazard Task 2 closes.
</interfaces>
</context>

<constraints>
- Frontend is Next.js with breaking changes vs training data (AGENTS.md). Before any
  routing/RSC/caching claim, read the relevant guide in `node_modules/next/dist/docs/`.
  The only RSC-adjacent edit here is one select-column addition inside the existing
  `unstable_cache`-wrapped `fetchAndBuildPayload` — do not restructure the cache.
- NO git branch operations. Commit per task on the current branch.
- NO new packages (RESEARCH: zero-install phase).
- Never derive compositeness or options-ness from `apiKeyId` / `daily_returns === null`
  (Pitfall 1). Server truth only.
- Never edit an existing cash golden/byte-identity assertion to make room for an MTM
  case (Pitfall 5) — ADD fixtures. The ONLY sanctioned existing-string edits are the
  two verbatim pins of the stale `unsmoothed_options_book` HUMAN copy
  (`basis-context.test.tsx:136`, `FactsheetBody.basis.test.tsx:184`) whose rewrite is
  mandated by CONTEXT, PLUS the default-copy assertion at `basis-context.test.tsx:141-144`
  (the unknown-reason + `undefined` fallback, which pins the OLD `"…for this composite."`
  default that Task 3 rewrites to the basis-agnostic `"…for this strategy."` — grep-confirmed
  there is NO analogous default-copy pin in `FactsheetBody.basis.test.tsx`), plus the
  FactsheetView leverage-comment update in Task 2.
- Never assert MTM == cash numeric equality (Pitfall 3 — MTM re-dates premium by design).
- DESIGN.md governs all copy/visual decisions: amber `--color-warning #B45309` is
  reserved for transient/recoverable states; steady-state honest-empty uses
  `text-text-muted #64748B`. 12px caption tier; DM Sans labels.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: singleKeyBasisOpts contract — F-4 gate + SC-4-safe threading (helper + types)</name>
  <files>src/lib/factsheet/composite-read-path.ts, src/lib/factsheet/composite-read-path.test.ts, src/lib/factsheet/types.ts, src/lib/factsheet/basis-metrics.test.ts</files>
  <behavior>
    New tests in composite-read-path.test.ts (RED first — write before implementing):
    - Test F4-1 (Wave-0 gap): a single-key row with `metrics_json_by_basis = {mark_to_market: {seven finite scalars}}` but `computation_status = "failed"` (and separately `"computing"`, `undefined`) yields `mtmGate.available === false` AND `metricsByBasis === undefined` (no live-looking MTM object escapes). MUST go RED if the status check is neutered to always-true.
    - Test F4-2: same row with `computation_status = "complete"` yields `mtmGate.available === true` and `metricsByBasis = { mark_to_market: {...} }`. Also passes for `"complete_with_warnings"`.
    - Test SC4-1 (stale-key hazard, observed in 101-01 "Observed-but-out-of-scope #1"): a single-key row whose raw `metrics_json_by_basis` contains a LINGERING `cash_settlement` key (composite→single stale window) NEVER threads it — the returned `metricsByBasis`, when present, contains ONLY `mark_to_market`. MUST go RED if the helper passes the raw column through.
    - Test HONEST-1: no MTM key + `mtm_gated_reason = "mtm_summary_coverage_incomplete"` yields `mtmGate = { available: false, reason: "mtm_summary_coverage_incomplete" }` and `metricsByBasis === undefined`.
    - Test SILENT-1: no MTM key AND no reason (every non-options single-key strategy) yields an EMPTY result — no `mtmGate`, no `metricsByBasis` — so the toggle never renders and the payload is byte-identical to today.
    - Test DEGEN-1: MTM key present but degenerate (fails `hasBasisHeadline`, e.g. all-null scalars) with status done yields `available === false` (and no metricsByBasis threaded).
    New test in basis-metrics.test.ts (additive case, existing assertions untouched):
    - a `mark_to_market`-ONLY by-basis object leaves `overlayBasisScalars(base, obj.cash_settlement)` returning `base` by reference/value (cash untouched — the SC-4 keystone unit pin).
  </behavior>
  <action>
    In `src/lib/factsheet/composite-read-path.ts`, add and export `singleKeyBasisOpts(dqf, metricsJsonByBasis, computationStatus)` — the single-key counterpart of the composite mtmGate assembly at :167-170, colocated with `singleKeyDataQuality` per the "one path" lesson (both factsheet surfaces MUST consume the same owner so they cannot diverge). Signature: `dqf` is the same loosely-typed data_quality_flags object `singleKeyDataQuality` takes; `metricsJsonByBasis: unknown` (raw jsonb); `computationStatus: unknown`. Returns `Pick<BuildFactsheetOpts, "metricsByBasis" | "mtmGate">` (possibly `{}`).

    Logic (defensive on unknown jsonb, mirroring the file's existing strict-coercion style):
    1. Extract `mtm` = `metricsJsonByBasis.mark_to_market` iff `metricsJsonByBasis` is a non-null non-array object and the key is a non-null non-array object; else undefined.
    2. Extract `reason` = `dqf.mtm_gated_reason` iff a string (same coercion as :169); else undefined.
    3. If neither `mtm` nor `reason` exists → return `{}` (non-options single-key: nothing renders, byte-identical — the ONLY branch every existing non-options strategy hits).
    4. `done` = `computationStatus === "complete" || computationStatus === "complete_with_warnings"` (F-4; exact PDF-route literals — verify per interfaces note).
    5. `available` = `done && hasBasisHeadline(mtm)`.
    6. Return `{ metricsByBasis: available ? { mark_to_market: mtm } : undefined, mtmGate: { available, reason } }`. CRITICAL: never pass the raw column through — thread ONLY the `mark_to_market` key, and ONLY when available. This makes F-4 structural (a failed row's payload carries NO MTM object) and closes the 101-01 stale-cash_settlement-key hazard by construction (a threaded stale cash key would activate the build-payload:243 overlay and perturb the cash headline — SC-4 breach).

    In `src/lib/factsheet/types.ts:486`, widen `cash_settlement: Record<string, number>` to `cash_settlement?: Record<string, number>` and update the :480-484 docstring: cash_settlement always present on a composite, ABSENT on a single-key options payload (which carries only mark_to_market — Phase 101 decision). Then `grep -rn "metricsByBasis" src --include="*.ts" --include="*.tsx"` and confirm every `.cash_settlement` consumer already optional-chains (composite-read-path.ts:95 and build-payload.ts:243 verified to; fix any other non-optional access found). Run `npx tsc --noEmit` to prove the widening breaks nothing.

    Write the behavior tests RED first against the not-yet-implemented export, then implement to GREEN. Commit test + impl per the RED/GREEN cycle (2 commits: `test(102-01): ...` then `feat(102-01): ...`).
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/composite-read-path.test.ts src/lib/factsheet/basis-metrics.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>singleKeyBasisOpts exported with all six behavior tests green; neutering the status check (always-done) makes F4-1 RED; passing the raw column through makes SC4-1 RED; tsc clean; existing composite-read-path + basis-metrics assertions untouched and green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire both surfaces + widen the render gate + leverage/MTM no-fabrication guard</name>
  <files>src/app/factsheet/[id]/v2/page.tsx, src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/leverage-context.tsx, src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx</files>
  <behavior>
    Additive fixtures in FactsheetBody.basis.test.tsx (existing assertions 4-5 and all composite/cash assertions stay byte-untouched):
    - Test SK-TOGGLE-1: a single-key payload (dataQuality.composite absent/false) WITH `mtmGate: {available: true}` + `metricsByBasis: {mark_to_market: {...}}` renders the SegmentedControl; activating mark_to_market relabels the mapped KPI scalars from the persisted object (reuse the file's existing composite toggle-activation pattern).
    - Test SK-TOGGLE-2: a single-key payload with `mtmGate: {available: false, reason: "mtm_summary_coverage_incomplete"}` renders the control with the mark_to_market option `aria-disabled="true"` and the mapped reason copy in the inline paragraph.
    - Test SK-BYTE-1: a single-key payload with NO mtmGate renders NO SegmentedControl (markup for the control region absent — the existing no-toggle byte-identity scope, as a NEW fixture).
    - Test LEV-MTM-1 (no-fabrication guard): with leverage set to 2 and basis switched to mark_to_market, the KPI cells show the PERSISTED MTM scalars (not leverage-scaled cash values) and NO "MODELED" eyebrow renders. MUST go RED if the useLeveragedMetrics basis guard is removed.
  </behavior>
  <action>
    Read the relevant guide in `node_modules/next/dist/docs/` for data fetching/caching before touching page.tsx (AGENTS.md mandate).

    1. `src/app/factsheet/[id]/v2/page.tsx`: add `computation_status` to the `strategy_analytics ( ... )` select at :44 (Pitfall 2 — without this the gate is always-closed). In the single-key else arm (:111-120), extend the buildOpts assignment to spread `singleKeyBasisOpts(dqf, analytics?.metrics_json_by_basis, analytics?.computation_status)` alongside the existing `dataQuality: singleKeyDataQuality(dqf)`. Note in a comment: the F-4 gate rides the `${id}::${computedAt}` cache key (:344) because a re-derive stamps a fresh computed_at; status is public-safe on a published row (unchanged RLS boundary — the outer request-scoped signature probe stays the auth gate).
    2. `src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx`: mirror the same spread on its single-key else arm (:106-115). Add `computation_status?: unknown` to the analyticsRow cast (:54-62). Verify the feeding query selects it: the detail fetch uses `strategy_analytics (*)` (src/lib/queries.ts:218/:416) so the column arrives — confirm by grep, and if the page's fetch turns out to use a narrower column list, add `computation_status` there too. The "one path" comments in both files already mandate this mirroring.
    3. `src/app/factsheet/[id]/v2/FactsheetView.tsx` ControlBar: widen the :1170 render gate from `composite &&` to `(composite || payload.mtmGate != null) &&` — server-truth predicate only (Pitfall 1: never an api-key heuristic). Update the :1062-1065 comment (no longer composite-only). The :1070-1074 leverageEligible comment claims the leverage cluster and basis control "never coexist" — now they CAN (single-key options): keep both rendering (leverage first, basis control second; both are `mr-auto` flex children in a wrap container — visually verify no overlap at narrow widths via the existing test render), but gate the leverage CLUSTER on `basis === "cash_settlement"` as part of the fabrication guard below, and rewrite that comment honestly.
    4. `src/app/factsheet/[id]/v2/leverage-context.tsx` (no-fabrication guard — LOCKED no-invented-data): in `useLeveragedMetrics`, extend the identity short-circuit (:124) to also short-circuit when `basis === "mark_to_market"` — leverage models the CASH return path (`payload.strategyReturns` IS the cash series); recomputing it under an MTM label would fabricate an MTM line. Returns the basis-overlaid `m` with `modeled: false` so the MODELED eyebrow can never decouple (same SFH-3/IN-01 principle documented at :100-106). In ControlBar, condition `leverageEligible` additionally on `basis === "cash_settlement"` so the input hides while MTM is displayed (leverage state is ephemeral context — flipping back to cash restores it). Both guards, not either: the hook guard is the invariant, the UI guard prevents a dead input.
    5. Write the four behavior tests RED first (the LEV-MTM-1 red run is against the unguarded hook), then implement to GREEN.

    Byte-identity self-check before commit: run the FULL FactsheetBody.basis.test.tsx + FactsheetBody.guard04-no-bleed.test.tsx suites — every pre-existing assertion must pass unmodified (composite behavior unchanged; single-key non-options unchanged; no storage write on toggle).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx" "src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <done>Single-key options payloads render the toggle (enabled with data, disabled-with-reason without); single-key non-options render no toggle (fixture-pinned); leverage≠1 under MTM basis shows persisted MTM scalars with no MODELED label and goes RED if the hook guard is neutered; all pre-existing basis/guard04 assertions green unmodified; both surfaces consume the ONE shared helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Honest reason copy + DESIGN.md tone split (delegated copy pass)</name>
  <files>src/app/factsheet/[id]/v2/basis-context.tsx, src/app/factsheet/[id]/v2/basis-context.test.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <behavior>
    In basis-context.test.tsx (update the stale-copy pin at :136 AND the default-copy assertion at :141-144 — the unknown-reason + `undefined` fallback — to the new basis-agnostic `"Mark-to-market unavailable for this strategy."` default; all OTHER existing assertions untouched; new cases additive):
    - mtmDisabledReasonCopy returns the exact new string for each of: unsmoothed_options_book (rewritten), mtm_summary_coverage_incomplete, mtm_series_uncomputable, mtm_second_pass_timeout, mtm_anchor_race, and the basis-agnostic default; mtm_basis_unavailable_for_venue stays byte-identical.
    - mtmReasonTone returns "transient" for mtm_second_pass_timeout and mtm_anchor_race; "steady" for every other input including undefined and unknown strings.
    In FactsheetBody.basis.test.tsx: update the one stale-copy pin at :184 to the rewritten unsmoothed_options_book string (assertion structure unchanged).
  </behavior>
  <action>
    Rewrite `mtmDisabledReasonCopy` (`basis-context.tsx:100-109`) with these EXACT strings (character-exact — they become the new pins; Fable-authored per delegation, DESIGN.md voice: factual, institutional, no contractions, never fabricating, coverage-mask honesty):
    - `unsmoothed_options_book` → "Mark-to-market unavailable: composites that include an options book report cash settlement only." (the honest CURRENT meaning — it is the live composite-gate return at stitch_composite.py:303; the dropped Phase-83 smoothing framing is gone)
    - `mtm_basis_unavailable_for_venue` → UNCHANGED: "Mark-to-market unavailable for this venue."
    - `mtm_summary_coverage_incomplete` → "Mark-to-market unavailable: settlement history does not fully cover this book, so a mark-to-market series cannot be reconstructed."
    - `mtm_series_uncomputable` → "Mark-to-market unavailable: the reconstructed mark-to-market series could not produce valid metrics."
    - `mtm_second_pass_timeout` → "Mark-to-market temporarily unavailable: reconstruction exceeded its time budget and will be retried on the next data refresh."
    - `mtm_anchor_race` → "Mark-to-market temporarily unavailable: the account changed during reconstruction; it will be recomputed on the next data refresh." (the reason constant ships in plan 102-02 — the string literal "mtm_anchor_race" is the cross-language contract; both plans pin the same literal)
    - default → "Mark-to-market unavailable for this strategy." (basis-agnostic — the old "for this composite" default is wrong for single-key; RESEARCH Also-Surface)
    Update the function docstring: still server-truth closed-switch + graceful default; no TS union edit required (types.ts:497 open union — adding literals is optional polish, skip it for the shortest diff).

    Add and export `mtmReasonTone(reason?: string): "transient" | "steady"` in basis-context.tsx: "transient" ONLY for `mtm_second_pass_timeout` and `mtm_anchor_race` (system re-attempts on the next derive — DESIGN.md amber = transient/recoverable); "steady" for everything else (coverage hole, series-uncomputable, venue, options-composite, unknown/default — steady-state honest-empty; amber would falsely signal self-healing; RESEARCH Pitfall 4 + DESIGN.md Color: "warning is reserved for transient recoverable states").

    In FactsheetView ControlBar, apply the tone to the inline disabled-reason `<p>` (:1186-1192): tone "transient" keeps the existing `style={{ color: "var(--color-warning, #B45309)" }}`; tone "steady" drops the inline style for `className="text-caption text-text-muted"` (#64748B — WCAG-AA 4.85:1 on white per DESIGN.md). Keep `text-caption`; no new type classes (v2 type contract). Note: this changes the rendered color of the existing composite steady-state reasons from amber to muted — a DESIGN.md-conformance fix (they were mis-toned), NOT a byte-identity breach (SC-4 scopes payload/metrics, and the copy for those reasons changes in this same task by mandate).

    Write the copy/tone tests RED first, then implement. Sweep: `grep -rn "un-smoothed\|Phase-83" src` must return ZERO hits after this task.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/basis-context.test.tsx" "src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx" && ! grep -rn "un-smoothed\|Phase-83" src --include="*.ts" --include="*.tsx" | grep -v node_modules | grep .</automated>
  </verify>
  <done>All six reason strings + default render character-exact per the authored copy; tone split pinned (transient=amber only for timeout/anchor-race, steady=muted otherwise); zero stale smoothing references remain in src/; only the two sanctioned stale-copy pins were edited among pre-existing assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public web → RSC read path | anonymous/authed users request published factsheets; RLS signature probe is the auth gate |
| DB jsonb → rendered DOM | `mtm_gated_reason` / `metrics_json_by_basis` are server-controlled but loosely-typed jsonb crossing into React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering (trust) | single-key MTM read (`singleKeyBasisOpts`) | mitigate | F-4: `computation_status ∈ {complete, complete_with_warnings}` gate + `hasBasisHeadline` strictness; failed row payload carries NO MTM object (Task 1, falsifiable test F4-1) |
| T-102-02 | Information disclosure | cached `fetchAndBuildPayload` select | mitigate | only `computation_status` added — public-safe on a published row (already exposed via PDF-route behavior); no per-user-filtered column enters the shared cache; cache key unchanged |
| T-102-03 | Tampering | reason string → DOM tooltip/paragraph | mitigate | closed `mtmDisabledReasonCopy` switch with honest generic default; raw server text never interpolated (Task 3) |
| T-102-04 | Tampering (integrity) | leverage recompute under MTM label | mitigate | basis guard in `useLeveragedMetrics` + hidden input; falsifiable test LEV-MTM-1 (Task 2) |
| T-102-SC | Tampering | npm installs | accept | zero-install phase (RESEARCH Package Legitimacy Audit: N/A); any install attempt is out of plan scope and must stop |
</threat_model>

<verification>
- `npx vitest run "src/lib/factsheet/composite-read-path.test.ts" src/lib/factsheet/basis-metrics.test.ts "src/app/factsheet/[id]/v2/basis-context.test.tsx" "src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx" "src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx"` — all green.
- `npx tsc --noEmit` and `npm run lint` — clean (lint catches react-hooks errors tsc misses; MEMORY).
- Byte-identity spot-check: `git diff --stat` on this plan's commits touches ONLY the files_modified list; no cash golden, no frozen chart island, no `factsheet-context.tsx`.
</verification>

<success_criteria>
- MTM-01: single-key options factsheet toggle wired end-to-end on both surfaces from the persisted object, F-4-gated, honest disabled-with-reason otherwise.
- MTM-03 (frontend static half): F-4, SC-4 stale-key, no-toggle byte-identity, and leverage/MTM no-fabrication all pinned by falsifiable additive tests; every pre-existing basis/guard/parity assertion green unmodified except the two sanctioned stale-copy pins.
- Delegated copy pass complete: six honest reason strings + basis-agnostic default + DESIGN.md tone split shipped.
- NO live MTM data claim anywhere: live Zavara corroboration is the ship-time OQ-3 gate (see plan 102-02 output notes) — no task or test in this plan asserts live DB state.
</success_criteria>

<output>
Create `.planning/phases/102-options-mtm-factsheet-composite-regression/102-01-SUMMARY.md` when done
(use Write only for this NEW file; never Write-truncate existing .planning docs — Edit only).
The SUMMARY must note: the leverage/MTM fabrication hazard found + closed, the exact copy strings shipped,
and that live MTM rendering awaits the ship-time OQ-3 re-derive backfill (do not claim live attestation).
</output>
