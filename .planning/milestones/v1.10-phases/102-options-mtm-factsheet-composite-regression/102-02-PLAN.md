---
phase: 102-options-mtm-factsheet-composite-regression
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/stitch_composite.py
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_mtm_single_key.py
  - analytics-service/tests/test_stitch_composite.py
autonomous: true
requirements: [MTM-02, MTM-03]
tags: [mtm, composite, stitch, anchor-race, sc-4, regression]

must_haves:
  truths:
    - "An options-member composite stays honestly gated OFF mark_to_market with reason unsmoothed_options_book, its metrics_json_by_basis carries NO mark_to_market key, and per-member coverage gaps stay MARKED (per_key n_days:0 / coverage_mask), never zero-filled (Option A disposition)"
    - "A perp-only all-native composite still admits mark_to_market end-to-end (verify-only — no behavior change)"
    - "A mid-crawl account event during the single-key MTM second pass stamps the honest transient reason mtm_anchor_race — not the permanent-sounding mtm_summary_coverage_incomplete — and cash STILL ships (degrade, never retry-to-failed)"
    - "Every existing cash pin (golden/parity/acceptance) is green with ZERO edits; the only red in the full suite is the pre-existing test_audit taxonomy failure"
  artifacts:
    - path: "analytics-service/services/stitch_composite.py"
      provides: "MTM_REASON_ANCHOR_RACE constant (single admissibility-vocabulary owner)"
      contains: "MTM_REASON_ANCHOR_RACE = \"mtm_anchor_race\""
    - path: "analytics-service/services/job_worker.py"
      provides: "InceptionReconciliationError classified as mtm_anchor_race inside the existing MTM structural-degrade catch (degrade semantics unchanged)"
      contains: "MTM_REASON_ANCHOR_RACE"
    - path: "analytics-service/tests/test_stitch_composite.py"
      provides: "options-member composite honest-disabled + marked-coverage compose pins"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "analytics-service/services/stitch_composite.py"
      via: "MTM_REASON_ANCHOR_RACE import at the existing reason-constant import site (~:2070)"
      pattern: "MTM_REASON_ANCHOR_RACE"
    - from: "analytics-service/tests/test_stitch_composite.py"
      to: "services.stitch_composite.mark_to_market_available"
      via: "value imports of the reason constants (rename-decouple guard, mirrors 101-02 pattern)"
      pattern: "MTM_REASON_OPTIONS"
---

<objective>
MTM-02 (Option A — the Q3 disposition): options-member composites STAY honestly gated
OFF mark_to_market; the compose contract is pinned by tests, not changed. Plus the
Phase-101 deferred `mtm_anchor_race` known-limitation resolution (a distinct transient
reason so a mid-crawl race is never mislabeled as a permanent coverage hole), and the
analytics half of the MTM-03 static byte-identity regression gate.

Q3 DISPOSITION (decided, per RESEARCH Q3 + CONTEXT locked boundary): **Option A.**
Rationale: (1) Option B (composing MTM across members) crosses the LOCKED "NO new
valuation math" boundary — it inherits the single-key coverage/anchor risks plus the
divergent-degraded-member TOCTOU at composite scope, a real feature for a separate
milestone; (2) the only live options book (Zavara) is SINGLE-KEY, so there is no live
options composite to serve; (3) honest disabled-with-reason is the coverage-mask-voice
correct behavior today. No compelling reason for B exists — no escalation needed.
Do NOT flip `mark_to_market_available`; do NOT touch the composite MTM pass.

NO new valuation math. The ONLY production-code change in this plan is reason
CLASSIFICATION inside an existing degrade path (still degrades, cash still ships).

Purpose: composite MTM honesty pinned + the anchor-race mislabel fixed before its
reason UI (plan 102-01) makes it user-visible.
Output: MTM_REASON_ANCHOR_RACE + classification, compose pins, full-suite regression gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/102-options-mtm-factsheet-composite-regression/102-CONTEXT.md
@.planning/phases/102-options-mtm-factsheet-composite-regression/102-RESEARCH.md
@.planning/phases/101-options-mtm-toggle/101-01-SUMMARY.md
@.planning/phases/101-options-mtm-toggle/deferred-items.md

Key source (read before editing — extract, do not re-read):
@analytics-service/services/stitch_composite.py   (:101-128 reason constants + docstrings, :290-306 mark_to_market_available)
@analytics-service/services/job_worker.py         (~:2063-2070 reason-constant imports; ~:2375-2398 the MTM structural catch `as _mtm_exc` stamping MTM_REASON_SUMMARY_COVERAGE; ~:2983 the comment referencing the catch; :4171-4324 composite MTM gate + coverage mask — READ-ONLY this plan)
@analytics-service/services/native_nav.py         (:137 InceptionReconciliationError(NavReconstructionError); :594 permanent/structural doc; :745,:763 raise sites)
@analytics-service/tests/test_mtm_single_key.py   (the 101-01 test module — fixture/mocking patterns to reuse)
@analytics-service/tests/test_stitch_composite.py (existing mark_to_market_available + coverage tests)

<interfaces>
Contracts verified against the current tree:
- `mark_to_market_available(members: Sequence[MemberBasisSignal]) -> tuple[bool, str | None]` (stitch_composite.py:290) — options member → `(False, MTM_REASON_OPTIONS)` checked FIRST; ccxt venue → `(False, MTM_REASON_VENUE)`; else `(True, None)`. DO NOT MODIFY (Option A).
- `MemberBasisSignal(seq, venue, has_option_activity)` (stitch_composite.py:~90).
- `InceptionReconciliationError` (native_nav.py:137) subclasses `NavReconstructionError`, which is already in the MTM structural catch tuple (job_worker.py ~:2375-2381) — so today it degrades with `MTM_REASON_SUMMARY_COVERAGE` (:2398). The classification change is INSIDE this catch.
- Composite coverage mask: `per_key` entries with `n_days: 0` for degraded/gap members (job_worker.py:4045-4055) — the marked-never-zero-filled contract to pin.
- Test interpreter: `analytics-service/.venv/bin/python -m pytest` (ambient Homebrew 3.14 lacks pandera — 101-02 deviation note). Run from `analytics-service/`.
</interfaces>
</context>

<constraints>
- NO git branch operations. Commit per task on the current branch.
- NO new packages. NO migration. NO change to `mark_to_market_available` or the
  composite MTM pass (job_worker.py:4171-4324 is read-only in this plan).
- Line numbers above drifted ~80 lines from the 101-01 SUMMARY already — re-locate by
  grep (`as _mtm_exc`, `MTM_REASON_SUMMARY_COVERAGE`), never by absolute line.
- Never assert MTM == cash numeric equality (Pitfall 3). Never edit an existing cash
  golden/acceptance assertion (Pitfall 5) — this plan ADDS tests only.
- The degrade contract is sacred: the anchor-race classification must NOT convert any
  degrade into a propagate/retry — a persistent inception breach retry-to-failed_final
  would sink the healthy cash headline (deferred-items.md option-2 risk, rejected).
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MTM_REASON_ANCHOR_RACE — honest transient classification (deferred-item resolution)</name>
  <files>analytics-service/services/stitch_composite.py, analytics-service/services/job_worker.py, analytics-service/tests/test_mtm_single_key.py</files>
  <behavior>
    New tests in test_mtm_single_key.py (reuse the module's existing options-book derive fixtures/mocks; RED first):
    - Test RACE-1: an options-book derive whose MTM second pass raises `InceptionReconciliationError` (simulating the mid-crawl-event race — patch the MTM-pass reconstruction to raise it) DEGRADES with `data_quality_flags.mtm_gated_reason == "mtm_anchor_race"` (import the constant from services.stitch_composite — the 101-02 rename-decouple guard pattern), the by-basis `mark_to_market` write is SQL NULL (stale-heal), and the cash derive COMPLETES (job success, cash returns persisted). MUST go RED if the isinstance classification is neutered (reason falls back to mtm_summary_coverage_incomplete).
    - Test RACE-2: a NON-inception structural failure (e.g. `LedgerCompletenessError` or a plain `NavReconstructionError`) still stamps `mtm_summary_coverage_incomplete` — the coverage reason is not hijacked by the new branch.
    - Test RACE-3: the transient-propagation contract is untouched — a bare `ValueError` from the MTM pass still PROPAGATES (existing test_transient_valueerror_on_mtm_propagates stays green unmodified; assert-by-running, no edit).
  </behavior>
  <action>
    1. `stitch_composite.py`: add `MTM_REASON_ANCHOR_RACE = "mtm_anchor_race"` alongside the other MTM_REASON_* constants (:101-128) — this module is the single admissibility-vocabulary owner (single-key and composite reasons never fork). Docstring (mirror the neighbors' style): Phase 102 resolution of the 101 deferred same-anchor race — the single-key MTM second pass deliberately REUSES the cash pass's account_state anchor (same terminal NAV = comparable bases); an event landing mid-crawl appears in the MTM rows but not the anchor → §5 InceptionReconciliationError. Classified as its OWN transient reason so the disabled-with-reason UI (plan 102-01 maps it to "recomputed on the next data refresh" copy, amber tone) never shows a permanent-sounding coverage explanation for a self-healing race. STILL DEGRADES (cash ships) — the propagate-to-retry alternative was rejected because InceptionReconciliationError is documented permanent/structural (native_nav.py:594) and a persistent breach would retry the whole derive to failed_final, sinking the healthy cash headline. Honest caveat: a genuinely persistent MTM-only reconciliation breach also lands here and will re-stamp the same reason on every derive — the copy therefore promises only re-computation, never recovery.
    2. `job_worker.py`: locate the MTM structural catch by grepping `as _mtm_exc` (currently ~:2375-2398). Inside the existing except block, replace the unconditional `mtm_gated_reason = MTM_REASON_SUMMARY_COVERAGE` with: `MTM_REASON_ANCHOR_RACE if isinstance(_mtm_exc, InceptionReconciliationError) else MTM_REASON_SUMMARY_COVERAGE`. Do NOT add a separate except clause and do NOT alter the catch tuple (bare ValueError stays excluded — 101-01 decision). Add `MTM_REASON_ANCHOR_RACE` to the existing reason-constant import (~:2070) and `InceptionReconciliationError` to the native_nav imports if not already in scope at that site (grep first — it may be). Also grep for the stale comment near ~:2983 that says the catch "keeps MTM_REASON_SUMMARY_COVERAGE" and update it to name both reasons. The compute-level degrades (MTM_REASON_SERIES_UNCOMPUTABLE / MTM_REASON_SECOND_PASS_TIMEOUT) are untouched.
    3. Write RACE-1/RACE-2 RED first, implement to GREEN, run RACE-3's existing test to confirm untouched. Neuter-falsifiability sweep (101 house style): temporarily remove the isinstance branch → RACE-1 RED; restore → green; record in the SUMMARY.
    4. mypy/ruff hygiene: run `.venv/bin/python -m mypy services/job_worker.py services/stitch_composite.py` if the repo gates it (match 101 practice); do not touch pre-existing ruff findings outside the edited ranges.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_mtm_single_key.py -x -q</automated>
  </verify>
  <done>MTM_REASON_ANCHOR_RACE exists in the vocabulary owner; InceptionReconciliationError in the MTM pass stamps mtm_anchor_race while every other structural family keeps the coverage reason; cash still ships on every degrade; transient ValueError still propagates; RACE-1 proven falsifiable by neuter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Option A compose pins + full-suite MTM-03 regression gate</name>
  <files>analytics-service/tests/test_stitch_composite.py</files>
  <behavior>
    New tests in test_stitch_composite.py (additive; Wave-0 gap from RESEARCH — the options-composite honest-disabled + marked-coverage compose pin):
    - Test COMPOSE-1 (honest-disabled): `mark_to_market_available` over a member set containing one `has_option_activity=True` member returns exactly `(False, MTM_REASON_OPTIONS)` — value-import the constant (rename-decouple guard). Assert the reason string equals the literal "unsmoothed_options_book" so the frontend copy switch in plan 102-01 can never silently decouple from the machine reason.
    - Test COMPOSE-2 (marked, never zero-filled): extend/reuse the existing composite-stitch fixtures to pin that a composite with a coverage gap keeps the gap MARKED — the stitched sparse series contains NO fabricated 0.0 rows for gap days, and the degraded/gap member's `per_key` entry carries `n_days: 0` (the job_worker.py:4045-4055 contract). If an equivalent pin already exists in test_stitch_composite.py or test_stitch_composite_job.py, extend it to ALSO assert the options-member case (an options-member composite gets `mtm_gated_reason == MTM_REASON_OPTIONS` and its metrics_json_by_basis has NO mark_to_market key) rather than duplicating.
    - Test COMPOSE-3 (perp-only verify): all-native perp-only member set returns `(True, None)` — if this exact pin already exists, confirm it runs green (verify-only; no duplicate).
  </behavior>
  <action>
    Implement the compose pins per behavior. Grep test_stitch_composite.py and test_stitch_composite_job.py FIRST for existing `mark_to_market_available` / coverage-mask assertions — extend rather than duplicate (the existing suite already covers parts of this; the NEW guarantees are the options-member honest-disabled pin tied to the exact reason literal and the no-mark_to_market-key-persisted pin).

    Then run the MTM-03 analytics regression gate (the phase's SC-4 evidence):
    1. Cash pins, zero edits: `cd analytics-service && .venv/bin/python -m pytest tests/test_golden_parity.py tests/test_metrics_parity.py tests/test_metrics_minigolden.py tests/test_composite_headline_parity.py tests/test_zavara_acceptance.py tests/test_deribit_acceptance.py tests/test_deribit_ground_truth.py tests/test_allocated_capital.py tests/test_deribit_txn.py -q` — ALL green, and `git diff --stat` on those nine test files is EMPTY (zero edits, the 101-02 standard).
    2. Full suite with coverage: `.venv/bin/python -m pytest tests --ignore=tests/e2e --cov --cov-fail-under=80 -q` — the ONLY acceptable failure is the pre-existing `test_audit.py::TestAuditTaxonomySyncWithTypeScript::test_action_literal_matches_ts_union` (deferred-items.md; RESEARCH grep-confirmed the MTM reasons are NOT under that guard, so this plan cannot move it). Any OTHER red is a regression this plan must fix before completing. Record the pass/fail/coverage numbers in the SUMMARY.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite.py tests/test_stitch_composite_job.py tests/test_mtm_single_key.py -q && git diff --stat -- tests/test_golden_parity.py tests/test_zavara_acceptance.py tests/test_deribit_acceptance.py tests/test_deribit_ground_truth.py tests/test_allocated_capital.py tests/test_deribit_txn.py tests/test_metrics_parity.py tests/test_metrics_minigolden.py tests/test_composite_headline_parity.py | (! grep .)</automated>
  </verify>
  <done>Options-member composite pinned honest-disabled with the exact unsmoothed_options_book literal + no persisted mark_to_market key + marked (n_days:0) coverage; perp-only composite verified admitting; all nine cash-pin files green with an empty diff; full suite green at ≥80% coverage with the pre-existing audit-taxonomy failure as the SOLE red.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exchange txn log → derive worker | untrusted-completeness external data feeds the MTM reconstruction |
| worker → strategy_analytics jsonb | persisted reasons/by-basis objects are the frontend's server truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-05 | Repudiation (honesty) | anchor-race reason classification | mitigate | distinct machine reason + docstring stating the persistent-breach caveat; copy promises re-computation only, never recovery (plan 102-01 Task 3) |
| T-102-06 | Denial of service | degrade path regression | mitigate | RACE-3 pins that transient ValueError still propagates and RACE-1 pins cash-still-ships; the classification is isinstance-only inside the existing catch — no new retry path |
| T-102-07 | Tampering (data integrity) | composite coverage mask | mitigate | COMPOSE-2 pins marked-never-zero-filled (per_key n_days:0, no fabricated 0.0 rows) |
| T-102-SC | Tampering | pip installs | accept | zero-install phase; no package-manager task exists in this plan |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_mtm_single_key.py tests/test_stitch_composite.py tests/test_stitch_composite_job.py tests/test_csv_analytics_runner.py -q` — green.
- Full suite `--cov-fail-under=80`: sole red = pre-existing audit taxonomy test.
- `git diff --stat` on the nine cash-pin test files: EMPTY.
</verification>

<success_criteria>
- MTM-02 resolved as Option A: gate unchanged, honest-disabled + marked-coverage pinned, perp-only verified; the stale-copy rewrite (frontend half) ships in plan 102-01.
- Deferred anchor-race known-limitation resolved: distinct transient reason, cash-safe, falsifiable regression test (the deferred-items.md exit condition).
- MTM-03 (analytics static half): cash pins byte-identical with zero edits; full suite green ≥80% with the sole pre-existing red.
</success_criteria>

<output>
Create `.planning/phases/102-options-mtm-factsheet-composite-regression/102-02-SUMMARY.md` when done
(Write only for this NEW file; Edit — never Write-truncate — for any existing .planning doc).

The SUMMARY MUST carry forward the SHIP-TIME gate verbatim (NOT an in-phase task, NOT attested here):
**MTM-03(b) LIVE Zavara MTM corroboration — post-deploy operational gate (OQ-3, 101-02 SUMMARY §Ship-time):**
after merge + green-first-try main CI, verify the Railway worker deployed (deployment
commitHash + /health), enqueue `derive_broker_dailies` per single-key Deribit options
strategy, then verify EITHER `metrics_json_by_basis ? 'mark_to_market'` with seven finite
scalars OR an honest `mtm_gated_reason` — never neither — AND cash headline +
csv_daily_returns byte-identical vs pre-deploy (live SC-4), THEN eyeball the Zavara MTM
curve corroborates in the factsheet toggle. Neither 102 plan claims live MTM attestation.
</output>
