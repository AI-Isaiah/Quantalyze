---
phase: 101-options-mtm-toggle
plan: 02
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_csv_analytics_runner.py
autonomous: true
requirements: [MTM-01]
must_haves:
  truths:
    - "A derive-stamped mtm_gated_reason SURVIVES the CSV finalizer's wholesale data_quality_flags rebuild (without this bridge the reason is wiped seconds after it is stamped and the Phase-102 disabled-with-reason UI has nothing to read)"
    - "mtm_gated_reason never promotes computation_status (it is an availability annotation like insufficient_window, not a data-quality warning)"
    - "A composite→single transition does NOT carry a stale composite-era mtm_gated_reason forward (consistent with the finalizer's existing by-basis NULLing)"
    - "The pre-existing Zavara acceptance parity and the full analytics suite (coverage ≥80) are green with zero modifications to the cash math"
  artifacts:
    - path: "analytics-service/services/analytics_runner.py"
      provides: "mtm_gated_reason carried through the broker→CSV flag bridge in run_csv_strategy_analytics"
      contains: "mtm_gated_reason"
    - path: "analytics-service/tests/test_csv_analytics_runner.py"
      provides: "Wiring test: reason survives the finalizer; non-promoting; dropped on composite→single"
  key_links:
    - from: "analytics-service/services/job_worker.py (_prestamp_dq_flags, written by plan 101-01)"
      to: "analytics-service/services/analytics_runner.py (run_csv_strategy_analytics data_quality_flags rebuild)"
      via: "existing_flags read at :2332-2343 → present-only carry into the fresh flags dict"
      pattern: "existing_flags.get(\"mtm_gated_reason\")"
---

<objective>
Complete the single-key honest-gate contract: the CSV finalizer (`run_csv_strategy_analytics`) rebuilds `data_quality_flags` WHOLESALE after every broker derive, which would silently wipe the `mtm_gated_reason` plan 101-01 stamps. Bridge the reason through (present-only, non-promoting), then run the phase-level SC-4 gate (Zavara acceptance + full-suite coverage) and hand off the ship-time re-derive instruction.

Purpose: Without this, the honest gate is stamped and then erased — the factsheet (Phase 102) would show a bare disabled toggle with no reason, violating the LOCKED degrade-with-reason decision.
Output: One small finalizer edit + wiring tests + phase verification + documented ship-time re-derive step (OQ-3 planner decision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-options-mtm-toggle/101-CONTEXT.md
@.planning/phases/101-options-mtm-toggle/101-RESEARCH.md
@.planning/phases/101-options-mtm-toggle/101-01-SUMMARY.md

Read these code ranges FIRST:
- `analytics-service/services/analytics_runner.py:2318-2411` — the finalizer flag rebuild: fresh `data_quality_flags = {"csv_source": True}`, `_read_existing_flags` (:2332-2343), the NAV_TWR/ALLOCATED guard-key bridges (:2348-2361), the non-promoting `insufficient_window` pattern (:2364-2370), and the Finding-5 `_was_composite` by-basis NULLing (:2372-2406)
- `analytics-service/tests/test_csv_analytics_runner.py` — existing finalizer test harness (reuse its supabase capture pattern)
</context>

<interfaces>
From `services/analytics_runner.py:2386-2387` (the gate variable to reuse):
```python
_was_composite = bool(existing_flags.get("composite"))
_clear_stale_by_basis = _was_composite
```

From `services/stitch_composite.py` (created by plan 101-01):
```python
MTM_REASON_SUMMARY_COVERAGE = "mtm_summary_coverage_incomplete"
```

Flow context (why the bridge is needed — verified during planning):
1. `run_derive_broker_dailies_job` prestamps `data_quality_flags` (incl. `mtm_gated_reason` when degraded) and writes `metrics_json_by_basis`, THEN enqueues `compute_analytics_from_csv`.
2. `run_csv_strategy_analytics` builds a FRESH flags dict and upserts it, REPLACING the column — only keys it explicitly bridges survive. `metrics_json_by_basis` survives for never-composite rows (not in the `_mark_complete` payload), but the reason flag does NOT.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bridge mtm_gated_reason through the finalizer flag rebuild</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/tests/test_csv_analytics_runner.py</files>
  <behavior>
    - Test (wiring — fails if the bridge is neutered): seed the mocked existing row's `data_quality_flags` with `{"csv_source": True, "mtm_gated_reason": "mtm_summary_coverage_incomplete"}` (the prestamp shape from a degraded derive); after `run_csv_strategy_analytics`, the upserted `data_quality_flags` STILL contains `mtm_gated_reason == "mtm_summary_coverage_incomplete"`.
    - Test (non-promoting): the same run reports `computation_status == "complete"` and `computation_warned is False` — the reason must NOT trip the `_warned` promotion (mirror the insufficient_window discipline).
    - Test (drop-stale on composite→single): existing flags `{"composite": True, "mtm_gated_reason": "unsmoothed_options_book"}` → the fresh flags do NOT carry the reason (and `metrics_json_by_basis` is NULLed by the existing Finding-5 branch, unchanged) — a stale composite-era reason never survives into a single-key headline.
    - Test (absence is absence): existing flags without the key → fresh flags without the key (no fabricated reason, no `None`-valued key).
  </behavior>
  <action>
    In `run_csv_strategy_analytics`, immediately after the `ALLOCATED_CAPITAL_GUARD_KEYS` bridge loop (:2358-2361) and BEFORE the `csv_status` computation, add the present-only carry:
    - Read `_mtm_reason = existing_flags.get("mtm_gated_reason")`.
    - Carry it into `data_quality_flags` ONLY when truthy AND `not _was_composite` — note `_was_composite` is currently assigned at :2386, AFTER this site: hoist the `_was_composite = bool(existing_flags.get("composite"))` assignment above the bridge (keep `_clear_stale_by_basis` reading it where it is) rather than duplicating the lookup.
    - Do NOT touch `_warned` (non-promoting — comment: availability annotation, mirrors insufficient_window/HARD-04; the composite path likewise never promotes on this key).
    - Comment the why: the broker derive prestamps the reason BEFORE enqueuing this run; this rebuild REPLACES the column wholesale, so an unbridged reason is wiped seconds after being stamped; the composite→single exclusion mirrors the Finding-5 by-basis NULLing (a composite-era reason like `unsmoothed_options_book` is meaningless for the new single-key headline; the next broker derive re-evaluates and re-stamps honestly).
    Write the behavior tests FIRST (RED) in `tests/test_csv_analytics_runner.py` using its existing harness, then implement (GREEN). Use the string literal in test fixtures but import `MTM_REASON_SUMMARY_COVERAGE` from `services.stitch_composite` in at least one assertion so a constant rename cannot silently decouple the two sites.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_csv_analytics_runner.py -x -q</automated>
  </verify>
  <done>Reason survives the finalizer for single-key rows, never promotes status, and is dropped on composite→single transitions; all pre-existing finalizer tests green unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Phase SC-4 gate — Zavara parity, full suite, coverage ratchet</name>
  <files>analytics-service/tests/test_csv_analytics_runner.py</files>
  <action>
    1. Run the untouched Zavara acceptance + pure-core parity families and assert zero modifications were needed: `test_zavara_acceptance.py`, `test_deribit_txn.py`, `test_allocated_capital.py`, `test_deribit_acceptance.py`, `test_deribit_ground_truth.py`. These pin the cash_settlement math end-to-end; Phase 101 must leave every one green WITHOUT edits (RESEARCH Q4: under cash_settlement the entire Phase-82 amendment is dark). If ANY needs an edit to pass, STOP — that is an SC-4 breach, not a test-maintenance chore; fail loud in the SUMMARY.
    2. Run the full analytics suite with the coverage gate: `python -m pytest --cov --cov-fail-under=80 -q`. New logic (job_worker second pass + finalizer bridge) must not drop coverage below the gate; if it does, add targeted tests for the uncovered branch rather than excluding it.
    3. RESEARCH Pitfall 2 guard (documentation-level): confirm no new test asserts MTM ≈ cash — the MTM curve re-dates premium BY DESIGN and must not match the cash track. Grep the new test files for any cash-vs-MTM equality assertion on returns/metrics; parity assertions must be cash-vs-cash only.
    4. Record the SHIP-TIME OPERATIONAL STEP (OQ-3 — planner decision: NOT an in-phase task, because the worker code must be deployed to Railway before a re-derive can write the new key, and deploys ride merge-to-main):
       In the SUMMARY's next-steps section, write verbatim instructions for the post-deploy backfill:
       - After merge + green CI + Railway deploy verification (`railway deployment list` commitHash + `/health` — deploy silently SKIPS on red main), enqueue a re-derive for EACH single-key Deribit options strategy: `SELECT enqueue_compute_job(strategy_id, 'derive_broker_dailies')` (or the wizard's re-sync path).
       - Then verify per strategy: `strategy_analytics.metrics_json_by_basis ? 'mark_to_market'` is true (or `data_quality_flags->>'mtm_gated_reason'` is `mtm_summary_coverage_incomplete` for a genuinely-uncoverable book — BOTH are honest outcomes), AND the headline scalars + `csv_daily_returns` are unchanged vs. pre-deploy values (SC-4 live check).
       - Note assumption A2 explicitly: if the target book has pre-2025-01-12 option activity, the gate degrading-with-reason is the CORRECT outcome, not a bug.
       - Phase 102 (UI wiring + composite compose + Zavara MTM regression) consumes this persisted data; it should not start its live verification before this backfill runs.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest --cov --cov-fail-under=80 -q</automated>
  </verify>
  <done>Full analytics suite green at ≥80% coverage; Zavara/parity families green with zero edits; ship-time re-derive instructions recorded in the SUMMARY for the land flow and Phase 102.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| strategy_analytics.data_quality_flags (DB) → finalizer rebuild | Persisted flags re-read and selectively re-emitted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-04 | Tampering | mtm_gated_reason bridge | mitigate | Present-only carry of the EXISTING value (no synthesis); composite→single exclusion prevents a stale reason masquerading as a fresh single-key verdict |
| T-101-05 | Repudiation | SC-4 live parity | mitigate | Ship-time step includes a pre/post headline + csv_daily_returns comparison per re-derived strategy |
| T-101-SC | Tampering | package installs | n/a | Zero new packages |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest --cov --cov-fail-under=80 -q` — the phase gate.
- `git diff --stat` shows NO changes under `analytics-service/tests/test_zavara_acceptance.py`, `test_deribit_txn.py`, `test_allocated_capital.py` (SC-4: the cash pins pass untouched).
</verification>

<success_criteria>
- MTM-01 core is complete on the analytics side: for a single-key options book the worker persists either `metrics_json_by_basis.mark_to_market` (honest data present) or a surviving `mtm_gated_reason` (honest degrade) — never neither, never a fabricated line.
- `cash_settlement` output byte-identical: every pre-existing cash pin green without edits (SC-4).
- Ship-time re-derive documented so the land flow backfills existing options rows and Phase 102 finds the data present.
</success_criteria>

<output>
Create `.planning/phases/101-options-mtm-toggle/101-02-SUMMARY.md` when done, including the verbatim ship-time re-derive instructions (Task 2 step 4) in a clearly-marked "Ship-time operational step" section.
</output>
