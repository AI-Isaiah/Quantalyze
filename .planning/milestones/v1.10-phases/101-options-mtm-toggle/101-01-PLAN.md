---
phase: 101-options-mtm-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/stitch_composite.py
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_mtm_single_key.py
autonomous: true
requirements: [MTM-01]
must_haves:
  truths:
    - "A single-key Deribit options-book derive persists metrics_json_by_basis.mark_to_market with the seven headline scalars (finite or honest JSON null), instead of the key being absent"
    - "A perp-only Deribit / any ccxt / any key-mode derive is byte-identical to today: no second ledger pass, no metrics_json_by_basis write, no new flag"
    - "An options book whose mark_to_market reconstruction fails structurally (pre-rollout straddle, mid-window summary hole) still completes its cash derive and stamps data_quality_flags.mtm_gated_reason — the derive job never crashes on the MTM pass"
    - "The single-key by-basis object NEVER contains a cash_settlement key (the strict overlay contract in src/lib/factsheet/basis-metrics.ts keeps single-key cash byte-identical only when that key is absent)"
  artifacts:
    - path: "analytics-service/services/stitch_composite.py"
      provides: "MTM_REASON_SUMMARY_COVERAGE reason constant next to MTM_REASON_OPTIONS/MTM_REASON_VENUE (single admissibility-vocabulary owner)"
      contains: "MTM_REASON_SUMMARY_COVERAGE"
    - path: "analytics-service/services/job_worker.py"
      provides: "Second mark_to_market ledger pass + honest gate + additive metrics_json_by_basis persistence in run_derive_broker_dailies_job"
      contains: "PNL_BASIS_MARK_TO_MARKET"
    - path: "analytics-service/tests/test_mtm_single_key.py"
      provides: "Wave-0 wiring tests: finite-MTM persistence, honest-degrade, second-pass-iff-options, SC-4 cash parity"
      min_lines: 120
  key_links:
    - from: "analytics-service/services/job_worker.py (run_derive_broker_dailies_job deribit branch)"
      to: "services.deribit_ingest.build_deribit_native_ledger"
      via: "second call with pnl_basis=PNL_BASIS_MARK_TO_MARKET, gated on _completeness.has_option_activity"
      pattern: "pnl_basis=PNL_BASIS_MARK_TO_MARKET"
    - from: "analytics-service/services/job_worker.py (_prestamp_dq_flags upsert)"
      to: "strategy_analytics.metrics_json_by_basis"
      via: "additive jsonb write {'mark_to_market': ...} or SQL NULL, options-book derives only"
      pattern: "metrics_json_by_basis"
---

<objective>
Close the MTM-01 persistence gap: teach the single-key Deribit derive (`run_derive_broker_dailies_job`) to run a SECOND `pnl_basis=mark_to_market` ledger pass — mirroring the composite dual-pass template — and persist its metrics ADDITIVELY into `strategy_analytics.metrics_json_by_basis.mark_to_market`, with an honest single-key gate that degrades-with-reason (never crashes, never fabricates) when the summary channel cannot cover the book.

Purpose: The MTM math already exists and is tested (`deribit_txn.py` Phase-82 options-aware channel). The factsheet shows seven "—" for single-key options books ONLY because no code ever writes the `mark_to_market` by-basis key. This plan writes it.

Output: Modified `job_worker.py` + one new reason constant + a new neuter-falsifiable test module. NO smoothing, NO new packages, NO migration (Phase-85 CHECK already allows any jsonb object), NO UI (Phase 102).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/101-options-mtm-toggle/101-CONTEXT.md
@.planning/phases/101-options-mtm-toggle/101-RESEARCH.md

Read these code ranges FIRST (they are the entire blast radius):
- `analytics-service/services/job_worker.py:1890-2310` — the single-key derive deribit branch (cash pass, error taxonomy, `account_state` one-read discipline at :2074, `pnl_basis` selection at :2122-2126)
- `analytics-service/services/job_worker.py:2733-2787` — `_prestamp_dq_flags` + CSV-analytics enqueue (the persistence seam)
- `analytics-service/services/job_worker.py:3129-3148` — `_reconstruct_deribit` (the composite per-member dual-pass template to mirror)
- `analytics-service/services/job_worker.py:3867-3951, 4016-4084` — composite MTM gate + conditional by-basis persist + `mtm_gated_reason` drop-stale pattern
- `analytics-service/services/stitch_composite.py:81-96, 258-274` — `MemberBasisSignal`, reason constants, `mark_to_market_available`
- `analytics-service/services/deribit_ingest.py:775-804, 1818` — `CompletenessReport.has_option_activity` (already computed on EVERY crawl, basis-agnostic)
- `analytics-service/services/deribit_txn.py:600-650` — basis constants + pre-rollout-straddle fail-loud semantics
- `analytics-service/tests/test_derive_broker_dailies_dualmode.py:1-120` — the `_build_ctx` capture harness to reuse
- `analytics-service/tests/test_job_worker_deribit.py:130-220` — the `services.deribit_ingest.build_deribit_native_ledger` patch pattern (function-local import → patch the source module)
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase 2026-07-12. Do not re-derive. -->

From `services/deribit_ingest.py` (CompletenessReport, field :784):
```python
has_option_activity: bool = False  # True iff RAW rows carry option evidence:
# an options_settlement_summary-typed row OR an option-instrument (-C/-P) row.
# Basis-agnostic — set on the cash crawl too (:1818). This IS the single-key gate signal.
```

From `services/deribit_txn.py:645-650`:
```python
PNL_BASIS_CASH_SETTLEMENT: str = "cash_settlement"
PNL_BASIS_MARK_TO_MARKET: str = "mark_to_market"
DEFAULT_PNL_BASIS: str = PNL_BASIS_CASH_SETTLEMENT
class LedgerValuationError(ValueError): ...   # :80 — ValueError subclass
```

From `services/job_worker.py:3135-3148` (`_reconstruct_deribit`, the pass template):
```python
ledger, completeness = await build_deribit_native_ledger(
    ctx.exchange, account_state=account_state, pnl_basis=basis,
    exclude_spot_extraction=exclude_spot,
)
assert_ledger_complete(completeness)
returns, member_meta = combine_native_ledger(
    ledger, completeness.indexable_currencies, denominator_config=denominator_config,
)
```

From `services/job_worker.py:4016-4020` (composite conditional-key persist — the shape to mirror, MINUS the cash key):
```python
metrics_json_by_basis: dict[str, Any] = {"cash_settlement": cash_metrics_json}
if mtm_metrics_json is not None:
    metrics_json_by_basis["mark_to_market"] = mtm_metrics_json
```

From `src/lib/factsheet/basis-metrics.ts` (READ-SIDE CONTRACT — do not violate):
- CASH overlay is applied only when `metrics_json_by_basis.cash_settlement` is PRESENT ("only passed when present (composite), so the absent branch keeps single-key byte-identical"). Therefore the single-key object must carry ONLY `mark_to_market` — writing a cash key would activate a recomputed cash overlay and risk SC-4 divergence.
- `hasBasisHeadline(obj)`: availability = all seven server keys present (`cumulative_return, volatility, max_drawdown, cagr, sharpe, sortino, calmar`) AND finite `cumulative_return`. `dict(metrics_result.metrics_json)` (as the composite persists it) satisfies this; degenerate scalars persist as JSON null and render "—" — never hand-build the object.

Supabase Phase-85 CHECK (migration `20260710120000_strategy_keys.sql:140`):
```sql
CHECK (metrics_json_by_basis IS NULL OR jsonb_typeof(metrics_json_by_basis) = 'object')
```
No key restrictions — `{"mark_to_market": {...}}` alone is valid. SQL NULL = Python `None` (never JSON null).
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reason constant + second mark_to_market ledger pass with structural-degrade catch</name>
  <files>analytics-service/services/stitch_composite.py, analytics-service/services/job_worker.py, analytics-service/tests/test_mtm_single_key.py</files>
  <behavior>
    - Test (wiring, neuter-falsifiable): patching `services.deribit_ingest.build_deribit_native_ledger` with a basis-recording mock whose CompletenessReport has `has_option_activity=True`, a strategy-mode derive calls it EXACTLY TWICE — first `pnl_basis="cash_settlement"`, second `pnl_basis="mark_to_market"` — and both calls receive the SAME `account_state` object (identity assert; 80-06 one-anchor-read discipline). Fails if the second pass is removed or re-fetches the anchor.
    - Test: with `has_option_activity=False`, the builder is called EXACTLY ONCE (cash) — perp-only derives are byte-identical, no second crawl.
    - Test: in key-mode (job carries api_key_id), the builder is called exactly once even with `has_option_activity=True` (key-mode has no strategy_analytics row to persist into).
    - Test (honest degrade — the pre-rollout-straddle path, RESEARCH Pitfall 1): mock raises `LedgerValuationError` ONLY on the `mark_to_market` call; the derive still returns `DispatchOutcome.DONE`, still upserts csv_daily_returns cash rows, still enqueues compute_analytics_from_csv, and the `_prestamp_dq_flags` payload carries `mtm_gated_reason == MTM_REASON_SUMMARY_COVERAGE`. Fails if the inner catch is removed (exception would escalate to the outer permanent-FAILED arm and the job result flips to FAILED).
    - Test: a `DeribitTransientReadError` raised on the MTM pass PROPAGATES (job fails retryable) — a transient blip must retry the whole derive, never permanently stamp a gated reason.
  </behavior>
  <action>
    1. In `analytics-service/services/stitch_composite.py`, add ONE constant next to `MTM_REASON_OPTIONS`/`MTM_REASON_VENUE` (:95-96): `MTM_REASON_SUMMARY_COVERAGE = "mtm_summary_coverage_incomplete"`. Extend the module-docstring gate paragraph (§4, :41-49) with one sentence: the single-key derive stamps this reason when an option book's `mark_to_market` reconstruction fails structurally (pre-rollout straddle / mid-window summary hole — deribit_txn.py:636-650). Per OQ-2 (planner decision): Phase 101 stamps the MACHINE reason only; human-facing copy is Phase-102 UI scope. Do NOT touch `mark_to_market_available` (composite gate semantics are Phase 102).
    2. In `run_derive_broker_dailies_job`'s deribit branch (job_worker.py), INSIDE the existing crawl `try:` (the one opened at :2136), immediately after the `pre_coverage_option_days` stamping (:2221-2225), add the second pass. Initialize `mtm_returns: pd.Series | None = None` and `mtm_gated_reason: str | None = None` BEFORE the deribit branch's try (so both exist on every path, including ccxt venues — initialize them right before `try:` at :2008 or at the top of the deribit branch with fallthrough defaults; simplest: initialize at :2008 level so the post-branch code can read them unconditionally). Condition for the second pass (ALL must hold — spell each in a comment):
       - `not is_key_mode` (key-mode has no strategy_analytics row — Phase 36),
       - `pnl_basis == PNL_BASIS_CASH_SETTLEMENT` (if the configured headline basis is ALREADY mark_to_market there is nothing additive to compute; a dual write there is Phase-102 territory),
       - `_completeness.has_option_activity` (the RESEARCH Q1 single-key gate signal; perp-only MTM ≡ cash — the Phase-82 amendment is dark — so skipping avoids doubling every Deribit crawl for zero information).
       The pass mirrors `_reconstruct_deribit` (:3135-3148) verbatim: `build_deribit_native_ledger(ctx.exchange, account_state=account_state, pnl_basis=PNL_BASIS_MARK_TO_MARKET, exclude_spot_extraction=exclude_spot_extraction)` → `assert_ledger_complete` → `combine_native_ledger(..., denominator_config=denominator_config)`. REUSE the branch's existing `account_state` (one summaries read for both passes — stronger than the composite's per-pass re-read: both bases anchor identically). Import `PNL_BASIS_MARK_TO_MARKET` alongside `DEFAULT_PNL_BASIS` at :2030.
       CRITICAL naming: bind results to `mtm_returns`/`_mtm_meta`/`_mtm_completeness` — NEVER reassign `returns`, `meta`, `_completeness`, `native_ledger` (the cash pass objects are what SC-4 protects). Discard `_mtm_meta` guard flags (mirror the composite's Finding-9 discard: cash-pass flags are authoritative).
    3. Wrap ONLY the second pass in an inner `try/except`. Catch the TYPED STRUCTURAL family — `(LedgerValuationError, NavReconstructionError, LedgerCompletenessError, LedgerTruncatedError, CurrencyEnumerationError, ScopeAuthError)` (`LedgerValuationError` is the pre-rollout-straddle and summary-hole fail-loud type; every genuine structural MTM failure raises one of these — verified `deribit_txn.py:1436-1491,1090-1140,961-990`, `nav_twr.py:201`) — set `mtm_gated_reason = MTM_REASON_SUMMARY_COVERAGE`, `mtm_returns = None`, and `logger.warning` with `scrub_freeform_string(str(exc))` (T-74-03: never log raw exchange text; never interpolate exception text into the reason — it is a fixed enum). Import the constant from `services.stitch_composite`. **Do NOT catch bare `ValueError`** — this deliberately mirrors the cash pass's narrowing at `job_worker.py:2249` (rationale `:2256-2259`): a transient network/parse `ValueError`/`json.JSONDecodeError` escaping the second crawl must fall through and stay transient-retryable, NEVER be stamped as a coverage reason. Do NOT catch `DeribitTransientReadError`, `ccxt` network errors, or `RateLimitExceeded` — transient errors must propagate so the whole job retries cleanly (the outer :2414 arm already stamps 429). Comment the deliberate asymmetry: a structural MTM failure degrades (cash factsheet still ships); a transient one retries everything. **Companion test (Task 1 verify):** a plain transient `ValueError`/`JSONDecodeError` raised on the MTM (second) crawl PROPAGATES (job stays retryable, no reason stamped) — sibling to the `DeribitTransientReadError` propagation test; it must FAIL if bare `ValueError` is re-added to the tuple.
    4. Timeout sanity (comment only, no code): `derive_broker_dailies` budget is 15 min (:266); the composite's per-crawl worst case is ~120 s (:2849). One extra crawl on options books only stays comfortably inside the envelope — reference this in the block comment so a future budget tune sees the dependency.
    5. Write the Task-1 behavior tests in a NEW `analytics-service/tests/test_mtm_single_key.py`, reusing the `_build_ctx`/capture harness from `tests/test_derive_broker_dailies_dualmode.py` and the `patch("services.deribit_ingest.build_deribit_native_ledger", ...)` pattern from `tests/test_job_worker_deribit.py` (imports are function-local in the job, so patch the SOURCE module). Also patch `fetch_deribit_native_account_state`, `assert_ledger_complete`, `combine_native_ledger` as those tests do. Run the tests BEFORE implementing (RED), then implement (GREEN).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_mtm_single_key.py tests/test_derive_broker_dailies_dualmode.py tests/test_job_worker_deribit.py -x -q</automated>
  </verify>
  <done>Second pass runs iff (strategy-mode AND cash headline basis AND has_option_activity); structural MTM failure degrades with MTM_REASON_SUMMARY_COVERAGE while the cash derive completes DONE; transient errors propagate; all pre-existing derive tests green unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MTM metrics compute (finalizer-mirrored conventions) + additive prestamp persistence</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_mtm_single_key.py</files>
  <behavior>
    - Test (finite-MTM persistence — Wave-0 gap 1): for an options-book derive whose MTM pass succeeds, the `strategy_analytics` prestamp upsert payload contains `metrics_json_by_basis == {"mark_to_market": &lt;metrics_json dict&gt;}` where the dict contains all seven server keys (`cumulative_return, volatility, max_drawdown, cagr, sharpe, sortino, calmar`) with `cumulative_return` finite — and does NOT contain a `"cash_settlement"` key. Fails if the persist is dropped or someone adds a cash key.
    - Test (honest gate persistence — Wave-0 gap 3): when the MTM pass degraded (Task 1's LedgerValuationError mock), the prestamp payload carries `metrics_json_by_basis is None` (SQL NULL — heals a stale key from a prior successful derive) AND `data_quality_flags["mtm_gated_reason"] == "mtm_summary_coverage_incomplete"`.
    - Test (zero blast radius): for a perp-only book (`has_option_activity=False`) and for a ccxt-venue derive, the prestamp payload contains NO `metrics_json_by_basis` key at all (column untouched) and NO `mtm_gated_reason` flag — byte-identical to the pre-Phase-101 payload shape.
    - Test (benchmark blip never gates): `get_benchmark_returns` raising still persists a finite `mark_to_market` object (the seven guaranteed scalars are benchmark-invariant) — MTM availability must not depend on the BTC benchmark fetch.
  </behavior>
  <action>
    1. After the deribit/ccxt branch closes (after the `finally` at :2417-2421, exchange now closed) and after the `<2-day` guard (:2558), in STRATEGY-mode only, compute `mtm_metrics_json` when `mtm_returns is not None`:
       - Conventions MUST mirror `run_csv_strategy_analytics` (analytics_runner.py:2291-2316) so the MTM object is convention-comparable with the cash headline: `periods_per_year_for_asset_class(ctx.strategy_row.get("asset_class"))` (mirror the import at job_worker:2960), `cumulative_method`/`day_basis` from the ALREADY-PARSED `denominator_config` (same fallbacks: geometric + calendar when None; `metrics_day_basis(...)` when present).
       - Benchmark: guarded `get_benchmark_returns("BTC")` exactly like the composite (:3808-3817) — on failure log a warning and compute with `benchmark_rets=None`. NEVER degrade MTM for a benchmark blip.
       - Compute: `compute_all_metrics(mtm_returns, benchmark_rets, periods_per_year=..., cumulative_method=..., day_basis=...)` and take `dict(result.metrics_json)` — the SAME already-JSON-safe object the composite persists (degenerate scalars are JSON null via `_safe_float`; postgrest rejects NaN, so never hand-build). Wrap this compute in a `try/except ValueError` → degrade: `mtm_metrics_json = None`, `mtm_gated_reason = MTM_REASON_SUMMARY_COVERAGE` (mirror the composite F-5 guard, but degrade instead of failing the job — the cash headline is unaffected).
    2. Extend the `_prestamp_dq_flags` seam (:2756-2777) — this is the persistence site (planner decision, RESEARCH Pitfall 4): the prestamp runs BEFORE the CSV finalizer, and the finalizer's `_mark_complete` upsert omits `metrics_json_by_basis` for never-composite rows (Finding 5 gates on the prior `composite` flag), so a prestamped key SURVIVES the finalizer. Changes:
       - When the second pass was ATTEMPTED (options book, strategy-mode, cash headline basis): add `"metrics_json_by_basis": {"mark_to_market": mtm_metrics_json}` on success, or `"metrics_json_by_basis": None` (Python None → SQL NULL, never JSON null — Phase-85 CHECK) on degrade. The NULL write heals a stale `mark_to_market` key left by a prior successful derive whose data later gated (mirror the composite Finding-5 drop-stale discipline).
       - When the pass was NOT attempted (perp-only, ccxt, key-mode, MTM-configured headline): do NOT include the key in the payload — the column is untouched and every non-options derive stays byte-identical (SC-4 by construction).
       - When degraded: `_prestamp_flags["mtm_gated_reason"] = mtm_gated_reason`. When succeeded or not-attempted: no reason key — the prestamp already REPLACES data_quality_flags wholesale (MED-3), so a stale reason self-heals on the next clean derive.
    3. Rename/extend the `_prestamp_dq_flags` docstring-comment to note it now also owns the single-key by-basis write, and cross-reference the composite persist (:4016-4020) as the sibling it mirrors minus the cash key (headline IS the cash truth for single-key; see the basis-metrics.ts strict-overlay contract quoted in this plan's interfaces).
    4. Extend `tests/test_mtm_single_key.py` with the Task-2 behavior tests (RED first). The success-path mock's `combine_native_ledger` must return a real ≥2-day pd.Series for BOTH bases so `compute_all_metrics` runs for real (do not mock the metrics compute — the finite-seven-scalars assertion must exercise the genuine compute; patch `services.benchmark.get_benchmark_returns` where needed).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_mtm_single_key.py -x -q</automated>
  </verify>
  <done>Options-book success derives persist {"mark_to_market": &lt;seven-scalar object&gt;} with no cash key; degraded derives persist SQL NULL + mtm_gated_reason; non-options derives leave the column and flags byte-identical; benchmark failure never gates.</done>
</task>

<task type="auto">
  <name>Task 3: SC-4 derive-level cash parity (falsifiable) + regression sweep</name>
  <files>analytics-service/tests/test_mtm_single_key.py</files>
  <action>
    Add the SC-4 parity test (Wave-0 gap 2, derive level — the pure-core parity lives in the untouched `test_zavara_acceptance.py`, re-verified in plan 101-02):
    1. Run the mocked options-book derive TWICE against the same fixtures: run A with `has_option_activity=True` (MTM pass runs and succeeds), run B with `has_option_activity=False` (MTM pass skipped — the pre-Phase-101 shape). Assert byte-equality of everything the CASH track persists: the `csv_daily_returns` upsert payload lists, the reconcile-delete span args, and the prestamp `data_quality_flags` dict (after popping the MTM-only keys from run A: `mtm_gated_reason` must be ABSENT in both here). This test proves the MTM pass cannot perturb the cash outputs — it fails if the second pass mutates shared state (e.g., someone "optimizes" by reusing/mutating the cash `native_ledger`, `returns`, or `meta`).
    2. Repeat parity with run A' where the MTM pass RAISES (degrade path): cash persists must again be byte-identical to run B (minus the additional `mtm_gated_reason` flag key and the by-basis NULL, which are asserted separately in Task 2).
    3. Verify each new test is wiring-tested, not helper-tested (per project invariant "test the wiring, not just the helper"): temporarily neuter each guarded branch locally (e.g., comment the inner except) and confirm the matching test FAILS, then restore. Record the neuter-check in the test module docstring (which test kills which mutation).
    4. Regression sweep of the touched surface: run the full deribit/derive/worker test families.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_mtm_single_key.py tests/test_job_worker.py tests/test_job_worker_deribit.py tests/test_derive_broker_dailies_dualmode.py tests/test_stitch_composite.py tests/test_stitch_composite_job.py tests/test_deribit_txn.py -q</automated>
  </verify>
  <done>Cash persists byte-identical across MTM-on/MTM-off/MTM-degraded runs; every guarded branch has a test that fails when the branch is neutered; the full touched-surface suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Deribit txn-log rows → worker ledger core | Untrusted exchange input crosses into the MTM re-attribution (second pass re-reads the same crawl surface) |
| Worker → strategy_analytics jsonb | Derived metrics persisted for factsheet consumption |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | DoS | run_derive_broker_dailies_job MTM pass | mitigate | Inner structural catch → degrade-with-reason; the whole derive can never be killed (or retried-forever) by a structurally-unreconstructable MTM book; transient errors propagate to the existing retry classifier |
| T-101-02 | Information disclosure | mtm_gated_reason / warning logs | mitigate | Reason is a FIXED enum constant (no exception-text interpolation); log lines route through scrub_freeform_string (T-74-03 account-size-leak discipline) |
| T-101-03 | Tampering | options_settlement_summary rows | accept (existing control) | deribit_txn.py already fails loud on nonzero summary `change`, absent realized_pl/unrealized_pl, sign drift — no new validation surface added |
| T-101-SC | Tampering | package installs | n/a | Zero new packages (RESEARCH: no Package Legitimacy Audit needed) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/ -q -x --ignore=tests/e2e 2>/dev/null || cd analytics-service && python -m pytest -q` — full analytics suite green.
- `grep -n "cash_settlement" analytics-service/services/job_worker.py | grep -n "metrics_json_by_basis"` — confirm no site builds a single-key by-basis object containing a cash key.
</verification>

<success_criteria>
- Options-book single-key derive persists `metrics_json_by_basis.mark_to_market` (seven scalars, `cumulative_return` finite) — the RESEARCH data-flow diagram's "⚠️ MISSING" pass now exists.
- Pre-rollout straddle / summary-hole books degrade with `mtm_gated_reason=mtm_summary_coverage_incomplete` and a completed cash derive (Pitfall 1 closed).
- Non-options derives byte-identical (SC-4 at the derive level, proven falsifiably).
- No smoothing, no `mark_price` reads, no new packages, no migration (LOCKED decisions honored).
</success_criteria>

<output>
Create `.planning/phases/101-options-mtm-toggle/101-01-SUMMARY.md` when done. Record: the exact insertion line ranges in job_worker.py, the final catch tuple, and any observed-but-out-of-scope findings (e.g., the pre-existing composite→single stale-by-basis window where the broker prestamp wipes the `composite` flag before the finalizer's Finding-5 check reads it — observed during planning, NOT fixed here).
</output>
