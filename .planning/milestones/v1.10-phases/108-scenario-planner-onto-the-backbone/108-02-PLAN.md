---
phase: 108-scenario-planner-onto-the-backbone
plan: 02
type: execute
wave: 2
depends_on: [108-01]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/lib/scenario-blend-panels.ts
  - src/lib/scenario-blend-panels.test.ts
  - src/lib/scenario-backbone-gates.test.ts
  - src/lib/diversification.ts
  - src/__tests__/phase-30-frozen-spine-guards.test.ts
autonomous: true
requirements: [SCEN-BB]

must_haves:
  truths:
    - "ScenarioComposer's blend panels derive via the backbone adapter; scenario-blend-panels.ts and its buildBlendPanels export no longer exist, and no NON-COMMENT reference to them survives anywhere in src/ (SC-1 + SC-2)"
    - "The 3M/6M/12M window toggle, the per-option disable, and both degenerate empty-states render identically — WR-02 and the whole ScenarioComposer suite stay green through the rewire (SC-4-UI)"
    - "portfolio-stats.ts, health-score.ts, scenario.ts, and metrics-parity.test.ts are byte-untouched by this phase; metrics-parity.test.ts is green (SC-3)"
    - "A permanent source-scan delete-gate makes any reintroduction of the second-Sharpe module fail CI (SC-2, 107 discipline)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Rewired consumer"
      contains: "@/lib/scenario-blend-adapter"
    - path: "src/lib/scenario-backbone-gates.test.ts"
      provides: "SC-2 delete-gate + SC-3 keep-siblings gate (permanent tripwire)"
      min_lines: 60
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/lib/scenario-blend-adapter.ts"
      via: "deriveBlendPanels import + the :2818-2823 useMemo call"
      pattern: "deriveBlendPanels"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      to: "ScenarioComposer.tsx source on disk"
      via: "re-anchored positive control in the static honesty guard"
      pattern: "deriveBlendPanels"
---

<objective>
Rewire the single consumer (`ScenarioComposer.tsx`) onto the Plan-01 backbone adapter, DELETE
`src/lib/scenario-blend-panels.ts` (211 LOC — the one real "second Sharpe" TS compute) plus its
251-LOC test, repoint the two now-DANGLING doc-comments that name the deleted file, re-anchor the
composer's static-guard positive control, land the permanent SC-2 delete-gate, and run the phase
verification sweep.

Purpose: SC-1/SC-2/SC-3 of Phase 108 — the scenario surface flows from the backbone, the bespoke
compute is gone with a tripwire against reintroduction, and the out-of-scope keep-list is proven
untouched. This is the Phase-107 delete-then-gate discipline applied to the blend panels.

Output: rewired composer, two files deleted, two doc-comments repointed,
`src/lib/scenario-backbone-gates.test.ts`, full suite + coverage + tsc + lint green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-scenario-planner-onto-the-backbone/108-CONTEXT.md
@.planning/phases/108-scenario-planner-onto-the-backbone/108-PATTERNS.md
@.planning/phases/108-scenario-planner-onto-the-backbone/108-UI-SPEC.md
@src/lib/scenario-blend-adapter.ts
@src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts

<interfaces>
<!-- From Plan 01 (108-01) — the contract this plan rewires against. -->

```typescript
// src/lib/scenario-blend-adapter.ts
export interface BlendPanelSeries { histogramSeries; quantiles; rollingSharpe; rollingVol; rollingSortino; usableN }
export function deriveBlendPanels(
  portfolioDaily: { date: string; value: number }[],
  window: number,
  periodsPerYear: number,
): BlendPanelSeries;
```

Identical arg order and return shape to the legacy `buildBlendPanels(portfolioDaily,
rollingWindow, blendBasis)` — the composer's memo body changes only the callee identifier.
</interfaces>

<surviving_references>
<!-- Pre-verified by grep at plan time. After deleting scenario-blend-panels.ts, these
     references to the token remain and MUST be resolved so no dangling ref survives. -->

Comment/doc references (NON-CODE — a comment-stripped scan ignores them, but two are now dangling and get repointed for correctness):
- `src/lib/diversification.ts:30` — JSDoc `* Pattern precedent: src/lib/scenario-blend-panels.ts (...)` — DANGLING after delete → repoint (Task 1 step 4).
- `src/__tests__/phase-30-frozen-spine-guards.test.ts:6` — docstring `(src/lib/scenario-blend-panels.ts)`; the test's FUNCTIONAL assertion targets scenario.ts's freeze (NOT the deleted file), so deletion does not break the test — comment only → reword (Task 1 step 4).
- `src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts:212` — the load-bearing PAYLOAD-03 note ("scenario-blend-panels.ts uses SAMPLE stdev (÷ n−1)"). LEAVE VERBATIM — it is a historical convention pin, not a live import, and this file is out of scope (do NOT touch). The comment-stripped delete-check ignores it.

Live-code references (all inside Task 1's files, resolved by the rewire): `ScenarioComposer.tsx:102/:2809/:2821`, `ScenarioComposer.test.tsx:3498/:3507/:3553`.
</surviving_references>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire ScenarioComposer onto the adapter, DELETE the legacy module + test, repoint 2 dangling doc-comments, re-anchor the positive control</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/lib/scenario-blend-panels.ts, src/lib/scenario-blend-panels.test.ts, src/lib/diversification.ts, src/__tests__/phase-30-frozen-spine-guards.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:100-110 (import block), :2800-2830 (portfolioDaily memo + the buildBlendPanels call at :2818-2823 + the :2809 comment), :4276-4382 (the three usableN UI keys at :4282/:4338/:4342 — DO NOT TOUCH the render tree)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx:3485-3561 (WR-02 at :3494-3510 incl. the comment mentions at :3498/:3507 + the static honesty guard :3545-3561 with the positive control at :3553 and the forbidden regex at :3554-3560)
    - src/lib/diversification.ts:25-35 (the dangling "Pattern precedent" JSDoc at :30)
    - src/__tests__/phase-30-frozen-spine-guards.test.ts:1-14 (the docstring naming the deleted file at :6; note the actual assertions target scenario.ts's zero-diff freeze)
    - .planning/phases/108-scenario-planner-onto-the-backbone/108-UI-SPEC.md §Component/Interaction Inventory + §States Matrix (the parity contract — nothing visual may change)
  </read_first>
  <action>
    Surgical edits + two deletions, one commit. NOTE (premise correction): the composer rewire does
    NOT remove the last on-disk `scenario-blend-panels`/`buildBlendPanels` token — three COMMENT
    references survive in files outside the composer (see &lt;surviving_references&gt;). The
    permanent Task-2 gate is comment-stripped and unaffected; this task's own delete-check must use
    the SAME comment-stripped discipline (below), and two of those comments get repointed for
    correctness.

    1. `ScenarioComposer.tsx:102` — swap the import to
       `import { deriveBlendPanels } from "@/lib/scenario-blend-adapter";` (removing the
       `@/lib/scenario-blend-panels` import). At :2821 change the memo callee to
       `deriveBlendPanels(portfolioDaily, rollingWindow, blendBasis)` — same args, same deps
       array, same returned shape; the variable stays `blendPanels` so the render tree
       (:4276-4382) is byte-untouched. Update the :2809 comment's `buildBlendPanels` mention to
       `deriveBlendPanels`. NO other edit in this 4000+-line file — the three usableN keys,
       WINDOW_LABEL, the frozen "252-day annualized" caption, and all copy stay verbatim per
       UI-SPEC.
    2. `ScenarioComposer.test.tsx:3553` — re-anchor the positive control from
       `/buildBlendPanels/` to `/deriveBlendPanels/` (the guard's on-disk-read liveness proof
       must track a token that actually exists in the rewired source; the old token is dead).
       Verify the forbidden regex at :3554-3560 is NOT tripped by the new
       `@/lib/scenario-blend-adapter` import (it forbids FactsheetBody|MetricsColumn|
       buildAllocatorPortfolioFactsheetPayload|ingestSource:"api"|PercentileRankBadge|*Panel —
       none match). Update the WR-02 comment mentions of `buildBlendPanels` (:3498, :3507) to
       the new name; the WR-02 assertions themselves are untouched — they must pass as-is
       because the adapter reproduces the gate verbatim.
    3. DELETE `src/lib/scenario-blend-panels.ts` and `src/lib/scenario-blend-panels.test.ts`
       (git rm).
    4. REPOINT the two now-dangling doc-comments (root-cause fix, no behavior change):
       - `src/lib/diversification.ts:30` — change the JSDoc "Pattern precedent:
         src/lib/scenario-blend-panels.ts (...)" to reference the new
         `src/lib/scenario-blend-adapter.ts` (the same-shape pure-TS engine-output adapter that
         replaced it), OR drop the "Pattern precedent" line entirely if a repoint reads awkwardly.
         Do NOT touch `diversification.ts`'s code — comment only.
       - `src/__tests__/phase-30-frozen-spine-guards.test.ts:6` — reword the docstring so it no
         longer names the deleted file (e.g. "…assembling EXISTING leaf charts over a pure-TS
         blend-panel adapter."). The test's FUNCTIONAL assertions target `scenario.ts`'s zero-diff
         freeze and are unaffected — comment/docstring only; do NOT change any assertion.
       - `scenario-factsheet-payload.test.ts:212` — LEAVE VERBATIM (out of scope; the PAYLOAD-03
         SAMPLE-stdev note is a historical convention pin, not a live import; the comment-stripped
         delete-check ignores it).
    5. Confirm no NON-COMMENT reference survives, using the comment-stripped discipline (matching
       the Task-2 permanent gate — a bare unfiltered `grep -rn` would false-positive on the three
       comments, so it is the WRONG check here):
       `grep -rn "scenario-blend-panels\|buildBlendPanels" src/ --include="*.ts" --include="*.tsx" | grep -v "scenario-blend-adapter" | grep -vE "^[^:]+:[0-9]+:\s*(//|\*|/\*)"`
       must return ZERO hits (i.e. only the three surviving COMMENT lines may remain, and only
       diversification.ts + phase-30 get repointed; the PAYLOAD-03 comment is the one allowed
       remaining reference). The authoritative gate is Task 2's comment-stripped scanner.

    Do NOT touch: `scenario.ts` (byte-frozen), `portfolio-stats.ts`, `health-score.ts`,
    `metrics-parity.test.ts`, `scenario-factsheet-payload.ts`/`.test.ts`, any chart leaf
    (`ReturnHistogram`/`ReturnQuantiles`/`RollingMetrics`).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" src/lib/scenario-blend-adapter.test.ts src/__tests__/phase-30-frozen-spine-guards.test.ts --no-file-parallelism && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The comment-stripped delete-check (action step 5) returns 0 NON-COMMENT hits; both legacy files absent from disk
    - No DANGLING file reference remains: `diversification.ts:30` and `phase-30-frozen-spine-guards.test.ts:6` no longer name `scenario-blend-panels.ts` (repointed/reworded); `scenario-factsheet-payload.test.ts:212` left verbatim
    - ScenarioComposer.test.tsx FULLY green with only the positive-control re-anchor + the two WR-02 comment updates — WR-02 (:3494, usableN degenerate collapse) and the static honesty guard (:3545-3561) both pass; phase-30-frozen-spine-guards.test.ts green (docstring-only change)
    - `git diff` on ScenarioComposer.tsx shows exactly: 1 import line, 1 callee identifier in the memo, 1 comment word — nothing in the :4276-4382 render region (SC-4 pixel-parity contract); `git diff` on diversification.ts + phase-30 guard shows COMMENT-only changes
    - tsc clean (proves no other file imported the deleted module)
  </acceptance_criteria>
  <done>Composer derives blend panels from the backbone adapter; legacy module + test deleted; two dangling doc-comments repointed; guard re-anchored; touched suites green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC-2/SC-3 permanent delete-gate (scenario-backbone-gates.test.ts)</name>
  <files>src/lib/scenario-backbone-gates.test.ts</files>
  <read_first>
    - src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts (WHOLE file — 107's exact template: `// @vitest-environment node`, recursive walkSource(SRC_ROOT), stripComments, SELF_REL self-exclusion, concatenated forbidden tokens at :56-60, and the hard-coded retired-line liveness fixture)
    - .planning/phases/108-scenario-planner-onto-the-backbone/108-PATTERNS.md §SC-2 delete-gate (adaptation notes)
  </read_first>
  <behavior>
    - Test: no file under src/ (COMMENT-STRIPPED, excluding this gate file itself) contains the token "scenario-blend-" + "panels" or "buildBlend" + "Panels" (tokens built by string concatenation so the gate never self-matches). Comment-stripping is REQUIRED — the three surviving doc-comment references (diversification.ts repointed, phase-30 reworded, scenario-factsheet-payload.test.ts:212 verbatim) must NOT trip the gate; only a live code token may.
    - Test: the files `src/lib/scenario-blend-panels.ts` and `src/lib/scenario-blend-panels.test.ts` do NOT exist on disk (paths built by concat)
    - Test (SC-3 keep-gate): `src/lib/portfolio-stats.ts`, `src/lib/health-score.ts`, `src/lib/scenario.ts`, and `src/__tests__/metrics-parity.test.ts` DO exist on disk (the out-of-scope keep-list cannot be silently deleted later)
    - Test (liveness): the scanner MATCHES a hard-coded retired-line fixture string (e.g. the old import statement, built by concat) — proving a regex typo can't pass everything vacuously (107's fixture discipline)
  </behavior>
  <action>
    Create `src/lib/scenario-backbone-gates.test.ts` by copying the structure of
    `leverage-backbone-gates.test.ts` wholesale: node environment pragma, the recursive src/
    walker, the comment-stripper (so the surviving doc-comments naming the old module in
    comments/docstrings can't trip the gate — grep-gate hygiene; this is load-bearing here because
    the PAYLOAD-03 comment intentionally remains), the self-exclusion, and a liveness sub-test.
    Adapt only the token list and the presence/absence assertions per the behavior block. The file
    name deliberately avoids the forbidden `scenario-blend-panels` token so no self-exclusion
    gymnastics are needed beyond excluding the gate file itself (its fixture string contains the
    tokens).

    RED→GREEN discipline is inverted for a gate: first prove liveness (the fixture sub-test must
    pass AND a temporary planted CODE token in a scratch check must be caught — neuter-confirm once,
    like 107 did), then land the gate green against the post-Task-1 tree.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-backbone-gates.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Gate green on the current tree; liveness sub-test proves the matcher fires on the retired-line fixture
    - Comment-stripping proven to ignore the surviving doc-comments: the gate is green WITH the PAYLOAD-03 comment present at scenario-factsheet-payload.test.ts:212
    - Neuter-confirmation performed once: temporarily re-adding a live `buildBlendPanels` code reference in any src/ file turns the gate RED (then reverted; note it in the commit message or summary)
    - Keep-list presence assertions cover all four: portfolio-stats.ts, health-score.ts, scenario.ts, metrics-parity.test.ts
    - Gate file uses comment-stripping + concatenated tokens (never a bare unfiltered grep count)
  </acceptance_criteria>
  <done>Permanent tripwire live: reintroducing the second-Sharpe module (live code) or deleting a keep-list sibling fails the suite forever; surviving doc-comments do not false-trip it.</done>
</task>

<task type="auto">
  <name>Task 3: Phase verification sweep — full suite, coverage ratchet, frozen-surface proof</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/108-scenario-planner-onto-the-backbone/108-VALIDATION.md §Sampling Rate + §Validation Sign-Off (the phase-gate contract)
  </read_first>
  <action>
    Run the phase gate in order, failing loud on the first red:
    1. `npm test` — full vitest suite (baseline 8151 green post-107; the net test-count delta is
       expected: −251-LOC legacy test, + adapter suite + gate).
    2. `npm run test:coverage` — must exit 0 with thresholds held (lines 82 / stmts 80 / funcs 74
       / branches 72; baseline actuals ~86.7 have headroom for the 211-LOC covered-module
       deletion, but VERIFY, don't assume).
    3. `npm run typecheck` and `npm run lint` (lint catches react-hooks errors tsc misses —
       the composer memo edit is a hooks surface).
    4. Frozen-surface proof (SC-3 + OUT-OF-SCOPE contract):
       `git log --oneline origin/main..HEAD --grep="108" -- src/lib/scenario.ts src/lib/portfolio-stats.ts src/lib/health-score.ts src/__tests__/metrics-parity.test.ts src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts`
       must print NOTHING (no 108 commit touched the keep-list or the out-of-scope PAYLOAD-03
       file), and `npx vitest run src/__tests__/metrics-parity.test.ts --no-file-parallelism` green.
    If metrics-parity is red at any point: STOP and investigate — it means backbone values moved,
    which this phase must not cause (the backbone itself is untouched; only a caller moved onto
    it). Do NOT re-pin it.
  </action>
  <verify>
    <automated>npm test && npm run test:coverage && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Full suite 0 failures; coverage exit 0 with thresholds held
    - tsc + lint clean
    - The frozen-surface git-log check prints nothing; metrics-parity.test.ts green and byte-untouched; scenario-factsheet-payload.test.ts:212 PAYLOAD-03 comment byte-untouched
    - All five VALIDATION.md rows (108-SC1/SC2/SC3/SC4/SC4-UI) map to a green automated check: SC1 = ScenarioComposer guard + adapter imports; SC2 = scenario-backbone-gates.test.ts; SC3 = metrics-parity green + keep-gate; SC4 = adapter parity pins (Plan 01); SC4-UI = WR-02 + toggle length pins
  </acceptance_criteria>
  <done>Phase gate green end-to-end; ready for /gsd:verify-work → code review → Fable red team.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Consumer rewire + file deletion inside the client bundle. No persistence, network, auth, or new input surface. The only user input on this surface (the pre-existing 3M/6M/12M SegmentedControl) maps to a closed set {63,126,252}. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering (data integrity) | blend charts — non-finite values rendering | mitigate | Adapter's verbatim usableN collapse (Plan 01) + WR-02 stays green through the rewire (ASVS V5) |
| T-108-03 | Tampering (code integrity) | reintroduction of the retired second-Sharpe path | mitigate | Permanent source-scan delete-gate (Task 2), liveness-proven, comment-stripped |
| T-108-04 | S/R/I/D/E | remaining surface | accept | Display-only derived output; no auth/session/crypto/persistence. No package installs this phase → no supply-chain row. |
</threat_model>

<verification>
- `npm test` full suite green; `npm run test:coverage` exit 0; tsc + lint clean
- Comment-stripped delete-check (Task 1 step 5) → 0 NON-COMMENT hits; scenario-backbone-gates.test.ts green (authoritative SC-2 gate)
- Frozen-surface git-log check empty; metrics-parity.test.ts green
</verification>

<success_criteria>
- SC-1: blend panels derive via the backbone adapter (composer guard re-anchored, imports proven)
- SC-2: scenario-blend-panels.ts + test DELETED with a permanent, liveness-proven, comment-stripped gate; no live-code reference survives; portfolio-stats.ts / health-score.ts remain
- SC-3: metrics-parity.test.ts kept, untouched, green
- SC-4 / SC-4-UI: parity pins green (Plan 01); WR-02 + toggle + empty-states render identically; full suite + coverage + tsc + lint green
</success_criteria>

<output>
Create `.planning/phases/108-scenario-planner-onto-the-backbone/108-02-SUMMARY.md` when done
</output>
