---
phase: 108-scenario-planner-onto-the-backbone
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/scenario-blend-adapter.ts
  - src/lib/scenario-blend-adapter.test.ts
autonomous: true
requirements: [SCEN-BB]

must_haves:
  truths:
    - "Blend rolling vol/Sharpe/Sortino values come from the backbone POPULATION-std primitives (factsheet/rolling.ts), computed at the caller-selected 63/126/252 window — no sample-std (÷ n−1) math anywhere in the adapter"
    - "Quantile whiskers remain positional min/max — {All:[min,p25,p50,p75,max]} — NOT p05/p95"
    - "Degenerate inputs (any non-finite value, length < 10, length < window) collapse every series to []/{} and report usableN exactly as the legacy module did (0 when non-finite present)"
    - "Rolling output is {date,value}[] dated at each window's last day, with the leading null warmup dropped (first point at portfolioDaily[window-1].date)"
  artifacts:
    - path: "src/lib/scenario-blend-adapter.ts"
      provides: "Backbone-routed blend-panel derivation (SC-1)"
      exports: ["deriveBlendPanels", "BlendPanelSeries"]
      contains: "from \"@/lib/factsheet/rolling\""
    - path: "src/lib/scenario-blend-adapter.test.ts"
      provides: "SC-4 parity pins (population-std at 63/126/252, min/max whiskers) + usableN gate pins"
      min_lines: 80
  key_links:
    - from: "src/lib/scenario-blend-adapter.ts"
      to: "src/lib/factsheet/rolling.ts"
      via: "rollingVol/rollingSharpe/rollingSortino imports with explicit (window, periodsPerYear) args"
      pattern: "rolling(Vol|Sharpe|Sortino)\\("
    - from: "src/lib/scenario-blend-adapter.ts"
      to: "src/lib/factsheet/quantiles.ts"
      via: "quantileSummary reshaped to positional min/max"
      pattern: "quantileSummary"
---

<objective>
Create the backbone blend-panel adapter: a thin, pure module that reproduces the exact public
shape of the soon-to-be-deleted `scenario-blend-panels.ts::buildBlendPanels`, but derives every
rolling series from the canonical backbone primitives in `factsheet/rolling.ts` (POPULATION std)
and the quantiles from `factsheet/quantiles.ts::quantileSummary` — plus the SC-4 parity-pin test
suite that makes a sample-std bleed fail loudly at each window.

Purpose: SC-1 of Phase 108 — the scenario-planner blend panels flow from the ONE backbone, not a
second Sharpe/annualization stack. This plan builds the replacement; Plan 02 rewires the consumer
and deletes the legacy module. USER DECISIONS honored: population-std canonical (108-CONTEXT
§Parity std), backbone PRIMITIVES + explicit window preserving the 3M/6M/12M toggle (§Adapter
route), min/max whiskers kept (§Quantile whiskers), usableN gate re-homed in this thin adapter
(§usableN degenerate gate — Claude's Discretion, resolved: co-located in the adapter).

Output: `src/lib/scenario-blend-adapter.ts` + `src/lib/scenario-blend-adapter.test.ts`, both green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/108-scenario-planner-onto-the-backbone/108-CONTEXT.md
@.planning/phases/108-scenario-planner-onto-the-backbone/108-PATTERNS.md
@.planning/phases/108-scenario-planner-onto-the-backbone/108-UI-SPEC.md
@src/lib/scenario-blend-panels.ts
@src/lib/factsheet/rolling.ts
@src/lib/factsheet/quantiles.ts

<interfaces>
<!-- Extracted contracts — executor uses these directly, no exploration needed. -->

Shape to reproduce EXACTLY (from the legacy `scenario-blend-panels.ts:43-56` — the composer is
props-only and its render tree must not change):

```typescript
export interface BlendPanelSeries {
  histogramSeries: { date: string; value: number }[];   // cumprod(1+r) WEALTH series
  quantiles: Record<string, number[]>;                   // { All: [min, p25, p50, p75, max] }
  rollingSharpe: Record<string, { date: string; value: number }[]>; // { sharpe_365d: series } — KEY FROZEN
  rollingVol: { date: string; value: number }[];
  rollingSortino: { date: string; value: number }[];
  usableN: number;
}
```

Backbone primitive signatures (`factsheet/rolling.ts:85,99,115` — all three identical shape):

```typescript
export function rollingVol(rets: number[], window = ROLL_WINDOW_6MO, periodsPerYear = 252): Array<number | null>;
export function rollingSharpe(rets: number[], window?, periodsPerYear?): Array<number | null>;
export function rollingSortino(rets: number[], window?, periodsPerYear?): Array<number | null>;
// Output is parallel to rets: a LEADING-WARMUP null prefix (null for i < window-1) then a finite
// value at EVERY index i ≥ window-1 (degenerate slice → 0, never null). No interior nulls.
// pstdev (÷ n, POPULATION) at rolling.ts:145-150; verified rollingVol/Sharpe/Sortino :85-137.
```

Quantile source (`factsheet/quantiles.ts:3-27`):

```typescript
export function quantileSummary(rets: number[]): { p05; p25; p50; p75; p95; min; max; mean };
```

Input type: `portfolioDaily: { date: string; value: number }[]` — `computeScenario`'s
`portfolio_daily_returns` (`scenario.ts:440-443`), full-resolution unrounded daily RETURNS.
Mirror the sibling's `import type { DailyPoint } from "@/lib/portfolio-math-utils"` if that type
fits; otherwise declare the input inline. Do NOT import `mean`/`stdDev` from
`portfolio-math-utils` — that is the sample-std path this phase retires.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backbone blend-panel adapter (deriveBlendPanels) with the usableN gate re-homed</name>
  <files>src/lib/scenario-blend-adapter.ts, src/lib/scenario-blend-adapter.test.ts</files>
  <read_first>
    - src/lib/scenario-blend-panels.ts (WHOLE file, 211 LOC — the module being replaced: shape :43-56, usableN gate :154-176, histogram :182-186, quantiles :190-198, sharpe_365d key :204-206)
    - src/lib/factsheet/rolling.ts:85-150 (rollingVol/rollingSharpe/rollingSortino + pstdev — the canonical primitives; note Array<number|null> output = leading-warmup null prefix only, finite from index window-1 onward)
    - src/lib/factsheet/quantiles.ts:3-27 (quantileSummary shape)
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts:59-74 (the LIVE sibling adapter — import discipline to mirror)
    - .planning/phases/108-scenario-planner-onto-the-backbone/108-PATTERNS.md §Pattern Assignments (adapter section — verbatim gate excerpt + contract notes)
  </read_first>
  <behavior>
    - Test: normal path (300 finite points, window 126, basis 252) → all five series populated; rollingVol/rollingSortino are {date,value}[] of length n−window+1; first point dated portfolioDaily[125].date (leading warmup dropped); rollingSharpe keyed EXACTLY "sharpe_365d"
    - Test: quantiles = { All: [min, p25, p50, p75, max] } — 5 positional numbers, tails are absolute min/max of the return values
    - Test: histogramSeries is the cumulative-wealth cumprod(1+r) series, same length/dates as input
    - Test: non-finite value anywhere → usableN === 0 AND every series []/{} (WR-02 contract)
    - Test: length 9 (< MIN_USABLE 10) → collapse, usableN === 9
    - Test: length 50 with window 126 (≥10 but < window) → collapse, usableN === 50
    - Test: leading-warmup drop is exact — for window w, the emitted rolling series length is n−w+1 and its first date is portfolioDaily[w-1].date (no null-valued points in any emitted series). NOTE: the primitives emit ONLY a leading-warmup null prefix (never interior nulls — verified rolling.ts:85-137, degenerate slice → 0); the adapter's null-filter is therefore a defensive guard, NOT a falsifiable interior-null pin — do not attempt to construct an interior-null fixture (it is impossible).
  </behavior>
  <action>
    Write the failing tests first (RED commit), then implement (GREEN commit) — the repo's 107
    test-then-feat discipline.

    Create `src/lib/scenario-blend-adapter.ts` exporting `deriveBlendPanels(portfolioDaily,
    window, periodsPerYear): BlendPanelSeries` and the `BlendPanelSeries` interface (re-declared
    here; same field names/types as the legacy module so ScenarioComposer's render tree needs
    zero shape changes in Plan 02).

    NAMING (deliberate deviation from 108-PATTERNS' "keep the buildBlendPanels name" suggestion):
    the export is `deriveBlendPanels`, NOT `buildBlendPanels` — the SC-2 delete-gate (Plan 02)
    forbids the `buildBlendPanels` token repo-wide, and the ScenarioComposer.test.tsx:3553
    positive control must re-anchor to a genuinely new live token. The file name
    `scenario-blend-adapter.ts` deliberately does NOT contain the forbidden
    `scenario-blend-panels` token.

    Implementation (all identifiers concrete):
    1. usableN degenerate gate FIRST, reproduced VERBATIM from `scenario-blend-panels.ts:154-176`
       (per 108-CONTEXT §usableN — re-homed here, co-located with the derivation): count
       `Number.isFinite(p.value)` into usableN, flag hasNonFinite; if
       `hasNonFinite || portfolioDaily.length < 10 || portfolioDaily.length < window` return the
       empty shape with `usableN: hasNonFinite ? 0 : usableN`. MIN_USABLE stays 10. Note the
       length checks use `portfolioDaily.length` (not usableN) — preserve exactly; WR-02 pins this.
    2. histogramSeries: the cumprod(1+r) wealth loop copied unchanged from
       `scenario-blend-panels.ts:182-186`. DELIBERATE deviation from RESEARCH "Don't Hand-Roll"
       (which suggested backbone `cumEq`): copying the geometric-wealth loop verbatim is NOT a
       second-Sharpe compute (it is the same geometric wealth `cumEq` produces) and is the safest
       choice for SC-4 pixel parity — checker-confirmed acceptable. Keep the copy.
    3. quantiles: `const q = quantileSummary(rets)` then
       `{ All: [q.min, q.p25, q.p50, q.p75, q.max] }` — min/max tails per USER DECISION
       (108-CONTEXT §Quantile whiskers); p05/p95 are NOT adopted.
    4. rolling series: call `rollingVol(rets, window, periodsPerYear)`,
       `rollingSharpe(rets, window, periodsPerYear)`, `rollingSortino(rets, window,
       periodsPerYear)` from `@/lib/factsheet/rolling` with the EXPLICIT window and basis args
       (this is what preserves the 3M/6M/12M toggle without the 235ms deriveSeriesBundle — per
       108-CONTEXT §Adapter route). OUTPUT-SHAPE SEAM: these return Array<number|null> parallel
       to rets with a LEADING-WARMUP null prefix (null for i < window-1) and a finite value at
       every subsequent index. Zip index i against portfolioDaily[i].date and drop the null
       entries — this yields the {date,value}[] the charts expect, of length n−window+1, dated at
       the window's last day. Implement the null-drop as a defensive `filter(v !== null)` (or
       equivalent) so it is robust regardless of where nulls sit; the primitives only ever produce
       a leading-warmup prefix, so this is a leading-warmup drop in practice. rollingSharpe wraps
       as `{ sharpe_365d: zipped }` — the key is frozen (RollingMetrics resolves CHART_ACCENT off
       it; the composer overrides the visible label).
    5. Imports: `rollingVol, rollingSharpe, rollingSortino` from `@/lib/factsheet/rolling`;
       `quantileSummary` from `@/lib/factsheet/quantiles`. FORBIDDEN: importing `mean`/`stdDev`
       from `@/lib/portfolio-math-utils` (the sample-std path), importing anything from
       `@/lib/scenario-blend-panels`, importing `deriveSeriesBundle` (too heavy for a per-toggle
       recompute), touching `scenario.ts`/`portfolio-stats.ts`/`health-score.ts`.
    Keep the module pure and synchronous (no fetch/DOM/time), mirroring the legacy header contract.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-blend-adapter.test.ts --no-file-parallelism && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/scenario-blend-adapter.ts` exists, exports `deriveBlendPanels` + `BlendPanelSeries`; grep shows imports from `@/lib/factsheet/rolling` and `@/lib/factsheet/quantiles`, and NO occurrence of `stdDev`, `scenario-blend-panels`, or `deriveSeriesBundle` in the file
    - All behavior tests above green; the degenerate-collapse trio (non-finite / <10 / <window) asserts BOTH the collapse and the exact usableN value
    - Rolling output first date === portfolioDaily[window-1].date, length === n−window+1, and no null-valued points in any emitted series (leading-warmup drop; the null-filter is a defensive guard, not an interior-null pin)
    - `sharpe_365d` key literal present in the adapter source (frozen chart-accent contract)
    - Two commits in RED→GREEN order: test(108-01) then feat(108-01)
  </acceptance_criteria>
  <done>Adapter reproduces the legacy public shape with backbone-primitive internals; its test file green; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC-4 parity pins — population-std at each window (63/126/252) + min/max whisker pin, mutation-falsifiable</name>
  <files>src/lib/scenario-blend-adapter.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts:184-238 (PAYLOAD-03 — the exact mutation-falsifiable pin template: closed-form population-σ fixture, 6dp assertion, WHY comment documenting that a sample-std bleed fails)
    - src/lib/factsheet/rolling.ts:99-137 (rollingSharpe/rollingSortino formulas — needed to derive closed-form expected values, NOT to call as the oracle)
    - .planning/phases/108-scenario-planner-onto-the-backbone/108-CONTEXT.md §Parity std (the USER DECISION this pin encodes)
  </read_first>
  <behavior>
    - Test (per window w ∈ {63, 126, 252}): for an alternating +0.0075/−0.0075 fixture of ≥ 300 points at basis 252, rollingVol values equal the CLOSED-FORM population value — for even w: exactly 0.0075·√252; for w=63 (odd): 0.0075·√(1−1/63²)·√252 — asserted with toBeCloseTo(…, 6). A sample-std source (÷ n−1) yields 0.0075·√(w/(w−1))·√252 which differs at the asserted precision for every window → the pin is mutation-falsifiable
    - Test: whisker pin — a fixture with one +0.05 outlier among ~100 small returns → quantiles.All[4] === 0.05 exactly (absolute max) and All[0] === the absolute min; a p95/p05 bleed produces a strictly smaller top whisker → fails
    - Test: window-toggle contract — for the same fixture, the three windows produce rolling series of lengths n−63+1 / n−126+1 / n−252+1 respectively (the explicit-window arg is actually honored)
  </behavior>
  <action>
    Extend `src/lib/scenario-blend-adapter.test.ts` with the SC-4 parity-pin block, mirroring
    PAYLOAD-03's discipline (scenario-factsheet-payload.test.ts:210-223): compute expected values
    from CLOSED-FORM math written inline in the test (constants like 0.0075 * Math.sqrt(252)),
    never by calling the primitive under test (no circular oracle). Derivation to encode in a
    WHY comment: an even-length alternating ±a window has mean 0 and population σ = a exactly;
    a 63-length window has mean ±a/63 and population σ = a·√(1−1/63²); sample std multiplies by
    √(w/(w−1)), which moves the annualized value at the 4th decimal — so the 6dp pin fails loudly
    on any sample-std bleed. Document per 108-CONTEXT §Parity std that this is the intentional
    convention unification (one rolling-std path, backbone canonical), NOT a regression.

    Add the outlier-fixture min/max whisker pin (USER DECISION §Quantile whiskers): top whisker
    must be the absolute max (0.05), which a p95 substitution cannot produce.

    Add the per-window length assertions proving the explicit-window arg drives the compute
    (toggle preservation, SC-4-UI groundwork for Plan 02).

    If a closed-form Sharpe/Sortino pin is cheap after reading rolling.ts:99-137 (e.g. even-window
    alternating fixture → rolling Sharpe exactly 0), add it; do NOT hand-wave a formula without
    reading the source first. The falsifiability carrier is the VOL pin — that one is mandatory
    at all three windows.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario-blend-adapter.test.ts src/__tests__/metrics-parity.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Parity pins exist at ALL THREE windows (63/126/252) with closed-form expected values at 6dp; test file contains the sample-vs-population WHY comment referencing 108-CONTEXT §Parity std
    - Falsifiability spot-check performed once locally: temporarily multiplying the adapter's vol path by √(w/(w−1)) turns the pins RED (then reverted) — note the check in the commit message or summary
    - Min/max whisker pin present with the outlier fixture (All[4] === absolute max)
    - `metrics-parity.test.ts` green and UNTOUCHED (it asserts backbone values this plan does not move; if it goes red, STOP — the backbone was accidentally edited — do not re-pin it)
  </acceptance_criteria>
  <done>SC-4 pins green and mutation-falsifiable; metrics-parity untouched and green; adapter suite is the parity contract Plan 02 rewires against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Pure client-side numeric transform of an already-loaded, already-validated return series (`computeScenario` output). No persistence, no network, no auth surface, no new user input beyond the existing window toggle. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering (data integrity) | deriveBlendPanels — NaN/Inf in portfolio_daily_returns reaching a chart | mitigate | usableN gate reproduced verbatim (collapse on hasNonFinite, usableN=0) + defensive null-filter at the zip seam; pinned by Task 1 behavior tests (ASVS V5) |
| T-108-02 | S/R/I/D/E | client-side blend-panel derivation | accept | No auth/session/access/crypto/persistence surface; display-only derived output. No package installs this phase → no supply-chain row. |
</threat_model>

<verification>
- `npx vitest run src/lib/scenario-blend-adapter.test.ts src/__tests__/metrics-parity.test.ts --no-file-parallelism` green
- `npm run typecheck` clean
- grep gate on the adapter: no `stdDev` / `scenario-blend-panels` / `deriveSeriesBundle` tokens
</verification>

<success_criteria>
- Adapter exports `deriveBlendPanels` with the exact legacy `BlendPanelSeries` shape, deriving via backbone population-std primitives at an explicit window
- SC-4 parity pins green at 63/126/252 with min/max whiskers, mutation-falsifiable
- usableN degenerate contract preserved verbatim (WR-02 semantics)
- metrics-parity.test.ts untouched and green
</success_criteria>

<output>
Create `.planning/phases/108-scenario-planner-onto-the-backbone/108-01-SUMMARY.md` when done
</output>
