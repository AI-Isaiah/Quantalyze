---
phase: 104-unified-queued-path-persists-process-key-long
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - analytics-service/tests/test_cash_basis_series_sc4.py
autonomous: true
requirements: [BB-01]

must_haves:
  truths:
    - "SC-4 dual-run: with the cash persist active vs no-opped, the csv_daily_returns delete+upsert payloads, the strategy_analytics prestamp payload, and the enqueue RPC calls are byte-identical — the ONLY delta is the cash strategy_analytics_series write — and because the benchmark-fetch hoist was dropped in 104-01, this dual-run now captures the ENTIRE 104 production delta"
    - "Series round-trip: the persisted sparse cash rows reproduce the finite fixture series bit-exactly, guard-day absent from BOTH the series row and csv_daily_returns, gap_spans covering it (coverage mask exists)"
    - "SERIES-ONLY boundary held: analytics_runner.py (the authoritative cash scalar path) contains zero non-comment references to derive_basis_series/basis_series"
    - "INERT read: no non-test source outside the write seam pairs cash_settlement with a series-kind read — the new row has zero consumers this phase"
    - "Conventions fidelity: a Zavara-style simple/active fixture and a traditional-asset √252 fixture echo the correct conventions in the persisted payload while the legacy outputs stay untouched"
  artifacts:
    - path: "analytics-service/tests/test_cash_basis_series_sc4.py"
      provides: "SC-4 dual-run harness + round-trip identity + convention fixtures + boundary guard tests"
      min_lines: 120
  key_links:
    - from: "analytics-service/tests/test_cash_basis_series_sc4.py"
      to: "analytics-service/services/job_worker.py"
      via: "mocked run_derive_broker_dailies_job dual-run"
      pattern: "run_derive_broker_dailies_job"
---

<objective>
Prove the four load-bearing Phase-104 guarantees (roadmap SC-2/SC-3/SC-4 + the SC-1 round-trip) with executor-runnable pytest — no MCP, no live DB: (1) SC-4 cash byte-identity via a dual-run at the seam, (2) series↔csv_daily_returns round-trip identity + coverage mask, (3) the SERIES-ONLY boundary (cash scalars NOT routed through `derive_basis_series`), (4) the INERT-read guarantee (no reader consumes the new `cash_settlement` row this phase).

The full-suite run doubles as the SC-3 cash golden sweep: the existing cash-pin/golden tests (nine analytics cash pins from MTM-03 + the 103-02 cash-golden sweep) guard every legacy cash output, and 104-01 touched neither `analytics_runner.py` nor `metrics.py`, so a green full suite IS the byte-identity sweep.

Output: `analytics-service/tests/test_cash_basis_series_sc4.py` (tests only — zero production-code changes in this plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-unified-queued-path-persists-process-key-long/104-RESEARCH.md
@.planning/phases/104-unified-queued-path-persists-process-key-long/104-01-SUMMARY.md

Executor constraints: NO git branch operations (only `git add <explicit paths>`); never stage `.planning/`. Tests must run in plain pytest (gsd-executor has no Supabase MCP; `*_live.py` skip in CI — do not write one).

<interfaces>
Pattern anchors (read, do not modify):
- `analytics-service/tests/test_mtm_single_key.py:710-795` — `test_sc4_cash_parity_mtm_on_vs_off` / `test_sc4_cash_parity_mtm_degraded`: the job-level dual-run SC-4 idiom (run the mocked derive twice, compare captured writes).
- `analytics-service/tests/test_native_nav_sc4_identity.py:1-45` — the `assert_series_equal(check_exact=True)` byte-identity discipline.
- `analytics-service/tests/test_derive_broker_dailies_dualmode.py:55-120` — `_build_ctx` mock-supabase capture harness (`capture['upserts']`, `capture['rpc_calls']`).
- `analytics-service/services/job_worker.py` post-104-01 — the cash block sits after the MTM series persist, before `_enqueue_csv_analytics`; `basis="cash_settlement"` appears exactly once.

Guard-scan facts (verified at plan time, 2026-07-14):
- Zero lines in `src/**/*.ts{,x}` (non-test) pair `cash_settlement` with `kind` or `strategy_analytics_series`.
- In `analytics-service/services/` + `analytics-service/routers/`, the only such pairing is inside `basis_series.py` (write seam) — and after 104-01, `job_worker.py` (write seam).
- Non-test files touching `strategy_analytics_series`: frontend readers key exclusively on `mtm_daily_returns` (`src/lib/types.ts:541`, `MTM_DAILY_RETURNS_SERIES_KIND`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC-4 dual-run byte-identity + series round-trip + convention fixtures</name>
  <files>analytics-service/tests/test_cash_basis_series_sc4.py</files>
  <read_first>analytics-service/tests/test_mtm_single_key.py:1-120 and :710-795 (harness + dual-run idiom), analytics-service/tests/test_derive_broker_dailies_dualmode.py:40-160 (_build_ctx capture), analytics-service/services/job_worker.py (the post-104-01 seam region around the cash block), analytics-service/services/basis_series.py</read_first>
  <behavior>
    - Test 1 (SC-4 dual-run): run the mocked strategy-mode `run_derive_broker_dailies_job` TWICE on identical fixtures — Run A as-shipped; Run B with `services.job_worker.persist_basis_series` wrapped so calls with `basis="cash_settlement"` no-op (calls with `basis="mark_to_market"` pass through — this simulates the pre-104 seam). Assert dict-equality of the captured `csv_daily_returns` delete filters + upsert payloads, the `strategy_analytics` prestamp payload (incl. `metrics_json_by_basis` — no `cash_settlement` key in either run), and the `enqueue_compute_job` RPC calls; assert the ONLY delta between runs is the presence of the `cash_settlement` entry in the `upsert_strategy_analytics_series_batch` p_kinds.
    - Test 2 (round-trip + mask, SC-1): a fixture with an interior NaN guard day — rebuild a pd.Series from the captured cash payload rows and `assert_series_equal(check_exact=True)` against the finite subset of the fixture `returns`; assert the guard day is ABSENT from both the payload rows AND the csv_daily_returns upsert dates (identical date sets, bit-equal values); assert `gap_spans` includes the guard day (the coverage mask exists and is a pure function of the rows).
    - Test 3 (Zavara conventions): a fixture with a `returns_denominator_config` resolving to `cumulative_method="simple"` → the cash payload conventions echo `{"cumulative_method": "simple"}` with the config's day basis; prestamp/enqueue captures identical to a dual-run no-op (SC-4 held under the override).
    - Test 4 (traditional clock + unconditional identity): an `asset_class` resolving to `periods_per_year=252` → conventions echo 252 (kills a hardcoded-365 mutation, Pitfall 2); conventions carry `benchmark: "BTC"` WITHOUT any benchmark fetch being mocked — the cash derive passes `benchmark_symbol="BTC"` with `benchmark_rets=None`, so the identity is present unconditionally (kills a mutation that drops the identity string or makes it fetch-contingent).
  </behavior>
  <action>
    Create `analytics-service/tests/test_cash_basis_series_sc4.py` with a module docstring stating what it pins: Phase-104 BB-01 SC-4 byte-identity (the additive dark cash-series persist changes NOTHING the legacy cash path writes or reads), the SC-1 round-trip identity (persisted sparse rows ≡ csv_daily_returns rows ≡ the finite fixture series), and convention fidelity. Reuse/adapt the `_build_ctx` capture-harness pattern (import helpers from tests.test_derive_broker_dailies_dualmode if importable, else replicate the minimal mock shape locally — do NOT modify the source files). Every assertion must be mutation-falsifiable: name in comments which mutation each test kills (e.g., Test 1 kills the cash block leaking a `cash_settlement` key into `metrics_json_by_basis` or perturbing rows_payload; Test 2 kills a `gap_fill`/0.0-coercion of the guard day; Test 4 kills convention hardcoding). Use `assert_series_equal(check_exact=True)` for all series comparisons — no tolerance.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_cash_basis_series_sc4.py -x -q</automated>
  </verify>
  <done>All four tests pass; each fails when its named mutation is applied (spot-check at least Test 1 by temporarily making the cash block write a `cash_settlement` key into the prestamp's `metrics_json_by_basis` — then revert); zero production files modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: boundary guards — SERIES-ONLY grep gate + INERT-read repo scan + full-suite golden sweep</name>
  <files>analytics-service/tests/test_cash_basis_series_sc4.py</files>
  <read_first>analytics-service/services/analytics_runner.py:2246-2320 (the legacy cash scalar branch being guarded — read-only), src/lib/types.ts:520-555 (the frontend series-kind surface — read-only)</read_first>
  <behavior>
    - Test 5 (SERIES-ONLY, SC-2): read `services/analytics_runner.py` source, strip lines whose first non-whitespace char is `#`, assert ZERO occurrences of `derive_basis_series` and of `basis_series` — the authoritative cash scalar path was not routed through the shared derive (that is Phase-105 scope).
    - Test 6 (INERT read, SC-4 roadmap): walk the repo from the project root — `src/**/*.ts` + `src/**/*.tsx` (excluding `*.test.*`) plus `analytics-service/services/*.py` + `analytics-service/routers/*.py` (excluding `basis_series.py` and `job_worker.py`, the two write-seam files) — and assert NO non-comment line contains BOTH the substring `cash_settlement` AND (`kind` OR `strategy_analytics_series`). Docstring: this guard is a PHASE GUARD — Phase 105/106 lands the reader and DELETES this test deliberately.
    - Test 7 (single seam): read `services/job_worker.py`, strip comment lines, assert exactly ONE occurrence of the literal `basis="cash_settlement"` — kills a second bootleg persist site (e.g., someone "helpfully" adding one to `run_compute_analytics_job`, the 106-slated dark-path re-entry point; A3 decision: legacy-tail strategies get an honest ABSENCE this phase, never a fabricated fill).
  </behavior>
  <action>
    Append the three guard tests to `analytics-service/tests/test_cash_basis_series_sc4.py`. Resolve the repo root robustly (walk up from `__file__` to the directory containing both `src/` and `analytics-service/`) so the scan works from the `analytics-service` pytest cwd and in CI. Comment-stripping is mandatory (grep-gate hygiene: a docstring/comment mentioning the token must not trip or satisfy the gate) — for the .ts/.tsx scan also skip lines whose stripped form starts with `//` or `*`. Keep the file-set predicates EXACTLY as specified (verified zero false positives at plan time); if a false positive appears at execution time, whitelist the specific file with an inline comment naming why, never loosen the predicate.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_cash_basis_series_sc4.py -x -q && python -m pytest --cov --cov-fail-under=80 -q</automated>
  </verify>
  <done>Tests 5-7 pass; Test 6 demonstrably fails when a line pairing `.eq("kind", "cash_settlement")` is temporarily added to a frontend reader (neuter check — then revert); the FULL analytics suite is green at >=80% coverage — this run includes every existing cash-pin/golden test and therefore constitutes the SC-3 cash golden byte-identity sweep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Supabase | unchanged from 104-01 (this plan adds tests only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03 | Repudiation/integrity | silent SC-4 regression at a later edit of the seam | mitigate | the dual-run + round-trip tests are permanent regressions; guards 5-7 pin the phase boundary until 105 deliberately retires them |
| T-104-SC | Tampering | package installs | accept | no packages installed (tests only) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_cash_basis_series_sc4.py -q` — all 7 tests green.
- `cd analytics-service && python -m pytest --cov --cov-fail-under=80 -q` — full suite green (= the SC-3 golden sweep; the nine cash-pin files + 103-02 cash-golden sweep all pass unmodified).
- `git diff --stat` for this plan shows ONLY `analytics-service/tests/test_cash_basis_series_sc4.py`.
- `grep -v '^\s*#' analytics-service/services/analytics_runner.py | grep -c 'derive_basis_series'` returns 0 (manual spot-check of the Test-5 gate).
</verification>

<success_criteria>
- All 5 roadmap Phase-104 Success Criteria have executor-runnable proof: SC-1 (Task 1 round-trip + mask), SC-2 (Test 5 grep gate), SC-3 (full-suite golden sweep), SC-4 (Test 1 dual-run + Test 6 inert-read), SC-5 (benchmark echo asserted in Task 1 Test 4; no-new-math held by the SC-2/diff gates).
- A3 edge handled honestly and pinned (Test 7 + the 104-01 seam comment): legacy-tail strategies get an honest absence, no fabricated fill, no persist added to the 106-slated dark path.
</success_criteria>

<output>
Create `.planning/phases/104-unified-queued-path-persists-process-key-long/104-02-SUMMARY.md` when done.
</output>
