---
phase: 104-unified-queued-path-persists-process-key-long
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/basis_series.py
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_basis_series.py
  - analytics-service/tests/test_derive_broker_dailies_dualmode.py
autonomous: true
requirements: [BB-01]

must_haves:
  truths:
    - "After a clean strategy-mode broker derive, a strategy_analytics_series row with kind='cash_settlement' exists carrying the sparse cash rows, gap_spans coverage mask, and a conventions echo that UNCONDITIONALLY includes benchmark identity 'BTC' (an identity STRING, not a returns fetch — cash derive is called with benchmark_rets=None; 105 re-derives α/β itself)"
    - "A cash derive that raises ValueError (<2 finite rows) heal-DELETEs any stale cash_settlement row instead of leaving it to outlive the authoritative state"
    - "csv_daily_returns writes, the strategy_analytics prestamp payload, and the MTM persist are byte-unchanged by this plan (SC-3/SC-4 by construction)"
    - "Key-mode derives write NO cash_settlement series row (early return preserved)"
  artifacts:
    - path: "analytics-service/services/basis_series.py"
      provides: "KIND_CASH_SETTLEMENT + _KIND_BY_BASIS['cash_settlement'] + optional benchmark_symbol conventions echo"
      contains: "cash_settlement"
    - path: "analytics-service/services/job_worker.py"
      provides: "additive dark cash-series persist at the broker-derive seam (benchmark_rets=None, benchmark_symbol=\"BTC\") + MTM call site passes benchmark_symbol=\"BTC\" for uniform identity carry (no benchmark-fetch hoist)"
      contains: "basis=\"cash_settlement\""
    - path: "analytics-service/tests/test_basis_series.py"
      provides: "cash kind mapping + round-trip + heal + benchmark-echo tests"
    - path: "analytics-service/tests/test_derive_broker_dailies_dualmode.py"
      provides: "seam-level persist-fires / heal / key-mode-absent / identity-independent-of-fetch tests"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "analytics-service/services/basis_series.py"
      via: "derive_basis_series(returns, ...) then persist_basis_series(basis='cash_settlement')"
      pattern: "basis=\"cash_settlement\""
    - from: "analytics-service/services/basis_series.py"
      to: "upsert_strategy_analytics_series_batch RPC"
      via: "persist_basis_series p_kinds payload"
      pattern: "upsert_strategy_analytics_series_batch"
---

<objective>
BB-01, Phase 104 (SERIES-ONLY): at the single-key broker-derive seam — directly beside Phase-103's MTM `derive_basis_series` block — add an additive DARK persist of the cash daily-return SERIES as a new `strategy_analytics_series` kind `cash_settlement`, plus the benchmark-identity carry in the conventions echo (roadmap 104-SC5, so Phase-105's scalar route can reproduce α/β/corr from persisted rows).

PREMISE (locked, do not re-open): `process_key_long` ALREADY persists via the legacy `sync_trades → compute_analytics` chain (fixed + prod-verified 2026-05-27, PR #335/#336). This plan is a ROUTING/persistence addition of ~15-40 production LOC, NOT a persistence bootstrap.

HARD BOUNDARIES (locked decisions):
- SERIES ONLY. The authoritative cash SCALARS stay on the legacy path (`analytics_runner.py` untouched). Routing cash scalars through `derive_basis_series` would bridge broker guard-day NaN breaks and 0.0-fill sparse user-CSV gaps → SC-4 violation. Scalar cache-ification is Phase 105.
- Additive DARK write: nothing reads the new row this phase. No `USE_COMPUTE_JOBS_QUEUE` gating on the write (Phase-103 MTM precedent).
- NO DDL, no new SECDEF, no migration: `strategy_analytics_series.kind` is unconstrained TEXT; reuse the hardened `upsert_strategy_analytics_series_batch` RPC. If you conclude DDL is needed, STOP and flag it — do not write a migration.
- The CSV single-key inline swap (`analytics_runner.py:2318`) is Phase 105. Do not touch it.
- No new valuation math: `compute_all_metrics`, `gap_fill_daily_returns`, `_drop_nonfinite` untouched.

Purpose: first cash adoption of the dailies-canonical backbone — the canonical cash series exists behind the scenes so Phase 105 can flip scalars to a cache of it.
Output: `_KIND_BY_BASIS` cash entry + benchmark-identity echo in `basis_series.py`; the cash derive+persist block in `run_derive_broker_dailies_job`; extended unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-unified-queued-path-persists-process-key-long/104-RESEARCH.md

Executor constraints: NO git branch operations (only `git add <explicit paths>`); never stage `.planning/`. No Supabase MCP available — all verification is pytest against mocks/fixtures.

<interfaces>
From analytics-service/services/basis_series.py (the shared route — read whole file, 225 lines):

- `derive_basis_series(returns: pd.Series, benchmark_rets: pd.Series | None, *, periods_per_year: int, cumulative_method: str, day_basis: str) -> BasisSeriesResult` — sparse = `_drop_nonfinite(returns).sort_index()`; raises ValueError when <2 finite rows; scalars from `compute_all_metrics(gap_fill_daily_returns(sparse), ...)`; `series_rows` = ascending `[{"date","return"}]` unrounded; `gap_spans` from `_consecutive_spans` over absent calendar days; `conventions` = `{"periods_per_year","cumulative_method","day_basis"}`.
- `persist_basis_series(supabase, strategy_id, *, basis="mark_to_market", result: BasisSeriesResult | None)` — result present → RPC `upsert_strategy_analytics_series_batch` with `p_kinds={kind: {schema, basis, rows, gap_spans, conventions}}`; result None → DELETE the `(strategy_id, kind)` row (HEAL). Unknown basis → ValueError.
- `_KIND_BY_BASIS: dict[str, str] = {"mark_to_market": KIND_MTM_DAILY_RETURNS}` at line 61 — the documented cash join point.
- `_PAYLOAD_SCHEMA_VERSION = 1` — do NOT bump (MTM payload shape stays byte-identical; the cash payload is a brand-new kind with zero readers).

From analytics-service/services/job_worker.py (`run_derive_broker_dailies_job`, the seam — read :2780-3146):

- `returns` (strategy mode) = the POST-terminus DENSE daily-return series; guard-broken days are np.nan. `rows_payload` (:2842-2850) keeps only `pd.notna(val)` rows → `_drop_nonfinite(returns)` inside the helper reproduces EXACTLY the csv_daily_returns rows (round-trip identity by construction).
- `is_key_mode` early-returns at :2915-2925 — everything below is strategy-mode only.
- MTM conventions resolution (:2957-2967): `periods_per_year_for_asset_class(ctx.strategy_row.get("asset_class") if isinstance(ctx.strategy_row, dict) else None)`; `denominator_config` present → `denominator_config.cumulative_method` + `metrics_day_basis(denominator_config.metrics_basis)`, else `("geometric", "calendar")`. Cash uses the SAME resolution (Pitfall 2).
- Guarded benchmark fetch (:2971-2981): `get_benchmark_returns("BTC")` returns `(pd.Series | None, is_stale)`; DB-cache-backed (`benchmark_prices`, 48h freshness) so one call per derive is cheap; failure → warning log, proceed with None. Currently INSIDE `if mtm_returns is not None:` — Task 2 LEAVES it there (NO hoist; the cash derive passes `benchmark_rets=None` and carries only the `benchmark_symbol="BTC"` identity string).
- MTM series persist success-matrix + `db_execute(_persist_mtm_series)` idiom (:3112-3136) — the DIRECT template for the cash persist.
- `_enqueue_csv_analytics` (:3138-3145) — the cash block lands BEFORE this handoff.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: cash joins _KIND_BY_BASIS + benchmark-identity conventions echo (contracts first)</name>
  <files>analytics-service/services/basis_series.py, analytics-service/tests/test_basis_series.py</files>
  <read_first>analytics-service/services/basis_series.py (whole file, incl. the docstring cash-adoption note), analytics-service/tests/test_basis_series.py, analytics-service/services/job_worker.py:2934-3146 (the MTM analog block, for context only)</read_first>
  <behavior>
    - Test 1 (mapping): `_KIND_BY_BASIS["cash_settlement"] == "cash_settlement"` and a module constant `KIND_CASH_SETTLEMENT` equals that string.
    - Test 2 (round-trip persist): `persist_basis_series(mock_supabase, sid, basis="cash_settlement", result=<derived>)` fires the `upsert_strategy_analytics_series_batch` RPC with `p_kinds` keyed `"cash_settlement"` and payload fields `schema=1, basis="cash_settlement", rows, gap_spans, conventions`; rebuilding a pd.Series from the payload rows reproduces the sparse input exactly (`assert_series_equal(check_exact=True)`).
    - Test 3 (heal): `result=None` issues a DELETE filtered `kind="cash_settlement"` (and `strategy_id`), no RPC.
    - Test 4 (benchmark echo present + omittable): `derive_basis_series(..., benchmark_symbol="BTC")` → `conventions["benchmark"] == "BTC"`; called WITHOUT the kwarg (default None) → `"benchmark"` NOT in conventions AND conventions keys are exactly `{"periods_per_year","cumulative_method","day_basis"}`. This pins the kwarg is ADDITIVE (a None default omits the key), so a caller that opts out is unaffected — while both real 104 call sites (cash AND MTM) pass `benchmark_symbol="BTC"` and therefore both carry the identity uniformly.
  </behavior>
  <action>
    In `analytics-service/services/basis_series.py`:
    1. Add `KIND_CASH_SETTLEMENT = "cash_settlement"` beside `KIND_MTM_DAILY_RETURNS` (line ~58) and add `"cash_settlement": KIND_CASH_SETTLEMENT` to `_KIND_BY_BASIS` (line 61). No DDL — kind is unconstrained TEXT by documented design (locked decision 4).
    2. Add keyword-only parameter `benchmark_symbol: str | None = None` to `derive_basis_series`. When `benchmark_symbol is not None`, include `"benchmark": benchmark_symbol` in the `conventions` dict; when None, the dict is UNCHANGED (three keys) so a caller that omits the kwarg is unaffected. This is the 104-SC5 benchmark-identity carry — an identity STRING only, NOT a returns fetch (STATE.md carry-forward: α/β/corr are outside the persist round-trip guarantee; Phase 105's scalar route re-derives them itself but needs to know WHICH benchmark, so the string travels alongside `conventions`). In THIS edit, also update the MTM persist call site (job_worker.py :2991-2997) to pass `benchmark_symbol="BTC"` so BOTH bases carry the field uniformly. SC-4-SAFE: the MTM persisted-payload `conventions` gaining a `benchmark` key is invisible at the factsheet level because NO reader consumes `conventions.benchmark` this phase — Phase 105's round-trip guard is its first consumer; the MTM factsheet scalars/series/mask are untouched. Do NOT bump `_PAYLOAD_SCHEMA_VERSION` — no reader keys on the version and the shape delta is an additive optional field. Document in the `BasisSeriesResult.conventions` docstring: `benchmark` present when the deriving call site passed the identity string.
    3. Update the module docstring tail note ("Cash paths are NOT routed through this module in Phase 103") with a Phase-104 addendum: the single-key broker derive now ALSO persists the cash SERIES here (`basis="cash_settlement"`, additive/dark, no reader yet); the authoritative cash SCALARS remain on the legacy `analytics_runner` path until Phase 105 — routing cash scalars through this module before the NaN/gap-fill reconciliation is an SC-4 violation (Pitfall 1, 104-RESEARCH).
    Then extend `analytics-service/tests/test_basis_series.py` with the four behaviors above, following the file's existing mock-supabase idiom (write the failing tests first, then the implementation).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_basis_series.py -x -q</automated>
  </verify>
  <done>All four behaviors pass; existing test_basis_series.py tests stay green; `grep -v '^\s*#' services/basis_series.py | grep -c 'KIND_CASH_SETTLEMENT'` >= 2 (definition + map); no change to `_PAYLOAD_SCHEMA_VERSION`. NO-DDL CONFIRM (confirm-not-discover — the MTM `kind` already flows through this RPC in prod): read `supabase/schema/functions/upsert_strategy_analytics_series_batch.sql` and confirm the body has NO `CASE`/whitelist/`IN (...)` gate on the incoming `kind` (it must accept an arbitrary-`kind` INSERT) — if any such gate exists, STOP and flag (the no-DDL premise would be false).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: additive dark cash-series persist at the broker-derive seam</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>analytics-service/services/job_worker.py:2780-3146 (the seam: rows_payload builder, key-mode return, MTM block, prestamp, MTM series persist, enqueue), analytics-service/services/basis_series.py (post-Task-1), analytics-service/tests/test_mtm_single_key.py:1154-1265 (test_single_key_routes_through_shared_derive_and_persists + heal tests — the DIRECT test analogs), analytics-service/tests/test_derive_broker_dailies_dualmode.py:1-120 (the _build_ctx capture harness)</read_first>
  <behavior>
    - Test 1 (persist fires): a strategy-mode clean derive fires `upsert_strategy_analytics_series_batch` with a `p_kinds` entry `"cash_settlement"` whose rows equal exactly the finite (pd.notna) rows of the fixture `returns` (dates + bit-equal floats), whose `gap_spans` covers an interior NaN guard-day fixture, and whose conventions echo carries the asset-class `periods_per_year` + `cumulative_method`/`day_basis` + `benchmark: "BTC"` UNCONDITIONALLY (the cash derive passes `benchmark_symbol="BTC"` with `benchmark_rets=None` — the identity does not depend on any fetch).
    - Test 2 (identity independent of any fetch): even with the MTM-side `get_benchmark_returns("BTC")` mocked to RAISE, the cash persist still fires AND its conventions echo `benchmark == "BTC"` (the cash derive never calls the fetch — it passes `benchmark_rets=None` + the unconditional identity string), job outcome DONE. Kills any mutation that makes the cash identity contingent on the MTM benchmark fetch.
    - Test 3 (heal on derive reject): `derive_basis_series` monkeypatched to raise ValueError → the cash persist runs the DELETE arm (`result=None`) and the job still completes DONE (cash rows + prestamp + enqueue unaffected).
    - Test 4 (key-mode absent): a key-mode derive fires NO `strategy_analytics_series` write of any kind (early-return pin).
    - Test 5 (byte-unchanged neighbors): the captured `csv_daily_returns` delete+upsert payloads and the `strategy_analytics` prestamp payload (incl. `metrics_json_by_basis` — NO `cash_settlement` key ever) are identical to the pre-change expectations already pinned in this file / test_mtm_single_key.py.
  </behavior>
  <action>
    In `run_derive_broker_dailies_job` (analytics-service/services/job_worker.py), strategy-mode only (below the :2915 key-mode return):
    1. LEAVE THE MTM BENCHMARK FETCH EXACTLY WHERE IT IS (:2971-2981, inside `if mtm_returns is not None:`). Do NOT hoist it and do NOT repoint `_mtm_benchmark_rets`. The cash persist is SERIES-ONLY, and `benchmark_rets` inside `derive_basis_series` feeds ONLY `compute_all_metrics`→`metrics_json`, which `persist_basis_series` DISCARDS (basis_series.py:214-224 persists rows/gap_spans/conventions only) — so a benchmark RETURNS fetch buys the cash persist NOTHING. SC-5 needs only the benchmark IDENTITY STRING in `conventions`. Adding a fetch to the cash-only derive path would introduce needless I/O AND make the identity contingent on a transient BTC-price outage (a failed fetch would persist a cash row WITHOUT the identity → Phase 105 could not reproduce α/β). So the cash derive gets `benchmark_rets=None` + an UNCONDITIONAL identity string.
    2. AFTER `await db_execute(_persist_mtm_series)` (:3136) and BEFORE `_enqueue_csv_analytics` (:3138), add the cash block mirroring the MTM idiom exactly:
       - Resolve cash conventions unconditionally (per Pitfall 2, same expressions as the MTM block :2957-2967): `_cash_periods = periods_per_year_for_asset_class(...)` from `ctx.strategy_row["asset_class"]`; `_cash_cumulative`/`_cash_day_basis` from `denominator_config` when present (Zavara `simple` + active), else `("geometric", "calendar")`. Compute these separately from the MTM locals (which only exist when `mtm_returns is not None`).
       - try `_cash_basis_result = derive_basis_series(returns, None, periods_per_year=_cash_periods, cumulative_method=_cash_cumulative, day_basis=_cash_day_basis, benchmark_symbol="BTC")` except ValueError → `_cash_basis_result = None` (heal arm; effectively unreachable given the <2-day early exit at :2780-2808, kept for the Pitfall-5 discipline). Note `benchmark_rets=None` (positional) — no benchmark fetch on this path — while `benchmark_symbol="BTC"` is UNCONDITIONAL so every cash row carries the identity regardless of any MTM-side fetch outcome.
       - `def _persist_cash_series(result=_cash_basis_result) -> None: persist_basis_series(ctx.supabase, strategy_id, basis="cash_settlement", result=result)` then `await db_execute(_persist_cash_series)` — the exact `_persist_mtm_series` idiom.
    3. Block comment (load-bearing, reviewers look here): (a) Phase 104 / BB-01 SERIES-ONLY — this row is a DARK additive write, no reader until Phase 105/106; the authoritative cash SCALARS stay on the legacy `run_csv_strategy_analytics` path (`analytics_runner.py`), and routing them through `derive_basis_series` before the NaN/gap-fill reconciliation is an SC-4 violation (104-RESEARCH Pitfall 1). (b) `returns` here is the dense post-terminus series with NaN guard days, so `_drop_nonfinite` inside the helper reproduces EXACTLY the csv_daily_returns rows written above — series round-trip identity by construction. (c) A3 honesty: strategies whose `sync_trades` tail enqueues legacy `compute_analytics` (`BROKER_DAILIES_VIA_FUNDING` off, :1509-1512) never reach this seam → they get NO `cash_settlement` series row this phase — an HONEST ABSENCE (dark write, zero readers), never a fabricated fill; do NOT add a persist to `run_compute_analytics_job` (a 106-slated dark-path re-entry point); unified coverage lands with the 105/106 route collapse.
    4. Touch NOTHING else: no change to the csv_daily_returns writes, the prestamp payload (never add a `cash_settlement` key to `metrics_json_by_basis`), `analytics_runner.py`, or `compute_all_metrics`.
    Write the five behavior tests in `analytics-service/tests/test_derive_broker_dailies_dualmode.py` first (mirror `test_single_key_routes_through_shared_derive_and_persists` / `test_single_key_derive_helper_valueerror_degrades_and_heals` from tests/test_mtm_single_key.py:1154-1265 and reuse this file's `_build_ctx` capture harness), then implement.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_derive_broker_dailies_dualmode.py tests/test_mtm_single_key.py tests/test_basis_series.py -x -q</automated>
  </verify>
  <done>All five behaviors pass; ALL existing tests in test_mtm_single_key.py (esp. test_benchmark_failure_never_gates_mtm and the SC-4 parity pair) and test_derive_broker_dailies_dualmode.py stay green WITHOUT modification; `grep -v '^\s*#' services/job_worker.py | grep -c 'basis="cash_settlement"'` == 1 (exactly one persist seam); production diff confined to basis_series.py + job_worker.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Supabase | service-role writes to `strategy_analytics_series` via the existing hardened SECDEF RPC |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering/EoP | series write path | mitigate | REUSE `upsert_strategy_analytics_series_batch` (SET search_path + REVOKE + service-role-only, RLS deny-all). NO new SQL, NO new grants — if any SQL is proposed, stop and flag. |
| T-104-02 | Information disclosure / integrity | stale `cash_settlement` row surfacing a wrong money number later | mitigate | Pitfall-5 heal matrix (`result=None` DELETE on derive reject) + the row is dark (inert-read guard in plan 104-02). |
| T-104-SC | Tampering | package installs | accept | No packages installed this phase (research Package Legitimacy Audit: N/A). |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_basis_series.py tests/test_derive_broker_dailies_dualmode.py tests/test_mtm_single_key.py -q` green.
- `grep -v '^\s*#' analytics-service/services/basis_series.py | grep -c '"cash_settlement"'` >= 1 (map entry present).
- `git diff --stat` shows production changes ONLY in `analytics-service/services/basis_series.py` + `analytics-service/services/job_worker.py` (no analytics_runner.py, no metrics.py, no migrations).
- `ls supabase/migrations/ | tail -3` shows no new migration file.
</verification>

<success_criteria>
- Roadmap 104-SC1 (persist + coverage mask + round-trip) implemented at the seam with unit proof.
- 104-SC5 benchmark-identity carry wired (conventions `benchmark` key, additive-only).
- MTM behavior + payloads byte-unchanged; cash scalars/csv_daily_returns/prestamp untouched (SC-2/SC-3 by construction — proven exhaustively in plan 104-02).
- ~15-40 production LOC; no DDL; no new SECDEF.
</success_criteria>

<output>
Create `.planning/phases/104-unified-queued-path-persists-process-key-long/104-01-SUMMARY.md` when done.
</output>
