---
phase: 107-leverage-as-a-dailies-transform
plan: 03
type: execute
wave: 3
depends_on: [107-01, 107-02]
files_modified:
  - src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts
autonomous: true
requirements: [LEV-BB]
must_haves:
  truths:
    - "A permanent source-scan gate fails CI if useLeveragedMetrics/useModeledLeverage/LEVERAGE_CAVEAT ever reappear in src/ (SC-3)"
    - "A permanent source-scan gate fails CI if a standalone compute(<series>.map(...)) leverage path exists outside scenario.ts (SC-5)"
    - "Full suite green with coverage thresholds; SC-4 snapshot and the LEV-02 surface (scenario.ts, leverage.ts) byte-untouched"
    - "bootstrapCI re-derive cost is MEASURED; the 200ms derive-debounce lands only if measurement demands it (CONTEXT performance decision)"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts"
      provides: "SC-3 + SC-5 source-scan gates"
      min_lines: 40
  key_links:
    - from: "src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts"
      to: "src/ tree"
      via: "recursive readFileSync source scan (GUARD-04 Test 6 shape)"
      pattern: "readFileSync"
---

<objective>
Land the permanent SC-3/SC-5 backbone gates and run the phase verification sweep: full suite +
coverage, SC-4 snapshot unchanged, LEV-02 surface untouched, and the measurement-gated bootstrapCI
performance decision.

Purpose: SC-5 is the phase's structural claim — ONE preparation transform, no second leverage
compute path — and it must be a falsifiable gate, not a code-review observation.
Output: gates test file + verification results recorded in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-leverage-as-a-dailies-transform/107-CONTEXT.md
@.planning/phases/107-leverage-as-a-dailies-transform/107-VALIDATION.md

<interfaces>
The source-scan template — leverage-context.test.tsx Test 6 (:191-205, GUARD-04): readFileSync the
source, strip comment lines, regex-assert absence. The new gates generalize it to a recursive walk.

Forbidden after this phase (SC-3 symbols): useLeveragedMetrics, useModeledLeverage, LEVERAGE_CAVEAT.
Forbidden pattern (SC-5): a `compute(` call fed directly by a `.map(` returns-scaling expression —
the retired shape was `compute(payload.strategyReturns.map(r => appliedLeverage * r), ...)`.
Exempt: src/lib/scenario.ts (LEV-02, out of scope) and the gates test file itself.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SC-3 + SC-5 source-scan gates</name>
  <files>src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts</files>
  <read_first>
    - src/app/factsheet/[id]/v2/leverage-context.test.tsx Test 6 (:~150-170 post-prune) — the readFileSync + regex shape to copy
    - src/lib/scenario.ts:424-427 — the exempt precedent (verify the SC-5 regex WOULD match it, proving the gate is live)
  </read_first>
  <action>
    New vitest file (node env, no jsdom needed) with two tests:
    1. SC-3 gate: recursively walk `src/` (node:fs readdirSync withFileTypes, .ts/.tsx only,
       skip node_modules/__snapshots__ and THIS test file's own path). Assert no file contains the
       tokens `useLeveragedMetrics`, `useModeledLeverage`, `LEVERAGE_CAVEAT` — build each token by
       string concatenation (e.g. "useLeveraged" + "Metrics") so the gate never matches itself even
       if the self-exclusion path drifts. On failure, report the offending file list.
    2. SC-5 gate: same walk, additionally skipping `src/lib/scenario.ts`. Strip comment lines
       (`grep -v`-equivalent: drop lines matching /^\s*(\/\/|\*|\/\*)/ — the gate must not fire on
       prose describing the deleted path). Assert no remaining line matches
       /compute\(\s*[\w$.\[\]]+\.map\(/ — a compute() fed directly by a mapped (scaled) series, the
       retired bespoke shape. Also assert the LIVENESS of the gate: the regex-family DOES match a
       hard-coded sample of the retired line (a string fixture inside the test), so a regex typo
       can't silently pass everything.
    3. Keep both scans line-based with comment-stripping (grep-gate hygiene — header prose must not
       self-invalidate the gate).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Both gates green against the post-plan-02 tree
    - Neuter-proof: temporarily re-adding the string `useLeveragedMetrics` to any src file (scratch check, reverted) turns the SC-3 gate red; the SC-5 liveness fixture assertion passes
    - scenario.ts exempt (gate green even though :427 levers a daily return); the gates file excludes itself
  </acceptance_criteria>
  <done>Falsifiable SC-3/SC-5 gates in the suite, proven live via the neuter check.</done>
</task>

<task type="auto">
  <name>Task 2: Phase verification sweep + measurement-gated perf decision</name>
  <files>(none — verification + conditional follow-up only)</files>
  <read_first>
    - .planning/phases/107-leverage-as-a-dailies-transform/107-VALIDATION.md §Sampling Rate + §Manual-Only Verifications
    - 107-UI-SPEC.md §Interaction Contract (Debounce row — the 200ms derive-debounce fallback spec)
  </read_first>
  <action>
    1. Full gate: `npm run test` (8000+ tests, coverage thresholds enforced by vitest.config.ts) +
       `npm run lint` + `npx tsc --noEmit`.
    2. Untouched-surface gates: `git diff --exit-code -- src/lib/factsheet/__snapshots__/build-payload.test.ts.snap src/lib/scenario.ts src/lib/leverage.ts src/lib/factsheet/joint.ts`.
    3. Perf measurement (CONTEXT: measurement-gated, prefer the honest full re-derive): time the
       levered re-derive at production scale with a one-off script (npx tsx, scratchpad only —
       do NOT commit): build a synthetic 3000-day DailyReturn[] and time 5 runs of
       deriveSeriesBundle(levered, { periodsPerYear: 365, isArithmetic: false, markets: ["binance"], strategyName: "perf" }).
       Decision rule: median < 100ms → NO debounce (default stands; record the number in the
       SUMMARY). Median ≥ 100ms → implement the UI-SPEC fallback: debounce the DERIVE (not the
       input) at 200ms inside the useBasisSeriesView leverage layer via useDeferredValue on the
       leverage read or a 200ms setTimeout-based deferred state — last-good bundle stays rendered,
       input stays immediate, no skeleton flash; add a hook test that the input value is never
       blocked. If the debounce lands, it modifies basis-context.tsx + basis-context.leverage.test.tsx
       (declare the deviation in the SUMMARY).
    4. Record in the SUMMARY: perf median, debounce decision, deleted-LOC recount
       (git diff --stat main-of-phase..HEAD over the five leverage files) vs the ~780 target, and the
       VALIDATION.md Per-Task Verification Map status column (all five SC rows → ✅ with commands).
  </action>
  <verify>
    <automated>npm run test && npx tsc --noEmit && npm run lint && git diff --exit-code -- src/lib/factsheet/__snapshots__/build-payload.test.ts.snap src/lib/scenario.ts src/lib/leverage.ts src/lib/factsheet/joint.ts</automated>
    <human-check>Optional feel-check (VALIDATION.md manual-only row): on a local dev factsheet, drag leverage 0→10 rapidly; charts must re-derive without visible freeze. Only actionable if the automated median contradicts the decision taken.</human-check>
  </verify>
  <acceptance_criteria>
    - Full suite green, coverage thresholds hold (lines 82 / stmts 80 / fns 74 / branches 72)
    - All four untouched-surface diffs empty
    - Perf median recorded with the decision applied per the 100ms rule (no undocumented debounce, no unmeasured jank acceptance)
    - SUMMARY carries the LOC recount and the SC-1..SC-5 status map
  </acceptance_criteria>
  <done>Phase gate green end-to-end; perf decision made on measurement, not vibes; SUMMARY complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Gates are test-only; the perf script is scratchpad-only (never committed). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-03 | DoS (client) | full re-derive per L change (bootstrapCI) | mitigate-if-measured | 100ms decision rule → 200ms derive-debounce per UI-SPEC; otherwise accept (client-only, self-inflicted, bounded by MAX_LEVERAGE clamp). |
| T-107-05 | Tampering (regression) | future re-introduction of a second leverage compute path | mitigate | SC-5 source-scan gate with liveness fixture — permanent CI tripwire. |
| T-107-SC | Tampering | package installs | accept | No installs this phase. |
</threat_model>

<verification>
- `npx vitest run "src/app/factsheet/[id]/v2/leverage-backbone-gates.test.ts" --no-file-parallelism` green
- `npm run test` full-suite green with coverage thresholds
- All untouched-surface `git diff --exit-code` gates pass
</verification>

<success_criteria>
- SC-3 and SC-5 hold as permanent falsifiable gates (proven live via neuter check)
- SC-4 snapshot + LEV-02 surface byte-untouched at phase end
- Performance decision documented with the measured number
</success_criteria>

<output>
Create `.planning/phases/107-leverage-as-a-dailies-transform/107-03-SUMMARY.md` when done.
</output>
