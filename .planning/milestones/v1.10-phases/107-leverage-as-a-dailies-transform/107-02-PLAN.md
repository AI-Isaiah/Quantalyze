---
phase: 107-leverage-as-a-dailies-transform
plan: 02
type: execute
wave: 2
depends_on: [107-01]
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/leverage-context.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx
  - src/app/factsheet/[id]/v2/leverage-context.test.tsx
  - src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx
autonomous: true
requirements: [LEV-BB]
must_haves:
  truths:
    - "At L≠1 the KpiStrip, every chart, and the right rail all render the levered view; α/IR show real L-scaled values, never '—' for a leverage reason (SC-1/SC-2)"
    - "The MODELED eyebrow, LEVERAGE_CAVEAT, α/IR leverage-blanking, and BASE·1× rail eyebrow are gone; the reworded muted what-if caption renders at applied L≠1 only (SC-3 + UI-SPEC copy)"
    - "useLeveragedMetrics and useModeledLeverage are deleted; LeverageProvider/useLeverage/LeverageContext and the ControlBar cluster remain (SC-3 keep-list)"
    - "The leverage input renders on whichever basis is ACTIVE and resolved (cash, or MTM with a bundle) — the cash-only gate is removed (CONTEXT scope decision)"
    - "At L=1 the rendered factsheet is byte-identical to pre-change (SC-4); GUARD-04 source-scan stays green"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "KpiStrip reading the levered view; disclosure deleted; reworded copy; widened eligibility"
    - path: "src/app/factsheet/[id]/v2/leverage-context.tsx"
      provides: "provider/state only (~58 lines) — derived hooks deleted"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx"
      provides: "rewritten follow-L component assertions + updated clamp copy pins"
    - path: "src/app/factsheet/[id]/v2/leverage-context.test.tsx"
      provides: "pruned to Test 1 (throws outside provider) + Test 6 (GUARD-04 source scan)"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "src/app/factsheet/[id]/v2/basis-context.tsx"
      via: "KpiStrip m = view.strategyMetrics (the levered view)"
      pattern: "view\\.strategyMetrics"
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "src/lib/leverage.ts"
      via: "MAX_LEVERAGE interpolation in clamp copy (never a hard-coded numeral)"
      pattern: "MAX_LEVERAGE"
---

<objective>
Rewire the two consumers of leverage-scaled values onto the levered view built in plan 01, DELETE the
entire MODELED/CAVEAT/α-IR-blanking/BASE·1× disclosure apparatus plus the two derived hooks, land the
reworded what-if copy (UI-SPEC verbatim), and widen leverage eligibility to the active resolved basis.

Purpose: this is the SC-3 delete (~780 LOC class) and the SC-1/SC-2 user-visible behavior — the whole
page follows L honestly, so the dishonesty disclosures have nothing left to disclose.
Output: rewired FactsheetView, provider-only leverage-context, rewritten/pruned test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/107-leverage-as-a-dailies-transform/107-CONTEXT.md
@.planning/phases/107-leverage-as-a-dailies-transform/107-PATTERNS.md
@.planning/phases/107-leverage-as-a-dailies-transform/107-UI-SPEC.md

<interfaces>
<!-- Verified against source 2026-07-15. -->

After plan 01, `useBasisSeriesView(payload)` returns the FULLY levered view (basis-merged then
r→L·r re-derived at applied L≠1; base by reference otherwise). KpiStrip already calls it (:791).

Current KpiStrip reads (FactsheetView.tsx):
- :783 `const { basis, m, modeled, appliedLeverage } = useLeveragedMetrics(payload);`  ← swap target
- :791-792 `const view = useBasisSeriesView(payload); const j = view.comparators[cmpKey].joint;` ← already correct
- :829 `const suppressRelative = modeled || (basis === "mark_to_market" && !mtmBundlePresent);`
- :767-768 LEVERAGE_CAVEAT const; :875-887 `{modeled && (...)}` amber eyebrow + caveat JSX
- :863-867 composite BASIS eyebrow uses `basis` — replace source with `useBasisOrCash()` (basis-context.tsx:63, exported)

MetricsColumnWithBasis (:326-384): `const { modeled } = useModeledLeverage(payload);` (:337),
BASE·1× block (:365-372), and the `if (!composite)` branch shape (:361-384) — see D4 no-orphan rule.

ControlBar (:1174-1175): `const leverageEligible = !composite && payload.periodsPerYear != null && basis === "cash_settlement";`
Clamp messages: :1185 (non-finite — KEEP), :1191 (negative — REWORD), :1194 (over-max — REWORD).
Input title/aria (:1233-1234) contain no "modeled" — KEEP unchanged per UI-SPEC.

leverage-context.tsx: KEEP :1-58 (imports it still needs + LeverageContext [exported in plan 01] +
LeverageProvider + useLeverage). DELETE :60-168 (useModeledLeverage + useLeveragedMetrics + doc
blocks) and the then-orphaned imports (compute, useBasisMetrics/useBasis from basis-context,
ComputeSummary, FactsheetPayload if unused, sanitizeLeverage if unused after the delete).

UI-SPEC verbatim strings (interpolate MAX_LEVERAGE from src/lib/leverage.ts, {L} = applied leverage):
- Disclosure caption (replaces LEVERAGE_CAVEAT; renders at applied L≠1 only; muted `text-caption
  text-text-muted`; `role="status" aria-live="polite"`; NO amber, NO eyebrow):
  `What-if projection at {L}× leverage: daily returns are scaled r → L·r and the whole factsheet re-derives. Excludes borrow, funding, and liquidation cost — not the strategy's realized track record.`
- Negative clamp: `Leverage can't be negative — shorting isn't included in this what-if. Clamped to 0.`
- Over-max clamp: `Leverage clamped to {MAX_LEVERAGE}× — the maximum in this what-if projection.`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: KpiStrip swap + disclosure delete + reworded copy + eligibility widen</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx:755-895 (KpiStrip + caveat/eyebrow), :1160-1270 (ControlBar cluster)
    - src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx (full, 429 lines) — the blocks map: KEEP :196-247 (eligibility, WI-1, L=1 baseline) + :357-421 (L-1 empty-field, clamp copy); REWRITE :249-356 (MODELED, WR-01 α/IR suppression, M-3 BASE·1×)
    - 107-UI-SPEC.md §Copywriting + §Deletion Contract (D1–D3) + §State Matrix — verbatim strings and no-orphan rules
  </read_first>
  <behavior>
    Rewrite FactsheetView.leverage.test.tsx per the block map:
    - L=1 baseline (:239-247, extended): view contains NO "MODELED", NO "What-if projection", NO
      "Reset 1×"; KPI cell text byte-identical to a never-touched render (SC-4 component pin).
    - Follow-L block (replaces :249-297 MODELED): at L=2 the Ann. Vol strip cell shows ~2× the base
      value AND the right-rail vol/§I scalars follow (rail == strip source, both from the view);
      the muted what-if caption renders with the verbatim reworded string and
      role="status" aria-live="polite"; NO amber MODELED eyebrow exists; Reset 1× restores the L=1
      byte-identical render.
    - Honest α/IR (inverts :298-327 WR-01): at L=2 with a comparator active, the α cell shows a real
      signed percent (≈ 2× the L=1 α within display rounding), never "—"; "—" still occurs for the
      surviving MTM-bundle-absent reason (keep that assertion).
    - Eligibility widen (updates :196-221 + new case): input renders on single-key cash (kept);
      NEW — input renders under basis=mark_to_market WITH a bundle; input does NOT render under
      MTM WITHOUT a bundle, on composites, or without periodsPerYear.
    - Clamp copy (:375-421): update the two reworded string pins verbatim; non-finite pin unchanged;
      valid-value-clears pin unchanged.
  </behavior>
  <action>
    In FactsheetView.tsx, referencing the CONTEXT "Copy" decision and UI-SPEC deletion contract:
    1. KpiStrip: delete the `useLeveragedMetrics` call (:783). Source the strip from the view that
       :791 already computes: `const m = view.strategyMetrics;` and `basis` from `useBasisOrCash()`.
       Keep `j = view.comparators[cmpKey].joint` (:792) — at L≠1 it is now the honest levered joint.
       Then remove `useLeveragedMetrics` from the FactsheetView.tsx import line (:13) + its :11 comment —
       this call at :783 was its only use, so the import is orphaned once deleted (required for the
       acceptance grep below). KEEP `useModeledLeverage` in the import (MetricsColumnWithBasis still
       uses it — removed in Task 2) and `LeverageProvider, useLeverage`.
    2. D3: `suppressRelative` (:829) drops the `modeled` disjunct → keep ONLY
       `basis === "mark_to_market" && !mtmBundlePresent` (orthogonal basis concern).
    3. D1+D2: delete LEVERAGE_CAVEAT (:767-768) and the entire `{modeled && (...)}` block (:875-887).
       In its place render the reworded what-if caption (verbatim string from <interfaces>) when the
       view actually applied leverage: compute `const appliedL = sanitizeLeverage(leverage, { signal: false })`
       from `useLeverage()` and gate on `appliedL !== 1 && !composite && payload.periodsPerYear != null
       && !(basis === "mark_to_market" && !mtmBundlePresent)` — the EXACT mirror of the plan-01 view
       guards, so the caption can never claim a what-if the view did not apply. Muted caption classes
       (`mt-6 text-caption text-text-muted`), role="status" aria-live="polite", INSERTED not reserved
       (no blank line at L=1). No amber. Import sanitizeLeverage if not already imported.
    4. Eligibility (:1174-1175): drop the `basis === "cash_settlement"` clause; ADD the resolved-basis
       clause: `!composite && payload.periodsPerYear != null && !(basis === "mark_to_market" && payload.seriesByBasis?.mark_to_market == null)`.
       Rewrite the stale :1164-1173 comment (leverage now levers the ACTIVE resolved basis — the old
       cash-only rationale died with the bespoke recompute; hidden only when MTM is displayed
       unresolved, mirroring the view's no-fabrication guard).
    5. Clamp copy: reword :1191 and :1194 to the verbatim UI-SPEC strings (over-max keeps the
       `${MAX_LEVERAGE}` interpolation — never hard-code 10). :1185 non-finite and :1233-1234
       title/aria stay byte-identical.
    6. Delete the now-stale WR-01/LEV-01 "stays on the base 1× track" comment prose around :762-766,
       :779-782, :818-828 (the comments describe deleted behavior). Do not touch unrelated comments.
    7. Rewrite FactsheetView.leverage.test.tsx per the behavior block. Note: at L≠1 the component
       tests now execute the full deriveSeriesBundle (incl. bootstrapCI 2000 resamples) on the test
       fixture — keep fixtures ≤ ~60 days so the suite stays fast.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useLeveragedMetrics\|LEVERAGE_CAVEAT\|MODELED" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` == 0 (comments included)
    - `grep -c "What-if projection at" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` == 1
    - `grep -c 'basis === "cash_settlement"' "src/app/factsheet/[id]/v2/FactsheetView.tsx"` returns 0 for the leverageEligible expression (the eyebrow ternary at :866 reads basis for LABELS and may keep its string)
    - Over-max clamp message interpolates `${MAX_LEVERAGE}` (grep confirms `MAX_LEVERAGE}×` in the string; no literal `10×` numeral in either reworded message)
    - Test file pins the disclosure + both clamp strings verbatim (exact-match assertions, not regex-loose)
    - α cell at L=2 asserts a real value ≈ 2× the L=1 α at display precision — falsifiable against re-blanking
  </acceptance_criteria>
  <done>Strip + α/IR + caption + eligibility + copy all follow the levered view; rewritten leverage component suite green; tsc clean. (useModeledLeverage still imported by MetricsColumnWithBasis — removed in Task 2.)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete the BASE·1× rail eyebrow (D4 no-orphan) + rewrite the LEV-MTM-1 pin</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx:326-395 — MetricsColumnWithBasis (the `modeled` branch + the `if (!composite)` return shape)
    - src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx:260-345 — fixture (g) + the LEV-MTM-1 test (:324-337) that PINS THE OLD cash-only behavior and must be inverted
    - 107-UI-SPEC.md §Deletion Contract D4 (no-orphan rule)
  </read_first>
  <behavior>
    - Rail: no `metricscolumn-base-track-eyebrow` testid exists anywhere; at L≠1 the rail renders
      levered values (it reads MetricsColumn → view.strategyMetrics, which plan 01 levered) with NO
      eyebrow; a plain single-key non-MTM-participant render returns the bare MetricsColumn DOM
      (no wrapper div — byte-identical to pre-Phase-90.5).
    - LEV-MTM-1 REWRITTEN (same fixture (g)): leverage=2 under basis=mark_to_market WITH a bundle now
      shows the LEVERED MTM scalars (strip vol ≈ 2× the persisted MTM vol at display precision) and
      the what-if caption — the honest re-derive replaced the old no-fabrication suppression. Keep a
      companion assertion: under MTM WITHOUT a bundle at leverage=2, values stay the unlevered cash
      fallback (the view's no-fabrication guard, D3 discipline).
  </behavior>
  <action>
    1. MetricsColumnWithBasis: delete the `useModeledLeverage` read (:337) + its round-3 comment
       block (:329-336) + the `{modeled && (...)}` BASE·1× JSX (:365-372) + `modeled` from the branch
       condition (:362). D4 no-orphan: with only `mtmParticipant` left, the `if (!composite)` path
       returns the bare `<MetricsColumn scenarioMode={scenarioMode} />` when `!mtmParticipant`, and
       keeps the wrapping `flex flex-col gap-4` div ONLY for the surviving mtmParticipant eyebrow
       (:373-380, untouched). Update the :352-360 comment (the BASE·1× rationale is dead — the rail
       levers with everything else).
    2. Remove `useModeledLeverage` from the FactsheetView.tsx import line (:13) — after step 1 this
       is its last use (`useLeveragedMetrics` was already dropped from the import in Task 1). End state:
       the import lists only `LeverageProvider, useLeverage`.
    3. Rewrite FactsheetBody.basis.test.tsx LEV-MTM-1 (:324-337) per the behavior block; update the
       fixture-(g) comment (:269-270) — the leverage input now renders on the RESOLVED MTM basis too.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx" "src/app/factsheet/[id]/v2/FactsheetView.leverage.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useModeledLeverage\|base-track-eyebrow\|BASE · 1× TRACK" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` == 0
    - FactsheetView.tsx :13 import line lists only `LeverageProvider, useLeverage` from "./leverage-context"
    - Rewritten LEV-MTM-1 asserts levered MTM scalars at L=2 WITH bundle AND unlevered fallback WITHOUT bundle
    - No empty wrapper div renders for a plain single-key strategy (DOM snapshot/structure assertion)
  </acceptance_criteria>
  <done>Rail eyebrow gone with no orphaned gap; FactsheetView no longer imports the derived hooks; MTM-under-leverage honestly pinned both ways; suites + tsc green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delete useLeveragedMetrics/useModeledLeverage; prune leverage-context.test.tsx</name>
  <files>src/app/factsheet/[id]/v2/leverage-context.tsx, src/app/factsheet/[id]/v2/leverage-context.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/leverage-context.tsx (full, 168 lines) — KEEP :1-58 minus orphaned imports; DELETE :60-168
    - src/app/factsheet/[id]/v2/leverage-context.test.tsx (full, 210 lines) — KEEP Test 1 (:91) + Test 6 (:191, GUARD-04 source scan); DELETE Tests 2/3/4/5/7 (:95-190)
  </read_first>
  <behavior>
    - useLeverage still throws outside its provider (Test 1, kept).
    - GUARD-04: leverage-context.tsx source has no storage/URL/cookie/history access (Test 6, kept,
      still green against the shrunken file).
    - Deleted-hook tests (2/3/4/5/7) removed — their behavior now lives in
      basis-context.leverage.test.tsx (plan 01) and the rewritten component suites.
  </behavior>
  <action>
    1. leverage-context.tsx: delete useModeledLeverage (:60-92 incl. doc block) and
       useLeveragedMetrics (:94-168 incl. doc block). This is the SC-5 kill — the standalone
       `compute(payload.strategyReturns.map(r => appliedLeverage * r), ...)` at :154-159 is the
       second leverage compute path being retired. Remove the then-orphaned imports (compute,
       useBasisMetrics/useBasis from "./basis-context", ComputeSummary, FactsheetPayload,
       sanitizeLeverage — whichever have zero remaining references; `LeverageContext` stays exported,
       LeverageProvider/useLeverage stay verbatim). Removing the basis-context import also dissolves
       the transient module cycle noted in plan 01. Update the file header doc to "slider state only".
    2. leverage-context.test.tsx: delete Tests 2/3/4/5/7 and their now-unused fixtures/imports; keep
       Test 1 + Test 6 byte-equivalent (Test 6's regex scan runs against the shrunken source).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/leverage-context.test.tsx" --no-file-parallelism && npx tsc --noEmit && npm run lint && npx vitest run "src/app/factsheet/[id]/v2" src/lib/factsheet --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v "^\s*//" "src/app/factsheet/[id]/v2/leverage-context.tsx" | grep -c "useLeveragedMetrics\|useModeledLeverage\|compute("` == 0
    - `grep -c "LeverageProvider\|export const LeverageContext\|export function useLeverage" "src/app/factsheet/[id]/v2/leverage-context.tsx"` >= 3 (keep-list intact)
    - Test 6 GUARD-04 source scan green (no storage/URL/cookie/history in leverage-context.tsx)
    - Full factsheet-v2 + lib/factsheet directory suites green (wave gate)
    - `git diff --exit-code -- src/lib/scenario.ts src/lib/leverage.ts` passes
  </acceptance_criteria>
  <done>leverage-context.tsx is provider/state only; pruned test file green; whole factsheet directory suite green; scenario.ts/leverage.ts byte-untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser input → leverage state | ControlBar number field — the only untrusted input; no persistence, no server call (GUARD-04). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | onLeverageChange input clamp | mitigate | KEEP the existing clamp logic byte-identical except the two reworded message strings (ASVS V5); clamp-copy tests pin non-finite/negative/over-max paths. |
| T-107-02 | Repudiation/Integrity | what-if caption vs applied state | mitigate | Caption gate mirrors the view-layer guards exactly — it can never announce a what-if the view did not apply (and vice versa). |
| T-107-04 | Information Disclosure | GUARD-04 leverage persistence | mitigate | leverage-context.test.tsx Test 6 source scan kept green; no storage/URL/history code added. |
| T-107-SC | Tampering | package installs | accept | No installs this phase. |
</threat_model>

<verification>
- `npx vitest run "src/app/factsheet/[id]/v2" src/lib/factsheet --no-file-parallelism` green (wave gate)
- `npx tsc --noEmit` + `npm run lint` clean
- `git diff --exit-code -- src/lib/factsheet/__snapshots__/build-payload.test.ts.snap src/lib/scenario.ts src/lib/leverage.ts` passes
</verification>

<success_criteria>
- SC-1/SC-2 user-visible: strip + charts + rail + α/β/IR all follow L honestly at L≠1
- SC-3: disclosure apparatus + derived hooks deleted; slider state + ControlBar cluster kept; reworded UI-SPEC copy landed verbatim
- SC-4: L=1 component-level byte-identity pinned; snapshot untouched
- Report the actual deleted/rewritten LOC (git diff --stat) in the SUMMARY against the ~780 target
</success_criteria>

<output>
Create `.planning/phases/107-leverage-as-a-dailies-transform/107-02-SUMMARY.md` when done.
</output>
