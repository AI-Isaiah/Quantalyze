---
phase: 98-portfolio-data-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/routers/portfolio.py
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_portfolio_compute_integration.py
autonomous: true
requirements: [PI-07]

must_haves:
  truths:
    - "Once the PI-07 UNIQUE index exists, the losing racer's `computing` INSERT (23505) surfaces as the EXISTING 409 semantics on the public route — never a 500"
    - "The cron recompute path routes a 23505-losing portfolio into the EXISTING `in_flight` outcome bucket — never `failed`"
    - "Non-23505 insert errors still fail loud (no broadened swallow)"
  artifacts:
    - path: "analytics-service/routers/portfolio.py"
      provides: "23505 detection around the computing-row INSERT in _compute_portfolio_analytics, raising HTTPException 409"
      contains: "23505"
    - path: "analytics-service/routers/cron.py"
      provides: "_guarded_recompute 409 -> in_flight branch"
      contains: "in_flight"
    - path: "analytics-service/tests/test_portfolio_compute_integration.py"
      provides: "Failing-first tests for the 23505->409 and 409->in_flight branches"
      contains: "23505"
  key_links:
    - from: "analytics-service/routers/portfolio.py _compute_portfolio_analytics INSERT"
      to: "HTTPException(status_code=409)"
      via: "except branch detecting code=='23505' (single choke point, both callers route through it)"
      pattern: "409"
    - from: "analytics-service/routers/cron.py _guarded_recompute except HTTPException"
      to: "(pid, \"in_flight\", None)"
      via: "status_code == 409 branch BEFORE the 400-skip branch"
      pattern: "in_flight"
---

<objective>
PI-07, code side (co-requisite of plan 98-01): once the partial UNIQUE index exists, the losing racer's `computing` INSERT at `portfolio.py:646-648` raises a PostgREST error with code `23505`. Today that would surface as an unhandled 500. Catch it at the INSERT and convert to the EXISTING 409 semantics; route the cron caller into the EXISTING `in_flight` bucket.

Purpose: the whole point of PI-07 is that the losing racer is handled gracefully. A migration without this change turns a silent duplicate into a loud 500 (research Pattern 2: "load-bearing").
Output: guarded INSERT in `portfolio.py`, 409->in_flight branch in `cron.py`, failing-first pytest cases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/98-portfolio-data-foundation/98-RESEARCH.md

<decisions_locked_by_planner>
- **D-P6 (single choke point):** catch the unique violation AT the INSERT inside `_compute_portfolio_analytics` (portfolio.py:646-648) and raise `HTTPException(status_code=409, detail="Analytics computation already in progress for this portfolio")` — byte-identical detail string to the existing in-flight 409 at portfolio.py:1561-1564. The public POST route then propagates 409 with zero route changes; only `cron.py` needs a new branch.
- **23505 detection pattern:** reuse the repo's OWN established detection at portfolio.py:1509-1511: `code = getattr(exc, "code", None)`, `msg = str(exc)`, branch on `code == "23505" or "23505" in msg or "duplicate key" in msg.lower()`. Research Pitfall 4: supabase-py raises a PostgREST `APIError` with `.code`, NOT a psycopg exception — do not import psycopg error classes.
- **cron branch placement:** in `_guarded_recompute`'s existing `except HTTPException as http_exc:` (cron.py:937), add the 409 case BEFORE the 400-skip check: `if http_exc.status_code == 409: logger.info(...in-flight...); return (pid, "in_flight", None)`. The `in_flight` bucket and its counter already exist (cron.py:921, 971-973) — reuse, do not add a new status.
- Everything else in the except chain stays byte-identical: 400 -> skipped, other HTTPException -> failed, TimeoutError -> failed. Do NOT broaden any swallow — a non-23505 insert error must still propagate (Rule 12 fail-loud).
</decisions_locked_by_planner>

<interfaces>
From analytics-service/routers/portfolio.py (current INSERT, lines 646-651):
```python
insert_result = supabase.table("portfolio_analytics").insert(
    {"portfolio_id": portfolio_id, "computation_status": ComputationStatus.COMPUTING.value}
).execute()

if not insert_result.data:
    raise HTTPException(status_code=500, detail="Failed to create analytics row")
```

From analytics-service/routers/portfolio.py:1509-1511 (the repo's 23505 detection to reuse):
```python
code = getattr(exc, "code", None)
msg = str(exc)
if code == "23505" or "23505" in msg or "duplicate key" in msg.lower():
```

From analytics-service/routers/cron.py:937-954 (the except chain to extend — 409 branch goes FIRST inside it):
```python
except HTTPException as http_exc:
    if http_exc.status_code == 400:
        ...
        return (pid, "skipped", None)
    logger.exception(...)
    return (pid, "failed", f"HTTPException {http_exc.status_code}: {http_exc.detail}")
```

Return type contract: `_guarded_recompute(pid) -> tuple[str, RecomputeStatus, str | None]`; `"in_flight"` is already a member of `RecomputeStatus`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Failing-first tests for 23505->409 (public) and 409->in_flight (cron)</name>
  <files>analytics-service/tests/test_portfolio_compute_integration.py</files>
  <read_first>
    - analytics-service/tests/test_portfolio_compute_integration.py (the WHOLE file — its supabase mocking convention, fixtures, and how _compute_portfolio_analytics is exercised; extend, do not fork a new style)
    - analytics-service/routers/portfolio.py:632-663 (the INSERT under test)
    - analytics-service/routers/cron.py:891-961 (_guarded_recompute and its except chain)
  </read_first>
  <behavior>
    - Test A (RED first): the `portfolio_analytics` INSERT inside `_compute_portfolio_analytics` raises an exception object with attribute `code == "23505"` (build a stub exception mirroring postgrest APIError — set `.code` and a "duplicate key value violates unique constraint \"portfolio_analytics_one_computing_per_portfolio\"" message; do NOT import postgrest just to construct it if the existing test file stubs errors differently — follow the file's convention). Assert `_compute_portfolio_analytics` raises `HTTPException` with `status_code == 409` and detail EXACTLY "Analytics computation already in progress for this portfolio". Without the fix this currently propagates the raw exception (test RED).
    - Test B (RED first): same 23505 stub, but assert the message-only fallback also maps: exception with `code=None` and str containing "duplicate key" -> 409.
    - Test C (RED first): a NON-23505 insert exception (e.g. code "42501" / RLS-style message) still propagates unchanged from `_compute_portfolio_analytics` (NOT converted to 409, NOT swallowed) — this pins the narrow-swallow contract.
    - Test D (RED first): `cron._guarded_recompute`-level: with `_compute_portfolio_analytics` patched to raise `HTTPException(409, ...)` and the in-flight pre-SELECT returning empty, the outcome tuple is `(pid, "in_flight", None)` — currently it lands in the `failed` bucket (test RED). Follow the lazy-import/patching approach the existing cron tests use (see test_cron_router.py TestPortfolioRecomputeErrorIsolation note referenced at cron.py:881-883).
    - Run the file and RECORD that A, B, D fail and C's baseline passes before implementation (RED evidence in the summary).
  </behavior>
  <action>
    Add the four test cases to `analytics-service/tests/test_portfolio_compute_integration.py` in the file's existing style (class/fixture conventions, pinned `.venv/bin/python` interpreter per 98-VALIDATION.md). Name them so the PI-07 linkage is greppable (e.g. a class or test-name prefix containing `pi07` or `23505`). Verify RED: `cd analytics-service && .venv/bin/python -m pytest tests/test_portfolio_compute_integration.py -x -q` must show the new tests failing for the RIGHT reason (unhandled stub exception / wrong bucket), not collection errors.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_portfolio_compute_integration.py -q --collect-only 2>&1 | grep -ci "23505\|pi07" | awk '$1>=1{print "TESTS-PRESENT OK"}'</automated>
  </verify>
  <acceptance_criteria>
    - Four new tests exist covering: code-attr 23505 -> 409; message-fallback 23505 -> 409; non-23505 propagates unchanged; cron 409 -> ("in_flight", None) tuple
    - The 409 detail string asserted is byte-identical to the existing route 409 (portfolio.py:1561-1564)
    - New tests FAIL before Task 2 (RED observed and recorded in summary); pre-existing tests in the file still pass
    - No production code touched in this task
  </acceptance_criteria>
  <done>RED test suite pinning the full 23505 contract exists and its failure was observed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 23505->409 at the INSERT + cron 409->in_flight branch</name>
  <files>analytics-service/routers/portfolio.py, analytics-service/routers/cron.py</files>
  <read_first>
    - analytics-service/routers/portfolio.py:632-663 (the INSERT to wrap) and :1503-1517 (the detection pattern to mirror) and :1552-1564 (the 409 whose semantics are being matched)
    - analytics-service/routers/cron.py:891-961 (the except chain to extend)
    - .planning/phases/98-portfolio-data-foundation/98-02-PLAN.md `<decisions_locked_by_planner>` (D-P6 — the design is locked, implement exactly)
  </read_first>
  <action>
    In `portfolio.py` `_compute_portfolio_analytics`: wrap ONLY the `computing`-row INSERT (lines 646-648) in try/except Exception. In the except: apply the portfolio.py:1509-1511 detection (`getattr(exc,"code",None)` + msg fallbacks); on 23505 log at info level (this is the fence WORKING, not an error — include portfolio_id) and raise `HTTPException(status_code=409, detail="Analytics computation already in progress for this portfolio")`; otherwise bare `raise` (preserve the original traceback). Leave the `if not insert_result.data: ... 500` check untouched after the try/except. Add a short comment naming PI-07 + the index `portfolio_analytics_one_computing_per_portfolio` so the coupling is greppable from either side.

    In `cron.py` `_guarded_recompute`, inside the existing `except HTTPException as http_exc:` block and BEFORE the `status_code == 400` check: on `status_code == 409` log info ("portfolio %s lost the computing-row race (PI-07 fence), in-flight elsewhere") and `return (pid, "in_flight", None)`. Touch nothing else in the chain (surgical change; the 400/skipped, failed, Timeout branches stay byte-identical).

    Then run the Task-1 tests GREEN, plus the full touched suites.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_portfolio_compute_integration.py tests/test_cron_router.py -q</automated>
  </verify>
  <acceptance_criteria>
    - All Task-1 tests pass; zero previously-passing tests in test_portfolio_compute_integration.py or test_cron_router.py regress
    - portfolio.py: the try/except covers ONLY the computing INSERT; non-23505 re-raised bare; 409 detail byte-identical to :1561-1564
    - cron.py: 409 branch precedes the 400 branch inside the existing except HTTPException; returns the existing "in_flight" bucket; no new status literal introduced
    - `grep -n "23505" analytics-service/routers/portfolio.py` shows the new detection at the _compute_portfolio_analytics INSERT (in addition to the pre-existing :1511 site)
    - No changes outside the two functions named (surgical-change rule)
  </acceptance_criteria>
  <done>Losing racer gets 409 (public) / in_flight (cron); non-23505 errors unchanged; suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /api/portfolio-analytics | authed request racing cron/other instances |
| cron tick -> per-portfolio recompute | multi-instance workers race the same portfolio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-04 | DoS | losing racer 500s post-index | mitigate | 23505 -> HTTPException 409 at the single INSERT choke point |
| T-98-05 | Repudiation | silent misclassification of real insert failures as races | mitigate | narrow detection (code/msg per repo pattern); non-23505 bare re-raise; Test C pins it |
| T-98-06 | Information Disclosure | error detail leakage | mitigate | 409 detail is the existing fixed string; raw DB error only logged server-side |
| T-98-SC | Tampering | package installs | accept | zero new dependencies |
</threat_model>

<verification>
- RED->GREEN on the four new pytest cases; `pytest tests/test_portfolio_compute_integration.py tests/test_cron_router.py` fully green
- Behavior contract: 23505 (attr or message) -> 409/in_flight; anything else propagates
</verification>

<success_criteria>
- The PI-07 fence's losing side is gracefully absorbed into pre-existing 409/in_flight semantics with zero new statuses, zero broadened swallows
</success_criteria>

<output>
Create `.planning/phases/98-portfolio-data-foundation/98-02-SUMMARY.md` when done
</output>
