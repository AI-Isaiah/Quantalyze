# Phase 103 — combined 102+103 Fable red-team findings (2026-07-14)

**Phase 103 is NOT closable until F1-F5 are fixed.** All three HIGH trace to a FALSE premise in `103-04-PLAN.md:96` ("HistogramChart/MasterBrush get data via props from TimeSeriesChart") — they call `usePayload()` directly; the frozen-spine diff-zero guard masked it.

## HIGH (block phase close)
- **F1 — HistogramChart shows CASH distribution under MTM.** `HistogramChart.tsx:40,65,85` reads `usePayload()`/`payload.strategyReturns`, never the view. Renders directly above the MTM-following QuantileBoxPlot (`FactsheetView.tsx:230`). Fix: route through the view — requires UNFREEZING HistogramChart (phase-52) or a prop seam.
- **F2 — MasterBrush cash-bound.** `MasterBrush.tsx:43,60-61,198-199` uses `payload.strategyEquity`/`payload.dates`. (1) cash sparkline under MTM; (2) xRange ordinal indices on cash axis applied to MTM axis (`TimeSeriesChart.tsx:196-198`) → brush labels window DIFFERENT MTM dates; (3) `factsheet-context.tsx:198-221` fullRange/clamp sized to `payload.dates.length` (cash) → if MTM axis longer, recent MTM days PERMANENTLY unreachable (FIX-E "transient" claim is WRONG — reset is cash-sized). Fix: route through view (unfreeze MasterBrush).
- **F3 — KpiStrip↔rail scalar parity breaks by construction.** KpiStrip = persisted Python scalars (dense 0.0-fill, `basis_series.py:132-139`); rail = TS `compute()` on SPARSE rows (no client gap-fill). Composite/gappy MTM: TS Sharpe ≈ Python/√(N/D) → ~5% divergence on the same page. Also `cumulative_method="simple"` (Zavara): Python arithmetic Σr + shifted CAGR (`metrics.py:501-576`) vs TS geometric-only (`compute.ts:38`). Keystone test FAKED parity (`FactsheetBody.basis.test.tsx:487-501` forces bundle scalars = persisted). **FIX (recommended): the rail scalar tables read the PERSISTED MTM scalar overlay (extend `overlayBasisScalars` to the full scalar set — the backend already derived them from the dailies with correct conventions), NOT a TS recompute — so KpiStrip==rail by construction and both arithmetic + sparse are handled. Keep series-derived panels (quantiles/streaks/calmar/bootstrap/stress) from the TS bundle. Add a REAL cross-runtime parity test on a gappy + arithmetic fixture (persisted Python scalar == what the rail displays).** Confirm the persisted `metrics_json_by_basis.mark_to_market` carries every field the rail reads; if any (MTD/YTD/best-worst/yearly) is absent, decide backend-add vs keep-TS-with-dense-gap-fill.

## MEDIUM
- **F4 (MED-HIGH) — fallback mislabels cash as MTM.** Rail eyebrow (`FactsheetView.tsx:360-367,385-392`) gated on basis only, NOT on `seriesByBasis.mark_to_market` presence (unlike the caption `:428,513-519`). Bundle-absent (Zavara pre-backfill / stale v5 cache / degraded read) → `m=view.strategyMetrics` is CASH while eyebrow says MARK-TO-MARKET. Fix: gate the rail eyebrow on bundle presence like the caption.
- **F5 (MED) — KpiStrip α/IR suppressed to "—" under MTM while §IV shows bundle MTM α/β/IR** (`FactsheetView.tsx:794-816` comment now FALSE). Fix: strip reads the view's joint, or a deliberate documented decision.
- **F6 (MED-LOW) — API-only strategy panels stay cash** under MTM (PeerPercentile ranks cash Sharpe `build-payload.ts:528`/`BatchDPanels.tsx:82`, AllocatorSection, Signatures), no basis note. Declared-scope tension — decide label vs carve-out.

## LOW
- **F7 — cache key not bumped** for required `bootstrapCI.n` (`page.tsx:251` still `v5`) → stale entries suppress low-N warning (incl. cash) for ≤1h. Bump the shape-version.
- **F8 — KpiStrip low-N caveat reads cash `n` under MTM** (`FactsheetView.tsx:904`).
- **F9 — composite partial-write window REVERSED not eliminated** (`job_worker.py:4561-4583`): series-before-scalar → transient headline-fail leaves fresh-series+stale-scalar. Self-healing; fix the comment's framing.
- **F10 — stale docs:** `types.ts seriesByBasis` docstring still says correlations NOT in bundle (contradicted 20 lines above); `FactsheetView.tsx:794-797` α/IR comment.

## ORCHESTRATOR DECISIONS (2026-07-14 — resolves the two fixer blockers)

**F1 — DONE + committed `725f16c5`** (HistogramChart routed through view; HistogramChart + MasterBrush unfrozen in the phase-52 guard; neuter-confirmed). MasterBrush.tsx itself still unedited (the guard unfreeze only PERMITS the F2 edit).

**Blocker 1 → F2(3): UNFREEZE `factsheet-context.tsx`.** Sizing the `fullRange`/`setXRange` clamp to the ACTIVE-basis axis length is a real correctness fix — if the MTM axis is longer than cash, recent MTM days are PERMANENTLY unreachable (not transient). Unfreeze the island in `phase-52-frozen-spine-guards.test.ts` with a guard comment citing the MTM-axis-length requirement + the diff-zero-masking root cause (same rationale as the F1 HistogramChart/MasterBrush unfreeze). MANDATORY: a neuter-confirmed test proving byte-identity/SC-4 STILL holds under cash (under cash the clamp must remain `payload.dates`-sized = today's behavior; only under MTM-with-bundle does it size to the bundle axis).

**Blocker 2 → F3: PURE-FRONTEND, NO backend change. Field-coverage audit COMPLETE (orchestrator, against `basis_series.py` + `metrics.py:797+` + `MetricsColumn.tsx` + `basis-metrics.ts`).**
- The ONLY fields shown on BOTH KpiStrip and the rail are the SEVEN `BASIS_KPI_MAP` headline scalars (cum_ret/cagr/ann_vol/sharpe/sortino/calmar/max_dd). KpiStrip (`useBasisMetrics`) overlays them from persisted; the rail (`useBasisSeriesView().strategyMetrics`) does NOT → the divergence.
- The persisted `metrics_json_by_basis.mark_to_market` CARRIES all seven (present, correct server keys). It does NOT carry the rail-only extended/series-derived fields (best_week/best_quarter/best_year, `yearly`, p6m/p1y, quantiles, rolling, equity, worst10) — but those appear ONLY on the rail, computed from the ONE TS bundle on the MTM series, internally consistent, with NO cross-surface counterpart on KpiStrip. So they have no parity to break and need NOT come from persisted.
- **FIX: in `useBasisSeriesView`, after the `{...payload, ...bundle}` merge, ALSO overlay the seven persisted headline scalars onto the merged `strategyMetrics` — i.e. `strategyMetrics: overlayBasisScalars(bundle.strategyMetrics, payload.metricsByBasis?.mark_to_market ?? {})` (mirror `useBasisMetrics`).** Then the rail's `m` for the seven == KpiStrip's `m` for the seven BY CONSTRUCTION (both = persisted-dense-Python), killing both the sparse-vs-dense AND the arithmetic-vs-geometric (Zavara `simple`) divergence for every cross-surface scalar. Extended/series-derived rail metrics STAY TS-bundle-derived (this is exactly how the rail already works for CASH — only the seven have a persisted authoritative cache; the rest are always client-computed).
- The red-team's "extend `overlayBasisScalars` to the full scalar set" is OVER-REACH: the full set isn't persisted and needn't be. Overlay ONLY the seven (the existing `BASIS_KPI_MAP`), in `useBasisSeriesView`.
- **DELETE the FAKED keystone** (`FactsheetBody.basis.test.tsx:487-501` forcing bundle scalars = persisted). Replace with a REAL cross-runtime parity test on a gappy + arithmetic (`cumulative_method:"simple"`) MTM fixture: assert the rail's DISPLAYED seven == the persisted Python seven (i.e. the overlay wins, and a bundle-TS-recompute value that differs would FAIL the test if the overlay were removed — neuter-confirm).
- OUT OF SCOPE (pre-existing, NOT a Phase-103 regression, note only): under `simple`/arithmetic the rail's p3m/p6m/p1y period returns are geometric while the headline cum_ret is arithmetic — a within-rail modeling choice that predates Phase 103 and is not cross-surface. Do NOT chase it here.

**Fixer model = opus. Sole owner, clean tree at start, NO git branch ops (only `git add <explicit paths>`, never `-A`/`.planning/`). Resume/verify: tsc + lint + the touched vitest files + neuter-confirm each fix.**

## Clean (verified no finding)
Correlations axis alignment, §IV joint MTM-regression, SC-4 under the root-cause flip (view===payload by ref under cash), backend seams (heal DELETE, anchor-race label, persist matrix, F-4 gate), PDF/tearsheet (fresh render → cash default + correct eyebrow).
