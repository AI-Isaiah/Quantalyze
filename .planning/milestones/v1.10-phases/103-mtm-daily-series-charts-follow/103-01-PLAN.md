---
phase: 103-mtm-daily-series-charts-follow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/basis_series.py
  - analytics-service/tests/test_basis_series.py
  - .planning/phases/103-mtm-daily-series-charts-follow/103-PROBE-OQ1.md
autonomous: true
requirements: [MTM-04]
must_haves:
  truths:
    - "derive_basis_series returns scalars that are re-derivable BY CONSTRUCTION from the sparse rows it emits (the anti-Phase-101 guard is a round-trip through ONE function)"
    - "The per-basis coverage mask (gap_spans) is derived FROM the series, computed once in Python, and persisted atomically inside the same JSONB payload"
    - "The OQ1 sparsity question (interior-sparse vs whole-or-degrade single-key MTM) is answered from source and documented"
  artifacts:
    - path: "analytics-service/services/basis_series.py"
      provides: "Shared dailies-canonical derive: series → scalars + sparse rows + gap spans; persist/heal to strategy_analytics_series"
      min_lines: 80
    - path: "analytics-service/tests/test_basis_series.py"
      provides: "Round-trip anti-divergence guard (MTM + cash-shaped), gap-span derivation, NaN semantics, persist/heal"
      min_lines: 120
    - path: ".planning/phases/103-mtm-daily-series-charts-follow/103-PROBE-OQ1.md"
      provides: "OQ1 answer: can single-key mtm_returns / composite stitched-MTM carry interior honest-absent days distinct from cash?"
  key_links:
    - from: "analytics-service/services/basis_series.py"
      to: "services/metrics.py compute_all_metrics + services/broker_dailies.py gap_fill_daily_returns"
      via: "imports — the helper composes existing primitives, NO new math"
      pattern: "from services\\.(metrics|broker_dailies) import"
    - from: "analytics-service/tests/test_basis_series.py"
      to: "derive_basis_series"
      via: "round-trip: rebuild Series from result.series_rows → gap_fill → compute_all_metrics == result.metrics_json"
      pattern: "gap_fill_daily_returns"
---

<objective>
Create the DURABLE shared route of the backbone-unification arc: `services/basis_series.py::derive_basis_series` — "take an already-computed daily-return series → emit the sparse honest persisted form + the scalar cache derived FROM that form + the coverage gap-spans derived FROM that form" — plus the persist/heal function targeting `strategy_analytics_series` kind `mtm_daily_returns`. Also resolve OQ1 (MTM interior sparsity) from source, first, because it gates what the coverage mask may honestly claim.

Purpose: This module IS the dailies-canonical route (LOCKED principle). Phases 104-106 ADOPT it for cash; Plan 103-02 wires both MTM derive sites to it. Getting the transform semantics right here makes the anti-divergence guard true by construction.
Output: `basis_series.py` + its test suite + `103-PROBE-OQ1.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-mtm-daily-series-charts-follow/103-RESEARCH.md
@.planning/phases/103-mtm-daily-series-charts-follow/103-CONTEXT.md
@analytics-service/services/broker_dailies.py
@analytics-service/services/stitch_composite.py

<interfaces>
<!-- Verified against source this session. Use directly — no exploration needed. -->

From analytics-service/services/metrics.py:
```python
def compute_all_metrics(
    returns: pd.Series,
    benchmark_returns: pd.Series | None = None,
    periods_per_year: int = DEFAULT_PERIODS_PER_YEAR,
    cumulative_method: str = "geometric",
    day_basis: str = "calendar",
) -> MetricsResult:  # carries .metrics_json (JSON-safe dict) + .sibling_kinds
# :197 def _drop_nonfinite(series: pd.Series) -> pd.Series:
#      returns series.replace([np.inf,-np.inf], np.nan).dropna() — THE sanitize helper
```

From analytics-service/services/broker_dailies.py:123:
```python
def gap_fill_daily_returns(returns: pd.Series) -> pd.Series:
    # Reindex to EVERY calendar day in [first, last], fill_value=0.0,
    # index unit pinned to "us". NOTE: reindex fills only ABSENT index
    # entries — pre-existing NaN VALUES survive it.
```

From analytics-service/services/stitch_composite.py:249:
```python
def _consecutive_spans(gap_days: list[pd.Timestamp]) -> list[dict[str, str]]:
    # ascending gap days → inclusive [{"start":"YYYY-MM-DD","end":"YYYY-MM-DD"}]
```

From supabase/migrations/20260428120919_strategy_analytics_series.sql:
- Table `strategy_analytics_series(strategy_id UUID FK CASCADE, kind TEXT, payload JSONB, computed_at, PK(strategy_id,kind))`
- `kind` is unconstrained TEXT; COMMENT: "Add a new kind = INSERT a new row; no ALTER TABLE"
- RLS deny-all; service-role bypasses
- RPC (service_role-only): `upsert_strategy_analytics_series_batch(p_strategy_id UUID, p_kinds JSONB /* {kind: payload} */)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OQ1 sparsity probe — answer "interior-sparse or whole-or-degrade?" from source</name>
  <files>.planning/phases/103-mtm-daily-series-charts-follow/103-PROBE-OQ1.md</files>
  <action>
Static source trace (no live crawl needed — the question is what the compute CAN produce):

1. Single-key: `combine_native_ledger` (broker_dailies.py:173) returns a GAP-FILLED dense daily Series ("a gap-filled float Series on an ascending daily DatetimeIndex" — verified docstring). Therefore single-key interior sparsity exists IFF the returned series can carry interior NaN VALUES (guard-refused days survive `gap_fill_daily_returns` reindex). Trace `reconstruct_native_nav_and_twr` and the NAV guards (negative_nav_guard / dust_nav_guard / flow_dominated_guard / unreadable-uPnL — see NAV_TWR_GUARD_KEYS in services/nav_twr.py) for the `pnl_basis=mark_to_market` pass: do any write np.nan into a day's return? Are any basis-dependent (i.e., can an MTM NaN day differ from the cash pass's NaN days)?
2. Composite: interior sparsity exists BY CONSTRUCTION for multi-member composites — `stitch_clipped_series(clipped_mtm)` (stitch_composite.py:217) concatenates member windows; days between non-contiguous member windows are ABSENT from the index. Confirm from `stitch_clipped_series` + `_metrics_result_for` (job_worker.py:4144-4165) that absent inter-member days reach the compute only via `gap_fill_daily_returns` 0.0 fill. Also confirm whether member-level NaN guard days survive into the stitched series.
3. Cross-check any existing test fixtures in tests/test_mtm_single_key.py + tests/test_stitch_composite*.py that construct MTM series with NaN/absent days — cite them.

Write `103-PROBE-OQ1.md` (Write tool, new file) with: (a) verdict per path — "single-key MTM interior sparsity: POSSIBLE via {guards} / IMPOSSIBLE (whole-or-degrade)"; "composite MTM interior sparsity: exists via inter-member gaps [+ NaN guards if applicable]"; (b) file:line evidence; (c) consequence for the mask claim: if single-key is whole-or-degrade, the single-key MTM mask is span-level only (empty interior mask) and the phase SUMMARY must say so explicitly — the user chose "full mask" assuming interior gaps exist. The mask CODE (Task 2) is sparsity-agnostic either way; this probe gates fixtures + claims, never invents sparsity (LOCKED: no new math).
  </action>
  <verify>
    <automated>test -s .planning/phases/103-mtm-daily-series-charts-follow/103-PROBE-OQ1.md && grep -c "verdict\|VERDICT\|POSSIBLE\|IMPOSSIBLE" .planning/phases/103-mtm-daily-series-charts-follow/103-PROBE-OQ1.md</automated>
  </verify>
  <done>Probe file exists with a per-path verdict, file:line evidence, and the mask-claim consequence spelled out. Findings feed Task 2 fixtures and Plans 02/04 SUMMARY claims.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: derive_basis_series — the shared dailies-canonical derive (tests first)</name>
  <files>analytics-service/services/basis_series.py, analytics-service/tests/test_basis_series.py</files>
  <behavior>
    - Round-trip guard (THE anti-Phase-101 test, name it test_basis_series_roundtrip): for a fixture series (dense with interior NaN days + leading/trailing NaN), `r = derive_basis_series(s, bench, periods_per_year=365, cumulative_method="geometric", day_basis="calendar")` satisfies: rebuild `pd.Series` from `r.series_rows` → `gap_fill_daily_returns` → `compute_all_metrics(..., same conventions).metrics_json == r.metrics_json` EXACTLY (dict equality).
    - Sparse emission: NaN and ±Inf days are ABSENT from `series_rows`; 0.0 days ARE present (a gap-filled flat day is real data, mirroring csv_daily_returns' `pd.notna` persist at job_worker.py:4291); rows ascending by date, ISO "YYYY-MM-DD".
    - Gap spans: `r.gap_spans` == inclusive spans of calendar days ABSENT from series_rows within [first_row, last_row] (reuse `_consecutive_spans`); dense input → `gap_spans == []`; input with a 3-day interior hole → one span with correct inclusive start/end.
    - Round-trip of the mask: `r.gap_spans` re-derivable from `r.series_rows` (assert derive-from-rows == r.gap_spans).
    - Cash-shape feasibility (SAME guard shape for cash, per mandate): a cash-convention fixture (√252, geometric, calendar, no NaN) round-trips identically — proving the backbone can adopt this guard for cash unchanged.
    - Conventions echo: `r.conventions == {"periods_per_year": ..., "cumulative_method": ..., "day_basis": ...}`.
    - Empty/degenerate input (<2 rows after sanitize) raises ValueError (mirrors compute_all_metrics contract) — call sites keep their existing ValueError degrade/fail handling.
  </behavior>
  <action>
Create `analytics-service/services/basis_series.py`:

```
@dataclass(frozen=True) BasisSeriesResult:
    metrics_json: dict[str, Any]       # JSON-safe scalar cache (derived)
    sibling_kinds: dict[str, Any]      # pass-through from MetricsResult (future cash/backbone adoption)
    series_rows: list[dict[str, Any]]  # sparse honest rows [{"date": "YYYY-MM-DD", "return": float}]
    gap_spans: list[dict[str, str]]    # [{"start","end"}] interior absent-day runs
    conventions: dict[str, Any]

def derive_basis_series(returns: pd.Series, benchmark_rets: pd.Series | None, *,
                        periods_per_year: int, cumulative_method: str,
                        day_basis: str) -> BasisSeriesResult
```

Implementation (compose EXISTING primitives only — no new valuation math, per D-LOCKED):
1. `sparse = _drop_nonfinite(returns)` (import from services.metrics) — this IS the persisted truth.
2. `scalars = compute_all_metrics(gap_fill_daily_returns(sparse), benchmark_rets, periods_per_year=..., cumulative_method=..., day_basis=...)` — scalars are computed FROM the sparse persisted form, so the round-trip guard holds by construction.
3. `gap_spans` = `_consecutive_spans` (import from services.stitch_composite) over calendar days in `[sparse.index.min(), sparse.index.max()]` absent from `sparse.index`.
4. `series_rows` from `sparse` (`ts.date().isoformat()`, `float(val)`).

DOCUMENT in the module docstring (load-bearing, reviewers will look here):
- This module is the dailies-canonical route (Phase 103 first increment; the process_key/unified-backbone program adopts it for cash — do NOT fork).
- Deliberate semantic: scalars derive from the SPARSE persisted form re-densified with `gap_fill_daily_returns` (0.0). Where the in-memory MTM series carried interior NaN guard days, this may shift the MTM scalar slightly vs the Phase-101/102 inline compute (which fed NaN through to statistics). That is the LOCKED principle's intent — the persisted dailies are the only truth and the scalar is their cache. Cash paths are NOT routed through this module in Phase 103 (SC-4 byte-identity untouched). Cite the Task-1 probe file for whether the NaN case is even reachable.
- Leading/trailing NaN days shrink the persisted span (and thus the calendar span CAGR sees) — same rationale.

Fixture guidance: use small hand-built `pd.Series` with `pd.date_range(..., unit handling matching gap_fill_daily_returns's "us" pin — construct via pd.to_datetime)`. Reflect the Task-1 probe: if single-key is whole-or-degrade, keep the interior-NaN fixture (it still pins helper semantics + serves composite NaN cases) but note in the test docstring which path can actually produce it.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_basis_series.py -x -q</automated>
  </verify>
  <done>All behavior tests pass; round-trip guard proves scalars == derive-from-persisted-dailies by construction for MTM AND cash-shaped conventions; no new math (only _drop_nonfinite, gap_fill_daily_returns, compute_all_metrics, _consecutive_spans composed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: persist_basis_series — authoritative upsert + stale-row heal</name>
  <files>analytics-service/services/basis_series.py, analytics-service/tests/test_basis_series.py</files>
  <behavior>
    - `persist_basis_series(supabase, strategy_id, basis="mark_to_market", result=<BasisSeriesResult>)` calls `supabase.rpc("upsert_strategy_analytics_series_batch", {"p_strategy_id": ..., "p_kinds": {"mtm_daily_returns": <payload>}})` with payload `{"schema": 1, "basis": "mark_to_market", "rows": result.series_rows, "gap_spans": result.gap_spans, "conventions": result.conventions}`.
    - `persist_basis_series(supabase, strategy_id, basis="mark_to_market", result=None)` DELETES the `(strategy_id, "mtm_daily_returns")` row — the heal path for degrade/gated/not-attempted derives (Pitfall 5: a stale series must never outlive the scalars' authoritative-NULL write at job_worker.py:3090).
    - Unknown basis raises ValueError (only "mark_to_market" mapped in Phase 103; the kind map is where cash joins later).
    - Tests use pure-stub supabase mocks (repo convention — no live DB; assert the rpc name/args and the delete filter chain).
  </behavior>
  <action>
Add to `basis_series.py`: `KIND_MTM_DAILY_RETURNS = "mtm_daily_returns"`, a `_KIND_BY_BASIS = {"mark_to_market": KIND_MTM_DAILY_RETURNS}` map, and `persist_basis_series` as specified. This is a sync function (call sites wrap in their existing `db_execute`/thread patterns — Plan 02's concern). PK `(strategy_id, kind)` makes the upsert a single-row authoritative replace — no span reconcile needed (contrast the cash multi-row `_reconcile_full_delete`).

Persistence-home decision (delegated, FINAL — record in SUMMARY): NO DDL. `kind` is unconstrained TEXT by documented design ("Add a new kind = INSERT a new row; no ALTER TABLE", migration 20260428120919); RLS deny-all already covers the new kind; the factsheet reads via the service-role admin client exactly like `csv_daily_returns` (no RPC panel needed); the only writer is our worker through the service_role-only RPC. A `kind` CHECK or COMMENT migration would trigger migration-reviewer + rls-auditor + test-project MCP catch-up for zero safety gain (a typo'd kind is caught by the round-trip guard + Plan 03 read tests). Consequence: NO test-project catch-up needed this phase.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_basis_series.py -x -q && python -m pytest tests --ignore=tests/e2e -q --co -q >/dev/null && echo COLLECT-OK</automated>
  </verify>
  <done>Persist upserts via the existing batch RPC with the exact payload shape; result=None deletes the row; tests pass with stub mocks; module importable by job_worker without cycles (imports only metrics/broker_dailies/stitch_composite).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → DB | service-role RPC write of JSONB series payload (trusted writer, deny-all RLS table) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | strategy_analytics_series payload | mitigate | Only writer is the worker via service_role-only RPC (REVOKE from anon/authenticated already in place); deny-all RLS blocks direct reads/writes |
| T-103-02 | Information Disclosure | persisted MTM series | accept | Rides the SAME published/owner visibility gate as csv_daily_returns; no new read surface in this plan |
| T-103-SC | Tampering | package installs | accept | Zero new packages this phase (RESEARCH: no Package Legitimacy Audit required) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_basis_series.py -q` green.
- `python -m pytest tests --ignore=tests/e2e -q` green (no collateral).
- `grep -n "compute_all_metrics\|gap_fill_daily_returns\|_consecutive_spans" analytics-service/services/basis_series.py` shows composition of existing primitives only.
</verification>

<success_criteria>
- OQ1 answered with evidence; mask claims for Plans 02/04 grounded in it.
- derive_basis_series round-trip guard green for MTM + cash-shaped conventions.
- persist/heal implemented against the existing batch RPC; NO DDL shipped.
</success_criteria>

<output>
Create `.planning/phases/103-mtm-daily-series-charts-follow/103-01-SUMMARY.md` when done. Record: OQ1 verdict, the no-DDL decision + rationale, the NaN→0.0 scalar-semantics consequence (and whether reachable per probe).
</output>
