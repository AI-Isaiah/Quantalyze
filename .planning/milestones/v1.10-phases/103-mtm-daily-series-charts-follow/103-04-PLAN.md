---
phase: 103-mtm-daily-series-charts-follow
plan: 04
type: execute
wave: 3
depends_on: [103-03]
files_modified:
  - src/app/factsheet/[id]/v2/basis-context.tsx
  - src/app/factsheet/[id]/v2/basis-context.test.tsx
  - src/app/factsheet/[id]/v2/TimeSeriesChart.tsx
  - src/app/factsheet/[id]/v2/HeatmapPanels.tsx
  - src/app/factsheet/[id]/v2/MetricsColumn.tsx
  - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx
  - src/app/factsheet/[id]/v2/DistributionPanels.tsx
  - src/app/factsheet/[id]/v2/StressWindowsPanel.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx
autonomous: true
requirements: [MTM-04]
must_haves:
  truths:
    - "Under basis === 'mark_to_market' with a bundle present, EVERY chart (equity, cumVsBench, volMatched, dailyReturns, rolling vol/sharpe/sortino/beta, worstDDs, underwaterAcc) renders from the MTM bundle — own dates axis, own missingSegments, MTM comparators"
    - "Every DAILIES-DERIVABLE statistics panel (quantiles, streaks, calmarByYear, bootstrapCI, styleDrift, stressWindows strat columns) renders its MTM values under mark_to_market; EXTERNAL-DATA panels (correlations, correlationMatrix) stay cash and carry no MTM-implying copy"
    - "Under cash, or when the bundle is absent (stale cache / not yet backfilled / gated), charts render byte-identically to today and the honest caption explains the fallback"
    - "MTM gaps render as the existing marked-break idiom (never 0.0-filled); the a11y summary counts the ACTIVE basis's gaps"
    - "Toggling basis resets the zoom range (a cash zoom window has no meaning on a different date axis) WITHOUT editing the frozen factsheet-context.tsx"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/basis-context.tsx"
      provides: "useBasisSeriesView(payload) — identity under cash/absent-bundle; memoized merged view under MTM"
      contains: "useBasisSeriesView"
    - path: "src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx"
      provides: "Falsifiable per-basis chart-series test (neuter → charts stay cash under MTM → test fails)"
  key_links:
    - from: "src/app/factsheet/[id]/v2/TimeSeriesChart.tsx"
      to: "useBasisSeriesView"
      via: "view-merge replaces every series read (payload[stratField], dates, missingSegments, strategyWorst10, comparators)"
      pattern: "useBasisSeriesView"
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "resetXRange"
      via: "effect on basis change (frozen context's own reset API — no context edits)"
      pattern: "resetXRange"
---

<objective>
Make the charts follow the toggle: a client-side view-merge hook selects the active basis's series bundle, and every chart surface reads through it. Replace the now-false "Charts show the cash-settlement series" caption with basis-aware honest copy (including the bundle-absent fallback state).

HARD CONSTRAINTS (frozen islands — phase-52-frozen-spine-guards.test.ts): `factsheet-context.tsx` (FactsheetProvider/xRange owner), `MasterBrush`, `HistogramChart`, `EquityChart`, `TouchTooltip`, `useTapPin` are git-diff-zero FROZEN. `TimeSeriesChart.tsx` was un-frozen in Phase 90 (additive-edit precedent) and IS editable. The design below routes everything through editable files: the swap happens where series are READ, zoom reset uses the context's existing `resetXRange` API, and frozen children keep receiving data via their existing props.

Purpose: MTM-04 user-visible half — toggling MTM moves the whole factsheet.
Output: charts + heatmaps + worst-10 + caption + a11y follow the basis; falsifiable tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-mtm-daily-series-charts-follow/103-PROBE-OQ1.md
@src/app/factsheet/[id]/v2/basis-context.tsx
@src/app/factsheet/[id]/v2/chart-configs.ts
@DESIGN.md

<interfaces>
<!-- Verified against source this session. -->

From src/lib/factsheet/types.ts (Plan 103-03): `FactsheetPayload.seriesByBasis?: { mark_to_market?: BasisSeriesBundle }` where BasisSeriesBundle = { dates, strategyReturns, strategyEquity, strategyDrawdowns, strategyRollingVol/Sharpe/Sortino, rollingWindow, rollingBetaWindow, strategyWorst10, comparators, monthlyReturns, dailyHeatmap, missingSegments } — every field name/type matches its FactsheetCommon top-level sibling, so `{...payload, ...bundle}` is a well-typed FactsheetPayload.

From src/app/factsheet/[id]/v2/basis-context.tsx: `useBasis(): { basis, setBasis }`; `Basis = "cash_settlement" | "mark_to_market"`; GUARD-04 file discipline: NO browser-storage/URL/history access anywhere in this file.

From src/app/factsheet/[id]/v2/factsheet-context.tsx (FROZEN — consume, never edit): `useXRange(): { xRange: readonly [number, number], setXRange, resetXRange }` (:30-33); `usePayload()`.

From src/app/factsheet/[id]/v2/TimeSeriesChart.tsx (editable): `usePayload()` :63; `resolveSeries(config, payload, cmp, xRange[0])` :103 (typed against FactsheetPayload — pass the merged view); payload.dates reads at :171/:241/:352/:684/:1022; segment overlay reads `payload.segmentBoundaries`/`payload.missingSegments`/`payload.dates` :237-241 (+ deps :337-339); worst-10 bands `payload.strategyWorst10` :829.

From src/app/factsheet/[id]/v2/chart-configs.ts: CHART_CONFIGS keys cumulative/cumVsBench/volMatched/dailyReturns/rollingVol/rollingSharpe/rollingSortino/rollingBeta/worstDDs/underwaterAcc; `stratField` ∈ {strategyEquity, strategyReturns, strategyRollingVol, strategyRollingSharpe, strategyRollingSortino, strategyDrawdowns, null}; comparator fields come from the cmp block (`useActiveComparator`).

From src/app/factsheet/[id]/v2/FactsheetView.tsx: caption block :469-478 (`composite && <p role="status">…"Charts show the cash-settlement series. Mark-to-market applies to summary metrics only."`); sr-only gap summary :451-459 reads payload.segmentBoundaries/missingSegments; F5 discipline: the live region must be PRE-MOUNTED (content change on an existing region, never mount-on-toggle).

HeatmapPanels.tsx reads payload.monthlyReturns :40, payload.dailyHeatmap :161. MetricsColumn.tsx reads payload.strategyWorst10 :466 (worst-10 table).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useBasisSeriesView hook + TimeSeriesChart view-merge + zoom reset</name>
  <files>src/app/factsheet/[id]/v2/basis-context.tsx, src/app/factsheet/[id]/v2/basis-context.test.tsx, src/app/factsheet/[id]/v2/TimeSeriesChart.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <behavior>
    - `useBasisSeriesView(payload)`: returns the ORIGINAL payload object (referential identity — the GUARD-02 byte/render-stability contract) when basis is cash OR `payload.seriesByBasis?.mark_to_market` is absent; returns a useMemo'd `{...payload, ...bundle}` when basis is MTM and the bundle exists. `segmentBoundaries` is NOT in the bundle so the merge inherits the shared top-level value (basis-invariant key handoffs). Identity + memo-stability tests in basis-context.test.tsx.
    - TimeSeriesChart under MTM+bundle: `resolveSeries` receives the merged view (equity/returns/rolling/drawdown series and comparator block all from the bundle); dates axis, segment overlay (missingSegments), worst-10 bands, tooltip dates, x-ticks, and the brush/tooltip child props all read the view. Under cash: view === payload → render byte-identical (existing marker/geometry tests unmodified and green).
    - Toggling basis calls `resetXRange()` exactly once per change (a cash zoom index range is meaningless on the MTM axis) — implemented in an EDITABLE component via an effect watching `basis` (place it in the chart-section component in FactsheetView that sits inside both providers), never by editing factsheet-context.tsx.
  </behavior>
  <action>
1. Add `useBasisSeriesView` to basis-context.tsx (keeps the GUARD-04 no-storage discipline — pure context + memo). Type: `(payload: FactsheetPayload) => FactsheetPayload`.
2. TimeSeriesChart: at the top of `TimeSeriesChartInner`, `const view = useBasisSeriesView(usePayload())` and replace every `payload.` series/dates/marker/worst10 read with `view.` (the anchors listed in interfaces; also the comparator: `view.comparators[cmpKey]`). Do NOT touch the frozen MasterBrush/HistogramChart/TouchTooltip files — they already receive data via props from TimeSeriesChart; just pass the view-derived values through the existing prop seams. Precedent note for the frozen-spine reviewer: this is the Phase-90 category (deliberate additive edit to the un-frozen TimeSeriesChart), keep the diff surgical.
3. FactsheetView: add the `resetXRange` effect on basis change (skip on mount — reset only on actual toggles).
4. Clamp defensively: where `xRange` indices meet `view.dates.length` in TimeSeriesChart, clamp end to `view.dates.length - 1` (the reset effect makes this a belt-and-suspenders guard against a same-render race, not the primary mechanism).
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/basis-context.test.tsx" "src/app/factsheet/[id]/v2/TimeSeriesChart.markers.test.tsx"</automated>
  </verify>
  <done>Hook identity/merge semantics pinned; every TimeSeriesChart series read goes through the view; zoom resets on toggle; existing marker tests green unmodified; frozen files byte-untouched (git diff on the 8 frozen paths is empty).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Heatmaps + worst-10 + dailies-derivable stats panels + basis-aware caption + a11y</name>
  <files>src/app/factsheet/[id]/v2/HeatmapPanels.tsx, src/app/factsheet/[id]/v2/MetricsColumn.tsx, src/app/factsheet/[id]/v2/AnalyticalPanels.tsx, src/app/factsheet/[id]/v2/DistributionPanels.tsx, src/app/factsheet/[id]/v2/StressWindowsPanel.tsx, src/app/factsheet/[id]/v2/BatchDPanels.tsx, src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <behavior>
    - HeatmapPanels renders `view.monthlyReturns` / `view.dailyHeatmap`; MetricsColumn renders `view.strategyWorst10` (worst-10 table, coherent with the worstDDs chart bands) + `view.quantiles`; AnalyticalPanels renders `view.streaks` / `view.calmarByYear` / `view.bootstrapCI`; DistributionPanels renders `view.quantiles`; StressWindowsPanel renders `view.stressWindows`; BatchDPanels renders `view.styleDrift`. All byte-identical under cash (view === payload).
    - EXTERNAL passthrough (honesty by construction): DistributionPanels' correlations (:332) and correlationMatrix (:442) read `view.correlations` / `view.correlationMatrix` — but because those fields are NOT in the bundle, the merged view carries the CASH top-level values, so they render cash under BOTH bases with no branching. Assert: under MTM, correlations render the cash values (falsifiable — an accidental bundling would change them). No basis eyebrow/label is added to these two panels (grep-verified none exists today).
    - Under mark_to_market with a bundle whose stats differ from cash, each dailies-derivable panel shows the MTM value (calmarByYear rows, quantile stats, streak histograms, bootstrap CI bounds, styleDrift KS, stressWindows[].stratReturn all differ from cash).
    - Caption (the stale FactsheetView:473 copy) becomes basis-aware on the PRE-MOUNTED role="status" region and renders for ANY strategy with the MTM toggle (single-key options included — today's block is composite-gated; the toggle is not): basis=cash → empty string (unchanged idiom); basis=MTM + bundle present → "Charts show the mark-to-market series."; basis=MTM + bundle ABSENT → the existing honest copy "Charts show the cash-settlement series. Mark-to-market applies to summary metrics only." (the fallback state is REAL: stale cache or a strategy not yet re-derived/backfilled).
    - The sr-only composite gap summary counts the ACTIVE view's missingSegments (MTM gaps ≠ cash gaps), still "never as zero returns".
  </behavior>
  <action>
Use `useBasisSeriesView` in each component (all are editable; NONE are frozen islands — verified against phase-52-frozen-spine-guards.test.ts: only TimeSeriesChart among factsheet components is touched, and it is the Phase-90 carve-out). The merged view gives MTM for bundle fields and cash for external fields (correlations/correlationMatrix) automatically, so a single `const view = useBasisSeriesView(usePayload())` per component + swapping `payload.` → `view.` on the reads is the whole change; no per-panel MTM/cash branching. For the caption: keep the F5 pre-mount discipline — the region exists whenever the basis toggle can render; only its text content changes. Gate the region's mount on "toggle available" (mtmGate available — same predicate KpiStrip uses), superseding the composite-only gate; verify the single-key arm renders it. Visual/DESIGN decision (delegated, FINAL): MTM gaps reuse the EXACT Phase-90 missingSegments marker treatment — identical shape in, identical render out; no new visual vocabulary, so DESIGN.md needs no update and no approval gate. The only new user-facing string is the caption copy above.

Consult 103-PROBE-OQ1.md: if the single-key verdict was whole-or-degrade, note in code comment + SUMMARY that single-key MTM interior gap markers are structurally empty today (mask fires on composites' inter-member gaps; span-level differences show via the shorter MTM axis) — the claim in user-facing copy must not promise interior marks the compute cannot produce.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2" -t "basis" --passWithNoTests=false || npx vitest run "src/app/factsheet/[id]/v2"</automated>
  </verify>
  <done>Heatmaps + worst-10 + every dailies-derivable stats panel (quantiles/streaks/calmarByYear/bootstrapCI/styleDrift/stressWindows) follow the view; correlations/correlationMatrix stay cash with no MTM copy; caption honest in all three states (cash / MTM+bundle / MTM-fallback); a11y summary counts active-basis gaps; cash render byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Falsifiable end-to-end basis tests + full frontend gates</name>
  <files>src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx</files>
  <action>
Extend FactsheetBody.basis.test.tsx (exists — Phase 90/102 suite) with the phase's keystone tests, using a payload fixture whose MTM bundle has DISTINGUISHABLE values (different dates length, different equity values, one MTM-only gap span):

1. **Charts follow (falsifiable)**: render, toggle to mark_to_market, assert the cumulative chart's rendered series/dates derive from the BUNDLE (query the chart's accessible output or exported test hooks the marker tests already use — e.g. tick labels from the MTM date range, gap marker count = MTM gap count). NEUTER CHECK built into the assertion: the asserted values must differ from the cash fixture's, so a regression that leaves charts on cash under MTM FAILS this test (Nyquist: this is the test that did not exist in Phase 102).
1b. **Dailies-derivable panels follow (falsifiable)**: pick TWO — calmarByYear (AnalyticalPanels) and quantiles (DistributionPanels) — and assert that under mark_to_market they render the MTM-bundle values (which the fixture makes distinct from cash). Neutering the useBasisSeriesView merge (return payload always) must make BOTH this and the chart assertion RED — the panels are the expanded-scope proof.
1c. **External panels stay cash (falsifiable the other way)**: assert correlations (DistributionPanels) render the CASH values under mark_to_market (a fixture where the bundle, if it wrongly carried correlations, would differ) — proving external-data panels do NOT follow the basis, and that no MTM-implying label renders on them.
2. **Cash unchanged**: same fixture, basis left at default → rendered values match the cash fields; toggling back after MTM restores them (and resetXRange fired).
3. **Fallback**: fixture WITHOUT seriesByBasis, toggle MTM → charts remain cash, caption shows the fallback copy, KpiStrip scalars still swap (Phase 102 behavior preserved).
4. **No-persistence discipline**: existing GUARD-04 no-storage-write-on-toggle assertions still pass with the new hook in the tree.

Then run the full gates: `npm run test` (full vitest, use --no-file-parallelism locally if flaky per repo MEMORY), `npx tsc --noEmit`, and `npm run lint` (repo rule: lint catches react-hooks errors tsc+vitest miss — the new effect/memo hooks make this load-bearing). Fix everything; never skip silently.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.basis.test.tsx" && npx tsc --noEmit && npm run lint</automated>
  </verify>
  <done>Keystone falsifiable test proves charts read the MTM bundle under MTM and cash otherwise; fallback + no-persistence pinned; full vitest + tsc + lint green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server payload → client render | bundle values already server-coerced (Plan 03); client treats absence as fallback, never fabricates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-07 | Spoofing (UI honesty) | caption/claims under MTM | mitigate | three-state caption keyed on bundle presence; gaps render as breaks, never 0.0 |
| T-103-08 | Denial of Service | out-of-range xRange on axis swap | mitigate | resetXRange on toggle + defensive clamp in TimeSeriesChart |
| T-103-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- Full `npm run test` green; `npx tsc --noEmit` clean; `npm run lint` clean.
- `git diff --name-only` contains NONE of the 8 frozen-island paths (phase-52-frozen-spine-guards.test.ts passes untouched).
- Keystone falsifiability: temporarily neutering the useBasisSeriesView merge (return payload always) makes FactsheetBody.basis.test.tsx fail.
</verification>

<success_criteria>
- Toggling MTM moves every chart surface (axis, series, mask, comparators, heatmaps, worst-10) AND every dailies-derivable statistics panel (quantiles/streaks/calmarByYear/bootstrapCI/styleDrift/stressWindows); external-data panels (correlations/correlationMatrix) stay cash with honest labeling; honest fallback + a11y; cash byte-identical; frozen spine untouched.
</success_criteria>

<output>
Create `.planning/phases/103-mtm-daily-series-charts-follow/103-04-SUMMARY.md` when done. Record the caption three-state matrix, the dailies-derivable-vs-external panel split (which panels follow MTM vs stay cash, incl. the stressWindows MIXED decision), and (from the probe) what the single-key interior mask can/cannot show. Ship-time reminder: the live Zavara MTM-curve corroboration is the POST-DEPLOY gate after the re-derive backfill (deferred, per CONTEXT).
</output>
