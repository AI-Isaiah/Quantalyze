---
phase: 103-mtm-daily-series-charts-follow
plan: 02
type: execute
wave: 2
depends_on: [103-01]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_mtm_single_key.py
  - analytics-service/tests/test_stitch_composite_job.py
autonomous: true
requirements: [MTM-04]
must_haves:
  truths:
    - "A single-key options derive persists the MTM daily series (kind mtm_daily_returns) alongside the scalar cache — both produced by the ONE shared derive_basis_series call"
    - "A composite stitch persists its stitched-MTM daily series through the SAME shared helper — the bespoke _metrics_result_for(clipped_mtm) invocation is GONE"
    - "A degraded/gated/not-attempted MTM derive heals (deletes) any stale mtm_daily_returns row, mirroring the authoritative metrics_json_by_basis NULL write"
    - "Cash paths are byte-identical: cash scalars, csv_daily_returns writes, and the composite cash compute are untouched (SC-4)"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "Both MTM derive sites call derive_basis_series + persist_basis_series"
      contains: "derive_basis_series"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "services/basis_series.py"
      via: "single-key seam (~:2983 region) AND composite seam (~:4249 region) both import and call the shared helper"
      pattern: "from services\\.basis_series import"
    - from: "analytics-service/tests/test_mtm_single_key.py"
      to: "job_worker single-key seam"
      via: "wiring test — proves the call site INVOKES the helper (fails when neutered)"
      pattern: "derive_basis_series|persist_basis_series"
---

<objective>
Wire BOTH MTM derive sites to the shared `basis_series` route from Plan 103-01: single-key (`job_worker.py` ~:2946-3010, replacing the inline `compute_all_metrics(mtm_returns, ...)`) and composite (~:4249, replacing `_metrics_result_for(clipped_mtm)`), with authoritative persist + stale-row healing. This retires the throwaway bespoke composite MTM compute (the path the backbone merge deletes) and makes the persisted MTM dailies the canonical source for Plans 03/04.

Purpose: MTM-04 backend half — the series stops being computed-then-discarded.
Output: both seams route through `derive_basis_series`; extended worker tests; cash goldens untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/103-mtm-daily-series-charts-follow/103-RESEARCH.md
@.planning/phases/103-mtm-daily-series-charts-follow/103-PROBE-OQ1.md
@analytics-service/services/basis_series.py

<interfaces>
<!-- Verified against source this session. -->

From analytics-service/services/basis_series.py (Plan 103-01):
```python
derive_basis_series(returns, benchmark_rets, *, periods_per_year, cumulative_method, day_basis) -> BasisSeriesResult
persist_basis_series(supabase, strategy_id, *, basis, result)  # result=None → heal (delete row)
KIND_MTM_DAILY_RETURNS = "mtm_daily_returns"
```

Single-key seam (job_worker.py, exact anchors verified):
- `mtm_attempted = True` set at :2312; MTM series computed at :2357 (`combine_native_ledger(..., pnl_basis=PNL_BASIS_MARK_TO_MARKET)` → `mtm_returns`); degrade paths set `mtm_returns = None` + `mtm_gated_reason`.
- Inline scalar compute region :2946-3010: builds `_mtm_periods` (periods_per_year_for_asset_class), `_mtm_cumulative`/`_mtm_day_basis` (from denominator_config, defaults geometric/calendar), guarded `_mtm_benchmark_rets` (BTC, failure → None), then `compute_all_metrics(mtm_returns, ...)` inside `try/except ValueError` → degrade with `MTM_REASON_SERIES_UNCOMPUTABLE`.
- Authoritative by-basis write at :3090: `_prestamp_payload["metrics_json_by_basis"] = {"mark_to_market": mtm_metrics_json} if (mtm_attempted and mtm_metrics_json is not None) else None`, upserted in `_prestamp_dq_flags` (:3100-3106 region) via `ctx.supabase`.

Composite seam (job_worker.py):
- `_metrics_result_for(clipped_series)` :4144-4165 = `stitch_clipped_series → gap_fill_daily_returns → compute_all_metrics(dense, benchmark_rets, periods_per_year, cumulative_method, day_basis)`. KEEP for cash (`cash_metrics_result = _metrics_result_for(clipped_cash)` — SC-4).
- MTM call :4249: `mtm_metrics_json = dict(_metrics_result_for(clipped_mtm).metrics_json)` inside `try/except CompositeOverlapError / ValueError` (both stamp permanent-failed).
- `mtm_ok, mtm_reason = mark_to_market_available(member_signals)` gates whether the MTM pass runs at all.
- Persist phase step 7 (:4285+): sparse `stitched_cash` → csv_daily_returns with `_reconcile_full_delete` + chunked upserts, all wrapped in `await db_execute(fn)`.

From services/stitch_composite.py:217: `stitch_clipped_series(clipped: list[tuple[int, pd.Series]]) -> pd.Series` (raises CompositeOverlapError on day collision).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-key seam — derive via shared helper + persist/heal the series row</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_mtm_single_key.py</files>
  <behavior>
    - Success path: with a stubbed `mtm_returns` Series, the derive produces `mtm_metrics_json` VIA `derive_basis_series` (patch it and assert called once with `mtm_returns`, `_mtm_benchmark_rets`, and the exact `periods_per_year/_mtm_cumulative/_mtm_day_basis` conventions) AND `persist_basis_series` is called with the same result. Wiring falsifiability: monkeypatch `derive_basis_series` to a sentinel-raising stub → the derive degrades exactly like today's ValueError path (proves the call site invokes it, not a parallel inline compute).
    - Degrade path: `mtm_returns is None` (gated) OR ValueError from the helper → `persist_basis_series(..., result=None)` heal is called (stale series row deleted) and `metrics_json_by_basis` write stays NULL — mirror matrix of the :3065 MED-HIGH comment (success → object+row; degrade → NULL+delete; not-attempted → NULL+delete).
    - Scalar parity: for a NaN-free dense fixture, the helper-produced `metrics_json` equals the old inline `compute_all_metrics(mtm_returns, ...)` output byte-for-byte (regression pin for the common case; the NaN case's documented semantic shift is pinned in test_basis_series.py).
  </behavior>
  <action>
In the :2946-3010 region: keep the existing convention assembly (`_mtm_periods`, `_mtm_cumulative`, `_mtm_day_basis`, guarded `_mtm_benchmark_rets`) EXACTLY as is; replace the inline `compute_all_metrics(mtm_returns, ...)` call with `_mtm_basis_result = derive_basis_series(mtm_returns, _mtm_benchmark_rets, periods_per_year=_mtm_periods, cumulative_method=_mtm_cumulative, day_basis=_mtm_day_basis)`; `mtm_metrics_json = dict(_mtm_basis_result.metrics_json)`. The surrounding `try/except ValueError → MTM_REASON_SERIES_UNCOMPUTABLE` degrade stays untouched (the helper propagates compute_all_metrics's ValueError by design).

Persist/heal: alongside the authoritative `metrics_json_by_basis` prestamp seam (:3090 region), add the series write with the SAME success matrix: success (`mtm_attempted and mtm_metrics_json is not None`) → `persist_basis_series(ctx.supabase, strategy_id, basis="mark_to_market", result=_mtm_basis_result)`; every other shape → `persist_basis_series(..., result=None)` (heal). Match the seam's existing sync-call idiom (the prestamp runs sync via `ctx.supabase` — follow whatever wrapper `_prestamp_dq_flags` uses; do not invent new async plumbing). A persist failure must not sink the cash derive: wrap in the same error-handling style the prestamp uses; if the prestamp itself is fail-loud, stay fail-loud (consistency over invention).

Comment the seam: "Phase 103 (MTM-04): series + scalars from the ONE shared derive_basis_series — the dailies-canonical route the backbone merge extends. Scalars remain a derived cache (round-trip guard in test_basis_series.py)."

SC-4 guard: touch NOTHING in the cash pass — no edits above :2946 except the import, none to the csv_daily_returns bridge.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_mtm_single_key.py -x -q</automated>
  </verify>
  <done>Single-key MTM scalars come from derive_basis_series; series row persisted on success, healed otherwise; degrade semantics and cash pass byte-identical; wiring test fails when the helper call is neutered.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Composite seam — replace _metrics_result_for(clipped_mtm) with the shared route</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <behavior>
    - Success path: composite MTM scalars produced via `stitch_clipped_series(clipped_mtm)` → `derive_basis_series(stitched_mtm, benchmark_rets, periods_per_year=..., cumulative_method=..., day_basis=...)` (the SAME convention variables the cash `_metrics_result_for` closure uses) and the series row persisted via `persist_basis_series`. Assert `_metrics_result_for` is NO LONGER invoked with `clipped_mtm` (e.g., spy on it — exactly one call, for cash).
    - Exception parity: `CompositeOverlapError` from `stitch_clipped_series` and `ValueError` from the helper still hit the EXISTING except arms (permanent `_stamp_failed`) — same stamps, same DispatchResults.
    - Gated path (`mtm_ok` False) and MTM-degrade paths: `persist_basis_series(..., result=None)` heal runs in the persist phase so a previously-successful strategy that re-derives gated loses its stale series row.
    - Scalar parity: for a NaN-free multi-member fixture, helper `metrics_json` == old `_metrics_result_for(clipped_mtm).metrics_json` byte-for-byte.
    - Inter-member gap: a two-member fixture with a window hole yields `gap_spans` covering exactly the hole (the composite interior mask exists by construction — probe file).
  </behavior>
  <action>
At :4249: replace `mtm_metrics_json = dict(_metrics_result_for(clipped_mtm).metrics_json)` with the explicit two-step: `stitched_mtm = stitch_clipped_series(clipped_mtm)` (inside the existing try so CompositeOverlapError handling is untouched) then `_mtm_basis_result = derive_basis_series(stitched_mtm, benchmark_rets, periods_per_year=periods_per_year, cumulative_method=cumulative_method, day_basis=day_basis)`; `mtm_metrics_json = dict(_mtm_basis_result.metrics_json)`. `_metrics_result_for` itself stays UNTOUCHED for cash (SC-4) — add a comment on it: "cash-only as of Phase 103; MTM routes through services/basis_series.py (dailies-canonical); the backbone merge later moves cash there too."

Persist: in step 7 (the persist phase, after the csv_daily_returns cash writes), add `await db_execute(lambda: persist_basis_series(supabase, strategy_id, basis="mark_to_market", result=_mtm_basis_result if mtm_metrics_json is not None else None))` — matching the surrounding `db_execute` idiom. Note the heal covers BOTH gated (`mtm_ok` False → result None) and any future degrade shape; initialize `_mtm_basis_result = None` before the gate.

Keep the composite ordering honest: series persist lands in the same persist phase as csv_daily_returns (never before the compute succeeds), so a failed stitch never half-persists.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_stitch_composite_job.py -x -q</automated>
  </verify>
  <done>Composite MTM routes through the shared helper; `_metrics_result_for` is cash-only; gated/degraded re-derives heal the series row; exception stamps unchanged; inter-member gap_spans proven in a fixture.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + cash byte-identity regression sweep</name>
  <files>analytics-service/tests/test_mtm_single_key.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <action>
Run the full analytics suite with coverage: `python -m pytest tests --ignore=tests/e2e -q --cov --cov-fail-under=80` — SERIAL, not `-n auto` (MEMORY: xdist flakes the global-claim fence tests). The nine cash-pin golden files and every existing cash test MUST pass UNMODIFIED — any diff there is an SC-4 violation: STOP and fix the seam, never re-pin a golden. If coverage dips below 80 from the new job_worker branches, add targeted tests for the uncovered persist/heal arms (write the missing test immediately — repo rule).

Also grep-audit the diff: `git diff --stat analytics-service/` must show ONLY job_worker.py + the two test files (+ basis_series.py from Plan 01 if same branch); `git diff analytics-service/services/job_worker.py | grep -c "^-.*csv_daily_returns"` should be 0 (no cash-bridge deletions). Do NOT run any git branch/commit operations — diff inspection only.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests --ignore=tests/e2e -q --cov --cov-fail-under=80</automated>
  </verify>
  <done>Full suite green at ≥80% coverage; zero cash-test/golden modifications; diff surface limited to the two seams + tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → DB | service-role series write/delete (mtm_daily_returns row) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-03 | Tampering | stale mtm_daily_returns row after degrade | mitigate | heal-on-every-terminal-shape matrix (Task 1/2), mirroring the authoritative metrics_json_by_basis NULL write |
| T-103-04 | Denial of Service | series persist failure sinking cash derive | mitigate | persist follows the prestamp/db_execute error idiom; cash writes complete before/independent of the MTM series write |
| T-103-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- Both seams import from `services.basis_series` (grep).
- `_metrics_result_for(clipped_mtm)` no longer exists (grep `_metrics_result_for(clipped_mtm)` → 0 hits).
- Full pytest suite green, coverage ≥80, goldens unmodified.
</verification>

<success_criteria>
- MTM dailies persisted on both paths via the ONE shared route; stale rows heal; cash byte-identical.
- Ship-time note for SUMMARY: existing options strategies (Zavara) have NO series row until the post-deploy re-derive backfill (`enqueue_compute_job(strategy_id, 'derive_broker_dailies')` on Railway) — that backfill remains the documented ship gate, NOT part of this plan.
</success_criteria>

<output>
Create `.planning/phases/103-mtm-daily-series-charts-follow/103-02-SUMMARY.md` when done. Record the heal matrix and the backfill ship-gate reminder.
</output>
