---
phase: 105-composite-the-one-csv-finalize-route
plan: 05
type: execute
wave: 3
depends_on: [105-01, 105-03]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_composite_headline_parity.py
  - analytics-service/tests/test_stitch_composite_job.py
autonomous: true
requirements: [BB-02]

must_haves:
  truths:
    - "Collapse #1 / SC-1: the bespoke `_metrics_result_for` closure (job_worker.py:4262-4290) is DELETED — composite cash routes through the shared derive_basis_series with scalar_returns=gap_fill_daily_returns(stitched_cash) (the closure's exact input, byte-identical by construction) + densify_policy='zero_fill' + benchmark_symbol='BTC' (LOW-2); `git grep _metrics_result_for` == 0 including comments/docstrings (:3335, :4319 and any test references)"
    - "Composite cash scalars are a cache of a persisted cash series: persist_basis_series(basis='cash_settlement') writes the (strategy_id, kind) row — sparse honest rows, gap_spans, conventions with densify='zero_fill' + nan_dates for member-guard NaN — ordered BEFORE the DONE-bearing headline scalar flip"
    - "Collapse #5 / SC-2: periods_per_year_for_asset_class(asset_class) is THE selector; the venue-blend computation (:4199-4203) survives ONLY as the retained fail-loud sanity assert (D4 — a wrong-asset_class composite still fails PERMANENT, never silently annualizes √252); _COMPOSITE_CRYPTO_VENUES/_COMPOSITE_DEGRADE_VENUES kept only as the unknown-venue/degrade backstop; equality of the two rules asserted across every existing composite fixture"
    - "The F-5 ValueError arm (:4291-4311) is re-homed onto derive_basis_series's ValueError (same PERMANENT disposition, same scrubbed message) and heal-deletes the stale cash series row"
    - "SC-5 / D5: the finalize sequence is ordered-idempotent — reconcile-delete → dailies upserts → cash+MTM series RPC → headline scalar/status flip LAST; a kill-point before the flip leaves NO complete scalar; the pre-existing re-derive partial-chart window is documented as accepted-transient (NO DDL, no SECDEF RPC — that option rides 106)"
    - "SC-4 composite flagship: dual-run DICT-EQUAL on a member-guard-NaN composite fixture — new-route scalars vs the legacy closure recomputed in-test (gap_fill(stitched) → compute_all_metrics); headline == metrics_json_by_basis.cash_settlement preserved"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "composite cash re-route + closure deletion + #5 collapse w/ retained assert + ordered finalize + LOW-2 benchmark identity on both composite derives"
      contains: "densify_policy=\"zero_fill\""
    - path: "analytics-service/tests/test_composite_headline_parity.py"
      provides: "member-guard-NaN dual-run parity + headline==by-basis preservation"
      contains: "guard"
    - path: "analytics-service/tests/test_stitch_composite_job.py"
      provides: "#5 two-rules-equal assertion + retained fail-loud assert test + SC-5 ordering/kill-point tests + cash-series-persist test"
      contains: "periods"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "analytics-service/services/basis_series.py"
      via: "derive_basis_series(stitched_cash, benchmark_rets, ..., scalar_returns=gap_fill_daily_returns(stitched_cash), densify_policy='zero_fill', benchmark_symbol='BTC')"
      pattern: "zero_fill"
    - from: "analytics-service/services/job_worker.py"
      to: "strategy_analytics headline"
      via: "cash series + MTM series persists ordered BEFORE the DONE-bearing headline upsert"
      pattern: "persist_basis_series"
    - from: "analytics-service/services/job_worker.py"
      to: "periods_per_year_for_asset_class"
      via: "THE one selector; venue blend retained only as fail-loud cross-check"
      pattern: "periods_per_year_for_asset_class"
---

<objective>
BB-02, Phase 105 Wave 3 — the composite core: collapse #1 (delete `_metrics_result_for`, the LAST cash bypass — Phase 103 only retired its MTM arm), collapse #5 (one `periods_per_year` convention, D4: selector collapses, fail-loud check RETAINED), SC-5 ordered-idempotent transactional finalize (D5), and the LOW-2 benchmark-identity carry on composite rows. The composite scalar input `gap_fill_daily_returns(stitched_cash)` is copied VERBATIM from the closure body (:4279-4280), so the scalar is byte-identical by construction; the D1 `nan_dates` mechanism (Plan 01) keeps the round-trip guard valid on the member-guard-NaN payload.

WHY #5 IS SAFE (locked, D4): the F-1 backstop (:4214-4231) landed in 044bee50 — the SAME commit that introduced stitch_composite.py — so no composite job has EVER run without it → no published composite carries a divergent clock. Collapsing the selector changes NO live scalar; the equality-assertion test pins this.

HARD BOUNDARIES (locked):
- NO DDL. Ordered-idempotent ONLY (D5): supabase-py has no cross-.table() transaction; the strict SECDEF finalize RPC, if ever wanted, rides 106's fold migration. If you conclude DDL is needed, STOP and flag.
- Do NOT silently delete the F-1 disagreement check — it becomes the sanity assert (D4). `_COMPOSITE_DEGRADE_VENUES` stays as the unknown-venue backstop.
- The single-key seam region (:2040-3230) is Plan 03's (already landed — rebase your read on current main); analytics_runner.py is Plan 04's. Touch ONLY the composite region here.
- The MTM arm (:4382-4445) keeps its structure; it gains ONLY `benchmark_symbol="BTC"` (LOW-2 — byte-visible solely in the payload conventions, which no reader consumes; the dual-run in test 104-style captures are structurally blind to it, documented).
- D5 honest boundary: ordered-idempotent = GATED EVENTUAL CONSISTENCY, not atomicity. The re-derive death window (old-scalar + partial-dailies until retry) is PRE-EXISTING (:4466-4492) — document as accepted-transient in the code comment; do NOT try to close it here.

Purpose: composite cash joins the one route; the phase's grep-gate lands; finalize can no longer ship a complete scalar without its series.
Output: re-routed composite, deleted closure, collapsed clock rule, ordered finalize, parity + ordering + equality tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md (§D1 D4 D5 + §specifics)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-RESEARCH.md (§Collapse #1 + #5 + §Transactional csv-finalize + §Architecture Patterns data-flow diagram)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md (§job_worker.py)

Executor constraints: NO git branch operations (only `git add <explicit paths>`); never stage `.planning/`. No Supabase MCP — pytest against the existing stitch-composite mock harness.

<interfaces>
From analytics-service/services/basis_series.py (post-105-01):
- `derive_basis_series(..., scalar_returns, densify_policy, benchmark_symbol)` — with densify_policy="zero_fill" + an in-index-NaN scalar_returns, result.nan_dates carries the guard dates and the persist payload includes them (schema 2). Raises ValueError on <2 finite rows AND propagates compute_all_metrics's ValueError (the F-5 class).
- `BasisSeriesResult` duck-fields the composite consumers need: `.metrics_json` (:4312 dict-spread), `.insufficient_window` (:4586), `.sibling_kinds` (:4688-4695).
- `persist_basis_series(supabase, strategy_id, *, basis="cash_settlement", result)` — result=None heal-deletes.

From analytics-service/services/job_worker.py composite region (line refs verified 2026-07-14; re-grep before editing — the file is actively edited):
- `stitched_cash = stitch_clipped_series(clipped_cash)` :4152 already exists; `_present_day_count` guard :4173.
- Venue-blend selector :4199-4203; F-1 backstop :4214-4231 (`_asset_class_periods` + `_stamp_failed` PERMANENT).
- Conventions :4234-4239 (composite denominator_config parsed branch-outer at :3540 — venue-agnostic already; MED-2 does not apply here).
- The closure :4262-4287; its call + F-5 arm :4289-4311; `cash_metrics_json = dict(...)` :4312.
- MTM arm derive :4411-4418 (add benchmark_symbol only).
- Persist sequence: rows_payload :4454-4462 → `_reconcile_full_delete` :4466-4481 → chunked dailies :4483-4492 → `_persist_mtm_series` :4667-4675 → `_write_headline_and_by_basis` :4677-4682 → guarded sibling kinds :4688+.
- `headline_payload` :4628-4643 (spread of cash_metrics_json; `composite_status` DONE-bearing).
- Textual references to the closure the `git grep _metrics_result_for == 0` gate must also purge (verified 2026-07-14): job_worker.py:3335 (docstring), :4262 (`def`), :4290 (call), :4319 (comment), :4384 (MTM-arm comment); test_stitch_composite_job.py:2780/:2788/:2792/:2845 (comments/docstrings). NOTE basis_series.py:20 is purged by 105-01 (its file) — do NOT edit basis_series.py here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse #5 — asset_class is THE clock selector; venue blend becomes the retained fail-loud check</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <read_first>analytics-service/services/job_worker.py:4190-4240, analytics-service/services/metrics.py:43 (periods_per_year_for_asset_class), analytics-service/tests/test_composite_headline_parity.py:470 (the existing mismatch-guard test — must stay green), .planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md §D4</read_first>
  <behavior>
    - `periods_per_year` used by the composite compute == periods_per_year_for_asset_class(strat_row asset_class) (the ONE rule, SC-2).
    - A composite whose venue blend disagrees with the asset_class clock still fails PERMANENT with the clock-disagreement message (the D4 retained assert; test_traditional_asset_class_composite_fails_loud_mismatch_guard green — update only if its patch/anchor points move, never its assertions).
    - For every existing composite fixture in the suite, the two rules resolve EQUAL (the #5 safety assertion — parametrized over the suite's fixture strategy_rows/venue sets).
    - `_COMPOSITE_DEGRADE_VENUES` degrade behavior unchanged (existing degrade tests green).
  </behavior>
  <action>Swap the roles at :4199-4231: assign `periods_per_year = periods_per_year_for_asset_class(strat_row.get("asset_class") ...)` (the :4214 expression becomes the selector) and keep the venue-blend expression (:4199-4203) bound to a check-local name feeding the SAME `_stamp_failed` fail-loud arm (:4217-4231) with the comparison direction updated (asset_class is now truth; the blend is the cross-check) and the F-1 comment rewritten to say so (cite D4: the non-blocking finalize-wizard force-derive means a wrong-asset_class composite must fail loud, not silently annualize √252). No other composite logic changes in this task. Tests in test_stitch_composite_job.py: a `-k periods` test asserting the two rules resolve equal across every composite fixture used in this file + parity file (import/enumerate their strategy_row/venues fixtures rather than duplicating); a test that the disagreement arm still stamps PERMANENT (may reuse/extend :470's fixture); docstrings name the neuter (delete the retained check → mismatch test RED; select by venue blend again → asset_class-truth test RED).</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_stitch_composite_job.py -k "periods" tests/test_composite_headline_parity.py -x</automated>
  </verify>
  <acceptance_criteria>Selector is the asset_class rule (grep: `periods_per_year = (` venue-blend form gone from :4199 region); fail-loud arm retained and tested; equality assertion parametrized over existing fixtures green; full test_stitch_composite_job.py green.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse #1 — re-route composite cash through the shared derive; DELETE _metrics_result_for (grep-gated)</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_composite_headline_parity.py</files>
  <read_first>analytics-service/services/job_worker.py:4241-4330 (benchmark fetch + closure + F-5 arm + MTM import block) and :4382-4418 (the Phase-103 MTM arm — the EXACT move to mirror), analytics-service/services/basis_series.py (post-105-01), analytics-service/tests/test_composite_headline_parity.py:323-470 (the parity harness), .planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md §job_worker "The closure to DELETE"</read_first>
  <behavior>
    - Member-guard-NaN composite dual-run (SC-4 FLAGSHIP): the new-route cash scalars are DICT-EQUAL to the legacy oracle computed in-test as compute_all_metrics(gap_fill_daily_returns(stitch_clipped_series(clipped_cash)), benchmark_rets, same conventions).metrics_json — on a fixture whose stitch carries an in-index member-guard NaN AND an inter-member absent gap (the two divergence shapes).
    - headline == metrics_json_by_basis.cash_settlement preserved (existing parity tests :323/:399 green).
    - The persisted cash_settlement payload carries densify="zero_fill", nan_dates == the guard dates, schema 2, conventions.benchmark == "BTC"; rows are the sparse honest set (guard + inter-member days ABSENT, matching csv_daily_returns row dates).
    - A simple-convention composite with an interior chain-break still fails PERMANENT with the interior-chain-break message (F-5 re-homed onto the derive's ValueError) AND heal-deletes any stale cash series row.
    - `git grep _metrics_result_for` returns NOTHING repo-wide.
  </behavior>
  <action>Mirror the Phase-103 MTM arm move (:4382-4418) for cash: replace the closure definition + call (:4262-4290, :4312) with `_cash_basis_result = derive_basis_series(stitched_cash, benchmark_rets, periods_per_year=periods_per_year, cumulative_method=cumulative_method, day_basis=day_basis, benchmark_symbol="BTC", scalar_returns=gap_fill_daily_returns(stitched_cash), densify_policy="zero_fill")` — `stitched_cash` already exists (:4152); import gap_fill_daily_returns beside the existing basis_series function-local imports (:4322-4326, hoist that import block above the cash derive since it now precedes the MTM gate). Bind `cash_metrics_json = dict(_cash_basis_result.metrics_json)` and rename the downstream `cash_metrics_result` reads (.insufficient_window :4586, .sibling_kinds :4688-4695) onto `_cash_basis_result` (duck-compatible post-105-01). Keep the F-5 `except ValueError` arm shape (:4291-4311) around the derive call — same PERMANENT disposition + scrubbed message — and add the heal-delete pair inside it (persist_basis_series result=None for cash_settlement; mirror Plan 03's guarded idiom). Note the derive ALSO raises the <2-finite-rows ValueError; the hoisted `_present_day_count` guard (:4173) fires first, so the arm's message stays honest — verify and keep. Sweep EVERY remaining textual reference for the grep-gate so `git grep _metrics_result_for | wc -l == 0` (basis_series.py:20 is 105-01's — already purged there; do NOT touch basis_series.py): rewrite job_worker.py:3335 (docstring), :4319 and :4384 (comments) to describe the shared-route reality, and rewrite the test_stitch_composite_job.py comments/docstrings at :2780/:2788/:2792/:2845 (the :2788/:2792 wording "cash keeps _metrics_result_for" is now FALSE — cash routes through derive_basis_series). Re-run `git grep _metrics_result_for` at the end and confirm zero hits. LOW-2: add `benchmark_symbol="BTC"` to the MTM derive (:4411-4417) with a one-line comment (identity carry; payload-only, no reader consumes conventions). Parity tests: extend test_composite_headline_parity.py with the flagship dual-run (legacy oracle computed in-test — the closure is gone, the test IS the oracle) + the payload assertions (densify/nan_dates/schema/benchmark) + the F-5 re-home test; name neuters (omit scalar_returns → guard-NaN scalars bridge → RED; drop nan_dates emission → Plan-01 round-trip on the captured payload → RED).</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_composite_headline_parity.py tests/test_stitch_composite_job.py -x && ! git grep -q _metrics_result_for</automated>
  </verify>
  <acceptance_criteria>`git grep _metrics_result_for | wc -l` == 0 (comments/docstrings/tests included — the locked grep-gate); flagship dual-run green with an in-index guard-NaN fixture; existing parity tests :323/:399/:501/:550 green; F-5 re-home test asserts PERMANENT + heal; captured cash payload asserts densify="zero_fill" AND nan_dates non-empty AND conventions.benchmark=="BTC".</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SC-5 — ordered-idempotent finalize: cash series persisted, everything DONE-bearing LAST</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <read_first>analytics-service/services/job_worker.py:4447-4700 (the persist sequence + the MTM ordering comment :4645-4666 — the discipline to extend), analytics-service/tests/test_stitch_composite_job.py:573 (test_mtm_series_persists_before_done_scalar_write — the ordering-test template), .planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md §D5</read_first>
  <behavior>
    - A `_persist_cash_series` step exists beside `_persist_mtm_series` (:4667-4675), persisting `_cash_basis_result` (basis="cash_settlement") — and BOTH series persists + the reconcile-delete + dailies upserts execute BEFORE `_write_headline_and_by_basis` (the DONE-bearing scalar/status flip is LAST).
    - Ordering test (cash twin of :573): the capture sequence shows cash-series RPC strictly before the headline upsert; moving the persist after the headline (neuter) → RED.
    - Kill-point test: a raising cash-series persist aborts the job BEFORE any headline write — the capture contains NO strategy_analytics upsert with computation_status complete/complete_with_warnings; a re-run (fresh capture) completes cleanly (idempotence via _reconcile_full_delete + single-row series upserts).
    - The accepted-transient boundary is documented in the persist-sequence comment: ordered-idempotent = gated eventual consistency; the re-derive death window (old scalar + partial dailies until retry) is PRE-EXISTING and unchanged; strict atomicity (SECDEF RPC) deliberately deferred to ride 106's fold migration.
  </behavior>
  <action>Add `_persist_cash_series` mirroring `_persist_mtm_series` (:4667-4675) — success shape persists `_cash_basis_result`, every failure/degrade shape persists None (heal), via db_execute (fail-loud: a cash-series persist failure aborts before the gating scalar, exactly the MTM comment's rationale :4645-4666). Place it adjacent to the MTM persist (both after the dailies chunk loop, before `_write_headline_and_by_basis`). Extend the :4645-4666 ordering comment to cover cash + the D5 honest boundary + the accepted-transient note (cite job_worker.py:4466-4492 pre-existing window; SECDEF option → 106). Confirm (and pin with the ordering test) that NOTHING DONE-bearing precedes the two series persists — the composite_status flip (:4626-4643) must remain the last write; if any earlier write carries computation_status, restructure it below the series persists (read :4447-4643 carefully; the prestamp pattern used in single-key does NOT exist here — verify). Tests in test_stitch_composite_job.py mirror :573's capture-order technique for the three behaviors; kill-point uses a side_effect-raising RPC mock scoped to the cash kind. Name neuters in docstrings (reorder → RED; swallow the persist failure → kill-point RED).</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_stitch_composite_job.py tests/test_composite_headline_parity.py -x</automated>
  </verify>
  <acceptance_criteria>Ordering + kill-point + heal tests green; existing test_mtm_series_persists_before_done_scalar_write green unmodified; full quick-run `python -m pytest tests/test_basis_series.py tests/test_cash_basis_series_sc4.py tests/test_stitch_composite_job.py -x` green; full suite `python -m pytest --cov --cov-fail-under=80` green (phase gate) and `! git grep -q _metrics_result_for` still holds.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker → strategy_analytics + _series + csv_daily_returns | multi-table finalize sequence without a cross-table transaction |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-13 | Integrity | complete scalar without its series (worker death mid-finalize) | mitigate | D5 ordering: series + dailies before the DONE-bearing flip; kill-point test |
| T-105-14 | Integrity | re-derive death window (old scalar + partial dailies) | accept | PRE-EXISTING (:4466-4492); documented accepted-transient; strict SECDEF RPC deferred to 106 (would be prod DDL) |
| T-105-15 | Integrity | silent √252 annualization of a wrong-asset_class composite | mitigate | D4 retained fail-loud clock-disagreement assert + test |
| T-105-16 | DoS | bare ValueError retry-burn on chain-break | mitigate | F-5 arm re-homed onto the derive's ValueError, PERMANENT disposition preserved |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_stitch_composite_job.py tests/test_composite_headline_parity.py tests/test_basis_series.py tests/test_cash_basis_series_sc4.py -x
cd analytics-service && python -m pytest --cov --cov-fail-under=80
! git grep -q _metrics_result_for
</verification>

<success_criteria>
- _metrics_result_for gone repo-wide (grep-gate == 0); composite cash on the one route; scalars a cache of the persisted series.
- One periods_per_year convention with the retained fail-loud check; equality proven across existing fixtures.
- Ordered-idempotent finalize with ordering + kill-point proofs; accepted-transient documented; zero DDL.
- Composite SC-4 flagship (member-guard-NaN) dual-run DICT-EQUAL; headline==by-basis preserved.
</success_criteria>

<output>
Create `.planning/phases/105-composite-the-one-csv-finalize-route/105-05-SUMMARY.md` when done.
</output>
