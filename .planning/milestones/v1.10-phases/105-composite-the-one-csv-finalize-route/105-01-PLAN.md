---
phase: 105-composite-the-one-csv-finalize-route
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/basis_series.py
  - analytics-service/tests/test_basis_series.py
  - analytics-service/tests/test_derive_broker_dailies_dualmode.py
autonomous: true
requirements: [BB-02]

must_haves:
  truths:
    - "derive_basis_series called WITHOUT scalar_returns/densify_policy is byte-identical to today for MTM and every current caller (conventions carries NO densify key; result.nan_dates is None; the persist payload carries NO nan_dates key). Pre-existing tests stay green; the ONLY edits are the payload-internal `schema` constant expectations at test_basis_series.py:309/:378 (full-dict payload asserts) + test_derive_broker_dailies_dualmode.py:994 (`cash[\"schema\"] == 2`) — reader-invisible (factsheet readers ignore `schema`; NOT an SC-4 violation)"
    - "derive_basis_series(scalar_returns=S) computes metrics_json on EXACTLY S (never on gap_fill(_drop_nonfinite(returns))) while series_rows/gap_spans stay derived from _drop_nonfinite(returns) — rows and scalar-input are decoupled (D1)"
    - "densify_policy='zero_fill' with an in-index-NaN scalar_returns yields nan_dates = the ISO dates of those NaN positions, carried as an ADDITIVE top-level payload key beside rows/gap_spans/conventions, with _PAYLOAD_SCHEMA_VERSION bumped to 2 (NO DDL)"
    - "The round-trip guard reconstructs the scalar input per densify policy — sparse: rows verbatim; broker_nan: reindex(date_range(min,max)); zero_fill: gap_fill(rows) then reinstate NaN at nan_dates (union-reindex when a nan_date falls outside the rebuilt span) — and stays GREEN on all 3 policies incl. the composite guard-NaN flagship; dropping nan_dates from the zero_fill reconstruction → RED"
  artifacts:
    - path: "analytics-service/services/basis_series.py"
      provides: "scalar_returns + densify_policy kwargs, nan_dates + insufficient_window fields on BasisSeriesResult, conventions.densify echo, payload nan_dates key, _PAYLOAD_SCHEMA_VERSION=2"
      contains: "scalar_returns"
    - path: "analytics-service/tests/test_basis_series.py"
      provides: "densify_policy-aware _roundtrip_recompute + byte-invisible-default tests + the 3-policy round-trip fixtures incl. composite guard-NaN (D1 flagship) + simple/active denominator round-trip; the two full-dict payload asserts at :309/:378 updated schema 1→2"
      contains: "densify"
    - path: "analytics-service/tests/test_derive_broker_dailies_dualmode.py"
      provides: "the live test_strategy_mode_persists_cash_settlement_series schema expectation at :994 updated 1→2 (schema-pinned; goes RED on the bump otherwise, orphaned under the full-suite cov gate)"
      contains: "schema"
  key_links:
    - from: "analytics-service/services/basis_series.py"
      to: "persist payload"
      via: "nan_dates emitted only when non-None (additive JSONB key, schema bump)"
      pattern: "nan_dates"
    - from: "analytics-service/tests/test_basis_series.py"
      to: "analytics-service/services/basis_series.py"
      via: "_roundtrip_recompute branches on r.conventions.get('densify')"
      pattern: "densify"
---

<objective>
BB-02, Phase 105 Wave 1 — the D1 SC-4 mechanism (LOCKED, do not re-open): decouple the honest sparse persisted ROWS from the SCALAR-input series in `derive_basis_series` so cash callers can pass the exact legacy-conditioned series and reproduce legacy scalars byte-identically by construction, while MTM and every current caller stay byte-identical by default.

This is the single `derive_basis_series` change every downstream plan (105-03 seam, 105-04 runner swap, 105-05 composite re-route) builds on. It also adds the D1 CRITICAL amendment: the composite `zero_fill` payload carries an additive `nan_dates` key so the round-trip guard can reconstruct member-guard NaN — without it the guard reddens on the composite guard-NaN fixture.

HARD BOUNDARIES (locked):
- NO DDL. `nan_dates` is a JSONB-additive payload key + `_PAYLOAD_SCHEMA_VERSION` bump (basis_series.py:84 → 2). `strategy_analytics_series` schema untouched. If you conclude DDL is needed, STOP and flag.
- The derive stays SINGLE and BRANCHLESS: no venue/source `if` inside it. The DECISION of which `scalar_returns` to build lives upstream in the callers (collapse #6, later plans).
- `compute_all_metrics`, `gap_fill_daily_returns`, `_drop_nonfinite` untouched (no new valuation math).
- The round-trip guard is NEVER weakened/skipped for composites — it must hold exactly (dict-equal) on all three densify policies.

Purpose: the SC-4 core — every re-route in Waves 2–3 is byte-identical BY CONSTRUCTION because of this param.
Output: extended `derive_basis_series` + `BasisSeriesResult` + payload; densify-aware round-trip guard + policy fixtures in `test_basis_series.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md
@.planning/phases/105-composite-the-one-csv-finalize-route/105-RESEARCH.md (§THE CENTRAL ANSWER + §Code Examples)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md (§basis_series.py + §test_basis_series.py)

Executor constraints: NO git branch operations (only `git add <explicit paths>`); never stage `.planning/`. No Supabase MCP — all verification is pytest against fixtures. Use Edit (never Write) on any existing `.planning/` doc.

<interfaces>
From analytics-service/services/basis_series.py (read whole file, 256 lines):
- `derive_basis_series(returns, benchmark_rets, *, periods_per_year, cumulative_method, day_basis, benchmark_symbol=None) -> BasisSeriesResult` — today: `sparse = _drop_nonfinite(returns).sort_index()`; ValueError <2 rows (:150-153); `dense = gap_fill_daily_returns(sparse)` → `compute_all_metrics(dense, ...)` (:155-162); rows/gap_spans from sparse; conventions 3 keys + optional `benchmark` (:186-187).
- `BasisSeriesResult` frozen dataclass (:87-121): metrics_json, sibling_kinds, series_rows, gap_spans, conventions.
- `persist_basis_series` payload (:245-251): `{schema, basis, rows, gap_spans, conventions}`; `_PAYLOAD_SCHEMA_VERSION = 1` (:84).
- DOWNSTREAM DUCK-TYPE REQUIREMENT (verified this session): the composite re-route (job_worker.py:4586) and the runner swap (analytics_runner.py:2399) consume `metrics_result.insufficient_window` off the compute result. `compute_all_metrics` returns `MetricsResult` (metrics.py:258) carrying `insufficient_window`; `BasisSeriesResult` does NOT. This plan MUST add an `insufficient_window: bool = False` pass-through field (NOT persisted in the payload) or Plans 04/05 cannot swap without behavior loss.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scalar_returns + densify_policy + nan_dates + insufficient_window to the derive (byte-invisible by default)</name>
  <files>analytics-service/services/basis_series.py, analytics-service/tests/test_basis_series.py, analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>analytics-service/services/basis_series.py (whole file, incl. the :16-20 module docstring), analytics-service/tests/test_basis_series.py:177-223 (the Phase-104 additive-kwarg test trio — the exact template) AND :305-315 + :375-385 (the two full-dict payload asserts pinning `"schema": 1`), analytics-service/tests/test_derive_broker_dailies_dualmode.py:940-996 (the live cash-persist test asserting `cash["schema"] == 1` at :994), .planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md §basis_series.py</read_first>
  <behavior>
    - Default path byte-invisible: calling with neither new kwarg reproduces today's result exactly — conventions has NO "densify" key, result.nan_dates is None, result.insufficient_window mirrors the MetricsResult flag, and the persist payload dict carries NO "nan_dates" key. The bump to `_PAYLOAD_SCHEMA_VERSION = 2` changes ONLY the payload's internal `schema` field (reader-invisible): three pre-existing schema-pinned asserts MUST be scoped-in and updated 1→2 — test_basis_series.py:309 (MTM full-dict payload), test_basis_series.py:378 (cash full-dict payload), test_derive_broker_dailies_dualmode.py:994 (`cash["schema"] == 2`). No OTHER pre-existing test changes.
    - scalar_returns honored: with a sparse fixture and scalar_returns=that-sparse-series, metrics_json == compute_all_metrics(sparse_series, ...).metrics_json (NOT the gap-filled recompute); series_rows/gap_spans still derived from _drop_nonfinite(returns).
    - densify echo: densify_policy="sparse" → conventions["densify"] == "sparse" and the other 3 (or 4 with benchmark) keys unchanged; omitted → key absent (mirror test_conventions_echo_omits_benchmark_by_default :212).
    - Unknown policy fails loud: densify_policy="bogus" raises ValueError naming the allowed set {sparse, broker_nan, zero_fill}.
    - nan_dates: densify_policy="zero_fill" + scalar_returns carrying in-index NaN → result.nan_dates == sorted ISO dates of those NaN positions; any other policy (or scalar_returns=None) → nan_dates is None.
    - Payload additive: the persist payload includes "nan_dates" ONLY when result.nan_dates is not None; payload["schema"] == 2 always (bump).
  </behavior>
  <action>Copy the Phase-104 additive-kwarg discipline already in this file (benchmark_symbol, :131 + :186-187). Add keyword-only params `scalar_returns: pd.Series | None = None` and `densify_policy: str | None = None` to `derive_basis_series` (:124-132). Replace the hardcoded scalar input at :155: scalar_input = scalar_returns when not None, else today's `gap_fill_daily_returns(sparse)` — the rows/gap_spans pipeline (:164-173) stays on `sparse` untouched. Validate densify_policy against the closed set {"sparse","broker_nan","zero_fill"} (ValueError otherwise, V5 fail-loud). Echo `conventions["densify"] = densify_policy` when not None, exactly the :186-187 shape. Compute `nan_dates` (sorted ISO date strings of in-index NaN positions of scalar_returns) ONLY when densify_policy=="zero_fill" and scalar_returns is not None — else None. Extend the frozen dataclass with `nan_dates: list[str] | None = None` and `insufficient_window: bool = False` (defaulted last-position fields, matching the frozen style at :87-121); populate insufficient_window from the MetricsResult so Plans 04/05 can duck-swap (per D1 + downstream consumption at job_worker.py:4586 / analytics_runner.py:2399). In `persist_basis_series` (:245-251) add `payload["nan_dates"] = result.nan_dates` ONLY when non-None, and bump `_PAYLOAD_SCHEMA_VERSION` to 2 at :84 (D1: additive JSONB key + version bump is NOT a migration — parseMtmSeriesPayload consumes only rows/gap_spans, no strict schema check). Update the module/dataclass docstrings to document the D1 rows-vs-scalar-input decoupling and the per-policy reconstruction contract. Rewrite the :16-20 module docstring as part of the D1 edit: it currently says "do NOT bolt derive logic onto the composite's bespoke `_metrics_result_for` path (that path is what the backbone merge deletes)" — this is the LAST textual `_metrics_result_for` reference outside 105-05's files (git grep hit at basis_series.py:20). PURGE the token here (rewrite to describe the shared-route reality post-105 — cash + composite now route through this helper; the bespoke path is deleted in 105-05) so 105-05's locked `git grep _metrics_result_for | wc -l == 0` gate is achievable. Update the three schema-pinned asserts to `2`: test_basis_series.py:309 and :378 (the full-dict payload `"schema": 1` → `2`) and test_derive_broker_dailies_dualmode.py:994 (`cash["schema"] == 1` → `2`). Do NOT touch _KIND_BY_BASIS, compute_all_metrics, gap_fill_daily_returns, _drop_nonfinite.</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_basis_series.py -x</automated>
  </verify>
  <acceptance_criteria>Pre-existing tests in test_basis_series.py pass with the ONLY edits being the payload-internal `schema` 1→2 at :309/:378 (reader-invisible; the byte-invisible default = SC-4 safety for published MTM is proven by the conventions/nan_dates asserts, NOT by the schema field). test_derive_broker_dailies_dualmode.py:994 updated to `== 2` and green. New tests cover every behavior bullet and each names its neuter target (e.g. "always-emit densify key → RED", "compute scalar from gap_fill(sparse) despite scalar_returns → RED", "emit nan_dates under a non-zero_fill policy → RED"). grep gates: `grep -c "scalar_returns" analytics-service/services/basis_series.py` ≥ 3; `grep -c "_PAYLOAD_SCHEMA_VERSION = 2" analytics-service/services/basis_series.py` == 1; `git grep _metrics_result_for -- analytics-service/services/basis_series.py | wc -l` == 0 (the :20 docstring token purged).</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Densify-policy-aware round-trip guard + the 3-policy fixtures (composite guard-NaN flagship)</name>
  <files>analytics-service/tests/test_basis_series.py</files>
  <read_first>analytics-service/tests/test_basis_series.py:73-114 (_roundtrip_recompute + the two round-trip tests), analytics-service/services/basis_series.py (post-Task-1), analytics-service/services/broker_dailies.py:123-137 (gap_fill_daily_returns semantics: fills ABSENT index positions only, preserves in-index NaN), .planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md §D1</read_first>
  <behavior>
    - _roundtrip_recompute reconstructs the scalar input per r.conventions.get("densify"): absent/None → today's gap_fill(rebuilt) (MTM unchanged); "sparse" → rebuilt verbatim; "broker_nan" → rebuilt.reindex(pd.date_range(min,max)) (every in-span absence becomes NaN); "zero_fill" → gap_fill(rebuilt), then union-reindex with DatetimeIndex(r.nan_dates) (a nan_date OUTSIDE [first_row,last_row] must extend the index), then set NaN at every nan_date.
    - sparse round-trip GREEN: a weekend-gapped sparse fixture (user-CSV shape, √252) derived with scalar_returns=sparse + densify_policy="sparse" satisfies _roundtrip_recompute(r) == r.metrics_json.
    - broker_nan round-trip GREEN: a dense-with-interior-NaN fixture (broker guard-day shape) derived with scalar_returns=dense_with_nan + densify_policy="broker_nan" round-trips dict-equal.
    - zero_fill+nan_dates round-trip GREEN (D1 FLAGSHIP): a stitched-composite-shaped fixture — inter-member ABSENT days + an in-index member-guard NaN — derived with scalar_returns=gap_fill(stitched) + densify_policy="zero_fill" round-trips dict-equal; a second case places a guard NaN at the stitched series EDGE (leading/trailing) and still round-trips (union-reindex path).
    - nan_dates neuter RED: the same flagship reconstruction WITHOUT reinstating NaN (plain gap_fill(rebuilt), i.e. the pre-D1 reconstruction) does NOT equal r.metrics_json — asserted explicitly so dropping nan_dates can never pass silently.
    - simple/active denominator round-trip GREEN: a fixture derived under cumulative_method="simple", day_basis="active" (the Zavara/ccxt-override convention pair) round-trips dict-equal — proving the guard mechanism holds under the MED-2 conventions (the seam WIRING test is Plan 03's).
  </behavior>
  <action>Extend `_roundtrip_recompute` (:73-90) to branch on the densify echo exactly as PATTERNS §test_basis_series.py specifies — the default (no "densify" key) branch MUST stay byte-identical to today so the existing MTM/cash round-trip tests (:93-114) pass unmodified. Build the fixtures from the divergence table in 105-RESEARCH §THE CENTRAL ANSWER: the flagship stitched fixture must make the 3 shapes distinguishable (a 0.0-bridged guard day yields DIFFERENT vol/Sharpe than a preserved NaN break — assert the neuter case is unequal, never assume). Use dict-equality on metrics_json (the existing guard style), NOT tolerant comparison — the guard is never weakened for composites (D1). Name every neuter target in docstrings (kill: drop nan_dates / wrong policy branch / reconstruct broker_nan with gap_fill).</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_basis_series.py -k "roundtrip or densify or denominator" -x</automated>
  </verify>
  <acceptance_criteria>All new round-trip tests green; the flagship test contains an explicit inequality assertion for the nan_dates-dropped reconstruction (RED-path pinned in the same test, not a comment); existing tests :93-114 unmodified and green; full file run `python -m pytest tests/test_basis_series.py -x` green.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker → strategy_analytics_series (SECDEF RPC) | payload shape changes (nan_dates key, schema 2) cross into persisted JSONB |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Tampering | derive_basis_series densify_policy | mitigate | closed-set validation (ValueError on unknown policy); values are code-controlled constants, never user input |
| T-105-02 | Integrity | nan_dates payload key | mitigate | computed server-side from scalar_returns NaN positions; additive-only; schema version bump lets readers detect shape |
| T-105-03 | Tampering | prod DDL auto-apply | mitigate | NO migration in this plan — JSONB-additive key only; executor STOPS and flags if DDL appears needed |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_basis_series.py tests/test_cash_basis_series_sc4.py -x  # sc4 suite must stay green (dual-run byte-identity unaffected by the schema bump — both runs on new code)
cd analytics-service && python -m mypy services/basis_series.py --strict 2>/dev/null || python -m mypy services/basis_series.py
</verification>

<success_criteria>
- Default-path byte-invisibility proven (existing tests unmodified + green).
- All 3 densify policies round-trip dict-equal; the composite guard-NaN flagship GREEN with nan_dates and RED without (explicit assertion).
- _PAYLOAD_SCHEMA_VERSION == 2; nan_dates additive-only; no DDL, no migration file created.
- BasisSeriesResult exposes insufficient_window (duck-compat for Plans 04/05).
</success_criteria>

<output>
Create `.planning/phases/105-composite-the-one-csv-finalize-route/105-01-SUMMARY.md` when done.
</output>
