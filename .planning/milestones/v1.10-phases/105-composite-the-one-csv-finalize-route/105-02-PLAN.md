---
phase: 105-composite-the-one-csv-finalize-route
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/factsheet/composite-read-path.test.ts
  - .planning/phases/105-composite-the-one-csv-finalize-route/105-FOLD-DECISION.md
autonomous: true
requirements: [BB-02]

must_haves:
  truths:
    - "A read-side status-gate predicate for the cash_settlement series exists, colocated with shouldReadSingleKeyMtmSeries (composite-read-path.ts:401-416), exported, and returns true ONLY when computation_status ∈ {complete, complete_with_warnings} AND metrics_json_by_basis carries a non-null non-array cash_settlement object — the MED-1 single choke point covering ALL terminal-failure arms including future ones"
    - "The predicate has NO production caller this phase (the 106 cash reader is its first) — no factsheet surface behavior changes; frontend payloads stay byte-identical"
    - "The series-store fold DECISION (D6) is locked in a written decision doc: tall daily_returns + basis column survives (NOT a JSONB blob), MTM kinds fold in, ~66-file reader inventory recorded, migration shape sketched, NO migration written this phase"
  artifacts:
    - path: "src/lib/factsheet/composite-read-path.ts"
      provides: "shouldReadCashSettlementSeries predicate (MED-1 read-gate, D3 primary fix)"
      contains: "shouldReadCashSettlementSeries"
    - path: "src/lib/factsheet/composite-read-path.test.ts"
      provides: "predicate truth-table tests mirroring the shouldReadSingleKeyMtmSeries suite"
      contains: "shouldReadCashSettlementSeries"
    - path: ".planning/phases/105-composite-the-one-csv-finalize-route/105-FOLD-DECISION.md"
      provides: "locked D6 fold decision + reader inventory + migration shape + 106 execution contract"
      contains: "daily_returns"
  key_links:
    - from: "src/lib/factsheet/composite-read-path.ts"
      to: "computation_status"
      via: "terminal-success DONE gate (complete | complete_with_warnings)"
      pattern: "complete_with_warnings"
    - from: ".planning/phases/105-composite-the-one-csv-finalize-route/105-FOLD-DECISION.md"
      to: "src/lib/factsheet/composite-read-path.ts"
      via: "locked caveat: the 106 cash reader MUST route through the predicate family"
      pattern: "shouldReadCashSettlementSeries"
---

<objective>
BB-02, Phase 105 Wave 1 — the two Phase-104-carry satellites that are independent of the derive mechanism: (a) MED-1's PRIMARY fix, the read-side status-gate predicate for the cash series (D3 LOCKED: gate primary, heal-deletes secondary — those land in Plan 03); (b) the series-store fold DECISION doc (D6 LOCKED: decide only, EXECUTE in 106 — write NO migration, repoint NO reader).

Why MED-1 is a predicate with no caller: a stale `cash_settlement` series row can outlive an authoritative-NULL scalar at pre-seam terminal-failure arms (dark today, DETONATES when 106 lights the cash reader). The exhaustive reader sweep (D3) found the only basis-series readers are `readMtmSeries` surfaces already gated by `shouldReadSingleKeyMtmSeries`. The cash twin must exist NOW, be the locked contract for the 106 reader, and be what the 105 test harnesses respect (Plan 03 wires harness gate-respect).

HARD BOUNDARIES (locked):
- NO production wiring of the predicate this phase (no factsheet surface change; VALIDATION.md: "no frontend surface in 105" means no behavior change — adding an exported, tested, uncalled predicate is the D3 deliverable).
- The fold doc DECIDES; it must NOT contain a migration file, DDL, or reader repoints. If you conclude DDL is needed now, STOP and flag.
- LOW-4 (locked context): do NOT cite the INERT-read grep tripwire as proof no reader landed — it misses a reader imported via a constant. The predicate + status-gate is the guarantee, and the doc must say so.

Purpose: MED-1's guarantee exists before any plan makes cash scalars a cache; 106 inherits a locked fold contract instead of renegotiating.
Output: `shouldReadCashSettlementSeries` + tests; `105-FOLD-DECISION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md (§D3 + §D6)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-RESEARCH.md (§MED-1 + §Series-store fold — DECISION)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md (§composite-read-path.ts)

Executor constraints: NO git branch operations (only `git add <explicit paths>`); never stage `.planning/` (the FOLD-DECISION doc is a NEW local planning file — create it, do NOT git add it). Use Edit (never Write) on any EXISTING `.planning/` doc.

<interfaces>
From src/lib/factsheet/composite-read-path.ts (read :330-416):
- `shouldReadSingleKeyMtmSeries(metricsJsonByBasis: unknown, computationStatus: unknown): boolean` (:401-416) — the EXACT twin to mirror: DONE gate `computationStatus === "complete" || computationStatus === "complete_with_warnings"` (:405), then non-null non-array object check on the raw jsonb, then the same check on its `mark_to_market` value.
- `singleKeyBasisOpts` (:342-385) — the strict-coercion discipline (thread ONLY the intended key, never raw `metrics_json_by_basis`).
- `readMtmSeries` (:94-114) — the degrade-never-throw reader shape the 106 cash reader will mirror (documented in the fold doc, NOT built now).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shouldReadCashSettlementSeries — the MED-1 read-side status-gate predicate</name>
  <files>src/lib/factsheet/composite-read-path.ts, src/lib/factsheet/composite-read-path.test.ts</files>
  <read_first>src/lib/factsheet/composite-read-path.ts:330-416, src/lib/factsheet/composite-read-path.test.ts (the existing shouldReadSingleKeyMtmSeries test block — mirror its truth-table style), .planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md §D3</read_first>
  <behavior>
    - Returns false for every non-terminal-success status: "failed", "computing", "pending", null, undefined, 0, "" (the MED-1 guarantee: a stale series row after ANY terminal-failure arm is never trusted).
    - Returns false when metricsJsonByBasis is null / not an object / an array / lacks cash_settlement / cash_settlement is null / an array / a scalar.
    - Returns true ONLY for status "complete" or "complete_with_warnings" AND a non-null non-array cash_settlement object.
    - shouldReadSingleKeyMtmSeries behavior unchanged (existing tests green unmodified).
  </behavior>
  <action>Add `export function shouldReadCashSettlementSeries(metricsJsonByBasis: unknown, computationStatus: unknown): boolean` directly below `shouldReadSingleKeyMtmSeries` (:416), copying its exact shape (D3: predicate colocated at :405's DONE-gate) with `cash_settlement` in place of `mark_to_market`. Do NOT refactor the two into a shared helper-with-key-param unless the diff stays trivially reviewable — if you do extract, both predicates' existing tests must pass unmodified (surgical-changes rule). JSDoc must state: (1) this is the MED-1 single choke point — a stale cash_settlement series row (terminal-failure arms job_worker.py:2790-2821 / _stamp_deribit_analytics_failed, or a BROKER_DAILIES_VIA_FUNDING=false rollback orphan, LOW-3) is refused HERE regardless of which arm produced it; (2) the Phase-106 cash reader is REQUIRED to route through this predicate family (D3 caveat a — cite 105-FOLD-DECISION.md); (3) no caller exists in 105 by design. Tests: mirror the existing predicate's truth-table block; every case in the behavior list gets an assertion; name the neuter target ("drop the DONE gate → RED on the failed-status case").</action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/composite-read-path.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>All existing composite-read-path tests green unmodified; new truth-table tests cover every listed case; `npx tsc --noEmit` clean; grep gate: `grep -c "shouldReadCashSettlementSeries" src/lib/factsheet/composite-read-path.ts` ≥ 2 (definition + JSDoc/family reference) and no production caller: `grep -rl "shouldReadCashSettlementSeries" src/app/ | wc -l` == 0.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Write 105-FOLD-DECISION.md — lock the D6 series-store fold (decide-only)</name>
  <files>.planning/phases/105-composite-the-one-csv-finalize-route/105-FOLD-DECISION.md</files>
  <read_first>.planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md §D6, .planning/phases/105-composite-the-one-csv-finalize-route/105-RESEARCH.md §Series-store fold — DECISION (reader inventory) + §Security Domain, .planning/ROADMAP.md §Phase 106</read_first>
  <action>Create the decision doc (NEW file — Write is fine; never Write over an existing .planning doc) with these REQUIRED sections, content sourced from the locked D6 + research inventory — this doc is the 106 execution contract: (1) **Decision (LOCKED)**: the tall table SURVIVES — rename `csv_daily_returns` → `daily_returns`, add a `basis` column (default `'cash_settlement'`), fold the `strategy_analytics_series` daily-return kinds (mtm_daily_returns + cash_settlement) IN; a per-strategy JSONB blob CANNOT serve the `allocator_id`+date-range GDPR/per-key/admin queries, the per-key axis (migration 20260624120000), the allocator-date index (20260625120000), the owner-coherence trigger, or per-key RLS. (2) **Reader inventory**: the ~66-file `grep -rln csv_daily_returns` breakdown from 105-RESEARCH (frontend ~12 files incl. queries.ts/composite-read-path.ts/compute.ts/strategyGate.ts/compositeAttribution.ts/gdpr-export-manifest.ts/database.types.ts/wizard/discovery/factsheet-v2/admin-review/csv-finalize; backend 4: analytics_runner.py/job_worker.py/long_fetch.py/csv_validator.py; SQL: persist_csv_daily_returns SECDEF, owner-coherence trigger, 2 index migrations, RLS test; GDPR: 2 export axes). Re-run the grep at write time and record the CURRENT count. (3) **Migration shape (SKETCH ONLY — executed in 106)**: rename + basis column + PK/index adjustments + RLS/SECDEF/trigger carry + backfill of the folded kinds + the 2 GDPR axes repoint, with the test-project MCP catch-up + migration-reviewer + rls-policy-auditor obligations flagged. (4) **Locked caveats**: (a) the 106 cash reader MUST route through the `shouldReadCashSettlementSeries` predicate family (D3); (b) GDPR-completeness bonus — `strategy_analytics_series` is ABSENT from the GDPR export manifest today; folding user daily returns into the tall table makes them exportable via existing axes; (c) the strict-atomicity finalize SECDEF RPC (D5 honest boundary) OPTIONALLY rides 106's fold migration, never 105; (d) benchmark identity + densify/nan_dates conventions travel with the folded rows. (5) **Non-goals of 105**: no DDL, no reader repoints, no flag flip. Keep it ≤ ~120 lines — a contract, not an essay.</action>
  <verify>
    <automated>test -f .planning/phases/105-composite-the-one-csv-finalize-route/105-FOLD-DECISION.md && grep -c "daily_returns" .planning/phases/105-composite-the-one-csv-finalize-route/105-FOLD-DECISION.md</automated>
  </verify>
  <acceptance_criteria>Doc exists with all 5 required sections (grep each heading); contains "shouldReadCashSettlementSeries" (D3 caveat) and "basis" column decision; contains NO CREATE/ALTER SQL statements intended for execution (sketch prose/pseudo only — grep-gate: `grep -c "^CREATE\|^ALTER" 105-FOLD-DECISION.md` == 0); NOT staged in git.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| DB series row → factsheet reader | untrusted-shaped jsonb + possibly-stale rows cross into render path (106) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | Info disclosure / Integrity | stale cash series row surfaced as a wrong money number | mitigate | shouldReadCashSettlementSeries DONE-gate (this plan) — single choke point; 106 reader contractually routes through it |
| T-105-05 | Tampering | malformed jsonb metrics_json_by_basis | mitigate | strict non-null/non-array coercion copied from :407-415 |
| T-105-06 | Info disclosure | GDPR export-axis breakage on the fold | accept (105) | fold is decide-only; 106 repoints both GDPR axes with tests (recorded in the decision doc) |
</threat_model>

<verification>
npx vitest run src/lib/factsheet/composite-read-path.test.ts --no-file-parallelism
npx tsc --noEmit
</verification>

<success_criteria>
- Predicate exists, exported, colocated, fully truth-tabled, zero production callers.
- Fold decision locked in writing with reader inventory + migration sketch + the 4 locked caveats; no DDL anywhere.
- No frontend behavior change (existing tests unmodified).
</success_criteria>

<output>
Create `.planning/phases/105-composite-the-one-csv-finalize-route/105-02-SUMMARY.md` when done.
</output>
