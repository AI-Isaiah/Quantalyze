---
phase: 105-composite-the-one-csv-finalize-route
plan: 03
type: execute
wave: 2
depends_on: [105-01]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_cash_basis_series_sc4.py
autonomous: true
requirements: [BB-02]

must_haves:
  truths:
    - "MED-2 closed: the single-key seam's cash conventions echo (job_worker.py:3193-3198) resolves returns_denominator_config from the strategy row VENUE-AGNOSTICALLY — a ccxt strategy with a simple/active override echoes cumulative_method=simple, day_basis=active (exactly analytics_runner.py:2304-2316), no longer geometric/calendar"
    - "Deribit-path behavior is byte-identical after the hoist: the malformed-config PERMANENT disposition is preserved, pnl_basis/exclude_spot_extraction/combine_native_ledger receive the same config, and the existing seam dual-run byte-identity test stays green"
    - "The seam cash derive passes scalar_returns (the dense-with-NaN post-terminus series) + densify_policy='broker_nan', so the persisted echo lets the round-trip guard / 106 reader rebuild the exact scalar input from the sparse rows (collapse #6: the per-source conditioning lives upstream, the derive stays branchless)"
    - "Defense-in-depth heal-deletes (D3 secondary): _stamp_deribit_analytics_failed and the <2-interpretable-days terminal arm heal-DELETE both series rows (cash_settlement AND mtm_daily_returns) so a stale row does not outlive the authoritative-NULL scalar write — the gate (Plan 02) remains the guarantee"
    - "The SC-4/round-trip test harness itself respects the MED-1 gate (D3 caveat b): it only trusts a captured series payload when the captured computation_status is terminal-success, and expects-absent after a terminal-failure arm"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "venue-agnostic branch-outer denominator parse; scalar_returns+densify_policy on the seam cash derive; heal-deletes at terminal arms"
      contains: "densify_policy=\"broker_nan\""
    - path: "analytics-service/tests/test_cash_basis_series_sc4.py"
      provides: "ccxt-override echo test (MED-2), broker_nan densify-tag test, terminal-arm heal tests, gate-respecting harness helper"
      contains: "broker_nan"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "analytics-service/services/basis_series.py"
      via: "derive_basis_series(returns, ..., scalar_returns=returns, densify_policy='broker_nan')"
      pattern: "broker_nan"
    - from: "analytics-service/services/job_worker.py"
      to: "parse_returns_denominator_config"
      via: "ONE venue-agnostic parse at branch-outer scope feeding echo + deribit consumers"
      pattern: "parse_returns_denominator_config"
---

<objective>
BB-02, Phase 105 Wave 2 — collapse #6 at the single-key broker-derive seam + MED-2 + the D3 secondary heal-deletes. The venue-source fork (deribit `combine_native_ledger` vs ccxt `combine_realized_and_funding`) already returns byte-identical `(returns, meta)` shape and feeds ONE derive at job_worker.py:3200 — #6 here is the remaining REAL fork: `returns_denominator_config` is parsed ONLY inside the deribit arm (:2162), so a ccxt strategy with an override echoes wrong conventions (MED-2, detonates when the round-trip guard covers cash). Move the parse upstream, venue-agnostic, feeding the single derive — no venue/denominator branching remains inside the derive path (ROADMAP SC-3).

VERIFIED THIS SESSION (executor: trust these, re-verify with grep before editing):
- `denominator_config` is initialized branch-outer at :2051 (None) and ASSIGNED only in the deribit arm at :2162.
- Its consumers are ALL deribit-arm-internal (pnl_basis :2179, exclude_spot_extraction :2192, combine_native_ledger :2249 + the MTM second pass :2373) — EXCEPT the post-branch echoes (:2975 MTM, :3193 cash). The ccxt arm's `combine_realized_and_funding` (:2664) takes NO denominator_config.
- Therefore the hoist changes ccxt behavior ONLY at the echoes (the intended MED-2 fix; zero prod ccxt-override rows today) plus makes a malformed ccxt config fail loud (parity with analytics_runner, which already fails PERMANENT on the same config).

HARD BOUNDARIES (locked):
- SCALARS at this seam stay non-authoritative (the metrics_json here is discarded by persist) — the authoritative single-key cash scalars flip in Plan 04 (analytics_runner). Do NOT touch analytics_runner.py in this plan.
- Deribit-path byte-identity is an SC-4 gate: the existing dual-run test (test_sc4_cash_series_dual_run_byte_identity) must stay green with zero fixture edits.
- The composite region (:4150+) is Plan 05's — do not touch it.
- NO DDL.

Purpose: MED-2 closed + the per-source conditioning upstream = the #6 preparation pattern the runner (04) and composite (05) mirror.
Output: hoisted parse, densify-tagged seam derive, heal-deletes, gate-respecting harness.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md (§D3 + §specifics)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-RESEARCH.md (§MED-1 + §MED-2 + §Collapse #6)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md (§job_worker.py + §test_cash_basis_series_sc4.py)

Executor constraints: NO git branch operations (only `git add <explicit paths>`); never stage `.planning/`. No Supabase MCP — pytest against the mocked seam harness only.

<interfaces>
From analytics-service/services/basis_series.py (post-105-01):
- `derive_basis_series(..., scalar_returns: pd.Series | None = None, densify_policy: str | None = None)` — pass scalar_returns=the-dense-with-NaN series + densify_policy="broker_nan"; conventions gains {"densify": "broker_nan"}.
- `persist_basis_series(supabase, strategy_id, *, basis, result)` — result=None DELETES the (strategy_id, kind) row (the HEAL primitive; kinds via _KIND_BY_BASIS: "cash_settlement", "mark_to_market").

From analytics-service/services/analytics_runner.py:2299-2316 (the MED-2 reference implementation to mirror — read only, do not edit):
- `periods_per_year_for_asset_class(row.get("asset_class"))`; defaults geometric/calendar; `parse_returns_denominator_config(row.get("returns_denominator_config"))` VENUE-AGNOSTIC; override → cumulative_method + metrics_day_basis(metrics_basis); malformed → PERMANENT.

From analytics-service/tests/test_cash_basis_series_sc4.py:69-171 (the seam harness):
- `_run_seam(strategy_row, *, has_option_activity, cash_noop=False, ...)` → capture dict; `_cash_series_payload(capture)` → the cash JSONB payload; `_noncash_track(capture)` → everything SC-4 protects; `_find_prestamp` from tests.test_mtm_single_key.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MED-2 — hoist the returns_denominator_config parse to branch-outer, venue-agnostic scope</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_cash_basis_series_sc4.py</files>
  <read_first>analytics-service/services/job_worker.py:2040-2200 (init :2051, deribit parse+failure arm :2155-2178, consumers :2179-2192) and :3157-3220 (the 104 cash echo + persist), analytics-service/services/analytics_runner.py:2299-2316 (reference impl), .planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md §job_worker "#6 fork-collapse + MED-2"</read_first>
  <behavior>
    - ccxt strategy row with returns_denominator_config override (Zavara-style simple/active) → the captured cash_settlement payload's conventions echo cumulative_method="simple", day_basis="active" (was geometric/calendar — the MED-2 bug, killed).
    - ccxt strategy row with NO override → conventions echo geometric/calendar (byte-identical to today).
    - deribit strategy row with override → conventions echo unchanged vs today (already correct); the existing dual-run byte-identity test green with zero fixture edits.
    - Malformed config on EITHER venue (strategy-mode) → PERMANENT DispatchResult + strategy_analytics stamped failed (the deribit arm's existing disposition, now venue-wide — parity with analytics_runner's B2 handling).
  </behavior>
  <action>Move the `parse_returns_denominator_config` call from the deribit arm (:2161-2178) to the branch-outer initialization site (:2046-2051, replacing the `denominator_config = None` + its Phase-104 comment), BEFORE the `if venue == "deribit":` branch. The parse + its `ReturnsDenominatorConfigError → PERMANENT` disposition move together; note `_stamp_deribit_analytics_failed` is defined INSIDE the deribit arm — hoist a venue-neutral equivalent (same upsert shape: computation_status='failed', computation_warned=False, metrics_json_by_basis=None, csv_source flag, strategy-mode only) or hoist the helper definition itself above the branch and rename venue-neutrally; either way the deribit arm keeps calling the SAME stamp for its other failures (surgical — do not restructure its other call sites :2168-2499 beyond the rename). Move the needed imports (`parse_returns_denominator_config`, `ReturnsDenominatorConfigError`) out of the deribit-arm-local import block to the branch-outer scope. Delete the now-redundant deribit-arm parse. The deribit consumers (:2179 pnl_basis, :2192 exclude_spot_extraction, combine calls) read the SAME variable — byte-identical for deribit by construction. Update the :2046-2050 comment (it documents the deribit-only parse this task kills). Tests (in test_cash_basis_series_sc4.py, extending the harness): the four behavior cases above; the ccxt cases need the harness to run a ccxt-venue seam — if `_run_seam` is deribit-shaped, add a minimal ccxt-mode runner reusing `_base_patches`' ccxt arm or patch venue routing; assert via `_cash_series_payload(capture)["conventions"]`. Docstring names the neuter: re-scoping the parse back inside the deribit arm → ccxt-override test RED.</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_cash_basis_series_sc4.py tests/test_mtm_single_key.py -x</automated>
  </verify>
  <acceptance_criteria>test_sc4_cash_series_dual_run_byte_identity green UNMODIFIED (deribit byte-identity); new ccxt-override echo test asserts conventions {"cumulative_method": "simple", "day_basis": "active"} dict-exact; malformed-config-on-ccxt test asserts PERMANENT error_kind; grep gate: `grep -n "parse_returns_denominator_config" analytics-service/services/job_worker.py` shows exactly ONE call site in the single-key seam region (< :3540; the composite parse at :3540 is separate and untouched).</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seam derive densify-tag + terminal-arm heal-deletes + gate-respecting harness</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_cash_basis_series_sc4.py</files>
  <read_first>analytics-service/services/job_worker.py:3157-3220 (the 104 cash derive+persist), :2780-2830 (the <2-interpretable-days terminal arm), :2096-2117 (_stamp_deribit_analytics_failed), analytics-service/tests/test_basis_series.py:73-90 (post-105-01 densify-aware _roundtrip_recompute), .planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md §D3</read_first>
  <behavior>
    - The captured cash_settlement payload carries conventions.densify == "broker_nan" and schema == 2; applying the densify-aware _roundtrip_recompute to the seam's derive result is dict-equal (the guard now covers the seam's cash echo end-to-end).
    - Non-cash track (csv_daily_returns writes, prestamp, MTM RPC, enqueue) byte-identical before/after the densify-tag change (dual-run green).
    - After the <2-interpretable-days terminal arm fires (strategy-mode), BOTH series rows (cash_settlement AND mtm_daily_returns) are heal-DELETED — the capture shows the two delete calls (persist_basis_series result=None path) and NO series upsert.
    - After a _stamp_deribit_analytics_failed-terminal failure, same heal-delete pair fires (single choke point: put the heal INSIDE the stamp helper, covering all its call sites :2168/:2223/:2447/:2472/:2499).
    - Harness gate-respect (D3 caveat b): a `_trusted_cash_payload(capture)` helper returns the cash payload ONLY when the captured prestamp/status is terminal-success and is used by the assertion paths; the terminal-arm tests assert expect-absent, never assert on a stale payload.
  </behavior>
  <action>At the seam cash derive (:3200-3207): pass `scalar_returns=returns` (the SAME dense post-terminus series with interior guard-NaN the csv rows were built from — :3168-3170 documents it) and `densify_policy="broker_nan"`, keeping benchmark_rets=None + benchmark_symbol="BTC". Update the block comment (:3157-3186): the SERIES-ONLY caveat is now stale — this seam's echo is round-trip-complete, but authoritative single-key scalars still flip in Plan 04 (analytics_runner); say so precisely. Heal-deletes (D3 SECONDARY, the gate is the guarantee): inside `_stamp_deribit_analytics_failed` (:2101-2115, strategy-mode guard already present) and in the <2-days strategy-mode arm (:2790-2821), add `persist_basis_series(..., basis="cash_settlement", result=None)` + `persist_basis_series(..., basis="mark_to_market", result=None)` via the existing db_execute wrapper idiom; import function-locally matching the file's style. Failures of the heal itself must not mask the terminal stamp (wrap; log at warning — mirror the guarded sibling-series persist idiom). Harness: add `_trusted_cash_payload` beside `_cash_series_payload` (:129-135) implementing the terminal-success check against the captured prestamp; refactor existing assertions that trust a payload to route through it where status is in play; new tests per the behavior list, each naming its neuter (remove the heal from ONE arm → that arm's test RED; harness trusting a failed-status payload → gate-respect test RED).</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_cash_basis_series_sc4.py tests/test_basis_series.py tests/test_mtm_single_key.py -x</automated>
  </verify>
  <acceptance_criteria>Densify-tag test asserts conventions["densify"]=="broker_nan" AND payload["schema"]==2; dual-run byte-identity green; both terminal-arm heal tests assert the DELETE pair and expect-absent series; `_trusted_cash_payload` exists and is exercised; full quick-run `python -m pytest tests/test_basis_series.py tests/test_cash_basis_series_sc4.py tests/test_stitch_composite_job.py -x` green.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| strategy row jsonb → worker conventions | returns_denominator_config (user-adjacent jsonb) now parsed on EVERY venue |
| worker → strategy_analytics_series | heal-deletes + densify-tagged payloads |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-07 | Tampering | malformed returns_denominator_config on ccxt | mitigate | fail-loud PERMANENT disposition (was silently ignored) — parity with analytics_runner B2 |
| T-105-08 | Integrity | stale series row after terminal arm | mitigate | heal-delete pair at both arm choke points (secondary) + Plan-02 read gate (primary) |
| T-105-09 | Integrity | deribit-path drift from the hoist | mitigate | dual-run byte-identity test green unmodified is a blocking gate |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_cash_basis_series_sc4.py tests/test_mtm_single_key.py tests/test_derive_broker_dailies_dualmode.py -x
cd analytics-service && python -m pytest --cov --cov-fail-under=80  # wave gate
</verification>

<success_criteria>
- MED-2 closed with a wiring-level test (ccxt override echoes simple/active from the REAL seam, not just a helper test).
- Deribit byte-identity proven by the unmodified dual-run test.
- Heal-deletes at both terminal-arm choke points; harness respects the MED-1 gate.
- Exactly one venue-agnostic parse in the single-key seam; derive path branchless (SC-3 for this seam).
</success_criteria>

<output>
Create `.planning/phases/105-composite-the-one-csv-finalize-route/105-03-SUMMARY.md` when done.
</output>
