---
phase: 105-composite-the-one-csv-finalize-route
plan: 04
type: execute
wave: 3
depends_on: [105-01, 105-03]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_cash_basis_series_sc4.py
  - analytics-service/tests/test_csv_analytics_runner.py
autonomous: true
requirements: [BB-02]

must_haves:
  truths:
    - "Collapse #2: the single-key inline compute_all_metrics at analytics_runner.py:2318 is GONE — the CSV runner routes through the shared derive_basis_series with scalar_returns = the exact legacy-conditioned series (broker: dense-reindex-with-NaN from :2272-2277; user CSV: sparse verbatim) and densify_policy = broker_nan | sparse — the scalar is byte-identical to legacy BY CONSTRUCTION (D1)"
    - "SC-4 proven on the three runner surfaces with dual-run DICT-EQUAL metrics: user-CSV-weekend (broadest blast radius — every weekend-spanning traditional CSV), broker-guard-day (interior NaN break vs 0.0-bridge), Zavara simple/active override — each new-route result compared against an in-test legacy recompute (compute_all_metrics on the same conditioned series), never weakened tolerance"
    - "Single-key cash scalars are now a cache of a persisted cash series: the runner persists the cash_settlement series row (persist_basis_series) BEFORE the scalar/status upsert (D5 ordering: a complete scalar never exists without its series), and the runner's unrecoverable terminal arm heal-deletes the row (defense-in-depth; the Plan-02 gate is the guarantee)"
    - "Behavior-asserting legacy tests (broker suffix-only headline :921, user-CSV sparse :981, conventions threading) stay green — mechanism-patching tests are re-pointed to the shared route with IDENTICAL assertions (test intent preserved)"
  artifacts:
    - path: "analytics-service/services/analytics_runner.py"
      provides: "shared-route swap at :2318 + cash series persist-before-scalar + terminal-arm heal"
      contains: "derive_basis_series"
    - path: "analytics-service/tests/test_cash_basis_series_sc4.py"
      provides: "user-CSV-weekend + broker-guard-day + Zavara simple/active dual-run byte-identity tests"
      contains: "test_user_csv_weekend"
    - path: "analytics-service/tests/test_csv_analytics_runner.py"
      provides: "re-pointed mechanism patches (services.basis_series.compute_all_metrics), unchanged behavior assertions"
      contains: "basis_series"
  key_links:
    - from: "analytics-service/services/analytics_runner.py"
      to: "analytics-service/services/basis_series.py"
      via: "derive_basis_series(returns, benchmark_rets, ..., scalar_returns=returns, densify_policy=...) then persist_basis_series(basis='cash_settlement')"
      pattern: "densify_policy"
    - from: "analytics-service/tests/test_cash_basis_series_sc4.py"
      to: "analytics-service/services/analytics_runner.py"
      via: "dual-run: new-route metrics vs in-test legacy compute_all_metrics on the identical conditioned series"
      pattern: "check_exact=True"
---

<objective>
BB-02, Phase 105 Wave 3 — collapse #2 (co-lands with #1 per ROADMAP): the CSV single-key runner's inline `compute_all_metrics` (analytics_runner.py:2318) swaps for the shared `derive_basis_series` route, making single-key cash scalars a cache of a persisted cash series. This is the BROADEST SC-4 blast radius in the phase: `derive_basis_series`'s default 0.0-densify would dilute vol/Sharpe for essentially EVERY user-uploaded traditional strategy spanning a weekend (105-RESEARCH worked example). The D1 mechanism kills that by construction: pass the ALREADY-conditioned series (the :2272-2277 fork IS collapse #6 in miniature — broker dense-reindex-with-NaN, user-CSV sparse) as `scalar_returns`.

HARD BOUNDARIES (locked):
- SC-4 is THE gate: byte-identical scalars for all three runner surfaces, proven dual-run DICT-EQUAL. Never weaken tolerance.
- The :2272-2277 conditioning block and the :2299-2316 venue-agnostic convention resolution are the LEGACY TRUTH — do not "improve" them; the swap consumes them verbatim.
- `compute_all_metrics` itself untouched (no new valuation math). The OTHER runner compute site (:1678, a different function) is OUT OF SCOPE — do not touch it.
- NO DDL. No onboarding/process_key.py work (D2 — carved to 105.1).
- job_worker.py is Plan 03/05 territory — do not edit it here.

Purpose: the last single-key cash bypass closes; the runner joins the one derive route with a persisted series under the round-trip guard.
Output: swapped runner + ordered persist + 3 dual-run SC-4 proofs + re-pointed mechanism tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/105-composite-the-one-csv-finalize-route/105-CONTEXT.md (§D1 + §D5)
@.planning/phases/105-composite-the-one-csv-finalize-route/105-RESEARCH.md (§THE CENTRAL ANSWER divergence table + §Code Examples "Single-key user-CSV")
@.planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md (§analytics_runner.py)

Executor constraints: NO git branch operations (only `git add <explicit paths>`); never stage `.planning/`. No Supabase MCP — pytest against mocked supabase only.

<interfaces>
From analytics-service/services/basis_series.py (post-105-01):
- `derive_basis_series(returns, benchmark_rets, *, periods_per_year, cumulative_method, day_basis, benchmark_symbol=None, scalar_returns=None, densify_policy=None) -> BasisSeriesResult` — metrics computed on scalar_returns EXACTLY when passed; rows/gap_spans from _drop_nonfinite(returns); raises ValueError <2 finite rows (same contract the legacy site relied on).
- `BasisSeriesResult` is DUCK-COMPATIBLE with the runner's downstream reads (105-01 added `insufficient_window`): `.metrics_json` (:2437 payload.update), `.sibling_kinds` (:2443-2455 RPC), `.insufficient_window` (:2399).
- `persist_basis_series(supabase, strategy_id, *, basis="cash_settlement", result)` — single-row authoritative upsert via the hardened RPC; result=None deletes (HEAL).

From analytics-service/services/analytics_runner.py:2240-2455 (the swap site — read the whole span):
- `returns` at :2318 is ALREADY the legacy-conditioned series: `_is_broker_sourced` → dense-reindexed with interior NaN (:2272-2277); user CSV (api_key_id NULL) → sparse verbatim (:2259-2262 comment).
- Conventions already venue-agnostic: `_periods_per_year` (:2299), `_cumulative_method`/`_day_basis` from `parse_returns_denominator_config` (:2304-2316) — the MED-2 reference impl.
- Downstream consumers of `metrics_result`: `.insufficient_window` :2399, `.metrics_json` :2437, `.sibling_kinds` :2443-2455.

TEST-MECHANICS WARNING (verified this session): several tests in test_csv_analytics_runner.py patch `services.analytics_runner.compute_all_metrics` (:127, :198, :246, :319, :372, :1050+ — the threading trio). After the swap, the :2318 path calls compute_all_metrics INSIDE services.basis_series, so those patches stop intercepting. Re-point them to `patch("services.basis_series.compute_all_metrics", ...)` keeping the SAME stubs and the SAME kwarg assertions (periods_per_year/cumulative_method/day_basis land on the inner compute unchanged). Behavior-asserting tests (:921 broker suffix-only, :981 user sparse) patch nothing on the compute path — they MUST stay green UNMODIFIED (they are SC-4 evidence).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap :2318 to the shared derive + persist the cash series before the scalar flip</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/tests/test_csv_analytics_runner.py</files>
  <read_first>analytics-service/services/analytics_runner.py:2240-2470 (conditioning + swap site + downstream consumers + the scalar upsert), analytics-service/services/basis_series.py (post-105-01), analytics-service/tests/test_csv_analytics_runner.py:84-260 + :1040-1140 (the patch-based tests to re-point), .planning/phases/105-composite-the-one-csv-finalize-route/105-PATTERNS.md §analytics_runner.py</read_first>
  <behavior>
    - Broker-sourced strategy: metrics_json spread into the scalar upsert is DICT-EQUAL to compute_all_metrics(dense_with_NaN_returns, benchmark_rets, same 3 conventions).metrics_json (byte-identical by construction).
    - User-CSV strategy: metrics_json DICT-EQUAL to compute_all_metrics(sparse_returns, ...).metrics_json — NO 0.0 weekend fill.
    - A cash_settlement series row is persisted (rows == the finite subset of the conditioned series, conventions echo periods/cumulative/day_basis/benchmark("BTC")/densify) and the persist happens BEFORE the strategy_analytics scalar/status upsert (capture order assertion — mirror test_mtm_series_persists_before_done_scalar_write, test_stitch_composite_job.py:573).
    - densify echo: broker → "broker_nan"; user CSV → "sparse".
    - The unrecoverable terminal arm heal-deletes the cash_settlement series row (persist_basis_series result=None) so a stale row never outlives the failed stamp.
    - ValueError from the derive lands in the SAME error handling the legacy compute used (insufficient-history test :145 green).
  </behavior>
  <action>Replace `metrics_result = compute_all_metrics(returns, benchmark_rets, ...)` at :2318-2324 with `metrics_result = derive_basis_series(returns, benchmark_rets, periods_per_year=_periods_per_year, cumulative_method=_cumulative_method, day_basis=_day_basis, benchmark_symbol="BTC", scalar_returns=returns, densify_policy=("broker_nan" if _is_broker_sourced else "sparse"))` — note `returns` serves BOTH params: as `returns` it feeds `_drop_nonfinite` → honest sparse rows; as `scalar_returns` it is the exact legacy compute input (the conditioning at :2272-2277 already ran). Import derive_basis_series/persist_basis_series function-locally matching the runner's import style. Downstream reads (.insufficient_window/.metrics_json/.sibling_kinds) are duck-compatible — keep the local name `metrics_result`. Add the series persist (persist_basis_series basis="cash_settlement", result=metrics_result) IMMEDIATELY BEFORE the strategy_analytics scalar/status upsert that spreads metrics_json (:2437's payload consumer) — D5 ordering, mirroring the job_worker series-before-DONE-scalar comment discipline (:4645-4675). Wrap in the runner's existing supabase-call idiom; a series-persist failure must abort before the scalar flip (fail-loud), not after. Add the heal-delete in the runner's unrecoverable/terminal stamp path (find `_mark_unrecoverable` — read its body first; guard so heal failure never masks the stamp). Update the Fix-A comment block (:2288-2298) to document the shared-route swap + D1. Re-point the mechanism-patching tests per the TEST-MECHANICS WARNING (same stubs, same assertions — only the patch target string changes); behavior tests stay untouched.</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_csv_analytics_runner.py tests/test_analytics_runner.py -x</automated>
  </verify>
  <acceptance_criteria>`grep -n "compute_all_metrics(" analytics-service/services/analytics_runner.py` shows the :2318 site GONE (only the out-of-scope :1678 site remains); tests :921 and :981 green with ZERO edits (SC-4 behavior evidence); ordering test proves series-persist precedes the scalar upsert and reddens when the persist is moved after (neuter named in docstring); heal-delete test green.</acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The three runner SC-4 dual-run fixtures (user-CSV-weekend flagship)</name>
  <files>analytics-service/tests/test_cash_basis_series_sc4.py</files>
  <read_first>analytics-service/tests/test_cash_basis_series_sc4.py:174-280 (the dual-run + check_exact pattern), analytics-service/tests/test_csv_analytics_runner.py:60-140 + :921-1040 (the runner supabase-mock harness + fixture shapes to reuse/import), analytics-service/services/analytics_runner.py:2240-2330 (post-Task-1), 105-RESEARCH.md §THE CENTRAL ANSWER (the worked weekend example + divergence table)</read_first>
  <behavior>
    - test_user_csv_weekend (BROADEST): a user CSV (api_key_id NULL) spanning Fri→Mon with no weekend rows — the new route's persisted scalar metrics are DICT-EQUAL to compute_all_metrics(sparse_series, ...) AND explicitly UNEQUAL to compute_all_metrics(gap_fill(sparse_series), ...) (the divergence is REAL in this fixture; both assertions present so a silently-degenerate fixture cannot fake the proof).
    - test_broker_guard_day: a broker-sourced series with an interior guard-NaN day — new-route metrics DICT-EQUAL to the legacy dense-reindex-with-NaN compute; the persisted rows exclude the guard day (check_exact=True series equality, mirroring :270-271); conventions.densify == "broker_nan".
    - test_zavara_simple_active: a returns_denominator_config simple/active strategy — new-route metrics DICT-EQUAL to the legacy compute under cumulative_method="simple", day_basis="active"; conventions echo carries them.
    - Each dual-run computes the LEGACY side in-test (compute_all_metrics on the identical conditioned series — the legacy path is deleted, so the test IS the legacy oracle) and each docstring names its neuter target (e.g. "drop scalar_returns from the runner call → weekend metrics dilute → RED").
  </behavior>
  <action>Add the three tests to test_cash_basis_series_sc4.py (the VALIDATION.md contract file), reusing the runner supabase-mock harness from tests.test_csv_analytics_runner via import (mirror how this file already imports the seam harness from tests.test_mtm_single_key at :51-63). Run the REAL run_csv_strategy_analytics (no compute patch — the whole point is exercising the actual pipeline) against fixture csv_daily_returns rows; extract the persisted scalar payload and the cash_settlement series payload from the mock capture. Fixture construction follows the divergence table exactly: the weekend fixture must span ≥1 weekend with returns whose std genuinely shifts under 0.0-fill; the guard-day fixture mirrors :240-248's shape at the runner level; the Zavara fixture reuses _ALLOC_CFG_SIMPLE_ACTIVE-style config (test_csv_analytics_runner.py:1050 area). Use `_trusted_cash_payload` (Plan 03) for any payload trust decision. Dict-equality on metrics; assert_series_equal(check_exact=True, check_freq=False, check_names=False) on rows — NEVER a tolerance.</action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_cash_basis_series_sc4.py -x</automated>
  </verify>
  <acceptance_criteria>All three tests green; test_user_csv_weekend contains BOTH the equality (vs sparse-legacy) and inequality (vs gap-filled) assertions; full quick-run `python -m pytest tests/test_basis_series.py tests/test_cash_basis_series_sc4.py tests/test_stitch_composite_job.py -x` green; full suite `python -m pytest --cov --cov-fail-under=80` green (wave gate).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| csv_daily_returns rows → runner compute | user-uploaded return data feeds the shared derive |
| worker → strategy_analytics / _series | scalar cache + series row ordering (D5) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-10 | Integrity | 0.0-densify dilution of user-CSV scalars | mitigate | scalar_returns = sparse verbatim (D1); dual-run equality + explicit inequality proof |
| T-105-11 | Integrity | complete scalar without its series | mitigate | series persist ordered BEFORE the scalar flip + ordering test (D5) |
| T-105-12 | Integrity | stale series row after runner terminal failure | mitigate | heal-delete at the unrecoverable arm + Plan-02 read gate |
</threat_model>

<verification>
cd analytics-service && python -m pytest tests/test_csv_analytics_runner.py tests/test_analytics_runner.py tests/test_cash_basis_series_sc4.py tests/test_basis_series.py -x
cd analytics-service && python -m pytest --cov --cov-fail-under=80
</verification>

<success_criteria>
- :2318 inline compute gone; runner on the one shared route with scalar_returns.
- 3/5 SC-4 sweep fixtures proven at the runner (user-CSV-weekend, broker-guard-day, Zavara) — composite + ccxt-override live in Plans 05/03.
- Series-before-scalar ordering enforced + tested; heal on terminal arm.
- Behavior-asserting legacy tests untouched and green.
</success_criteria>

<output>
Create `.planning/phases/105-composite-the-one-csv-finalize-route/105-04-SUMMARY.md` when done.
</output>
