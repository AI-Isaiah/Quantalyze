---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 04
type: execute
stage: A
wave: 2
depends_on: ["106-01"]
files_modified:
  - src/app/api/strategies/csv-finalize/route.ts
  - src/__tests__/csv-finalize-after-failloud.test.ts
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "Every console.warn-only failure path inside csv-finalize's after() epilogue also captures to Sentry — a silent after() failure that leaves a strategy stuck computing is alertable"
    - "The console.warn lines are KEPT (Vercel log parity); captureToSentry is ADDED alongside, matching the :620 idiom"
  artifacts:
    - path: "src/app/api/strategies/csv-finalize/route.ts"
      provides: "4 fail-loud after() paths (placeholder upsert error/throw + enqueue error/throw)"
      contains: "captureToSentry"
    - path: "src/__tests__/csv-finalize-after-failloud.test.ts"
      provides: "vitest proving captureToSentry fires on each of the 4 paths"
  key_links:
    - from: "src/app/api/strategies/csv-finalize/route.ts"
      to: "@/lib/sentry-capture"
      via: "captureToSentry on each after() failure arm"
      pattern: "captureToSentry"
---

<objective>
STAGE A (reversible — executes now). D7 fail-loud: the 4 console.warn-only `after()`
failure paths in `csv-finalize/route.ts` get `captureToSentry` alongside the warn (the
prevention half of D7; the janitor in 106-03 is the reaping half). Additive only.

Scope note (verified drift): `finalize-wizard/route.ts`'s after() failure paths are
ALREADY Sentry-paired (`:811`, `:972-984` Promise.allSettled channel) — no gap there. The
gap is exactly the 4 csv-finalize sites. Re-grep at execution; if a new warn-only after()
path appeared in finalize-wizard since 2026-07-14, pair it in the same commit.

Purpose: a failed placeholder write or enqueue inside after() currently vanishes into
Vercel logs while the strategy sits stuck in `computing` — make it alert.
Output: 4 paired capture calls + a regression test per path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D7)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D7 after() fail-loud)

Anchors (re-grepped 2026-07-14 — RE-GREP, this file is edited by 106-07 later):
- The idiom to copy: `csv-finalize/route.ts:620` — `captureToSentry(selectErr, { tags: { surface: "csv-finalize", step: "placeholder-precheck" }, extra: { strategy_id, correlation_id } })`. Import already present at `:13`.
- The 4 warn-only gaps (all inside the after() epilogue helpers):
  1. `:658-661` — placeholder upsert returned `placeholderErr` (inside `writeFailedStrategyAnalyticsPlaceholder`)
  2. `:663-666` — placeholder upsert threw `placeholderThrow` (catch arm of the same helper)
  3. `:711-714` — `enqueue_compute_job` RPC returned `enqueueErr` (inside `enqueueCsvAnalyticsAfter`'s after())
  4. `:718-721` — the enqueue side-effect threw (catch arm)
- Test analog: `src/__tests__/csv-finalize-c14-regression.test.ts` (NEW-C14-04 — pairs console.error with captureToSentry; mirror its mocking approach for @/lib/sentry-capture).
- DO NOT touch the `USE_COMPUTE_JOBS_QUEUE` branch at `:684-694` — its deletion is Stage B (plan 106-07). This plan adds capture calls only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pair captureToSentry onto the 4 warn-only after() paths (TDD)</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/__tests__/csv-finalize-after-failloud.test.ts</files>
  <behavior>
    New test file `src/__tests__/csv-finalize-after-failloud.test.ts` (mirror the
    csv-finalize-c14-regression harness: mock @/lib/sentry-capture, drive the helpers):
    - placeholder upsert error -> captureToSentry called once with tags.surface
      "csv-finalize", tags.step "placeholder-upsert", extra carrying strategy_id + correlation_id
    - placeholder upsert throw -> captureToSentry with tags.step "placeholder-upsert-throw"
    - enqueue RPC error -> captureToSentry with tags.step "csv-analytics-enqueue"
    - enqueue throw -> captureToSentry with tags.step "csv-analytics-enqueue-throw"
    - each path STILL emits its console.warn (assert warn spy called — the warn is kept)
    Tests encode WHY: a silent after() failure = strategy stuck computing with zero trace;
    say so in the describe block.
  </behavior>
  <action>
    RED: write the 4 test cases; run
    `npx vitest run src/__tests__/csv-finalize-after-failloud.test.ts` — MUST fail (no
    capture calls exist). Commit `test(106-04): failing after()-fail-loud coverage for csv-finalize`
    (git add the test file only).

    GREEN: at each of the 4 anchors add a `captureToSentry(err, { tags: { surface:
    "csv-finalize", step: <per-path step above> }, extra: { strategy_id, correlation_id:
    opts.correlationId } })` immediately after the existing console.warn — copy the `:620`
    idiom exactly (import exists). Do not restructure the helpers; do not touch the
    `:684-694` flag branch. If the helpers' in-scope variable names differ from the idiom
    (e.g. `strategyId` not `strategy_id`), keep the Sentry payload KEYS identical to `:620`
    and map the local variable in. Run the test — green. Run the sibling suites
    (`csv-finalize-rpc`, `csv-finalize-c14-regression`) — green. Commit
    `feat(106-04): after() failures in csv-finalize capture to Sentry (D7 fail-loud)`
    (git add src/app/api/strategies/csv-finalize/route.ts).
    NO git branch operations.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-after-failloud.test.ts src/__tests__/csv-finalize-rpc.test.ts src/__tests__/csv-finalize-c14-regression.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>4 paths capture with distinct step tags; warns kept; sibling suites + tsc green; RED precedes GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| after() epilogue -> Sentry | fire-and-forget failures surfaced to org metaworld-fund-ltd |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-08 | Repudiation (silent failure) | after() epilogue | mitigate | captureToSentry with per-path step tags on all 4 arms |
| T-106-09 | Information Disclosure | Sentry payload | mitigate | extra carries only strategy_id + correlation_id (the :620 precedent) — no PII, no key material |
</threat_model>

<verification>
- New + sibling vitest suites green; tsc + `npm run lint` clean on the touched file.
- `grep -c "captureToSentry" src/app/api/strategies/csv-finalize/route.ts` increased by exactly 4.
</verification>

<success_criteria>
- Every after() failure arm in csv-finalize is alertable; zero behavioral change on the success paths (SC-4 untouched by construction — observability only).
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-04-SUMMARY.md` when done.
</output>
