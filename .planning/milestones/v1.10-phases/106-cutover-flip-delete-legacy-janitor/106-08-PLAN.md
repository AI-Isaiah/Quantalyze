---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 08
type: execute
stage: B
gated: true
gated_on: "Explicit user go + 106-05 E2E approval + empirical prod query re-run (compute_analytics 30d count == 0)"
wave: 5
depends_on: ["106-06", "106-07"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/routers/cron.py
  - analytics-service/main_worker.py
  - analytics-service/main.py
  - analytics-service/routers/analytics.py
  - analytics-service/scripts/phase12_backfill_enqueue.py
  - analytics-service/scripts/phase12_deploy.py
  - analytics-service/tests/test_job_worker.py
  - analytics-service/tests/test_main_worker.py
  - analytics-service/tests/test_phase12_backfill_enqueue.py
  - analytics-service/tests/test_phase12_deploy.py
  - analytics-service/tests/test_phase35_backfill_enqueue.py
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "ALL Python dark-path re-entry points are retired IN THE LOCKED D4 ORDER: phase12 backfill script gone, BOTH funding ternaries gone (job_worker sync epilogue AND cron.py periodic re-sync — the 5th live site), HTTP /api/compute-analytics gone, dispatch arm + handler + TIMEOUT_PER_KIND entry + watchdog map entry gone"
    - "The BROKER_DAILIES_VIA_FUNDING flag constant is deleted with its ternaries (zero readers remain) — derive_broker_dailies is unconditional"
    - "run_strategy_analytics has ZERO callers after this plan (its two callers — routers/analytics.py:24 and job_worker.py:1607 — are both deleted here); the chain itself is deleted in 106-09"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "worker without compute_analytics dispatch/handler/timeout/funding-ternary"
    - path: "analytics-service/routers/cron.py"
      provides: "cron_sync enqueues derive_broker_dailies unconditionally"
    - path: "analytics-service/main.py"
      provides: "FastAPI app without the analytics router"
  key_links:
    - from: "analytics-service/routers/cron.py cron_sync"
      to: "derive_broker_dailies"
      via: "unconditional kind string (ternary deleted)"
      pattern: "derive_broker_dailies"
---

<objective>
**STAGE B — GATED, DO NOT AUTO-EXECUTE.** Waits for explicit user go (see gated_on).

D4 (Python half, LOCKED zombie order): retire every remaining dark-path re-entry point
BEFORE the core chain is deleted (106-09). One agent owns `job_worker.py` + `cron.py` —
they are shared files; do not parallelize within this plan. The TS re-entry
(`legacyKeysSyncHandler`) died in 106-07 (wave 4); the RPC guard landed in 106-06.

The LOCKED internal order (execute tasks strictly in sequence):
pin/delete BROKER_DAILIES_VIA_FUNDING -> delete phase12 backfill enqueue (+deploy wiring)
-> delete BOTH funding ternaries -> delete routers/analytics.py + dispatch arm + handler
+ TIMEOUT_PER_KIND entry + watchdog map entry. In-flight/poisoned rows are NOT a zombie:
unknown-kind dispatch goes permanent-FAILED (job_worker.py:5870-5882) and enqueue is
rejected by the 106-06 RPC guard.

Purpose: after this plan, `run_strategy_analytics` has zero live callers — the
precondition the 106-09 grep-gate enforces before the chain deletion.
Output: all Python re-entries dead; suites pruned; worker green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D4)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D4 inventory table)

Deletion inventory (re-grepped 2026-07-14 — RE-GREP every anchor; job_worker.py churns
and 106-02 moved the M2 seam block):
1. `job_worker.py:185-186` — `BROKER_DAILIES_VIA_FUNDING: Final[bool]` constant (+ its `:179-184` rationale comment).
2. `scripts/phase12_backfill_enqueue.py` — WHOLE FILE (enqueues compute_analytics at `:121`, pre-checks at `:54`).
3. `scripts/phase12_deploy.py` — remove the backfill step: import at `:39` (`from scripts import phase12_backfill_enqueue, phase12_kill_switch` — drop only the backfill name), the Step-4 call block `:338-346`, and the queue-depth monitor print `:350-353`. `phase12_kill_switch` stays.
4. `job_worker.py:1519-1521` — sync-epilogue funding ternary -> unconditional `"derive_broker_dailies"`; also sweep the surrounding comments `:1486-1518` + `:1539` + `:2834` + `:3232-3235` that describe the legacy enqueue (update, don't leave lies).
5. `routers/cron.py:448-452` — the 5th LIVE site: `from services.job_worker import BROKER_DAILIES_VIA_FUNDING` import + ternary -> unconditional `"derive_broker_dailies"`; fix comments `:421-426`, `:442-443`, `:465`.
6. `routers/analytics.py` — WHOLE FILE (the HTTP wrapper, re-entry #4). Unregister: `main.py:27` import + `:239` include_router. Check `models/schemas.py` `ComputeRequest` — delete IF orphaned (grep callers).
7. `job_worker.py:5830-5831` — dispatch arm `elif kind == "compute_analytics": handler = run_compute_analytics_job` -> delete (unknown-kind FAILED arm at `:5870-5882` covers strays).
8. `job_worker.py:1590-1608` — `run_compute_analytics_job` handler (imports run_strategy_analytics at `:1605`, calls at `:1607`). Fix the `:1629` comment that says "mirrors run_compute_analytics_job".
9. `job_worker.py:260-263` — `TIMEOUT_PER_KIND` entry `"compute_analytics": 15 * 60` (KEEP `compute_analytics_from_csv` — substring cousin, different kind).
10. `main_worker.py:140` — `WATCHDOG_PER_KIND_OVERRIDES` entry `"compute_analytics": "20 minutes"` (KEEP `"compute_analytics_from_csv"` at `:144`); fix header comments `:14`, `:115` naming it.
Tests: delete `tests/test_phase12_backfill_enqueue.py`; update `test_phase12_deploy.py`
(backfill step assertions), `test_job_worker.py` (run_compute_analytics_job mocks +
dispatch-table cases + funding-ternary cases), `test_main_worker.py`
(watchdog-invariant iterates TIMEOUT_PER_KIND — should self-adjust; verify),
`test_phase35_backfill_enqueue.py` (references only — it enqueues derive_broker_dailies
and STAYS).
⚠️ KEEP: `compute_analytics_from_csv` everywhere (the LIVE CSV kind); `trades_to_daily_returns_with_status`; `run_derive_broker_dailies_job`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retire funding-flag re-entry (#1 in the locked order) — flag + phase12 + both ternaries</name>
  <files>analytics-service/services/job_worker.py, analytics-service/routers/cron.py, analytics-service/scripts/phase12_backfill_enqueue.py, analytics-service/scripts/phase12_deploy.py, analytics-service/tests/test_phase12_backfill_enqueue.py, analytics-service/tests/test_phase12_deploy.py, analytics-service/tests/test_job_worker.py</files>
  <action>
    Execute inventory items 1-5 in that exact order (the LOCKED sequence): delete the
    phase12 backfill script + deploy wiring FIRST (highest blast radius — unguarded bulk
    enqueuer), then both funding ternaries (job_worker epilogue + cron.py re-sync — BOTH,
    or a cron tick re-enqueues after the epilogue is clean), then the
    BROKER_DAILIES_VIA_FUNDING constant once `git grep BROKER_DAILIES_VIA_FUNDING` shows
    zero remaining readers. Prune/update the named test files in the same commit (delete
    the ternary-branch and backfill tests; the derive_broker_dailies-unconditional cases
    must still pass unchanged). Update the swept comments so no doc claims a legacy
    fallback exists.
    Commit `feat(106-08)!: STAGE B — retire funding-flag + phase12 re-entries (D4 order 1-3)`
    (git add the explicit files). NO git branch operations.
  </action>
  <verify>
    <automated>cd analytics-service && ! git grep -qn "BROKER_DAILIES_VIA_FUNDING" -- . && ! test -f scripts/phase12_backfill_enqueue.py && python -m pytest tests/test_job_worker.py tests/test_phase12_deploy.py tests/test_phase35_backfill_enqueue.py -x -q</automated>
  </verify>
  <done>Zero BROKER_DAILIES_VIA_FUNDING references; phase12 backfill gone; both enqueue sites unconditional derive_broker_dailies; touched suites green.</done>
</task>

<task type="auto">
  <name>Task 2: Retire the HTTP + dispatch re-entry (#4) — router, dispatch arm, handler, timeout, watchdog</name>
  <files>analytics-service/routers/analytics.py, analytics-service/main.py, analytics-service/services/job_worker.py, analytics-service/main_worker.py, analytics-service/tests/test_job_worker.py, analytics-service/tests/test_main_worker.py</files>
  <action>
    Execute inventory items 6-10 in order: delete routers/analytics.py + unregister from
    main.py (+ ComputeRequest if orphaned), then the dispatch arm, the handler, the
    TIMEOUT_PER_KIND entry, the watchdog map entry. Sweep the named comments. Prune
    test_job_worker.py handler/dispatch cases and verify the test_main_worker.py
    watchdog-headroom invariant self-adjusts (it iterates TIMEOUT_PER_KIND — the deleted
    key simply drops out; if any case hardcodes the kind, delete that case).
    Then the plan gate: `git grep -n "run_strategy_analytics" -- analytics-service` must
    show callers ONLY in `services/analytics_runner.py` (the def itself + its internal
    docs) and test files slated for 106-09 — i.e. both PROD callers
    (routers/analytics.py:24, job_worker.py:1607) are gone. Record the grep output in the
    SUMMARY (this is the SC-2 evidence 106-09's gate builds on).
    Run the full pytest suite (coverage gate --cov-fail-under=80 must hold — deleting
    dead-code tests alongside dead code keeps the ratio; if coverage dips, the deletion
    was unbalanced, investigate).
    Commit `feat(106-08)!: STAGE B — delete compute_analytics HTTP route + dispatch/handler/timeout/watchdog (D4 order 4)`
    (git add the explicit files). NO git branch operations.
  </action>
  <verify>
    <automated>cd analytics-service && ! grep -qn '"compute_analytics"' services/job_worker.py main_worker.py routers/cron.py && ! test -f routers/analytics.py && python -m pytest -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>No quoted compute_analytics literal in worker/cron/main_worker; router gone; full pytest green with coverage gate; SC-2 caller-grep recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| queue -> worker dispatch | unknown kinds must fail permanent, never retry-loop |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-19 | DoS (zombie re-enqueue) | cron.py re-sync (5th site) | mitigate | both ternaries deleted in the SAME task; grep-gate zero BROKER_DAILIES_VIA_FUNDING |
| T-106-20 | DoS (stray job of deleted kind) | dispatch loop | mitigate | unknown-kind permanent-FAILED arm (:5870-5882) + 106-06 RPC guard; verified not deleted |
| T-106-21 | Tampering (wrong-money funding regression) | derive_broker_dailies unconditional | mitigate | D6 falsifier CLEARED (default true in prod; else-branch never taken) — deletion shifts no numbers; ratified in 106-01 |
</threat_model>

<verification>
- Both task gates green; full pytest + coverage hold; mypy/ruff clean on touched files.
- SC-2 precondition established: zero prod callers of run_strategy_analytics.
</verification>

<success_criteria>
- Every Python re-entry dead in the locked order; worker dispatch table has no compute_analytics; ships in the single Stage-B PR.
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-08-SUMMARY.md` when done.
</output>
