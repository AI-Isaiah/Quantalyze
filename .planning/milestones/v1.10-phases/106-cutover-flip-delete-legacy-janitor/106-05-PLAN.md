---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 05
type: execute
stage: A
wave: 3
depends_on: ["106-02", "106-03", "106-04"]
files_modified: []
autonomous: false
requirements: [BB-03]
must_haves:
  truths:
    - "The FULL D6 E2E surface is verified green on prod's unified path AFTER the Stage-A code lands: 8 basis-by-connector cells + onboarding sync-teaser + per-key allocator dashboard + one resync"
    - "Every factsheet surface is confirmed byte-identical on the unified path (SC-4) — this approval is the Stage-A exit gate and a hard precondition for Stage B"
  artifacts:
    - path: ".planning/phases/106-cutover-flip-delete-legacy-janitor/106-RATIFICATION.md"
      provides: "E2E gate result appended (cell-by-cell PASS/FAIL + approver + date)"
  key_links:
    - from: "Stage-A E2E approval"
      to: "Stage B (plans 106-06..106-10)"
      via: "orchestrator gate — Stage B must not start without this approval"
---

<objective>
STAGE A EXIT GATE (checkpoint — pauses for the user). The D6 full E2E re-test surface,
run against PROD after the Stage-A code (M2 swap, janitor, fail-loud) is deployed. This
is deliberately a MANUAL/LIVE gate, not an executor task: the executor has no Supabase
MCP and no authed prod browser; a prior flag flip exposed 2 latent CSV bugs that only
live E2E caught, so no cell is "obviously fine".

This checkpoint is the ONE documented exception to the Nyquist automated-verify rule for
this phase (see 106-VALIDATION.md §Exceptions).

Purpose: prove every factsheet surface is byte-identical on the unified path before ANY
irreversible deletion is considered. Stage B is gated on: (a) this approval, (b) explicit
user go, (c) the empirical prod query re-run (== 0 compute_analytics jobs in 30d).
Output: appended gate record in 106-RATIFICATION.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D1 step 1, D6)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-RATIFICATION.md

Deploy preconditions before running the surface (orchestrator/user, from project memory):
- Wave-2 PRs merged; CI GREEN-first-try; Railway auto-deploy verified via
  `railway deployment list` commitHash + `/health` (Railway silently skips on red main).
- Prod URL: quantalyze.xyz (never quantalyze.com). Authed QA: qa-demo@quantalyze.app.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Stage-A exit gate — full D6 E2E surface on prod (manual/live)</name>
  <action>Executor: draft the 12-point results table (PASS/FAIL columns, prefilled point descriptions from how-to-verify below) and append it to 106-RATIFICATION.md, then PAUSE for the user to run the surface and fill it in. Do NOT attempt the live verification yourself (no Supabase MCP, no authed prod session).</action>
  <what-built>
    Stage A landed: (1) M2 series-before-scalar ordering on the single-key seam,
    (2) the */15 cron-janitor reaping stuck `computing` rows, (3) Sentry fail-loud on all
    4 csv-finalize after() failure paths. All reversible; nothing deleted; the flags were
    ratified (already on) in 106-01.
  </what-built>
  <how-to-verify>
    On PROD (quantalyze.xyz, qa-demo@quantalyze.app or your own account), verify the D6
    surface — for each cell confirm the factsheet renders complete and unchanged
    (KPIs + equity/drawdown/returns/rolling charts + rail; no new gaps, no shifted numbers):

    1-8. The 8 basis-by-connector cells: {CSV single-key, ccxt single-key, Deribit
         single-key, composite (stitched)} x {cash, MTM toggle} — one representative
         published strategy each.
    9.  Onboarding sync-teaser: run a fresh verify-strategy teaser flow; scalars render,
        nothing persisted (105.1 permanent exception intact).
    10. Per-key allocator dashboard: the allocator blend/per-key reads render
        (csv_daily_returns per-key axis).
    11. One RESYNC: trigger a key resync (keys/sync) on a broker strategy; it completes on
        the unified path (derive_broker_dailies -> compute_analytics_from_csv), the
        factsheet refreshes, and NO compute_jobs row with kind='compute_analytics' appears.
    12. Janitor spot-check: `/api/cron/janitor` ran at least once in Vercel cron logs with
        a 200 (counts body) — or trigger it manually with the CRON_SECRET.

    Record each cell PASS/FAIL. Append the results table + date + your identity to
    106-RATIFICATION.md (the executor drafts the table; you confirm).
  </how-to-verify>
  <resume-signal>Type "approved" to close Stage A (unlocks Stage-B consideration), or describe failing cells — any FAIL blocks Stage B and routes back to a fix plan.</resume-signal>
  <done>All 12 points dispositioned in 106-RATIFICATION.md and the user typed "approved" (any FAIL = Stage B blocked).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Stage A -> Stage B | irreversibility boundary — human approval required to cross |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-10 | Tampering (latent divergence) | factsheet surfaces | mitigate | full 12-point live surface; any FAIL blocks Stage B |
| T-106-11 | Elevation (premature Stage B) | orchestrator flow | mitigate | blocking checkpoint + stage:B markers on all deletion plans |
</threat_model>

<verification>
- Gate record appended to 106-RATIFICATION.md with all 12 points dispositioned.
</verification>

<success_criteria>
- User approval recorded; Stage A closed shippable-on-its-own; Stage B remains held for explicit go.
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-05-SUMMARY.md` when done.
</output>
