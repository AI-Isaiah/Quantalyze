---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 02
type: tdd
stage: A
wave: 2
depends_on: ["106-01"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_job_worker.py
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "On the single-key broker-derive seam, BOTH basis series (MTM + cash) persist BEFORE the DONE-gating metrics_json_by_basis scalar prestamp lands (series-first, mirroring the composite seam)"
    - "The partial-write window is reversed into the self-healing fresh-series + stale-scalar direction (a worker death between writes can no longer leave a fresh scalar over a stale/absent series)"
    - "The stale composite cross-reference comment (job_worker.py:4747) no longer claims the single-key route lands series-first at a line range that contradicts the code"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "single-key seam with series-before-scalar write order"
      contains: "_prestamp_dq_flags"
    - path: "analytics-service/tests/test_job_worker.py"
      provides: "ordering test: strategy_analytics upsert carrying metrics_json_by_basis happens after both persist_basis_series calls"
  key_links:
    - from: "analytics-service/services/job_worker.py single-key seam"
      to: "persist_basis_series"
      via: "await db_execute ordering — series calls precede the prestamp upsert"
      pattern: "persist_basis_series"
---

<objective>
STAGE A (reversible — executes now). D5 / carry-forward M2: swap the single-key
broker-derive seam's write order. Today the DONE-gating MTM scalar prestamp
(`job_worker.py:3163-3175`) lands BEFORE the MTM series persist (`:3188-3201`) and the
cash series persist (`:3268-3275`) — the harmful "fresh scalar over stale/absent series"
direction. Move the prestamp AFTER both series persists, mirroring the composite seam
(`:4735-4782`: cash series -> MTM series -> DONE-bearing scalar LAST). Zero DDL, zero new
behavior — a pure ordering swap of independent writes (D5 LOCKED: no data dependency; no
reader consumes the prestamp between the writes).

Purpose: reverse the partial-write window into the self-healing direction (a worker death
between the writes leaves fresh-series + stale-scalar, which the next derive heals) —
identical rationale to the composite seam's Phase-103 fix.
Output: swapped seam + a RED->GREEN ordering test + corrected comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D5)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D5 — the exact-order analog + the payload-split subtlety)

Anchors (re-grepped 2026-07-14 — RE-GREP before editing, the file churns):
- `job_worker.py:3134` — `_prestamp_payload` dict init (carries `strategy_id` + `data_quality_flags`)
- `job_worker.py:3163-3167` — `_prestamp_payload["metrics_json_by_basis"]` assignment (the DONE-gating MTM scalar)
- `job_worker.py:3169-3173` — `_prestamp_dq_flags` def (upsert to `strategy_analytics`, on_conflict strategy_id)
- `job_worker.py:3175` — `await db_execute(_prestamp_dq_flags)` (the write that must MOVE)
- `job_worker.py:3188-3201` — `_persist_mtm_series_result` + `_persist_mtm_series` + its await
- `job_worker.py:3268-3275` — `_persist_cash_series` + its await
- `job_worker.py:3278-3284` — `_enqueue_csv_analytics` def + await (enqueues `compute_analytics_from_csv`; the prestamp must still land BEFORE this)
- `job_worker.py:4735-4782` — composite analog (correct order): `_persist_cash_series` :4743 -> `_persist_mtm_series` :4775 -> `_write_headline_and_by_basis` :4782 LAST
- `job_worker.py:4747` — the STALE comment: claims the composite matches "the single-key route (:3112-3136), which lands the series before the DONE-gating headline" — FALSE today; fix after the swap
- `job_worker.py:4744-4766` — the canonical rationale comment (self-healing direction) — copy its reasoning into the single-key fix comment
</context>

<feature>
  <name>M2: single-key seam series-before-scalar ordering</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_job_worker.py</files>
  <behavior>
    In the single-key broker-derive seam, the observable supabase call order becomes:
    1. `persist_basis_series(..., basis="mark_to_market", ...)`
    2. `persist_basis_series(..., basis="cash_settlement", ...)`
    3. `strategy_analytics.upsert` carrying the `metrics_json_by_basis` key (the prestamp)
    4. `enqueue_compute_job` RPC for `compute_analytics_from_csv` (unchanged position — still after the prestamp)
    Test cases (test_job_worker.py, mirror the existing supabase-mock harness in that file):
    - Ordering test: run the seam with a successful MTM second pass; record the sequence of
      `persist_basis_series` invocations (patch it) and `strategy_analytics` upserts (mock
      table recorder); assert the upsert whose payload contains "metrics_json_by_basis"
      has a sequence index GREATER than both persist calls AND LESS than the enqueue RPC.
      This test MUST FAIL against the current code (scalar-first) — that is the RED commit.
    - Payload integrity: the moved upsert still carries strategy_id + data_quality_flags +
      metrics_json_by_basis in ONE payload (no accidental split).
  </behavior>
  <implementation>
    RED: write the ordering test; run
    `cd analytics-service && python -m pytest tests/test_job_worker.py -k <new test name> -x`
    — MUST fail (prestamp currently lands first). Commit
    `test(106-02): add failing series-before-scalar ordering test for single-key seam`
    (git add analytics-service/tests/test_job_worker.py only).

    GREEN: move the block {`_prestamp_payload["metrics_json_by_basis"]` assignment
    (:3163-3167) + `_prestamp_dq_flags` def (:3169-3173) + `await db_execute(_prestamp_dq_flags)`
    (:3175)} to AFTER `await db_execute(_persist_cash_series)` (:3275) and BEFORE the
    `_enqueue_csv_analytics` def (:3278). Keep the whole payload together — CONTEXT D5 moves
    the WHOLE prestamp. Pre-move verification (PATTERNS subtlety): grep the moved-over range
    (current :3176-:3275) for any read of `strategy_analytics` or `data_quality_flags` — if
    anything in between consumes the prestamp, STOP and split the payload (move only the
    `metrics_json_by_basis` key; keep the dq-flags prestamp in place) and record the
    deviation. Planner's own read of the range found no such reader — expect a clean move.
    The `_prestamp_payload` dict INIT (:3134) and the long MED-HIGH rationale comment above
    it may stay where they are (init is side-effect-free) — only the assignment + def +
    await move. Write a fix comment on the moved block copying the composite rationale
    (:4744-4766): series-first REVERSES the partial-write window into the self-healing
    fresh-series + stale-scalar direction. Run the test — MUST pass. Commit
    `feat(106-02): single-key seam persists both basis series before the DONE-gating scalar prestamp (M2)`
    (git add analytics-service/services/job_worker.py).

    REFACTOR: fix the stale composite comment at :4747 — replace the false
    "(:3112-3136)" cross-reference with the new (post-swap) single-key line anchor or drop
    the line-range and reference "the single-key broker-derive seam" by name. Grep
    `analytics-service/tests/` for any sibling test pinning the OLD scalar-first order
    (`grep -rn "prestamp" tests/test_job_worker.py tests/test_csv_analytics_runner.py tests/test_cash_basis_series_sc4.py`)
    — any test now red because it asserted scalar-first is EXPECTED red: fix it to the new
    order (these are intent-encoding updates, not weakening). Run the touched suites. Commit
    `refactor(106-02): fix stale composite cross-ref comment + sibling order assertions`.
    NO git branch operations anywhere; only `git add` with the explicit paths above.
  </implementation>
</feature>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| worker -> Supabase | ordered idempotent writes to strategy_analytics_series / strategy_analytics |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03 | Tampering (partial write) | single-key seam | mitigate | series-first ordering: remaining window is fresh-series+stale-scalar, benign + self-healing on next derive (composite-analog proof :4744-4766) |
| T-106-04 | DoS (seam abort) | persist failure | accept (existing) | fail-loud db_execute already aborts the derive before the gating scalar — unchanged by the swap |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_job_worker.py tests/test_cash_basis_series_sc4.py -x` green.
- The sc4 seam-count invariant (`test_single_cash_settlement_persist_seam` — exactly 2 result-bearing cash persists) still holds — the swap moves the scalar, never adds a persist.
- `grep -n ":3112-3136" analytics-service/services/job_worker.py` returns nothing (stale anchor gone).
- mypy/ruff clean on the touched file (match repo gates).
</verification>

<success_criteria>
- Ordering test red before, green after — 2-3 atomic TDD commits.
- SC-4 byte-identity by construction: identical payloads, identical destinations, only ORDER changed; no reader between the writes (verified in-plan).
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-02-SUMMARY.md` when done.
</output>
