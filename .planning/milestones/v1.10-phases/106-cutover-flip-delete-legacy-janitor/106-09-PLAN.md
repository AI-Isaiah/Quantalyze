---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 09
type: execute
stage: B
gated: true
gated_on: "Explicit user go + 106-05 E2E approval + empirical prod query re-run (compute_analytics 30d count == 0)"
wave: 6
depends_on: ["106-08"]
files_modified:
  - analytics-service/services/analytics_runner.py
  - analytics-service/tests/test_analytics_runner.py
  - analytics-service/tests/test_dark_path_deleted.py
  - analytics-service/tests/test_cash_basis_series_sc4.py
  - analytics-service/tests/fixtures/regen_golden.py
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "The dark trades-based chain run_strategy_analytics (analytics_runner.py:1208, computes at :1678) is DELETED — only after the grep-gate proves ZERO live callers (SC-2)"
    - "run_csv_strategy_analytics, compute_all_metrics, trades_to_daily_returns_with_status, and every SHARED helper are KEPT live (SC-3)"
    - "A permanent source-scan grep-gate test (test_dark_path_deleted.py) enforces the deletion invariants across BOTH runtimes forever"
  artifacts:
    - path: "analytics-service/tests/test_dark_path_deleted.py"
      provides: "grep-gate: 0 run_strategy_analytics, 0 run_compute_analytics_job, 0 quoted compute_analytics enqueue/dispatch, deleted files stay deleted, TS re-entry stays dead"
      min_lines: 60
    - path: "analytics-service/services/analytics_runner.py"
      provides: "runner without the dark chain; CSV chain intact"
      contains: "run_csv_strategy_analytics"
  key_links:
    - from: "analytics-service/tests/test_dark_path_deleted.py"
      to: "analytics-service/services + src routes"
      via: "comment-stripped literal-count scan (mirror test_cash_basis_series_sc4.py:705-750)"
      pattern: "_strip_comment"
---

<objective>
**STAGE B — GATED, DO NOT AUTO-EXECUTE.** Waits for explicit user go (see gated_on).

D4 final step (LOCKED order, last): grep-gate ZERO `run_strategy_analytics` callers, THEN
delete the chain itself. Plus the permanent regression gate `test_dark_path_deleted.py`
(SC-2/SC-3) that keeps every deletion from this stage dead — mirroring the established
source-scan style of `test_cash_basis_series_sc4.py:705-750` (`_strip_comment` +
comment-stripped literal counts).

⚠️ SHARED-HELPER DISCIPLINE (SC-3): `run_csv_strategy_analytics` (`:2124`, KEPT — the
live CSV path) shares lifted helpers with the dark chain (`analytics_runner.py:133` notes
the shared structure). For EVERY private helper the dark chain calls, grep its callers
BEFORE deleting: only callers-zero-after-chain-delete helpers die. When in doubt, KEEP —
the full pytest suite is the arbiter.

Purpose: the dark path is gone; nothing can silently compute a strategy off the
non-backbone route; the gate makes the state permanent.
Output: chain deleted, ~40 dark-chain tests pruned, grep-gate test green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D4, D6)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D4 grep-gate analog)

Anchors (re-grepped 2026-07-14):
- `analytics_runner.py:1208` — `async def run_strategy_analytics` (docstring ref at `:11` too); direct `compute_all_metrics(` at `:1678`.
- `analytics_runner.py:2124` — `run_csv_strategy_analytics` KEEP (routes through derive_basis_series at `:2342` since 105).
- Grep-gate analog: `tests/test_cash_basis_series_sc4.py:722-750` (`_strip_comment(ln, lang="py")`, `_repo_root()`, literal-count assert) + the two-lang boundary scan `:705-719`.
- `tests/test_cash_basis_series_sc4.py:727-731` — the seam test's docstring NAMES `run_compute_analytics_job` as "the 106-slated dark-path re-entry point" — after deletion the warning premise is stale; update the docstring, keep the 2-persist count invariant.
- `tests/fixtures/regen_golden.py:509` — comment references run_strategy_analytics; update the comment.
- `tests/test_analytics_runner.py` — ~40 dark-chain tests to DELETE (those exercising run_strategy_analytics); KEEP every run_csv_strategy_analytics / shared-helper test.
- Substring safety (planner-verified): `run_strategy_analytics` is NOT a substring of `run_csv_strategy_analytics`; `run_compute_analytics_job` is NOT a substring of `run_compute_analytics_from_csv_job`; the quoted literal `"compute_analytics"` does NOT match `"compute_analytics_from_csv"`. The gate can count these literals safely.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The grep-gate test (RED against the not-yet-deleted chain)</name>
  <files>analytics-service/tests/test_dark_path_deleted.py</files>
  <behavior>
    New `test_dark_path_deleted.py`, mirroring the sc4 source-scan harness (copy or import
    `_strip_comment`/`_repo_root`). Comment-stripped assertions over an EXPLICIT scan list:
    - `services/analytics_runner.py`, `services/job_worker.py`, `routers/cron.py`,
      `main_worker.py`, `main.py`, `routers/` (walk), `scripts/` (walk):
      0 x `run_strategy_analytics`, 0 x `run_compute_analytics_job`,
      0 x quoted `"compute_analytics"`, 0 x `BROKER_DAILIES_VIA_FUNDING`.
    - Files stay deleted: `routers/analytics.py` and `scripts/phase12_backfill_enqueue.py`
      do not exist.
    - TS re-entry stays dead (two-lang scan, lang="ts"): `src/app/api/keys/sync/route.ts`
      0 x `legacyKeysSyncHandler`; `src/lib/analytics-client.ts` 0 x `computeAnalytics`.
    - KEEP-alive positive asserts (SC-3 — prove the gate isn't over-broad):
      `run_csv_strategy_analytics` present in analytics_runner.py;
      `"compute_analytics_from_csv"` present in job_worker.py;
      `trades_to_daily_returns_with_status` present (grep its def home first).
    Each assert's message names the invariant it kills (mirror the sc4 style: "dark path
    re-entry survived deletion: ...").
  </behavior>
  <action>
    Write the test; run `cd analytics-service && python -m pytest tests/test_dark_path_deleted.py -x`
    — the analytics_runner assertions MUST FAIL (chain still present; everything 106-08
    deleted already passes). That partial-RED is the gate evidence. Commit
    `test(106-09): dark-path deletion grep-gate (RED on the surviving chain)`
    (git add the test file). NO git branch operations.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_dark_path_deleted.py -x 2>&1 | grep -q "run_strategy_analytics" && echo RED-AS-EXPECTED</automated>
  </verify>
  <done>Gate exists, fails ONLY on the not-yet-deleted chain, passes on every 106-07/08 deletion.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the run_strategy_analytics chain + prune its test surface (GREEN)</name>
  <files>analytics-service/services/analytics_runner.py, analytics-service/tests/test_analytics_runner.py, analytics-service/tests/test_cash_basis_series_sc4.py, analytics-service/tests/fixtures/regen_golden.py</files>
  <action>
    Pre-delete gate (SC-2, mandatory): `git grep -n "run_strategy_analytics" -- analytics-service src`
    — every hit must be analytics_runner.py itself, test files pruned in this task, or the
    new gate test. Any OTHER hit = a missed re-entry: STOP, fail loud, route back.
    Then delete: the `run_strategy_analytics` function (`:1208` through its end), its
    module-docstring line (`:11`), and each private helper whose caller count hits zero
    (per-helper grep discipline from the objective). KEEP run_csv_strategy_analytics,
    compute_all_metrics, trades_to_daily_returns_with_status, shared helpers.
    Prune tests: delete the dark-chain tests in test_analytics_runner.py; update the
    sc4 `:727-731` docstring (stale premise) WITHOUT touching its count invariant; update
    the regen_golden.py:509 comment. Run the grep-gate — now fully GREEN — then the FULL
    pytest suite with coverage (the gate: deleting tested-dead code + its tests must hold
    --cov-fail-under=80; record the before/after coverage number in the SUMMARY).
    Run mypy/ruff on analytics_runner.py.
    Commit `feat(106-09)!: STAGE B — delete the dark run_strategy_analytics chain (D4 final)`
    (git add the four files). NO git branch operations.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_dark_path_deleted.py tests/test_cash_basis_series_sc4.py -x -q && python -m pytest -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>Chain gone; gate green; full suite + coverage green; SC-3 keeps proven by the gate's positive asserts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| deletion -> live compute surface | SC-3 keeps must survive; gate enforces both directions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-22 | DoS (missed caller -> dead invocation) | run_strategy_analytics chain | mitigate | mandatory pre-delete repo grep; STOP on any unexpected hit |
| T-106-23 | Tampering (over-deletion of shared helper) | run_csv_strategy_analytics | mitigate | per-helper caller grep + gate's positive KEEP asserts + full suite |
| T-106-24 | Tampering (gate self-invalidation) | test_dark_path_deleted.py | mitigate | comment-stripped counts (grep-gate hygiene — comments never count) |
</threat_model>

<verification>
- Gate green on all invariants (negative + positive); full pytest + coverage hold.
- SC-2 satisfied: the pre-delete grep output is recorded in the SUMMARY.
</verification>

<success_criteria>
- The dark path no longer exists in the codebase and CANNOT come back unnoticed (permanent gate); the one CSV/backbone route is the sole derive path (governing principle).
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-09-SUMMARY.md` when done.
</output>
