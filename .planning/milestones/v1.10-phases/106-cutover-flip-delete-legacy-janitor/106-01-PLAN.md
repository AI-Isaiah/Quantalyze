---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 01
type: execute
stage: A
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "The three cutover env values are re-verified as already-on in prod (ratification, not a flip): USE_COMPUTE_JOBS_QUEUE=\"true\" (Vercel), PROCESS_KEY_UNIFIED_BACKBONE=\"on\" (Vercel + Railway), BROKER_DAILIES_VIA_FUNDING unset -> code default true (Railway)"
    - "Every code comparison string matches the pinned value exactly (=== \"true\", === \"on\", default-true)"
    - "A ratification record exists that Stage B's empirical gate can be checked against"
  artifacts:
    - path: ".planning/phases/106-cutover-flip-delete-legacy-janitor/106-RATIFICATION.md"
      provides: "Stage-A ratification record: env values, verification method, code-default greps, Stage-B gate preconditions"
  key_links:
    - from: "106-RATIFICATION.md"
      to: "job_worker.py:186"
      via: "BROKER_DAILIES_VIA_FUNDING default-true grep evidence"
      pattern: "BROKER_DAILIES_VIA_FUNDING"
---

<objective>
STAGE A (reversible — executes now). Wave-0 gate: RATIFY the three cutover flags. Prod is
already fully on the unified path (CONTEXT prod-state audit 2026-07-14: kill-switch row
'on' since 2026-05-25; `compute_analytics` 48-days cold; env values CLI-resolved). This
plan does NOT flip anything — it re-verifies (belt-and-braces per D6) and documents, so
every later deletion has a written ratification to point at.

Purpose: the Stage-A "pin" per D1 step 1 — audit + document. Gates all of Wave 2.
Output: `106-RATIFICATION.md` (planning artifact, NOT committed to git — `.planning/` is
gitignored/local; do not `git add` it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md
@.planning/STATE.md

Canonical anchors (re-grepped 2026-07-14 by planner — RE-GREP at execution):
- `src/lib/feature-flags.ts:95` — `process.env.PROCESS_KEY_UNIFIED_BACKBONE === "on"` (env fallback comparison)
- `analytics-service/services/job_worker.py:185-186` — `BROKER_DAILIES_VIA_FUNDING` read-once, default `"true"`, disabled only by literal `"false"`
- `analytics-service/services/feature_flags.py:96` — Python `is_unified_backbone_active()` (kill-switch row + env fallback)
- TS `USE_COMPUTE_JOBS_QUEUE` readers (all compare `=== "true"` / `!== "true"`): `csv-finalize/route.ts:684,:1247`, `finalize-wizard/route.ts:890,:934`, `keys/sync/route.ts:183,:534`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-verify prod env values via CLI (belt-and-braces) and grep code comparison strings</name>
  <files>(read-only — no source edits)</files>
  <action>
    Per D6/D1: Wave 0 re-verifies before anything lands. Two halves:

    (1) LIVE ENV RE-VERIFY (best-effort CLI, read-only):
    - Vercel: run `vercel env ls production` from the repo root (project is linked). Confirm
      `USE_COMPUTE_JOBS_QUEUE` exists for production and `PROCESS_KEY_UNIFIED_BACKBONE` exists.
      CLI cannot print encrypted values — existence + the CONTEXT-recorded resolution
      (2026-07-14, values "true"/"on") is the accepted evidence; record BOTH facts.
    - Railway: run `railway variables --service <analytics worker service>` (Railway MCP token
      is stale per project memory — use the CLI). Confirm `PROCESS_KEY_UNIFIED_BACKBONE=on`
      and that `BROKER_DAILIES_VIA_FUNDING` is UNSET (unset -> code default true).
    - If either CLI is unauthenticated/unavailable in the executor environment: do NOT guess.
      Record "CLI unavailable — relying on CONTEXT resolution of 2026-07-14" in the
      ratification doc and flag it in the SUMMARY as a deviation for the orchestrator to
      re-run. FAIL LOUD, never silently skip.

    (2) CODE-COMPARISON GREPS (fully automated, must all pass):
    - `grep -rn "USE_COMPUTE_JOBS_QUEUE" src --include="*.ts"` — every read compares against
      the literal string "true"; count and list the reader sites (expect the 6 anchors in
      context; drift = update the list, do not fail).
    - `grep -n 'PROCESS_KEY_UNIFIED_BACKBONE' src/lib/feature-flags.ts analytics-service/services/feature_flags.py`
      — env fallback compares against literal "on" in both runtimes.
    - `grep -n 'BROKER_DAILIES_VIA_FUNDING' analytics-service/services/job_worker.py` — the
      default is `"true"` and only the literal `"false"` disables (`:185-186`).
    No source file is modified in this task.
  </action>
  <verify>
    <automated>grep -c 'os.environ.get("BROKER_DAILIES_VIA_FUNDING", "true")' analytics-service/services/job_worker.py | grep -qx 1 && grep -q 'PROCESS_KEY_UNIFIED_BACKBONE === "on"' src/lib/feature-flags.ts && echo RATIFY-GREPS-OK</automated>
  </verify>
  <done>All code comparison strings verified against the pinned values; live env evidence gathered (or the CLI-unavailable deviation recorded loudly).</done>
</task>

<task type="auto">
  <name>Task 2: Write the ratification record</name>
  <files>.planning/phases/106-cutover-flip-delete-legacy-janitor/106-RATIFICATION.md</files>
  <action>
    Write `106-RATIFICATION.md` (NEW file — Write tool) recording:
    - The three flag values + where each lives (Vercel / Railway / code default), the
      verification method used (CLI output vs CONTEXT resolution), and the timestamp.
    - The statement of record: "Stage A is a RATIFICATION, not a flip — prod has run the
      unified path since 2026-05-25; the dark path is 48+ days cold as of 2026-07-14."
    - Stage-B preconditions checklist (for the orchestrator, NOT executor tasks):
      (a) explicit user go; (b) re-run the empirical prod query
      `SELECT count(*) FROM compute_jobs WHERE kind='compute_analytics' AND created_at > now() - interval '30 days'`
      == 0 via Supabase MCP (prod project khslejtfbuezsmvmtsdn — executor has NO Supabase
      MCP, this is an orchestrator/human step); (c) Stage-A E2E gate (plan 106-05) approved.
    - The D6 wrong-money falsifier closure: BROKER_DAILIES_VIA_FUNDING unset -> default true
      -> the `compute_analytics` else-branch (`job_worker.py:1520`, `cron.py:451`) is NOT
      taken in prod -> deleting the dark path shifts NO funding numbers.
    Do NOT `git add` this file (.planning is gitignored/local). No git branch operations.
  </action>
  <verify>
    <automated>test -s .planning/phases/106-cutover-flip-delete-legacy-janitor/106-RATIFICATION.md && grep -q "Stage-B preconditions" .planning/phases/106-cutover-flip-delete-legacy-janitor/106-RATIFICATION.md && echo RATIFICATION-OK</automated>
  </verify>
  <done>Ratification record exists with all three flag values, method, Stage-B preconditions, and the falsifier closure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| executor -> Vercel/Railway CLI | read-only env listing; no values printed for encrypted vars |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Repudiation | ratification record | mitigate | written record with method + timestamp; CLI-unavailable path fails loud as a recorded deviation, never silent |
| T-106-02 | Information Disclosure | env listing | accept | CLI lists names only; no secret values enter the doc |
</threat_model>

<verification>
- Both automated greps pass.
- 106-RATIFICATION.md exists, is not staged for git, and contains the Stage-B preconditions checklist.
</verification>

<success_criteria>
- Ratification is documented with verifiable evidence; Wave 2 plans may proceed.
- Zero source files modified; zero git commits required by this plan (docs are local-only).
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-01-SUMMARY.md` when done.
</output>
