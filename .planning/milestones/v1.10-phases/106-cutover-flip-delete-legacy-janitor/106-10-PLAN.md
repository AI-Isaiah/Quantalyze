---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 10
type: execute
stage: B
gated: true
gated_on: "Explicit user go + 106-05 E2E approval + empirical prod query re-run (compute_analytics 30d count == 0)"
wave: 7
depends_on: ["106-09"]
files_modified:
  - src/app/api/cron/flag-monitor/route.ts
  - src/app/api/cron/flag-monitor/route.test.ts
  - src/app/api/cron/phase19-error-rollup/route.ts
  - vercel.json
  - src/__tests__/vercel-cron-limits.test.ts
  - src/lib/feature-flags.ts
  - analytics-service/routers/process_key.py
  - analytics-service/main_worker.py
  - analytics-service/services/feature_flags.py
  - analytics-service/tests/test_feature_flags.py
  - src/components/admin/ComputeJobsTable.tsx
  - src/lib/types.ts
  - analytics-service/tests/test_dark_path_deleted.py
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "flag-monitor NO LONGER writes the feature_flags kill-switch row — the auto-flip is ALERT-ONLY (the error-rate email survives, reworded honestly: rollback retired, investigate manually)"
    - "phase19-error-rollup (the 2nd kill-switch-row reader) is RETIRED: route + cron entry gone (its day-index premise — time since flag flip — died with the kill-switch)"
    - "The kill-switch readers die with the net: process_key.py 503 arm gone (unified unconditional), main_worker claim stamp passes constant true (claim-RPC signature UNTOUCHED — no DDL), isUnifiedBackboneActive/is_unified_backbone_active deleted once caller-count is zero"
    - "Post-Stage-B rollback is git revert + redeploy — stated in the PR body (D1, honest)"
  artifacts:
    - path: "src/app/api/cron/flag-monitor/route.ts"
      provides: "alert-only monitor (no feature_flags kill-switch upsert)"
    - path: "analytics-service/tests/test_dark_path_deleted.py"
      provides: "scan list extended: flag machinery + cosmetic residue stay dead"
  key_links:
    - from: "src/app/api/cron/flag-monitor/route.ts"
      to: "Resend alert email"
      via: "error-rate threshold -> email WITHOUT kill-switch upsert"
      pattern: "ALERT"
---

<objective>
**STAGE B — GATED, DO NOT AUTO-EXECUTE.** Waits for explicit user go (see gated_on).

D1 net removal (same-PR with all Stage-B plans, LOCKED): the kill-switch row, its
readers, and its monitor die together with the arms they controlled. Keeping the
auto-flip alive after 106-07/09 deleted the legacy arms would arm the
auto-rollback-INTO-outage trap (`process_key.py` 503s when the row reads off — an
OUTAGE, not a rollback). Plus the cosmetic D4 residue.

Decision (planner, within D1's "repoint/retire" latitude): phase19-error-rollup is
RETIRED outright — its entire output is a rollup indexed on days-since-flag-flip
(`feature_flags.updated_at` on the kill-switch row), a Phase-19 stability-window
instrument; with the row's semantics dead there is nothing honest to repoint it to.
flag-monitor STAYS as the ongoing error-rate alerter (alert-only).

Purpose: no machinery remains that can flip prod onto a path that no longer exists.
Output: alert-only monitor, retired rollup cron, dead readers deleted, residue cleaned,
grep-gate extended.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D1, D2)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D2/D1 flag-monitor block)

Anchors (re-grepped 2026-07-14):
- `flag-monitor/route.ts`: KILL_SWITCH_KEY const `:61`; the auto-flip upsert is inside `triggerAutoRollback` `:233-241` (flag_key: KILL_SWITCH_KEY, value: "off") + its D-3 PGRST fallback catch `:242-263` (exists ONLY for that upsert); the [ALERT] email `:266-270+` (subject says "auto-rolled-back... flipped to off" — REWORD). The `:194-201` upsert is the ZERO_DENOM_STREAK row — DIFFERENT KEY, STAYS. The zero-denominator H-2 email `:204-213` STAYS.
- `flag-monitor/route.test.ts` header enumerates the cases — case (5) D-3 fallback and the kill-switch-flip assertions must become "never upserts the kill-switch row"; keep threshold-math + email cases.
- `phase19-error-rollup/route.ts:41` KILL_SWITCH_KEY + `:215-228` flag_flipped_at read — DELETE the whole route directory; remove the vercel.json entry (`"path": "/api/cron/phase19-error-rollup"`); update vercel-cron-limits.test.ts if it names the route; grep repo-wide for other references (docs/tests).
- `src/lib/feature-flags.ts`: after 106-07, expected zero callers of `isUnifiedBackboneActive` (`:111`) — verify then delete it + the kill-switch cache machinery (`:35` KILL_SWITCH_KEY, `:82` row read, `:90-108` _refreshCache, `:95` env fallback, `:138` export). If OTHER exports in the file still have callers, keep those; if the file empties, delete it + its test.
- `analytics-service/routers/process_key.py:539-556` — the flag-off 503 arm (`UNIFIED_BACKBONE_DISABLED` envelope) + import `:47`. Delete arm + import; the route body becomes unconditional.
- `analytics-service/main_worker.py:385-404` — `flag_active = await is_unified_backbone_active()` feeds `p_unified_backbone_active` into the claim RPC (migration-104 metadata stamp). Replace with a literal `True` + comment ("Phase 106: backbone permanent-on; param retained — claim-RPC signature unchanged, NO DDL in 106-proper"); delete import `:50`. Do NOT touch the RPC or its signature.
- `analytics-service/services/feature_flags.py:96` `is_unified_backbone_active` — delete once `git grep is_unified_backbone_active` shows zero callers; prune `tests/test_feature_flags.py` accordingly (keep tests of any surviving functions).
- Cosmetic residue (D4): `src/components/admin/ComputeJobsTable.tsx:62` — remove the compute_analytics KIND_OPTIONS entry; `src/lib/types.ts:1582` — remove "compute_analytics" from the JobKind union + fix the `:1639` comment.
- `long_fetch.py:25` mentions is_unified_backbone_active in a COMMENT only — update the comment, no code change.
- KILL-SWITCH ROW disposition: the `feature_flags` DB row itself is NOT deleted by code (no DDL/DML in this plan); note in the SUMMARY that the row becomes inert (zero readers) — orchestrator may clean it up manually post-merge.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: flag-monitor -> alert-only + retire phase19-error-rollup</name>
  <files>src/app/api/cron/flag-monitor/route.ts, src/app/api/cron/flag-monitor/route.test.ts, src/app/api/cron/phase19-error-rollup/route.ts, vercel.json, src/__tests__/vercel-cron-limits.test.ts</files>
  <behavior>
    flag-monitor route.test.ts updated/extended cases:
    - error rate above threshold -> [ALERT] email sent AND NO feature_flags upsert with
      flag_key KILL_SWITCH_KEY ever occurs (assert the recorder captured zero kill-switch
      writes — the streak-row upsert with ZERO_DENOM_STREAK_KEY still occurs on its path).
    - the email body/subject no longer claims a rollback happened; it says auto-rollback
      is RETIRED (Phase 106) and directs to manual investigation.
    - zero-denominator streak behavior unchanged (existing cases pass).
  </behavior>
  <action>
    RED: rewrite the affected test cases first (kill-switch-flip cases become
    never-flips cases; D-3 fallback case deleted with its code path); run
    `npx vitest run src/app/api/cron/flag-monitor/route.test.ts` — MUST fail against the
    current auto-flip code. Commit `test(106-10): flag-monitor alert-only expectations (RED)`.
    GREEN: in `triggerAutoRollback` remove the kill-switch upsert `:233-241` + the D-3
    PGRST fallback that exists only for it; keep + reword the alert email (subject
    `[ALERT] Phase 19 backbone error rate ...` -> honest "auto-rollback retired (Phase
    106); investigate manually", body updated, runbook link kept); rename the function to
    `sendErrorRateAlert` (its old name lies) and update callers/comments/header `:6-10`.
    Delete `src/app/api/cron/phase19-error-rollup/` entirely; remove its vercel.json cron
    entry; update vercel-cron-limits.test.ts; `git grep -n "phase19-error-rollup"` and fix
    every reference (docs comments included). Run the flag-monitor suite + cron-limits.
    Commit `feat(106-10)!: STAGE B — flag-monitor alert-only; phase19-error-rollup retired (D1)`
    (git add the five files/dirs explicitly). NO git branch operations.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/cron/flag-monitor/route.test.ts src/__tests__/vercel-cron-limits.test.ts && ! test -d src/app/api/cron/phase19-error-rollup && ! grep -q "phase19-error-rollup" vercel.json && echo NET-RETIRED</automated>
  </verify>
  <done>Monitor alerts but can never flip; rollup route + cron entry gone; RED preceded GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the kill-switch readers (TS + Python) once caller-count is zero</name>
  <files>src/lib/feature-flags.ts, analytics-service/routers/process_key.py, analytics-service/main_worker.py, analytics-service/services/feature_flags.py, analytics-service/tests/test_feature_flags.py</files>
  <action>
    Order matters — arms first, readers second:
    (1) process_key.py: delete the `:539-556` flag-off 503 arm + the `:47` import; the
    unified body runs unconditionally (this converts the row-off OUTAGE into a
    non-existent state). Update/prune the pytest cases that exercised the 503 arm
    (test_feature_flags.py::test_supabase_outage_falls_back_to_env class of tests follow
    the reader's fate below).
    (2) main_worker.py: `flag_active` -> literal `True` with the no-DDL comment; delete
    the import; the claim-RPC call is otherwise byte-identical.
    (3) Reader deletion, gated: `git grep -n "isUnifiedBackboneActive" -- src` must be
    empty (106-07 removed all call sites) -> delete the function + kill-switch cache
    machinery from feature-flags.ts (whole file if it empties, plus its test file);
    `git grep -n "is_unified_backbone_active" -- analytics-service` must show only
    feature_flags.py + its tests + the long_fetch.py:25 comment -> delete the function,
    prune its tests, fix the comment. If either grep shows an unexpected caller: STOP,
    fail loud, record — do not delete the reader.
    Run full pytest + targeted vitest + tsc + lint.
    Commit `feat(106-10)!: STAGE B — kill-switch readers deleted; process-key unconditional (D1)`
    (git add the explicit files). NO git branch operations.
  </action>
  <verify>
    <automated>! git grep -qn "isUnifiedBackboneActive" -- src && ! git grep -qn "is_unified_backbone_active" -- analytics-service/services analytics-service/routers analytics-service/main_worker.py && cd analytics-service && python -m pytest -x -q 2>&1 | tail -3</automated>
  </verify>
  <done>Zero flag readers in both runtimes; process-key unconditional; claim RPC untouched; suites green.</done>
</task>

<task type="auto">
  <name>Task 3: Cosmetic residue + extend the grep-gate + final full gates</name>
  <files>src/components/admin/ComputeJobsTable.tsx, src/lib/types.ts, analytics-service/tests/test_dark_path_deleted.py</files>
  <action>
    Remove the ComputeJobsTable KIND_OPTIONS compute_analytics entry and the JobKind union
    member + `:1639` comment. Extend test_dark_path_deleted.py's scan list (lang="ts"):
    ComputeJobsTable.tsx + src/lib/types.ts 0 x quoted "compute_analytics";
    src/lib/feature-flags.ts (if kept) 0 x isUnifiedBackboneActive; flag-monitor route
    0 x KILL_SWITCH_KEY upsert-with-"off" (assert the literal `value: "off"` count is 0 in
    that file). Then the STAGE-B EXIT GATES, all recorded in the SUMMARY:
    full `python -m pytest` + `npx vitest run --coverage` + `npx tsc --noEmit` +
    `npm run lint` green; `git grep` zero for: USE_COMPUTE_JOBS_QUEUE (src),
    run_strategy_analytics (non-gate files), legacyKeysSyncHandler, BROKER_DAILIES_VIA_FUNDING.
    State in the SUMMARY (for the single Stage-B PR body): POST-STAGE-B ROLLBACK = git
    revert + redeploy (D1, the honest statement); VERSION + package.json bump in the same
    commit per repo rule happens at ship time (orchestrator).
    Commit `feat(106-10): STAGE B — cosmetic compute_analytics residue + extended grep-gate`
    (git add the three files). NO git branch operations.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_dark_path_deleted.py -x -q && cd .. && npx vitest run --coverage 2>&1 | tail -5 && npx tsc --noEmit</automated>
  </verify>
  <done>Residue gone; extended gate green; all four exit gates recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| flag-monitor cron -> feature_flags | WRITE capability removed; read-only alerting remains |
| kill-switch row -> prod routing | row becomes inert (zero readers) — no path can 503 on it |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-25 | DoS (auto-flip into outage) | flag-monitor | mitigate | upsert removed; test asserts zero kill-switch writes ever |
| T-106-26 | DoS (row flipped manually -> 503) | process_key.py | mitigate | reader deleted — row-off state unreachable by construction |
| T-106-27 | Repudiation (lost alerting) | error-rate email | mitigate | alert email KEPT + reworded; test-pinned |
| T-106-28 | Tampering (claim-RPC metadata drift) | main_worker stamp | accept | constant-true equals prod's 7-week steady state; signature untouched (no DDL) |
</threat_model>

<verification>
- All three task gates green; the four Stage-B exit greps zero; coverage thresholds hold in both suites.
</verification>

<success_criteria>
- The rollback net is fully retired WITH the arms it controlled (D1); alerting survives; rollback = git revert, stated in the PR; phase code-complete pending ship gates (migration-reviewer, rls-policy-auditor, test-project catch-up, CI-green Railway deploy).
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-10-SUMMARY.md` when done.
</output>
