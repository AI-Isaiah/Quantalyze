---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 06
type: execute
stage: B
gated: true
gated_on: "Explicit user go + 106-05 E2E approval + empirical prod query re-run (compute_analytics 30d count == 0, orchestrator via Supabase MCP)"
wave: 4
depends_on: ["106-05"]
files_modified:
  - supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql
  - supabase/tests/test_compute_analytics_kind_retired.sql
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "_enqueue_compute_job_internal REJECTS kind='compute_analytics' with ERRCODE invalid_parameter_value — in BOTH live overloads (7-param and 10-param)"
    - "The compute_job_kinds registry row and BOTH CHECK constraints still ADMIT the kind (the 45 historical prod rows FK-reference it — no CHECK/registry drop)"
    - "The migration is a pure CREATE OR REPLACE (touches zero table data, validates no existing rows, reverses cleanly) and self-verifies via a DO block"
  artifacts:
    - path: "supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql"
      provides: "RPC admission guard on both overloads + self-verifying DO block"
      contains: "invalid_parameter_value"
    - path: "supabase/tests/test_compute_analytics_kind_retired.sql"
      provides: "SQL gate: both overload bodies contain the retired-kind reject"
  key_links:
    - from: "guard block"
      to: "_enqueue_compute_job_internal p_kind NULL guard"
      via: "inserted immediately after the NULL guard, same RAISE idiom (20260510180226:190-193)"
      pattern: "compute_analytics is retired"
---

<objective>
**STAGE B — GATED, DO NOT AUTO-EXECUTE.** The execute-phase orchestrator runs Stage A
only and STOPS; this plan (and every stage:B plan) waits for an explicit user go after
the Stage-A gate + the empirical prod re-check (see gated_on).

D3: the `compute_analytics` kind teardown as an RPC ADMISSION GUARD — NOT a CHECK or
registry drop (45 historical prod rows make those fail mid-deploy; migrations auto-apply
to prod on merge). Insert a fail-loud reject for the retired kind into
`_enqueue_compute_job_internal`, re-based on the LATEST body of EACH live overload.

⚠️ RE-BASE DISCIPLINE (pattern-mapper correction, LOCKED): the re-base targets are
`20260510180226_compute_jobs_audit_2026_05_07_g10b.sql:164` (7-param — latest 7-param
body) and `20260420073003_allocator_holdings.sql:330` (10-param — latest 10-param body).
`20260710130000:53,:85` is CHECK DDL, NOT the RPC — use it only as the reference for
which kind string to reject. Before writing, grep ALL migrations for any NEWER
CREATE OR REPLACE of either overload (project rule: re-base SQL fns on the LATEST def).

Purpose: after Stage B's deletions, any stray enqueue of the retired kind fails loud at
the RPC instead of poisoning the queue. Defense-in-depth pairing: a direct table INSERT
(service-role only) would still pass the CHECK, but the worker's unknown-kind dispatch
goes permanent-FAILED (`job_worker.py:5870-5882`) — accepted residual, documented below.
Output: one reversible migration + one SQL gate test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D3)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D3 — full re-base + idiom detail)

Anchors (re-grepped 2026-07-14):
- 7-param overload latest body: `supabase/migrations/20260510180226_compute_jobs_audit_2026_05_07_g10b.sql:164-176` — `(uuid,uuid,text,text,uuid[],text,jsonb)`, SECURITY DEFINER, `SET search_path = public, pg_catalog`; p_kind NULL guard at `:190-193`.
- 10-param overload latest body: `supabase/migrations/20260420073003_allocator_holdings.sql:330-345` — adds `p_allocator_id`, `p_api_key_id`, `p_run_at` (all DEFAULT NULL); its own p_kind NULL guard at `:365-367`; the public `enqueue_compute_job` wrappers delegate into it (`:486,:494,:503`).
- Reject idiom to copy: `20260510180226:190-193` — `RAISE EXCEPTION ... USING ERRCODE = 'invalid_parameter_value';`
- Overload-coexistence discipline: `20260510180226:284-303` — COMMENT/REVOKE must be arg-list-qualified or fail SQLSTATE 42725.
- Migration discipline analog: `20260710130000_stitch_composite_kind.sql` — header re-base rules `:22-34`, `SET LOCAL lock_timeout = '3s'` `:39`, NO explicit BEGIN/COMMIT (Supabase wraps each migration), self-verifying DO block `:110-168` (greps pg_get_functiondef/pg_get_constraintdef, RAISEs on regression; every RAISE format string a SINGLE literal — invariant #21, no ||).
- Latest migration timestamp: `20260715090000_user_notes_dashboard_scope.sql` — the new file MUST sort after it; re-run `ls supabase/migrations/ | tail` at execution and bump the timestamp if newer files landed.
- SQL test home per project rule: `supabase/tests/test_*.sql` (mirror an existing file's structure — read one before writing).

<interfaces>
The guard block to insert in EACH overload, immediately AFTER its existing p_kind NULL
guard (identical text in both bodies):

    IF p_kind = 'compute_analytics' THEN
      RAISE EXCEPTION '_enqueue_compute_job_internal: kind compute_analytics is retired (Phase 106) — no enqueue path remains'
        USING ERRCODE = 'invalid_parameter_value';
    END IF;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The guard migration (both overloads) with self-verifying DO block</name>
  <files>supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql</files>
  <behavior>
    - Enqueue via either overload with p_kind='compute_analytics' raises
      invalid_parameter_value naming the retirement.
    - Every OTHER kind enqueues exactly as before (bodies otherwise byte-identical to the
      re-base sources).
    - The migration's own DO block FAILS the deploy if either overload's
      pg_get_functiondef lacks the retired-kind reject, or if the compute_jobs kind CHECK
      stopped admitting 'compute_analytics' (regression guard against a "helpful" drop).
  </behavior>
  <action>
    Write the migration:
    1. Header mirroring 20260710130000:22-34 — name BOTH re-base sources by file:line,
       state the D3 rationale (45 historical rows -> registry/CHECKs STAY; guard is
       RPC-level, reversible, validates zero rows), and the re-base instruction for future
       editors.
    2. `SET LOCAL lock_timeout = '3s';` — no BEGIN/COMMIT.
    3. CREATE OR REPLACE the 7-param overload: copy the ENTIRE body from
       20260510180226:164 verbatim (first grep migrations for any newer 7-param
       redefinition; re-base on the newest), inserting ONLY the guard block (see
       interfaces) directly after the p_kind NULL guard. Preserve SECURITY DEFINER +
       `SET search_path = public, pg_catalog` exactly.
    4. CREATE OR REPLACE the 10-param overload: same procedure re-based on
       20260420073003:330 (NULL guard at :365); insert the same block after it.
    5. Do NOT touch grants (CREATE OR REPLACE preserves ACLs — 20260515130001 hardened
       them). Any COMMENT ON FUNCTION must be arg-list-qualified (42725 discipline).
    6. Self-verifying DO block mirroring 20260710130000:110-168: for EACH overload resolve
       the OID via to_regprocedure with the full arg list, assert pg_get_functiondef
       contains the retirement message, RAISE single-literal messages on failure; plus the
       CHECK-still-admits assertion via pg_get_constraintdef.
    TDD note: draft the DO block FIRST — it is the failing-first check (it would RAISE if
    the guard were omitted from either overload).
    Commit `feat(106-06)!: STAGE B — retire compute_analytics kind via RPC admission guard (D3)`
    (git add the migration file only). NO git branch operations.
  </action>
  <verify>
    <automated>test "$(grep -c 'compute_analytics is retired' supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql)" -ge 4 && grep -q "lock_timeout" supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql && grep -c "CREATE OR REPLACE FUNCTION _enqueue_compute_job_internal" supabase/migrations/20260716090000_retire_compute_analytics_kind_rpc_guard.sql | grep -qx 2 && echo GUARD-SHAPE-OK</automated>
  </verify>
  <done>Migration contains both overload replacements (2 CREATE OR REPLACE), the guard in each body, the DO-block assertions, and the discipline header.</done>
</task>

<task type="auto">
  <name>Task 2: SQL gate test + merge-gate documentation</name>
  <files>supabase/tests/test_compute_analytics_kind_retired.sql</files>
  <action>
    Create `supabase/tests/test_compute_analytics_kind_retired.sql` mirroring the structure
    of an existing `supabase/tests/test_*.sql` file (read one first — match its
    assertion/reporting style exactly): assert BOTH overloads' pg_get_functiondef contain
    'compute_analytics is retired' and that ERRCODE invalid_parameter_value appears in the
    guard region. This test is RED-GUARDED until the test project catches up — that is
    expected and by design ([[project_test_project_catchup_unmasks_stale_tests]]).

    In the SUMMARY (and the PR description when shipped), surface the three MERGE GATES
    that are orchestrator/human steps, NOT executor tasks (executor has no Supabase MCP):
    (a) migration-reviewer pass, (b) rls-policy-auditor pass, (c) apply the migration to
    the TEST project (qmnijlgmdhviwzwfyzlc) via Supabase MCP BEFORE merge so the SQL test
    goes green in CI; after merge, watch the auto-apply run on PROD and verify both
    function bodies via a pg_get_functiondef query.
    Commit `test(106-06): SQL gate — retired-kind guard present in both overloads`
    (git add the test file only). NO git branch operations.
  </action>
  <verify>
    <automated>test -s supabase/tests/test_compute_analytics_kind_retired.sql && grep -q "compute_analytics is retired" supabase/tests/test_compute_analytics_kind_retired.sql && echo SQLTEST-OK</automated>
  </verify>
  <done>SQL gate exists; merge gates documented loudly for the orchestrator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| any enqueue caller -> compute_jobs | SECDEF RPC is the sole sanctioned insert path |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-12 | DoS (stray retired-kind enqueue) | _enqueue_compute_job_internal | mitigate | reject with invalid_parameter_value in BOTH overloads |
| T-106-13 | Elevation (SECDEF regression) | replaced fn bodies | mitigate | verbatim re-base on latest bodies; SECURITY DEFINER + search_path preserved; migration-reviewer + rls-policy-auditor gates |
| T-106-14 | Tampering (direct table INSERT of retired kind) | compute_jobs | accept | service-role-only path; worker unknown-kind dispatch goes permanent-FAILED (job_worker.py:5870-5882) — fail-loud residual |
| T-106-15 | DoS (mid-deploy migration failure) | prod auto-apply | mitigate | CREATE OR REPLACE validates zero rows; lock_timeout 3s; self-verifying DO block |
</threat_model>

<verification>
- Both automated greps pass; migration filename sorts after the latest migration.
- Orchestrator gates documented: migration-reviewer, rls-policy-auditor, test-project MCP catch-up BEFORE merge.
</verification>

<success_criteria>
- Retired-kind enqueue is impossible via RPC in both overloads; registry/CHECKs untouched; migration reversible by re-running the prior bodies.
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-06-SUMMARY.md` when done.
</output>
