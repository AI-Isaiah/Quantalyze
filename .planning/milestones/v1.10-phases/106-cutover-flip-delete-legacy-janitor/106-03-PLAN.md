---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 03
type: execute
stage: A
wave: 2
depends_on: ["106-01"]
files_modified:
  - analytics-service/routers/cron.py
  - analytics-service/tests/test_cron_router.py
  - src/app/api/cron/janitor/route.ts
  - vercel.json
  - src/__tests__/vercel-cron-limits.test.ts
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "A recurring janitor tick reaps strategy_analytics rows stuck computation_status='computing' past a stale threshold that exceeds every watchdog ceiling"
    - "A row with ANY non-terminal compute_jobs row (pending/running/done_pending_children/failed_retry) matched on the strategy_id COLUMN is skipped — a live process_key_long onboarding can never be reaped (race already closed by coherence CHECK 20260710130000:93)"
    - "The reap is idempotent compare-and-set: the update filters on computation_status='computing' so it never stomps a row the worker just flipped"
    - "The tick runs every 15 minutes via Vercel Cron -> Next proxy -> Python endpoint"
  artifacts:
    - path: "analytics-service/routers/cron.py"
      provides: "POST /api/cron-janitor tick (X-Service-Key protected by existing middleware)"
      contains: "cron-janitor"
    - path: "src/app/api/cron/janitor/route.ts"
      provides: "CRON_SECRET-gated Vercel Cron proxy to the Python tick"
    - path: "vercel.json"
      provides: "cron registry entry /api/cron/janitor */15"
      contains: "/api/cron/janitor"
    - path: "analytics-service/tests/test_cron_router.py"
      provides: "janitor tests: reap-when-stale, skip-when-active-job, CAS no-stomp"
  key_links:
    - from: "src/app/api/cron/janitor/route.ts"
      to: "analytics-service /api/cron-janitor"
      via: "fetch with X-Service-Key header (analytics-client idiom)"
      pattern: "cron-janitor"
    - from: "vercel.json"
      to: "/api/cron/janitor"
      via: "crons array entry"
      pattern: "api/cron/janitor"
---

<objective>
STAGE A (reversible — executes now). D7 janitor: promote the one-time
`scripts/reset_stuck_computing_rows.py` logic to a recurring `routers/cron.py` tick that
reaps orphaned `computing` rows. Purely additive — no deletion, no DDL, trivially
revertible.

Scheduling decision (planner, per PATTERNS "no Python tick registered in vercel.json"
gap): NEW Python `@router.post("/cron-janitor")` + a thin Next proxy cron route
`src/app/api/cron/janitor/route.ts` + a `vercel.json` `*/15` entry. Rationale: folding
into `cron_sync` would inherit its daily-ish cadence; D7 wants a 10-15 min tick, and the
proxy mirrors the established CRON_SECRET + X-Service-Key idioms exactly.

Threshold decision (DRIFT CORRECTION over CONTEXT's "~20-25 min"): the max per-kind
watchdog ceiling in `main_worker.py` `WATCHDOG_PER_KIND_OVERRIDES` is **40 minutes**
(`process_key_long`), not 20-25. Stale threshold = **60 minutes** (max ceiling + headroom)
so the janitor can never race a legitimately-slow job. Re-grep the map at execution; if
the max ceiling changed, keep threshold >= max + 15 min.

Purpose: the reaping half of D7 (plan 106-04 is the prevention half); no strategy stays
stuck in `computing` forever.
Output: janitor tick + proxy + cron registry + pytest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D7)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D7 janitor)

Anchors (re-grepped 2026-07-14):
- Logic seed: `analytics-service/scripts/reset_stuck_computing_rows.py` (whole file, 112 lines):
  select `strategy_analytics` where `computation_status='computing'` and `.lt("updated_at", threshold)` (:61-66);
  active-job probe `.eq("strategy_id", sid).in_("status", ["pending","running","done_pending_children","failed_retry"])` (:82-89);
  compare-and-set update `.eq("strategy_id", sid).eq("computation_status","computing")` (:97-102).
  Leave the script itself UNTOUCHED (one-time deploy artifact; do not delete — dead-code deletion needs its own gate).
- Router: `analytics-service/routers/cron.py:14` `APIRouter(prefix="/api")`; recurring-tick analog `@router.post("/cron-sync")` :566.
  Auth: the X-Service-Key middleware in `analytics-service/main.py:198-235` covers ALL /api routes by default — the new endpoint needs NO in-route auth.
- Watchdog map: `analytics-service/main_worker.py:133-169` `WATCHDOG_PER_KIND_OVERRIDES` — max today = "40 minutes" (`process_key_long`, :159).
- Proxy analog: `src/app/api/cron/reconcile-strategies/route.ts:39-44` — `Authorization: Bearer ${CRON_SECRET}` via `safeCompare`, `export const dynamic = "force-dynamic"`, GET (Vercel Cron dispatches GET) + POST for incident response.
- Service-call idiom: `src/lib/analytics-client.ts:19-20,:92` — `ANALYTICS_SERVICE_URL` + `X-Service-Key: ANALYTICS_SERVICE_KEY` header.
- Cron registry: `vercel.json:6-16` (9 entries today); sub-daily crons MUST be added to `SUB_DAILY_ALLOWLIST` in `src/__tests__/vercel-cron-limits.test.ts` (flag-monitor precedent).
- Race-already-closed proof (D7, pattern-mapper): coherence CHECK `supabase/migrations/20260710130000_stitch_composite_kind.sql:93` forces `process_key_long` rows to carry NON-NULL `strategy_id` COLUMN -> the `.eq("strategy_id", sid)` column probe catches a live onboarding. KEEP the column probe; do NOT extend to metadata.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Janitor tick in routers/cron.py (TDD)</name>
  <files>analytics-service/routers/cron.py, analytics-service/tests/test_cron_router.py</files>
  <behavior>
    - Reap-when-stale: a strategy_analytics row computing + updated_at older than 60 min +
      NO non-terminal compute_jobs row -> updated to computation_status='failed' with a
      computation_error naming the janitor; response counts it as reaped.
    - Skip-when-active-job: same stale row but a compute_jobs row with status in
      {pending, running, done_pending_children, failed_retry} matched via the strategy_id
      COLUMN -> untouched; counted as skipped_active. (This is the live-onboarding guard —
      encode the WHY in the test docstring: coherence CHECK 20260710130000:93 guarantees
      process_key_long rows carry the column.)
    - Fresh rows (updated_at within threshold) are never selected.
    - CAS no-stomp: the update carries `.eq("computation_status", "computing")` — assert the
      recorded update filter includes it (a worker flipping the row concurrently wins).
    - Endpoint returns {scanned, reaped, skipped_active} and 200; per-row failures are
      logged and do not abort the batch (mirror the reset script's per-row try semantics).
  </behavior>
  <action>
    RED first: add the janitor test class to the existing `tests/test_cron_router.py`,
    mirroring its established supabase-mock harness style. Run
    `cd analytics-service && python -m pytest tests/test_cron_router.py -k janitor -x` — MUST fail
    (endpoint absent). Commit `test(106-03): failing janitor cron tests` (git add the test file).

    GREEN: implement `@router.post("/cron-janitor")` in `routers/cron.py`, seeded from
    `scripts/reset_stuck_computing_rows.py` logic verbatim where possible:
    - Module constant `STALE_COMPUTING_THRESHOLD_MINUTES = 60` with a comment deriving it:
      "> max(WATCHDOG_PER_KIND_OVERRIDES) = 40 min (process_key_long) + headroom — the
      janitor must never race a legitimately-slow job" (re-grep the map; adjust if it grew).
    - Python-computed ISO threshold (the script's :57-61 PostgREST `.lt()` note applies).
    - Column-probe active-job check + compare-and-set update exactly as the seed (:82-102).
    - Keep the status set as a module constant `NON_TERMINAL_JOB_STATUSES` so the grep-gate
      and future kinds have one place to look.
    - No new auth code (X-Service-Key middleware covers /api/*). Fail-loud: an unexpected
      top-level exception -> HTTPException 500 (the cron runner only alarms on non-2xx —
      cron_sync's KEK precedent at :577-583).
    Run the tests — green. Commit `feat(106-03): recurring cron-janitor tick reaps stuck computing rows (D7)`
    (git add analytics-service/routers/cron.py analytics-service/tests/test_cron_router.py).
    NO git branch operations.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_cron_router.py -x -q</automated>
  </verify>
  <done>All janitor behaviors green; RED commit precedes GREEN commit; threshold constant documented against the watchdog map.</done>
</task>

<task type="auto">
  <name>Task 2: Vercel Cron proxy + registry + allowlist</name>
  <files>src/app/api/cron/janitor/route.ts, vercel.json, src/__tests__/vercel-cron-limits.test.ts</files>
  <action>
    Create `src/app/api/cron/janitor/route.ts` mirroring `reconcile-strategies/route.ts`
    structure: `export const dynamic = "force-dynamic"`; a shared `handle(req)` exported as
    both GET and POST; Bearer `CRON_SECRET` check via `safeCompare` (401 on mismatch,
    identical shape to reconcile-strategies :39-44). Body: POST to
    `${process.env.ANALYTICS_SERVICE_URL}/api/cron-janitor` with the
    `X-Service-Key: process.env.ANALYTICS_SERVICE_KEY` header (idiom from
    analytics-client.ts:92); on missing ANALYTICS_SERVICE_URL return 503 fail-loud (the
    analytics-client :362-364 precedent); forward the Python JSON body + status through.
    No new env vars — CRON_SECRET, ANALYTICS_SERVICE_URL, ANALYTICS_SERVICE_KEY all exist.

    vercel.json: append `{ "path": "/api/cron/janitor", "schedule": "*/15 * * * *" }` to the
    crons array (D7 cadence, mirrors flag-monitor).

    src/__tests__/vercel-cron-limits.test.ts: add "/api/cron/janitor" to SUB_DAILY_ALLOWLIST
    with a one-line justification comment (Phase 106 D7 — stuck-computing reaper; */15
    matches flag-monitor precedent).

    Then run the FULL vitest suite for the route-enumerating gates — `rbac-matrix.test.ts`,
    `audit-coverage.test.ts`, `no-store-coverage.test.ts` enumerate `src/app/api` routes and
    may require classifying the new route (cron/service class, mirrors how
    reconcile-strategies is classified). Update those classification tables as needed —
    match the existing cron-route entries exactly.

    Commit `feat(106-03): register janitor cron — Vercel proxy + */15 schedule` with
    explicit paths (route.ts, vercel.json, vercel-cron-limits.test.ts, plus any
    classification-table test files touched). NO git branch operations.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/vercel-cron-limits.test.ts src/__tests__/rbac-matrix.test.ts src/__tests__/audit-coverage.test.ts src/__tests__/no-store-coverage.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Proxy route compiles + is auth-gated; cron registered; all route-enumeration gates green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Vercel Cron -> /api/cron/janitor | CRON_SECRET Bearer (timing-safe compare) |
| Next proxy -> FastAPI /api/cron-janitor | X-Service-Key middleware (existing, main.py:198) |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-05 | Spoofing | janitor proxy | mitigate | safeCompare CRON_SECRET gate identical to reconcile-strategies; Python side behind X-Service-Key middleware |
| T-106-06 | DoS (false reap of live job) | janitor sweep | mitigate | 60-min threshold > 40-min max watchdog ceiling + column-probe skip + CAS update on computation_status='computing' |
| T-106-07 | Repudiation | reap actions | mitigate | per-row log line naming strategy_id + counts in the 200 body (non-2xx alarms the cron runner) |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_cron_router.py -x` green.
- vitest route gates + tsc green; `npm run lint` clean on touched TS files.
- `grep -c "api/cron/janitor" vercel.json` == 1.
</verification>

<success_criteria>
- Janitor reaps stale + skips live, idempotently, on a */15 schedule, with both auth layers intact.
- Zero deletions, zero DDL — fully reversible by removing the tick + registry entry.
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-03-SUMMARY.md` when done.
</output>
