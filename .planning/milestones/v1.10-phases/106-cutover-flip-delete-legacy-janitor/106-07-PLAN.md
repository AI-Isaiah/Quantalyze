---
phase: 106-cutover-flip-delete-legacy-janitor
plan: 07
type: execute
stage: B
gated: true
gated_on: "Explicit user go + 106-05 E2E approval + empirical prod query re-run (compute_analytics 30d count == 0)"
wave: 4
depends_on: ["106-05"]
files_modified:
  - src/app/api/keys/sync/route.ts
  - src/app/api/keys/sync/route.test.ts
  - src/lib/analytics-client.ts
  - src/app/api/strategies/csv-finalize/route.ts
  - src/app/api/strategies/finalize-wizard/route.ts
  - src/app/api/strategies/finalize-wizard/route.test.ts
  - src/app/api/strategies/csv-validate/route.ts
  - src/__tests__/csv-validate-route.test.ts
  - src/app/api/verify-strategy/route.ts
  - src/app/api/keys/validate-and-encrypt/route.ts
autonomous: true
requirements: [BB-03]
must_haves:
  truths:
    - "legacyKeysSyncHandler (the sole computeAnalytics caller — dark-path re-entry #3) is DELETED and the unified handler is unconditional in keys/sync"
    - "Every USE_COMPUTE_JOBS_QUEUE false-branch is DELETED (they were 503/placeholder arms, not working legacy) — the unified/queue arm is unconditional at every reader site"
    - "Every isUnifiedBackboneActive()===false arm in the TS routes is DELETED with a written dormancy proof per site (D6: each false arm PROVEN dormant); the true-path code is byte-identical"
    - "computeAnalytics is deleted from analytics-client (zero callers remain)"
  artifacts:
    - path: "src/app/api/keys/sync/route.ts"
      provides: "unconditional unified sync handler; no legacy handler; no queue-off arm"
    - path: "src/lib/analytics-client.ts"
      provides: "analytics client without computeAnalytics"
  key_links:
    - from: "src/app/api/keys/sync/route.ts"
      to: "unifiedKeysSyncHandler"
      via: "unconditional dispatch (no isUnifiedBackboneActive branch)"
      pattern: "unifiedKeysSyncHandler"
---

<objective>
**STAGE B — GATED, DO NOT AUTO-EXECUTE.** Waits for explicit user go (see gated_on).

D2 + D4 #10: delete the TS-route flag arms and the legacy keys/sync handler. This is the
"retire ALL re-entry FIRST" TS half of the LOCKED D4 zombie order — `legacyKeysSyncHandler`
(re-entry #3, POSTs /api/compute-analytics via `computeAnalytics`) dies HERE, one wave
BEFORE the Python dark-path core is deleted (106-09). All of Stage B ships as ONE PR, so
the deploy is atomic; the wave order enforces the grep-gate sequencing.

DISCIPLINE for every deletion in this plan (SC-4): delete ONLY code reachable exclusively
under flag-off/queue-off. For each site, write the one-line dormancy proof (the condition
chain that makes it unreachable with both flags pinned on) in the commit message. If an
arm turns out to be reachable on the true path (e.g. a side-effect the unified flow still
invokes), LEAVE IT and record the deviation loudly — do not force the delete.

Scope boundary: the flag READER functions (`feature-flags.ts` isUnifiedBackboneActive,
flag-monitor, error-rollup, Python arms) are plan 106-10, NOT here. Here: route arms only.
Output: dead flag arms gone from 6 TS files; unified arms unconditional; tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-CONTEXT.md (D1 stage-B, D2, D4)
@.planning/phases/106-cutover-flip-delete-legacy-janitor/106-PATTERNS.md (§D2/D1 per-flag table, §D4 #10)

Deletion inventory (re-grepped 2026-07-14 — RE-GREP every anchor before editing):

keys/sync/route.ts:
- `:183-199` queue-off composite arm (`USE_COMPUTE_JOBS_QUEUE !== "true"` -> stamp failed + 503). Delete; check `stampCompositeFailedUnlessComplete` for remaining callers before removing it.
- `:292` `if (await isUnifiedBackboneActive())` wrapper -> make the unified block unconditional; delete the `:321-322` legacy dispatch (`return await legacyKeysSyncHandler(...)`).
- `:526-664` `legacyKeysSyncHandler` (whole function, incl. its internal `:534` queue branch and the `:619` `computeAnalytics` call). DEPRECATED comment at `:524`.
- `:2` imports — drop `computeAnalytics`; drop `fetchTrades` too IF the legacy handler was its last caller (grep).
- `:20-50` header comment describes the two legacy paths — rewrite to describe the single unified path.

analytics-client.ts:
- `:162` `computeAnalytics` — delete AFTER `git grep -n "computeAnalytics"` shows zero non-test callers; also update the `:78`/`:412` comments that enumerate it.

csv-finalize/route.ts (⚠️ 106-04 added captureToSentry near these lines — re-grep, lines have shifted):
- `:684-694` flag-off placeholder arm inside `enqueueCsvAnalyticsAfter`'s after() (writes the "queue disabled" failed placeholder). Delete the `if (process.env.USE_COMPUTE_JOBS_QUEUE !== "true") {...return}` block. `writeFailedStrategyAnalyticsPlaceholder` KEEPS its other caller (the enqueue-failure W-2 path) — verify, don't delete the helper.
- `:1247` `USE_COMPUTE_JOBS_QUEUE` gate on the enqueue -> unconditional enqueue.
- `:1019` `if (await isUnifiedBackboneActive())` -> read the surrounding structure first; make the unified arm unconditional and delete the else/fall-through legacy arm ONLY if it is flag-off-exclusive.

finalize-wizard/route.ts:
- `:890-904` composite queue-off arm (stamp failed + throw) -> delete (enqueue becomes unconditional).
- `:934` `if (process.env.USE_COMPUTE_JOBS_QUEUE !== "true") return;` legacy single-key gate -> delete the guard line.
- `:626` `if (await isUnifiedBackboneActive())` -> unified arm unconditional; delete only the flag-off-exclusive arm. ⚠️ `runLegacyFinalize` (`:697+`) fires after() side-effects (founder email, last_sync_at touch, sync_trades enqueue) that the `:1015` load-bearing comment says the unified path does NOT replicate — if `runLegacyFinalize` is invoked ON the true path, it STAYS (it is not a false arm). Only the dispatch branches that require the flag to be off are deleted.

csv-validate/route.ts:
- `:155` `isUnifiedBackboneActive` branch -> unified unconditional; delete the false arm.

verify-strategy/route.ts:
- `:75` reader + `:293+` `legacyVerifyStrategyHandler` (comment: "Runs when isUnifiedBackboneActive() returns false... DEPRECATED: remove after 2026-05-15") -> delete the handler + make unified unconditional. This deletion was already scheduled by its own comment.

keys/validate-and-encrypt/route.ts:
- `:14-19` dormant import + `void isUnifiedBackboneActive; void getCorrelationId;` suppressions -> remove the dormant isUnifiedBackboneActive import + its void (keep getCorrelationId if actually used — grep).

Tests to update in the same commits: keys/sync/route.test.ts (legacy-handler + queue-off cases -> delete/convert), finalize-wizard/route.test.ts, csv-validate-route.test.ts; then run the FULL vitest suite — rbac-matrix/audit-coverage/no-store-coverage enumerate routes and must stay green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: keys/sync — delete legacy handler + queue-off arm; analytics-client computeAnalytics</name>
  <files>src/app/api/keys/sync/route.ts, src/app/api/keys/sync/route.test.ts, src/lib/analytics-client.ts</files>
  <action>
    Execute the keys/sync + analytics-client items from the inventory above, in order:
    queue-off arm -> unconditional unified dispatch -> delete legacyKeysSyncHandler ->
    prune imports -> rewrite header comment -> grep zero `computeAnalytics` callers ->
    delete it from analytics-client + fix the enumerating comments. Update route.test.ts:
    delete the legacy-path and queue-off cases; keep + strengthen the unified-path cases
    (they must still pass unchanged — that is the SC-4 evidence for this file). Dormancy
    proofs in the commit message per site (D2: the false arms are 503/placeholder, and the
    `:292` legacy dispatch requires the kill-switch row AND env fallback both off — off
    never occurs with the ratified pins).
    Commit `feat(106-07)!: STAGE B — delete legacyKeysSyncHandler + queue-off arms (re-entry #3 retired)`
    (git add the three files). NO git branch operations.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/keys/sync/route.test.ts && npx tsc --noEmit && ! git grep -qn "legacyKeysSyncHandler" -- src && ! git grep -qn "computeAnalytics" -- src/lib/analytics-client.ts && echo KEYS-SYNC-CLEAN</automated>
  </verify>
  <done>Zero legacyKeysSyncHandler / computeAnalytics references in src; unified-path tests green unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: csv-finalize + finalize-wizard — delete queue/flag false arms</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, src/app/api/strategies/finalize-wizard/route.ts, src/app/api/strategies/finalize-wizard/route.test.ts</files>
  <action>
    Execute the csv-finalize and finalize-wizard inventory items. Respect the two guarded
    subtleties: (a) `writeFailedStrategyAnalyticsPlaceholder` keeps its enqueue-failure
    caller — only the flag-off CALL SITE dies; (b) `runLegacyFinalize` stays if the true
    path invokes it (the :1015 side-effect-gap comment is load-bearing — do not delete it
    or the Sentry warning it references unless the arm is provably flag-off-exclusive).
    Each deletion gets its dormancy proof in the commit message. Update
    finalize-wizard/route.test.ts (queue-off composite case now asserts the unconditional
    enqueue instead of the 503 stamp). The 106-04 fail-loud tests
    (csv-finalize-after-failloud) must remain green — the 4 capture sites survive minus
    the flag-off arm, whose warn-only branch was deleted with its block; if one of the 4
    tested paths WAS the flag-off arm, update that test to the surviving paths and say so.
    Planner note: the 4 tested paths (placeholder err/throw, enqueue err/throw) are all
    OUTSIDE the flag-off block — expect no overlap.
    Commit `feat(106-07)!: STAGE B — finalize routes lose USE_COMPUTE_JOBS_QUEUE + backbone false arms`
    (git add the three files). NO git branch operations.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/finalize-wizard/route.test.ts src/__tests__/csv-finalize-rpc.test.ts src/__tests__/csv-finalize-c14-regression.test.ts src/__tests__/csv-finalize-after-failloud.test.ts && ! grep -qn "USE_COMPUTE_JOBS_QUEUE" src/app/api/strategies/csv-finalize/route.ts src/app/api/strategies/finalize-wizard/route.ts && echo FINALIZE-CLEAN</automated>
  </verify>
  <done>Zero USE_COMPUTE_JOBS_QUEUE reads in both finalize routes; all finalize suites green; runLegacyFinalize disposition recorded.</done>
</task>

<task type="auto">
  <name>Task 3: csv-validate + verify-strategy + validate-and-encrypt false arms; full-suite gate</name>
  <files>src/app/api/strategies/csv-validate/route.ts, src/__tests__/csv-validate-route.test.ts, src/app/api/verify-strategy/route.ts, src/app/api/keys/validate-and-encrypt/route.ts</files>
  <action>
    Execute the remaining inventory: csv-validate `:155` false arm; verify-strategy
    `legacyVerifyStrategyHandler` deletion + unconditional unified at `:75` (keep the
    105.1 compute-unified/persist-nothing teaser semantics byte-identical — the true arm
    is untouched); validate-and-encrypt dormant import removal. Update
    csv-validate-route.test.ts. Then the plan-level gate: run the FULL vitest suite with
    coverage (route-enumeration + coverage thresholds are blocking CI gates) and
    `npm run lint` (react-hooks errors are lint-only). Grep-gate the whole src tree:
    `git grep -n "USE_COMPUTE_JOBS_QUEUE" -- src` must return ZERO hits (all reads
    deleted; the env var itself stays set in Vercel harmlessly — note it in the SUMMARY
    as orphaned-env, cleanup optional post-phase).
    Commit `feat(106-07)!: STAGE B — csv-validate/verify-strategy/validate-and-encrypt false arms deleted`
    (git add the four files). NO git branch operations.
  </action>
  <verify>
    <automated>! git grep -qn "USE_COMPUTE_JOBS_QUEUE" -- src && npx vitest run --coverage 2>&1 | tail -5 && npm run lint && echo TS-ARMS-CLEAN</automated>
  </verify>
  <done>Zero USE_COMPUTE_JOBS_QUEUE readers in src; full vitest + coverage + lint green; dormancy proofs recorded per site.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> finalize/sync routes | auth/validation layers of the TRUE path are untouched |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-16 | Tampering (SC-4 divergence) | true-path code | mitigate | delete only flag-off-exclusive arms; per-site dormancy proof; unified-path tests must pass UNCHANGED |
| T-106-17 | DoS (side-effect loss) | finalize-wizard runLegacyFinalize | mitigate | explicit stay-if-true-path rule; :1015 load-bearing comment preserved |
| T-106-18 | Elevation | route auth | accept | no auth code touched — deletions are post-auth branches |
</threat_model>

<verification>
- All three task gates green; full vitest coverage thresholds hold.
- SC-4 evidence: zero changes to any unified-arm test expectation.
</verification>

<success_criteria>
- Re-entry #3 dead; all TS flag false-arms dead; unified arms unconditional; ships in the single Stage-B PR with 106-06/08/09/10.
</success_criteria>

<output>
Create `.planning/phases/106-cutover-flip-delete-legacy-janitor/106-07-SUMMARY.md` when done.
</output>
