# Roadmap: Quantalyze

## Milestones

- ✅ **v1.0–v1.8** — shipped; see `MILESTONES.md` + `milestones/` archives.
- ✅ **v1.9 — Multi-Key Composite Strategy** — Phases 85–91 + 90.5 (tag `v1.9` @ `044bee50`, 2026-07-11). Archive: `milestones/v1.9-ROADMAP.md`.
- ✅ **v1.9.1 — Composite Onboarding Hardening** — Phases 92–97 (tag `v1.9.1` @ `be215b15`, v0.41.1.0, 2026-07-12; PR #611). Archive: `milestones/v1.9.1-ROADMAP.md`. ⚠️ CI-01 `-n auto` parallelization deferred → v1.11.
- 🚧 **v1.10 — Demo-Hero Portfolio Intelligence + Options MTM + Backbone Unification** — Phases 98–108 (below). Scope EXPANDED (user, 2026-07-12): Backbone merge (M-E) pulled IN as Phases 103–106 ("add 2-3 more phases → a finished great feature").
- ⏳ **v1.11 (proposed)** — CI/Test-Infra Ratchet (M-C), incl. the v1.9.1-deferred `-n auto`. See `FUTURE-MILESTONES.md`. (M-E moved into v1.10.)

---

# v1.10 — Demo-Hero Portfolio Intelligence + Options MTM + Backbone Unification (Phases 98–108)

**Milestone goal:** Make the allocator dashboard the 10/10 demo-hero surface for the next cap-intro / pilot-allocator meeting (the project north-star), and give options-trading strategies an honest working mark-to-market view on the factsheet. Scope EXPANDED (user, 2026-07-12): after the PI dashboard (98–100) + options-MTM toggle (101–102), the user chose the FULL architecture — the Backbone Unification (M-E) is pulled in as Phases 103–106 so the dailies-canonical toggle is complete (charts follow) and the composite routes through the one CSV path. Only M-C (CI/test-infra) stays deferred to v1.11.

**Standing invariants (LOCKED, from v1.8/v1.9):**
- **No-invented-data** — a widget/strategy with no data renders an honest empty state or DQ marker, never a fabricated number or flat zero-line.
- **SC-4 byte-identity** — every existing single-key + published-composite factsheet stays byte-identical unless a fix's blast radius is explicitly evidenced (esp. MTM must not perturb `cash_settlement`).
- **Worker-only decryption** — key ciphertext never leaves the server; new position/notes reads return metadata only.
- **Migrations auto-apply to prod on merge** — SECDEF-hardened, routed through migration-reviewer + rls-policy-auditor.

## Phases

- [x] **Phase 98: Portfolio Data Foundation** ✅ — the position-level exchange data + historical-positions + weight-history read layer the PI widgets need, plus the cross-process recompute UNIQUE INDEX (PI-07). Data plumbing before UI. `[PI-07; foundation for PI-01/02/03]`
- [x] **Phase 99: Exposure & Allocation Widgets** ✅ — render Exposure by Asset Class (PI-01), Net Exposure Over Time (PI-02), Allocation Over Time (PI-03) on the Phase-98 foundation, coverage-mask-consistent, honest empty states. `[PI-01/02/03]`
- [x] **Phase 100: Optimizer Sleeve + Favorites UX + Notes + KPI Panel** ✅ — replace the hardcoded 10% sleeve with the optimizer + favorites sort/group/bulk/tooltips (PI-05); Notes widget + `user_notes` storage (PI-04); fold `PortfolioKPIRow` into the shared-panel pattern (PI-06). `[PI-04/05/06]`
- [ ] **Phase 101: Options MTM toggle (analytics/pnl_basis)** — wire the factsheet mark_to_market basis to recompute from the ingested API mark data via the EXISTING `pnl_basis=mark_to_market` path for options strategies (NOT a new smoothing algorithm — user-rescoped 2026-07-12); single-strategy first; `cash_settlement` byte-identical. `[MTM-01 core]`
- [x] **Phase 102: Options MTM Factsheet + Composite + Regression** ✅ — enable the factsheet basis toggle for single-key options (MTM-01 UI), Option-A honest-gated composite (MTM-02), static byte-identity regression + `mtm_anchor_race` (MTM-03). `[MTM-01/02/03]`
- [ ] **Phase 103: MTM daily series → charts follow the toggle** — persist the MTM daily return series (COMPUTED then DISCARDED today at `job_worker.py:2357/:2983` single-key, `:4258` composite) via a SHARED "persist dailies → derive scalars → build coverage mask" route both derive paths call (backbone-aligned, NOT bolted onto the bespoke composite path), so the basis toggle swaps the DAILIES and ALL charts follow. **Unify single-key + composite.** **Full per-basis coverage mask.** Scalars kept as a derived cache + anti-divergence guard. No new valuation math. `[MTM-04; the durable shared route the backbone merge extends]`
- [x] **Phase 104: Cash daily series onto the backbone** ✅ — ⚠️premise corrected (104-RESEARCH): `process_key_long` ALREADY persists (fixed+prod-verified 2026-05-27, PR #335/#336 — [[project_unified_queued_path_scaffold]]) via the LEGACY chain; 104 is a ROUTING change — at the single-key broker-derive seam (`job_worker.py:2934-3136`, where 103's MTM rides `derive_basis_series`) ADD an additive DARK cash-**series** persist (`derive_basis_series(cash) → persist_basis_series`, ~15-40 LOC). SERIES-ONLY: cash SCALARS stay legacy (scalar cache-ification = 105 — SC-4). `[BB-01]`
- [x] **Phase 105: Composite → the one CSV finalize route** ✅ CLOSED 2026-07-14 (`60b80cc6..6eef30d1`, 5 plans/3 waves; verifier 6/6; Fable red team NO-BLOCKER, SC-4 byte-identity held vs the legacy oracle on all 3 surfaces; M1 fixed `6eef30d1`) — composite cash + CSV runner + single-key seam all on the ONE shared `derive_basis_series`; `_metrics_result_for` DELETED (grep-gate 0); two `periods_per_year` rules → `periods_per_year_for_asset_class` (fail-loud retained); SC-5 ordered-idempotent finalize; MED-1 + MED-2 landed; no DDL. Carries: M2→106 (seam MTM prestamp ordering, pre-existing), M3 doc (round-trip = build-time not runtime), L1–L4 known-limitations. Behind flag. `[BB-02]`
- [ ] **Phase 105.1: Onboarding onto the backbone** — Tier-2 collapse #4: the sync/teaser onboarding arm persists a daily series through the shared route so no scalar-without-series window exists. CARVED from 105 (discuss-phase D2) because the teaser arm (`routers/process_key.py:854-948`) can run with NO `strategy_id` (landing-page verify; wizard creates the strategy at finalize), so `persist_basis_series`'s `(strategy_id,kind)` key needs its own verification-scoped persistence design. Gets its own research→discuss→plan cycle after 105. `[BB-02 part]`
- [ ] **Phase 106: Cutover — flip + delete legacy + janitor** — staged prod flag flip (`USE_COMPUTE_JOBS_QUEUE` permanent), delete legacy finalize branches (dark `run_strategy_analytics` path + `routers/analytics.py` + `phase12_backfill_enqueue.py` — retire ALL 4 re-entry points first), `computing`-janitor cron + `after()` fail-loud, FULL E2E after each flip ([[project_unified_backbone_csv_flag_flip]]). Series-store fold decision (rename `csv_daily_returns`→`daily_returns` + `basis` col, MTM folds in — recommended) locked in 105 planning. The prod-risk phase. `[BB-03]`
- [x] **Phase 107: Leverage as a dailies transform** — leverage becomes a preparation transform (levered basis): apply `r→L·r` to the dailies, re-run the shared `deriveSeriesBundle`/`derive_basis_series` so the ENTIRE factsheet re-derives levered (charts + rail + α→L·α, β→L·β render honestly). Precedent: `scenario.ts:427`. DELETES the frontend `useLeveragedMetrics`/`useModeledLeverage` re-scale + its disclosure code (~780 LOC); keeps the slider state. `[LEV-BB]` (completed 2026-07-15)
- [x] **Phase 108: Scenario-planner onto the backbone** — the scenario-planner/blend flows from the one backbone (dailies→`deriveSeriesBundle`), not the TS second-Sharpe. DELETES `scenario-blend-panels.ts` (~211 LOC). Keep `metrics-parity.test.ts` (guards the Python↔TS backbone). `[SCEN-BB]` (completed 2026-07-15)

## Phase Details

### Phase 98: Portfolio Data Foundation
**Depends on:** Nothing (v1.9.1 shipped). **Delivers:** PI-07 + the read layer for PI-01/02/03.
The PI widgets are blocked on data, not UI: Exposure/Net-Exposure/Allocation need position-level exchange data, historical positions, and weight history that today is absent or placeholder. This phase builds/plumbs those reads (owner-scoped, secretless) and fixes PI-07 (cross-process portfolio-recompute duplicate-`computing` rows) with a real-PG UNIQUE-INDEX integration test — a data-integrity foundation the widgets and optimizer both stand on.

**Plans:** 3/3 plans executed ✅ (+ fable red-team F-1/F-2 fixed)

Plans:
- [x] 98-01-PLAN.md — PI-07 DB fence: Wave-0 real-PG concurrency SQL test + dedupe-first partial UNIQUE index migration (replaces `idx_portfolio_analytics_computing`) + down rollback
- [x] 98-02-PLAN.md — PI-07 code side: 23505→409 at the `computing` INSERT (`portfolio.py`) + 409→`in_flight` branch (`cron.py`), failing-first pytest
- [x] 98-03-PLAN.md — PI-01/02/03 read foundation: `src/lib/portfolio-exposure.ts` (holding_type-primary exposure grain, signed net series, per-venue allocation weights, marked gaps, honest-empty) + vitest

### Phase 99: Exposure & Allocation Widgets
**Depends on:** Phase 98. **Delivers:** PI-01/02/03.
Three dashboard widgets render on the foundation: asset-class exposure breakdown (per-exchange), net exposure over time, allocation-over-time from weight history. Gap-handling mirrors the factsheet coverage-mask discipline (marked gaps, no zero-fill); a strategy with no position data shows an honest empty state, never a fabricated series.

**Plans:** 4 plans

Plans:
- [ ] 99-01-PLAN.md — PI-01 ExposureByClass: segmented composition bar + KPI strip (Gross/Net/Long/Short) + drilldown table + honest-empty
- [ ] 99-02-PLAN.md — shared chart-gaps lib (hatched bands, null sentinels, F-2 domain) + PI-02 NetExposureChart (gross Area + net Line, USD axes)
- [ ] 99-03-PLAN.md — PI-03 AllocationOverTime: 0–100% stacked area, STRATEGY_PALETTE venues, F-2 boundary gap bands
- [ ] 99-04-PLAN.md — page.tsx Promise.all wiring (auth.uid id, distinct exposure prop, error→error.tsx) + HoldingsTabPanel Exposure section + integration tests + live >1000-row checkpoint

### Phase 100: Optimizer Sleeve + Favorites UX + Notes + KPI Panel
**Depends on:** Phase 98 (optimizer needs the data layer). **Delivers:** PI-04/05/06.
Replaces the hardcoded 10% favorites sleeve with real optimizer output and adds favorites sorting/grouping + bulk toggle + KPI narrative tooltips (PI-05); adds the Notes widget backed by new owner-scoped `user_notes` storage (PI-04); folds the bespoke `PortfolioKPIRow` into the shared-panel pattern (PI-06).

**Plans:** 2/4 plans executed

Plans:
- [x] 100-01-PLAN.md — PI-04: dashboard scope_kind migration + RLS SQL test + DashboardNoteCard (notes stack reused verbatim)
- [x] 100-02-PLAN.md — PI-05 components: watchlist-read contract + WatchlistPanel (table/sort/group/bulk) + OptimizerPanel (ranked scores, never weights)
- [ ] 100-03-PLAN.md — PI-06: KpiPanel extraction (KpiStrip byte-identical) + PortfolioKpiPanel adapter + delete PortfolioKPIRow
- [ ] 100-04-PLAN.md — Wave 2 wiring: page.tsx Promise.all + prop threading + HoldingsTabPanel sections (SC-4 additive)

### Phase 101: Options MTM toggle (analytics / pnl_basis)
**Depends on:** Nothing (independent of PI; could run in parallel). **Delivers:** MTM-01 core.
**Goal:** the single-key Deribit derive runs a SECOND `pnl_basis=mark_to_market` pass (mirroring the composite dual-pass) and persists `metrics_json_by_basis.mark_to_market` additively, with an honest single-key gate that degrades-with-reason (`mtm_gated_reason`) when the `options_settlement_summary` channel can't cover the book (pre-rollout straddle / summary hole). NO smoothing (user-rescoped 2026-07-12 — the raw MTM curve IS the honest output); the MTM signal is the ingested settlement-summary `realized_pl + unrealized_pl` session delta (NOT a `mark_price` column — RESEARCH A1). `cash_settlement` output stays byte-identical (SC-4). Re-derive of existing options rows = ship-time step (post-deploy).
**Plans:** 2 plans

Plans:
- [ ] 101-01-PLAN.md — Second single-key MTM ledger pass + honest gate + additive metrics_json_by_basis persistence (+ SC-4 derive-level parity tests)
- [ ] 101-02-PLAN.md — Finalizer mtm_gated_reason bridge (survives the CSV flag rebuild) + phase SC-4 gate (Zavara parity, coverage) + ship-time re-derive handoff

### Phase 102: Options MTM Factsheet + Composite + Regression
**Depends on:** Phase 101 (+ v1.9 composite read-path). **Delivers:** MTM-01 UI + MTM-02/03.
Enables the factsheet `cash_settlement ↔ mark_to_market` SegmentedControl for options strategies (removing the disabled-with-reason gate where smoothing now works), composes it with the composite stitch + coverage mask (MTM-02), and pins Zavara MTM corroboration + a full golden/parity byte-identity regression on every existing non-options factsheet (MTM-03).

### Phase 103: MTM daily series → charts follow the toggle
**Depends on:** Phase 102 (MTM scalars + toggle shipped). **Delivers:** MTM-04 — the durable shared route Phases 104–106 extend.
**Goal:** the basis toggle swaps the daily SERIES feeding the one common downstream (`buildFactsheetPayload`), so ALL charts follow (equity/drawdown/returns/rolling/heatmaps), single-key AND composite, with a per-basis coverage mask derived from the persisted sparse MTM series. Shared `services/basis_series.py::derive_basis_series` is the dailies-canonical route BOTH derive sites call (replaces the bespoke composite `_metrics_result_for(clipped_mtm)`); scalars stay a derived cache with a round-trip anti-divergence guard. NO DDL (new `strategy_analytics_series` kind `mtm_daily_returns` via the existing batch RPC). No new valuation math; SC-4 cash byte-identity.
**Plans:** 4/4 plans executed (+14 `fix(103)` commits; Fable red team NO BLOCKERS; MED-2 deferred known-limitation)

Plans:
- [x] 103-01-PLAN.md — OQ1 sparsity probe + shared `derive_basis_series`/`persist_basis_series` (round-trip guard, gap spans, heal) — Wave 1
- [x] 103-02-PLAN.md — wire single-key (:2983) + composite (:4249) seams to the shared route + stale-row heal + cash-golden sweep — Wave 2
- [x] 103-03-PLAN.md — payload plumbing: `seriesByBasis.mark_to_market` bundle (own axis + mask), shared `readMtmSeries`, SC-4 snapshot pin — Wave 2 (parallel)
- [x] 103-04-PLAN.md — charts follow: `useBasisSeriesView` view-merge (frozen-spine-safe), heatmaps/worst-10, basis-aware caption + a11y, falsifiable keystone test — Wave 3

### Phase 104: Cash daily series onto the backbone (single-key/broker-derive seam)
**Depends on:** Phase 103 (the shared `derive_basis_series` route). **Delivers:** BB-01.
**Goal:** ⚠️PREMISE CORRECTED (104-RESEARCH, source-verified): `process_key_long` is NO LONGER an unfinished scaffold — it was fixed + prod-E2E-verified 2026-05-27 (PR #335/#336) and already persists trades/positions/analytics via the LEGACY `sync_trades → compute_analytics` chain ([[project_unified_queued_path_scaffold]]). So 104 is a backbone-ROUTING change, not a persistence bootstrap: at the single-key broker-derive seam (`job_worker.py:2934-3136`, where Phase-103's MTM already rides `derive_basis_series`) ADD an additive cash-series persist — `derive_basis_series(cash_returns, basis="cash_settlement") → persist_basis_series` + one `_KIND_BY_BASIS` entry (~15-40 LOC, mirrors 103's MTM block). A DARK/additive write: nothing reads the new `cash_settlement` series row this phase, so it is "no prod behavior change." **SC-4 SERIES-ONLY BOUNDARY (load-bearing):** persist the cash SERIES only; the authoritative cash SCALARS STAY on the legacy path. Do NOT make cash scalars a `derive_basis_series` cache in 104 — its `_drop_nonfinite`→`gap_fill`(0.0) pipeline diverges from the legacy cash NaN/sparse semantics (broker series reinstates interior NaN breaks; user-CSV stays sparse) → different Sharpe/vol → SC-4 violation. Scalar cache-ification (with the convention-matching that preserves SC-4) = Phase 105. No DDL (existing `strategy_analytics_series` TEXT kind + hardened batch RPC), no new SECDEF, no test-project catch-up. No new valuation math. `[BB-01]`

**Plans:** 2/2 plans executed ✅ (Fable red team NO BLOCKERS; MED-1/MED-2 recorded as Phase-105 pre-reqs; LOW-1 docstring fixed `bca0ac0d`)

Plans:
- [x] 104-01-PLAN.md — persist seam: `_KIND_BY_BASIS` cash entry + benchmark-identity conventions echo (SC-5) + additive DARK cash-series persist at the broker-derive seam (mirrors 103's MTM block; heal matrix; A3 honest-skip)
- [x] 104-02-PLAN.md — proof: SC-4 dual-run byte-identity + series round-trip/coverage mask + SERIES-ONLY grep gate + INERT-read repo scan + full-suite cash golden sweep (7 tests, `test_cash_basis_series_sc4.py`; commits `64be3e7c`/`c8e2c39b`; 92.50% cov, SC-3 sweep green; 2 mutations spot-checked; tests-only)

**Success Criteria** (what must be TRUE):
  1. The single-key/broker cash daily SERIES is persisted via the shared `derive_basis_series`/`persist_basis_series` route (a new `cash_settlement` entry in `_KIND_BY_BASIS`), at the same seam where Phase-103's MTM rides the backbone — a `cash_settlement` series row + coverage mask exists after a broker-derive run, and the series round-trips (the sparse persisted rows reproduce the persisted series).
  2. SERIES-ONLY boundary held: the authoritative cash SCALARS are UNCHANGED (still the legacy compute path) — a grep/assert proves 104 did NOT route cash scalars through `derive_basis_series` (that is Phase-105 scope); no scalar column in `strategy_analytics` is rewritten by this phase.
  3. SC-4: every existing single-key + published-composite CASH factsheet stays byte-identical — the new series row is UNREAD this phase (dark write) and scalars are untouched; proven by a cash golden byte-identity sweep.
  4. INERT to prod: with `USE_COMPUTE_JOBS_QUEUE` at its prod default, path selection and outputs are unchanged; a test asserts no read path consumes the new `cash_settlement` series row yet (the reader lands in a later phase).
  5. No new valuation math (`compute_all_metrics` untouched; diff is persistence/routing only); the benchmark-identity carry (α/β/corr alongside `conventions`) is wired so Phase-105's scalar route can reproduce them from persisted rows.
**UI hint:** no (worker/persist seam; no frontend surface).

### Phase 105: Composite → the one CSV finalize route
**Depends on:** Phase 104. **Delivers:** BB-02 + delete the bespoke composite CASH path (`_metrics_result_for`, the last cash bypass — Phase 103 only retired its MTM arm) + the CSV single-key inline swap (#2, `analytics_runner.py:2318`, co-lands here) + collapse the two `periods_per_year` rules into one convention (#5) + **fold the venue-source + allocated-capital forks (Tier-2 collapse #6)** + the series-store fold DECISION. ⚠️**Onboarding unify (#4) CARVED to Phase 105.1** (unconditional — discuss-phase D2: the teaser arm can run with NO `strategy_id`, so `persist_basis_series`'s `(strategy_id,kind)` key can't be reused → separate persistence design).
**Goal:** route composite stitch through the shared/CSV finalize path — composites first derive stitched dailies then take the SAME route as CSV/API-keys ([[feedback_dailies_canonical_unified_derive]]); make csv-finalize transactional; behind flag. Lock the series-store fold (rename `csv_daily_returns`→`daily_returns` + `basis` column; MTM folds in — RECOMMENDED, a tall allocator-indexed table with ~15 readers incl. GDPR, not a JSONB blob). ⭐Carry the BENCHMARK IDENTITY alongside `conventions` (α/β/corr are outside the persist round-trip guarantee). SC-4. `[BB-02]`

**Plans:** 3/5 plans executed
Plans:
- [x] 105-01-PLAN.md — W1: D1 SC-4 mechanism (`scalar_returns`/`densify_policy`/`nan_dates` + schema-2 bump) + densify-aware round-trip guard + composite guard-NaN flagship fixture
- [x] 105-02-PLAN.md — W1: MED-1 read-gate predicate (`shouldReadCashSettlementSeries`, D3 primary) + series-store fold DECISION doc (D6, decide-only)
- [x] 105-03-PLAN.md — W2: #6/MED-2 venue-agnostic `denominator_config` hoist at the single-key seam + seam `broker_nan` densify-tag + terminal-arm heal-deletes + gate-respecting harness
- [x] 105-04-PLAN.md — W3: #2 runner swap (`analytics_runner.py:2318` → shared derive, series-before-scalar) + user-CSV-weekend/broker-guard-day/Zavara SC-4 dual-runs
- [x] 105-05-PLAN.md — W3: #1 composite re-route + `_metrics_result_for` DELETE (grep-gated) + #5 clock collapse (D4 retained assert) + SC-5 ordered-idempotent finalize + LOW-2 benchmark identity

**Collapses owned (Tier-2):** #1 (cash → `derive_basis_series` + DELETE `_metrics_result_for` at `job_worker.py:4178`), #2 (CSV single-key inline swap `analytics_runner.py:2318` — co-lands with #1), #5 (one `periods_per_year` convention — collapse to `periods_per_year_for_asset_class`, KEEP a fail-loud sanity assert; F-1 backstop `044bee50` proves no live divergence), #6 (venue-source `combine_native_ledger`/`combine_realized_and_funding` `job_worker.py:2045/1954` + allocated-capital `denominator_config` `job_worker.py:2149` forks move UPSTREAM into preparation, feeding a single `scalar_returns` derive — this fixes MED-2). Series-store fold (#3) DECIDED here, EXECUTED in 106. **#4 (onboarding) → Phase 105.1.**
**⭐SC-4 mechanism (LOCKED, discuss-phase D1):** add `scalar_returns` param + `densify_policy` echo to `derive_basis_series` (default = today's `gap_fill(sparse)` → MTM byte-identical); cash callers pass the exact legacy-conditioned series → scalar byte-identical by construction; persisted rows stay `_drop_nonfinite`. ⚠️composite (`"zero_fill"`) payload ALSO carries an additive `nan_dates` key (NO DDL — schema-version bump) so the round-trip guard reconstructs member-guard NaN (`gap_fill(rows)` then reinstate NaN) — WITHOUT it the guard reddens on the composite guard-NaN fixture.
**⚠️104-CARRY PRE-REQS (from the Phase-104 Fable red team — DARK/latent in 104, DETONATE when 105 lights the cash reader / makes cash scalars a cache):**
- **MED-1 (Pitfall-5 stale-row heal):** pre-seam terminal-failure arms (`<2-interpretable-days` `job_worker.py:2790-2821`, `_stamp_deribit_analytics_failed`, NAV-error) null the scalars (`metrics_json_by_basis=None`, `computation_status='failed'`) and return BEFORE the heal seam → a stale `cash_settlement` (and `mtm_daily_returns`) series row can outlive an authoritative-NULL scalar write. Today mitigated by read-side status-gating. 105 MUST either (a) status-gate the cash reader/round-trip like the MTM read side, OR (b) heal BOTH series rows at those terminal arms. Fixes the pre-existing Phase-103 MTM half too.
- **MED-2 (conventions echo divergence):** the 104 cash echo resolves `denominator_config` only in the deribit arm (`job_worker.py:2162`); a ccxt strategy with a non-null override echoes geometric/calendar while its authoritative legacy scalars use simple/active (`analytics_runner.py:2304-2316`, venue-agnostic) → 105's round-trip guard reddens / collapse reproduces wrong scalars. Zero prod rows today (config Zavara/Deribit-only). 105 MUST resolve the cash echo from the strategy row venue-agnostically, exactly as `analytics_runner.py` does.
- **LOW-2:** the composite stitch derive (`job_worker.py:4411`) omits `benchmark_symbol` → composite MTM/cash payloads carry NO benchmark identity (keeps composite byte-identical now); 105's α/β re-derive needs it carried on composite rows too.
- **LOW-3/LOW-4:** `BROKER_DAILIES_VIA_FUNDING=false` rollback also orphans dark rows (same class as MED-1); the INERT-read grep tripwire misses a reader via an imported constant — 105's reader-landing review must not rely on it as proof.
**Success Criteria** (what must be TRUE):
  1. Composite stitch first derives stitched dailies then takes the SAME shared `derive_basis_series` route as CSV/API-keys; the bespoke `_metrics_result_for` composite-cash closure is DELETED (grep-gated) and composite cash scalars become a cache of a persisted cash series.
  2. The two `periods_per_year` rules collapse to ONE convention — the composite venue-blend rule (`job_worker.py:4115`) and the single-key `asset_class` rule (`analytics_runner.py:2299`) resolve identically; `_COMPOSITE_CRYPTO_VENUES` kept ONLY as the degrade/unknown-venue backstop.
  3. The venue-source fork (deribit `combine_native_ledger` vs ccxt `combine_realized_and_funding`) and the allocated-capital `denominator_config` branch (Zavara) vary the returns SOURCE in preparation, feeding a SINGLE derive/persist call — no venue/denominator branching remains inside the derive path.
  4. (MOVED → Phase 105.1) Onboarding opens NO scalar-without-series window. — Carved out of 105 per D2.
  5. csv-finalize is ordered-idempotent (per LOCKED D5 — GATED eventual consistency, NOT literal cross-table atomicity): series + dailies persist BEFORE the scalar/status flip, so a `complete` scalar never exists without its series; MED-1's status-gate un-trusts every partial predecessor state; a worker death → an authoritative re-derive retry. The residual re-derive partial-chart window is PRE-EXISTING (not made worse); a strict-atomicity SECDEF RPC, if ever wanted, rides 106's fold DDL. Behind flag; NO DDL in 105.
  6. SC-4 holds: every existing single-key + published-composite CASH factsheet stays byte-identical (the live composite-cash re-route guarded by a golden byte-identity sweep); the series-store fold is DECIDED + locked and the benchmark identity (α/β/correlation) travels alongside `conventions`.
**UI hint:** no (worker/finalize orchestration; no frontend surface).

### Phase 105.1: Onboarding onto the backbone (Tier-2 collapse #4) ✅ CLOSED 2026-07-14
**Depends on:** Phase 105 (the shared cash route + `scalar_returns`/`densify_policy` mechanism). **Delivers:** BB-02 part (collapse #4). **CLOSED** (`2a6c6888..45900f48`, 2 plans/1 wave; verifier 6/6; Fable red team NO-BLOCKER; opus code-review NO-BLOCKER; 3731 tests / 92.60% cov). Carries: M1 period-returns tiles residual (fold via period-returns-from-dailies helper — own slice), M3 build-time-not-runtime, deferred lead-capture persistence (106+), L1/L3/L4 known-limitations.
**Goal:** unify the sync-teaser onboarding COMPUTE onto the shared route. LOCKED (research→fable-discuss→2 user decisions, CONTEXT D1–D6): **compute-unified, persist-NOTHING** (Option c — the teaser is a throwaway public preview with no series reader; the archived `00000…0001` sentinel makes any persisted row unreadable) → route the return-based scalars (`twr/sharpe/return_ytd/max_drawdown`) through `derive_basis_series` (4-key map), keep the trade-level builder, re-source `equity_curve` from the derived series, add a no-anchor-persist guard + advisory golden, and DOCUMENT a **permanent preview-only exception** to the persisted-series half of the invariant (SC1 reworded to "no scalar computed off-backbone"). Conventions = **crypto √365 per #597** (public preview numbers shift — accepted; flow-agnostic swap also shifts csv/internal_report previews). Zero DDL. The `metrics_snapshot` DELETE-tail + the TS teaser twin stay Phase-106. `[BB-02 part]`
**UI hint:** no (onboarding worker/verification; no factsheet surface).

### Phase 106: Cutover — flip + delete legacy + janitor
**Depends on:** Phase 105. **Delivers:** BB-03.
**Goal:** staged prod flag flip (`USE_COMPUTE_JOBS_QUEUE` permanent), delete legacy finalize branches — retire ALL 4 dark-path re-entry points FIRST (dark `run_strategy_analytics` path + `routers/analytics.py` + the UNGUARDED `scripts/phase12_backfill_enqueue.py` + the 4th) then delete the dark path (the ZOMBIE trap — [[project_milestone_v1_10_backbone_unification]]); `computing`-janitor cron + `after()` fail-loud; FULL E2E after EACH flip (a flag flip already exposed 2 latent CSV bugs — [[project_unified_backbone_csv_flag_flip]]). The prod-risk phase. `[BB-03]`

**Delivers also:** Tier-2 collapse #3 EXECUTION (series-store fold per the 105 decision) + the onboarding `metrics_snapshot` legacy-delete tail of collapse #4. ⭐**+ Deferred (USER 2026-07-14): persist a teaser daily series for LEAD CAPTURE / contactability** — key on the SUBMITTER (not the archived sentinel), readable for sales outreach; overturns the 105.1 "no reader" premise (Option a becomes justified). Likely prod DDL + RLS + lead linkage. See [[project_teaser_series_persist_for_lead_contact]]. The FOUR dark-path re-entry points: (1) `BROKER_DAILIES_VIA_FUNDING` flag path, (2) UNGUARDED `scripts/phase12_backfill_enqueue.py`, (3) `legacyKeysSyncHandler` (`api/keys/sync/route.ts:619`), (4) HTTP `POST /api/compute-analytics` + `run_compute_analytics_job`.
**Success Criteria** (what must be TRUE):
  1. `USE_COMPUTE_JOBS_QUEUE` is flipped permanent in prod via a STAGED flip, with FULL E2E (CSV/ccxt/Deribit/composite, cash+MTM) GREEN after EACH stage (a prior flip exposed 2 latent CSV bugs — [[project_unified_backbone_csv_flag_flip]]).
  2. All FOUR dark-path re-entry points are retired BEFORE the dark path is deleted; a grep proves ZERO live callers reach `run_strategy_analytics` (`analytics_runner.py:1208`) at delete time.
  3. Legacy finalize branches are DELETED — dark `run_strategy_analytics` path, `routers/analytics.py`, `phase12_backfill_enqueue.py`, `_metrics_result_for` residue — with `trades_to_daily_returns_with_status` KEPT live.
  4. The series-store fold is EXECUTED (`csv_daily_returns`→`daily_returns` + `basis` column, MTM folded in); all ~15 readers (frontend, 2 GDPR export axes, admin, composite-read) + RLS/SECDEF/migrations repointed and green; the migration auto-applies to prod on merge and objects are verified.
  5. A `computing`-janitor cron reaps orphaned `computing` rows and `after()` fail-loud surfaces silent finalize failures (no strategy stuck in `computing`); the onboarding `metrics_snapshot` home is retired only after its 3 repoints (landing card / 90-day teaser / `matched_strategy_id`·`return_24h`·`equity_curve`) land.
**UI hint:** no (prod cutover, worker/cron, deletes; no new frontend surface).

**Plans (106-PROPER, planned 2026-07-14):** 10 plans / 7 waves, split by REVERSIBILITY. SC-4 (series-store fold) → Phase 106.1; metrics_snapshot tail of SC-5 → Phase 106.2 (split adopted in 106-CONTEXT).
Plans — STAGE A (reversible) — ✅ CODE LANDED 2026-07-14 (`23bfca23..aaf54de7`; opus code-review + Fable red team both run; post-merge gate green: pytest 3732, tsc 0, lint 0, vitest 8210). ⭐**Red team B-1 (BLOCKER) → 106-03 janitor REVERTED/DEFERRED to 106.1** (queried non-existent `strategy_analytics.updated_at`; correct reaper needs a transition-timestamp DDL, fenced out of 106). Pre-existing 105-02 gate latent fixed `ddbcbb50`; MD-02 test-tightening `aaf54de7`:
- [x] 106-01-PLAN.md — W1: ratify the 3 flag env values + code-comparison greps + ratification record ✅ (live env CLI-corroborated: Vercel USE_COMPUTE_JOBS_QUEUE+PROCESS_KEY_UNIFIED_BACKBONE present; Railway PROCESS_KEY_UNIFIED_BACKBONE=on, BROKER_DAILIES_VIA_FUNDING unset→default true; RATIFY-GREPS-OK)
- [x] 106-02-PLAN.md — W2: D5/M2 single-key seam series-before-scalar ordering swap (TDD) ✅ (3 commits `23bfca23..56221947`; RED failed right; 135+68 tests green; composite heal NOT reintroduced; SC-4 2-persist invariant intact; red team + code-review SOUND)
- [~] 106-03-PLAN.md — W2: D7 computing-janitor cron ⛔**REVERTED/DEFERRED → 106.1** (`742cfb1c` reverts `77844ae1`+`a5e5a86a`+`190127c9`). Red team B-1: filtered `strategy_analytics.updated_at` (no such column; table has `computed_at` only) → 400/500 alarms every 15min, reaps nothing; wiring test mocked supabase + pinned the phantom column. `computed_at` is the WRONG semantic (last-completion → mid-compute reap hazard). Needs a real transition timestamp (store DDL) → belongs in 106.1 with migration-reviewer/rls-auditor/test-DB catch-up.
- [x] 106-04-PLAN.md — W2: D7 after() fail-loud — captureToSentry on the 4 csv-finalize warn-only paths ✅ (2 commits `242c68a7`/`0725a2f2`; +4 exactly; finalize-wizard confirmed already-paired; MD-02 warn-parity tightened `aaf54de7`; red team + code-review SOUND)
- [ ] 106-05-PLAN.md — W3: STAGE-A EXIT GATE — full D6 E2E surface on prod (checkpoint:human-verify; the one manual gate) ⏸️ DRAFTED (11-point table in 106-RATIFICATION.md; janitor spot-check dropped with the deferral) — parked: live surface runs only post-deploy (code on branch, not prod); awaits milestone deploy + user "approved"
Plans — STAGE B (IRREVERSIBLE — GATED, DO NOT AUTO-EXECUTE; one PR; needs explicit user go + empirical query == 0):
- [ ] 106-06-PLAN.md — W4: D3 RPC admission guard (both _enqueue_compute_job_internal overloads; registry/CHECKs stay)
- [ ] 106-07-PLAN.md — W4: D2/D4#10 TS arm deletions (legacyKeysSyncHandler + all USE_COMPUTE_JOBS_QUEUE/backbone false arms)
- [ ] 106-08-PLAN.md — W5: D4 Python re-entry retirement in the LOCKED zombie order (phase12 + both funding ternaries + dispatch/handler/timeout/watchdog + routers/analytics.py)
- [ ] 106-09-PLAN.md — W6: dark run_strategy_analytics chain deletion + permanent test_dark_path_deleted.py grep-gate
- [ ] 106-10-PLAN.md — W7: D1 net removal (flag-monitor alert-only, phase19-error-rollup retired, kill-switch readers deleted) + cosmetic residue

### Phase 107: Leverage as a dailies transform
**Depends on:** Phase 106 (unified backbone live). **Delivers:** LEV-BB.
**Goal:** leverage becomes a PREPARATION transform (levered basis) — apply `r→L·r` to the dailies, re-run the shared `deriveSeriesBundle`/`derive_basis_series` so the ENTIRE factsheet re-derives levered (charts + rail + α→L·α, β→L·β render honestly). DELETES the frontend `useLeveragedMetrics`/`useModeledLeverage` re-scale + its disclosure code (~780 LOC); keeps the slider state. Precedent: `scenario.ts:427`. `[LEV-BB]`

**Success Criteria** (what must be TRUE):
  1. At leverage L≠1, `r→L·r` is applied to the DAILIES and the shared `deriveSeriesBundle`/`derive_basis_series` re-runs, so the ENTIRE factsheet re-derives levered — every chart (equity/drawdown/returns/rolling) AND the rail follow, not just the 7 KpiStrip scalars.
  2. α→L·α and β→L·β render HONESTLY at L≠1 (no MODELED-eyebrow blanking of α/IR); levered α/β are real derived values.
  3. The frontend `useLeveragedMetrics`/`useModeledLeverage` re-scale + its CAVEAT/MODELED-eyebrow/α-IR-blanking disclosure code (~780 LOC) is DELETED; the slider STATE is KEPT and LEV-02 scenario leverage is untouched.
  4. At L=1 the factsheet is byte-identical to pre-change (SC-4 — the transform is a no-op at unity), proven by a golden.
  5. The transform mirrors the scenario-engine precedent (`scenario.ts:427`) — one preparation transform feeding the one backbone; no second leverage compute path remains.
**UI hint:** yes (factsheet charts + rail + leverage slider — frontend re-derive; ~780 LOC frontend delete).

**Plans:** 3/3 plans complete

Plans:
- [x] 107-01-PLAN.md — Levered-view backbone: export deriveSeriesBundle + compose the r→L·r layer into useBasisSeriesView (SC-4 by-reference short-circuit, active-basis + no-fabrication guards) + SC-2 joint L-scaling proof
- [x] 107-02-PLAN.md — Consumer rewire + delete: KpiStrip/rail read the levered view; DELETE MODELED/CAVEAT/α-IR-blanking/BASE·1× + useLeveragedMetrics/useModeledLeverage; reworded what-if copy (UI-SPEC verbatim); leverage eligible on the active resolved basis
- [x] 107-03-PLAN.md — Phase gates: permanent SC-3/SC-5 source-scan gates + full-suite/coverage/snapshot/LEV-02-untouched sweep + measurement-gated bootstrapCI perf decision

### Phase 108: Scenario-planner onto the backbone
**Depends on:** Phase 107. **Delivers:** SCEN-BB.
**Goal:** the scenario-planner/blend flows from the one backbone (dailies→`deriveSeriesBundle`), not the TS second-Sharpe stack. DELETES `scenario-blend-panels.ts` (~211 LOC). Keep `metrics-parity.test.ts` (guards the Python↔TS backbone identity). `[SCEN-BB]`

**Sequencing:** depends on Phase 107 for ORDER only — 108 does not consume 107's leverage output, so it can parallelize with 107 if leverage slips.
**Success Criteria** (what must be TRUE):
  1. The scenario-planner/blend flows derive from the one backbone (dailies → `deriveSeriesBundle`) — blend panels are emitted by the shared bundle, not a separate TS Sharpe/annualization compute.
  2. `scenario-blend-panels.ts` (~211 LOC, the one real "second Sharpe" TS compute) is DELETED; `portfolio-stats.ts`/`health-score.ts` remain (out of scope).
  3. `metrics-parity.test.ts` is KEPT and green — it now guards the Python↔TS backbone identity (more load-bearing after the reorg).
  4. Scenario-planner outputs match pre-change within parity tolerance for representative blends (no user-visible regression).
**UI hint:** yes (scenario-planner surface — frontend).

**Plans:** 2/2 plans executed

Plans:
- [x] 108-01-PLAN.md — Backbone blend-panel adapter `deriveBlendPanels` (population-std rolling primitives + explicit 63/126/252 window, min/max whiskers, usableN gate re-homed) + SC-4 mutation-falsifiable parity pins ✅ (`e9eeb097`/`0bed82bf`/`bd9033f2`; adapter suite 13 green, metrics-parity untouched, tsc+grep-gate clean)
- [x] 108-02-PLAN.md — Rewire ScenarioComposer onto `deriveBlendPanels` + DELETE scenario-blend-panels.ts (211 LOC) + its test (251 LOC) + re-anchor the :3553 positive control + permanent SC-2/SC-3 delete-gate + full verification sweep ✅ (`89e982c0`/`140e8b6e`; 3-line composer diff render-tree-untouched, 2 dangling doc-comments repointed, gate neuter-confirmed; full suite 8158/0, coverage exit 0, tsc+lint clean, frozen surface empty)

## Backbone coverage matrix (104–108)

Rows = the six Tier-2 collapses (`BACKBONE-BYPASS-INVENTORY.md` §Tier-2) + LEV-BB + SCEN-BB. Every collapse maps to exactly one owning phase; splits are intentional and marked.

| # | Collapse / transform | Phase (owner) | Req | SC anchor | Cleanly owned? |
|---|----------------------|---------------|-----|-----------|----------------|
| 1 | Cash → `derive_basis_series` + DELETE `_metrics_result_for` (last composite-cash bypass) | **105** | BB-02 | 105-SC1 | ✅ single owner |
| 2 | CSV runner / unified QUEUED path → `derive_basis_series` | **104 + 105** | BB-01/02 | 104-SC1 / 105-SC1 | ✅ split: queued-path cash SERIES persist = 104 (done); the CSV single-key inline SWAP (`analytics_runner.py:2318`) CONFIRMED in 105 (co-lands with #1, discuss-phase D-scope) |
| 3 | Fold the two daily-series stores into one (`csv_daily_returns` + MTM kind → `daily_returns`+`basis`) | **105** decision → **106** exec | BB-02 / BB-03 | 105-SC6 / 106-SC4 | ✅ decision/exec split (intentional) |
| 4 | Unify onboarding — kill the scalar-without-series window | **105.1** | BB-02 | 105.1-SC1 | ✅ CARVED to 105.1 (discuss-phase D2 — teaser arm may have NO `strategy_id`; needs own persistence design; legacy `metrics_snapshot` DELETE tail still in 106) |
| 5 | Collapse the two `periods_per_year` rules into one convention | **105** | BB-02 | 105-SC2 | ✅ single owner |
| 6 | Fold venue-source + allocated-capital forks upstream of a single derive | **105** | BB-02 | 105-SC3 | ✅ single owner |
| L | Leverage as a dailies transform (`r→L·r` → re-derive) | **107** | LEV-BB | 107-SC1 | ✅ single owner |
| S | Scenario-planner onto the backbone (delete `scenario-blend-panels.ts`) | **108** | SCEN-BB | 108-SC1 | ✅ single owner |

**Coverage verdict:** all six Tier-2 collapses + LEV-BB + SCEN-BB are owned; no orphan, no silent overlap. Two intentional splits: #3 (decide in 105 / execute in 106) and #4 (unify in 105 / legacy delete-tail in 106). One boundary to confirm at plan-time: #2's CSV single-key inline swap may sit in 104 or co-land with #1 in 105 (inventory sequences it "with #1"). **Load-note:** Phase 105 carries collapses #1/#4/#5/#6 — a heavy phase; a 105.1 split (onboarding OR fork-collapse) is a legitimate plan-time option if the plan exceeds ~2 waves.

**Dead-route retirements (all in Phase 106, gated on re-confirm-against-source):** `_metrics_result_for` (dead after #1), dark `run_strategy_analytics` (4 re-entry points — the ZOMBIE trap), CSV inline compute swap, `csv_daily_returns` store fold, composite `periods_per_year` rule + F-1 backstop, onboarding `metrics_snapshot` (3 repoints).

**Explicitly OUT OF SCOPE (deferred → v1.11, no phase created here):** Tier-4 E1 portfolio aggregation (`portfolio_metrics.py` second Sharpe/TWR — MWR/Dietz STAY, cashflow/IRR not series-derivable), Tier-4 E2 allocator equity reconstruction (`equity_reconstruction.py`), E3 allocator matching (`match.py`), and Tier-5 remainder (`portfolio-stats.ts`/`health-score.ts` stay; out-of-scope live Sharpe stacks: optimizer, allocated_capital, NAV builder).

## Open sequencing note
PI (98–100) and MTM (101–102) are independent tracks — MTM can start any time. Ordered PI-first because the dashboard is the primary demo surface; if the options-MTM demo is more urgent, 101–102 can lead. Confirm at `/gsd:plan-phase`.
