# Requirements — v1.15 MetaTrader 5 (live api_verified account sync) — ARCHIVED

**Status:** ✅ SHIPPED + LIVE 2026-07-25 · **Tag:** `v1.15` · **15/15 v1 requirements satisfied**
**Defined:** 2026-07-23 · **Went live:** 2026-07-25 (flags flipped; quantalyze.xyz → 200)
**Core Value:** Allocators/managers connect a LIVE MetaTrader 5 account (read-only investor login) and get an `api_verified` institutional factsheet from real deal history + equity — defeating the fabricatable legacy MT5 EA/CSV `self_reported` numbers. The v1.12 sFOX "live read = ground truth" arc applied to MT5's forex/CFD world.

**Founder scope decisions (2026-07-23):** LIVE account sync (not statement ingest); self-hosted Wine gateway + `mt5linux` net client (not MetaApi.cloud / broker Manager API); FULL GO-LIVE this milestone.

**Outcome:** All 15 v1 reqs satisfied. The 4 `human_needed` go-live legs (MT5GW-01, MT5SPIKE-01, MT5GOLIVE-01/02) were completed 2026-07-25 with evidence — live cold-boot on Vantage acct 26547876 @ `VantageMarkets-Live 5` (trade_allowed=False), soak day-1 green (recon_residual 1.4e-10), full factsheet proven (−20.9%, √252), flags flipped LIVE. See `milestones/v1.15-ROADMAP.md` Go-Live section.

## v1 Requirements — all ✅

### MT5GW — Self-hosted MT5 gateway
- [x] **MT5GW-01** ✅ — Prod gateway `mt5-gateway` on Railway (gmag11 v2.3, pinned digest, `/config` volume, VNC install + investor login), reachable worker→gateway over PRIVATE net only (0 domains/proxies on :8001). Bridge fixed via 3-pin PIP_CONSTRAINT (mt5linux 0.1.9 + rpyc 5.0.1 + numpy<2).
- [x] **MT5GW-02** ✅ (134) — Worker reads MT5 as a pure `mt5linux` network client (`MetaTrader5(host, port)`), timeout-bounded, never importing the Windows pkg in-process. Contract + offline suite.

### MT5SPIKE — Feasibility gate
- [x] **MT5SPIKE-01** ✅ — Live feasibility PROVEN end-to-end cold-boot: unattended Wine auto-login, `order_check` investor-vs-master read-only proof (trade_allowed=False), `history_deals_get` deal-reconstruction (415 deals), server-time offset established (UTC+3). Harness `scripts/mt5_spike.py` + go/no-go doc.

### MT5SRC — Source integration
- [x] **MT5SRC-01** ✅ — `'mt5'` first-class `Source` in lockstep (Literal + SUPPORTED_SOURCES + _FACTORIES), parity tests green.
- [x] **MT5SRC-02** ✅ — Worker `is_mt5` read-only validate branch: auth+read via login+`account_info()`, structural no-`order_*` + `order_check` master rejection, honest `KEY_AUTH_FAILED`, never `order_send`. 3 creds → `{api_key, api_secret, passphrase}`.
- [x] **MT5SRC-03** ✅ — All 3 key routes accept `mt5`; RED-guarded constraint migration `20260723172032` admits `'mt5'` (prod-verified), invalid still rejected.

### MT5RECON — Equity reconstruction → backbone → api_verified
- [x] **MT5RECON-01** ✅ — `combine_mt5_deal_ledger` folds realized profit/swap/commission/fee + `DEAL_TYPE_BALANCE` flows vs the equity anchor → daily returns → the ONE backbone with `api_verified`. Fail-loud (None≠()); no invented data; deposit-day never a spike.
- [x] **MT5RECON-02** ✅ — MT5 = TRADITIONAL √252 (not crypto √365); `isCryptoExchange` excludes `'mt5'`, mutation-guarded.
- [x] **MT5RECON-03** ✅ — Reconstructed equity reconciles to `account_info().equity` within tolerance; uPnL-wedge / missing-history flagged fail-loud, never fabricated. (Live soak recon_residual 1.4e-10.)

### MT5CONC — Concurrency + terminal hardening
- [x] **MT5CONC-01** ✅ — Derive seam under `to_thread`+`wait_for` bounds with terminal-restart-on-timeout (WEDGE-01 lesson).
- [x] **MT5CONC-02** ✅ — Per-terminal lock (host:port) + `account_info().login==expected` bracket pre+post read → `api_verified` never on the wrong account; persists nothing on mismatch.

### MT5UI — Add-key UI + verification surface
- [x] **MT5UI-01** ✅ — Flag-gated wizard (`NEXT_PUBLIC_MT5_ENABLED`, OFF=byte-identical) with 3 fields + investor-password setup guide. LIVE 2026-07-25.
- [x] **MT5UI-02** ✅ — `api_verified` badge across roles; all-roles e2e in blocking CI. Server gate `MT5_ENABLED` admits the read at routes + worker derive. LIVE 2026-07-25.

### MT5GOLIVE — Founder-gated go-live
- [x] **MT5GOLIVE-01** ✅ — Hosting = Railway co-locate (dual-stack verified); gateway credential-isolated, private-net only.
- [x] **MT5GOLIVE-02** ✅ — Real broker investor account onboarded + soaked to parity (day-1 green); `MT5_ENABLED` (Railway) + `NEXT_PUBLIC_MT5_ENABLED` (Vercel) flipped LIVE 2026-07-25 and verified in prod. MT5 usable at close.

## Deferred (future)
- lot→USD notional exposure widgets (P2); terminal pool sharded by broker (scale-out); `copy_rates` historical uPnL marking (only if realized-basis proves insufficient).
- Server-time DST edge (EET↔EEST) auto-switch — founder VNC/live-tick confirm; day-bucketing only.

## Out of scope
- Live trading / order execution (read-only investor login, server-enforced). MT4, broker Manager/Web APIs, MetaApi.cloud. Real-time streaming. Report/statement ingest (legacy self_reported EA/CSV stays as-is).
