---
phase: 44-foundation-primitives-verification-gates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useBreakpoint.ts
  - src/hooks/useBreakpoint.test.ts
  - src/components/ResponsiveTable.tsx
  - src/components/ResponsiveTable.test.tsx
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "useBreakpoint returns 'desktop' on the SSR snapshot (no window) and resolves to mobile/tablet/desktop on the client without a hydration mismatch"
    - "ResponsiveTable wraps arbitrary children in an overflow-x-auto container with an sr-only scroll hint"
    - "Both primitives' unit tests exercise every branch so the coverage ratchet (branches 72) holds un-lowered"
  artifacts:
    - path: "src/hooks/useBreakpoint.ts"
      provides: "SSR-safe breakpoint hook, thin wrapper over useMediaQuery, server snapshot 'desktop'"
      contains: "useBreakpoint"
    - path: "src/components/ResponsiveTable.tsx"
      provides: "overflow-x-auto container + sr-only scroll hint"
      contains: "overflow-x-auto"
    - path: "src/hooks/useBreakpoint.test.ts"
      provides: "SSR 'desktop' assertion + all 3 breakpoint branches"
    - path: "src/components/ResponsiveTable.test.tsx"
      provides: "overflow + sr-only hint, both hint-provided/default branches"
  key_links:
    - from: "src/hooks/useBreakpoint.ts"
      to: "src/hooks/useMediaQuery.ts"
      via: "import + wrap useSyncExternalStore-based hook"
      pattern: "useMediaQuery"
---

<objective>
Build the two pure presentational primitives that phases 45–48 consume: `useBreakpoint` (an SSR-safe breakpoint hook) and `ResponsiveTable` (an overflow-x-auto wrapper with an sr-only scroll hint). Both are small, pure, and fully unit-tested so they introduce viewport branches WITHOUT tripping the coverage ratchet.

Purpose: Build the responsive recipe ONCE so later phases wrap + apply classes instead of re-deriving it 40×. These two are file-disjoint from every other phase-44 plan and run fully in parallel.
Output: `src/hooks/useBreakpoint.ts` (+ test), `src/components/ResponsiveTable.tsx` (+ test). Delivers part of SC#3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundation-primitives-verification-gates/44-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- The existing SSR-safe hook useBreakpoint wraps. Use it directly; do NOT modify it. -->
From src/hooks/useMediaQuery.ts (EXISTS — do not modify):
```typescript
"use client";
export function useMediaQuery(query: string): boolean;
// getSnapshot:       window.matchMedia(query).matches   (browser)
// getServerSnapshot: () => false                        (SSR — both reads false)
// implemented with useSyncExternalStore (no setState-in-useEffect)
```

Tailwind v4 DEFAULT breakpoints (no `--breakpoint-*` tokens exist in globals.css `@theme` — verified):
sm 640px, md 768px, lg 1024px, xl 1280px, 2xl 1536px.

Existing sr-only convention (TimeSeriesChart.tsx:493): `<span className="sr-only">…</span>`.
DESIGN.md: tables already have ~44px row height (touch-compliant); ResponsiveTable adds ONLY the scroll affordance, it does NOT restyle the table (column reshape is phase 46 / TABLE-01).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement useBreakpoint (SSR snapshot 'desktop', inverse-query shape)</name>
  <read_first>
    - src/hooks/useMediaQuery.ts (the wrapped hook — getServerSnapshot returns false, useSyncExternalStore)
    - 44-RESEARCH.md "Pattern 1: SSR-safe useBreakpoint" (lines 160-196) — the inverse-query recipe + the critical SSR-detail
    - DESIGN.md decision-log 2026-04-29 (strategy.ui_v2 two-pass SSR-false convention this mirrors)
  </read_first>
  <files>src/hooks/useBreakpoint.ts, src/hooks/useBreakpoint.test.ts</files>
  <behavior>
    - On the server (no `window` / getServerSnapshot=false on every inner useMediaQuery read), useBreakpoint() returns 'desktop' (SC#3 server snapshot). This is why the inverse-query (max-width) shape is required: all-false → falls through to 'desktop', NOT 'mobile'.
    - Client at <640px CSS width → 'mobile'; ≥640px and <1024px → 'tablet'; ≥1024px → 'desktop'.
    - Exactly three breakpoint values exported as a `Breakpoint` type union: 'mobile' | 'tablet' | 'desktop'.
    - No hydration mismatch: desktop-first server render matches the all-false initial client snapshot.
  </behavior>
  <action>
    Create `src/hooks/useBreakpoint.ts` with `"use client"`. Export `type Breakpoint = "mobile" | "tablet" | "desktop"` and `function useBreakpoint(): Breakpoint`. Use the INVERSE-QUERY shape from RESEARCH Pattern 1 option 1 so the all-false SSR case maps to 'desktop' for free: read `useMediaQuery("(max-width: 1023px)")` (true = tablet-or-below) and `useMediaQuery("(max-width: 639px)")` (true = mobile). Map: if mobile-query true → 'mobile'; else if below-lg true → 'tablet'; else → 'desktop'. Do NOT modify useMediaQuery. Do NOT invent custom breakpoints — use the Tailwind v4 default 640/1024 thresholds so the JS values match the CSS utilities phases 45-48 apply. Co-locate `useBreakpoint.test.ts` per the repo convention (vitest include `src/**/*.test.ts`). The test MUST cover: (a) the SSR/no-window path yields 'desktop' (mock `window.matchMedia` undefined / server snapshot path); (b) each of the three branches (mobile/tablet/desktop) by mocking matchMedia matches per query — so all branches are covered and the ratchet holds.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useBreakpoint.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/hooks/useBreakpoint.test.ts` exits 0 with ≥4 assertions (SSR-desktop + mobile + tablet + desktop).
    - `src/hooks/useBreakpoint.ts` contains `"use client"`, `export type Breakpoint`, `export function useBreakpoint`, and imports from `./useMediaQuery`.
    - `src/hooks/useMediaQuery.ts` is byte-unchanged (`git diff --quiet src/hooks/useMediaQuery.ts`).
    - The SSR-path test asserts the returned value is exactly `'desktop'` (string equality), not `'mobile'`.
  </acceptance_criteria>
  <done>useBreakpoint exists, server snapshot is 'desktop', all 3 branches + the SSR path are unit-covered, useMediaQuery untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement ResponsiveTable (overflow-x-auto + sr-only scroll hint)</name>
  <read_first>
    - 44-RESEARCH.md "Pattern 3: ResponsiveTable" (lines 233-248) — the exact shape + DESIGN.md grounding
    - src/app/factsheet/[id]/v2/TimeSeriesChart.tsx:493 (existing sr-only span convention)
    - DESIGN.md §Component Patterns + §Spacing (tables: no outer border, ~44px row height already touch-compliant)
  </read_first>
  <files>src/components/ResponsiveTable.tsx, src/components/ResponsiveTable.test.tsx</files>
  <behavior>
    - Renders an `overflow-x-auto` container wrapping `children` with a focusable `role="region"` and an aria-label.
    - Emits an `sr-only` scroll hint string so screen-reader users know the table scrolls horizontally.
    - When a `hint` prop is provided, uses it for both the aria-label and the sr-only text; when omitted, uses a sensible default ("Table scrolls horizontally…"). Both branches must be tested.
    - Does NOT restyle the wrapped table (no border/row-height changes — column reshape is phase 46).
  </behavior>
  <action>
    Create `src/components/ResponsiveTable.tsx` exporting `ResponsiveTable({ children, hint }: { children: React.ReactNode; hint?: string })`. Render a `<div className="overflow-x-auto" role="region" aria-label={hint ?? <default>} tabIndex={0}>` containing a `<span className="sr-only">{hint ?? <default>}</span>` then `{children}`. Default hint wording is Claude's discretion against DESIGN.md — use a static informative string (e.g. "Table scrolls horizontally. Swipe or use arrow keys to see more columns."); it is an `sr-only` static hint, NOT a `role="status"`/aria-live region. Do not add table styling. Co-locate `ResponsiveTable.test.tsx`; test BOTH the default-hint and provided-hint branches and assert the rendered container carries `overflow-x-auto`, the sr-only span is present, and children render.
  </action>
  <verify>
    <automated>npx vitest run src/components/ResponsiveTable.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ResponsiveTable.test.tsx` exits 0.
    - `src/components/ResponsiveTable.tsx` contains the literal class string `overflow-x-auto`, `className="sr-only"`, `role="region"`, and exports `ResponsiveTable`.
    - The test renders with NO `hint` prop AND with a custom `hint` prop and asserts the sr-only text differs between the two (proves both `hint ?? default` branches execute).
    - The test asserts `children` passed in are rendered inside the overflow container.
  </acceptance_criteria>
  <done>ResponsiveTable exists with overflow-x-auto + sr-only hint, both hint branches covered, no table restyle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan delivers two pure, presentational client primitives. There is NO new auth, data access, network endpoint, user-input trust boundary, secret handling, or SQL. No new attack surface is introduced — additive presentation over the frozen engine.

| Boundary | Description |
|----------|-------------|
| (none) | No untrusted input crosses any boundary in this plan; useBreakpoint reads `window.matchMedia` (browser API, no user data), ResponsiveTable renders caller-supplied children with no parsing/escaping concern beyond React's default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-01 | Information Disclosure | useBreakpoint / ResponsiveTable | accept | Pure presentation, no PII, no secrets, no network; nothing to disclose. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this plan (RESEARCH Package Legitimacy Audit: no installs). `npm install` adds nothing. No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/hooks/useBreakpoint.test.ts src/components/ResponsiveTable.test.tsx` — both green.
- `git diff --quiet src/hooks/useMediaQuery.ts` — existing hook untouched.
- Branch coverage on the two new files is 100% (small, pure) so `npm run test:coverage` ratchet (branches 72) is not lowered.
</verification>

<success_criteria>
- useBreakpoint returns 'desktop' on the SSR snapshot and resolves all 3 client branches without hydration mismatch.
- ResponsiveTable wraps children in overflow-x-auto with an sr-only hint; both hint branches tested.
- Coverage ratchet holds un-lowered.
</success_criteria>

<output>
Create `.planning/phases/44-foundation-primitives-verification-gates/44-01-SUMMARY.md` when done
</output>
