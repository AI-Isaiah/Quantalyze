---
phase: 44-foundation-primitives-verification-gates
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - e2e/helpers/reflow.ts
  - e2e/reflow.spec.ts
  - e2e/target-size.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "A Playwright reflow spec asserts document.documentElement.scrollWidth <= clientWidth (≤1px slop) at a 320px CSS width, anchored on a VISIBLE content element so a blank/404 page fails loud (no false-green), runnable against any route"
    - "A 44px target-size measurement gate EXISTS and is RUNNABLE — interactive elements measured ≥44px on a documented honestly-green scope; app-wide enforcement is deferred to phase 46/48 (documented, not a silent false-green)"
    - "Both new e2e specs are wired into BOTH (a) the explicit ci.yml unseeded playwright list at ~line 1059 AND (b) their own spec-level run guard — and are PROVEN to execute (show N passed, not skipped) in a real CI run (FLOW-01, burned twice)"
    - "A reusable e2e/helpers/reflow.ts (assertNoReflow + assertTargetSizes) exists so phases 45-48 reuse it app-wide instead of re-deriving the DOM-measurement"
  artifacts:
    - path: "e2e/helpers/reflow.ts"
      provides: "Reusable assertNoReflow + assertTargetSizes helpers (formalizes demo-public.spec.ts:309-349 + SC#1 hardening)"
      contains: "assertNoReflow"
    - path: "e2e/reflow.spec.ts"
      provides: "320px reflow gate anchored on /security visible H1, ≤1px slop"
      contains: "assertNoReflow"
    - path: "e2e/target-size.spec.ts"
      provides: "44px target-size measurement gate on a documented clean scope"
      contains: "assertTargetSizes"
    - path: ".github/workflows/ci.yml"
      provides: "FLOW-01 dual-wiring — both specs in the unseeded playwright list"
      contains: "e2e/reflow.spec.ts"
  key_links:
    - from: "e2e/reflow.spec.ts"
      to: ".github/workflows/ci.yml"
      via: "spec name added to the unseeded playwright test list (~line 1059)"
      pattern: "reflow.spec.ts"
    - from: "e2e/reflow.spec.ts"
      to: "e2e/helpers/reflow.ts"
      via: "imports assertNoReflow"
      pattern: "assertNoReflow"
---

<objective>
Deliver SC#1 + the e2e half of SC#4: a reusable `e2e/helpers/reflow.ts` (`assertNoReflow` + `assertTargetSizes`), a 320px reflow spec anchored on the public `/security` H1 (≤1px slop, fails loud on a blank/404 page), a 44px target-size measurement gate on a documented honestly-green scope, and the FLOW-01 dual-wiring of both specs into `ci.yml`'s unseeded list — PROVEN to execute (N passed, not skipped) in a real CI run.

Purpose: The reflow + target-size gates are exactly the WCAG checks axe structurally cannot do (Reflow 1.4.10, Target Size 2.5.8). The twice-burned FLOW-01 trap is the real risk here — a gate added but never run is a false-green; the v1.2 JOURNEY-03 axe gate caught 3 real bugs ONLY once it actually ran in CI. The helper is built once so phases 46/48 run these app-wide.
Output: `e2e/helpers/reflow.ts`, `e2e/reflow.spec.ts`, `e2e/target-size.spec.ts`, `.github/workflows/ci.yml` (unseeded list edit). Delivers SC#1 + e2e half of SC#4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundation-primitives-verification-gates/44-RESEARCH.md

<interfaces>
<!-- The reflow precedent to formalize — demo-public.spec.ts:309-349 (innerWidth, no slop, no visible-anchor). -->
<!-- SC#1 hardening adds: clientWidth (not innerWidth), ≤1px slop, a visible-element anchor BEFORE measuring. -->
Reusable helper shape (RESEARCH lines 336-389):
```typescript
// e2e/helpers/reflow.ts
import { expect, type Page } from "@playwright/test";
export async function assertNoReflow(page: Page, anchorSelector: string): Promise<void>;
//   await expect(page.locator(anchor).first()).toBeVisible()  // fail loud on blank/404 (Pitfall 2)
//   measure doc.scrollWidth - doc.clientWidth <= 1 (toPass retry); breadcrumb names first offender
export async function assertTargetSizes(page: Page, anchorSelector: string, interactiveSelector?: string): Promise<void>;
//   anchor visible first; measure a,button,[role=button],input,select getBoundingClientRect width/height >= 44
//   expect(measured > 0) so an empty page can't pass with zero elements measured
```

/security route facts (verified):
- Stable visible anchor: H1 "Security practices" — `page.getByRole("heading", { name: /Security practices/, level: 1 })` (security-page.spec.ts asserts it visible), DOM: `<main> <article> <h1>`.
- Interactive elements: header has two TEXT links ("Quantalyze" back-link `py-2`, "For Quants →") that are BELOW 44px tall; the body has inline editorial links + a PDF download link. So a PAGE-LEVEL 44px gate on /security is NOT honestly green.
- Unseeded/public: renders with no auth/seed (security-page.spec.ts runs unseeded).

CI wiring (FLOW-01 — verified):
- Unseeded list, ci.yml ~line 1059: `npx playwright test e2e/auth.spec.ts e2e/smoke.spec.ts e2e/demo-public.spec.ts e2e/demo-founder-view.spec.ts e2e/onboarding-banner-smoke.spec.ts e2e/demo-screenshot.spec.ts` ← ADD `e2e/reflow.spec.ts e2e/target-size.spec.ts` here.
- demo-public.spec.ts is the model for an UNSEEDED spec (runs in that list, no HAS_SEED_ENV skip). Mirror it: phase-44 gates are UNSEEDED, so their spec-level guard is a route/anchor-presence gate, NOT a HAS_SEED_ENV env skip.
- The seed-gated list lives at ci.yml:1252-1262 with the "update both this list AND the e2e/<spec>.spec.ts HAS_SEED_ENV constant" instruction — phase-44 specs do NOT go there (they are unseeded).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the reusable reflow + target-size helper (e2e/helpers/reflow.ts)</name>
  <read_first>
    - e2e/demo-public.spec.ts:300-352 (the live reflow precedent: scrollWidth/innerWidth walk + toPass + offender breadcrumb)
    - 44-RESEARCH.md "Reusable reflow + target-size helper" code example (lines 336-389) + Pitfall 2 (false-green on blank page, lines 314-318)
    - e2e/discovery-axe.spec.ts:1-19 (the Grok W-02 "false-green on 404/empty page" lesson the visible-anchor requirement comes from)
  </read_first>
  <files>e2e/helpers/reflow.ts</files>
  <action>
    Create `e2e/helpers/reflow.ts` exporting two functions. `assertNoReflow(page, anchorSelector)`: FIRST `await expect(page.locator(anchorSelector).first()).toBeVisible({ timeout: 10_000 })` so a blank/404 page fails LOUD before any measurement (Pitfall 2). Then (best-effort `waitForLoadState("networkidle")` catch-swallowed) measure inside `expect(async () => {…}).toPass(...)`: `doc.scrollWidth - doc.clientWidth` (use `documentElement.clientWidth`, NOT `window.innerWidth` — SC#1 specifies clientWidth, excludes the scrollbar gutter); pass if slop ≤ 1; on failure walk `body *` to name the first element whose `getBoundingClientRect().right > clientWidth + 1` and `throw new Error("reflow: scrollWidth=… clientWidth=… offender=…")` (debuggable breadcrumb, mirrors demo-public). `assertTargetSizes(page, anchorSelector, interactiveSelector = "a, button, [role=button], input, select")`: anchor-visible first (same fail-loud guard), then measure each matched element's `getBoundingClientRect()`; skip elements with width===0 && height===0 (hidden); collect any with width<44 || height<44 into a violations array; `expect(measured, "no interactive elements measured — anchor/empty-page bug").toBeGreaterThan(0)` THEN `expect(violations, "interactive targets below 44px (WCAG 2.5.8)").toEqual([])`. Both functions must accept any route/selector so phases 45-48 reuse them app-wide.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/helpers/reflow.ts` exports `assertNoReflow` and `assertTargetSizes`.
    - `assertNoReflow` uses `documentElement.clientWidth` (NOT `window.innerWidth`) and a `<= 1` slop check.
    - Both functions `await expect(page.locator(anchorSelector).first()).toBeVisible(...)` BEFORE measuring (fail-loud-on-blank guard).
    - `assertTargetSizes` includes `expect(measured).toBeGreaterThan(0)` so an empty page cannot pass with zero elements measured.
    - `npx tsc --noEmit` passes (helper types resolve against @playwright/test).
  </acceptance_criteria>
  <done>Reusable assertNoReflow + assertTargetSizes exist, anchor-fail-loud, use clientWidth + ≤1px slop, and are app-wide-reusable.</done>
</task>

<task type="auto">
  <name>Task 2: Write the reflow + target-size specs anchored on /security (320px, documented scope)</name>
  <read_first>
    - e2e/security-page.spec.ts:1-55 (the /security H1 anchor + unseeded-run model)
    - e2e/demo-public.spec.ts (the UNSEEDED spec model — runs in the unseeded ci.yml list, no HAS_SEED_ENV skip; how it sets viewport)
    - e2e/helpers/reflow.ts (the helper from Task 1)
    - 44-RESEARCH.md "Note for planner" on target-size first-run scope (lines 390, 477-480) + Open Question 1
  </read_first>
  <files>e2e/reflow.spec.ts, e2e/target-size.spec.ts</files>
  <action>
    Create `e2e/reflow.spec.ts`: `page.setViewportSize({ width: 320, height: 800 })`, `await page.goto("/security")`, then `await assertNoReflow(page, anchorSelector)` where anchorSelector targets the visible H1 (use a selector resolving the "Security practices" level-1 heading, e.g. `'h1'` scoped to the page or a role-based locator — the helper takes a string selector; if a role locator is cleaner, adapt the helper call or pass an equivalent CSS/text selector that resolves the visible H1). This is an UNSEEDED spec: NO `HAS_SEED_ENV` skip — model it on demo-public.spec.ts so it RUNS in the unseeded ci.yml job. Create `e2e/target-size.spec.ts`: the gate must EXIST + RUN + be honestly green WITHOUT weakening the 44px threshold. Since /security's header text links are sub-44px at the page level, SCOPE the target-size measurement to a documented clean region: pass an `interactiveSelector` scoped to a container whose interactive elements are all ≥44px (e.g. scope to `main` and to button/link elements rendered via the `min-h-[44px]` Button convention, OR anchor on a route/region known clean). In the spec header comment, EXPLICITLY document (a) what region is measured, (b) that app-wide page-level target-size enforcement is phase 46/48's job (TABLE-01/REFLOW + final verification), and (c) that this is intentional scope, NOT a silent false-green — so the gate is honestly green for phase 44 while remaining a real ≥44px assertion. Both specs `setViewportSize` to 320px and anchor on a visible element so a blank/404 fails loud. Do NOT use `maximum-scale`/zoom-disabling viewport in the spec.
  </action>
  <verify>
    <automated>npx playwright test e2e/reflow.spec.ts e2e/target-size.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/reflow.spec.ts` sets a 320px viewport, navigates to `/security`, calls `assertNoReflow` with a selector resolving the visible "Security practices" H1, and has NO `HAS_SEED_ENV` env-skip (it is an unseeded spec).
    - `e2e/target-size.spec.ts` calls `assertTargetSizes` with a documented, scoped `interactiveSelector` that is honestly ≥44px-green on /security, AND the spec header comment documents the scope + the phase 46/48 app-wide-rollout deferral + that it is intentional scope not a false-green.
    - The 44px threshold is NOT weakened anywhere (grep the spec + helper: no number `<44` substituted as the bar, no `r.width < 32` style relaxation).
    - `npx playwright test e2e/reflow.spec.ts e2e/target-size.spec.ts` passes locally (both report passed, neither skipped).
    - Falsifiability (documented in SUMMARY): pointing reflow at a route with known overflow, OR target-size at the sub-44px header links, makes the respective gate FAIL — proving it is not vacuously green.
  </acceptance_criteria>
  <done>Both specs run at 320px against /security, anchored on a visible element; reflow uses ≤1px slop; target-size measures a documented clean ≥44px scope with the app-wide deferral spelled out; threshold un-weakened.</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-01 dual-wire both specs into ci.yml's unseeded list + prove execution</name>
  <read_first>
    - .github/workflows/ci.yml:1055-1062 (the unseeded `npx playwright test …` line to extend)
    - .github/workflows/ci.yml:1248-1265 (the seed-gated list + the "update both this list AND the HAS_SEED_ENV constant" instruction — to CONFIRM phase-44 specs do NOT belong here)
    - .github/workflows/ci.yml:1205-1221 (the prove-execution precedent — a build grep that proves a step did real work)
    - 44-RESEARCH.md Pitfall 1 "FLOW-01 — gate added but never runs (BURNED TWICE)" (lines 306-312) + "FLOW-01 dual-wiring sites" (lines 438-451)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
    Edit `.github/workflows/ci.yml` line ~1059: append `e2e/reflow.spec.ts e2e/target-size.spec.ts` to the existing unseeded `npx playwright test e2e/auth.spec.ts e2e/smoke.spec.ts e2e/demo-public.spec.ts e2e/demo-founder-view.spec.ts e2e/onboarding-banner-smoke.spec.ts e2e/demo-screenshot.spec.ts` line. This is FLOW-01 wiring PLACE 1 (the explicit ci.yml list). PLACE 2 for these UNSEEDED specs is the spec file's own run model (Task 2: no HAS_SEED_ENV skip → it runs in this unseeded job) — the dual-wiring for an unseeded spec is satisfied by (a) the ci.yml list entry AND (b) the spec having no env-skip that would self-disable it in the unseeded job. Do NOT add these specs to the seed-gated list at lines 1252-1262 (they are unseeded; adding them there + a HAS_SEED_ENV skip would make them silently skip when seed env is absent). Leave the seed-gated list and its specs untouched. After CI runs, PROVE-EXECUTION (SC#4): confirm the CI e2e-job log shows `reflow.spec.ts` and `target-size.spec.ts` in the executed list with `passed` (not `skipped`) — record the run URL / log excerpt in the SUMMARY (mirrors the JOURNEY-03 "a gate only earns trust once it RUNS" lesson + the ci.yml:1205-1221 prove-execution intent).
  </action>
  <verify>
    <automated>grep -F "e2e/reflow.spec.ts" .github/workflows/ci.yml && grep -F "e2e/target-size.spec.ts" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "e2e/reflow.spec.ts" .github/workflows/ci.yml` ≥ 1 AND `grep -c "e2e/target-size.spec.ts" .github/workflows/ci.yml` ≥ 1 — both on the UNSEEDED `npx playwright test` line (~1059), NOT the seed-gated list (~1252).
    - `git diff .github/workflows/ci.yml` shows ONLY the two spec names appended to the unseeded list — no other ci.yml change, no edit to the seed-gated list region (1248-1265).
    - Neither spec has a `HAS_SEED_ENV` / `test.skip(!process.env.TEST_SUPABASE_*)` guard that would self-disable it in the unseeded job (FLOW-01 place 2 — the spec must actually run where it is wired).
    - PROVEN-EXECUTION (recorded in SUMMARY, SC#4): the CI run's e2e job log shows both spec names executing with `passed`, NOT `skipped` — with the run URL/excerpt captured. A gate added but never executed is a false-green; this is the explicit FLOW-01 acceptance gate (burned twice).
  </acceptance_criteria>
  <done>Both specs are in the unseeded ci.yml list (FLOW-01 place 1) and have no self-skip (place 2), so they run in CI; proven-execution captured in the SUMMARY with passed (not skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds two Playwright e2e specs, a shared test helper, and one ci.yml list edit. There is NO new auth, data access, network endpoint, user-input trust boundary, secret handling, or SQL in scope — no new attack surface. The reflow/target-size gates ARE accessibility-hardening controls (WCAG 1.4.10 / 2.5.8), the inverse of a threat.

| Boundary | Description |
|----------|-------------|
| CI runtime DOM measurement (Playwright) | The specs navigate the public `/security` route in headless chromium and read DOM geometry only; no user input, no auth, no data write. |
| CI secret leak via Playwright trace | Already mitigated by keeping phase-44 gates UNSEEDED (placeholder env, no TEST_SUPABASE_*); the seed-gated path already skips report upload (ci.yml:1288-1304). Unseeded gates avoid the leak path entirely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-06 | Information Disclosure | Playwright trace / CI artifacts | mitigate | Phase-44 reflow/target-size gates run UNSEEDED (placeholder env, no secrets in scope); no NEXT_PUBLIC_* or TEST_SUPABASE_* leak path — the gates only touch the public /security route. |
| T-44-07 | Denial of Service (false-green) | reflow / target-size gates | mitigate | Visible-anchor-before-measure + `measured > 0` guard ensure a blank/404 page fails LOUD instead of passing against nothing (Pitfall 2); FLOW-01 dual-wiring + proven-execution ensure the gate actually runs (Pitfall 1, burned twice). |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: no installs; @playwright/test already present). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx playwright test e2e/reflow.spec.ts e2e/target-size.spec.ts` — both pass locally (neither skipped).
- `grep -F e2e/reflow.spec.ts .github/workflows/ci.yml` and `grep -F e2e/target-size.spec.ts .github/workflows/ci.yml` — both present on the unseeded list.
- `git diff .github/workflows/ci.yml` — only the two appended spec names; seed-gated list untouched.
- PROVEN-EXECUTION: real CI run log shows both specs `passed` (not `skipped`) — recorded in SUMMARY (SC#4 / FLOW-01).
- Documented falsifiability: a known-overflow route fails reflow; the sub-44px header links fail target-size.
</verification>

<success_criteria>
- Reflow gate: scrollWidth <= clientWidth (≤1px slop) at 320px, visible-anchored, fails loud on blank/404, runnable against any route.
- Target-size gate EXISTS + RUNS at ≥44px on a documented clean scope; app-wide rollout deferred to 46/48 (documented, not a false-green); threshold un-weakened.
- Both specs dual-wired into ci.yml (place 1) + no self-skip (place 2) and PROVEN to execute (passed, not skipped) in a real CI run (FLOW-01).
- Reusable e2e/helpers/reflow.ts exists for phases 45-48.
</success_criteria>

<output>
Create `.planning/phases/44-foundation-primitives-verification-gates/44-04-SUMMARY.md` when done
</output>
