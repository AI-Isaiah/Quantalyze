---
phase: 44-foundation-primitives-verification-gates
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/visual/viewport-zoom-meta.test.ts
  - src/app/layout.tsx
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "A Vitest source-scan guard fails on any maximum-scale / user-scalable=no / maximumScale: / userScalable: false anywhere in src/ (viewport export OR raw <meta name='viewport'>)"
    - "Root src/app/layout.tsx declares an explicit zoom-permissive `export const viewport: Viewport` (width device-width, initialScale 1; NO maximumScale, NO userScalable:false)"
    - "The guard runs unconditionally in the existing frontend-test + frontend-coverage jobs (gated by the `frontend` aggregator) with ZERO ci.yml edits and ZERO seed gate — strictly less FLOW-01 surface than a tsx script"
  artifacts:
    - path: "tests/visual/viewport-zoom-meta.test.ts"
      provides: "Whole-src source-grep guard cloning chart-accessibility-layer.test.ts"
      contains: "maximumScale"
    - path: "src/app/layout.tsx"
      provides: "Zoom-permissive root viewport export"
      contains: "export const viewport"
  key_links:
    - from: "tests/visual/viewport-zoom-meta.test.ts"
      to: "src/app/layout.tsx"
      via: "the guard scans src/ (incl. layout.tsx) and the viewport export must pass it"
      pattern: "viewport"
---

<objective>
Deliver SC#2: a zoom-meta source-scan CI guard (Vitest, cloned from `chart-accessibility-layer.test.ts`) that fails the build on any zoom-disabling viewport directive anywhere in `src/`, AND an explicit zoom-permissive `viewport` export in the root `src/app/layout.tsx` (which has none today). The guard is green from the start (no `maximum-scale`/`user-scalable=no` exists anywhere — verified) and the viewport export keeps it green forever.

Purpose: WCAG 1.4.4 Resize Text is invisible to axe — this is exactly the structural gap the bespoke gates close. Implementing the guard as a Vitest test (not a tsx script + ci.yml step) means it runs unconditionally in the existing `frontend-test` + `frontend-coverage` jobs with ZERO ci.yml edits and ZERO seed gate — the lowest possible FLOW-01 risk surface.
Output: `tests/visual/viewport-zoom-meta.test.ts`, `src/app/layout.tsx` (+ viewport export). Delivers SC#2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundation-primitives-verification-gates/44-RESEARCH.md

<interfaces>
<!-- The clone target: a whole-src walk()+regex+violations[] Vitest guard. -->
From tests/visual/chart-accessibility-layer.test.ts (the pattern to clone):
```typescript
import { describe, it, expect } from "vitest";
import { readFileSync, readdirSync, statSync } from "node:fs";
import { join } from "node:path";
const REPO_ROOT = join(__dirname, "..", "..");
const SRC_DIR = join(REPO_ROOT, "src");
function walk(dir: string): string[] { /* recursive, skips node_modules/.next, .tsx?/.html files */ }
// describe(...) { it(...) { for (file of walk(SRC_DIR)) { regex-test; push violations } expect(violations).toEqual([]) } }
```

Verified Next 16 Viewport API (node_modules/next/dist/docs/.../generate-viewport.md):
```tsx
import type { Viewport } from "next";
export const viewport: Viewport = { width: "device-width", initialScale: 1 };
// emits <meta name="viewport" content="width=device-width, initial-scale=1">
// OMIT maximumScale and userScalable:false (those disable pinch-zoom = WCAG 1.4.4 fail = what the guard fails on)
```
layout.tsx is already a server component (`async function RootLayout`); `viewport` and `metadata` can both be exported from the same segment; the existing `export const dynamic = "force-dynamic"` coexists fine.

Verified: NO `maximum-scale` / `user-scalable` / `maximumScale` / `userScalable` exists anywhere in src/ today (guard green from the first run).
vitest include globs (vitest.config.ts:26-29) already cover `tests/visual/**/*.test.ts` → the guard runs in frontend-test (sharded) + frontend-coverage (full), both gated by the `frontend` aggregator. NO ci.yml edit needed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the zoom-meta Vitest source-scan guard (clone of chart-accessibility-layer.test.ts)</name>
  <read_first>
    - tests/visual/chart-accessibility-layer.test.ts (the exact walk()+regex+violations[] pattern to clone, incl. node_modules/.next skip)
    - 44-RESEARCH.md "zoom-meta source-scan guard (clone of chart-accessibility-layer.test.ts)" code example (lines 392-436)
    - vitest.config.ts:24-44 (the include globs that pick up tests/visual/**)
  </read_first>
  <files>tests/visual/viewport-zoom-meta.test.ts</files>
  <action>
    Create `tests/visual/viewport-zoom-meta.test.ts` cloning the `walk()` + per-file regex + `violations[]` structure of `chart-accessibility-layer.test.ts`. `REPO_ROOT = join(__dirname, "..", "..")`, `SRC_DIR = join(REPO_ROOT, "src")`. `walk()` recurses src, skips `node_modules`/`.next`, collects `.tsx?`/`.html` files (mirror the clone target's file filter). FORBIDDEN regex array (4 patterns): `/maximumScale\s*:/` (Next Viewport field), `/userScalable\s*:\s*false/` (Next Viewport field), `/maximum-scale\s*=/` (raw meta content), `/user-scalable\s*=\s*no/` (raw meta content). One `it(...)` collects every match across `walk(SRC_DIR)` into a `violations` array with a `path: regex` breadcrumb and `expect(violations, "viewport must never disable pinch-zoom (WCAG 1.4.4)").toEqual([])`. Use a grep-hygiene-safe approach: the guard reads file CONTENT (not header comments of test files); the FORBIDDEN tokens MUST NOT appear unescaped in the guard's own comment prose in a way that would self-match if the guard ever scanned its own dir (it scans only src/, so the test file in tests/visual/ is out of scope — but keep the forbidden literal tokens inside regex/string literals, never bare in prose). The guard MUST only `readFileSync` + regex-test; never `eval`/`import` scanned files (mirrors the clone target).
  </action>
  <verify>
    <automated>npx vitest run tests/visual/viewport-zoom-meta.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/visual/viewport-zoom-meta.test.ts` exits 0 (green from the start — no violations exist in src/ today).
    - The file contains all four detection patterns: `maximumScale`, `userScalable`, `maximum-scale`, `user-scalable` (inside regex/string literals).
    - Negative control (manual, documented in SUMMARY): temporarily adding `export const viewport = { maximumScale: 1 }` to any src file makes the guard FAIL — proving it is falsifiable, not vacuously green. Revert before commit.
    - The guard uses only `readFileSync`/`readdirSync`/`statSync` — no `eval`, no dynamic `import()` of scanned files.
  </acceptance_criteria>
  <done>The Vitest zoom-meta guard exists, is green against current src/, and is proven falsifiable via the documented negative control.</done>
</task>

<task type="auto">
  <name>Task 2: Add the zoom-permissive root viewport export to layout.tsx</name>
  <read_first>
    - src/app/layout.tsx (the full file — has metadata + dynamic="force-dynamic", NO viewport export today; server component)
    - 44-RESEARCH.md "Pattern 4: zoom-permissive root viewport export (Next 16 App Router)" (lines 250-267)
    - node_modules/next/dist/lib/metadata/types/extra-types.d.ts:45-54 (ViewportLayout fields — confirm width/initialScale exist, maximumScale/userScalable optional)
  </read_first>
  <files>src/app/layout.tsx</files>
  <action>
    In `src/app/layout.tsx` add `import type { Viewport } from "next";` (alongside the existing `import type { Metadata } from "next";`) and `export const viewport: Viewport = { width: "device-width", initialScale: 1 };`. Place it near the existing `export const metadata` / `export const dynamic` exports. OMIT `maximumScale` and `userScalable` entirely (setting them is the WCAG 1.4.4 failure the guard fails on, and would itself trip the new guard). Do NOT add a hand-written `<meta name="viewport">` in `<head>` (App Router emits the meta from the typed export; a hand-written one would trip the guard's `<meta name="viewport">` clause and fragment the source of truth). Leave everything else in layout.tsx unchanged (the correlation-id meta, fonts, force-dynamic, body classes).
  </action>
  <verify>
    <automated>npx vitest run tests/visual/viewport-zoom-meta.test.ts && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains `import type { Viewport }` from `"next"` and `export const viewport: Viewport = { width: "device-width", initialScale: 1 }`.
    - `src/app/layout.tsx` does NOT contain `maximumScale`, `userScalable`, or a hand-written `<meta name="viewport"`.
    - `npx vitest run tests/visual/viewport-zoom-meta.test.ts` exits 0 with the new export present (the viewport export passes its own guard).
    - `npx tsc --noEmit` passes (the `Viewport` type resolves and the object satisfies it).
    - The existing `export const dynamic = "force-dynamic"`, `export const metadata`, and the correlation-id `<meta>` are unchanged (`git diff` shows only the two added viewport lines).
  </acceptance_criteria>
  <done>Root layout declares an explicit zoom-permissive viewport export with no maximumScale/userScalable; the guard passes it and tsc is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds a static source-scan CI test and a typed config export. There is NO new auth, data access, network endpoint, user-input trust boundary, secret handling, or SQL — no new attack surface. The `viewport` export + zoom-meta guard are themselves a config-hardening control (WCAG 1.4.4 enforcement, ASVS V14 Config), not a vulnerability.

| Boundary | Description |
|----------|-------------|
| Source-grep guard reading repo content | The Vitest guard `readFileSync`s + regex-tests repo files; it never `eval`s or `import()`s scanned content (mirrors chart-accessibility-layer.test.ts). No code execution of scanned files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-04 | Tampering | zoom-meta source guard | mitigate | Guard only reads + regex-tests files, never executes scanned content; this IS the hardening control for WCAG 1.4.4 (config integrity), the inverse of a threat. |
| T-44-05 | Denial of Service (zoom disabled) | root viewport export | mitigate | Explicit zoom-permissive viewport (no maximumScale/userScalable:false) prevents a future commit from disabling pinch-zoom; the guard enforces it CI-wide. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: no installs). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run tests/visual/viewport-zoom-meta.test.ts` — green against current src/ AND with the new viewport export present.
- Documented negative control proves the guard is falsifiable (temporarily inject maximumScale → guard fails → revert).
- `npx tsc --noEmit` — the Viewport type resolves.
- The guard runs in frontend-test + frontend-coverage with NO ci.yml edit (confirmed via vitest include glob `tests/visual/**`).
</verification>

<success_criteria>
- Zoom-meta guard fails on any maximum-scale/user-scalable=no/maximumScale:/userScalable:false in src/; green today.
- Root layout declares an explicit zoom-permissive viewport export.
- Guard runs unconditionally in the existing vitest jobs with zero ci.yml edits / zero seed gate.
</success_criteria>

<output>
Create `.planning/phases/44-foundation-primitives-verification-gates/44-03-SUMMARY.md` when done
</output>
