---
phase: 44-foundation-primitives-verification-gates
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ResponsiveChartFrame.tsx
  - src/components/ResponsiveChartFrame.test.tsx
  - src/app/factsheet/[id]/v2/TimeSeriesChart.tsx
autonomous: true
requirements: [A11Y-02]

must_haves:
  truths:
    - "ResponsiveChartFrame emits the exact responsive SVG recipe (viewBox, preserveAspectRatio='xMidYMid meet', className containing 'block w-full', style.aspectRatio) extracted from TimeSeriesChart"
    - "TimeSeriesChart adopts the frame and its rendered SVG output stays byte-identical (parity-by-construction), proven by a structural unit test — NOT by the dead e2e/strategy-v2-chart-parity.spec.ts"
    - "All of TimeSeriesChart's existing SVG props (ref, 7 pointer/key handlers, role, aria-label, aria-describedby, tabIndex, focusable, full className, style) pass through the frame unchanged"
  artifacts:
    - path: "src/components/ResponsiveChartFrame.tsx"
      provides: "Reusable viewBox + preserveAspectRatio + w-full SVG frame extracted from TimeSeriesChart"
      contains: "preserveAspectRatio"
    - path: "src/components/ResponsiveChartFrame.test.tsx"
      provides: "Structural byte-identity assertion of the exact attribute strings"
    - path: "src/app/factsheet/[id]/v2/TimeSeriesChart.tsx"
      provides: "TimeSeriesChart rendering through ResponsiveChartFrame (parity-by-construction)"
      contains: "ResponsiveChartFrame"
  key_links:
    - from: "src/app/factsheet/[id]/v2/TimeSeriesChart.tsx"
      to: "src/components/ResponsiveChartFrame.tsx"
      via: "TimeSeriesChart's <svg> renders through the frame"
      pattern: "ResponsiveChartFrame"
---

<objective>
Extract the EXACT responsive SVG container recipe from `TimeSeriesChart` into a reusable `ResponsiveChartFrame`, then have `TimeSeriesChart` adopt the frame so its rendered output is byte-identical (parity-by-construction). Prove byte-identity with a NEW structural unit test asserting the verbatim attribute strings — the named parity spec `e2e/strategy-v2-chart-parity.spec.ts` is permanently `test.skip(true)`, targets the wrong route/stack, has no goldens, and does NOT test TimeSeriesChart, so it CANNOT be the guard.

Purpose: Phases 47/48 wrap 16+ SVG charts; the responsive recipe must live in ONE place. The single correctness boundary here is the FROZEN-OUTPUT regression boundary — the extraction must not change one attribute of TimeSeriesChart's rendered SVG (this is a regression boundary, not a security threat).
Output: `src/components/ResponsiveChartFrame.tsx` (+ test), `src/app/factsheet/[id]/v2/TimeSeriesChart.tsx` (refactored to use the frame). Delivers part of SC#3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/44-foundation-primitives-verification-gates/44-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- The EXACT live recipe to extract — verified at TimeSeriesChart.tsx:564-588. -->
<!-- VB_W = 880 (line 9); height = config.height ?? 280 (line 37). -->
The current TimeSeriesChart <svg> (must stay byte-identical after extraction):
```tsx
<svg
  ref={svgRef}
  viewBox={`0 0 ${VB_W} ${height}`}                     // VB_W=880
  preserveAspectRatio="xMidYMid meet"                   // line 570
  role="img"
  aria-label={ariaLabel(...)}
  aria-describedby={`chart-help-${config.key}`}
  tabIndex={0}
  focusable="true"
  className="block w-full cursor-crosshair touch-pan-y select-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent"  // line 576
  style={{ aspectRatio: `${VB_W} / ${height}`, maxHeight: height, width: "100%", height: "auto" }}      // line 580
  onPointerMove={onPointerMove}
  onPointerLeave={onPointerLeave}
  onPointerDown={onPointerDown}
  onPointerUp={onPointerUp}
  onPointerCancel={onPointerUp}
  onWheel={onWheel}
  onDoubleClick={onDoubleClick}
  onKeyDown={onKeyDown}
>{children}</svg>
```
DEAD spec — do NOT rely on it: `e2e/strategy-v2-chart-parity.spec.ts:47-51` is `test.skip(true)` (Recharts/lightweight-charts assumptions, wrong `/strategy/[id]/v2` route, no goldens; TimeSeriesChart lives at `/factsheet/[id]/v2`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ResponsiveChartFrame with the verbatim responsive recipe + structural unit test</name>
  <read_first>
    - src/app/factsheet/[id]/v2/TimeSeriesChart.tsx:9,37,564-588 (the exact viewBox/preserveAspectRatio/className/style recipe + all svg props)
    - 44-RESEARCH.md "Pattern 2: ResponsiveChartFrame extraction (parity-by-construction)" (lines 198-231)
    - 44-RESEARCH.md Pitfall 4 (breaking TimeSeriesChart's rendered output) (lines 324-327)
  </read_first>
  <files>src/components/ResponsiveChartFrame.tsx, src/components/ResponsiveChartFrame.test.tsx</files>
  <behavior>
    - The frame renders an `<svg>` whose responsive core is byte-identical to TimeSeriesChart's: `viewBox="0 0 {width} {height}"`, `preserveAspectRatio="xMidYMid meet"`, `className` containing `block w-full` (caller className appended verbatim), `style` containing `aspectRatio: "{width} / {height}"`, `maxHeight: {height}`, `width: "100%"`, `height: "auto"`.
    - The frame forwards `ref` to the underlying `<svg>` (TimeSeriesChart needs svgRef for export/pointer math).
    - The frame passes through ALL caller props: children, every `on*` handler, role, aria-label, aria-describedby, tabIndex, focusable, the full className, the rest of style.
    - With width=880, height=280 the emitted attribute strings equal the verbatim values from TimeSeriesChart.
  </behavior>
  <action>
    Create `src/components/ResponsiveChartFrame.tsx` (`"use client"`). Export `ResponsiveChartFrame` as a `forwardRef<SVGSVGElement, ...>` component taking `width: number`, `height: number`, `className?: string`, `style?: React.CSSProperties`, `children`, and the rest spread (`...rest`) so every `on*`/aria/role/tabIndex/focusable prop forwards. Internally render `<svg ref={ref} viewBox={`0 0 ${width} ${height}`} preserveAspectRatio="xMidYMid meet" className={`block w-full ${className ?? ""}`.trim()} style={{ aspectRatio: `${width} / ${height}`, maxHeight: height, width: "100%", height: "auto", ...style }} {...rest}>{children}</svg>`. Keep the responsive class core `block w-full` and `preserveAspectRatio="xMidYMid meet"` VERBATIM. Co-locate `ResponsiveChartFrame.test.tsx`: render the frame with width=880, height=280 and assert the rendered svg's attributes equal EXACTLY: `viewBox="0 0 880 280"`, `preserveAspectRatio="xMidYMid meet"`, className contains `block w-full`, and `style` carries `aspect-ratio: 880 / 280`. Also assert a passed `data-testid`/`onClick`/`ref`/`aria-label`/`tabIndex`/`focusable` forwards through (proves the passthrough contract Pitfall 4 depends on).
  </action>
  <verify>
    <automated>npx vitest run src/components/ResponsiveChartFrame.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ResponsiveChartFrame.test.tsx` exits 0.
    - `src/components/ResponsiveChartFrame.tsx` contains the literal strings `preserveAspectRatio="xMidYMid meet"` and `block w-full` and uses `forwardRef`.
    - The test asserts the rendered svg `viewBox` is exactly `0 0 880 280` and `preserveAspectRatio` is exactly `xMidYMid meet` (string equality, falsifiable if an attribute drifts).
    - The test asserts a forwarded ref resolves to the `<svg>` element and an arbitrary passthrough prop (e.g. `aria-label`) appears on the rendered svg.
  </acceptance_criteria>
  <done>ResponsiveChartFrame emits the verbatim recipe, forwards ref + all props, and a structural unit test pins the exact attribute strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adopt the frame in TimeSeriesChart (byte-identical) + assert parity</name>
  <read_first>
    - src/app/factsheet/[id]/v2/TimeSeriesChart.tsx:564-589 (the <svg> being replaced) + the surrounding component for svgRef + all handler identifiers
    - src/components/ResponsiveChartFrame.tsx (the new frame from Task 1 — the contract it accepts)
    - 44-RESEARCH.md Pitfall 4 + "Safest adoption strategy" (lines 231, 324-327)
    - e2e/strategy-v2-keyboard.spec.ts header + tests/visual/chart-accessibility-layer.test.ts (the SVG-focusability-sensitive guards that must stay green)
  </read_first>
  <files>src/app/factsheet/[id]/v2/TimeSeriesChart.tsx, src/components/ResponsiveChartFrame.test.tsx</files>
  <behavior>
    - TimeSeriesChart's `<svg>` is replaced by `<ResponsiveChartFrame>` passing `width={VB_W}` (880), `height={height}` and ALL existing props: `ref={svgRef}`, `role="img"`, `aria-label`, `aria-describedby`, `tabIndex={0}`, `focusable="true"`, the full existing className (cursor-crosshair touch-pan-y select-none focus-visible:…), and the 7 handlers (onPointerMove/Leave/Down/Up/Cancel, onWheel, onDoubleClick, onKeyDown).
    - The rendered SVG DOM is byte-identical to before: same viewBox, preserveAspectRatio, the FULL className (block w-full + all the chart-specific classes), style, role, aria, tabIndex, focusable, handlers.
    - VB_W stays 880; height default stays 280.
  </behavior>
  <action>
    Refactor `TimeSeriesChart.tsx`'s top-level `<svg ref={svgRef} …>…</svg>` (currently lines 564-589) to render through `ResponsiveChartFrame` (import from `@/components/ResponsiveChartFrame`). Pass `width={VB_W}` and `height={height}`; pass the chart-specific className `cursor-crosshair touch-pan-y select-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent` as the `className` prop (the frame prepends `block w-full`, reconstituting the EXACT original full className string — verify the concatenation order matches `block w-full cursor-crosshair …`). Forward `ref={svgRef}`, `role`, `aria-label`, `aria-describedby`, `tabIndex`, `focusable`, and all 7 `on*` handlers verbatim. Do NOT move the chart-specific style (the frame already supplies aspectRatio/maxHeight/width/height — confirm the merged style equals the original `{ aspectRatio: '880 / 280', maxHeight: 280, width: '100%', height: 'auto' }`; if the original svg had additional inline style keys they must be preserved). Keep VB_W=880 and the children unchanged. Then ADD an adoption-parity assertion to `ResponsiveChartFrame.test.tsx` (or a co-located TimeSeriesChart parity test if RTL-rendering the full chart is feasible without heavy context mocks): assert the className string the frame produces from `block w-full` + the chart classes equals the verbatim original full className, so a future class-order drift fails loud. If rendering the full TimeSeriesChart requires extensive FactsheetProvider mocking, prefer the frame-level className-concatenation assertion (cheaper, equally falsifiable per RESEARCH Open Question 2 recommendation).
  </action>
  <verify>
    <automated>npx vitest run src/components/ResponsiveChartFrame.test.tsx && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/factsheet/[id]/v2/TimeSeriesChart.tsx` contains `ResponsiveChartFrame` and NO longer contains a bare top-level `<svg ref={svgRef}` (the frame owns the svg).
    - `VB_W = 880` is still present in TimeSeriesChart.tsx and `height = config.height ?? 280` unchanged (`git diff` shows only the svg→frame swap, not a constant change).
    - `npx vitest run src/components/ResponsiveChartFrame.test.tsx` exits 0 including the className-parity assertion that the produced full className equals `block w-full cursor-crosshair touch-pan-y select-none focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-accent`.
    - `npx tsc --noEmit` passes (the frame's prop types accept svgRef + all handlers).
    - `npx vitest run tests/visual/chart-accessibility-layer.test.ts` still exits 0 (no recharts contract regression).
  </acceptance_criteria>
  <done>TimeSeriesChart renders through ResponsiveChartFrame with byte-identical output; a falsifiable className-parity assertion guards against drift; the dead parity spec is NOT relied upon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan extracts a presentational SVG wrapper and refactors one client component to use it. There is NO new auth, data access, network endpoint, user-input trust boundary, secret handling, or SQL. No new attack surface — additive presentation over the frozen engine.

The one real correctness boundary is the **FROZEN-OUTPUT byte-identity invariant**: the `ResponsiveChartFrame` extraction must not change `TimeSeriesChart`'s rendered SVG (viewBox/preserveAspectRatio/className/style/handlers/a11y attrs). This is a **regression boundary, not a security threat** — guarded by the structural parity unit test, not by any access control.

| Boundary | Description |
|----------|-------------|
| (none, security) | No untrusted input crosses any boundary. |
| TimeSeriesChart rendered output (regression) | The extraction must preserve byte-identical SVG output; enforced by the className/attribute parity unit test, not a security control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-44-02 | Tampering | TimeSeriesChart rendered SVG | mitigate | Structural unit test pins the exact viewBox/preserveAspectRatio/className strings so a silent output regression fails CI loud (regression mitigation, not security). |
| T-44-03 | Information Disclosure | ResponsiveChartFrame | accept | Pure presentation; renders caller-supplied SVG children, no PII/secrets/network. |
| T-44-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (RESEARCH Package Legitimacy Audit: no installs). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run src/components/ResponsiveChartFrame.test.tsx` — frame + adoption-parity assertions green.
- `npx tsc --noEmit` — types accept svgRef + all handlers.
- `npx vitest run tests/visual/chart-accessibility-layer.test.ts` — recharts a11y contract unchanged.
- `git diff src/app/factsheet/[id]/v2/TimeSeriesChart.tsx` shows ONLY the svg→frame swap (no constant/height/children changes).
</verification>

<success_criteria>
- ResponsiveChartFrame emits the verbatim viewBox/preserveAspectRatio/w-full recipe and forwards ref + all props.
- TimeSeriesChart adopts it with byte-identical output, proven by a falsifiable structural unit test (NOT the dead e2e spec).
- chart-accessibility-layer + tsc stay green.
</success_criteria>

<output>
Create `.planning/phases/44-foundation-primitives-verification-gates/44-02-SUMMARY.md` when done
</output>
