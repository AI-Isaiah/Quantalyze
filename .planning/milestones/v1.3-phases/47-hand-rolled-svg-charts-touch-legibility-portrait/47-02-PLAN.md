---
phase: 47-hand-rolled-svg-charts-touch-legibility-portrait
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/factsheet/[id]/v2/DistributionPanels.tsx
  - src/app/factsheet/[id]/v2/SignaturePanels.tsx
  - src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx
  - src/app/factsheet/[id]/v2/HistogramChart.tsx
  - src/app/factsheet/[id]/v2/MasterBrush.tsx
  - src/app/factsheet/[id]/v2/no-hover-panels-viewport.test.tsx
autonomous: true
requirements: [CHART-02, CHART-03]
must_haves:
  truths:
    - "At 320px, axis/label text on these panels renders at an effective ≥~12px (legibility floor)"
    - "On mobile the dense 880/1100-wide panels get a taller viewBox + fewer ticks; the desktop viewBox/fontSize/tick-count is byte-identical to today"
    - "The correlation matrix keeps ALL cells at 320px (no row/col drop), using the existing overflow-x-auto scroll region"
    - "No chart recomputes a series/metric/domain — every value is read from the existing precomputed prop arrays"
    - "Each tuned panel's mobile (isMobile=true) AND desktop (isMobile=false) render branch is exercised by a unit/component test in THIS wave (holds the coverage ratchet branches; the desktop render also doubles as a cheap byte-identity assertion)"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/DistributionPanels.tsx"
      provides: "Legibility+portrait for EndOfYearBars / QuantileBoxPlot / CorrelationStrip / CorrelationsMatrix"
      contains: "useBreakpoint"
    - path: "src/app/factsheet/[id]/v2/SignaturePanels.tsx"
      provides: "Legibility+portrait for SignaturesSection"
      contains: "ResponsiveChartFrame"
    - path: "src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx"
      provides: "Legibility+portrait for CrossSignaturesSection"
      contains: "ResponsiveChartFrame"
    - path: "src/app/factsheet/[id]/v2/HistogramChart.tsx"
      provides: "Legibility+portrait only (NO tap-reveal — no desktop hover)"
      contains: "useBreakpoint"
    - path: "src/app/factsheet/[id]/v2/MasterBrush.tsx"
      provides: "Legibility+portrait; brush behavior intact; NO tap-reveal"
      contains: "useBreakpoint"
    - path: "src/app/factsheet/[id]/v2/no-hover-panels-viewport.test.tsx"
      provides: "Wave-1 viewport-branch coverage: renders each Plan-02 panel with isMobile=true AND isMobile=false (mock useBreakpoint) to exercise both branches + assert desktop render unchanged"
      contains: "useBreakpoint"
  key_links:
    - from: "DistributionPanels.tsx / SignaturePanels.tsx / CrossSignaturePanels.tsx / HistogramChart.tsx / MasterBrush.tsx"
      to: "useBreakpoint()"
      via: "mobile-gated tick/font/viewBox selection"
      pattern: "useBreakpoint"
    - from: "each in-scope <svg>"
      to: "ResponsiveChartFrame"
      via: "wrap the root svg"
      pattern: "ResponsiveChartFrame"
    - from: "no-hover-panels-viewport.test.tsx"
      to: "the 5 Plan-02 panels"
      via: "render both isMobile branches by mocking useBreakpoint"
      pattern: "vi.mock"
---

<objective>
Bring the NO-hover hand-rolled SVG panels (the ones that get legibility + portrait ONLY, never a new
interaction surface per the binding parity gate) to 320px legibility (CHART-02) and portrait tuning
(CHART-03): EndOfYearBars / QuantileBoxPlot / CorrelationStrip / CorrelationsMatrix (DistributionPanels),
SignaturesSection (SignaturePanels), CrossSignaturesSection (CrossSignaturePanels), HistogramChart, and
MasterBrush (brush stays; legibility only). Each `<svg>` is wrapped in `ResponsiveChartFrame`; a
`useBreakpoint`-gated mobile branch bumps font + reduces ticks + raises the mobile viewBox height — while
the DESKTOP branch returns today's EXACT literals so the desktop golden stays byte-identical. A Wave-1
viewport-branch test renders every tuned panel in BOTH branches (isMobile=true / false) so the new
conditionals are covered in the SAME wave they're introduced (the coverage ratchet is a BLOCKING CI gate
with only ~3.5pts of branch headroom — the Plan 05 ratchet gate is too late to fix cheaply).

Purpose: Fix the WCAG 1.4.4 viewBox-downscale trap (effective ~3-6px today) on the densest panels without
inventing UI or recomputing any value — and keep the branch-coverage ratchet green in-wave.
Output: 5 edited panel files + 1 new viewport-branch test file; desktop render byte-identical; mobile
legible at 320px.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-RESEARCH.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-PATTERNS.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-UI-SPEC.md

<interfaces>
<!-- The two locked primitives every task wraps. Use these directly — no re-derivation. -->

From src/components/ResponsiveChartFrame.tsx (Phase-44 primitive, 60 LOC):
- Props: `width` (viewBox W), `height` (viewBox H), `className`, `style`, plus SVGProps passthrough (`role`, `aria-label`, ref, handlers)
- Emits: `viewBox="0 0 {width} {height}"` + `preserveAspectRatio="xMidYMid meet"` + className `block w-full` (LEADS) + style `aspectRatio`/`maxHeight`/`width:100%`/`height:auto`
- Byte-identity is pinned by src/components/ResponsiveChartFrame.test.tsx (exact attribute + class order)

From src/hooks/useBreakpoint.ts (32 LOC):
- `useBreakpoint(): "mobile" | "tablet" | "desktop"` — `max-width:639px` → mobile; SSR snapshot is `"desktop"`
- Wraps `useMediaQuery`. For UNIT TESTS the cleanest seams (zero net-new deps) are EITHER:
  (a) `vi.mock("@/hooks/useBreakpoint", () => ({ useBreakpoint: vi.fn() }))` then set `.mockReturnValue("mobile" | "desktop")` per render — simplest for component branch tests, OR
  (b) drive the real hook via the existing `installMatchMedia({...})` matchMedia stub from src/hooks/useBreakpoint.test.ts:40-59 (per-query map; `{"(max-width: 639px)": true}` → mobile, `{}` → desktop). Prefer (a) for these panel component renders.

Canonical mobile-gated tuning (desktop branch = today's literals → golden unchanged):
  const bp = useBreakpoint(); const isMobile = bp === "mobile";
  const vbH = isMobile ? VB_H_MOBILE : VB_H_DESKTOP;   // desktop = today's literal
  const tickFont = isMobile ? 14 : 10;                  // desktop = today's literal
  const tickCount = isMobile ? 4 : 8;                   // desktop = today's literal

From src/components/charts/chart-tokens.ts:
- All hex literals live here (CHART_ACCENT #1B6B5A, CHART_AXIS_TICK #64748B, baked heatmap ramp). Any SVG hex must come from here or var(--color-*). NEVER container-opacity a baked heatmap cell.

Test analogs (reuse — do NOT invent a new harness):
- src/components/charts/DailyHeatmap.test.tsx (props-only `render(...)` + structural assertions on the rendered SVG)
- src/components/ResponsiveChartFrame.test.tsx (`/** @vitest-environment jsdom */`, `render` + `screen.getByTestId`, exact-attribute assertions)
- src/hooks/useBreakpoint.test.ts (`installMatchMedia` stub if driving the real hook)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DistributionPanels — RCF wrap + mobile legibility/portrait for 4 svg panels (keep-all-cells correlation matrix)</name>
  <files>src/app/factsheet/[id]/v2/DistributionPanels.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/DistributionPanels.tsx (EndOfYearBarsPanel L26, QuantileBoxPlotPanel L192, CorrelationStripPanel L318 in overflow-x-auto, CorrelationsMatrixPanel L423/scroll region L427)
    - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx (lines 55-60 — the EXACT inline svg recipe that ResponsiveChartFrame replaces, the sibling analog)
    - src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts
    - .planning/phases/.../47-RESEARCH.md (Legibility Math table; Portrait Strategy; Pitfall 2 desktop-golden)
  </read_first>
  <action>
    For EndOfYearBarsPanel (VB_W=880, fontSize 10-12), QuantileBoxPlotPanel (880×130, fontSize 9-11), CorrelationStripPanel (880×var, in overflow-x-auto), CorrelationsMatrixPanel (880×var, the dense heatmap in overflow-x-auto at L427):
    1. Replace each raw inline `<svg viewBox preserveAspectRatio className style>` with `<ResponsiveChartFrame width={VB_W} height={vbH} role="img" aria-label={...}>`. Keep existing `role`/`aria-label` text verbatim.
    2. Add `const bp = useBreakpoint(); const isMobile = bp === "mobile";`. Gate font/tick/viewBox-height behind `isMobile`. DESKTOP branch MUST return today's exact fontSize, tick count, and VB_H (Pitfall 2 — desktop golden must not change). The mobile font-bump target is effective ≥~12px at 320px; per the Legibility Math the 880-wide panels can't clear that with an in-svg bump alone — use aggressive tick-reduction + a taller mobile viewBox, and/or HTML-overlay real-px labels (DM Sans/Geist Mono at literal 12px positioned over the svg) for the densest (EndOfYearBars). Executor discretion per RESEARCH A3; iterate against the 320px portrait snapshot Plan 05 bakes.
    3. CorrelationsMatrixPanel (CHART-03 keep-all-cells): KEEP ALL cells — no row/col drop. Keep the existing `overflow-x-auto` scroll region. At mobile reduce/rotate axis labels and bump cell-label fontSize; do NOT drop strategies from the matrix and do NOT downsample data.
    4. All hex from chart-tokens.ts; never container-opacity a baked cell. Read every value from the precomputed prop arrays — NEVER recompute a series/metric/domain.
  </action>
  <verify>
    <automated>grep -q "useBreakpoint" src/app/factsheet/[id]/v2/DistributionPanels.tsx && grep -q "ResponsiveChartFrame" src/app/factsheet/[id]/v2/DistributionPanels.tsx && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -c "DistributionPanels" | grep -q "^0$" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `DistributionPanels.tsx` imports and calls `useBreakpoint` and renders `<ResponsiveChartFrame` for all 4 svg panels
    - Every viewport-dependent value is `isMobile ? mobileValue : todaysLiteral` (the desktop arm equals the pre-edit literal)
    - The CorrelationsMatrixPanel cell-render loop is unchanged in cell COUNT (no `.slice`/`.filter` that drops rows/cols added) — `grep -c "no row/col drop" ...` not required, but the matrix mapping length is data-driven, not capped
    - No new hex literals: `grep -nE "#[0-9A-Fa-f]{6}" src/app/factsheet/[id]/v2/DistributionPanels.tsx` shows no NEW hex vs pre-edit (all colors via chart-tokens or var(--color-*))
    - `npx tsc --noEmit` reports 0 errors in this file
  </acceptance_criteria>
  <done>All 4 DistributionPanels svg panels are RCF-wrapped, mobile-legible (effective ≥~12px via font-bump/tick-reduction/overlay), portrait-tuned; correlation matrix keeps all cells; desktop branch byte-identical; no recompute.</done>
</task>

<task type="auto">
  <name>Task 2: SignaturePanels + CrossSignaturePanels — RCF wrap + mobile legibility/portrait</name>
  <files>src/app/factsheet/[id]/v2/SignaturePanels.tsx, src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/SignaturePanels.tsx (SignaturesSection L36, 880×230, fontSize 10)
    - src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx (CrossSignaturesSection L30, 880×200, fontSize 10)
    - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx (the sibling RCF/inline-recipe analog)
    - src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts
  </read_first>
  <action>
    Both are NO-hover → legibility + portrait ONLY (no tap-reveal). For each: wrap the root `<svg>` in `<ResponsiveChartFrame width={880} height={vbH} ...>` keeping existing role/aria-label verbatim; add `const isMobile = useBreakpoint() === "mobile"`; mobile branch bumps fontSize from 10, reduces tick count, raises VB_H (e.g. 230→ taller, 200→280); DESKTOP branch returns today's exact fontSize (10), tick count, and VB_H (Pitfall 2). 880-wide → ~3.3px effective today; per RESEARCH use tick-reduction + taller mobile viewBox (and overlay labels only if needed). Read all values from precomputed props; all hex via chart-tokens.
  </action>
  <verify>
    <automated>grep -q "useBreakpoint" src/app/factsheet/[id]/v2/SignaturePanels.tsx && grep -q "ResponsiveChartFrame" src/app/factsheet/[id]/v2/SignaturePanels.tsx && grep -q "useBreakpoint" src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx && grep -q "ResponsiveChartFrame" src/app/factsheet/[id]/v2/CrossSignaturePanels.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files call `useBreakpoint` and render `<ResponsiveChartFrame`
    - Every viewport-dependent value is `isMobile ? mobile : todaysLiteral`
    - No tap-reveal/`useTapPin`/`tabIndex`/pointer handlers added (these panels have no desktop hover)
    - `npx tsc --noEmit` reports 0 errors in these two files
  </acceptance_criteria>
  <done>Both signature panels RCF-wrapped, mobile-legible and portrait-tuned, desktop byte-identical, no interaction added.</done>
</task>

<task type="auto">
  <name>Task 3: HistogramChart + MasterBrush — RCF wrap + mobile legibility/portrait (NO tap-reveal)</name>
  <files>src/app/factsheet/[id]/v2/HistogramChart.tsx, src/app/factsheet/[id]/v2/MasterBrush.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/HistogramChart.tsx (L34 export, 880×200, fontSize 10; the inline svg style recipe ~L240; CORRECTED hover inventory: NO per-bar value reveal — only wheel-zoom + double-click)
    - src/app/factsheet/[id]/v2/MasterBrush.tsx (L36 export, 1100×60, fontSize 9; its 5 pointer handlers are a brush/scrub DRAG at L99-215, NOT a value tooltip)
    - .planning/phases/.../47-RESEARCH.md (Pitfall 1 — UI-SPEC "yes (1 marker)" for HistogramChart is empirically FALSE; Open Question 1 — MasterBrush leans NO tap-reveal)
    - src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts
  </read_first>
  <action>
    BOTH are legibility + portrait ONLY. Re-confirm at edit time (read the source) that HistogramChart has NO per-bar `<title>`/`hovered`-state value reveal (only wheel-zoom + double-click) and that MasterBrush's pointer handlers are brush/scrub drag, not a value tooltip — therefore by the binding parity gate NEITHER gets tap-reveal. Do NOT wire useTapPin here.
    HistogramChart: wrap root `<svg>` in `<ResponsiveChartFrame width={880} height={vbH}>`; mobile-gate fontSize bump from 10 + fewer ticks + taller VB_H; KEEP wheel-zoom + double-click behavior intact; desktop branch = today's literals.
    MasterBrush: wrap root `<svg>` in `<ResponsiveChartFrame width={1100} height={vbH}>`; 1100-wide → ~2.4px effective (worst offender) — bump fontSize from 9, render minimal labels at mobile, raise VB_H; KEEP the brush/scrub drag handlers + setPointerCapture exactly as-is; desktop branch = today's literals.
    Read all values from precomputed props; all hex via chart-tokens; never container-opacity.
  </action>
  <verify>
    <automated>grep -q "useBreakpoint" src/app/factsheet/[id]/v2/HistogramChart.tsx && grep -q "ResponsiveChartFrame" src/app/factsheet/[id]/v2/HistogramChart.tsx && grep -q "useBreakpoint" src/app/factsheet/[id]/v2/MasterBrush.tsx && grep -q "ResponsiveChartFrame" src/app/factsheet/[id]/v2/MasterBrush.tsx && ! grep -q "useTapPin" src/app/factsheet/[id]/v2/HistogramChart.tsx && ! grep -q "useTapPin" src/app/factsheet/[id]/v2/MasterBrush.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Both files call `useBreakpoint` and render `<ResponsiveChartFrame`
    - NEITHER file imports/uses `useTapPin` (parity gate — no desktop value-reveal → no new interaction)
    - MasterBrush still contains its brush drag handlers + `setPointerCapture` (the existing brush behavior is intact, not removed)
    - HistogramChart still contains its wheel/double-click handlers
    - Every viewport-dependent value is `isMobile ? mobile : todaysLiteral`; `npx tsc --noEmit` 0 errors in both files
  </acceptance_criteria>
  <done>HistogramChart + MasterBrush RCF-wrapped, mobile-legible and portrait-tuned, existing brush/zoom behavior intact, no tap-reveal invented, desktop byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave-1 viewport-branch coverage test — render the 5 no-hover panels in BOTH isMobile branches (hold the ratchet)</name>
  <files>src/app/factsheet/[id]/v2/no-hover-panels-viewport.test.tsx</files>
  <read_first>
    - src/components/charts/DailyHeatmap.test.tsx (the props-only `render(...)` + structural-assertion analog — reuse this harness shape)
    - src/components/ResponsiveChartFrame.test.tsx (`/** @vitest-environment jsdom */` header + `render`/`screen` usage + exact-attribute assertions; the byte-identity assertion style)
    - src/hooks/useBreakpoint.test.ts (the `installMatchMedia` matchMedia stub at L40-59 — fallback seam if `vi.mock` of the hook is awkward for a panel)
    - the 5 Plan-02 panel exports: DistributionPanels.tsx, SignaturePanels.tsx, CrossSignaturePanels.tsx, HistogramChart.tsx, MasterBrush.tsx (read each export signature + the minimal precomputed prop shape it needs)
  </read_first>
  <behavior>
    - Each of the 5 tuned panels (the 4 DistributionPanels svg panels — EndOfYearBars / QuantileBoxPlot / CorrelationStrip / CorrelationsMatrix — plus SignaturesSection, CrossSignaturesSection, HistogramChart, MasterBrush) renders WITHOUT throwing when `useBreakpoint()` returns `"mobile"` (isMobile=true branch covered).
    - The same panels render WITHOUT throwing when `useBreakpoint()` returns `"desktop"` (isMobile=false branch covered).
    - Desktop branch byte-identity: in the desktop render, the panel's root `ResponsiveChartFrame` svg emits `viewBox="0 0 {W} {desktopH}"` equal to today's literal VB dims (assert the exact desktop viewBox string per panel — a mutation of the desktop VB_H literal makes this FAIL).
    - Mobile vs desktop differ: for at least the dense panels, the mobile render's `viewBox` height (and/or tick-label count) differs from the desktop render's (proves the conditional is live, not dead).
    - The CorrelationsMatrix mobile render still emits the SAME cell count as the desktop render (no row/col drop) — assert the rendered cell-element count is equal across both branches.
  </behavior>
  <action>
    Author `src/app/factsheet/[id]/v2/no-hover-panels-viewport.test.tsx` with `/** @vitest-environment jsdom */`. Mock the breakpoint seam with `vi.mock("@/hooks/useBreakpoint", () => ({ useBreakpoint: vi.fn() }))` and a typed handle to set `"mobile"` / `"desktop"` per render (or, if a panel imports useBreakpoint by relative path, use `installMatchMedia({"(max-width: 639px)": true})` for mobile / `installMatchMedia({})` for desktop — pick whichever the panel's import resolves cleanly). Render each of the 5 panels twice (mobile, desktop) with a minimal deterministic precomputed prop fixture (reuse/trim the synthetic-payload shape from DailyHeatmap.test.tsx / existing chart tests; do NOT compute real metrics — hand-author tiny arrays). Assert the behaviors above: both branches render, desktop viewBox equals today's literal (byte-identity), mobile differs, correlation cell count equal across branches. Keep it ONE combined test file (do NOT over-engineer a per-function suite). Zero net-new npm deps — use the already-installed `vitest` + `@testing-library/react`.
    CRITICAL: this test must EXERCISE both arms of every new `isMobile ? a : b` conditional introduced by Tasks 1-3 so the branch ratchet (branches ≥72, BLOCKING) holds in THIS wave — not at the Plan 05 gate. If a panel's branch can't be reached via render alone (e.g. a value only used in an overlay path), render the path that reaches it.
  </action>
  <verify>
    <automated>npx vitest run src/app/factsheet/[id]/v2/no-hover-panels-viewport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/factsheet/[id]/v2/no-hover-panels-viewport.test.tsx` exits 0
    - The test renders each of the 5 Plan-02 panels with `useBreakpoint` forced to BOTH `"mobile"` and `"desktop"` (both `isMobile` arms exercised) — `grep -c "mobile" ...` ≥ 1 AND `grep -c "desktop" ...` ≥ 1
    - At least one assertion pins the DESKTOP `viewBox` to today's literal VB dims (mutating a desktop VB_H literal in a panel makes this test FAIL — falsifiable byte-identity)
    - An assertion proves the CorrelationsMatrix renders the SAME cell count in mobile and desktop (no row/col drop)
    - No new npm dependency added (`git diff package.json` shows no dependency change); the file uses only `vitest` + `@testing-library/react`
  </acceptance_criteria>
  <done>A single Wave-1 test renders all 5 no-hover panels in both isMobile branches, holding the branch-coverage ratchet in-wave, and doubles as a falsifiable desktop byte-identity + keep-all-cells assertion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| precomputed payload → svg render | Frozen-engine output crosses into the panel render path. The panels read it; they must not recompute it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-02-INTEG | Tampering (data integrity) | the 5 no-hover panels | mitigate | Desktop branch returns today's exact literals → desktop golden byte-identical (the no-recompute proof, Plan 05). The Wave-1 viewport-branch test (Task 4) also pins the desktop viewBox literals so a desktop drift fails IN-WAVE. Every value read from precomputed prop arrays; no series/metric/domain re-derivation. |
| T-47-02-XSS | Tampering / XSS | `<text>{value}</text>` | accept | React auto-escapes svg text; no `dangerouslySetInnerHTML`. Existing pattern, no new risk. |
| T-47-02-A11Y | (a11y, not security) | non-interactive panels | mitigate | No `tabIndex`/interaction added to these no-hover panels (avoids the empty-focus-stop trap DESIGN.md pins against). |

> Presentation-only retrofit: no new routes, data flow, auth, or network surface. The genuine invariant is frozen-math byte-identity, asserted via the desktop golden + SCENARIO-05/BODY-02 in Plan 05, plus the in-wave desktop-viewBox assertion in Task 4.
</threat_model>

<verification>
- `npx tsc --noEmit` 0 errors across the 5 files.
- All 5 files call `useBreakpoint` + render `ResponsiveChartFrame`; HistogramChart/MasterBrush do NOT use useTapPin.
- No new hex literals; correlation matrix keeps all cells.
- `npx vitest run src/app/factsheet/[id]/v2/no-hover-panels-viewport.test.tsx` green — both isMobile branches exercised for all 5 panels; desktop viewBox byte-identity asserted.
- The DESKTOP byte-identity proof is ALSO asserted in Plan 05 (fresh parity spec); SCENARIO-05/BODY-02/compute.ts parity stay green.
</verification>

<success_criteria>
- All 5 no-hover svg panels legible at 320px (effective ≥~12px) and portrait-tuned, with the desktop render byte-identical.
- Correlation matrix keeps ALL cells at 320px via the existing scroll region.
- The new viewport conditionals are branch-covered IN THIS WAVE (ratchet held); no interaction invented; no value recomputed.
</success_criteria>

<output>
Create `.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-02-SUMMARY.md` when done.
</output>
</content>
</invoke>
