---
phase: 47-hand-rolled-svg-charts-touch-legibility-portrait
plan: 05
type: execute
wave: 3
depends_on: ["47-02", "47-03", "47-04"]
files_modified:
  - e2e/svg-chart-parity.spec.ts
  - e2e/__snapshots__/svg-chart-parity.spec.ts/
  - e2e/target-size.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [CHART-01a, CHART-02, CHART-03]
must_haves:
  truths:
    - "A fresh seeded Playwright spec captures DESKTOP byte-identity goldens for the in-scope SVG charts on /strategy/[id]/v2 (the no-recompute proof)"
    - "The same spec captures 320px PORTRAIT snapshots proving axis/label text is legible (CHART-02 floor) and mobile is portrait-tuned (CHART-03)"
    - "The target-size gate asserts the tap-reveal charts' hit rects ≥44px at 320px on the seeded factsheet route"
    - "Both new/extended gates are FLOW-01 dual-wired (HAS_SEED_ENV const + ci.yml MA-8 list) and PROVEN to run (not skip) in a real CI run"
    - "The dead e2e/strategy-v2-chart-parity.spec.ts is NOT resurrected; the full vitest suite + coverage ratchet stay green"
  artifacts:
    - path: "e2e/svg-chart-parity.spec.ts"
      provides: "Fresh seeded desktop-golden + 320px-portrait parity spec for the in-scope SVG charts"
      contains: "HAS_SEED_ENV"
    - path: "e2e/target-size.spec.ts"
      provides: "Extended ≥44px chart-tap-rect assertion on the seeded factsheet route"
      contains: "assertTargetSizes"
    - path: ".github/workflows/ci.yml"
      provides: "FLOW-01 wiring: new spec added to the MA-8 seeded playwright list"
      contains: "svg-chart-parity.spec.ts"
  key_links:
    - from: "e2e/svg-chart-parity.spec.ts"
      to: "seedStrategyWithHistory + /strategy/[id]/v2?strategy_v2=on"
      via: "seeded route render → toHaveScreenshot goldens"
      pattern: "seedStrategyWithHistory"
    - from: ".github/workflows/ci.yml"
      to: "e2e/svg-chart-parity.spec.ts"
      via: "MA-8 npx playwright test list (FLOW-01 place 1)"
      pattern: "svg-chart-parity.spec.ts"
---

<objective>
Stand up the falsifiable verification for the whole phase: a FRESH seeded Playwright spec
(`e2e/svg-chart-parity.spec.ts`) that bakes DESKTOP byte-identity goldens (the no-recompute proof,
CHART-03) AND 320px PORTRAIT snapshots (the CHART-02 legibility floor + CHART-03 portrait tuning) for the
in-scope SVG charts on the seeded `/strategy/[id]/v2?strategy_v2=on` route; an extension to
`e2e/target-size.spec.ts` asserting the tap-reveal charts' hit rects ≥44px at 320px (CHART-01a); and the
FLOW-01 dual-wiring into `.github/workflows/ci.yml` so both gates actually RUN in CI (not silently skip —
the twice-burned trap). The dead `e2e/strategy-v2-chart-parity.spec.ts` is left as-is (skipped, Phase-48
concern) — author a fresh spec (Area-4 correction, Rule 7).

Purpose: Prove no chart recomputes a value (desktop goldens byte-identical), prove 320px legibility, prove
≥44px touch targets — and prove the gates execute in CI.
Output: fresh spec + baked goldens + extended target-size gate + ci.yml wiring; SCENARIO-05/BODY-02/
compute.ts parity + coverage ratchet all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-RESEARCH.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-PATTERNS.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-VALIDATION.md

<interfaces>
<!-- The seeded-route template, helpers, FLOW-01 wiring sites, and the bake-order discipline. -->

Seeded-route template — e2e/reflow-sweep-authed.spec.ts:
- `const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;` (L46)
- `import { seedTestAllocator } from "./helpers/seed-test-project";` + a local `loginViaForm(page, email, password)` (L39, L50)
- `test.skip(!HAS_SEED_ENV, "...")` inside the describe (L101); `beforeAll` seeds; `beforeEach` logs in; `page.setViewportSize(...)` per test; HTTP-status fail-loud on goto; anchor on a VISIBLE element before measuring

Seed helper — e2e/helpers/seed-test-project.ts:
- `seedStrategyWithHistory({ days, name? }): Promise<string>` (L306) → returns the strategy id; the factsheet panels render on `/strategy/{id}/v2?strategy_v2=on` (the proven seeded route for the panels)
- auth helper also available: `loginAs(...)` in e2e/helpers/login.ts

Tolerance pattern (reuse, documented in the dead spec header strategy-v2-chart-parity.spec.ts:4-11):
- per-panel `toHaveScreenshot(name, { maxDiffPixelRatio: 0.02 })`; full-page `{ maxDiffPixelRatio: 0.05 }`

Target-size helper — e2e/helpers/reflow.ts:
- `assertTargetSizes(page, anchorSelector, interactiveSelector)`; `MIN_TARGET_PX = 44` (L27) — do NOT lower it; scope the selector instead
- existing usage pattern — e2e/target-size.spec.ts:40-54 (setViewport 320 → goto → status>=400 throws → assertTargetSizes(page, "main h1", scopedSelector))

FLOW-01 — TWO required wiring places:
1. ci.yml MA-8 list at lines 1252-1264 (the seeded `npx playwright test \` list ending `--timeout 60000`). The dead `e2e/strategy-v2-chart-parity.spec.ts` is ALREADY in this list at L1258 — ADD `e2e/svg-chart-parity.spec.ts \` as a NEW line; do NOT remove the dead one.
2. the spec's own `HAS_SEED_ENV` const + `test.skip(!HAS_SEED_ENV, ...)`.

Bake-order discipline (Pitfall 2): bake the DESKTOP goldens FIRST (from desktop-byte-identical output), commit them, THEN add the 320px portrait goldens. NEVER `--update-snapshots` a desktop golden after a tuning change — a desktop golden diff means the no-recompute proof was crossed.

Frozen guards that must stay green (do NOT weaken): src/__tests__/phase-31-frozen-spine-guards.test.ts (SCENARIO-05), BODY-02 byte-identity, compute.ts parity.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fresh seeded svg-chart-parity spec — desktop goldens + 320px portrait snapshots (FLOW-01 wired)</name>
  <files>e2e/svg-chart-parity.spec.ts, e2e/__snapshots__/svg-chart-parity.spec.ts/</files>
  <read_first>
    - e2e/reflow-sweep-authed.spec.ts (the seeded MA-8 template: HAS_SEED_ENV L46, loginViaForm L50, test.skip L101, beforeAll/beforeEach, per-test viewport, HTTP fail-loud, visible-anchor-before-measure)
    - e2e/strategy-v2-chart-parity.spec.ts (DEAD — read ONLY for the documented ±2%/±5% tolerance pattern header L4-11; do NOT resurrect/re-target it)
    - e2e/helpers/seed-test-project.ts (seedStrategyWithHistory L306 → strategy id; the /strategy/[id]/v2 panel route)
    - e2e/helpers/login.ts (loginAs)
    - .planning/phases/.../47-RESEARCH.md (Pitfall 2 bake-order; Pitfall 5 FLOW-01; Validation Architecture)
  </read_first>
  <action>
    Author a NEW `e2e/svg-chart-parity.spec.ts` (do NOT touch the dead spec). Add the FLOW-01 `HAS_SEED_ENV` const + `test.skip(!HAS_SEED_ENV, "...skipping prevents false-green on login/404...")` inside the describe.
    `beforeAll`: `const strategyId = await seedStrategyWithHistory({ days: 252, name: "Phase 47 svg-parity" })`. `beforeEach`: log in (loginViaForm/loginAs). For each test: `page.goto('/strategy/' + strategyId + '/v2?strategy_v2=on')`; if `res.status() >= 400` throw a loud error; await a VISIBLE panel anchor before any screenshot (fail-loud on blank/unhydrated).
    DELIVERABLE 1 — DESKTOP goldens (no-recompute proof): at a desktop viewport, per-panel `locator.screenshot`/`toHaveScreenshot('<panel>-desktop.png', { maxDiffPixelRatio: 0.02 })` for the in-scope panels that render on this route (StreakDistribution, BootstrapCI, EndOfYearBars, QuantileBoxPlot, CorrelationStrip, CorrelationsMatrix, Signatures, CrossSignatures, Histogram, MasterBrush, DailyReturnsHeatmap, and DailyHeatmap/ReturnQuantiles if they mount via ReturnsDistributionPanel on this route) + one full-page `{ maxDiffPixelRatio: 0.05 }`. BAKE THESE FIRST (Pitfall 2) and commit them.
    DELIVERABLE 2 — 320px PORTRAIT snapshots (CHART-02/03): `page.setViewportSize({ width: 320, height: 800 })`; per-panel `toHaveScreenshot('<panel>-portrait-320.png', { maxDiffPixelRatio: 0.02 })`. These verify the mobile legibility floor + portrait tuning visually.
    Commit the baked goldens under `e2e/__snapshots__/svg-chart-parity.spec.ts/`. NEVER `--update-snapshots` a desktop golden after a tuning change.
    NOTE: MonteCarloBandChart is NOT snapshotted here (0-positions seed → won't render; its byte-identity is the Vitest snapshot in Plan 04).
  </action>
  <verify>
    <automated>grep -q "HAS_SEED_ENV" e2e/svg-chart-parity.spec.ts && grep -q "seedStrategyWithHistory" e2e/svg-chart-parity.spec.ts && grep -q "maxDiffPixelRatio" e2e/svg-chart-parity.spec.ts && grep -q "portrait" e2e/svg-chart-parity.spec.ts && test -d "e2e/__snapshots__/svg-chart-parity.spec.ts" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/svg-chart-parity.spec.ts` exists with a `HAS_SEED_ENV` const + `test.skip(!HAS_SEED_ENV, ...)` and seeds via `seedStrategyWithHistory`
    - It captures BOTH desktop goldens (no-recompute proof) AND 320px portrait snapshots (uses `maxDiffPixelRatio: 0.02` per-panel, `0.05` full-page)
    - Baked goldens are committed under `e2e/__snapshots__/svg-chart-parity.spec.ts/` (directory exists and is non-empty)
    - The dead `e2e/strategy-v2-chart-parity.spec.ts` is unchanged (`git diff --quiet -- e2e/strategy-v2-chart-parity.spec.ts` exits 0)
    - The spec throws loud on HTTP >= 400 / blank page (a status check + a visible anchor precede every screenshot)
  </acceptance_criteria>
  <done>A fresh seeded spec proves desktop byte-identity (no-recompute) + 320px legibility/portrait for the in-scope SVG charts; goldens baked desktop-first and committed; dead spec untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the target-size gate — chart tap-rects ≥44px at 320px on the seeded factsheet route</name>
  <files>e2e/target-size.spec.ts</files>
  <read_first>
    - e2e/target-size.spec.ts (the existing scoped-selector + anchor + fail-loud pattern L40-54; the SCOPE/DEFERRAL doc comment)
    - e2e/helpers/reflow.ts (assertTargetSizes L119; MIN_TARGET_PX=44 L27 — do NOT lower)
    - e2e/reflow-sweep-authed.spec.ts (HAS_SEED_ENV + seed + login pattern for the seeded case)
    - e2e/helpers/seed-test-project.ts (seedStrategyWithHistory)
  </read_first>
  <action>
    Add a chart-tap-rect target-size case asserting ≥44px hit areas at 320px on the tap-reveal charts (StreakDistribution / DailyReturnsHeatmap / DailyHeatmap). The charts render on the SEEDED `/strategy/[id]/v2` route, so this case is SEEDED: add a `HAS_SEED_ENV` const + `test.skip(!HAS_SEED_ENV, ...)` and seed via `seedStrategyWithHistory` + login (mirror reflow-sweep-authed), OR add the case to the new svg-chart-parity spec — choose ONE and keep the existing UNSEEDED /security case in target-size.spec.ts intact. Use `assertTargetSizes(page, '<visible chart anchor>', '<scoped pointer-coarse tap-rect selector>')` — scope the selector to the chart tap interaction layer (e.g. a `data-testid` or the `pointer-coarse` hit-rect added in Plan 03). Do NOT lower MIN_TARGET_PX=44; if a target is sub-44 the FIX is in Plan 03's hit-rect, not a weakened gate. Set viewport 320 first; throw loud on HTTP >= 400; anchor on a visible chart element before measuring.
    If this case lands in target-size.spec.ts as a SEEDED test, it must ALSO be FLOW-01 wired into the MA-8 seeded list (Task 3) — confirm placement so it actually runs seeded (the existing /security case stays in the UNSEEDED list at ci.yml:1059).
  </action>
  <verify>
    <automated>grep -q "assertTargetSizes" e2e/target-size.spec.ts && grep -qiE "chart|tap|strategy" e2e/target-size.spec.ts && grep -q 'footer nav\[aria-label="Legal"\]' e2e/target-size.spec.ts && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - A chart-tap-rect ≥44px case exists at 320px scoped to the tap-reveal charts (in target-size.spec.ts OR the svg-chart-parity spec — and that file's CI list entry runs seeded)
    - The existing UNSEEDED /security legal-footer case is unchanged (`grep -c 'aria-label="Legal"'` ≥ 1)
    - `MIN_TARGET_PX` is NOT lowered (`grep -c "MIN_TARGET_PX = 44" e2e/helpers/reflow.ts` = 1; no edit to reflow.ts that weakens it)
    - The seeded chart case has a `HAS_SEED_ENV`/`test.skip` guard and seeds via `seedStrategyWithHistory`; throws loud on HTTP >= 400
  </acceptance_criteria>
  <done>The tap-reveal charts' hit rects are asserted ≥44px at 320px on the seeded route, scoped (not weakened), with the existing /security case intact.</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-01 ci.yml wiring + full-suite + coverage-ratchet gate (prove gates run, not skip)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (MA-8 seeded list L1252-1264 ending `--timeout 60000`; the dead spec is already at L1258; the UNSEEDED list at L1059; the `vars.E2E_TEST_DB_CONFIGURED == 'true'` gate)
    - e2e/svg-chart-parity.spec.ts (the new spec from Task 1)
    - .planning/phases/.../47-PATTERNS.md (FLOW-01 dual-wiring section; "prove it RAN")
    - vitest.config.ts (coverage thresholds lines 82 / stmts 80 / fns 74 / branches 72)
  </read_first>
  <action>
    Add `e2e/svg-chart-parity.spec.ts \` as a NEW line in the MA-8 seeded `npx playwright test \` list (lines 1252-1264), alongside the existing entries (do NOT remove `e2e/strategy-v2-chart-parity.spec.ts` — the dead spec stays skipped). If the seeded chart-tap-rect case (Task 2) lives in target-size.spec.ts, ensure that seeded case runs in the seeded job (target-size.spec.ts is currently in the UNSEEDED list at L1059; a SEEDED chart case inside it self-skips when HAS_SEED_ENV is false in the unseeded run and runs in the seeded job only if also listed there — prefer placing the seeded chart-tap-rect case in the svg-chart-parity spec to avoid a split, OR add target-size.spec.ts to the MA-8 list too; pick the placement that makes the seeded case actually execute and document it inline).
    Run the FULL unit suite + coverage ratchet locally: `npm run test:coverage` must exit 0 (lines ≥82 / stmts ≥80 / fns ≥74 / branches ≥72) — the new useTapPin branches (Plan 01) + viewport conditionals (Plans 02-04) must keep branches ≥72. NEVER lower a threshold or blanket-update a snapshot to go green; add a unit test instead.
    Confirm the frozen guards are green and un-weakened: `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts` (SCENARIO-05) + the BODY-02 + compute.ts parity tests.
    FLOW-01 proof: the new spec must EXECUTE (passed, not skipped) in a real CI run with `vars.E2E_TEST_DB_CONFIGURED == 'true'` — note this in the SUMMARY so the verifier confirms it ran, not just that it exists (the JOURNEY-03 lesson).
  </action>
  <verify>
    <automated>grep -q "svg-chart-parity.spec.ts" .github/workflows/ci.yml && grep -q "strategy-v2-chart-parity.spec.ts" .github/workflows/ci.yml && npm run test:coverage && npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/svg-chart-parity.spec.ts` appears in the MA-8 seeded `npx playwright test` list in ci.yml; the dead `e2e/strategy-v2-chart-parity.spec.ts` line is still present (not removed)
    - `npm run test:coverage` exits 0 with lines ≥82 / stmts ≥80 / fns ≥74 / branches ≥72 (no threshold lowered, no snapshot blanket-updated)
    - `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts` exits 0 (SCENARIO-05 un-weakened); BODY-02 + compute.ts parity green
    - The seeded chart-tap-rect case (Task 2) has a confirmed CI list entry that runs it seeded (documented inline in ci.yml or the spec)
    - SUMMARY notes the FLOW-01 "prove it RAN in CI (not skipped)" follow-up for the verifier
  </acceptance_criteria>
  <done>The new spec is FLOW-01 dual-wired into the MA-8 seeded list; the full suite + coverage ratchet pass un-weakened; SCENARIO-05/BODY-02/compute.ts parity green; the gates are positioned to actually execute in CI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen engine output → committed golden | The desktop golden is the falsifiable record of the engine's rendered output; a diff means the frozen-math boundary was crossed. |
| CI gate config → branch protection | The MA-8 list + coverage ratchet gate merges; a gate that silently skips is a false-green (the FLOW-01 trap). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-05-INTEG | Tampering (data integrity) | desktop chart goldens | mitigate | Desktop goldens baked from byte-identical output; a value/recompute change produces a golden diff → red. NEVER `--update-snapshots` a desktop golden after a tuning change (Pitfall 2). SCENARIO-05/BODY-02/compute.ts parity stay green un-weakened. |
| T-47-05-FALSEGREEN | Repudiation (silent skip) | ci.yml MA-8 list | mitigate | FLOW-01 dual-wiring (HAS_SEED_ENV const + MA-8 list); the spec self-skips ONLY when seed env absent and PROVEN to run (passed, not skipped) in the seeded CI job. The /security anchor + status-fail-loud prevent false-green against a blank page. |
| T-47-05-RATCHET | Tampering (gate weakening) | vitest.config.ts thresholds | mitigate | Coverage ratchet kept un-lowered; new branches covered by Plan-01 unit tests; never lower a threshold to go green. |

> The genuine phase invariant — no chart recomputes/fabricates a value (frozen-math byte-identity) — is asserted HERE via the desktop goldens + the un-weakened SCENARIO-05/BODY-02/compute.ts parity. This is a data-integrity guard, not an external-attacker threat. No new routes/auth/network surface; zero packages installed.
</threat_model>

<verification>
- `npm run test:coverage` exits 0 (ratchet held).
- `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts` green (SCENARIO-05).
- New spec FLOW-01 dual-wired; dead spec untouched; MIN_TARGET_PX un-lowered.
- Desktop goldens committed (baked desktop-first); 320px portrait snapshots present.
- Follow-up for the verifier: confirm the new spec PASSED (not skipped) in a real seeded CI run.
</verification>

<success_criteria>
- Desktop byte-identity goldens (no-recompute proof) + 320px portrait snapshots + ≥44px chart tap-rect gate exist, are FLOW-01 dual-wired, and are positioned to execute in the seeded CI job.
- SCENARIO-05 / BODY-02 / compute.ts parity / coverage ratchet all green and un-weakened.
- The dead Recharts spec is not resurrected (left to Phase 48).
</success_criteria>

<output>
Create `.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-05-SUMMARY.md` when done.
</output>
