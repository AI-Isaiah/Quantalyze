---
phase: 47-hand-rolled-svg-charts-touch-legibility-portrait
plan: 03
type: execute
wave: 2
depends_on: ["47-01"]
files_modified:
  - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx
  - src/app/factsheet/[id]/v2/HeatmapPanels.tsx
  - src/components/charts/DailyHeatmap.tsx
  - src/components/charts/DailyHeatmap.test.tsx
  - src/app/factsheet/[id]/v2/tap-charts-viewport.test.tsx
autonomous: true
requirements: [CHART-01a, CHART-02, CHART-03]
must_haves:
  truths:
    - "On a touch device, tapping a StreakDistribution bar / a DailyReturnsHeatmap cell / a DailyHeatmap cell reveals AND pins the same value the desktop hover/<title> shows"
    - "A re-tap near the pin un-pins; a tap elsewhere moves the pin; the pin survives pointerleave; no auto-dismiss timer"
    - "Each tap target's hit rectangle measures ≥44×44 CSS px under pointer-coarse at 320px"
    - "Desktop hover/<title> behavior + the desktop render are byte-identical (tap logic is touch-only)"
    - "All three charts read every value from the precomputed payload/props — no recompute"
    - "Each tap chart's mobile (isMobile=true) AND desktop (isMobile=false) viewport branch is exercised by a unit/component test in THIS wave (Wave 2), holding the coverage ratchet branches"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/AnalyticalPanels.tsx"
      provides: "StreakDistribution tap-reveal + BootstrapCI legibility/portrait, RCF-wrapped"
      contains: "useTapPin"
    - path: "src/app/factsheet/[id]/v2/HeatmapPanels.tsx"
      provides: "DailyReturnsHeatmap cell tap-reveal (pinned tooltip) + legibility/portrait"
      contains: "useTapPin"
    - path: "src/components/charts/DailyHeatmap.tsx"
      provides: "DailyHeatmap <title>-promoted cell tap-reveal + mobile legibility"
      contains: "useTapPin"
    - path: "src/app/factsheet/[id]/v2/tap-charts-viewport.test.tsx"
      provides: "Wave-2 viewport-branch coverage: renders AnalyticalPanels + HeatmapPanels in isMobile=true AND isMobile=false (mock useBreakpoint) to exercise both branches + assert desktop render unchanged. DailyHeatmap's new branch is covered by its existing DailyHeatmap.test.tsx (extended)."
      contains: "useBreakpoint"
  key_links:
    - from: "AnalyticalPanels.tsx / HeatmapPanels.tsx / DailyHeatmap.tsx"
      to: "useTapPin (Plan 01)"
      via: "consume the shared hook with a per-chart pointerToIndex callback"
      pattern: "useTapPin"
    - from: "tapped element"
      to: "precomputed prop array at selectedIdx"
      via: "value lookup (the same value the desktop tooltip/<title> shows)"
      pattern: "selectedIdx"
    - from: "tap-charts-viewport.test.tsx"
      to: "AnalyticalPanels + HeatmapPanels"
      via: "render both isMobile branches by mocking useBreakpoint"
      pattern: "vi.mock"
---

<objective>
Bring the THREE charts that have a real desktop value-reveal today (the binding parity gate) to touch
tap-reveal/pin parity (CHART-01a) via the shared `useTapPin` hook from Plan 01, plus their legibility
(CHART-02) and portrait (CHART-03) tuning: StreakDistributionPanel (`<title>` per bar, AnalyticalPanels),
DailyReturnsHeatmap (JS `onPointerMove`→floating tooltip, HeatmapPanels), and DailyHeatmap (per-cell
`<title>`, components/charts). AnalyticalPanels' BootstrapCIPanel (no hover) gets legibility/portrait only
in the same file. Each chart provides its own `pointerToIndex` mapping and renders the pinned reveal using
its EXISTING tooltip/`<title>` content (no new accent surface). The desktop hover path and the desktop
render stay byte-identical — tap logic is touch-only. A Wave-2 viewport-branch test renders the two
panel files (AnalyticalPanels, HeatmapPanels) in BOTH isMobile branches, and the DailyHeatmap branch is
covered by extending its existing test — so the new viewport conditionals are branch-covered in THIS wave
(the coverage ratchet, branches ≥72, is a BLOCKING CI gate — the Plan 05 gate is too late to fix cheaply).

Purpose: A tap reveals (and pins) the value hover gives on desktop, with ≥44px coarse hit targets, on the
exact charts that already disclose a value — without inventing UI on no-hover charts — while the new
viewport branches stay covered in-wave.
Output: 3 edited files consuming `useTapPin` + 1 new viewport-branch test; ≥44px tap rects; desktop
byte-identical; no recompute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-RESEARCH.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-PATTERNS.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-UI-SPEC.md
@src/hooks/useTapPin.ts

<interfaces>
<!-- The hook contract (Plan 01) every tap chart consumes, the existing desktop-reveal sites to promote, and the two locked primitives. -->

From src/hooks/useTapPin.ts (Plan 01 — consume, do not re-derive):
- `useTapPin({ count, pointerToIndex })` → `{ selectedIdx, pinned, svgRef, onPointerDown, onPointerMove, onPointerUp, onPointerLeave }`
- The chart supplies `pointerToIndex(clientX, clientY, rect): number | null` and renders its reveal from `selectedIdx`. Constants (slop 8px, 350ms, re-tap 3) live in the hook.

EXISTING desktop value-reveal sites to promote (do NOT remove the desktop path):
- AnalyticalPanels.tsx:98-100 — per-bar `<title>` `Length {i+1}{...}: {c} streak{...}` (StreakDistribution)
- HeatmapPanels.tsx:335-361 — `onPointerMove` cell-lookup → `setHovered({cx,cy,iso,v})`; tooltip div L417-436; canvas ResizeObserver `scale` L267-279 (it MAY keep its measure — locked exception)
- DailyHeatmap.tsx:195 — per-cell `<title>` `"2024-03-15: 0.00%"` (asserted by DailyHeatmap.test.tsx:187-194)

From src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts:
- Wrap each root svg in `<ResponsiveChartFrame width height role aria-label>`; `useBreakpoint() === "mobile"` gates font/tick/viewBox. Desktop branch = today's literals (Pitfall 2).
- UNIT-TEST seams (zero net-new deps): `vi.mock("@/hooks/useBreakpoint", () => ({ useBreakpoint: vi.fn() }))` + `.mockReturnValue("mobile" | "desktop")`, OR drive the real hook via `installMatchMedia({...})` (src/hooks/useBreakpoint.test.ts:40-59).

Pointer→cell mapping pattern (generalize the existing onPointerMove cell-lookup as the hook's pointerToIndex):
  function pointerToCell(clientX, clientY, rect) {
    const vbX = ((clientX - rect.left)/rect.width)*VB_W; const vbY = ((clientY - rect.top)/rect.height)*VB_H;
    const col = Math.floor((vbX - LEFT_GUTTER)/CELL_W); const row = Math.floor((vbY - TOP_GUTTER)/CELL_H);
    return inBounds ? row*COLS + col : null;  // value read from precomputed prop array
  }

Coarse hit-target: the tap target's hit rectangle must measure ≥44×44 CSS px under pointer-coarse: ONLY (an invisible/expanded interaction <rect> or padded layer), NOT a change to the visual cell/bar size and NOT on pointer-fine.

Test analogs (reuse — do NOT invent a harness): src/components/charts/DailyHeatmap.test.tsx (props-only render + structural assertions; this is ALSO the file to extend for DailyHeatmap's new branch), src/components/ResponsiveChartFrame.test.tsx (jsdom env + exact-attribute assertions).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AnalyticalPanels — StreakDistribution tap-reveal via useTapPin + BootstrapCI legibility/portrait</name>
  <files>src/app/factsheet/[id]/v2/AnalyticalPanels.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx (StreakDistributionPanel L19; the inline svg recipe L55-60; the per-bar `<title>` desktop reveal L98-100; BootstrapCIPanel L213; CalmarByYearPanel L156 is an HTML table — OUT of scope)
    - src/hooks/useTapPin.ts (Plan 01 hook contract)
    - src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts + src/components/charts/chart-tokens.ts
  </read_first>
  <action>
    StreakDistributionPanel (440×200, fontSize 9, 2 side-by-side histograms): wrap the root `<svg>` in `<ResponsiveChartFrame width={440} height={vbH} role="img" aria-label={...}>` with `ref={svgRef}` + the hook's pointer handlers. Consume `useTapPin({ count: bars.length, pointerToIndex })` where `pointerToIndex` maps clientX→bar index (generalize the existing per-bar x positions). On `selectedIdx != null`, render a pinned reveal showing the SAME text the per-bar `<title>` shows (`Length {i+1}{maxLen?"+"}: {c} streak{...}`) — reuse the existing copy, no new format/accent. KEEP the desktop `<title>` elements (mouse path unchanged). Add a `pointer-coarse:`-only invisible/expanded interaction `<rect>` per bar (or a single overlay) so each tap target's hit rect ≥44×44 CSS px at 320px — do NOT enlarge the visible bar.
    Legibility/portrait (both panels): `const isMobile = useBreakpoint() === "mobile"`; mobile font-bump from 9 (target effective ≥~12px; 440-wide → ~5.9px today) + fewer x-ticks + taller mobile VB_H; DESKTOP branch returns today's exact fontSize/tick/VB_H. BootstrapCIPanel (340×36, no hover) = legibility + portrait ONLY, no tap-reveal.
    Read all values from precomputed props; hex via chart-tokens; never container-opacity.
  </action>
  <verify>
    <automated>grep -q "useTapPin" src/app/factsheet/[id]/v2/AnalyticalPanels.tsx && grep -q "ResponsiveChartFrame" src/app/factsheet/[id]/v2/AnalyticalPanels.tsx && grep -q "useBreakpoint" src/app/factsheet/[id]/v2/AnalyticalPanels.tsx && grep -q "<title>" src/app/factsheet/[id]/v2/AnalyticalPanels.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File imports and calls `useTapPin` (StreakDistribution) and `useBreakpoint`, and renders `<ResponsiveChartFrame`
    - The per-bar desktop `<title>` elements still exist (`grep -c "<title>"` ≥ 1 — desktop hover path preserved)
    - A `pointer-coarse` interaction layer/rect provides ≥44px hit targets (a `pointer-coarse:` class or an explicit ≥44 hit-rect width/height appears) and is NOT applied on pointer-fine
    - The pinned reveal text reuses the existing `Length ...: ... streak` copy (no new string format introduced)
    - The tap/pin path is touch-only (only fires via the hook, whose guard is `type === "touch"`); desktop branch values equal today's literals; `npx tsc --noEmit` 0 errors in this file
  </acceptance_criteria>
  <done>StreakDistribution is tap-revealable/pinnable on touch with ≥44px hit targets reusing the desktop `<title>` value; BootstrapCI is legible+portrait-tuned; desktop hover + desktop render byte-identical; no recompute.</done>
</task>

<task type="auto">
  <name>Task 2: HeatmapPanels — DailyReturnsHeatmap cell tap-reveal via useTapPin + legibility/portrait</name>
  <files>src/app/factsheet/[id]/v2/HeatmapPanels.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/HeatmapPanels.tsx (DailyReturnsHeatmap L149; onPointerMove cell-lookup L335-361; floating tooltip div L417-436; canvas ResizeObserver `scale` L262/L267-279; MonthlyReturnsHeatmap L35 is an HTML table — OUT of scope)
    - src/hooks/useTapPin.ts (Plan 01 hook contract)
    - src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts + src/components/charts/chart-tokens.ts
  </read_first>
  <action>
    DailyReturnsHeatmap (canvas-measured `scale` state, fontSize 8-9). It already measures via ResizeObserver — KEEP its existing measure (locked exception). Consume `useTapPin({ count: cellCount, pointerToIndex })` where `pointerToIndex` reuses the EXISTING cell-lookup math at L335-361 (the same `(clientX-rect.left)/scale` → wk/d derivation) to return a flat cell index, returning null off-grid. On a touch tap, set the SAME `hovered` content (`{cx, cy, iso, v}`) the desktop onPointerMove sets and PIN it (render the existing tooltip div L417-436 from the pinned state). KEEP the desktop `onPointerMove` hover path for mouse. Re-tap toggles the pin, tap moves it, pin survives pointerleave — all via the hook.
    Provide a `pointer-coarse:`-only ≥44×44 hit area for the tap (cells are tiny; use an expanded interaction layer mapping the tap to the nearest cell), NOT a visual cell resize.
    Legibility: mobile-gate label fontSize bump from 8-9 (target effective ≥~12px) via `useBreakpoint`; portrait: taller mobile container/viewBox if applicable. Desktop branch returns today's literals.
    Read the cell value from the precomputed payload (`year.cells[wk][d]`) — NEVER recompute. Hex via chart-tokens; never container-opacity a baked cell.
  </action>
  <verify>
    <automated>grep -q "useTapPin" src/app/factsheet/[id]/v2/HeatmapPanels.tsx && grep -q "useBreakpoint" src/app/factsheet/[id]/v2/HeatmapPanels.tsx && grep -q "onPointerMove" src/app/factsheet/[id]/v2/HeatmapPanels.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File imports and calls `useTapPin` and `useBreakpoint`
    - The desktop `onPointerMove` hover→`setHovered` path still exists (`grep -c "onPointerMove"` ≥ 1 — mouse hover preserved)
    - The pinned reveal reuses the existing tooltip div + `{iso, v}` content (no new tooltip format/accent)
    - The cell value is read from the precomputed `year.cells[...]` (no new computation of the cell value)
    - A pointer-coarse ≥44px tap area exists; tap/pin is touch-only via the hook; `npx tsc --noEmit` 0 errors in this file
  </acceptance_criteria>
  <done>DailyReturnsHeatmap cells are tap-revealable/pinnable on touch (≥44px) reusing the existing floating-tooltip value; desktop hover + desktop render byte-identical; legible at mobile; no recompute.</done>
</task>

<task type="auto">
  <name>Task 3: DailyHeatmap (components/charts) — promote per-cell <title> to a tappable pinned reveal + mobile legibility (extend existing test)</name>
  <files>src/components/charts/DailyHeatmap.tsx, src/components/charts/DailyHeatmap.test.tsx</files>
  <read_first>
    - src/components/charts/DailyHeatmap.tsx (per-cell `<title>` L195 `"2024-03-15: 0.00%"`; SVG branch vs canvas branch `SVG_THRESHOLD_CELLS=365`; existing canvas measure)
    - src/components/charts/DailyHeatmap.test.tsx (KEEP GREEN + EXTEND: baked-tint fills, `fill-opacity` null L168-185/235-260, save/restore/clearRect ordering, font-load gate, `<title>` format L187-194)
    - tests/visual/strategy-v2-type-scale.test.ts (DailyHeatmap.tsx IS in the 6-file lint list — keep className strings in the allowed set; SVG numeric fontSize props are unaffected)
    - src/hooks/useTapPin.ts + src/hooks/useBreakpoint.ts + src/components/charts/chart-tokens.ts
  </read_first>
  <action>
    Promote the per-cell `<title>` value to a tappable/pinnable reveal on the SVG branch via `useTapPin({ count: cellCount, pointerToIndex })`. `pointerToIndex` maps clientX/clientY→cell index over the rendered grid; the pinned reveal shows the SAME `"{ISO}: {pct}%"` text the `<title>` shows (reuse the format asserted by DailyHeatmap.test.tsx:187-194). KEEP the `<title>` elements (desktop/AT path) and KEEP the canvas branch + its existing measure.
    Legibility: fontSize is already at the 12 floor — the real mobile fix is the existing horizontal scroll region + larger labels at `useBreakpoint() === "mobile"`, NOT a taller viewBox (RESEARCH). Do NOT add a row/col drop. Provide a pointer-coarse ≥44px tap area without resizing visible cells.
    CRITICAL: keep ALL existing DailyHeatmap.test.tsx assertions green — do NOT introduce `fill-opacity` on cells (the 138-violation regression); use baked ramp hex from chart-tokens directly. Do NOT alter className strings outside the type-scale lint's allowed set.
    EXTEND DailyHeatmap.test.tsx (same-wave branch coverage): mock `useBreakpoint` (`vi.mock("@/hooks/useBreakpoint", () => ({ useBreakpoint: vi.fn() }))`) and render the SVG-branch fixture (data.length ≤ 365) with the hook forced to BOTH `"mobile"` and `"desktop"` so the new `isMobile` label/scroll-region branch is exercised both ways. Assert the desktop render keeps today's `<title>` format + the existing baked-tint fills (byte-identity), and the mobile render still emits the SAME cell count (no row/col drop). Add a small touch-tap case that drives the hook's pointer handlers (synthetic touch pointerdown→up within slop/time) and asserts the pinned reveal text equals the `<title>` format. Reuse the existing harness/fixtures in the file — do NOT add a new dependency.
    Read the cell value from the precomputed prop array — NEVER recompute.
  </action>
  <verify>
    <automated>npx vitest run src/components/charts/DailyHeatmap.test.tsx && npx vitest run tests/visual/strategy-v2-type-scale.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/charts/DailyHeatmap.test.tsx` exits 0 (all existing assertions still green — no fill-opacity, `<title>` format intact, canvas ordering intact)
    - DailyHeatmap.test.tsx now renders the SVG branch with `useBreakpoint` forced to BOTH `"mobile"` and `"desktop"` (the new viewport branch covered in-wave) — `grep -c '"mobile"' src/components/charts/DailyHeatmap.test.tsx` ≥ 1 AND `grep -c '"desktop"' ...` ≥ 1
    - A touch-tap case asserts the pinned reveal reuses the `"{ISO}: {pct}%"` `<title>` format (no new string); a mobile-vs-desktop assertion proves the same cell count (no row/col drop)
    - `npx vitest run tests/visual/strategy-v2-type-scale.test.ts` exits 0 (className strings stayed in the allowed set)
    - File imports and calls `useTapPin` and `useBreakpoint`; the per-cell `<title>` elements still exist; `npx tsc --noEmit` 0 errors in this file; no new npm dependency added
  </acceptance_criteria>
  <done>DailyHeatmap cells are tap-revealable/pinnable reusing the `<title>` value; existing DailyHeatmap + type-scale tests stay green AND the new isMobile branch + tap path are covered in-wave via the extended test; mobile labels legible via the scroll region; no recompute, no fill-opacity, no cell drop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Wave-2 viewport-branch coverage test — render AnalyticalPanels + HeatmapPanels in BOTH isMobile branches (hold the ratchet)</name>
  <files>src/app/factsheet/[id]/v2/tap-charts-viewport.test.tsx</files>
  <read_first>
    - src/components/charts/DailyHeatmap.test.tsx (the props-only render + structural-assertion analog — reuse the harness shape; jsdom env)
    - src/components/ResponsiveChartFrame.test.tsx (exact-attribute / byte-identity assertion style)
    - src/hooks/useBreakpoint.test.ts (the `installMatchMedia` matchMedia stub at L40-59 — fallback seam)
    - src/app/factsheet/[id]/v2/AnalyticalPanels.tsx + src/app/factsheet/[id]/v2/HeatmapPanels.tsx (the StreakDistribution + DailyReturnsHeatmap export signatures + minimal precomputed prop shapes)
    - src/hooks/useTapPin.ts (the hook handlers, to drive a synthetic touch tap if asserting the pinned reveal)
  </read_first>
  <behavior>
    - StreakDistributionPanel + BootstrapCIPanel (AnalyticalPanels) and DailyReturnsHeatmap (HeatmapPanels) each render WITHOUT throwing when `useBreakpoint()` returns `"mobile"` (isMobile=true branch covered) AND when it returns `"desktop"` (isMobile=false branch covered).
    - Desktop branch byte-identity: in the desktop render, the StreakDistribution `ResponsiveChartFrame` svg emits `viewBox="0 0 440 {desktopH}"` equal to today's literal (mutating the desktop VB_H literal makes this FAIL).
    - Mobile vs desktop differ: the mobile render's viewBox height and/or tick-label count differs from desktop (proves the conditional is live).
    - (StreakDistribution) a synthetic TOUCH tap (pointerdown→pointerup within slop/time via the hook handlers) pins a reveal whose text equals the existing `Length ...: ... streak` copy — exercising the tap path branch in-wave; desktop mouse render does NOT show the pinned reveal by default.
  </behavior>
  <action>
    Author `src/app/factsheet/[id]/v2/tap-charts-viewport.test.tsx` with `/** @vitest-environment jsdom */`. Mock the breakpoint seam (`vi.mock("@/hooks/useBreakpoint", () => ({ useBreakpoint: vi.fn() }))` + `.mockReturnValue` per render, or `installMatchMedia` if the relative import resolves cleaner). Render StreakDistributionPanel + BootstrapCIPanel (AnalyticalPanels) and DailyReturnsHeatmap (HeatmapPanels) twice each (mobile, desktop) with a minimal deterministic precomputed prop fixture (hand-author tiny arrays — do NOT compute real metrics). Assert the behaviors above: both branches render, desktop viewBox equals today's literal (byte-identity), mobile differs. Add ONE synthetic-touch-tap case on StreakDistribution proving the pinned reveal reuses the existing `<title>` copy (drives the useTapPin path so its mobile branch is covered here too, complementing Plan 01's hook unit tests). Keep it ONE combined test file (do NOT over-engineer per-function suites). Zero net-new npm deps — `vitest` + `@testing-library/react` only.
    NOTE: DailyHeatmap's new isMobile branch is covered by the EXTENDED src/components/charts/DailyHeatmap.test.tsx (Task 3), not here — do not duplicate it.
    CRITICAL: this test must exercise BOTH arms of every new `isMobile ? a : b` conditional in AnalyticalPanels + HeatmapPanels so the branch ratchet (branches ≥72, BLOCKING) holds in THIS wave — not at the Plan 05 gate.
  </action>
  <verify>
    <automated>npx vitest run src/app/factsheet/[id]/v2/tap-charts-viewport.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/app/factsheet/[id]/v2/tap-charts-viewport.test.tsx` exits 0
    - The test renders AnalyticalPanels' StreakDistribution + BootstrapCI and HeatmapPanels' DailyReturnsHeatmap with `useBreakpoint` forced to BOTH `"mobile"` and `"desktop"` — `grep -c '"mobile"' ...` ≥ 1 AND `grep -c '"desktop"' ...` ≥ 1
    - At least one assertion pins a DESKTOP `viewBox` to today's literal (mutating a desktop VB_H literal makes the test FAIL — falsifiable byte-identity)
    - A synthetic-touch-tap case asserts the StreakDistribution pinned reveal reuses the existing `Length ...: ... streak` copy
    - No new npm dependency added; the file uses only `vitest` + `@testing-library/react`
  </acceptance_criteria>
  <done>A single Wave-2 test renders AnalyticalPanels + HeatmapPanels in both isMobile branches and drives the StreakDistribution tap path, holding the branch-coverage ratchet in-wave and doubling as a falsifiable desktop byte-identity assertion (DailyHeatmap's branch covered by its extended test).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser touch input → pinned reveal | Finger tap coordinates map (via the chart's pointerToIndex) to a cell/bar index, clamped in the hook; the reveal reads a precomputed value. |
| precomputed payload → svg/tooltip render | Frozen-engine cell/bar values cross into the render + reveal; read-only, never recomputed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03-OOB | Tampering | tap→cell/bar index mapping | mitigate | `pointerToIndex` returns null off-grid; the hook clamps to `[0, count-1]`. Pinned reveal reads `payload[selectedIdx]` only within bounds. No OOB. |
| T-47-03-INTEG | Tampering (data integrity) | the 3 tap charts | mitigate | Tap reveal reuses the EXISTING desktop value (`<title>`/`hovered` content) read from the precomputed payload; no series/metric/domain recompute. Desktop branch byte-identical → desktop golden unchanged (Plan 05); the Wave-2 viewport test + extended DailyHeatmap test pin desktop literals so a drift fails IN-WAVE. |
| T-47-03-XSS | Tampering / XSS | reveal `<text>`/tooltip `<div>` | accept | React auto-escapes the inserted value text; no `dangerouslySetInnerHTML`. Existing pattern, no new risk. |

> Presentation-only retrofit; the only new input is synthetic pointer coordinates, clamped to chart bounds. No new auth/network/data-write surface. Frozen-math byte-identity is the genuine invariant, asserted in Plan 05 and pinned in-wave by the Task 3/4 desktop-literal assertions.
</threat_model>

<verification>
- `npx vitest run src/components/charts/DailyHeatmap.test.tsx` + `tests/visual/strategy-v2-type-scale.test.ts` green.
- `npx vitest run src/app/factsheet/[id]/v2/tap-charts-viewport.test.tsx` green — both isMobile branches exercised for AnalyticalPanels + HeatmapPanels; StreakDistribution tap path driven.
- All 3 files consume `useTapPin`; desktop hover/`<title>` paths preserved; desktop branch = today's literals.
- DESKTOP byte-identity ALSO asserted in Plan 05; SCENARIO-05/BODY-02/compute.ts parity stay green.
</verification>

<success_criteria>
- The 3 charts with a real desktop value-reveal are tap-revealable/pinnable on touch (re-tap toggles, pin survives leave, no timer), each with ≥44px coarse hit targets, reusing the existing desktop value.
- Desktop hover behavior + desktop render byte-identical; mobile legible.
- The new viewport conditionals are branch-covered IN THIS WAVE (ratchet held); no interaction invented on no-hover charts; no value recomputed; no cell/row/col dropped.
</success_criteria>

<output>
Create `.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-03-SUMMARY.md` when done.
</output>
</content>
