---
phase: 47-hand-rolled-svg-charts-touch-legibility-portrait
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/charts/ReturnQuantiles.tsx
  - src/components/charts/ReturnQuantiles.test.tsx
  - src/components/charts/Sparkline.tsx
  - src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx
  - src/app/(dashboard)/allocations/components/MonteCarloBandChart.test.tsx
autonomous: true
requirements: [CHART-02, CHART-03]
must_haves:
  truths:
    - "ReturnQuantiles is wrapped in ResponsiveChartFrame and legible at 320px (effective ≥~12px); portrait-tuned"
    - "MonteCarloBandChart is legible at 320px (mobile font bump) and stays role=img with NO tabIndex/interaction"
    - "MonteCarloBandChart's desktop render is byte-identical, proven by a Vitest component snapshot (the seeded route yields 0 positions so it never renders in e2e)"
    - "Sparkline is explicitly handled (likely NO-OP — no text, tiny inline) so the chart set reconciles with no phantom task"
    - "None of the three recomputes a series/metric/domain"
    - "ReturnQuantiles' AND MonteCarloBandChart's mobile (isMobile=true) AND desktop (isMobile=false) viewport branch is exercised by a unit test in THIS wave (Wave 1), holding the coverage ratchet branches"
  artifacts:
    - path: "src/components/charts/ReturnQuantiles.tsx"
      provides: "RCF-wrapped, mobile-legible box plot"
      contains: "ResponsiveChartFrame"
    - path: "src/components/charts/ReturnQuantiles.test.tsx"
      provides: "Extended to render BOTH isMobile branches (mock useBreakpoint) + assert desktop render unchanged"
      contains: "useBreakpoint"
    - path: "src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx"
      provides: "Mobile-legible band chart, role=img preserved"
      contains: "useBreakpoint"
    - path: "src/app/(dashboard)/allocations/components/MonteCarloBandChart.test.tsx"
      provides: "Vitest component snapshot of the rendered SVG (desktop byte-identity + role=img) + both isMobile branches rendered"
      contains: "MonteCarloBandChart"
  key_links:
    - from: "ReturnQuantiles.tsx / MonteCarloBandChart.tsx"
      to: "useBreakpoint()"
      via: "mobile-gated font/viewBox selection"
      pattern: "useBreakpoint"
    - from: "ReturnQuantiles.test.tsx / MonteCarloBandChart.test.tsx"
      to: "the tuned chart's mobile AND desktop branch"
      via: "render both branches by mocking useBreakpoint"
      pattern: "vi.mock"
---

<objective>
Bring the standalone (non-factsheet-panel) hand-rolled SVG charts to legibility (CHART-02) + portrait
(CHART-03): ReturnQuantiles (box plot, currently raw `<svg className="w-full">` — wrap in
`ResponsiveChartFrame`), Sparkline (120×32, no text — likely a documented NO-OP), and MonteCarloBandChart
(allocations band chart, `role="img"` by design — mobile font bump only, NO interaction). Because the
seeded allocator route renders 0 positions (MonteCarloBandChart never appears on the seeded e2e route),
prove its desktop byte-identity via a Vitest component snapshot rather than a Playwright golden (Pitfall 4).
Both ReturnQuantiles' and MonteCarloBandChart's new `isMobile` viewport branches are rendered in BOTH arms
by their (extended/new) Vitest tests so the coverage ratchet (branches ≥72, a BLOCKING CI gate) holds in
THIS wave — not at the Plan 05 gate.

Purpose: Close the legibility/portrait work for the charts that mount outside the factsheet panel route,
without inventing interaction (all three are no-hover), without breaking the role=img a11y contract, and
keeping the branch ratchet green in-wave.
Output: ReturnQuantiles + MonteCarloBandChart tuned with both-branch tests; Sparkline dispositioned; MCB
unit snapshot added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-RESEARCH.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-PATTERNS.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-UI-SPEC.md

<interfaces>
<!-- The locked primitives + the MonteCarloBandChart a11y contract + the unit-snapshot analog. -->

From src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts:
- Wrap each root svg in `<ResponsiveChartFrame width height role aria-label>`; `useBreakpoint() === "mobile"` gates font/viewBox; desktop branch = today's literals (Pitfall 2).
- UNIT-TEST seams (zero net-new deps): `vi.mock("@/hooks/useBreakpoint", () => ({ useBreakpoint: vi.fn() }))` + `.mockReturnValue("mobile" | "desktop")`, OR drive the real hook via `installMatchMedia({...})` (src/hooks/useBreakpoint.test.ts:40-59).

MonteCarloBandChart (src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx, 132 LOC):
- `role="img"` by DESIGN — deliberately non-interactive (header L18-22 records the empty-focus-stop / Recharts accessibilityLayer regression DESIGN.md pins against, 2026-04-30). Do NOT add `tabIndex`/pointer handlers/interaction.
- props-only `bands: MonteCarloBandPoint[]`; imports hex from @/components/charts/chart-tokens. fontSize already 12; 600-wide → ~5.8px effective at 320px → mobile bump ~18-20.

Unit-snapshot analog: src/components/charts/DailyHeatmap.test.tsx (props-only render + structural assertions). Use a deterministic synthetic `MonteCarloBandPoint[]` and assert the rendered SVG structure (role=img, the band path elements, fontSize).

From REQUIREMENTS.md: a net-new charting library / UI kit / CSS framework is an out-of-scope anti-feature. Install nothing.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReturnQuantiles + Sparkline — RCF wrap + mobile legibility (Sparkline dispositioned) + extend ReturnQuantiles test for BOTH isMobile branches</name>
  <files>src/components/charts/ReturnQuantiles.tsx, src/components/charts/ReturnQuantiles.test.tsx, src/components/charts/Sparkline.tsx</files>
  <read_first>
    - src/components/charts/ReturnQuantiles.tsx (101 LOC; raw `<svg viewBox className="w-full">` 600×200, fontSize 10-11; NO hover)
    - src/components/charts/ReturnQuantiles.test.tsx (KEEP GREEN + EXTEND for the new isMobile branch)
    - src/components/charts/Sparkline.tsx (74 LOC; raw `<svg width height>` 120×32; NO text, NO hover — likely NO-OP per RESEARCH Open Question 2)
    - src/components/ResponsiveChartFrame.tsx + src/hooks/useBreakpoint.ts + src/hooks/useBreakpoint.test.ts (the matchMedia/mock seams)
  </read_first>
  <action>
    ReturnQuantiles (no hover → legibility + portrait ONLY): wrap its raw `<svg viewBox className="w-full">` in `<ResponsiveChartFrame width={600} height={vbH} role="img" aria-label={...}>` (keep existing aria text verbatim); add `const isMobile = useBreakpoint() === "mobile"`; mobile-bump fontSize from 10-11 (600-wide → ~4.8-5.3px today, target effective ≥~12px) + fewer tick labels + taller mobile VB_H; DESKTOP branch returns today's exact fontSize/VB_H. Read all values from precomputed props; hex via chart-tokens.
    EXTEND ReturnQuantiles.test.tsx (same-wave branch coverage): mock `useBreakpoint` and render the chart with the hook forced to BOTH `"mobile"` and `"desktop"` so the new `isMobile` conditional is exercised both ways. Assert the desktop render keeps today's literal viewBox/fontSize (byte-identity — mutating a desktop literal makes it FAIL) and the mobile render differs (taller viewBox and/or fewer ticks). Reuse the existing fixtures/harness in the file. Keep every pre-existing assertion green. No new dependency.
    Sparkline: read the file and CONFIRM the no-text/tiny-inline disposition. If it has no axis/label text and no hover (expected), make it an explicit documented NO-OP — add a short `// Phase 47: legibility/portrait N/A — 120×32 decorative inline sparkline, no text, no hover (RESEARCH Open Question 2)` comment and leave it functionally unchanged (NO isMobile conditional → no new branch → no test needed). If (unexpectedly) it has label text, apply the same mobile font-bump pattern AND add a both-branch render assertion to a Sparkline test. Do NOT invent interaction.
  </action>
  <verify>
    <automated>grep -q "ResponsiveChartFrame" src/components/charts/ReturnQuantiles.tsx && grep -q "useBreakpoint" src/components/charts/ReturnQuantiles.tsx && grep -q "useBreakpoint" src/components/charts/ReturnQuantiles.test.tsx && npx vitest run src/components/charts/ReturnQuantiles.test.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `ReturnQuantiles.tsx` imports/calls `useBreakpoint` and renders `<ResponsiveChartFrame` (no longer a raw `<svg className="w-full">`)
    - `ReturnQuantiles.test.tsx` renders the chart with `useBreakpoint` forced to BOTH `"mobile"` and `"desktop"` (`grep -c '"mobile"' ...` ≥ 1 AND `grep -c '"desktop"' ...` ≥ 1) and pins the desktop viewBox/fontSize literal (mutating it FAILS the test)
    - `npx vitest run src/components/charts/ReturnQuantiles.test.tsx` exits 0 (existing assertions + the new both-branch assertions all green)
    - ReturnQuantiles adds NO `useTapPin`/`tabIndex`/pointer handlers (no desktop hover → no interaction)
    - `Sparkline.tsx` either carries the explicit NO-OP disposition comment (no new branch) OR (if it has text) applies the mobile font-bump WITH a both-branch test — and the chart set reconciles with no untouched in-scope file
    - `npx tsc --noEmit` 0 errors in the edited files; no new npm dependency added
  </acceptance_criteria>
  <done>ReturnQuantiles is RCF-wrapped + mobile-legible + portrait-tuned with its test extended to cover BOTH isMobile branches in-wave; Sparkline is explicitly dispositioned; no interaction invented.</done>
</task>

<task type="auto">
  <name>Task 2: MonteCarloBandChart — mobile legibility (role=img preserved) + Vitest desktop-byte-identity snapshot rendering BOTH isMobile branches</name>
  <files>src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx, src/app/(dashboard)/allocations/components/MonteCarloBandChart.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx (132 LOC, role=img L18-20, 600×240 fontSize 12, props-only `bands: MonteCarloBandPoint[]`, header L18-22 a11y rationale)
    - src/components/charts/DailyHeatmap.test.tsx (the props-only render + structural-assertion analog for the new test)
    - src/hooks/useBreakpoint.test.ts (the `installMatchMedia`/mock seam to force mobile vs desktop)
    - DESIGN.md (accessibilityLayer / empty-focus-stop rule — do NOT add tabIndex to role=img charts)
    - src/hooks/useBreakpoint.ts + src/components/charts/chart-tokens.ts
  </read_first>
  <action>
    MonteCarloBandChart (no hover, role=img by design → legibility ONLY, NO interaction): add `const isMobile = useBreakpoint() === "mobile"`; mobile-bump fontSize from 12 to ~18-20 (600-wide → ~5.8px effective today, target ≥~12px) + reduce tick labels if needed; DESKTOP branch returns today's exact fontSize (12) and VB dims. KEEP `role="img"` + its aria-label; do NOT add `tabIndex`, pointer handlers, or any focusable interaction (re-introduces the empty-focus-stop regression DESIGN.md pins against). Hex via chart-tokens; read bands from props — no recompute. Optionally wrap the root svg in ResponsiveChartFrame for SoT (preserve role=img + aria-label).
    NEW TEST MonteCarloBandChart.test.tsx (analog DailyHeatmap.test.tsx): mock `useBreakpoint`. Render with a deterministic synthetic `MonteCarloBandPoint[]` in BOTH branches:
      - DESKTOP (`useBreakpoint → "desktop"`): assert (a) the root has `role="img"` and NO `tabIndex`, (b) the band/fan path elements render, (c) the desktop fontSize is 12 (byte-identity of the desktop branch — the no-recompute proof for MCB since it never renders on the seeded e2e route, Pitfall 4).
      - MOBILE (`useBreakpoint → "mobile"`): assert the chart still renders, still has `role="img"` + NO `tabIndex`, and the mobile fontSize is the bumped value (~18-20) — proving the isMobile branch is live and exercised in-wave (the ratchet branch).
    Use `@testing-library/react` + a jsdom environment. This guards the desktop byte-identity AND covers both viewport branches in CI without Playwright. No new dependency.
  </action>
  <verify>
    <automated>grep -q "useBreakpoint" src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx && grep -q 'role="img"' src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx && ! grep -q "tabIndex" src/app/(dashboard)/allocations/components/MonteCarloBandChart.tsx && npx vitest run src/app/(dashboard)/allocations/components/MonteCarloBandChart.test.tsx && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - MonteCarloBandChart.tsx calls `useBreakpoint`, keeps `role="img"`, and contains NO `tabIndex` / pointer handlers / `useTapPin`
    - `npx vitest run .../MonteCarloBandChart.test.tsx` exits 0; the test renders with `useBreakpoint` forced to BOTH `"desktop"` and `"mobile"` and asserts role=img + absence of tabIndex in both, desktop fontSize=12, mobile fontSize=~18-20
    - Mutating the desktop fontSize literal (12 → 13) makes the desktop assertion FAIL (falsifiable desktop byte-identity), then revert
    - Mobile fontSize is `isMobile ? ~18-20 : 12`; bands read from props (no recompute); `npx tsc --noEmit` 0 errors in both files; no new npm dependency added
  </acceptance_criteria>
  <done>MonteCarloBandChart is mobile-legible with role=img and zero interaction; a Vitest component test proves its desktop render is byte-identical AND exercises both isMobile branches in-wave (the no-recompute proof + ratchet branch for the chart that can't render on the seeded e2e route).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| precomputed bands/quantile props → svg render | Frozen-engine output crosses into render; read-only, never recomputed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-04-INTEG | Tampering (data integrity) | ReturnQuantiles / MonteCarloBandChart | mitigate | Desktop branch returns today's literals (fontSize 12 for MCB) — proven byte-identical by the MCB Vitest test + the extended ReturnQuantiles test (both pin the desktop literal in-wave) and the ReturnQuantiles desktop golden (Plan 05). Values read from props; no recompute. |
| T-47-04-A11Y | (a11y, not security) | MonteCarloBandChart | mitigate | Keep `role="img"`, add NO `tabIndex`/interaction (avoids the empty-focus-stop regression DESIGN.md records). Test asserts absence of tabIndex in BOTH viewport branches. |
| T-47-04-XSS | Tampering / XSS | svg `<text>` labels | accept | React auto-escapes; no `dangerouslySetInnerHTML`. Existing pattern, no new risk. |

> Presentation-only retrofit; no new inputs, auth, network, or data-write surface. Zero packages installed. Frozen-math byte-identity is the genuine invariant — asserted here by the MCB unit test + the extended ReturnQuantiles test, and in Plan 05 by the desktop goldens.
</threat_model>

<verification>
- `npx vitest run src/components/charts/ReturnQuantiles.test.tsx` + `.../MonteCarloBandChart.test.tsx` green — both render BOTH isMobile branches and pin the desktop literal.
- MonteCarloBandChart keeps role=img and has no tabIndex/interaction (in both branches).
- ReturnQuantiles RCF-wrapped; Sparkline dispositioned; no recompute; no new npm dependency.
</verification>

<success_criteria>
- ReturnQuantiles + MonteCarloBandChart legible at 320px (effective ≥~12px) and portrait-tuned, desktop byte-identical.
- MonteCarloBandChart's desktop byte-identity is proven by a falsifiable Vitest component test (the e2e-can't-render case), which ALSO exercises the mobile branch.
- The new viewport conditionals are branch-covered IN THIS WAVE (ratchet held); Sparkline explicitly dispositioned; no interaction invented; no value recomputed.
</success_criteria>

<output>
Create `.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-04-SUMMARY.md` when done.
</output>
</content>
