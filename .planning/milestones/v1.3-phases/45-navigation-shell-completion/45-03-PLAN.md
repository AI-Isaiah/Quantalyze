---
phase: 45-navigation-shell-completion
plan: 03
type: execute
wave: 2
depends_on: ["45-01", "45-02"]
files_modified:
  - e2e/mobile-drawer-keyboard.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [NAV-03]
user_setup: []
must_haves:
  truths:
    - "A seeded authed mobile-drawer keyboard e2e proves Tab/Shift+Tab stay contained inside the open drawer (no leak to the inert background), focus moves into the drawer on open, and restores to the hamburger on close."
    - "The same e2e proves the app-shell skip-link is the first focusable element and jumps focus to #main-content, and that drawer links ARE focusable while open (catches an accidental ancestor-inert regression)."
    - "The e2e asserts the nav shell at 320px: no horizontal reflow anchored on the nav, and bottom-nav + hamburger targets measure >=44px (reusing the Phase 44 assertNoReflow/assertTargetSizes helpers)."
    - "The new seeded spec is wired into BOTH FLOW-01 sites — its own HAS_SEED_ENV const AND the ci.yml seeded MA-8 list — and is PROVEN to actually execute in CI (appears 'passed', not 'skipped')."
  artifacts:
    - path: "e2e/mobile-drawer-keyboard.spec.ts"
      provides: "Seeded authed drawer-keyboard containment + skip-link + inert + 320px nav-shell reflow/target-size e2e, mirroring composer-axe.spec.ts seeded-auth"
      contains: "HAS_SEED_ENV"
      min_lines: 60
    - path: ".github/workflows/ci.yml"
      provides: "mobile-drawer-keyboard.spec.ts added to the seeded MA-8 npx playwright test list"
      contains: "mobile-drawer-keyboard.spec.ts"
  key_links:
    - from: "e2e/mobile-drawer-keyboard.spec.ts"
      to: "e2e/helpers/seed-test-project.ts seedTestAllocator"
      via: "import + call (seeded authed login, NOT strategy-v2-keyboard which is dead test.skip(true))"
      pattern: "seedTestAllocator"
    - from: "e2e/mobile-drawer-keyboard.spec.ts"
      to: "e2e/helpers/reflow.ts assertNoReflow/assertTargetSizes"
      via: "import + call at 320px on the nav shell"
      pattern: "assert(NoReflow|TargetSizes)"
    - from: ".github/workflows/ci.yml seeded MA-8 list"
      to: "e2e/mobile-drawer-keyboard.spec.ts"
      via: "npx playwright test list entry (FLOW-01 place 1 of 2)"
      pattern: "mobile-drawer-keyboard.spec.ts"
---

<objective>
Prove the NAV-03 drawer hardening + skip-link work (delivered in Plan 01) and the SC#4 nav-shell geometry with a new SEEDED authed e2e, wired into BOTH FLOW-01 sites and proven to actually run in CI.

Purpose: The hardening is only trustworthy once a test that actually executes proves it — the v1.2 JOURNEY-03 axe gate caught 3 real prod bugs ONLY after it ran in CI. The FLOW-01 dual-wiring trap (a seeded spec wired to only ONE of the two required places => false-green) has burned this project twice; this plan closes both places and makes "proven to execute in CI" an explicit acceptance criterion.

Output: `e2e/mobile-drawer-keyboard.spec.ts` (seeded authed, mirroring composer-axe.spec.ts auth — NOT the dead strategy-v2-keyboard.spec.ts) asserting focus containment + skip-link + inert + 320px reflow + >=44px targets via the Phase 44 helpers, plus the ci.yml MA-8 list entry and the spec's own HAS_SEED_ENV self-skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-navigation-shell-completion/45-CONTEXT.md
@.planning/phases/45-navigation-shell-completion/45-RESEARCH.md
@.planning/phases/45-navigation-shell-completion/45-UI-SPEC.md
@AGENTS.md

<!-- Plan 01 delivers the implementation this e2e asserts; reference its SUMMARY for the exact ids/classes landed. -->
@.planning/phases/45-navigation-shell-completion/45-01-SUMMARY.md

<interfaces>
<!-- Contracts the executor needs. Extracted from this repo. Use directly. -->

Seeded-auth pattern to MIRROR (e2e/composer-axe.spec.ts:47-106) — this is the LIVE pattern:
- `const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;`
- `import { seedTestAllocator } from "./helpers/seed-test-project";` — seedTestAllocator() (seed-test-project.ts:75) returns `{ userId, email, password }` and stamps a VERIFIED allocator profile + investor_attestations (clears the universal approval gate; /allocations redirects unverified profiles to /pending-approval).
- `loginViaForm(page, email, password)`: goto /login, fill `input[name="email"], input[placeholder*="email" i]`, fill `input[type="password"]`, click `button:has-text("Sign in")`, waitForURL `/\/(discovery|strategies|allocations|dashboard)/`.
- `test.skip(!HAS_SEED_ENV, "...prevents false-green on empty/404/login pages (W-02)...")` immediately inside the describe.
- Fail-loud visible anchor before any assertion (composer-axe :104-106): expect a visible element unique to the rendered surface so a 404/login page fails loud, never false-greens.

DOM contracts to assert against (landed by Plan 01 + existing drawer):
- Hamburger: `page.getByRole("button", { name: "Open menu" })` — toggles to "Close menu" when open (MobileTopBar.tsx:28-30); already min-h/min-w-[44px] (:31); aria-controls="mobile-sidebar-drawer".
- Drawer: `#mobile-sidebar-drawer[role="dialog"]` (MobileSidebarDrawer.tsx:155-158); on open focus moves to the first nav link (:139-142); Escape closes (:93) and restores focus to triggerRef/hamburger (:85-86); drawer links: `#mobile-sidebar-drawer a[href]`.
- Inert background: `<main id="main-content" inert={menuOpen}>` (Plan 01) — while the drawer is open, focusing/Tabbing cannot land inside #main-content.
- Skip-link (Plan 01): `a[href="#main-content"]` text "Skip to main content" is the FIRST focusable element; activating/focusing it moves focus to `#main-content`.
- Bottom nav: `nav[aria-label="Primary mobile"]` (Plan 01) — `nav[aria-label="Primary mobile"] a` are the bottom-nav targets (min-h-[44px]).

Phase 44 helpers to REUSE (e2e/helpers/reflow.ts):
- `assertNoReflow(page, anchorSelector)` — asserts scrollWidth - clientWidth <= 1 at the current viewport; anchors on a visible selector first (fail-loud).
- `assertTargetSizes(page, anchorSelector, interactiveSelector)` — asserts every visible element matched by interactiveSelector measures >=44x44 CSS px; requires >=1 measured (false-green guard).

FLOW-01 wiring sites:
- ci.yml SEEDED MA-8 list at :1252-1262 — the `npx playwright test \` block ending `e2e/composer-axe.spec.ts \` then `--timeout 60000`. ADD `e2e/mobile-drawer-keyboard.spec.ts \` to this list (the AUTHED spec belongs in the SEEDED list, NOT the unseeded :1059 list).
- The instruction at ci.yml:1250-1251: "Adding/removing a seed-gated spec? Update both this list and the e2e/<spec>.spec.ts HAS_SEED_ENV constant."
- DEAD reference — do NOT mirror: e2e/strategy-v2-keyboard.spec.ts is `test.skip(true)` (a no-op in the MA-8 list).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded authed mobile-drawer keyboard + skip-link + inert + 320px nav-shell e2e</name>
  <files>e2e/mobile-drawer-keyboard.spec.ts</files>
  <read_first>
    - e2e/composer-axe.spec.ts:47-119 (the LIVE seeded-auth pattern: HAS_SEED_ENV const, loginViaForm, seedTestAllocator, test.skip-on-no-seed, fail-loud visible anchor)
    - e2e/helpers/seed-test-project.ts:65-159 (seedTestAllocator returns {userId,email,password}, stamps verified + attestation)
    - e2e/helpers/reflow.ts (assertNoReflow :47, assertTargetSizes :119 — exact signatures + the false-green guards)
    - src/components/layout/MobileSidebarDrawer.tsx (#mobile-sidebar-drawer role=dialog :155-158; focus-in on open :139-142; Escape :93; restore :85-86; drawer link selector)
    - src/components/layout/MobileTopBar.tsx (hamburger aria-label "Open menu"/"Close menu" :28; min-h/min-w-[44px] :31)
    - .planning/phases/45-navigation-shell-completion/45-01-SUMMARY.md (the exact ids/classes Plan 01 landed: #main-content, .app-skip-link copy, nav[aria-label="Primary mobile"])
    - .planning/phases/45-navigation-shell-completion/45-RESEARCH.md (Code Examples: the drawer-keyboard spec skeleton + the FLOW-01 two sites; Pitfall 2 assert drawer links focusable; Pitfall 3 FLOW-01 burned-twice + proven-execution)
    - e2e/reflow.spec.ts (the Phase 44 reuse template: setViewportSize then assertNoReflow(page, anchor))
  </read_first>
  <action>
    Create `e2e/mobile-drawer-keyboard.spec.ts` mirroring composer-axe.spec.ts seeded-auth (NOT strategy-v2-keyboard.spec.ts, which is a dead test.skip(true)). Declare `const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;` (FLOW-01 place 2 of 2) and `test.skip(!HAS_SEED_ENV, "...prevents false-green on empty/404/login pages (W-02)...")` at the top of the describe. Copy the loginViaForm helper from composer-axe. In the test body: `const allocator = await seedTestAllocator(); await loginViaForm(page, allocator.email, allocator.password); await page.setViewportSize({ width: 375, height: 800 }); await page.goto("/allocations");`. Assert these behaviors, each gated behind a fail-loud visible anchor so a login/404 page fails loud (mirror composer-axe :104):
    (1) SKIP-LINK: the first focusable element is the skip-link — `await page.keyboard.press("Tab")` from a neutral start and assert `a[href="#main-content"]` ("Skip to main content") is focused; activating it (Enter) moves focus to `#main-content` (assert document.activeElement id === "main-content").
    (2) OPEN + FOCUS-IN: click `getByRole("button",{name:"Open menu"})`; assert `#mobile-sidebar-drawer[role="dialog"]` visible (fail-loud anchor); assert the first drawer link `#mobile-sidebar-drawer a[href]` is focused (focus moved INTO the drawer).
    (3) DRAWER LINKS FOCUSABLE (Pitfall 2 ancestor-inert regression guard): assert at least one `#mobile-sidebar-drawer a[href]` is focusable/visible while open (an accidental inert on the wrapper would make these non-focusable).
    (4) CONTAINMENT: press Tab several times and after each assert document.activeElement is contained within `#mobile-sidebar-drawer` (Element.contains) — focus never lands inside the inert `#main-content`; press Shift+Tab and assert the same.
    (5) RESTORE ON CLOSE: press Escape; assert the drawer is gone and `getByRole("button",{name:"Open menu"})` is focused again.
    (6) NAV-SHELL 320px REFLOW + 44px (SC#4): `await page.setViewportSize({ width: 320, height: 800 });` then `await assertNoReflow(page, "nav[aria-label='Primary mobile']");` and `await assertTargetSizes(page, "[aria-label='Open menu']", "nav[aria-label='Primary mobile'] a, [aria-label='Open menu']");` (reuse the Phase 44 helpers; the bottom-nav targets carry min-h-[44px] from Plan 01 and the hamburger is already 44px). Use the exact selectors landed by Plan 01 per 45-01-SUMMARY.md. Do NOT add the spec to the unseeded ci.yml:1059 list.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx playwright test e2e/mobile-drawer-keyboard.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HAS_SEED_ENV" e2e/mobile-drawer-keyboard.spec.ts` is >= 2 (const + test.skip) — FLOW-01 place 2.
    - `grep -c "seedTestAllocator" e2e/mobile-drawer-keyboard.spec.ts` is >= 1 and `grep -c "strategy-v2-keyboard" e2e/mobile-drawer-keyboard.spec.ts` is 0 (mirrors the LIVE pattern, not the dead one).
    - `grep -c "assertNoReflow\|assertTargetSizes" e2e/mobile-drawer-keyboard.spec.ts` is >= 2 (both Phase 44 helpers reused).
    - The spec references the Plan 01 DOM contracts: `grep -c '#main-content' e2e/mobile-drawer-keyboard.spec.ts` >= 1, `grep -c "Skip to main content\|#main-content" e2e/mobile-drawer-keyboard.spec.ts` >= 1, `grep -c "Primary mobile" e2e/mobile-drawer-keyboard.spec.ts` >= 1, `grep -c "mobile-sidebar-drawer" e2e/mobile-drawer-keyboard.spec.ts` >= 1.
    - The spec asserts containment (document.activeElement within #mobile-sidebar-drawer across Tab presses), focus-in on open, restore to hamburger on close, drawer-links-focusable-while-open, and skip-link focus move.
    - `npx playwright test e2e/mobile-drawer-keyboard.spec.ts --list` lists the test (compiles/discovers) and `npx tsc --noEmit` passes.
  </acceptance_criteria>
  <done>e2e/mobile-drawer-keyboard.spec.ts exists, seeded-authed (mirrors composer-axe), asserts containment + focus-in/restore + drawer-links-focusable + skip-link + 320px reflow + >=44px targets via the Phase 44 helpers; HAS_SEED_ENV self-skip present (FLOW-01 place 2); compiles and lists.</done>
</task>

<task type="auto">
  <name>Task 2: FLOW-01 place 1 — wire the seeded spec into ci.yml MA-8 list + proven-execution acceptance</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml:1246-1262 (the MA-8 comment + the `npx playwright test \` seeded list ending `e2e/composer-axe.spec.ts \` then `--timeout 60000`; the :1250-1251 "update both" instruction)
    - .github/workflows/ci.yml:1059 (the UNSEEDED list — the new authed spec must NOT go here)
    - .planning/phases/45-navigation-shell-completion/45-RESEARCH.md (Code Examples: the exact MA-8 list with the ADD line; Pitfall 3 FLOW-01 burned-twice + proven-execution grep-the-CI-log requirement)
  </read_first>
  <action>
    Add `e2e/mobile-drawer-keyboard.spec.ts \` to the SEEDED MA-8 `npx playwright test \` block at ci.yml:1252-1262, placed after `e2e/composer-axe.spec.ts \` and before the `--timeout 60000` line (FLOW-01 place 1 of 2). Do NOT touch the unseeded :1059 list (the spec is authed/seed-gated). Change NOTHING else in ci.yml (env block, upload-skip gate, kill/wait pattern all unchanged). This completes the dual-wiring: place 1 = this ci.yml entry, place 2 = the HAS_SEED_ENV const from Task 1. PROVEN-EXECUTION is a post-push human-verify item (the seeded MA-8 step needs vars.E2E_TEST_DB_CONFIGURED + the TEST_SUPABASE_* secrets, which only run in CI): after CI, confirm `e2e/mobile-drawer-keyboard.spec.ts` appears as `passed` (not `skipped`, not absent) in the seed-gated e2e job log — this is the explicit must_have, mirroring Phase 44's proven-execution gate.
  </action>
  <verify>
    <automated>grep -c "e2e/mobile-drawer-keyboard.spec.ts" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "e2e/mobile-drawer-keyboard.spec.ts" .github/workflows/ci.yml` is exactly 1 (added once, to the MA-8 list).
    - The entry sits inside the SEEDED MA-8 `npx playwright test` block (between line ~1252 `e2e/onboarding-funnel.spec.ts` and the `--timeout 60000` terminator), NOT in the unseeded :1059 single-line list: `grep -n "mobile-drawer-keyboard" .github/workflows/ci.yml` returns a line number > 1240.
    - The unseeded list at :1059 does NOT contain the spec: the grep line number is NOT 1059.
    - No other ci.yml change: the env block (TEST_SUPABASE_*), upload-skip gate, and kill/wait lines are byte-unchanged (`git diff --stat .github/workflows/ci.yml` shows only insertions, +1 line region).
  </acceptance_criteria>
  <done>The seeded spec is in the ci.yml MA-8 list (FLOW-01 place 1); dual-wiring complete with Task 1's HAS_SEED_ENV; proven-execution recorded as the post-push human-verify must_have.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded test session -> /allocations (authed) | The new e2e exercises the EXISTING auth/approval gate via seedTestAllocator (stamps a verified profile). It adds NO auth code and crosses no new boundary — it tests the existing one. |

This phase change adds NO new attack surface: a Playwright spec + a one-line ci.yml list entry. The one real concern is a CI false-green (the spec authored but never running) — addressed below.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | (verification integrity, not STRIDE) | FLOW-01 dual-wiring | mitigate | Wire BOTH places (ci.yml MA-8 list + HAS_SEED_ENV const) and make "proven to execute in CI (passed, not skipped)" an explicit must_have + human-verify item — the twice-burned trap. Every assertion is gated behind a fail-loud visible anchor so a login/404 page fails loud rather than false-greening. |
| T-45-05 | Information Disclosure | Playwright trace on seeded run | accept | Already mitigated by the existing ci.yml:1288-1304 upload-skip on the seed-gated path (the new spec runs in that same gated step); no new exposure. |
</threat_model>

<verification>
- `npx tsc --noEmit` + `npx playwright test e2e/mobile-drawer-keyboard.spec.ts --list` — the spec compiles and is discovered.
- `grep -c "e2e/mobile-drawer-keyboard.spec.ts" .github/workflows/ci.yml` is 1 (FLOW-01 place 1) and the spec contains its HAS_SEED_ENV const (FLOW-01 place 2).
- POST-PUSH (human-verify, FLOW-01 proven-execution): the seed-gated e2e job log shows `e2e/mobile-drawer-keyboard.spec.ts` as `passed` (not skipped) — the explicit must_have, mirroring Phase 44.
- The existing seeded composer-axe.spec.ts (JOURNEY-03) stays green in the same MA-8 run.
</verification>

<success_criteria>
- The seeded authed e2e proves drawer focus containment (Tab/Shift+Tab), focus-in on open, restore to hamburger on close, drawer-links-focusable-while-open, and the app-shell skip-link focus move (ROADMAP SC#3).
- The e2e asserts the nav shell at 320px: no reflow + bottom-nav/hamburger >=44px via the Phase 44 helpers (ROADMAP SC#4).
- FLOW-01 dual-wiring complete (HAS_SEED_ENV const + ci.yml MA-8 list) and proven to execute in CI (passed, not skipped) — explicit must_have.
</success_criteria>

<output>
Create `.planning/phases/45-navigation-shell-completion/45-03-SUMMARY.md` when done.
</output>
