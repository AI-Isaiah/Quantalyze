---
phase: 45-navigation-shell-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
autonomous: true
requirements: [NAV-02]
must_haves:
  truths:
    - "On a phone (<sm) the multi-tab surfaces (Overview / Holdings / Outcomes / Mandate / Risk / Scenario) stay reachable via a horizontally scrollable strip — no tab dropped, every tab reachable by scroll."
    - "The JOURNEY-03 a11y fix is preserved: role=tablist contains ONLY role=tab children; Export and + Allocation remain siblings of the tablist."
    - "On tab activation the active tab is scrolled into view; the scroll respects prefers-reduced-motion (behavior: auto when reduce)."
    - "The data-allocator-tabstrip hook remains so the Phase 44 reflow gate can anchor on it at 320px."
  artifacts:
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      provides: "CSS-first horizontally-scrollable tablist at <sm preserving role=tablist/role=tab siblings; active-tab scrollIntoView with reduced-motion handling"
      contains: "overflow-x-auto"
  key_links:
    - from: "AllocationsTabs tablist <div role='tablist'>"
      to: "the 6 role=tab buttons"
      via: "direct unbroken parent (no role on the scroll wrapper)"
      pattern: "role=\"tablist\""
---

<objective>
Make the allocator multi-tab strip horizontally scrollable on phones (NAV-02) so all six tabs stay reachable at `<sm`, while preserving the JOURNEY-03 `role=tablist`/`role=tab` sibling a11y fix.

Purpose: At `<sm` the 6 tab buttons plus the Export / + Allocation actions exceed 320-375px and wrap awkwardly. A CSS-first scrollable strip keeps every tab reachable with no dropped tab and no horizontal PAGE overflow. JOURNEY-03 (a v1.2 axe gate that caught 3 real prod bugs) must not regress.

Output: A scoped `overflow-x-auto flex-nowrap` scroll strip on the existing tablist, scroll-snap affordance (no edge-fade — DESIGN.md no-gradient), active-tab `scrollIntoView` with reduced-motion handling, and the `data-allocator-tabstrip` anchor preserved for the Plan 03 reflow assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/45-navigation-shell-completion/45-CONTEXT.md
@.planning/phases/45-navigation-shell-completion/45-RESEARCH.md
@.planning/phases/45-navigation-shell-completion/45-UI-SPEC.md
@DESIGN.md
@AGENTS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from this repo. Use directly. -->

From src/app/(dashboard)/allocations/AllocationsTabs.tsx (the region to edit):
- Header row container :543-546 — `<div data-allocator-tabstrip className="mb-4 flex flex-wrap items-end gap-x-4 gap-y-3 border-b border-border pb-3">` (KEEP the data hook + the border-b underline rail).
- The tablist :566-570 — `<div role="tablist" aria-label="Allocation surfaces" className="flex items-center gap-1">` holding `VISIBLE_TAB_KEYS.map(...)` of `<button role="tab" id={`tab-${key}`} aria-selected aria-controls={`panel-${key}`} tabIndex={isActive?0:-1} onClick={changeTab} onKeyDown={handleTabKeyDown} className={isActive?TAB_BUTTON_ACTIVE:TAB_BUTTON_INACTIVE}>`.
- JOURNEY-03 comment :560-564 — a role="tablist" may contain ONLY role="tab" children (axe aria-required-children, critical).
- Export button + "+ Allocation" button :609-655 — SIBLINGS of the tablist inside the `ml-auto flex items-center gap-1` cluster (:565). KEEP them out of the tablist.
- The component already manages `tabRefs.current[key]` (the tab DOM nodes), roving `tabIndex`, and `handleTabKeyDown` — reuse `tabRefs` for the active-tab scrollIntoView.
- TAB_BUTTON_ACTIVE / TAB_BUTTON_INACTIVE / TAB_COUNT_BADGE_* are class consts pinned by the dashboard-parity contract — KEEP byte-identical (do not retune them here).

Precedent (live overflow-x-auto strip): src/app/factsheet/[id]/v2/FactsheetView.tsx:749-775 — a role-less <nav> wrapping a `flex items-center gap-1 overflow-x-auto` list with `pointer-coarse:min-h-[44px]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS-first scrollable tab strip preserving JOURNEY-03 + active-tab scrollIntoView</name>
  <files>src/app/(dashboard)/allocations/AllocationsTabs.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx (:543-657 — the header row, the tablist :566-608, the Export/+Allocation siblings :609-655, the JOURNEY-03 comment :560-564; tabRefs + changeTab + handleTabKeyDown definitions earlier in the file)
    - src/app/factsheet/[id]/v2/FactsheetView.tsx:749-775 (the live overflow-x-auto section-nav precedent to mirror)
    - .planning/phases/45-navigation-shell-completion/45-UI-SPEC.md (Component Contract 2: exact class delta `flex flex-nowrap items-center gap-1 overflow-x-auto sm:overflow-x-visible sm:flex-wrap` + `[scrollbar-width:none] [-webkit-overflow-scrolling:touch]`; snap-x on strip + snap-start shrink-0 on each tab; NO edge-fade gradient; active-tab scrollIntoView({inline:"nearest",block:"nearest"}); reduced-motion behavior:"auto")
    - .planning/phases/45-navigation-shell-completion/45-RESEARCH.md (Pattern 2 + Pitfall 4 JOURNEY-03 nesting regression; Anti-Patterns: do NOT add a role to the scroll wrapper, do NOT move Export/+Allocation inside the tablist)
    - .planning/phases/45-navigation-shell-completion/45-UI-SPEC.md States row: reduced-motion mandatory; precedent motion-reduce:transition-none at CustomizeDrawer.tsx:158,166
  </read_first>
  <action>
    Make the existing `role="tablist"` div scroll horizontally on phones WITHOUT re-nesting tabs or adding a role to any new wrapper (JOURNEY-03 / Pitfall 4). Apply the class delta from UI-SPEC Contract 2 to the tablist element itself: `flex items-center gap-1` -> `flex flex-nowrap items-center gap-1 overflow-x-auto sm:overflow-x-visible sm:flex-wrap` plus `[scrollbar-width:none] [-webkit-overflow-scrolling:touch]` so the native scrollbar adds no chrome and iOS momentum-scrolls. The tablist stays the SAME element with the SAME `role="tablist"` and the same direct `role="tab"` button children (unbroken parent). Add scroll-snap as the CSS-first affordance: `snap-x` on the tablist strip and `snap-start shrink-0` on each `role="tab"` button (shrink-0 keeps labels from compressing; the cut-off peeking tab IS the scroll affordance — NO edge-fade gradient per DESIGN.md). Keep `TAB_BUTTON_ACTIVE`/`TAB_BUTTON_INACTIVE`/`TAB_COUNT_BADGE_*` byte-identical (dashboard-parity contract) — append the snap classes via the existing className composition without reordering the pinned tokens (if these consts are append-only strings, concatenate `" snap-start shrink-0"`; verify the parity guard still passes). Wire active-tab visibility: in `changeTab` (or a small effect keyed on `activeTab`) call `tabRefs.current[activeTab]?.scrollIntoView({ inline: "nearest", block: "nearest", behavior: prefersReducedMotion ? "auto" : "smooth" })` where `prefersReducedMotion = typeof window !== "undefined" && window.matchMedia("(prefers-reduced-motion: reduce)").matches` — never animate a forced scroll for reduced-motion users. KEEP `data-allocator-tabstrip` on the header row (the Plan 03 reflow gate anchors on it) and KEEP the `border-b` underline rail so the active tab's `-mb-[10px] border-b-2 border-accent` seats on the shared baseline. Do NOT move Export / + Allocation inside the tablist — they stay siblings. Do NOT add a `role` to any scroll wrapper.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && npx vitest run src/app/\(dashboard\)/allocations 2>/dev/null || npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "overflow-x-auto" "src/app/(dashboard)/allocations/AllocationsTabs.tsx"` is >= 1 and the same line (or the tablist element) also contains `flex-nowrap` and `sm:flex-wrap`.
    - The `role="tablist"` element is unchanged structurally: `grep -c 'role="tablist"' "src/app/(dashboard)/allocations/AllocationsTabs.tsx"` is 1 and the 6 `role="tab"` buttons remain its direct children (no new element with a `role` wraps them).
    - `grep -c "data-allocator-tabstrip" "src/app/(dashboard)/allocations/AllocationsTabs.tsx"` is >= 1 (anchor preserved).
    - `grep -c "scrollIntoView" "src/app/(dashboard)/allocations/AllocationsTabs.tsx"` is >= 1 and the call site references `prefers-reduced-motion` (behavior conditional).
    - No edge-fade gradient added: `grep -ci "gradient\|bg-gradient" "src/app/(dashboard)/allocations/AllocationsTabs.tsx"` is 0 in the tab-strip region (DESIGN.md no-gradient).
    - Export / + Allocation remain OUTSIDE the tablist (the `aria-label="Export"` button and `aria-label="Add allocation` button are not nested inside the `role="tablist"` div).
    - `npx tsc --noEmit` passes; the existing AllocationsTabs unit/parity tests stay green.
    - JOURNEY-03 regression check is the existing seeded `composer-axe.spec.ts` (scans /allocations?tab=scenario) — it must still report zero aria-required-children violations (verified in CI; this plan must not change its result).
  </acceptance_criteria>
  <done>The tablist scrolls horizontally at <sm with snap affordance and no dropped tab; role=tablist/role=tab siblings + Export/+Allocation siblings preserved (JOURNEY-03); active-tab scrollIntoView honors reduced motion; data-allocator-tabstrip anchor kept; tsc + existing tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This task is a pure CSS-first presentation change to an existing client component plus a scrollIntoView call. No data, input, endpoint, or auth boundary is touched. |

This phase change adds NO new attack surface — it restyles an existing tablist and adds a viewport-scroll call. The only correctness boundary is a11y (preserving JOURNEY-03), tracked below.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | (a11y regression, not STRIDE) | tablist scroll wrapper | mitigate | Never add a role to the scroll wrapper and never re-nest role=tab; the existing seeded composer-axe.spec.ts (ci.yml:1261) scans /allocations?tab=scenario and fails loud on an aria-required-children re-nesting regression. |
</threat_model>

<verification>
- `npx tsc --noEmit` — no type regressions.
- Existing AllocationsTabs unit/parity tests + the seeded `composer-axe.spec.ts` JOURNEY-03 gate stay green (no aria-required-children).
- The 320px nav-shell reflow assertion anchored on `[data-allocator-tabstrip]` is added in Plan 03 (Wave 2) — this plan keeps the anchor present for it.
</verification>

<success_criteria>
- All six tabs reachable via horizontal scroll at <sm with no dropped tab and no horizontal PAGE overflow (ROADMAP SC#2).
- JOURNEY-03 role=tablist/role=tab sibling fix preserved (ROADMAP SC#2; composer-axe stays green).
- Active-tab scrollIntoView honors prefers-reduced-motion.
- data-allocator-tabstrip anchor retained for the Plan 03 reflow gate.
</success_criteria>

<output>
Create `.planning/phases/45-navigation-shell-completion/45-02-SUMMARY.md` when done.
</output>
