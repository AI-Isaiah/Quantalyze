---
phase: 46-surface-by-surface-reflow
plan: 04
type: execute
wave: 2
depends_on: ["46-01", "46-02", "46-03"]
files_modified:
  - e2e/reflow-sweep.spec.ts
  - e2e/reflow-sweep-authed.spec.ts
  - .github/workflows/ci.yml
  - src/components/ui/EmptyStateCard.tsx
  - src/components/ui/Skeleton.tsx
  - src/app/(dashboard)/allocations/EmptyState.tsx
  - src/components/scenarios/SampleFloorEmptyState.tsx
autonomous: true
requirements: [REFLOW-01, REFLOW-02, REFLOW-03]
must_haves:
  truths:
    - "A parametrized public-route reflow sweep proves every curated public route has no horizontal page overflow at 320px (and is usable at 400% zoom)"
    - "A seeded authed reflow sweep proves every curated authed route (allocations + tabs, composer, factsheets, bridge, risk, discovery, single-strategy, wizard, portfolios, security, admin) has no overflow at 320px"
    - "≥1 degenerate-state route is in the sweep, anchored on a VISIBLE honest-empty DOM node, so a broken honest-empty layout fails loud"
    - "Both new specs are FLOW-01 dual-wired (ci.yml list + env self-skip / its deliberate absence) and PROVEN to execute (passed, not skipped) in CI"
    - "Honest-state components (EmptyStateCard / Skeleton family / allocations EmptyState / SampleFloorEmptyState) reflow at 320px (fix only on overflow)"
  artifacts:
    - path: "e2e/reflow-sweep.spec.ts"
      provides: "public unseeded parametrized sweep at 320px + 400%"
      contains: "assertNoReflow"
    - path: "e2e/reflow-sweep-authed.spec.ts"
      provides: "seeded authed parametrized sweep + ≥1 degenerate route"
      contains: "HAS_SEED_ENV"
    - path: ".github/workflows/ci.yml"
      provides: "both specs wired (unseeded list + seeded MA-8 list)"
      contains: "reflow-sweep"
  key_links:
    - from: "e2e/reflow-sweep.spec.ts"
      to: "e2e/helpers/reflow.ts assertNoReflow"
      via: "import + per-route anchor"
      pattern: "assertNoReflow"
    - from: "e2e/reflow-sweep-authed.spec.ts"
      to: "seedTestAllocator + loginViaForm"
      via: "seeded login then iterate authed routes"
      pattern: "seedTestAllocator"
    - from: ".github/workflows/ci.yml:1059"
      to: "e2e/reflow-sweep.spec.ts"
      via: "appended to the unseeded Playwright list"
      pattern: "reflow-sweep.spec.ts"
    - from: ".github/workflows/ci.yml:1254-1263"
      to: "e2e/reflow-sweep-authed.spec.ts"
      via: "appended to the seeded MA-8 list"
      pattern: "reflow-sweep-authed.spec.ts"
---

<objective>
Prove "every authed + public route reflows at 320px / 400% with no horizontal page overflow" with two parametrized Playwright sweeps extending the phase-44 `assertNoReflow` helper — a public unseeded spec and a seeded authed spec (with ≥1 degenerate-state route) — FLOW-01 dual-wired into `ci.yml` and proven to execute; and verify-and-fix the honest-state components at 320px.

Purpose: REFLOW-01/02/03 — this is the verification backbone of the phase. It depends on the table wraps (46-01/46-02) and wizard de-block (46-03) landing first so the swept surfaces already reflow. The degenerate-state route + honest-state verification close REFLOW-03.
Output: 2 new sweep specs, ci.yml dual-wired, honest-state components verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md
@.planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md
@.planning/phases/46-surface-by-surface-reflow/46-UI-SPEC.md
@.planning/phases/46-surface-by-surface-reflow/46-VALIDATION.md

<interfaces>
<!-- The reflow helper — reuse verbatim, do NOT re-derive the geometry probe. -->
From e2e/helpers/reflow.ts:
- `export async function assertNoReflow(page, anchorSelector)` — asserts `documentElement.scrollWidth - clientWidth <= 1` at the CURRENT viewport; the anchor must be VISIBLE first (fail-loud on blank/404/unhydrated); breadcrumbs the offending element.
- `export async function assertTargetSizes(page, anchorSelector, interactiveSelector)` — `MIN_TARGET_PX = 44` (WCAG floor, never lowered).

<!-- Unseeded precedent (FLOW-01 place 2 = NO env guard). -->
From e2e/reflow.spec.ts: set `page.setViewportSize({ width: 320, height: 800 })`, `page.goto(path)`, early-throw on `status() >= 400`, then `assertNoReflow(page, "main h1")`. NO HAS_SEED_ENV guard.

<!-- Seeded precedent (FLOW-01 place 2 = HAS_SEED_ENV test.skip). -->
From e2e/mobile-drawer-keyboard.spec.ts:
- `const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;`
- `import { seedTestAllocator } from "./helpers/seed-test-project";` → returns `{ userId, email, password }`, stamps a VERIFIED allocator so /allocations renders real chrome
- a `loginViaForm(page, email, password)` form-fill that waits on `/\/(discovery|strategies|allocations|dashboard)/`
- `test.skip(!HAS_SEED_ENV, "...")` inside the describe.

<!-- ci.yml dual-wiring targets (CODE-VERIFIED line numbers). -->
.github/workflows/ci.yml:1059 — the UNSEEDED list line:
`npx playwright test e2e/auth.spec.ts e2e/smoke.spec.ts e2e/demo-public.spec.ts e2e/demo-founder-view.spec.ts e2e/onboarding-banner-smoke.spec.ts e2e/demo-screenshot.spec.ts e2e/reflow.spec.ts e2e/target-size.spec.ts`
.github/workflows/ci.yml:1254-1263 — the SEEDED MA-8 list (`\`-continued, gated `if: vars.E2E_TEST_DB_CONFIGURED == 'true'`), last two entries `e2e/composer-axe.spec.ts \` then `e2e/mobile-drawer-keyboard.spec.ts \` then `--timeout 60000`. The in-file comment at 1250-1252 is the "update BOTH" reminder.

<!-- Public route anchors (verified). /demo has no direct <h1> — use a per-route visible anchor at discretion. -->
- /security → "main h1" (verified). /browse → page <h1> at :28. /for-quants → <h1> at :88. /demo → choose a visible content anchor (not generic chrome).

<!-- Honest-state components (verified fluid by construction — fix only on overflow). -->
- src/components/ui/EmptyStateCard.tsx:25 — `rounded-lg border border-border bg-surface px-4 py-8 text-center …` (no fixed width)
- src/components/ui/Skeleton.tsx — fractional widths only (w-full / w-2/3 / w-1/3, h-4/h-5), aria-hidden
- src/app/(dashboard)/allocations/EmptyState.tsx:48 — `max-w-md mx-auto` (caps, does not overflow)
- src/components/scenarios/SampleFloorEmptyState.tsx — renders EmptyStateCard tokens
- CorrelationMatrix.tsx:166 — "No correlation data available" empty branch + `data-testid="correlation-matrix"` (degenerate-route anchor candidate)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify honest-state components reflow at 320px (fix only on overflow)</name>
  <read_first>
    - src/components/ui/EmptyStateCard.tsx, src/components/ui/Skeleton.tsx
    - src/app/(dashboard)/allocations/EmptyState.tsx, src/components/scenarios/SampleFloorEmptyState.tsx
    - .planning/phases/46-surface-by-surface-reflow/46-UI-SPEC.md §"3. Honest States Across Breakpoints"
  </read_first>
  <files>src/components/ui/EmptyStateCard.tsx, src/components/ui/Skeleton.tsx, src/app/(dashboard)/allocations/EmptyState.tsx, src/components/scenarios/SampleFloorEmptyState.tsx (verify; edit only on overflow)</files>
  <action>
    Read each honest-state component. They are fluid by construction (no fixed-px widths; `max-w-md mx-auto` caps without overflowing). Change a component ONLY if it carries a fixed-px width or a non-wrapping element that would overflow 320px — if so, swap the fixed width for a fluid/`max-w-*` utility (do NOT shrink text, do NOT recolor; honest-empty stays a neutral muted card with NO `role="alert"`). If all four are already fluid (expected), make NO code change to them and record "verified fluid, no fix needed" in the SUMMARY. Do NOT add a new empty-state component (CONTEXT forbids it). Em-dash stays the canonical "no real value" placeholder.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/ui/EmptyStateCard src/components/ui/Skeleton --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - No new empty-state component created; no `role="alert"` added to any honest-empty card
    - Any change is a fixed-px-width → fluid swap only (no text shrink, no recolor); if no overflow, zero code change and SUMMARY says "verified fluid"
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The four honest-state components are verified to reflow at 320px; fixes (if any) are fluid-width-only; the SUMMARY records the verification outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Write the public unseeded parametrized reflow sweep + wire into ci.yml unseeded list</name>
  <read_first>
    - e2e/reflow.spec.ts (the unseeded precedent — full file)
    - e2e/helpers/reflow.ts (assertNoReflow signature + fail-loud behavior)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"Reflow sweep — public unseeded spec" + §"FLOW-01 dual-wiring"
    - .github/workflows/ci.yml line 1059 (the exact unseeded list line)
  </read_first>
  <files>e2e/reflow-sweep.spec.ts, .github/workflows/ci.yml</files>
  <action>
    Create `e2e/reflow-sweep.spec.ts` modeled on `reflow.spec.ts` — NO `HAS_SEED_ENV` guard (unseeded; FLOW-01 place 2 = the deliberate absence of an env guard). Define a curated `PUBLIC_ROUTES: { path; anchor }[]` (landing/`/`, `/security`, `/for-quants`, `/browse`, `/demo`, public factsheet/share, `/discovery` public surface — executor discretion within the SC#1 public floor). Parametrize: for each route set viewport 320×800, `page.goto(path)`, early-throw on `status() >= 400`, then `assertNoReflow(page, r.anchor)` anchored on a VISIBLE content element per route (never generic chrome — Pitfall 5). Add a 400%-zoom assertion for at least the representative public routes by emulating zoom (set a 320-equivalent CSS width via `setViewportSize` per the phase-44 reflow-at-narrow-width convention — match how reflow.spec.ts/target-size.spec.ts express the zoom case; do not invent a new mechanism). Then APPEND ` e2e/reflow-sweep.spec.ts` to the end of the ci.yml:1059 unseeded list line (FLOW-01 place 1).
  </action>
  <verify>
    <automated>npx playwright test e2e/reflow-sweep.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/reflow-sweep.spec.ts` exists, has NO `HAS_SEED_ENV`/`test.skip(!process.env.TEST_SUPABASE`...) guard, iterates ≥4 public routes via `assertNoReflow`
    - `grep -c "reflow-sweep.spec.ts" .github/workflows/ci.yml` ≥ 1 AND it is on the unseeded line 1059 (same line as `e2e/reflow.spec.ts`)
    - Local `npx playwright test e2e/reflow-sweep.spec.ts` passes (public routes reachable unseeded)
  </acceptance_criteria>
  <done>Public reflow sweep passes locally and is wired into the ci.yml unseeded list.</done>
</task>

<task type="auto">
  <name>Task 3: Write the seeded authed sweep (+ ≥1 degenerate route) + wire into the ci.yml seeded MA-8 list</name>
  <read_first>
    - e2e/mobile-drawer-keyboard.spec.ts (seeded precedent — HAS_SEED_ENV, seedTestAllocator, loginViaForm, test.skip)
    - e2e/helpers/seed-test-project.ts (seedTestAllocator returns {userId,email,password})
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"Reflow sweep — seeded authed spec" + §"≥1 degenerate-state route" + §"FLOW-01 dual-wiring"
    - .github/workflows/ci.yml lines 1250-1263 (the seeded MA-8 list + "update BOTH" comment)
  </read_first>
  <files>e2e/reflow-sweep-authed.spec.ts, .github/workflows/ci.yml</files>
  <action>
    Create `e2e/reflow-sweep-authed.spec.ts` modeled on `mobile-drawer-keyboard.spec.ts`: declare `const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;` (FLOW-01 place 2), `import { seedTestAllocator } from "./helpers/seed-test-project"` + the `loginViaForm` form-fill, and `test.skip(!HAS_SEED_ENV, "seed env not wired — prevents false-green on login/404")` inside the describe. In a `beforeAll`/`beforeEach`, seed + login once. Iterate the authed route floor (`/allocations` + tabs Overview/Holdings/Outcomes/Mandate/Risk/Scenario, the Scenario composer, factsheet routes, Bridge, Risk, Discovery, single-strategy, `/strategies/new/wizard`, `/portfolios`, `/security`, an admin route) — for each, set viewport 320×800, navigate, and `assertNoReflow(page, <a VISIBLE content anchor for that route>)`. Add ≥1 DEGENERATE-state route: navigate to a 0-strategy / <10-overlapping-day surface (e.g. the composer or a correlation surface that renders the honest-empty card) and `assertNoReflow` anchored on the VISIBLE honest-empty node (`data-testid="correlation-matrix"` empty branch, or an `EmptyStateCard` heading) — NOT generic chrome (Pitfall 5). Then APPEND `e2e/reflow-sweep-authed.spec.ts \` to the seeded MA-8 `\`-continued list in ci.yml (right before `--timeout 60000`, after `e2e/mobile-drawer-keyboard.spec.ts \`) — FLOW-01 place 1.
  </action>
  <verify>
    <automated>npx playwright test e2e/reflow-sweep-authed.spec.ts --reporter=line; echo "exit=$? (skipped-without-seed-env is the expected local outcome; CI proves execution)"</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/reflow-sweep-authed.spec.ts` exists with the `HAS_SEED_ENV` const + `test.skip(!HAS_SEED_ENV, …)` (FLOW-01 place 2) and uses `seedTestAllocator` + `loginViaForm`
    - It iterates the authed route floor and includes ≥1 degenerate-state route anchored on a visible honest-empty DOM node
    - `grep -c "reflow-sweep-authed.spec.ts" .github/workflows/ci.yml` ≥ 1 AND it sits inside the seeded MA-8 `\`-continued list (1254-1263), not the unseeded line
    - Locally the spec SKIPS cleanly without seed env (no crash); the SUMMARY notes CI must show it PASSED (not skipped) when E2E_TEST_DB_CONFIGURED is set
  </acceptance_criteria>
  <done>The seeded authed sweep (with a degenerate route) is written, self-skips without seed env, and is wired into the seeded MA-8 list.</done>
</task>

<task type="auto">
  <name>Task 4: Prove FLOW-01 dual-wiring locally + record the CI-execution check for ship/land</name>
  <read_first>
    - .github/workflows/ci.yml lines 1059 (unseeded list) + 1250-1263 (seeded MA-8 list + the "update BOTH" comment)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"FLOW-01 dual-wiring" (the twice-burned trap)
    - .planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md Pitfall 2
  </read_first>
  <files>(verification only — no edit unless a wiring place is missing)</files>
  <action>
    Prove FLOW-01 place 1 (ci.yml) AND place 2 (the spec's own env-guard / its deliberate absence) for BOTH new specs — a gate wired in only one place silently never runs (burned twice). Place-1 check: confirm `e2e/reflow-sweep.spec.ts` is on the UNSEEDED list line (ci.yml:1059, same line as `e2e/reflow.spec.ts`) and `e2e/reflow-sweep-authed.spec.ts` is inside the SEEDED MA-8 backslash-continued list (ci.yml:1254-1263). Place-2 check: confirm `reflow-sweep.spec.ts` has NO `HAS_SEED_ENV`/`test.skip(!process.env.TEST_SUPABASE` guard (unseeded) and `reflow-sweep-authed.spec.ts` HAS the `HAS_SEED_ENV` const + `test.skip(!HAS_SEED_ENV, …)` (seeded). Local behavior check: run the public spec (must PASS) and the authed spec (must SKIP cleanly without seed env, never crash). If any place is missing, add it. Because actual CI execution ("passed, not skipped" in the real run) is a post-push observation, record an explicit ship/land directive in the SUMMARY: at /ship or /land, confirm the CI run shows reflow-sweep PASSED in the unseeded job and reflow-sweep-authed PASSED (not skipped) in the seeded MA-8 job when `vars.E2E_TEST_DB_CONFIGURED == 'true'`.
  </action>
  <verify>
    <automated>grep -q "reflow-sweep.spec.ts" .github/workflows/ci.yml && grep -q "reflow-sweep-authed.spec.ts" .github/workflows/ci.yml && ! grep -q "HAS_SEED_ENV" e2e/reflow-sweep.spec.ts && grep -q "HAS_SEED_ENV" e2e/reflow-sweep-authed.spec.ts && echo "FLOW-01 dual-wiring proven"</automated>
  </verify>
  <acceptance_criteria>
    - Both new specs appear in ci.yml: `reflow-sweep.spec.ts` on the unseeded line, `reflow-sweep-authed.spec.ts` in the seeded MA-8 list
    - `reflow-sweep.spec.ts` carries NO env guard; `reflow-sweep-authed.spec.ts` carries the `HAS_SEED_ENV` self-skip
    - Public spec passes locally; authed spec skips cleanly (no crash) without seed env
    - SUMMARY records the ship/land CI-execution directive (confirm both specs PASSED-not-skipped in the real CI run)
  </acceptance_criteria>
  <done>FLOW-01 dual-wiring is proven locally for both specs (both places); the CI-execution observation is queued for ship/land in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The sweeps are read-only DOM geometry probes; honest-state verification is presentation-only. No input, network, auth, or data flow is introduced. The seeded authed spec uses the existing test-only `seedTestAllocator` helper (CI-gated, fork-skipped). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04-FG | (false-green gate) | new e2e sweeps in ci.yml | mitigate | FLOW-01 dual-wiring: each spec wired in BOTH ci.yml list AND its env-guard (or its deliberate absence); Task 4 proves both places locally (grep + skip/pass behavior); the SUMMARY queues the CI PASSED-not-skipped observation for ship/land (the twice-burned trap). |
| T-46-04-ID | Information Disclosure | seeded authed spec test data | accept | Uses the existing `seedTestAllocator` (timestamped `@example.test`, CI-gated on `vars.E2E_TEST_DB_CONFIGURED`, fork-skipped) — same pattern as every seeded spec; no new data path. |
| T-46-04-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed (RESEARCH audit N/A; `@lhci/cli` lands in phase 48). |
</threat_model>

<verification>
- `npx playwright test e2e/reflow-sweep.spec.ts` green locally (public unseeded).
- `npx playwright test e2e/reflow-sweep-authed.spec.ts` SKIPS cleanly without seed env; PASSES in the seeded CI run.
- Local: both specs wired in both FLOW-01 places (Task 4 automated grep proof); public passes, authed skips cleanly. CI-run observation (PASSED-not-skipped) is recorded in the SUMMARY for ship/land.
- `npm run test:coverage` all four ratchet metrics green; `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- REFLOW-01: public + authed routes proven no-horizontal-overflow at 320px by the parametrized sweeps.
- REFLOW-02: zoom-permissive viewport (phase-44) verified by the sweep's 400%/narrow-width assertion; zoom never disabled.
- REFLOW-03: ≥1 degenerate-state route in the sweep anchored on a visible honest-empty node; honest-state components verified fluid.
- FLOW-01: both specs dual-wired and PROVEN to execute (passed, not skipped) in CI.
</success_criteria>

<output>
Create `.planning/phases/46-surface-by-surface-reflow/46-04-SUMMARY.md` when done (record both CI spec statuses + the four coverage numbers).
</output>
