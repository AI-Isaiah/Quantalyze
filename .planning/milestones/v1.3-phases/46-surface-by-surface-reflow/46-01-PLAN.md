---
phase: 46-surface-by-surface-reflow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/HoldingsTable.tsx
  - src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx
  - src/app/(dashboard)/allocations/components/HoldingsTable.all-columns.test.tsx
autonomous: true
requirements: [TABLE-01]
must_haves:
  truths:
    - "The 3 HoldingsTable inner tables (StrategyRows, Legacy, Design) scroll horizontally inside a ResponsiveTable region at 320px instead of overflowing the page"
    - "OpenPositionsTable scrolls horizontally inside a ResponsiveTable region at 320px"
    - "A render-test guard fails loudly if a future edit drops a material column from LegacyHoldingsTable (7 cols) or DesignHoldingsTable (9 cols)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"
      provides: "3 inner tables each wrapped in ResponsiveTable"
      contains: "ResponsiveTable"
    - path: "src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx"
      provides: "table wrapped in ResponsiveTable"
      contains: "ResponsiveTable"
    - path: "src/app/(dashboard)/allocations/components/HoldingsTable.all-columns.test.tsx"
      provides: "fail-loud all-columns guard for legacy(7) + design(9) modes"
      contains: "TOTAL_COLUMNS"
  key_links:
    - from: "HoldingsTable.tsx"
      to: "@/components/ResponsiveTable"
      via: "import + wrap"
      pattern: "ResponsiveTable"
    - from: "HoldingsTable.all-columns.test.tsx"
      to: "HoldingsTable render output"
      via: "render + assert full <th> set"
      pattern: "getAllByRole\\(.columnheader|<th"
---

<objective>
Wrap the 3 unprotected HoldingsTable inner tables and OpenPositionsTable in the existing `ResponsiveTable` Server Component (horizontal scroll + ARIA region, zero restyle), and add a fail-loud all-columns render guard for the two highest-stakes holdings modes (legacy 7-col, design 9-col).

Purpose: TABLE-01 — the holdings family is the densest financial table set; it must stay usable at 320px with every material column reachable, and a future `hidden`/`truncate` edit that drops a metric or status must fail CI loudly.
Output: `HoldingsTable.tsx` + `OpenPositionsTable.tsx` wrapped; new `HoldingsTable.all-columns.test.tsx` guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md
@.planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md
@.planning/phases/46-surface-by-surface-reflow/46-UI-SPEC.md

<interfaces>
<!-- The wrap primitive (Server Component). Extracted from codebase — use directly, no exploration needed. -->
From src/components/ResponsiveTable.tsx:
- `export function ResponsiveTable({ children, hint }: { children: ReactNode; hint?: string })`
- Renders `<div className="overflow-x-auto" role="region" aria-label={label} tabIndex={0}>{children}</div>`
- `DEFAULT_HINT` = "Table scrolls horizontally. Swipe or use arrow keys to see more columns." — pass `hint` only for a clearer affordance; default is canonical.
- It is the accessible name itself — do NOT add a duplicate `sr-only` scroll node (double-announce).
- Import path: `import { ResponsiveTable } from "@/components/ResponsiveTable";`

<!-- CODE-VERIFIED column constants (the UI-SPEC mode labels are INVERTED — trust THESE, not the spec). -->
From src/app/(dashboard)/allocations/components/HoldingsTable.tsx:
- `StrategyRowsTable` `<table className="w-full text-sm" data-table="strategies">` at line 260 (8 cols, NO guard)
- `LegacyHoldingsTable` `TOTAL_COLUMNS = 7` at line 378; `<table className="w-full text-sm">` at line 399; Notes icon `<th className="w-10 px-2 py-2" aria-label="Notes" />` at line 410
- `DesignHoldingsTable` `DESIGN_TOTAL_COLUMNS = 9` at line 551; `<table className="w-full text-sm">` at line 620; Status icon `<th className="w-3 px-2 py-2" aria-label="Status" />` at line 623

<!-- Render-test harness analog — mirror its mocks. -->
From src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx:1-45:
- `vi.mock("next/navigation", () => ({ useRouter: () => ({ refresh, replace, push, back, forward, prefetch }), usePathname: () => "/allocations", useSearchParams: () => new URLSearchParams() }))` — REQUIRED (HoldingsTable imports useRouter at module scope)
- `vi.mock("next/link", () => ({ ... plain anchor }))`
- `render(<HoldingsTable … />)` fed by real adapter output / minimal payload fixture
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap the 3 HoldingsTable inner tables + OpenPositionsTable in ResponsiveTable</name>
  <read_first>
    - src/app/(dashboard)/allocations/components/HoldingsTable.tsx (the 3 table sites: lines 260, 399, 620)
    - src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx (line 127 + tfoot total at ~199)
    - src/components/ResponsiveTable.tsx (the primitive — read full file once)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"Table wrap" (before/after excerpts)
  </read_first>
  <files>src/app/(dashboard)/allocations/components/HoldingsTable.tsx, src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx</files>
  <action>
    Add `import { ResponsiveTable } from "@/components/ResponsiveTable";` to both files. In `HoldingsTable.tsx`, wrap each of the 3 inner `<table>…</table>` elements (StrategyRowsTable at line 260, LegacyHoldingsTable at 399, DesignHoldingsTable at 620) in `<ResponsiveTable>…</ResponsiveTable>` — wrap the `<table>` branch of each `{cond ? <p…/> : (<table>…</table>)}` ternary ONLY; leave the empty-state `<p>` branch untouched. In `OpenPositionsTable.tsx`, wrap the `<table>` at line 127 (the `<tfoot>` total rides inside — wrap the whole table). Restyle NOTHING: keep every className, data-attr (`data-table="strategies"`, `data-row-id`, `data-strategy-row`), and the `style={{ minHeight: 44 }}` row heights verbatim. Do NOT pass a `hint` (default is canonical). Do NOT add any `sr-only` scroll node. Do NOT touch the `TOTAL_COLUMNS`/`DESIGN_TOTAL_COLUMNS` constants or any `<th>`.
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/allocations/components/HoldingsTable --reporter=dot && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ResponsiveTable "src/app/(dashboard)/allocations/components/HoldingsTable.tsx"` ≥ 4 (1 import + 3 wraps)
    - `grep -c ResponsiveTable "src/app/(dashboard)/allocations/components/OpenPositionsTable.tsx"` ≥ 2 (1 import + 1 wrap)
    - All existing HoldingsTable.*.test.tsx files still pass (no restyle, data-attrs preserved)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>3 holdings tables + OpenPositionsTable each render inside a `ResponsiveTable` region; existing render tests green; no restyle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the fail-loud all-columns render guard (legacy 7 + design 9)</name>
  <read_first>
    - src/app/(dashboard)/allocations/components/HoldingsTable.strategy-rows.test.tsx:1-70 (render harness + mocks to mirror)
    - src/components/MobileNav.test.tsx:82-103 (exact-set pinning assertion style — toEqual + not.toContain)
    - src/components/viewport-zoom-meta.test.ts:97-114 (falsifiability precedent)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"All-columns guard" (the verbatim <th> sets)
  </read_first>
  <files>src/app/(dashboard)/allocations/components/HoldingsTable.all-columns.test.tsx</files>
  <behavior>
    - LegacyHoldingsTable renders exactly 7 columns: the named material set Venue/Symbol · Type · Quantity · Entry price · Value (USD) · Unrealized P&L + the Notes icon `<th aria-label="Notes">` — assert count === 7 and every named material header present.
    - DesignHoldingsTable renders exactly 9 columns: the Status icon `<th aria-label="Status">` + Strategy · Symbol · Weight · Allocation · MTD · Sharpe · Max DD · Age — assert count === 9 and every named material header present.
    - Falsifiable: deleting any material `<th>` makes the guard FAIL (proven once by the implementer, then restored).
    - No material header carries a `hidden` / `md:table-cell` / column-`truncate` class.
  </behavior>
  <action>
    Create `src/app/(dashboard)/allocations/components/HoldingsTable.all-columns.test.tsx`. Mirror the `next/navigation` + `next/link` mocks from `HoldingsTable.strategy-rows.test.tsx`. Render `HoldingsTable` in legacy mode and in design mode with minimal fixture rows (use the existing test fixtures/adapter as the analog). For each mode, query `getAllByRole("columnheader")` (or scope to the rendered `<thead>`) and assert: (a) the count equals the code constant intent (7 for legacy, 9 for design — anchor on the named material `<th>` set, count the Notes/Status icon columns by aria-label not text), (b) every named material header text is present, (c) no material `<th>` element has a className matching `/\bhidden\b|md:table-cell|truncate/`. Anchor on the verbatim label sets from PATTERNS.md (do NOT use the UI-SPEC's inverted "NEW"/"DESIGN" naming). After writing, manually delete one material `<th>` to confirm the guard FAILS, then restore it (CLAUDE.md Rule 12 falsifiability — note the proof in the SUMMARY).
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/allocations/components/HoldingsTable.all-columns.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - New file `HoldingsTable.all-columns.test.tsx` exists and passes
    - Legacy-mode test asserts 7 columns incl. the Notes aria-label column; design-mode test asserts 9 incl. the Status aria-label column
    - Guard asserts no material `<th>` carries `hidden`/`md:table-cell`/`truncate`
    - SUMMARY records the falsifiability proof (deleted a column → red → restored → green)
  </acceptance_criteria>
  <done>The all-columns guard for both highest-stakes holdings modes passes and is proven falsifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is pure presentation (CSS scroll wrapper + a render test). No new input, network, auth, or data flow crosses any boundary. Tables render the existing tenant-scoped payload unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01-ID | Information Disclosure | HoldingsTable column visibility | accept (mitigated by design) | The reshape SCROLLS columns (all present + reachable), never hides them; the all-columns guard fails loudly if a future `hidden md:table-cell` is added — a "smaller truth" is structurally blocked, not introduced. |
| T-46-01-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed this phase (RESEARCH Package Legitimacy Audit: N/A); `ResponsiveTable` is an existing in-repo Server Component. |
</threat_model>

<verification>
- `npx vitest run src/app/(dashboard)/allocations/components/HoldingsTable --reporter=dot` green (incl. the new guard).
- `npx tsc --noEmit` clean.
- Manual: at 320px the holdings tables scroll inside their region; the page does not overflow (proven app-wide by the Wave 2 reflow sweep, 46-05).
</verification>

<success_criteria>
- TABLE-01 (holdings family): 3 holdings tables + OpenPositionsTable wrapped in `ResponsiveTable`; no restyle; existing tests green.
- SC#2 (highest-stakes guard, holdings half): legacy-7 + design-9 all-columns render guard passes and is falsifiable.
</success_criteria>

<output>
Create `.planning/phases/46-surface-by-surface-reflow/46-01-SUMMARY.md` when done (record the falsifiability proof).
</output>
