---
phase: 46-surface-by-surface-reflow
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/page.tsx
  - src/app/(dashboard)/strategies/new/wizard/DesktopGate.tsx
  - src/app/(dashboard)/strategies/new/wizard/DesktopGate.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx
autonomous: true
requirements: [WIZARD-01]
must_haves:
  truths:
    - "Below 640px the wizard renders the REAL flow (not the email-capture block) — DesktopGate is gone and page.tsx renders the Suspense subtree directly"
    - "The server auth gate (supabase.auth.getUser → redirect /login) and the <Suspense key={source}> CSR-bailout boundary are preserved verbatim"
    - "The wizard stepper + step layout reflow to single-column at <sm with no horizontal page overflow and ≥44px nav controls"
    - "No new hydration warning (the de-block REMOVES a viewport branch); coverage ratchet measured-and-held after DesktopGate deletion"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/page.tsx"
      provides: "Suspense subtree rendered directly; DesktopGate import + wrapper removed; auth gate intact"
      contains: "Suspense key={source}"
    - path: "src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx"
      provides: "stepper grid reflows at <sm"
      contains: "grid-cols"
  key_links:
    - from: "page.tsx"
      to: "WizardClient via Suspense"
      via: "direct render (no DesktopGate wrapper)"
      pattern: "Suspense key=\\{source\\}"
    - from: "page.tsx"
      to: "/login redirect"
      via: "supabase.auth.getUser() server auth gate (PRESERVED)"
      pattern: "redirect\\(.*/login"
---

<objective>
De-block the onboarding / API-key wizard below 640px: delete `DesktopGate.tsx` (the desktop-only hard-block + email-capture fallback) and its test, render the `<Suspense key={source}>` subtree directly from `page.tsx`, and reflow the wizard's own layout CSS-first so it is usable at 320px — WITHOUT touching the server auth gate or the Suspense CSR-bailout boundary, and WITHOUT adding any new JS viewport branch.

Purpose: WIZARD-01 — a phone user must complete onboarding. The de-block removes a viewport branch (no hydration risk, SC#5), the auth gate stays (no privilege escalation), and the coverage ratchet must be MEASURED after the DesktopGate test deletion (not assumed).
Output: wizard renders real flow at all widths; DesktopGate + test deleted; stepper/layout reflowed; coverage measured green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md
@.planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md
@.planning/phases/46-surface-by-surface-reflow/46-UI-SPEC.md

<interfaces>
<!-- CODE-VERIFIED wizard structure. -->
From src/app/(dashboard)/strategies/new/wizard/page.tsx:
- `import { Suspense } from "react";` (line 1)
- `import { redirect } from "next/navigation";` (line 3)
- `import { DesktopGate } from "./DesktopGate";` (line 5) — REMOVE
- AUTH GATE (lines 67-72) — PRESERVE VERBATIM, DO NOT TOUCH:
  `const { data: { user } } = await supabase.auth.getUser();`
  `if (!user) redirect("/login?next=/strategies/new/wizard");`
- BEFORE (lines 92-122): `<DesktopGate>` wraps a load-bearing comment (93-118 explaining the CSR-bailout) + `<Suspense key={source} fallback={null}><WizardClient key={source} initialDraft={initialDraft} /></Suspense>`
- AFTER: drop the `<DesktopGate>` wrapper; render the `<Suspense key={source} fallback={null}>…</Suspense>` directly. The Suspense boundary + `key={source}` on BOTH Suspense and WizardClient are LOAD-BEARING (Next-16/React-19 CSR-bailout for useSearchParams) — preserve verbatim; re-anchor the 93-118 comment under the new top-level Suspense.

DELETE both:
- src/app/(dashboard)/strategies/new/wizard/DesktopGate.tsx (dead after de-block: matchMedia `(max-width:639px)`, two-pass isNarrow, email-capture branch 77-114, the `wizard_session_id:"desktop-gate"` POST)
- src/app/(dashboard)/strategies/new/wizard/DesktopGate.test.tsx (subject gone — COVERAGE-BEARING, see Task 3)

The `/api/for-quants-lead` route STAYS (other callers: src/app/for-quants/RequestCallModal.tsx). Only the wizard's `desktop-gate` POST value goes away. Do NOT relocate the deleted copy strings.

<!-- Layout reflow targets (CSS-first, NO new matchMedia, NO new breakpoint). -->
From src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx:81:
- `const gridColsClass = totalCount === 3 ? "grid-cols-3" : "grid-cols-4";` — the stepper grid has NO responsive prefix → overflow candidate at 320px.
Step grids already responsive (e.g. MetadataStep `grid-cols-1 md:grid-cols-3`, ConnectKeyStep `grid-cols-1 ... md:grid-cols-3`) — verify, fix only on overflow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: De-block the wizard — remove DesktopGate from page.tsx, delete the component + test</name>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/page.tsx (full file — esp. auth gate 67-72, Suspense block 92-122, comment 93-118)
    - src/app/(dashboard)/strategies/new/wizard/DesktopGate.tsx (full — confirm it's dead after de-block)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"Wizard de-block" (before/after)
    - .planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md §"Wizard De-Block" + §Security Domain
  </read_first>
  <files>src/app/(dashboard)/strategies/new/wizard/page.tsx, src/app/(dashboard)/strategies/new/wizard/DesktopGate.tsx (DELETE), src/app/(dashboard)/strategies/new/wizard/DesktopGate.test.tsx (DELETE)</files>
  <action>
    In `page.tsx`: remove the `import { DesktopGate } from "./DesktopGate";` line (5). Replace the `return (<DesktopGate> … </DesktopGate>);` block with `return (<Suspense key={source} fallback={null}><WizardClient key={source} initialDraft={initialDraft} /></Suspense>);` — keep the 93-118 CSR-bailout comment attached to the Suspense (re-anchor it directly above the now-top-level `<Suspense>`). DO NOT touch the auth gate at 67-72 (`supabase.auth.getUser()` + `redirect("/login?next=…")`) — it sits ABOVE the boundary and stays (RESEARCH Security: Elevation-of-Privilege must-not-regress). Then DELETE `DesktopGate.tsx` and `DesktopGate.test.tsx`. Run typecheck to catch any dangling import.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/app/(dashboard)/strategies/new/wizard --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f "src/app/(dashboard)/strategies/new/wizard/DesktopGate.tsx"` and `test ! -f ".../DesktopGate.test.tsx"` (both deleted)
    - `grep -c DesktopGate "src/app/(dashboard)/strategies/new/wizard/page.tsx"` === 0
    - `grep -c "Suspense key={source}" page.tsx` ≥ 1 AND `grep "redirect(\"/login" page.tsx` still present (auth gate intact)
    - `npx tsc --noEmit` clean; remaining wizard tests pass
  </acceptance_criteria>
  <done>Wizard renders the Suspense subtree directly at all widths; DesktopGate + test deleted; auth gate + Suspense boundary preserved verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Reflow the wizard layout CSS-first (stepper + step grids + nav at 320px)</name>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx (the stepper grid at 81 + footer/nav controls)
    - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, MetadataStep.tsx (existing grid-cols-1 md:grid-cols-* patterns)
    - .planning/phases/46-surface-by-surface-reflow/46-UI-SPEC.md §"2. Wizard De-Block" (layout reflow rules + broker-selector 3→1 col + ≥44px nav)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"Wizard layout reflow"
  </read_first>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx</files>
  <action>
    Reflow CSS-first using the project's existing Tailwind v4 responsive pattern — NO new breakpoint, NO new matchMedia, NO new JS branch. In `WizardChrome.tsx`: the stepper `gridColsClass` (`grid-cols-3`/`grid-cols-4` with no responsive prefix) must not force horizontal page overflow at 320px — apply the project's responsive collapse (e.g. a `grid-cols-1 sm:` prefix on the stepper grid, or allow it to wrap) so the stepper reflows; keep `aria-current="step"` and step labels. Verify the multi-column step grids (broker selector, metadata, connect-key) collapse to one column at `<sm` — those already use `grid-cols-1 md:grid-cols-*`; change ONLY if a specific grid overflows at 320px (prefer `sm:` to match the 640px de-block boundary where you add a prefix). Ensure inputs are `w-full` at `<sm` and the footer Back/Next/Submit controls remain visible + ≥44px tappable with no horizontal page overflow. Preserve every broker-card style (`border-accent bg-accent/5` active, 1px `#E2E8F0` border, 8px radius), the `Button bg-accent` primary CTA, `role="alert"`/`role="status"` live regions, and Tab/Shift+Tab DOM order verbatim — the reflow is layout-only.
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/strategies/new/wizard --reporter=dot && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The stepper grid in `WizardChrome.tsx` carries a responsive collapse (no bare fixed `grid-cols-3/4` that overflows at 320px)
    - No new `matchMedia`/`useMediaQuery`/`useBreakpoint` call added to any wizard file (`grep -rc "matchMedia\|useMediaQuery\|useBreakpoint" src/app/(dashboard)/strategies/new/wizard/` net-zero new sites)
    - Existing wizard unit tests + WizardChrome.test.tsx pass; `npx tsc --noEmit` clean
    - 320px page overflow proven absent by the Wave-2 sweep (46-05 adds the wizard route)
  </acceptance_criteria>
  <done>Wizard stepper + steps + nav reflow to single-column at <sm with no page overflow and ≥44px controls; a11y + broker-card styling preserved.</done>
</task>

<task type="auto">
  <name>Task 3: Measure the coverage ratchet after the DesktopGate deletion (the gate is the exit code, not a human read)</name>
  <read_first>
    - vitest.config.ts (the thresholds: lines 82 / stmts 80 / fns 74 / branches 72)
    - CLAUDE.md §"Test Coverage" (the ratchet is a BLOCKING CI gate; never lower a threshold or blanket-update a snapshot)
    - .planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md Pitfall 3 + Assumption A3 (MEDIUM: deleting a "use client" component + its test shifts both numerator and denominator)
  </read_first>
  <files>(measurement only — no file edited unless a metric regresses and focused coverage must be added)</files>
  <action>
    Run `npm run test:coverage` (== `npx vitest run --coverage`). The command EXITS NON-ZERO if any of the four ratchet metrics (lines 82 / stmts 80 / fns 74 / branches 72) is below threshold — the exit code IS the gate, so this is automated, not a human read. If it exits non-zero: do NOT lower a threshold and do NOT blanket-update a snapshot (CLAUDE.md). Add focused test coverage instead — the all-columns guards (46-01/46-02, same phase) already add covered lines; if a wizard branch lost coverage, add a small render assertion on a previously-uncovered branch until all four clear. Record the four measured numbers (lines/stmts/fns/branches) in the SUMMARY (RESEARCH A3 requires the net be MEASURED, not assumed).
  </action>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:coverage` exits 0 (all four ratchet metrics ≥ threshold)
    - No threshold lowered in `vitest.config.ts`; no snapshot blanket-updated (`git diff vitest.config.ts` shows no threshold change)
    - SUMMARY records the four measured coverage numbers
  </acceptance_criteria>
  <done>The coverage ratchet is measured green after the DesktopGate + test deletion; the four numbers are recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| unauthenticated user → wizard route | The server `supabase.auth.getUser()` gate in page.tsx is the trust boundary. The de-block must NOT weaken it — it sits above the Suspense boundary and stays. |
| client island → /api/for-quants-lead | The wizard STOPS posting `wizard_session_id:"desktop-gate"`; the route stays for legit callers. One fewer input form (the email-capture) — attack surface REDUCED. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-03-EoP | Elevation of Privilege | wizard/page.tsx auth gate during RSC-tree "simplification" | mitigate | Delete ONLY the `DesktopGate` wrapper; preserve `supabase.auth.getUser()` + `redirect("/login?next=…")` verbatim (acceptance gate greps for the redirect). The `(dashboard)` layout auth still applies. |
| T-46-03-V5 | (reduced) Input surface | deleted email-capture form | accept (improvement) | Removing the email-capture form removes one input surface; `/api/for-quants-lead` stays for its other callers, no new endpoint or data path. |
| T-46-03-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx tsc --noEmit` clean (no dangling DesktopGate import).
- `npx vitest run src/app/(dashboard)/strategies/new/wizard --reporter=dot` green.
- `npx playwright test e2e/wizard-hydration-probe.spec.ts e2e/wizard-axe.spec.ts` green (no new hydration warning, a11y clean) — run in the seeded context.
- `npm run test:coverage` exits 0 — all four ratchet metrics measured ≥ threshold (Task 3, the gate is the exit code).
</verification>

<success_criteria>
- WIZARD-01 / SC#3: wizard renders the real flow at all widths; DesktopGate + test deleted; auth gate + Suspense boundary preserved.
- SC#5: no new hydration warning (viewport branch removed); coverage ratchet MEASURED green after the DesktopGate deletion.
</success_criteria>

<output>
Create `.planning/phases/46-surface-by-surface-reflow/46-03-SUMMARY.md` when done (record the four measured coverage numbers).
</output>
