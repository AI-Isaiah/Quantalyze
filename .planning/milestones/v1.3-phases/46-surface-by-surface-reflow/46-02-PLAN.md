---
phase: 46-surface-by-surface-reflow
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx
  - src/components/admin/ComputeJobsTable.tsx
  - src/app/(dashboard)/allocations/components/ScenarioCompareTable.all-columns.test.tsx
  - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.all-columns.test.tsx
autonomous: true
requirements: [TABLE-01]
must_haves:
  truths:
    - "ScenarioCompareTable scrolls inside a ResponsiveTable region (gains role=region + sr-only hint) instead of a bare overflow-x-auto div"
    - "CorrelationMatrix scrolls horizontally inside a ResponsiveTable region at 320px; the N×N matrix stays usable (page provides vertical scroll)"
    - "The admin ComputeJobsTable is scroll-wrapped in ResponsiveTable"
    - "A render guard fails loudly if a scenario column / metric row, or a correlation row/column, is dropped"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx"
      provides: "raw overflow-x-auto div replaced with ResponsiveTable"
      contains: "ResponsiveTable"
    - path: "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx"
      provides: "raw overflow-auto div replaced with ResponsiveTable; inline hex preserved verbatim"
      contains: "ResponsiveTable"
    - path: "src/app/(dashboard)/allocations/components/ScenarioCompareTable.all-columns.test.tsx"
      provides: "fail-loud guard: Metric axis + every scenario col + every METRICS row"
      contains: "scenario-col"
    - path: "src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.all-columns.test.tsx"
      provides: "fail-loud guard: header <th> count === row-label count === N"
      contains: "correlation-matrix"
  key_links:
    - from: "ScenarioCompareTable.tsx"
      to: "@/components/ResponsiveTable"
      via: "import + replace raw div"
      pattern: "ResponsiveTable"
    - from: "CorrelationMatrix.tsx"
      to: "@/components/ResponsiveTable"
      via: "import + replace inner overflow-auto div"
      pattern: "ResponsiveTable"
---

<objective>
Migrate the 2 highest-stakes already-scrolling tables (`ScenarioCompareTable`, `CorrelationMatrix`) from a raw `overflow-*` div onto the `ResponsiveTable` Server Component so they gain the `role="region"` + focusable + sr-only-hint a11y contract, scroll-wrap the admin `ComputeJobsTable`, and add fail-loud all-columns render guards for the two highest-stakes tables.

Purpose: TABLE-01 — these two financial tables already scroll but lack the a11y region; migrating them onto the one canonical primitive gives keyboard/SR users reachable off-screen columns, and the guards block a future column drop. Admin tables get scroll-wrap only (internal, lower-stakes, no guard).
Output: 2 tables migrated + admin table wrapped + 2 new all-columns guards.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md
@.planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md
@.planning/phases/46-surface-by-surface-reflow/46-UI-SPEC.md

<interfaces>
<!-- The wrap primitive — Server Component. Use directly. -->
From src/components/ResponsiveTable.tsx:
- `export function ResponsiveTable({ children, hint }: { children: ReactNode; hint?: string })`
- Renders `<div className="overflow-x-auto" role="region" aria-label={label} tabIndex={0}>{children}</div>` — one-axis (overflow-X only).
- Import: `import { ResponsiveTable } from "@/components/ResponsiveTable";`
- Do NOT add a duplicate sr-only scroll node.

<!-- CODE-VERIFIED migration sites + test anchors. -->
From src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx:
- raw `<div className="overflow-x-auto">` at line 185 wraps `<table className="w-full text-sm">` at 186
- `data-testid={`scenario-col-${c.name}`}` at 195; `data-testid={`cell-${c.name}-${metric.key}`}` at 220; `data-testid={isWinner ? `winner-${metric.key}` : undefined}` at 224; `METRICS` array at line 70

From src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx:
- outer `<div className="flex flex-col gap-3" data-testid="correlation-matrix">` at line 172
- inner raw `<div className="overflow-auto">` at line 173 (TWO-AXIS) wraps `<table className="w-full border-collapse text-center" style={{ fontSize: 11 }}>` at 174
- `data-testid="corr-cell"` at 204; header `<th>` truncate maxWidth:80 title={n} with inline hex `#4A5568`; "No correlation data available" empty branch at line 166
- inline hex `#4A5568` / `#64748B` are a KNOWN non-token site — preserve VERBATIM (Rule 3, do not "tidy")

From src/components/admin/ComputeJobsTable.tsx:
- raw `<div className="overflow-x-auto"><table className="w-full text-sm">` at line 195-196
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate ScenarioCompareTable + CorrelationMatrix + admin ComputeJobsTable onto ResponsiveTable</name>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx (lines 185-281)
    - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx (lines 160-219)
    - src/components/admin/ComputeJobsTable.tsx (lines 195-end of table)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"Table migration" (both before/after) + §"Admin tables"
    - .planning/phases/46-surface-by-surface-reflow/46-RESEARCH.md Pitfall 4 (overflow-AUTO vs overflow-X + inline hex)
  </read_first>
  <files>src/app/(dashboard)/allocations/components/ScenarioCompareTable.tsx, src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.tsx, src/components/admin/ComputeJobsTable.tsx</files>
  <action>
    Add the `ResponsiveTable` import to all three files. In `ScenarioCompareTable.tsx`: replace the raw `<div className="overflow-x-auto">` at line 185 (and its matching closing `</div>`) with `<ResponsiveTable>…</ResponsiveTable>` — the `<table>` and every data-testid/winner-logic/METRICS row unchanged. In `CorrelationMatrix.tsx`: replace ONLY the INNER `<div className="overflow-auto">` at line 173 with `<ResponsiveTable>` — KEEP the outer `<div className="flex flex-col gap-3" data-testid="correlation-matrix">` wrapper; preserve every inline `style` hex (`#4A5568`/`#64748B`) verbatim and the `truncate maxWidth:80 title={n}` labels; per RESEARCH Pitfall 4 the matrix's vertical scroll now comes from the page (ResponsiveTable is one-axis) — this is intended. In `ComputeJobsTable.tsx`: replace the raw `<div className="overflow-x-auto">` at line 195 with `<ResponsiveTable>` (scroll-wrap only, NO guard). Restyle nothing in any file. Do NOT migrate the other low-stakes admin/factsheet tables (out of scope; optional executor discretion only if trivial and zero-risk).
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/allocations/components/ScenarioCompareTable src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix src/components/admin/ComputeJobsTable --reporter=dot && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ResponsiveTable` ≥ 2 in each of the 3 files (import + use)
    - Zero `overflow-x-auto`/`overflow-auto` raw `<div>` remaining at the migrated sites (replaced by the primitive)
    - CorrelationMatrix still contains the inline hex `#4A5568` and `#64748B` verbatim (`grep -c '#4A5568' CorrelationMatrix.tsx` unchanged from baseline) and the outer `data-testid="correlation-matrix"` wrapper is intact
    - All existing ScenarioCompareTable / CorrelationMatrix / ComputeJobsTable tests pass; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Both highest-stakes tables migrated onto `ResponsiveTable`; admin table scroll-wrapped; inline hex + data-testids preserved verbatim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add fail-loud all-columns guards for ScenarioCompareTable + CorrelationMatrix</name>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioCompareTable.test.tsx (existing render harness + fixtures to mirror)
    - src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.boundary.test.tsx (existing render harness)
    - src/components/MobileNav.test.tsx:82-103 (exact-set pinning style)
    - .planning/phases/46-surface-by-surface-reflow/46-PATTERNS.md §"All-columns guard" (the anchor tables)
  </read_first>
  <files>src/app/(dashboard)/allocations/components/ScenarioCompareTable.all-columns.test.tsx, src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.all-columns.test.tsx</files>
  <behavior>
    - ScenarioCompareTable: with ≥2 scenario columns rendered, the guard asserts the "Metric" axis `<th>` is present, every scenario column appears via `data-testid="scenario-col-{name}"` (count === number of scenarios), and every `METRICS`-row key is rendered. Deleting a scenario column or a METRICS row makes it FAIL.
    - CorrelationMatrix: with N strategies rendered, header `<th>` count === row-label `<td>` count === N (each name appears as BOTH a column header and a row label). `data-testid="correlation-matrix"` present; per-cell `data-testid="corr-cell"` count === N×N (or the rendered symmetric set). Dropping a row or column makes it FAIL. Label ellipsis (`truncate`+`title`) is NOT a drop — the guard polices PRESENCE, not ellipsis.
    - Both guards proven falsifiable (delete → red → restore → green).
  </behavior>
  <action>
    Create `ScenarioCompareTable.all-columns.test.tsx` (next to the component) and `CorrelationMatrix.all-columns.test.tsx` (under widgets/risk/). Mirror the existing test files' render setup + fixtures (do NOT bloat the existing tests — add focused NEW files). For ScenarioCompareTable: render with a fixture of ≥2 scenarios, assert the Metric axis `<th>`, `getAllByTestId(/^scenario-col-/)` length === scenario count, and assert each `METRICS` key renders a row. For CorrelationMatrix: render with N≥3 strategies, assert header-`<th>` count === row-label count === N and that each strategy name is present as both a header and a row label (anchor on the names; ellipsis OK). For both, after writing, delete a column/row to confirm RED, then restore (record the proof in SUMMARY).
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/allocations/components/ScenarioCompareTable.all-columns.test.tsx src/app/(dashboard)/allocations/widgets/risk/CorrelationMatrix.all-columns.test.tsx --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - Both new `*.all-columns.test.tsx` files exist and pass
    - ScenarioCompareTable guard asserts `scenario-col-*` count === scenario count AND every METRICS row present
    - CorrelationMatrix guard asserts header count === row-label count === N
    - SUMMARY records both falsifiability proofs
  </acceptance_criteria>
  <done>Both all-columns guards pass and are proven falsifiable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure presentation (scroll-wrapper swap + render tests). No new input, network, auth, or data flow. Tables render existing tenant-scoped payloads unchanged. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-02-ID | Information Disclosure | ScenarioCompareTable / CorrelationMatrix column visibility | accept (mitigated by design) | Reshape SCROLLS columns (all present + reachable), never hides them; the all-columns guards fail loudly on a future column drop — a "smaller truth" is structurally blocked. |
| T-46-02-T | Tampering | CorrelationMatrix inline-hex "tidy" temptation | accept | Rule 3 — preserve the known non-token `#4A5568`/`#64748B` verbatim; acceptance gate greps for their continued presence so a drift fails. |
| T-46-02-SC | Tampering | npm/pip/cargo installs | n/a | Zero packages installed (RESEARCH audit N/A). |
</threat_model>

<verification>
- `npx vitest run` over the 3 migrated components + 2 new guards green.
- `npx tsc --noEmit` clean.
- CorrelationMatrix inline hex + `data-testid` intact; N×N still scrolls horizontally at 320px (confirmed app-wide by 46-05).
</verification>

<success_criteria>
- TABLE-01 (scenario/risk/admin): ScenarioCompareTable + CorrelationMatrix migrated onto `ResponsiveTable`; admin ComputeJobsTable scroll-wrapped.
- SC#2 (highest-stakes guard, scenario/risk half): both all-columns guards pass and are falsifiable.
</success_criteria>

<output>
Create `.planning/phases/46-surface-by-surface-reflow/46-02-SUMMARY.md` when done (record both falsifiability proofs).
</output>
