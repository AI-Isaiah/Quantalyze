---
phase: 48-recharts-equitychart-final-verification
plan: 03
type: execute
wave: 1
depends_on: ["48-01"]
files_modified:
  - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx
  - src/app/(dashboard)/allocations/widgets/performance/EquityChart.touch.test.tsx
autonomous: true
requirements: [CHART-01b]
user_setup: []

must_haves:
  truths:
    - "A tap on EquityChart pins the same value the desktop hover reveals (pointerToIndex reuses the exact handleMove px→epoch→nearestIndex chain)"
    - "Re-tap toggles the pin off; a tap elsewhere moves it; a pinned reveal survives pointerleave (TimeSeriesChart semantics via useTapPin)"
    - "The desktop onMouseMove/handleMove/hoverIdx mouse path is byte-identical (additive touch path, never a replacement)"
    - "The measured-width/ResizeObserver path and the projection useMemo keying are untouched"
    - "The tap hit area measures ≥44px under pointer-coarse"
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx"
      provides: "Additive useTapPin touch path wired onto the existing <svg> + nearestIndex binary-search"
      contains: "useTapPin"
    - path: "src/app/(dashboard)/allocations/widgets/performance/EquityChart.touch.test.tsx"
      provides: "Unit proof that pointerToIndex maps px→nearestIndex identically to handleMove"
      contains: "nearestIndex"
  key_links:
    - from: "src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx"
      to: "src/hooks/useTapPin.ts"
      via: "useTapPin({ count: n, pointerToIndex }) + setChartEl + onPointer* on the existing <svg>"
      pattern: "useTapPin\\("
    - from: "EquityChart pointerToIndex"
      to: "nearestIndex(visibleEpochs, targetEpoch)"
      via: "same clamp + epoch + binary-search math as handleMove (L1142-1159)"
      pattern: "nearestIndex\\(visibleEpochs"
---

<objective>
Wire CHART-01b touch parity into the 2277-LOC hand-rolled `EquityChart` WITHOUT a rewrite: integrate the existing Phase-47 `useTapPin` hook additively onto the existing `<svg>`, mapping its `pointerToIndex(clientX, clientY, rect)` to the exact `px → clampedPx → targetEpoch → nearestIndex(visibleEpochs, targetEpoch)` chain `handleMove` already runs — so a tap pins the value the desktop hover shows. The desktop mouse path stays byte-identical.

Purpose: EquityChart is the live-book Overview chart and the highest-regression-risk surface in the milestone. Rewriting it to the viewBox pattern is a banned anti-feature (REQUIREMENTS Out of Scope). The touch path is PURELY additive (`useTapPin` owns its own `selectedIdx`/`pinned` state, rendered alongside `hoverIdx`); the measured-width path and the projection memo keying are NOT touched (D-Area-2). The falsifiable proof of "no rewrite" is the byte-identical desktop mouse path.
Output: EquityChart.tsx with an additive `useTapPin` touch path + the EquityChart.touch.test.tsx parity test (the plan-01 scaffold, now satisfied).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md
@.planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md
@.planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md

<interfaces>
From src/hooks/useTapPin.ts (VERIFIED — the hook is NOT modified, only consumed):
```typescript
export interface UseTapPinOptions {
  count: number;                                                   // selectable indices; selectedIdx clamped to [0, count-1]
  pointerToIndex: (clientX: number, clientY: number, rect: DOMRect) => number | null;  // null = off-grid (un-pin)
}
export function useTapPin(opts: UseTapPinOptions): {
  selectedIdx: number | null; pinned: boolean;
  setChartEl: (el: Element | null) => void;                        // callback ref (no .current mutation — react-compiler-safe)
  onPointerDown; onPointerMove; onPointerUp; onPointerLeave;       // typed ReactPointerEvent<SVGSVGElement>
};
// tap-vs-drag: TAP_SLOP_SQ=64 px², TAP_MAX_MS=350ms; re-tap within RETAP_THRESHOLD=3 idx toggles pin off.
```

The EXISTING hover math the touch path MUST mirror (EquityChart.tsx L1142-1159):
```tsx
function handleMove(e: React.MouseEvent<SVGSVGElement>) {
  if (n === 0) return;
  const rect = e.currentTarget.getBoundingClientRect();
  const px = e.clientX - rect.left;
  if (n === 1) { setHoverIdx(0); return; }
  const clampedPx = Math.max(pad.l, Math.min(pad.l + chartW, px));
  const targetEpoch = firstEpochX + ((clampedPx - pad.l) / chartW) * totalMs;
  setHoverIdx(nearestIndex(visibleEpochs, targetEpoch));
}
// The <svg> at L1425-1429: role="img" aria-label="Equity chart"
//   onMouseMove={handleMove} onMouseLeave={() => setHoverIdx(null)}   ← KEEP BYTE-IDENTICAL
// The reveal block at L1589+ is keyed on `hoverIdx != null && hoverIdx < n`.
```

The LIVE analog (the single best template — useTapPin onto a hand-rolled surface):
src/app/factsheet/[id]/v2/HeatmapPanels.tsx — useTapPin call shape L398-451, mobile gate L263, reveal precedence (pinned ?? hovered) L455, pointer-coarse:min-h-[44px] floor L474.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract/define pointerToIndex and prove px→nearestIndex parity with handleMove (RED→GREEN)</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx, src/app/(dashboard)/allocations/widgets/performance/EquityChart.touch.test.tsx</files>
  <behavior>
    - Test 1: for a given (clientX, rect, pad.l, chartW, firstEpochX, totalMs, visibleEpochs), `pointerToIndex(clientX, _y, rect)` returns the SAME index that `handleMove`'s px→clampedPx→targetEpoch→nearestIndex chain produces for the same clientX.
    - Test 2: n===0 → pointerToIndex returns null (no selectable index).
    - Test 3: n===1 → pointerToIndex returns 0.
    - Test 4: a clientX left of pad.l clamps to the first index; a clientX right of pad.l+chartW clamps to the last index (same clamp as handleMove).
  </behavior>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§EquityChart — the handleMove math at L1142-1159 the touch path mirrors; RESEARCH Pattern 2 code block)
    - .planning/phases/48-recharts-equitychart-final-verification/48-VALIDATION.md (CHART-01b row: "EquityChart pointerToIndex maps px→nearestIndex identically to handleMove — extract pointerToIndex pure-fn or mount+synthetic pointer")
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx (L46 nearestIndex impl; L1142-1159 handleMove; L502 hoverIdx; L1425-1429 svg handlers; L1589+ reveal; L517-528 ResizeObserver DO-NOT-TOUCH; L670 projection useMemo DO-NOT-DISTURB)
    - src/app/factsheet/[id]/v2/HeatmapPanels.tsx (L398-451 the live useTapPin call shape whose pointerToIndex reuses the same coord→payload math as the hover path)
    - src/hooks/useTapPin.test.ts (the unit-test idiom for the hook's consumers)
  </read_first>
  <action>
    First (RED): expand `EquityChart.touch.test.tsx` (the plan-01 skipped scaffold) into real tests covering the four behaviors above, importing whatever surface Task makes testable. To make `pointerToIndex` unit-testable WITHOUT mounting the 2277-LOC component, extract the px→clampedPx→targetEpoch→nearestIndex mapping into a small pure helper alongside `nearestIndex` (e.g. `epochIndexFromPx(px, { padL, chartW, firstEpochX, totalMs, visibleEpochs, n })`) and have BOTH `handleMove` and the new `pointerToIndex` call it — so the two paths share one implementation by construction (the parity is structural, not asserted-by-coincidence). Run the test; it must FAIL before the helper exists. Then (GREEN): add the pure helper, refactor `handleMove` to call it (the refactor must keep `handleMove`'s observable behavior byte-identical — `setHoverIdx(nearestIndex(...))` for the same input), and define `pointerToIndex(clientX, _clientY, rect)` to compute `px = clientX - rect.left` then return `epochIndexFromPx(...)` (returning null when n===0). Run the test; it must pass. Do NOT touch the ResizeObserver/measured-width path (L517-528) or add anything to the projection `useMemo` deps (L670).
  </action>
  <verify>
    <automated>npx vitest run src/app/\(dashboard\)/allocations/widgets/performance/EquityChart.touch.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - The four behaviors above are asserted and green.
    - Both `handleMove` and `pointerToIndex` route through ONE shared pure helper (`grep -c "epochIndexFromPx\|<the shared helper name>" EquityChart.tsx` ≥ 2 call sites).
    - `git diff EquityChart.tsx` shows NO change to the ResizeObserver block (L517-528) and NO addition to the projection `useMemo` dep array (L670 region).
    - Existing EquityChart tests still pass: `npx vitest run "src/app/(dashboard)/allocations/widgets/performance/EquityChart.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.v2.test.tsx"`.
  </acceptance_criteria>
  <done>pointerToIndex shares handleMove's exact mapping via one pure helper; parity proven; do-not-touch regions untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Wire useTapPin additively onto the existing <svg> + render the pinned reveal + ≥44px coarse hit layer</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§EquityChart — "Attaching to the EXISTING <svg>" KEEP the two mouse handlers + ADD ref + pointer handlers; "The reveal site" render the same reveal when (hoverIdx ?? tap.selectedIdx) is set; "DO-NOT-TOUCH guardrails")
    - .planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md (§Interaction Contract — EquityChart: tap pins, re-tap toggles, pin survives pointerleave, no auto-dismiss, hit target ≥44px under pointer-coarse, desktop mouse path BYTE-IDENTICAL)
    - src/app/factsheet/[id]/v2/HeatmapPanels.tsx (L398-451 useTapPin call shape; L455 reveal = pinned ?? hovered; L474 className pointer-coarse:min-h-[44px])
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx (L1425-1429 the <svg>; L1589+ the hoverIdx reveal block to also drive from selectedIdx)
  </read_first>
  <action>
    Add `import { useTapPin } from "@/hooks/useTapPin";` and (if not already imported) `import type { PointerEvent as ReactPointerEvent } from "react";`. Call `const tap = useTapPin({ count: n, pointerToIndex: (clientX, _clientY, rect) => pointerToIndex(...) })` using the Task-1 mapping. On the EXISTING `<svg>` (L1425-1429): KEEP `onMouseMove={handleMove}` and `onMouseLeave={() => setHoverIdx(null)}` BYTE-IDENTICAL, and ADD `ref={tap.setChartEl}`, `onPointerDown={tap.onPointerDown}`, `onPointerMove={tap.onPointerMove}`, `onPointerUp={tap.onPointerUp}`, `onPointerLeave={tap.onPointerLeave}` (the handlers are typed `ReactPointerEvent<SVGSVGElement>` — the `<svg>` types line up directly, no cast needed). Change the reveal block (L1589+) gate from `hoverIdx != null && hoverIdx < n` to a `reveal = hoverIdx ?? tap.selectedIdx` precedence (mirror HeatmapPanels L455: pinned ?? hovered) so a pinned tap renders the SAME crosshair + dot + value the hover reveal renders — reading the SAME precomputed values, NO recompute. Add the `pointer-coarse:` ≥44px hit floor (mirror HeatmapPanels L474 `className="... pointer-coarse:min-h-[44px]"`) on the chart's interactive wrapper so the tap rect measures ≥44px on touch while the desktop `pointer-fine` mouse path is unchanged. Do NOT add `selectedIdx`/`pinned` to any existing `useMemo` dep array (Pitfall 7). Do NOT touch the ResizeObserver/measured-width path. Surgical, additive-only (Rule 3).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/EquityChart.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.v2.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.boundary.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.tweaks.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useTapPin(" EquityChart.tsx` returns 1; the `<svg>` carries `ref={tap.setChartEl}` and all four `onPointer*` handlers AND still carries the unchanged `onMouseMove={handleMove}` / `onMouseLeave`.
    - The reveal is driven by `hoverIdx ?? tap.selectedIdx` (or equivalent pinned-then-hovered precedence); `grep -q "selectedIdx" EquityChart.tsx`.
    - A `pointer-coarse:min-h-[44px]` (or equivalent ≥44px coarse-only) class is present on the interactive wrapper.
    - `git diff EquityChart.tsx` shows the ResizeObserver block (L517-528) and projection useMemo dep array (L670 region) UNCHANGED; the desktop mouse handlers UNCHANGED.
    - All listed EquityChart tests pass (desktop mouse-path behavior byte-identical).
  </acceptance_criteria>
  <done>useTapPin wired additively; pinned reveal renders the same values as hover; ≥44px coarse hit layer present; desktop path + do-not-touch regions unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Verify small-width legibility floor (intervene only if breached) + coverage ratchet</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md (§Typography — "EquityChart: bump axis font / reduce tick density ONLY if the ≈12px-at-320px floor is breached"; §Portrait — verify only, reduce tick density at the mobile breakpoint only if needed, NEVER downsample data)
    - .planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md (§Project Constraints — coverage ratchet; D-Area-2 "small-width legibility = minimal")
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx (the smart x-tick density loop + the hand-rolled <text> tick rendering — measure effective size at a 320px CSS viewport)
    - src/hooks/useBreakpoint.ts (the mobile gate to use IF a tick-density reduction is genuinely needed — width breakpoint, never an orientation media query)
  </read_first>
  <action>
    Determine whether EquityChart's effective axis/label `<text>` size at a 320px CSS viewport already clears the ≈12px legibility floor (it runs a smart x-tick density loop + a measured-width ResizeObserver floored at 400px). If it ALREADY clears ≈12px effective: make NO code change here and DOCUMENT in the SUMMARY that the floor is met (the contract is "intervene only if breached"). ONLY if the effective text falls below ≈12px: reduce tick density at the `useBreakpoint() === "mobile"` arm (a width breakpoint, not orientation) — prefer reducing tick COUNT over shrinking font — and NEVER downsample data points (banned anti-feature / frozen-math guard). Then run the full coverage gate; if any new branch from the touch path is uncovered, add the assertion to EquityChart.touch.test.tsx (do NOT lower a threshold, do NOT blanket-update a snapshot). Record the new actual coverage numbers in the SUMMARY.
  </action>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY states explicitly whether the ≈12px floor was already met (no change) or was breached (tick-density reduced at the mobile breakpoint, data untouched).
    - If a change was made, `grep -q "useBreakpoint" EquityChart.tsx` and NO orientation media query is introduced; no data-point downsampling (`git diff` shows no change to the series/projection arrays).
    - `npm run test:coverage` exit 0 with lines ≥82, stmts ≥80, fns ≥74, branches ≥72; actuals recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Legibility floor verified (changed only if breached, data never downsampled); coverage ratchet held with recorded actuals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none net-new) | Additive client-side pointer handling on an existing SVG; no input validation, data, auth, or persistence surface crosses a boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-03-BI | Tampering | desktop mouse path (byte-identity) | mitigate | The touch path is additive; `onMouseMove`/`onMouseLeave`/`hoverIdx` stay byte-identical — proven by the existing EquityChart test suite staying green (the no-rewrite proof). |
| T-48-03-PERF | Denial of Service (self-inflicted regression) | projection useMemo / ResizeObserver | mitigate | Do NOT add `selectedIdx`/`pinned` to any existing `useMemo` deps and do NOT touch the ResizeObserver path (Pitfall 7) — guarded by the diff-scoped acceptance criteria; re-introducing the per-pixel hover regression is the failure mode this prevents. |
| T-48-03-MATH | Tampering | frozen-math / no-recompute | mitigate | The pinned reveal reads the SAME precomputed values the hover reveal reads (no recompute); `pointerToIndex` reuses the exact `nearestIndex` mapping; SCENARIO-05 frozen-spine guard stays green. |
</threat_model>

<verification>
- `npx vitest run src/app/\(dashboard\)/allocations/widgets/performance/EquityChart.touch.test.tsx` green (parity proof).
- The full EquityChart test suite green (desktop byte-identical): EquityChart.test.tsx + .v2 + .boundary + .tweaks + .scenario.
- `npm run test:coverage` exit 0, all four thresholds held, actuals recorded.
- Frozen-spine guard green: `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts`.
- `git diff EquityChart.tsx` confirms ResizeObserver + projection memo deps + desktop mouse handlers unchanged.
</verification>

<success_criteria>
- A tap pins the same value desktop hover shows; re-tap toggles, tap-elsewhere moves, pin survives pointerleave.
- Desktop mouse path byte-identical; ResizeObserver/projection-memo keying untouched.
- ≥44px coarse-only tap hit layer present.
- Legibility floor verified (data never downsampled); coverage ratchet held.
</success_criteria>

<output>
Create `.planning/phases/48-recharts-equitychart-final-verification/48-03-SUMMARY.md` when done
</output>
