---
phase: 48-recharts-equitychart-final-verification
plan: 05
type: execute
wave: 3
depends_on: ["48-04"]
files_modified:
  - lighthouserc.json
  - .github/workflows/ci.yml
  - e2e/reflow-sweep-authed.spec.ts
  - .planning/phases/48-recharts-equitychart-final-verification/48-HUMAN-UAT.md
autonomous: false
requirements: [A11Y-03]
user_setup: []

must_haves:
  truths:
    - "A mobile-form-factor @lhci/cli budget gates the 5 public routes in CI via a new lighthouse-mobile job (build/restore -> start -> lhci autorun -> assert), error-level"
    - "lighthouserc thresholds are seeded from a real baseline run, set a few points UNDER measured actual (no hard 90+ day one)"
    - "The lighthouse-mobile job receives NO TEST_SUPABASE_* secrets (public routes only -> no info disclosure to temporary-public-storage)"
    - "A rotate-stability assertion (no ResizeObserver-loop console error + stable memory on rotate) is folded into an existing mobile e2e -- no new harness"
    - "A 48-HUMAN-UAT.md real-device authed checklist exists; verification ends human_needed"
    - "The coverage ratchet is verified held and all frozen-math/byte-identity/parity guards are green un-weakened"
  artifacts:
    - path: "lighthouserc.json"
      provides: "Mobile lhci budget with baseline-seeded error-level thresholds on public routes"
      contains: "minScore"
    - path: ".github/workflows/ci.yml"
      provides: "New lighthouse-mobile job (build/restore -> start -> lhci autorun)"
      contains: "lighthouse-mobile"
    - path: ".planning/phases/48-recharts-equitychart-final-verification/48-HUMAN-UAT.md"
      provides: "Real-device authed sign-off checklist (SC#5)"
      contains: "human_needed"
  key_links:
    - from: ".github/workflows/ci.yml lighthouse-mobile job"
      to: "lighthouserc.json"
      via: "npx lhci autorun against npm run start (production .next)"
      pattern: "lhci autorun"
    - from: "e2e/reflow-sweep-authed.spec.ts"
      to: "rotate-stability assertion"
      via: "page.on('console') ResizeObserver-loop check + stable-memory-on-rotate"
      pattern: "ResizeObserver loop"
---

<objective>
Stand up A11Y-03: a `@lhci/cli` mobile performance budget gating the 5 public routes in CI (a new `lighthouse-mobile` job: build/restore -> `npm run start` -> `lhci autorun` -> assert), with thresholds seeded from a real baseline run a few points under measured actual; fold a rotate-stability assertion (no ResizeObserver-loop console error + stable memory on rotate) into an existing mobile e2e; author the `48-HUMAN-UAT.md` real-device authed checklist; and verify the coverage ratchet held and all frozen-math/byte-identity/parity guards are green un-weakened.

Purpose: This closes the falsifiable "v1.3 done" matrix. lhci gates PUBLIC routes only (headless can't auth -- the authed surface is covered by the human real-device sign-off, a documented limitation). Thresholds follow the coverage-ratchet philosophy (seed lenient under baseline, ratchet over time -- no hard 90+ day one, which flakes red). The lighthouse-mobile job gets NO Supabase secrets (Security §). Verification ends `human_needed` (D-Area-4).
Output: a baseline-seeded `lighthouserc.json`, the `lighthouse-mobile` CI job, a rotate-stability fold, `48-HUMAN-UAT.md`, and a recorded final-guards/ratchet verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md
@.planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md
@.planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md
@.planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-HUMAN-UAT.md

<interfaces>
lighthouserc.json schema (RESEARCH Pattern 4 -- minScore is a PLACEHOLDER until baselined):
collect: startServerCommand "npm run start", startServerReadyPattern "ready|started|listening", startServerReadyTimeout 30000, url = the 5 public localhost:3000 routes (/, /security, /for-quants, /browse, /demo), numberOfRuns 3, settings.preset "mobile".
assert: preset "lighthouse:no-pwa", assertions { "categories:performance": ["error", { "minScore": <SEED FROM BASELINE> }] }.
upload: target "temporary-public-storage".

CI build-artifact-restore + start-server shape to copy (ci.yml ~L1040-1059):
- actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c (v8.0.1) with name nextjs-build -> restores .next + public at cwd
- npm run start & + a `for i in $(seq 1 30)` curl-:3000 wait loop
- Next 16: production build -> .next, next start -> port 3000 (Pitfall 6 -- must run prod build, never next dev)

Rotate-stability fold (RESEARCH A5 -- additive, into the ALREADY-seeded reflow-sweep-authed.spec.ts):
page.on("console", m => if m.type()==="error" push m.text()); rotate portrait->landscape->portrait via setViewportSize;
expect(consoleErrors.filter(t => /ResizeObserver loop/i.test(t))).toEqual([]).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run a baseline lhci, seed lighthouserc thresholds under measured, add the lighthouse-mobile CI job</name>
  <files>lighthouserc.json, .github/workflows/ci.yml</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§lighthouserc.json -- config shape, minScore is a placeholder to seed from a real baseline; §.github/workflows/ci.yml -- the new lighthouse-mobile job copies the build-artifact-restore + start-server shape, NO TEST_SUPABASE_* secrets)
    - .planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md (§Pitfall 6 -- run against production .next via npm run start, never next dev; §Security Domain -- public routes only, no secret, do not pass TEST_SUPABASE_* to this job; §Pattern 4)
    - .github/workflows/ci.yml (the e2e job's actions/download-artifact restore + npm run start & + curl-:3000 wait loop at ~L1040-1059, and the placeholder NEXT_PUBLIC_* env block at ~L1106-1109 to mirror; the aggregator/branch-protection job set)
    - node_modules/next/dist/docs/01-app/03-api-reference/06-cli/next.md (AGENTS.md docs-first -- confirm next build -> .next, next start -> :3000 before wiring)
  </read_first>
  <action>
    First run a real baseline: `npm run build` then `npx lhci autorun` (or `npx lhci collect` + `npx lhci assert --preset lighthouse:no-pwa`) against the production `next start` on the 5 public routes, mobile preset, to obtain the measured performance scores. Record the per-route measured `categories:performance` scores in the SUMMARY. Then set `lighthouserc.json`'s `minScore` to a value a few points UNDER the lowest measured route score (coverage-ratchet philosophy -- e.g. if the lowest measured is 0.78, seed ~0.72; NEVER a hard 0.90+ day one, which flakes red) as an `error`-level assertion; remove the plan-01 PLACEHOLDER comment and replace it with a comment recording the baseline date + measured floor. Keep public routes only (no authed URL -> no info-disclosure to temporary-public-storage). Then add a new `lighthouse-mobile` job to `.github/workflows/ci.yml`: copy the `actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (v8.0.1, name `nextjs-build`) restore step, add a step that runs `npm run start &` + the curl-:3000 wait loop then `npx lhci autorun`, and mirror the placeholder `NEXT_PUBLIC_*` env block (public routes render on placeholder env). Do NOT pass `TEST_SUPABASE_URL`/`TEST_SUPABASE_SERVICE_ROLE_KEY` (or any secret) into this job's env. The job needs the `frontend-build` artifact like the e2e job does. Wire the job into the aggregator/branch-protection set the same way the existing e2e/coverage jobs are (so it is BLOCKING).
  </action>
  <verify>
    <automated>node -e "const c=JSON.parse(require('fs').readFileSync('lighthouserc.json','utf8'));const m=c.ci.assert.assertions['categories:performance'][1].minScore;if(typeof m!=='number'||m<=0||m>=0.9)throw new Error('minScore not baselined <0.9: '+m);console.log('minScore',m)" && grep -c "lighthouse-mobile" .github/workflows/ci.yml && python3 -c "import yaml;yaml.safe_load(open('.github/workflows/ci.yml'))"</automated>
  </verify>
  <acceptance_criteria>
    - `lighthouserc.json` minScore is a numeric baseline-derived value strictly between 0 and 0.9 (not the 0.5/0.70 placeholder, not a hard 90+); the per-route baseline measurements are recorded in 48-05-SUMMARY.md.
    - `grep -c "lighthouse-mobile" .github/workflows/ci.yml` >= 1; the YAML parses (`python3 -c "import yaml;yaml.safe_load(...)"` exit 0).
    - The lighthouse-mobile job env contains NO `TEST_SUPABASE_URL` / `TEST_SUPABASE_SERVICE_ROLE_KEY` (`grep -A40 "lighthouse-mobile" .github/workflows/ci.yml | grep -c "TEST_SUPABASE"` returns 0).
    - The job restores the `nextjs-build` artifact and runs `npx lhci autorun` against `npm run start` (production), never `next dev`.
    - `lighthouserc.json` `url` list contains only the 5 public routes (no `/allocations`, `/strategy`, `/discovery`, `/factsheet`, `/strategies/new` URL).
  </acceptance_criteria>
  <done>lighthouserc minScore baselined a few points under measured; lighthouse-mobile job added (production build, no Supabase secret, blocking); baseline recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Fold the rotate-stability assertion into reflow-sweep-authed.spec.ts (no new harness)</name>
  <files>e2e/reflow-sweep-authed.spec.ts</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§e2e/reflow-sweep-authed.spec.ts -- the fold pattern: page.on("console") + rotate via setViewportSize + the ResizeObserver-loop assertion; this spec is ALREADY in the seeded MA-8 list so an additive fold needs NO new FLOW-01 wiring)
    - .planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md (§A5 -- the fold is additive; §A11Y-03 contract -- fold into an existing mobile e2e, not a new harness; §SC#4)
    - e2e/reflow-sweep-authed.spec.ts (the existing seed+login idiom + HAS_SEED_ENV self-skip + a route that mounts a ResizeObserver-driven chart, e.g. /allocations with EquityChart)
    - e2e/composer-axe.spec.ts (the page.on("console")/pageerror listener idiom + seed+login on an authed route)
  </read_first>
  <action>
    Add an additive test (or augment an existing authed test) in `e2e/reflow-sweep-authed.spec.ts`: register a `page.on("console", m => { if (m.type()==="error") consoleErrors.push(m.text()); })` listener (and optionally a `pageerror` listener) BEFORE navigation, seed+login via the spec's existing helper, navigate to an authed route that mounts a ResizeObserver-driven chart (`/allocations`, which mounts EquityChart), then rotate the viewport portrait -> landscape -> portrait via `setViewportSize` (e.g. 375x812 -> 812x375 -> 375x812) with a brief settle between each. Assert `consoleErrors.filter(t => /ResizeObserver loop/i.test(t))` equals `[]` (no ResizeObserver-loop error). For "stable memory on rotate" (SC#4): assert a BOUNDED condition, not a hard byte count -- e.g. if `performance.memory` is available read `usedJSHeapSize` before and after the rotate cycle and assert growth stays within a generous bound (and skip the memory assertion gracefully if `performance.memory` is undefined in the runner). Keep it additive -- do NOT create a new spec or harness. Because this spec is already in the seeded MA-8 list and already HAS_SEED_ENV-gated, no ci.yml change is needed.
  </action>
  <verify>
    <automated>grep -c "ResizeObserver loop" e2e/reflow-sweep-authed.spec.ts && npx playwright test e2e/reflow-sweep-authed.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ResizeObserver loop" e2e/reflow-sweep-authed.spec.ts` >= 1 and a `page.on("console"` listener is present.
    - The rotate cycle uses `setViewportSize` portrait -> landscape -> portrait.
    - `--list` shows the new/augmented rotate-stability case under the existing HAS_SEED_ENV gate (self-skips unseeded).
    - No new spec file created and no ci.yml edit in this task (the host spec is already dual-wired).
  </acceptance_criteria>
  <done>Rotate-stability (no ResizeObserver-loop error + bounded memory) folded into the already-seeded reflow-sweep-authed spec; no new harness, no new CI wiring.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Author 48-HUMAN-UAT.md + verify ratchet held + all frozen-math/parity guards green (phase exit)</name>
  <files>.planning/phases/48-recharts-equitychart-final-verification/48-HUMAN-UAT.md</files>
  <action>Author `.planning/phases/48-recharts-equitychart-final-verification/48-HUMAN-UAT.md` by copying the Phase-47 47-HUMAN-UAT.md frontmatter + section shape (## Current Test / ## Tests / ## Summary / ## Gaps); fill it with the real-device authed checklist (tap-to-pin on a Recharts Line + Bar + Pie chart AND EquityChart: tap reveals AND pins, re-tap toggles off, >=44px hit ergonomics; no horizontal overflow at 320px; no ResizeObserver-loop console error; stable memory on rotate). Then run the automated phase-gate verification (coverage ratchet held; SCENARIO-05 frozen-spine; accessibilityLayer grep; svg-chart-parity NOT made false-green). Then PAUSE: verification ends `human_needed` — the real-device authed walkthrough on a physical phone (preview/prod after deploy) is the SC#5 sign-off the human performs and approves.</action>
  <read_first>
    - .planning/phases/47-hand-rolled-svg-charts-touch-legibility-portrait/47-HUMAN-UAT.md (the analog -- copy its frontmatter + ## Current Test / ## Tests / ## Summary / ## Gaps shape exactly; its deferred item #2 "real-device authed walkthrough ... formally Phase 48 SC#5" is the carry-forward this file fulfills)
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§48-HUMAN-UAT.md -- content to cover)
    - .planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md (§Copywriting -- the UAT row; §SC#5; §Frozen-math/byte-identity/parity guards table)
    - .planning/phases/48-recharts-equitychart-final-verification/48-VALIDATION.md (the guard rows: SCENARIO-05, accessibilityLayer grep, svg-chart-parity no-false-green, coverage ratchet)
  </read_first>
  <what-built>
    Authored `.planning/phases/48-recharts-equitychart-final-verification/48-HUMAN-UAT.md` (mirroring the Phase-47 UAT shape) and ran the full automated phase-gate verification: coverage ratchet held, all frozen-math/byte-identity/parity guards green un-weakened. The UAT checklist covers the authed surfaces headless cannot hydrate -- it is the SC#5 real-device sign-off that makes verification end `human_needed`.
  </what-built>
  <how-to-verify>
    1. Confirm `48-HUMAN-UAT.md` exists with: frontmatter + a checklist covering -- tap-to-pin works on a Recharts Line + a Bar + the Pie (CompositionDonut) chart AND on EquityChart (tap reveals AND pins; re-tap toggles off; >=44px hit ergonomics); no horizontal overflow at 320px; no ResizeObserver-loop console error; stable memory on rotate; and a statement that verification ends `human_needed`.
    2. Confirm the automated guards are all green (the commands in <verify>): coverage ratchet held; SCENARIO-05 frozen-spine; accessibilityLayer grep; svg-chart-parity NOT made false-green (still self-skips loudly, no placeholder goldens).
    3. On a REAL phone (preview/prod after deploy), run the checklist and record pass/fail per item; this is the SC#5 sign-off (the milestone-level acceptance).
  </how-to-verify>
  <resume-signal>Type "approved" after the real-device authed walkthrough passes (or list the failing items).</resume-signal>
  <acceptance_criteria>
    - `48-HUMAN-UAT.md` exists and contains the token `human_needed` and a tap-to-pin checklist for a Recharts Line + Bar + Pie chart AND EquityChart.
    - `npm run test:coverage` exit 0 with lines >=82, stmts >=80, fns >=74, branches >=72 (ratchet held, never lowered); final actuals recorded in 48-05-SUMMARY.md.
    - `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts tests/visual/chart-accessibility-layer.test.ts` green (SCENARIO-05 zero-diff + accessibilityLayer grep both pass).
    - `npx playwright test e2e/svg-chart-parity.spec.ts` self-skips loudly (no false-green; no placeholder golden PNGs committed) -- the Phase-47 carryover is NOT made false-green and NOT baked here.
    - Verification status ends `human_needed` (the real-device authed walkthrough is the human sign-off).
  </acceptance_criteria>
  <done>48-HUMAN-UAT.md authored; coverage ratchet verified held; all frozen-math/parity guards green un-weakened; verification ends human_needed pending the real-device sign-off.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI lighthouse-mobile job -> temporary-public-storage | lhci uploads route reports to a public storage target; only PUBLIC routes are collected so nothing sensitive is uploaded. |
| npm registry -> @lhci/cli (install done in plan 01) | The net-new dep is consumed here; its legitimacy gate was cleared in plan 01. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05-INFO | Information disclosure | lhci -> temporary-public-storage | mitigate | `lighthouserc.json` `url` list is PUBLIC routes ONLY (no authed URL); the lighthouse-mobile job receives NO `TEST_SUPABASE_*` secret (acceptance grep = 0). Authed perf is covered by the human sign-off, not lhci. |
| T-48-05-FLAKY | Denial of Service (self-inflicted CI flake) | lhci minScore threshold | mitigate | Seed minScore a few points UNDER measured baseline (no hard 90+ day one) -- the coverage-ratchet philosophy avoids flaky-red; error-level fails the build only on a real regression. |
| T-48-05-DEVENV | Tampering | lhci against wrong build dir | mitigate | Job restores the production `nextjs-build` artifact and runs `npm run start` (production `.next`), never `next dev` (Pitfall 6). |
| T-48-05-GUARD | Tampering (weakening a guard to go green) | coverage ratchet / frozen-math / svg-chart-parity | mitigate | Verify-only: ratchet never lowered, no blanket snapshot update, svg-chart-parity NOT made false-green (still self-skips, no placeholder goldens). Acceptance criteria assert each guard green un-weakened. |
</threat_model>

<verification>
- `lighthouserc.json` parses; minScore is a baselined numeric <0.9; url list public-only; baseline recorded in SUMMARY.
- `grep -c "lighthouse-mobile" .github/workflows/ci.yml` >= 1; YAML valid; no TEST_SUPABASE_* in that job.
- `grep -c "ResizeObserver loop" e2e/reflow-sweep-authed.spec.ts` >= 1; the spec lists the rotate case (self-skips unseeded).
- `48-HUMAN-UAT.md` exists with the tap-to-pin checklist + `human_needed`.
- `npm run test:coverage` exit 0, all four thresholds held (final actuals recorded).
- `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts tests/visual/chart-accessibility-layer.test.ts` green.
- `npx playwright test e2e/svg-chart-parity.spec.ts` self-skips loudly (no false-green; not baked here).
</verification>

<success_criteria>
- Mobile @lhci/cli budget gates the 5 public routes via a blocking lighthouse-mobile CI job, thresholds baseline-seeded under measured, no Supabase secret.
- Rotate-stability (no ResizeObserver-loop error + bounded memory) folded into an existing mobile e2e.
- 48-HUMAN-UAT.md authored; verification ends human_needed (real-device authed sign-off).
- Coverage ratchet verified held; all frozen-math/byte-identity/parity guards green un-weakened; svg-chart-parity not made false-green.
</success_criteria>

<output>
Create `.planning/phases/48-recharts-equitychart-final-verification/48-05-SUMMARY.md` when done
</output>
