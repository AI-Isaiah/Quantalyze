---
phase: 48-recharts-equitychart-final-verification
plan: 04
type: execute
wave: 2
depends_on: ["48-02", "48-03"]
files_modified:
  - e2e/axe-app-wide.spec.ts
  - e2e/target-size.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [A11Y-01, CHART-01b]
user_setup: []

must_haves:
  truths:
    - "Zero WCAG-AA (wcag2a+wcag2aa+best-practice) violations across all primary routes × {Desktop, mobile 375} via one parametrized axe-app-wide spec"
    - "The embedded-factsheet/composer rows use a scoped serious+critical filter (never a rule disable); standalone routes stay strict"
    - "Every analyze() is gated behind a visible anchor + HTTP<400 (no hollow-zero false-green on a blank/404/login page)"
    - "axe-app-wide is FLOW-01 dual-wired: HAS_SEED_ENV const (authed self-skip) + ci.yml (authed rows in the seeded MA-8 list, public rows in the unseeded list) — proven to RUN in CI"
    - "The bespoke gate matrix (320px reflow, 44px target-size, zoom-meta grep, mobile keyboard/focus) is confirmed app-wide BLOCKING beside axe"
    - "target-size is extended to assert the EquityChart tap-rect ≥44px under pointer-coarse at 320px on /allocations"
  artifacts:
    - path: "e2e/axe-app-wide.spec.ts"
      provides: "Route × viewport WCAG-AA axe matrix"
      contains: "buildAxe"
    - path: "e2e/target-size.spec.ts"
      provides: "Extended with the EquityChart coarse tap-rect ≥44px case on /allocations"
      contains: "/allocations"
    - path: ".github/workflows/ci.yml"
      provides: "axe-app-wide wired into BOTH the seeded MA-8 list and the unseeded list"
      contains: "axe-app-wide"
  key_links:
    - from: "e2e/axe-app-wide.spec.ts"
      to: ".github/workflows/ci.yml"
      via: "FLOW-01 dual-wire — spec in both the seeded MA-8 list and the unseeded list"
      pattern: "axe-app-wide"
    - from: "e2e/axe-app-wide.spec.ts"
      to: "e2e/helpers/axe.ts"
      via: "buildAxe(page) — the single shared axe factory (no second harness)"
      pattern: "buildAxe"
---

<objective>
Stand up A11Y-01: extend the WCAG-AA axe gate from the current 5 focused per-route specs to ONE new parametrized `axe-app-wide.spec.ts` that scans all primary routes at BOTH Desktop and a mobile (375px) viewport, with the embedded-factsheet/composer landmark exception handled via the scoped `serious+critical` filter (never a rule disable). Extend `target-size.spec.ts` with the EquityChart coarse tap-rect ≥44px case, confirm the full bespoke gate matrix runs app-wide as BLOCKING CI checks beside axe, and FLOW-01 dual-wire the new spec so it actually RUNS in CI.

Purpose: A gate earns trust only once it actually executes in CI (the JOURNEY-03 / Phase-44 / Phase-47 lesson — burned ≥3×). This plan makes the app-wide axe matrix real and provable, scoped precisely so standalone routes stay strict and only the legitimately-nested embedded factsheet gets the impact filter (D-Area-3, Assumption A2).
Output: `axe-app-wide.spec.ts` (filled in from the plan-01 scaffold), the extended `target-size.spec.ts`, and `ci.yml` dual-wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md
@.planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md
@.planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md

<interfaces>
From e2e/helpers/axe.ts (the single shared factory — VERIFIED, reuse; no second harness):
```typescript
export function buildAxe(page: Page): AxeBuilder; // already .withTags(["wcag2a","wcag2aa","best-practice"])
```

From e2e/helpers/reflow.ts (VERIFIED — MIN_TARGET_PX=44, do NOT lower):
```typescript
export async function assertTargetSizes(page, /* anchor + interactive selector */): Promise<void>; // gates on MIN_TARGET_PX=44
```

The seed-guard + login idiom (copy verbatim from composer-axe.spec.ts:54-70):
```ts
const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;
async function loginViaForm(page, email, password) { /* /login → fill → submit → waitForURL */ }
```

The serious+critical scoped filter (composer-axe.spec.ts:208-212 — embedded/composer ONLY):
```ts
const blocking = results.violations.filter(v => v.impact === "serious" || v.impact === "critical");
expect(blocking).toEqual([]);
// standalone routes stay strict: expect(results.violations).toEqual([]);
```

CI anchors (VERIFIED 2026-06-28): unseeded Playwright list ends `... e2e/reflow-sweep.spec.ts` at ~L1059; seeded MA-8 list `npx playwright test \ ... e2e/target-size.spec.ts \ --timeout 60000` at ~L1252-1265, with the FLOW-01 reminder comment "Adding/removing a seed-gated spec? Update both this list and the e2e/<spec>.spec.ts HAS_SEED_ENV constant."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the route × viewport axe matrix (public strict + authed seeded + embedded serious+critical)</name>
  <files>e2e/axe-app-wide.spec.ts</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§e2e/axe-app-wide.spec.ts — the route-loop shape, the seed-guard+login verbatim, the anti-false-green visible-anchor gate, the serious+critical filter scope)
    - .planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md (§A11Y-01 — route coverage list public+authed, both viewports, embedded-factsheet exception scope)
    - .planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md (§Open Question 1 — concrete seeded URLs: composer=/allocations?tab=scenario, factsheet=/factsheet/[id]/v2, strategy-v2=/strategy/[id]/v2, discovery=/discovery/[slug], wizard=/strategies/new/wizard; Assumption A2 — keep standalone routes STRICT, apply the filter ONLY to the composer/embedded surface)
    - e2e/reflow-sweep.spec.ts (L41-82 — the {path,anchor}[] route-loop + HTTP≥400 fail-loud anchor template)
    - e2e/composer-axe.spec.ts (the HAS_SEED_ENV const L54-56, loginViaForm L58-70, buildAxe usage, seedTestAllocator, the serious+critical filter L208-212)
    - e2e/helpers/axe.ts (buildAxe — the single factory to reuse)
  </read_first>
  <action>
    Fill in the plan-01 `axe-app-wide.spec.ts` scaffold into the full matrix. Declare `HAS_SEED_ENV` (copy verbatim). `VIEWPORTS = [{w:1280,h:800,name:"Desktop"},{w:375,h:812,name:"mobile"}]` (375 per Assumption A3). PUBLIC_ROUTES (run UNSEEDED, no skip): `/`→`h1`, `/security`→`main h1`, `/for-quants`→`main h1`, `/browse`→`main h1`, `/demo`→`#editorial-hero-headline`. For each public route × viewport: `setViewportSize`, `goto`, throw on HTTP≥400, `expect(page.locator(anchor)).toBeVisible({timeout:5000})`, then `buildAxe(page).analyze()` with STRICT `expect(results.violations).toEqual([])`. AUTHED_ROUTES (gated by `test.skip(!HAS_SEED_ENV, ...)`, login via the composer-axe seed+loginViaForm idiom): `/allocations` (strict), `/strategy/[id]/v2` (strict), `/discovery/[slug]` (strict), `/strategies/new/wizard` (strict), and the EMBEDDED-FACTSHEET surfaces — `/allocations?tab=scenario` (composer) and `/factsheet/[id]/v2` (per Assumption A2: standalone `/factsheet/[id]/v2` already passes STRICT under the existing focused specs, so keep standalone strict and apply the `serious+critical` filter ONLY to the composer/embedded-factsheet surface) — each at BOTH viewports, each gated behind a visible anchor + HTTP<400. Reuse the seeded URL construction from the existing focused specs (mirror how composer-axe/strategy-v2-axe build their ids). Do NOT introduce a second axe harness; do NOT disable any rule. Per RESEARCH Open Question 1, the new spec covers all primary routes at both viewports using the same seed helpers (accept cheap Desktop overlap with the focused specs for matrix completeness) — document the chosen scope in the SUMMARY.
  </action>
  <verify>
    <automated>npx playwright test e2e/axe-app-wide.spec.ts --list && TEST_SUPABASE_URL= TEST_SUPABASE_SERVICE_ROLE_KEY= npx playwright test e2e/axe-app-wide.spec.ts --project=chromium --grep-invert "@seeded" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `--list` enumerates the full route × viewport matrix (public + authed, Desktop + mobile).
    - The unseeded run executes the 5 public routes × 2 viewports (10 cases) with zero violations OR fails loud with the specific route+impact (no hollow-zero); the authed cases self-skip when seed env is absent.
    - `grep -c "buildAxe" e2e/axe-app-wide.spec.ts` ≥ 1 and `grep -c "AxeBuilder\|jest-axe" e2e/axe-app-wide.spec.ts` returns 0 (single shared harness).
    - The `serious+critical` filter appears ONLY in the composer/embedded-factsheet block; standalone-route blocks use `expect(results.violations).toEqual([])`.
    - Every `analyze()` is preceded by an HTTP<400 check and a visible-anchor assertion.
  </acceptance_criteria>
  <done>The route × viewport axe matrix is implemented; public routes pass strict unseeded; authed/embedded scoping is correct; no rule disabled; no second harness.</done>
</task>

<task type="auto">
  <name>Task 2: Extend target-size.spec.ts with the EquityChart coarse tap-rect ≥44px case on /allocations</name>
  <files>e2e/target-size.spec.ts</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§e2e/target-size.spec.ts — the Phase-47 coarse-emulation self-analog at L58-177; the test.use({hasTouch,isMobile}) idiom; note target-size is ALREADY dual-wired so no new FLOW-01 wiring needed)
    - e2e/target-size.spec.ts (L58-177 — the existing coarse-emulation block to mirror; the HAS_SEED_ENV const; the assertTargetSizes helper usage)
    - e2e/helpers/reflow.ts (assertTargetSizes + MIN_TARGET_PX=44 — un-lowered)
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx (L1425-1426 the <svg role="img" aria-label="Equity chart"> visible anchor; the pointer-coarse:min-h-[44px] layer added in plan 03 is the measured surface)
  </read_first>
  <action>
    Add a new SEEDED `test.describe` mirroring the existing Phase-47 coarse block: `test.use({ hasTouch: true, isMobile: true })` (makes Chromium report pointer:coarse), `test.skip(!HAS_SEED_ENV, ...)`. In the test: `setViewportSize({width:320, height:800})`, seed+login via the same helper the existing seeded cases use, `goto("/allocations")`, throw on HTTP≥400, anchor on the EquityChart `<svg role="img" aria-label="Equity chart">` (fail loud), then `assertTargetSizes` against the EquityChart coarse hit surface (the `pointer-coarse:min-h-[44px]` interactive layer from plan 03) asserting ≥44px. Do NOT lower `MIN_TARGET_PX`. Because `target-size.spec.ts` is ALREADY in both ci.yml lists (unseeded /security at ~L1059 + seeded at ~L1265), this new describe self-skips unseeded and runs seeded with no new FLOW-01 wiring needed.
  </action>
  <verify>
    <automated>npx playwright test e2e/target-size.spec.ts --list 2>&1 | grep -i "equity\|allocations" ; npx playwright test e2e/target-size.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `--list` shows a new EquityChart/`/allocations` coarse target-size case under a `test.use({hasTouch,isMobile})` describe.
    - `grep -q "MIN_TARGET_PX" e2e/helpers/reflow.ts` still resolves to 44 (un-lowered); the spec does not redefine a lower threshold.
    - The new describe self-skips when seed env is absent (it is HAS_SEED_ENV-gated).
    - `grep -c "/allocations" e2e/target-size.spec.ts` ≥ 1.
  </acceptance_criteria>
  <done>target-size.spec.ts asserts the EquityChart tap-rect ≥44px under coarse at 320px on /allocations; no threshold lowered; no new CI wiring needed (already dual-wired).</done>
</task>

<task type="auto">
  <name>Task 3: FLOW-01 dual-wire axe-app-wide into ci.yml + add its HAS_SEED_ENV authed gate + confirm bespoke matrix app-wide blocking</name>
  <files>.github/workflows/ci.yml, e2e/axe-app-wide.spec.ts</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§.github/workflows/ci.yml — the seeded MA-8 list L1252-1266 + unseeded list L1059 + FLOW-01 reminder comment; §Shared Patterns — FLOW-01 dual-wiring "two switches" lesson)
    - .planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md (§Pitfall 3 — the gate exists but never RUNS; authed rows → seeded MA-8 list, public rows → unseeded list; PROVE it ran)
    - .github/workflows/ci.yml (the unseeded `npx playwright test ... e2e/reflow-sweep.spec.ts` line ~L1059; the seeded MA-8 `npx playwright test \ ...e2e/target-size.spec.ts \ --timeout 60000` block ~L1252-1265 with the reminder comment ~L1250-1251)
  </read_first>
  <action>
    FLOW-01 dual-wire (the load-bearing CI step). Place 1 (ci.yml): add `e2e/axe-app-wide.spec.ts` to BOTH lists — append it to the unseeded `npx playwright test ...` list at ~L1059 (its public-route rows run there) AND add it as a continuation line in the seeded MA-8 block at ~L1252-1265 (its authed/embedded rows run there, alongside `composer-axe`/`svg-chart-parity`/`target-size`). Place 2 (the spec): confirm the authed/embedded `test.describe` blocks are gated by `test.skip(!HAS_SEED_ENV, ...)` so they self-skip in the unseeded job and run in the seeded job (the public-route blocks run in both — that overlap is cheap and intended). Add a short comment next to the new ci.yml entries mirroring the existing FLOW-01 reminder. Then CONFIRM (verify-only, no edit unless a gap is found) the rest of the bespoke gate matrix is app-wide BLOCKING beside axe: `reflow.spec.ts`/`reflow-sweep.spec.ts` (unseeded) + `reflow-sweep-authed.spec.ts` (seeded) for 320px reflow; `tests/visual/viewport-zoom-meta.test.ts` (vitest, in the unit gate) for zoom-meta; `mobile-drawer-keyboard.spec.ts` + `strategy-v2-keyboard.spec.ts` (seeded) for keyboard/focus; `target-size.spec.ts` (both lists). Document in the SUMMARY each gate's CI list membership (the proof they run). If any bespoke gate is found NOT wired, wire it and note it.
  </action>
  <verify>
    <automated>grep -c "axe-app-wide" .github/workflows/ci.yml ; grep -n "reflow.spec\|reflow-sweep\|target-size\|mobile-drawer-keyboard\|strategy-v2-keyboard\|viewport-zoom-meta\|axe-app-wide" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "axe-app-wide" .github/workflows/ci.yml` returns ≥ 2 (present in BOTH the unseeded list and the seeded MA-8 list).
    - The seeded MA-8 block still ends with `--timeout 60000` and the YAML is valid (the new continuation line uses the same `\` continuation style).
    - The spec's authed/embedded describes are `HAS_SEED_ENV`-gated; the public describes are NOT (run unseeded).
    - The SUMMARY lists, for each bespoke gate (reflow, target-size, zoom-meta, keyboard) and axe-app-wide, exactly which ci.yml list(s) it appears in — the FLOW-01 proof.
    - YAML lint sanity: `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (or the project's existing actionlint/yaml check passes).
  </acceptance_criteria>
  <done>axe-app-wide is dual-wired into ci.yml (both lists) with the correct HAS_SEED_ENV gating; the full bespoke matrix is confirmed app-wide blocking with documented CI-list membership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → seeded test DB | The seeded axe rows reuse the existing `TEST_SUPABASE_*` seed+login helpers (already wired); no new secret introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-04-FLOW | Repudiation (gate that silently never runs) | axe-app-wide.spec.ts CI wiring | mitigate | FLOW-01 dual-wire into BOTH ci.yml lists + HAS_SEED_ENV gating; PROVE it ran by checking the CI run log for the test names (Pitfall 3). The acceptance criterion `grep -c axe-app-wide ≥ 2` is the falsifiable guard. |
| T-48-04-HOLLOW | Spoofing (hollow-zero false-green) | axe analyze() on a blank/404/login page | mitigate | Every analyze() gated behind HTTP<400 + a visible-anchor assertion (Pitfall 4). |
| T-48-04-RULE | Tampering (weakening a gate) | embedded-factsheet axe exception | mitigate | Use the `serious+critical` impact filter (every rule still RUNS), NEVER a rule disable; scoped to the composer/embedded surface only, standalone routes stay strict. |
</threat_model>

<verification>
- `npx playwright test e2e/axe-app-wide.spec.ts --list` enumerates the full matrix; unseeded public run is green or fails loud per-route.
- `npx playwright test e2e/target-size.spec.ts --list` shows the EquityChart coarse case.
- `grep -c "axe-app-wide" .github/workflows/ci.yml` ≥ 2; YAML valid.
- Existing focused axe specs left in place and still green (composer-axe / strategy-v2-axe / discovery-axe / wizard-axe / admin-csv-status-axe).
- The SUMMARY documents the CI-list membership of every bespoke gate (FLOW-01 proof) — to be confirmed RUN in the real CI run at the phase gate.
</verification>

<success_criteria>
- One parametrized axe-app-wide spec covers all primary routes × {Desktop, mobile 375}, zero WCAG-AA violations.
- Embedded factsheet scoped serious+critical (no rule disabled); standalone strict.
- target-size extended with the EquityChart ≥44px coarse case on /allocations.
- axe-app-wide FLOW-01 dual-wired (both ci.yml lists); bespoke matrix confirmed app-wide blocking with documented list membership.
</success_criteria>

<output>
Create `.planning/phases/48-recharts-equitychart-final-verification/48-04-SUMMARY.md` when done
</output>
