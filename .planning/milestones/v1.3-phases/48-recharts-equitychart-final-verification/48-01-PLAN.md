---
phase: 48-recharts-equitychart-final-verification
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/charts/TouchTooltip.tsx
  - src/components/charts/TouchTooltip.test.tsx
  - e2e/axe-app-wide.spec.ts
  - lighthouserc.json
  - src/app/(dashboard)/allocations/widgets/performance/EquityChart.touch.test.tsx
autonomous: false
requirements: [CHART-01b, A11Y-01, A11Y-03]
user_setup: []

must_haves:
  truths:
    - "@lhci/cli@0.15.1 is installed as a pinned devDependency (no caret/tilde range)"
    - "TouchTooltip renders trigger='click' when useBreakpoint()==='mobile' and trigger='hover' otherwise, spreading formatter/contentStyle through unchanged"
    - "A failing/skipping e2e/axe-app-wide.spec.ts scaffold exists with a HAS_SEED_ENV self-skip"
    - "A lighthouserc.json scaffold exists with a placeholder minScore (to be baselined in plan 05)"
    - "An EquityChart touch-path test scaffold exists asserting pointerToIndex parity with handleMove math"
  artifacts:
    - path: "src/components/charts/TouchTooltip.tsx"
      provides: "Breakpoint-gated Recharts <Tooltip trigger> shim (the DRY shared wrapper)"
      contains: "useBreakpoint"
      min_lines: 8
    - path: "src/components/charts/TouchTooltip.test.tsx"
      provides: "Both-branch coverage of the trigger ternary (mobile click / desktop hover) + props spread"
      contains: "trigger"
    - path: "e2e/axe-app-wide.spec.ts"
      provides: "Route × viewport axe matrix scaffold (Wave 0 stub, filled in plan 04)"
      contains: "HAS_SEED_ENV"
    - path: "lighthouserc.json"
      provides: "lhci config scaffold (mobile preset, public routes, error-level)"
      contains: "preset"
    - path: "src/app/(dashboard)/allocations/widgets/performance/EquityChart.touch.test.tsx"
      provides: "EquityChart pointerToIndex parity test scaffold (Wave 0 stub, satisfied in plan 03)"
      contains: "nearestIndex"
  key_links:
    - from: "src/components/charts/TouchTooltip.tsx"
      to: "src/hooks/useBreakpoint.ts"
      via: "useBreakpoint() === 'mobile' ? 'click' : 'hover'"
      pattern: "useBreakpoint\\(\\)\\s*===\\s*[\"']mobile[\"']"
    - from: "src/components/charts/TouchTooltip.tsx"
      to: "recharts"
      via: "wraps <Tooltip trigger={...} {...props} />"
      pattern: "from [\"']recharts[\"']"
---

<objective>
Stand up the Wave 0 foundation for Phase 48: install the single net-new dependency `@lhci/cli@0.15.1`, build the one shared `TouchTooltip` shim that every downstream Recharts swap (plan 02) consumes, and scaffold the failing/skipping test+spec+config stubs that the implementation and verification waves (plans 03/04/05) fill in — so nyquist sampling has a target the moment each later task starts.

Purpose: Per 48-VALIDATION.md Wave 0 Requirements, the install + stubs must exist BEFORE the implementation waves. The `TouchTooltip` shim is the DRY anchor for CHART-01b (avoids 18× inline `trigger` duplication, D-Area-1). Building it once here, fully tested with both-branch coverage, means plan 02 is a pure one-token swap and the branch-coverage ratchet is satisfied by this plan's own test.
Output: `@lhci/cli` devDep (pinned), `TouchTooltip.tsx` + `TouchTooltip.test.tsx` (green), and four scaffold stubs (`axe-app-wide.spec.ts`, `lighthouserc.json`, `EquityChart.touch.test.tsx`) that self-skip or fail-pending until their owning plan completes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md
@.planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md
@.planning/phases/48-recharts-equitychart-final-verification/48-VALIDATION.md
@.planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — extracted from live source. No codebase exploration needed. -->

From src/hooks/useBreakpoint.ts (the gate — SSR-safe two-pass, server snapshot 'desktop'):
```typescript
export type Breakpoint = "mobile" | "tablet" | "desktop";
export function useBreakpoint(): Breakpoint;   // "mobile" = max-width:639px
```

From node_modules/recharts (VERIFIED 2026-06-28, the must-verify item):
```typescript
// Tooltip.d.ts L168-175: trigger default "hover"; "click" → shows after clicking and STAYS ACTIVE.
// types.d.ts:3: export type TooltipTrigger = 'hover' | 'click';
import { Tooltip } from "recharts";   // ComponentProps<typeof Tooltip> is the prop surface to spread
```

From src/hooks/useTapPin.ts (consumed by EquityChart in plan 03 — referenced here only for the touch-test scaffold):
```typescript
export function useTapPin(opts: { count: number; pointerToIndex: (clientX:number, clientY:number, rect:DOMRect) => number | null }): {
  selectedIdx: number | null; pinned: boolean; setChartEl: (el: Element|null)=>void;
  onPointerDown; onPointerMove; onPointerUp; onPointerLeave;   // typed ReactPointerEvent<SVGSVGElement>
};
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify @lhci/cli legitimacy, then install pinned</name>
  <files>package.json (the upcoming install target — no edit in this task; the install runs in Task 2 after approval)</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md (§Package Legitimacy Audit + §Security Domain — provenance already verified: GoogleChrome/lighthouse-ci org, maintainers paulirish/patrickhulce/hoten/adamraine, NO postinstall script)
  </read_first>
  <action>PAUSE for the supply-chain legitimacy gate. Do NOT run `npm install` in this task. Present the @lhci/cli@0.15.1 provenance (GoogleChrome/lighthouse-ci org, Lighthouse-core maintainers, no postinstall) and WAIT for the human to verify on npmjs.com and approve. Only after the human types "approved" does Task 2 run the pinned install. This checkpoint is never auto-approved (auto_advance ignored for package-legitimacy gates).</action>
  <what-built>
    Nothing automated yet — this is the supply-chain legitimacy gate BEFORE the one net-new install. RESEARCH classifies @lhci/cli as [VERIFIED] (official org, no postinstall) but slopcheck was unavailable, so per the package-legitimacy protocol this install gets a blocking human checkpoint.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@lhci/cli — confirm version 0.15.1 exists, repository is github.com/GoogleChrome/lighthouse-ci, maintained by the Lighthouse core team.
    2. Confirm there is NO postinstall script on the package page / `npm view @lhci/cli scripts`.
    3. Approve the pinned install (exact `0.15.1`, no `^`/`~`).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `npm install --save-dev @lhci/cli@0.15.1`, or describe concerns.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" before any `npm install` runs.
    - This checkpoint is NEVER auto-approved (workflow.auto_advance is ignored for package-legitimacy gates).
  </acceptance_criteria>
  <done>Human has approved the pinned @lhci/cli@0.15.1 install.</done>
</task>

<task type="auto">
  <name>Task 2: Install @lhci/cli@0.15.1 (pinned) + build the TouchTooltip shim with both-branch test</name>
  <files>package.json, package-lock.json, src/components/charts/TouchTooltip.tsx, src/components/charts/TouchTooltip.test.tsx</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§`src/components/charts/TouchTooltip.tsx` — the exact shim body; §`TouchTooltip.test.tsx` — the mock-a-hook test idiom; the canonical `useBreakpoint() === "mobile"` spelling from HeatmapPanels.tsx:263)
    - src/components/charts/RollingMetrics.tsx (representative chart: the `"use client"` + named recharts import convention the shim mirrors; the existing `<Tooltip formatter contentStyle>` usage the shim must spread)
    - src/components/charts/chart-tokens.ts (shared-chart-module convention this file follows)
    - src/hooks/useTapPin.test.ts (the mock-a-hook unit-test idiom to mirror)
  </read_first>
  <action>
    Run `npm install --save-dev @lhci/cli@0.15.1` (pinned exact, no caret/tilde — confirm `package.json` devDependencies shows `"@lhci/cli": "0.15.1"`, not `^0.15.1`). Then create `src/components/charts/TouchTooltip.tsx`: a client component starting with `"use client"`, importing `{ Tooltip }` from `recharts`, `type { ComponentProps } from "react"`, and `useBreakpoint` from `@/hooks/useBreakpoint`. Export `function TouchTooltip(props: ComponentProps<typeof Tooltip>)` that computes `const trigger = useBreakpoint() === "mobile" ? "click" : "hover"` and returns `<Tooltip trigger={trigger} {...props} />` (spread AFTER trigger so a caller could override; none do today). Do NOT read the breakpoint during SSR or force single-pass — the two-pass `useBreakpoint` is the locked hydration-safe pattern (Pitfall 1). Then create `src/components/charts/TouchTooltip.test.tsx`: `vi.mock("@/hooks/useBreakpoint", () => ({ useBreakpoint: vi.fn() }))`; arm 1 `mockReturnValue("mobile")` asserts the rendered `<Tooltip>` receives `trigger="click"`; arm 2 `mockReturnValue("desktop")` asserts `trigger="hover"` (the byte-identical-desktop proof); a third assertion confirms a passed `formatter`/`contentStyle` prop spreads through unchanged. BOTH ternary branches MUST be exercised so the branches-72 ratchet covers this new viewport conditional.
  </action>
  <verify>
    <automated>npm ls @lhci/cli | grep -q "@lhci/cli@0.15.1" && npx vitest run src/components/charts/TouchTooltip.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '"@lhci/cli": "0.15.1"' package.json` (pinned, no `^`/`~`).
    - `npx vitest run src/components/charts/TouchTooltip.test.tsx` passes with at least 3 assertions (mobile→click, desktop→hover, props spread).
    - `grep -c "useBreakpoint() === \"mobile\" ? \"click\" : \"hover\"" src/components/charts/TouchTooltip.tsx` returns 1 (the exact gated ternary).
    - `src/components/charts/TouchTooltip.tsx` does NOT touch any chart root tag and contains no `accessibilityLayer` token (it wraps `<Tooltip>` only — Pitfall 5).
  </acceptance_criteria>
  <done>@lhci/cli@0.15.1 pinned-installed; TouchTooltip shim exists and its both-branch test is green.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the three Wave-0 stubs (axe-app-wide spec, lighthouserc, EquityChart touch test)</name>
  <files>e2e/axe-app-wide.spec.ts, lighthouserc.json, src/app/(dashboard)/allocations/widgets/performance/EquityChart.touch.test.tsx</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§`e2e/axe-app-wide.spec.ts` — the HAS_SEED_ENV const + route-loop shape; §`lighthouserc.json` — the config shape with placeholder minScore; §EquityChart — handleMove L1142-1159 math the touch test mirrors)
    - e2e/reflow-sweep.spec.ts (L41-82 — the `{path, anchor}[]` route-loop template + HTTP≥400 fail-loud anchor pattern)
    - e2e/composer-axe.spec.ts (the HAS_SEED_ENV const + buildAxe usage + serious+critical filter precedent the full spec in plan 04 will use)
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx (L46 nearestIndex, L1142-1159 handleMove: px → clampedPx (clamp to [pad.l, pad.l+chartW]) → targetEpoch → nearestIndex(visibleEpochs, targetEpoch))
  </read_first>
  <action>
    Create `e2e/axe-app-wide.spec.ts` as a minimal scaffold: declare `const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;`, the `PUBLIC_ROUTES` `{path,anchor}[]` array (`/`→`h1`, `/security`→`main h1`, `/for-quants`→`main h1`, `/browse`→`main h1`, `/demo`→`#editorial-hero-headline`) and a `VIEWPORTS` array (`{w:1280,h:800,name:"Desktop"}`, `{w:375,h:812,name:"mobile"}`), and ONE placeholder public-route test that navigates, fails loud on HTTP≥400, asserts the anchor visible, and runs `buildAxe(page).analyze()` with `expect(results.violations).toEqual([])`. Add a top-of-file comment: "Wave 0 scaffold — plan 04 fills the authed rows + serious+critical embedded-factsheet filter + FLOW-01 dual-wiring." Do NOT wire it into ci.yml yet (plan 04 owns that). Create `lighthouserc.json` per the PATTERNS §lighthouserc shape: `ci.collect` with `startServerCommand:"npm run start"`, the 5 public `http://localhost:3000/...` urls, `numberOfRuns:3`, `settings.preset:"mobile"`; `ci.assert` with `preset:"lighthouse:no-pwa"` and `categories:performance: ["error", {minScore: 0.5}]` plus an inline comment `PLACEHOLDER — plan 05 reseeds from a real baseline run`; `ci.upload.target:"temporary-public-storage"`. Create `src/app/(dashboard)/allocations/widgets/performance/EquityChart.touch.test.tsx` as a `describe.todo` / pending scaffold (a single skipped test named "pointerToIndex maps px→nearestIndex identically to handleMove") with a comment block documenting the px→clampedPx→targetEpoch→nearestIndex chain plan 03 must prove; the test stays `.skip`/`.todo` until plan 03 extracts/wires the touch path. These stubs MUST NOT break the suite (skipped/todo tests do not fail).
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('lighthouserc.json','utf8'))" && npx vitest run src/app/\(dashboard\)/allocations/widgets/performance/EquityChart.touch.test.tsx && npx playwright test e2e/axe-app-wide.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - `lighthouserc.json` is valid JSON; `grep -q '"preset": "mobile"' lighthouserc.json` and `grep -q 'temporary-public-storage' lighthouserc.json` and a PLACEHOLDER comment marks the minScore.
    - `grep -q "HAS_SEED_ENV" e2e/axe-app-wide.spec.ts` and `grep -q "VIEWPORTS" e2e/axe-app-wide.spec.ts`; `npx playwright test e2e/axe-app-wide.spec.ts --list` enumerates ≥1 test without error.
    - `e2e/axe-app-wide.spec.ts` is NOT present in `.github/workflows/ci.yml` yet (plan 04 wires it): `grep -c "axe-app-wide" .github/workflows/ci.yml` returns 0.
    - `EquityChart.touch.test.tsx` runs with 0 failures (the parity test is skipped/todo pending plan 03).
    - Full suite stays green: `npm run test` exit 0 (the new stubs add no failing tests).
  </acceptance_criteria>
  <done>All three Wave-0 stubs exist; lighthouserc is valid JSON with a placeholder minScore; the axe spec lists; the EquityChart touch test is a pending scaffold; suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local devDependencies | The one net-new install (`@lhci/cli`) crosses the supply-chain boundary at install time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-SC | Tampering | `npm install --save-dev @lhci/cli@0.15.1` | mitigate | Pin exact `0.15.1` (no caret/tilde). Blocking human checkpoint (Task 1) verifies npmjs.com provenance (GoogleChrome org, no postinstall) before install — never auto-approved. |
| T-48-01 | Tampering | `TouchTooltip.tsx` prop spread | accept | The shim only injects `trigger` and spreads existing props onto `<Tooltip>`; no user input, no chart-root tag touched, no new data path. Frozen-math/accessibilityLayer grep unaffected. Low risk. |
</threat_model>

<verification>
- `npm ls @lhci/cli` shows `0.15.1` (pinned).
- `npx vitest run src/components/charts/TouchTooltip.test.tsx` green (both branches + spread).
- `lighthouserc.json` parses; `e2e/axe-app-wide.spec.ts` lists; `EquityChart.touch.test.tsx` pending-green.
- `npm run test` exit 0 (no new failing tests introduced).
- Frozen-math/parity guards untouched: `npx vitest run tests/visual/chart-accessibility-layer.test.ts src/__tests__/phase-31-frozen-spine-guards.test.ts` green.
</verification>

<success_criteria>
- `@lhci/cli@0.15.1` is a pinned devDependency, install human-approved.
- `TouchTooltip` shim exists, fully tested (mobile→click, desktop→hover, props spread), both branches covered.
- Wave-0 stubs (`axe-app-wide.spec.ts`, `lighthouserc.json`, `EquityChart.touch.test.tsx`) exist and do not break the suite.
- No chart files swapped yet (plan 02), EquityChart untouched (plan 03), ci.yml untouched (plans 04/05).
</success_criteria>

<output>
Create `.planning/phases/48-recharts-equitychart-final-verification/48-01-SUMMARY.md` when done
</output>
