---
phase: 48-recharts-equitychart-final-verification
plan: 02
type: execute
wave: 1
depends_on: ["48-01"]
files_modified:
  - src/app/(dashboard)/allocations/widgets/attribution/AlphaBetaDecomposition.tsx
  - src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx
  - src/app/(dashboard)/allocations/widgets/risk/RiskDecomposition.tsx
  - src/app/(dashboard)/allocations/widgets/risk/TailRisk.tsx
  - src/components/charts/CorrelationWithBenchmark.tsx
  - src/components/charts/DrawdownChart.tsx
  - src/components/charts/NetGrossExposureChart.tsx
  - src/components/charts/ReturnHistogram.tsx
  - src/components/charts/RollingAlphaBetaChart.tsx
  - src/components/charts/RollingMetrics.tsx
  - src/components/charts/RollingSortinoChart.tsx
  - src/components/charts/RollingVolatilityChart.tsx
  - src/components/charts/TurnoverChart.tsx
  - src/components/charts/YearlyReturns.tsx
  - src/components/portfolio/AttributionBar.tsx
  - src/components/portfolio/CompositionDonut.tsx
  - src/components/portfolio/RiskAttribution.tsx
  - src/components/strategy/CompareEquityOverlay.tsx
autonomous: true
requirements: [CHART-01b]
user_setup: []

must_haves:
  truths:
    - "All 18 Recharts charts that have a <Tooltip> today render it via TouchTooltip (mobile→tap-to-pin, desktop→hover byte-identical)"
    - "OutcomesWidget sparkline is untouched (parity-only — no invented tap-reveal)"
    - "Every chart-root tag keeps its literal accessibilityLayer={false} (the whole-codebase grep stays green)"
    - "Desktop render of every chart family is byte-identical (trigger defaults to hover)"
  artifacts:
    - path: "src/components/charts/RollingMetrics.tsx"
      provides: "Representative LineChart now using TouchTooltip"
      contains: "TouchTooltip"
    - path: "src/components/portfolio/CompositionDonut.tsx"
      provides: "Representative PieChart/Cell variant now using TouchTooltip"
      contains: "TouchTooltip"
  key_links:
    - from: "the 18 Recharts charts"
      to: "src/components/charts/TouchTooltip.tsx"
      via: "import { TouchTooltip } and <TouchTooltip .../> replacing <Tooltip .../>"
      pattern: "import \\{ TouchTooltip \\}"
    - from: "tests/visual/chart-accessibility-layer.test.ts"
      to: "the 18 chart-root tags"
      via: "whole-codebase grep for accessibilityLayer={false} still passes"
      pattern: "accessibilityLayer=\\{false\\}"
---

<objective>
Wire CHART-01b touch parity into the 18 Recharts charts that carry a `<Tooltip>` today: swap each `<Tooltip ... />` for `<TouchTooltip ... />` (the shim from plan 01), which injects the `useBreakpoint`-gated `trigger` (`mobile → "click"` tap-to-show/pin; `desktop → "hover"` byte-identical). Leave `OutcomesWidget`'s tooltip-less sparkline alone (parity-only).

Purpose: This is the lazy-correct rung — Recharts already provides native tap-to-pin via `<Tooltip trigger="click">` (verified against `node_modules/recharts/types/component/Tooltip.d.ts`). One shared shim, one-token swaps, zero new gesture machinery (D-Area-1). The falsifiable proof of "no rewrite/no recompute" is desktop byte-identity: the desktop `trigger` defaults to `"hover"`, exactly what the shim injects.
Output: 18 chart files each importing `TouchTooltip` and rendering it in place of `<Tooltip>`, with every chart-root tag's literal `accessibilityLayer={false}` untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md
@.planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md
@.planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md

<interfaces>
From src/components/charts/TouchTooltip.tsx (created in plan 01):
```typescript
import { TouchTooltip } from "@/components/charts/TouchTooltip"; // or relative ./TouchTooltip for charts/* siblings
// drop-in replacement for recharts <Tooltip> — accepts the SAME props (formatter, contentStyle, ...)
export function TouchTooltip(props: ComponentProps<typeof Tooltip>): JSX.Element;
```

The exact swap (RollingMetrics.tsx:157-160, the representative target):
```tsx
// BEFORE: <Tooltip contentStyle={...} formatter={...} />
// AFTER:  <TouchTooltip contentStyle={...} formatter={...} />   // + import line; remove now-unused Tooltip import if no other use
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the 14 src/components Recharts charts (charts/ + portfolio/ + strategy/)</name>
  <files>src/components/charts/CorrelationWithBenchmark.tsx, src/components/charts/DrawdownChart.tsx, src/components/charts/NetGrossExposureChart.tsx, src/components/charts/ReturnHistogram.tsx, src/components/charts/RollingAlphaBetaChart.tsx, src/components/charts/RollingMetrics.tsx, src/components/charts/RollingSortinoChart.tsx, src/components/charts/RollingVolatilityChart.tsx, src/components/charts/TurnoverChart.tsx, src/components/charts/YearlyReturns.tsx, src/components/portfolio/AttributionBar.tsx, src/components/portfolio/CompositionDonut.tsx, src/components/portfolio/RiskAttribution.tsx, src/components/strategy/CompareEquityOverlay.tsx</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§"18 Recharts charts" — the exact one-token swap shape + the per-chart guardrail that the swap touches the `<Tooltip>` element ONLY, never the chart root tag, Pitfall 5)
    - src/components/charts/RollingMetrics.tsx (representative LineChart: lines 157-160 `<Tooltip contentStyle formatter>` and the recharts import line at the top)
    - src/components/portfolio/CompositionDonut.tsx (PieChart/Cell variant: lines 56-59 — confirms the spread is uniform, only formatter+contentStyle)
    - src/components/charts/chart-tokens.ts (CHART_BORDER / CHART_TOOLTIP_STYLE — any hex stays sourced here, never a fresh literal)
  </read_first>
  <action>
    For each of the 14 files: add `import { TouchTooltip } from "@/components/charts/TouchTooltip";` (use the relative `./TouchTooltip` for the four `src/components/charts/*` siblings if that matches the file's existing import style, else the `@/` alias). Replace the existing `<Tooltip ... />` element with `<TouchTooltip ... />`, keeping every existing prop (`formatter`, `contentStyle`, and for the Cell-based charts like CompositionDonut/YearlyReturns/AttributionBar/ReturnHistogram/RiskDecomposition the same formatter shape) verbatim. If `Tooltip` is no longer referenced anywhere else in the file, remove `Tooltip` from the `recharts` named import (leave the other recharts imports — `LineChart`/`AreaChart`/`BarChart`/`PieChart`/`ComposedChart`/`Cell`/axes — exactly as-is). DO NOT touch the chart-root opening tag (`<LineChart accessibilityLayer={false}>` etc.) — the `accessibilityLayer={false}` literal must stay byte-identical so the whole-codebase grep (`tests/visual/chart-accessibility-layer.test.ts`) stays green (Pitfall 5). DO NOT spread props onto the chart root. DO NOT introduce any new hex literal. This is a surgical, additive-only diff per Rule 3 — no adjacent-code "improvements".
  </action>
  <verify>
    <automated>npx vitest run src/components/charts/RollingMetrics.test.tsx src/components/charts/CorrelationWithBenchmark.test.tsx src/components/charts/NetGrossExposureChart.test.tsx src/components/charts/ReturnHistogram.test.tsx src/components/charts/RollingAlphaBetaChart.test.tsx src/components/charts/RollingSortinoChart.test.tsx src/components/charts/RollingVolatilityChart.test.tsx src/components/charts/TurnoverChart.test.tsx src/components/charts/YearlyReturns.test.tsx src/components/portfolio/CompositionDonut.test.tsx && npx vitest run tests/visual/chart-accessibility-layer.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 14 files contain `import { TouchTooltip }` and a `<TouchTooltip` element; `grep -rl "<Tooltip[ />]" <these 14 files>` returns nothing (every `<Tooltip>` swapped) UNLESS the file legitimately retains a non-recharts tooltip (none of these do).
    - `npx vitest run tests/visual/chart-accessibility-layer.test.ts` passes (the `accessibilityLayer={false}` grep + files.length≥18 floor both hold).
    - The existing per-chart tests listed in the verify command pass unchanged (desktop render byte-identical — desktop trigger defaults to hover, which the shim injects).
    - No file's chart-root opening tag changed: `git diff <file>` shows the change is confined to the import line(s) and the `<Tooltip>`→`<TouchTooltip>` element.
  </acceptance_criteria>
  <done>14 src/components charts render via TouchTooltip; accessibilityLayer grep green; per-chart tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Swap the 4 (dashboard)/allocations widget Recharts charts + verify OutcomesWidget parity-only</name>
  <files>src/app/(dashboard)/allocations/widgets/attribution/AlphaBetaDecomposition.tsx, src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx, src/app/(dashboard)/allocations/widgets/risk/RiskDecomposition.tsx, src/app/(dashboard)/allocations/widgets/risk/TailRisk.tsx</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-PATTERNS.md (§"18 Recharts charts" list + §"PARITY-ONLY — DO NOT TOUCH OutcomesWidget")
    - .planning/phases/48-recharts-equitychart-final-verification/48-UI-SPEC.md (the inventory table rows 1/3/4/5 = the 4 widget charts; row 2 = OutcomesWidget parity-only)
    - src/app/(dashboard)/allocations/widgets/outcomes/OutcomesWidget.tsx (confirm it has NO `<Tooltip>` — its Sparkline at ~L289-318 is a hidden-axis LineChart; its value lives in the adjacent KpiCell ~L231-286)
    - src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx (this allocator DrawdownChart is an AreaChart with a `<Tooltip>` — distinct from the separately-handled hand-rolled EquityChart in plan 03; do NOT touch EquityChart)
  </read_first>
  <action>
    For each of the 4 widget files (AlphaBetaDecomposition = BarChart, performance/DrawdownChart = AreaChart, RiskDecomposition = BarChart with Cell, TailRisk = BarChart): add `import { TouchTooltip } from "@/components/charts/TouchTooltip";` and swap the existing `<Tooltip ... />` for `<TouchTooltip ... />` with all existing props verbatim; drop the now-unused `Tooltip` from the `recharts` import if unreferenced elsewhere. Keep each chart-root tag's `accessibilityLayer={false}` literal untouched. Then OPEN `src/app/(dashboard)/allocations/widgets/outcomes/OutcomesWidget.tsx` READ-ONLY and confirm it contains no `<Tooltip>` element — make NO edit (parity-only: adding a tap-reveal to a chart with no desktop hover is the invented-interaction the contract forbids; its value is already in the KPI cell). This task must NOT modify OutcomesWidget.tsx and must NOT modify EquityChart.tsx (plan 03 owns EquityChart).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx" && npx vitest run tests/visual/chart-accessibility-layer.test.ts && bash -c 'test "$(grep -c "<Tooltip" "src/app/(dashboard)/allocations/widgets/outcomes/OutcomesWidget.tsx")" -eq 0'</automated>
  </verify>
  <acceptance_criteria>
    - The 4 widget files each contain `import { TouchTooltip }` and a `<TouchTooltip` element; none contains a residual `<Tooltip ` recharts element.
    - `grep -c "<Tooltip" src/app/(dashboard)/allocations/widgets/outcomes/OutcomesWidget.tsx` returns 0 AND `git diff --stat src/app/(dashboard)/allocations/widgets/outcomes/OutcomesWidget.tsx` shows no change (parity-only, untouched).
    - `git diff --stat "src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx"` shows NO change (plan 03 owns it; this plan must not touch it).
    - `npx vitest run tests/visual/chart-accessibility-layer.test.ts` passes.
    - The allocator DrawdownChart test passes (desktop render byte-identical).
  </acceptance_criteria>
  <done>4 widget charts render via TouchTooltip; OutcomesWidget confirmed untouched (no Tooltip); EquityChart untouched; accessibilityLayer grep green.</done>
</task>

<task type="auto">
  <name>Task 3: Extend per-chart desktop-byte-identity coverage + run full coverage ratchet</name>
  <files>src/components/charts/RollingMetrics.test.tsx, src/components/portfolio/CompositionDonut.test.tsx</files>
  <read_first>
    - .planning/phases/48-recharts-equitychart-final-verification/48-VALIDATION.md (CHART-01b row: "Each chart-family desktop render byte-identical — extend existing per-chart")
    - src/components/charts/RollingMetrics.test.tsx (existing test to extend — the LineChart family desktop snapshot)
    - src/components/portfolio/CompositionDonut.test.tsx (existing test to extend — the PieChart/Cell family desktop snapshot)
    - .planning/phases/48-recharts-equitychart-final-verification/48-RESEARCH.md (§Project Constraints — coverage ratchet lines 82/stmts 80/fns 74/branches 72; Assumption A1 — desktop byte-identity via default-hover)
  </read_first>
  <action>
    The branch-coverage of the `trigger` ternary is already owned by `TouchTooltip.test.tsx` (plan 01). Here, ADD a desktop-arm assertion to ONE representative LineChart test (RollingMetrics.test.tsx) and ONE representative PieChart/Cell test (CompositionDonut.test.tsx): with `useBreakpoint` defaulting to `"desktop"` (its SSR/jsdom snapshot), render the chart and assert the rendered Recharts tooltip resolves to `trigger="hover"` (or that no `trigger="click"` is present) — proving the desktop render is byte-identical (Assumption A1). Keep the assertions minimal and family-representative (one Line, one Pie); do not add a test per chart. Then run the full coverage gate and record the new actuals in the SUMMARY. If branches dipped below 72, add the missing branch assertion to `TouchTooltip.test.tsx` (do NOT lower any threshold, do NOT blanket-update a snapshot).
  </action>
  <verify>
    <automated>npx vitest run src/components/charts/RollingMetrics.test.tsx src/components/portfolio/CompositionDonut.test.tsx && npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - RollingMetrics.test.tsx and CompositionDonut.test.tsx each assert the desktop arm resolves to `trigger="hover"` (no `trigger="click"` in the default-desktop render).
    - `npm run test:coverage` exits 0 with lines ≥82, stmts ≥80, fns ≥74, branches ≥72 (no threshold lowered).
    - The new actual coverage numbers are recorded in 48-02-SUMMARY.md.
  </acceptance_criteria>
  <done>Desktop byte-identity asserted for the Line and Pie families; coverage ratchet held with recorded actuals.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none net-new) | Presentation-only change to render-time tooltip trigger; no input, no data path, no auth/session surface crosses any boundary in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-02-AL | Tampering | chart-root `accessibilityLayer={false}` grep | mitigate | Swap touches the `<Tooltip>` element ONLY; chart-root tags stay literal — verified by `chart-accessibility-layer.test.ts` staying green (Pitfall 5). |
| T-48-02-BI | Tampering | desktop chart render (byte-identity) | mitigate | Desktop `trigger` defaults to `"hover"`, which the shim injects; per-chart tests + the Line/Pie desktop-arm assertions prove no desktop change (the no-rewrite proof). |
</threat_model>

<verification>
- `npx vitest run tests/visual/chart-accessibility-layer.test.ts` green (grep + ≥18 files floor).
- All listed per-chart tests green (desktop byte-identical).
- `npm run test:coverage` exit 0, all four thresholds held, actuals recorded.
- `git diff --stat` confirms OutcomesWidget.tsx and EquityChart.tsx are untouched by this plan.
- Frozen-spine guard untouched: `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts` green.
</verification>

<success_criteria>
- All 18 tooltip-bearing Recharts charts render via TouchTooltip.
- OutcomesWidget sparkline parity-only (no Tooltip, no change).
- accessibilityLayer={false} whole-codebase grep green.
- Desktop render byte-identical (Line + Pie families asserted); coverage ratchet held.
- EquityChart.tsx untouched (plan 03 owns it).
</success_criteria>

<output>
Create `.planning/phases/48-recharts-equitychart-final-verification/48-02-SUMMARY.md` when done
</output>
