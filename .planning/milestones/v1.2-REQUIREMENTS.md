# Requirements: Quantalyze — v1.2 Allocator Cohesion

**Defined:** reconstructed-at-archive 2026-06-24
**Core Value:** Allocators act on Bridge recommendations and see whether those suggestions actually worked — and can model the impact of composition changes before they make them, in ONE cohesive composer.

> **Reconstructed at archive (2026-06-24).** No canonical v1.2 REQUIREMENTS.md was
> ever written into the live ledger — it desynced during execution (v1.2 shipped +
> deployed + QA-verified before its ledger files existed; lost to the gitignore-drop
> hazard). These requirements are reconstructed from what was actually built, using
> the phase 29–33 `*-SUMMARY.md` + `*-VERIFICATION.md` files as the source of truth
> (the requirement IDs are the UNIFY / GRAPH / LAYOUT / FLOW / JOURNEY families the
> executors tagged each plan with). Every requirement is COMPLETE — the milestone
> shipped (git tag `v1.2` @ `11775460`, app versions v0.29.0.0 → v0.30.0.1).

## v1.2 Requirements

Each maps to a phase (29–33) and was verified per-phase (`*-VERIFICATION.md`) + live via headed-browser /qa.

### Unified composer spine (Phase 29)

- [x] **UNIFY-01**: An allocator reaches ONE portfolio composer with an entry-mode control — compose from a blank slate OR seed from the live book in the same surface (`role="radiogroup"`, "From my book" / "Blank slate"). — *Done: entry-mode segmented control in `ScenarioComposer.tsx`; a no-book allocator defaults to Blank-slate-only.*
- [x] **UNIFY-02**: A dirty-draft entry-mode switch routes through the existing reset-confirmation modal — a mode switch can never silently wipe in-progress edits (the confirm path is the only mutator). — *Done: `ResetConfirmationModal` gates the switch; Cancel preserves the draft, confirm applies the parked mode.*
- [x] **UNIFY-03**: Browse verified + example-universe strategies in ONE tagged catalog — `is_example` co-fetched (no `.or()` published bypass), `displayStrategyName` pseudonymity preserved, neutral-outline "Example" provenance pill (never accent). — *Done: browse route + StrategyBrowseDrawer; H-0300 allow-list fence intact.*
- [x] **UNIFY-04**: An added catalog strategy lazily resolves its `daily_returns` via a scoped RLS route and actually moves the composer's projection through the unchanged adapter + frozen engine (closing the H-0133 / example-add data gap). — *Done: `GET /api/strategies/[id]/returns` (RLS `createClient()`, `withPublishedOnly`, honest `[]`); composer merges `fromBook ?? addedReturnsById[id] ?? []`; "Loading returns…" `role=status` affordance.*
- [x] **UNIFY-05**: Save/reopen named portfolios against the existing `scenarios` store with "portfolio" user-facing copy; the codec ok/readonly/reset trichotomy is preserved byte-for-byte; frozen-spine exit gates hold (no scenarios/share migration, `scenario.ts` zero-diff). — *Done: copy relabel across composer + SavedScenariosList; phase-29 frozen-spine guard + full-suite consolidation gate.*

### Factsheet graphs on the blend (Phase 30)

- [x] **GRAPH-01**: Equity + drawdown render in the factsheet visual identity on the blend (shared `chart-tokens.ts`, no inline hexes). — *Done: `DrawdownChart.tsx` reskinned to chart-tokens; `EquityChart` confirmed already CSS-var accent-compliant; both pinned by a token-contract test.*
- [x] **GRAPH-02**: A returns-distribution view (cumulative-wealth histogram + quantiles) on the blended portfolio. — *Done: `buildBlendPanels` derives `cumprod(1+r)` wealth (never raw daily / never the rounded curve); Returns-distribution Card mounts `ReturnHistogram` + `ReturnQuantiles`.*
- [x] **GRAPH-03**: Rolling Sharpe / volatility / Sortino on the blend with a 3M/6M/12M window control. — *Done: Rolling-metrics Card; rolling math mirrors `computeRollingMetric` (sample-std n-1 × √252; engine-mirror Sortino ÷ total-window-n) for exact parity; WR-01 window-label fix.*
- [x] **GRAPH-04**: Every blend graph declares its method/overlap-N/horizon, renders an honest empty state (`role=status`, never `role=alert`) below the sample floor, and NEVER shows peer/percentile/signature ranking on a hypothetical blend. — *Done: per-panel disclosures + `PartialDataBanner`; IMPACT-02 honesty guard extended non-vacuously WITH the panels mounted + a static import guard (no FactsheetBody/MetricsColumn/payload-builder/PercentileRankBadge, no `ingestSource:"api"`).*

### Graphs-lead layout (Phase 31)

- [x] **LAYOUT-01**: The strategy composition controls are collapsible so the factsheet-grade graphs lead the surface. — *Done: factsheet `CollapsibleSection` lifted to `src/components/ui/` (neutral open-all event + injected `onToggle`); `CompositionList` wrapped with a composer-scoped `storageKey`; graphs already DOM-ordered above → no panel reorder; collapse persists across reload.*
- [x] **LAYOUT-02**: Collapsing preserves every in-progress weight + leverage edit (HIDE, never UNMOUNT) and the projection still reflects them. — *Done: native `<details>` keeps `CompositionList` mounted; edit state (`leverageByRef`, `weightOverrides`) lives above the collapsible boundary; non-vacuous survive-collapse regression test + a no-conditional-mount exit-gate guard; CR-01 cross-account localStorage leak fixed.*

### Dead-link fix & route retirement (Phase 32)

- [x] **FLOW-01**: Adding a strategy from a portfolio context attaches back with no dead link (the bare `/discovery/crypto-sma` dead link fixed). — *Done: portfolio-context links carry `?portfolio={id}`; shipped as the manual add-dropdown path on `/discovery/crypto-sma` (the planned auto-attach was reworked at /ship #520 as a dead/unreachable feature; revert netted those files zero-diff).*
- [x] **FLOW-02**: `/scenarios` resolves into the unified composer with no dead links and no manager breakage, and NO table DDL. — *Done: `/scenarios/page.tsx` is a thin server-component 307 `redirect("/allocations?tab=scenario")`; `ScenarioBuilder.tsx` + honesty test deleted; composer self-loop removed; `/portfolios` + its 30+ consumers untouched; net security win (eliminates the `createAdminClient()` RLS-bypass read).*
- [x] **FLOW-03**: A new allocator has ONE discoverable nav entry (`/allocations`); managers keep `/portfolios` (not orphaned). — *Done: `/scenarios` Strategy Sandbox sidebar item + unused `BeakerIcon` removed; Sidebar retirement gate asserts no role resurrects it; phase-32 frozen-spine + 6-content-gate retirement guard + knip no-new-orphans exit-gate.*

### Journey polish (Phase 33)

- [x] **JOURNEY-01**: An allocator can carry a Bridge recommendation into the composer (Bridge → composer continuity) and it MOVES the projection. — *Done: non-vacuous `bridge-to-composer-seam.test.tsx` drives `addStrategyBridge` → `computeMetricsForDraft` and asserts a twr + volatility delta (RED when neutered); FLOW-01 reachability guard pins the composer-owned BridgeDrawer as the sole `onAddToScenario` seam (1 vs 0).*
- [x] **JOURNEY-02**: The Discovery / onboarding entry points and empty states are visually consistent with DESIGN.md. — *Done: both blank-slate CTAs carry the accent `focus-visible:ring-2 focus-visible:ring-accent/50` token (focus-ring confirmed painting live); 3 other inventoried surfaces verified compliant + byte-unchanged.*
- [x] **JOURNEY-03**: The unified composer + new graphs pass automated accessibility checks at WCAG-AA. — *Done: `e2e/composer-axe.spec.ts` scans `/allocations?tab=scenario` incl. the Phase-30 cards with a triple false-green guard + zero rule suppression; GREEN in CI — caught + fixed 3 real prod WCAG-AA violations (duplicate `<main>`, Export/+Allocation inside `role=tablist`, `role=region` on `<footer>`).*

## Out of Scope (v1.2)

Explicitly excluded — carried forward from the v1.2 milestone framing.

| Feature | Reason |
|---------|--------|
| Full Overview-factsheet recompute on a hypothetical blend | False-precision risk — `ingestSource:"api"` unlocks peer/allocator panels on a what-if (no-invented-data violation). Revisit only if an allocator asks twice. |
| Consolidating the 3 correlation surfaces (Risk-tab matrix / reusable heatmap / `/scenarios`) | Code-motion with parallel-agent collision risk; defer until a feature forces convergence on the WCAG-audited palette. |
| Decision-memo generator | Strong future direction, not this milestone. |
| Scenario export (PDF / CSV) | Defer until save/load proves the workflow. |
| Touching `/portfolios` (route-level redirect) | No route-level role gate + 25–30+ consumers — a redirect there would strand managers. |

## Deferred to v1.2.1 (scenario-tab-hardening)

- `api_key_id`-per-key dailies unification.
- Overview-stat repoint onto the unified composer source.
- Composer chart-parity items #3 / #4.
- Blank-mode equity-projection gap.
- (PR #522 v0.30.1.0 shipped post-tag as a v1.2.1-prelude: honest-Overview / blank-slate fix.)
- WR-03 (pre-existing from Phase 25-03): "Copy link" silent share-token rotation — advisory.

## Traceability

Reconstructed at archive (2026-06-24) from the phase 29–33 SUMMARY/VERIFICATION files.
Phases continue from v1.1.0's Phase 28 (no reset) — this milestone is Phases 29–33.

| Requirement | Phase | Status |
|-------------|-------|--------|
| UNIFY-01 | Phase 29 | Complete |
| UNIFY-02 | Phase 29 | Complete |
| UNIFY-03 | Phase 29 | Complete |
| UNIFY-04 | Phase 29 | Complete |
| UNIFY-05 | Phase 29 | Complete |
| GRAPH-01 | Phase 30 | Complete |
| GRAPH-02 | Phase 30 | Complete |
| GRAPH-03 | Phase 30 | Complete |
| GRAPH-04 | Phase 30 | Complete |
| LAYOUT-01 | Phase 31 | Complete |
| LAYOUT-02 | Phase 31 | Complete |
| FLOW-01 | Phase 32 | Complete |
| FLOW-02 | Phase 32 | Complete |
| FLOW-03 | Phase 32 | Complete |
| JOURNEY-01 | Phase 33 | Complete |
| JOURNEY-02 | Phase 33 | Complete |
| JOURNEY-03 | Phase 33 | Complete |

**Coverage:**
- v1.2 requirements: 17 total
- Mapped to phases: 17 ✓
- Complete: 17 / 17

---
*Requirements reconstructed at archive: 2026-06-24 (the live ledger desynced during execution; the milestone shipped before its REQUIREMENTS were written).*
