---
phase: 43-edge-states-toggle-fold-guards
plan: 03
type: execute
wave: 2
depends_on: ["43-01"]
files_modified:
  - "e2e/composer-axe.spec.ts"
  - "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
autonomous: true
requirements: [GUARD-03, GUARD-01]
must_haves:
  truths:
    - "The composer WCAG-AA axe gate covers the new factsheet body + the Diversification/Peer/Mandate/OwnBookDelta sections (axe serious+critical = 0); the coverage ratchet stays green (lines 82 / fns 74 / branches 72)"
    - "The static guard (no FactsheetBody literal in ScenarioComposer.tsx) is asserted by an automated test, not just an ad-hoc grep"
    - "The assembled folded surface renders honest empty/safe states SIMULTANEOUSLY across the full degenerate matrix (0/1 constituent, n<10, n<252, no own-book, no mandate)"
  artifacts:
    - path: "e2e/composer-axe.spec.ts"
      provides: "extended Scan 2 with visible-anchor gates for #factsheet-main + Diversification/Peer/Mandate sections before analyze()"
      contains: "factsheet-main"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "static-guard assertion (FactsheetBody literal count == 0) + assembled-surface degenerate-matrix cross-check"
      contains: "FactsheetBody"
  key_links:
    - from: "composer-axe.spec.ts Scan 2"
      to: "the new folded surface (#factsheet-main + new sections)"
      via: "visible-anchor gates before analyze() (defense against false-green if the body fails to mount)"
      pattern: "factsheet-main"
    - from: "ScenarioComposer.test.tsx degenerate cross-check"
      to: "the assembled folded surface honest-empty states"
      via: "integration render across the degenerate matrix"
      pattern: "add a second strategy"
---

<objective>
GUARD-03 (axe + coverage) + the remaining GUARD-01 edge-state work: extend the already-CI-wired composer-axe e2e to cover the newly-folded factsheet body + sections, add the static-guard assertion as an automated test, and add the assembled-surface degenerate-matrix cross-check (the one genuine new edge-state gap research identified).

Purpose: prove WCAG-AA on the assembled "one surface" and prove every degenerate blend renders honestly on the FOLDED surface (not just each panel in isolation, which the per-phase tests already covered). Wave 2 because the axe anchors + the cross-check assert against the folded layout that plan 43-01 produces.
Output: extended e2e/composer-axe.spec.ts (verify-only CI wiring — FLOW-01 does NOT apply) + extended ScenarioComposer.test.tsx (static guard + degenerate cross-check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-CONTEXT.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-VALIDATION.md

<interfaces>
<!-- Verified file:line anchors this session. -->

e2e/composer-axe.spec.ts (full read, 148 lines — VERIFY against current file as 43-01 may have shifted anchors):
- :42 const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY
- :61 test.skip(!HAS_SEED_ENV, ...) — authored-but-skipped (do NOT touch the gating)
- buildAxe(page) from ./helpers/axe; withTags(["wcag2a","wcag2aa","best-practice"]) — REUSE verbatim, no new dep
- Flow: seedStrategyWithHistory({days:400}) -> seedTestAllocator() -> loginViaForm -> goto /allocations?tab=scenario -> Scan 1 blank-slate (gated on h2 "Start a portfolio") -> drive into composed mode (Browse -> add first -> close drawer) -> Scan 2 composed (gated on h2 "Portfolio" + [data-panel="blend-returns-distribution"] + [data-panel="blend-rolling"] visible) -> expect(violations).toEqual([])
- :124-130 the "either real OR honest-empty banner is a real surface" idiom — REUSE for sections that honestly empty at n=1
- CI wiring CONFIRMED: ci.yml:1261 lists e2e/composer-axe.spec.ts in the seeded playwright npx list. FLOW-01 does NOT apply (extends an existing, already-listed, already-HAS_SEED_ENV-gated spec). VERIFY-ONLY: confirm line 1261 still lists it; do NOT re-add to HAS_SEED_ENV or ci.yml.

New folded-surface anchors (post-43-01) to gate Scan 2 on (before analyze()):
- the factsheet body article: [id="factsheet-main"] (FactsheetView.tsx:188) — proves the real FactsheetBody mounted
- the Diversification CollapsibleSection title "Diversification" (ScenarioComposer.tsx:2603)
- the folded Data-sources section ("Data sources" CollapsibleSection from 43-01)
- Mandate / Peer / OwnBookDelta sections: gate on "section present (real OR honest-empty banner)" — a single-strategy seed honestly empties Diversification/Mandate (n<2 / no metadata); MandatePanels honest-empty copy = "No mandate metadata available for this blend's constituents." (MandatePanels.tsx:204)

ScenarioComposer.test.tsx — extend (existing file):
- static guard: assert grep-equivalent (readFileSync the ScenarioComposer.tsx source, assert it contains "FactsheetBody" zero times) — mirrors composer-width.test.tsx static-source-scan pattern
- degenerate matrix: the per-phase panel tests already prove each panel; this cross-check renders the ASSEMBLED folded surface and asserts the honest-empty states co-exist
- Diversification 0/1-constituent honest empty copy = "add a second strategy to see diversification" (CORR-03)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend composer-axe.spec.ts Scan 2 with new folded-surface visible-anchor gates</name>
  <files>e2e/composer-axe.spec.ts</files>
  <read_first>
    - e2e/composer-axe.spec.ts (FULL current file — re-read; 43-01 may have shifted the composed-mode flow; confirm the Scan 2 anchors blend-returns-distribution + blend-rolling still exist at ScenarioComposer.tsx:2785/:2835 and that the body mounts)
    - .planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md §must-answer 4 (what to ADD; the single analyze() already covers new DOM; the additive work is visible-anchor gates) + Open Question 1 (gate on "real OR honest-empty") + Pitfall 4 (FLOW-01 does NOT apply — verify-only)
    - .github/workflows/ci.yml line ~1261 (confirm composer-axe.spec.ts is still listed — do NOT re-add)
  </read_first>
  <action>
    Extend the EXISTING e2e/composer-axe.spec.ts (do NOT create a new spec — FLOW-01 does not apply; the spec is already CI-wired at ci.yml:1261 and already HAS_SEED_ENV-gated). In Scan 2 (the composed surface), BEFORE the existing analyze()/expect(violations).toEqual([]), add visible-anchor gates for the new folded surface so axe cannot false-green on a body that failed to mount: (a) the factsheet body article [id="factsheet-main"] (proves the real FactsheetBody mounted); (b) the Diversification CollapsibleSection heading "Diversification" (:2603); (c) the folded "Data sources" section heading (from 43-01). For the Mandate / Peer / OwnBookDelta sections — which honestly empty at the single-strategy seed — gate on "section present (real OR honest-empty banner)" using the existing idiom at :124-130 (do NOT require non-degenerate bodies; a single seeded strategy renders honest-empty Diversification/Mandate). Keep the single analyze() over the whole composed <main> — it already covers any newly-mounted DOM; do NOT add a second axe call. Do NOT touch the HAS_SEED_ENV gate, the buildAxe harness, or ci.yml. After editing, VERIFY (read-only) that ci.yml still lists e2e/composer-axe.spec.ts in the seeded playwright block.
  </action>
  <verify>
    <automated>grep -q "factsheet-main" e2e/composer-axe.spec.ts && grep -q "composer-axe.spec.ts" .github/workflows/ci.yml && npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "composer-axe" | grep -c "composer-axe" | grep -qx 0</automated>
  </verify>
  <acceptance_criteria>
    - composer-axe.spec.ts Scan 2 gates on [id="factsheet-main"] + "Diversification" + "Data sources" before analyze()
    - Mandate/Peer/OwnBookDelta gated with the "real OR honest-empty banner" idiom (no requirement of non-degenerate bodies)
    - The single analyze() + expect(violations).toEqual([]) is preserved (no second axe call)
    - HAS_SEED_ENV gate + buildAxe harness untouched; ci.yml still lists the spec (verify-only, no re-add)
    - tsc clean on the spec
  </acceptance_criteria>
  <done>The axe e2e covers the assembled folded surface with anti-false-green anchors; CI wiring confirmed intact; FLOW-01 not triggered.</done>
</task>

<task type="auto">
  <name>Task 2: Static-guard assertion + assembled-surface degenerate-matrix cross-check (in ScenarioComposer.test.tsx)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (existing composer test — the mount harness + how it drives degenerate blends)
    - src/app/(dashboard)/allocations/components/__tests__ or composer-width.test.tsx (the static-source-scan pattern: readFileSync + className/literal substring assertions)
    - .planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md §must-answer 6 (the assembled-surface cross-check is the ONE genuine GUARD-01 gap; per-panel honesty already proven) + the honest-empty copy strings (Diversification "add a second strategy to see diversification"; Mandate "No mandate metadata available for this blend's constituents")
  </read_first>
  <action>
    Extend ScenarioComposer.test.tsx with two assertions. (a) STATIC GUARD: add a test that readFileSync-reads the ScenarioComposer.tsx source and asserts it contains the literal "FactsheetBody" ZERO times (mirrors the composer-width.test.tsx static-source-scan; render-engine-independent and permanent — the mount must stay exclusively in ScenarioFactsheetChart.tsx). (b) DEGENERATE-MATRIX CROSS-CHECK: add an integration render of the ASSEMBLED folded surface (body + folded Data-sources + Diversification + Peer + Mandate + OwnBookDelta) on degenerate blends, asserting the honest empty/safe states co-exist SIMULTANEOUSLY (the per-phase tests prove each panel in isolation; this proves the assembled surface). Cover the matrix research lists: 0/1-constituent (Diversification shows "add a second strategy to see diversification"; no NaN/Inf; safe-empty KPIs), n<10 and n<252 (low-N caveats fire; no fabricated metrics), no-own-book (OwnBookDelta degrades honestly), no-mandate (MandatePanels shows "No mandate metadata available for this blend's constituents"). Use the existing composer test mount/seed helpers; do NOT introduce a real engine call beyond what the composer test already wires (the blend re-blends through the frozen computeScenario via existing channels). Also confirm the folded Data-sources section honestly disappears (showDataSources false) in blank/degenerate mode and the all-excluded empty still routes after the fold.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism && grep -q "add a second strategy" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - A static-guard test asserts ScenarioComposer.tsx contains "FactsheetBody" zero times (readFileSync source scan)
    - An assembled-surface render across the degenerate matrix (0/1-constituent, n<10, n<252, no-own-book, no-mandate) asserts honest empty/safe states co-exist (no NaN/Inf, no fabricated zeros)
    - Diversification honest-empty ("add a second strategy...") + Mandate honest-empty ("No mandate metadata...") both assert on the SAME degenerate render
    - The folded Data-sources section disappears when showDataSources is false; the all-excluded empty still routes
    - ScenarioComposer.test.tsx green
  </acceptance_criteria>
  <done>The static guard is a permanent automated test; the assembled folded surface is proven honest across the full degenerate matrix; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seeded composed surface -> rendered DOM (e2e) | The real-Chromium WCAG-AA scan surface; the only runtime-rendered boundary this plan asserts on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-06 | Tampering | static-guard separation (FactsheetBody literal leaking into ScenarioComposer.tsx) | mitigate | A permanent readFileSync source-scan test asserts count == 0 (not just an ad-hoc grep) |
| T-43-07 | Denial of Service (a11y exclusion) | WCAG-AA regressions on the assembled folded surface | mitigate | composer-axe.spec.ts extended with visible-anchor gates (anti-false-green) + the existing axe analyze() over the whole composed main |
| T-43-08 | Information Disclosure (dishonest empty) | a degenerate blend presenting fabricated/NaN values on the assembled surface | mitigate | the degenerate-matrix cross-check asserts honest empty/safe states co-exist across the full matrix |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism` green
- `npm run test:coverage` ratchet green (lines 82 / fns 74 / branches 72)
- `grep -q "composer-axe.spec.ts" .github/workflows/ci.yml` (CI wiring intact — verify-only)
- (CI, seed env) `npx playwright test e2e/composer-axe.spec.ts` — axe serious+critical = 0 on blank-slate + composed
</verification>

<success_criteria>
- GUARD-03: axe e2e extended over the new body + sections (serious+critical = 0); coverage ratchet green; CI wiring confirmed (no FLOW-01 re-add)
- GUARD-01 closing: static guard is a permanent test; the assembled folded surface renders honestly across the full degenerate matrix
</success_criteria>

<output>
Create `.planning/phases/43-edge-states-toggle-fold-guards/43-03-SUMMARY.md` when done
</output>
