---
phase: 43-edge-states-toggle-fold-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
  - "src/app/factsheet/[id]/v2/FactsheetView.tsx"
  - "src/app/factsheet/[id]/v2/MandatePanels.tsx"
  - "src/app/globals.css"
  - "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
autonomous: true
requirements: [GUARD-01]
must_haves:
  truths:
    - "The Phase-37 Data-sources include/exclude toggle reads as a factsheet-shaped editorial section sibling to Diversification + Strategies-&-weights (compose + read on one surface)"
    - "The footer 'Page 1 / 1' page-stamp does NOT render on screen in the composer mount (scenarioMode), while the disclaimer still shows; the real /factsheet route still shows the stamp"
    - "The Diversification too-similar badge resolves through warning tokens (dark-mode/AA aware), not hardcoded hex"
    - "The 'risk-reducing' PCR tag reads as neutral accent, not P&L green"
    - "A leverage chip of exactly 1x is suppressed (leverage-alone is noise); >1x still renders"
    - "border-text / text-text-2 resolve correctly in light mode via @theme tokens WITHOUT changing any live factsheet class string"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Data-sources toggle folded into a factsheet-shaped CollapsibleSection; badge + PCR token swaps; new-section h3 normalized to the v2 contract; mount-seam padding compensated"
      contains: "bg-warning-bg border-warning-border"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "FactsheetFooter takes additive scenarioMode (default false); Page-stamp scenarioMode-gated"
      contains: "scenarioMode"
    - path: "src/app/factsheet/[id]/v2/MandatePanels.tsx"
      provides: "leverage chip guarded by c.leverage > 1"
      contains: "c.leverage > 1"
    - path: "src/app/globals.css"
      provides: "--color-text / --color-text-2 light-mode @theme inline entries"
      contains: "--color-text:"
  key_links:
    - from: "ScenarioComposer.tsx Data-sources CollapsibleSection"
      to: "the existing showDataSources role=group block"
      via: "reposition the EXISTING controls (reuse, no redesign)"
      pattern: "showDataSources"
    - from: "FactsheetBody:291 FactsheetFooter call site"
      to: "FactsheetFooter scenarioMode param"
      via: "thread scenarioMode (already in FactsheetBody scope)"
      pattern: "FactsheetFooter"
---

<objective>
GUARD-01: fold the existing compose toggles into the factsheet-shaped layout (compose + read on one surface) and land the seven accumulated Phase-40/41/42 UI-review polish carry-forwards — all additive or byte-identity-preserving.

Purpose: close the milestone's visual debt and make the composer read as ONE factsheet surface, without touching the FROZEN `scenario.ts` engine or breaking byte-identity of the real `/factsheet/[id]/v2` route (GUARD-02 in plan 43-02 then pins it permanently).
Output: repositioned Data-sources toggle in a CollapsibleSection sibling; scenarioMode-gated footer stamp; token-swapped badge + PCR tag; guarded leverage chip; formalized `--color-text`/`--color-text-2` tokens; normalized new-section h3; compensated mount-seam padding; extended ScenarioComposer.test.tsx assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-CONTEXT.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-VALIDATION.md
@DESIGN.md

<interfaces>
<!-- Verified file:line anchors this session (line numbers shift with edits — re-grep the named token if a prior task in THIS plan moved them). -->

Data-sources toggle (GUARD-01 fold target) — ScenarioComposer.tsx:
- :1395 const showDataSources = usePerKeySources
- :2339 {showDataSources && ( <div role="group" aria-label="Data sources"> ... )}  <- the block to fold
- :2395 {showDataSourcesFallback && ( ... )}  <- fallback, keep wired
- :2459 <div className="relative mt-6"> ... :2464 <ScenarioFactsheetChart ...>  <- body mount + seam
- :2603 <CollapsibleSection title="Diversification" ...>  <- precedent container (storageKey OMITTED at :2595)
- :2620 too-similar badge: bg-[#FEF3C7] border-[#FDE68A]  -> swap to tokens
- :2743 risk-reducing PCR tag: bg-positive/10 ... text-positive  -> swap to accent
- :2962 Strategies-&-weights <CollapsibleSection storageKey="composer-collapse:controls"> (already factsheet-shaped; LEAVE its storageKey)
- new-section h3s use tracking-wider; the v2 contract is text-[12px] ... tracking-[0.18em]

FactsheetFooter (FactsheetView.tsx):
- :291 {!hideFooter && <FactsheetFooter payload={payload} />}  <- call site is inside FactsheetBody, which HAS scenarioMode in scope (:169 scenarioMode = false)
- :968 function FactsheetFooter({ payload }: { payload: FactsheetPayload })  <- add scenarioMode?: boolean (default false)
- :971 <footer className="mt-16 border-t border-text pt-6 ...">  <- keep the disclaimer <p> unconditional
- :982 Page 1 / 1  <- wrap this <p> in {!scenarioMode && (...)}
- v2 h3 contract reference: :385 text-[12px] font-semibold uppercase tracking-[0.18em] text-text-primary

MandatePanels.tsx:
- :192 <Chip>{formatLeverage(c.leverage)}x</Chip>  <- guard with {c.leverage > 1 && (...)}
- :204 honest-empty "No mandate metadata available for this blend's constituents."

globals.css:
- :3 @theme inline {  — --color-text-primary(:13) / -secondary(:14) / -muted(:18) exist; --color-text / --color-text-2 ABSENT (grep count 0)
- :20 --color-accent; :56 --color-warning-bg #FEF3C7; :57 --color-warning-border #FDE68A; dark override :453 .bg-warning-bg[data-theme=dark]
- border-text / text-text-2 are used on LIVE factsheet elements (FactsheetView.tsx:420/447/971) — DO NOT repoint those classes (byte-identity); formalize the tokens so the SAME class resolves in light mode.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fold the Data-sources toggle into a factsheet-shaped CollapsibleSection</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the showDataSources block :2339-:2393, the showDataSourcesFallback :2395, the body mount :2459-:2464, the Diversification CollapsibleSection precedent :2603, the Strategies-&-weights CollapsibleSection :2962)
    - .planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md §must-answer 1 + Pattern 1 (the exact fold recommendation: a new CollapsibleSection around the EXISTING controls; do NOT push toggles into ScenarioFactsheetChart.tsx)
  </read_first>
  <action>
    Reposition the EXISTING Phase-37 Data-sources role="group" block (currently :2339-:2393, showDataSources && <div role="group" aria-label="Data sources">...) into a factsheet-shaped CollapsibleSection so it sits as a sibling editorial section with Diversification (:2603) and Strategies-&-weights (:2962) — compose + read on one surface. REUSE the existing controls verbatim (the per-key role="switch" rows): only the wrapping container + DOM position change, NOT a visual redesign of the toggles (per locked decision GUARD-01). Use the same CollapsibleSection import the Diversification/Strategies sections already use; give it a clear title (e.g. "Data sources"). Do NOT add a storageKey (mirror Diversification's deliberate omission at :2595 — GUARD-04 in plan 43-03 asserts no new persisted key). Keep the showDataSourcesFallback (:2395) and the all-excluded empty state wired and reachable after the move. Static guard: do NOT add a FactsheetBody literal or import to ScenarioComposer.tsx, and do NOT move the body mount — it stays in ScenarioFactsheetChart.tsx (the only file with that literal). Fold the toggle AROUND <ScenarioFactsheetChart/>, not inside it. Do NOT touch scenario.ts (FROZEN) — the toggle already re-blends through the frozen computeScenario via the existing includeByApiKeyId/projectionState.selected channel; this is JSX-only repositioning.
  </action>
  <verify>
    <automated>grep -c "FactsheetBody" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" | grep -qx 0 && npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "FactsheetBody" ScenarioComposer.tsx == 0 (static guard holds)
    - git diff src/lib/scenario.ts is empty (engine FROZEN — zero diff)
    - The Data-sources role="group" aria-label="Data sources" controls render inside a CollapsibleSection; the per-key role="switch" rows are byte-unchanged in behavior (same handlers)
    - No storageKey added to the new Data-sources section
    - showDataSourcesFallback + all-excluded empty state still render in their respective states
    - ScenarioComposer.test.tsx passes
  </acceptance_criteria>
  <done>The Data-sources toggle reads as a factsheet-shaped CollapsibleSection sibling around the body mount; static guard + frozen-engine invariants intact; composer tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: scenarioMode-gate the footer page-stamp (additive, byte-identity-preserving)</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (FactsheetFooter :968, the page-stamp <p> :982, the disclaimer <p> :972, the call site :291 inside FactsheetBody which has scenarioMode at :169)
    - .planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md §must-answer 2 row P40-W1 (additive, default false -> byte-identical)
  </read_first>
  <behavior>
    - With scenarioMode={false} (or omitted): FactsheetFooter renders the "Page 1 / 1" stamp <p> AND the disclaimer <p> — innerHTML unchanged from today (real route byte-identical; GUARD-02 in plan 43-02 pins this)
    - With scenarioMode={true}: the "Page 1 / 1" stamp <p> is ABSENT; the disclaimer <p> still renders
  </behavior>
  <action>
    Add an additive scenarioMode?: boolean param to FactsheetFooter (:968, currently function FactsheetFooter({ payload }: { payload: FactsheetPayload })) defaulting to false. Thread it from the call site (:291, {!hideFooter && <FactsheetFooter payload={payload} />} — scenarioMode is already in FactsheetBody scope at :169) as scenarioMode={scenarioMode}. Wrap ONLY the Page 1 / 1 stamp <p> (:982) in {!scenarioMode && ( ... )}. Keep the disclaimer <p> (:972) and the <footer className="mt-16 border-t border-text pt-6 ..."> element UNCONDITIONAL — do NOT change the footer's class string (border-t border-text stays; the token fix is Task 3). Default false means every existing call site (page.tsx, Discovery, Overview EquityChartWidget — none pass scenarioMode) renders identical HTML.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - FactsheetBody.scenario-mode.test.tsx innerHTML-equality test (default == scenarioMode={false}) stays GREEN — proves the stamp is still present at default/false
    - At scenarioMode={true} the "Page 1 / 1" text is absent; the disclaimer text is present (add/extend an assertion to the scenario-mode test)
    - The <footer> class string (border-t border-text) is unchanged
  </acceptance_criteria>
  <done>FactsheetFooter takes an additive scenarioMode (default false); the page-stamp hides only in scenarioMode; the real route stays byte-identical.</done>
</task>

<task type="auto">
  <name>Task 3: Token swaps + leverage guard + @theme token formalization + h3 normalize + seam padding</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/factsheet/[id]/v2/MandatePanels.tsx, src/app/globals.css</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (badge :2620, PCR tag :2743, new-section h3s using tracking-wider, the mount seam div :2459 relative mt-6)
    - src/app/factsheet/[id]/v2/MandatePanels.tsx (leverage chip :192, honest-empty :204)
    - src/app/globals.css (@theme inline :3; --color-text-primary :13, --color-accent :20, --color-warning-bg :56, --color-warning-border :57; --color-text / --color-text-2 ABSENT; dark overrides :453/:468)
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (v2 h3 contract reference :385 text-[12px] ... tracking-[0.18em] text-text-primary; live border-text/text-text-2 uses at :420/:447/:971)
    - .planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md §must-answer 2 (all rows) + Pitfall 1 (do NOT repoint a live factsheet class)
  </read_first>
  <action>
    Apply the remaining six polish carry-forwards. (a) Diversification too-similar badge (ScenarioComposer.tsx:2620): replace the hardcoded bg-[#FEF3C7] border-[#FDE68A] with bg-warning-bg border-warning-border (tokens exist globals.css:56/57 + dark override :453) — additive scenario section, no byte-identity impact. (b) Risk-reducing PCR tag (:2743): replace bg-positive/10 ... text-positive with bg-accent/10 text-accent (token globals.css:20 + dark override :468) — removes the P&L-green connotation. (c) Leverage chip (MandatePanels.tsx:192): guard the chip so 1x is suppressed — wrap <Chip>{formatLeverage(c.leverage)}x</Chip> in {c.leverage > 1 && (...)} (leverage-alone is noise; >1x still renders). (d) @theme token formalization (globals.css @theme inline block :3): ADD --color-text and --color-text-2 light-mode entries so border-text / text-text-2 (used on LIVE factsheet elements at FactsheetView.tsx:420/447/971) resolve correctly in light mode WITHOUT changing any class string — choose values matching the existing dark-mode override intent / the design ladder (e.g. --color-text aligned to the primary text ink, --color-text-2 to the secondary). Do NOT repoint border-text/text-text-2 classes on the live factsheet — that changes the real route's HTML and breaks byte-identity (Pitfall 1). (e) h3 type contract: normalize ONLY the NEW factsheet-shaped composer sections' h3/heading (the tracking-wider headings in the Data-sources/Diversification composer sections) to the v2 contract text-[12px] ... tracking-[0.18em] (reference FactsheetView.tsx:385) — do NOT churn pre-existing factsheet files. (f) Mount-seam padding (P40-W2): compensate the compound vertical padding at the seam on the COMPOSER side only — adjust the composer mount wrapper margin (<div className="relative mt-6"> at :2459, e.g. a reduced/negative-margin compensator) so the seam reads single-padded; do NOT change the FactsheetBody article class py-6 sm:py-10 lg:py-12 (FactsheetView.tsx:192 — byte-identity).
  </action>
  <verify>
    <automated>grep -q "bg-warning-bg border-warning-border" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" && grep -q "bg-accent/10 text-accent" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" && grep -q "c.leverage > 1" "src/app/factsheet/[id]/v2/MandatePanels.tsx" && grep -Eq -- "--color-text:|--color-text-2:" src/app/globals.css && ! grep -q "FEF3C7" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" && npx vitest run "src/app/factsheet/[id]/v2/" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - ScenarioComposer.tsx no longer contains FEF3C7/FDE68A; the badge uses bg-warning-bg border-warning-border
    - The PCR tag uses bg-accent/10 text-accent (no bg-positive/text-positive on that tag)
    - MandatePanels.tsx guards the leverage chip with c.leverage > 1 (1x suppressed, >1x renders)
    - globals.css @theme inline contains --color-text AND --color-text-2 light-mode entries; no live factsheet class string was repointed (FactsheetView.tsx still uses border-text/text-text-2 verbatim)
    - The new composer sections' headings use text-[12px] tracking-[0.18em]; pre-existing factsheet h3s untouched
    - The FactsheetBody article class py-6 sm:py-10 lg:py-12 is unchanged; only the composer-side seam margin moved
    - FactsheetBody.scenario-mode.test.tsx innerHTML equality + the factsheet + composer suites stay GREEN
  </acceptance_criteria>
  <done>All six remaining polish fixes land additively/byte-safely; tokens formalized without class repointing; new-section h3 normalized; seam compensated composer-side; suites green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer client JSX -> rendered DOM | Pure client render; no untrusted input crosses; toggles reuse existing validated Phase-37 channels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | byte-identity of the real /factsheet/[id]/v2 route (footer + token edits) | mitigate | Footer change is additive (scenarioMode default false); token fix is a @theme add, NOT a class repoint; FactsheetBody.scenario-mode.test innerHTML equality stays green (GUARD-02 in 43-02 makes this permanent) |
| T-43-02 | Tampering | FROZEN scenario.ts engine | mitigate | JSX-only fold; `git diff src/lib/scenario.ts` must be empty (frozen-spine guards) |
| T-43-03 | Information Disclosure | static-guard separation (no FactsheetBody literal in ScenarioComposer) | mitigate | grep -c FactsheetBody ScenarioComposer.tsx == 0; the fold wraps AROUND the mount island, never inside it |
</threat_model>

<verification>
- `grep -c "FactsheetBody" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` == 0
- `git diff src/lib/scenario.ts` empty
- `npx vitest run "src/app/factsheet/[id]/v2/" "src/app/(dashboard)/allocations/" --no-file-parallelism` green
- `tsc` clean; frozen-spine guards green
</verification>

<success_criteria>
- GUARD-01 fold: Data-sources toggle reads as a factsheet-shaped CollapsibleSection sibling around the body; Browse + scenario include/exclude already factsheet-shaped and co-located
- All seven polish carry-forwards landed (footer gate, badge token, PCR token, leverage guard, @theme tokens, h3 normalize, seam padding)
- Byte-identity of the real route preserved (additive footer + token-add only); scenario.ts zero-diff; static guard intact
</success_criteria>

<output>
Create `.planning/phases/43-edge-states-toggle-fold-guards/43-01-SUMMARY.md` when done
</output>
