---
phase: 43-edge-states-toggle-fold-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx"
  - "src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx"
autonomous: true
requirements: [GUARD-02, GUARD-04]
must_haves:
  truths:
    - "A PERMANENT regression gate pins the real /factsheet/[id]/v2 route byte-identical at scenarioMode default (default == scenarioMode={false} innerHTML on a populated payload)"
    - "The Overview EquityChartWidget path is proven untouched (does not mount FactsheetBody / #factsheet-main)"
    - "Mounting the body under persist={false} writes NOTHING to the factsheet keyspace (no factsheet-v2: localStorage, no ?range/?cmp/?dark URL mutation) — the Phase-38 RT2 class stays closed"
    - "The legitimate composer-collapse:controls key is NOT false-flagged by the bleed test"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx"
      provides: "PERMANENT GUARD-02 byte-identity gate (promoted from per-phase) + Overview-untouched assertion"
      contains: "PERMANENT"
    - path: "src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx"
      provides: "GUARD-04 cross-tab-bleed spy test scoped to the factsheet keyspace/URL"
      contains: "persist={false}"
  key_links:
    - from: "FactsheetBody.scenario-mode.test.tsx"
      to: "the real /factsheet/[id]/v2 route byte-identity"
      via: "innerHTML equality default vs scenarioMode={false}"
      pattern: "innerHTML"
    - from: "FactsheetBody.guard04-no-bleed.test.tsx"
      to: "factsheet-context persist={false} gate"
      via: "spy on localStorage.setItem + history.replaceState scoped to factsheet keyspace/URL"
      pattern: "replaceState"
---

<objective>
GUARD-02 + GUARD-04: install the two PERMANENT vitest regression gates that close the milestone — (a) the byte-identity gate pinning the real factsheet route unchanged at scenarioMode default plus an Overview-untouched assertion, and (b) the cross-tab-bleed spy test proving the persist={false} mount writes nothing to the factsheet keyspace.

Purpose: make the byte-identity and no-state-bleed invariants PERMANENT (the per-phase tests were disposable; these stay through milestone close), so any future regression on the real route or the RT2 class fails CI loudly.
Output: a promoted-and-extended FactsheetBody.scenario-mode.test.tsx (GUARD-02) + a new FactsheetBody.guard04-no-bleed.test.tsx (GUARD-04).

NOTE on parallelism: this plan is file-disjoint from 43-01 (test files only) and runs in the SAME wave. The GUARD-02 byte-identity assertion is robust to 43-01's footer change BY CONSTRUCTION (43-01's footer gate is additive/default-false), so this test does not need 43-01 to land first — it must stay green both before and after 43-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-CONTEXT.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md
@.planning/phases/43-edge-states-toggle-fold-guards/43-VALIDATION.md

<interfaces>
<!-- Verified file:line anchors this session. -->

Existing GUARD-02 promotion target — src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx (full file read this session, 113 lines):
- Already mocks @/lib/sentry-capture and stubs localStorage (Map-backed localStorageMock + vi.stubGlobal + Object.defineProperty) — the persist primitive touches them on mount even at persist={false}. REUSE this stub block for GUARD-04.
- buildScenarioFactsheetPayload({ portfolioDaily: makeReturnsSeries(300), benchmark: null }) -> populatedPayload (~300 points, fully populated)
- renderBody(scenarioMode, omitProp) mounts <FactsheetProvider payload persist={false}><FactsheetBody payload hideHeader hideAllocatorSection hideFooter={false} {...scenarioMode} /></FactsheetProvider>
- Test 1 (KEEP, this IS the GUARD-02 core): expect(def.container.innerHTML).toBe(explicitFalse.container.innerHTML) — default == scenarioMode={false}
- describe block: "FactsheetBody — scenarioMode prop (BODY-02)"

persist gate — src/app/factsheet/[id]/v2/factsheet-context.tsx:
- :177 persist = true default; :195 persist?: boolean prop
- write half early-returns on `|| !persist` (no history.replaceState ?range= rewrite, no factsheet-v2: localStorage)
- read half (:273-282) `if (!persist) return;` — no hydrate under persist={false}

Overview untouched (GUARD-02 second assertion):
- Overview EquityChartWidget stays on the LEGACY EquityChart (STATE.md 38-03) — it does NOT import/mount FactsheetBody. The factsheet body article id is `factsheet-main` (FactsheetView.tsx:188). Lowest-coupling permanent guard = import-shape grep/read assertion that the Overview EquityChartWidget module does not reference FactsheetBody.

GUARD-04 scope (Pitfall 5):
- ALLOWED-to-write key: composer-collapse:controls (ScenarioComposer.tsx:2966) — legitimate composer UI-pref, NOT in scope.
- IN-SCOPE forbidden writes: any key matching ^factsheet-v2 / factsheet-collapse, and any history.replaceState mutating ?range / ?cmp / ?dark.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote FactsheetBody.scenario-mode.test.tsx into the PERMANENT GUARD-02 gate + Overview-untouched assertion</name>
  <files>src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx (the existing innerHTML-equality proof + stub block — full file)
    - .planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md §must-answer 3 (promote, do NOT create a parallel test; add the Overview-untouched assertion; recommended shape = import-shape assertion) + Open Question 2
    - the Overview widget module that renders the equity widget (search: the AllocationDashboardV2 Overview EquityChartWidget; confirm it imports the legacy EquityChart, not FactsheetBody)
  </read_first>
  <action>
    Promote the EXISTING per-phase test into the permanent milestone-closing GUARD-02 gate IN PLACE (do NOT create a parallel byte-identity test — research is explicit). (a) Keep the innerHTML-equality test (default == scenarioMode={false}) verbatim — it is the GUARD-02 core. (b) Add a one-line header note and retag the describe block to mark it PERMANENT, e.g. describe "FactsheetBody — PERMANENT byte-identity gate (GUARD-02)" with a header comment: "PERMANENT (GUARD-02) — pins the real /factsheet/[id]/v2 route byte-identical at scenarioMode default; do not delete at milestone close." (c) Add the Overview-untouched assertion as the lowest-coupling permanent guard (research Open Question 2 recommendation): a structural/import-shape assertion that the Overview EquityChartWidget module does NOT reference FactsheetBody (i.e. the Overview equity widget stays on the legacy EquityChart, never mounts #factsheet-main). Prefer reading the Overview widget source via readFileSync + asserting it contains no "FactsheetBody" literal (mirrors the static-source-scan pattern the codebase uses for scope-boundary guards, e.g. composer-width.test.tsx) — this is render-engine-independent and permanent. After 43-01 lands its additive footer gate, this innerHTML test still proves the "Page 1 / 1" stamp is present at default/false (the gate only hides it at scenarioMode={true}); keep/extend the suppression test from 43-01 Task 2 here OR in the composer test (do not duplicate).
  </action>
  <verify>
    <automated>grep -q "PERMANENT" "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" && npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - The innerHTML-equality test (default == scenarioMode={false}) is present and GREEN
    - The describe/header marks the test PERMANENT (GUARD-02)
    - An Overview-untouched assertion proves the Overview EquityChartWidget module does not reference FactsheetBody / #factsheet-main
    - Test stays green both before and after 43-01 (additive footer change does not break innerHTML equality)
  </acceptance_criteria>
  <done>The byte-identity proof is promoted to a permanent GUARD-02 gate with an Overview-untouched assertion; green.</done>
</task>

<task type="auto">
  <name>Task 2: GUARD-04 cross-tab-bleed spy test (scoped to the factsheet keyspace/URL)</name>
  <files>src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx (REUSE the localStorage stub + sentry mock + renderBody mount pattern)
    - src/app/factsheet/[id]/v2/factsheet-context.tsx (persist gate :177/:195; write half `|| !persist`; read half :273-282 `if (!persist) return;`)
    - .planning/phases/43-edge-states-toggle-fold-guards/43-RESEARCH.md §must-answer 5 (exact GUARD-04 test) + Pitfall 5 (do NOT flag composer-collapse:controls)
  </read_first>
  <action>
    Create src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx. Mount the body under the persist={false} provider exactly as the scenario-mode test does (<FactsheetProvider payload persist={false}><FactsheetBody payload scenarioMode hideHeader hideAllocatorSection .../></FactsheetProvider> on a populated payload). Spy on the persistence sinks: vi.spyOn(window.localStorage, "setItem") and vi.spyOn(window.history, "replaceState"). Simulate an interaction that WOULD persist on the real route — drive a view-state change (e.g. a PeriodControl/brush range change that calls setXRange, or toggle a Display option) — then assert ZERO writes SCOPED to the factsheet keyspace/URL: no setItem call whose key matches /^factsheet-v2/ or /^factsheet-collapse/, and no replaceState call whose url contains ?range / ?cmp / ?dark. Scope the assertion to the factsheet keyspace ONLY — the legitimate composer-collapse:controls key (ScenarioComposer.tsx:2966) is OUT of scope and must NOT be flagged (Pitfall 5); since this test mounts only the factsheet body (not the full composer), that key won't fire, but write the assertions on a keyspace predicate (not "setItem never called at all") so the test documents the scoping. Add a header comment naming it the GUARD-04 RT2-class gate.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - The test mounts FactsheetBody under <FactsheetProvider persist={false}> and spies setItem + replaceState
    - After a view-state interaction, no setItem with a /^factsheet-v2|^factsheet-collapse/ key and no replaceState with ?range/?cmp/?dark
    - The assertion is keyspace-scoped (a predicate filter), NOT a blanket "never called" — documents that composer-collapse:controls would be allowed
    - Mutation check: if persist={false} is removed from the mount, the test goes RED (the write fires) — note this in the test's comments
  </acceptance_criteria>
  <done>A scoped GUARD-04 spy test proves no factsheet-keyspace/URL write under persist={false}; the legitimate composer-collapse key is not over-flagged; green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| factsheet view-state -> localStorage / URL | The cross-tab bleed surface (Phase-38 RT2 class); the only state-persisting boundary in this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Information Disclosure | factsheet view-state bleeding cross-tab via shared /allocations URL/localStorage (RT2 class) | mitigate | GUARD-04 spy test asserts 0 factsheet-keyspace writes under persist={false}; the persist gate (factsheet-context.tsx:282) is the runtime control this test pins |
| T-43-05 | Tampering | future regression silently changing the real /factsheet route HTML | mitigate | GUARD-02 permanent innerHTML-equality gate fails CI on any default-render change |
</threat_model>

<verification>
- `npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" "src/app/factsheet/[id]/v2/FactsheetBody.guard04-no-bleed.test.tsx" --no-file-parallelism` green
- GUARD-02 describe marked PERMANENT; Overview-untouched assertion present
- GUARD-04 assertion keyspace-scoped (does not flag composer-collapse:controls)
</verification>

<success_criteria>
- GUARD-02: permanent byte-identity gate (default == scenarioMode={false}) + Overview-untouched assertion, green and marked permanent
- GUARD-04: scoped cross-tab-bleed spy test (no factsheet-v2:/URL write under persist={false}), green
- Both robust to 43-01's additive footer change (no ordering dependency)
</success_criteria>

<output>
Create `.planning/phases/43-edge-states-toggle-fold-guards/43-02-SUMMARY.md` when done
</output>
