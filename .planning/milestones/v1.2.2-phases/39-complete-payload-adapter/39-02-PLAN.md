---
phase: 39-complete-payload-adapter
plan: 02
type: execute
wave: 2
depends_on: ["39-01-complete-payload-adapter"]
files_modified:
  - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts
  - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [PAYLOAD-01, PAYLOAD-02, PAYLOAD-03, PAYLOAD-04, PAYLOAD-05]
must_haves:
  truths:
    - "For a healthy ≥10-day blend, strategyMetrics carries real compute() scalars (cum_ret, cagr, ann_vol, sharpe, sortino, calmar, max_dd, skew, kurt, var95, cvar95, win_rate, profit_factor, mtd/ytd/p3m/p6m/p1y, best/worst day/week/month/quarter/year, recovery/pain/ulcer/tail/omega) — not zeros"
    - "Panel arrays (monthlyReturns, dailyHeatmap, calmarByYear, bootstrapCI, streaks, quantiles, strategyWorst10, strategyRollingVol/Sharpe/Sortino, stressWindows) are populated from the pure helpers — not []"
    - "strategyMetrics.ann_vol equals the population-std value (0.0075·√252 on the 30-day fixture); a sample-std bleed fails the golden assertion at 6 decimals"
    - "strategyMetrics.n equals portfolioDaily.length (true overlap count), so the n<252 caveat fires on a 30-day blend and is off at exactly 252"
    - "Degenerate blends (empty / non-finite / <2 dated points) return the existing safe-empty payload, short-circuited BEFORE any compute() call (which throws on empty)"
    - "ingestSource stays 'csv'; the 4 synthesized panels (peerPercentile, allocatorPortfolios, eventSignatures, benchEventSignatures) are structurally absent"
    - "The composer threads scenarioMetrics.portfolio_daily_returns through ScenarioFactsheetChart into the adapter (daily-RETURN form), and the equity/drawdown LINE still reads the wealth series (Phase-38 pins intact)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts"
      provides: "Complete FactsheetCsvPayload synthesized via compute() + factsheet helpers from portfolioDaily"
      contains: "compute("
      min_lines: 260
    - path: "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"
      provides: "Threads the new portfolioDaily prop to the adapter"
      contains: "portfolioDaily"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Passes scenarioMetrics.portfolio_daily_returns to the chart"
      contains: "portfolioDaily={scenarioMetrics.portfolio_daily_returns"
    - path: "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts"
      provides: "Golden-parity + convention-drift + n-boundary + ingestSource-absence tests"
      contains: "toBeCloseTo"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "ScenarioFactsheetChart portfolioDaily prop"
      via: "scenarioMetrics.portfolio_daily_returns ?? []"
      pattern: "portfolioDaily=\\{scenarioMetrics\\.portfolio_daily_returns"
    - from: "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts"
      to: "compute(rets, dates)"
      via: "import from @/lib/factsheet/compute on the populated path"
      pattern: "compute\\(rets"
    - from: "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts"
      to: "quantileSummary"
      via: "named import from @/lib/factsheet/quantiles (Plan 01)"
      pattern: "quantileSummary"
---

<objective>
Replace the minimal/zeroed body of `buildScenarioFactsheetPayload` with a COMPLETE `FactsheetCsvPayload` synthesized from the blend's `portfolio_daily_returns` (daily-RETURN form) via the population-convention `compute.ts`/`rolling.ts` helper family — mirroring the `build-payload.ts` csv-arm assembly field-for-field — and thread that returns series from the composer through `ScenarioFactsheetChart` into the adapter. Pin the result with a golden-parity fixture that fails on a sample-std convention drift.

This implements all five PAYLOAD requirements: full scalars (PAYLOAD-01), populated panel arrays (PAYLOAD-02), population-std/252/365.25 convention locked by golden fixture (PAYLOAD-03), honest `n` driving the `n<252` caveat (PAYLOAD-04), and degenerate-safe collapse (PAYLOAD-05).

Purpose: Phase 40 mounts the real `FactsheetBody` on this payload — it must be parity-by-construction with what the real factsheet route produces from `compute.ts`. The blend never hits the Python compute.
Output: Rewritten adapter + threaded prop + extended adapter test + updated composer-mock test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-complete-payload-adapter/39-RESEARCH.md
@.planning/phases/39-complete-payload-adapter/39-CONTEXT.md

<interfaces>
<!-- The assembly TEMPLATE is build-payload.ts:129–231 (csv arm). Mirror it; substitute -->
<!-- honest-empty for the market-correlation fields and keep the existing BTC comparator wiring. -->
<!-- All helper imports come from @/lib/factsheet/* (the adapter today imports only types + drawdown). -->

compute (src/lib/factsheet/compute.ts:15) — population stdev, 252 vol/Sharpe, 365.25 CAGR:
```typescript
export function compute(rets: number[], dates: string[], rf = 0): ComputeResult; // THROWS if n===0 or dates.length!==n
// ComputeResult = ComputeSummary + { eq: number[]; dd: number[] }  → strip eq/dd, keep the rest as strategyMetrics
// strategyMetrics.n === rets.length (set at compute.ts:16,156) → PAYLOAD-04 automatic
```

Helper signatures (all take daily RETURNS unless noted; src/lib/factsheet/*):
```typescript
cumEq(rets: number[]): number[];                          // compute.ts (wealth base 1.0)
worstDrawdowns(dd: number[], n=10): DrawdownPeriod[];     // compute.ts — feed compute().dd
rollingVol(rets, window): Array<number|null>;             // rolling.ts:82  (population std)
rollingSharpe(rets, window): Array<number|null>;          // rolling.ts:92
rollingSortino(rets, window): Array<number|null>;         // rolling.ts:103
pickRollingWindow(len, tiers?): RollWindowPick;           // rolling.ts:30
ROLL_WINDOW_90D, ROLL_WINDOW_30D;                         // rolling.ts (beta-window tiers)
streakLengths(rets): { wins:number[]; losses:number[] };  // streak.ts:9
streakHistogram(streaks:number[], maxLen=14): number[];   // streak.ts:48
calmarByYear(rets, dates): CalmarYearRow[];               // calmar-by-year.ts:17
bootstrapCI(rets, n_resamples=2000, block_len=5, seed=42); // bootstrap.ts:31 — SEEDED, deterministic
monthlyReturnsMatrix(rets, dates): MonthlyReturnsRow[];   // period-buckets.ts:11
dailyReturnsByYear(rets, dates): DailyHeatmapYear[];      // period-buckets.ts:51
computeStressWindows(dates, stratRet, benchRet, benchName, markets=[]): StressWindowPayload; // stress-windows.ts:82 — THROWS if dates[0] not ISO (blend dates ARE ISO → safe)
quantileSummary(rets): QuantilePayload;                   // @/lib/factsheet/quantiles (Plan 01 — NEW)
```

The streaks block recipe (verbatim from build-payload.ts:210–222, MAX_LEN=14):
```typescript
const { wins, losses } = streakLengths(rets);
const MAX_LEN = 14;
streaks = {
  winsByLength: streakHistogram(wins, MAX_LEN),
  lossesByLength: streakHistogram(losses, MAX_LEN),
  totalWins: wins.length, totalLosses: losses.length,
  longestWin: wins.length>0?Math.max(...wins):0,
  longestLoss: losses.length>0?Math.max(...losses):0,
  maxLen: MAX_LEN,
};
```

Composer mount site (ScenarioComposer.tsx:2224) — portfolio_daily_returns is ALREADY in scope
(passed to sibling sections at :2272 and :2289 as `scenarioMetrics.portfolio_daily_returns ?? []`):
```tsx
<ScenarioFactsheetChart
  equityDailyPoints={baselineEquityDailyPoints}
  scenarioSeries={scenarioWealthSeries}
  benchmark={btcWealth}
  // NEW:
  portfolioDaily={scenarioMetrics.portfolio_daily_returns ?? []}
/>
```

DailyPoint (src/lib/portfolio-math-utils.ts:11): `{ date: string; value: number }`.
For portfolioDaily, `value` = the daily RETURN (decimal, e.g. 0.012) — NOT wealth.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread portfolioDaily (composer → chart → adapter args)</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts, src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx (the props interface :58–70, the useMemo adapter call :137–145, and the JSX prop list)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:2218–2295 (the mount site at :2224; confirm `scenarioMetrics.portfolio_daily_returns ?? []` is already used at :2272/:2289 so the value is in scope)
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts:93–108 (the `ScenarioFactsheetPayloadArgs` interface)
  </read_first>
  <action>
    Add an optional `portfolioDaily: DailyPoint[]` field to `ScenarioFactsheetPayloadArgs` (scenario-factsheet-payload.ts) documented as "the engine's portfolio_daily_returns — daily RETURN form (decimal), the input compute() consumes; distinct from `scenario` which is cumulative wealth". Add a matching `portfolioDaily: DailyPoint[]` prop to `ScenarioFactsheetChartProps` (ScenarioFactsheetChart.tsx) and pass it into the `buildScenarioFactsheetPayload({...})` call inside the `useMemo`, adding `portfolioDaily` to the memo dependency array. In ScenarioComposer.tsx, add `portfolioDaily={scenarioMetrics.portfolio_daily_returns ?? []}` to the `<ScenarioFactsheetChart>` element at :2224 (use the exact `?? []` guard the sibling sections already use at :2272/:2289 — `portfolio_daily_returns` is an OPTIONAL field). Do NOT change `scenarioSeries`, `benchmark`, or `equityDailyPoints`. This task only wires the prop; the adapter still ignores it until Task 2 — so the build stays green between tasks (the new field is optional). Per CLAUDE.md Rule 3 these are three surgical additive edits.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "portfolioDaily" src/app/\(dashboard\)/allocations/widgets/performance/ScenarioFactsheetChart.tsx` shows the prop in the interface AND in the `buildScenarioFactsheetPayload({...})` call AND in the useMemo deps.
    - `grep -n "portfolioDaily={scenarioMetrics.portfolio_daily_returns" src/app/\(dashboard\)/allocations/components/ScenarioComposer.tsx` returns one match at the chart mount.
    - `ScenarioFactsheetPayloadArgs` in the adapter has an optional `portfolioDaily?: DailyPoint[]` (or required — Task 2 finalizes; either compiles since the adapter default-guards).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    portfolioDaily flows composer → chart → adapter args; tsc clean; no change to existing equity/benchmark props.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synthesize the complete FactsheetCsvPayload body from portfolioDaily via compute()</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts (the WHOLE file — the degenerate gate :199–201, the wealth-derived strategyEquity/strategyDrawdowns :203–207, the benchmark alignment :217–228, and the return literal :230–294 you are replacing the empty fields of)
    - src/lib/factsheet/build-payload.ts:129–231 (the csv-arm assembly TEMPLATE — mirror field-for-field)
    - src/lib/factsheet/compute.ts:15–60 (compute signature + throw-on-empty + ComputeResult eq/dd to strip)
    - src/lib/factsheet/stress-windows.ts:82–93 (computeStressWindows signature + ISO-date throw guard)
    - src/lib/factsheet/quantiles.ts (Plan 01's exported quantileSummary — the parity source)
    - 39-RESEARCH.md "Field-by-Field Assembly Recipe" + "Common Pitfalls" (Pitfalls 1/2/4/5 are load-bearing)
  </read_first>
  <behavior>
    - On a healthy blend (portfolioDaily.length ≥ 2, all finite): strategyMetrics === compute(rets, dates) field-by-field (after stripping eq/dd) at toBeCloseTo(_,6); panel arrays populated (calmarByYear.length>0, monthlyReturns.length>0, streaks.totalWins+totalLosses>0, bootstrapCI.sharpe.point finite, quantiles match quantileSummary(rets), strategyWorst10 from worstDrawdowns(dd), rolling* from rollingVol/Sharpe/Sortino).
    - strategyMetrics.n === portfolioDaily.length (PAYLOAD-04).
    - strategyMetrics.ann_vol === compute(rets,dates).ann_vol (population √(SS/n)·√252) — a sample-std (n-1) implementation differs and fails at 6 decimals.
    - Degenerate (portfolioDaily.length===0 OR any non-finite return OR <2 dated points): the existing safe-empty payload (zeroedComputeSummary + empty arrays), short-circuited BEFORE compute() is called (compute throws on empty).
    - strategyEquity and strategyDrawdowns still come from the WEALTH series (`scenario` / deriveSnapshotDrawdowns) — the existing Phase-38 exact-equality pins (test :30–37, :73–80) stay green.
    - ingestSource === "csv"; styleDrift === null; correlations === []; correlationMatrix === {labels:[],matrix:[]}; the 4 synth panels structurally absent.
    - When BOTH series degenerate-disagree (e.g. wealth present but portfolioDaily empty), the payload still collapses metrics/panels to safe-empty (degenerate keys on portfolioDaily) while the chart-LINE fields follow their own wealth-degeneracy gate — never NaN/Inf into a panel.
  </behavior>
  <action>
    Rewrite the populated path of `buildScenarioFactsheetPayload`, mirroring build-payload.ts:129–231 (csv arm). Compute `rets = portfolioDaily.map(p => p.value)` and `datesR = portfolioDaily.map(p => p.date)`. Add a SEPARATE returns-degenerate gate evaluated BEFORE any compute() call: `portfolioDaily.length === 0 || rets.some(v => !Number.isFinite(v)) || datesR.length < 2`. If returns-degenerate, populate `strategyMetrics` via `zeroedComputeSummary()` and all panel arrays with their existing empty literals (current behavior — never call compute()). Else: call `const full = compute(rets, datesR); const { eq, dd, ...strategyMetrics } = full;` and assemble — `strategyReturns: rets`; `strategyRollingVol/Sharpe/Sortino: rolling*(rets, rollWindow.window)` with `rollWindow = pickRollingWindow(rets.length)` and `rollingBetaWindow = pickRollingWindow(rets.length, [{window:ROLL_WINDOW_90D,label:"90d"},{window:ROLL_WINDOW_30D,label:"30d"}])`; `strategyWorst10: worstDrawdowns(dd, 10)`; `streaks` via the streakLengths+streakHistogram recipe (MAX_LEN=14, from the interfaces block); `calmarByYear: calmarByYear(rets, datesR)`; `bootstrapCI: bootstrapCI(rets)` (seeded → deterministic); `monthlyReturns: monthlyReturnsMatrix(rets, datesR)`; `dailyHeatmap: dailyReturnsByYear(rets, datesR)`; `quantiles: quantileSummary(rets)` (imported from @/lib/factsheet/quantiles — Plan 01); `stressWindows: computeStressWindows(datesR, rets, rets, "", [])` (strat returns as benchRet + empty benchName + markets [] per D-4 / Pitfall 5). KEEP `strategyEquity`/`strategyDrawdowns` sourced from the existing wealth-series path (`scenario` + `deriveSnapshotDrawdowns(scenario)`) — D-2 Option (a), preserving Phase-38 chart parity and the existing exact-equality test pins (Pitfall 4). KEEP the existing BTC comparator wiring (`inertComparatorBlock` + benchmark→`btc.cumulative` align + `activeComparator`). Hold `styleDrift: null` (D-5; CONTEXT wins over PAYLOAD-02's style-drift mention — Rule 7), `correlations: []` and `correlationMatrix: {labels:[],matrix:[]}` (D-6 honest-empty; Phase 41 owns constituent correlation), `ingestSource: "csv"` (D-7, never flip). Note `dates` stays the SCENARIO axis (existing chart contract) — distinct from `datesR` (the returns axis fed to compute/helpers); on a healthy blend they are the same ISO dates but the chart-line fields read `dates`/`scenario` and the metrics read `datesR`/`rets`. Add the helper imports from @/lib/factsheet/{compute,rolling,streak,calmar-by-year,bootstrap,period-buckets,stress-windows} and quantiles. Per CLAUDE.md Rule 3, do NOT touch the frozen engine or scenario-blend-panels.ts.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts" && npx tsc --noEmit && npx eslint "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "compute(rets" src/app/\(dashboard\)/allocations/widgets/performance/scenario-factsheet-payload.ts` shows compute() called on the populated path only.
    - The returns-degenerate gate appears textually BEFORE the `compute(` call (a `compute()` on empty would throw — verify by `grep -n` line order).
    - `grep -n 'ingestSource: "csv"' …/scenario-factsheet-payload.ts` still present; `grep -n 'styleDrift: null' …` present; `grep -n "correlations: \[\]" …` present.
    - The existing Phase-38 exact-equality tests for strategyEquity (test :30–37) and strategyDrawdowns (test :73–80) still pass unchanged (strategyEquity/Drawdowns kept on the wealth-series source).
    - `npx tsc --noEmit` exits 0; `npx eslint` 0 errors on the adapter.
  </acceptance_criteria>
  <done>
    The populated path emits real compute() scalars + populated panel arrays; the degenerate path short-circuits to safe-empty before compute(); equity/drawdown LINE unchanged; ingestSource csv / styleDrift null / honest-empty correlations preserved; tsc + eslint clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Golden-parity, convention-drift, n-boundary, degenerate & ingestSource-absence tests + composer mock</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts (the WHOLE existing file — the SCENARIO 30-day wealth fixture :13–19, the existing pins you must NOT break)
    - 39-VALIDATION.md "Per-Task Verification Map" + "Golden-parity fixture shape" (the recommended fixture in 39-RESEARCH.md §"Golden-parity fixture shape")
    - src/lib/factsheet/compute.ts:15 (the parity reference compute() the assertions compare against)
    - src/lib/factsheet/audit-c20.test.ts:371–412 (the `in payload === false` synth-panel-absence pattern to mirror)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (the existing `ScenarioFactsheetChart` mock-prop assertions — RESEARCH names :552,640,694,1863,1934,3611)
  </read_first>
  <behavior>
    - Golden parity: build a deterministic ≥252-day returns-form BLEND_252 fixture AND extend the 30-day fixture to returns form (BLEND_30, alternating +0.01/-0.005 matching compute.metrics.test.ts). For BLEND_30: for every numeric key of `compute(rets,dates)` except eq/dd/yearly, `p.strategyMetrics[k]` toBeCloseTo(ref[k], 6).
    - Convention-drift pin: `p.strategyMetrics.ann_vol` toBeCloseTo(0.0075 * Math.sqrt(252), 6) (recompute the constant in-test — population-std exact; a sample-std bleed yields ~0.12110 and fails).
    - n-boundary: BLEND_30 → `p.strategyMetrics.n === 30` (caveat ON, n<252 true); BLEND_252 → `p.strategyMetrics.n === 252` (caveat OFF, n<252 false at exactly 252).
    - Panel spot-check: `p.calmarByYear.length > 0` AND `p.monthlyReturns.length > 0`; bootstrapCI deterministic (`p.bootstrapCI.sharpe.point` toBeCloseTo(bootstrapCI(rets).sharpe.point, 6)).
    - Degenerate: empty portfolioDaily / a non-finite return / a single-dated point → safe-empty strategyMetrics (cum_ret===0, sharpe===0, yearly==={}) and empty panel arrays; never throws; no NaN/Inf in any panel.
    - ingestSource invariant: `p.ingestSource === "csv"` AND for each of `["peerPercentile","allocatorPortfolios","eventSignatures","benchEventSignatures"]`, `f in p === false`.
    - Composer mock: the `ScenarioFactsheetChart` mock now receives `portfolioDaily` equal to the engine's `portfolio_daily_returns` (returns form, NOT wealth); existing prop assertions remain green.
  </behavior>
  <action>
    Extend scenario-factsheet-payload.test.ts. Add a deterministic returns-form fixture pair using the RESEARCH "Golden-parity fixture shape": `BLEND_30` (30 points, value = i%2===0 ? 0.01 : -0.005, ISO dates via Date.UTC) and `BLEND_252` (252 points). Add a `describe("buildScenarioFactsheetPayload — complete-payload parity")` block with: (1) the field-by-field `compute()` parity loop (skip eq/dd/yearly) at toBeCloseTo(_,6); (2) the hand-pinned population-std `ann_vol` assertion (compute the constant in-test as `0.0075 * Math.sqrt(252)`) — encode WHY in a comment per CLAUDE.md Rule 9 (sample-std bleed fails here); (3) the n-boundary pair (30 → caveat-on, 252 → caveat-off); (4) the panel spot-checks + deterministic bootstrapCI; (5) degenerate cases for the NEW returns input (empty portfolioDaily, a non-finite return, a single point) asserting safe-empty + no-throw; (6) the ingestSource-absence block mirroring audit-c20.test.ts (`ingestSource==="csv"` + the 4 fields `in payload === false`). Feed each case `buildScenarioFactsheetPayload({ scenario: <a matching wealth series>, portfolioDaily: <BLEND_xx> })`. In ScenarioComposer.test.tsx, update the `ScenarioFactsheetChart` mock-prop assertion(s) so the mock now also captures/asserts `portfolioDaily` is the engine's `portfolio_daily_returns` (returns form); keep all existing prop assertions. Do NOT delete any existing pin — these are additive (Phase-38 chart pins must stay).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - The field-by-field parity loop and `expect(p.strategyMetrics.ann_vol).toBeCloseTo(0.0075 * Math.sqrt(252), 6)` pass; mutating the adapter to a sample-std basis turns this assertion RED (state this as the falsifiability proof in the SUMMARY).
    - `expect(p.strategyMetrics.n).toBe(30)` and `expect(p2.strategyMetrics.n).toBe(252)` both pass.
    - The ingestSource block: `expect(p.ingestSource).toBe("csv")` and a loop asserting `expect(f in p).toBe(false)` for all four synth fields passes.
    - The degenerate cases assert empty payload + `expect(() => …).not.toThrow()`.
    - ScenarioComposer.test.tsx asserts the mock received `portfolioDaily` (returns form); existing assertions remain green.
    - Both files green via the verify command; no pre-existing pin in either file regresses.
  </acceptance_criteria>
  <done>
    Golden-parity + convention-drift + n-boundary + degenerate + ingestSource-absence tests pass; composer mock asserts the new prop; the convention-drift pin is mutation-verified falsifiable.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frozen engine → adapter | The adapter's only input is `scenario.ts::computeScenario`'s own output (`portfolio_daily_returns`, wealth series). No network, auth, DB, user-supplied input, or RSC boundary — the adapter runs client-side inside a `useMemo` (RESEARCH Pitfall 6). The real risk is not a network/injection threat but a HONESTY threat: presenting fabricated/garbage metrics as real. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-02-01 | Information disclosure (dishonesty) | strategyMetrics presented as real metrics | mitigate | PAYLOAD-05 degenerate gate + the golden-parity/convention-drift tests: empty/non-finite/<2-date blends collapse to safe-empty BEFORE compute(); a sample-std bleed or a wealth-vs-returns mix-up fails the 6-decimal golden assertion loudly. Never NaN/Inf, never fabricated zeros on the populated path. |
| T-39-02-02 | Elevation (unauthorized panels) | ingestSource arm | mitigate | ingestSource hard-coded "csv"; the ingestSource-absence test asserts the 4 synthesized panels satisfy `f in payload === false` (mirrors audit-c20.test.ts), so a flip to the "api" arm fails CI. |
| T-39-SC | Tampering | npm/pip/cargo installs | n/a | No package installs this phase (RESEARCH §Package Legitimacy Audit: zero new deps, `npm install` adds nothing). No slopcheck surface; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.test.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` green.
- `npx tsc --noEmit` exits 0.
- `npx eslint` 0 errors on the adapter + chart wrapper.
- Wave-merge gate: `npm run test:coverage` full suite green (blocking CI gate; the new populated + degenerate branches both covered).
</verification>

<success_criteria>
1. The synthesized payload's `strategyMetrics` + every panel-array field are populated from real `compute()`/helper output for a healthy blend (no zeroed summary, no empty arrays).
2. The golden fixture asserts the blend's metrics equal `compute()` field-by-field within 1e-6; a sample-std/convention drift fails CI (population-std `ann_vol` pinned, mutation-verified).
3. `strategyMetrics.n` equals `portfolioDaily.length`; the `n<252` caveat is ON at 30 and OFF at exactly 252.
4. Empty / non-finite / sub-2-date blends collapse to safe-empty before any compute() call — no NaN/Inf, no fabricated zeros.
5. `ingestSource` stays "csv"; the 4 synthesized panels are structurally absent; `styleDrift` null; correlations honest-empty.
6. The equity/drawdown LINE stays on the wealth-series source (Phase-38 pins intact); the composer threads `portfolio_daily_returns` to the adapter.
</success_criteria>

<output>
Create `.planning/phases/39-complete-payload-adapter/39-02-SUMMARY.md` when done.
</output>
