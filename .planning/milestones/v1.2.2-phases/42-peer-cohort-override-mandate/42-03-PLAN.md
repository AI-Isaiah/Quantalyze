---
phase: 42-peer-cohort-override-mandate
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/factsheet/types.ts
  - src/app/factsheet/[id]/v2/MetricsColumn.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.tsx
  - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts
  - src/lib/factsheet/audit-c20.test.ts
  - src/lib/scenario.peer-basis.test.ts
autonomous: true
requirements: [PEER-01, PEER-02]
user_setup: []

must_haves:
  truths:
    - "scenarioPeer is an additive optional field on the FactsheetCsvPayload arm ONLY; ingestSource stays 'csv'; the api arm + peerPercentile + the 4 api-only fields are untouched."
    - "MetricsColumn renders the peer panel when ingestSource==='api' OR (scenarioMode && ingestSource==='csv' && scenarioPeer != null); with scenarioMode=false the api path is byte-identical."
    - "PeerPercentilePanel reads peerPercentile on the api arm OR scenarioPeer on the csv arm; the csv-arm read is narrowed (no bare-union field access)."
    - "The audit-c20 behavioral invariant is REPLACED: csv+scenarioPeer renders peer while peerPercentile/allocatorPortfolios/eventSignatures/benchEventSignatures stay structurally ABSENT and ingestSource stays 'csv'. The type-field invariant (4 api-only fields never on csv) is KEPT."
    - "A convention pin test asserts the blend's ranking Sharpe/Sortino come from scenario.ts's sample/252 basis (matching the cohort's quantstats basis), NOT compute.ts's population basis."
  artifacts:
    - path: "src/lib/factsheet/types.ts"
      provides: "scenarioPeer?: PeerPercentilePayload on FactsheetCsvPayload"
      contains: "scenarioPeer"
    - path: "src/app/factsheet/[id]/v2/MetricsColumn.tsx"
      provides: "The OR-gate activating the scenarioMode seam"
      contains: "scenarioPeer"
    - path: "src/app/factsheet/[id]/v2/BatchDPanels.tsx"
      provides: "PeerPercentilePanel dual-read (api peerPercentile OR csv scenarioPeer)"
      contains: "scenarioPeer"
    - path: "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts"
      provides: "buildScenarioFactsheetPayload accepts an optional scenarioPeer arg"
      contains: "scenarioPeer"
    - path: "src/lib/factsheet/audit-c20.test.ts"
      provides: "The replaced behavioral invariant + the kept type-field invariant"
      contains: "scenarioPeer"
    - path: "src/lib/scenario.peer-basis.test.ts"
      provides: "The convention pin (engine sample/252 == quantstats reference)"
      contains: "scenarioMetrics"
  key_links:
    - from: "src/app/factsheet/[id]/v2/MetricsColumn.tsx"
      to: "payload.scenarioPeer"
      via: "the scenarioMode OR-gate"
      pattern: "scenarioMode.*scenarioPeer"
    - from: "src/app/factsheet/[id]/v2/BatchDPanels.tsx"
      to: "payload.scenarioPeer"
      via: "ingestSource==='csv' narrowed read"
      pattern: "ingestSource === .csv.*scenarioPeer|scenarioPeer"
---

<objective>
Lay the additive carve-out contract: `scenarioPeer?: PeerPercentilePayload` on the
`FactsheetCsvPayload` arm, the `MetricsColumn` OR-gate that activates the inert
`scenarioMode` seam, the `PeerPercentilePanel` dual-read path, the
`buildScenarioFactsheetPayload` optional arg, the REPLACED audit-c20 behavioral
invariant, and the convention-basis pin test.

Purpose: PEER-01 (surface peer on the blend WITHOUT flipping ingestSource — the 3
synthetic panels stay structurally absent) + PEER-02 (the basis the rank is computed
on is pinned to the engine's sample/252 metrics, never the population headline).
This is the CONTRACT plan 04 consumes — no behavior change to the real route
(scenarioMode is false everywhere except the composer).

Output: the type + gate + panel + builder-arg + the two pin tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-peer-cohort-override-mandate/42-CONTEXT.md
@.planning/phases/42-peer-cohort-override-mandate/42-RESEARCH.md
@docs/architecture/adr-0025-scenario-peer-carveout.md
@.planning/phases/42-peer-cohort-override-mandate/42-UI-SPEC.md

<interfaces>
<!-- Exact current shapes + line anchors — do NOT re-explore. -->

From src/lib/factsheet/types.ts:171-176:
  export type PeerPercentilePayload = { cohortSize: number; sharpe: number; sortino: number; max_dd: number; };
From types.ts:420-438 (the discriminated union — the additive change goes on the csv arm):
  export type FactsheetApiPayload = FactsheetCommon & { ingestSource: "api"; peerPercentile: PeerPercentilePayload | null; allocatorPortfolios: ...; eventSignatures: ...; benchEventSignatures: ...; };
  export type FactsheetCsvPayload = FactsheetCommon & { ingestSource: "csv"; };   // ← add scenarioPeer?: here
  export type FactsheetPayload = FactsheetApiPayload | FactsheetCsvPayload;       // (:449)

From src/app/factsheet/[id]/v2/MetricsColumn.tsx:116-122 (the gate to change):
  <EditorialSection label="III" name="Style">
    <StyleDriftPanel />
    {payload.ingestSource === "api" && <PeerPercentilePanel />}   // ← :121, replace with the OR-gate
  </EditorialSection>
  // scenarioMode is already a threaded prop (MetricsColumn.tsx:19-24, currently `void scenarioMode;` inert — drop that no-op).

From src/app/factsheet/[id]/v2/BatchDPanels.tsx:78-107 (PeerPercentilePanel):
  if (payload.ingestSource !== "api") return null;   // ← :84, replace with the dual-read
  const p = payload.peerPercentile;                   // api arm
  // DemoBadge "Demo cohort" at :91 + the synthesized-cohort footnote at :102-104.
  // The 3 <PercentileBar> rows (Sharpe/Sortino/Max DD) are REUSED unchanged.
  // usePayload() returns the discriminated FactsheetPayload.

From scenario-factsheet-payload.ts:401-487 (buildScenarioFactsheetPayload):
  returns FactsheetCsvPayload { ingestSource: "csv", ... } at :435-486.
  Add `scenarioPeer` to ScenarioFactsheetPayloadArgs (optional) and set it on the return
  object (omit when undefined so existing call sites stay byte-identical).

From scenario.ts:341-388 (the SAMPLE/252 ranking basis — the cohort-matching basis):
  sharpe = (meanR*252)/volatility where variance uses /(n-1) (sample ddof=1)  // :347-351
  sortino downside RMS divides by total n; rf=0  // :364-371; MAR=0 confirmed → matches cohort
  max_drawdown peak-to-trough (basis-invariant)  // :373-388
  → the composer holds this as `scenarioMetrics` (ScenarioComposer.tsx:1511).
From src/lib/factsheet/compute.ts:24,33,36 (the WRONG basis for ranking):
  pstdev (POPULATION) → payload.strategyMetrics. NEVER rank from this.

audit-c20 test (src/lib/factsheet/audit-c20.test.ts):
  SYNTH_FIELDS = [peerPercentile, allocatorPortfolios, eventSignatures, benchEventSignatures]  // :364-369
  the behavioral describe at :371-393 + the type-level B6 block at :422+ (KEEP the type-level block).

Convention reference numbers (RESEARCH §Convention Reconciliation):
  analytics-service/tests/test_mt5_golden_fixtures.py:370-388 hand-derives sample std (ddof=1)
  and a known sample-basis Sharpe (~31.148 for that fixture) — use a fixed returns series with
  a hand-computed sample-basis Sharpe/Sortino as the TS-side reference.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Additive scenarioPeer field + MetricsColumn OR-gate + PeerPercentilePanel dual-read + builder arg</name>
  <files>src/lib/factsheet/types.ts, src/app/factsheet/[id]/v2/MetricsColumn.tsx, src/app/factsheet/[id]/v2/BatchDPanels.tsx, src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts</files>
  <read_first>
    - src/lib/factsheet/types.ts:171-176,420-449 (PeerPercentilePayload + the union — add the optional field to the csv arm only)
    - src/app/factsheet/[id]/v2/MetricsColumn.tsx:19-24,116-122 (the scenarioMode seam + the gate at :121)
    - src/app/factsheet/[id]/v2/BatchDPanels.tsx:78-107 (PeerPercentilePanel — the hard guard at :84, the DemoBadge at :91, the footnote at :102-104; the 3 PercentileBar rows REUSED)
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts:401-487 (the builder + its args type + the csv return object)
    - docs/architecture/adr-0025-scenario-peer-carveout.md §Decision 1-2 (additive carve-out, never ingestSource flip)
    - .planning/phases/42-peer-cohort-override-mandate/42-UI-SPEC.md §"PeerPercentilePanel — header re-label" (the scenario-path header copy + disclosure — the disclosure render itself lands in plan 04; here just the dual-read plumbing)
  </read_first>
  <action>
    (a) types.ts: add `scenarioPeer?: PeerPercentilePayload;` to `FactsheetCsvPayload` with a doc comment (PEER-01: blend-only, additive, sample/252 basis; the api arm + peerPercentile + the 4 api-only fields untouched; NOT promoted to FactsheetCommon per ADR §6). Leave `FactsheetApiPayload` and the union untouched.

    (b) MetricsColumn.tsx: drop the `void scenarioMode;` no-op (:24). Replace the gate at :121 with: render `<PeerPercentilePanel />` when `payload.ingestSource === "api" || (scenarioMode && payload.ingestSource === "csv" && payload.scenarioPeer != null)`. The explicit `ingestSource === "csv"` narrow is required before the `scenarioPeer` field access type-checks (Pitfall 3 — mirror the B6 narrowing discipline).

    (c) BatchDPanels.tsx PeerPercentilePanel: replace the hard `if (payload.ingestSource !== "api") return null;` (:84) with a dual-read: `const p = payload.ingestSource === "api" ? payload.peerPercentile : (payload.scenarioPeer ?? null);` then `if (!p) return null;`. Compute `const isScenario = payload.ingestSource === "csv";`. The 3 PercentileBar rows stay UNCHANGED. (The header re-label + disclosure copy swap is plan 04's render task — DO NOT change the badge/footnote text here beyond what's needed for the read path to compile; leave the demo badge/footnote intact on the api path.)

    (d) scenario-factsheet-payload.ts: add `scenarioPeer?: PeerPercentilePayload` to `ScenarioFactsheetPayloadArgs` and, in the returned csv object, set `scenarioPeer` from the arg — but OMIT the key when the arg is undefined (so every existing call site stays byte-identical; e.g. conditionally spread `...(args.scenarioPeer ? { scenarioPeer: args.scenarioPeer } : {})`). Import PeerPercentilePayload from the factsheet types.

    NO fenced code in this action — implement in the FILES.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | head -10; grep -q "scenarioPeer" src/lib/factsheet/types.ts && grep -q "scenarioPeer" src/app/factsheet/[id]/v2/MetricsColumn.tsx && grep -q "scenarioPeer" "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `scenarioPeer?: PeerPercentilePayload` is on `FactsheetCsvPayload` only; `FactsheetApiPayload` + the union are unchanged (diff shows no edit to those lines).
    - The MetricsColumn gate is the 3-clause OR with the explicit `ingestSource === "csv"` narrow; the `void scenarioMode;` no-op is removed.
    - PeerPercentilePanel reads `peerPercentile` (api) OR `scenarioPeer` (csv) via a narrowed conditional; a bare-union field access is NOT present (no tsc error).
    - `buildScenarioFactsheetPayload` accepts an optional `scenarioPeer` arg and OMITS the key when undefined (existing call sites byte-identical).
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>The carve-out contract compiles: scenarioPeer on the csv arm, the OR-gate, the panel dual-read, and the optional builder arg — with the api path provably unchanged when scenarioMode=false.</done>
  <threat_model>
  ## Trust Boundaries
  | Boundary | Description |
  |----------|-------------|
  | the discriminated FactsheetPayload union | The csv arm must never expose the 3 genuinely-synthetic api panels; scenarioPeer must not bleed onto the api arm. |

  ## STRIDE Threat Register
  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-42-10 | Tampering (data contract) | ingestSource flip unlocking synthetic panels | mitigate | scenarioPeer is a csv-only additive field; ingestSource stays "csv"; the 4 api-only fields stay structurally absent (kept type-field invariant, Task 2). |
  | T-42-11 | Information disclosure (compile-time) | bare-union scenarioPeer read on the api arm | mitigate | explicit `ingestSource === "csv"` narrow before field access; tsc enforces it. |
  </threat_model>
</task>

<task type="auto" tdd="true">
  <name>Task 2: REPLACE the audit-c20 behavioral invariant (keep the type-field invariant)</name>
  <files>src/lib/factsheet/audit-c20.test.ts</files>
  <behavior>
    - New behavioral case: a csv payload built WITH `scenarioPeer` set renders/carries the peer carve-out (scenarioPeer present) WHILE all 4 SYNTH_FIELDS (peerPercentile/allocatorPortfolios/eventSignatures/benchEventSignatures) stay structurally ABSENT (`f in payload === false`) AND `ingestSource` stays "csv".
    - KEEP: the existing type-field invariant — the 4 api-only fields never on a csv payload (the :371-393 absence describe + the :422+ type-level B6 block remain; scenarioPeer is a DIFFERENT field name so the absence assertions still hold).
    - KEEP: the api-arm case (all 4 synth fields present + non-null on api).
  </behavior>
  <read_first>
    - src/lib/factsheet/audit-c20.test.ts:356-411 (SYNTH_FIELDS + the behavioral describe to extend) + :422+ (the type-level block to KEEP verbatim)
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts:401-487 (buildScenarioFactsheetPayload — now accepts scenarioPeer; build a csv payload with it for the new case)
  </read_first>
  <action>
    Add a new `it(...)` (or describe) to audit-c20.test.ts: build a csv payload via `buildScenarioFactsheetPayload({ portfolioDaily: <fixture returns>, scenarioPeer: { cohortSize: 42, sharpe: 70, sortino: 65, max_dd: 55 } })`. Assert `payload.ingestSource === "csv"`, `payload.scenarioPeer` is present/non-null, and `for (const f of SYNTH_FIELDS) expect(f in payload).toBe(false)`. Do NOT delete the existing type-field invariant describe (:371-393) or the type-level B6 block (:422+) — they remain as the kept invariant. (The MetricsColumn render assertion — peer panel mounts under scenarioMode=true — lands in plan 04's render test; here pin the payload-shape invariant.)
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/audit-c20.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - A new behavioral case asserts: csv+scenarioPeer → scenarioPeer present, all 4 SYNTH_FIELDS absent (`in` === false), ingestSource stays "csv".
    - The existing type-field invariant (4 api-only fields never on csv) is still present and passing.
    - The type-level B6 block (:422+) is unchanged.
    - `npx vitest run src/lib/factsheet/audit-c20.test.ts -x` green.
  </acceptance_criteria>
  <done>The behavioral invariant is replaced (carve-out renders, synth panels absent, ingestSource csv) while the type-field invariant is kept.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convention pin — the blend's ranking basis == engine sample/252, not population</name>
  <files>src/lib/scenario.peer-basis.test.ts</files>
  <behavior>
    - Feed a fixed daily-returns series through scenario.ts's computeScenario and assert its sharpe/sortino equal a hand-computed SAMPLE (ddof=1) × √252 reference (the quantstats basis) within a float tolerance.
    - Assert the SAME series through compute.ts (population, pstdev) yields a DIFFERENT (higher) Sharpe — proving the two bases diverge and that ranking MUST use the engine's sample basis, not the population headline.
    - The reference Sharpe/Sortino are reproducible/deterministic (pure function of the fixed series → reload-stable, no PRNG, no Date.now()).
  </behavior>
  <read_first>
    - src/lib/scenario.ts:341-388 (the sample-basis sharpe/sortino/max_drawdown math — the ranking basis)
    - src/lib/factsheet/compute.ts:24,33,36 (the population basis — the wrong-for-ranking headline)
    - .planning/phases/42-peer-cohort-override-mandate/42-RESEARCH.md §"Convention Reconciliation" + Pitfall 1 (the divergence + the golden-fixture reference at test_mt5_golden_fixtures.py:370-388)
    - src/lib/scenario.ts (computeScenario's signature + StrategyForBuilder/ScenarioState inputs — how to drive it with a single fixture strategy)
  </read_first>
  <action>
    Create `src/lib/scenario.peer-basis.test.ts`. Build a single-strategy scenario (one StrategyForBuilder with a fixed daily_returns array + a ScenarioState with weight 1.0, leverage 1.0) and run `computeScenario(...)`. Hand-compute the sample-basis Sharpe and Sortino for that exact series (variance /(n-1), downside RMS /n, ×√252, rf=0) and assert `scenarioMetrics.sharpe`/`.sortino` match within tolerance. Then compute the population-basis Sharpe (variance /n) for the same series and assert it is DISTINCT from (and greater than) the engine value — pinning that the rank uses the engine sample basis. Comment the test as the PEER-02 convention pin (a future drift in either convention fails it). The max_drawdown leg is basis-invariant — optionally assert it matches both.

    NO fenced code in this action — write it in the test FILE.
  </action>
  <verify>
    <automated>npx vitest run src/lib/scenario.peer-basis.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - The test drives computeScenario with a fixed returns series (deterministic, no PRNG).
    - It asserts scenarioMetrics.sharpe/sortino == a hand-computed sample/252 reference (within tolerance).
    - It asserts the population-basis Sharpe for the same series is distinct (higher) — proving the bases diverge.
    - The test is reload-stable (re-running yields identical values).
    - `npx vitest run src/lib/scenario.peer-basis.test.ts -x` green.
  </acceptance_criteria>
  <done>The convention pin fails if the blend's ranking ever uses the population basis or either convention drifts.</done>
  <threat_model>
  ## STRIDE Threat Register
  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-42-12 | Integrity / honesty | convention bias (population vs sample) inflating the rank | mitigate | pin scenarioMetrics (sample/252) as the ranking basis; the test asserts divergence from the population headline; the panel discloses the basis (plan 04). |
  </threat_model>
</task>

</tasks>

<verification>
- `npm run typecheck` clean (the additive field + the narrowed gate + the panel dual-read).
- `npx vitest run src/lib/factsheet/audit-c20.test.ts src/lib/scenario.peer-basis.test.ts -x` green.
- Diff confirms the api arm + the union + every existing buildScenarioFactsheetPayload call site are byte-identical.
</verification>

<success_criteria>
- scenarioPeer is an additive csv-only field; the OR-gate activates the scenarioMode seam; the api path is byte-identical when scenarioMode=false.
- The audit-c20 behavioral invariant is replaced (carve-out renders, 3 synth absent, ingestSource csv); the type-field invariant is kept.
- The ranking basis is pinned to the engine's sample/252 metrics, never the population headline.
</success_criteria>

<output>
Create `.planning/phases/42-peer-cohort-override-mandate/42-03-SUMMARY.md` when done.
Record: the exact gate expression, the builder-arg shape, and the convention reference numbers used.
</output>
