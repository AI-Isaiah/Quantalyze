---
phase: 42-peer-cohort-override-mandate
plan: 05
type: execute
wave: 4
depends_on: [42-03, 42-04]
files_modified:
  - src/lib/factsheet/types.ts
  - src/lib/scenario.ts
  - src/lib/scenario-sample-ratios.test.ts
  - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts
  - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/factsheet/[id]/v2/MandatePanels.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.tsx
  - src/app/factsheet/[id]/v2/MetricsColumn.tsx
  - src/app/factsheet/[id]/v2/MandatePanels.scenario.test.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.ownbook.test.tsx
autonomous: true
requirements: [PEER-04, PEER-05]
user_setup: []

must_haves:
  truths:
    - "A ConstituentMandatePanel renders per-constituent chips (strategy_types + markets + per-constituent leverage) from genuinely-available fields; honest-empty 'no mandate metadata' per constituent when absent; NO fabricated aggregate."
    - "An OwnBookDeltaPanel shows the blend's core ratios MINUS the allocator's live-book ratios (Sharpe/Sortino/maxDD) on the SAME sample/252 basis, as signed deltas (Unicode minus U+2212, +/- prefix non-color-only)."
    - "The maxDD delta sign is INVERTED for color (shallower = positive = good)."
    - "Both panels render ONLY under scenarioMode; non-scenario factsheets are byte-identical (panels return null)."
    - "When the allocator has no live book, the own-book delta is silently absent (no zeroed deltas, no CTA)."
  artifacts:
    - path: "src/lib/scenario.ts"
      provides: "Exported sampleBasisRatios(dailyReturns) pure helper (the scenario.ts:346-388 sample/252 math)"
      contains: "sampleBasisRatios"
    - path: "src/lib/factsheet/types.ts"
      provides: "scenarioMandate? + scenarioOwnBookDelta? optional fields on FactsheetCsvPayload"
      contains: "scenarioOwnBookDelta"
    - path: "src/app/factsheet/[id]/v2/MandatePanels.tsx"
      provides: "ConstituentMandatePanel (per-constituent read-only chips)"
      contains: "ConstituentMandatePanel"
    - path: "src/app/factsheet/[id]/v2/BatchDPanels.tsx"
      provides: "OwnBookDeltaPanel (signed sample-basis deltas)"
      contains: "OwnBookDeltaPanel"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "sampleBasisRatios"
      via: "own-book delta computed on the same basis as the blend"
      pattern: "sampleBasisRatios"
    - from: "src/app/factsheet/[id]/v2/MetricsColumn.tsx"
      to: "ConstituentMandatePanel + OwnBookDeltaPanel"
      via: "scenarioMode-gated render in §V Terms / §III Style"
      pattern: "ConstituentMandatePanel|OwnBookDeltaPanel"
---

<objective>
Add the per-constituent Mandate chips (PEER-04) and the own-book head-to-head delta
(PEER-05) to the scenario blend. Mandate chips come from genuinely-available
per-constituent fields (`strategy_types`, `markets`, per-constituent leverage); the
own-book delta is the blend's core ratios MINUS the allocator's live-book ratios,
both computed on the SAME sample/252 basis via a shared extracted helper, rendered as
signed deltas with the maxDD sign inverted for color.

Purpose: PEER-04 (honest per-constituent mandate, no fabricated aggregate) + PEER-05
(a delta — not a percentile — basis-consistent with the peer rank). Both render only
under scenarioMode; the real route stays byte-identical.

Output: the sampleBasisRatios helper + 2 additive csv-arm fields + 2 new panels + the
composer wiring + render tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-peer-cohort-override-mandate/42-CONTEXT.md
@.planning/phases/42-peer-cohort-override-mandate/42-UI-SPEC.md
@.planning/phases/42-peer-cohort-override-mandate/42-03-SUMMARY.md
@.planning/phases/42-peer-cohort-override-mandate/42-04-SUMMARY.md

# DESIGN.md: read before the chip + delta styling (the neutral-outline chip token ladder,
# the positive/negative delta tokens, the U+2212 minus, the non-color-only sign rule).

<interfaces>
<!-- Verified per-constituent + own-book anchors — do NOT re-explore. -->

StrategyForBuilder (src/lib/scenario.ts:58-71) — the per-constituent fields available client-side:
  { id, name, codename, disclosure_tier, strategy_types: string[], markets: string[],
    start_date, daily_returns, cagr, sharpe, volatility, max_drawdown }
  → strategy_types + markets are genuinely available; leverage_range + description are NOT on this type
    (they live on FactsheetCommon — OUT of v1.2.2 chip scope per 42-UI-SPEC §2 / CONTEXT D-07).
ScenarioState.leverage (scenario.ts:91): Record<string, number> (id → L; default 1.0).
  → per-constituent leverage chip source: deAliased.state.leverage[id] ?? 1.0.
The composer holds deAliased.strategies (the constituents) at ScenarioComposer.tsx:1498-1512.

sampleBasisRatios (TO EXTRACT from scenario.ts:346-388):
  the SAMPLE (ddof=1)×√252 sharpe + sortino (downside RMS /n, rf=0) + basis-invariant max_drawdown.
  scenarioMetrics already uses this math; extract a pure `sampleBasisRatios(dailyReturns: number[]): { sharpe: number|null; sortino: number|null; max_drawdown: number }`
  and have computeScenario call it (or keep computeScenario's inline math + the new export sharing one impl — single code path).

Own-book data (already client-side via props — NO new fetch):
  - props.payload: MyAllocationDashboardPayload (ScenarioComposer.tsx:268); payload.liveBaselineMetrics (custom-named).
  - baselineEquityDailyPoints (ScenarioComposer.tsx:531) — the own-book daily series (cumulative-wealth points)
    from which own-book DAILY RETURNS are derived (the composer already derives them upstream).
  - The own-book delta must recompute own-book Sharpe/Sortino/maxDD via sampleBasisRatios on the own-book
    daily returns (NOT liveBaselineMetrics, which is a different/population basis) → basis-consistent with the blend.

UI-SPEC contracts (verbatim):
  §2 ConstituentMandatePanel: Panel title "Mandate"; one sub-block per constituent (border-b border-border/40 last:border-0);
    constituent name 11px semibold; chip rows flex flex-wrap gap-1; chip =
    inline-flex items-center rounded-sm border border-border px-2 py-0.5 text-[10px] font-medium uppercase tracking-wider text-text-secondary;
    per-constituent honest-empty "no mandate metadata" (11px italic muted);
    whole-panel empty "No mandate metadata available for this blend's constituents." (panel still renders).
  §3 OwnBookDeltaPanel: Panel title "vs Your Book"; 3 Kpm/Row rows (Sharpe/Sortino/Max DD);
    value = signed +0.24 / −0.18 (U+2212); color via --color-positive/--color-negative;
    maxDD color INVERTED (positive delta = shallower = positive token), label "Max DD (blend vs book)", pp units;
    basis note (10px italic muted): "Delta = blend minus your live book · sample/252 basis · {N} book observations";
    silently absent when no live book.
  §Placement: ConstituentMandatePanel in §V Terms BEFORE TermsPanel; OwnBookDeltaPanel in §III Style AFTER PeerPercentilePanel.

MetricsColumn anchors: §III Style at :116-122, §V Terms at :124-127. Kpm/Row pattern at :40-74.
ScenarioFactsheetChart props at :64-92; buildScenarioFactsheetPayload args at scenario-factsheet-payload.ts:401.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract sampleBasisRatios + add scenarioMandate/scenarioOwnBookDelta csv fields + builder/chart/composer wiring</name>
  <files>src/lib/scenario.ts, src/lib/scenario-sample-ratios.test.ts, src/lib/factsheet/types.ts, src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts, src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <behavior>
    - sampleBasisRatios(dailyReturns) returns sample(ddof=1)×√252 sharpe, sortino (downside RMS /n, rf=0), basis-invariant max_drawdown — identical numbers to what computeScenario produces for the same series (one shared code path).
    - The composer computes: (a) scenarioMandate = per-constituent { name, strategy_types, markets, leverage } from deAliased.strategies + deAliased.state.leverage; (b) scenarioOwnBookDelta = blend sampleBasisRatios − own-book sampleBasisRatios (on own-book daily returns), with book_n; null when there is no own-book series.
    - Both thread through ScenarioFactsheetChart → buildScenarioFactsheetPayload onto the csv payload (omitted when absent → existing call sites byte-identical).
  </behavior>
  <read_first>
    - src/lib/scenario.ts:341-388 (the sample/252 math to extract into sampleBasisRatios; keep computeScenario producing identical numbers)
    - src/lib/factsheet/types.ts:436-438 (the csv arm — add the two optional fields alongside scenarioPeer from plan 03)
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts:401-487 (the builder args + return object — add the two optional args, omit when undefined)
    - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx:64-92,150-169 (add the two props, thread into the builder + the useMemo deps)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:268,305-340,531,1498-1525 (props.payload, liveBaselineMetrics, baselineEquityDailyPoints, deAliased.strategies + state.leverage, scenarioMetrics) — the own-book daily-returns source + the constituent metadata
  </read_first>
  <action>
    (a) scenario.ts: export `sampleBasisRatios(dailyReturns: number[]): { sharpe: number|null; sortino: number|null; max_drawdown: number }` implementing the EXACT :346-388 math (sample variance /(n-1), downside RMS /n, ×√252, rf=0; peak-to-trough max_drawdown). Refactor computeScenario to call it OR share one impl so the numbers stay identical (the convention pin from plan 03 must still pass). Add `src/lib/scenario-sample-ratios.test.ts` asserting sampleBasisRatios on a fixed series equals the hand-computed sample/252 reference AND equals computeScenario's output for the same single-strategy blend (one code path).

    (b) types.ts: add to FactsheetCsvPayload two optional additive fields with doc comments — `scenarioMandate?: ScenarioMandatePayload` and `scenarioOwnBookDelta?: OwnBookDeltaPayload`. Define `ScenarioMandatePayload = { constituents: { name: string; strategy_types: string[]; markets: string[]; leverage: number }[] }` and `OwnBookDeltaPayload = { sharpe: number|null; sortino: number|null; max_dd: number|null; book_n: number }`. The api arm + the union stay untouched; both fields are blend-only (NOT FactsheetCommon).

    (c) scenario-factsheet-payload.ts: add optional `scenarioMandate` + `scenarioOwnBookDelta` args; set them on the csv return object, OMITTING each key when its arg is undefined (existing call sites byte-identical).

    (d) ScenarioFactsheetChart.tsx: add `scenarioMandate?` + `scenarioOwnBookDelta?` props; thread into buildScenarioFactsheetPayload + the synthPayload useMemo deps.

    (e) ScenarioComposer.tsx: compute `scenarioMandate` (map deAliased.strategies → { name, strategy_types, markets, leverage: deAliased.state.leverage[id] ?? 1.0 }) in a useMemo keyed on deAliased; compute `scenarioOwnBookDelta` in a useMemo: derive own-book daily returns from the own-book series (baselineEquityDailyPoints / the same derivation the composer already uses), run sampleBasisRatios on BOTH the blend's portfolio_daily_returns and the own-book returns, set each delta = blend − book (book_n = own-book obs count); null when there is no own-book series. Pass both as props to ScenarioFactsheetChart.

    NO fenced code in this action — implement in the FILES.
  </action>
  <verify>
    <automated>grep -q "sampleBasisRatios" src/lib/scenario.ts && grep -q "scenarioOwnBookDelta" src/lib/factsheet/types.ts && grep -q "scenarioMandate" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" && npx vitest run src/lib/scenario-sample-ratios.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `sampleBasisRatios` is exported and produces numbers identical to computeScenario for the same series (asserted; the plan-03 convention pin still passes).
    - `ScenarioMandatePayload` + `OwnBookDeltaPayload` types exist; the two optional fields are on the csv arm only.
    - The builder + chart omit each field when its arg is undefined (existing call sites byte-identical).
    - The composer computes scenarioMandate (per-constituent, leverage from state) + scenarioOwnBookDelta (blend − own-book on sample basis, null when no book) and passes both to the chart.
    - The own-book delta uses sampleBasisRatios on the own-book daily returns — NOT liveBaselineMetrics (basis-consistent with the blend).
    - `npm run typecheck` clean; the sample-ratios test is green.
  </acceptance_criteria>
  <done>The shared sample-basis helper + the two additive carve-out fields + the composer computation are wired; both are basis-consistent and blend-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ConstituentMandatePanel (per-constituent chips, honest-empty) + render test</name>
  <files>src/app/factsheet/[id]/v2/MandatePanels.tsx, src/app/factsheet/[id]/v2/MetricsColumn.tsx, src/app/factsheet/[id]/v2/MandatePanels.scenario.test.tsx</files>
  <behavior>
    - Under scenarioMode with scenarioMandate present: one sub-block per constituent showing the constituent name + chips for strategy_types, markets, and a leverage chip ({leverage}×); honest-empty "no mandate metadata" per constituent when that constituent has no types AND no markets (leverage alone always present at 1.0 — render the leverage chip; treat a constituent as empty only when types+markets are both empty, per UI-SPEC honest-empty rule).
    - Whole-panel empty copy "No mandate metadata available for this blend's constituents." when NO constituent has any types/markets (the Panel title "Mandate" still renders).
    - Non-scenario (scenarioMode false / no scenarioMandate): the panel returns null (real route byte-identical).
    - No fabricated aggregate mandate — only per-constituent data.
  </behavior>
  <read_first>
    - src/app/factsheet/[id]/v2/MandatePanels.tsx (the existing Panel primitive + TermsPanel/LeverageProfilePanel exports — add ConstituentMandatePanel here; mirror the Panel shell)
    - .planning/phases/42-peer-cohort-override-mandate/42-UI-SPEC.md §"2. ConstituentMandatePanel" (the EXACT chip token classes, the per-constituent + whole-panel empty copy, the DOM structure)
    - src/app/factsheet/[id]/v2/MetricsColumn.tsx:124-127 (§V Terms — add ConstituentMandatePanel BEFORE TermsPanel, gated on scenarioMode)
    - src/app/factsheet/[id]/v2/MetricsColumn.tsx:19-24 (the scenarioMode prop threading)
    - DESIGN.md (the neutral-outline chip token ladder)
  </read_first>
  <action>
    Add `ConstituentMandatePanel` to MandatePanels.tsx: read `scenarioMandate` off the csv-narrowed payload (usePayload + ingestSource==="csv" narrow); return null when not scenario or scenarioMandate absent. Render the Panel shell titled "Mandate"; per constituent render a sub-block (name + chip rows for strategy_types, markets, leverage) with the UI-SPEC chip span classes verbatim; per-constituent honest-empty "no mandate metadata" when types+markets both empty; whole-panel empty copy when no constituent has any types/markets. A local `Chip` span (not a shared primitive). NO interactive MandateChipGroup (it's role=checkbox — wrong a11y for read-only display).

    In MetricsColumn.tsx §V Terms (:124-127): render `<ConstituentMandatePanel />` BEFORE `<TermsPanel />`, gated so it only mounts under scenarioMode (the panel's own internal null-guard handles the gate; the placement is per UI-SPEC).

    Then create `src/app/factsheet/[id]/v2/MandatePanels.scenario.test.tsx`: render with scenarioMandate (mixed constituents — one with types+markets, one empty) and assert the chips + the per-constituent honest-empty; render with all-empty constituents and assert the whole-panel empty copy + the "Mandate" title still present; render non-scenario and assert null.

    NO fenced code in this action — implement in the FILES.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/MandatePanels.scenario.test.tsx" -x</automated>
  </verify>
  <acceptance_criteria>
    - ConstituentMandatePanel renders per-constituent chips (strategy_types + markets + leverage×) with the verbatim UI-SPEC chip classes.
    - Per-constituent honest-empty "no mandate metadata" when a constituent has no types+markets.
    - Whole-panel honest-empty copy when no constituent has any types/markets (title still renders).
    - It returns null on the non-scenario path (real route byte-identical) and uses NO fabricated aggregate.
    - It does NOT reuse the interactive MandateChipGroup.
    - The render test is green.
  </acceptance_criteria>
  <done>Per-constituent mandate chips render honestly (no aggregate fabrication, honest-empty per constituent and whole-panel), scenario-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OwnBookDeltaPanel (signed sample-basis deltas, maxDD sign inverted) + render test</name>
  <files>src/app/factsheet/[id]/v2/BatchDPanels.tsx, src/app/factsheet/[id]/v2/MetricsColumn.tsx, src/app/factsheet/[id]/v2/BatchDPanels.ownbook.test.tsx</files>
  <behavior>
    - Under scenarioMode with scenarioOwnBookDelta present: a Panel "vs Your Book" with 3 rows (Sharpe/Sortino/Max DD); each value is a signed delta with a leading +/− (U+2212 for negative) — the sign is carried in text, never color-only.
    - Sharpe/Sortino: positive delta → --color-positive, negative → --color-negative.
    - Max DD row: color INVERTED (a positive delta = shallower = good = positive token); label "Max DD (blend vs book)", shown in pp.
    - Basis note: "Delta = blend minus your live book · sample/252 basis · {N} book observations" (N = book_n).
    - No live book (scenarioOwnBookDelta null/absent): the panel returns null (silently absent — no zeroed deltas, no CTA).
    - Non-scenario: null (real route byte-identical).
  </behavior>
  <read_first>
    - src/app/factsheet/[id]/v2/BatchDPanels.tsx:78-122 (PeerPercentilePanel + the Panel/PercentileBar primitives — add OwnBookDeltaPanel as a sibling export)
    - .planning/phases/42-peer-cohort-override-mandate/42-UI-SPEC.md §"3. OwnBookDeltaPanel" (the EXACT row format, the U+2212 sign, the color rule + the maxDD inversion, the basis-note copy, the silent-absent rule)
    - src/app/factsheet/[id]/v2/MetricsColumn.tsx:116-122 (§III Style — add OwnBookDeltaPanel AFTER PeerPercentilePanel) + :40-74 (the Kpm/Row value-cell pattern + :569-574 the signed-Δ styling precedent)
    - DESIGN.md (--color-positive / --color-negative tokens, U+2212 minus, non-color-only sign rule)
  </read_first>
  <action>
    Add `OwnBookDeltaPanel` to BatchDPanels.tsx: read `scenarioOwnBookDelta` off the csv-narrowed payload; return null when not scenario or the delta is absent. Render the Panel titled "vs Your Book" with 3 rows: Sharpe, Sortino, Max DD (blend vs book). Each value formatted with a leading "+"/"−" (U+2212 for negative) + the magnitude; color Sharpe/Sortino via --color-positive (delta>0) / --color-negative (delta<0); for Max DD INVERT the color mapping (delta>0 = shallower = positive token) and render in pp. Render the basis note "Delta = blend minus your live book · sample/252 basis · {book_n.toLocaleString()} book observations" (10px italic muted). The sign is ALWAYS in the text (WCAG non-color-only). A null delta value in any single ratio renders "—" (insufficient data) without crashing.

    In MetricsColumn.tsx §III Style: render `<OwnBookDeltaPanel />` AFTER `<PeerPercentilePanel />` inside the same EditorialSection.

    Then create `src/app/factsheet/[id]/v2/BatchDPanels.ownbook.test.tsx`: render with a delta (positive Sharpe, negative Sortino, positive maxDD-delta) and assert the signs/U+2212 + the inverted maxDD color semantics (assert the token class applied) + the basis note with N; render with scenarioOwnBookDelta null and assert the panel renders nothing; render non-scenario and assert null.

    NO fenced code in this action — implement in the FILES.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/BatchDPanels.ownbook.test.tsx" -x</automated>
  </verify>
  <acceptance_criteria>
    - OwnBookDeltaPanel renders "vs Your Book" with 3 signed-delta rows; the sign is in the text (U+2212 for negative), never color-only.
    - Sharpe/Sortino color follows sign; Max DD color is INVERTED (positive delta = positive token), shown in pp.
    - The basis note shows "sample/252 basis · {N} book observations".
    - No live book → the panel renders nothing; non-scenario → null.
    - A null single-ratio delta renders "—" without crashing.
    - The render test is green.
  </acceptance_criteria>
  <done>The own-book delta renders as signed sample-basis deltas with the maxDD inversion, discloses the basis, and is silently absent without a live book.</done>
  <threat_model>
  ## STRIDE Threat Register
  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-42-15 | Integrity / honesty | own-book delta on a mismatched basis | mitigate | both legs use sampleBasisRatios on daily returns (Task 1) — basis-consistent with the blend's ranking metrics and the cohort; the panel discloses the basis. |
  | T-42-16 | Information disclosure | own-book data is the caller's own | accept | scenarioOwnBookDelta is derived from props.payload (own-tenant via owner-RLS); no cross-tenant data; no new server surface. |
  </threat_model>
</task>

</tasks>

<verification>
- `npx vitest run src/lib/scenario-sample-ratios.test.ts "src/app/factsheet/[id]/v2/MandatePanels.scenario.test.tsx" "src/app/factsheet/[id]/v2/BatchDPanels.ownbook.test.tsx" -x` green.
- `npm run typecheck` clean.
- The plan-03 convention pin (`scenario.peer-basis.test.ts`) STILL passes after the sampleBasisRatios extraction.
- `npm run test:coverage` full suite + ratchet held (per-wave merge gate).
- Diff confirms: non-scenario factsheet render is byte-identical (both panels null off the scenario path).
</verification>

<success_criteria>
- Per-constituent mandate chips render from genuinely-available fields with honest-empty fallbacks and no fabricated aggregate.
- The own-book delta is a signed sample-basis delta (maxDD inverted), basis-consistent with the peer rank, disclosed, and silently absent without a live book.
- Both panels are scenario-only; the real route is byte-identical.
</success_criteria>

<output>
Create `.planning/phases/42-peer-cohort-override-mandate/42-05-SUMMARY.md` when done.
Record the two new payload field shapes, the sampleBasisRatios signature, and the own-book
daily-returns derivation used.
</output>
