---
phase: 42-peer-cohort-override-mandate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260626120000_get_verified_cohort_rank.sql
  - src/__tests__/verified-cohort-rank-rls.test.ts
autonomous: true
requirements: [PEER-03]
user_setup: []

must_haves:
  truths:
    - "A SECURITY DEFINER RPC get_verified_cohort_rank returns ONLY (cohort_n, sharpe_pct, sortino_pct, max_dd_pct) — never any per-strategy id/name/returns/PII."
    - "The cohort is verified+published strategies (strategy_verifications.trust_tier present AND strategies.status='published')."
    - "When the verified cohort is below min-N=20 the RPC returns a NULL-rank row (honest empty), never a percentile."
    - "A normal authed allocator client cannot read other allocators' verification rows (RLS owner-scoped); the aggregate cohort is reachable ONLY through this DEFINER RPC."
    - "The RPC is REVOKEd from PUBLIC/anon, granted to authenticated+service_role, gated on auth.role()!='anon' / auth.uid() IS NOT NULL, and sets search_path."
  artifacts:
    - path: "supabase/migrations/20260626120000_get_verified_cohort_rank.sql"
      provides: "The get_verified_cohort_rank SECURITY DEFINER RPC + REVOKE/GRANT + self-verifying DO block"
      contains: "CREATE OR REPLACE FUNCTION public.get_verified_cohort_rank"
    - path: "src/__tests__/verified-cohort-rank-rls.test.ts"
      provides: "HAS_LIVE_DB integration test: no cross-tenant leak, min-N empty, no-identity columns"
      contains: "get_verified_cohort_rank"
  key_links:
    - from: "supabase/migrations/20260626120000_get_verified_cohort_rank.sql"
      to: "strategy_verifications + strategy_analytics + strategies"
      via: "EXISTS(... trust_tier IS NOT NULL) + status='published' join"
      pattern: "strategy_verifications"
    - from: "src/__tests__/verified-cohort-rank-rls.test.ts"
      to: "get_verified_cohort_rank"
      via: "supabase.rpc"
      pattern: "rpc\\(.get_verified_cohort_rank"
---

<objective>
Build the security backbone for the scenario peer rank: a `get_verified_cohort_rank`
SECURITY DEFINER RPC (migration, forward-dated > 20260625120000) that returns ONLY
an aggregated rank + cohort count against the REAL verified-strategy universe — never
per-strategy identity — suppressed below min-N=20, plus a HAS_LIVE_DB RLS integration
test proving no cross-tenant leak.

Purpose: PEER-03's cohort cannot be read from an authed client — `strategy_verifications`
RLS (migration 093) grants an allocator SELECT only on THEIR OWN strategies' verification
rows, so the cross-tenant verified aggregate is reachable ONLY through a privileged
DEFINER function. This is the [BLOCKING] task: the route (plan 02) cannot land until
the RPC exists and is applied to the TEST project.

Output: one timestamp-named migration + one RLS integration test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-peer-cohort-override-mandate/42-CONTEXT.md
@.planning/phases/42-peer-cohort-override-mandate/42-RESEARCH.md
@.planning/phases/42-peer-cohort-override-mandate/42-VALIDATION.md

<interfaces>
<!-- Verified facts the executor needs — do NOT re-explore the codebase for these. -->

LATEST migration timestamp (the new one MUST forward-date strictly after this):
  supabase/migrations/20260625120000_csv_daily_returns_allocator_date_index.sql
  → new file: supabase/migrations/20260626120000_get_verified_cohort_rank.sql

strategy_analytics columns (confirmed via queries.ts:116 getPercentiles):
  sharpe, sortino, max_drawdown (max_drawdown stored NEGATIVE; queries.ts:162-168 takes Math.abs)

The canonical published predicate (src/lib/visibility.ts:74): status='published'.
  NB: a TS capstone bans raw `.eq("status","published")` in TS query code — that ban is
  TS-only; inside SQL the literal `s.status = 'published'` predicate is correct and expected.

strategy_verifications RLS (migration 20260501055202_strategy_verifications.sql):
  policy strategy_verifications_owner_select grants SELECT only WHERE
  strategy_id IN (SELECT id FROM strategies WHERE user_id = auth.uid()).
  → a normal authed client CANNOT read peers' verification rows. DEFINER RPC required.

MAR = 0.0 (analytics-service/services/metrics.py:123) — the cohort's Sortino uses rf=MAR=0,
  matching the blend engine's rf=0. No rf-offset desync. (Research A1 resolved.)

Self-verifying DO block pattern to MIRROR (migration 093 STEP 7, lines 296-369):
  DO $$ DECLARE v_fn_oid OID; BEGIN
    IF NOT EXISTS (... pg_proc proname='get_verified_cohort_rank' ...) THEN RAISE EXCEPTION ...;
    -- assert SECURITY DEFINER (pg_proc.prosecdef = true)
    -- assert EXECUTE revoked from PUBLIC/anon (has_function_privilege)
    RAISE NOTICE '...all assertions passed';
  END $$;

REVOKE/GRANT pattern to MIRROR (migration 093 STEP 6, lines 281-283):
  REVOKE ALL ON FUNCTION ... FROM PUBLIC, anon;
  GRANT EXECUTE ON FUNCTION ... TO authenticated;
  GRANT EXECUTE ON FUNCTION ... TO service_role;

RLS test model: src/__tests__/strategy-verifications-rls.test.ts (HAS_LIVE_DB gate via
  @/lib/test-helpers/live-db: HAS_LIVE_DB, createLiveAdminClient, createTestUser,
  cleanupLiveDbRow, advertiseLiveDbSkipReason).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: Write + apply the get_verified_cohort_rank SECURITY DEFINER migration</name>
  <files>supabase/migrations/20260626120000_get_verified_cohort_rank.sql</files>
  <read_first>
    - supabase/migrations/20260501055202_strategy_verifications.sql:185-369 (the SECURITY DEFINER fn body shape, the REVOKE/GRANT block at 281-283, and the self-verifying DO block at 296-369 — MIRROR this structure exactly)
    - src/lib/queries.ts:113-191 (getPercentiles — confirms strategy_analytics column names sharpe/sortino/max_drawdown, the published predicate, and the negative-max_drawdown convention at 162-168)
    - .planning/phases/42-peer-cohort-override-mandate/42-RESEARCH.md §"Pattern 2" + §"The Cohort Query" (the RPC SQL skeleton + min-N + identity-strip rationale)
  </read_first>
  <action>
    Create the migration file `20260626120000_get_verified_cohort_rank.sql` (forward-dated strictly after 20260625120000 per AGENTS.md migration convention). Define `public.get_verified_cohort_rank(p_sharpe DOUBLE PRECISION, p_sortino DOUBLE PRECISION, p_max_dd DOUBLE PRECISION)` RETURNS TABLE `(cohort_n INT, sharpe_pct INT, sortino_pct INT, max_dd_pct INT)`, LANGUAGE plpgsql, SECURITY DEFINER, `SET search_path = public, pg_catalog`.

    Body requirements (defense-in-depth, all locked decisions):
    - First statement: caller gate — `IF auth.role() = 'anon' OR auth.uid() IS NULL THEN RAISE EXCEPTION '...requires an authenticated session' USING ERRCODE = '42501';`.
    - Cohort definition: count verified+published strategies — `FROM strategies s JOIN strategy_analytics a ON a.strategy_id = s.id WHERE s.status = 'published' AND EXISTS (SELECT 1 FROM strategy_verifications v WHERE v.strategy_id = s.id AND v.trust_tier IS NOT NULL)`. The explicit `s.status = 'published'` predicate is defense-in-depth that excludes the caller's own drafts (D-02 locked).
    - Min-N gate: declare a named threshold (a `v_min_n CONSTANT INT := 20;` or inline-commented `20`). `IF v_n < 20 THEN RETURN QUERY SELECT v_n, NULL::INT, NULL::INT, NULL::INT; RETURN; END IF;` — honest empty, never a percentile against a thin set (prevents cell-size inference, D-02 locked).
    - Rank: percentile = % of cohort whose value is <= the blend's, for sharpe/sortino (higher=better). For max_dd use magnitude inversion "shallower = better": compare `abs(a.max_drawdown) >= p_max_dd` where `p_max_dd` is the magnitude (abs) of the blend's max_dd — matching getPercentiles' Math.abs convention (queries.ts:162-168). `round(100.0 * count(*) FILTER (...) / v_n)::INT`.
    - Identity strip: the function SELECTs ONLY aggregates (count, count FILTER). It NEVER selects or returns id/name/returns/PII. The RETURNS TABLE is provably 4 scalars.

    After the function: REVOKE ALL FROM PUBLIC, anon; GRANT EXECUTE TO authenticated; GRANT EXECUTE TO service_role (mirror 093 STEP 6). Add a COMMENT ON FUNCTION documenting PEER-03 + min-N=20 + aggregate-only.

    Self-verifying DO block (mirror 093 STEP 7): assert (a) the fn is registered in pg_proc under public; (b) it is SECURITY DEFINER (`pg_proc.prosecdef = true`); (c) EXECUTE is NOT granted to PUBLIC and NOT to anon (`has_function_privilege('public', oid, 'EXECUTE') = false` and same for anon if the role check is expressible) — at minimum assert the fn exists + is prosecdef; RAISE NOTICE on success. Each RAISE EXCEPTION names "Migration 20260626120000".

    Then APPLY the migration to the linked TEST project via the Supabase MCP `apply_migration` tool (name: `get_verified_cohort_rank`, the SQL body). This is the BLOCKING step — the RLS test and plan 02's route depend on the RPC existing on TEST.

    NO fenced SQL in this prose — write the actual SQL in the migration FILE; this action only specifies its structure.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260626120000_get_verified_cohort_rank.sql && grep -q "CREATE OR REPLACE FUNCTION public.get_verified_cohort_rank" supabase/migrations/20260626120000_get_verified_cohort_rank.sql && grep -q "SECURITY DEFINER" supabase/migrations/20260626120000_get_verified_cohort_rank.sql && grep -q "REVOKE ALL ON FUNCTION public.get_verified_cohort_rank" supabase/migrations/20260626120000_get_verified_cohort_rank.sql && grep -vq "^--" supabase/migrations/20260626120000_get_verified_cohort_rank.sql && grep -q "auth.uid() IS NULL\|auth.role()" supabase/migrations/20260626120000_get_verified_cohort_rank.sql</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `supabase/migrations/20260626120000_get_verified_cohort_rank.sql`, timestamp strictly after 20260625120000.
    - The function is SECURITY DEFINER with `SET search_path = public, pg_catalog`.
    - The cohort predicate is `s.status='published' AND EXISTS(... trust_tier IS NOT NULL)` (both clauses present).
    - Min-N=20 gate returns a NULL-rank row below the floor (grep for `< 20` and `NULL::INT`).
    - The RETURNS TABLE is exactly `(cohort_n INT, sharpe_pct INT, sortino_pct INT, max_dd_pct INT)` — no id/name/returns columns anywhere in the RETURNS or SELECT list.
    - REVOKE ALL FROM PUBLIC, anon + GRANT EXECUTE TO authenticated + service_role are present.
    - The auth.role()/auth.uid() guard raises 42501 for anon.
    - A self-verifying DO block asserts the fn is registered + SECURITY DEFINER.
    - The migration was applied to the TEST project via `apply_migration` (the apply call succeeded — record the result in SUMMARY).
  </acceptance_criteria>
  <done>Migration written, structurally complete, and applied to the TEST project without error (the self-verifying DO block's NOTICE fired).</done>
  <threat_model>
  ## Trust Boundaries
  | Boundary | Description |
  |----------|-------------|
  | authed client → DEFINER RPC | The RPC runs as owner; it crosses the tenant boundary to aggregate OTHER allocators' verified strategies. |

  ## STRIDE Threat Register
  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-42-01 | Information disclosure | get_verified_cohort_rank result | mitigate | RETURNS only (cohort_n, 3 percentiles); no id/name/returns ever SELECTed. Aggregate-only count FILTER. |
  | T-42-02 | Information disclosure | tiny-cohort cell-size inference | mitigate | min-N=20 floor → NULL-rank row below it; a percentile can't near-identify an individual. |
  | T-42-03 | Elevation of privilege | the SECURITY DEFINER fn callable by anon | mitigate | REVOKE ALL FROM PUBLIC, anon; GRANT only authenticated/service_role; in-fn auth.role()='anon'/auth.uid() IS NULL → 42501; SET search_path prevents search-path hijack. |
  | T-42-04 | Tampering | caller's own unpublished drafts polluting the cohort | mitigate | explicit `status='published'` predicate (defense-in-depth vs the published-OR-owner RLS). |
  </threat_model>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RLS + min-N + no-identity integration test (HAS_LIVE_DB)</name>
  <files>src/__tests__/verified-cohort-rank-rls.test.ts</files>
  <behavior>
    - Test: a non-admin authed client calling supabase.rpc('get_verified_cohort_rank', {...}) gets back ONLY (cohort_n, sharpe_pct, sortino_pct, max_dd_pct) — the result object has no id/name/returns keys.
    - Test (min-N): when the verified+published cohort on TEST is < 20, the RPC returns a row with cohort_n set but the three pct columns NULL (honest empty). Construct/assert against the live state; if the live cohort is naturally < 20 (likely — no clients), assert the NULL-rank shape directly.
    - Test (no cross-tenant leak): a normal authed allocator client SELECTing strategy_verifications directly sees only their own rows (RLS owner-scope) — re-pin the boundary that forces the RPC path. (Mirror strategy-verifications-rls.test.ts assertions.)
    - Test (auth gate): an anon/unauthenticated rpc call is rejected (42501 / error), not served.
  </behavior>
  <read_first>
    - src/__tests__/strategy-verifications-rls.test.ts (the HAS_LIVE_DB gate, createLiveAdminClient/createTestUser/cleanupLiveDbRow usage, and the owner-scope SELECT assertion pattern — MIRROR it)
    - src/lib/test-helpers/live-db.ts (HAS_LIVE_DB, advertiseLiveDbSkipReason, the client factories)
    - .planning/phases/42-peer-cohort-override-mandate/42-RESEARCH.md §"Cross-tenant leak surface" (the cell-size + identity-strip assertions to encode)
  </read_first>
  <action>
    Create `src/__tests__/verified-cohort-rank-rls.test.ts` gated on `HAS_LIVE_DB` (use `describe.skipIf(!HAS_LIVE_DB)` + `advertiseLiveDbSkipReason` so CI without live-DB creds skips cleanly — the standard model). Assertions per the <behavior> block: result-shape (no identity keys), min-N NULL-rank, owner-scope SELECT boundary, anon-rejection. Clean up any created test users/rows via `cleanupLiveDbRow`. The test runs against the TEST project where Task 1 applied the RPC.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/verified-cohort-rank-rls.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and imports the live-db helpers + `get_verified_cohort_rank`.
    - It uses `HAS_LIVE_DB` to skip cleanly when creds are absent (CI green without live-DB).
    - When live-DB is present, it asserts: (1) the RPC result has NO per-strategy identity keys; (2) min-N → NULL-rank row; (3) a normal client cannot read peers' verification rows; (4) anon rpc is rejected.
    - `npx vitest run src/__tests__/verified-cohort-rank-rls.test.ts -x` is green (or cleanly skipped if HAS_LIVE_DB is false in the exec env — record which in SUMMARY).
  </acceptance_criteria>
  <done>The RLS integration test passes (or skips honestly) and pins the no-leak + min-N + no-identity + anon-reject boundaries.</done>
</task>

</tasks>

<verification>
- `test -f supabase/migrations/20260626120000_get_verified_cohort_rank.sql` and it forward-dates 20260625120000.
- The migration applied to TEST via `apply_migration` without the DO block raising.
- `npx vitest run src/__tests__/verified-cohort-rank-rls.test.ts -x` green or cleanly skipped.
- `npm run typecheck` clean (the test file type-checks).
</verification>

<success_criteria>
- The verified-cohort aggregate is reachable ONLY through the DEFINER RPC; a normal authed client cannot read peers' verification rows.
- The RPC returns ONLY (cohort_n, sharpe_pct, sortino_pct, max_dd_pct); min-N=20 → NULL-rank; no identity leaks.
- The migration is applied to TEST and ready for prod auto-apply at /ship.
</success_criteria>

<output>
Create `.planning/phases/42-peer-cohort-override-mandate/42-01-SUMMARY.md` when done.
Record: the migration filename, whether `apply_migration` to TEST succeeded, and whether
the RLS test ran live or skipped (HAS_LIVE_DB state).
</output>
