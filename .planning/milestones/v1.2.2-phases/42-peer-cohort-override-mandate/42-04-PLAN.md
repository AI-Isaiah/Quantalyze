---
phase: 42-peer-cohort-override-mandate
plan: 04
type: execute
wave: 3
depends_on: [42-02, 42-03]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.tsx
  - src/app/factsheet/[id]/v2/BatchDPanels.peer-scenario.test.tsx
autonomous: true
requirements: [PEER-01, PEER-02, PEER-03]
user_setup: []

must_haves:
  truths:
    - "When the blend has n>=252 obs, the composer POSTs the engine's sample-basis sharpe/sortino/maxDD+n to /api/scenario/peer-rank and threads the returned peer into scenarioPeer on the synth payload."
    - "When n<252 the composer skips the fetch and scenarioPeer is null → the peer panel does not render (sample-floor suppression)."
    - "On the scenario path the PeerPercentilePanel shows 'Peer Percentile' (no Demo badge) + N=cohortSize + the disclosure 'hypothetical blend · ranked vs verified strategies · sample/252 basis'."
    - "The rank is reload-stable (a pure function of the blend metrics + the cohort snapshot; no PRNG/Date.now)."
    - "Below min-N the route returns { peer: null } → scenarioPeer stays null → the panel is silently absent."
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "The peer-rank fetch keyed on the engine metrics + n>=252 gate; threads scenarioPeer to the chart"
      contains: "peer-rank"
    - path: "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"
      provides: "scenarioPeer prop plumbed into buildScenarioFactsheetPayload"
      contains: "scenarioPeer"
    - path: "src/app/factsheet/[id]/v2/BatchDPanels.tsx"
      provides: "The scenario-path header re-label + disclosure copy"
      contains: "ranked vs verified strategies"
    - path: "src/app/factsheet/[id]/v2/BatchDPanels.peer-scenario.test.tsx"
      provides: "Render: scenario disclosure shows + n<252/null suppress + no Demo badge"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "/api/scenario/peer-rank"
      via: "fetch keyed on scenarioMetrics + n>=252"
      pattern: "scenario/peer-rank"
    - from: "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"
      to: "buildScenarioFactsheetPayload"
      via: "scenarioPeer arg"
      pattern: "scenarioPeer"
---

<objective>
Wire the live peer rank end-to-end on the blend: the composer fetches the rank from
`/api/scenario/peer-rank` (feeding the engine's sample-basis `scenarioMetrics`, gated on
n>=252), threads the result through a new `ScenarioFactsheetChart` `scenarioPeer` prop into
`buildScenarioFactsheetPayload`, and re-labels the `PeerPercentilePanel`'s scenario path
(no Demo badge, the hypothetical disclosure, sample/252 basis).

Purpose: PEER-01/02/03 made visible — the blend shows an honest peer rank against the real
verified universe, disclosed as a hypothetical, suppressed below the sample floor and below
min-N (the route returns null), reload-stable.

Output: the panel re-label + render test + the chart prop + the composer fetch wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-peer-cohort-override-mandate/42-UI-SPEC.md
@.planning/phases/42-peer-cohort-override-mandate/42-02-SUMMARY.md
@.planning/phases/42-peer-cohort-override-mandate/42-03-SUMMARY.md

# DESIGN.md: read before the panel re-label (token confirmation for the disclosure line).

<interfaces>
<!-- The integration chain (verified anchors) — do NOT re-explore. -->

The route (plan 02): POST /api/scenario/peer-rank
  body: { sharpe: number, sortino: number, maxDD: number, n: number }
  response: { peer: PeerPercentilePayload | null }   // PeerPercentilePayload = {cohortSize, sharpe, sortino, max_dd}

The carve-out plumbing (plan 03):
  - FactsheetCsvPayload now has optional `scenarioPeer?: PeerPercentilePayload`.
  - buildScenarioFactsheetPayload(args) accepts optional `args.scenarioPeer` and omits the key when undefined.
  - MetricsColumn gate renders PeerPercentilePanel when scenarioMode && ingestSource==="csv" && scenarioPeer != null.
  - PeerPercentilePanel dual-reads (api peerPercentile OR csv scenarioPeer); `isScenario = ingestSource==="csv"`.

ScenarioComposer.tsx anchors:
  - scenarioMetrics = useMemo(() => computeScenario(deAliased.strategies, deAliased.state, dateMapCache), [deAliased, dateMapCache])  // :1511-1513
  - scenarioMetrics (engine ComputedMetrics, SAMPLE/252 basis) has: sharpe, sortino, max_drawdown, n, portfolio_daily_returns.
  - ScenarioFactsheetChart call site at :2296-2301 (portfolioDaily={scenarioMetrics.portfolio_daily_returns ?? []}).
  - The composer is a "use client" component already using useMemo/useEffect/useState heavily.

ScenarioFactsheetChart.tsx anchors:
  - interface ScenarioFactsheetChartProps  // :64-92  (add `scenarioPeer?: PeerPercentilePayload`)
  - synthPayload = useMemo(() => buildScenarioFactsheetPayload({ benchmark, portfolioDaily }), [benchmark, portfolioDaily])  // :162-169
  - FactsheetBody mount with scenarioMode  // :187-189

UI-SPEC §"PeerPercentilePanel — header re-label" (verbatim contract):
  - scenario path header: Peer Percentile (NO DemoBadge) + retain the N={cohortSize} span.
  - disclosure (after the bars, NOT italic, 10px muted, U+00B7 middle-dot separators):
      "hypothetical blend · ranked vs verified strategies · sample/252 basis"
  - the api path keeps the existing "Demo cohort" badge + synthesized-cohort footnote UNCHANGED.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PeerPercentilePanel scenario-path re-label + disclosure (render test)</name>
  <files>src/app/factsheet/[id]/v2/BatchDPanels.tsx, src/app/factsheet/[id]/v2/BatchDPanels.peer-scenario.test.tsx</files>
  <behavior>
    - On the csv/scenario path (isScenario): header shows "Peer Percentile" with NO Demo badge; the N={cohortSize} span shows; the 3 PercentileBar rows render; the disclosure "hypothetical blend · ranked vs verified strategies · sample/252 basis" renders (plain 10px muted, not italic).
    - On the api path: the existing "Demo cohort" badge + synthesized-cohort footnote are UNCHANGED (regression guard).
    - When scenarioPeer is null on the csv arm: the panel renders nothing (null).
  </behavior>
  <read_first>
    - src/app/factsheet/[id]/v2/BatchDPanels.tsx:78-107 (PeerPercentilePanel — dual-reads via plan 03; add the isScenario header/disclosure branch)
    - .planning/phases/42-peer-cohort-override-mandate/42-UI-SPEC.md §"1. PeerPercentilePanel — header re-label" (the EXACT header copy, the disclosure copy + token classes, the no-badge rule)
    - .planning/phases/42-peer-cohort-override-mandate/42-03-SUMMARY.md (the dual-read shape plan 03 left)
    - DESIGN.md (token confirmation: text-[10px] text-text-muted; U+00B7 separator)
  </read_first>
  <action>
    In PeerPercentilePanel, branch on `isScenario` (computed in plan 03): when scenario, render the header text "Peer Percentile" WITHOUT the DemoBadge, keep the `N={p.cohortSize}` span, render the 3 existing PercentileBar rows unchanged, and render the disclosure paragraph with classes `mt-2 text-[10px] text-text-muted` and the verbatim copy "hypothetical blend · ranked vs verified strategies · sample/252 basis" (U+00B7 middle dots, NOT italic). When api, render the existing header (with DemoBadge) + the existing italic synthesized-cohort footnote — byte-identical to today. Use the exact UI-SPEC copy verbatim.

    Then create `src/app/factsheet/[id]/v2/BatchDPanels.peer-scenario.test.tsx`: render the panel under a FactsheetProvider with a csv+scenarioPeer payload (scenarioMode context) and assert the disclosure text + N + absence of "Demo cohort"; render with an api payload and assert the Demo badge + footnote still present; render with csv + null scenarioPeer and assert nothing renders.

    NO fenced code in this action — implement in the FILES.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/BatchDPanels.peer-scenario.test.tsx" -x</automated>
  </verify>
  <acceptance_criteria>
    - The scenario-path header is "Peer Percentile" with NO Demo badge; the N span + 3 bars render.
    - The disclosure renders the verbatim copy "hypothetical blend · ranked vs verified strategies · sample/252 basis" (middle-dot separators, plain 10px muted).
    - The api path is unchanged (Demo badge + italic footnote still present — asserted).
    - csv + null scenarioPeer → the panel renders nothing.
    - The render test is green.
  </acceptance_criteria>
  <done>The scenario-path panel is honestly disclosed (no Demo badge, hypothetical + sample/252 basis) while the api path is byte-identical.</done>
</task>

<task type="auto">
  <name>Task 2: Thread scenarioPeer through ScenarioFactsheetChart into the synth payload</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx:64-92 (the props interface) + :150-169 (the synthPayload useMemo + buildScenarioFactsheetPayload call)
    - .planning/phases/42-peer-cohort-override-mandate/42-03-SUMMARY.md (the buildScenarioFactsheetPayload optional scenarioPeer arg)
  </read_first>
  <action>
    Add `scenarioPeer?: PeerPercentilePayload` (import the type from `@/lib/factsheet/types`) to `ScenarioFactsheetChartProps`. Destructure it in the component signature. Pass it into `buildScenarioFactsheetPayload({ benchmark, portfolioDaily, scenarioPeer })` and add `scenarioPeer` to the synthPayload useMemo dependency array. Add a one-line PEER-01 comment that scenarioPeer is the only carve-out field flowing onto the synth csv payload. Nothing else in this file changes.

    NO fenced code in this action — implement in the FILE.
  </action>
  <verify>
    <automated>grep -q "scenarioPeer" "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx" && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `ScenarioFactsheetChartProps` has an optional `scenarioPeer?: PeerPercentilePayload`.
    - It is passed into `buildScenarioFactsheetPayload` and present in the synthPayload useMemo deps.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>scenarioPeer flows from the chart prop into the synth payload; an absent prop → byte-identical to today.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Composer peer-rank fetch (n>=252 gated, stale-guarded) + thread scenarioPeer to the chart</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <behavior>
    - When scenarioMetrics.n >= 252 AND the metrics are finite, the composer POSTs { sharpe, sortino, maxDD, n } (from the engine scenarioMetrics — sample/252 basis) to /api/scenario/peer-rank and stores response.peer in state.
    - When scenarioMetrics.n < 252, the composer does NOT fetch and scenarioPeer state is null (sample-floor suppression).
    - The fetch is keyed on the engine metrics so the same blend → same request → reload-stable; a changed blend re-fetches.
    - The route returning { peer: null } (below min-N) leaves scenarioPeer null → the panel is absent.
    - An out-of-order resolve must not overwrite a newer blend's result (stale-response guard).
    - scenarioPeer state is passed as the new prop to the ScenarioFactsheetChart call site.
  </behavior>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1494-1525 (the scenarioMetrics useMemo + portfolio_daily_returns memo — the engine metrics source) + :2296-2301 (the ScenarioFactsheetChart call site)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the file's existing useEffect/useState/fetch conventions — MIRROR them; do not introduce a new data-fetch abstraction)
    - .planning/phases/42-peer-cohort-override-mandate/42-02-SUMMARY.md (the route path + request/response shape)
    - .planning/phases/42-peer-cohort-override-mandate/42-RESEARCH.md §"Server-Fetch Design" (the client-side suppression order + reload-stability rationale)
  </read_first>
  <action>
    Add `scenarioPeer` state (`PeerPercentilePayload | null`, default null). Add a fetch effect keyed on `[scenarioMetrics.sharpe, scenarioMetrics.sortino, scenarioMetrics.max_drawdown, scenarioMetrics.n]`: when `scenarioMetrics.n >= 252` AND sharpe/sortino/max_drawdown are finite numbers, POST `{ sharpe: scenarioMetrics.sharpe, sortino: scenarioMetrics.sortino, maxDD: scenarioMetrics.max_drawdown, n: scenarioMetrics.n }` to `/api/scenario/peer-rank` (same-origin, `credentials: "same-origin"`, JSON body, `Content-Type: application/json`); on a 200 set `scenarioPeer = body.peer`; on any non-200 or a null peer set `scenarioPeer = null`. When `n < 252` (or metrics non-finite), skip the fetch and set null. Use an effect-cleanup ignore flag (or AbortController) so a stale resolve never overwrites a newer blend's result (the standard React pattern). Pass `scenarioPeer={scenarioPeer}` to the ScenarioFactsheetChart call site at :2296.

    Do NOT pass any cohort distribution anywhere — only the returned { cohortSize, sharpe, sortino, max_dd } rank. Do NOT change any other composer behavior.

    NO fenced code in this action — implement in the FILE.
  </action>
  <verify>
    <automated>grep -q "scenario/peer-rank" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" && grep -q "scenarioPeer" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" && npm run typecheck 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - scenarioPeer state exists; the fetch effect is keyed on the engine metrics triple + n.
    - The fetch fires only when n>=252 and metrics are finite; n<252 → null, no fetch.
    - The POST body is { sharpe, sortino, maxDD, n } from scenarioMetrics; the response.peer is stored.
    - A stale resolve cannot overwrite a newer result (ignore-flag/AbortController present).
    - `scenarioPeer={scenarioPeer}` is passed to ScenarioFactsheetChart.
    - No cohort distribution is referenced anywhere in the composer.
    - `npm run typecheck` clean.
  </acceptance_criteria>
  <done>The composer fetches the rank for a sufficiently-long blend, suppresses below the sample floor / min-N, guards against stale responses, and threads the rank to the chart.</done>
  <threat_model>
  ## Trust Boundaries
  | Boundary | Description |
  |----------|-------------|
  | composer (client) → POST /api/scenario/peer-rank | Only the caller's own blend metrics leave; only the caller's own rank returns. |

  ## STRIDE Threat Register
  | Threat ID | Category | Component | Disposition | Mitigation Plan |
  |-----------|----------|-----------|-------------|-----------------|
  | T-42-13 | Information disclosure | the client never receives the cohort distribution | mitigate | the composer reads only response.peer (3 percentiles + count); the route + RPC keep the distribution server-side (flow a). |
  | T-42-14 | Integrity / honesty | ranking a thin/short blend | mitigate | n>=252 client gate + min-N route null + the on-panel disclosure (Task 1). |
  </threat_model>
</task>

</tasks>

<verification>
- `npx vitest run "src/app/factsheet/[id]/v2/BatchDPanels.peer-scenario.test.tsx" -x` green.
- `npm run typecheck` clean.
- Full suite + coverage ratchet held: `npm run test:coverage` (per-wave merge gate).
- Diff confirms: the api path of PeerPercentilePanel is byte-identical; no cohort distribution crosses into the composer.
</verification>

<success_criteria>
- The blend shows a live peer rank against the real verified universe, disclosed as a hypothetical on the sample/252 basis.
- Suppressed below the sample floor (n<252, client-gated) and below min-N (route returns null).
- The rank is reload-stable; the cohort distribution never reaches the client.
</success_criteria>

<output>
Create `.planning/phases/42-peer-cohort-override-mandate/42-04-SUMMARY.md` when done.
Record the fetch effect's dependency key, the stale-guard mechanism used, and the chart prop name.
</output>
