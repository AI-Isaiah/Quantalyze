---
phase: 40-mount-the-real-factsheet-body
plan: 02
type: execute
wave: 2
depends_on: ["40-01"]
files_modified:
  - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx
  - src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx
  - src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx
  - src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx
autonomous: true
requirements: [BODY-01, BODY-03, BODY-04]
must_haves:
  truths:
    - "The Scenario tab renders the REAL FactsheetBody (not the Phase-38 two-chart subset) under the existing FactsheetProvider persist={false}"
    - "The composer's PeriodControl lives in the body's topSlot and drives the one shared XRangeContext alongside MasterBrush + ControlBar"
    - "Every factsheet panel renders without throwing across safe-empty / single-strategy / sub-N / healthy / non-finite blends, with no NaN/Infinity in the serialized SVG"
    - "The api-only panels (allocator / signatures / peer) are absent on the csv blend by construction"
    - "ScenarioComposer.tsx contains the literal FactsheetBody zero times (the static-guard landmine stays green)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"
      provides: "FactsheetBody mount replacing the two-chart render, inside the existing persist={false} provider"
      contains: "FactsheetBody"
    - path: "src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx"
      provides: "BODY-03 degenerate render matrix + BODY-04 render-absence assertions"
      contains: "Infinity"
  key_links:
    - from: "ScenarioFactsheetChart.tsx"
      to: "FactsheetBody (@/app/factsheet/[id]/v2/FactsheetView)"
      via: "mount inside the existing FactsheetProvider persist={false}, scenarioMode + topSlot={PeriodControl}"
      pattern: "FactsheetBody[\\s\\S]*scenarioMode"
    - from: "FactsheetBody.degenerate.test.tsx"
      to: "buildScenarioFactsheetPayload"
      via: "safe-empty / single / sub-N / healthy / non-finite payloads fed to the real body"
      pattern: "buildScenarioFactsheetPayload"
---

<objective>
Replace the Phase-38 two-chart render inside `ScenarioFactsheetChart.tsx`
(MasterBrush + `SCENARIO_EQUITY_CONFIG` + `SCENARIO_DRAWDOWN_CONFIG`) with the
REAL `<FactsheetBody>` — `scenarioMode hideHeader hideAllocatorSection
hideFooter={false} topSlot={<PeriodControl/>}` — inside the SAME existing
`<FactsheetProvider payload={synthPayload} persist={false}>`. This delivers BODY-01
(the scenario tab renders the complete factsheet on the blend). Prove BODY-03 (every
panel renders across degenerate blends, no NaN/Infinity) and BODY-04 (api-only
panels absent) with a net-new render test, and update the two affected existing
tests for the new mount shape.

Purpose: This is the "mount the complete factsheet" goal — the blend gets every
factsheet panel for free, parity-by-construction. The hard landmine: the mount MUST
live in `ScenarioFactsheetChart.tsx`, NEVER `ScenarioComposer.tsx`, because
`ScenarioComposer.test.tsx:3377` is a static source guard that fails if
`ScenarioComposer.tsx` contains the literal `FactsheetBody`.

Output: the real body mounted in the composer; a BODY-03/04 degenerate render test;
updated `scenario-shared-window.test.tsx` + `EquityChart.scenario.test.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-mount-the-real-factsheet-body/40-CONTEXT.md
@.planning/phases/40-mount-the-real-factsheet-body/40-RESEARCH.md
@.planning/phases/40-mount-the-real-factsheet-body/40-UI-SPEC.md
@.planning/phases/40-mount-the-real-factsheet-body/40-01-SUMMARY.md

<interfaces>
<!-- Exact mount mechanics. Extracted from codebase this session. Use directly. -->

scenarioMode prop (added in Plan 40-01): FactsheetBody now accepts
  scenarioMode?: boolean (default false). Plan 40-02 passes scenarioMode (true).

FactsheetBody import (same module AllocationDashboardV2.tsx:13 already imports from):
  import { FactsheetBody } from "@/app/factsheet/[id]/v2/FactsheetView";

Current ScenarioFactsheetChart.tsx mount (Phase 38, ~lines 162-193) — to REPLACE:
  <FactsheetProvider payload={synthPayload} persist={false}>
    <div className="mb-1 flex items-center justify-end"><PeriodControl axisLength={axisLength} /></div>
    <MasterBrush />
    <div data-testid="equity-chart-scenario-overlay" className="mt-4"><TimeSeriesChart config={SCENARIO_EQUITY_CONFIG} /></div>
    <div className="mt-4"><TimeSeriesChart config={SCENARIO_DRAWDOWN_CONFIG} /></div>
  </FactsheetProvider>

Phase-40 target (RESEARCH Must-Answer #1) — keep the SAME provider:
  <FactsheetProvider payload={synthPayload} persist={false}>
    <FactsheetBody
      payload={synthPayload}
      scenarioMode
      hideHeader
      hideAllocatorSection
      hideFooter={false}
      topSlot={<div data-testid="equity-chart-scenario-overlay" className="mb-1 flex items-center justify-end"><PeriodControl axisLength={axisLength} /></div>}
    />
  </FactsheetProvider>

KEEP defined (do NOT delete — locked decision #9 / RESEARCH Open Q + Pitfall 5):
  SCENARIO_EQUITY_CONFIG, SCENARIO_DRAWDOWN_CONFIG exports stay (asserted by
  scenario-factsheet-payload.test.ts:160-176). The inline MasterBrush/TimeSeriesChart
  imports become unused — remove only the now-unused imports in ScenarioFactsheetChart.tsx
  if lint flags them, but do NOT delete the exported configs from scenario-factsheet-payload.ts.

PeriodControl is defined locally in ScenarioFactsheetChart.tsx (lines 95-138), uses
  useXRange(); axisLength = synthPayload.dates.length (line 165). It MUST stay a
  descendant of FactsheetProvider — topSlot renders inside the <article> under the
  provider (FactsheetView.tsx:188), so useXRange resolves. KEEP aria-selected={false}
  on each period tab (a11y requirement, ScenarioFactsheetChart.tsx:125).

BODY-04 render-absence test hooks (csv blend → these are NEVER rendered):
  document.getElementById("factsheet-allocator")  → null  (gated ingestSource==="api", FactsheetView.tsx:275)
  document.getElementById("factsheet-signatures") → null  (gated hasComparator && ingestSource==="api", FactsheetView.tsx:241)
  PeerPercentilePanel → absent (gated ingestSource==="api" in MetricsColumn.tsx:116)

Degenerate payloads via buildScenarioFactsheetPayload (scenario-factsheet-payload.ts):
  - safe-empty: portfolioDaily=[] → all arrays [], scalars 0/null, n=0
  - single/sub-N: portfolioDaily of ~30 points (10 ≤ n < 252) → populated, low-N caveats fire
  - healthy: portfolioDaily of ~300 points → fully populated
  - non-finite: a portfolioDaily containing a NaN/Inf value → adapter degenerate gate collapses to safe-empty BEFORE compute()

Test stub block (MANDATORY for real FactsheetProvider mounts) — copy verbatim
  from scenario-shared-window.test.tsx:1-43 (vi.mock sentry-capture + localStorage Map stub).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mount the real FactsheetBody in ScenarioFactsheetChart (replace the two-chart render)</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx (the FULL file — the mount return ~162-193, PeriodControl 95-138, imports 1-12, axisLength 165)
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (FactsheetBody 156-269 as the body template — confirm topSlot renders at line 188 inside the provider/article; the prop set hideHeader/hideAllocatorSection/hideFooter/scenarioMode from Plan 40-01)
    - src/app/(dashboard)/allocations/AllocationDashboardV2.tsx (lines 143-150 — the EXISTING production FactsheetBody mount precedent: same hideHeader hideAllocatorSection topSlot shape in the same dashboard chrome)
    - .planning/phases/40-mount-the-real-factsheet-body/40-RESEARCH.md Must-Answer #1 (exact edit), #3 (degenerate audit — no new guard needed), #5 (a11y: body root is <article id="factsheet-main">, NOT <main>), Pitfall 1 (the ScenarioComposer landmine), Pitfall 5 (keep SCENARIO_* exports)
    - .planning/phases/40-mount-the-real-factsheet-body/40-UI-SPEC.md §Mount Layout Contract
  </read_first>
  <action>
    In ScenarioFactsheetChart.tsx, import `FactsheetBody` from
    `@/app/factsheet/[id]/v2/FactsheetView`. Replace the JSX inside the existing
    `<FactsheetProvider payload={synthPayload} persist={false}>` (the PeriodControl
    wrapper div + MasterBrush + the two `<div><TimeSeriesChart config={SCENARIO_*}/></div>`
    blocks) with a single `<FactsheetBody payload={synthPayload} scenarioMode hideHeader
    hideAllocatorSection hideFooter={false} topSlot={…} />`. Keep the SAME provider — do
    NOT add a second FactsheetProvider (GUARD-04 class; persist={false} isolation lives
    in this one provider).

    The topSlot is a div carrying `data-testid="equity-chart-scenario-overlay"`
    (RELOCATED here from the old equity-chart wrapper) with the existing
    `className="mb-1 flex items-center justify-end"`, wrapping
    `<PeriodControl axisLength={axisLength} />`. PeriodControl stays the local component
    (keep its `aria-selected={false}` tabs). Because topSlot renders inside the
    <article> under the provider, PeriodControl's useXRange() still resolves and drives
    the one shared XRangeContext alongside the body's MasterBrush + ControlBar.

    Remove the now-unused MasterBrush / TimeSeriesChart imports from THIS file if lint
    flags them. Do NOT delete SCENARIO_EQUITY_CONFIG / SCENARIO_DRAWDOWN_CONFIG from
    scenario-factsheet-payload.ts (locked decision #9 — keep the exports + their tests;
    they become merely unmounted, not deleted). Leave their import in this file only if
    still referenced; otherwise drop the unused import.

    Do NOT touch ScenarioComposer.tsx. The literal `FactsheetBody` must appear ZERO
    times in ScenarioComposer.tsx (the static guard at ScenarioComposer.test.tsx:3377).
    The mount lives exclusively in this file, which ScenarioComposer.test.tsx mocks.
  </action>
  <verify>
    <automated>grep -v '^\s*//' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" | grep -c "FactsheetBody"</automated>
    <automated>npx tsc --noEmit 2>&1 | grep -E "ScenarioFactsheetChart" ; echo "tsc done"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "FactsheetBody" "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"` ≥ 1 (mount present).
    - `grep -v '^\s*//' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" | grep -c "FactsheetBody"` returns 0 (the static-guard landmine stays green — checked on non-comment lines).
    - `grep -c "FactsheetProvider" "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"` returns the SAME count as before (one provider — no second one added).
    - `grep -c "equity-chart-scenario-overlay" "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"` returns 1 (testid relocated onto the topSlot wrapper).
    - `grep -c "SCENARIO_EQUITY_CONFIG\|SCENARIO_DRAWDOWN_CONFIG" "src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts"` is unchanged (exports kept).
    - `npx eslint "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"` exits 0 (no unused-import failures).
  </acceptance_criteria>
  <done>The composer renders the real FactsheetBody (scenarioMode, hideHeader, hideAllocatorSection, hideFooter={false}, topSlot=PeriodControl) inside the one existing persist={false} provider; ScenarioComposer.tsx is untouched and FactsheetBody-free; SCENARIO_* exports retained.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BODY-03/04 degenerate render test — every panel safe across blends, api panels absent</name>
  <files>src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts (buildScenarioFactsheetPayload + the safe-empty defaults; the degenerate gate that collapses non-finite to safe-empty BEFORE compute())
    - src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx (lines 1-43 — copy the stub block; makeReturnsSeries helper 56-74)
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (the api-only render-absence ids: factsheet-allocator:276, factsheet-signatures:243; ingestSource gates 241/275)
    - .planning/phases/40-mount-the-real-factsheet-body/40-RESEARCH.md Must-Answer #3 (the per-panel degenerate audit — DO NOT add guards the audit says are unnecessary; the only tripwire is StreakHist barW=Infinity), Must-Answer #6 (test-infra stub note + the BODY-04 render-absence approach)
    - src/lib/factsheet/audit-c20.test.ts (the existing ingestSource pin to lean on for BODY-04)
  </read_first>
  <behavior>
    - For each payload in {safe-empty (portfolioDaily=[]), single/sub-N (~30 pts), healthy (~300 pts), non-finite (a series with a NaN value)} the real FactsheetBody mounts under a real FactsheetProvider WITHOUT throwing.
    - For every rendered case, the serialized container.innerHTML contains NO "NaN" and NO "Infinity" substring (the StreakHist barW=Infinity tripwire + format-drift guard).
    - On the csv blend (any payload), the api-only panels are absent: getElementById("factsheet-allocator") === null, getElementById("factsheet-signatures") === null, and no PeerPercentile panel renders (BODY-04 render-absence).
  </behavior>
  <action>
    Create FactsheetBody.degenerate.test.tsx in src/app/factsheet/[id]/v2/. Copy the
    localStorage + sentry-capture stub block verbatim from
    scenario-shared-window.test.tsx:1-43 (MANDATORY — the provider persistence primitive
    touches both on mount). Reuse a makeReturnsSeries-style helper to build daily-return
    DailyPoint arrays.

    Build the four payloads via buildScenarioFactsheetPayload: safe-empty
    (portfolioDaily: []), single/sub-N (~30 points so 10 ≤ n < 252), healthy (~300
    points), and non-finite (a ~120-point series with one value set to NaN — the adapter
    degenerate gate collapses it to safe-empty before compute, so the body never sees
    NaN/Inf). For each, render the real FactsheetBody under a real FactsheetProvider with
    the composer-mount props (scenarioMode, hideHeader, hideAllocatorSection,
    hideFooter={false}). Assert the render does not throw, then assert
    `container.innerHTML` contains neither "NaN" nor "Infinity".

    BODY-04 render-absence (assert on the healthy AND safe-empty cases, where a crash is
    most/least likely): after render, assert
    `document.getElementById("factsheet-allocator")` is null,
    `document.getElementById("factsheet-signatures")` is null, and there is no element
    with text matching the peer-percentile panel (or assert the payload.ingestSource is
    "csv" as the by-construction pin). Do NOT add any new defensive guard to a panel —
    RESEARCH audited all ~24 panels and confirmed none is needed; this is a render test,
    not a guard-adding task.

    Run with --no-file-parallelism. If a lazy next/dynamic panel (heatmaps) is involved,
    the render must still not throw — the body's empty-array early-returns cover the
    degenerate cases per the audit.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx" --no-file-parallelism` passes every case.
    - The test mounts the real FactsheetBody across all four blends (safe-empty / sub-N / healthy / non-finite) and asserts no throw.
    - Each case asserts container.innerHTML has no "NaN" and no "Infinity" substring (`grep -c '"Infinity"\|/NaN/\|Infinity' src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx` ≥ 1 proves the assertion exists).
    - The BODY-04 render-absence assertions are present: `grep -c "factsheet-allocator\|factsheet-signatures" "src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx"` ≥ 1.
    - The mandatory stub block is present (`grep -c "captureToSentry\|stubGlobal(\"localStorage\"" "src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx"` ≥ 1).
    - No new guard added to any panel file (`git diff --name-only` lists no src/app/factsheet/[id]/v2/*Panel*.tsx or MetricsColumn/DistributionPanels/AnalyticalPanels changes from this task).
  </acceptance_criteria>
  <done>BODY-03 proven: real body renders without throwing across safe-empty/sub-N/healthy/non-finite, no NaN/Infinity in SVG. BODY-04 proven: allocator/signatures/peer panels absent on the csv blend. No unnecessary guards added.</done>
</task>

<task type="auto">
  <name>Task 3: Update scenario-shared-window + EquityChart.scenario tests for the new mount shape</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx, src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx (the `=== 2` chart-SVG assertions at ~96-97 and ~143; the equity-chart-scenario-overlay testid queries at ~99-102 and the keyboard-nav query at ~127-130 that scopes svg INSIDE the overlay; the 3M/ALL SegmentedControl proof ~146-186 stays valid)
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx (the PARITY-03 block ~498-565 — it queries `svg[role="img"][tabindex="0"]` INSIDE the equity-chart-scenario-overlay testid; after the swap that testid wraps PeriodControl, NOT a chart, so the selector must re-point to the body's equity chart svg)
    - .planning/phases/40-mount-the-real-factsheet-body/40-RESEARCH.md Must-Answer #6 (the exact UPDATE actions for both tests) + Pitfall 2 + Pitfall 3
  </read_first>
  <action>
    scenario-shared-window.test.tsx: the full body now renders MANY chart SVGs
    (PerformanceCharts: cumulative + underwater + others), so every `=== 2` assertion on
    `svg[role="img"][tabindex="0"]` is wrong — relax each to `>= 1` (or assert the
    specific MasterBrush + at least the equity chart). The testid
    `equity-chart-scenario-overlay` now lives on the topSlot wrapper (PeriodControl), NOT
    on a chart wrapper. The keyboard-nav assertion that scoped the equity chart svg
    INSIDE that overlay (~127-130) must re-point: query the body's equity chart svg by
    its own selector (the first `svg[role="img"][tabindex="0"]` in the rendered article,
    or the MasterBrush-driven window), NOT inside the overlay testid. Keep the
    shared-window proof intact — driving the window (via PeriodControl 3M/ALL or keyboard)
    still narrows/widens the MasterBrush label; that is the load-bearing assertion and
    must stay green. The 3M/ALL SegmentedControl test still works (PeriodControl is in
    topSlot, still a role="tablist" name="Period").

    EquityChart.scenario.test.tsx PARITY-03: the assertion proves the scenario equity
    line is DRAWN (≥1 non-empty `<path d>`) and exactly ONE strategy line in blank mode,
    no "Equity data warming up" copy. After the swap the equity line renders through the
    body's PerformanceCharts `cumulative` config (still a real `<path>`), but the selector
    keys off `svg[role="img"][tabindex="0"]` INSIDE the equity-chart-scenario-overlay
    testid — which now wraps PeriodControl. Re-point the selector to the body's equity
    chart svg (the cumulative TimeSeriesChart — query the first chart svg in the rendered
    container, or a stable hook on the Performance section). Keep the honesty intent: ≥1
    drawn path, exactly one strategy line (no fabricated baseline), no warming-up copy.
    Do NOT touch the legacy-EquityChart describe blocks (the Overview path) — those are
    untouched by Phase 40.

    Both files already have the stub block at their top (verify; if a re-render path now
    needs it, it is already present). Run both with --no-file-parallelism.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Both test files pass: `npx vitest run "src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx" --no-file-parallelism` is green.
    - `grep -c "toBe(2)\|=== 2\|).toBe(2)" "src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx"` returns 0 for the chart-SVG-count assertions (relaxed off the Phase-38 `=== 2`).
    - The shared-window proof still present: `grep -c "narrowed\|fullLabel\|afterLabel\|not.toBe" "src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx"` ≥ 1 (window-driving assertion retained).
    - PARITY-03 still asserts a drawn path: `grep -c "drawnLines\|getAttribute(\"d\")" "src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx"` ≥ 1, and the test is green (selector re-pointed to the body's equity chart).
    - The legacy-EquityChart describe blocks are unchanged (only the scenario/PARITY-03 selectors moved).
  </acceptance_criteria>
  <done>scenario-shared-window relaxed off `=== 2` with the shared-window proof intact; PARITY-03 selector re-pointed to the body's equity chart, honesty assertions (≥1 drawn path, one line, no warming-up copy) green; legacy paths untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-render reuse. No fetch, no input, no auth, no DB, no secret, no persistence-write. The synth payload is engine-derived + Phase-39-validated; persist={false} blocks storage writes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-03 | Tampering (a11y-landmark regression — the v1.2 JOURNEY-03 duplicate-`<main>`/region/tablist class) | FactsheetBody mount in composer chrome | mitigate | RESEARCH confirmed the body root is `<article id="factsheet-main">` (NOT `<main>`, NOT role=region on a footer); the page `<main>` lives in the dashboard chrome; PeriodControl tablist is a sibling (distinct aria-label="Period"). The existing AllocationDashboardV2 production mount is the empirical proof. No new landmark introduced; the permanent axe gate is Phase 43 (GUARD-03). Phase 40's obligation is non-regression, satisfied by mounting the same body the precedent already ships. |
| T-40-04 | Information disclosure (cross-tab state bleed, Phase-38 RT2 class) | FactsheetProvider persistence | mitigate | The mount stays inside the existing single `<FactsheetProvider persist={false}>`; no second provider, no new storageKey is added (asserted by the FactsheetProvider-count acceptance criterion in Task 1). persist={false} gates both the URL write and the hydration read. Permanent pin is GUARD-04 (Phase 43). |
| T-40-05 | Tampering (the static-guard landmine: FactsheetBody inlined in ScenarioComposer.tsx) | ScenarioComposer.tsx source | mitigate | The mount lives exclusively in ScenarioFactsheetChart.tsx (which ScenarioComposer.test.tsx mocks); Task 1 acceptance criterion greps ScenarioComposer.tsx non-comment lines for FactsheetBody === 0. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING (in-repo TSX + tests). No package-manager surface; legitimacy gate N/A. |
</threat_model>

<verification>
- `grep -v '^\s*//' ScenarioComposer.tsx | grep -c FactsheetBody` returns 0 (static-guard landmine green).
- `npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.degenerate.test.tsx" --no-file-parallelism` green (BODY-03/04).
- `npx vitest run` on scenario-shared-window + EquityChart.scenario green (updated mount shape).
- Phase gate (run after the wave merges): `npm run test:coverage` (full suite + ratchet lines 82 / fns 74 / branches 72 / stmts 80) green, and `npx tsc --noEmit` clean. The new scenarioMode={true} render + the BODY-02 scenarioMode={false} render (Plan 40-01) exercise both ControlBar branches so the ratchet does not regress (RESEARCH Pitfall 6).
</verification>

<success_criteria>
- The Scenario tab renders the real FactsheetBody (full panel set) on the blend, replacing the Phase-38 two-chart subset, inside the one existing persist={false} provider.
- PeriodControl lives in topSlot and drives the shared XRangeContext alongside MasterBrush + ControlBar.
- BODY-03: every panel renders without throwing across safe-empty / single / sub-N / healthy / non-finite; no NaN/Infinity in the SVG.
- BODY-04: allocator / signatures / peer panels absent on the csv blend.
- ScenarioComposer.tsx contains FactsheetBody zero times (static-guard landmine green).
- scenario-shared-window + EquityChart.scenario updated and green; legacy paths untouched; SCENARIO_* exports retained.
</success_criteria>

<output>
Create `.planning/phases/40-mount-the-real-factsheet-body/40-02-SUMMARY.md` when done.
</output>
