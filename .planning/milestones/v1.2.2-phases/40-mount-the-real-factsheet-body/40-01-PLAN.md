---
phase: 40-mount-the-real-factsheet-body
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/MetricsColumn.tsx
  - src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx
autonomous: true
requirements: [BODY-02]
must_haves:
  truths:
    - "FactsheetBody accepts an additive scenarioMode?: boolean prop that defaults to false"
    - "With scenarioMode={true}, ControlBar omits the Copy-share-link and Compare-strategies actions while keeping Display / Reset view / ComparatorPicker"
    - "FactsheetBody default props render byte-identically to scenarioMode={false}"
    - "MetricsColumn accepts scenarioMode as an inert Phase-42 seam without changing its render"
  artifacts:
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "scenarioMode in FactsheetBodyOptions + threading into ControlBar and MetricsColumn"
      contains: "scenarioMode"
    - path: "src/app/factsheet/[id]/v2/MetricsColumn.tsx"
      provides: "scenarioMode prop accepted (inert seam, void scenarioMode)"
      contains: "scenarioMode"
    - path: "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx"
      provides: "BODY-02 prop-equivalence + Share/Compare suppression test"
      contains: "scenarioMode"
  key_links:
    - from: "FactsheetBody"
      to: "ControlBar"
      via: "scenarioMode prop threaded through"
      pattern: "ControlBar scenarioMode=\\{scenarioMode\\}"
    - from: "FactsheetBody"
      to: "MetricsColumn"
      via: "scenarioMode prop threaded through (Phase-42 seam)"
      pattern: "MetricsColumn scenarioMode=\\{scenarioMode\\}"
---

<objective>
Add the additive `scenarioMode?: boolean` prop (default `false`) to the factsheet
body, threading it into exactly two children — `ControlBar` (suppress the
Share-link + Compare-strategies actions when true) and `MetricsColumn` (accept it
as the inert Phase-42 peer-carve-out seam). Default `false` keeps every existing
call site (`page.tsx`, the Discovery detail page, the Overview
`EquityChartWidget`) BYTE-IDENTICAL.

Purpose: This is the contract Plan 40-02's mount consumes. Defining `scenarioMode`
first (interface-first ordering) means the mount task receives the prop already
in place — no scavenger hunt. It is the only `factsheet/[id]/v2/*` source change
in this phase, and it must be provably zero-behavior-change at the default.

Output: `scenarioMode` on `FactsheetBodyOptions`, threaded to `ControlBar` +
`MetricsColumn`; a net-new BODY-02 test pinning default ≡ `scenarioMode={false}`
and proving `scenarioMode={true}` suppresses Share + Compare.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/40-mount-the-real-factsheet-body/40-CONTEXT.md
@.planning/phases/40-mount-the-real-factsheet-body/40-RESEARCH.md
@.planning/phases/40-mount-the-real-factsheet-body/40-UI-SPEC.md

<interfaces>
<!-- Exact signatures the executor needs. Extracted from codebase this session. -->
<!-- Use these directly — no exploration needed. -->

FactsheetBodyOptions (src/app/factsheet/[id]/v2/FactsheetView.tsx:135-146) — current:
  hideHeader?: boolean
  hideAllocatorSection?: boolean
  hideFooter?: boolean
  topSlot?: ReactNode

FactsheetBody (FactsheetView.tsx:156-162) destructures:
  { payload, hideHeader = false, hideAllocatorSection = false, hideFooter = false, topSlot }
  Renders (FactsheetView.tsx:187-269): FactsheetHeader → topSlot → KpiStrip →
  SectionNav → <ControlBar /> (line 191) → grid → … → <MetricsColumn /> (line 269).

ControlBar (FactsheetView.tsx:825-866) — currently takes NO props, reads context only
  (usePayload, useXRange, useComparator, useShareMode). Renders:
    <DisplayMenu />
    <button>Reset view</button>
    <ShareLinkButton strategyId={payload.strategyId} />        ← line ~852 (suppress when scenarioMode)
    {!shareMode && <a href={`/compare?ids=${payload.strategyId}`}>Compare strategies</a>}  ← line ~853-862 (suppress when scenarioMode)
    <ComparatorPicker />

MetricsColumn (src/app/factsheet/[id]/v2/MetricsColumn.tsx:19) — currently:
  export function MetricsColumn() { … }   ← takes NO props

Three byte-identity call sites that pass NO scenarioMode (must stay unchanged):
  - src/app/factsheet/[id]/v2/page.tsx:384  → <FactsheetView payload={payloadWithTrust} />
  - src/app/discovery/[slug]/[strategyId]/page.tsx:128 → <FactsheetView payload=… />
  - src/app/(dashboard)/allocations/AllocationDashboardV2.tsx:143 → <FactsheetBody … /> (no scenarioMode)

Test stub block (REQUIRED for any test mounting a real FactsheetProvider) —
copy verbatim from scenario-shared-window.test.tsx:24-43:
  vi.mock("@/lib/sentry-capture", () => ({ captureToSentry: vi.fn() }));
  + the localStorage Map-backed stub via vi.stubGlobal + Object.defineProperty.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add scenarioMode to FactsheetBodyOptions and thread into ControlBar + MetricsColumn</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/MetricsColumn.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (FactsheetBodyOptions 135-146; FactsheetBody 156-269; ControlBar 825-866 — the Share/Compare children)
    - src/app/factsheet/[id]/v2/MetricsColumn.tsx (signature line 19; body unchanged)
    - .planning/phases/40-mount-the-real-factsheet-body/40-RESEARCH.md Must-Answer #2 (the exact threading mechanics + Pitfall 4 lint trap)
    - .planning/phases/40-mount-the-real-factsheet-body/40-UI-SPEC.md §scenarioMode Visual Effect + §Byte-Identity Boundary
  </read_first>
  <behavior>
    - scenarioMode defaults to false → FactsheetBody with no scenarioMode renders Share + Compare in ControlBar (byte-identical to today).
    - scenarioMode={true} → ControlBar renders neither the Copy-share-link button nor the Compare-strategies anchor; Display / Reset view / ComparatorPicker still render.
    - MetricsColumn accepts scenarioMode and renders identically regardless of its value (inert seam in Phase 40).
  </behavior>
  <action>
    In FactsheetView.tsx add `scenarioMode?: boolean` to the `FactsheetBodyOptions`
    interface with a doc comment naming it the composer-mount flag (default false →
    every existing call site byte-identical; Phase-42 peer-carve-out seam). In the
    `FactsheetBody` destructure add `scenarioMode = false`. Change the `<ControlBar />`
    call (FactsheetView.tsx:191) to `<ControlBar scenarioMode={scenarioMode} />` and
    the `<MetricsColumn />` call (FactsheetView.tsx:269) to
    `<MetricsColumn scenarioMode={scenarioMode} />`.

    Give `ControlBar` a `scenarioMode?: boolean` prop (default false). Wrap the
    `<ShareLinkButton strategyId={payload.strategyId} />` render in `!scenarioMode &&`
    and add `!scenarioMode &&` to the existing `!shareMode &&` guard on the
    Compare-strategies `<a>`. Keep DisplayMenu, Reset view, and ComparatorPicker
    unconditional. Absence (not aria-hidden, not disabled) is the correct a11y
    treatment per UI-SPEC §Accessibility — do not add aria-hidden/disabled.

    Give `MetricsColumn` a `scenarioMode?: boolean` prop (default false). It gates
    nothing in Phase 40 — it is the Phase-42 seam (PEER-01). To avoid the
    `@typescript-eslint/no-unused-vars` lint failure (RESEARCH Pitfall 4), add
    `void scenarioMode;` with a short comment naming Phase 42 as the consumer.
    Do NOT add any conditional render keyed off scenarioMode in MetricsColumn this
    phase.

    Touch nothing else in FactsheetView.tsx or MetricsColumn.tsx. Do NOT alter
    page.tsx, the Discovery detail page, or AllocationDashboardV2.tsx — they pass
    no scenarioMode and must stay byte-identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "FactsheetView|MetricsColumn" ; echo "tsc-exit done"</automated>
    <automated>grep -c "scenarioMode" "src/app/factsheet/[id]/v2/MetricsColumn.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "scenarioMode?: boolean" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` shows it on FactsheetBodyOptions AND on the ControlBar prop signature.
    - `grep -c "ControlBar scenarioMode={scenarioMode}" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` returns 1 and `grep -c "MetricsColumn scenarioMode={scenarioMode}" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` returns 1.
    - `grep -c "void scenarioMode" "src/app/factsheet/[id]/v2/MetricsColumn.tsx"` returns 1 (the lint-safe inert seam).
    - `npx eslint "src/app/factsheet/[id]/v2/MetricsColumn.tsx" "src/app/factsheet/[id]/v2/FactsheetView.tsx"` exits 0 (no no-unused-vars failure).
    - `git diff --name-only` lists ONLY FactsheetView.tsx + MetricsColumn.tsx (no page.tsx / discovery / AllocationDashboardV2 changes).
  </acceptance_criteria>
  <done>scenarioMode threads through FactsheetBody → ControlBar (+ MetricsColumn seam); default false; lint + tsc clean; no byte-identity call site touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BODY-02 test — prop equivalence + Share/Compare suppression</name>
  <files>src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (FactsheetBody export + ControlBar Share/Compare copy strings "Copy share link", "Compare strategies")
    - src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx (lines 1-43 — the localStorage + sentry stub block to copy verbatim; lines 24-43 are the canonical stub)
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts (buildScenarioFactsheetPayload — used to synthesize a healthy populated FactsheetCsvPayload fixture)
    - .planning/phases/40-mount-the-real-factsheet-body/40-RESEARCH.md Must-Answer #4 (byte-identity test shape) + Must-Answer #6 test-infra note (the stub block is MANDATORY)
  </read_first>
  <behavior>
    - Test 1 (prop equivalence): FactsheetBody with default props and FactsheetBody with scenarioMode={false}, both under a real FactsheetProvider with the same populated payload, produce identical container.innerHTML.
    - Test 2 (suppression): FactsheetBody with scenarioMode={true} renders NEITHER "Copy share link" NOR "Compare strategies"; the same body with scenarioMode={false} renders BOTH.
    - Test 3 (controls retained): with scenarioMode={true}, the "Reset view" button and the Display menu still render (ComparatorPicker present).
  </behavior>
  <action>
    Create FactsheetBody.scenario-mode.test.tsx in src/app/factsheet/[id]/v2/.
    Copy the localStorage Map-backed stub + the `vi.mock("@/lib/sentry-capture", …)`
    block verbatim from scenario-shared-window.test.tsx:1-43 (RESEARCH marks this
    MANDATORY — the provider's persistence primitive touches both on mount even at
    persist={false}).

    Build a populated csv payload by calling `buildScenarioFactsheetPayload` with a
    healthy returns series (~300 points so the body is fully populated; reuse the
    makeReturnsSeries helper pattern from scenario-shared-window.test.tsx). Render
    FactsheetBody under a real FactsheetProvider (import both from
    @/app/factsheet/[id]/v2/factsheet-context and @/app/factsheet/[id]/v2/FactsheetView).

    Test 1: render default-props and scenarioMode={false} variants into separate
    containers; assert container.innerHTML strings are identical. Test 2: render
    scenarioMode={true}; assert queryByText(/Copy share link/i) and
    queryByText(/Compare strategies/i) are both null; render scenarioMode={false};
    assert both are present. Test 3: with scenarioMode={true}, assert
    getByText(/Reset view/i) and the Display menu still resolve.

    Use hideHeader / hideFooter={false} / hideAllocatorSection consistent with the
    composer mount so the test exercises the real mount shape. Run with
    --no-file-parallelism to avoid the documented local contention flake.
  </action>
  <verify>
    <automated>npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - The test file exists and `npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" --no-file-parallelism` passes all cases.
    - The prop-equivalence test (default ≡ scenarioMode={false}) is present and green — proving BODY-02 default-inert.
    - The suppression test proves scenarioMode={true} drops Share + Compare AND scenarioMode={false} keeps both.
    - `grep -c "captureToSentry\|stubGlobal(\"localStorage\"" "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx"` is ≥ 1 (the mandatory stub block is present).
  </acceptance_criteria>
  <done>BODY-02 pinned: default props ≡ scenarioMode={false} (byte-identical innerHTML); scenarioMode={true} suppresses exactly Share + Compare; both ControlBar branches exercised for coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Phase 40 adds no fetch/input/auth/DB/secret/persistence-write. The only data is the ephemeral, engine-derived, Phase-39-validated synth payload, rendered client-side. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Information disclosure | scenarioMode ControlBar gate | mitigate | scenarioMode={true} suppresses the Share-link + Compare actions so a hypothetical blend never exposes a copyable `/compare?ids=` URL or shareable link for a non-existent strategy; asserted by the BODY-02 suppression test (Task 2). |
| T-40-02 | Information disclosure (cross-tab bleed, Phase-38 RT2 class) | FactsheetProvider persistence | accept | Plan 40-01 adds NO provider and NO storageKey; it only threads a presentational prop. The persist={false} isolation lives in the existing provider (consumed in Plan 40-02). No new surface here. |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | This plan installs NOTHING (pure in-repo TSX + test). No package-manager surface; legitimacy gate N/A. |
</threat_model>

<verification>
- `npx tsc --noEmit` clean for the two touched source files.
- `npx eslint` clean (the `void scenarioMode;` seam passes no-unused-vars).
- `npx vitest run "src/app/factsheet/[id]/v2/FactsheetBody.scenario-mode.test.tsx" --no-file-parallelism` green.
- `git diff --name-only` touches only FactsheetView.tsx, MetricsColumn.tsx, and the new test file — NO page.tsx / discovery / AllocationDashboardV2 (byte-identity preserved).
</verification>

<success_criteria>
- scenarioMode?: boolean exists on FactsheetBodyOptions (default false) and threads to ControlBar + MetricsColumn.
- scenarioMode={true} suppresses exactly the Share-link + Compare-strategies ControlBar actions; everything else unchanged.
- MetricsColumn accepts scenarioMode as an inert Phase-42 seam (void scenarioMode; lint-clean).
- BODY-02 prop-equivalence + suppression test green.
- Three byte-identity call sites untouched.
</success_criteria>

<output>
Create `.planning/phases/40-mount-the-real-factsheet-body/40-01-SUMMARY.md` when done.
</output>
