# Milestone v1.9.1 — Composite Onboarding Hardening (ARCHIVED)

**Shipped:** 2026-07-12 · **Version:** 0.41.1.0 · **Tag:** `v1.9.1` · **Merge:** `be215b15` (PR #611)
**Range:** `v1.9`..`be215b15` · **79 files, +12,368 / −1,596** · Phases 92–97

**Goal:** Make the multi-key composite onboarding flow correct and resilient end-to-end,
driven by live SC-3 dogfooding failures (Zavara 3-key Deribit composite, 2026-07-11):
fix the factsheet analytics blow-up, make the wizard a persistent freely-navigable
stepper, give honest live stitch progress, and close the surrounding correctness/infra gaps.

## Phases
- **92 — composite-metric-blow-up / annualization-honesty:** `pnl_dominated_guard` (nav_twr.py) clamps degenerate inverse-Deribit days (`|r| ≥ 10`, strict no-op below); `insufficient_window` DQ flag stamped at the CAGR site (value untouched); shared `NAV_TWR_GUARD_KEYS` registry.
- **93 — composite-data-path-correctness:** first-key data window persisted; `cumulative_method` persisted in `data_quality_flags` at stitch time (read-path prefers persisted over live re-derive); multi-venue (Bybit/OKX/Binance) composites reconstruct honestly or degrade with an explicit DQ reason.
- **94 — wizard-resumability:** owner-scoped `composite/members` GET + `set-members` (secretless); draft rehydration; non-destructive "Review your keys"; clickable/keyboard stepper; cached-snapshot back-nav with no re-kick of a COMPLETE stitch; composite factsheet KPIs.
- **95 — stitch-progress-transparency:** secretless owner-scoped `sync-progress` route; fenced per-member progress (`set_compute_job_progress` SECDEF RPC + worker writes); `useStrategySyncPoller` unifies wizard + key-manager poll loops; 12-min stall backstop; interrupted banner; idempotent retry.
- **96 — draft-key-hygiene-onboarding-polish:** daily `cleanup_abandoned_wizard_drafts` SECDEF cron (7-day draft delete + scoped orphaned-api_keys sweep, published-guard + sanitize_user GUC respected); wizard `X-Correlation-Id`; Deribit `DRB` badge.
- **97 — composite-ci-schema-debt:** live compute-job claim scoping (`want_job_id`, required kwarg) + offline decoy-foreign-row regression; audit-coverage/fanout instrumentation; 3 owed SQL-fn snapshots regenerated. **`-n auto` parallelization DEFERRED** — global-claim fence tests flake under xdist ordering; `python` job kept serial (matches main, green).

## Requirement outcomes (20 mapped)
- **HARD-01..05** ✅ (analytics blow-up, first-key window, cumulative_method persist, insufficient_window flag, multi-venue honesty).
- **WIZ-01..05** ✅ (secretless members read, rehydration, non-destructive review, free stepper nav, cached snapshot).
- **PROG-01..03** ✅ (user-facing copy, per-key progress, stall visibility within the patience window).
- **CLEAN-01/02** ✅ (atomic 7d draft delete + orphaned-key sweep).
- **UX-01/02/03** ✅ (DRB badge, X-Correlation-Id, shared poller hook).
- **CI-02** ✅ (audit instrumentation + owed snapshots).
- **CI-01** ⚠️ **PARTIAL** — the claim-scoping root-cause + offline decoy regression landed and are correct; the `-n auto --dist loadgroup` parallelization (absorbing #610) is DEFERRED because the global-claim fence tests flake under xdist ordering. `python` CI reverted to serial (green). Re-enable needs per-test claim-space isolation (see TODOS.md Phase-97 deferral). #610 closed as absorbed.

## Ship review trail
Per-phase: researcher → planner → plan-checker → executor → goal-backward verifier → concern-matched specialists → fable red team (each fix RED→GREEN).
Whole-diff ship: frontend cross-phase + analytics SC-4 parity + RLS/migrations specialists → 1 HIGH fixed (retry patience-clock banner seam) → whole-diff fable red team → 2 fixed (set-members draft gate MEDIUM; fence-boolean contract LOW) → migration re-clearance CLEAN. Post-merge: mypy narrowing + test-DB catch-up + serial revert to reach green.

## Deploy verification (2026-07-12)
- 3 migrations auto-applied to prod (`khslejtfbuezsmvmtsdn`): both new functions present, draft gate active, ACLs revoked.
- Railway analytics worker deployed on merge sha `be215b15`, `/health` ok + actively ticking.
- Vercel frontend (quantalyze.xyz) HTTP 200.

## Carried-forward known-limitations (TODOS.md)
1. In-flight `computing` stitch stale-attest window — backstopped by finalize `after()` re-enqueue.
2. D-1 cleanup check-then-delete READ COMMITTED race (ms-window, once-daily, recoverable, pre-existing).
3. Same-user `draft→published` TOCTOU on set-members (caller's own rows only).
4. Phase-97 `-n auto` parallelization deferral (per-test claim-space isolation needed).

## Open user gate (unchanged from v1.9)
**SC-3 piece 3** — the one-shot LIVE GUI-onboarded Zavara session (real Deribit keys + running worker + `USE_COMPUTE_JOBS_QUEUE=true`) remains a user smoke test. NOT fully GUI-live-attested until it runs.
