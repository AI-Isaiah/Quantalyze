---
phase: 143-job-dropped-enqueue-reconciliation-sweep
plan: 04
type: execute
wave: 3
depends_on: [143-03]
files_modified:
  - TODOS.md
  - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md
autonomous: false
requirements: [JOB-04]
tags: [supabase-mcp, test-db, pg_cron, railway, sentry, human-verify]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

user_setup:
  - service: sentry
    why: "SC#1's alert is a silent no-op unless the WORKER Railway service (not the FastAPI app) has a DSN"
    env_vars:
      - name: SENTRY_DSN
        source: "Sentry -> Settings -> Projects -> [analytics worker project] -> Client Keys (DSN); set on the Railway WORKER service"

must_haves:
  truths:
    - "The migration is applied to TEST (qmnijlgmdhviwzwfyzlc) BEFORE merge, via Supabase MCP — never `supabase db push`, never merge-first (merging supabase/migrations/** to main AUTO-APPLIES to PROD khslejtfbuezsmvmtsdn)"
    - "The sql-tests CI gate went RED on Part 1 pre-apply and GREEN post-apply — the SQL gate's designed TDD RED, observed"
    - "A REAL cron tick on TEST healed a seeded orphan: cron.job_run_details shows the run, and the compute_jobs row with the reconcile-sweep marker EXISTS — the ONLY possible proof that the cron role can write compute_jobs under FORCE ROW LEVEL SECURITY (RESEARCH L-2/A2; no CI gate can prove this because sql-tests runs as the psql user, never the cron role)"
    - "SENTRY_DSN presence on the WORKER Railway service is verified, or its absence is recorded honestly as a founder action item (never asserted)"
    - "Both non-coverages (composite; wizard first-hop) are on record in TODOS.md (D-05, D-09)"
  artifacts:
    - "143-CENSUS.md part B appended: cron role BYPASSRLS verdict (census query 5) + slot check (query 6) + the observed tick evidence"
    - "TODOS.md — two new items under a Phase 143 deferrals heading"
  key_links:
    - "cron.job_run_details tick evidence <-> T-143-02 (the FORCE-RLS write question) — the merge gate"
    - "Railway worker SENTRY_DSN <-> SC#1 'a Sentry alert fires' being true in production (D-11)"
---

<objective>
Discharge the evidence no local run or CI gate can produce: apply the migration to TEST via the
Supabase MCP, flip the SQL gate RED->GREEN in CI, observe ONE real cron tick heal a seeded orphan
(the only proof of the cron role's RLS posture on `compute_jobs` — threat T-143-02), verify the
worker's Railway SENTRY_DSN, and file the two documented non-coverages. This is the [BLOCKING]
pre-merge sequence — nothing merges to main before it completes, because merge auto-applies to PROD.

Purpose: RESEARCH A2 is HIGH-risk-if-wrong: if the cron role cannot write compute_jobs under FORCE
RLS, the sweep silently inserts zero rows forever while every CI gate stays green.
Output: applied-to-TEST migration, RED->GREEN CI evidence, live-tick evidence, DSN verdict, TODOS
entries, human approval to proceed to ship.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CONTEXT.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-02-SUMMARY.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-03-SUMMARY.md
</context>

## Decision map (cited as D-NN below; source: 143-CONTEXT.md locked decisions)

| ID | Locked decision honored here |
|----|------------------------------|
| D-05 | Wizard first-hop non-coverage -> TODOS.md entry (Task 3) |
| D-09 | Composite non-coverage -> TODOS.md entry (Task 3) |
| D-11 | SENTRY_DSN on the WORKER Railway service verified or honestly escalated (Task 3) |
| D-12 | The header's cron.job_run_details inspection query is exercised for real; what return_message ACTUALLY carries on a live tick is RECORDED (the locked RAISE-NOTICE premise is verified empirically, not assumed — if the NOTICE text does not surface in return_message, record the observed truth and correct the header's operator-observability wording in this plan, never leave prose claiming an unobserved behavior) |
| D-14 | The apply-to-TEST + gate + tick sequence is the merge gate |

## Artifacts this phase produces

Same full inventory as 143-01-PLAN.md — this plan delivers `143-CENSUS.md` part B (cron role +
slot evidence + tick evidence), the two TODOS.md entries, and the pre-merge evidence bundle.

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Apply to TEST via Supabase MCP + census part B + CI RED->GREEN</name>
  <files>.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md</files>
  <precondition>Supabase MCP tools (apply_migration, execute_sql) are available in THIS session (they are stripped from subagents — if absent, this plan must run in the orchestrator session; do not substitute `supabase db push`, which is forbidden for this repo)</precondition>
  <reversibility rating="costly">Applying to TEST is undoable (cron.unschedule + delete the migration ledger row) but pollutes the shared-TEST migration history, and MCP apply_migration stamps now(), creating a KNOWN filename-vs-applied-timestamp drift (the PR-Y2 rename class) — record the drift, do not fight it. The true ONE-WAY door (merge -> PROD auto-apply) is NOT taken in this phase: it happens at /ship after phase verification, and the founder's 2026-08-14 "land them unattended" call (ROADMAP v1.19 header, line 77) is the standing decision covering it — no fresh checkpoint:decision is required, but the census STOP rule and this plan's tick evidence are its guard rails.</reversibility>
  <read_first>
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md "## Environment Availability" (census queries 5 and 6 — cron-role BYPASSRLS + slot check; these need cron.job/pg_roles and are NOT reachable over PostgREST, which is why they live here with MCP and not in Plan 02)
    - supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql (the file to apply — verbatim, from the committed tree)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-03-SUMMARY.md (confirm the SQL gate landed and its Part 1 is the ungated structural part)
  </read_first>
  <action>
    (1) PRE-APPLY RED: confirm the PR branch's latest sql-tests CI run FAILED on
    test_reconcile_dropped_enqueue_sweep.sql Part 1 (cron job missing) — capture the run URL + failing
    output. This is the SQL gate's designed TDD RED (Plan 03 staged it); if sql-tests silently skipped
    (E2E_TEST_DB_CONFIGURED unset), STOP and escalate — a skipped gate is not evidence.
    (2) APPLY: Supabase MCP apply_migration against TEST project qmnijlgmdhviwzwfyzlc with the
    migration file's exact content. Record the applied-migration name the MCP stamped (it stamps
    now() — the drift from the repo filename is expected and recorded, never "fixed" by renaming the
    repo file).
    (3) POST-APPLY VERIFY via MCP execute_sql (read-only): the cron.job row for
    reconcile_dropped_enqueue_sweep exists with schedule 35 * * * * and active; census query 5
    (SELECT jobname, username, rolbypassrls, rolsuper FROM cron.job JOIN pg_roles ...) — the L-2
    verdict on paper; census query 6 (all jobids/jobnames/schedules) — the slot check confirming no
    collision. Append all three results to 143-CENSUS.md as part B (counts and role flags only — no
    connection strings, no keys; the repo is PUBLIC).
    (4) POST-APPLY GREEN: re-run the PR's CI (empty commit or re-run button) and confirm sql-tests is
    now GREEN with the new gate's Parts 1-4 all passing NOTICEs. Capture the run URL.
  </action>
  <verify>
    <automated>grep -A3 "part B" .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md | head -10 && gh run list --branch "$(git branch --show-current)" --workflow ci.yml --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
  </verify>
  <acceptance_criteria>
    - RED evidence captured (run URL + Part 1 failure text) BEFORE apply; GREEN evidence captured
      after; both pasted into the SUMMARY.
    - cron.job row confirmed on TEST: jobname reconcile_dropped_enqueue_sweep, schedule string
      35 * * * *, exactly one row.
    - Census part B in 143-CENSUS.md: the cron-run role's rolbypassrls/rolsuper flags recorded verbatim
      (the paper half of the L-2 verdict — Task 2 is the empirical half), plus the full slot table.
    - The MCP timestamp drift is recorded; the repo filename was NOT renamed.
    - Zero secret material added to any committed file.
  </acceptance_criteria>
  <done>Migration live on TEST; SQL gate RED->GREEN observed in CI; role + slot evidence on record.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Observe one REAL tick heal a seeded orphan on TEST (the L-2 proof)</name>
  <files>.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md</files>
  <precondition>Task 1 complete (migration applied to TEST; cron job active). Wall-clock: the job ticks at minute 35 of each hour — this task spans up to ~65 minutes of waiting; do not simulate the tick, and do not substitute an EXECUTE-as-psql-user run (that is exactly the proof gap this task exists to close)</precondition>
  <read_first>
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md "### L-2" (why only a real tick proves the cron role can write compute_jobs under FORCE RLS: sql-tests runs as the psql user; A2 risk = silent zero-insert forever with all gates green)
    - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql (the FK seed chain shape to reuse for the committed seed — auth.users -> profiles -> strategies -> csv_daily_returns with a century-backdated created_at)
    - supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql (the header's operator inspection query to run verbatim)
  </read_first>
  <action>
    Via Supabase MCP execute_sql on TEST, all writes clearly named and cleaned up afterward:
    (1) SEED (committed — the cron session must see it, so no ROLLBACK): one auth.users row with an
    @invalid.local email prefixed reconcile-143-live-, its profiles row, one strategies row named
    reconcile-143-live-probe, one csv_daily_returns row with created_at backdated 100 years. No
    compute_jobs row, no strategy_analytics row — the arm-A orphan shape.
    (2) WAIT for the next minute-35 tick (up to ~1h; poll after :36).
    (3) INSPECT: run the header's cron.job_run_details query verbatim — record start_time, status,
    and return_message VERBATIM (this is also the D-12 empirical check: record what return_message
    ACTUALLY carries for this DO-block body; if the healed-count NOTICE text is not surfaced there,
    correct the header's operator-observability wording in a follow-up commit in THIS plan and record
    the finding — never leave the header claiming an unobserved behavior). Then SELECT the
    compute_jobs row for the seeded strategy: it must exist with status pending, kind
    compute_analytics_from_csv, metadata source reconcile-sweep.
    (4) VERDICT: row present = T-143-02/A2 RESOLVED (the cron role CAN write compute_jobs under FORCE
    RLS) — append the evidence to 143-CENSUS.md part B. Row ABSENT with a succeeded run = L-2
    MATERIALIZED: STOP, do NOT merge, escalate to the orchestrator with the run details (the fix
    direction is a role/grant change that needs its own review — out of this phase's authority).
    (5) CLEAN UP (after evidence capture): delete the seeded compute_jobs row, csv_daily_returns row,
    strategies row, profiles row, auth.users row — verify zero residue by re-selecting each by the
    probe name/email. The shared TEST project must not keep the probe (CI-pollution hazard).
  </action>
  <verify>
    <automated>grep -c "tick" .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - cron.job_run_details evidence recorded verbatim (start_time, status, return_message) for the
      observed tick.
    - The healed compute_jobs row's (strategy_id, kind, status, metadata) captured BEFORE cleanup —
      the empirical L-2/T-143-02 resolution, stated as such in 143-CENSUS.md.
    - The D-12 return_message finding recorded either way (NOTICE surfaced / not surfaced), and the
      migration header wording matches the OBSERVED behavior at the end of this task.
    - Cleanup verified: zero rows remain for the probe identifiers across all five tables.
    - If the row did not appear: the plan STOPPED before any merge and the escalation is on record —
      this acceptance criterion is satisfied by the STOP, not by proceeding.
  </acceptance_criteria>
  <done>One real tick observed end-to-end on TEST; the FORCE-RLS write question answered empirically; probe cleaned up.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Worker SENTRY_DSN verdict + TODOS entries + pre-merge evidence review</name>
  <what-built>
    The full Phase 143 delivery: sweep migration applied to TEST with a live-tick heal observed
    (Tasks 1-2), worker-side Sentry alert path (Plan 01), three CI gates with nine observed neuter
    REDs (Plan 03), census on both projects (Plan 02). Before this checkpoint the executor also:
    (a) attempts the SENTRY_DSN check on the WORKER Railway service via the Railway MCP/CLI
    (list variables for the analytics worker service — the service running python -m main_worker,
    NOT the FastAPI app); if tooling cannot read it, records it as UNVERIFIED with a founder action
    item in TODOS.md (the RESEND_API_KEY precedent: computed correctly, pages nobody) — never asserts
    it; (b) appends two items to TODOS.md under a "Phase 143 — recorded deferrals" heading:
    (i) composite strategies stranded without analytics are EXCLUDED from the sweep — healing them
    needs a stitch_composite re-run mechanism with its own predicate (D-09); (ii) the wizard/API
    first-hop enqueue drop (finalize-wizard -> sync_trades never enqueued; no dailies, no jobs) is
    NOT covered — needs its own signal and false-positive analysis (D-05); (c) confirms the migration
    file is byte-identical to what Plan 02's review agents approved (no post-review edits — if edits
    happened, re-run migration-reviewer + rls-policy-auditor and record).
  </what-built>
  <how-to-verify>
    Review the evidence bundle in the SUMMARY + 143-CENSUS.md:
    1. CI sql-tests RED (pre-apply) and GREEN (post-apply) run URLs.
    2. The live-tick evidence: cron.job_run_details row + the healed compute_jobs row with the
       reconcile-sweep marker, and the cleanup confirmation.
    3. The cron-role BYPASSRLS flags (census part B) — consistent with the observed write.
    4. The SENTRY_DSN verdict: SET on the worker service, or the recorded founder action item.
    5. TODOS.md carries both deferral items.
    6. The census headline counts — the number PROD will enqueue on the first post-merge tick.
    Approving this checkpoint authorizes proceeding to /ship (PR + merge), which AUTO-APPLIES the
    migration to PROD on merge per the founder's standing 2026-08-14 "land them unattended" call.
    After merge: watch the first PROD tick via the header's inspection query (post-ship step, recorded
    in the SUMMARY as the operator handoff).
  </how-to-verify>
  <resume-signal>Type "approved" to authorize ship, or describe issues (a missing tick row, an unexplained census number, or an unverified DSN you want resolved first)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP session -> TEST database | committed writes on a SHARED project other PRs' CI reads |
| merge -> PROD | supabase/migrations/** on main auto-applies; there is no separate deploy step |
| Railway dashboard/MCP -> worker env | secret presence check; values must never be copied into the repo |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-02 | Tampering | cron role vs FORCE-RLS compute_jobs | high | mitigate | Task 2's real tick is THE resolution; a missing row = hard STOP before merge (the A2 failure mode is silent-zero-forever with green CI) |
| T-143-01 | Elevation of Privilege | callable surface | high | mitigate | Post-apply verify confirms the deployed artifact is an inline body — census part B lists jobs; no function/EXECUTE grant appeared (D-13) |
| T-143-05 | Repudiation | worker Sentry channel | high | mitigate | SENTRY_DSN verified on the WORKER service or escalated as a named founder action (never asserted); the RESEND_API_KEY precedent is the register |
| T-143-07 | Information Disclosure | evidence artifacts in a PUBLIC repo | high | mitigate | Census part B and the SUMMARY carry counts, role flags, timestamps — never DSN values, keys, or connection strings; no-allowlist gitleaks scan before push |
| T-143-13 | Tampering | shared-TEST probe residue | medium | mitigate | Task 2's committed seed is name-spaced (reconcile-143-live-), evidence-captured, then deleted with a verified zero-residue re-select |
| T-143-16 | DoS | PROD first tick after merge | medium | mitigate | The census headline count IS the first-tick enqueue volume; the Plan-02 STOP rule capped it pre-authoring; the checkpoint surfaces it to the human before ship; LIMIT 25 bounds any surprise |
| T-143-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- sql-tests RED->GREEN pair on record with URLs
- Live tick + healed marked row + cleanup on record in 143-CENSUS.md part B
- SENTRY_DSN verdict recorded (verified or escalated, never assumed)
- TODOS.md carries both deferrals; human approval received
</verification>

<success_criteria>
- All three ROADMAP SCs hold with evidence that includes the one fact no CI gate can produce (the
  cron role's write under FORCE RLS, observed on a real tick).
- The phase is merge-ready: merging is the only remaining step, and its PROD consequence (auto-apply,
  first-tick volume = census headline) is understood and human-acknowledged.
</success_criteria>

<output>
Create `.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-04-SUMMARY.md` when done,
including the evidence bundle (run URLs, tick record, DSN verdict) and the operator handoff note
(watch the first PROD tick via the header's inspection query).
</output>
