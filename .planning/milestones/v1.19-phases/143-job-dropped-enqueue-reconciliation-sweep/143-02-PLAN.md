---
phase: 143-job-dropped-enqueue-reconciliation-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql
  - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md
  - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql
autonomous: true
requirements: [JOB-04]
tags: [supabase, migration, pg_cron, plpgsql, job-queue, reconciliation]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "A strategy with csv_daily_returns rows, ZERO compute_jobs rows of any kind/status, no strategy_analytics row at computing/complete/complete_with_warnings/failed, whose MAX(csv_daily_returns.created_at) is older than 1 hour, gets exactly one pending compute_analytics_from_csv job with metadata source='reconcile-sweep' when the deployed cron body runs (SC#1, D-01, D-02, D-06)"
    - "Executing the deployed body twice produces no duplicate job — ON CONFLICT DO NOTHING against the EXISTING compute_jobs_one_inflight_per_kind_strategy partial unique index (SC#2, D-10)"
    - "In-grace, any-job-row, terminal-analytics, computing, composite (strategy_keys member), and archived strategies are NEVER touched (SC#3, D-03, D-04, D-09, DX-06)"
    - "Per-tick heal count is bounded at 25 via a MATERIALIZED batch CTE — the D-19 fence (D-08)"
    - "The census counts for TEST and PROD are on record BEFORE the predicate is finalized, and the first-tick enqueue population is known (CONTEXT Claude's-Discretion census, 142 precedent)"
  artifacts:
    - "supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql — the ONLY migration; inline cron body, no DDL, no function, no policy"
    - ".planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md — TEST+PROD counts + method evidence (no secrets)"
    - ".planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql — stub schema + fake pg_cron for local end-to-end proof, reused by Plan 03"
  key_links:
    - "$cron$ body's jsonb_build_object('source','reconcile-sweep','detected_at',now()) <-> main_worker.py marker read (Plan 01) — the cross-language alert contract"
    - "ON CONFLICT DO NOTHING <-> compute_jobs_one_inflight_per_kind_strategy current def at 20260416125430:156 (NOT the DROPped 20260411144407:179 original)"
    - "terminal-status exclusion list ('computing','complete','complete_with_warnings','failed') <-> the computation_status CHECK at 20260602120000:46 — the mass-re-enqueue tripwire"
    - "NOT EXISTS strategy_keys <-> composite identification (finalize-wizard route.ts:1363-1372; job_worker.py:5472-5485)"
---

<objective>
Author the ONE migration of this phase: an inline pg_cron sweep (`reconcile_dropped_enqueue_sweep`,
hourly at minute 35) that detects, BY ABSENCE, strategies whose `after()` enqueue never ran — dailies
persisted, zero compute_jobs rows, no terminal analytics — and heals them with a bounded, idempotent
direct INSERT. Census first (the first-tick enqueue count must be looked at before the predicate is
frozen), then the migration, then a throwaway-Postgres end-to-end proof of every directional arm.

Purpose: closes the JOB-04 hole that is architecturally invisible from inside csv-finalize
(route.ts:813 — every guard lives inside the closure that never ran).
Output: census artifact, the migration (header + body + self-verify blocks), the reusable throwaway
harness, and observed end-to-end behavior on a real Postgres.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CONTEXT.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-PATTERNS.md
@.planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-04-SUMMARY.md
</context>

## Decision map (cited as D-NN below; source: 143-CONTEXT.md locked decisions)

| ID | Locked decision |
|----|-----------------|
| D-01 | Dailies anchor = `public.csv_daily_returns`, one table, source-agnostic, no join to strategies.source |
| D-02 | "No compute_jobs row" = ANY kind AND ANY status (kind-scoping would re-enqueue a healthy mid-chain derive_broker_dailies) |
| D-03 | The terminal-`strategy_analytics` conjunct is the ONLY protection for healthy 31-day-old strategies (retention deletes done rows at 30d) — say so in the header, pin with a test |
| D-04 | Non-racing split with 142's reaper by computation_status: absent/pending -> this sweep; computing -> 142 only; complete/cww/failed -> nobody |
| D-05 | Wizard first-hop drop OUT OF SCOPE — documented non-coverage in header, SUMMARY, TODOS.md |
| D-06 | Grace window = 1 hour, with the derivation stated in the header (route-commit -> after() gap is sub-second; 16h explicitly rejected; NTP-bounded skew) |
| D-07 | Cadence hourly; header states post-threshold detection latency honestly (worst case ~grace + 1h); never claim it bounds live-wizard spinner time (poller self-escalates at 15 min) |
| D-08 | Bounded per run via MATERIALIZED-CTE LIMIT, reusing 20260803130000's shape (the D-19 fence) |
| D-09 | Re-enqueued kind = `compute_analytics_from_csv` — EXCEPT composites, which are EXCLUDED (2026-08-16 correction: stitch_composite is chain-terminal; enqueueing the csv kind would overwrite a correct composite headline with divergent single-key math — silent money corruption) |
| D-10 | Idempotency rides the EXISTING partial unique index (current def 20260416125430:156); no new index; twice = provable no-op |
| D-11 | Alert = worker-side on claim via metadata marker (Plan 01 owns the Python half; this plan writes the SQL half with keys `source`/`detected_at` EXACTLY) |
| D-12 | pg_cron run log is the secondary surface: RAISE NOTICE the healed count; inspection query verbatim in the header; every RAISE format string a single literal |
| D-13 | NO new callable SQL surface — INLINE cron body, no SECURITY DEFINER, no EXECUTE grant, no caller-suppliable interval (the 20260516170100 incident class: "the parameter IS the attack surface") |
| D-14 | CI-visible gates only; every gate observed RED when neutered |

Claude's-discretion choices made in this plan (each with stated rationale, per CONTEXT "Claude's Discretion"):

| ID | Choice | Rationale |
|----|--------|-----------|
| DX-01 | Filename `20260816140000_reconcile_dropped_enqueue_sweep.sql` | > repo tip `20260814120000` (migration-policy.yml requires newer-than-remote-tip); Task 1 re-checks the tip and bumps if a newer migration landed, moving the Plan-03 gate constants in the SAME commit (RESEARCH P-7) |
| DX-02 | Jobname `reconcile_dropped_enqueue_sweep`; cadence `35 * * * *` | No `reconcile*` name taken (RESEARCH §8). Hourly preserved per D-07; minute 35 clears every registered slot (`:00`,`:10`,`:15`,`:20`,`:30`) and the 142 reaper's `*/15` grid — `0 * * * *` would coincide with the reaper hourly and with audit_log_hot_to_cold at hour 3 (RESEARCH L-5/Q5) |
| DX-03 | LIMIT 25 | Matches 142's per-tick bound; drains 25/hour; PROD candidate count expected single-digit (39 total analytics rows at 2026-08-02); Task 1's census sanity-checks this before authoring — a TEST count above the escalation threshold revisits it |
| DX-04 | Grace anchor = `MAX(csv_daily_returns.created_at)` per candidate | The L-4 four-way rejection: `updated_at` is re-stamped on every refresh (the Phase-106 shape); `strategies.created_at` bears no relation to when dailies landed (instant false positives on old strategies with fresh CSVs); `strategy_analytics.computed_at` is the literal Phase-106 revert cause and absent in the row-ABSENT arm; `compute_jobs.created_at` impossible (predicate requires zero rows). `created_at` is NOT NULL DEFAULT now(), never re-stamped (persist/derive upserts touch updated_at), and directly INSERT-writable so the SQL gate can backdate it. NO new index (20260522111839:31-34 records one dropped for doubling write I/O) — drive from strategies, grace conjunct LAST |
| DX-05 | Composite exclusion predicate = `NOT EXISTS (SELECT 1 FROM public.strategy_keys sk WHERE sk.strategy_id = s.id)` | Pinned at source this session: finalize-wizard route.ts:1363-1372 ("a strategy with >=1 strategy_keys member ALWAYS enqueues stitch_composite"; composite <-> single-key mutually exclusive by construction) and run_stitch_composite_job loads members from strategy_keys and FAILS on zero members (job_worker.py:5472-5485). ⛔ `strategies.api_key_id IS NULL` is NOT a valid discriminator — CSV single-key strategies also have it NULL. Exclusion errs safe: a mis-tagged row is left un-healed (visible), never money-corrupted |
| DX-06 | strategies.status gate: exclude `'archived'` ONLY; `'draft'` INCLUDED | L-6: a drop victim may sit at a pre-terminal status precisely because nothing advanced it — excluding draft could excise the exact healed population. Archived exclusion is hygiene (wasted worker slots), stated in the header so the absence does not read as oversight. Census breakdown (Task 1) goes in the header as the evidence |

## Artifacts this phase produces

Same full inventory as 143-01-PLAN.md ("Artifacts this phase produces") — this plan delivers the
migration `20260816140000_reconcile_dropped_enqueue_sweep.sql`, the cron job
`reconcile_dropped_enqueue_sweep` (`35 * * * *`, LIMIT 25, grace `interval '1 hour'`), the metadata
marker `{"source": "reconcile-sweep", "detected_at": <timestamptz>}`, `143-CENSUS.md` (part A), and
`143-throwaway-harness.sql`.

<tasks>

<task type="auto">
  <name>Task 1: Pre-authoring verification + read-only census (TEST + PROD)</name>
  <files>.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md</files>
  <precondition>Service-role PostgREST credentials exist locally (`.env.test.local` for TEST, `.env.local` for PROD, as used by 142-04) OR Supabase MCP execute_sql is available in this session. If NEITHER exists, STOP and escalate — the census is a hard prerequisite, not skippable.</precondition>
  <read_first>
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md "## Environment Availability" (the six ready-to-run census queries and the STOP rule) and "### §1" (index re-base rule)
    - .planning/phases/142-job-strategy-analytics-stuck-computing-reaper-computing-star/142-04-SUMMARY.md (the PostgREST-substitution precedent: read-only GETs, content-range evidence, keys never printed, substitution recorded loudly)
    - supabase/migrations/20260416125430_contact_request_metadata.sql:140-180 (current partial-unique-index definition to re-confirm as latest)
    - .github/workflows/migration-policy.yml:15-19,196-272 (newer-than-tip rule), .github/workflows/migration-drift-check.yml and .github/workflows/sql-function-snapshot.yml (RESEARCH A7 — UNREAD at research time; read both and record whether a cron-only migration triggers either)
  </read_first>
  <action>
    Five verifications, all read-only, results written to 143-CENSUS.md:
    (1) RE-BASE FRESHNESS (the standing grep-ALL-migrations rule): grep every file in
    supabase/migrations/ for compute_jobs_one_inflight_per_kind_strategy and confirm no definition
    newer than 20260416125430:156 exists; same for the compute_jobs kind/target coherence CHECK
    (latest must remain 20260717233529:168-181) and the computation_status CHECK (latest must remain
    20260602120000:46). Any newer definition = STOP, re-derive before authoring.
    (2) MIGRATION TIP: `ls supabase/migrations/ | tail` — if any file sorts >= 20260816140000, bump
    DX-01's timestamp above it and record the new name (Plan 03's gate constants must then use it).
    (3) CI WORKFLOW CHECK (RESEARCH A7): read migration-drift-check.yml and sql-function-snapshot.yml;
    record in 143-CENSUS.md whether a migration that creates no function and no DDL triggers either
    (expected: sql-function-snapshot is a no-op — verify, do not assume).
    (4) CENSUS PART A on TEST (qmnijlgmdhviwzwfyzlc) and PROD (khslejtfbuezsmvmtsdn), read-only.
    Primary method: Supabase MCP execute_sql with RESEARCH's queries (1)-(4). Sanctioned fallback
    (142-04 precedent, record the substitution loudly): PostgREST service-role reads computing the
    predicate client-side — fetch strategies ids, strategy_analytics (strategy_id, computation_status),
    compute_jobs strategy_ids (service-role bypasses the deny-all RLS at role level), subtract to get
    survivors, then per-survivor existence probes on csv_daily_returns (strategy_id=eq.X, limit 1) and
    strategy_keys (composite exposure). Report: headline candidate count per project; breakdown by
    strategies.status (answers DX-06's census check); breakdown analytics-ABSENT vs 'pending';
    composite count among candidates. Paste count evidence (content-range headers or MCP row output) —
    NEVER a key, token, or connection string: the repo is PUBLIC and .planning/ is TRACKED.
    (5) STOP RULE: if PROD headline count > 10, or TEST > 200, STOP and escalate to the orchestrator
    before Task 2 runs — the headline count is what the sweep enqueues on its first tick after merge
    (PROD) and what it feeds the workerless TEST backlog (TEST: sweep-inserted pending rows are never
    drained and JOB-08 confirms nothing sweeps stale pending — a large count is a CI-flake feeder).
  </action>
  <verify>
    <automated>test -s .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md && grep -c "TEST\|PROD" .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - 143-CENSUS.md exists with: both projects' headline candidate counts, the status breakdown, the
      analytics-absent-vs-pending breakdown, the composite exposure count, the index/CHECK freshness
      verdicts with file:line, the migration-tip verdict, and the two CI-workflow verdicts.
    - `grep -riE "service_role|eyJ|postgres://|postgresql://" 143-CENSUS.md` returns zero matches (no
      secret material; counts and headers only).
    - The method used (MCP vs PostgREST substitution) is stated explicitly, 142-04 style.
    - If any STOP rule fired, the plan halted BEFORE Task 2 and the escalation is recorded.
  </acceptance_criteria>
  <done>Census on record for both projects; all re-base targets confirmed fresh; STOP rules evaluated; zero secrets in the artifact.</done>
</task>

<task type="tracer">
  <name>Task 2: Author the migration and prove the single happy path end-to-end on a throwaway Postgres</name>
  <files>supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql, .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql</files>
  <precondition>A local Postgres 16 toolchain exists (`/opt/homebrew/opt/postgresql@16/bin/psql` and initdb — present per 142-04). If absent, STOP and escalate; do not skip the smoke.</precondition>
  <read_first>
    - supabase/migrations/20260802120000_strategy_analytics_stuck_computing_reaper.sql (FULL FILE — the structural template: header section order :1-207, BEGIN/SET lock_timeout framing :209-210, fail-loud pg_cron DO block :434-450, clause-by-clause body commentary :452-500, cron.schedule + $cron$ :501-528, self-verify blocks :634-688)
    - supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql (FULL FILE — the MATERIALIZED fence, the three do-not-simplify warnings :64-73, the single-job + MATERIALIZED-count + IN-SELECT-LIMIT negative anchors :172-190)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-PATTERNS.md (the 13-section header map with 142's line ranges; the silent-skip ELSE arm at 20260717233529:288 that must NOT be copied; the No-Analog disclosure for the INSERT arm)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md "### Sweep body sketch" (the SHAPE — every literal re-derived here per this plan's decision map) and "### §6" (minimal INSERT column set: strategy_id, kind, metadata; all else defaulted) and "### §2" (why the enqueue RPC must not be called: its race-loss arm raises serialization_failure and aborts the whole tick)
    - supabase/migrations/20260416125430_contact_request_metadata.sql:154-161 (the partial unique index the INSERT conflicts against — copy its predicate into the harness verbatim)
    - supabase/migrations/20260717233529_allocator_equity_derived_surface.sql:168-181 (the kind/target coherence CHECK the INSERT must satisfy — copy into the harness verbatim)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-CENSUS.md (Task 1 output — numbers for the CENSUS header section; the DX-06 status-breakdown evidence)
  </read_first>
  <action>
    TWO deliverables.
    (A) THE MIGRATION — one file, NO DDL, NO function, NO policy change (D-13). Structure:
    - Header: reproduce 142's 13 sections IN ORDER (PATTERNS map). Content per section: the after()
      hole and why no in-request guard can see it (route.ts:813); WHERE THE VALUE LANDS (page-refresh /
      factsheet path, NOT a live-wizard rescue — the poller self-escalates at 15 min, D-07); CADENCE
      HONESTY (hourly at :35 = post-threshold detection latency, worst case ~grace + 1h, D-07 + DX-02
      collision rationale); Threshold rationale (the D-06 1-hour derivation, explicit rejection of 16h
      — that figure bounds computing-with-chain-in-flight, and this predicate requires ZERO job rows so
      no chain can be in flight by construction); SAFETY vs DEBOUNCE (the SAFETY carrier here is the
      terminal-analytics conjunct because retention_compute_jobs_done deletes done rows at 30 days —
      current body 20260515113853:192-200, NOT the stale :88 header table — so every healthy 31-day-old
      strategy matches dailies-present + zero-jobs; weakening or reordering that conjunct re-opens a
      mass-re-enqueue incident, D-03); CLOCK SKEW; Grace anchor rationale (DX-04's full four-way
      rejection, Phase-106 register: name each rejected column and say HOW it is wrong); CENSUS AT
      AUTHORING TIME (Task 1's real numbers, both projects, method named — plus the TEST note that
      sweep-enqueued pending rows are never drained there because TEST has no worker, expected and
      bounded at one row per candidate ever); Scope discipline (ONE cron job; does NOT touch
      compute_jobs DDL/RLS, the claim RPC, retention crons — Phase 144 owns those — or 142's reaper;
      the two known non-coverages: wizard first-hop drop D-05 and composites D-09, each with the reason
      and the TODOS.md pointer); Idempotency (unschedule-then-schedule re-apply; D-10 index citation
      with the re-base note that 20260411144407:179 was DROPped); Convention deviation (migration-
      reviewer invariant #14 BEGIN/COMMIT known-stale, 142's pre-documentation register; also state
      that invariants #3/#12/#13/#17/#19/#20 are vacuously satisfied because no function and no policy
      is created — say so, don't leave the reviewer to infer it); Operator observability (the
      cron.job_run_details inspection query verbatim with jobname reconcile_dropped_enqueue_sweep,
      D-12); PROD-AUTO-APPLY WARNING (merge to main auto-applies to PROD khslejtfbuezsmvmtsdn; apply
      to TEST qmnijlgmdhviwzwfyzlc + run the gate first; founder call 2026-08-14 "land them unattended"
      is on record in ROADMAP v1.19 — cite it).
      Also reproduce, adapted: the no-in-repo-precedent disclosure — no pg_cron body in this repo
      INSERTs into compute_jobs directly (PATTERNS "No Analog Found" measured it), so the INSERT arm is
      written from first principles and deserves extra review weight (142's :485-487 register). And the
      prose-hygiene note: the header discusses rejected anchor columns; every mechanical gate scoping
      to the $cron$ body only is what keeps that prose from tripping gates — keep every rejected-column
      token OUT of the $cron$ literal.
    - Body framing: BEGIN; SET lock_timeout = '5s'; ... COMMIT; (142 convention).
    - Fail-loud DO block: pg_cron-absent RAISE EXCEPTION with ERRCODE feature_not_supported, message
      prefixed JOB-04 (NEVER the 20260717233529:288 silent-skip ELSE); idempotent
      cron.unschedule-then-cron.schedule for jobname reconcile_dropped_enqueue_sweep at '35 * * * *'.
    - The $cron$ body: a DO block (distinct inner dollar-tag) containing ONE statement pair —
      WITH batch AS MATERIALIZED (SELECT s.id FROM public.strategies s WHERE s.status <> 'archived'
      AND EXISTS dailies for s.id AND NOT EXISTS any compute_jobs row for s.id (no kind filter, no
      status filter — D-02) AND NOT EXISTS strategy_analytics row at any of the four excluded statuses
      ('computing','complete','complete_with_warnings','failed' — enumerated so a future sixth CHECK
      value defaults to EXCLUDED, the safe direction; absent and 'pending' both pass, D-04) AND NOT
      EXISTS strategy_keys row for s.id (DX-05) AND correlated MAX(csv_daily_returns.created_at) for
      s.id < now() - interval '1 hour' (D-06, LAST conjunct per DX-04 cost note) ORDER BY that same
      correlated MAX ascending LIMIT 25 FOR UPDATE SKIP LOCKED) INSERT INTO public.compute_jobs
      (strategy_id, kind, metadata) SELECT id, 'compute_analytics_from_csv',
      jsonb_build_object('source','reconcile-sweep','detected_at',now()) FROM batch ON CONFLICT DO
      NOTHING; — then GET DIAGNOSTICS the row count and RAISE NOTICE it (single-literal format string
      with a % placeholder, D-12). Every table schema-qualified public.*. Keep the batch CTE a plain
      SELECT FROM strategies with EXISTS/NOT EXISTS subqueries only (FOR UPDATE is illegal on outer
      joins/aggregates — RESEARCH Pattern 2 adaptation note); the correlated MAX stays a scalar
      subquery, not a join. ON CONFLICT carries NO conflict target (a targeted form cannot name a
      partial unique index without repeating its predicate). Never call enqueue_compute_job or any
      RPC from this body. Annotate the body clause-by-clause in the commentary block ABOVE the
      cron.schedule call, 142's :452-500 register.
    - Self-verify DO blocks (adapted from 142 :634-688 + 20260803130000:172-190): job present; schedule
      STRING-equals '35 * * * *' (never an INT cast); exactly ONE cron.job row with this name;
      positive anchors on the deployed command (public.csv_daily_returns, public.compute_jobs,
      public.strategy_analytics, public.strategy_keys, reconcile-sweep, ON CONFLICT DO NOTHING,
      FOR UPDATE SKIP LOCKED, interval literal); MATERIALIZED occurrence count exactly 1; negative
      anchors — the deployed command must NOT match the un-hashable IN-SELECT-LIMIT regex shape, must
      NOT reference either rejected timestamp anchor column, and must NOT contain the enqueue RPC name.
      Every failure message NAMES THE CONSEQUENCE (e.g. the rejected-anchor message names the Phase-106
      revert; the terminal-conjunct-related messages name the mass-re-enqueue incident), 142's register.
    (B) THE HARNESS — 143-throwaway-harness.sql, committed to the phase dir (stub DDL only, zero
    secrets): auth.users + public.profiles + public.strategies (status column + default) +
    public.csv_daily_returns (created_at NOT NULL DEFAULT now()) + public.compute_jobs with the REAL
    status CHECK, priority default, metadata column, the kind/target coherence CHECK copied verbatim
    from 20260717233529:168-181, and the partial unique index copied verbatim from 20260416125430:156-160
    + public.strategy_analytics with the five-value CHECK + public.strategy_keys (minimal: strategy_id,
    api_key_id, seq — no FK to api_keys needed in the harness) + a cron schema stub (cron.job table;
    cron.schedule/cron.unschedule functions that upsert/delete rows) + the faked pg_extension row
    (142-04's approach: superuser catalog insert on the THROWAWAY cluster only). Header comment states
    it is a fidelity-limited local proof harness — no RLS, no triggers, no real pg_cron scheduler —
    and that the real-schema proof is CI sql-tests post-apply plus Plan 04's live tick.
    TRACER VERIFY (the end-to-end happy path): initdb a throwaway cluster in the scratchpad, apply
    harness then migration with psql -v ON_ERROR_STOP=1 (must exit 0, all self-verify NOTICEs firing);
    seed one strategy + one csv_daily_returns row with created_at backdated 100 years; extract the
    deployed command from the stub cron.job and EXECUTE it; assert exactly one compute_jobs row exists
    with status pending, kind compute_analytics_from_csv, and metadata source reconcile-sweep.
  </action>
  <verify>
    <automated>PGBIN=/opt/homebrew/opt/postgresql@16/bin; SCRATCH="${TMPDIR:-/tmp}/143-tracer"; "$PGBIN/psql" -h "$SCRATCH" -d q143 -v ON_ERROR_STOP=1 -c "SELECT count(*) AS healed, min(metadata->>'source') AS marker FROM public.compute_jobs WHERE kind='compute_analytics_from_csv'"</automated>
  </verify>
  <acceptance_criteria>
    - psql apply of harness + migration exits 0; the fail-loud pg_cron gate is ALSO observed firing
      (run once before the faked extension row exists — it must RAISE, not skip; 142-04's Rule-12 proof).
    - The tracer query returns healed=1, marker=reconcile-sweep after one EXECUTE of the deployed body.
    - Self-verify NOTICEs all fired on apply; their text recorded for the SUMMARY.
    - `awk` single-literal RAISE check (142-04's reviewer-#21 check) passes on the migration file.
    - The $cron$ body contains none of: the enqueue RPC name, either rejected timestamp anchor column,
      the IN-SELECT-LIMIT shape — verified by grepping ONLY the extracted body, not the whole file
      (header prose legitimately discusses the rejected columns; grep-gate hygiene).
    - The header contains all 13 section titles in 142's order, the census numbers from Task 1, both
      non-coverage disclosures, the no-precedent disclosure for the INSERT arm, and the invariant-#14
      pre-documentation.
    - Migration filename sorts strictly after the current repo tip (re-checked at commit time).
  </acceptance_criteria>
  <done>The migration exists, applies cleanly, and the single happy path (backdated orphan -> EXECUTE deployed body -> one pending marked job) is proven end-to-end on a real Postgres. Harness committed for Plan 03 reuse.</done>
</task>

<task type="auto">
  <name>Task 3: Expansion smoke — every directional arm + idempotency + bound + review agents</name>
  <files>.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql</files>
  <read_first>
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-RESEARCH.md "### Neutering proof" table (the arm inventory) and "### Pattern 3" (identity-scoped assertions; backdate-never-sleep; frozen-clock trap)
    - .claude/agents/migration-reviewer.md (all 21 invariants; #14 is known-stale and pre-documented in the header) and .claude/agents/rls-policy-auditor.md (triggers on anything touching compute_jobs; its BYPASSRLS section is the L-2 frame)
    - 142-04-SUMMARY.md "Offline apply + behavior smoke" (the observed-arm table format to reproduce)
  </read_first>
  <action>
    On the Task-2 throwaway cluster, drive the DEPLOYED command (always extracted from the stub
    cron.job, never retyped) through every directional arm and record an observed table in 142-04's
    format. Arms, each seeded with a distinct strategy: (A) orphan past grace, analytics ABSENT ->
    healed with marker; (A2) analytics row at pending -> healed (D-04); (B) in-grace (fresh
    created_at) -> untouched; (C1) a running derive_broker_dailies job present -> untouched (the D-02
    ANY-kind proof); (C2) only a failed_final job -> untouched; (C3) only a done job -> untouched;
    (D1-D4) analytics at complete / complete_with_warnings / failed / computing -> untouched (D-03,
    D-04 — computing is 142's reaper's row); (E) strategy_keys member row present -> untouched (D-09/
    DX-05); (F) status archived -> untouched (DX-06). Then: (G) EXECUTE the body twice on arm A ->
    still exactly one job row (SC#2/D-10); (H) seed 26 staggered century-old candidates -> tick 1
    heals exactly the 25 oldest and the youngest is untouched, tick 2 heals the 26th (bounded AND
    progressing, D-08). Then BODY-NEUTER previews (mutate a COPY of the deployed command in the stub
    cron.job, observe, restore from the committed migration, never retype): drop 'complete' from the
    exclusion list -> arm D1 gets healed (the mass-re-enqueue signature); replace ON CONFLICT DO
    NOTHING with a bare INSERT -> second EXECUTE raises unique_violation; remove the MATERIALIZED
    keyword -> tick 1 heals 26 of 26 (the D-19 signature). Record all three observations.
    Finally: run the migration-reviewer agent and the rls-policy-auditor agent on the migration file;
    every finding either fixed or pre-documented in the header (invariant #14 is the only sanctioned
    deviation); record both verdicts in the SUMMARY. If either agent demands a change, re-run the
    Task-2 tracer verify after editing.
  </action>
  <verify>
    <automated>PGBIN=/opt/homebrew/opt/postgresql@16/bin; SCRATCH="${TMPDIR:-/tmp}/143-tracer"; "$PGBIN/psql" -h "$SCRATCH" -d q143 -v ON_ERROR_STOP=1 -c "SELECT kind, status, count(*) FROM public.compute_jobs GROUP BY 1,2 ORDER BY 1,2"</automated>
  </verify>
  <acceptance_criteria>
    - The observed-arm table shows all 13 arms (A, A2, B, C1-C3, D1-D4, E, F + G idempotency + H bound)
      with expected == observed for every row; untouched arms verified by zero compute_jobs rows for
      those strategy ids (identity-scoped, never global counts).
    - All three body-neuter previews observed with the predicted signature, then the deployed command
      restored byte-identical to the committed migration (diff against the file's $cron$ extraction = empty).
    - migration-reviewer verdict recorded: zero unaddressed findings outside the pre-documented #14.
    - rls-policy-auditor verdict recorded, including its read of the L-2 posture (the cron role's
      BYPASSRLS question is answered empirically only by Plan 04's live tick — the auditor's output
      must not be read as that proof).
    - `grep -rn MUTANT` over the repo returns 0; git status clean except the intended files.
  </acceptance_criteria>
  <done>Every SC#1/SC#2/SC#3 behavior observed on a real Postgres against the deployed body; three neuter signatures observed; both review agents run and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pg_cron body (postgres role) -> tenant tables | cross-tenant scheduled writes with elevated privilege; the predicate is the only tenant gate |
| repo (PUBLIC) -> migration/census artifacts | .planning/ is tracked; anything written here is world-readable |

## STRIDE Threat Register (ASVS L1 — block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-143-01 | Elevation of Privilege | callable SQL surface | high | mitigate | NO function created — INLINE cron body, fixed literal interval, no EXECUTE grant (D-13; the 20260516170100 class). Self-verify + Plan-03 TS gate assert no CREATE FUNCTION and no dynamic SQL in the migration |
| T-143-02 | Tampering | cron role vs FORCE-RLS deny-all compute_jobs | high | mitigate | The INSERT either lands or silently inserts zero forever (RESEARCH A2 — every CI gate runs as the psql user and CANNOT prove the cron role's posture). Mitigation = evidence, not assumption: census query 5 (rolbypassrls) + Plan 04's REAL TEST tick inspected in cron.job_run_details, gated blocking before merge |
| T-143-03 | DoS / Tampering | terminal-analytics conjunct | high | mitigate | Mass re-enqueue of the historical corpus if weakened/reordered (D-03: retention deletes done rows at 30d). Enumerated four-status exclusion (future statuses default excluded); Task-3 neuter preview observes the signature; Plan-03 SQL gate arm's failure message names the incident; LIMIT 25 caps per-tick blast radius; Task-1 census STOP rule caps first-tick exposure |
| T-143-04 | Tampering | composite money surface | high | mitigate | NOT EXISTS strategy_keys conjunct (DX-05, pinned at source); Task-3 arm E observes untouched; enqueueing the csv kind on a composite would overwrite a correct headline with divergent single-key math (D-09) — documented non-coverage + TODOS entry (Plan 04) |
| T-143-07 | Information Disclosure | migration + census artifacts in a PUBLIC repo | high | mitigate | No DSN/credential/connection string anywhere in the migration, harness, or census file (grep-asserted in Task 1); RAISE NOTICE carries a count only, never identifiers or row data; no-allowlist gitleaks scan before push (the allowlist is path-based over .planning/) |
| T-143-08 | DoS | unbounded batch (D-19 class) | medium | mitigate | MATERIALIZED fence + LIMIT 25 + FOR UPDATE SKIP LOCKED; Task-3 arm H executes the body against 26 rows (grep-only gates proven insufficient by D-19); self-verify counts MATERIALIZED occurrences = 1 and forbids the IN-SELECT-LIMIT shape |
| T-143-09 | DoS | cron slot contention | low | accept | `35 * * * *` clears every registered slot; residual = pg_cron concurrency cap (RESEARCH A1, LOW) — detectable in job_run_details, inspected in Plan 04 |
| T-143-12 | DoS (CI quality) | TEST stale-pending feed | medium | mitigate | Sweep-inserted pending rows on workerless TEST are never drained (JOB-08); self-limiting to ONE row per candidate ever (a job row of any status excludes the strategy); Task-1 census quantifies + STOP rule at 200; header documents it |
| T-143-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- Migration applies cleanly on the throwaway cluster (exit 0, all self-verify NOTICEs)
- All 13 directional arms + idempotency + bound observed against the DEPLOYED command
- Three body-neuter signatures observed and restored
- migration-reviewer + rls-policy-auditor run; #14 the only (pre-documented) deviation
- 143-CENSUS.md exists with both projects' counts and zero secret material
</verification>

<success_criteria>
- SC#1 detect half, SC#2, and SC#3 are all TRUE against the deployed body on a real Postgres.
- The census numbers are known BEFORE merge and the STOP rules were evaluated.
- The only unproven links are the ones no local run CAN prove: the cron ROLE's RLS posture and the
  real schedule — both owned by Plan 04's live TEST tick.
</success_criteria>

<output>
Create `.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-02-SUMMARY.md` when done,
including the observed-arm table and the `Gate | Neuter | Expected RED | Observed` table.
</output>
