---
phase: 145-job-csv-finalize-atomicity
plan: 04
type: execute
wave: 4
depends_on: [145-02, 145-03]
files_modified:
  - src/app/api/strategies/csv-finalize/route.ts
  - analytics-service/routers/process_key.py
  - src/lib/process-key-client.ts
  - src/__tests__/csv-finalize-cross-submission-merge.test.ts
  - src/__tests__/csv-finalize-after-failloud.test.ts
  - src/app/api/strategies/csv-finalize/route.test.ts
  - analytics-service/tests/test_process_key.py
autonomous: true
requirements: [JOB-06]
tags: [nextjs, route, python, seam, sentry, copy-honesty, csv-finalize, tdd]

estimate:
  tokens: 115000
  raw_tokens: 115000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "Both finalize paths (manager + contribution) reach csv_daily_returns ONLY through the folded RPC — the standalone persist call and the route-side stale-probe read are gone from the request path (D-07)"
    - "The caller shape implements EXACTLY the option recorded in 145-DECISION.md — never the other one, never a blend (D-06)"
    - "The 23505 resolve arm runs its CR-01 identity checks (name, then range-as-READ) BEFORE any metadata write — the 409 refusal is now truthful because nothing of THIS submission has been persisted when it fires (D-09, D-11; RESEARCH Pitfall 6)"
    - "The contribution path still passes p_terminal_status='private' explicitly and route.test.ts's CONTRIB-02 describe stays green — the wire half of SC#3 (D-08)"
    - "All three dishonest copy sentences are gone from route.ts; every surviving failure copy states exactly what was and was not persisted, and each sentence is pinned by a test that can fail (D-11)"
    - "The fold-failure arm carries a captureToSentry with a NEW grep-unique step tag, and the resolve-arm refusal carries its own capture/warn — window B/C observability delivered in folded form (D-12)"
    - "The five CR-01 tests are re-pointed at the resolve arm and EACH was observed RED under neuter before restore — inherited green is not evidence (D-09)"
    - "mypy --strict passes on analytics-service after the Python change (cast(), never type-ignore), and pytest runs from analytics-service/ with python3"
  artifacts:
    - "src/app/api/strategies/csv-finalize/route.ts — fold call (cast-through-unknown idiom), resolve arm ordering, honest copy, new capture sites"
    - "analytics-service/routers/process_key.py — per 145-DECISION.md: branch deleted (i-b) or rewritten to call the fold with a slimmed read-only resolve arm (i-a)"
    - "src/__tests__/csv-finalize-cross-submission-merge.test.ts — five arms re-pointed at the resolve arm, observed-RED record per arm"
    - "src/lib/process-key-client.ts — per decision: csv-finalize dispatch removed (i-b) or daily_returns_series added to the body (i-a)"
  key_links:
    - "route fold call <-> TS-13 discipline — success check is (error || !isUuid(id)), validating the payload, not just error-null (route.ts:1462-1489 idiom)"
    - "resolve arm re-fetch <-> C-08 tenant scope — scoped (user_id, wizard_session_id, source='csv') on a USER client, per process_key.py:1179-1204's reasoning, wherever the arm now lives"
    - "copy sentences <-> the mechanism — the copy is an assertion about the transaction; it lands in the SAME commit as the wiring it describes (D-11)"
    - "deleted/changed arms <-> their tests — every test the wiring breaks is re-pointed in the SAME commit (the 144-§8 class, TS edition)"
---

<objective>
Wire the fold into production per the founder's recorded decision, make every failure copy truthful,
and add the missing failure observability — with every test the wiring breaks re-pointed in the same
commit. After this plan: one write path to dailies, a read-only idempotent resolve arm whose checks
precede any metadata write, three honest copy sentences, and Sentry coverage on the arms that had
none.

Purpose: completes SC#2's request-path half (the fold is only a mechanism until callers use it) and
D-11/D-12 — the honesty and observability halves that are correct under any option.
Output: route + Python + client wiring, honest copy, capture sites, re-pointed TS/Python tests —
ONE commit.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-job-csv-finalize-atomicity/145-CONTEXT.md
@.planning/phases/145-job-csv-finalize-atomicity/145-RESEARCH.md
@.planning/phases/145-job-csv-finalize-atomicity/145-PATTERNS.md
@.planning/phases/145-job-csv-finalize-atomicity/145-DECISION.md
@src/app/api/strategies/csv-finalize/route.ts
@src/lib/sentry-capture.ts
@src/__tests__/csv-finalize-cross-submission-merge.test.ts
</context>

## Decision map (cited as D-NN below)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-06 | The caller choice was made by the founder on measurement (145-DECISION.md); this plan implements the recorded option and ONLY that option |
| D-07 | The fold is the sole write path; windows B/C dissolve in the route accordingly |
| D-08 | `p_terminal_status='private'` survives on the contribution wire (SC#3) |
| D-09 | CR-01 moves but never weakens: name + range checks move into the resolve arm as reads; the five tests are re-pointed and each observed RED. The economic oracle pinned is: a strategy's persisted series equals exactly the file that was submitted — never the fence's own predicate |
| D-11 | The three copy sentences move together with the mechanism, each pinned by a test that can fail |
| D-12 | Failure observability via captureToSentry (the SEAMCORE-06 scrub chokepoint — never a raw Sentry import; this flow's headers carry a live user JWT); request-scoped one-shot captures keyed by correlation_id in extra — no marker ceremony (143's heal-identity lesson applies to loop-emitted alerts, not these) |

## Preconditions (plan-wide)

- 145-DECISION.md exists with a recorded option (Plan 02's checkpoint resolved) — HALT if absent.
- Plan 03's fold migration + gates are committed at HEAD — HALT if absent.
- TS gates must run from a checkout WITH node_modules (GSD worktree agents get none — `npx vitest`
  exits 1 with MODULE_NOT_FOUND there; run from the main checkout or `npm ci` first).

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the fold per 145-DECISION.md (both arms specified; execute exactly one)</name>
  <files>src/app/api/strategies/csv-finalize/route.ts, analytics-service/routers/process_key.py, src/lib/process-key-client.ts</files>
  <behavior>
    - Manager-path finalize with a valid session and fresh wizard_session_id -> 200 + UUID; dailies present; enqueue fires in after() (unchanged hop 5)
    - Fold RPC error (non-23505) -> single 5xx arm; NOTHING persisted (the fold guarantees it); envelope shape byte-compatible ({ ok:false, code, human_message, debug_context, correlation_id } + NO_STORE_HEADERS)
    - 23505 -> resolve arm: re-fetch by (user_id, wizard_session_id, source='csv'); name mismatch -> 409; any committed daily outside THIS payload's [min,max] -> 409; both BEFORE metadata update; match -> 200 echoing the existing id (dailies guaranteed present — nothing persisted by the resolve arm)
    - Contribution path -> fold with p_terminal_status='private'; owner-only draft, never the admin queue
  </behavior>
  <read_first>
    - .planning/phases/145-job-csv-finalize-atomicity/145-DECISION.md (the branch key — read FIRST)
    - 145-RESEARCH.md §1 "The two callers" table + "The partial-unique index interaction" (the changed retry story: a window-B/C failure now commits NOTHING, so the instructed retry is a clean first submit; a 23505 has exactly ONE meaning — a prior attempt FULLY committed) and the Code Examples resolve-arm pseudocode
    - route.ts:1469-1489 (the CONTRIB-02 cast-through-unknown call shape — the house pattern; database.types.ts does not carry these RPCs), :514-662 (the helper being dissolved), :1378-1385 (the metadata call site being re-ordered), :233-241 (duplicate-date 400 — the route boundary the fold relies on)
    - analytics-service/routers/process_key.py:1117-1269 (the branch being deleted or rewritten; the C-08 scope reasoning at :1179-1204; the CR-01 name comparison at :1258-1269)
  </read_first>
  <action>
    Read the recorded option, then implement EXACTLY that arm (D-06):

    ARM (i-b) — route calls the fold directly, both paths:
    (1) In `unifiedCsvFinalizeHandler`: replace the /process-key POST dispatch (hop 0) with a
    direct call to `finalize_csv_strategy_with_returns` on the SSR cookie client, using the
    CONTRIB-02 cast-through-unknown shape and the TS-13 success check (error || !isUuid). Pass the
    validated rows as p_rows and p_terminal_status='pending_review'. The pre-create validation
    battery (:1116-1157, incl. the 5000 cap and duplicate-date 400) stays exactly where it is —
    the route boundary remains the load-bearing gate; the fold re-validates as defense-in-depth.
    (2) In `contributionCsvFinalizeHandler`: re-point the existing direct RPC call at the fold,
    keeping p_terminal_status='private' verbatim (D-08), and pass its rows as p_rows; delete its
    separate persist step.
    (3) Implement the 23505 resolve arm in TypeScript per the RESEARCH pseudocode: on error code
    '23505', re-fetch id+name scoped (user_id, wizard_session_id, source='csv') on the SSR user
    client (C-08); name mismatch -> 409 CSV_SESSION_REUSED; then the range check as a READ (any
    csv_daily_returns row for that id outside this payload's [min,max] -> 409); only after both
    pass, echo 200 with the existing id. Fail CLOSED: if either resolve read errors, refuse with
    the 5xx arm — a fence that cannot run must refuse, not pass (the C-3 lesson in PATTERNS).
    (4) Move `applyCsvMetadataUpdate` so it runs ONLY after a successful create or a successful
    resolve — never before the resolve checks (Pitfall 6; this is what makes the 409 truthful).
    (5) Delete `persistDailyReturnsOrErrorResponse`'s stale-probe + persist call path from the
    request flow; the failed-analytics placeholder write attached to the dissolved persist arm goes
    with it (nothing exists to placeholder when the fold fails — the wizard receives the 5xx
    directly); the enqueue arm's placeholder (window E) is UNTOUCHED. Carry the CR-01 rationale
    comment block (route.ts:523-557) to the resolve arm — it exists to stop simplification.
    (6) Delete the Python csv-finalize branch (process_key.py:1117-1269) — under (i-b) it is dead
    code and a second-writer drift bomb. Keep `X-User-Access-Token` forwarding itself (other-use
    verification is a 140.1 obligation, not this phase — D-01's no-removal warning applies in both
    directions). Remove the csv-finalize dispatch from process-key-client.ts.

    ARM (i-a) — Python calls the fold:
    (1) Add `daily_returns_series` to the /process-key csv-finalize body in process-key-client.ts
    (the route passes its already-validated rows; measured cost ~280 KB at cap). Python relays the
    array into p_rows WITHOUT re-implementing validation (one gate, one place — Python's
    contribution is transport; RESEARCH §1 "where the cap lives").
    (2) Rewrite process_key.py:1147-1156 to call `finalize_csv_strategy_with_returns` on the
    user-scoped client; slim the 23505 resolve arm to read-only per the RESEARCH pseudocode (C-08
    re-fetch, name check kept at :1258-1269's shape, range check as a READ against committed
    dailies, echo the id; the persist half is deleted).
    (3) In route.ts: delete the stale-probe + persist call path and its placeholder (as in i-b step
    5); re-point `contributionCsvFinalizeHandler` at the fold with 'private' + rows (as in i-b step
    2); re-order metadata after the (now Python-side) resolve outcome — the 409 envelope from
    Python must return BEFORE any metadata write, which the hop order now provides; keep hop 5
    unchanged.

    BOTH arms: log every new fail-arm with console.error(prefix + correlation_id, error.code,
    scrubSeamError(error)) — .code only, never raw .message (SEAMRIM-06). Do not commit yet — the
    single commit lands in Task 3 with the copy, captures, and test re-points.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && grep -c "finalize_csv_strategy_with_returns" src/app/api/strategies/csv-finalize/route.ts && grep -rn "persist_csv_daily_returns" src/ analytics-service/ --include="*.ts" --include="*.py" | grep -v -e test -e __tests__ -e "\.types\.ts" | grep -cv "^$" | grep -qx "0" && echo no-live-persist-callers</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one arm implemented, matching 145-DECISION.md; the other arm's shape appears nowhere.
    - Zero non-test references to the old persist RPC remain in src/ or analytics-service/; the old
      finalize name survives only where the decision keeps it (comments updated).
    - Resolve checks precede any metadata write (code order verified by reading the diff).
    - tsc clean; nothing committed yet.
  </acceptance_criteria>
  <done>One write path to dailies exists end-to-end, the resolve arm is read-only with checks-first ordering, and the wire-level 'private' survives (D-06, D-07, D-08, D-09)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the three copy sentences truthful + add the folded Sentry captures</name>
  <files>src/app/api/strategies/csv-finalize/route.ts</files>
  <behavior>
    - Fold-failure 5xx: copy states nothing was saved and invites retry — TRUE under the fold (the rollback is total)
    - 409 CSV_SESSION_REUSED: copy states the refusal happened before anything of this submission was written — TRUE because resolve checks precede metadata (Task 1)
    - No arm anywhere claims a partial write that did not happen, and no arm claims no-write where a write occurred; the dissolved persist arm's created-but-unsaved copy is gone with its arm
    - Fold-failure arm calls captureToSentry with tags { surface: 'csv-finalize', step: 'finalize-fold-fail' } and extra { correlation_id }; the resolve-arm refusal calls it with its own new step tag
  </behavior>
  <read_first>
    - 145-RESEARCH.md §5 (the three-sentence table — the 503 arm at route.ts:589-591, the 409 arm at :601-611, the 500 arm at :652-655, and what each must become under the fold) and §6 (capture placement, the taken step-name list, the no-marker rationale, the per-request secrets option)
    - src/lib/sentry-capture.ts:191-205 (the helper contract) and route.ts:933-936 / :836-839 (the two live capture exemplars to mirror)
  </read_first>
  <action>
    (1) Rewrite the failure copy on every surviving arm to state exactly what is true under the
    fold (D-11): the fold-failure 5xx arm says nothing was saved and to try again (keep code
    CSV_PERSIST_FAIL for envelope compatibility unless a test pins otherwise — verify against
    csv-finalize-after-failloud.test.ts before renaming anything); the 409 CSV_SESSION_REUSED arm
    keeps its refusal copy minus the false no-write claim, now accurate by ordering. The dissolved
    500 arm's copy is deleted with its arm — RESEARCH §5: left alone it would lie in the OTHER
    direction. Keep the envelope byte-shape ({ ok:false, code, human_message, debug_context,
    correlation_id } + NO_STORE_HEADERS) — after-failloud and merge tests pin the codes.
    (2) Sweep route.ts (code AND comments) for the two retired sentences so no copy of either
    survives anywhere in the file — the acceptance greps below are count-asserted and
    comment-inclusive; do not park the old wording in a comment.
    (3) Add the captures (D-12): on the fold-failure arm, captureToSentry(err, { tags: { surface:
    "csv-finalize", step: "finalize-fold-fail" }, extra: { correlation_id } }) after the
    console.error, before the return; on the resolve-arm refusal, a capture with step
    "finalize-resolve-refused" and extra { strategy_id: existingId, correlation_id }. Both step
    values are NEW — verify grep-unique against the taken list in RESEARCH §6 (placeholder-precheck,
    placeholder-upsert, placeholder-upsert-throw, csv-analytics-enqueue, csv-analytics-enqueue-throw,
    metadata-update, finalize-rpc). Request-scoped one-shot: no shouldCaptureNow throttle initially
    (finalize volume is low; the WR-06 throttle is for the seam's high-volume paths — say so in a
    short comment). Pass the per-request secrets option if the captured error object could carry
    the forwarded JWT (mirror :933-936's scrubSeamError-adjacent shape).
    Do not commit yet.
  </action>
  <verify>
    <automated>grep -c "Nothing was changed" src/app/api/strategies/csv-finalize/route.ts | grep -qx "0" && grep -c "Your strategy was created but" src/app/api/strategies/csv-finalize/route.ts | grep -qx "0" && grep -c "finalize-fold-fail" src/app/api/strategies/csv-finalize/route.ts && grep -c "finalize-resolve-refused" src/app/api/strategies/csv-finalize/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both retired sentences have ZERO occurrences in route.ts (code and comments — the greps are
      count-asserted, not presence-only).
    - Both new step tags present exactly once each at their arms; neither collides with a taken
      step name.
    - Envelope codes unchanged unless a pinning test was updated in the same diff.
  </acceptance_criteria>
  <done>Every user-facing failure sentence on this surface is true, and both failure arms that had zero observability now capture (D-11, D-12)</done>
</task>

<task type="auto">
  <name>Task 3: Same-commit test re-points — five CR-01 arms observed RED, Python tests per branch, suites green, ONE commit</name>
  <files>src/__tests__/csv-finalize-cross-submission-merge.test.ts, src/__tests__/csv-finalize-after-failloud.test.ts, src/app/api/strategies/csv-finalize/route.test.ts, analytics-service/tests/test_process_key.py</files>
  <read_first>
    - src/__tests__/csv-finalize-cross-submission-merge.test.ts IN FULL (the five arms incl. fail-closed at :260 — the intent to preserve, re-targeted)
    - 145-RESEARCH.md §7 rows for after-failloud, route.test.ts, and test_process_key.py (what each pins and its Phase-145 disposition); RESEARCH Pitfall 7 (csv-finalize-rpc.test.ts is skipIf live-DB and is NOT a gate — do not count it, do not "fix" anything only there)
  </read_first>
  <action>
    (1) RE-POINT the five CR-01 arms at the resolve arm (D-09): each test now drives a 23505 from
    the fold mock and asserts the resolve behavior (name-mismatch 409; out-of-range 409; in-range
    match echoes the existing id with NOTHING persisted by the arm; fail-closed when a resolve read
    errors; the fifth arm per its current intent, re-targeted). For EACH arm: neuter the resolve
    check it pins (invert/remove in a scratch edit of route.ts), run, OBSERVE RED, restore, re-run
    GREEN, record (arm, neuter, observed message) — five rows for the SUMMARY. Keep the economic
    oracle: assertions pin persisted-equals-submitted outcomes, never the implementation's own
    predicate re-typed.
    (2) UPDATE after-failloud: its four D7 tests must still pass (metadata-update and enqueue arms
    survive the fold untouched); any test pinning a dissolved arm is re-pointed at the fold-failure
    arm in this same commit. route.test.ts: CONTRIB-02 describe (:187) must pass against the fold
    call — update the mocked RPC name; the 'private' wire assertion itself is UNCHANGED (SC#3).
    (3) PYTHON, per the recorded option: (i-b) delete the csv-finalize tests that test the deleted
    branch (test_process_key.py:2164-2266, :3889+, :4033+ — verify membership by reading, not by
    line numbers) and keep/retarget any that pin surviving envelope behavior; (i-a) rewrite the
    mocks to the fold's name and the slimmed read-only resolve arm, preserving the
    user-client-not-service-client assertions verbatim (they are the arm-2/arm-3 evidence class).
    (4) RUN: `npx vitest run src/__tests__/csv-finalize-after-failloud.test.ts
    src/__tests__/csv-finalize-cross-submission-merge.test.ts
    src/__tests__/csv-finalize-c14-regression.test.ts
    src/app/api/strategies/csv-finalize/route.test.ts` (main checkout — the worktree
    node_modules caveat; add --no-file-parallelism if local flakes appear); `cd analytics-service
    && python3 -m pytest tests/test_process_key.py -q` and `python3 -m mypy --strict` on the
    touched modules (cast(), never a type-ignore pragma). ALL green. If the c14 file's vacuous
    block (untouched until Plan 05) fails against the new wiring, mark it `.skip` with a
    `Plan 05 rebuilds this` comment rather than deleting here — Plan 05 owns that file.
    (5) COMMIT — ONE commit containing Tasks 1+2+3 (wiring + copy + captures + every re-pointed
    test): e.g. `feat(145-04): wire the csv-finalize fold per 145-DECISION + honest copy + captures + same-commit test re-points`.
    Verify with `git show --stat` that route.ts, the Python router, the client, and all touched
    test files are in that single commit.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/csv-finalize-after-failloud.test.ts src/__tests__/csv-finalize-cross-submission-merge.test.ts src/app/api/strategies/csv-finalize/route.test.ts --no-file-parallelism && (cd analytics-service && python3 -m pytest tests/test_process_key.py -q)</automated>
  </verify>
  <acceptance_criteria>
    - Five observed-RED records exist, one per CR-01 arm (D-09); every re-point happened in the
      same commit as the wiring that broke the old target.
    - Scoped vitest + full test_process_key.py + mypy --strict all green from the correct
      directories.
    - git show --stat HEAD lists production files and test files together in ONE commit.
  </acceptance_criteria>
  <done>The wiring, its truth claims, and every gate that guards them land atomically, and no CR-01 protection was weakened in the move (D-09, D-11)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> route | caller-supplied rows/metadata; the route's validation battery stays load-bearing |
| route -> fold RPC | user-scoped clients only (SSR cookie session; user-scoped Python client under i-a) — never the admin client |
| route -> Sentry | this flow's headers carry a live user JWT — captures go through the scrub chokepoint only |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-13 | Tampering (money) | cross-submission merge via the resolve arm | critical | mitigate | Name + range checks precede echo; fail-closed on resolve-read error; five re-pointed tests each observed RED (D-09) |
| T-145-14 | Repudiation | user told a false write state | high | mitigate | Three sentences rewritten to match the transaction; count-asserted absence greps; per-sentence pinning tests (Plan 05 hardens further) |
| T-145-15 | Information disclosure | JWT/raw error in logs or Sentry | high | mitigate | scrubSeamError on console lines; captureToSentry chokepoint + secrets option; .code-only logging (SEAMRIM-06) |
| T-145-16 | Elevation of privilege | private contribution promoted to admin queue | high | mitigate | 'private' passed verbatim on the wire (D-08); route.test.ts CONTRIB-02 green; SQL half pinned in Plan 03 |
| T-145-17 | Denial of service | second live writer left behind (drift bomb) | medium | mitigate | Dead branch deleted per the decision arm; A6-class grep in Task 1's acceptance proves zero live old-RPC callers |
| T-145-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- One arm implemented per 145-DECISION.md; tsc + scoped vitest + pytest + mypy --strict green
- Retired sentences at zero occurrences; both new step tags present; envelope byte-shape preserved
- Five CR-01 observed-RED records; ONE commit carries wiring + copy + captures + re-points
</verification>

<success_criteria>
- SC#2's request path is complete: every failure between finalize and persist now rolls back to nothing, and the after() enqueue drop remains window D, healed by 143 (D-07, D-10).
- SC#3's wire half held: contribution 'private' unchanged and tested (D-08).
- D-11/D-12 delivered: honest copy pinned, both new captures live.
</success_criteria>

<output>
Create `.planning/phases/145-job-csv-finalize-atomicity/145-04-SUMMARY.md` when done, including
which decision arm was implemented, the five CR-01 observed-RED records, and the commit hash.
</output>
