---
phase: 145-job-csv-finalize-atomicity
plan: 02
type: execute
wave: 2
depends_on: [145-01]
files_modified:
  - .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md
  - .planning/phases/145-job-csv-finalize-atomicity/145-MEASUREMENT.md
  - .planning/phases/145-job-csv-finalize-atomicity/145-DECISION.md
  - TODOS.md
autonomous: false
requirements: [JOB-06]
tags: [supabase, census, live-verification, measurement, founder-decision, csv-finalize]

estimate:
  tokens: 80000
  raw_tokens: 80000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "The four CONTEXT census queries have been run VERBATIM on BOTH projects (PROD khslejtfbuezsmvmtsdn, TEST qmnijlgmdhviwzwfyzlc) and the per-row results are recorded BEFORE any SC#2 code exists (RESEARCH Pitfall 8)"
    - "Query (1) minus query (2) — the wizard first-hop population — is recorded as EXCLUDED from 145 scope, never silently absorbed (143's filed non-coverage)"
    - "Arm 4 executed: one live TEST finalize returned 200 + UUID with no 42501 in any layer, and the row-state baseline (strategies / strategy_verifications / csv_daily_returns count / compute_jobs / strategy_analytics) is captured as the SC#3 measured before-state (D-01)"
    - "145-REPRODUCTION.md carries the FINAL committed verdict (expected: CANNOT REPRODUCE — the GUARD is live, the PATH is closed) and the TODOS 42501 bullet is closed citing which of its own two remedies shipped (forward JWT, Phase 19.1) (D-01, D-03)"
    - "The (i-a) latency measurement exists as NUMBERS: median control vs payload seam cost (Step A, TEST only, no side effects) and the 5000-vs-10-row live-finalize delta (Step B), tabulated in 145-MEASUREMENT.md (D-06)"
    - "The (i-a) vs (i-b) choice was made by the FOUNDER at a blocking decision checkpoint fed by the measurement — never assumed by a plan (D-06)"
    - "145-DECISION.md records the selected option, the numbers table, and the architectural rationale — Plans 03-06 read it as their branch key"
  artifacts:
    - ".planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md — arm 4 + census sections completed, final verdict line written"
    - ".planning/phases/145-job-csv-finalize-atomicity/145-MEASUREMENT.md — the Step A/B/C memo with the committed payload one-liner and raw curl timings"
    - ".planning/phases/145-job-csv-finalize-atomicity/145-DECISION.md — the founder's (i-a)/(i-b) selection with rationale"
  key_links:
    - "arm-4 baseline <-> Plan 06's SC#3 after-state diff — without this measured before-state, 'happy path unchanged' is an assertion, not a measurement"
    - "census id list <-> Plan 06's one-time terminalize — the list captured here (re-verified fresh in Plan 06) is what gets terminalized under founder ruling 1 (D-05)"
    - "145-DECISION.md <-> Plan 04 Task 1's branch — the caller wiring implements EXACTLY the recorded option"
---

<objective>
Execute everything SC#1 and founder ruling 2 require from the orchestrator session (Supabase MCP is
stripped from subagents): the four-query census on both projects, reproduction arm 4 (one live TEST
finalize, doubling as the SC#3 baseline and measurement Step B's first run), the (i-a) seam-cost
latency measurement, the final CANNOT-REPRODUCE verdict + TODOS closure, and the blocking founder
decision checkpoint on (i-a) vs (i-b) fed by the numbers.

Purpose: SC#1 completes as a committed pass/fail artifact; founder ruling 2 is honored — the caller
choice is made on measurement, by the founder, before any caller wiring is planned into existence.
Output: completed 145-REPRODUCTION.md, 145-MEASUREMENT.md, 145-DECISION.md, closed TODOS bullet.

⛔ ORCHESTRATOR-SESSION-ONLY, BLOCKING: every task here needs the Supabase MCP, live TEST
credentials, or a founder response. Do not delegate any task in this plan to a subagent.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/145-job-csv-finalize-atomicity/145-CONTEXT.md
@.planning/phases/145-job-csv-finalize-atomicity/145-RESEARCH.md
@.planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md
</context>

## Decision map (cited as D-NN below)

| ID | Locked decision honored in this plan |
|----|--------------------------------------|
| D-01 | The verdict is CANNOT REPRODUCE, made executable and committed — arm 4 is its live confirmation; no 42501 fix scoped |
| D-03 | The TODOS bullet (TODOS.md:818-820) is closed citing which of its own two proposed remedies shipped ("forward JWT" — implemented Phase 19.1, verified at HEAD: route.ts:1324 forwards, process_key.py:1135 reads) |
| D-05 | Orphan disposition = TERMINALIZE (founder ruling 1); the census taken here is what bounds the one-time terminalize list; deletion arms out of scope |
| D-06 | (i-a) vs (i-b) = MEASURE FIRST, founder decides (founder ruling 2). This plan executes the measurement and STOPS at a decision checkpoint — picking an option in a plan would violate the ruling |

## STOP rules (pre-registered, from CONTEXT :343-346)

| Observation | Action |
|-------------|--------|
| Arm 4 observes a 42501 / CSV_FINALIZE_FAIL on the live finalize | The verdict premise is FALSIFIED. Write the REPRODUCED verdict with full evidence, do NOT proceed to the decision checkpoint, HALT the phase and return to the orchestrator — SC#2's shape must be re-scoped |
| Census (1)/(2) zero on both projects | Legitimate, budget-saving: the fold ships as prospective hardening (143's precedent); Plan 06's terminalize is a recorded no-op |
| Census (2) non-zero on PROD | Re-rank from prospective hardening to live cleanup: record the id list per-row; Plan 06's terminalize gets a real population; continue |
| Census (1) minus (2) non-zero | That is the wizard first-hop population — 143's filed non-coverage. Record it, EXCLUDE it from 145, Plan 05 files it to TODOS |

<tasks>

<task type="auto">
  <name>Task 1: Run the four census queries verbatim on BOTH projects and record per-row results</name>
  <files>.planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md</files>
  <precondition>Supabase MCP is available in this session with access to both projects (PROD khslejtfbuezsmvmtsdn, TEST qmnijlgmdhviwzwfyzlc) — this is why the plan is orchestrator-only; if the MCP is absent, HALT</precondition>
  <read_first>
    - 145-CONTEXT.md the census SQL block (queries 1-4, copied verbatim — do not re-derive the predicates) and the interpretation paragraph beneath it
  </read_first>
  <action>
    Run CONTEXT's queries (1)-(4) VERBATIM on both projects via the Supabase MCP (read-only
    SELECTs; no writes in this task). Record in 145-REPRODUCTION.md's census section: for (1) the
    full per-row output (id, user_id, status, source, wizard_session_id, created_at,
    analytics_status) per project; for (2)/(3) the counts; for (4) the flag-row state (⛔ do NOT
    flip or delete the row regardless of what it reads — 20260620120000:86-89 RAISEs at apply time
    if it reads 'off'; cleanup is a Plan 05 TODOS deferral). Compute and record (1) minus (2) as
    the wizard first-hop population with the EXCLUDED-from-145 statement. Apply the pre-registered
    STOP-rule interpretations above and write which row of that table fired. Also derive the
    candidate terminalize list for Plan 06: the query-(2)-shaped rows (source='csv', no dailies),
    each annotated with the human-review signals RESEARCH §2 names (source, wizard_session_id
    present, strategy_verifications row present).
  </action>
  <verify>
    <automated>grep -c "khslejtfbuezsmvmtsdn" .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md && grep -c "qmnijlgmdhviwzwfyzlc" .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md</automated>
  </verify>
  <done>The residual orphan population is a measured, per-row fact on both projects — SC#2 is scoped on measurement, not on a standing claim (the 142/143/144 lesson) (D-05)</done>
</task>

<task type="auto">
  <name>Task 2: Arm 4 live finalize + the Step A/B latency measurement; finalize the verdict; close the TODOS bullet</name>
  <files>.planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md, .planning/phases/145-job-csv-finalize-atomicity/145-MEASUREMENT.md, TODOS.md</files>
  <precondition>TEST app URL + service-role key for the passwordless magic-link idiom, and the TEST Railway URL + INTERNAL_API_TOKEN are available in this session; ⛔ nothing in this task touches PROD (Step A pollutes Sentry/limiter; Step B mints real strategies — RESEARCH Pitfall 10)</precondition>
  <reversibility rating="costly">Two real strategies are minted on TEST (10-row and 5000-row); record their ids so Plan 06 can archive them after the SC#3 diff — never delete (D-05 register)</reversibility>
  <read_first>
    - 145-RESEARCH.md §3 (Half 2 Steps A/B/C verbatim — the curl loops, the isolation property, the memo shape) and §4 arm 4 (the oracle + the five baseline SQL queries)
    - analytics-service/routers/process_key.py:874-875 (check the slowapi limiter values BEFORE running Step A's 10 requests — never assume the budget)
  </read_first>
  <action>
    (1) STEP A — marginal seam cost, no side effects: generate `control.json` (envelope alone) and
    `payload.json` (envelope + 5000-row series) with the node one-liner committed in RESEARCH §3
    Half 1 (re-run it; commit the command's output figures into 145-MEASUREMENT.md so the numbers
    are re-derivable). Check the limiter values first; then run the §3 curl loop (5 reps each file)
    against TEST Railway `/process-key` with `Authorization: Bearer $INTERNAL_API_TOKEN` and NO
    X-User-Access-Token header — the csv-finalize branch 401s at process_key.py:1136-1146 AFTER the
    body is received and pydantic-parsed, so the timing isolates upload+parse and writes nothing.
    Record all ten `size_upload time_total` lines verbatim; compute median(payload) −
    median(control).
    (2) ARM 4 + STEP B — two live finalizes on TEST via the real route (passwordless service-role
    magic-link -> setSession -> curl, the repo's authed idiom): first a 10-row `daily_returns`
    finalize with a fresh wizard_session_id (this IS arm 4: oracle = HTTP 200, body ok:true + UUID
    strategy_id, no 42501/CSV_FINALIZE_FAIL in any layer's logs); then a 5000-row finalize (Step
    B). Record wall-clock for both; the 5000−10 delta bounds today's hop-4 persist cost, which is
    what the folded call inherits under (i-b). Immediately run the five arm-4 baseline SQL queries
    from RESEARCH §4 against the 10-row strategy and record the row states verbatim — this is the
    SC#3 measured before-state Plan 06 diffs against. Record both minted strategy ids.
    (3) VERDICT: update 145-REPRODUCTION.md — arm 4 result, census cross-reference, and the FINAL
    verdict line: CANNOT REPRODUCE, with the D-02 split statement (the GUARD is live, the PATH is
    closed). If instead the STOP rule fired, write REPRODUCED + evidence and HALT per the table
    above.
    (4) TODOS closure (D-03): edit the 42501 bullet (TODOS.md:818-820) to closed/superseded status,
    citing 145-REPRODUCTION.md and stating which of the bullet's own two remedies shipped: "forward
    JWT" — implemented Phase 19.1 (2026-05-27), verified at HEAD at route.ts:1324 (forwarding) and
    process_key.py:1135 (read). Note the flag concept itself was deleted (zero readers at HEAD).
    Scoped Edit; touch no other bullet.
    (5) STEP C — the memo: write 145-MEASUREMENT.md with the one table founder ruling 2 demands:
    payload bytes | marginal Next->Railway seam cost (A) | full-cap persist cost (B delta) | route
    totals (B) — plus the §3 framing: the measurement's job is to retire the latency argument; the
    remaining axis is architectural ((i-b) reverses Phase 106 Stage B's sole-finalize-path ruling;
    (i-a) grows the seam payload ~280 KB and keeps the Python resolve arm).
    (6) COMMIT everything from Tasks 1-2 in one commit, e.g.
    `docs(145-02): SC#1 verdict (arm 4 + census) + (i-a)/(i-b) measurement + TODOS closure`.
  </action>
  <verify>
    <automated>grep -c "CANNOT REPRODUCE" .planning/phases/145-job-csv-finalize-atomicity/145-REPRODUCTION.md && test -f .planning/phases/145-job-csv-finalize-atomicity/145-MEASUREMENT.md && grep -c "time_total\|median" .planning/phases/145-job-csv-finalize-atomicity/145-MEASUREMENT.md</automated>
  </verify>
  <acceptance_criteria>
    - Arm 4's oracle observed and recorded (200 + UUID, no 42501 anywhere); baseline row states
      captured verbatim; both TEST strategy ids recorded for Plan 06.
    - All ten Step-A timing lines recorded verbatim; medians computed; limiter values checked first.
    - The final verdict line exists in 145-REPRODUCTION.md; TODOS bullet closed with the D-03
      citation; ONE commit carries the artifact updates + TODOS + memo.
  </acceptance_criteria>
  <done>SC#1 is a committed pass/fail fact with a live confirmation, and the founder has NUMBERS, not estimates, for the caller choice (D-01, D-03, D-06)</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Founder decision checkpoint — (i-a) vs (i-b), fed by the measurement</name>
  <decision>Fold caller: (i-a) Python calls the folded RPC (series crosses the /process-key seam) vs (i-b) the route calls it directly (Phase 106 Stage B "sole finalize path" is reversed for this flow)</decision>
  <context>
    Founder ruling 2 (2026-08-17) reserved this choice to the founder, measure-first (D-06). The
    measurement is now done — present the 145-MEASUREMENT.md table verbatim, plus the §1 cost
    table: under (i-a) window A survives but downgrades to window D (healed by 143), the Python
    csv-finalize branch is rewritten and the seam body grows ~280 KB; under (i-b) window A ceases
    to exist, both writers converge on one route-side caller, but Phase 106 Stage B's "unified
    backbone is the sole finalize path" is reversed for this flow and the Python branch becomes
    dead code that MUST be deleted. Both options ship the identical migration (Plan 03 is
    caller-agnostic); only Plan 04's wiring branches.
  </context>
  <options>
    <option id="i-a">
      <name>(i-a) Forward the dailies to Python; Python calls the fold</name>
      <pros>Preserves Phase 106 Stage B's sole-finalize-path architecture; smallest conceptual change to the seam's ownership story</pros>
      <cons>One additional ~280 KB seam crossing per finalize (measured); window A survives (downgraded to D); the Python resolve arm is retained and rewritten; hop 0's HTTP boundary remains outside the transaction</cons>
    </option>
    <option id="i-b">
      <name>(i-b) Route calls the fold directly (the CONTRIB-02 shape, for both paths)</name>
      <pros>Window A ceases to exist (hop 0 removed); two writers converge on one; zero new seam payload; the CONTRIB-02 path is the working existence proof</pros>
      <cons>Reverses Phase 106 Stage B's ruling for this flow; the Python csv-finalize branch becomes dead code and must be deleted with its tests re-pointed (leaving it live is a second writer and a drift bomb)</cons>
    </option>
  </options>
  <resume-signal>Select: i-a or i-b. On resume, write .planning/phases/145-job-csv-finalize-atomicity/145-DECISION.md recording the selected option, the measurement table, and the stated rationale; commit it (`docs(145-02): founder caller decision — (i-x)`). Plans 03-06 read this file as the branch key.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator session -> PROD | census SELECTs only in this plan; zero PROD writes (the terminalize writes are Plan 06's, post-fold, human-gated) |
| orchestrator session -> TEST | two real finalizes minted + Step-A 401 probes; limiter budget checked first |
| measurement -> founder decision | the numbers must be raw and re-derivable (committed commands + verbatim timings) so the decision is fed by data, not summary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-145-04 | Tampering | census-before-scoping discipline | high | mitigate | Queries run verbatim, per-row records committed, STOP rules pre-registered — no post-hoc reinterpretation |
| T-145-05 | Denial of service | TEST limiter / Sentry noise from Step A | low | mitigate | Limiter values checked first; 5 reps per file; TEST only (Pitfall 10) |
| T-145-06 | Repudiation | the founder decision's evidence trail | medium | mitigate | 145-DECISION.md commits option + numbers + rationale; Plans 03-06 cite it |
| T-145-07 | Information disclosure | live-session JWT in curl transcripts | medium | mitigate | Redact tokens from every recorded command/output before committing artifacts (.planning/ is world-readable on push) |
| T-145-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- Census recorded per-row on both projects; (1)−(2) exclusion stated
- Arm 4 oracle + baseline captured; verdict finalized; TODOS bullet closed with citation
- Measurement memo has raw timings + medians + the committed generation commands, tokens redacted
- Founder decision recorded in 145-DECISION.md before any Wave-3+ plan executes
</verification>

<success_criteria>
- SC#1 fully discharged: committed pass/fail artifact with all four arms executed (ROADMAP SC#1).
- Founder ruling 2 honored end-to-end: measurement -> numbers -> founder checkpoint -> recorded decision (D-06).
- The SC#3 measured before-state exists for Plan 06's diff.
</success_criteria>

<output>
Create `.planning/phases/145-job-csv-finalize-atomicity/145-02-SUMMARY.md` when done, including the
verdict line, the census counts, the measurement medians, the decision, and the commit hashes.
</output>
