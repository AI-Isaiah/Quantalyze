---
phase: 144-job-wr-02-orphaned-running
plan: 02
type: execute
wave: 2
depends_on: [144-01]
files_modified:
  - src/__tests__/retention-orphaned-running-terminalize.test.ts
  - .planning/REQUIREMENTS.md
  - TODOS.md
autonomous: true
requirements: [JOB-05, JOB-08]
tags: [vitest, migration-content-gate, ci, requirements, todos]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "A pure-text vitest gate pins the deployed two-arm shape, SCOPED to the dollar-quoted cron body with an anti-vacuity guard on the extraction — header prose neither satisfies nor trips it"
    - "Every occurrence count is RECALIBRATED for the two-arm body and documented in-file (public.compute_jobs = 4, word-bounded LIMIT 100 x 2, AS MATERIALIZED = 2, failed_final = 2, next_attempt_at = 2) — never copied from 143's one-arm numbers"
    - "The gate scans every LATER migration for a silent re-registration of the jobname retention_compute_jobs_orphaned_running"
    - "Every TS assertion has been OBSERVED red under its neuter (temporary migration-file edit, restored via git checkout), and the observations extend Plan 01's neuter table"
    - "JOB-08's WON'T-FIX is committed in REQUIREMENTS.md carrying the census table and the structural argument — a WON'T-FIX without its measurement attached is just a skip (D-11)"
    - "TODOS.md carries the three Phase 144 deferrals and nothing from the CONTEXT Deferred Ideas list is implemented (D-13)"
  artifacts:
    - "src/__tests__/retention-orphaned-running-terminalize.test.ts — no DB required, runs in every vitest shard"
    - ".planning/REQUIREMENTS.md — JOB-08 dated WON'T-FIX resolution block with the 2026-08-17 census numbers"
    - "TODOS.md — three entries under a Phase 144 deferrals heading"
  key_links:
    - "FIX_TS/FIX_FILENAME constants <-> supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql — a gate that reads its expectation out of the artifact it guards cannot fail; every expected literal is declared locally"
    - "TS occurrence counts <-> migration STEP 2 counts <-> SQL gate Part 1 counts — three siblings; a future body-shape change moves all three in one commit"
    - "REQUIREMENTS.md JOB-08 block <-> the CONTEXT census (PROD pending = 0, structurally so) <-> Plan 03's pre-merge re-census — the dated-claim discipline"
---

<objective>
Land the third gate (the vitest migration-content gate, which runs in every CI shard with no DB) with
counts deliberately recalibrated for the two-arm body, observe its neuter-REDs, and commit the JOB-08
paper trail: the WON'T-FIX-with-measurement in REQUIREMENTS.md and the three deferral entries in
TODOS.md.

Purpose: JOB-05's shape survives future edits only if a no-DB gate pins it in every shard; JOB-08 is
satisfied by a recorded decision carrying its evidence, not by silence.
Output: the TS gate, its neuter-RED records, the REQUIREMENTS.md resolution block, three TODOS entries.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-job-wr-02-orphaned-running/144-CONTEXT.md
@.planning/phases/144-job-wr-02-orphaned-running/144-RESEARCH.md
@.planning/phases/144-job-wr-02-orphaned-running/144-PATTERNS.md
@.planning/phases/144-job-wr-02-orphaned-running/144-01-SUMMARY.md
@src/__tests__/reconcile-dropped-enqueue-sweep.test.ts
@supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql
</context>

## Decision map (cited as D-NN below; source: 144-CONTEXT.md locked decisions; same table as 144-01-PLAN.md)

| ID | Locked decision honored here |
|----|------------------------------|
| D-01 | Never-remove is pinned by a body-scoped negative assertion in the TS gate (the third sibling of STEP 2 and SQL-gate Part 1) |
| D-05 | The 4h literal and the RT-01 story are pinned textually (interval '4 hours' present, interval '2 hours' absent from the body) |
| D-06 | The '50 * * * *' schedule literal is pinned |
| D-08 | Arm B's interval '48 hours' + claimed_at IS NULL are pinned; the occurrence counts are what detect a silently-deleted arm |
| D-09 | The NULL-claim writer finding (test residue, evidence in RESEARCH §3) drives the fixture-hygiene TODOS entry — NO edit to test_compute_jobs_fencing.py is made in this phase (plan-time settled call: record + route to TODOS) |
| D-11 | SC#4 WON'T-FIX carrying the measurement lands in REQUIREMENTS.md under JOB-08 and is restated in the phase SUMMARY |
| D-12 | The two standing traps are restated verbatim in the JOB-08 block: never remove a pending row; never cron.unschedule(9) |
| D-13 | TEST's 2819-row stale-pending backlog is filed to TODOS as CI hygiene — never solved in production code |

<tasks>

<task type="auto">
  <name>Task 1: TS migration-content gate with recalibrated counts + observed neuter-REDs</name>
  <files>src/__tests__/retention-orphaned-running-terminalize.test.ts</files>
  <precondition>node_modules is present and `npx vitest --version` resolves (GSD worktree agents get NO node_modules — measured: npx vitest exits 1 MODULE_NOT_FOUND and npx tsc resolves a joke package; if that happens, run from the main checkout or `npm ci` first, and if neither is possible HALT and escalate — never claim green from a run that could not execute)</precondition>
  <read_first>
    - src/__tests__/reconcile-dropped-enqueue-sweep.test.ts (the template, in full: FIX_TS/FIX_FILENAME/FIX_PATH constants at :57-97, the cronBody() extractor with its anti-vacuity guard at :99-123, body-scoping rationale at :37-50, negative assertions at :344-384, required-migration-content anchors at :386-413, the later-migration re-registration scan at :415-437)
    - supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql (the artifact under guard — read STEP 2's counts; the TS gate mirrors them exactly)
    - 144-PATTERNS.md §3 and Anti-patterns 1-3 (word-bounded LIMIT in TS form; occurrence-count rationale; the corrected single-statement regex window)
  </read_first>
  <action>
    Create src/__tests__/retention-orphaned-running-terminalize.test.ts from the 143 template:
    (1) Constants: FIX_TS = '20260817120000', FIX_FILENAME / FIX_PATH for the migration; every
    expected literal declared LOCALLY with a comment naming its production source (a gate that reads
    its expectation out of the artifact it guards cannot fail).
    (2) cronBody(sql) extractor between the first and second occurrence of the dollar tag, WITH the
    anti-vacuity guard asserting the extracted span contains the UPDATE statement — an empty or
    prose-only extraction must fail loudly, never pass negatives by default (the 143 incident).
    (3) Whole-file anchors: filename matches ^\d{14}_snake_case; prefix strictly greater than
    20260816140000; contains pg_extension, feature_not_supported, RAISE EXCEPTION, FROM cron.job;
    schedule literal '50 * * * *' present exactly once in the cron.schedule call.
    (4) BODY-SCOPED positive anchors with counts recalibrated for the TWO-ARM body and the meaning
    of each count documented in a comment (never copy 143's one-arm numbers — the constraint):
    public.compute_jobs occurs 4 times (arm A CTE, arm A UPDATE target, arm B CTE, arm B UPDATE
    target); "status = 'running'" occurs 4 times (two predicates + two compare-and-set fences);
    failed_final 2; next_attempt_at 2 (the B3 retention link — one per SET list); error_kind +
    permanent present; orphaned_running_reaped 2; interval '4 hours' exactly 1; interval '48 hours'
    exactly 1; claimed_at IS NULL present; AS MATERIALIZED 2; FOR UPDATE SKIP LOCKED present; the
    bound word-bounded via the TS form from the constraint list — a regex matching LIMIT, whitespace,
    100, with a negative lookahead for a further digit — asserted to match exactly 2 times (one per
    arm; the plain-substring form passes for a 10x widening, the measured 143 red-team finding).
    (5) BODY-SCOPED negative anchors, each with a comment naming the incident it prevents: the
    removal keyword the pre-144 janitor used (D-01 — body-scoped so the header may discuss history
    freely); the IN-SELECT-LIMIT shape via the corrected single-statement window regex (anything but
    a semicolon between SELECT and LIMIT — the [^)]* window matched nothing, Anti-pattern 3); the
    enqueue RPC name (no re-enqueue, §6); claimed_by (forensics preserved); the claimable
    failed-retry status value (the wrong terminal — inside 142's exclusion set); INSERT INTO (this
    janitor only ever UPDATEs).
    (6) The later-migration scan: every supabase/migrations file with a 14-digit prefix strictly
    greater than FIX_TS must NOT call cron.schedule with the jobname
    retention_compute_jobs_orphaned_running — a silent re-registration would strand this gate
    guarding a superseded body.
    (7) NEUTER-REDS: for each assertion class, temporarily edit the MIGRATION file (widen the LIMIT,
    delete arm B, swap the terminal status, drop next_attempt_at, change the schedule minute,
    reinsert a removal statement into the body), run `npx vitest run src/__tests__/
    retention-orphaned-running-terminalize.test.ts --no-file-parallelism`, OBSERVE the red, then
    restore via `git checkout -- supabase/migrations/20260817120000_*.sql` and re-run to green.
    Record each (neuter, failing assertion, observed message) — this extends Plan 01's table.
    Commit the test file once green with the migration byte-restored (verify `git status` shows the
    migration unmodified before committing).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/retention-orphaned-running-terminalize.test.ts --no-file-parallelism && git diff --stat supabase/migrations/ | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - Gate green at HEAD; migration file byte-identical to Plan 01's commit (git diff empty over
      supabase/migrations/).
    - Every count assertion documents in-file WHICH occurrence is which arm.
    - The anti-vacuity guard demonstrably fires on an empty extraction (covered in the neuter set by
      pointing the extractor at a copy with the body removed).
    - At least seven neuter-REDs observed and recorded for the SUMMARY.
  </acceptance_criteria>
  <done>The no-DB shard-level gate pins the deployed shape with counts that are correct for THIS body, and every assertion has been seen to fail.</done>
</task>

<task type="auto">
  <name>Task 2: JOB-08 WON'T-FIX with measurement (REQUIREMENTS.md) + three TODOS deferrals</name>
  <files>.planning/REQUIREMENTS.md, TODOS.md</files>
  <read_first>
    - 144-RESEARCH.md §9 (the exact census table to carry verbatim and the structural argument) and §6 "Residual gap" (the chain-mid TODOS wording)
    - .planning/REQUIREMENTS.md JOB-08 bullet (the requirement text this block resolves — it already names both traps)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-04-PLAN.md Task 3 (the "Phase 143 — recorded deferrals" TODOS heading format to mirror)
  </read_first>
  <action>
    (1) REQUIREMENTS.md — append a dated resolution block to the JOB-08 bullet (Edit, scoped — do
    NOT flip the checkbox; phase verification owns that): "RESOLVED 2026-08-17 (Phase 144): WON'T-FIX
    carrying the measurement, per SC#4's sanctioned budget-saving outcome." Carry the census table
    VERBATIM from RESEARCH §9 (PROD pending = 0, TEST pending = 2819 with date ranges; PROD running
    = 0, TEST running = 402 with the corrected 396+6 decomposition and the correction note). State
    the structural argument in full: nothing sweeps pending — that absence is the whole of JOB-08 —
    so a zero PROD snapshot is not a snapshot, it is a statement that zero rows have EVER stranded
    on PROD (corroborated in-repo at 20260816140000:82). Restate the two traps verbatim per D-12
    (never remove a pending row — auto-applies to PROD and destroys real queued work; never
    cron.unschedule(9) — test_derive_allocator_keys_fanout.sql assertion 6 requires it registered).
    Note that TEST's 2819 is a CI-hygiene artifact (no worker + jobid 9 fan-out), filed separately,
    and that Plan 03 re-measures PROD pre-merge so the decision rests on a fresh number (dated-claim
    discipline — a ledger claim is a dated CLAIM, not a fact).
    (2) TODOS.md — add a "Phase 144 — recorded deferrals" heading (root TODOS.md is the single
    backlog; add items ONLY there) with three items:
    (i) TEST stale-pending backlog (2819 rows, 2026-08-17) is a CI-hygiene problem — TEST has no
    draining worker and cron jobid 9 fans out daily; candidate fixes are a drain or a TEST-only
    cleanup, never production code (D-13);
    (ii) chain-mid residual: a terminalized orphan permanently excludes its strategy from Phase
    143's reconcile sweep (its predicate requires ZERO compute_jobs rows of any status) — candidate
    fix is widening that conjunct to "no NON-terminal row", a change to a shipped predicate with its
    own safety analysis re the 31-day retention interaction (RESEARCH §6 residual, v1.19+);
    (iii) fixture hygiene: analytics-service/tests/test_compute_jobs_fencing.py:1138-1152 and
    :1191-1205 drive rows to running WITHOUT stamping claimed_at — the attributed producer of the
    NULL-claim orphan class (RESEARCH §3, five-attribute match; Plan 03 runs the live confirming
    query) — one-line fix each (stamp claimed_at in the direct UPDATE) deferred here by the
    plan-time settled call (D-09): record + route, do not edit the file in this phase.
    Do NOT re-file the 20260802120000 header correction (already filed by Phase 143 — CONTEXT
    Deferred Ideas), and do NOT implement any deferral. Record in the SUMMARY notes that the
    operator-signal question (RESEARCH Open Q1) was considered and deferred: a first PROD
    terminalization implies a >4h worker outage, which Railway/Sentry already surface.
    Commit the two files.
  </action>
  <verify>
    <automated>grep -c "RESOLVED 2026-08-17 (Phase 144)" .planning/REQUIREMENTS.md && grep -c "Phase 144 — recorded deferrals" TODOS.md && grep -A2 "Phase 144 — recorded deferrals" TODOS.md | head -5</automated>
  </verify>
  <acceptance_criteria>
    - JOB-08 block carries the census table verbatim, the structural argument, both traps, and the
      pre-merge re-measure pointer; the checkbox is NOT flipped.
    - TODOS.md has exactly the three items under the Phase 144 heading; no deferral is implemented
      anywhere in the diff.
    - test_compute_jobs_fencing.py is untouched in this phase (git log confirms no commit touches it).
  </acceptance_criteria>
  <done>SC#4/JOB-08 is closed as a recorded, measured decision, and every consciously-deferred item has a named home in the single backlog.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo text -> CI verdict | the TS gate is pure file-text; a vacuous extraction would bless any body |
| .planning/ + TODOS.md -> public GitHub | the repo is PUBLIC and .planning/ is tracked; census numbers are fine, secrets are not |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-07 | Tampering | future edit silently weakening the deployed body | high | mitigate | Three-sibling occurrence counts + word-bounded bound + later-migration re-registration scan, all neuter-proven |
| T-144-08 | Repudiation | JOB-08 closed as an unmeasured skip | medium | mitigate | The REQUIREMENTS.md block carries the census verbatim + the structural argument + a fresh pre-merge re-measure in Plan 03 |
| T-144-09 | Information disclosure | committed artifacts in a public repo | medium | mitigate | Counts, dates and role names only — no connection strings, keys, or user data in REQUIREMENTS/TODOS |
| T-144-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- vitest gate green at HEAD, migration byte-unmodified
- Neuter-RED records extended (≥7 TS entries)
- JOB-08 block + three TODOS entries committed; no deferred idea implemented
</verification>

<success_criteria>
- The two-arm shape is pinned by three sibling gates (migration STEP 2, SQL gate Part 1, TS gate) whose counts agree and were each observed to fail.
- JOB-08 is decided on measured evidence with the measurement attached (SC#4).
</success_criteria>

<output>
Create `.planning/phases/144-job-wr-02-orphaned-running/144-02-SUMMARY.md` when done, including the
TS neuter-RED table and the exact REQUIREMENTS/TODOS hunks.
</output>
