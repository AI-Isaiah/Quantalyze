---
phase: 144-job-wr-02-orphaned-running
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql
  - supabase/tests/test_retention_orphaned_running.sql
  - .planning/phases/144-job-wr-02-orphaned-running/144-throwaway-harness.sql
autonomous: true
requirements: [JOB-05]
tags: [supabase, migration, pg_cron, plpgsql, job-queue, retention, sql-gate]

estimate:
  tokens: 100000
  raw_tokens: 100000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "The deployed cron body transitions a `running` row with `claimed_at` older than 4h to `failed_final` with `next_attempt_at = now()`, `error_kind = 'permanent'` and a fixed-literal `last_error` — the row SURVIVES (D-01, D-02, B3/§7)"
    - "A `running` row with `claimed_at IS NULL` and `created_at` older than 48h is terminalized identically by arm B (D-08); a 12h-old NULL-claim row is untouched"
    - "A 3h batch-tail `running` row is NEVER touched — the RT-01 regression arm is carried forward at the UNCHANGED 4h threshold (SC#2, D-05)"
    - "Executing the deployed body removes ZERO rows — count conservation over the seeded ids is asserted behaviorally, which is what makes 'never DELETE' checkable (D-01)"
    - "The jobname stays `retention_compute_jobs_orphaned_running` and the schedule becomes '50 * * * *' — a minute verified free on BOTH projects at plan time (D-06)"
    - "supabase/tests/test_retention_orphaned_running.sql is rewritten in 143's register IN THE SAME COMMIT as the migration (B1) — its hour-band ::INT cast is replaced by string equality on the schedule (B2)"
    - "The per-arm bound is proven by EXECUTING the deployed body against 101 seeded rows (100 oldest move, youngest survives, tick 2 progresses) — never by grepping for the token"
  artifacts:
    - "supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql — the ONLY migration; inline two-arm cron body, no DDL, no function, no policy, STEP 2 self-verify"
    - "supabase/tests/test_retention_orphaned_running.sql — rewritten: ungated structural Part 1, per-part BEGIN/ROLLBACK behavioral parts, executed bound part"
    - ".planning/phases/144-job-wr-02-orphaned-running/144-throwaway-harness.sql — stub schema + fake pg_cron adapted from the 143 harness, reused by Plans 02/03 for neuter-RED work"
  key_links:
    - "SET next_attempt_at = now() <-> retention_compute_jobs_failed's COALESCE(next_attempt_at, created_at) 90-day cutoff (20260515210200:255-259) — omit it and an old orphan is collected on the NEXT 03:30 tick, voiding SC#1's audit promise (B3/§7)"
    - "status = 'failed_final' <-> Phase 142 reaper's exclusion set (20260803130000:139-142) — failed_final is the ONLY terminal-failure value NOT in it, so the flip UNBLOCKS the reaper that writes the real user-facing analytics error (settled fact 2)"
    - "status = 'failed_final' <-> FINISHED_JOB_STATUSES (SyncPreviewStep.tsx:207-214) — the status flip ALONE satisfies SC#1's poller requirement; no frontend change, no new write (settled fact 4)"
    - "jobname continuity <-> the deployed cron entry on both projects and the rewritten SQL gate — renaming would strand both"
---

<objective>
Author Phase 144's single migration: re-register `retention_compute_jobs_orphaned_running` at
`'50 * * * *'` with a two-arm bounded terminal UPDATE (arm A: `claimed_at` past the UNCHANGED 4h
window; arm B: `claimed_at IS NULL` keyed on `created_at` past a DERIVED 48h window), replacing the
shipped DELETE body. Prove it end-to-end on a throwaway local Postgres (the tracer), then rewrite
`supabase/tests/test_retention_orphaned_running.sql` in 143's register and land migration + gate in
ONE commit (B1), then run the neuter-RED matrix so every new assertion is OBSERVED to fail.

Purpose: resolves WR-02 (JOB-05) — an orphaned `running` job terminates VISIBLY: the poller breaks
out on `failed_final`, the row survives for audit, and Phase 142's reaper is unblocked to write the
user-facing analytics error.
Output: the migration, the rewritten SQL gate (same commit), the reusable harness, and a recorded
neuter-RED table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-job-wr-02-orphaned-running/144-CONTEXT.md
@.planning/phases/144-job-wr-02-orphaned-running/144-RESEARCH.md
@.planning/phases/144-job-wr-02-orphaned-running/144-PATTERNS.md
@supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql
@supabase/migrations/20260720120000_retention_orphaned_running_window_4h.sql
@supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql
@supabase/tests/test_retention_orphaned_running.sql
@supabase/tests/test_reconcile_dropped_enqueue_sweep.sql
@.planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql
</context>

## Decision map (cited as D-NN below; source: 144-CONTEXT.md locked decisions, corrected at HEAD by 144-RESEARCH.md)

| ID | Locked decision honored here |
|----|------------------------------|
| D-01 | DELETE -> terminal UPDATE; the row must survive until jobid 8 collects it at 90 days; a removal-shaped janitor is forbidden |
| D-02 | Terminal status = `failed_final` (verified in the CHECK at 20260411144407:112-120; it is the only terminal-failure value outside 142's exclusion set AND outside the claimable set) |
| D-03 | "Write a user-facing reason" — CORRECTED at HEAD (RESEARCH §2): there is NO `user_message` column; the synthesized message is a pure function of (status, error_kind), so `error_kind = 'permanent'` selects the accurate arm; the verbose reason goes to `last_error` (operator/audit channel, hard-redacted from users); Phase 142's reaper writes the analytics-surface string once unblocked |
| D-04 | ONE NEW migration layered on 20260720120000; the shipped file is never edited; `cron.unschedule`-then-`cron.schedule` re-apply pattern |
| D-05 | Threshold UNCHANGED at 4 hours on `claimed_at` — the threshold, not the frequency, protects live jobs (WORKER-04) |
| D-06 | Cadence hourly at `'50 * * * *'` — verified free on BOTH projects (occupied minutes {0,5,10,15,20,30,35,45}); Plan 03 re-verifies live pre-apply, fallback `:25` |
| D-07 | Cadence-honesty header paragraph: the cadence is post-threshold DETECTION latency; worst case ~= 4h + 1h for the job row, plus 142's 16h for the analytics surface (~21h) — it does not bound user-visible wait |
| D-08 | Arm B INCLUDED: `claimed_at IS NULL` keyed on `created_at` with its OWN derived threshold (48h — see Task 1); a bare number is the Phase-106 revert class |
| D-09 | The NULL-claim writer is traced: RESEARCH §3 excludes every production writer at HEAD and attributes the rows to the live-DB fixture in test_compute_jobs_fencing.py — the header records this as TEST RESIDUE with evidence; no speculative production "writer fix" is shipped (fixture-hygiene follow-up routes to TODOS in Plan 02; live confirmation query runs in Plan 03) |
| D-10 | The invariant gate is BEHAVIORAL: the rewritten SQL gate seeds a NULL-claim aged row and asserts the deployed body terminalizes it — a global census gate was rejected as a shared-DB flake magnet |

## Blockers this plan closes

| ID | Blocker | Where |
|----|---------|-------|
| B1 | `test_retention_orphaned_running.sql:161-164` asserts the orphan row is GONE (count = 0) — encodes the superseded behavior and reds the moment the migration reaches TEST; its header prose at `:6-7`/`:21-23` argues the superseded rationale | Task 2 rewrites the file; same commit as the migration |
| B2 | `:110-114` casts cron schedule field 2 to INT — an hourly schedule makes that field `'*'` and the cast raises 22P02, a hard error | Task 2 replaces it with string equality on the full schedule |
| B3 | `retention_compute_jobs_failed` keys on `COALESCE(next_attempt_at, created_at) < now() - 90 days` and the claim RPC never advances `next_attempt_at` — a status-only flip lets an old orphan be collected the very next 03:30 tick | Task 1 puts `next_attempt_at = now()` in both SET lists; Task 2 pins it with a gate assertion |

<tasks>

<task type="tracer">
  <name>Task 1: Author the terminalizer migration + prove it end-to-end on a throwaway Postgres</name>
  <files>supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql, .planning/phases/144-job-wr-02-orphaned-running/144-throwaway-harness.sql</files>
  <precondition>A local Postgres 16 is available (the 143 tracer used PGBIN=/opt/homebrew/opt/postgresql@16/bin with a throwaway cluster on a TMPDIR socket — see the usage block at the top of 143-throwaway-harness.sql); if psql/initdb are absent, HALT and escalate rather than substituting a grep-only "proof"</precondition>
  <reversibility rating="reversible">Nothing is applied to any shared project in this plan; the file is repo-only until Plan 03 applies it to TEST via MCP and merge applies it to PROD</reversibility>
  <read_first>
    - 144-RESEARCH.md "## Code Examples" (the migration skeleton — shape only; every literal is sourced in §§1-8) and §4 (the 48h derivation), §5 (slot table), §6 (why NO re-enqueue), §7 (B3)
    - 144-PATTERNS.md §1 (header structure to copy from 20260816140000, section by section) and "Contradictions" (A) — the 142 NULL-stamp divergence that MUST be argued in the header
    - supabase/migrations/20260816140000_reconcile_dropped_enqueue_sweep.sql:577-773 (transaction frame, fail-loud pg_cron guard, unschedule-then-schedule, inline-literal discipline) and :775-930 (STEP 2 register: consequence-naming messages, occurrence counts, word-bounded LIMIT regex)
    - supabase/migrations/20260803130000_reaper_limit_bound_materialized_cte.sql:111-156 (the bounded terminal-UPDATE arm shape: MATERIALIZED CTE, ORDER BY ASC, LIMIT, FOR UPDATE SKIP LOCKED, compare-and-set fence on the outer UPDATE)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-throwaway-harness.sql (the stub-cron + minimal-schema pattern to adapt)
  </read_first>
  <action>
    (1) Write `supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql`
    (timestamp strictly greater than repo tip 20260816140000 — verify the tip with ls before writing).
    STEP 1 re-registers the EXISTING jobname `retention_compute_jobs_orphaned_running` (keeping the
    name is what keeps the deployed jobid and the SQL gate continuous) at `'50 * * * *'`, with the
    canonical frame: BEGIN + SET lock_timeout '5s', fail-loud pg_cron guard (RAISE EXCEPTION USING
    ERRCODE feature_not_supported — never the silent-skip arm), IF EXISTS unschedule, then schedule.
    The body is an INLINE dollar-quoted literal (no function, no parameter, no dynamic SQL — the
    caller-suppliable-INTERVAL incident class), one DO block with TWO sequential statements:
      - ARM A: WITH batch AS MATERIALIZED (SELECT id FROM public.compute_jobs WHERE status =
        'running' AND claimed_at IS NOT NULL AND claimed_at older than interval '4 hours', ORDER BY
        claimed_at ASC, LIMIT 100, FOR UPDATE SKIP LOCKED) feeding UPDATE public.compute_jobs SET
        status = 'failed_final', next_attempt_at = now(), error_kind = 'permanent', last_error =
        a fixed arm-A literal prefixed `orphaned_running_reaped:` naming the 4h claim window — with
        the trailing compare-and-set fence AND cj.status = 'running' on the outer UPDATE (142's
        pattern).
      - ARM B: same shape keyed on claimed_at IS NULL AND created_at older than interval '48 hours',
        ORDER BY created_at ASC, LIMIT 100, same four-column SET with a fixed arm-B literal naming
        the invariant-violation shape, same fence.
    Keep `status = 'running'` single-spaced everywhere (Pitfall 4 — the gate anchors on that exact
    spacing). SET exactly those four columns: claimed_at/claimed_by/attempts/metadata are preserved
    untouched (forensics; the M-0779 precedent; the 8KB metadata CHECK). The 142-style ON-UPDATE
    trigger stamps updated_at for free — say so in the header, write nothing for it. The body
    contains NO re-enqueue arm and never calls the enqueue RPC (§6: the orphan itself suppresses its
    replacement via the in-flight probe and partial unique index, both of which exclude failed_final
    — terminalizing RELEASES the slot and the next fan-out re-enqueues automatically; re-enqueueing
    from the janitor would duplicate for cron-fanned kinds and is unsafe for user-initiated ones).
    Keep the body literal free of every token the gates negative-assert (see Task 2's negative list)
    INCLUDING in body comments — prose about rejected shapes lives in the header, outside the
    dollar-quoted body, and the opening dollar tag is never written in any comment (the 143
    extraction-vacuity incident).
    (2) HEADER, in 143's register and section order (144-PATTERNS §1 lists the headings): why the
    migration exists (WR-02 resolution; TEST-need vs PROD-need reconciled by terminal UPDATE — the
    substance of SC#3); cadence honesty per D-07 with the full occupied-minute enumeration from
    RESEARCH §5 and why :50; threshold rationale — arm A 4h UNCHANGED (WORKER-04: threshold not
    frequency protects live jobs, D-05) and arm B 48h DERIVED per RESEARCH §4: 24h enqueue cadence
    (main_worker.py:1089) + 2.5h max batch wall-clock ~= 26.5h ceiling, rounded UP to the next whole
    cadence multiple => 48h, with each rejected import named (NOT 4h — bounds one claimed job; NOT
    16h — bounds a strategy_analytics chain; NOT 1h — bounds a route-commit gap) AND the honesty
    point stated verbatim in substance: arm B's threshold is NOT a live-job guard the way arm A's is
    (every claim stamps status and claimed_at in ONE statement, so no legitimate window exists) — it
    is margin against a future two-statement writer plus legibility; do not overclaim. Argue the 142
    DIVERGENCE explicitly: 20260802120000:475-476 says a NULL stamp is a writer bug — SKIP it; arm B
    REVERSES that call for THIS table because compute_jobs.created_at is NOT NULL DEFAULT now() (a
    real derivable anchor 142's row lacked) and because 142's rule is precisely what kept the
    measured TEST rows immortal for 14 days; name the divergence, never blend (D-08). Record the D-09
    finding with its evidence chain (RESEARCH §3: all SQL writers stamp claimed_at in the same
    statement; zero Python/TS writers; the only in-repo producer is the fencing-test fixture at
    test_compute_jobs_fencing.py:1138-1152/:1191-1205; PROD holds zero such rows => test residue,
    not a production bug; Plan 03 runs the confirming query live). Rejected-anchor bullets for arm B:
    claimed_at (NULL by construction), updated_at (the set_updated_at trigger re-stamps it on every
    write — the Phase-106 shape on this very table), next_attempt_at (a scheduling column, and the
    one B3 repurposes as the retention clock). B3 paragraph: why next_attempt_at = now() is in the
    SET list, citing 20260515210200:255-259 and the mig-109 convention note at :242-245. LIMIT
    derivation (⚠️ REVISED at plan-check; the earlier "100/500 strictly dominates the measured 402"
    framing is DELETED as snapshot-fitted — a bound justified by "bigger than today's backlog"
    becomes FALSE the moment the backlog grows, which is the Phase-106 revert lineage in softened
    form): the bound is sized to PER-TICK STATEMENT COST, not to any observed population. A 100-row
    UPDATE on public.compute_jobs fires the compute_jobs_set_updated_at_trigger once per row and is
    single-digit-millisecond work; both arms take FOR UPDATE SKIP LOCKED, so a live writer is never
    blocked and the only rows locked are by-predicate orphans nothing legitimate holds. Postgres
    would tolerate an order of magnitude more — safety therefore does NOT discriminate between
    candidate bounds, so the tie is broken on EVIDENCE. 100 is chosen DELIBERATELY BELOW the
    measured live TEST population (396 arm-A rows, census 2026-08-17) so that the bound's live
    behaviour is OBSERVABLE before merge: Plan 03 sees successive real ticks move exactly 100,
    oldest-first, then progress — bounded AND progressing, against real stuck data, under the real
    cron role with the real trigger and real RLS. That is the evidence class Phase 143 set with
    `healed 25` -> `healed 1`, and the offline harness structurally cannot reproduce it (no RLS, no
    real pg_cron). A bound at or above the population would drain everything in one tick and make
    that observation impossible. Drain cost of the choice: a day-scale backlog still clears within a
    working day at hourly cadence. Scope
    discipline: registers ONE cron job, no DDL/function/policy/index/grant; does NOT touch jobid 4
    (retention done), jobid 8 (retention failed), jobid 9 (derive fan-out — never unschedule), the
    claim RPC, or 142's reaper. Honesty notes: non-strategy-scoped orphans (api_key/allocator
    targets) are invisible to the wizard poller (get_user_compute_jobs filters on user linkage) —
    the header must not claim the poller sees every terminalized orphan; the chain-mid residual
    (terminalized orphan permanently excludes its strategy from 143's zero-jobs sweep) is NAMED and
    filed to TODOS in Plan 02, not fixed here. Operator observability: cron.job_run_details.
    return_message carries the COMMAND TAG, not NOTICE text (measured 2026-08-17) — count rows, not
    the log. Convention-deviation + invariant checklist and PROD-AUTO-APPLY warning copied from
    20260816140000's register with 144 content. Prose hygiene paragraph, both directions.
    (3) STEP 2 self-verify, read back from cron.job, every failure message naming the CONSEQUENCE:
    exactly ONE job row for the name; schedule compared by STRING equality to '50 * * * *' (never a
    ::INT cast — four fields are '*'); positive anchors and DELIBERATE occurrence counts for the
    two-arm body, each count's meaning stated in its message: public.compute_jobs = 4 (arm A CTE +
    arm A UPDATE target + arm B CTE + arm B UPDATE target — recalibrated from 143's one-arm 2, per
    the constraint), status = 'running' = 4 (two predicates + two compare-and-set fences),
    failed_final = 2, next_attempt_at = 2 (B3 — one per SET list), error_kind/permanent present,
    orphaned_running_reaped = 2, interval '4 hours' = 1, interval '48 hours' = 1, claimed_at IS NULL
    present, AS MATERIALIZED = 2 (one fence per arm), FOR UPDATE SKIP LOCKED present, and the bound
    pinned WORD-BOUNDED with the PL/pgSQL form `v_command !~ 'LIMIT[[:space:]]+100([^0-9]|$)'`
    (the substring form passes for LIMIT 1000 — the measured Phase 143 red-team finding; the |$ arm
    is required). Negative anchors on the body: the removal keyword the old janitor used (absent —
    this is what makes D-01 mechanically checkable at the deployed body; the RESEARCH skeleton's
    closing note calls it the one assertion not to omit), the IN-SELECT-LIMIT shape via the
    single-statement-window regex from 20260816140000:916, the enqueue RPC name (no re-enqueue),
    claimed_by (forensics preserved), and failed_retry (the wrong terminal — claimable AND inside
    142's exclusion set). AS MATERIALIZED is shape enforcement, never a bound proof — say so in the
    gate comment; the bound is proven only by (5).
    (4) Adapt the 143 harness into `.planning/phases/144-job-wr-02-orphaned-running/
    144-throwaway-harness.sql`: stub cron schema (cron.job table + schedule/unschedule upsert
    functions, copied from the 143 harness), plus MINIMAL real-constraint replicas of the tables the
    gate touches — compute_jobs with the status CHECK, error_kind CHECK, target-xor and per-key
    in-flight partial unique index copied verbatim with their source migration:line cited; auth.users
    / profiles / api_keys stubs sufficient for the gate's FK seed chain. Keep the 143 header's
    FIDELITY LIMITS discipline (no RLS — this harness cannot answer the cron-role question; Plan 03's
    real tick can; no real pg_cron; no triggers).
    (5) TRACER PROOF on a throwaway cluster (initdb into TMPDIR, per the 143 usage block): (a) run
    the migration BEFORE faking pg_cron — it MUST raise feature_not_supported (fail-loud proof);
    (b) apply harness then migration — STEP 2 must pass; (c) seed six directional rows (arm-A 5h
    orphan with a century-backdated next_attempt_at; 3h batch-tail; fresh running; aged done; arm-B
    NULL-claim 5-day; arm-B NULL-claim 12h) and EXECUTE the DEPLOYED command read from the stub
    cron.job — verify: the 5h and 5-day rows are failed_final with next_attempt_at advanced off the
    century-backdated seed, error_kind permanent, last_error non-null, claimed_at/claim_token
    preserved; the other four rows are byte-untouched; the total row count over the seeded ids is
    unchanged; (d) BOUND proof: seed 101 century-backdated arm-A rows via generate_series (distinct
    api_keys to clear the per-key in-flight index), EXECUTE once — exactly the 100 OLDEST move and
    the youngest survives (asserting WHICH rows moved is strictly stronger than counting); EXECUTE
    again — the survivor moves (forward progress). Record every observation for the SUMMARY. Do NOT
    commit yet — the commit lands in Task 2 together with the rewritten gate (B1's same-commit
    requirement).
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql && grep -c "interval '48 hours'" supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql && grep -c "next_attempt_at" supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql && psql -h "${TMPDIR:-/tmp}/144-tracer" -U postgres -d q144 -v ON_ERROR_STOP=1 -c "SELECT command FROM cron.job WHERE jobname = 'retention_compute_jobs_orphaned_running'" | head -3</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists at the exact filename; timestamp strictly greater than the repo tip.
    - Fail-loud proof observed (feature_not_supported raised on a cluster without the fake pg_cron).
    - STEP 2 passed on the harness apply; all six directional-arm observations and both bound-proof
      observations recorded verbatim for the SUMMARY.
    - The deployed body read back from the stub cron.job contains both arms, both fixed literals,
      the four-column SET twice, and none of the negative-list tokens.
    - Nothing committed yet (the same-commit constraint belongs to Task 2).
  </acceptance_criteria>
  <done>The one production artifact of this phase exists and its deployed body has been EXECUTED end-to-end against real rows on a real Postgres, both arms, both negatives, bound included.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite test_retention_orphaned_running.sql in 143's register (B1+B2) and commit gate + migration + harness in ONE commit</name>
  <files>supabase/tests/test_retention_orphaned_running.sql</files>
  <read_first>
    - supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:1-70 (the gate contract: ungated Part 1, per-part BEGIN / SET LOCAL lock_timeout / ROLLBACK, EXECUTE-the-deployed-body oracle, century-backdating, frozen-clock rule, identity-scoped assertions) and :740-848 (the dedicated bound part shape)
    - supabase/tests/test_retention_orphaned_running.sql (current file — the RT-01 3h batch-tail arm at :147-152/:175-182 and the non-running-survivor arm at :143-146/:170-174 are PRESERVED conceptually; the presence gates at :71-83, the hour-band cast at :110-114, and the removal-oracle at :158-182 are all REPLACED)
    - 144-RESEARCH.md §8 (the two breaks + the seven-item minimum assertion list) and 144-PATTERNS.md §2 (the convention table, including why this cron is deliberately NOT added to test_retention_crons_safe.sql)
  </read_first>
  <action>
    Rewrite the file top to bottom in 143's register; this is a REWRITE, not a patch:
    (1) HEADER: keep the Rule-9 "why this cron exists" story (the workerless-TEST re-pollution and
    the fence-collision mechanism) but REPLACE the superseded removal rationale at :6-7/:21-23 with
    the terminal-UPDATE rationale — a failed_final row ends the daily re-pollution just as removal
    did, because failed_final leaves BOTH the claim RPC's partition-dedupe predicate
    (20260719073701:159-179) and the claimable set (:204); and it resolves WR-02 (the :34-36
    "founder-deferred, resolved at FLIP-01" sentence is superseded — Phase 144 IS the resolution).
    Leaving either stale paragraph is the one-file-scope-amendment failure mode.
    (2) PART 1 — STRUCTURAL, UNGATED, no transaction, no skip arm: it MUST red when the migration is
    unapplied (this is the designed TDD RED that Plan 03 observes in CI pre-apply; the old :71-83
    presence gates that NOTICE-and-RETURN are the named anti-pattern and are dropped). Assert:
    exactly one cron.job row for the jobname; schedule EQUALS '50 * * * *' by string comparison
    (B2 — never a ::INT cast on a schedule field); then the same positive-anchor / occurrence-count
    / word-bounded-bound / negative-anchor battery as the migration's STEP 2 (Task 1 step 3 lists
    every count and its meaning — the two files are siblings and their counts move together), with
    every failure message naming the CONSEQUENCE.
    (3) PART 2 — BEHAVIORAL (skips ONLY on genuinely absent pg_cron; a missing cron job with pg_cron
    present is an EXCEPTION, not a skip). Per-part BEGIN; SET LOCAL lock_timeout = '5s'; ...
    ROLLBACK. Seed the six directional rows from Task 1 step 5c through the real FK chain
    (auth.users -> profiles -> api_keys, distinct keys per running row to clear the per-key
    in-flight index; mirror the census shape for arm-B seeds — non-NULL claim_token, and an
    EXPLICIT century-backdated next_attempt_at on the arm-A positive seed so the B3 assertion can
    fail). EXECUTE the DEPLOYED command read from cron.job (the oracle — never a re-typed
    predicate), then assert identity-scoped: (a) arm-A 5h row PRESENT with status failed_final,
    next_attempt_at advanced past the backdated seed (never compare two now()-derived values — the
    frozen-clock rule), error_kind permanent, last_error non-null, claimed_at still set; (b) 3h
    batch-tail row untouched and still running (the RT-01 regression, carried forward per SC#2);
    (c) fresh running untouched; (d) aged done untouched; (e) arm-B 5-day NULL-claim row
    terminalized identically (D-10's behavioral invariant gate); (f) arm-B 12h row untouched;
    (g) COUNT CONSERVATION: the count over ALL seeded ids equals the seeded total — the assertion
    that makes D-01 behaviorally checkable, not just textually.
    (4) PART 3 — THE BOUND, executed: seed 101 century-backdated arm-A rows via generate_series
    (101 api_keys + 101 compute_jobs inside the rolled-back part; century-backdating makes the seeds
    outrank any foreign TEST rows under ORDER BY claimed_at ASC, so the budget is deterministic on
    the shared project), EXECUTE once, assert exactly 100 of the seeded ids moved AND the youngest
    seed survived (WHICH rows, not just how many), EXECUTE again, assert the survivor moved.
    (5) Do NOT add this cron to test_retention_crons_safe.sql, and say why in a comment: its
    expected_jobs register loop asserts every body matches a created_at-keyed removal shape that
    this two-arm UPDATE body does not have — adding it would make the register look complete while
    pinning nothing this phase cares about (144-PATTERNS §2, last row).
    (6) No psql backslash meta-commands anywhere including comments; no fixed UUID literals;
    gen_random_uuid() everywhere.
    (7) RUN the full rewritten gate against the Task-1 harness cluster — it must pass GREEN end to
    end there (on TEST it stays RED at Part 1 until Plan 03 applies; that is designed).
    (8) COMMIT — ONE commit containing the migration, this rewritten gate, and the harness file
    (B1's same-commit requirement; message in the repo convention, e.g. feat(144-01): terminalize
    orphaned-running compute_jobs + rewrite the gate in the same commit). Verify with git show
    --stat that all three files are in that single commit.
  </action>
  <verify>
    <automated>psql -h "${TMPDIR:-/tmp}/144-tracer" -U postgres -d q144 -v ON_ERROR_STOP=1 -f supabase/tests/test_retention_orphaned_running.sql && git show --stat HEAD | grep -c "supabase" </automated>
  </verify>
  <acceptance_criteria>
    - Part 1 is ungated and structurally reds when the cron job is absent (proven in Task 3's neuter
      matrix by pointing it at a cluster without the migration).
    - The schedule assertion is string equality; no ::INT cast survives anywhere in the file.
    - All seven RESEARCH §8 minimum assertions are present: arm-A positive, RT-01 3h negative, arm-B
      positive, arm-B negative, non-running negative, count conservation, schedule + 4h-threshold
      retention.
    - The gate passes green against the harness cluster with the migration applied.
    - git show --stat HEAD lists the migration, the rewritten gate, and the harness in ONE commit.
  </acceptance_criteria>
  <done>The shipped gate no longer encodes the superseded behavior anywhere (prose included), it cannot crash on the hourly schedule, and it lands in the same commit as the migration it guards.</done>
</task>

<task type="auto">
  <name>Task 3: Neuter-RED matrix — observe every new assertion fail, restore, record</name>
  <files>.planning/phases/144-job-wr-02-orphaned-running/144-throwaway-harness.sql</files>
  <read_first>
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-03-SUMMARY.md (the recorded neuter-RED table format — nine observed REDs; Phase 143 found two vacuous gates this way, one written ten minutes earlier)
  </read_first>
  <action>
    On the harness cluster, for each neuter below: deploy a VARIANT body (via the stub cron.schedule
    upsert — the repo files are never edited for this), run the migration STEP 2 logic and/or the
    rewritten SQL gate, OBSERVE the actual RED output, restore the real body, re-run to GREEN, and
    record (neuter, gate part, observed message) in a table for the SUMMARY. Minimum matrix:
    (1) the OLD 20260720120000 removal body deployed -> Part 1 occurrence counts red AND Part 2
    count-conservation red (the D-01 pair); (2) next_attempt_at dropped from both SET lists ->
    Part 2 arm-A B3 assertion red; (3) LIMIT widened to 1000 -> the word-bounded bound assertion
    red (the substring form would have stayed green — state that in the record); (4) arm B removed
    entirely -> public.compute_jobs count red (4 -> 2) and Part 2 arm-B behavioral red; (5) window
    narrowed to interval '2 hours' -> the RT-01 3h batch-tail arm red (the carried-forward SC#2
    regression); (6) terminal status switched to the claimable failed-retry value -> the
    failed_final count red and the negative anchor red; (7) schedule changed to a different minute
    -> the string-equality schedule assertion red; (8) Part 1 run against a cluster WITHOUT the
    migration applied -> Part 1 reds structurally (proves it is genuinely ungated — the old file's
    green-skip is the named anti-pattern). If any assertion stays GREEN under its neuter, the
    assertion is vacuous: fix the assertion in a follow-up commit (gate files only — the migration
    is already committed; if the MIGRATION's STEP 2 itself is vacuous, amend it before Plan 03
    applies anything, since nothing has left the repo yet) and re-observe. Every fix commit message
    names the vacuity it closes.
  </action>
  <verify>
    <automated>psql -h "${TMPDIR:-/tmp}/144-tracer" -U postgres -d q144 -v ON_ERROR_STOP=1 -f supabase/tests/test_retention_orphaned_running.sql</automated>
  </verify>
  <acceptance_criteria>
    - All eight neuters observed RED with their actual output captured; final state GREEN with the
      real body restored.
    - Zero assertions found vacuous, or each vacuity fixed and re-observed with its own commit.
    - The recorded table is ready for the SUMMARY (Plan 02 extends it with the TS-gate neuters).
  </acceptance_criteria>
  <done>Every gate in the commit has been seen to fail for the reason it exists, not assumed to.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| merge -> PROD | supabase/migrations/** on main auto-applies to PROD; the body written here runs unattended against production compute_jobs |
| pg_cron role -> compute_jobs | BYPASSRLS role writes through FORCE RLS — proven by 143's live tick, inherited not re-litigated |
| gate seeds -> shared TEST | the rewritten gate seeds and rolls back on a project other PRs' CI reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-01 | Denial of service | janitor body vs production job rows | critical | mitigate | UPDATE-only body; STEP 2 + gate negative anchor on the removal keyword; Part 2 count-conservation assertion makes it behavioral (D-01) |
| T-144-02 | Denial of service | unbounded lock hold during a backlog | high | mitigate | MATERIALIZED CTE per arm + word-bounded LIMIT 100 assertions + the EXECUTED 101-row bound proof (grep gates alone are the D-19 defect) |
| T-144-03 | Tampering | audit trail vs retention_compute_jobs_failed | high | mitigate | next_attempt_at = now() in both SET lists, pinned by a behavioral assertion (B3) |
| T-144-04 | Information disclosure | last_error in a public-repo-adjacent audit channel | medium | mitigate | Fixed literals only, no identifiers/row data; last_error is hard-redacted from users at the RPC and zod layers already |
| T-144-05 | Elevation of privilege | callable SQL surface | high | mitigate | Inline dollar-quoted body: no function, no SECURITY DEFINER, no grant, no caller-suppliable interval |
| T-144-06 | Denial of service | poison-job resurrection via janitor re-enqueue | high | mitigate | No INSERT/enqueue arm; negative anchor on the enqueue RPC name; recovery rides the existing fan-outs + user Retry (§6) |
| T-144-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase (RESEARCH Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Harness tracer: both arms executed end-to-end, six directional rows + 101-row bound, observations recorded
- Rewritten gate green on harness, structurally red without the migration
- ONE commit carries migration + gate + harness (git show --stat)
- Neuter-RED table complete, eight rows, zero unresolved vacuities
</verification>

<success_criteria>
- SC#1 mechanism exists and is proven offline: >4h claimed orphan -> failed_final, row survives, next_attempt_at advanced.
- SC#2 held: 3h batch-tail untouched at the unchanged 4h threshold; cadence literal is hourly.
- SC#3 held: one NEW migration layered on 20260720120000; the shipped file untouched (verify with git diff — no hunk touches 20260720120000 or 20260719120000).
- B1/B2/B3 all closed in this plan's single commit.
</success_criteria>

<output>
Create `.planning/phases/144-job-wr-02-orphaned-running/144-01-SUMMARY.md` when done, including the
tracer observations, the neuter-RED table, and the exact commit hash carrying the same-commit pair.
</output>
