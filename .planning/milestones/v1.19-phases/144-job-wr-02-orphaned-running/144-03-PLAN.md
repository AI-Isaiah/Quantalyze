---
phase: 144-job-wr-02-orphaned-running
plan: 03
type: execute
wave: 3
depends_on: [144-01, 144-02]
files_modified:
  - .planning/phases/144-job-wr-02-orphaned-running/144-CENSUS.md
autonomous: false
requirements: [JOB-05, JOB-08]
tags: [supabase-mcp, test-db, pg_cron, blocking, human-verify, orchestrator-session-only]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low   # 0 calibration samples; factor 1.0 applied

must_haves:
  truths:
    - "The migration is applied to TEST (qmnijlgmdhviwzwfyzlc) BEFORE merge, via Supabase MCP — never `supabase db push`, never merge-first (merging supabase/migrations/** to main AUTO-APPLIES to PROD khslejtfbuezsmvmtsdn)"
    - "The sql-tests CI gate went RED on the rewritten Part 1 pre-apply and GREEN post-apply — the designed TDD RED, observed with run URLs"
    - "Real :50 ticks on TEST drained the LIVE stuck population — arm A's ~396 claimed derive_broker_dailies rows and arm B's 6 NULL-claim poll_positions rows — verified by QUERYING THE ROWS (return_message carries only the command tag), with population conservation proving nothing was removed"
    - "The pre-apply census re-ran ALL numbers (running by arm, pending on PROD for SC#4) and re-verified minute :50 free in live cron.job on BOTH projects — dated claims re-measured at HEAD, not trusted"
    - "The §3 confirming query ran against the 6 NULL-claim rows BEFORE the first tick consumed them; a kill-criteria hit (a real production writer) is a hard STOP, not a footnote"
    - "A human reviewed the evidence bundle and approved BEFORE anything merges — merge is the one-way door to PROD"
  artifacts:
    - ".planning/phases/144-job-wr-02-orphaned-running/144-CENSUS.md — pre-apply census, slot check, §3 verdict, tick evidence, post-tick census"
  key_links:
    - "cron.job live slot SELECT <-> the '50 * * * *' literal in the committed migration — a collision found live means STOP and a coordinated three-sibling edit (migration + SQL gate + TS gate schedule literals in ONE commit), never a silent minute change"
    - "post-tick row queries <-> SC#1's promises: status failed_final, next_attempt_at ~ tick time (B3), rows PRESENT (D-01)"
    - "the §3 confirming query <-> the migration header's test-residue attribution (D-09) — the header claim must match the live verdict at merge time"
---

<objective>
Discharge the evidence no offline run can produce, in the ORCHESTRATOR SESSION (the Supabase MCP is
stripped from subagents — Phase 143 Plan 04 hit exactly this): re-census live state, settle the §3
attribution on the real rows, apply the migration to TEST via MCP, observe the SQL gate flip
RED->GREEN in CI, watch real :50 ticks terminalize the live 402-row fixture, and hold a blocking
human gate before merge (merge auto-applies to PROD).

Purpose: the live TEST population is a rare real-data fixture for BOTH arms — richer than any seeded
probe — and PROD auto-apply means everything must be proven before the merge, not after.
Output: 144-CENSUS.md evidence bundle, RED->GREEN URLs, tick evidence, §3 verdict, human approval.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/144-job-wr-02-orphaned-running/144-CONTEXT.md
@.planning/phases/144-job-wr-02-orphaned-running/144-RESEARCH.md
@.planning/phases/144-job-wr-02-orphaned-running/144-01-SUMMARY.md
@.planning/phases/144-job-wr-02-orphaned-running/144-02-SUMMARY.md
@supabase/migrations/20260817120000_retention_orphaned_running_terminalize.sql
</context>

## Decision map (cited as D-NN below; source: 144-CONTEXT.md locked decisions; same table as 144-01-PLAN.md)

| ID | Locked decision honored here |
|----|------------------------------|
| D-01 | The live-tick verification includes population CONSERVATION: every pre-tick `running` id still exists post-tick, status flipped — the never-remove promise observed on real data |
| D-06 | Minute :50 re-verified in live cron.job on BOTH projects immediately pre-apply; fallback :25 requires a STOP + coordinated three-sibling edit, never an ad-hoc change |
| D-09 | The §3 confirming query runs BEFORE the first tick (the tick consumes the evidence); its verdict is recorded either way, and a kill-criteria hit stops the merge |
| D-11 | The PROD stale-pending count is re-measured pre-merge so the committed JOB-08 WON'T-FIX rests on a fresh number |
| D-12 | Nothing in this plan removes a pending row or unschedules jobid 9 |

## ⛔ Session + safety constraints (read before Task 1)

- **ORCHESTRATOR SESSION ONLY.** Every task here needs Supabase MCP (apply_migration, execute_sql),
  which subagents do not have. If MCP tools are absent in this session, HALT — do not substitute
  `supabase db push` (forbidden) or psql-as-user (it proves nothing about the cron role).
- Merging `supabase/migrations/**` to main AUTO-APPLIES to PROD. Nothing merges before Task 3's
  human approval.
- Never `cron.unschedule(9)`. Never remove a `pending` row. Never edit the shipped `20260720120000`.
- MCP apply_migration stamps now() in the remote ledger — the filename-vs-ledger drift is the known
  PR-Y2 class: record it, never "fix" it by renaming the repo file.

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Pre-apply census + §3 verdict + slot check, then MCP apply + CI RED->GREEN</name>
  <files>.planning/phases/144-job-wr-02-orphaned-running/144-CENSUS.md</files>
  <precondition>Supabase MCP tools are available in THIS session (stripped from subagents); the 144-01/144-02 commits are on the branch and pushed so CI has run the rewritten gate</precondition>
  <reversibility rating="costly">Applying to TEST is undoable (unschedule + ledger row) but pollutes the shared-TEST migration history and starts real ticks against real rows; the true ONE-WAY door (merge -> PROD auto-apply) is NOT taken in this plan — it is gated behind Task 3's human approval</reversibility>
  <read_first>
    - 144-RESEARCH.md §3 "The one query that settles it" (the confirming query + its confirm/kill criteria, verbatim) and §5's UNVERIFIED note (why the live cron.job SELECT must run before trusting :50)
    - .planning/phases/143-job-dropped-enqueue-reconciliation-sweep/143-04-SUMMARY.md (the census-part/evidence-bundle format that worked)
  </read_first>
  <action>
    All DB access via Supabase MCP execute_sql, read-only in this task except the apply itself.
    (1) PRE-APPLY CENSUS on BOTH projects, recorded in a new 144-CENSUS.md (counts/dates/kinds only
    — the repo is public): compute_jobs by status; `running` decomposed into claimed vs
    claimed_at-IS-NULL with kind, created_at range, and (for claimed rows) claimed_at age range;
    PROD `pending` count (the SC#4/D-11 fresh number — if it is ever NON-zero, record it and flag
    for the human gate: the committed WON'T-FIX's premise has changed). Expected shape per the
    2026-08-17 plan-time census: PROD 0/0, TEST ~2819 pending / ~402 running (396 claimed
    derive_broker_dailies + 6 NULL-claim poll_positions) — re-measure, never assume.
    (2) §3 CONFIRMING QUERY (D-09), BEFORE any apply — the first tick will consume these rows: run
    RESEARCH §3's SELECT over the NULL-claim rows on TEST and evaluate its explicit criteria.
    CONFIRMS test-residue: non-NULL claim_token, exchange okx, priority normal, claimed_by NULL,
    strategies named p97-fence-test-%. KILLS: claim_token NULL, or claimed_by set, or
    metadata->>'enqueued_by' = 'daily_loop', or non-fence-test strategy names. On a kill: STOP the
    plan — there IS an unknown production writer, the migration header's D-09 attribution is false
    at HEAD, and the phase must not merge over an undiagnosed invariant violation; escalate with the
    row data (this is the Rule-6 gate). On confirm: record the verdict verbatim.
    (3) SLOT CHECK (D-06): SELECT jobid, jobname, schedule FROM cron.job on BOTH projects. Confirm
    minute 50 is free and the occupied set still matches RESEARCH §5. If :50 is taken: STOP — the
    fallback is :25, but changing the minute is a coordinated edit to the migration + SQL gate + TS
    gate schedule literals in ONE commit, re-running the affected neuter-REDs; never an ad-hoc apply
    of a body that disagrees with the committed files.
    (4) PRE-APPLY CI RED: confirm the branch's latest sql-tests run FAILED on the rewritten
    test_retention_orphaned_running.sql Part 1 (cron job/schedule mismatch — the old daily DELETE
    job is still deployed on TEST at this point, so Part 1's '50 * * * *' equality and occurrence
    counts red against it). Capture the run URL + failing output. If sql-tests skipped entirely,
    STOP and escalate — a skipped gate is not evidence. (Known interference: the daily 05:30 UTC
    TEST-DB backlog mechanism reddens `python` deterministically — that is a DIFFERENT job; judge
    sql-tests on its own output.)
    (5) APPLY via MCP apply_migration against TEST with the migration file's exact committed
    content. Record the ledger name drift (PR-Y2 class).
    (6) POST-APPLY VERIFY via execute_sql: exactly one cron.job row for the jobname, schedule
    '50 * * * *', active; the command text contains both arms (spot-check the two interval literals
    and the four-column SET — read, do not re-type).
    (7) POST-APPLY CI GREEN: re-run the branch CI; sql-tests must now pass the rewritten gate
    end-to-end (Parts 1-3 against the real TEST schema, real RLS, real constraints). Capture the
    run URL. Append everything to 144-CENSUS.md.
  </action>
  <verify>
    <automated>grep -c "verdict" .planning/phases/144-job-wr-02-orphaned-running/144-CENSUS.md && gh run list --branch "$(git branch --show-current)" --workflow ci.yml --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
  </verify>
  <acceptance_criteria>
    - 144-CENSUS.md carries: both-project status census, the running decomposition, the fresh PROD
      pending number, the §3 verdict with the raw criteria outcomes, the slot table, and the
      RED-then-GREEN run URLs.
    - The migration on TEST is byte-equivalent to the committed file (read back, not assumed).
    - Any STOP condition that fired is recorded as the outcome — a STOP satisfies this task; a
      worked-around STOP fails it.
  </acceptance_criteria>
  <done>TEST is running the new janitor, CI proves the gate is real (RED observed, then GREEN), and every plan-time claim this phase rests on has been re-measured live.</done>
</task>

<task type="auto">
  <name>Task 2: [BLOCKING] Observe real :50 ticks drain the live 402-row fixture — by querying rows</name>
  <files>.planning/phases/144-job-wr-02-orphaned-running/144-CENSUS.md</files>
  <precondition>Task 1 complete (cron active on TEST at :50). ⚠️ Wall-clock REVISED at plan-check: ticks land at minute 50 and this task now requires at least TWO successive real ticks to observe bounded-AND-progressing against the 396-row arm-A fixture, so it spans up to ~2h05m of waiting (not ~65 min). That cost buys the phase's headline evidence and is deliberate. Do NOT simulate the tick, do NOT hand-invoke the DO block to "catch up", and do NOT substitute an EXECUTE-as-psql-user run — the cron-role write through FORCE RLS is what a real tick proves; 143 proved the role CAN write compute_jobs, this observes that it DID, with this body and this bound. The residual ~196 rows drain on later ticks and are explicitly NOT a merge gate.</precondition>
  <read_first>
    - 144-RESEARCH.md "Runtime State Inventory" (the live-fixture expectations: arm A ~396 targets, arm B 6) and §5's cadence-honesty numbers (what may and may not be claimed about latency)
  </read_first>
  <action>
    Via MCP execute_sql on TEST, read-only throughout (the janitor does the writing):
    (1) SNAPSHOT pre-tick: capture the full id list of `running` rows (id, kind, claimed_at,
    created_at, next_attempt_at) — this is the conservation baseline.
    (2) WAIT for the :50 tick (poll after :51). ⚠️ PRIMARY PATH (revised at plan-check — this is the
    phase's headline evidence, not a fallback): the measured arm-A population (~396) EXCEEDS the
    per-arm bound of 100 BY DESIGN, so a single tick must NOT drain it. Observe at minimum TWO
    successive real ticks and assert, for each:
      - EXACTLY 100 arm-A rows moved (never 99, never 101) — the bound HOLDS;
      - they are the OLDEST by claimed_at, and the id set is DISJOINT from the previous tick's —
        the sweep PROGRESSES rather than re-processing the same head;
      - the remaining `running` count falls by exactly 100 each tick.
    Arm B's 6 NULL-claim rows are below its bound and are expected to clear in tick 1; that is arm
    B's positive, not a bound observation (arm B's bound is unobservable live at any plausible
    number — say so, do not imply otherwise).
    ⭐ Two ticks establish bounded-AND-progressing on REAL stuck data under the REAL cron role, with
    the real trigger and real RLS — the evidence class Phase 143 set with `healed 25` -> `healed 1`,
    and the one thing the offline harness structurally cannot produce. The remaining ~196 rows drain
    on later ticks and are NOT a merge gate; record the trajectory and move on.
    ⛔ If ONE tick drains everything, that is a BOUND FAILURE, not a convenience — STOP and
    investigate: it means the deployed body does not carry LIMIT 100.
    (3) VERIFY BY QUERYING ROWS, never the run log (cron.job_run_details.return_message carries the
    command tag only — record it once to confirm the tick ran and succeeded, then leave it):
    (a) CONSERVATION (D-01): every pre-tick id still EXISTS; zero rows vanished. (b) TRANSITION:
    moved rows have status failed_final, error_kind permanent, last_error carrying the
    orphaned_running_reaped prefix with the correct per-arm text, next_attempt_at within the tick
    window (B3 — this is what keeps jobid 8 from collecting them for 90 days), claimed_at and
    claim_token PRESERVED. (c) ARM ATTRIBUTION: the NULL-claim rows moved via arm B (their
    last_error carries the arm-B literal), the claimed rows via arm A. (d) NON-INTERFERENCE:
    `pending` count on TEST unchanged by the tick (the janitor never touches pending — D-12);
    jobid 9 still scheduled.
    (4) OBSERVATION-ONLY note (ROADMAP: NOT an acceptance criterion): record whether the fence-test
    partition-dedupe collision surface shrank (the terminalized rows leave the claim RPC's dedupe
    predicate). Do not gate on it.
    (5) Append tick evidence + post-tick census to 144-CENSUS.md. If any moved row is missing, or
    any row vanished, or next_attempt_at did not advance: STOP, do not merge, escalate with the row
    data — these are the exact SC#1 promises, observed failing.
  </action>
  <verify>
    <automated>grep -c "conservation" .planning/phases/144-job-wr-02-orphaned-running/144-CENSUS.md</automated>
  </verify>
  <acceptance_criteria>
    - Pre-tick snapshot, tick record (start_time/status/command-tag), and post-tick row evidence all
      in 144-CENSUS.md.
    - Conservation held over the full pre-tick id list; transitions verified column-by-column
      including next_attempt_at (B3) and preserved forensics (claimed_at, claim_token).
    - Both arms observed acting on their real populations (or the bound's behavior observed across
      two ticks, recorded as such).
    - A failed promise = a recorded STOP, not a merged phase.
  </acceptance_criteria>
  <done>SC#1 has been observed on live data: orphans terminated visibly, nothing removed, audit trail retention-safe — the WR-02 resolution is measured, not asserted.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human gate — evidence review before the one-way merge</name>
  <what-built>
    Phase 144 end-to-end: the two-arm terminalizer migration live on TEST at '50 * * * *' (Plan 01),
    the rewritten SQL gate + TS gate with observed neuter-REDs (Plans 01-02), the JOB-08
    WON'T-FIX-with-measurement and three TODOS deferrals (Plan 02), and this plan's live evidence:
    fresh census, §3 verdict on the real NULL-claim rows, RED->GREEN CI pair, and real ticks
    draining the live 402-row fixture with conservation and B3 verified row-by-row.
  </what-built>
  <how-to-verify>
    Review 144-CENSUS.md + the plan SUMMARYs:
    1. The RED (pre-apply) and GREEN (post-apply) sql-tests run URLs.
    2. The tick evidence: pre-tick snapshot vs post-tick rows — failed_final, next_attempt_at
       advanced, claimed_at/claim_token preserved, ZERO rows vanished.
    3. The §3 verdict: the NULL-claim rows matched the test-residue criteria (or the recorded STOP).
    4. The fresh PROD numbers: running = 0 (the janitor's first PROD tick should move ZERO rows —
       PROD quietness is expected, not proof; the TEST evidence is the proof) and pending = 0 (the
       committed JOB-08 WON'T-FIX premise, re-confirmed).
    5. The slot table: :50 free on both projects at apply time.
    Approving authorizes /ship (PR + merge). Merge AUTO-APPLIES the migration to PROD — after merge,
    watch the first PROD :50 tick via cron.job_run_details (expect status succeeded, command tag
    'DO', zero rows moved) and spot-check that PROD compute_jobs counts are unchanged. That
    post-merge watch is the operator handoff, recorded in the SUMMARY.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize ship, or describe issues (a vanished row, a stale next_attempt_at, a §3 kill-criteria hit, or a census number that moved)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| MCP session -> TEST database | the apply + real ticks mutate a SHARED project other PRs' CI reads |
| merge -> PROD | supabase/migrations/** on main auto-applies; no separate deploy step, no prompt |
| evidence artifacts -> public repo | 144-CENSUS.md is world-readable on push |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-144-10 | Denial of service | first ticks against the live TEST backlog | high | mitigate | LIMIT 100 bounds each arm per tick; the pre-tick snapshot + conservation check catch any removal-shaped defect BEFORE merge |
| T-144-11 | Tampering | merging over a live-falsified claim (§3 kill, slot collision, non-zero PROD pending) | critical | mitigate | Explicit STOP conditions in Tasks 1-2; the human gate reviews the raw evidence, not a summary verdict |
| T-144-12 | Repudiation | "the tick worked" asserted from return_message | medium | mitigate | return_message is the command tag only (measured); all verification queries rows |
| T-144-13 | Information disclosure | census/evidence in a public repo | high | mitigate | Counts, kinds, dates, jobnames only — no connection strings, keys, tokens, or user data; strategy names recorded only for the fence-test probe pattern match |
| T-144-SC | Tampering | npm/pip/cargo installs | high | mitigate | ZERO packages installed this phase |
</threat_model>

<verification>
- 144-CENSUS.md complete: census, §3 verdict, slot table, RED->GREEN URLs, tick evidence, post-tick census
- All STOP conditions either did not fire or are recorded as the outcome
- Human approval received before any merge
</verification>

<success_criteria>
- All four ROADMAP SCs hold with live evidence: SC#1 observed on real orphans; SC#2's threshold untouched and batch-tail safety carried by the gate; SC#3's single-behavior reconciliation deployed to TEST and merge-ready for PROD; SC#4's WON'T-FIX resting on a fresh pre-merge measurement.
- The phase is merge-ready and the PROD consequence (auto-apply; expected zero-row first tick) is understood and human-acknowledged.
</success_criteria>

<output>
Create `.planning/phases/144-job-wr-02-orphaned-running/144-03-SUMMARY.md` when done, including the
evidence bundle references and the operator handoff note (watch the first PROD :50 tick post-merge).
</output>
