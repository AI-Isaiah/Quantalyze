---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
verified: 2026-08-19T10:49:49Z
verified_at_head: 47022a6f7d88290ac635fcdf27e6c4bd1b3f11dd
phase_merged_as: a6a2dee8 (PR #692, v0.67.0.0)
status: passed
score: 12/13 findings ACHIEVED (1 PARTIAL)
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "C1 — the 200 resolve echo tells the user it compared only row count and boundary dates, not the individual daily values"
    status: partial
    reason: >-
      The honest sentence SHIPPED, is gated by four tests including an
      absence assertion that survives concatenation, and the false
      \"presence is proven\" comment is gone. But the sentence is carried on
      the 200 success envelope only, and NOTHING consumes it there.
      `CsvSubmitStep.tsx:322` reads `human_message` exclusively inside
      `if (!res.ok && !isIdempotentSuccess)`. The resolve echo is
      `200 / ok:true`, so it takes the success path and the field is
      dropped. A grep of every `human_message` consumer under
      `src/app/(dashboard)/strategies/` and `src/components/` finds no
      success-path reader. The founder chose \"copy that admits the echo did
      not re-verify interiors\" over a content hash; the admission exists in
      the API contract and is unreachable by the person it was written for.
    artifacts:
      - path: "src/app/api/strategies/csv-finalize/route.ts"
        issue: "humanMessage set at :1172-1178 and emitted at :1959-1961 — correct, and the only end of the link that exists."
      - path: "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx"
        issue: "Reads data.human_message only in the error arm (:332). The 200 echo path never renders it."
    missing:
      - "A success-path render of the echo's human_message in CsvSubmitStep (or an explicit, written decision that the API-contract-only placement is the intended end state, filed in TODOS.md)."
    severity: warning
    blocks_milestone_close: false
deferred: []
human_verification:
  - test: "Post-merge PROD verification of the B4 sweep — re-count `orphaned_running_reaped:%` on PROD and observe ONE real `cron.job_run_details` tick of `reconcile_dropped_enqueue_sweep` after 20260819130500."
    expected: "Marker count still 0 (or, if non-zero, the eligible-strategy delta is understood); one tick of the re-scheduled job recorded with a succeeded status."
    why_human: "The ROADMAP's 146.1-08 line names this as the post-merge half of the migration gate. 146.1-08-SUMMARY.md records the PRE-merge census and rehearsal only, and closes with 'Nothing has been applied to PROD.' Both migrations HAVE now applied (the `apply` job on a6a2dee8 is green and its log shows both files pushed), so the post-merge half is outstanding. This verifier is fenced out of the database."
  - test: "B4-BLANKET neuter — widen the deployed sweep predicate on TEST to the blanket 'no NON-TERMINAL row' form and confirm arms C2 AND C3 both redden."
    expected: "Both arms RED under the blanket form, GREEN under the shipped narrow marker predicate."
    why_human: "146.1-03-SUMMARY and 146.1-08-SUMMARY both name this neuter as the DECISIVE evidence for choosing the narrow predicate, and both record it as NOT RUN (it requires mutating the deployed function body on the shared TEST project). What IS proven is presence of the narrow predicate and absence of both blanket spellings in the deployed `cron.job.command`."
  - test: "C1 delivery decision — is the echo's honest sentence meant to reach the user, or is the API contract the intended end state?"
    expected: "A founder call, then either a success-path render or a TODOS entry closing the question."
    why_human: "This is the single gap above. It is a product/copy decision, not a defect with one correct repair."
---

# Phase 146.1: v1.19 xhigh close-out — Verification Report

**Phase Goal (ROADMAP.md:284-289):** The 15-finding v1.19 xhigh review (2026-08-18) is
closed: the fold refuses NULL/empty/poisoned series (22023), the resolve arm refuses
cross-flow and constraint-mismatched echoes, failure copy never claims rollback it cannot
observe, the Python rate-limit surface has a route-enumeration gate a new route cannot
silently bypass, and the terminalizer's `failed_final` rows no longer exclude their
strategies from the reconciliation sweep — plus the v1.19-topical TODOS deferrals absorbed
into one pass.

**Verified at:** `47022a6f` (main). Phase merged as `a6a2dee8` / PR #692 / v0.67.0.0.
**Status:** gaps_found (1 PARTIAL, warning-severity; 12 of 13 findings fully ACHIEVED)
**Re-verification:** No — initial verification.

⚠️ **Note on method.** No SUMMARY claim was accepted as evidence. Every verdict below rests
on the code at HEAD, on tests this verifier executed itself, or on CI/apply job logs pulled
from GitHub. Where a SUMMARY and the code disagree, the code wins and the disagreement is
recorded.

---

## Per-finding verdicts

| # | Finding | Verdict | Evidence (measured at HEAD) |
|---|---------|---------|------------------------------|
| **A1** | Fold commits zero-dailies strategies on `p_rows` NULL or `[]`; casts accept NaN/Inf/1e300/far-future | ✅ **ACHIEVED** | `20260819130000_csv_finalize_fold_input_guards.sql` — `CREATE OR REPLACE` (not DROP), GUARD 3 `p_rows IS NULL` → 22023 (`:275`), GUARD 4 NULL-explicit fmt whitelist (`:285`), GUARD 8 empty-array refusal scoped with `IS DISTINCT FROM 'trades'` (`:332`), GUARD 9 one-predicate value scan `NOT (x BETWEEN -10 AND 10)` + explicit-UTC future fence + `< 1900` (`:343-354`), GUARD 10 duplicate-date scan (`:361`). All evaluated BEFORE the `strategies` INSERT. **Applied to PROD**: `apply` check-run on `a6a2dee8` green, log shows `Applying migration 20260819130000…`, so its 44-site STEP-4 self-verify ran silently on production. 13 behavioral arms (Part 6a–6m) + Part 3d in `supabase/tests/test_csv_finalize_atomic_fold.sql`, auto-discovered by the `sql-tests` job (`ci.yml:1020,1032`) — **`sql-tests` green on both PR #692 and the merged commit.** |
| **A2** | Resolve arm never compares terminal status (manager↔contribution cross-flow echo) | ✅ **ACHIEVED** | `route.ts:1042-1049` — check 0, ahead of the name check, `existingRow.status !== args.terminalStatus` → `refuse()` → 409 `CSV_SESSION_REUSED`. `status` is in the select (`:958 .select("id, name, status")`), so the reading is real. Gated by `csv-finalize-cross-submission-merge.test.ts:773`; **executed by this verifier — 53/53 pass** across the three csv-finalize suites. Residual disclosed below. |
| **A3** | "Nothing was saved — rolled back completely" asserted where the commit outcome is unknowable or provably COMMITTED | ✅ **ACHIEVED** | `route.ts:775-780` — `outcomeClass` computed ONCE from `typeof error?.code === "string" && error.code.length === 5` (the explicit length check, not truthiness — class 2's `code` is `""` and would merge with class 3 under `if (error?.code)`). Class 1 keeps the verbatim rollback sentence; classes 2/3 get commit-agnostic copy naming the non-destructive check first. Separate Sentry step tag `finalize-fold-outcome-unknown` and `outcome_class` in `extra`. Gated at `csv-validate-route.test.ts:2054` and `csv-finalize/route.test.ts:341,379`. 42501→401 explicitly considered and deferred with a stated reason. |
| **A4** | Echoed 23505 outcomes still run `applyCsvMetadataUpdate` with THIS request's metadata | ✅ **ACHIEVED** | `FinalizeAtomicOutcome` carries a required `fresh: boolean` (`:567`); set `true` at `:630` (create), `false` at `:1172` (echo). `route.ts:1923-1930` — `const metaErrResponse = outcome.fresh ? await applyCsvMetadataUpdate(...) : null`. The trades sub-concern dissolves: skipping the write means `asset_class` is never overwritten, so no √252/√365 clock mismatch can arise. Gated at `cross-submission-merge.test.ts:1098`, and the pre-existing call-count assertions were flipped 1→0 at `:407` and `:827` (a revert of A4 makes them RED). |
| **B1** | Python rate limiting bypassable by omission | ✅ **ACHIEVED — and the gate is NON-VACUOUS, independently confirmed** | See the dedicated section below. |
| **B2** | X-User-Access-Token drop-vs-wire adjudication (decision was NOT made) | ✅ **ACHIEVED** | Option (a) taken: forwarding removed from `keys/sync/route.ts`, `verify-strategy/route.ts` and `process-key-client.ts` (only prose references survive — grep at HEAD shows zero assignments). New `src/lib/seam-user-jwt-emitters.invariant.test.ts` derives emitters from disk and compares to an **EMPTY** roster by equality. The 140.2 obligation was amended in the **same commit** (`140.1-TS-OBLIGATIONS.md:251` — the forward-looking column is marked ⛔ RETIRED/SUPERSEDED while the historically-true ✅ SATISFIED record is preserved, and the not-taken option (b) is costed). `get_user_scoped_supabase` deliberately kept (`db.py:80`, docstring updated at `:91`); `test_process_key.py:2220` still pins its non-import; zero real callers confirmed by grep. **19/19 tests pass** (invariant + both seam suites), executed here. |
| **B3** | Any-23505 enters the resolve arm undiscriminated | ✅ **ACHIEVED** | `route.ts:667-668` — `pgConstraintName(error)`; enter the resolve arm only for `constraint === null \|\| WIZARD_SESSION_CONSTRAINTS.has(constraint)`. ⭐ The non-obvious half is right: `null` still enters the resolve arm (null = "could not READ a name", and the resolve arm already fails closed with 503), so the tightening does not convert a fail-closed 503 into a fail-open 500. A named-but-unrecognised constraint falls through to the fold-failure arm **with its own Sentry capture** (`finalize-23505-unknown-constraint`). Gated at `cross-submission-merge.test.ts:571`. |
| **B4** | Terminalizer × sweep do not compose | ✅ **ACHIEVED** | `20260819130500_reconcile_sweep_readmit_terminalized_orphans.sql` ships the **narrow** predicate the CONTEXT mandated — `AND NOT ((cj.status = 'failed_final' AND cj.last_error LIKE 'orphaned_running_reaped:%') IS TRUE)` (`:338-339`) — **not** the roster's blanket widening. The `IS TRUE` wrapper is present and its own STEP-2 self-verify RAISEs without it (`:442`, with the measured justification: `last_error` is NULLABLE and the unwrapped form heals a NULL-`last_error` `failed_final` row, i.e. exactly the arm-C2 failure mode). Idempotent unschedule-then-schedule, one jobname only. New must-heal arm **C4** added at `test_reconcile_dropped_enqueue_sweep.sql:741-748`; C2/C3 untouched, so C2/C4 form the discriminating pair. **Applied to PROD** (`apply` log shows `20260819130500`), 25-site self-verify silent. Framed correctly as PREVENTIVE — the PROD marker population was measured at 0. |
| **B5** | `csv-finalize-rpc.test.ts` calls the DROPped `finalize_csv_strategy` | ✅ **ACHIEVED** | Zero references to the dropped name remain (`grep -c "finalize_csv_strategy'"` = 0); the file is 139 lines with a tombstone docblock. The file was **not** deleted. The three real guard behaviours (invalid fmt / empty name / name > 80) are re-homed to Part 7a/7b/7c of `test_csv_finalize_atomic_fold.sql`, which **executes** under `psql -v ON_ERROR_STOP=1` in the `sql-tests` job. **11 pure-unit asset_class tests still run** — executed here, 11/11 pass. ⚠️ The plan-01 must-have said "12"; the pre-phase file had 12 `it(` blocks of which one was the *skip-reason advertiser that asserted nothing* and was deliberately deleted. All 11 real asset_class tests survive. The must-have's integer was stale, the coverage is not. |
| **C1** | Interior-values echo residual | ⚠️ **PARTIAL** | See the Gaps section. Copy shipped and gated; delivery to the user is unwired. |
| **C2** | Two ~90-line token-identical handlers | ✅ **ACHIEVED** | `contributionCsvFinalizeHandler` is **gone** — the only surviving occurrence in `src/` is the tombstone comment at `route.ts:1803`. One `unifiedCsvFinalizeHandler(args: {…})` with `terminalStatus` and `logPrefix` as arguments, passed VERBATIM from both call sites (`:1764` `'private'`, `:1783` `'pending_review'`). The source-shape gates were **re-pointed, not deleted**: Tests 8b/8c at `csv-validate-route.test.ts:1464,1489` still pin the name, the single typed args object, the explicit `dailyReturnsSeries: CsvDailyReturnRow[]` field and the single-object call site. 56/56 pass here. |
| **C3** | TEST-project sweep cron residual | ✅ **ACHIEVED (as scoped: document-or-guard → documented)** | `test_reconcile_dropped_enqueue_sweep.sql:195-220` — a DATED (2026-08-17) census claim explicitly flagged "A DATED CLAIM, NOT A FACT — RE-MEASURE BEFORE RELYING ON IT", plus item (e): a written refusal recording that C3 must NOT become a cleanup migration, and three named NEVERs (never DELETE a `pending` row; never unschedule any cron job but `reconcile_dropped_enqueue_sweep` by name; never touch the backbone flag row). |
| **C4** | Remaining v1.19-topical TODOS to absorb | ✅ **ACHIEVED (all named sub-items)** | Resolve-arm fail-closed pins → `cross-submission-merge.test.ts:870`. Fold gate Part 3d (`p_terminal_status='published'` → 22023, zero rows) → `test_csv_finalize_atomic_fold.sql:297-348`. Types regen + cast-through-unknown deleted → `route.ts:611` is now a typed `supabase.rpc("finalize_csv_strategy_with_returns", {…})`, `database.types.ts:3714` carries the 6-arg signature, and **`npx tsc --noEmit` exits 0 with the cast gone** — which is the actual proof the signature is right. Python tombstone `CSV_FINALIZE_MOVED` → message-only, code deliberately stays `MISSING_STRATEGY_ID` because WIZFORM-02 is OPEN (`process_key.py:1154-1177`); both arms pinned by tests. service_role REVOKE → migration STEP 2 `:442` + STEP-4 assertion `:533`. RT-3 → the burned csv-submit **fingerprint** (bounded, not the raw signature) is a sticky field on the signed `saveWizardState` envelope (`localStorage.ts:132,273,335,539`) with a serialized save queue; 120/120 tests pass here. Stale-comment batch → done, with the honest note that one plan `<verify>` grep fired on truthful prose and the prose was kept rather than deleted to make a grep pass. |

**Score: 12/13 ACHIEVED, 1 PARTIAL.**

---

## Special scrutiny: the gates this phase ADDED

This phase's whole purpose was eliminating vacuous gates, so each new gate was tested
against the question *"what named change would make this fail?"*

### 1. `analytics-service/tests/test_limiter_route_coverage.py` (B1) — ✅ NON-VACUOUS, CONFIRMED BY INDEPENDENT MEASUREMENT

The brief flagged that this gate was broken after the plans completed (enumerating
`main.app.routes` with `isinstance(r, APIRoute)` saw **1 of 20** routes under the pinned
`fastapi==0.139.0`, because `include_router` is deferred). **The current gate at HEAD is
the fixed one**, and I measured it rather than reading the docstring:

| Measurement | Result |
|---|---|
| installed fastapi | `0.139.0` (the pin) |
| `fastapi.routing.iter_route_contexts` | PRESENT — the gate takes the flattener branch (`enumeration: "iter_route_contexts"`) |
| routes derived by the gate's own probe | **20** |
| routes I enumerated independently (`iter_route_contexts` → `original_route` → `isinstance APIRoute`, printed by path) | **20** — identical surface, `/health`, `/process-key`, all 10 routers |
| limited (static ∪ dynamic) | 13 (12 `_route_limits` + `routers.process_key.process_key` in `_dynamic_route_limits`) |
| unlimited partition | exactly the 7 `NO_LIMITER_QUARANTINE` entries — set equality holds |
| `pytest tests/test_limiter_route_coverage.py -q` (run from `analytics-service/`, `python3`) | **4 passed** |

**What named change makes it fail.** Adding an undecorated route to any router puts a new
key in `derived - limited`, and `test_the_unlimited_partition_equals_the_quarantine`
reddens with `unexpected=[…]` naming it. Deleting a route that holds a quarantine entry
reddens with `missing=[…]` (equality, not containment). A slowapi key-format change breaks
the join and trips `MIN_LIMITED_ROUTES=10`. A FastAPI rename of `iter_route_contexts` takes
the `ImportError` fallback → 1 route → trips `MIN_API_ROUTES=18`. Fences at 18/10 sit below
the measured 20/13, so ordinary growth does not redden it but a truncated walk does.

⚠️ One honest residual, which 146.1-02-SUMMARY.md itself states rather than hides: on
today's tree the vacuity fence is not the *unique* catch for an empty walk — the partition
equality would also redden (with `missing=` listing all 7). The fence's value is
conditional on the roster ever shrinking to empty.

⚠️ Second residual, **not gated**: the quarantine entry for
`routers.internal.get_key_permissions` claims the route is protected by a hand-rolled
per-`key_id` bucket. That bucket does exist (`routers/internal.py:125` `_consume_rate_limit`,
consumed at `:231`) — I checked — but nothing asserts it continues to. The comment says so
("If that bespoke bucket is ever deleted, this entry becomes a lie"), which is the honest
form, but it is prose and not a gate.

### 2. The B2 empty-emitter-roster invariant — ✅ NON-VACUOUS

`src/lib/seam-user-jwt-emitters.invariant.test.ts` is the shape an empty-roster equality
should have, and it survives the standard objection ("an empty roster agrees with an empty
scan"):

- **Vacuity fence:** `SOURCE_FILES.length > 200` (measured 900+).
- **⭐ Positive control (the part a fence cannot do):** it asserts that a *known,
  deliberate, NON-emitting* occurrence — `x-user-access-token` in `resilient-fetch.ts`'s
  `CREDENTIAL_HEADER_NAMES` — is still FOUND, and simultaneously that the emitter pattern
  does **not** classify it as an emitter. This is what proves the needle still matches
  after a rename or a quote-style change.
- **Both-polarity self-tests:** the comment-stripper is tested for being too lax (a
  commented-out emitter must not count) *and* too aggressive (real code must survive).
- **Named failing change:** re-adding `"X-User-Access-Token": session.access_token` to
  `process-key-client.ts` puts a path into `EMITTERS`, and the equality against
  `EXPECTED_JWT_EMITTERS = []` reddens naming the file.
- Executed here: **19/19 pass** including the six self-tests.

⚠️ Residual, honestly scoped in the file's own docblock: `EMITTER_PATTERN` matches two
shapes — a quoted object key and `.set/.append("…",`. An emission via a *computed* or
*constant-referenced* header name (`headers[HEADER_CONST] = tok`) would evade it. The
docblock says "the two shapes this repo can express it in", which is true today.

### 3. The self-verify blocks inside both migrations — ✅ NON-VACUOUS, and they run on PROD

The easy-batch had already caught one false-PASS of this exact class (the unbounded
`%5000%` LIKE). The new blocks do not repeat it:

- `20260819130000` STEP 4 comment-strips **once** (`v_code := regexp_replace(v_fn_src,
  '--[^\n]*', '', 'g')`) and every check reads `v_code`, never the raw body — so a deleted
  guard whose explanatory comment survives cannot pass.
- Matches are word-/fragment-bounded: `'jsonb_array_length\(p_rows\)\s*>\s*5000\M'`,
  `'BETWEEN[[:space:]]+-10[[:space:]]+AND[[:space:]]+10\M'` (the `\M` is what stops a
  widening to `BETWEEN -10 AND 100` from passing as a prefix — and the header states, correctly,
  that the NaN and 1e300 behavioral probes would BOTH still raise under the wider bound, so
  nothing else would notice), `'\mwizard_session_id\M'` vs `'\mp_wizard_session_id\M'`.
- The `search_path` check reads `pg_proc.proconfig` rather than the re-rendered text, with
  the reason stated: `pg_get_functiondef` normalizes the SET clause, so a text match would
  RAISE on a healthy database — "a self-verify that fails on a healthy database is worse
  than none".
- Anchor-drift is handled by RAISEing "FIX THE ANCHORS rather than deleting this check",
  not by silently skipping.
- **They executed on PRODUCTION.** GitHub `apply` check-run `96023016379` on `a6a2dee8` is
  green and its log shows both files applied. A RAISE would have failed the job.

⚠️ Structural residual: a migration self-verify runs **once, at apply time**. The recurring
CI gate `test_csv_finalize_atomic_fold.sql` Part 1 asserts overload count, SECDEF,
`authenticated` EXECUTE and `anon` NO-EXECUTE — but **not** the C4 `service_role` REVOKE.
So the function COMMENT's "authenticated ONLY" claim is pinned only at the moment
`20260819130000` applied; a future `DROP + CREATE` that re-granted `service_role` via
`pg_default_acl` would pass the recurring gate. (Impact is bounded: a service-role call
NULLs `auth.uid()` and GUARD 2 refuses it with 42501 before any write — the migration says
so itself.)

---

## Behavioral evidence gathered by this verifier

| Check | Command | Result |
|---|---|---|
| B1 route-coverage gate | `python3 -m pytest tests/test_limiter_route_coverage.py -q` (from `analytics-service/`) | ✅ 4 passed |
| B1 population, independently derived | `iter_route_contexts` walk, printed by path | ✅ 20 routes, 13 limited, 7 unlimited == roster |
| Python siblings | `python3 -m pytest tests/test_process_key.py tests/test_main_worker.py tests/test_limiter_identity.py -q` | ✅ 241 passed |
| csv-finalize route family | `vitest run --no-file-parallelism cross-submission-merge + route.test + rpc.test` | ✅ 53 passed (3 files) |
| B2 invariant + seams | `vitest run seam-user-jwt-emitters.invariant + keys/sync route.seam + verify-strategy route.seam` | ✅ 19 passed |
| RT-3 + sweep + wizard burn | `vitest run localStorage.test + WizardClient.csv-burn-persistence + reconcile-dropped-enqueue-sweep` | ✅ 120 passed |
| C2 source-shape gates | `vitest run csv-validate-route.test.ts` | ✅ 56 passed |
| Types regen proof | `npx tsc --noEmit` | ✅ exit 0 (with the `as unknown as` cast deleted) |
| Debt markers in every file this phase touched | `grep -nE '\b(TBD\|FIXME\|XXX)\b'` and `TODO\|HACK\|PLACEHOLDER` over the 40 non-`.planning` files in `a6a2dee8` | ✅ zero matches |
| CI on the merged commit | `gh api …/commits/a6a2dee8/check-runs` | ✅ 23/23 success, incl. `sql-tests`, `python`, `frontend-typecheck`, `policy` (Migration Policy), `apply` |
| PROD migration apply | `gh run view --job 96023016379 --log` | ✅ both `20260819130000` and `20260819130500` applied; ledger shows both versions |

⛔ Not run, per the task's fencing: nothing was executed against any database, no migration
was re-applied, and no source/test/migration file was modified.

---

## Asserted but NOT demonstrated

These are claims that are load-bearing somewhere in the phase's artifacts and are **not**
backed by executed evidence. None of them is a falsehood; each is an untested assertion.

1. **The B4-BLANKET neuter — the decisive one — was never run.** Both `146.1-03-SUMMARY.md`
   and `146.1-08-SUMMARY.md` name it as *the* evidence for preferring the narrow marker
   predicate over the roster's blanket widening ("arms C2 AND C3 must BOTH redden"), and
   both record it as NOT RUN because it requires mutating the deployed body on shared TEST.
   What IS proven is *presence* of the narrow form and *absence* of both blanket spellings
   in the deployed `cron.job.command`. The discriminating-pair claim (C2/C4) rests on
   reading, not on a double-RED.

2. **The gate files' own `ALL PASS` notice text has never been read by a human or an
   agent.** `146.1-08-SUMMARY.md` states plainly that the Supabase MCP transport discards
   `RAISE NOTICE`, so the TEST rehearsal proved only *"the file ran to its last statement
   without raising"* via a trailing sentinel. CI's `psql` run is the closing evidence and it
   is green — which is sufficient — but no artifact quotes the notices.

3. **PROD post-merge verification is outstanding.** 146.1-08 closes with "⛔ Nothing has
   been applied to PROD" and defers the re-count of `orphaned_running_reaped:%` plus one
   real `cron.job_run_details` tick to a post-merge task. Both migrations have since
   applied. No artifact records that task running. → human verification item 1.

4. **`test_limiter_route_coverage.py`'s quarantine reason for
   `routers.internal.get_key_permissions`** — "it carries a hand-rolled per-`key_id` 10/min
   token bucket" — is prose, not a gate. I verified the bucket exists today
   (`routers/internal.py:125,231`); nothing asserts it will tomorrow. The file says this
   about itself.

5. **`146.1-07-SUMMARY.md` is STALE on task 1.** It states task 1 was "NOT attempted" and
   `database.types.ts` is "UNCHANGED". At HEAD the regen DID happen (a later commit on the
   branch, visible in the squash message: *"task 1 — regenerate types, delete the
   cast-through-unknown … Closes the deferral I filed in 22e9fd32"*), `database.types.ts`
   moved 12 lines, the cast is gone and `tsc` is clean. **The code is right and the SUMMARY
   is wrong** — recorded here because reading that SUMMARY alone would understate the
   phase.

6. **The plan-01 must-have's "12 pure-unit asset_class tests" is an off-by-one.** The
   pre-phase file held 12 `it(` blocks, one of which was the *skip-reason advertiser that
   asserted nothing* — which the tombstone deliberately deleted. 11 real asset_class tests
   survive and pass. No coverage was lost; the integer in the must-have was simply counting
   the vacuous case.

7. **A2 leaves a disclosed residual that is not closed and is not closable by A2.** When
   `existingRow.status` comes back absent, A2 correctly does not refuse (absence is not a
   measurement), and the echo then falls back to `existingRow.status ?? "pending_review"` —
   so a *contribution* request would be told `pending_review` while it asked for `private`.
   The database row is untouched (the resolve arm writes nothing), so this is a response
   misreport, not a state error. `146.1-04-SUMMARY.md` records it deliberately.

8. **The A2 refusal reuses a 409 sentence that does not describe the A2 case.** `refuse()`
   emits *"This wizard session already created a strategy with a different track record"* —
   but on the A2 arm the track record may be byte-identical and the actual refusal reason
   is a manager-vs-contribution flow collision. The reuse is a **deliberate, reasoned**
   choice (one code, to avoid moving `KNOWN_CSV_FINALIZE_CODES` + `EXPECTED_TABLE_SIZE` +
   the vocabulary invariant for a state the user cannot act on differently), and the precise
   reason does ride the Sentry capture. Flagged because it is the one place in a
   copy-honesty phase where the sentence a user reads does not describe what happened.

9. **CSV finalize still emits no audit event.** Surfaced by the types regen (the call is now
   visible to the audit-coverage law and carries an explicit `@audit-skip` with a named
   reason at `route.ts:609`), filed in `TODOS.md:2979`. Out of the roster; recorded so it is
   not mistaken for something this phase closed.

10. **Bookkeeping is not closed.** `.planning/STATE.md:8,11` still says *"11 residual
    findings"* and does not record the phase as shipped; all eight plan checkboxes in
    `ROADMAP.md:300-307` are `[ ]`. Orchestrator/close-out work, not a code gap.

---

## Gaps Summary

**One gap, warning severity, and it does not block milestone close.**

**C1 — the honest copy exists but no one can read it.** The founder chose option (b) —
"copy that admits the echo did not re-verify interiors" — over a content hash. The sentence
that shipped is exactly right and is defended by four tests, including a `🔴 ABSENCE` case
that reddens if an over-claiming sentence is restored *alongside* the honest one (the
concatenation attack). The false code comment *"presence is proven, not assumed"* is gone.
Every part of that is achieved.

What is missing is the last link. The sentence rides `human_message` on a `200 / ok:true`
envelope, and `CsvSubmitStep.tsx` reads `human_message` only inside
`if (!res.ok && !isIdempotentSuccess)` (`:322-334`). A 200 takes the success path and the
field is dropped. There is no other consumer — I grepped every `human_message` reference
under `src/app/(dashboard)/strategies/` and `src/components/`. So the user whose mid-file
correction was silently discarded still learns nothing.

Two things keep this a warning and not a blocker:

- **Nothing regressed.** The prior envelope carried no message at all, so the user's
  experience is unchanged; what changed is that the API contract stopped implying the
  residual was closed.
- **It is a product decision, not a defect with one correct repair.** Rendering a caution on
  an otherwise-clean success is a UX call the founder may reasonably decline — in which case
  the right closure is a TODOS entry saying the API-contract placement is the end state, not
  a code change.

Per the standing stopping rule (block only on user-facing breaks or data-integrity), this is
logged rather than blocking. **Recommendation: close v1.19 / milestone-close on the strength
of 12/13, with C1's delivery and the three human-verification items carried into TODOS.md.**

The two items that would matter most before declaring the migrations settled are the
**post-merge PROD verification** (human item 1 — outstanding by the phase's own plan) and
the **B4-BLANKET double-RED neuter** (human item 2 — the evidence the narrow predicate was
chosen for a measured reason rather than an argued one).

---

_Verified: 2026-08-19T10:49:49Z at `47022a6f`_
_Verifier: Claude (gsd-verifier) — goal-backward, adversarial stance_

---

## Gap closure record (2026-08-20, orchestrator session — v1.19 milestone audit)

The single PARTIAL (C1 — the honest resolve-echo sentence shipped on the 200
envelope but no client consumed it) was CLOSED BY SUCCESSOR PHASE 146.2, which
existed for exactly this: plan 146.2-07 (R6/W1) wires `CsvSubmitStep` to render
the route's echo sentence in-step on the 200 resolve path, gated by jsdom tests,
and the live-flow UAT (`146.2-*/146.2-UAT.md`, 2026-08-20) OBSERVED the amber
echo notice rendering that sentence verbatim on a running server. 146.2's own
verification is `passed` (5/5, all human items closed same day). Status flipped
`gaps_found` → `passed`.
