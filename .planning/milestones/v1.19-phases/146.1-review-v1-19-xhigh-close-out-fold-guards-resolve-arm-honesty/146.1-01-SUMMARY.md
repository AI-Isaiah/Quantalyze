# Plan 146.1-01 — SUMMARY (A1 fold input guards + B5 re-point + C4)

**Status: code COMPLETE, statically verified. SQL guarantees NOT proven — that is 146.1-08's job.**

⚠️ **Authorship note.** The assigned executor died mid-task-3 (ENOTFOUND, laptop power
loss) before writing this file. The orchestrator preserved its uncommitted work
(`e06aa580`), inspected every artifact, ran the gates centrally, and wrote this SUMMARY.
Treat the "verified" column as the orchestrator's own measurements, not the executor's
claims — the executor never reported.

## Delivered

| Item | Artifact |
|---|---|
| A1 | `supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql` |
| A1 gate | Parts 6a–6j, 7a–7c, 3d in `supabase/tests/test_csv_finalize_atomic_fold.sql` |
| B5 | `src/__tests__/csv-finalize-rpc.test.ts` re-pointed; citations fixed in `strategy-verifications-rls.test.ts` |
| C4 | `p_terminal_status='published'` Part 3d + `service_role` REVOKE |

## Orchestrator-measured verification

- `CREATE OR REPLACE` ×2, **`DROP FUNCTION` ×0** — house law honoured; the fold's
  DROP-and-restate-grants prose was correctly NOT copied.
- Guards present: `p_rows IS NULL`, `p_fmt IS DISTINCT FROM 'trades'` (NULL-safe),
  `count(DISTINCT …)`, `now() AT TIME ZONE 'UTC'`, `FROM service_role` REVOKE.
- **Magnitude bound checked WORD-BOUNDED, not by substring**: exactly ONE executable
  `BETWEEN -10 AND 10`; the one `BETWEEN -10 AND 100` occurrence is inside a COMMENT
  explaining why Part 6j exists. (A comment-blind grep flags this as drift — it is not.
  Same lesson the self-verify checks keep teaching.)
- **Part 6j present** (the near-boundary 10.5 probe the plan-checker added). Without it a
  widening to ±100 is invisible: NaN fails any BETWEEN and 1e300 still exceeds 100.
- `GET STACKED DIAGNOSTICS` count = **19**, above the ≥15 floor (13 new negatives + 2
  pre-existing).
- **B5 did NOT delete the file.** All **11** asset_class unit tests survive. The only
  removed `it()` was `"advertises skip reason when live DB is unavailable"`, which tested
  the live-DB skip harness that no longer exists. `it.skipIf` count = **0**; the six dead
  live-DB cases are replaced by a tombstone comment and re-homed as SQL Parts 7a–7c.
- tsc clean · vitest 4 suites 41 passed/5 skipped · mypy --strict clean (91 files).

## NOT verified — do not read this plan's completion as proof

⛔ **No SQL Part in this plan has been EXECUTED.** There is no DB access here. Every Part
6a–6j / 7a–7c / 3d is written but unproven, and the migration has never run. The following
neuters are **DEFERRED TO 146.1-08** by name: A1-NULL, A1-EMPTY, A1-VALUE (two-probe form,
NaN + 10.5), A1-DATE, A1-DUP, A1-SUBSTR, A1-SECDEF, C4-PUBLISHED, C4-ACL.

⛔ Merging `supabase/migrations/**` to main AUTO-APPLIES to PROD. 146.1-08 is the blocking
gate: TEST rehearsal before merge, PROD verification after.

## Framing (measured, do not re-file)

A1 is **boundary hardening**, reachable only by an authenticated direct-RPC caller
poisoning their own tenant — `route.ts:1337-1352` already 400s the route path. The
migration header states this correctly and negates the live-break reading explicitly.
