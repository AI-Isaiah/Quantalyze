---
phase: 146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql
  - supabase/schema/functions/finalize_csv_strategy_with_returns.sql
  - supabase/tests/test_csv_finalize_atomic_fold.sql
  - src/__tests__/csv-finalize-rpc.test.ts
  - src/__tests__/strategy-verifications-rls.test.ts
autonomous: true
requirements: [A1, B5, C4]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "An authenticated direct-RPC call to finalize_csv_strategy_with_returns with p_rows := NULL raises SQLSTATE 22023 and writes ZERO rows (A1)."
    - "The same call with p_rows := '[]' and any p_fmt other than 'trades' — including p_fmt := NULL — raises 22023 and writes ZERO rows (A1)."
    - "A p_rows element carrying NaN, ±Infinity, |daily_return| > 10, a future date, a calendar-absurd date, a NULL daily_return, or a duplicate date raises 22023 BEFORE any write (A1)."
    - "p_rows := '[]' with p_fmt := 'trades' still succeeds and still returns a UUID — the existing Part 4 regression fence stays GREEN (A1)."
    - "p_terminal_status := 'published' raises 22023 before any write, asserted by a gate that runs in CI (C4/Part 3d)."
    - "The three 22023 guard behaviours orphaned in csv-finalize-rpc.test.ts (invalid fmt, empty name, name > 80) are asserted by a gate that ACTUALLY EXECUTES in CI (B5)."
    - "The 12 pure-unit asset_class tests in csv-finalize-rpc.test.ts still run in CI (B5 — deleting the file is forbidden)."
  artifacts:
    - supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql
    - supabase/schema/functions/finalize_csv_strategy_with_returns.sql
    - supabase/tests/test_csv_finalize_atomic_fold.sql
  key_links:
    - "The new migration's self-verify DO block replicates checks (a)-(g) of 20260819120000:373-495 — otherwise a CREATE OR REPLACE that silently drops an invariant ships green, because the fold's own STEP 3 never re-runs."
    - "supabase/tests/test_csv_finalize_atomic_fold.sql is auto-discovered by the ci.yml sql-tests glob — a new Part needs no workflow edit."
    - "The migration and its gate Parts land in ONE commit (tasks 1+2) so the 144-§8 same-commit rule holds."
    - "146.1-08 blocks merge until this migration has been rehearsed on TEST via Supabase MCP — merging supabase/migrations/** to main AUTO-APPLIES to PROD."
---

<objective>
Close **A1** (the fold commits a zero-dailies strategy on `p_rows` NULL or `[]`, and its
casts accept NaN / ±Infinity / 1e300 / 9999-12-31), re-home **B5**'s three orphaned 22023
guard tests into a gate that actually executes, and land two **C4** residue items that ride
the same two files (fold gate Part 3d; the `service_role` ACL claim).

⚠️ **SCOPE HONESTY — read before writing a single line of prose.** A1 is **NOT reachable
through the route**. `src/app/api/strategies/csv-finalize/route.ts:1337-1352` already 400s
`(fmt === 'daily_returns' || fmt === 'daily_nav') && dailyReturnsSeries.length === 0`, and
`parseDailyReturnsSeries` maps `undefined`/`null` to `{ok:true, rows:[]}` at `:144-146`, so the
legacy-client shape lands in that 400 and never reaches the fold. A1's real reachability is
**authenticated direct-RPC, own-tenant only** — `auth.uid() = p_user_id` confines the damage to
the caller's own `pending_review`/`private` strategy. This is **boundary hardening / defense in
depth on a SECURITY DEFINER body the route cannot be trusted to fence**, and every comment,
migration header and commit message this plan writes MUST say so. ⛔ Do NOT describe A1 as a
live user-facing data-integrity break — the roster's original claim was measured WRONG and
corrected in `146.1-CONTEXT.md`.

Purpose: the fold is a `SECURITY DEFINER` RPC granted to `authenticated`. Its input guards are
the only boundary a direct-RPC caller cannot bypass, and every rows/fmt guard in it is
non-NULL-safe (MEASURED on a throwaway `postgres:16-alpine`, `146.1-RESEARCH.md` §1.2:
`jsonb_typeof(NULL)` is NULL, `NULL <> 'array'` is NULL, and plpgsql `IF NULL THEN` takes the
ELSE branch — so `p_rows := NULL` bypassed EVERY rows guard, skipped the dailies INSERT and
returned a UUID). Phase 143's reconciliation sweep structurally cannot heal the result: its
`EXISTS(csv_daily_returns)` conjunct excludes a zero-dailies strategy by construction.

Output: one forward migration, a regenerated canonical snapshot, three new Parts on the fold's
SQL gate, and `csv-finalize-rpc.test.ts` reduced to the coverage that actually runs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-CONTEXT.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-RESEARCH.md
@.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-PATTERNS.md

@supabase/migrations/20260819120000_csv_finalize_atomic_fold.sql
@supabase/schema/functions/finalize_csv_strategy_with_returns.sql
@supabase/tests/test_csv_finalize_atomic_fold.sql
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Forward migration — NULL / empty / value guards on the fold, CREATE OR REPLACE, with its own self-verify</name>
  <files>supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql, supabase/schema/functions/finalize_csv_strategy_with_returns.sql</files>
  <precondition>`node_modules/.bin/tsx` exists in this checkout. GSD worktree agents are created WITHOUT node_modules and `npx tsx` silently resolves a decoy package — if the binary is absent run `npm ci` first, and if that fails HALT rather than skipping the snapshot regeneration.</precondition>
  <read_first>
    - `supabase/schema/functions/finalize_csv_strategy_with_returns.sql` lines 9-147 — this is the `@generated` canonical current body and the ONLY re-base target. `grep -rln "finalize_csv_strategy_with_returns" supabase/migrations/` returns EXACTLY ONE migration (`20260819120000`), so the re-base enumeration required by house law (`20260728120000:186-195`) is already discharged — but RUN THE GREP ANYWAY and paste its output into the migration header, because a citation nobody re-measured is a claim, not a fact.
    - `supabase/migrations/20260819120000_csv_finalize_atomic_fold.sql:1-147` (header shape), `:153-199` (STEP 0 pre-flight), `:218-285` (the guard block, in order), `:344-369` (grants + COMMENT), `:373-495` (STEP 3 self-verify, checks (a)-(g)).
    - `146.1-PATTERNS.md` §1 — the (a)-(g) checklist of what a `CREATE OR REPLACE` re-base MUST preserve, and §"Contradictions Found" items 1-4.
  </read_first>
  <behavior>
    The migration is the artifact; these are the behaviours its guards must produce when the
    function is called (each one becomes a sub-Part in Task 2, so write them so they are
    testable):
    - `p_rows := NULL`, any fmt → SQLSTATE 22023, message contains a distinguishing substring naming `p_rows` and NULL. Zero rows in `strategies`, `strategy_verifications`, `csv_daily_returns`.
    - `p_rows := '[]'::jsonb`, `p_fmt := 'daily_returns'` → 22023, distinct substring from the NULL case.
    - `p_rows := '[]'::jsonb`, `p_fmt := NULL` → 22023 (proves the fmt whitelist itself is NULL-safe; a `<>`-scoped guard would let this through).
    - `p_rows := '[]'::jsonb`, `p_fmt := 'trades'` → SUCCEEDS, returns a UUID, zero dailies. (Existing Part 4 — the regression fence.)
    - An element with `daily_return` = `"NaN"` → 22023. With `"Infinity"` → 22023. With `1e300` → 22023. With `10.5` → 22023. With `9.9` → accepted.
    - An element with `daily_return` absent/JSON null → 22023 (NOT a downstream 23502).
    - An element whose `date` is later than `(now() AT TIME ZONE 'UTC')::date` → 22023. `'9999-12-31'` → 22023. `'0001-01-01'` → 22023.
    - Two elements with the same `date` → 22023 (and NOT a 23505 from `csv_daily_returns_strategy_date_key`).
    - `p_terminal_status := 'published'` → 22023 (unchanged behaviour, newly gated in Task 2).
  </behavior>
  <action>
Create `supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql`.

⛔ **The stamp is fixed and non-negotiable: `20260819130000`.** The orchestrator VERIFIED via
Supabase MCP on 2026-08-18 that PROD's `schema_migrations` tip is `20260819120000`. A
`date -u +%Y%m%d%H%M%S` stamp produces `20260818…`, which is BELOW the tip and trips the
`migration-policy.yml` backdated guard (it queries the remote tip live and fails closed).
`20260819130000` sorts above the tip and stays valid past the 2026-08-19 12:00 UTC expiry of
the sort-above rule. ⛔ Do NOT add this stamp to `.github/migrate-backdated-allowlist.txt` —
that allowlist is for history-only restorations.

⛔ **`CREATE OR REPLACE FUNCTION`, never `DROP` + `CREATE`.** House law is
`20260813150106:69` / `20260812083206:739-747`. `20260819120000` used DROP legitimately because
it was RETIRING two parent functions — do NOT carry over its "DROP FUNCTION loses ACLs"
justification prose, which would be false in a `CREATE OR REPLACE`.

**Body derivation — do NOT re-type the function.** Extract lines 9-147 of
`supabase/schema/functions/finalize_csv_strategy_with_returns.sql` mechanically (e.g.
`sed -n '9,147p'`) as the base, change only the `CREATE FUNCTION` keyword line to
`CREATE OR REPLACE FUNCTION`, and insert the new guards. Re-typing invites the exact
copy-a-stale-body regression `scripts/dump-sql-functions.ts`'s own docblock records
(G23-187).

**Header block**, following `20260819120000:1-147`'s delta-list style, in this order: why the
migration exists (state the direct-RPC-only reachability verbatim from this plan's objective —
do NOT overclaim); the RE-BASE ENUMERATION with the pasted grep output; the numbered EXACT
delta list against `20260819120000:204-342`, each item citing the source line it copies or
adapts; a "READ THIS BEFORE SIMPLIFYING" block; the ERRCODE map with `22023` extended to name
the new cases; the ⚠️ PROD AUTO-APPLY paragraph; the gate list (this file's own self-verify plus
the three Parts Task 2 adds, plus `test_csv_finalize_auth_guard.sql`); and a Manual rollback
block. The header MUST state that `20260819120000:283-285`'s "the parents' empty-array raise is
deliberately ABSENT" note is being **narrowed to `fmt='trades'`, not reverted** — otherwise the
next reader re-litigates it. The header MUST also pre-document the `BEGIN;`/`COMMIT;` deviation
from migration-reviewer invariant #14, exactly as `20260816140000` does.

Open with `BEGIN;` then `SET lock_timeout = '3s';` — match the function-migration family
(`20260819120000:149-151`), not the cron family's `'5s'`.

**STEP 0 pre-flight DO block** copying `20260819120000:164-199`: assert exactly one 6-arg
`finalize_csv_strategy_with_returns` exists and is `prosecdef`, fail loud with a message naming
the state this migration was authored against, and carry the honesty paragraph naming what the
pre-flight canNOT prove.

**STEP 1 — the replaced function.** Insert guards in this ORDER, because ordering decides which
message a caller sees and one of these orderings is load-bearing:

1. `p_terminal_status` whitelist — unchanged, stays FIRST (parent `:224-229`).
2. Both auth-identity guards — unchanged, `42501`, and the message literal
   `finalize_csv_strategy_with_returns called without an auth session` is pinned by
   `test_csv_finalize_auth_guard.sql` Part A. Change it and that gate REDs.
3. **NEW: `IF p_rows IS NULL THEN` raise 22023.** Must sit BEFORE the `jsonb_typeof` guard so
   the NULL case gets its own distinguishing message instead of falling into the typeof guard's
   `got <NULL>` arm. Provenance comment: every guard below it is a three-valued-logic
   comparison that a NULL argument passes, MEASURED on a throwaway `postgres:16-alpine`
   (`146.1-RESEARCH.md` §1.2).
4. **CHANGED: the fmt whitelist becomes NULL-explicit** — `IF p_fmt IS NULL OR p_fmt NOT IN
   ('daily_returns','daily_nav','trades')`. Keep the existing `invalid fmt %` distinguishing
   substring so no gate moves.
5. Strategy-name guards — unchanged (parent `:262-272`).
6. `jsonb_typeof(p_rows) <> 'array'` — unchanged.
7. `jsonb_array_length(p_rows) > 5000` — unchanged, and it MUST stay spelled exactly that way:
   `20260819120000` STEP 3(d) pins it with the word-bounded regex
   `jsonb_array_length\(p_rows\)\s*>\s*5000\M`.
8. **NEW: empty-array allowance scoped to trades** — `IF jsonb_array_length(p_rows) = 0 AND
   p_fmt IS DISTINCT FROM 'trades' THEN` raise 22023 naming `p_fmt` in the message. ⛔ Use
   `IS DISTINCT FROM`, never `<>`: MEASURED, `p_fmt := NULL` makes `p_fmt <> 'trades'` NULL and
   the guard does not fire. This raise must sit AFTER the fmt whitelist so a bogus fmt still
   gets `invalid fmt`.
9. **NEW: one value-scan `EXISTS` over `jsonb_array_elements(p_rows)`, evaluated BEFORE the
   strategies INSERT**, raising 22023 when any element satisfies any of: `elem->>'daily_return'
   IS NULL`; `NOT ((elem->>'daily_return')::DOUBLE PRECISION BETWEEN -10 AND 10)`;
   `(elem->>'date')::DATE > (now() AT TIME ZONE 'UTC')::date`; `(elem->>'date')::DATE <
   '1900-01-01'`. The `NOT (x BETWEEN -10 AND 10)` form is deliberate and MEASURED: it catches
   NaN, +Infinity, −Infinity AND out-of-magnitude in one predicate (three separate predicates
   are three chances to drift from the route's mirror value `MAX_DAILY_RETURN = 10` at
   `route.ts:198-200`). ⚠️ It does NOT catch a NULL `daily_return` — that is why the explicit
   `IS NULL` arm is listed first; relying on `csv_daily_returns.daily_return NOT NULL` would
   surface a 23502 instead of a legible 22023. ⛔ Use `(now() AT TIME ZONE 'UTC')::date`, NOT
   `CURRENT_DATE`: `CURRENT_DATE` is server-timezone-dependent and the route's fence is UTC
   (`route.ts:227-236`).
10. **NEW: duplicate-date scan** — raise 22023 when
    `count(*) <> count(DISTINCT elem->>'date')` over the same elements. This is not tidiness: it
    removes the one concrete path by which the fold's own handler-free dailies INSERT (a plain
    INSERT with no ON CONFLICT, `20260819120000:325-331`) can raise 23505 on
    `csv_daily_returns_strategy_date_key`, which is the wrong-arm defect 146.1-04/B3 fences
    independently.

Every new raise follows the sibling wording shape `finalize_csv_strategy_with_returns: <subject>
<verb phrase>` with ONE distinguishing substring per guard, so Task 2 can pin each separately —
`20260819120000:259-261` states this requirement explicitly. Every new raise carries a
`-- (parent: <file>:<lines>)` or `-- (new, v1.19 review A1)` provenance comment.

⛔ **Preserve, verbatim, every invariant the fold's own STEP 3 asserts** — it lives in an
already-applied file and will NEVER re-run, so losing one of these ships silently:
`SECURITY DEFINER`; `SET search_path = public, pg_catalog`; the 6 args with identical
names/types/order including `p_terminal_status TEXT DEFAULT 'pending_review'`;
`wizard_session_id` written from `p_wizard_session_id` in the strategies INSERT; the 5000-cap
spelling; the dailies INSERT column list exactly `(strategy_id, date, daily_return)`; and the
total absence of any handler clause in the body (the fold's atomicity oracle depends on the
raise aborting the whole function).

**STEP 2 — grants.** Re-issue `REVOKE ALL ON FUNCTION public.finalize_csv_strategy_with_returns(UUID, UUID, TEXT, TEXT, JSONB, TEXT) FROM PUBLIC, anon;`
and `GRANT EXECUTE ... TO authenticated;` with a comment stating that `CREATE OR REPLACE`
PRESERVES ACLs and the restatement is for auditability at the newest file.

**C4 — the `service_role` ACL claim.** `20260819120000:365-366`'s COMMENT claims "Grants:
authenticated ONLY" while STEP 3(b) only asserted `authenticated` present and `anon` absent — it
never asserted `service_role` lacks EXECUTE. Close the gap the honest way: add
`REVOKE ALL ON FUNCTION ... FROM service_role;` here AND a matching assertion in STEP 3 below.
This is inert today (the `auth.uid()` guard 42501s a service-role call), so it is hygiene, not a
live hole — say exactly that in the comment.

**STEP 3 — re-issue `COMMENT ON FUNCTION`** superseding `20260819120000:355-369`. Two
corrections ride here because the parent file must not be edited (invariant 11): the ERRCODE map
gains the new 22023 cases, and the parent's attribution of 23505 SOLELY to
`strategies_user_wizard_session_source_uniq` (`20260819120000:114-116`, `:353-368`) is
CORRECTED — `csv_daily_returns_strategy_date_key` is a second source
(`20260624120000:55-56`), now unreachable from a conforming payload because of guard 10 above,
and fenced at the route by 146.1-04/B3.

**STEP 4 — self-verify DO block.** Replicate checks (a)-(g) of `20260819120000:373-495` and ADD
one check per new guard, plus the `service_role` REVOKE assertion. Load the live body with
`pg_get_functiondef(...::regprocedure)` into a variable, then **comment-strip it first**
(`regexp_replace(v_fn_src, '--[^\n]*', '', 'g')`) and match with **word-bounded regexes**.

⛔ **Never match an unbounded substring over the whole body.** That exact class has now produced
THREE measured false-PASSes in this repo: `%5000%` satisfied by `50000`; `%LIMIT 25%` satisfied
by `LIMIT 2500`; a bare `%public.compute_jobs%` satisfied by the INSERT target alone. Any
numeric literal needs `\M` or a `([^0-9]|$)` alternation. Every RAISE in the block names the
CONSEQUENCE of the missing token, not the token (`20260816140000:778-780` states this as law).
Close with a `RAISE NOTICE` naming what is now live.

Finish with `COMMIT;`.

Then regenerate the canonical snapshot with `npm run schema:functions` (hermetic — pure text
replay of `supabase/migrations/**`, no DB, no Docker). ⛔ Never hand-edit
`supabase/schema/functions/finalize_csv_strategy_with_returns.sql`; it carries a
`@generated ... DO NOT EDIT BY HAND` header and a `--check` CI gate.
  </action>
  <verify>
    <automated>
M=supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql
fail(){ echo "FAIL: $1"; exit 1; }
test -f "$M" || fail "migration missing"
[ "$(grep -c 'CREATE OR REPLACE FUNCTION public.finalize_csv_strategy_with_returns' "$M")" -eq 1 ] || fail "not exactly one CREATE OR REPLACE"
grep -qE '^[[:space:]]*DROP[[:space:]]+FUNCTION' "$M" && fail "DROP FUNCTION present - house law forbids it here"
grep -q 'p_rows IS NULL' "$M" || fail "NULL guard missing"
grep -q "p_fmt IS DISTINCT FROM 'trades'" "$M" || fail "empty-array guard is not NULL-safe"
grep -q "p_fmt IS NULL OR p_fmt NOT IN" "$M" || fail "fmt whitelist still NULL-blind"
grep -qE "BETWEEN -10 AND 10([^0-9]|$)" "$M" || fail "value-magnitude guard missing or WIDENED (a bare substring match would accept 'BETWEEN -10 AND 100' - that is the %LIMIT 25%-matches-LIMIT-2500 class this phase exists to close)"
grep -q "now() AT TIME ZONE 'UTC'" "$M" || fail "future-date guard uses a server-timezone date"
grep -qE 'CURRENT_DATE' "$M" && fail "CURRENT_DATE is server-timezone-dependent - use the explicit UTC form"
grep -q 'count(DISTINCT' "$M" || fail "duplicate-date guard missing"
grep -q 'FROM service_role' "$M" || fail "service_role REVOKE missing (C4)"
grep -qE 'jsonb_array_length\(p_rows\)[[:space:]]*>[[:space:]]*5000([^0-9]|$)' "$M" || fail "5000 cap spelling changed or WIDENED to 50000 - STEP 3(d) of the parent pins it"
grep -q "SECURITY DEFINER" "$M" || fail "SECDEF dropped"
grep -q "SET search_path = public, pg_catalog" "$M" || fail "search_path dropped"
grep -q "regexp_replace" "$M" || fail "self-verify does not comment-strip"
grep -qE "NOT LIKE '%" "$M" && fail "unbounded substring match in self-verify - three measured false-PASSes in this repo"
npm run schema:functions
git diff --quiet supabase/schema/functions/finalize_csv_strategy_with_returns.sql && fail "snapshot did not change - the replay did not pick up the new definition"
grep -q 'p_rows IS NULL' supabase/schema/functions/finalize_csv_strategy_with_returns.sql || fail "snapshot does not carry the new guard"
echo OK
    </automated>
  </verify>
  <reversibility rating="costly">Merging supabase/migrations/** to main auto-applies to PROD with no deploy step. Rolling back needs a further forward migration restoring the prior body; the Manual rollback block in the header is that script, written in advance.</reversibility>
  <done>The migration exists at the fixed stamp, replaces the function via CREATE OR REPLACE with all ten guards in the stated order, carries a self-verify that comment-strips and word-bounds every anchor, re-issues the corrected COMMENT and the grants including the service_role REVOKE, and the regenerated snapshot shows the new guards. ⛔ Do NOT commit yet — task 2 commits with this one.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fold SQL gate — new Parts for the A1 guards, the re-homed B5 guards, and C4's Part 3d</name>
  <files>supabase/tests/test_csv_finalize_atomic_fold.sql</files>
  <read_first>
    - `supabase/tests/test_csv_finalize_atomic_fold.sql` in full — header `:1-61` (transaction discipline, register line, Usage), Part 4 `:248-290` (the UNWRAPPED success case, the regression fence that must stay green), Part 5 `:296-351` (the canonical wrapped negative case: `BEGIN … EXCEPTION WHEN OTHERS … GET STACKED DIAGNOSTICS`, then the three-table zero-row proof).
    - `146.1-PATTERNS.md` §3 — the house shape for a `supabase/tests/test_*.sql` Part, message convention, and the ⚠️ note that a case which must SUCCEED is deliberately NOT wrapped in a handler.
  </read_first>
  <behavior>
    Each behaviour below is one lettered sub-Part. Every negative sub-Part asserts three things
    in order: (i) the call RAISED (not `IF NOT raised THEN` … it returned `%`), (ii)
    `RETURNED_SQLSTATE` = `'22023'` exactly, (iii) `strategies`, `strategy_verifications` and
    `csv_daily_returns` each hold ZERO rows for the probe user/session — proving the guard ran
    BEFORE any write, not after.
    - Part 6a `p_rows := NULL`, `p_fmt := 'daily_returns'` → 22023
    - Part 6b `p_rows := '[]'`, `p_fmt := 'daily_returns'` → 22023
    - Part 6c `p_rows := '[]'`, `p_fmt := NULL` → 22023 (the `IS DISTINCT FROM` proof)
    - Part 6d one element with `daily_return` `"NaN"` → 22023
    - Part 6e one element with `daily_return` `1e300` → 22023
    - Part 6f one element with `daily_return` absent → 22023 (SQLSTATE is 22023, NOT 23502)
    - Part 6g one element with `date` `'9999-12-31'` → 22023
    - Part 6h two elements sharing one `date` → 22023 (SQLSTATE is 22023, NOT 23505)
    - Part 6i a conforming 2-row `daily_returns` payload with `daily_return` `9.9` SUCCEEDS and persists 2 dailies — the positive counterpart that proves Part 6 is not vacuously refusing everything
    - Part 6j one element with `daily_return` `10.5` → 22023 — ⭐ **THE NEAR-BOUNDARY PROBE, added by the plan-checker gate (BLOCKER 1). Do not drop it.** Without 6j the whole magnitude guard can be widened to `BETWEEN -10 AND 100` with EVERY detector staying green: 6d's NaN fails any `BETWEEN` (Postgres sorts NaN above all numbers) so it raises regardless of the upper bound, 6e's `1e300` still exceeds 100, and the static grep was a bare substring that `-10 AND 100` satisfies as a prefix. 6j is the only probe that sits between 10 and 100, and with 6i (`9.9` accepted) it forms the discriminating pair that pins the bound at exactly ±10.
    - Part 7a `p_fmt := 'nonsense'` → 22023 (re-homed from csv-finalize-rpc.test.ts)
    - Part 7b `p_strategy_name := ''` → 22023 (re-homed)
    - Part 7c `p_strategy_name` of 81 characters → 22023 (re-homed)
    - Part 3d `p_terminal_status := 'published'` → 22023, zero rows (C4)
  </behavior>
  <action>
Extend `supabase/tests/test_csv_finalize_atomic_fold.sql` with Part 6 (A1's new raises), Part 7
(B5's re-homed guards) and Part 3d (C4). ⛔ Do NOT restructure or renumber the existing Parts
1-5; Part 4's UNWRAPPED trades-empty success case is the regression fence for A1's scoping and
must be left byte-identical.

Follow the file's own conventions exactly (`146.1-PATTERNS.md` §3): no outer file-level
transaction; each writing Part opens its own `BEGIN; SET LOCAL lock_timeout = '5s';` and closes
with `ROLLBACK;`; seeds go through the real FK chain with `gen_random_uuid()`; the caller
identity is set with `PERFORM set_config('request.jwt.claims', json_build_object('sub', <uuid>,
'role', 'authenticated')::text, true)` and reset to NULL at the end of the Part — the fold
42501s without it. ⛔ No psql backslash meta-commands anywhere: CI greps for them and bails
(`.github/workflows/ci.yml:843-858`).

Message convention, copied from Part 5: `TEST FAILED (Part 6d): <observed> - <expected> - <the
consequence>`. State the consequence, not just the mismatch. Example register: a NaN
`daily_return` that finalizes writes a strategy whose Sharpe is undefined for every downstream
consumer and whose KPIs can never be recomputed to a number.

For Part 6f and Part 6h assert the SQLSTATE is `'22023'` **exactly**, not `LIKE '22%'` and not
"any error". These two sub-Parts exist precisely to prove the guard fires ahead of the table
constraint (`csv_daily_returns.daily_return NOT NULL` would give 23502) and ahead of the dailies
unique index (which would give 23505) — a class-pinned assertion would pass on the very failure
mode being fenced.

Part 6i is the anti-vacuity counterpart. Do NOT wrap it in a handler (Part 4's rule, `:268-272`):
a regression must surface as the raw SQLSTATE under `ON_ERROR_STOP=1`. Assert it returns a
non-NULL UUID and that exactly 2 rows landed in `csv_daily_returns` with the submitted values —
so a future guard that refuses everything cannot pass this file.

Extend the file header's `WHAT IS ASSERTED` list and the final `ALL PASS` NOTICE to name the new
Parts. A gate whose header no longer describes it is how a stale claim outlives its code.

Update the register/why-this-file line to record that the fmt / name guards moved here from
`src/__tests__/csv-finalize-rpc.test.ts`, and WHY: that file's live cases are
`it.skipIf(!HAS_LIVE_DB)` and `HAS_LIVE_DB` is false in every CI vitest shard by explicit
instruction (`.github/workflows/ci.yml:286`), so they have never executed in CI; this file is
auto-discovered by the `sql-tests` glob and runs under `psql -v ON_ERROR_STOP=1` against TEST.

Commit tasks 1 and 2 TOGETHER as a single commit. The migration convention (144-§8) requires a
migration's gates to be in the same commit as the migration; splitting them would ship a
function whose new invariants are asserted only by a file that already ran.
  </action>
  <verify>
    <automated>
T=supabase/tests/test_csv_finalize_atomic_fold.sql
fail(){ echo "FAIL: $1"; exit 1; }
for p in "Part 6a" "Part 6b" "Part 6c" "Part 6d" "Part 6e" "Part 6f" "Part 6g" "Part 6h" "Part 6i" "Part 7a" "Part 7b" "Part 7c" "Part 3d"; do
  grep -q "$p" "$T" || fail "$p missing"
done
grep -qE '\\!|\\copy|\\COPY|\\o ' "$T" && fail "psql meta-command present - the sql-tests job greps for these and bails"
[ "$(grep -c 'GET STACKED DIAGNOSTICS' "$T")" -ge 15 ] || fail "fewer wrapped negative cases than sub-Parts declared (13 new negatives - 6a-6h,6j,7a-7c,3d - plus the 2 pre-existing at HEAD = 15; 6i is the UNWRAPPED positive and must NOT add one)"
grep -q "count(\*)" "$T" || fail "no row-count proof - a negative Part that does not count rows cannot prove the guard ran BEFORE the write"
grep -q "set_config('request.jwt.claims'" "$T" || fail "no jwt claim seeding - the fold 42501s"
grep -q "ALL PASS" "$T" || fail "final NOTICE missing"
awk '/Part 4 —/,/Part 5 —/' "$T" | grep -q 'EXCEPTION WHEN' && fail "Part 4 got wrapped in a handler - it must fail raw under ON_ERROR_STOP"
git diff --stat supabase/tests/test_csv_finalize_atomic_fold.sql
echo OK
    </automated>
  </verify>
  <done>Fourteen new sub-Parts exist, each negative one asserting RAISED + exact SQLSTATE 22023 + zero rows across all three tables; Part 6i is the unwrapped positive counterpart; Parts 1-5 are untouched; the header and ALL PASS line name the new Parts; tasks 1+2 are committed as ONE commit. The Parts have NOT yet been executed — 146.1-08 runs them against TEST and that is the blocking gate.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the dead live describe in csv-finalize-rpc.test.ts and re-point the stale citation</name>
  <files>src/__tests__/csv-finalize-rpc.test.ts, src/__tests__/strategy-verifications-rls.test.ts</files>
  <precondition>`node_modules/.bin/vitest` exists in this checkout. GSD worktree agents get no node_modules and `npx vitest` exits MODULE_NOT_FOUND — run `npm ci` first or HALT; do not report this task green on an unrun suite.</precondition>
  <read_first>
    - `src/__tests__/csv-finalize-rpc.test.ts` — the boundaries matter: `describe("finalize_csv_strategy RPC (Phase 15 / CSV-01)")` spans `:129-318` and holds the six `it.skipIf(!HAS_LIVE_DB)` cases; `describe("parseCsvMetadata — asset_class closed-set validation (#597 part 2)")` `:336-385` and `describe("buildMetadataUpdatePayload — asset_class UPDATE persistence (#597 part 2)")` `:387-407` are PURE UNIT tests that DO run in CI.
    - `src/__tests__/strategy-verifications-rls.test.ts:15-17` — the stale cover-citation.
  </read_first>
  <action>
⛔ **Do NOT delete `src/__tests__/csv-finalize-rpc.test.ts`.** The review's "nine 22023
assertions" figure was a `grep -c` of the string, not an assertion count — there are THREE real
guard assertions — and the file also holds **12 pure-unit tests that DO run in CI**, covering
exactly the `asset_class` path 146.1-04/A4 is about. Deleting the file deletes live coverage.

Delete ONLY the `describe("finalize_csv_strategy RPC (Phase 15 / CSV-01)")` block and any
imports/helpers it alone used (`HAS_LIVE_DB`, the live-db client helper) that become unreferenced
— `npm run lint` will name them. Every one of its six cases calls `finalize_csv_strategy`, a
function `20260819120000` DROPped, so they can never pass again against a live DB; they were
skipping silently rather than failing, which is worse than absent.

In place of the deleted block, leave a dated tombstone comment at the top of the file naming:
what was removed; that the function was dropped by `20260819120000`; that the three 22023 guard
behaviours (invalid fmt, empty name, name > 80) now live in
`supabase/tests/test_csv_finalize_atomic_fold.sql` Part 7, which executes in CI whereas the
deleted cases never did; and that the two `#597 part 2` describes below it are LIVE and
load-bearing so a future reader does not "finish the cleanup".

Re-point `src/__tests__/strategy-verifications-rls.test.ts:15-17` — it cites
`csv-finalize-rpc.test.ts` as the integration test that covers the behaviour. Point it at
`supabase/tests/test_csv_finalize_atomic_fold.sql` and name the Part. A citation that outlives
the thing it cites is how the three false forwarding comments this phase already corrected came
to exist.

Commit as its own commit, separate from tasks 1+2.
  </action>
  <verify>
    <automated>
fail(){ echo "FAIL: $1"; exit 1; }
F=src/__tests__/csv-finalize-rpc.test.ts
test -f "$F" || fail "file was deleted - it holds 12 live unit tests"
grep -q 'it.skipIf' "$F" && fail "a skipIf live case survived"
grep -q 'parseCsvMetadata' "$F" || fail "the #597 unit describe was collateral damage"
grep -q 'buildMetadataUpdatePayload' "$F" || fail "the #597 UPDATE-persistence describe was collateral damage"
grep -q 'csv-finalize-rpc.test.ts' src/__tests__/strategy-verifications-rls.test.ts && fail "stale citation still points at the retired suite"
grep -q 'test_csv_finalize_atomic_fold' src/__tests__/strategy-verifications-rls.test.ts || fail "citation not re-pointed"
npx --no-install vitest run src/__tests__/csv-finalize-rpc.test.ts src/__tests__/strategy-verifications-rls.test.ts --no-file-parallelism --reporter=basic || fail "suite red"
npx --no-install vitest run src/__tests__/csv-finalize-rpc.test.ts --reporter=json 2>/dev/null | node -e 'let s="";process.stdin.on("data",d=>s+=d).on("end",()=>{const j=JSON.parse(s);if(j.numPassedTests<12){console.error("FAIL: only "+j.numPassedTests+" tests ran, expected >=12 live unit tests");process.exit(1)}console.log("OK "+j.numPassedTests+" tests")})'
npm run lint
    </automated>
  </verify>
  <done>The dead live describe is gone, a dated tombstone names where its coverage went and warns off the two live describes, at least 12 tests still pass in that file, the stale citation points at the SQL gate, and lint is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authenticated client → `finalize_csv_strategy_with_returns` (direct PostgREST RPC) | An authenticated user can call the SECDEF function directly, bypassing every validator in `csv-finalize/route.ts`. This is A1's entire reachability. |
| `authenticated` role → `SECURITY DEFINER` body | The function executes as its owner; only the in-body `auth.uid()` guards confine it to the caller's own tenant. |
| migration file → PROD database | Merging `supabase/migrations/**` to `main` auto-applies with no deploy step or prompt. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.1-01 | Tampering | `finalize_csv_strategy_with_returns` input guards | medium | mitigate | Task 1 guards 3-10: NULL, NULL-safe fmt, trades-scoped empty, NaN/±Inf/magnitude, NULL value, absurd/future date, duplicate date — all 22023 before any write. Confined to own tenant by the pre-existing `auth.uid() = p_user_id` guard, which Task 1 must not weaken. |
| T-146.1-02 | Elevation of Privilege | ACL drift on `CREATE OR REPLACE` | low | mitigate | `CREATE OR REPLACE` preserves ACLs; grants restated anyway, `service_role` REVOKEd (C4), and both asserted in the STEP 4 self-verify. |
| T-146.1-03 | Tampering | Silent invariant loss in the re-based body | high | mitigate | The parent's STEP 3 never re-runs. Task 1 replicates checks (a)-(g) in a NEW self-verify plus one check per new guard; the `<verify>` block statically pins SECDEF, search_path and the 5000-cap spelling. |
| T-146.1-04 | Repudiation | A self-verify that cannot fail | high | mitigate | Comment-strip then word-bounded regex; unbounded substring matching is grep-refused in `<verify>`. Three measured false-PASSes of this exact class are on record. |
| T-146.1-05 | Denial of Service | Migration auto-applies to PROD unrehearsed | high | transfer | Deferred to 146.1-08's blocking orchestrator gate: TEST rehearsal via Supabase MCP + SQL gates run on TEST BEFORE merge. |
| T-146.1-SC | Tampering | npm/pip/cargo installs | n/a | accept | This plan installs no packages. `146.1-RESEARCH.md` records the Package Legitimacy Audit as not applicable — every library used is an existing declared dependency. `npm ci` in a preconditon restores the committed lockfile only. |
</threat_model>

<verification>
1. `bash` blocks in each task's `<verify>` all exit 0.
2. `npm run schema:functions:check` exits 0 (the committed snapshot is not stale).
3. `npm run lint` and `npm run typecheck` exit 0.
4. `npx vitest run src/__tests__/csv-finalize-rpc.test.ts src/__tests__/strategy-verifications-rls.test.ts --no-file-parallelism` is green with ≥12 tests in the first file.
5. ⛔ The SQL Parts are NOT verified by this plan. They are executed against TEST by 146.1-08, which is a hard merge gate. Reporting this plan complete does NOT mean the guards work — it means they are written and statically conformant.
</verification>

<success_criteria>
- [ ] `supabase/migrations/20260819130000_csv_finalize_fold_input_guards.sql` exists, uses `CREATE OR REPLACE` exactly once, contains zero `DROP FUNCTION` statements, and its header pastes the live re-base grep output.
- [ ] The body contains all ten guards in the stated order, uses `IS DISTINCT FROM` for the trades scoping, `p_fmt IS NULL OR p_fmt NOT IN` for the whitelist, `NOT (… BETWEEN -10 AND 10)` for magnitude, and `(now() AT TIME ZONE 'UTC')::date` (never `CURRENT_DATE`) for the date fence.
- [ ] `SECURITY DEFINER`, `SET search_path = public, pg_catalog`, the 6-arg signature, `wizard_session_id` write, the `jsonb_array_length(p_rows) > 5000` spelling, the `(strategy_id, date, daily_return)` column list, the 42501 message literal, and the absence of any handler clause all survive the re-base.
- [ ] The self-verify DO block comment-strips before matching and contains zero unbounded-substring matches.
- [ ] `supabase/schema/functions/finalize_csv_strategy_with_returns.sql` regenerated by `npm run schema:functions` (never hand-edited) and carries the new guards.
- [ ] `supabase/tests/test_csv_finalize_atomic_fold.sql` gains Parts 6a-6i, 7a-7c and 3d; Parts 1-5 are byte-identical; Part 6i is unwrapped.
- [ ] `src/__tests__/csv-finalize-rpc.test.ts` still exists, has zero `it.skipIf` cases, still runs ≥12 tests, and carries a dated tombstone.
- [ ] `src/__tests__/strategy-verifications-rls.test.ts` cites the SQL gate, not the retired suite.
- [ ] Tasks 1+2 are ONE commit; task 3 is a separate commit.
- [ ] Every artifact's prose describes A1 as authenticated direct-RPC own-tenant boundary hardening — no document produced by this plan calls it a live user-facing break.
</success_criteria>

<falsifiability_ledger>
| # | Success criterion | What makes it go RED | Named neuter to observe (record the receipt in the SUMMARY) |
|---|---|---|---|
| F-01-1 | NULL `p_rows` raises 22023, zero rows | Delete the `IF p_rows IS NULL` raise from the migration body, re-apply to TEST, run the gate | **Neuter A1-NULL**: remove guard 3. Part 6a must RAISE `TEST FAILED (Part 6a)`. If it still passes, the Part is not calling the function it thinks it is. |
| F-01-2 | The trades scoping is NULL-safe | Change `IS DISTINCT FROM` back to `<>` | **Neuter A1-FMT**: Part 6c (`p_fmt := NULL`, `p_rows := '[]'`) must RAISE, while 6a/6b stay green. This is the whole reason 6c exists. |
| F-01-3 | Value guard catches poison | Widen `BETWEEN -10 AND 10` to `-1e9 AND 1e9` | **Neuter A1-VALUE**: ⚠️ TWO probes required. Part 6e (`1e300`) must go RED. Part 6d (`"NaN"`) will still PASS under the widened bound because NaN fails ANY `BETWEEN` — a NaN-only neuter false-PASSes and is the same failure class as the `%5000%` gate. |
| F-01-4 | Duplicate dates are 22023, not 23505 | Delete the `count(DISTINCT …)` guard | **Neuter A1-DUP**: Part 6h must RED with `SQLSTATE 23505` in the observed value, proving the assertion is exact-code and not `LIKE '22%'`. |
| F-01-5 | The guard runs BEFORE any write | Move any new guard below the `strategies` INSERT | **Neuter A1-ORDER**: the row-count arm of the affected sub-Part (`n_strat <> 0`) must RED. A Part with no row-count arm cannot detect this and is therefore incomplete. |
| F-01-6 | Part 6 is not vacuously refusing everything | Make any guard unconditional | **Neuter A1-DENYALL**: Part 6i (the unwrapped conforming payload) must RED with a raw SQLSTATE under `ON_ERROR_STOP=1`. Without 6i, a deny-everything body passes Part 6 perfectly. |
| F-01-7 | trades-empty still works | Scope the empty guard to all fmts | **Neuter A1-TRADES**: existing Part 4 must RED. No new test needed — it is the pre-existing regression fence. |
| F-01-8 | `published` terminal status refused | Delete the `p_terminal_status NOT IN` guard | **Neuter C4-3D**: Part 3d must RED. Today this behaviour ships ungated, which is why 3d is being added. |
| F-01-9 | B5 coverage actually executes | — | The criterion is falsified by inspection: `grep -c it.skipIf src/__tests__/csv-finalize-rpc.test.ts` must be 0 AND Part 7a/7b/7c must appear in a file matched by `supabase/tests/test_*.sql`. If either fails, the "re-homed" coverage is still not running. |
| F-01-10 | 12 live unit tests preserved | Delete the file | The JSON-reporter assertion in Task 3 fails at `numPassedTests < 12`. This criterion exists because "delete the file" was the review's original recommendation and it was WRONG. |
| F-01-11 | Self-verify cannot false-PASS | Replace a bounded regex with a substring match | **Neuter A1-SUBSTR**: widen the 5000 literal to 50000 in the body; the self-verify must RAISE. Under a substring form it PASSes — that exact case was measured on TEST 2026-08-18. |
</falsifiability_ledger>

<output>
Create `.planning/phases/146.1-review-v1-19-xhigh-close-out-fold-guards-resolve-arm-honesty/146.1-01-SUMMARY.md` when done.

The SUMMARY MUST record, per row of the Falsifiability Ledger, whether the neuter was observed
and what the observed failure text was. A neuter that was reasoned about rather than run is
recorded as NOT OBSERVED, explicitly. ⛔ Neuters that require applying SQL to TEST cannot be run
by this plan's executor — mark them `DEFERRED TO 146.1-08` by name so the orchestrator inherits
a list, not a vague obligation.
</output>
