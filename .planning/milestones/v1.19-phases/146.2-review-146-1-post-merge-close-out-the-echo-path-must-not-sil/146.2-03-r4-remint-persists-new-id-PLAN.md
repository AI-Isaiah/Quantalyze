---
phase: 146.2
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-burn-persistence.test.tsx
autonomous: true
requirements: [R4]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "After the changed-content re-mint, the persisted wizard envelope carries the NEW session id together with the cleared burn — a reload in the re-mint window can never resume a spent id with a disarmed fence (ROADMAP SC#4)"
    - "The telemetry event still reports the RETIRED id (deliberate, per the file's own comment)"
  artifacts:
    - path: src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
      provides: "re-mint ordering fix: mint into a local, feed the React setter and the persisted envelope from that ONE local, single save call"
    - path: src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-burn-persistence.test.tsx
      provides: "regression case pinning the pair (new id + cleared burn) in the persisted envelope"
  key_links:
    - from: "re-mint site in WizardClient.tsx (~:595)"
      to: "saveWizardState serialized queue (src/lib/wizard/localStorage.ts)"
      via: "one save call carrying BOTH wizardSessionId next and failedCsvSubmitSig null — the queue serializes by call order, one call cannot de-sync"
      pattern: "saveWizardState"
---

<objective>
Close R4: the RT-3 re-mint at `WizardClient.tsx:595` persists the RETIRED session id —
`saveWizardState({ wizardSessionId, …, failedCsvSubmitSig: null })` captures the OLD closure
value, then `setWizardSessionId(newWizardSessionId())` runs with no follow-up save. A reload in
that window restores the SPENT id with the burn CLEARED — precisely the state RT-3 exists to
prevent — and the changed resubmit then takes a 409 with nothing left to re-mint it. The
name-autosave cannot repair it (gated on step csv_upload; the re-mint fires on the submit step).

The fix conforms to the file's own house rule (its comment at :619-621: the ref and the envelope
are set from one expression so they cannot disagree): mint ONCE into a local, feed BOTH the React
setter and the persisted envelope from that local, in ONE save call — the saveWizardState queue
serializes by CALL order, so one call cannot de-sync. The other two re-mint sites (:853, :900)
are NOT this bug (both run after clearWizardState, no envelope survives) — instance fix, class
already checked at plan time; do not touch them.

Purpose: ROADMAP 146.2 SC#4.
Output: reordered re-mint + regression case, observed RED first (shipped ordering is the free neuter).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-CONTEXT.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-PATTERNS.md

Read before coding (PATTERNS.md section 5 quotes the defective site and the conforming shape):
- src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:540-640 (the re-mint site, the RT-3 write-through house rule, the step gate on the name-autosave)
- src/lib/wizard/localStorage.ts:150-160, :270-310 (failedCsvSubmitSig sticky field, saveWizardState async + serialized saveQueue semantics)
- src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-burn-persistence.test.tsx (existing RT-3 suite: advanceToSubmit helper, reload simulation; the case at ~:355 is the sibling of the missing one)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — pin the pair (new id + cleared burn) in the persisted envelope, observe it fail against HEAD</name>
  <precondition>node_modules present in the working checkout (npx vitest --version succeeds); in a fresh worktree run npm install first</precondition>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-burn-persistence.test.tsx</files>
  <behavior>
    - Drive the changed-content re-mint path (the existing suite's helpers reach it), then read the persisted envelope via loadWizardState.
    - Assert the envelope's wizardSessionId equals the NEW (post-re-mint) id AND failedCsvSubmitSig is null — the pair RT-3's threat model requires never de-syncs. Asserting the pair in ONE case is the point: id-only or burn-only assertions each pass on HEAD.
    - The WHY message states the threat: a reload between the burn-clear save and any next save must not restore a spent id with a disarmed fence.
  </behavior>
  <action>
    Add the regression case to the existing RT-3 suite following its house style (sibling case at
    ~:355 is the template — same helpers, same reload simulation). To read the NEW id for the
    assertion, capture it from the component state the suite already observes (or from the
    newWizardSessionId mock if the suite mocks it — follow whichever mechanism the sibling cases
    use). Run the file and OBSERVE the case FAIL against HEAD: the shipped ordering persists the
    retired id, so this red is free and IS the named neuter (SC#5). Record the verbatim failure in
    the commit body. Commit as test(146.2-03).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-burn-persistence.test.tsx" --no-file-parallelism 2>&1 | tail -15 (EXPECTED failing at this task on the new case only; capture the vitest exit code directly)</automated>
  </verify>
  <done>New case observed RED against HEAD with output recorded; all pre-existing RT-3 cases green</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — mint-first ordering, one save</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx</files>
  <action>
    At the re-mint site (~:590-608): compute the new id ONCE into a local const; clear the
    failedCsvSubmitSigRef; call setWizardSessionId with that local; issue ONE fire-and-forget
    saveWizardState call carrying the local as wizardSessionId together with failedCsvSubmitSig
    null and the existing strategyId-empty-string sentinel, step, source, strategyName fields —
    exactly the conforming shape PATTERNS section 5 spells out. Keep the telemetry call
    unchanged, still reading the RETIRED id from the old binding (its comment at :604-606 says
    that is deliberate — preserve comment and behavior). No flush choreography, no second save
    (Don't Hand-Roll: the serialized queue makes one call sufficient). Re-cut the surrounding
    comment to state the ordering invariant (setter and envelope fed from one local). Run the
    suite green. Commit as fix(146.2-03).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.csv-burn-persistence.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>Full RT-3 suite green including the new pair case; tsc clean; the other two re-mint sites untouched</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser localStorage → wizard resume | a persisted envelope is replayed into live wizard state on reload |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.2-06 | Tampering | wizard resume state | medium | mitigate | the persisted pair (session id, burn) can no longer de-sync — one save carries both; the server-side equality refusal remains the operative fence if the write never lands (the file's own fire-and-forget rationale) |
| T-146.2-SC | Tampering | package installs | low | accept | zero package installs in this phase |
</threat_model>

<verification>
- RT-3 suite green under `--no-file-parallelism`; tsc clean.
- git diff confirms only the one re-mint site and its comment moved; sites :853/:900 untouched.
</verification>

<success_criteria>
- ROADMAP 146.2 SC#4: a reload in the re-mint window resumes the NEW id with the burn cleared — never a spent id with a disarmed fence.
- SC#5: regression case observed RED against shipped HEAD (the free neuter) before the fix.
</success_criteria>

<output>
Create `.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-03-SUMMARY.md` when done, recording the RED observation verbatim.
</output>
