# Plan 146.2-08 — Migration gate (ORCHESTRATOR-ONLY)

**Status:** Tasks 1–2 COMPLETE (pre-merge rehearsal + evidence). Task 3 (post-merge PROD
verification) PENDING — it cannot run before the merge that applies the migrations to PROD.

## Task 1 — TEST rehearsal

Both migrations applied to TEST (`qmnijlgmdhviwzwfyzlc`) in timestamp order. TEST was quiet
(zero in-flight CI runs verified via `gh run list` immediately before each apply), per the
shared-TEST-DB contention rule.

### `20260819150000` — sweep readmit attempt ceiling

Applied, then the deployed body re-read out of `cron.job` and MEASURED (not a silent pass):

| property | value | meaning |
|---|---|---|
| `public.compute_jobs` refs | **3** | was 2 — the ceiling subquery is present |
| `orphaned_running_reaped` refs | **2** | the inherited exemption + the new ceiling |
| ceiling counts markers | **true** | `count(*) … orphaned_running_reaped … < 3` matched |
| word-bounded `< 3` | **true** | rejects `<= 3` and a widening to `30`/`300` |
| `LIMIT 25` word-bounded | **true** | the separate per-tick bound, intact |
| schedule | `35 * * * *` | off the 142 reaper quarter-hour grid |
| **`username` / `database`** | **`postgres` / `postgres`** | ⭐ closes the RLS audit's WARNING 1 for this apply |
| rows / active | 1 / true | no duplicate registration |

⚠️ The `username` read is the point: the RLS audit found NO cron migration in this repo has ever
asserted it, and the `v_count <> 1` guard is RLS-BLIND to the duplicate-row path a non-BYPASSRLS
applier would create. Measured `postgres` here ⇒ neither drift path occurred.

### `20260819151000` — fold GUARD 1 (+ GUARD 2) NULL-safety

Applied via the canonical snapshot `supabase/schema/functions/finalize_csv_strategy_with_returns.sql`
(byte-identical to the migration body; the fixer's regen diffs empty). Post-apply catalog state:

| property | value |
|---|---|
| `prosecdef` | `true` |
| `pronargs` | `6` |
| `proconfig` | `search_path=public, pg_catalog` |
| overloads | **1** — no second function minted by signature drift |
| owner | `postgres` |
| ACL | `postgres=X/postgres \| authenticated=X/postgres` — anon and service_role hold NO execute |
| EXCEPTION handler | **absent** (comment-stripped check) — the fold's atomicity mechanism |
| GUARD 1 / GUARD 2 fixes | both present |

### Behavioural probes — the guards RAISE, not merely contain the right text

| probe | SQLSTATE | message |
|---|---|---|
| A — GUARD 1, NULL `p_terminal_status` | **22023** | `p_terminal_status <NULL> is not allowed …` |
| B — GUARD 2, NULL `p_user_id` (live `auth.uid()`) | **42501** | `p_user_id (<NULL>) does not match auth.uid (…)` |
| C — CONTROL, valid status `'private'` | 42501 | **advanced PAST GUARD 1**, refused at GUARD 2 |

Both now raise the codes their own `COMMENT` ERRCODE map promises. Pre-fix each surfaced as
**23502** from a NOT NULL column — an error naming a COLUMN instead of the offending INPUT.

⭐ **Probe C is the anti-vacuity control.** A valid terminal status passed GUARD 1 and was refused
further down. Without it, "GUARD 1 raises 22023" is satisfied by a guard that refuses EVERYTHING.

**Side effects: none.** `SELECT count(*) FROM strategies WHERE name LIKE 'probe-%'` → **0**. The
guards refused before any write, which is the fold's atomicity property holding.

## Task 2 — evidence for the blocking merge checkpoint

Pre-merge reviews, run per founder instruction ("Run Migration Reviews and Rls policy Reviews
before applying"), on BOTH migrations SEPARATELY — a PASS on one says nothing about the other:

| migration | `migration-reviewer` | `rls-policy-auditor` |
|---|---|---|
| `20260819150000` | **PASS** — 0 blocker, 0 warning, 2 INFO (prose citations) | **PASS** — 0 critical/high, 2 MEDIUM |
| `20260819151000` | **PASS** — 1 WARNING (GUARD 2) → **FIXED** | **PASS** — 1 MEDIUM (`proconfig` pin) → **FIXED** |

Both reviewers independently `sed`-extracted the prior bodies and ran real diffs rather than
trusting either migration's own header delta list. Re-base results:
- `20260819150000` vs `20260819130500`: **+7 lines, 0 deletions, 0 modifications** — the ceiling only.
- `20260819151000` vs `20260819130000`: **exactly 2 hunks** — GUARD 1 and GUARD 2, nothing else.
  Orchestrator re-verified this independently: 4 non-comment changed lines, all four accounted for.

Two findings were FIXED before apply rather than logged (commit `9b6c6f17`):
1. **GUARD 2 carried the same NULL-unsafe `<>` shape this migration exists to close** — the file
   states the law itself at GUARD 8 and violated it. Closed the class rather than point-fixing.
2. **The standing gate never read `proconfig`** — so the `search_path` pin was asserted only once
   at apply time, with no recurring gate. If lost, an RLS-exempt definer body would resolve
   tenant tables through the CALLER's `search_path`.

Two non-blocking findings LOGGED to `TODOS.md` per the blast-radius stopping rule: no cron
migration ever asserts `cron.job.username` (class fix), and the ceiling is SILENT at exhaustion
(the Sentry alert is keyed on the HEAL, so giving up emits no signal — founder call).

## Task 3 — post-merge PROD verification — PENDING

Cannot run until the merge applies both migrations to PROD. Required checks:
- live cron body carries the ceiling conjunct; ONE real sweep tick `succeeded`
- fold GUARD 1 + GUARD 2 carry their NULL arms; `prosecdef`/`proconfig`/ACLs intact
- `suspect_and_visible` still **0**
- PROD `schema_migrations` tip advances to `20260819151000`

## PROD pre-merge census (re-measured immediately before the gate)

- `schema_migrations` tip: `20260819130500` (both new files sort strictly after — no backdating)
- reaped markers / running / pending: **0 / 0 / 0** — the `20260819150000` header's dated census
  still holds, so its in-file paragraph needs no correction
- `suspect_and_visible`: **0**; csv strategies created since A4: **0** ⇒ no backfill in scope
