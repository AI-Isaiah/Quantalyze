---
phase: 146.2
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/verify-strategy/route.ts
  - src/app/api/verify-strategy/route.seam.test.ts
autonomous: true
requirements: [R2]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A raw exchange passphrase inlined into an undici-style error message (TRAP-1) never reaches the dispatched Sentry payload from any of the verify-strategy capture sites"
    - "The scrub array and the outgoing teaserContext credential fields are built from ONE shared file-local tuple, so a fourth credential cannot disagree by construction"
    - "The docblock's enumeration matches the array (SC#2) — no 'Both remaining members' prose over a three-member reality"
  artifacts:
    - path: src/app/api/verify-strategy/route.ts
      provides: "three-member perRequestSecrets derived from a shared credential tuple; corrected docblock"
    - path: src/app/api/verify-strategy/route.seam.test.ts
      provides: "TRAP-1 both-directions case extended with RAW_PASSPHRASE"
  key_links:
    - from: "credential tuple in verify-strategy route"
      to: "captureToSentry secrets option at :364,:420,:464,:485 and scrubSeamError at :424,:468,:489"
      via: "perRequestSecrets referencing the same tuple at every consumer"
      pattern: "perRequestSecrets"
---

<objective>
Close R2 (SECURITY): `src/app/api/verify-strategy/route.ts:285-288` builds
`perRequestSecrets = [body.api_key, body.api_secret]` while `:228-230` puts `body.passphrase`
into the outgoing `teaserContext`. undici inlines the request body into `err.message` (TRAP-1 —
the file's own docblock documents the mechanism), so on OKX/KuCoin/Coinbase-style keys a RAW
EXCHANGE CREDENTIAL reaches Sentry from six capture/scrub sites.

**Instance, NOT a class — orchestrator-verified, do not sweep:** exactly two `perRequestSecrets`
sites exist in `src/`, and the sibling (`keys/validate-and-encrypt/route.ts:401`) already carries
all three members. The class-closure here is (a) the third member, (b) the docblock enumeration
fix (`:276-280` reads "Both remaining members" — complete-sounding while listing two of three),
and (c) a FILE-LOCAL structural agreement: the outgoing body's credential fields and the scrub
array derive from one shared tuple so the enumeration cannot rot again. No cross-route framework
(Simplicity First — the other routes already agree).

Purpose: no raw exchange credential reaches Sentry (phase goal, ROADMAP 146.2 SC#2).
Output: fixed route + extended seam test, observed RED first (shipped HEAD is the free neuter).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-CONTEXT.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-PATTERNS.md

Read before coding:
- src/app/api/verify-strategy/route.ts:200-300 (teaserContext build, docblock, perRequestSecrets), consumer sites :364,:420,:424,:464,:468,:485,:489
- src/app/api/verify-strategy/route.seam.test.ts:480-521 (the TRAP-1 both-directions template — it runs the REAL captureToSentry with only the transport mocked; the header explains why route.test.ts cannot host this)
- src/app/api/keys/validate-and-encrypt/route.ts:401 (the already-correct sibling shape)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — extend the TRAP-1 seam case with RAW_PASSPHRASE and observe it fail against HEAD</name>
  <precondition>node_modules present in the working checkout (npx vitest --version succeeds); in a fresh worktree run npm install first</precondition>
  <files>src/app/api/verify-strategy/route.seam.test.ts</files>
  <behavior>
    - The fixture request body (postReqWithSecret) gains a passphrase field carrying a RAW_PASSPHRASE constant (grep-unique literal, house style like the existing RAW_API_KEY).
    - The fabricated undici-style thrown message inlines RAW_PASSPHRASE alongside the existing raw key/secret.
    - The dispatched Sentry payload is asserted .not.toContain(RAW_PASSPHRASE), with a failure message naming WHY (only the route's secrets array can know a body credential — same register as the existing assertions).
  </behavior>
  <action>
    Extend the TRAP-1 both-directions case in route.seam.test.ts per the behavior list, copying the
    exact shape of the existing RAW_API_KEY/RAW_API_SECRET assertions at :480-521. Run the file and
    OBSERVE the new assertion FAIL — shipped HEAD omits passphrase from the array, so this red is
    free and IS the named neuter (SC#5). Record the verbatim failure in the commit body. Commit as
    test(146.2-02).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/verify-strategy/route.seam.test.ts --no-file-parallelism 2>&1 | tail -15 (EXPECTED failing at this task on the passphrase assertion only; capture the vitest exit code directly, never $? after a pipe)</automated>
  </verify>
  <done>New assertion observed RED against HEAD with output recorded; all pre-existing seam cases still green</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — shared credential tuple + docblock enumeration fix</name>
  <files>src/app/api/verify-strategy/route.ts</files>
  <action>
    In verify-strategy route.ts: declare ONE file-local const tuple of the request-body credentials
    (api_key, api_secret, passphrase — passphrase possibly undefined, which the scrub helpers accept
    as unknown[] members; the sibling at validate-and-encrypt:401 already does this). Build the
    teaserContext credential fields AND perRequestSecrets from that tuple's members so the outgoing
    body and the scrub array cannot disagree by construction — keep the existing conditional that
    omits passphrase from teaserContext when undefined (the wire shape must not change; only the
    scrub array unconditionally includes the member). Rewrite the docblock at :276-280: the
    enumeration must list all three credentials and state the tuple-agreement invariant (the prose
    defect was an enumeration that read complete while incomplete — the fix is naming the mechanism,
    not just the third literal). Verify all six consumer sites still reference perRequestSecrets
    (no site left holding a stale inline array). Run the seam file green, then the route's other
    test files for the surface. Commit as fix(146.2-02).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/verify-strategy/route.seam.test.ts --no-file-parallelism && npx tsc --noEmit && grep -c "perRequestSecrets" src/app/api/verify-strategy/route.ts</automated>
  </verify>
  <done>Seam file fully green including the passphrase assertion; tsc clean; grep confirms the consumer count did not shrink (>= 7 references: declaration + six sites); docblock enumeration matches the array</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route → Sentry (de.sentry.io) | error payloads cross to a third party; body credentials must be scrubbed before crossing |
| route → upstream venue seam | request body carries raw credentials that undici can reflect into error messages |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.2-04 | Information Disclosure | verify-strategy capture sites | high | mitigate | passphrase joins perRequestSecrets via the shared tuple; TRAP-1 seam test pins it with the REAL captureToSentry in the loop |
| T-146.2-05 | Information Disclosure | future fourth credential | medium | mitigate | structural tuple agreement — body credential fields and scrub array share one declaration |
| T-146.2-SC | Tampering | package installs | low | accept | zero package installs in this phase |
</threat_model>

<verification>
- Seam file green under `--no-file-parallelism`; tsc clean.
- The adjacent process-key-client console-channel observation (scrubSeamError with header-derived values only, no captureToSentry in that file) is OUT OF SCOPE per the stopping rule — plan 06's bookkeeping task records it in TODOS.md; do NOT fix it here.
</verification>

<success_criteria>
- ROADMAP 146.2 SC#2: passphrase scrubbed from every Sentry capture site on the route; docblock enumeration matches the array.
- SC#5: the regression test was observed RED against shipped HEAD (the free neuter) before the fix.
</success_criteria>

<output>
Create `.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-02-SUMMARY.md` when done.
Record the RED observation verbatim and note the process-key-client console observation as handed to plan 06's TODOS task.
</output>
