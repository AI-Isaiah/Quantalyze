---
phase: 146.2
plan: 07
type: execute
wave: 2
depends_on: ["146.2-01"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx
autonomous: true
requirements: [R6, W1]
estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The echo's honest sentence (200, ok true, human_message present) is RENDERED to the user before navigation — C1's founder-chosen mitigation is no longer inert (R6)"
    - "A fresh success (no human_message) navigates exactly as today — zero change to the first-submit flow"
    - "The upstream-arm test constants labelled 'route.ts — verbatim' again exist verbatim in route.ts at HEAD (W1)"
  artifacts:
    - path: src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
      provides: "success-path echo notice: renders human_message with an explicit continue action instead of auto-navigating"
    - path: src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx
      provides: "echo-notice case: sentence renders, no auto-navigation, continue fires onSubmitted"
    - path: src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx
      provides: "re-typed HEAD-accurate constants, provenance claim true again"
  key_links:
    - from: "CsvSubmitStep success branch"
      to: "route echo envelope (200 + ok + human_message, route.ts:1953-1963)"
      via: "client discriminator res.ok && data.ok === true && typeof data.human_message === 'string' — the only shape the echo produces (refuse() alone makes ok:false 409s)"
      pattern: "human_message"
---

<objective>
Close R6 and W1 — the client half of the echo-copy story, sequenced after plan 01 because the
route now ships TWO echo sentences (the shipped no-op sentence and plan 01's FILL-arm variant)
and the default refuse literal had to be confirmed stable before W1 re-types it.

**R6 (three independent confirmations):** `CsvSubmitStep` reads `human_message` only inside the
error/idempotent branch; a 200 with ok:true goes straight to `onSubmitted(strategy_id)` and the
WizardClient handler navigates immediately. No success-path consumer exists anywhere — the
founder-chosen C1 sentence ("open the strategy to check it holds the numbers you meant to
upload") is spoken by the server and heard by no one. Non-regression, and WHERE it renders is a
product call.

**Product decision (Claude's discretion, taken here so the executor does not improvise):**
option (a), render in-step. When a submit response is 200 + ok:true + human_message present, the
step renders the wire sentence verbatim as a non-blocking notice and does NOT auto-call
onSubmitted — auto-navigating away would defeat the sentence's own instruction to check the
numbers. An explicit continue action (existing button vocabulary) fires onSubmitted with the
strategy id. A fresh success (no human_message) auto-continues exactly as today, so the
first-submit flow is untouched. No prop-signature change, smallest diff, the user reads the
sentence at the moment of the echo. DESIGN.md governs the visual treatment — read it before
styling; no disabled buttons (v1.11 UAT direction).

**W1:** `CsvSubmitStep.upstream-arm.test.tsx:51-58` declares two constants labelled
"route.ts — verbatim" that exist NOWHERE in route.ts at HEAD (both sentences were re-cut in
146.1). The tests are behaviorally valid (they assert the mocked wire sentence renders) but the
provenance claim is false. Re-type both from post-plan-01 HEAD, preserving the file's
hand-typed-never-imported independent-oracle rule.

Output: rendered echo notice + provenance-true fixtures, RED-first.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-CONTEXT.md
@.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-01-SUMMARY.md
@DESIGN.md

Read before coding:
- src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx:290-370, :420-435 (the success path, the error/idempotent branch that already renders human_message, KNOWN_CSV_FINALIZE_CODES membership)
- src/app/api/strategies/csv-finalize/route.ts — the two echo sentences at HEAD after plan 01 (the no-op sentence near :1173 and the FILL-arm variant plan 01 added), and the default refuse literal + CSV_PERSIST_FAIL sentence (W1's re-type sources)
- src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx:40-60 (the hand-typed rule and both stale constants), assertions at :241,:251,:293,:303
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: R6 — render the echo sentence in-step, RED first</name>
  <precondition>node_modules present in the working checkout (npx vitest --version succeeds); in a fresh worktree run npm install first</precondition>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx</files>
  <behavior>
    - Echo case: mock a 200 body with ok true, a strategy_id, a status, and a human_message constant → the sentence renders verbatim; onSubmitted is NOT called on response arrival; activating the continue action calls onSubmitted exactly once with the strategy id. (The onSubmitted half keeps the case from passing vacuously on a component that stopped submitting.)
    - Fresh case (control): mock the same 200 WITHOUT human_message → no notice, onSubmitted fires immediately — today's behavior, pinned so the new branch cannot leak into the fresh flow.
  </behavior>
  <action>
    Write both cases in CsvSubmitStep.test.tsx using the suite's existing fetch-mock machinery,
    run, observe the echo case RED against HEAD (the component navigates without rendering —
    free red; record it). Then implement in CsvSubmitStep.tsx: in the success branch, gate on
    the exact echo discriminator (response ok AND body ok true AND human_message a string);
    when it holds, set notice state rendering the wire sentence verbatim (no editorialising —
    the SEAMUX-04 register lives server-side) plus a continue button in the step's existing
    button vocabulary that invokes onSubmitted with the strategy id; otherwise the unchanged
    immediate onSubmitted. Keep the wizard_step_complete_3 telemetry call firing on BOTH arms at
    response time (completion happened; only navigation defers). Style per DESIGN.md; no
    disabled buttons. Named neuter: remove the render branch → the echo case reds while the
    fresh control stays green (observe, restore). Commit as feat(146.2-07).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.test.tsx" --no-file-parallelism</automated>
  </verify>
  <done>Echo sentence renders with explicit continue; fresh flow byte-identical; RED observed against HEAD and under the render-branch neuter</done>
</task>

<task type="auto">
  <name>Task 2: W1 — re-type the provenance constants from HEAD</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx</files>
  <action>
    Replace both stale constants with the sentences that exist in route.ts at (post-plan-01) HEAD:
    the CSV_PERSIST_FAIL sentence (route.ts near :940) and the default CSV_SESSION_REUSED refuse
    literal (near :1003) — HAND-RETYPE them, do not import (the file's :44-45 rule: independent
    oracles). Update the surrounding provenance comments to name the re-verification date. The
    four render assertions stay as they are — they consume the constants. Named neuter (proves
    the assertions consume the constants): alter one word of one re-typed constant → its verbatim
    render assertion reds; restore, green. Do NOT add a route-content grep coupling the files —
    the stopping rule says don't force it; note the option in the SUMMARY instead. Commit as
    test(146.2-07).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.upstream-arm.test.tsx" --no-file-parallelism && npx tsc --noEmit</automated>
  </verify>
  <done>Both constants verbatim-true against HEAD (verified by manual diff against the route sentences, recorded in SUMMARY); one-word neuter observed RED and restored; suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server wire copy → user | the honest-uncertainty sentence must reach the human it was written for, unmodified |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-146.2-15 | Repudiation | echo success surface | medium | mitigate | the two-reads honesty sentence renders before navigation; verbatim wire render (no client editorialising) |
| T-146.2-SC | Tampering | package installs | low | accept | zero package installs in this phase |
</threat_model>

<verification>
- Both CsvSubmitStep suites green under `--no-file-parallelism`; tsc clean.
- Manual UAT note for the phase verifier: submit a CSV, kill the connection post-fold, resubmit per A3's copy — the echo notice renders and continue lands on the strategy list.
</verification>

<success_criteria>
- R6: a success-path consumer for human_message exists; the founder's C1 mitigation is live.
- W1: the verbatim provenance claim is true at HEAD.
- SC#5: echo-render case observed RED against HEAD; render-branch and one-word neuters observed and restored.
</success_criteria>

<output>
Create `.planning/phases/146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil/146.2-07-SUMMARY.md` when done, recording the product call (in-step render with explicit continue) and all neuter observations.
</output>
