---
phase: 146.2-review-146-1-post-merge-close-out-the-echo-path-must-not-sil
verified: 2026-08-20T07:26:48Z
verified_at_commit: d2b1702b3718187539e23d18d6b4a9349f5b3a41
status: passed
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: human_needed
  previous_score: 5/5
  previous_verified_at_commit: 0f7fac91
  supersedes: "The 2026-08-19 artifact is DISCARDED, not amended. Every route.ts line citation in it was invalidated by b853f440 (+257/-114) and 8b05d2b3 (+174 in route.ts); its statement that the clock-safety guard gates only the empty-series FILL is FALSE at HEAD (it gates both arms); its 5/5 score did not distinguish 'regression test passes' from 'observed RED under a named neuter'. Nothing below is copied from it."
  gaps_closed:
    - "The clock-safety guard is a truth table (4 arms) rather than a presence test — re-derived at route.ts:1526-1666"
    - "The FILL discriminator's premise ('every metadata UPDATE this route issues writes a category_id') is now enforced at the boundary, not merely asserted about the client — route.ts:562-570"
    - "The 409 refusal offers a working escape control instead of an uncarryable instruction"
    - "The escape label is pinned across route.ts and CsvSubmitStep.tsx by src/lib/seam-escape-label.invariant.test.ts"
  gaps_remaining: []
  regressions: []
behavior_unverified_items:
  - truth: "SC#5 — each fix carries a regression test that is observed RED under a named neuter"
    test: "For the four gap-closure commits (8b05d2b3, 002542dd, b372d37d, d2b1702b): neuter each new oracle by its named mechanism (e.g. collapse `computeInFlight` to `storedKpis !== null`; delete the `Object.keys(out).length > 0 && out.category_id === undefined` rejection; rename START_NEW_STRATEGY_LABEL on one side only; return `undefined` from onStartNewStrategy) and observe the named test RED, then restore."
    expected: "Each named test fails with the message its author wrote, and passes again on restore."
    why_human: "No SUMMARY or commit body records a RED transcript for the four gap-closure commits — unlike the seven original fix plans, which each carry verbatim transcripts with exit codes. I could not produce the transcripts myself: this session's environment refused every write to a source file (the neuter edit was denied by the sandbox classifier), so the falsification could not be run. Structural discrimination IS established for each new oracle (see 'The anti-vacuity argument I could make' below), but structural discrimination is an argument, not an observation, and the founder rule is explicit that a test which cannot fail is worse than none."
human_verification:
  - test: "MERGE GATE (plan 08 Task 3): merge PR #694, then run the post-merge PROD census — the deployed sweep body carries the word-bounded `< 3` readmit ceiling and ONE real `cron.job_run_details` tick succeeds; fold GUARD 1 raises 22023 on a NULL p_terminal_status and GUARD 2 raises 42501 on a NULL p_user_id, with prosecdef / proconfig / ACLs intact; `suspect_and_visible` is still 0; PROD schema_migrations tip advances to 20260819151000."
    expected: "Every census value matches the TEST rehearsal table in 146.2-08-SUMMARY.md. Any drift is stop-the-line."
    why_human: "Both migrations are applied to TEST only. PROD state cannot exist before the merge, and this verifier has NO database access of any kind (no Supabase MCP, no psql) — I cannot read TEST either. Plan 08 reserves Task 3 as ORCHESTRATOR-ONLY post-merge work; it is the phase's own designed blocking checkpoint, not an oversight."
  - test: "R6 + escape live-flow UAT on a running server: submit a CSV, kill the connection after the fold commits, resubmit the same file. Then separately drive a CSV_SESSION_REUSED refusal and click 'Start a new strategy'."
    expected: "(a) The amber echo notice renders the route's sentence in-step; the footer reads [Back] [Continue] with no disabled 'Submitting…' button; Continue lands on the strategies list with the strategy present. (b) The refusal panel shows a 'Start a new strategy' button below the body and above the <details>; clicking it clears the refusal, shows the 'This will be a new strategy' line, leaves the submit CTA live, and a subsequent submit creates a SECOND strategy from the file still in hand — with no re-upload."
    why_human: "jsdom pins each half separately and both halves pass, but NO test mounts the real CsvSubmitStep inside the real WizardClient. The seam that joins them — `onStartNewStrategy={handleCsvStartNewStrategy}` at WizardClient.tsx:1497 — is verified by grep only. Visual placement, amber tone against DESIGN.md, and the navigation handoff on a live server are not reachable by grep or jsdom."
  - test: "Push HEAD and confirm the CI aggregator (`frontend`, `sql-tests`, Contracts, Migration Policy, SQL Function Snapshot) is green at d2b1702b before merging."
    expected: "All checks green at d2b1702b."
    why_human: "MEASURED: the newest CI run on this branch is headSha 181eed3b (2026-08-19T22:54Z). FIVE commits have landed since — 8b05d2b3, 002542dd, b372d37d, 3ad360a9, d2b1702b — and NONE has been through CI. I ran targeted vitest, tsc --noEmit and all eleven repo invariant gates green locally (evidence below), but the hard constraint on this verification forbids the full local suite (it reds ~274 live-DB tests by design on this machine), so full-suite green at HEAD is the one thing only CI can establish. The d2b1702b commit body reports a CI-parity full-suite run by its author; that is a claim I did not re-derive."
---

# Phase 146.2 Verification Report

**Phase Goal:** A CSV finalize that recovers via the resubmit path A3's own copy instructs lands the user's classification instead of silently defaulting it, and no raw exchange credential reaches Sentry.

**Verified at:** `d2b1702b` (2026-08-20T07:26:48Z), branch `docs/v1-19-verification-sweep-146.2`
**Status:** `passed` — ALL human items closed 2026-08-20 (see "Human-item closure record" at the end of this file): 5/5 ROADMAP success criteria verified, all 4 gap-closure truths verified, SC#5 falsification OBSERVED at HEAD.

> ⚠️ **HEAD moved during this verification.** I opened at `3ad360a9`; a concurrent
> agent committed `d2b1702b` ("carry the category_id requirement into two adjacent
> fixtures") at 09:20:45 local while I was reading. Every finding below was
> re-derived at `d2b1702b` and the working tree was clean at that commit. The
> shared-git-index hazard is real on this repo and it fired here.

**Stance.** This artifact replaces a 2026-08-19 report that over-claimed. Nothing was
copied forward. Every line citation below was produced by a grep or a `sed` range run
in THIS session against the file at `d2b1702b`. Where I could not re-derive a claim, it
is in "What I could NOT verify" and it is not scored.

---

## Goal Achievement — the 5 ROADMAP Success Criteria

| # | Success criterion | Verdict | Evidence re-derived at `d2b1702b` |
|---|---|---|---|
| 1 | The 23505 echo applies submitted `category_id`/`asset_class` when the committed row has none, and REFUSES 409 `CSV_SESSION_REUSED` on conflict | **✓ VERIFIED** | Tri-state at `route.ts:1441-1697`: FILL arm `existingRow.category_id === null` (:1444), REFUSE arm `typeof existingRow.category_id === "string"` with two mismatch refusals (:1675, :1684), no-op arm for `undefined` (:1692). `fillClassification` returned on the outcome (:1751) and widens the metadata gate at the call site (`outcome.fresh \|\| outcome.fillClassification`, :2594). Behaviourally exercised: **112 tests pass** across `csv-finalize-cross-submission-merge`, `csv-finalize-rpc`, `csv-finalize-c14-regression` and `seam-escape-label.invariant` (run this session). |
| 2 | `passphrase` scrubbed at every Sentry capture site on verify-strategy; docblock enumeration matches the array | **✓ VERIFIED** | Full-file grep census of `src/app/api/verify-strategy/route.ts`: `bodyCredentials` includes `passphrase` (:235-238); `perRequestSecrets = Object.values(bodyCredentials)` (:328); **all four** `captureToSentry` sites (:394, :457, :502, :523) pass `secrets: perRequestSecrets`; **all three** `scrubSeamError` sites (:464, :508, :529) pass it as the second argument. Zero unscrubbed sites in the file. Wiring confirmed downstream: `sentry-capture.ts:207-210` consumes `options.secrets` into `scrubCaptureInput` / `scrubRecord`. Docblock at :296-328 names all three credentials. **40 tests pass** (`route.seam.test.ts` + `route.test.ts`), including the RAW-passphrase dispatch oracle at seam-test :535. |
| 3 | The B4 readmit predicate cannot drive an unbounded reaped-orphan retry loop | **✓ VERIFIED (code + gates); PROD apply is post-merge → human item 1** | `20260819150000_reconcile_sweep_readmit_attempt_ceiling.sql:393-396` — `count(*) … last_error LIKE 'orphaned_running_reaped:%' … ) < 3` AND'ed into the batch CTE; word-bounded self-verify at :499-508 (`'count\(\*\)[^;]*orphaned_running_reaped[^;]*<[[:space:]]*\m3\M'`). Matched-pair behavioural arms C5 (at-ceiling → 0 readmits) and C5b (below-ceiling → exactly 1) at `supabase/tests/test_reconcile_dropped_enqueue_sweep.sql:850-870`, with an explicit written argument for why neither passes alone (:514-521). ⭐ **The stale artifact's CI citation is repaired, not repeated:** `sql-tests` last ran at `181eed3b`, not at HEAD — but `git diff --stat 181eed3b..HEAD` touches 14 files, **none** under `supabase/`, so that green run is still evidence for the migration and gate content at `d2b1702b`. |
| 4 | RT-3's re-mint persists the NEW session id — no spent-id-with-cleared-burn window | **✓ VERIFIED** | `WizardClient.tsx:599` mints first (`const nextWizardSessionId = newWizardSessionId()`); ONE `saveWizardState` at :608-614 carries `wizardSessionId: nextWizardSessionId` **and** an explicit `failedCsvSubmitSig: null`; the setter at :615 is fed from the same local, not from the closure. Pair-regression passes in `WizardClient.csv-burn-persistence.test.tsx` (part of the 34 passing wizard tests this session). |
| 5 | Each fix carries a regression test observed RED under a named neuter | **⚠️ PRESENT_BEHAVIOR_UNVERIFIED** | Every named regression test **exists and passes** at HEAD (261 tests across 10 files run this session, 0 failures). The seven original fix plans each record a verbatim RED transcript with an exit code in their SUMMARY. **The four gap-closure commits record none**, and this session could not produce them: the sandbox classifier denied every source-file write, so no neuter could be applied. See `behavior_unverified_items` and "The anti-vacuity argument I could make" below. Not counted toward the score. |

**Score: 4/5 verified, 1 present-behaviour-unverified.** No truth is scored on symbol
presence: SC#1/#2/#4 were exercised by named test runs in this session; SC#3 rests on the
migration body plus a CI job whose applicability to HEAD I proved by diffing the paths.

---

## The gap closure just landed — all four truths VERIFIED

### G1 — the clock-safety guard is a TRUTH TABLE, and case 4 is still reachable

The guard now sits **outside** `if (args.rows.length === 0)`, at `route.ts:1483-1666`,
inside the `category_id === null` FILL arm — so it gates **both** arms. (The stale
artifact's claim that it gates only the empty-series FILL is FALSE at HEAD. The proof
that it gates both is `route.ts:1597-1600`: the refuse *reason* is a ternary on
`args.rows.length === 0`, i.e. both arms reach the same refusal and only the operator
string differs.)

The projection was widened to eight columns — the seven `CLOCK_SAFETY_KPI_COLUMNS`
plus `computation_status` (`:1503-1506`). I checked the mirror claim rather than
trusting it: `CLOCK_SAFETY_KPI_COLUMNS` (`route.ts:1039-1047`) is member-for-member
identical to `PERCENTILE_ANALYTICS_COLUMNS` (`queries.ts:126-127`).

| Case | Condition | Code | Behaviour | Behavioural test |
|---|---|---|---|---|
| 1 | no row at all | `storedKpis === null` falls through both guards | **FILL** | pre-existing fixtures (`row: null`) |
| 2 | any KPI column non-null | `storedKpiPresent` at `:1584-1586` | **REFUSE 409** | `analyticsRowKpiBearing` fixtures :1400-1414, :1847-1867 |
| 3 | all KPIs null **AND** `computation_status` `'computing'` or unmeasured | `computeInFlight` at `:1623-1626` | **REFUSE 409** | `cross-submission-merge.test.ts:2047` (series arm) **and** `:2144` (empty-series arm) |
| 4 | all KPIs null **AND** any other measured status | falls through to `:1650` | **FILL** | `:2096` (`'failed'`) and `:2128` (`'pending'`) |

⛔ **Case 4 — the load-bearing arm — is REACHABLE, confirmed two independent ways.**
(a) Statically: `computeInFlight` is true only when `computation_status` is a non-string
or exactly `'computing'`, so `'failed'` and `'pending'` fall through. (b) By population:
`writeFailedStrategyAnalyticsPlaceholder` — the writer that CREATES the primary
R1-recovery row — upserts `computation_status: 'failed'`, `computation_warned`,
`computing_started_at`, `computation_error` and `data_quality_flags` and **no KPI
column** (`route.ts:1862-1873`), which is exactly case 4. The test at `:2096` drives it
end-to-end and asserts a 200, one UPDATE carrying `{asset_class:'crypto', category_id}`,
and an `enqueue_compute_job` — the clock lands AND the recompute rides it.

The `⛔ WHY CASE 4 MUST STAY A FILL` comment at `:1553-1559` names the failure mode of a
future "simplification" to `refuse whenever a row exists`. That comment is load-bearing
and correct.

### G2 — the metadata invariant holds, so the FILL discriminator's proof is sound

The invariant is *every metadata UPDATE this route issues writes a `category_id`*
(`route.ts:529-570`). I verified it structurally rather than accepting the comment:

1. **There is exactly ONE `.update()` on `strategies` in this route** — `route.ts:2062-2063`.
   Census: `grep -n '\.update(\|\.upsert(' route.ts` returns two hits, `:1862` (a
   `strategy_analytics` upsert) and `:2062`. `grep -n '\.rpc('` returns two calls,
   `finalize_csv_strategy_with_returns` (:759) and `enqueue_compute_job` (:1936);
   neither UPDATEs `strategies`.
2. That payload comes from `buildMetadataUpdatePayload(parsed.payload)` (`:2054`), and
   an empty payload short-circuits to `kind: "noop"` with **no UPDATE issued** (`:2055`).
3. `buildMetadataUpdatePayload` (`:583-632`) copies eleven fields, **each** gated on
   `metadata.X !== undefined` — it derives nothing. So a non-empty payload implies a
   non-empty parsed `out`.
4. `parseCsvMetadata` rejects a non-empty `out` lacking `category_id` at `:562-570` (400,
   `field: "metadata.category_id"`).

∴ non-empty UPDATE ⇒ `category_id` present. The committed SQL NULL is therefore real
proof the UPDATE never ran. The fold corroborates the other half: the strategies INSERT
in `20260819120000_csv_finalize_atomic_fold.sql:295-305` writes
`user_id, name, status, source, strategy_types, subtypes, markets, supported_exchanges,
wizard_session_id` — **no `category_id`, no `asset_class`**.

The oracle at `csv-finalize-rpc.test.ts:188-233` asserts this over the **composition**
`parseCsvMetadata → buildMetadataUpdatePayload` across eight blobs, with an explicit
anti-vacuity floor (`expect(updatesChecked).toBe(2)`) and a counterweight test that the
metadata-less path stays legal. This is an economic-invariant oracle, not a
self-referential one.

⚠️ **Measured blast radius, and it is honestly recorded.** G2 is a wire-contract change:
a blob with other fields but no `category_id` was a 200 and is now a 400. It reddened 9
tests in `csv-validate-route.test.ts` — caught by a CI-parity full-suite run, NOT by the
fixer's targeted runs — and `d2b1702b` repairs them. I read that diff: it is a fixture
update, and the projection test is **strengthened** (`toEqual` now demands two known
fields survive while all four hostile ones drop). Not a weakening. The wizard is
unaffected: `CsvSubmitStep.tsx:442` always sends the `category_id` key
(`category_id: metadata.categoryId`), and an explicit null was already 400-refused at
`route.ts:409`. `CsvSubmitStep` is the only non-test caller of `/api/strategies/csv-finalize`
in `src/`.

### G3 — the wizard refusal renders a working escape that mints and preserves the upload

| Link | Site | Status |
|---|---|---|
| Refusal envelope → escape button | `CsvSubmitStep.tsx:718-728`, gated `envelope.code === CSV_SESSION_REUSED` | ✓ WIRED |
| Button → step handler | `handleStartNewStrategy` at `:385-392` — calls `onStartNewStrategy()`, then `showCsvEnvelope(null)` (the clear shape, which does NOT re-burn), then `setSessionRestarted(true)` | ✓ WIRED |
| Step → WizardClient | `onStartNewStrategy={handleCsvStartNewStrategy}` at `WizardClient.tsx:1497` | ✓ WIRED (grep-verified; no test crosses this seam — human item 2) |
| Handler → fresh id + cleared burn | `WizardClient.tsx:680-704` — mints (`:681`), nulls the ref (`:682`), ONE `saveWizardState` carrying the new id **and** explicit `failedCsvSubmitSig: null` (`:686-693`), setter from the same local (`:694`) | ✓ WIRED |
| Envelope shell renders the action | `CsvValidationEnvelope.tsx:116, :187-189` | ✓ WIRED |

**All refusals that name the control render it.** `refuse()` emits
`code: "CSV_SESSION_REUSED"` unconditionally (`route.ts:1274`) for all eight call sites
(:1319, :1330, :1387, :1403, :1597, :1628, :1675, :1684), and the button is gated on
exactly that code. So the instruction is carryable on every refusal that gives it.

**Series preservation.** The handler body (`:680-704`) touches only the session id, the
burn ref and telemetry. `setCsvDailyReturnsSeries` has exactly ONE call site in the whole
file — `WizardClient.tsx:1328`, in the upload step — so nothing on the escape path can
clear the series. ⚠️ This is a **single-writer code proof, not a test oracle**: the
WizardClient test asserts the submit step is still mounted, but the mount guard is
`step === "csv_submit" && csvFmt && csvPreview && csvMetadataDraft` (`:1479`) and does
NOT include the series, so that assertion cannot see series loss. Recorded, not scored
against.

Behavioural coverage (34 wizard tests pass): the mint/persist/no-reset assertions at
`WizardClient.csv-burn-persistence.test.tsx:506-553`; the delegate/retire/announce/CTA-live
assertions at `CsvSubmitStep.test.tsx:546-573`; the does-not-re-burn test at `:575-604`;
and a **negative control** at `:606-628` proving a non-refusal error gets NO escape.

### G4 — the escape label is pinned as a two-file contract

`src/lib/seam-escape-label.invariant.test.ts` (99 lines, 3 tests, passing) reads BOTH
real sources off disk — never a fixture — extracts
`const START_NEW_STRATEGY_LABEL = "…"` from each, asserts both are present (so two
absences cannot read as agreement), asserts equality, and asserts route.ts interpolates
the constant more than once (so the contract cannot go decorative). Both sides declare
`"Start a new strategy"` (`route.ts:1075`, `CsvSubmitStep.tsx:221`).

⚠️ **The contract is real but NOT total** — see WARNING W-1 below.

---

## Anti-Patterns and Warnings

| # | Severity | File / line | Finding |
|---|---|---|---|
| W-1 | ⚠️ WARNING | `route.ts:1277` | **The two-file contract covers 2 of the 3 refusal sentences that name the control.** `refuse()`'s DEFAULT `human_message` hardcodes the phrase in prose — "…Start a new strategy to upload a different file." — instead of interpolating `START_NEW_STRATEGY_LABEL`. It is the sentence shown for the name-mismatch, series-mismatch and date-mismatch refusals (:1319, :1330, :1387, :1403), all user-reachable. The copy is CORRECT today, so nothing is broken; but rename the constant and the invariant test stays green while this sentence silently drifts — the exact failure mode the test was written to stop. One-line fix: interpolate the constant here too. |
| W-2 | ⚠️ WARNING | `146.2-01-SUMMARY.md:15, :76, :128, :167` | **The plan-01 SUMMARY is now STALE and describes the shipped guard incorrectly.** It says the clock-safety read gates "an empty-series FILL" and that "Absent row or all-seven-null → FILL". At HEAD the guard gates BOTH arms and all-seven-null is TWO outcomes (case 3 REFUSE / case 4 FILL). A reader trusting this SUMMARY would conclude case 3 is unguarded. Prose-only, non-blocking under the stopping rule. |
| W-3 | ⚠️ WARNING | `TODOS.md`, entry IN-03 | **Partially stale on landing.** IN-03 was measured at `181eed3b` and states the classification-conflict REFUSE "has no in-wizard escape except a rename". `002542dd` landed the escape 30 seconds later. The fingerprint half of IN-03 is still accurate; the "no escape" half is not. |
| W-4 | ℹ️ INFO | `route.ts:1483` vs `:1650` | **The guard runs before the fill is known to be requested**, so a metadata-less resubmit onto a never-classified KPI-bearing row takes a 409 whose copy talks about "changing its classification" — which was not attempted. Already recorded as a deliberate deferral in `146.2-01-SUMMARY.md:488-497` with a stated reason (narrowing the probe would weaken a shipped money-fence). Copy-only, no write, no economic consequence. |

**Debt-marker gate: CLEAN.** `grep -nE '\bTBD\b|\bFIXME\b|\bXXX\b'` over all five
non-test source files touched by the gap closure returns zero hits.

---

## Behavioural Spot-Checks (run this session)

| Check | Command | Result | Status |
|---|---|---|---|
| csv-finalize suites + escape-label invariant | `npx vitest run seam-escape-label.invariant + csv-finalize-cross-submission-merge + csv-finalize-rpc + csv-finalize-c14-regression` | 4 files, **112 passed** | ✓ PASS |
| wizard escape + burn persistence + upstream arm | `npx vitest run CsvSubmitStep.test + CsvSubmitStep.upstream-arm.test + WizardClient.csv-burn-persistence.test` | 3 files, **34 passed** | ✓ PASS |
| verify-strategy passphrase scrub (SC#2) | `npx vitest run verify-strategy/route.seam.test + route.test` | 2 files, **40 passed** | ✓ PASS |
| G2 blast-radius file | `npx vitest run csv-validate-route.test.ts` | **56 passed** | ✓ PASS |
| SEAMPROSE-01 + seam log coverage (CI gates) | `npx vitest run seam-citations.invariant + seam-log-coverage` | **123 passed** | ✓ PASS — the deferred-items.md RED is closed at HEAD |
| 7 remaining seam/wizard invariants + audit-coverage law | `npx vitest run seam-budgets/ratelimit/transport/user-jwt/venue/wire .invariant + wizardErrors.invariant + audit-coverage` | 8 files, **217 passed, 1 skipped** | ✓ PASS |
| Typecheck | `npx tsc --noEmit -p tsconfig.json` | no output | ✓ PASS |
| Full local vitest suite | — | **NOT RUN** | ? SKIP — forbidden by this verification's hard constraint (`.env.test.local` un-skips ~193 live-DB tests CI withholds; reporting those reds would be a false finding) |
| SQL gate execution | — | **NOT RUN** | ? SKIP — no database access of any kind from this session |

**Total: 261 tests executed across 20 files, 0 failures, 1 skipped.**

## Probe Execution

`find scripts -path '*/tests/probe-*.sh'` — no probe scripts are declared by any 146.2
plan and none exist for this surface. Step 7c: **N/A**.

---

## Requirements Coverage

| Req | Source plan | Status | Evidence |
|---|---|---|---|
| R1 | 146.2-01 | ✓ SATISFIED | SC#1 above; tri-state + truth table + G2 invariant |
| R2 | 146.2-02 | ✓ SATISFIED | SC#2 above; 7/7 scrub sites, 40 tests |
| R3 | 146.2-04, -08 | ✓ SATISFIED (code); PROD apply → human item 1 | SC#3 above |
| R4 | 146.2-03 | ✓ SATISFIED | SC#4 above |
| R5, W2, W3 | 146.2-05, -08 | ✓ SATISFIED (code); PROD apply → human item 1 | GUARD 1 `IS NULL OR` migration `20260819151000` present; SQL Function Snapshot drift gate green at `181eed3b` with zero `supabase/` changes since |
| R6, W1 | 146.2-07 | ✓ SATISFIED (jsdom); live flow → human item 2 | Echo notice render + hold + Continue; W1 fixture provenance recorded as IN-05 |
| R7 | 146.2-06 | ✓ SATISFIED | Date lower-bound mirror + `strategy.csv_finalize` audit event; `audit-coverage.test.ts` green |

No orphaned requirements: every ID mapped to Phase 146.2 appears in a plan's
`requirements` frontmatter.

---

## What I could NOT verify

Stated plainly, because the previous pass's failure was over-claiming.

1. **Any live database state — TEST or PROD.** No Supabase MCP, no psql, no network to
   either. Every claim in `146.2-08-SUMMARY.md` about the TEST rehearsal (applied
   migrations, measured body properties, executed gate files, 14 neuters observed RED)
   is **unverified by me**. I verified only that the migration SQL and the gate SQL at
   HEAD contain what they claim to contain.
2. **CI green at HEAD.** Newest run is `181eed3b`; five commits are un-CI'd. I proved
   the `supabase/` tree is unchanged since that run, which carries the SQL gates
   forward, but not the frontend suite.
3. **The full local vitest suite.** Forbidden by constraint. The d2b1702b author's
   claim of a clean CI-parity full-suite run is not re-derived here.
4. **RED-under-neuter for the four gap-closure commits.** Every attempt to write to a
   source file in this session was denied by the sandbox classifier, including the one
   surgical neuter I designed (collapsing `computeInFlight` to `storedKpis !== null`
   to redden the two case-4 tests). I did not work around it.
5. **RED-under-neuter for the seven original fix plans.** Their SUMMARYs carry verbatim
   transcripts with exit codes. I read them; I did not reproduce them.

### The anti-vacuity argument I could make (an argument, not an observation)

Recorded so a human can decide whether it substitutes for item 4:

- **Case 3 vs case 4** — `analyticsRowAllNull("computing")` → 409 and
  `analyticsRowAllNull("failed")` → 200 are the SAME fixture differing in ONE field, and
  the expected statuses differ. Any degradation of `computeInFlight` toward
  `storedKpis !== null` flips case 4 to 409 and reddens two tests. The suite therefore
  provably discriminates on `computation_status`.
- **G2** — the loop carries an explicit `expect(updatesChecked).toBe(2)` floor, so a
  boundary that refused everything fails rather than passing vacuously; and a
  counterweight test pins that the metadata-less path stays a 200.
- **G3** — the escape suite has a negative control (a non-refusal code gets NO button)
  and a does-not-re-burn test, so "render a button always" and "clear the envelope by
  failing the submit" both redden.
- **G4** — the invariant test reads the two real sources off disk and asserts presence
  on each side before comparing, so a symmetric rename cannot pass as agreement.

What this argument cannot establish is that each oracle fails **for the reason its
author wrote in the failure message**. That is what observing the RED is for.

---

## Gaps Summary

**No blocking gaps.** The three findings that could have been gaps are all either
recorded deferrals (W-4, IN-01…IN-05, the Part 3e vacuous count block) or prose-only
staleness (W-2, W-3), and under the project's stopping rule — block only on user-facing
or data-integrity defects — none qualifies. W-1 is the closest call: it is user-facing
copy, but the shipped string is correct today and the risk is future drift, so it is a
WARNING with a one-line fix rather than a blocker.

The phase's own hardest claim — that the echo path lands a dropped classification
instead of silently defaulting it, without ever moving the annualization clock out from
under stored metrics — is **achieved and behaviourally proven** at `d2b1702b`, including
the arm that makes the repair reachable at all.

What remains is what the phase always said would remain: a post-merge PROD census that
cannot exist pre-merge, a live-flow UAT that jsdom cannot reach, a CI run at HEAD, and
one honest hole in the RED-first discipline on the four commits that closed the review.

---

_Verified: 2026-08-20T07:26:48Z at `d2b1702b`_
_Verifier: Claude (gsd-verifier) — supersedes the 2026-08-19 artifact produced at `0f7fac91`_

---

## Human-item closure record (2026-08-20, orchestrator session)

All four human items are CLOSED. Status flipped `human_needed` → `passed`.

**Where the evidence was taken.** Items 1–3 closed against PR #694 (merged
`0ba62be9`, v0.68.0.0). Item 4's falsification ran at main HEAD `00e73aa5`
(v0.68.1.0, post-#695). The oracle files differ from the verified commit
`d2b1702b` only by the W-1 fix squashed into `0ba62be9` — the default refusal
sentence now interpolates `START_NEW_STRATEGY_LABEL` and the two-file invariant
gained a no-other-literal assertion — i.e. the contract got STRONGER between
verification and falsification, and the falsification exercised the code that
actually shipped.

### Item 1 — post-merge PROD census: CLOSED

Run this session after #694 merged, values matched the TEST rehearsal table in
`146.2-08-SUMMARY.md` 7/7. Highlight: readmit-ceiling cron (PROD jobid 36)
first real `cron.job_run_details` tick runid 6176 `succeeded` 2026-08-20
08:35:00 UTC in 219ms; `suspect_and_visible` 0; PROD `schema_migrations` tip
`20260819151000`.

### Item 2 — R6 + escape live-flow UAT: CLOSED

`146.2-UAT.md` (this directory, merged to main in #695): both arms PASS on a
live localhost server against TEST — killed-connection echo renders in-step
with `[Back] [Continue]` and no duplicate row; the mutated-resubmit 409 renders
the escape control, one click re-mints, and a second strategy is created with
no re-upload.

### Item 3 — CI green at HEAD before merge: CLOSED

PR #694 merged with its full check suite green at the PR head (the merge could
not have completed otherwise under the land flow used); both 146.2 migrations
auto-applied to PROD on merge and the item-1 census verified their effects.

### Item 4 — SC#5 observed-RED falsification: CLOSED BY OBSERVATION

All four named neuters from `behavior_unverified_items` were applied at
`00e73aa5`, each observed RED with its author's message, each restored
(`git restore` from HEAD; `git status --porcelain` empty after every cycle).
Baseline before any neuter: the four oracle test files 118/118 green.
Final combined restore run: 164/164 green across the finalize suites.

| # | Gap-closure commit | Neuter applied | Observed RED (exit 1) | Restore |
|---|---|---|---|---|
| N1 | `8b05d2b3` | collapse `computeInFlight` (route.ts:1630) to `storedKpis !== null` | 2 failed / 49 passed in `csv-finalize-cross-submission-merge.test.ts` — both G1 CASE-4 arms (`'failed'` and `'pending'` all-null rows) | 51/51 |
| N2 | `8b05d2b3` boundary, pinned again by `d2b1702b` fixtures | delete the `Object.keys(out).length > 0 && out.category_id === undefined` rejection (route.ts:562) | 4 failed / 104 passed — 3 G2-invariant tests in `csv-finalize-rpc.test.ts` + the G2 boundary test in `csv-finalize-cross-submission-merge.test.ts` | 164/164 |
| N3 | `b372d37d` | rename `START_NEW_STRATEGY_LABEL`'s value in `CsvSubmitStep.tsx` only | 1 failed / 4 passed in `seam-escape-label.invariant.test.ts` — "the server names the control the wizard actually renders" | 5/5 |
| N4 | `002542dd` | `return undefined;` as first statement of `handleCsvStartNewStrategy` (WizardClient.tsx:680) | 1 failed / 5 passed in `WizardClient.csv-burn-persistence.test.tsx` — the B1 mint test | 6/6 |

Verbatim RED assertions (first line each), proving the failures are the
authored oracles and not collateral:

- **N1:** `AssertionError: the guard refused a row with NO stored KPI at all — the clock-safety rule became 'never repair', and a crypto track record keeps the sqrt(252) column default with no editor anywhere that could fix it: expected 409 to be 200`
- **N2:** `AssertionError: a metadata blob with no category_id ran a real UPDATE and left category_id NULL — the committed NULL no longer proves the UPDATE never ran, and the FILL arm's whole discriminator is unsound: expected 200 to be 400`
- **N3:** `AssertionError: The refusal copy in csv-finalize/route.ts tells the user to use "Start a new strategy", but the wizard renders a control labelled "Start a brand new strategy". […] expected 'Start a brand new strategy' to be 'Start a new strategy'`
- **N4:** `AssertionError: the escape did not mint: the next submit replays the SAME spent id and takes the same 409 — the refusal is permanent: expected 1 to be greater than 1`
