# Outside Voices — Phase 4 (Feedback Loop)

**Voice A (Claude subagent, fresh context):** verdict=revise — 3 BLOCKERs, 4 WARNINGs, 2 INFOs: real SQL predicate bug in migration 063 enqueue loop + golden-snapshot `{}` placeholder ambiguity + unguarded inline overhead defeating Phase 3's skip-check + various test/doc tightening.
**Voice B (Grok grok-4-1-fast-reasoning):** verdict=revise — 2 BLOCKERs, 2 WARNINGs, 1 INFO: flagged audit-event scope + human-blocking push, plus line-number fragility, missing end-to-end propagation test, and migration CTE copy risk.

## Consensus findings (auto-fold into replan)

| # | Priority | Area | Title | Severity (A/B) | Confidence (A/B) | Recommendation |
|---|----------|------|-------|----------------|------------------|----------------|
| C1 | P1 | verification | Migration 063 verification is semantically weak — static text assertion passes against broken SQL; copied CTE from migration 060 not pinned | WARN / INFO | HIGH / HIGH | Add an integration-style test that seeds 2 bridge_outcomes rows (one with delta transition, one without) and asserts compute_jobs has exactly 1 rescore_allocator row for the transitioned allocator. Pin the copied CTE with a read_first requirement that the full current body of migration 060 has been re-read + include a self-verify DO assertion on the extract_delta signature. |

## Divergent findings (require user decision — auto-resolved under auto-mode per policy below)

| # | Priority | Area | Title | Voice A says | Voice B says | Resolution |
|---|----------|------|-------|--------------|--------------|------------|
| D1 | P0 | architecture | Migration 063 predicate `deltas_computed_at = v_started AND kind='allocated'` fires on every row the UPDATE touched, not just NULL→non-NULL transitions. Every daily cron run re-enqueues every allocator with any allocated outcome, defeating CONTEXT.md D-11 "wait-for-delta is the natural gate". | BLOCKER — restructure function to capture updated rowset in CTE array via `RETURNING`, iterate DISTINCT allocator_ids from that array. Update test_migration_063_body_has_enqueue and add a new transition-vs-unchanged seed test. | (not flagged) | **APPLY A** — genuine SQL correctness bug. |
| D2 | P0 | verification | Golden snapshot's only asserted value is `{}` — identical to Wave 0 placeholder. REGENERATE_GOLDEN=1 silently accepts whatever code produces; regen + equality + acceptance text all pass against a wrong value. | BLOCKER — add 2 additional golden fixtures (ceiling `{"W_PORTFOLIO_FIT": 1.5}` and floor `{"W_PREFERENCE_FIT": 0.5}`) covering non-empty dicts; fail regen if it writes `{}` when scenario should have produced an adjustment. | (not flagged) | **APPLY A** — genuine Wave 1 verification blind spot. |
| D3 | P0 | architecture | Inline `compute_adjusted_weights` runs inside `_scoring_semaphore` (3-max), adds ~5 Supabase round-trips per scoring run unconditionally, defeats `_should_skip_allocator` optimization; no benchmark gate. | BLOCKER — add a trivial first-touch skip (`SELECT 1 FROM bridge_outcomes WHERE allocator_id=$1 LIMIT 1`) short-circuit at top of compute_adjusted_weights; add an acceptance criterion that p95 `_score_one_allocator` latency_ms stays within +10% of pre-Phase-4 baseline. | (not flagged) | **APPLY A** — preserves hot-path latency. |
| D4 | P1 | risk | No rollback plan / dry-run step for migration 063 if pg_cron function-body replacement self-verify fails. | BLOCKER — add rollback SQL snippet restoring migration 060's body + pre-apply dry-run via `supabase db query --linked --dry-run`. | (not flagged) | **APPLY A** — low-risk defensive addition. |
| D5 | P1 | verification | `REJECTION_REASON_TO_DIMENSION` dict has 3 keys but CONTEXT.md D-06 enumerates 5 entries; test wording "equals D-06 exactly" is ambiguous. | WARNING — add explicit comment at constant site naming the two deliberate omissions (`already_owned` filtered at SQL, `other` falls through to score-dominant); rewrite test to assert (a) 3 direct-mapped keys present, (b) `already_owned` NOT a key, (c) `other` NOT a key. | (not flagged) | **APPLY A** — prevents attribution regression. |
| D6 | P1 | risk | Lazy-import acceptance check uses `grep -c "from services.feedback_engine import compute_adjusted_weights" analytics-service/routers/match.py == 1` — passes against a top-level import that would violate CONVENTIONS.md lazy-import pattern. | WARNING — tighten grep to leading-whitespace pattern OR add a test that imports `routers.match` without pre-importing `services.feedback_engine` and asserts no ImportError. | (not flagged) | **APPLY A** — enforces body-placement. |
| D7 | P2 | scope | Audit taxonomy addition to `src/lib/audit.ts` not mirrored in `docs/architecture/adr-0023-audit-event-taxonomy.md` — violates in-file "keep this union in sync" contract at src/lib/audit.ts:~83. | WARNING — add ADR-0023 to files_modified; append `feedback.overrides_updated` + `allocator_preference_feedback` rows to registered tables; add grep acceptance criterion. | (not flagged) | **APPLY A** — closes documentation-drift loop. |
| D8 | P1 | verification | No test verifies feedback overrides actually propagate through `match_engine.py` multiplicative clamp + renormalize into `effective_preferences` snapshot on `match_batches`. Phase 3 engine consumption is trusted but not exercised end-to-end in Phase 4. | (not flagged) | WARNING — add `test_full_scoring_propagation` that seeds 5+ positive W_PORTFOLIO_FIT outcomes, calls `_score_one_allocator`, asserts `effective_preferences['W_PORTFOLIO_FIT']` reflects the renormalized post-override value. | **APPLY B** — closes the FEEDBACK-06 story. |
| D9 | P1 | clarity | Line-number references in Task 1 ("line 259", "lines 251-275") will drift once the surrounding file is edited; CONTEXT.md + research use the contextual anchor pattern. | (not flagged) | WARNING — replace literal line numbers with anchor patterns (`immediately after ctx = await asyncio.to_thread(_load_allocator_context, allocator_id)`). | **APPLY B** — anchor-based instructions survive upstream edits. |
| D10 | P3 | clarity | CONTEXT.md canonical_refs names migration as `060_bridge_outcome_deltas.sql` but the file is `060_bridge_outcome_cron.sql` — documentation drift. | INFO — correct the filename in CONTEXT.md (and VALIDATION.md if present). | (not flagged) | **APPLY A** — trivial rename. |
| D11 | P3 | verification | ROADMAP SC1 wording "toward 1.5×" conflicts with D-13 literal step (exactly 1.5×) — drift-prone if future reader adds smoothing. | INFO — annotate plan `must_haves.truths` with "toward 1.5× = exactly 1.5× per D-13 literal step", or reword ROADMAP SC1. | (not flagged) | **APPLY A** — 2-word note, prevents regression. |
| D12 | — | scope | Audit event emission + src/lib/audit.ts changes exceed ROADMAP 04-01 spec (which lists only the 9 unit test categories). | (not flagged) | BLOCKER — remove src/lib/audit.ts; delete audit calls; drop test_audit_event_emitted. | **IGNORE** — user explicitly approved audit event emission via AskUserQuestion earlier in this planning session ("Feedback overrides write — emit an audit event on every write?" → "Yes — always emit"). Finding predates user decision. |
| D13 | — | sequencing | Task 3 `checkpoint:human-verify gate="blocking"` `supabase db push` breaks autonomous execution. | (not flagged) | BLOCKER — change to INFO non-blocking, move push instructions to SUMMARY.md. | **IGNORE** — `[BLOCKING]` schema-push is the standard GSD workflow gate (triggered automatically by schema-push-detection in step 5.7); Phase 1 plan 01-01 and Phase 3 plan 03-01 use the same pattern. |

**Auto-mode policy:** Consensus finding auto-folds. Divergent BLOCKER + WARNING findings from Voice A auto-fold because each cites a specific correctness issue with a concrete code-level fix and high confidence; user previously authorized autonomous execution in this planning session. Voice B's two BLOCKERs that conflict with user-locked decisions are explicitly ignored with justification above (CLAUDE.md rule: "After ANY user correction or feedback: immediately update and never repeat" — user already decided audit=YES, and workflow gates are user-locked via step 5.7).
