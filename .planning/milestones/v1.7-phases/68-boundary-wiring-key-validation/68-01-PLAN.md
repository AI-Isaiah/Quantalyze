---
phase: 68-boundary-wiring-key-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.test.ts
  - src/lib/constants.ts
  - src/components/landing/VerificationForm.tsx
  - src/app/api/cron/sync-funding/route.ts
  - src/app/api/cron/sync-funding/route.test.ts
  - src/app/api/cron/reconcile-strategies/route.ts
  - analytics-service/models/schemas.py
  - analytics-service/routers/debug_key_flow.py
  - analytics-service/services/ingestion/adapter.py
  - analytics-service/tests/test_portfolio_router_audit_2026_05_07.py
  - src/lib/strategy-sources.ts
  - src/components/admin/AdminTabs.tsx
  - supabase/migrations/NEW_deribit_exchange_boundary_checks.sql
autonomous: true
requirements: [DRB-02]
must_haves:
  truths:
    - "A deribit key-save request clears the TS allowlist (isSupportedExchange('deribit') === true), the pydantic Literals (no 422), and the SQL CHECKs (no 23514)"
    - "The marketing page still claims 3 exchanges and the public VerificationForm dropdown still offers only binance/okx/bybit (OQ4 gate)"
    - "The sync-funding and reconcile-strategies crons never enqueue a deribit key (Pitfall 2)"
    - "_FUNDING_BUCKET_HOURS and funding_fees_exchange_check remain 3-exchange (BYB-02 exclusion)"
  artifacts:
    - path: "src/lib/closed-sets.ts"
      provides: "4-value SUPPORTED_EXCHANGES + deribit display label + decoupled 3-value UI_EXCHANGE_CODES and FUNDING_EXCHANGES consts"
      contains: "deribit"
    - path: "supabase/migrations/"
      provides: "ONE migration re-basing api_keys_exchange_check, compute_jobs_exchange_check, strategies_source_check, strategy_verifications_source_check with 'deribit', each with a self-verify DO-block"
    - path: "analytics-service/models/schemas.py"
      provides: "VerifyRequest.exchange Literal admits deribit"
      contains: "deribit"
    - path: "src/lib/strategy-sources.ts"
      provides: "STRATEGY_SOURCES includes deribit, paired with the migration in the same plan"
      contains: "deribit"
  key_links:
    - from: "src/app/api/cron/sync-funding/route.ts"
      to: "src/lib/closed-sets.ts"
      via: "PERP_EXCHANGES built from FUNDING_EXCHANGES (not SUPPORTED_EXCHANGES)"
      pattern: "new Set\\(FUNDING_EXCHANGES\\)"
    - from: "src/components/landing/VerificationForm.tsx"
      to: "src/lib/closed-sets.ts"
      via: "dropdown options built from UI_EXCHANGE_CODES (not SUPPORTED_EXCHANGES)"
      pattern: "UI_EXCHANGE_CODES"
    - from: "src/lib/strategy-sources.ts"
      to: "supabase/migrations (strategies_source_check)"
      via: "strategy-sources-migration-parity.test.ts set-equality against the LATEST migration"
      pattern: "deribit"
---

<objective>
DRB-02 boundary widening: "deribit" is accepted at every key-saving boundary — TS `SUPPORTED_EXCHANGES` + display map, the three pydantic Literals, and the SQL CHECK constraints via ONE migration — while the funding surfaces and user-facing UI surfaces are DECOUPLED into explicit 3-exchange consts so nothing auto-widens (OQ4 gate + Pitfall 2).

Purpose: Phase 69 (wizard UX) and Phase 70 (ingestion) build on a boundary that already admits deribit; the exclusions stay conscious, pinned by Plan 68-03's parity tests.
Output: widened TS/pydantic/SQL boundaries + decoupled `UI_EXCHANGE_CODES`/`FUNDING_EXCHANGES` consts + one self-verifying CHECK migration + flipped stale assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-boundary-wiring-key-validation/68-CONTEXT.md
@.planning/phases/68-boundary-wiring-key-validation/68-RESEARCH.md
@.planning/phases/68-boundary-wiring-key-validation/68-PATTERNS.md

Key locked decisions (68-CONTEXT.md):
- Boundary IN set: closed-sets.ts SoT + display map; pydantic Literals in schemas.py / debug_key_flow.py / adapter.py; SQL CHECKs via ONE migration re-based on the LATEST definition of each constraint.
- Boundary OUT set (parity-pinned in 68-03, NOT touched here): `_FUNDING_BUCKET_HOURS`, `funding_fees_exchange_check`, `position_snapshots_exchange_check` (Phase 71), ingestion `SUPPORTED_SOURCES` / `process_key` flow sets / `funding_fetch` dispatch (OQ2 — only the `Source` Literal widens for type-consistency).
- OQ3 (live prod schema verified 2026-07-04): WIDEN `api_keys_exchange_check`, `compute_jobs_exchange_check`, `strategies_source_check`, `strategy_verifications_source_check` (lockstep-bias decision: WIDEN — Phase 72 verification needs it, no hazard found). NEVER TOUCH `verification_requests` (now a VIEW) or `verification_requests_legacy` (frozen Phase-19 table).
- OQ4 GATE: marketing count, public VerificationForm dropdown, filter chips, and the PERP/RECONCILABLE cron sets stay 3-exchange via decoupled consts.
</context>

<interfaces>
<!-- Verified current state (2026-07-04) the executor builds against. -->

From src/lib/closed-sets.ts (lines 32-61):
```typescript
export const SUPPORTED_EXCHANGES = ["binance", "okx", "bybit"] as const;
export type SupportedExchange = (typeof SUPPORTED_EXCHANGES)[number];
export const exchangeEnum = z.enum(SUPPORTED_EXCHANGES);
export const EXCHANGE_DISPLAY = { /* binance/okx/bybit */ } as const satisfies Record<SupportedExchange, string>;
export const EXCHANGES: readonly ExchangeDisplay[] = SUPPORTED_EXCHANGES.map((code) => EXCHANGE_DISPLAY[code]);
export function isSupportedExchange(value: string): boolean;
```

From src/lib/strategy-sources.ts:
```typescript
export const STRATEGY_SOURCES = ["legacy","wizard","admin_import","allocator_connected","csv","okx","binance","bybit"] as const;
```

Latest SQL CHECK definitions (grep of ALL migrations confirmed no later ALTER exists for any):
- `api_keys_exchange_check` — inline, 20260405061911_initial_schema.sql:22 — `NOT NULL CHECK (exchange IN ('binance','okx','bybit'))`
- `compute_jobs_exchange_check` — inline, 20260411144407_compute_jobs_queue.sql:132 — `CHECK (exchange IS NULL OR exchange IN (...))`
- `strategies_source_check` — NAMED, latest re-base 20260506211806_strategies_source_csv.sql:45-58 — 8 values `('legacy','wizard','admin_import','allocator_connected','csv','okx','binance','bybit')`
- `strategy_verifications_source_check` — inline, 20260501055202_strategy_verifications.sql:91 — `('okx','binance','bybit','csv')` (order per file)

Compile-forced edits when widening (verified — the ONLY exhaustive maps):
- `EXCHANGE_DISPLAY` (`satisfies Record<SupportedExchange, string>`) — closed-sets.ts:45
- `SOURCE_BADGE_LABEL: Record<StrategySource, string>` — src/components/admin/AdminTabs.tsx:26
All other display maps (ScenarioComposer EXCHANGE_LABELS, queries.ts VENUE_DISPLAY, SyncBadge) are open-keyed `Record<string,string>` with fallback — no edits needed.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Widen the TS SoT allowlist and decouple the UI + funding/reconcile surfaces (OQ4 + Pitfall 2)</name>
  <files>src/lib/closed-sets.ts, src/lib/closed-sets.test.ts, src/lib/constants.ts, src/components/landing/VerificationForm.tsx, src/app/api/cron/sync-funding/route.ts, src/app/api/cron/sync-funding/route.test.ts, src/app/api/cron/reconcile-strategies/route.ts</files>
  <read_first>
    - src/lib/closed-sets.ts (full file — the SoT block :32-61 and surrounding comments)
    - src/lib/closed-sets.test.ts (existing assertions :20-56 that flip)
    - src/lib/constants.ts (:60-95 — EXCHANGES re-export + B8 comment + canonical-snap loop)
    - src/components/landing/VerificationForm.tsx (:7, :18, :24 — SUPPORTED_EXCHANGES-derived dropdown + default)
    - src/app/api/cron/sync-funding/route.ts (:4, :40-42, :60) and src/app/api/cron/reconcile-strategies/route.ts (:4, :34, :55)
    - src/app/api/cron/sync-funding/route.test.ts (~:429 — the arrayContaining subset assertion)
    - .planning/phases/68-boundary-wiring-key-validation/68-PATTERNS.md (§closed-sets.ts, §cron decoupling)
  </read_first>
  <action>
    In src/lib/closed-sets.ts, per D "Boundary set" (68-CONTEXT):
    1. Add "deribit" to SUPPORTED_EXCHANGES (:32). exchangeEnum, isSupportedExchange, utils.ts re-export, create-with-key route, verify-strategy route, and debug-key-flow route all auto-follow — do not touch them.
    2. Add `deribit: "Deribit"` to EXCHANGE_DISPLAY (:41-45) — required or the `satisfies Record<SupportedExchange, string>` fails compile.
    3. Add TWO new explicit 3-value exported consts (OQ4 GATE + Pitfall 2 decoupling), each typed to keep literal narrowing, e.g. `["binance","okx","bybit"] as const satisfies readonly SupportedExchange[]`:
       - `UI_EXCHANGE_CODES` — the user-facing "offered" set. Comment: deribit joins in Phase 69 with the wizard card + /security#deribit-readonly guide; do NOT derive from SUPPORTED_EXCHANGES.
       - `FUNDING_EXCHANGES` — the funding/reconcile-eligible set, TS mirror of `funding_fees_exchange_check` + `_FUNDING_BUCKET_HOURS` staying 3-exchange. Comment: Phase 70 flips this TOGETHER with the SQL CHECK and a native-id/exact-ts dedup axis (BYB-02 — Deribit funding is continuous; a floor bucket would collapse distinct events).
    4. Redefine `EXCHANGES` (:54) to map over UI_EXCHANGE_CODES instead of SUPPORTED_EXCHANGES (still through EXCHANGE_DISPLAY so casing cannot drift). This keeps the marketing "{EXCHANGES.length} exchanges supported" count at 3 and every chip surface (MandateForm/StrategyFilters/PreferencesPanel/ApiKeyForm/StrategyForm/MetadataStep) 3-exchange with ZERO edits to those files. Update the B8 derivation comment (:48-53) accordingly.
    In src/lib/constants.ts: update the stale comment (~:65-68) that says the `(typeof EXCHANGES)[number]` union stays "Binance"|"OKX"|"Bybit" — the TYPE now includes "Deribit" (ExchangeDisplay widened) while the runtime array stays 3-value UI-offered.
    In src/components/landing/VerificationForm.tsx: repoint EXCHANGE_OPTIONS (:18) and the default state (:24) from SUPPORTED_EXCHANGES to UI_EXCHANGE_CODES (public verify dropdown must NOT offer deribit before the Phase-69 scope guide exists — OQ4).
    In both cron routes: repoint `PERP_EXCHANGES` (sync-funding:42) and `RECONCILABLE_EXCHANGES` (reconcile-strategies:34) from `new Set(SUPPORTED_EXCHANGES)` to `new Set(FUNDING_EXCHANGES)`; update the adjacent sourcing comments. Without this, a saved deribit key would be enqueued and hit `funding_fetch.py:857 raise ValueError` every cron run (Pitfall 2).
    In src/lib/closed-sets.test.ts: flip :22 to the 4-value set; flip :53 `isSupportedExchange("deribit")` to true (update its "// wider ccxt set" comment); KEEP :29 `EXCHANGES` asserting exactly ["Binance","OKX","Bybit"] and add a comment that this is the OQ4 Phase-69 gate pin; ADD assertions that UI_EXCHANGE_CODES and FUNDING_EXCHANGES each equal exactly ["binance","okx","bybit"] and do NOT include "deribit".
    In src/app/api/cron/sync-funding/route.test.ts (~:429): replace the `arrayContaining(["binance","okx","bybit"])` subset assertion with an EXACT-set assertion (sorted toEqual) on the `.in("api_keys.exchange", …)` argument so a deribit leak into the cron filter FAILS the test instead of passing silently.
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.test.ts src/app/api/cron/sync-funding/route.test.ts --no-file-parallelism && npx tsc --noEmit && npm run lint -- --quiet</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"deribit"' src/lib/closed-sets.ts` >= 2 (SUPPORTED_EXCHANGES entry + EXCHANGE_DISPLAY key)
    - `grep -n "UI_EXCHANGE_CODES\|FUNDING_EXCHANGES" src/lib/closed-sets.ts` shows both exported consts; neither const's array literal contains "deribit"
    - `grep -n "new Set(FUNDING_EXCHANGES)" src/app/api/cron/sync-funding/route.ts src/app/api/cron/reconcile-strategies/route.ts` matches in BOTH files; `grep -c "new Set(SUPPORTED_EXCHANGES)"` in those two files == 0
    - `grep -n "UI_EXCHANGE_CODES" src/components/landing/VerificationForm.tsx` matches; `grep -c "SUPPORTED_EXCHANGES" src/components/landing/VerificationForm.tsx` == 0
    - closed-sets.test.ts asserts: SUPPORTED_EXCHANGES equals ["binance","okx","bybit","deribit"]; EXCHANGES equals ["Binance","OKX","Bybit"]; isSupportedExchange("deribit") === true; UI_EXCHANGE_CODES and FUNDING_EXCHANGES exclude "deribit"
    - sync-funding/route.test.ts no longer uses arrayContaining for the exchange filter — exact 3-element set asserted
    - `npx vitest run src/lib/closed-sets.test.ts src/app/api/cron/sync-funding/route.test.ts --no-file-parallelism` exits 0; `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SUPPORTED_EXCHANGES is 4-value; EXCHANGES/UI dropdown/cron filters are pinned 3-value via the two new consts; all flipped assertions green; tsc + lint clean.</done>
</task>

<task type="auto">
  <name>Task 2: Widen the three pydantic Literals and flip the stale audit test</name>
  <files>analytics-service/models/schemas.py, analytics-service/routers/debug_key_flow.py, analytics-service/services/ingestion/adapter.py, analytics-service/tests/test_portfolio_router_audit_2026_05_07.py</files>
  <read_first>
    - analytics-service/models/schemas.py (:200-215 — VerifyRequest.exchange Literal + the H-0530 comment that uses deribit as its counter-example)
    - analytics-service/routers/debug_key_flow.py (:55-65 — Broker Literal)
    - analytics-service/services/ingestion/adapter.py (:15-30 — Source Literal + the CONTEXT.md L72 locked-enums comment)
    - analytics-service/tests/test_portfolio_router_audit_2026_05_07.py (:755-770 — test_deribit_exchange_rejected, flips)
    - analytics-service/services/ingestion/__init__.py (:90-95 — SUPPORTED_SOURCES, which must NOT change; read to write the deferral comment accurately)
  </read_first>
  <action>
    Per D "Boundary set" IN-list (68-CONTEXT) and OQ2 resolution:
    1. schemas.py `VerifyRequest.exchange` (:210): `Literal["binance","okx","bybit","deribit"]`. Rewrite the H-0530 comment block (:202-209): it currently uses "deribit" as the out-of-domain example — replace the example with e.g. "kraken" and note the Literal is pinned to the TS SUPPORTED_EXCHANGES boundary (now 4-value, Phase 68).
    2. debug_key_flow.py `Broker` (:61): add "deribit".
    3. adapter.py `Source` (:23): add "deribit". Add a one-line comment: the ingestion registry (`SUPPORTED_SOURCES` in services/ingestion/__init__.py) and the process_key per-flow sets intentionally EXCLUDE deribit until Phase 70 — the Literal widens for boundary type-consistency only (OQ2; Plan 68-03 pins the exclusion). Do NOT touch SUPPORTED_SOURCES, process_key.py, or funding_fetch.py.
    4. test_portfolio_router_audit_2026_05_07.py `test_deribit_exchange_rejected` (:760): the Literal now admits deribit, so this test flips. Replace it with two tests: (a) `VerifyStrategyRequest(**{**self._BASE, "exchange": "deribit"})` constructs WITHOUT raising (deribit accepted at the boundary, Phase 68); (b) an out-of-domain value ("kraken") still raises ValidationError — the closed-set gate itself remains proven.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_portfolio_router_audit_2026_05_07.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"deribit"' analytics-service/models/schemas.py analytics-service/routers/debug_key_flow.py analytics-service/services/ingestion/adapter.py` matches the exchange/Broker/Source Literal line in each of the 3 files
    - `grep -c "deribit" analytics-service/services/ingestion/__init__.py` == 0 and `grep -c "deribit" analytics-service/routers/process_key.py` == 0 (OQ2 exclusions untouched)
    - The audit test file contains a passing test constructing VerifyStrategyRequest with exchange="deribit" AND a test asserting "kraken" raises ValidationError
    - Targeted pytest run exits 0 (if the local Py3.14 venv segfaults at import, fall back to `python -c "from models.schemas import VerifyRequest; import typing; assert 'deribit' in typing.get_args(VerifyRequest.model_fields['exchange'].annotation)"` and let CI be the pytest authority — record which path ran)
    - mypy --strict clean on the three touched analytics-service files (CI-pinned venv caveat noted)
  </acceptance_criteria>
  <done>All three Literals admit deribit; ingestion registry/flow sets untouched; stale audit test flipped with the closed-set gate still proven via kraken.</done>
</task>

<task type="auto">
  <name>Task 3: ONE self-verifying SQL migration widening the four CHECKs + paired TS STRATEGY_SOURCES update</name>
  <files>supabase/migrations/{timestamp}_deribit_exchange_boundary_checks.sql, src/lib/strategy-sources.ts, src/components/admin/AdminTabs.tsx</files>
  <read_first>
    - supabase/migrations/20260602180000_funding_fees_exchange_check.sql (WHOLE file — the DROP/ADD + self-verify DO-block idiom to copy; note :36-81)
    - supabase/migrations/20260506211806_strategies_source_csv.sql (:40-98 — LATEST strategies_source_check re-base incl. lock_timeout idiom + COMMENT ON CONSTRAINT)
    - supabase/migrations/20260405061911_initial_schema.sql (:22 — api_keys inline CHECK), 20260411144407_compute_jobs_queue.sql (:132 — IS NULL OR form), 20260501055202_strategy_verifications.sql (:91, :161-162 — source CHECK + COMMENT)
    - supabase/migrations/20260620120000_verification_requests_view_shim_apply.sql (:105-165 — proof verification_requests is a VIEW; do NOT touch it)
    - src/__tests__/strategy-sources-migration-parity.test.ts (asserts STRATEGY_SOURCES == LATEST strategies_source_check list — this task must keep it green atomically)
    - src/components/admin/AdminTabs.tsx (:20-36 — SOURCE_BADGE_LABEL exhaustive Record<StrategySource,string>)
  </read_first>
  <action>
    Create ONE migration (filename `$(date +%Y%m%d%H%M%S)_deribit_exchange_boundary_checks.sql`; timestamp MUST sort after 20260704150835). Before writing, re-run the memory rule: `grep -rn "<constraint_name>" supabase/migrations/*.sql` for EACH of the four constraints and re-base each ADD on the newest definition found (current latest listed in <interfaces>). Per widened table, copy the funding_fees idiom: `SET lock_timeout = '3s';` once at top; then `ALTER TABLE <t> DROP CONSTRAINT IF EXISTS <name>;` + `ALTER TABLE <t> ADD CONSTRAINT <name> CHECK (...);` + a self-verify DO-block asserting `pg_get_constraintdef` contains 'deribit' (and each pre-existing value), RAISE EXCEPTION on mismatch. The four constraints (canonical names are LOAD-BEARING — 68-03's resolveColumnCheck resolves named constraints):
    1. `api_keys_exchange_check` → `CHECK (exchange IN ('binance','okx','bybit','deribit'))`
    2. `compute_jobs_exchange_check` → `CHECK (exchange IS NULL OR exchange IN ('binance','okx','bybit','deribit'))` — PRESERVE the IS NULL OR nullable form
    3. `strategies_source_check` → the full 8-value latest list + 'deribit' (9 values; key-created strategies stamp source=exchange, OQ3)
    4. `strategy_verifications_source_check` → `('okx','binance','bybit','csv','deribit')` (OQ3 lockstep-bias decision: WIDEN — Phase 72 verification acceptance needs it); also update the COMMENT ON COLUMN strategy_verifications.source (:161-162) vocabulary text.
    Do NOT touch: `funding_fees_exchange_check` (Phase 70, BYB-02), `position_snapshots_exchange_check` (Phase 71), anything on `verification_requests` (VIEW — DROP CONSTRAINT would error) or `verification_requests_legacy` (frozen Phase-19 table). Widening adds a value so no existing row can violate — no pre-flight data scan needed.
    Paired TS updates in the SAME task (the strategy-sources parity test asserts set-equality with the LATEST migration — these must land atomically):
    - src/lib/strategy-sources.ts: add "deribit" to STRATEGY_SOURCES with the paired-migration doc note.
    - src/components/admin/AdminTabs.tsx: add `deribit: "deribit"` to SOURCE_BADGE_LABEL (compile-forced by Record<StrategySource,string>; admin-only surface — NOT an OQ4 leak).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/strategy-sources-migration-parity.test.ts src/__tests__/strategies-source-csv-constraint.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Exactly ONE new file in supabase/migrations/ whose numeric prefix > 20260704150835 and name ends `_deribit_exchange_boundary_checks.sql`
    - Each of the 4 ADD CONSTRAINT statements uses the exact canonical name and its IN list contains 'deribit'; `grep -c "ADD CONSTRAINT" <new_migration>` == 4 and `grep -c "DO \$\$" <new_migration>` >= 4 (self-verify per table)
    - `grep -c "funding_fees\|position_snapshots\|verification_requests" <new_migration>` == 0
    - compute_jobs ADD preserves `exchange IS NULL OR`
    - STRATEGY_SOURCES in strategy-sources.ts contains "deribit"; SOURCE_BADGE_LABEL has a deribit entry; strategy-sources-migration-parity + strategies-source-csv-constraint vitest suites exit 0
  </acceptance_criteria>
  <done>One migration re-bases exactly the four OQ3-WIDEN constraints with self-verify blocks; funding/position/legacy constraints untouched; TS STRATEGY_SOURCES parity green atomically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public API → key-save allowlist | Untrusted exchange strings from the wizard / public verify form cross the zod/pydantic/SQL closed-set gates |
| SoT const → derived UI/cron surfaces | Widening the base const can silently propagate into surfaces that must stay gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-01 | Tampering | TS↔pydantic↔SQL allowlist drift (TS admits what DB rejects → 23514) | mitigate | Same-PR lockstep widening; Plan 68-03 parity contract test pins set-equality both directions |
| T-68-02 | Elevation of Privilege | Public VerificationForm / marketing offering deribit before the scope guide exists | mitigate | UI_EXCHANGE_CODES decoupled 3-value const; EXCHANGES pinned by closed-sets.test.ts |
| T-68-03 | Denial of Service | Deribit keys enrolled into funding/reconcile crons → per-run ValueError, cron error-budget burn | mitigate | FUNDING_EXCHANGES decoupled const + exact-set assertion in sync-funding/route.test.ts |
| T-68-04 | Tampering | Migration DROPs a wrong/nonexistent constraint (IF EXISTS silently no-ops) leaving the stale 3-value CHECK live | mitigate | Canonical auto-names + per-table self-verify DO-block raising on missing 'deribit'; migration-reviewer agent pre-ship (orchestrator) |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/lib/closed-sets.test.ts src/app/api/cron/sync-funding/route.test.ts src/__tests__/strategy-sources-migration-parity.test.ts src/__tests__/strategies-source-csv-constraint.test.ts --no-file-parallelism` — all green
- `cd analytics-service && .venv/bin/python -m pytest tests/test_portfolio_router_audit_2026_05_07.py -q` — green (CI is the authority if the local Py3.14 venv segfaults)
- `npx tsc --noEmit` and `npm run lint` — clean
- Orchestrator notes (NOT executor tasks — no git/ship in plans): (1) run the migration-reviewer agent on the new migration before PR (memory rule; auto-applies to prod on merge); (2) apply the migration to test project qmnijlgmdhviwzwfyzlc via Supabase MCP before any future deribit-inserting e2e (none exist in this phase — all tests are file-read/mocked).
</verification>

<success_criteria>
- Roadmap SC1 (boundary half): deribit admitted by TS allowlist + display map, all three pydantic Literals, and all four OQ3-WIDEN SQL CHECKs, in one plan set / one PR.
- OQ4 gate holds: marketing count 3, VerificationForm dropdown 3, chips 3, crons 3 — each pinned by an assertion that fails on a leak.
- Funding surfaces untouched: `_FUNDING_BUCKET_HOURS`, funding_fees_exchange_check, SUPPORTED_SOURCES, process_key flow sets all deribit-free (grep-verified; parity-pinned in 68-03).
</success_criteria>

<output>
Create `.planning/phases/68-boundary-wiring-key-validation/68-01-SUMMARY.md` when done.
</output>
