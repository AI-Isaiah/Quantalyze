---
phase: 68-boundary-wiring-key-validation
plan: 03
type: execute
wave: 2
depends_on: ["68-01"]
files_modified:
  - src/__tests__/contracts/check-zod-db-check-parity.test.ts
  - analytics-service/tests/test_boundary_literals_parity.py
  - analytics-service/tests/test_funding_match_key_sql_parity.py
autonomous: true
requirements: [DRB-02]
must_haves:
  truths:
    - "The vitest contract matrix proves every widened key-boundary CHECK (api_keys, compute_jobs, strategy_verifications) set-equals its TS source INCLUDING deribit"
    - "The same matrix proves funding_fees.exchange and position_snapshots.exchange EXCLUDE deribit (rejects mechanism) — the flip in Phase 70/71 must consciously edit a pinned spec"
    - "The pytest side proves all three pydantic Literals contain deribit AND the funding/ingestion surfaces (_FUNDING_BUCKET_HOURS, SUPPORTED_SOURCES, process_key flow sets, funding CHECK migration text) exclude it"
    - "Both directions asserted in the SAME PR as the wiring (roadmap SC1)"
  artifacts:
    - path: "src/__tests__/contracts/check-zod-db-check-parity.test.ts"
      provides: "api_keys.exchange + strategy_verifications.source + position_snapshots.exchange specs; funding spec decoupled to FUNDING_EXCHANGES with rejects:['deribit']; EXPECTED_COLUMNS identity list updated"
      contains: "rejects: [\"deribit\"]"
    - path: "analytics-service/tests/test_boundary_literals_parity.py"
      provides: "pydantic Literal inclusion pins + intentional-exclusion pins mirroring the vitest matrix"
      min_lines: 50
    - path: "analytics-service/tests/test_funding_match_key_sql_parity.py"
      provides: "extended BYB-02 pin: _FUNDING_BUCKET_HOURS has no deribit key; funding CHECK migration text still exactly 3-exchange"
      contains: "deribit"
  key_links:
    - from: "src/__tests__/contracts/check-zod-db-check-parity.test.ts"
      to: "supabase/migrations/*_deribit_exchange_boundary_checks.sql"
      via: "resolveColumnCheck named-constraint resolution (newest ADD CONSTRAINT wins)"
      pattern: "api_keys.*exchange"
    - from: "analytics-service/tests/test_boundary_literals_parity.py"
      to: "analytics-service/models/schemas.py + routers/debug_key_flow.py + services/ingestion/adapter.py"
      via: "typing.get_args introspection on the three Literals"
      pattern: "get_args"
---

<objective>
The DRB-02 parity contract test, both runtimes, both directions: the key-boundary allowlists (TS ↔ pydantic ↔ SQL CHECKs) all contain deribit, AND the funding/ingestion/position surfaces intentionally exclude it — each exclusion pinned with a comment pointing at the phase that flips it (70/71), so the flip is conscious, never drift.

Purpose: roadmap SC1's "parity contract test proving the TS allowlist and SQL CHECKs agree" lands in the same PR as the wiring; boundary drift (TS admits what DB rejects → 23514) becomes structurally impossible.
Output: extended vitest contract matrix + new pytest Literal-parity file + extended BYB-02 funding pin.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-boundary-wiring-key-validation/68-CONTEXT.md
@.planning/phases/68-boundary-wiring-key-validation/68-PATTERNS.md
@src/__tests__/contracts/check-zod-db-check-parity.test.ts
@analytics-service/tests/test_funding_match_key_sql_parity.py

Depends on 68-01 outputs: the widened SUPPORTED_EXCHANGES + new FUNDING_EXCHANGES const (closed-sets.ts), the widened Literals, and the `*_deribit_exchange_boundary_checks.sql` migration whose canonical constraint names resolveColumnCheck resolves.

Locked decisions:
- Parity test asserts BOTH directions (68-CONTEXT "Boundary set" + "Testing"): key boundaries CONTAIN deribit; funding surfaces EXCLUDE it, pinned with Phase-70 comments. Follow the byte-parity pattern from test_funding_match_key_sql_parity.py (BYB-02): read the migration file, assert set/substring equality.
- OQ2 pin: SUPPORTED_SOURCES + process_key per-flow sets excluded intentionally (ingestion is Phase 70).
- OQ3 pin: position_snapshots_exchange_check stays 3-exchange (Phase 71).
</context>

<interfaces>
<!-- Verified current state of the vitest matrix (2026-07-04). -->

From src/__tests__/contracts/check-zod-db-check-parity.test.ts:
```typescript
// Spec shape (:179-189): { column, ts, sql: () => string[], rejects?: readonly string[] }
// resolveColumnCheck(table, col, createFile) (:136): prefers newest named
//   "ADD CONSTRAINT <table>_<col>_check", else the inline CHECK in createFile.
//   Handles the "IS NULL OR ... IN (...)" nullable form (compute_jobs spec proves it).
// Existing entries: compute_jobs.exchange (:220, ts: SUPPORTED_EXCHANGES — auto-gains deribit, no edit)
//   funding_fees.exchange (:227, ts: SUPPORTED_EXCHANGES — MUST decouple or it EXPECTS deribit in the 3-exchange funding CHECK)
// EXPECTED_COLUMNS identity guard (:318-340): exact sorted column list — adding specs requires deliberate edit.
// rejects mechanism (:186, :358-363): asserts value absent from the resolved SQL set.
// Header comment (:194) notes strategies.source is covered by strategy-sources-migration-parity.test.ts — leave it there.
```

From analytics-service/tests/test_funding_match_key_sql_parity.py:
```python
# Pattern: _MIGRATION = Path(__file__).resolve().parents[2] / "supabase" / "migrations" / "<file>.sql"
#          sql = _MIGRATION.read_text(encoding="utf-8"); assert sql.count(LITERAL) == N
```

Pydantic introspection targets (post-68-01, all contain "deribit"):
- `models.schemas.VerifyRequest.model_fields["exchange"].annotation`
- `routers.debug_key_flow.Broker`
- `services.ingestion.adapter.Source`
Exclusion targets (must NOT contain "deribit"):
- `services.funding_fetch._FUNDING_BUCKET_HOURS` (dict keys)
- `services.ingestion.SUPPORTED_SOURCES` (tuple, __init__.py:93)
- `routers/process_key.py` per-flow sets (:131-134) — pin via source-text read to avoid importing router deps
- `supabase/migrations/20260602180000_funding_fees_exchange_check.sql` CHECK text (exactly binance/okx/bybit)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the vitest contract matrix — widen specs, decouple funding, pin exclusions</name>
  <files>src/__tests__/contracts/check-zod-db-check-parity.test.ts</files>
  <read_first>
    - src/__tests__/contracts/check-zod-db-check-parity.test.ts (FULL file — Spec shape, resolveColumnCheck, inlineCheckSet, SPECS entries :198-310, EXPECTED_COLUMNS :313-340, rejects assertion :358-363, and the header contract comments)
    - src/lib/closed-sets.ts (68-01 result — SUPPORTED_EXCHANGES 4-value, FUNDING_EXCHANGES 3-value)
    - The new supabase/migrations/*_deribit_exchange_boundary_checks.sql (constraint names + IN lists the specs resolve)
    - src/__tests__/contracts/REGISTRY.md (update if it indexes covered columns)
  </read_first>
  <action>
    In the SPECS matrix:
    1. DECOUPLE the `funding_fees.exchange` spec (:227): change `ts: SUPPORTED_EXCHANGES` to `ts: FUNDING_EXCHANGES` (import from closed-sets) and add `rejects: ["deribit"]`. Update its note: deribit funding is continuous (BYB-02) — Phase 70 flips this spec TOGETHER with the SQL CHECK and _FUNDING_BUCKET_HOURS via a native-id/exact-ts dedup axis. Without this decoupling the spec EXPECTS deribit in the 3-exchange funding CHECK and fails — the central refactor.
    2. ADD spec `api_keys.exchange` — ts: SUPPORTED_EXCHANGES, sql: resolveColumnCheck("api_keys", "exchange", "20260405061911_initial_schema.sql") (resolves to the new named constraint).
    3. ADD spec `strategy_verifications.source` — ts: [...SUPPORTED_EXCHANGES, "csv"] (the live verify write path admits csv; the frozen verification_requests_legacy is deliberately NOT covered — note why in the spec comment).
    4. ADD spec `position_snapshots.exchange` — ts: an explicit inline `["binance", "okx", "bybit"]` literal (NOT FUNDING_EXCHANGES — semantically distinct surface) with `rejects: ["deribit"]` and a comment: positions are Phase 71; the CHECK stays 3-exchange until DeribitNotSupportedError lifts. sql: resolveColumnCheck("position_snapshots", "exchange", "20260412094450_position_snapshots.sql").
    5. `compute_jobs.exchange` (:220) needs NO edit — ts: SUPPORTED_EXCHANGES auto-gains deribit and the migration re-based its CHECK.
    6. Update EXPECTED_COLUMNS (:318) deliberately with the three new columns (the identity guard fails otherwise — that is its job).
    Keep every existing spec and assertion untouched otherwise; update REGISTRY.md if it enumerates covered columns.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/contracts/check-zod-db-check-parity.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - SPECS contains entries for api_keys.exchange, strategy_verifications.source, position_snapshots.exchange; EXPECTED_COLUMNS updated to match (sorted identity assertion green)
    - funding_fees.exchange spec references FUNDING_EXCHANGES and carries rejects: ["deribit"]; position_snapshots.exchange spec carries rejects: ["deribit"]
    - `grep -c "SUPPORTED_EXCHANGES" src/__tests__/contracts/check-zod-db-check-parity.test.ts` shows the funding spec no longer uses it (only key-boundary specs do)
    - MUTATION CHECK (run once, then revert): temporarily removing 'deribit' from the api_keys IN list in the new migration makes the suite FAIL; restoring it makes it PASS — proves the spec resolves the new named constraint, not the stale inline one
    - `npx vitest run src/__tests__/contracts/check-zod-db-check-parity.test.ts --no-file-parallelism` exits 0
  </acceptance_criteria>
  <done>Vitest matrix proves TS↔SQL set-equality including deribit on every widened boundary and pins the funding + position exclusions via rejects — both directions of roadmap SC1's TS side.</done>
</task>

<task type="auto">
  <name>Task 2: Pytest parity — Literal inclusion pins + intentional-exclusion pins (new file + BYB-02 extension)</name>
  <files>analytics-service/tests/test_boundary_literals_parity.py, analytics-service/tests/test_funding_match_key_sql_parity.py</files>
  <read_first>
    - analytics-service/tests/test_funding_match_key_sql_parity.py (FULL file — the Path/read_text/count pin idiom + existing class layout to extend)
    - analytics-service/services/funding_fetch.py (:205-220 — _FUNDING_BUCKET_HOURS + the BYB-02 guard comment the pins reference)
    - analytics-service/services/ingestion/__init__.py (:90-95 — SUPPORTED_SOURCES) and analytics-service/routers/process_key.py (:125-140 — per-flow sets; decide import vs source-text read based on module import weight)
    - The new supabase/migrations/*_deribit_exchange_boundary_checks.sql (for the migration-text widen pins)
  </read_first>
  <action>
    NEW analytics-service/tests/test_boundary_literals_parity.py (I/O-free, no pandas construction — local Py3.14 segfault class):
    - INCLUSION pins via typing.get_args: "deribit" in VerifyRequest.exchange annotation args, in debug_key_flow.Broker args, in adapter.Source args; plus set-equality of the VerifyRequest exchange args against {"binance","okx","bybit","deribit"} (drift guard, mirrors the TS SoT).
    - SQL widen pins (pytest mirror of the vitest matrix, per the CONTEXT byte-parity pattern): read the new deribit_exchange_boundary_checks migration via the Path idiom; assert each of the four canonical ADD CONSTRAINT names appears exactly once and each paired IN list contains 'deribit'.
    - EXCLUSION pins, each with a comment naming the flipping phase: "deribit" not in SUPPORTED_SOURCES (Phase 70 — OQ2: Source Literal widened for type-consistency only, the registry gate is intentional); process_key per-flow sets contain no "deribit" (pin via reading routers/process_key.py source text if importing the router drags heavy deps); no key-permission surface asserted here (68-02 owns DRB-03 tests).
    EXTEND test_funding_match_key_sql_parity.py with the BYB-02 exclusion half:
    - `"deribit" not in _FUNDING_BUCKET_HOURS` (import from services.funding_fetch), comment pointing at Phase 70's native-id/exact-ts dedup axis flip.
    - The 20260602180000 funding CHECK migration text still admits exactly binance/okx/bybit and contains no 'deribit' (read_text pin — the SQL mirror of the same exclusion).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_boundary_literals_parity.py tests/test_funding_match_key_sql_parity.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - test_boundary_literals_parity.py asserts all three Literals contain "deribit" via typing.get_args AND asserts the four widened constraint names + 'deribit' presence by reading the new migration file
    - Exclusion pins present and passing: SUPPORTED_SOURCES, process_key flow sets, _FUNDING_BUCKET_HOURS, funding CHECK migration text — each with a Phase-70/71 flip comment
    - MUTATION CHECK (run once, then revert): temporarily adding "deribit" to _FUNDING_BUCKET_HOURS makes the extended funding parity test FAIL — proves the pin bites
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_boundary_literals_parity.py tests/test_funding_match_key_sql_parity.py -q` exits 0 (targeted files only; CI full-suite is the coverage authority)
  </acceptance_criteria>
  <done>Pytest side mirrors the vitest matrix: inclusion proven on all pydantic Literals + SQL text, every intentional exclusion pinned so Phase 70/71 must consciously edit a failing test to flip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TS runtime ↔ pydantic runtime ↔ SQL persistence | Three independent allowlist encodings of one closed set |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-09 | Tampering | Allowlist drift (TS admits what DB rejects → 23514 at insert; or DB admits what validation never sees) | mitigate | Set-equality specs both directions in both runtimes, reading the migration files as the pinned artifact |
| T-68-10 | Tampering | Silent funding/position widening by a future SUPPORTED_EXCHANGES edit | mitigate | rejects:["deribit"] pins + pytest exclusion pins — the flip requires editing a named, commented test |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages — pure test files |
</threat_model>

<verification>
- `npx vitest run src/__tests__/contracts/check-zod-db-check-parity.test.ts --no-file-parallelism` — green
- `cd analytics-service && .venv/bin/python -m pytest tests/test_boundary_literals_parity.py tests/test_funding_match_key_sql_parity.py -q` — green
- Both mutation checks performed and reverted (recorded in the SUMMARY): migration-deribit removal fails vitest; _FUNDING_BUCKET_HOURS deribit addition fails pytest.
</verification>

<success_criteria>
- Roadmap SC1 (parity half): the contract test proves the TS allowlist and SQL CHECKs agree on deribit at every widened boundary, in the same PR as the wiring, in both runtimes.
- Every intentional exclusion (funding, positions, ingestion registry, flow sets) is pinned with a phase-named comment — Phase 70/71 flips are conscious edits, not drift.
</success_criteria>

<output>
Create `.planning/phases/68-boundary-wiring-key-validation/68-03-SUMMARY.md` when done.
</output>
