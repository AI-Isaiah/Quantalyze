---
phase: 68-boundary-wiring-key-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/key_permissions.py
  - analytics-service/services/exchange.py
  - analytics-service/scripts/deribit_ground_truth.py
  - analytics-service/tests/test_deribit_scope_validation.py
autonomous: true
requirements: [DRB-03]
must_haves:
  truths:
    - "A deribit key with any write scope (:read_write / :read_trade) is rejected with a 400 whose detail names the offending scope token"
    - "A read-only deribit key missing account:read or trade:read is rejected with a 400 naming the missing scope — including the missing-account:read case that would otherwise die generically in fetch_balance"
    - "A compliant LTP-shaped read-only key returns valid:true, read_only:true (passphrase=None accepted)"
    - "The validator is INVOKED at the key-save path: the end-to-end rejection test FAILS when the _DISPATCH['deribit'] entry is removed (wiring guard, memory F1-F12)"
  artifacts:
    - path: "analytics-service/services/key_permissions.py"
      provides: "detect_deribit_permissions probe + _DISPATCH['deribit'] entry + relocated scope_is_read_only gate + additive scope_detail surface"
      contains: "detect_deribit_permissions"
    - path: "analytics-service/services/exchange.py"
      provides: "deribit-scoped honest-error branch in validate_key_permissions preferring scope_detail over generic copy"
      contains: "scope_detail"
    - path: "analytics-service/tests/test_deribit_scope_validation.py"
      provides: "SC2/SC3/SC4 mocked public/auth tests + wiring-invocation guard"
      min_lines: 80
  key_links:
    - from: "analytics-service/services/key_permissions.py (_DISPATCH)"
      to: "detect_deribit_permissions"
      via: "dispatch registration — the SINGLE wiring point lighting up all 6 validate_key_permissions call sites"
      pattern: "\"deribit\": detect_deribit_permissions"
    - from: "analytics-service/services/exchange.py (validate_key_permissions)"
      to: "key_permissions.detect_permissions"
      via: "deribit scope precheck BEFORE fetch_balance + scope_detail-preferred error"
      pattern: "scope_detail"
    - from: "analytics-service/scripts/deribit_ground_truth.py"
      to: "services/key_permissions.py"
      via: "re-import of the relocated scope gate (single definition, harness tests stay green)"
      pattern: "from services.key_permissions import"
---

<objective>
DRB-03 key validation: add a `detect_deribit_permissions` probe to the existing per-exchange dispatch that reads the Deribit `public/auth` `result.scope` string directly, rejecting any write scope and requiring `account:read` AND `trade:read` by name — with honest errors naming the exact scope, surfaced through `validate_key_permissions` and the existing router 400 path.

Purpose: only correctly-scoped read-only Deribit keys can ever be saved; rejections tell the manager exactly which scope to fix (no generic "invalid key").
Output: probe + dispatch entry + additive scope_detail error surface + relocated single-definition scope gate + full mocked test suite with wiring-invocation guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/68-boundary-wiring-key-validation/68-CONTEXT.md
@.planning/phases/68-boundary-wiring-key-validation/68-PATTERNS.md
@analytics-service/scripts/deribit_ground_truth.py
@analytics-service/services/key_permissions.py
@analytics-service/tests/test_deribit_ground_truth.py

Locked decisions (68-CONTEXT "Scope validation"):
- Semantics source of truth: the shipped harness gate — `public/auth` (grant_type=client_credentials) → `result.scope` whitespace-joined tokens; ANY `:read_write`/`:read_trade` token → reject; required `account:read` AND `trade:read` present.
- Error copy: write scope → "key has write scope '<scope>' — create a read-only key"; missing → "key is missing required scope '<name>'". Never generic.
- Placement: the existing per-exchange probe pattern (`services/key_permissions` `_DISPATCH` + `validate_key_permissions`); `EXCHANGE_CLASSES` already carries deribit (exchange.py:788).
- OQ1: extend the probe result surface ADDITIVELY (scope-naming field alongside the `{read,trade,withdraw,probe_error}` contract); binance/okx/bybit probes and copy unchanged.
- A1 caveat (67-03 blocked on founder key): parse by token SUFFIX/prefix matching, never exact-string; flag live re-verify for Phase 72.
- Credential shape: passphrase-less deribit keys valid (`api_keys.passphrase_encrypted` nullable — verified; `create_exchange` only sets ccxt password when truthy).
</context>

<interfaces>
<!-- Verified current state (2026-07-04). -->

From analytics-service/services/key_permissions.py:
```python
PermissionDict = dict[str, bool]                 # :39 — must widen to carry scope_detail (str)
_FAIL_CLOSED: PermissionDict = {"read": True, "trade": True, "withdraw": True, "probe_error": True}  # :48
_DISPATCH = {"binance": ..., "okx": ..., "bybit": ...}   # :251 — deribit MISSING
# detect_permissions (:258): unknown exchange.id → {read:False,trade:False,withdraw:False,probe_error:False}
#   → read_only=False but error=None → key passes with a confusing 200 (Pitfall 4)
# Pre-store path (api_key_id=None) bypasses the TTL cache.
```

From analytics-service/services/exchange.py validate_key_permissions (:895-1090):
```python
# flow: load_markets() [RateLimit/PermissionDenied swallowed] → fetch_balance()
#   [ccxt errors classified: PERMISSION_DENIED / AUTH_FAILED / DDOS_PROTECTION /
#    RATE_LIMITED / EXCHANGE_UNAVAILABLE / NETWORK_UNAVAILABLE / VALIDATION_UNEXPECTED]
# → EXCHANGE_CLASSES membership check → detect_permissions(exchange, api_key_id=None)
# → read_only derivation; has_withdraw → WITHDRAW_SCOPE; has_trade → TRADE_SCOPE (generic copy)
# HAZARD: ccxt deribit fetch_balance() (privateGetGetAccountSummaries) requires account:read —
# a key missing account:read dies in the generic PERMISSION_DENIED branch BEFORE the probe runs.
```

From analytics-service/scripts/deribit_ground_truth.py:
```python
_WRITE_SCOPE_SUFFIXES: tuple[str, ...] = (":read_write", ":read_trade")   # :86
def scope_is_read_only(scope: str) -> bool: ...                            # :89-100 (relocate to services)
# auth call shape proven live-shaped (:503-509):
#   await ex.public_get_auth({"grant_type": "client_credentials",
#                             "client_id": ..., "client_secret": ...})
#   scope = str(auth.get("result", {}).get("scope", ""))
```

From analytics-service/routers/exchange.py (:52-84): `ValidateKeyRequest.exchange: str` (plain — deribit already reaches the flow); `if result["error"]: HTTPException(400, detail=result["error"])`. TS layer (src/app/api/keys/validate-and-encrypt/route.ts F5b/R8) FORWARDS the curated 4xx detail — the honest text reaches the caller with zero TS changes.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Relocate the scope gate into services and add detect_deribit_permissions + _DISPATCH entry</name>
  <files>analytics-service/services/key_permissions.py, analytics-service/scripts/deribit_ground_truth.py, analytics-service/tests/test_deribit_scope_validation.py</files>
  <read_first>
    - analytics-service/services/key_permissions.py (FULL file — PermissionDict/_FAIL_CLOSED contract :36-53, detect_okx_permissions :140-172 and detect_bybit_permissions :175-243 as the signature/fail-CLOSED templates, _DISPATCH :251, detect_permissions unknown-branch :285-293)
    - analytics-service/scripts/deribit_ground_truth.py (:54-100 — _redact_secret_values + _WRITE_SCOPE_SUFFIXES + scope_is_read_only; :495-522 — the proven public_get_auth call + scope extraction)
    - analytics-service/tests/test_deribit_ground_truth.py (import list :31 and mocking style — these tests MUST stay green after the relocation)
    - .planning/phases/68-boundary-wiring-key-validation/68-PATTERNS.md (§key_permissions.py — fail-CLOSED + credential-redaction shared patterns)
  </read_first>
  <behavior>
    - scope "trade:read account:read wallet:read custody:read block_trade:read" (LTP-shaped) → {read: True, trade: False, withdraw: False, probe_error: False}, no scope_detail
    - scope containing "wallet:read_write" → trade=True and scope_detail == "key has write scope 'wallet:read_write' — create a read-only key" (offending TOKEN named)
    - scope "trade:read wallet:read" (no account:read) → read=False and scope_detail == "key is missing required scope 'account:read'"
    - scope "account:read wallet:read" (no trade:read) → read=False and scope_detail == "key is missing required scope 'trade:read'"
    - public_get_auth raising any Exception → returns fail-CLOSED (all True, probe_error=True) and the logged/raised text contains neither the client_id nor client_secret literal values
    - _DISPATCH["deribit"] is detect_deribit_permissions (registration test)
    - relocated scope_is_read_only importable from BOTH services.key_permissions and scripts.deribit_ground_truth with identical semantics
  </behavior>
  <action>
    1. MOVE `_WRITE_SCOPE_SUFFIXES` and `scope_is_read_only` from scripts/deribit_ground_truth.py into services/key_permissions.py verbatim (production validation must not depend on a scripts module; single definition per CONTEXT "reuse, do not re-implement"). In the script, replace the definitions with `from services.key_permissions import _WRITE_SCOPE_SUFFIXES, scope_is_read_only` so its public names, all harness call sites, and test_deribit_ground_truth.py imports stay valid (scripts already import from services — same direction as create_exchange).
    2. Widen the permission payload typing ADDITIVELY so a probe may carry `scope_detail: str` (e.g. convert PermissionDict to a TypedDict with read/trade/withdraw/probe_error as bool and scope_detail as typing.NotRequired[str], or an equivalent alias) — sibling detector BODIES unchanged, mypy --strict green on the touched file.
    3. Add `async def detect_deribit_permissions(exchange: ccxt.Exchange) -> PermissionDict` mirroring the okx/bybit template: call `await exchange.public_get_auth({"grant_type": "client_credentials", "client_id": exchange.apiKey, "client_secret": exchange.secret})` (the harness-proven shape, A2); extract `scope = str(response.get("result", {}).get("scope", ""))`; then:
       - any token ending in _WRITE_SCOPE_SUFFIXES → return read=True, trade=True, withdraw=False, probe_error=False, scope_detail="key has write scope '<token>' — create a read-only key" (first offending token named);
       - else for each required scope name in ("account:read", "trade:read"): satisfied iff any token starts with "<name-part>:" AND ends with ":read" or equals the name (suffix-tolerant per A1 — never exact-string equality on the full scope string); first missing one → return read=False, trade=False, withdraw=False, probe_error=False, scope_detail="key is missing required scope '<name>'";
       - else return read=True, trade=False, withdraw=False, probe_error=False (compliant).
       On ANY exception: log via logger.warning with scrub_freeform_string AND strip literal exchange.apiKey/exchange.secret values from the message (credential-redaction pattern, PATTERNS §shared), then `return dict(_FAIL_CLOSED)` — fail-CLOSED is the security boundary (ASVS V4). Scope strings themselves are not secrets.
    4. Register `"deribit": detect_deribit_permissions` in _DISPATCH (:251) — the single wiring point covering all validate_key_permissions call sites (Pitfall 4: without it a deribit key returns 200 with read_only:false and NO error).
    5. Write the unit half of analytics-service/tests/test_deribit_scope_validation.py per <behavior>: mock only `public_get_auth` on a stub exchange object with `id="deribit"`, `apiKey`, `secret` attributes; every rejection asserts the EXACT honest error text; include the relocation test (both import paths agree) and the _DISPATCH registration test. Tests are I/O-free — no live network, no pandas construction (local Py3.14 segfault class).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_scope_validation.py tests/test_deribit_ground_truth.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "def scope_is_read_only" analytics-service/services/key_permissions.py` matches; `grep -c "def scope_is_read_only" analytics-service/scripts/deribit_ground_truth.py` == 0 and the script imports it from services (single definition)
    - `grep -n "\"deribit\": detect_deribit_permissions" analytics-service/services/key_permissions.py` matches inside _DISPATCH
    - Rejection texts match CONTEXT verbatim shapes: "key has write scope '<token>' — create a read-only key" and "key is missing required scope '<name>'"
    - Exception path test proves fail-CLOSED (all-True, probe_error=True) and asserts credential literals absent from logged output
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_scope_validation.py tests/test_deribit_ground_truth.py -q` exits 0 (targeted files only — never the full local suite)
    - `cd analytics-service && .venv/bin/python -m mypy --strict services/key_permissions.py` exits 0 (B-mypy caveat: CI-pinned venv is authoritative if local drifts)
  </acceptance_criteria>
  <done>Probe registered and unit-proven against mocked public/auth for all four scope classes + fail-CLOSED; scope gate has one definition in services with the harness re-importing; harness tests still green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Honest-error surfacing in validate_key_permissions (deribit precheck before fetch_balance) + wiring-invocation guard</name>
  <files>analytics-service/services/exchange.py, analytics-service/tests/test_deribit_scope_validation.py</files>
  <read_first>
    - analytics-service/services/exchange.py (:895-1090 — full validate_key_permissions: load_markets swallow-path, fetch_balance error-classification ladder with its ordering comments, EXCHANGE_CLASSES check :1062, detect_permissions call + read_only derivation + WITHDRAW_SCOPE/TRADE_SCOPE block :1067-1090)
    - analytics-service/routers/exchange.py (:40-90 — validate_key endpoint: create_exchange, result["error"] → HTTPException 400, aclose_exchange finally)
    - analytics-service/services/key_permissions.py (Task 1 result — scope_detail contract)
  </read_first>
  <behavior>
    - validate_key_permissions(mocked deribit exchange, write-scoped auth) → {valid: False-path irrelevant, error: "key has write scope '…' — create a read-only key", error_code: "TRADE_SCOPE", read_only: False} — fetch_balance NEVER called (precheck ordering)
    - validate_key_permissions(deribit, scope missing account:read) → error == "key is missing required scope 'account:read'", error_code: "MISSING_SCOPE" — even though fetch_balance would have raised PermissionDenied (the mock proves fetch_balance was not reached)
    - validate_key_permissions(deribit, compliant scope + fetch_balance succeeding) → {valid: True, read_only: True, error: None}; detect_permissions probe executed exactly ONCE (precheck result reused, no double public/auth)
    - validate_key_permissions(binance/okx/bybit mocks) → byte-identical error copy to today (generic TRADE_SCOPE/WITHDRAW_SCOPE preserved — additive change only)
    - WIRING GUARD: with _DISPATCH["deribit"] deleted (monkeypatch.delitem), the write-scoped deribit key is NO LONGER rejected (error becomes None) — proving the dispatch entry is the load-bearing invocation and the end-to-end rejection test fails if the branch is neutered
  </behavior>
  <action>
    In validate_key_permissions (services/exchange.py), add a deribit-scoped precheck AFTER the load_markets block and BEFORE `await exchange.fetch_balance()`: when `exchange.id == "deribit"`, call `detect_permissions(exchange, api_key_id=None)` once; if the result carries `scope_detail`, set result["error"] = the scope_detail text and result["error_code"] = "TRADE_SCOPE" when a write scope was found (trade flag True) else the NEW additive code "MISSING_SCOPE", set read_only=False, and return — this is REQUIRED because ccxt deribit fetch_balance needs account:read, so the missing-account:read case would otherwise die in the generic PERMISSION_DENIED branch and violate roadmap SC3 (honest naming). If the precheck result has no scope_detail (compliant or fail-CLOSED), continue to fetch_balance as today and REUSE the precheck perms for the read_only derivation instead of calling detect_permissions a second time (deribit only; the non-deribit path is character-identical to today). Document the ordering rationale in a comment citing DRB-03 + the fetch_balance/account:read dependency, and flag: exact live scope string re-verified when 67-03 runs (A1; Phase 72 gates re-verify).
    Extend tests/test_deribit_scope_validation.py with the end-to-end half per <behavior>: drive validate_key_permissions directly with a mocked ccxt-like deribit object (id="deribit", async load_markets/fetch_balance/public_get_auth mocks; assert fetch_balance not called on rejection paths). Add the wiring-invocation guard via monkeypatch.delitem on key_permissions._DISPATCH. Add a compliant-key test passing passphrase-less construction semantics (probe operates on apiKey/secret only — no passphrase attribute required).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_scope_validation.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MISSING_SCOPE" analytics-service/services/exchange.py` matches (new additive error_code); `grep -n "scope_detail" analytics-service/services/exchange.py` matches inside validate_key_permissions
    - The precheck sits textually BEFORE the `await exchange.fetch_balance()` line and is guarded on exchange.id == "deribit"; the generic WITHDRAW_SCOPE/TRADE_SCOPE copy block (:1082-1087) is byte-unchanged for other exchanges
    - Test asserts fetch_balance NOT awaited on both rejection classes; compliant path asserts exactly one detect_permissions probe execution
    - Wiring guard test exists and passes: deleting _DISPATCH["deribit"] flips the write-scope rejection to error=None (would fail if the deribit branch is neutered)
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_scope_validation.py -q` exits 0; `cd analytics-service && .venv/bin/python -m mypy --strict services/exchange.py services/key_permissions.py` exits 0 (CI-pinned venv authoritative on drift)
  </acceptance_criteria>
  <done>Roadmap SC2/SC3/SC4 proven end-to-end through validate_key_permissions with honest per-scope errors surfacing via the existing router 400 → TS detail-forwarding path; wiring guard fails on a neutered dispatch entry; sibling exchanges byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wizard/API → analytics validate flow | Untrusted per-request credentials cross into the ccxt probe |
| Probe → exchange OAuth | Raw client_id/client_secret cross the wire exactly once (public/auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-68-05 | Elevation of Privilege | Write-capable deribit key saved as "read-only" | mitigate | Scope gate rejects any :read_write/:read_trade token; fail-CLOSED (_FAIL_CLOSED all-True) on ambiguous probe |
| T-68-06 | Elevation of Privilege | Unknown-exchange silent-open hole (Pitfall 4: read_only=false with NO error → 200) | mitigate | _DISPATCH["deribit"] entry + wiring guard test that fails when removed |
| T-68-07 | Information Disclosure | ccxt error echoing client_id/client_secret into logs | mitigate | scrub_freeform_string + literal-credential stripping on the probe exception path; asserted by test |
| T-68-08 | Spoofing | Missing-scope key passing via fetch_balance generic branch mislabeled as network/permission issue | mitigate | Deribit precheck ordered before fetch_balance; MISSING_SCOPE named error |
| T-68-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages (ccxt.deribit already in EXCHANGE_CLASSES) |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_scope_validation.py tests/test_deribit_ground_truth.py -q` — green (TARGETED file list only; the full local suite segfaults at collection under Py3.14 — CI coverage gate is the full-suite authority)
- `cd analytics-service && .venv/bin/python -m mypy --strict services/key_permissions.py services/exchange.py` — clean (verify against the CI-pinned uv venv if the shared local .venv drifts, memory B-mypy)
- A1 flag recorded in code comments: live Deribit scope-string format re-verified when 67-03 runs; Phase 72 acceptance gates re-verify end-to-end.
</verification>

<success_criteria>
- Roadmap SC2: write-scoped deribit key → 400 naming the write scope token.
- Roadmap SC3: key missing account:read or trade:read → 400 naming the missing scope (each scope proven individually, including the account:read case that bypasses fetch_balance's generic death).
- Roadmap SC4: compliant LTP-shaped read-only key → valid:true, read_only:true, passphrase-less.
- Wiring-invocation guard proves the deribit branch is exercised at the key-save path, not just unit-correct.
</success_criteria>

<output>
Create `.planning/phases/68-boundary-wiring-key-validation/68-02-SUMMARY.md` when done.
</output>
