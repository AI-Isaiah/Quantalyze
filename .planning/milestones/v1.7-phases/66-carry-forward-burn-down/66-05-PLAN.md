---
phase: 66-carry-forward-burn-down
plan: 05
type: execute
wave: 3
depends_on: [66-01, 66-02, 66-03]
files_modified:
  - TODOS.md
  - src/components/layout/MobileNav.tsx
  - src/components/layout/MobileNav.test.tsx
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Sidebar.test.tsx
  - src/app/api/for-quants-lead/route.ts
autonomous: true
requirements: [CF-06]

must_haves:
  truths:
    - "Every entry remaining in TODOS.md has been re-verified against the live codebase with file/symbol evidence — nothing survives on memory alone"
    - "Stale/done entries are DELETED (no strike-through graveyard), including the v1.6 red-team F-3/F-4/F-5 section and the holdingReturnsByScopeRef removal entry (both landed in this phase)"
    - "The flagged-count badge displays 99+ for counts over 99 on both the bottom nav and the desktop sidebar (quick win, regression-tested)"
    - "The stale DesktopGate comment rot in for-quants-lead/route.ts is corrected to describe current behavior (quick win, doc-only)"
  artifacts:
    - path: "TODOS.md"
      provides: "Only live, verified debt — the phase's triage deliverable"
  key_links:
    - from: "TODOS.md"
      to: "live codebase"
      via: "per-entry grep verification recorded in the SUMMARY triage table"
      pattern: "n/a — evidence lives in the SUMMARY"
---

<objective>
CF-06: triage all of TODOS.md (1002 lines) down to only live, verified debt — fix the
named quick wins in-phase with regression tests, DELETE stale/done entries same-pass, and
re-verify every survivor against the live code with file/symbol evidence.

Purpose: the ROADMAP success criterion makes the triage a deliverable, not housekeeping —
the file must END the phase reflecting only live debt.
Output: fixed quick wins (99+ badge cap, DesktopGate comment rot) + a triaged TODOS.md +
a per-entry verdict table in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-carry-forward-burn-down/66-CONTEXT.md
@.planning/phases/66-carry-forward-burn-down/66-RESEARCH.md
@.planning/phases/66-carry-forward-burn-down/66-PATTERNS.md

<interfaces>
Quick-win target locations (grep-verified at plan time):
- Flagged-count badge: `flaggedCount` flows through src/components/layout/MobileNav.tsx
  (:34/:40/:52, via buildPrimaryMobileNav) and src/components/layout/Sidebar.tsx
  (:23/:64/:197/:232 — `badge: flaggedCount` / `badge: p.flaggedCount`). The actual badge
  RENDER site (where the numeric value becomes text) may live in these files or a shared
  nav-item component they call — trace `badge` from these entry points to the render.
  Both files have existing test files (MobileNav.test.tsx, Sidebar.test.tsx).
- DesktopGate comments: src/app/api/for-quants-lead/route.ts:189 and :398 (TODOS cites
  ~167/182/391 — lines drifted; grep "DesktopGate" in the file).

Triage raw material: the pre-classification table in 66-RESEARCH.md §CF-06 covers every
open TODOS.md section with a pre-verdict (DELETE / QUICK-WIN / KEEP / VERIFY). It is raw
material, NOT the final verdict — every entry must be re-verified against live code.

Known pins for the triage:
- KEEP: P2 holdingsSummary symbol-only dedup bug — verify queries.ts (research cites :1779,
  grep `.set(r.symbol, r)`) is still the live shape; it is a genuine bug in a LIVE field.
- VERIFY-not-execute: Phase 19.1 P1 Plans 07-10 (gated deploy work) — check whether the
  USE_COMPUTE_JOBS_QUEUE flip + stop-gap removal shipped; record verdict, do NOT execute.
- DELETE after landing: v1.6 red-team F-3/F-4/F-5 section (CF-01..03 landed in plans
  66-01/66-02) and the "queries / SSR payload" P1 holdingReturnsByScopeRef entry
  (CF-04 landed in plan 66-03) — this plan runs in wave 3, after all three.
- KEEP: idea backlogs explicitly headed "IDEAS, not a plan" (north-star / Moment 1-3 /
  demo-narrative).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix the named quick wins — 99+ flagged-count badge cap and DesktopGate comment rot (CF-06 quick wins, per D threshold: small, decision-free, test-coverable)</name>
  <files>src/components/layout/MobileNav.tsx, src/components/layout/MobileNav.test.tsx, src/components/layout/Sidebar.tsx, src/components/layout/Sidebar.test.tsx, src/app/api/for-quants-lead/route.ts</files>
  <read_first>
    - TODOS.md (lines 55-75 — the exact quick-win entry texts: "Cap the displayed value to `99+`" and the v1.3 P3 context)
    - src/components/layout/MobileNav.tsx and src/components/layout/Sidebar.tsx (trace `flaggedCount` → `badge` → the render site where the number becomes visible text; the render may be in a shared nav-item component — follow the prop)
    - src/components/layout/MobileNav.test.tsx and src/components/layout/Sidebar.test.tsx (existing harnesses to extend)
    - src/app/api/for-quants-lead/route.ts (grep "DesktopGate" — currently :189 and :398; read the surrounding code to determine what the comments SHOULD say about current behavior)
    - DESIGN.md (badge is a visual element — the 99+ cap must not change badge styling, only the text content)
  </read_first>
  <behavior>
    - Test 1 (MobileNav.test.tsx): flaggedCount = 150 renders badge text "99+"; flaggedCount = 99 renders "99"; flaggedCount = 5 renders "5". The 150 case fails against current code (renders "150").
    - Test 2 (Sidebar.test.tsx): same three assertions for the desktop sidebar badge.
  </behavior>
  <action>
    Badge cap: at the single point where the badge value is rendered as text (trace it —
    if MobileNav and Sidebar share a render path, cap once at the shared site; if they render
    independently, apply the same tiny format at both, or extract one shared formatter if a
    natural shared module already exists — do NOT create a new module for a one-liner),
    display `count > 99 ? "99+" : String(count)`. No layout/styling change (the TODOS entry
    is about the unbounded WIDTH; capping the text is the fix). Keep aria/accessibility
    text consistent with what the tests can assert.

    DesktopGate comment rot: TODOS.md flags the comments in for-quants-lead/route.ts as
    stale. Read the current wizard/DesktopGate behavior the comments describe, and correct
    each stale comment (currently :189, :398 — grep, don't trust line numbers) to describe
    what the code actually does today. Comment-only change — zero behavior diff in this file
    (no test needed for comments; the vitest run guards against accidental code edits).
  </action>
  <verify>
    <automated>npx vitest run src/components/layout/MobileNav.test.tsx src/components/layout/Sidebar.test.tsx --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - The six badge assertions (3 per surface) pass; the count>99 assertions fail when the cap is reverted
    - `git diff src/app/api/for-quants-lead/route.ts` contains only comment-line changes (every changed line is inside a comment)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Both named quick wins fixed; badge cap regression-tested on both surfaces; comments describe current behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Full TODOS.md triage — verify every entry against live code, delete stale/done, keep only live debt (CF-06)</name>
  <files>TODOS.md</files>
  <read_first>
    - TODOS.md (FULL file, 1002 lines — the triage subject)
    - .planning/phases/66-carry-forward-burn-down/66-RESEARCH.md (§CF-06 pre-classification table — the raw-material verdicts to re-verify, entry by entry)
    - The SUMMARYs of plans 66-01, 66-02, 66-03 in .planning/phases/66-carry-forward-burn-down/ (proof that F-3/F-5, F-4, and the SSR-payload entry landed — the precondition for deleting their TODOS sections)
  </read_first>
  <action>
    Work through TODOS.md section by section against the research pre-classification. For
    EVERY open entry: grep the named file/symbol in the live tree; classify with evidence.
    Rules (all locked per D):
    (1) DELETE same-pass — entries whose described state no longer matches live code (fix
    shipped), the v1.6 red-team F-3/F-4/F-5 section (landed: plans 66-01/66-02), the
    "queries / SSR payload" holdingReturnsByScopeRef P1 entry (landed: plan 66-03), the two
    quick-win entries fixed in Task 1, and any `~~struck~~` already-done Sprint-1 rows. No
    strike-through graveyard — deletion, not annotation.
    (2) KEEP — genuinely live debt, re-verified: the P2 holdingsSummary symbol-only dedup bug
    (confirm queries.ts still has the `.set(r.symbol, r)` shape near the cited line), Phase
    19.1 sections that are still live (VERIFY the USE_COMPUTE_JOBS_QUEUE state and whether
    Plans 07-10 stop-gap work shipped — record the verdict, do NOT execute the work), WCAG
    focus-ring items (CSS with DESIGN.md implications — not decision-free, keep), wizard
    320px reflow, ResponsiveTable migrations, unbuilt widgets, DISCO-05 ratification record,
    idea backlogs ("IDEAS, not a plan" headers).
    (3) Additional quick wins are executor's discretion ONLY if small + decision-free +
    test-coverable (per D threshold); anything requiring product judgment stays as recorded
    debt. If one is taken, it needs its own regression test before its entry is deleted.
    (4) VERIFY-class entries from the research table (v0.23 vitest flakes, PR #149 skipped
    fence tests, founder-LP cron items, extractAnalytics barrel M2): grep the cited
    file/symbol; DELETE if shipped, KEEP with refreshed evidence if live.

    Produce a triage table for the SUMMARY: every entry → verdict (DELETED-stale /
    DELETED-landed-this-phase / FIXED-quick-win / KEPT-live) + the file/symbol evidence line.
    End state: TODOS.md contains only live, verified debt.
  </action>
  <verify>
    <automated>bash -c 'grep -v "^#" TODOS.md | grep -c "isBookOnlyDraft\|holdingReturnsByScopeRef" | awk "{exit (\$1==0)?0:1}" && ! grep -q "~~" TODOS.md'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "F-3\|F-4\|F-5" TODOS.md` counts only occurrences outside the deleted v1.6 red-team section (expected 0 unless another live section legitimately references those ids — justify any survivor in the SUMMARY)
    - Non-header lines of TODOS.md contain zero references to isBookOnlyDraft or holdingReturnsByScopeRef (landed removals)
    - No `~~` strike-through markers remain in the file
    - The SUMMARY triage table has one row per pre-classification entry from 66-RESEARCH.md §CF-06, each with a verdict and a file/symbol evidence citation
    - The P2 holdingsSummary dedup entry survives (it is live debt) with refreshed line evidence
  </acceptance_criteria>
  <done>TODOS.md reflects only live, verified debt; every verdict evidenced in the SUMMARY; landed items gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Docs triage + a display-text cap + comment corrections; no input crosses a new boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-10 | Repudiation | Deleting a TODOS entry that was actually live debt erases the record of a real problem | mitigate (this plan) | Delete only with file/symbol grep evidence recorded in the SUMMARY triage table — the evidence trail replaces the entry; git history preserves the text |
| T-66-01 | Denial of Service | raised MAX_MEMBER_KEY_IDS cap | transfer → plan 66-01 | Owned and mitigated in plan 66-01 |
| T-66-02 | Tampering | F-4 prod sweep UPDATE | transfer → plan 66-02 | Owned and mitigated in plan 66-02 |
| T-66-03 | Tampering/Repudiation | prod auth.users DELETE | transfer → plan 66-02 | Owned and mitigated in plan 66-02 |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan |
</threat_model>

<verification>
- Badge tests green on both surfaces; tsc + lint clean.
- TODOS.md end state satisfies the ROADMAP success criterion 5 (only live debt).
- SUMMARY triage table complete — one evidenced verdict per entry.
</verification>

<success_criteria>
- CF-06: quick wins fixed with regression tests; every TODOS entry verified with evidence; stale/done/landed entries deleted; file ends reflecting only live, verified debt.
</success_criteria>

<output>
Create `.planning/phases/66-carry-forward-burn-down/66-05-SUMMARY.md` when done — it MUST contain the full triage verdict table.
</output>
