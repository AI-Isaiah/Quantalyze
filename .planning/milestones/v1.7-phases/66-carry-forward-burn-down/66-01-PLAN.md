---
phase: 66-carry-forward-burn-down
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/allocator/scenario/share/route.ts
  - src/app/api/allocator/scenario/share/route.test.ts
  - src/app/(dashboard)/allocations/lib/scenario-state.ts
  - src/app/(dashboard)/allocations/lib/scenario-state.test.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx
autonomous: true
requirements: [CF-01, CF-02]

must_haves:
  truths:
    - "Minting a share for a draft with zero added strategies returns 409 (code book_only_draft) via the addedStrategies-empty check alone — no isBookOnlyDraft call exists in the gate"
    - "A draft with more than 64 and up to MAX_MEMBER_KEY_IDS (1000) memberKeyIds passes schema validation; 1001 is rejected — the stamp is never silently clamped"
    - "When a save fails with the over-cap 400, the composer shows honest copy naming the 1000 ceiling, never the generic connection-error message"
  artifacts:
    - path: "src/app/api/allocator/scenario/share/route.ts"
      provides: "Share-mint gate keyed purely on addedStrategies emptiness, honest comment"
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      provides: "Exported MAX_MEMBER_KEY_IDS = 1000 named const wired into the memberKeyIds zod bound"
      contains: "MAX_MEMBER_KEY_IDS"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "One shared save-error helper mapping the over-cap 400 to honest ceiling copy, reused at all four save sites"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/app/(dashboard)/allocations/lib/scenario-state.ts"
      via: "import of MAX_MEMBER_KEY_IDS used to interpolate the ceiling into the honest copy"
      pattern: "MAX_MEMBER_KEY_IDS"
    - from: "src/app/(dashboard)/allocations/lib/scenario-state.ts:793"
      to: "memberKeyIds zod field"
      via: "z.array(...).max(MAX_MEMBER_KEY_IDS)"
      pattern: "max\\(MAX_MEMBER_KEY_IDS\\)"
---

<objective>
Close red-team findings F-3 (CF-01) and F-5 (CF-02) at root: delete the provably-dead
`isBookOnlyDraft` disjunct from the share-mint gate (and the now-orphaned function),
raise the arbitrary `memberKeyIds` `.max(64)` cap to a named `MAX_MEMBER_KEY_IDS = 1000`
const, and replace the misleading generic connection-error copy with honest over-cap
copy naming the real ceiling at all four composer save sites.

Purpose: the v1.6 red-team set is the phase's core debt — both fixes are locked user
decisions (D: DELETE not promote; D: RAISE not clamp; D: honest copy).
Output: three regression-tested fixes in the scenario-membership subsystem.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-carry-forward-burn-down/66-CONTEXT.md
@.planning/phases/66-carry-forward-burn-down/66-RESEARCH.md
@.planning/phases/66-carry-forward-burn-down/66-PATTERNS.md

<interfaces>
From src/app/(dashboard)/allocations/lib/scenario-state.ts (verified):
- `isBookOnlyDraft(draft: ScenarioDraft): boolean` at :684-689 — returns
  `(draft.memberKeyIds ?? []).length >= 1 && (draft.addedStrategies ?? []).length === 0`.
  Only non-test caller is share/route.ts:203 (re-grep before deleting — assumption A5).
- Const idiom at :739-747: `const MAX_DRAFT_RECORD_ENTRIES = 2000;` /
  `const MAX_DRAFT_KEY_LENGTH = 512;` — mirror this for MAX_MEMBER_KEY_IDS.
- Cap site at :793: `memberKeyIds: z.array(z.string().max(MAX_DRAFT_KEY_LENGTH)).max(64).optional(),`
  with the `T-62-02 DoS: ≤64 ids` comment at :791.

From src/app/api/allocator/scenario/saved/route.ts (verified):
- `MAX_DRAFT_BODY_BYTES = 256_000` at :52 (route-level DoS guard — keep).
- The over-cap failure path: `SaveScenarioBodySchema.safeParse` fail →
  :102-106 returns `{ error: "Invalid request body", issues }` status 400.

From src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (verified):
- Generic copy set at FOUR sites: `postNewScenario` :1573 and :1590,
  `putUpdateScenario` :1617 and :1625 — all set
  "Couldn't save this portfolio. Check your connection and try again."
- Shared-pure-helper idiom: `dataSourceLabel(k)` at :632-648.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Delete the dead isBookOnlyDraft disjunct, fix the overstated comment, remove the orphaned function (CF-01, per D — DELETE not promote)</name>
  <files>src/app/api/allocator/scenario/share/route.ts, src/app/api/allocator/scenario/share/route.test.ts, src/app/(dashboard)/allocations/lib/scenario-state.ts, src/app/(dashboard)/allocations/lib/scenario-state.test.ts</files>
  <read_first>
    - src/app/api/allocator/scenario/share/route.ts (lines 50-230 — the import at :58, comment block :191-199, gate :200-213)
    - src/app/api/allocator/scenario/share/route.test.ts (lines 340-430 — the :354 source-string assertion and T_SH15 at :395)
    - src/app/(dashboard)/allocations/lib/scenario-state.ts (lines 665-710 — isBookOnlyDraft definition :684-689)
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts (lines 770-815 — the isBookOnlyDraft test block :778-809)
  </read_first>
  <behavior>
    - Test 1 (NEW regression, next to T_SH15 in route.test.ts): a share-mint request for an owned scenario whose draft has memberKeyIds populated (e.g. 2 ids) AND addedStrategies: [] returns 409 with code "book_only_draft" — proving the surviving addedStrategies-empty check alone blocks book-only drafts. This test must fail if the nothingShareable block is removed (it is the neuter-check for the fix).
    - Test 2 (existing T_SH15) still passes unchanged.
    - The route.test.ts:354 assertion `expect(routeSrc).toContain("isBookOnlyDraft")` is RETIRED or repointed to assert the disjunct is GONE (e.g. `not.toContain("isBookOnlyDraft")`) — it is a source-string pin on the deleted code, not a regression (Pitfall 2).
  </behavior>
  <action>
    Per D (CF-01): DELETE, do not promote. In share/route.ts: (1) remove
    `|| isBookOnlyDraft(draft as ScenarioDraft)` from the `if` at :203 — the disjunct is
    provably dead (addedStrategies empty ⇒ nothingShareable already true and short-circuits;
    addedStrategies non-empty ⇒ isBookOnlyDraft returns false); (2) remove the
    `isBookOnlyDraft` import at :58 after grepping the file to confirm :203 is its only use;
    (3) rewrite the "MEMBER-03 — ONE definition of book-only across mint/resolve/compare"
    comment block at :191-199 — it overstates that the gate reads the shared predicate.
    Replace with the honest invariant: book-only ⇔ zero added strategies; keep the accurate
    note that the share-resolve.ts counterpart (:213-226) keys on strategies.length === 0,
    not memberKeyIds. The rewritten comment must not claim the gate reads isBookOnlyDraft.

    Then re-grep the whole repo for non-test isBookOnlyDraft callers (assumption A5: route.ts
    is the only one; share-resolve.ts:210/222/287 references are prose comments, not calls).
    When confirmed orphaned, delete the function at scenario-state.ts:684-689 (including its
    export) and the scenario-state.test.ts:778-809 test block in the same pass. Sanction:
    research Open Question 4 (RESOLVED — delete in the same pass) under the CONTEXT
    Claude's-discretion charter; the CF-01 locked decision itself covers ONLY the disjunct
    deletion, not the orphaned function. This deviates from the CLAUDE.md dead-code
    AskUserQuestion gate — record the deviation and its sanction explicitly in the 66-01
    SUMMARY (per the CLAUDE.md deviation-documentation policy). Reword any prose comment in share-resolve.ts that names the now-deleted symbol so
    no comment references a nonexistent function.

    In route.test.ts: retire/repoint the :354 source-string assertion and add the Task-1
    regression test per the behavior block.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/allocator/scenario/share/route.test.ts "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isBookOnlyDraft" src/app/api/allocator/scenario/share/route.ts` outputs 0
    - `grep -rn "isBookOnlyDraft(" src --include="*.ts" --include="*.tsx" | grep -v ".test."` outputs nothing (no non-test call sites remain anywhere)
    - `grep -c "export function isBookOnlyDraft" "src/app/(dashboard)/allocations/lib/scenario-state.ts"` outputs 0
    - route.ts comment block no longer contains the string "SAME null-safe" or any claim the gate reads a shared predicate; it states book-only ⇔ zero added strategies
    - New regression test exists in route.test.ts asserting 409/book_only_draft for a draft with memberKeyIds populated and addedStrategies: []
    - Both test files green via the automated command; `npx tsc --noEmit` clean
    - The SUMMARY records the CLAUDE.md dead-code-gate deviation (orphaned isBookOnlyDraft deleted without AskUserQuestion) with its sanction: research Q4 resolution + CONTEXT Claude's-discretion
  </acceptance_criteria>
  <done>Share-mint gate keys purely on addedStrategies emptiness; isBookOnlyDraft fully removed (route disjunct, import, function, orphaned tests); regression test proves book-only drafts stay blocked; comment is honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Raise the memberKeyIds cap to a named MAX_MEMBER_KEY_IDS const (CF-02, per D — RAISE, no silent clamp)</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-state.ts, src/app/(dashboard)/allocations/lib/scenario-state.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.ts (lines 735-800 — const block :739-747, T-62-02 comment :791, cap site :793)
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts (existing schema-bound tests, to extend with the same describe/naming idiom)
  </read_first>
  <behavior>
    - Test 1: a draft with 65 memberKeyIds passes scenarioDraftSchema/scenarioDraftSaveSchema parsing (fails on current code where .max(64) rejects it — this is the fails-without-fix regression).
    - Test 2: a draft with exactly 1000 (MAX_MEMBER_KEY_IDS) memberKeyIds passes.
    - Test 3: a draft with 1001 memberKeyIds is rejected with a zod too_big issue on the memberKeyIds path.
    - Tests reference the exported MAX_MEMBER_KEY_IDS const (build arrays from it), not hard-coded 1000 literals, so the tests track the const.
  </behavior>
  <action>
    In scenario-state.ts, following the same-file named-cap idiom (MAX_DRAFT_RECORD_ENTRIES /
    MAX_DRAFT_KEY_LENGTH at :739-747): add `export const MAX_MEMBER_KEY_IDS = 1000;` with a
    self-documenting comment (research-recommended value: no DB or product cap bounds eligible
    api_keys per allocator; 1000 UUIDs ≈ 36KB, two orders of magnitude under the
    MAX_DRAFT_BODY_BYTES = 256_000 route guard which, with the per-id
    .max(MAX_DRAFT_KEY_LENGTH), remains the real DoS defense). Replace `.max(64)` at :793 with
    `.max(MAX_MEMBER_KEY_IDS)`. Update the `T-62-02 DoS: ≤64 ids` comment at :791 to name the
    new bound and the surviving guards. Export the const — Task 3's composer helper imports it.
    Do NOT add any clamping of the save-time stamp anywhere (locked decision: fail-loud;
    silent clamping breaks share-caption honesty). Do NOT weaken the per-id
    .max(MAX_DRAFT_KEY_LENGTH) or MAX_DRAFT_BODY_BYTES.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-state.test.ts" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export const MAX_MEMBER_KEY_IDS = 1000" "src/app/(dashboard)/allocations/lib/scenario-state.ts"` outputs 1
    - `grep -c "memberKeyIds:.*\.max(64)" "src/app/(dashboard)/allocations/lib/scenario-state.ts"` outputs 0 (do NOT touch the legitimate `lastEditedAt: z.string().max(64)` at :795 — out of scope)
    - Line :793's field reads `.max(MAX_MEMBER_KEY_IDS)`; per-id `.max(MAX_DRAFT_KEY_LENGTH)` still present on the same field
    - Tests for 65-accepted / 1000-accepted / 1001-rejected exist and pass; the 65-accepted test fails when run against the pre-change `.max(64)` code
    - No `.slice(0, ` or equivalent clamp added to any memberKeyIds stamping path
  </acceptance_criteria>
  <done>Cap raised to a named, exported, tested MAX_MEMBER_KEY_IDS = 1000; DoS guards intact; no clamping anywhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Honest over-cap error copy via one shared save-error helper at all four composer sites (CF-02 error surface, per D)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (lines 625-660 — dataSourceLabel helper idiom; lines 1550-1640 — the four generic-copy sites :1573/:1590/:1617/:1625)
    - src/app/api/allocator/scenario/saved/route.ts (lines 95-110 — the 400 body shape `{ error: "Invalid request body", issues }`)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx (lines 550-620 — T_SAVE9 at :574 pins the generic copy for a 500; mirror its harness)
    - DESIGN.md (copy tone for user-facing error text — no new visual language)
  </read_first>
  <behavior>
    - Test 1 (NEW regression): mock the save fetch to return status 400 with body `{ error: "Invalid request body", issues: [{ code: "too_big", path: ["memberKeyIds"], maximum: 1000 }] }` — the composer renders the honest over-cap copy containing the string form of MAX_MEMBER_KEY_IDS (assert via `String(MAX_MEMBER_KEY_IDS)` interpolation, not a hard-coded literal) and does NOT render "Check your connection". Must fail against current code (which shows the generic copy for all !res.ok).
    - Test 2: existing T_SAVE9 (500 → generic copy) still passes unchanged.
    - Test 3: a 400 whose issues do NOT touch memberKeyIds still renders the generic copy (the helper only special-cases the over-cap shape — no scope creep).
  </behavior>
  <action>
    Mirror the dataSourceLabel one-small-pure-helper idiom (Don't-Hand-Roll: no inline
    per-site string logic ×4). Add a module-level helper (suggested name: saveErrorMessage)
    that takes the response status and the parsed body's zod issues array and returns:
    the honest over-cap copy when status === 400 AND issues contain an entry with
    code "too_big" whose path includes "memberKeyIds" — copy must name the ceiling by
    interpolating the imported MAX_MEMBER_KEY_IDS (e.g. "This portfolio references more than
    1000 book sources — the current save limit. Remove some sources and try again." with 1000
    interpolated, exact wording at executor's discretion within DESIGN.md tone); otherwise the
    existing generic string unchanged. In postNewScenario and putUpdateScenario, in the
    `!res.ok` branches, defensively parse the response JSON (await res.json() inside
    try/catch; on parse failure fall back to generic) and set the error via the helper at all
    four sites (:1573, :1590, :1617, :1625). The catch blocks (true network failures) keep the
    generic connection copy — that copy is honest there. Import MAX_MEMBER_KEY_IDS from
    scenario-state.ts (exported in Task 2).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - Exactly one helper function encapsulates the status→message mapping; `grep -c "Couldn't save this portfolio. Check your connection" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` outputs at most 2 (the helper's generic-return and, if kept, the catch-path constant) — the four inline duplicates are gone
    - ScenarioComposer.tsx imports MAX_MEMBER_KEY_IDS from the scenario-state module
    - The new 400/too_big regression test passes and the rendered copy contains "1000" (via the const) and does not contain "Check your connection"
    - T_SAVE9 unchanged and green; full file green via the automated command
  </acceptance_criteria>
  <done>Over-cap 400 renders honest ceiling copy at every save site via one shared helper; generic copy survives only for genuine network/server failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → /api/allocator/scenario/saved | Untrusted draft body crosses here; zod save schema is the gate |
| client → /api/allocator/scenario/share | Share-mint request for owned scenario |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-01 | Denial of Service | memberKeyIds cap raised 64→1000 (scenario-state.ts:793) | mitigate (this plan) | Per-id `.max(MAX_DRAFT_KEY_LENGTH=512)` and route-level `MAX_DRAFT_BODY_BYTES=256_000` remain the real DoS bounds — 1000 UUIDs ≈ 36KB, two orders under the byte guard; Task 2 acceptance forbids removing either guard |
| T-66-02 | Tampering | F-4 prod sweep UPDATE (wrong-membership corruption) | transfer → plan 66-02 | Owned and mitigated in plan 66-02 (fixture-proven transform, detection-first, evidence) |
| T-66-03 | Tampering/Repudiation | prod auth.users DELETE (wrong-row deletion) | transfer → plan 66-02 | Owned and mitigated in plan 66-02 (SELECT-before-DELETE, exact-pattern match, evidence) |
| T-66-04 | Information Disclosure | Share-mint gate loosening via disjunct deletion | mitigate (this plan) | The deleted disjunct is provably dead (subsumption proof in RESEARCH CF-01); Task 1's regression test pins that book-only drafts still 409 — the gate's effective behavior is unchanged |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this phase (RESEARCH: no new dependencies; Package Legitimacy Audit N/A) |
</threat_model>

<verification>
- Both regression tests fail when their fix is reverted (neuter-check honored).
- `npx tsc --noEmit` clean; `npm run lint` clean.
- No memberKeyIds clamp introduced anywhere (grep stamping paths).
</verification>

<success_criteria>
- CF-01: gate carries no dead disjunct; comment honest; isBookOnlyDraft fully removed; regression test green.
- CF-02: cap is a named exported 1000 const; over-cap 400 shows honest ceiling copy at all four sites; regression tests green.
</success_criteria>

<output>
Create `.planning/phases/66-carry-forward-burn-down/66-01-SUMMARY.md` when done.
</output>
