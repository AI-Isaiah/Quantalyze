---
phase: 66-carry-forward-burn-down
plan: 03
type: execute
wave: 2
depends_on: [66-01]
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.my-allocation.test.ts
  - src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts
  - src/app/(dashboard)/allocations/AllocationsTabs.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.onboarding.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComparePanel.test.tsx
autonomous: true
requirements: [CF-04]

must_haves:
  truths:
    - "The dashboard SSR payload no longer computes or carries holdingReturnsByScopeRef — the per-request reconstruction cost is gone"
    - "The Holdings tab, mandate AUM gates, composer seeding, and drift reference all still work — holdingsSummary is untouched (RISK-1 scope correction)"
    - "The full vitest suite is green and the coverage thresholds (82/80/74/72) hold after the removal"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "getMyAllocationDashboard payload without the dead field; holdingsSummary producer intact"
  key_links:
    - from: "src/lib/queries.ts"
      to: "MyAllocationDashboardPayload consumers"
      via: "payload type no longer declares holdingReturnsByScopeRef; tsc proves no consumer reads it"
      pattern: "holdingReturnsByScopeRef"
---

<objective>
Remove the dead `holdingReturnsByScopeRef` SSR pipeline end-to-end (producer function,
payload type field, call site, payload-construction sites, prose comments) with the full
vitest suite green as the gate — and record the honest verdict that the `holdingsSummary`
half of the ROADMAP criterion was a misclassification already satisfied by the v1.6
phase-63 engine-input removal (RISK-1: `holdingsSummary` is LIVE and must not be touched).

Purpose: CF-04 — the planning-lock on this dead per-request compute expired with v1.6
close; carrying a documented-dead field in the RSC payload is pure waste.
Output: straight deletion (no deprecation flag, per D), coverage-neutral (code AND its
tests removed together), SUMMARY verdict on holdingsSummary.
Atomicity note: the 10-file touch-list is deliberately ONE atomic change — typed test
stubs carrying the dead field make any partial deletion fail tsc; splitting cannot compile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-carry-forward-burn-down/66-CONTEXT.md
@.planning/phases/66-carry-forward-burn-down/66-RESEARCH.md
@.planning/phases/66-carry-forward-burn-down/66-PATTERNS.md

<interfaces>
Deletion touch-list in src/lib/queries.ts (research-verified, non-test):
- :2037 `reconstructHoldingReturnsByScopeRef` function definition — DELETE
- :1734 payload TYPE field `holdingReturnsByScopeRef: Record<string, DailyPoint[]>` — DELETE
- :2998 `const holdingReturnsByScopeRef = reconstructHoldingReturnsByScopeRef(...)` — DELETE
- :3013 explanatory comment — DELETE/reword
- :3107 and :3417 payload-construction `holdingReturnsByScopeRef,` — DELETE
- :2462, :2840 prose comment references — reword (doc-only)
- :1729 doc comment ("NO PRODUCTION CONSUMER (as of v1.6 phase 63)…") — delete with the field

DO-NOT-TOUCH list (RISK-1 — holdingsSummary is ALIVE): its producer (queries.ts:2472-2487),
liveBaselineMetrics consumer (:2128-2212, :3000-3073), and the consumers in
AllocationDashboardV2.tsx, HoldingsTabPanel.tsx, ScenarioComposer.tsx, useScenarioState.ts,
mandate-gates.ts, holdings-adapter.ts, ScenarioComparePanel.tsx. TODOS.md:976 even tracks a
live P2 bug in it. Any diff hunk touching a holdingsSummary production line is a defect.

Test sites carrying the dead field (research-verified):
- Mechanical stub removals (`holdingReturnsByScopeRef: {}` in fixture payloads):
  AllocationsTabs.test.tsx:143, AllocationsTabs.onboarding.test.tsx:112,
  AllocationsTabs.scenario-state-preservation.test.tsx:203,
  ScenarioComposer.test.tsx ×9 (453, 535, 1577, 1777, 2054, 2293, 2338, 2876, 4955),
  ScenarioComposer.save.test.tsx:208, ScenarioComparePanel.test.tsx:120
- Active assertions to RETIRE/REPOINT (they pin the dead field — Pitfall 2):
  AllocationsTabs.scenario-composer.test.tsx T_AT3 (:308-321) — RETIRE;
  queries.my-allocation.test.ts:2117-2141 — REPOINT (drop the dead-field axis, KEEP the
  liveBaselineMetrics byte-identity axis);
  src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts:301 — RETIRE.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete holdingReturnsByScopeRef from queries.ts and sweep every test reference in the same atomic change (CF-04)</name>
  <files>src/lib/queries.ts, src/lib/queries.my-allocation.test.ts, src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts, src/app/(dashboard)/allocations/AllocationsTabs.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.onboarding.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.scenario-state-preservation.test.tsx, src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx, src/app/(dashboard)/allocations/components/ScenarioComparePanel.test.tsx</files>
  <read_first>
    - src/lib/queries.ts (lines 1720-1740, 2030-2060, 2450-2500, 2830-2850, 2990-3120, 3410-3430 — every touch-list site AND the adjacent holdingsSummary code you must NOT touch; line numbers may have drifted since plan 66-01's wave — re-locate each site by grepping "holdingReturnsByScopeRef" rather than trusting line numbers)
    - src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx (lines 300-330 — T_AT3, the assertion to retire)
    - src/lib/queries.my-allocation.test.ts (lines 2110-2150 — the Phase 37 byte-identity test to repoint)
    - .planning/milestones/v1.6-phases/63-carry (if present) or grep .planning/milestones for the 63-04 plan's "LEAVE-IT" note — context for the SUMMARY verdict
  </read_first>
  <action>
    Grep-locate then delete every non-test `holdingReturnsByScopeRef` reference in
    src/lib/queries.ts per the touch-list in <interfaces>: the
    `reconstructHoldingReturnsByScopeRef` function, the payload type field, the call site,
    the two payload-construction lines, and the :3013 comment; reword the :2462/:2840 prose
    comments so no comment references the removed field. In the SAME change (a partial
    deletion cannot type-check — typed test stubs carrying the field become excess-property
    errors), sweep the test files: remove the `holdingReturnsByScopeRef: {}` stub lines from
    the fixture payloads listed in <interfaces>; RETIRE T_AT3 in
    AllocationsTabs.scenario-composer.test.tsx (it asserts the composer receives the dead
    field — delete the test, it is a pin on removed code, not a regression); REPOINT
    queries.my-allocation.test.ts:2117-2141 by dropping the holdingReturnsByScopeRef
    assertions while KEEPING the liveBaselineMetrics byte-identity assertions in that test;
    RETIRE the scope_ref type-smoke at getMyAllocationDashboard.scenario.test.ts:301.

    HARD CONSTRAINT (RISK-1): zero production-code changes to holdingsSummary or its
    consumers. holdingsSummary appears near several deletion sites in queries.ts — verify
    each hunk touches only the dead field. Note: ScenarioComposer.save.test.tsx was modified
    by plan 66-01 (new over-cap test) — rebase your stub removal on its current state.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/queries.my-allocation.test.ts "src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts" "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rni "holdingReturnsByScopeRef" src/` outputs nothing — case-INSENSITIVE so it also catches the capital-H `reconstructHoldingReturnsByScopeRef` function and its prose refs (production AND test references all gone)
    - `git diff --stat` for src/lib/queries.ts shows deletions only around the touch-list sites; `git diff src/lib/queries.ts | grep "^[-+].*holdingsSummary"` outputs nothing (no holdingsSummary production line added or removed)
    - queries.my-allocation.test.ts still contains its liveBaselineMetrics byte-identity assertions (repointed, not retired)
    - `npx tsc --noEmit` clean; the targeted vitest command green
  </acceptance_criteria>
  <done>The dead pipeline is gone repo-wide in one atomic change; holdingsSummary untouched; targeted tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite + coverage gate, and record the holdingsSummary already-satisfied verdict (CF-04 closure)</name>
  <files>src/lib/queries.ts</files>
  <read_first>
    - vitest.config.ts (the ratchet thresholds: lines 82 / statements 80 / functions 74 / branches 72 — functions/branches are closest to the floor, Pitfall 4)
    - .planning/phases/66-carry-forward-burn-down/66-RESEARCH.md (§CF-04 / RISK-1 — the misclassification analysis and option (a) resolution the SUMMARY must record)
  </read_first>
  <action>
    Run the full suite with coverage locally (`npm run test:coverage`; add
    --no-file-parallelism if composer/RTL files flake under worker contention — known local
    behavior). Confirm all four thresholds hold — removing tested code together with its
    tests is coverage-neutral, but verify functions (74) and branches (72) specifically. If a
    threshold dips, the cause is a stranded test-only helper or an assertion removed without
    its code — fix the removal, never lower the thresholds.

    Then draft the CF-04 closure verdict for the SUMMARY (no code change): the ROADMAP
    criterion's "holdingsSummary SSR removal" half is a misclassification — holdingsSummary
    is live display data (Holdings tab, mandate AUM gates, composer seeding, drift
    reference); the intended removal (holdingsSummary as a scenario-ENGINE input) already
    shipped in v1.6 phase 63. Record: scope corrected per research RISK-1, resolution option
    (a) already-satisfied, and that the "no consumer breaks / full suite green" success gate
    passes precisely because holdingsSummary was not touched.
  </action>
  <verify>
    <automated>npm run test:coverage</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:coverage` exits 0 (thresholds enforced by vitest config — a red exit is a hard fail)
    - `npm run lint` exits 0 (react-hooks errors are lint-only — standing pre-ship gate)
    - SUMMARY draft contains the RISK-1 verdict naming v1.6 phase 63 as where the engine-input removal shipped
  </acceptance_criteria>
  <done>Full suite green with coverage ratchet intact; CF-04 honestly closed with the scope-correction verdict recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC payload → client components | The SSR payload crosses the server/client boundary; this plan removes a field from it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-06 | Denial of Service (availability regression) | Over-broad deletion breaking the live holdingsSummary surface (Holdings tab, mandate gates, composer seeding) | mitigate (this plan) | RISK-1 hard constraint: diff-level assertion that no holdingsSummary line changes; full suite + coverage gate is the success criterion by design |
| T-66-07 | Information Disclosure | Removing the field REDUCES payload surface (per-scope daily returns no longer shipped to the client) | accept (positive) | Deletion strictly shrinks the RSC payload; no new exposure |
| T-66-01 | Denial of Service | raised MAX_MEMBER_KEY_IDS cap | transfer → plan 66-01 | Owned and mitigated in plan 66-01 |
| T-66-02 | Tampering | F-4 prod sweep UPDATE | transfer → plan 66-02 | Owned and mitigated in plan 66-02 |
| T-66-03 | Tampering/Repudiation | prod auth.users DELETE | transfer → plan 66-02 | Owned and mitigated in plan 66-02 |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan |
</threat_model>

<verification>
- Repo-wide grep for the dead field returns nothing.
- Full suite green, coverage thresholds hold, lint clean.
- SUMMARY carries the holdingsSummary scope-correction verdict.
</verification>

<success_criteria>
- CF-04: dead pipeline removed end-to-end, no consumer breaks, full suite + coverage green, holdingsSummary half honestly resolved as already-satisfied (v1.6 phase 63).
</success_criteria>

<output>
Create `.planning/phases/66-carry-forward-burn-down/66-03-SUMMARY.md` when done — it MUST include the RISK-1 scope-correction verdict.
</output>
