---
phase: 66-carry-forward-burn-down
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/test_scenario_downgrade_sweep.sql
  - scripts/sweeps/f4-memberkeyids-restamp.sql
autonomous: false
requirements: [CF-03, CF-05]

must_haves:
  truths:
    - "Every prod scenarios row with schema_version >= 4 carries the memberKeyIds key after the sweep — OR recorded evidence shows zero downgraded rows existed (valid closure)"
    - "The sweep's detection predicate and re-derive transform are proven by a SQL fixture independent of prod state: it flags the downgraded shape, leaves genuine-v4 rows untouched, and stamps gate-correct ids"
    - "All phase10-rpc-*@test.local auth.users rows are deleted from prod with SELECT-before/after evidence recording the actual count (docs disputed 6 vs 2 — the SELECT resolves it)"
  artifacts:
    - path: "supabase/tests/test_scenario_downgrade_sweep.sql"
      provides: "CI-discovered PL/pgSQL fixture proving discriminator + re-derive transform"
      contains: "memberKeyIds"
    - path: "scripts/sweeps/f4-memberkeyids-restamp.sql"
      provides: "One-off detection-first sweep: sanity SELECT, discriminator SELECT, idempotent re-stamp UPDATE"
      contains: "schema_version >= 4"
  key_links:
    - from: "scripts/sweeps/f4-memberkeyids-restamp.sql"
      to: "scenarios.draft JSONB"
      via: "discriminator `schema_version >= 4 AND NOT (draft ? 'memberKeyIds')` + jsonb_set re-stamp"
      pattern: "draft \\? 'memberKeyIds'"
    - from: "supabase/tests/test_scenario_downgrade_sweep.sql"
      to: "scripts/sweeps/f4-memberkeyids-restamp.sql"
      via: "fixture exercises the SAME discriminator and UPDATE transform the sweep runs"
      pattern: "jsonb_set"
---

<objective>
Close red-team finding F-4 (CF-03) with a one-off, detection-first re-derive/re-stamp
sweep for prod scenarios rows downgraded v4→v3 in the mixed-version deploy window, and
close the CF-05 prod-residue item: SELECT-verify then DELETE the phase10-rpc-*@test.local
auth.users rows. Both run against prod (khslejtfbuezsmvmtsdn) via Supabase MCP with
before/after evidence. Per D: no cron, no migration — the deploy window is past.

Purpose: F-4 rows silently degrade share-caption honesty until reopened; the residue
rows are stale test pollution in prod auth.users.
Output: committed sweep SQL + CI fixture, plus recorded prod evidence (possibly "0 rows
found" — a valid, honest closure).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-carry-forward-burn-down/66-CONTEXT.md
@.planning/phases/66-carry-forward-burn-down/66-RESEARCH.md
@.planning/phases/66-carry-forward-burn-down/66-PATTERNS.md

<interfaces>
From supabase/migrations/20260621120000_scenarios_table_and_rls.sql (verified):
- `scenarios(id UUID PK, allocator_id UUID NOT NULL REFERENCES profiles, name TEXT, draft JSONB NOT NULL, schema_version INT NOT NULL, created_at, updated_at)`

From src/app/(dashboard)/allocations/lib/scenario-state.ts:670-675 (verified):
- `deriveMembershipFromGate(gate, eligibleApiKeyIds)` = `gate ? [...eligibleApiKeyIds] : []`

From src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx:252-261 (verified —
the runtime re-derive the sweep must mirror):
- when `draft.memberKeyIds === undefined` → `setMemberKeyIds(draft, deriveMembershipFromGate(payload.perKeyDailiesGateSatisfied ?? false, payload.eligibleApiKeyIds ?? []))`

Eligible-key predicate (v1.6 grounding, MUST be re-verified against queries.ts before use):
- api_keys where `is_active AND sync_status <> 'revoked' AND disconnected_at IS NULL`
- gate = per-key csv_daily_returns presence, exactly as getMyAllocationDashboard computes it

Genuine-v4 invariant (assumption A1, verified against composer STAMP tests
ScenarioComposer.test.tsx:7630-7649): a genuine v4 write ALWAYS carries the memberKeyIds
key — blank save persists [], book save persists ids. Key ABSENCE on a schema_version >= 4
row is the downgrade signature.

Operational constraint: executor subagents have NO Supabase MCP (project standing rule) —
Tasks 2 and 3 are checkpoints because the prod statements must run from the main session
via the Supabase MCP against prod project `khslejtfbuezsmvmtsdn`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the F-4 sweep SQL artifact and the CI fixture proving its discriminator + re-derive transform (CF-03)</name>
  <files>scripts/sweeps/f4-memberkeyids-restamp.sql, supabase/tests/test_scenario_downgrade_sweep.sql</files>
  <read_first>
    - supabase/tests/test_scenarios_rls.sql (the SQL-fixture convention: plain PL/pgSQL DO $$ blocks, RAISE EXCEPTION on fail / RAISE NOTICE on pass, NO pgTAP, NO psql meta-commands, filename test_*.sql auto-discovered by ci.yml sql-tests)
    - supabase/migrations/20260621120000_scenarios_table_and_rls.sql (scenarios table shape + FK targets the fixture must seed)
    - src/lib/queries.ts (grep `eligibleApiKeyIds` and `perKeyDailiesGateSatisfied` — extract the EXACT eligible-key predicate and gate computation getMyAllocationDashboard uses; do NOT invent the predicate — Pitfall 3)
    - src/app/(dashboard)/allocations/lib/scenario-state.ts (lines 665-680 — deriveMembershipFromGate semantics the UPDATE must mirror)
    - scripts/backfill_funding.py (header only — the one-shot idempotent SELECT-before-mutate contract to replicate)
  </read_first>
  <behavior>
    - Fixture seeds (with explicit cleanup): one DOWNGRADED-shape row (schema_version = 4, draft JSONB WITHOUT the memberKeyIds key) and two GENUINE-v4 rows (one with memberKeyIds: [] — the blank-save shape — and one with populated ids), plus the minimal auth.users/profiles/api_keys/csv_daily_returns scaffolding the derive needs.
    - Assertion 1: the discriminator SELECT returns exactly the downgraded row's id and neither genuine row (RAISE EXCEPTION otherwise).
    - Assertion 2: after running the sweep's UPDATE transform, the downgraded row's draft carries memberKeyIds equal to the gate-derived ids for its seeded allocator (seed the gate-true case at minimum).
    - Assertion 3: both genuine-v4 rows' draft JSONB are byte-identical to their seeded values (the UPDATE's WHERE reuses the discriminator, so idempotent re-runs touch nothing).
  </behavior>
  <action>
    Create scripts/sweeps/f4-memberkeyids-restamp.sql (new directory scripts/sweeps/) as a
    documented one-off with three labeled sections, following the backfill_funding.py
    contract (detection-first, idempotent, evidence-oriented):
    (1) SANITY — a SELECT proving assumption A1 against prod before any mutation: pick known
    schema_version >= 4 rows and show `draft ? 'memberKeyIds'` is true for genuine writes;
    (2) DETECT — `SELECT id, allocator_id, schema_version, updated_at FROM scenarios WHERE
    schema_version >= 4 AND NOT (draft ? 'memberKeyIds')` (the locked discriminator);
    (3) RESTAMP — an idempotent UPDATE that sets `draft = jsonb_set(draft, '{memberKeyIds}',
    <derived>)` ONLY for rows matching the discriminator, where <derived> mirrors
    deriveMembershipFromGate server-side per allocator: when that allocator's per-key dailies
    gate holds → the JSONB array of their eligible api_key ids (predicate copied verbatim
    from queries.ts, expected shape: is_active AND sync_status <> 'revoked' AND
    disconnected_at IS NULL); when the gate fails → '[]'::jsonb. Derive the gate exactly as
    getMyAllocationDashboard does (per-key csv_daily_returns presence) — read queries.ts and
    transcribe, do not guess.

    Create supabase/tests/test_scenario_downgrade_sweep.sql per the test_scenarios_rls.sql
    convention, implementing the behavior block. The fixture must exercise the SAME
    discriminator WHERE clause and the SAME jsonb_set transform as the sweep script (copy the
    SQL, don't paraphrase), so CI proves the exact statements that will run against prod.
    Clean up all seeded rows at the end so the persistent test project is left unpolluted.
  </action>
  <verify>
    <automated>test -f scripts/sweeps/f4-memberkeyids-restamp.sql && test -f supabase/tests/test_scenario_downgrade_sweep.sql && grep -c "DO \$\$" supabase/tests/test_scenario_downgrade_sweep.sql | awk '{exit ($1>=1)?0:1}' && grep -c "draft ? 'memberKeyIds'" scripts/sweeps/f4-memberkeyids-restamp.sql | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist; the fixture filename matches the `supabase/tests/test_*.sql` auto-discovery glob (DB-test CI wiring rule: SQL gates MUST live there, never *_live.py)
    - The fixture contains no pgTAP calls and no psql meta-commands (no lines starting with backslash)
    - The fixture seeds the downgraded shape AND the blank-save genuine shape (memberKeyIds: []) — the [] case is what distinguishes key-absent from key-empty
    - The sweep's UPDATE WHERE clause textually contains the discriminator (`schema_version >= 4` and `NOT (draft ? 'memberKeyIds')`) making re-runs no-ops
    - The eligible-key predicate in the sweep matches the predicate found in queries.ts (cite the queries.ts line range in a SQL comment)
    - CI sql-tests job will execute the fixture (it runs every supabase/tests/test_*.sql against the persistent test project)
  </acceptance_criteria>
  <done>Sweep SQL and CI fixture committed; the pre-prod proof of the transform runs at Task 2 step 0 (fixture executed on the test project via Supabase MCP) and in CI sql-tests thereafter.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute the F-4 sweep against prod via Supabase MCP — detection-first, evidence recorded (CF-03)</name>
  <files>scripts/sweeps/f4-memberkeyids-restamp.sql</files>
  <what-built>Task 1's sweep script: sanity SELECT (A1 check), discriminator SELECT, idempotent re-stamp UPDATE — proven by the committed fixture, run against the TEST project at step 0 below (CI sql-tests re-proves it on every subsequent run).</what-built>
  <read_first>
    - scripts/sweeps/f4-memberkeyids-restamp.sql (the exact statements to run — no ad-hoc SQL)
    - .planning/phases/66-carry-forward-burn-down/66-RESEARCH.md (§CF-03 — Open Question 2: a 0-row detection result is a valid, evidence-recorded closure)
  </read_first>
  <action>
    This is a human/main-session action: executor subagents have NO Supabase MCP, so the
    committed sweep statements must be run from the main session via Supabase MCP against
    prod project `khslejtfbuezsmvmtsdn`, in this order:
    (0) FIXTURE-ON-TEST GATE — run the committed
    supabase/tests/test_scenario_downgrade_sweep.sql against the TEST project
    `qmnijlgmdhviwzwfyzlc` via Supabase MCP and confirm every assertion passes (the fixture
    self-cleans its seeded rows). CI sql-tests cannot have executed this same-session commit
    yet, so this test-project run IS the pre-prod proof of the transform; abort on any
    failure;
    (1) run the SANITY section — confirm known genuine schema_version >= 4 rows carry the
    memberKeyIds key (validates assumption A1 before trusting the discriminator);
    (2) run the DETECT section — record the full result set (ids, allocator_ids, updated_at);
    (3) if DETECT returns 0 rows: STOP — record "0 downgraded rows in prod as of <timestamp>"
    as the closure evidence, F-4 is honestly closed with no mutation;
    (4) if DETECT returns rows: capture BEFORE snapshots (SELECT id, draft->'memberKeyIds',
    schema_version FROM scenarios WHERE id IN (...)), run the RESTAMP UPDATE, capture AFTER
    snapshots;
    (5) paste all query outputs into the plan SUMMARY as the before/after evidence.
    Never run the UPDATE without a same-session DETECT first (detection-first, per D).
  </action>
  <how-to-verify>
    Re-run the DETECT SELECT after any mutation — it must return 0 rows. If no mutation was
    needed, the recorded 0-row DETECT output IS the verification. All outputs go into the
    SUMMARY verbatim.
  </how-to-verify>
  <verify>
    <automated>MISSING — prod-side check; the post-condition query is: SELECT count(*) FROM scenarios WHERE schema_version >= 4 AND NOT (draft ? 'memberKeyIds') — must be run via Supabase MCP (prod khslejtfbuezsmvmtsdn) and must return 0</automated>
  </verify>
  <acceptance_criteria>
    - Step-0 fixture run against the test project (qmnijlgmdhviwzwfyzlc) passed BEFORE any prod statement — output recorded in SUMMARY
    - SANITY output shows genuine v4 rows with the key present (A1 holds) — recorded in SUMMARY
    - DETECT output recorded verbatim (row count may be 0 — valid closure)
    - If any UPDATE ran: before/after snapshots recorded per affected id, and post-UPDATE DETECT returns 0 rows
    - No UPDATE executed without a prior same-session DETECT
  </acceptance_criteria>
  <done>Prod carries zero downgraded v4 rows (verified by post-condition query), with the evidence trail in the SUMMARY.</done>
  <resume-signal>Paste the SANITY + DETECT (+ before/after if mutated) outputs, then type "approved" to continue</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: SELECT-verify then DELETE the phase10-rpc auth.users residue rows from prod (CF-05 residue)</name>
  <files>none — prod data operation only</files>
  <what-built>Nothing to build — this is the CF-05 prod-data cleanup with the SELECT-before-DELETE contract.</what-built>
  <read_first>
    - .planning/phases/66-carry-forward-burn-down/66-RESEARCH.md (§CF-05 — the 6-vs-2 count dispute, assumption A2: do NOT hard-code a count; FK cascade note)
  </read_first>
  <action>
    Human/main-session action via Supabase MCP against prod `khslejtfbuezsmvmtsdn`:
    (1) SELECT id, email, created_at FROM auth.users WHERE email LIKE
    'phase10-rpc-%@test.local' — record the ACTUAL count and rows (docs disputed 6 vs 2;
    this SELECT is the resolver, per D — do not hard-code an expected count);
    (2) for the returned ids, spot-check dependent data before deleting: profiles, api_keys,
    scenarios, holdings rows keyed to those user ids — confirm nothing real hangs off them
    (expected: stale test residue only, latest asof 2026-04-26 per v1.6 grounding); deletion
    cascades (e.g. scenarios.allocator_id REFERENCES profiles ON DELETE CASCADE);
    (3) DELETE FROM auth.users WHERE email LIKE 'phase10-rpc-%@test.local' — the exact same
    pattern as the SELECT, never a hand-typed id list; abort if any returned row does not
    match the pattern or looks real;
    (4) re-run the SELECT — must return 0 rows;
    (5) record before-rows, dependent-data check results, and the after-SELECT in the plan
    SUMMARY, including the resolved actual count.
  </action>
  <how-to-verify>
    The after-SELECT (step 4) returning 0 rows is the verification; before/after outputs
    recorded verbatim in the SUMMARY.
  </how-to-verify>
  <verify>
    <automated>MISSING — prod-side check; the post-condition query is: SELECT count(*) FROM auth.users WHERE email LIKE 'phase10-rpc-%@test.local' — must be run via Supabase MCP (prod khslejtfbuezsmvmtsdn) and must return 0</automated>
  </verify>
  <acceptance_criteria>
    - Before-SELECT output recorded with the actual row count (resolves the 6-vs-2 dispute — no hard-coded expectation)
    - Every returned email matches the exact `phase10-rpc-%@test.local` pattern; any non-matching or real-looking row aborts the DELETE
    - Dependent-data check recorded (which child tables held rows for these ids)
    - After-SELECT returns 0 rows; all evidence in SUMMARY
  </acceptance_criteria>
  <done>Zero phase10-rpc residue rows remain in prod auth.users; evidence trail complete.</done>
  <resume-signal>Paste the before/after SELECT outputs, then type "approved" to continue</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator/MCP → prod database | Service-role SQL bypasses RLS; the SELECT-before-mutate contract is the control |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-02 | Tampering | F-4 re-stamp UPDATE corrupting live drafts (wrong membership → share-caption dishonesty, the exact harm F-4 protects against) | mitigate (this plan) | (a) transform proven against seeded downgraded/genuine/blank-save shapes BEFORE prod contact by running the committed fixture on the TEST project (qmnijlgmdhviwzwfyzlc) via Supabase MCP at checkpoint step 0 — CI sql-tests re-proves it on every later run; (b) UPDATE WHERE reuses the discriminator so genuine-v4 rows (including memberKeyIds: []) are untouchable; (c) A1 sanity SELECT before trusting the discriminator; (d) gate/eligible predicate transcribed from queries.ts, not invented; (e) before/after snapshots per affected row |
| T-66-03 | Tampering/Repudiation | prod auth.users DELETE — wrong-row deletion cascades through profiles/api_keys/scenarios | mitigate (this plan) | Exact-pattern `phase10-rpc-%@test.local` SELECT with recorded count precedes DELETE using the identical pattern; dependent-data check per id; abort on any non-matching row; after-SELECT proves scope; evidence in SUMMARY |
| T-66-01 | Denial of Service | raised MAX_MEMBER_KEY_IDS cap | transfer → plan 66-01 | Owned and mitigated in plan 66-01 (byte-cap + per-id guards retained) |
| T-66-05 | Elevation of Privilege | Sweep runs with service-role via MCP (bypasses RLS by design) | accept | One-off, operator-supervised, statements pre-committed in the repo and fixture-proven; no persistent elevated pathway is created (no cron, no migration — locked decision) |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan |
</threat_model>

<verification>
- CI sql-tests job runs test_scenario_downgrade_sweep.sql green against the persistent test project.
- SUMMARY contains: A1 sanity evidence, DETECT output (count may be 0), before/after snapshots if mutated, residue-row before/after SELECTs with resolved count.
</verification>

<success_criteria>
- CF-03: prod carries no schema_version >= 4 row missing the memberKeyIds key (or evidence records zero existed); sweep logic CI-proven.
- CF-05 (residue half): zero phase10-rpc-*@test.local rows remain in prod auth.users; evidence recorded.
</success_criteria>

<output>
Create `.planning/phases/66-carry-forward-burn-down/66-02-SUMMARY.md` when done — it MUST embed the prod evidence outputs.
</output>
