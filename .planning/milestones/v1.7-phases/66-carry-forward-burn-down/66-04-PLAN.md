---
phase: 66-carry-forward-burn-down
plan: 04
type: execute
wave: 3
depends_on: [66-01, 66-03]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/(dashboard)/allocations/components/CoverageTimeline.tsx
  - src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
autonomous: true
requirements: [CF-05]

must_haves:
  truths:
    - "A per-key (book-member) row on the coverage gantt renders a friendly exchange/account label, never a raw key UUID — in the visible text, title, and aria-label"
    - "The ScenarioComparePanel mount in AllocationsTabs receives a compile-time-checked payload — the `as unknown as` double-cast at :964 is gone"
    - "The D3 no-persistence decision (YAGNI, revisit on user demand) is recorded at the toggle site and in the SUMMARY"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx"
      provides: "Regression test: per-key gantt row renders friendly label, not raw id (EXTENDS the existing 197-line phase-58 COVERAGE-01 suite, PR #566 — never overwrite)"
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      provides: "Explicit structural narrow for the ScenarioComparePanel payload"
  key_links:
    - from: "ScenarioComposer.tsx timelineRows useMemo"
      to: "payload.apiKeys via the dataSourceLabel idiom"
      via: "per-key row name resolution before rows reach CoverageTimeline"
      pattern: "dataSourceLabel"
    - from: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      to: "ScenarioComparePanelProps[\"payload\"]"
      via: "explicitly constructed payload object (no cast)"
      pattern: "ScenarioComparePanelProps"
---

<objective>
Close the CF-05 code smalls: friendly key labels on the coverage gantt (reusing the
composer's existing dataSourceLabel/payload.apiKeys idiom, per D), replace the
AllocationsTabs.tsx:964 `as unknown as` payload double-cast with an explicit structural
narrow (per D), and record the D3 source-toggle no-persistence decision (per D — YAGNI).

Purpose: planning-ledger smalls from the v1.6 close; each is a locked decision with a
named mechanism.
Output: two code fixes (one extending an existing regression test file) + one documented decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/66-carry-forward-burn-down/66-CONTEXT.md
@.planning/phases/66-carry-forward-burn-down/66-RESEARCH.md
@.planning/phases/66-carry-forward-burn-down/66-PATTERNS.md
@DESIGN.md

<interfaces>
From src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (verified):
- `dataSourceLabel(k: { exchange: string; label: string; id: string })` at :632-648 —
  resolves EXCHANGE_LABELS[k.exchange] + nickname; the file's key→friendly-label idiom.
- Gantt row-build `timelineRows` useMemo at ~:2344-2355 (post-66-01/66-03 line drift
  possible — grep "timelineRows"): maps `engineSet.strategies.filter(selected)` to
  `{ id: s.id, name: s.name, span, inBlend }`.
- Eligible-keys memo idiom at ~:1912-1918: `(payload.apiKeys ?? []).filter(k => eligible.includes(k.id))`.
- D3 toggle vicinity: ~:1790 (comment "Phase-36 D3").

From src/app/(dashboard)/allocations/components/CoverageTimeline.tsx (verified):
- Row shape `{ id, name, ... }` at :35; renders `row.name` as title (:130), text (:132),
  aria-label (:155). CoverageTimeline receives fully-built rows — it never derives names.

From src/app/(dashboard)/allocations/AllocationsTabs.tsx (verified):
- :964 `payload={props as unknown as ScenarioComparePanelProps["payload"]}` — the double-cast.
- Sibling mounts at :945-958 pass explicitly-named props (the idiom to match).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Friendly key labels on the coverage gantt + D3 decision comment (CF-05, per D — reuse the surrounding label idiom)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx, src/app/(dashboard)/allocations/components/CoverageTimeline.tsx, src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the dataSourceLabel helper :632-648, the timelineRows useMemo — grep "timelineRows", the D3 toggle vicinity — grep "Phase-36 D3", AND where book-member/per-key entries enter engineSet.strategies — trace how a per-key row's `name` is populated to confirm it is a raw id there)
    - src/app/(dashboard)/allocations/components/CoverageTimeline.tsx (full file, 172 lines — row shape and the three name-render sites :130/:132/:155)
    - src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx (full file, 197 lines — the EXISTING phase-58 COVERAGE-01/WCAG/timezone regression suite from PR #566; you EXTEND this file, never overwrite it; note its STATIC GUARD test pins the CoverageTimeline.tsx source — no `new Date(`, utcEpoch required, no recharts)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (the STAMP/gantt test vicinity — grep "timelineRows" or "CoverageTimeline" for the harness to extend)
    - DESIGN.md (label copy must reuse existing exchange/account label language — no new visual vocabulary)
  </read_first>
  <behavior>
    - Test 1 (EXTEND the EXISTING CoverageTimeline.test.tsx — the 197-line phase-58 COVERAGE-01/WCAG/timezone suite from PR #566; APPEND a new `it` block, never overwrite the file or delete/modify existing tests): rendering CoverageTimeline with a row whose name is a friendly label shows that label in the row text, title attr, and aria-label (pins the render contract). Every pre-existing COVERAGE-01/WCAG/timezone test stays untouched and green.
    - Test 2 (composer-level regression, in ScenarioComposer.test.tsx next to the existing gantt/STAMP tests): with a payload containing an api key (id = a UUID, exchange = e.g. "bybit", label = e.g. "Main") selected as a book-member row, the rendered gantt row shows the dataSourceLabel-derived friendly text and does NOT show the raw UUID. Must fail against current code (which renders the raw id).
  </behavior>
  <action>
    First trace where per-key/book-member rows get their `name` (read_first item 1) — the
    research finding is that per-key rows carry the raw key id into `timelineRows`. Apply the
    fix at the row-build in ScenarioComposer.tsx: when a timeline row's id matches an entry
    in `payload.apiKeys`, resolve its display name through the existing `dataSourceLabel`
    helper (the locked-decision idiom) instead of the raw id; strategy rows keep `s.name`
    unchanged. If tracing shows the raw id is injected upstream (where the book-member
    strategy entry is created), fix at that single upstream site instead — whichever site is
    the ONE place the name is born; do not map in CoverageTimeline (it renders, never
    derives). CoverageTimeline.tsx itself should need no logic change — only touch it if the
    row type needs a doc-comment update.

    EXTEND the existing CoverageTimeline.test.tsx (197 lines — phase-58 COVERAGE-01/WCAG/
    timezone regression tests, PR #566; the earlier "verified absent" claim was wrong) by
    APPENDING the render-contract test; never overwrite the file or remove any existing
    test. Add the composer-level regression per the behavior block. Note ScenarioComposer.tsx and ScenarioComposer.test.tsx were modified by plans
    66-01/66-03 — work from their current state.

    Same file, second deliverable: at the D3 source-toggle site (~:1790), add a short comment
    recording the decision: "D3 source-toggle persistence: DECIDED no persistence (YAGNI,
    Phase 66 CF-05) — toggle is deliberately transient UI state; revisit only on user
    demand." Carry the same decision text into the SUMMARY (the planning docs track D3 —
    PROJECT.md:33 / ROADMAP.md:62 — and close against the SUMMARY record).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/CoverageTimeline.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - CoverageTimeline.test.tsx passes IN FULL: the appended render-contract test AND every pre-existing phase-58 COVERAGE-01/WCAG/timezone test (git diff for this file shows additions only — no existing test deleted or modified)
    - The composer-level test asserts the friendly label is present AND the raw UUID string is absent from the rendered gantt row; it fails when the name-mapping is reverted
    - The name resolution goes through dataSourceLabel (grep confirms the timeline row-build or the upstream name-birth site references it) — no second label formatter was written
    - A comment containing "no persistence" and "YAGNI" exists within the D3 toggle block in ScenarioComposer.tsx
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Per-key gantt rows render friendly labels via the existing idiom, pinned by tests; D3 decision recorded in code and SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the AllocationsTabs:964 payload double-cast with an explicit structural narrow (CF-05, per D)</name>
  <files>src/app/(dashboard)/allocations/AllocationsTabs.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx (lines 930-975 — the :964 cast and the sibling explicit-prop mounts at :945-958 whose idiom this must match)
    - src/app/(dashboard)/allocations/components/ScenarioComparePanel.tsx (the ScenarioComparePanelProps["payload"] type — enumerate exactly which fields the panel requires)
  </read_first>
  <action>
    Replace `payload={props as unknown as ScenarioComparePanelProps["payload"]}` with an
    explicitly constructed object listing the fields ScenarioComparePanelProps["payload"]
    requires, sourced from `props` — compile-time checked, zero casts (per D and the
    Don't-Hand-Roll table: NO new zod schema; the data is server-trusted SSR props, a
    structural narrow is proportionate). Build the object inline or via a useMemo consistent
    with the sibling mounts' style at :945-958. If the payload type requires many fields,
    a typed helper `const comparePanelPayload: ScenarioComparePanelProps["payload"] = { ... }`
    keeps the annotation as the compile gate. Note: CF-04's payload-field removal (plan
    66-03) already landed — the payload type no longer has holdingReturnsByScopeRef.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/allocations/AllocationsTabs.test.tsx" "src/app/(dashboard)/allocations/AllocationsTabs.scenario-composer.test.tsx" --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "as unknown as" "src/app/(dashboard)/allocations/AllocationsTabs.tsx"` output is strictly lower than before the change, and the ScenarioComparePanel mount line contains no cast
    - The constructed payload carries an explicit `ScenarioComparePanelProps["payload"]` type annotation (or is passed to a prop typed as such with zero `as` operators in between)
    - `npx tsc --noEmit` clean — the compiler is the regression gate (deleting a required field from the constructed object must fail the build)
    - Existing AllocationsTabs tests green
  </acceptance_criteria>
  <done>The compare-panel payload is type-safe; tsc enforces the contract; no cast survives at the mount.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR props → client components | Server-trusted payload flows into client mounts; type erasure at :964 was the weakness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-66-08 | Tampering (type confusion) | `as unknown as` double-cast silently masks payload/panel contract drift | mitigate (this plan) | Task 2 replaces the cast with a compile-time-checked structural narrow; tsc becomes the standing gate |
| T-66-09 | Information Disclosure | Friendly labels could leak key nicknames into gantt title/aria attrs | accept | Labels come from the user's OWN payload.apiKeys via the same dataSourceLabel already rendering these labels elsewhere in the composer — no new audience, no new data |
| T-66-01 | Denial of Service | raised MAX_MEMBER_KEY_IDS cap | transfer → plan 66-01 | Owned and mitigated in plan 66-01 |
| T-66-02 | Tampering | F-4 prod sweep UPDATE | transfer → plan 66-02 | Owned and mitigated in plan 66-02 |
| T-66-03 | Tampering/Repudiation | prod auth.users DELETE | transfer → plan 66-02 | Owned and mitigated in plan 66-02 |
| T-66-SC | Tampering | npm/pip/cargo installs | accept | Zero package installs in this plan |
</threat_model>

<verification>
- New and extended tests green; tsc clean; lint clean.
- DESIGN.md respected: no new label vocabulary, existing dataSourceLabel output reused.
</verification>

<success_criteria>
- CF-05 (code smalls): gantt shows friendly key labels (tested), :964 cast replaced with a compile-checked narrow, D3 no-persistence decision recorded.
</success_criteria>

<output>
Create `.planning/phases/66-carry-forward-burn-down/66-04-SUMMARY.md` when done — include the D3 decision record.
</output>
