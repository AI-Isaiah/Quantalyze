---
phase: 70-trades-ingestion-dailies-risky
plan: 05
type: execute
wave: 3
depends_on: [70-03]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_broker_dailies.py
autonomous: true
requirements: [DRB-07, DRB-08]
must_haves:
  truths:
    - "run_derive_broker_dailies_job has a deribit venue branch that sources realized returns from ONE transaction-log ledger pass (fetch_deribit_ledger_daily_records, 70-03) — NOT fetch_all_trades and NOT the fills/trades endpoint (D-08: realized comes from the ledger cash deltas)"
    - "The deribit branch passes EMPTY funding_rows to combine_realized_and_funding — funding is already inside the ledger settlement cash delta (single sum, no separate funding stream, no funding_fees write); Deribit rows never touch the funding_fees table"
    - "Deribit ledger daily_records flow through combine_realized_and_funding → trades_to_daily_returns_with_status → csv_daily_returns, identical shape to bybit/okx (no Deribit-specific dailies/metrics path)"
    - "The re-anchored D-02 gate (assert_ledger_complete, 70-03) runs before the upsert: a truncated/incomplete ledger (any scope×currency not reaching continuation=null, a 10028 truncation, or a -32602 skip) returns FAILED and does NOT upsert a partial track record"
    - "The deribit branch anchors returns to a USD-denominated equity value (fetch_account_equity_usd or an account-summary-derived USD anchor). A CI shape test asserts the anchor is USD-based (revert to a coin/non-USD base → red)"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "deribit branch in run_derive_broker_dailies_job (ledger realized, empty funding, ledger-completeness gate, USD equity anchor, csv_daily_returns upsert)"
      contains: "fetch_deribit_ledger_daily_records"
    - path: "analytics-service/tests/test_broker_dailies.py"
      provides: "test_deribit_one_path_shape (parity) + ledger-completeness-gate fail-loud + empty-funding + USD-equity-anchor shape tests"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "fetch_deribit_ledger_daily_records + assert_ledger_complete + combine_realized_and_funding"
      via: "deribit venue branch feeding the exchange-agnostic dailies tail with empty funding_rows; gate before upsert"
      pattern: "fetch_deribit_ledger_daily_records|assert_ledger_complete|combine_realized_and_funding"
---

<objective>
Wire the DRB-08 ONE-path, reworked to the LOCKED ledger design
(`analytics-service/docs/deribit-ingestion-design.md`): a `deribit` venue branch in
`run_derive_broker_dailies_job` that pulls realized returns from the SINGLE transaction-log ledger
pass (70-03), passes EMPTY funding_rows to `combine_realized_and_funding` (funding is inside the
settlement cash delta — no separate stream, no funding_fees write), enforces the re-anchored D-02
gate (`assert_ledger_complete`) before any upsert, and pushes the result through the exchange-agnostic
`combine_realized_and_funding → trades_to_daily_returns_with_status → csv_daily_returns` tail — the
SAME honest source Overview/Scenario/factsheet read.

Purpose: this is the payoff plan — a real Deribit key's per-key dailies become honest and
completeness-verified. Realized + funding come from ONE ledger sum (funding-inclusive settlement,
count-once), and a silently-partial ledger FAILS LOUD before any partial track record is written.
Output: job_worker deribit branch + the one-path shape parity test + the USD-equity-anchor shape test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@analytics-service/docs/deribit-ingestion-design.md
@.planning/phases/70-trades-ingestion-dailies-risky/70-CONTEXT.md
@.planning/phases/70-trades-ingestion-dailies-risky/70-03-SUMMARY.md
@analytics-service/services/broker_dailies.py
</context>

<interfaces>
LOCKED design pins (deribit-ingestion-design.md — authoritative):
- Dailies source = the txn-log cash-delta ledger (70-03), NOT reconstructed from fills. Funding is
  inside the settlement cash delta → funding_rows passed to combine is EMPTY; NO funding_fees write for
  Deribit (this is why the phase needs NO funding_fees_exchange_check migration — the prior plan is dropped).
- D-02 RE-ANCHOR: the gate is assert_ledger_complete (ledger completeness over scope×currency to
  continuation=null), NOT a reconcile to 18,778/21,014/61,248.

Consume (do not re-implement):
- `services.deribit_ingest.fetch_deribit_ledger_daily_records(exchange, since_ms=None) ->
  (daily_records, completeness_report)` (70-03) and `assert_ledger_complete(report)` (raises
  LedgerCompletenessError) + `LedgerTruncatedError` (70-03).
- `services.broker_dailies.combine_realized_and_funding(realized_pnl_records, funding_rows,
  account_balance, balance_error)` (:119-134) → (returns Series, meta). Pass the ledger daily_records as
  realized_pnl_records and `[]` as funding_rows.

Seam in job_worker.py (this file — re-grep the current ranges):
- `run_derive_broker_dailies_job` (~:1772-1966). Existing venue branch does `realized = await
  fetch_all_trades(...)` then per-venue funding; existing combine (~:1860); csv_daily_returns upsert
  (~:1911-1945, key-mode api_key_id,date / strategy-mode strategy_id,date); <2-days handling (~:1863-1903).
- ADD `elif venue == "deribit":` that instead calls fetch_deribit_ledger_daily_records ONCE to get the
  ledger daily_records + completeness_report, runs assert_ledger_complete BEFORE combine, passes the
  records + `[]` funding to combine. Keep everything downstream (combine, <2-days, upsert,
  key-mode/strategy-mode) UNCHANGED. Do NOT call upsert_funding_rows for deribit (no funding rows).
- `fetch_account_equity_usd(ctx.exchange, venue)` (~:1835) — the anchor; verify it supports deribit. If
  not, compute the USD equity anchor from the account summary (per-currency equity × event/mark index →
  USD). NEVER anchor to a coin/non-USD base (the broker_dailies anchor-shift class).
- Rate limit: `ccxt.RateLimitExceeded` → `_stamp_429` stays.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: deribit branch in run_derive_broker_dailies_job — ledger realized + empty funding + ledger-completeness gate + USD equity anchor + combine</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_broker_dailies.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:1772-1966 (whole function — the dual-mode + combine + upsert tail to leave unchanged; re-grep exact lines)
    - analytics-service/services/broker_dailies.py:73-134 (combine + funding_rows_to_daily_pnl_records shape; note funding_rows=[] path)
    - analytics-service/docs/deribit-ingestion-design.md §"Net plan revisions" items 1/2/7 (ledger dailies; drop funding stream; ONE path)
    - 70-03-SUMMARY (fetch_deribit_ledger_daily_records + assert_ledger_complete signatures)
  </read_first>
  <behavior>
    - test_deribit_branch_sources_from_ledger: with fetch_deribit_ledger_daily_records monkeypatched to
      return (records, complete_report) and fetch_all_trades monkeypatched to RAISE, the deribit branch
      still produces dailies (proves deribit does NOT call fetch_all_trades — D-08)
    - test_deribit_passes_empty_funding: the branch calls combine_realized_and_funding with funding_rows
      == [] and NEVER calls upsert_funding_rows (funding is inside the ledger settlement sum) — a spy on
      upsert_funding_rows sees zero calls; wiring a funding stream turns this red
    - test_deribit_completeness_gate_fails_loud: when assert_ledger_complete raises LedgerCompletenessError
      (a scope×currency did not reach continuation=null), the job returns DispatchResult FAILED and does
      NOT upsert csv_daily_returns (no partial track record) — neuter the gate call → red
    - test_deribit_ledger_truncation_fails_loud: a fetch_deribit_ledger_daily_records that raises
      LedgerTruncatedError → job FAILED, no upsert (a truncated crawl never renders as complete — D-14)
    - test_deribit_equity_anchor_is_usd: given a synthetic deribit account summary (coin-margined equity +
      event/mark index), the branch's equity anchor is a USD-denominated figure (positive float), NOT the
      raw coin quantity — reverting the anchor to a coin/non-USD base mis-scales and turns the test red
  </behavior>
  <action>
    In run_derive_broker_dailies_job add `elif venue == "deribit":`. Inside: call
    `fetch_deribit_ledger_daily_records(ctx.exchange, None)` ONCE → (records, report). Do NOT call
    fetch_all_trades for deribit (cite D-08 in a comment — realized comes from the ledger cash deltas).
    Run `assert_ledger_complete(report)` BEFORE combine; on LedgerCompletenessError or LedgerTruncatedError
    return DispatchResult FAILED-permanent with a scrubbed message and do NOT proceed to the
    csv_daily_returns upsert (fail-loud, no partial render — the re-anchored D-02 gate). Then call
    `combine_realized_and_funding(records, [], account_balance, balance_error)` — funding_rows is EMPTY
    (funding is inside the ledger settlement sum; there is NO funding_fees write for deribit).

    Equity anchor: confirm fetch_account_equity_usd supports deribit; if not, compute the USD equity
    anchor from the account summary (per-currency equity × event/mark index → USD) in this branch. NEVER
    anchor to a coin/non-USD base. Extract the anchor computation into a small testable helper so
    test_deribit_equity_anchor_is_usd can assert the USD shape without live I/O.

    Leave the combine, <2-days handling, and the key-mode/strategy-mode csv_daily_returns upsert
    UNCHANGED — deribit realized flows through the identical tail. Keep the ccxt.RateLimitExceeded →
    _stamp_429 path.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_broker_dailies.py -k deribit -x -q</automated>
  </verify>
  <acceptance_criteria>
    - deribit branch sources realized from fetch_deribit_ledger_daily_records (fetch_all_trades not called for deribit)
    - funding_rows passed to combine is []; upsert_funding_rows is NEVER called for deribit (grep + spy)
    - assert_ledger_complete runs before upsert; LedgerCompletenessError / LedgerTruncatedError → job FAILED, no csv_daily_returns upsert
    - The equity anchor is USD-based; a coin/non-USD base turns test_deribit_equity_anchor_is_usd red
  </acceptance_criteria>
  <done>A real Deribit key's ledger returns flow through the ONE path with the re-anchored D-02 ledger-completeness gate as the honesty backstop, empty funding (no funding_fees write), and a revert-proof USD equity anchor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_deribit_one_path_shape — parity of the compute_all_metrics output shape</name>
  <files>analytics-service/tests/test_broker_dailies.py</files>
  <read_first>
    - analytics-service/services/broker_dailies.py:119-134 (combine) + services/metrics.py (compute_all_metrics input contract: ascending DatetimeIndex + float64 — re-grep the range)
    - an existing bybit/okx dailies-shape test in tests/test_broker_dailies.py (the fixture to mirror for parity)
  </read_first>
  <behavior>
    - test_deribit_one_path_shape: feed a synthetic set of deribit ledger daily_records (hand-built, ≥2
      days, incl one inverse coin→USD settlement day and one option-delivery day) with EMPTY funding
      through combine_realized_and_funding → assert the returned Series has an ascending gap-free
      DatetimeIndex + float64 dtype, and that compute_all_metrics returns a dict with the SAME keys as the
      equivalent bybit fixture (structural parity — no Deribit-specific keys, no forked path)
    - test_deribit_no_specific_metrics_path: assert (structural) that the deribit dailies go through
      combine_realized_and_funding (not a bespoke function) — the produced records are the same daily_pnl
      shape bybit emits, with funding_rows empty
  </behavior>
  <action>
    Add test_deribit_one_path_shape to tests/test_broker_dailies.py mirroring the existing bybit/okx
    dailies-shape fixture. Build synthetic deribit ledger daily_records (daily_pnl shape from 70-02/70-03)
    with funding_rows=[] spanning ≥2 days, run them through combine_realized_and_funding and
    compute_all_metrics, and assert the metrics dict keys match the bybit fixture's keys exactly (proves
    DRB-08: same honest source, no fork). This is the DRB-08/D-15(e) revert-proof pin — a Deribit-specific
    metrics path would diverge the key set and go red.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_broker_dailies.py -k "deribit_one_path" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - The synthetic deribit ledger records + empty funding produce a valid compute_all_metrics input (ascending float64 index)
    - The metrics dict key set is identical to the bybit fixture's (no Deribit-specific keys)
    - The test goes red if a Deribit-specific dailies/metrics path is introduced
  </acceptance_criteria>
  <done>DRB-08 ONE-path parity is pinned: deribit ledger dailies are shape-identical to bybit/okx through compute_all_metrics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fetched ledger → csv_daily_returns | a partial/truncated ledger must never render as a complete track record |
| deribit dailies → Overview/Scenario/factsheet | all read csv_daily_returns; a forked metrics path would silently diverge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-14 | Tampering (silent partial render) | csv_daily_returns upsert | mitigate | assert_ledger_complete (ledger completeness, 70-03) FAILS the job before upsert; LedgerTruncatedError / -32602 shortfall also fail; no partial track record (the re-anchored D-02 gate) |
| T-70-15 | Tampering (funding double-count / misroute) | combine funding_rows | mitigate | funding is inside the ledger settlement sum → funding_rows=[] (count-once); no funding_fees write for deribit (no separate stream to double-count) |
| T-70-16 | Tampering (wrong anchor) | equity anchor | mitigate | fetch_account_equity_usd supports deribit or a USD anchor computed from account summary; CI shape test asserts USD base (coin/non-USD base → red) |
| T-70-SC | Tampering | package installs | accept | zero new packages |
</threat_model>

<verification>
- cd analytics-service && python -m pytest tests/test_broker_dailies.py -k deribit -q (CI-runnable synthetic)
- grep: job_worker deribit branch calls fetch_deribit_ledger_daily_records (NOT fetch_all_trades); passes []-funding; never calls upsert_funding_rows; runs assert_ledger_complete before upsert
- Live: actual multi-year ledger crawl reaching continuation=null across scopes×currencies on the Railway worker (skipIf-no-live-creds) — evidence, NOT CI
</verification>

<success_criteria>
- Deribit per-key realized (funding-inclusive) flows through the ONE compute_all_metrics path (DRB-08)
- Re-anchored D-02 gate (ledger completeness) fails loud before any partial upsert; empty funding, no funding_fees write (DRB-07 count-once)
- USD equity anchor shape pinned (revert-proof); one-path shape parity with bybit/okx pinned; no Deribit-specific metrics path
</success_criteria>

<output>
Create `.planning/phases/70-trades-ingestion-dailies-risky/70-05-SUMMARY.md` when done —
note whether fetch_account_equity_usd supported deribit or a summary-based USD anchor was added, and that
the branch passes empty funding + runs assert_ledger_complete before the upsert.
</output>
</content>
