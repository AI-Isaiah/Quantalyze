---
phase: 70-trades-ingestion-dailies-risky
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - analytics-service/scripts/deribit_ground_truth.py
  - analytics-service/tests/test_deribit_ground_truth.py
  - analytics-service/docs/deribit-ground-truth.md
  - analytics-service/docs/evidence/p70-wave0-deribit-subaccount-probe.json
autonomous: false
requirements: [DRB-04, DRB-06, DRB-07]
must_haves:
  truths:
    - "A1 is RECORDED with live evidence: whether index_price/mark_price are populated on type=settlement txn-log rows (per-account presence counts, not a single sample)"
    - "A2 is RECORDED with live evidence: whether the read-only LTP keys can pass subaccount_id and receive subaccount trade/txn-log history — per-scope row counts per account"
    - "A3 is RECORDED with live evidence: whether inverse-perp type=trade rows carry nonzero cashflow (double-count risk vs settlement rows) — per-kind cashflow stats"
    - "BOTH stream counts are reconciled per-account against the known 18,778 / 21,014 / 61,248: total_trades_all_scopes (trades endpoint) AND total_txn_trade_rows_all_scopes (txn-log type=trade). The evidence + docs RECORD which stream matches the known totals as a LOCKED design pin — this is the honesty anchor the D-02 gate (70-03/70-06) must guard (Pitfall 5: the trades endpoint under-returns vs the txn-log)"
    - "Per-account USD equity is captured from the account summary and RECORDED (the dailies equity-anchor value that 70-06's anchor + revert-proof shape test rely on)"
    - "No committed evidence contains an unmasked credential, email, or long opaque token (assert_sanitized enforced)"
  artifacts:
    - path: "analytics-service/scripts/deribit_ground_truth.py"
      provides: "Widened _TXN_LOG_WHITELIST + subaccount_id loop + settlement price-presence / trade cashflow stats + per-account equity capture"
      contains: "index_price"
    - path: "analytics-service/docs/deribit-ground-truth.md"
      provides: "Phase-70 Wave-0 recorded answers section (A1/A2/A3) + the honesty-anchor determination (which stream matched) + per-account USD equity, status RECORDED"
      contains: "A1"
    - path: "analytics-service/docs/evidence/p70-wave0-deribit-subaccount-probe.json"
      provides: "Sanitized per-account probe evidence (both stream counts + account_equity)"
  key_links:
    - from: "analytics-service/scripts/deribit_ground_truth.py"
      to: "private/get_transaction_log + private/get_user_trades_by_currency_and_time"
      via: "subaccount_id request param looped over get_subaccounts ids"
      pattern: "subaccount_id"
---

<objective>
Resolve the THREE load-bearing live-only unknowns (A1/A2/A3 from 70-RESEARCH.md) BEFORE any
production ingestion design is locked, AND resolve the honesty-anchor question the plan-checker
surfaced: the D-02 count gate protects `csv_daily_returns`, whose realized stream comes from the
TXN-LOG (D-08), not the trades endpoint — and RESEARCH Pitfall 5 (live-observed) shows the trades
endpoint UNDER-RETURNS vs the txn-log (0 vs 650/860 for accounts 1&2). So Wave-0 MUST reconcile
BOTH streams and RECORD which one matches the known totals. The 67-03 harness whitelist excluded
the fields that answer A1/A3, and the main-account-only fetch captured only 3.5% / 4.1% / 0.8% of
the known trade totals. This plan widens the harness and re-runs it live on the Railway worker.

Purpose: Wave-0 gate for the RISKY phase — the inverse coin→USD conversion source (A1), the
subaccount fetch strategy (A2), the realized/funding no-double-count partition (A3), the
honesty-anchor stream (which count matches the known totals), and the per-account USD equity
anchor are all pinned to this plan's recorded evidence. Downstream plans 70-02..70-07 MUST NOT
proceed until the checkpoint here is approved.
Output: widened harness + recorded answers in docs/deribit-ground-truth.md (incl. the honesty-anchor
determination + per-account equity) + sanitized evidence JSON.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/70-trades-ingestion-dailies-risky/70-CONTEXT.md
@.planning/phases/70-trades-ingestion-dailies-risky/70-RESEARCH.md
@.planning/phases/70-trades-ingestion-dailies-risky/70-VALIDATION.md
@analytics-service/docs/deribit-ground-truth.md
@analytics-service/scripts/deribit_ground_truth.py
</context>

<interfaces>
Current harness landmarks (verified 2026-07-04):
- `_TXN_LOG_WHITELIST` at scripts/deribit_ground_truth.py:101-112 — currently
  (type, amount, balance, equity, cashflow, instrument_name, side, position, timestamp, currency).
- `summarize_txn_log(rows)` at :115-133 — one whitelisted sample per type + type_counts.
- `classify_instrument(name)` at :148-165 — returns inverse_perpetual | linear_perpetual | option | future | unknown.
- `sanitize_evidence` masks values for keys in `_MASK_KEYS = {username, user_id, email, system_name, id}`
  via truncate_account_id (***last4) — a whitelisted `id` field will appear MASKED in evidence; that is
  sufficient (presence + distinctness demonstrable from last4) and MUST stay masked.
- `_paginate_trades` (:298-381, has_more + cursor=last_ts + IN-8 stall guard) and
  `_paginate_txn_log` (:384-435, continuation token) — both take a params dict built inline.
- `run()` (:438-633): scope gate → get_currencies/account_summary held-set → per-currency capture →
  `private_get_get_subaccounts` count-only observation (:598-613). account_summary already fetched
  here carries per-currency `equity` — the source for the per-account USD equity capture.
- CLI `main()` (:636+): env DERIBIT_CLIENT_ID/DERIBIT_CLIENT_SECRET; args --start-ms --end-ms
  --max-pages --count; prints ONE sanitized JSON to stdout; exit 0/2/3/1.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Widen the harness — whitelist fields, subaccount_id loop, A1/A3 stats, both-stream counts, equity capture</name>
  <files>analytics-service/scripts/deribit_ground_truth.py, analytics-service/tests/test_deribit_ground_truth.py</files>
  <read_first>
    - analytics-service/scripts/deribit_ground_truth.py (whole file — you are modifying its capture core)
    - analytics-service/tests/test_deribit_ground_truth.py (existing pure-logic test conventions: I/O-free, no pandas construction)
    - 70-RESEARCH.md §Assumptions Log (A1/A2/A3) + §Open Questions + §Common Pitfalls 5 — the exact questions the new fields must answer AND why BOTH stream counts matter
  </read_first>
  <behavior>
    - Test: summarize_txn_log passes through the six new whitelist fields (index_price, mark_price, price, id, trade_id, user_seq) when present on a row
    - Test: settlement price-presence stats — given N type=settlement rows where K carry index_price, the summary reports {"settlement": {"total": N, "index_price_present": K, "mark_price_present": M}}
    - Test: trade cashflow stats — given type=trade rows of kind inverse_perpetual with mixed zero/nonzero cashflow, the summary reports per-kind {"total": n, "cashflow_nonzero": k} (this is the A3 answer, numeric)
    - Test: txn-log type=trade row count — summarize_txn_log exposes a txn_trade_row_count (count of type=="trade" rows) so a per-scope roll-up can compute total_txn_trade_rows_all_scopes (the txn-log completeness stream, distinct from the trades-endpoint count — Pitfall 5)
    - Test: per-type samples are capped at 5 and prefer distinct instrument kinds (at least one sample per observed kind per type while under the cap)
    - Test: the request-params builder includes "subaccount_id" exactly when a non-None sub id is passed, and omits it for the main scope (None)
    - Test: sanitize_evidence still masks values under an "id" key (***last4) — the widened whitelist must not defeat masking
  </behavior>
  <action>
    Widen `_TXN_LOG_WHITELIST` with: index_price, mark_price, price, id, trade_id, user_seq
    (exactly the 70-RESEARCH Wave-0 set). Do NOT remove "id" from `_MASK_KEYS` — evidence ids stay
    masked; masked presence is all A1/A4 need.

    Extend `summarize_txn_log` to return, in addition to type_counts/type_samples:
    (a) `type_samples` as up to 5 samples per type (kind-diverse preference via classify_instrument
    on instrument_name), (b) `settlement_price_stats` = {total, index_price_present,
    mark_price_present} counted over type=settlement rows (A1, numeric — never trust a single
    sample), (c) `trade_cashflow_stats` = per-classify-kind {total, cashflow_nonzero} over
    type=trade rows (A3, numeric), (d) `txn_trade_row_count` = count of type=="trade" rows (the
    txn-log completeness stream). Keep the function pure and never-raising on malformed rows.

    Add the subaccount loop to `run()`: after the scope gate, enumerate sub ids from
    `private_get_get_subaccounts` (result[].id, ints), build scopes = [None, *sub_ids], and run the
    existing per-currency `_paginate_trades` + `_paginate_txn_log` capture once per scope. Thread an
    optional `subaccount_id` through both paginators into the request params (present only when not
    None — extract a small pure `_build_history_params(...)` helper so the inclusion rule is
    unit-testable without I/O). Label scopes in evidence as "main", "sub_1", "sub_2", ... by ordinal
    (never the raw id — defense in depth on top of masking). Record per-scope per-currency
    trade_count/txn type_counts plus per-scope roll-up totals, and a top-level
    `history_completeness` = {total_trades_all_scopes, total_txn_trade_rows_all_scopes} — capturing
    BOTH streams so the checkpoint can determine which one matches the known totals (Pitfall 5: they
    diverge). Also record a top-level `account_equity` block: per scope/account, the raw
    account_summary equity figures (per-currency `equity` + any `equity_usd`/`margin_balance` fields
    present, numeric only) — this is the dailies equity-anchor evidence 70-06 relies on. Record
    per-scope errors verbatim-scrubbed (an auth/permission error on a sub scope IS the A2 answer —
    capture it, do not swallow). Keep the -32602 / 10028 handling behavior identical to today
    (record, continue).

    Keep the module's lazy-import discipline (pure-logic layer stays ccxt-free) and the existing
    sanitization pipeline (sanitize_evidence → assert_sanitized before print) untouched at the
    output boundary.
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_deribit_ground_truth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `_TXN_LOG_WHITELIST` contains exactly the 10 prior fields + the 6 new fields
    - `summarize_txn_log` returns settlement_price_stats, trade_cashflow_stats, and txn_trade_row_count with the shapes tested above
    - A pure params-builder proves subaccount_id inclusion/omission without network I/O
    - Evidence structure carries per-scope ("main"/"sub_N") per-currency counts + history_completeness roll-up with BOTH total_trades_all_scopes AND total_txn_trade_rows_all_scopes + a per-account account_equity block
    - "id" remains in _MASK_KEYS; assert_sanitized still guards stdout
    - All new tests fail if the corresponding change is reverted (run each against the pre-change function shape mentally; the stats/count tests cannot pass on the old single-sample summary)
  </acceptance_criteria>
  <done>Harness captures everything needed to answer A1/A2/A3 numerically, per-subaccount, with BOTH stream counts + per-account equity, sanitization intact; targeted pytest file green locally.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live Wave-0 probe on the Railway worker — record A1/A2/A3 + the honesty anchor + equity</name>
  <files>analytics-service/docs/deribit-ground-truth.md, analytics-service/docs/evidence/p70-wave0-deribit-subaccount-probe.json</files>
  <read_first>
    - analytics-service/docs/deribit-ground-truth.md §Run metadata (the 67-03 procedure this re-run mirrors)
    - 70-RESEARCH.md §Pitfalls 1-3 + §Pitfall 5 (trades endpoint under-returns vs txn-log — the crux of the honesty-anchor question) and §Open Questions (the exact partition question)
  </read_first>
  <what-built>
    Task 1's widened harness. The orchestrator (NOT an autonomous executor — founder Deribit creds
    live only in the Railway env, staged from 67-03) runs it once per LTP key from the worker:
    `railway ssh "cd /app && python -m scripts.deribit_ground_truth --count 1000 --max-pages 500"`
    with the key's DERIBIT_CLIENT_ID/DERIBIT_CLIENT_SECRET set per run (67-03 procedure; env only,
    never argv, never tracked files). Capture stdout JSON per account.
  </what-built>
  <how-to-verify>
    1. Run the harness for each of the 3 LTP keys; save the 3 sanitized outputs (merged or as an
       array) to analytics-service/docs/evidence/p70-wave0-deribit-subaccount-probe.json.
    2. Read the evidence and confirm each answer is decidable:
       - A1: settlement_price_stats — is index_price_present ≈ total on type=settlement rows?
         (If index absent but mark present → the conversion source is mark_price; if BOTH absent →
         escalate: the conversion design must change and 70-02 is blocked.)
       - A2: do sub_1/sub_2 scopes return real trade/txn counts (vs 0 rows or permission errors)?
       - A3: trade_cashflow_stats for inverse_perpetual — is cashflow_nonzero == 0 on trade rows
         (settlement is the sole inverse cash carrier) or > 0 (partition must exclude one side)?
    3. HONESTY-ANCHOR DETERMINATION (the plan-checker BLOCKER): reconcile BOTH streams per account
       against 18,778 / 21,014 / 61,248 — total_trades_all_scopes (trades endpoint) AND
       total_txn_trade_rows_all_scopes (txn-log type=trade). RECORD which stream matches the known
       totals. Pitfall 5 (live-observed: 0 vs 650/860 for accts 1&2) says the trades endpoint
       under-returns, so the two streams can diverge. The stream that matches is the LOCKED honesty
       anchor for the D-02 gate — and because csv_daily_returns realized comes from the txn-log
       (D-08), the dailies-driving stream 70-06 must gate is the txn-log type=trade count. If BOTH
       streams reconcile to the known totals, state that explicitly (70-03/70-06 still gate the
       txn-log stream). Write the determination as a one-sentence LOCKED design pin.
    4. EQUITY ANCHOR: record the per-account USD equity from the evidence `account_equity` block —
       this is the live value 70-06's equity-anchor (and its revert-proof shape test) rely on.
    5. Update analytics-service/docs/deribit-ground-truth.md with a new "## Phase 70 Wave-0
       recorded answers (A1/A2/A3 + honesty anchor + equity)" section: each answer stated in one
       sentence, its evidence excerpt, and status RECORDED — including which stream is the honesty
       anchor and the per-account equity. These sentences are the LOCKED design pins for
       70-02/70-03/70-04/70-06.
    6. If A2 shows subaccount history UNREACHABLE for read-only keys: STOP. This is a phase blocker
       (the D-02 count gate can never reconcile) — surface to the user before any Wave-1 execution;
       do not ship a <5% track record.
  </how-to-verify>
  <resume-signal>Type "approved: A1=<answer> A2=<answer> A3=<answer> anchor=<trades|txnlog|both> equity=<per-account>" (or describe the blocker) after the docs + evidence are committed</resume-signal>
  <done>docs/deribit-ground-truth.md carries RECORDED A1/A2/A3 answers + the honesty-anchor stream determination + per-account equity, backed by committed sanitized evidence; both-stream per-account totals vs known counts recorded; downstream plans have their design pins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → Deribit API | authed calls with founder read-only creds; error strings can echo signatures |
| harness stdout → tracked git artifact | evidence crosses from live-secret context into the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-70-01 | Information Disclosure | harness stdout / evidence JSON | mitigate | existing sanitize_evidence + assert_sanitized + _redact_secret_values pipeline stays the sole stdout path; "id" stays in _MASK_KEYS; creds via Railway env only, never argv/tracked files; account_equity captured numeric-only |
| T-70-02 | Tampering (integrity) | per-scope capture / honesty anchor | mitigate | per-scope errors recorded (never swallowed) so a permission failure cannot masquerade as "no activity"; BOTH stream counts (trades endpoint + txn-log type=trade) reconciled vs known totals so the D-02 gate anchors to whichever stream matches (Pitfall 5 divergence made explicit) |
| T-70-SC | Tampering | package installs | accept | zero new packages this phase (RESEARCH §Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- cd analytics-service && python -m pytest tests/test_deribit_ground_truth.py -q (CI-runnable, synthetic)
- Live: 3× railway ssh harness runs recorded (NOT CI-runnable; checkpoint evidence)
- grep -c "RECORDED" analytics-service/docs/deribit-ground-truth.md increases by ≥3 (A1/A2/A3), plus the honesty-anchor + equity pins
</verification>

<success_criteria>
- A1/A2/A3 each answered with numeric live evidence and written as one-sentence design pins
- BOTH stream counts reconciled per-account against 18,778 / 21,014 / 61,248; the honesty-anchor stream RECORDED as a LOCKED pin (per Pitfall 5)
- Per-account USD equity recorded (the 70-06 anchor value)
- Evidence committed sanitized; harness unit tests green; checkpoint approved (or blocker surfaced)
</success_criteria>

<output>
Create `.planning/phases/70-trades-ingestion-dailies-risky/70-01-SUMMARY.md` when done —
MUST restate the three recorded answers verbatim AND the honesty-anchor determination (which stream
matched the known totals) + per-account equity (downstream plans read them from the SUMMARY + docs).
</output>
