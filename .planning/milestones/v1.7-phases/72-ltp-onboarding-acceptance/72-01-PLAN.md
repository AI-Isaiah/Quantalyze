# P72-01 PLAN — Wire Deribit onboarding (5 bisectable commits)

Blueprint by opus code-architect (verified against source). Corrects the canary diagnosis:
the 422 is `flow_type="resync"` (via `/api/keys/sync` from SyncPreviewStep), not teaser;
finalize uses `onboard` (same whitelist gap). THIRD blocker found: the shared strategy gate +
SyncPreviewStep read the `trades` table (Deribit writes `csv_daily_returns`) and
`isCsvSourced` requires `!apiKeyId` → a keyed Deribit strategy hits INSUFFICIENT_TRADES.

## Commits

1. **Whitelist** — `analytics-service/routers/process_key.py:130-136`: add `"deribit"` to the
   `onboard` and `resync` sets ONLY (not teaser/internal_report/csv — those run the synchronous
   fill-based pipeline that raises for deribit). Update H-11 comment. Preserves anti-DoS intent.

2. **Ledger routing** — `analytics-service/services/ingestion/long_fetch.py`
   `run_process_key_long_job`: `is_ledger_backed = source == "deribit"`. Guard the fill steps
   (skip `fetch_raw`+`compute_metrics` → `metrics_captured` metadata `{}`; skip
   `compute_fingerprint`+persist and `reconstruct_positions`). Keep the `encrypted` +
   `published` transitions. Swap the tail enqueue: for deribit enqueue `derive_broker_dailies`
   (strategy-mode, `p_strategy_id`) instead of `sync_trades` — the exact analogue of the perp
   tail. `validate()` scope-gate + credential resolution already cover deribit.

3. **Fail-loud stamp** — `analytics-service/services/job_worker.py` deribit permanent-FAIL
   branches (~1876-1884 ledger-incomplete/unenumerable/scope; ~1896-1905 material-equity-empty):
   upsert `strategy_analytics {computation_status:'failed', computation_error:<scrubbed>,
   data_quality_flags:{csv_source:True}}` before the `return DispatchResult(FAILED, permanent)`,
   mirroring the existing `<2-days` branch (1966-1991) so the wizard poller reaches a gate
   instead of spinning. (Belt-and-suspenders vs the migration-038 bridge; ship regardless.)

4. **Shared gate** — `src/lib/strategyGate.ts:102-103`: broaden `isCsvSourced` from
   `!apiKeyId && tradeCount===0 && csvRowCount>0` to `tradeCount===0 && csvRowCount>0` (rename
   → `isDailyReturnsSourced`). `NO_DATA_SOURCE` guard (line 88, still `!apiKeyId`) unchanged, so
   keyed strategies always have a source; perp with trades stays on the trade branch (no
   regression). Mirror the same predicate in `src/app/api/admin/strategy-review/route.ts:204-207`.

5. **Wizard step** — `src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx`: add a
   `csv_daily_returns` count query to the terminal `Promise.all` (~380-431); pass `csvRowCount`
   into `checkStrategyGate` (~441-452); gate the "N trades detected" copy on `tradeCount>0` else
   render a series/days line. No new states/routes — reuses the existing resync→202→poll→
   FactsheetPreview machinery + the "up to 3 minutes, draft saved" async UX.

## Revert-proof tests (A-F)
- A `test_process_key.py`: deribit ACCEPTED for resync/onboard; REJECTED for teaser/internal_report/csv; H-11 otherwise intact.
- B `test_long_fetch.py`: deribit run enqueues `derive_broker_dailies` (not `sync_trades`), reaches `published`, never calls `compute_metrics`/`fetch_raw`.
- C `test_broker_dailies.py`: deribit ledger-incomplete → stamps `strategy_analytics.computation_status='failed'` + FAILED/permanent.
- D `strategyGate.test.ts`: keyed+tradeCount0+csvRows30+complete → passed; csvRows3 → INSUFFICIENT_CSV_HISTORY; keyed+trades3+csv0 → INSUFFICIENT_TRADES (perp guard).
- E `SyncPreviewStep.render.test.tsx`: deribit complete + 0 trades + ≥7 csv rows → reaches passed + FactsheetPreview.
- F `admin/strategy-review/route.test.ts`: approve deribit (0 trades, ≥7 csv, complete) → 200.

## Risks (from architect)
1. migration-038 `sync_strategy_analytics_status` may/may-not emit 'failed' for a failed derive job → ship Commit 3 regardless.
2. **Biggest dependency:** P70 ledger must return COMPLETE data for the live LTP accounts (memory single-scope finding says each key sees its own subaccount fully). If under-fetch → `assert_ledger_complete` fails loud → wizard GATE_ANALYTICS_FAILED (loud, not silently-wrong). The canary re-run verifies end-to-end.
3. Handler timeout: `derive_broker_dailies` = 15min ceiling; the 61,248 account at ~1req/s may approach it → transient retry. Confirm largest account completes or raise ceiling.
4. Fingerprint deferred (deribit skips fills) → not in similarity matching until follow-up. Accept for P72.
5. Next.js caveat low-risk (commits 4-5 touch client Supabase queries + a pure module).
