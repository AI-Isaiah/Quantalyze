---
phase: 69-onboarding-ux-wizard-card-setup-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx
  - src/app/(marketing)/security/page.tsx
  - src/app/(marketing)/security/page.test.tsx
  - src/lib/closed-sets.ts
  - src/lib/closed-sets.test.ts
  - src/lib/utils.ts
  - src/components/landing/VerificationForm.tsx
  - src/components/landing/__tests__/VerificationSection.test.tsx
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "The wizard shows a Deribit exchange card; selecting it shows a 'Client ID' and a 'Client Secret' labeled field and NO passphrase field (SC-1)"
    - "/security#deribit-readonly renders a Deribit setup guide whose scope checklist literally names account:read and steers away from Trade/Withdraw/:read_write grants (SC-2)"
    - "Every UI_EXCHANGE_CODES consumer (VerificationForm dropdown, RequestIntroButton chips, marketing exchange count) renders Deribit with a real display label — no undefined/blank option, no 'unsupported exchange' fallback (SC-3)"
    - "FUNDING_EXCHANGES stays 3-value — the flip does NOT leak Deribit into funding/reconcile cron surfaces (D-08, Phase-70 gate intact)"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      provides: "deribit entry in the local EXCHANGES array + credentialLabels rendering"
      contains: 'id: "deribit"'
    - path: "src/app/(marketing)/security/page.tsx"
      provides: "SubAnchor id=deribit-readonly with the account:read scope checklist"
      contains: "deribit-readonly"
    - path: "src/lib/closed-sets.ts"
      provides: "4-value UI_EXCHANGE_CODES (conscious Phase-69 flip)"
      contains: '"deribit"'
    - path: "src/components/landing/VerificationForm.tsx"
      provides: "dropdown labels sourced from EXCHANGE_DISPLAY (no local drift map)"
      contains: "EXCHANGE_DISPLAY"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx"
      to: "/security#deribit-readonly"
      via: "generic deep-link href={`/security#${exchange}-readonly`}"
      pattern: "-readonly"
    - from: "src/components/landing/VerificationForm.tsx"
      to: "src/lib/closed-sets.ts"
      via: "EXCHANGE_DISPLAY lookup keyed by UI_EXCHANGE_CODES"
      pattern: "EXCHANGE_DISPLAY\\["
    - from: "src/lib/closed-sets.test.ts"
      to: "src/lib/closed-sets.ts"
      via: "inverted gate pin — UI_EXCHANGE_CODES CONTAINS deribit, FUNDING_EXCHANGES still excludes it"
      pattern: 'toContain\\("deribit"\\)'
---

<objective>
Ship the conscious Phase-69 "offer Deribit to users" flip on the presentation layer: (1) a Deribit exchange card in the strategy wizard with Client ID / Client Secret credential labels and NO passphrase field (UX-01), (2) a `/security#deribit-readonly` setup guide whose scope checklist names `account:read` (UX-02), and (3) the `UI_EXCHANGE_CODES` widening with every consumer rendering a real "Deribit" label and the Phase-68 gate-pin tests inverted (not deleted) into post-flip regression guards.

Purpose: Phase 68 wired the key-save boundary to accept Deribit; this phase makes a strategy manager able to self-serve Deribit onboarding. Presentation-only — the server route, encryption path, and DRB-03 scope gate are untouched.
Output: Deribit wizard card + security-guide SubAnchor + 4-value UI_EXCHANGE_CODES, all revert-proofed by extended vitest/RTL suites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/69-onboarding-ux-wizard-card-setup-guide/69-CONTEXT.md
@.planning/phases/69-onboarding-ux-wizard-card-setup-guide/69-RESEARCH.md
@.planning/phases/69-onboarding-ux-wizard-card-setup-guide/69-UI-SPEC.md
@DESIGN.md
@analytics-service/docs/deribit-ground-truth.md

<interfaces>
<!-- Key contracts, extracted from live code 2026-07-04. Use directly — no exploration needed. -->

From src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx (lines 48-74):
```typescript
type ExchangeId = SupportedExchange;
interface ExchangeOption {
  id: ExchangeId;
  name: string;
  caption: string;
  requiresPassphrase: boolean;
}
const EXCHANGES: ExchangeOption[] = [
  { id: "binance", name: "Binance", caption: "Spot + USDⓈ-M Futures + COIN-M Futures supported.", requiresPassphrase: false },
  { id: "okx", name: "OKX", caption: "Spot + Perpetuals. Passphrase required.", requiresPassphrase: true },
  { id: "bybit", name: "Bybit", caption: "Spot + Linear + Inverse supported.", requiresPassphrase: false },
];
```
Render loop (`:225-244`) auto-derives `data-testid={`wizard-exchange-${ex.id}`}` and `aria-pressed`. Deep-link (`:247-256`): `href={`/security#${exchange}-readonly`}` with link text `{EXCHANGES.find((e) => e.id === exchange)?.name} setup guide →`. Key field is an `<Input label="API Key" placeholder="Paste the read-only key" …/>` (`:259-266`). Secret field is a RAW `<input id="wizard-api-secret" placeholder="Paste the secret">` with a SIBLING `<label htmlFor="wizard-api-secret">API Secret</label>` plus a Show/Hide toggle (`:268-294`) — NOT an `<Input>` component. Passphrase block is gated by `requiresPassphrase` (`:296-312`); submit payload sends `passphrase: requiresPassphrase ? passphrase : null` (`:144`).

From src/lib/closed-sets.ts (lines 39-100):
```typescript
export const SUPPORTED_EXCHANGES = ["binance", "okx", "bybit", "deribit"] as const;
export const EXCHANGE_DISPLAY = { binance: "Binance", okx: "OKX", bybit: "Bybit", deribit: "Deribit" } as const satisfies Record<SupportedExchange, string>;
export const UI_EXCHANGE_CODES = ["binance", "okx", "bybit"] as const satisfies readonly SupportedExchange[]; // ← THE Phase-69 flip target
export const FUNDING_EXCHANGES = ["binance", "okx", "bybit"] as const satisfies readonly SupportedExchange[]; // ← DO NOT TOUCH (Phase 70)
export const EXCHANGES: readonly ExchangeDisplay[] = UI_EXCHANGE_CODES.map((code) => EXCHANGE_DISPLAY[code]);
```

From src/components/landing/VerificationForm.tsx (lines 7-24):
```typescript
import { UI_EXCHANGE_CODES } from "@/lib/utils";
const EXCHANGE_LABELS: Record<string, string> = { binance: "Binance", okx: "OKX", bybit: "Bybit" }; // ← DRIFT: missing deribit → undefined label after the flip (Gap 1)
const EXCHANGE_OPTIONS = UI_EXCHANGE_CODES.map((value) => ({ value, label: EXCHANGE_LABELS[value] }));
```
Passphrase input is gated on `exchange === "okx"` (`:104`) and the payload spread on okx (`:49`) — already correct for Deribit, no change there. NOTE: `@/lib/utils` re-exports UI_EXCHANGE_CODES but NOT EXCHANGE_DISPLAY — import EXCHANGE_DISPLAY from `@/lib/closed-sets`.

From src/app/(marketing)/security/page.tsx (lines 593-614):
```typescript
function SubAnchor({ id, title, children }: { id: string; title: string; children: React.ReactNode }) // renders <div id={id} className="border-t border-border pt-3"> + <h4> self-link
```
Existing readonly-key blocks: `<SubAnchor id="binance-readonly">` (:420), `okx-readonly` (:438), `bybit-readonly` (:454-466). Each contains `<ol className="mt-2 list-decimal space-y-1 pl-5 text-body text-text-secondary">` with `<strong>` on the scope token.

From src/lib/closed-sets.test.ts (lines 31-74):
- `:31-37` pin `expect(EXCHANGES).toEqual(["Binance", "OKX", "Bybit"])` — UPDATE to 4-value.
- `:39-48` pin `expect(UI_EXCHANGE_CODES).toEqual(["binance","okx","bybit"])` + `includes("deribit")).toBe(false)` — INVERT for UI_EXCHANGE_CODES; the two `FUNDING_EXCHANGES` assertions in the same block (`:45`, `:47`) STAY unchanged.
- `:58-74` chip-surface source-scan guard (CHIP_SURFACES must import UI_EXCHANGE_CODES, never SUPPORTED_EXCHANGES) — DO NOT weaken; stays green by construction.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deribit wizard card — EXCHANGES entry + credentialLabels (UX-01, D-01/D-02/D-03)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx</files>
  <read_first>src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx (full file — EXCHANGES array :55-74, requiresPassphrase derivation :122-123, submit payload :144, render loop :220-256, key Input :259-266, RAW secret field :268-294, passphrase conditional :296-312), src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx (existing render/mock harness), 69-UI-SPEC.md §Copywriting Contract + §Surface A</read_first>
  <behavior>
    - Test 1 (card renders): `getByTestId("wizard-exchange-deribit")` resolves; the card shows name "Deribit" and caption "Spot + Inverse Perpetuals + Options supported."
    - Test 2 (no passphrase, with failing-contrast): after clicking the deribit card, `queryByLabelText(/passphrase/i)` is null; contrast half — after clicking the okx card, `getByLabelText(/passphrase/i)` resolves (proves the null assertion can fail).
    - Test 3 (credential labels): after selecting deribit, `getByLabelText("Client ID")` and `getByLabelText("Client Secret")` both resolve, and `getByPlaceholderText("Paste the Deribit Client ID")` + `getByPlaceholderText("Paste the Deribit Client Secret")` resolve; default/binance state still shows `getByLabelText("API Key")` + `getByLabelText("API Secret")` with placeholders "Paste the read-only key" / "Paste the secret" (revert-proof for the label wiring, both directions).
    - Test 4 (setup-guide deep-link): after selecting deribit, the link with accessible name matching /Deribit setup guide/ has `href` equal to "/security#deribit-readonly".
    - Wizard-array revert-proof (D-09): Tests 1/3/4 all derive from the single `EXCHANGES` deribit entry — removing that entry turns all of them red.
  </behavior>
  <action>
    In ConnectKeyStep.tsx, per D-01: append a fourth entry to the local EXCHANGES array with id "deribit", name "Deribit", caption exactly "Spot + Inverse Perpetuals + Options supported.", requiresPassphrase false. The render loop auto-derives data-testid "wizard-exchange-deribit", aria-pressed, and the "#deribit-readonly" deep-link — write NO bespoke card JSX.

    Per D-03: extend the ExchangeOption interface with an optional credentialLabels field of shape { key: string; secret: string } and an optional credentialPlaceholders field of the same shape (labels and placeholders are per-exchange, presentational only; the submit payload keys api_key/api_secret and the generic storage columns are UNCHANGED). Set both on the deribit entry only: labels { key: "Client ID", secret: "Client Secret" }, placeholders { key: "Paste the Deribit Client ID", secret: "Paste the Deribit Client Secret" }. Inside the component, derive the active option once (EXCHANGES.find on the current exchange) and resolve labels with defaults "API Key"/"API Secret" and placeholders "Paste the read-only key"/"Paste the secret".

    Apply the resolved strings to TWO DIFFERENT edit shapes (RESEARCH Gap 5): (a) the key field is an Input component — swap its label prop (:260) and placeholder prop (:263); (b) the secret field is a RAW input with a sibling label — swap the label TEXT NODE at :274 and the placeholder attribute at :289. Do NOT convert the raw secret input to an Input component and do NOT touch the Show/Hide toggle, the wizard-api-secret id, or the htmlFor association (A11Y-4 depends on it).

    Do NOT add a passphrase branch for deribit (D-02 — requiresPassphrase false already hides the field and nulls the payload). Do NOT change the grid class mt-2 grid grid-cols-1 gap-2 md:grid-cols-3 (UI-SPEC: Deribit wraps to row 2 on desktop; reflow is out of scope). Do NOT touch the OKX passphrase copy.

    In ConnectKeyStep.test.tsx, add a new describe block "Phase 69 — Deribit wizard card (UX-01)" implementing the four behaviors above, reusing the file's existing render/fetch-mock harness. Write the tests FIRST, confirm they fail against the unmodified component, then implement.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - EXCHANGES array in ConnectKeyStep.tsx has exactly 4 entries; the deribit entry is `{ id: "deribit", name: "Deribit", caption: "Spot + Inverse Perpetuals + Options supported.", requiresPassphrase: false }` plus the credentialLabels/credentialPlaceholders values pinned above.
    - Rendered deribit state: labels "Client ID" / "Client Secret", placeholders "Paste the Deribit Client ID" / "Paste the Deribit Client Secret", zero passphrase field, deep-link href "/security#deribit-readonly".
    - Rendered binance/bybit/okx states are byte-identical to before (labels "API Key"/"API Secret", placeholders "Paste the read-only key"/"Paste the secret"; OKX still shows the passphrase field).
    - All four new tests fail when the deribit EXCHANGES entry (or the label wiring) is removed — verified by writing them RED first.
    - No change to handleSubmit payload shape, the create-with-key route, or any server file (D-04: the OKX-only passphrase gate stays as-is).
    - `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx"` exits 0 with the new describe block included.
  </acceptance_criteria>
  <done>Deribit card + Client ID/Client Secret labels render in the wizard with no passphrase field; the extended ConnectKeyStep suite is green and revert-proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /security#deribit-readonly setup guide SubAnchor (UX-02, D-05/D-06)</name>
  <files>src/app/(marketing)/security/page.tsx, src/app/(marketing)/security/page.test.tsx</files>
  <read_first>src/app/(marketing)/security/page.tsx (Section id="readonly-key" :411-468, the binance/okx/bybit SubAnchor blocks :420-466, SubAnchor helper :593-614), src/app/(marketing)/security/page.test.tsx (existing render pattern + the anchor-preservation pin :110-130 — it lists top-level Section ids only, a new SubAnchor is additive and must NOT trip it), 69-UI-SPEC.md §Copywriting Contract (pinned guide steps) + §Surface B, analytics-service/docs/deribit-ground-truth.md (scope string ground truth)</read_first>
  <behavior>
    - Test 1 (anchor renders): after rendering SecurityPage, `document.getElementById("deribit-readonly")` is not null and the block's heading text is "Deribit".
    - Test 2 (scope checklist, SC-2 hard requirement): `within(deribit-readonly block).getByText(/account:read/)` resolves — the literal token `account:read` appears in the block.
    - Test 3 (steer-away wording): the block's text contains the granting-phrased negatives "Do not enable Trade or Withdraw" and "do not grant any :read_write scope". IMPORTANT (UI-SPEC test trap): assert on the GRANTING phrasing, never on the absence of bare tokens Trade/Withdraw/:read_write — those tokens legitimately appear in steer-away language here and in the sibling blocks.
    - Revert-proof: deleting the SubAnchor turns Test 1 red; dropping account:read from the copy turns Test 2 red.
  </behavior>
  <action>
    In security/page.tsx, per D-05/D-06: insert one new SubAnchor block with id "deribit-readonly" and title "Deribit" inside the existing Section id "readonly-key", immediately after the closing bybit SubAnchor (~line 466), inside the same `mt-4 space-y-4` wrapper. Mirror the sibling structure exactly: an ol with className "mt-2 list-decimal space-y-1 pl-5 text-body text-text-secondary" containing exactly three li steps with the UI-SPEC pinned copy:
    Step 1: "Go to Deribit Account → API and add a new API key. Deribit issues an OAuth-style credential — a Client ID and Client Secret, with no passphrase."
    Step 2: "Grant read-only scopes only. Enable account:read (and, where offered, trade:read and wallet:read). Do not enable Trade or Withdraw, and do not grant any :read_write scope." — wrap the account:read token in a strong element (emphasis by weight, matching the Enable Reading/Read/Read-Only precedent; NO color, WCAG 1.4.1).
    Step 3: "Copy the Client ID and Client Secret into the wizard."

    Use the existing SubAnchor helper — write NO new helper, NO new classes, NO new tokens (UI-SPEC reuse contract). Do NOT edit the binance/okx/bybit blocks, the Section helper, or the page's stale 3-exchange header comment at :11 (Gap 4 — flagged, out of SC scope, Rule 3 surgical).

    In security/page.test.tsx, add a new describe block "Phase 69 — Deribit readonly setup guide (UX-02)" implementing the three behaviors above (render SecurityPage, scope queries with within() on the deribit-readonly element). Do NOT add "deribit-readonly" to the existing anchor-preservation expected[] array — that pin is for pre-existing ids; the new dedicated describe block IS the deribit revert-proof. Write the tests RED first, then add the SubAnchor.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(marketing)/security/page.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - security/page.tsx contains exactly one element with id "deribit-readonly", rendered via the existing SubAnchor helper, positioned after the bybit-readonly block inside Section id "readonly-key".
    - The block's ordered list has 3 steps matching the UI-SPEC pinned copy; the literal token `account:read` is present and strong-wrapped; trade:read and wallet:read are named; the negatives use granting phrasing ("Do not enable Trade or Withdraw", "do not grant any :read_write scope").
    - The pre-existing anchor-preservation test still passes unmodified (additive change only).
    - New tests fail when the SubAnchor is deleted or account:read is dropped from the copy (verified RED first).
    - `npx vitest run "src/app/(marketing)/security/page.test.tsx"` exits 0.
  </acceptance_criteria>
  <done>/security#deribit-readonly renders a 3-step Deribit guide naming account:read; the extended page suite is green and revert-proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: UI_EXCHANGE_CODES flip + VerificationForm label fix + gate-pin inversion (SC-3, D-07/D-08, Gaps 1-3) — single commit</name>
  <files>src/lib/closed-sets.ts, src/lib/closed-sets.test.ts, src/lib/utils.ts, src/components/landing/VerificationForm.tsx, src/components/landing/__tests__/VerificationSection.test.tsx</files>
  <read_first>src/lib/closed-sets.ts (:56-100 — UI_EXCHANGE_CODES + FUNDING_EXCHANGES + EXCHANGES derivation and their doc comments), src/lib/closed-sets.test.ts (:31-74 — the two Phase-68 gate-pin tests + the chip-surface source-scan guard), src/components/landing/VerificationForm.tsx (:7-27 local EXCHANGE_LABELS, :49 okx payload spread, :104 okx passphrase gate), src/components/landing/__tests__/VerificationSection.test.tsx (render harness), src/components/strategy/RequestIntroButton.tsx (:21-27 — verify-only, NO edit), src/lib/utils.ts (:150-165 re-export comment)</read_first>
  <behavior>
    - Inverted pin a (SC-3-a): `expect(UI_EXCHANGE_CODES).toEqual(["binance", "okx", "bybit", "deribit"])` and `expect((UI_EXCHANGE_CODES as readonly string[]).includes("deribit")).toBe(true)` — the exact inverse of the Phase-68 pin, red if the flip is reverted.
    - Kept pin (D-08 / Phase-70 gate): `expect(FUNDING_EXCHANGES).toEqual(["binance", "okx", "bybit"])` and its `includes("deribit")).toBe(false)` assertion stay byte-identical.
    - Updated display pin: `expect(EXCHANGES).toEqual(["Binance", "OKX", "Bybit", "Deribit"])`.
    - Dropdown label test (SC-3-b, Gap 1 revert-proof): rendering the verification form yields `getByRole("option", { name: "Deribit" })`, AND every option has non-empty trimmed textContent (catches the silent undefined-label regression); selecting deribit shows NO passphrase input (`queryByLabelText(/passphrase/i)` null) while selecting okx shows one (contrast).
    - Chip-surface source-scan guard (:58-74) stays green with zero edits (SC-3-c).
  </behavior>
  <action>
    ALL edits in this task land in ONE commit (RESEARCH Gap 3: the flip and its pin inversions must be atomic, or CI is red between commits).

    In closed-sets.ts, per D-07: add "deribit" as the fourth member of UI_EXCHANGE_CODES. Do NOT touch FUNDING_EXCHANGES, SUPPORTED_EXCHANGES, or EXCHANGE_DISPLAY (deribit label already present). Update the now-stale doc comments in the same file: the UI_EXCHANGE_CODES comment ("a user is not OFFERED deribit until Phase 69" → flipped in Phase 69; still decoupled from SUPPORTED_EXCHANGES, still never derived) and the EXCHANGES derivation comment ("keeps the marketing count at 3" → the count follows the UI-offered set, 4 as of Phase 69). In utils.ts, correct the one stale re-export comment line describing UI_EXCHANGE_CODES/FUNDING_EXCHANGES as "the DECOUPLED 3-value consts" (UI is now 4-value, funding stays 3-value) — comment-only, no export change.

    In closed-sets.test.ts, per Gap 3: INVERT, never delete. Update the :31-37 EXCHANGES display pin to the 4-value array and retitle it as the post-flip pin. Restructure the :39-48 test so the UI_EXCHANGE_CODES assertions expect the 4-value array / includes-deribit true, while the two FUNDING_EXCHANGES assertions stay byte-identical (3-value, excludes deribit) — update the test title and comments to say the Phase-69 flip is DONE and the remaining exclusion is the Phase-70 funding gate. Leave the chip-surface guard (:58-74) and everything else untouched — do NOT weaken it (it is SC-3-c). The FUNDING_EXCHANGES 3-value pin in check-zod-db-check-parity.test.ts needs no change and must stay green (leak backstop).

    In VerificationForm.tsx, per Gap 1 (recommended fix — single source of truth): DELETE the local EXCHANGE_LABELS record and build EXCHANGE_OPTIONS as UI_EXCHANGE_CODES.map with label EXCHANGE_DISPLAY[value], importing EXCHANGE_DISPLAY from "@/lib/closed-sets" (NOT from "@/lib/utils" — utils does not re-export it; and NEVER import SUPPORTED_EXCHANGES — the source-scan guard fails on that import). Keep the existing UI_EXCHANGE_CODES import from "@/lib/utils" as-is. Update the OQ4 comment above EXCHANGE_OPTIONS to reflect the Phase-69 flip and the drift-proof label source. NO change to the okx-only passphrase gate (:104) or payload spread (:49) — Deribit correctly gets no passphrase, mirroring the wizard.

    In VerificationSection.test.tsx, add a describe block "Phase 69 — Deribit offered on the public verify dropdown (SC-3)" implementing the dropdown-label behaviors above using the file's existing render harness. Write it RED first (against the unflipped const), then apply the flip + fix.

    Verify-only (NO edit, Gap 2): confirm RequestIntroButton.tsx auto-widens — its EXCHANGE_OPTIONS derives capitalize(e) from UI_EXCHANGE_CODES, so "deribit" renders "Deribit" with zero change; its OQ4 comment remains accurate (it explains WHY the decoupled const is used). Record this confirmation in the SUMMARY. Likewise the marketing count at src/app/(marketing)/page.tsx (:115, :215) auto-renders 4 via the EXCHANGES derivation — no edit, no test hardcodes "3 exchanges" (grep-verified in RESEARCH).

    Out of scope, flag-only (Gap 4, Rule 3): the stale "Binance, OKX, or Bybit" prose in for-quants/page.tsx:317, AllocatorExchangeManager.tsx:684, wizardErrors.ts:143, and security/page.tsx:11 does NOT derive from UI_EXCHANGE_CODES and is NOT part of UX-01/UX-02/SC-3 — leave untouched and list in the SUMMARY as a follow-up candidate.
  </action>
  <verify>
    <automated>npx vitest run src/lib/closed-sets.test.ts src/components/landing/__tests__/VerificationSection.test.tsx src/__tests__/contracts/check-zod-db-check-parity.test.ts --no-file-parallelism</automated>
  </verify>
  <acceptance_criteria>
    - closed-sets.ts UI_EXCHANGE_CODES equals ["binance","okx","bybit","deribit"]; FUNDING_EXCHANGES, SUPPORTED_EXCHANGES, EXCHANGE_DISPLAY byte-identical to before.
    - closed-sets.test.ts asserts UI_EXCHANGE_CODES CONTAINS deribit and EXCHANGES equals the 4-value display array (inverted pins, not deletions); the FUNDING_EXCHANGES 3-value/excludes-deribit assertions and the :58-74 chip-surface guard are unmodified and green.
    - VerificationForm.tsx contains no local EXCHANGE_LABELS record; labels come from EXCHANGE_DISPLAY; the file imports UI_EXCHANGE_CODES (guard requirement) and does NOT import SUPPORTED_EXCHANGES.
    - The verify dropdown renders an option with accessible name "Deribit"; no option has empty text; deribit shows no passphrase input (okx contrast proves the assertion can fail).
    - check-zod-db-check-parity.test.ts passes unmodified (funding surfaces still exclude deribit — D-08 leak backstop).
    - All edits in this task are in a single commit; `npx vitest run src/lib/closed-sets.test.ts src/components/landing/__tests__/VerificationSection.test.tsx src/__tests__/contracts/check-zod-db-check-parity.test.ts --no-file-parallelism` exits 0.
  </acceptance_criteria>
  <done>UI_EXCHANGE_CODES is consciously 4-value with inverted (not deleted) gate pins, every consumer renders a real "Deribit" label, and the funding/cron gates remain 3-value and pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentation-only phase. The client→API key-save boundary (create-with-key / verify-strategy) was wired and hardened in Phase 68 (isSupportedExchange allowlist, DRB-03 scope gate, AES-256-GCM envelope encryption) and is NOT touched here — Phase 69 only exposes it in the UI and adds docs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Information Disclosure / Elevation of Privilege (user-granted over-scope) | /security#deribit-readonly guide copy | mitigate | Doc-integrity: the guide MUST steer users to read-only scopes — literal `account:read` (+ trade:read/wallet:read) and explicit "do not enable Trade or Withdraw / any :read_write" phrasing, grounded in deribit-ground-truth.md. This reinforces (never replaces) the server-side DRB-03 scope gate. Pinned by the SC-2 test asserting `account:read` presence. |
| T-69-02 | Tampering (scope-gate bypass via UI drift) | UI_EXCHANGE_CODES consumers | mitigate | Chip-surface source-scan guard (closed-sets.test.ts:58-74) stays intact: user-facing surfaces must bind UI_EXCHANGE_CODES, never SUPPORTED_EXCHANGES; funding/cron exclusion pins (FUNDING_EXCHANGES + check-zod-db-check-parity) stay 3-value. |
| T-69-SC | Tampering (supply chain) | npm installs | accept (N/A) | Zero package installs this phase (RESEARCH Package Legitimacy Audit: N/A). All work uses in-repo modules. |
</threat_model>

<verification>
Phase-level checks after all tasks:
1. Targeted suites green: `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.test.tsx" "src/app/(marketing)/security/page.test.tsx" src/lib/closed-sets.test.ts src/components/landing/__tests__/VerificationSection.test.tsx src/__tests__/contracts/check-zod-db-check-parity.test.ts --no-file-parallelism`
2. Full frontend suite: `npm run test` (coverage ratchet: lines 82 / stmt 80 / fn 74 / br 72 — all new code paths are test-covered by construction).
3. `npx tsc --noEmit` and `npm run lint` clean (react-hooks + no-raw-font-px gates; no raw px introduced — all classes reused verbatim per UI-SPEC).
4. Grep gates: `grep -c '"deribit"' src/lib/closed-sets.ts` counts ≥ 3 (SUPPORTED + DISPLAY key is unquoted-key so: UI_EXCHANGE_CODES member present); `grep -n 'EXCHANGE_LABELS' src/components/landing/VerificationForm.tsx` returns nothing; `grep -n 'id="deribit-readonly"\|deribit-readonly' "src/app/(marketing)/security/page.tsx"` hits.
5. VALIDATION.md per-task map statuses flipped to green.
</verification>

<success_criteria>
- SC-1 (UX-01): Wizard shows the Deribit card; selecting it renders "Client ID" + "Client Secret" labeled fields, deribit placeholders, and NO passphrase field; deep-link resolves to /security#deribit-readonly. Proven by the extended ConnectKeyStep suite (revert-proof: all assertions derive from the single EXCHANGES entry + label wiring).
- SC-2 (UX-02): /security#deribit-readonly renders a 3-step guide whose scope checklist literally includes `account:read` and steers away from write grants. Proven by the extended security page suite.
- SC-3: UI_EXCHANGE_CODES is 4-value; VerificationForm renders a real "Deribit" option label (no undefined/blank), RequestIntroButton and the marketing count auto-widen (verified, no edit), no "unsupported exchange" fallback on any consumer; FUNDING_EXCHANGES stays 3-value. Proven by the inverted closed-sets pins + the new dropdown-label test + the untouched chip-surface guard and parity backstop.
- No server files, no migrations, no package installs, no new design primitives.
</success_criteria>

<output>
Create `.planning/phases/69-onboarding-ux-wizard-card-setup-guide/69-01-SUMMARY.md` when done (include the Gap 2 auto-widen confirmation and the Gap 4 stale-prose flag list).
</output>
