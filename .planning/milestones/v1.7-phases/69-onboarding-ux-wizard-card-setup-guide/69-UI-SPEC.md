---
phase: 69
slug: onboarding-ux-wizard-card-setup-guide
status: approved
reviewed_at: 2026-07-04
shadcn_initialized: false
preset: none
created: 2026-07-04
---

# Phase 69 — UI Design Contract

> Visual and interaction contract for the Deribit onboarding surfaces. Generated by gsd-ui-researcher, verified by gsd-ui-checker.
>
> **Nature of this phase: REUSE-ONLY.** No new visual design, no new component, no new token. Two additions bolt onto existing primitives verbatim: (1) one more exchange card in the wizard's existing `EXCHANGES.map` card grid, (2) one more `<SubAnchor>` in the existing `/security#readonly-key` guide. Every token below cites the live component + DESIGN.md — nothing is invented. The contract's job is to pin the exact reuse surface, the Deribit-specific COPY, and the interaction/a11y invariants the reuse must preserve.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (established in-repo design system — hand-rolled `src/components/ui/*`, no shadcn / no `components.json`) |
| Preset | not applicable |
| Component library | none (bespoke primitives: `Input`, `Button`, plus local `Section`/`SubAnchor` helpers in `security/page.tsx`) |
| Icon library | none used on these surfaces (text-only "→" affordance on links) |
| Font | DM Sans (body/UI/labels), Instrument Serif (`font-display`, guide headings), Geist Mono (data — not used here). Fluid `--text-*` clamp spine per DESIGN.md v1.4 Phase 49. |

**shadcn gate outcome:** NOT initialized and deliberately not prompted. The project has a mature DESIGN.md-governed system across 60+ phases; CONTEXT.md locks "reuse existing components verbatim, zero new visual design." Initializing shadcn would violate CLAUDE.md Rule 3 (surgical changes) and DESIGN.md conformance. Registry safety gate: not applicable.

---

## Reuse Contract (component inventory — the load-bearing section)

### Surface A — Wizard exchange card (`ConnectKeyStep.tsx`, UX-01)

| Element | Reuse target | Exact classes / props (verbatim from live code) |
|---------|-------------|--------------------------------------------------|
| Card button | `EXCHANGES.map` render loop `:225–244` | `<button type="button">` · `rounded-md border px-4 py-3 text-left transition-colors` · inactive `border-border bg-white hover:border-accent/50` · active `border-accent bg-accent/5` |
| Card name | `:240` | `text-body font-semibold text-text-primary` |
| Card caption | `:241` | `mt-1 text-micro text-text-secondary` |
| Grid | `:224` | `mt-2 grid grid-cols-1 gap-2 md:grid-cols-3` — Deribit becomes the 4th cell; grid stays `md:grid-cols-3` (Deribit wraps to row 2 on desktop). **Do NOT change the grid column count** unless the planner explicitly decides on a 2×2 / 4-col reflow — out of scope here. |
| Key field | `Input` primitive `:259–266` | `<Input label={credentialLabels.key} placeholder=… />` |
| Secret field | raw `<input>` + sibling `<label>` `:268–294` | label text node `:274`, `<input class="mt-1 w-full rounded-md border border-border bg-white px-3 py-2 text-body text-text-primary placeholder:text-text-muted focus:border-accent focus:outline-none">`. **Not an `<Input>` — the label swap targets the text node at `:274`, the placeholder at `:289`** (Gap 5). |
| Passphrase field | conditional `:296–312` | Gated by `requiresPassphrase`. Deribit = `false` → **field does NOT render** (SC-1). No new code. |
| Setup-guide deep-link | `:248–255` | `href={/security#${exchange}-readonly}` — generic, resolves to `#deribit-readonly` for free (D-02/D-06). |

### Surface B — Security setup guide (`security/page.tsx`, UX-02)

| Element | Reuse target | Exact classes (verbatim) |
|---------|-------------|--------------------------|
| Block wrapper | `SubAnchor` helper `:593–614` | `<div id="deribit-readonly" className="border-t border-border pt-3">` |
| Block title | `SubAnchor` `<h4>` `:604` | `font-display text-body text-text-primary` (Instrument Serif) |
| Ordered list | mirror binance/okx/bybit `:421,439,455` | `mt-2 list-decimal space-y-1 pl-5 text-body text-text-secondary` |
| Emphasis | mirror existing `<strong>` scope callouts | `<strong>` on the scope token (no color; emphasis carried by weight, matching the "Enable Reading"/"Read"/"Read-Only" precedent) |
| Insertion point | inside `<Section id="readonly-key">`, after the bybit `SubAnchor` `~:466` | — |

---

## Spacing Scale

Reused verbatim from DESIGN.md (4px base ladder: 2·4·8·12·16·24·32·48·64). This phase authors no new spacing.

| Token | Value | Usage on these surfaces |
|-------|-------|-------------------------|
| sm | 8px | Card grid gap (`gap-2`) |
| — | 12px | Card vertical padding (`py-3`), SubAnchor top padding (`pt-3`) |
| md | 16px | Card horizontal padding (`px-4`) |

Exceptions: none. (`--space-grid-gap: 10px` is not used on either surface.)

---

## Typography

All roles reuse the fluid `--text-*` spine (DESIGN.md v1.4 Phase 49). No raw px — the `no-raw-font-px` repo-wide `error` lint applies.

| Role | Tier / Size | Weight | Where |
|------|-------------|--------|-------|
| Card name | `text-body` (14→16px) | semibold (600) | Deribit card name "Deribit" |
| Card caption | `text-micro` (10→11px) | regular (400) | Deribit card caption |
| Credential label | `text-caption` (12→13px) | medium (500) | "Client ID" (`Input` label) / "Client Secret" (`<label>`) |
| Guide title | `text-body` (14→16px) Instrument Serif (`font-display`) | regular (400) | SubAnchor "Deribit" |
| Guide steps | `text-body` (14→16px) | regular (400), `<strong>` = 700 on scope token | ordered-list `<li>` prose |

> ⚠ **Pre-existing inconsistency to preserve, NOT fix (Rule 3):** DESIGN.md §"Broker Selector Grid" specifies the card caption as "12px DM Sans regular", but the live cards render `text-micro` (10→11px). Deribit's caption MUST match its **siblings** (`text-micro`) for row consistency, not the stale DESIGN.md line. Do not "correct" the binance/okx/bybit captions in this phase — flag only.

---

## Color

Reused verbatim from DESIGN.md. No new color, no destructive action on either surface.

| Role | Value | Usage on these surfaces |
|------|-------|-------------------------|
| Dominant (60%) | `#F8F9FA` page / `#FFFFFF` surface | Wizard page bg; inactive card `bg-white` |
| Secondary (30%) | `#E2E8F0` border, `#4A5568` text-secondary, `#64748B` text-muted | Card/SubAnchor hairline borders, caption + guide body text |
| Accent (10%) | `#1B6B5A` | Active card border + `bg-accent/5` tint; focused input border; guide title/link `hover:text-accent` |
| Destructive | `#DC2626` | not used this phase |

Accent reserved for: the **selected** Deribit card (border + 5% teal fill), the **focused** credential input border, and the guide anchor-link hover — nothing else. The `account:read` scope emphasis is carried by `<strong>` weight, **not** color (consistency with existing scope callouts + WCAG 1.4.1).

---

## Copywriting Contract

New copy this phase introduces (Claude's discretion per CONTEXT, grounded in `deribit-ground-truth.md` + Deribit console terminology):

| Element | Copy (pinned) |
|---------|---------------|
| Card name | `Deribit` |
| Card caption | `Spot + Inverse Perpetuals + Options supported.` (matches sibling "… supported." pattern; ground-truth confirms options + inverse perps + spot) |
| Credential label — key | `Client ID` (default is "API Key"; Deribit override) |
| Credential label — secret | `Client Secret` (default is "API Secret"; Deribit override) |
| Placeholder — key | `Paste the Deribit Client ID` |
| Placeholder — secret | `Paste the Deribit Client Secret` |
| Guide title | `Deribit` |
| Guide step 1 | `Go to Deribit Account → API and add a new API key. Deribit issues an OAuth-style credential — a Client ID and Client Secret, with no passphrase.` |
| Guide step 2 | `Grant read-only scopes only. Enable ` **`account:read`** ` (and, where offered, trade:read and wallet:read). Do not enable Trade or Withdraw, and do not grant any :read_write scope.` |
| Guide step 3 | `Copy the Client ID and Client Secret into the wizard.` |

**SC-2 hard requirement:** the guide block MUST literally contain the token `account:read` (revert-proof asserted by `security/page.test.tsx`).

Unchanged copy (this phase adds NO new instances):
| Element | Status |
|---------|--------|
| Primary CTA | `Validate key and continue` — existing submit button, unchanged. Phase adds no new CTA. |
| Empty state | N/A — neither surface renders a data/empty state. |
| Error state | Reuses `ErrorEnvelope` + `wizardErrors.ts` verbatim. No new error code or copy. |
| Destructive confirmation | none — no destructive action in this phase. |

**Steer-away wording safety (test trap):** the negative-scope phrasing mirrors the live OKX block's "Do not enable Trade or Withdraw." Any SC-2 test asserting "no write-granting" must match on *granting* phrasing, not the bare tokens `Trade`/`Withdraw`/`:read_write` — those tokens legitimately appear in steer-away language here and in the existing binance/okx/bybit blocks.

---

## Interaction & Accessibility Invariants (the reuse MUST preserve)

| # | Invariant | Source |
|---|-----------|--------|
| A11Y-1 | Card carries `data-testid="wizard-exchange-deribit"` (auto-derived from `id`) | `:238` |
| A11Y-2 | Card carries `aria-pressed={active}` — Deribit toggles it like every sibling | `:237` |
| A11Y-3 | Selecting Deribit renders **NO** passphrase field (`requiresPassphrase:false`); OKX-contrast proves the assertion can fail | `:296` |
| A11Y-4 | Credential labels are `<label htmlFor>`-associated (key via `Input`, secret via `:270–275`) so `getByLabelText("Client ID"/"Client Secret")` resolves | `:259,270` |
| A11Y-5 | Setup-guide link resolves to `#deribit-readonly` (anchor convention `#{exchange}-readonly`) | `:249` |
| A11Y-6 | SubAnchor uses `id="deribit-readonly"` and the anchored `<a href="#deribit-readonly">` self-link | `:603–606` |
| A11Y-7 | Inline body-prose links keep persistent underline (`underline underline-offset-4`) per DESIGN.md 2026-06-28 WCAG 1.4.1 rule — the wizard "setup guide" link uses `underline-offset-4 hover:underline` (it is a labelled affordance, not bare prose, so hover-underline is acceptable and unchanged) | DESIGN.md Decisions Log |
| A11Y-8 | `/strategies/new/wizard` is under axe-core CI (`wcag2a + wcag2aa + best-practice`, zero violations). The added card must not introduce violations — it reuses the passing card markup verbatim, so this holds by construction. | DESIGN.md §9-State Matrix |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| none | none | not applicable — no shadcn, no third-party registries, zero package installs |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
