---
phase: 67-deribit-live-harness-exchange-ground-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/scripts/deribit_ground_truth.py
  - analytics-service/tests/test_deribit_ground_truth.py
  - analytics-service/docs/deribit-ground-truth.md
  - analytics-service/docs/evidence/.gitkeep
autonomous: true
requirements: [DRB-01]

must_haves:
  truths:
    - "A committed harness script exists that can authenticate to Deribit read-only, enumerate currencies, and capture trades + transaction-log rows with full pagination"
    - "The read-only scope gate rejects a write-scoped key BEFORE any data fetch (proven by a unit test)"
    - "Evidence sanitization provably strips secrets and masks account identifiers (proven by a unit test)"
    - "A tracked answers-doc template exists at analytics-service/docs/deribit-ground-truth.md with sections for the 3 mandated answers"
  artifacts:
    - path: "analytics-service/scripts/deribit_ground_truth.py"
      provides: "DRB-01 harness: scope gate, per-currency trades + txn-log capture, instrument classification, sanitized JSON stdout"
      min_lines: 150
    - path: "analytics-service/tests/test_deribit_ground_truth.py"
      provides: "Pure-logic unit tests: scope gate, txn-log type summary, instrument classification, sanitization"
      min_lines: 80
    - path: "analytics-service/docs/deribit-ground-truth.md"
      provides: "Tracked answers-doc template (3 answer sections, each with an evidence-excerpt placeholder)"
      contains: "Funding-netting shape"
  key_links:
    - from: "analytics-service/scripts/deribit_ground_truth.py"
      to: "services/exchange.py"
      via: "create_exchange / aclose_exchange imports"
      pattern: "from services\\.exchange import"
    - from: "analytics-service/scripts/deribit_ground_truth.py"
      to: "services/redact.py"
      via: "scrub_freeform_string / truncate_account_id imports"
      pattern: "from services\\.redact import"
    - from: "analytics-service/tests/test_deribit_ground_truth.py"
      to: "analytics-service/scripts/deribit_ground_truth.py"
      via: "pure-function imports"
      pattern: "from scripts\\.deribit_ground_truth import"
---

<objective>
Build the DRB-01 Deribit ground-truth harness script and its pure-logic unit tests, plus the tracked answers-doc template. The script is a committed one-off (locked decision: `analytics-service/scripts/`, run later via `railway ssh "cd /app && python -m scripts.deribit_ground_truth"` — the live run is Plan 67-03, orchestrator-only). It captures raw transaction-log rows verbatim (whitelisted fields) so the funding-netting question is ANSWERED by data, not assumed (RESEARCH Pitfall 1).

Purpose: DRB-04..07 (Phase 70) design against this harness's recorded answers; the script also becomes the Phase 70 fixture generator.
Output: `scripts/deribit_ground_truth.py`, `tests/test_deribit_ground_truth.py`, `docs/deribit-ground-truth.md` template, `docs/evidence/` dir (tracked).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-CONTEXT.md
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-PATTERNS.md

<interfaces>
Verified seams (from 67-PATTERNS.md, introspected this session):

From analytics-service/services/exchange.py:
- `create_exchange(name, api_key, api_secret, passphrase=None)` (line 792) — HMAC `deri-hmac-sha256` auth for Deribit; "deribit" is in EXCHANGE_CLASSES
- `aclose_exchange(ex)` (line 830) — await in a `finally` (avoids "Unclosed connector")

From analytics-service/services/redact.py:
- `scrub_freeform_string(s)` (line 186) — strips `&signature=<HMAC>` etc. from ccxt error strings
- `truncate_account_id(s)` (line 174) — masks ids to `***<last4>`

From analytics-service/services/geo_block.py:
- `_GEO_BLOCK_MARKERS` (line 34), `is_geo_blocked(...)` (line 55) — signature-based classifier

ccxt 4.5.59 raw methods (verified installed): `public_get_auth`, `public_get_get_currencies`, `private_get_get_account_summary`, `private_get_get_user_trades_by_currency_and_time` (paginate via `result.has_more`), `private_get_get_transaction_log` (paginate via `result.continuation`), `private_get_get_subaccounts`. `fetchLedger`/`fetchFundingHistory` are `None` for Deribit — the transaction log IS the funding source.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure-logic helpers for scope gate, txn-log summary, instrument mix, and sanitization — tests first</name>
  <files>analytics-service/tests/test_deribit_ground_truth.py, analytics-service/scripts/deribit_ground_truth.py</files>
  <read_first>
    - analytics-service/tests/test_reconciliation.py (test structure analog: docstring encoding WHY each regression gate matters, pure-fn tests over constructed dicts, no mocking)
    - analytics-service/services/redact.py (scrub_freeform_string, truncate_account_id — reuse, do not reimplement masking)
    - analytics-service/scripts/probe_exchange_egress.py (module docstring WHY/USAGE/RUNBOOK shape, `from __future__ import annotations`, exit-code contract)
    - .planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md (Pitfall 1 whitelisted txn-log fields; Pitfall 3 scope source; Code Examples for exact endpoint param names)
  </read_first>
  <behavior>
    - Test scope_gate_rejects_write: `scope_is_read_only("account:read trade:read_write wallet:read")` is False; `scope_is_read_only("trade:read account:read wallet:read custody:read block_trade:read")` is True (this exact string is the observed grounding fact); a scope with zero `:read`-suffixed tokens is False (auth must have proven at least one read grant)
    - Test txnlog_type_summary: `summarize_txn_log(rows)` over a constructed fixture containing rows with type values "trade", "settlement", "deposit" returns distinct type -> count mapping AND one sample row per type containing ONLY the whitelisted fields (type, amount, balance, equity, cashflow, instrument_name, side, position, timestamp, currency) — username/user_id/email never appear in samples
    - Test instrument_classification: `classify_instrument("BTC-PERPETUAL")` == "inverse_perpetual"; `classify_instrument("BTC_USDC-PERPETUAL")` == "linear_perpetual"; `classify_instrument("BTC-27MAR26-60000-C")` == "option"; `classify_instrument("BTC-27MAR26")` == "future"; unknown patterns -> "unknown" (never raises)
    - Test evidence_is_masked: `sanitize_evidence(obj)` on a nested dict/list containing keys `username`, `user_id`, `email`, `access_token`, `refresh_token`, `api_key`, any key containing "secret" or "token" -> token/secret-valued keys are REMOVED entirely; username/user_id/email/id-like values are masked via truncate_account_id; a value containing "&signature=deadbeef" is scrubbed via scrub_freeform_string
    - Test assert_sanitized: `assert_sanitized(obj)` raises ValueError naming the offending key path when given a dict containing an `access_token` key or an unmasked email-shaped value; passes silently on the sanitized fixture
  </behavior>
  <action>
    RED first: write analytics-service/tests/test_deribit_ground_truth.py with the five test groups above importing `from scripts.deribit_ground_truth import scope_is_read_only, summarize_txn_log, classify_instrument, sanitize_evidence, assert_sanitized` (pytest.ini has `pythonpath = .` and scripts/__init__.py exists — the import works). Run it, confirm failure (module absent).

    GREEN: create analytics-service/scripts/deribit_ground_truth.py with the module docstring (WHY / USAGE showing `railway ssh "cd /app && python -m scripts.deribit_ground_truth"` / RUNBOOK, modeled on probe_exchange_egress.py) and the five pure functions. Scope rule per RESEARCH Pitfall 3: reject if any whitespace-split token ends with `:read_write`; require at least one token ending with `:read`. Whitelist for txn-log samples is exactly the RESEARCH Pitfall 1 field set. sanitize_evidence recurses dicts/lists; the deny-key predicate is case-insensitive substring match on {"secret", "token", "api_key", "apikey", "password"}; mask-key set is {"username", "user_id", "email", "system_name", "id"} where the value is a string (mask via truncate_account_id); every remaining string value passes through scrub_freeform_string. assert_sanitized re-walks the structure and raises ValueError with the JSON-pointer-ish path on any deny-key or on a value matching an email regex or a 40+ char base64/hex-looking token — this function is reused by Plans 67-03/67-04 to verify REAL artifacts before commit. All functions fully type-annotated (project B-mypy standard), stdlib + services.redact only — no ccxt import needed at this layer so tests stay I/O-free.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_ground_truth.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_deribit_ground_truth.py -q` green with >= 5 test functions collected
    - `scope_is_read_only("trade:read account:read wallet:read custody:read block_trade:read")` returns True and the write-scoped case returns False (asserted in tests)
    - Test file docstring states WHY each gate matters (funding-netting evidence integrity, key-scope safety, secret-leak prevention) — Rule 9
    - No network/ccxt import required to run the tests (pure functions only at this layer)
    - `grep -c "from services.redact import" analytics-service/scripts/deribit_ground_truth.py` >= 1 (masking reused, not reimplemented)
  </acceptance_criteria>
  <done>Five pure functions implemented and unit-tested; tests fail without the module (RED commit) and pass with it (GREEN commit).</done>
</task>

<task type="auto">
  <name>Task 2: Async harness main — scope gate, per-currency full-pagination capture, geo-block observation, sanitized JSON stdout</name>
  <files>analytics-service/scripts/deribit_ground_truth.py</files>
  <read_first>
    - analytics-service/services/exchange.py lines 780-840 (create_exchange / aclose_exchange exact signatures and Deribit registration)
    - analytics-service/services/geo_block.py (marker tuple + is_geo_blocked shape — the harness only OBSERVES; it does not modify this file)
    - analytics-service/scripts/probe_exchange_egress.py lines 86-114 (main() exit-code contract; egress-country capture via ipinfo)
    - .planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md Code Examples (exact raw-endpoint params: grant_type/client_id/client_secret for public_get_auth; currency/kind/start_timestamp/end_timestamp/count/include_old/sorting for user trades; currency/start_timestamp/end_timestamp/count/continuation for transaction log)
  </read_first>
  <action>
    Extend scripts/deribit_ground_truth.py with `async def run() -> dict` and `def main() -> int` (argparse: `--start-ms` default 1420070400000 i.e. 2015-01-01 for full history, `--end-ms` default now, `--max-pages` default 500 safety cap, `--count` default 1000). main() wraps asyncio.run, prints ONE sanitized JSON object to stdout (json.dumps indent=2 after sanitize_evidence + assert_sanitized), returns exit codes: 0 success, 2 FAIL-LOUD scope violation, 3 missing env vars, 1 any other failure.

    Flow, in order (per D: harness fails loudly if scopes exceed read-only, BEFORE any data fetch):
    1. Read DERIBIT_CLIENT_ID / DERIBIT_CLIENT_SECRET from env (exit 3 with a clear message if absent — never print values).
    2. `create_exchange("deribit", client_id, client_secret)`; in a try/finally with `await aclose_exchange(ex)`.
    3. Scope gate: `public_get_auth({"grant_type": "client_credentials", "client_id": ..., "client_secret": ...})` -> `result.scope`; if not scope_is_read_only(scope): print the FAIL-LOUD message WITH the scope string (scopes are not secrets) and exit 2 without any further call. Record the verbatim scope string in the evidence (Phase 68 DRB-02 designs against it).
    4. Currency enumeration (RESEARCH Pitfall 2 — never hard-code): `public_get_get_currencies` list intersected with currencies where `private_get_get_account_summary({"currency": ccy})` succeeds with nonzero equity OR balance; record the full enumeration result.
    5. Per currency: (a) trades via `private_get_get_user_trades_by_currency_and_time` with kind="any", include_old="true", sorting="asc", count=<count>; follow `result.has_more` by advancing start_timestamp past the last trade's timestamp until has_more is false or --max-pages hit (record pages_used and a `max_pages_hit` boolean — Phase 70 verifies counts against 18,778 / 21,014 / 61,248); keep TOTAL count + instrument-mix tally via classify_instrument + up to 3 sample trades per instrument kind. (b) transaction log via `private_get_get_transaction_log`, following `result.continuation` until null or --max-pages; feed ALL rows through summarize_txn_log incrementally (distinct `type` -> count + one whitelisted sample per type) — THE phase question falls out of this summary.
    6. `private_get_get_subaccounts({"with_portfolio": "true"})` — record ONLY the count of subaccounts and whether the key sees any (masked; bonus Phase 72 observation, non-blocking; wrap in try/except — a scope-denied call here is recorded as "not permitted", not a failure).
    7. Geo-block observation: wrap every raw call's exception handler to check the HTTP body via is_geo_blocked semantics; since the worker egresses Amsterdam a block is NOT expected — on zero blocks record geo_block_observation = {"blocked": false, "note": "no block observed from Amsterdam egress — marker deferred to observed-on-block; classifier is the fail-safe"}. Never fabricate a marker (locked decision). If a block IS observed, record status + the marker substring candidate from the body (scrubbed).
    8. Evidence object keys: run_meta (utc timestamp, ccxt version, egress country via ipinfo like the probe script, args), scope, currencies, per_currency (trade_count, pages_used, max_pages_hit, txn_log_type_summary), instrument_mix (per-kind counts + samples), subaccounts_observation, geo_block_observation. Every exception string logged passes through scrub_freeform_string (ccxt embeds &signature= in error URLs).

    Keep full type annotations. Do NOT touch services/geo_block.py in this plan (marker addition happens in 67-03 only if a real block body is observed).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_ground_truth.py -x -q && .venv/bin/python -c "import scripts.deribit_ground_truth as m; import inspect; assert inspect.iscoroutinefunction(m.run); assert m.main.__annotations__.get('return') is int or str(m.main.__annotations__.get('return'))=='int'" && { .venv/bin/python -c "import mypy" 2>/dev/null || .venv/bin/pip install -q -r requirements-dev.txt; } && .venv/bin/python -m mypy --strict --follow-imports=silent scripts/deribit_ground_truth.py</automated>
  </verify>
  <acceptance_criteria>
    - Running `python -m scripts.deribit_ground_truth` locally WITHOUT env vars exits with code 3 and a message naming DERIBIT_CLIENT_ID/DERIBIT_CLIENT_SECRET, never printing any value (verify: `cd analytics-service && env -u DERIBIT_CLIENT_ID -u DERIBIT_CLIENT_SECRET .venv/bin/python -m scripts.deribit_ground_truth; test $? -eq 3`)
    - Scope gate call (`public_get_auth`) appears in source BEFORE any `private_get_` call in the run() flow; scope violation path raises/exits 2 with zero subsequent private calls (source-order assertion: the only private_get_* names before the scope check are none)
    - Trades pagination follows `has_more`; txn-log pagination follows `continuation`; both bounded by --max-pages with a recorded `max_pages_hit` flag (grep: `has_more` and `continuation` each appear in the script)
    - stdout JSON passes assert_sanitized before printing (the call is in main(), not optional)
    - mypy --strict clean on the script (project B-mypy standard, even though the CI gate scopes services/routers/models)
  </acceptance_criteria>
  <done>Harness main is runnable end-to-end (env-gated), fully paginated, sanitized-by-construction, with fail-loud exit codes; unit tests still green.</done>
</task>

<task type="auto">
  <name>Task 3: Tracked docs scaffold + full-suite gate</name>
  <files>analytics-service/docs/deribit-ground-truth.md, analytics-service/docs/evidence/.gitkeep</files>
  <read_first>
    - .planning/phases/67-deribit-live-harness-exchange-ground-truth/67-CONTEXT.md (locked: answers TRACKED in-repo, 3 mandated answers each WITH raw evidence excerpt; NOT in gitignored .planning/)
    - analytics-service/.gitignore and repo-root .gitignore (verify docs/ paths are not ignored — `git check-ignore` already confirmed clean, re-verify after creation)
  </read_first>
  <action>
    Create analytics-service/docs/deribit-ground-truth.md as the ANSWERS TEMPLATE (populated by Plan 67-03 after the live run). Structure: header stating provenance ("Recorded from a live read-only LTP key via scripts/deribit_ground_truth.py run on the Railway worker; Phases 68/70 design against this file"), then three H2 sections each with an "Answer:" placeholder, an "Evidence:" fenced-JSON placeholder, and a "Status: PENDING LIVE RUN (Plan 67-03)" line:
    1. "## Funding-netting shape" — netted into realized PnL vs separate transaction-log rows; evidence = distinct txn-log `type` values with counts + per-type sample rows.
    2. "## Instrument mix (inverse / linear / options)" — per-kind counts for the LTP account; evidence = instrument_mix tally.
    3. "## Geo-block body marker" — marker string for geo_block.py, OR the honest "not observable from Amsterdam egress" record (locked: do NOT fabricate).
    Plus a "## Bonus observations (non-blocking)" section for the LTP subaccount-structure note (Phase 72) and per-currency trade counts (Phase 70 pagination verification against 18,778 / 21,014 / 61,248), and a "## Run metadata" section.

    Create analytics-service/docs/evidence/.gitkeep so the evidence dir exists tracked. Verify neither path is gitignored: `git check-ignore analytics-service/docs/deribit-ground-truth.md analytics-service/docs/evidence/.gitkeep` must exit non-zero. Then run the full analytics suite to prove the plan is regression-free.
  </action>
  <verify>
    <automated>cd analytics-service && git check-ignore docs/deribit-ground-truth.md docs/evidence/.gitkeep; test $? -ne 0 && .venv/bin/python -m pytest -q --cov=services --cov=routers --cov=main_worker --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - `git check-ignore` exits non-zero for both docs paths (tracked, not ignored)
    - docs/deribit-ground-truth.md contains exactly the three mandated answer H2 sections plus bonus + run-metadata sections; every answer section contains the literal string "PENDING LIVE RUN"
    - Full pytest suite green with --cov-fail-under=80 (scripts/ is outside the coverage denominator by CI construction — no ratchet regression)
  </acceptance_criteria>
  <done>Tracked docs scaffold exists; full suite + coverage gate green; plan leaves the repo shippable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker -> Deribit API | live credentials cross here (HMAC-signed requests); responses are untrusted input |
| script stdout -> tracked git artifact | sanitization boundary — anything printed can end up committed |
| env vars -> script | DERIBIT_CLIENT_ID/SECRET read from Railway env only, never files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Information Disclosure | evidence JSON / stdout | mitigate | sanitize_evidence + assert_sanitized run BEFORE print, unit-tested (Task 1); scrub_freeform_string on every exception (ccxt embeds &signature=); deny-keys removed, ids masked via truncate_account_id |
| T-67-02 | Elevation of Privilege | Deribit key scope | mitigate | fail-loud read-only gate via public_get_auth result.scope BEFORE any private call; exit 2, zero subsequent fetches; unit test proves rejection of :read_write |
| T-67-03 | Information Disclosure | secrets in repo | mitigate | key material env-only (exit 3 if absent, values never printed); pre-existing gitleaks scan; docs/evidence committed only via assert_sanitized-verified artifacts |
| T-67-04 | Tampering | untrusted exchange responses | mitigate | whitelisted-field capture only (Pitfall 1 field set); no eval/exec; JSON-only handling |
| T-67-05 | Denial of Service | unbounded pagination | mitigate | --max-pages cap (default 500) with recorded max_pages_hit flag |
| T-67-SC | Tampering | npm/pip installs | accept (n/a) | phase adds ZERO dependencies (RESEARCH Package Legitimacy Audit: none added); any install task is out of scope |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_deribit_ground_truth.py -q` green
- `cd analytics-service && { .venv/bin/python -c "import mypy" 2>/dev/null || .venv/bin/pip install -q -r requirements-dev.txt; } && .venv/bin/python -m mypy --strict --follow-imports=silent scripts/deribit_ground_truth.py` clean
- Full suite + coverage gate green (Task 3)
- No git branch operations performed (work stays on the current feature branch; orchestrator owns branching)
</verification>

<success_criteria>
- Harness script committed, importable, env-gated, fully paginated, sanitized-by-construction with fail-loud exit codes
- All Wave 0 DRB-01 pure-logic tests from 67-VALIDATION.md exist and pass (scope gate, txn-log summary, masking)
- Tracked answers-doc template + evidence dir exist and are not gitignored
</success_criteria>

<output>
Create `.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-01-SUMMARY.md` when done.
</output>
