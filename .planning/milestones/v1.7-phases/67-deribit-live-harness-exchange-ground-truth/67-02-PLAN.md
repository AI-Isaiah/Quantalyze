---
phase: 67-deribit-live-harness-exchange-ground-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/scripts/bybit_reconcile.py
  - analytics-service/tests/test_bybit_reconcile.py
autonomous: true
requirements: [BYB-01]

must_haves:
  truths:
    - "A committed reconciliation script composes the EXACT production seams (diff_strategy_fills, fetch_raw_trades, fetch_funding_bybit, combine_realized_and_funding) so the run exercises the #563 code paths"
    - "Fills reconcile by native execId; funding reconciles by match_key bucket / per-day sum — NEVER native-id equality on funding (Bybit rotates transaction ids)"
    - "Dailies compare is a unit-tested pure function asserting abs(delta) < 1e-9 per overlapping date"
    - "The script is read-only against prod: no writes to reconciliation_reports / strategy_analytics / any table"
  artifacts:
    - path: "analytics-service/scripts/bybit_reconcile.py"
      provides: "BYB-01 reconciliation harness: fills diff + funding bucket diff + dailies recompute-vs-stored, sanitized JSON report, exit code encodes verdict"
      min_lines: 150
    - path: "analytics-service/tests/test_bybit_reconcile.py"
      provides: "Pure-logic unit tests: dailies-within-1e-9, funding bucket summary, fills-diff wiring (clean set -> clean status), report sanitization"
      min_lines: 80
  key_links:
    - from: "analytics-service/scripts/bybit_reconcile.py"
      to: "services/reconciliation.py"
      via: "diff_strategy_fills import"
      pattern: "from services\\.reconciliation import"
    - from: "analytics-service/scripts/bybit_reconcile.py"
      to: "services/encryption.py"
      via: "decrypt_credentials(key_row, get_kek())"
      pattern: "decrypt_credentials"
    - from: "analytics-service/scripts/bybit_reconcile.py"
      to: "services/broker_dailies.py"
      via: "combine_realized_and_funding import"
      pattern: "combine_realized_and_funding"
    - from: "analytics-service/scripts/bybit_reconcile.py"
      to: "services/db.py"
      via: "get_supabase (SUPABASE_SERVICE_KEY on worker)"
      pattern: "get_supabase"
---

<objective>
Build the BYB-01 Bybit ground-truth reconciliation script and its pure-logic unit tests. The script proves Bybit ingestion correct end-to-end against exchange truth for one live key: fresh exchange fills vs DB `trades` by native execId (two-stage `diff_strategy_fills`), fresh funding vs DB `funding_fees` by match_key bucket, and per-key realized+funding dailies recomputed vs stored `csv_daily_returns` within 1e-9. Runs later via `railway ssh` (Plan 67-04, orchestrator-only). Independent of the Deribit key gate — no dependency on Plans 67-01/67-03.

Purpose: fix any Bybit correctness bug before Phase 70 stacks Deribit onto the shared dailies path; re-check the #563 under-fetch class with the now-clean Amsterdam egress.
Output: `scripts/bybit_reconcile.py`, `tests/test_bybit_reconcile.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-CONTEXT.md
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-PATTERNS.md

<interfaces>
Verified seams (from 67-PATTERNS.md, file:line read this session):

From analytics-service/services/reconciliation.py:
- `diff_strategy_fills(strategy_id, date_range, exchange_fills, db_fills)` (line 216) — Stage 1 PRIMARY exact match on `(exchange, exchange_fill_id)`; Stage 2 SECONDARY tuple match emitting `id_drift` (INFORMATIONAL — Bybit rotates order ids) vs true discrepancy; returns report with .status/.discrepancies

From analytics-service/services/encryption.py:
- `get_kek()` (line 14), `decrypt_credentials(key_row, kek) -> (api_key, api_secret, passphrase|None)` (line 92) — fails loud (InvalidToken naming key id) on NULL columns

From analytics-service/services/exchange.py:
- `create_exchange("bybit", api_key, api_secret, passphrase)` (line 792) — read-only keys already get fetchCurrencies disabled
- `fetch_raw_trades(ex, strategy_id, supabase, since_ms)` (line 1737) — Bybit branch is `_fetch_raw_trades_bybit(exchange, effective_since)` (line 2251): cursor PAGE_CAP=100, stuck-cursor guard; DQ flags go to an IN-MEMORY ContextVar buffer only (verified: `_record_dq_flag` writes no DB) — read after via `get_and_clear_last_dq_flags` and record `sync_truncated_bybit` in the report
- `fetch_all_trades(ex, symbol=None, since_ms=None)` (line 2519) — realized daily-pnl records, funding-EXCLUDED (C-0319)
- `fetch_account_equity_usd(ex, "bybit") -> (equity, balance_error)` (line 2668)

From analytics-service/services/funding_fetch.py:
- `fetch_funding_bybit(ex, strategy_id, since_ms)` (line 602) — 7-day window walk, linear+inverse fan-out, MAX_PAGES=200
- `_build_match_key(...)` (line 233), `_FUNDING_BUCKET_HOURS` (line 197) — the funding dedup axis

From analytics-service/services/broker_dailies.py:
- `combine_realized_and_funding(realized, funding, account_balance, balance_error=False) -> (pd.Series, meta)` (line 119)

From analytics-service/services/db.py:
- `get_supabase() -> Client` (line 71) — requires SUPABASE_URL + SUPABASE_SERVICE_KEY (worker has both)

Call-shape mirrors: services/job_worker.py line 2171 `run_reconcile_strategy_job` (fills half) and line 1716 `run_derive_broker_dailies_job` (dailies half) — mirror the shapes; do NOT write reconciliation_reports/strategy_analytics.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure comparison/report functions — tests first</name>
  <files>analytics-service/tests/test_bybit_reconcile.py, analytics-service/scripts/bybit_reconcile.py</files>
  <read_first>
    - analytics-service/tests/test_reconciliation.py (test structure analog + the `_fill(...)` fixture-builder shape matching exchange._normalize_fill output — reuse that dict shape for diff wiring tests)
    - analytics-service/tests/test_broker_dailies.py (dailies fixture conventions)
    - analytics-service/services/funding_fetch.py lines 190-260 (_FUNDING_BUCKET_HOURS, _build_match_key — the exact bucket key to reuse, never reimplement)
    - analytics-service/services/reconciliation.py lines 216-296 (diff_strategy_fills contract: matched rows removed from both sets; mismatch_quantity/mismatch_price on id-match disagreement; id_drift classification)
  </read_first>
  <behavior>
    - Test dailies_within_1e9: `compare_dailies(recomputed_series, stored_rows, tol=1e-9)` over a constructed pd.Series and stored `[{"date": ..., "daily_return": ...}]` rows: identical overlap -> {"max_abs_delta": < 1e-9, "dates_beyond_tol": [], "overlap_days": N, "clean": True}; a single date perturbed by 1e-6 -> clean False with that date listed; dates present only on one side are EXCLUDED from the tolerance check but counted in "only_in_recomputed"/"only_in_stored" (anchor-to-today caveat: the most-recent day may legitimately move — compare the overlapping historical tail only)
    - Test funding_bucket_summary: `funding_bucket_summary(fresh_rows, db_rows)` compares by match_key bucket set (built via services.funding_fetch._build_match_key) and per-day funding sums — NEVER by transaction id; equal buckets -> clean True; a missing bucket on the DB side -> listed in "missing_in_db"; per-day sum deltas reported
    - Test fills_set_equality: feeding two identical constructed fill lists (the _fill shape from test_reconciliation.py) through `diff_strategy_fills` yields a clean status; the wiring test proves the script's fill-dict construction from DB rows matches the shape diff_strategy_fills expects (exchange, exchange_fill_id, symbol, side, price, quantity, timestamp)
    - Test report_is_sanitized: `build_report(...)` output passes scripts.deribit_ground_truth-independent sanitization: no api_key/secret material, api_key_id masked to ***last4, verdict/exit-code fields present ("clean", "id_drift_only", "discrepancy"); an injected "&signature=deadbeef" string in an error field is scrubbed
  </behavior>
  <action>
    RED first: write analytics-service/tests/test_bybit_reconcile.py importing `from scripts.bybit_reconcile import compare_dailies, funding_bucket_summary, build_report` plus the diff wiring test. Run, confirm failure. Docstring encodes WHY: 1e-9 is the BYB-01 reconciliation definition; funding-by-bucket guards the Bybit id-rotation trap (RESEARCH Pitfall 4); #563 discipline means count deltas are RECORDED, not auto-bugs (Pitfall 5).

    GREEN: create analytics-service/scripts/bybit_reconcile.py with module docstring (WHY / USAGE `railway ssh "cd /app && python -m scripts.bybit_reconcile --api-key-id <uuid> [--window-days 180]"` / RUNBOOK incl. the #563 discipline: a fills-count delta is only a bug if it moves reconciled P&L/dailies beyond 1e-9 OR drops funding rows) and the three pure functions, fully type-annotated. compare_dailies aligns on the intersection of date keys; deltas as floats; clean iff every overlap delta < tol. funding_bucket_summary reuses services.funding_fetch._build_match_key for both sides (import it — private-name import from our own package is acceptable for a committed ops script; note it in a comment). build_report assembles: run_meta (window, timestamps, args), fills section (exchange_count, db_count, count_delta RECORDED EVEN IF ZERO, diff status, id_drift count, true discrepancies, dq_flags incl. sync_truncated_bybit), funding section (bucket diff, per-day sums), dailies section (compare_dailies output), verdict in {"clean", "id_drift_only", "discrepancy"}; masks api_key_id via services.redact.truncate_account_id and scrubs every free-form string via scrub_freeform_string.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_bybit_reconcile.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_bybit_reconcile.py -q` green with >= 4 test functions
    - compare_dailies rejects a 1e-6 perturbation and accepts identical series (asserted); only-overlap dates enter the tolerance check
    - funding_bucket_summary imports _build_match_key from services.funding_fetch (grep confirms) — no reimplemented bucket math
    - build_report output contains the literal count_delta field even when zero, and verdict is one of the three enumerated strings
    - Tests are I/O-free (no network, no DB, no mocking of ccxt)
  </acceptance_criteria>
  <done>Pure comparison layer implemented and unit-tested; RED and GREEN commits made.</done>
</task>

<task type="auto">
  <name>Task 2: Async reconciliation main — decrypt, fetch fresh via production seams, read prod (read-only), report + exit code</name>
  <files>analytics-service/scripts/bybit_reconcile.py</files>
  <read_first>
    - analytics-service/services/job_worker.py around line 2171 (run_reconcile_strategy_job: the exact decrypt -> create_exchange -> fetch_raw_trades -> db_fills load -> diff_strategy_fills sequence to mirror over a FIXED window, minus the reconciliation_reports write)
    - analytics-service/services/job_worker.py around line 1716 (run_derive_broker_dailies_job: the exact fetch_account_equity_usd -> fetch_all_trades -> fetch_funding_bybit -> combine_realized_and_funding sequence; confirm the second positional arg to fetch_funding_bybit at this call site and mirror it)
    - analytics-service/services/db.py lines 60-110 (get_supabase; note _force_http1)
    - analytics-service/services/exchange.py get_and_clear_last_dq_flags (read-and-reset surface for sync_truncated_bybit)
  </read_first>
  <action>
    Extend scripts/bybit_reconcile.py with `async def run(api_key_id, window_days) -> tuple[dict, int]` and `def main() -> int` (argparse: required `--api-key-id`, `--window-days` default 180 — Claude's-discretion window chosen to exercise multi-page cursor pagination on a high-volume key while staying bounded; the window is RECORDED in the report). Exit codes: 0 clean or id_drift_only, 1 true discrepancy (beyond-1e-9 dailies delta, dropped funding bucket, or diff_strategy_fills true discrepancy), 2 usage/env error.

    Flow:
    1. supabase = get_supabase(); load the api_keys row by id (select the columns decrypt_credentials needs plus strategy_id and exchange; exit 2 with a masked id if not found or exchange != "bybit").
    2. decrypt_credentials(key_row, get_kek()) — fails loud on malformed rows (do not catch InvalidToken).
    3. ex = create_exchange("bybit", api_key, api_secret, passphrase); all fetches inside try/finally with await aclose_exchange(ex).
    4. Fills half (mirror run_reconcile_strategy_job over the fixed window): exchange_fills = await fetch_raw_trades(ex, strategy_id, supabase, since_ms=window_start_ms); immediately capture dq_flags = get_and_clear_last_dq_flags() (records sync_truncated_bybit — the #563 signal). db_fills: SELECT exchange, exchange_fill_id, symbol, side, price, quantity, timestamp FROM trades WHERE strategy_id = ? AND is_fill = true AND timestamp >= window_start (paginate the select if needed; read-only). report_fills = diff_strategy_fills(strategy_id=..., date_range=(start, end), exchange_fills=..., db_fills=...). id_drift is informational, not a discrepancy.
    5. Funding half: fresh = await fetch_funding_bybit(ex, <second arg exactly as at the job_worker call site>, window_start_ms); db funding rows: SELECT from funding_fees for this strategy/key over the window; funding_bucket_summary(fresh, db_rows).
    6. Dailies half (mirror run_derive_broker_dailies_job): equity, balance_error = await fetch_account_equity_usd(ex, "bybit"); realized = await fetch_all_trades(ex, since_ms=None); funding_full = await fetch_funding_bybit(ex, <same arg>, None); returns, meta = combine_realized_and_funding(realized, funding_full, account_balance=equity, balance_error=balance_error). stored: SELECT date, daily_return FROM csv_daily_returns WHERE api_key_id = ? (assumption A6 fallback: if zero rows on the api_key_id axis, retry keyed by strategy_id and record which axis was used). compare_dailies over the OVERLAPPING historical tail.
    7. build_report(...) -> print sanitized JSON to stdout; return the verdict-mapped exit code.

    READ-ONLY invariant (locked + threat model): the script performs zero INSERT/UPDATE/DELETE/UPSERT — no reconciliation_reports, no strategy_analytics, no DQ-flag persistence. Recompute is in-memory only. Full type annotations; every logged/reported exception string through scrub_freeform_string.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_bybit_reconcile.py -x -q && { .venv/bin/python -c "import mypy" 2>/dev/null || .venv/bin/pip install -q -r requirements-dev.txt; } && .venv/bin/python -m mypy --strict --follow-imports=silent scripts/bybit_reconcile.py && { grep -nE "\.(insert|update|upsert|delete)\(" scripts/bybit_reconcile.py; test $? -ne 0; } && .venv/bin/python -m pytest -q --cov=services --cov=routers --cov=main_worker --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - `python -m scripts.bybit_reconcile` with no args exits 2 with usage (argparse required --api-key-id); no secrets printed on any path
    - grep for `.insert(|.update(|.upsert(|.delete(` in the script finds ZERO matches (read-only invariant, mechanically enforced)
    - The fills half calls fetch_raw_trades (NOT a hand-rolled execution-list loop) and the dailies half calls combine_realized_and_funding (NOT reimplemented series math) — grep confirms both identifiers
    - dq_flags captured via get_and_clear_last_dq_flags appear in the report (the #563 sync_truncated_bybit signal is observable)
    - mypy --strict clean on the script; full suite + coverage gate green
  </acceptance_criteria>
  <done>Reconciliation main runnable end-to-end (worker env-gated), read-only by construction, verdict-encoded exit code; all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker -> Bybit API | decrypted live credentials cross here; responses untrusted |
| script -> prod Supabase | reconciliation reads (trades, funding_fees, csv_daily_returns, api_keys) — must be READ-ONLY |
| script stdout -> tracked git artifact | sanitization boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-06 | Information Disclosure | decrypted Bybit key | mitigate | decrypt via existing decrypt_credentials only; plaintext never logged/printed/persisted; api_key_id masked via truncate_account_id in the report |
| T-67-07 | Tampering | prod DB mutation from harness | mitigate | zero write calls — mechanically enforced by the grep gate in Task 2 verify (no .insert/.update/.upsert/.delete); recompute in-memory only |
| T-67-08 | Information Disclosure | ccxt error URLs (&signature=HMAC) | mitigate | scrub_freeform_string on every free-form string entering the report |
| T-67-09 | Repudiation | unrecorded reconciliation basis | mitigate | window, axis used (api_key_id vs strategy_id fallback), dq_flags, and count deltas (even if zero) recorded in the report — #563 discipline |
| T-67-10 | Elevation of Privilege | using a write-capable path | accept | the Bybit key is an existing prod read-only ingestion key; the script adds no order/withdraw calls (only fetch seams already in production use) |
| T-67-SC | Tampering | npm/pip installs | accept (n/a) | zero new dependencies this phase (RESEARCH Package Legitimacy Audit: none added) |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_bybit_reconcile.py -q` green
- mypy --strict clean on the script; read-only grep gate passes
- Full suite + coverage (--cov-fail-under=80) green
- No git branch operations performed
</verification>

<success_criteria>
- Reconciliation script committed, composing the exact production seams (diff_strategy_fills / fetch_raw_trades / fetch_funding_bybit / combine_realized_and_funding), read-only by construction
- All Wave 0 BYB-01 pure-logic tests from 67-VALIDATION.md exist and pass (fills diff wiring, dailies-within-1e-9, funding bucket compare, report sanitization)
- BYB-01 live-run path (Plan 67-04) fully unblocked, independent of the Deribit key gate
</success_criteria>

<output>
Create `.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-02-SUMMARY.md` when done.
</output>
