---
phase: 67-deribit-live-harness-exchange-ground-truth
plan: 03
type: execute
wave: 2
depends_on: [67-01]
files_modified:
  - analytics-service/docs/deribit-ground-truth.md
  - analytics-service/docs/evidence/deribit-ground-truth-run.json
autonomous: false
requirements: [DRB-01]

user_setup:
  - service: deribit
    why: "DRB-01 live harness needs ONE read-only LTP Deribit API key (founder-held; Claude cannot create it)"
    env_vars:
      - name: DERIBIT_CLIENT_ID
        source: "Founder: Deribit account -> Settings -> API -> create key with READ-ONLY scopes (account:read trade:read wallet:read; NO :read_write)"
      - name: DERIBIT_CLIENT_SECRET
        source: "Same Deribit API key creation screen (client secret)"
    dashboard_config:
      - task: "Set both env vars on the Railway analytics worker service (triggers a redeploy)"
        location: "Railway dashboard -> analytics service -> Variables (or `railway variables --set`)"

must_haves:
  truths:
    - "An authed harness run from the Railway worker fetched real trades + transaction-log rows for one LTP Deribit key, and the output is committed as a sanitized artifact (ROADMAP SC-1)"
    - "The 3 mandated answers are recorded, each WITH a raw evidence excerpt: funding-netting shape, inverse/linear/options mix, geo-block body marker (or the honest 'not observable from Amsterdam egress' record — never fabricated) (ROADMAP SC-2)"
    - "No secret, unmasked account id, username, or email exists in any committed artifact"
    - "Per-currency trade counts are recorded (Phase 70 pagination verification against 18,778 / 21,014 / 61,248) and the LTP subaccount-structure bonus observation is noted for Phase 72"
  artifacts:
    - path: "analytics-service/docs/deribit-ground-truth.md"
      provides: "The 3 recorded answers with evidence excerpts; zero remaining 'PENDING LIVE RUN' markers"
      contains: "Funding-netting shape"
    - path: "analytics-service/docs/evidence/deribit-ground-truth-run.json"
      provides: "Sanitized full harness output (run_meta, scope, per_currency, instrument_mix, txn_log_type_summary, geo_block_observation)"
  key_links:
    - from: "analytics-service/docs/deribit-ground-truth.md"
      to: "analytics-service/docs/evidence/deribit-ground-truth-run.json"
      via: "each answer cites its evidence excerpt from the artifact"
      pattern: "deribit-ground-truth-run\\.json"
---

<objective>
Execute the DRB-01 live harness run: founder provisions the read-only Deribit LTP key on the Railway worker (checkpoint:human-action), the orchestrator runs `scripts/deribit_ground_truth.py` via `railway ssh`, and the sanitized output becomes the committed evidence artifact + the 3 recorded answers in `docs/deribit-ground-truth.md`. Phases 68 (scope strings, block marker) and 70 (funding-netting, instrument mix, pagination targets) design against this output.

Purpose: answer the two design-driving Deribit unknowns from real worker-side data (THE reason this phase exists).
Output: populated `docs/deribit-ground-truth.md` + `docs/evidence/deribit-ground-truth-run.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-CONTEXT.md
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md
@analytics-service/scripts/deribit_ground_truth.py
@analytics-service/docs/deribit-ground-truth.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Deribit key provisioning + worker deploy verification (founder gate — BLOCKS DRB-01 only; BYB-01/Plan 67-04 proceeds independently)</name>
  <files>none (Railway env vars only — LTP secrets are env/Keychain-only, never tracked files; rotate after onboarding per ONB-02)</files>
  <read_first>
    - .planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md Environment Availability table (the Deribit key is the phase's single human gate)
  </read_first>
  <action>
    Orchestrator-only human-action gate (executor subagents have NO railway auth — never delegate). Sequence: (1) confirm the deployed worker image carries scripts/deribit_ground_truth.py (the module ships via /ship + merge, which the ORCHESTRATOR owns — no git ops inside this plan; railway ssh executes against the deployed /app built from main); (2) founder supplies ONE read-only LTP Deribit key; (3) set DERIBIT_CLIENT_ID + DERIBIT_CLIENT_SECRET on the Railway analytics worker (variable set triggers a redeploy — wait for it); (4) verify presence with the booleans-only ssh check. Never echo key values into any transcript or file. Exact command lines are in how-to-verify.
  </action>
  <what-built>Plan 67-01 shipped scripts/deribit_ground_truth.py with a fail-loud read-only scope gate (exit 2 on write scope, exit 3 on missing env vars). It reads DERIBIT_CLIENT_ID / DERIBIT_CLIENT_SECRET from the worker environment.</what-built>
  <how-to-verify>
    Orchestrator runs these steps (executor subagents have NO railway auth — do not delegate):

    1. PREREQUISITE — the harness script must be ON the deployed worker image. `railway ssh` executes against the deployed /app, which builds from main. Confirm the branch containing Plan 67-01 has been shipped/merged (orchestrator owns /ship and branching — no git ops inside this plan) AND the Railway deployment includes it:
       `railway deployment list` — newest deployment SUCCESS and newer than the merge commit (memory: red main CI silently skips Railway deploys; if stale, `railway up` to force after CI is green).
       Then confirm the module exists on the worker:
       `railway ssh "cd /app && python -c 'import scripts.deribit_ground_truth; print(\"module present\")'"`

    2. FOUNDER ACTION — provide ONE read-only LTP Deribit key (client_id + client_secret). Scopes at creation: read-only (account:read trade:read wallet:read custody:read block_trade:read); the harness will hard-reject any :read_write scope anyway.

    3. Set the env vars on the analytics worker service (either Railway dashboard -> Variables, or):
       `railway variables --set "DERIBIT_CLIENT_ID=<id>" --set "DERIBIT_CLIENT_SECRET=<secret>"`
       Note: setting variables triggers a service redeploy — wait for it to complete (`railway deployment list` again).

    4. Verify presence WITHOUT printing values:
       `railway ssh "cd /app && python -c 'import os; print(\"id_set=\", bool(os.getenv(\"DERIBIT_CLIENT_ID\")), \"secret_set=\", bool(os.getenv(\"DERIBIT_CLIENT_SECRET\")))'"`
       Expected output: `id_set= True secret_set= True`
  </how-to-verify>
  <verify>
    <automated>railway ssh "cd /app && python -c 'import os; print(\"id_set=\", bool(os.getenv(\"DERIBIT_CLIENT_ID\")), \"secret_set=\", bool(os.getenv(\"DERIBIT_CLIENT_SECRET\")))'"</automated>
    <human-check>Orchestrator-run only (executors lack railway auth). Expected literal output: `id_set= True secret_set= True`. Also confirm `railway deployment list` newest deployment SUCCESS and the module-present check printed "module present".</human-check>
  </verify>
  <acceptance_criteria>
    - `railway deployment list` shows the newest deployment SUCCESS and containing the harness script (module-present check prints "module present")
    - The env-presence check prints `id_set= True secret_set= True`; no key value ever echoed into the terminal transcript or any file
  </acceptance_criteria>
  <resume-signal>Type "provisioned" once both checks pass, or describe what failed (e.g., founder unavailable — in that case Plan 67-04 still executes; this plan pauses).</resume-signal>
  <done>Deribit read-only key live in the worker env; deployment carrying the harness verified green.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run the harness from the worker and capture sanitized output</name>
  <files>none yet (output captured to local scratch for review before any commit)</files>
  <read_first>
    - analytics-service/scripts/deribit_ground_truth.py module docstring (USAGE + exit-code contract: 0 success, 2 scope violation, 3 missing env)
  </read_first>
  <action>
    Orchestrator executes the harness on the worker via railway ssh and captures stdout to local scratch (never straight into the repo). Interpret exit codes: 0 = proceed; 2 = key is NOT read-only (FAIL-LOUD scope gate) -> back to Task 1 for key reissue, do NOT proceed; 3 = env vars missing (redeploy race) -> re-check Task 1 steps 3-4. If any per-currency `max_pages_hit` is true, re-run with `--max-pages 2000` before accepting counts. Spot-check the capture answers all 3 questions and eyeball for anything token/email/id-shaped (defense-in-depth on top of assert_sanitized). Exact command lines are in how-to-verify.
  </action>
  <what-built>The harness prints ONE sanitized JSON object to stdout (assert_sanitized runs before print) covering scope, per-currency trade counts with pagination flags, txn-log type summary, instrument mix, subaccounts observation, geo-block observation.</what-built>
  <how-to-verify>
    Orchestrator runs (executors have no railway auth):

    1. Execute and capture (allow several minutes — full-history pagination over up to ~62+ pages/currency):
       `railway ssh "cd /app && python -m scripts.deribit_ground_truth" > /tmp/deribit-ground-truth-run.json; echo "exit=$?"`

    2. Expected: `exit=0` and the file is a single valid JSON object with top-level keys: run_meta, scope, currencies, per_currency, instrument_mix, subaccounts_observation, geo_block_observation.
       - `exit=2` means the founder's key is NOT read-only (FAIL-LOUD scope gate) -> return to Task 1, founder must reissue the key. Do NOT proceed.
       - `exit=3` means env vars missing on the worker (redeploy race) -> re-check Task 1 step 3/4.

    3. Spot-check the answers are answerable from the capture:
       - `per_currency.<ccy>.txn_log_type_summary` shows the distinct `type` values with counts (the funding-netting answer: does a settlement/funding-typed row exist SEPARATELY from trade rows, or not?)
       - `instrument_mix` shows per-kind counts (inverse_perpetual / linear_perpetual / option / future)
       - `geo_block_observation.blocked` is false with the honest deferred-marker note (expected from Amsterdam), OR true with a captured marker candidate
       - `per_currency.<ccy>.trade_count` totals exist (compare order-of-magnitude against 18,778 / 21,014 / 61,248 — the key maps to ONE of the three LTP accounts)
       - If any `max_pages_hit` is true, re-run with a higher `--max-pages` before accepting the counts:
         `railway ssh "cd /app && python -m scripts.deribit_ground_truth --max-pages 2000" > /tmp/deribit-ground-truth-run.json`

    4. Eyeball /tmp/deribit-ground-truth-run.json for anything that looks like a token, email, or unmasked id (defense-in-depth on top of assert_sanitized).
  </how-to-verify>
  <verify>
    <automated>railway ssh "cd /app && python -m scripts.deribit_ground_truth" > /tmp/deribit-ground-truth-run.json && python3 -c "import json; d=json.load(open('/tmp/deribit-ground-truth-run.json')); assert all(k in d for k in ('run_meta','scope','currencies','per_currency','instrument_mix','subaccounts_observation','geo_block_observation')); print('CAPTURE OK')"</automated>
    <human-check>Orchestrator-run only. Exit code must be 0 (2 = scope violation -> reissue key; 3 = env missing). Confirm the 3 answers are answerable per how-to-verify step 3 and no max_pages_hit=true remains.</human-check>
  </verify>
  <acceptance_criteria>
    - Exit code 0; /tmp/deribit-ground-truth-run.json parses as JSON with all seven top-level keys
    - txn_log_type_summary contains >= 1 distinct type with a sample row (the funding-netting question is answerable from the data)
    - No max_pages_hit=true remains in the accepted capture
  </acceptance_criteria>
  <resume-signal>Type "captured" with the scratch file path confirmed, or paste the failure output (scrubbed).</resume-signal>
  <done>Sanitized harness output captured to local scratch, verified answerable and complete.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the evidence artifact and record the 3 answers with raw excerpts</name>
  <files>analytics-service/docs/deribit-ground-truth.md, analytics-service/docs/evidence/deribit-ground-truth-run.json</files>
  <read_first>
    - /tmp/deribit-ground-truth-run.json (the captured artifact from Task 2 — the ONLY source of truth for the answers; never assert anything not present in it)
    - analytics-service/docs/deribit-ground-truth.md (the template from Plan 67-01 — fill it, keep its section structure)
    - analytics-service/scripts/deribit_ground_truth.py (assert_sanitized — rerun it over the artifact before committing)
    - analytics-service/services/geo_block.py (ONLY if a real block marker was observed: _GEO_BLOCK_MARKERS tuple is where it would be added — see action)
  </read_first>
  <action>
    1. Copy /tmp/deribit-ground-truth-run.json to analytics-service/docs/evidence/deribit-ground-truth-run.json, then re-verify sanitization mechanically:
       `cd analytics-service && .venv/bin/python -c "import json; from scripts.deribit_ground_truth import assert_sanitized; assert_sanitized(json.load(open('docs/evidence/deribit-ground-truth-run.json'))); print('SANITIZED OK')"`
       If this raises: STOP, do not commit; fix the sanitizer (that is a bug — add a failing test first per write-tests-immediately), regenerate via Task 2.
    2. Populate docs/deribit-ground-truth.md — replace every "PENDING LIVE RUN" with the recorded answer + a fenced raw-JSON excerpt copied VERBATIM from the artifact (locked decision: each answer WITH the evidence proving it):
       - Funding-netting shape: state the OBSERVED shape (separate settlement/funding-typed rows vs netted into trade rows), citing the distinct `type` values + counts + the per-type sample rows. If the data is ambiguous, record the ambiguity honestly and what additional capture would resolve it — do not force a conclusion.
       - Instrument mix: per-kind counts from instrument_mix, per LTP account (this key = one account).
       - Geo-block body marker: the observed marker, OR the honest "no block observed from Amsterdam egress — marker deferred to observed-on-block; classifier is the fail-safe" record. Fabricating a marker is prohibited.
       - Bonus sections: subaccount-structure observation (Phase 72 note), per-currency trade counts vs the known totals 18,778 / 21,014 / 61,248 (Phase 70 pagination targets), run metadata.
    3. ONLY IF a real block body was observed (not expected): add the observed marker substring to _GEO_BLOCK_MARKERS in services/geo_block.py with a regression test in the existing geo_block test file proving is_geo_blocked matches it (test fails without the tuple entry). If no block observed, geo_block.py is untouched.
    4. If gitleaks flags the (redacted) evidence fixture, allowlist that specific file in .gitleaks.toml in the same batch (established Gitleaks-redaction-fixtures convention) — only after step 1 passed.
    5. Run the full analytics suite to confirm nothing regressed.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -c "import json; from scripts.deribit_ground_truth import assert_sanitized; assert_sanitized(json.load(open('docs/evidence/deribit-ground-truth-run.json'))); print('SANITIZED OK')" && { grep -n "PENDING LIVE RUN" docs/deribit-ground-truth.md; test $? -ne 0; } && .venv/bin/python -m pytest -q --cov=services --cov=routers --cov=main_worker --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - assert_sanitized passes over the committed artifact (prints "SANITIZED OK")
    - Zero "PENDING LIVE RUN" markers remain in docs/deribit-ground-truth.md; each of the 3 answer sections contains a fenced JSON excerpt whose content appears verbatim in docs/evidence/deribit-ground-truth-run.json
    - The geo-block section contains either an observed marker (with a geo_block regression test) or the exact honest-deferral wording — never an invented string
    - Full suite + coverage gate green
  </acceptance_criteria>
  <done>ROADMAP SC-1 and SC-2 satisfied: artifact committed, 3 answers recorded with evidence, honestly stated where unobservable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder -> Railway env | live Deribit credentials enter the system here (env-only, never tracked files) |
| worker -> Deribit API | authed HMAC requests; untrusted responses |
| scratch capture -> tracked git artifact | the commit boundary — sanitization re-verified mechanically before any commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-11 | Information Disclosure | committed evidence artifact | mitigate | assert_sanitized rerun over the ACTUAL artifact (blocking verify step); manual eyeball in Task 2; gitleaks scan (allowlist only per redaction-fixtures convention, only after assert_sanitized passes) |
| T-67-12 | Elevation of Privilege | write-scoped key accepted | mitigate | harness exit-2 scope gate BEFORE any private call; Task 2 explicitly halts on exit=2 and routes back to key reissue |
| T-67-13 | Information Disclosure | key values in terminal transcript / env checks | mitigate | env-presence checks print booleans only; variables set via railway dashboard/CLI, never echoed into files; ONB-02 rotation after onboarding recorded in Task 1 |
| T-67-14 | Repudiation | fabricated/unattributable answers | mitigate | every answer carries a verbatim excerpt from the committed artifact; ambiguity and unobservables recorded honestly (locked decision) |
| T-67-SC | Tampering | npm/pip installs | accept (n/a) | zero new dependencies this phase |
</threat_model>

<verification>
- Task 3 automated gate: assert_sanitized over the committed artifact + zero PENDING markers + full suite green
- Evidence chain: each answer section cites deribit-ground-truth-run.json
- No git branch operations performed inside the plan (orchestrator owns shipping)
</verification>

<success_criteria>
- ROADMAP SC-1: authed worker-side harness run completed, output committed as a sanitized artifact
- ROADMAP SC-2: funding-netting shape, instrument mix, and geo-block marker each recorded with raw evidence (or honest non-observability)
- Deribit key remains env-only; rotation note (ONB-02) carried into the SUMMARY
</success_criteria>

<output>
Create `.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-03-SUMMARY.md` when done. SUMMARY must record: exit codes observed, per-currency counts vs the known totals, which of the 3 answers were conclusive vs deferred, and the ONB-02 key-rotation reminder.
</output>
