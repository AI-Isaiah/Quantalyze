---
phase: 67-deribit-live-harness-exchange-ground-truth
plan: 04
type: execute
wave: 2
depends_on: [67-01, 67-02]
files_modified:
  - analytics-service/docs/evidence/bybit-reconcile-report.json
  - analytics-service/docs/bybit-reconciliation.md
autonomous: false
requirements: [BYB-01]

must_haves:
  truths:
    - "Bybit ingestion reconciled end-to-end against exchange ground truth for at least one live key: fills completeness (#563 class re-checked), funding rows, per-key realized+funding dailies (ROADMAP SC-3)"
    - "Count deltas are recorded EVEN IF ZERO — a clean reconciliation IS the BYB-01 evidence; no manufactured fix (#563 was NOT a P&L bug)"
    - "Any true discrepancy (dailies beyond 1e-9, dropped funding bucket, or true fills discrepancy) is root-caused and fixed with a regression test that fails without the fix (ROADMAP SC-4)"
    - "No secret or unmasked identifier exists in any committed artifact; prod DB untouched by the run"
  artifacts:
    - path: "analytics-service/docs/evidence/bybit-reconcile-report.json"
      provides: "Sanitized reconciliation report: fills counts + diff status + dq_flags, funding bucket diff, dailies compare, verdict"
    - path: "analytics-service/docs/bybit-reconciliation.md"
      provides: "Human-readable BYB-01 record: window, key (masked), deltas (even if zero), verdict, and — if any — the root-caused fix + regression test reference"
      contains: "verdict"
  key_links:
    - from: "analytics-service/docs/bybit-reconciliation.md"
      to: "analytics-service/docs/evidence/bybit-reconcile-report.json"
      via: "the record cites the report artifact"
      pattern: "bybit-reconcile-report\\.json"
---

<objective>
Execute the BYB-01 live reconciliation: the orchestrator runs `scripts/bybit_reconcile.py` on the Railway worker against the founder's live Bybit key (the #563 investigation subject), commits the sanitized report as evidence, and — only if a TRUE discrepancy surfaces — root-causes and fixes it with a failing-without-fix regression test. Independent of the Deribit KEY GATE (67-03 Task 1 — founder provisioning); depends on 67-01 only for the shared `assert_sanitized` helper, never on the Deribit key.

Purpose: prove the shared dailies path correct before Phase 70 stacks Deribit onto it; discharge the #563 under-fetch re-check with the clean Amsterdam egress.
Output: `docs/evidence/bybit-reconcile-report.json` + `docs/bybit-reconciliation.md`, plus a conditional fix + regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-CONTEXT.md
@.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md
@analytics-service/scripts/bybit_reconcile.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Run the reconciliation from the worker against the founder's live Bybit key</name>
  <files>none yet (output captured to local scratch for review before any commit)</files>
  <read_first>
    - analytics-service/scripts/bybit_reconcile.py module docstring (USAGE + exit-code contract: 0 clean/id_drift_only, 1 true discrepancy, 2 usage/env error; #563 discipline in the RUNBOOK)
  </read_first>
  <action>
    Orchestrator-only human-action gate (executor subagents have NO railway auth and NO Supabase MCP — never delegate). Sequence: (1) confirm the deployed worker image carries scripts/bybit_reconcile.py (shipping/merge is orchestrator-owned — no git ops inside this plan); (2) resolve the founder's live Bybit key id via a SELECT-only Supabase MCP query against prod (the #563 investigation subject); (3) execute the reconciliation via railway ssh with --api-key-id and --window-days 180, capturing stdout to local scratch; (4) interpret the verdict-encoded exit code (0 = clean/id_drift_only = expected BYB-01 evidence; 1 = TRUE discrepancy, Task 3 becomes mandatory; 2 = usage/env error, fix invocation and re-run); (5) spot-check the report fields per how-to-verify. Never paste key material anywhere. Exact command lines are in how-to-verify.
  </action>
  <what-built>Plan 67-02 shipped scripts/bybit_reconcile.py: read-only composition of the production seams (diff_strategy_fills native-execId two-stage, funding by match_key bucket, dailies recompute within 1e-9), sanitized JSON report to stdout, verdict-encoded exit code.</what-built>
  <how-to-verify>
    Orchestrator runs these steps (executor subagents have NO railway auth and NO Supabase MCP — do not delegate):

    1. PREREQUISITE — script deployed: confirm the branch containing Plan 67-02 is shipped/merged and deployed:
       `railway deployment list` — newest deployment SUCCESS and newer than the merge commit (memory: red main CI silently skips Railway deploys; `railway up` to force after green CI if stale).
       `railway ssh "cd /app && python -c 'import scripts.bybit_reconcile; print(\"module present\")'"`

    2. Resolve the subject key via Supabase MCP against prod (khslejtfbuezsmvmtsdn), read-only:
       `SELECT id, strategy_id, created_at FROM api_keys WHERE exchange = 'bybit' ORDER BY created_at;`
       Pick the founder's live key — the #563 investigation subject (cross-check against the #563 memory/PR notes if multiple rows). Note the id (uuid); never paste key material anywhere.

    3. Execute and capture (several minutes — full-history dailies recompute + windowed fills fetch):
       `railway ssh "cd /app && python -m scripts.bybit_reconcile --api-key-id <uuid> --window-days 180" > /tmp/bybit-reconcile-report.json; echo "exit=$?"`

    4. Interpret:
       - `exit=0`: verdict is "clean" or "id_drift_only" (id_drift is INFORMATIONAL — Bybit rotates order ids). This is the expected outcome and IS the BYB-01 evidence.
       - `exit=1`: TRUE discrepancy (dailies delta beyond 1e-9, dropped funding bucket, or a true fills discrepancy) — Task 3 becomes mandatory.
       - `exit=2`: env/usage error — fix the invocation (wrong uuid, missing env), re-run.
       - `exit=3`: harness/runtime error (network flake, ccxt timeout, DB error) — NOT a verdict; re-run. Never treat as a discrepancy (ship-review IN-6).

    5. Spot-check the report JSON: fills.count_delta present (even if zero), fills.dq_flags present (sync_truncated_bybit observable — the #563 signal), funding bucket diff present, dailies.max_abs_delta + overlap_days present, verdict enumerated, api_key_id masked to ***last4.
  </how-to-verify>
  <verify>
    <automated>railway ssh "cd /app && python -m scripts.bybit_reconcile --api-key-id <uuid> --window-days 180" > /tmp/bybit-reconcile-report.json; echo "exit=$?"; python3 -c "import json; r=json.load(open('/tmp/bybit-reconcile-report.json')); s=json.dumps(r); assert 'count_delta' in s and 'verdict' in s; print('CAPTURE OK')"</automated>
    <human-check>Orchestrator-run only (executors lack railway auth + Supabase MCP). Substitute the resolved uuid. Note the exit code verbatim; exit=1 makes Task 3 mandatory.</human-check>
  </verify>
  <acceptance_criteria>
    - Report captured at /tmp/bybit-reconcile-report.json, valid JSON, with fills/funding/dailies sections and an enumerated verdict
    - fills.count_delta is recorded regardless of value; dq_flags key present
    - Exit code noted verbatim for the SUMMARY
  </acceptance_criteria>
  <resume-signal>Type "captured exit=0" or "captured exit=1" (routes Task 3), or paste the scrubbed failure output.</resume-signal>
  <done>Live reconciliation executed from the worker; report captured to scratch with the verdict known.</done>
</task>

<task type="auto">
  <name>Task 2: Commit the sanitized report + the human-readable BYB-01 record (deltas recorded even if zero)</name>
  <files>analytics-service/docs/evidence/bybit-reconcile-report.json, analytics-service/docs/bybit-reconciliation.md</files>
  <read_first>
    - /tmp/bybit-reconcile-report.json (the captured report — sole source of truth; never assert anything not in it)
    - analytics-service/scripts/deribit_ground_truth.py (assert_sanitized — reused to mechanically re-verify the artifact before commit)
    - .planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md Pitfall 5 (#563 discipline: a count delta is only a bug if it moves P&L/dailies beyond 1e-9 OR drops funding rows)
  </read_first>
  <action>
    1. Copy /tmp/bybit-reconcile-report.json to analytics-service/docs/evidence/bybit-reconcile-report.json; re-verify mechanically:
       `cd analytics-service && .venv/bin/python -c "import json; from scripts.deribit_ground_truth import assert_sanitized; assert_sanitized(json.load(open('docs/evidence/bybit-reconcile-report.json'))); print('SANITIZED OK')"`
       On failure: STOP, do not commit; that is a sanitizer bug — failing test first, fix, re-capture via Task 1.
    2. Write analytics-service/docs/bybit-reconciliation.md: provenance header (script, worker run, date, window_days, masked key id), then sections mirroring the report — Fills (exchange count, DB count, count_delta EVEN IF ZERO, diff status, id_drift count with the "informational — Bybit rotates order ids" note, dq_flags incl. sync_truncated_bybit), Funding (bucket diff by match_key, per-day sums), Dailies (overlap_days, max_abs_delta vs the 1e-9 bar, axis used), Verdict, and "#563 re-check" explicitly stating the under-fetch class outcome with the clean Amsterdam egress. Each section cites its field in bybit-reconcile-report.json. If verdict is clean/id_drift_only, state plainly: "Clean reconciliation is the BYB-01 evidence; no fix manufactured (Pitfall 5)."
    3. If gitleaks flags the redacted artifact, allowlist the specific file in .gitleaks.toml same-batch (established convention) — only after step 1 passed.
    4. Full suite green.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -c "import json; from scripts.deribit_ground_truth import assert_sanitized; r=json.load(open('docs/evidence/bybit-reconcile-report.json')); assert_sanitized(r); assert 'count_delta' in json.dumps(r), 'count_delta missing'; print('SANITIZED OK')" && grep -c "bybit-reconcile-report.json" docs/bybit-reconciliation.md && .venv/bin/python -m pytest -q --cov=services --cov=routers --cov=main_worker --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - assert_sanitized passes over the committed report; count_delta field present in the artifact regardless of value
    - docs/bybit-reconciliation.md contains Fills/Funding/Dailies/Verdict sections and the explicit "#563 re-check" statement, citing the JSON artifact
    - Full suite + coverage gate green
  </acceptance_criteria>
  <done>BYB-01 evidence committed: sanitized report + human-readable record with deltas recorded even if zero (ROADMAP SC-3).</done>
</task>

<task type="auto">
  <name>Task 3: CONDITIONAL — root-cause, fix, and regression-test any TRUE discrepancy</name>
  <files>determined by the found root cause (candidates: analytics-service/services/exchange.py, analytics-service/services/funding_fetch.py, analytics-service/services/broker_dailies.py) + a new/extended test file alongside the existing suite</files>
  <read_first>
    - analytics-service/docs/evidence/bybit-reconcile-report.json (which axis failed: fills true-discrepancy, dropped funding bucket, or dailies beyond 1e-9)
    - The implicated fetcher/combiner at the site the report points to (services/exchange.py _fetch_raw_trades_bybit for fills; services/funding_fetch.py fetch_funding_bybit for funding; services/broker_dailies.py combine_realized_and_funding for dailies)
    - .planning/phases/67-deribit-live-harness-exchange-ground-truth/67-RESEARCH.md Pitfall 5 (what does NOT count as a bug)
  </read_first>
  <action>
    GATE: execute this task ONLY if Task 1 returned exit=1 / verdict "discrepancy". If verdict was clean or id_drift_only, record "Task 3 skipped — clean reconciliation, no discrepancy to fix (per #563 discipline)" in the SUMMARY and stop; do NOT manufacture a fix (locked decision + Pitfall 5).

    If a true discrepancy exists:
    1. Root-cause to the actual source (Rule 6 — no bandaids): reproduce the failing comparison from the report's raw numbers, trace into the implicated seam, identify the exact defect (e.g., a pagination boundary, a window-edge exclusion, a bucket-key mismatch).
    2. Regression test FIRST, failing without the fix (write-tests-immediately + locked decision): a unit/integration test in the existing suite exercising the defect with a constructed fixture derived from the (sanitized) observed data. Run it, confirm RED.
    3. Fix at the found site, minimal and surgical (Rule 3). Confirm the regression test goes GREEN and the full suite + coverage gate stays green.
    4. Re-run the live reconciliation (return to Task 1 command with the same window) to confirm the verdict flips to clean/id_drift_only; update both evidence artifacts (Task 2 procedure, including assert_sanitized) with the post-fix run, keeping the pre-fix report renamed to docs/evidence/bybit-reconcile-report-prefix.json as the discrepancy record.
    5. Guard the wiring, not just the helper (HIGH-tackle lesson): the regression test must prove the CALL SITE invokes the fixed path — neuter the fix, confirm the test fails.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest -q --cov=services --cov=routers --cov=main_worker --cov-fail-under=80</automated>
  </verify>
  <acceptance_criteria>
    - If skipped: the SUMMARY states the clean verdict and the skip rationale; zero production-code changes in this task
    - If executed: the regression test exists, demonstrably fails with the fix reverted (`git stash` the fix, run the test, confirm RED, unstash), and the post-fix live re-run verdict is clean/id_drift_only with both pre-fix and post-fix reports committed
    - Full suite + coverage gate green either way
  </acceptance_criteria>
  <done>ROADMAP SC-4 satisfied: any discrepancy root-caused + fixed + regression-tested — or honestly recorded as none found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker -> Bybit API | decrypted live prod credentials cross here; untrusted responses |
| script -> prod Supabase | read-only reconciliation reads (api_keys, trades, funding_fees, csv_daily_returns) |
| scratch capture -> tracked git artifact | commit boundary — sanitization re-verified mechanically |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-15 | Information Disclosure | committed report artifact | mitigate | assert_sanitized rerun over the ACTUAL artifact (blocking verify); api_key_id masked ***last4; gitleaks allowlist only post-verification per redaction-fixtures convention |
| T-67-16 | Tampering | prod DB mutation during the run | mitigate | script is read-only by construction (Plan 67-02 grep gate: zero .insert/.update/.upsert/.delete); orchestrator SQL in Task 1 is SELECT-only |
| T-67-17 | Information Disclosure | decrypted key in transcript/logs | mitigate | decryption happens only inside the worker process; orchestrator handles the uuid only; every free-form string scrubbed (&signature= class) |
| T-67-18 | Repudiation | unverifiable "clean" claim or manufactured fix | mitigate | deltas recorded even if zero in a committed artifact; conditional-fix gate requires exit=1; regression test proven RED with fix reverted |
| T-67-SC | Tampering | npm/pip installs | accept (n/a) | zero new dependencies this phase |
</threat_model>

<verification>
- Task 2 automated gate: assert_sanitized + count_delta presence + record cites artifact + full suite green
- Verdict path integrity: Task 3 executed iff exit=1; skip recorded otherwise
- No git branch operations performed inside the plan (orchestrator owns shipping)
</verification>

<success_criteria>
- ROADMAP SC-3: fills (#563 class re-checked, deltas recorded even if zero), funding (match_key buckets), and per-key dailies (1e-9) reconciled for one live Bybit key, evidence committed
- ROADMAP SC-4: any true discrepancy root-caused + fixed + failing-without-fix regression test; clean verdict honestly recorded as the evidence otherwise
</success_criteria>

<output>
Create `.planning/phases/67-deribit-live-harness-exchange-ground-truth/67-04-SUMMARY.md` when done. SUMMARY must record: the exit code/verdict, the window used, fills/funding/dailies deltas (verbatim numbers), whether Task 3 ran, and — if it did — the fix site + regression test id.
</output>
