---
milestone: v1.2
milestone_name: Allocator Cohesion
audited: 2026-06-24
audited_by: gsd-audit-milestone (orchestrator override — see ledger note)
status: passed
scope_override: true
scope_override_reason: >
  gsd init.milestone-op reports current milestone = v1.1.0 (8 phases, 3 complete),
  which is STALE. v1.1.0 already shipped+archived (milestones/v1.1.0-*). The real
  current milestone is v1.2 (phases 29-33), shipped+deployed+QA-verified at
  VERSION 0.30.0.1. Audited v1.2 against phase VERIFICATION.md files + ship/land/qa
  evidence, NOT the corrupted root ROADMAP/REQUIREMENTS (which are still v1.1.0's).
scores:
  phases: 5/5 (all must-haves verified)
  requirements: n/a (v1.2 REQUIREMENTS ledger was lost — see ledger_debt)
  integration: verified via live /qa + per-phase VERIFICATION (formal checker skipped — see notes)
  flows: FLOW-01 (Bridge→composer) + FLOW-02 (/scenarios retirement) verified
phases:
  - phase: 29-unified-composer-spine
    must_haves: 5/5
    status: passed
    evidence: "headed-browser /qa 2026-06-23 (entry-mode control, example pills, reset-confirmation modal)"
  - phase: 30-factsheet-graphs-on-the-blend
    must_haves: 12/12
    status: passed
    evidence: "headed-browser /qa 2026-06-23 (returns-distribution, rolling-metrics WR-01 window label, equity/drawdown)"
  - phase: 31-graphs-lead-layout-collapsible-controls
    must_haves: 5/5
    status: passed (automated); 3 authed-headed live-render confirms outstanding
    evidence: "vitest verifies wrap + structural order + HIDE-not-UNMOUNT; collapse-visual / localStorage-persist / factsheet-reset need authed headed browser"
  - phase: 32-dead-link-fix-route-retirement
    must_haves: 7/7
    status: passed (automated); /scenarios→307 confirmed live, 2 authed items outstanding
    evidence: "/scenarios → 307 → /login confirmed in live /qa; add-strategy auto-attach + role-conditional nav code-verified (grep), authed-headed confirm outstanding"
  - phase: 33-journey-polish
    must_haves: 3/3
    status: passed
    evidence: "live WCAG-AA axe GREEN in CI (composer-axe.spec.ts) — caught+fixed 3 real prod a11y bugs (duplicate-main, tablist-children, footer-region); focus-ring + Bridge add-to-scenario are authed-headed (deferred — password prohibition), code+unit verified"
gaps: {}   # no product blockers
verification_debt:   # low-risk, all shipped+deployed; authed-headed live-render confirms
  - phase: 31-graphs-lead-layout-collapsible-controls
    items:
      - "Collapse makes graphs visually lead (CSS/scroll) — headed-browser confirm"
      - "Collapse choice persists across reload (localStorage/cross-tab) — headed-browser confirm"
      - "Factsheet 'Reset view' opens all sections + analytics event — headed-browser confirm"
  - phase: 32-dead-link-fix-route-retirement
    items:
      - "Add-strategy auto-attach back to source portfolio (RLS-scoped insert) — authed headed-browser confirm"
      - "Role-conditional nav (allocator one entry / manager Portfolios) — authed headed-browser confirm"
  - phase: 33-journey-polish
    items:
      - "Accent focus-ring paints on blank-slate CTAs — headed-browser confirm"
      - "Bridge 'Add to scenario' updates projection strip live — authed headed-browser confirm (Vitest proves the code path)"
ledger_debt:   # THE real finding — process/bookkeeping, not product
  - "gsd init.milestone-op reports current = v1.1.0 (stale); STATE.md says Phase 27 planning"
  - "v1.1.0 completion half-done: archive files exist (milestones/v1.1.0-*) but ROADMAP never collapsed + no git tag v1.1.0"
  - "v1.2 has no ROADMAP/REQUIREMENTS in .planning root (root files are still v1.1.0's) and no milestones/v1.2 archive — lost to the .planning-gitignore-drop hazard"
  - "No git tag for v1.1.0 OR v1.2 (tags stop at v1.0.0)"
  - "complete-milestone v1.2 must reconcile the ledger BEFORE archiving/tagging"
---

# v1.2 Allocator Cohesion — Milestone Audit

## Verdict: ✅ PASSED (product) · ⚠ ledger reconciliation required before complete

All five phases (29–33) had their must-have success criteria verified per-phase
(`*-VERIFICATION.md`), and the milestone shipped + deployed + was QA-verified on
production through the real engineering gates:

- **Phases 29+30** → PR #518, v0.29.0.0
- **Phases 31+32** → PR #520, v0.30.0.0 (squash `0408ee77`)
- **Phase 33** → PR #521, v0.30.0.1 (squash `1177546`) — the composer-axe gate, once
  it ran in CI, caught and fixed **3 genuine production WCAG-AA violations** on
  `/allocations` (duplicate `<main>`, Export/+Allocation inside `role=tablist`,
  `role=region` on `<footer>`) that 5 specialists + code review + prior /qa missed.

The product question the audit exists to answer — *did the milestone hit its
definition of done?* — is **yes**, confirmed retrospectively (the milestone shipped
before this audit because `/gsd-autonomous` deferred the lifecycle until P33 landed).

## Why this audit overrode the tool's milestone pointer

`gsd-sdk query init.milestone-op` reports the current milestone as **v1.1.0** with
3/8 phases complete. That is **stale**: v1.1.0 shipped (PRs #510–#517), was archived
to `milestones/v1.1.0-*` on Jun 22, and the real current milestone is **v1.2**
(VERSION `0.30.0.1`). Auditing v1.1.0 verbatim would have read VERIFICATION files for
phases 21–28 and falsely reported 27/28 as gaps (those dirs were archived out of the
active `phases/` tree). I therefore audited v1.2 against the on-disk phase
VERIFICATION files + the ship/land/qa evidence.

## Verification debt — RESOLVED 2026-06-24 (headed-browser /qa)

The "human_needed" items on phases 31–33 were resolved via a headed-browser /qa
(Playwright MCP) against a **pre-authenticated allocator session on prod** (the user
logged the browser in; I entered no password). 7 of 9 items confirmed live; the other
2 are role/data-gated on that account and carry strong alternative evidence:

| Phase | Item | Result |
|---|---|---|
| 31 | Collapse → graphs lead | ✅ live (screenshot; details.open=false, Hide→Show) |
| 31 | Collapse persists on reload | ✅ live (composer-collapse:controls='closed') |
| 31 | Factsheet "Reset view" opens all | ✅ live (allOpen=true; 0 console errors) |
| 32 | /scenarios → 307 → composer | ✅ live (FLOW-02) |
| 32 | Role-nav (allocator one entry) | ✅ live (only "My Allocation"; no Sandbox) (FLOW-03) |
| 32 | Add-strategy → /discovery (FLOW-01) | ◑ confirmed in #520 /qa + code-verified (zero-diff revert + rewritten test); manager/portfolio surface not on this allocator account |
| 33 | Focus-ring paints on CTAs | ✅ live (accent lab() ring via box-shadow + screenshot; identical class to blank-slate CTAs — segment used since booked account never hits the no-book front door) |
| 33 | Live WCAG-AA axe | ✅ GREEN in CI (caught+fixed 3 real a11y bugs); structural fixes re-confirmed live |
| 33 | Bridge "Add to scenario" updates projection | ◑ code-verified by the non-vacuous bridge-to-composer-seam regression (RED when neutered); no active Bridge recommendations on this account to exercise live |

0 console errors observed on every authed surface visited. No product blockers; the
two ◑ items are not defects.

## Integration check

Formal `gsd-integration-checker` spawn was **skipped** (stated explicitly, not silent):
v1.2 is already integration-verified through the live /qa, the 5 per-phase
VERIFICATION files, FLOW-01 (Bridge→composer, with a non-vacuous projection-delta
regression test) and FLOW-02 (`/scenarios` retirement, confirmed 307→/login live), and
the merged+deployed prod state. There is also no v1.2 REQUIREMENTS ledger for the
checker to map findings to. Re-run `/gsd:audit-milestone v1.2` after restoring the
ledger if a formal cross-phase pass is wanted.

## Ledger reconciliation required before `complete-milestone`

`complete-milestone` archives ROADMAP/REQUIREMENTS + git-tags. The `.planning` ledger
is desynced and must be fixed first:

1. **v1.1.0**: collapse its still-"active" ROADMAP entry (archive already exists) and
   create the missing `git tag v1.1.0`.
2. **v1.2**: there is no v1.2 ROADMAP/REQUIREMENTS to archive (lost). Either reconstruct
   a minimal v1.2 roadmap from the 5 phase dirs, or accept the phase SUMMARYs +
   CHANGELOG + this audit as the record and tag `v1.2` on merge commit `1177546`.
3. Update STATE.md (currently "v1.1.0 / Phase 27 / planning").
