---
phase: 14a
plan: 06
type: execute
wave: 3
depends_on: [14a-01]
files_modified:
  - package.json
  - package-lock.json
  - DESIGN.md
  - .github/PULL_REQUEST_TEMPLATE.md
autonomous: true
requirements: [DESIGN-03, CLEANUP-01]
must_haves:
  truths:
    - "package.json no longer lists @nivo/boxplot as a dependency"
    - "package-lock.json no longer references @nivo/boxplot"
    - "grep -r '@nivo/boxplot' src/ tests/ e2e/ returns ZERO matches (verifies the package was actually unused, per RESEARCH §A1)"
    - "DESIGN.md ## Decisions Log carries the UC#7 7-panel density-rule deviation entry"
    - "DESIGN.md ## Decisions Log carries the v2 4-size / 2-weight type contract entry"
    - ".github/PULL_REQUEST_TEMPLATE.md exists and contains the per-chart identity checklist (8 boxes from UI-SPEC §10)"
  artifacts:
    - path: "package.json"
      provides: "Boxplot dependency removed"
    - path: "DESIGN.md"
      provides: "Decisions log extended with 2 new entries"
      contains: "UC#7"
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "PR template with per-chart identity checklist (DESIGN-03)"
  key_links:
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "DESIGN.md identity rules + UI-SPEC §10 checklist"
      via: "literal markdown checkboxes"
      pattern: "CHART_ACCENT|CHART_TICK_STYLE"
---

<objective>
Land the Phase 14a chrome surfaces in parallel with Plan 14A-05 (Wave 3): (1) `npm uninstall @nivo/boxplot` (CLEANUP-01 — verified-zero-imports per RESEARCH §A1, ~80KB gzipped saved); (2) extend `DESIGN.md ## Decisions Log` with the 2 new UI-SPEC §12 entries (UC#7 7-panel density-rule deviation + v2 4-size/2-weight type contract); (3) create `.github/PULL_REQUEST_TEMPLATE.md` with the per-chart identity checklist from UI-SPEC §10 (no template exists today per RESEARCH Pitfall 10).

Wave 3 — depends on Plan 14A-01 (`CHART_TICK_STYLE` and `CHART_ACCENT` referenced in the PR template checklist must already be exported from `chart-tokens.ts`). Independent of Plan 14A-05 (test files) — both run in parallel.

Purpose: The 3 chrome surfaces are independent of code changes but lock the institutional-process artifacts that Phase 14a's success criteria 9-10 require. Without these, Phase 14a does not formally close even if every component ships green.

Output: 1 modified `package.json` (1 line removed; lockfile regenerated by `npm install`); 1 modified `DESIGN.md` (2 new rows in the decisions log table); 1 new `.github/PULL_REQUEST_TEMPLATE.md` file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-CONTEXT.md
@.planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-UI-SPEC.md
@.planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-RESEARCH.md
@.planning/phases/14a-single-strategy-v2-eager-panels-identity/14a-01-PLAN.md
@DESIGN.md
@AGENTS.md
@package.json

<interfaces>
<!-- Verified package.json line 20 (2026-04-29): -->
```
"@nivo/boxplot": "^0.99.0",
```

<!-- Verified DESIGN.md ## Decisions Log starts at line 126 (table header at 127-128); existing entries at 129-135 with table rows like:
| 2026-04-06 | Decision | Rationale |
-->

<!-- Verified .github/ directory exists but contains only `workflows/` (no PR template today). Per RESEARCH Pitfall 10, the recommendation is a single-file template at .github/PULL_REQUEST_TEMPLATE.md (vs the directory variant). -->

<!-- UI-SPEC §10 per-chart identity checklist (verbatim 8 boxes — must appear literally in the PR template): -->
<!-- (1) White card surface (`bg-card`)
     (2) Strategy series uses `CHART_ACCENT` (`#1B6B5A`) — NOT `#0D9488` (current bright teal in `EquityCurve.tsx`)
     (3) Benchmark stroke uses `CHART_TEXT_MUTED` (`#94A3B8`), 1px width, dashed or 1px-vs-2px differentiation
     (4) Positive cells (KPI strip) use `--color-positive` (`#16A34A`); negative cells use `--color-negative` (`#DC2626`)
     (5) Gridlines (Recharts) use `CHART_BORDER` (`#E2E8F0`); lightweight-charts uses `--color-track` (`#F1F5F9`)
     (6) No Plotly chrome (no modebar, no toolbar, no Plotly attribution)
     (7) Axis ticks use `CHART_TICK_STYLE` token (Geist Mono **12px** tabular-nums `#64748B` — DM Sans is wrong here; 11px is wrong here per the v2 4-size contract)
     (8) No decorative animation; chart enter via Recharts default fade only (≤250ms)
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: npm uninstall @nivo/boxplot (CLEANUP-01)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (full file — confirm `"@nivo/boxplot": "^0.99.0"` is at line 20 in dependencies; verify no devDependency variant)
    - .planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-UI-SPEC.md §11 (CLEANUP-01 verification — `package.json` diff + `package-lock.json` diff + zero-imports grep)
    - .planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-RESEARCH.md "To Remove" section (RESEARCH §A1 confirms zero `@nivo/boxplot` imports today)
  </read_first>
  <behavior>
    - Before uninstalling, RE-VERIFY that no source file imports `@nivo/boxplot`: `grep -rn "@nivo/boxplot" src/ tests/ e2e/ scripts/` returns zero matches
    - `npm uninstall @nivo/boxplot` removes the package from `dependencies` in `package.json` AND removes its entry from `package-lock.json`
    - After uninstall, `npm run build` exits 0 (no broken imports surface — but the build is the safety net)
    - After uninstall, `npm test --run` exits 0 (existing tests not affected — boxplot was unused)
  </behavior>
  <action>
1. Pre-flight grep: `grep -rn "@nivo/boxplot" src/ tests/ e2e/ scripts/ 2>/dev/null | wc -l` MUST return `0`. If it returns ≥1, STOP and document the actual import sites in SUMMARY — the `ReturnQuantiles.tsx` audit may have been incomplete.

2. Run `npm uninstall @nivo/boxplot` from the project root. This single command:
   - Removes the line `"@nivo/boxplot": "^0.99.0",` from `package.json` `dependencies`
   - Removes all `@nivo/boxplot` references from `package-lock.json`
   - Removes `node_modules/@nivo/boxplot/`
   - Re-resolves the lockfile cleanly

3. Run `npm run build` and confirm it exits 0.

4. Run `npm test --run` and confirm no test regressions.

5. (Optional, manual) Note the size delta. The expectation is ~80KB gzipped saved on whatever route bundle the chart components landed in. The exact delta is informational — record it in SUMMARY but do NOT gate the plan on a specific number (no CI-gated bundle-size assertion in 14a per CONTEXT.md "Deferred Ideas").
  </action>
  <verify>
    <automated>grep -c "@nivo/boxplot" package.json package-lock.json 2>/dev/null; grep -rn "@nivo/boxplot" src/ tests/ e2e/ scripts/ 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@nivo/boxplot" package.json` returns `0`
    - `grep -c "@nivo/boxplot" package-lock.json` returns `0`
    - `grep -rn "@nivo/boxplot" src/ tests/ e2e/ scripts/` returns `0` lines
    - `npm run build` exits 0
    - `npm test --run` exits 0 (or completes with the same pass count as pre-uninstall — no regressions introduced by the package removal)
  </acceptance_criteria>
  <done>@nivo/boxplot fully removed from package.json + package-lock.json + node_modules; zero imports in codebase; build + tests green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend DESIGN.md ## Decisions Log with 2 new entries (DESIGN-03)</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md (full file — confirm `## Decisions Log` heading at line 126; existing table rows at lines 129-135 use the format `| {date} | {decision} | {rationale} |`; the executor MUST append two new rows AFTER the existing rows, NOT in the middle of the table)
    - .planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-UI-SPEC.md §12 (verbatim text for the two decisions log rows — DO NOT paraphrase; the rationale text is the contract)
  </read_first>
  <behavior>
    - DESIGN.md ## Decisions Log gains exactly 2 new rows appended at the end of the existing table
    - Row 1 captures UC#7 7-panel single-strategy density-rule deviation
    - Row 2 captures v2 4-size / 2-weight type contract
    - Date column on both rows is `2026-04-29` (today, per CLAUDE.md `currentDate`) OR the actual phase ship date if executed later — executor's discretion; the date format MUST match the existing entries (`YYYY-MM-DD`)
    - The existing 7 entries (2026-04-06 through 2026-04-27) are NOT modified
  </behavior>
  <action>
1. Read `DESIGN.md` and locate the last row of the Decisions Log table (currently line 135).

2. Append two new rows at the END of the table (after line 135). The exact rows (verbatim from UI-SPEC §12 — preserve all punctuation, numbers, and code-styled fragments):

```
| 2026-04-29 | UC#7 — accept 7-panel single-strategy density-rule deviation | Quantstats parity requires 7 distinct analytical panels (Overview / Headline+Equity / Drawdown / Returns Distribution / Rolling / Trades / Exposure) on `/strategy/[id]/v2`. The "data density > card density" rule is preserved within each panel (multi-cell strips, shared-axis charts, no card-on-card nesting), but the 7-panel scrollable shell exceeds the "3+ cards in a row → make it one panel" guideline at the page level. Accepted as a deliberate institutional-factsheet density choice; reference: FactSet quarterly factsheet pages where 8+ panels per page is standard. Single-page scroll; no tabs; IntersectionObserver-deferred mount on panels 4–7 keeps TTI under budget. |
| 2026-04-29 | v2 single-strategy 4-size / 2-weight type contract | The v2 surface restricts itself to a tight 4-size / 2-weight subset of DESIGN.md's typography scale. Sizes: page H1 = 32px Instrument Serif; panel H2 = 16px DM Sans semibold; KPI metric values = 18px Geist Mono semibold tabular-nums; everything else (cell labels, sub-headings via `uppercase tracking-wider`, axis ticks via `CHART_TICK_STYLE`, table cells, tooltips, banner copy, disabled labels) = 12px caption tier. Weights: exactly 2 — 400 regular and 600 semibold. Sub-headings differentiate via `uppercase tracking-wider` transform, not a third weight. Chart axis ticks consolidated 11px → 12px (Geist Mono tabular-nums at `#64748B` on `#FFFFFF` = 4.85:1, well within WCAG AA). The wider DESIGN.md scale (10–11px micro, 13px small, 14px body, 24px H2, 500 medium weight) remains the project superset; this contract is v2-specific and grep-enforced via `tests/visual/strategy-v2-type-scale.test.ts`. |
```

3. Do NOT modify any of the existing 7 rows in the table. Do NOT modify the `## Decisions Log` heading or the `| Date | Decision | Rationale |` header row at line 127.

4. Do NOT modify any other section of DESIGN.md.
  </action>
  <verify>
    <automated>grep -c "UC#7 — accept 7-panel single-strategy density-rule deviation" DESIGN.md; grep -c "v2 single-strategy 4-size / 2-weight type contract" DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "UC#7 — accept 7-panel single-strategy density-rule deviation" DESIGN.md` returns 1
    - `grep -c "v2 single-strategy 4-size / 2-weight type contract" DESIGN.md` returns 1
    - `grep -c "## Decisions Log" DESIGN.md` returns 1 (heading not duplicated)
    - `grep -cE "^\\| 2026-04-06 \\|" DESIGN.md` returns at least 4 (existing 4 rows from 2026-04-06 preserved)
    - `grep -cE "^\\| 2026-04-29 \\|" DESIGN.md` returns at least 2 (the 2 new rows)
    - The new rows appear AFTER line 135 (existing last row from 2026-04-27) — verify with `grep -n "2026-04-29" DESIGN.md` returning line numbers > 135
    - File line count increased by exactly 2 (no other content changes; verify with `wc -l DESIGN.md` before/after if needed)
  </acceptance_criteria>
  <done>DESIGN.md decisions log carries 9 total entries (7 existing + 2 new); both UC#7 and 4-size/2-weight contracts captured verbatim from UI-SPEC §12.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create .github/PULL_REQUEST_TEMPLATE.md (DESIGN-03)</name>
  <files>.github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-UI-SPEC.md §10 (verbatim 8-box per-chart identity checklist)
    - .planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-CONTEXT.md "PR template &amp; workflow" (the 7-box variant — UI-SPEC §10's 8-box version is the canonical post-revision format; use the 8-box version)
    - .planning/phases/14a-single-strategy-v2-eager-panels-identity/14A-RESEARCH.md Pitfall 10 (single-file template at .github/PULL_REQUEST_TEMPLATE.md preferred over multi-template directory)
  </read_first>
  <behavior>
    - File `.github/PULL_REQUEST_TEMPLATE.md` exists with markdown content
    - Contains a "## Summary" placeholder section (1 sentence "what + why")
    - Contains a "## Test plan" section with checkbox-style items
    - Contains a "## Identity audit (per-chart)" section with the 8 verbatim checkboxes from UI-SPEC §10
    - Each chart-identity checkbox is a markdown task list item starting with `- [ ]`
    - Token names referenced in the checklist (`CHART_ACCENT`, `CHART_TICK_STYLE`, `CHART_BORDER`, `CHART_TEXT_MUTED`, `bg-card`, `--color-positive`, `--color-negative`, `--color-track`) are written as inline-code spans (with backticks)
  </behavior>
  <action>
1. Create the parent directory only if it does not exist: `.github/` already exists (contains `workflows/`), so just write the file directly.

2. Create `.github/PULL_REQUEST_TEMPLATE.md` with this exact content:

```markdown
## Summary

<!-- 1-2 sentences: what changed and why. Focus on the "why" — the diff
     shows the "what". -->

## Test plan

- [ ] `npm test` passes
- [ ] `npm run typecheck` passes
- [ ] `npm run build` passes
- [ ] `npm run lint` passes
- [ ] Playwright E2E specs for affected routes pass (if applicable)
- [ ] Manual smoke on the changed surface — describe what was tested and how

## Identity audit (per-chart)

For every chart added or modified inside `/strategy/[id]/v2` panels (or any
new chart added anywhere in the app), verify each chart against the
DESIGN.md identity rules:

- [ ] White card surface (`bg-card`)
- [ ] Strategy series uses `CHART_ACCENT` (`#1B6B5A`) — NOT `#0D9488` (legacy bright teal)
- [ ] Benchmark stroke uses `CHART_TEXT_MUTED` (`#94A3B8`), 1px width, dashed or 1px-vs-2px differentiation; never as text fill
- [ ] Positive cells (KPI strip) use `--color-positive` (`#16A34A`); negative cells use `--color-negative` (`#DC2626`)
- [ ] Gridlines (Recharts) use `CHART_BORDER` (`#E2E8F0`); lightweight-charts uses `--color-track` (`#F1F5F9`)
- [ ] No Plotly chrome (no modebar, no toolbar, no Plotly attribution)
- [ ] Axis ticks use `CHART_TICK_STYLE` token (Geist Mono 12px tabular-nums `#64748B` — DM Sans is wrong here; 11px is wrong here per the v2 4-size contract)
- [ ] No decorative animation; chart enter via Recharts default fade only (≤250ms)

## Notes

<!-- Anything else: screenshots, deviations from spec, deferred items,
     follow-up TODOs. -->
```

3. Confirm the file is recognized by GitHub (this is automatic when the file appears at `.github/PULL_REQUEST_TEMPLATE.md` — no further config needed). The next PR opened against this repo will pre-populate with this template.
  </action>
  <verify>
    <automated>ls .github/PULL_REQUEST_TEMPLATE.md &amp;&amp; wc -l .github/PULL_REQUEST_TEMPLATE.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.github/PULL_REQUEST_TEMPLATE.md` exists
    - `grep -c "## Identity audit (per-chart)" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `grep -c "CHART_ACCENT" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c "CHART_TICK_STYLE" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c "CHART_BORDER" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c "CHART_TEXT_MUTED" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c "bg-card" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c -- "--color-positive" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c -- "--color-negative" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c -- "--color-track" .github/PULL_REQUEST_TEMPLATE.md` returns at least 1
    - `grep -c "No Plotly chrome" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `grep -cE "^- \\[ \\]" .github/PULL_REQUEST_TEMPLATE.md` returns at least 14 (8 chart-identity + 6 test-plan checkboxes)
    - `grep -c "## Summary" .github/PULL_REQUEST_TEMPLATE.md` returns 1
    - `grep -c "## Test plan" .github/PULL_REQUEST_TEMPLATE.md` returns 1
  </acceptance_criteria>
  <done>PR template file shipped at .github/PULL_REQUEST_TEMPLATE.md with the 8-box per-chart identity checklist + Summary + Test plan + Notes scaffolding; auto-applies to all future PRs against the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (no production trust boundary — these are documentation + dependency artifacts) | n/a |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14a-06-01 | T (Tampering) | npm uninstall removes a transitively-needed package | mitigate | Pre-flight grep + post-install `npm run build` + `npm test --run` catch any broken-import surface. RESEARCH §A1 verified zero imports today. |
| T-14a-06-02 | I (Information disclosure) | DESIGN.md decisions log entries leak v0.18 / Phase 14b plans | accept | Both entries describe shipped contracts (UC#7 + 4-size/2-weight); no future-feature disclosure. Public DESIGN.md content is acceptable. |
</threat_model>

<verification>
- `grep -c "@nivo/boxplot" package.json package-lock.json` returns 0
- `grep -rn "@nivo/boxplot" src/ tests/ e2e/ scripts/` returns 0 lines
- `grep -c "## Decisions Log" DESIGN.md` returns 1
- `grep -cE "^\| 2026-04-29 \|" DESIGN.md` returns 2
- `ls .github/PULL_REQUEST_TEMPLATE.md` succeeds
- `npm run build` exits 0
</verification>

<success_criteria>
1. `@nivo/boxplot` removed from package.json + package-lock.json + node_modules; zero imports anywhere in the codebase.
2. DESIGN.md decisions log carries the 2 new UI-SPEC §12 entries (UC#7 + 4-size/2-weight).
3. `.github/PULL_REQUEST_TEMPLATE.md` exists with the 8-box per-chart identity checklist.
4. `npm run build` and `npm test --run` both exit 0 — no regressions from any of the 3 changes.
</success_criteria>

<output>
After completion, create `.planning/phases/14a-single-strategy-v2-eager-panels-identity/14a-06-SUMMARY.md` describing:
- Bundle size delta from `npm run build` (informational; not a gate)
- Confirmation that DESIGN.md line count grew by exactly 2 lines (the 2 new rows; no other content changes)
- Confirmation that .github/PULL_REQUEST_TEMPLATE.md was created (not editing an existing template)
- Build + test status post-uninstall
</output>
