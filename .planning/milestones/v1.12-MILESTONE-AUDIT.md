---
milestone: v1.12
name: sFOX Verified Integration
audit_date: 2026-07-19
status: foundation_close
verdict: shipped_flag_off_gaps_rehomed
---

# v1.12 Milestone Audit — sFOX Verified Integration (FOUNDATION close)

> **Close type: FOUNDATION (flag-OFF).** This is not a full-coverage milestone
> audit. v1.12 shipped the sFOX **foundation dormant** (adapter + `api_verified`
> surfaces + DB migrations + worker-hardening groundwork + flag-gated UI, all
> OFF). The go-live ops + open defects were **deliberately re-homed to v1.13
> Infra**, not silently accepted. A full `/gsd:audit-milestone` was NOT run — the
> "gaps" are the known founder-gated go-live ops, already enumerated below and in
> `v1.13-infra-ROADMAP-SEED.md`. Founder decision to close as Foundation:
> 2026-07-19 (post-PR-#623 land).

## What shipped (landed to main + prod, PR #623 @ `92be47af`, v0.46.0.0)

- **Phase 118 — SFOX Research + adapter contract:** non-ccxt `SfoxClient`
  (Bearer, 4 read endpoints, proxy seam, rate gate); 25-test offline contract
  suite. SC-3 live sandbox smoke is founder-gated (`human_needed`, skips green).
- **Phase 119 — Read adapter + key validation + DB constraint-widen:** worker
  `is_sfox` validate branch (honest `KEY_AUTH_FAILED`), Q1 secret carve-out in all
  3 key routes, constraint-widening migration admitting `'sfox'` across the ≥5
  exchange CHECKs — **applied + verified on prod**.
- **Phase 120 — Equity reconstruction + backbone:** `SfoxAdapter` (fail-loud,
  balance-history → daily returns → the ONE `derive_basis_series` backbone),
  `api_verified` provenance stamp, lockstep Source/registry registration. Live
  ground-truth parity leg founder-gated (`human_needed`).
- **Phase 122 (UI) — Add-key + badge:** SFOX 3-letter tag, `api_verified`
  VerifiedBadge coverage, flag-gated wizard offer (OFF = byte-identical, tested),
  sfox-aware `ApiKeyForm`, `/security#sfox-readonly` setup guide.
- **Phases 121 & 123 — code-complete groundwork:** Fly egress-proxy artifacts +
  worker proxy wiring (121-01/121-02); `asyncio.wait_for`-bounded derive crawls +
  kind-filtered claim RPC migration (prod-applied) + E2 anchor/flip fixtures +
  go-live/rollback runbook (123-01/02/03).

**Provenance / dormancy proof:** `NEXT_PUBLIC_SFOX_ENABLED` / `SFOX_ENABLED`
empty in prod → no user can connect sFOX, nothing reads live. QA verified the
flag-OFF invariant (sFOX not offerable) at land.

## Re-homed to v1.13 Infra (founder-gated go-live ops + open defects)

| v1.12 item | State at close | v1.13 home |
|---|---|---|
| 121-03 Fly deploy + IP whitelist | founder op; artifacts + wiring landed | Fly static-egress + IP-bind |
| 122-04 factsheet e2e | code landed; e2e RED (real defect on the `api_verified` connected-key factsheet render) | Factsheet e2e green |
| 123 FLIP go-live (cutover/enqueue/cron) | code-complete; ops `human_needed`-OPEN | Worker rebuild + FLIP retry |
| 123 E2 live acceptance | harness + hand-derived-oracle fixtures landed; live run pending | E2 ground-truth |
| 124 DERIBITFIX (`correction`) | evidence-gated; not started (no dir) | Deribit correction |

Plus the worker-architecture rebuild (dedicated backfill worker) + test-DB
retention hygiene (the recurring python fence-test flake root cause), founder-chosen
broad scope. Seed: `milestones/v1.13-infra-ROADMAP-SEED.md`.

## Verification basis

- **Per-phase close pipeline:** each phase red-teamed by a fresh-context Fable
  agent; findings fixed fail-loud (P115 economic-oracle discipline held —
  money-math oracles pin economics, never the impl's own formula).
- **CI green at land:** all required `frontend` aggregator jobs + `python` +
  `sql-tests` + `secret-scan` + `contracts` green on the PR #623 main-push run
  (advisory e2e-red only — the deferred factsheet spec). Full analytics suite
  4050 passed; tsc/lint clean.
- **Migrations verified in prod:** exchange-CHECK widen (119) + claim kind-filter
  (123-02, `20260719073701`) both applied and confirmed on prod DB
  (`khslejtfbuezsmvmtsdn`).
- **Deploy verified:** Vercel healthy; Railway force-deployed via `railway up`
  (Railway skipped the auto-deploy on advisory-e2e-red main CI) — `/health` ok
  post-deploy.

## Deferred / known items at close

- All items in the re-home table above (founder-gated ops, tracked in v1.13 seed).
- Standing latent bugs (independent of sFOX): quantstats price-detection Sharpe
  sign-flip on the strategy-analytics path; blend √252 unknown-asset_class
  annualization. Founder-decision-gated (change displayed numbers).
- `allocator_equity_snapshots` legacy retirement — unblocked only post-FLIP-live.

## Verdict

**Foundation close accepted.** The sFOX foundation is code-complete and shipped
dormant with zero prod impact; every remaining go-live gate is a founder-gated op
or an evidence-gated defect, explicitly re-homed to v1.13 with a continuous plan.
No silent gaps. Next: `/gsd:new-milestone` (v1.13) absorbs the seed.
