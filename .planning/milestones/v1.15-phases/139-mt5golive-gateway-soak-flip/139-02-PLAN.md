---
phase: 139-mt5golive-gateway-soak-flip
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/mt5-gateway/railway-gateway.md
  - deploy/mt5-gateway/fly.toml
  - deploy/mt5-gateway/docker-compose.yml
  - docs/runbooks/mt5-go-live.md
  - docs/runbooks/README.md
autonomous: true
requirements: [MT5GW-01, MT5GOLIVE-01, MT5GOLIVE-02]

must_haves:
  truths:
    - "A founder can execute the entire go-live from docs/runbooks/mt5-go-live.md alone: decision matrix → stand-up → VNC install → soak → explicit GATE-CHECK → flip → prod verify → rollback"
    - "The runbook makes private-network-only RPyC binding NON-NEGOTIABLE (the unauthenticated-RCE finding) — no step ever exposes :8001 publicly"
    - "The runbook states the auto-update correction: the terminal self-updates and CANNOT be disabled; only the image tag+digest is pinnable; the soak window is the parity-break detector"
    - "The host decision presents Railway co-locate (NEW dual-stack env) as PRIMARY, VPS+Tailscale as FALLBACK, Fly as secondary — with the IPv4-bind-vs-IPv6-private constraint explained; the actual pick + the A2 dual-stack check are founder calls"
    - "The flip is exact copy-paste commands (Railway variables + verify-not-SKIPPED; Vercel env add ×2 + mandatory redeploy from clean main) with a both-flags-to-empty rollback"
  artifacts:
    - path: "docs/runbooks/mt5-go-live.md"
      provides: "The go-live runbook: matrix, stand-up, VNC pattern, credential isolation, gateway-IP allowlisting, soak, GATE-CHECK, flip, rollback"
      min_lines: 150
    - path: "deploy/mt5-gateway/railway-gateway.md"
      provides: "PRIMARY config: Railway co-locate service, dual-stack env requirement, no public domain, gateway.railway.internal:8001"
      contains: "gmag11/metatrader5_vnc:2.3"
    - path: "deploy/mt5-gateway/docker-compose.yml"
      provides: "VPS fallback: loopback-bound ports + Tailscale note + persistent named volume"
      contains: "127.0.0.1:8001:8001"
    - path: "deploy/mt5-gateway/fly.toml"
      provides: "Secondary fallback: [[mounts]] /config, NO services.ports (private-only), Tailscale note"
      contains: "[[mounts]]"
  key_links:
    - from: "docs/runbooks/mt5-go-live.md"
      to: "analytics-service/scripts/mt5_soak.py"
      via: "the soak procedure invokes the 139-01 runner"
      pattern: "mt5_soak"
    - from: "docs/runbooks/mt5-go-live.md"
      to: "analytics-service/services/job_worker.py"
      via: "worker env contract MT5_GATEWAY_HOST/MT5_GATEWAY_PORT/MT5_ENABLED (:926)"
      pattern: "MT5_GATEWAY_HOST"
    - from: "deploy/mt5-gateway/railway-gateway.md"
      to: "docs/runbooks/mt5-go-live.md"
      via: "digest-pin instruction (record sha256 at stand-up)"
      pattern: "sha256"
---

<objective>
Land the committed go-live artifacts: the three deployment config templates
(`deploy/mt5-gateway/`) and the founder runbook (`docs/runbooks/mt5-go-live.md`)
containing the host decision matrix, the one-time VNC install pattern, credential
isolation, the soak procedure, the explicit GATE-CHECK list, the exact flag-flip
commands, and the trivial rollback.

Purpose: MT5GW-01 + MT5GOLIVE-01/02 are founder LIVE ops — the buildable half is a
runbook so complete the founder never has to re-derive a decision, grounded ONLY in
the VERIFIED image facts (gmag11/metatrader5_vnc:2.3, RPyC :8001 bound IPv4 0.0.0.0,
noVNC :3000, /config volume, CUSTOM_USER/PASSWORD/mt5server_port) and honoring the two
research corrections (Railway-primary; auto-update NOT disableable).

Output: 3 config templates + the runbook + a README index entry, all committed in-repo
(mirroring docs/runbooks/sfox-go-live.md, the exact precedent).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-mt5golive-gateway-soak-flip/139-RESEARCH.md
@docs/runbooks/sfox-go-live.md
@docs/runbooks/railway-worker.md
@analytics-service/services/job_worker.py
@analytics-service/services/closed_sets.py
</context>

<interfaces>
<!-- The env contract the runbook documents — extracted from code, do not invent. -->

From analytics-service/services/job_worker.py:909-938 (_make_mt5_session):
```python
host = os.getenv("MT5_GATEWAY_HOST")        # e.g. gateway.railway.internal
port_raw = os.getenv("MT5_GATEWAY_PORT")    # e.g. 8001
# missing → RuntimeError "server misconfiguration, never a credential failure"
```

From analytics-service/services/closed_sets.py:107-109:
```python
def mt5_enabled_server() -> bool:
    return (os.getenv("MT5_ENABLED") or "").strip().lower() == "true"  # fail-closed
```

Vercel plane (138 mechanics): `MT5_ENABLED` gates isMt5EnabledServer (validate-and-encrypt
route, src/lib/closed-sets.ts); `NEXT_PUBLIC_MT5_ENABLED` (strict "true") shows the wizard
card and is BUILD-TIME inlined → redeploy mandatory (Pitfall 3).

Verified image facts (139-RESEARCH.md, HIGH confidence — repo start.sh + Docker Hub + README):
image `gmag11/metatrader5_vnc:2.3` (= latest, linux/amd64 only, ~1.57 GB compressed);
RPyC via `python3 -m mt5linux --host 0.0.0.0 -p $mt5server_port` with `mt5server_port=8001`
(IPv4-only bind); noVNC `:3000`; persistent `/config`; env `CUSTOM_USER`/`PASSWORD`/
`mt5server_port`/`MT5_CMD_OPTIONS`; "MetaTrader program is updated independently from image".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: deploy/mt5-gateway/ — three host config templates (Railway PRIMARY, VPS fallback, Fly secondary)</name>
  <files>deploy/mt5-gateway/railway-gateway.md, deploy/mt5-gateway/docker-compose.yml, deploy/mt5-gateway/fly.toml</files>
  <read_first>
    139-RESEARCH.md "Code Examples" (the three vetted templates — transcribe them, do not
    invent deployment APIs), "Common Pitfalls" 1/4/6, "Runtime State Inventory"
  </read_first>
  <action>
    Create the deploy/mt5-gateway/ directory with the three templates from 139-RESEARCH.md
    Code Examples, adapted verbatim — every port/volume/env fact is VERIFIED; where a
    host-side detail is not (e.g. the exact Railway volume UI path, broker IP policy),
    tag it [ASSUMED] inline for founder confirmation rather than asserting.

    railway-gateway.md (PRIMARY, per the research correction — NOT Fly): a NEW service in
    the SAME Railway project as the worker, deployed from Docker image
    gmag11/metatrader5_vnc:2.3 with an instruction to PIN the sha256 digest at stand-up;
    /config volume (Railway single-volume-per-service, single-writer, brief redeploy
    downtime — Pitfall 6); NO public domain — reachable ONLY at
    gateway.railway.internal:8001; env CUSTOM_USER/PASSWORD (gateway secret, never git) +
    mt5server_port=8001; the DUAL-STACK requirement stated loudly: the RPyC server binds
    IPv4 0.0.0.0 while legacy (pre-2025-10-16) Railway private networking is IPv6-only —
    the environment MUST be dual-stack (post-2025-10-16) or the worker cannot connect
    (Pitfall 1; the A2 founder check); the one-time VNC :3000 access via railway
    port-forward or a TEMPORARY public domain torn down after install; the worker-side
    env to set at flip (MT5_GATEWAY_HOST=gateway.railway.internal, MT5_GATEWAY_PORT=8001,
    MT5_ENABLED=true).

    docker-compose.yml (VPS FALLBACK — Hetzner/Contabo amd64, the clean-static-IP option
    for broker allowlisting): the researched compose verbatim — image pinned by tag with
    an @sha256 pin comment, restart unless-stopped, CUSTOM_USER/PASSWORD/mt5server_port
    from env, named volume mt5_config:/config, and BOTH port bindings on loopback ONLY
    (127.0.0.1:3000:3000 and 127.0.0.1:8001:8001 — RPyC never on the public interface);
    a comment block on the Tailscale sidecar/host-daemon pattern (worker joins the
    tailnet; RPyC exposed only on the tailscale interface; firewall drops :8001 on the
    public NIC).

    fly.toml (SECONDARY — documented as offering no advantage over the VPS since a
    cross-provider tunnel is needed either way): the researched toml verbatim — app name,
    primary_region ams, [build] image pin, [[vm]] shared-cpu-2x/2gb, [[mounts]]
    mt5_config → /config, and NO [[services.ports]]/[http_service] section at all
    (private-only, no public IP), with comments on the Tailscale requirement to reach the
    Railway worker and `fly ips allocate-v4` for a dedicated broker-allowlist egress IP.
  </action>
  <verify>
    <automated>grep -l "gmag11/metatrader5_vnc:2.3" deploy/mt5-gateway/railway-gateway.md deploy/mt5-gateway/docker-compose.yml deploy/mt5-gateway/fly.toml | wc -l | grep -qx 3 && grep -q "127.0.0.1:8001:8001" deploy/mt5-gateway/docker-compose.yml && grep -q '\[\[mounts\]\]' deploy/mt5-gateway/fly.toml && ! grep -q "services.ports" deploy/mt5-gateway/fly.toml && grep -qi "dual-stack" deploy/mt5-gateway/railway-gateway.md && grep -q "gateway.railway.internal" deploy/mt5-gateway/railway-gateway.md</automated>
  </verify>
  <acceptance_criteria>
    All three templates name the verified image tag exactly once or more; docker-compose
    binds BOTH :3000 and :8001 to 127.0.0.1 only; fly.toml mounts /config and contains
    zero public service-port sections; railway-gateway.md states the dual-stack (Pitfall 1)
    constraint, the internal-only hostname, and the digest-pin instruction; unverifiable
    host details carry [ASSUMED] tags. No hallucinated deployment APIs.
  </acceptance_criteria>
  <done>
    deploy/mt5-gateway/ exists with the three grounded templates; the automated grep set
    passes; the founder can hand any one file to its host without cross-referencing the
    research doc.
  </done>
</task>

<task type="auto">
  <name>Task 2: docs/runbooks/mt5-go-live.md — the founder go-live runbook + README index</name>
  <files>docs/runbooks/mt5-go-live.md, docs/runbooks/README.md</files>
  <read_first>
    docs/runbooks/sfox-go-live.md (the structural precedent: Owner/Risk header, standing
    abort-to-rollback rule, per-step Verify + Abort path), 139-RESEARCH.md (decision
    matrix, Pattern 2 VNC steps, Pattern 3 credential isolation, Pitfalls 1–6, GATE-CHECK,
    flag-flip commands), docs/runbooks/railway-worker.md (redeploy/skip-on-red-CI cross-ref)
  </read_first>
  <action>
    Write docs/runbooks/mt5-go-live.md modeled on sfox-go-live.md (Owner: founder; every
    leg a LIVE op; standing rule: abort at ANY step → jump to ROLLBACK; each step ends
    with an explicit Verify + Abort path). Steps, in order:

    Step 0 — HOST DECISION (MT5GOLIVE-01, founder call): the research decision matrix —
    Railway co-locate in a NEW DUAL-STACK env = PRIMARY (the ONLY zero-tunnel option; the
    RPyC server binds IPv4 0.0.0.0 and legacy IPv6-only railway.internal cannot reach it —
    Pitfall 1), VPS + Tailscale = FALLBACK (clean single static IP, best for broker
    allowlisting), Fly = secondary (needs the same cross-provider tunnel, no advantage).
    Include the explicit A2 check: confirm the target Railway environment is
    post-2025-10-16 dual-stack BEFORE standing up; if legacy, create a new environment or
    take the VPS fallback. Do NOT regress to the superseded "Fly reuse-ops vs Railway"
    framing — Railway co-locate is primary per the 2026-07-24 research correction.

    Step 1 — STAND-UP (MT5GW-01): deploy per the chosen deploy/mt5-gateway/ template;
    record the image sha256 DIGEST in the runbook's provenance line at stand-up. State
    the auto-update correction LOUDLY: the MT5 terminal self-updates independently of the
    image ("MetaTrader program is updated independently from image" — gmag11 README) and
    CANNOT be disabled; there is NO switch to chase; only the image tag+digest is
    pinnable; the soak window is the parity-break detector for a terminal-update
    regression. State the HARD CONSTRAINT as non-negotiable: the RPyC bridge (:8001) is
    an UNAUTHENTICATED arbitrary-remote-code channel — PRIVATE NETWORK ONLY, never a
    public port, never a public domain, no exceptions (the Phase-134 T-134-03 finding).

    Step 2 — ONE-TIME VNC INSTALL + INVESTOR LOGIN (MT5GW-01): the research Pattern-2
    steps — /config on the persistent volume + CUSTOM_USER/PASSWORD set first; reach
    noVNC :3000 ONCE via port-forward/SSH-tunnel or a TEMPORARY password-gated domain;
    install the terminal, add the broker account with the INVESTOR (read-only) password,
    enable save-account/auto-login; verify the VNC-displayed server clock against UTC to
    confirm the broker-server-time offset (feeds MT5_SOAK_SERVER_OFFSET_MIN and closes
    the 134 leg-4 [ASSUMED] estimate); TEAR DOWN public :3000 access afterward; verify
    /config persistence by restarting the service and confirming the saved login survives.

    Step 3 — CREDENTIAL ISOLATION + BROKER ALLOWLISTING (MT5GOLIVE-01): the gateway holds
    ONLY the one investor login it syncs; the worker passes creds per-sync via the
    encrypted slots (login→api_key, investor-pw→api_secret, server→passphrase — the 135
    convention); IP-allowlisting, IF the broker requires it, keys off the GATEWAY egress
    IP — NOT the worker's Railway static egress set (the anti-sFOX-muscle-memory callout,
    Pitfall 4; on Railway the gateway egress rotates within a set → whitelist all; a VPS
    gives one stable IP); most brokers do not IP-restrict investor logins [ASSUMED A1 —
    confirm with the chosen broker].

    Step 4 — SOAK (MT5GOLIVE-02): run `cd analytics-service && python -m scripts.mt5_soak`
    daily over 5–10 business days [ASSUMED A5, extend on any red] with the MT5_SPIKE_*
    env + the confirmed MT5_SOAK_SERVER_OFFSET_MIN; every run within tolerance
    max($1, 1e-6·|equity|); an INCONCLUSIVE (empty-ledger) run never counts as green;
    results into the soak-log table in analytics-service/docs/mt5-spike-gonogo.md +
    evidence JSONs under analytics-service/docs/evidence/.

    Step 5 — GATE-CHECK (explicit, never assumed): the research checklist as literal
    "[ ]" items under a "## GATE-CHECK" heading — 134 spike recorded GO; 135
    test_mt5_exchange_boundary.sql green + constraint migration applied/verified on PROD +
    TS route/parity vitest + Python lockstep green; 136 test_mt5_derive_branch.py green
    incl. the reconciliation gate + $2-drift negative control + process_key api_verified +
    √252 mutation guard; 137 hung-terminal timeout / restart-on-timeout / per-terminal
    lock / login==expected bracket regressions green; 138 mt5-badge.spec.ts registered +
    green in the BLOCKING e2e-seeded list + byte-identity/envelope + go-dark tests green;
    SOAK every run in the window within tolerance; CI full analytics pytest + full vitest
    coverage gate + e2e-seeded green on main; NET worker reaches
    gateway.railway.internal:8001 privately (dual-stack confirmed); Railway deploy
    verified SUCCESS not SKIPPED.

    Step 6 — FLIP (MT5GOLIVE-02): the exact commands from the research in fenced blocks —
    Railway plane: `railway variables --set MT5_ENABLED=true --set
    MT5_GATEWAY_HOST=gateway.railway.internal --set MT5_GATEWAY_PORT=8001`, then
    `railway up` from the repo on the intended clean main commit, then
    `railway deployment list --json` asserting status=SUCCESS + the right commitHash —
    Railway "Wait for CI" silently SKIPS the deploy on a red main check-suite (Pitfall 5;
    cross-reference railway-worker.md for the recovery). Vercel plane:
    `vercel env add MT5_ENABLED production` + `vercel env add NEXT_PUBLIC_MT5_ENABLED
    production`, then a MANDATORY redeploy from a clean MAIN checkout (`vercel redeploy
    <prod-url>` or `vercel --prod`) because NEXT_PUBLIC_* is build-time inlined — an
    un-redeployed env change is a silent no-op (Pitfall 3, the exact sFOX precedent).
    Note the VPS/Fly variant: MT5_GATEWAY_HOST is the tailnet address instead.

    Step 7 — PROD VERIFY (MT5GOLIVE-02): a user connects an MT5 key end-to-end through
    the wizard; the api_verified strategy renders LIVE across factsheet/discovery/edit
    across roles (owner/allocator/admin/anon); the 138 all-roles e2e is the automated
    proxy — the live prod check is the founder's post-flip proof; test the WHOLE flow E2E
    after the flip (the v1.10 flag-flip lesson). Prod URL is quantalyze.xyz.

    Step 8 — ROLLBACK (the standing abort target): both flags back to empty
    (`railway variables --set MT5_ENABLED=`; `vercel env rm MT5_ENABLED production`;
    `vercel env rm NEXT_PUBLIC_MT5_ENABLED production`) + redeploy both planes →
    byte-identical dark state (the 138 flag-OFF byte-identity guarantee makes rollback
    riskless); MT5_GATEWAY_HOST/PORT may stay set — harmless while MT5_ENABLED is empty.

    Finally add a one-line mt5-go-live.md entry to the docs/runbooks/README.md index,
    matching its existing format (surgical — touch nothing else in it).
  </action>
  <verify>
    <automated>test -f docs/runbooks/mt5-go-live.md && grep -qi "PRIVATE NETWORK ONLY" docs/runbooks/mt5-go-live.md && grep -q "GATE-CHECK" docs/runbooks/mt5-go-live.md && grep -c "railway variables --set MT5_ENABLED=true" docs/runbooks/mt5-go-live.md | grep -qx 1 && [ "$(grep -c NEXT_PUBLIC_MT5_ENABLED docs/runbooks/mt5-go-live.md)" -ge 2 ] && grep -qi "rollback" docs/runbooks/mt5-go-live.md && grep -q "sha256" docs/runbooks/mt5-go-live.md && grep -qiE "updated independently|cannot be disabled" docs/runbooks/mt5-go-live.md && [ -z "$(grep -i 'auto-update' docs/runbooks/mt5-go-live.md | grep -viE 'cannot|not disable|no .*switch|independen|self-updat|detector')" ] && grep -q "mt5_soak" docs/runbooks/mt5-go-live.md && grep -q "mt5-go-live" docs/runbooks/README.md</automated>
  </verify>
  <acceptance_criteria>
    Runbook contains, greppably: the PRIVATE NETWORK ONLY hard constraint; a GATE-CHECK
    section whose "[ ]" items cover 134/135/136/137/138 + SOAK + CI + NET + deploy-SUCCESS;
    the exact Railway flip command exactly once; NEXT_PUBLIC_MT5_ENABLED at least twice
    (flip + rollback); a ROLLBACK section; a digest-pin (sha256) instruction; the
    auto-update correction — and NO line instructs disabling auto-update (every
    "auto-update" mention is the correction/detector context). Railway co-locate is the
    stated PRIMARY. The soak step invokes mt5_soak. README index updated with one line.
  </acceptance_criteria>
  <done>
    Automated grep set passes; the runbook reads start-to-finish as an executable founder
    procedure with per-step Verify + Abort paths, mirroring the sfox-go-live.md shape.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → gateway container | noVNC :3000 (one-time, password-gated, torn down); RPyC :8001 must NEVER cross this boundary |
| worker (Railway) → gateway RPyC :8001 | unauthenticated RCE channel; transport privacy = Railway internal mesh or Tailscale |
| gateway container → broker | investor login; the broker sees the GATEWAY egress IP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-05 | Elevation of Privilege | RPyC :8001 publicly exposed | mitigate | runbook hard constraint "PRIVATE NETWORK ONLY" (grep-gated); compose binds 127.0.0.1; fly.toml has no service ports; railway doc mandates no public domain |
| T-139-06 | Information Disclosure | VNC :3000 left public post-install | mitigate | runbook Step 2 explicit tear-down step + verify |
| T-139-07 | Spoofing | gateway secrets (VNC PASSWORD) in git | mitigate | templates read from env/host secret store; never literal values in committed files |
| T-139-08 | Tampering | silent image re-tag changes the base | mitigate | digest-pin instruction (record sha256 at stand-up) in runbook + templates |
| T-139-09 | Denial of Service | broker allowlist keyed off the WRONG (worker) IP | mitigate | Pitfall-4 callout: gateway egress IP, not worker egress; [ASSUMED A1] flag for founder confirmation |
| T-139-10 | Tampering (integrity) | terminal self-update breaks parity mid-soak | accept | not disableable (verified); soak window is the detector; documented as managed risk |
| T-139-SC | Tampering | supply chain | accept | no package installs; prebuilt image pulled by pinned tag+digest (RESEARCH.md audit) |
</threat_model>

<verification>
- Both tasks' grep sets green (run them verbatim).
- `git status` shows only the five declared files created/modified.
- Read-through: every runbook step ends with a Verify + an Abort path; abort always routes to Step 8.
- No step anywhere instructs disabling terminal auto-update; Railway co-locate is primary; Fly is explicitly secondary.
</verification>

<success_criteria>
- The buildable halves of MT5GW-01 + MT5GOLIVE-01 + MT5GOLIVE-02 (runbook, templates, gate-check, flip checklist, rollback) are committed in-repo.
- All templates grounded in VERIFIED image facts; unverifiable details tagged [ASSUMED].
- Both research corrections honored and greppably enforced.
- Nothing claims any live leg executed — execution is plan 139-03's checkpoints.
</success_criteria>

<output>
Create `.planning/phases/139-mt5golive-gateway-soak-flip/139-02-SUMMARY.md` when done
</output>
