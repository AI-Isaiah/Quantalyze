---
phase: 139-mt5golive-gateway-soak-flip
plan: 03
type: execute
wave: 2
depends_on: [139-01, 139-02]
files_modified: []
autonomous: false
requirements: [MT5GW-01, MT5GOLIVE-01, MT5GOLIVE-02]

must_haves:
  truths:
    - "Every founder LIVE leg (hosting decision + A2 check, gateway stand-up + VNC install, broker onboard + soak run, gate-check + flag flip + prod verify) exists as a blocking human-verify checkpoint — none is ever claimed done by an autonomous run"
    - "A deferred/skipped checkpoint is recorded human_needed-OPEN in the SUMMARY (the 134-03 / 121-03 / 130-01 precedent), never marked complete"
    - "The execution path for every leg is the committed runbook docs/runbooks/mt5-go-live.md + the deploy/mt5-gateway/ templates + scripts/mt5_soak.py — no leg requires re-deriving a decision"
  artifacts: []
  key_links:
    - from: ".planning/phases/139-mt5golive-gateway-soak-flip/139-03-SUMMARY.md"
      to: "docs/runbooks/mt5-go-live.md"
      via: "each checkpoint resolution (done or human_needed-OPEN) cites the runbook step it maps to"
      pattern: "mt5-go-live"
---

<objective>
Model the three founder LIVE-ops legs of Phase 139 as blocking human-verify checkpoints:
(1) hosting decision + gateway stand-up (MT5GW-01, MT5GOLIVE-01), (2) broker investor
account onboarding + the soak run (MT5GOLIVE-02), (3) the explicit gate-check + flag flip
+ prod verification (MT5GOLIVE-02).

Purpose: all three phase requirements are founder LIVE ops an autonomous agent CANNOT
perform (a hosting decision, a running Wine gateway, a real broker account, a multi-day
soak, LIVE env flips). Per the standing pattern (134's live spike legs, 136-05, 121-03,
130-01): the buildable artifacts landed in wave 1 (plans 139-01/139-02); this plan holds
the execution gates. A checkpoint the founder defers is recorded human_needed-OPEN —
a skipped gate is NEVER claimed done.

Output: 139-03-SUMMARY.md recording each checkpoint's resolution (executed-with-evidence,
or human_needed-OPEN with the runbook as the execution path).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/139-mt5golive-gateway-soak-flip/139-RESEARCH.md
@docs/runbooks/mt5-go-live.md
@analytics-service/docs/mt5-spike-gonogo.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: FOUNDER — hosting decision + prod gateway stand-up + one-time VNC install (MT5GW-01, MT5GOLIVE-01)</name>
  <read_first>docs/runbooks/mt5-go-live.md Steps 0–3; deploy/mt5-gateway/ (the template for the chosen host)</read_first>
  <action>
    PAUSE. The executor performs NO live op here — it presents runbook Steps 0–3 + the
    matching deploy/mt5-gateway/ template to the founder and waits for the resume signal.
    Everything automatable already shipped in wave 1; this leg needs a human because it
    involves a hosting decision, a paid host provisioning, an interactive VNC session
    with real broker credentials, and a private-network topology only the founder can
    stand up.
  </action>
  <what-built>
    The go-live runbook (decision matrix with the IPv4-bind-vs-IPv6-private constraint;
    Railway co-locate PRIMARY / VPS+Tailscale FALLBACK / Fly secondary), the three host
    config templates grounded in the verified gmag11/metatrader5_vnc:2.3 facts, the VNC
    install pattern, and the credential-isolation + gateway-IP-allowlisting rules. All
    committed; nothing is running yet.
  </what-built>
  <how-to-verify>
    Execute runbook Steps 0–3 and confirm each step's Verify line:
    1. Step 0 — decide the host. If Railway co-locate: run the A2 check FIRST (the target
       environment must be post-2025-10-16 dual-stack, else the worker cannot reach the
       image's IPv4 0.0.0.0 bind over railway.internal). Record the decision + the A2
       result in the runbook.
    2. Step 1 — stand up the gateway from the matching deploy/mt5-gateway/ template;
       record the image sha256 digest in the runbook provenance line; confirm the RPyC
       bridge (:8001) is reachable ONLY over the private network (no public port, no
       public domain — non-negotiable).
    3. Step 2 — one-time VNC install + INVESTOR (read-only) login; enable auto-login;
       confirm the broker-server-clock-vs-UTC offset (closes the 134 leg-4 estimate;
       feeds MT5_SOAK_SERVER_OFFSET_MIN); TEAR DOWN public :3000 access; restart the
       service and confirm the saved login survives in /config.
    4. Step 3 — confirm credential isolation (gateway holds only the one investor login)
       and, if the broker IP-restricts, that the allowlist keys off the GATEWAY egress IP,
       not worker egress.
    5. Prove private reachability from the worker (runbook NET gate item), e.g.
       railway ssh from the worker service connecting to the gateway host:8001.
  </how-to-verify>
  <resume-signal>
    Type "gateway-up" with the host decision + A2 result + digest + reachability evidence,
    or "defer" — the checkpoint is then recorded human_needed-OPEN (MT5GW-01 +
    MT5GOLIVE-01 NOT satisfied; the runbook remains the execution path).
  </resume-signal>
  <acceptance_criteria>
    Either founder evidence for Steps 0–3 (decision, A2, digest, private-only :8001,
    torn-down :3000, /config persistence, offset confirmed) — or an explicit
    human_needed-OPEN record. NEVER an inferred pass. This checkpoint is never
    auto-approved regardless of workflow.auto_advance.
  </acceptance_criteria>
  <verify>Founder resume signal received and recorded verbatim in the SUMMARY with its evidence, or the defer recorded as human_needed-OPEN citing runbook Steps 0–3.</verify>
  <done>Checkpoint resolved to exactly one of: gateway-up-with-evidence / human_needed-OPEN. No live claim without founder evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: FOUNDER — real broker investor account onboarded + soak run to parity (MT5GOLIVE-02 first half)</name>
  <read_first>docs/runbooks/mt5-go-live.md Step 4; analytics-service/docs/mt5-spike-gonogo.md "Soak log" section</read_first>
  <action>
    PAUSE. The executor performs NO live op — it presents runbook Step 4 + the soak-log
    table to the founder and waits. This leg needs a human because it requires a real
    broker investor account (a hard human_needed blocker with no fallback — 139-RESEARCH
    Environment Availability) and a multi-business-day wall-clock window no agent can
    compress.
  </action>
  <what-built>
    scripts/mt5_soak.py (offline-proven: parity within max($1, 1e-6·|equity|), $2-drift
    negative control, fail-loud on error reads, INCONCLUSIVE on empty ledgers, sanitized
    log records) and the soak-log table in the go/no-go doc with human_needed placeholders.
  </what-built>
  <how-to-verify>
    1. Onboard the real broker investor account (login + investor password + exact server
       string). The soak runs standalone against the gateway with the MT5_SPIKE_* env —
       no prod flag flip needed for this step.
    2. Run `cd analytics-service && python -m scripts.mt5_soak` once per day over the
       5–10 business-day window [ASSUMED A5 — confirm or adjust the window]. Every run
       must exit 0 with parity_ok=true; an INCONCLUSIVE (empty-ledger) run never counts;
       extend the window on any red run.
    3. Fill the soak-log table in analytics-service/docs/mt5-spike-gonogo.md per run;
       evidence JSONs land in analytics-service/docs/evidence/.
    4. Same session (per 139-CONTEXT's fold-in): record the live confirmations of the
       136-05 DEAL_TYPE ambiguous-middle acknowledgment and the WR-03 master-rejection
       retcode in the go/no-go doc.
  </how-to-verify>
  <resume-signal>
    Type "soak-green" with the filled soak-log table (all runs within tolerance), or
    "soak-red" (window extends; phase blocked), or "defer" — recorded human_needed-OPEN
    (MT5GOLIVE-02 NOT satisfied).
  </resume-signal>
  <acceptance_criteria>
    Either a filled soak-log with EVERY run parity_ok=true over the window — or an
    explicit human_needed-OPEN / soak-red record. An unfilled or partial table is OPEN,
    never green. Never auto-approved.
  </acceptance_criteria>
  <verify>Founder resume signal received; the soak-log table state (filled/partial/untouched) cross-checked against the claimed resolution before recording it.</verify>
  <done>Checkpoint resolved to exactly one of: soak-green-with-filled-table / soak-red / human_needed-OPEN. A partial table can never resolve green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: FOUNDER — explicit GATE-CHECK → flag flip LIVE → prod verification (MT5GOLIVE-02 second half)</name>
  <read_first>docs/runbooks/mt5-go-live.md Steps 5–8</read_first>
  <action>
    PAUSE. The executor performs NO live op — it presents runbook Steps 5–8 to the
    founder and waits. This leg needs a human because the flip is a LIVE prod env op on
    Railway + Vercel gated on Tasks 1–2 of this plan resolving green; the executor must
    treat any OPEN prior checkpoint as a structural block, not a formality.
  </action>
  <what-built>
    The GATE-CHECK checklist (134/135/136/137/138 gates + SOAK + CI + NET + deploy-SUCCESS),
    the exact flip commands (Railway variables + verify-not-SKIPPED; Vercel env add ×2 +
    mandatory redeploy from clean main), and the both-flags-to-empty rollback.
  </what-built>
  <how-to-verify>
    1. Step 5 — tick EVERY GATE-CHECK item green with evidence (never assumed). Tasks 1
       and 2 of this plan must be resolved green (not OPEN) — the flip is structurally
       blocked on them.
    2. Step 6 — execute the flip exactly as written: Railway
       (MT5_ENABLED/MT5_GATEWAY_HOST/MT5_GATEWAY_PORT) + verify the deploy is SUCCESS not
       SKIPPED (Wait-for-CI gotcha); Vercel (MT5_ENABLED + NEXT_PUBLIC_MT5_ENABLED) +
       MANDATORY redeploy from clean main (build-time inlining).
    3. Step 7 — prod verify on quantalyze.xyz: connect an MT5 key end-to-end through the
       wizard; the api_verified strategy renders live across factsheet/discovery/edit
       across roles; the whole flow E2E, not one surface.
    4. Any failure at any point → Step 8 ROLLBACK (both flags to empty + redeploy both
       planes → byte-identical dark state) and record what broke.
  </how-to-verify>
  <resume-signal>
    Type "live" with the ticked gate-check + flip evidence + prod-verify evidence
    (MT5GOLIVE-02 satisfied; milestone finish line reached), or "rolled-back" with the
    failure record, or "defer" — recorded human_needed-OPEN (ships flag-OFF for now; the
    runbook remains the execution path).
  </resume-signal>
  <acceptance_criteria>
    The flip is NEVER executed or claimed with any GATE-CHECK item non-green or any prior
    checkpoint OPEN. Resolution is exactly one of: live-with-evidence /
    rolled-back-with-record / human_needed-OPEN. Never auto-approved.
  </acceptance_criteria>
  <verify>Founder resume signal received; if "live", the gate-check evidence and Tasks 1–2 resolutions are cross-checked green before MT5GOLIVE-02 is marked satisfied.</verify>
  <done>Checkpoint resolved to exactly one of: live-with-evidence / rolled-back-with-record / human_needed-OPEN. MT5GOLIVE-02 is marked satisfied ONLY on "live".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| autonomous executor → founder LIVE ops | the executor may prepare and verify artifacts but must never perform or claim the live legs |
| gate-check → prod flag state | flipping before gates green exposes a broken/unproven trust surface (api_verified) to users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-139-11 | Repudiation | checkpoint resolutions | mitigate | SUMMARY records each leg as evidence-backed done OR human_needed-OPEN; an unfilled gate can never read as passed (the gonogo-doc convention) |
| T-139-12 | Tampering (integrity) | premature flip before gates green | mitigate | Task 3 structurally blocked on Tasks 1–2 + the explicit GATE-CHECK; blocking-human checkpoints, never auto-approved |
| T-139-13 | Spoofing | wrong-flag-state trust leak (MT5 offered while unproven) | mitigate | fail-closed flags (strict "true"), both-to-empty rollback rehearsed in the runbook; 138 byte-identity guarantee |
| T-139-14 | Denial of Service | flip lands on a SKIPPED Railway deploy → half-flipped state | mitigate | Pitfall-5 verify (deployment SUCCESS + commitHash) is a gate-check item |
</threat_model>

<verification>
- Each checkpoint resolved to exactly one of: done-with-evidence / human_needed-OPEN / rolled-back.
- No requirement (MT5GW-01, MT5GOLIVE-01, MT5GOLIVE-02) marked satisfied while its checkpoint is OPEN.
- The SUMMARY cites the runbook step each resolution maps to.
</verification>

<success_criteria>
- All three founder legs exist as blocking checkpoints and are honestly resolved.
- If all three resolve "live": MT5 is usable in prod, api_verified renders across roles — the v1.15 finish line.
- If any defer: the phase closes with those legs recorded human_needed-OPEN and the committed runbook/templates/soak script as the complete execution path — exactly the 130-01 precedent, never a false "done".
</success_criteria>

<output>
Create `.planning/phases/139-mt5golive-gateway-soak-flip/139-03-SUMMARY.md` when done
</output>
