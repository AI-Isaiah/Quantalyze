---
phase: 135-mt5src-source-lockstep
plan: 02
type: execute
wave: 2
depends_on: [135-01]
files_modified:
  - supabase/migrations/20260723HHMMSS_mt5_exchange_boundary_checks.sql
  - supabase/tests/test_mt5_exchange_boundary.sql
  - analytics-service/tests/test_boundary_literals_parity.py
  - src/lib/closed-sets.ts
  - src/lib/strategy-sources.ts
autonomous: true
requirements: [MT5SRC-03]

must_haves:
  truths:
    - "The DB admits 'mt5' at EXACTLY the four parity-pinned exchange CHECKs; a bogus value is still rejected (widened, NOT dropped)"
    - "compute_jobs_exchange_check preserves its nullable (IS NULL OR) form"
    - "funding_fees, position_snapshots and the verification_requests VIEW are UNTOUCHED (parity-pinned exclusions)"
    - "TS SUPPORTED_EXCHANGES + STRATEGY_SOURCES widen in the SAME plan as the migration (set-equality vitest parity forces atomicity)"
    - "The migration is applied + RED-guard-verified on the TEST project BEFORE merge; prod apply is inherent to merge"
  artifacts:
    - path: "supabase/migrations/20260723HHMMSS_mt5_exchange_boundary_checks.sql"
      provides: "4-constraint widen with self-verify DO blocks, forward-dated timestamp"
      contains: "ADD CONSTRAINT api_keys_exchange_check"
    - path: "supabase/tests/test_mt5_exchange_boundary.sql"
      provides: "RED guard: admit-mt5 / reject-bogus / NULL-preserved per constraint (CI auto-discovers supabase/tests/test_*.sql)"
    - path: "src/lib/closed-sets.ts"
      provides: "SUPPORTED_EXCHANGES + EXCHANGE_DISPLAY widened; isMt5EnabledServer() gate helper"
      contains: "isMt5EnabledServer"
  key_links:
    - from: "src/lib/closed-sets.ts"
      to: "supabase/migrations (newest <table>_<column>_check)"
      via: "check-zod-db-check-parity.test.ts set-equality (onlyInSql/onlyInTs both empty)"
      pattern: "\"mt5\""
    - from: "src/lib/strategy-sources.ts"
      to: "strategies_source_check (this migration)"
      via: "strategy-sources-migration-parity.test.ts set-equality"
      pattern: "\"mt5\""
    - from: "analytics-service/tests/test_boundary_literals_parity.py"
      to: "the new migration file"
      via: "TestMt5MigrationWidensEveryKeyBoundaryCheck byte-parity read"
      pattern: "_MT5_BOUNDARY_MIGRATION"
---

<objective>
Land the RED-guarded constraint-widening migration admitting 'mt5' at EXACTLY the four
parity-pinned exchange CHECK constraints (cloning 20260718182056 verbatim), the SQL RED-guard
test, the Python byte-parity class, AND the TS const lockstep (SUPPORTED_EXCHANGES /
EXCHANGE_DISPLAY / STRATEGY_SOURCES / isMt5EnabledServer) — then MCP-apply to the TEST project and
verify reject-before / accept-after.

Purpose: MT5SRC-03's DB half + the TS enum half that the set-equality vitest parity tests couple to
the migration file (they compare TS sets against the NEWEST migration IN-lists — splitting these
across plans leaves the suite red in between).
Output: 1 migration + 1 SQL gate test + parity class + 3 TS const edits, TEST project verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/135-mt5src-source-lockstep/135-RESEARCH.md
@supabase/migrations/20260718182056_sfox_exchange_boundary_checks.sql
@supabase/tests/test_sfox_exchange_boundary.sql

<interfaces>
CORRECTION baked in (trust RESEARCH over ROADMAP/REQUIREMENTS wording): the migration widens
EXACTLY FOUR CHECK constraints — the "≥5" count upstream is inaccurate. The four, with their final
IN-lists (sfox list + 'mt5'):
1. api_keys_exchange_check           — ('binance','okx','bybit','deribit','sfox','mt5')
2. compute_jobs_exchange_check      — exchange IS NULL OR exchange IN (same 6) — PRESERVE the nullable form
3. strategies_source_check          — 10-value vocab + 'mt5' (→ 11)
4. strategy_verifications_source_check — ('okx','binance','bybit','csv','deribit','sfox','mt5')

DELIBERATE EXCLUSIONS (parity-pinned — widening any of these BREAKS the funding parity tests and
contradicts the deribit/sfox precedent): funding_fees exchange CHECK (MT5 is not a perp-funding
source), position_snapshots exchange CHECK, the verification_requests VIEW + its frozen Phase-19
legacy table (DROP CONSTRAINT on a VIEW errors — never touch).

Supabase projects: TEST = qmnijlgmdhviwzwfyzlc (MCP-apply FIRST), PROD = khslejtfbuezsmvmtsdn
(auto-applies on merge to main via supabase-migrate.yml — NEVER MCP-apply to prod).
Migration tip at plan time: 20260720120000_retention_orphaned_running_window_4h.sql — the new
filename timestamp MUST be current UTC (> tip); backdated timestamps are BLOCKED by
migration-policy.yml.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration file + SQL RED-guard test + Python byte-parity class</name>
  <files>supabase/migrations/20260723HHMMSS_mt5_exchange_boundary_checks.sql, supabase/tests/test_mt5_exchange_boundary.sql, analytics-service/tests/test_boundary_literals_parity.py</files>
  <read_first>
    - supabase/migrations/20260718182056_sfox_exchange_boundary_checks.sql (whole file — clone verbatim, s/sfox/mt5/, s/Phase 119/Phase 135/, s/SFOX-04/MT5SRC-03/)
    - supabase/tests/test_sfox_exchange_boundary.sql (whole file — the 4 DO-block RED-guard template incl. the compute_jobs fixture-coherence notes at :59-67)
    - analytics-service/tests/test_boundary_literals_parity.py:40-75 (_SFOX_BOUNDARY_MIGRATION const) and :171-201 (TestSfoxMigrationWidensEveryKeyBoundaryCheck — the class to mirror)
    - .planning/phases/135-mt5src-source-lockstep/135-RESEARCH.md Q4 section
  </read_first>
  <action>
    First re-verify assumption A5 at execution time: run
    grep -n "api_keys_exchange_check\|compute_jobs_exchange_check\|strategies_source_check\|strategy_verifications_source_check" supabase/migrations/*.sql
    and confirm 20260718182056 is still the newest ADD CONSTRAINT for all four. If ANY later
    migration re-based one, re-base the mt5 IN-list on THAT definition instead and say so in the
    migration header (per D: "Re-base SQL fn before CREATE OR REPLACE" memory rule generalized to
    constraints).

    Create the migration named with the CURRENT UTC 14-digit timestamp (format YYYYMMDDHHMMSS,
    strictly greater than 20260720120000 — replace HHMMSS in the planned filename with the real
    clock; backdating is CI-blocked). Clone 20260718182056 structure exactly: header explaining
    Phase 135 (MT5SRC-03), why EXACTLY FOUR constraints (state the ≥5 correction), the exclusions
    list, the no-backfill safety note, forward-only/no-DOWN rationale; BEGIN; SET lock_timeout='3s';
    then per constraint DROP CONSTRAINT IF EXISTS + ADD CONSTRAINT with the exact IN-lists from the
    interfaces block (nullable IS NULL OR form for compute_jobs) + a self-verify DO block FOREACHing
    every expected value including 'mt5' (and asserting IS NULL survives for compute_jobs); COMMIT;
    then the COMMENT ON COLUMN strategy_verifications.source refresh appending the Phase 135 widen
    to the vocabulary history.

    Create supabase/tests/test_mt5_exchange_boundary.sql cloning test_sfox_exchange_boundary.sql:
    four DO blocks, each proving (a) 'mt5' is ADMITTED and the row exists, (b) 'notanexchange'
    raises check_violation (23514) — widened NOT dropped, plus (c) for compute_jobs: NULL still
    admitted. Keep the gen_random_uuid() fixtures + full cleanup (shared TEST project, concurrent
    CI) and satisfy the two unrelated compute_jobs guards (kind_target_coherence,
    one_inflight_per_kind_strategy) exactly as the sfox test does. Header documents: RED without the
    migration (the mt5 INSERT arms fail the pre-widen CHECK), GREEN after; run with
    psql -v ON_ERROR_STOP=1. CI auto-discovers supabase/tests/test_*.sql (ci.yml:819-823) — no
    workflow edit needed; confirm the discovery glob still exists.

    In analytics-service/tests/test_boundary_literals_parity.py: add a _MT5_BOUNDARY_MIGRATION path
    const (mirroring _SFOX_BOUNDARY_MIGRATION :48-53, pointing at the new filename) and a
    TestMt5MigrationWidensEveryKeyBoundaryCheck class mirroring :171-201 — file exists; each of the
    four _WIDENED_CONSTRAINTS has exactly one ADD CONSTRAINT whose CHECK body contains 'mt5'.
  </action>
  <acceptance_criteria>
    - Migration filename timestamp > 20260720120000 and matches ^[0-9]{14}_mt5_exchange_boundary_checks\.sql$
    - `grep -cE "^[[:space:]]+ADD CONSTRAINT " supabase/migrations/*_mt5_exchange_boundary_checks.sql` returns 4 (exactly four SQL statement lines — the `^[[:space:]]+` anchor excludes header/exclusion COMMENT lines that also contain the substring "ADD CONSTRAINT", which would otherwise false-fail; no funding_fees/position_snapshots/verification_requests widened, only referenced in the exclusions comment)
    - `grep -c "IS NULL OR" supabase/migrations/*_mt5_exchange_boundary_checks.sql` >= 2 (ADD CONSTRAINT + self-verify)
    - supabase/tests/test_mt5_exchange_boundary.sql exists with 4 DO blocks + cleanup
    - `cd analytics-service && pytest tests/test_boundary_literals_parity.py -x -q` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd analytics-service && pytest tests/test_boundary_literals_parity.py -x -q</automated>
  </verify>
  <done>Migration + RED-guard + byte-parity class all in-repo and pinned to each other; exclusions documented; timestamp forward-dated.</done>
</task>

<task type="auto">
  <name>Task 2: TS const lockstep — SUPPORTED_EXCHANGES, EXCHANGE_DISPLAY, STRATEGY_SOURCES, isMt5EnabledServer</name>
  <files>src/lib/closed-sets.ts, src/lib/strategy-sources.ts</files>
  <read_first>
    - src/lib/closed-sets.ts:28-60 (SUPPORTED_EXCHANGES :39, exchangeEnum :41, EXCHANGE_DISPLAY :48-55 with its satisfies clause) and :120-160 (isSfoxEnabledServer :123, the UI_EXCHANGE_CODES block that must NOT be touched)
    - src/lib/strategy-sources.ts:1-30 (STRATEGY_SOURCES :17)
    - src/__tests__/strategy-sources-migration-parity.test.ts + src/__tests__/contracts/check-zod-db-check-parity.test.ts (the set-equality assertions this task must turn green against Task 1's migration)
    - DESIGN.md (confirm no exchange display-label conventions conflict with the "MT5" label — A4)
  </read_first>
  <action>
    In src/lib/closed-sets.ts: append "mt5" to SUPPORTED_EXCHANGES (:39) — exchangeEnum and
    isSupportedExchange widen automatically; add mt5: "MT5" to EXCHANGE_DISPLAY (REQUIRED in the
    same edit — the satisfies Record<SupportedExchange,string> makes the missing label a compile
    error, Pitfall 2); add isMt5EnabledServer() next to isSfoxEnabledServer (:123) with the
    identical strict process.env.MT5_ENABLED === "true" form and a Phase-135 go-dark comment
    (resolved Q-C; the UI-facing NEXT_PUBLIC_MT5_ENABLED is Phase 138 — do not add it). Do NOT
    touch UI_EXCHANGE_CODES / the SFOX_UI_ENABLED-gated arrays / FUNDING_EXCHANGES — mt5 stays OUT
    of every offered/funding surface (the seam ships dark).

    In src/lib/strategy-sources.ts: append "mt5" to STRATEGY_SOURCES (:17), pairing with Task 1's
    strategies_source_check widen (the migration-parity vitest enforces set-equality).

    Then hunt stale set pins: grep -rn '"sfox"' src/__tests__ src/lib src/app --include='*.test.ts'
    --include='*.test.tsx' and update any test asserting the exact 5-value exchange set / 10-value
    source set / EXCHANGE_DISPLAY shape so they include mt5 (update pins to the new truth — do not
    weaken set-equality to containment). create-with-key and composite/add-key auto-accept mt5 via
    isSupportedExchange with zero route edits (their mt5 test cases land in plan 135-04).
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0 (EXCHANGE_DISPLAY satisfies clause proves the paired edit)
    - `npm run test -- check-zod-db-check-parity strategy-sources-migration-parity` green (TS sets == this plan's migration IN-lists; onlyInSql/onlyInTs both empty)
    - grep -n '"mt5"' src/lib/closed-sets.ts shows SUPPORTED_EXCHANGES + EXCHANGE_DISPLAY entries only (no UI_EXCHANGE_CODES/FUNDING hit)
    - isMt5EnabledServer defined with strict === "true"
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm run test -- check-zod-db-check-parity strategy-sources-migration-parity closed-sets</automated>
  </verify>
  <done>TS single source of truth widened in lockstep with the migration; parity vitest green; offered/funding surfaces untouched; server gate helper exported for plan 135-04.</done>
</task>

<task type="auto">
  <name>Task 3: MCP-apply to TEST project — RED before, GREEN after, drift fix, exclusion audit</name>
  <files>supabase/migrations/20260723HHMMSS_mt5_exchange_boundary_checks.sql</files>
  <read_first>
    - .planning/phases/135-mt5src-source-lockstep/135-RESEARCH.md Q5 section (ops rules incl. Pitfall 5 apply_migration now()-stamp drift)
    - supabase/tests/test_mt5_exchange_boundary.sql (Task 1 output — the blocks to execute)
  </read_first>
  <action>
    ALL of this against the TEST project qmnijlgmdhviwzwfyzlc ONLY — never MCP-apply to prod
    khslejtfbuezsmvmtsdn (prod applies automatically on merge to main; that is the documented,
    inherent prod path, watched at ship time).

    1. RED proof BEFORE apply: via Supabase MCP execute_sql on TEST, attempt an api_keys INSERT
       with exchange='mt5' (gen_random_uuid fixture, mirroring the SQL test's block 1a) and assert
       it FAILS with check_violation 23514 (the pre-widen CHECK rejects mt5). Roll back / clean up
       the fixture. Record the observed error in the SUMMARY as the RED half of the guard.
    2. Apply the migration via MCP apply_migration with the migration name matching the file.
    3. Fix the version drift (Pitfall 5): MCP apply_migration stamps now() into
       supabase_migrations.schema_migrations — SELECT the newest row and UPDATE its version to the
       file's exact 14-digit timestamp so the merge-time CLI sees a consistent history.
    4. GREEN proof AFTER apply: execute the full content of supabase/tests/test_mt5_exchange_boundary.sql
       on TEST (each DO block raises on failure; all four must complete with NOTICEs only).
    5. Constraint-def audit: SELECT pg_get_constraintdef for the four widened constraints — each
       contains 'mt5'; compute_jobs retains IS NULL OR. Then the EXCLUSION audit: the funding_fees
       and position_snapshots exchange CHECK defs contain NO 'mt5' (untouched), proving the widen
       did not leak past the parity-pinned four.
    Ship-time notes to record in the SUMMARY (not executable here): the migration-reviewer agent
    reviews this file before the PR (the /ship step runs it — CONTEXT locked), and the
    supabase-migrate.yml prod auto-apply run must be watched + constraint defs re-verified on prod
    after merge.
  </action>
  <acceptance_criteria>
    - RED-before evidence captured: pre-apply mt5 INSERT on TEST raised 23514
    - apply_migration succeeded on qmnijlgmdhviwzwfyzlc; schema_migrations version row equals the filename timestamp (drift fixed)
    - All four DO blocks of test_mt5_exchange_boundary.sql ran GREEN on TEST
    - pg_get_constraintdef: 4/4 contain 'mt5'; compute_jobs keeps IS NULL OR; funding_fees + position_snapshots defs contain NO 'mt5'
  </acceptance_criteria>
  <verify>
    <automated>MISSING — verified via Supabase MCP execute_sql outputs against TEST (qmnijlgmdhviwzwfyzlc); the same SQL gate re-runs automatically in CI via the ci.yml supabase/tests/test_*.sql discovery job</automated>
  </verify>
  <done>TEST project proves reject-before/accept-after; migration history consistent; prod apply documented as merge-inherent with reviewer + watch steps recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any writer → Postgres | the CHECK constraints are the last line of defense against smuggled exchange values |
| repo merge → PROD schema | supabase/migrations/** auto-applies to prod on merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-135-05 | Tampering | 4 exchange CHECK constraints | mitigate | widened NOT dropped — RED-guard (b) arms prove a bogus value still raises 23514; self-verify DO blocks fail-loud at apply |
| T-135-06 | Tampering | parity-pinned exclusions | mitigate | exclusion audit asserts funding_fees/position_snapshots defs unchanged; only 4 ADD CONSTRAINTs in the file |
| T-135-07 | Denial of Service | prod apply lock contention | mitigate | SET lock_timeout='3s' bounds blast radius; TEST-first apply proves the script |
| T-135-08 | Repudiation | schema_migrations drift | mitigate | version row fixed to filename timestamp post-MCP-apply (Pitfall 5) |
| T-135-SC | Tampering | package installs | accept | N/A — no installs this phase |
</threat_model>

<verification>
- pytest tests/test_boundary_literals_parity.py green (byte-parity incl. new mt5 class)
- npx tsc --noEmit + parity vitest green
- TEST project: RED-before + GREEN-after evidence, drift fixed, exclusions untouched
</verification>

<success_criteria>
- MT5SRC-03 DB half done: 'mt5' admitted at exactly the four parity-pinned CHECKs on TEST, invalid values still rejected
- TS SoT consts widened atomically with the migration (no red window in the vitest parity suite)
- Prod path documented: reviewer at ship, auto-apply on merge, watch + re-verify
</success_criteria>

<output>
Create `.planning/phases/135-mt5src-source-lockstep/135-02-SUMMARY.md` when done
</output>
