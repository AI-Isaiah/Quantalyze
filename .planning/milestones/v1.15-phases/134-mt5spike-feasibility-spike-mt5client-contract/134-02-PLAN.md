---
phase: 134-mt5spike-feasibility-spike-mt5client-contract
plan: 02
type: execute
wave: 2
depends_on: [134-01]
files_modified:
  - analytics-service/scripts/mt5_spike.py
  - analytics-service/tests/test_mt5_spike_harness.py
  - analytics-service/docs/mt5-spike-gonogo.md
autonomous: true
requirements: [MT5SPIKE-01]
must_haves:
  truths:
    - "The founder can run `python -m scripts.mt5_spike` against a demo account and get a structured go/no-go report over the four unknowns (unattended login cycles, order_check read-only proof, deal-reconstruction viability, server-time offset) — with a documented runbook"
    - "The harness NEVER calls order_send; credentials come from env only and never reach stdout/stderr (sanitize_evidence + assert_sanitized on the emitted report)"
    - "If leg 1 (unattended login) is NO-GO, the report prints the native-Windows-VPS escape hatch (identical Mt5Client contract) — recorded, never papered over"
    - "The harness's report-assembly and exit-code logic is proven OFFLINE against an injected fake client (the live legs are the only human part)"
    - "A go/no-go doc template exists with founder-fillable cells per leg plus the server-time→UTC normalization approach note (single seam applied in Phase 136)"
  artifacts:
    - path: analytics-service/scripts/mt5_spike.py
      provides: "standalone live feasibility harness (execution human_needed), main(argv) with deribit-shaped exit codes"
      exports: [main, run_spike]
      min_lines: 150
    - path: analytics-service/tests/test_mt5_spike_harness.py
      provides: "offline harness tests: exit codes, report shape, escape-hatch text, no order_send token, sanitization"
      min_lines: 60
    - path: analytics-service/docs/mt5-spike-gonogo.md
      provides: "go/no-go doc template — Runbook + Security constraint + Environment + 4 leg sections + overall verdict/fallback + normalization note"
      contains: "human_needed"
  key_links:
    - from: analytics-service/scripts/mt5_spike.py
      to: analytics-service/services/mt5_client.py
      via: "the harness drives the SAME Mt5Client contract downstream phases use (validating it live is part of the spike's value)"
      pattern: "from services.mt5_client import"
    - from: analytics-service/scripts/mt5_spike.py
      to: analytics-service/scripts/deribit_ground_truth.py
      via: "single-definition sanitization primitives (sfox_ground_truth precedent)"
      pattern: "from scripts.deribit_ground_truth import"
    - from: analytics-service/docs/mt5-spike-gonogo.md
      to: analytics-service/scripts/mt5_spike.py
      via: "runbook section documents the exact invocation + env vars"
      pattern: "mt5_spike"
---

<objective>
Land the buildable half of MT5SPIKE-01: the standalone live-feasibility harness
(`scripts/mt5_spike.py`), its offline tests, and the founder-fillable go/no-go doc
template + runbook. The four LIVE proof legs are human_needed (founder demo
credentials + a running gmag11 v2.3 gateway are not available in this run) — they
execute in plan 134-03's checkpoint; NOTHING here claims a live gate passed.

Purpose: Phase 134 is the milestone go/no-go gate. The harness turns four fuzzy
unknowns (Wine auto-login reliability, order_check investor-vs-master signal,
deal-reconstruction viability, server-time offset) into a structured, secret-safe,
per-leg GO/NO-GO report the founder can produce in one command.

Output: `analytics-service/scripts/mt5_spike.py`,
`analytics-service/tests/test_mt5_spike_harness.py`,
`analytics-service/docs/mt5-spike-gonogo.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-CONTEXT.md
@.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-RESEARCH.md
@.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-PATTERNS.md
@.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-01-SUMMARY.md
@analytics-service/scripts/deribit_ground_truth.py
@analytics-service/scripts/sfox_ground_truth.py

<interfaces>
<!-- Existing exports the executor uses directly. -->

From analytics-service/scripts/deribit_ground_truth.py (single-definition sanitization
— reuse, never re-implement; sfox_ground_truth.py:74-80 is the import precedent):
```python
def _redact_secret_values(text: str, *secrets: str | None) -> str   # line 63 — by-value belt-and-suspenders
def sanitize_evidence(obj: Any) -> Any                              # line 359
def assert_sanitized(obj: Any, _path: str = "") -> None             # line 390
class ScopeViolationError(RuntimeError)                             # line 430
def main(argv: list[str] | None = None) -> int                      # line 971 — the exit-code/argparse shape to mirror
```

From analytics-service/services/mt5_client.py (plan 134-01 output — the contract
the harness drives):
```python
class Mt5ClientError(RuntimeError)      # .code carries the MT5 last_error() code
class Mt5Client:
    def __init__(self, host, port, *, _connect=None, request_timeout_s=...)
    def login(self, login: int, password: str, server: str) -> None
    def account_info(self) -> dict          # includes trade_allowed
    def history_deals_get(self, from_ts, to_ts) -> list[dict]   # raw server-time epochs verbatim
    def order_check(self, request: dict) -> dict                # probe ONLY
    def close(self) -> None
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/mt5_spike.py — four-leg live harness with deribit-shaped CLI contract + offline harness tests</name>
  <files>analytics-service/scripts/mt5_spike.py, analytics-service/tests/test_mt5_spike_harness.py</files>
  <read_first>
    - analytics-service/scripts/deribit_ground_truth.py — lines 971-1036 (main(argv)/argparse/exit-code contract to copy in shape: 0 success, 2 scope violation, 3 missing env, 1 other; env-only creds; redacted stderr)
    - analytics-service/scripts/sfox_ground_truth.py — lines 46-54 (env-only credential docstring convention) and 74-80 (importing the sanitization primitives from deribit_ground_truth — single definition)
    - analytics-service/services/mt5_client.py — the plan 134-01 contract being driven
    - .planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-RESEARCH.md — harness leg spec (lines 382-394), Pitfall 4 (order_check signal is a live unknown), Pitfall 5 (server-time offset), Pitfall 6 (unattended login)
  </read_first>
  <behavior>
    - test_missing_env_exits_3: main([]) with none of the MT5_SPIKE_* env vars set returns 3 and prints an ERROR line to stderr that names the missing vars but contains no secret values
    - test_report_has_four_legs_with_verdicts: run_spike driven by an injected fake Mt5Client factory (happy scenario) returns a report dict with keys unattended_login, read_only_proof, deal_reconstruction, server_time_offset, each carrying a verdict field in {"GO", "NO-GO", "INCONCLUSIVE"} plus an overall verdict
    - test_leg1_no_go_emits_escape_hatch: fake client whose login raises Mt5ClientError on >50% of cycles → leg 1 verdict NO-GO and the report contains the native-Windows-VPS escape-hatch note (string includes "Windows VPS" and states the Mt5Client contract is identical)
    - test_leg2_never_calls_order_send_and_records_probe: fake records every method touched; after run_spike, the touched set contains order_check and account_info but NOT order_send; leg 2 report carries retcode, comment, and trade_allowed fields
    - test_leg3_distinguishes_none_error_from_empty: fake history_deals_get raising Mt5ClientError → leg 3 records an error observation with its code (not "zero deals"); fake returning [] → leg 3 records honest-empty; fake returning populated deal dicts → leg 3 reports field-presence booleans for profit/swap/commission/fee and whether any DEAL_TYPE_BALANCE row exists
    - test_leg4_offset_rounded_to_half_hour: fake deals with a known most-recent time epoch vs an injected utc_now → leg 4 reports candidate_offset_minutes rounded to the nearest 30 and a founder_confirmation_required=true flag (deal-derived offsets are an estimate, not ground truth)
    - test_emitted_report_is_sanitized: report produced with secret-bearing scenario text passes assert_sanitized after sanitize_evidence, and none of the env secret values appear in the JSON dump
    - test_harness_source_has_no_order_send_call_token: the mt5_spike.py source text contains no "order_send(" token (structural guard mirroring the client surface test)
  </behavior>
  <action>
    Create analytics-service/scripts/mt5_spike.py — a standalone CLI harness the
    founder runs against a broker DEMO/investor account (execution is human_needed;
    this task only lands + offline-proves it).

    Structure (mirror deribit_ground_truth.py main-shape verbatim):
    - Module docstring: purpose (MT5SPIKE-01 four-leg go/no-go), invocation runbook
      (`cd analytics-service && python -m scripts.mt5_spike > /tmp/mt5_spike_report.json`),
      env-only credentials (never argv, never a file), prerequisite: mt5linux==1.0.3
      installed (gated by the plan 134-03 human-verify checkpoint) + a running
      gmag11/MetaTrader5-Docker v2.3 gateway reachable over a PRIVATE network only
      (RPyC SlaveService is an unauthenticated RCE channel — never a public port).
      Refer to the forbidden trade call as order_send without parentheses so the
      grep gate stays clean.
    - Env vars: MT5_SPIKE_LOGIN (int), MT5_SPIKE_INVESTOR_PASSWORD,
      MT5_SPIKE_SERVER (exact broker server string), MT5_SPIKE_HOST,
      MT5_SPIKE_PORT (gateway RPyC port — 18812 constructor default vs 8001 common
      image map, verify per container), optional MT5_SPIKE_MASTER_PASSWORD (enables
      the leg-2 master-side comparison; absent → leg 2 runs investor-only and is
      marked partial), optional MT5_SPIKE_CYCLES (default 10, leg-1 repetitions),
      optional MT5_SPIKE_HISTORY_DAYS (default 90, leg-3 window).
    - def main(argv: list[str] | None = None) -> int with the deribit exit-code
      contract: 0 success, 2 ScopeViolationError (read-only premise violated),
      3 missing env vars, 1 other; every stderr line passes
      _redact_secret_values(str(exc), login, investor_pw, master_pw, server) FIRST,
      then the report passes sanitize_evidence + assert_sanitized before
      json.dumps to stdout. Import ScopeViolationError, _redact_secret_values,
      assert_sanitized, sanitize_evidence from scripts.deribit_ground_truth
      (single definition — sfox_ground_truth precedent; do NOT re-implement).
    - def run_spike(env: dict, *, client_factory, utc_now=None) -> dict — the
      injectable seam for offline tests: client_factory(host, port) returns an
      Mt5Client-shaped object; utc_now injectable for leg 4 determinism. Default
      client_factory constructs services.mt5_client.Mt5Client (the harness drives
      the SAME contract 135/136 use, so the live run also validates the contract
      against a real bridge).
    - Leg 1 (unattended_login): N = MT5_SPIKE_CYCLES fresh login→account_info→close
      cycles with no human dialog-dismissal; record per-cycle ok/error(code) +
      success_rate; verdict GO if success_rate == 1.0, INCONCLUSIVE if >= 0.8,
      NO-GO below; on NO-GO append the escape-hatch note: native Windows VPS running
      the official MetaTrader5 wheel behind the IDENTICAL Mt5Client contract (only
      the host swaps) — recorded, never papered over.
    - Leg 2 (read_only_proof): investor login → order_check with a minimal
      market-order-shaped request dict (probe only) → record retcode + comment +
      account_info trade_allowed; if MT5_SPIKE_MASTER_PASSWORD set, repeat on the
      master login and record BOTH signal tuples so Phase 135 can encode the real
      per-broker rule; the exact distinguishing retcode is [ASSUMED] until this leg
      runs (RESEARCH Pitfall 4). The trade path is never touched.
    - Leg 3 (deal_reconstruction): history_deals_get over the last
      MT5_SPIKE_HISTORY_DAYS; record deal_count, earliest/latest raw time epochs
      (history-depth evidence), field-presence booleans for
      profit/swap/commission/fee/type/time/time_msc, whether any DEAL_TYPE_BALANCE
      (type == 2) row exists, and the observed None-vs-() behavior (an
      Mt5ClientError here is recorded as an error observation with its code — never
      coerced to "zero deals").
    - Leg 4 (server_time_offset): utc_now vs the most recent deal's raw time epoch →
      candidate_offset_minutes rounded to the nearest 30 (broker offsets are
      whole/half hours), plus founder_confirmation_required=true and an instruction
      to confirm against the terminal's displayed server clock (VNC) and note DST
      behavior. The client returns raw server-time epochs verbatim; the ONE
      normalize-to-UTC seam is Phase 136's combine_mt5_deal_ledger — this leg only
      ESTABLISHES the offset.
    - Emit: json.dumps(sanitized_report, indent=2, default=str) with per-leg and
      overall GO/NO-GO/INCONCLUSIVE verdicts.

    Create analytics-service/tests/test_mt5_spike_harness.py implementing every
    <behavior> case OFFLINE via the injected client_factory + env dict — no network,
    no mt5linux, no live terminal. Module docstring in the "Regression gates — WHY"
    convention (e.g. leg-3 None-coercion would fabricate a flat account inside the
    very harness meant to prove honesty). TDD flow: failing tests first, implement
    to green.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_spike_harness.py -x -q && grep -c "order_send(" scripts/mt5_spike.py | grep -qx "0" && echo HARNESS-CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - analytics-service/scripts/mt5_spike.py defines main(argv) and run_spike with the injectable client_factory seam
    - `cd analytics-service && pytest tests/test_mt5_spike_harness.py -x -q` exits 0 with mt5linux NOT installed
    - `grep -c "order_send(" analytics-service/scripts/mt5_spike.py` returns 0
    - `grep -c "from scripts.deribit_ground_truth import" analytics-service/scripts/mt5_spike.py` returns 1 (sanitization single-definition reuse)
    - `grep -c "from services.mt5_client import" analytics-service/scripts/mt5_spike.py` returns 1 (drives the real contract)
    - `cd analytics-service && python -m scripts.mt5_spike; test $? -eq 3` succeeds in an env with no MT5_SPIKE_* vars set (missing-env exit code, no live deps needed)
    - report dict contains keys unattended_login, read_only_proof, deal_reconstruction, server_time_offset with verdict fields (pinned by test)
  </acceptance_criteria>
  <done>Harness lands with deribit-shaped CLI contract; four legs implemented against the Mt5Client contract; all harness logic proven offline; secrets provably never emitted; the trade path provably never touched.</done>
</task>

<task type="auto">
  <name>Task 2: docs/mt5-spike-gonogo.md — founder-fillable go/no-go template + runbook + server-time normalization approach note</name>
  <files>analytics-service/docs/mt5-spike-gonogo.md</files>
  <read_first>
    - .planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-RESEARCH.md — go/no-go template section spec (lines 381-394), Pitfall 5 (server-time normalization), Pitfall 6 (Wine login), Security Domain (lines 489-507, private-network constraint)
    - analytics-service/scripts/mt5_spike.py — the Task 1 harness (env vars + invocation the runbook must match exactly)
    - analytics-service/docs/deribit-ground-truth.md — the repo's existing harness-doc convention (tone/structure reference)
  </read_first>
  <action>
    Create analytics-service/docs/mt5-spike-gonogo.md — the MT5SPIKE-01 go/no-go
    document TEMPLATE the founder fills after running the live spike. (Location
    note: RESEARCH sketched `.planning/phases/134-.../MT5_GONOGO.md (or similar)`;
    `.planning` is a gitignored local ledger, and the founder-filled verdict must be
    a committed, reviewable artifact — `analytics-service/docs/` is the established
    harness-doc home (deribit-ground-truth.md precedent), so "or similar" resolves
    there.)

    Required sections (every live-result cell pre-filled with the literal
    placeholder `human_needed` so an unfilled template can never read as passed):
    1. **Runbook** — prerequisites: mt5linux==1.0.3 installed (gated by the plan
       134-03 supply-chain human-verify checkpoint), a running
       gmag11/MetaTrader5-Docker v2.3 container (amd64-only, ~4 GB, persistent
       /config, one-time VNC install + investor login at :3000), RPyC port
       verification (18812 constructor default vs 8001 common image map), the exact
       env-var list from mt5_spike.py, the invocation
       `cd analytics-service && python -m scripts.mt5_spike > /tmp/mt5_spike_report.json`,
       and the exit-code table (0 success / 2 scope violation / 3 missing env / 1 other).
    2. **Security constraint (hard)** — the RPyC bridge is an unauthenticated
       arbitrary-remote-code channel (SlaveService): reachable ONLY over a private
       network (Railway internal / WireGuard / SSH tunnel), NEVER a public port;
       credentials env-only; this constraint carries forward as a Phase 139
       provisioning requirement.
    3. **Environment** — broker, exact server string, container image + pin,
       RPyC port, image build date (founder fills; `human_needed` placeholders).
    4. **Leg 1 — unattended Wine auto-login**: cycles run, success rate, observed
       failure modes, VERDICT (GO/NO-GO/INCONCLUSIVE), escape-hatch trigger
       statement: NO-GO here pivots the milestone to a native Windows VPS running
       the official MetaTrader5 wheel behind the IDENTICAL Mt5Client contract
       (only the host swaps) — recorded, never papered over.
    5. **Leg 2 — order_check investor-vs-master read-only proof**: observed
       order_check retcode + comment on investor login (and master login if
       provided), account_info trade_allowed values, VERDICT; note the exact
       retcode is [ASSUMED] until observed and that Phase 135 encodes the real rule
       from the COMBINATION of retcode/comment + trade_allowed; the trade path was
       never touched (harness-enforced).
    6. **Leg 3 — deal-reconstruction viability**: deal count, history depth
       (earliest deal), field presence (profit/swap/commission/fee),
       DEAL_TYPE_BALANCE rows observed, None-vs-() behavior observed live, VERDICT.
    7. **Leg 4 — broker-server-time-vs-UTC offset**: measured candidate offset,
       terminal-clock confirmation, DST note, VERDICT — plus the **normalization
       approach note** (the deliverable of ROADMAP success criterion 4): the client
       returns raw server-time epochs VERBATIM; Phase 136's
       combine_mt5_deal_ledger is the ONE seam that subtracts the recorded offset
       to normalize to UTC BEFORE day-bucketing (Deribit/sFOX UTC-day-bucketing
       precedent), so deals near midnight land on the correct calendar day; the
       offset is recorded per broker in this doc and re-verified when a new broker
       server is onboarded.
    8. **Overall verdict + fallback decision** — GO/NO-GO for the milestone,
       fallback election if any, founder signature/date (`human_needed`).
  </action>
  <verify>
    <automated>test -f analytics-service/docs/mt5-spike-gonogo.md && grep -c "human_needed" analytics-service/docs/mt5-spike-gonogo.md | awk '{exit !($1>=6)}' && grep -qi "Windows VPS" analytics-service/docs/mt5-spike-gonogo.md && grep -qi "private network" analytics-service/docs/mt5-spike-gonogo.md && grep -q "combine_mt5_deal_ledger" analytics-service/docs/mt5-spike-gonogo.md && echo TEMPLATE-OK</automated>
  </verify>
  <acceptance_criteria>
    - analytics-service/docs/mt5-spike-gonogo.md exists with all 8 sections (Runbook, Security constraint, Environment, Legs 1-4, Overall verdict)
    - `grep -c "human_needed" analytics-service/docs/mt5-spike-gonogo.md` returns >= 6 (every live-result cell placeholder-filled — an unfilled template can never read as passed)
    - the doc contains the exact invocation `python -m scripts.mt5_spike` and every MT5_SPIKE_* env var named in the harness
    - `grep -qi "Windows VPS"` and `grep -qi "private network"` and `grep -q "combine_mt5_deal_ledger"` all succeed on the doc (escape hatch, security constraint, normalization seam all present)
  </acceptance_criteria>
  <done>Founder-fillable go/no-go template lands in the repo docs home with runbook, hard security constraint, per-leg human_needed cells, escape hatch, and the server-time normalization approach note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| founder shell → RPyC bridge | the spike speaks rpyc classic/SlaveService to the gateway — an unauthenticated arbitrary-remote-code channel |
| harness → stdout/stderr/report | credentials (login/investor pw/master pw/server) must never cross into any emitted text |
| harness → broker account | a live account is touched — the read-only premise must hold structurally |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-134-06 | Information Disclosure | mt5_spike.py report/stderr | mitigate | env-only creds; _redact_secret_values by-value first, then sanitize_evidence + assert_sanitized on the whole report before stdout; offline test proves no secret survives |
| T-134-07 | Elevation of Privilege | harness touching the trade path | mitigate | harness drives the Mt5Client narrowing facade (no trade surface reachable); "order_send(" grep gate + touched-methods offline test; ScopeViolationError → exit 2 |
| T-134-08 | Tampering / EoP | RPyC SlaveService transport | mitigate (constraint documented) / transfer (Phase 139 owns hardening) | runbook + go/no-go doc state the hard PRIVATE-network-only constraint; never a public port |
| T-134-09 | Repudiation (unverifiable go/no-go) | go/no-go doc | mitigate | every live-result cell pre-filled `human_needed`; an unfilled template structurally cannot read as passed; founder signature/date cell |
</threat_model>

<verification>
- `cd analytics-service && pytest tests/test_mt5_spike_harness.py -x -q` exits 0 (offline, no mt5linux)
- `cd analytics-service && pytest -q` exits 0 (full suite regression)
- `grep -c "order_send(" analytics-service/scripts/mt5_spike.py` returns 0
- `analytics-service/docs/mt5-spike-gonogo.md` passes the Task 2 grep gates (human_needed >= 6, Windows VPS, private network, combine_mt5_deal_ledger)
</verification>

<success_criteria>
- MT5SPIKE-01 buildable half complete: harness + offline harness tests + runbook + founder-fillable go/no-go template with escape hatch and normalization approach note
- The four LIVE proof legs remain explicitly human_needed — nothing in this plan claims them done (they gate in plan 134-03)
</success_criteria>

<output>
Create `.planning/phases/134-mt5spike-feasibility-spike-mt5client-contract/134-02-SUMMARY.md` when done
</output>
