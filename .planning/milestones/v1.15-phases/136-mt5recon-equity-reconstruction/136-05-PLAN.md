---
phase: 136-mt5recon-equity-reconstruction
plan: 05
type: execute
wave: 3
depends_on: ["136-01", "136-03"]
files_modified:
  - analytics-service/services/mt5_deals.py
autonomous: false
requirements: [MT5RECON-01]
must_haves:
  truths:
    - "Every [ASSUMED] row of the DEAL_TYPE classification table has been human-reviewed against the official MetaTrader5 DEAL_TYPE documentation (and live-spike evidence where available) and either confirmed fail-loud or explicitly reclassified"
    - "The server-time→UTC offset approach (env MT5_SERVER_UTC_OFFSET_S, default 0) is acknowledged as [ASSUMED pending the 134/139 live spike] — never silently treated as confirmed"
  artifacts:
    - path: "analytics-service/services/mt5_deals.py"
      provides: "classification table with [ASSUMED] tags resolved or re-confirmed per the checkpoint outcome"
  key_links:
    - from: "analytics-service/services/mt5_deals.py"
      to: "136-05 checkpoint record"
      via: "per-type rationale comments cite the human-verified disposition"
      pattern: "ASSUMED|confirmed"
---

<objective>
Human-verify checkpoint locking the DEAL_TYPE ambiguous middle (the Q2 locked
decision): CHARGE(4) / INTEREST(12) / BUY_CANCELED(13) / SELL_CANCELED(14) /
DIVIDEND(15) / DIVIDEND_FRANKED(16) / TAX(17) shipped FAIL-LOUD by default in
plan 136-01; the exact classification is locked behind THIS checkpoint — do NOT
guess a silent classification. Also surfaces the two other [ASSUMED] evidence
gaps for acknowledgment: the per-row field fold rule (A3) and the
server-time→UTC offset (A2).

Purpose: the deribit-'correction' lesson — a mis-classified deal type turns
capital into performance. A human decision on ASSUMED semantics is the only
honest lock until live-spike evidence exists (Phase 134 human_needed / 139).
Output: confirmed-or-amended classification table; recorded dispositions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-mt5recon-equity-reconstruction/136-RESEARCH.md
@analytics-service/services/mt5_deals.py
@docs/mt5-spike-gonogo.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Lock the ASSUMED DEAL_TYPE dispositions + acknowledge the A2/A3 evidence gaps</name>
  <files>analytics-service/services/mt5_deals.py</files>
  <action>
    Present the shipped classification table (fail-loud defaults from plan
    136-01) to the human alongside the official MetaTrader5 DEAL_TYPE
    documentation and any Phase-134 live-spike evidence; pause for the
    per-type disposition. Apply any approved reclassification to the
    mt5_deals.py frozensets + rationale comments + a NEW hand-derived oracle
    test per reclassified type (never just a moved constant). Record every
    disposition (including keep-fail-loud confirmations and the A2 offset /
    A3 fold-rule acknowledgments) in the SUMMARY.
  </action>
  <what-built>
    Plans 136-01/03 shipped the MT5 deal classifier with the locked-safe
    defaults: trading = {BUY 0, SELL 1, COMMISSION family 7-11}; external flow =
    {BALANCE 2, CREDIT 3, BONUS 6}; EVERYTHING ELSE fails loud
    (Mt5DealClassificationError), including the ambiguous middle
    CHARGE/INTEREST/CANCELED/DIVIDEND/TAX and CORRECTION(5). A real account
    whose history contains one of these types will FAIL its derive (permanent,
    honest) until this checkpoint reclassifies or confirms.
  </what-built>
  <how-to-verify>
    1. Open the official MetaTrader5 Python docs for the DEAL_TYPE enum
       (mql5.com "Deal Properties" / the MetaTrader5 PyPI docs) and compare each
       int value 0-17 against the frozensets + rationale comments in
       analytics-service/services/mt5_deals.py.
    2. For each ambiguous type (4, 12, 13, 14, 15, 16, 17): decide
       KEEP-FAIL-LOUD (default — a live account hitting it fails honestly and we
       reclassify on evidence) or RECLASSIFY (only with documentary evidence;
       e.g. DIVIDEND as trading return for CFD books). CORRECTION(5) STAYS
       fail-loud regardless (the deribit lesson — never assume trading vs
       capital).
    3. Acknowledge the two evidence gaps as ASSUMED-until-live-spike: (a) the
       per-row fold rule profit+swap+commission+fee (A3 — double-posted broker
       commission is caught by the reconcile gate); (b) the server-time→UTC
       offset env default 0 (A2 — near-midnight day-bucketing is per-broker;
       the 134/139 spike confirms). If the Phase-134 live spike ran, check
       docs/mt5-spike-gonogo.md for actual deal-type/offset evidence and apply it.
    4. If any reclassification is decided: update mt5_deals.py frozensets +
       rationale comments + the plan-01 tests (a reclassified type gets a
       hand-derived oracle, not just a moved constant).
  </how-to-verify>
  <resume-signal>Type "approved" to confirm the fail-loud defaults, or list reclassifications as "type N → trading|external_flow because [evidence]"</resume-signal>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_deal_reconstruction.py -x</automated>
  </verify>
  <done>Human disposition recorded for types 4/12/13/14/15/16/17 + CORRECTION(5) confirmed fail-loud; A2/A3 acknowledged as ASSUMED-until-live-spike; any reclassification carries an updated hand oracle and the suite is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assumed enum semantics → money-math | undocumented/ambiguous broker deal semantics decide flow-vs-performance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-17 | Tampering | ambiguous DEAL_TYPE silently guessed | mitigate | fail-loud default + THIS blocking human checkpoint; reclassification only on documentary evidence with a new hand oracle |
| T-136-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
Checkpoint disposition recorded in the SUMMARY (per-type decisions + the A2/A3 acknowledgments); any reclassification carries an updated hand-oracle test, pytest green.
</verification>

<success_criteria>
- Human decision recorded for every ASSUMED row; CORRECTION stays fail-loud
- No silent classification shipped; tests reflect any change
</success_criteria>

<output>
Create `.planning/phases/136-mt5recon-equity-reconstruction/136-05-SUMMARY.md` when done.
</output>
