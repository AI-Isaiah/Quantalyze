---
phase: 136-mt5recon-equity-reconstruction
plan: 03
type: execute
wave: 2
depends_on: ["136-01"]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/services/ingestion/long_fetch.py
  - analytics-service/routers/process_key.py
  - analytics-service/services/ingestion/mt5.py
  - analytics-service/tests/test_mt5_derive_branch.py
  - analytics-service/tests/test_process_key.py
autonomous: true
requirements: [MT5RECON-01, MT5RECON-03]
must_haves:
  truths:
    - "An mt5 onboard/resync routes through the long-fetch derive_broker_dailies tail (never the fill path, never sync_trades) and persists via the UNCHANGED derive_basis_series/persist downstream with conventions.periods_per_year == 252"
    - "With MT5_ENABLED off, the queued mt5 derive fails CLOSED permanent (no live read) — the sfox kill-switch parity"
    - "A material uPnL wedge (|equity−balance|/equity > 0.05, equity > dust floor) promotes to complete_with_warnings via unrealized_pnl_in_anchor — never silently reconciled"
    - "A None deal read / login failure / unclassifiable deal type fails the WHOLE job typed (transient for hangs, permanent for structural) — no partial or fabricated-flat series is ever persisted"
    - "A materially-funded account with <2 usable days fails loud permanent (never an empty-but-green track record)"
    - "An mt5 onboard AND resync yields trust_tier='api_verified' on the strategy_verifications row via process_key.py:847 + the transition — EXPLICITLY proven at the mt5 seam (mirror of the sFOX analog), not merely inherited"
    - "Reconstructed terminal equity equals account_info().equity within the defined tolerance (max($1, 1e-6·|terminal|)) — asserted directly at the mt5 job seam against a hand-derived oracle, so an anchor-path regression reddens here"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "venue=='mt5' branch in run_derive_broker_dailies_job modeled on sfox :2981; mt5 in _NATIVE_RETURNS_VENUES; Mt5 session construction + close routing"
      contains: "combine_mt5_deal_ledger"
    - path: "analytics-service/services/ingestion/long_fetch.py"
      provides: "mt5 in _LEDGER_BACKED_SOURCES (routes to the derive tail)"
      contains: "mt5"
    - path: "analytics-service/routers/process_key.py"
      provides: "mt5 admitted to onboard/resync behind mt5_enabled_server"
    - path: "analytics-service/tests/test_mt5_derive_branch.py"
      provides: "offline job-level tests via the Mt5Client _connect double, incl. the mt5-seam reconciliation assertion"
      min_lines: 120
    - path: "analytics-service/tests/test_process_key.py"
      provides: "mt5 onboard+resync → api_verified draft/transition (mirror of the sFOX analog :1610-1642)"
      contains: "api_verified"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "services/broker_dailies.combine_mt5_deal_ledger"
      via: "the mt5 branch's ONE combine call"
      pattern: "combine_mt5_deal_ledger"
    - from: "analytics-service/services/ingestion/long_fetch.py"
      to: "derive_broker_dailies tail"
      via: "_LEDGER_BACKED_SOURCES membership selects tail_kind"
      pattern: "_LEDGER_BACKED_SOURCES"
    - from: "analytics-service/routers/process_key.py"
      to: "services/closed_sets.mt5_enabled_server"
      via: "fail-closed wire-boundary gate (sfox :156 parity)"
      pattern: "mt5_enabled_server"
---

<objective>
Wire the MT5 reconstruction end-to-end through the worker: the `venue == "mt5"`
branch in `run_derive_broker_dailies_job` (modeled on the sFOX single-equity
branch), the enqueue-path admission (process_key onboard/resync + long_fetch
ledger-backed tail), the reconciliation/DQ gates (uPnL wedge, material-equity
floor, fail-loud dispositions), and the Mt5Adapter stub-posture resolution —
all against the Phase-134 offline `_connect` contract double, NO live broker.

Purpose: MT5RECON-01's "connected account → api_verified series through the ONE
backbone" story plus MT5RECON-03's ground-truth parity/DQ semantics.
Output: worker branch + wiring + offline job-level test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/136-mt5recon-equity-reconstruction/136-CONTEXT.md
@.planning/phases/136-mt5recon-equity-reconstruction/136-RESEARCH.md
@.planning/phases/136-mt5recon-equity-reconstruction/136-01-SUMMARY.md
@analytics-service/services/mt5_client.py
@analytics-service/services/ingestion/mt5.py

<interfaces>
The sfox branch sets these shared downstream variables the mt5 branch must set
identically (job_worker.py:3168-3186): `returns`, `meta`, `equity`,
`balance_error=False`, `open_unrealized_usd`, `upnl_unreadable=False`,
`funding=[]`, `realized=[]`, `external_flows` (list[ExternalFlow] evidence).
Downstream (derive_basis_series → persist) is UNTOUCHED; it reads
periods_per_year off strategies.asset_class (job_worker.py:3897/4147 →
metrics.periods_per_year_for_asset_class).

Seams to widen (exact anchors):
- job_worker.py:261 `_NATIVE_RETURNS_VENUES = frozenset({"deribit","sfox"})` → add "mt5" (excludes mt5 from the ccxt combine at :3392)
- job_worker.py:759-780 `_make_exchange_client` — the SINGLE preflight construction chokepoint (sfox branches at :778); the `_ExchangeContext.exchange` union type at :756
- job_worker.py:2981-3186 — the sfox branch to mirror: kill-switch gate :2991, wait_for-bounded crawls :3022-3063, material-equity floor :3106-3132, combine + NavReconstructionError catch :3138-3166
- long_fetch.py:51 `_LEDGER_BACKED_SOURCES = frozenset({"deribit","sfox"})` → add "mt5" (F1 lesson :256-264: sfox once fell into the fill branch → fetch_raw NotImplementedError crashed EVERY onboard in prod)
- process_key.py:156 (sfox_enabled_server fail-closed gate) and :163-165 (flow→source admission sets) — mirror for mt5 with services.closed_sets.mt5_enabled_server (:107)
- deribit uPnL-wedge precedent job_worker.py:2496-2503 (UNREALIZED_MATERIALITY_RATIO=0.05, DUST_NAV_FLOOR gate)

Mt5Client (Phase 134): synchronous blocking RPyC; `login(login:int, password, server)`
raises Mt5ClientError on falsy; `account_info() -> dict` (equity/balance/login/...);
`history_deals_get(from_ts, to_ts) -> list[dict]` (None already converted to typed
raise at :233-243); `close()`; offline `_connect` injection seam :158-162; the
adapter's to_thread + wait_for posture: ingestion/mt5.py:65-72, :144-203.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enqueue-path + registry wiring (process_key, long_fetch, venue sets, session factory)</name>
  <files>analytics-service/services/job_worker.py, analytics-service/services/ingestion/long_fetch.py, analytics-service/routers/process_key.py, analytics-service/services/ingestion/mt5.py, analytics-service/tests/test_process_key.py</files>
  <read_first>
    - analytics-service/services/ingestion/long_fetch.py:45-70, :245-270, :495-525 (ledger-backed routing + tail_kind selection)
    - analytics-service/routers/process_key.py:130-170 (sfox admission + fail-closed gate), :427 (_is_long_fetch)
    - analytics-service/services/job_worker.py:745-781 (_ExchangeContext + _make_exchange_client), :261 (_NATIVE_RETURNS_VENUES), aclose_exchange (grep — the single close chokepoint that routes SfoxClient.aclose)
    - analytics-service/services/ingestion/sfox.py:103-140 (fetch_raw/compute_metrics NotImplementedError BY DESIGN — the posture mt5 mirrors)
    - analytics-service/tests/test_process_key.py:1610-1642 (test_process_key_sfox_onboard_draft_carries_trust_tier_api_verified — the EXACT analog to mirror, venue swapped to mt5) + the transition at process_key.py:985-996 and the draft insert at :847-864
  </read_first>
  <action>
    (1) long_fetch.py:51 — add "mt5" to _LEDGER_BACKED_SOURCES with a comment
    citing the F1 sfox production incident (:256-264): mt5 returns come from the
    deal-ledger via the broker-dailies ONE-path, so onboard/resync must run
    validate + encrypt + state machine and enqueue the derive_broker_dailies
    tail, never the fill steps. (2) process_key.py — admit "mt5" to the
    "onboard" and "resync" source sets (:163-165) and add the fail-closed
    wire-boundary gate mirroring sfox :156: source=="mt5" and not
    mt5_enabled_server() → the same clean 4xx/refusal shape, detail
    MT5_DISABLED_DETAIL (services/closed_sets.py:96). Audit every other 'sfox'
    literal in process_key.py (grep) and mirror only where the semantics apply
    (teaser stays sfox-and-mt5-free; _resolve_asset_class needs NO edit — mt5 is
    not in CRYPTO_VENUES so it already resolves off the DB row / 252 default).
    (3) job_worker.py:261 — add "mt5" to _NATIVE_RETURNS_VENUES. (4)
    job_worker.py _make_exchange_client — add an "mt5" arm returning a small
    Mt5Session holder (Mt5Client built from MT5_GATEWAY_HOST/PORT env — reuse
    the construction + misconfig-raise posture of ingestion/mt5.py:133-144 —
    plus the parsed credentials via services.mt5_validation.parse_mt5_credentials:
    login int, investor password, broker server; slot mapping api_key/api_secret/
    passphrase per the 135 convention). Widen the _ExchangeContext.exchange union
    type and route aclose_exchange to Mt5Session → Mt5Client.close() (sync — run
    via asyncio.to_thread at the close chokepoint, bounded). (5)
    ingestion/mt5.py — resolve the stub posture: per the sfox F1 precedent,
    fetch_raw/compute_metrics REMAIN NotImplementedError BY DESIGN (the
    ledger-backed flow routes AROUND the fill steps — implementing a fill-based
    path would reopen BYB-02), but their messages/docstrings MUST be updated
    from "until Phase 136" to the permanent by-design wording mirroring
    sfox.py:109-140. This honors the locked decision's economic intent (returns
    route through the broker-dailies ONE-path, never fill-based) — note the
    wording-vs-precedent conflict resolution in the SUMMARY.
    (6) EXPLICITLY PROVE the phase-goal api_verified stamp at the mt5 seam: add
    test_process_key.py tests mirroring the sFOX analog :1610-1642 EXACTLY,
    venue swapped to mt5 (MT5_ENABLED='true' via monkeypatch, cleaned up) — one
    for flow_type='onboard' and one for 'resync' — asserting the
    strategy_verifications DRAFT insert carries trust_tier='api_verified'
    (source != 'csv' → process_key.py:847 earns it FREE), and that the
    validated-transition (transition_strategy_verification, :985-996) is invoked
    so get_published_trust_signals (reads the most-recent row, no status filter,
    mig 20260719140000:80-88) surfaces api_verified. This makes MT5RECON-01's
    api_verified stamp EXPLICITLY verified at the mt5 seam — NOT merely inherited
    (the plan-checker's required proof; the finalize_wizard_strategy RPC IN-list
    is IRRELEVANT to this stamp path and is deliberately NOT touched this phase).
  </action>
  <verify>
    <automated>cd analytics-service && python -c "from services.ingestion.long_fetch import _LEDGER_BACKED_SOURCES; from services.job_worker import _NATIVE_RETURNS_VENUES; assert 'mt5' in _LEDGER_BACKED_SOURCES and 'mt5' in _NATIVE_RETURNS_VENUES" && pytest tests/test_process_key.py -x -q -k "mt5 and api_verified" && pytest tests -x -q -k "mt5 and (adapter or process_key or long_fetch)"</automated>
  </verify>
  <done>mt5 rides the ledger-backed tail; onboard/resync admit mt5 behind the fail-closed gate; _NATIVE_RETURNS_VENUES + session factory + close routing in place; stub messages updated to the by-design posture; the mt5 onboard+resync → api_verified draft/transition tests are green (the phase-goal stamp EXPLICITLY proven at the seam); existing sfox/deribit tests untouched and green.</done>
</task>

<task type="auto">
  <name>Task 2: The venue == "mt5" derive branch — bounded read, combine, reconciliation + DQ gates</name>
  <files>analytics-service/services/job_worker.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:2981-3186 (the ENTIRE sfox branch — gate, wait_for bounds, transient/permanent dispositions, material-equity floor, NavReconstructionError catch, shared downstream vars)
    - analytics-service/services/job_worker.py:2496-2503 (deribit uPnL-wedge flag: not balance_error, equity > DUST_NAV_FLOOR, |open_unrealized|/equity > UNREALIZED_MATERIALITY_RATIO → meta["unrealized_pnl_in_anchor"]=True)
    - analytics-service/services/nav_twr.py:56-96 (DUST_NAV_FLOOR=1000.0, UNREALIZED_MATERIALITY_RATIO=0.05), :572-602 (reconcile_flow_residual tolerance max(1.00, 1e-6·|terminal|))
    - analytics-service/services/ingestion/mt5.py:65-72, :144-203 (the to_thread + wait_for + Mt5ClientError classification posture the branch reuses)
  </read_first>
  <action>
    Insert an `elif venue == "mt5":` branch between the sfox branch and the ccxt
    else in run_derive_broker_dailies_job, modeled line-for-line on sfox: (a)
    FIRST the kill-switch chokepoint — not mt5_enabled_server() → permanent
    FAILED with MT5_DISABLED_DETAIL (sfox :2991 parity; the queue path must
    honor the go-dark gate, DB CHECK admits mt5 unconditionally). (b) One
    synchronous read block via asyncio.to_thread wrapped in asyncio.wait_for
    (module constant _MT5_DERIVE_READ_TIMEOUT_S = MT5_REQUEST_TIMEOUT_S-derived
    with margin, the ingestion/mt5.py:72 pattern): login(creds) →
    account_info() → history_deals_get(epoch-0, now + one day margin) — full
    history, one bounded call; deep hardening (terminal-restart-on-timeout,
    per-terminal lock, login-bracket assertion) is Phase 137 by locked scope, but
    the wait_for bound itself is the existing-posture baseline, not new
    hardening. asyncio.TimeoutError → transient FAILED (FLIPRETRY-01 arm, sfox
    :3041 wording); Mt5ClientError → classify via
    services.mt5_validation.classify_mt5_login_error: auth/wrong_server →
    permanent FAILED + _stamp_strategy_analytics_failed (scrubbed), transient →
    transient FAILED. The client's None→raise discipline means NO fabricated
    flat account can enter here — never re-conflate None with empty. (c) Extract
    equity + balance from account_info (fail-loud _coerce-style on non-finite);
    open_unrealized_usd = equity − balance (the MT5 realized-vs-MTM wedge,
    directly observable — v1.8 convention). (d) Material-equity floor: equity >
    _DERIBIT_EMPTY_LEDGER_FLOOR_USD with <2 usable PnL/flow days → permanent
    FAILED "material equity but no interpretable daily history" (sfox :3116
    analog). (e) THE combine: returns, meta = combine_mt5_deal_ledger(deals,
    account_equity=equity, account_balance=balance, server_utc_offset_s=
    int(os.getenv("MT5_SERVER_UTC_OFFSET_S", "0")) — the [ASSUMED A2] offset,
    env-threaded, confirmed at the 134/139 live spike; plan 136-05 checkpoint).
    Catch Mt5DealClassificationError → permanent FAILED + stamp (the deribit
    'correction' disposition: an unclassifiable deal must never retry forever);
    catch NavReconstructionError → _dispose_broker_nav_error (sfox :3156
    parity). (f) Belt-and-braces wedge flag: if the combiner's meta lacks
    unrealized_pnl_in_anchor, apply the deribit :2496-2503 condition here (not
    balance_error, equity > DUST_NAV_FLOOR, |open_unrealized_usd|/equity > 0.05
    → meta["unrealized_pnl_in_anchor"] = True → complete_with_warnings
    downstream). (g) Set the shared downstream vars exactly as sfox :3168-3186
    (funding=[], realized=[], external_flows = the ExternalFlow evidence list
    from the combiner/fold — same type sfox_flows_by_day yields). All raise
    messages: codes/counts/ratios only, never raw USD beyond the existing
    floor-log convention; run scrub_freeform_string on anything user-sourced.
  </action>
  <verify>
    <automated>cd analytics-service && python -m py_compile services/job_worker.py && pytest tests/test_mt5_deal_reconstruction.py -x -q</automated>
  </verify>
  <done>Branch compiles and mirrors the sfox dispositions; gate-first ordering; every read bounded; wedge + floor + classification failures dispose correctly; downstream shared code untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Offline job-level tests — wiring, honesty, wedge, 252 echo</name>
  <files>analytics-service/tests/test_mt5_derive_branch.py</files>
  <read_first>
    - analytics-service/tests/ (grep for the existing sfox derive-branch job-level test — mirror its double/monkeypatch harness; e.g. tests exercising run_derive_broker_dailies_job with a fake supabase + patched _make_exchange_client)
    - analytics-service/services/mt5_client.py:158-162 (the _connect transport double seam)
    - analytics-service/tests/test_mt5_client_contract.py:280-300 (None≠() contract half already pinned at the client)
  </read_first>
  <behavior>
    - test_mt5_disabled_fails_closed: MT5_ENABLED unset → the mt5 derive job returns permanent FAILED carrying MT5_DISABLED_DETAIL, and the transport double records ZERO calls (no live read while disabled)
    - test_mt5_routes_one_backbone: flag on + a healthy double (the plan-01 canonical ledger + equity 110_500/balance 110_500) → job succeeds; the persisted series equals the plan-01 hand literals (deposit day 300/100_400 — asserted at the JOB level so the wiring, not just the helper, is proven); conventions echo periods_per_year == 252 for a strategy_row with asset_class 'traditional'
    - test_upnl_wedge_flags: double with balance 100_000 / equity 110_000 (10% wedge > 0.05, equity > dust floor) → meta/status promotes to complete_with_warnings via unrealized_pnl_in_anchor
    - test_read_error_fails_whole_job: double raising Mt5ClientError mid-read → typed FAILED, and NOTHING persisted (no partial series row) — the no-invented-data guarantee
    - test_unclassifiable_deal_permanent: a CORRECTION deal in the double's ledger → permanent FAILED + terminal analytics stamp, never transient retry
    - test_missing_window_masked: a ledger whose deals span only [D3..D5] → the persisted series starts at the first evidence day; no rows fabricated before it (the coverage-masked-absence half of MT5RECON-03: absence renders as absent rows/gap_spans, never a fabricated flat run)
    - test_long_fetch_tail_wiring: source 'mt5' selects tail_kind 'derive_broker_dailies' (never sync_trades) via _LEDGER_BACKED_SOURCES
    - test_reconstruction_reconciles_to_equity: the plan-01 canonical double (deals summing to net PnL +500 over the window, anchor account_info().equity=110_500, balance 110_500, no open positions) → the reconstructed terminal NAV equals account_info().equity within max($1, 1e-6·110_500) — asserted DIRECTLY at the mt5 job seam (the ground-truth parity gate, MT5RECON-03), the tolerance and the terminal literal hand-derived (110_500), NOT read back from the SUT; an injected anchor-path drift (e.g. terminal off by $2) turns this RED so a future regression reddens at the mt5 seam rather than silently
  </behavior>
  <action>
    Create tests/test_mt5_derive_branch.py using the same offline harness the
    sfox branch tests use (fake supabase capture + monkeypatched
    _make_exchange_client returning an Mt5Session over the _connect double; env
    MT5_ENABLED/MT5_GATEWAY_* set per-test with monkeypatch, cleaned up in
    teardown — remember vi.unstubGlobals is the TS analog; here use
    monkeypatch.setenv). Every money assertion uses the plan-01 HAND literals —
    never a value read back from the SUT to assert against itself. Keep the
    module import-safe on a pandera-less local env.
  </action>
  <verify>
    <automated>cd analytics-service && pytest tests/test_mt5_derive_branch.py -x</automated>
  </verify>
  <done>All eight behaviors green offline (incl. the mt5-seam reconciliation-to-equity parity gate); wiring (not just helpers) proven — neutering the branch's combine call, the tail_kind selection, or the anchor path turns tests RED; full analytics suite green (pytest, --cov-fail-under=80 satisfied by the new coverage).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → MT5 gateway (RPyC) | untrusted broker responses (account dict, deal rows) enter money-math |
| queue → derive branch | a stored mt5 key can fire live reads; the go-dark flag must gate the QUEUE path too |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-136-08 | Spoofing/Info | None-vs-empty deal read | mitigate | Mt5Client typed raise preserved; branch never coerces error→empty (a fabricated flat account is the top severity pitfall) |
| T-136-09 | Tampering | wrong-account numbers stamped api_verified | transfer | Phase 137 owns the login-bracket assertion + per-terminal lock (locked scope); 136 branch is single-read serialized via the existing sequential worker |
| T-136-10 | DoS | hung Wine terminal wedging the sequential worker | mitigate | asyncio.to_thread + wait_for bound on the ONE read block (existing-posture baseline); restart-on-timeout is Phase 137 |
| T-136-11 | Elevation | live read while founder-disabled | mitigate | mt5_enabled_server() gate FIRST in the branch (sfox kill-switch parity), tested with a zero-call double |
| T-136-12 | Info disclosure | account size in failure raises | mitigate | scrub_freeform_string + codes/counts-only messages (nav_twr leak-safe convention) |
| T-136-SC | Tampering | package installs | accept | no packages installed this phase |
</threat_model>

<verification>
cd analytics-service && pytest tests/test_mt5_derive_branch.py tests/test_mt5_deal_reconstruction.py -x — green.
cd analytics-service && pytest — full suite green (coverage gate holds).
grep -n "mt5" analytics-service/services/job_worker.py | grep -v "^\s*#" — branch + venue-set + factory arms present.
</verification>

<success_criteria>
- mt5 onboard→derive→persist story proven offline end-to-end at the JOB level with hand-literal money assertions
- Kill-switch, wedge, floor, fail-loud, missing-window, 252-echo behaviors all pinned
- No live broker touched; no fill-based path exists for mt5
</success_criteria>

<output>
Create `.planning/phases/136-mt5recon-equity-reconstruction/136-03-SUMMARY.md` when done.
</output>
