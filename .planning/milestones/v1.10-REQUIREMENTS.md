# Requirements Archive: v1.10 Demo-Hero Portfolio Intelligence + Options MTM + Backbone Unification

**Archived:** 2026-07-15
**Status:** SHIPPED

For current requirements, see `.planning/REQUIREMENTS.md`.

---

# Requirements: v1.10 — Demo-Hero Portfolio Intelligence + Options MTM

**Status:** Milestone v1.10 — user-confirmed scope (2026-07-12). Ready for `/gsd:plan-phase 98`.

**Milestone goal:** Turn the allocator dashboard into the 10/10 demo-hero surface for the next cap-intro / pilot-allocator meeting (the project north-star), and give options-trading strategies an honest working mark-to-market view on the factsheet.

**Chosen scope (user, 2026-07-12):** M-A Portfolio Intelligence + a NEW options-MTM factsheet toggle. **Split decision:** the infra/pipeline tail (M-E Pipeline Cutover + M-C CI/Test-Infra Ratchet) is deferred to **v1.11** (back in `FUTURE-MILESTONES.md`) so v1.10 stays demo-focused and shippable. M-B (cron/notification reliability) also stays deferred.

**Standing invariants (LOCKED, from v1.8/v1.9):** no-invented-data (degenerate inputs → honest empty/DQ, never fabricated); single-key + published-composite metrics byte-identical unless a fix's blast radius is evidenced (SC-4); worker-only decryption (key ciphertext never leaves the server); migrations auto-apply to prod on merge; every SECDEF fn hardened (search_path + REVOKE PUBLIC/anon/authenticated) + routed through migration-reviewer + rls-policy-auditor.

**Phase numbering:** continues from v1.9.1's Phase 97 → **starts at Phase 98**.

---

## v1 Requirements (this milestone)

### PI — Portfolio Intelligence (demo-hero; M-A) `[TODOS.md:408–456,699]`
- [ ] **PI-01** — **Exposure by Asset Class** widget renders real position-level data (per-exchange asset-class breakdown), not a placeholder. Honest empty state when a strategy has no position data.
- [ ] **PI-02** — **Net Exposure Over Time** widget renders from historical positions (a real time series), gap-handling consistent with the factsheet coverage-mask discipline.
- [ ] **PI-03** — **Allocation Over Time** widget renders from weight history (allocator holdings over time).
- [x] **PI-04** — **Notes** widget persists per-allocator `user_notes` (new storage, owner-scoped RLS, no secret surface).
- [x] **PI-05** — The hardcoded 10% favorites sleeve is replaced by the real optimizer output; favorites support sorting/grouping + bulk toggle + KPI narrative tooltips.
- [ ] **PI-06** — `PortfolioKPIRow` folds into the shared-panel pattern (removes the divergent bespoke KPI surface).
- [x] **PI-07** — Cross-process portfolio-recompute is deduped by a UNIQUE INDEX (the process-local semaphore lets duplicate `computing` rows through today). Integration test against real PG.

### MTM — Options mark-to-market toggle on the NORMAL factsheet `[user scope-clarified 2026-07-12]`
> **SCOPE (user, 2026-07-12):** This is JUST the existing `cash_settlement ↔ mark_to_market` toggle on the normal factsheet. The toggle changes WHICH ingested API data drives the return calculation — the exchange's mark-to-market valuation (daily marks) vs the cash-settlement values. It is NOT a new daily-option smoothing algorithm (the Phase-83 "smoothing" framing is DROPPED). Reuse the existing `pnl_basis` machinery (v1.8: cash_settlement / mark_to_market). Phase-101 research must CONFIRM the MTM API data (daily option marks) is ingested + the pnl_basis path, NOT design a smoothing method.
- [ ] **MTM-01** — The factsheet `cash_settlement ↔ mark_to_market` basis toggle is ENABLED (not disabled-with-reason) for an **options-trading** strategy: selecting `mark_to_market` recomputes returns from the ingested API mark data via the existing `pnl_basis=mark_to_market` path; `cash_settlement` stays byte-identical (SC-4). If the MTM API data genuinely isn't present for a book, it still degrades with a reason — never a fabricated line.
- [x] **MTM-02** — Option A (LOCKED): options-member composites stay honestly gated OFF (`unsmoothed_options_book`, pinned to the constant); per-member gaps stay MARKED (`per_key n_days:0`), never zero-filled; perp-only native composites still toggle (verify-only). No new valuation math. (102-02)
- [~] **MTM-03** — Static byte-identity regression DONE in-phase (102-02): nine analytics cash-pin files byte-identical, full suite green ≥80% cov, sole red = pre-existing audit-taxonomy drift. LIVE Zavara MTM-curve corroboration (b) is the POST-DEPLOY ship-time gate (OQ-3) — see 102-02-SUMMARY §Ship-Time Gate.
- [ ] **MTM-04** — **The basis toggle swaps the daily return SERIES, not just the seven scalars** (user, 2026-07-12). Phase 101/102 persist only `metrics_json_by_basis.mark_to_market` (scalars); the charts unconditionally read the cash daily series — so toggling MTM moves the metric cards but leaves every chart on cash. MTM-04 persists the MTM daily series (already COMPUTED then DISCARDED at `job_worker.py:2357/:2983` single-key and `:4258` composite) so the toggle swaps the dailies and **all charts follow** (equity/drawdown/returns), metrics + charts both deriving from the same series. **Unify single-key AND composite** (user). **Full per-basis coverage mask** (user) — MTM gaps ≠ cash gaps; render MTM-specific MARKED gaps, never zero-filled. Scalars are KEPT (they mirror the cash precompute-cache: cash persists BOTH `daily_returns`/`csv_daily_returns` AND `metrics_json` — not redundant). = **Phase 103**, folded into v1.10 (user); milestone does NOT close until the toggle moves the whole factsheet. No new valuation math (persist the series we already compute).

---

### BB — Backbone Unification (M-E, PULLED INTO v1.10 per user 2026-07-12: "add 2-3 more phases → a finished great feature")
> The dailies-canonical principle ([[feedback_dailies_canonical_unified_derive]]) fully realized: composites first derive stitched dailies then take the SAME route as CSV/API-keys; one pipeline everywhere; legacy branches deleted. Built ON the Phase-103 shared route (which the backbone merge EXTENDS, not rewrites — nothing throwaway).
- [x] **BB-01** — `process_key_long` (unified queued path) actually PERSISTS trades/positions/analytics via the Phase-103 shared route, behind the existing flag (no prod behavior change yet). (Phase 104)
- [x] **BB-02** — Composite stitch routes through the shared/CSV finalize path (adopts the shared route as common orchestration); csv-finalize transactional. Behind flag. (Phase 105)
- [ ] **BB-03** — Cutover: staged prod flag flip (`USE_COMPUTE_JOBS_QUEUE` permanent), delete legacy finalize branches, `computing`-janitor cron + `after()` fail-loud; FULL E2E after each flip (flag-flip here already exposed 2 latent CSV bugs). The prod-risk phase. (Phase 106)
- [x] **LEV-BB** — Leverage becomes a dailies-level preparation transform (levered basis) feeding the ONE backbone; the whole factsheet re-derives levered (charts + rail + honest α→L·α). Deletes the frontend re-scale (`useLeveragedMetrics`/`useModeledLeverage`) + disclosure code (~780 LOC). Precedent `scenario.ts:427`. (Phase 107) `[scope-expanded to v1.10 per user 2026-07-14: "single-strategy complete"]`
- [x] **SCEN-BB** — The scenario-planner flows from the backbone; deletes the TS second-Sharpe `scenario-blend-panels.ts` (~211 LOC); keep `metrics-parity.test.ts`. (Phase 108)

### Deferred → v1.11 (from the backbone bypass inventory — see `.planning/BACKBONE-BYPASS-INVENTORY.md`)
- **E1 Portfolio aggregation** onto the backbone (delete `portfolio_metrics.py` second Sharpe/TWR stack; MWR/Dietz stay — cashflow/IRR, not return-series-derivable).
- **E2 Allocator equity reconstruction** onto the backbone. E3 allocator matching. Out-of-scope live Sharpe stacks (optimizer, allocated_capital, equity_reconstruction NAV builder).

## Deferred → v1.11 (in FUTURE-MILESTONES.md)
- **M-C CI/Test-Infra Ratchet** — land the v1.9.1-deferred `-n auto` fence parallelization (per-test claim-space isolation), unskip the 44 live-DB vitest + import-guard python, routers/worker coverage, Railway silent-skip fail-loud, cassette-refresh alerting.

## Out of Scope (v1.10)
- **M-B Cron & Notification Reliability** — deferred (next-up alongside v1.11).
- **M-D Analytics god-file / dead-code splits** — opportunistic-only; batch when PI work touches `queries.ts`/`portfolio.py`.
- Marketing/discovery (#33/#34/#80); SOC 2 (#43); loose UI-polish (batch opportunistically).

## Traceability
Requirements → phases assigned in ROADMAP.md (Phases 98+). PI is the bulk (position/weight-history data plumbing + 4 widgets + optimizer); MTM is the research-heavy factsheet feature.
