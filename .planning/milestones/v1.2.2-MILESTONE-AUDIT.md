---
milestone: v1.2.2
milestone_name: scenario-tab-factsheet-parity
audited: 2026-06-26
status: passed
scores:
  requirements: 24/24
  phases: 5/5
  integration: 24/24
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - phase: cross-cutting
    items:
      - "Bookkeeping drift: SUMMARY `requirements_completed` frontmatter empty for 39-01/02, 40-01/02, 41-01/02, 42-05; REQUIREMENTS.md traceability showed PAYLOAD-01..05, BODY-01..04, CORR-01..06, PEER-04, PEER-05 as Pending [ ] despite all phase VERIFICATION.md = passed and all 24 reqs WIRED in code (integration-checker confirmed). Traceability corrected to Complete in this audit. Not a coverage gap."
  - phase: 41-constituent-correlation-diversification
    items:
      - "41-VERIFICATION.md IN-02 claim is STALE: it states DEFAULT_INCLUDE_FROM is exported from scenario.ts + imported by diversification.ts, but commit 3be64980 reverted that hoist to preserve the frozen-spine (scenario.ts zero-diff). Both files carry a private `2022-01-01` literal (no functional divergence; diversification.test.ts:137 pins it behaviorally). Latent drift risk if one date changes separately. Accepted: frozen-spine > DRY. Doc-accuracy only; archived phase."
  - phase: 43-edge-states-toggle-fold-guards
    items:
      - "43 VERIFICATION = human_needed: 2 authed-Chromium visual canaries (live folded layout seam/ordering; live footer-gate prop threading) — deferred-by-construction post-deploy authed UATs (43-HUMAN-UAT.md). Automated structure CI-proven (4/4 GUARDs verified). Headless can't hydrate authed pages; nothing deployed yet."
      - "STYLE-V2-01 deliberately deferred: payload `styleDrift: null` (Out-of-Scope v2 item), not a gap."
---

# Milestone v1.2.2 — scenario-tab-factsheet-parity — Audit

**Definition of done:** Render the REAL factsheet on a hypothetical scenario blend,
computed client-side (compute.ts), with parity to the real `/factsheet/[id]/v2`
route — constituent diversification, honest peer-percentile + per-constituent
mandate + own-book delta, compose+read on one surface, behind permanent
byte-identity / a11y / coverage / no-state-bleed guards. Phases 39–43.

## Verdict: PASSED

All 24 requirements delivered and verified-wired end-to-end in code. No critical
gaps, no broken flows, no orphaned exports, no unprotected routes. Hard invariants
intact. Remaining items are bookkeeping drift (corrected here), one stale archived
verification note, and deferred-by-construction authed visual canaries.

## Scores

| Dimension | Score | Notes |
|-----------|-------|-------|
| Requirements | 24/24 | All WIRED in code (integration-checker file:line evidence) |
| Phases | 5/5 | 39–42 VERIFICATION=passed; 43=human_needed (authed canary only, structure verified) |
| Integration | 24/24 | 0 orphaned exports, 0 missing connections |
| E2E flows | 1/1 | North-star scenario-factsheet-parity flow traces end-to-end |

## Requirements Coverage (3-source cross-reference)

| Phase | Reqs | VERIFICATION | Integration | Final |
|-------|------|--------------|-------------|-------|
| 39 Complete payload adapter | PAYLOAD-01..05 | passed | WIRED ×5 | **satisfied** |
| 40 Mount real factsheet body | BODY-01..04 | passed | WIRED ×4 | **satisfied** |
| 41 Constituent correlation | CORR-01..06 | passed | WIRED ×6 | **satisfied** |
| 42 Peer-cohort + mandate | PEER-01..05 | passed | WIRED ×5 | **satisfied** |
| 43 Edge states, fold & guards | GUARD-01..04 | human_needed* | WIRED ×4 | **satisfied** |

*43 human_needed = authed visual canary only; all 4 GUARDs structurally verified
(full suite 6768 green, coverage ratchet green, axe e2e CI-wired, byte-identity +
no-bleed permanent gates installed).

## Hard Invariants

| Invariant | Status |
|-----------|--------|
| `src/lib/scenario.ts` zero-diff this milestone | CONFIRMED |
| `ScenarioComposer.tsx` zero `FactsheetBody` literals (mount via ScenarioFactsheetChart) | CONFIRMED |
| Real `/factsheet/[id]/v2` + Overview byte-identical (scenarioMode default false) | CONFIRMED |
| `/api/scenario/peer-rank` auth chain (same-origin → auth → approved → limiter) | CONFIRMED |

## E2E Flow — North-Star (verified by integration-checker)

ScenarioComposer (portfolioDaily + scenarioPeer/Mandate/OwnBookDelta) →
ScenarioFactsheetChart (buildScenarioFactsheetPayload → compute()) →
FactsheetProvider persist={false} → real FactsheetBody scenarioMode →
MetricsColumn (additive peer gate + OwnBookDeltaPanel + ConstituentMandatePanel) →
panels read payload via context. Peer fetch: ScenarioComposer → /api/scenario/peer-rank
→ get_verified_cohort_rank RPC (RLS-scoped, min-cohort-N=20). No breaks.

## Disposition

Code delivered + verified. Per the project's **ship-then-tag** pattern (v1.0/v1.1/v1.2/v1.2.1),
`complete-milestone` (git tag `v1.2.2`) + `cleanup` are DEFERRED until `/ship` lands
the code on `main` — tagging the unmerged feature branch would point `v1.2.2` at a
commit absent from main after squash-merge. Next: `/ship`.
