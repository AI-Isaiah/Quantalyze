---
phase: 32-dead-link-fix-route-retirement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/scenarios/page.tsx
  - src/app/(dashboard)/scenarios/page.test.ts
  - src/app/(dashboard)/scenarios/page.role-gate.test.ts
  - src/components/scenarios/ScenarioBuilder.tsx
  - src/components/scenarios/ScenarioBuilder.honesty.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
autonomous: true
requirements: [FLOW-02]

must_haves:
  truths:
    - "Navigating to /scenarios issues a server 307 redirect to /allocations?tab=scenario (the unified composer deep-link)"
    - "The composer's blank-slate empty state no longer links to /scenarios (no self-redirect loop on the front door)"
    - "ScenarioBuilder.tsx and ScenarioBuilder.honesty.test.tsx are deleted; the /scenarios role-gate test is deleted (it imports the old page body and would go RED)"
    - "The composer honesty test still covers IMPACT-02 peer-rank suppression (the deleted file's coverage did not evaporate)"
  artifacts:
    - path: "src/app/(dashboard)/scenarios/page.tsx"
      provides: "Thin RSC that calls redirect('/allocations?tab=scenario')"
      contains: "redirect(\"/allocations?tab=scenario\")"
    - path: "src/app/(dashboard)/scenarios/page.test.ts"
      provides: "Wave-0 test asserting the 307 redirect target (replaces page.role-gate.test.ts)"
      contains: "/allocations?tab=scenario"
  key_links:
    - from: "src/app/(dashboard)/scenarios/page.tsx"
      to: "/allocations?tab=scenario"
      via: "next/navigation redirect()"
      pattern: "redirect\\(\"/allocations\\?tab=scenario\"\\)"
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      to: "IMPACT-02 percentile-rank-badge suppression"
      via: "existing R3 guard at ScenarioComposer.test.tsx:2978-2993"
      pattern: "percentile-rank-badge"
---

<objective>
FLOW-02: Retire the legacy `/scenarios` Strategy-Sandbox surface into the unified composer with no dead links — WITHOUT touching `/portfolios` (which has no route-level role gate and 25 consumers; adding a redirect there would strand managers — RESEARCH.md FLOW-02 + Pitfall 5).

Four coordinated edits (RESEARCH.md FLOW-02 + landmines #2/#3/#4):
1. Replace `/scenarios/page.tsx` body with a thin server-component `redirect("/allocations?tab=scenario")` (Next.js 16 `redirect()` from `next/navigation`, 307 default — NOT a `next.config` redirect, NOT `permanentRedirect`).
2. Delete `page.role-gate.test.ts` in the SAME change — it `await import("./page")` and asserts a `createAdminClient()` read that no longer happens, so it goes RED after conversion (Pitfall 3).
3. Delete the composer's self-referential `/scenarios` link (`ScenarioComposer.tsx:1619-1624`) — after retirement it loops the user from the composer back into the composer (landmine #2).
4. Delete `ScenarioBuilder.tsx` + `ScenarioBuilder.honesty.test.tsx` AFTER asserting the composer honesty test already covers IMPACT-02 (it does — `ScenarioComposer.test.tsx:2978-2993` is a verified superset; RESEARCH.md IMPACT-02 migration section). The only ScenarioBuilder-unique assertion (`sandbox-example-universe-badge`) is intrinsic to the retired surface and needs no port.

Phase 29 (the HARD predecessor) is COMPLETE — the example universe is absorbed into the merged Browse catalog (`is_example` tag), so retiring `/scenarios` now strands nothing.

Purpose: collapse one of the three fractured allocator surfaces into the composer; net security improvement (removes a `createAdminClient()` RLS-bypass read).
Output: 307 redirect + 3 deletions + self-loop removed + a new redirect test + an IMPACT-02 parity assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dead-link-fix-route-retirement/32-RESEARCH.md

# AGENTS.md mandate: this is NOT the Next.js you know. READ
# node_modules/next/dist/docs/01-app/03-api-reference/04-functions/redirect.md
# BEFORE writing the redirect — verify: redirect() from next/navigation, 307
# default, throws NEXT_REDIRECT, return type `never` (no `return` needed), and
# it must NOT be a next.config redirect (loses any auth context) nor
# permanentRedirect/308 (CDN-cacheable; RESEARCH.md Runtime State Inventory).

<interfaces>
<!-- Extracted from codebase — executor uses these directly. -->

Current /scenarios page (src/app/(dashboard)/scenarios/page.tsx):
  - `export const dynamic = "force-dynamic"` + a role gate (createClient → profiles.role IN allocator/both) + a `createAdminClient()` read of is_example strategies feeding `<ScenarioBuilder strategies={enriched} />`. ALL of this is REPLACED by the 3-line redirect.

page.role-gate.test.ts (src/app/(dashboard)/scenarios/page.role-gate.test.ts):
  - `redirectMock` hoisted; mocks next/navigation.redirect to throw `__REDIRECT__:${path}`.
  - Asserts the admin-client read happens for allocators and NOT for managers. Becomes false after conversion → DELETE. Reuse its redirectMock pattern in the new page.test.ts.

Redirect target (VERIFIED deep-link): /allocations?tab=scenario
  - AllocationsTabs derives activeTab from searchParams; AllocationsTabs.test.tsx:272-283 asserts tab=scenario → Scenario panel visible. InsightStrip.tsx:163 already links here.

ScenarioComposer self-loop (src/app/(dashboard)/allocations/components/ScenarioComposer.tsx):
  - L1619-1624 block:
      <p className="mt-6 text-xs text-text-muted">
        Want to compare strategies without your portfolio?{" "}
        <Link href="/scenarios" className="text-accent underline">
          Try the Strategy Sandbox →
        </Link>
      </p>
  - Delete the WHOLE <p> (1619-1624). The blank-slate already offers "Browse strategies" (button, L1611-1617) for the same intent.

Composer honesty coverage (ScenarioComposer.test.tsx — VERIFIED superset of ScenarioBuilder.honesty):
  - :2978-2993  IMPACT-02 percentile-rank-badge absent + non-vacuous positive control (STRONGER — runs with the Phase-30 blend panels mounted).
  - :3388-3404 / :3408-3417  PROJECTED badge text + neutral-outline pill.
  - :3440-3454  coverage caveat. :3360-3378 CORR-03 "Avg |ρ|".
  - Blank-slate empty state test at :409 ("Start a portfolio") — add the self-link-removed assertion near here.

ScenarioBuilder.tsx EquityCurveChart (L57) / MetricCard (L486) are FILE-PRIVATE (never exported) — they vanish with the file; knip orphans nothing (RESEARCH.md knip section). knip is gated in Plan 03.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert /scenarios to a 307 redirect; replace its role-gate test with a redirect test</name>
  <read_first>
    - src/app/(dashboard)/scenarios/page.tsx (the whole file being replaced)
    - src/app/(dashboard)/scenarios/page.role-gate.test.ts (the redirectMock pattern to reuse; the file to delete)
    - node_modules/next/dist/docs/01-app/03-api-reference/04-functions/redirect.md (Next.js 16 redirect API — AGENTS.md mandate)
    - .planning/phases/32-dead-link-fix-route-retirement/32-RESEARCH.md (FLOW-02 redirect section + Pitfall 3)
  </read_first>
  <action>
    Replace the ENTIRE contents of `src/app/(dashboard)/scenarios/page.tsx` with a thin server component that imports `redirect` from `next/navigation` and, in a default-exported function `ScenariosPage`, calls `redirect("/allocations?tab=scenario")`. Drop `dynamic`, the role gate, the admin client, and the ScenarioBuilder import — none are reachable after an unconditional redirect (a logged-out user hitting `/allocations` is bounced to `/login` by the existing dashboard guards; preserving the old `?next=/scenarios` is pointless — we never want to send anyone back). Use the default 307; do NOT use `permanentRedirect`. No `return` is needed (redirect returns `never`).

    Delete `src/app/(dashboard)/scenarios/page.role-gate.test.ts` (its admin-read assertions are now false → it would fail CI; Pitfall 3).

    Create `src/app/(dashboard)/scenarios/page.test.ts` mirroring the deleted test's `redirectMock` hoist pattern (mock `next/navigation.redirect` to record the path and throw to unwind). Assert: calling the default export redirects to exactly `/allocations?tab=scenario` (non-vacuous — fail if the target string changes). Mock `server-only` if the import path requires it.
  </action>
  <acceptance_criteria>
    - `grep -c 'redirect("/allocations?tab=scenario")' "src/app/(dashboard)/scenarios/page.tsx"` == 1.
    - `grep -c 'permanentRedirect\|createAdminClient\|ScenarioBuilder\|force-dynamic' "src/app/(dashboard)/scenarios/page.tsx"` == 0.
    - `src/app/(dashboard)/scenarios/page.role-gate.test.ts` no longer exists (`test ! -f`).
    - `src/app/(dashboard)/scenarios/page.test.ts` exists and asserts the `/allocations?tab=scenario` target.
    - `npx vitest run "src/app/(dashboard)/scenarios/page.test.ts"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/scenarios/page.test.ts" && test ! -f "src/app/(dashboard)/scenarios/page.role-gate.test.ts"</automated>
  </verify>
  <done>/scenarios is a 307 redirect to the composer; the stale role-gate test is gone; a new test pins the redirect target.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the composer self-loop link; assert IMPACT-02 parity; delete ScenarioBuilder + its honesty test</name>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the self-loop <p> at L1619-1624)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (blank-slate test ~L409; existing IMPACT-02 guard at :2978-2993)
    - src/components/scenarios/ScenarioBuilder.honesty.test.tsx (the 4 assertions; confirm parity before deleting — RESEARCH.md IMPACT-02 migration table)
    - .planning/phases/32-dead-link-fix-route-retirement/32-RESEARCH.md (IMPACT-02 migration section: the parity table + the one unique-but-not-portable assertion)
  </read_first>
  <action>
    In `ScenarioComposer.tsx`: delete the entire self-referential paragraph at L1619-1624 (the "Want to compare strategies without your portfolio? … Try the Strategy Sandbox →" `<p>` linking `href="/scenarios"`). Do not alter the sibling "Browse strategies" button or any other blank-slate content (surgical). Confirm no remaining `href="/scenarios"` in the file.

    In `ScenarioComposer.test.tsx`: add ONE assertion in the blank-slate empty-state test (near "Start a portfolio", ~L409) that the rendered blank slate contains NO anchor with `href="/scenarios"` and NO "Strategy Sandbox" text — this pins landmine #2 (non-vacuous: it would fail today, before the L1619-1624 deletion). Do NOT weaken the existing IMPACT-02 guard at :2978-2993; instead add a brief code comment near it noting it is now the SOLE peer-rank-suppression coverage after the ScenarioBuilder honesty test is deleted (the parity is verified in RESEARCH.md — the composer guard is a superset).

    Verify parity BEFORE deleting: confirm `ScenarioComposer.test.tsx` already asserts `percentile-rank-badge` absence with a positive control. Then delete `src/components/scenarios/ScenarioBuilder.tsx` and `src/components/scenarios/ScenarioBuilder.honesty.test.tsx`. (The only ScenarioBuilder-unique assertion — `sandbox-example-universe-badge` — is intrinsic to the retired example-universe surface; provenance now lives as the `is_example` Browse tag with its own Phase-29 coverage; do NOT port it. RESEARCH.md.)

    Do NOT delete `src/components/scenarios/SampleFloorEmptyState.tsx` or anything else in that directory — only the two ScenarioBuilder files leave.
  </action>
  <acceptance_criteria>
    - `grep -c 'href="/scenarios"' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` == 0.
    - `grep -c 'Strategy Sandbox' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` == 0 (the only matches were the self-loop block).
    - `src/components/scenarios/ScenarioBuilder.tsx` and `src/components/scenarios/ScenarioBuilder.honesty.test.tsx` no longer exist (`test ! -f` both).
    - `grep -c 'percentile-rank-badge' "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` ≥ 1 (IMPACT-02 coverage preserved).
    - The new blank-slate assertion asserts no `href="/scenarios"` / no "Strategy Sandbox" in the empty state.
    - `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" && test ! -f src/components/scenarios/ScenarioBuilder.tsx && test ! -f src/components/scenarios/ScenarioBuilder.honesty.test.tsx</automated>
  </verify>
  <done>Self-loop link removed + pinned; ScenarioBuilder + honesty test deleted with IMPACT-02 coverage proven to survive on the composer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| any authenticated user → /scenarios route | a manager could previously deep-link to /scenarios; the old page used createAdminClient() (RLS-bypass) to read the institutional strategy universe. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-03 | Information disclosure | /scenarios createAdminClient() read | mitigate (ELIMINATED) | The retirement DELETES the admin-client read entirely — the page now reads nothing and only redirects. The C-0017 institutional-universe leak vector the old role gate guarded is removed at the source (net security improvement, RESEARCH.md Security Domain). The redirect target `/allocations` keeps its own auth (dashboard layout + page guards). |
| T-32-04 | Tampering (open redirect) | redirect() target | accept | The redirect target is a hardcoded internal path (`/allocations?tab=scenario`), not user-controlled — no open-redirect surface. |
| T-32-05 | Availability | composer self-loop removal | accept | Removing the dead self-link cannot strand a user; the blank slate still offers Browse strategies + Connect Exchange. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/scenarios/page.test.ts" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` passes.
- `git diff --exit-code src/lib/scenario.ts` clean (engine untouched — pinned hard by Plan 03, but do not edit it here).
- No `href="/scenarios"` remains in ScenarioComposer.tsx; the role-gate test and both ScenarioBuilder files are deleted.
- IMPACT-02 `percentile-rank-badge` suppression still asserted on the composer test.
</verification>

<success_criteria>
`/scenarios` resolves into the unified composer via a 307 with no dead link, the composer no longer loops back to itself, and the example-universe Sandbox surface is fully retired with zero honesty-coverage loss (FLOW-02 success criterion 2 + the IMPACT-02 migration exit gate).
</success_criteria>

<output>
Create `.planning/phases/32-dead-link-fix-route-retirement/32-02-SUMMARY.md` when done
</output>
