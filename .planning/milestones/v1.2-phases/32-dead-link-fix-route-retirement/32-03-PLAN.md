---
phase: 32-dead-link-fix-route-retirement
plan: 03
type: execute
wave: 2
depends_on: ["32-01", "32-02"]
files_modified:
  - src/components/layout/Sidebar.tsx
  - src/components/layout/Sidebar.test.tsx
  - src/__tests__/phase-32-frozen-spine-guards.test.ts
autonomous: true
requirements: [FLOW-03]

must_haves:
  truths:
    - "The sidebar nav has ONE allocator entry point to the composer (/allocations); the legacy /scenarios 'Strategy Sandbox' nav item is gone"
    - "Managers still see /portfolios in nav (not orphaned); /allocations + /portfolios are unchanged as nav targets"
    - "A new allocator's single nav entry lands on the Phase-29 blank-slate composer front door"
    - "A durable phase-32 guard pins the retirement invariants: scenario.ts zero-diff, /scenarios is a redirect, ScenarioBuilder is deleted, no /scenarios nav item, and the 2 portfolio-context add links carry ?portfolio= (no bare-slug dead link in the portfolios tree)"
  artifacts:
    - path: "src/components/layout/Sidebar.tsx"
      provides: "Sidebar with the /scenarios (Strategy Sandbox) nav item removed + the now-unused BeakerIcon import dropped"
      contains: "/allocations"
    - path: "src/components/layout/Sidebar.test.tsx"
      provides: "Sidebar tests with the Sandbox describe block removed; remaining nav assertions green"
    - path: "src/__tests__/phase-32-frozen-spine-guards.test.ts"
      provides: "Frozen-spine + phase-32 retirement content guard (engine zero-diff + the FLOW-02/03 structural invariants), mirroring phase-31"
      contains: "scenario.ts"
  key_links:
    - from: "src/components/layout/Sidebar.tsx"
      to: "/allocations"
      via: "single allocator nav item (composer)"
      pattern: "/allocations"
    - from: "src/__tests__/phase-32-frozen-spine-guards.test.ts"
      to: "src/lib/scenario.ts"
      via: "git-delta zero-diff baseline (fail-loud)"
      pattern: "scenario\\.ts"
---

<objective>
FLOW-03: give a new allocator ONE discoverable entry point to the composer, and lock the Phase-32 retirement with a durable guard.

Two coordinated pieces (RESEARCH.md FLOW-03 + the phase exit gates):
1. **Nav consolidation:** `Sidebar.tsx` currently lists three fractured surfaces — `/allocations` (composer), `/scenarios` (Strategy Sandbox, L74-80), `/portfolios` (managers). Remove the `/scenarios` "Strategy Sandbox" nav item + the now-unused `BeakerIcon` import. Keep `/allocations` as the single allocator entry (it lands on the Phase-29 blank-slate front door) and keep `/portfolios` (manager-by-nav-convention — do NOT orphan managers). Update `Sidebar.test.tsx` (delete the Sandbox describe block; `DashboardChrome.test.tsx` needs no change — RESEARCH.md verified).
2. **Phase-32 frozen-spine + retirement guard:** a new `src/__tests__/phase-32-frozen-spine-guards.test.ts` mirroring `phase-31-frozen-spine-guards.test.ts` — pins `src/lib/scenario.ts` zero-diff (fail-loud baseline) AND content-inspects the live source for the Phase-32 structural invariants so a future regression trips CI. It also runs `knip` as an exit-gate assertion (the ScenarioBuilder deletion from 32-02 must leave knip clean; RESEARCH.md verified the file-private functions orphan nothing — this gate proves it stays true).

Purpose: collapse the three-surface nav to one allocator door + make the retirement non-reversible-by-accident.
Output: Sidebar nav item removed + tests updated + a durable phase-32 guard (incl. a knip-clean assertion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/32-dead-link-fix-route-retirement/32-RESEARCH.md

# This plan is Wave 2 — it runs AFTER 32-01 (FLOW-01 ?portfolio= links) and
# 32-02 (FLOW-02 /scenarios redirect + ScenarioBuilder delete + self-loop removal).
# The guard's content assertions and the knip-clean check depend on those edits
# already being on disk.

<interfaces>
<!-- Extracted from codebase + RESEARCH.md — executor uses these directly. -->

Sidebar (src/components/layout/Sidebar.tsx):
  - L62: /allocations nav item (KEEP — the composer, single allocator entry).
  - L74-80: the /scenarios "Strategy Sandbox" nav item (REMOVE the whole entry).
  - L84: { label: "Portfolios", href: "/portfolios", icon: PieChartIcon } (KEEP — managers).
  - BeakerIcon: a FILE-PRIVATE function definition (Sidebar.tsx:273-281), NOT an import. Used ONLY by the Sandbox item being removed → delete the function definition too (else an unused-function lint/tsc-noUnusedLocals error). Verified: no other file imports it.

Sidebar.test.tsx (src/components/layout/Sidebar.test.tsx):
  - Has a Sandbox describe/it block (RESEARCH cites the /scenarios link assertion ~L100). Delete the Sandbox block; keep the /allocations + /portfolios assertions.
  - DashboardChrome.test.tsx: NO change needed (verified — it does not assert the Sandbox item).

Phase-31 guard to mirror (src/__tests__/phase-31-frozen-spine-guards.test.ts):
  - resolveBaselineRef() with a FALLBACK_BASE_SHA + fail-loud throw; git-delta zero-diff of src/lib/scenario.ts; readFileSync content gates; non-vacuity self-pins.
  - Reuse the SAME baseline-resolution helper shape; set FALLBACK_BASE_SHA to this branch's merge-base with origin/main (b8a0337b) — the same base phase-31 used.

knip: project has a knip config (RESEARCH.md knip section). Run the project's knip command (e.g. `npx knip` or the npm script) and assert exit 0 / no new orphans introduced by the ScenarioBuilder deletion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the /scenarios "Strategy Sandbox" nav item + unused BeakerIcon; update Sidebar tests</name>
  <read_first>
    - src/components/layout/Sidebar.tsx (the nav array L62/L74-80/L84 + the BeakerIcon import)
    - src/components/layout/Sidebar.test.tsx (the Sandbox describe block to delete; the /allocations + /portfolios assertions to keep)
    - .planning/phases/32-dead-link-fix-route-retirement/32-RESEARCH.md (FLOW-03 section — exact lines + the DashboardChrome no-change finding)
  </read_first>
  <action>
    In `Sidebar.tsx`: delete the entire `/scenarios` "Strategy Sandbox" nav entry (L74-80). Then delete the now-unused `BeakerIcon` FILE-PRIVATE function definition (Sidebar.tsx:273-281) — it is NOT an import; it is defined in-file and used ONLY by the removed Sandbox item (grep the file to confirm no other reference). Leave the `/allocations` (L62) and `/portfolios` (L84) entries and every other nav item byte-identical (surgical). Do NOT add a redirect or reorder.

    In `Sidebar.test.tsx`: delete the Sandbox describe/it block (the one asserting the `/scenarios` link, ~L100). Do NOT weaken the `/allocations` or `/portfolios` nav assertions. If a render-snapshot or nav-count assertion counts items, update the expected count by one (the removed Sandbox item) — do not delete the count assertion, adjust it.

    Do NOT touch `DashboardChrome.test.tsx` (RESEARCH verified it does not reference the Sandbox).
  </action>
  <acceptance_criteria>
    - `grep -c '"/scenarios"' src/components/layout/Sidebar.tsx` == 0 (no Sandbox nav href).
    - `grep -c 'BeakerIcon' src/components/layout/Sidebar.tsx` == 0 (unused icon import dropped).
    - `grep -c '"/allocations"' src/components/layout/Sidebar.tsx` >= 1 AND `grep -c '"/portfolios"' src/components/layout/Sidebar.tsx` >= 1 (single allocator entry kept; managers not orphaned).
    - `npx vitest run src/components/layout/Sidebar.test.tsx` exits 0.
    - `npx tsc --noEmit` exits 0 (no unused-import / no-unused-locals error from the dropped icon).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/layout/Sidebar.test.tsx && test "$(grep -c '"/scenarios"' src/components/layout/Sidebar.tsx)" = "0"</automated>
  </verify>
  <done>The sidebar has one allocator entry (/allocations); the Sandbox /scenarios item + its unused icon are gone; managers keep /portfolios; tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the phase-32 frozen-spine + retirement content guard (incl. knip-clean)</name>
  <read_first>
    - src/__tests__/phase-31-frozen-spine-guards.test.ts (the pattern to mirror — baseline resolution, fail-loud, content gates, non-vacuity self-pins)
    - src/lib/scenario.ts (the frozen engine — the guard asserts zero-diff; do NOT edit it)
    - src/app/(dashboard)/scenarios/page.tsx (post-32-02: must contain the redirect — content gate reads it)
    - src/components/layout/Sidebar.tsx (post-Task-1: must NOT contain "/scenarios")
    - .planning/phases/32-dead-link-fix-route-retirement/32-RESEARCH.md (the exit-gate list + the knip command)
  </read_first>
  <action>
    Create `src/__tests__/phase-32-frozen-spine-guards.test.ts` mirroring the phase-31 guard's structure:
    - `resolveBaselineRef()` with a `FALLBACK_BASE_SHA` = this branch's base (b8a0337b) and a fail-loud throw if no baseline resolves (Rule 12).
    - Assert ZERO git-delta to `src/lib/scenario.ts` vs the baseline (frozen engine — SCENARIO-05).
    - Content gates (readFileSync the live source — durable structural invariants of the Phase-32 retirement):
      1. `src/app/(dashboard)/scenarios/page.tsx` contains `redirect("/allocations?tab=scenario")` AND does NOT contain `createAdminClient` / `ScenarioBuilder` (FLOW-02 redirect landed).
      2. `src/components/scenarios/ScenarioBuilder.tsx` does NOT exist (existsSync === false) — the Sandbox component is retired.
      3. `src/components/layout/Sidebar.tsx` does NOT contain `"/scenarios"` (FLOW-03 single entry).
      4. `src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` does NOT contain `href="/scenarios"` (no self-loop — landmine #2).
      5. The two portfolio-context add links carry `?portfolio=`: `portfolios/[id]/manage/page.tsx` and `portfolios/[id]/page.tsx` each contain `portfolio=` in a `/discovery/crypto-sma` href, AND there is NO bare `href="/discovery/crypto-sma"` (without `?portfolio=`) anywhere under `src/app/(dashboard)/portfolios/` (FLOW-01 attach-back; the 28 intentional landings elsewhere are untouched and NOT asserted here).
    - Add non-vacuity self-pins where a regex is used (assert the pattern matches a synthetic positive sample), mirroring the phase-31 guard's self-pins.

    Then run the project's `knip` command as an exit-gate assertion: confirm the ScenarioBuilder deletion (32-02) introduced NO new orphans (RESEARCH verified EquityCurveChart/MetricCard were file-private → nothing orphaned; this gate keeps that true). If knip is run in CI rather than as a vitest assertion, document the command in the test file header and run it once in <verify>; do NOT add a flaky network/slow call inside the unit test if knip is heavy — prefer running it in <verify>.
  </action>
  <acceptance_criteria>
    - `src/__tests__/phase-32-frozen-spine-guards.test.ts` exists; `npx vitest run src/__tests__/phase-32-frozen-spine-guards.test.ts` exits 0.
    - The guard asserts scenario.ts zero-diff (fail-loud baseline) + the 5 content gates above; non-vacuity self-pins present for any regex.
    - `npx knip` (or the project knip script) reports NO new unused files/exports introduced by Phase 32 (exit 0, or the pre-existing baseline unchanged).
    - `git diff --name-only -- src/lib/scenario.ts` is empty (engine untouched).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/phase-32-frozen-spine-guards.test.ts && npx knip 2>&1 | tail -5</automated>
  </verify>
  <done>A durable phase-32 guard pins the retirement invariants (engine zero-diff, redirect present, ScenarioBuilder gone, no /scenarios nav or self-loop, portfolio links carry ?portfolio=); knip is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan removes a nav item and adds a test guard — no new inputs, routes, data flows, or auth surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-06 | Availability (manager orphaning) | Sidebar nav removal | mitigate | Removing ONLY the `/scenarios` item; `/portfolios` (managers) + `/allocations` (allocators) are explicitly kept. Acceptance criteria assert both hrefs remain. No manager is stranded. |
</threat_model>

<verification>
- `npx vitest run src/components/layout/Sidebar.test.tsx src/__tests__/phase-32-frozen-spine-guards.test.ts` passes.
- `npx tsc --noEmit` clean (dropped BeakerIcon import leaves no unused-locals error).
- `npx knip` reports no new orphans from the Phase-32 deletions.
- `git diff --name-only -- src/lib/scenario.ts` empty (frozen engine).
- Sidebar shows ONE allocator entry (/allocations) + manager /portfolios; no /scenarios item.
</verification>

<success_criteria>
A new allocator has a single discoverable nav entry to the composer (FLOW-03 success criterion 3), managers keep /portfolios, and the Phase-32 retirement is locked by a durable guard that fails CI if the redirect, the ScenarioBuilder deletion, the nav consolidation, the self-loop removal, or the ?portfolio= attach-back is ever regressed — with the frozen engine pinned zero-diff.
</success_criteria>

<output>
Create `.planning/phases/32-dead-link-fix-route-retirement/32-03-SUMMARY.md` when done
</output>
