---
phase: 30-factsheet-graphs-on-the-blend
plan: 02
type: execute
wave: 2
depends_on: ["30-01"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/__tests__/phase-30-frozen-spine-guards.test.ts
autonomous: true
requirements: [GRAPH-02, GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - "The composer projection shows a Returns-distribution Card (histogram + quantiles) of the blend"
    - "The composer projection shows a Rolling-metrics Card (Sharpe + vol + Sortino) with a 3M/6M/12M window toggle"
    - "Each new panel renders its OWN method/overlap-N/horizon disclosure above the floor and a role=status PartialDataBanner below it — never role=alert"
    - "The R3/IMPACT-02 percentile-rank guard stays non-vacuous WITH the new panels mounted (positive control finds a real PercentileRankBadge)"
    - "The composer source imports no FactsheetBody/MetricsColumn/buildAllocatorPortfolioFactsheetPayload and contains no ingestSource:\"api\" literal"
    - "src/lib/scenario.ts and src/lib/scenario.test.ts have ZERO diff vs the phase baseline (SCENARIO-05)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Two new <Card className=\"mt-6\"> siblings after the CorrelationHeatmap Card feeding leaf charts from buildBlendPanels"
      contains: "buildBlendPanels"
    - path: "src/__tests__/phase-30-frozen-spine-guards.test.ts"
      provides: "Frozen-engine zero-diff guard (scenario.ts + scenario.test.ts) mirroring the phase-29 guard"
      contains: "src/lib/scenario.ts"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/lib/scenario-blend-panels.ts"
      via: "buildBlendPanels(scenarioMetrics.portfolio_daily_returns ?? [], window)"
      pattern: "buildBlendPanels\\("
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/components/charts/ReturnHistogram, ReturnQuantiles, RollingMetrics, RollingVolatilityChart, RollingSortinoChart"
      via: "leaf-chart imports fed by the adapter — never the *Panel.tsx wrappers"
      pattern: "from \"@/components/charts/(ReturnHistogram|ReturnQuantiles|RollingMetrics|RollingVolatilityChart|RollingSortinoChart)\""
---

<objective>
Mount the two new blend-graph panels (Returns distribution = GRAPH-02; Rolling metrics = GRAPH-03) as Card siblings in the ScenarioComposer projection region, fed by `buildBlendPanels` from Plan 30-01, each with its own disclosure + honest empty branch (GRAPH-04). Extend the composer test so the R3/IMPACT-02 honesty guard stays non-vacuous with the panels mounted, add the static import guard, and add the frozen-engine zero-diff exit-gate guard.

Purpose: Deliver the user-visible distribution + rolling-metrics graphs on the blend without touching the frozen engine, and harden the LOCKED honesty + frozen-engine invariants with content-inspecting tests (the silent-failure surfaces this phase exists to guard).
Output: Two new Cards in ScenarioComposer.tsx + extended ScenarioComposer.test.tsx + new phase-30 frozen-spine guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-RESEARCH.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-PATTERNS.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-UI-SPEC.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-01-SUMMARY.md

<interfaces>
<!-- Contracts extracted this session. Mount point + leaf prop contracts + the existing R3 guard. -->

Adapter (from Plan 30-01 — consume, do not edit):
```typescript
import { buildBlendPanels, type BlendPanelSeries } from "@/lib/scenario-blend-panels";
// buildBlendPanels(portfolioDaily: {date,value}[], window: 63|126|252): BlendPanelSeries
//   { histogramSeries, quantiles, rollingSharpe (key "sharpe_365d"), rollingVol, rollingSortino, usableN }
```

Mount point — ScenarioComposer.tsx (verified line numbers this session):
- The data prop the NEW panels read (SAME as benchmark/stress/MC): `scenarioMetrics.portfolio_daily_returns ?? []` (reads at :1977 / :1994 / :2013). NEVER `equity_curve` (rounded/downsampled).
- `scenarioMetrics.n` is the overlapping-day count for disclosure copy (length === n on the success path, A4).
- Existing sibling Cards: ScenarioBenchmarkSection (:1975), StressVarSection (:1992), MonteCarloSection (:2011), WeightOptimizerSection (:2027), CorrelationHeatmap (:2048). The CorrelationHeatmap Card CLOSES at line 2064.
- INSERT the two new `<Card className="mt-6">` siblings AFTER the CorrelationHeatmap Card (line 2064), BEFORE the Bridge-flagged block `{flaggedHoldings.length > 0 && (` at line 2066.
- Existing imports already present: `Card`, `EquityChart`/`toWealth` (:86), `DrawdownChart` (:87), `CorrelationHeatmap` (:70).

Leaf-chart prop contracts (READ-ONLY — do NOT modify the leaves; verified self-gates):
```
ReturnHistogram({ returns: {date,value}[], benchmarkReturns?, bins=20 })   — returns is CUMULATIVE wealth → pass histogramSeries. null when <10 or max===min.
ReturnQuantiles({ data: Record<string,number[]> })                          — each value [q0,q25,q50,q75,q100]. null when 0 keys.
RollingMetrics({ data: Record<string,{date,value}[]>, overallSharpe?, daysOfHistory? }) — key "sharpe_365d"→CHART_ACCENT; pass daysOfHistory={usableN} so the avg line self-suppresses <365. null when 0 keys.
RollingVolatilityChart({ data: {date,value}[] })   — percent Y; role="img" aria-label="Rolling volatility". null when length 0.
RollingSortinoChart({ data: {date,value}[] })      — ratio Y; role="img" aria-label="Rolling Sortino". null when length 0.
SegmentedControl({ options:{id,label,disabled?}[], activeId, onChange, ariaLabel })
PartialDataBanner({ heading, body })               — role="status", neutral bg-surface-subtle. NEVER role="alert".
```

Existing R3/IMPACT-02 guard to EXTEND — ScenarioComposer.test.tsx:2916-2953 (do not replace):
```typescript
// Positive control: screen.getByTestId("kpi-strip-mock") present.
// ABSENT asserts (keyed on unique render-only testids, NOT queryByText(/percentile/i) which is vacuous):
expect(document.getElementById("factsheet-allocator")).toBeNull();
expect(document.getElementById("factsheet-signatures")).toBeNull();
expect(screen.queryByTestId("percentile-rank-badge")).toBeNull();
expect(screen.queryByText(/ranked against peers/i)).toBeNull();
// Positive control proves NON-VACUITY:
cleanup(); render(<PercentileRankBadge metric="sharpe" percentile={95} />);
expect(screen.getByTestId("percentile-rank-badge")).toBeInTheDocument();
// PercentileRankBadge already imported at :147. vi.mock spies for EquityChart/DrawdownChart/KpiStrip at :70-127.
```

Phase-29 frozen-spine guard to MIRROR — src/__tests__/phase-29-frozen-spine-guards.test.ts:
```typescript
// execFileSync("git", ["diff","--name-only", BASE, "HEAD"]) ∪ ls-files --others --exclude-standard,
// resolveBaselineRef() (merge-base origin/main HEAD, fallback sha, FAIL LOUD if neither), assert
// CHANGED does NOT contain the frozen path. FALLBACK_BASE_SHA for phase 30 = "03d0699c" (HEAD at planning).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount the Returns-distribution + Rolling-metrics Cards in ScenarioComposer</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1948-2066 (the equity/drawdown grid + the existing mt-6 Card siblings + the CorrelationHeatmap Card close at :2064 + the Bridge block at :2066 — the exact insert window)
    - src/components/charts/ReturnHistogram.tsx, ReturnQuantiles.tsx, RollingMetrics.tsx, RollingVolatilityChart.tsx, RollingSortinoChart.tsx (prop contracts + self-gates — read-only)
    - src/components/strategy-v2/SegmentedControl.tsx, PartialDataBanner.tsx (toggle + empty-banner contracts — read-only)
    - 30-UI-SPEC.md §"Component Inventory" 1/2/3 + §"Copywriting Contract" (the prescriptive headings, sub-headings, disclosure + empty copy — VERBATIM)
    - 30-PATTERNS.md "ScenarioComposer.tsx (MODIFIED)" section (the Card-with-heading + caveat pattern, the leaf prop table, the MUST-NOT-import list)
  </read_first>
  <behavior>
    - With ≥10 overlapping daily returns: a Card data-panel="blend-returns-distribution" renders ReturnHistogram(returns=histogramSeries, bins=20) + ReturnQuantiles(data=quantiles) + the disclosure line "Distribution of {n} overlapping daily returns · historical realized · not a forecast."
    - With ≥ selected-window overlapping returns: a Card data-panel="blend-rolling" renders a SegmentedControl (3M/6M/12M → 63/126/252, default 6M=126), RollingMetrics(data=rollingSharpe, daysOfHistory={usableN}) + RollingVolatilityChart(data=rollingVol) + RollingSortinoChart(data=rollingSortino) + the disclosure "{W}-day rolling window · 252-day annualized · {n} overlapping days · not a forecast."
    - Below floor (portfolio_daily_returns.length < 10, or < selected window): the panel body swaps to PartialDataBanner(role="status") with the prescribed copy; the Card heading stays; NO role="alert" anywhere.
  </behavior>
  <action>Import `{ buildBlendPanels }` from `@/lib/scenario-blend-panels`; import the five leaf charts from `@/components/charts/*` and `SegmentedControl` + `PartialDataBanner` from `@/components/strategy-v2/*`. Compute panels via `buildBlendPanels(scenarioMetrics.portfolio_daily_returns ?? [], rollingWindow)` inside the component (wrap in `useMemo` keyed on `portfolio_daily_returns` + `rollingWindow` to match the props-only sibling discipline). Add `const [rollingWindow, setRollingWindow] = useState(126)` (6M default per UI-SPEC; the SegmentedControl options map 3M→63, 6M→126, 12M→252; disable an option when `usableN < its window` via the `disabled` flag). Insert TWO `<Card className="mt-6">` siblings AFTER the CorrelationHeatmap Card (closes :2064) and BEFORE `{flaggedHoldings.length > 0 && (` (:2066), mirroring the CorrelationHeatmap Card chrome (H2 `text-base font-semibold text-text-primary`, sub-heading H3 `text-xs font-normal uppercase tracking-wider text-text-secondary`, disclosure `text-xs text-text-muted`). Card 1 = Returns distribution (`data-panel="blend-returns-distribution"` `aria-label="Returns distribution"`): heading "Returns distribution", sub-sections "Return histogram" → ReturnHistogram + "Return quantiles" → ReturnQuantiles, with the GRAPH-04 disclosure line. Card 2 = Rolling metrics (`data-panel="blend-rolling"` `aria-label="Rolling metrics"`): heading "Rolling metrics", SegmentedControl(ariaLabel="Rolling window"), sub-sections "Rolling Sharpe"→RollingMetrics, "Rolling volatility"→RollingVolatilityChart, "Rolling Sortino"→RollingSortinoChart, with the GRAPH-04 disclosure. Empty branch on EACH Card: when `portfolioDaily.length < 10` (distribution) or `usableN < rollingWindow` (rolling), swap the body to `<PartialDataBanner heading="Awaiting more data" body="…" />` (copy verbatim from UI-SPEC §Copywriting) — keep the Card heading; NEVER `role="alert"`. Use VERBATIM copy from 30-UI-SPEC §Copywriting Contract (surface noun "portfolio", never "scenario"). DO NOT import or render `FactsheetBody`/`MetricsColumn`/`buildAllocatorPortfolioFactsheetPayload`/`PercentileRankBadge` or any `strategy-v2/*Panel.tsx` wrapper; introduce NO `ingestSource:"api"` literal; structurally omit any Trade/Position/Greeks/rolling-alpha-beta panel. Do NOT reorder to graphs-lead or add collapsibility (that is Phase 31) — these are plain Card siblings.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - source: ScenarioComposer.tsx imports `buildBlendPanels` and the five named leaf charts; imports NO `*Panel.tsx` wrapper, NO `FactsheetBody`/`MetricsColumn`/`buildAllocatorPortfolioFactsheetPayload`/`PercentileRankBadge`; contains no `ingestSource:"api"` substring.
    - source: the new panels read `scenarioMetrics.portfolio_daily_returns ?? []` (grep finds a 4th occurrence of `portfolio_daily_returns` in a buildBlendPanels call); no `equity_curve` read in the new code.
    - behavior: both Cards carry `data-panel="blend-returns-distribution"` and `data-panel="blend-rolling"`; the existing composer test suite still passes (panels mount in the default render).
    - test-command: `grep -c 'role="alert"' ` against the two new Card blocks is 0; the empty branches use `PartialDataBanner`.
  </acceptance_criteria>
  <done>Two Cards mounted after CorrelationHeatmap feeding leaf charts from buildBlendPanels; each owns disclosure + role=status empty branch; no forbidden imports; no graphs-lead reorder.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the composer test — R3-guard-with-panels, per-panel empty, static import guard</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx:70-127 (existing vi.mock spies for EquityChart/DrawdownChart/KpiStrip — the mock precedent for new leaves), :147 (PercentileRankBadge import), :2916-2953 (the R3 guard to extend)
    - 30-PATTERNS.md "ScenarioComposer.test.tsx (MODIFIED)" section (the three required extensions + the mock precedent + vi.mocked(X).mock.calls technique)
    - 30-RESEARCH.md Pitfall 5 (vacuity hazard — the positive control must fail loudly if the testid is renamed)
  </read_first>
  <behavior>
    - R3-guard-with-panels: the default render mounts both new Cards; assert `screen.getByTestId` / `data-panel="blend-returns-distribution"` and `data-panel="blend-rolling"` ARE present, AND the existing ABSENT asserts (percentile-rank-badge null, /ranked against peers/i null, factsheet-allocator/signatures null) still hold WITH the panels mounted; the isolated positive control (real PercentileRankBadge) stays intact and finds the testid.
    - Per-panel empty: render with portfolio_daily_returns below floor (length < 10) → assert each panel body shows a PartialDataBanner (role="status") with the prescribed copy AND no role="alert" appears; render with a healthy ≥252-pt series → assert each panel's disclosure line ("… overlapping … not a forecast.") is present (heading matches body — #509 rule).
    - Static import guard: a source-read test asserts ScenarioComposer.tsx source does NOT import FactsheetBody/MetricsColumn/buildAllocatorPortfolioFactsheetPayload and contains no `ingestSource:"api"` literal (reads the .tsx via fs — non-vacuous).
  </behavior>
  <action>Extend the existing R3 guard `it` (or add adjacent `it`s in the same describe) per the three behaviors. (1) In the R3 guard render, add asserts that `document.querySelector('[data-panel="blend-returns-distribution"]')` and `[data-panel="blend-rolling"]` are non-null (the panels mounted) alongside the existing ABSENT asserts — proving the guard runs WITH the new surface present; keep the `cleanup(); render(<PercentileRankBadge…/>)` positive control unchanged. (2) Add an `it` "blend panel empty branch" that renders with a payload whose `portfolio_daily_returns` is < 10 points → `screen.getByRole("status")` present (PartialDataBanner) and `screen.queryByRole("alert")` null for the panel region; and an `it` "blend panel disclosure" with a ≥252-pt series asserting the disclosure copy `/overlapping daily returns/i` + `/not a forecast/i` is present. Mock the five new leaf charts with `vi.mock` inert spies following the :70-127 precedent (so the test asserts the panel CHROME — Card/heading/disclosure/empty — not the recharts internals); use `vi.mocked(ReturnHistogram).mock.calls[0][0]` to assert the histogram receives the cumulative `histogramSeries` (value[0] ≈ 1+r). (3) Add an `it` "no factsheet import on the blend path" reading ScenarioComposer.tsx via `node:fs` and asserting the source matches none of `/FactsheetBody|MetricsColumn|buildAllocatorPortfolioFactsheetPayload|ingestSource:\s*["']api["']/`. Every assert must be falsifiable (Rule 9): the empty-branch test fails if a role=alert is used; the import guard fails if a forbidden import is added.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - source: the R3 guard `it` now asserts both `data-panel` Cards present AND the percentile-badge ABSENT, with the positive control retained.
    - behavior: an empty-branch `it` proves PartialDataBanner (role=status) renders below floor and NO role=alert; a disclosure `it` proves the per-panel "not a forecast" + overlapping-N copy renders above floor.
    - test-command: the static import-guard `it` reads the .tsx SOURCE via fs and would FAIL if FactsheetBody/MetricsColumn/buildAllocatorPortfolioFactsheetPayload were imported or `ingestSource:"api"` appeared; full file green.
  </acceptance_criteria>
  <done>Composer test extended: R3 guard non-vacuous with panels mounted (positive control intact), per-panel empty + disclosure tested, static import guard added; all green.</done>
</task>

<task type="auto">
  <name>Task 3: Frozen-engine zero-diff exit-gate guard (phase-30-frozen-spine-guards.test.ts)</name>
  <files>src/__tests__/phase-30-frozen-spine-guards.test.ts</files>
  <read_first>
    - src/__tests__/phase-29-frozen-spine-guards.test.ts (the EXACT pattern to mirror: execFileSync git, resolveBaselineRef with FALLBACK_BASE_SHA + FAIL-LOUD, changedFiles, .not.toContain asserts, non-vacuity note)
    - ROADMAP.md Phase 30 Exit Gates ("Frozen engine untouched: zero diff to src/lib/scenario.ts / src/lib/scenario.test.ts")
  </read_first>
  <action>Create `src/__tests__/phase-30-frozen-spine-guards.test.ts` mirroring the phase-29 guard verbatim in structure (copy `git()`, `refExists()`, `resolveBaselineRef()`, `changedFiles()` helpers unchanged). Set `FALLBACK_BASE_SHA = "03d0699c"` (HEAD at planning time this phase). Keep the FAIL-LOUD behavior (Rule 12): throw if no baseline ref resolves — never silently pass. Assert the phase delta `CHANGED` does NOT contain `src/lib/scenario.ts` (frozen engine, SCENARIO-05) and does NOT contain `src/lib/scenario.test.ts` (the 252-day annualization pins) — two `.not.toContain` asserts with the actionable VIOLATED messages. Drop the phase-29 scenarios/share-migration assertion (that gate belongs to phase 29; phase 30 adds no migration — but DO keep a one-line comment noting phase 30 is migration-free so a stray migration in the delta is still surfaced by the existing phase-29 guard if it lands). Include the same module-doc header explaining the silent-failure rationale + the non-vacuity note (touch scenario.ts → assertion FAILS, revert → green).</action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-30-frozen-spine-guards.test.ts && git diff --quiet HEAD -- src/lib/scenario.ts src/lib/scenario.test.ts && echo "ENGINE FROZEN"</automated>
  </verify>
  <acceptance_criteria>
    - source: the guard reads the REAL git delta via execFileSync (no hardcoded file list), resolves a baseline via merge-base/origin-main with `FALLBACK_BASE_SHA="03d0699c"`, and FAILS LOUD if neither resolves.
    - behavior: two `.not.toContain` asserts on `src/lib/scenario.ts` and `src/lib/scenario.test.ts`; `npx vitest run src/__tests__/phase-30-frozen-spine-guards.test.ts` green (engine + engine test unchanged this phase).
    - test-command: `git diff --quiet HEAD -- src/lib/scenario.ts src/lib/scenario.test.ts` exits 0 (no diff).
  </acceptance_criteria>
  <done>Phase-30 frozen-spine guard mirrors the phase-29 pattern, pins scenario.ts + scenario.test.ts zero-diff, fails loud on an unresolvable baseline; green with the engine untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blend projection → rendered panels | The honesty-relevant boundary: a peer/percentile/signature panel or an `ingestSource:"api"` factsheet payload leaking onto a HYPOTHETICAL blend is the security-of-trust failure for this phase (a what-if has no verified track record to rank — LOCKED IMPACT-02 invariant). |
| phase delta → frozen engine | The frozen-engine integrity boundary: any edit to `scenario.ts`/`scenario.test.ts` silently changes the 252-day annualization the whole product relies on. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Information disclosure / trust | ScenarioComposer blend panels | mitigate | R3/IMPACT-02 guard EXTENDED to run non-vacuously WITH the new panels mounted (percentile-rank-badge absent + ranking copy absent; real PercentileRankBadge positive control intact) — a future percentile/peer panel on the blend fails it. |
| T-30-05 | Tampering / spoofing | blend import path | mitigate | Static import guard: composer source must not import FactsheetBody/MetricsColumn/buildAllocatorPortfolioFactsheetPayload and must contain no `ingestSource:"api"` literal — blocks the api-only peer-panel path structurally. |
| T-30-06 | Tampering | frozen engine `scenario.ts` | mitigate | Phase-30 frozen-spine git-diff guard asserts zero diff to scenario.ts + scenario.test.ts; fails loud if the baseline is unresolvable (never a silent skip). |
| T-30-07 | Information disclosure / trust | per-panel disclosure / empty state | mitigate | Each panel owns its method/overlap-N/horizon line + a role="status" (never role="alert") below-floor banner; tested for both above- and below-floor branches (no fabricated zeros, no false-precision). |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | Phase 30 installs ZERO packages — no install task, no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` green (R3 guard non-vacuous with panels, per-panel empty + disclosure, static import guard).
- `npx vitest run src/__tests__/phase-30-frozen-spine-guards.test.ts` green.
- `git diff --quiet HEAD -- src/lib/scenario.ts src/lib/scenario.test.ts` exits 0.
- Forbidden-import check: `grep -E "FactsheetBody|MetricsColumn|buildAllocatorPortfolioFactsheetPayload|ingestSource:\s*[\"']api[\"']" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns nothing.
- `npm test` full suite green at wave merge.
</verification>

<success_criteria>
- Returns-distribution + Rolling-metrics Cards render on the blend, fed by buildBlendPanels.
- Each panel owns its disclosure + a role=status honest empty branch; never role=alert.
- R3/IMPACT-02 guard stays non-vacuous with the new panels mounted; static import guard blocks the api-only peer path.
- scenario.ts + scenario.test.ts are zero-diff (SCENARIO-05 intact).
- No graphs-lead reorder / no collapsibility (deferred to Phase 31).
</success_criteria>

<output>
Create `.planning/phases/30-factsheet-graphs-on-the-blend/30-02-SUMMARY.md` when done.
</output>
