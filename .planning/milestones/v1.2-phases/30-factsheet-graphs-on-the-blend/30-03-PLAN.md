---
phase: 30-factsheet-graphs-on-the-blend
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx
  - src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx
autonomous: true
requirements: [GRAPH-01]
must_haves:
  truths:
    - "The drawdown chart reads its axis-tick / gridline / axis-line / tooltip / negative-fill styling from chart-tokens.ts, not inline literal hexes"
    - "The equity chart already renders in the factsheet chart-stack identity (CSS-var token strokes) — verified, no change needed"
    - "GRAPH-01 changes are visual-identity only — no new data path, no relocation, the existing portfolio_daily_returns-derived series is untouched"
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx"
      provides: "Recharts drawdown chart reskinned to the chart-tokens.ts factsheet chart-stack contract"
      contains: "chart-tokens"
    - path: "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx"
      provides: "Chart-stack token assertion — drawdown axes/grid/tooltip/negative-fill read chart-tokens literals"
      contains: "CHART_"
  key_links:
    - from: "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx"
      to: "src/components/charts/chart-tokens.ts"
      via: "import CHART_TICK_STYLE / CHART_BORDER / CHART_TOOLTIP_STYLE / CHART_NEGATIVE / CHART_AXIS_TICK"
      pattern: "from \"@/components/charts/chart-tokens\""
---

<objective>
GRAPH-01: align the composer's already-mounted equity + drawdown charts to the DESIGN.md factsheet chart-stack visual identity. Source inspection at planning resolved this to a surgical token swap on `DrawdownChart.tsx` (it carries inline literal hexes `#DC2626` / `#64748B` / `#E2E8F0` and `fontSize: 11/12` instead of the `chart-tokens.ts` constants), plus a verification-only acceptance that `EquityChart` already reads the chart-stack tokens (its SVG strokes resolve `var(--color-chart-strategy)` = `#1B6B5A` = `CHART_ACCENT`).

Purpose: One factsheet chart stack on the blend — the drawdown chart must read the SAME literal-hex token contract every other factsheet chart reads, so a future palette change (e.g. the WCAG-audited shift Phase 33 anticipates) lands in one place instead of drifting.
Output: DrawdownChart.tsx reskinned to chart-tokens + a token-assertion test. No new data path, no relocation. This plan is independent of the adapter (Plan 30-01) and runs in Wave 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-RESEARCH.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-PATTERNS.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-UI-SPEC.md

<interfaces>
<!-- chart-tokens.ts literal-hex contract (verified this session) + the drawdown chart's current inline hexes to replace. -->

From src/components/charts/chart-tokens.ts (the factsheet chart-stack contract — Recharts can't resolve CSS vars, so it reads these literals):
```typescript
export const CHART_ACCENT = "#1B6B5A";
export const CHART_TEXT_MUTED = "#94A3B8";
export const CHART_BORDER = "#E2E8F0";       // axis lines / tooltip border
export const CHART_TRACK = "#F1F5F9";        // gridlines
export const CHART_AXIS_TICK = "#64748B";    // axis tick text fill
export const CHART_NEGATIVE = "#DC2626";     // drawdown fill / negative
export const CHART_FONT_MONO = "var(--font-mono), monospace";
export const CHART_TOOLTIP_STYLE = { /* fontSize, borderColor: CHART_BORDER, ... */ };
export const CHART_TICK_STYLE = { fontSize: 12, fill: CHART_AXIS_TICK, fontFamily: CHART_FONT_MONO }; // Geist Mono 12px tabular
```

Current inline hexes in DrawdownChart.tsx to REPLACE (verified line refs this session):
```
:155  liveStroke = hasScenario ? "var(--color-chart-benchmark)" : "#DC2626"   → CHART_NEGATIVE for the bare-hex arm
:213  <stop ... stopColor="#DC2626" stopOpacity={0.3} />                       → CHART_NEGATIVE
:214  <stop ... stopColor="#DC2626" stopOpacity={0.02} />                      → CHART_NEGATIVE
:227  tick={{ fontSize: 11, fill: "#64748B" }}                                 → CHART_TICK_STYLE
:229  axisLine={{ stroke: "#E2E8F0" }}                                         → { stroke: CHART_BORDER }
:234  tick={{ fontSize: 11, fill: "#64748B", fontFamily: "var(--font-geist-mono), monospace" }} → CHART_TICK_STYLE
:242  contentStyle={{ fontSize: 12, borderColor: "#E2E8F0" }}                  → CHART_TOOLTIP_STYLE
```

EquityChart compliance (verified — VERIFICATION-ONLY, do not edit): its SVG strokes already read `var(--color-chart-strategy)` (= #1B6B5A = CHART_ACCENT, :1564), `var(--color-chart-benchmark)` (:1518/:1583), `var(--color-border)` (:1494). It is a hand-rolled SVG (not Recharts) so CSS vars resolve correctly; it is already chart-stack compliant.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reskin DrawdownChart to the chart-tokens.ts contract</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx:150-245 (the Recharts XAxis/YAxis/Tooltip/Area + the inline hexes at :155/:213-214/:227/:229/:234/:242)
    - src/components/charts/chart-tokens.ts (CHART_TICK_STYLE, CHART_BORDER, CHART_TOOLTIP_STYLE, CHART_NEGATIVE, CHART_AXIS_TICK — the literals to swap in)
    - src/components/charts/RollingVolatilityChart.tsx:25-45 (the canonical token-consuming Recharts leaf — tick={CHART_TICK_STYLE}, axisLine={{stroke:CHART_BORDER}}, contentStyle={CHART_TOOLTIP_STYLE} — copy this exact usage shape)
    - 30-UI-SPEC.md §4 "GRAPH-01 — equity + drawdown chart-stack alignment" (the LOCKED token list + "no new data path, no relocation")
  </read_first>
  <behavior>
    - The drawdown chart's X/Y axis ticks render via CHART_TICK_STYLE (Geist Mono 12px tabular-nums, fill #64748B), axis lines via CHART_BORDER (#E2E8F0), tooltip via CHART_TOOLTIP_STYLE, and the drawdown gradient fill + bare-hex stroke arm via CHART_NEGATIVE (#DC2626) — all sourced from chart-tokens.ts, not inline string literals.
    - The existing data path is untouched: the chart still consumes the same `data`/`scenarioDailyPoints` props; the `hasScenario` benchmark-stroke arm (`var(--color-chart-benchmark)`) stays verbatim; no series math, no relocation, no prop-shape change.
  </behavior>
  <action>Add `import { CHART_TICK_STYLE, CHART_BORDER, CHART_TOOLTIP_STYLE, CHART_NEGATIVE } from "@/components/charts/chart-tokens"` to DrawdownChart.tsx. Replace each inline hex per the interfaces table: the two `<stop stopColor="#DC2626" …>` (:213-214) → `stopColor={CHART_NEGATIVE}` (keep the existing `stopOpacity` values verbatim); the bare-hex arm of `liveStroke` (:155) `"#DC2626"` → `CHART_NEGATIVE` (leave the `var(--color-chart-benchmark)` scenario arm unchanged); both axis `tick={{ fontSize: 11, fill: "#64748B", … }}` (:227, :234) → `tick={CHART_TICK_STYLE}` (this also bumps tick size to the canonical 12px and applies the mono font from the token — the intended factsheet alignment); `axisLine={{ stroke: "#E2E8F0" }}` (:229) → `axisLine={{ stroke: CHART_BORDER }}`; `contentStyle={{ fontSize: 12, borderColor: "#E2E8F0" }}` (:242) → `contentStyle={CHART_TOOLTIP_STYLE}`. Surgical only (Rule 3): touch ONLY the styling literals listed; do NOT change the data prop, the `hasScenario` logic, the gradient `id`, the `tickFormatter`s, or any layout. No new data path, no relocation.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - source: DrawdownChart.tsx imports from `@/components/charts/chart-tokens`; `grep -E '#DC2626|#64748B|#E2E8F0|fontSize:\s*11' DrawdownChart.tsx` (excluding comments) returns nothing — every styling literal is now a token reference.
    - behavior: axis ticks use `CHART_TICK_STYLE`, axis lines `CHART_BORDER`, tooltip `CHART_TOOLTIP_STYLE`, drawdown fill `CHART_NEGATIVE`; the `var(--color-chart-benchmark)` scenario stroke arm is unchanged.
    - test-command: the existing DrawdownChart render test (if present) still passes; no data-prop or layout change.
  </acceptance_criteria>
  <done>DrawdownChart reads axes/grid/tooltip/negative-fill from chart-tokens.ts; data path and benchmark arm untouched; no remaining inline styling hexes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Chart-stack token assertion test (DrawdownChart + EquityChart verification)</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx (if it exists — extend it; else create following the RollingVolatilityChart.test.tsx render pattern)
    - src/components/charts/RollingVolatilityChart.test.tsx (the recharts-mock render-test precedent for a token-consuming chart)
    - 30-VALIDATION.md "Per-Task Verification Map" row "equity/drawdown factsheet-stack alignment | GRAPH-01"
  </read_first>
  <behavior>
    - A source-read assertion proves DrawdownChart.tsx reads the chart-stack tokens: the source imports `CHART_TICK_STYLE`/`CHART_BORDER`/`CHART_TOOLTIP_STYLE`/`CHART_NEGATIVE` from chart-tokens and contains NO inline `#DC2626`/`#64748B`/`#E2E8F0` styling literal and no `fontSize: 11` (excluding comments). Non-vacuous: re-introducing an inline hex fails the assert.
    - An EquityChart verification assertion proves the equity chart is already chart-stack compliant: EquityChart.tsx source uses `var(--color-chart-strategy)` for its primary stroke (the CHART_ACCENT identity) — documents GRAPH-01's verification-only resolution for that file.
  </behavior>
  <action>Add an `it` "GRAPH-01 chart-stack tokens — drawdown reads chart-tokens, not inline hexes" that reads DrawdownChart.tsx via `node:fs` readFileSync, strips comment lines, and asserts: (a) `/from ["']@\/components\/charts\/chart-tokens["']/` matches; (b) `/#DC2626|#64748B|#E2E8F0/` does NOT match the non-comment source; (c) `/fontSize:\s*11/` does NOT match. Add an `it` "GRAPH-01 — equity chart already chart-stack compliant (verification-only)" that reads EquityChart.tsx via fs and asserts `/var\(--color-chart-strategy\)/` matches (the accent primary stroke) — pinning the verified resolution so a future regression that hardcodes the equity stroke is caught. Each assert is falsifiable (Rule 9): the drawdown assert fails if an inline hex returns; the equity assert fails if the CSS-var stroke is replaced with a literal. Use `node:fs` (no render needed for the token assertions). If a render test for DrawdownChart already exists, keep it; these are additive source-contract `it`s.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - source: the test reads DrawdownChart.tsx + EquityChart.tsx via `node:fs` (non-vacuous source assertions, not constants).
    - behavior: the drawdown token `it` passes only when chart-tokens are imported AND no inline `#DC2626`/`#64748B`/`#E2E8F0`/`fontSize: 11` remains in non-comment source; the equity `it` passes confirming `var(--color-chart-strategy)` is the primary stroke.
    - test-command: `npx vitest run "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx"` green.
  </acceptance_criteria>
  <done>Token-assertion test proves DrawdownChart reads chart-tokens (no inline hexes) and verifies EquityChart's CSS-var accent stroke; GRAPH-01 chart-stack alignment pinned non-vacuously.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chart styling source → rendered chart | The only boundary here is visual-identity drift: an inline hex that diverges from the chart-tokens contract degrades the factsheet identity silently. No data, no auth, no input crosses this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-08 | Information disclosure / trust | DrawdownChart styling | mitigate | Token-assertion test pins the chart-stack contract (no inline hex, chart-tokens imported) so a future palette drift on the drawdown chart fails CI rather than shipping an inconsistent factsheet identity. |
| T-30-09 | Tampering | GRAPH-01 data path | accept | GRAPH-01 is styling-only; the task touches no series math or data prop. Rationale: the existing `portfolio_daily_returns`-derived series is already shipped and SCENARIO-05-pinned; reskinning literals introduces no data-integrity surface. |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | Phase 30 installs ZERO packages — no install task, no legitimacy checkpoint needed. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.test.tsx"` green.
- `grep -vE '^\s*(//|\*)' src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx | grep -cE '#DC2626|#64748B|#E2E8F0|fontSize:\s*11'` returns 0 (filter comments first).
- `git diff --quiet HEAD -- src/lib/scenario.ts` exits 0 (GRAPH-01 touches no engine code).
- `npm test` full suite green at wave merge.
</verification>

<success_criteria>
- DrawdownChart reads axes/grid/tooltip/negative-fill from chart-tokens.ts; no inline styling hexes remain.
- EquityChart confirmed already chart-stack compliant (CSS-var accent stroke) — verification-only, pinned.
- GRAPH-01 is visual-identity only: no new data path, no relocation, engine untouched.
</success_criteria>

<output>
Create `.planning/phases/30-factsheet-graphs-on-the-blend/30-03-SUMMARY.md` when done.
</output>
