---
phase: 30-factsheet-graphs-on-the-blend
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/lib/scenario-blend-panels.ts
  - src/lib/scenario-blend-panels.test.ts
autonomous: true
requirements: [GRAPH-02, GRAPH-03, GRAPH-04]
must_haves:
  truths:
    - "Adapter derives a CUMULATIVE-wealth histogram series from raw daily returns (cumprod of 1+r), never raw daily values"
    - "Adapter rolling vol/Sharpe are numerically identical to portfolio-stats.ts::computeRollingMetric (sample-std n-1 × √252)"
    - "Adapter rolling Sortino divides downside RMS by the TOTAL window n × √252 (mirrors the frozen engine)"
    - "Degenerate windows (length<window, <10 usable points, any non-finite value) return [] / {} for every series"
    - "No √365 / *365 / √250 anywhere in the adapter — 252-day annualization only"
  artifacts:
    - path: "src/lib/scenario-blend-panels.ts"
      provides: "buildBlendPanels(portfolioDaily, window) → BlendPanelSeries pure-TS adapter"
      contains: "export function buildBlendPanels"
      min_lines: 60
    - path: "src/lib/scenario-blend-panels.test.ts"
      provides: "Convention-pin tests (parity, sortino÷n, sharpe, 252-only, degenerate-[], histogram-cumulative, quantiles-monotonic)"
      contains: "computeRollingMetric"
  key_links:
    - from: "src/lib/scenario-blend-panels.ts"
      to: "src/lib/portfolio-math-utils.ts"
      via: "import { mean, stdDev } — reuse the SAME sample-std so parity is exact"
      pattern: "from \"@/lib/portfolio-math-utils\""
    - from: "src/lib/scenario-blend-panels.test.ts"
      to: "src/lib/portfolio-stats.ts"
      via: "numeric-parity assert vs computeRollingMetric"
      pattern: "computeRollingMetric"
---

<objective>
Build the ONE genuinely-new file of Phase 30: the pure-TS adapter `src/lib/scenario-blend-panels.ts` that derives every blend-graph series (returns-distribution histogram + quantiles, rolling Sharpe/vol/Sortino) from the frozen engine's UNROUNDED `portfolio_daily_returns`. This is the contract Plan 30-02 consumes; it MUST land first.

Purpose: Single source of truth for the blend's derived series, with the convention pins (sample-std × √252, Sortino ÷ total-n, degenerate-[], histogram-cumulative) encoded as non-vacuous tests so the silent annualization/cumulative drift this milestone exists to prevent fails CI loudly.
Output: `scenario-blend-panels.ts` (the adapter) + `scenario-blend-panels.test.ts` (RED-first convention pins).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-CONTEXT.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-RESEARCH.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-PATTERNS.md
@.planning/phases/30-factsheet-graphs-on-the-blend/30-UI-SPEC.md

<interfaces>
<!-- Contracts the executor needs — extracted from the codebase this session. No exploration required. -->

From src/lib/portfolio-math-utils.ts (REUSE these — do NOT re-implement std/mean, parity depends on it):
```typescript
export interface DailyPoint { date: string; value: number }
export function mean(values: number[]): number;
// sample=true ⇒ divides by n-1 (Bessel). stdDev(slice, true) IS "sample-std". n<2 && sample → 0 (built-in guard).
export function stdDev(values: number[], sample = true): number;
```

From src/lib/portfolio-stats.ts (the math to MIRROR — the parity target):
```typescript
// Returns (n - window + 1) points dated at the window's last day; [] if daily.length < window.
// sharpe: s>0 ? (m * √252) / s : 0  ; volatility: s * √252  ; s = stdDev(slice, true) (SAMPLE).
export function computeRollingMetric(
  daily: DailyPoint[], window: number, metric: "sharpe" | "volatility",
): DailyPoint[];
```

Frozen-engine Sortino convention (src/lib/scenario.ts:354-361 — MIRROR, do NOT import; engine is frozen):
```
downsideSumSq = Σ (r<0 ? r*r : 0)   over the slice
downsideVar   = downsideSumSq / n   // ÷ TOTAL window n, NOT ÷ count-of-down-days
downsideVol   = √(downsideVar) × √252
sortino value = downsideVol > 0 ? (mean × 252) / downsideVol : 0   // per-window: 0 when no down days
```

ReturnHistogram CUMULATIVE-input contract (src/components/charts/ReturnHistogram.tsx:35-39 — why histogramSeries must be cumprod-wealth):
```typescript
// The leaf RE-derives daily returns from a CUMULATIVE series:
const cumulative = returns.map((r) => r.value);
const dailyReturns = cumulative.slice(1).map((v, i) => cumulative[i] !== 0 ? (v / cumulative[i]) - 1 : 0);
// Self-gates: returns.length < 10 → null ; max === min → null
```

ReturnQuantiles positional-array contract (src/components/charts/ReturnQuantiles.tsx:68):
```typescript
// data: Record<string, number[]>, each value destructured as [q0, q25, q50, q75, q100]
// Self-gate: Object.keys(data).length === 0 → null
```

RollingMetrics accent-key contract (src/components/charts/RollingMetrics.tsx:50-54):
```typescript
// Only key "sharpe_365d" → CHART_ACCENT. Key the single blend Sharpe series "sharpe_365d" for accent.
const STROKE_BY_KEY = { sharpe_30d: CHART_TEXT_MUTED, sharpe_90d: CHART_TEXT_SECONDARY, sharpe_365d: CHART_ACCENT };
```
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — convention-pin test scaffold (scenario-blend-panels.test.ts)</name>
  <files>src/lib/scenario-blend-panels.test.ts</files>
  <read_first>
    - src/lib/portfolio-stats.test.ts:1-71 (deterministic sinusoidal fixture + toBeCloseTo(expected,8) precedent — the exact analog)
    - src/lib/portfolio-stats.ts:104-135 (computeRollingMetric — the parity target)
    - src/lib/scenario.ts:340-361 (frozen Sortino/vol/Sharpe convention to mirror)
    - src/components/charts/ReturnHistogram.tsx:32-50 (cumulative→daily re-derivation; self-gate <10 / max===min)
    - 30-PATTERNS.md "scenario-blend-panels.test.ts (NEW — test, transform)" section (the seven `it` cases to encode)
  </read_first>
  <behavior>
    - Test (sample-std parity / vol): buildBlendPanels(DAILY, 63).rollingVol equals computeRollingMetric(DAILY, 63, "volatility") point-for-point via toBeCloseTo(_, 8). PROVES "mirror portfolio-stats.ts, NOT factsheet/rolling.ts".
    - Test (sharpe convention): rolling Sharpe series parity vs computeRollingMetric(DAILY, 63, "sharpe") (= mean × √252 ÷ sample-std).
    - Test (sortino ÷ total n): on a tiny hand-checkable fixed window, assert downside RMS divides by the FULL window length (n), not the down-day count; numerator = mean × 252. Anchor against a by-hand value.
    - Test (252-only): a constant-return fixture → rollingVol matches stdDev(slice,true) × √252 exactly; AND a source-read assertion that the adapter module text contains no "365" / "250" literal and no "√365"/"*365"/"√250" pattern (read the .ts file via fs, grep-style — non-vacuous: assert the live module body, not a constant).
    - Test (histogram cumulative): histogramSeries from DAILY starts near 1.0 and is cumprod-monotone in trend; piping histogramSeries through the leaf's `v/cumulative[i]-1` recovers the ORIGINAL daily returns (toBeCloseTo), NOT wealth-ratio garbage. PROVES Pitfall 1 fixed.
    - Test (quantiles monotonic): each quantiles record value is non-decreasing [q0 ≤ q25 ≤ q50 ≤ q75 ≤ q100].
    - Test (degenerate → []/{}): positive+negative control — length<window, <10 points, and a NaN/Infinity-injected series EACH return empty (rollingVol/Sharpe/Sortino [], quantiles {}, histogramSeries []); a healthy 252-pt series returns NON-empty (proves non-vacuity).
  </behavior>
  <action>Create `scenario-blend-panels.test.ts` importing `buildBlendPanels` from `./scenario-blend-panels` (does not exist yet — RED), `computeRollingMetric` from `./portfolio-stats`, type `DailyPoint` from `./portfolio-math-utils`, and `{ describe, it, expect }` from vitest. Build a deterministic 252-pt sinusoidal `DAILY` fixture (no Math.random — mirror portfolio-stats.test.ts:23-26). Encode the seven `it` cases in the behavior block, one per case. For the 252-only source-read assertion use `node:fs` readFileSync on the adapter path and assert `/√365|\*\s*365|√250|Math\.sqrt\(\s*365\s*\)|Math\.sqrt\(\s*250\s*\)/` does NOT match the source. Use `toBeCloseTo(_, 8)` for float asserts (Rule 9: each assert must fail when the convention is wrong — the parity asserts fail if population-std is used, the histogram assert fails if raw-daily is passed). Run the suite to confirm it FAILS RED (module-not-found / undefined).</action>
  <verify>
    <automated>npx vitest run src/lib/scenario-blend-panels.test.ts 2>&1 | grep -qiE "cannot find module|buildBlendPanels|failed" && echo "RED OK"</automated>
  </verify>
  <acceptance_criteria>
    - source: `scenario-blend-panels.test.ts` exists, imports `buildBlendPanels` + `computeRollingMetric` + `DailyPoint`, and has exactly the seven `it` cases named in the behavior block.
    - behavior: `npx vitest run src/lib/scenario-blend-panels.test.ts` fails (RED) because the adapter module does not yet exist.
    - test-command: the 252-only test reads the adapter SOURCE via fs (non-vacuous source assertion), not a re-exported constant.
  </acceptance_criteria>
  <done>RED test file exists with 7 convention-pin cases; suite fails because the adapter is not yet implemented.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — implement buildBlendPanels (scenario-blend-panels.ts)</name>
  <files>src/lib/scenario-blend-panels.ts</files>
  <read_first>
    - src/lib/portfolio-stats.ts:117-135 (computeRollingMetric loop — copy the EXACT arithmetic: slice→mean→stdDev(slice,true)→×√252)
    - src/lib/portfolio-math-utils.ts:11-14,64-81 (DailyPoint, mean, stdDev sample=true)
    - src/lib/scenario.ts:354-361 (Sortino downsideSumSq/n × √252, numerator mean×252)
    - 30-PATTERNS.md "scenario-blend-panels.ts (NEW — utility, transform)" section (all five code blocks: rolling, sortino, histogram cumprod, quantiles, degenerate guard, signature)
    - 30-UI-SPEC.md "Adapter contract" section (LOCKED convention pins, bins=20, quantile 5-number shape, window default 63)
  </read_first>
  <action>Implement `buildBlendPanels(portfolioDaily: {date:string;value:number}[], window: number): BlendPanelSeries` per the LOCKED contract. Import `{ mean, stdDev }` and type `DailyPoint` from `@/lib/portfolio-math-utils` (REUSE — do NOT re-implement std, parity depends on identical arithmetic). Export the `BlendPanelSeries` interface: `histogramSeries: {date,value}[]`, `quantiles: Record<string,number[]>`, `rollingSharpe: Record<string,{date,value}[]>` (key the single series `sharpe_365d` so RollingMetrics resolves CHART_ACCENT — per A3, zero-touch on the leaf), `rollingVol: {date,value}[]`, `rollingSortino: {date,value}[]`, `usableN: number`. Degenerate guard FIRST (LOCKED pin): define `MIN_USABLE = 10`; if `portfolioDaily.length < 10` OR any `!Number.isFinite(p.value)` present, return all-empty (`[]`/`{}`) with `usableN`=count of finite points. rollingVol/rollingSharpe: mirror computeRollingMetric EXACTLY (`if (daily.length < window) return []`; loop `i=window-1..`, `slice.map(d=>d.value)`, `m=mean(slice)`, `s=stdDev(slice,true)`, vol=`s*√252`, sharpe=`s>0?(m*√252)/s:0`, push `{date: daily[i].date, value}`). rollingSortino: same loop shape but `downSq=Σ(x<0?x*x:0)`, `dd=√(downSq/window)*√252` (÷ window = total n, mirroring the engine), `value = dd>0 ? (m*252)/dd : 0`. histogramSeries: cumprod off the UNROUNDED input — `let c=1; map(p => { c *= 1 + p.value; return {date:p.date, value:c}; })` (wealth form ~1.0 — NEVER raw daily, NEVER the rounded equity_curve). quantiles: single `"All"` key (the minimal honest read per A2) = `[sorted[0], Q(.25), Q(.5), Q(.75), sorted.at(-1)!]` via linear-interp percentile; `{}` if degenerate. NO `√365`/`*365`/`√250` anywhere — 252 only. Pure TS: no fetch, no DOM, no Date.now, no imports beyond portfolio-math-utils + types. Run the test to confirm GREEN.</action>
  <verify>
    <automated>npx vitest run src/lib/scenario-blend-panels.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - source: `scenario-blend-panels.ts` exports `buildBlendPanels` + `BlendPanelSeries`; imports `mean`/`stdDev` from `@/lib/portfolio-math-utils` (NOT a hand-rolled std); contains no `365`/`250` literal and no `Math.sqrt(365)`/`Math.sqrt(250)`.
    - behavior: `npx vitest run src/lib/scenario-blend-panels.test.ts` passes all 7 cases (GREEN) — parity vs computeRollingMetric holds; histogram round-trips to original daily returns; degenerate → []/{}.
    - test-command: rollingSharpe is keyed `sharpe_365d`; rollingSortino divides by `window` (total n); histogramSeries is cumprod-of-(1+r), value[0] ≈ 1 + portfolioDaily[0].value.
  </acceptance_criteria>
  <done>buildBlendPanels implemented; all 7 convention pins GREEN; adapter is pure TS reusing portfolio-math-utils' sample-std; no 365/250 annualization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frozen engine → adapter | The adapter consumes already-authorized, already-computed `portfolio_daily_returns`. No new untrusted input crosses here; the risk is data-quality (NaN/Infinity) and convention drift, not external attack. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information disclosure / trust | `buildBlendPanels` annualization | mitigate | Convention-pin tests assert √252 only + numeric parity vs `computeRollingMetric`; a √365/√250 drift (a false risk display on the blend) fails the "252-only" + parity tests. |
| T-30-02 | Tampering (data quality) | `buildBlendPanels` non-finite handling | mitigate | LOCKED degenerate guard returns `[]`/`{}` on any non-finite value or <10 points; the NaN/Infinity-injected negative-control test proves it (no NaN poisons a chart into garbage). |
| T-30-03 | Information disclosure / trust | histogram cumulative-input | mitigate | The histogram round-trip test proves the cumprod-wealth series recovers the original daily distribution — a raw-daily mis-feed (meaningless ratios) fails the test. |
| T-30-SC | Tampering | npm/pip/cargo installs | n/a | Phase 30 installs ZERO packages (RESEARCH §Package Legitimacy Audit) — no install task, no `[ASSUMED]`/`[SUS]` package, no `T-30-SC` checkpoint needed. |
</threat_model>

<verification>
- `npx vitest run src/lib/scenario-blend-panels.test.ts` green (all 7 convention pins).
- Adapter is pure TS: `grep -L "fetch\|Date.now\|document\|window\." src/lib/scenario-blend-panels.ts` confirms no DOM/time/network.
- No annualization drift: `grep -vE '^\s*(//|\*)' src/lib/scenario-blend-panels.ts | grep -cE '365|250'` returns 0 (filter comments first per grep-gate hygiene).
</verification>

<success_criteria>
- buildBlendPanels derives histogram (cumulative), quantiles, rolling Sharpe/vol/Sortino, usableN from `portfolio_daily_returns`.
- Rolling vol/Sharpe are point-for-point identical to `computeRollingMetric` (sample-std × √252) — proven by parity test.
- Rolling Sortino divides downside RMS by total window n × √252 (engine mirror).
- Degenerate input → []/{} for every series.
- Zero new dependencies; engine untouched.
</success_criteria>

<output>
Create `.planning/phases/30-factsheet-graphs-on-the-blend/30-01-SUMMARY.md` when done.
</output>
