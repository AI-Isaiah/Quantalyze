---
phase: 33-journey-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [JOURNEY-02]
must_haves:
  truths:
    - "All 4 inventoried entry/empty-state surfaces are confirmed DESIGN.md-compliant (verified, not assumed)"
    - "The composer blank-slate CTAs carry a visible accent focus ring, matching the rest of the surface (the one residual drift)"
    - "No new entry-point surface is introduced; the 3 already-compliant surfaces are byte-unchanged"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Blank-slate front door with consistent focus-visible accent ring on both CTAs"
      contains: "focus-visible:ring-accent/50"
  key_links:
    - from: "blank-slate Connect Exchange CTA (:1604)"
      to: "DESIGN.md focus-ring token"
      via: "focus-visible:ring-2 focus-visible:ring-accent/50"
      pattern: "focus-visible:ring-accent"
    - from: "blank-slate Browse strategies CTA (:1611)"
      to: "DESIGN.md focus-ring token"
      via: "focus-visible:ring-2 focus-visible:ring-accent/50"
      pattern: "focus-visible:ring-accent"
---

<objective>
JOURNEY-02: confirm the inventoried path to the composer is visually consistent
with DESIGN.md, and close the ONE genuine residual drift the pattern map
identified. This is a flag-and-fix-genuine-drift sweep over EXACTLY 4 surfaces —
NOT a redesign, NO new component family, NO new surface (sprawl guard).

The pattern map + UI-SPEC verified 3 of the 4 surfaces are already compliant
(allocations EmptyState, OnboardingBanner, discovery/[slug]) and the composer
blank-slate front door already ships (Phase 29). The single concrete drift:
the blank-slate CTAs at ScenarioComposer.tsx:1604/1611 use `hover:` only, while
the rest of the composer (entry-mode segments :1684/1700) and OnboardingBanner
(:65) carry `focus-visible:ring-2 focus-visible:ring-accent/50`. Add the
existing focus-ring class to the two blank-slate CTAs — a token-exact,
DESIGN.md-blessed consistency fix using an EXISTING class, not a new style.

Purpose: One visual language into the composer; keyboard users get a visible
accent focus ring on the blank-slate front door, matching every other CTA.
Output: A minimal, surgical edit to two `className` strings in ScenarioComposer.tsx;
all other surfaces verified byte-unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-journey-polish/33-CONTEXT.md
@.planning/phases/33-journey-polish/33-PATTERNS.md
@.planning/phases/33-journey-polish/33-UI-SPEC.md
@DESIGN.md

<interfaces>
<!-- The exact surfaces + the residual drift. Extracted from the codebase. -->

The 4 inventoried surfaces (DESIGN.md consistency target = Card 8px/white/1px #E2E8F0,
KpiStrip Geist-Mono tabular-nums, Instrument-Serif headings / DM-Sans body, accent
reserved for action/verified, focus ring = `focus-visible:ring-2 focus-visible:ring-accent/50`):
  1. src/app/(dashboard)/allocations/EmptyState.tsx:34-59 — COMPLIANT, verify only
  2. src/app/(dashboard)/allocations/components/OnboardingBanner.tsx:28-80 — COMPLIANT, verify only (carries the focus ring at :65 — the analog for the fix)
  3. src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1583-1632 — blank-slate front door, the ONE likely touch
  4. src/app/(dashboard)/discovery/[slug]/page.tsx:41-77 — COMPLIANT, verify only

The residual drift (confirmed live) — ScenarioComposer.tsx blank-slate CTAs:
```
:1604  className="inline-flex items-center rounded-lg bg-accent px-4 py-2 text-sm font-medium text-white hover:bg-accent/90"   ← "Connect Exchange →" Link — NO focus ring
:1611  className="inline-flex items-center rounded-lg border border-border px-4 py-2 text-sm font-medium text-text-primary hover:border-accent"  ← "Browse strategies" button — NO focus ring
```
The same surface already has the ring on the entry-mode segments:
```
:1684 / :1700  ...focus:outline-none focus-visible:ring-2 focus-visible:ring-accent/50...
```
DESIGN.md focus-ring recipe (UI-SPEC §Color reserved-for #4): `focus-visible:ring-2 focus-visible:ring-accent/50`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the DESIGN.md focus ring to the two blank-slate CTAs; verify the other 3 surfaces are byte-unchanged</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1583-1632 (the blank-slate `isEmptyState` branch — the only edit target)
    - src/app/(dashboard)/allocations/components/OnboardingBanner.tsx:60-70 (the focus-ring analog at :65 to copy verbatim)
    - DESIGN.md (focus-ring + accent-reserved rules — read before any visual change, per CLAUDE.md)
    - .planning/phases/33-journey-polish/33-PATTERNS.md (JOURNEY-02 table + "DESIGN.md Card primitive shell" shared pattern)
    - .planning/phases/33-journey-polish/33-UI-SPEC.md (JOURNEY-02 contract — the verify-only verdicts on surfaces 1/2/4)
  </read_first>
  <action>
    In src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, append
    `focus:outline-none focus-visible:ring-2 focus-visible:ring-accent/50` to the
    `className` of BOTH blank-slate CTAs: the "Connect Exchange →" Link at ~:1604
    and the "Browse strategies" button at ~:1611. Match the exact class string
    already used on the entry-mode segments at :1684/:1700 and on
    OnboardingBanner.tsx:65 — reuse the existing token, do NOT invent a new ring
    width/color/opacity. Do NOT change the CTA copy ("Connect Exchange →",
    "Browse strategies" are LOCKED), the `bg-accent`/`border-border` recipe, the
    `hover:` states, the `rounded-lg` radius, the serif "Start a portfolio"
    heading, or the card shell `rounded-lg border border-border bg-surface p-12 text-center`.
    Touch ONLY the two `className` strings. Do NOT add a new component, a new
    surface, an illustration, or a checklist (minimalism gate + sprawl guard).
    Do NOT edit EmptyState.tsx, OnboardingBanner.tsx, or discovery/[slug]/page.tsx
    — they are verify-only (Rule 3 — surgical; do not "improve" compliant surfaces).
    Record the verify-only verdict for surfaces 1/2/4 in the SUMMARY (each
    confirmed DESIGN.md-compliant; no edit). Do NOT edit src/lib/scenario.ts
    (frozen, SCENARIO-05) — this plan does not touch it.
  </action>
  <verify>
    <automated>npx eslint "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx" && grep -c "focus-visible:ring-accent/50" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "focus-visible:ring-accent/50" ScenarioComposer.tsx` increases by exactly 2 vs the pre-edit count (the two blank-slate CTAs now carry the ring; the entry-mode segment rings are unchanged).
    - The "Connect Exchange →" Link and the "Browse strategies" button each include `focus-visible:ring-2 focus-visible:ring-accent/50` with `focus:outline-none`.
    - `git diff src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` shows ONLY the two `className` strings changed — no copy, no heading, no card-shell, no new element, no other line touched.
    - `git diff --exit-code src/app/(dashboard)/allocations/EmptyState.tsx src/app/(dashboard)/allocations/components/OnboardingBanner.tsx "src/app/(dashboard)/discovery/[slug]/page.tsx"` is clean (verify-only surfaces unchanged).
    - `git diff --exit-code src/lib/scenario.ts src/lib/scenario.test.ts` is clean (SCENARIO-05).
    - eslint passes on the edited file (no token typo, valid Tailwind v4 class).
    - The full existing ScenarioComposer test suite stays green (`npx vitest run src/app/(dashboard)/allocations/components/__tests__/`) — the edit must not break a snapshot or a locked-copy assertion.
  </acceptance_criteria>
  <done>
    Both blank-slate CTAs carry the DESIGN.md accent focus ring; the 3 other
    inventoried surfaces are verified compliant and byte-unchanged; no copy,
    no new surface, no scenario.ts diff; existing tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan changes two `className` strings on an existing client component. It
introduces NO new endpoint, NO schema/RLS change, NO new dependency, NO auth
change, NO new copy. The honest threat model is the carry-through guard.

| Boundary | Description |
|----------|-------------|
| polish edit ↔ honesty invariants | A visual sweep must not strip a disclosure or recolor a muted honesty pill to accent (IMPACT-02 / PROJECTED-pill LOCKED) |
| polish edit ↔ frozen engine | `ScenarioComposer.tsx` consumes `scenario.ts`; the edit must not touch the engine (SCENARIO-05 zero-diff) |
| polish edit ↔ locked copy | Locked CTA/heading copy must stay byte-identical (existing tests pin some of it) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-03 | Tampering | honesty invariants (PROJECTED pill, IMPACT-02 percentile-rank-badge) | mitigate | Edit is scoped to two CTA `className` strings; acceptance criterion `git diff` proves no other line changed; the phase-29/30 frozen-spine + IMPACT-02 guard tests stay green |
| T-33-04 | Tampering | frozen engine (scenario.ts) | mitigate | `git diff --exit-code src/lib/scenario.ts` clean (SCENARIO-05) |
| T-33-05 | Information disclosure | accent semantic misuse | accept | Accent is added ONLY as a focus ring (UI-SPEC §Color reserved-for #4 explicitly blesses accent for focus rings); no accent fill added to a non-action element |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependency installed this plan; no package-manager install task; nothing to vet |

No new attack surface: no endpoint, no schema/RLS, no auth, no copy change.
</threat_model>

<verification>
- `npx eslint "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` → clean.
- `grep -c "focus-visible:ring-accent/50" ScenarioComposer.tsx` → +2 vs baseline.
- `git diff` on ScenarioComposer.tsx shows only the two className strings changed.
- The 3 verify-only surfaces + scenario.ts are byte-unchanged (`git diff --exit-code`).
- `npx vitest run src/app/(dashboard)/allocations/components/__tests__/` → green.
</verification>

<success_criteria>
- JOURNEY-02 success criterion 2 ("the path to the composer is visually
  consistent with DESIGN.md — one chart stack, one metric strip") is satisfied:
  all 4 surfaces verified compliant, the one residual focus-ring drift closed
  with the existing DESIGN.md token.
- No sprawl: no new surface, no new component, locked copy byte-identical, the
  3 compliant surfaces untouched.
</success_criteria>

<output>
Create `.planning/phases/33-journey-polish/33-02-SUMMARY.md` when done.
Record the verify-only verdict for surfaces 1/2/4 (each confirmed compliant, no edit).
</output>
