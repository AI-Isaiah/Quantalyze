---
phase: 33-journey-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-state.test.ts
  - src/app/(dashboard)/allocations/components/__tests__/bridge-to-composer-seam.test.tsx
autonomous: true
requirements: [JOURNEY-01]
must_haves:
  truths:
    - "An allocator carrying a Bridge candidate into the composer sees the projection MOVE (not just a membership flag flip)"
    - "The Bridge-add seam is pinned by a test that FAILS when the seam is neutered (vacuous-test guard)"
    - "Only the composer-owned BridgeDrawer can seed the draft (BridgeWidget drawers are structurally inert) — the FLOW-01 reachability hinge is pinned"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/__tests__/bridge-to-composer-seam.test.tsx"
      provides: "Integrated CTA-click → mutator → projection-delta regression test + reachability guard"
      contains: "computeMetricsForDraft"
    - path: "src/app/(dashboard)/allocations/lib/scenario-state.test.ts"
      provides: "Existing addStrategyBridge weight-transform pin (extended/unchanged)"
      contains: "addStrategyBridge"
  key_links:
    - from: "BridgeDrawer onAddToScenario CTA"
      to: "scenario-state.ts addStrategyBridge"
      via: "ScenarioComposer.tsx:2334 handler"
      pattern: "addStrategyBridge"
    - from: "addStrategyBridge mutated draft"
      to: "computeMetricsForDraft projection"
      via: "weightOverrides delta"
      pattern: "computeMetricsForDraft"
---

<objective>
Pin JOURNEY-01 (Bridge → composer continuity) with a NON-VACUOUS regression test
that fails if the seam is neutered. The seam already ships and is structurally
wired (BridgeDrawer.onAddToScenario → scenario-state.addStrategyBridge →
ScenarioComposer handler at :2334). What is MISSING (per the pattern-map gap
analysis): no existing test drives the integrated path CTA → mutator →
PROJECTION MOVES. The existing T_USE6 hook test asserts membership only
(`addedStrategies` contains the id), which is vacuous w.r.t. the projection.

Purpose: Prove the user-reachable carry-through actually delivers a candidate
into the draft AND changes a projection-bearing value, and pin the FLOW-01
reachability hinge (only the composer-owned drawer carries `onAddToScenario`).
This is the shipped-but-dead guard the FLOW-01 lesson demands.

Output: One new test file pinning the integrated seam (projection-delta +
reachability), with the existing `scenario-state.test.ts` weight-transform pin
left intact. ZERO production-code change. `src/lib/scenario.ts` stays zero-diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-journey-polish/33-CONTEXT.md
@.planning/phases/33-journey-polish/33-PATTERNS.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase — no exploration required. -->

From src/app/(dashboard)/allocations/lib/scenario-state.ts (the DRAFT module — NOT the frozen engine):
```typescript
export function addStrategyBridge(
  draft: ScenarioDraft,
  holdingScopeRef: string,
  strategy: AddedStrategy,
): ScenarioDraft;
// M9 dedupe: second add of the same id returns the SAME draft reference (no-op).
// On a fresh add: candidate takes the swapped holding's current weight; the
// holding stays enabled; the full enabled set renormalizes so sum ≈ 1.0.
```

From src/app/(dashboard)/allocations/lib/scenario-compare.ts:89 (the pure projection path — runs through the SAME frozen engine, never edits it):
```typescript
export function computeMetricsForDraft(draft: ScenarioDraft, liveInputs: ...): ScenarioMetrics | null;
// An empty enabled set yields null. A non-empty draft yields the six projection
// metrics. This is the projection-bearing value the JOURNEY-01 test asserts MOVES.
```

Existing non-vacuous weight-transform pin to MIRROR (style + tolerance) — src/app/(dashboard)/allocations/lib/scenario-state.test.ts:242-254 (`T1.5`):
```typescript
expect(next.weightOverrides["holding:binance:BTC:spot"]).toBeCloseTo(0.6 / 1.6, 9);
expect(next.weightOverrides["uuid-2"]).toBeCloseTo(0.6 / 1.6, 9);
expect(sumEnabled(next)).toBeCloseTo(1.0, 9);
```

Reachability hinge (verify, do not edit) — ScenarioComposer.tsx:2334 is the ONLY
BridgeDrawer mount passing `onAddToScenario`; BridgeWidget.tsx:278/317/377 pass
it 0 times (`grep -n "onAddToScenario" BridgeWidget.tsx` → 0 hits).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Non-vacuous Bridge-add projection-delta + reachability regression test</name>
  <files>src/app/(dashboard)/allocations/components/__tests__/bridge-to-composer-seam.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-state.ts (the addStrategyBridge mutator under test)
    - src/app/(dashboard)/allocations/lib/scenario-compare.ts (computeMetricsForDraft — the projection path)
    - src/app/(dashboard)/allocations/lib/scenario-state.test.ts:241-278 (T1.5 / dedupe — the analog to MIRROR for exact-weight assertions)
    - src/app/(dashboard)/allocations/hooks/useScenarioState.test.tsx:217-236 (T_USE6 — the membership-only test whose projection gap this closes)
    - .planning/phases/33-journey-polish/33-PATTERNS.md (JOURNEY-01 gap analysis + "Pure-mutator unit test with exact-value assertions" shared pattern)
  </read_first>
  <behavior>
    - Test (PROJECTION MOVES): build a 2-holding draft via the existing `defaultDraftFromHoldings(HOLDINGS_2)` fixture, snapshot the baseline projection via `computeMetricsForDraft(draft, liveInputs)`, apply `addStrategyBridge(draft, "holding:binance:BTC:spot", STRAT_B)`, recompute, and assert (a) the candidate id is present in `addedStrategies`, (b) the candidate's `weightOverrides` entry equals `0.6 / 1.6` to 9 places (mirroring scenario-state.test.ts:250-253), (c) the flagged holding is diluted (`weightOverrides["holding:binance:BTC:spot"]` strictly less than its pre-add value), and (d) at least one projection-bearing value from `computeMetricsForDraft` DIFFERS from the baseline (a metric field changes, or the enabled-weight vector feeding the projection changes). Assertion (d) is the load-bearing non-vacuous gate — a membership-only assertion is vacuous and does NOT satisfy JOURNEY-01.
    - Test (FAILS WHEN NEUTERED — falsifiability proof, documented in a comment): the test must be written so that neutering `addStrategyBridge` to a pass-through (`return draft`) makes assertions (a)/(b)/(d) FAIL. Note this explicitly in a code comment so a future reader can verify falsifiability without re-deriving it.
    - Test (REACHABILITY HINGE): assert the composer-owned drawer is the only seeding path by reading the two source files as text — `ScenarioComposer.tsx` contains exactly one `onAddToScenario={` on a `<BridgeDrawer` mount, and `BridgeWidget.tsx` contains zero `onAddToScenario` occurrences. Use a `fs.readFileSync` + `grep`-equivalent count in the test (mirror the source-inspecting idiom used by the phase-3x frozen-spine guard tests in src/__tests__/). This pins the FLOW-01 shipped-but-dead lesson: if a future edit wires `onAddToScenario` into a BridgeWidget drawer OR drops it from the composer, the count assertion fails loudly.
  </behavior>
  <action>
    Create src/app/(dashboard)/allocations/components/__tests__/bridge-to-composer-seam.test.tsx as a Vitest spec (the project uses Vitest — match the import idiom of scenario-state.test.ts). Reuse the existing fixtures from scenario-state.test.ts (`HOLDINGS_2`, `STRAT_B`, `defaultDraftFromHoldings`, `sumEnabled`) by importing them from their source module, or redefine minimal equivalents locally if they are not exported — do NOT export-widen the production module just for the test. Drive the projection via `computeMetricsForDraft` from `@/app/(dashboard)/allocations/lib/scenario-compare` (the same engine path the composer's `scenarioMetrics` useMemo uses), constructing whatever minimal `liveInputs` shape it requires from the holdings fixture. Mirror the exact-weight `toBeCloseTo(..., 9)` style and the `sumEnabled ≈ 1.0` assertion from scenario-state.test.ts:250-253. For the reachability hinge, read both `ScenarioComposer.tsx` and `BridgeWidget.tsx` via `fs.readFileSync` and assert occurrence counts of the literal `onAddToScenario` (exactly 1 vs 0). Use `grep -v '^#'`-style comment-filtering only if a token appears in a comment — these are TS/TSX source files so filter JSDoc/`//` lines if a false positive arises. Do NOT edit scenario-state.ts, scenario-compare.ts, ScenarioComposer.tsx, or scenario.ts. Do NOT rename the "Add to scenario" CTA (code path is named `scenario` — LOCKED). Add a regression test per CLAUDE.md Rule 9 (test encodes WHY: the seam must carry a candidate into the projection, not just a flag).
  </action>
  <verify>
    <automated>npx vitest run src/app/(dashboard)/allocations/components/__tests__/bridge-to-composer-seam.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - bridge-to-composer-seam.test.tsx exits 0 (all assertions pass) against the live, un-neutered seam.
    - The test FAILS when `addStrategyBridge` is locally stubbed to `return draft` (falsifiability — assertions a/b/d break). The executor must demonstrate this once (stub → run → see red → revert) and record the red output in the SUMMARY.
    - The reachability assertion FAILS if `onAddToScenario` is removed from ScenarioComposer.tsx or added to a BridgeWidget BridgeDrawer mount (count 1↔0 inversion).
    - At least one `computeMetricsForDraft` projection field differs pre- vs post-add (non-vacuous projection delta) — NOT only `addedStrategies` membership.
    - `git diff --exit-code src/lib/scenario.ts src/lib/scenario.test.ts` is clean (frozen-spine zero-diff, SCENARIO-05).
    - No production source file is modified (only the new test file + optional untouched scenario-state.test.ts).
  </acceptance_criteria>
  <done>
    A new regression test pins the integrated Bridge-add path with a projection
    delta and a reachability count guard; it passes live and fails when neutered;
    scenario.ts is zero-diff; no production code changed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds ONE test file. It introduces NO new endpoint, NO schema/RLS
change, NO new dependency, NO auth surface. The only "boundary" relevant here is
the carry-through guard: the regression-test addition must NOT weaken the
existing honesty / frozen-engine invariants.

| Boundary | Description |
|----------|-------------|
| frozen engine ↔ test | The test reads through `computeMetricsForDraft` (engine path) but must not edit `scenario.ts` (SCENARIO-05 zero-diff guard) |
| draft mutator ↔ projection | `addStrategyBridge` mutates the in-memory draft only; no persistence/network crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | frozen engine (scenario.ts) | mitigate | Acceptance criterion asserts `git diff --exit-code src/lib/scenario.ts` is clean; the phase-29/30 frozen-spine guard tests remain green (no edit here) |
| T-33-02 | Repudiation (false-green) | the regression test itself | mitigate | Falsifiability is a hard acceptance criterion — the test MUST fail when the seam is neutered; a vacuous membership-only assertion is rejected |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependency installed this plan (no package-manager install task); no `[ASSUMED]`/`[SUS]` package to vet |

No new attack surface: no new endpoint, no schema/RLS change, no auth change.
The honest threat model for this plan is the carry-through guard above.
</threat_model>

<verification>
- `npx vitest run src/app/(dashboard)/allocations/components/__tests__/bridge-to-composer-seam.test.tsx` → green.
- Falsifiability demonstrated once (stub `addStrategyBridge`, observe red, revert).
- `git diff --exit-code src/lib/scenario.ts src/lib/scenario.test.ts` → clean.
- `git status --porcelain src/` shows only the new test file added (no production edits).
</verification>

<success_criteria>
- JOURNEY-01 success criterion 1 ("an allocator can carry a Bridge recommendation
  into the composer as part of a portfolio") is pinned by a test that proves the
  candidate lands in the draft AND moves the projection, and fails if neutered.
- The FLOW-01 reachability hinge is pinned (composer-owned drawer is the only
  seeding path).
- Zero production-code change; frozen engine zero-diff.
</success_criteria>

<output>
Create `.planning/phases/33-journey-polish/33-01-SUMMARY.md` when done.
Record the falsifiability red-run output (the stubbed-seam failure) in the SUMMARY.
</output>
