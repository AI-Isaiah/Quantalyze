---
phase: 33-journey-polish
plan: 03
type: execute
wave: 2
depends_on: ["33-02"]
files_modified:
  - e2e/composer-axe.spec.ts
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [JOURNEY-03]
must_haves:
  truths:
    - "An authed axe-core scan of /allocations?tab=scenario INCLUDING the Phase-30 graph cards reports zero WCAG-AA violations"
    - "The spec cannot false-green on an empty <main> / login chrome (sanity gate + graph-card visibility gate)"
    - "Any real violation surfaced is fixed at ROOT (semantic HTML/labels/contrast) — never by suppressing a rule"
  artifacts:
    - path: "e2e/composer-axe.spec.ts"
      provides: "Authed composer WCAG-AA axe verification spec"
      contains: "buildAxe"
  key_links:
    - from: "e2e/composer-axe.spec.ts"
      to: "e2e/helpers/axe.ts buildAxe"
      via: "import + analyze()"
      pattern: "buildAxe\\(page\\)"
    - from: "e2e/composer-axe.spec.ts"
      to: "ScenarioComposer Phase-30 cards"
      via: "data-panel selectors"
      pattern: "data-panel=.blend-(returns-distribution|rolling)"
---

<objective>
JOURNEY-03: prove the unified composer + the Phase-30 graphs pass automated
accessibility checks at WCAG-AA. Add ONE authed axe-core e2e spec targeting
/allocations?tab=scenario that waits for the composer + the Phase-30 graph cards
to render, then asserts zero violations. Reuse the EXISTING harness verbatim
(`e2e/helpers/axe.ts` `buildAxe()`, already `wcag2a+wcag2aa+best-practice`) and
the EXISTING authed-seed + form-login pattern from `discovery-axe.spec.ts` — NO
new dependency, NO jest-axe, NO second harness.

Purpose: A regression gate that fails loudly if the composer (or a Phase-30
card it now hosts) regresses below WCAG-AA. The standalone charts already have
coverage in `strategy-v2-axe.spec.ts`; this EXTENDS the composer surface, it
does not duplicate the chart specs.

Output: e2e/composer-axe.spec.ts. If the scan surfaces a REAL violation, fix it
at ROOT in ScenarioComposer.tsx (semantic HTML / label / contrast) — never via
`.disableRules` / `.exclude`. `src/lib/scenario.ts` stays zero-diff.

Depends on Plan 33-02: this spec scans `ScenarioComposer.tsx` after the
JOURNEY-02 focus-ring fix lands (shared file ownership → Wave 2).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-journey-polish/33-CONTEXT.md
@.planning/phases/33-journey-polish/33-PATTERNS.md
@.planning/phases/33-journey-polish/33-UI-SPEC.md
@e2e/discovery-axe.spec.ts
@e2e/helpers/axe.ts

<interfaces>
<!-- Harness + selectors the executor needs. Extracted from the codebase — reuse verbatim. -->

From e2e/helpers/axe.ts (reuse — do NOT add a second harness, no jest-axe):
```typescript
export function buildAxe(page: Page) {
  return new AxeBuilder({ page }).withTags(["wcag2a", "wcag2aa", "best-practice"]);
}
```

From e2e/helpers/seed-test-project.ts:75 (stamps a VERIFIED allocator profile +
investor_attestations row — exactly the session the composer route needs):
```typescript
export async function seedTestAllocator(): Promise<{ userId: string; email: string; password: string }>;
```

Authed-seed + form-login + false-green gate idiom to COPY — e2e/discovery-axe.spec.ts:24-80:
```typescript
const HAS_SEED_ENV = !!process.env.TEST_SUPABASE_URL && !!process.env.TEST_SUPABASE_SERVICE_ROLE_KEY;
// async loginViaForm(page, email, password): goto /login → fill email + password →
//   click "Sign in" → waitForURL(/\/(discovery|strategies|allocations|dashboard)/)
test.skip(!HAS_SEED_ENV, "<skip string — keeps the spec authored-but-not-CI-blocking until seed env wired; prevents false-green>");
// sanity gate: a heading must be visible before scanning (W-02 false-green guard)
const results = await buildAxe(page).analyze();
expect(results.violations).toEqual([]);
```

Stable composer selectors (verified live in ScenarioComposer.tsx):
- Returns-distribution card — `data-panel="blend-returns-distribution" aria-label="Returns distribution"` (~:2076)
- Rolling-metrics card — `data-panel="blend-rolling" aria-label="Rolling metrics"` (~:2126)
- Entry-mode group — `role="radiogroup" aria-label="Composition entry mode"` (~:1666)
- Route: `/allocations?tab=scenario` (TabKey "scenario" in VISIBLE_TAB_KEYS)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Authed composer axe spec — seed → login → navigate → gate-on-render → assert zero AA violations</name>
  <files>e2e/composer-axe.spec.ts, src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - e2e/discovery-axe.spec.ts (the authed-seed + form-login + skip-gate + sanity-heading pattern to COPY verbatim)
    - e2e/strategy-v2-axe.spec.ts:39-71 (the scroll-each-card-ready-before-scan pattern to ADAPT for the Phase-30 graph cards)
    - e2e/helpers/axe.ts (the shared buildAxe factory — reuse, do not fork)
    - e2e/helpers/seed-test-project.ts:75-166 (seedTestAllocator return shape + what it stamps)
    - .planning/phases/33-journey-polish/33-PATTERNS.md (JOURNEY-03 section + "Authed e2e" shared pattern — the exact navigate/sanity-gate/scan sequence + composer selectors)
    - .planning/phases/33-journey-polish/33-UI-SPEC.md (JOURNEY-03 a11y acceptance-criteria table — landmarks/heading-order/names/keyboard/live-regions/contrast)
  </read_first>
  <action>
    Create e2e/composer-axe.spec.ts mirroring e2e/discovery-axe.spec.ts structure:
    import `test, expect` from "@playwright/test", `buildAxe` from "./helpers/axe",
    `seedTestAllocator` from "./helpers/seed-test-project". Define the same
    `HAS_SEED_ENV` gate (`TEST_SUPABASE_URL` + `TEST_SUPABASE_SERVICE_ROLE_KEY`)
    and the same `loginViaForm(page, email, password)` helper (goto /login → fill
    email + password → click Sign in → waitForURL allocations/dashboard). Wrap the
    test in a `test.skip(!HAS_SEED_ENV, "<skip string>")` mirroring the
    discovery-axe skip idiom so the spec is authored-but-not-CI-blocking until the
    seed env vars are wired (this is the W-02 false-green guard — do NOT let it
    silently pass against an unseeded DB). In the test: `seedTestAllocator()` →
    `loginViaForm(...)` → `page.goto("/allocations?tab=scenario")` →
    `page.waitForLoadState("networkidle")`. Add the SANITY GATE: assert a visible
    composer heading (e.g. a Portfolio/Scenario section `h2`) is visible with a
    5s timeout (fails loudly on a 404 / empty `<main>` / login chrome — NOT a
    silent zero-violation pass). Then ensure the Phase-30 graph cards rendered
    before scanning: `scrollIntoViewIfNeeded()` + `toBeVisible()` (10s) on
    `[data-panel="blend-returns-distribution"]` and `[data-panel="blend-rolling"]`
    (adapt the strategy-v2-axe scroll-each-card pattern). Then
    `const results = await buildAxe(page).analyze(); expect(results.violations).toEqual([]);`.

    Run the spec (locally, with seed env if available, OR confirm the skip gate
    fires cleanly when seed env is absent). If a REAL violation surfaces, fix it
    at ROOT in ScenarioComposer.tsx — add the missing semantic element / aria
    label / heading-order correction / contrast token from DESIGN.md — and re-run.
    NEVER call `.disableRules(...)` or `.exclude(...)` to dodge a finding, and
    never lighten a DESIGN.md-blessed shade. Do NOT duplicate the standalone-chart
    assertions already in strategy-v2-axe.spec.ts — scan the composer surface as a
    whole. Do NOT edit scenario.ts (frozen, SCENARIO-05) and do NOT strip any
    Phase-30 disclosure / the PROJECTED pill to "tidy up" for axe (honesty
    invariants LOCKED). Any ScenarioComposer.tsx edit, if needed, is a root a11y
    fix only and must keep `git diff --exit-code src/lib/scenario.ts` clean.
  </action>
  <verify>
    <automated>npx playwright test e2e/composer-axe.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - e2e/composer-axe.spec.ts exists, imports `buildAxe` from ./helpers/axe (no second harness, no jest-axe added to package.json).
    - With seed env present: the spec RUNS (not skipped), the sanity-heading gate passes (real composer rendered), both `data-panel` graph cards are visible before the scan, and `results.violations` equals `[]` (WCAG-AA zero-violations).
    - With seed env absent: the spec SKIPS via `test.skip(!HAS_SEED_ENV, ...)` (no false-green) — `npx playwright test e2e/composer-axe.spec.ts` reports the test skipped, not passed-against-empty.
    - The spec scans the COMPOSER surface (route `/allocations?tab=scenario`) and asserts the Phase-30 cards rendered first — it does NOT re-implement the standalone-chart assertions from strategy-v2-axe.spec.ts.
    - `grep -E "disableRules|\.exclude\(" e2e/composer-axe.spec.ts` returns no matches (no rule suppression).
    - If any ScenarioComposer.tsx edit was needed, it is a root a11y fix (semantic/label/contrast), `git diff` shows no disclosure/PROJECTED-pill removed, and `git diff --exit-code src/lib/scenario.ts src/lib/scenario.test.ts` is clean.
    - The phase-29/30 frozen-spine + IMPACT-02 guard tests stay green (`npx vitest run src/__tests__/phase-30-frozen-spine-guards.test.ts`).
  </acceptance_criteria>
  <done>
    A reusable authed composer axe spec asserts zero WCAG-AA violations on
    /allocations?tab=scenario including the Phase-30 cards, with a false-green
    skip gate; any real violation is fixed at root with no rule suppression and
    no honesty/frozen-engine regression.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan adds ONE e2e spec and, only if a real violation surfaces, a root a11y
fix in an existing client component. It introduces NO new endpoint, NO schema/RLS
change, NO new dependency. The authed spec exercises the existing seed +
form-login path (test-only credentials, test project). The honest threat model
is the carry-through guard + the false-green guard.

| Boundary | Description |
|----------|-------------|
| test seed ↔ auth | The spec seeds a VERIFIED test allocator via `seedTestAllocator()` (test project, test-only service-role key from env) — no prod credential, no new auth surface |
| axe spec ↔ false-green | A spec that scans an empty `<main>` / login chrome silently passes; the sanity-heading + graph-card visibility gates + the `HAS_SEED_ENV` skip prevent this |
| root a11y fix ↔ honesty invariants | Any fix must not strip a Phase-30 disclosure / the PROJECTED pill to satisfy axe (LOCKED) |
| root a11y fix ↔ frozen engine | Any fix must keep `scenario.ts` zero-diff (SCENARIO-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-06 | Repudiation (false-green) | the axe spec | mitigate | `test.skip(!HAS_SEED_ENV)` + a visible-heading sanity gate + graph-card `toBeVisible()` gate before `analyze()` — the spec cannot pass against an empty/404/login page |
| T-33-07 | Tampering | honesty invariants (Phase-30 disclosures, PROJECTED pill, IMPACT-02) | mitigate | Acceptance criterion: a11y fixes must not remove a disclosure; `git diff` reviewed; phase-30 frozen-spine + IMPACT-02 guard tests stay green |
| T-33-08 | Tampering | frozen engine (scenario.ts) | mitigate | `git diff --exit-code src/lib/scenario.ts` clean (SCENARIO-05) |
| T-33-09 | Repudiation (suppressed finding) | axe rule config | mitigate | `grep -E "disableRules|\.exclude\("` returns no matches — violations fixed at root, never suppressed |
| T-33-SC | Tampering | npm/pip/cargo installs | accept | ZERO new dependency installed (`@axe-core/playwright` already at ^4.11.2); no package-manager install task; nothing to vet |

Carry-through guard from 29–32: the RLS / share-RPC / honesty / frozen-engine
guards must not regress. This plan touches no RLS/schema and (at most) makes a
root a11y fix in one component; the existing frozen-spine + IMPACT-02 guard
tests remain the regression net.
</threat_model>

<verification>
- `npx playwright test e2e/composer-axe.spec.ts --reporter=line` → passes (seed env) or skips cleanly (no seed env), never false-green.
- `grep -E "disableRules|\.exclude\(" e2e/composer-axe.spec.ts` → no matches.
- `git diff --exit-code src/lib/scenario.ts src/lib/scenario.test.ts` → clean.
- `npx vitest run src/__tests__/phase-30-frozen-spine-guards.test.ts` → green (no honesty/frozen regression from any root a11y fix).
- No new dependency in `package.json` (`@axe-core/playwright` already installed).
</verification>

<success_criteria>
- JOURNEY-03 success criterion 3 ("the unified composer + new graphs pass
  automated accessibility checks (axe-core) at WCAG-AA") is satisfied by a
  reusable authed e2e spec asserting zero violations, with a false-green guard.
- Any real violation fixed at root; no rule suppression; no honesty/frozen-engine
  regression; no new dependency or second harness.
</success_criteria>

<output>
Create `.planning/phases/33-journey-polish/33-03-SUMMARY.md` when done.
If a real axe violation was found, record it + the root fix in the SUMMARY.
If the spec skipped (no seed env), record that the skip gate fired (authored-not-blocking).
</output>
