---
phase: 31-graphs-lead-layout-collapsible-controls
plan: 02
type: execute
wave: 2
depends_on: ["31-01"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/__tests__/phase-31-frozen-spine-guards.test.ts
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02]

must_haves:
  truths:
    - "An allocator can collapse/hide the composition controls (toggle/weight/leverage) so the graphs lead, and re-expand them"
    - "Collapsing preserves in-progress weight + leverage edits: a non-default weight + a leverage survive collapse→expand (no reset)"
    - "The projection/blend behind the collapsed controls still reflects the in-progress edits while collapsed"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "CompositionList wrapped in the lifted CollapsibleSection (hide-don't-unmount); always-MOUNTED when collapsed; composer-scoped storageKey"
      contains: "CollapsibleSection"
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"
      provides: "Non-vacuous type→collapse→expand→survives regression test (non-default weight + leverage + projection reflects them)"
      contains: "collapse"
    - path: "src/__tests__/phase-31-frozen-spine-guards.test.ts"
      provides: "Phase-31 exit-gate guard — scenario.ts/scenario.test.ts ZERO diff + grep gate proving no conditional MOUNT of CompositionList"
      contains: "CompositionList"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      to: "src/components/ui/CollapsibleSection.tsx"
      via: "import { CollapsibleSection }"
      pattern: "from \"@/components/ui/CollapsibleSection\""
    - from: "CollapsibleSection (collapsed)"
      to: "CompositionList"
      via: "children — always mounted, never conditional-mounted"
      pattern: "<CollapsibleSection[\\s\\S]*<CompositionList"
    - from: "leverageByRef + scenario.draft.weightOverrides (parent state, above the collapsible boundary)"
      to: "the projection / blend panels"
      via: "parent useState/draft read by the graph panels regardless of CompositionList collapse"
      pattern: "leverageByRef"
---

<objective>
Wrap the `CompositionList` (toggle / weight / leverage controls) in `ScenarioComposer` with the lifted `CollapsibleSection` so the factsheet-grade graphs (already rendered BEFORE `CompositionList` in DOM order — Correlation ~L2065, Returns distribution ~L2085, Rolling ~L2135) lead the surface when the controls are collapsed. Collapsing MUST hide, never unmount — and every in-progress weight + leverage edit must survive collapse→expand because the edit state lives in the PARENT, above the collapsible boundary.

Purpose: LAYOUT-01 (controls collapsible so graphs lead) + LAYOUT-02 (collapse preserves in-progress weight + leverage edits — Pitfall 5, the core silent-failure gate). A naive `{open && <CompositionList />}` would wipe edits on collapse; native `<details>` keeps children mounted and the parent-held state survives by construction.

Output: `ScenarioComposer.tsx` wraps `CompositionList`; a NON-VACUOUS regression test (non-default weight + leverage, collapse, expand, assert both survive AND the projection reflects them); a phase-31 frozen-spine guard enforcing `scenario.ts` zero-diff + the no-conditional-mount grep gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-graphs-lead-layout-collapsible-controls/31-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the current codebase. -->
<!-- Lifted primitive (delivered by Plan 31-01): src/components/ui/CollapsibleSection.tsx -->
- `CollapsibleSection({ id, title, subtitle?, defaultOpen=true, storageKey?, onToggle?, children })` — native <details>; children stay MOUNTED when collapsed (browser hides them). Persists open/closed via storageKey through useCrossTabStorage + rawStringCodec.

<!-- Host: src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (2646 lines) -->
- L501: `const [leverageByRef, setLeverageByRef] = useState<Record<string, number>>({});` — leverage edit state, in the PARENT component (above CompositionList). DO NOT move this down.
- Weight edit state is `scenario.draft.weightOverrides` — managed by the parent `scenario` state hook; written via `scenario.setWeightOverride(scopeRef, clampedWeight)` inside the parent's `handleWeightChange` (~L603). Also in the parent, above CompositionList.
- The graph panels read parent state, NOT CompositionList's mount: Correlation (`<CorrelationHeatmap>` ~L2065, fed `scenarioMetrics`), Returns distribution (`data-panel="blend-returns-distribution"` ~L2085, fed `blendPanels`), Rolling (`data-panel="blend-rolling"` ~L2135). These render in DOM order BEFORE CompositionList.
- `<CompositionList ... />` is rendered ~L2245 with props: `draft={scenario.draft}`, `onSetWeight={handleWeightChange}`, `leverageByRef={leverageByRef}`, `onSetLeverage={handleLeverageChange}`, etc. CompositionList is DEFINED ~L2394 as a sub-component in the same file (props include `leverageByRef`). The added-strategy rows expose `id={`weight-${a.id}`}` (label "<name> weight"), `id={`leverage-${a.id}`}` (aria-label "<name> leverage multiplier"), and a `data-scope-ref={a.id}` wrapper `<li>`.

<!-- Existing tests: src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (3525 lines) -->
- Render helper: `render(<ScenarioComposer payload={makePayload()} allocatorId={ALLOCATOR_A} allocatorMandate={null} />)`.
- `addStrategy({ id, name, markets, strategy_types })` helper simulates a Browse-drawer Add → row appears in the composition list.
- Charts are mocked to inert spies via `vi.mock`; their props are observable through `vi.mocked(<Chart>).mock.calls`. `KpiStrip` is mocked and its props (incl. scenarioMetrics) are inspectable — the projection's reaction to edits is observable through it.
- Weight input fetched via `screen.getByLabelText(/<name> weight/i)`; rows via `container.querySelector('[data-scope-ref="<id>"]')`. Established matchers in this file: `fireEvent.change`, `getByLabelText`, `within`, `act`, `waitFor`.

<!-- Frozen-spine guard pattern (delivered Phase 29/30): src/__tests__/phase-30-frozen-spine-guards.test.ts -->
- Reads the REAL git delta (merge-base origin/main..HEAD + untracked-not-ignored) via execFileSync("git", [...]); asserts src/lib/scenario.ts AND src/lib/scenario.test.ts are NOT in the changed set; fails LOUD if the baseline ref is unresolvable (FALLBACK_BASE_SHA + throw). Non-vacuity verified by authoring (a no-op line in scenario.ts makes it fail). NOTE: CI must checkout with fetch-depth: 0 for these git-baseline guards (already configured project-wide; do not re-fix).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap CompositionList in the lifted CollapsibleSection (hide-don't-unmount)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (read the import block ~L78-130, the leverageByRef state ~L501, handleWeightChange ~L580-604, the graphs render block ~L2048-2175, the CompositionList render ~L2245-2260)
    - src/components/ui/CollapsibleSection.tsx (the lifted primitive's prop surface, from Plan 31-01)
  </read_first>
  <action>
    Add `import { CollapsibleSection } from "@/components/ui/CollapsibleSection";` to the import block (alongside the existing `@/components/ui/*` imports near L80-82). At the `<CompositionList ... />` render site (~L2245), wrap the EXISTING `<CompositionList ... />` element in a `<CollapsibleSection>` — do NOT change any CompositionList prop and do NOT move CompositionList's definition. Pass the wrapper these props: `id="composer-composition-controls"`, `title` a neutral controls label (e.g. "Strategies & weights"), `defaultOpen={true}` (EXPANDED on first load — an allocator composing needs the controls visible; hiding to focus on graphs is opt-in), and `storageKey="composer-collapse:controls"` (a composer-scoped key, independent of the factsheet `factsheet-collapse:` namespace). Do NOT pass `onToggle` (analytics on the composer collapse is out of scope this phase). The graphs already render before CompositionList in DOM order, so NO panel reorder is needed — collapsing the controls gives the graphs the full surface (per 31-CONTEXT).

    CRITICAL hide-don't-unmount constraint: the wrap MUST be `<CollapsibleSection ...><CompositionList ... /></CollapsibleSection>` — CompositionList is an unconditional child, always mounted. NEVER write `{open && <CompositionList` or any conditional that unmounts CompositionList on collapse. Do NOT move `leverageByRef` (L501) or the weight state (`scenario.draft` / `scenario.setWeightOverride`) down into CompositionList — they MUST stay in the parent, above the collapsible boundary, so edits survive collapse by construction. Make NO other change to ScenarioComposer (no engine touch, no reorder, no state relocation).
  </action>
  <acceptance_criteria>
    - `grep -c 'from "@/components/ui/CollapsibleSection"' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 1.
    - The wrap exists: `grep -Pzoc '<CollapsibleSection[\s\S]*?<CompositionList' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` matches (CollapsibleSection encloses CompositionList).
    - NO conditional mount: `grep -nE '\{[^}]*open[^}]*&&[^}]*<CompositionList' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns NOTHING. (The phase-31 guard in Task 3 enforces this durably.)
    - leverageByRef stays in the parent: `grep -nc 'const \[leverageByRef, setLeverageByRef\] = useState' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 1, and that line is OUTSIDE the `function CompositionList(` body (still at the top-level ScenarioComposer component, ~L501).
    - composer storageKey present: `grep -c 'composer-collapse:controls' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` returns 1.
    - ZERO diff to the frozen engine: `git diff --name-only -- src/lib/scenario.ts src/lib/scenario.test.ts` returns NOTHING.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -z "$(git diff --name-only -- src/lib/scenario.ts src/lib/scenario.test.ts)"</automated>
  </verify>
  <done>CompositionList is wrapped in the lifted CollapsibleSection (always mounted when collapsed), default-expanded, composer-scoped storageKey; edit state untouched in the parent; engine zero-diff; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Non-vacuous type→collapse→expand→survives regression test</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx (read the render helper, makePayload, the addStrategy helper, and an existing added-strategy test e.g. T_C7 ~L682 and T_C10 ~L753 for the weight-input + chart-mock-prop patterns)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the wrapped CompositionList from Task 1 — confirm the CollapsibleSection id/summary so the test can toggle the right <details>)
  </read_first>
  <behavior>
    - Add a non-book strategy via the existing addStrategy helper so its weight + leverage inputs render.
    - Type a NON-DEFAULT weight (e.g. 0.250 — distinct from the 0.000 default an added strategy shows) into that strategy's weight input, and set a NON-DEFAULT leverage (e.g. 2 — distinct from the 1× default) into its leverage input.
    - Capture the projection signal the graphs/KPI read AFTER the edits (e.g. the latest scenarioMetrics prop passed to the mocked KpiStrip, or the blend series prop to a mocked chart) — assert it reflects the edited composition (changed vs the pre-edit capture).
    - Collapse the controls (toggle the CollapsibleSection <details> to closed), then expand it (toggle back open).
    - After expand: the weight input STILL shows the non-default weight AND the leverage input STILL shows the non-default leverage (no reset to defaults) — assert the input .value strings.
    - AND the projection signal captured while collapsed (or after expand) STILL reflects the edited composition (unchanged from the post-edit capture; NOT reverted to the default-weight projection). This is the load-bearing LAYOUT-02 assertion that the blend behind the collapsed panel keeps reflecting the edits.
  </behavior>
  <action>
    Add a new `it(...)` (or a small `describe`) to ScenarioComposer.test.tsx — name it explicitly for the gate, e.g. "LAYOUT-02 collapsing the composition controls preserves in-progress weight + leverage edits and the projection still reflects them". Reuse the file's existing render helper, makePayload, addStrategy helper, and the chart/KpiStrip mocks already declared at the top of the file (do NOT add new module mocks). Drive the inputs with `fireEvent.change` on `screen.getByLabelText(/<name> weight/i)` and the leverage input (by its label/aria-label), exactly as the existing tests do. Toggle the CollapsibleSection by getting its `<details>` (by the `id="composer-composition-controls"` from Task 1) and firing the toggle the same way the CollapsibleSection.test.tsx does (`detailsEl().open = false; fireEvent(detailsEl(), new Event("toggle"))` then back to true) — wrap state-changing calls in `act(...)`. Capture the projection via `vi.mocked(KpiStrip).mock.calls` (last call's scenarioMetrics) or the relevant mocked chart's last-call props; take one capture after the edits and one after expand and assert equality (edits preserved), and assert it DIFFERS from a baseline default-weight capture (non-vacuity — proves the edit actually moved the projection, so "survives" is meaningful). This test MUST NOT collapse with default state — it types a non-default weight AND sets a non-default leverage FIRST (a default-state collapse is vacuous and fails the exit gate). Make NO change to ScenarioComposer.tsx in this task.
  </action>
  <acceptance_criteria>
    - The new test references collapse + both edit axes: `grep -nc "composer-composition-controls\|toggle" "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` is >= 1 AND the new test body sets BOTH a non-default weight and a non-default leverage before collapsing (verified by reading the test: it calls `fireEvent.change` on a weight input with a non-"0.000" value AND on a leverage input with a non-"1" value prior to the collapse toggle).
    - The test is non-vacuous: it asserts the post-edit projection capture DIFFERS from a default-weight baseline capture (so the survival assertion is meaningful), AND that the weight + leverage input values are unchanged after expand.
    - `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` exits 0 (the new test + the full existing suite in this file stay green).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <done>A non-vacuous regression test types a non-default weight + leverage, collapses, expands, and asserts both survive AND the projection still reflects them; the differs-from-baseline assertion proves it is not vacuous; the file's full suite passes.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-31 frozen-spine + no-conditional-mount exit-gate guard</name>
  <files>src/__tests__/phase-31-frozen-spine-guards.test.ts</files>
  <read_first>
    - src/__tests__/phase-30-frozen-spine-guards.test.ts (the pattern to mirror — git-delta read, frozen-engine assertion, fail-loud baseline resolution, non-vacuity comment — read fully)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the file the no-conditional-mount grep gate inspects)
  </read_first>
  <action>
    Create src/__tests__/phase-31-frozen-spine-guards.test.ts mirroring the phase-30 guard's structure. Include TWO assertions. (1) Frozen-engine zero-diff: reuse the phase-30 git-delta approach (merge-base origin/main..HEAD + untracked-not-ignored via execFileSync("git", [...]), fail LOUD if the baseline ref is unresolvable, set this phase's own FALLBACK_BASE_SHA to the current HEAD sha at planning time) and assert that `src/lib/scenario.ts` and `src/lib/scenario.test.ts` are NOT in the phase delta. (2) No-conditional-mount grep gate (Pitfall 5, the durable LAYOUT-02 guard): read src/app/(dashboard)/allocations/components/ScenarioComposer.tsx from disk and assert there is NO conditional MOUNT of CompositionList — i.e. no `open && <CompositionList` / `&& <CompositionList` pattern anywhere; AND assert positively that CompositionList IS wrapped by a CollapsibleSection (the lifted import is present AND a `<CollapsibleSection` encloses `<CompositionList`). Use content-inspecting reads from disk (readFileSync), not a hardcoded snapshot. Add a non-vacuity comment documenting that introducing `{open && <CompositionList ...}` makes the gate fail (the silent-failure surface this guard exists to catch). Determine the planning-time HEAD sha for FALLBACK_BASE_SHA via `git rev-parse --short HEAD` at execution time and bake it in.
  </action>
  <acceptance_criteria>
    - `test -f src/__tests__/phase-31-frozen-spine-guards.test.ts` succeeds.
    - The guard asserts the frozen engine: `grep -c "scenario.ts" src/__tests__/phase-31-frozen-spine-guards.test.ts` is >= 1 AND `grep -c "scenario.test.ts" src/__tests__/phase-31-frozen-spine-guards.test.ts` is >= 1.
    - The guard inspects the composer for conditional mount: `grep -c "CompositionList" src/__tests__/phase-31-frozen-spine-guards.test.ts` is >= 1 AND `grep -c "CollapsibleSection" src/__tests__/phase-31-frozen-spine-guards.test.ts` is >= 1.
    - The guard reads from disk, not a hardcoded list: `grep -c "readFileSync\|execFileSync" src/__tests__/phase-31-frozen-spine-guards.test.ts` is >= 1.
    - `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts` exits 0 (passes against the Task-1/Task-2 state).
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts</automated>
  </verify>
  <done>A phase-31 guard enforces scenario.ts/scenario.test.ts zero-diff (fail-loud on unresolvable baseline) AND a content-inspecting no-conditional-mount gate proving CompositionList is wrapped, never `{open && <CompositionList`; it passes against the wrapped composer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure client-side collapse UI on an existing surface; a component wrap + tests. No new inputs, routes, data flows, auth, or external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-02 | (n/a) | ScenarioComposer collapsible controls | accept | No new attack surface — client-only disclosure UI; no new inputs/routes/data/auth. The only new persisted key is `composer-collapse:controls` (an open/closed string), no PII, no tenant data. Block-on threshold: high; none present. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"` exits 0 (regression test + existing suite green).
- `npx vitest run src/__tests__/phase-31-frozen-spine-guards.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
- ZERO diff to `src/lib/scenario.ts` and `src/lib/scenario.test.ts` (`git diff --name-only -- src/lib/scenario.ts src/lib/scenario.test.ts` empty) — enforced by the phase-31 guard.
- No conditional mount of CompositionList anywhere in ScenarioComposer.tsx (`grep -nE '&&[^}]*<CompositionList' "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` empty) — enforced by the phase-31 guard.
- The lifted primitive's own suite still green: `npx vitest run src/components/ui/CollapsibleSection.test.tsx` exits 0.
</verification>

<success_criteria>
- An allocator can collapse/hide the composition controls so the graphs (already DOM-ordered before them) lead, and re-expand them; the choice persists via `composer-collapse:controls`.
- Collapsing preserves in-progress weight + leverage edits — a non-default weight + leverage survive collapse→expand (no reset) — proven by a NON-VACUOUS regression test that also asserts the edits moved the projection (differs-from-baseline) and that the projection behind the collapsed panel still reflects them.
- The hide-don't-unmount invariant is durably guarded: CompositionList is always mounted (wrapped, never `{open && ...}`), and a content-inspecting phase-31 test fails if anyone reintroduces a conditional mount.
- The frozen engine `src/lib/scenario.ts` (SCENARIO-05) is zero-diff, guard-enforced.
</success_criteria>

<output>
Create `.planning/phases/31-graphs-lead-layout-collapsible-controls/31-02-SUMMARY.md` when done.
</output>
