---
phase: 31-graphs-lead-layout-collapsible-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/CollapsibleSection.tsx
  - src/components/ui/CollapsibleSection.test.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
autonomous: true
requirements: [LAYOUT-01]

must_haves:
  truths:
    - "The disclosure primitive lives at src/components/ui/CollapsibleSection.tsx (generalized out of factsheet)"
    - "The factsheet renders and behaves identically after the lift (Reset view still pops every section open; analytics still fire on user toggle)"
    - "Existing stored factsheet open/closed states survive (storageKey strings are byte-identical)"
  artifacts:
    - path: "src/components/ui/CollapsibleSection.tsx"
      provides: "Lifted, factsheet-agnostic CollapsibleSection + neutral open-all event constant + optional onToggle analytics callback"
      contains: "COLLAPSIBLE_OPEN_ALL_EVENT"
    - path: "src/components/ui/CollapsibleSection.test.tsx"
      provides: "Migrated persistence/open-all/no-storageKey tests against the new path + neutral event name + onToggle callback"
      contains: "COLLAPSIBLE_OPEN_ALL_EVENT"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "Factsheet repointed to the lifted primitive — import path, open-all dispatch, analytics via onToggle"
      contains: "@/components/ui/CollapsibleSection"
  key_links:
    - from: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      to: "src/components/ui/CollapsibleSection.tsx"
      via: "import { CollapsibleSection, COLLAPSIBLE_OPEN_ALL_EVENT }"
      pattern: "from \"@/components/ui/CollapsibleSection\""
    - from: "FactsheetView ControlBar Reset view"
      to: "every CollapsibleSection in the factsheet tree"
      via: "window.dispatchEvent(new Event(COLLAPSIBLE_OPEN_ALL_EVENT))"
      pattern: "COLLAPSIBLE_OPEN_ALL_EVENT"
    - from: "FactsheetView CollapsibleSection usages"
      to: "trackFactsheetEvent"
      via: "onToggle callback prop (no longer a hard import inside the primitive)"
      pattern: "onToggle"
---

<objective>
Lift the `CollapsibleSection` disclosure primitive out of `src/app/factsheet/[id]/v2/` into the shared `src/components/ui/` directory and generalize its factsheet coupling, so Phase 31 Plan 02 can reuse it on the `ScenarioComposer` without dragging a factsheet dependency into the allocations surface.

Purpose: LAYOUT-01 needs a reusable hide-don't-unmount disclosure primitive. The existing one is factsheet-namespaced (`FACTSHEET_OPEN_ALL_EVENT`) and hard-imports `trackFactsheetEvent`. Generalize both, repoint the sole consumer (`FactsheetView`), and prove the factsheet still behaves byte-identically.

Output: `src/components/ui/CollapsibleSection.tsx` (+ its migrated test), `FactsheetView.tsx` repointed. ZERO behavior change to the factsheet; existing stored open/closed states survive (storageKey strings unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/31-graphs-lead-layout-collapsible-controls/31-CONTEXT.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the current codebase. -->
<!-- Current primitive: src/app/factsheet/[id]/v2/CollapsibleSection.tsx -->
- Exported component: `CollapsibleSection({ id, title, subtitle?, defaultOpen=true, storageKey?, children })`
- Exported constant: `FACTSHEET_OPEN_ALL_EVENT = "factsheet-v2:open-all"` — broadcast on `window`; every section listens and pops open + persists "open".
- Persistence: `useCrossTabStorage` (from `@/lib/storage/cross-tab`) + `rawStringCodec` (from `@/lib/storage/codecs`), `sentryArea: "factsheet.section"`. The codec stores the LITERAL "open"/"closed" string (byte-compat with pre-B7 writes). Key = the raw `storageKey` prop unchanged (factsheet passes `factsheet-collapse:${id}:${section}`).
- Analytics: hard `import { trackFactsheetEvent } from "./factsheet-analytics"`, called inside the `<details onToggle>` handler ONLY for user-initiated toggles (guarded by `if (hydrated && nextOpen !== open)`) as `trackFactsheetEvent("factsheet_v2_section_toggle", { section: id, open: nextOpen })`.

<!-- Sole current consumer: src/app/factsheet/[id]/v2/FactsheetView.tsx -->
- L18: `import { CollapsibleSection, FACTSHEET_OPEN_ALL_EVENT } from "./CollapsibleSection";`
- L826 (ControlBar.resetView): `window.dispatchEvent(new Event(FACTSHEET_OPEN_ALL_EVENT));`
- FactsheetView already has `trackFactsheetEvent` in scope (used at L828 for `factsheet_v2_reset_view`).

<!-- Non-importer to leave UNTOUCHED -->
- src/components/auth/SignOutButton.test.tsx L134 references the STRING literal "factsheet-collapse:{strategyId}:{section}" in a localStorage-purge key inventory — it does NOT import CollapsibleSection. Because the factsheet storageKey strings stay byte-identical, this test stays green and MUST NOT be edited.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lift + generalize CollapsibleSection into src/components/ui/</name>
  <files>src/components/ui/CollapsibleSection.tsx, src/components/ui/CollapsibleSection.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/CollapsibleSection.tsx (the source-of-truth pattern to move — read fully)
    - src/app/factsheet/[id]/v2/CollapsibleSection.test.tsx (the test to migrate — read fully)
    - src/lib/storage/cross-tab.ts (the useCrossTabStorage contract the primitive depends on)
    - src/lib/storage/codecs.ts (the rawStringCodec contract)
    - src/components/ui/Card.tsx (sibling primitive — match the ui/ file conventions)
  </read_first>
  <behavior>
    - Absent storageKey value → section honors `defaultOpen` (open=true / closed=false), zero storage IO when no `storageKey` prop.
    - Stored literal "closed"/"open" overrides `defaultOpen`; junk falls back to `defaultOpen` (codec self-coerces); a stored value is NOT re-persisted over on hydration (no-clobber).
    - A user toggle persists the literal "closed"/"open" at the raw storageKey (byte-compat, no JSON envelope).
    - The new `COLLAPSIBLE_OPEN_ALL_EVENT` pops a closed section open AND persists "open".
    - When an `onToggle` callback is provided, it is invoked with the new open boolean ONLY on a user-initiated toggle (hydrated && changed), NOT on the mount-time default-vs-stored reconciliation. When `onToggle` is absent, toggling still works and persists with no error.
  </behavior>
  <action>
    Create src/components/ui/CollapsibleSection.tsx by moving the factsheet primitive's logic verbatim EXCEPT for two generalizations: (1) rename the exported constant `FACTSHEET_OPEN_ALL_EVENT` → `COLLAPSIBLE_OPEN_ALL_EVENT` with value `"collapsible-section:open-all"`, and update every internal use (the addEventListener/removeEventListener pair and the handler that pops the section open + persists). (2) Remove the hard `import { trackFactsheetEvent } from "./factsheet-analytics"`; replace it with a new OPTIONAL prop `onToggle?(open: boolean): void` added to the component's prop type. Inside the existing `<details onToggle>` handler, where the code currently calls `trackFactsheetEvent("factsheet_v2_section_toggle", ...)` under the `if (hydrated && nextOpen !== open)` guard, call `onToggle?.(nextOpen)` instead. Keep EVERY other line identical: the `useCrossTabStorage` wiring, the `rawStringCodec`/`defaultOpen` useMemo, the hydration effect, the persist-on-toggle logic, the `<summary>` markup + class strings (uppercase tracking-wider title, `Hide`/`Show` mono affordance, caret rotation, `min-h-[44px]`, `focus-visible:outline-accent`), and the `sentryArea: "factsheet.section"` string (leave it — changing it is out of scope and would alter Sentry attribution). Do NOT change the default `storageKey ?? "factsheet-collapse:__unused__"` fallback string (the factsheet still passes real keys; the composer in Plan 02 will pass its own).

    Then migrate the test: create src/components/ui/CollapsibleSection.test.tsx as a move of the factsheet test with these edits — import `{ CollapsibleSection, COLLAPSIBLE_OPEN_ALL_EVENT }` from "./CollapsibleSection"; replace every `FACTSHEET_OPEN_ALL_EVENT` reference with `COLLAPSIBLE_OPEN_ALL_EVENT`; the existing `vi.mock("./factsheet-analytics", ...)` line MUST be removed (the primitive no longer imports it) and the open-all/toggle tests adapted to the `onToggle` callback prop where they previously relied on the hard analytics import — add a focused test passing an `onToggle` spy that asserts it fires with the new open boolean on a user toggle and does NOT fire on mount-time hydration. Keep the localStorage mock, the `KEY = "factsheet-collapse:strat-1:perf"` fixture string (byte-compat coverage), and all hydration/persist/no-clobber/no-storageKey cases.

    DELETE the old files in this task: remove src/app/factsheet/[id]/v2/CollapsibleSection.tsx and src/app/factsheet/[id]/v2/CollapsibleSection.test.tsx (the move is a move, not a copy — FactsheetView is repointed in Task 2). Do NOT touch src/app/factsheet/[id]/v2/factsheet-analytics.ts.
  </action>
  <acceptance_criteria>
    - `test -f src/components/ui/CollapsibleSection.tsx` succeeds AND `test -f src/components/ui/CollapsibleSection.test.tsx` succeeds.
    - `test ! -f "src/app/factsheet/[id]/v2/CollapsibleSection.tsx"` succeeds (old file removed) AND `test ! -f "src/app/factsheet/[id]/v2/CollapsibleSection.test.tsx"` succeeds.
    - `grep -c 'COLLAPSIBLE_OPEN_ALL_EVENT = "collapsible-section:open-all"' src/components/ui/CollapsibleSection.tsx` returns 1.
    - `grep -rn "FACTSHEET_OPEN_ALL_EVENT" src/components/ui/CollapsibleSection.tsx src/components/ui/CollapsibleSection.test.tsx` returns NOTHING (renamed everywhere in the lifted files).
    - `grep -c "factsheet-analytics" src/components/ui/CollapsibleSection.tsx` returns 0 (no hard analytics import; decoupled to onToggle).
    - `grep -c "onToggle" src/components/ui/CollapsibleSection.tsx` is >= 1 (the optional callback prop is wired).
    - `npx vitest run src/components/ui/CollapsibleSection.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/ui/CollapsibleSection.test.tsx</automated>
  </verify>
  <done>The primitive lives at src/components/ui/CollapsibleSection.tsx with a neutral event constant and an optional onToggle analytics callback; its migrated test passes; the old factsheet copies are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint FactsheetView to the lifted primitive (zero behavior change)</name>
  <files>src/app/factsheet/[id]/v2/FactsheetView.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (the consumer — read the import block ~L1-30, every `<CollapsibleSection` usage, and the ControlBar resetView ~L814-829)
    - src/components/ui/CollapsibleSection.tsx (the lifted primitive's new prop surface, from Task 1)
  </read_first>
  <action>
    Update the import at L18 from `import { CollapsibleSection, FACTSHEET_OPEN_ALL_EVENT } from "./CollapsibleSection";` to `import { CollapsibleSection, COLLAPSIBLE_OPEN_ALL_EVENT } from "@/components/ui/CollapsibleSection";`. Replace the dispatch in ControlBar.resetView (currently `window.dispatchEvent(new Event(FACTSHEET_OPEN_ALL_EVENT));`) with the renamed `COLLAPSIBLE_OPEN_ALL_EVENT`. For each `<CollapsibleSection ...>` usage in FactsheetView, pass the analytics behavior that the primitive previously hard-coded via the new callback: `onToggle={(open) => trackFactsheetEvent("factsheet_v2_section_toggle", { section: <the same id passed to that section>, open })}` so the exact same event name + payload shape fires as before. `trackFactsheetEvent` is already imported in FactsheetView (used at L828). Keep every `id`, `title`, `subtitle`, `defaultOpen`, and `storageKey` prop value byte-identical — especially the `storageKey` strings (the `factsheet-collapse:...` keys), so existing stored open/closed states survive (rawStringCodec back-compat) and the SignOutButton purge-key inventory stays accurate. Do NOT edit src/components/auth/SignOutButton.test.tsx. Make NO other change to FactsheetView.
  </action>
  <acceptance_criteria>
    - `grep -c 'from "@/components/ui/CollapsibleSection"' src/app/factsheet/[id]/v2/FactsheetView.tsx` returns 1.
    - `grep -c "FACTSHEET_OPEN_ALL_EVENT" src/app/factsheet/[id]/v2/FactsheetView.tsx` returns 0 (renamed to COLLAPSIBLE_OPEN_ALL_EVENT).
    - `grep -c "from \"./CollapsibleSection\"" src/app/factsheet/[id]/v2/FactsheetView.tsx` returns 0 (no stale relative import to the moved file).
    - `grep -c "onToggle=" src/app/factsheet/[id]/v2/FactsheetView.tsx` is >= 1 AND `grep -c "factsheet_v2_section_toggle" src/app/factsheet/[id]/v2/FactsheetView.tsx` is >= 1 (analytics behavior preserved at the call site).
    - Factsheet storageKey strings unchanged: `grep -c "factsheet-collapse:" src/app/factsheet/[id]/v2/FactsheetView.tsx` returns the SAME count as before the change (byte-compat — run the grep before and after; the count must not drop).
    - `npx tsc --noEmit` exits 0 (no dangling import to the deleted relative path).
    - Existing factsheet view tests pass: `npx vitest run "src/app/factsheet"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run "src/app/factsheet" && npx tsc --noEmit</automated>
  </verify>
  <done>FactsheetView imports the lifted primitive, dispatches the renamed event, and fires the same analytics event via onToggle; tsc clean; existing factsheet tests green; storageKey strings byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan moves a client-only component file and repoints one consumer. No new inputs, routes, data flows, auth, or external surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | (n/a) | CollapsibleSection lift + FactsheetView repoint | accept | No new attack surface — client-only disclosure UI; no new inputs/routes/data/auth. localStorage keys are byte-identical (no new storage key namespace). Block-on threshold: high; none present. |
</threat_model>

<verification>
- `npx vitest run src/components/ui/CollapsibleSection.test.tsx` exits 0.
- `npx vitest run "src/app/factsheet"` exits 0 (factsheet behavior unchanged).
- `npx tsc --noEmit` exits 0 (no dangling relative import to the deleted file).
- `grep -rn "FACTSHEET_OPEN_ALL_EVENT" src` returns NOTHING (fully renamed across the lifted primitive + consumer).
- `grep -rln "from \"./CollapsibleSection\"\|from \"@/app/factsheet/.*CollapsibleSection\"" src` returns NOTHING (no importer still points at the old path).
- ZERO diff to `src/lib/scenario.ts` and `src/lib/scenario.test.ts` (untouched this plan; the phase-31 guard added in Plan 02 enforces this).
</verification>

<success_criteria>
- The disclosure primitive is reusable at `src/components/ui/CollapsibleSection.tsx`, factsheet-agnostic (neutral event constant + optional `onToggle`).
- The factsheet renders and behaves identically: "Reset view" still pops every section open, user toggles still fire `factsheet_v2_section_toggle` with the same payload, and existing stored open/closed states survive (storageKey strings byte-identical).
- No file outside `files_modified` (plus the two deleted old factsheet files) is touched; `SignOutButton.test.tsx` left untouched and still green.
</success_criteria>

<output>
Create `.planning/phases/31-graphs-lead-layout-collapsible-controls/31-01-SUMMARY.md` when done.
</output>
