---
phase: 29-unified-composer-spine
plan: 04
type: execute
wave: 2
depends_on: ["29-01", "29-02", "29-03"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx
autonomous: true
requirements: [UNIFY-01, UNIFY-02, UNIFY-04, UNIFY-05]
must_haves:
  truths:
    - "The composer presents one surface with two entry modes: 'From my book' (seeded from live holdings) and 'Blank slate' (empty working composition)"
    - "A mode switch that would discard a dirty draft routes through the existing reset-confirmation modal — never a silent wipe"
    - "Adding a catalog strategy lazy-fetches its daily_returns and the projection recomputes (n>0, curve changes) once resolved"
    - "While the lazy fetch is in flight the added strategy contributes [] (warm-up-gated) with an honest loading affordance — never a fabricated flat series"
    - "Save/Update/Save-as-new/name-input/saved-list/reopen-notice copy reads 'portfolio'; reopen still honors the ok/readonly/reset codec trichotomy"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "Entry-mode segmented control + lazy-returns plumbing + 'portfolio' copy over the unchanged adapter/engine/codec"
      contains: "addedReturnsById"
  key_links:
    - from: "ScenarioComposer addStrategyBrowse handler"
      to: "/api/strategies/[id]/returns"
      via: "lazy fetch on add → addedReturnsById map → addedStrategyReturnsLookup"
      pattern: "strategies/.+/returns|addedReturnsById"
    - from: "entry-mode segment onClick"
      to: "setResetModalOpen / handleReset"
      via: "dirty-draft mode switch routes through the existing reset confirmation"
      pattern: "setResetModalOpen|handleReset"
---

<objective>
Wire the three genuinely-new client behaviors into the existing `ScenarioComposer` (the unification host at `/allocations?tab=scenario`): (1) an entry-mode segmented control ("From my book" / "Blank slate") routed through the existing reset/confirm discipline; (2) lazy-returns plumbing so an added catalog strategy's `daily_returns` (from Plan 01's route) actually moves the projection through the UNCHANGED adapter + frozen engine; (3) the "portfolio" copy relabel + reopen-notice relabel that preserves the LOCKED ok/readonly/reset codec trichotomy.

Purpose: This closes UNIFY-01 (one surface), UNIFY-02 (blank-slate OR live-book in the same surface), UNIFY-04 (add reflects in the projection immediately), and UNIFY-05 (named "portfolio" over the unchanged scenarios persistence). The composer already owns every hard part — this is ~90% wiring of landed, test-pinned primitives (29-RESEARCH key insight).
Output: Composer edits + synchronized test additions. The frozen engine (`scenario.ts`) and the codec are reused verbatim — zero diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

DESIGN.md is the LOCKED visual contract — read it before the segmented-control UI. The control, copy, and a11y are pinned in 29-UI-SPEC.md (§1 entry-mode control, §Copywriting, §Accessibility). React semantics (useMemo/useCallback/effect cleanup): see react.dev. AGENTS.md / Next 16: this is a "use client" component — do NOT call server-only helpers from it; the lazy fetch is a client `fetch()` to Plan 01's route.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-unified-composer-spine/29-UI-SPEC.md
@.planning/phases/29-unified-composer-spine/29-RESEARCH.md
@.planning/phases/29-unified-composer-spine/29-PATTERNS.md

<interfaces>
Reset discipline — ScenarioComposer.tsx:519-528 `handleReset` (scenario.reset() + clear loadedScenarioId/Name/readonly/notice); `resetModalOpen` state (373) drives `ResetConfirmationModal` (rendered ~1787, footer `onResetRequested={() => setResetModalOpen(true)}` at 1751); `scenario.diffCount` (1749) is the dirty signal.

Lazy-lookup target — ScenarioComposer.tsx:777-790 `addedStrategyReturnsLookup` useMemo (currently keyed ONLY off `strategyById` from book-only `payload.strategies`). Adapter call at 827-856 (`buildStrategyForBuilderSet`), projection overlay at 891-918, frozen engine `computeScenario` at 939-942 — all UNCHANGED. `DailyPoint = {date,value}` (src/lib/portfolio-math-utils.ts:11).

Add gesture — `scenario.addStrategyBrowse({ id, name, markets, strategy_types })` (wired in the empty-state at 1280 and the non-empty body via the drawer `onAdd`). `scenario.draft.addedStrategies` is the source of the lookup loop.

Reopen trichotomy (LOCKED) — `openSavedScenario` at 539-584: decode via `scenarioDraftCodec(defaultDraft).decode(JSON.stringify(row.draft))`; `reset` → notice + NO hydrate; `readonly` → hydrate + adopt id + block Update + notice; `ok` → hydrate + adopt id. Notices at 552 / 564 reference "scenario" — relabel to "portfolio".

Save toolbar copy — "Save scenario" (1334), "Update scenario" (1348), "Save as new scenario" (1361), placeholder/aria "Name this scenario" (1374-1375). Empty-state heading "Scenario builder needs holdings" (1247) + body (1249-1252).

Test harness — ScenarioComposer.test.tsx mocks `StrategyBrowseDrawer`, `KpiStrip`, `buildStrategyForBuilderSet` (vi.mocked); T_C1 empty-state, T_C10/T_C13 add+reset, T_C12-14 reset modal. ScenarioComposer.save.test.tsx covers save/reopen. The drawer mock captures `onAdd` (test line 353) so an add can be simulated; `vi.mocked(buildStrategyForBuilderSet)` observes the lookup it receives.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Entry-mode segmented control routed through the reset discipline</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (handleReset 519-528; resetModalOpen 373; ResetConfirmationModal render ~1787 + onResetRequested 1751; diffCount 1749; isEmptyState 750-751; defaultDraftFromHoldings usage 542; the title row at 1304-1311 where the control sits)
    - .planning/phases/29-unified-composer-spine/29-UI-SPEC.md (§1 entry-mode control: placement, two segments, active=border-accent/text-accent NOT fill, role=radiogroup/tablist + aria, no-live-book default to Blank slate; §Color accent-reserved list; Pitfall 5 no silent wipe)
    - .planning/phases/29-unified-composer-spine/29-RESEARCH.md (Pattern 1 + Pitfall 5)
    - src/app/(dashboard)/allocations/hooks/useScenarioState.ts (reset + the draft shape; "Blank slate" = an empty working composition seeded from no holdings)
    - DESIGN.md (segmented-control / pill active treatment, accent rules)
  </read_first>
  <action>Add an entry-mode segmented control at the top of the composer body title row (above the composition list, near the "Scenario" H2 + PROJECTED pill). Two segments: "From my book" (default when `holdingsSummary.length > 0`) and "Blank slate" (default + only sensible option when no live book). Track the selected mode in component state. Visual per 29-UI-SPEC §1: on-surface segmented group, 1px `--color-border` container, `--radius-md`; active segment `border-accent text-accent` (NO accent fill — accent = action/verified); inactive `text-text-secondary`; mirror the existing FilterPill active treatment. A11y: `role="radiogroup"` + `role="radio"`/`aria-checked` (or tablist + aria-selected), arrow-key nav, visible accent focus ring. Behavior: "Blank slate" presents an empty working composition (no live-book holdings seeded); "From my book" seeds the live holdings as today. CRITICAL (Pitfall 5): a mode switch that would DISCARD a dirty draft (`scenario.diffCount > 0`) MUST open the existing reset confirmation (`setResetModalOpen(true)`) and only apply the mode change on confirm — route it through `handleReset`, NEVER call `scenario.reset()` / re-seed silently. When there is no live book, do NOT render a dead "From my book" default — default to Blank slate (and the empty-state card remains the blank-slate front door). The adapter/engine path is untouched; the mode only changes which initial draft renders.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <done>The control renders two accessible segments; switching with a dirty draft opens the reset confirmation (no silent wipe); no-live-book defaults to Blank slate; existing reset-modal tests (T_C12-14) still pass.</done>
  <acceptance_criteria>
    - `grep -cE "radiogroup|role=\"tablist\"" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 1
    - `grep -cE "From my book|Blank slate" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 2
    - a NEW test asserts: with `scenario.diffCount > 0`, clicking the inactive segment opens `ResetConfirmationModal` (the existing reset-modal testid/title) and does NOT immediately change the draft; on confirm the mode applies — and it FAILS if the onClick calls reset directly (non-vacuous, verify once)
    - existing T_C12 / T_C13 / T_C14 reset-modal tests stay green
    - the Example/PROJECTED pills and KPI strip remain the visual anchor (the control uses no accent fill — grep shows no new `bg-accent` on the segment)
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lazy-returns plumbing — added catalog strategy moves the projection</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (addedStrategyReturnsLookup 777-790; strategyById 772-775; adapter call 827-856; the add gesture wiring at 1280 + the non-empty drawer onAdd; the existing /api/benchmark/btc fetch effect 599-614 as the in-component client-fetch + honest-degrade pattern to mirror)
    - src/app/(dashboard)/allocations/lib/scenario-adapter.ts (buildStrategyForBuilderSet signature — the lookup flows through UNCHANGED)
    - .planning/phases/29-unified-composer-spine/29-RESEARCH.md (Implementation shape "Composer wiring" + Pitfall 4: book-only payload gap; feed lookup from BOTH payload.strategies AND the lazy map, payload wins; honest loading, never a fabricated series)
    - .planning/phases/29-unified-composer-spine/29-PATTERNS.md (the ScenarioComposer MOD section (2) — the exact merge shape `fromBook ?? addedReturnsById[a.id] ?? []`)
    - src/app/api/strategies/[id]/returns/route.ts (Plan 01 — the route this fetches: returns `{ daily_returns: DailyPoint[] }`)
  </read_first>
  <behavior>
    - Adding a catalog strategy (verified OR example) that is NOT in the book triggers a client `fetch("/api/strategies/<id>/returns")`; on resolve, its `daily_returns` lands in a client-side `addedReturnsById` map keyed by id.
    - `addedStrategyReturnsLookup` feeds each added strategy's series from BOTH `payload.strategies` (book strategies present) AND `addedReturnsById` — payload wins when present (Open Question #1).
    - Once the series resolves, the projection recomputes: the lookup passed to `buildStrategyForBuilderSet` contains the non-empty series for the added id (observable via the existing `vi.mocked(buildStrategyForBuilderSet)` capture), so `scenarioMetrics.n` increases / the curve changes.
    - While the fetch is in flight, the added strategy maps to `[]` (warm-up-gated out) AND an honest "loading returns…" affordance shows; NO fabricated flat/zero series is fed to the engine.
    - A failed/aborted lazy fetch leaves the entry `[]` + an honest signal (mirror the btc effect's degrade-not-crash posture); it does not wedge or fabricate.
  </behavior>
  <action>In `ScenarioComposer.tsx`: add a client-side `addedReturnsById` state map (`Record<string, DailyPoint[]>`) and a per-id loading set. On `addStrategyBrowse` (both the empty-state `onAdd` at 1280 and the non-empty drawer `onAdd`), after appending to the draft, if the id is not already in the book (`strategyById`) and not already fetched, fire `fetch("/api/strategies/" + id + "/returns")`, parse `{ daily_returns }`, and `setAddedReturnsById(prev => ({ ...prev, [id]: Array.isArray(daily_returns) ? daily_returns : [] }))`; use an AbortController/cancelled guard and mirror the btc-effect honest-degrade (non-ok / throw → leave `[]`, log, no crash, no fabricated series). Extend the `addedStrategyReturnsLookup` useMemo (777-790) to merge: `const fromBook = Array.isArray(raw) ? raw as DailyPoint[] : null; map[a.id] = fromBook ?? addedReturnsById[a.id] ?? []` and add `addedReturnsById` to its deps. Surface an honest "loading returns…" affordance on the added-strategy row while its id is in the loading set (NOT a flat curve). The adapter call, `projectionState`, `collapseAliasedHoldingStrategies`, `buildDateMapCache`, and `computeScenario` are UNCHANGED — the series flows through the frozen engine. Do NOT add a second annualization path; do NOT edit `scenario.ts`.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx"</automated>
  </verify>
  <done>An added catalog strategy lazy-fetches its series; once resolved the lookup passed to the adapter carries the non-empty series and the projection changes; in-flight shows an honest loading state with `[]` (no fabricated series); failure degrades honestly.</done>
  <acceptance_criteria>
    - `grep -cE "addedReturnsById" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 3 (state + merge + setter)
    - `grep -cE "strategies/.+/returns|\`/api/strategies/\$\{" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 1 (the lazy fetch URL)
    - `git diff --exit-code src/lib/scenario.ts` is clean (engine untouched)
    - a NEW test mocks `global.fetch` to resolve `{ daily_returns: [...nonEmpty] }`, simulates an add via the captured drawer `onAdd`, and asserts the lookup arg captured by `vi.mocked(buildStrategyForBuilderSet)` contains the non-empty series for the added id (and `[]` before the fetch resolves) — non-vacuous: stubbing the fetch to reject leaves `[]` and shows the loading/degrade state
    - `grep -ciE "loading returns|loading…|loading\.\.\." "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 1 (honest in-flight affordance) AND no `fill(0)`/fabricated-series added on the add path
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: "Portfolio" copy relabel + reopen-notice relabel (codec trichotomy preserved)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (save toolbar copy 1334/1348/1361, placeholder+aria 1374-1375; empty-state heading 1247 + body 1249-1252; reopen notices 552/564 inside openSavedScenario 539-584; the H2 "Scenario" at 1305 — code/UI decision below)
    - .planning/phases/29-unified-composer-spine/29-UI-SPEC.md (§Copywriting verbatim table: Save/Update/Save-as-new/Name placeholder/empty-state heading+body+CTAs/reopen notices)
    - src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx (which copy strings the save/reopen tests assert)
    - .planning/phases/29-unified-composer-spine/29-RESEARCH.md (Pitfall: reopen must keep scenarioDraftCodec — never a bare JSON.parse cast (M-0153))
  </read_first>
  <action>Relabel UI-facing copy per 29-UI-SPEC §Copywriting verbatim, WITHOUT touching code/route/state names or the codec logic: save toolbar "Save scenario"→"Save portfolio" (1334), "Update scenario"→"Update portfolio" (1348), "Save as new scenario"→"Save as new portfolio" (1361); name input placeholder + aria "Name this scenario"→"Name this portfolio" (1374-1375); empty-state heading "Scenario builder needs holdings"→"Start a portfolio" (1247) + body to the UI-SPEC "Connect a read-only exchange API key to project portfolio scenarios from your live book — or start from a blank slate and browse strategies to compose one." (keep the existing dual CTA: "Connect Exchange →" accent + "Browse strategies" border; the "Try the Strategy Sandbox →" /scenarios link STAYS — Phase 32 retires it, not this phase). In `openSavedScenario`, relabel ONLY the two notice strings: 552 → "This saved portfolio uses an older format and can't be reopened.", 564 → "This portfolio was saved by a newer version and is read-only here." Do NOT alter the decode trichotomy: `reset` still does NOT hydrate (no silent empty composer), `readonly` still hydrates real data + blocks Update, `ok` still hydrates + adopts id — and the codec stays `scenarioDraftCodec(...).decode(...)`, never a bare `JSON.parse(row.draft) as ScenarioDraft`. The H2 visible title may read "Scenario" still (it is the tab name) OR be left as-is; the decision is the noun on the SAVE/portfolio affordances per UI-SPEC — relabel those. Update ScenarioComposer.save.test.tsx in lockstep for any asserted copy + add an assertion that the `reset` outcome shows the relabeled notice AND does not hydrate.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx"</automated>
  </verify>
  <done>Save/Update/Save-as-new/name-input/empty-state/reopen-notice copy reads "portfolio"; the reopen codec trichotomy is byte-identical (reset does not hydrate); the save test passes with the relabeled copy and a reset-no-hydrate assertion.</done>
  <acceptance_criteria>
    - `grep -cE "Save portfolio|Update portfolio|Save as new portfolio|Name this portfolio|Start a portfolio|saved portfolio" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` ≥ 5
    - `grep -c "scenarioDraftCodec" "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"` unchanged vs baseline (codec preserved); `grep -c "row.draft as ScenarioDraft" ...` === 0 (no bare cast introduced)
    - `grep -c "Scenario builder needs holdings" ...` === 0 (empty-state relabeled)
    - the save test asserts the `reset` reopen outcome shows "older format" notice AND `hydrateFromSaved` was NOT called for that branch (trichotomy non-regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer (client) → /api/strategies/[id]/returns | client fetch of a published strategy's series; the route (Plan 01) enforces RLS + published + redaction |
| composer (client) → scenarioDraftCodec on reopen | a persisted JSONB draft (possibly drifted) crosses into the running draft — the trichotomy is the honesty guard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-12 | Tampering / Integrity | reopen decode | mitigate | reopen keeps `scenarioDraftCodec` — never a bare `JSON.parse as ScenarioDraft` (M-0153); `reset` outcome does NOT hydrate (no silent empty composer); test asserts reset-no-hydrate. |
| T-29-13 | Integrity (false data) | lazy-returns in-flight | mitigate | in-flight added strategy contributes `[]` (warm-up-gated) + honest "loading returns…" affordance; NO fabricated flat/zero series fed to the engine (Pitfall 4). |
| T-29-14 | Integrity (data wipe) | entry-mode switch | mitigate | a dirty-draft mode switch routes through the existing reset-confirmation modal — no silent draft wipe (Pitfall 5); test asserts the modal opens and the draft is not changed before confirm. |
| T-29-15 | Information Disclosure | client never trusts series | mitigate | the lazy fetch hits the RLS+published route (Plan 01); the composer does not bypass it or read analytics directly; no admin client in a client component (structurally impossible). |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new deps; any install in the diff is a red flag — block and escalate. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx"` green.
- `git diff --exit-code src/lib/scenario.ts` clean (frozen engine — SCENARIO-05).
- `git status --porcelain supabase/migrations/` empty.
- The reopen codec trichotomy is unchanged (codec call count preserved; reset-no-hydrate asserted).
</verification>

<success_criteria>
One composer surface with two entry modes (book / blank slate) routed through the reset discipline; an added catalog strategy lazy-fetches its series and the projection recomputes through the frozen engine; copy reads "portfolio" while the codec trichotomy + persistence stay unchanged. UNIFY-01/02/04/05 client side complete.
</success_criteria>

<output>
Create `.planning/phases/29-unified-composer-spine/29-04-SUMMARY.md` when done.
</output>
