---
phase: 29-unified-composer-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/[id]/returns/route.ts
  - src/app/api/strategies/[id]/returns/route.test.ts
autonomous: true
requirements: [UNIFY-04]
must_haves:
  truths:
    - "A new scoped GET route returns the daily_returns series for one published strategy id"
    - "The route reads via the RLS-scoped server client, never createAdminClient()"
    - "An unpublished / non-existent / cross-tenant id returns 404 (no existence leak)"
    - "A malformed (non-uuid) id returns 400 before auth/rate-limit burns a token"
    - "A DB error returns a static 500 envelope — the raw Postgres error never reaches the client"
  artifacts:
    - path: "src/app/api/strategies/[id]/returns/route.ts"
      provides: "Scoped lazy daily_returns supply for catalog adds (the H-0133 / example-add data gap fix)"
      contains: "withPublishedOnly"
    - path: "src/app/api/strategies/[id]/returns/route.test.ts"
      provides: "Non-vacuous coverage: 400/404/200/500-redaction/no-admin/429"
      contains: "createAdminClient"
  key_links:
    - from: "src/app/api/strategies/[id]/returns/route.ts"
      to: "strategy_analytics.daily_returns"
      via: "RLS createClient() select scoped by published-existence probe"
      pattern: "from\\(\"strategy_analytics\"\\)"
---

<objective>
Create the scoped lazy-returns route `GET /api/strategies/[id]/returns` that supplies a single published strategy's `daily_returns` series under the RLS-scoped client. This is the data-supply backbone for UNIFY-04: it closes the existing "no series in payload" gap for BOTH catalog halves (verified-add AND example-add) per 29-RESEARCH reason #2 — `payload.strategies` is book-only, so a browse-added strategy currently contributes `[]` and is warm-up-gated out of the projection.

Purpose: An added catalog strategy must move the projection. The series flows through the UNCHANGED adapter + frozen engine; this route is the only new server code the phase needs.
Output: One new route file + its non-vacuous test, fully owned by this plan (no overlap with any other Plan).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

AGENTS.md: this is NOT the Next.js in your training data. Before writing route-handler code, read `node_modules/next/dist/docs/` for the App Router route-handler + async-params API (Next 16.2.9). Default to `runtime = "nodejs"` (the supabase server client needs the cookie store).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-unified-composer-spine/29-RESEARCH.md
@.planning/phases/29-unified-composer-spine/29-PATTERNS.md

<interfaces>
<!-- Verified analog excerpts the executor builds against — no codebase exploration needed. -->

Auth wrapper — src/lib/api/withAllocatorAuth.ts (the wrapper calls handler with (req, user) ONLY; it does NOT forward route ctx):
```
export type AllocatorUser = User & { readonly __allocatorRoleVerified: true };
export function withAllocatorAuth(handler: (req: NextRequest, user: AllocatorUser) => Promise<NextResponse>): (req: NextRequest) => Promise<NextResponse>;
```

RLS read + redaction + headers analog — src/app/api/strategies/browse/route.ts:1-10,98-131 (imports, createClient(), withPublishedOnly, console.error + captureToSentry + static 500 envelope, NO_STORE_HEADERS).

Dynamic-[id] + isUuid-first + inner-wrapper analog — src/app/api/allocator/scenario/saved/[id]/route.ts:34-55,142-147 (RouteCtx = { params: Promise<{ id: string }> }; await ctx.params; isUuid → 400 BEFORE delegating to withAllocatorAuth(...)(req); B15 ordering).

Test-harness analog — src/app/api/strategies/browse/route.test.ts:26-149 (vi.mock("server-only"), vi.hoisted(STATE) with profileRole + from("profiles") arm for end-to-end withAllocatorAuth, observedFilters, strategiesQueried short-circuit flag, captureSpy for redaction). The strategies-table arm in that mock asserts the chained builder; mirror it for from("strategies") existence-probe + from("strategy_analytics").
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0 (Wave 0 scaffold): Write the failing route test</name>
  <files>src/app/api/strategies/[id]/returns/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/browse/route.test.ts (the exact STATE-driven supabase mock harness to copy: vi.mock("server-only"), vi.hoisted STATE, from("profiles") role arm, observedFilters, strategiesQueried short-circuit, captureSpy)
    - src/app/api/allocator/scenario/saved/[id]/route.ts:34-55,142-147 (the async-params + isUuid-first + inner-wrapper shape the route under test will use)
    - .planning/phases/29-unified-composer-spine/29-PATTERNS.md (the route.test.ts analog section + required cases)
  </read_first>
  <behavior>
    - 400 on a malformed (non-uuid) id, returned BEFORE auth and rate-limit run (assert the strategies/analytics query never fired — mirror the strategiesQueried short-circuit flag)
    - 403 when caller role is not allocator/both (gate runs before any DB query)
    - 404 when the published-existence probe finds no row (unpublished / non-existent / not-readable-under-RLS), with NO_STORE_HEADERS
    - 200 + `{ daily_returns: DailyPoint[] }` on a published strategy whose strategy_analytics row carries a daily_returns array
    - 200 + `{ daily_returns: [] }` when the analytics row is absent or daily_returns is non-array (honest empty, never fabricated)
    - 500 static envelope on a DB error: body.error is a fixed string (NOT the raw error.message), captureToSentry called with tags.route === "api/strategies/returns", Cache-Control private, no-store
    - 429 + Retry-After when checkLimit returns success:false, keyed per user
    - Non-vacuity: assert the route uses withPublishedOnly on the strategies probe (observe the .eq("status","published")) and that NO createAdminClient is imported/called (the mock only exposes the RLS createClient; an admin import would not resolve / would not be wired)
  </behavior>
  <action>Create `src/app/api/strategies/[id]/returns/route.test.ts`. Copy the `vi.hoisted(STATE)` + `vi.mock("@/lib/supabase/server")` chained-builder harness from `browse/route.test.ts` verbatim, then add a `from("strategy_analytics")` arm whose `.select("daily_returns").eq("strategy_id", id).maybeSingle()` resolves `STATE.analyticsRow` (and a `STATE.analyticsQueryError` arm for the 500 path), and a `from("strategies")` existence-probe arm whose `.select("id").eq("id", id).maybeSingle()` resolves `STATE.publishedExists ? { id } : null` while recording `STATE.observedFilters.status` (so the withPublishedOnly published predicate is observable). Keep `STATE.strategiesQueried` so the 400/403 short-circuit is provable. Keep `captureSpy`. Drive the request through `new NextRequest("http://localhost:3000/api/strategies/<id>/returns")` and invoke the exported `GET(req, { params: Promise.resolve({ id }) })`. The test MUST fail now (route file does not exist) — this is the RED step.</action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts" 2>&1 | grep -qE "Cannot find module|Failed to resolve|No test files|failed" && echo "RED-OK (test present, route absent)"</automated>
  </verify>
  <done>Test file exists with the 8 behavior cases above; running it fails because the route file is not yet created (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement the scoped lazy-returns route (GREEN)</name>
  <files>src/app/api/strategies/[id]/returns/route.ts</files>
  <read_first>
    - src/app/api/strategies/browse/route.ts:1-10,98-131 (import block + RLS createClient + withPublishedOnly + console.error/captureToSentry/static-500 + NO_STORE_HEADERS — copy posture verbatim)
    - src/app/api/allocator/scenario/saved/[id]/route.ts:34-55,142-147 (RouteCtx Promise params; await ctx.params; isUuid-first 400; inner withAllocatorAuth(...)(req) delegation; B15 rate-limit-after-validation ordering)
    - src/lib/utils.ts (isUuid)
    - src/lib/visibility.ts (withPublishedOnly)
    - src/lib/api/headers.ts (NO_STORE_HEADERS)
    - .planning/phases/29-unified-composer-spine/29-PATTERNS.md (the NEW-route section: exact import list, dynamic-[id] shape, RLS read + published existence-probe + redaction, rate-limit ordering, exit-gate guards)
  </read_first>
  <action>Create `src/app/api/strategies/[id]/returns/route.ts`. Set `export const runtime = "nodejs"`. Export an async `GET(req: NextRequest, ctx: RouteCtx)` where `RouteCtx = { params: Promise<{ id: string }> }`. Await `ctx.params`; if `!isUuid(id)` return 400 `{ error: "Invalid strategy id" }` + NO_STORE_HEADERS BEFORE any auth/rate-limit (so structurally-bad input never burns a token — per saved/[id] B15 ordering). Then delegate to `withAllocatorAuth(async (_req, user) => { ... })(req)` closing over `id`. Inside: rate-limit first via `checkLimit(userActionLimiter, \`returns:${user.id}\`)` → 429 + Retry-After + NO_STORE_HEADERS on failure (and 503 on isRateLimitMisconfigured if you mirror saved/[id]). Then `const supabase = await createClient()` (RLS — NEVER createAdminClient). Do a published-existence probe: `withPublishedOnly(supabase.from("strategies").select("id").eq("id", id)).maybeSingle()` → if no row, return 404 `{ error: "Not found" }` + NO_STORE_HEADERS (do not reveal existence; covers unpublished + cross-tenant + missing). Then `supabase.from("strategy_analytics").select("daily_returns").eq("strategy_id", id).maybeSingle()`; on error, console.error + `captureToSentry(error, { tags: { route: "api/strategies/returns" } })` + static 500 `{ error: "Failed to load returns" }` + NO_STORE_HEADERS (NEVER forward error.message). On success, `const daily_returns = Array.isArray(data?.daily_returns) ? data.daily_returns : []` and return 200 `{ daily_returns }` + NO_STORE_HEADERS. Scope is ONE id per call — `is_example` is NOT a separate gate; `status='published'` (via withPublishedOnly + RLS analytics_read) covers verified AND example published rows.</action>
  <verify>
    <automated>npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts"</automated>
  </verify>
  <done>All 8 test cases pass; the route imports `createClient` (not `createAdminClient`), calls `withPublishedOnly`, and reads one id per call.</done>
  <acceptance_criteria>
    - `npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts"` passes (all cases GREEN)
    - `grep -L createAdminClient "src/app/api/strategies/[id]/returns/route.ts"` lists the file (the token is ABSENT)
    - `grep -c "withPublishedOnly" "src/app/api/strategies/[id]/returns/route.ts"` ≥ 1
    - `grep -c 'runtime = "nodejs"' "src/app/api/strategies/[id]/returns/route.ts"` === 1
    - `grep -c 'route: "api/strategies/returns"' "src/app/api/strategies/[id]/returns/route.ts"` === 1 (redaction tag present)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allocator browser → GET /api/strategies/[id]/returns | untrusted `[id]` path param + untrusted caller identity crosses here |
| route → Supabase (RLS analytics_read) | RLS-scoped read of `strategy_analytics.daily_returns` for a published strategy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Information Disclosure | the new returns route | mitigate | RLS `createClient()` + `withPublishedOnly` existence probe; NEVER `createAdminClient()`. Non-vacuous test: unpublished/cross-tenant id → 404, never the series. |
| T-29-02 | Information Disclosure | DB error path | mitigate | console.error + captureToSentry + static 500 envelope; raw Postgres error.message never forwarded (test pins body.error is a fixed string). |
| T-29-03 | Denial of Service / enumeration | `[id]` param | mitigate | `isUuid` 400 before auth; one-id-per-call (never an unbounded pull); `withAllocatorAuth` + per-user `returns:${user.id}` rate-limit (B15 ordering). |
| T-29-04 | Elevation of Privilege | client choice | mitigate | only `createClient()` (RLS) imported; admin client structurally absent (grep-asserted in acceptance criteria). |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | This phase installs NOTHING (29-RESEARCH: zero new deps). If any `npm install <pkg>` appears in this plan's diff it is a red flag — block and escalate. No package legitimacy checkpoint needed because there are no installs. |
</threat_model>

<verification>
- `npx vitest run "src/app/api/strategies/[id]/returns/route.test.ts"` is green.
- `git diff --exit-code src/lib/scenario.ts` clean (this plan must not touch the frozen engine).
- `git status --porcelain supabase/migrations/` empty (no migration in this plan).
- `grep -rL createAdminClient "src/app/api/strategies/[id]/returns/route.ts"` confirms the admin client is absent.
</verification>

<success_criteria>
The lazy-returns route exists, is RLS-scoped + published-only, returns `{ daily_returns }` for a published id, 404s on unpublished/cross-tenant/missing, 400s on bad uuid before burning a token, redacts DB errors, and never uses the admin client. UNIFY-04 server side is complete and consumable by Plan 04.
</success_criteria>

<output>
Create `.planning/phases/29-unified-composer-spine/29-01-SUMMARY.md` when done.
</output>
