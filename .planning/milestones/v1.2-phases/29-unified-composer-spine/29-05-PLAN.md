---
phase: 29-unified-composer-spine
plan: 05
type: execute
wave: 3
depends_on: ["29-01", "29-02", "29-03", "29-04"]
files_modified:
  - src/__tests__/phase-29-frozen-spine-guards.test.ts
autonomous: true
requirements: [UNIFY-01, UNIFY-02, UNIFY-03, UNIFY-04, UNIFY-05]
must_haves:
  truths:
    - "No new migration touching scenarios / scenario_shares / get_shared_scenario / create_scenario_share ships in this phase (the LOCKED no-schema-change exit gate, enforced by an automated guard)"
    - "src/lib/scenario.ts has zero diff vs the phase baseline (frozen engine — SCENARIO-05)"
    - "test_scenarios_rls.sql + test_scenario_shares_rls.sql + the share-RPC body-shape DO-block are unchanged"
    - "The full vitest suite passes after all phase edits land"
  artifacts:
    - path: "src/__tests__/phase-29-frozen-spine-guards.test.ts"
      provides: "Automated exit-gate guards: no scenarios/share migration added, scenario.ts unmodified, RLS sql files untouched"
      contains: "scenario"
  key_links:
    - from: "src/__tests__/phase-29-frozen-spine-guards.test.ts"
      to: "git diff / git ls-files"
      via: "execSync diff vs the phase baseline ref to enumerate added/changed files"
      pattern: "execSync|git diff|ls-files"
---

<objective>
Add the automated phase exit-gate guards and run the consolidation gate. The three LOCKED silent-failure guards for Phase 29 are: (1) NO migration touching `scenarios` / `scenario_shares` / `get_shared_scenario` / `create_scenario_share` ships; (2) `src/lib/scenario.ts` has zero diff (frozen engine, SCENARIO-05); (3) the RLS sql tests (`test_scenarios_rls.sql`, `test_scenario_shares_rls.sql`) and the share-RPC body-shape DO-block stay green/untouched. This plan turns those into a single non-vacuous vitest guard and runs the full-suite + sql gate.

Purpose: these failures ship GREEN unless a content/diff-inspecting test catches them (the milestone's defining risk). The guard makes "no schema change + frozen engine" enforceable in CI, not a hope.
Output: one new guard test file + a recorded full-suite + sql-suite gate run in the SUMMARY. Owns only the new test file (no overlap with Plans 01-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

This plan runs LAST (Wave 3) after Plans 01-04 have landed their edits, so the git-diff guard sees the full phase delta. The supabase `.sql` RLS tests are pgTAP-style files run against a DB (no local vitest runner) — verify them via the project's sql-suite path; the vitest guard enforces the "files untouched" half automatically.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-unified-composer-spine/29-RESEARCH.md
@.planning/ROADMAP.md

<interfaces>
git-diff guard precedent — src/__tests__/strategy-sources-migration-parity.test.ts (uses `resolve(process.cwd(), "supabase/migrations")` + reads the migrations dir; the project already runs execSync/git-driven guard tests in src/__tests__).

Frozen set (must NOT gain a sibling this phase): the scenarios/share migrations are `supabase/migrations/20260621120000_scenarios_table_and_rls.sql` + `20260622120000_scenario_shares_and_read_rpc.sql`. Any NEW migration filename matching `/scenario|share|get_shared_scenario|create_scenario_share/i` added in this phase is the red-flag the guard fails on.

RLS sql test files (must be byte-unchanged this phase): `supabase/tests/test_scenarios_rls.sql`, `supabase/tests/test_scenario_shares_rls.sql`.

Frozen engine — `src/lib/scenario.ts` (SCENARIO-05; `src/lib/scenario.test.ts` has the 252-annualization pins, 11 matches). Zero diff required.

Phase baseline ref: the merge-base with origin/main (`a759022c…` at planning time) — or the branch point. Compute it in-test via `git merge-base origin/main HEAD` with a fallback to a configured base, so the guard works in CI and locally.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the frozen-spine exit-gate guard test</name>
  <files>src/__tests__/phase-29-frozen-spine-guards.test.ts</files>
  <read_first>
    - src/__tests__/strategy-sources-migration-parity.test.ts (the execSync/git + migrations-dir read precedent to mirror for cwd + path resolution)
    - supabase/tests/test_scenarios_rls.sql + supabase/tests/test_scenario_shares_rls.sql (the RLS files the guard asserts are unchanged — read their headers to pin the assertion target)
    - .planning/ROADMAP.md (Phase 29 Exit Gates — the exact forbidden migration name set)
    - .planning/phases/29-unified-composer-spine/29-RESEARCH.md (Pitfall 3 — scenarios schema / share-RPC must not be touched; Validation Architecture exit-gate rows)
  </read_first>
  <behavior>
    - Enumerate files added/changed vs the phase baseline (git merge-base origin/main HEAD; fallback to a constant base sha if origin/main is absent). Assert NO added file under `supabase/migrations/` whose name matches `/scenario|share/i` (the forbidden set: scenarios / scenario_shares / get_shared_scenario / create_scenario_share). A stray new scenarios/share migration FAILS the test.
    - Assert `src/lib/scenario.ts` is NOT in the changed-files set (zero diff vs baseline — frozen engine). If it appears, FAIL with a message pointing at SCENARIO-05.
    - Assert `supabase/tests/test_scenarios_rls.sql` and `supabase/tests/test_scenario_shares_rls.sql` are NOT in the changed-files set (RLS guards untouched).
    - Non-vacuity: the guard reads the REAL git delta (not a hardcoded empty list) — verify once by temporarily `touch`-ing a `supabase/migrations/29_test_scenario_dummy.sql` and confirming the test FAILS, then remove it.
    - Robustness: if the git base cannot be resolved at all, the test FAILS LOUD (never silently passes — project Rule 12) with an actionable message, rather than skipping.
  </behavior>
  <action>Create `src/__tests__/phase-29-frozen-spine-guards.test.ts`. Use `execSync` (child_process) with `cwd: process.cwd()`. Resolve the baseline: try `git merge-base origin/main HEAD`; if that errors, fall back to a documented constant base sha (the phase branch point) — and if BOTH fail, the test throws a loud error (do not skip). Build the changed/added file set from `git diff --name-only <base> HEAD` PLUS `git ls-files --others --exclude-standard` (untracked, since .planning is gitignored but src/supabase files are tracked — uncommitted new migrations may be untracked). Three assertions: (a) `.filter(f => f.startsWith("supabase/migrations/") && /scenario|share/i.test(f))` is empty; (b) the set does not include `src/lib/scenario.ts`; (c) the set includes neither RLS sql file. Each `expect(...).toEqual([])` / `.not.toContain(...)` carries a message naming the exit gate it enforces (no-schema-change / frozen-engine SCENARIO-05 / RLS-untouched). Keep it deterministic and fast (<2s).</action>
  <verify>
    <automated>npx vitest run src/__tests__/phase-29-frozen-spine-guards.test.ts</automated>
  </verify>
  <done>The guard passes against the real phase delta (Plans 01-04 touched no scenarios/share migration, no scenario.ts, no RLS sql file); a temporary dummy scenarios migration makes it fail (verified once, then removed); an unresolvable base fails loud.</done>
  <acceptance_criteria>
    - `npx vitest run src/__tests__/phase-29-frozen-spine-guards.test.ts` passes
    - `grep -cE "execSync|git diff|ls-files" src/__tests__/phase-29-frozen-spine-guards.test.ts` ≥ 1
    - `grep -ciE "SCENARIO-05|frozen" src/__tests__/phase-29-frozen-spine-guards.test.ts` ≥ 1
    - `grep -ciE "scenario|share" src/__tests__/phase-29-frozen-spine-guards.test.ts` ≥ 3 (forbidden-set patterns + RLS files)
    - non-vacuity verified: `touch supabase/migrations/29_test_scenario_dummy.sql && npx vitest run src/__tests__/phase-29-frozen-spine-guards.test.ts` FAILS, then `rm` it (record in SUMMARY)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run the phase consolidation gate (full vitest + frozen-engine diff + RLS sql)</name>
  <files>(no source files — verification/gate step; runs npm test + git diff checks)</files>
  <read_first>
    - package.json (the `test` script = `vitest run`; `test:coverage` is the blocking CI gate)
    - src/lib/scenario.test.ts (SCENARIO-05 pins — must stay green)
    - .planning/phases/29-unified-composer-spine/29-RESEARCH.md (Validation Architecture → Phase gate: full suite + git diff clean on scenario.ts + migrations)
    - .planning/ROADMAP.md (Phase 29 Exit Gates — the sql tests + share-RPC DO-block)
  </read_first>
  <action>Run the phase gate and record results in the SUMMARY (this task creates no source files — it is the consolidation/verification step): (1) `npm test` (full vitest) — must be green; capture the pass count. (2) `npx vitest run src/lib/scenario.test.ts` — SCENARIO-05 pins green. (3) `git diff --exit-code src/lib/scenario.ts` — clean (frozen engine). (4) `git status --porcelain supabase/migrations/` — empty (no migration added). (5) The RLS sql suite (`test_scenarios_rls.sql` + `test_scenario_shares_rls.sql`) — run via the project's sql-test path against the test DB; if no local runner is available in this environment, record the command + that the vitest guard (Task 1) already proves the files are byte-unchanged, and flag the live sql run as the CI/verify-work responsibility. If ANY check fails, do NOT mark the phase done — surface the failure loudly (project Rule 12) and route the gap back into the relevant plan. Note: this task may legitimately FAIL if a sibling plan regressed; that is the gate doing its job.</action>
  <verify>
    <automated>npm test 2>&1 | tail -20</automated>
  </verify>
  <done>Full vitest suite green; `git diff --exit-code src/lib/scenario.ts` clean; `git status --porcelain supabase/migrations/` empty; SCENARIO-05 pins green; the RLS sql run (or its CI deferral with the byte-unchanged guard backing it) recorded in the SUMMARY.</done>
  <acceptance_criteria>
    - `npm test` exits 0 (full suite green)
    - `git diff --exit-code src/lib/scenario.ts` exits 0
    - `git status --porcelain supabase/migrations/` prints nothing
    - `npx vitest run src/lib/scenario.test.ts` passes (SCENARIO-05)
    - the SUMMARY records the full-suite pass count + the RLS sql verification status (run-green or CI-deferred-with-guard)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phase delta → CI guard | the aggregate file delta of Plans 01-04 is inspected here; the boundary is "did any locked-frozen file change" |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-16 | Tampering (silent schema change) | scenarios/share migrations | mitigate | automated guard fails on any added `supabase/migrations/*scenario*|*share*` file; non-vacuity proven by a temporary dummy migration. |
| T-29-17 | Tampering (frozen-engine drift) | src/lib/scenario.ts | mitigate | guard + `git diff --exit-code` assert zero diff; SCENARIO-05 pins re-run green. |
| T-29-18 | Repudiation (vacuous guard) | the guard test itself | mitigate | the guard reads the REAL git delta (execSync), not a hardcoded list; fails loud if the base is unresolvable (no silent skip — Rule 12). |
| T-29-19 | Information Disclosure (RLS regression) | RLS sql tests | mitigate | guard asserts the two `*_rls.sql` files are byte-unchanged; the live sql suite is run/CI-deferred and recorded. |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new deps; a phase-wide check — if `git diff package.json` shows a new dependency this gate flags it. |
</threat_model>

<verification>
- `npx vitest run src/__tests__/phase-29-frozen-spine-guards.test.ts` green + non-vacuity proven.
- `npm test` green (full suite).
- `git diff --exit-code src/lib/scenario.ts` clean; `git status --porcelain supabase/migrations/` empty.
- `npx vitest run src/lib/scenario.test.ts` green (SCENARIO-05).
</verification>

<success_criteria>
The three LOCKED exit gates (no scenarios/share migration, frozen engine zero-diff, RLS sql untouched) are enforced by an automated non-vacuous guard, and the full vitest suite + frozen-engine diff + SCENARIO-05 pins are green. The phase cannot ship a silent schema change, an engine drift, or an RLS regression undetected.
</success_criteria>

<output>
Create `.planning/phases/29-unified-composer-spine/29-05-SUMMARY.md` when done.
</output>
