---
phase: 29-unified-composer-spine
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx
  - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx
  - src/app/(dashboard)/allocations/components/SavedScenariosList.tsx
  - src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx
autonomous: true
requirements: [UNIFY-03, UNIFY-05]
must_haves:
  truths:
    - "Example-universe rows in the Browse drawer carry a neutral-outline 'Example' tag (never accent, never a filled Badge)"
    - "The drawer title reads 'Browse strategies' (no longer 'verified') and the empty-state copy drops 'verified'"
    - "The saved-list surfaces the noun 'portfolio' in UI copy while persisting to scenarios (routes unchanged)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"
      provides: "Merged-catalog UI: is_example on the row shape + the Example tag + 'Browse strategies' relabel"
      contains: "is_example"
    - path: "src/app/(dashboard)/allocations/components/SavedScenariosList.tsx"
      provides: "'Saved portfolios' / 'Save portfolio' copy over the unchanged saved-scenario CRUD"
      contains: "portfolio"
  key_links:
    - from: "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"
      to: "StrategyBrowseRow.is_example"
      via: "row.is_example gates the Example pill"
      pattern: "is_example"
---

<objective>
Wire the merged-catalog UI and the "portfolio" copy relabel — the two catalog/list component surfaces that are independent of the route work and the composer. The Browse drawer renders example-universe rows with a neutral-outline "Example" tag and relabels its title to "Browse strategies"; `SavedScenariosList` surfaces the noun "portfolio" in copy while the underlying scenario CRUD routes stay unchanged.

Purpose: UNIFY-03 UI (example rows clearly tagged in one catalog) + UNIFY-05 copy (named "portfolio" over the existing scenarios persistence). No schema, no route, no logic change — pure UI tag + copy.
Output: Drawer `is_example` row field + Example pill + title relabel; saved-list copy relabel; synchronized test updates. No overlap with Plans 01/02/04 files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

DESIGN.md is the LOCKED visual contract — read it before any UI change. The "Example" tag and all copy below are pinned in 29-UI-SPEC.md (the design checker-verified contract). Do NOT invent tokens, colors, or layouts. React: see react.dev for hooks/memo semantics if touching the filter memo.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-unified-composer-spine/29-UI-SPEC.md
@.planning/phases/29-unified-composer-spine/29-PATTERNS.md

<interfaces>
Drawer row type — StrategyBrowseDrawer.tsx:43-49 `StrategyBrowseRow { id, name, codename, markets, strategy_types }`. Add `is_example?: boolean`. The default fetcher maps `/api/strategies/browse` rows (Plan 02 now emits `is_example`); the `fetchStrategies?` test override stays.

Example pill recipe (VERBATIM, neutral-outline) — ScenarioBuilder.tsx:288 / the composer's PROJECTED pill at ScenarioComposer.tsx:1308:
`inline-flex items-center rounded-sm border border-text-muted px-2 py-0.5 text-[10px] uppercase tracking-wide font-semibold text-text-muted` → label "Example". NOT accent, NOT a filled <Badge> (accent = verified/action; Example = provenance metadata).

Drawer copy to relabel — `aria-label="Browse verified strategies"` (line 373), heading "Browse verified strategies" (392), empty-state "No verified strategies are live yet." (452). Drawer test asserts these verbatim (test lines 120, 291-295, 423-427).

Saved-list copy to relabel — heading "Saved scenarios" (376-381), empty-state heading "No saved scenarios yet" (397) + body "Save scenario"/"Saved scenarios reopen…" (399-400), rename aria-label `Rename scenario ${row.name}` (449), validation "Enter a name to save this scenario." (101), mutation errors "Couldn't rename/delete this scenario." (322/332/346/358). Saved-list test asserts "No saved scenarios yet" (75), `/^Rename$/` button (97/123), `/rename scenario/i` label (126/149).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drawer — add is_example to the row shape, render the Example tag, relabel the title</name>
  <files>src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx, src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx (StrategyBrowseRow at 43-49; the default fetcher at ~158; the filtered memo at 264-289; the row label render at ~486-493; the add handler at 317-346; the title/aria at 373/392; empty-state at 452)
    - src/components/scenarios/ScenarioBuilder.tsx:281-294 (the verbatim neutral-outline pill recipe)
    - .planning/phases/29-unified-composer-spine/29-UI-SPEC.md (§2 Example tag, §3 merged drawer, §Copywriting drawer rows)
    - src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx (the copy assertions at 120, 291-295, 423-427 that must move in lockstep)
    - DESIGN.md (badge/pill rules + accent-is-rare)
  </read_first>
  <action>In `StrategyBrowseDrawer.tsx`: add `is_example?: boolean` to `StrategyBrowseRow`; the default fetcher already maps browse rows — pass `is_example` through (Plan 02 emits it). The client-side `filtered` memo searches/filters across all rows already, so example rows interleave alphabetically with no logic change. In the row label block (next to `s.name`), render the Example pill gated on `s.is_example === true`, using the VERBATIM neutral-outline recipe from ScenarioBuilder.tsx:288 (`border-text-muted ... text-text-muted`, label "Example") — NOT accent, NOT a filled Badge. Relabel: `aria-label="Browse verified strategies"` → `"Browse strategies"` (line 373); heading "Browse verified strategies" → "Browse strategies" (392); empty-state "No verified strategies are live yet." → "No strategies are live yet." (452). The add gesture (`handleAdd` → `onAdd`) and the fetch lifecycle (AbortController / H-0117 loud-fail / H-0082b close-reset) stay UNCHANGED.</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx"</automated>
  </verify>
  <done>Drawer renders the Example pill on example rows, the title/aria/empty copy drop "verified", the add + fetch lifecycle are untouched, and the test passes with the relabeled strings.</done>
  <acceptance_criteria>
    - `grep -c "is_example" "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"` ≥ 2 (type field + render gate)
    - `grep -c "border-text-muted" "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"` ≥ 1 (neutral-outline pill, not accent)
    - `grep -c "bg-accent" "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"` does NOT increase for the Example tag (the pill uses no accent fill)
    - `grep -c "Browse verified strategies" "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.tsx"` === 0 (relabeled)
    - drawer test asserts the new "Browse strategies" / "No strategies are live yet." copy (updated in lockstep) and a new case asserts the Example pill renders only for `is_example` rows
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: SavedScenariosList — relabel copy to "portfolio" (routes + logic unchanged)</name>
  <files>src/app/(dashboard)/allocations/components/SavedScenariosList.tsx, src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/SavedScenariosList.tsx (heading 376-381; empty-state 397-400; rename aria 449; validation 101; mutation errors 322/332/346/358; the unchanged fetch/PATCH/DELETE routes at 316/342)
    - src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx (assertions at 75 "No saved scenarios yet", 97/123 `/^Rename$/`, 126/149 `/rename scenario/i`)
    - .planning/phases/29-unified-composer-spine/29-UI-SPEC.md (§Copywriting saved-list rows verbatim)
  </read_first>
  <action>In `SavedScenariosList.tsx`, relabel UI copy per 29-UI-SPEC §Copywriting verbatim: heading "Saved scenarios" → "Saved portfolios"; empty-state heading "No saved scenarios yet" → "No saved portfolios yet"; empty-state body to the UI-SPEC "Compose a draft above, then choose \"Save portfolio\"…" string; rename aria-label `Rename scenario ${row.name}` → `Rename portfolio ${row.name}`; validation "Enter a name to save this scenario." → "Enter a name to save this portfolio."; mutation errors "Couldn't rename/delete this scenario." → "…this portfolio."; list-load error "Couldn't load your saved scenarios." → "…saved portfolios." Do NOT change the fetch URLs (`/api/allocator/scenario/saved/${row.id}` PATCH/DELETE stay), the route names, the `onOpen` codec-trichotomy delegation, the Share affordance, or the "Compare selected" gate. The `aria-labelledby="saved-scenarios-heading"` id may stay (code id) or move to "saved-portfolios-heading" — keep it internally consistent with its `id=`. Update `SavedScenariosList.test.tsx` in lockstep: "No saved portfolios yet", `/rename portfolio/i` label matcher (and keep the `/^Rename$/` button matcher — the button text "Rename" is unchanged).</action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx"</automated>
  </verify>
  <done>The saved-list UI reads "portfolio"; the fetch routes / codec-open / share / compare logic are byte-identical; the test passes with the relabeled copy.</done>
  <acceptance_criteria>
    - `grep -c "portfolio" "src/app/(dashboard)/allocations/components/SavedScenariosList.tsx"` ≥ 6 (heading + empty + body + rename aria + validation + errors)
    - `grep -c "/api/allocator/scenario/saved" "src/app/(dashboard)/allocations/components/SavedScenariosList.tsx"` unchanged vs baseline (routes untouched)
    - `grep -c "No saved scenarios yet" "src/app/(dashboard)/allocations/components/SavedScenariosList.tsx"` === 0 (relabeled)
    - saved-list test green with "No saved portfolios yet" + `/rename portfolio/i`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browse response → drawer render | the `is_example` flag + `displayStrategyName` label cross into the UI; the pill must not reintroduce a raw-name leak |
| saved-list → scenario CRUD routes | unchanged PATCH/DELETE under RLS scenarios_owner (no boundary change this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-09 | Information Disclosure | Example tag rendering | mitigate | the row renders `s.name` (already the `displayStrategyName` pseudonymity-safe label from Plan 02) — the tag adds no raw-name field; pseudonymity is enforced server-side (Plan 02 leak test) and the drawer never receives the raw name. |
| T-29-10 | Tampering | saved-list copy relabel | accept | copy-only change; the fetch URLs + codec-open + RLS scope are unchanged (grep-asserted). No new write surface, no boundary moved. |
| T-29-11 | Spoofing | accent misuse on Example tag | mitigate | neutral-outline pill (grep: no `bg-accent` increase) — accent stays reserved for verified/action so an example strategy is never visually implied "verified". |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new deps; any install in the diff is a red flag — block and escalate. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/components/StrategyBrowseDrawer.test.tsx" "src/app/(dashboard)/allocations/components/SavedScenariosList.test.tsx"` green.
- `git diff --exit-code src/lib/scenario.ts` clean.
- `git status --porcelain supabase/migrations/` empty.
- No new `/api/allocator/scenario/saved` route added; URLs unchanged.
</verification>

<success_criteria>
Example rows are tagged with the neutral-outline "Example" pill in one merged Browse drawer titled "Browse strategies"; the saved list reads "portfolio" while persisting to the unchanged scenarios CRUD. UNIFY-03 UI tag + UNIFY-05 copy land; the composer (Plan 04) consumes the drawer's `is_example`-aware rows.
</success_criteria>

<output>
Create `.planning/phases/29-unified-composer-spine/29-03-SUMMARY.md` when done.
</output>
