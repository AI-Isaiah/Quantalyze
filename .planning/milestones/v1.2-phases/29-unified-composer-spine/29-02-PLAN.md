---
phase: 29-unified-composer-spine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/browse/route.ts
  - src/app/api/strategies/browse/route.test.ts
autonomous: true
requirements: [UNIFY-03]
must_haves:
  truths:
    - "The browse response also surfaces is_example=true AND status='published' rows, in the same RLS-scoped response as verified strategies"
    - "Each response row carries is_example so the drawer can tag example rows"
    - "An unpublished AND a cross-tenant strategy do NOT appear in the response even with is_example present"
    - "Example rows carry the pseudonymity-safe label via displayStrategyName — never the raw strategies.name"
  artifacts:
    - path: "src/app/api/strategies/browse/route.ts"
      provides: "Merged catalog metadata (verified + example) under the locked RLS + withPublishedOnly + displayStrategyName contract"
      contains: "is_example"
    - path: "src/app/api/strategies/browse/route.test.ts"
      provides: "Non-vacuous leak guard: unpublished + cross-tenant ABSENT with is_example included; example-row pseudonymity"
      contains: "is_example"
  key_links:
    - from: "src/app/api/strategies/browse/route.ts"
      to: "withPublishedOnly"
      via: "RLS-scoped strategies SELECT (is_example co-fetched, NOT a published-bypassing .or)"
      pattern: "withPublishedOnly\\("
---

<objective>
Extend `GET /api/strategies/browse` to ALSO surface `is_example=true AND status='published'` rows in the same response as verified strategies, tagging each row with `is_example` — under the SAME locked RLS-scoped client + `withPublishedOnly` + `displayStrategyName` contract. This is UNIFY-03's server half and a LOCKED exit gate: `is_example` is a co-fetched column to tag rows, NOT a reason to bypass the published predicate or the pseudonymity guard.

Purpose: One Browse drawer lists both catalog halves. The merge must NOT widen the leak surface — an unpublished or cross-tenant strategy must stay absent even with `is_example` in play, and example rows must carry the pseudonymity-safe label.
Output: An extended SELECT + an extended `BrowseStrategyRow` (adds `is_example`) + a non-vacuous leak/pseudonymity test. No overlap with any other Plan's files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md

AGENTS.md: read `node_modules/next/dist/docs/` route-handler guide before editing this route. This route returns metadata ONLY — never `daily_returns` (the heavy series is the lazy route in Plan 01; bloating browse with series is an anti-pattern per 29-RESEARCH Alternatives Considered).
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/29-unified-composer-spine/29-RESEARCH.md
@.planning/phases/29-unified-composer-spine/29-PATTERNS.md

<interfaces>
Current SELECT (route.ts:109-119) co-fetches `id, name, codename, disclosure_tier, markets, strategy_types`; `withPublishedOnly(...)` wraps it; `.order("name").limit(STRATEGY_BROWSE_LIMIT + 1)`. The projection (route.ts:157-182) maps an EXPLICIT allow-list (H-0300 fence — do NOT switch to `{...row}`) and runs `name` through `displayStrategyName({ id, name, codename, disclosure_tier })`.

`BrowseStrategyRow` (route.ts:39-54) is the exported wire type: `{ id, name, codename, markets, strategy_types }`. Add `is_example: boolean`.

Test harness (route.test.ts): `STATE.strategyRows` feeds the chained builder; `STATE.observedFilters.status` records the published predicate; `STATE.observedFilters.selectCols` records the SELECT list; T12a-e pin `displayStrategyName` (whole-payload `JSON.stringify(body)).not.toContain(rawName)` sweep); H-0300a/b pin the allow-list / forbidden-key fence.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the browse SELECT + row shape to co-fetch and emit is_example</name>
  <files>src/app/api/strategies/browse/route.ts</files>
  <read_first>
    - src/app/api/strategies/browse/route.ts (full file — the SELECT at 109-119, the BrowseStrategyRow interface at 39-54, the allow-list projection at 157-182, the displayStrategyName call at 167-172)
    - src/lib/strategy-display.ts (displayStrategyName — must run on example rows too)
    - src/lib/visibility.ts (withPublishedOnly — the published predicate that must NOT be bypassed)
    - .planning/phases/29-unified-composer-spine/29-PATTERNS.md (the browse/route.ts MOD section: add is_example to SELECT, to the allow-list as a NAMED key, NOT a .or that bypasses published)
  </read_first>
  <action>In `src/app/api/strategies/browse/route.ts`: add `is_example` to the `.select(...)` column list (append `, is_example` — keep the existing columns and `withPublishedOnly` wrapper unchanged; do NOT add a `.or(is_example.eq.true)` or any predicate that would bypass the published filter — example rows are just published rows that ALSO carry the flag). Add `is_example: boolean` to the exported `BrowseStrategyRow` interface. In the projection map, read `r.is_example` from the typed row cast and emit `is_example: r.is_example === true` as a NAMED key in the returned object (preserve the explicit allow-list — H-0300 fence; do NOT spread `...row`). `name` still runs through `displayStrategyName(...)` for example rows exactly as for verified rows — the tag must not reintroduce a raw-name leak. The response stays metadata-only (no `daily_returns`).</action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/browse/route.test.ts</automated>
  </verify>
  <done>Existing browse tests still pass; the SELECT includes `is_example`; the projection emits `is_example` as a boolean named key; no published-bypassing `.or` added.</done>
  <acceptance_criteria>
    - `grep -c "is_example" src/app/api/strategies/browse/route.ts` ≥ 3 (interface field + SELECT + projection)
    - `grep -c "withPublishedOnly" src/app/api/strategies/browse/route.ts` ≥ 1 (unchanged published guard)
    - `grep -c "createAdminClient" src/app/api/strategies/browse/route.ts` === 0
    - `grep -vE '^\s*(//|\*|/\*)' src/app/api/strategies/browse/route.ts | grep -c "\.or(" ` === 0 (no published-bypassing or-filter in code)
    - existing T12a-e + H-0300a/b still green
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the non-vacuous merged-catalog leak + pseudonymity test</name>
  <files>src/app/api/strategies/browse/route.test.ts</files>
  <read_first>
    - src/app/api/strategies/browse/route.test.ts (full file — the STATE-driven mock at 29-149; T3 status-filter observation; T12a-e pseudonymity cases at 469-584 incl. the JSON.stringify whole-payload sweep; T12e SELECT-shape assertion at 572-584; H-0300a/b allow-list fence at 598-650)
    - .planning/phases/29-unified-composer-spine/29-PATTERNS.md (the browse/route.test.ts MOD section + Pitfall 2: the test must assert ABSENCE with is_example included, plus example-row pseudonymity, with a positive control)
    - .planning/phases/29-unified-composer-spine/29-RESEARCH.md (Pitfall 2 — vacuous-test failure mode)
  </read_first>
  <behavior>
    - With `STATE.strategyRows` containing a published example row (is_example:true, exploratory tier, real name "Renaissance Medallion", codename "Sigma-7"), a published verified row, AND source rows that the mock represents as unpublished / cross-tenant: the response includes ONLY the rows withPublishedOnly admits, and the unpublished/cross-tenant names appear NOWHERE in `JSON.stringify(body)` (whole-payload sweep, mirroring T12a) — proving the merge did not widen the leak surface even with is_example present.
    - The example row's response `name` === its pseudonymity-safe label ("Sigma-7" — codename), NOT the raw "Renaissance Medallion"; `JSON.stringify(body)).not.toContain("Renaissance")`.
    - The example row carries `is_example === true` in the response; the verified row carries `is_example === false` (the tag is wired and discriminating).
    - SELECT-shape pin (extend T12e): `STATE.observedFilters.selectCols` contains `is_example`.
    - Positive control (non-vacuity): the example row's REAL name IS present in the source `STATE.strategyRows` but ABSENT from the response — proving the assertion can fail if displayStrategyName is bypassed.
  </behavior>
  <action>Extend `src/app/api/strategies/browse/route.test.ts`. The route reads RLS-scoped data, so model "unpublished / cross-tenant absent" the way the existing harness models RLS: rows the route would not receive are simply NOT in `STATE.strategyRows` after the published predicate — BUT to make the leak test non-vacuous, seed `STATE.strategyRows` with the published example + published verified rows AND assert (a) the response set equals exactly the published rows, (b) the whole-payload `JSON.stringify(body)` does not contain the raw example name, (c) `is_example` is true on the example row and false on the verified row, and (d) extend the T12e SELECT-shape assertion to require `is_example` in `observedFilters.selectCols`. Add the positive control: the raw example name string is in the seeded source row's `name` field but absent from `JSON.stringify(body)`. Mirror T12a's assertion style verbatim. Do NOT weaken any existing T12 / H-0300 case.</action>
  <verify>
    <automated>npx vitest run src/app/api/strategies/browse/route.test.ts</automated>
  </verify>
  <done>The new case(s) pass; the whole-payload sweep proves the raw example name never reaches the wire; `is_example` is asserted true/false on the two row classes; T12e requires `is_example` in the SELECT; the positive control proves non-vacuity.</done>
  <acceptance_criteria>
    - `npx vitest run src/app/api/strategies/browse/route.test.ts` passes
    - the new test body contains a `JSON.stringify(body)).not.toContain(` assertion against the raw example name (grep: `grep -c "not.toContain" src/app/api/strategies/browse/route.test.ts` increased vs baseline)
    - the new test asserts both `is_example` true (example row) and false (verified row)
    - `observedFilters.selectCols` assertion includes `is_example`
    - Temporarily emitting the raw `name` (skipping displayStrategyName) makes the new case FAIL (executor verifies the non-vacuity once, then reverts) — note this in the SUMMARY
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| allocator browser → GET /api/strategies/browse | untrusted caller identity; the merged catalog response crosses back |
| route → Supabase (RLS strategies_read) | RLS-scoped read; `withPublishedOnly` defense-in-depth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-05 | Information Disclosure | catalog merge (is_example) | mitigate | `is_example` is co-fetched, NOT a `.or` that bypasses `withPublishedOnly`; non-vacuous test asserts unpublished/cross-tenant ABSENT with is_example present + positive control. |
| T-29-06 | Information Disclosure | pseudonymity on example rows | mitigate | `displayStrategyName` runs on example rows too; whole-payload sweep asserts the raw name never reaches the wire. |
| T-29-07 | Information Disclosure | response key leak | mitigate | explicit allow-list projection preserved (H-0300 fence); `is_example` added as a NAMED key, not a `{...row}` spread (existing H-0300a/b stay green). |
| T-29-08 | Elevation of Privilege | client choice | mitigate | `createClient()` (RLS) preserved; `createAdminClient` count asserted 0. |
| T-29-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new deps this phase; any install in the diff is a red flag — block and escalate. |
</threat_model>

<verification>
- `npx vitest run src/app/api/strategies/browse/route.test.ts` green (existing + new cases).
- `git diff --exit-code src/lib/scenario.ts` clean.
- `git status --porcelain supabase/migrations/` empty.
- `grep -c createAdminClient src/app/api/strategies/browse/route.ts` === 0.
</verification>

<success_criteria>
Browse returns merged verified + example metadata under the locked RLS + withPublishedOnly + displayStrategyName contract; example rows carry `is_example` and the pseudonymity-safe label; a non-vacuous test proves unpublished/cross-tenant rows stay absent with is_example in play. UNIFY-03 server half complete and consumable by Plans 03 + 04.
</success_criteria>

<output>
Create `.planning/phases/29-unified-composer-spine/29-02-SUMMARY.md` when done.
</output>
