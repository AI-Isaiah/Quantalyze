# Requirements — v1.13 Infra: sFOX go-live + worker rebuild

**Defined:** 2026-07-19
**Core Value:** Take the v1.12 sFOX foundation LIVE (the `api_verified` ground truth a submitter can't fabricate) and rebuild the worker architecture the v1.11 FLIP rollback proved is load-bearing — so the derived-allocator-equity flip can retry safely.

**Legend:** ⏳ planned/in-milestone · ⚠️ gated (founder-run op or evidence) · ✅ shipped · 🧱 v1.12 groundwork landed (this req finishes/proves it live)

> REQ→phase mapping is filled by the roadmapper (Traceability section). Per-requirement acceptance detail lives in each phase's ROADMAP Success Criteria once planned. Full context: `PROJECT.md` → Current Milestone · seed `milestones/v1.13-infra-ROADMAP-SEED.md` · memory `project_sfox_verified_integration_milestone`. This milestone continues the four go-live phases re-homed from v1.12 (121-03, 122-04, 123 go-live, 124) plus the worker rebuild + test-DB hygiene. Phase numbering continues from **125**.

## v1 Requirements

### EGRESS — Railway Pro static-outbound IP + sFOX whitelist (re-homes v1.12 SFOX-07 / 121-03)

> **DECISION 2026-07-19 (founder): egress through Railway Pro static-outbound IPs; FLY FULLY DROPPED.** The worker already runs on Railway and egresses from Amsterdam (`152.55.184.85`, AS400940, NL — geo-allowed); Railway Pro (upgraded 2026-07-19) includes static outbound at no extra cost → ZERO new infra, NO code (`WORKER_EGRESS_PROXY_URL` stays UNSET; native egress covers sFOX AND ccxt). `fly-egress-proxy/` is now UNUSED (fallback only). **⚠️ Railway assigns a FIXED SET OF 3 static outbound IPs (load-balanced, cannot reduce to 1) — ALL 3 must be whitelisted.** sFOX's per-key IP-restriction count is undocumented → founder to confirm with sFOX/trading team; if sFOX requires a single IP, a proxy addon (QuotaGuard-type), NOT Fly, is the fallback.
>
> **⭐PHASE NOTE: EGRESS is no longer a standalone build phase (Fly deploy is gone). It is a founder-ops gate + a small egress-stability verification that FOLDS INTO the GOLIVE phase.** These reqs map to the go-live phase; the whitelist is a founder action started in parallel from day 1.

- [ ] **EGRESS-01** ⚠️ **human_needed-OPEN** — Railway `quantalyze-analytics` is on Pro with static outbound IPs active, and the FULL 3-IP set (Amsterdam/NL) is read from the dashboard (service → Settings → Networking) — redeploy the worker if the dashboard says static outbound activates on next deploy. _(Railway upgraded to Pro 2026-07-19.)_ **[Phase 130 disposition: DEFERRED — founder LIVE op (Railway Pro dashboard read), also gated on the external sFOX IP-bind long-pole. Delivered execution path = `docs/runbooks/sfox-go-live.md` Step 0. NEVER claimed done without the recorded FULL 3-IP dashboard set. Milestone ships FLAG-OFF.]**
- [ ] **EGRESS-02** ⚠️ **human_needed-OPEN** — The worker's realized egress is VERIFIED to stay within the 3-IP static set via repeated `railway ssh … ipinfo.io` probes (country NL) BEFORE relying on the whitelist — a single probe misses load-balanced sibling IPs, so whitelist the WHOLE dashboard set, never one observed IP. **[Phase 130 disposition: DEFERRED — founder LIVE op (`railway ssh` into the prod worker). Delivered execution path = `docs/runbooks/sfox-go-live.md` Step 1. NEVER claimed done without the multi-probe (≥5) NL/in-set observation log.]**
- [ ] **EGRESS-03** ⚠️ **human_needed-OPEN** — All 3 static IPs are whitelisted at sFOX (security@quantalyze.com / sFOX dashboard), and an IP-whitelisted sFOX key authenticates end-to-end from the worker's native Railway egress — no proxy in the path. Gates the sFOX flag flip. **[Phase 130 disposition: DEFERRED — founder LIVE op (sFOX dashboard / security@ handoff + native-egress key-auth proof), gated on the external sFOX IP-bind long-pole. Delivered execution path = `docs/runbooks/sfox-go-live.md` Step 2. NEVER claimed done without an authenticating whitelisted key from native egress (`WORKER_EGRESS_PROXY_URL` unset).]**

### WORKER — worker-architecture rebuild (re-homes v1.12 FLIPRETRY-02/04 + test-DB hygiene)

> The v1.11 FLIP rollback root cause: a slow/hanging live crawl blocked the SEQUENTIAL prod worker's event loop → healthz stale 12 min, auto-restart didn't fire. This rebuild is what makes the FLIP retry (FLIP category) safe.

- [ ] **WORKER-01** — The derived-equity backfill runs on its OWN dedicated worker, never the sequential prod worker's event loop (`WORKER_CLAIM_ROLE` + the kind-filtered claim RPC — migration `20260719073701`, already prod-applied). A slow backfill can never wedge live analytics.
- [x] **WORKER-02** 🧱 — Every derived-equity exchange crawl is hard-bounded by `asyncio.wait_for` (groundwork landed v1.12 123-01), proven end-to-end so a deliberately-hung crawl times out and the worker stays live; per-job `LAST_TICK_AT` refresh keeps healthz honest through long backfills. ✅ 125-02 (`tests/test_worker_isolation_e2e.py`)
- [ ] **WORKER-03** — The key-mode backfill runs batched / off-hours and the cron is re-scheduled (`cron.schedule('derive-allocator-key-dailies', '30 5 * * *')` — a founder LIVE SQL op, NOT a migration: auto-apply + a skipped worker deploy would re-wedge).
- [ ] **WORKER-04** — Orphaned `running` compute_jobs are purged on a retention schedule (test project nightly + a prod safety sweep), killing the recurring `python` fence-test CI flake at its root (the `derive-allocator-key-dailies` cron re-pollutes daily on the workerless test project). See memory `project_shared_testdb_concurrent_ci_flake`.

### FACTSHEET — connected-key api_verified factsheet e2e (re-homes v1.12 122-04)

- [ ] **FACTSHEET-01** — The `/strategy/[id]` factsheet renders (or degrades gracefully) for an `api_verified` + api_key-linked sFOX strategy — the connected-key provenance render no longer throws (the same strategy's edit-page tag + browse badge already render fine, so the throw is factsheet-specific). Hardens prod against a transient analytics outage too.
- [ ] **FACTSHEET-02** — `e2e/sfox-badge.spec.ts` is GREEN across all roles (owner / allocator / admin) incl. axe, and wired into the `frontend` branch-protection gate (not advisory). MUST be green before the sFOX flag flip.

### E2GT — E2 ground-truth validation (re-homes v1.12 FLIPRETRY-03)

> The `api_verified` anti-fabrication anchor for the derived curve. Harness + hand-derived-oracle fixtures landed v1.12 123-03; this is the live acceptance run + the display gate.

- [ ] **E2GT-01** ⚠️🧱 — `e2_allocator_ground_truth.py` runs live with `E2_GROUND_TRUTH_*` env (a read-only exchange key in Railway) and passes anchor-consistency on real account data.
- [x] **E2GT-02** 🧱 — A derived curve is displayed ONLY after it passes anchor-consistency; `extractTrustworthyDerivedCurve` (`src/lib/queries.ts` — the flip is data-driven, no flag) flips `equityCurveSource` to derived only for a trustworthy curve, else the legacy basis renders unchanged. **[Phase 127 audit: all 3 display-gate criteria (a/b/c) proven by existing v1.12 123-03 fixtures — `queries.test.ts` FLIPRETRY-03 pair, `queries.my-allocation.test.ts` 115.1 display-repoint 8-case block, `test_e2_ground_truth_harness.py` PASS/FAIL anchor + blocking-degradation neuter-proofs. No new test needed.]**

### DERIBITFIX — deribit `correction` txn-log classification (re-homes v1.12 124)

- [x] **DERIBITFIX-01** — The deribit native-ledger ingest classifies the `correction` txn-log type correctly, on EVIDENCE of what deribit `correction` means (CASH_BEARING vs INFORMATIONAL — docs and/or the real Railway key3 ledger entry) — never a guess — so a `correction`-bearing deribit account ingests without raising `LedgerValuationError`. The evidence + determination are written down with the change. ✅ Phase 128 (commit `001b00de`): gated PER ROW on `info.reason` (founder decision) — a funding-calc correction (the key3 evidence) is realized cash; a capital/unrecognized/missing-reason correction fails loud. Evidence written at the change in `deribit_txn.py`.
- [x] **DERIBITFIX-02** — A regression fixture with a `correction`-bearing ledger proves ingest succeeds with correct equity (fails without the fix); genuinely-unknown future txn types STILL fail loud (no silent absorption / corruption). ✅ Phase 128: funding-correction hand-derived USD+native money oracles + capital-reason and unrecognized/missing-reason fail-loud guards + unknown-type (`quorble`) still-fails-loud + WR-01 collision guards (capital reason containing a trading substring → fail loud) — all RED-proven in every neuter direction.

### FLIP — derived-allocator-equity FLIP retry (the payoff)

- [ ] **FLIP-01** ⚠️🧱 **human_needed-OPEN** — The v1.11-rolled-back flip is re-attempted on the hardened dedicated worker (WORKER category) AFTER E2GT validation; the production backfill enqueue completes without wedging the worker (healthz never stale past the restart threshold). **[Phase 129 disposition: DEFERRED — founder LIVE op requiring prod Railway + prod-SQL access, GATED on E2GT-01 (still human_needed-OPEN). NEVER claimed done without healthz-fresh-throughout + queue-drained + `allocator_equity_derived`-repopulating live evidence. Delivered execution path = `docs/runbooks/flipretry-derived-equity-go-live.md` Steps 0–5 + Step 8 abort. Explicitly gates Phase 130 GOLIVE.]**
- [x] **FLIP-02** — The enqueue is idempotent / safe to re-run and the rollback is documented + executable (delete flip jobs + empty `allocator_equity_derived` + unschedule cron — the exact v1.11 recovery). Runbook: `docs/runbooks/flipretry-derived-equity-go-live.md`. **[Phase 129 audit SATISFIED, live evidence — 6/6 checklist items verdict SATISFIED: `test_phase35_backfill_enqueue.py` 10 passed/0 skipped (idempotent bail + count-None RAISE + data-None RAISE + malformed-skip + 23505 atomic-race abort); Step-5 SQL fan-out idempotency (advisory lock + per-(key,UTC-date) key + one-inflight index) pinned by `test_claim_kind_filter.sql` Part-3 double-invoke + `test_derive_allocator_keys_fanout.sql`; runbook Step 8 rollback carries all 3 SQL ops (compute_jobs DELETE scoped to the 2 flip kinds × pending/running, `allocator_equity_derived` DELETE, `cron.unschedule`) — grep gates green. ZERO source edits; no gap found (ponytail).]**

### GOLIVE — sFOX flag flip to live (the milestone culmination)

- [ ] **GOLIVE-01** ⚠️ **human_needed-OPEN** — Once EGRESS (an IP-bound authenticating key), FACTSHEET, and E2GT are all green, the founder flips `SFOX_ENABLED` + `NEXT_PUBLIC_SFOX_ENABLED` on (LIVE ops, not migrations). sFOX becomes offerable and live. **[Phase 130 disposition: DEFERRED — founder LIVE op (Railway + Vercel prod env flips + both redeploys), gated on the upstream OPEN legs EGRESS-01/02/03 + E2GT-01. Delivered execution path = `docs/runbooks/sfox-go-live.md` Step 3 (explicit 5-row gate: EGRESS 1-3 + `gh pr checks` FACTSHEET + two-part E2GT `within_same_day_tolerance===true`). NEVER claimed done without the flags-observed set + both redeploys verified. Milestone ships FLAG-OFF — the flip is delivered as a gated execution path, not performed.]**
- [ ] **GOLIVE-02** **human_needed-OPEN** — With the flag on, a user connects a live IP-whitelisted sFOX key end-to-end and its `api_verified` strategy renders live across surfaces (factsheet + discovery + edit), proven E2E across roles — the full flow, not one surface. **[Phase 130 disposition: DEFERRED — founder LIVE op (real whitelisted key + multi-role prod session), gated on GOLIVE-01. Delivered execution path = `docs/runbooks/sfox-go-live.md` Step 4 (+ Step 5 trivial rollback). NEVER claimed done without the `api_verified` render observed on all 3 surfaces × 3 roles (owner/allocator/admin).]**

## v2 / Future Requirements

- Additional non-ccxt exchanges beyond sFOX — deferred until a second one is requested.
- Per-key stitch progress in the multi-key connect wizard (founder dogfood 2026-07-18) — new UI work, not infra; deferred.
- Per-day uPnL true-up on the reconstruction (v1.8 decision, availability-gated) — deferred.

## Out of Scope

- **Executing trades / moving money via sFOX** — read-only ground-truth ingestion only; keys validated read-only, no order/withdraw path. Financial-action safety rule.
- **Railway static IP** — Hobby has none (Pro-only $20/mo); the $2/mo Fly dedicated-v4 + static egress is the sanctioned path (founder 2026-07-17). _(⚠️ STALE 2026-07-17 entry — SUPERSEDED by the 2026-07-19 EGRESS decision above: Railway Pro IS the path now; Fly fully dropped. Kept for the record.)_
- **NordVPN / in-container tunnel egress** — rejected as fragile for server egress; the Fly proxy is the sanctioned static-IP path. _(⚠️ second clause stale per the 2026-07-19 decision — the NordVPN rejection stands; the fallback is a proxy addon, not Fly.)_
- **US-region egress** — geo-matters (Bybit/Binance 403/451 from US); egress stays Amsterdam/NL (Railway-native, matching today's egress).
- **`allocator_equity_snapshots` legacy retirement (STITCH-02/BACKBONE-03)** — unblocked only AFTER the FLIP is live + the derived surface proven; a follow-up, not this milestone.
- **The two standing latent bugs** (quantstats price-detection Sharpe sign-flip on the strategy-analytics path; blend √252 unknown-asset_class annualization) — founder-decision-gated (they change displayed numbers), independent of sFOX/infra; tracked in memory, not scoped here.

## Traceability

<!-- Mapped by the v1.13 roadmap (restructured 2026-07-19: 6 phases 125–130 — Fly dropped, EGRESS folded into Phase 130 GOLIVE). Every v1 requirement → exactly one phase; 17/17 mapped, no orphans, no duplicates. -->

| REQ-ID | Phase | Status |
|--------|-------|--------|
| EGRESS-01 | Phase 130 | Pending (⚠️ founder op — start day 1 in parallel) |
| EGRESS-02 | Phase 130 | Pending (⚠️ founder op — start day 1 in parallel) |
| EGRESS-03 | Phase 130 | Pending (⚠️ founder op — start day 1 in parallel) |
| WORKER-01 | Phase 125 | Pending |
| WORKER-02 | Phase 125 | Done (125-02) |
| WORKER-03 | Phase 125 | Pending (cron reschedule = founder LIVE op) |
| WORKER-04 | Phase 125 | Pending |
| FACTSHEET-01 | Phase 126 | Pending |
| FACTSHEET-02 | Phase 126 | Pending |
| E2GT-01 | Phase 127 | Pending (⚠️ founder env + live run) |
| E2GT-02 | Phase 127 | ✅ Complete (audit — all 3 criteria pre-covered, no new test) |
| DERIBITFIX-01 | Phase 128 | ✅ Complete (commits `001b00de`+`d410d3b7`) |
| DERIBITFIX-02 | Phase 128 | ✅ Complete (commits `001b00de`+`d410d3b7`) |
| FLIP-01 | Phase 129 | Pending (⚠️ founder enqueue op) |
| FLIP-02 | Phase 129 | Pending |
| GOLIVE-01 | Phase 130 | Pending (⚠️ founder flag flip) |
| GOLIVE-02 | Phase 130 | Pending |
