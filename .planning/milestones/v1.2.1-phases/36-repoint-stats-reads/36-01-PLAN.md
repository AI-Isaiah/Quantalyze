---
phase: 36-repoint-stats-reads
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/database.types.ts
autonomous: true
requirements: [UNIFY-01]
must_haves:
  truths:
    - "A TS read of csv_daily_returns can project id / api_key_id / allocator_id and treat strategy_id as nullable without a type error"
  artifacts:
    - path: "src/lib/database.types.ts"
      provides: "csv_daily_returns Row/Insert/Update with the per-key axis columns + nullable strategy_id"
      contains: "api_key_id"
  key_links:
    - from: "src/lib/database.types.ts"
      to: "supabase/migrations/20260624120000_csv_daily_returns_per_key_axis.sql"
      via: "hand-patched column set mirrors the landed ALTER"
      pattern: "allocator_id"
---

<objective>
Hand-patch the `csv_daily_returns` table types in `src/lib/database.types.ts` so they match the landed migration `20260624120000_csv_daily_returns_per_key_axis.sql` (D5). The current Row/Insert/Update are stale: `strategy_id` is typed non-null and the per-key axis columns (`id`, `api_key_id`, `allocator_id`) are absent.

This is the foundation plan: the per-key read added in 36-03 selects `api_key_id, allocator_id, date, daily_return` from `csv_daily_returns` and cannot compile against the stale types. No CI gate regenerates `database.types.ts` (repo convention: hand-patch when TS reads new columns).

Purpose: unblock the typed per-key read in 36-03.
Output: corrected `csv_daily_returns` type block, `npx tsc --noEmit` still passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-repoint-stats-reads/36-CONTEXT.md

<interfaces>
<!-- The landed migration is the source of truth for the patched columns. -->
From supabase/migrations/20260624120000_csv_daily_returns_per_key_axis.sql:
- `strategy_id` becomes NULLABLE (ALTER COLUMN strategy_id DROP NOT NULL) — per-key rows have no strategy.
- `id BIGINT GENERATED ALWAYS AS IDENTITY` is the surrogate PRIMARY KEY (a number).
- `api_key_id UUID REFERENCES api_keys(id) ON DELETE CASCADE` — nullable (NULL for strategy rows).
- `allocator_id UUID REFERENCES auth.users(id) ON DELETE CASCADE` — nullable (NULL for strategy rows).
- XOR check: exactly one of (strategy_id, api_key_id) is non-null per row.

Current stale block in src/lib/database.types.ts (~854-885):
Row { created_at: string; daily_return: number; date: string; strategy_id: string; updated_at: string }
Insert / Update mirror it with strategy_id required / optional respectively.
The Relationships array already carries csv_daily_returns_strategy_id_fkey.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Patch csv_daily_returns Row/Insert/Update with the per-key axis</name>
  <files>src/lib/database.types.ts</files>
  <read_first>
    - src/lib/database.types.ts (the csv_daily_returns block, ~854-885 — Row, Insert, Update, Relationships)
    - supabase/migrations/20260624120000_csv_daily_returns_per_key_axis.sql (the landed ALTER — the column source of truth)
  </read_first>
  <action>
    In the `csv_daily_returns` table type, edit Row, Insert, and Update to add the per-key axis and make strategy_id nullable, mirroring the migration:
    - Row: change `strategy_id: string` to `strategy_id: string | null`; add `id: number`, `api_key_id: string | null`, `allocator_id: string | null`. Keep `created_at`, `daily_return`, `date`, `updated_at` as-is.
    - Insert: change `strategy_id: string` to `strategy_id?: string | null`; add `id?: never` (IDENTITY GENERATED ALWAYS — never client-supplied, matching the repo's existing convention for identity surrogate PKs; if no such precedent exists in this file, use `id?: number`), `api_key_id?: string | null`, `allocator_id?: string | null`.
    - Update: change `strategy_id?: string` to `strategy_id?: string | null`; add `id?: never` (or `id?: number` to match the Insert choice), `api_key_id?: string | null`, `allocator_id?: string | null`.
    Leave the Relationships array unchanged (the existing strategy FK entry stays; do NOT add api_keys/auth.users relationship rows — the repo's hand-patch convention only patches the columns the TS read needs, and no code reads csv_daily_returns via a typed nested join on those FKs). Match the existing 2-space-per-level indentation and alphabetical-ish key ordering already present in the block. Touch ONLY the csv_daily_returns block — no other table.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/database.types.ts` csv_daily_returns Row contains `api_key_id`, `allocator_id`, `id`, and `strategy_id: string | null`.
    - `grep -n "api_key_id" src/lib/database.types.ts` shows a match inside the csv_daily_returns block.
    - `npx tsc --noEmit` exits 0 (no type errors introduced).
    - No table other than csv_daily_returns is modified (the diff is confined to the ~854-885 block).
  </acceptance_criteria>
  <done>
    csv_daily_returns Row/Insert/Update carry the nullable strategy_id + id/api_key_id/allocator_id columns matching the landed migration; tsc passes.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` exits 0.
- The diff touches only the `csv_daily_returns` type block.
</verification>

<success_criteria>
- A typed select of `api_key_id, allocator_id, date, daily_return` from `csv_daily_returns` would compile (the per-key read in 36-03 depends on this).
- strategy_id is nullable; the per-key columns exist on Row/Insert/Update.
</success_criteria>

<output>
Create `.planning/phases/36-repoint-stats-reads/36-01-SUMMARY.md` when done.
</output>
