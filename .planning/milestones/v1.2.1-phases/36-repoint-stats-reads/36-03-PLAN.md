---
phase: 36-repoint-stats-reads
plan: 03
type: execute
wave: 2
depends_on: ["36-01"]
files_modified:
  - src/lib/queries.ts
  - src/lib/queries.my-allocation.test.ts
  - src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts
autonomous: true
requirements: [UNIFY-01, UNIFY-02, UNIFY-03]
must_haves:
  truths:
    - "When every active key of the allocator has a per-key csv_daily_returns series, Overview stats (equity curve + Sharpe + maxDD + returns + avgRho) derive from the per-key blend through computeScenario, not from the allocator_equity_snapshots reconstruction"
    - "When any active key lacks a per-key series, the WHOLE allocator falls back to the existing reconstructHoldingReturnsByScopeRef snapshot path (never a mixed-basis curve)"
    - "AUM (total USD) is unchanged — still summed from allocator_holdings equity contribution"
    - "The liveBaselineMetrics payload shape is byte-identical on both branches (per-key and fallback)"
    - "Live holdings / positions still read the allocator_holdings poll/snapshot path — provably untouched"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "a per-key csv_daily_returns fetch in getMyAllocationDashboard + a per-key blend feeding liveBaselineMetrics with an all-or-nothing fallback"
      contains: "csv_daily_returns"
    - path: "src/lib/queries.my-allocation.test.ts"
      provides: "per-key branch + fallback branch + mixed-population honesty fixtures"
      contains: "csv_daily_returns"
  key_links:
    - from: "src/lib/queries.ts getMyAllocationDashboard"
      to: "csv_daily_returns (api_key_id axis)"
      via: "parallel fetch in the Step-1 fan-out, .eq(allocator_id, userId)"
      pattern: "from\\(\"csv_daily_returns\"\\)"
    - from: "src/lib/queries.ts"
      to: "computeScenario"
      via: "per-key blend builds one StrategyForBuilder per api_key_id and runs the frozen engine"
      pattern: "computeScenario"
    - from: "src/lib/queries.ts liveBaselineMetrics"
      to: "allocator_holdings"
      via: "AUM + per-key weights still sourced from holdingsSummary (api_key_id grouped)"
      pattern: "holdingEquityContribution"
---

<objective>
Repoint the allocator **Overview** equity curve + KPIs (Sharpe, returns, volatility, max-drawdown, avg-ρ) from the `allocator_equity_snapshots` per-symbol reconstruction onto the persisted per-key dailies in `csv_daily_returns`, blended through the SAME `computeScenario` path the CSV/factsheet stats already use (UNIFY-01, UNIFY-02). The blend unit is per `api_key_id` (D1). AUM stays from holdings (D2). The fallback is all-or-nothing per allocator (D3) so a curve is never a mixed annualization basis. Live HOLDINGS stay on the `allocator_holdings` poll path (UNIFY-03) — that read is provably untouched.

`computeScenario` is the FROZEN engine (SCENARIO-05) — reuse, never fork. The `liveBaselineMetrics` output contract MUST stay byte-identical (the scenario composer baseline + SSR payload depend on it) — only the SOURCE of the curve/KPIs changes.

Purpose: Overview numbers converge to the realized+funding / unified-252 basis (the same honest source Scenario and factsheets read) once per-key rows exist.
Output: a per-key fetch + blend in `getMyAllocationDashboard`, the D3 gate, and test coverage for both branches + the mixed-population honesty guard + the shape-identical proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-repoint-stats-reads/36-CONTEXT.md
@.planning/phases/36-repoint-stats-reads/36-01-SUMMARY.md

<interfaces>
<!-- queries.ts already imports the frozen engine + helpers (lines 9-37): -->
From @/lib/scenario: buildDateMapCache, computeScenario, type DailyPoint, type ScenarioState, type StrategyForBuilder
From ./keys: holdingScopeKey
From ./scenario-dealias: collapseAliasedHoldingStrategies
From @/app/(dashboard)/allocations/lib/drawdown: deriveSnapshotDrawdowns

<!-- The EXISTING fallback path (KEEP it — becomes the D3 fallback):
     reconstructHoldingReturnsByScopeRef(equitySnapshots, holdingsSummary): Record<scopeRef, DailyPoint[]>
     queries.ts ~2002-2044. -->

<!-- The output contract that must stay byte-identical (queries.ts ~1750-1758):
     liveBaselineMetrics: { aum:number; ytdTwr:number|null; sharpe:number|null;
       maxDd:number|null; avgRho:number|null; equity:DailyPoint[] (wealth-form);
       drawdown:DailyPoint[] } -->

<!-- liveBaselineMetricsFromHoldings(holdingsSummary, holdingReturnsByScopeRef)
     queries.ts ~2099-2209 — builds one StrategyForBuilder per HOLDING (scopeRef),
     equity-weighted by holdingEquityContribution, de-aliases multi-venue via
     collapseAliasedHoldingStrategies, runs computeScenario, converts the
     cumulative-return curve to wealth (value+1), derives drawdown over
     (value+1)*totalAum. AUM = Σ holdingEquityContribution(h). -->

<!-- holdingsSummary rows carry api_key_id (queries.ts ~2320, derivePhase07Fields
     output) — this is the per-key WEIGHT source for D2. -->

<!-- The Step-1 parallel fan-out (queries.ts ~2404-2491) already parallel-fetches
     allocator_equity_snapshots + allocator_holdings + api_keys via Promise.all,
     scoped by .eq("allocator_id", userId). Add the csv_daily_returns per-key
     fetch HERE. assertOk(res, label) upgrades a silent .error into a throw. -->

<!-- BOTH return branches call:
     liveBaselineMetricsFromHoldings(phase07.holdingsSummary, holdingReturnsByScopeRef)
     where holdingReturnsByScopeRef = reconstructHoldingReturnsByScopeRef(...)
     !portfolio branch ~2845-2848; portfolio branch ~3153-3156. -->

<!-- Per-key csv_daily_returns row shape (written by run_derive_broker_dailies_job
     key-mode): { api_key_id, allocator_id, date, daily_return, strategy_id: NULL },
     on_conflict=(api_key_id,date). Owner RLS: allocator_id = auth.uid(). The user
     supabase client (NOT admin) can read these under that RLS policy. -->

<!-- D3 predicate: "active keys" = apiKeys.filter(k => k.is_active). An allocator
     qualifies for the per-key blend iff EVERY active key id appears with >=1
     per-key row in the window. Any active key with 0 per-key rows -> fallback. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the per-key fetch + per-key blend + D3 all-or-nothing gate in getMyAllocationDashboard</name>
  <files>src/lib/queries.ts</files>
  <read_first>
    - src/lib/queries.ts: reconstructHoldingReturnsByScopeRef (~2002-2044); liveBaselineMetricsFromHoldings (~2099-2209); holdingEquityContribution (~2081-2091); the liveBaselineMetrics payload type (~1750-1758); the Step-1 fan-out Promise.all (~2404-2491) + assertOk (~2388-2402); the holdingReturnsByScopeRef construction (~2803-2809); the two return branches that call liveBaselineMetricsFromHoldings (~2845-2848 and ~3153-3156); the apiKeys / is_active usage (~2257, derivePhase07Fields)
    - src/lib/scenario.ts: computeScenario, StrategyForBuilder, ScenarioState, buildDateMapCache signatures (the frozen engine — reuse, do not edit)
    - src/lib/keys.ts: holdingScopeKey
    - supabase/migrations/20260624120000_csv_daily_returns_per_key_axis.sql (per-key row shape + owner RLS)
  </read_first>
  <behavior>
    - Per-key blend (D1/D2): when the D3 gate passes, build one StrategyForBuilder per api_key_id whose daily_returns is that key's (date, daily_return) series from csv_daily_returns; weight each key by its current equity share = Σ holdingEquityContribution(h) over holdings where h.api_key_id === that key (grouped from holdingsSummary — no new weight source); run computeScenario; produce the SAME liveBaselineMetrics shape (aum from holdings, ytdTwr/sharpe/maxDd/avgRho/equity(wealth-form)/drawdown from the engine output, identical post-processing to liveBaselineMetricsFromHoldings).
    - D3 gate (all-or-nothing): the per-key blend is used IFF every active key (apiKeys.filter is_active) has >=1 per-key row in the fetched window. If any active key has zero per-key rows -> fall back to the existing reconstructHoldingReturnsByScopeRef snapshot path for the WHOLE allocator. Mixed population (one key with dailies, one without) -> fallback (never a half-per-key/half-snapshot curve).
    - AUM unchanged (D2): totalAum = Σ holdingEquityContribution(h) on BOTH branches.
    - Shape-identical: the returned liveBaselineMetrics object has the same keys/types on both branches; empty-default (aum=totalAum, nulls, empty arrays) when no usable series.
    - Holdings untouched (UNIFY-03): the allocator_holdings fetch + derivePhase07Fields holdings collapse are NOT modified.
  </behavior>
  <action>
    Implement the seam as a sibling selector keeping `liveBaselineMetricsFromHoldings` byte-identical (Claude's-discretion guidance: smallest diff that keeps the output contract identical). Concretely:
    1) Add a parallel fetch in the Step-1 Promise.all fan-out: `supabase.from("csv_daily_returns").select("api_key_id, allocator_id, date, daily_return").eq("allocator_id", userId).order("date", { ascending: true })`. Use the user supabase client (owner RLS `allocator_id = auth.uid()` covers it — do NOT use admin). **Bound by a DATE-WINDOW filter, not a bare ascending `.limit()`:** add `.gte("date", <ISO date 730 days ago>)` (mirrors the snapshot 2yr window, per key, with NO row-count truncation of recent data — a bare ascending `.limit(730)` over K keys would silently drop the NEWEST rows, corrupting the curve). Add a flat safety ceiling `.limit(20000)` (≈27 keys × 730d) so the payload cannot grow unbounded as csv_daily_returns accumulates; document the 730-day window + the safety ceiling in a comment. Add the result to the destructured array and call `assertOk(res, "csv_daily_returns")`.
    2) Add an internal pure helper `liveBaselineMetricsFromPerKeyDailies(holdingsSummary, perKeyReturnsByApiKeyId)` that mirrors liveBaselineMetricsFromHoldings EXACTLY except: each "strategy" is keyed by api_key_id (id = api_key_id, name = a stable per-key label), daily_returns = that key's series; weights = Σ holdingEquityContribution over holdings with that api_key_id; AUM = Σ holdingEquityContribution(all holdings); reuse the SAME computeScenario → wealth-conversion (value+1) → deriveSnapshotDrawdowns post-processing and the SAME empty-default. (De-alias collapse is a multi-venue-symbol concern; per-key strategies are already one-per-key so the avgRho fabrication risk does not arise — do not invent a collapse step, but DO keep avgRho sourced from computeScenario's avg_pairwise_correlation so the contract field is populated.)
    3) Add an internal pure predicate `allActiveKeysHavePerKeyDailies(activeKeyIds, perKeyReturnsByApiKeyId)` returning true iff every active key id has a non-empty series. Build `perKeyReturnsByApiKeyId: Record<string, DailyPoint[]>` from the csv_daily_returns rows (group by api_key_id, map to { date, value: daily_return }, drop non-finite).
    4) At the seam where `holdingReturnsByScopeRef` + liveBaselineMetrics are computed (shared by both return branches): compute `activeKeyIds = apiKeys.filter(k => k.is_active).map(k => k.id)`; if `allActiveKeysHavePerKeyDailies(activeKeyIds, perKeyReturnsByApiKeyId)` AND activeKeyIds.length > 0, set `liveBaselineMetrics = liveBaselineMetricsFromPerKeyDailies(holdingsSummary, perKeyReturnsByApiKeyId)`; else `liveBaselineMetrics = liveBaselineMetricsFromHoldings(holdingsSummary, holdingReturnsByScopeRef)` (the fallback). Compute it ONCE and reuse in both the !portfolio and portfolio return branches (replace the two inline liveBaselineMetricsFromHoldings(...) calls with the single computed value). Keep `holdingReturnsByScopeRef` in the payload unchanged (it is a separate payload field consumed elsewhere; only the liveBaselineMetrics SOURCE changes).
    Do NOT modify the allocator_holdings fetch, derivePhase07Fields, reconstructHoldingReturnsByScopeRef, or computeScenario. Keep liveBaselineMetricsFromHoldings exactly as-is (it is the fallback). Add a comment block citing D1/D2/D3 + UNIFY-01/02/03 at the new seam.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/lib/queries.my-allocation.test.ts src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from(\"csv_daily_returns\")" src/lib/queries.ts` matches inside getMyAllocationDashboard (the per-key fetch).
    - `grep -n "liveBaselineMetricsFromPerKeyDailies\|allActiveKeysHavePerKeyDailies" src/lib/queries.ts` matches (the blend helper + the D3 predicate).
    - The csv_daily_returns fetch uses `.eq("allocator_id", userId)` and the user supabase client (NOT admin).
    - reconstructHoldingReturnsByScopeRef and liveBaselineMetricsFromHoldings source is unchanged (`git diff` shows no edits to their bodies — they remain the fallback).
    - The allocator_holdings select string + derivePhase07Fields are unchanged (UNIFY-03).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>
    getMyAllocationDashboard fetches per-key csv_daily_returns, blends per api_key_id through computeScenario behind the D3 all-or-nothing gate, falls back to the snapshot reconstruction otherwise, keeps AUM from holdings, and leaves the holdings read untouched; tsc passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test the per-key branch, the fallback branch, the mixed-population honesty guard, and shape-identity</name>
  <files>src/lib/queries.my-allocation.test.ts, src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts</files>
  <read_first>
    - src/lib/queries.my-allocation.test.ts: the supabase mock `switch(table)` rowsFor seam (~180-220) + the chain mock (~223-280); the Phase-07 payload-extension describe (~850-1320); the avgRho de-alias wiring test (~1443-1506)
    - src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts: the liveBaselineMetrics shape pin (~225-285); reconstructHoldingReturnsByScopeRef describe (~35)
    - src/lib/queries.ts: the new liveBaselineMetricsFromPerKeyDailies + allActiveKeysHavePerKeyDailies + the D3 seam (from Task 1)
  </read_first>
  <behavior>
    - Per-key branch: an allocator whose every active key has a per-key csv_daily_returns series gets liveBaselineMetrics derived from the per-key blend (assert it differs from the snapshot reconstruction for a fixture where the two bases disagree, AND has the full shape).
    - Fallback branch: an allocator with NO per-key rows gets the existing reconstruction-derived liveBaselineMetrics (regression-pins the current behavior).
    - Mixed-population honesty (D3): an allocator with key A having per-key rows and active key B having NONE takes the FALLBACK — never a half-per-key/half-snapshot blended curve. Assert the result equals the snapshot-fallback result (or a marker proving the fallback ran), and is NOT the per-key blend.
    - Shape-identity: the liveBaselineMetrics object on the per-key branch and the fallback branch have identical keys/types (the SSR/composer contract is unaffected).
    - The avgRho de-alias wiring test (1443-1506) still holds on whichever branch it exercises.
  </behavior>
  <action>
    1) In `src/lib/queries.my-allocation.test.ts`: extend the mock `switch(table)` rowsFor seam with a `case "csv_daily_returns": return applyFilters(state.csvDailyReturns ...)` arm and add a `csvDailyReturns` field to the mock state (default []). Add tests under the Phase-07 / liveBaselineMetrics area: (a) per-key branch fixture (active keys all present in csvDailyReturns) → liveBaselineMetrics reflects the per-key blend; (b) fallback fixture (csvDailyReturns empty, snapshots present) → reconstruction-derived metrics; (c) mixed-population fixture (key A in csvDailyReturns, active key B absent) → result equals the fallback (assert NOT the per-key blend — falsifiable: a naive per-key-only path would differ). Keep the existing avgRho de-alias test green (adjust fixtures only if the new csvDailyReturns default forces a branch — prefer leaving csvDailyReturns empty there so it stays on the fallback it already pins).
    2) In `src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts`: add a shape-identity assertion — construct both a per-key-branch liveBaselineMetrics and a fallback liveBaselineMetrics and assert identical key sets + value types (aum:number, ytdTwr/sharpe/maxDd/avgRho:number|null, equity/drawdown:array). If liveBaselineMetricsFromPerKeyDailies is not exported, export it for unit testing (matching the `@internal Exported for unit testing only` convention on liveBaselineMetricsFromHoldings).
    Make every new assertion falsifiable: a mixed-population test must FAIL if the code blends half-per-key/half-snapshot; the per-key-vs-fallback divergence test must FAIL if the per-key fetch is ignored.
  </action>
  <verify>
    <automated>npx vitest run src/lib/queries.my-allocation.test.ts src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "csv_daily_returns\|csvDailyReturns" src/lib/queries.my-allocation.test.ts` matches (mock arm + fixtures).
    - A test asserts the mixed-population case takes the fallback (not a mixed-basis blend).
    - A test asserts per-key-branch metrics differ from snapshot-fallback metrics on a divergent fixture.
    - A test asserts liveBaselineMetrics has the identical shape on both branches.
    - `npx vitest run src/lib/queries.my-allocation.test.ts src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts` exits 0.
  </acceptance_criteria>
  <done>
    Both branches, the D3 honesty guard, and shape-identity are pinned by falsifiable tests; the full suite for these files passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| userId → csv_daily_returns SELECT | The allocator's own id scopes the per-key read; admin client would bypass RLS, so the user client + owner RLS is the tenant boundary. |
| per-key dailies → Overview KPIs | Wrong blend (mixed basis) would publish a dishonest curve to the allocator. |
| holdings read → live positions | Repointing stats must NOT alter the holdings/positions read (would stale live positions). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-03-01 | Information Disclosure | csv_daily_returns per-key fetch | mitigate | Use the user supabase client + `.eq("allocator_id", userId)`; owner RLS (`allocator_id = auth.uid()`) is the DB gate; the function's existing auth.uid()===userId backstop fails closed. Do NOT use the admin client for this read. |
| T-36-03-02 | Spoofing / Integrity (honest stats) | D3 fallback gate | mitigate | All-or-nothing per allocator: a mixed population falls back to the snapshot basis; a unit test asserts the mixed case never produces a half-per-key/half-snapshot curve. |
| T-36-03-03 | Denial of Service | unbounded per-key fetch | mitigate | `.limit(...)` bounds the per-key fetch (mirrors the snapshot 730-day cap) so the payload cannot grow unbounded as csv_daily_returns accumulates. |
| T-36-03-04 | Tampering | regression to UNIFY-03 (holdings) | mitigate | The allocator_holdings select + derivePhase07Fields are not edited; acceptance criteria + `git diff` confirm the holdings path is untouched. |
| T-36-03-SC | Tampering | npm/pip/cargo installs | accept | No package installs (reuses computeScenario + existing imports + vitest). No legitimacy gate needed. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run src/lib/queries.my-allocation.test.ts src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts` exits 0.
- `git diff src/lib/queries.ts` shows the allocator_holdings fetch + derivePhase07Fields + reconstructHoldingReturnsByScopeRef + liveBaselineMetricsFromHoldings bodies unchanged (only additions: the per-key fetch, the new helpers, and the D3 seam).

## Post-deploy operational step (D6 — NOT code; run after ship+deploy)
After this phase's code (36-01/02/03) ships, lands, and deploys to prod, the per-key blend only SHOWS once per-key rows exist. The dual-mode derive job + the backfill script already exist from Phase 35 (shipped) — do NOT author them. Operational sequence:
1. Confirm 36-02 (GDPR per-key axis) is live so no un-exportable per-key data exists.
2. Run `railway ssh "cd /app && python -m scripts.phase35_backfill_enqueue"` (service-role) to enqueue an api_key-scoped derive_broker_dailies job per active connected key. The backfill is idempotent (pre-check guard + 23505-safe insert).
3. Wait for the worker to drain; verify per-key rows populated and that Overview KPIs converged to the realized+funding basis in prod for at least one allocator whose every active key is a crypto-exchange key. Allocators with a non-derivable key (e.g. MT5/IBKR — no funding-dailies feeder) correctly stay on the snapshot basis (D3) until a feeder exists; that is honest, not a regression.
</verification>

<success_criteria>
- Overview stats derive from the per-key blend through computeScenario when every active key has a per-key series (UNIFY-01, UNIFY-02), converging to the realized+funding / unified-252 basis.
- A mixed population falls back to the snapshot reconstruction for the whole allocator (D3) — never a mixed-basis curve.
- AUM is unchanged (summed from holdings); the liveBaselineMetrics shape is byte-identical on both branches.
- Live holdings / positions still read the allocator_holdings poll path (UNIFY-03), provably untouched.
</success_criteria>

<output>
Create `.planning/phases/36-repoint-stats-reads/36-03-SUMMARY.md` when done.
</output>
