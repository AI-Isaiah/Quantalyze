---
phase: 38-composer-factsheet-parity-blank-mode-fix
plan: 04
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
  - src/app/(dashboard)/allocations/AllocationsTabs.tsx
  - src/app/(dashboard)/allocations/widgets/performance/composer-width.test.tsx
autonomous: true
requirements: [PARITY-02]

must_haves:
  truths:
    - "The whole composer body renders at max-w-[1440px] so the factsheet-grade chart has room — the 3 in-scope literals are relaxed from 1100 to 1440."
    - "The out-of-scope Overview empty-state container (AllocationDashboardV2.tsx:157) stays max-w-[1100px]."
  artifacts:
    - path: "src/app/(dashboard)/allocations/components/ScenarioComposer.tsx"
      provides: "the composer body + empty-state containers at 1440"
      contains: "max-w-[1440px]"
    - path: "src/app/(dashboard)/allocations/AllocationsTabs.tsx"
      provides: "the Scenario-tab loading skeleton at 1440 (skeleton↔loaded width consistency)"
      contains: "max-w-[1440px]"
    - path: "src/app/(dashboard)/allocations/widgets/performance/composer-width.test.tsx"
      provides: "source assertion: 3 in-scope literals = 1440; out-of-scope stays 1100"
      contains: "max-w-[1440px]"
  key_links:
    - from: "composer-width.test.tsx"
      to: "ScenarioComposer.tsx + AllocationsTabs.tsx (1440) / AllocationDashboardV2.tsx (1100)"
      via: "source-text assertion"
      pattern: "max-w-\\[1440px\\]"
---

<objective>
Relax the composer max width from `max-w-[1100px]` to the literal `max-w-[1440px]` at exactly the 3
in-scope sites (`ScenarioComposer.tsx:1810`, `ScenarioComposer.tsx:1860`, `AllocationsTabs.tsx:127`)
so the factsheet-grade chart (Plan 03) has room to render, and pin the boundary with a source-assertion
test that also proves the out-of-scope `AllocationDashboardV2.tsx:157` stays `max-w-[1100px]`.

Purpose: PARITY-02. The two `ScenarioComposer` literals are the BINDING constraint (the live
`panel-scenario` tabpanel imposes no max-w of its own — verified); `AllocationsTabs.tsx:127` is the
dynamic-import loading skeleton — change it too so the skeleton↔loaded width is consistent (no
flash-narrow), even though it is not the binding wrapper. The other `max-w-[1100px]` pages
(AllocationDashboardV2 Overview empty state, demo/layout, security/page) are explicitly out of scope
and must stay 1100.

Output: 3 one-literal edits + a source-assertion test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-UI-SPEC.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-RESEARCH.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-03-SUMMARY.md

<interfaces>
<!-- The exact literal sites, grep-confirmed. -->

IN SCOPE (1100 → 1440):
  src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1810
    className="mx-auto max-w-[1100px] py-12"               (empty-state container)
  src/app/(dashboard)/allocations/components/ScenarioComposer.tsx:1860
    className="mx-auto flex max-w-[1100px] flex-col"       (main composer body — BINDING)
  src/app/(dashboard)/allocations/AllocationsTabs.tsx:127
    className="mx-auto max-w-[1100px] py-6"                (Scenario-tab loading skeleton)

OUT OF SCOPE (must stay 1100):
  src/app/(dashboard)/allocations/AllocationDashboardV2.tsx:157
    className="mx-auto mt-8 max-w-[1100px] py-12 text-center"   (Overview empty state)
  src/app/.../demo/layout.tsx, src/app/.../security/page.tsx    (other pages — untouched)

Note: line numbers may shift by ±a few after Plan 03's call-site swap in ScenarioComposer.tsx —
locate by the className text, not the absolute line.
</interfaces>

<analog>
Self — in-place literal edits. No logic. The source-assertion test analog: any existing
source-text-reading test in the repo (read a file via fs, assert the literal count) — e.g. the DESIGN
token regex-assertion tests referenced in STATE.md. Read the file content and assert substring
presence/absence; do NOT render.
</analog>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Relax the 3 in-scope width literals + pin with a source-assertion test</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx, src/app/(dashboard)/allocations/AllocationsTabs.tsx, src/app/(dashboard)/allocations/widgets/performance/composer-width.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (the two max-w-[1100px] literals near :1810 and :1860 — locate by className text)
    - src/app/(dashboard)/allocations/AllocationsTabs.tsx (:127 skeleton max-w-[1100px])
    - src/app/(dashboard)/allocations/AllocationDashboardV2.tsx (:157 — the out-of-scope literal that MUST stay 1100)
  </read_first>
  <action>
    Change `max-w-[1100px]` → `max-w-[1440px]` at the two ScenarioComposer container literals
    (empty-state container ~:1810 and main composer body ~:1860 — locate by the className text
    `mx-auto max-w-[1100px] py-12` and `mx-auto flex max-w-[1100px] flex-col`) and at the
    AllocationsTabs.tsx Scenario-tab loading skeleton (~:127, `mx-auto max-w-[1100px] py-6`). Use the
    literal `max-w-[1440px]` (matches the explicit criterion + the existing literal convention). Do NOT
    touch AllocationDashboardV2.tsx:157, demo/layout, or security/page. Then create
    `composer-width.test.tsx`: read each source file's text via fs and assert (a) the two ScenarioComposer
    container literals are `max-w-[1440px]` (and the old `max-w-[1100px]` is absent from those exact
    container classNames), (b) AllocationsTabs.tsx:127's skeleton is `max-w-[1440px]`, and (c)
    AllocationDashboardV2.tsx STILL contains `max-w-[1100px]` (the out-of-scope guard — proves the change
    was scoped). Assert by stable className substrings, not absolute line numbers (Plan 03 may have shifted
    them). This is a static source assertion — no render, no DOM.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/composer-width.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run ".../composer-width.test.tsx"` passes (3 in-scope = 1440; out-of-scope stays 1100).
    - `grep -c "max-w-\[1440px\]" src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` returns 2.
    - `grep -c "max-w-\[1440px\]" src/app/(dashboard)/allocations/AllocationsTabs.tsx` returns 1.
    - `grep -c "max-w-\[1100px\]" src/app/(dashboard)/allocations/AllocationDashboardV2.tsx` returns >= 1 (out-of-scope literal untouched).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The 3 in-scope composer-width literals are max-w-[1440px]; AllocationDashboardV2.tsx:157 stays max-w-[1100px]; the source-assertion test pins both the change and the out-of-scope boundary; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Pure CSS utility-class literal change. No data, no input, no auth, no persistence crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04-01 | Tampering | width-literal scope creep (changing an out-of-scope container) | mitigate | The source-assertion test explicitly pins AllocationDashboardV2.tsx still at 1100, so an accidental over-broad edit fails CI. |
| T-38-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan (CSS literal edits only). RESEARCH §Package Legitimacy Audit: zero external packages this phase. |
</threat_model>

<verification>
- `npx vitest run ".../composer-width.test.tsx"` green.
- grep counts confirm 3 in-scope = 1440, out-of-scope = 1100.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- The whole composer body widens to max-w-[1440px] (PARITY-02).
- The out-of-scope Overview empty-state container stays max-w-[1100px].
- Source-assertion test pins both directions.
</success_criteria>

<output>
Create `.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-04-SUMMARY.md` when done.
</output>
