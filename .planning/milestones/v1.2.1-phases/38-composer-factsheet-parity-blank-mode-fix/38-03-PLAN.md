---
phase: 38-composer-factsheet-parity-blank-mode-fix
plan: 03
type: execute
wave: 2
depends_on: ["38-01", "38-02"]
files_modified:
  - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx
  - src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx
  - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx
autonomous: true
requirements: [PARITY-01]

must_haves:
  truths:
    - "The composer's equity + drawdown charts render through the REAL factsheet TimeSeriesChart + MasterBrush (wheel-zoom / drag-pan / brush / keyboard nav inherited)."
    - "Equity and drawdown share ONE brush-zoom window — they mount under a single FactsheetProvider, so pan/zoom on either moves both."
    - "The scenario blend is the accent-teal strategy line; the benchmark is the muted comparator — colored via resolveSeries, never inline strokes."
    - "The provider mount sets persist=false, so a scenario pan never rewrites the dashboard URL or writes a factsheet-v2: localStorage blob."
    - "The 3M/6M/12M/ALL SegmentedControl is kept and drives the shared xRange via setXRange."
    - "The Overview EquityChartWidget (AllocationDashboardV2) is UNTOUCHED — still on the legacy render; its header test stays green."
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx"
      provides: "the composer-side factsheet-backed equity+drawdown mount (provider + MasterBrush + two TimeSeriesChart) — the new render path"
      exports: ["ScenarioFactsheetChart"]
      min_lines: 60
    - path: "src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx"
      provides: "Q4 proof: one provider ⇒ equity + drawdown share xRange"
      contains: "FactsheetProvider"
  key_links:
    - from: "ScenarioFactsheetChart.tsx"
      to: "buildScenarioFactsheetPayload + FactsheetProvider(persist=false) + MasterBrush + TimeSeriesChart"
      via: "synth payload mounted under one provider"
      pattern: "FactsheetProvider"
    - from: "ScenarioComposer.tsx (call sites :2228 equity / :2259 drawdown)"
      to: "ScenarioFactsheetChart"
      via: "composer swaps to the factsheet-backed component"
      pattern: "ScenarioFactsheetChart"
---

<objective>
Build the composer-side `ScenarioFactsheetChart` component that mounts the REAL factsheet
`TimeSeriesChart` + `MasterBrush` under ONE `FactsheetProvider` (persist=false) fed the synthesized
payload from Plan 01 — delivering full engine reuse (Q1), the shared brush-zoom window (Q4), and the
scenario→strategy / benchmark→comparator color contract (Q2). Switch the composer's two chart call
sites (`ScenarioComposer.tsx:2228` equity, `:2259` drawdown) to this single stacked component. The
Overview `EquityChartWidget` (AllocationDashboardV2) is left entirely on the legacy render.

Purpose: This is the core of PARITY-01 — the convergence the governing principle demands ("the
scenario should look exactly the same and use the same factsheet assets"). Phase 30 gave the composer
factsheet chart-token identity; this gives it factsheet component identity. Mounting both panels under
ONE provider IS the entire Q4 shared-window mechanism (do NOT lift a parallel range). Scoping the swap
to a NEW composer-side component (not an in-place re-back of EquityChart.tsx) keeps the Overview widget
+ its 328-line header test on the legacy render — the LOAD-BEARING scope boundary.

Output: `ScenarioFactsheetChart.tsx` (provider mount), the shared-window test, and the composer
call-site swap. DrawdownChart moves off Recharts by being rendered through this component's factsheet
drawdown config rather than its old Recharts path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-RESEARCH.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-PATTERNS.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-UI-SPEC.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-01-SUMMARY.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-02-SUMMARY.md

<interfaces>
<!-- The mount template + the props the composer plumbs today. Extracted from the codebase. -->

From Plan 01 (src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts):
  buildScenarioFactsheetPayload({ scenario, baseline?, benchmark?, strategyId? }): FactsheetPayload
  SCENARIO_EQUITY_CONFIG: ChartConfig    // stratField "strategyEquity", comparatorField "cumulative"
  SCENARIO_DRAWDOWN_CONFIG: ChartConfig  // stratField "strategyDrawdowns", fill:true

From Plan 02 (src/app/factsheet/[id]/v2/factsheet-context.tsx):
  <FactsheetProvider payload={synthPayload} persist={false}>...</FactsheetProvider>

The factsheet chart assets (reuse verbatim — context-bound, NOT prop-driven):
  src/app/factsheet/[id]/v2/TimeSeriesChart.tsx  — export default/named TimeSeriesChart;
    takes ONLY `config: ChartConfig` (TimeSeriesChart.tsx:31); reads payload/xRange/comparator from context.
  src/app/factsheet/[id]/v2/MasterBrush.tsx      — export MasterBrush; takes NO props; draws payload.strategyEquity.

The mount template to mirror (FactsheetView.tsx:78-84 provider wrap + :195/:203 chart stack):
  <FactsheetProvider payload={synthPayload} persist={false}>
    <MasterBrush />                                       {/* draws scenario equity sparkline */}
    <TimeSeriesChart config={SCENARIO_EQUITY_CONFIG} />   {/* scenario (strat) + benchmark (comparator) */}
    <TimeSeriesChart config={SCENARIO_DRAWDOWN_CONFIG} /> {/* SAME provider ⇒ shared xRange (Q4) */}
  </FactsheetProvider>

Composer props plumbed TODAY at the call sites (ScenarioComposer.tsx:2228 / :2259) — these become the
ScenarioFactsheetChart inputs:
  equityDailyPoints={baselineEquityDailyPoints}     // [] in blank mode (ScenarioComposer.tsx:527-529)
  scenarioSeries={scenarioWealthSeries}             // WealthPoint[] (toWealth, :1565)
  benchmark={btcWealth}                             // DailyPoint[] | undefined (:1020)
  scenarioDailyPoints={scenarioDailyPointsForDrawdown}  // DailyPoint[] (:1627) — for drawdown derivation
  stale / lastSyncAt — blank mode passes false/null (honesty)

Q3 — the 3M/6M/12M/ALL SegmentedControl: the factsheet has no period buttons; the composer keeps its
own. Translate a period click into setXRange([startIdx,endIdx]) over the scenario date axis (setXRange
already clamps to MIN_VISIBLE_SAMPLES at factsheet-context.tsx:194-205) — NOT the legacy sliceByPeriod.

XRange shared-window mechanism (factsheet-context.tsx:328-331): useXRange() returns the single
{xRange,setXRange,resetXRange}; both TimeSeriesChart instances under one provider read it.
</interfaces>

<analog>
The provider→chart mount template: src/app/factsheet/[id]/v2/FactsheetView.tsx (FactsheetProvider wrap
:78-84; MasterBrush placement :195; TimeSeriesChart map :351-355). Copy this wiring verbatim — it is
THE pattern the governing principle mandates.

The benchmark date-alignment is already done inside Plan 01's adapter; this component just passes the
composer's date-keyed props into buildScenarioFactsheetPayload.

Drawdown re-back: chart-configs.ts#underwaterAcc (:206-218) rendered through TimeSeriesChart is the
factsheet's OWN drawdown rendering — Plan 01 already exports SCENARIO_DRAWDOWN_CONFIG shaped like it.
deriveSnapshotDrawdowns (DrawdownChart.tsx:61) feeds strategyDrawdowns inside the Plan 01 adapter.

⚠️ SCOPE BOUNDARY (PATTERNS.md "Critical Scope Boundary"): EquityChart.tsx is BOTH the named export
EquityChart (composer) AND the default-export EquityChartWidget (Overview, AllocationDashboardV2 —
OUT of scope). Do NOT re-back EquityChart.tsx in place. Create the NEW ScenarioFactsheetChart component
and switch ONLY the two composer call sites to it. The Overview path and EquityChartWidget.header.test.tsx
must stay on the legacy render.
</analog>

<scope_note>
Honesty chrome the composer owns (NOT moved into the factsheet engine — keep at the call site /
wrapping ScenarioFactsheetChart): the "PROJECTED — hypothetical, not your live book" pill
(ScenarioComposer.tsx:1874 — stays rendered unconditionally, NEVER weakened in blank mode), the
"BTC Benchmark" toggle (:2239-2252), and the scenario data-testid the existing tests query
(`data-testid="equity-chart-scenario-overlay"`). Preserve a stable test hook on the new scenario line
so EquityChart.scenario.test.tsx assertions can be retargeted in Plan 05 (carry the same data-testid
or an equivalent documented in the SUMMARY). Keep the DrawdownChart `WidgetProps` call-site signature
(`data={{}} timeframe="ALL" width={6} height={4}`) compatible if any consumer still passes it, but the
new render reads the synth payload, not `data`.
</scope_note>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ScenarioFactsheetChart (provider mount, Q1/Q2/Q4) + shared-window test</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx, src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx</files>
  <read_first>
    - src/app/factsheet/[id]/v2/FactsheetView.tsx (:78-84 provider wrap, :188-205 MasterBrush + chart stack, :351-355 TimeSeriesChart map — the exact wiring to mirror)
    - src/app/factsheet/[id]/v2/TimeSeriesChart.tsx (:31-71 — config-only prop, context reads, resolveSeries call)
    - src/app/factsheet/[id]/v2/MasterBrush.tsx (:36-49 — no props, draws payload.strategyEquity)
    - src/app/factsheet/[id]/v2/factsheet-context.tsx (:328-368 — useXRange shared value; the persist prop from Plan 02)
    - src/app/(dashboard)/allocations/widgets/performance/scenario-factsheet-payload.ts (Plan 01 — buildScenarioFactsheetPayload + the two configs)
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx (:467-510 props + hasScenario :507; the SegmentedControl period UI to lift into the wrapper)
  </read_first>
  <action>
    Create `ScenarioFactsheetChart.tsx` ("use client") that takes the composer's existing chart props
    (`equityDailyPoints`, `scenarioSeries`, `benchmark`, `scenarioDailyPoints`, `stale`, `lastSyncAt`,
    plus the period-control props). Call `buildScenarioFactsheetPayload({ scenario: scenarioSeries,
    baseline: equityDailyPoints, benchmark })` to get the synth payload, then mount EXACTLY the
    FactsheetView template: `<FactsheetProvider payload={synthPayload} persist={false}>` wrapping
    `<MasterBrush/>` + `<TimeSeriesChart config={SCENARIO_EQUITY_CONFIG}/>` +
    `<TimeSeriesChart config={SCENARIO_DRAWDOWN_CONFIG}/>` (the two panels under ONE provider = Q4).
    Keep the 3M/6M/12M/ALL SegmentedControl in the wrapper (Q3) and translate a period click into a
    `setXRange([startIdx,endIdx])` over the scenario date axis (compute the index window from the period
    against `synthPayload.dates`; rely on the provider's existing clamp). To call setXRange from the
    wrapper, render the period control as a small CHILD inside the provider that reads `useXRange()`
    (the SegmentedControl cannot live outside the provider and still drive the shared range). Render the
    "PROJECTED — hypothetical" pill and BTC-benchmark toggle as composer-owned chrome around the provider
    (do NOT push them into the factsheet engine; do NOT weaken the pill in blank mode). Carry a stable
    `data-testid` on the scenario strategy line wrapper (preserve `equity-chart-scenario-overlay` or an
    equivalent, documented in the SUMMARY) so Plan 05 can assert overlay presence. Do NOT inline any
    stroke — color flows through resolveSeries via the Plan-01 configs. Do NOT lift a parallel xRange
    state. Then write `scenario-shared-window.test.tsx`: render ScenarioFactsheetChart with a scenario +
    benchmark, assert BOTH a `MasterBrush` (its "Master timeline brush" affordance / aria) and TWO chart
    SVGs (`role="img"`, `tabIndex=0`) are present under ONE provider; drive a `setXRange` (via a brush/
    keyboard interaction or by asserting both charts subscribe to the same XRangeContext) and assert the
    equity and drawdown panels reflect the SAME window (Q4 — one provider ⇒ shared xRange). The
    load-bearing assertion: there is exactly ONE FactsheetProvider / one XRangeContext, not two.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run ".../performance/scenario-shared-window.test.tsx"` passes (MasterBrush + two charts under one provider; equity & drawdown share the window).
    - `grep -c "FactsheetProvider" .../ScenarioFactsheetChart.tsx` returns 1 (exactly one provider — Q4; not a parallel range).
    - `grep -n "persist={false}" .../ScenarioFactsheetChart.tsx` matches (URL/localStorage suppression on the composer mount).
    - `grep -vn '^[[:space:]]*//' .../ScenarioFactsheetChart.tsx | grep -c 'stroke=\|color:' ` returns 0 (no inline strokes; color via resolveSeries).
    - `grep -n "MasterBrush" .../ScenarioFactsheetChart.tsx` and `grep -c "TimeSeriesChart" .../ScenarioFactsheetChart.tsx` (>=2) confirm the real factsheet assets are mounted (reuse, not lookalike).
    - `grep -n "useXRange\|setXRange" .../ScenarioFactsheetChart.tsx` shows the SegmentedControl drives the shared range (Q3), not sliceByPeriod.
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>ScenarioFactsheetChart mounts the real TimeSeriesChart + MasterBrush under ONE persist=false FactsheetProvider fed the synth payload; equity + drawdown share one xRange (Q4); the SegmentedControl drives setXRange (Q3); scenario=accent strategy line, benchmark=muted comparator via resolveSeries (Q2); no inline strokes; shared-window test green; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Swap composer call sites to ScenarioFactsheetChart (scope boundary honored)</name>
  <files>src/app/(dashboard)/allocations/components/ScenarioComposer.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/components/ScenarioComposer.tsx (:2222-2270 — the EquityChart :2228 + DrawdownChart :2259 call sites, the "PROJECTED" pill :1874, the BTC toggle :2239-2252; baselineEquityDailyPoints :527-529, scenarioWealthSeries :1565, scenarioDailyPointsForDrawdown :1627, btcWealth :1020)
    - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx (Task 1 — the new component's prop surface)
    - src/app/(dashboard)/allocations/AllocationDashboardV2.tsx (confirm the Overview still imports the legacy EquityChartWidget — must NOT change)
  </read_first>
  <action>
    Replace the composer's two chart renders (the `<EquityChart .../>` at :2228 and the
    `<DrawdownChart .../>` at :2259) with a single `<ScenarioFactsheetChart .../>` that receives the
    same data props already in scope: `equityDailyPoints={baselineEquityDailyPoints}`,
    `scenarioSeries={scenarioWealthSeries}`, `benchmark={btcWealth}`,
    `scenarioDailyPoints={scenarioDailyPointsForDrawdown}`, `stale={isBlankMode ? false : allKeysStale}`,
    `lastSyncAt={isBlankMode ? null : lastSyncAt}`, plus the existing period-control props. Keep the
    "PROJECTED — hypothetical, not your live book" pill (:1874) rendered unconditionally and the
    "BTC Benchmark" toggle (:2239-2252) intact around the new component (do NOT remove or weaken either;
    blank mode must still show the pill). Remove the now-dead legacy EquityChart/DrawdownChart imports
    from ScenarioComposer.tsx ONLY IF they are no longer referenced anywhere else in the file (grep
    first). Do NOT touch AllocationDashboardV2.tsx or the EquityChartWidget default export — the Overview
    stays on the legacy render. Run the composer + tabs integration suites; some child-chart-presence
    assertions may need retargeting to the new component (retarget, do not delete coverage).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/components/ScenarioComposer.test.tsx" "src/app/(dashboard)/allocations/components/ScenarioComposer.save.test.tsx" "src/app/(dashboard)/allocations/widgets/performance/EquityChartWidget.header.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "ScenarioFactsheetChart" src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` shows the composer renders the new component at the former equity/drawdown call sites.
    - `npx vitest run ".../EquityChartWidget.header.test.tsx"` passes UNCHANGED (the 328-line Overview header test — proves the scope boundary held; EquityChartWidget legacy render untouched).
    - `git diff --name-only` does NOT include `AllocationDashboardV2.tsx` or `EquityChart.tsx` (Overview path untouched in this plan).
    - `grep -n "PROJECTED\|hypothetical" src/app/(dashboard)/allocations/components/ScenarioComposer.tsx` confirms the honesty pill is still present (unconditional).
    - ScenarioComposer + save integration suites pass (presence assertions retargeted to ScenarioFactsheetChart, no coverage deleted).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>The composer renders ScenarioFactsheetChart at both former chart call sites with the same data props; the PROJECTED pill + BTC toggle survive; the Overview EquityChartWidget + its header test are untouched and green; integration suites pass with retargeted presence assertions; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composer dashboard tab → chart view-state (URL + localStorage) | The reused FactsheetProvider would, by default, write `?range=` to the dashboard URL and a `factsheet-v2:` localStorage blob on every pan. The composer mount must NOT cross this boundary. |
| synthesized payload → factsheet chart engine | The payload is built client-side from data already in the composer (frozen engine output). No new data crosses any boundary; nothing fetched/sent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-03-01 | Tampering (UX) | FactsheetProvider mounted in the composer | mitigate | `persist={false}` (Plan 02 opt-out) gates both the URL replaceState and the localStorage write, so scenario pans cannot rewrite the dashboard URL or write a `factsheet-v2:` key. Asserted by the `grep persist={false}` acceptance check + the Plan 02 negative tests. |
| T-38-03-02 | Information Disclosure | scenario series rendered as SVG text/tooltips | accept | Series names/values are numeric or the trusted "Scenario"/benchmark labels, rendered as React-escaped SVG `<text>`; no dangerouslySetInnerHTML in any chart file. No PII, no AUM in the composer chart (KpiStrip is not mounted here). |
| T-38-03-03 | Information Disclosure | TimeSeriesChart ExportMenu emits factsheet_v2_chart_export PostHog events | accept | Reusing the factsheet chart fires factsheet-named analytics from the composer; this is a benign analytics-hygiene note (RESEARCH A4), no data leak. Documented in the SUMMARY; no mitigation required for ASVS L1. |
| T-38-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan (composes existing first-party modules only). RESEARCH §Package Legitimacy Audit: zero external packages this phase. |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/widgets/performance/scenario-shared-window.test.tsx"` green (Q4 one provider).
- `npx vitest run "src/app/(dashboard)/allocations/widgets/performance/"` green (the perf-dir suites — DrawdownChart Recharts-DOM assertions that the swap invalidates are retargeted; behavioral assertions stay valid).
- `npx vitest run "src/app/factsheet/"` green (reuse, not fork — factsheet files untouched in this plan; the only factsheet change is the additive persist prop landed in Plan 02).
- `npx vitest run ".../EquityChartWidget.header.test.tsx"` green (scope boundary held).
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Composer equity + drawdown render through the real factsheet TimeSeriesChart + MasterBrush (Q1).
- One provider ⇒ one shared brush-zoom window across equity + drawdown (Q4).
- Scenario = accent strategy line, benchmark = muted comparator via resolveSeries (Q2).
- SegmentedControl kept, drives setXRange (Q3).
- persist=false ⇒ no dashboard URL rewrite / no factsheet-v2: localStorage write.
- Overview EquityChartWidget untouched; its header test green (scope boundary).
</success_criteria>

<output>
Create `.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-03-SUMMARY.md` when done.
Record the scenario-line `data-testid` you carried so Plan 05 can assert against it.
</output>
