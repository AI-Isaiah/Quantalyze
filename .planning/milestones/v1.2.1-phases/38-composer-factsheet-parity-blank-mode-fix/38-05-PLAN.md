---
phase: 38-composer-factsheet-parity-blank-mode-fix
plan: 05
type: execute
wave: 3
depends_on: ["38-03"]
files_modified:
  - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx
  - src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx
autonomous: true
requirements: [PARITY-03]

must_haves:
  truths:
    - "In blank-slate mode (empty baseline, scenario present) the composer's NEW factsheet-backed equity render shows the scenario overlay — the scenario line renders, NO synthetic baseline, the PROJECTED pill is present, and the 'Equity data warming up' copy is absent."
    - "The legacy EquityChart projection guard (~:675) no longer bails on an empty baseline before considering hasScenario — it mirrors DrawdownChart.tsx:147's `&& !hasScenario` so the legacy path is also correct."
  artifacts:
    - path: "src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx"
      provides: "legacy projection guard fixed to mirror DrawdownChart's hasScenario guard"
      contains: "hasScenario"
    - path: "src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx"
      provides: "blank-slate render case on the FINAL composer render path (load-bearing PARITY-03 proof) + retargeted scenario-overlay assertions"
      contains: "scenario"
  key_links:
    - from: "EquityChart projection guard (~:675)"
      to: "hasScenario (EquityChart.tsx:507)"
      via: "&& !hasScenario (mirror DrawdownChart.tsx:147)"
      pattern: "&& !hasScenario"
    - from: "EquityChart.scenario.test.tsx blank-slate case"
      to: "ScenarioFactsheetChart (the new composer render path)"
      via: "empty baseline + scenario ⇒ overlay renders"
      pattern: "equity-chart-scenario-overlay"
---

<objective>
Satisfy PARITY-03 (blank-slate equity renders the scenario overlay) on TWO surfaces: (1) the
LOAD-BEARING proof on the composer's NEW factsheet-backed render path (Plan 03's
ScenarioFactsheetChart) — an empty baseline + a present scenario still renders the scenario overlay,
with no synthetic baseline and the PROJECTED pill intact; and (2) the one-line correctness fix on the
LEGACY EquityChart projection guard (~:675) so it mirrors `DrawdownChart.tsx:147`'s `hasScenario`
pattern and never bails on an empty baseline before considering the scenario.

Purpose: PARITY-03. The bug is `EquityChart.tsx:675`
(`if (equityDailyPoints.length === 0 || composite.length === 0) return null;`) bailing in blank mode
before `hasScenario` is considered, so a blank-slate scenario renders nothing. After Plan 03 the
composer renders through the factsheet engine where the data precondition is already satisfied (Plan
01's adapter emits a non-empty `strategyEquity` from the scenario even with an empty baseline) — so the
load-bearing PARITY-03 proof lives on the NEW path. The legacy guard fix keeps the (still-used,
out-of-composer) EquityChart correct too. RESEARCH is explicit: satisfy PARITY-03 in whatever the FINAL
render path is, not merely patch the old guard.

Output: the legacy guard fix + a blank-slate render case on the new path + retargeted scenario-overlay
assertions in the scenario suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-UI-SPEC.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-RESEARCH.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-PATTERNS.md
@.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-03-SUMMARY.md

<interfaces>
<!-- The guard relationship + the new render path the blank-slate proof targets. -->

The bug (EquityChart.tsx:668-676 — the projection useMemo):
  const projection = useMemo(() => {
    if (equityDailyPoints.length === 0 || composite.length === 0) {   // ← :675 bails before hasScenario
      return null;
    }
    ...

The correct pattern to mirror (DrawdownChart.tsx:147):
  if (liveDrawdownData.length === 0 && !hasScenario) { /* empty state */ }

hasScenario already exists (EquityChart.tsx:507):
  const hasScenario = !!scenarioSeries && scenarioSeries.length > 0;

The FINAL composer render path (Plan 03):
  ScenarioFactsheetChart (src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx)
  — renders the scenario through the factsheet TimeSeriesChart fed Plan 01's synth payload. The
    blank-slate data precondition (non-empty strategyEquity from scenario alone) is proven in Plan 01;
    this plan proves the RENDER shows the overlay end-to-end. Use the scenario-line data-testid recorded
    in 38-03-SUMMARY.md (e.g. equity-chart-scenario-overlay).

Honesty contract (UI-SPEC §Blank-Slate / §Copywriting — must hold on the new path):
  - Render the scenario overlay ONLY — NO synthetic/fabricated baseline line.
  - "PROJECTED — hypothetical, not your live book" pill present (unconditional).
  - "Equity data warming up" copy ABSENT when a scenario exists.
</interfaces>

<analog>
The guard fix analog: DrawdownChart.tsx:147 (`&& !hasScenario`) — copy the boolean shape exactly.
The blank-slate test analog: EquityChart.scenario.test.tsx existing cases (makeWealthSeries helper :27,
data-testid query `equity-chart-scenario-overlay` :55) — add a case where equityDailyPoints is `[]`
(or absent) and a scenario IS supplied, asserting the overlay renders, no synthetic baseline, PROJECTED
pill present, "warming up" copy absent. Retarget the existing overlay-presence assertions to the new
factsheet-backed render path / testid recorded in 38-03-SUMMARY.md (rewrite assertions, do NOT delete
coverage — coverage is a BLOCKING gate).
</analog>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Blank-slate proof on the new path + legacy guard fix (TDD)</name>
  <files>src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx, src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx (:507 hasScenario, :668-676 the projection guard to fix)
    - src/app/(dashboard)/allocations/widgets/performance/DrawdownChart.tsx:147 (the `&& !hasScenario` pattern to mirror)
    - src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx (existing cases :27 helper, :55 data-testid query — the assertions to retarget + the blank-slate case to add)
    - src/app/(dashboard)/allocations/widgets/performance/ScenarioFactsheetChart.tsx (Plan 03 — the new render path the blank-slate proof targets)
    - .planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-03-SUMMARY.md (the scenario-line data-testid Plan 03 carried)
  </read_first>
  <behavior>
    - Blank-slate on the NEW path: render the composer scenario chart (ScenarioFactsheetChart, or
      EquityChart.scenario suite's equivalent that exercises the new path) with `equityDailyPoints={[]}`
      and a non-empty `scenarioSeries` ⇒ the scenario overlay element (the testid from 38-03-SUMMARY) is
      present; the "Equity data warming up" copy is ABSENT; no synthetic/fabricated baseline line is drawn
      (assert only the scenario series renders, not a second flat baseline series); the
      "PROJECTED — hypothetical" pill is present.
    - Legacy guard correctness: the EquityChart projection no longer returns null when
      `equityDailyPoints.length === 0` but `hasScenario` is true — the guard short-circuits ONLY when
      there is neither a baseline nor a scenario. (Mutation check: removing `&& !hasScenario` makes the
      blank-slate-renders assertion fail.)
    - Non-regression: a populated baseline (no scenario) still renders exactly as before; a populated
      baseline + scenario still renders both.
  </behavior>
  <action>
    Fix the legacy guard at EquityChart.tsx:675: change
    `if (equityDailyPoints.length === 0 || composite.length === 0) return null;` so it mirrors
    DrawdownChart.tsx:147 — short-circuit only when BOTH the baseline is empty AND there is no scenario
    (add the `&& !hasScenario` term; preserve the `composite.length === 0` condition's intent so a truly
    empty render still bails, but never bail when a scenario exists). `hasScenario` is already defined at
    :507. Keep the change surgical (one boolean). Do NOT introduce a synthetic baseline anywhere — blank
    mode renders the scenario only (honesty). Then update EquityChart.scenario.test.tsx: retarget the
    existing overlay-presence assertions to the new factsheet-backed render path / the data-testid recorded
    in 38-03-SUMMARY.md, and ADD the blank-slate case (empty baseline + scenario ⇒ overlay renders, no
    synthetic baseline, PROJECTED pill present, "warming up" copy absent). Write/extend the test FIRST so
    the blank-slate case is RED against the unfixed guard (or against the new path if the precondition
    flows from Plan 01), then implement to GREEN. Do NOT delete coverage — rewrite assertions that the
    engine swap invalidated (coverage is a BLOCKING gate; the deleted-projection-memo coverage is offset
    by Plan 01's adapter test — note that offsetting decision in this plan's SUMMARY).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/widgets/performance/EquityChart.scenario.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run ".../EquityChart.scenario.test.tsx"` passes incl. the new blank-slate case (overlay renders, no synthetic baseline, PROJECTED present, "warming up" absent).
    - `grep -n "&& !hasScenario" src/app/(dashboard)/allocations/widgets/performance/EquityChart.tsx` matches the projection guard (legacy path correct, mirrors DrawdownChart.tsx:147).
    - The blank-slate assertion is mutation-verified: temporarily removing `&& !hasScenario` (or the new-path precondition) makes the blank-slate case RED (document the mutation check in the SUMMARY; revert).
    - `grep -c "warming up" .../EquityChart.scenario.test.tsx` shows the blank-slate case asserts the warm-up copy is ABSENT (not present).
    - `npx vitest run ".../performance/"` green (DrawdownChart blank-slate guard regression at :147 still green; no coverage deleted).
    - `npx tsc --noEmit` clean.
  </acceptance_criteria>
  <done>Blank-slate (empty baseline + scenario) renders the scenario overlay on the new composer path with no synthetic baseline, PROJECTED pill present, warming-up copy absent; the legacy EquityChart guard mirrors DrawdownChart's hasScenario pattern; the proof is mutation-verified falsifiable; no coverage deleted; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blank-slate render → user reading | The honesty concern: blank mode must NOT fabricate a baseline line (there is no live book) — a synthetic flat baseline would be a dishonest data claim. The render shows the scenario overlay only, framed "PROJECTED — hypothetical". No data/auth/persistence boundary is crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-05-01 | Spoofing (data honesty) | blank-slate equity render | mitigate | The blank-slate case asserts NO synthetic baseline is drawn and the "PROJECTED — hypothetical, not your live book" pill is present — preventing a fabricated live-book line from being presented as real. Mutation-verified. |
| T-38-05-02 | Information Disclosure | scenario overlay in blank mode | accept | The scenario series is the allocator's own hypothetical built client-side; no AUM/PII (KpiStrip not mounted in the composer chart). React-escaped SVG text. |
| T-38-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this plan (boolean guard edit + test). RESEARCH §Package Legitimacy Audit: zero external packages this phase. |
</threat_model>

<verification>
- `npx vitest run ".../EquityChart.scenario.test.tsx"` green (incl. blank-slate case).
- `npx vitest run ".../performance/"` green (DrawdownChart :147 regression intact; no coverage deleted).
- `grep "&& !hasScenario" EquityChart.tsx` confirms the legacy guard fix.
- `npx tsc --noEmit` clean.
</verification>

<success_criteria>
- Blank-slate (empty baseline + scenario) renders the scenario overlay on the FINAL composer path — no synthetic baseline, PROJECTED pill present, "warming up" copy absent.
- The legacy EquityChart guard mirrors DrawdownChart.tsx:147 (`&& !hasScenario`).
- The proof is mutation-verified falsifiable.
- No coverage deleted (deletions offset by Plan 01's adapter test).
</success_criteria>

<output>
Create `.planning/phases/38-composer-factsheet-parity-blank-mode-fix/38-05-SUMMARY.md` when done.
Record the mutation-check result and the coverage-offset decision (deleted projection-memo tests
removed vs repurposed).
</output>
