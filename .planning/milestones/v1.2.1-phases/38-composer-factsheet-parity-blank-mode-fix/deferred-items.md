# Deferred Items — Phase 38

Out-of-scope discoveries logged during execution (NOT fixed; pre-existing or unrelated to the current task).

- [38-04] AllocationsTabs.tsx:33 — `trackUsageEventClient` imported but never used (`@typescript-eslint/no-unused-vars` warning). PRE-EXISTING (confirmed via stash: present on unmodified code), unrelated to the one-line skeleton-width edit. Not fixed — out of scope per the executor SCOPE BOUNDARY rule.
