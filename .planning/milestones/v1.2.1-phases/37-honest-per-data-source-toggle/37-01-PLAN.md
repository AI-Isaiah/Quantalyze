---
phase: 37-honest-per-data-source-toggle
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/queries.ts
  - src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts
  - src/lib/queries.my-allocation.test.ts
autonomous: true
requirements: [DSRC-01]
tags: [scenario, per-key-dailies, payload, allocations]

must_haves:
  truths:
    - "The dashboard payload carries each of the allocator's own per-api_key daily-return series (perKeyReturnsByApiKeyId)"
    - "The payload carries the Phase-36 D3 all-or-nothing gate result (perKeyDailiesGateSatisfied) and the eligible-key id list (eligibleApiKeyIds)"
    - "Both return branches (!portfolio and main) carry the three new fields with the same type contract"
    - "liveBaselineMetrics and holdingReturnsByScopeRef remain byte-identical to Phase 36 (no repoint)"
    - "perKeyReturnsByApiKeyId keys are a subset of the allocator's own apiKeys[].id (no cross-tenant series)"
  artifacts:
    - path: "src/lib/queries.ts"
      provides: "Three additive payload fields on MyAllocationDashboardPayload + both return sites; hoisted gate const"
      contains: "perKeyDailiesGateSatisfied"
    - path: "src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts"
      provides: "Payload-shape pins for the three new fields + byte-identity guard"
      contains: "perKeyReturnsByApiKeyId"
    - path: "src/lib/queries.my-allocation.test.ts"
      provides: "Integration assertion the new fields appear on a seeded per-key fixture + both-branch defaults"
      contains: "perKeyDailiesGateSatisfied"
  key_links:
    - from: "src/lib/queries.ts getMyAllocationDashboard return (:3122 + :3437)"
      to: "MyAllocationDashboardPayload type (:1493)"
      via: "three additive fields present on both branches"
      pattern: "perKeyReturnsByApiKeyId|perKeyDailiesGateSatisfied|eligibleApiKeyIds"
    - from: "hoisted const perKeyDailiesGateSatisfied"
      to: "the existing baseline-selection ternary (:3090)"
      via: "single allActiveKeysHavePerKeyDailies(...) call reused for selection AND return"
      pattern: "allActiveKeysHavePerKeyDailies"
---

<objective>
Expose Phase 36's per-key channel on the dashboard payload so the composer (Plans 02/03) can recompute the blend client-side. The values are ALREADY computed inside `getMyAllocationDashboard` (`perKeyReturnsByApiKeyId` at :3075, `eligibleKeyIds` at :3086, the D3 gate at :3090) but are never returned. This plan ADDS three fields to `MyAllocationDashboardPayload` and to BOTH return sites — a purely additive payload contract.

Purpose: This is the DSRC-01 enabler. Without the per-key series on the wire, the client cannot build per-key projection units or recompute on toggle. It must NOT disturb `liveBaselineMetrics` / `holdingReturnsByScopeRef` (Phase 36 pinned those byte-identical).
Output: Three new payload fields, the gate boolean hoisted to a named const, and payload-shape tests on both return branches.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-honest-per-data-source-toggle/37-CONTEXT.md
@.planning/phases/37-honest-per-data-source-toggle/37-RESEARCH.md
@.planning/phases/37-honest-per-data-source-toggle/37-PATTERNS.md
@.planning/phases/37-honest-per-data-source-toggle/37-VALIDATION.md

<interfaces>
<!-- Already in the codebase — executor uses these directly, no exploration needed. -->

From src/lib/queries.ts (existing, reuse verbatim — DO NOT rebuild):
- `buildPerKeyReturnsByApiKeyId(rows): Record<string, DailyPoint[]>` (:2413) — groups raw csv_daily_returns rows by api_key_id, drops null id / non-finite daily_return.
- `allActiveKeysHavePerKeyDailies(eligibleKeyIds: string[], perKeyReturnsByApiKeyId): boolean` (:2362) — the D3 all-or-nothing gate.
- `isPerKeyDailiesEligibleKey(key): boolean` (:2397) — SoT-mirror active-key predicate (Python backfill is authoritative; never re-derive in the browser).
- `MyAllocationDashboardPayload` interface (:1493); existing additive fields to mirror: `holdingReturnsByScopeRef: Record<string, DailyPoint[]>` (:1731), `liveBaselineMetrics: {...}` (:1750).

Existing in getMyAllocationDashboard (all BEFORE the `if (!portfolio)` at :3111 — so real values are available on both branches):
- `perKeyReturnsByApiKeyId` const computed at :3075.
- `eligibleKeyIds` const computed at :3086.
- The gate is called INLINE in the baseline-selection ternary at :3090 (`allActiveKeysHavePerKeyDailies(eligibleKeyIds, perKeyReturnsByApiKeyId) ? ... : ...`).

Both return sites:
- `!portfolio` branch return object: queries.ts:3122–3139 (carries holdingReturnsByScopeRef, allocator_id, liveBaselineMetrics).
- main branch return object: queries.ts:3425–3444 (same fields).

Fetch window (resolves RESEARCH Open-Question 1 / Pitfall 6 / A3): the csv_daily_returns read at :2726–2740 uses a 730-day `.gte("date", ...)` window with a flat `.limit(20000)` ceiling and is allocator-scoped (`.eq("allocator_id", userId)`) — every key keeps its full 730-day window; no per-key starvation. No change needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hoist the D3 gate to a const + add three additive payload fields to both return sites</name>
  <files>src/lib/queries.ts</files>
  <read_first>
    - src/lib/queries.ts:1493–1758 (MyAllocationDashboardPayload — the additive-field style at :1731 holdingReturnsByScopeRef and :1750 liveBaselineMetrics is the template)
    - src/lib/queries.ts:3057–3140 (holdingReturnsByScopeRef build, the inline gate ternary at :3089–3098, the !portfolio return at :3122)
    - src/lib/queries.ts:3425–3444 (main return)
    - .planning/phases/37-honest-per-data-source-toggle/37-PATTERNS.md (§"queries.ts — expose per-key channel": exact hoist + both-return-site recipe)
  </read_first>
  <behavior>
    - Type contract: MyAllocationDashboardPayload gains `perKeyReturnsByApiKeyId: Record<string, DailyPoint[]>`, `perKeyDailiesGateSatisfied: boolean`, `eligibleApiKeyIds: string[]`.
    - Both branches return real values (the per-key series + eligible ids + gate are computed before the `if (!portfolio)` split, so the !portfolio branch carries the SAME values, not synthetic empties).
    - liveBaselineMetrics + holdingReturnsByScopeRef are byte-identical to before (no derivation change).
  </behavior>
  <action>
    Hoist the inline D3 gate at queries.ts:3089–3090 into a named const `perKeyDailiesGateSatisfied = allActiveKeysHavePerKeyDailies(eligibleKeyIds, perKeyReturnsByApiKeyId)` placed immediately after `eligibleKeyIds` (:3088), then USE that const in the existing baseline-selection ternary (so `liveBaselineMetricsFromPerKeyDailies` vs `liveBaselineMetricsFromHoldings` selection is unchanged — same single gate call, no behavior change). Add the three fields to `MyAllocationDashboardPayload` (after `liveBaselineMetrics`, mirroring its additive-field JSDoc style): `perKeyReturnsByApiKeyId: Record<string, DailyPoint[]>`, `perKeyDailiesGateSatisfied: boolean`, `eligibleApiKeyIds: string[]`. Add the SAME three lines to BOTH return objects — the `!portfolio` branch (:3122–3139) and the main branch (:3425–3444): `perKeyReturnsByApiKeyId,` `perKeyDailiesGateSatisfied,` `eligibleApiKeyIds: eligibleKeyIds,`. Do NOT touch the `liveBaselineMetrics` or `holdingReturnsByScopeRef` lines (Phase 36 byte-identity pin). Do NOT add a new fetch, query, or eligibility re-derivation — reuse the existing `perKeyReturnsByApiKeyId`/`eligibleKeyIds`/gate consts only (RESEARCH §Don't-Hand-Roll; threat T-37-01-01).
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts src/lib/queries.my-allocation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "perKeyDailiesGateSatisfied" src/lib/queries.ts` shows the hoisted const AND its presence in both return objects (≥3 matches).
    - `grep -c "eligibleApiKeyIds: eligibleKeyIds" src/lib/queries.ts` returns 2 (both branches).
    - `npx tsc --noEmit` (or the project's typecheck) passes — MyAllocationDashboardPayload is satisfied on both branches (Pitfall 2: no branch leaves a required field unset).
    - The baseline-selection ternary still calls the SAME gate (now via the const); `liveBaselineMetrics` derivation is unchanged.
  </acceptance_criteria>
  <done>Three additive fields exist on the payload type and on both return objects; the D3 gate is a single hoisted const reused for selection and return; liveBaselineMetrics/holdingReturnsByScopeRef untouched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the new payload fields on both branches + byte-identity + cross-tenant guards</name>
  <files>src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts, src/lib/queries.my-allocation.test.ts</files>
  <read_first>
    - src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts:227–260 (T_M4 payload-type pins) + :488–560 (Phase 36 liveBaselineMetrics shape-identity block — the byte-identity model)
    - src/lib/queries.my-allocation.test.ts:476–491 (M-0480 liveBaselineMetrics empty-default pin) + :881–901 (payload `toHaveProperty` block) + :1551–1650 (Phase 36 per-key repoint block: seedPerKeyDailiesA / activeKeyA fixtures + the getMyAllocationDashboard("user-1") → assert pattern)
    - .planning/phases/37-honest-per-data-source-toggle/37-PATTERNS.md (§"queries.my-allocation.test.ts + getMyAllocationDashboard.scenario.test.ts": exact pins)
  </read_first>
  <behavior>
    - With a seeded eligible per-key fixture (reuse seedPerKeyDailiesA / activeKeyA): result.perKeyDailiesGateSatisfied === true; result.eligibleApiKeyIds contains the seeded key id; result.perKeyReturnsByApiKeyId has the seeded key's series.
    - Fresh-allocator (!portfolio) branch: the three fields are present and well-typed (gate false / empty series / empty ids when no eligible per-key coverage).
    - Byte-identity: result.liveBaselineMetrics + result.holdingReturnsByScopeRef are unchanged vs the existing Phase 36 expectation.
    - Security pin: every key in result.perKeyReturnsByApiKeyId is a member of result.apiKeys[].id (no foreign keys leak through the new field).
  </behavior>
  <action>
    In `getMyAllocationDashboard.scenario.test.ts`, extend the payload-type pins (model on T_M4 at :227) so a typed `MyAllocationDashboardPayload` literal must include `perKeyReturnsByApiKeyId` / `perKeyDailiesGateSatisfied` / `eligibleApiKeyIds` (a compile-time shape guard), and add a runtime assertion in the Phase-36 shape-identity block (:493) that the additive fields do not alter the liveBaselineMetrics output. In `queries.my-allocation.test.ts`, in the Phase 36 per-key block (:1551), reuse `seedPerKeyDailiesA`/`activeKeyA`/`seedHoldingsKeyA` and assert `result.perKeyDailiesGateSatisfied === true`, `result.eligibleApiKeyIds` contains the seeded key id, and `result.perKeyReturnsByApiKeyId` carries the seeded series; add a `toHaveProperty` block (model :881) for all three fields; add the byte-identity guard `expect(result.liveBaselineMetrics).toEqual(<existing expectation>)` and `expect(result.holdingReturnsByScopeRef).toEqual(<existing>)`; add the cross-tenant guard asserting `Object.keys(result.perKeyReturnsByApiKeyId)` ⊆ `result.apiKeys.map(k => k.id)` (threat T-37-01-01). Add a fresh-allocator/!portfolio fixture assertion that the three fields are present with the empty/false defaults. Do NOT relax or delete any existing Phase 36 assertion.
  </action>
  <verify>
    <automated>npx vitest run src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts src/lib/queries.my-allocation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both suites GREEN; the new assertions fail if Task 1's fields are removed from either branch (mutation check: deleting one branch's field block turns the !portfolio or main assertion red).
    - The byte-identity assertion proves liveBaselineMetrics + holdingReturnsByScopeRef unchanged (Pitfall 2).
    - The cross-tenant subset assertion is present and passing (security — Information Disclosure mitigation).
    - No existing Phase 36 test was weakened (diff shows only additions to these two test files).
  </acceptance_criteria>
  <done>Payload-shape, byte-identity, both-branch, and cross-tenant-subset pins exist and pass; the per-key channel is provably on the wire for both fresh and populated allocators.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR query → RSC payload → client | `getMyAllocationDashboard` runs allocator-scoped (`.eq("allocator_id", userId)`); its return crosses to the client. The new fields ride this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01-01 | Information Disclosure | `perKeyReturnsByApiKeyId` / `eligibleApiKeyIds` new payload fields | mitigate | Derived ONLY from the existing allocator-scoped `csv_daily_returns` read (:2726–2740, `.eq("allocator_id", userId)`) and the allocator's own `apiKeys` — no new query, no cross-allocator fetch. Pinned by a test asserting `perKeyReturnsByApiKeyId` keys ⊆ `apiKeys[].id` (Task 2). The field carries `api_key_id` + (date, daily_return) only — NO api_key secret/ciphertext material crosses the boundary. |
| T-37-01-02 | Tampering | D3 eligibility predicate | accept | `eligibleApiKeyIds` is computed from `isPerKeyDailiesEligibleKey` (the Python-backfill SoT mirror), exposed read-only. The client must not re-derive it (enforced in Plan 03). No client write path to this field. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package install in this phase (RESEARCH §Package Legitimacy Audit: N/A — pure in-repo refactor). |
</threat_model>

<verification>
- `npx vitest run src/lib/__tests__/getMyAllocationDashboard.scenario.test.ts src/lib/queries.my-allocation.test.ts` is green.
- Project typecheck passes (both return branches satisfy the augmented payload type).
- `git diff src/lib/queries.ts` shows ONLY: one hoisted gate const, three fields on the type, and three identical field lines per return branch — no change to liveBaselineMetrics / holdingReturnsByScopeRef lines.
</verification>

<success_criteria>
- MyAllocationDashboardPayload exposes `perKeyReturnsByApiKeyId`, `perKeyDailiesGateSatisfied`, `eligibleApiKeyIds`.
- Both return sites carry the three fields with real (not synthetic) values.
- liveBaselineMetrics + holdingReturnsByScopeRef byte-identical (Phase 36 pin held).
- Cross-tenant subset invariant pinned by test.
</success_criteria>

<output>
Create `.planning/phases/37-honest-per-data-source-toggle/37-01-SUMMARY.md` when done.
</output>
