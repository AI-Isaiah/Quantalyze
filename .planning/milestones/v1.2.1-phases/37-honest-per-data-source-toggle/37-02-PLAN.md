---
phase: 37-honest-per-data-source-toggle
plan: 02
type: execute
wave: 2
depends_on: ["37-01"]
files_modified:
  - src/app/(dashboard)/allocations/lib/scenario-adapter.ts
  - src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts
autonomous: true
requirements: [DSRC-01]
tags: [scenario, scenario-adapter, per-key-dailies, allocations]

must_haves:
  truths:
    - "A sibling builder emits one StrategyForBuilder per api_key_id (id === api_key_id), keyed per data source not per holding (DSRC-01)"
    - "Each per-key unit's daily_returns is its own per-key series; weight is that key's clamped equity share; default selected=true"
    - "Keys with an empty/absent series are skipped"
    - "Weights are RAW equity-share USD — NOT renormalized to sum-to-1 (the frozen engine owns renormalization)"
    - "buildStrategyForBuilderSet's B4 positional signature and the H-0132 commit oracle stay byte-identical (sibling does not touch them)"
  artifacts:
    - path: "src/app/(dashboard)/allocations/lib/scenario-adapter.ts"
      provides: "buildPerKeyStrategyForBuilderSet sibling builder returning { strategies, state }"
      contains: "buildPerKeyStrategyForBuilderSet"
    - path: "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts"
      provides: "Per-key builder unit cases + H-0132 regression still green"
      contains: "per-key"
  key_links:
    - from: "buildPerKeyStrategyForBuilderSet"
      to: "StrategyForBuilder[] + ScenarioState shape"
      via: "id = api_key_id, weight = equityByApiKeyId clamped, selected=true"
      pattern: "buildPerKeyStrategyForBuilderSet"
    - from: "the sibling builder"
      to: "buildStrategyForBuilderSet (untouched)"
      via: "separate function — no shared branch, B4 signature preserved"
      pattern: "export function buildStrategyForBuilderSet"
---

<objective>
Add a sibling pure builder `buildPerKeyStrategyForBuilderSet` beside `buildStrategyForBuilderSet` in `scenario-adapter.ts` that emits one `StrategyForBuilder` per `api_key_id` from the per-key series exposed by Plan 01. This is DSRC-01: the adapter keys projection units per data source (per `api_key`) rather than per blended book. The composer (Plan 03) consumes `{ strategies, state }` and overlays the toggle + collapse + `computeScenario`.

Purpose: Per-key keying is the structural change that makes honest per-source exclusion possible. A SIBLING (not a branch inside `buildStrategyForBuilderSet`) keeps the B4 positional signature and the H-0132 commit oracle tests byte-identical (RESEARCH §Alternatives, A4).
Output: One new exported pure function + unit cases; the existing B4 suite stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-honest-per-data-source-toggle/37-CONTEXT.md
@.planning/phases/37-honest-per-data-source-toggle/37-RESEARCH.md
@.planning/phases/37-honest-per-data-source-toggle/37-PATTERNS.md
@.planning/phases/37-honest-per-data-source-toggle/37-VALIDATION.md
@.planning/phases/37-honest-per-data-source-toggle/37-01-SUMMARY.md

<interfaces>
<!-- Templates to copy from — read at file:line, do not re-invent. -->

Template A — the per-api_key_id loop to lift (queries.ts:2285–2316, inside liveBaselineMetricsFromPerKeyDailies):
- `for (const [apiKeyId, returns] of Object.entries(perKeyReturnsByApiKeyId))`
- skip when `!returns || returns.length === 0` (:2290)
- one StrategyForBuilder per key: id = apiKeyId, name, codename:null, disclosure_tier:"exploratory" (:2295), strategy_types:[], markets:[], start_date = returns[0]?.date ?? null, daily_returns = returns, cagr/sharpe/volatility/max_drawdown = null
- weight = Math.max(0, equityByApiKeyId[apiKeyId] ?? 0) — clamp negative (:2315)

Template B — the return-shape contract to match (scenario-adapter.ts:87–190, buildStrategyForBuilderSet):
- signature returns `{ strategies: StrategyForBuilder[]; state: ScenarioState }`
- final `return { strategies, state: { selected, weights, startDates } };`
- StrategyForBuilder + ScenarioState types are already imported in this file.

equity-share weight source (D2): each key's current equity share, derived client-side in Plan 03 by grouping `payload.holdingsSummary` (carries api_key_id) with `holdingEquityContribution` (queries.ts:2081). This builder receives `equityByApiKeyId: Record<string, number>` as a parameter — it does NOT compute equity itself.

Existing unit-test scaffold to model on:
- T1 empty → empty (scenario-adapter.test.ts:45)
- T2 two units → weights + both selected true (:58–81)
- T3 short-series exclusion (:83)
- H-0132 commit oracle (:415–494) — MUST stay green untouched.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the buildPerKeyStrategyForBuilderSet sibling builder</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-adapter.ts</files>
  <read_first>
    - src/lib/queries.ts:2248–2353 (liveBaselineMetricsFromPerKeyDailies — the per-key keying + weight + skip template; copy the :2285–2316 loop body)
    - src/app/(dashboard)/allocations/lib/scenario-adapter.ts:87–190 (buildStrategyForBuilderSet — the return-shape contract + StrategyForBuilder/ScenarioState imports; DO NOT edit this function)
    - .planning/phases/37-honest-per-data-source-toggle/37-PATTERNS.md (§"scenario-adapter.ts — sibling per-key builder": exact loop + Pitfall 1 raw-weight rule)
  </read_first>
  <behavior>
    - Input `({}, {})` → `{ strategies: [], state: { selected:{}, weights:{}, startDates:{} } }`.
    - Two keys with non-empty series → 2 strategies; `strategies.map(s=>s.id).sort()` equals the two api_key_ids; `state.selected[id] === true` for both (default included); `state.weights[id]` equals the clamped equity-share for that key.
    - A key present in perKeyReturnsByApiKeyId with `[]`/absent series is skipped entirely (not in strategies, not in state).
    - A key with negative equity share → weight clamped to 0 (never negative).
    - Weights are RAW USD equity-share (NOT divided to sum-to-1) — assert weights are the raw clamped equity values, not normalized fractions.
    - `state.startDates[id]` = `returns[0]?.date ?? "2022-01-01"`.
  </behavior>
  <action>
    Export a new pure function `buildPerKeyStrategyForBuilderSet(perKeyReturnsByApiKeyId: Record<string, DailyPoint[]>, equityByApiKeyId: Record<string, number>): { strategies: StrategyForBuilder[]; state: ScenarioState }` placed BESIDE (not inside) `buildStrategyForBuilderSet`. Copy the loop body from `liveBaselineMetricsFromPerKeyDailies` (queries.ts:2285–2316): iterate `Object.entries(perKeyReturnsByApiKeyId)`, `continue` on empty/absent series, push one StrategyForBuilder per key with `id === apiKeyId`, `disclosure_tier:"exploratory"`, `daily_returns` from the series, scalar metrics null; set `selected[apiKeyId]=true`, `weights[apiKeyId]=Math.max(0, equityByApiKeyId[apiKeyId] ?? 0)`, `startDates[apiKeyId]=returns[0]?.date ?? "2022-01-01"`. Return `{ strategies, state: { selected, weights, startDates } }`. CRITICAL (Pitfall 1): pass RAW equity-share weights — do NOT add any sum-to-1 renormalization here; the frozen engine renormalizes per-day over the selected set (`r / activeWeightSum`, scenario.ts:254). Do NOT import from `queries.ts` if it would create a cycle — duplicate the `disclosure_tier`/null-scalar literal shape locally (it is a small inline object, consistent with the existing duplication noted in PATTERNS §"No Analog Found"). Do NOT touch `buildStrategyForBuilderSet` or its signature.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function buildPerKeyStrategyForBuilderSet" "src/app/(dashboard)/allocations/lib/scenario-adapter.ts"` matches exactly once.
    - `git diff` shows `buildStrategyForBuilderSet`'s signature and body UNCHANGED (only an additive sibling).
    - `grep -n "normalize\|sum.*1\|/ total" ` within the new function body returns nothing — no manual renorm (Pitfall 1).
    - Project typecheck passes (return type matches `{ strategies: StrategyForBuilder[]; state: ScenarioState }`).
  </acceptance_criteria>
  <done>The sibling builder exists, emits one unit per api_key_id with raw equity-share weights and default selected=true, skips empty series, and clamps negative equity — without altering buildStrategyForBuilderSet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit cases for the per-key builder + H-0132 regression confirmation</name>
  <files>src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts</files>
  <read_first>
    - src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts:44–99 (T1/T2/T3 happy-path structure to model the new cases on)
    - src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts:415–494 (H-0132 commit oracle — the regression that proves the sibling is isolated)
    - .planning/phases/37-honest-per-data-source-toggle/37-PATTERNS.md (§"scenario-adapter.test.ts — per-key builder cases")
  </read_first>
  <behavior>
    - Cases assert: id === api_key_id keying; default selected=true per key; weight === clamped equity-share (raw, not normalized); empty-series key skipped; negative-equity key clamped to 0; startDates fallback.
    - The whole scenario-adapter.test.ts suite (including H-0132 oracle + B4 T1–T10) stays green — proves the sibling did not disturb buildStrategyForBuilderSet.
  </behavior>
  <action>
    Add a new `describe("buildPerKeyStrategyForBuilderSet — per-key keying", ...)` block modeling T2 (:58–81). Cover: (a) empty inputs → empty result; (b) two keys with full series → ids === api_key_ids sorted, both `selected===true`, weights === the raw clamped equity-share values supplied; (c) a key whose series is `[]` is skipped (mirror T3's exclusion at :83); (d) a key with a negative `equityByApiKeyId` entry → `weights[id]===0`; (e) startDates falls back to "2022-01-01" when `returns[0].date` is absent. Add one explicit raw-weight assertion: with `equityByApiKeyId = { A: 70, B: 30 }`, assert `weights.A === 70` and `weights.B === 30` (NOT 0.7/0.3) — this is the Pitfall-1 guard that the builder does not pre-normalize. Run the FULL file so the H-0132 oracle (:415) and B4 T1–T10 confirm green in the same invocation.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - The new per-key cases pass; the raw-weight assertion (`weights.A===70`) is present and passing (mutation: adding a sum-to-1 normalize to the builder turns it red).
    - The existing H-0132 oracle and B4 T1–T10 cases remain green in the same run (DSRC-01 regression guard).
    - `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts" -t "per-key"` selects and passes the new block.
  </acceptance_criteria>
  <done>Per-key builder behavior is pinned (keying, default-included, raw equity-share weights, skip/clamp/startDates) and the B4 + H-0132 oracle stay green, proving sibling isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| payload → pure client adapter | The builder is a pure function over already-owned payload data (per-key series + equity map). No I/O, no new boundary crossing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-02-01 | Tampering | Frozen engine / B4 signature | mitigate | Sibling builder ONLY; `buildStrategyForBuilderSet` and `computeScenario` untouched (SCENARIO-05). H-0132 oracle re-run in Task 2 proves the B4 commit contract is intact. |
| T-37-02-02 | Information Disclosure | StrategyForBuilder.name = api_key_id | accept | The unit name embeds the allocator's OWN api_key_id (a uuid, not a secret). No cross-tenant data; the display label is resolved separately in Plan 03 from the allocator's own apiKeys[]. No ciphertext/secret in the unit. |
| T-37-SC | Tampering | npm/pip/cargo installs | accept | No package install (pure in-repo function). |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/allocations/lib/scenario-adapter.test.ts"` green (new per-key block + B4 + H-0132).
- `git diff` shows `buildStrategyForBuilderSet` unchanged; only an additive sibling export + new test block.
- No manual weight normalization anywhere in the new function (Pitfall 1).
</verification>

<success_criteria>
- `buildPerKeyStrategyForBuilderSet` keys projection units per api_key_id (DSRC-01) with raw equity-share weights, default-included, empty-series skip.
- The B4 signature + H-0132 commit oracle remain byte-identical and green.
</success_criteria>

<output>
Create `.planning/phases/37-honest-per-data-source-toggle/37-02-SUMMARY.md` when done.
</output>
