---
phase: 35-per-key-dailies-foundation
plan: 02
type: execute
wave: 2
depends_on: ["35-01"]
files_modified:
  - analytics-service/services/job_worker.py
  - supabase/migrations/20260624HHMMSS2_derive_broker_dailies_api_key_arm.sql
  - analytics-service/tests/test_derive_broker_dailies_dualmode.py
autonomous: true
requirements: [DAILIES-02]

must_haves:
  truths:
    - "run_derive_broker_dailies_job branches on the job's identity axis: an api_key_id payload runs key-mode, a strategy_id payload runs the unchanged strategy-mode (DAILIES-02)."
    - "Key-mode upserts {api_key_id, allocator_id=key.user_id, strategy_id:NULL, date, daily_return} with on_conflict='api_key_id,date', chunked 1000 rows, service-role (DAILIES-02)."
    - "Key-mode does NOT enqueue compute_analytics_from_csv and does NOT stamp strategy_analytics (those are strategy-keyed; per-key reads are Phase 36)."
    - "Strategy-mode behavior is byte-unchanged: same preflight, same upsert conflict target, same compute_analytics_from_csv enqueue."
    - "compute_jobs_kind_target_coherence admits derive_broker_dailies in BOTH the strategy-scoped arm AND a new api_key-scoped arm; all prior arms are preserved (DAILIES-02)."
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "dual-mode run_derive_broker_dailies_job (key vs strategy branch via preflight selection)"
      contains: "_allocator_key_preflight"
    - path: "supabase/migrations/20260624HHMMSS2_derive_broker_dailies_api_key_arm.sql"
      provides: "the one new api_key-scoped arm on compute_jobs_kind_target_coherence + strict-superset DO-block guard"
      contains: "derive_broker_dailies"
    - path: "analytics-service/tests/test_derive_broker_dailies_dualmode.py"
      provides: "DAILIES-02 unit proofs for key-mode payload/conflict-target/no-CSV-enqueue/allocator wiring + strategy-mode non-regression"
      min_lines: 80
  key_links:
    - from: "analytics-service/services/job_worker.py::run_derive_broker_dailies_job"
      to: "csv_daily_returns (api_key_id, date)"
      via: "service-role upsert on_conflict='api_key_id,date' in key-mode"
      pattern: "api_key_id,date"
    - from: "analytics-service/services/job_worker.py::run_derive_broker_dailies_job"
      to: "_allocator_key_preflight"
      via: "branch when job.get('api_key_id') is set"
      pattern: "_allocator_key_preflight"
---

<objective>
Generalize `run_derive_broker_dailies_job` from strategy-scoped to dual-mode so it ALSO derives realized+funding dailies for an allocator exchange key (no strategy), writing the per-key series into the `csv_daily_returns` table that Plan 01 created (DAILIES-02). Add the single api_key-scoped arm to `compute_jobs_kind_target_coherence` so a `{kind:'derive_broker_dailies', api_key_id:…}` job passes coherence.

Purpose: One job, two modes. Key-mode reuses the already-built `_allocator_key_preflight` (loads the api_key directly, no strategy hop), the existing `combine_realized_and_funding` dense-calendar math, and the existing service-role upsert — the genuinely new code is the branch, the key-mode payload/conflict-target, and skipping the two strategy-keyed side-effects. Strategy-mode stays byte-unchanged so live factsheets do not regress.
Output: An edited handler, one small migration (the coherence arm), and a unit test proving both modes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-key-dailies-foundation/35-CONTEXT.md
@.planning/phases/35-per-key-dailies-foundation/35-RESEARCH.md
@.planning/phases/35-per-key-dailies-foundation/35-01-SUMMARY.md

<read_first>
<!-- The exact surfaces the executor edits. Read these first — they contain the contracts. -->

- analytics-service/services/job_worker.py
    run_derive_broker_dailies_job (line 1716-1847) — the handler to generalize.
    _allocator_key_preflight (line 727) — loads job['api_key_id'] directly, runs the
    circuit breaker, decrypts; returns _ExchangeContext with key_row (has "id",
    "user_id", "exchange") and strategy_row=None. _exchange_preflight (line 658) —
    the unchanged strategy path. _ExchangeContext (line 647) already tolerates
    strategy_row=None. Dispatch routing (~line 2874) is UNCHANGED — the handler
    branches internally. fetch_funding_{binance,okx,bybit}(exchange, label, None):
    the second arg is a label/log/match-key ONLY (it never scopes the fetch).
    BROKER_DAILIES_VIA_FUNDING kill-switch (line 177) applies to both modes.

- supabase/migrations/20260614120000_derive_broker_dailies_kind.sql
    The LATEST compute_jobs_kind_target_coherence definition (lines 69-92) — the
    new migration must re-base on this VERBATIM, add ONE arm, and keep the
    strict-superset DO-block guard (Pitfall 6 / b5b lesson: re-base on the latest def).

- analytics-service/services/broker_dailies.py
    combine_realized_and_funding — reads only timestamp+amount from funding rows;
    returns the dense ~365-row daily series. UNCHANGED.
</read_first>

<interfaces>
<!-- Contracts the executor needs; extracted from the codebase. No exploration required. -->

# _ExchangeContext (job_worker.py:647) — already supports key-mode:
#   supabase: Client
#   strategy_row: dict | None   (None on the allocator/key path)
#   key_row: dict               (has "id", "user_id", "exchange")
#   exchange: ccxt.Exchange

# _allocator_key_preflight(job, handler_name) -> DispatchResult | _ExchangeContext
#   loads job['api_key_id'] directly; FAILED if api_key_id missing.

# Key-mode entry branch (from RESEARCH Code Examples — verified pattern):
#   is_key_mode = bool(job.get("api_key_id"))
#   ctx = await _allocator_key_preflight(job, "run_derive_broker_dailies_job") if is_key_mode
#         else await _exchange_preflight(job, "run_derive_broker_dailies_job")
#   if isinstance(ctx, DispatchResult): return ctx
#   key-mode: api_key_id = ctx.key_row["id"]; allocator_id = ctx.key_row["user_id"]; funding_label = api_key_id
#   strategy-mode: strategy_id = job["strategy_id"]; funding_label = strategy_id
#   venue = ctx.key_row["exchange"]   (both modes)

# Key-mode upsert payload + conflict target:
#   rows = [{"api_key_id": api_key_id, "allocator_id": allocator_id,
#            "strategy_id": None, "date": ts.date().isoformat(),
#            "daily_return": float(val)} for ts, val in returns.items()]
#   on_conflict = "api_key_id,date"   (strategy-mode keeps "strategy_id,date")
#   key-mode: NO compute_analytics_from_csv enqueue; NO strategy_analytics stamp.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the api_key-scoped arm to compute_jobs_kind_target_coherence</name>
  <files>supabase/migrations/20260624HHMMSS2_derive_broker_dailies_api_key_arm.sql</files>
  <read_first>
    supabase/migrations/20260614120000_derive_broker_dailies_kind.sql (the LATEST coherence def, lines 69-92, + the self-verifying DO-block idiom — re-base verbatim).
  </read_first>
  <behavior>
    - A compute_jobs row {kind:'derive_broker_dailies', api_key_id:<uuid>, strategy_id:NULL, portfolio_id:NULL, allocator_id:NULL} PASSES the coherence CHECK after this migration.
    - A compute_jobs row {kind:'derive_broker_dailies', strategy_id:<uuid>, all others NULL} STILL PASSES (the existing strategy arm is preserved).
    - All prior coherence arms survive (process_key_long, compute_analytics_from_csv, poll_allocator_positions are the canonical tripwires) — the DO-block superset guard raises if any regressed.
  </behavior>
  <action>
    Create ONE migration with a timestamp that sorts AFTER the Plan 01 migration (e.g. the same date with a later HHMMSS — confirm ordering against the Plan 01 filename so they apply in sequence). In a BEGIN/COMMIT with SET lock_timeout='3s': DROP CONSTRAINT IF EXISTS compute_jobs_kind_target_coherence, then ADD CONSTRAINT with the EXACT body from 20260614120000 lines 69-92 copied verbatim, PLUS one new arm: `(kind = 'derive_broker_dailies' AND api_key_id IS NOT NULL AND strategy_id IS NULL AND portfolio_id IS NULL AND allocator_id IS NULL)` per RESEARCH "compute_jobs Blocker Analysis". Keep derive_broker_dailies in the EXISTING strategy-scoped IN(...) arm (so strategy-mode still passes) — derive_broker_dailies becomes a dual-target kind. Do NOT touch compute_jobs_kind_check (the kind is already admitted there). Then a self-verifying DO block mirroring 20260614120000 STEP 4: assert the coherence def now contains an api_key-scoped derive_broker_dailies arm, AND a strict-superset guard FOREACH prior arm (at minimum process_key_long, compute_analytics_from_csv, poll_allocator_positions, reconstruct_allocator_history, refresh_allocator_equity_daily, rescore_allocator, compute_portfolio) raising if any regressed out. RAISE EXCEPTION prose only — no `||` in message literals. This is a strict superset of the prior constraint (every previously-valid row stays valid), so no in-flight insert can fail under the swap.
  </action>
  <verify>
    <automated>grep -c "derive_broker_dailies" supabase/migrations/20260624*_derive_broker_dailies_api_key_arm.sql | grep -qv '^0$' && grep -q "api_key_id IS NOT NULL AND strategy_id IS NULL" supabase/migrations/20260624*_derive_broker_dailies_api_key_arm.sql && grep -q "process_key_long" supabase/migrations/20260624*_derive_broker_dailies_api_key_arm.sql</automated>
  </verify>
  <done>Migration exists: re-bases the coherence CHECK on the latest def, adds exactly one api_key-scoped derive_broker_dailies arm, keeps the strategy arm + all prior arms, and a DO-block superset guard asserts no prior arm regressed. Applied to the TEST project (via Supabase MCP apply_migration) with the DO block running clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize run_derive_broker_dailies_job to dual-mode (key vs strategy)</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_derive_broker_dailies_dualmode.py</files>
  <read_first>
    analytics-service/services/job_worker.py run_derive_broker_dailies_job (line 1716-1847), _allocator_key_preflight (line 727), _exchange_preflight (line 658), _ExchangeContext (line 647); analytics-service/tests/test_broker_dailies.py (for the existing mock patterns for fetch_account_equity_usd / fetch_all_trades / fetch_funding_* / combine_realized_and_funding and the supabase upsert mock).
  </read_first>
  <behavior>
    - Key-mode (job has api_key_id, no strategy_id): preflight = _allocator_key_preflight; api_key_id = ctx.key_row["id"], allocator_id = ctx.key_row["user_id"]; funding fetch is called with api_key_id as the label; upsert payload rows are {api_key_id, allocator_id, strategy_id:None, date, daily_return}; on_conflict="api_key_id,date"; chunked 1000; NO compute_analytics_from_csv enqueue; NO strategy_analytics stamp on the <2-day branch (return DONE with a log instead); returns DONE.
    - Strategy-mode (job has strategy_id): byte-unchanged — _exchange_preflight, on_conflict="strategy_id,date", strategy_analytics <2-day stamp, compute_analytics_from_csv enqueue, returns DONE.
    - allocator_id is read from ctx.key_row["user_id"] (authoritative), NEVER from the job payload (threat T-35-tampering: service-role write must not trust a payload allocator_id).
    - <2-day key-mode: no strategy_analytics write (there is no per-key analytics row to stamp), returns DONE.
  </behavior>
  <action>
    Edit run_derive_broker_dailies_job to branch at the top on `is_key_mode = bool(job.get("api_key_id"))`, selecting _allocator_key_preflight vs _exchange_preflight (per the interfaces block + RESEARCH Code Examples). Compute api_key_id/allocator_id (key-mode) or strategy_id (strategy-mode) and a single `funding_label` used for the fetch_funding_* second arg (api_key_id in key-mode, strategy_id in strategy-mode — the arg is label-only per RESEARCH planning_critical_unknown #2). Keep `venue = ctx.key_row["exchange"]` for both. Gate the two strategy-keyed side-effects behind `if not is_key_mode`: (a) the <2-day strategy_analytics insufficient-history upsert (key-mode logs + returns DONE without touching strategy_analytics); (b) the trailing enqueue_compute_job(compute_analytics_from_csv). Build the upsert payload and on_conflict per-mode (key-mode: api_key_id/allocator_id/strategy_id=None, on_conflict="api_key_id,date"; strategy-mode: unchanged). Keep the 1000-row chunking, the service-role upsert, the 429 stamping, the aclose_exchange finally, and the BROKER_DAILIES_VIA_FUNDING kill-switch posture all shared. Read allocator_id ONLY from ctx.key_row["user_id"] — never from job. Update the docstring to note dual-mode. Keep the diff surgical (Rule 3) and mypy --strict clean.

    Create test_derive_broker_dailies_dualmode.py (extend the test_broker_dailies.py mock patterns): mock the exchange fetchers + combine_realized_and_funding + the supabase client. Key-mode test: a job with api_key_id (mock _allocator_key_preflight or its supabase api_keys load so key_row has id/user_id/exchange) asserts the upsert was called with rows carrying api_key_id + allocator_id (= key_row user_id) + strategy_id None and on_conflict="api_key_id,date", AND that enqueue_compute_job(compute_analytics_from_csv) was NOT called AND strategy_analytics was NOT upserted. Strategy-mode test: a job with strategy_id asserts the unchanged payload + on_conflict="strategy_id,date" + the compute_analytics_from_csv enqueue still fires (non-regression). Add a key-mode <2-day test asserting no strategy_analytics write + DONE. Each assertion must be mutation-falsifiable (it fails if the branch is neutered).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_derive_broker_dailies_dualmode.py tests/test_broker_dailies.py -q</automated>
  </verify>
  <done>run_derive_broker_dailies_job is dual-mode: key-mode writes api_key_id-keyed rows with allocator_id from key_row.user_id, skips both strategy-keyed side-effects, and returns DONE; strategy-mode is byte-unchanged (proven by the non-regression test). New unit tests green; test_broker_dailies.py still green; mypy --strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| job payload → service-role write | The worker writes per-key rows bypassing RLS; allocator_id MUST come from the authoritative api_keys.user_id (ctx.key_row), never from the job payload, or a forged payload could write rows under the wrong tenant. |
| compute_jobs coherence CHECK → enqueue | Only a correctly-targeted job ({kind, api_key_id} XOR {kind, strategy_id}) may be queued; the coherence CHECK is the DB-layer gate against a miswired derive job. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-05 | Tampering | service-role write with attacker-controlled allocator_id | mitigate | allocator_id is read from ctx.key_row["user_id"] (authoritative), never from job payload; unit-asserted in the key-mode test. |
| T-35-06 | Tampering | strategy-path regression from the dual-mode edit (live factsheets) | mitigate | Strategy-mode is byte-unchanged; the strategy-mode non-regression unit test asserts the unchanged conflict target + the compute_analytics_from_csv enqueue still fires. |
| T-35-07 | Denial of Service / data pollution | key-mode wrongly enqueues compute_analytics_from_csv with a NULL-strategy target | mitigate | The enqueue + the strategy_analytics stamp are gated behind `if not is_key_mode`; the key-mode test asserts neither fires (Pitfall 4). |
| T-35-08 | Tampering | miswired derive job (both axes / neither) reaches the queue | mitigate | compute_jobs_kind_target_coherence admits derive_broker_dailies ONLY as strategy-xor-api_key; strict-superset DO-block guard preserves all prior arms. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Phase 35 installs ZERO packages (RESEARCH Package Legitimacy Audit: not applicable). No install task in this plan. |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_derive_broker_dailies_dualmode.py tests/test_broker_dailies.py -q` green.
- The coherence-arm migration applies to the TEST project with its DO block clean (after Plan 01's migration); a manual probe insert of {kind:'derive_broker_dailies', api_key_id:<uuid>} into compute_jobs on TEST passes coherence, and a {…, strategy_id:<uuid>} insert still passes.
- mypy --strict clean; full analytics suite green (coverage gate held — the new branches are covered by the dual-mode tests).
- The coherence migration is designed to pass the `migration-reviewer` (re-based on the latest def, strict-superset guard, no `||` in RAISE) — run it before the PR.
</verification>

<success_criteria>
- DAILIES-02: run_derive_broker_dailies_job is generalized to allocator-key-scoped, deriving realized+funding dailies per crypto exchange key on the dense ~365-row calendar and upserting keyed by api_key_id; the coherence CHECK admits the api_key-scoped derive job; strategy-mode is byte-unchanged.
- No compute_analytics_from_csv enqueue and no strategy_analytics stamp in key-mode; allocator_id sourced from api_keys.user_id.
</success_criteria>

<output>
Create `.planning/phases/35-per-key-dailies-foundation/35-02-SUMMARY.md` when done.
</output>
