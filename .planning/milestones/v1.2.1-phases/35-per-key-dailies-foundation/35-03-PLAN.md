---
phase: 35-per-key-dailies-foundation
plan: 03
type: execute
wave: 3
depends_on: ["35-02"]
files_modified:
  - analytics-service/scripts/phase35_backfill_enqueue.py
  - analytics-service/tests/test_phase35_backfill_enqueue.py
autonomous: true
requirements: [DAILIES-03]

must_haves:
  truths:
    - "Running the backfill enqueues exactly one {kind:'derive_broker_dailies', api_key_id} compute_jobs row per active connected exchange key (DAILIES-03)."
    - "Active-key predicate is is_active=true AND sync_status IS DISTINCT FROM 'revoked' AND disconnected_at IS NULL (role-agnostic, per A1)."
    - "The script is idempotent: a re-run with in-flight jobs already present pre-checks and bails (0 enqueued); a racing duplicate aborts atomically (23505-safe)."
    - "The script exits non-zero on ANY skip (missing-id rows, race, partial) — fail loud (Rule 12)."
  artifacts:
    - path: "analytics-service/scripts/phase35_backfill_enqueue.py"
      provides: "enqueue-based, idempotent, fail-loud per-key derive backfill (mirror of phase12_backfill_enqueue.py)"
      contains: "derive_broker_dailies"
    - path: "analytics-service/tests/test_phase35_backfill_enqueue.py"
      provides: "DAILIES-03 unit proofs: predicate, one-job-per-key payload, idempotent pre-check, 23505-safe, exit code"
      min_lines: 70
  key_links:
    - from: "analytics-service/scripts/phase35_backfill_enqueue.py"
      to: "compute_jobs (kind='derive_broker_dailies', api_key_id)"
      via: "single atomic bulk INSERT guarded by a pending-job pre-check"
      pattern: "derive_broker_dailies"
    - from: "analytics-service/scripts/phase35_backfill_enqueue.py"
      to: "api_keys (active connected keys)"
      via: "is_active + sync_status != revoked + disconnected_at IS NULL"
      pattern: "disconnected_at"
---

<objective>
Add a one-off, operator-run backfill script that enqueues the dual-mode derive job (api_key_id-scoped, from Plan 02) for every EXISTING active connected exchange key, so historical keys get a per-key dailies series — not only keys onboarded after this milestone (DAILIES-03).

Purpose: The dual-mode handler + the coherence arm (Plan 02) make a per-key derive job runnable; this script fans it out across the current key population. It mirrors the proven `phase12_backfill_enqueue.py` shape (pre-check duplicate guard + single atomic bulk INSERT + 23505 race-catch + non-zero exit on any skip), so it is idempotent and fail-loud. Backfill = re-derive from the exchange API (no existing per-key data to copy — the enqueued job does the fetch).
Output: One new script (run via `railway ssh "cd /app && python -m scripts.phase35_backfill_enqueue"`) and a unit test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/35-per-key-dailies-foundation/35-CONTEXT.md
@.planning/phases/35-per-key-dailies-foundation/35-RESEARCH.md
@.planning/phases/35-per-key-dailies-foundation/35-02-SUMMARY.md

<read_first>
<!-- The exact precedent to mirror + the predicate source. -->

- analytics-service/scripts/phase12_backfill_enqueue.py
    The full shape to mirror: get_supabase() + db_execute; the pending-job pre-check
    (count="exact", fail loud if count is None); the strategies select with
    "rows.data is None → RuntimeError" fail-loud; defensive id extraction with a
    `skipped` list; ONE atomic bulk INSERT; the 23505 race-catch (code/msg sniff);
    the non-zero exit union (`if skipped or raced or inserted != len(payload): return 1`).
    Phase 35 swaps: kind='derive_broker_dailies' (not compute_analytics), the row
    target column is api_key_id (not strategy_id), and the source table is api_keys
    (not strategies) with the active-connected predicate below.

- analytics-service/services/equity_reconstruction.py (around line 1489)
    The canonical active-key predicate: is_active=true AND sync_status IS DISTINCT
    FROM 'revoked' AND disconnected_at IS NULL.
</read_first>

<interfaces>
<!-- Contracts; no exploration needed. -->

# Active-key predicate (PostgREST):
#   .eq("is_active", True).neq("sync_status", "revoked").is_("disconnected_at", "null")
#   NOTE: PostgREST .neq does NOT match NULL (three-valued logic). A NULL sync_status
#   must still be INCLUDED (it is NOT 'revoked'). Use an OR filter or .or_(
#   "sync_status.is.null,sync_status.neq.revoked") to faithfully encode
#   "sync_status IS DISTINCT FROM 'revoked'". Verify the emitted filter includes
#   NULL-sync_status keys (the test must cover a NULL-sync_status active key).
#   exchange is already constrained to (binance,okx,bybit) by the column CHECK —
#   an explicit .in_("exchange", [...]) is harmless defense-in-depth, optional.

# Enqueue row shape (one per key):
#   {"api_key_id": <uuid>, "kind": "derive_broker_dailies", "status": "pending",
#    "next_attempt_at": <now_iso>, "metadata": {"phase": "35-backfill"}}
#   (match phase12's column set; do NOT set strategy_id — coherence requires it NULL.)

# Pre-check (idempotency): count pending {kind:'derive_broker_dailies', status:'pending'}
#   with api_key_id NOT NULL; if count is None → RuntimeError (fail loud); if >0 → print
#   skip notice + return 0. The durable guarantee is the single atomic bulk INSERT +
#   the compute_jobs (api_key_id, kind) in-flight partial unique index (23505 abort).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: phase35_backfill_enqueue.py — per-key derive backfill + unit test</name>
  <files>analytics-service/scripts/phase35_backfill_enqueue.py, analytics-service/tests/test_phase35_backfill_enqueue.py</files>
  <read_first>
    analytics-service/scripts/phase12_backfill_enqueue.py (the full shape to mirror); analytics-service/services/equity_reconstruction.py ~line 1489 (the active-key predicate); analytics-service/services/db.py (get_supabase + db_execute signatures).
  </read_first>
  <behavior>
    - Selects api_keys WHERE is_active=true AND sync_status IS DISTINCT FROM 'revoked' (a NULL sync_status key IS included) AND disconnected_at IS NULL — role-agnostic (no profiles.role filter, per A1).
    - Builds exactly one pending compute_jobs row per selected key: {api_key_id, kind:'derive_broker_dailies', status:'pending', next_attempt_at, metadata:{phase:'35-backfill'}}; never sets strategy_id.
    - Pre-check: if any pending derive_broker_dailies (api_key_id NOT NULL) jobs already exist → print skip notice, return 0 (idempotent re-run). If the pre-check count comes back None → RuntimeError (fail loud, do not run the guard blind).
    - api_keys select returning None data → RuntimeError (fail loud).
    - A key row missing/invalid 'id' is recorded in `skipped` (not a KeyError abort) and forces a non-zero exit.
    - A 23505 collision on the bulk INSERT (a racing operator/worker already landed an in-flight (api_key_id, kind) row) is caught, reported, and forces a non-zero exit; the whole INSERT rolls back atomically (0 enqueued).
    - Non-zero exit on ANY skip/race/partial; exit 0 only on a clean full enqueue (or a clean pre-check skip).
  </behavior>
  <action>
    Create phase35_backfill_enqueue.py as a faithful adaptation of phase12_backfill_enqueue.py (Don't-Hand-Roll: reuse the proven structure). Swap: the pre-check counts pending derive_broker_dailies jobs with api_key_id NOT NULL; the source select is api_keys with the active-connected predicate (encode "sync_status IS DISTINCT FROM 'revoked'" so NULL sync_status is INCLUDED — see the interfaces note; .neq alone drops NULLs, which would wrongly skip never-synced active keys); the bulk payload rows carry api_key_id + kind='derive_broker_dailies' (no strategy_id). Preserve ALL fail-loud invariants from phase12: count-is-None → RuntimeError, data-is-None → RuntimeError, defensive id extraction with a `skipped` list, ONE atomic bulk INSERT, the 23505 race-catch with a clear message, and the non-zero exit union (`if skipped or raced or inserted != len(payload): return 1`). Module docstring documents the railway ssh usage + the idempotency model. Keep mypy --strict clean.

    Create test_phase35_backfill_enqueue.py (mirror any phase12 backfill test patterns; mock the supabase client + db_execute): (1) happy path — 3 active keys (one with sync_status NULL, one with 'active', one with 'connected') → exactly 3 rows enqueued, each with kind='derive_broker_dailies' + api_key_id + no strategy_id, exit 0; assert a revoked/disconnected/inactive key is EXCLUDED. (2) idempotent re-run — pre-check returns count>0 → 0 enqueued, exit 0, no INSERT call. (3) pre-check count None → RuntimeError. (4) api_keys data None → RuntimeError. (5) a key row missing 'id' → it is skipped and the exit code is 1. (6) bulk INSERT raises 23505 → caught, exit 1, reported. Each assertion mutation-falsifiable. The NULL-sync_status inclusion test is load-bearing (proves the predicate did not drop never-synced active keys).
  </action>
  <verify>
    <automated>cd analytics-service && python -m pytest tests/test_phase35_backfill_enqueue.py -q</automated>
  </verify>
  <done>phase35_backfill_enqueue.py enqueues one derive_broker_dailies (api_key_id) job per active connected key (including NULL-sync_status keys), is idempotent (pre-check + atomic INSERT + 23505-safe), and exits non-zero on any skip/race/partial. All unit tests green; mypy --strict clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator script → compute_jobs | The backfill runs with SUPABASE_SERVICE_KEY (bypasses RLS); it enqueues jobs via a direct INSERT. Idempotency/fencing must come from the DB (the (api_key_id, kind) in-flight partial unique index), not from the script's best-effort pre-check. |
| backfill fan-out → exchange APIs | Each enqueued derive job will later pull a key's full account history; the script itself does NOT call exchanges (it only enqueues), so it adds no new credential surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-35-09 | Denial of Service / duplicate pile-up | re-run enqueues a second redundant backfill batch | mitigate | Pending-job pre-check (fail loud if count None) + the durable (api_key_id, kind) in-flight partial unique index aborting a racing duplicate INSERT atomically (23505-caught); idempotent re-run test. |
| T-35-10 | Repudiation / silent gap | a NULL-sync_status or malformed key is silently skipped | mitigate | `sync_status IS DISTINCT FROM 'revoked'` includes NULL keys (test-pinned); missing-id rows go to `skipped` and force a non-zero exit (fail loud, Rule 12). |
| T-35-11 | Information Disclosure | enumerating which api_key_ids exist via the script | accept | The script runs operator-side with service-role; it introduces no new authenticated RPC/route (RESEARCH probe-oracle note). No external surface added. |
| T-35-SC | Tampering | npm/pip/cargo installs | accept | Phase 35 installs ZERO packages (RESEARCH Package Legitimacy Audit: not applicable). No install task in this plan. |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_phase35_backfill_enqueue.py -q` green.
- mypy --strict clean; full analytics suite green (coverage gate held).
- Manual (non-blocking, post-merge, recorded in 35-VALIDATION.md Manual-Only): run `railway ssh "cd /app && python -m scripts.phase35_backfill_enqueue"` against prod, confirm per-key rows appear in csv_daily_returns for ≥1 real key after the worker drains.
</verification>

<success_criteria>
- DAILIES-03: existing allocator exchange keys are backfilled — the script enqueues one api_key-scoped derive job per active connected key, idempotently and fail-loud, so historical keys produce a per-key series.
</success_criteria>

<output>
Create `.planning/phases/35-per-key-dailies-foundation/35-03-SUMMARY.md` when done.
</output>
