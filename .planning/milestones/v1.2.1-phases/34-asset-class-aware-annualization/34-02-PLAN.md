---
phase: 34-asset-class-aware-annualization
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - analytics-service/services/equity_reconstruction.py
  - analytics-service/tests/fixtures/equity-curve-golden/binance-spot-only.json
  - analytics-service/tests/fixtures/equity-curve-golden/bybit-perp-with-funding.json
  - analytics-service/tests/fixtures/equity-curve-golden/csv-spot-only.json
  - analytics-service/tests/fixtures/equity-curve-golden/okx-multi-month-perps.json
  - analytics-service/tests/test_equity_curve_builder.py
  - analytics-service/tests/test_ana_recon_audit.py
  - analytics-service/tests/test_mt5_golden_fixtures.py
autonomous: true
requirements: [ANNUAL-02, ANNUAL-03, ANNUAL-05]
must_haves:
  truths:
    - "EquityCurveBuilder.compute_sharpe defaults to 252 (was 365); the ×1.20 mismatch with compute_all_metrics is gone"
    - "The two paths agree at 252 on the same daily-return series within tolerance — no residual scale factor"
    - "Equity-curve golden expected_sharpe literals recomputed at 252; the equity-curve test suite is green"
    - "MT5 doc block describes the explicit-param design (still resolving 252); MT5 golden test unchanged-green"
  artifacts:
    - path: "analytics-service/services/equity_reconstruction.py"
      provides: "compute_sharpe(periods=252) default converged onto the shared 252 basis; NEW-C01-15 docstrings reversed"
      contains: "periods: int = 252"
    - path: "analytics-service/tests/test_equity_curve_builder.py"
      provides: "Updated periods=252 cross-check + a no-residual-scale-factor agreement test (builder vs compute_all_metrics at 252)"
      contains: "periods=252"
    - path: "analytics-service/tests/test_mt5_golden_fixtures.py"
      provides: "Refreshed doc block describing the explicit periods_per_year param (resolved basis still 252)"
      contains: "periods_per_year"
  key_links:
    - from: "EquityCurveBuilder.to_metrics_snapshot"
      to: "compute_sharpe (periods=252)"
      via: "default param, shared DEFAULT_PERIODS_PER_YEAR semantics"
      pattern: "compute_sharpe\\("
    - from: "test_sharpe_within_tolerance"
      to: "equity-curve-golden/*.json expected_sharpe (recomputed at 252)"
      via: "builder Sharpe == golden within 0.05"
      pattern: "expected_sharpe"
---

<objective>
Converge `EquityCurveBuilder.compute_sharpe` from its `periods=365` default DOWN to 252 so it shares
the unified basis with `compute_all_metrics` (Plan 01) — eliminating the documented ×1.20
(≈√(365/252)) scale-factor mismatch (NEW-C01-15). This is the SINGLE user-visible behavior change in
the phase: it lowers the persisted `metrics_snapshot.sharpe` rendered on the landing verification card
by ×√(252/365) ≈ 0.8312. This is the intended ×1.20 fix, not a regression — call it out loudly in the
commit.

Then fix the hand-maintained test artifacts that the flip turns red: the 4 `expected_sharpe` literals
in the equity-curve golden fixtures (×√(252/365)), the `periods=365` quantstats cross-check, the
`365 ** 0.5` literal in the recon-audit test, and the NEW-C01-15 docstrings/comments. Finally, refresh
the now-stale MT5 doc block to describe the explicit-param design (the resolved MT5 basis is still 252).

Purpose: Satisfies ANNUAL-05 (two paths agree at 252, no residual scale), ANNUAL-02 (both production
callers + the equity path all resolve 252), and re-states ANNUAL-03 (unified 252 convention in the MT5 doc).

Output: `equity_reconstruction.py` with `compute_sharpe(periods=252)`; recomputed golden literals;
updated cross-check + a new no-residual-scale-factor agreement test; refreshed MT5 doc block.

Depends on Plan 01: shares the `DEFAULT_PERIODS_PER_YEAR` / unified-252 semantics and the agreement
test compares against the Plan-01 threaded `compute_all_metrics`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-asset-class-aware-annualization/34-CONTEXT.md
@.planning/phases/34-asset-class-aware-annualization/34-RESEARCH.md
@.planning/phases/34-asset-class-aware-annualization/34-01-SUMMARY.md

<read_first>
@analytics-service/services/equity_reconstruction.py
@analytics-service/tests/test_equity_curve_builder.py
@analytics-service/tests/test_ana_recon_audit.py
@analytics-service/tests/test_mt5_golden_fixtures.py
</read_first>

<interfaces>
<!-- Verified against live code this session. -->

Convergence target (analytics-service/services/equity_reconstruction.py:3041):
  def compute_sharpe(self, risk_free_rate: float = 0.0, periods: int = 365) -> float | None
    body (~3084-3088): excess = returns - (risk_free_rate / periods); std = excess.std();
                       return float((excess.mean() / std) * (periods ** 0.5))
  This is a HAND-ROLLED √-scaled formula (NOT a quantstats call) → pure √ scaling, so flipping the
  default 365→252 multiplies the result by exactly √(252/365) ≈ 0.8312.

Production caller — EXACTLY ONE, passes NO periods arg (verified):
  equity_reconstruction.py:3121  to_metrics_snapshot() → sharpe=self.compute_sharpe()
Test callers (no arg): test_equity_curve_builder.py:125, test_ana_recon_audit.py:1025.
NO caller anywhere passes periods=365 explicitly → the flip is safe.

DO NOT TOUCH — different, unrelated function (shares only a name fragment):
  services/portfolio_optimizer.py:250  _compute_sharpe(returns, rf=0)  — already ×√252, used by bridge/match/simulator.

Hand-maintained golden literals to recompute (VERIFIED locations; ×√(252/365) = ×0.831232... ):
  tests/fixtures/equity-curve-golden/binance-spot-only.json:931      3.0719603935764024  → ≈ 2.5535
  tests/fixtures/equity-curve-golden/bybit-perp-with-funding.json:330 5.088077470517692  → ≈ 4.2293
  tests/fixtures/equity-curve-golden/csv-spot-only.json:619           2.55693759          → ≈ 2.1254
  tests/fixtures/equity-curve-golden/okx-multi-month-perps.json:3249  0.8337293172341194 → ≈ 0.6930
  (Compute each exact value programmatically = old_value × (252/365) ** 0.5; do not eyeball.)

Test literals to flip (VERIFIED):
  test_equity_curve_builder.py:116  def test_sharpe_within_tolerance(fixture_name) — asserts builder Sharpe
                                     within 0.05 of expected_sharpe (line ~129)
  test_equity_curve_builder.py:135-137,159  NEW-C01-15 comment + the quantstats cross-check:
                                     qs.stats.sharpe(df["daily_return"], periods=365)  → periods=252  (line 160)
  test_ana_recon_audit.py:1036      (365 ** 0.5)  → (252 ** 0.5)   (this assertion only checks the two DIFFER,
                                     so it is convention-agnostic, but update for consistency)

NEW-C01-15 docstrings to reverse (per RESEARCH "State of the Art"):
  equity_reconstruction.py:3044+ (compute_sharpe docstring) and the class docstring (~:2742) —
  change "default changed from 252 to 365 … √(252/365)≈0.83 under-scale" → "252, unified with
  compute_all_metrics (Phase 34); no residual scale factor." Drop the under-scale rationale.

MT5 doc block to refresh (analytics-service/tests/test_mt5_golden_fixtures.py:21-31):
  currently says: "`compute_all_metrics` has NO `periods` parameter; its qs.stats calls use the
  quantstats 0.0.81 DEFAULT periods=252." After Plan 01 a `periods_per_year: int = 252` param EXISTS.
  Rewrite to: the param exists and DEFAULTS to 252; MT5 resolves the same 252 basis as the crypto
  trades path and the CSV path — apples-to-apples on the ranking page. Do NOT change MT5's resolved
  basis (still 252); the MT5 golden expected values are UNCHANGED (doc-only edit).

For the no-residual-scale-factor agreement test (ANNUAL-05 direct contract, RESEARCH Open Question 2):
  compute_all_metrics (Plan 01) is imported from services.metrics; builder Sharpe and
  compute_all_metrics Sharpe on the SAME daily-return series won't be bit-equal (builder uses
  NAV-derived returns with C01-06/C01-14 bar adjustments), so assert agreement at the SAME basis on a
  fixture WITHOUT terminal-bar/day-0 adjustments, tolerance ±0.05 (mirrors the existing
  builder-vs-quantstats cross-check). The point is "no ×1.204 factor," not bit-equality.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Converge compute_sharpe 365→252 + reverse the NEW-C01-15 docstrings</name>
  <files>analytics-service/services/equity_reconstruction.py</files>
  <behavior>
    - EquityCurveBuilder.compute_sharpe() (no-arg, the production path) now annualizes at 252; its returned Sharpe is exactly the old 365-basis value × √(252/365) ≈ 0.8312.
    - portfolio_optimizer._compute_sharpe is untouched (different function).
    - Docstrings no longer claim "365 / √(252/365) under-scale"; they describe the unified-252 convergence.
  </behavior>
  <action>
    First re-confirm via grep that no caller passes `periods=365` to `compute_sharpe` (RESEARCH verified:
    only the def + the 2 test cross-check literals — none is a `compute_sharpe(...periods=365)` CALL).
    Flip the default in the `compute_sharpe` signature (equity_reconstruction.py:3042) from
    `periods: int = 365` to `periods: int = 252`. The hand-rolled body (`* (periods ** 0.5)`) needs no
    other change — it already reads the param. Reverse the NEW-C01-15 docstring on `compute_sharpe`
    (~:3044) AND the class docstring (~:2742) per <interfaces>: state the basis is 252, unified with
    `compute_all_metrics` (Phase 34), no residual scale factor; drop the √(252/365)≈0.83 under-scale
    rationale. Do NOT touch `portfolio_optimizer._compute_sharpe` (different function, already ×√252).
    Keep the diff surgical (Rule 3) — this is a default flip + doc reversal, not a rewrite. Do NOT
    attempt to delegate compute_sharpe to compute_all_metrics (RESEARCH: different NAV-derived return
    series + C01-06/C01-14 bar logic — much larger surface than the decision wants).
  </action>
  <verify>
    <automated>cd analytics-service && grep -n "periods: int = 252" services/equity_reconstruction.py | grep -v '^#' && ! grep -n "periods: int = 365" services/equity_reconstruction.py && python -c "import ast,sys; src=open('services/equity_reconstruction.py').read(); assert 'periods: int = 252' in src and 'periods: int = 365' not in src; print('flip ok')"</automated>
  </verify>
  <done>
    `compute_sharpe` default is `periods: int = 252`; no `periods: int = 365` remains in
    equity_reconstruction.py; both NEW-C01-15 docstrings describe the unified-252 convergence (no
    under-scale rationale); `portfolio_optimizer._compute_sharpe` unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recompute the 4 golden expected_sharpe literals at 252 + flip the test cross-check literals + add the no-residual-scale-factor agreement test</name>
  <files>analytics-service/tests/fixtures/equity-curve-golden/binance-spot-only.json, analytics-service/tests/fixtures/equity-curve-golden/bybit-perp-with-funding.json, analytics-service/tests/fixtures/equity-curve-golden/csv-spot-only.json, analytics-service/tests/fixtures/equity-curve-golden/okx-multi-month-perps.json, analytics-service/tests/test_equity_curve_builder.py, analytics-service/tests/test_ana_recon_audit.py</files>
  <behavior>
    - test_sharpe_within_tolerance is GREEN against the recomputed 252-basis expected_sharpe values.
    - The quantstats cross-check uses periods=252 (matches the converged builder convention).
    - A new agreement test proves the builder Sharpe and compute_all_metrics Sharpe on the same series now agree at 252 within ±0.05 — the ×1.204 factor is gone.
    - test_ana_recon_audit's differ-assertion still passes with 252 ** 0.5.
  </behavior>
  <action>
    Recompute each of the 4 `expected_sharpe` literals as `old_value * (252/365) ** 0.5` (RESEARCH
    Pitfall 3 — pure √ scaling for this hand-rolled formula, NOT the geometric CAGR case). Compute the
    exact replacement values programmatically (not by eye) and write each into its fixture JSON at the
    line cited in <interfaces>. In test_equity_curve_builder.py: change the quantstats cross-check
    `qs.stats.sharpe(df["daily_return"], periods=365)` (line ~160) → `periods=252`, and refresh the
    NEW-C01-15 comments (lines ~135-137, ~159) to say the builder and the cross-check now share the 252
    basis. In test_ana_recon_audit.py (line ~1036): change `(365 ** 0.5)` → `(252 ** 0.5)` for
    consistency (this assertion only checks the two values DIFFER, so it is convention-agnostic, but
    update it). THEN add a `test_no_residual_scale_factor` (the direct ANNUAL-05 contract, RESEARCH
    proof #4 / Open Question 2): on a fixture WITHOUT terminal-bar/day-0 adjustments, assert the builder
    Sharpe and `compute_all_metrics(...).metrics_json["sharpe"]` on the SAME daily-return series agree
    within ±0.05 (mirroring the existing builder-vs-quantstats tolerance). Import `compute_all_metrics`
    from `services.metrics` (the Plan-01 threaded function). The point is "no ×1.204 factor," not
    bit-equality.
  </action>
  <verify>
    <automated>cd analytics-service && grep -c "periods=252" tests/test_equity_curve_builder.py && grep -q "252 \*\* 0.5" tests/test_ana_recon_audit.py && ! grep -q "periods=365" tests/test_equity_curve_builder.py && python -m pytest tests/test_equity_curve_builder.py tests/test_ana_recon_audit.py -k "sharpe or residual or recon" -x -q</automated>
  </verify>
  <done>
    All 4 fixture `expected_sharpe` values recomputed at 252 (= old × √(252/365)); the quantstats
    cross-check uses `periods=252`; no `periods=365` remains in test_equity_curve_builder.py;
    test_ana_recon_audit uses `252 ** 0.5`; `test_no_residual_scale_factor` exists and is GREEN
    (builder == compute_all_metrics at 252 within ±0.05); `test_sharpe_within_tolerance` GREEN for all 4
    fixtures.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refresh the stale MT5 doc block (explicit-param design, resolved basis still 252)</name>
  <files>analytics-service/tests/test_mt5_golden_fixtures.py</files>
  <behavior>
    - The doc block no longer claims "compute_all_metrics has NO periods parameter"; it describes the explicit periods_per_year param defaulting to 252.
    - MT5 golden expected values are unchanged; the MT5 golden test stays green (doc-only edit).
  </behavior>
  <action>
    Update the doc block at test_mt5_golden_fixtures.py:21-31 (the "ANNUALIZATION = the LIVE periods=252
    (UNCHANGED)" section). Per the CONTEXT MT5-documentation-refresh decision: replace the now-false
    "`compute_all_metrics` has NO `periods` parameter" wording with a description of the
    explicit-param design — `compute_all_metrics(periods_per_year: int = 252)` exists (Phase 34) and
    DEFAULTS to 252; the crypto trades path (analytics_runner.py:1584), the CSV/MT5 path (:2027) and the
    converged equity-reconstruction path all resolve the SAME 252 basis, so Sharpe/vol/CAGR stay
    apples-to-apples on the ranking page; plumbing 365 for MT5 alone would inflate its Sharpe ~×1.20.
    Keep the existing oracle-discipline / sqrt(252)=15.8745… hand-arithmetic notes intact. Do NOT change
    MT5's resolved basis (still 252) and do NOT change any MT5 golden expected value — this is a doc-only
    edit so the test stays green. Keep it surgical (Rule 3).
  </action>
  <verify>
    <automated>cd analytics-service && grep -q "periods_per_year" tests/test_mt5_golden_fixtures.py && ! grep -q "has NO .periods. parameter" tests/test_mt5_golden_fixtures.py && python -m pytest tests/test_mt5_golden_fixtures.py -x -q</automated>
  </verify>
  <done>
    The MT5 doc block describes the explicit `periods_per_year` (default 252) design and no longer claims
    "no periods parameter"; MT5 golden test green with no expected-value changes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted metrics_snapshot.sharpe → landing verification card | The converged value is serialized into the `metrics_snapshot` JSONB and rendered. Existing stored rows keep their 365-basis number until re-verified (no backfill in scope — RESEARCH Runtime State Inventory; CONTEXT requests no migration). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-03 | Tampering | compute_sharpe flip silently breaks a caller relying on periods=365 | mitigate | RESEARCH-verified exactly ONE production caller, no-arg; grep gate `! grep "periods: int = 365"` + the no-arg caller proves safety. Test-caller goldens recomputed in Task 2. |
| T-34-04 | Information disclosure | Wrong fixture recompute (eyeballed instead of × √(252/365)) ships an incorrect displayed Sharpe | mitigate | Literals computed programmatically (old × (252/365)**0.5); `test_sharpe_within_tolerance` (±0.05) + `test_no_residual_scale_factor` cross-validate against both the hand-rolled formula and compute_all_metrics. |
| T-34-05 | Repudiation | Wrong function converged (portfolio_optimizer._compute_sharpe) | mitigate | <interfaces> explicitly flags the look-alike as DO-NOT-TOUCH; only equity_reconstruction.py is in files_modified for the flip. |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No installs — no package operations in this phase. |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_equity_curve_builder.py tests/test_ana_recon_audit.py tests/test_mt5_golden_fixtures.py -q` — all green.
- `cd analytics-service && python -m pytest -q` full suite green (+ `--cov-fail-under=80` on the CI job).
- `mypy --strict --follow-imports=silent services/equity_reconstruction.py` clean. Validate against a
  CI-pinned `uv venv --python 3.12` + requirements.txt per the B-mypy-local-venv-drift memory note if any
  mypy surprise appears.
- `git diff analytics-service/tests/fixtures/mt5/` shows NO change (MT5 golden values unchanged; doc-only).
- Commit message calls out the single user-visible change loudly: "converge equity_reconstruction Sharpe
  365→252 — lowers landing-card Sharpe ×√(252/365)≈0.83 (the documented ×1.20 fix, not a regression)."
</verification>

<success_criteria>
- ANNUAL-05: `compute_sharpe` default is 252; builder and `compute_all_metrics` agree at 252 within
  tolerance (`test_no_residual_scale_factor` green); no residual ×1.204 scale factor.
- ANNUAL-02: every path (2 production callers via default + the equity path) resolves 252.
- ANNUAL-03: MT5 doc block re-states the unified-252 convention under the explicit-param design.
- 4 golden `expected_sharpe` literals recomputed (× √(252/365)); cross-check at `periods=252`;
  recon-audit at `252 ** 0.5`; NEW-C01-15 docstrings reversed.
- MT5 golden expected values unchanged; `portfolio_optimizer._compute_sharpe` untouched; diff surgical.
</success_criteria>

<output>
Create `.planning/phases/34-asset-class-aware-annualization/34-02-SUMMARY.md` when done.
</output>
