---
phase: 34-asset-class-aware-annualization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/metrics.py
  - analytics-service/tests/test_metrics_parity.py
autonomous: true
requirements: [ANNUAL-01, ANNUAL-03, ANNUAL-04]
must_haves:
  truths:
    - "compute_all_metrics accepts periods_per_year (default 252) and every annualization site uses it"
    - "Default-252 output is byte-identical to today (golden_252d_expected.json unchanged, parity test green)"
    - "Passing periods_per_year=365 actually rescales output by the known relationships (sharpe/sortino/vol √, CAGR geometric, info_ratio/alpha linear) — a silent threading hole fails CI"
  artifacts:
    - path: "analytics-service/services/metrics.py"
      provides: "DEFAULT_PERIODS_PER_YEAR constant + periods_per_year param threaded through all 9 annualization sites + rolling helper signatures"
      contains: "DEFAULT_PERIODS_PER_YEAR"
    - path: "analytics-service/tests/test_metrics_parity.py"
      provides: "Parametrized 365-proof test asserting rescale relationships incl. a synthetic benchmark for alpha/info_ratio"
      contains: "periods_per_year=365"
  key_links:
    - from: "compute_all_metrics(periods_per_year=...)"
      to: "qs.stats.cagr/volatility/sharpe/sortino/greeks + np.sqrt(periods_per_year) + rolling helpers"
      via: "single threaded parameter"
      pattern: "periods=periods_per_year|np.sqrt\\(periods_per_year\\)"
    - from: "test_periods_param_rescales_365"
      to: "compute_all_metrics"
      via: "compares 252 default vs explicit 365 output"
      pattern: "compute_all_metrics\\(.*periods_per_year=365"
---

<objective>
Add an explicit `periods_per_year: int = 252` parameter to `compute_all_metrics` and thread it
through ALL 9 annualization sites in `metrics.py`, backed by one module-level
`DEFAULT_PERIODS_PER_YEAR = 252` constant (single source of truth). The default preserves today's
behavior exactly — the param makes the annualization basis visible at the call site instead of
hardcoded/hidden, so a future per-asset divergence becomes a one-line call-site change.

This plan does NOT edit the two production callers (`analytics_runner.py:1584` and `:2027`) — both
inherit the 252 default, which is precisely how ANNUAL-02 is satisfied without a resolver. Do NOT
build an asset-class resolver (it would resolve 252-for-all = dead constant function; violates Rule 2).

Purpose: Satisfies ANNUAL-01 (param threaded), ANNUAL-03 (unified 252 preserved), and ANNUAL-04
(byte-identical-at-252 golden + a parametrized 365 proof). The 365 proof is the REAL guard — it
proves the param actually threads, not merely that "a param exists."

Output: `metrics.py` with the constant + threaded param; `test_metrics_parity.py` with a new
`test_periods_param_rescales_365` proof case. `golden_252d_expected.json` stays untouched (that IS
the unchanged-at-252 proof).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/34-asset-class-aware-annualization/34-CONTEXT.md
@.planning/phases/34-asset-class-aware-annualization/34-RESEARCH.md

<read_first>
@analytics-service/services/metrics.py
@analytics-service/tests/test_metrics_parity.py
</read_first>

<interfaces>
<!-- Verified against live code this session. Use these directly — no exploration needed. -->

Public function (analytics-service/services/metrics.py:333):
  def compute_all_metrics(
      returns: pd.Series,
      benchmark_returns: pd.Series | None = None,
  ) -> MetricsResult

The 9 annualization sites to thread (VERIFIED line numbers; file is 1499 lines — re-confirm with grep before editing, lines may shift as you edit above them):
  1. metrics.py:439  cagr        = qs.stats.cagr(returns)              → periods=periods_per_year
  2. metrics.py:440  volatility  = qs.stats.volatility(returns)        → periods=periods_per_year
  3. metrics.py:441  sharpe      = qs.stats.sharpe(returns)            → periods=periods_per_year
  4. metrics.py:446  sortino     = qs.stats.sortino(returns, rf=MAR)   → ..., periods=periods_per_year
  5. metrics.py:807  greeks      = qs.stats.greeks(aligned_returns, aligned_benchmark)
                       → periods=periods_per_year  (greeks annualizes ALPHA via alpha *= periods, default 252.0 — scout-missed)
  6. metrics.py:812  te          = excess.std() * np.sqrt(252)         → np.sqrt(periods_per_year)
  7. metrics.py:814  info_ratio  = excess.mean() * 252 / te            → excess.mean() * periods_per_year / te
  8. metrics.py:1368 (_rolling_alpha_beta) qs.stats.rolling_greeks(aligned_returns, aligned_benchmark, window)
                       → thread periods=periods_per_year (rolling_greeks annualizes rolling alpha; verify sig accepts periods kw)
  9a. metrics.py:1212 (_rolling_sharpe)                ratio_series * np.sqrt(252)            → np.sqrt(periods_per_year)
  9b. metrics.py:1294 (_rolling_sortino_from_components) ratio_series * np.sqrt(252)          → np.sqrt(periods_per_year)
  9c. metrics.py:1335 (_rolling_volatility)            returns.rolling(window).std() * np.sqrt(252) → np.sqrt(periods_per_year)

Treynor (metrics.py:817 `cagr / beta`) needs NO edit — cagr is annualized at site #1, so it follows.

Rolling helper signatures that must gain a `periods_per_year: int = DEFAULT_PERIODS_PER_YEAR` param
(VERIFIED — do NOT confuse the new annualization param with the existing `window: int`):
  metrics.py:1197  def _rolling_sharpe(returns, window)
  metrics.py:1215  def _rolling_sortino_from_components(returns, ..., window)
  metrics.py:1297  def _rolling_sortino(returns, window, mar=MAR)              ← thin wrapper, forwards to _from_components
  metrics.py:1327  def _rolling_volatility(returns, window)
  metrics.py:1338  def _rolling_alpha_beta(returns, benchmark, window)
  metrics.py:1391  def _rolling_alpha(returns, benchmark, window=90)           ← thin wrapper → _rolling_alpha_beta
  metrics.py:1405  def _rolling_beta(returns, benchmark, window=90)            ← thin wrapper → _rolling_alpha_beta

Helper call sites INSIDE compute_all_metrics (must forward periods_per_year; verified lines 470-474, 921-926):
  _rolling_sharpe(returns, 30/90/365)
  _rolling_sortino_from_components(returns, sortino_neg_sq, 63/126/252)
  _rolling_volatility(returns, 63/126/252)
  (the 63/126/252/30/90/365 here are WINDOW lengths — leave them literal; only the annualization sqrt changes)

Existing parity test anchor (analytics-service/tests/test_metrics_parity.py):
  FIXTURES_DIR = Path(__file__).parent / "fixtures"   (line 39)
  golden_252d_input  pytest fixture → dict with ["returns"] (pd.Series) and ["benchmark"] (pd.Series)
  golden_252d_expected pytest fixture → committed expected dict
  def test_metrics_parity_full(golden_252d_input, golden_252d_expected)  (line 791) —
    calls compute_all_metrics(golden_252d_input["returns"], golden_252d_input["benchmark"]) with NO periods arg.
    This test STAYS UNCHANGED and IS the "byte-identical at 252" half of ANNUAL-04.

EMPIRICALLY-VERIFIED rescale relationships (quantstats 0.0.81, this session, match < 1e-12):
  sharpe / sortino / volatility :  ×365  =  ×252 * sqrt(365/252)   where sqrt(365/252) = 1.2035001862952488
  CAGR (NONLINEAR — geometric)  :  cagr_365 = (1 + cagr_252) ** (365/252) - 1     (NOT ×1.2035 — that assertion FAILS)
  info_ratio                    :  ×365  =  ×252 * (365/252)       (linear, = 1.4484126984126984)
  alpha (greeks)                :  ×365  =  ×252 * (365/252)       (linear; greeks does alpha *= periods)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DEFAULT_PERIODS_PER_YEAR constant + thread periods_per_year through all 9 annualization sites</name>
  <files>analytics-service/services/metrics.py</files>
  <behavior>
    - compute_all_metrics(returns, benchmark) with NO periods arg produces output IDENTICAL to today (golden_252d parity unchanged) — the =252 default preserves behavior.
    - compute_all_metrics(returns, benchmark, periods_per_year=365) changes sharpe/sortino/volatility by ×sqrt(365/252), CAGR geometrically, info_ratio and alpha linearly by ×(365/252).
    - Every rolling helper (_rolling_sharpe/_rolling_sortino_from_components/_rolling_volatility/_rolling_alpha_beta + their thin wrappers) accepts and uses the threaded factor; the rolling WINDOW lengths (30/63/90/126/252/365) are unchanged.
  </behavior>
  <action>
    Introduce one module-level constant `DEFAULT_PERIODS_PER_YEAR = 252` near the top of metrics.py
    (mirror the existing `optimizer.py:43` `TRADING_DAYS = 252` precedent — this is the single source
    of truth the CONTEXT integration note asks for). Add `periods_per_year: int = DEFAULT_PERIODS_PER_YEAR`
    as a keyword-default param on `compute_all_metrics` (after `benchmark_returns`, fully typed `int`,
    no `Any` — mypy --strict is enforced). Thread it through all 9 sites listed in <interfaces>:
    pass `periods=periods_per_year` into the four `qs.stats` scalar calls (cagr/volatility/sharpe/sortino
    at ~439-446) and `qs.stats.greeks` (~807) and `qs.stats.rolling_greeks` (~1368); replace the bare
    `np.sqrt(252)` at ~812/1212/1294/1335 with `np.sqrt(periods_per_year)`; replace the `* 252` at ~814
    with `* periods_per_year`. Add `periods_per_year: int = DEFAULT_PERIODS_PER_YEAR` to the 7 rolling
    helper signatures and forward it from the call sites inside compute_all_metrics (the thin wrappers
    _rolling_sortino/_rolling_alpha/_rolling_beta must forward to their delegates). CRITICAL per RESEARCH
    Pitfall 4: do NOT touch the `window` args (the 63/126/252 rolling-window lengths are NOT the
    annualization factor). Treynor (~817) needs no edit (cagr at #1 carries the basis). Do NOT edit the
    two production callers in analytics_runner.py — they inherit the default (ANNUAL-02). Do NOT build an
    asset-class resolver (RESEARCH anti-pattern — dead 252-for-all constant function). Keep the diff
    surgical (Rule 3). Re-confirm each line number with grep before editing, since edits above a site
    shift the lines below it.
  </action>
  <verify>
    <automated>cd analytics-service && grep -n "DEFAULT_PERIODS_PER_YEAR" services/metrics.py | grep -v '^#' | head && grep -c "periods=periods_per_year\|np.sqrt(periods_per_year)\|periods_per_year / te\|\* periods_per_year / te" services/metrics.py && ! grep -n "np.sqrt(252)" services/metrics.py && python -m pytest tests/test_metrics_parity.py::test_metrics_parity_full -x -q</automated>
  </verify>
  <done>
    DEFAULT_PERIODS_PER_YEAR=252 constant exists; `compute_all_metrics` has the typed `periods_per_year`
    param; zero `np.sqrt(252)` literals remain in metrics.py (all converted to `np.sqrt(periods_per_year)`);
    all four qs.stats scalar calls + greeks + rolling_greeks pass `periods=periods_per_year`; rolling
    helpers thread the param; `test_metrics_parity_full` is GREEN (default-252 output byte-identical);
    `golden_252d_expected.json` is UNCHANGED on disk.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the parametrized periods_per_year=365 rescale proof (the real threading guard)</name>
  <files>analytics-service/tests/test_metrics_parity.py</files>
  <behavior>
    - test_periods_param_rescales_365 computes base = compute_all_metrics(r, b) and p365 = compute_all_metrics(r, b, periods_per_year=365) on the SAME golden_252d input, then asserts:
      • sharpe, sortino, volatility each ≈ base × sqrt(365/252)  (rel=1e-9)
      • cagr ≈ (1 + base_cagr) ** (365/252) - 1   (geometric — NOT × sqrt; asserting ×1.2035 here MUST fail)
    - A second small synthetic-benchmark case asserts alpha and info_ratio rescale linearly × (365/252), closing the greeks-alpha (#5) / info_ratio (#7) / rolling_alpha (#8) threading holes that golden_252d cannot prove (its info_ratio/alpha are null).
    - A silent threading hole (any site left at 252) makes this test RED.
  </behavior>
  <action>
    Add `test_periods_param_rescales_365` to test_metrics_parity.py reusing the existing `golden_252d_input`
    fixture (it exposes `["returns"]` and `["benchmark"]` pd.Series — see <interfaces>). Use the
    empirically-verified ratios: `sqrt_ratio = (365/252) ** 0.5` for sharpe/sortino/volatility via
    `pytest.approx(..., rel=1e-9)`; the geometric form `(1 + base_cagr) ** (365/252) - 1` for CAGR (RESEARCH
    Pitfall 1 — the √ ratio FAILS on CAGR). Because golden_252d_expected has `info_ratio: None` / `alpha: None`
    (its benchmark calendar yields nulls), ALSO add a tiny ALIGNED synthetic returns+benchmark pair (a few
    dozen rows, same date index, non-degenerate) and assert that `alpha` and `info_ratio` rescale linearly
    by `365/252` between the 252-default and the 365 call — this exercises sites #5/#7/#8 which would
    otherwise be threaded-but-unproven (RESEARCH Open Question 1). Read result scalars via the same
    accessor `test_metrics_parity_full` uses (the MetricsResult `.metrics_json` / `__getitem__` proxy).
    Do NOT add a new fixture JSON file — RESEARCH "Don't Hand-Roll" prefers the in-test parametrized recompute
    over a `golden_365d_*.json` (no fixture drift). Leave `test_metrics_parity_full` and the committed
    `golden_252d_expected.json` untouched.
  </action>
  <verify>
    <automated>cd analytics-service && grep -q "periods_per_year=365" tests/test_metrics_parity.py && python -m pytest tests/test_metrics_parity.py::test_periods_param_rescales_365 tests/test_metrics_parity.py::test_metrics_parity_full -x -q</automated>
  </verify>
  <done>
    `test_periods_param_rescales_365` exists, calls `compute_all_metrics(..., periods_per_year=365)`,
    asserts sharpe/sortino/volatility ×sqrt(365/252), CAGR via the geometric formula, and (synthetic
    benchmark) alpha + info_ratio ×(365/252); both the new test and `test_metrics_parity_full` are GREEN.
    Mutating any single site back to a literal 252 turns the proof RED (falsifiable).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Pure in-process math change on the analytics compute path; no new input crosses a trust boundary, no I/O, no install, no migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-01 | Tampering | Silent threading hole (a site left at hardcoded 252 while the param claims to thread) | mitigate | `test_periods_param_rescales_365` asserts numeric rescale at every metric class incl. alpha/info_ratio via a synthetic benchmark; a missed site fails CI loudly (Rule 12 fail-loud). The `! grep np.sqrt(252)` gate proves no literal remains. |
| T-34-02 | Tampering | Accidental change to the displayed-at-252 numbers (golden drift) | mitigate | `test_metrics_parity_full` + the untouched `golden_252d_expected.json` (regen refuses without `--i-am-fixing-a-real-bug`) prove the default basis is byte-identical to today. |
| T-34-SC | Tampering | npm/pip/cargo installs | accept | No installs in this phase — `quantstats==0.0.81` already pinned in requirements.txt; RESEARCH "Package Legitimacy Audit" = Not Applicable. No checkpoint needed. |
</threat_model>

<verification>
- `cd analytics-service && python -m pytest tests/test_metrics_parity.py -q` — both the unchanged
  full-parity test and the new 365-proof green.
- `git diff --stat analytics-service/tests/fixtures/golden_252d_expected.json` shows NO change (the
  unchanged-at-252 invariant).
- `mypy --strict --follow-imports=silent services/metrics.py` clean (new param + helper signatures typed).
  Per the B-mypy-local-venv-drift memory note, validate against a CI-pinned `uv venv --python 3.12` +
  requirements.txt, NOT the local `.venv` (supabase 2.28.3 drift), if any APIResponse/ndarray-style mypy
  surprise appears.
- `npx vitest run src/__tests__/metrics-parity.test.ts` still green (TS oracle unaffected at 252).
</verification>

<success_criteria>
- ANNUAL-01: `compute_all_metrics` accepts `periods_per_year` (default 252) threaded through all 9 sites.
- ANNUAL-03: default-252 output byte-identical → all displayed/ranking metrics stay on unified 252.
- ANNUAL-04: golden_252d unchanged (252 proof) + `test_periods_param_rescales_365` green (365 proof with
  correct √ / geometric / linear ratios). Both halves of the guard present.
- No production caller edited (analytics_runner.py untouched — ANNUAL-02 by default inheritance).
- No asset-class resolver built; diff surgical.
</success_criteria>

<output>
Create `.planning/phases/34-asset-class-aware-annualization/34-01-SUMMARY.md` when done.
</output>
