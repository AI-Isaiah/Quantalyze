# Milestone v1.6 "Scenario Series-Space Purification" — AUDIT

**Date:** 2026-07-04 · **Verdict: PASS — 0 blockers**
**Shipped:** PR #572, squash `f78f036b` = **v0.36.0.0** (phases 62-64 in one
branch, ~58 commits); Phase 65 = live authed prod canary (verification-only).
Vercel Production success at exact SHA + Railway analytics SUCCESS; main CI
green first-try.

## Definition of done

All 4 phases complete and verified (62: 5/5 · 63: 5/5 · 64: 8/8 SC checks ·
65: GUARD-04 canary). **16/16 requirements covered**, each mapped to exactly
one phase, none orphaned.

## Cross-phase integration (6/6 WIRED)

1. **ONE membership selector** — P62's persisted `memberKeyIds` is the live
   selector every surface reads: compare `scenario-compare.ts:154`
   (`usePerKeySources`), mint gate `share/route.ts:203` (`isBookOnlyDraft`),
   resolve `share-resolve.ts:291` (`isMixed`), composer stamps at both save
   sites + reopen-derive. Single definitions in `scenario-state.ts`.
2. **ONE engine** — `buildAddedOnlySet` (defined once, `scenario-adapter.ts:256`)
   wired into composer (`ScenarioComposer.tsx:1871`) AND compare
   (`scenario-compare.ts:201`); `buildStrategyForBuilderSet` and
   `scenario-dealias.ts` have ZERO production importers tree-wide (incl.
   PDF/cron/api routes) — they survive only in the ENGINE-05 guard ban-list.
3. **P64 reads P62; commit boundary intact** — caption chain
   `share-resolve.ts:291 → page.tsx:171 → :219-221` (verbatim copy at
   `scenario-mixed-caption`); KpiStrip mounted with no `aum` prop while the
   `scenarioAum` weight×AUM commit chain (`:2805 → :2912 → :4368`) is
   byte-intact.
4. **GUARDs** — GUARD-03: `git diff e5e83247..HEAD` on `src/lib/scenario.ts`
   + `scenario-window.ts` = **0 lines** (whole milestone); GUARD-01: prod
   residue holdings deleted, `gate_false_holders = 0` (recorded in
   63-05-SUMMARY); GUARD-04: canary satisfied at `f78f036b`.
5. **E2E chain live** — one draft exercised save → blank-session reopen
   (F-1 fix: adopts book basis) → Update (membership intact at the persisted
   layer, `schema_version: 4`) → compare (persisted membership; Live book 155
   overlapping days golden) → mint → logged-out public resolve (caption
   verbatim, 0 member-UUID leak hits) → book-only 409 → delete cascade → 404.
   0 unexpected console errors.
6. **Deferred/tech-debt aggregated** — see below; none block.

## Pre-landing review chain (process record)

6-specialist fan-out (8 findings, all addressed; #1 `holdingReturnsByScopeRef`
kept per user decision + P1 TODO) → fix pass (5 items, RED-verified tests) →
**Fable red team** (found F-1 Critical: blank-session Update silently wiped
book membership — an interaction of two individually-correct fixes; fixed at
the root with 3 RED-verified regression tests; F-2 vacuous test de-vacuoused;
F-3/F-4/F-5 tracked) → final fix pass. The F-1 fix was then verified live by
the Phase 65 canary at the persisted-draft layer.

## Deferred items / tech debt (carried forward, none blocking)

Tracked in **TODOS.md (git-tracked, shipped in `f78f036b`)** — the
integration checker's note that TODOS.md is untracked was verified FALSE
(`git ls-files` confirms):
1. P1: dead `holdingReturnsByScopeRef` SSR pipeline removal (planning-locked
   KEEP this milestone; helper `queries.ts:2994` + payload field).
2. P2: share-mint `isBookOnlyDraft` disjunct is dead code (red-team F-3).
3. P3: memberKeyIds save-stamp unbounded vs `.max(64)` schema cap →
   misleading save error for >64-key allocators (red-team F-5).
4. P3: deploy-skew window can strip/downgrade v4 membership (transient,
   graceful; consider post-deploy re-derive sweep) (red-team F-4).

Planning-ledger only (local): `holdingsSummary` SSR removal; 6 `phase10-rpc-*`
residue auth.users rows (holders were the GUARD-01 scope); D3 persist
source-toggles with the draft; compare per-column dropped-member caption;
friendly gantt labels (P61 B1); compare-panel payload type derivation
(`AllocationsTabs.tsx:964` cast); pre-v4 codec `as unknown as` casts
(intentional, documented); IN-06 pre-existing Phase-62 edge.

## Notes

- One WARNING-level nuance (documented, intentional): mint gates on
  `isBookOnlyDraft` (membership-based) while resolve's book-only branch keys
  on `strategies.length === 0` — the P62 IN-01 contract split; both align on
  membership semantics.
- Canary console ledger: 4 entries session-wide, all accounted for (2 stale
  P61-era buffer, 1 canary probe artifact, 1 expected B4 409). 0 app errors.
