# v1.9 Multi-Key Composite Strategy — Milestone Audit (manual close)

**Closed:** 2026-07-11 (manual reconciliation)
**Tag:** `v1.9` @ `044bee50` · **App version:** v0.41.0.0 · **PR:** #607
**Phases:** 8 (85–91 + 90.5) · **Timeline:** 2026-07-10 → 2026-07-11

> This is a **manual** milestone audit, not a `gsd-verifier` goal-backward run.
> `/gsd:complete-milestone` was never executed at ship time (v1.9 closed on the
> green `frontend` branch-protection gate + founder sign-off + git tag, same as
> v1.7/v1.8). The local, gitignored phase working-dirs (plans / evidence /
> verification for phases 85–91) and the v1.9 `REQUIREMENTS.md` were deleted /
> overwritten during `/gsd-new-milestone v1.9.1` (a `phases.clear` before this
> archive existed) and are **not recoverable** — they lived only under
> `.planning/` (gitignored). Recovery via Time Machine local snapshots was
> possible but deliberately skipped: the durable record below is sufficient.
> All shipped **code is in git** under tag `v1.9`.

## Ship evidence (durable, verifiable)

- **Tag `v1.9` @ `044bee50`** — squash commit, 122 files, +20,412/−141. The
  milestone's self-contained content (intervening main PRs #599–#605 were
  non-milestone asset-class/dep work under v0.39–v0.40).
- **Close gate:** green `frontend` branch-protection aggregator + the #338
  all-N-keys-complete publish invariant proven 4 independent ways; sole red
  check was one redundant advisory `e2e-seeded` test (#338 discoverability
  200-vs-404), deferred post-deploy per user decision.
- **Deploy verified:** 5 migrations applied on prod; Railway analytics worker
  force-deployed past a red-CI-timing skip (healthy); Vercel prod Ready;
  `process_key_unified_backbone='on'` + `USE_COMPUTE_JOBS_QUEUE=true`.
- **Live corroboration:** Zavara 3-key composite at cum **62.6646%** / maxDD
  **−4.1328%** (Phase 86), matching the manager's claimed track.

## Requirement / phase coverage (from MILESTONES.md ledger)

| Phase | Delivered | Evidence |
|-------|-----------|----------|
| 85 | Thin data model `strategy_keys` (per-key window + owner-coherence + RLS, migration-first) | in tag |
| 86 | Production `stitch_composite` worker path; Zavara corroborated LIVE | cum 62.66% / maxDD −4.13% |
| 87 | #338 all-N-keys-complete publish gate (published-sole-writer + supersede-failed-per-kind) | migs `20260710150000` / `20260710160000` |
| 88 | Wizard `MultiKeyConnectStep` + composite finalize routing (stitch queue before unified arm) | in tag |
| 89 | `SyncPreviewStep` composite render + `attributionBasisFromConfig` | in tag |
| 90 | Factsheet gap markers + fail-loud-gated `cash_settlement ↔ mark_to_market` toggle | in tag |
| 90.5 | Dynamic per-strategy leverage (LEV-01 single-key; LEV-02 3-reader persistence) | in tag |
| 91 | Seed-gated composite onboarding + render e2e, wizard axe, published-sole-writer guards | verifier 7/8 |

## Open items carried into v1.9.1 / backlog

- **⚠️ OPEN USER GATE — SC-3 piece 3:** the one-shot LIVE GUI-onboarded 3-key
  Zavara session (real Deribit keys + running worker) is a user-run smoke test.
  **Never claimed live-attested by the orchestrator.** Still open.
- **#338 discoverability 200-vs-404** (draft composite returns 200 not 404 at
  `/strategy/[id]` — app-layer streamed-shell artifact) → fix landed separately
  (PR #608, asserts not-found CONTENT).
- **Phase-90 LOW-2** chart-method/headline drift → folded into v1.9.1 HARD-03
  (persist `cumulative_method` at stitch time). See `.planning/REQUIREMENTS.md`.
- The composite metric blow-up found during SC-3 dogfooding (inverse-Deribit
  per-key return scale) → v1.9.1 Phase 92 (HARD-01/04).

## Archive integrity note

- `v1.9-ROADMAP.md` — archived (present).
- `v1.9-phases/` — **stub only** (README explains the un-recovered artifacts).
- `v1.9-REQUIREMENTS.md` — **not archived** (overwritten by v1.9.1's; unrecoverable).
- This audit + the MILESTONES.md entry are the authoritative v1.9 record.
