# v1.15 — MetaTrader 5: live api_verified account sync — ARCHIVED ROADMAP

**Shipped:** 2026-07-24 (code, DARK) · **Went LIVE:** 2026-07-25 (flags flipped) · **App version:** v0.49.0.0 → v0.49.4.0 · **Tag:** `v1.15`
**PRs:** #636 (`feat/v1.15-mt5-api-verified`, merge be8ebdf8, v0.49.0.0 — milestone code, landed DARK) + go-live hardening #637/#640/#641 (v0.49.1.0→0.49.3.0, mt5linux/rpyc bridge + 6 soak-surfaced bugs) + #642 (v0.49.4.0 — server-time offset + CI).
**Audit:** `.planning/v1.15-MILESTONE-AUDIT.md` — engineering_complete: true (12/16 reqs satisfied, 6/6 integration WIRED, migration prod-verified). The 4 `human_needed` go-live legs (MT5SPIKE-01 / MT5GW-01 / MT5GOLIVE-01/02) were all COMPLETED 2026-07-25 with evidence (see Go-Live below) — audit was written 2026-07-24 pre-go-live.

## Core value

Elevate MetaTrader 5 from the legacy `self_reported` Expert-Advisor/CSV push (fabricatable) to a LIVE `api_verified` account sync — the shipped sFOX 118–123 arc applied to MT5's forex/CFD world. An allocator/manager connects a read-only (investor-password) MT5 account; the worker reads deal history + equity through the ONE `derive_basis_series` backbone and stamps `api_verified`. Read path = self-hosted `gmag11/metatrader5_vnc:2.3` Wine gateway (RPyC :8001) + `mt5linux` network client; the Windows-only `MetaTrader5` pkg NEVER runs in the Linux worker process. ~95% verbatim sFOX-seam clone; the gateway is the one net-new infra piece.

**Two deliberate divergences (hold everywhere):** (1) MT5 = forex/CFD = TRADITIONAL √252 annualization (NOT crypto √365); `isCryptoExchange` narrowed to exclude `'mt5'`. (2) THREE credential fields (login / investor password / broker server) onto `{api_key, api_secret, passphrase}`; broker server first-class + required.

## Phases (all complete)

- **134 — MT5SPIKE — feasibility spike + `Mt5Client` contract** ✅ (3/3). Offline `Mt5Client`/`Mt5ClientError` RPyC facade + 25-test contract suite + `scripts/mt5_spike.py` four-leg live harness + go/no-go doc. mt5linux==1.0.3 supply-chain verified + pinned.
- **135 — MT5SRC — source lockstep + validate + routes + migration** ✅ (4/4). `'mt5'` first-class across Source/SUPPORTED_SOURCES/_FACTORIES; worker read-only validate branch (3-cred mapping, master-password rejection, wrong-server distinct); all 3 key routes; RED-guarded 4-CHECK constraint-widen migration `20260723172032` (prod-verified admits 'mt5').
- **136 — MT5RECON — equity reconstruction → backbone → api_verified** ✅. `combine_mt5_deal_ledger` deal-ledger → daily NAV → the ONE backbone with `api_verified`; √252 traditional (mutation-guarded); ground-truth parity gate vs `account_info().equity`; deposit-day-not-a-spike + fail-loud unclassifiable DEAL_TYPE.
- **137 — MT5CONC — concurrency + terminal hardening** ✅ (2/2). `to_thread`+`wait_for` bounds + terminal-restart-on-timeout (WEDGE-01 lesson); per-terminal `asyncio.Lock` (host:port) + pre/post `account_info().login==expected` bracket so `api_verified` can never stamp the wrong account.
- **138 — MT5UI — flag-gated add-key UI + badge + guide + e2e** ✅ (3/3). MT5 in the wizard behind `NEXT_PUBLIC_MT5_ENABLED` (OFF = byte-identical); 3-field form; investor-password `/security#mt5-readonly` guide; `api_verified` badge; all-roles `mt5-badge.spec.ts` in blocking e2e-seeded.
- **139 — MT5GOLIVE — prod gateway + real-broker soak + flag flip LIVE** ✅ (3/3). Soak runner `scripts/mt5_soak.py` + `docs/runbooks/mt5-go-live.md` + `deploy/mt5-gateway/`; the founder LIVE-ops legs completed 2026-07-25 (below).

## Go-Live (2026-07-25) — the 4 human_needed legs, done with evidence

- **MT5GW-01** — prod gateway `mt5-gateway` on Railway (gmag11 v2.3, pinned digest), VNC install + investor login persisted to `/config`, FULLY PRIVATE (0 domains/proxies on the :8001 RCE bridge). Bridge fix: 3-pin `PIP_CONSTRAINT` (`mt5linux==0.1.9` + `rpyc==5.0.1` + `numpy<2`) — the "server started" false-green was a stacked rpyc-6↔5 + numpy-2↔1 skew.
- **MT5GOLIVE-01** — hosting = Railway co-locate (dual-stack verified); gateway credential-isolated.
- **MT5SPIKE-01** — live feasibility PROVEN end-to-end cold-boot: acct **26547876 @ `VantageMarkets-Live 5`**, REAL, **trade_allowed=False** (read-only investor login confirmed), equity=balance=$199,268.44.
- **MT5GOLIVE-02** — soak day-1 GREEN (`recon_residual` 1.4e-10, reconstructed vs balance to the cent); full factsheet proven on the real ledger (−20.9% cumulative, √252, reconciled to trading-pnl/deposits). Flags flipped LIVE 2026-07-25: worker `MT5_ENABLED=true` + `MT5_GATEWAY_HOST=mt5-gateway.railway.internal:8001` (Railway 9d310b40), Vercel `NEXT_PUBLIC_MT5_ENABLED=true` (fresh build). quantalyze.xyz → 200.

## Key decisions

- FULL GO-LIVE this milestone (not a flag-OFF Foundation close like sFOX v1.12) — founder 2026-07-23.
- Self-hosted Wine gateway + `mt5linux` net client (NOT MetaApi.cloud / broker Manager API).
- MT5 = traditional asset class (√252); read-only investor password only, master rejected at connect.
- Server-time offset set post-go-live: `MT5_SERVER_UTC_OFFSET_S=10800` (EEST/UTC+3), matching the soak.

## Technical debt / follow-ups

- **DST edge** (EET↔EEST): fixed env can't auto-switch at Oct/Mar; founder VNC/live-tick confirm. Day-bucketing only, not the balance-anchored parity.
- **DEAL_TYPE ambiguous middle** (CHARGE/INTEREST/CANCELED/DIVIDEND/TAX) + master-reject retcode 10009 [ASSUMED] — fail-loud defaults; confirm at richer live data.
- **mt5linux 0.1.9 arg-form fragility** — f-string-interpolates call args into a Wine-side eval; handled at `Mt5Client` (order_check(**request), epoch-int history bounds, initialize-before-login). Documented in memory.
- **Worker `requirements.txt`** carries `rpyc==5.2.3` (hand-added, not `make lock` — local uv drifts pandas) + `mt5linux --no-deps` in Dockerfile; `requirements.in` vs lock pandas drift logged (tech-debt).
- **Read-only investor password** was leaked in a `railway variables` dump — founder declined rotation (read-only, limited blast radius).

_For current project status, see .planning/ROADMAP.md_
