---
phase: 93-composite-data-path-correctness
plan: 03
type: execute
wave: 2
depends_on: [93-01, 93-02]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_stitch_composite_job.py
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/factsheet/composite-read-path.test.ts
  - src/lib/factsheet/types.ts
  - src/app/factsheet/[id]/v2/FactsheetView.tsx
  - src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx
autonomous: true
requirements: [HARD-05]
tags: [composite, ccxt, degraded_members, fail-loud-removal, data_quality_flags, HARD-05]

must_haves:
  truths:
    - "A composite containing a Bybit/OKX/Binance member NEVER fails PERMANENT on the venue check — the Deribit members stitch, the ccxt member degrades with an explicit machine-readable DQ reason, and computation_status is complete_with_warnings"
    - "The degrade reason is USER-VISIBLE on both existing DQ surfaces (wizard composite-preview amber role=status block + factsheet hero-strip caveat) and carries NO account-size magnitude — a fixed enum reason code + seq + venue only"
    - "A degraded member appears in per_key with n_days 0 (so the wizard table renders its ENTERED window via Plan 93-02's fallback, honestly showing zero coverage)"
    - "A composite where NO member reconstructs still fails PERMANENT with a scrubbed message (never an empty invented track record)"
    - "All-Deribit composites (Zavara class) never enter the new branch — byte-identical (SC-4)"
    - "BLOCKING pre-check: no CHECK constraint on strategy_analytics.data_quality_flags rejects the new key — verified with evidence BEFORE the key is introduced"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "venue routing in _reconstruct_all: deribit → native; {binance,okx,bybit} → degrade channel (this plan) with the PERMANENT rejection removed"
      contains: "degraded_members"
    - path: "analytics-service/tests/test_stitch_composite_job.py"
      provides: "rejection-removed RED test + degrade visibility + all-degraded permanent-fail + drop-stale + all-Deribit parity"
    - path: "src/lib/factsheet/composite-read-path.ts"
      provides: "strict-coerced degraded_members parse exposed on buildOpts.dataQuality"
    - path: "src/app/factsheet/[id]/v2/FactsheetView.tsx"
      provides: "hero-strip degraded-member caveat (92-03 insufficient_window pattern)"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "amber Data-quality block line for degraded members"
  key_links:
    - from: "job_worker.py _reconstruct_all venue check (:3154-3175)"
      to: "merged_flags['degraded_members'] (set/pop drop-stale, insufficient_window idiom :3571-3575)"
      via: "degraded list threaded from the cash-pass fan-out into the persist block"
      pattern: "degraded_members"
    - from: "merged_flags['degraded_members']"
      to: "wizard amber block + factsheet hero strip"
      via: "composite-read-path dataQuality + SyncPreviewStep dq parse, strict coercion"
      pattern: "degraded_members"
---

<objective>
HARD-05 REQUIRED scope (Option B): remove the PERMANENT fail-loud rejection of non-Deribit composite members (`job_worker.py:3154-3175`) and replace it with a graceful, user-visible DQ degrade — a Bybit/OKX/Binance member never hard-fails the whole composite. "Only Deribit composites work" stops being a hard failure mode; honest reconstruction (Option A) lands in Plan 93-04 on top of this channel.

Purpose: the Phase-86 scope fence deliberately hard-failed ccxt members ("composing it partially = silently-wrong series"). The honest middle path is neither a wrong series nor a dead composite: the member is EXCLUDED from the stitch, the exclusion is stamped as a machine-readable DQ reason the user sees, coverage shows the truth (n_days 0), and status promotes to complete_with_warnings. Fail-loud is preserved where it is honest: zero reconstructable members stays PERMANENT.
Output: degrade channel end-to-end (worker → merged_flags → both DQ render surfaces), gated by the BLOCKING no-CHECK verify; parity byte-identical for all-Deribit composites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-composite-data-path-correctness/93-RESEARCH.md  (§HARD-05 — rejection site, degrade mechanism, leak discipline)
@.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-03-SUMMARY.md  (the exact additive-DQ + two-surface render pattern this plan mirrors: merged_flags set/pop, strict `=== true`-style coercion, wizard amber block + FactsheetView hero strip, DESIGN.md #B45309)

<interfaces>
The rejection to remove (job_worker.py:3154-3175, inside _reconstruct_all's per-member loop):
```python
venue = str(ctx.key_row["exchange"])
if venue != "deribit":
    await aclose_exchange(ctx.exchange)
    await _stamp_failed(f"Composite member on venue {venue!r} is not yet supported (Deribit-only this phase).")
    return DispatchResult(outcome=DispatchOutcome.FAILED, ..., error_kind="permanent")
```

The degrade record shape (NEW, leak-safe — fixed enum code, no exception text, no USD):
```python
{"seq": int, "venue": str, "reason": "venue_reconstruction_unavailable"}
```
(Plan 93-04 adds the second code `"reconstruction_failed"` when an ATTEMPTED ccxt reconstruction fails structurally.)

Persist idiom to mirror (job_worker.py:3571-3575):
```python
if degraded:  # non-empty list
    merged_flags["degraded_members"] = degraded
else:
    merged_flags.pop("degraded_members", None)   # drop-stale: heals on re-stitch
```

Existing venue set (job_worker.py:2794): `_COMPOSITE_CRYPTO_VENUES = frozenset({"deribit", "binance", "okx", "bybit"})`.
Existing warn channel (job_worker.py:3517-3533): `member_warn_flags` / `member_warned` → `composite_status = "complete_with_warnings"`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BLOCKING — verify no CHECK constraint on data_quality_flags rejects a new key (A2)</name>
  <files>(evidence only — no source change)</files>
  <read_first>
    - Output of: `grep -rn "data_quality_flags" supabase/migrations/*.sql` — read EVERY hit's surrounding DDL (the by-basis `metrics_json_by_basis` column DOES carry a Phase-85 `jsonb_typeof='object'` CHECK; confirm no analogous constraint names `data_quality_flags`).
    - The `strategy_analytics` CREATE TABLE / ALTER TABLE migrations found by: `grep -rln "strategy_analytics" supabase/migrations/ | head` — confirm the `data_quality_flags` column definition is a bare `jsonb` (default/nullable) with no CHECK, domain, or trigger-based key whitelist.
  </read_first>
  <action>
    Execute both greps, read the matching DDL, and record the evidence (migration filenames scanned + the exact column definition line) in the SUMMARY. This gates the introduction of the `degraded_members` key in Task 2 (research assumption A2).

    Session pre-scan found zero CHECKs referencing `data_quality_flags` (only the `computation_status` CHECK appears nearby) — this task produces the DEFINITIVE evidence.

    IF a CHECK / whitelist IS found: STOP the plan immediately and return blocked with the constraint's migration file + line — widening it means a NEW MIGRATION, and migrations AUTO-APPLY TO PROD ON MERGE (must be flagged prominently to the orchestrator, never slipped in silently).
  </action>
  <verify>
    <automated>grep -rn "data_quality_flags" supabase/migrations/*.sql | grep -ic "check" | grep -qx "0" && echo "NO-CHECK-VERIFIED"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints NO-CHECK-VERIFIED (zero same-line CHECK references), AND the SUMMARY quotes the actual `data_quality_flags` column DDL from the strategy_analytics migration proving no multi-line CHECK either.
    - If any constraint found → plan returns BLOCKED (Task 2/3 not executed) with the migration file:line cited.
  </acceptance_criteria>
  <done>A2 discharged with quoted DDL evidence; the additive-JSONB no-migration premise is proven, not assumed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove the PERMANENT ccxt rejection — degrade channel in run_stitch_composite_job</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:3083-3230 — `_reconstruct_deribit`, `_reconstruct_all` (the per-member loop: preflight, the venue rejection at :3154-3175, the typed error arms — `_PERMANENT_LEDGER_ERRORS` / `ccxt.RateLimitExceeded` `_stamp_429` / geo-block transient — and the `clipped/signals/venues/metas` accumulation at :3226-3229).
    - job_worker.py:3233-3300 — how `_reconstruct_all`'s return is consumed (cash pass at :3233; `coverage_mask` input; the `venues` ppy decision at :3279) and :3517-3600 (member_warn_flags union, merged_flags block incl. Plan 93-01's `cumulative_method` line).
    - analytics-service/services/stitch_composite.py — `coverage_mask` signature/shape (whether it accepts an empty series per member, or the per_key entry must be synthesized post-hoc).
    - analytics-service/tests/test_stitch_composite_job.py:86-400 — `_FakeSupabase`, the preflight/combine mock seams, `test_happy_path_two_member_fanout_combined_scalars`.
  </read_first>
  <behavior>
    - Test 1 (rejection-removed, RED on current code): a 2-member composite (seq 1 deribit fixture member + seq 2 bybit member) completes — outcome is NOT a permanent FAILED; persisted `computation_status == "complete_with_warnings"`; `data_quality_flags["degraded_members"] == [{"seq": 2, "venue": "bybit", "reason": "venue_reconstruction_unavailable"}]`; the stitched `csv_daily_returns` equal the deribit-only stitch (member 2 contributes zero rows); `per_key` contains an entry for seq 2 with `n_days == 0`.
    - Test 2 (honest floor): a 1-member all-ccxt composite (single okx member) → PERMANENT FAILED with a scrubbed `_stamp_failed` message stating no member could be reconstructed (never an empty 'complete' track record).
    - Test 3 (drop-stale heal): existing_flags seeded with a stale `degraded_members` list; an all-Deribit re-stitch pops the key (mirrors mtm_gated_reason/insufficient_window healing).
    - Test 4 (leak discipline): the persisted degrade entry contains EXACTLY the keys {seq, venue, reason} with reason the fixed literal — assert no "$", no digits-looking-like-USD, no exception text in the flag or in the _stamp_failed message beyond the scrubbed template.
    - Test 5 (parity): the full existing test_stitch_composite_job.py suite passes unmodified — all-Deribit composites never enter the branch.
  </behavior>
  <action>
    In `_reconstruct_all` (job_worker.py), replace the `venue != "deribit"` PERMANENT rejection (:3154-3175) with three-way routing:

    1. `venue == "deribit"` → existing `_reconstruct_deribit` path, byte-identical (including all existing except arms).
    2. `venue in {"binance", "okx", "bybit"}` → DEGRADE (this plan does not attempt reconstruction — Plan 93-04 adds the attempt): `await aclose_exchange(ctx.exchange)`; append `{"seq": seq, "venue": venue, "reason": "venue_reconstruction_unavailable"}` to a new `degraded: list[dict[str, Any]]` accumulator; `continue` the member loop (NO `_stamp_failed`, NO DispatchResult return). Still append `venue` to `venues` (the #597 ppy blend decision at :3279 must keep seeing the crypto venue; deribit+ccxt mixes stay 365 either way — state this in a comment).
    3. Any OTHER venue (outside `_COMPOSITE_CRYPTO_VENUES`) → keep the current PERMANENT fail semantics (message updated to name the venue as unsupported) — a truly unknown venue is a structural error, not a degradable one.

    Thread `degraded` out of `_reconstruct_all` (extend its return tuple; update the two call sites — cash :3233 and MTM :3394; MTM never runs for a composite containing a ccxt member because `mark_to_market_available` gates OFF any non-deribit venue, so the CASH pass's `degraded` list is authoritative — take it from there only).

    After the fan-out:
    - Zero-reconstructed guard: if `clipped` (successfully reconstructed members) is empty → `_stamp_failed("No composite member could be reconstructed.")` + PERMANENT DispatchResult (Test 2). This replaces what today's rejection guaranteed implicitly.
    - per_key visibility: ensure each degraded seq appears in `mask["per_key"]` as `{"seq": seq, "first_day": None, "last_day": None, "n_days": 0}` — either by passing an empty series into `coverage_mask`'s input IF the pure fn handles empties cleanly (check stitch_composite.py first), or by appending synthesized entries to `mask["per_key"]` post-call, re-sorted by seq. Do NOT modify services/stitch_composite.py (pure core stays untouched).
    - merged_flags persist block (after Plan 93-01's `cumulative_method` line): `degraded_members` set-when-non-empty / pop-when-empty (drop-stale idiom, see interfaces).
    - Status: any degraded member sets `member_warned = True` (rides the existing `complete_with_warnings` promotion at :3589) — deliberate per research Pitfall 5: a composite missing a member IS warn-worthy, unlike the pure-annotation insufficient_window. Do NOT add anything to `NAV_TWR_GUARD_KEYS` (single-key blast radius stays zero).
    - Leak discipline: `reason` is ALWAYS a fixed literal code, never `str(exc)`/f-string interpolation of dynamic values other than seq/venue.

    Write Tests 1-4 FIRST (Test 1 must FAIL on current code — the current permanent fail is the RED), then implement. Transient semantics untouched: the 429 and geo-block arms remain exactly as-is for deribit members (ccxt members in THIS plan never crawl, so those arms are unreachable for them until 93-04).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -q && .venv/bin/python -m mypy services/job_worker.py</automated>
  </verify>
  <acceptance_criteria>
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -k "degrad" -q` exits 0 with ≥4 passed; Test 1's RED proof on pre-change code documented in SUMMARY.
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -q` exits 0 (full file incl. all pre-existing all-Deribit tests, unmodified).
    - `grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c "Deribit-only this phase"` == 0 (the fence is gone).
    - `grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c "venue_reconstruction_unavailable"` >= 1.
    - Parity set green: `cd analytics-service && .venv/bin/python -m pytest tests/test_composite_headline_parity.py tests/test_golden_parity.py tests/test_metrics_parity.py -q` exits 0.
    - mypy exits 0.
  </acceptance_criteria>
  <done>ccxt members degrade instead of killing the composite; zero-member floor stays fail-loud; all-Deribit byte-identical; the DQ key persists with drop-stale healing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render the degrade reason on both existing DQ surfaces (wizard + factsheet)</name>
  <files>src/lib/factsheet/composite-read-path.ts, src/lib/factsheet/composite-read-path.test.ts, src/lib/factsheet/types.ts, src/app/factsheet/[id]/v2/FactsheetView.tsx, src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx</files>
  <read_first>
    - 92-03-SUMMARY.md Task 3 (the pattern to mirror EXACTLY: composite-read-path dqf widen + strict coercion → types.ts dataQuality → FactsheetView hero-strip amber sibling `<p>` (#B45309, `px-3 sm:px-4 py-2 text-micro font-mono`) → SyncPreviewStep `CompositePreviewData` + `hasDqCaveat` OR + `<p>` in the amber `role="status"` Data quality block).
    - src/lib/factsheet/composite-read-path.ts:46-130 (dqf type as left by Plan 93-01, `dataQuality` construction at :121) and src/lib/factsheet/types.ts (the `dataQuality` optional shape).
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx — the existing dq parse + amber Data-quality block (search `hasDqCaveat` / `insufficientWindow`).
    - DESIGN.md — warning-amber token semantics (#B45309) before any visual choice.
  </read_first>
  <behavior>
    - Test 1 (read-path strict coercion): dqf `degraded_members: [{seq: 2, venue: "bybit", reason: "venue_reconstruction_unavailable"}]` → `buildOpts.dataQuality.degradedMembers` equals `[{seq: 2, venue: "bybit"}]`; malformed inputs (a string, `[{seq: "x"}]`, `[42]`, `{}`) → `[]` (nothing renders from junk — T-92-05 discipline).
    - Test 2 (factsheet): FactsheetView with `dataQuality.degradedMembers: [{seq: 2, venue: "bybit"}]` renders an amber hero-strip caveat naming Key 2 / Bybit as excluded; absent/empty → the caveat is NOT in the DOM.
    - Test 3 (wizard): SyncPreviewStep composite snapshot whose dq carries `degraded_members` renders a line inside the existing amber `role="status"` Data quality block; the block also appears when degraded_members is the ONLY caveat (hasDqCaveat OR extended).
  </behavior>
  <action>
    Mirror the 92-03 insufficient_window render slice one-for-one:
    1. composite-read-path.ts: widen the dqf input type with `degraded_members?: unknown`; parse defensively — accept only an array whose entries are objects with a finite numeric `seq` and a string `venue`; map to `{seq, venue}` (drop `reason` from the render payload — the enum code is server vocabulary; the components own the user copy); expose as `dataQuality: { composite: true, insufficientWindow: ..., degradedMembers }`.
    2. types.ts: extend the optional `dataQuality` shape with `degradedMembers?: Array<{ seq: number; venue: string }>`.
    3. FactsheetView.tsx: a sibling amber `<p>` in the hero strip (exact 92-03 pattern/classes/token), copy along the lines of "Key {seq} ({venue}) could not be included — its data is excluded from this track record." One line per degraded member (or one aggregated line if >2 — executor's copy discretion within DESIGN.md).
    4. SyncPreviewStep.tsx: add `degradedMembers` to `CompositePreviewData`, parse from the snapshot dq with the same strict shape-check, OR it into `hasDqCaveat`, render a `<p>` inside the existing amber block. Venue display may reuse the existing exchange-label helper if present.
    5. Tests FIRST per behavior list (RED: caveat absent on current code), then implement.

    Do NOT create a new component (0 new .tsx besides tests), do NOT touch the attribution table (Plan 93-02 owns windowText), do NOT render the raw `reason` string.
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/composite-read-path.test.ts "src/app/factsheet/[id]/v2/FactsheetView.kpistrip.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - All three test files exit 0 with the new behaviors passing; RED proofs documented.
    - Strict-coercion cases: every malformed `degraded_members` shape yields `[]` and renders nothing (asserted, not assumed).
    - `npx vitest run src/lib/factsheet "src/app/factsheet/[id]/v2" "src/app/(dashboard)/strategies/new/wizard/steps"` exits 0 (full surface suites green).
    - No new component file: `git status --porcelain src | grep "^A" | grep -v test | grep -c ".tsx"` == 0.
    - `npx tsc --noEmit` exits 0; `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>A degraded member is visible to the user on both surfaces with honest copy; junk dqf renders nothing; no new component.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exchange → worker | Already-authenticated broker data via existing adapters/preflight (worker-only decrypt in `_allocator_key_preflight` — UNTOUCHED; this plan's ccxt members never even crawl). |
| worker → DB jsonb → render | New `degraded_members` key written by the worker, parsed strictly by both frontends. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-03-01 | Information Disclosure | degraded_members flag + _stamp_failed messages | mitigate | reason is a FIXED enum literal; entry keys closed-set {seq, venue, reason}; no exception text/USD/NAV ever interpolated (Test 4 pins it); scrub_freeform_string retained on the zero-member fail message. Reuses T-73-02/T-76-03-LEAK/T-92-04 discipline per the phase security note. |
| T-93-03-02 | Tampering | frontend degraded_members parse | mitigate | Closed-shape strict coercion (finite numeric seq + string venue, else []) — malformed jsonb renders nothing (T-92-05). |
| T-93-03-03 | Repudiation/Integrity | silent member exclusion | mitigate | The exclusion is NEVER silent: DQ flag + complete_with_warnings + per_key n_days 0 + two render surfaces — the no-invented-data invariant is the mitigation's design center. |
| T-93-03-04 | Denial of Service | zero-member composite | mitigate | Explicit PERMANENT fail with terminal stamp (poller reaches a gate; no infinite 'computing' spin). |
| T-93-03-SC | Tampering | package installs | accept | No packages installed this plan. |

ASVS L1 rationale: no new endpoint/auth path; the degrade path consumes already-authenticated data and emits booleans/enum codes. Blocking severity threshold (high): none identified.
</threat_model>

<verification>
- Full offline gate: `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py tests/test_composite_headline_parity.py tests/test_golden_parity.py tests/test_metrics_parity.py -q` exits 0 AND `npx vitest run src/lib/factsheet src/lib/composite "src/app/(dashboard)/strategies/new/wizard/steps" "src/app/factsheet/[id]/v2"` exits 0.
- `git status --porcelain supabase/migrations/` empty (Task 1 proved additive-JSONB; no migration).
- NON-BLOCKING live gate (document in SUMMARY, do NOT block on it): the full Railway ccxt canary with real Bybit/OKX/Binance keys is the user's corroboration gate (mirrors SC-3 piece 3) — offline fixtures + the degrade path close the phase requirement.
</verification>

<success_criteria>
- HARD-05's success criterion satisfied by Option B alone: ccxt member → honest visible degrade, never fail-loud PERMANENT; zero-member floor stays fail-loud.
- Rejection-removed test RED on pre-change code; all-Deribit parity byte-identical; leak-discipline pinned.
- Plan 93-04 can attach the reconstruction attempt to this channel without frontend churn (reason codes are additive).
</success_criteria>

<output>
Create `.planning/phases/93-composite-data-path-correctness/93-03-SUMMARY.md` when done (include the Task-1 DDL evidence, the RED proofs, and the documented non-blocking Railway canary gate).
</output>
