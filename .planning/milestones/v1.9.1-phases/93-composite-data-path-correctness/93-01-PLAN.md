---
phase: 93-composite-data-path-correctness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_stitch_composite_job.py
  - src/lib/factsheet/composite-read-path.ts
  - src/lib/factsheet/composite-read-path.test.ts
  - src/app/factsheet/[id]/v2/page.tsx
  - src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx
autonomous: true
requirements: [HARD-03]
tags: [composite, cumulative_method, data_quality_flags, factsheet, read-path, HARD-03]

must_haves:
  truths:
    - "A composite stitched after this plan carries data_quality_flags.cumulative_method with the RAW worker value ('geometric' or 'simple') — the method the ONE canonical compute actually used"
    - "The factsheet chart's cumulation basis follows the PERSISTED method when present — editing returns_denominator_config without re-stitching can no longer make the chart disagree with the frozen headline scalars (closes #69 / Phase-90 LOW-2)"
    - "An older composite with NO persisted cumulative_method renders exactly as before (live-config fallback) — zero behavior change until its next re-stitch stamps the key (self-heal, HARD-04 insufficient_window precedent)"
    - "No persisted scalar moves: parity set byte-identical"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "merged_flags['cumulative_method'] unconditional set in the run_stitch_composite_job persist block"
      contains: "cumulative_method"
    - path: "src/lib/factsheet/composite-read-path.ts"
      provides: "prefer-persisted-with-fallback method resolution in readCompositeFactsheet"
      contains: "cumulative_method"
    - path: "src/lib/factsheet/composite-read-path.test.ts"
      provides: "drift-kill regression: persisted method beats live config; absent/malformed falls back"
    - path: "analytics-service/tests/test_stitch_composite_job.py"
      provides: "stitch-persist regression: cumulative_method lands in data_quality_flags for both geometric and simple runs"
  key_links:
    - from: "analytics-service/services/job_worker.py cumulative_method decision (:3312-3317)"
      to: "merged_flags persist block (:3549-3587)"
      via: "unconditional merged_flags['cumulative_method'] = cumulative_method"
      pattern: "merged_flags\\[\"cumulative_method\"\\]"
    - from: "src/lib/factsheet/composite-read-path.ts"
      to: "buildOpts.cumulativeMethod"
      via: "strict-literal persisted read with attributionBasisFromConfig fallback"
      pattern: "attributionBasisFromConfig"
---

<objective>
HARD-03: freeze the composite's cumulation method into `data_quality_flags` at stitch time and make `readCompositeFactsheet` prefer the persisted value over the live-config re-derive, so the chart can never disagree with the persisted headline scalars.

Purpose: today the headline scalars are frozen at stitch under the method chosen at `job_worker.py:3312-3317`, but the chart re-derives the method LIVE from `strategies.returns_denominator_config` (`composite-read-path.ts:103`). Editing config after publish without re-stitching flips the chart basis while the headline stays frozen — chart/headline drift (#69).
Output: additive DQ key at stitch + prefer-persisted read with fallback, both regression-pinned; parity byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-composite-data-path-correctness/93-RESEARCH.md  (§HARD-03 — exact sites, verified this session)
@.planning/phases/92-composite-metric-blow-up-annualization-honesty/92-03-SUMMARY.md  (the insufficient_window additive-DQ + strict `=== true` coercion patterns this plan copies)

<interfaces>
Current read-path re-derive (src/lib/factsheet/composite-read-path.ts:101-103):
```ts
// C-1: cumulative method follows the persisted config (arithmetic only for the
// "simple"/allocated-capital override), NOT a hardcoded arithmetic.
const cumulativeMethod = attributionBasisFromConfig(returnsDenominatorConfig);
```

The ONE mapping rule (src/lib/composite/compositeAttribution.ts:49-60 — DO NOT MODIFY, the fallback keeps it):
```ts
export function attributionBasisFromConfig(raw: unknown): "arithmetic" | "geometric" {
  // raw?.cumulative_method === "simple" -> "arithmetic"; everything else -> "geometric"
}
```

Worker method decision (analytics-service/services/job_worker.py:3312-3317, in scope at the persist block — same function body `run_stitch_composite_job`):
```python
cumulative_method = denominator_config.cumulative_method  # "geometric" | "simple"
# else: cumulative_method = "geometric"
```

Persist idiom to mirror (job_worker.py:3571-3575, HARD-04 precedent):
```python
if cash_metrics_result.insufficient_window:
    merged_flags["insufficient_window"] = True
else:
    merged_flags.pop("insufficient_window", None)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist the RAW cumulative_method into merged_flags at stitch (job_worker.py) + regression tests</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:3300-3600 — the method decision (:3312-3317), the `_metrics_result_for` compute feed (:3355-3361), and the `merged_flags` read-modify-write block (:3549-3600). Verified this session.
    - analytics-service/tests/test_stitch_composite_job.py:86-400 — the offline `_FakeSupabase` harness (:162), the `combine_native_ledger` mock seam (:271), and `test_happy_path_two_member_fanout_combined_scalars` (:331) — the fixture pattern to extend. Search the file for `returns_denominator_config` to find how a non-None config is injected.
  </read_first>
  <behavior>
    - Test 1 (RED-first): a default stitch run (denominator_config None → method "geometric") persists `data_quality_flags["cumulative_method"] == "geometric"` in the strategy_analytics upsert payload captured by _FakeSupabase. FAILS on current code (key absent).
    - Test 2: a stitch run whose strategies row carries `returns_denominator_config` with `cumulative_method: "simple"` persists `data_quality_flags["cumulative_method"] == "simple"`.
    - Test 3 (Pitfall-1 vocabulary pin): the persisted value is asserted `in {"geometric", "simple"}` and explicitly `!= "arithmetic"` — the RAW worker vocabulary is persisted, never the resolved read-path basis.
  </behavior>
  <action>
    In `run_stitch_composite_job`'s merged_flags block (job_worker.py:3549-3587), immediately after the HARD-04 `insufficient_window` set/pop block (:3571-3575) and before the `benchmark_unavailable` block, add ONE unconditional line:

    `merged_flags["cumulative_method"] = cumulative_method`

    `cumulative_method` is already in scope (assigned at :3313/:3316 in the same function body) and always defined — a plain unconditional set is the correct drop-stale form (every re-stitch overwrites; no stale value can survive; no pop branch needed). Persist the RAW worker string (`"geometric" | "simple"`), NOT the resolved `"arithmetic"/"geometric"` basis — the `"simple"→"arithmetic"` translation lives in exactly ONE place (the read side, Task 2), so the persisted value and the live-fallback value share one rule and cannot diverge (research Pitfall 1).

    Write the three tests in `test_stitch_composite_job.py` FIRST (RED), then the one-line fix (GREEN). Reuse the existing offline harness verbatim: `_FakeSupabase` payload capture + the `combine_native_ledger` monkeypatch — no live DB, no new fixtures beyond a `returns_denominator_config` variant of the existing happy-path strategy row.

    Do NOT touch: `metrics_json_by_basis` construction, the `_metrics_result_for` compute, `insufficient_window`/`mtm_gated_reason` handling, or any single-key path. This is one additive line + tests.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -k cumulative_method -q && .venv/bin/python -m pytest tests/test_stitch_composite_job.py tests/test_composite_headline_parity.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -k cumulative_method -q` exits 0 with ≥3 passed.
    - RED proof documented in SUMMARY: with the `merged_flags["cumulative_method"]` line reverted (or before adding it), the same selector has ≥1 FAILED.
    - `grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c 'merged_flags\["cumulative_method"\]'` == 1 (exactly one persist site, comment lines excluded).
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py tests/test_composite_headline_parity.py -q` exits 0 (parity/orchestration unmoved).
    - `cd analytics-service && .venv/bin/python -m mypy services/job_worker.py` exits 0.
  </acceptance_criteria>
  <done>Every fresh stitch persists the raw method into data_quality_flags; both method values pinned offline; headline parity byte-identical.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: readCompositeFactsheet prefers the persisted method, falls back for older composites</name>
  <files>src/lib/factsheet/composite-read-path.ts, src/lib/factsheet/composite-read-path.test.ts, src/app/factsheet/[id]/v2/page.tsx, src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx</files>
  <read_first>
    - src/lib/factsheet/composite-read-path.ts:46-130 — the `dqf` input type (:50-59) and the C-1 resolution site (:101-103). Verified this session.
    - src/lib/composite/compositeAttribution.ts:49-60 — `attributionBasisFromConfig` (the fallback; unchanged).
    - src/app/factsheet/[id]/v2/page.tsx:80-120 and src/app/(dashboard)/discovery/[slug]/[strategyId]/page.tsx:84-114 — the two page-level `dqf` casts feeding `readCompositeFactsheet` (both surfaces route through the SAME function — one fix covers both).
    - src/lib/factsheet/composite-read-path.test.ts — existing C-1 method-resolution tests (must stay green: they ARE the older-composite fallback pin).
  </read_first>
  <behavior>
    - Test 1 (drift-kill, RED-first): dqf `{ composite: true, cumulative_method: "simple" }` + returnsDenominatorConfig that would live-derive "geometric" → `buildOpts.cumulativeMethod === "arithmetic"`. FAILS on current code (live re-derive wins).
    - Test 2 (reverse drift-kill): dqf `{ composite: true, cumulative_method: "geometric" }` + config `{ cumulative_method: "simple" }` → `buildOpts.cumulativeMethod === "geometric"`.
    - Test 3 (older-composite fallback): dqf WITHOUT `cumulative_method` + config `{ cumulative_method: "simple" }` → `"arithmetic"` (existing live behavior byte-identical).
    - Test 4 (strict-literal coercion, T-92-05 discipline): dqf `cumulative_method` of `true`, `42`, `"arithmetic"`, `{}` → each falls back to `attributionBasisFromConfig(returnsDenominatorConfig)` (only the exact strings "simple"/"geometric" are honored).
  </behavior>
  <action>
    In `readCompositeFactsheet` (composite-read-path.ts):
    1. Widen the `dqf` input type (:50-59) with `cumulative_method?: unknown;`.
    2. Replace the single resolution line at :103 with prefer-persisted-with-fallback, keeping the ONE mapping rule identical to `attributionBasisFromConfig`:
       ```
       const persisted = dqf?.cumulative_method;
       const cumulativeMethod =
         persisted === "simple" ? "arithmetic"
         : persisted === "geometric" ? "geometric"
         : attributionBasisFromConfig(returnsDenominatorConfig);
       ```
       Keep/extend the C-1 comment to name the new precedence: persisted (frozen at stitch, matches the headline compute by construction — Task 1) > live config re-derive (older composites without the key; self-heals on next re-stitch, HARD-04 precedent).
    3. Check the two page-level `dqf` casts (v2/page.tsx:86-89; discovery [strategyId]/page.tsx:84-88): if the cast enumerates fields explicitly, add `cumulative_method?: unknown`; if it casts to the imported input type, no edit is needed — record which in the SUMMARY.

    Do NOT modify `attributionBasisFromConfig` (compositeAttribution.ts) — it stays the single fallback rule and its tests must stay green. Do NOT touch the wizard preview's live re-derive (`SyncPreviewStep.tsx:1089`): it runs on a fresh pre-publish stitch where drift cannot occur — deliberately out of scope (research §HARD-03), note in SUMMARY.

    Write the four tests FIRST in composite-read-path.test.ts (RED on Test 1), then the fix (GREEN).
  </action>
  <verify>
    <automated>npx vitest run src/lib/factsheet/composite-read-path.test.ts src/lib/composite/compositeAttribution.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/factsheet/composite-read-path.test.ts` exits 0 with the 4 new behaviors passing; RED proof for Test 1 documented in SUMMARY.
    - `npx vitest run src/lib/composite/compositeAttribution.test.ts` exits 0 with ZERO test-file changes (`git diff --stat src/lib/composite/compositeAttribution.ts src/lib/composite/compositeAttribution.test.ts` is empty).
    - Pre-existing composite-read-path tests all pass unmodified except any that seed a dqf fixture needing the widened optional field (record any such edit as a deviation).
    - `npx tsc --noEmit` exits 0; `npm run lint` reports 0 errors.
  </acceptance_criteria>
  <done>Chart basis == persisted headline basis whenever the key exists; older composites byte-identical via fallback; one mapping rule, two consumers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB jsonb → factsheet render | `data_quality_flags.cumulative_method` (server-written) read by the factsheet read-path — treated as untrusted at the parse site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01-01 | Tampering | composite-read-path.ts persisted-method parse | mitigate | Strict-literal coercion: only exact strings "simple"/"geometric" honored; anything else falls back to the existing live derive (T-92-05 `=== true` discipline). |
| T-93-01-02 | Information Disclosure | merged_flags persist | accept | The persisted value is a fixed enum string ("geometric"/"simple") — no account-size, USD, or freeform content (leak discipline T-73-02/T-92-04 upheld). |
| T-93-01-SC | Tampering | package installs | accept | No packages installed this plan (RESEARCH Package Legitimacy Audit: none required). |

ASVS L1 rationale: no new endpoint, no new auth path, no new input boundary — internal compute annotation + read-only display preference. No high-severity threat introduced.
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py tests/test_composite_headline_parity.py tests/test_golden_parity.py tests/test_metrics_parity.py -q` exits 0 (SC-4 parity set).
- `npx vitest run src/lib/factsheet src/lib/composite` exits 0.
- No file under `supabase/migrations/` is added or modified (`git status --porcelain supabase/migrations/` empty) — additive JSONB key only. If a migration turns out to be needed, STOP and flag prominently (migrations auto-apply to PROD on merge).
</verification>

<success_criteria>
- HARD-03 regression tests exist on both layers and FAIL without their fixes (RED evidence in SUMMARY).
- Persisted RAW method ("geometric"|"simple"); "simple"→"arithmetic" mapped on read with the identical rule; older composites fall back byte-identically.
- Parity set green; no migration; mypy/tsc/lint clean.
</success_criteria>

<output>
Create `.planning/phases/93-composite-data-path-correctness/93-01-SUMMARY.md` when done (include the RED proofs, the page-cast finding from Task 2 step 3, and the deliberate SyncPreviewStep:1089 non-change).
</output>
