---
phase: 93-composite-data-path-correctness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/strategies/composite/set-members/route.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx
autonomous: true
requirements: [HARD-02]
tags: [composite, wizard, window-capture, set-members, SyncPreviewStep, HARD-02]

must_haves:
  truths:
    - "The FIRST member's entered window_start VALUE is pinned end-to-end offline: panel[0].windowStart survives the handleContinue mapping into keys[0].window_start, and the route forwards that exact value into set_wizard_composite_members p_members[0].window_start — a regression test fails without each link"
    - "A member whose ENTERED window is persisted in strategy_keys never renders 'window –' in the wizard's Data window column — a reconstructed-coverage n_days=0 can no longer masquerade as a missing window (Days stays honest coverage)"
    - "HARD-02 is closed on the hardened offline contract + the display fix — NOT on the live symptom; the preserved-live-composite corroboration is a documented NON-BLOCKING gate"
  artifacts:
    - path: "src/app/api/strategies/composite/set-members/route.test.ts"
      provides: "first-member window_start pinned BY VALUE in p_members (today only existence + last-member null are pinned)"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts"
      provides: "offline pure test of the panel→keys[] mapping (no Supabase, no render)"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "Data window column falls back to the member's DECLARED strategy_keys window when per_key coverage is absent"
  key_links:
    - from: "MultiKeyConnectStep.tsx handleContinue"
      to: "exported pure payload builder"
      via: "handleContinue calls the extracted builder (no inline duplicate mapping)"
      pattern: "buildSetMembersKeys"
    - from: "SyncPreviewStep.tsx attribution table windowText"
      to: "memberBySeq declared window (strategy_keys read at :602-608)"
      via: "declared-window fallback when pk first_day/last_day absent"
      pattern: "windowStart"
---

<objective>
HARD-02: lock the first member key's entered-window contract with offline value-level regression tests, and fix the wizard display so an ENTERED window is always shown — never a reconstructed `n_days=0` masquerading as `window "–" / Days 0`.

Purpose: research verified the WRITE path is correct (`MultiKeyConnectStep.handleContinue:502-507` → `set-members/route.ts:111-115` → `set_wizard_composite_members` RPC, migration 20260710180000:208-220 — NOT `/api/keys/sync`). The live symptom therefore lives on the read/display side (Branch A — the wizard renders `per_key` reconstructed coverage as "window") or a client-state edge (Branch B). The strengthened route + mapping tests pin Branch B's contract; the display fix closes Branch A. Per the resolved phase decision, this is a read/display + hardened-contract fix — no write-path change, no migration.
Output: value-level offline pins on both write-path links + honest declared-window display; live repro stays a documented non-blocking corroboration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-composite-data-path-correctness/93-RESEARCH.md  (§HARD-02 — verified write path + the two candidate branches)

<interfaces>
Current client mapping (MultiKeyConnectStep.tsx:502-507, inside handleContinue):
```ts
const keys = current.map((p, i) => ({
  api_key_id: p.apiKeyId,
  window_start: p.windowStart,
  window_end: p.stillLive ? null : p.windowEnd || null,
  seq: i + 1,
}));
```

Current route payload (set-members/route.ts:111-115 — seq intentionally omitted; RPC re-derives from window_start ASC):
```ts
const p_members = members.map((k) => ({
  api_key_id: k.api_key_id,
  window_start: k.window_start,
  window_end: k.window_end,
}));
```

Current display defect site (SyncPreviewStep.tsx:1213-1219, the composite attribution table):
```ts
const pk = perKeyBySeq.get(row.seq);
const member = memberBySeq.get(row.seq);
const windowText =
  pk?.first_day && pk?.last_day
    ? `${pk.first_day} – ${pk.last_day}`
    : "—";                       // ← an entered window renders "–" when coverage is absent
```
`member` carries the DECLARED strategy_keys window (`windowStart`, `windowEnd` — read at :602-608, mapped at :685-691) but is only used for label/exchange today.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin the first member's window_start BY VALUE — extracted pure mapping + strengthened route test</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts, src/app/api/strategies/composite/set-members/route.test.ts</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx:440-535 — `handleContinue` (the inline keys[] mapping at :502-507), `panelsRef`, and the panel state shape (`apiKeyId`, `windowStart`, `windowEnd`, `stillLive`). Also :714-719 (`canValidate` requires `!!p.windowStart`) and :452-457 (`allValidated`/`canContinue`) — the invariants the mapping test encodes.
    - src/app/api/strategies/composite/set-members/route.test.ts:60-186 — `VALID_KEYS` fixture (:64-77, first member window_start "2025-08-01"), the happy-path p_members assertions (:173-183: existence-only today + members[2].window_end null).
    - src/app/api/strategies/composite/set-members/route.ts:100-125 — the p_members build the test pins.
  </read_first>
  <behavior>
    - Test A (new file, pure/offline — no Supabase, no render): `buildSetMembersKeys(panels)` with 3 panels where panel[0] has windowStart "2025-08-01" → `keys[0].window_start === "2025-08-01"` (exact value). Additional cases: `stillLive: true` → `window_end === null`; `stillLive: false, windowEnd: ""` → `window_end === null`; `stillLive: false, windowEnd: "2025-09-30"` → `window_end === "2025-09-30"`; `seq === i + 1` for every element; order preserved (panel index i → keys[i]).
    - Test B (route.test.ts, strengthen :173-183): `expect(members[0].window_start).toBe("2025-08-01")` and value-pins for members[1]/members[2] window_start from VALID_KEYS — the FIRST member's VALUE round-trips into p_members, not just the property's existence. Existing pins (no seq, member_count, last window_end null) unchanged.
  </behavior>
  <action>
    1. In MultiKeyConnectStep.tsx, extract the inline `current.map((p, i) => ...)` at :502-507 into an EXPORTED pure function in the same file (behavior byte-identical — same field names, same `p.stillLive ? null : p.windowEnd || null` expression, same `seq: i + 1`):
       `export function buildSetMembersKeys(panels: ReadonlyArray<{ apiKeyId: string | null; windowStart: string; windowEnd: string; stillLive: boolean }>): Array<{ api_key_id: string | null; window_start: string; window_end: string | null; seq: number }>`
       (match the ACTUAL panel-state field types found in the file — the structural param type must accept the real PanelState). `handleContinue` calls the helper; the inline map is deleted. No other change to the component (Rule 3 — do not touch enterMulti, panel editing, or the summary-chip path).
    2. New offline test file `MultiKeyConnectStep.payload.test.ts` (same dir): Test A cases above. Pure import — no jsdom render, no fetch mock, no Supabase.
    3. Strengthen route.test.ts per Test B. Mutation-honesty check (document in SUMMARY): temporarily change route.ts's p_members map to drop/blank `window_start` for index 0 → Test B FAILS; revert.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts" src/app/api/strategies/composite/set-members/route.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exit 0; the mutation-honesty FAIL (window_start dropped for index 0 → route test RED) is documented in SUMMARY and reverted.
    - `grep -v '^\s*//' "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx" | grep -c "buildSetMembersKeys"` >= 2 (definition + handleContinue call site — the helper is WIRED, not orphaned).
    - The wizard suite still passes: `npx vitest run "src/app/(dashboard)/strategies/new/wizard"` exits 0.
    - `npx tsc --noEmit` exits 0; `npm run lint` 0 errors.
    - NO change to set-members/route.ts, the RPC, or any migration (`git status --porcelain src/app/api/strategies/composite/set-members/route.ts supabase/migrations/` empty) — the write path is verified correct; this task hardens its contract only.
  </acceptance_criteria>
  <done>Both write-path links (client mapping, route forwarding) pin the first member's window_start by exact value, offline; a future regression on either link goes RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyncPreviewStep Data-window column — declared-window fallback, never "–" for an entered window</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx:595-700 (strategy_keys read with window_start/window_end at :602-608; members mapping `windowStart`/`windowEnd` at :685-691; perKey parse at :694-696) and :1180-1260 (the attribution table: `windowText` at :1213-1219, Days column `pk?.n_days ?? "—"` at :1245).
    - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx — existing render-test fixture shapes (how members + per_key snapshots are seeded).
  </read_first>
  <behavior>
    - Test 1 (RED-first, the Branch-A symptom): a composite snapshot where member seq=1 has declared windowStart "2025-08-03" / windowEnd null, and its per_key entry has `first_day: null, last_day: null, n_days: 0` → the Data window cell renders "2025-08-03 – live" (NOT "—"); the Days cell still renders 0 (coverage stays honest).
    - Test 2 (regression): a member WITH per_key coverage (`first_day: "2025-08-03", last_day: "2025-09-24"`) renders "2025-08-03 – 2025-09-24" exactly as today (actual coverage beats declared when present).
    - Test 3 (bounded end): declared windowStart "2025-08-03" + windowEnd "2025-09-30" + absent coverage → "2025-08-03 – 2025-09-30".
  </behavior>
  <action>
    Change ONLY the `windowText` derivation at SyncPreviewStep.tsx:1213-1219 to a three-tier fallback:

    ```
    const windowText =
      pk?.first_day && pk?.last_day
        ? `${pk.first_day} – ${pk.last_day}`
        : member?.windowStart
          ? `${member.windowStart} – ${member.windowEnd ?? "live"}`
          : "—";
    ```

    Tier 1 (unchanged): actual reconstructed coverage when present. Tier 2 (new): the member's DECLARED entered window from strategy_keys (`memberBySeq`) — this is user-entered metadata read from the DB, not invented data; the Days column continues to show coverage (`pk?.n_days ?? "—"`) UNCHANGED, so a zero-coverage member reads "declared window / Days 0" honestly. "live" matches the wizard's stillLive open-ended vocabulary. Tier 3 (unchanged): "—" only when no window was ever entered.

    Do NOT change: the Days column, the coverage gantt (declared bars already correct), the union rangeClause (:1041-1055), contribution rendering, or any per_key parsing. One expression + tests.

    Note for the executor (context, not scope): after Plan 93-03 lands, a degraded ccxt member appears in per_key with n_days 0 — this fallback is exactly what makes such a member render its entered window honestly. HARD-02's live-repro corroboration (the preserved live composite) is a documented NON-BLOCKING gate: do not claim the live symptom reproduced-then-fixed; claim the offline contract + display fix (state this in the SUMMARY).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - The render test file exits 0 with the 3 behaviors passing; Test 1's RED proof (current code renders "—") documented in SUMMARY.
    - `npx vitest run "src/app/(dashboard)/strategies/new/wizard"` exits 0 (whole wizard suite green).
    - `git diff --stat` for SyncPreviewStep.tsx shows changes confined to the windowText derivation (no Days/gantt/contribution churn).
    - `npx tsc --noEmit` exits 0; `npm run lint` 0 errors.
  </acceptance_criteria>
  <done>An entered window always renders in the Data window column; coverage honesty (Days) untouched; Branch A closed at the display source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → set-members route | Untrusted wizard payload — ALREADY re-validated server-side with the shared `keyWindowsSchema` (route.ts:71) + SECURITY DEFINER RPC with auth.uid() guard. This plan changes NO server code. |
| DB → wizard render | strategy_keys declared windows + per_key jsonb rendered read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02-01 | Tampering | buildSetMembersKeys extraction | mitigate | Byte-identical mapping pinned by value-level tests on BOTH links (client mapping + route p_members) — a silent field drop/rename goes RED. |
| T-93-02-02 | Information Disclosure | Data-window declared fallback | accept | Renders only the user's OWN entered dates already displayed elsewhere in the wizard (gantt bars); no key material, no USD, worker-only-decryption untouched. |
| T-93-02-SC | Tampering | package installs | accept | No packages installed this plan. |

ASVS L1 rationale: no new endpoint, no auth change, no new input crossing a trust boundary — a pure-function extraction, test hardening, and a read-only display fallback. No high-severity threat introduced.
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard" src/app/api/strategies/composite/set-members/route.test.ts` exits 0.
- `git status --porcelain supabase/migrations/` empty (no migration — the column + RPC already exist).
- SUMMARY records the HARD-02 closure basis explicitly: hardened offline contract (value pins on both links) + display fix; live preserved-composite corroboration documented as NON-BLOCKING and NOT claimed.
</verification>

<success_criteria>
- First-member window_start pinned BY VALUE offline at both write-path links; mutation-honest (dropping it reddens the route test).
- "window –/Days 0" impossible for an entered window; Days stays coverage-honest.
- Wizard suite green; tsc/lint clean; zero server/migration changes.
</success_criteria>

<output>
Create `.planning/phases/93-composite-data-path-correctness/93-02-SUMMARY.md` when done (include both RED proofs and the explicit non-blocking-live-gate closure statement).
</output>
