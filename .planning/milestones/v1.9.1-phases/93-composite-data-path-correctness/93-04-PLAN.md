---
phase: 93-composite-data-path-correctness
plan: 04
type: execute
wave: 3
depends_on: [93-03]
files_modified:
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_stitch_composite_job.py
autonomous: true
requirements: [HARD-05]
tags: [composite, ccxt, reconstruction, combine_realized_and_funding, flow-terminus, HARD-05, option-a]

must_haves:
  truths:
    - "A Bybit/OKX/Binance composite member RECONSTRUCTS honestly through the SAME primitives the single-key derive path uses (combine_realized_and_funding + ccxt_rows_to_dated_flows + the evidence-gated flow-coverage terminus) — its clipped window contributes real rows to the stitched series"
    - "The ccxt member's reconstructed series is byte-consistent (rtol 1e-12) with a reference series computed directly from the same primitives on the same fixture inputs — no fork, no silently-divergent math (research A3)"
    - "A ccxt member whose ATTEMPTED reconstruction fails structurally degrades via Plan 93-03's channel with reason 'reconstruction_failed' — the composite still completes with the visible DQ caveat; 429/geo-block stay TRANSIENT (whole-job retry, never a degrade)"
    - "Member guard flags from the ccxt reconstruction (NAV_TWR_GUARD_KEYS, flow_coverage_incomplete, unrealized_pnl_unreadable) union into merged_flags by the EXISTING per-member meta loop — honest caveats surface by construction (Phase 92 2c4753a9 unblocked this)"
    - "All-Deribit composites and the single-key derive path are byte-identical — run_derive_broker_dailies is NOT refactored this phase (additive new-branch-only, SC-4)"
  artifacts:
    - path: "analytics-service/services/job_worker.py"
      provides: "_reconstruct_ccxt_member composing the imported derive-path primitives; venue routing attempts reconstruction before degrading"
      contains: "_reconstruct_ccxt_member"
    - path: "analytics-service/tests/test_stitch_composite_job.py"
      provides: "offline mixed deribit+ccxt stitch fixture (primitives mocked at the FETCH layer, math REAL), byte-consistency pin, structural-failure degrade, transient passthrough"
  key_links:
    - from: "_reconstruct_all ccxt branch"
      to: "_reconstruct_ccxt_member"
      via: "try-reconstruct-then-degrade routing (reason 'reconstruction_failed' on structural failure)"
      pattern: "_reconstruct_ccxt_member"
    - from: "_reconstruct_ccxt_member"
      to: "combine_realized_and_funding / ccxt_rows_to_dated_flows / apply_flow_coverage_terminus"
      via: "verbatim reuse of the derive-path primitives (job_worker.py:2318-2560 mirrored, not forked)"
      pattern: "combine_realized_and_funding"
---

<objective>
HARD-05 STRETCH-in-phase (Option A, resolved decision: prefer reconstruct where the existing machinery makes it low-risk): actually reconstruct ccxt (Bybit/OKX/Binance) composite members by composing the derive path's existing, importable primitives per member — removing "only Deribit composites work" as a limitation rather than merely degrading around it. Plan 93-03's degrade channel remains the fallback for members that fail structurally.

Why one plan covers all three venues: the derive path's ccxt reconstruction is ONE venue-generic branch (`job_worker.py:2318-2560`, verified this session) — equity+wedge read, full-history trades, per-venue funding fetch dispatch, retention-bounded flow fetch, same-day flow valuation, `combine_realized_and_funding`, evidence-gated flow-coverage terminus. Only the funding-fetch dispatch is per-venue (3 lines). Phase 92's `2c4753a9` fixed the meta boundary so every guard flag round-trips — the historical blocker to partial ccxt composition is gone.

Output: honest ccxt member reconstruction, offline-proven byte-consistent with the derive semantics; live Railway ccxt canary documented as the NON-BLOCKING user corroboration gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/93-composite-data-path-correctness/93-RESEARCH.md  (§HARD-05 — the machinery inventory + reuse mandate, Pitfall 3)
@.planning/phases/93-composite-data-path-correctness/93-03-SUMMARY.md  (the degrade channel + reason-code contract this plan extends)

<interfaces>
The derive-path ccxt block to mirror per-member (job_worker.py:2318-2560 — read it in full; the helper composes these EXACT calls):
```python
equity, balance_error, open_unrealized_usd, upnl_unreadable = await fetch_account_equity_and_upnl_usd(ctx.exchange, venue)
realized = await fetch_all_trades(ctx.exchange, since_ms=None)
funding = await fetch_funding_{binance|okx|bybit}(ctx.exchange, funding_label, None)   # 3-way venue dispatch
_retention_floor = flow_retention_floor(venue, _now_utc)                                # LOW-2 shared floor
_deposits/_withdrawals = await fetch_ccxt_transfers(ctx.exchange, ..., _flow_since_ms, now_ms)
_price_index = await _resolve_ccxt_flow_price_index(ctx.exchange, venue, ctx.supabase, _flow_rows)
external_flows = ccxt_rows_to_dated_flows(_flow_rows, venue=venue, price_index=_price_index)   # raises NavReconstructionError (structural)
# FLOW-04 noise guard: balance_error or equity None or |equity| <= DUST_NAV_FLOOR → open_unrealized_usd = 0.0
returns, meta = combine_realized_and_funding(realized, funding, account_balance=equity,
    balance_error=balance_error, external_flows=external_flows, open_unrealized_usd=open_unrealized_usd)
# MUST-2: upnl_unreadable on a trustworthy anchor → meta["unrealized_pnl_unreadable"] = True
# DQ-02 evidence-gated terminus (CRITICAL-1): flow_coverage_terminus_day → negative_nav_guard_pre_terminus
#   + flow_coverage_gap_evidence gate → apply_flow_coverage_terminus → meta["flow_coverage_incomplete"]
```

Return contract (must match `_reconstruct_deribit`'s shape): `tuple[pd.Series, bool, dict[str, Any]]` = `(returns, has_option_activity=False, dict(meta))` — `False` keeps the MTM gate OFF for ccxt (correct: no options book signal; `mark_to_market_available` already gates any non-deribit venue).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _reconstruct_ccxt_member — per-member reuse of the derive primitives + try-reconstruct-then-degrade routing</name>
  <files>analytics-service/services/job_worker.py</files>
  <read_first>
    - analytics-service/services/job_worker.py:2318-2560 — the ENTIRE ccxt derive block (fetches, noise guard, combine, MUST-2 upnl_unreadable stamp, the CRITICAL-1 evidence-gated terminus with `negative_nav_guard_pre_terminus` + `flow_coverage_gap_evidence`). This is the specification; the helper mirrors it faithfully.
    - job_worker.py:3083-3230 as left by Plan 93-03 — `_reconstruct_deribit` (the shape sibling), the routing that currently degrades ccxt members unconditionally, and the typed error arms (`_PERMANENT_LEDGER_ERRORS`, `ccxt.RateLimitExceeded` → `_stamp_429` transient, geo-block transient).
    - The import sites of `fetch_account_equity_and_upnl_usd`, `fetch_all_trades`, `fetch_funding_binance/okx/bybit`, `fetch_ccxt_transfers`, `_resolve_ccxt_flow_price_index`, `ccxt_rows_to_dated_flows`, `combine_realized_and_funding`, `flow_retention_floor`, `flow_coverage_terminus_day`, `apply_flow_coverage_terminus`, `negative_nav_guard_pre_terminus`, `flow_coverage_gap_evidence`, `DUST_NAV_FLOOR` — all already imported or importable in job_worker.py (grep each before writing).
  </read_first>
  <behavior>
    - (Behavior is pinned by Task 2's tests; this task lands the implementation the RED tests target. Write Task 2's Test 1/2 first if practical — the two tasks may interleave RED→GREEN.)
  </behavior>
  <action>
    1. Add `async def _reconstruct_ccxt_member(ctx, venue: str) -> tuple[pd.Series, bool, dict[str, Any]]` inside `run_stitch_composite_job` (sibling to `_reconstruct_deribit`). Compose the derive block's calls VERBATIM in the same order (see interfaces): equity/wedge read → full-history trades → per-venue funding dispatch (use `strategy_id` as the funding label — rows are consumed in-memory only, never upserted) → retention-bounded transfers via the SHARED `flow_retention_floor` → `_resolve_ccxt_flow_price_index` → `ccxt_rows_to_dated_flows` → FLOW-04 noise guard (balance_error / equity None / dust → wedge 0.0) → `combine_realized_and_funding(...)` → MUST-2 `unrealized_pnl_unreadable` stamp gated on a trustworthy anchor → the CRITICAL-1 evidence-gated terminus (terminus day → `negative_nav_guard_pre_terminus` + `flow_coverage_gap_evidence` gate → `apply_flow_coverage_terminus` → `meta["flow_coverage_incomplete"]`). Return `(returns, False, dict(meta))`. Cash basis only — assert/comment that the MTM pass can never request a ccxt member (`mark_to_market_available` gates OFF non-deribit venues).
       DELIBERATE non-refactor (SC-4): `run_derive_broker_dailies_job` is NOT modified — the helper composes the same imported primitives rather than extracting a shared orchestrator; the primitives carry the math, so no fork of MATH exists (research Pitfall 3 satisfied at the primitive layer). Record the derive/stitch orchestration-consolidation as an explicit deferred follow-up in the SUMMARY.
    2. Update the ccxt arm of the Plan 93-03 routing: `venue in {"binance","okx","bybit"}` → `try: returns, has_opt, member_meta = await _reconstruct_ccxt_member(ctx, venue)` with error routing:
       - `except (NavReconstructionError, *_PERMANENT_LEDGER_ERRORS)` (structural) → degrade via the 93-03 channel with reason `"reconstruction_failed"` (aclose + append to `degraded` + `continue`) — never `_stamp_failed`, never a whole-job PERMANENT.
       - `except ccxt.RateLimitExceeded` → `_stamp_429` + TRANSIENT DispatchResult (whole-job retry — mirror the deribit arm EXACTLY; a rate limit is not a member defect).
       - geo-block (`is_geo_blocked`) → TRANSIENT DispatchResult (mirror the deribit arm).
       - `finally: await aclose_exchange(ctx.exchange)` (match the deribit arm's close discipline; ensure no double-close with the degrade path).
       On success: `clipped.append((seq, clip_to_window(returns, m["window_start"], m.get("window_end"))))`, `signals.append(MemberBasisSignal(seq=seq, venue=venue, has_option_activity=False))`, `venues.append(venue)`, `metas.append(member_meta)` — the downstream clip/stitch/coverage_mask/member_warn_flags loop is venue-agnostic and UNCHANGED (guard flags union by construction via NAV_TWR_GUARD_KEYS).
    3. Keep the `"venue_reconstruction_unavailable"` reason code ONLY for venues outside the supported set if 93-03 used it there; supported ccxt venues now either reconstruct or degrade with `"reconstruction_failed"`. Frontend needs no change (93-03 renders any entry).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m mypy services/job_worker.py && grep -v '^\s*#' services/job_worker.py | grep -c "_reconstruct_ccxt_member" | awk '$1 >= 2 {print "WIRED"; exit 0} {exit 1}'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -v '^\s*#' analytics-service/services/job_worker.py | grep -c "_reconstruct_ccxt_member"` >= 2 (definition + routing call — wired, not orphaned).
    - `git diff` shows ZERO changes inside `run_derive_broker_dailies_job` (the derive path body untouched — additive new-branch-only).
    - mypy exits 0.
    - Task 2's suite (the actual behavior gate) passes.
  </acceptance_criteria>
  <done>ccxt members attempt honest reconstruction through the shared primitives; structural failure degrades visibly; transients retry the whole job.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Offline proofs — mixed-venue stitch, byte-consistency, degrade-on-structural-failure, blast radius</name>
  <files>analytics-service/tests/test_stitch_composite_job.py</files>
  <read_first>
    - analytics-service/tests/test_stitch_composite_job.py — the `_FakeSupabase` harness, the deribit member fixture pattern, and Plan 93-03's degrade tests (the fixtures to extend).
    - The 92-02 Layer-3 precedent in the same file (`test_blowup_member_persists_finite_series_and_plausible_contribution`) — the "mock the FETCH layer, run the REAL math" pattern this task follows.
  </read_first>
  <behavior>
    - Test 1 (Option A happy path, RED before Task 1): a 2-member composite (seq 1 deribit fixture, seq 2 bybit member with monkeypatched FETCH primitives — `fetch_account_equity_and_upnl_usd` → (10000.0, False, 0.0, False), `fetch_all_trades` → a small deterministic realized fixture, `fetch_funding_bybit` → a small funding fixture, `fetch_ccxt_transfers` → [], `_resolve_ccxt_flow_price_index` → {} — while `ccxt_rows_to_dated_flows` and `combine_realized_and_funding` run REAL) → job completes; `degraded_members` ABSENT from persisted data_quality_flags (empty → popped); per_key seq 2 has `n_days > 0`; persisted csv_daily_returns include rows inside member 2's window.
    - Test 2 (byte-consistency, research A3): the seq-2 rows persisted through the stitch equal, at rtol 1e-12, a reference series computed in-test by calling `combine_realized_and_funding` directly on the SAME fixture inputs then clipping with `clip_to_window` — the stitch path introduces zero divergence from the derive semantics.
    - Test 3 (structural failure → degrade): monkeypatch `ccxt_rows_to_dated_flows` (or the combine) to raise `NavReconstructionError` for the bybit member → composite completes `complete_with_warnings`; `degraded_members == [{"seq": 2, "venue": "bybit", "reason": "reconstruction_failed"}]`; stitched series = deribit-only.
    - Test 4 (transient passthrough): monkeypatch a fetch primitive to raise `ccxt.RateLimitExceeded` for the bybit member → DispatchResult transient (whole-job retry), `_stamp_429` called, NO degrade entry persisted.
    - Test 5 (guard-flag union): seed the bybit fixture so `combine_realized_and_funding` fires a NAV_TWR_GUARD_KEYS flag (e.g. a flow-dominated day) → the flag appears in merged_flags and status is `complete_with_warnings` (the existing member-meta union loop, no new wiring).
  </behavior>
  <action>
    Write the five tests using the existing offline harness (`_FakeSupabase`; no live DB, no live creds, no network). Monkeypatch ONLY the I/O fetch primitives at their job_worker import sites; the valuation/combine/terminus math runs REAL (92-02 Layer-3 pattern). Keep fixtures small (≤ ~10 days) and deterministic; reuse Plan 93-03's member fixtures where possible.

    Blast-radius gate (SC-4): after all five pass, run the full parity set and the full stitch file — all pre-existing tests unmodified.

    NON-BLOCKING live corroboration (document in SUMMARY, do NOT block): the real-key Railway ccxt canary (Bybit/OKX/Binance read-only creds, `railway run` per the STATE [86-04] Zavara pattern) is the user's attestation gate, mirroring SC-3 piece 3. Offline fixtures + this suite close the requirement; never claim live-attested.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -q && .venv/bin/python -m pytest tests/test_composite_headline_parity.py tests/test_golden_parity.py tests/test_metrics_parity.py tests/test_broker_dailies.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -q` exits 0 with the 5 new tests passing; Test 1's RED proof (pre-Task-1 code degrades the member → per_key n_days == 0) documented in SUMMARY.
    - Byte-consistency pin asserts rtol 1e-12 equality against the in-test reference series (Test 2) — a forked/divergent orchestration goes RED.
    - Full parity set + test_broker_dailies.py exit 0 (single-key derive semantics unmoved).
    - Full analytics suite green under the pinned venv: `cd analytics-service && .venv/bin/python -m pytest -q` exits 0 (baseline ~3584 pass / 92 skip + this phase's additions).
  </acceptance_criteria>
  <done>Option A proven offline for the shared ccxt branch (all three venues via the one generic path); degrade fallback + transient taxonomy pinned; SC-4 byte-identity evidenced; live canary documented as the user's non-blocking gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| exchange → worker | ccxt member crawls use the EXISTING authenticated adapters + `_allocator_key_preflight` (worker-only decrypt, owner re-read from api_keys — T-86-09 untouched; secrets never read from the job payload). |
| worker → DB | Same persist surfaces as before (csv_daily_returns, strategy_analytics jsonb) — no new columns. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-04-01 | Information Disclosure | degrade reason / error messages | mitigate | reason stays the fixed literal "reconstruction_failed"; transient/permanent messages reuse the existing scrub_freeform_string templates; no USD/NAV in any flag (93-03 Test 4 discipline carried forward). |
| T-93-04-02 | Tampering (data integrity) | ccxt series divergence from derive semantics | mitigate | Byte-consistency pin (rtol 1e-12) against a direct-primitive reference series; math primitives reused verbatim, never re-implemented (the exact no-invented-data threat that motivated the Phase-86 fence). |
| T-93-04-03 | Denial of Service | member crawl retried forever on structural errors | mitigate | Structural (NavReconstructionError family) → member degrade, job completes; transient (429/geo) → typed transient retry with _stamp_429 circuit-breaker — the T-74-02 retried-forever class cannot occur. |
| T-93-04-04 | Elevation of Privilege | member key decryption | accept | No change: decrypt remains sole-sited in _allocator_key_preflight (grep-verifiable, Phase-86 pin). |
| T-93-04-SC | Tampering | package installs | accept | No packages installed this plan. |

ASVS L1 rationale: reuses existing authenticated adapters and existing persist surfaces; no new input boundary. Blocking severity threshold (high): none identified.
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest -q` exits 0 under the pinned 3.12 venv (never the local 3.14 — pandas SIGSEGV).
- `cd analytics-service && .venv/bin/python -m mypy services/job_worker.py` exits 0.
- `git status --porcelain supabase/migrations/` empty; `git diff` clean inside `run_derive_broker_dailies_job`.
- SUMMARY documents: (a) the non-blocking Railway ccxt live canary as the user's attestation gate; (b) the derive/stitch orchestration consolidation as a deferred follow-up.
</verification>

<success_criteria>
- "Only Deribit composites work" removed as a limitation: ccxt members reconstruct through the one shared generic branch (all three venues), offline-proven byte-consistent with derive semantics.
- Structural failure degrades via 93-03's visible channel; transients stay transient.
- Parity set + full suite green; no migration; derive path untouched.
</success_criteria>

<output>
Create `.planning/phases/93-composite-data-path-correctness/93-04-SUMMARY.md` when done.
</output>
