---
phase: 97-composite-ci-schema-debt
plan: 03
type: execute
wave: 2
depends_on: [97-01]
files_modified:
  - analytics-service/tests/test_compute_jobs_fencing.py
  - TODOS.md
autonomous: true
requirements: [CI-02]

must_haves:
  truths:
    - "CI-02.1 is closed by a RE-JUSTIFIED deferral: the 3 skipped tests' skip reasons and the TODOS.md L165 tracker state the httpx.ReadTimeout root cause, why CI-01's foreign-row isolation does NOT cover it, and the sanctioned post-ship re-enable path"
    - "The 3 tests remain collected-and-skipped (never deleted, never silently re-enabled) and the fencing file's offline subset stays green"
    - "No new flake source is introduced onto the milestone→main ship PR's python check"
  artifacts:
    - path: "analytics-service/tests/test_compute_jobs_fencing.py"
      provides: "updated skip reasons on the 3 late-mark tests (dated 2026-07 re-justification)"
      contains: "ReadTimeout"
    - path: "TODOS.md"
      provides: "re-justified deferral entry in the PR #149 / L165 tracker block"
      contains: "re-justified"
  key_links:
    - from: "TODOS.md L165 tracker block"
      to: "the 3 @pytest.mark.skip reasons in test_compute_jobs_fencing.py"
      via: "both cite the same root cause + the same post-ship re-enable route (guard-wrapped RPCs)"
      pattern: "_rpc_retry_timeout"
---

<objective>
CI-02.1 (NON-BLOCKING): resolve the 3 deferred flaky live-DB fence tests — by RE-JUSTIFYING the deferral with the ReadTimeout evidence, per locked decision 3's default.

⚠️ NUMBER RESOLUTION (locked, from research): "#165" is **TODOS.md line 165** (the "PR #149 (audit-2026-05-07 P97) — flaky live-DB fence tests, deferred 2026-05-13" block), NOT GitHub issue #165 (a merged match-router PR — unrelated). Do not touch GitHub issues.

DECISION RATIONALE (locked decision 3 applied — do NOT re-litigate, do NOT re-enable):
- The 3 tests (`test_late_mark_done_with_stale_token_raises_serialization_failure` ~L908, `test_late_mark_failed_with_stale_token_raises_serialization_failure` ~L1008, `test_late_mark_done_after_w2_completed_raises_serialization_failure` ~L1429) skip for `httpx.ReadTimeout` at ~120s under live-DB suite load — a contention/latency root cause, DIFFERENT from the foreign-row isolation CI-01 fixed. CI-01's scoping does NOT make them safe.
- The locked decision permits re-enabling behind the existing `_rpc_retry_timeout` guard only "IF it demonstrably stabilizes them". There is NO offline venue to demonstrate that: the live fence tests execute only in CI against the shared test project, so the first demonstration would be the milestone→main SHIP PR itself. The decision's own default — "re-justify if re-enabling risks re-introducing flake before ship" — therefore governs. Re-justify.
- Either outcome satisfies CI-02.1 (roadmap success criterion 2: "re-enabled ... or their continued deferral is explicitly re-justified").
- The fence contract stays independently guarded regardless: mocked equivalents (`_is_serialization_failure` classifier, `LATE_MARK_IGNORED` contract, `dispatch_tick` token threading) pass, and the migration-117 self-verify DO block pins it server-side on every prod + test-DB apply.

Purpose: close the milestone's last open CI-02 sub-item without gambling the ship PR's `python` check on an undemonstrable stabilization.

Output: updated skip reasons + a dated re-justification in the TODOS.md tracker, with the concrete post-ship re-enable recipe recorded.

Wave note: depends on 97-01 solely because both plans edit `test_compute_jobs_fencing.py` (file-ownership rule) — and 97-01's scoping of the 6 `_claim_one` sites INSIDE these skipped tests is a stated precondition of the future re-enable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/97-composite-ci-schema-debt/97-RESEARCH.md
@.planning/phases/97-composite-ci-schema-debt/97-VALIDATION.md
@.planning/phases/97-composite-ci-schema-debt/97-01-SUMMARY.md

Source: analytics-service/tests/test_compute_jobs_fencing.py (post-97-01 state), TODOS.md:165-186
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the 3 skip reasons with the 2026-07 re-justification + post-ship re-enable recipe</name>
  <files>analytics-service/tests/test_compute_jobs_fencing.py</files>
  <action>
Rewrite the `reason=` text of the three `@pytest.mark.skip` decorators (anchors pre-97-01: 908, 1008, 1429 — re-locate by test name after 97-01's edits). The first test carries the full text; the other two keep their existing "same flaky timeout pattern as test_late_mark_done_with_stale_token — see that test's skip reason + TODOS.md" cross-reference style (update only if the referenced text's meaning shifts).

The full reason must state, in order:
1. Root cause: `httpx.ReadTimeout` at ~120s under live-DB suite load (contention/latency on the shared test project, python job concurrent with e2e) — UNCHANGED since the 2026-05-13 investigation.
2. Re-justified 2026-07 (Phase 97 / CI-02.1): the Phase-97 per-run-`job_id` claim scoping fixed a DIFFERENT failure mode (foreign-row isolation under xdist parallelism) and does NOT address this timeout; re-enabling before the v1.9.1 ship was declined because stabilization is only demonstrable on live CI — the ship PR itself would have been the canary.
3. Contract independently pinned: mocked equivalents (`_is_serialization_failure`, `LATE_MARK_IGNORED`, `dispatch_tick` token threading) + the migration-117 self-verify DO block.
4. Sanctioned re-enable recipe (post-ship): remove the skip and wrap the contention-prone RPC calls (`reset_stalled_compute_jobs`, the late `mark_compute_job_done`/`mark_compute_job_failed`) in the existing `_rpc_retry_timeout` — its semantics already fit: genuine timeout → `pytest.skip` (a BaseException, so the enclosing `except Exception` cannot swallow it), while the asserted `serialization_failure` re-raises immediately and still reaches the assertion. Then canary on a non-ship PR's python check.

Do NOT remove the decorators, do NOT delete the tests, do NOT change any test body (97-01 already scoped their `_claim_one` calls).
  </action>
  <verify>
    <automated>cd analytics-service && test "$(.venv/bin/python -m pytest tests/test_compute_jobs_fencing.py --collect-only -q -k "late_mark" 2>/dev/null | grep -c "late_mark")" -ge 3 && grep -c "Re-justified 2026-07" tests/test_compute_jobs_fencing.py | awk '{exit ($1>=1)?0:1}' && .venv/bin/python -m pytest tests/test_compute_jobs_fencing.py -q</automated>
  </verify>
  <done>All 3 tests still collected (skipped, not deleted); the headline skip reason carries the 4-part re-justification; the fencing file's offline subset is green.</done>
</task>

<task type="auto">
  <name>Task 2: Re-justify the deferral in the TODOS.md L165 tracker block</name>
  <files>TODOS.md</files>
  <action>
In the "PR #149 (audit-2026-05-07 P97) — flaky live-DB fence tests, deferred 2026-05-13" block (starts at TODOS.md:165), append a dated subsection (do not rewrite the 2026-05-13 investigation history — it is the evidence base):

"**Re-justified 2026-07-12 (Phase 97 / v1.9.1 CI-02.1)**" stating: (a) the Phase-97 per-run-`job_id` scoping (CI-01, absorbing PR #610) fixed foreign-row isolation under xdist — a different root cause; the ReadTimeout contention flake is unaddressed by it; (b) re-enable-behind-`_rpc_retry_timeout` was evaluated and declined pre-ship because the only stabilization venue is live CI (the ship PR would be the canary); (c) the fence contract remains pinned by the mocked equivalents + migration-117 self-verify DO block + the 9 other live fence tests; (d) the concrete re-enable recipe (guard-wrap `reset_stalled_compute_jobs` + the late mark calls in `_rpc_retry_timeout`, canary on a non-ship PR) — matching the skip-reason text from Task 1 verbatim in substance; (e) note the 3 tests' claim sites are already own-job_id scoped (97-01), so isolation is NOT a blocker for the future re-enable.

Use Edit (surgical insertion into the existing block) — never Write over TODOS.md. This satisfies roadmap success criterion 2 via the "explicitly re-justified" arm.
  </action>
  <verify>
    <automated>grep -q "Re-justified 2026-07-12" TODOS.md && grep -q "_rpc_retry_timeout" TODOS.md && sed -n '160,220p' TODOS.md | grep -q "deferred 2026-05-13"</automated>
  </verify>
  <done>TODOS.md tracker block carries the dated re-justification with the 5 elements; the original 2026-05-13 investigation history is intact above it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| skip-reason text → future maintainers | the deferral record is the only thing preventing a naive re-enable that flakes the ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-07 | Repudiation | deferral without evidence (roadmap criterion 2 unsatisfied) | mitigate | dated re-justification in BOTH the skip reasons and TODOS.md, citing the ReadTimeout evidence + why CI-01 doesn't cover it |
| T-97-08 | Tampering | tests silently deleted instead of skipped | mitigate | Task 1 verify asserts all 3 still collected |
</threat_model>

<verification>
- `pytest --collect-only -k "late_mark"` still lists all 3 tests; file's offline subset green under the pinned interpreter.
- "Re-justified 2026-07-12" present in both the test file and TODOS.md, mutually consistent.
- Nothing in this plan can redden the ship PR's `python` check (doc/reason changes + a tracker edit only).
</verification>

<success_criteria>
- CI-02.1 closed via the re-justify arm of roadmap success criterion 2, with the evidence trail a future re-enable needs (root cause, why CI-01 doesn't apply, the guard recipe, the canary protocol).
- Zero behavioral test changes; zero new flake surface before ship.
</success_criteria>

<output>
Create `.planning/phases/97-composite-ci-schema-debt/97-03-SUMMARY.md` when done (Write ONLY this new file — never Write over existing .planning docs; Edit them if needed).
</output>
