---
phase: 97-composite-ci-schema-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - analytics-service/tests/test_compute_jobs_fencing.py
  - analytics-service/tests/conftest.py
  - analytics-service/pytest.ini
  - analytics-service/requirements-dev.txt
  - analytics-service/Makefile
  - .github/workflows/ci.yml
autonomous: true
requirements: [CI-01]

must_haves:
  truths:
    - "Every live fence test asserts against the job it inserted (own job_id), never against res.data[0] of the global claim queue — a foreign pending compute_jobs row can no longer break any fence assertion"
    - "The offline decoy-foreign-row regression FAILS without the scoping fix and PASSES with it (repro-gate — provable without the live DB)"
    - "The analytics-service python suite invocation is `pytest -n auto --dist loadgroup --cov=... --cov-fail-under=80`, byte-identical between ci.yml and the Makefile, with the coverage floor untouched"
    - "Every DB-touching test module is pinned to xdist_group('shared_test_db') via the content-based conftest hook (both PostgREST and psycopg sentinel idioms)"
  artifacts:
    - path: "analytics-service/tests/test_compute_jobs_fencing.py"
      provides: "own-job_id-scoped _claim_one + scoped batch assertions + decoy regressions"
      contains: "want_job_id"
    - path: "analytics-service/tests/conftest.py"
      provides: "content-based xdist_group pinning for shared-test-DB modules"
      contains: "pytest_collection_modifyitems"
    - path: "analytics-service/pytest.ini"
      provides: "xdist_group marker registration"
      contains: "xdist_group"
    - path: "analytics-service/requirements-dev.txt"
      provides: "pytest-xdist dependency"
      contains: "pytest-xdist"
    - path: ".github/workflows/ci.yml"
      provides: "parallelized python job"
      contains: "-n auto --dist loadgroup"
    - path: "analytics-service/Makefile"
      provides: "byte-identical local test target"
      contains: "-n auto --dist loadgroup"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "analytics-service/Makefile"
      via: "byte-identical pytest flag string (repo convention, #610 preserved it)"
      pattern: "-n auto --dist loadgroup --cov=services --cov=routers --cov=main_worker --cov-report=term-missing --cov-fail-under=80"
    - from: "analytics-service/tests/conftest.py"
      to: "analytics-service/tests/test_compute_jobs_fencing.py"
      via: "sentinel content-scan (SUPABASE_TEST_URL at line 552 / _need_supabase at 556) groups the fence file onto the shared_test_db worker"
      pattern: "SUPABASE_TEST_URL"
    - from: "_claim_one(want_job_id=...)"
      to: "all 12 _claim_one call sites + 9 batch-claim assertion sites"
      via: "own-row filter threaded through every live claim/assert"
      pattern: "want_job_id=job_id"
---

<objective>
CI-01: make `test_compute_jobs_fencing.py` parallelism-safe via per-run-`job_id` claim scoping, prove the isolation OFFLINE with a decoy-foreign-row regression, and re-apply PR #610's pytest-xdist parallelization as FRESH commits on the milestone branch — unblocking the milestone→main ship PR's `python` check.

⚠️ LOUD FLAG — #610 IS A REFERENCE, NOT A MERGE TARGET (locked decision 2, do NOT re-litigate):
- PR #610 (branch `ci/pytest-xdist-parallel`) already parallelizes and ships the `xdist_group("shared_test_db")` serial lane, but is HELD because the lane serializes WITHOUT ISOLATING — the live fence tests do a GLOBAL claim (`_claim_one` → `res.data[0]`) and assert `== own job_id`, breaking on foreign rows from interleaved grouped DB tests.
- Re-apply #610's changes as fresh edits on THIS branch (`gsd/v1.9.1-composite-onboarding-hardening`). Do NOT cherry-pick, do NOT check out or touch `ci/pytest-xdist-parallel`, do NOT land #610 to main, NO git branch operations of any kind. #610 gets closed as absorbed at SHIP time (not by this plan).
- Do NOT bump VERSION / package.json / CHANGELOG in this plan — the milestone branch bumps once at ship (/ship handles it; #610's CHANGELOG text gets folded into the ship changelog then).

Purpose: the milestone→main ship PR must go green on the `python` check. The fix is root-cause (removes the global-queue assumption), robust to both within-run interleave AND the concurrently-running e2e job on the same shared test project, and preserves #610's full parallelization at zero CI cost.

Output: scoped fence tests + offline decoy regression (RED→GREEN) + the #610 parallelization re-applied.

NON-BLOCKING caveat (research assumption A1): the live fence tests only execute against the shared test Supabase project (`E2E_TEST_DB_CONFIGURED=true` in CI; locally they `pytest.skip`). The offline decoy regression is the local repro-gate; the full live confirmation is the ship PR's `python` check. If that live run still reddens, fallback is a dedicated `fence_serial` xdist_group — record it as a follow-up, do not improvise it here.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/97-composite-ci-schema-debt/97-RESEARCH.md
@.planning/phases/97-composite-ci-schema-debt/97-VALIDATION.md

Source (read in tasks; anchors cited in interfaces):
@analytics-service/tests/test_compute_jobs_fencing.py
@analytics-service/tests/conftest.py
</context>

<interfaces>
<!-- Extracted from the codebase + `gh pr diff 610` this session. Use directly — no re-exploration needed. -->

**Current unscoped helper** (`test_compute_jobs_fencing.py:691-699`):

```python
def _claim_one(admin, worker_id: str) -> dict[str, Any] | None:
    """Call claim_compute_jobs_with_priority and return the first row, or
    None if nothing was claimed."""
    res = admin.rpc("claim_compute_jobs_with_priority", {
        "p_batch_size": 50,
        "p_worker_id": worker_id,
        "p_unified_backbone_active": False,
    }).execute()
    return res.data[0] if res.data else None   # ← the defect: assumes data[0] is OUR job
```

**Target scoped shape** (from 97-RESEARCH.md "Code Examples", verified against the RPC signature):

```python
def _claim_one(admin, worker_id: str, *, want_job_id: str | None = None) -> dict[str, Any] | None:
    res = admin.rpc("claim_compute_jobs_with_priority", {
        "p_batch_size": 50,
        "p_worker_id": worker_id,
        "p_unified_backbone_active": False,
    }).execute()
    rows = res.data or []
    if want_job_id is not None:
        return next((r for r in rows if r["id"] == want_job_id), None)
    return rows[0] if rows else None
```

**`_claim_one` call sites** (12, current line anchors): 713, 749, 790, 941, 955, 1024, 1034, 1082, 1382, 1459, 1473, 2555. Sites 941/955, 1024/1034, 1459/1473 are inside the 3 `@pytest.mark.skip` tests — scope them anyway (CI-02.1 may re-enable them later; plan 97-03 touches only their skip reasons).

**Batch-claim assertion sites to audit for global-queue assumptions** (allocator dedupe/backoff tests via `_claim_with_priority` at 1616 / `_claim_legacy` at 1635): claims consumed at lines 1706, 1756, 1868, 1948, 2037, 2151, 2268, 2394, 2481. Known-bad example at 1706-1713: `assert len(claimed) == 1` — a foreign pending row claimed into the same batch breaks the count. These tests DO purge their own partition (`_purge_allocator_jobs`, 1567) but the batch can still pick up FOREIGN rows from other modules/jobs.

**Already own-row-scoped (evidence — do NOT change):** `test_concurrent_claim_disjoint_under_skip_locked` (1157-1215) filters both workers' results through `ours = set(job_ids)` (1189-1207) with an explicit comment about unrelated pending rows. Use it as the in-file model for scoping the batch sites.

**#610's exact changes to re-apply as fresh edits** (from `gh pr diff 610`; functional files only — skip its VERSION/package.json/CHANGELOG hunks):

1. `.github/workflows/ci.yml` (current line 984): replace
   `- run: pytest --cov=services --cov=routers --cov=main_worker --cov-report=term-missing --cov-fail-under=80`
   with
   `- run: pytest -n auto --dist loadgroup --cov=services --cov=routers --cov=main_worker --cov-report=term-missing --cov-fail-under=80`
   plus #610's 7-line explanatory comment above it (xdist_group pinning, cross-run `concurrency: shared-test-db`, xdist-aware pytest-cov, "Keep byte-identical with Makefile `test:`"). The `env:` block below stays untouched.

2. `analytics-service/Makefile` `test:` target (line ~72-73): same flag insertion, byte-identical.

3. `analytics-service/pytest.ini`: append
   ```
   markers =
       xdist_group: pin tests sharing a group name to the same xdist worker (serializes shared-test-DB tests under `-n auto --dist loadgroup`)
   ```

4. `analytics-service/requirements-dev.txt` (after `pytest-mock`, before `cryptography`): #610's 4-line comment + `pytest-xdist` (unpinned; needs >=2.5 for `--dist loadgroup`). Package legitimacy: approved in 97-RESEARCH.md audit table — first-party pytest-dev plugin, ~10 yrs, tens of M downloads/mo `[CITED: pypi.org/project/pytest-xdist]`. No human gate needed.

5. `analytics-service/tests/conftest.py`: add `import functools` at top, then #610's block after the existing imports — the `_DB_MODULE_SENTINELS` tuple (`"SUPABASE_TEST_URL"`, `"SUPABASE_TEST_SERVICE_KEY"`, `"_need_supabase"`, `"TEST_SUPABASE_DB_URL"`, `"HAS_LIVE_DB"`), the `@functools.lru_cache` `_is_shared_db_module(path)` content-scan, and `pytest_collection_modifyitems` adding `pytest.mark.xdist_group("shared_test_db")` to matching modules — including #610's full doc comment (deliberately NOT the bare `SUPABASE_URL` sentinel; grouping those ~10 offline files would defeat the parallelism).

**Pinned interpreter:** `cd analytics-service && .venv/bin/python -m pytest ...` — never the shared system python.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decoy-foreign-row regressions (Wave-0 blocker) — write RED first</name>
  <files>analytics-service/tests/test_compute_jobs_fencing.py</files>
  <behavior>
    - OFFLINE (the repro-gate — no DB, no skip): a stub admin object whose `.rpc(...).execute()` returns `data=[{foreign row}, {own row}]` (foreign FIRST). `_claim_one(stub, "decoy-offline", want_job_id=own_id)` returns the OWN row; `_claim_one(stub, "decoy-offline")` (unscoped legacy arm) returns the FOREIGN row — pinning exactly why the scoping is load-bearing. When `res.data` contains only foreign rows, the scoped call returns None (not a foreign row). This test MUST fail against the current helper (no `want_job_id` kwarg → TypeError) and pass after Task 2.
    - LIVE (supplementary, `_need_supabase`-gated like its neighbors): seed the own pending job on the test's `strategy_id` fixture, THEN insert a decoy pending `compute_jobs` row on a second throwaway strategy (reuse `_make_strategy` at 1141 + `_insert_pending_sync_trades` at 1128 — a distinct dedupe partition, so both rows are claimable in one batch). Call the scoped `_claim_one(admin, "decoy-live", want_job_id=own_job_id)` → returns the OWN job with a non-NULL claim_token. `finally`: delete BOTH jobs + the throwaway strategy (decoy included — the claim RPC will have stamped it `processing`/running as a side effect; research flags this as self-healing via watchdog, but clean up anyway).
  </behavior>
  <action>
Add both tests near `test_claim_stamps_claim_token` (~702) with a comment block naming this the Phase-97 CI-01 decoy-foreign-row regression (cite: xdist_group serializes but does NOT isolate; foreign rows from interleaved grouped DB tests broke `data[0]`).

The offline stub needs no supabase import: a tiny local class with `rpc(name, params)` returning an object whose `execute()` returns a `SimpleNamespace(data=[...])`. Do NOT decorate it with `_need_supabase` — it must run everywhere (it is the only signal that works without a live CI run).

RED gate (run BEFORE Task 2, record output in SUMMARY): the offline decoy test must FAIL against the current unscoped `_claim_one`. Commit message per house TDD style: `test(97-01): add failing decoy-foreign-row fence regression`.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_compute_jobs_fencing.py -k "decoy" -q; test $? -ne 0</automated>
  </verify>
  <done>Both decoy tests exist; the offline one FAILS (RED) against the unscoped helper with the failure output captured for the SUMMARY; the live one is `_need_supabase`-gated and skips locally.</done>
</task>

<task type="auto">
  <name>Task 2: Per-run-job_id claim scoping across ALL live claim/assert sites (turns Task 1 GREEN)</name>
  <files>analytics-service/tests/test_compute_jobs_fencing.py</files>
  <action>
Implement the scoped `_claim_one` exactly as in the interfaces block (keyword-only `want_job_id`, filter `r["id"] == want_job_id`, fall back to legacy `rows[0]` when None — the offline decoy pins both arms).

Thread `want_job_id=job_id` through all 12 call sites (anchors in interfaces: 713, 749, 790, 941, 955, 1024, 1034, 1082, 1382, 1459, 1473, 2555). Site-specific notes:
- 1082 (`test_mark_done_without_token_raises_strict`) currently discards the result; capture it and assert `claimed is not None and claimed["id"] == job_id` — the test's later `status == "running"` assertion silently depends on the own job having been claimed.
- 790 (`test_reclaim_invalidates_claim_token`) asserts only `is not None`; with scoping that now correctly means "OUR job was claimed" — keep the existing assertion text.
- The 6 sites inside the 3 skipped tests get scoped identically (they must be correct if CI-02.1 ever re-enables them). Do NOT touch the `@pytest.mark.skip` decorators or reasons — that is plan 97-03's file surface.

Then audit the 9 batch-claim consumption sites (1706, 1756, 1868, 1948, 2037, 2151, 2268, 2394, 2481) for global-queue assumptions, per research Open Question 1. For each: filter the returned list to the test's own seeded rows BEFORE any count/содержимое assertion — model on `test_concurrent_claim_disjoint_under_skip_locked`'s `ours = set(job_ids)` scoping (1189-1207). Example: 1707's `assert len(claimed) == 1` becomes an assertion that exactly the seeded `job_id` is present among `[r for r in claimed if r["id"] in ours]` (or `r["allocator_id"] == allocator_id` where the seeded-id set is the natural key). PRESERVE each assertion's intent — dedupe tests assert "≤1 of MY partition claimed", never weaken to "own row merely present" where the test pins dedupe-collapse semantics. SECURITY PIN (threat T-97-03): the `serialization_failure`/40001 fence assertions themselves must remain byte-equivalent — scope WHICH row is claimed, never WHETHER the fence raises.

Do not modify `_claim_with_priority`/`_claim_legacy` return shapes (tests need full lists); scoping happens at the assertion sites.

GREEN gate: Task 1's offline decoy passes; the whole file's offline subset passes (live tests skip locally without SUPABASE_TEST_URL).
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_compute_jobs_fencing.py -q && ! grep -n "return res.data\[0\] if res.data else None" tests/test_compute_jobs_fencing.py</automated>
  </verify>
  <done>All 12 `_claim_one` sites scoped; all 9 batch sites audited (each disposition — scoped or already-safe — listed with line anchors in the SUMMARY); offline decoy GREEN; unscoped `data[0]` helper shape gone; fence-raise assertions untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Re-apply #610's parallelization as fresh edits (ci.yml, Makefile, pytest.ini, requirements-dev, conftest)</name>
  <files>.github/workflows/ci.yml, analytics-service/Makefile, analytics-service/pytest.ini, analytics-service/requirements-dev.txt, analytics-service/tests/conftest.py</files>
  <action>
Apply the 5 changes exactly as itemized in the interfaces block ("#610's exact changes to re-apply"). These are Edit operations on the current files — NO git branch ops, NO checkout of `ci/pytest-xdist-parallel`, NO VERSION/package.json/CHANGELOG edits (ship-time concern; flagged in objective).

Keep the ci.yml and Makefile pytest invocations byte-identical (repo convention #610 preserved; the verify below gates it). Keep `--cov-fail-under=80` untouched (threat T-97-02).

Local sanity: attempt `cd analytics-service && .venv/bin/pip install pytest-xdist` — if it installs, run the full offline suite once under `-n auto --dist loadgroup` with the pinned interpreter and record the wall-clock + pass count; if pip is offline in this environment, run the suite serially instead and state in the SUMMARY that the parallel invocation is verified by CI only (NON-BLOCKING, matches research's environment table). Either way the offline suite must be green before commit.

Sanity-check the conftest hook does not capture offline modules: `grep -rl "SUPABASE_TEST_URL\|SUPABASE_TEST_SERVICE_KEY\|_need_supabase\|TEST_SUPABASE_DB_URL\|HAS_LIVE_DB" analytics-service/tests/ | wc -l` — expect a small set (the DB-touching files, roughly 5-10), NOT the majority of the ~3.6k-test module list. Record the grouped-module list in the SUMMARY.
  </action>
  <verify>
    <automated>bash -c 'CI_LINE=$(grep -o "pytest -n auto --dist loadgroup[^\"]*--cov-fail-under=80" .github/workflows/ci.yml); MK_LINE=$(grep -o "pytest) -n auto --dist loadgroup.*--cov-fail-under=80" analytics-service/Makefile | sed "s/^pytest) /pytest /"); test -n "$CI_LINE" && grep -q -- "-n auto --dist loadgroup" analytics-service/Makefile && grep -q "xdist_group" analytics-service/pytest.ini && grep -q "pytest-xdist" analytics-service/requirements-dev.txt && grep -q "pytest_collection_modifyitems" analytics-service/tests/conftest.py'</automated>
  </verify>
  <done>All 5 files carry #610's changes; flags byte-identical CI↔Makefile; coverage floor unchanged; offline suite green under the pinned interpreter (parallel if xdist installable locally, serial otherwise with the CI-only caveat recorded); grouped-module inventory in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → shared test Supabase project | live fence tests mutate the shared `compute_jobs` table also touched by the concurrent e2e job |
| requirements-dev.txt → PyPI | one new dev dependency enters the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | fence tests claiming/stamping FOREIGN rows as a side effect (batch_size=50) | accept | self-healing via watchdog reset (research); own cleanup stays scoped to own job_id; decoy test cleans its decoy row |
| T-97-02 | Tampering | CI edit silently lowering the coverage gate | mitigate | `--cov-fail-under=80` byte-identical CI↔Makefile; Task 3 verify greps the full flag string |
| T-97-03 | Tampering | scoping-to-own-rows accidentally weakening the serialization_failure fence assertion | mitigate | Task 2 explicitly pins: scope WHICH row, never WHETHER the fence raises; 40001 assertions byte-equivalent |
| T-97-SC | Tampering | pytest-xdist supply chain | mitigate | approved in 97-RESEARCH.md Package Legitimacy Audit — first-party pytest-dev plugin `[CITED: pypi.org/project/pytest-xdist]`; not [ASSUMED]/[SUS], no blocking gate required |
</threat_model>

<verification>
- Repro-gate: Task 1's offline decoy test RED before Task 2, GREEN after (both outputs in SUMMARY).
- `cd analytics-service && .venv/bin/python -m pytest tests/test_compute_jobs_fencing.py -q` green (live tests skip locally — expected).
- Full offline suite green under the pinned interpreter.
- ci.yml + Makefile flag strings byte-identical; `--cov-fail-under=80` present in both.
- LIVE gate (NON-BLOCKING here, ship-blocking overall): the milestone→main PR's `python` check — the only place the shared-test-DB contention actually reproduces. If red there, fall back to a dedicated `fence_serial` xdist_group (recorded, not improvised).
</verification>

<success_criteria>
- No `res.data[0]` global-queue assumption anywhere in the live fence/claim assertions; all 12 + 9 sites dispositioned with anchors in the SUMMARY.
- Offline decoy regression proves the isolation without a live DB and fails when the scoping is neutered.
- #610's parallelization re-applied as fresh edits — no branch ops, no cherry-pick, no VERSION/CHANGELOG churn; #610 itself untouched (closed as absorbed at ship time).
</success_criteria>

<output>
Create `.planning/phases/97-composite-ci-schema-debt/97-01-SUMMARY.md` when done (Write ONLY this new file — never Write over existing .planning docs; Edit them if needed).
</output>
