---
phase: 97-composite-ci-schema-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema/functions/set_compute_job_progress.sql
  - supabase/schema/functions/cleanup_abandoned_wizard_drafts.sql
  - supabase/schema/functions/set_wizard_composite_members.sql
autonomous: true
requirements: [CI-02]

must_haves:
  truths:
    - "`npm run schema:functions:check` exits 0 — the hermetic sql-function-snapshot gate will pass on the milestone→main ship PR"
    - "The regeneration diff is bounded to EXACTLY the 3 owed files (2 new + 1 modified) — any additional drift halts the plan instead of being committed"
    - "Every already-closed CI-02.2 sub-item is dropped with cited run/grep evidence in the phase record, not re-done (roadmap success criterion 4)"
  artifacts:
    - path: "supabase/schema/functions/set_compute_job_progress.sql"
      provides: "hermetic snapshot of the Phase-95 fenced progress RPC (currently missing)"
      contains: "set_compute_job_progress"
    - path: "supabase/schema/functions/cleanup_abandoned_wizard_drafts.sql"
      provides: "hermetic snapshot of the Phase-96 cleanup RPC (currently missing)"
      contains: "cleanup_abandoned_wizard_drafts"
    - path: "supabase/schema/functions/set_wizard_composite_members.sql"
      provides: "refreshed snapshot (currently stale vs migration 20260712120000)"
      contains: "set_wizard_composite_members"
  key_links:
    - from: "supabase/migrations/** (Phase 95/96 migrations, branch-only)"
      to: "supabase/schema/functions/*.sql"
      via: "scripts/dump-sql-functions.ts hermetic text-replay (npm run schema:functions)"
      pattern: "@generated"
---

<objective>
CI-02.2: regenerate the 3 owed SQL-function snapshots so the hermetic `sql-function-snapshot` CI gate goes green — and record the verify-and-drop evidence for every CI-02.2 sub-item that Phases 88–96 already closed.

⚠️ LOUD FLAG — THIS PLAN IS SHIP-BLOCKING (not optional cleanup): the Phase-95/96 migrations that introduce the drift exist ONLY on the milestone branch (`gsd/v1.9.1-composite-onboarding-hardening`, ahead 75). The `sql-function-snapshot` workflow is path-filtered on `supabase/migrations/**` + `supabase/schema/**` and last ran green on `origin/main` at #607 — it WILL fire and FAIL on the milestone→main ship PR unless these 3 snapshots land. There is no path to shipping v1.9.1 without this plan.

⚠️ VERIFY-AND-DROP (locked decision 5 — cite evidence, do NOT re-do): the roadmap named the WRONG snapshots. `enforce_strategy_keys_owner_coherence` and `sync_strategy_analytics_status` already have snapshots (landed with v1.9 #607, commit `044bee50`; re-confirmed present in `supabase/schema/functions/` this planning session). The audit instrumentation items are already green: `audit-coverage.test.ts` + `audit-fanout-integration.test.ts` → 28 passed, 1 skipped; the `stitch_composite` enqueue in `finalize-wizard/route.ts` already carries `logAuditEventAsUser`; the `strategy_keys` mock already exists at `audit-fanout-integration.test.ts:811`. The actually-owed set — confirmed by running `npm run schema:functions:check` this planning session — is exactly: `set_compute_job_progress` (missing), `cleanup_abandoned_wizard_drafts` (missing), `set_wizard_composite_members` (stale).

Purpose: the ship PR's `sql-function-snapshot` check goes green; the roadmap's "checked against main first, closed items dropped with a note" criterion is satisfied with evidence.

Output: exactly 3 regenerated `@generated` snapshot files + a cited verify-and-drop record in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/97-composite-ci-schema-debt/97-RESEARCH.md
@.planning/phases/97-composite-ci-schema-debt/97-VALIDATION.md

Tooling (do not modify): scripts/dump-sql-functions.ts, package.json:22-23 (`schema:functions` / `schema:functions:check`), .github/workflows/sql-function-snapshot.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify-and-drop evidence for the already-closed CI-02.2 sub-items</name>
  <files>(read-only — evidence recorded in the plan SUMMARY)</files>
  <action>
Re-run and record (fresh output, this execution — do not copy the research's numbers):

1. `ls supabase/schema/functions/enforce_strategy_keys_owner_coherence.sql supabase/schema/functions/sync_strategy_analytics_status.sql` — both exist (the roadmap-named "owed" snapshots landed with v1.9 #607). DROP with this note.
2. `npx vitest run src/__tests__/audit-coverage.test.ts src/__tests__/audit-fanout-integration.test.ts` — expect 2 files green (research measured 28 passed, 1 skipped; record the actual fresh count). DROP the "instrument for audit-coverage" + "green in CI" sub-items.
3. `grep -n "logAuditEventAsUser" src/app/api/strategies/finalize-wizard/route.ts` — the stitch_composite enqueue audit call exists (research located it adjacent to the `admin.rpc("enqueue_compute_job", …)` queue arm, with `kind:"stitch_composite"` metadata). DROP the instrumentation sub-item, citing file:line.
4. `grep -n "strategy_keys" src/__tests__/audit-fanout-integration.test.ts | head -5` — the table mock exists (research anchor: line 811). DROP, citing the fresh line number.

If ANY of these four checks does NOT reproduce (a later commit regressed it), STOP and report the discrepancy as a blocker instead of silently re-doing the work — the phase record must reflect reality, and a regression here is new information the orchestrator needs.

Record all four evidence blocks verbatim in the SUMMARY under a "Verified-and-dropped (roadmap criterion 4)" heading.
  </action>
  <verify>
    <automated>test -f supabase/schema/functions/enforce_strategy_keys_owner_coherence.sql && test -f supabase/schema/functions/sync_strategy_analytics_status.sql && npx vitest run src/__tests__/audit-coverage.test.ts src/__tests__/audit-fanout-integration.test.ts</automated>
  </verify>
  <done>All four dropped items carry fresh cited evidence in the SUMMARY; zero code changes made by this task; any non-reproducing check surfaced as a blocker.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate the 3 owed snapshots — bounded-diff gated</name>
  <files>supabase/schema/functions/set_compute_job_progress.sql, supabase/schema/functions/cleanup_abandoned_wizard_drafts.sql, supabase/schema/functions/set_wizard_composite_members.sql</files>
  <action>
Preflight: `git status --porcelain supabase/schema/functions/` must be empty (clean start — research left the tree clean after its dry run). If not empty, STOP and report.

Run `npm run schema:functions` (the ONLY sanctioned writer — these files are `@generated`; NEVER hand-edit them, per the research anti-patterns and Pitfall 2).

BOUNDED-DIFF GATE (hard STOP condition, locked decision 4): `git status --porcelain supabase/schema/functions/` must show EXACTLY these 3 entries and nothing else:
- ` M supabase/schema/functions/set_wizard_composite_members.sql`
- `?? supabase/schema/functions/cleanup_abandoned_wizard_drafts.sql`
- `?? supabase/schema/functions/set_compute_job_progress.sql`

If MORE files changed: do NOT commit anything — `git checkout -- supabase/schema/functions/ && git clean -f supabase/schema/functions/` to restore, then halt with a blocker naming the extra files. Extra drift means a snapshot has drifted vs the migration train in a way this phase did not budget for — a SEPARATE problem (and possibly a migration-drift-vs-prod finding, which is a different gate entirely; see research Pitfall 2). Do not "fix" it opportunistically.

Then `npm run schema:functions:check` — must exit 0 with no owed files. Skim the 3 generated bodies for sanity (each contains its own function name and `@generated`; the stale one's diff should reflect migration `20260712120000_wizard_composite_members_invalidate_analytics.sql`'s newer body) — but change NOTHING by hand.

Commit exactly the 3 files. NO VERSION/package.json/CHANGELOG bump (ship-time concern).
  </action>
  <verify>
    <automated>npm run schema:functions:check && test "$(git status --porcelain supabase/schema/functions/ | wc -l | tr -d ' ')" = "0"</automated>
  </verify>
  <done>`schema:functions:check` exits 0; exactly 3 snapshot files committed; tree clean; the ship PR's `sql-function-snapshot` gate can no longer fail on Phase-95/96 drift. Repro-gate satisfied by construction: the check FAILED before this task (research + planning session both reproduced the 3-file verdict) and passes after.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migrations → generated snapshots | committed migration text is replayed into committed snapshot files consumed by a CI gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-04 | Tampering | hand-edited `@generated` snapshot masking real drift | mitigate | only `npm run schema:functions` writes; bounded-diff gate halts on anything beyond the 3 owed files |
| T-97-05 | Repudiation | snapshot body ≠ prod-applied body slipping through | accept | out of scope by design — repo-vs-prod is `migration-drift-check.yml`'s job (research Pitfall 2); this gate only pins migrations→snapshot coherence |
| T-97-06 | Info Disclosure | snapshot gate acquiring a DB/secret dependency | mitigate | gate stays hermetic (pure text-replay); this plan touches only generated outputs, never the workflow or script |
</threat_model>

<verification>
- `npm run schema:functions:check` → exit 0 (was: 3 owed files — reproduced this planning session).
- `git log --stat -1` for the regen commit shows exactly the 3 snapshot paths.
- Verify-and-drop evidence (4 items) present in the SUMMARY with fresh command output.
</verification>

<success_criteria>
- The milestone→main ship PR's `sql-function-snapshot` check has nothing left to fail on: all Phase-95/96 function-body drift is snapshotted.
- Roadmap success criterion 4 satisfied: every already-closed CI-02.2 sub-item dropped with a cited note in the phase record.
- Zero hand edits to `@generated` files; zero scope beyond the 3 owed snapshots.
</success_criteria>

<output>
Create `.planning/phases/97-composite-ci-schema-debt/97-02-SUMMARY.md` when done (Write ONLY this new file — never Write over existing .planning docs; Edit them if needed).
</output>
