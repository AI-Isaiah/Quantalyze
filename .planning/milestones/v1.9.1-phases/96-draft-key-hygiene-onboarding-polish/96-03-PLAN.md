---
phase: 96-draft-key-hygiene-onboarding-polish
plan: 03
type: execute
wave: 3
depends_on: [96-02]
files_modified:
  - src/app/api/cron/cleanup-wizard-drafts/route.test.ts
  - src/app/api/cron/cleanup-wizard-drafts/route.ts
  - vercel.json
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "The cron route no longer does SELECT-then-DELETE or a per-key revoke loop — it makes exactly ONE rpc('cleanup_abandoned_wizard_drafts') call"
    - "Auth is unchanged: Bearer CRON_SECRET via safeCompare, GET (Vercel) + POST (manual incident response), 401 otherwise"
    - "RPC failure returns a generic 500 envelope (no SQL state / table / constraint leak); success returns the monitor-stable shape {deleted, orphaned_keys_revoked, key_sweep_errors: 0}"
    - "The cron schedule is daily so the 7d window is effective (weekly + 7d = up to 14d lifetime)"
  artifacts:
    - path: "src/app/api/cron/cleanup-wizard-drafts/route.ts"
      provides: "thin auth + single-RPC dispatch + response mapping"
      contains: "cleanup_abandoned_wizard_drafts"
    - path: "src/app/api/cron/cleanup-wizard-drafts/route.test.ts"
      provides: "rewritten route contract tests (auth, single-RPC, mapping, error redaction)"
      contains: "cleanup_abandoned_wizard_drafts"
    - path: "vercel.json"
      provides: "daily schedule for /api/cron/cleanup-wizard-drafts"
      contains: "0 2 * * *"
  key_links:
    - from: "src/app/api/cron/cleanup-wizard-drafts/route.ts"
      to: "public.cleanup_abandoned_wizard_drafts()"
      via: "admin.rpc()"
      pattern: "rpc\\(.cleanup_abandoned_wizard_drafts"
---

<objective>
Rewire the existing Vercel Cron route to the atomic RPC shipped in 96-02, replacing the racy SELECT-then-DELETE + per-key `delete_api_key_if_unreferenced` loop, and move the schedule to daily. Test-first: the route test is rewritten RED before the route changes.

Purpose: completes CLEAN-01/CLEAN-02 end-to-end — Vercel keeps scheduling + failure alerting; the DB owns atomicity.

Output: rewritten route + rewritten route.test.ts + vercel.json schedule change.

⚠️ Deviation flags to carry into code comments:
- 7d window (locked; VALIDATION decision 1) — the window now lives in SQL, NOT in the route (`ABANDON_DAYS` is deleted).
- Schedule weekly→daily (`0 2 * * 0` → `0 2 * * *`) is planner discretion, NOT a locked decision: with a 7d window a weekly cadence leaves drafts alive 7-14 days; daily keeps effective lifetime 7-8 days. Documented here + in the vercel.json-adjacent route comment; trivially reversible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-draft-key-hygiene-onboarding-polish/96-VALIDATION.md
@src/app/api/cron/cleanup-wizard-drafts/route.ts
@src/app/api/cron/cleanup-wizard-drafts/route.test.ts
</context>

<interfaces>
<!-- Current route contract (to preserve) and RPC contract (to consume). -->

Preserve from the current route (route.ts:37-46, 174-175): `export const dynamic = "force-dynamic"`, `safeCompare(auth, \`Bearer ${process.env.CRON_SECRET}\`)` with 401 on missing/mismatched secret, `export const GET = handle; export const POST = handle;`.

Delete from the current route: `ABANDON_DAYS`, the `.select()`→`.delete()` two-step (L55-108), the per-key `delete_api_key_if_unreferenced` loop + `sweepErrors` machinery (L110-171). The H-1251 "partial sweep = 500" concern is now structurally impossible: the RPC is one transaction — any failure fails the WHOLE call → plain 500.

RPC contract (from 96-02): `admin.rpc("cleanup_abandoned_wizard_drafts")` with NO arguments → supabase-js returns `data` as an array of one row: `[{ deleted_drafts: number, swept_keys: number }]` (RETURNS TABLE). Map: `deleted: row.deleted_drafts`, `orphaned_keys_revoked: row.swept_keys`, `key_sweep_errors: 0` (kept for monitor-shape continuity — monitors read `key_sweep_errors` uniformly across clean runs).

Test file (route.test.ts, 778 lines): keeps `vi.mock("server-only", …)` + the `vi.resetModules()`/`vi.doMock()` per-case pattern and the auth cases (1-2). The chain-recorder machinery (`recorders` for .from/.select/.eq/.lt/.in/.delete) becomes obsolete — replace with a simple `rpcMock` on the mocked `createAdminClient`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite route.test.ts to the single-RPC contract (RED against the current route)</name>
  <files>src/app/api/cron/cleanup-wizard-drafts/route.test.ts</files>
  <behavior>
    - Auth (kept from the existing file): no Authorization header → 401; wrong bearer → 401; missing CRON_SECRET env → 401; equal-length wrong secret exercises the timingSafeEqual path.
    - Happy path: valid bearer → `rpc` called EXACTLY ONCE with `"cleanup_abandoned_wizard_drafts"` and no args object (or an empty one); response 200 `{deleted: 3, orphaned_keys_revoked: 2, key_sweep_errors: 0}` when the mock resolves `data: [{deleted_drafts: 3, swept_keys: 2}]`.
    - Zero-work run: `data: [{deleted_drafts: 0, swept_keys: 0}]` → 200 `{deleted: 0, orphaned_keys_revoked: 0, key_sweep_errors: 0}` (uniform monitor shape).
    - RPC error: mock resolves `error: {message: "relation … constraint 23503 details"}` → 500 with a GENERIC body (assert the body does NOT contain the SQL detail string — least-disclosure, mirrors the old route's redaction posture) and console.error carries the detail.
    - Purity: the route must NOT touch `.from("strategies")` / `.from("api_keys")` / `delete_api_key_if_unreferenced` — assert the from/rpc mocks record zero such calls.
  </behavior>
  <action>
Rewrite `route.test.ts`: keep the header rationale comment style, the `vi.mock("server-only")` stub, and the auth section; replace the recorder-chain builder with a mocked `createAdminClient` returning `{ rpc: rpcMock, from: fromMock }` where `fromMock` exists only to assert it is never called. Update the file's doc-comment to describe the new 5-behavior coverage. RED gate: run the file BEFORE touching route.ts — the new single-RPC assertions MUST fail against the current select/delete/loop implementation. Capture the RED output for the SUMMARY.
  </action>
  <verify>
    <automated>npx vitest run src/app/api/cron/cleanup-wizard-drafts/route.test.ts 2>&1 | grep -qE "fail|FAIL"</automated>
  </verify>
  <done>New test file expresses the single-RPC contract and is RED against the unmodified route (repro-gate satisfied).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite route.ts to the single RPC call + flip vercel.json to daily (GREEN)</name>
  <files>src/app/api/cron/cleanup-wizard-drafts/route.ts, vercel.json</files>
  <action>
**route.ts:** keep auth + force-dynamic + GET/POST exactly as-is. Replace the body after auth with:
- `const { data, error } = await admin.rpc("cleanup_abandoned_wizard_drafts");`
- on `error`: `console.error("[cron/cleanup-wizard-drafts] cleanup RPC failed:", error)` then generic `NextResponse.json({ error: "Cron cleanup failed" }, { status: 500 })` — raw PostgREST detail stays in the log only (same redaction rule the old route documented at L66-69).
- on success: read `const row = Array.isArray(data) ? data[0] : data;` and return `{ deleted: row?.deleted_drafts ?? 0, orphaned_keys_revoked: row?.swept_keys ?? 0, key_sweep_errors: 0 }`.
- Rewrite the header doc-comment: 7d policy + the LOCKED 24h→7d deviation rationale (Phase-94 resumability, no updated_at — cite 96-VALIDATION decision 1); M-0255 rejected-draft exemption now enforced INSIDE the RPC (`review_note IS NULL`); the atomicity/EPQ race note (single DELETE, `status='draft'` re-checked — finalize promotion is a guarded UPDATE, verified in 96-01); why `key_sweep_errors` is now constantly 0 (one transaction — partial failure impossible; any failure = 500, which Vercel Cron alerts on, preserving H-1251's intent); daily schedule note.
- Keep the `@audit-skip` cron-garbage-collection annotations where the doc-comment references them.

**vercel.json:** change ONLY the `/api/cron/cleanup-wizard-drafts` entry's schedule from `"0 2 * * 0"` to `"0 2 * * *"`. Touch nothing else in the file.

Then: run the Task-1 test file → GREEN; `npx tsc --noEmit` (or `npm run typecheck` if that script exists); `npm run lint` (react-hooks/lint errors are not caught by tsc+vitest — standing pre-ship rule).
  </action>
  <verify>
    <automated>npx vitest run src/app/api/cron/cleanup-wizard-drafts/route.test.ts && grep -c "delete_api_key_if_unreferenced\|ABANDON_DAYS" src/app/api/cron/cleanup-wizard-drafts/route.ts | awk '{exit ($1==0)?0:1}' && python3 -c "import json;c=[x for x in json.load(open('vercel.json'))['crons'] if x['path']=='/api/cron/cleanup-wizard-drafts'];assert c[0]['schedule']=='0 2 * * *',c;print('daily ok')" && npm run lint</automated>
  </verify>
  <done>Route is a thin auth + single-RPC dispatcher (zero references to the old loop/constants), tests GREEN, schedule daily, lint + typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet → /api/cron/cleanup-wizard-drafts | destructive endpoint reachable by HTTP |
| route → error response | PostgREST errors can carry SQL state/table/constraint names |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-09 | Spoofing/Elevation | cron endpoint abuse | mitigate | Bearer CRON_SECRET + safeCompare (timing-safe), pinned by kept auth tests |
| T-96-10 | Info Disclosure | SQL detail leak in 500 body | mitigate | generic envelope, detail to console only — asserted in Task 1 |
| T-96-11 | DoS | repeated failed runs (e.g., unexpected RESTRICT) | mitigate | non-2xx marks the Vercel run FAILED and alerts; RPC lock_timeout='3s' bounds lock waits |
</threat_model>

<verification>
- `npx vitest run src/app/api/cron/cleanup-wizard-drafts/route.test.ts` GREEN; RED output from Task 1 captured.
- Route grep-gate: 0 hits for `delete_api_key_if_unreferenced|ABANDON_DAYS`.
- vercel.json daily schedule assert; `npm run lint` + typecheck clean.
- Deploy note for SUMMARY (advisory, NON-BLOCKING per Nyquist gate): after the milestone lands, a manual `POST` with the CRON_SECRET can smoke-test prod; the offline suite is the gate.
</verification>

<success_criteria>
- CLEAN-01 + CLEAN-02 are live end-to-end: Vercel Cron (daily) → authed route → one atomic RPC.
- Response shape is monitor-stable; failures are loud (500 → Vercel alert) and redacted.
</success_criteria>

<output>
Create `.planning/phases/96-draft-key-hygiene-onboarding-polish/96-03-SUMMARY.md` when done (never Write over existing .planning docs).
</output>
