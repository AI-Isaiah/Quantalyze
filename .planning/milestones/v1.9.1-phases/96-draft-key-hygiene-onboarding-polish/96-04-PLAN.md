---
phase: 96-draft-key-hygiene-onboarding-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/strategy/ApiKeyManager.tsx
  - src/components/strategy/ApiKeyManager.test.tsx
  - src/lib/wizard/wizard-correlation.ts
  - src/lib/wizard/wizard-correlation.test.ts
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx
autonomous: true
requirements: [UX-01, UX-02]

must_haves:
  truths:
    - "A Deribit api_keys row renders the DRB badge in ApiKeyManager, not '?'"
    - "Every wizard fetch carries an X-Correlation-Id header of shape wizard:<uuid>, stable for the wizard session"
    - "The correlation id the user sees in a wizard error envelope is THE SAME id sent on the failing fetch — copy/paste matches server logs, Sentry tags, and compute_jobs.metadata.correlation_id"
    - "No wizard step calls bare fetch() anymore (grep-gated)"
  artifacts:
    - path: "src/lib/wizard/wizard-correlation.ts"
      provides: "client-safe session correlation id + wizardFetch wrapper"
      exports: ["getWizardCorrelationId", "wizardFetch"]
    - path: "src/components/strategy/ApiKeyManager.tsx"
      provides: "deribit entry in the exchangeIcon map"
      contains: "deribit"
  key_links:
    - from: "wizard steps"
      to: "src/lib/wizard/wizard-correlation.ts"
      via: "wizardFetch + getWizardCorrelationId imports"
      pattern: "wizardFetch\\("
    - from: "wizardFetch X-Correlation-Id header"
      to: "server getCorrelationId() (src/lib/correlation-id.ts) + Sentry tag (instrumentation.ts:52)"
      via: "inbound-header preference (already shipped — verified read-only)"
      pattern: "X-Correlation-Id"
---

<objective>
Close the two onboarding polish items: UX-01 (Deribit keys render "?" in ApiKeyManager — the local icon map is missing the `deribit` case) and UX-02 (wizard fetches never SEND a correlation id, so the id shown to the user can never match server logs).

Purpose: Deribit went live in v1.7 but its keys look broken in the manager; and a user reporting a wizard failure today copies an id (the page-render meta id) that matches NOTHING the failing API request logged — support dead-end.

Output: one-line icon fix + test; a client-safe wizard-correlation module; header threaded through all 11 wizard fetch sites; envelope display unified onto the sent id.

Locked decisions honored (96-VALIDATION.md): D5 — reuse the canonical `deribit` label already in `AllocatorExchangeManager` (`DRB`), no new asset, DESIGN.md consistency (no emoji, letter tags). D6 — client-generated `crypto.randomUUID()` header via a NEW client module (`correlation-id.ts` is `server-only` and cannot be imported client-side); `getCorrelationId()` already prefers a valid inbound header, so no server changes are expected (verified read-only in Task 3).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/96-draft-key-hygiene-onboarding-polish/96-VALIDATION.md
@DESIGN.md
@src/lib/correlation-id.ts
@src/components/strategy/ApiKeyManager.tsx
@src/components/exchanges/AllocatorExchangeManager.tsx
</context>

<interfaces>
<!-- Extracted this session. Use directly. -->

UX-01 root cause — `ApiKeyManager.tsx:290-294` local map + `:347` fallback:
```ts
const exchangeIcon: Record<string, string> = { binance: "B", okx: "O", bybit: "By" };
// L347: {exchangeIcon[key.exchange] ?? "?"}
```
Canonical sibling — `AllocatorExchangeManager.tsx:111-118` `EXCHANGE_TAGS` (module-local, NOT exported): `deribit: { label: "DRB", bg: "#DBEAFE", fg: "#1E3A8A" }`. Comment there: "3-letter code, tier-colored. No emoji in the UI per DESIGN.md."

UX-02 server allowlist (`src/lib/correlation-id.ts`): `CORRELATION_HEADER = "x-correlation-id"`; inbound values must match `/^[A-Za-z0-9._:-]{1,128}$/` (else the server falls back to a fresh UUID). `wizard:` + 36-char uuid = 43 chars → passes. Wire-form header casing: `X-Correlation-Id` (repo precedent).

Inbound-header consumers (already shipped — Task 3 verifies, does not build):
- `keys/sync/route.ts:244,540` → `getCorrelationId()` → `compute_jobs.metadata.correlation_id`.
- `finalize-wizard/route.ts:1128` → `postProcessKey({...})` with NO explicit correlationId → defaults to `getCorrelationId()` (`process-key-client.ts:99`).
- `instrumentation.ts:52` → Sentry tag `correlation_id: request.headers["x-correlation-id"]`.

The 11 wizard fetch sites (all in `src/app/(dashboard)/strategies/new/wizard/`):
| File:line | Call |
|---|---|
| ConnectKeyStep.tsx:200 | POST /api/strategies/create-with-key |
| WizardClient.tsx:579 | DELETE /api/strategies/draft/{id} |
| MultiKeyConnectStep.tsx:417 | GET /api/strategies/composite/members |
| MultiKeyConnectStep.tsx:597 | POST /api/strategies/composite/add-key |
| MultiKeyConnectStep.tsx:731 | POST /api/strategies/composite/set-members |
| SyncPreviewStep.tsx:506, 1107 | POST /api/keys/sync (×2) |
| SyncPreviewStep.tsx:636 | GET /api/strategies/{id}/sync-progress (poll) |
| SubmitStep.tsx:74 | POST /api/strategies/finalize-wizard |
| CsvUploadStep.tsx:267 | POST /api/strategies/csv-validate |
| CsvSubmitStep.tsx:98 | POST /api/strategies/csv-finalize |

Existing per-step display plumbing: ConnectKeyStep:167, MultiKeyConnectStep:352, SyncPreviewStep:390, SubmitStep:66 each hold `const [correlationId] = useState(() => readCorrelationId())` where `readCorrelationId()` is a duplicated local helper reading the `<meta name="x-correlation-id">` PAGE-RENDER id — this is exactly why displayed ids never match fetch ids. Envelopes flow through `buildEnvelope(code, correlationId, …)` (`src/lib/envelope.ts`) into `ErrorEnvelope` which already renders `envelope.correlation_id` (`src/components/error/ErrorEnvelope.tsx:173-174`). Csv steps have no readCorrelationId copies.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UX-01 — deribit entry in the ApiKeyManager icon map</name>
  <files>src/components/strategy/ApiKeyManager.test.tsx, src/components/strategy/ApiKeyManager.tsx</files>
  <behavior>
    - Test (extend the existing M-0456 render-contract describe block in ApiKeyManager.test.tsx, reusing its selectResultMock full-row fixture pattern): a row with `exchange: "deribit"` renders the badge text "DRB" and does NOT render "?" in the icon span.
  </behavior>
  <action>
RED first: add the deribit render case → run → fails (renders "?"). GREEN: add `deribit: "DRB"` to the `exchangeIcon` map at ApiKeyManager.tsx:290, with a one-line comment pointing at the canonical `EXCHANGE_TAGS` map in `AllocatorExchangeManager.tsx` ("keep labels in lockstep — DRB per DESIGN.md 3-letter, no-emoji tag convention"). Surgical change only (Rule 3): do NOT extract a shared constant, do NOT add other exchanges, do NOT touch colors — the ApiKeyManager badge is monochrome by design. Note in SUMMARY: the two maps can still drift (flagged for a future consolidation, out of scope per locked decision D5).
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/ApiKeyManager.test.tsx</automated>
  </verify>
  <done>Deribit case GREEN (was RED), all existing ApiKeyManager tests still GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: UX-02 — client wizard-correlation module</name>
  <files>src/lib/wizard/wizard-correlation.ts, src/lib/wizard/wizard-correlation.test.ts</files>
  <behavior>
    - `getWizardCorrelationId()` returns `wizard:<uuid-v4>` matching `/^wizard:[0-9a-f-]{36}$/` and ≤128 chars (server allowlist-safe), and returns the SAME id on repeated calls (module-memoized — one id per wizard session/page load).
    - `wizardFetch(input, init?)` calls global `fetch` with `X-Correlation-Id: <that id>` merged into headers; preserves method/body; preserves caller-supplied headers whether passed as a plain object or a `Headers` instance; caller-supplied `X-Correlation-Id` is NOT silently dropped (the session id wins — assert deterministically either way and document).
    - `_resetWizardCorrelationIdForTests()` clears the memo (test isolation).
  </behavior>
  <action>
Create `src/lib/wizard/wizard-correlation.ts` — client-safe: NO `import "server-only"`, NO `next/headers`. Doc-comment: why this module exists (`correlation-id.ts` is server-only; the server's `getCorrelationId()` prefers a valid inbound header, so sending this id makes client↔server↔Sentry↔compute_jobs joinable — UX-02/#30), the `wizard:<uuid>` shape rationale (documented `<wizard>:<uuid>` form, passes `CORRELATION_ID_SHAPE`), and the one-id-per-session semantics. Implementation: module-level `let cached: string | null`; `crypto.randomUUID()` (browser + Node 20 global, precedent `correlation-id.ts:22`). `wizardFetch` merges via `new Headers(init?.headers)` then `headers.set("X-Correlation-Id", getWizardCorrelationId())`. Test with `vi.stubGlobal("fetch", …)`; reset the memo in beforeEach.
  </action>
  <verify>
    <automated>npx vitest run src/lib/wizard/wizard-correlation.test.ts</automated>
  </verify>
  <done>Module + tests GREEN; zero server-only imports (grep the file for "server-only|next/headers" → 0 hits).</done>
</task>

<task type="auto">
  <name>Task 3: UX-02 — thread the header through all 11 wizard fetch sites + unify the displayed id</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx, src/app/(dashboard)/strategies/new/wizard/steps/ConnectKeyStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvUploadStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/CsvSubmitStep.tsx, src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx</files>
  <action>
1. **Thread:** replace every bare `fetch(` at the 11 sites in `<interfaces>` with `wizardFetch(` (import from `@/lib/wizard/wizard-correlation`). No other call-shape changes.
2. **Unify the displayed id:** in ConnectKeyStep, MultiKeyConnectStep, SyncPreviewStep, SubmitStep replace `useState(() => readCorrelationId())` with `useState(() => getWizardCorrelationId())` and DELETE each file's local `readCorrelationId()` helper (4 duplicated copies — dead after this). The id shown in every `buildEnvelope(...)`/`ErrorEnvelope` is now exactly the id sent on the failing request. (`ErrorEnvelope` already renders it at :173-174 — no change there.)
3. **Server-side verify (read-only, no code expected):** confirm the inbound-preference chain in `<interfaces>` still holds, and grep the remaining target routes for hard-generated ids: `grep -n "randomUUID" src/app/api/strategies/create-with-key/route.ts src/app/api/strategies/composite/add-key/route.ts src/app/api/strategies/composite/set-members/route.ts "src/app/api/strategies/[id]/sync-progress/route.ts" "src/app/api/strategies/draft/[id]/route.ts"` — if any of these routes generates a correlation id directly instead of `getCorrelationId()`, switch that call to `getCorrelationId()` (server file edit is in-scope for this task if — and only if — this grep finds a hard-generate; record it in SUMMARY).
4. **Test:** extend `SubmitStep.test.tsx` (it already mocks global fetch — wizardFetch delegates to global fetch, so existing mocks keep working): assert the finalize-wizard call's request carried `X-Correlation-Id` matching `/^wizard:/`, AND that the id rendered in the error envelope equals the header value (the log-matching contract). RED first: the header assertion fails before step 1 is applied to SubmitStep.
5. Run the touched wizard step test files (`ConnectKeyStep.test.tsx`, `MultiKeyConnectStep.test.tsx`, `SubmitStep.test.tsx`, `SyncPreviewStep.render.test.tsx`, `CsvUploadStep.test.tsx`, `CsvSubmitStep.test.tsx`) — existing suites must stay green (fetch-mock compatibility is expected; fix fallout without weakening assertions).
  </action>
  <verify>
    <automated>bash -c 'test "$(grep -rn "await fetch(\|void fetch(" "src/app/(dashboard)/strategies/new/wizard" --include="*.tsx" | grep -v "\.test\." | wc -l)" -eq 0' && bash -c 'test "$(grep -rln "readCorrelationId" "src/app/(dashboard)/strategies/new/wizard" | wc -l)" -eq 0' && npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx" src/lib/wizard/wizard-correlation.test.ts && npm run lint</automated>
  </verify>
  <done>Zero bare fetches and zero readCorrelationId copies left in the wizard dir; SubmitStep test proves header-sent == id-displayed; all touched step suites green; lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API routes | client-supplied X-Correlation-Id header is untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-12 | Tampering | header-split / log injection via correlation header | mitigate | existing server allowlist `/^[A-Za-z0-9._:-]{1,128}$/` in getCorrelationId (verified, not modified); client id is uuid-derived |
| T-96-13 | Info Disclosure | correlation id shown to user | accept | id is a random uuid, carries no PII/session material |
</threat_model>

<verification>
- `npx vitest run src/components/strategy/ApiKeyManager.test.tsx src/lib/wizard/wizard-correlation.test.ts "src/app/(dashboard)/strategies/new/wizard/steps/SubmitStep.test.tsx"` GREEN (each was RED first).
- Grep gates: 0 bare `fetch(` and 0 `readCorrelationId` in the wizard dir; 0 `server-only` in wizard-correlation.ts.
- `npm run lint` clean (react-hooks errors invisible to tsc+vitest).
- Advisory (NON-BLOCKING): a live wizard run showing the envelope id present as a Sentry tag / compute_jobs.metadata is nice-to-have evidence, not a gate.
</verification>

<success_criteria>
- UX-01: deribit renders DRB, pinned by a test that fails without the map entry.
- UX-02: one session id, sent on every wizard fetch, displayed in every wizard error envelope, accepted by the server's inbound preference — copy/paste log-matching works end-to-end.
</success_criteria>

<output>
Create `.planning/phases/96-draft-key-hygiene-onboarding-polish/96-04-SUMMARY.md` when done (never Write over existing .planning docs).
</output>
