---
phase: 96-draft-key-hygiene-onboarding-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/tests/test_cleanup_wizard_drafts_race.sql
  - supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "Every draft→pending_review promotion path is PROVEN to be a committed guarded UPDATE (not delete+insert) — the EPQ race proof's precondition (OQ3) is verified with cited evidence, or the phase is HALTED"
    - "Both SQL safety-test files exist, contain no psql meta-commands, and FAIL (RED) with a 'cleanup_abandoned_wizard_drafts missing' assertion because the RPC does not exist yet"
    - "The race test pins BOTH orderings sequentially (finalize-first → draft survives; cron-first → finalize fails clean P0002) plus structural prosrc asserts including the 7-day window"
    - "The sweep test pins all 5 safety cases (orphan swept; composite-member, published-member, single-key, allocator_holdings-RESTRICT spared) + sanitize-unaffected"
  artifacts:
    - path: "supabase/tests/test_cleanup_wizard_drafts_race.sql"
      provides: "CLEAN-01 race semantics + structural pins (RED until 96-02)"
      contains: "cleanup_abandoned_wizard_drafts"
    - path: "supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql"
      provides: "CLEAN-02 five safety cases + sanitize-unaffected pins (RED until 96-02)"
      contains: "allocator_holdings"
  key_links:
    - from: "supabase/tests/test_cleanup_wizard_drafts_race.sql"
      to: "pg_proc.prosrc of cleanup_abandoned_wizard_drafts + finalize_wizard_strategy"
      via: "structural prosrc asserts"
      pattern: "prosrc"
    - from: "supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql"
      to: "public.api_keys FK topology (strategies / strategy_keys / allocator_holdings)"
      via: "seeded 5-case behavioral asserts on seeded ids only"
      pattern: "NOT EXISTS"
---

<objective>
Gate the phase on the finalize-shape precondition (OQ3) and lay down the RED safety net for the data-deletion danger zone BEFORE any migration exists.

Purpose: CLEAN-02 deletes `api_keys` rows and the migration auto-applies to PROD on the milestone merge. The tests must exist first (repro-gate: each fails without the change), and the entire race proof for CLEAN-01 rests on finalize being a committed guarded UPDATE — if that assumption is false, planning must stop before a single destructive line ships.

Output: OQ3 verification evidence (in SUMMARY) + two RED SQL test files.

⚠️ REQUIREMENT DEVIATION (flag loudly, locked — do NOT re-litigate): CLEAN-01's ROADMAP text says a 24h window. The LOCKED decision (96-VALIDATION.md, decision 1) is **7 days on `created_at`** — `strategies` has no `updated_at` column, so a 24h cutoff would delete drafts users actively resume on day 2, colliding with Phase-94 wizard resumability. Every window assertion in these tests pins `interval '7 days'` and asserts the prosrc does NOT contain `'24 hours'`.

Note (branching_strategy=none, sequential opus executor on the main tree): this plan intentionally ends RED — both SQL tests fail until 96-02 ships the RPC. Do not "fix" the RED by weakening asserts; 96-02 turns them GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/96-draft-key-hygiene-onboarding-polish/96-RESEARCH.md
@.planning/phases/96-draft-key-hygiene-onboarding-polish/96-VALIDATION.md

Source (read in the tasks, cited here for scope):
@supabase/migrations/20260521185008_wizard_finalize_inserts_verification.sql
@supabase/tests/test_api_key_delete_atomicity.sql
@supabase/tests/test_guard_wizard_draft_updates_auth_uid.sql
@supabase/tests/test_sanitize_user_hardening.sql
@supabase/tests/test_strategy_keys_publish_integrity.sql
</context>

<interfaces>
<!-- Extracted from the codebase this session. Use directly — no re-exploration needed. -->

`finalize_wizard_strategy` — latest definition is migration `20260521185008` (grep-all confirms defs also in 20260411103316, 20260418150632, 20260501055202, 20260513082230, 20260513084844, 20260515114310 — the 20260521185008 body is the live one). Signature:

```sql
finalize_wizard_strategy(
  p_strategy_id UUID, p_user_id UUID, p_name TEXT, p_description TEXT,
  p_category_id UUID, p_strategy_types TEXT[], p_subtypes TEXT[], p_markets TEXT[],
  p_supported_exchanges TEXT[], p_leverage_range TEXT, p_aum NUMERIC, p_max_capacity NUMERIC)
```
Body (L77-121 of 20260521185008): `SELECT status, source, user_id, api_key_id … FROM strategies WHERE id = p_strategy_id FOR UPDATE;` → `IF v_current_status IS NULL THEN RAISE 'not found'` → `IF v_current_status <> 'draft' THEN RAISE` → `UPDATE strategies SET … status = 'pending_review' WHERE id = p_strategy_id`. It ALSO raises `'finalize_wizard_strategy called without an auth session'` when `auth.uid()` is NULL — SQL tests must emulate a session (model: `test_guard_wizard_draft_updates_auth_uid.sql` uses `set_config('request.jwt.claims', …)`).

`api_keys` FK topology (the 7 referencing FKs — all enumerated, verified against migrations):

| Referencing table | ON DELETE | Source migration |
|---|---|---|
| `strategies.api_key_id` | SET NULL | `20260405061911_initial_schema.sql:51` |
| `strategy_keys.api_key_id` | CASCADE | `20260710120000_strategy_keys.sql` |
| `allocator_holdings.api_key_id` | **RESTRICT** (23503 aborts set-based DELETE) | `20260420073003_allocator_holdings.sql:95` |
| `csv_daily_returns.api_key_id` | CASCADE | `20260624120000_csv_daily_returns_per_key_axis.sql:39` |
| `compute_jobs.api_key_id` | CASCADE | `20260420073003_allocator_holdings.sql` (step 2) |
| `key_permission_audit.api_key_id` | CASCADE | `20260416125433_key_permission_audit.sql:35` |

Seed-relevant schemas:
- `strategies`: `user_id NOT NULL REFERENCES profiles`, `api_key_id NULL`, `status CHECK IN ('draft','pending_review','published','archived')`, `source` column (wizard), `review_note` column, `created_at` (NO `updated_at`).
- `api_keys`: `user_id NOT NULL REFERENCES profiles`, `exchange` CHECK — `deribit` allowed since `20260704200446`; `api_key_encrypted NOT NULL`, `label NOT NULL`.
- `strategy_keys`: `strategy_id`, `api_key_id`, `owner_id NOT NULL` (owner-coherence trigger couples it to `api_keys.user_id`), `window_start DATE NOT NULL`, `seq INTEGER NOT NULL`, UNIQUE `(strategy_id, seq)`.
- `allocator_holdings` (`20260420073003:89-130`): NOT NULL `allocator_id` (must equal `api_keys.user_id` — owner-coherence trigger), `api_key_id`, `venue`, `symbol`, `asof DATE`, `holding_type IN ('spot','derivative')`, `side IN ('long','short','flat')`, `quantity`, plus further NOT NULL value columns — read L89-140 before writing the seed.

Published-composite BEFORE DELETE guard: `enforce_api_keys_published_composite_integrity()` + trigger `api_keys_published_composite_delete_guard` (`20260710160000:46,103`); exempts `current_setting('quantalyze.sanitize_in_progress',true)='on'` (L67-69).

`sanitize_user` sets its own GUC via `set_config('quantalyze.sanitize_in_progress','on',true)` (`20260513073518_sanitize_user_hardening.sql:249`) and deletes api_keys BEFORE archiving strategies (`20260417110538_sanitize_user.sql:316`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: OQ3 GATING VERIFY — every draft→pending_review promotion is a committed guarded UPDATE, never delete+insert</name>
  <files>(read-only — evidence recorded in the plan SUMMARY)</files>
  <action>
This task is a GATE (VALIDATION locked decision 2): the CLEAN-01 race proof (READ-COMMITTED EvalPlanQual re-check on `status='draft'`) is only valid if the promotion is a committed UPDATE of the SAME strategies row. Verify BOTH promotion paths:

1. **Classic finalize RPC**: `grep -rln "finalize_wizard_strategy" supabase/migrations/*.sql` (expect the 7 files listed in interfaces). Read the LATEST definition (`20260521185008_wizard_finalize_inserts_verification.sql`). Confirm and record: (a) `SELECT … FROM strategies WHERE id = p_strategy_id FOR UPDATE`, (b) `status <> 'draft'` guard RAISE, (c) promotion is `UPDATE strategies SET … status = 'pending_review' WHERE id = p_strategy_id` — no `DELETE FROM strategies` + `INSERT INTO strategies` pair anywhere in the promotion path.

2. **Unified backbone path** (`finalize-wizard/route.ts:1128` → `postProcessKey({flow_type:'onboard',…})` → analytics-service `/process-key`): find where the strategy row is flipped to `pending_review` on this arm. Run `grep -rn "pending_review" analytics-service --include='*.py' | grep -v test` and `grep -rn "pending_review" supabase/migrations/*.sql | grep -iv "comment\|--"`. For every write site that promotes a wizard draft, confirm it is an `UPDATE … SET status` (or an RPC whose body is a guarded UPDATE) of the existing row — NOT a delete-and-reinsert. Note: the unified arm REJECTS composites (`COMPOSITE_UNSUPPORTED_UNIFIED`, route.ts ~L1120), so composite finalize always goes through the stitch_composite queue — trace that promotion write too if it is a distinct site.

3. Record the evidence (file:line for each promotion write + the shape found) in the SUMMARY.

**STOP CONDITION (gating):** if ANY promotion path is a delete+insert, do NOT proceed to Tasks 2-3. Halt the plan and return a blocker: "OQ3 violated — promotion at {file:line} is delete+insert; the EPQ race proof in VALIDATION decision 2 is invalid and the phase must be re-planned."
  </action>
  <verify>
    <automated>grep -c "FOR UPDATE" supabase/migrations/20260521185008_wizard_finalize_inserts_verification.sql | awk '{exit ($1>=1)?0:1}' && grep -E "UPDATE strategies|UPDATE public.strategies" supabase/migrations/20260521185008_wizard_finalize_inserts_verification.sql | grep -c "pending_review\|SET" | awk '{exit ($1>=1)?0:1}'</automated>
  </verify>
  <done>SUMMARY contains a cited (file:line) inventory of every draft→pending_review write site, each confirmed as a committed guarded UPDATE; FOR UPDATE confirmed in the latest finalize def; no delete+insert promotion exists (or the plan halted with a blocker).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write supabase/tests/test_cleanup_wizard_drafts_race.sql (CLEAN-01, RED)</name>
  <files>supabase/tests/test_cleanup_wizard_drafts_race.sql</files>
  <behavior>
    - Structural: `cleanup_abandoned_wizard_drafts` exists (RAISE 'missing — apply migration 20260713120000' otherwise — this is the RED lever); its prosrc contains `source = 'wizard'`, `status = 'draft'`, `review_note IS NULL`, `interval '7 days'`; prosrc does NOT contain `24 hours` (window-deviation pin) and does NOT contain `sanitize_in_progress`; proconfig bakes `search_path`; `has_function_privilege` shows service_role EXECUTE allowed, anon + authenticated DENIED.
    - Structural: `finalize_wizard_strategy` prosrc contains `FOR UPDATE` and a `<> 'draft'` guard (pins the other half of the race proof so a future finalize rewrite reddens this file).
    - Finalize-first ordering (sequential sim): seeded 8-day-old wizard draft is promoted by the committed UPDATE shape (`UPDATE strategies SET status='pending_review' WHERE id=<seed>`) → RPC call → row SURVIVES as pending_review AND its api_key survives.
    - Cron-first ordering: seeded 8-day-old wizard draft (+ a strategy_keys member row for cascade evidence) → RPC call → strategies row GONE, strategy_keys rows GONE (cascade clean); then the REAL `finalize_wizard_strategy(<seed>, <user>, 'n', NULL, NULL, '{}','{}','{}','{}', NULL, NULL, NULL)` under an emulated session RAISEs its not-found error — caught in a nested BEGIN/EXCEPTION block, asserting the failure is the clean fail-loud path (no torn state).
    - Window pins: an 8-day-old draft WITH `review_note` set is SPARED (M-0255); a 1-day-old draft is SPARED (7d window, not 24h).
  </behavior>
  <action>
Write the file following the repo SQL-test conventions (models: `test_api_key_delete_atomicity.sql` for structure/prosrc-assert style, `test_guard_wizard_draft_updates_auth_uid.sql` for auth-session emulation via `set_config('request.jwt.claims', …)`):

- Wrap the ENTIRE file in `BEGIN;` … `ROLLBACK;` (offline-first, shared test project — no residue). All seeds use `gen_random_uuid()` fresh UUIDs; ALL assertions target seeded ids ONLY (the RPC also sweeps any real stale rows inside the txn — global counts are meaningless; never assert them).
- NO psql meta-commands (no `\set`, `\echo`, …) — the CI preflight rejects them. Assertions are `DO $$ … RAISE EXCEPTION … $$` blocks; a clean run emits NOTICEs only.
- Seeding: create throwaway `auth.users` + `profiles` rows per the model tests' pattern, then `api_keys` (use a valid `exchange`, e.g. 'binance'), then `strategies` rows with `source='wizard'`, `status='draft'`, explicit `created_at = now() - interval '8 days'` (or `'1 day'` for the fresh-spared case). For the cascade-evidence composite: `strategy_keys` needs `owner_id` = the key's `user_id` (owner-coherence trigger) and `window_start`/`seq`.
- The real-finalize call in the cron-first ordering: emulate the authed session first (per the model test); if the auth emulation proves impossible in this harness, FALLBACK (document why in a comment + SUMMARY): assert the not-found guard structurally in finalize's prosrc (`v_current_status IS NULL` branch / 'not found' RAISE) AND behaviorally that the row is gone. Prefer the real call.
- Nested `BEGIN … EXCEPTION WHEN OTHERS` blocks for every expected RAISE (subtransaction — keeps the outer txn alive).
- Header comment: what this file pins, the 7d-vs-24h locked deviation (cite 96-VALIDATION.md decision 1), run-order note ("AFTER migration 20260713120000").
  </action>
  <verify>
    <automated>grep -cE '^\\\\' supabase/tests/test_cleanup_wizard_drafts_race.sql | awk '{exit ($1==0)?0:1}' && psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_cleanup_wizard_drafts_race.sql 2>&1 | grep -qi "cleanup_abandoned_wizard_drafts.*missing"</automated>
  </verify>
  <done>File exists, zero meta-commands, and running it against the test DB FAILS with the 'cleanup_abandoned_wizard_drafts missing' assertion (RED — the RPC does not exist yet). RED output captured for the SUMMARY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql (CLEAN-02, RED)</name>
  <files>supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql</files>
  <behavior>
    - Structural: `cleanup_abandoned_wizard_drafts` prosrc contains ALL THREE reference checks — `NOT EXISTS … strategies`, `NOT EXISTS … strategy_keys`, `NOT EXISTS … allocator_holdings` (the RESTRICT-abort guard); prosrc does NOT contain `sanitize_in_progress` and does NOT call `delete_api_key_if_unreferenced` (the incomplete RPC must not be reused — it checks only `strategies`); `sanitize_user` prosrc STILL contains `set_config('quantalyze.sanitize_in_progress'` (its own exemption intact, untouched); `enforce_api_keys_published_composite_integrity` prosrc STILL contains the `sanitize_in_progress` exemption.
    - Behavioral (ALL five cases seeded together, ONE RPC call — this simultaneously proves no 23503 abort): Case A orphan key on a doomed 8d draft, no other refs → SWEPT. Case B key on a doomed composite draft (draft `api_key_id` NULL, membership via `strategy_keys`) that is ALSO a `strategy_keys` member of a SURVIVING (non-doomed) strategy → SPARED. Case C key on a doomed draft that is ALSO a `strategy_keys` member of a `status='published'` composite → SPARED (predicate is a superset of the guard-protected set; guard is backstop, statement completes). Case D key on a doomed draft that is ALSO `strategies.api_key_id` of a surviving published strategy → SPARED. Case E key on a doomed draft that is ALSO referenced by an `allocator_holdings` row (ON DELETE RESTRICT) → SPARED, AND Case A was still swept in the same call (no statement abort).
    - Composite-member capture: Case B's doomed draft has `api_key_id = NULL` — its member key reaches the candidate set ONLY via the pre-cascade `strategy_keys` capture. A second sub-case: a member key of a doomed composite with NO surviving refs → SWEPT (proves the capture actually happened before the CASCADE destroyed the membership rows).
    - Sanitize-unaffected (behavioral, preferred): per the `test_sanitize_user_hardening.sql` model, run `sanitize_user` for a seeded throwaway user owning an api_key → the key IS deleted (account-deletion path unaffected by the new sweep machinery). If the model shows this requires seeds disproportionate to this file, the structural pins above are the minimum bar — document the fallback in a comment + SUMMARY.
  </behavior>
  <action>
Same conventions as Task 2 (BEGIN/ROLLBACK, fresh UUIDs, seeded-id-only assertions, no meta-commands, nested exception blocks, NOTICE-only clean run).

Seeding notes:
- The `allocator_holdings` seed must satisfy ALL NOT NULL columns and the owner-coherence trigger (`allocator_id` = the key's `user_id`) — read `20260420073003_allocator_holdings.sql:89-140` for the full column list before writing it.
- `strategy_keys` seeds: `owner_id` = key's `user_id`, valid `window_start`, unique `(strategy_id, seq)`.
- The published composite for Case C needs `status='published'` — check for publish-side triggers/constraints on strategies before seeding (model: `test_strategy_keys_publish_integrity.sql` seeds published composites; follow it).
- Doomed drafts: `source='wizard'`, `status='draft'`, `review_note IS NULL`, `created_at = now() - interval '8 days'`.
- Assert AFTER the single RPC call: `EXISTS`/`NOT EXISTS` per case on the seeded key ids; also assert the doomed draft rows are gone and the RPC's returned `swept_keys ≥ 1`.
- Header comment: DATA-DELETION danger-zone rationale, the 5 cases, run-order note.
  </action>
  <verify>
    <automated>grep -cE '^\\\\' supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql | awk '{exit ($1==0)?0:1}' && psql "$TEST_SUPABASE_DB_URL" -v ON_ERROR_STOP=1 -f supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql 2>&1 | grep -qi "cleanup_abandoned_wizard_drafts.*missing"</automated>
  </verify>
  <done>File exists, zero meta-commands, and running it FAILS with the 'missing' assertion (RED). Both RED outputs recorded in SUMMARY; the two files are committed as the phase's Wave-0 safety net.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL tests → shared test DB | tests run against the shared test project; residue or global asserts poison concurrent CI runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Tampering | future finalize rewrite silently breaking the EPQ precondition | mitigate | structural prosrc pins on finalize (`FOR UPDATE`, `<> 'draft'`) in the race test |
| T-96-02 | Repudiation | shared-test-DB flake from seed collisions | mitigate | BEGIN/ROLLBACK + gen_random_uuid seeds + seeded-id-only assertions |
| T-96-03 | Info Disclosure | none (tests, no prod data emitted) | accept | RAISE messages reference seeded ids only |
</threat_model>

<verification>
- `grep -rEc '^\\\\' supabase/tests/test_cleanup_wizard_drafts_race.sql supabase/tests/test_cleanup_orphaned_api_keys_sweep.sql` → 0 each (CI meta-command preflight).
- Both files RED against the test DB with the 'missing' RAISE (repro-gate: they fail without the 96-02 change, by construction).
- OQ3 evidence in SUMMARY (or plan halted).
</verification>

<success_criteria>
- OQ3 verified with file:line citations for every promotion path (all guarded UPDATEs) — or phase halted with a blocker.
- Two RED SQL test files committed, covering: both race orderings + structural pins + 7d window + M-0255 spare (CLEAN-01); 5 safety cases + composite-capture + sanitize-unaffected (CLEAN-02).
- No production code or migration touched by this plan.
</success_criteria>

<output>
Create `.planning/phases/96-draft-key-hygiene-onboarding-polish/96-01-SUMMARY.md` when done (Edit/Write ONLY this new file — never Write over existing .planning docs).
</output>
