---
phase: 95-stitch-progress-transparency
plan: 04
type: execute
wave: 3
depends_on: ["95-02", "95-03"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.progress.render.test.tsx
autonomous: true
branching_strategy: none
use_worktrees: false
executor_model: opus
requirements: [PROG-01, PROG-02, PROG-03]
must_haves:
  truths:
    - "During a composite stitch the wizard shows a per-key panel (Key 1 Successful / Key 2 In process / Key 3 Waiting / Degraded) fed by GET /api/strategies/[id]/sync-progress"
    - "The literal string 'Stitching composite…' is gone from the user surface; the in-progress copy is user-facing and phase-aware (downloaded vs processed)"
    - "The debug strategy_id/status/elapsed <pre> block (and its expand toggle) is gone"
    - "route stalled:true renders a DISTINCT interrupted state with a retry CTA that re-POSTs /api/keys/sync (idempotent); stalled:false never shows it, regardless of elapsed time"
    - "Frozen tests (SyncPreviewStep.render.test.tsx, SyncPreviewStep.composite.render.test.tsx, SyncPreviewStep.test.ts) are UNTOUCHED and green — WIZ-05 durability and the kickoff effect are not modified"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.progress.render.test.tsx"
      provides: "NEW sibling render test: panel + PROG-01 copy + interrupted state + retry CTA + debug-block-gone"
      min_lines: 150
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      provides: "Per-key panel, user-facing copy, interrupted state, sync-progress poll wiring"
      contains: "sync-progress"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      to: "/api/strategies/[id]/sync-progress"
      via: "piggybacked fetch inside the existing poll tick (composite-only, fail-open)"
      pattern: "sync-progress"
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"
      to: "src/lib/sync-progress.ts"
      via: "SyncProgressResponse import (contract from 95-03)"
      pattern: "from \"@/lib/sync-progress\""
---

<objective>
PROG-01 (user-facing copy) + PROG-02 (per-key render, debug block gone) + PROG-03
(distinct interrupted state + retry CTA) on the wizard waiting surface.

All render changes live in the `waiting_for_complete` section of `SyncPreviewStep.tsx`
(`:1539-1626`). New coverage goes in a NEW sibling
`SyncPreviewStep.progress.render.test.tsx` — the three frozen sibling test files are
Phase-94 SC pins and MUST NOT be edited (95-VALIDATION sign-off).

Live granularity is LOCKED (decision 3): per-key status only
(Successful / In process / Waiting / Degraded). Degrade REASONS stay post-completion
(Phase-93 degradedMembers DQ channel) — do NOT duplicate them live.

Purpose: honest, per-key, user-facing progress; a stall surfaces as recoverable
instead of an indefinite hang.
Output: modified waiting render + poll wiring + NEW sibling render test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-stitch-progress-transparency/95-RESEARCH.md
@.planning/phases/95-stitch-progress-transparency/95-VALIDATION.md
@src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx
@src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx
@src/lib/sync-progress.ts
@DESIGN.md
</context>

<interfaces>
<!-- Consumed contract (created by 95-03 — import, do not redeclare): -->
```ts
import type { SyncProgressResponse, MemberProgressEntry } from "@/lib/sync-progress";
// { jobStatus: StitchJobStatus | null; stalled: boolean; memberProgress: MemberProgressEntry[] }
// entry: { seq: number; exchange: string | null; label: string | null;
//          status: "waiting" | "in_process" | "successful" | "degraded" }
```

Anchors in SyncPreviewStep.tsx (verified at source):
- Waiting render: `:1545-1626`. Status line with `"Stitching composite…"`: `:1565-1574`.
  Debug `<pre>` block: `:1614-1623`; its `expandLog` toggle button
  (`data-testid="wizard-sync-expand-log"`): `:1603-1612`.
- Poll effect (self-scheduling setTimeout, backoff ladder): `:502-1065`. `isComposite`
  state threaded from the kickoff response: `:472-478`. `computationStatus` state:
  `:327` (written at `:605`).
- Kickoff POST (the retry CTA re-uses this exact call shape): `:451-455` —
  `fetch("/api/keys/sync", { method:"POST", headers:{"Content-Type":"application/json"}, body: JSON.stringify({ strategy_id: strategyId }) })`.
- DO-NOT-TOUCH: kickoff effect `:347-492` (WIZ-05 durability: COMPLETE-composite
  short-circuit `:398-450`, fail-closed unreadable marker `:437-441`, stale
  fall-through `:451`). The heavy terminal arms `:655-1017`. This plan adds state +
  render + one fetch inside the poll tick — nothing else.

Status→label map (exact UI strings, LOCKED set):
`waiting → "Waiting"`, `in_process → "In process"`, `successful → "Successful"`,
`degraded → "Degraded"`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — NEW sibling render test (panel + copy + interrupted + debug-gone)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.progress.render.test.tsx</files>
  <behavior>
    Create `SyncPreviewStep.progress.render.test.tsx`, cribbing the mount harness +
    browser-supabase mock from the FROZEN `SyncPreviewStep.composite.render.test.tsx`
    (read it for the harness; NEVER edit it). Additionally mock global fetch: POST
    /api/keys/sync → 202 {composite:true}; GET .../sync-progress → a mutable
    `progressResponse` the test drives. Drive the component into
    `waiting_for_complete` with `isComposite` true (kickoff response composite:true),
    using fake timers to step the poll ladder. Pins (all RED pre-implementation):
    - PANEL: with progressResponse memberProgress
      [{seq:1,...,status:"successful"},{seq:2,...,status:"in_process"},{seq:3,...,status:"waiting"}]
      → `wizard-member-progress` testid visible with 3 rows (`member-progress-1..3`)
      showing exact labels "Successful", "In process", "Waiting"; row text includes the
      key identity ("Key 1" + label/exchange when present). A "degraded" entry renders
      "Degraded" with NO reason text (decision 3 — reason stays post-completion).
    - PANEL EMPTY: memberProgress [] → panel absent (no invented rows).
    - PROG-01 COPY: exact literal "Stitching composite…" ABSENT from the DOM
      (screen.queryByText matching the exact string → null); the composite status line
      shows "Trades are being downloaded…" when computationStatus is pending/unknown
      and "Trades are being processed…" once computing.
    - DEBUG GONE: `wizard-sync-expand-log` testid absent; no DOM text matching
      /strategy_id=/ at any elapsed.
    - PROG-03 INTERRUPTED: progressResponse stalled:true → `wizard-sync-interrupted`
      testid visible with recoverable copy + a "Retry sync" button; clicking it issues
      exactly one POST /api/keys/sync with {strategy_id} (assert on the fetch mock) and
      polling CONTINUES (next tick still fetches strategy_analytics).
    - NOT-STALLED NEVER INTERRUPTED: stalled:false with jobStatus "running" → no
      interrupted testid even after advancing past RETRY_THRESHOLD_MS (15 min) — the
      generic "taking much longer" copy may show, but the interrupted state is
      EXCLUSIVELY route-driven (this is the render half of the RT-1 guarantee).
    - FAIL-OPEN: sync-progress fetch rejecting/500 → no crash, no interrupted state,
      panel simply absent/stale, analytics polling unaffected.
    - SINGLE-KEY NEUTRALITY: with composite:false kickoff, NO sync-progress fetch is
      ever issued and no panel renders (the frozen render tests remain the
      authoritative single-key pins; this is just the no-new-traffic guard).
    Run RED and record it.
  </behavior>
  <action>
Write the test file per <behavior>. Use `data-testid` queries plus exact-string copy
assertions (the copy strings are part of the PROG-01 contract). Keep every assertion
against the NEW surface in THIS file — zero edits to the three frozen siblings.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.progress.render.test.tsx" 2>&1 | grep -qiE "fail" && echo RED-CONFIRMED</automated>
  </verify>
  <done>Sibling test exists, RED for the right reasons (panel/copy/interrupted absent in current code); frozen files byte-untouched (git status clean for them).</done>
</task>

<task type="auto">
  <name>Task 2: GREEN part 1 — poll wiring + per-key panel + PROG-01 copy + debug removal</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <action>
1. STATE: add `const [syncProgress, setSyncProgress] = useState<SyncProgressResponse | null>(null);`
   (import type from `@/lib/sync-progress`).
2. POLL WIRING: inside the existing poll tick (`:502-1065`), when `isComposite` and the
   phase is still waiting, piggyback a fire-and-forget
   `fetch(\`/api/strategies/\${strategyId}/sync-progress\`)` → `setSyncProgress(json)`.
   HARD RULES: (a) failures are swallowed with a console.warn and MUST NOT increment
   `consecutiveErrors` or `heavyFetchErrors` — progress is cosmetic, strategy_analytics
   stays the authoritative poll; (b) no new timer — reuse the existing tick so cadence
   follows POLL_BACKOFF_MS; (c) guard setState behind the existing `mountedRef` /
   cancellation convention of the effect. Single-key (`!isComposite`): never fetch.
3. PANEL: in the waiting render (`after :1578`, inside the bordered card), when
   `isComposite && syncProgress?.memberProgress.length`, render
   `<ul data-testid="wizard-member-progress">` with one
   `<li data-testid={\`member-progress-\${m.seq}\`}>` per entry sorted by seq:
   left = `Key {seq}` plus ` — {label ?? capitalized exchange}` when available;
   right = status chip with EXACT text Waiting / In process / Successful / Degraded.
   Style per DESIGN.md tokens already used in this card (text-caption, text-text-muted,
   text-positive for Successful, text-amber-600 for Degraded, accent pulse for
   In process — match the file's existing chip/dot idioms; read DESIGN.md before
   choosing anything visual). NO degrade reasons (decision 3).
4. PROG-01 COPY: replace the composite arm of the status line (`:1569-1570`) —
   `"Stitching composite…"` → phase-aware, keyed off existing `computationStatus`
   (A2, no new worker state): `computationStatus === "computing"` →
   `"Trades are being processed…"`, else → `"Trades are being downloaded…"`.
   Leave the single-key arm (`:1571-1573`) and the heading/body copy byte-unchanged.
5. DEBUG REMOVAL: delete the `expandLog` `<pre>` block (`:1614-1623`), the toggle
   button (`:1603-1612`), and the now-dead `expandLog` state. `computationError`
   remains surfaced by the existing failure gates — do not add a replacement debug UI.
6. Do NOT touch: kickoff effect, WIZ-05 short-circuits, heavy terminal arms,
   POLL_BACKOFF_MS, MAX_CONSECUTIVE_POLL_ERRORS, RETRY_THRESHOLD_MS.
Run the panel/copy/debug/fail-open/neutrality pins GREEN (interrupted pins go green in Task 3).
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/" && npx tsc --noEmit</automated>
  </verify>
  <done>Panel + copy + debug-gone + fail-open + single-key-neutrality pins green; ALL frozen sibling tests green with zero edits; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN part 2 — distinct interrupted state + idempotent retry CTA</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx</files>
  <action>
1. INTERRUPTED STATE: in the waiting render, when `syncProgress?.stalled === true`,
   render `<div data-testid="wizard-sync-interrupted">` (visually distinct — negative/
   amber treatment per DESIGN.md) with copy along the lines of: "The sync appears to
   have been interrupted. This can happen if the worker restarted mid-run. You can
   retry safely — a healthy run in progress is unaffected." plus a
   `<Button size="sm">Retry sync</Button>`. The interrupted state renders ALONGSIDE the
   ongoing spinner card (polling continues — the job may self-recover via the 30-min
   watchdog reclaim), it does NOT replace the phase or abort the poll loop.
2. RETRY CTA: onClick re-POSTs `/api/keys/sync` with `{ strategy_id: strategyId }`
   using exactly the kickoff call shape (`:451-455`). Idempotency is server-side
   (partial-unique index `compute_jobs_one_inflight_per_kind_strategy` — a genuinely
   inflight job makes the re-POST a no-op; a watchdog-reclaimed or dead job gets
   re-enqueued; the route fails closed on unknowable membership). On response:
   `setSyncProgress(null)` (clears stalled until the next route poll re-asserts it),
   disable the button while the POST is in flight (guard double-click), keep phase
   as-is. A non-2xx retry response keeps the interrupted state visible (user can retry
   again) — do NOT route it into the SYNC_FAILED terminal gate (the authoritative
   analytics poll owns terminal transitions).
3. The interrupted state is driven EXCLUSIVELY by `syncProgress.stalled` (route truth,
   job-heartbeat-derived). Never derive it from elapsed time or from
   `strategy_analytics` status regression (RT-1: a member-set change legitimately
   resets analytics to pending while the job re-stitches — the route returns
   stalled:false and the wizard shows normal in-progress).
Run the full sibling test file GREEN, then the whole wizard-steps + strategy-component
directories.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/" "src/components/strategy/" && npm run lint</automated>
  </verify>
  <done>All Task-1 pins green incl. interrupted/retry/not-stalled-never-interrupted; frozen files untouched (`git status --porcelain` shows only SyncPreviewStep.tsx + the new sibling test); lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser render ← route JSON | progress payload is server-projected but still untrusted at render (shape-guard) |
| retry CTA → POST /api/keys/sync | user-triggered re-enqueue mid-wizard |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-11 | Info Disclosure | debug <pre> leaked strategy_id/status/error | mitigate | block deleted; render test pins /strategy_id=/ absent |
| T-95-12 | Denial of Service | retry CTA spam re-enqueueing jobs | mitigate | server-side one-inflight index + keys/sync two-tier rate limit already gate it; client disables button in flight |
| T-95-13 | Tampering | malformed progress JSON crashing the wizard | mitigate | fail-open fetch handling + optional-chained render guards; fail-open pin in the sibling test |
| T-95-14 | Spoofing | elapsed-time heuristics faking a stall (RT-1 regression) | mitigate | interrupted state exclusively route-driven; not-stalled-never-interrupted pin at >15 min elapsed |
| T-95-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/" src/components/strategy/` green.
- Frozen-file gate: `git status --porcelain -- "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.render.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.composite.render.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.test.ts"` is EMPTY.
- Copy gate: `grep -c "Stitching composite" "src/app/(dashboard)/strategies/new/wizard/steps/SyncPreviewStep.tsx"` returns 0 (or only inside a comment explaining the removal — prefer 0).
- Post-land: /qa proves the panel + copy land in the browser (feedback_qa_to_verify_ui_changes); live 3-key stitch + OOM interruption remain NON-BLOCKING dogfooding follow-ups (95-RESEARCH live-corroboration note).
</verification>

<success_criteria>
- SC-1: user-facing, phase-aware in-progress copy; "Stitching composite…" gone.
- SC-2 (render half): per-key Successful/In process/Waiting/Degraded panel live during
  the stitch; debug block gone.
- SC-3 (render half): stall renders distinct + recoverable via idempotent retry, never
  an indefinite hang; RT-1 re-stitch never presents as a stall.
- Phase-94 pins: frozen tests untouched and green; kickoff/WIZ-05 logic byte-unchanged.
</success_criteria>

<output>
Create `.planning/phases/95-stitch-progress-transparency/95-04-SUMMARY.md` when done.
</output>
