---
phase: 95-stitch-progress-transparency
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/strategy/SyncProgress.poll.test.tsx
autonomous: true
branching_strategy: none
use_worktrees: false
executor_model: opus
requirements: [UX-03]
must_haves:
  truths:
    - "The SyncProgress poll loop's 3s cadence, 120s cap, 30s missing-row grace, and onStatusChange transition order are pinned by an automated test BEFORE any refactor touches them"
    - "The characterization test is green against CURRENT main (it pins existing behavior — it is NOT a red-first feature test)"
  artifacts:
    - path: "src/components/strategy/SyncProgress.poll.test.tsx"
      provides: "Characterization pins for the currently-unpinned SyncProgress poll loop"
      min_lines: 120
  key_links:
    - from: "src/components/strategy/SyncProgress.poll.test.tsx"
      to: "src/components/strategy/SyncProgress.tsx"
      via: "render + fake timers driving the real setInterval(pollStatus, 3000) loop"
      pattern: "vi\\.useFakeTimers"
---

<objective>
Wave 0 HARD BLOCKER for the #46 `useStrategySyncPoller` extraction (plan 95-05):
pin the CURRENTLY-UNPINNED `SyncProgress` poll loop with a characterization test.

`SyncProgress.test.ts` pins ONLY the pure `toSyncStatus` mapping — the poll loop
itself (3s interval, 120s cap via POLL_MAX_ATTEMPTS=40, 30s missing-row grace via
MISSING_ROW_GRACE_POLLS=10, onStatusChange transition filtering) has ZERO coverage.
Extracting the loop into a shared hook with no characterization test = silent
behavior change ("no behavior change" would be asserted, not proven).

Purpose: make plan 95-05's zero-edit green-diff parity method possible.
Output: `src/components/strategy/SyncProgress.poll.test.tsx` — green against
current main, sensitive to every loop parameter.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-stitch-progress-transparency/95-RESEARCH.md
@.planning/phases/95-stitch-progress-transparency/95-VALIDATION.md
@src/components/strategy/SyncProgress.tsx
@src/components/strategy/SyncProgress.test.ts
</context>

<interfaces>
<!-- Extracted from src/components/strategy/SyncProgress.tsx — the behavior under pin. -->

Constants (module-level, `SyncProgress.tsx:23,29`):
```ts
const POLL_MAX_ATTEMPTS = 40;        // > 40 polls (~120s at 3s) → onStatusChange("error")
const MISSING_ROW_GRACE_POLLS = 10;  // missing row tolerated for 10 polls (~30s), 11th → "error"
```

Poll mechanics (`SyncProgress.tsx:211-282`):
- `isActive = syncStatus === "syncing" || syncStatus === "computing"` (`:145`)
- On `isActive`: `pollAttemptsRef.current = 0`, then `setInterval(pollStatus, 3000)` (`:269-282`)
- `pollStatus` (`:213-267`): increments attempts FIRST; `if (attempts > POLL_MAX_ATTEMPTS) → onStatusChange?.("error"); return;`
  then reads `strategy_analytics(computation_status, computation_error, computed_at)` via
  `createClient().from("strategy_analytics").select(...).eq("strategy_id", id).single()`;
  `!data && attempts > MISSING_ROW_GRACE_POLLS → onStatusChange?.("error")`;
  DB status routed through `toSyncStatus`; ONLY `computing | complete | complete_with_warnings | error`
  are forwarded to `onStatusChange` — DB `pending` (UI `idle`) is NOT propagated mid-sync (`:257-266`).
- Separate effect fetches exchange name via `from("strategies")` then `from("api_keys")` (`:148-186`) —
  the supabase mock MUST handle these tables or the render throws.

Props (`:78-86`): `{ strategyId, syncStatus, lastSyncAt, syncError, syncWarnings?, onRetry, onStatusChange? }`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pin the cap, grace, and cadence (timing pins)</name>
  <files>src/components/strategy/SyncProgress.poll.test.tsx</files>
  <action>
Create `src/components/strategy/SyncProgress.poll.test.tsx` — a NEW sibling to the
existing `SyncProgress.test.ts` (which stays untouched; it pins only `toSyncStatus`).
This is a CHARACTERIZATION test: it must pass against CURRENT main with zero
production-code changes. It exists to make plan 95-05's extraction provably
behavior-preserving (Wave-0 blocker per 95-VALIDATION.md).

Harness: `vi.mock("@/lib/supabase/client")` with a controllable chain mock whose
`from(table)` dispatches on table name — `strategies` → `{ data: { api_key_id: "..." } }`,
`api_keys` → `{ data: { exchange: "deribit" } }`, `strategy_analytics` → a mutable
`analyticsResult` the test drives per-poll (crib the browser-client mock style from
existing component tests, e.g. the wizard render tests; the chain must terminate in
`.single()` resolving `{ data, error }`). Use `vi.useFakeTimers()` +
`await vi.advanceTimersByTimeAsync(3000)` to step polls (async advance so the awaited
supabase promise inside `pollStatus` settles). Render the REAL `SyncProgress` with
`syncStatus="computing"` (isActive) and a `vi.fn()` `onStatusChange`.

Pins (each its own `it`):
1. CADENCE — advancing 3000ms triggers exactly ONE `strategy_analytics` select; 9000ms
   → exactly three. Count via a spy on the mock's `strategy_analytics` branch.
2. MISSING-ROW GRACE — `analyticsResult = { data: null, error: { code: "PGRST116" } }`:
   polls 1..10 produce ZERO `onStatusChange` calls; poll 11 calls `onStatusChange("error")`
   exactly once. (Pin the exact boundary: assert not-called after 10×3000ms, called after one more tick.)
3. 120s CAP — `analyticsResult = { data: { computation_status: "computing", computation_error: null, computed_at: null }, error: null }`
   (a present-but-non-terminal row so the grace path never fires): polls 1..40 never call
   `onStatusChange("error")` (they DO call `onStatusChange("computing")` — filter assertions
   to the "error" value); poll 41 calls `onStatusChange("error")`.
4. COUNTER RESET — after driving several polls, rerender with `syncStatus="complete"`
   (inactive) then back to `"computing"`: the attempt counter restarts (drive 10 more
   missing-row polls post-reset and assert NO "error" — proves `pollAttemptsRef.current = 0`
   on re-activation, `SyncProgress.tsx:272`).

Header comment MUST state: "CHARACTERIZATION — pins the pre-#46 poll-loop behavior.
Plan 95-05 (useStrategySyncPoller extraction) must keep this file GREEN WITH ZERO EDITS.
Do not update these pins to match new behavior; new behavior is a regression."

Do NOT modify SyncProgress.tsx. Do NOT touch SyncProgress.test.ts.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/SyncProgress.poll.test.tsx</automated>
  </verify>
  <done>File exists; cadence/grace/cap/reset pins pass against unmodified SyncProgress.tsx; exact boundaries (10 vs 11, 40 vs 41) asserted, not approximated.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the transition-forwarding contract (semantic pins)</name>
  <files>src/components/strategy/SyncProgress.poll.test.tsx</files>
  <action>
Extend the same file with the `onStatusChange` FORWARDING pins (the contract plan
95-05's hook must reproduce byte-for-byte behaviorally):

5. FORWARDED STATES — for each DB `computation_status` in
   `computing → "computing"`, `complete → "complete"`,
   `complete_with_warnings → "complete_with_warnings"`, `failed → "error"`:
   drive one poll with that row and assert `onStatusChange` was called with exactly
   the mapped value.
6. NON-PROPAGATION — DB `pending` (UI `idle`) is NEVER forwarded: drive polls with
   `computation_status: "pending"` and assert `onStatusChange` not called (the caller
   primed "computing"; `SyncProgress.tsx:250-266` deliberately drops idle).
7. NON-PGRST116 ERROR PATH — a poll error with `error: { code: "500", message: "boom" }`
   and `data: null` consumes grace polls exactly like a missing row (no immediate
   escalation, no throw): assert no `onStatusChange` before the grace boundary. This
   pins that SyncProgress has NO consecutive-error escalation — unlike the wizard's
   MAX_CONSECUTIVE_POLL_ERRORS=3 — a load-bearing asymmetry the parametrized hook
   must preserve (95-RESEARCH "Pitfall 6").
8. INACTIVE = NO POLLING — with `syncStatus="complete"` from mount, advancing 30s
   produces ZERO `strategy_analytics` selects.

Run the full strategy-component directory to prove no collateral damage to the
frozen `SyncProgress.test.ts` sibling.
  </action>
  <verify>
    <automated>npx vitest run src/components/strategy/</automated>
  </verify>
  <done>All 8 pins green against unmodified SyncProgress.tsx; `SyncProgress.test.ts` untouched (git status clean for it) and green; header documents the zero-edit contract for 95-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Test-only plan; no production code, routes, or data paths change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Tampering (future) | SyncProgress poll loop | mitigate | This plan IS the mitigation: characterization pins make a silent behavior change in 95-05 impossible to land green |
| T-95-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (95-RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `npx vitest run src/components/strategy/` green with ZERO production-code diffs
  (`git status --porcelain src/components/strategy/SyncProgress.tsx` empty).
- New file is the ONLY diff.
</verification>

<success_criteria>
- SyncProgress poll loop fully pinned: cadence, 120s cap (40/41 boundary), 30s grace
  (10/11 boundary), counter reset, forwarding table, idle non-propagation, no-escalation
  asymmetry, inactive silence.
- Test is green NOW (characterization, not red-first) and documented as zero-edit-frozen
  for plan 95-05.
</success_criteria>

<output>
Create `.planning/phases/95-stitch-progress-transparency/95-01-SUMMARY.md` when done.
</output>
