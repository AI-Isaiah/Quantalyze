---
phase: 95-stitch-progress-transparency
plan: 02
type: execute
wave: 2
depends_on: ["95-01"]
files_modified:
  - supabase/migrations/20260712130000_set_compute_job_progress.sql
  - supabase/tests/test_set_compute_job_progress.sql
  - analytics-service/services/job_worker.py
  - analytics-service/tests/test_stitch_composite_job.py
autonomous: true
branching_strategy: none
use_worktrees: false
executor_model: opus
requirements: [PROG-02]
must_haves:
  truths:
    - "During a stitch, the worker writes per-member `{seq, exchange, label, status}` progress into `compute_jobs.metadata.member_progress` via a claim-token-fenced JSONB-merge RPC"
    - "Existing metadata keys (`source`, `correlation_id`) survive every progress write (merge, never replace)"
    - "The stitched series/metrics are byte-identical to pre-change (SC-4): all four parity pins pass unmodified"
    - "Progress metadata NEVER contains key material (api_key_encrypted / api_secret_encrypted / passphrase_encrypted / dek_encrypted / nonce)"
    - "The second MTM `_reconstruct_all` pass does NOT restart the per-member counter (progress writes are cash-pass-only)"
  artifacts:
    - path: "supabase/migrations/20260712130000_set_compute_job_progress.sql"
      provides: "Fenced JSONB-merge RPC set_compute_job_progress, service-role-only"
      contains: "set_compute_job_progress"
    - path: "supabase/tests/test_set_compute_job_progress.sql"
      provides: "Fence + merge + privilege SQL gate (CI sql-tests lane)"
    - path: "analytics-service/services/job_worker.py"
      provides: "Best-effort progress writes in the _reconstruct_all member loop (cash pass only)"
      contains: "set_compute_job_progress"
  key_links:
    - from: "analytics-service/services/job_worker.py"
      to: "set_compute_job_progress RPC"
      via: "supabase.rpc with p_job_id + p_claim_token fence"
      pattern: "set_compute_job_progress"
    - from: "supabase/migrations/20260712130000_set_compute_job_progress.sql"
      to: "compute_jobs.metadata"
      via: "COALESCE(metadata,'{}') || jsonb_build_object(...) merge"
      pattern: "member_progress"
---

<objective>
PROG-02 (write side): the worker writes per-member stitch progress to
`compute_jobs.metadata` so the poll route (plan 95-03) has something to surface.

Mechanism (LOCKED, 95-VALIDATION decision + user decision 4): a claim-token-fenced
JSONB-merge RPC `set_compute_job_progress`, called best-effort from the
`_reconstruct_all` member loop (`job_worker.py:~3331`), cash pass only. The RPC also
stamps `member_progress_at = now()` — the heartbeat plan 95-03's stall detector reads.

Purpose: per-key transparency data + the PROG-03 heartbeat, with zero effect on
stitched output (SC-4 byte-identity).
Output: migration + SQL gate + worker write + extended pytest coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/95-stitch-progress-transparency/95-RESEARCH.md
@.planning/phases/95-stitch-progress-transparency/95-VALIDATION.md
@supabase/migrations/20260515114555_compute_jobs_claim_token_fencing.sql
@supabase/tests/test_wizard_composite_fence.sql
@analytics-service/tests/test_stitch_composite_job.py
</context>

<interfaces>
<!-- The cross-plan metadata contract. Plans 95-03/95-04 build against EXACTLY this shape. -->

Written by the worker (this plan), merged into `compute_jobs.metadata`:
```jsonc
{
  // ...existing keys preserved: "source", "correlation_id" (enqueue_compute_job, route.ts:250)
  "member_progress": [
    { "seq": 1, "exchange": "deribit", "label": "Main account", "status": "successful" },
    { "seq": 2, "exchange": "deribit", "label": null,           "status": "in_process" },
    { "seq": 3, "exchange": null,      "label": null,           "status": "waiting" }
  ],
  "member_progress_at": "2026-07-12T14:03:22.123456+00:00"  // stamped SERVER-SIDE by the RPC via now()
}
```
Status enum (closed set): `"waiting" | "in_process" | "successful" | "degraded"`.
`exchange`/`label` are null until that member's preflight resolves its `api_keys` row
(members are loaded as `strategy_keys(api_key_id, owner_id, window_start, window_end, seq)` —
`job_worker.py:3011-3018` — which carries NO exchange/label; `ctx.key_row` is the full
`api_keys` row post-preflight, `job_worker.py:754-764`).

FORBIDDEN in any progress payload (WIZ-01 secretless boundary): `api_key_encrypted`,
`api_secret_encrypted`, `passphrase_encrypted`, `dek_encrypted`, `nonce`, or any
`key_row` passthrough. Build entries FIELD-BY-FIELD — never spread/copy `key_row`.

Worker RPC call pattern (mirror the fenced `defer_compute_job` call, `job_worker.py:562-569`):
```python
supabase.rpc("set_compute_job_progress", {
    "p_job_id": job["id"],
    "p_claim_token": job.get("claim_token"),
    "p_progress": progress_list,   # the full member_progress array, replaced wholesale each write
}).execute()
```

compute_jobs.status values (CHECK, `20260411144407:112-120`):
`pending | running | done | done_pending_children | failed_retry | failed_final`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Migration — fenced set_compute_job_progress RPC + SQL gate</name>
  <files>supabase/migrations/20260712130000_set_compute_job_progress.sql, supabase/tests/test_set_compute_job_progress.sql</files>
  <behavior>
    - Fenced merge: UPDATE with matching (id, claim_token) on a 'running' row merges
      member_progress + member_progress_at into metadata; pre-existing keys
      (source, correlation_id) survive.
    - Stale-token no-op: a mismatched claim_token changes NOTHING (watchdog-reclaim race safety).
    - Non-running no-op: a 'done' row is not written even with a matching token.
    - Privilege: anon + authenticated have NO EXECUTE (has_function_privilege false);
      the projection to users happens ONLY via get_user_compute_jobs.
  </behavior>
  <action>
Per user decision 4 (migration discipline), FIRST run
`grep -rn "set_compute_job_progress" supabase/migrations/` and confirm NO existing
definition (expected: none — this is a NEW function, so there is nothing to re-base;
if a def unexpectedly exists, re-base on the latest one). No new delete-guard is
created, so the `sanitize_in_progress` GUC exemption rule is N/A — state this in the
migration header comment.

Create `supabase/migrations/20260712130000_set_compute_job_progress.sql` (timestamp
AFTER the current latest `20260712120000_...`; if a later migration has landed on main
by execution time, bump past it):

```sql
CREATE OR REPLACE FUNCTION set_compute_job_progress(
  p_job_id      UUID,
  p_claim_token UUID,
  p_progress    JSONB
) RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  UPDATE compute_jobs
     SET metadata = COALESCE(metadata, '{}'::jsonb)
                 || jsonb_build_object(
                      'member_progress',    p_progress,
                      'member_progress_at', to_jsonb(now())
                    )
   WHERE id = p_job_id
     AND claim_token IS NOT DISTINCT FROM p_claim_token
     AND claim_token IS NOT NULL
     AND status = 'running';
  RETURN FOUND;
END;
$$;
REVOKE ALL ON FUNCTION set_compute_job_progress(UUID, UUID, JSONB) FROM PUBLIC, anon, authenticated;
GRANT EXECUTE ON FUNCTION set_compute_job_progress(UUID, UUID, JSONB) TO service_role;
COMMENT ON FUNCTION set_compute_job_progress IS '...';
```
Mirror the style/comments of the existing fenced RPCs
(`20260515114555_compute_jobs_claim_token_fencing.sql`,
`20260529170000_defer_compute_job_claim_token_fence.sql`). The `claim_token IS NOT NULL`
guard means a watchdog-reclaimed row (token nulled) can never be written by the old owner.
Best-effort semantics: RETURN FOUND (no exception on no-op) — the worker treats false as
"lost ownership, stop writing progress" at most; it must never fail the stitch.

Create `supabase/tests/test_set_compute_job_progress.sql` following the house style of
`test_wizard_composite_fence.sql` (READ ITS HEADER: no pgTAP — plain `DO $$` blocks with
RAISE EXCEPTION on fail / RAISE NOTICE on pass; NO psql backslash meta-commands; ALL
fixture work inside an explicit transaction ending in ROLLBACK; every id from
gen_random_uuid() so concurrent CI runs on the shared test DB cannot collide —
95-RESEARCH Pitfall 5 / parallelism-safe requirement). Assert the four behaviors above,
seeding a compute_jobs row with `metadata = '{"source":"x","correlation_id":"y"}'` and
asserting both keys survive the merge and `member_progress_at` is populated.

This migration ships with the milestone PR; migration-reviewer + rls-policy-auditor
routing happens post-land (user decision 4) — note it in the SUMMARY.
  </action>
  <verify>
    <automated>test -f supabase/migrations/20260712130000_set_compute_job_progress.sql && grep -v '^--' supabase/migrations/20260712130000_set_compute_job_progress.sql | grep -c "set_compute_job_progress" | awk '$1>=3{exit 0} {exit 1}' && grep -v '^--' supabase/tests/test_set_compute_job_progress.sql | grep -q "ROLLBACK" && grep -v '^--' supabase/migrations/20260712130000_set_compute_job_progress.sql | grep -q "REVOKE ALL ON FUNCTION set_compute_job_progress" </automated>
    <human-check>SQL gate executes in the CI `sql-tests` lane (psql -v ON_ERROR_STOP=1) — pre-migration it is RED (function absent aborts the file), post-migration GREEN. The executor has no live DB creds; CI is the runtime gate.</human-check>
  </verify>
  <done>Migration + SQL test exist; fence/merge/privilege/no-op assertions written; ROLLBACK hygiene + uuid-scoped fixtures (parallelism-safe); grep confirms no prior definition and service-role-only grants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Worker — best-effort progress writes in the _reconstruct_all member loop</name>
  <files>analytics-service/services/job_worker.py, analytics-service/tests/test_stitch_composite_job.py</files>
  <behavior>
    RED first — extend `test_stitch_composite_job.py` (EXTEND, don't rewrite — 95-VALIDATION
    Wave 0) with a `TestMemberProgress` group asserting, against the existing pure-stub
    run_stitch_composite_job harness:
    - Test 1: a 2-member happy-path stitch issues `set_compute_job_progress` RPC calls
      (captured from the supabase stub's rpc mock) with `p_job_id == job["id"]` and
      `p_claim_token == job["claim_token"]` on EVERY call.
    - Test 2: payload sequence — first write: all members `waiting` (exchange/label null);
      then per member seq ascending: an `in_process` write for that seq, then a
      `successful` write carrying `exchange` (and `label` when the stubbed api_keys row
      has one). Final payload: all members `successful`.
    - Test 3: a degraded ccxt member (reuse the existing degrade-path stub) ends
      `status == "degraded"` for its seq while others are `successful`.
    - Test 4 (SC-4 pass-scoping): with the MTM second pass admissible, NO progress write
      occurs during the MTM `_reconstruct_all` call — the write count equals the
      cash-pass-only expectation (Pitfall 1: no counter restart).
    - Test 5 (secretless): recursively walk EVERY captured p_progress payload and assert
      none of {api_key_encrypted, api_secret_encrypted, passphrase_encrypted,
      dek_encrypted, nonce} appears as a key at any depth.
    - Test 6 (fail-open): make the progress rpc stub raise; the stitch still completes
      with the SAME DispatchResult and the SAME strategy_analytics upsert as the
      happy path (progress is a side-channel — a progress-write blip must never kill
      a 20-minute stitch).
    Run RED: `-k member_progress` fails on unmodified job_worker.py.
  </behavior>
  <action>
Implement in `analytics-service/services/job_worker.py`, inside
`run_stitch_composite_job`:

1. Change `_reconstruct_all(basis)` (`:3315`) to accept `report_progress: bool = False`.
   The cash-pass call site `cash_result = await _reconstruct_all(cash_pnl_basis)`
   (`:3532`) passes `report_progress=True`; the MTM pass call site stays default False
   (Pitfall 1 — the panel must not appear to restart at Key 1).
2. Add a local closure `_write_member_progress(progress: list[dict]) -> None` that
   no-ops when `not report_progress` or `job.get("id") is None`, else calls the RPC via
   the `db_execute(...)` wrapper using the exact pattern at `job_worker.py:562-569`
   (`supabase.rpc("set_compute_job_progress", {"p_job_id": ..., "p_claim_token":
   job.get("claim_token"), "p_progress": progress}).execute()`), wrapped in
   `try/except asyncio.CancelledError: raise / except Exception: logger.warning(...)` —
   NEVER re-raise a plain exception (fail-open, Test 6). Follow the surrounding
   cancellation-safety convention exactly.
3. Maintain a `progress_by_seq: dict[int, dict]` initialized from `members` as
   `{seq: {"seq": seq, "exchange": None, "label": None, "status": "waiting"}}` and:
   - write once BEFORE the loop (all waiting);
   - at the TOP of each member iteration (before `_allocator_key_preflight`), set that
     seq to `in_process` and write;
   - after `ctx` resolves, backfill `exchange = str(ctx.key_row["exchange"])` and
     `label = ctx.key_row.get("label")` — field-by-field, NEVER a key_row spread
     (Test 5 / WIZ-01);
   - where the member lands in the `degraded` list (the ccxt degrade branches), set
     `degraded` and write;
   - after the successful `clipped.append(...)` (`:3525`), set `successful` and write.
   Each write sends the FULL array sorted by seq (wholesale replace of the
   `member_progress` key — idempotent, order-stable).
4. Touch NOTHING in the series/metrics/upsert path (SC-4, Pitfall 4). The diff to
   existing lines should be limited to the `_reconstruct_all` signature, the `:3532`
   call site, and inserted progress statements.

Then GREEN: the new `-k member_progress` tests pass.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py -k member_progress -x -q</automated>
  </verify>
  <done>All 6 behavior tests green; writes fenced + fail-open + cash-pass-only; entries built field-by-field with the closed status enum; no changes to stitch math.</done>
</task>

<task type="auto">
  <name>Task 3: SC-4 byte-identity gate — full parity pin re-run</name>
  <files>analytics-service/tests/test_stitch_composite_job.py</files>
  <action>
Run the four SC-4 parity pins UNMODIFIED (any edit to these files to "make them pass"
is a phase-failing scope violation):
`test_stitch_composite_job.py` (whole file), `test_golden_parity.py`,
`test_metrics_parity.py`, `test_composite_headline_parity.py`.
If ANY parity pin fails, the worker change leaked into the series/metrics path —
fix the worker, never the pin. Confirm via `git status --porcelain` that the only
analytics-service diffs are `services/job_worker.py` and
`tests/test_stitch_composite_job.py`.
  </action>
  <verify>
    <automated>cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py tests/test_golden_parity.py tests/test_metrics_parity.py tests/test_composite_headline_parity.py -q</automated>
  </verify>
  <done>All four parity files green with zero edits to the three pure-parity files; stitched output byte-identical.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker → compute_jobs.metadata | worker-written JSONB later projected to browsers via 95-03 |
| watchdog reclaim ↔ progress write | two workers may hold the same job across a reclaim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-02 | Info Disclosure | metadata.member_progress | mitigate | Field-by-field payload construction; pytest Test 5 recursively asserts no ciphertext keys at any depth (WIZ-01) |
| T-95-03 | Tampering | progress write vs watchdog reclaim | mitigate | claim-token fence (`claim_token IS NOT DISTINCT FROM p_claim_token AND claim_token IS NOT NULL AND status='running'`); stale token no-ops; SQL gate pins it |
| T-95-04 | Elevation of Privilege | set_compute_job_progress | mitigate | SECURITY DEFINER but REVOKE from PUBLIC/anon/authenticated, GRANT service_role only; SQL gate asserts has_function_privilege false |
| T-95-05 | Denial of Service | stitch killed by progress-write failure | mitigate | fail-open try/except with logger.warning; pytest Test 6 pins stitch completion under rpc failure |
| T-95-SC | Tampering | package installs | accept | No packages installed this phase |
</threat_model>

<verification>
- `cd analytics-service && .venv/bin/python -m pytest tests/test_stitch_composite_job.py tests/test_golden_parity.py tests/test_metrics_parity.py tests/test_composite_headline_parity.py -q` green
  (add `--no-file-parallelism` on local contention flakes).
- SQL gate file present + ROLLBACK-hygienic; runs in CI sql-tests lane.
- Deploy note for SUMMARY: migration auto-applies to PROD on main merge
  (supabase-migrate-auto-on-push) and the worker change needs a GREEN-first-try main
  commit or a Railway force-deploy (red-CI-skips-Railway hazard).
</verification>

<success_criteria>
- Worker writes fenced, fail-open, cash-pass-scoped, secretless per-member progress +
  the `member_progress_at` heartbeat that 95-03's stall detector consumes.
- Existing metadata keys survive (merge semantics pinned in SQL gate).
- SC-4: all parity pins byte-identical, zero edits.
</success_criteria>

<output>
Create `.planning/phases/95-stitch-progress-transparency/95-02-SUMMARY.md` when done.
</output>
