---
phase: 94-wizard-resumability
plan: 04
type: execute
wave: 4
depends_on: ["94-03"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardChrome.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx
autonomous: true
requirements: [WIZ-04]
must_haves:
  truths:
    - "Stepper steps are clickable in both directions: past steps and completed forward steps navigate on click AND on Enter (real <button> semantics, DESIGN.md:241)"
    - "Forward jumps past an incomplete step are BLOCKED — e.g. review is not navigable before syncSnapshot/metadataDraft exist (the render guards at WizardClient.tsx:682/:707 would blank otherwise)"
    - "After going back and changing nothing, the user returns forward to any later completed step without redoing work (state persists in WizardClient; forward steps stay completed ⇒ clickable)"
    - "aria-current='step' preserved on the active step; non-navigable cells stay inert; no new visual language (existing border-accent / text-* tokens only)"
    - "Each stepper navigation persists the resume pointer (persistPointer) so a refresh resumes at the clicked step"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx"
      provides: "onStepSelect + stepNavigable props; navigable cells render as buttons"
      contains: "onStepSelect"
    - path: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      provides: "per-step completion predicate + stepNavigable + handleStepSelect wiring (API branch)"
      contains: "stepNavigable"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      to: "WizardChrome"
      via: "onStepSelect={handleStepSelect} stepNavigable={stepNavigable}"
      pattern: "onStepSelect="
---

<objective>
WIZ-04 — clickable free stepper. WizardChrome renders steps as inert <div>
cells (WizardChrome.tsx:143-170; no onClick, no button, no onStepSelect;
currentStep is a read-only prop). The step machine lives in WizardClient
(STEP_INDEX :60-78; transitions are imperative setStep + persistPointer inside
handlers). Add an onStepSelect seam to WizardChrome and a WizardClient-owned
completion predicate that allows free back-nav plus forward-nav to any
COMPLETED step, blocking forward skips past incomplete steps.

DESIGN.md conformance (hard constraint): DESIGN.md:241 already specifies
"stepper Tab/Shift+Tab in DOM order; Enter activates; aria-current='step'" —
the pills were already specified activatable, so rendering them as real
<button>s is within approved design direction. No new colors/spacing/
typography; reuse the existing border-accent active + text-text-secondary past
tokens (:146-166).

Output: keyboard-and-mouse navigable stepper wired on the API branch, with
component tests. CSV branch stays inert (out of this phase's composite-wizard
scope — WizardChrome without onStepSelect renders byte-identically).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-wizard-resumability/94-RESEARCH.md
@DESIGN.md

<interfaces>
<!-- Extracted from the codebase this session. -->

From WizardChrome.tsx:42-69 (props to extend) + :137-172 (the inert cell render):
```tsx
export interface WizardChromeProps {
  currentStep: WizardStepKey;
  savedAt: number | null;
  canDelete: boolean;
  onDeleteDraft: () => void;
  onRequestCall: () => void;
  toastKey?: number;
  steps?: { key: WizardStepKey; label: string; number: string }[];
  source?: "api" | "csv";
  children: React.ReactNode;
}
// cell: <div key={step.key} className={`border-b-2 px-3 py-3 transition-colors ${isActive ? "border-accent" : isPast ? "border-border" : "border-transparent"}`} aria-current={isActive ? "step" : undefined}>
//   inner <p> number/total (font-metric text-micro ...) + <p> label (text-caption font-medium, isActive text-text-primary / isPast text-text-secondary / else text-text-muted)
```

From WizardClient.tsx — the state the completion predicate reads:
```typescript
const STEP_INDEX: Record<WizardStepKey, 1|2|3|4|5> = { connect_key: 1, sync_preview: 2, metadata: 3, review: 4, submit: 5, csv_upload: 1, csv_preview: 2, csv_metadata: 3, csv_review: 4, csv_submit: 5 };  // :60-78
const [step, setStep] = useState<WizardStepKey>(...)            // :128
const [strategyId, ...] = useState<string | null>(...)          // :134
const [syncSnapshot, ...] = useState<SyncPreviewSnapshot | null>(null)  // :144
const [metadataDraft, ...] = useState<MetadataDraft | null>(...)         // :147
// persistPointer(nextStep: WizardStepKey, id: string | null)   // :376-393
// WizardChrome render site :562-571 (currentStep={step} ...)
// render guards: review needs strategyId && syncSnapshot && metadataDraft (:682); submit likewise (:707)
```

Completion predicate (research §WIZ-04, verified against the render guards):
- connect_key complete ⇔ strategyId != null
- sync_preview complete ⇔ syncSnapshot != null
- metadata AND review complete ⇔ metadataDraft != null
- navigable(key) = isPast(key) OR every step with ordinal < STEP_INDEX[key] is complete (blocks forward skip); the active step is not navigable (no-op)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WizardChrome — navigable step cells as real buttons</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardChrome.tsx (whole file, 237 lines)
    - DESIGN.md:236-248 (DESIGN-05 a11y minimums — Enter activates, aria-current, focus rules)
  </read_first>
  <behavior>
    - With onStepSelect + stepNavigable provided: a navigable, non-active cell renders as <button type="button"> firing onStepSelect(step.key) on click (Enter comes free with button semantics)
    - The active cell and non-navigable cells stay NON-interactive (keep the current <div>; do not add disabled buttons to the tab order)
    - aria-current="step" stays on the active cell; the border/text token classes are byte-identical per cell state
    - WITHOUT onStepSelect (e.g. the CSV branch): rendered DOM is unchanged from today (all <div>s) — back-compat by construction
  </behavior>
  <action>
    Add to WizardChromeProps: `onStepSelect?: (key: WizardStepKey) => void` and
    `stepNavigable?: (key: WizardStepKey) => boolean`. In the cell map
    (:138-171) compute `const navigable = !isActive && !!onStepSelect &&
    (stepNavigable?.(step.key) ?? false)`. When navigable, render the cell as
    `<button type="button" onClick={() => onStepSelect(step.key)}
    data-testid={\`wizard-step-\${step.key}\`} ...>` carrying the SAME
    className ladder (add `w-full text-left cursor-pointer` plus the app's
    standard focus-visible treatment used elsewhere in this file — see the
    Request-a-Call button :225-232 for the focus-visible idiom) and the same
    two inner <p> elements; otherwise keep the existing <div> byte-identical.
    Keep aria-current on the active cell. NO new colors/spacing/typography
    tokens (DESIGN.md hard constraint — reuse border-accent /
    text-text-secondary / text-text-muted exactly as :146-166).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardChrome.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - Both new props optional; omitting them renders the exact pre-change DOM (existing WizardChrome tests green unmodified)
    - Navigable cells are <button type="button"> with data-testid="wizard-step-{key}"; active + non-navigable cells are not buttons
    - No hex/color/space literal added anywhere in the diff
  </acceptance_criteria>
  <done>Stepper cells activatable per DESIGN-05, inert by default.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WizardClient — completion predicate + step-select wiring (API branch)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:60-78 (STEP_INDEX), :128-155 (state), :376-393 (persistPointer), :560-571 (WizardChrome render site), :682 + :707 (render guards the predicate must protect)
  </read_first>
  <behavior>
    - stepNavigable("metadata") is false until syncSnapshot exists even if the user is on connect_key with a strategyId (forward-skip block)
    - From review, stepNavigable("connect_key") and ("sync_preview") and ("metadata") are all true (free back-nav)
    - After back-nav to connect_key with all state intact, stepNavigable("review") is true (return forward without redoing work — syncSnapshot/metadataDraft persisted in WizardClient state)
    - handleStepSelect(key): setStep(key) + persistPointer(key, strategyId) — nothing else (no state clearing, no session regen)
  </behavior>
  <action>
    In WizardClient, API branch only (source === "api"):

    1. `const stepCompleted = (key: WizardStepKey): boolean` → connect_key:
       strategyId != null; sync_preview: syncSnapshot != null; metadata:
       metadataDraft != null; review: metadataDraft != null; submit: false
       (never pre-completed). CSV keys: false.
    2. `const stepNavigable = (key: WizardStepKey): boolean` → false when key
       === step; true when STEP_INDEX[key] < STEP_INDEX[step] (backward always
       allowed); for forward keys: every s in DEFAULT_STEPS order with
       STEP_INDEX[s] < STEP_INDEX[key] must be stepCompleted(s). This protects
       the render guards at :682/:707 (research Pitfall 4 — a forward skip to
       review before syncSnapshot/metadataDraft renders a guard-failed blank).
    3. `const handleStepSelect = (key: WizardStepKey) => { setStep(key);
       persistPointer(key, strategyId); }`.
    4. Pass `onStepSelect={source === "api" ? handleStepSelect : undefined}`
       and `stepNavigable={source === "api" ? stepNavigable : undefined}` at
       the WizardChrome render site (:562). CSV branch deliberately stays
       inert this phase (WIZ-04 scope is the composite wizard; WizardChrome's
       optional props make CSV byte-neutral) — note this in a comment.

    Memoization per the file's conventions (useCallback with the state deps)
    to satisfy react-hooks lint. Touch nothing else — the imperative
    setStep/persistPointer handlers on the step components stay as-is.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - stepNavigable blocks forward jumps past incomplete steps (test-pinned with the exact matrix from behavior)
    - handleStepSelect body contains only setStep + persistPointer
    - CSV branch passes undefined for both props (grep the render site)
    - Existing WizardClient tests green unmodified
  </acceptance_criteria>
  <done>Free bidirectional navigation with forward-skip protection, resume pointer persisted per click.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Stepper component tests (both files)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/WizardChrome.test.tsx, src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/WizardChrome.test.tsx (existing harness)
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.test.tsx (harness: how to drive the wizard to later steps)
  </read_first>
  <behavior>
    - WizardChrome: navigable cells render as buttons and fire onStepSelect with the step key on click AND on keyboard Enter activation (userEvent keyboard on the focused button); non-navigable future cells and the active cell are not buttons; aria-current="step" present on active; omitting onStepSelect renders zero buttons in the rail
    - WizardClient: at connect_key with no strategyId, no forward step is navigable; after reaching review (all state present), clicking back to sync_preview then forward to review works with NO handler re-runs — assert zero new /api/keys/sync POSTs and zero add-key/set-members POSTs during the back-and-forward round-trip (the WIZ-05 cachedSnapshot short-circuit from 94-03 is what makes the sync_preview revisit inert; this test also guards that integration)
  </behavior>
  <action>
    Extend both test files with a "clickable stepper (WIZ-04)" describe each,
    reusing existing harness idioms. The WizardClient round-trip test must
    inventory fetch calls before/after the stepper round-trip and assert the
    delta is empty. Verify RED: neuter stepNavigable to always-true locally →
    the forward-skip-block test fails; revert.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard" && npm run lint -- "src/app/(dashboard)/strategies/new/wizard" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - An Enter-activation assertion exists (DESIGN.md:241 conformance pinned in a test, not just claimed)
    - A forward-skip-block assertion exists and goes RED under an always-true stepNavigable mutation
    - A no-refetch round-trip assertion exists (fetch-call inventory delta empty)
    - Full wizard directory vitest green; tsc + lint clean
  </acceptance_criteria>
  <done>WIZ-04 pinned: clickable both ways, Enter-activatable, forward-skip blocked, no redone work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user navigation → step render guards | free navigation must not reach a step whose data prerequisites are absent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-15 | DoS (client blank-state) | forward navigation | mitigate | stepNavigable blocks forward jumps past incomplete steps (render guards :682/:707 never hit with missing deps); test-pinned RED under always-true mutation |
| T-94-16 | Tampering (state loss) | handleStepSelect | mitigate | handler is setStep + persistPointer only — never clears strategyId/syncSnapshot/metadataDraft or regenerates the session (acceptance-grepped) |
| T-94-17 | Information Disclosure | stepper UI | accept | step labels/ordinals only; no data crosses this surface |
| T-94-18 | Tampering (supply chain) | dependencies | accept | zero new packages |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard"` green
- `npx tsc --noEmit` clean; `npm run lint` 0 new errors
- No DESIGN.md deviation: diff contains no new color/spacing/typography literals; Enter-activation test green (DESIGN.md:241)
</verification>

<success_criteria>
WIZ-04 satisfied: stepper clickable in both directions with keyboard Enter
activation, forward-skip past incomplete steps blocked, back-then-forward
returns without redoing work, aria-current preserved, zero visual-language
changes, CSV branch byte-neutral.
</success_criteria>

<output>
Create `.planning/phases/94-wizard-resumability/94-04-SUMMARY.md` when done
</output>
