---
phase: 94-wizard-resumability
plan: 05
type: execute
wave: 4
depends_on: ["94-01", "94-02", "94-03"]
files_modified:
  - e2e/helpers/seed-test-project.ts
  - e2e/composite-onboarding.spec.ts
autonomous: true
requirements: [WIZ-03, WIZ-05]
must_haves:
  truths:
    - "An OWNER-seeded (Ph91 lesson) resumable composite draft round-trips through the REAL app: resume at sync_preview → 'Review your keys' → rehydrated panels via the REAL WIZ-01 GET (RLS browser read resolves because owner matches) → Continue via the REAL set-members → back at sync_preview with the snapshot"
    - "Zero POST /api/keys/sync calls occur across the entire walk (WIZ-05: a COMPLETE composite never re-kicks) and zero DELETE /api/strategies/draft/* calls occur (WIZ-03: review is non-destructive)"
    - "The spec is seed-gated in BOTH places: its own HAS_SEED_ENV self-skip AND the ci.yml e2e-seeded list — composite-onboarding.spec.ts is ALREADY in ci.yml:1452, so extending the existing spec keeps both wires intact with no ci.yml edit"
  artifacts:
    - path: "e2e/helpers/seed-test-project.ts"
      provides: "resumable variant of seedCompositeStrategy (status draft, source wizard, COMPLETE analytics, owner-matched)"
      contains: "resumable"
    - path: "e2e/composite-onboarding.spec.ts"
      provides: "WIZ-03 non-destructive round-trip + WIZ-05 no-re-kick e2e"
      contains: "Review your keys"
  key_links:
    - from: "e2e/composite-onboarding.spec.ts"
      to: "seedCompositeStrategy"
      via: "ownerUserId = the logged-in allocator's user id (Ph91: owner-matched or RLS reads return empty and the test false-REDs)"
      pattern: "ownerUserId"
---

<objective>
The ONE true-e2e need (resolved decision 2): prove the WIZ-03 non-destructive
round-trip and the WIZ-05 back-to-crawled snapshot against the REAL routes +
REAL RLS browser reads. Bulk coverage is already vitest (plans 01-04); this
plan corroborates the two behaviors that need a real unmount/remount navigation
+ RLS resolution.

Why this is low-risk to wire: `e2e/composite-onboarding.spec.ts` ALREADY exists,
is ALREADY seed-gated (HAS_SEED_ENV self-skip, place 1) and ALREADY listed in
ci.yml:1452 (place 2 — the Ph91 two-place rule is satisfied by extension, no
ci.yml edit needed). `seedCompositeStrategy` ALREADY supports `ownerUserId`
(seed-test-project.ts:761-806) — the owner-matched seam exists. What's missing
is a "resumable" variant (the current variants are `published`, which the
wizard's draft-resume query skips, and `failed`, which deliberately uses
source='legacy' to stay OUT of resume).

Owner-matching is what makes this REAL: the existing spec stubs set-members
because its seed is owned by a different user (spec header, "the real route
403s here"). With ownerUserId = the logged-in allocator, the WIZ-01 GET, the
set-members POST, and the wizard's RLS browser reads all resolve unstubbed.

Fallback (resolved decision 2): if the seeded walk proves unreachable in the
executor's environment, mark the new test block advisory (test.fixme with a
reason) + confirm the same behaviors are component-covered (they are, plans
02/03), and NOTE the gap loudly in the summary — fail-loud, no silent skip.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-wizard-resumability/94-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase this session. -->

From e2e/helpers/seed-test-project.ts:761-806 (owner-matched seam ALREADY exists):
```typescript
export async function seedCompositeStrategy(opts?: {
  name?: string;
  variant?: "published" | "failed";
  mtm?: "available" | "gated";
  ownerUserId?: string;   // when supplied: reuse (the logged-in allocator) — RLS-bound wizard reads resolve
}): Promise<{ strategyId: string; ownerUserId: string; memberApiKeyIds: string[] }>
// strategies insert: variant "failed" → status "draft" + source "legacy" (deliberately OUT of resume);
// "published" → status "published" + source "wizard".
// Wizard draft-resume query (strategies/new/wizard/page.tsx:85-86): .eq("source","wizard").eq("status","draft")
// Seeds 3 api_keys (deribit, placeholder ciphertext) + 3 strategy_keys windows + sparse csv_daily_returns
// + a strategy_analytics row.
```

From e2e/composite-onboarding.spec.ts (header + :55-60): HAS_SEED_ENV =
TEST_SUPABASE_URL && TEST_SUPABASE_SERVICE_ROLE_KEY; inline loginViaForm copied
from wizard-axe.spec.ts (intentional duplication per PATTERNS); uses
seedTestAllocator + seedCompositeStrategy + cleanupStrategiesByNamePrefix; the
existing walk stubs add-key / set-members / keys-sync via page.route because
its seed is NON-owner. ci.yml:1452 lists this spec in the e2e-seeded batch.

Behavior chain under test (landed in plans 01-03):
- Resume: initialDraft found (source wizard + status draft, owned) → WizardClient inits step "sync_preview" (WizardClient.tsx:128-132)
- WIZ-05 durability: strategy_analytics COMPLETE + data_quality_flags.composite === true → mount effect SKIPS the /api/keys/sync kickoff regardless of computed_at age; poll materializes the snapshot from seeded rows
- WIZ-03: "Review your keys" (data-testid="wizard-try-another-key", composite label) → onReviewKeys → connect_key, no DELETE, session kept
- WIZ-02: MultiKeyConnectStep rehydrates via GET /api/strategies/composite/members (REAL, owner-matched) → 3 validated panels
- Secretless resubmit: Continue → REAL POST set-members (owner-matched, succeeds) → sync_preview again, still no /api/keys/sync
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: seedCompositeStrategy "resumable" variant</name>
  <files>e2e/helpers/seed-test-project.ts</files>
  <read_first>
    - e2e/helpers/seed-test-project.ts:740-950 (seedCompositeStrategy in full: owner handling, strategies insert, api_keys/strategy_keys/csv_daily_returns/strategy_analytics seeding)
    - src/app/(dashboard)/strategies/new/wizard/page.tsx (the draft-resume query the variant must match: source='wizard' AND status='draft')
  </read_first>
  <action>
    Extend the `variant` union with `"resumable"`: strategies row gets
    `status: "draft"` AND `source: "wizard"` (so the wizard page's
    `.eq("source","wizard").eq("status","draft")` resume query hydrates
    initialDraft — the exact opposite intent of the "failed" variant's
    source:'legacy' escape hatch, :828-837), keep everything else identical to
    the published variant: 3 owner-matched api_keys + 3 strategy_keys windows
    + sparse csv_daily_returns + a COMPLETE strategy_analytics row whose
    `data_quality_flags` includes `composite: true` and whose `computed_at` is
    deliberately OLDER than 5 minutes (the seeded fixed date already is) — the
    WIZ-05 durability skip, not the freshness skip, is what must carry the
    walk. Callers pass `ownerUserId` (the variant itself stays owner-agnostic;
    the spec supplies the logged-in allocator's id). Keep the
    `e2e-composite-` name prefix so cleanupStrategiesByNamePrefix collects it.
    Preserve every existing variant byte-identically.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx playwright test e2e/composite-onboarding.spec.ts --list</automated>
  </verify>
  <acceptance_criteria>
    - variant union is "published" | "failed" | "resumable"; the resumable strategies insert carries status "draft" AND source "wizard"
    - data_quality_flags on the seeded analytics row contains composite: true; computation_status is a COMPLETE value
    - Existing variants' insert literals unchanged (git diff scoped to the new branch)
  </acceptance_criteria>
  <done>Owner-matchable resumable composite draft fixture exists.</done>
</task>

<task type="auto">
  <name>Task 2: WIZ-03 round-trip + WIZ-05 no-re-kick e2e block</name>
  <files>e2e/composite-onboarding.spec.ts</files>
  <read_first>
    - e2e/composite-onboarding.spec.ts (whole file — seed-gate, loginViaForm, page.route stub idioms, cleanup discipline)
    - e2e/composite-factsheet-render.spec.ts:50-140 (the HAS_SEED_ENV two-place-rule commentary — this spec extension inherits place 2 via ci.yml:1452, note it in the block header)
  </read_first>
  <action>
    Add a new seed-gated `test.describe("wizard resumability (Phase 94)")` to
    the EXISTING spec (no new spec file — a new file would need a new ci.yml
    entry; extending inherits ci.yml:1452). Flow:

    1. Seed: `const allocator = await seedTestAllocator();` then
       `seedCompositeStrategy({ variant: "resumable", ownerUserId: <the
       allocator's user id from the seed helper's return> })` — OWNER-MATCHED
       (Ph91: a non-owner seed makes the wizard's RLS browser reads return
       empty and the poller loops forever / panels come back blank — the
       false-RED failure mode this plan exists to avoid). Cleanup in
       afterAll via cleanupStrategiesByNamePrefix as the file already does.
    2. Route spies (NOT stubs — the whole point is REAL routes now that the
       owner matches): `page.route` passthrough counters on
       `**/api/keys/sync` and `**/api/strategies/draft/**` recording
       method+URL; do NOT stub add-key / set-members / members.
    3. loginViaForm as the allocator → goto /strategies/new/wizard → the
       resume path lands on sync_preview → expect the composite snapshot UI
       (the "Use this composite and continue" button, exact copy from
       SyncPreviewStep.tsx:1392) to become visible — materialized from the
       SEEDED rows with ZERO /api/keys/sync calls (WIZ-05 durability: the
       seeded computed_at is stale, only the COMPLETE-composite skip explains
       a zero count).
    4. Click "Review your keys" (data-testid="wizard-try-another-key") →
       expect the connect-key step with 3 REHYDRATED validated panels (assert
       on the seeded member labels rendered) — this exercises the REAL WIZ-01
       GET through browser RLS.
    5. Assert the DELETE spy count is 0 (non-destructive) and, via a direct
       seed-helper admin read, that the 3 strategy_keys rows still exist.
    6. Click Continue → REAL set-members (owner-matched → succeeds; empty
       secret fields — the true-e2e secretless-resubmit proof) → back at
       sync_preview showing the snapshot; final assertion: /api/keys/sync
       spy count is STILL 0 across the whole walk.

    FALLBACK (resolved decision 2, fail-loud): if the walk cannot be made
    deterministic in this environment, convert the block to test.fixme with a
    one-line reason, verify the same assertions exist in the plan 02/03
    component tests, and put a prominent "e2e advisory gap" note in the
    summary. NEVER let it silently self-skip beyond the standard HAS_SEED_ENV
    gate.
  </action>
  <verify>
    <automated>npx playwright test e2e/composite-onboarding.spec.ts --list && npx tsc --noEmit && npm run lint -- e2e</automated>
  </verify>
  <acceptance_criteria>
    - The new describe uses variant "resumable" with ownerUserId = the logged-in allocator (grep: ownerUserId in the new block)
    - add-key / set-members / members are NOT page.route-stubbed inside the new block (real routes)
    - Assertions include: /api/keys/sync call count === 0 (twice: after resume, after round-trip), DELETE draft call count === 0, 3 rehydrated panels visible, strategy_keys rows survive
    - No ci.yml change (the spec is already listed at ci.yml:1452); the spec's HAS_SEED_ENV gate covers the new block
    - If the fallback fired: test.fixme with reason + summary gap note (no silent skip)
  </acceptance_criteria>
  <done>Owner-seeded true-e2e corroboration of WIZ-03 + WIZ-05 (or a loudly-documented advisory fallback).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| e2e seed helper → test DB | admin-key seeding; placeholder ciphertext only |
| spec assertions → real routes | must not weaken the routes' postures to pass |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-19 | Information Disclosure | seeded api_keys rows | mitigate | reuse the existing "e2e-placeholder-ciphertext" idiom (seed-test-project.ts:858) — no real credential ever seeded; gitleaks-safe by precedent |
| T-94-20 | Spoofing (test-validity) | owner mismatch | mitigate | ownerUserId = logged-in allocator (Ph91 lesson) — the RLS reads under test are the REAL tenant gate, not stubs |
| T-94-21 | Tampering (supply chain) | dependencies | accept | zero new packages |
</threat_model>

<verification>
- `npx playwright test e2e/composite-onboarding.spec.ts` green locally when TEST_SUPABASE_* env present (or the block lists + self-skips without it)
- `npx tsc --noEmit` clean; `npm run lint` 0 new errors
- ci.yml untouched (`git diff --quiet .github/workflows/ci.yml`)
</verification>

<success_criteria>
The owner-seeded e2e proves against real routes + real RLS: leave → return →
review keeps the draft and members intact (WIZ-03), rehydrated panels render
(WIZ-02 corroboration), and a completed crawl never re-kicks (WIZ-05, zero
/api/keys/sync calls). Wired in both HAS_SEED_ENV and ci.yml by construction —
or the gap is loudly documented, never silently skipped.
</success_criteria>

<output>
Create `.planning/phases/94-wizard-resumability/94-05-SUMMARY.md` when done
</output>
