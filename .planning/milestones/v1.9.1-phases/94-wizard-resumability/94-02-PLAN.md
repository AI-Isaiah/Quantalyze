---
phase: 94-wizard-resumability
plan: 02
type: execute
wave: 2
depends_on: ["94-01"]
files_modified:
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx
  - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx
  - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx
autonomous: true
requirements: [WIZ-02]
must_haves:
  truths:
    - "Re-entering MultiKeyConnectStep with an existing composite draft rehydrates State B: stored keys appear pre-filled and marked verified, no re-validation fires, and back-nav never shows a blank form"
    - "An unchanged key is re-submittable with an EMPTY secret field — Continue POSTs set-members with only api_key_id/window/seq (secretless by construction), no add-key call"
    - "A single-key or empty-membership draft stays on the byte-neutral single-key State A path"
    - "Rehydrated panels carry EMPTY plaintext fields (apiKey/apiSecret/passphrase: '') — no secret ever enters browser state on rehydrate"
  artifacts:
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx"
      provides: "draftStrategyId prop + mount rehydration effect from the WIZ-01 GET"
      contains: "draftStrategyId"
    - path: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx"
      provides: "rehydration component tests (mode flip, validated panels, secretless Continue, no re-validate)"
  key_links:
    - from: "src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx"
      to: "MultiKeyConnectStep"
      via: "draftStrategyId={strategyId} at the step === 'connect_key' render site"
      pattern: "draftStrategyId"
    - from: "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx"
      to: "/api/strategies/composite/members"
      via: "mount useEffect fetch"
      pattern: "composite/members\\?strategy_id="
---

<objective>
WIZ-02 — State B rehydration. MultiKeyConnectStep holds all key state in
ephemeral local React state (`panels`, `mode`, `strategyId` — MultiKeyConnectStep.tsx:294-296)
that is never rehydrated and is not even handed the draft's strategyId
(props are only wizardSessionId + onSuccess, :285-288; render site WizardClient.tsx:635-638).
Any unmount re-mounts a blank single-key form. Thread the draft's strategyId in
and add a mount effect that rehydrates panels from the WIZ-01 GET.

The server contract needs ZERO change: `buildSetMembersKeys` (:180-198) already
sends only { api_key_id, window_start, window_end, seq } — never a secret — and
`set_wizard_composite_members` is a wholesale idempotent DELETE-then-INSERT, so
an unchanged verified key resubmits with an empty secret field by construction.

Purpose: navigating back never shows a blank form (dogfooding defect C/D).
Output: rehydrating MultiKeyConnectStep + component tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/94-wizard-resumability/94-RESEARCH.md

<interfaces>
<!-- Extracted from the codebase this session. -->

From steps/MultiKeyConnectStep.tsx:135-150 (the panel shape to rehydrate into):
```typescript
interface PanelState {
  id: string;              // genId()
  exchange: ExchangeId;
  nickname: string;
  apiKey: string;          // plaintext — MUST be "" on rehydrate
  apiSecret: string;       // MUST be ""
  passphrase: string;      // MUST be ""
  showSecret: boolean;     // false
  windowStart: string;     // "YYYY-MM-DD"
  windowEnd: string;       // "" when open-ended
  stillLive: boolean;      // true ⇔ server window_end === null
  status: "editing" | "validating" | "validated";  // "validated" on rehydrate
  apiKeyId: string | null; // the server api_key_id
  errorCode: WizardErrorCode | null;  // null
  confirmingRemove: boolean;          // false
}
```
A post-validate panel (the shape to mirror, :448-464) is exactly:
status "validated", apiKeyId set, errorCode null, confirmingRemove false,
apiKey/apiSecret/passphrase all "" (T-88-18 clears plaintext on validate).

From :285-296 (current props + state):
```typescript
export interface MultiKeyConnectStepProps {
  wizardSessionId: string;
  onSuccess: (result: ConnectKeySuccess) => void;
}
// local state: mode "single"|"multi" (init "single"), panels [] , strategyId null
```

Gating predicates (:482-487) — a rehydrated panel satisfies them, so NO
re-validation fires and Continue enables:
```typescript
const allValidated = panels.length > 0 && panels.every((p) => p.status === "validated" && !!p.windowStart);
const canContinue = allValidated && !hasBlockingError && !continuing;
```

Continue payload (:526-558): `buildSetMembersKeys(current)` →
`POST /api/strategies/composite/set-members { strategy_id, keys }` where keys =
`[{ api_key_id, window_start, window_end, seq }]` — NO secret fields exist in
the payload type. Requires local `strategyId` set (`if (continuing || !strategyId) return;`).

From WizardClient.tsx:134-136 + 635-638 (the value to thread + the render site):
```tsx
const [strategyId, setStrategyId] = useState<string | null>(initialDraft?.id ?? null);
...
{step === "connect_key" && (
  <MultiKeyConnectStep wizardSessionId={wizardSessionId} onSuccess={handleConnectSuccess} />
)}
```

WIZ-01 GET response (built in 94-01):
```json
{ "ok": true, "members": [{ "seq": 1, "api_key_id": "<uuid>", "exchange": "deribit",
  "nickname": "label-or-null", "window_start": "2025-08-03",
  "window_end": "2025-09-27 | null", "verified": true }] }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thread draftStrategyId + mount rehydration effect</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx, src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.tsx:135-199 (PanelState + newPanel + buildSetMembersKeys), :285-340 (props, state, existing effects), :420-490 (validatePanel success shape + gating predicates)
    - src/app/(dashboard)/strategies/new/wizard/WizardClient.tsx:628-640 (render site)
  </read_first>
  <behavior>
    - Mount with draftStrategyId + GET returns 2 members → setStrategyId(draftStrategyId), setMode("multi"), 2 panels in validated state with empty plaintext
    - Mount with draftStrategyId + GET returns { members: [] } → stays single-key State A (byte-neutral — single-key drafts have no strategy_keys rows)
    - Mount with no draftStrategyId → no fetch, byte-neutral State A
    - GET non-ok / network error → console.error, stays State A (status quo ante: the pre-phase behavior was a blank form; degrading to it is honest, and the error is logged — no silent secret-touching path exists here)
    - Rehydration NEVER clobbers in-progress work: it applies only when mode === "single" AND panels.length === 0 (checked via panelsRef at resolve time)
  </behavior>
  <action>
    In `MultiKeyConnectStep.tsx` (per resolved WIZ-02 design, research §WIZ-02):

    1. Add `draftStrategyId?: string | null` to `MultiKeyConnectStepProps`.
       (Named draftStrategyId — NOT strategyId — to avoid shadowing the local
       `const [strategyId, setStrategyId]` state at :296.)
    2. Add a mount `useEffect` with deps `[draftStrategyId]` and a `cancelled`
       cleanup flag: if no draftStrategyId, return. Otherwise
       `fetch(\`/api/strategies/composite/members?strategy_id=\${draftStrategyId}\`)`.
       On `res.ok` with `Array.isArray(members) && members.length > 0` and not
       cancelled and panelsRef.current.length === 0:
       `setStrategyId(draftStrategyId); setMode("multi");
       setPanels(members.map(toRehydratedPanel))`.
    3. `toRehydratedPanel(member)` maps EXACTLY like a post-validate panel
       (mirror :448-464): `{ id: genId(), exchange: member.exchange as
       ExchangeId, nickname: member.nickname ?? "", apiKey: "", apiSecret: "",
       passphrase: "", showSecret: false, windowStart: member.window_start,
       windowEnd: member.window_end ?? "", stillLive: member.window_end ==
       null, status: "validated", apiKeyId: member.api_key_id, errorCode:
       null, confirmingRemove: false }`.
    4. Do NOT touch validatePanel, buildSetMembersKeys, handleContinue,
       computeValidation, or the State A ConnectKeyStep delegation — the
       gating predicates already accept a rehydrated panel (allValidated
       checks status === "validated" && windowStart, both satisfied) so no
       re-validation can fire and the empty-secret resubmit works by
       construction (set-members takes only api_key_id — hard constraint:
       do NOT require re-entering the secret for an unchanged key).

    In `WizardClient.tsx`: at the step === "connect_key" render site (:635-638)
    add `draftStrategyId={strategyId}` — one line; nothing else in WizardClient
    changes in this plan.

    Partial-membership honesty (research Risk 2): if a member's api_key was
    deleted, the FK cascade removed the row and the GET simply returns fewer
    members — rehydrate what exists; the existing computeValidation +
    set-members server re-validation surface any invariant breach. No special
    casing.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx" "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.payload.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - MultiKeyConnectStepProps gains optional `draftStrategyId`; WizardClient passes `draftStrategyId={strategyId}` at exactly one site
    - The rehydrated panel literal sets apiKey: "", apiSecret: "", passphrase: "" (grep the effect body: no member field maps into those three)
    - No change to buildSetMembersKeys, validatePanel, handleContinue, or the State A branch (git diff shows the effect + props + one WizardClient line only)
    - Existing MultiKeyConnectStep tests stay green unmodified (byte-neutral A1 path)
  </acceptance_criteria>
  <done>Draft strategyId threaded; mount rehydrate effect lands; A1 path byte-neutral.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rehydration component tests (fails without the fix)</name>
  <files>src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx</files>
  <read_first>
    - src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx (existing harness: fetch mocking idiom, render helpers, panel assertions)
  </read_first>
  <behavior>
    - Render with draftStrategyId + fetch mock answering GET .../composite/members?strategy_id=... with 2 members (one live window_end:null, one bounded) → asserts: mode is multi (State B section renders, single-key ConnectKeyStep absent), 2 panels rendered in validated state, NO POST to /api/strategies/composite/add-key ever fired (no re-validation)
    - Continue is enabled with empty secret fields; clicking it POSTs set-members whose body carries strategy_id = draftStrategyId and keys = the two api_key_ids with window_start/window_end/seq — and JSON.stringify(body) contains NO apiKey/apiSecret/passphrase material
    - Render with draftStrategyId + GET → { ok: true, members: [] } → single-key State A renders (ghost "+ Add another key window" affordance present)
    - Render WITHOUT draftStrategyId → no fetch to composite/members at all
  </behavior>
  <action>
    Extend MultiKeyConnectStep.test.tsx with a "rehydration (WIZ-02)" describe
    using the file's existing fetch-mock idiom. Assert the negative space
    explicitly: collect all fetch calls and assert none targets add-key
    (re-validation) on the rehydrated path, and that the set-members body
    string contains neither "apiSecret" nor any planted secret value. Verify
    RED-ness: stash Task 1's effect (comment it out locally) → the mode-flip
    and no-blank-form tests fail; restore.
  </action>
  <verify>
    <automated>npx vitest run "src/app/(dashboard)/strategies/new/wizard/steps/MultiKeyConnectStep.test.tsx" && npm run lint -- "src/app/(dashboard)/strategies/new/wizard"</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert the EXACT set-members body keys: api_key_id, window_start, window_end, seq (value-pinned for member 1)
    - A no-add-key-fetch assertion exists and fails if the effect re-validates
    - RED-proof documented in the summary (tests fail with the effect neutered)
  </acceptance_criteria>
  <done>WIZ-02 behavior pinned offline; empty-secret resubmit proven at the component seam.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → browser (GET members) | only non-secret metadata may enter client state |
| browser state → set-members POST | payload must stay secretless |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-06 | Information Disclosure | rehydrated PanelState | mitigate | toRehydratedPanel hardcodes apiKey/apiSecret/passphrase to "" (mirrors T-88-18 post-validate clearing); acceptance grep + test pin |
| T-94-07 | Information Disclosure (logs) | rehydrate effect catch | mitigate | console.error logs the error object only, never panel/member fields (T-88-18 posture, :470-471 precedent) |
| T-94-08 | Spoofing/IDOR | GET fetch with draftStrategyId | mitigate | server-side: 94-01's withAuth + ownership guard + RLS; client sends only the id it already holds |
| T-94-09 | Tampering (supply chain) | dependencies | accept | zero new packages |
</threat_model>

<verification>
- `npx vitest run "src/app/(dashboard)/strategies/new/wizard"` green (whole wizard suite)
- `npx tsc --noEmit` clean; `npm run lint` 0 new errors
- Existing WizardClient.test.tsx green unmodified (the one-prop addition is render-inert for single-key)
</verification>

<success_criteria>
WIZ-02 satisfied: back-nav to connect_key rehydrates stored keys pre-filled and
verified, no re-validation, empty secret acceptable for an unchanged key,
never a blank form. Single-key State A byte-neutral.
</success_criteria>

<output>
Create `.planning/phases/94-wizard-resumability/94-02-SUMMARY.md` when done
</output>
